# Build artifacts (Makefile outputs — binaries, no extension)
evaluator/rule110
tests/test_rule110
tests/test_cyclic_tag
tests/test_encoder
tests/test_mark

# Object files (we don't currently produce any, but just in case)
*.o

# macOS noise
.DS_Store
