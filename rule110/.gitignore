# Build artifacts (Makefile outputs — binaries, no extension)
evaluator/rule110
encoder/generate_r110
encoder/generate_r110_algo
tests/test_*
!tests/test_*.c
!tests/test_*.h

# Object files
*.o

# macOS noise
.DS_Store


# Local research dump (papers, not committed)
ignores/

# Generated Rule 110 manifests — encoder outputs, not source.
# Source manifests (.algo.ct / .enum.ct) stay tracked.
manifests/**/*.r110.ct
