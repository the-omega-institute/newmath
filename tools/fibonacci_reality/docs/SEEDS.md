# fibonacci_reality — 研究 backlog / 起步锚点

源: automath(`the-omega-institute/automath`,Lean 0-axiom forced 证书) + Loning 的"哥本哈根之路"BEDC 对话(`docs/source_bedc_conversation.txt`,28 turns,NyxID 全文捕获)。

## 关键定调(对话自带纪律,非外部强加)

- `α_EM⁻¹` = **黄金-Fibonacci 相位几何在可见端的投影读数(projection signature)**,不是基本常数。契合 BEDC"从有限记录/编码/投影/残差账本出发"的灵魂。
- **必须写进 BEDC visions,不是 mature theorem/closed NameCert**(对话原话)。作 vision-level NameCert obligation inventory,**绝不写成 `=α`**。
- **反 numerology 的硬边界(对话自带)**: 有限 Fibonacci+φ 代数落在 ℚ(√5),π 是超越数→**不可能精确生成 π**。所以 π/cos 不当裸解析对象,改写成 **Fibonacci-indexed phase-circle projection certificate**(有限索引/窗口/权重全 Zeckendorf,π/cos 作极限投影)。
- **φ = Zeckendorf/Fibonacci encoder 的 Perron-Frobenius 自尺度**(φ=1+1/φ 自指闭包,lim F_{n+1}/F_n),非外部常数→接 BEDC 自指主题。
- `47 = F_7+F_9 = 13+34`(Zeckendorf 表示,非连续 Fibonacci)。

## 第一自驱任务(gate 适配,优先)

引擎 fork 自 bio_reality,其 gate/schema 仍是生物味(`biological_object`、`reality_contact_refs`=生物数据)。**fibonacci 域的 "reality contact" = automath Lean 证书,不是生物数据集。** 第一任务: 派 codex 适配 `deepening_gates.py`/`conjecture.schema.json`/相关 gate,让"reality contact"能接 **automath Lean finite certificate**(类比 bio 的 `\leanchecked`:NameCert 指向的 Lean 对象必须在 automath 真存在)。适配前 seed 的数学 conjecture 会被 gate_blocked(域不匹配)——这是预期信号,不是 bug。

## Track B 锚点(向上→BEDC,principled,对话直接点名)

- **F-B1 `ZeckendorfNormal↑`**: Fibonacci/Zeckendorf normal-form 账本 + finite-prefix projection tower。**最 principled 核心**(Zeckendorf 定理=唯一非连续 Fibonacci 表示=automath 无连续-1 golden-mean 核)。reality contact = Zeckendorf 唯一性 + automath `BinFold`/golden-mean shift Lean 证书。
- **F-B2 phase-circle projection certificate**: 把 π/cos 重构成 Fibonacci-indexed phase circle 的极限投影(ℚ(√5) vs 超越数边界证书)。这是反 numerology 的纪律对象。
- **F-B3 `GoldenFibonacciPhaseCascade↑`(vision obligation inventory)**: 顶层 vision——α⁻¹ 作为黄金-Fibonacci 相位几何投影的**义务清单**(连物理常数+空间结构,不闭合)。新开 BEDC sibling vision chapter,**不擅动 papers/bedc 现有章节**,走显式 bridge,经 Loning 协调/用户授权。
- **F-B4(gated, needs_certificate)**: α-readout `D_2 = 47+φ⁻⁷-(1/2)φ⁻¹⁷` 及 `q_3·φ⁻³⁷` 下一阶。状态 `needs_certificate`/`numerical_tuning_risk`——先要 automath formal def + 过对抗审计(Null95/λDL/独立证书),**不当已证,不进 mature NameCert**。
- **F-B5 Window6/571(automath 已证,principled)**: edge-flux skeleton(Φ_R=1/12,P(leave U_R)=8/9,384=6·2⁶ native_decide)+ 571 Green/Kirchhoff(τ=123336=2³·3³·571,mod-571 spectral collision)→ NameCert,指向 automath 真 Lean 证书。

## Track A 锚点(向下→生物,charter 安全,不搬常数)

- **F-A1 codon 边界响应塌缩**: `R_g = base_g + ε_g·Q_g(ρ_g) + U_g`,`Q_g(ρ)=c₁ρ+c₂ρ²`。ε/ρ **从生物数据估**(ribosome pause-return/A-P-site transition persistence/ramp-stop 边界泄漏,数据复用 `data/`),系数绑机制(wobble seam/tRNA 相位/核糖体逃逸)+ 独立证书。门 λDL+Null95+C_H+独立证书+负面显式。**不预设 φ⁻¹⁰/571/Z₆**。status 算出→passed(正负都真),数据不足→needs_data。
- **F-A2(gated 假设)**: codon 序列上是否存在"无连续-X 稳定约束"给出 Fibonacci-like 计数(Zeckendorf↔genetic code 的可证伪桥)。只作假设跑,不假设成立。

## 红线(charter §2 复述)

principled-vs-numerology gate;独立证书 vs 数值微调;α 拟合挡审计门外、不进 BEDC mature;φ⁻¹⁰/571/Z₆/1-12/8-9 常数不跨域硬搬进生物;0-axiom 纯度;BEDC 写入走显式 bridge 经 Loning 协调;禁编造/over-claim、负面显式、needs_data。
