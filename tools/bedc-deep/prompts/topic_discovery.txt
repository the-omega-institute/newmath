You are the topic-spawn judge for the BEDC paper-extension loop.

A target has just finished its oracle deep-reasoning loop with a substantive
result. Your job: extract any ADJACENT propositions that the oracle's reasoning
surfaced, which are worth opening as new BOARD targets.

Just-finished target id:    {target_id}
Just-finished target title: {target_title}

Full transcript (all turns, prompts and responses):
{full_transcript}

Current BOARD.md (existing targets and their object names):
{board_content}

Each candidate must satisfy ALL of the following:
1. Concrete claim level. Must be expressible as a single implication of the
   form "P(x) implies Q(x) under setup S", not a survey or discussion item.
2. Belongs in an existing BEDC chapter:
   core / proof_obligations / concrete_hardening / concrete_instances /
   hardening / formalization / proof_sprint / proof_standing.
3. Does NOT duplicate any existing BOARD entry (the existing Object names are
   in the BOARD.md content above).
4. Was actually surfaced by the just-finished transcript (cite which turn).

Score each candidate:
- fit_score (0-10): how well it lands in the existing BEDC theory surface.
- novelty   (0-10): how distinct it is from the existing BOARD entries.

Output JSON only on stdout, no prose, no fences:

{{
  "candidates": [
    {{
      "title": "<short title, like 'Gap separation under nontrivial sig identifications'>",
      "concrete_claim": "<single-sentence implication form>",
      "local_inputs": ["papers/bedc/parts/...", "papers/bedc/parts/..."],
      "fit_score": <0-10>,
      "novelty": <0-10>,
      "rationale": "<one paragraph: where it came from in the transcript, why it deserves its own loop>"
    }}
  ]
}}

Empty list is acceptable. Do NOT invent candidates that were not in the
transcript. Do NOT propose anything outside BEDC scope (no number theory,
no general categorical extensions, no comparison to other formalizations).
