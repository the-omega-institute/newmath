{target_id}: {target_title}

具体目标对象: {object}
具体待证: {problem}

## TOP-LEVEL HARD GATE — three_pass_oracle_spine

本轮必须在同一次 oracle 调用内完成三遍研究; 若没有完成三遍, 不得输出终端答案。三遍是内部工作骨架, 不是三个 API 调用, 也不是三段可见交付物。最终终端输出只能是 Pass C 的单一研究结果, 继续兼容后续 LaTeX 写回阶段。

**HARD GATE — Pass A / DRAFT**: 先按下文全部既有要求生成候选 theorem / lemma / proposition / definition / corollary 组, 连同证明义务与所依赖的 BEDC kernel macros。每个候选必须明确落在 closure / inversion / coverage / determinacy / uniqueness / bridge / obstruction 七类绑定词之一, 且必须绑定 {object} 或其依赖结构。

**HARD GATE — Pass B / ADVERSARIAL CRITIQUE**: 在输出前攻击 Pass A 中最弱的一条链。必须点名:
1. Pass A 依赖的最弱 binder 是 closure / inversion / coverage / determinacy / uniqueness / bridge / obstruction 中哪一个;
2. 该 binder 要合法闭合, 具体需要哪些 BEDC kernel macros 或字段事实成立;
3. Pass A 是否实际建立了这些事实。

若 Pass A 引入了不被对象、构造子、历史、包、签名、见证或既有定义合法支持的 binder, Pass B 必须判定该 binder 未建立, 并说明缺口属于缺少前提、缺少构造、缺少反演、缺少覆盖、缺少确定性、缺少唯一性、缺少 bridge 见证, 还是遇到 obstruction。

**HARD GATE — Pass C / REVISE_AND_EMIT**: 将 Pass B 的结论合并回最终答案。若能消除异议, 强化或重述定理, 使其前提最小且证明链真实闭合; 若不能消除异议, 不得把 unsupported claim 写成 theorem, 必须降格为明确编号的 `\begin{remark}` 风格 open obstruction, 并显式命名未解决的 obligation。终端输出禁止打印 Pass A / Pass B / Pass C 标题、审查日志或草稿; 只打印修订后的单一结果。

请基于已附加的 BEDC 论文 PDF (papers/bedc/main.pdf), 继续深入研究上述对象。结合项目论文 PDF 分析, 找一些能够惊艳到你的深刻结论链条 (禁止同义表述)。请研究到一些有发表价值的结论再结束, 不要挤牙膏。不要重复本对话此前已产出的内容。不要重复他人已发表的公开标准结论本身 (允许把它们作为工具或引理使用)。要求发现定理、推论、猜想、命题及证明。

不要中间过程结论。请使用顶级数学期刊学术化语言表达, 禁止口语。

每次输出时先打印当前日期与时间。本对话每个新结论编号累加计数, 不得重新编号。

每个新条目必须满足:
1. 与 BEDC PDF 中具体对象直接绑定 ({object} 或其依赖结构)
2. 题目精确、前提最小化、结论非平凡
3. 证明完整且自洽, 不是启发式论证
4. 不仅是 "换符号 / 换记号 / 换常数" 的旧结果变体
5. 内容可直接转写为 LaTeX 进入 papers/bedc/parts/
6. 不要写 `closurestatus` / Lean verification / marker-only 内容;
   本通道只产出 theorem / lemma / proposition / definition 正文.
7. 不要写抽象 carrier/classifier 参数搬运、source-equivalence 续集、
   字段逐项 repackaging. 结论必须绑定具体 BEDC 对象的 closure /
   inversion / coverage / determinacy / uniqueness / bridge / obstruction.

请直接开始产生新的研究结果。不要解释任务, 不要重述要求, 不要给导言。
