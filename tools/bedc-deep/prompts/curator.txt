You are doing meta-review of the BEDC bedc-deep loop's progress. Goal:
identify systematic blindspots — BEDC subareas where the loop has not yet
produced canonical content, and propose 3-8 strong new BOARD candidates
that purposely explore under-represented territory.

Use Read and Grep on the repo to do an actual investigation, not just
reasoning from memory. Take time.

Repo root: papers/bedc/parts/ (paper) and lean4/BEDC/ (formalization, read-only).

Sources to read:
- `papers/bedc/parts/` — current paper state.
- `tools/bedc-deep/BOARD.md` — full target queue with rationales.
- `tools/bedc-deep/state/*.json` — completed-target final states (verdict, stage2 result, attempts, failure_kind).
- `tools/bedc-deep/targets/<slug>/full_transcript.md` — successful Stage 1 transcripts.

Existing BOARD (skip semantically equivalent targets):
```
{board_content}
```

Recently completed targets (do not propose downstream-redundant work):
```
{completed_summary}
```

Think across two axes:

1. **Topic coverage**. Which chapter directories have NO completed targets
   that produced new LaTeX? Which have many completed but all in the same
   sub-area? Which are entirely definition-only with no theorems anywhere?

2. **Logical structure**. Which definitions exist with no companion theorem?
   Which theorem schemas have only one instance and lack the others? Which
   claim chains have a missing middle step?

Hard constraints (same as theory_probe — these will be cross-checked):
- Single-implication concrete claims, not surveys.
- Within an existing BEDC chapter directory.
- Not duplicating existing BOARD entries / `\label` / Lean markers.
- Not marker-only, `closurestatus`, or Lean verification-axis work.
- Not abstract carrier/classifier parameter transport, source-equivalence
  sequels, or field-by-field repackaging.
- Not a direct append to a hub file or a file already near the 800-line cap;
  cite a concrete child body file when the chapter has been split.
- Within BEDC scope (no external math, no general categorical generalization
  for its own sake, no cross-paper imports).
- Each `local_inputs` path must exist in the repo.

Output strict JSON only on stdout, no prose, no markdown fences:

{{
  "candidates": [
    {{
      "title": "<short, ≤80 chars>",
      "concrete_claim": "<single-sentence implication form>",
      "local_inputs": ["papers/bedc/parts/...", ...],
      "fit_score": <0-10>,
      "novelty": <0-10>,
      "axiom_budget": "<B0_finite_witness | B1_primitive_recursive_schedule | B2_rate_or_modulus | B3_finite_cover_or_compactness | B4_countable_construction | B5_impredicative_or_choice_like>",
      "strength_level": "<same vocabulary as axiom_budget>",
      "budget_reason": "<why this is the weakest adequate BEDC resource surface>",
      "existence_mode": "<constructive_witness | bounded_search | compactness_extracted | classical_noncomputable | none>",
      "witness_extractor": "<empty only when existence_mode is none or classical_noncomputable; otherwise name the finite witness/schedule extractor>",
      "cut_rank": "<0 | 1 | 2 | noneliminable | unknown>",
      "elimination_plan": "<required for bridge/transport/composition claims; otherwise empty>",
      "equality_kind": "<definitional_equal | propositionally_equal | isomorphic | equivalent | bisimilar | substrate_mirror | heuristically_corresponding | none>",
      "interpretation_kind": "<definitional_extension | conservative_extension | interpretation | faithful_embedding | quotient_projection | substrate_mirror | heuristic_translation | none>",
      "resource_trace": "<dependency/resource trace, especially for obligations or ledger rows>",
      "dependency_trace": "<specific paper dependencies consumed by the candidate>",
      "rate_modulus_surface": "<required for limit/completion/compactness/continuity/Cauchy claims; otherwise empty>",
      "oracle_mode": "<candidate_generation | proof_search | witness_synthesis | failure_diagnosis | rewrite_to_packet | continuation_refill | forbid>",
      "rationale": "<paragraph: which subarea / structural gap is under-represented, with file:line evidence>"
    }}
  ],
  "rejected": [
    {{
      "concept": "<concept I considered>",
      "reason": "<why it's already covered, with file:line>"
    }}
  ]
}}

Every kept candidate must include the Logic packet fields above. Use `none`
only when the field is genuinely not triggered by the claim. Do not use
`B5_impredicative_or_choice_like` unless the target is explicitly a boundary,
obstruction, failure, or nonconstructive theorem.

Aim for 3-8 strong candidates favoring breadth over depth. Empty `candidates`
is fine if everything that should be opened is already on BOARD or already
done. Empty `rejected` is suspicious — show what you considered and dropped.
