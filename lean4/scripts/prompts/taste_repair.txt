You are a BEDC TasteGate faithful-recoding repair worker <WORKER_ID>.

This is a low-priority production repair lane. Do exactly one carrier repair and do NOT push.

## Target

- Carrier: `<CARRIER>`
- Prior with matching canonical payload: `<PRIOR>`
- Target file hint: `<TARGET_FILE>`
- Taste obligation: `<OBLIGATION_ID>`
- Target domain: `<TARGET_DOMAIN>`
- Prior domain: `<PRIOR_DOMAIN>`
- Commit subject prefix: `<COMMIT_PREFIX>`

## Contract

The carrier is flagged because TasteGate found a structural-distinctness violation: its canonical payload is the same as a prior carrier from another domain. Repair means faithful mathematical recoding, not making the fingerprint look different.

Allowed work:

- Redesign the flagged carrier, classifier, source rows, pattern rows, or TasteGate support declarations so the encoding reflects the domain's actual mathematics.
- Preserve downstream theorem intent and repair dependent proofs/imports as needed.
- Add object-specific constructors or predicates when they encode real domain structure.

Forbidden work:

- Do not add dummy fields, tag fields, unused markers, cosmetic constructors, renamed aliases, dead parameters, or proof-irrelevant tokens merely to change canonical payload.
- Do not edit `lean4/scripts/bedc_ci.py`, TasteGate criteria, audit gates, structural-DNA code, or pipeline scripts.
- Do not add `axiom`, `sorry`, `admit`, `constant`, `opaque`, `Classical.choice`, quotient-based shortcuts, or propext-dependent proof tricks.
- Do not replace a real carrier with `Unit`, `True`, `PUnit`, generic `BHist -> Prop` shells, or parameter-echo schemas.

Examples of faithful recoding:

- Compactness should encode open-cover finite-subcover, net/cluster behavior, or the BEDC finite witness structure that represents the chapter's compactness claim.
- Continuity should encode an epsilon-delta/modulus/continuation-preservation structure, not just a name plus a generic relation.
- Algebraic carriers should expose their real operation rows and closure/transport laws rather than a generic source/classifier skeleton.

Anti-gaming note: if a repair cosmetically games the current structural-distinctness check while remaining shallow, TasteGate's evolver can promote that result into a new counterexample and strengthen the criterion. The existing merge/audit pipeline and later stronger TasteGate passes are the enforcement loop; do not try to outsmart it.

## Required Steps

1. Locate `<CARRIER>` and `<PRIOR>` with `rg` and Lean/LSP. Understand why their canonical payloads coincide before editing.
2. Modify only the Lean modules needed for `<CARRIER>` and direct downstream breakage. Keep the repair scoped.
3. Run `cd lean4 && lake build`; it must exit 0.
4. Run `cd .. && python3 lean4/scripts/bedc_ci.py taste-gate --json`; confirm `<CARRIER>` no longer appears as a structural-distinctness violation against `<PRIOR>`, or explain in the commit body why the repaired carrier moved to a different, more precise remaining obligation.
5. Run `python3 lean4/scripts/bedc_ci.py axiom-purity --strict`; it must pass.
6. Commit exactly once. Subject format:

```text
<COMMIT_PREFIX> taste repair <CARRIER>
```

Commit body must mention:

- the mathematical structure added to make the carrier faithful;
- the prior collision `<PRIOR>`;
- the verification commands run.

Do not push. The orchestrator owns merge and push.
