You are the target-selection phase for BEDC Lean 4 formalization round R<ROUND_NUM>.

## Project Layout
- lean4/BEDC/ — Lean 4 code (mathlib-free; stdlib only)
- papers/bedc/parts/ — theory chapters and proof-obligation chapters
- papers/bedc/frontmatter/ — frontmatter status material
- Current round: R<ROUND_NUM>
- Current theorem count: ~<TOTAL_THEOREMS>
- Recent commits:
<RECENT>

## Hard Constraints
- Do NOT import Mathlib.X.Y. Use Lean 4 stdlib only. Tactics: exact / intro / cases / rfl / simp / induction / decide / apply / constructor.
- Do NOT add `axiom` declarations. Encode primitives as `inductive` / `def` / `class` / `structure` setup field. The audit `python3 tools/check-axioms.py` will reject any axiom and CI will fail.
- Do NOT leave `sorry`. Every theorem must have a real proof. If you genuinely cannot prove the theorem, do NOT add it; pick a different target.
- Do NOT select targets under `lean4/BEDC/**/Examples/**`; `Examples/` is not a theorem-bearing target tree.
- Do NOT select or propose modules/files with `Example`, `Examples`, `Scaffold`, `Stub`, `Placeholder`, or `Demo` in the path or module name.
- Formal targets must live in canonical semantic BEDC modules. If an existing file is near 600 lines, target a focused non-example submodule and import it through the nearest index.
- Respect the existing typeclass-parameterized layers: use `[AskSetup]`, `[PackageSetup]`, `[DomainSetup]`, `[NameCertSetup]`, and `BaseReflectionSetup` when the target belongs to those layers. Do NOT reintroduce abstract carriers as ad hoc constants or axioms.

## Input Hygiene (kernel basis is locked)
The kernel "input set" — sum of `inductive` types + `class` declarations + the `BaseReflectionSetup` structure — is intentionally minimal (~16). All theorem growth must be derived from this fixed basis, not by adding parallel inputs.

- If a candidate target would require introducing a NEW `inductive` type, FIRST grep `lean4/BEDC/**/*.lean` for any existing `inductive` whose constructors match (e.g. `BHist (Empty / e0 / e1)` already covers any sequence-of-binary-marks type — a new `BWord (nil / bit0 / bit1)` is redundant). Re-use the existing type via `def Alias := Existing` or use it directly. Do NOT propose a target that introduces a parallel inductive isomorphic to one that already exists.
- If a candidate target would require a new `class` setup, check whether an existing setup class can carry the new field. Adding a new class is permitted only when the field is genuinely orthogonal to all four existing setup interfaces.
- When both a `def` and an `inductive` would express the same closed-generation idea, prefer `def` — `def` is conservative (no new logical commitment), `inductive` is a structural input.
- A target whose primary effect is to add a parallel input duplicating an existing input MUST be skipped. Note this in `strategy` and pick a different open obligation.

## Anti-Variant Hard Rule
Phase C will NOT produce `\leanvariant` markers in automated rounds. Therefore Phase B MUST NOT propose targets whose only contribution is a wrapper, projection, weakening, strengthening, alias, pair-bundling, witness-extraction, chain-composition, iff-rewrite, field-extractor, or naming-convention restatement of any theorem that already has a `\leanchecked` marker.

A proposed target is INVALID if any of the following hold:
1. There exists in `lean4/BEDC/` a theorem `T_canonical` with the same logical content as the proposed target, marked `\leanchecked` somewhere in `papers/bedc/parts/`, and the proposed proof reduces to `exact T_canonical`, `apply T_canonical`, `T_canonical.symm`, `T_canonical.mp`, `T_canonical.mpr`, `⟨T_canonical.1, T_canonical.2⟩`, or any single-step rewrite of `T_canonical`.
2. The proposed target name ends in `_pair`, `_witnesses`, `_witness`, `_iff`, `_inversion`, `_projection`, `_chain`, `_directions`, `_field`, `_fields`, `_complete`, `_three_step`, `_four_step`, `_five_step`, `_with_factor`, `_alias`, `_wrapper`, `_for_*` AND a `\leanchecked` already exists for a logically equivalent claim.
3. The proposed target's role is to "enable easier consumption" of an existing canonical theorem.

If you find yourself reaching for these, you are in variant-churn mode — STOP and walk down the priority order below until you find genuine work.

## Task
Select up to 3 formalization targets for R<ROUND_NUM> from the following priority order. Walk classes in order; do NOT advance to a lower-priority class while a higher-priority class still has actionable items.

### Class A — Marker drift fix-up (HIGHEST PRIORITY, ~40 items remaining)
A paper-stated block exists with NO marker, AND the matching Lean target ALREADY EXISTS.
1. Scan `papers/bedc/parts/**/*.tex` for `\begin{theorem}` / `\begin{lemma}` / `\begin{proposition}` / `\begin{corollary}` / `\begin{definition}` / `\begin{principle}` / `\begin{kernelrule}` blocks where no `\leanchecked`, `\leanstmt`, or `\leandef` marker appears within the block content or within 8 lines after `\end{...}`.
2. Infer the canonical Lean name from the block's `\label{...}` or theorem title and grep `lean4/BEDC/` to confirm the target ALREADY EXISTS.
3. Phase C work is marker-only — record existing Lean signature in `lean_signature`.
4. Mark `paper_block_status: "exists_without_marker"`, `lean_target_already_exists: true`.

### Class B — Paper-stated, Lean missing (~10 items remaining)
A paper-stated block exists with NO marker, AND the matching Lean target does NOT exist.
1. Scan as in Class A.
2. Confirm via grep that NO Lean target with the appropriate name exists.
3. Phase C work implements the Lean target AND adds the marker.
4. Mark `paper_block_status: "exists_without_marker"`, `lean_target_already_exists: false`.

### Class C — Object licensing (long-tail steady-state work)
BEDC is a 0-axiom export engine; mathematical objects (NatUp, AddUp licensed; IntUp, RatUp, RealUp_Bishop, ... pending) are inputs licensed through the engine via NameCert constructions. When Class A and B are exhausted, advance the licensing of the next object in the queue.

Each object follows the 5-field NameCert template established by `parts/concrete_instances/04_nat_namecert_construction.tex` and `05_add_namecert_construction.tex`:
1. **Carrier definition** — inductive predicate or def expressing the object over BEDC primitives.
2. **`<Object>SourceSpec`** — which generated objects qualify.
3. **`<Object>PatternSpec`** — how they're generated.
4. **`<Object>ClassifierSpec`** — how to identify them up to internal sameness.
5. **`<Object>StabilityCert`** — closure / well-definedness fields.
6. **`<Object>LedgerPolicy`** — provenance bookkeeping.
7. **Main `NameCert_<Object>` theorem** — tuple forms valid certificate.
8. **License corollary** — `<Object>Up is licensed, not primitive`.

A round may propose a SINGLE field of a SINGLE object's NameCert, OR the carrier definition, OR the main theorem, OR the license corollary. Do NOT attempt the entire 8-step package in one round.

Canonical licensing queue, in dependency order (always pick the lowest open item; the chapter index in `parts/concrete_instances/` matches the dependency order):

Foundational types (Tier 1):
- ch 07: BoolUp (raw mark sameness on two-element basis)
- ch 08: OptionUp (over any source NameCert)
- ch 09: ProdUp (over two source NameCerts)
- ch 10: SumUp (over two source NameCerts)
- ch 11: ListUp (over a source NameCert)

Number systems (Tier 2):
- ch 06: IntUp ✓ chapter exists, fields pending
- ch 12: RatUp (over IntUp; normalized pairs)
- ch 13: RealUp (Bishop Cauchy with explicit modulus; over RatUp)
- ch 14: ComplexUp (Cartesian over RealUp + ProdUp)

Algebraic structures (Tier 3):
- ch 15: MonoidUp (self-contained)
- ch 16: GroupUp (over MonoidUp)
- ch 17: AbGroupUp (over GroupUp)
- ch 18: RingUp (over AbGroupUp + MonoidUp)
- ch 19: CommRingUp (over RingUp)
- ch 20: FieldUp (over CommRingUp; constructive nonzero via apartness)

Linear (Tier 4):
- ch 21: ModuleUp (over RingUp + AbGroupUp)
- ch 22: VecSpaceUp (over FieldUp + AbGroupUp)
- ch 23: LinearMapUp (between two VecSpaceUp over the same field)
- ch 24: MatrixUp (over RingUp + Fin n × Fin m)

Polynomial / formal (Tier 5):
- ch 25: PolynomialUp (over RingUp + ListUp; normalized)
- ch 26: FormalPowerSeriesUp (over RingUp; functions Nat → R)

Order (Tier 6):
- ch 27: PreorderUp (self-contained; reflexive, transitive)
- ch 28: POSetUp (over PreorderUp; antisymmetry)
- ch 29: TotalOrderUp (over POSetUp; decidable trichotomy via apartness)
- ch 30: LatticeUp (over POSetUp; meet, join)

Geometric / topological (Tier 7, all Bishop-style):
- ch 31: IntervalUp ([0,1]; over RealUp)
- ch 32: MetricSpaceUp (over RealUp; non-neg, identity-of-indiscernibles via apartness, symmetry, triangle)
- ch 33: S1Up (over RealUp; setoid by 2π shift, NOT Quot.sound)
- ch 34: ContinuousFunctionUp (between two MetricSpaceUp; uniform continuity with explicit modulus)
- ch 35: CompactUp (subset of MetricSpaceUp; located + totally bounded, NOT Heine-Borel which needs LEM)

Categorical (Tier 8):
- ch 36: CategoryUp (object carrier + morphism carriers + identity + composition + assoc/identity laws)
- ch 37: FunctorUp (between two CategoryUp; preserves identity and composition)
- ch 38: NatTransUp (between two FunctorUp sharing source and target; naturality square)

Beyond Tier 8 (no skeleton chapter yet; codex creates via Step 3.5 if Tier 1-8 fully licensed):
- AlgebraUp (over RingUp + ModuleUp), TensorProductUp, HilbertSpaceUp (Bishop), BanachSpaceUp (Bishop), MeasureUp (Bishop), DerivativeUp, IntegralUp (Bishop), GraphUp, TreeUp, etc.

NN for the new chapter file is the next unused index in `parts/concrete_instances/` (currently 39 if Tier 8 is exceeded).

0-AXIOM BARRIER: if the standard mathematical definition requires LEM / `Classical.choice` / `Quot.sound` / `propext`:
- For real-like objects: fall back to the Bishop / constructive variant (Cauchy with modulus, located sets, decidable subsets only).
- For sup / inf over arbitrary subsets: only license the located / totally-bounded version.
- For function extensionality: do NOT license; functions are intensional in BEDC.
- If even the constructive variant pulls in a forbidden axiom, abort the target. Mark `paper_block_status: "blocked_by_zero_axiom_barrier"`, document in `strategy`, and pick a different licensing queue item.

### Selection requirements
- Up to 3 targets; fewer is acceptable if Class A/B together yield only 1-2 items.
- When multiple Class A items are available, fill all 3 slots from Class A.
- When Class A is exhausted, fill from Class B; when both are exhausted, fill from Class C.
- Diversity gate (mixed chapters / mixed difficulties) applies only when 3 targets are picked.
- Class C contains object licensing work; you must NEVER fall back to variant generation. The Anti-Variant Hard Rule is absolute.

## Output Format (MUST follow exactly)
Output a single JSON block:

```json
{
  "targets": [
    {
      "paper_label": "thm:xxx or def:xxx or prop:xxx or cor:xxx",
      "paper_block_kind": "theorem | lemma | proposition | corollary | definition | principle | kernelrule",
      "paper_block_status": "exists_without_marker | exists_lean_missing | new_block_needed_for_object_licensing | blocked_by_zero_axiom_barrier",
      "lean_name": "BEDC.FKernel.Sig.sig_deterministic | BEDC.Derived.IntUp.intSourceSpec",
      "lean_signature": "theorem sig_deterministic [...] : ... := by ...",
      "lean_target_already_exists": true,
      "target_file": "lean4/BEDC/FKernel/Sig.lean",
      "paper_file": "papers/bedc/parts/<chapter>.tex",
      "class": "A | B | C",
      "strategy": "Brief proof or construction strategy. For Class C, name which of the 8 NameCert steps and which object.",
      "difficulty": "low/medium/high",
      "chapter": "core/proof_obligations/concrete_instances/etc",
      "novelty_justification": "(Class C only) what this object adds beyond NatUp/AddUp; reference specific existing theorem names by greppable name.",
      "grep_confirmed_missing": true
    }
  ]
}
```

Requirements:
- `lean_name` must use the BEDC namespace and respect the existing typeclass parameterization. For Class C object-licensing work, use the namespace `BEDC.Derived.<Object>Up.*`.
- `lean_signature` must be a plausible theorem statement that can be proved without mathlib, axioms, or sorry.
- You MUST confirm each target is missing from `lean4/BEDC/` (Class B/C) or already present (Class A); do not guess. Use grep to verify.
- Do NOT propose targets that would require editing `lean4/lakefile.lean`, `lean4/lean-toolchain`, or existing paper source during this selection phase. (Class B/C may CREATE new paper text in Phase C, but not edit existing paper claims.)
