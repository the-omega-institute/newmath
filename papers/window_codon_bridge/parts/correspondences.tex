\section{Bridge Correspondence Verdicts}
The following table lists every bridge claim in the registry.  The verdict column is
generated directly from the registry status field.

\begin{longtable}{>{\raggedright\arraybackslash}p{0.22\linewidth}>{\raggedright\arraybackslash}p{0.55\linewidth}>{\raggedright\arraybackslash}p{0.15\linewidth}}
\textbf{Claim} & \textbf{Statement} & \textbf{Verdict} \\
\hline
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.cardinality_forcing status=coincidence
Cardinality Forcing & The codon-Q6 boundary cardinality ($|R|=13=F_7$, 21 synonymous families) is FORCED by the Window6 Fibonacci horizon ($X_6=21=F_8$) via an explicit structural map standard-code-boundary -> width-6 Fibonacci words, not coincidence. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.spectral_correspondence status=refuted
Spectral Correspondence & The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, mu) equal the Window6 coarse Markov kernel spectrum (high/low energy superposition). & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.foldbin_codon_positions status=coincidence
Foldbin Codon Positions & The Window6 Foldbin tail-cube (3 free Boolean coords, seam-return $\rho_6-s_6=3$) corresponds to the 3 codon positions / wobble folding; the bio $Q^{(2)}$ second-order structure is the biological image of the second fold. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.killed_walk_spectral_correspondence status=refuted
Killed Walk Spectral Correspondence & The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, $\mu=6\lambda_M-1$) -- where lambda = (1/6)*spectral radius of the induced 6-cube subgraph adjacency on R and on M=median-closure(R) -- are structurally forced by the Window6 Fibonacci-cube killed-walk operator (same induced-subgraph spectrum / canonical map), not a numeric coincidence. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.cyclic_sector_frame_symmetry status=refuted
Cyclic Sector Frame Symmetry & Window6 cyclic Zeckendorf sector rotation corresponds to cyclic rotation of the 3 codon positions as a code-label-preserving sector character. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.right_boundary_selection_axis status=coincidence
Right Boundary Selection Axis & The Window6 right-boundary/foldbin escape functional $U_R$ (cyclic endpoint collision $w_1=w_6=1$, escape ledger |d$U_R$|/(6|$U_R$|)=8/9) maps onto the BIOLOGICAL low-optimality boundary axis -- R=tRNA-poor (BIO-M) and the unexplained universal residual axis $d_{\perp}$ (BIO-P) -- via sign/rank/projection constraints, NOT via naked cardinalities (|R|=13, 21, 3). & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fold_residual_wobble_correspondence status=coincidence
Fold Residual Wobble Correspondence & Window6's Fold6 hidden tail-offset residual modes and the standard code's third-position wobble residual modes are the same kind of hidden residual in a shared 'regular micrograph -> visible family -> hidden residual' fold-forcing mechanism (low-energy = first/second-position core, high-energy = third-position/tail residual): they leave visible family/type invariant but govern fiber multiplicity, lumpability failure, and quotient-kernel subleading spectrum -- tested by label-free invariants + hidden-mode alignment under graph/wobble-preserving nulls, NOT by the 64->21 count or any 21-label pairing. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.edge_isoperimetric_contravariance status=certified
Edge Isoperimetric Contravariance & Window6 Fold6 and the standard genetic code realize OPPOSITE achievable extremes of one label-free local-edge-hiding functional e\_in (one-step micrograph edges hidden inside a family): Window6 is the exact separating endpoint (e\_in=0=e\_min, tau\_W=0; every Q6 1-bit edge changes the stable type) and the standard code is the near-maximal tolerant endpoint (e\_in=69, e\_max=72, tau\_C=0.958), surviving graph- and wobble/box-preserving nulls. This is a C-0 contravariant-extremality certificate: it does NOT claim the two are isomorphic folds, that 64->21 is biologically meaningful, that high/low-energy maps to DNA/RNA/translation, or any common fold-FORCING law. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.parry_measure_usage_correspondence status=coincidence
Parry Measure Usage Correspondence & The Window6 Parry measure (golden maximal-entropy stationary measure of the no-adjacent-one transfer operator) corresponds, per-codon in the Q6 frame, to the codon usage-frequency selection axis ($d_{\perp}$) -- tested by rank/projection vs label-free nulls, not by any label pairing. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.smith_normal_form_cokernel status=refuted
Smith Normal Form Cokernel & The Smith-normal-form cokernel (finite abelian group invariant) of the Window6 edge matrix is isomorphic to that of the canonical codon adjacency/quotient matrix -- a label-free group-isomorphism test. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.three_rigidity_codon_graph status=refuted
Three Rigidity Codon Graph & The Window6 type-quotient graph and the codon synonymous family-quotient graph are rigidity-equivalent (share canonical label-free rigidity invariants: automorphism group, degree sequence, spectrum, distance profile) beyond a degree-preserving null. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.mod3_flux_frame_obstruction status=coincidence
Mod3 Flux Frame Obstruction & The Window6 edge-flux mod-3 obstruction class in H\textasciicircum{}1(G\_W;Z3) is cohomologically of the same type as the codon reading-frame/f3-ramp three-periodicity phase class, tested by Z3-Hodge signature (r3,T3,phase,cycle-integrals) up to D3 vs frame/wobble/family nulls. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.partition_quotient_wobble_foldbin status=coincidence
Partition Quotient Wobble Foldbin & The Window6 (Fold6, Zeckendorf-cylinder) partition pair and the codon (16-box, synonymous-family) partition pair have the same label-free partition-lattice signature (meet/join sizes, VI, equitable defect, boundary profile, quotient SNF) beyond size/graph-preserving nulls. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.green_spectral_codon_q_spectra status=refuted
Green Spectral Codon Q Spectra & The Window6 Green/resolvent spectrum has the same label-free spectral signature (density, gap, moments) as the codon q1/q3/q4 codon-cube quotient spectra beyond a degree-preserving null. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.edge_exhaustion_meta_certificate status=certified
Edge Exhaustion Meta Certificate & Within the pre-registered label-free invariant battery (tau plus spectral/Parry/SNF/mod3/partition/rigidity), the ONLY certified Window6<->codon cross-domain relation is the contravariant local-edge-hiding extremality (tau\_W=0 separating endpoint, tau\_C=0.958 tolerant endpoint); conditioned on this e\_in extremality and native graph/fiber constraints, all remaining invariants have residual alignment indistinguishable from a conditional null (operational independence). The realization mechanisms are polarized (Window6 isotropic separator, codon anisotropic third-position tolerator). NOT an isomorphism, NOT a forcing, NOT strict C-1 profile duality. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fourcell_selection_enrichment status=refuted
Fourcell Selection Enrichment & The Window6 4-cell coarse partition (sizes 27/22/9/6) explains significant non-random variance in the real 4D codon selection vectors (tRNA/f3/$d_{\perp}$/optimal) beyond cell-size-preserving and GC-conservative nulls. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.fold_structure_64_16_25_21 status=certified
Fold Structure 64 16 25 21 & The standard genetic code is an exact two-layer 64->16->25->21 fold (16 doublet boxes = 8 pure + 7 binary-split + 1 tri-split; 25 box-output fragments minus 4 cross-box merges = 21 classes; degeneracy spectrum 1\textasciicircum{}2,2\textasciicircum{}9,3\textasciicircum{}2,4\textasciicircum{}5,6\textasciicircum{}3) -- the hard biological instance of the abstract microstate->block->seam->stable fold pattern. NOT a golden-ratio forcing; Zeckendorf decompositions universal/presentational; ratios are ratio-of-counts. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.neutral_hamming_adjacency status=certified
Neutral Hamming Adjacency & The standard code's per-codon Hamming-1 neutral-neighbor histogram is exactly 0:2,1:22,2:8,3:28,4:4 (merged defect = 28/22/8/6), close to but not equal to the Window6 27/22/9/6; no natural untuned biological weighting bridges them. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.fold_order_parameter_usage status=coincidence
Fold Order Parameter Usage & The fold order parameter m\_bio = P\_bulk - P\_seam computed from real multi-organism codon usage carries an independent (non-GC) cross-organism signal. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.neutral_trna_edge_coupling status=coincidence
Neutral Trna Edge Coupling & The certified Window6 edge score modulates the tRNA supply-demand alignment on synonymous Hamming-1 edges (usage-edge-gradient \textasciitilde{} tRNA-edge-gradient x Window6-edge-score, interaction beta\_bridge), beyond GC/f3 and edge-permutation nulls, with held-out-species predictive lift. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.twolayer_fold_edge_hiding_extremality status=certified
Twolayer Fold Edge Hiding Extremality & The contravariant edge-hiding extremality iterates across the genetic code's two-layer fold: the first-two-base box partition (64->16) is the EXACT maximum-edge-hiding partition for its size-4x16 block profile (tau\_box=1.0), and the family layer (64->21) is near-maximal (tau\_family=0.958), both in the extreme upper tail of a same-block-size null; Window6 Fold6 sits at the opposite extreme (tau=0). Restricted to iterated edge-hiding extremality; NOT isomorphism or biological forcing. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.foldlayer_zigzag_extremality status=certified
Foldlayer Zigzag Extremality & The standard genetic code realizes a zig-zag edge-hiding fold layer B <- M=B meet F -> F: the first-two-base box layer and box-family fragment layer are exact edge-hiding endpoints, while the family layer is near-maximal with a small deficit. This is a finite label-free extremality certificate, not a monotone quotient tower, isomorphism, biological forcing, or live-usage claim. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.halfbox_fibonacci_tower status=coincidence
Halfbox Fibonacci Tower & The genetic-code halfbox and family-meet layers create exact edge-hiding endpoint facts and a strict Fibonacci tower across the naturally generated block counts 13, 21, 34, and 55 under canonical finite partition operations. The claim must separate hard extremality facts from numerical tower appearances. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6.fold_tower_separating_extremality status=refuted
Fold Tower Separating Extremality & The Window fold tower is edge-separating at each tested finite width under the same value-window convention that reproduces the Window6 certificate, yielding a cross-width revealing endpoint dual to the genetic-code hiding tower. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.edge_defect_axis_cert status=coincidence
Edge Defect Axis Cert & The standard genetic code's local-edge-hiding deficit is exactly e\_max-e\_in=3, supported only on Stop and Ser, with Stop contributing one punctuation defect and Ser contributing two sense split defects. The sense-side SerSplitDefect vector should project onto an available codon-Q6 selection axis, especially an independent fourth residual axis if present. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.edge_defect_projection_harness status=certified
Edge Defect Projection Harness & The SerSplit edge-defect projection audit has a deterministic 61-sense-codon harness: it builds forbidden controls from available codon-Q6 selection and codon-chemistry columns, residualizes candidate axes and the SerSplit contrast through the same matrix, detects only the forbidden positive control, and does not promote oracle output or local controls into biological evidence. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.ser_island_fidelity_cost status=coincidence
Ser Island Fidelity Cost & The Ser UCN/AGY split creates a code-table near-cognate error-cost asymmetry: UCN and AGY have different one-step missense/stop neighborhoods, and this asymmetry could seed a translation-fidelity selection route only if it is exceptional beyond matched codon-family nulls and later survives site-level expression/mistranslation tests. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.synthetic.negative.control.axis.pack status=needs_derivation
Pack & Use only in-repo deterministic codon features and seeded synthetic vectors to audit the common projection harness before any external candidate axis is attempted. This autonomous bridge claim is evaluated only by its registered deterministic experiment. & \texttt{needs\_derivation} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.sersplit.edge_defect.nonestimability.certificate status=certified
Certificate & The Ser-split edge-defect contrast (within-Ser 4-vs-2: \{UCU,UCC,UCA,UCG\} vs \{AGU,AGC\}) lies exactly in the column span of the registered forbidden-control matrix C (verified against the actual registered z-scored columns to 1e-9), so its residual is algebraically zero, v\_SerSplit is undefined, and no real-data biological axis can yield a certified independent projection onto this target. On the Ser support, sqrt(12)*u = 1 - 3*indicator(first base is purine), so the edge defect IS a registered composition control. The 5-dim within-Ser zero-sum contrast subspace is NOT fully absorbed, so the 15-way within-Ser permutation null is non-degenerate. This autonomous bridge claim is evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.foldboundary.residual.exhaustion.certificate status=certified
Certificate & The pre-registered, q6-label-free fold-boundary contrast subspaces over the 61 sense codons -- box split-type (pure/split/tri-split coarsening of the 16 first-two-position boxes), degeneracy-class (amino-acid degeneracy sizes), and fold-layer (64->16->25->21) -- are exactly absorbed (residual rank 0) by the registered forbidden-control matrix C, so these bio-intrinsic fold boundaries provide no independent residual axis beyond the registered composition controls (a bounding exhaustion extension of the edge-exhaustion meta-certificate). The Window6 four-cell partition mapped via q6\_label is correspondence-derived, not bio-intrinsic; because C contains no q6 information its residual survival (rank 3) is expected and is barred from the certificate, not treated as independent forcing. NOT certified here: the full 16-box partition's fine structure -- its only possible escape directions are the Leu 4/2 and Arg 4/2 within-amino-acid branch splits (analogous to the certified Ser 4/2), which are the next frontier. This autonomous bridge claim is evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.box_partition.composition_collapse.certificate status=certified
Certificate & Each of the three 6-fold-degenerate amino acids has a 4-vs-2 within-amino-acid branch split (Ser UCN|AGY, Leu CUN|UUR, Arg CGN|AGR), and exact rational elimination proves ALL THREE lie in the span of the registered forbidden-control matrix C (residual 0), with on-support identities sqrt(12)*u = 1-3*indicator(first base purine) for Ser, 1-3*indicator(first two bases equal) for Leu, and -2+3*indicator(first base GC)+... for Arg. The full mean-zero 16-box first-two-position indicator subspace (dim 15) has exact residual rank 0 against C (exact\_control\_rank 29 = rank with box partition appended), so the entire 64->16 box partition is absorbed by the registered composition control frame -- it is composition, not an independent forcing direction. Generalizes the certified Ser-split non-estimability to all three 6-fold amino acids and completes the box-partition thread of the fold-boundary exhaustion. Proof tied to the actual registered columns (registered-equivalence, fail-closed on drift), branch contrasts label-free (no q6/Stop). Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.forcing_direction_closure.metacertificate status=certified
Metacertificate & Consolidation meta-certificate for the math->bio forcing direction. Exact rational rank proves the FULL pre-registered label-free fold-boundary battery (box partition + degeneracy classes + fold layers + the three 6-fold amino-acid 4/2 branch splits Ser/Leu/Arg; 44 columns, span rank 22) lies AS ONE UNION exactly in span(C): exact control rank 29 is unchanged when the battery is appended (absorbed). The only fold-boundary direction outside span(C) is the q6\_label-derived Window6 four-cell partition, which is correspondence-derived and barred as circular (q6 is the correspondence under test). Therefore the bridge's certified scope is: (1) POSITIVE - the sole certified structural signal is the BC7 contravariant edge-hiding extremality plus the edge-exhaustion meta-certificate (Window6/Fold6 and the standard code occupy opposite endpoints of one label-free local-edge visibility functional); (2) BOUNDING-NEGATIVE - the label-free bio fold-boundary structure is composition (= span(C)), leaving no independent bio-side residual axis for Window6 to force; (3) the math->bio FORCING direction is closed-as-not-supported under the current frame, with count/spectrum matches classified coincidence and q6-mediated forcing barred as circular. REOPENING CONDITION (falsifiable): a label-free genetic-code invariant outside span(C), defined without q6 or family counts, whose preservation forced-Window6/Fibonacci/Zeckendorf structure necessitates more strongly than a pre-registered composition-controlled null. Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.h3_higherorder.complement_stresstest status=refuted
Complement Stresstest & Stress-test claim: the composition-exhaustion closure extends to the canonical higher-order class H3 (the 27 three-position nucleotide-chemistry interaction tensors chi\_a(base1)*chi\_b(base2)*chi\_c(base3), chi in \{R/Y, W/S, K/M\}; genuinely higher-order, label-free, non-q6). VERDICT REFUTED: exact Fraction rank shows H3 has residual rank 26 of 27 after the registered composition control frame C (exact control rank 29; rank with H3 appended 55), so higher-order three-position chemistry structure lies almost entirely OUTSIDE span(C). This precisely DELIMITS the composition-exhaustion: not all label-free codon structure collapses to composition -- the additive/pairwise+fold structure does, but higher-order three-way chemistry interactions do not. CRITICAL: this is a delimitation, NOT a math->bio forcing reopening -- there is no Window6-necessitation test, no pre-registered necessitation null, and no principle selecting H3 among higher-order audit languages (Walsh/Fourier, codon-graph spectral, automorphism-orbit); H3 is recorded only as a candidate class for future forcing work requiring a frozen necessitation null. The forcing-direction closure stands. Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fibcube_embedding.conventionrobust_test status=needs_derivation
Conventionrobust Test & Reopening attempt for the math->bio forcing direction via a NON-q6, first-principles embedding. Test: is the Fibonacci-cube vertex set S = \{21 no-adjacent-1 six-bit words\} (the canonical Gamma\_6 isometric inclusion into the codon 6-cube Q6) a CONVENTION-ROBUST code-structure outlier when mapped into codon space across ALL 17280 admissible base->2bit x bit/position conventions (q6 being just one marked convention, class size 2), vs a composition-matched null on pre-declared label-free statistics (synonymous-edge / family-boundary / fold-cell / conservative-substitution), Holm-Bonferroni corrected? VERDICT needs\_derivation: NO forcing signal -- the q6 class is NOT an outlier (q6\_class\_outlier False) and only 4.7\% of non-q6 conventions are outliers (far below the 0.5 robustness threshold, \textasciitilde{}noise). So the canonical Gamma\_6->Q6 isometric-inclusion route produces neither a convention-robust non-q6 forcing nor a q6-special alignment: this most-natural non-circular embedding route is EXHAUSTED on the pre-declared battery. This is NOT an exhaustive impossibility proof (other label-free invariants / non-inclusion constructions untested), hence needs\_derivation not refuted. It does not touch BC7 (which concerns the Fold6 PARTITION edge functional, a different object from the Gamma\_6 vertex set). Evaluated only by its registered deterministic experiment (seed 619423). & \texttt{needs\_derivation} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fibcube_codonfamily_graphiso_test status=refuted
Fibcube Codonfamily Graphiso Test & Convention-free reopening route (distinct from the inclusion route): is the Fibonacci cube Gamma\_6 (graph on the 21 no-adjacent-1 six-bit words, Hamming-1 edges; 21 vertices, 38 edges) ISOMORPHIC as an abstract graph to the codon family-adjacency graph (families adjacent iff some codon pair is Hamming-1)? A graph iso is intrinsic (no codon-coordinate convention), so it sidesteps the inclusion route's convention problem. VERDICT REFUTED: they fail necessary isomorphism invariants -- Gamma\_6 has 38 edges vs 85 for the 21-family graph (incl Stop) and the excl-Stop variant has 20 vertices/75 edges; degree sequences also differ (Gamma\_6 max degree 6 vs family-graph degrees 6-13). Robust across family-graph definitions. So the convention-free graph-isomorphism reopening route is CLOSED: the 21<->21 cardinality match is a graph-level COINCIDENCE, not structural forcing. Together with the inclusion-route exhaustion (fibcube\_embedding\_conventionrobust\_test), both natural non-q6 Window6->codon embedding constructions are now closed. Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.synfiber_quotient.extremality status=coincidence
Extremality & Factory second instance. Is the genetic code's synonymous-fiber partition of the codon Q6 cube an extremal/outlier point for a label-free QUOTIENT functional (cross\_equitability\_defect of the block-to-block palette graph; distinct from e\_in/BC7) among Q6 partitions matched to its degeneracy-size multiset? VERDICT COINCIDENCE (reducible): the partition is an extreme low-tail equitability outlier under the WEAK size-only null (p=3.9999e-05, 50000 draws) BUT p=0.496 (dead median) under the STRINGENT box/wobble-preserving null (box\_wobble\_atom\_edge\_profile\_preserving: keeps position-3 wobble atoms intact + matches the same-fiber Q6 edge profile 47 within-box/3 cross-box). So the code's degeneracy-ASSIGNMENT equitability is ENTIRELY reducible to the already-certified box/wobble composition (box-partition-collapse) -- NOT an independent forcing signal. Reinforces the composition-exhaustion finding at the quotient/assignment level. The functional is genuinely distinct from e\_in (variance-like cross-block uniformity defect), and the certified-positive over-claim was caught by the review triplet (unanimous reject of the weak-null certified) and corrected. Evaluated only by its registered deterministic experiment. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.square_motif_residual.extremality status=coincidence
Extremality & Oracle-planned non-reducible frontier (the next canonical object beyond edges: Q6 square 2-faces). Is the genetic code's cross-box square-motif structure (label-free 4-cycle vertex equality-pattern census, by box/wobble automorphism orbit) extremal under the conditioned null Omega\_BW,E -- all unlabeled regroupings of the 25 fixed box/wobble atoms into 21 fibers preserving the global fiber-signature multiset AND every box/wobble edge-orbit's cross-atom synonymous-edge-count profile (so e\_in + its directional profile + distribution + box/wobble + edge-margins are ALL conditioned out)? The interpretable coordinate is diagonal-recovery faces (synonymy as a 2-step diagonal recurrence, NOT edge-hiding), but the verdict uses the omnibus Mahalanobis T on the edge-margin-orthogonal residual. VERDICT COINCIDENCE: prerequisite GATE passed -- Omega\_BW,E is non-trivial (162468 states, EXACT enumeration; observed state included) and the residual has nonzero variance (trace 4.28), so this was a genuine non-reducible test, NOT a forced triviality -- but the standard code is NOT an extreme outlier (omnibus T\_obs=2.54, p=0.246, typical). So even the square 2-face / 2-step-diagonal structure, conditioned out of everything already certified, carries NO independent forcing signal; the composition+e\_in closure deepens to the 2-face level. Review triplet (quality+tests) independently re-enumerated the 162468-state null + verified the omnibus statistic (deterministic, no RNG). Evaluated only by its registered deterministic experiment. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.bypass_conductance.extremality status=coincidence
Extremality & Oracle Route 3 (strongest GLOBAL complement to the square-motif residual). Is the genetic code extremal for EXTERNAL BYPASS CONDUCTANCE between the disconnected components of synonymous fibers -- the ambient placement of fiber pieces in Q6 that edge-hiding and 2-face motifs do NOT see? For each disconnected fiber, for each component pair, the effective conductance through the external routing graph (excluding the fiber's own vertices) is computed (grounded-Laplacian effective resistance; observed value 35712/6925 per disconnected fiber), summed over ALL disconnected fibers without naming Ser/Stop. Null Omega\_BW,E,d,comp conditions out e\_in + box/wobble + edge-margins + boundary-degree vectors + per-signature connected-component sizes \& isomorphism types (162468 states, EXACT enumeration). VERDICT COINCIDENCE: both prerequisite gates PASS (statistic has nonzero variance + the null has >=2 box/wobble-preserving orbits -- a genuine non-reducible test) but the standard code is TYPICAL (omnibus T\_obs=10.31, upper-tail p=0.202). So even the strongest global ambient-placement invariant, conditioned out of everything certified, carries no independent forcing signal. Together with the square-motif coincidence, the e\_in+composition closure is now confirmed robust across edges, square 2-faces, AND global disconnected-component conductance. Review (tests) independently re-solved the conductance + verified the component-conditioned null. not tailored to any named fiber. Evaluated only by its registered deterministic experiment. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.quotient_spanningtree.extremality status=coincidence
Extremality & Oracle Route 2/last (quotient boundary-channel topology). Is the genetic code extremal for quotient spanning-tree COMPRESSION T\_chan = -log tau, where tau is the exact weighted spanning-tree count (Matrix-Tree) of the 21-fiber quotient multigraph W\_ab = cross-fiber Q6 edge counts? Null Omega\_BW,E,d conditions out e\_in + box/wobble + edge-margins + boundary-degree vectors (so total cross-fiber weight is fixed and tau's variation is channel TOPOLOGY only; 162468 states exact). VERDICT COINCIDENCE: both prerequisite gates pass (nonzero variance + multi-orbit) so it is a genuine non-reducible test, but the standard code is typical (tau=253827722980651696128 exact via Bareiss; T\_chan=-46.98; borderline upper-tail p=0.0647, above the p<=0.01 certification threshold). This COMPLETES the oracle's 3-route systematic sweep beyond edge-hiding: square-motif residual (p=0.246), bypass-conductance (p=0.202), and quotient spanning-tree (p=0.065) -- ALL coincidence. Review (quality+tests) verified the exact determinant (independent SymPy match) + the boundary-degree-conditioned null + borderline-p honesty. Evaluated only by its registered deterministic experiment. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.edge_hiding_optimality status=certified
Edge Hiding Optimality & The standard genetic code's codon ASSIGNMENT is an edge-hiding optimum among degeneracy-matched codes. Using the bridge's certified functional e\_in = \# Hamming-1 (single-base, H(3,4)) codon pairs that are synonymous, the standard code has e\_in = 67 (sense-to-sense) / 69 (all codons), versus the null Omega\_deg of random codes with the standard amino-acid degeneracy profile FIXED (Leu/Ser/Arg=6; five fourfold=4; Ile=3; nine twofold=2; Met/Trp=1; Stop=3) and codon assignments uniformly randomized: null mean 12.4, sd 3.2, max 30 (analytic mean \textasciitilde{}288*(244-64)/(64*63)\textasciitilde{}12.9 confirms). p\_hide=0.0, z=17.1 -- NOT a single degeneracy-matched random code reached the standard code's edge-hiding. VERDICT CERTIFIED. HONEST FRAMING: this is the e\_in edge-hiding extremality (BC7's lone certified positive) demonstrated against the NATURAL 'alternative genetic code' null, quantitatively CONNECTING the bridge's certified structural positive to the classic genetic-code error-minimization/optimality result (Freeland-Hurst lineage) -- i.e. the code's redundancy is organized (box/wobble) to keep synonymous codons mutationally adjacent far beyond what its degeneracy profile alone requires. It is a confirmation + biological connection of BC7 vs a new null, NOT a new independent functional, and is distinct from the box-partition-collapse result (a linear statement; e\_in is the one nonlinear genuine signal). It is the structural foundation of the relayed bridge->bio DMS-robustness mediation test (does this edge-hiding buy measurable mistranslation robustness, R(g)=alpha-beta*e\_in+eps via ProteinGym). Review triplet (architecture+quality+tests) independently verified the uniform null, the 288-edge graph, e\_in=67/69, and the analytic null-mean match. Evaluated only by its registered deterministic experiment (fixed seed). & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.codon_v4cubed_eigenspace.canonicity status=certified
Canonicity & Codon function space has the canonical V4\textasciicircum{}3 character / Hamming-association-scheme decomposition, and the bridge's H3 class is exactly its highest-frequency eigenspace. Bases = Klein four-group V4 with three chemical dichotomy characters chi\_R (purine/pyrimidine), chi\_W (weak/strong), chi\_K (keto/amino); EXACT: chi\_R(b)*chi\_W(b)*chi\_K(b) = -1 for every base, and \{1,chi\_R,chi\_W,chi\_K\} is an orthogonal character basis. The 64 character tensors Psi\_\{abc\}(c)=chi\_a(b1)chi\_b(b2)chi\_c(b3) form an orthogonal basis of R\textasciicircum{}64, grouped by level j into E\_0..E\_3 of dims 1/9/27/27 = C(3,j)3\textasciicircum{}j; each E\_j is EXACTLY an eigenspace of the H(3,4) single-base adjacency (64 codons, 288 edges) with Krawtchouk eigenvalue lambda\_j = 9-4j (9,5,1,-3) -- verified by applying the adjacency to each tensor in exact integer arithmetic (no float eigensolver). THE BRIDGE LINK: the H3 class (the 27 three-position R/Y,W/S,K/M interaction tensors used in run\_h3\_higherorder\_complement\_stresstest) equals E\_3, the lambda=-3 highest-frequency eigenspace, proved by exact ranks (h3\_rank=e3\_rank=combined\_rank=27). VERDICT CERTIFIED. This is the standard H(3,4) association-scheme spectral decomposition realized via the genetic code's chemical characters; the bridge-specific content is H3 = canonical E\_3 (not an arbitrary higher-order class) + the V4\textasciicircum{}3 group-algebra/Krawtchouk number-theory entry point for a future bio-validated translation operator (SNF/trace/zeta). Review (tests) independently verified the exact eigenspace check + the H3=E3 span equality. Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.altcode_edge_hiding.tau_conservation status=certified
Tau Conservation & Edge-hiding is a CONSERVED constraint across the genetic-code family. For each of the 27 NCBI genetic code tables, the number of synonymous single-base (H(3,4)) codon edges e\_in(t) is compared to a degeneracy-profile-matched random null (random codes with t's exact amino-acid family-size multiset; 20000 draws, fixed seed; sense-to-sense, Stop retained in profile but Stop-Stop edges excluded). RESULT: all 27 codes' per-code profile-matched-null upper-tail p sit at the Monte-Carlo floor \textasciitilde{}5e-5 (all\_at\_floor=True); natural e\_in is far above the null (min e\_in minus null p99 = 43; minimum null z-score = 15.4). This survives a 9-cluster PHYLOGENETIC bootstrap (clusters by code-table reassignment similarity, resampling clusters NOT individual codes to avoid pseudo-replication of related mito variants): cluster\_bootstrap\_p = 5e-5. The union of codons reassigned across all tables vs the standard = exactly 13 (reproduces |R|=13). VERDICT CERTIFIED: natural genetic codes are systematically edge-hiding-extremal vs degeneracy-matched random codes, conserved across the family AND robust to phylogenetic non-independence -- extending the BC7 edge-hiding optimality from the standard code to the whole genetic-code family. EXPLICITLY NOT claimed: global maximality (no valid e\_max -- the broken pair-swap optimizer was removed after review; the claim rests purely on the natural-vs-degeneracy-matched-null contrast, which is independent of any maximum estimate) and NOT a causal/selection proof. Review (architecture+quality+tests, then re-review) verified e\_in, the profile-matched null, the per-code floor, and the cluster bootstrap, and caught+removed the optimizer over-claim. Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.bstar_q6_e3_spectral_concentration status=refuted
Bstar Q6 E3 Spectral Concentration & REFUTED: the bio-intrinsic B*\_Q6 translation-residual basis does NOT concentrate in the canonical lambda=-3 Krawtchouk eigenspace E3 (=H3) of the codon Hamming graph H(3,4); it is significantly ANTI-concentrated there. Setup: the 9 B*\_Q6 within-synonymous zero-sum codon contrasts (K\_AAA, Arg\_AGR, Ile\_AUA, Leu CUN-vs-UUR, Leu UUA-vs-UUG, Ser UCR-vs-AGY, Ser UCA-vs-UCG, Thr ACR-vs-ACY, f3\_stress), defined purely from codon spelling + genetic-code synonymous degeneracy (NO q6/Foldbin/Zeckendorf labels -> non-circular against the pure-math E3), are embedded in R\textasciicircum{}64 (zero on the 3 stops), synonymous-mean-removed, orthonormalized (rank 9). The H(3,4) eigenspace projectors are built exactly via the minimal polynomial (eigenvalues 9,5,1,-3; dims 1,9,27,27; sum P\_Ej = I to max\_abs 0; idempotent; symmetric; matching the chemical-character tensor projectors exactly). Statistic T3 = (1/9) tr(Q\_B\textasciicircum{}T P\_E3 Q\_B) = mean squared cosine of principal angles span(B*\_Q6)<->E3. RESULT: the B*\_Q6 spectral fingerprint is (T0,T1,T2,T3)=(0.000, 0.185, 0.414, 0.401); T3=0.401 is FAR BELOW a triviality-controlled matched-synonymous null (random within-synonymous zero-sum contrasts preserving each template's support + per-fiber zero-sum + L2 norm, 20000 draws) whose mean is 0.528 (null\_z=-5.59, lower-tail p=5e-5); an independent all-family secondary null agrees (mean 0.531). VERDICT REFUTED: the spectral-redirect hypothesis that the highest-frequency canonical lambda=-3 eigenspace H3=E3 predicts/forces the translation-relevant synonymous residual is FALSE -- generic synonymous contrasts concentrate in E3 MORE than B*\_Q6 does, so B*\_Q6 carries a real structural signal but in the LOWER-frequency E1/E2 directions, not E3. SCOPE / not-claimed: this refutes only the E3-concentration hypothesis; the excess B*\_Q6 mass in E1(+0.x)/E2 vs null is a separately-pre-registerable follow-up (NOT claimed here, to avoid garden-of-forking-paths); no causal/selection claim. Verified: structural asserts exact, null fidelity manually audited + adversarially verified (no E2<->E3 mislabel, fair matched null). Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.bstar_q6_eigenspace_concentration_profile status=certified
Bstar Q6 Eigenspace Concentration Profile & CERTIFIED (scoped spectral-localization extremality, NOT forcing isomorphism, NOT causal): the bio-intrinsic B*\_Q6 translation-residual basis (9 within-synonymous zero-sum codon contrasts, defined from codon spelling + genetic-code degeneracy; no q6/Foldbin/Zeckendorf labels -> non-circular against the pure-math eigenspaces) is structurally CONCENTRATED in E1 = the lambda=5, FIRST-ORDER (single-position / additive-character) Krawtchouk eigenspace of the codon Hamming graph H(3,4) (dim 9), beyond a triviality-controlled matched-synonymous null. Pre-registered Bonferroni test (alpha = 0.001/3 over the three nontrivial eigenspaces E1,E2,E3; E0 trivially zero after synonymous-mean removal): B*\_Q6 spectral fingerprint (T0,T1,T2,T3) = (0.000, 0.185, 0.414, 0.401) vs matched-null mean (0.000, 0.083, 0.388, 0.528). EXCESS by eigenspace: E1 +0.102 (null\_z +7.94, upper\_p 5e-5 -- PASSES Bonferroni, CONCENTRATED); E2 +0.026 (z +1.64, upper\_p 0.057 -- NOT significant); E3 -0.128 (z -5.59, lower\_p 5e-5 -- DEPLETED, re-confirms the landed E3-concentration refutation). KEY (non-obvious, null-dependent): B*\_Q6's LARGEST raw component is E2 (0.414), but the EXCESS-over-generic-synonymous-contrasts is in E1 (more than double the generic E1 mass 0.185 vs 0.083) -- the matched null (preserving each template's support + per-fiber zero-sum + L2 norm; 20000 draws) is what isolates the genuine first-order signal from generic synonymous structure. INTERPRETATION: the genetic code's translation-relevant synonymous residual lives in the FIRST-ORDER (additive, single-position) spectral component, not the highest-frequency lambda=-3 that the spectral redirect hypothesized -- biologically consistent with synonymous/wobble effects being largely additive per position. ROBUSTNESS: structural asserts exact (projector dims [1,9,27,27], sum P=I to max\_abs 0, idempotent, symmetric, polynomial projectors = chemical-character tensor projectors); E1 label verified (rank 9, lambda=5, no E1<->E2 swap); matched null is FAIR and reaches E1 on cross-box supports (Arg AGR-vs-CGN, Leu CUN-vs-UUR, Ser UCR-vs-AGY); the E1 excess is basis-wide (survives leave-one-out of the largest contributor f3\_stress -- still above all 5000 null draws), adversarially verified. SCOPE / not-claimed: no forcing isomorphism, no causal/selection mechanism, no cross-organism law (bio's cross-organism data is non-resolving); this certifies only the structural concentration of the residual basis in E1 vs the matched-synonymous null on the standard code. Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.bstar_q6_first_order_position_character status=certified
Bstar Q6 First Order Position Character & CERTIFIED (scoped spectral-localization, NOT causal, NOT forcing): the first-order (E1, lambda=5) spectral excess of bio's B*\_Q6 translation-residual basis localizes to specific codon POSITIONS and chemical CHARACTERS. E1 (dim 9) is decomposed EXACTLY into the 9 single-position chemical-character functions e\_\{p,c\} for codon position p in \{1,2,3\} and dichotomy c in \{chi\_R purine/pyrimidine, chi\_W weak/strong (A,U vs G,C), chi\_K keto/amino\} (verified orthonormal: unit-norm defect 0, Gram off-diagonal 0, sum\_\{p,c\} e\_\{p,c\} e\_\{p,c\}\textasciicircum{}T = P\_E1 to max\_abs 0). For the orthonormalized B*\_Q6 basis (rank 9) the first-order mass per component is scored against the SAME triviality-controlled matched-synonymous null (preserve each template's support + per-fiber zero-sum + L2 norm; 20000 draws), grouped by position and by character, with pre-registered Bonferroni alpha = 0.001/6 over the 3 positions + 3 characters. RESULT (excess over generic synonymous contrasts): POSITION 3 (wobble) M=0.120 vs null 0.060, excess +0.060, z=+8.76, p=5e-5 -> SIGNIFICANT (dominant); POSITION 1 M=0.051 vs 0.016, excess +0.035, z=+4.53, p=5e-5 -> SIGNIFICANT; POSITION 2 z=+1.38 p=0.027 -> NOT significant. CHARACTER chi\_W (weak/strong) M=0.091 vs 0.029, excess +0.063, z=+10.05, p=5e-5 -> SIGNIFICANT (dominant); chi\_R (purine/pyrimidine) excess +0.031, z=+3.47, p=5e-5 -> SIGNIFICANT; chi\_K (keto/amino) z=+1.32 p=0.097 -> NOT significant. Dominant single component = p3\_W (third-position weak/strong) = 0.072. INTERPRETATION: the genetic code's translation-relevant synonymous residual's additive structure concentrates at the WOBBLE (3rd) position -- beyond the already-wobble-heavy synonymous null, so non-trivial -- plus the FIRST position (traceable to B*\_Q6's cross-box contrasts in the 6-fold-degenerate families Arg AGR/CGN, Ser UCN/AGY, Leu CUN/UUR), and is carried by the weak/strong (base-pairing-strength / GC-like) and purine/pyrimidine dichotomies but NOT keto/amino. This is consistent with (but does NOT assert a mechanism for) wobble-pairing and GC/tRNA-availability being the dominant synonymous axes. ROBUSTNESS: E1-basis exact; null apples-to-apples (same basis + orthonormalization for observed and null); consistency sums = T1 exactly; full picture honestly reported (pos2 + chi\_K null); adversarially verified. SCOPE / not-claimed: scoped spectral-localization vs matched-synonymous null on the standard code; no causal/selection mechanism, no cross-organism law, no forcing isomorphism. Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.codon_e1_heldout_crossorganism_conservation status=certified
Codon E1 Heldout Crossorganism Conservation & CERTIFIED (held-out cross-organism, scoped; NOT Window6, NOT causal): the codon-E1 component of the synonymous codon-usage residual is CONSERVED across organisms. For each of 29 organisms spanning 18 genera (public NCBI coding-sequence codon counts; frozen panel synced/codon\_e1\_heldout\_panel.json), d\_resid4,o is the organism's relative codon frequencies over the 61 sense codons with synonymous-family means removed (Q\_syn). The frozen codon-E1 map is B1 = col(Q\_syn R U1) where U1 is the 9-dim first-order (lambda=5) Krawtchouk eigenspace of the codon Hamming graph H(3,4), R restricts to 61 sense codons; after sense-restriction + synonymous-mean-removal B1 has rank 6 (orthogonal projector P\_B1 symmetric/idempotent, trace 6). Per organism r\_o = ||P\_B1 d\_resid4,o||\textasciicircum{}2 / ||d\_resid4,o||\textasciicircum{}2; the clade-weighted statistic T\_heldout = (1/18) sum over genera of the within-genus mean r\_o (equal weight per genus to defuse pseudo-replication -- Burkholderia 7 + Mycobacterium 4 of the 29). RESULT: T\_heldout = 0.571, vs a matched-synonymous-relabel null (a within-synonymous-family codon relabeling applied GLOBALLY to all organisms, preserving family structure + magnitudes + cross-organism dependence, 20000 draws) with mean 0.137 (z=8.75, one-sided p=5e-5); the cluster bootstrap over the 18 genera (10000 resamples) gives lower-95\% excess-over-null = 0.352 > 0. Both pre-registered gate conditions (p<=0.01 AND bootstrap lower-95 excess>0) are met. The per-genus mean r is broadly high (0.63-0.73 across the leading genera), so the conservation is not driven by any single clade. CRITICAL -- this CLEARS the 26/41 chance-projection trap: B1 is 6-dimensional, so the chance projection of a random within-synonymous (\textasciitilde{}41-dim) vector onto B1 is \textasciitilde{}6/41=0.146, which the relabel null reproduces (0.137, independently verified by isotropic-random baseline 0.1464); the real data sits \textasciitilde{}4x above chance. INTERPRETATION: the first-order (additive, single-position; wobble + weak/strong + purine/pyrimidine, per the E1-localization certificate) spectral structure certified on the standard code's table is EMPIRICALLY CONSERVED in real codon usage across 18 mostly-bacterial genera -- a genuine held-out cross-organism validation, where bio's own diffuse full-residual meta was non-resolving (p=0.58). ROBUSTNESS: adversarially verified -- null is a fair chance baseline (independent isotropic + relabel baselines both \textasciitilde{}0.146/0.137), B1 rank-6 exact, d\_resid4 synonymous-mean-zero to roundoff (no organism skipped), panel RNA-sense-only with matching totals, cluster bootstrap correct. SCOPE / not-claimed: this certifies ONLY a conserved codon-E1 component of d\_resid4; it does NOT certify Window6 (per the bridge audit, reopening Window6 requires a NEW independently-defined nonzero structural map, not spectral-order matching -- the Window6 adjacency spectrum is provably disjoint from codon E1, coprime characteristic polynomials forcing the only intertwiner to zero); no causal/selection mechanism; the panel is 29 organisms / 18 genera, mostly bacteria (taxonomic-range caveat). Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.intertwiner_impossibility status=refuted
Intertwiner Impossibility & CERTIFIED IMPOSSIBILITY (certified-negative, pure math): there is NO nonzero intertwiner J: R\textasciicircum{}21 -> R\textasciicircum{}64 satisfying A\_\{H(3,4)\} J = J A\_\{Gamma6\}, where A\_\{H(3,4)\} is the codon Hamming-graph adjacency (64 codons, eigenvalues \{9,5,1,-3\} with multiplicities \{1,9,27,27\}) and A\_\{Gamma6\} is the adjacency of the Window6 graph Gamma6 = the Fibonacci cube on length-6 binary strings with no two consecutive 1s (21 vertices = F8, 38 edges). The homogeneous Sylvester equation A X - X B = 0 has a nonzero solution iff A and B share an eigenvalue, i.e. iff gcd(charpoly(A), charpoly(B)) != 1. Gamma6's integer characteristic polynomial evaluated at each H(3,4) eigenvalue is nonzero: at lambda=9 it is \textasciitilde{}6.65e19, at 5 \textasciitilde{}6.75e13, at 1 it is -144, at -3 it is \textasciitilde{}1.307e8 (equivalently det(A\_Gamma6 - lambda I) = 144, -1.307e8, -6.75e13, -6.65e19 for lambda=1,-3,5,9); Gamma6's spectral radius is 3.90, trivially excluding 9 and 5, and the exact integer evaluations exclude the borderline 1 and -3. The polynomial gcd is a nonzero constant (=1 up to scale), cross-confirming no shared eigenvalue. Therefore the only intertwiner is J=0, and the certified codon-E1 (lambda=5) eigenspace -- indeed EVERY codon adjacency eigenspace -- is NOT an adjacency-spectral image, eigenspace, or quotient of the Window6 graph. CONSEQUENCE: the temptation to connect the bridge's bio-side codon-E1 conservation finding (the conserved first-order/additive translation-residual structure) BACK to Window6 via spectral-order matching is formally closed -- any such comparison (eigenvalue rank, normalized frequency, rescaling) is vacuous, and convention-embedding into Q6 is circular (consistent with the prior 17280-convention audit finding no convention-robust forcing). This STRENGTHENS the scope discipline of the codon-E1 results: they certify genetic-code-internal spectral structure, NOT a Window6<->codon forcing. Reopening a Window6<->codon structural relation requires a NEW, independently-defined nonzero structural map -- not spectral-order matching; edge-hiding contravariance remains the sole certified cross-domain relation. ROBUSTNESS: Gamma6 |V|=21,|E|=38 and H(3,4) eigenspace dims [1,9,27,27] asserted; the no-shared-eigenvalue conclusion is cross-verified two independent ways (the experiment's exact characteristic-polynomial evaluation AND an independent fraction-free Bareiss integer determinant det(A\_Gamma6 - lambda I), matching to the digit). Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.codon_e1_sense_restriction_positional_rank status=certified
Codon E1 Sense Restriction Positional Rank & CERTIFIED (pure math, codon-side sharpening of the E1 results; no Window6, no biology beyond the dimensional statement): consider the first-order (lambda=5) eigenspace E1 of the codon Hamming graph H(3,4), spanned by the 9 single-position chemical-character functions e\_\{p,c\}(codon)=chi\_c(codon[p]) for codon position p in \{1,2,3\} and dichotomy c in \{chi\_R purine/pyrimidine, chi\_W weak/strong, chi\_K keto/amino\}. Let R restrict to the 61 sense codons and Q\_syn remove synonymous-family means (standard code). The composed map M = Q\_syn R U1 (U1 = the 9 E1 columns) has RANK EXACTLY 6, with KERNEL exactly 3-dimensional spanned by \{ e\_\{1,R\} - e\_\{1,W\} + e\_\{1,K\}, e\_\{2,W\}, e\_\{2,R\} - e\_\{2,K\} \} (each maps to the zero vector under M, verified by exact arithmetic). Decomposed by codon POSITION, the image ranks are (position1, position2, position3) = (2, 1, 3): the wobble position (3) retains all THREE first-order degrees of freedom after sense-restriction + synonymous-centering, while positions 1 and 2 collapse to ranks 2 and 1. Decomposed by CHARACTER, the ranks are (chi\_R, chi\_W, chi\_K) = (3, 2, 3): the keto/amino character family chi\_K retains FULL rank 3. CONSEQUENCE: the empirical non-enrichment of chi\_K found in the E1-localization certificate (bridge.genetic\_code.bstar\_q6\_first\_order\_position\_character: chi\_K not significant, z=1.32) is NOT a dimensional artifact -- chi\_K has its full 3 first-order degrees of freedom available in the sense-restricted synonymous-centered space, so its empirical absence is a genuine biological non-enrichment, not a missing dimension. This also confirms the rank-6 reduction underlying the held-out gate's B1 (rank 6) is exactly the (2,1,3) positional / (3,2,3) character structure. ROBUSTNESS: exact (Fraction) arithmetic; kernel basis maps to zero exactly; cross-verified two independent ways (the experiment + an independent orchestrator recompute with its own standard-code table and character definitions, all ranks and kernel matching). Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.dms_codon_escape_conditional_allocation status=refuted
Dms Codon Escape Conditional Allocation & COINCIDENCE (adversarially-verified TRUE null; the flagship functional test of the certified BC7 edge-hiding contravariance): in public deep-mutational-scanning (DMS) fitness data, experimentally-fragile protein sites do NOT preferentially use codons with more one-step synonymous escape routes, beyond what codon composition and optimality already explain. SETUP: from ProteinGym substitution assays (frozen panel synced/dms\_codon\_escape\_panel.json: 144 of 217 assays passing an identity gate -- translated UniProt/ENA CDS exactly contains the assay target\_seq under the standard nuclear code -- yielding 119 distinct proteins, 27 organisms, 6307 measured Leu/Arg/Ser sites, the only amino acids whose synonymous Hamming-1 escape degree h varies within-family), site intolerance I = negative mean standardized DMS over nonsynonymous substitutions. The oracle-mandated VALID estimator is a CONDITIONAL codon-choice logit (the within-stratum codon-permutation null is invalid -- it destroys the intolerance-optimality association the null must condition on): for each site the choice set is the synonymous codons of its amino acid, the chosen alternative is the native codon, and the utility is alpha\_codon + I\_i*(beta\_h h(c) + beta\_g GC(c) + beta\_r RSCU\_org(c)) with organism x amino-acid x codon baselines absorbed, testing beta\_h>0. RESULT: beta\_h = 0.0154 (SE 0.031, Wald two-sided p=0.62 / one-sided 0.31), cluster-bootstrap over the 119 proteins lower-95\% below zero, 95\% CI \textasciitilde{}[-0.045,0.076] -- a well-powered near-zero effect. The estimate is IDENTIFIED (collinearity of h with composition only 0.35, far below the 0.95 non-identification gate) and the model is NOT degenerate: the POSITIVE CONTROL beta\_RSCU = 0.068 is SIGNIFICANT (z=2.41, two-sided p=0.016), i.e. intolerant sites DO bias codon choice -- toward more OPTIMAL codons (translational-accuracy selection), NOT toward higher-escape codons. CONCLUSION: edge-hiding (synonymous escape topology) has NO detectable functional codon-allocation signature in DMS fitness data beyond composition and optimality; the certified BC7 edge-hiding contravariance therefore remains a STATIC genetic-code-table extremality with no demonstrated functional deployment at fragile sites. HONEST SCOPE (oracle-mandated): this is a null beyond a preregistered composition+optimality model; it does NOT (and an observational DMS study cannot) prove h is non-causal -- a within-site randomized synonymous-recoding intervention matched for decoding + local context would be needed for any causal claim; tAI/decoding-accuracy (tRNA) and dinucleotide/dMFE context are documented sensitivity extensions, not expected to revive an already-null marginal (raw marginal covariance p=0.23). NOT a Window6 result. ROBUSTNESS: triple-confirmed (raw marginal p=0.23; optimality-residualized sensitivity coincidence; valid conditional logit p=0.31) and adversarially verified as a TRUE null (positive control significant, h varies + identified, choice-set/native-chosen correct with 0 mismatches over 6307 sites, intolerance non-degenerate). Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.codon_e1_conservation_transportability status=certified
Codon E1 Conservation Transportability & CERTIFIED (held-out, scoped E1-transportability; extends \#18, NOT Window6, NOT causal): the first-order codon-E1 structure of the synonymous codon-usage residual is conserved NOT ONLY in bacteria (the landed \#18 result: 29 organisms / 18 genera, T=0.571) but INDEPENDENTLY in archaea AND in eukaryotes, analyzed as SEPARATE external strata (never pooled). From Kazusa CUTG standard-nuclear-code (genetic code table 1) per-species codon usage (frozen panel synced/codon\_e1\_transport\_panel.json: 12 archaea + 14 eukaryotes), each organism's d\_resid4 = relative codon frequencies over the 61 sense codons with synonymous-family means removed; r\_o = ||P\_B1 d\_resid4,o||\textasciicircum{}2/||d\_resid4,o||\textasciicircum{}2 onto the SAME codon-E1 subspace B1 = col(Q\_syn R U1) (rank 6, U1 = first-order lambda=5 H(3,4) eigenspace) used in \#18; T = organism-mean r\_o per domain; the null is the same matched-synonymous-relabel control (global within-synonymous codon relabel, 20000 draws). RESULT: ARCHAEA (n=12) T=0.522 vs null mean 0.137 (z=9.62, one-sided p=5e-5), cluster/organism bootstrap lower-95\% excess 0.294>0 -> conserved; EUKARYOTA (n=14) T=0.515 vs null mean 0.140 (z=8.80, one-sided p=5e-5), bootstrap lower-95\% 0.270>0 -> conserved. Both domains independently clear the pre-registered gate; the chance projection onto the 6-dim B1 of a within-synonymous (\textasciitilde{}41-dim) vector is \textasciitilde{}6/41=0.146, which the null reproduces, and the data sits \textasciitilde{}3.5x above it. INTERPRETATION: combined with \#18 (bacteria), the genetic code's first-order (additive, wobble-dominated) translation-residual structure is UNIVERSALLY conserved across ALL THREE DOMAINS OF LIFE (bacteria, archaea, eukaryotes), at \textasciitilde{}0.51-0.57 of each organism's synonymous-residual norm vs \textasciitilde{}0.146 chance. SCOPE / not-claimed: this is E1-transportability (a generalization of \#18 to new domains), NOT Window6 (the Window6 spectrum is provably disjoint from codon E1); NOT a causal/selection mechanism; standard-nuclear-code species only (alternative-code taxa excluded). ROBUSTNESS: adversarially verified -- Kazusa codon counts are genuine per-species tables (live re-fetch matched all 26 taxids exactly; 61 RNA sense codons, stops excluded, nonuniform real counts), all genetic\_code\_table=1 with no alternative-code contaminants, the null is fair (\textasciitilde{}chance baseline, independent draws reproduce), the SAME rank-6 B1 as \#18, archaea and eukaryota analyzed strictly separately. Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.translation_kernel.golden_fibonacci_impossibility status=refuted
Golden Fibonacci Impossibility & REFUTED (correspondence-negative; closes bridge direction C, the dynamical-kernel Fibonacci program): no biology-defined dynamical translation kernel K\_theta (an operator pushing an ORF through codon->tRNA-decoding->protein) can carry the actual Fibonacci recurrence $x^2-x-1$ (golden, root phi=(1+sqrt5)/2\textasciitilde{}1.618) in its characteristic polynomial / spectrum / trace recurrence. A biologically legitimate discrete decoding kernel is a nonnegative column-(sub)stochastic matrix P, whose spectral radius rho(P)<=1; since phi>1, $x^2-x-1$ cannot divide chi\_P (divisibility would require phi as an eigenvalue). A CTMC generator Q (nonneg off-diagonal, <=0 column sums) has all eigenvalues with non-positive real part, so phi cannot appear there either, and its eigenvalues carry inverse-time units so a unit-free integer polynomial is not an invariant. The companion/golden matrix [[1,1],[1,0]] realizing $x^2-x-1$ requires an UNNORMALIZED two-state path-counting quotient with a no-consecutive-state rule -- an imposed Window6 automaton biology does not supply. Oracle-derived (spectral-radius argument) + codex-implemented + orchestrator re-ran. SCOPE: closes the dynamical-Fibonacci route; does NOT touch the certified edge-hiding extremality (the sole certified Window6<->codon relation, a static code-table functional not a dynamical operator). REOPENING CONDITION: a biology-defined, parameter-independent two-dimensional invariant quotient with a canonically integer-valued, unit-invariant transfer matrix whose characteristic polynomial is forced to $x^2-x-1$ for every admissible theta -- not currently specifiable. Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.edge_hiding_e1_orientation_same_signal status=refuted
Edge Hiding E1 Orientation Same Signal & REFUTED (operational independence of the two certified bridge positives): the static code-table edge-hiding extremality (e\_in / BC7) and the live codon-E1 synonymous codon-usage residual are NOT the same structural signal. They share the |R|=13 support set, but that equality is design-induced (the three sixfold families canonically give 18 codons in three 4+2 branches, not 13) and is excluded from the statistic. CRITICAL anti-tautology guard (oracle-identified): the E1 projector satisfies (P\_1)\_\{cc'\}=(9-4 d\_H)/64, hence tr(U1\textasciicircum{}T S\_g U1)=(5/32) e\_in(g) EXACTLY for every code -- the scalar 'edge-hiding has E1 mass' is algebraically guaranteed and is NOT evidence. The test deflates this: C\_g=U1\textasciicircum{}T S\_g U1-(5 e\_in/288) I\_9; B\_g=P C\_g P-(tr(P C\_g P)/6) P (trace-deflated orientation operator in the rank-6 identifiable E1 subspace, P=L\textasciicircum{}+L, L=Q\_syn R U1); bio side a\_o=L\textasciicircum{}+ d\_resid4,o, Sigma\_bio=sum\_o w\_o a\_o a\_o\textasciicircum{}T/||a\_o||\textasciicircum{}2 (equal-genus, equal-domain); statistic T\_same=tr[Sigma\_bio B\_g\textasciicircum{}2/tr(B\_g\textasciicircum{}2)]. Null = exact Omega\_\{BW,E,d,comp\} (162468 states; conditions OUT e\_in + box/wobble + edge-margins + boundary-degrees + component-structure), recomputing B\_g per code while holding the measured Sigma\_bio fixed. RESULT (per domain, separately): bacteria T=0.1920 vs null mean 0.1981 (upper-tail p=0.984, bootstrap lo95 excess -0.024); archaea T=0.1733 vs null 0.1761 (p=0.677, lo95 -0.014); eukaryota T=0.1726 vs null 0.1722 (p=0.443, lo95 -0.023). No domain shows exceptional positive orientation alignment; all three bootstrap lower-95\% excess < 0. INTERPRETATION: edge-hiding is static code-table geometry; codon-E1 is an independent live usage residual -> the bridge holds TWO INDEPENDENT certified positives, not one. SCOPE: bio-internal (relates two genetic-code positives), does NOT reopen Window6. ROBUSTNESS: adversarially verified -- algebraic-identity sanity check passed exactly (tr=10.46875=(5/32)e\_in), |R|=13 excluded from the statistic, exact 162468-state null, deflation faithful to the oracle formula, no scratch-file dependency, numpy-free. Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.codon_e1_frameshift_mpra_translation_dependent_function status=coincidence
Codon E1 Frameshift Mpra Translation Dependent Function & COINCIDENCE (well-controlled, CSC-validity-gated honest negative; scope: codon biology, NOT Window6, NOT causal-certified): the universal codon-E1 direction (first-order/wobble synonymous codon-usage residual, conserved across all 3 domains of life) has NO detected GC-orthogonal TRANSLATION-DEPENDENT functional effect on mRNA level in the Chen-Park-Subramaniam 2024 yeast 8x-dicodon reporter. METHOD: bridge reconstructed per-codon-pair mRNA effects from raw SRA (PRJNA974090) for the in-frame (pHPSC1142) and -1 frameshift (pHPSC1114) libraries by faithfully reproducing the rasilab counting+linkage pipeline (validated: known destabilizing dipeptides KK/RR/KW/proline sit in the bottom in-frame tail and the -1 frameshift relieves them, Delta up to +1.6; non-destabilizer AA positive). Translation-dependent contrast Delta\_y=y\_inframe-y\_frameshift per codon-pair. v\_star in R\textasciicircum{}61 = leave-S.cerevisiae-out cross-domain codon-E1 consensus (54 non-yeast organisms / 41 genus groups, rank-6 B1 subspace, sign fixed externally -> non-circular, does not use the yeast reporter). T\_causal=weighted corr of residualized(Delta\_y, s\_cd=(v\_star[c]+v\_star[d])/2) against control matrix Z; within-synonymous-family permutation null (100k), sign-invariant two-sided; mandatory CSC assay-validity gate. RESULT: baseline Z (in-frame aa-pair FE + -1-frame aa-pair FE + yeast CSC both frames + yeast tAI both frames + read-depth) gives |T|=0.154, two-sided p=0.0052, bootstrap CI [-0.221,-0.085] (excludes 0); ADDING GC + GC3 + the 16 dinucleotide sequence controls COLLAPSES it to |T|=0.029, p=0.605, bootstrap CI [-0.090,+0.032] (includes 0), with s still estimable (residual frac 0.185, control rank 9->24). CSC positive control passed (corr 0.157, p 0.0013) -> the assay genuinely detects translation-dependent stability, so this is a real, powered null. INTERPRETATION: the apparent codon-E1 reporter association is the GC/RNA-secondary-structure (Delta-MFE) composition confound -- which is FRAME-INVARIANT and therefore NOT removed by the in-frame-minus-frameshift subtraction -- not a codon-E1 translation-dependent effect; codon-E1 remains a conserved usage GEOMETRY without a detected direct functional phenotype beyond composition. PROCESS: an sshx adversarial panel (2 needs\_derivation / 1 coincidence) caught that the original one-sided test was ill-posed (arbitrary v\_star sign) AND that the omitted Delta-MFE/GC confound could manufacture the magnitude; the GC3+dinucleotide control was the decisive numpy-free fix that resolved it to a clean coincidence. NOT-CLAIMED: an exact ViennaRNA-Delta-MFE-controlled + within-site randomized synonymous-recoding (MAVE) CAUSAL test (relayed to bio as needs\_bio\_data) could still detect a GC/folding-orthogonal effect; this result bounds the OBSERVATIONAL public-reporter reach only, and is not Window6 (the structural fibonacci<->codon frontier is separately closed). Evaluated only by its registered deterministic experiment. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.fibonacci_tracka.wobble_boundary_vs_codon_e1 status=needs_derivation
Wobble Boundary Vs Codon E1 & needs\_derivation (distinct bio fact; the bridge houses fibonacci\_reality's homeless Track-A result + bounds it). fibonacci\_reality's Track-A codon-wobble-boundary-response (deliberately math-free, ' phi/571/Z6 preset') reproduces as a REAL cross-organism bio signal: codons whose Watson-Crick cognate tRNA is ABSENT (wobble-decoding-required) show a tRNA-supply boundary response (yeast delta\_r2=0.0198, null percentile 1.0, n=5785; human delta\_r2=0.0047, null percentile 1.0, n=18974). Bridge adjudication vs its certified positives (run\_fibonacci\_tracka\_wobble\_vs\_codon\_e1.py): the pooled per-codon Track-A signal is NOT significantly concentrated in codon-E1 (rho\_E1=0.183 vs synonymous-relabel null mean 0.104, upper-tail p=0.113; rank-B1 chance 6/41=0.146), and overlaps the edge-hiding |R|=13 set at only 3 codons (CUC,CUG,CUU) vs degeneracy-matched null 3.82 (p=0.91, chance-level). The Window6 connection is q6\_circular: Track-A is bio-internal codon/tRNA-supply structure; treating its wobble coincidence as Window6 evidence would reuse the already-closed q6 convention (the Gamma\_6/H(3,4) intertwiner is 0; measure-level maps orbit-average to 0). CONCLUSION: Track-A is a DISTINCT, real, conserved bio supply-boundary fact that (a) is not the same signal as codon-E1 or edge-hiding under matched nulls and (b) does not establish a new non-circular math<->bio Window6 link. SCOPE: bio-internal codon biology; NOT Window6 forcing; reopening still requires the oracle's no-adjacent-one mechanism condition. The bridge correctly serves as the venue for this math-pipeline-originated bio result that fibonacci's math-only paper cannot house. Evaluated only by its registered deterministic experiment. & \texttt{needs\_derivation} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.codon_e1_alternative_code_deformation status=coincidence
Codon E1 Alternative Code Deformation & Codon-E1 alternative-code deformation: tests whether the first-order/wobble codon-E1 synonymous-usage residual is code-table-specifically conserved across NCBI alternative genetic-code mitochondrial genomes (vertebrate-mito table 2, invertebrate-mito table 5), using per-table B1\_t = col(Q\_syn,t R\_t U1), matched within-synonymous-family relabel nulls (20000), a code-specific-vs-standard-restricted strong-version Delta T, and lineage-cluster bootstrap (10000). Deepens the certified codon-E1 three-domain conservation; explicitly NOT Window6 forcing. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.codon_e1_trna_decoding_supply_orientation status=refuted
Codon E1 Trna Decoding Supply Orientation & Codon-E1 tRNA decoding-supply orientation certificate: tests whether the synonymous codon-usage residual E1 direction aligns, within the same RefSeq complete bacterial/archaeal genome accession, with that genome's tRNA anticodon decoding-supply E1 direction (frozen standard-code rank-6 B1, reused not refit; cos statistic; genus-weighted; global + restricted synonymous relabel nulls; full-residual positive control; B1-minus-p3W + GC3 collapse gate). Deepens the certified codon-E1 result as a mechanism-orientation probe; decoding-supply orientation concordance only, NOT Window6 forcing, NOT causal proof. & \texttt{refuted} \\
\end{longtable}

\subsection{Cardinality Forcing}\label{sec:bridge-cardinality-forcing}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.cardinality_forcing status=coincidence
\textbf{Statement.} The codon-Q6 boundary cardinality ($|R|=13=F_7$, 21 synonymous families) is FORCED by the Window6
Fibonacci horizon ($X_6=21=F_8$) via an explicit structural map standard-code-boundary -> width-6
Fibonacci words, not coincidence.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The counts 21 and 13 match the width-6 and width-5 Fibonacci cubes, but the tested canonical
biological structures do not yield a structure-respecting forcing map.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc1\_cardinality\_forcing.py

\subsection{Spectral Correspondence}\label{sec:bridge-spectral-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.spectral_correspondence status=refuted
\textbf{Statement.} The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, mu) equal the Window6 coarse Markov
kernel spectrum (high/low energy superposition).

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} bio spectral scalars are not the coarse Markov kernel spectrum (subdominant eig do not match lambda;
mu>1 is not a stochastic eigenvalue).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc2\_markov\_kernel\_spectrum.py

\subsection{Foldbin Codon Positions}\label{sec:bridge-foldbin-codon-positions}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.foldbin_codon_positions status=coincidence
\textbf{Statement.} The Window6 Foldbin tail-cube (3 free Boolean coords, seam-return $\rho_6-s_6=3$) corresponds to the
3 codon positions / wobble folding; the bio $Q^{(2)}$ second-order structure is the biological image
of the second fold.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} foldbin tail-cube vs codon positions: 3=3 count match only; weight-order vs
value-window-marginal-order vs reverse-constraint give 3 different assignments (no forcing); 8-state
reading fails (only 4 tail states); $Q^{(2)}$ abundance reading failed (Route B).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc3\_foldbin\_codon\_correspondence.py

\subsection{Killed Walk Spectral Correspondence}\label{sec:bridge-killed-walk-spectral-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.killed_walk_spectral_correspondence status=refuted
\textbf{Statement.} The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, $\mu=6\lambda_M-1$) -- where lambda =
(1/6)*spectral radius of the induced 6-cube subgraph adjacency on R and on M=median-closure(R) --
are structurally forced by the Window6 Fibonacci-cube killed-walk operator (same induced-subgraph
spectrum / canonical map), not a numeric coincidence.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} BC2-R1 is refuted: the operators have the same induced-subgraph killed-walk form, but the bio sets
and Window Fibonacci cubes are not the same cube-embedded graph, their exact adjacency
characteristic polynomials differ, and the bio radial factor does not occur in the Window graph
spectra.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc2\_killed\_walk\_spectrum.py

\subsection{Cyclic Sector Frame Symmetry}\label{sec:bridge-cyclic-sector-frame-symmetry}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.cyclic_sector_frame_symmetry status=refuted
\textbf{Statement.} Window6 cyclic Zeckendorf sector rotation corresponds to cyclic rotation of the 3 codon positions as
a code-label-preserving sector character.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} codon-position rotation preserves the amino-acid/stop label for only 4/64 codons (UUU,CCC,AAA,GGG)
and positional degeneracy is strongly asymmetric
(pos1\textasciitilde{}0.042,pos2\textasciitilde{}0.010,pos3\textasciitilde{}0.667), so no
code-label-preserving rotation image exists.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc4\_cyclic\_sector\_frame\_symmetry.py

\subsection{Right Boundary Selection Axis}\label{sec:bridge-right-boundary-selection-axis}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.right_boundary_selection_axis status=coincidence
\textbf{Statement.} The Window6 right-boundary/foldbin escape functional $U_R$ (cyclic endpoint collision $w_1=w_6=1$,
escape ledger |d$U_R$|/(6|$U_R$|)=8/9) maps onto the BIOLOGICAL low-optimality boundary axis --
R=tRNA-poor (BIO-M) and the unexplained universal residual axis $d_{\perp}$ (BIO-P) -- via
sign/rank/projection constraints, NOT via naked cardinalities (|R|=13, 21, 3).

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Window6 $U_R$ is reconstructed from origin/feat/fibonacci\_reality-deepening
window6-green-spectral-response-alpha-readout-frontier.tex and
window6-markov-kernel-coarse-spectrum-fibonacci-leading.tex: X6 boundary $w_1=w_6=1$, Foldbin tail
constraints, and cutoff value<=63 give $U_R$ labels 001110,010001,010011,110000,110011,110101.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc5\_right\_boundary\_selection\_axis.py

\subsection{Fold Residual Wobble Correspondence}\label{sec:bridge-fold-residual-wobble-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fold_residual_wobble_correspondence status=coincidence
\textbf{Statement.} Window6's Fold6 hidden tail-offset residual modes and the standard code's third-position wobble
residual modes are the same kind of hidden residual in a shared 'regular micrograph -> visible
family -> hidden residual' fold-forcing mechanism (low-energy = first/second-position core,
high-energy = third-position/tail residual): they leave visible family/type invariant but govern
fiber multiplicity, lumpability failure, and quotient-kernel subleading spectrum -- tested by
label-free invariants + hidden-mode alignment under graph/wobble-preserving nulls, NOT by the 64->21
count or any 21-label pairing.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} BC6 verdict: STRONG is refuted.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc6\_fold\_residual\_wobble.py

\subsection{Edge Isoperimetric Contravariance}\label{sec:bridge-edge-isoperimetric-contravariance}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.edge_isoperimetric_contravariance status=certified
\textbf{Statement.} Window6 Fold6 and the standard genetic code realize OPPOSITE achievable extremes of one label-free
local-edge-hiding functional e\_in (one-step micrograph edges hidden inside a family): Window6 is
the exact separating endpoint (e\_in=0=e\_min, tau\_W=0; every Q6 1-bit edge changes the stable
type) and the standard code is the near-maximal tolerant endpoint (e\_in=69, e\_max=72,
tau\_C=0.958), surviving graph- and wobble/box-preserving nulls. This is a C-0
contravariant-extremality certificate: it does NOT claim the two are isomorphic folds, that 64->21
is biologically meaningful, that high/low-energy maps to DNA/RNA/translation, or any common
fold-FORCING law.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Fold6 table copied from origin/feat/fibonacci\_reality-deepening forced\_window\_structure;
reconstructed from the finite no-adjacent Zeckendorf representatives on 0..63 as a self-check.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc7\_edge\_isoperimetric\_contravariance.py

\subsection{Parry Measure Usage Correspondence}\label{sec:bridge-parry-measure-usage-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.parry_measure_usage_correspondence status=coincidence
\textbf{Statement.} The Window6 Parry measure (golden maximal-entropy stationary measure of the no-adjacent-one transfer
operator) corresponds, per-codon in the Q6 frame, to the codon usage-frequency selection axis
($d_{\perp}$) -- tested by rank/projection vs label-free nulls, not by any label pairing.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Parry measure reconstructed from origin/feat/fibonacci\_reality-deepening
forced\_window\_structure/window6-parry-measure-golden-maximal-entropy.tex and
window6-transfer-operator-golden-perron-spectrum.tex: transfer matrix [[1,1],[1,0]], Perron root
phi, transition kernel [[1/phi,1/phi\textasciicircum{}2],[1,0]], stationary distribution
(phi\textasciicircum{}2/(phi\textasciicircum{}2+1),1/(phi\textasciicircum{}2+1)).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_parry\_usage\_correspondence.py

\subsection{Smith Normal Form Cokernel}\label{sec:bridge-smith-normal-form-cokernel}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.smith_normal_form_cokernel status=refuted
\textbf{Statement.} The Smith-normal-form cokernel (finite abelian group invariant) of the Window6 edge matrix is
isomorphic to that of the canonical codon adjacency/quotient matrix -- a label-free
group-isomorphism test.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} Window6 uses the finite four-cell edge-flux matrix from the Fibonacci branch and has coker torsion
Z/3Z + Z/3450Z.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_smith\_normal\_form\_cokernel.py

\subsection{Three Rigidity Codon Graph}\label{sec:bridge-three-rigidity-codon-graph}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.three_rigidity_codon_graph status=refuted
\textbf{Statement.} The Window6 type-quotient graph and the codon synonymous family-quotient graph are
rigidity-equivalent (share canonical label-free rigidity invariants: automorphism group, degree
sequence, spectrum, distance profile) beyond a degree-preserving null.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The two quotient graphs share 21 nodes, diameter 3, WL-discrete color refinement, and trivial
automorphism group.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_three\_rigidity\_codon\_graph.py

\subsection{Mod3 Flux Frame Obstruction}\label{sec:bridge-mod3-flux-frame-obstruction}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.mod3_flux_frame_obstruction status=coincidence
\textbf{Statement.} The Window6 edge-flux mod-3 obstruction class in H\textasciicircum{}1(G\_W;Z3) is cohomologically of
the same type as the codon reading-frame/f3-ramp three-periodicity phase class, tested by Z3-Hodge
signature (r3,T3,phase,cycle-integrals) up to D3 vs frame/wobble/family nulls.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Both sides have nonzero mod-3 cohomology classes and compatible nonzero cycle integrals, but the
codon harmonic fraction is not extreme against every stricter codon null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc10\_mod3\_flux\_frame\_obstruction.py

\subsection{Partition Quotient Wobble Foldbin}\label{sec:bridge-partition-quotient-wobble-foldbin}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.partition_quotient_wobble_foldbin status=coincidence
\textbf{Statement.} The Window6 (Fold6, Zeckendorf-cylinder) partition pair and the codon (16-box, synonymous-family)
partition pair have the same label-free partition-lattice signature (meet/join sizes, VI, equitable
defect, boundary profile, quotient SNF) beyond size/graph-preserving nulls.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Window6 partitions are reconstructed from origin/feat/fibonacci\_reality-deepening
forced\_window\_structure: Fold6 is the visible six-prefix fiber; Zyl is the finite
Zeckendorf/Foldbin tail-offset cylinder partition on the same 0..63 value carrier.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc11\_partition\_quotient\_wobble\_foldbin.py

\subsection{Green Spectral Codon Q Spectra}\label{sec:bridge-green-spectral-codon-q-spectra}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.green_spectral_codon_q_spectra status=refuted
\textbf{Statement.} The Window6 Green/resolvent spectrum has the same label-free spectral signature (density, gap,
moments) as the codon q1/q3/q4 codon-cube quotient spectra beyond a degree-preserving null.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The Window Green/resolvent object is a four-state reversible Markov spectrum, while the codon
q1/q3/q4 coface-affinity spectra are 21-family quotient spectra.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc13\_green\_spectral\_codon\_q\_spectra.py

\subsection{Edge Exhaustion Meta Certificate}\label{sec:bridge-edge-exhaustion-meta-certificate}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.edge_exhaustion_meta_certificate status=certified
\textbf{Statement.} Within the pre-registered label-free invariant battery (tau plus
spectral/Parry/SNF/mod3/partition/rigidity), the ONLY certified Window6<->codon cross-domain
relation is the contravariant local-edge-hiding extremality (tau\_W=0 separating endpoint,
tau\_C=0.958 tolerant endpoint); conditioned on this e\_in extremality and native graph/fiber
constraints, all remaining invariants have residual alignment indistinguishable from a conditional
null (operational independence). The realization mechanisms are polarized (Window6 isotropic
separator, codon anisotropic third-position tolerator). NOT an isomorphism, NOT a forcing, NOT
strict C-1 profile duality.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} This is not a structural isomorphism, not a 64-to-21 numerology claim, and not a biological-origin
claim.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_meta\_edge\_exhaustion\_certificate.py

\subsection{Fourcell Selection Enrichment}\label{sec:bridge-fourcell-selection-enrichment}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fourcell_selection_enrichment status=refuted
\textbf{Statement.} The Window6 4-cell coarse partition (sizes 27/22/9/6) explains significant non-random variance in
the real 4D codon selection vectors (tRNA/f3/$d_{\perp}$/optimal) beyond cell-size-preserving and
GC-conservative nulls.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The four-cell partition does not explain more 4D selection variance than strict same-size random
partitions.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bcd\_fourcell\_selection\_enrichment.py

\subsection{Fold Structure 64 16 25 21}\label{sec:bridge-fold-structure-64-16-25-21}
% CLAIM_DETAIL claim_id=bridge.genetic_code.fold_structure_64_16_25_21 status=certified
\textbf{Statement.} The standard genetic code is an exact two-layer 64->16->25->21 fold (16 doublet boxes = 8 pure + 7
binary-split + 1 tri-split; 25 box-output fragments minus 4 cross-box merges = 21 classes;
degeneracy spectrum
1\textasciicircum{}2,2\textasciicircum{}9,3\textasciicircum{}2,4\textasciicircum{}5,6\textasciicircum{}3)
-- the hard biological instance of the abstract microstate->block->seam->stable fold pattern. NOT a
golden-ratio forcing; Zeckendorf decompositions universal/presentational; ratios are
ratio-of-counts.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} The standard genetic code is certified here as a 64->21 two-layer fold with bulk(8 pure box)/seam(7
split)/defect(1 tri-split) stratification and exact degeneracy spectrum; Zeckendorf and small-ratio
observations are explicitly flagged non-forcing.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_genetic\_code\_fold\_cert.py

\subsection{Neutral Hamming Adjacency}\label{sec:bridge-neutral-hamming-adjacency}
% CLAIM_DETAIL claim_id=bridge.genetic_code.neutral_hamming_adjacency status=certified
\textbf{Statement.} The standard code's per-codon Hamming-1 neutral-neighbor histogram is exactly 0:2,1:22,2:8,3:28,4:4
(merged defect = 28/22/8/6), close to but not equal to the Window6 27/22/9/6; no natural untuned
biological weighting bridges them.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} No fixed natural weighting tested here turns the exact 28/22/8/6 neutral-adjacency profile into
27/22/9/6.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_neutral\_adjacency\_cert.py

\subsection{Fold Order Parameter Usage}\label{sec:bridge-fold-order-parameter-usage}
% CLAIM_DETAIL claim_id=bridge.genetic_code.fold_order_parameter_usage status=coincidence
\textbf{Statement.} The fold order parameter m\_bio = P\_bulk - P\_seam computed from real multi-organism codon usage
carries an independent (non-GC) cross-organism signal.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} m\_bio is dominated by GC3 and shows no GC-controlled residual signal; this is a GC proxy
coincidence rather than an independent fold signal.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_usage\_projection\_cert.py

\subsection{Neutral Trna Edge Coupling}\label{sec:bridge-neutral-trna-edge-coupling}
% CLAIM_DETAIL claim_id=bridge.genetic_code.neutral_trna_edge_coupling status=coincidence
\textbf{Statement.} The certified Window6 edge score modulates the tRNA supply-demand alignment on synonymous Hamming-1
edges (usage-edge-gradient \textasciitilde{} tRNA-edge-gradient x Window6-edge-score, interaction
beta\_bridge), beyond GC/f3 and edge-permutation nulls, with held-out-species predictive lift.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} AUTHORITATIVE per-species GtRNAdb test (degeneracy fixed, TH\_norm=55.5): beta\_T=0.73 real but
beta\_bridge=0.031 fails CV + all 3 nulls.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_claim20\_per\_species\_trna\_edge\_coupling.py

\subsection{Twolayer Fold Edge Hiding Extremality}\label{sec:bridge-twolayer-fold-edge-hiding-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.twolayer_fold_edge_hiding_extremality status=certified
\textbf{Statement.} The contravariant edge-hiding extremality iterates across the genetic code's two-layer fold: the
first-two-base box partition (64->16) is the EXACT maximum-edge-hiding partition for its size-4x16
block profile (tau\_box=1.0), and the family layer (64->21) is near-maximal (tau\_family=0.958),
both in the extreme upper tail of a same-block-size null; Window6 Fold6 sits at the opposite extreme
(tau=0). Restricted to iterated edge-hiding extremality; NOT isomorphism or biological forcing.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Statement is restricted to iteration of two edge-hiding extremes: the first-two-base box layer
attains the exact size-4x16 maximum, and the family layer is within three edges of the exact
family-profile maximum while lying in the extreme upper tail of the same-size null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_twolayer\_fold\_extremality.py

\subsection{Foldlayer Zigzag Extremality}\label{sec:bridge-foldlayer-zigzag-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.foldlayer_zigzag_extremality status=certified
\textbf{Statement.} The standard genetic code realizes a zig-zag edge-hiding fold layer B <- M=B meet F -> F: the
first-two-base box layer and box-family fragment layer are exact edge-hiding endpoints, while the
family layer is near-maximal with a small deficit. This is a finite label-free extremality
certificate, not a monotone quotient tower, isomorphism, biological forcing, or live-usage claim.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Certified zig-zag extremality, not a monotone quotient tower: 64->16 box and 64->25 box-family
fragments are exact edge-hiding extrema; the 21-family layer is near-max with deficit 3.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc15\_foldlayer\_zigzag\_extremality.py

\subsection{Halfbox Fibonacci Tower}\label{sec:bridge-halfbox-fibonacci-tower}
% CLAIM_DETAIL claim_id=bridge.genetic_code.halfbox_fibonacci_tower status=coincidence
\textbf{Statement.} The genetic-code halfbox and family-meet layers create exact edge-hiding endpoint facts and a strict
Fibonacci tower across the naturally generated block counts 13, 21, 34, and 55 under canonical
finite partition operations. The claim must separate hard extremality facts from numerical tower
appearances.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} BC16 exact halfbox extremality is a hard endpoint fact; BC17 is not a certified Fibonacci tower
because only 21 and 34 appear naturally under the preregistered canonical operations.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc1617\_halfbox\_fibonacci\_tower.py

\subsection{Fold Tower Separating Extremality}\label{sec:bridge-fold-tower-separating-extremality}
% CLAIM_DETAIL claim_id=bridge.window6.fold_tower_separating_extremality status=refuted
\textbf{Statement.} The Window fold tower is edge-separating at each tested finite width under the same value-window
convention that reproduces the Window6 certificate, yielding a cross-width revealing endpoint dual
to the genetic-code hiding tower.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The tower-wide separating claim fails under the finite value-window convention that reproduces the
required Window6 histogram: widths 4,5 have e\_in>0.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_window\_fold\_tower\_extremality.py

\subsection{Edge Defect Axis Cert}\label{sec:bridge-edge-defect-axis-cert}
% CLAIM_DETAIL claim_id=bridge.genetic_code.edge_defect_axis_cert status=coincidence
\textbf{Statement.} The standard genetic code's local-edge-hiding deficit is exactly e\_max-e\_in=3, supported only on
Stop and Ser, with Stop contributing one punctuation defect and Ser contributing two sense split
defects. The sense-side SerSplitDefect vector should project onto an available codon-Q6 selection
axis, especially an independent fourth residual axis if present.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The Stop/Ser edge-defect decomposition is exact and graph-theoretically sharp, but the currently
delivered selection-vector packet has no independent d\_resid4 axis and the Ser split defect does
not certify a projection onto the available axes.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_edge\_defect\_axis\_cert.py

\subsection{Edge Defect Projection Harness}\label{sec:bridge-edge-defect-projection-harness}
% CLAIM_DETAIL claim_id=bridge.genetic_code.edge_defect_projection_harness status=certified
\textbf{Statement.} The SerSplit edge-defect projection audit has a deterministic 61-sense-codon harness: it builds
forbidden controls from available codon-Q6 selection and codon-chemistry columns, residualizes
candidate axes and the SerSplit contrast through the same matrix, detects only the forbidden
positive control, and does not promote oracle output or local controls into biological evidence.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Projection harness is operational: forbidden negative controls collapse or do not pass, and the
forbidden SerSplit positive control is detected.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_edge\_defect\_projection\_harness.py

\subsection{Ser Island Fidelity Cost}\label{sec:bridge-ser-island-fidelity-cost}
% CLAIM_DETAIL claim_id=bridge.genetic_code.ser_island_fidelity_cost status=coincidence
\textbf{Statement.} The Ser UCN/AGY split creates a code-table near-cognate error-cost asymmetry: UCN and AGY have
different one-step missense/stop neighborhoods, and this asymmetry could seed a translation-fidelity
selection route only if it is exceptional beyond matched codon-family nulls and later survives
site-level expression/mistranslation tests.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Ser UCN has higher mean visible near-cognate cost than AGY in the deterministic amino-acid-property
audit, but the delta is not exceptional under matched random codon partitions and is comparable to
other sixfold-family four-versus-two splits; this remains a mechanistic candidate rather than a
certificate.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_ser\_island\_fidelity\_cost.py

\subsection{Pack}\label{sec:bridge-pack}
% CLAIM_DETAIL claim_id=bridge.genetic_code.synthetic.negative.control.axis.pack status=needs_derivation
\textbf{Statement.} Use only in-repo deterministic codon features and seeded synthetic vectors to audit the common
projection harness before any external candidate axis is attempted. This autonomous bridge claim is
evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{needs\_derivation}.

\textbf{Why.} the forbidden-control matrix makes the SerSplit contrast non-testable, so this negative-control pack
cannot audit projection selectivity.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_synthetic\_negative\_control\_axis\_pack.py

\subsection{Certificate}\label{sec:bridge-certificate}
% CLAIM_DETAIL claim_id=bridge.genetic_code.sersplit.edge_defect.nonestimability.certificate status=certified
\textbf{Statement.} The Ser-split edge-defect contrast (within-Ser 4-vs-2: \{UCU,UCC,UCA,UCG\} vs \{AGU,AGC\}) lies
exactly in the column span of the registered forbidden-control matrix C (verified against the actual
registered z-scored columns to 1e-9), so its residual is algebraically zero, v\_SerSplit is
undefined, and no real-data biological axis can yield a certified independent projection onto this
target. On the Ser support, sqrt(12)*u = 1 - 3*indicator(first base is purine), so the edge defect
IS a registered composition control. The 5-dim within-Ser zero-sum contrast subspace is NOT fully
absorbed, so the 15-way within-Ser permutation null is non-degenerate. This autonomous bridge claim
is evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Exact rational elimination proves sqrt(12) times the Ser-split edge-defect contrast lies in the span
of registered forbidden-control columns.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_sersplit\_edge\_defect\_nonestimability\_certificate.py

\subsection{Certificate}\label{sec:bridge-certificate}
% CLAIM_DETAIL claim_id=bridge.genetic_code.foldboundary.residual.exhaustion.certificate status=certified
\textbf{Statement.} The pre-registered, q6-label-free fold-boundary contrast subspaces over the 61 sense codons -- box
split-type (pure/split/tri-split coarsening of the 16 first-two-position boxes), degeneracy-class
(amino-acid degeneracy sizes), and fold-layer (64->16->25->21) -- are exactly absorbed (residual
rank 0) by the registered forbidden-control matrix C, so these bio-intrinsic fold boundaries provide
no independent residual axis beyond the registered composition controls (a bounding exhaustion
extension of the edge-exhaustion meta-certificate). The Window6 four-cell partition mapped via
q6\_label is correspondence-derived, not bio-intrinsic; because C contains no q6 information its
residual survival (rank 3) is expected and is barred from the certificate, not treated as
independent forcing. NOT certified here: the full 16-box partition's fine structure -- its only
possible escape directions are the Leu 4/2 and Arg 4/2 within-amino-acid branch splits (analogous to
the certified Ser 4/2), which are the next frontier. This autonomous bridge claim is evaluated only
by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} The bio-intrinsic fold-boundary contrast subspaces box\_split\_type, degeneracy\_class, fold\_layer
are q6-label-free and fully absorbed by the registered composition control frame, so they provide no
independent residual axis beyond those controls.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_foldboundary\_residual\_exhaustion\_certificate.py

\subsection{Certificate}\label{sec:bridge-certificate}
% CLAIM_DETAIL claim_id=bridge.genetic_code.box_partition.composition_collapse.certificate status=certified
\textbf{Statement.} Each of the three 6-fold-degenerate amino acids has a 4-vs-2 within-amino-acid branch split (Ser
UCN|AGY, Leu CUN|UUR, Arg CGN|AGR), and exact rational elimination proves ALL THREE lie in the span
of the registered forbidden-control matrix C (residual 0), with on-support identities sqrt(12)*u =
1-3*indicator(first base purine) for Ser, 1-3*indicator(first two bases equal) for Leu, and
-2+3*indicator(first base GC)+... for Arg. The full mean-zero 16-box first-two-position indicator
subspace (dim 15) has exact residual rank 0 against C (exact\_control\_rank 29 = rank with box
partition appended), so the entire 64->16 box partition is absorbed by the registered composition
control frame -- it is composition, not an independent forcing direction. Generalizes the certified
Ser-split non-estimability to all three 6-fold amino acids and completes the box-partition thread of
the fold-boundary exhaustion. Proof tied to the actual registered columns (registered-equivalence,
fail-closed on drift), branch contrasts label-free (no q6/Stop). Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Exact rational elimination proves the Ser, Leu, and Arg 4-vs-2 within-amino-acid branch splits all
lie in the span of the registered composition control frame.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_box\_partition\_composition\_collapse\_certificate.py

\subsection{Metacertificate}\label{sec:bridge-metacertificate}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.forcing_direction_closure.metacertificate status=certified
\textbf{Statement.} Consolidation meta-certificate for the math->bio forcing direction. Exact rational rank proves the
FULL pre-registered label-free fold-boundary battery (box partition + degeneracy classes + fold
layers + the three 6-fold amino-acid 4/2 branch splits Ser/Leu/Arg; 44 columns, span rank 22) lies
AS ONE UNION exactly in span(C): exact control rank 29 is unchanged when the battery is appended
(absorbed). The only fold-boundary direction outside span(C) is the q6\_label-derived Window6
four-cell partition, which is correspondence-derived and barred as circular (q6 is the
correspondence under test). Therefore the bridge's certified scope is: (1) POSITIVE - the sole
certified structural signal is the BC7 contravariant edge-hiding extremality plus the
edge-exhaustion meta-certificate (Window6/Fold6 and the standard code occupy opposite endpoints of
one label-free local-edge visibility functional); (2) BOUNDING-NEGATIVE - the label-free bio
fold-boundary structure is composition (= span(C)), leaving no independent bio-side residual axis
for Window6 to force; (3) the math->bio FORCING direction is closed-as-not-supported under the
current frame, with count/spectrum matches classified coincidence and q6-mediated forcing barred as
circular. REOPENING CONDITION (falsifiable): a label-free genetic-code invariant outside span(C),
defined without q6 or family counts, whose preservation forced-Window6/Fibonacci/Zeckendorf
structure necessitates more strongly than a pre-registered composition-controlled null. Evaluated
only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Exact Fraction rank elimination certifies the full label-free fold-boundary battery, taken as one
union, lies in span(C\_nonselection).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_forcing\_direction\_closure\_metacertificate.py

\subsection{Complement Stresstest}\label{sec:bridge-complement-stresstest}
% CLAIM_DETAIL claim_id=bridge.genetic_code.h3_higherorder.complement_stresstest status=refuted
\textbf{Statement.} Stress-test claim: the composition-exhaustion closure extends to the canonical higher-order class H3
(the 27 three-position nucleotide-chemistry interaction tensors
chi\_a(base1)*chi\_b(base2)*chi\_c(base3), chi in \{R/Y, W/S, K/M\}; genuinely higher-order,
label-free, non-q6). VERDICT REFUTED: exact Fraction rank shows H3 has residual rank 26 of 27 after
the registered composition control frame C (exact control rank 29; rank with H3 appended 55), so
higher-order three-position chemistry structure lies almost entirely OUTSIDE span(C). This precisely
DELIMITS the composition-exhaustion: not all label-free codon structure collapses to composition --
the additive/pairwise+fold structure does, but higher-order three-way chemistry interactions do not.
CRITICAL: this is a delimitation, NOT a math->bio forcing reopening -- there is no
Window6-necessitation test, no pre-registered necessitation null, and no principle selecting H3
among higher-order audit languages (Walsh/Fourier, codon-graph spectral, automorphism-orbit); H3 is
recorded only as a candidate class for future forcing work requiring a frozen necessitation null.
The forcing-direction closure stands. Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} Exact Fraction rank elimination shows H3 has nonzero residual rank after the actual registered
non-selection control columns.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_h3\_higherorder\_complement\_stresstest.py

\subsection{Conventionrobust Test}\label{sec:bridge-conventionrobust-test}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fibcube_embedding.conventionrobust_test status=needs_derivation
\textbf{Statement.} Reopening attempt for the math->bio forcing direction via a NON-q6, first-principles embedding.
Test: is the Fibonacci-cube vertex set S = \{21 no-adjacent-1 six-bit words\} (the canonical
Gamma\_6 isometric inclusion into the codon 6-cube Q6) a CONVENTION-ROBUST code-structure outlier
when mapped into codon space across ALL 17280 admissible base->2bit x bit/position conventions (q6
being just one marked convention, class size 2), vs a composition-matched null on pre-declared
label-free statistics (synonymous-edge / family-boundary / fold-cell / conservative-substitution),
Holm-Bonferroni corrected? VERDICT needs\_derivation: NO forcing signal -- the q6 class is NOT an
outlier (q6\_class\_outlier False) and only 4.7\% of non-q6 conventions are outliers (far below the
0.5 robustness threshold, \textasciitilde{}noise). So the canonical Gamma\_6->Q6 isometric-inclusion
route produces neither a convention-robust non-q6 forcing nor a q6-special alignment: this
most-natural non-circular embedding route is EXHAUSTED on the pre-declared battery. This is NOT an
exhaustive impossibility proof (other label-free invariants / non-inclusion constructions untested),
hence needs\_derivation not refuted. It does not touch BC7 (which concerns the Fold6 PARTITION edge
functional, a different object from the Gamma\_6 vertex set). Evaluated only by its registered
deterministic experiment (seed 619423).

\textbf{Verdict.} \texttt{needs\_derivation}.

\textbf{Why.} the pre-declared statistics do not show a q6-only obstruction or a convention-robust non-q6 signal
under the deterministic matched null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_fibcube\_embedding\_conventionrobust\_test.py

\subsection{Fibcube Codonfamily Graphiso Test}\label{sec:bridge-fibcube-codonfamily-graphiso-test}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fibcube_codonfamily_graphiso_test status=refuted
\textbf{Statement.} Convention-free reopening route (distinct from the inclusion route): is the Fibonacci cube Gamma\_6
(graph on the 21 no-adjacent-1 six-bit words, Hamming-1 edges; 21 vertices, 38 edges) ISOMORPHIC as
an abstract graph to the codon family-adjacency graph (families adjacent iff some codon pair is
Hamming-1)? A graph iso is intrinsic (no codon-coordinate convention), so it sidesteps the inclusion
route's convention problem. VERDICT REFUTED: they fail necessary isomorphism invariants -- Gamma\_6
has 38 edges vs 85 for the 21-family graph (incl Stop) and the excl-Stop variant has 20 vertices/75
edges; degree sequences also differ (Gamma\_6 max degree 6 vs family-graph degrees 6-13). Robust
across family-graph definitions. So the convention-free graph-isomorphism reopening route is CLOSED:
the 21<->21 cardinality match is a graph-level COINCIDENCE, not structural forcing. Together with
the inclusion-route exhaustion (fibcube\_embedding\_conventionrobust\_test), both natural non-q6
Window6->codon embedding constructions are now closed. Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The convention-free graph-isomorphism route is closed: Gamma\_6 and the codon-family adjacency graph
fail necessary abstract-graph isomorphism invariants.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_fibcube\_codonfamily\_graphiso\_test.py

\subsection{Extremality}\label{sec:bridge-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.synfiber_quotient.extremality status=coincidence
\textbf{Statement.} Factory second instance. Is the genetic code's synonymous-fiber partition of the codon Q6 cube an
extremal/outlier point for a label-free QUOTIENT functional (cross\_equitability\_defect of the
block-to-block palette graph; distinct from e\_in/BC7) among Q6 partitions matched to its
degeneracy-size multiset? VERDICT COINCIDENCE (reducible): the partition is an extreme low-tail
equitability outlier under the WEAK size-only null (p=3.9999e-05, 50000 draws) BUT p=0.496 (dead
median) under the STRINGENT box/wobble-preserving null
(box\_wobble\_atom\_edge\_profile\_preserving: keeps position-3 wobble atoms intact + matches the
same-fiber Q6 edge profile 47 within-box/3 cross-box). So the code's degeneracy-ASSIGNMENT
equitability is ENTIRELY reducible to the already-certified box/wobble composition
(box-partition-collapse) -- NOT an independent forcing signal. Reinforces the composition-exhaustion
finding at the quotient/assignment level. The functional is genuinely distinct from e\_in
(variance-like cross-block uniformity defect), and the certified-positive over-claim was caught by
the review triplet (unanimous reject of the weak-null certified) and corrected. Evaluated only by
its registered deterministic experiment.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The standard synonymous-fiber partition is a low-tail outlier for cross\_equitability\_defect under
the weak size-matched null (p=3.9999200016e-05) but not under the stringent
box\_wobble\_atom\_edge\_profile\_preserving null (p=0.496190076198); the equitability extremality
is reducible to the already-certified box/wobble composition.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_synfiber\_quotient\_extremality.py

\subsection{Extremality}\label{sec:bridge-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.square_motif_residual.extremality status=coincidence
\textbf{Statement.} Oracle-planned non-reducible frontier (the next canonical object beyond edges: Q6 square 2-faces).
Is the genetic code's cross-box square-motif structure (label-free 4-cycle vertex equality-pattern
census, by box/wobble automorphism orbit) extremal under the conditioned null Omega\_BW,E -- all
unlabeled regroupings of the 25 fixed box/wobble atoms into 21 fibers preserving the global
fiber-signature multiset AND every box/wobble edge-orbit's cross-atom synonymous-edge-count profile
(so e\_in + its directional profile + distribution + box/wobble + edge-margins are ALL conditioned
out)? The interpretable coordinate is diagonal-recovery faces (synonymy as a 2-step diagonal
recurrence, NOT edge-hiding), but the verdict uses the omnibus Mahalanobis T on the
edge-margin-orthogonal residual. VERDICT COINCIDENCE: prerequisite GATE passed -- Omega\_BW,E is
non-trivial (162468 states, EXACT enumeration; observed state included) and the residual has nonzero
variance (trace 4.28), so this was a genuine non-reducible test, NOT a forced triviality -- but the
standard code is NOT an extreme outlier (omnibus T\_obs=2.54, p=0.246, typical). So even the square
2-face / 2-step-diagonal structure, conditioned out of everything already certified, carries NO
independent forcing signal; the composition+e\_in closure deepens to the 2-face level. Review
triplet (quality+tests) independently re-enumerated the 162468-state null + verified the omnibus
statistic (deterministic, no RNG). Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The conditioned null is non-trivial and the residual has nonzero variance, but the standard code is
not an extreme omnibus outlier (p=0.246300810006).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_square\_motif\_residual\_extremality.py

\subsection{Extremality}\label{sec:bridge-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.bypass_conductance.extremality status=coincidence
\textbf{Statement.} Oracle Route 3 (strongest GLOBAL complement to the square-motif residual). Is the genetic code
extremal for EXTERNAL BYPASS CONDUCTANCE between the disconnected components of synonymous fibers --
the ambient placement of fiber pieces in Q6 that edge-hiding and 2-face motifs do NOT see? For each
disconnected fiber, for each component pair, the effective conductance through the external routing
graph (excluding the fiber's own vertices) is computed (grounded-Laplacian effective resistance;
observed value 35712/6925 per disconnected fiber), summed over ALL disconnected fibers without
naming Ser/Stop. Null Omega\_BW,E,d,comp conditions out e\_in + box/wobble + edge-margins +
boundary-degree vectors + per-signature connected-component sizes \& isomorphism types (162468
states, EXACT enumeration). VERDICT COINCIDENCE: both prerequisite gates PASS (statistic has nonzero
variance + the null has >=2 box/wobble-preserving orbits -- a genuine non-reducible test) but the
standard code is TYPICAL (omnibus T\_obs=10.31, upper-tail p=0.202). So even the strongest global
ambient-placement invariant, conditioned out of everything certified, carries no independent forcing
signal. Together with the square-motif coincidence, the e\_in+composition closure is now confirmed
robust across edges, square 2-faces, AND global disconnected-component conductance. Review (tests)
independently re-solved the conductance + verified the component-conditioned null. not tailored to
any named fiber. Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The prerequisite gates pass, but the standard code is typical for the external bypass-conductance
statistic (upper-tail p=0.202181352636).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bypass\_conductance\_extremality.py

\subsection{Extremality}\label{sec:bridge-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.quotient_spanningtree.extremality status=coincidence
\textbf{Statement.} Oracle Route 2/last (quotient boundary-channel topology). Is the genetic code extremal for quotient
spanning-tree COMPRESSION T\_chan = -log tau, where tau is the exact weighted spanning-tree count
(Matrix-Tree) of the 21-fiber quotient multigraph W\_ab = cross-fiber Q6 edge counts? Null
Omega\_BW,E,d conditions out e\_in + box/wobble + edge-margins + boundary-degree vectors (so total
cross-fiber weight is fixed and tau's variation is channel TOPOLOGY only; 162468 states exact).
VERDICT COINCIDENCE: both prerequisite gates pass (nonzero variance + multi-orbit) so it is a
genuine non-reducible test, but the standard code is typical (tau=253827722980651696128 exact via
Bareiss; T\_chan=-46.98; borderline upper-tail p=0.0647, above the p<=0.01 certification threshold).
This COMPLETES the oracle's 3-route systematic sweep beyond edge-hiding: square-motif residual
(p=0.246), bypass-conductance (p=0.202), and quotient spanning-tree (p=0.065) -- ALL coincidence.
Review (quality+tests) verified the exact determinant (independent SymPy match) + the
boundary-degree-conditioned null + borderline-p honesty. Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The prerequisite gates pass, but the standard code is typical for quotient spanning-tree compression
(upper-tail p=0.0646958170224).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_quotient\_spanningtree\_extremality.py

\subsection{Edge Hiding Optimality}\label{sec:bridge-edge-hiding-optimality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.edge_hiding_optimality status=certified
\textbf{Statement.} The standard genetic code's codon ASSIGNMENT is an edge-hiding optimum among degeneracy-matched
codes. Using the bridge's certified functional e\_in = \# Hamming-1 (single-base, H(3,4)) codon
pairs that are synonymous, the standard code has e\_in = 67 (sense-to-sense) / 69 (all codons),
versus the null Omega\_deg of random codes with the standard amino-acid degeneracy profile FIXED
(Leu/Ser/Arg=6; five fourfold=4; Ile=3; nine twofold=2; Met/Trp=1; Stop=3) and codon assignments
uniformly randomized: null mean 12.4, sd 3.2, max 30 (analytic mean
\textasciitilde{}288*(244-64)/(64*63)\textasciitilde{}12.9 confirms). p\_hide=0.0, z=17.1 -- NOT a
single degeneracy-matched random code reached the standard code's edge-hiding. VERDICT CERTIFIED.
HONEST FRAMING: this is the e\_in edge-hiding extremality (BC7's lone certified positive)
demonstrated against the NATURAL 'alternative genetic code' null, quantitatively CONNECTING the
bridge's certified structural positive to the classic genetic-code error-minimization/optimality
result (Freeland-Hurst lineage) -- i.e. the code's redundancy is organized (box/wobble) to keep
synonymous codons mutationally adjacent far beyond what its degeneracy profile alone requires. It is
a confirmation + biological connection of BC7 vs a new null, NOT a new independent functional, and
is distinct from the box-partition-collapse result (a linear statement; e\_in is the one nonlinear
genuine signal). It is the structural foundation of the relayed bridge->bio DMS-robustness mediation
test (does this edge-hiding buy measurable mistranslation robustness, R(g)=alpha-beta*e\_in+eps via
ProteinGym). Review triplet (architecture+quality+tests) independently verified the uniform null,
the 288-edge graph, e\_in=67/69, and the analytic null-mean match. Evaluated only by its registered
deterministic experiment (fixed seed).

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} standard code is a high edge-hiding outlier under the fixed labeled degeneracy null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_code\_edge\_hiding\_optimality.py

\subsection{Canonicity}\label{sec:bridge-canonicity}
% CLAIM_DETAIL claim_id=bridge.genetic_code.codon_v4cubed_eigenspace.canonicity status=certified
\textbf{Statement.} Codon function space has the canonical V4\textasciicircum{}3 character / Hamming-association-scheme
decomposition, and the bridge's H3 class is exactly its highest-frequency eigenspace. Bases = Klein
four-group V4 with three chemical dichotomy characters chi\_R (purine/pyrimidine), chi\_W
(weak/strong), chi\_K (keto/amino); EXACT: chi\_R(b)*chi\_W(b)*chi\_K(b) = -1 for every base, and
\{1,chi\_R,chi\_W,chi\_K\} is an orthogonal character basis. The 64 character tensors
Psi\_\{abc\}(c)=chi\_a(b1)chi\_b(b2)chi\_c(b3) form an orthogonal basis of R\textasciicircum{}64,
grouped by level j into E\_0..E\_3 of dims 1/9/27/27 = C(3,j)3\textasciicircum{}j; each E\_j is
EXACTLY an eigenspace of the H(3,4) single-base adjacency (64 codons, 288 edges) with Krawtchouk
eigenvalue lambda\_j = 9-4j (9,5,1,-3) -- verified by applying the adjacency to each tensor in exact
integer arithmetic (no float eigensolver). THE BRIDGE LINK: the H3 class (the 27 three-position
R/Y,W/S,K/M interaction tensors used in run\_h3\_higherorder\_complement\_stresstest) equals E\_3,
the lambda=-3 highest-frequency eigenspace, proved by exact ranks
(h3\_rank=e3\_rank=combined\_rank=27). VERDICT CERTIFIED. This is the standard H(3,4)
association-scheme spectral decomposition realized via the genetic code's chemical characters; the
bridge-specific content is H3 = canonical E\_3 (not an arbitrary higher-order class) + the
V4\textasciicircum{}3 group-algebra/Krawtchouk number-theory entry point for a future bio-validated
translation operator (SNF/trace/zeta). Review (tests) independently verified the exact eigenspace
check + the H3=E3 span equality. Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Exact integer character tensors form the H(3,4) Krawtchouk eigenspace decomposition; the bridge H3
construction is the full level-three span.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_v4cubed\_eigenspace\_canonicity.py

\subsection{Tau Conservation}\label{sec:bridge-tau-conservation}
% CLAIM_DETAIL claim_id=bridge.genetic_code.altcode_edge_hiding.tau_conservation status=certified
\textbf{Statement.} Edge-hiding is a CONSERVED constraint across the genetic-code family. For each of the 27 NCBI
genetic code tables, the number of synonymous single-base (H(3,4)) codon edges e\_in(t) is compared
to a degeneracy-profile-matched random null (random codes with t's exact amino-acid family-size
multiset; 20000 draws, fixed seed; sense-to-sense, Stop retained in profile but Stop-Stop edges
excluded). RESULT: all 27 codes' per-code profile-matched-null upper-tail p sit at the Monte-Carlo
floor \textasciitilde{}5e-5 (all\_at\_floor=True); natural e\_in is far above the null (min e\_in
minus null p99 = 43; minimum null z-score = 15.4). This survives a 9-cluster PHYLOGENETIC bootstrap
(clusters by code-table reassignment similarity, resampling clusters NOT individual codes to avoid
pseudo-replication of related mito variants): cluster\_bootstrap\_p = 5e-5. The union of codons
reassigned across all tables vs the standard = exactly 13 (reproduces |R|=13). VERDICT CERTIFIED:
natural genetic codes are systematically edge-hiding-extremal vs degeneracy-matched random codes,
conserved across the family AND robust to phylogenetic non-independence -- extending the BC7
edge-hiding optimality from the standard code to the whole genetic-code family. EXPLICITLY NOT
claimed: global maximality (no valid e\_max -- the broken pair-swap optimizer was removed after
review; the claim rests purely on the natural-vs-degeneracy-matched-null contrast, which is
independent of any maximum estimate) and NOT a causal/selection proof. Review
(architecture+quality+tests, then re-review) verified e\_in, the profile-matched null, the per-code
floor, and the cluster bootstrap, and caught+removed the optimizer over-claim. Evaluated only by its
registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} natural codes' synonymous edge-hiding is systematically far above degeneracy-matched random codes
across the family and survives phylogenetic clustering; no global bound claim is made.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_altcode\_edge\_hiding\_tau.py

\subsection{Bstar Q6 E3 Spectral Concentration}\label{sec:bridge-bstar-q6-e3-spectral-concentration}
% CLAIM_DETAIL claim_id=bridge.genetic_code.bstar_q6_e3_spectral_concentration status=refuted
\textbf{Statement.} REFUTED: the bio-intrinsic B*\_Q6 translation-residual basis does NOT concentrate in the canonical
lambda=-3 Krawtchouk eigenspace E3 (=H3) of the codon Hamming graph H(3,4); it is significantly
ANTI-concentrated there. Setup: the 9 B*\_Q6 within-synonymous zero-sum codon contrasts (K\_AAA,
Arg\_AGR, Ile\_AUA, Leu CUN-vs-UUR, Leu UUA-vs-UUG, Ser UCR-vs-AGY, Ser UCA-vs-UCG, Thr ACR-vs-ACY,
f3\_stress), defined purely from codon spelling + genetic-code synonymous degeneracy (NO
q6/Foldbin/Zeckendorf labels -> non-circular against the pure-math E3), are embedded in
R\textasciicircum{}64 (zero on the 3 stops), synonymous-mean-removed, orthonormalized (rank 9). The
H(3,4) eigenspace projectors are built exactly via the minimal polynomial (eigenvalues 9,5,1,-3;
dims 1,9,27,27; sum P\_Ej = I to max\_abs 0; idempotent; symmetric; matching the chemical-character
tensor projectors exactly). Statistic T3 = (1/9) tr(Q\_B\textasciicircum{}T P\_E3 Q\_B) = mean
squared cosine of principal angles span(B*\_Q6)<->E3. RESULT: the B*\_Q6 spectral fingerprint is
(T0,T1,T2,T3)=(0.000, 0.185, 0.414, 0.401); T3=0.401 is FAR BELOW a triviality-controlled
matched-synonymous null (random within-synonymous zero-sum contrasts preserving each template's
support + per-fiber zero-sum + L2 norm, 20000 draws) whose mean is 0.528 (null\_z=-5.59, lower-tail
p=5e-5); an independent all-family secondary null agrees (mean 0.531). VERDICT REFUTED: the
spectral-redirect hypothesis that the highest-frequency canonical lambda=-3 eigenspace H3=E3
predicts/forces the translation-relevant synonymous residual is FALSE -- generic synonymous
contrasts concentrate in E3 MORE than B*\_Q6 does, so B*\_Q6 carries a real structural signal but in
the LOWER-frequency E1/E2 directions, not E3. SCOPE / not-claimed: this refutes only the
E3-concentration hypothesis; the excess B*\_Q6 mass in E1(+0.x)/E2 vs null is a
separately-pre-registerable follow-up (NOT claimed here, to avoid garden-of-forking-paths); no
causal/selection claim. Verified: structural asserts exact, null fidelity manually audited +
adversarially verified (no E2<->E3 mislabel, fair matched null). Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} No derivation note is recorded yet.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bstar\_q6\_e3\_spectral\_concentration.py

\subsection{Bstar Q6 Eigenspace Concentration Profile}\label{sec:bridge-bstar-q6-eigenspace-concentration-profile}
% CLAIM_DETAIL claim_id=bridge.genetic_code.bstar_q6_eigenspace_concentration_profile status=certified
\textbf{Statement.} CERTIFIED (scoped spectral-localization extremality, NOT forcing isomorphism, NOT causal): the
bio-intrinsic B*\_Q6 translation-residual basis (9 within-synonymous zero-sum codon contrasts,
defined from codon spelling + genetic-code degeneracy; no q6/Foldbin/Zeckendorf labels ->
non-circular against the pure-math eigenspaces) is structurally CONCENTRATED in E1 = the lambda=5,
FIRST-ORDER (single-position / additive-character) Krawtchouk eigenspace of the codon Hamming graph
H(3,4) (dim 9), beyond a triviality-controlled matched-synonymous null. Pre-registered Bonferroni
test (alpha = 0.001/3 over the three nontrivial eigenspaces E1,E2,E3; E0 trivially zero after
synonymous-mean removal): B*\_Q6 spectral fingerprint (T0,T1,T2,T3) = (0.000, 0.185, 0.414, 0.401)
vs matched-null mean (0.000, 0.083, 0.388, 0.528). EXCESS by eigenspace: E1 +0.102 (null\_z +7.94,
upper\_p 5e-5 -- PASSES Bonferroni, CONCENTRATED); E2 +0.026 (z +1.64, upper\_p 0.057 -- NOT
significant); E3 -0.128 (z -5.59, lower\_p 5e-5 -- DEPLETED, re-confirms the landed E3-concentration
refutation). KEY (non-obvious, null-dependent): B*\_Q6's LARGEST raw component is E2 (0.414), but
the EXCESS-over-generic-synonymous-contrasts is in E1 (more than double the generic E1 mass 0.185 vs
0.083) -- the matched null (preserving each template's support + per-fiber zero-sum + L2 norm; 20000
draws) is what isolates the genuine first-order signal from generic synonymous structure.
INTERPRETATION: the genetic code's translation-relevant synonymous residual lives in the FIRST-ORDER
(additive, single-position) spectral component, not the highest-frequency lambda=-3 that the
spectral redirect hypothesized -- biologically consistent with synonymous/wobble effects being
largely additive per position. ROBUSTNESS: structural asserts exact (projector dims [1,9,27,27], sum
P=I to max\_abs 0, idempotent, symmetric, polynomial projectors = chemical-character tensor
projectors); E1 label verified (rank 9, lambda=5, no E1<->E2 swap); matched null is FAIR and reaches
E1 on cross-box supports (Arg AGR-vs-CGN, Leu CUN-vs-UUR, Ser UCR-vs-AGY); the E1 excess is
basis-wide (survives leave-one-out of the largest contributor f3\_stress -- still above all 5000
null draws), adversarially verified. SCOPE / not-claimed: no forcing isomorphism, no
causal/selection mechanism, no cross-organism law (bio's cross-organism data is non-resolving); this
certifies only the structural concentration of the residual basis in E1 vs the matched-synonymous
null on the standard code. Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Scoped spectral-localization verdict for B*\_Q6: concentrated eigenspaces are E1 (lambda=5,
D=0.101903149222, upper\_p=4.99975e-05); depleted eigenspaces are E3 (lambda=-3, D=-0.127817741639,
lower\_p=4.99975e-05).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bstar\_q6\_eigenspace\_concentration\_profile.py

\subsection{Bstar Q6 First Order Position Character}\label{sec:bridge-bstar-q6-first-order-position-character}
% CLAIM_DETAIL claim_id=bridge.genetic_code.bstar_q6_first_order_position_character status=certified
\textbf{Statement.} CERTIFIED (scoped spectral-localization, NOT causal, NOT forcing): the first-order (E1, lambda=5)
spectral excess of bio's B*\_Q6 translation-residual basis localizes to specific codon POSITIONS and
chemical CHARACTERS. E1 (dim 9) is decomposed EXACTLY into the 9 single-position chemical-character
functions e\_\{p,c\} for codon position p in \{1,2,3\} and dichotomy c in \{chi\_R
purine/pyrimidine, chi\_W weak/strong (A,U vs G,C), chi\_K keto/amino\} (verified orthonormal:
unit-norm defect 0, Gram off-diagonal 0, sum\_\{p,c\} e\_\{p,c\} e\_\{p,c\}\textasciicircum{}T =
P\_E1 to max\_abs 0). For the orthonormalized B*\_Q6 basis (rank 9) the first-order mass per
component is scored against the SAME triviality-controlled matched-synonymous null (preserve each
template's support + per-fiber zero-sum + L2 norm; 20000 draws), grouped by position and by
character, with pre-registered Bonferroni alpha = 0.001/6 over the 3 positions + 3 characters.
RESULT (excess over generic synonymous contrasts): POSITION 3 (wobble) M=0.120 vs null 0.060, excess
+0.060, z=+8.76, p=5e-5 -> SIGNIFICANT (dominant); POSITION 1 M=0.051 vs 0.016, excess +0.035,
z=+4.53, p=5e-5 -> SIGNIFICANT; POSITION 2 z=+1.38 p=0.027 -> NOT significant. CHARACTER chi\_W
(weak/strong) M=0.091 vs 0.029, excess +0.063, z=+10.05, p=5e-5 -> SIGNIFICANT (dominant); chi\_R
(purine/pyrimidine) excess +0.031, z=+3.47, p=5e-5 -> SIGNIFICANT; chi\_K (keto/amino) z=+1.32
p=0.097 -> NOT significant. Dominant single component = p3\_W (third-position weak/strong) = 0.072.
INTERPRETATION: the genetic code's translation-relevant synonymous residual's additive structure
concentrates at the WOBBLE (3rd) position -- beyond the already-wobble-heavy synonymous null, so
non-trivial -- plus the FIRST position (traceable to B*\_Q6's cross-box contrasts in the
6-fold-degenerate families Arg AGR/CGN, Ser UCN/AGY, Leu CUN/UUR), and is carried by the weak/strong
(base-pairing-strength / GC-like) and purine/pyrimidine dichotomies but NOT keto/amino. This is
consistent with (but does NOT assert a mechanism for) wobble-pairing and GC/tRNA-availability being
the dominant synonymous axes. ROBUSTNESS: E1-basis exact; null apples-to-apples (same basis +
orthonormalization for observed and null); consistency sums = T1 exactly; full picture honestly
reported (pos2 + chi\_K null); adversarially verified. SCOPE / not-claimed: scoped
spectral-localization vs matched-synonymous null on the standard code; no causal/selection
mechanism, no cross-organism law, no forcing isomorphism. Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Scoped spectral-localization verdict for B*\_Q6 first-order mass: Bonferroni-significant excess
carriers are position1 (D=0.035136515709, upper\_p=4.99975e-05), position3 (D=0.05992991177,
upper\_p=4.99975e-05), char\_R (D=0.030878562839, upper\_p=4.99975e-05), char\_W (D=0.062617530832,
upper\_p=4.99975e-05).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bstar\_q6\_first\_order\_position\_character.py

\subsection{Codon E1 Heldout Crossorganism Conservation}\label{sec:bridge-codon-e1-heldout-crossorganism-conservation}
% CLAIM_DETAIL claim_id=bridge.genetic_code.codon_e1_heldout_crossorganism_conservation status=certified
\textbf{Statement.} CERTIFIED (held-out cross-organism, scoped; NOT Window6, NOT causal): the codon-E1 component of the
synonymous codon-usage residual is CONSERVED across organisms. For each of 29 organisms spanning 18
genera (public NCBI coding-sequence codon counts; frozen panel
synced/codon\_e1\_heldout\_panel.json), d\_resid4,o is the organism's relative codon frequencies
over the 61 sense codons with synonymous-family means removed (Q\_syn). The frozen codon-E1 map is
B1 = col(Q\_syn R U1) where U1 is the 9-dim first-order (lambda=5) Krawtchouk eigenspace of the
codon Hamming graph H(3,4), R restricts to 61 sense codons; after sense-restriction +
synonymous-mean-removal B1 has rank 6 (orthogonal projector P\_B1 symmetric/idempotent, trace 6).
Per organism r\_o = ||P\_B1 d\_resid4,o||\textasciicircum{}2 / ||d\_resid4,o||\textasciicircum{}2;
the clade-weighted statistic T\_heldout = (1/18) sum over genera of the within-genus mean r\_o
(equal weight per genus to defuse pseudo-replication -- Burkholderia 7 + Mycobacterium 4 of the 29).
RESULT: T\_heldout = 0.571, vs a matched-synonymous-relabel null (a within-synonymous-family codon
relabeling applied GLOBALLY to all organisms, preserving family structure + magnitudes +
cross-organism dependence, 20000 draws) with mean 0.137 (z=8.75, one-sided p=5e-5); the cluster
bootstrap over the 18 genera (10000 resamples) gives lower-95\% excess-over-null = 0.352 > 0. Both
pre-registered gate conditions (p<=0.01 AND bootstrap lower-95 excess>0) are met. The per-genus mean
r is broadly high (0.63-0.73 across the leading genera), so the conservation is not driven by any
single clade. CRITICAL -- this CLEARS the 26/41 chance-projection trap: B1 is 6-dimensional, so the
chance projection of a random within-synonymous (\textasciitilde{}41-dim) vector onto B1 is
\textasciitilde{}6/41=0.146, which the relabel null reproduces (0.137, independently verified by
isotropic-random baseline 0.1464); the real data sits \textasciitilde{}4x above chance.
INTERPRETATION: the first-order (additive, single-position; wobble + weak/strong +
purine/pyrimidine, per the E1-localization certificate) spectral structure certified on the standard
code's table is EMPIRICALLY CONSERVED in real codon usage across 18 mostly-bacterial genera -- a
genuine held-out cross-organism validation, where bio's own diffuse full-residual meta was
non-resolving (p=0.58). ROBUSTNESS: adversarially verified -- null is a fair chance baseline
(independent isotropic + relabel baselines both \textasciitilde{}0.146/0.137), B1 rank-6 exact,
d\_resid4 synonymous-mean-zero to roundoff (no organism skipped), panel RNA-sense-only with matching
totals, cluster bootstrap correct. SCOPE / not-claimed: this certifies ONLY a conserved codon-E1
component of d\_resid4; it does NOT certify Window6 (per the bridge audit, reopening Window6
requires a NEW independently-defined nonzero structural map, not spectral-order matching -- the
Window6 adjacency spectrum is provably disjoint from codon E1, coprime characteristic polynomials
forcing the only intertwiner to zero); no causal/selection mechanism; the panel is 29 organisms / 18
genera, mostly bacteria (taxonomic-range caveat). Evaluated only by its registered deterministic
experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} This certifies only that d\_resid4 has a conserved codon-E1 component across these organisms/clades;
it does not certify Window6, because the oracle requires a new nonzero structural map to reopen
Window6, not spectral-order matching.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_e1\_heldout\_crossorganism\_gate.py

\subsection{Intertwiner Impossibility}\label{sec:bridge-intertwiner-impossibility}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.intertwiner_impossibility status=refuted
\textbf{Statement.} CERTIFIED IMPOSSIBILITY (certified-negative, pure math): there is NO nonzero intertwiner J:
R\textasciicircum{}21 -> R\textasciicircum{}64 satisfying A\_\{H(3,4)\} J = J A\_\{Gamma6\}, where
A\_\{H(3,4)\} is the codon Hamming-graph adjacency (64 codons, eigenvalues \{9,5,1,-3\} with
multiplicities \{1,9,27,27\}) and A\_\{Gamma6\} is the adjacency of the Window6 graph Gamma6 = the
Fibonacci cube on length-6 binary strings with no two consecutive 1s (21 vertices = F8, 38 edges).
The homogeneous Sylvester equation A X - X B = 0 has a nonzero solution iff A and B share an
eigenvalue, i.e. iff gcd(charpoly(A), charpoly(B)) != 1. Gamma6's integer characteristic polynomial
evaluated at each H(3,4) eigenvalue is nonzero: at lambda=9 it is \textasciitilde{}6.65e19, at 5
\textasciitilde{}6.75e13, at 1 it is -144, at -3 it is \textasciitilde{}1.307e8 (equivalently
det(A\_Gamma6 - lambda I) = 144, -1.307e8, -6.75e13, -6.65e19 for lambda=1,-3,5,9); Gamma6's
spectral radius is 3.90, trivially excluding 9 and 5, and the exact integer evaluations exclude the
borderline 1 and -3. The polynomial gcd is a nonzero constant (=1 up to scale), cross-confirming no
shared eigenvalue. Therefore the only intertwiner is J=0, and the certified codon-E1 (lambda=5)
eigenspace -- indeed EVERY codon adjacency eigenspace -- is NOT an adjacency-spectral image,
eigenspace, or quotient of the Window6 graph. CONSEQUENCE: the temptation to connect the bridge's
bio-side codon-E1 conservation finding (the conserved first-order/additive translation-residual
structure) BACK to Window6 via spectral-order matching is formally closed -- any such comparison
(eigenvalue rank, normalized frequency, rescaling) is vacuous, and convention-embedding into Q6 is
circular (consistent with the prior 17280-convention audit finding no convention-robust forcing).
This STRENGTHENS the scope discipline of the codon-E1 results: they certify genetic-code-internal
spectral structure, NOT a Window6<->codon forcing. Reopening a Window6<->codon structural relation
requires a NEW, independently-defined nonzero structural map -- not spectral-order matching;
edge-hiding contravariance remains the sole certified cross-domain relation. ROBUSTNESS: Gamma6
|V|=21,|E|=38 and H(3,4) eigenspace dims [1,9,27,27] asserted; the no-shared-eigenvalue conclusion
is cross-verified two independent ways (the experiment's exact characteristic-polynomial evaluation
AND an independent fraction-free Bareiss integer determinant det(A\_Gamma6 - lambda I), matching to
the digit). Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} No derivation note is recorded yet.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_window6\_codon\_e1\_intertwiner\_impossibility.py

\subsection{Codon E1 Sense Restriction Positional Rank}\label{sec:bridge-codon-e1-sense-restriction-positional-rank}
% CLAIM_DETAIL claim_id=bridge.genetic_code.codon_e1_sense_restriction_positional_rank status=certified
\textbf{Statement.} CERTIFIED (pure math, codon-side sharpening of the E1 results; no Window6, no biology beyond the
dimensional statement): consider the first-order (lambda=5) eigenspace E1 of the codon Hamming graph
H(3,4), spanned by the 9 single-position chemical-character functions
e\_\{p,c\}(codon)=chi\_c(codon[p]) for codon position p in \{1,2,3\} and dichotomy c in \{chi\_R
purine/pyrimidine, chi\_W weak/strong, chi\_K keto/amino\}. Let R restrict to the 61 sense codons
and Q\_syn remove synonymous-family means (standard code). The composed map M = Q\_syn R U1 (U1 =
the 9 E1 columns) has RANK EXACTLY 6, with KERNEL exactly 3-dimensional spanned by \{ e\_\{1,R\} -
e\_\{1,W\} + e\_\{1,K\}, e\_\{2,W\}, e\_\{2,R\} - e\_\{2,K\} \} (each maps to the zero vector under
M, verified by exact arithmetic). Decomposed by codon POSITION, the image ranks are (position1,
position2, position3) = (2, 1, 3): the wobble position (3) retains all THREE first-order degrees of
freedom after sense-restriction + synonymous-centering, while positions 1 and 2 collapse to ranks 2
and 1. Decomposed by CHARACTER, the ranks are (chi\_R, chi\_W, chi\_K) = (3, 2, 3): the keto/amino
character family chi\_K retains FULL rank 3. CONSEQUENCE: the empirical non-enrichment of chi\_K
found in the E1-localization certificate
(bridge.genetic\_code.bstar\_q6\_first\_order\_position\_character: chi\_K not significant, z=1.32)
is NOT a dimensional artifact -- chi\_K has its full 3 first-order degrees of freedom available in
the sense-restricted synonymous-centered space, so its empirical absence is a genuine biological
non-enrichment, not a missing dimension. This also confirms the rank-6 reduction underlying the
held-out gate's B1 (rank 6) is exactly the (2,1,3) positional / (3,2,3) character structure.
ROBUSTNESS: exact (Fraction) arithmetic; kernel basis maps to zero exactly; cross-verified two
independent ways (the experiment + an independent orchestrator recompute with its own standard-code
table and character definitions, all ranks and kernel matching). Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} The sense-restricted, synonymous-centered codon-E1 map has kernel exactly spanned by
\{e\_\{1,R\}-e\_\{1,W\}+e\_\{1,K\}, e\_\{2,W\}, e\_\{2,R\}-e\_\{2,K\}\} with rank M=6 and dim ker=3.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_e1\_sense\_restriction\_kernel.py

\subsection{Dms Codon Escape Conditional Allocation}\label{sec:bridge-dms-codon-escape-conditional-allocation}
% CLAIM_DETAIL claim_id=bridge.genetic_code.dms_codon_escape_conditional_allocation status=refuted
\textbf{Statement.} COINCIDENCE (adversarially-verified TRUE null; the flagship functional test of the certified BC7
edge-hiding contravariance): in public deep-mutational-scanning (DMS) fitness data,
experimentally-fragile protein sites do NOT preferentially use codons with more one-step synonymous
escape routes, beyond what codon composition and optimality already explain. SETUP: from ProteinGym
substitution assays (frozen panel synced/dms\_codon\_escape\_panel.json: 144 of 217 assays passing
an identity gate -- translated UniProt/ENA CDS exactly contains the assay target\_seq under the
standard nuclear code -- yielding 119 distinct proteins, 27 organisms, 6307 measured Leu/Arg/Ser
sites, the only amino acids whose synonymous Hamming-1 escape degree h varies within-family), site
intolerance I = negative mean standardized DMS over nonsynonymous substitutions. The oracle-mandated
VALID estimator is a CONDITIONAL codon-choice logit (the within-stratum codon-permutation null is
invalid -- it destroys the intolerance-optimality association the null must condition on): for each
site the choice set is the synonymous codons of its amino acid, the chosen alternative is the native
codon, and the utility is alpha\_codon + I\_i*(beta\_h h(c) + beta\_g GC(c) + beta\_r RSCU\_org(c))
with organism x amino-acid x codon baselines absorbed, testing beta\_h>0. RESULT: beta\_h = 0.0154
(SE 0.031, Wald two-sided p=0.62 / one-sided 0.31), cluster-bootstrap over the 119 proteins
lower-95\% below zero, 95\% CI \textasciitilde{}[-0.045,0.076] -- a well-powered near-zero effect.
The estimate is IDENTIFIED (collinearity of h with composition only 0.35, far below the 0.95
non-identification gate) and the model is NOT degenerate: the POSITIVE CONTROL beta\_RSCU = 0.068 is
SIGNIFICANT (z=2.41, two-sided p=0.016), i.e. intolerant sites DO bias codon choice -- toward more
OPTIMAL codons (translational-accuracy selection), NOT toward higher-escape codons. CONCLUSION:
edge-hiding (synonymous escape topology) has NO detectable functional codon-allocation signature in
DMS fitness data beyond composition and optimality; the certified BC7 edge-hiding contravariance
therefore remains a STATIC genetic-code-table extremality with no demonstrated functional deployment
at fragile sites. HONEST SCOPE (oracle-mandated): this is a null beyond a preregistered
composition+optimality model; it does NOT (and an observational DMS study cannot) prove h is
non-causal -- a within-site randomized synonymous-recoding intervention matched for decoding + local
context would be needed for any causal claim; tAI/decoding-accuracy (tRNA) and dinucleotide/dMFE
context are documented sensitivity extensions, not expected to revive an already-null marginal (raw
marginal covariance p=0.23). NOT a Window6 result. ROBUSTNESS: triple-confirmed (raw marginal
p=0.23; optimality-residualized sensitivity coincidence; valid conditional logit p=0.31) and
adversarially verified as a TRUE null (positive control significant, h varies + identified,
choice-set/native-chosen correct with 0 mismatches over 6307 sites, intolerance non-degenerate).
Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} No derivation note is recorded yet.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_dms\_codon\_escape\_conditional\_logit.py

\subsection{Codon E1 Conservation Transportability}\label{sec:bridge-codon-e1-conservation-transportability}
% CLAIM_DETAIL claim_id=bridge.genetic_code.codon_e1_conservation_transportability status=certified
\textbf{Statement.} CERTIFIED (held-out, scoped E1-transportability; extends \#18, NOT Window6, NOT causal): the
first-order codon-E1 structure of the synonymous codon-usage residual is conserved NOT ONLY in
bacteria (the landed \#18 result: 29 organisms / 18 genera, T=0.571) but INDEPENDENTLY in archaea
AND in eukaryotes, analyzed as SEPARATE external strata (never pooled). From Kazusa CUTG
standard-nuclear-code (genetic code table 1) per-species codon usage (frozen panel
synced/codon\_e1\_transport\_panel.json: 12 archaea + 14 eukaryotes), each organism's d\_resid4 =
relative codon frequencies over the 61 sense codons with synonymous-family means removed; r\_o =
||P\_B1 d\_resid4,o||\textasciicircum{}2/||d\_resid4,o||\textasciicircum{}2 onto the SAME codon-E1
subspace B1 = col(Q\_syn R U1) (rank 6, U1 = first-order lambda=5 H(3,4) eigenspace) used in \#18; T
= organism-mean r\_o per domain; the null is the same matched-synonymous-relabel control (global
within-synonymous codon relabel, 20000 draws). RESULT: ARCHAEA (n=12) T=0.522 vs null mean 0.137
(z=9.62, one-sided p=5e-5), cluster/organism bootstrap lower-95\% excess 0.294>0 -> conserved;
EUKARYOTA (n=14) T=0.515 vs null mean 0.140 (z=8.80, one-sided p=5e-5), bootstrap lower-95\% 0.270>0
-> conserved. Both domains independently clear the pre-registered gate; the chance projection onto
the 6-dim B1 of a within-synonymous (\textasciitilde{}41-dim) vector is \textasciitilde{}6/41=0.146,
which the null reproduces, and the data sits \textasciitilde{}3.5x above it. INTERPRETATION:
combined with \#18 (bacteria), the genetic code's first-order (additive, wobble-dominated)
translation-residual structure is UNIVERSALLY conserved across ALL THREE DOMAINS OF LIFE (bacteria,
archaea, eukaryotes), at \textasciitilde{}0.51-0.57 of each organism's synonymous-residual norm vs
\textasciitilde{}0.146 chance. SCOPE / not-claimed: this is E1-transportability (a generalization of
\#18 to new domains), NOT Window6 (the Window6 spectrum is provably disjoint from codon E1); NOT a
causal/selection mechanism; standard-nuclear-code species only (alternative-code taxa excluded).
ROBUSTNESS: adversarially verified -- Kazusa codon counts are genuine per-species tables (live
re-fetch matched all 26 taxids exactly; 61 RNA sense codons, stops excluded, nonuniform real
counts), all genetic\_code\_table=1 with no alternative-code contaminants, the null is fair
(\textasciitilde{}chance baseline, independent draws reproduce), the SAME rank-6 B1 as \#18, archaea
and eukaryota analyzed strictly separately. Evaluated only by its registered deterministic
experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} certified because codon-E1 conservation transports in archaea, eukaryota.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_e1\_transport\_gate.py

\subsection{Golden Fibonacci Impossibility}\label{sec:bridge-golden-fibonacci-impossibility}
% CLAIM_DETAIL claim_id=bridge.translation_kernel.golden_fibonacci_impossibility status=refuted
\textbf{Statement.} REFUTED (correspondence-negative; closes bridge direction C, the dynamical-kernel Fibonacci
program): no biology-defined dynamical translation kernel K\_theta (an operator pushing an ORF
through codon->tRNA-decoding->protein) can carry the actual Fibonacci recurrence $x^2-x-1$ (golden,
root phi=(1+sqrt5)/2\textasciitilde{}1.618) in its characteristic polynomial / spectrum / trace
recurrence. A biologically legitimate discrete decoding kernel is a nonnegative
column-(sub)stochastic matrix P, whose spectral radius rho(P)<=1; since phi>1, $x^2-x-1$ cannot
divide chi\_P (divisibility would require phi as an eigenvalue). A CTMC generator Q (nonneg
off-diagonal, <=0 column sums) has all eigenvalues with non-positive real part, so phi cannot appear
there either, and its eigenvalues carry inverse-time units so a unit-free integer polynomial is not
an invariant. The companion/golden matrix [[1,1],[1,0]] realizing $x^2-x-1$ requires an UNNORMALIZED
two-state path-counting quotient with a no-consecutive-state rule -- an imposed Window6 automaton
biology does not supply. Oracle-derived (spectral-radius argument) + codex-implemented +
orchestrator re-ran. SCOPE: closes the dynamical-Fibonacci route; does NOT touch the certified
edge-hiding extremality (the sole certified Window6<->codon relation, a static code-table functional
not a dynamical operator). REOPENING CONDITION: a biology-defined, parameter-independent
two-dimensional invariant quotient with a canonically integer-valued, unit-invariant transfer matrix
whose characteristic polynomial is forced to $x^2-x-1$ for every admissible theta -- not currently
specifiable. Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} any biological (sub)stochastic translation operator has spectral radius <= 1 < phi, and any CTMC
generator has eigenvalues with non-positive real part; $x^2-x-1$ cannot divide their characteristic
polynomial because divisibility would require the forbidden root phi; realizing it needs an
unnormalized 2-state no-adjacent counting quotient biology does not supply.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_translation\_kernel\_golden\_impossibility.py

\subsection{Edge Hiding E1 Orientation Same Signal}\label{sec:bridge-edge-hiding-e1-orientation-same-signal}
% CLAIM_DETAIL claim_id=bridge.genetic_code.edge_hiding_e1_orientation_same_signal status=refuted
\textbf{Statement.} REFUTED (operational independence of the two certified bridge positives): the static code-table
edge-hiding extremality (e\_in / BC7) and the live codon-E1 synonymous codon-usage residual are NOT
the same structural signal. They share the |R|=13 support set, but that equality is design-induced
(the three sixfold families canonically give 18 codons in three 4+2 branches, not 13) and is
excluded from the statistic. CRITICAL anti-tautology guard (oracle-identified): the E1 projector
satisfies (P\_1)\_\{cc'\}=(9-4 d\_H)/64, hence tr(U1\textasciicircum{}T S\_g U1)=(5/32) e\_in(g)
EXACTLY for every code -- the scalar 'edge-hiding has E1 mass' is algebraically guaranteed and is
NOT evidence. The test deflates this: C\_g=U1\textasciicircum{}T S\_g U1-(5 e\_in/288) I\_9; B\_g=P
C\_g P-(tr(P C\_g P)/6) P (trace-deflated orientation operator in the rank-6 identifiable E1
subspace, P=L\textasciicircum{}+L, L=Q\_syn R U1); bio side a\_o=L\textasciicircum{}+ d\_resid4,o,
Sigma\_bio=sum\_o w\_o a\_o a\_o\textasciicircum{}T/||a\_o||\textasciicircum{}2 (equal-genus,
equal-domain); statistic T\_same=tr[Sigma\_bio B\_g\textasciicircum{}2/tr(B\_g\textasciicircum{}2)].
Null = exact Omega\_\{BW,E,d,comp\} (162468 states; conditions OUT e\_in + box/wobble + edge-margins
+ boundary-degrees + component-structure), recomputing B\_g per code while holding the measured
Sigma\_bio fixed. RESULT (per domain, separately): bacteria T=0.1920 vs null mean 0.1981 (upper-tail
p=0.984, bootstrap lo95 excess -0.024); archaea T=0.1733 vs null 0.1761 (p=0.677, lo95 -0.014);
eukaryota T=0.1726 vs null 0.1722 (p=0.443, lo95 -0.023). No domain shows exceptional positive
orientation alignment; all three bootstrap lower-95\% excess < 0. INTERPRETATION: edge-hiding is
static code-table geometry; codon-E1 is an independent live usage residual -> the bridge holds TWO
INDEPENDENT certified positives, not one. SCOPE: bio-internal (relates two genetic-code positives),
does NOT reopen Window6. ROBUSTNESS: adversarially verified -- algebraic-identity sanity check
passed exactly (tr=10.46875=(5/32)e\_in), |R|=13 excluded from the statistic, exact 162468-state
null, deflation faithful to the oracle formula, no scratch-file dependency, numpy-free. Evaluated
only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} At least one domain is not an exceptional positive upper-tail alignment after deflating the
algebraically forced E1 trace and conditioning on the exact null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_edge\_hiding\_e1\_orientation\_same\_signal.py

\subsection{Codon E1 Frameshift Mpra Translation Dependent Function}\label{sec:bridge-codon-e1-frameshift-mpra-translation-dependent-function}
% CLAIM_DETAIL claim_id=bridge.genetic_code.codon_e1_frameshift_mpra_translation_dependent_function status=coincidence
\textbf{Statement.} COINCIDENCE (well-controlled, CSC-validity-gated honest negative; scope: codon biology, NOT Window6,
NOT causal-certified): the universal codon-E1 direction (first-order/wobble synonymous codon-usage
residual, conserved across all 3 domains of life) has NO detected GC-orthogonal
TRANSLATION-DEPENDENT functional effect on mRNA level in the Chen-Park-Subramaniam 2024 yeast
8x-dicodon reporter. METHOD: bridge reconstructed per-codon-pair mRNA effects from raw SRA
(PRJNA974090) for the in-frame (pHPSC1142) and -1 frameshift (pHPSC1114) libraries by faithfully
reproducing the rasilab counting+linkage pipeline (validated: known destabilizing dipeptides
KK/RR/KW/proline sit in the bottom in-frame tail and the -1 frameshift relieves them, Delta up to
+1.6; non-destabilizer AA positive). Translation-dependent contrast
Delta\_y=y\_inframe-y\_frameshift per codon-pair. v\_star in R\textasciicircum{}61 =
leave-S.cerevisiae-out cross-domain codon-E1 consensus (54 non-yeast organisms / 41 genus groups,
rank-6 B1 subspace, sign fixed externally -> non-circular, does not use the yeast reporter).
T\_causal=weighted corr of residualized(Delta\_y, s\_cd=(v\_star[c]+v\_star[d])/2) against control
matrix Z; within-synonymous-family permutation null (100k), sign-invariant two-sided; mandatory CSC
assay-validity gate. RESULT: baseline Z (in-frame aa-pair FE + -1-frame aa-pair FE + yeast CSC both
frames + yeast tAI both frames + read-depth) gives |T|=0.154, two-sided p=0.0052, bootstrap CI
[-0.221,-0.085] (excludes 0); ADDING GC + GC3 + the 16 dinucleotide sequence controls COLLAPSES it
to |T|=0.029, p=0.605, bootstrap CI [-0.090,+0.032] (includes 0), with s still estimable (residual
frac 0.185, control rank 9->24). CSC positive control passed (corr 0.157, p 0.0013) -> the assay
genuinely detects translation-dependent stability, so this is a real, powered null. INTERPRETATION:
the apparent codon-E1 reporter association is the GC/RNA-secondary-structure (Delta-MFE) composition
confound -- which is FRAME-INVARIANT and therefore NOT removed by the in-frame-minus-frameshift
subtraction -- not a codon-E1 translation-dependent effect; codon-E1 remains a conserved usage
GEOMETRY without a detected direct functional phenotype beyond composition. PROCESS: an sshx
adversarial panel (2 needs\_derivation / 1 coincidence) caught that the original one-sided test was
ill-posed (arbitrary v\_star sign) AND that the omitted Delta-MFE/GC confound could manufacture the
magnitude; the GC3+dinucleotide control was the decisive numpy-free fix that resolved it to a clean
coincidence. NOT-CLAIMED: an exact ViennaRNA-Delta-MFE-controlled + within-site randomized
synonymous-recoding (MAVE) CAUSAL test (relayed to bio as needs\_bio\_data) could still detect a
GC/folding-orthogonal effect; this result bounds the OBSERVATIONAL public-reporter reach only, and
is not Window6 (the structural fibonacci<->codon frontier is separately closed). Evaluated only by
its registered deterministic experiment.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The apparent codon-E1 association collapses after GC3+dinucleotide sequence controls; the reporter
result is consistent with a GC/RNA-folding composition confound, not a GC-orthogonal
translation-dependent codon-E1 effect.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_e1\_frameshift\_mpra\_tcausal.py

\subsection{Wobble Boundary Vs Codon E1}\label{sec:bridge-wobble-boundary-vs-codon-e1}
% CLAIM_DETAIL claim_id=bridge.fibonacci_tracka.wobble_boundary_vs_codon_e1 status=needs_derivation
\textbf{Statement.} needs\_derivation (distinct bio fact; the bridge houses fibonacci\_reality's homeless Track-A result
+ bounds it). fibonacci\_reality's Track-A codon-wobble-boundary-response (deliberately math-free, '
phi/571/Z6 preset') reproduces as a REAL cross-organism bio signal: codons whose Watson-Crick
cognate tRNA is ABSENT (wobble-decoding-required) show a tRNA-supply boundary response (yeast
delta\_r2=0.0198, null percentile 1.0, n=5785; human delta\_r2=0.0047, null percentile 1.0,
n=18974). Bridge adjudication vs its certified positives
(run\_fibonacci\_tracka\_wobble\_vs\_codon\_e1.py): the pooled per-codon Track-A signal is NOT
significantly concentrated in codon-E1 (rho\_E1=0.183 vs synonymous-relabel null mean 0.104,
upper-tail p=0.113; rank-B1 chance 6/41=0.146), and overlaps the edge-hiding |R|=13 set at only 3
codons (CUC,CUG,CUU) vs degeneracy-matched null 3.82 (p=0.91, chance-level). The Window6 connection
is q6\_circular: Track-A is bio-internal codon/tRNA-supply structure; treating its wobble
coincidence as Window6 evidence would reuse the already-closed q6 convention (the Gamma\_6/H(3,4)
intertwiner is 0; measure-level maps orbit-average to 0). CONCLUSION: Track-A is a DISTINCT, real,
conserved bio supply-boundary fact that (a) is not the same signal as codon-E1 or edge-hiding under
matched nulls and (b) does not establish a new non-circular math<->bio Window6 link. SCOPE:
bio-internal codon biology; NOT Window6 forcing; reopening still requires the oracle's
no-adjacent-one mechanism condition. The bridge correctly serves as the venue for this
math-pipeline-originated bio result that fibonacci's math-only paper cannot house. Evaluated only by
its registered deterministic experiment.

\textbf{Verdict.} \texttt{needs\_derivation}.

\textbf{Why.} Track-A is a distinct bio-internal codon/tRNA supply-boundary fact: yeast and human support the
boundary-response, but the pooled per-codon signal is only moderately in codon-E1 and not
significant under the synonymous relabel null; edge-hiding overlap is chance-level under degeneracy
matching; no non-circular Window6 forcing argument is present.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_fibonacci\_tracka\_wobble\_vs\_codon\_e1.py

\subsection{Codon E1 Alternative Code Deformation}\label{sec:bridge-codon-e1-alternative-code-deformation}
% CLAIM_DETAIL claim_id=bridge.genetic_code.codon_e1_alternative_code_deformation status=coincidence
\textbf{Statement.} Codon-E1 alternative-code deformation: tests whether the first-order/wobble codon-E1
synonymous-usage residual is code-table-specifically conserved across NCBI alternative genetic-code
mitochondrial genomes (vertebrate-mito table 2, invertebrate-mito table 5), using per-table B1\_t =
col(Q\_syn,t R\_t U1), matched within-synonymous-family relabel nulls (20000), a
code-specific-vs-standard-restricted strong-version Delta T, and lineage-cluster bootstrap (10000).
Deepens the certified codon-E1 three-domain conservation; explicitly NOT Window6 forcing.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} This is a codon-E1 alternative-code deformation test with table-specific synonymy, matched
within-family relabel nulls, and lineage-cluster bootstrap.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_e1\_alternative\_code\_deformation.py

\subsection{Codon E1 Trna Decoding Supply Orientation}\label{sec:bridge-codon-e1-trna-decoding-supply-orientation}
% CLAIM_DETAIL claim_id=bridge.genetic_code.codon_e1_trna_decoding_supply_orientation status=refuted
\textbf{Statement.} Codon-E1 tRNA decoding-supply orientation certificate: tests whether the synonymous codon-usage
residual E1 direction aligns, within the same RefSeq complete bacterial/archaeal genome accession,
with that genome's tRNA anticodon decoding-supply E1 direction (frozen standard-code rank-6 B1,
reused not refit; cos statistic; genus-weighted; global + restricted synonymous relabel nulls;
full-residual positive control; B1-minus-p3W + GC3 collapse gate). Deepens the certified codon-E1
result as a mechanism-orientation probe; decoding-supply orientation concordance only, NOT Window6
forcing, NOT causal proof.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} decoding-supply orientation concordance only; not Window6 forcing; not causal closure or causal
proof; B1 is reused from the frozen standard-code artifact and is not refit.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_e1\_trna\_decoding\_supply\_orientation.py
