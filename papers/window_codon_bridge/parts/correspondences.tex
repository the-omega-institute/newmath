\section{Bridge Correspondence Verdicts}
The following table lists every bridge claim in the registry.  The verdict column is
generated directly from the registry status field.

\begin{longtable}{>{\raggedright\arraybackslash}p{0.22\linewidth}>{\raggedright\arraybackslash}p{0.55\linewidth}>{\raggedright\arraybackslash}p{0.15\linewidth}}
\textbf{Claim} & \textbf{Statement} & \textbf{Verdict} \\
\hline
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.cardinality_forcing status=coincidence
Cardinality Forcing & The codon-Q6 boundary cardinality ($|R|=13=F_7$, 21 synonymous families) is FORCED by the Window6 Fibonacci horizon ($X_6=21=F_8$) via an explicit structural map standard-code-boundary -> width-6 Fibonacci words, not coincidence. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.spectral_correspondence status=refuted
Spectral Correspondence & The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, mu) equal the Window6 coarse Markov kernel spectrum (high/low energy superposition). & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.foldbin_codon_positions status=coincidence
Foldbin Codon Positions & The Window6 Foldbin tail-cube (3 free Boolean coords, seam-return $\rho_6-s_6=3$) corresponds to the 3 codon positions / wobble folding; the bio $Q^{(2)}$ second-order structure is the biological image of the second fold. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.killed_walk_spectral_correspondence status=refuted
Killed Walk Spectral Correspondence & The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, $\mu=6\lambda_M-1$) -- where lambda = (1/6)*spectral radius of the induced 6-cube subgraph adjacency on R and on M=median-closure(R) -- are structurally forced by the Window6 Fibonacci-cube killed-walk operator (same induced-subgraph spectrum / canonical map), not a numeric coincidence. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.cyclic_sector_frame_symmetry status=refuted
Cyclic Sector Frame Symmetry & Window6 cyclic Zeckendorf sector rotation corresponds to cyclic rotation of the 3 codon positions as a code-label-preserving sector character. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.right_boundary_selection_axis status=coincidence
Right Boundary Selection Axis & The Window6 right-boundary/foldbin escape functional $U_R$ (cyclic endpoint collision $w_1=w_6=1$, escape ledger |d$U_R$|/(6|$U_R$|)=8/9) maps onto the BIOLOGICAL low-optimality boundary axis -- R=tRNA-poor (BIO-M) and the unexplained universal residual axis $d_{\perp}$ (BIO-P) -- via sign/rank/projection constraints, NOT via naked cardinalities (|R|=13, 21, 3). & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fold_residual_wobble_correspondence status=coincidence
Fold Residual Wobble Correspondence & Window6's Fold6 hidden tail-offset residual modes and the standard code's third-position wobble residual modes are the same kind of hidden residual in a shared 'regular micrograph -> visible family -> hidden residual' fold-forcing mechanism (low-energy = first/second-position core, high-energy = third-position/tail residual): they leave visible family/type invariant but govern fiber multiplicity, lumpability failure, and quotient-kernel subleading spectrum -- tested by label-free invariants + hidden-mode alignment under graph/wobble-preserving nulls, NOT by the 64->21 count or any 21-label pairing. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.edge_isoperimetric_contravariance status=certified
Edge Isoperimetric Contravariance & Window6 Fold6 and the standard genetic code realize OPPOSITE achievable extremes of one label-free local-edge-hiding functional e\_in (one-step micrograph edges hidden inside a family): Window6 is the exact separating endpoint (e\_in=0=e\_min, tau\_W=0; every Q6 1-bit edge changes the stable type) and the standard code is the near-maximal tolerant endpoint (e\_in=69, e\_max=72, tau\_C=0.958), surviving graph- and wobble/box-preserving nulls. This is a C-0 contravariant-extremality certificate: it does NOT claim the two are isomorphic folds, that 64->21 is biologically meaningful, that high/low-energy maps to DNA/RNA/translation, or any common fold-FORCING law. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.parry_measure_usage_correspondence status=coincidence
Parry Measure Usage Correspondence & The Window6 Parry measure (golden maximal-entropy stationary measure of the no-adjacent-one transfer operator) corresponds, per-codon in the Q6 frame, to the codon usage-frequency selection axis ($d_{\perp}$) -- tested by rank/projection vs label-free nulls, not by any label pairing. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.smith_normal_form_cokernel status=refuted
Smith Normal Form Cokernel & The Smith-normal-form cokernel (finite abelian group invariant) of the Window6 edge matrix is isomorphic to that of the canonical codon adjacency/quotient matrix -- a label-free group-isomorphism test. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.three_rigidity_codon_graph status=refuted
Three Rigidity Codon Graph & The Window6 type-quotient graph and the codon synonymous family-quotient graph are rigidity-equivalent (share canonical label-free rigidity invariants: automorphism group, degree sequence, spectrum, distance profile) beyond a degree-preserving null. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.mod3_flux_frame_obstruction status=coincidence
Mod3 Flux Frame Obstruction & The Window6 edge-flux mod-3 obstruction class in H\textasciicircum{}1(G\_W;Z3) is cohomologically of the same type as the codon reading-frame/f3-ramp three-periodicity phase class, tested by Z3-Hodge signature (r3,T3,phase,cycle-integrals) up to D3 vs frame/wobble/family nulls. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.partition_quotient_wobble_foldbin status=coincidence
Partition Quotient Wobble Foldbin & The Window6 (Fold6, Zeckendorf-cylinder) partition pair and the codon (16-box, synonymous-family) partition pair have the same label-free partition-lattice signature (meet/join sizes, VI, equitable defect, boundary profile, quotient SNF) beyond size/graph-preserving nulls. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.green_spectral_codon_q_spectra status=refuted
Green Spectral Codon Q Spectra & The Window6 Green/resolvent spectrum has the same label-free spectral signature (density, gap, moments) as the codon q1/q3/q4 codon-cube quotient spectra beyond a degree-preserving null. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.edge_exhaustion_meta_certificate status=certified
Edge Exhaustion Meta Certificate & Within the pre-registered label-free invariant battery (tau plus spectral/Parry/SNF/mod3/partition/rigidity), the ONLY certified Window6<->codon cross-domain relation is the contravariant local-edge-hiding extremality (tau\_W=0 separating endpoint, tau\_C=0.958 tolerant endpoint); conditioned on this e\_in extremality and native graph/fiber constraints, all remaining invariants have residual alignment indistinguishable from a conditional null (operational independence). The realization mechanisms are polarized (Window6 isotropic separator, codon anisotropic third-position tolerator). NOT an isomorphism, NOT a forcing, NOT strict C-1 profile duality. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fourcell_selection_enrichment status=refuted
Fourcell Selection Enrichment & The Window6 4-cell coarse partition (sizes 27/22/9/6) explains significant non-random variance in the real 4D codon selection vectors (tRNA/f3/$d_{\perp}$/optimal) beyond cell-size-preserving and GC-conservative nulls. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.fold_structure_64_16_25_21 status=certified
Fold Structure 64 16 25 21 & The standard genetic code is an exact two-layer 64->16->25->21 fold (16 doublet boxes = 8 pure + 7 binary-split + 1 tri-split; 25 box-output fragments minus 4 cross-box merges = 21 classes; degeneracy spectrum 1\textasciicircum{}2,2\textasciicircum{}9,3\textasciicircum{}2,4\textasciicircum{}5,6\textasciicircum{}3) -- the hard biological instance of the abstract microstate->block->seam->stable fold pattern. NOT a golden-ratio forcing; Zeckendorf decompositions universal/presentational; ratios are ratio-of-counts. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.neutral_hamming_adjacency status=certified
Neutral Hamming Adjacency & The standard code's per-codon Hamming-1 neutral-neighbor histogram is exactly 0:2,1:22,2:8,3:28,4:4 (merged defect = 28/22/8/6), close to but not equal to the Window6 27/22/9/6; no natural untuned biological weighting bridges them. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.fold_order_parameter_usage status=coincidence
Fold Order Parameter Usage & The fold order parameter m\_bio = P\_bulk - P\_seam computed from real multi-organism codon usage carries an independent (non-GC) cross-organism signal. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.neutral_trna_edge_coupling status=coincidence
Neutral Trna Edge Coupling & The certified Window6 edge score modulates the tRNA supply-demand alignment on synonymous Hamming-1 edges (usage-edge-gradient \textasciitilde{} tRNA-edge-gradient x Window6-edge-score, interaction beta\_bridge), beyond GC/f3 and edge-permutation nulls, with held-out-species predictive lift. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.twolayer_fold_edge_hiding_extremality status=certified
Twolayer Fold Edge Hiding Extremality & The contravariant edge-hiding extremality iterates across the genetic code's two-layer fold: the first-two-base box partition (64->16) is the EXACT maximum-edge-hiding partition for its size-4x16 block profile (tau\_box=1.0), and the family layer (64->21) is near-maximal (tau\_family=0.958), both in the extreme upper tail of a same-block-size null; Window6 Fold6 sits at the opposite extreme (tau=0). Restricted to iterated edge-hiding extremality; NOT isomorphism or biological forcing. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.foldlayer_zigzag_extremality status=certified
Foldlayer Zigzag Extremality & The standard genetic code realizes a zig-zag edge-hiding fold layer B <- M=B meet F -> F: the first-two-base box layer and box-family fragment layer are exact edge-hiding endpoints, while the family layer is near-maximal with a small deficit. This is a finite label-free extremality certificate, not a monotone quotient tower, isomorphism, biological forcing, or live-usage claim. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.halfbox_fibonacci_tower status=coincidence
Halfbox Fibonacci Tower & The genetic-code halfbox and family-meet layers create exact edge-hiding endpoint facts and a strict Fibonacci tower across the naturally generated block counts 13, 21, 34, and 55 under canonical finite partition operations. The claim must separate hard extremality facts from numerical tower appearances. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.window6.fold_tower_separating_extremality status=refuted
Fold Tower Separating Extremality & The Window fold tower is edge-separating at each tested finite width under the same value-window convention that reproduces the Window6 certificate, yielding a cross-width revealing endpoint dual to the genetic-code hiding tower. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.edge_defect_axis_cert status=coincidence
Edge Defect Axis Cert & The standard genetic code's local-edge-hiding deficit is exactly e\_max-e\_in=3, supported only on Stop and Ser, with Stop contributing one punctuation defect and Ser contributing two sense split defects. The sense-side SerSplitDefect vector should project onto an available codon-Q6 selection axis, especially an independent fourth residual axis if present. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.edge_defect_projection_harness status=certified
Edge Defect Projection Harness & The SerSplit edge-defect projection audit has a deterministic 61-sense-codon harness: it builds forbidden controls from available codon-Q6 selection and codon-chemistry columns, residualizes candidate axes and the SerSplit contrast through the same matrix, detects only the forbidden positive control, and does not promote oracle output or local controls into biological evidence. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.ser_island_fidelity_cost status=coincidence
Ser Island Fidelity Cost & The Ser UCN/AGY split creates a code-table near-cognate error-cost asymmetry: UCN and AGY have different one-step missense/stop neighborhoods, and this asymmetry could seed a translation-fidelity selection route only if it is exceptional beyond matched codon-family nulls and later survives site-level expression/mistranslation tests. & \texttt{coincidence} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.synthetic.negative.control.axis.pack status=needs_derivation
Pack & Use only in-repo deterministic codon features and seeded synthetic vectors to audit the common projection harness before any external candidate axis is attempted. This autonomous bridge claim is evaluated only by its registered deterministic experiment. & \texttt{needs\_derivation} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.sersplit.edge_defect.nonestimability.certificate status=certified
Certificate & The Ser-split edge-defect contrast (within-Ser 4-vs-2: \{UCU,UCC,UCA,UCG\} vs \{AGU,AGC\}) lies exactly in the column span of the registered forbidden-control matrix C (verified against the actual registered z-scored columns to 1e-9), so its residual is algebraically zero, v\_SerSplit is undefined, and no real-data biological axis can yield a certified independent projection onto this target. On the Ser support, sqrt(12)*u = 1 - 3*indicator(first base is purine), so the edge defect IS a registered composition control. The 5-dim within-Ser zero-sum contrast subspace is NOT fully absorbed, so the 15-way within-Ser permutation null is non-degenerate. This autonomous bridge claim is evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.foldboundary.residual.exhaustion.certificate status=certified
Certificate & The pre-registered, q6-label-free fold-boundary contrast subspaces over the 61 sense codons -- box split-type (pure/split/tri-split coarsening of the 16 first-two-position boxes), degeneracy-class (amino-acid degeneracy sizes), and fold-layer (64->16->25->21) -- are exactly absorbed (residual rank 0) by the registered forbidden-control matrix C, so these bio-intrinsic fold boundaries provide no independent residual axis beyond the registered composition controls (a bounding exhaustion extension of the edge-exhaustion meta-certificate). The Window6 four-cell partition mapped via q6\_label is correspondence-derived, not bio-intrinsic; because C contains no q6 information its residual survival (rank 3) is expected and is barred from the certificate, not treated as independent forcing. NOT certified here: the full 16-box partition's fine structure -- its only possible escape directions are the Leu 4/2 and Arg 4/2 within-amino-acid branch splits (analogous to the certified Ser 4/2), which are the next frontier. This autonomous bridge claim is evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.box_partition.composition_collapse.certificate status=certified
Certificate & Each of the three 6-fold-degenerate amino acids has a 4-vs-2 within-amino-acid branch split (Ser UCN|AGY, Leu CUN|UUR, Arg CGN|AGR), and exact rational elimination proves ALL THREE lie in the span of the registered forbidden-control matrix C (residual 0), with on-support identities sqrt(12)*u = 1-3*indicator(first base purine) for Ser, 1-3*indicator(first two bases equal) for Leu, and -2+3*indicator(first base GC)+... for Arg. The full mean-zero 16-box first-two-position indicator subspace (dim 15) has exact residual rank 0 against C (exact\_control\_rank 29 = rank with box partition appended), so the entire 64->16 box partition is absorbed by the registered composition control frame -- it is composition, not an independent forcing direction. Generalizes the certified Ser-split non-estimability to all three 6-fold amino acids and completes the box-partition thread of the fold-boundary exhaustion. Proof tied to the actual registered columns (registered-equivalence, fail-closed on drift), branch contrasts label-free (no q6/Stop). Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.forcing_direction_closure.metacertificate status=certified
Metacertificate & Consolidation meta-certificate for the math->bio forcing direction. Exact rational rank proves the FULL pre-registered label-free fold-boundary battery (box partition + degeneracy classes + fold layers + the three 6-fold amino-acid 4/2 branch splits Ser/Leu/Arg; 44 columns, span rank 22) lies AS ONE UNION exactly in span(C): exact control rank 29 is unchanged when the battery is appended (absorbed). The only fold-boundary direction outside span(C) is the q6\_label-derived Window6 four-cell partition, which is correspondence-derived and barred as circular (q6 is the correspondence under test). Therefore the bridge's certified scope is: (1) POSITIVE - the sole certified structural signal is the BC7 contravariant edge-hiding extremality plus the edge-exhaustion meta-certificate (Window6/Fold6 and the standard code occupy opposite endpoints of one label-free local-edge visibility functional); (2) BOUNDING-NEGATIVE - the label-free bio fold-boundary structure is composition (= span(C)), leaving no independent bio-side residual axis for Window6 to force; (3) the math->bio FORCING direction is closed-as-not-supported under the current frame, with count/spectrum matches classified coincidence and q6-mediated forcing barred as circular. REOPENING CONDITION (falsifiable): a label-free genetic-code invariant outside span(C), defined without q6 or family counts, whose preservation forced-Window6/Fibonacci/Zeckendorf structure necessitates more strongly than a pre-registered composition-controlled null. Evaluated only by its registered deterministic experiment. & \texttt{certified} \\
% CLAIM_STATUS claim_id=bridge.genetic_code.h3_higherorder.complement_stresstest status=refuted
Complement Stresstest & Stress-test claim: the composition-exhaustion closure extends to the canonical higher-order class H3 (the 27 three-position nucleotide-chemistry interaction tensors chi\_a(base1)*chi\_b(base2)*chi\_c(base3), chi in \{R/Y, W/S, K/M\}; genuinely higher-order, label-free, non-q6). VERDICT REFUTED: exact Fraction rank shows H3 has residual rank 26 of 27 after the registered composition control frame C (exact control rank 29; rank with H3 appended 55), so higher-order three-position chemistry structure lies almost entirely OUTSIDE span(C). This precisely DELIMITS the composition-exhaustion: not all label-free codon structure collapses to composition -- the additive/pairwise+fold structure does, but higher-order three-way chemistry interactions do not. CRITICAL: this is a delimitation, NOT a math->bio forcing reopening -- there is no Window6-necessitation test, no pre-registered necessitation null, and no principle selecting H3 among higher-order audit languages (Walsh/Fourier, codon-graph spectral, automorphism-orbit); H3 is recorded only as a candidate class for future forcing work requiring a frozen necessitation null. The forcing-direction closure stands. Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fibcube_embedding.conventionrobust_test status=needs_derivation
Conventionrobust Test & Reopening attempt for the math->bio forcing direction via a NON-q6, first-principles embedding. Test: is the Fibonacci-cube vertex set S = \{21 no-adjacent-1 six-bit words\} (the canonical Gamma\_6 isometric inclusion into the codon 6-cube Q6) a CONVENTION-ROBUST code-structure outlier when mapped into codon space across ALL 17280 admissible base->2bit x bit/position conventions (q6 being just one marked convention, class size 2), vs a composition-matched null on pre-declared label-free statistics (synonymous-edge / family-boundary / fold-cell / conservative-substitution), Holm-Bonferroni corrected? VERDICT needs\_derivation: NO forcing signal -- the q6 class is NOT an outlier (q6\_class\_outlier False) and only 4.7\% of non-q6 conventions are outliers (far below the 0.5 robustness threshold, \textasciitilde{}noise). So the canonical Gamma\_6->Q6 isometric-inclusion route produces neither a convention-robust non-q6 forcing nor a q6-special alignment: this most-natural non-circular embedding route is EXHAUSTED on the pre-declared battery. This is NOT an exhaustive impossibility proof (other label-free invariants / non-inclusion constructions untested), hence needs\_derivation not refuted. It does not touch BC7 (which concerns the Fold6 PARTITION edge functional, a different object from the Gamma\_6 vertex set). Evaluated only by its registered deterministic experiment (seed 619423). & \texttt{needs\_derivation} \\
% CLAIM_STATUS claim_id=bridge.window6_codon_q6.fibcube_codonfamily_graphiso_test status=refuted
Fibcube Codonfamily Graphiso Test & Convention-free reopening route (distinct from the inclusion route): is the Fibonacci cube Gamma\_6 (graph on the 21 no-adjacent-1 six-bit words, Hamming-1 edges; 21 vertices, 38 edges) ISOMORPHIC as an abstract graph to the codon family-adjacency graph (families adjacent iff some codon pair is Hamming-1)? A graph iso is intrinsic (no codon-coordinate convention), so it sidesteps the inclusion route's convention problem. VERDICT REFUTED: they fail necessary isomorphism invariants -- Gamma\_6 has 38 edges vs 85 for the 21-family graph (incl Stop) and the excl-Stop variant has 20 vertices/75 edges; degree sequences also differ (Gamma\_6 max degree 6 vs family-graph degrees 6-13). Robust across family-graph definitions. So the convention-free graph-isomorphism reopening route is CLOSED: the 21<->21 cardinality match is a graph-level COINCIDENCE, not structural forcing. Together with the inclusion-route exhaustion (fibcube\_embedding\_conventionrobust\_test), both natural non-q6 Window6->codon embedding constructions are now closed. Evaluated only by its registered deterministic experiment. & \texttt{refuted} \\
\end{longtable}

\subsection{Cardinality Forcing}\label{sec:bridge-cardinality-forcing}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.cardinality_forcing status=coincidence
\textbf{Statement.} The codon-Q6 boundary cardinality ($|R|=13=F_7$, 21 synonymous families) is FORCED by the Window6
Fibonacci horizon ($X_6=21=F_8$) via an explicit structural map standard-code-boundary -> width-6
Fibonacci words, not coincidence.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The counts 21 and 13 match the width-6 and width-5 Fibonacci cubes, but the tested canonical
biological structures do not yield a structure-respecting forcing map.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc1\_cardinality\_forcing.py

\subsection{Spectral Correspondence}\label{sec:bridge-spectral-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.spectral_correspondence status=refuted
\textbf{Statement.} The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, mu) equal the Window6 coarse Markov
kernel spectrum (high/low energy superposition).

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} bio spectral scalars are not the coarse Markov kernel spectrum (subdominant eig do not match lambda;
mu>1 is not a stochastic eigenvalue).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc2\_markov\_kernel\_spectrum.py

\subsection{Foldbin Codon Positions}\label{sec:bridge-foldbin-codon-positions}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.foldbin_codon_positions status=coincidence
\textbf{Statement.} The Window6 Foldbin tail-cube (3 free Boolean coords, seam-return $\rho_6-s_6=3$) corresponds to the
3 codon positions / wobble folding; the bio $Q^{(2)}$ second-order structure is the biological image
of the second fold.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} foldbin tail-cube vs codon positions: 3=3 count match only; weight-order vs
value-window-marginal-order vs reverse-constraint give 3 different assignments (no forcing); 8-state
reading fails (only 4 tail states); $Q^{(2)}$ abundance reading failed (Route B).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc3\_foldbin\_codon\_correspondence.py

\subsection{Killed Walk Spectral Correspondence}\label{sec:bridge-killed-walk-spectral-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.killed_walk_spectral_correspondence status=refuted
\textbf{Statement.} The bio codon-Q6 spectral scalars ($\lambda_M$, $\lambda_R$, $\mu=6\lambda_M-1$) -- where lambda =
(1/6)*spectral radius of the induced 6-cube subgraph adjacency on R and on M=median-closure(R) --
are structurally forced by the Window6 Fibonacci-cube killed-walk operator (same induced-subgraph
spectrum / canonical map), not a numeric coincidence.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} BC2-R1 is refuted: the operators have the same induced-subgraph killed-walk form, but the bio sets
and Window Fibonacci cubes are not the same cube-embedded graph, their exact adjacency
characteristic polynomials differ, and the bio radial factor does not occur in the Window graph
spectra.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc2\_killed\_walk\_spectrum.py

\subsection{Cyclic Sector Frame Symmetry}\label{sec:bridge-cyclic-sector-frame-symmetry}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.cyclic_sector_frame_symmetry status=refuted
\textbf{Statement.} Window6 cyclic Zeckendorf sector rotation corresponds to cyclic rotation of the 3 codon positions as
a code-label-preserving sector character.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} codon-position rotation preserves the amino-acid/stop label for only 4/64 codons (UUU,CCC,AAA,GGG)
and positional degeneracy is strongly asymmetric
(pos1\textasciitilde{}0.042,pos2\textasciitilde{}0.010,pos3\textasciitilde{}0.667), so no
code-label-preserving rotation image exists.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc4\_cyclic\_sector\_frame\_symmetry.py

\subsection{Right Boundary Selection Axis}\label{sec:bridge-right-boundary-selection-axis}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.right_boundary_selection_axis status=coincidence
\textbf{Statement.} The Window6 right-boundary/foldbin escape functional $U_R$ (cyclic endpoint collision $w_1=w_6=1$,
escape ledger |d$U_R$|/(6|$U_R$|)=8/9) maps onto the BIOLOGICAL low-optimality boundary axis --
R=tRNA-poor (BIO-M) and the unexplained universal residual axis $d_{\perp}$ (BIO-P) -- via
sign/rank/projection constraints, NOT via naked cardinalities (|R|=13, 21, 3).

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Window6 $U_R$ is reconstructed from origin/feat/fibonacci\_reality-deepening
window6-green-spectral-response-alpha-readout-frontier.tex and
window6-markov-kernel-coarse-spectrum-fibonacci-leading.tex: X6 boundary $w_1=w_6=1$, Foldbin tail
constraints, and cutoff value<=63 give $U_R$ labels 001110,010001,010011,110000,110011,110101.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc5\_right\_boundary\_selection\_axis.py

\subsection{Fold Residual Wobble Correspondence}\label{sec:bridge-fold-residual-wobble-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fold_residual_wobble_correspondence status=coincidence
\textbf{Statement.} Window6's Fold6 hidden tail-offset residual modes and the standard code's third-position wobble
residual modes are the same kind of hidden residual in a shared 'regular micrograph -> visible
family -> hidden residual' fold-forcing mechanism (low-energy = first/second-position core,
high-energy = third-position/tail residual): they leave visible family/type invariant but govern
fiber multiplicity, lumpability failure, and quotient-kernel subleading spectrum -- tested by
label-free invariants + hidden-mode alignment under graph/wobble-preserving nulls, NOT by the 64->21
count or any 21-label pairing.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} BC6 verdict: STRONG is refuted.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc6\_fold\_residual\_wobble.py

\subsection{Edge Isoperimetric Contravariance}\label{sec:bridge-edge-isoperimetric-contravariance}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.edge_isoperimetric_contravariance status=certified
\textbf{Statement.} Window6 Fold6 and the standard genetic code realize OPPOSITE achievable extremes of one label-free
local-edge-hiding functional e\_in (one-step micrograph edges hidden inside a family): Window6 is
the exact separating endpoint (e\_in=0=e\_min, tau\_W=0; every Q6 1-bit edge changes the stable
type) and the standard code is the near-maximal tolerant endpoint (e\_in=69, e\_max=72,
tau\_C=0.958), surviving graph- and wobble/box-preserving nulls. This is a C-0
contravariant-extremality certificate: it does NOT claim the two are isomorphic folds, that 64->21
is biologically meaningful, that high/low-energy maps to DNA/RNA/translation, or any common
fold-FORCING law.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Fold6 table copied from origin/feat/fibonacci\_reality-deepening forced\_window\_structure;
reconstructed from the finite no-adjacent Zeckendorf representatives on 0..63 as a self-check.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc7\_edge\_isoperimetric\_contravariance.py

\subsection{Parry Measure Usage Correspondence}\label{sec:bridge-parry-measure-usage-correspondence}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.parry_measure_usage_correspondence status=coincidence
\textbf{Statement.} The Window6 Parry measure (golden maximal-entropy stationary measure of the no-adjacent-one transfer
operator) corresponds, per-codon in the Q6 frame, to the codon usage-frequency selection axis
($d_{\perp}$) -- tested by rank/projection vs label-free nulls, not by any label pairing.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Parry measure reconstructed from origin/feat/fibonacci\_reality-deepening
forced\_window\_structure/window6-parry-measure-golden-maximal-entropy.tex and
window6-transfer-operator-golden-perron-spectrum.tex: transfer matrix [[1,1],[1,0]], Perron root
phi, transition kernel [[1/phi,1/phi\textasciicircum{}2],[1,0]], stationary distribution
(phi\textasciicircum{}2/(phi\textasciicircum{}2+1),1/(phi\textasciicircum{}2+1)).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_parry\_usage\_correspondence.py

\subsection{Smith Normal Form Cokernel}\label{sec:bridge-smith-normal-form-cokernel}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.smith_normal_form_cokernel status=refuted
\textbf{Statement.} The Smith-normal-form cokernel (finite abelian group invariant) of the Window6 edge matrix is
isomorphic to that of the canonical codon adjacency/quotient matrix -- a label-free
group-isomorphism test.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} Window6 uses the finite four-cell edge-flux matrix from the Fibonacci branch and has coker torsion
Z/3Z + Z/3450Z.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_smith\_normal\_form\_cokernel.py

\subsection{Three Rigidity Codon Graph}\label{sec:bridge-three-rigidity-codon-graph}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.three_rigidity_codon_graph status=refuted
\textbf{Statement.} The Window6 type-quotient graph and the codon synonymous family-quotient graph are
rigidity-equivalent (share canonical label-free rigidity invariants: automorphism group, degree
sequence, spectrum, distance profile) beyond a degree-preserving null.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The two quotient graphs share 21 nodes, diameter 3, WL-discrete color refinement, and trivial
automorphism group.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_three\_rigidity\_codon\_graph.py

\subsection{Mod3 Flux Frame Obstruction}\label{sec:bridge-mod3-flux-frame-obstruction}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.mod3_flux_frame_obstruction status=coincidence
\textbf{Statement.} The Window6 edge-flux mod-3 obstruction class in H\textasciicircum{}1(G\_W;Z3) is cohomologically of
the same type as the codon reading-frame/f3-ramp three-periodicity phase class, tested by Z3-Hodge
signature (r3,T3,phase,cycle-integrals) up to D3 vs frame/wobble/family nulls.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Both sides have nonzero mod-3 cohomology classes and compatible nonzero cycle integrals, but the
codon harmonic fraction is not extreme against every stricter codon null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc10\_mod3\_flux\_frame\_obstruction.py

\subsection{Partition Quotient Wobble Foldbin}\label{sec:bridge-partition-quotient-wobble-foldbin}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.partition_quotient_wobble_foldbin status=coincidence
\textbf{Statement.} The Window6 (Fold6, Zeckendorf-cylinder) partition pair and the codon (16-box, synonymous-family)
partition pair have the same label-free partition-lattice signature (meet/join sizes, VI, equitable
defect, boundary profile, quotient SNF) beyond size/graph-preserving nulls.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Window6 partitions are reconstructed from origin/feat/fibonacci\_reality-deepening
forced\_window\_structure: Fold6 is the visible six-prefix fiber; Zyl is the finite
Zeckendorf/Foldbin tail-offset cylinder partition on the same 0..63 value carrier.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc11\_partition\_quotient\_wobble\_foldbin.py

\subsection{Green Spectral Codon Q Spectra}\label{sec:bridge-green-spectral-codon-q-spectra}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.green_spectral_codon_q_spectra status=refuted
\textbf{Statement.} The Window6 Green/resolvent spectrum has the same label-free spectral signature (density, gap,
moments) as the codon q1/q3/q4 codon-cube quotient spectra beyond a degree-preserving null.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The Window Green/resolvent object is a four-state reversible Markov spectrum, while the codon
q1/q3/q4 coface-affinity spectra are 21-family quotient spectra.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc13\_green\_spectral\_codon\_q\_spectra.py

\subsection{Edge Exhaustion Meta Certificate}\label{sec:bridge-edge-exhaustion-meta-certificate}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.edge_exhaustion_meta_certificate status=certified
\textbf{Statement.} Within the pre-registered label-free invariant battery (tau plus
spectral/Parry/SNF/mod3/partition/rigidity), the ONLY certified Window6<->codon cross-domain
relation is the contravariant local-edge-hiding extremality (tau\_W=0 separating endpoint,
tau\_C=0.958 tolerant endpoint); conditioned on this e\_in extremality and native graph/fiber
constraints, all remaining invariants have residual alignment indistinguishable from a conditional
null (operational independence). The realization mechanisms are polarized (Window6 isotropic
separator, codon anisotropic third-position tolerator). NOT an isomorphism, NOT a forcing, NOT
strict C-1 profile duality.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} This is not a structural isomorphism, not a 64-to-21 numerology claim, and not a biological-origin
claim.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc\_meta\_edge\_exhaustion\_certificate.py

\subsection{Fourcell Selection Enrichment}\label{sec:bridge-fourcell-selection-enrichment}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fourcell_selection_enrichment status=refuted
\textbf{Statement.} The Window6 4-cell coarse partition (sizes 27/22/9/6) explains significant non-random variance in
the real 4D codon selection vectors (tRNA/f3/$d_{\perp}$/optimal) beyond cell-size-preserving and
GC-conservative nulls.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The four-cell partition does not explain more 4D selection variance than strict same-size random
partitions.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bcd\_fourcell\_selection\_enrichment.py

\subsection{Fold Structure 64 16 25 21}\label{sec:bridge-fold-structure-64-16-25-21}
% CLAIM_DETAIL claim_id=bridge.genetic_code.fold_structure_64_16_25_21 status=certified
\textbf{Statement.} The standard genetic code is an exact two-layer 64->16->25->21 fold (16 doublet boxes = 8 pure + 7
binary-split + 1 tri-split; 25 box-output fragments minus 4 cross-box merges = 21 classes;
degeneracy spectrum
1\textasciicircum{}2,2\textasciicircum{}9,3\textasciicircum{}2,4\textasciicircum{}5,6\textasciicircum{}3)
-- the hard biological instance of the abstract microstate->block->seam->stable fold pattern. NOT a
golden-ratio forcing; Zeckendorf decompositions universal/presentational; ratios are
ratio-of-counts.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} The standard genetic code is certified here as a 64->21 two-layer fold with bulk(8 pure box)/seam(7
split)/defect(1 tri-split) stratification and exact degeneracy spectrum; Zeckendorf and small-ratio
observations are explicitly flagged non-forcing.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_genetic\_code\_fold\_cert.py

\subsection{Neutral Hamming Adjacency}\label{sec:bridge-neutral-hamming-adjacency}
% CLAIM_DETAIL claim_id=bridge.genetic_code.neutral_hamming_adjacency status=certified
\textbf{Statement.} The standard code's per-codon Hamming-1 neutral-neighbor histogram is exactly 0:2,1:22,2:8,3:28,4:4
(merged defect = 28/22/8/6), close to but not equal to the Window6 27/22/9/6; no natural untuned
biological weighting bridges them.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} No fixed natural weighting tested here turns the exact 28/22/8/6 neutral-adjacency profile into
27/22/9/6.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_neutral\_adjacency\_cert.py

\subsection{Fold Order Parameter Usage}\label{sec:bridge-fold-order-parameter-usage}
% CLAIM_DETAIL claim_id=bridge.genetic_code.fold_order_parameter_usage status=coincidence
\textbf{Statement.} The fold order parameter m\_bio = P\_bulk - P\_seam computed from real multi-organism codon usage
carries an independent (non-GC) cross-organism signal.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} m\_bio is dominated by GC3 and shows no GC-controlled residual signal; this is a GC proxy
coincidence rather than an independent fold signal.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_codon\_usage\_projection\_cert.py

\subsection{Neutral Trna Edge Coupling}\label{sec:bridge-neutral-trna-edge-coupling}
% CLAIM_DETAIL claim_id=bridge.genetic_code.neutral_trna_edge_coupling status=coincidence
\textbf{Statement.} The certified Window6 edge score modulates the tRNA supply-demand alignment on synonymous Hamming-1
edges (usage-edge-gradient \textasciitilde{} tRNA-edge-gradient x Window6-edge-score, interaction
beta\_bridge), beyond GC/f3 and edge-permutation nulls, with held-out-species predictive lift.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} AUTHORITATIVE per-species GtRNAdb test (degeneracy fixed, TH\_norm=55.5): beta\_T=0.73 real but
beta\_bridge=0.031 fails CV + all 3 nulls.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_claim20\_per\_species\_trna\_edge\_coupling.py

\subsection{Twolayer Fold Edge Hiding Extremality}\label{sec:bridge-twolayer-fold-edge-hiding-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.twolayer_fold_edge_hiding_extremality status=certified
\textbf{Statement.} The contravariant edge-hiding extremality iterates across the genetic code's two-layer fold: the
first-two-base box partition (64->16) is the EXACT maximum-edge-hiding partition for its size-4x16
block profile (tau\_box=1.0), and the family layer (64->21) is near-maximal (tau\_family=0.958),
both in the extreme upper tail of a same-block-size null; Window6 Fold6 sits at the opposite extreme
(tau=0). Restricted to iterated edge-hiding extremality; NOT isomorphism or biological forcing.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Statement is restricted to iteration of two edge-hiding extremes: the first-two-base box layer
attains the exact size-4x16 maximum, and the family layer is within three edges of the exact
family-profile maximum while lying in the extreme upper tail of the same-size null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_twolayer\_fold\_extremality.py

\subsection{Foldlayer Zigzag Extremality}\label{sec:bridge-foldlayer-zigzag-extremality}
% CLAIM_DETAIL claim_id=bridge.genetic_code.foldlayer_zigzag_extremality status=certified
\textbf{Statement.} The standard genetic code realizes a zig-zag edge-hiding fold layer B <- M=B meet F -> F: the
first-two-base box layer and box-family fragment layer are exact edge-hiding endpoints, while the
family layer is near-maximal with a small deficit. This is a finite label-free extremality
certificate, not a monotone quotient tower, isomorphism, biological forcing, or live-usage claim.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Certified zig-zag extremality, not a monotone quotient tower: 64->16 box and 64->25 box-family
fragments are exact edge-hiding extrema; the 21-family layer is near-max with deficit 3.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc15\_foldlayer\_zigzag\_extremality.py

\subsection{Halfbox Fibonacci Tower}\label{sec:bridge-halfbox-fibonacci-tower}
% CLAIM_DETAIL claim_id=bridge.genetic_code.halfbox_fibonacci_tower status=coincidence
\textbf{Statement.} The genetic-code halfbox and family-meet layers create exact edge-hiding endpoint facts and a strict
Fibonacci tower across the naturally generated block counts 13, 21, 34, and 55 under canonical
finite partition operations. The claim must separate hard extremality facts from numerical tower
appearances.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} BC16 exact halfbox extremality is a hard endpoint fact; BC17 is not a certified Fibonacci tower
because only 21 and 34 appear naturally under the preregistered canonical operations.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_bc1617\_halfbox\_fibonacci\_tower.py

\subsection{Fold Tower Separating Extremality}\label{sec:bridge-fold-tower-separating-extremality}
% CLAIM_DETAIL claim_id=bridge.window6.fold_tower_separating_extremality status=refuted
\textbf{Statement.} The Window fold tower is edge-separating at each tested finite width under the same value-window
convention that reproduces the Window6 certificate, yielding a cross-width revealing endpoint dual
to the genetic-code hiding tower.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The tower-wide separating claim fails under the finite value-window convention that reproduces the
required Window6 histogram: widths 4,5 have e\_in>0.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_window\_fold\_tower\_extremality.py

\subsection{Edge Defect Axis Cert}\label{sec:bridge-edge-defect-axis-cert}
% CLAIM_DETAIL claim_id=bridge.genetic_code.edge_defect_axis_cert status=coincidence
\textbf{Statement.} The standard genetic code's local-edge-hiding deficit is exactly e\_max-e\_in=3, supported only on
Stop and Ser, with Stop contributing one punctuation defect and Ser contributing two sense split
defects. The sense-side SerSplitDefect vector should project onto an available codon-Q6 selection
axis, especially an independent fourth residual axis if present.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} The Stop/Ser edge-defect decomposition is exact and graph-theoretically sharp, but the currently
delivered selection-vector packet has no independent d\_resid4 axis and the Ser split defect does
not certify a projection onto the available axes.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_edge\_defect\_axis\_cert.py

\subsection{Edge Defect Projection Harness}\label{sec:bridge-edge-defect-projection-harness}
% CLAIM_DETAIL claim_id=bridge.genetic_code.edge_defect_projection_harness status=certified
\textbf{Statement.} The SerSplit edge-defect projection audit has a deterministic 61-sense-codon harness: it builds
forbidden controls from available codon-Q6 selection and codon-chemistry columns, residualizes
candidate axes and the SerSplit contrast through the same matrix, detects only the forbidden
positive control, and does not promote oracle output or local controls into biological evidence.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Projection harness is operational: forbidden negative controls collapse or do not pass, and the
forbidden SerSplit positive control is detected.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_edge\_defect\_projection\_harness.py

\subsection{Ser Island Fidelity Cost}\label{sec:bridge-ser-island-fidelity-cost}
% CLAIM_DETAIL claim_id=bridge.genetic_code.ser_island_fidelity_cost status=coincidence
\textbf{Statement.} The Ser UCN/AGY split creates a code-table near-cognate error-cost asymmetry: UCN and AGY have
different one-step missense/stop neighborhoods, and this asymmetry could seed a translation-fidelity
selection route only if it is exceptional beyond matched codon-family nulls and later survives
site-level expression/mistranslation tests.

\textbf{Verdict.} \texttt{coincidence}.

\textbf{Why.} Ser UCN has higher mean visible near-cognate cost than AGY in the deterministic amino-acid-property
audit, but the delta is not exceptional under matched random codon partitions and is comparable to
other sixfold-family four-versus-two splits; this remains a mechanistic candidate rather than a
certificate.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_ser\_island\_fidelity\_cost.py

\subsection{Pack}\label{sec:bridge-pack}
% CLAIM_DETAIL claim_id=bridge.genetic_code.synthetic.negative.control.axis.pack status=needs_derivation
\textbf{Statement.} Use only in-repo deterministic codon features and seeded synthetic vectors to audit the common
projection harness before any external candidate axis is attempted. This autonomous bridge claim is
evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{needs\_derivation}.

\textbf{Why.} the forbidden-control matrix makes the SerSplit contrast non-testable, so this negative-control pack
cannot audit projection selectivity.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_synthetic\_negative\_control\_axis\_pack.py

\subsection{Certificate}\label{sec:bridge-certificate}
% CLAIM_DETAIL claim_id=bridge.genetic_code.sersplit.edge_defect.nonestimability.certificate status=certified
\textbf{Statement.} The Ser-split edge-defect contrast (within-Ser 4-vs-2: \{UCU,UCC,UCA,UCG\} vs \{AGU,AGC\}) lies
exactly in the column span of the registered forbidden-control matrix C (verified against the actual
registered z-scored columns to 1e-9), so its residual is algebraically zero, v\_SerSplit is
undefined, and no real-data biological axis can yield a certified independent projection onto this
target. On the Ser support, sqrt(12)*u = 1 - 3*indicator(first base is purine), so the edge defect
IS a registered composition control. The 5-dim within-Ser zero-sum contrast subspace is NOT fully
absorbed, so the 15-way within-Ser permutation null is non-degenerate. This autonomous bridge claim
is evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Exact rational elimination proves sqrt(12) times the Ser-split edge-defect contrast lies in the span
of registered forbidden-control columns.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_sersplit\_edge\_defect\_nonestimability\_certificate.py

\subsection{Certificate}\label{sec:bridge-certificate}
% CLAIM_DETAIL claim_id=bridge.genetic_code.foldboundary.residual.exhaustion.certificate status=certified
\textbf{Statement.} The pre-registered, q6-label-free fold-boundary contrast subspaces over the 61 sense codons -- box
split-type (pure/split/tri-split coarsening of the 16 first-two-position boxes), degeneracy-class
(amino-acid degeneracy sizes), and fold-layer (64->16->25->21) -- are exactly absorbed (residual
rank 0) by the registered forbidden-control matrix C, so these bio-intrinsic fold boundaries provide
no independent residual axis beyond the registered composition controls (a bounding exhaustion
extension of the edge-exhaustion meta-certificate). The Window6 four-cell partition mapped via
q6\_label is correspondence-derived, not bio-intrinsic; because C contains no q6 information its
residual survival (rank 3) is expected and is barred from the certificate, not treated as
independent forcing. NOT certified here: the full 16-box partition's fine structure -- its only
possible escape directions are the Leu 4/2 and Arg 4/2 within-amino-acid branch splits (analogous to
the certified Ser 4/2), which are the next frontier. This autonomous bridge claim is evaluated only
by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} The bio-intrinsic fold-boundary contrast subspaces box\_split\_type, degeneracy\_class, fold\_layer
are q6-label-free and fully absorbed by the registered composition control frame, so they provide no
independent residual axis beyond those controls.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_foldboundary\_residual\_exhaustion\_certificate.py

\subsection{Certificate}\label{sec:bridge-certificate}
% CLAIM_DETAIL claim_id=bridge.genetic_code.box_partition.composition_collapse.certificate status=certified
\textbf{Statement.} Each of the three 6-fold-degenerate amino acids has a 4-vs-2 within-amino-acid branch split (Ser
UCN|AGY, Leu CUN|UUR, Arg CGN|AGR), and exact rational elimination proves ALL THREE lie in the span
of the registered forbidden-control matrix C (residual 0), with on-support identities sqrt(12)*u =
1-3*indicator(first base purine) for Ser, 1-3*indicator(first two bases equal) for Leu, and
-2+3*indicator(first base GC)+... for Arg. The full mean-zero 16-box first-two-position indicator
subspace (dim 15) has exact residual rank 0 against C (exact\_control\_rank 29 = rank with box
partition appended), so the entire 64->16 box partition is absorbed by the registered composition
control frame -- it is composition, not an independent forcing direction. Generalizes the certified
Ser-split non-estimability to all three 6-fold amino acids and completes the box-partition thread of
the fold-boundary exhaustion. Proof tied to the actual registered columns (registered-equivalence,
fail-closed on drift), branch contrasts label-free (no q6/Stop). Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Exact rational elimination proves the Ser, Leu, and Arg 4-vs-2 within-amino-acid branch splits all
lie in the span of the registered composition control frame.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_box\_partition\_composition\_collapse\_certificate.py

\subsection{Metacertificate}\label{sec:bridge-metacertificate}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.forcing_direction_closure.metacertificate status=certified
\textbf{Statement.} Consolidation meta-certificate for the math->bio forcing direction. Exact rational rank proves the
FULL pre-registered label-free fold-boundary battery (box partition + degeneracy classes + fold
layers + the three 6-fold amino-acid 4/2 branch splits Ser/Leu/Arg; 44 columns, span rank 22) lies
AS ONE UNION exactly in span(C): exact control rank 29 is unchanged when the battery is appended
(absorbed). The only fold-boundary direction outside span(C) is the q6\_label-derived Window6
four-cell partition, which is correspondence-derived and barred as circular (q6 is the
correspondence under test). Therefore the bridge's certified scope is: (1) POSITIVE - the sole
certified structural signal is the BC7 contravariant edge-hiding extremality plus the
edge-exhaustion meta-certificate (Window6/Fold6 and the standard code occupy opposite endpoints of
one label-free local-edge visibility functional); (2) BOUNDING-NEGATIVE - the label-free bio
fold-boundary structure is composition (= span(C)), leaving no independent bio-side residual axis
for Window6 to force; (3) the math->bio FORCING direction is closed-as-not-supported under the
current frame, with count/spectrum matches classified coincidence and q6-mediated forcing barred as
circular. REOPENING CONDITION (falsifiable): a label-free genetic-code invariant outside span(C),
defined without q6 or family counts, whose preservation forced-Window6/Fibonacci/Zeckendorf
structure necessitates more strongly than a pre-registered composition-controlled null. Evaluated
only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{certified}.

\textbf{Why.} Exact Fraction rank elimination certifies the full label-free fold-boundary battery, taken as one
union, lies in span(C\_nonselection).

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_forcing\_direction\_closure\_metacertificate.py

\subsection{Complement Stresstest}\label{sec:bridge-complement-stresstest}
% CLAIM_DETAIL claim_id=bridge.genetic_code.h3_higherorder.complement_stresstest status=refuted
\textbf{Statement.} Stress-test claim: the composition-exhaustion closure extends to the canonical higher-order class H3
(the 27 three-position nucleotide-chemistry interaction tensors
chi\_a(base1)*chi\_b(base2)*chi\_c(base3), chi in \{R/Y, W/S, K/M\}; genuinely higher-order,
label-free, non-q6). VERDICT REFUTED: exact Fraction rank shows H3 has residual rank 26 of 27 after
the registered composition control frame C (exact control rank 29; rank with H3 appended 55), so
higher-order three-position chemistry structure lies almost entirely OUTSIDE span(C). This precisely
DELIMITS the composition-exhaustion: not all label-free codon structure collapses to composition --
the additive/pairwise+fold structure does, but higher-order three-way chemistry interactions do not.
CRITICAL: this is a delimitation, NOT a math->bio forcing reopening -- there is no
Window6-necessitation test, no pre-registered necessitation null, and no principle selecting H3
among higher-order audit languages (Walsh/Fourier, codon-graph spectral, automorphism-orbit); H3 is
recorded only as a candidate class for future forcing work requiring a frozen necessitation null.
The forcing-direction closure stands. Evaluated only by its registered deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} Exact Fraction rank elimination shows H3 has nonzero residual rank after the actual registered
non-selection control columns.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_h3\_higherorder\_complement\_stresstest.py

\subsection{Conventionrobust Test}\label{sec:bridge-conventionrobust-test}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fibcube_embedding.conventionrobust_test status=needs_derivation
\textbf{Statement.} Reopening attempt for the math->bio forcing direction via a NON-q6, first-principles embedding.
Test: is the Fibonacci-cube vertex set S = \{21 no-adjacent-1 six-bit words\} (the canonical
Gamma\_6 isometric inclusion into the codon 6-cube Q6) a CONVENTION-ROBUST code-structure outlier
when mapped into codon space across ALL 17280 admissible base->2bit x bit/position conventions (q6
being just one marked convention, class size 2), vs a composition-matched null on pre-declared
label-free statistics (synonymous-edge / family-boundary / fold-cell / conservative-substitution),
Holm-Bonferroni corrected? VERDICT needs\_derivation: NO forcing signal -- the q6 class is NOT an
outlier (q6\_class\_outlier False) and only 4.7\% of non-q6 conventions are outliers (far below the
0.5 robustness threshold, \textasciitilde{}noise). So the canonical Gamma\_6->Q6 isometric-inclusion
route produces neither a convention-robust non-q6 forcing nor a q6-special alignment: this
most-natural non-circular embedding route is EXHAUSTED on the pre-declared battery. This is NOT an
exhaustive impossibility proof (other label-free invariants / non-inclusion constructions untested),
hence needs\_derivation not refuted. It does not touch BC7 (which concerns the Fold6 PARTITION edge
functional, a different object from the Gamma\_6 vertex set). Evaluated only by its registered
deterministic experiment (seed 619423).

\textbf{Verdict.} \texttt{needs\_derivation}.

\textbf{Why.} the pre-declared statistics do not show a q6-only obstruction or a convention-robust non-q6 signal
under the deterministic matched null.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_fibcube\_embedding\_conventionrobust\_test.py

\subsection{Fibcube Codonfamily Graphiso Test}\label{sec:bridge-fibcube-codonfamily-graphiso-test}
% CLAIM_DETAIL claim_id=bridge.window6_codon_q6.fibcube_codonfamily_graphiso_test status=refuted
\textbf{Statement.} Convention-free reopening route (distinct from the inclusion route): is the Fibonacci cube Gamma\_6
(graph on the 21 no-adjacent-1 six-bit words, Hamming-1 edges; 21 vertices, 38 edges) ISOMORPHIC as
an abstract graph to the codon family-adjacency graph (families adjacent iff some codon pair is
Hamming-1)? A graph iso is intrinsic (no codon-coordinate convention), so it sidesteps the inclusion
route's convention problem. VERDICT REFUTED: they fail necessary isomorphism invariants -- Gamma\_6
has 38 edges vs 85 for the 21-family graph (incl Stop) and the excl-Stop variant has 20 vertices/75
edges; degree sequences also differ (Gamma\_6 max degree 6 vs family-graph degrees 6-13). Robust
across family-graph definitions. So the convention-free graph-isomorphism reopening route is CLOSED:
the 21<->21 cardinality match is a graph-level COINCIDENCE, not structural forcing. Together with
the inclusion-route exhaustion (fibcube\_embedding\_conventionrobust\_test), both natural non-q6
Window6->codon embedding constructions are now closed. Evaluated only by its registered
deterministic experiment.

\textbf{Verdict.} \texttt{refuted}.

\textbf{Why.} The convention-free graph-isomorphism route is closed: Gamma\_6 and the codon-family adjacency graph
fail necessary abstract-graph isomorphism invariants.

\textbf{Derivation script.} tools/window\_codon\_bridge/experiments/run\_fibcube\_codonfamily\_graphiso\_test.py
