\section{Biological Inputs Returned to the Bridge}
The bridge ledger records biological reality contact as incoming constraints on the
mathematical certificate search.  The entries below are generated from ledger rows with
\texttt{bio->bridge} direction.

\subsection{BIO-K-OPT}
\textbf{Status.} \texttt{proposed}.

Bio finding (Route K): the abundance-optimal synonymous-codon direction across 15 organisms is
CLUSTERED (mean pairwise cosine 0.26), organized mainly by genomic GC3 (GC3-delta vs cosine Pearson
-0.36), with a weak universal core (mean held-out R2 0.088); yeast optimizes hardest but
idiosyncratically.

Bridge target: does the Window6 structure predict (a) why optimality is GC3-organized, (b) why a
weak universal core exists, (c) the universal-core direction itself? Feeds BC2 (spectrum) since the
optimality manifold may relate to the kernel's eigenvectors.

\textbf{Linked biological experiment.} b\_star\_q6\_optimal\_codon\_conservation\_powered (Route K, landed).

\subsection{BIO-F3-DRIVER}
\textbf{Status.} \texttt{proposed}.

Bio finding (Routes I/J): f3\_stress->abundance direction is near-universal (14/15 organisms
positive) but magnitude is yeast-dominant; f3 effect = translational-selection strength x
directional alignment (Spearman 0.71). f3 is a 1-D proxy for the organism's translational selection.

Bridge relevance: f3\_stress is a fixed direction in the same Q6 synonymous space the bridge lives
in. If BC2/BC3 give the math-forced optimal direction, compare it to f3 and to each organism's
empirical optimal direction.

\textbf{Linked biological experiment.} b\_star\_q6\_f3\_stress\_strength\_driver\_powered (Route J, landed).

\subsection{BIO-M-RTRNA}
\textbf{Status.} \texttt{empirical\_supported}.

Route M (bio): the Window6 boundary set R = the tRNA-POOR codons (R sense-codons have lower dos-Reis
tAI weight than non-R in 31/40 organisms, sign-test p\textasciitilde{}7e-4).

Deepens BC1 empirical prong: R is not just avoided (Route L) but mechanistically = translationally
non-optimal / tRNA-scarce codons. For the BC1 MATH forcing map, this suggests the right biological
semantics of the Window6 boundary = the tRNA-poverty / non-optimal stratum, not an arbitrary codon
set. Bridge could test whether the Window6 forced boundary maps to the tRNA-poverty stratum
structurally.

\textbf{Linked biological experiment.} b\_star\_q6\_boundary\_set\_trna\_poverty\_powered (bio claims=74).

\subsection{BIO-N-OPT-TRNA}
\textbf{Status.} \texttt{empirical\_supported}.

Route N (bio): the cross-organism UNIVERSAL optimal synonymous-codon direction (Route K's weak
universal core) IS substantially the tRNA-adaptation direction. Per-organism optimal-dot-tRNA mean
cos 0.267 (10/11 organisms positive, sign-test p\textasciitilde{}6e-3), and the tRNA direction ALONE
recovers \textasciitilde{}43\% (pooled held-out R2 ratio) of the abundance signal the full optimal
direction captures. So tRNA-supply selection is a MAJOR-but-PARTIAL (\textasciitilde{}43\%)
component of universal codon optimality. CONNECTION to BC1: R being tRNA-POOR (Route M) is not an
isolated 13-set coincidence -- R sits at the LOW END of a tRNA-selection gradient that pervades the
WHOLE genetic code (\textasciitilde{}43\% of optimality). A structure-respecting BC1 map should
target this tRNA-poverty STRATUM as a continuous selection axis, not an arbitrary 13-set. The
remaining \textasciitilde{}57\% non-tRNA optimality is being decomposed now (Route O: GC mutation
bias vs f3 selection axis).

\textbf{Linked biological experiment.} b\_star\_q6\_universal\_core\_is\_trna\_adaptation\_powered.

\subsection{BIO-O-RESIDUAL-F3}
\textbf{Status.} \texttt{empirical\_supported}.

Route O (bio): the NON-tRNA component of universal codon optimality is the f3-stress
TRANSLATIONAL-SELECTION axis, NOT GC mutational bias. After Gram-Schmidt-removing tRNA-adaptation
(Route N) from each organism's abundance-optimal direction, the residual aligns with f3-stress (mean
cos 0.139, 10/11 organisms, sign-test p\textasciitilde{}6e-3) and ANTI-aligns with GC3 (mean cos
-0.073); GC3 and f3 are near-antiparallel (cos -0.905), so optimality picks the SELECTION pole. So
B*\_Q6 codon optimality = tRNA-supply adaptation (\textasciitilde{}43\%, Route N) + f3-ramp
translational selection (residual), BOTH selection, with GC mutation as the opposite pole.
CONNECTION for BC3 (foldbin tail-cube <-> codon 3 positions/wobble): the dominant bio
second-order/residual codon structure is the f3-stress axis (first-vs-last synonymous codon,
Gly/Ile-led) -- a foldbin->codon-position map should be tested against f3-stress, NOT GC3. For BC1:
R=tRNA-poor lives on the tRNA axis; f3 is a SEPARATE selection dimension, so the code-layer
'reality' is two-axis translational-selection-shaped.

\textbf{Linked biological experiment.} b\_star\_q6\_universal\_optimal\_residual\_axis\_powered.

\subsection{BIO-P-THIRD-AXIS}
\textbf{Status.} \texttt{empirical\_supported}.

Route P (bio): codon optimality is NOT 2-dimensional. tRNA(Route N)+f3(Route O) span only
\textasciitilde{}13\% of the optimal direction's norm\textasciicircum{}2; a THIRD universal
selection axis $d_{\perp}$ exists -- cross-organism aligned (55-pair mean cos 0.179,
p\textasciitilde{}3e-3) and predictively strong (recovers \textasciitilde{}64\% of self R2,
out-predicts tRNA+f3 in 8/11 organisms). Identity of $d_{\perp}$ unknown = bio's open frontier.
ANTI-NUMEROLOGY WARNING for the bridge: do NOT match 'codon optimality has >=3 universal axes' to
the Foldbin tail-cube's '3 free coordinates' -- that is a 3=3 coincidence with no structural forcing
(the optimality axes are statistical selection directions in a 41-dim contrast space; the foldbin
coords are graded Fibonacci tail aliases 21/34/55). A real BC3 map must respect the foldbin GRADING
(21<34<55) and the codon POSITIONAL degeneracy gradient, not just the count 3. bio offers to TEST
any concrete foldbin->codon assignment the bridge proposes.

\textbf{Linked biological experiment.} b\_star\_q6\_universal\_optimal\_two\_axis\_closure\_powered.

\subsection{BIO-Q-THIRD-AXIS-ID}
\textbf{Status.} \texttt{empirical\_supported}.

Route Q (bio): the third universal codon-optimality axis $d_{\perp}$ (BIO-P) is provisionally
identified as the within-synonymous-family USAGE-FREQUENCY axis (preferred-codon bias) -- mean cos
0.128, 11/11 organisms, sign-test p\textasciitilde{}4.9e-4, the ONLY candidate axis clearing the
cross-organism gate (position composition, GC3, the other 8 B*\_Q6 coords all weak/ns). So codon
optimality = THREE universal axes: tRNA-supply (N) + f3-ramp selection (O) + preferred-codon
usage-frequency (Q). Caveat: cos modest 0.128; usage-freq correlates with expression (partial). For
BC5: the three biological selection axes are now named; the boundary set R / the right-boundary
functional should be tested against these (tRNA-poor = low tRNA axis; and whether $U_R$ aligns with
low usage-frequency / low $d_{\perp}$).

\textbf{Linked biological experiment.} b\_star\_q6\_third\_axis\_identity\_powered.

\subsection{BC5-DATA-DELIVERED}
\textbf{Status.} \texttt{data\_delivered}.

BC5-DATAREQ fulfilled. Per-codon selection vectors in the codon-Q6 6-bit frame exported to
papers/window\_codon\_bridge/data/codon\_q6\_selection\_vectors.json (schema
codon\_q6\_selection\_vectors; 64 codons; uses codon\_topology\_refs.codon\_to\_q6 base\_to\_bits
labeling so it aligns with the bridge codon-Q6 frame). Each codon entry has q6\_bits/q6\_label,
amino\_acid, is\_sense, $d_{\perp}$\_loading (consensus third-axis = usage-frequency direction),
trna\_supply\_tai\_mean (tRNA-supply axis), plus optimal loading. consensus\_summaries holds
$d_{\perp}$/optimal/trna consensus directions. Use these as the per-codon biological selection
vectors to test BC5 (Window6 right-boundary $U_R$ vs tRNA-poor + $d_{\perp}$), via
sign/rank/projection -- NOT naked cardinalities.

papers/window\_codon\_bridge/data/codon\_q6\_selection\_vectors.json

\textbf{Reference.} papers/window\_codon\_bridge/data/codon\_q6\_selection\_vectors.json.

\subsection{BIO-R-THIRD-AXIS-SELECTION}
\textbf{Status.} \texttt{empirical\_supported}.

Route R (bio): the third universal codon-optimality axis $d_{\perp}$ (delivered in BC5 vectors) is a
GENUINE cross-organism SELECTION signal -- NOT GC/mutational (survives GC3 orthogonalization
unchanged: aligned cos 0.177 p\textasciitilde{}3e-3, held-out R2 0.071 11/11 p\textasciitilde{}5e-4;
GC3 alone R2 0.028) and NOT the wobble-decoding axis (loadings don't cluster by wobble class).
Leading identity = CSC/mRNA-stability codon optimality (needs external data). So the BC5 per-codon
$d_{\perp}$\_loading vector is a clean SELECTION axis the bridge can safely test the Window6
right-boundary $U_R$ against (it is not a GC artifact). The three bio selection axes are now:
tRNA-supply (trna\_supply\_tai in the export), f3-ramp, and $d_{\perp}$ ($d_{\perp}$\_loading in the
export).

\textbf{Linked biological experiment.} b\_star\_q6\_third\_axis\_mechanism\_powered.

\subsection{BIO-S-FOURTH-MECHANISM}
\textbf{Status.} \texttt{empirical\_supported}.

Route S (bio, capstone): the three NAMED codon-optimality axes (tRNA-supply, f3-ramp,
usage-frequency) are NOT sufficient. Using independent-of-d\_opt axis definitions
(anti-circularity), they jointly span only \textasciitilde{}16\% of the abundance-optimal
direction's norm and recover \textasciitilde{}45\% of its R2; a FOURTH universal mechanism remains
-- residual d\_resid4 is cross-organism aligned (55-pair mean cos 0.172, 38/55,
p\textasciitilde{}3e-3) and carries \textasciitilde{}56\% of self R2, still UNIDENTIFIED (most
plausibly the CSC/mRNA-stability axis oracle ranked \#1, which needs external half-life/CSC data not
in repo). IMPLICATION for the bridge: the codon-Q6 biological selection geometry is at least
4-dimensional, and the 3 axes delivered in the BC5 vector file (tRNA + $d_{\perp}$/usage) are a
MINORITY of it -- so a BC5 forcing map should be tested against the named axes but NOT assume they
are the complete biological selection structure; the dominant universal component is still
unidentified. (Self-correction note: an earlier residual-basis design tautologically said '3 axes
sufficient'; corrected with independent axes.)

\textbf{Linked biological experiment.} b\_star\_q6\_optimality\_axis\_sufficiency\_powered.

\subsection{BIO-U-FOURTH-NOT-STABILITY}
\textbf{Status.} \texttt{empirical\_supported}.

Route U (bio): the dominant unidentified 4th codon-optimality axis is NOT mRNA-stability/CSC
(refutes oracle \#1). In-repo per-gene mRNA-half-life data: half-life-optimal direction aligns with
d\_resid4 only cos 0.098. POSITIVE narrowing: the 4th axis IS shared by abundance/TE/mRNA-level
optimal directions (TE.d\_resid4=0.616, mRNA-level.d\_resid4=0.544) but not half-life -- so it's an
EXPRESSION/TRANSLATION-level optimality, not mRNA decay. For the bridge BC5: the biological
selection geometry's dominant axis is an expression/translation-level pressure (still unidentified
specifically), shared across abundance/TE/mRNA-level; the named axes (tRNA/f3/usage) are
geometrically correlated (f3-usage 0.71) so treat them as an overlapping selection bundle, not
orthogonal coordinates.

\textbf{Linked biological experiment.} b\_star\_q6\_fourth\_mechanism\_mrna\_stability\_powered.
