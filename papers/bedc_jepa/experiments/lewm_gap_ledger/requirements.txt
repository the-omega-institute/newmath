# LeWM gap-ledger experiments — dependencies.
# torch was used as the CUDA 12.4 build (torch==2.6.0+cu124) on an 8 GB
# RTX 4060 Laptop GPU; the CPU build of the same version reproduces the
# anchored numbers with torch.set_num_threads(4).
torch==2.6.0
numpy
h5py
hdf5plugin
einops
huggingface_hub
transformers
zstandard
scikit-learn
