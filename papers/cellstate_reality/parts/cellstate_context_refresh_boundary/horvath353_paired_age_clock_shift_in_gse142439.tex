\subsection{Horvath353 paired age-clock shift in GSE142439}
\label{sec:age-clock-shift-gse142439-horvath353}

\paragraph{Record.}
The object $\AgeClockShiftUp$ is recorded here as a scoped age-signature claim, not as a claim about cell identity, tissue function, organismal repair, or durable rejuvenation. The finite carrier is
$$
\begin{aligned}
B_A=(M,W,P,S_A,\Delta_A,L,N),
\end{aligned}
$$
where $M$ is the paired methylation beta matrix restricted to covered Horvath353 CpGs, $W$ is the Horvath353 coefficient table together with its intercept, $P$ is the list of normal--treated person-level pairs, $S_A$ is the DNAm-age readback, $\Delta_A$ is treated minus normal paired DNAm age, $L$ marks the age-signature layer, and $N$ is the local prohibition against promoting the clock readout into identity, function, safety, rejuvenation, renewable maintenance, or immortality claims. The verified run identified as $\mathrm{d79addaacf6d}$ assigns the claim $\mathrm{cellstate.age\mbox{-}clock\mbox{-}shift.gse142439}$ to contact $\mathrm{k\mbox{-}a.gse142439.horvath353}$ and returns $\mathrm{Support\_A}$ with support marked true. Thus the row is ready only in the narrow sense that the paired methylation-clock computation supports an age-signature direction under the stated coverage and pairing constraints.

\paragraph{Finite contact.}
The methylation-array contact contains $16$ samples organized as $8$ normal--treated pairs. The scoring table covers $334$ of the $353$ Horvath clock CpGs on the GPL21145 EPIC platform. This clears the fixed coverage rule used by the probe, namely at least $330$ covered CpGs and at least $0.93$ proportional coverage, since
$$
\begin{aligned}
334 &\geq 330,\\
334/353 &\approx 0.946 > 0.93.
\end{aligned}
$$
The available rows therefore support evaluating each paired beta vector against the Horvath353 coefficient list and intercept. The contact resolution is CpG-level beta values for the covered markers, coefficient weights, the intercept, sample annotations, and the person-level normal--treated pairing. Its finite scope is correspondingly exact: it measures a methylation-array age-signature readout and does not measure identity markers, functional assays, safety assays, longitudinal renewal, organismal maintenance, or any immortality-relevant phenotype.

\paragraph{Readback.}
For each sample $x$ in the paired matrix, the readback first restricts the beta vector to the $334$ covered Horvath coordinates, then applies the matching entries of $W$ and the intercept to obtain a clock sum, and then transforms that sum into the reported DNAm age scale. For each person-level pair $p\in P$, the paired contrast is
$$
\begin{aligned}
\Delta_A(p)=S_A(\mathrm{treated}_p)-S_A(\mathrm{normal}_p).
\end{aligned}
$$
The support condition is directional and paired: the frozen eight-pair split must be present, the mean of $\Delta_A$ must be negative, and the paired sign or paired $t$ criterion must support the treated-lower-than-normal direction. The verified fact reports the result as $\mathrm{Support\_A}$ for the experiment $\mathrm{gse142439\_horvath353\_age\_clock\_shift}$, so the accepted conclusion is that the curated GSE142439/Horvath353 contact supports a local DNAm-age decrease in the treated samples relative to their matched normal samples.

\paragraph{Boundary.}
The same rows that make the age-signature readback testable also make the non-promotion boundary sharp. A positive age-clock shift is not an identity-preserving age reset, not a rejuvenation certificate, not a partial-reprogramming certificate, not a renewable-maintenance certificate, and not evidence for immortality potential. No translation, folding, physical admissibility, biological function, molecular mechanism, or universality follows from this methylation-array contact unless a separate layer-matched contact supplies that information. In particular, the carrier coordinates and the Horvath readback are an internal finite scoring construction attached to a curated biological assay; they do not by themselves become external evidence for cell-state preservation, functional restoration, safety, or durable maintenance.

\paragraph{Null conditions.}
The record becomes null for this scoped claim if any of the finite gates fail: coverage below $330/353$ or below $0.93$, loss of the frozen eight-pair normal--treated structure, nonnegative mean treated-minus-normal DNAm age, failure of the paired directional criterion, or any attempt to infer a promoted biological interpretation from the methylation-array contact alone. When those failures occur, the packet remains in a needs-data state for the age-signature layer and is blocked for all stronger interpretations. With the verified support row present, however, the current standing is a bounded age-signature result: $334$ covered CpGs, $8$ paired contrasts, $16$ array samples, and a support-true $\mathrm{Support\_A}$ verdict for the Horvath353 readback.

\origin{ai}
