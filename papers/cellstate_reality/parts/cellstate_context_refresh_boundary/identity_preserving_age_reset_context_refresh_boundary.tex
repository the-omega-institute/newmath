\subsection{identity-preserving-age-reset.context-refresh-boundary}

\paragraph{Biological object.} IdentityPreservingAgeReset↑ promotion boundary for GSE142439-style paired normal/treated cell-state rows

\paragraph{Current claim.} The next scoped promotion after AgeClockShift↑ is a cell-identity gated carrier that pairs the age-clock shift with an explicit identity-retention readout; the conjecture is that IdentityPreservingAgeReset↑ is writable exactly when the age-signature decrease and the identity-retention boundary both pass on the same paired sample scope.

\paragraph{External reality input.}
No external contact is attached.

\paragraph{Internal newmath/BEDC derivation.} Carrier: finite paired-cell readout surface joining an age-signature row to an identity-retention row for the same normal/treated samples. Distinctions: age-clock decrease is present versus absent; cell identity retained versus identity boundary changed; promotion permitted only when the identity readout is present and passes. Readback: read back only as IdentityPreservingAgeReset↑ when the treated-minus-normal age-signature shift is negative and an independent identity-retention readout certifies preserved cell identity; otherwise read back as age-signature-only or blocked\_null. Internal structure: relation; closure; trigger.

\paragraph{Falsifiable probes.}
No derived probe is attached.

\paragraph{Mismatch ledger.}
\paragraph{Cannot-claim boundary.} Do not promote a DNAm age-clock decrease to identity-preserving age reset without an identity-retention contact on the same scoped carrier.; Do not rephrase AgeClockShift↑ as rejuvenation, renewable maintenance, or immortality potential.; Do not infer preserved cell identity from methylation clock rows alone.

\paragraph{Verified facts.}
No verified facts attached yet.
