You are recovering a BEDC paper revision worktree after merging
the base branch back into the round branch.

The merge succeeded (no merge conflicts), but the combined tree now
fails `python3 lean4/scripts/bedc_ci.py audit`. The audit failure
comes from this round's additions colliding with a sibling round
that landed on the base branch concurrently — typical causes:

  - duplicate `\label{{{{thm:...}}}}` (you added the same label name
    that a sibling round just merged);
  - duplicate `\leanchecked{{{{X}}}}` for a Lean target that a sibling
    round already registered at a different paper site;
  - an `\leanchecked` / `\leanstmt` / `\leandef` referencing a
    Lean name that no longer exists after the merge.
  - a `closurestatus` block whose `\leantarget` no longer resolves,
    or whose theoryclosure × formalstatus axes contradict the live
    paper and Lean witnesses after the merge.

## Audit output (last lines)
{audit_msg}

## Your task
Remove ONLY this round's offending additions to make audit pass:

1. Run `python3 lean4/scripts/bedc_ci.py audit` to see the live
   failure list.
2. For each duplicate label: identify which `\label{{{{...}}}}`
   occurrence is the one this round added. The round's own commit
   (titled `P<round>: …`) is one parent of the most recent merge
   commit; the OTHER parent is the base branch's tip, so anything
   from there is a sibling, not yours — do NOT delete it. Use
   `git diff HEAD@{{{{1}}}}` or `git show <round-commit>` to identify
   your delta. Delete the occurrence and the surrounding
   `\begin{{{{theorem}}}} … \end{{{{theorem}}}}` block if the block exists
   only because of this round.
3. For each unresolved marker added by this round: delete the
   offending `\leanchecked` / `\leanstmt` / `\leandef` line.
4. For each offending `closurestatus` block added by this round:
   either repair the seven-field block so its `\leantarget` resolves
   and its theoryclosure × formalstatus axes remain independent, or
   delete the whole block if this round cannot justify it.
5. Do NOT introduce iteration-narrative words.
6. Do NOT touch lean4/BEDC/.
7. Re-run `cd papers/bedc && make` and `python3 lean4/scripts/bedc_ci.py audit`
   until both exit 0. If the round becomes empty after removing the
   collisions, that is acceptable. While iterating on the strip-out
   you may use `make check` (single -draftmode pass, ~half the time)
   to confirm syntax; the FINAL verification before commit must be
   the full `make` so cross-references and the audit still line up.
8. After audit passes, `git add papers/bedc` and create a small
   follow-up commit on the round branch (or amend the merge commit
   with `git commit --amend --no-edit`) so the strip-out is
   recorded without breaking the merge. Do NOT git push.

If the only valid recovery is to drop the round entirely (every
addition collided), `git reset --merge HEAD~` to undo the merge
and the round commit, then leave the worktree clean so the
pipeline can record the failure.
