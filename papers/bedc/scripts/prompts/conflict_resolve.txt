You are resolving git merge conflicts in the BEDC paper repository.

Conflicted files: {conflicted}

If both sides touch a `closurestatus` block, preserve a complete
seven-field block and keep theoryclosure × formalstatus as independent
axes. Do not collapse the block into a single closure-strength reading.

## Resolution rules
- papers/bedc/parts/**/*.tex: keep both sides' independent edits;
  dedup identical \leanchecked / \leanvariant lines; if both sides
  touched the SAME paragraph in incompatible ways, prefer the upstream
  (origin/{base_branch}) version.
- lean4/BEDC.lean and lean4/BEDC/**/*.lean: union all `import` lines,
  dedup; for declaration conflicts, prefer upstream.
- Do NOT introduce iteration-narrative words (修订/新增/patch/legacy/
  frozen/supersede/deprecated/etc.) when reconciling text.

## Deletion / narrowing rule (CRITICAL)

If one conflict side DELETED a declaration (`\begin{{theorem}}...\end{{theorem}}` with `\label`, or a chapter prose paragraph) and the other side kept or modified it, **prefer the deletion** — drop the declaration / paragraph. Deletion in BASE is deliberate cleanup (dup-label, audit fail). Re-adding via "keep both" recreates the original problem.

If a conflict touches a `\origin{{...}}` lineage line, and BASE has the NARROWER lineage (e.g. `\origin{{ai}}` while ours has `\origin{{ai-composite}}`), **trust BASE**. Lineage simplification is intentional; "keep ours" re-introduces deprecated categories.

Same rule for VALID_ORIGINS / option-sets in any Python source file: prefer the side with FEWER choices.

After resolving, `git add` each file and run `git commit --no-edit` to
complete the merge. Do NOT run `git push` and do NOT amend the merge
commit's message.
