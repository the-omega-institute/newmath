\chapter{A Concrete Naming Certificate for $\FiniteOscillationUniformModulusUp$}
\label{ch:concrete-instances-finite-oscillation-uniform-modulus-namecert}
\origin{human}

Following Bishop--Bridges compact metric continuity, the $\FiniteOscillationUniformModulusUp$ packet records the finite route from compact source data and pointwise continuity rows through finite oscillation partitions to a displayed uniform-modulus output. It sits over the compact-metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite oscillation partition row of \autoref{ch:concrete-instances-finite-oscillation-partition-namecert}, the oscillation modulus boundary of \autoref{ch:concrete-instances-oscillation-modulus-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names a compact-uniform handoff; it does not select an external modulus, identify host functions, choose points from a cover, assert quotient equality, or import ambient compactness.

\begin{definition}[Finite oscillation uniform-modulus carrier]
\label{def:finite-oscillation-uniform-modulus-carrier}
A $\FiniteOscillationUniformModulusUp$ carrier is a finite $\BHist$ packet
$$
F=(K,M,A,B,O,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact source row, carrying the compact-metric carrier, finite cover data, and metric distance rows inspected by the packet.
\item $M$ is the metric compatibility row tying the compact source to the distance comparisons used by oscillation estimates.
\item $A$ is the continuous-map pointwise row, exposing source, target, graph, and local continuity evidence for the displayed map.
\item $B$ is the $\FiniteOscillationPartitionUp$ row, listing the finite partition cells and their source-side cover witnesses.
\item $O$ is the $\OscillationModulusUp$ row, recording the oscillation bound attached to each partition cell and requested tolerance.
\item $U$ is the $\UniformModulusUp$ output row obtained by folding the finite oscillation bounds into one tolerance-only precision ledger.
\item $H$ records componentwise $\hsame$ transport for compact, metric, continuous-map, partition, oscillation, uniform-modulus, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-uniform-continuity request through $K,M,A,B,O,U,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteOscillationUniformModulusUp$, $\CompactMetricUp$, $\MetricSpaceUp$, $\ContinuousMapUp$, $\FiniteOscillationPartitionUp$, $\OscillationModulusUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteOscillationUniformModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,A,B,O,U,H,C,P,N$. The carrier has no coordinate for an external modulus selector, host function equality, point choice from a cover, quotient representation, ambient compactness theorem, or bridge certificate.
\end{definition}

\begin{theorem}[Finite oscillation uniform-modulus NameCert obligations]
\label{thm:finite-oscillation-uniform-modulus-namecert-obligations}
For every accepted $\FiniteOscillationUniformModulusUp$ carrier $F=(K,M,A,B,O,U,H,C,P,N)$, the local $\NameCert_{\FiniteOscillationUniformModulusUp}$ surface is exhausted by the following obligations:
\begin{enumerate}
\item carrier admission: the accepted object is exactly the displayed packet $K,M,A,B,O,U,H,C,P,N$;
\item compact source exposure: $K$ and $M$ display the compact-metric source and distance comparisons used by the packet;
\item continuity visibility: the pointwise continuous-map row $A$ is read before any oscillation cell is accepted;
\item finite partition exactness: $B$ lists the finite oscillation partition cells and their cover witnesses;
\item oscillation-bound compatibility: $O$ attaches each partition cell to the requested tolerance through the same metric and continuity rows;
\item uniform-modulus output: $U$ is the only exported tolerance-only precision ledger and is obtained after $B$ and $O$ are displayed;
\item ledger nonescape: $H,C,P,N$ export no external modulus selector, host function equality, point choice from a cover, quotient representation, ambient compactness theorem, or bridge certificate.
\end{enumerate}
No local NameCert obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:finite-oscillation-uniform-modulus-carrier}. Its accepted rows are exactly $K,M,A,B,O,U,H,C,P,N$, so carrier admission exposes compact source data, metric comparisons, continuous-map evidence, finite partition rows, oscillation bounds, a uniform-modulus output, and structural rows only.

The compact and continuity clauses are read first. The compact source row $K$ and metric row $M$ supply the finite cover and distance comparisons, while $A$ supplies the pointwise continuity evidence used on each displayed cell.

The partition route then reads $B$ and $O$. The finite oscillation partition row lists the cells, and the oscillation-modulus row binds each cell to the requested tolerance through the same compact, metric, and continuity data.

Finally the output row $U$ records the uniform-modulus ledger obtained from the finite oscillation bounds. The transport, replay, provenance, and naming rows preserve that displayed route and contain no coordinate for the excluded selector, host equality, point choice, quotient representation, ambient compactness theorem, or bridge certificate.
\end{proof}

\begin{theorem}[Finite oscillation compact-uniform handoff]
\label{thm:finite-oscillation-uniform-modulus-compact-handoff}
Every compact-uniform-continuity consumer of an accepted $\FiniteOscillationUniformModulusUp$ packet factors through
$$
K \longmapsto M \longmapsto A \longmapsto B \longmapsto O \longmapsto U \longmapsto H,C,P,N .
$$
In particular, a consumer preserving compact-metric, continuous-map, finite oscillation partition, oscillation modulus, and uniform-modulus provenance must read the finite oscillation partition and its oscillation bounds before exporting the tolerance-only $\UniformModulusUp$ row.
\end{theorem}
\begin{proof}
Begin with a compact-uniform request. The rows $K$ and $M$ display the compact source and metric comparisons, so the request is tied to finite source-side data rather than to an ambient compactness theorem.

The continuous-map row $A$ supplies the pointwise continuity evidence used on partition cells. After that, $B$ displays the finite oscillation partition, including the cells and cover witnesses read by the source-side request.

The oscillation row $O$ binds those cells to the requested tolerance. Since $O$ is downstream of $K,M,A,B$, it cannot be read as a detached modulus or host-level continuity theorem.

The uniform-modulus row $U$ is then the only exported tolerance-only precision ledger. The structural rows $H,C,P,N$ transport and name the same finite route, so any compact-uniform consumer reaching $U$ without $B$ and $O$ bypasses the accepted packet.
\end{proof}

\falsifiablePrediction{An accepted $\FiniteOscillationUniformModulusUp$ read is refuted if a compact-uniform-continuity consumer exports a $\UniformModulusUp$ row without displaying compact source rows, metric comparisons, continuous-map evidence, finite oscillation partitions, oscillation-modulus bounds, transport, replay, provenance, and local naming.}

\independenceWitness{compactmetric, metricspace, continuousmap, finite-oscillation-partition, oscillation-modulus, uniformmodulus: none alone is bijective with $\FiniteOscillationUniformModulusUp$ because the packet jointly carries compact source data, metric comparisons, pointwise continuity rows, finite partition cells, oscillation bounds, uniform-modulus output, transport, replay, provenance, and local NameCert.}

\closureat{FiniteOscillationUniformModulusUp}{seedStr}

\begin{closurestatus}{\FiniteOscillationUniformModulusUp}
  \constructivestory{A $\FiniteOscillationUniformModulusUp$ packet is generated from compact-metric source rows, metric comparison rows, ContinuousMap pointwise rows, finite oscillation partition rows, oscillation-modulus bounds, a UniformModulus output row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-oscillation-uniform-modulus-carrier}, \autoref{thm:finite-oscillation-uniform-modulus-namecert-obligations}, and \autoref{thm:finite-oscillation-uniform-modulus-compact-handoff} seed the finite oscillation route from compact local control to a uniform-modulus output.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close external modulus selectors, host function equality, point choice from covers, quotient representations, ambient compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact source exposure, continuity visibility, finite partition exactness, oscillation-bound compatibility, uniform-modulus output, ledger nonescape, and local naming for BEDC.Derived.FiniteOscillationUniformModulusUp.}
\end{closurestatus}
