\chapter{A Concrete Naming Certificate for $\CompactUniformCauchyMapUp$}
\label{ch:concrete-instances-compact-uniform-cauchy-map-namecert}
\origin{human}

Following Bishop and Bridges on uniform convergence and finite-net compact metric arguments, the $\CompactUniformCauchyMapUp$ packet records a compact-domain family of uniformly Cauchy continuous-map rows as finite NameCert data. It sits over the compact metric net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-Cauchy family route of \autoref{ch:concrete-instances-uniform-cauchy-map-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seals of \autoref{ch:concrete-instances-real-namecert}.

The packet names the finite handoff from compact-domain net control to a uniform-Cauchy map family. It is not an open-cover compactness principle, a selected limiting map, a quotient equality of functions, a choice operator, an ambient $\RealUp$ completeness theorem, Lean verification, or bridge checking.

\begin{definition}[Compact uniform Cauchy map carrier]
\label{def:compact-uniform-cauchy-map-carrier}
A $\CompactUniformCauchyMapUp$ carrier is a finite $\BHist$ packet
$$
K=(A,F,U,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the compact-net datum for the domain, including the finite $\CompactMetricUp$ cover windows, Lebesgue-style mesh requests, and the metric comparison rows used to inspect the domain.
\item $F$ stores the $\ContinuousMapUp$ modulus rows for the displayed family members, including source windows, target tolerances, and the local continuity budgets consumed at each compact-net point.
\item $U$ is the $\UniformCauchyMapUp$ family row, requiring all family comparisons to use one displayed tolerance schedule across the compact-domain windows named by $A$.
\item $R$ records the $\RegSeqRatUp$ readback rows for every finite tolerance, oscillation, and image-distance request exposed by $A,F,U$.
\item $E$ stores the terminal $\RealUp$ seals for the finite image-distance bounds and Cauchy comparison rows that downstream consumers may read.
\item $H$ records componentwise $\hsame$ transport for compact-net data, continuous-map moduli, uniform-Cauchy family rows, regular rational readbacks, Real seals, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-net requests through continuous-map modulus rows, then through the uniform-Cauchy family row, and finally into the readback and Real seal rows.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformCauchyMapUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformCauchyMapUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformCauchyMapUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,F,U,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected limits, quotient equality of functions, choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[CompactUniformCauchyMap NameCert obligations]
\label{thm:compact-uniform-cauchy-map-namecert-obligations}
For every accepted $\CompactUniformCauchyMapUp$ carrier
$$
K=(A,F,U,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformCauchyMapUp}$ surface consists exactly of compact-net admission for $A$, continuous-map modulus admission for $F$, uniform-Cauchy family admission for $U$, regular rational readback through $R$, Real seal exposure through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.

No compact uniform Cauchy map obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The compact-domain information available to the packet is exactly the finite net row $A$, so every later modulus request is tied to displayed compact metric windows rather than to an open-cover principle.

The analytic rows are then forced. The continuous-map data $F$ supplies the local modulus budgets, while $U$ requires the family comparison to use one tolerance schedule across the compact-domain windows named by $A$.

The readback and seal rows close the public surface. The row $R$ records the regular rational observations used by the compact-net, modulus, and uniform-Cauchy routes, and $E$ is visible only after those rows have been replayed.

Finally inspect the structural rows. The transport, continuation, provenance, and local NameCert rows preserve the same finite route, and the carrier has no coordinate for open-cover compactness, selected limiting maps, quotient function equality, choice, ambient completeness, Lean verification, or bridge checking.
\end{proof}

\begin{theorem}[CompactUniformCauchyMap uniform-modulus handoff]
\label{thm:compact-uniform-cauchy-map-uniform-modulus-handoff}
Let $K=(A,F,U,R,E,H,C,P,N)$ be an accepted $\CompactUniformCauchyMapUp$ carrier. Every downstream uniform-modulus read from $K$ factors through the finite route
$$
A,F \longrightarrow U \longrightarrow R \longrightarrow E
$$
with transport, replay, provenance, and local naming supplied by $H,C,P,N$. Thus the exported modulus is a compact-net uniform-Cauchy handoff, not a selected limit map or an equality of quotient functions.
\end{theorem}
\begin{proof}
Start with a downstream uniform-modulus read. Carrier admission exposes the compact-net row $A$ and the continuous-map modulus row $F$ before any family-level comparison is available.

The uniform-Cauchy row $U$ then consumes those same compact-domain windows and modulus budgets. Because $U$ stores one family tolerance schedule over the displayed finite net data, the handoff is uniform over the named compact-domain requests.

The readback route is finite. The row $R$ records the regular rational bounds used in the compact-net and uniform-Cauchy comparison, and the terminal Real seals in $E$ may be read only after that route is displayed.

The support rows $H,C,P,N$ transport and replay the route without adding another analytic coordinate. Hence the public handoff excludes open-cover compactness, selected limits, quotient function equality, choice, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{proof}

\closureat{CompactUniformCauchyMapUp}{seedStr}

\begin{closurestatus}{\CompactUniformCauchyMapUp}
  \constructivestory{A $\CompactUniformCauchyMapUp$ packet is generated from compact-net data, ContinuousMapUp modulus rows, one UniformCauchyMapUp family row, RegSeqRat readback rows, RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-cauchy-map-carrier}, \autoref{thm:compact-uniform-cauchy-map-namecert-obligations}, and \autoref{thm:compact-uniform-cauchy-map-uniform-modulus-handoff} seed the compact-domain uniform-Cauchy map packet from compact-net rows, continuous-map modulus rows, uniform-Cauchy family rows, regular rational readback, Real seals, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected limits, quotient function equality, choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact-net admission, continuous-map modulus admission, uniform-Cauchy family exactness, regular rational readback, Real seal nonescape, transport stability, replay stability, and TasteGate field faithfulness for BEDC.Derived.CompactUniformCauchyMapUp.}
\end{closurestatus}
