\chapter{A Concrete Naming Certificate for $\RegularCauchyFilterUniversalPropertyUp$}
\label{ch:concrete-instances-regular-cauchy-filter-universal-property-namecert}
\origin{human}

Following Bishop and Bridges, Constructive Analysis, the $\RegularCauchyFilterUniversalPropertyUp$ packet records the filter-facing universal-property route for regular Cauchy real names in the L10 Real and Completion spine. It sits after the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the criterion surface of \autoref{ch:concrete-instances-regularcauchycriterion-namecert}, the Cauchy-filter source of \autoref{ch:concrete-instances-cauchyfilter-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a universal readout through displayed rows only; it does not quotient streams, select limits, compare host real equality, or invoke ambient $\RealUp$ completeness.

\begin{definition}[RegularCauchyFilterUniversalProperty carrier]
\label{def:regular-cauchy-filter-universal-property-carrier}
A $\RegularCauchyFilterUniversalPropertyUp$ carrier is a finite $\BHist$ packet
$$
U=(W,R,K,F,B,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the $\StreamNameUp$ finite-window ledger, recording the inspected stream stages and filter requests.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational values observed through $W$.
\item $K$ is the $\RegularCauchyCriterionUp$ row certifying that the same windows and readbacks meet the regular Cauchy tail discipline.
\item $F$ is the $\CauchyFilterUp$ source row reached from $W,R,K$ and used as the filter-facing input to the universal property.
\item $B$ is the $\RealUp$ boundary row reached only after the regular Cauchy criterion and Cauchy-filter source rows have been displayed.
\item $T$ records componentwise $\hsame$ transport for the window, readback, criterion, filter source, Real boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
W,R,K,F \longmapsto B,T,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyFilterUniversalPropertyUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RegularCauchyCriterionUp$, $\CauchyFilterUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyFilterUniversalPropertyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $W,R,K,F,B,T,C,P,N$. It has no coordinate for quotient stream equality, a selected limit representative, host real equality, countable choice, or ambient Real completeness.
\end{definition}

\begin{theorem}[RegularCauchyFilterUniversalProperty NameCert obligations]
\label{thm:regular-cauchy-filter-universal-property-obligations}
For every accepted $\RegularCauchyFilterUniversalPropertyUp$ carrier $U=(W,R,K,F,B,T,C,P,N)$, the local $\NameCert_{\RegularCauchyFilterUniversalPropertyUp}$ surface consists exactly of StreamName window exposure through $W$, RegSeqRat readback through $R$, RegularCauchyCriterion tail testing through $K$, CauchyFilter source exposure through $F$, Real boundary exposure through $B$, componentwise $\hsame$ transport through $T$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its rows are precisely the finite window ledger, regular rational readback, regular Cauchy criterion, Cauchy-filter source, Real boundary, transport, replay, provenance, and naming rows.

Read the source route in order. The window row $W$ supplies the inspected stages, $R$ reads their regular rational values, and $K$ checks the regular Cauchy tail discipline before any filter-facing row is available.

The filter and Real rows are then confined to the same route. The source row $F$ is reached from $W,R,K$, and $B$ is read only as the terminal Real boundary of that displayed source.

Finally $T,C,P,N$ transport, replay, source, and name the same packet. Since the carrier has no quotient stream, selected limit, host equality, countable-choice, or ambient-completeness coordinate, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[RegularCauchyFilterUniversalProperty route]
\label{thm:regular-cauchy-filter-universal-property-route}
Every Real-and-Completion consumer of an accepted $\RegularCauchyFilterUniversalPropertyUp$ packet reads the finite route
$$
W,R,K,F \longmapsto B,T,C,P,N .
$$
Thus the universal-property handoff starts with StreamName windows, RegSeqRat readback, and RegularCauchyCriterion rows, reaches the CauchyFilter source only after those rows are visible, and reaches the Real boundary only through the same displayed packet.
\end{theorem}
\begin{proof}
Start with the NameCert obligations. They order the consumer read from $W$ to $R$ to $K$ before the Cauchy-filter row $F$ can be inspected.

The terminal boundary is likewise displayed. The Real row $B$ is not an ambient completeness theorem; it is the boundary row reached by replaying the regular Cauchy filter route.

Transport, replay, provenance, and local naming preserve that order. Hence any universal-property handoff exported by the packet factors through $W,R,K,F,B,T,C,P,N$.
\end{proof}

\begin{theorem}[RegularCauchyFilterUniversalProperty nonescape]
\label{thm:regular-cauchy-filter-universal-property-nonescape}
An accepted $\RegularCauchyFilterUniversalPropertyUp$ packet cannot export quotient stream equality, a selected limit representative, host equality of real names, countable choice, or ambient Real completeness. Any consumer claiming such data must supply it from a different certificate, because the local carrier stores only $W,R,K,F,B,T,C,P,N$.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates. The source data are finite StreamName windows, RegSeqRat readbacks, a RegularCauchyCriterion row, and a CauchyFilter row; the target data are a displayed Real boundary together with transport, replay, provenance, and naming.

None of these rows is a quotient-stream representative, a selected limit, a host equality proof, a choice selector, or an ambient completeness theorem. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the listed rows rather than adding new host-side objects.

Therefore a consumer can escape the packet only by citing an additional certificate. The local $\RegularCauchyFilterUniversalPropertyUp$ NameCert itself exports exactly the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyFilterUniversalPropertyUp$ consumer that exports a filter-facing universal property while omitting one of the displayed StreamName, RegSeqRat, RegularCauchyCriterion, CauchyFilter, Real boundary, transport, replay, provenance, or local naming rows refutes the packet.}

\independenceWitness{streamname, regseqrat, regularcauchycriterion, cauchyfilter, real: none is bijective because $\RegularCauchyFilterUniversalPropertyUp$ jointly carries finite windows, regular rational readback, regular Cauchy tail testing, Cauchy-filter source exposure, terminal Real boundary, transport, replay, provenance, and local NameCert data.}

\closureat{RegularCauchyFilterUniversalPropertyUp}{seedStr}

\begin{closurestatus}{\RegularCauchyFilterUniversalPropertyUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-filter-universal-property-carrier}, \autoref{thm:regular-cauchy-filter-universal-property-obligations}, \autoref{thm:regular-cauchy-filter-universal-property-route}, and \autoref{thm:regular-cauchy-filter-universal-property-nonescape} seed the filter-facing universal-property route over StreamName, RegSeqRat, RegularCauchyCriterion, CauchyFilter, Real boundary, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, countable choice, ambient Real completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, route ordering, transport stability, replay exactness, and non-escape proofs for BEDC.Derived.RegularCauchyFilterUniversalPropertyUp.}
  \constructivestory{A $\RegularCauchyFilterUniversalPropertyUp$ packet is generated from StreamName windows, RegSeqRat readback, RegularCauchyCriterion rows, a CauchyFilter source row, a RealUp boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
