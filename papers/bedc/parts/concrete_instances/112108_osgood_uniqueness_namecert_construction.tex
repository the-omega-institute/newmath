\chapter{A Concrete Naming Certificate for $\OsgoodUniquenessUp$}
\label{ch:concrete-instances-osgood-uniqueness-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and Osgood's uniqueness criterion for ordinary differential equations, the $\OsgoodUniquenessUp$ packet records the finite modulus ledger needed to separate constructive uniqueness from a Lipschitz or contraction hypothesis. It sits after the local initial-data surface of \autoref{ch:concrete-instances-initial-value-problem-namecert}, the ODE row of \autoref{ch:concrete-instances-ode-namecert}, the Picard handoff of \autoref{ch:concrete-instances-picard-lindelof-namecert}, the contraction neighbour of \autoref{ch:concrete-instances-bishop-interval-contraction-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names an Osgood-style integral divergence budget as displayed BEDC data; it is not a host Lipschitz constant, a Banach contraction theorem, a selected solution representative, or quotient equality of solution curves.

\begin{definition}[OsgoodUniqueness carrier]
\label{def:osgood-uniqueness-carrier}
An $\OsgoodUniquenessUp$ carrier is a finite $\BHist$ packet
$$
U=(D,I,F,M,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\OdeUp$ source row, recording the differential equation, state window, and time window read by the uniqueness request.
\item $I$ is the initial-data row, carrying the common initial time and initial $\RealUp$ state for the two compared solution traces.
\item $F$ is the $\ContinuousMapUp$ vector-field row, read as a displayed continuous map on the same ODE window rather than as a host function.
\item $M$ is the Osgood modulus ledger, listing dyadic positive radii, comparison budgets, and the finite divergence checks consumed by the uniqueness argument.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance row used to compare distances, radii, and accumulated modulus budgets.
\item $R$ is the $\RegSeqRatUp$ readback row for the two solution-distance traces inspected by the packet.
\item $E$ is the terminal $\RealUp$ seal reached only after $D,I,F,M,Q,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the ODE row, initial data, continuous-map row, modulus ledger, dyadic row, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the Osgood uniqueness request through the ODE, initial-data, vector-field, modulus, dyadic, regular-rational, and Real-seal rows.
\item $P$ is the $\Pkg$ provenance over $\OsgoodUniquenessUp$, $\OdeUp$, $\ContinuousMapUp$, $\RealUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\OsgoodUniquenessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,I,F,M,Q,R,E,H,C,P,N$. It has no coordinate for a Lipschitz constant, a contraction ratio, selected solution representatives, quotient equality of curves, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[OsgoodUniqueness NameCert obligations]
\label{thm:osgood-uniqueness-namecert-obligations}
For every accepted $\OsgoodUniquenessUp$ carrier
$$
U=(D,I,F,M,Q,R,E,H,C,P,N),
$$
the local $\NameCert_{\OsgoodUniquenessUp}$ surface consists exactly of carrier admission, ODE-window visibility through $D$, common initial-data visibility through $I$, continuous vector-field readback through $F$, Osgood modulus accounting through $M$, dyadic tolerance exactness through $Q$, regular rational distance readback through $R$, terminal $\RealUp$ seal nonescape through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.

These obligations bind the uniqueness route without adding a Lipschitz constant, a contraction ratio, a host fixed-point theorem, selected solution representatives, quotient equality of curves, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:osgood-uniqueness-carrier}. The displayed rows $D,I,F,M,Q,R,E,H,C,P,N$ are the entire local surface, so carrier admission is exactly the admission of those rows.

The ODE and initial data are read first. The row $D$ supplies the differential equation and local window, while $I$ records the common initial time and initial real state. Thus the two solution traces are compared only inside one displayed initial-value surface.

The vector-field and modulus data are then consumed. The row $F$ exposes continuity through $\ContinuousMapUp$, and the row $M$ records the Osgood modulus ledger: dyadic radii, comparison budgets, and the finite checks that witness the divergence barrier used by the uniqueness argument.

The readback and seal rows are downstream. The row $Q$ fixes the dyadic tolerance arithmetic, $R$ reads the compared solution-distance traces as $\RegSeqRatUp$ data, and $E$ exposes the terminal $\RealUp$ boundary only after those rows are present. Since no carrier coordinate stores a Lipschitz constant, contraction ratio, selected solution, quotient curve equality, countable-choice schedule, or ambient completeness theorem, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[OsgoodUniqueness non-Lipschitz route]
\label{thm:osgood-uniqueness-non-lipschitz-route}
Every uniqueness-facing consumer of an accepted $\OsgoodUniquenessUp$ packet must read the finite route
$$
\OdeUp,\ \ContinuousMapUp,\ M,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp
$$
as displayed by \autoref{def:osgood-uniqueness-carrier}. The route supplies an Osgood modulus ledger for comparing solution traces; it does not export a Lipschitz constant, a Banach contraction certificate, a selected solution representative, or quotient equality between curves.
\end{theorem}
\begin{proof}
Begin with the common ODE surface. The rows $D$ and $I$ bind the equation, time window, state window, and common initial data before any solution-distance comparison is admitted.

Next read the vector-field and Osgood ledger. The continuous-map row $F$ gives the displayed continuity route, while $M$ records the finite modulus budgets consumed by the Osgood comparison. This is the analytic distinction from the contraction packets: the certificate reads a divergence ledger, not a strict contraction ratio.

Finally pass through dyadic tolerance, regular rational readback, and the terminal Real seal. The rows $Q,R,E$ expose only the displayed distance comparisons and Real-facing boundary, and $H,C,P,N$ transport, replay, source, and name the same finite route. Hence the accepted read remains an Osgood uniqueness packet rather than a Lipschitz or contraction theorem.
\end{proof}

\falsifiablePrediction{An accepted $\OsgoodUniquenessUp$ uniqueness read that reaches $\RegSeqRatUp$ distance readback or a $\RealUp$ seal without displaying the rows $D,I,F,M,Q,R,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{ode, initial-value-problem, picard-lindelof, bishop-interval-contraction, regseqrat, dyadicratcore, real: no listed sibling supplies the joint packet containing an ODE source, common initial data, ContinuousMap vector-field readback, Osgood modulus ledger, dyadic tolerance arithmetic, regular rational distance readback, Real seal, transport, replay, provenance, and local NameCert row.}

\closureat{OsgoodUniquenessUp}{seedStr}

\begin{closurestatus}{\OsgoodUniquenessUp}
  \constructivestory{An $\OsgoodUniquenessUp$ packet is generated from an ODE source row, common initial data, a ContinuousMap vector-field row, an Osgood modulus ledger, DyadicRatCore tolerance arithmetic, RegSeqRat distance readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:osgood-uniqueness-carrier}, \autoref{thm:osgood-uniqueness-namecert-obligations}, and \autoref{thm:osgood-uniqueness-non-lipschitz-route} seed the finite Osgood uniqueness route from displayed ODE data and continuous vector-field rows through modulus accounting, dyadic tolerance arithmetic, regular readback, and the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, bridge checking, Lipschitz uniqueness, Banach contraction, selected solution representatives, quotient equality of curves, countable choice, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, ODE-window visibility, common initial-data visibility, continuous-map readback, Osgood modulus accounting, dyadic tolerance exactness, regular readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.OsgoodUniquenessUp.}
\end{closurestatus}
