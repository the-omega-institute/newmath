\chapter{A Concrete Naming Certificate for $\RegularCauchyFilterBasisFunctorUp$}
\label{ch:concrete-instances-regular-cauchy-filter-basis-functor-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley filter terminology, the $\RegularCauchyFilterBasisFunctorUp$ packet records the functorial handoff by which a displayed regular Cauchy filter-basis route is transported between completion-facing sources without choosing a completed limit. It sits after the basis carrier of \autoref{ch:concrete-instances-regular-cauchy-filter-basis-namecert}, the filter surface of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the displayed basis-to-filter route and its structural preservation; it does not assert $\RealUp$ completeness, selected limits, quotient filters, host equality of completions, or countable choice.

\begin{definition}[RegularCauchyFilterBasisFunctor carrier]
\label{def:regular-cauchy-filter-basis-functor-carrier}
A $\RegularCauchyFilterBasisFunctorUp$ carrier is a finite $\BHist$ packet
$$
F=(S,R,D,B,M,\Phi,G,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window schedule inherited from the source regular Cauchy basis.
\item $R$ is the $\RegSeqRatUp$ readback row attached to those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger transported with the same windows.
\item $B$ is the source $\RegularCauchyFilterBasisUp$ basis row, including its finite meet surface.
\item $M$ records preservation of the finite meet row under the handoff.
\item $\Phi$ is the displayed map row sending each accepted source basis observation to a target basis observation.
\item $G$ is the $\CauchyFilterUp$ consumer row exposed only after $S,R,D,B,M,\Phi$ have been read.
\item $E$ is the $\RealUp$ seal row reached by the target filter-basis handoff.
\item $H$ records componentwise $\hsame$ transport for the schedule, readback, tolerance, basis, meet, map, filter, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same handoff route.
\item $P$ records $\Pkg$ provenance over $\RegularCauchyFilterBasisFunctorUp$, $\RegularCauchyFilterBasisUp$, $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyFilterBasisFunctorUp}$ row.
\end{enumerate}
The carrier stores no quotient filter, no chosen limit point, no external completion object, no countable-choice selector, and no ambient proof that every Cauchy filter converges.
\end{definition}

\begin{theorem}[RegularCauchyFilterBasisFunctor NameCert obligations]
\label{thm:regular-cauchy-filter-basis-functor-namecert-obligations}
For every accepted $\RegularCauchyFilterBasisFunctorUp$ carrier
$$
F=(S,R,D,B,M,\Phi,G,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyFilterBasisFunctorUp}$ surface consists exactly of carrier admission for the finite schedule, regular-rational readback, dyadic tolerance ledger, source basis row, meet-preservation row, displayed handoff map, target $\CauchyFilterUp$ consumer row, terminal $\RealUp$ seal, componentwise transport, displayed replay, provenance, and local naming. Every Real-facing or Completion-facing consumer must first read the finite route
$$
S,R,D,B,M,\Phi \longmapsto G,E,H,C,P,N .
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-filter-basis-functor-carrier}. The source side is the finite window schedule $S$, the regular-rational readback $R$, the dyadic tolerance ledger $D$, the source basis row $B$, and the meet-preservation row $M$ inherited from \autoref{ch:concrete-instances-regular-cauchy-filter-basis-namecert}.

Read the functorial handoff through $\Phi$. It sends accepted source basis observations to target basis observations while preserving the finite meet and tolerance rows, so the target filter row $G$ is exposed only after the displayed basis data have already been read.

Finally consume the seal and structural rows. The real-name seal $E$ and the transport, replay, provenance, and local naming rows $H,C,P,N$ preserve the same route. Since the carrier has no coordinate for quotient filters, selected limits, countable choice, host completion equality, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[RegularCauchyFilterBasisFunctor finite handoff scope]
\label{thm:regular-cauchy-filter-basis-functor-finite-handoff-scope}
Every accepted $\RegularCauchyFilterBasisFunctorUp$ handoff from a $\RegularCauchyFilterBasisUp$ source to a $\CauchyFilterUp$ consumer factors through $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, the source basis and meet rows, the displayed handoff map, the target filter row, the $\RealUp$ seal, and the structural rows $H,C,P,N$. The handoff is a finite functorial route; it is not quotient-filter equality, selected convergence, countable choice, or ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Begin with the source basis carrier. The rows $S,R,D,B,M$ provide all source observations and finite common-refinement data before a target filter consumer can read the packet.

Apply the displayed map row $\Phi$. It preserves the basis observations and the meet row at the level of the carried $\BHist$ data, so the consumer row $G$ is reached by a finite handoff rather than by quotienting filters.

The remaining rows $E,H,C,P,N$ only seal, transport, replay, source, and name the same handoff. Therefore no selected limit, countable-choice selector, host equality of completions, or ambient $\RealUp$ completeness principle is available from the packet.
\end{proof}

\falsifiablePrediction{A $\RegularCauchyFilterBasisFunctorUp$ consumer that exposes a target $\CauchyFilterUp$ or $\RealUp$ row while omitting the finite schedule, regular-rational readback, dyadic tolerance ledger, source basis, meet-preservation, displayed handoff map, transport, replay, provenance, or local $\NameCert$ rows refutes \autoref{def:regular-cauchy-filter-basis-functor-carrier}.}

\independenceWitness{regular-cauchy-filter-basis, cauchyfilter, streamname, regseqrat, dyadicratcore, real: $\RegularCauchyFilterBasisFunctorUp$ is not bijective to any listed sibling because it carries a displayed preservation map from a regular Cauchy basis into a filter consumer. The basis sibling has the source meet row without the handoff map; CauchyFilterUp has filter consumption without the source basis functor; StreamNameUp, RegSeqRatUp, and DyadicRatCoreUp provide schedule, readback, and tolerance rows without filter functoriality; RealUp supplies the terminal seal without the basis-to-filter route.}

\closureat{RegularCauchyFilterBasisFunctorUp}{seedStr}

\begin{closurestatus}{\RegularCauchyFilterBasisFunctorUp}
  \constructivestory{A $\RegularCauchyFilterBasisFunctorUp$ packet is generated from finite StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RegularCauchyFilterBasis source row, meet preservation, a displayed handoff map, a CauchyFilter consumer row, a RealUp seal, componentwise transport, displayed replay, package provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-filter-basis-functor-carrier}, \autoref{thm:regular-cauchy-filter-basis-functor-namecert-obligations}, and \autoref{thm:regular-cauchy-filter-basis-functor-finite-handoff-scope} seed the functorial handoff from regular Cauchy filter bases to completion-facing filter and Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Real completeness, selected limits, quotient filters, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, meet preservation, displayed handoff stability, filter consumer nonescape, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyFilterBasisFunctorUp.}
\end{closurestatus}
