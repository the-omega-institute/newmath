The sibling route for $\SubgroupUp$ is the algebraic ladder already displayed by $\GroupUp$ and $\MonoidUp$. The subgroup packet consumes the ambient carrier, operation, identity, inverse, and closure rows from \autoref{ch:concrete-instances-group-namecert}, while its operation and unit layer is inherited through the monoid certificate of \autoref{ch:concrete-instances-monoid-namecert}; the carried subset is therefore a refinement of that ambient algebraic surface rather than a detached kernel entry.
