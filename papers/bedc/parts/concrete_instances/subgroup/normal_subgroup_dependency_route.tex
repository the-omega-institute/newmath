Quotient-facing $\SubgroupUp$ consumers read the ambient operation rows of \autoref{ch:concrete-instances-group-namecert}, the unit and multiplication discipline of \autoref{ch:concrete-instances-monoid-namecert}, and the classifier boundary of \autoref{ch:concrete-instances-quotientgroup-namecert} before any normalizer or kernel route is accepted. The dependency route keeps $\SubgroupUp$ inside the displayed algebra lattice: a normal-subgroup consumer may reuse the subgroup carrier only after the group, monoid, and quotient-group rows have fixed the operation, identity, equivalence, and kernel-facing classifier surface.
