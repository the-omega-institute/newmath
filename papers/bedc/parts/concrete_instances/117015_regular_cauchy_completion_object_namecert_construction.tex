\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionObjectUp$}
\label{ch:concrete-instances-regular-cauchy-completion-object-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyCompletionObjectUp$ packet records the object-level completion carrier before theorem-facing dense-embedding, universal-property, functoriality, or $\RealUp$ sealing routes are consumed. It sits over the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names the completion object as displayed regular Cauchy data with transport, replay, provenance, and local naming; it does not assert quotient completion equality, select limits, identify host real equality, invoke countable choice, or import ambient completeness of $\RealUp$.

\begin{definition}[Regular Cauchy completion object carrier]
\label{def:regular-cauchy-completion-object-carrier}
A $\RegularCauchyCompletionObjectUp$ carrier is a finite $\BHist$ packet
$$
X=(R,W,D,E,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row, carrying the regular rational name and its regularity readback surface.
\item $W$ is the $\StreamNameUp$ finite observation row, naming the windows opened by completion-facing requests.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording dyadic radii, slack cells, and comparison rows used by the regular Cauchy read.
\item $E$ is the object-admission row saying that $R,W,D$ jointly form the displayed completion carrier before any theorem route is inspected.
\item $A$ is the terminal $\RealUp$ boundary row available to Real-facing consumers only as a downstream seal over $R,W,D,E$.
\item $H$ records componentwise $\hsame$ transport for the regular source, window, dyadic tolerance, object admission, Real boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion request through $W,D,R,E,A$ and the structural rows.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionObjectUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionObjectUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $R,W,D,E,A,H,C,P,N$. It has no coordinate for quotient streams, selected representatives, selected limits, countable-choice schedules, host real equality, theorem-level universal properties, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy completion object NameCert obligations]
\label{thm:regular-cauchy-completion-object-namecert-obligations}
For every accepted $\RegularCauchyCompletionObjectUp$ carrier
$$
X=(R,W,D,E,A,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyCompletionObjectUp}$ surface consists exactly of regular rational source admission, finite StreamName observation, DyadicRatCore tolerance visibility, object-admission exactness, Real-boundary nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through the displayed rows. Dense-embedding, universal-property, functoriality, and Real-sealing chapters may cite this object only through those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-completion-object-carrier}. The rows $R,W,D,E,A,H,C,P,N$ are the full accepted packet.

The object rows are $R,W,D,E$. The regular rational source, finite observation windows, and dyadic tolerance ledger are displayed before the admission row $E$ may name the completion carrier.

The Real-facing row $A$ is downstream of the same displayed data. It seals the route for consumers of \autoref{ch:concrete-instances-real-namecert} without adding quotient equality, a selected representative, a selected limit, or ambient completeness.

The structural rows $H,C,P,N$ transport, replay, source, and name only that finite route. Therefore the listed obligations exhaust the local NameCert surface and give theorem-facing consumers a single object-level carrier to cite.
\end{proof}

\begin{theorem}[Regular Cauchy completion object dense-embedding handoff]
\label{thm:regular-cauchy-completion-object-dense-embedding-handoff}
Every dense-embedding, universal-property, functoriality, or $\RealUp$ sealing consumer that cites an accepted $\RegularCauchyCompletionObjectUp$ packet first reads the displayed route
$$
\RegSeqRatUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The handoff is the object-level completion carrier $R,W,D,E,A,H,C,P,N$, not a theorem route itself, not quotient completion equality, not a selected limit, not countable choice, and not ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $X=(R,W,D,E,A,H,C,P,N)$. The completion-facing consumer first opens the finite StreamName windows $W$ and dyadic tolerance ledger $D$ attached to the regular rational source $R$.

The admission row $E$ then names those displayed rows as the completion object. This is the object-level boundary that theorem chapters may consume before proving dense embedding, universal mapping, or functorial transport.

If a Real-facing seal is needed, it is read through $A$ after the same regular source, window, tolerance, and admission rows. The structural rows $H,C,P,N$ preserve that route under transport, replay, provenance, and local naming.

Since no carrier coordinate stores a quotient completion, selected representative, selected limit, countable-choice schedule, host real equality, or ambient completeness theorem, the consumer receives only the displayed completion object.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyCompletionObjectUp$ read factors through a RegSeqRat source row, StreamName finite observation row, DyadicRatCore tolerance ledger, object-admission row, RealUp boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row before any theorem-facing completion route is consumed.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real: none is bijective with $\RegularCauchyCompletionObjectUp$ because this packet alone binds regular rational source data, finite observation windows, dyadic tolerance ledgers, object admission, Real boundary sealing, transport, replay, provenance, and local naming as one completion object carrier.}

\closureat{RegularCauchyCompletionObjectUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionObjectUp}
  \constructivestory{A $\RegularCauchyCompletionObjectUp$ packet is generated from a RegSeqRat source row, StreamName finite observation rows, DyadicRatCore tolerance rows, an object-admission row, a RealUp boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-object-carrier}, \autoref{thm:regular-cauchy-completion-object-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-object-dense-embedding-handoff} seed the object-level regular Cauchy completion carrier for later dense-embedding, universal-property, functoriality, and RealUp sealing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close dense embedding, universal properties, functoriality, quotient completion equality, selected representatives, selected limits, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked regular source admission, finite observation, dyadic tolerance visibility, object-admission exactness, Real-boundary nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyCompletionObjectUp.}
\end{closurestatus}
