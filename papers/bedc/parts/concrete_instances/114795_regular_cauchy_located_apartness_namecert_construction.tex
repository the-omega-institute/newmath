\chapter{A Concrete Naming Certificate for $\RegularCauchyLocatedApartnessUp$}
\label{ch:concrete-instances-regular-cauchy-located-apartness-namecert}
\origin{human}

Following Bishop--Bridges constructive real analysis, the $\RegularCauchyLocatedApartnessUp$ packet records located apartness between regular Cauchy real names as finite NameCert data before any completed real object is consulted. It reads the regular rational source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the positive dyadic separation witnesses of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window schedules of \autoref{ch:concrete-instances-streamname-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the separated completion boundary of \autoref{ch:concrete-instances-realapartnesscompletion-namecert}. The carrier names a constructive apartness route for later Real and Completion consumers; it does not assert trichotomy, Dedekind cuts, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular-Cauchy located-apartness carrier]
\label{def:regular-cauchy-located-apartness-carrier}
A $\RegularCauchyLocatedApartnessUp$ carrier is a finite $\BHist$ packet
$$
L=(A,B,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two $\RegSeqRatUp$ source rows naming the regular Cauchy rational streams under comparison.
\item $W$ is the shared $\StreamNameUp$ finite-window row exposing the two sources at the same requested precisions.
\item $D$ is the $\DyadicRatCoreUp$ positive separation witness row; it displays a dyadic radius and a finite window at which the two regular Cauchy reads are separated by that positive tolerance.
\item $R$ is the paired $\RegSeqRatUp$ readback row reached from $A,B,W,D$, recording the inspected rational values and their dyadic separation ledger.
\item $E$ is the $\RealUp$ seal row reached only after the regular sources, shared windows, positive dyadic witness, and readback row have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two source rows, the shared window, the positive dyadic witness, the readback row, the real seal, the replay row, the provenance row, and the local name row.
\item $C$ records displayed $\Cont$ replay from the two regular Cauchy source rows through the common finite window, the positive dyadic witness, the regular readback, and then the real seal.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLocatedApartnessUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, $\RealApartnessCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyLocatedApartnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,W,D,R,E,H,C,P,N$. It has no coordinate for real trichotomy, Dedekind cuts, quotient equality of real names, selected limits, countable choice, or completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Regular-Cauchy located-apartness NameCert obligations]
\label{thm:regular-cauchy-located-apartness-namecert-obligations}
For every accepted $\RegularCauchyLocatedApartnessUp$ carrier $L=(A,B,W,D,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyLocatedApartnessUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ sources, shared $\StreamNameUp$ window, $\DyadicRatCoreUp$ positive separation witness, paired readback, $\RealUp$ seal, transport, replay, provenance, and local naming rows;
\item located positivity, requiring the witness row $D$ to display a positive dyadic separation before any apartness consumer can inspect the seal row $E$;
\item regular readback exactness, requiring the values compared by $D$ to be the values exposed by $A,B$ through the shared window $W$ and repacked in $R$;
\item componentwise stability, requiring $H$ and $C$ to transport and replay only the displayed rows $A,B,W,D,R,E,P,N$;
\item ledger exhaustion, requiring the packet to export no trichotomy decision, Dedekind cut, quotient real equality, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness principle.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is obtained by projecting the tuple of \autoref{def:regular-cauchy-located-apartness-carrier}. The displayed rows are precisely the two regular Cauchy sources, common finite window, dyadic positivity witness, paired readback, Real seal, transport, replay, provenance, and local name.

Located positivity is the role of $D$. The row stores a positive dyadic separation witness together with the finite window on which the two source reads are inspected, so apartness is exposed as positive data rather than as a trichotomy branch.

Regular readback exactness follows from the order of $A,B,W,D,R$. The readback row $R$ is downstream of the two sources, the common window, and the dyadic witness; hence it cannot compare host representatives or quotient classes.

Componentwise stability and ledger exhaustion are supplied by $H,C,P,N$. These rows transport, replay, source, and name the same finite packet, and no row remains that could introduce Dedekind cuts, quotient real equality, selected limits, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Regular-Cauchy located-apartness Real-seal boundary]
\label{thm:regular-cauchy-located-apartness-real-seal-boundary}
Let $L=(A,B,W,D,R,E,H,C,P,N)$ be an accepted $\RegularCauchyLocatedApartnessUp$ packet. Every Real-facing apartness consumer of $L$ factors through the finite route
$$
A,B,W,D,R \longmapsto E \longmapsto H,C,P,N .
$$
Consequently the $\RealUp$ seal exposes only a regular-Cauchy located-apartness read through $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, $\RealApartnessCompletionUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. It cannot be reinterpreted as trichotomy, a Dedekind cut, quotient real equality, a selected limit, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an accepted Real-facing apartness read. Carrier admission gives the two regular Cauchy source rows $A,B$ and the shared finite window row $W$ before any separation data can be inspected.

Next read the positive witness and regular readback. The dyadic row $D$ supplies the positive separation, while $R$ records that the compared values are exactly the regular rational reads exposed through $A,B,W$.

Finally expose the seal row and structural rows. Since $E$ is downstream of $A,B,W,D,R$, and since $H,C,P,N$ transport, replay, source, and name only that packet, every consumer remains inside the displayed finite route and cannot escape to trichotomy, Dedekind cuts, quotient real equality, selected limits, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyLocatedApartnessUp$ consumer that reads apartness without two $\RegSeqRatUp$ sources, a shared $\StreamNameUp$ finite window, a positive $\DyadicRatCoreUp$ separation witness, paired readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row refutes the carrier surface.}

\independenceWitness{regseqrat, dyadicratcore, streamname, real, realapartnesscompletion: no listed sibling alone carries the two regular Cauchy source rows, shared finite window, positive dyadic separation witness, paired readback, terminal Real seal, transport, replay, provenance, and local naming rows of $L=(A,B,W,D,R,E,H,C,P,N)$.}

\closureat{RegularCauchyLocatedApartnessUp}{seedStr}

\begin{closurestatus}{\RegularCauchyLocatedApartnessUp}
  \constructivestory{A $\RegularCauchyLocatedApartnessUp$ packet is generated from two RegSeqRat source rows, a shared StreamName finite window, a positive DyadicRatCore separation witness, paired RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-located-apartness-carrier}, \autoref{thm:regular-cauchy-located-apartness-namecert-obligations}, and \autoref{thm:regular-cauchy-located-apartness-real-seal-boundary} seed the finite located-apartness carrier over regular Cauchy names, dyadic positive witnesses, shared stream windows, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close real trichotomy, Dedekind cuts, quotient real equality, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive dyadic witness exactness, RegSeqRat readback exposure, StreamName window sharing, Real-seal nonescape, structural transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyLocatedApartnessUp.}
\end{closurestatus}
