\chapter{A Concrete Naming Certificate for $\DyadicNestedIntervalChoiceFreeUp$}
\label{ch:concrete-instances-dyadic-nested-interval-choice-free-namecert}
\origin{human}

Following Bishop-style constructive nested interval reasoning, the $\DyadicNestedIntervalChoiceFreeUp$ packet records the finite dyadic window route by which nested located intervals yield regular rational readback and a terminal real-name handoff. It sits after the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal seal of \autoref{ch:concrete-instances-real-namecert}, the interval nesting route of \autoref{ch:concrete-instances-nestedinterval-namecert}, and the located endpoint discipline of \autoref{ch:concrete-instances-locatedinterval-namecert}. The packet names a displayed nested-interval-to-real carrier; it is not a selected limit, quotient real equality, countable-choice branch, trichotomy principle, or ambient completeness theorem.

\begin{definition}[Dyadic nested interval carrier]\label{def:dyadic-nested-interval-choice-free-carrier}
A $\DyadicNestedIntervalChoiceFreeUp$ carrier is a finite $\BHist$ packet
$$
D=(L,U,J,K,S,A,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the finite row of dyadic left endpoints, classified through $\DyadicRatCoreUp$.
\item $U$ is the finite row of dyadic right endpoints, over the same endpoint window as $L$.
\item $J$ is the nesting row asserting that each displayed successor interval lies inside its predecessor.
\item $K$ is the located nonempty-window row, recording that each displayed interval has a certified dyadic inhabitance witness or overlap certificate internal to the packet.
\item $S$ is the shrink schedule row that assigns a displayed precision request to a finite interval stage.
\item $A$ is the diameter ledger, proving that the displayed dyadic width at the scheduled stage is bounded by the requested tolerance.
\item $W$ is the $\StreamNameUp$ read window that records the inspected finite stages.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the scheduled dyadic endpoints or midpoints over $W$.
\item $E$ is the $\RealUp$ handoff row exposed only after $L,U,J,K,S,A,W,R$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for endpoint, nesting, located-window, shrink, diameter, StreamName, RegSeqRat, Real handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a precision request through $L,U,J,K,S,A,W,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\DyadicNestedIntervalChoiceFreeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\NestedIntervalUp$, $\LocatedIntervalUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicNestedIntervalChoiceFreeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for a selected limit point, quotient real equality, countable-choice subsequence, external interval oracle, trichotomy split, or ambient completeness principle.
\end{definition}

\begin{theorem}[Dyadic nested interval NameCert obligations]\label{thm:dyadic-nested-interval-choice-free-namecert-obligations}
For every accepted $\DyadicNestedIntervalChoiceFreeUp$ carrier $D=(L,U,J,K,S,A,W,R,E,H,C,P,N)$, the local $\NameCert_{\DyadicNestedIntervalChoiceFreeUp}$ obligation surface is exactly endpoint admission through $L,U$, nesting through $J$, located nonempty-window exposure through $K$, shrink scheduling through $S$, diameter control through $A$, $\StreamNameUp$ windowing through $W$, $\RegSeqRatUp$ readback through $R$, $\RealUp$ handoff through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.

No downstream consumer may replace this surface by selected limits, quotient-real equality, a countable-choice branch, host interval completeness, trichotomy, or an external locatedness predicate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-nested-interval-choice-free-carrier}. The endpoint rows $L,U$ are the only dyadic data, while $J,K,S,A$ are the displayed interval, locatedness, schedule, and diameter rows attached to them.

Read the analytic handoff through $W,R,E$. The StreamName row exposes only inspected finite stages, the RegSeqRat row repacks those stages as regular rational readback, and the Real handoff appears only after those rows have been replayed.

Finally inspect $H,C,P,N$. Transport, replay, provenance, and local naming preserve the same finite route, so the NameCert surface has no coordinate for selected limits, quotient-real equality, countable choice, host completeness, trichotomy, or external locatedness.
\end{proof}

\begin{theorem}[Dyadic nested interval window induction]\label{thm:dyadic-nested-interval-choice-free-window-induction}
For every accepted $\DyadicNestedIntervalChoiceFreeUp$ carrier, finite induction on the displayed $\StreamNameUp$ window preserves the route
$$
L,U,J,K,S,A,W,R,E,H,C,P,N .
$$
The base window exposes one located dyadic interval with its diameter ledger, and the successor step refines the previous interval through the nesting row $J$ while keeping the same shrink schedule, readback, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Use induction on the finite window row $W$. In the base case, the first displayed interval is certified by $L,U,K$, and its width is bounded by the corresponding diameter row $A$.

For the successor step, append one inspected stage. The nesting row $J$ places the new dyadic interval inside the previous displayed interval, and the located row $K$ keeps the successor window nonempty inside the packet.

The schedule and readback rows are unchanged in kind. The row $S$ chooses the displayed stage for the requested tolerance, $A$ supplies the width bound at that stage, and $R$ records the same finite chain as regular rational readback.

The structural rows $H,C,P,N$ transport, replay, source, and name the whole route. Hence the induction never introduces a selected limit, quotient equality, external branch, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Dyadic nested interval Real handoff]\label{thm:dyadic-nested-interval-choice-free-real-handoff}
Every $\RealUp$ consumer of an accepted $\DyadicNestedIntervalChoiceFreeUp$ packet must first expose the dyadic endpoint rows $L,U$, nesting row $J$, located nonempty-window row $K$, shrink schedule $S$, diameter ledger $A$, $\StreamNameUp$ window $W$, and $\RegSeqRatUp$ readback row $R$. The terminal handoff $E$ is therefore a displayed regular-rational route to $\RealUp$, not a selected point of the intersection and not quotient-real equality.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadic-nested-interval-choice-free-namecert-obligations}. It makes $E$ downstream of the endpoint, nesting, located-window, schedule, diameter, StreamName, and RegSeqRat rows.

Use \autoref{thm:dyadic-nested-interval-choice-free-window-induction}. The finite windows used by $R$ are preserved across displayed stages, so the Real handoff reads a regular rational route rather than an external intersection selector.

Finally use the refusal part of the carrier definition. Since the accepted packet has no selected limit, quotient-real equality, countable-choice branch, trichotomy split, external interval oracle, or ambient completeness row, every $\RealUp$ consumer must take the displayed handoff $E$ through the finite route.
\end{proof}

\falsifiablePrediction{An accepted $\DyadicNestedIntervalChoiceFreeUp$ consumer that reaches a $\RealUp$ handoff without displaying dyadic endpoints, nesting, located nonempty windows, shrink schedule, diameter ledger, StreamName windows, and RegSeqRat readback refutes the packet surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, nestedinterval, locatedinterval: no listed sibling is bijective to $\DyadicNestedIntervalChoiceFreeUp$ because only this packet binds dyadic endpoints, nesting, located windows, shrink scheduling, diameter control, finite observation, regular rational readback, Real handoff, transport, replay, provenance, and local naming as one NameCert route.}

\closureat{DyadicNestedIntervalChoiceFreeUp}{seedStr}

\begin{closurestatus}{\DyadicNestedIntervalChoiceFreeUp}
  \constructivestory{A $\DyadicNestedIntervalChoiceFreeUp$ packet is generated from dyadic endpoint rows, nesting rows, located nonempty-window rows, a shrink schedule, a diameter ledger, StreamName windows, RegSeqRat readback, a RealUp handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-nested-interval-choice-free-carrier}, \autoref{thm:dyadic-nested-interval-choice-free-namecert-obligations}, \autoref{thm:dyadic-nested-interval-choice-free-window-induction}, and \autoref{thm:dyadic-nested-interval-choice-free-real-handoff} seed the finite dyadic nested-interval route from displayed windows to a Real handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected intersection points, quotient Real equality, countable choice, trichotomy, host interval completeness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint admission, nesting, located-window exposure, shrink scheduling, diameter control, StreamName window stability, RegSeqRat readback, Real handoff, transport, replay, provenance, and local naming for BEDC.Derived.DyadicNestedIntervalChoiceFreeUp.}
\end{closurestatus}
