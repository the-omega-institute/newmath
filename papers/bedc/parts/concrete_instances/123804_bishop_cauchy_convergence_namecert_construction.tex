\chapter{A Concrete Naming Certificate for $\BishopCauchyConvergenceUp$}
\label{ch:concrete-instances-bishop-cauchy-convergence-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopCauchyConvergenceUp$ packet records Cauchy convergence as a finite NameCert route rather than as an ambient completeness principle. It sits over the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the criterion surface of \autoref{ch:concrete-instances-bishop-cauchy-convergence-criterion-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The carrier names the constructive handoff from displayed tail windows to a Real-facing convergence seal; it does not assert quotient stream equality, selected limits, countable choice, trichotomy, host real completeness, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop Cauchy convergence carrier]
\label{def:bishop-cauchy-convergence-carrier}
A $\BishopCauchyConvergenceUp$ carrier is a finite $\BHist$ packet
$$
K=(S,R,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row, carrying the inspected tail windows and requested precisions.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, displaying the common dyadic radii, split budgets, and endpoint comparison rows used by the convergence read.
\item $Q$ is the Bishop Cauchy convergence row, recording that every displayed tail request is met by the same $S,R,D$ route.
\item $E$ is the terminal $\RealUp$ seal row, reachable only after the window, regular readback, tolerance, and convergence rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for the window, readback, tolerance, convergence, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S \longmapsto R \longmapsto D \longmapsto Q \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopCauchyConvergenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BishopCauchyConvergenceCriterionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCauchyConvergenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,D,Q,E,H,C,P,N$.
\end{definition}

\begin{theorem}[Bishop Cauchy convergence NameCert obligations]
\label{thm:bishop-cauchy-convergence-namecert-obligations}
For every accepted $\BishopCauchyConvergenceUp$ carrier
$$
K=(S,R,D,Q,E,H,C,P,N),
$$
the local $\NameCert_{\BishopCauchyConvergenceUp}$ surface consists exactly of tail-window admission through $S$, regular rational readback through $R$, dyadic tolerance accounting through $D$, Bishop Cauchy convergence routing through $Q$, terminal Real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-cauchy-convergence-carrier}. The analytic rows are $S,R,D,Q,E$, and the structural rows are $H,C,P,N$.

The finite windows in $S$ are read before the regular rational row $R$ and dyadic tolerance row $D$. Thus a convergence read cannot start from a quotient stream class or an uninspected tail.

The convergence row $Q$ is local to the displayed route. It binds each requested precision to the already named windows, regular readback, and dyadic budget rather than to a selected limit or a host convergence theorem.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name only those rows. Since no carrier coordinate stores quotient equality, countable choice, trichotomy, selected limits, host completeness, or ambient $\RealUp$ completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop Cauchy convergence RegSeqRat--Real route]
\label{thm:bishop-cauchy-convergence-regseqrat-real-route}
Every $\RealUp$-facing consumer of an accepted $\BishopCauchyConvergenceUp$ packet must pass through
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \BishopCauchyConvergenceCriterionUp \longmapsto \RealUp .
$$
The route exposes finite StreamName windows, RegSeqRat readback, dyadic tolerance rows, a Bishop Cauchy convergence row, and a terminal Real seal. It exports no selected limit, quotient equality of Cauchy streams, countable-choice selector, trichotomy row, host real completeness, or ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:bishop-cauchy-convergence-namecert-obligations}. They place the finite window row before the regular rational readback, the dyadic tolerance ledger, and the convergence row.

The terminal Real seal is downstream of those rows. A consumer therefore receives $E$ only after the finite windows, regular rational data, dyadic budgets, and convergence route have been displayed.

The excluded principles are absent from the carrier. Projection of $S,R,D,Q,E,H,C,P,N$ gives no coordinate from which to read a selected limit, quotient stream equality, countable-choice selector, trichotomy classifier, host completeness theorem, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopCauchyConvergenceUp$ real-facing read exposes finite StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a Bishop Cauchy convergence row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, bishop-cauchy-convergence-criterion, real: $\BishopCauchyConvergenceUp$ is not bijective to any listed sibling because it jointly carries finite tail windows, regular rational readback, dyadic tolerance accounting, convergence routing, Real sealing, transport, replay, provenance, and local NameCert rows.}

\closureat{BishopCauchyConvergenceUp}{seedStr}

\begin{closurestatus}{\BishopCauchyConvergenceUp}
  \constructivestory{A $\BishopCauchyConvergenceUp$ packet is generated from StreamName finite windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a Bishop Cauchy convergence row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-cauchy-convergence-carrier}, \autoref{thm:bishop-cauchy-convergence-namecert-obligations}, and \autoref{thm:bishop-cauchy-convergence-regseqrat-real-route} seed the finite convergence route through StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, Bishop convergence routing, Real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected limits, countable choice, trichotomy, host real completeness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window-readback exactness, dyadic tolerance coverage, convergence nonescape, Real seal discipline, transport, replay, provenance, and local naming for BEDC.Derived.BishopCauchyConvergenceUp.}
\end{closurestatus}
