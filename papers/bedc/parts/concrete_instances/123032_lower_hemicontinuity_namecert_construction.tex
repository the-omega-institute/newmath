\chapter{A Concrete Naming Certificate for $\LowerHemicontinuityUp$}
\label{ch:concrete-instances-lower-hemicontinuity-namecert}
\origin{human}

Following Aubin--Frankowska and Berge on open-hit continuity for correspondences, the $\LowerHemicontinuityUp$ packet records the finite BEDC companion to the compact closed-graph route of \autoref{ch:concrete-instances-upperhemicontinuity-namecert}. It reads the multifunction carrier of \autoref{ch:concrete-instances-set-valued-map-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the selection-facing consumer surface of \autoref{ch:concrete-instances-michael-selection-namecert}. The packet names an open-hit correspondence boundary: finite source windows, target-open witness rows, fibre-hit ledgers, transport, replay, provenance, and local naming. It is not a host powerset-valued function, not pointwise choice of a value, not upper hemicontinuity, and not a selected continuous Michael selector.

\begin{definition}[Lower hemicontinuity carrier]
\label{def:lower-hemicontinuity-carrier}
A $\LowerHemicontinuityUp$ carrier is a finite $\BHist$ packet
$$
L=(M,X,Y,O,F,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\SetValuedMapUp$ correspondence row whose graph and fibre ledgers provide the source relation.
\item $X$ is the $\CompactMetricUp$ or metric source row whose finite probes name the domain windows.
\item $Y$ is the $\RealUp$ target seal through which value coordinates and open targets are read.
\item $O$ is the finite open-target row naming a displayed target-open window that is required to hit the value fibre.
\item $F$ is the fibre-hit ledger: it records that the displayed fibre over the current source probe meets the open target row $O$.
\item $Q$ is the lower-hit stability row: after a permitted source-window refinement, the packet displays a refined fibre-hit ledger for the same open target row.
\item $H$ records componentwise $\hsame$ transport for $M,X,Y,O,F,Q$ and the structural rows.
\item $C$ records displayed $\Cont$ replay from the correspondence row through source windows, target-open rows, fibre-hit ledgers, lower-hit stability, and consumer handoff.
\item $P$ is the $\Pkg$ provenance over $\LowerHemicontinuityUp$, $\SetValuedMapUp$, $\UpperHemicontinuityUp$, $\MichaelSelectionUp$, $\CompactMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LowerHemicontinuityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,X,Y,O,F,Q,H,C,P,N$. It has no coordinate for a host correspondence object, an ambient powerset, selected values, closed-graph readback, quotient equality of real names, or a global selection theorem.
\end{definition}

\begin{theorem}[Lower hemicontinuity NameCert obligations]
\label{thm:lower-hemicontinuity-namecert-obligations}
For every accepted $\LowerHemicontinuityUp$ carrier $L=(M,X,Y,O,F,Q,H,C,P,N)$, the local $\NameCert_{\LowerHemicontinuityUp}$ surface consists exactly of carrier admission, set-valued-map source exposure through $M$, compact or metric source-window exposure through $X$, real target sealing through $Y$, target-open exposure through $O$, fibre-hit exposure through $F$, lower-hit stability through $Q$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:lower-hemicontinuity-carrier}. The tuple $L=(M,X,Y,O,F,Q,H,C,P,N)$ is the whole packet, so the source correspondence, source-window row, target seal, open-target row, fibre-hit row, stability row, and structural rows are all visible.

The lower side is read through $O,F,Q$. The row $O$ names the open target window, $F$ records that the displayed fibre hits it, and $Q$ records the permitted refined source-window hit for the same target-open row.

Finally inspect $H,C,P,N$. These rows transport, replay, source, and name the same finite route. Since the carrier contains no host multifunction, powerset, selected value, closed-graph readback, quotient real equality, or global selection coordinate, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Lower hemicontinuity open-hit stability]
\label{thm:lower-hemicontinuity-open-hit-stability}
Every open-hit consumer of an accepted $\LowerHemicontinuityUp$ packet must read the finite route
$$
\SetValuedMapUp \longmapsto \CompactMetricUp \longmapsto \RealUp \longmapsto O \longmapsto F \longmapsto Q \longmapsto H,C,P,N .
$$
In particular, the $\MichaelSelectionUp$ consumer receives lower-hit data only after the correspondence row, source-window row, target seal, open-target witness, fibre-hit ledger, and lower-hit stability row have been displayed.
\end{theorem}
\begin{proof}
Start with an accepted carrier $L=(M,X,Y,O,F,Q,H,C,P,N)$. The correspondence row $M$, source row $X$, and target seal $Y$ are displayed before any open-hit row can be read.

The open-hit boundary is the pair $O,F$ together with the stability row $Q$. Thus a consumer sees a target-open row and a fibre-hit ledger, then the refined source-window hit for the same open target.

Apply the obligation inventory of \autoref{thm:lower-hemicontinuity-namecert-obligations}. The structural rows keep the same route under componentwise transport and replay, so $\MichaelSelectionUp$ receives a finite lower-hit surface rather than a host selection principle or a closed-graph replacement.
\end{proof}

\falsifiablePrediction{Every accepted $\LowerHemicontinuityUp$ consumer read factors through a SetValuedMapUp correspondence row, source-window row, RealUp target seal, target-open row, fibre-hit ledger, lower-hit stability row, componentwise transport, displayed replay, provenance, and local NameCert row.}

\independenceWitness{set-valued-map, upper-hemicontinuity, michael-selection, compactmetric, real: $\LowerHemicontinuityUp$ is not bijective to any listed sibling because it binds an open-hit row and a lower-hit stability row to the correspondence route, while the upper packet records compact closed-graph readback and the Michael packet records the downstream selection surface.}

\closureat{LowerHemicontinuityUp}{seedStr}

\begin{closurestatus}{\LowerHemicontinuityUp}
  \constructivestory{A $\LowerHemicontinuityUp$ packet is generated from a set-valued-map correspondence row, source-window row, RealUp target seal, target-open row, fibre-hit ledger, lower-hit stability row, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:lower-hemicontinuity-carrier}, \autoref{thm:lower-hemicontinuity-namecert-obligations}, and \autoref{thm:lower-hemicontinuity-open-hit-stability} seed the finite open-hit correspondence surface over SetValuedMap, CompactMetric, Real, UpperHemicontinuity, and MichaelSelection consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host multifunction semantics, pointwise value selection, upper hemicontinuity, global Michael selection, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, open-target exposure, fibre-hit stability, consumer handoff, transport and replay stability, provenance exhaustion, and TasteGate field faithfulness for BEDC.Derived.LowerHemicontinuityUp.}
\end{closurestatus}
