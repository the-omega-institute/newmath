\chapter{A Concrete Naming Certificate for $\CauchyCompletionDensitySourceUp$}
\label{ch:concrete-instances-cauchy-completion-density-source-namecert}
\origin{human}

Following Bishop and Bridges and Kelley on constructive completions and dense embeddings, the $\CauchyCompletionDensitySourceUp$ packet records the finite dense-source face used by the Cauchy completion universal-property route. It sits between the universal-property surface of \autoref{ch:concrete-instances-cauchy-completion-universal-property-namecert}, the metric dense-image packet of \autoref{ch:concrete-instances-metric-completion-dense-embedding-namecert}, the regular-Cauchy dense route of \autoref{ch:concrete-instances-regular-cauchy-dense-embedding-namecert}, the Real admission route of \autoref{ch:concrete-instances-real-cauchy-dense-embedding-namecert}, the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the stream-window source of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet lets universal-property, completion-functor, and Real-completion consumers cite a dense source without importing quotient completion, selected limits, countable choice, or ambient real completeness.

\begin{definition}[CauchyCompletionDensitySource carrier]
\label{def:cauchy-completion-density-source-carrier}
A $\CauchyCompletionDensitySourceUp$ carrier is a finite $\BHist$ packet
$$
D=(U,M,J,R,W,Q,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\CauchyCompletionUniversalPropertyUp$ consumer row that reads the dense source before any extension or uniqueness row is available.
\item $M$ is the $\MetricCompletionDenseEmbeddingUp$ row, exposing the source metric, completion carrier, dense image, separated comparison, uniform handoff, and regular rational readback used by metric completion consumers.
\item $J$ is the $\RegularCauchyDenseEmbeddingUp$ row, recording the regular Cauchy rational source-to-completion route.
\item $R$ is the $\RealCauchyDenseEmbeddingUp$ row, recording the admission of the same regular Cauchy source into the $\RealUp$ seal.
\item $W$ is the $\RegSeqRatUp$ source row, carrying the regular rational name and its displayed modulus discipline.
\item $Q$ is the $\StreamNameUp$ finite-window row used to inspect the source only at requested stages.
\item $S$ is the $\DyadicRatCoreUp$ tolerance row, recording precision budgets, slack rows, and finite comparison data before the $\RealUp$ seal is read.
\item $H$ records componentwise $\hsame$ transport for $U,M,J,R,W,Q,S,C,P,N$.
\item $C$ records displayed $\Cont$ replay from dense-source requests through the metric dense image, regular Cauchy route, Real admission route, source windows, dyadic tolerance ledger, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyCompletionDensitySourceUp$, the eight sibling chapters named above, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCompletionDensitySourceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows and their replay paths. It has no coordinate for quotient representatives, selected completion limits, selected real representatives, countable choice, trichotomy, host equality of completion classes, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CauchyCompletionDensitySource NameCert obligations]
\label{thm:cauchy-completion-density-source-namecert-obligations}
For every accepted $\CauchyCompletionDensitySourceUp$ carrier $D=(U,M,J,R,W,Q,S,H,C,P,N)$, the local $\NameCert_{\CauchyCompletionDensitySourceUp}$ surface consists exactly of carrier admission for the universal-property consumer row, metric dense-image row, regular Cauchy dense route, Real admission route, RegSeqRat source, StreamName finite windows, DyadicRatCore tolerance ledger, componentwise transport, displayed replay, provenance, and local naming. A completion universal-property consumer must read $M,J,R,W,Q,S$ as dense-source evidence before it can consume the extension and uniqueness rows of $U$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-density-source-carrier}. The tuple displays the universal-property consumer row, metric dense-image row, regular Cauchy dense route, Real admission route, regular rational source, stream windows, dyadic tolerance ledger, transport, replay, provenance, and local naming row.

The dense-source evidence is finite and source-facing. The metric dense-image row supplies the completion-side dense embedding, the regular Cauchy route supplies the rational source discipline, the Real admission route records the terminal seal, and the RegSeqRat, StreamName, and DyadicRatCore rows expose only requested source observations and tolerance budgets.

The universal-property row $U$ is downstream of those rows. Its extension and uniqueness reads therefore consume displayed dense-source evidence rather than quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyCompletionDensitySource universal route]
\label{thm:cauchy-completion-density-source-universal-route}
Every universal-property, completion-functor, or $\RealUp$-completion consumer of an accepted $\CauchyCompletionDensitySourceUp$ packet factors through the finite route
$$
\MetricCompletionDenseEmbeddingUp,\ \RegularCauchyDenseEmbeddingUp,\ \RealCauchyDenseEmbeddingUp,\ \RegSeqRatUp,\ \StreamNameUp,\ \DyadicRatCoreUp \longmapsto \CauchyCompletionUniversalPropertyUp .
$$
The exported object is a dense-source packet. It is not a quotient completion, a selected limit operator, a countable-choice principle, a host equality comparison, or an ambient real-completeness theorem.
\end{theorem}
\begin{proof}
Begin with the local obligations of \autoref{thm:cauchy-completion-density-source-namecert-obligations}. Any consumer reaching the universal-property row must first read the metric dense image, regular Cauchy dense route, Real admission route, regular rational source, finite stream windows, and dyadic tolerance ledger.

The sibling packets determine the route. The metric dense-image row gives completion-facing density, the regular Cauchy dense route gives the source discipline, the Real admission route gives the seal handoff, and the RegSeqRat, StreamName, and DyadicRatCore rows give finite observation and tolerance data.

Transport, replay, provenance, and local naming preserve the same displayed route. Since the carrier contains no row for quotient representatives, selected limits, countable choice, host equality, or ambient real completeness, the universal route exports only the dense-source packet.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyCompletionDensitySourceUp$ consumer that reaches a universal-property extension or uniqueness read while omitting the metric dense image, regular Cauchy dense route, Real admission route, RegSeqRat source, StreamName windows, DyadicRatCore tolerance ledger, transport, replay, provenance, or local naming row refutes the packet.}

\independenceWitness{cauchy-completion-universal-property, metric-completion-dense-embedding, regular-cauchy-dense-embedding, real-cauchy-dense-embedding, regseqrat, streamname, dyadicratcore, real: no listed sibling is bijective with $\CauchyCompletionDensitySourceUp$ because the packet jointly carries dense-source evidence, universal-property consumption, completion-facing dense image data, regular rational source discipline, finite windows, dyadic tolerance accounting, Real-seal handoff, transport, replay, provenance, and local naming.}

\closureat{CauchyCompletionDensitySourceUp}{seedStr}

\begin{closurestatus}{\CauchyCompletionDensitySourceUp}
  \constructivestory{A $\CauchyCompletionDensitySourceUp$ packet is generated from a universal-property consumer row, a metric dense-image row, a regular Cauchy dense route, a Real admission route, a RegSeqRat source row, finite StreamName windows, a DyadicRatCore tolerance ledger, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-completion-density-source-carrier}, \autoref{thm:cauchy-completion-density-source-namecert-obligations}, and \autoref{thm:cauchy-completion-density-source-universal-route} seed the dense-source face consumed by constructive Cauchy completion universal-property routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion, selected representatives, selected limits, countable choice, trichotomy, host equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-source exactness, metric dense-image handoff, regular Cauchy source readback, Real-seal handoff, finite-window and tolerance accounting, transport replay, provenance non-escape, and TasteGate field faithfulness for BEDC.Derived.CauchyCompletionDensitySourceUp.}
\end{closurestatus}
