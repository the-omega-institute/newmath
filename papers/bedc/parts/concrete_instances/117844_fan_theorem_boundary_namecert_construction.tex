\chapter{A Concrete Naming Certificate for $\FanTheoremBoundaryUp$}
\label{ch:concrete-instances-fan-theorem-boundary-namecert}
\origin{human}

Following Brouwer and Bishop--Bridges finite-fan analysis, the $\FanTheoremBoundaryUp$ packet records the L10 boundary row that lets finite fan and bar evidence be consumed as displayed BEDC data before any compactness theorem is read. It sits beside the finite prefix-bar certificate of \autoref{ch:concrete-instances-finite-fan-theorem-namecert}, the finite fan-bar modulus packet of \autoref{ch:concrete-instances-finite-fan-bar-modulus-namecert}, the stream-window carrier of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names the boundary object that prevents finite bar evidence from being re-read as open-cover compactness, a chosen branch, countable choice, quotient stream equality, a selected limit, or ambient $\RealUp$ completeness.

\begin{definition}[Fan theorem boundary carrier]
\label{def:fan-theorem-boundary-carrier}
A $\FanTheoremBoundaryUp$ carrier is a finite $\BHist$ packet
$$
F=(T,B,S,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\FiniteFanTheoremUp$ finite prefix-bar row, exposing the admitted fan depth and the retained branch-prefix window.
\item $B$ is the $\FiniteFanBarModulusUp$ bar-modulus row, exposing the finite lower-bound handoff available to uniform-modulus consumers.
\item $S$ is the $\StreamNameUp$ observation-window row read by downstream Real and Completion consumers.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row attached to the finite prefix and bar windows.
\item $Q$ is the $\RegSeqRatUp$ readback row that keeps rational sequence data inside the displayed finite window.
\item $E$ is the terminal $\RealUp$ seal row for any real-name comparison reached from the finite bar data.
\item $H$ records componentwise $\hsame$ transport for $T,B,S,D,Q,E$ and the local packet shape.
\item $C$ records displayed $\Cont$ replay from finite fan evidence through the bar-modulus row and into the analytic readback rows.
\item $P$ is the $\Pkg$ provenance over $\FanTheoremBoundaryUp$, $\FiniteFanTheoremUp$, $\FiniteFanBarModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FanTheoremBoundaryUp}$ row.
\end{enumerate}
The carrier has no coordinate for open-cover compactness, selected branches, countable choice, quotient stream equality, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Fan theorem boundary NameCert obligations]
\label{thm:fan-theorem-boundary-namecert-obligations}
For every accepted $\FanTheoremBoundaryUp$ carrier
$$
F=(T,B,S,D,Q,E,H,C,P,N),
$$
the local $\NameCert_{\FanTheoremBoundaryUp}$ surface consists exactly of finite prefix-bar admission through $T$, finite fan-bar modulus admission through $B$, stream-window exposure through $S$, dyadic tolerance exposure through $D$, regular-rational readback through $Q$, real-name sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.

Thus a Real or Completion consumer may use the packet only after the finite fan theorem row, finite bar-modulus row, observation window, tolerance row, rational readback row, real seal, transport, replay, provenance, and local naming rows have all been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-theorem-boundary-carrier}. The tuple displays exactly the finite fan theorem row, finite fan-bar modulus row, stream window, dyadic tolerance row, regular-rational readback row, real seal, transport, replay, provenance, and local naming row.

The fan and bar evidence is finite because $T$ and $B$ are the only rows through which branch-facing information enters the packet. The row $T$ exposes a retained prefix window, while $B$ exposes the lower-bound handoff available after the finite bar rows have been inspected.

The analytic readback is downstream of those finite rows. The rows $S,D,Q,E$ record the observation window, tolerance ledger, rational readback, and terminal real seal; none of them contains a selected branch or an open-cover subcover.

Finally $H,C,P,N$ transport, replay, source, and name the same displayed route. Since the carrier has no coordinate for open-cover compactness, selected branches, countable choice, quotient stream equality, selected limits, or ambient real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Fan theorem boundary uniform-bar readback]
\label{thm:fan-theorem-boundary-uniform-bar-readback}
Every Real or Completion read of an accepted $\FanTheoremBoundaryUp$ packet factors through
$$
\FiniteFanTheoremUp \longrightarrow \FiniteFanBarModulusUp \longrightarrow \StreamNameUp \longrightarrow \DyadicRatCoreUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp
$$
and then through $H,C,P,N$. The readback is a boundary read: it may consume the finite bar row and the finite modulus handoff, but it exports no open-cover compactness, selected branch, countable-choice functional, quotient stream equality, selected limit, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:fan-theorem-boundary-namecert-obligations}. They force every accepted consumer to expose $T$ before $B$, because the bar-modulus handoff is meaningful only after the finite prefix-bar row has been displayed.

After $T$ and $B$ are visible, the read proceeds through the analytic rows $S,D,Q,E$. The stream window supplies the finite observation schedule, the dyadic row supplies the tolerance ledger, the regular-rational row supplies sequence readback, and the real seal records the terminal comparison.

The structural rows $H,C,P,N$ do not add new analytic principles. They preserve and name the same finite route under componentwise transport, displayed replay, provenance, and local NameCert packaging.

Therefore the read remains a boundary read. It carries finite fan and bar evidence into Real and Completion consumers, while the excluded compactness, selection, quotient, choice, limit, and completeness data remain outside the carrier.
\end{proof}

\falsifiablePrediction{Every $\FanTheoremBoundaryUp$ read factors through one $\FiniteFanTheoremUp$ prefix-bar row, one $\FiniteFanBarModulusUp$ finite bar-modulus row, one $\StreamNameUp$ observation window, one $\DyadicRatCoreUp$ tolerance row, one $\RegSeqRatUp$ readback row, one terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{finite fan theorem, finite fan-bar modulus, streamname, dyadicratcore, regseqrat, real: $\FanTheoremBoundaryUp$ is not bijective to any listed sibling because it jointly carries the boundary between finite fan/bar evidence and Real-or-Completion readback while refusing compactness, selection, quotient, choice, limit, and completeness data.}

\closureat{FanTheoremBoundaryUp}{seedStr}

\begin{closurestatus}{\FanTheoremBoundaryUp}
  \constructivestory{A $\FanTheoremBoundaryUp$ packet is generated from finite fan theorem rows, finite fan-bar modulus rows, StreamName windows, DyadicRatCore tolerances, RegSeqRat readbacks, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fan-theorem-boundary-carrier}, \autoref{thm:fan-theorem-boundary-namecert-obligations}, and \autoref{thm:fan-theorem-boundary-uniform-bar-readback} seed the L10 boundary route from finite fan and bar evidence into Real and Completion readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected branches, countable choice, quotient stream equality, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite fan theorem exposure, finite bar-modulus exposure, stream-window coverage, dyadic tolerance exactness, rational readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.FanTheoremBoundaryUp.}
\end{closurestatus}
