\chapter{A Concrete Naming Certificate for $\HeineBorelFiniteIntervalUp$}
\label{ch:concrete-instances-heine-borel-finite-interval-namecert}
\origin{human}

Following Heine, Borel, and Bishop--Bridges constructive analysis, the $\HeineBorelFiniteIntervalUp$ packet records the finite-subcover boundary available for a displayed located real interval after the cover has already been reduced to finite dyadic interval data. It sits beside the compact interval route of \autoref{ch:concrete-instances-bishop-compact-interval-namecert}, the dyadic interval-cover packet of \autoref{ch:concrete-instances-dyadic-interval-cover-namecert}, the finite closed-bounded interval compactness surface of \autoref{ch:concrete-instances-closed-bounded-interval-compact-namecert}, and the L10 Real and Completion route through $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$. The packet names a constructive finite interval subcover interface; it does not claim general open-cover compactness, a choice-selected subcover, quotient real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Heine--Borel finite-interval carrier]
\label{def:heine-borel-finite-interval-carrier}
A $\HeineBorelFiniteIntervalUp$ carrier is a finite $\BHist$ packet
$$
K=(I,D,B,C,S,Q,T,E,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\LocatedRealIntervalUp$ source row, exposing lower and upper endpoint names, locatedness data, and interval-radius requests;
\item $D$ is the $\DyadicIntervalCoverUp$ finite cover row, recording dyadic cells, mesh scale, membership checks, and cover-read witnesses;
\item $B$ is the $\BishopCompactIntervalUp$ compact-interval boundary row used only as the finite constructive interval route of \autoref{ch:concrete-instances-bishop-compact-interval-namecert};
\item $C$ is the $\ClosedBoundedIntervalCompactUp$ row, supplying the closed-bounded interval compactness handoff after finite cover data is displayed;
\item $S$ is the $\StreamNameUp$ finite-window row attached to endpoint and cover observations;
\item $Q$ is the $\RegSeqRatUp$ readback row for the same observed windows;
\item $T$ is the $\DyadicRatCoreUp$ tolerance and mesh-refinement ledger;
\item $E$ is the terminal $\RealUp$ seal reached only after the interval, cover, mesh, window, and readback rows have been projected;
\item $H$ records componentwise $\hsame$ transport for all displayed rows;
\item $R$ records displayed $\Cont$ replay for interval admission, cover readback, dyadic refinement, finite subcover witness, and Real-seal handoff;
\item $P$ is the $\Pkg$ provenance over $\HeineBorelFiniteIntervalUp$, $\LocatedRealIntervalUp$, $\DyadicIntervalCoverUp$, $\BishopCompactIntervalUp$, $\ClosedBoundedIntervalCompactUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\HeineBorelFiniteIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for arbitrary open covers, countable choice, selected host representatives, quotient equality of real names, or ambient real completeness.
\end{definition}

\begin{theorem}[Heine--Borel finite-interval NameCert obligations]
\label{thm:heine-borel-finite-interval-namecert-obligations}
For every accepted $\HeineBorelFiniteIntervalUp$ carrier $K=(I,D,B,C,S,Q,T,E,H,R,P,N)$, the local $\NameCert_{\HeineBorelFiniteIntervalUp}$ surface consists exactly of five finite rows: interval-source admission through $I$; finite cover admission through $D$; dyadic mesh refinement through $T$ with the $\StreamNameUp$ and $\RegSeqRatUp$ rows $S,Q$; finite subcover witness readback through the $\BishopCompactIntervalUp$ and $\ClosedBoundedIntervalCompactUp$ rows $B,C$; and Real-seal nonescape through $E,H,R,P,N$. No row exports general open-cover compactness, a choice-selected subcover, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The source interval row is exactly $I$, so endpoint, radius, locatedness, and containment requests begin with the displayed $\LocatedRealIntervalUp$ packet rather than with a host interval.

The finite cover and mesh rows are $D$ and $T$. The cover cells, mesh scale, and membership checks are therefore read through $\DyadicIntervalCoverUp$ and $\DyadicRatCoreUp$, with $S,Q$ supplying only the finite window and regular-rational readback needed to inspect the same observations.

The finite subcover witness is obtained only after the compact-interval boundary rows $B,C$ have been displayed. These rows are the constructive compact interval surfaces cited above; they do not add an arbitrary open cover or a choice functional.

Finally read the seal and structural rows. The terminal $\RealUp$ row $E$ is reached only after $I,D,B,C,S,Q,T$ have been projected, and $H,R,P,N$ transport, replay, provenance, and name that same finite packet. Since the carrier has no coordinate for the excluded principles, the five listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Heine--Borel finite-interval dyadic mesh obligation]
\label{thm:heine-borel-finite-interval-interval-cover-mesh}
For every accepted $\HeineBorelFiniteIntervalUp$ carrier $K=(I,D,B,C,S,Q,T,E,H,R,P,N)$, the interval-cover mesh obligation is read only through the displayed located interval endpoints in $I$, the finite dyadic cover cells in $D$, and the $\DyadicRatCoreUp$ mesh ledger $T$. The $\StreamNameUp$ and $\RegSeqRatUp$ rows $S,Q$ may report the endpoint windows used by the mesh, but the obligation exports no arbitrary open-cover refinement, no host interval cover, no quotient real equality, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project $I$ first. The closed interval endpoints are available only as displayed located real names, so every endpoint read begins inside the $\LocatedRealIntervalUp$ row rather than in a host interval object.

Next project $D$ and $T$. The cover cells, mesh scale, and dyadic comparisons are finite rows, and the mesh refinement is certified by the $\DyadicRatCoreUp$ ledger attached to those rows.

Finally use $S,Q$ only as the observation route for the same endpoint and cell windows. Because the carrier contains no coordinate for an arbitrary open cover or quotient real equality, the mesh obligation remains a finite dyadic-cover obligation over the displayed interval.
\end{proof}

\begin{theorem}[Heine--Borel finite-interval subcover witness obligation]
\label{thm:heine-borel-finite-interval-subcover-witness}
For every accepted $\HeineBorelFiniteIntervalUp$ carrier $K=(I,D,B,C,S,Q,T,E,H,R,P,N)$, a finite subcover witness may be consumed only after the finite cover row $D$, the compact-interval handoff rows $B,C$, and the displayed witness rows carried through $\mathsf{ProbeBundle}$, $\CompactMetricUp$, and $\TotallyBoundedUp$-style finite data have been exposed by the same $\BHist$ packet. The witness is a finite row selection inside the packet, not a countable-choice selector, not an arbitrary open-cover subcover, and not a host compactness theorem.
\end{theorem}
\begin{proof}
Start with the finite cover row $D$. It lists the cover cells and membership checks already reduced to finite dyadic interval data, so the witness has a finite source to inspect.

Read the compact-interval handoff rows $B,C$ next. These rows provide the constructive compact and closed-bounded interval surfaces used by the carrier, and they keep the witness attached to the same interval packet.

The probe, compact-metric, and totally-bounded reads are therefore finite rows inside the displayed $\BHist$. The structural rows $H,R,P,N$ transport, replay, source, and name that finite witness, while the carrier still lacks any coordinate for countable choice or arbitrary open-cover compactness.
\end{proof}

\begin{theorem}[Heine--Borel finite-interval Real-seal nonescape]
\label{thm:heine-borel-finite-interval-real-seal}
Every $\RealUp$-facing read of an accepted $\HeineBorelFiniteIntervalUp$ carrier must display $I,D,B,C,S,Q,T,H,R,P,N$ before it reads the terminal seal row $E$. The seal records that the finite interval subcover route is Real-facing, but it does not claim arbitrary subset Heine--Borel compactness, general open-cover compactness, choice-selected subcovers, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the carrier in the order of \autoref{def:heine-borel-finite-interval-carrier}. The interval, cover, compact handoff, window, readback, and dyadic mesh rows are all displayed before $E$ is available.

The transport and replay rows preserve that same finite route. They allow the interval-cover and subcover-witness rows to be moved componentwise, but they do not create an arbitrary cover family or a selected subcover functional.

Finally inspect the provenance and local certificate. The $\Pkg$ and $\NameCert$ rows name only the displayed finite interval packet, so the terminal $\RealUp$ seal is a nonescape boundary rather than a completeness or open-cover compactness principle.
\end{proof}

\closureat{HeineBorelFiniteIntervalUp}{seedStr}

\begin{closurestatus}{\HeineBorelFiniteIntervalUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:heine-borel-finite-interval-carrier}, \autoref{thm:heine-borel-finite-interval-namecert-obligations}, \autoref{thm:heine-borel-finite-interval-interval-cover-mesh}, \autoref{thm:heine-borel-finite-interval-subcover-witness}, and \autoref{thm:heine-borel-finite-interval-real-seal} seed the finite interval subcover boundary over $\LocatedRealIntervalUp$, $\DyadicIntervalCoverUp$, $\BishopCompactIntervalUp$, $\ClosedBoundedIntervalCompactUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim general open-cover compactness, arbitrary Heine--Borel compactness for subsets of $\RealUp$, choice-selected subcovers, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked interval-source admission, finite cover admission, dyadic mesh refinement, finite subcover witness readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.HeineBorelFiniteIntervalUp.}
  \constructivestory{A $\HeineBorelFiniteIntervalUp$ packet is generated from a LocatedRealInterval row, a DyadicIntervalCover finite cover row, BishopCompactInterval and ClosedBoundedIntervalCompact handoff rows, StreamName windows, RegSeqRat readback, a DyadicRatCore mesh ledger, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
