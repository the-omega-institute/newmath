\chapter{A Concrete Naming Certificate for $\BoundedRealUniformContinuityModulusUp$}
\label{ch:concrete-instances-bounded-real-uniform-continuity-modulus-namecert}
\origin{human}

Following Bishop and Bridges and the Heine--Cantor theorem for compact intervals, the $\BoundedRealUniformContinuityModulusUp$ packet records the finite modulus surface for a displayed continuous map on a bounded real interval. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the pointwise continuity rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet names the bounded Real and Completion modulus route; it is not a host Heine--Cantor theorem, an open-cover compactness principle, a quotient of real functions, a selected subcover, or ambient $\RealUp$ completeness.

\begin{definition}[Bounded real uniform-continuity modulus carrier]
\label{def:bounded-real-uniform-continuity-modulus}
A $\BoundedRealUniformContinuityModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(I,K,F,U,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the bounded $\RealUp$ interval row, recording endpoints, membership, and the displayed closed-bounded interval source;
\item $K$ is the $\CompactMetricUp$ finite-net row over the same interval source, with a finite family of centers and mesh histories;
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row for the displayed map on the interval;
\item $U$ is the $\UniformModulusUp$ extraction row obtained only after the finite-net and pointwise-modulus rows have been replayed;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording positive target tolerance, center radii, mesh bounds, slack, and the exported source precision;
\item $E$ is the terminal $\RealUp$ seal row for all endpoint, value, and tolerance reads used by the packet;
\item $H$ records componentwise $\hsame$ transport for the interval, finite-net, pointwise-modulus, uniform-modulus, dyadic-ledger, real-seal, provenance, and naming rows;
\item $C$ records displayed $\Cont$ replay routes for reading the same rows in consumer order;
\item $P$ is the $\Pkg$ provenance over $\BoundedRealUniformContinuityModulusUp$, $\RealUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\BoundedRealUniformContinuityModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for host compactness, open-cover compactness, a selected subcover, quotient equality of functions, trichotomy of reals, countable choice, ambient $\RealUp$ completeness, or a detached semantic continuity theorem.
\end{definition}

\begin{theorem}[Bounded real uniform-continuity modulus NameCert obligations]
\label{thm:bounded-real-uniform-continuity-modulus-namecert-obligations}
For the carrier of \autoref{def:bounded-real-uniform-continuity-modulus}, the local $\NameCert_{\BoundedRealUniformContinuityModulusUp}$ surface consists exactly of carrier admission for
$$
M=(I,K,F,U,D,E,H,C,P,N),
$$
bounded real interval exposure through $I$, compact finite-net exposure through $K$, $\ContinuousMapUp$ pointwise-modulus exposure through $F$, $\UniformModulusUp$ extraction through $U$, dyadic tolerance exactness through $D$, terminal $\RealUp$ sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet $M$. The rows $I,K,F,U,D,E,H,C,P,N$ list the bounded interval, finite-net source, pointwise continuity data, uniform-modulus extraction, tolerance ledger, Real seal, transports, replay routes, provenance, and local name.

The positive modulus route is finite. A consumer first reads $I$ as the bounded $\RealUp$ interval, then $K$ as the finite compact-metric net over that source, then $F$ as the pointwise $\ContinuousMapUp$ modulus row at the displayed centers.

The output row $U$ is exposed only after the dyadic ledger $D$ records the target tolerance, mesh, slack, and source precision, and after $E,H,C,P,N$ seal, transport, replay, source, and name those rows. Since no further coordinate is present, the local NameCert surface is exhausted by the displayed packet.
\end{proof}

\begin{theorem}[Bounded real uniform-continuity modulus consumer handoff]
\label{thm:bounded-real-uniform-continuity-modulus-consumer-handoff}
Every Real or Completion consumer of an accepted $\BoundedRealUniformContinuityModulusUp$ carrier reads a uniform-continuity modulus only through the finite route
$$
I,K,F,D,E,H,C,P,N \longmapsto U.
$$
The handoff may cite the compact finite-net source, the $\ContinuousMapUp$ pointwise-modulus rows, the $\DyadicRatCoreUp$ tolerance ledger, and the terminal $\RealUp$ seal, but it exports no host Heine--Cantor theorem, open-cover compactness principle, selected subcover, quotient equality of real functions, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:bounded-real-uniform-continuity-modulus-namecert-obligations}. They identify $I,K,F,U,D,E,H,C,P,N$ as the complete local certificate surface for the modulus packet.

Read the consumer route in order. The interval row $I$ and compact finite-net row $K$ supply the source coverage; the pointwise-modulus row $F$ supplies local target control at the displayed centers; and the dyadic ledger $D$ records the finite tolerance calculation that feeds the output row $U$.

Finally use the structural rows $E,H,C,P,N$. They seal the Real-facing reads, transport the rows componentwise, replay the displayed route, record provenance, and name the packet. None of these rows adds host compactness, a selected subcover, function quotient equality, countable choice, or ambient Real completeness, so the handoff is exactly the finite uniform-modulus route.
\end{proof}

\falsifiablePrediction{Every accepted BoundedRealUniformContinuityModulusUp consumer that exports a uniform-continuity modulus without displaying a bounded Real interval row, compact finite-net row, ContinuousMap pointwise-modulus row, DyadicRatCore tolerance ledger, RealUp seal, transport, replay, provenance, and local NameCert row refutes the packet.}

\independenceWitness{real, compactmetric, continuousmap, uniformmodulus, dyadicratcore: none is bijective to BoundedRealUniformContinuityModulusUp because the packet jointly carries the bounded interval source, finite-net coverage, pointwise-modulus data, uniform-modulus extraction, dyadic tolerance accounting, terminal Real seal, transport, replay, provenance, and local naming.}

\closureat{BoundedRealUniformContinuityModulusUp}{seedStr}
\closureat{BoundedRealUniformContinuityModulusUp}{obligationStr}

\begin{closurestatus}{\BoundedRealUniformContinuityModulusUp}
  \constructivestory{A $\BoundedRealUniformContinuityModulusUp$ packet is generated from a bounded Real interval row, compact finite-net row, ContinuousMap pointwise-modulus row, UniformModulus extraction row, DyadicRatCore tolerance ledger, Real seal, componentwise hsame transports, Cont replay routes, package provenance, and a local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:bounded-real-uniform-continuity-modulus}, \autoref{thm:bounded-real-uniform-continuity-modulus-namecert-obligations}, and \autoref{thm:bounded-real-uniform-continuity-modulus-consumer-handoff} bind the finite bounded-Real uniform-modulus surface for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host Heine--Cantor, open-cover compactness, selected subcovers, quotient equality of real functions, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires checked source coverage, pointwise-modulus replay, dyadic tolerance exactness, Real sealing, transport, provenance, and non-escape for the same finite packet.}
\end{closurestatus}
