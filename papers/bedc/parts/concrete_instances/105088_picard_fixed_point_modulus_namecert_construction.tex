\chapter{A Concrete Naming Certificate for $\PicardFixedPointModulusUp$}
\label{ch:concrete-instances-picard-fixed-point-modulus-namecert}
\origin{human}

Following Banach's fixed-point theorem and Bishop--Bridges constructive analysis, the $\PicardFixedPointModulusUp$ packet records the finite tail modulus that a fixed-point consumer reads before any endpoint is selected. It sits after the contraction-map surface of \autoref{ch:concrete-instances-contractionmapping-namecert}, the one-step Picard route of \autoref{ch:concrete-instances-picardcontraction-namecert}, the separate complete-metric consumer row of \autoref{ch:concrete-instances-completemetric-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a constructive modulus handoff for Picard tails; it is not a selected fixed point, not a completed orbit endpoint, not host fixed-point equality, and not ambient $\RealUp$ completeness.

\begin{definition}[PicardFixedPointModulus carrier]
\label{def:picard-fixed-point-modulus-carrier}
A $\PicardFixedPointModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(K,I,L,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the accepted $\ContractionMappingUp$ source packet, including its metric carrier, self-map graph, rational contraction-bound ledger, and one-step Picard route.
\item $I$ is the $\PicardContractionUp$ iterate ledger that records the inspected Picard approximants and adjacent contraction comparisons.
\item $L$ is the strict-ratio tail ledger, listing the finite bound powers and tolerance requests used to answer tail estimates.
\item $T$ is the modulus threshold row. Each entry names a displayed stage after which the Picard tail comparison is admitted for the requested tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row that exposes the Picard orbit as regular rational sequence data only after $K,I,L,T$ have been displayed.
\item $E$ is the $\CompleteMetricUp/\RealUp$ seal boundary reached by completion and Real-facing consumers after the regular readback route has been exposed.
\item $H$ records componentwise $\hsame$ transport for the contraction source, Picard ledger, tail ledger, threshold row, regular readback, seal boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from contraction-map admission through Picard iteration, tail-threshold extraction, regular readback, and completion-facing seal.
\item $P$ is the $\Pkg$ provenance over $\PicardFixedPointModulusUp$, $\ContractionMappingUp$, $\PicardContractionUp$, $\CompleteMetricUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PicardFixedPointModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,I,L,T,R,E,H,C,P,N$. It has no coordinate for endpoint selection, completed orbit equality, quotient stream equality, host function extensionality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[PicardFixedPointModulus NameCert obligations]
\label{thm:picard-fixed-point-modulus-namecert-obligations}
For every accepted $\PicardFixedPointModulusUp$ carrier
$$
M=(K,I,L,T,R,E,H,C,P,N),
$$
the local $\NameCert_{\PicardFixedPointModulusUp}$ surface consists exactly of carrier admission, contraction-source visibility through $K$, Picard-iterate visibility through $I$, strict-ratio tail accounting through $L$, threshold extraction through $T$, $\RegSeqRatUp$ readback through $R$, completion and Real seal nonescape through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.

These obligations bind the fixed-point modulus route without adding endpoint selection, completed orbit equality, quotient stream equality, host function extensionality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:picard-fixed-point-modulus-carrier}. The displayed rows $K,I,L,T,R,E,H,C,P,N$ are the entire local surface, so carrier admission is exactly the admission of those rows.

The contraction and Picard data are read first. The row $K$ supplies the contraction-map packet named by \autoref{ch:concrete-instances-contractionmapping-namecert}, while $I$ supplies the Picard iterate route named by \autoref{ch:concrete-instances-picardcontraction-namecert}.

The modulus row is extracted from finite tail accounting. The strict-ratio ledger $L$ lists the bound powers and tolerance requests, and $T$ records the displayed threshold entries consumed by a fixed-point modulus request.

Only then can regular readback and completion-facing seals be consumed. The row $R$ exposes the $\RegSeqRatUp$ route, while $E$ marks the later $\CompleteMetricUp$ and $\RealUp$ boundary. Since endpoint selection and ambient completeness are not carrier coordinates, the local NameCert obligations stop at this displayed modulus handoff.
\end{proof}

\begin{theorem}[PicardFixedPointModulus tail handoff]
\label{thm:picard-fixed-point-modulus-tail-handoff}
Every fixed-point or completion-facing consumer of an accepted $\PicardFixedPointModulusUp$ packet must read
$$
\ContractionMappingUp,\ \PicardContractionUp,\ L,\ T,\ \RegSeqRatUp,\ \CompleteMetricUp,\ \RealUp
$$
in the order displayed by \autoref{def:picard-fixed-point-modulus-carrier}. The handoff supplies a finite Picard-tail modulus for downstream fixed-point consumers; it does not select a fixed point, identify a completed orbit endpoint, assert host fixed-point equality, or import ambient completeness.
\end{theorem}
\begin{proof}
Begin with the contraction source and Picard ledger. A consumer first reads $K$ and $I$, so each iterate and adjacent contraction comparison remains anchored in the same displayed contraction packet.

Next read the finite tail accounting. The rows $L$ and $T$ expose the strict-ratio budget and the requested threshold; no infinite tail selector or completed endpoint is available at this stage.

The regular rational and completion-facing rows are downstream. The $\RegSeqRatUp$ readback $R$ receives only the displayed modulus route, and the $\CompleteMetricUp/\RealUp$ seal boundary $E$ is visible only after that route has been replayed.

Thus the packet supplies a fixed-point modulus handoff rather than an endpoint selection principle. The omitted host-side principles have no carrier row and cannot be exported by $H,C,P,N$.
\end{proof}

\closureat{PicardFixedPointModulusUp}{seedStr}

\begin{closurestatus}{\PicardFixedPointModulusUp}
  \constructivestory{A $\PicardFixedPointModulusUp$ packet is generated from a ContractionMapping source packet, a PicardContraction iterate ledger, a strict-ratio tail ledger, a displayed threshold row, a RegSeqRat readback, a CompleteMetric/Real seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:picard-fixed-point-modulus-carrier}, \autoref{thm:picard-fixed-point-modulus-namecert-obligations}, and \autoref{thm:picard-fixed-point-modulus-tail-handoff} seed the finite Picard tail-modulus handoff from contraction and Picard data to regular readback and completion-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim endpoint selection, completed orbit equality, quotient stream equality, host fixed-point equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction-source visibility, Picard-iterate visibility, strict-ratio tail accounting, threshold extraction, regular readback, seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.PicardFixedPointModulusUp.}
\end{closurestatus}
