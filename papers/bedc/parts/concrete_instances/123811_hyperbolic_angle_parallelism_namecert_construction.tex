\chapter{A Concrete Naming Certificate for $\HyperbolicAngleParallelismUp$}
\label{ch:concrete-instances-hyperbolic-angle-parallelism-namecert}
\origin{human}

Following Lobachevsky and Poincare, the $\HyperbolicAngleParallelismUp$ packet records the finite angle-of-parallelism route for a Poincare disk observer, a boundary end, and a comparison geodesic. It sits after the metric surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the disk metric packet of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the geodesic-boundary packet of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the visual-boundary packet of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, and the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}. The packet names finite angle readback rows; it does not construct a completed hyperbolic plane, select ideal geodesics, quotient boundary points, or import ambient $\RealUp$ completeness.

\begin{definition}[Hyperbolic angle-of-parallelism carrier]
\label{def:hyperbolic-angle-parallelism-carrier}
A $\HyperbolicAngleParallelismUp$ carrier is a finite $\BHist$ packet
$$
A=(B,D,G,V,R,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the boundary-point row, read through the finite $\HyperbolicVisualBoundaryUp$ surface.
\item $D$ is the $\PoincareDiskMetricUp$ disk-point row for the observer and the inspected disk coordinate.
\item $G$ is the $\HyperbolicGeodesicBoundaryUp$ comparison row naming the finite geodesic side and its boundary incidence.
\item $V$ is the visual-angle readback row, recording the angle-of-parallelism comparison visible from the displayed disk point toward $B$ along $G$.
\item $R$ is the $\RealUp$ tolerance and seal row for the angle value, available only after $B,D,G,V$ have been displayed.
\item $T$ records transport from the disk, boundary, geodesic, and visual-angle rows to the real-sealed readback.
\item $H$ records componentwise $\hsame$ transport for $B,D,G,V,R,T$ and the structural rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
B,D,G \longmapsto V \longmapsto R \longmapsto T,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\HyperbolicAngleParallelismUp$, $\HyperbolicMetricUp$, $\PoincareDiskMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicVisualBoundaryUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicAngleParallelismUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,D,G,V,R,T,H,C,P,N$. It has no coordinate for a completed hyperbolic plane, host ideal boundary, selected geodesic representative, quotient boundary equality, or ambient real-completeness principle.
\end{definition}

\begin{theorem}[Hyperbolic angle-of-parallelism NameCert obligations]
\label{thm:hyperbolic-angle-parallelism-namecert-obligations}
For every accepted $\HyperbolicAngleParallelismUp$ carrier $A=(B,D,G,V,R,T,H,C,P,N)$, the local $\NameCert_{\HyperbolicAngleParallelismUp}$ obligations are exhausted by boundary-point admission through $B$, Poincare disk-point admission through $D$, geodesic comparison through $G$, visual-angle readback through $V$, Real tolerance and sealing through $R$, transport through $T$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-angle-parallelism-carrier}. The geometric rows available to the local classifier are exactly $B,D,G,V,R,T$.

Read the finite route in order. The boundary-point row and disk-point row supply the visible endpoints, the geodesic row supplies the comparison side, and the visual-angle row records the angle-of-parallelism readback over those displayed data. The Real row is downstream of this route and only seals the displayed tolerance.

The structural rows $H,C,P,N$ preserve the same packet by componentwise transport, replay, provenance, and naming. Since the carrier contains no completed plane, host boundary, selected geodesic, quotient equality, or real-completeness row, the listed obligations exhaust the local certificate.
\end{proof}

\begin{theorem}[Hyperbolic angle-of-parallelism finite route]
\label{thm:hyperbolic-angle-parallelism-finite-route}
Every public $\HyperbolicAngleParallelismUp$ read exposes the finite route
$$
B,D,G,V,R,T,H,C,P,N
$$
through boundary point, disk point, geodesic comparison, visual-angle readback, Real tolerance, transport, replay, provenance, and local naming. The route may be consumed by the hyperbolic observer-expansion surface, but it exports no completed hyperbolic plane, selected ideal geodesic, quotient boundary equality, host visual metric, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with an accepted carrier. The rows $B,D,G$ are the only displayed source rows for the boundary end, disk observer, and geodesic comparison.

Use $V$ and $R$ next. The visual-angle row reads the angle-of-parallelism comparison from those source rows, and the Real row seals only the finite tolerance attached to that readback.

Finally replay $T,H,C,P,N$. Transport, componentwise equality, continuation replay, provenance, and naming preserve the same finite route. The excluded global objects are not carrier rows, so no public consumer can read them from the packet.
\end{proof}

\falsifiablePrediction{An accepted $\HyperbolicAngleParallelismUp$ read is refuted if it exports a Real-valued angle before displaying boundary point, Poincare disk point, geodesic comparison, visual-angle readback, transport, replay, provenance, and local naming rows.}

\independenceWitness{hyperbolicmetric, poincarediskmetric, hyperbolicgeodesicboundary, hyperbolicvisualboundary, real: $\HyperbolicAngleParallelismUp$ is not bijective to any listed sibling because it jointly carries boundary point, disk point, geodesic comparison, visual-angle readback, Real tolerance, transport, replay, provenance, and local NameCert rows.}

\closureat{HyperbolicAngleParallelismUp}{seedStr}

\begin{closurestatus}{\HyperbolicAngleParallelismUp}
  \constructivestory{A $\HyperbolicAngleParallelismUp$ packet is generated from a HyperbolicVisualBoundaryUp boundary row, a PoincareDiskMetricUp disk-point row, a HyperbolicGeodesicBoundaryUp comparison row, a visual-angle readback row, a RealUp tolerance seal, transport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-angle-parallelism-carrier}, \autoref{thm:hyperbolic-angle-parallelism-namecert-obligations}, and \autoref{thm:hyperbolic-angle-parallelism-finite-route} seed the finite angle-of-parallelism packet over disk, boundary, geodesic, visual-angle, Real-seal, transport, provenance, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a completed hyperbolic plane, host ideal-boundary equality, selected geodesics, quotient boundary points, ambient Real completeness, Lean checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked boundary-point admission, disk-point admission, geodesic comparison, visual-angle readback, Real tolerance sealing, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicAngleParallelismUp.}
\end{closurestatus}
