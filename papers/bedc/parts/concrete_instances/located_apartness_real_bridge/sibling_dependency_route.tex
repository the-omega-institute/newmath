\begin{lemma}[Located apartness real bridge sibling dependency route]
\label{lem:located-apartness-real-bridge-sibling-dependency-route}
Every accepted $\LocatedApartnessRealBridgeUp$ read consumes the located endpoint rows of \autoref{ch:concrete-instances-locatedreal-namecert}, the positive Cauchy apartness evidence of \autoref{ch:concrete-instances-cauchy-real-apartness-namecert}, the order-facing handoff rows of \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert}, and the cotransitivity routing of \autoref{ch:concrete-instances-apartness-cotransitivity-namecert} before the local bridge name is inspected.
\end{lemma}
\begin{proof}
Project the carrier of \autoref{def:located-apartness-real-bridge-carrier}. The endpoint rows $L_0,L_1$ are located-real packets, so their finite located-radius windows are read through the sibling $\LocatedRealUp$ surface.

The positive separation row $A$ is then read through $\CauchyRealApartnessUp$, and the order-facing row $O$ is downstream of that apartness evidence. If a third displayed real name is present, the row $T$ routes the comparison through $\ApartnessCotransitivityUp$.

The structural rows $H,C,P,N$ transport, replay, package, and name this same route. Since the carrier has no additional dependency coordinate, every local bridge read factors through exactly the listed sibling chapters.
\end{proof}
