\begin{lemma}[Located apartness real bridge Cauchy setoid route]
\label{thm:located-apartness-real-bridge-cauchy-setoid-route}
Every located-apartness read exported by an accepted $\LocatedApartnessRealBridgeUp$ packet to a completion or separated-setoid consumer must pass through $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and the $\CauchyEquivalenceSetoidUp$ classifier route before it can be used as apartness evidence. The exported evidence is the finite L10 readback chain through \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}; it is not quotient-real equality, a selected completion representative, host order data, or ambient completeness.
\end{lemma}
\begin{proof}
Project the carrier of \autoref{def:located-apartness-real-bridge-carrier}. The located-radius readbacks are $R_0,R_1$, and each exposes the stream-name window, regular-rational readback, dyadic-radius ledger, and terminal $\RealUp$ seal before any apartness row is consumed.

Use \autoref{thm:located-apartness-real-bridge-namecert-obligations}. The positive apartness row $A$ is tied to those same endpoint windows, so a completion or separated-setoid consumer cannot detach the apartness witness from the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ route.

Now route the completion-facing read through the $\CauchyEquivalenceSetoidUp$ classifier. The setoid classifier is the sibling surface that records equality-compatible Cauchy comparison before a completion consumer may reuse the apartness evidence.

The structural rows $H,C,P,N$ only transport, replay, source, and name this route. Since no row stores quotient-real equality, selected completion representatives, host order data, or ambient completeness, the L10 chain is nonescaping.
\end{proof}
