The L10 Real and Completion dependency route consumed by $\LocatedApartnessRealBridgeUp$ is the finite readback chain through \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-dyadicratcore-namecert}. A located-apartness comparison therefore reaches the terminal $\RealUp$ seal only after the stream-name window, regular-rational readback, and dyadic-radius ledger have been displayed, so the bridge remains a constructive comparison packet rather than a quotient-real equality test, selected completion representative, host order theorem, or ambient completeness claim.
