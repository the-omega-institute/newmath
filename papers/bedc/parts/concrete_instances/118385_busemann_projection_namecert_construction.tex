\chapter{A Concrete Naming Certificate for $\BusemannProjectionUp$}
\label{ch:concrete-instances-busemann-projection-namecert}
\origin{ai}

The $\BusemannProjectionUp$ packet realizes the hyperbolic observer-expansion route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as a finite boundary projection surface. It sits after the Busemann-boundary rows of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, the visual-boundary rows of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the observer-expansion rows of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, the hyperbolic metric rows of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, and the disk-metric rows of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}. The packet records a finite projection from observer windows to Busemann boundary coordinates; it is not a completed ideal boundary, a quotient ray space, a selected geodesic representative, or a host compactification theorem.

\begin{definition}[Busemann projection carrier]
\label{def:busemann-projection-carrier}
A $\BusemannProjectionUp$ carrier is a finite $\BHist$ packet
$$
B=(M,D,\Xi,V,O,W,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicMetricUp$ metric-read row for the inspected disk or boundary-facing source.
\item $D$ is the $\PoincareDiskMetricUp$ disk-metric row supplying the local disk coordinate and distance comparison windows.
\item $\Xi$ is the $\HyperbolicBusemannBoundaryUp$ boundary row that receives the projected direction.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ visual-boundary row used to compare the projected direction with observer-visible boundary data.
\item $O$ is the $\HyperbolicObserverExpansionUp$ observer-window row that supplies finite expansion and phase-readout windows.
\item $W$ is the projection-window row, listing the finite metric, boundary, visual, and observer windows consumed by the projection.
\item $R$ is the refusal row excluding host compactifications, quotient ray spaces, selected geodesic representatives, and unlogged global boundary models.
\item $H$ records componentwise $\hsame$ transport for the metric, disk, Busemann, visual, observer, projection-window, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through $M,D,\Xi,V,O,W,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BusemannProjectionUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicObserverExpansionUp$, $\HyperbolicMetricUp$, $\PoincareDiskMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BusemannProjectionUp}$ row.
\end{enumerate}
The later Lean TasteGate file for $\BusemannProjectionUp$ must provide Nontrivial and FieldFaithful instances before the packet can be treated as a positive discovery gate rather than a finite carrier row.
\end{definition}

\begin{theorem}[Busemann projection NameCert obligations]
\label{thm:busemann-projection-namecert-obligations}
For every accepted $\BusemannProjectionUp$ carrier $B=(M,D,\Xi,V,O,W,R,H,C,P,N)$, the local $\NameCert_{\BusemannProjectionUp}$ surface consists exactly of metric admission, disk-metric admission, Busemann-boundary admission, visual-boundary comparison, observer-window admission, projection-window exactness, refusal exactness, componentwise transport, displayed replay, provenance, and local naming. Every projection read must expose the finite boundary, Busemann, visual-metric, observer-window, provenance, and NameCert rows before it is consumed as a boundary projection.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:busemann-projection-carrier}. The metric and disk rows are $M,D$, the boundary-facing rows are $\Xi,V,O,W,R$, and the structural rows are $H,C,P,N$.

The projection begins with displayed metric data. The hyperbolic metric and disk-metric rows provide the distance windows and disk coordinate comparisons used by the boundary-facing read.

The Busemann, visual, and observer rows are then read together. The boundary target $\Xi$, the visual comparison $V$, and the observer-expansion window $O$ determine the finite projection route, while $W$ records which windows are consumed.

Finally the refusal and structural rows prevent escape to host geometry. Since $R,H,C,P,N$ contain no completed ideal boundary, quotient ray space, selected geodesic representative, or global compactification theorem, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\BusemannProjectionUp$ read exposes HyperbolicMetric, PoincareDiskMetric, HyperbolicBusemannBoundary, HyperbolicVisualBoundary, HyperbolicObserverExpansion, projection-window, refusal, transport, replay, provenance, and local NameCert rows before any boundary projection is consumed.}

\independenceWitness{hyperbolic-busemann-boundary, hyperbolic-visual-boundary, hyperbolic-observer-expansion, hyperbolic-metric, poincare-disk-metric: $\BusemannProjectionUp$ is not bijective to any listed sibling because it jointly carries metric admission, disk-metric comparison, Busemann boundary target, visual-boundary comparison, observer-window projection, host-geometry refusal, transport, replay, provenance, and local naming.}

\closureat{BusemannProjectionUp}{seedStr}

\begin{closurestatus}{\BusemannProjectionUp}
  \constructivestory{A $\BusemannProjectionUp$ packet is generated from HyperbolicMetric rows, PoincareDiskMetric rows, HyperbolicBusemannBoundary rows, HyperbolicVisualBoundary rows, HyperbolicObserverExpansion windows, finite projection-window data, refusal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:busemann-projection-carrier} and \autoref{thm:busemann-projection-namecert-obligations} seed the finite boundary projection route over hyperbolic metric, disk metric, Busemann boundary, visual boundary, observer-window, refusal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, completed ideal boundaries, quotient ray spaces, selected geodesic representatives, host compactification theorems, or global hyperbolic models.}
  \upgradepath{Obligation closure requires checked metric admission, disk-metric admission, Busemann-boundary admission, visual-boundary comparison, observer-window admission, projection-window exactness, refusal exactness, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.BusemannProjectionUp.}
\end{closurestatus}
