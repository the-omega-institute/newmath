\chapter{A Concrete Naming Certificate for $\ClosedSubstitutionBoundaryUp$}
\label{ch:concrete-instances-closed-substitution-boundary-namecert}
\origin{ai}

The $\ClosedSubstitutionBoundaryUp$ surface packages the MetaCIC closed-term substitution boundary as a finite naming certificate. It records the exact packet by which a \texttt{Term} row, a \texttt{ClosedAt} classifier row, the \texttt{shift} and \texttt{substitute} operation rows, displayed $\hsame$ transport, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ rows show that substitution is inert on the closed-at-boundary slice.

The sibling closed-term substitution boundary of \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert} supplies the finite \texttt{Term}, \texttt{ClosedAt}, \texttt{shift}, and \texttt{substitute} surface that this chapter specializes to the closed-at-boundary inertness route.

\begin{definition}[ClosedSubstitutionBoundary carrier]
\label{def:closed-substitution-boundary-carrier}
A $\ClosedSubstitutionBoundaryUp$ carrier is a finite $\BHist$ packet
$$
S=(T,D,V,K,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the MetaCIC \texttt{Term} generator row, recording that terms are built only from \texttt{var}, \texttt{app}, \texttt{lam}, \texttt{pi}, and \texttt{sort};
\item $D$ is the depth row for the boundary at which the classifier is read;
\item $V$ is the substitution payload term row, carried as data but not inspected by the inertness route;
\item $K$ is the \texttt{ClosedAt} classifier row certifying that the term row is closed at depth $D$;
\item $R$ is the \texttt{shift} operation row, including the boundary-budget route witnessed by the closed slice;
\item $Q$ is the \texttt{substitute} operation row, including the route that attempts substitution at depth $D$ with payload $V$;
\item $H$ records componentwise $\hsame$ transports for the term row, depth row, payload row, classifier row, \texttt{shift} row, \texttt{substitute} row, continuation row, package row, and local naming row;
\item $C$ records the displayed $\Cont$ routes by which consumers read the term, classifier, shift route, substitute route, provenance, and local certificate;
\item $P$ is the $\Pkg$ provenance over $\BHist$, \texttt{Term}, \texttt{ClosedAt}, \texttt{shift}, \texttt{substitute}, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\ClosedSubstitutionBoundaryUp}$ row.
\end{enumerate}
The carrier contains no semantic substitution theorem for arbitrary open terms, no subject-reduction theorem for an evaluator, no host equality of syntax quotients, and no untracked binder name.
\end{definition}
\leanchecked{BEDC.Derived.ClosedSubstitutionBoundaryUp.ClosedSubstitutionBoundaryTasteGate\_single\_carrier\_alignment}

\begin{theorem}[ClosedSubstitutionBoundary NameCert obligations]
\label{thm:closed-substitution-boundary-namecert-obligations}
The local $\NameCert_{\ClosedSubstitutionBoundaryUp}$ surface for the carrier of \autoref{def:closed-substitution-boundary-carrier} consists of carrier admission for
$$
S=(T,D,V,K,R,Q,H,C,P,N),
$$
\texttt{ClosedAt} classifier stability, boundary-budget stability for \texttt{shift}, inertness of the displayed \texttt{substitute} route on the closed-at-boundary slice, componentwise $\hsame$ transport, displayed $\Cont$ locality, $\Pkg$ provenance, and non-escape for unregistered free variables, arbitrary open-term substitution, evaluator subject reduction, host syntax quotients, and ambient metatheoretic truth.
\end{theorem}
\leanchecked{BEDC.Derived.ClosedSubstitutionBoundaryUp.ClosedSubstitutionBoundary\_namecert\_obligations}
\leanvariant{BEDC.Derived.ClosedSubstitutionBoundaryUp.ClosedSubstitutionBoundaryNameCertObligations}
\begin{proof}
Carrier admission is projection from \autoref{def:closed-substitution-boundary-carrier}. The rows $T,D,V,K,R,Q,H,C,P,N$ are the only rows admitted to the packet.

The classifier row $K$ fixes the boundary. It records that the term row has no free de Bruijn index at or above the displayed depth $D$, so the \texttt{shift} route $R$ and the \texttt{substitute} route $Q$ are read only through that closed slice.

Transport and consumer access are confined by $H$ and $C$. These rows replay the same term, boundary, payload, classifier, and operation routes under componentwise $\hsame$ movement and do not add a hidden binder name or a second substitution surface.

Finally $P$ and $N$ record provenance and the local certificate. Since the excluded objects have no coordinate in the carrier, no NameCert projection can export arbitrary open-term substitution, evaluator subject reduction, host syntax quotient equality, or ambient metatheoretic truth.
\end{proof}

\begin{theorem}[ClosedSubstitutionBoundary substitute inertness route]
\label{thm:closed-substitution-boundary-substitute-inertness-route}
For an accepted $\ClosedSubstitutionBoundaryUp$ carrier $S=(T,D,V,K,R,Q,H,C,P,N)$, every public read of the \texttt{substitute} operation at boundary $D$ factors through the displayed \texttt{ClosedAt} classifier row $K$ and returns the original term row. The payload row $V$ is carried through the recursive operation route, but it has no access point on the closed-at-boundary slice.
\end{theorem}
\begin{proof}
Read the classifier first. The row $K$ says that every free index visible in the term row of $T$ lies below the boundary $D$.

Now inspect the displayed substitution route $Q$. At a variable row, the equality branch for the boundary index cannot fire, and the decrement branch above the boundary cannot fire, because $K$ supplies the strict below-boundary reading.

For compound rows, the route follows the constructor tree of $T$. The binder cases raise the depth and carry the payload row through the operation, but the classifier row continues to block any access point at the active boundary.

Transport through $H$ and consumer reading through $C$ preserve that same route. Thus the public substitute read is inert on the \texttt{ClosedAt} slice and does not export an unregistered free variable, a second payload access channel, or a host-level semantic substitution claim.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSubstitutionBoundaryUp.ClosedSubstitutionBoundary\_substitute\_inertness\_route}

\begin{theorem}[ClosedSubstitutionBoundary public nonescape]
\label{thm:closed-substitution-boundary-public-nonescape}
For an accepted $\ClosedSubstitutionBoundaryUp$ carrier $S=(T,D,V,K,R,Q,H,C,P,N)$, every public consumer of the packet reads only the displayed rows $T,D,V,K,R,Q,H,C,P,N$ and their registered $\Cont$ projections. In particular, the public surface cannot export arbitrary open-term substitution, evaluator subject reduction, host syntax quotient equality, or ambient metatheoretic truth.
\end{theorem}
\begin{proof}
Start with the carrier coordinates of \autoref{def:closed-substitution-boundary-carrier}. The only admitted rows are the term row, boundary row, payload row, \texttt{ClosedAt} classifier, \texttt{shift} and \texttt{substitute} routes, componentwise transport, continuation routes, provenance, and local naming row.

Now read a public consumer through $C$. Each continuation has a named source coordinate in $S$, so the consumer can project the displayed term, boundary, payload, classifier, operation route, transport route, provenance, or certificate, but no additional semantic object.

The excluded claims have no carrier coordinate and no continuation route. Thus arbitrary open substitution, evaluator subject reduction, quotient equality for host syntax, and ambient truth cannot be reached from an accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSubstitutionBoundaryUp.ClosedSubstitutionBoundary\_public\_nonescape}

\begin{theorem}[ClosedSubstitutionBoundary closed-slice transport]
\label{thm:closed-substitution-boundary-closed-slice-transport}
Let $S=(T,D,V,K,R,Q,H,C,P,N)$ and $S'=(T',D',V',K',R',Q',H',C',P',N')$ be accepted $\ClosedSubstitutionBoundaryUp$ carriers. If the componentwise $\hsame$ row $H$ transports $T,D,V,K,R,Q,C,P,N$ to $T',D',V',K',R',Q',C',P',N'$ and preserves the displayed \texttt{ClosedAt} row, then the inert \texttt{substitute} route on the closed slice transports from $Q$ to $Q'$.
\end{theorem}
\begin{proof}
First transport the boundary data. The $\hsame$ row identifies the term row, depth row, payload row, and \texttt{ClosedAt} classifier row with their primed counterparts while keeping the classifier attached to the same closed-slice reading.

Next apply \autoref{thm:closed-substitution-boundary-substitute-inertness-route} to the source packet. The substitute route $Q$ is inert precisely because each public read factors through $K$ at boundary $D$.

Transport the operation row along $H$. Since the \texttt{ClosedAt} row is preserved, the same boundary-blocking argument applies after transport, and the primed route $Q'$ remains inert on the transported closed slice.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSubstitutionBoundaryUp.ClosedSubstitutionBoundary\_closed\_slice\_transport}

\begin{theorem}[ClosedSubstitutionBoundary consumer exhaustion]
\label{thm:closed-substitution-boundary-consumer-exhaustion}
The carrier of \autoref{def:closed-substitution-boundary-carrier}, the NameCert obligations of \autoref{thm:closed-substitution-boundary-namecert-obligations}, the inert substitute route of \autoref{thm:closed-substitution-boundary-substitute-inertness-route}, the public nonescape theorem of \autoref{thm:closed-substitution-boundary-public-nonescape}, and the closed-slice transport theorem of \autoref{thm:closed-substitution-boundary-closed-slice-transport} jointly give the consumer-exhaustion surface used by the sibling closed-term substitution boundary.
\end{theorem}
\begin{proof}
The carrier and NameCert obligations provide the finite packet and its admissible obligations. They account for the displayed term, boundary, payload, classifier, operation, transport, continuation, provenance, and naming rows.

The inertness theorem fixes the only substitute route available to consumers on the closed slice. The transport theorem shows that componentwise $\hsame$ movement cannot change that route into an open-substitution surface.

Finally apply public nonescape. Since every consumer is exhausted by the displayed rows and their continuations, the sibling closed-term substitution boundary receives exactly the finite closed-substitution packet and no ambient substitution theorem, evaluator theorem, syntax quotient equality, or metatheoretic truth object.
\end{proof}

\closureat{ClosedSubstitutionBoundaryUp}{seedStr}
\closureat{ClosedSubstitutionBoundaryUp}{obligationStr}
\closureat{ClosedSubstitutionBoundaryUp}{scopedStr}

\begin{closurestatus}{\ClosedSubstitutionBoundaryUp}
  \constructivestory{A $\ClosedSubstitutionBoundaryUp$ packet is generated from a MetaCIC \texttt{Term} row, a boundary depth row, a payload term row, a \texttt{ClosedAt} classifier row, \texttt{shift} and \texttt{substitute} operation rows, displayed $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The scoped surface binds the finite $\BHist$ packet, \texttt{ClosedAt} classifier, \texttt{shift} and \texttt{substitute} rows, componentwise $\hsame$, displayed $\Cont$, $\Pkg$ provenance, local $\NameCert$, and TasteGate marker for the closed-substitution boundary slice.}
  \leanchecked{BEDC.Derived.ClosedSubstitutionBoundaryUp.taste\_gate}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close arbitrary open-term substitution, evaluator subject reduction, host syntax quotient equality, semantic preservation for all compiler passes, or bridge checking.}
  \upgradepath{Public closure requires a public consumer surface for the displayed closed-substitution boundary packet.}
\end{closurestatus}
