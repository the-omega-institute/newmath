\chapter{A Concrete Naming Certificate for $\RegularCauchyTailFusionUp$}
\label{ch:concrete-instances-regularcauchytailfusion-namecert}
\origin{ai}

The $\RegularCauchyTailFusionUp$ packet records the finite cofinal tail-fusion step used by Real completeness consumers before a diagonal $\RealUp$ seal is read. It joins regular-sequence tail witnesses, selected finite stream windows, dyadic tolerance ledgers, a shared tail-meet row, a diagonal seal route, and the local naming certificate into one displayed $\BHist$ surface. It exports no quotient stream, host real equality, selected completed limit, unbounded diagonal object, or ambient completeness theorem.

\begin{definition}[Regular Cauchy tail fusion carrier]
\label{def:regular-cauchy-tail-fusion-carrier}
A $\RegularCauchyTailFusionUp$ carrier is a finite $\BHist$ packet
$$
F=(Q,X,W,D,T,M,R,L,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $Q$ is the rational or dyadic precision request routed to the tail-fusion consumer;
\item $X$ is the $\RegSeqRatUp$ regular rational family row whose tail witnesses are being fused;
\item $W$ is the finite $\StreamNameUp$ window family selected for the precision request;
\item $D$ is the $\DyadicRatCoreUp$ tolerance and observation ledger for the selected windows;
\item $T$ is the finite tail-witness row exposing the cofinal tail data read from $X$ through $W$ and $D$;
\item $M$ is the $\RegularCauchyTailMeetUp$ shared-threshold row that makes the displayed tail witnesses use one cofinal tail bound;
\item $R$ is the $\RegSeqRatUp$ diagonal readback row reached after the shared tail bound and dyadic ledger are fixed;
\item $L$ is the $\RealUp$ seal row reached only after $Q,X,W,D,T,M,R$ have been exposed;
\item $H$ stores componentwise $\hsame$ transports for the precision request, regular family, windows, dyadic ledger, tail witnesses, tail-meet row, readback row, seal row, provenance, and local name;
\item $C$ stores the displayed $\Cont$ routes from precision request to window selection, dyadic observation, tail witness, shared tail meet, diagonal readback, and Real seal;
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyTailMeetUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchyTailFusionUp}$ row.
\end{enumerate}
The carrier is only the finite cofinal tail-fusion packet. It has no coordinate for quotient stream equality, host real equality, an externally selected limit, an unbounded diagonal stream, or an ambient completeness principle.
\end{definition}

\begin{definition}[Regular Cauchy tail fusion classifier]
\label{def:regular-cauchy-tail-fusion-classifier}
Two accepted $\RegularCauchyTailFusionUp$ carriers classify together when their displayed precision requests, regular-family rows, finite windows, dyadic ledgers, tail witnesses, shared tail-meet rows, diagonal readbacks, and Real seal rows are related componentwise by the stored $\hsame$ transports and replay the same $\Cont$ route from $Q$ through $M$ to $L$.

The classifier compares only the rows listed in \autoref{def:regular-cauchy-tail-fusion-carrier}; it does not compare quotient streams, host real values, selected external limits, or ambient completeness witnesses.
\end{definition}

\begin{theorem}[Regular Cauchy tail fusion NameCert obligations]
\label{thm:regular-cauchy-tail-fusion-namecert-obligations}
The local $\NameCert_{\RegularCauchyTailFusionUp}$ surface for the carrier of \autoref{def:regular-cauchy-tail-fusion-carrier} consists exactly of carrier admission, precision-request exposure, $\RegSeqRatUp$ family admission, finite $\StreamNameUp$ window coverage, $\DyadicRatCoreUp$ tolerance-ledger readback, finite tail-witness exposure, $\RegularCauchyTailMeetUp$ shared-threshold routing, diagonal $\RegSeqRatUp$ readback, $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ route replay, $\Pkg$ provenance, and the local naming row.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTailFusionUp.RegularCauchyTailFusionTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the carrier $F$. Its coordinates list the precision request, regular family, selected windows, dyadic ledger, tail witnesses, shared tail meet, diagonal readback, Real seal, transports, routes, provenance, and local name.

The cofinal tail route is finite. A consumer first reads $Q$, then the selected windows $W$ and dyadic ledger $D$, then the tail witness row $T$, and only then the shared-threshold row $M$ used by the diagonal readback $R$.

The remaining rows $H,C,P,N$ transport, route, record provenance, and name the same packet. Since no carrier coordinate contains quotient streams, host real equality, selected limits, unbounded diagonal data, or ambient completeness, the listed obligations exhaust the local naming surface.
\end{proof}

\begin{theorem}[Regular Cauchy tail fusion tail-meet handoff]
\label{thm:regular-cauchy-tail-fusion-tail-meet-handoff}
For an accepted $\RegularCauchyTailFusionUp$ carrier, every cofinal tail witness read reaches the diagonal readback row $R$ through the finite route
$$
Q,X,W,D,T,M,H,C,P,N.
$$
The $\RegularCauchyTailMeetUp$ row $M$ is the only shared-threshold surface available to the packet, so the handoff cannot replace it by a hidden tail oracle, quotient-stream comparison, selected limit, host real equality, or ambient completeness row.
\end{theorem}
\begin{proof}
Read the carrier coordinates from \autoref{def:regular-cauchy-tail-fusion-carrier}. The only row that can merge tail witnesses into a common bound is $M$, and it appears after the precision request, regular-family row, finite windows, dyadic ledger, and tail-witness row.

Apply the NameCert obligation theorem. It binds the tail-witness exposure to $T$ and the shared-threshold routing to $M$, with all movement recorded by $H$ and $C$ and all provenance recorded by $P$ and $N$.

Because the classifier of \autoref{def:regular-cauchy-tail-fusion-classifier} compares only the displayed rows, no transported carrier can introduce a second tail source or an external comparison. The diagonal readback therefore receives the tail-meet handoff through exactly the finite route shown above.
\end{proof}

\begin{theorem}[Regular Cauchy tail fusion monotone window handoff]
\label{thm:regular-cauchy-tail-fusion-window-monotone-handoff}
Every accepted $\RegularCauchyTailFusionUp$ packet reads its selected $\StreamNameUp$ finite windows $W$ and dyadic ledger $D$ before the tail witnesses $T$ and shared threshold $M$ are consumed. Later $\RealUp$ seal reads through $L$ cannot skip $W,D$ or replace them by an unwindowed tail oracle, quotient stream comparison, selected limit, or ambient completeness row.
\end{theorem}
\begin{proof}
Project
$$
F=(Q,X,W,D,T,M,R,L,H,C,P,N).
$$
The carrier orders the finite windows and dyadic ledger before tail witnesses and before the shared threshold row.

Use \autoref{thm:regular-cauchy-tail-fusion-tail-meet-handoff}. It routes every cofinal tail witness through $Q,X,W,D,T,M,H,C,P,N$ before the diagonal readback $R$ is reached.

Now apply the limit-transport link to the later seal row. Since the $\RealUp$ handoff reads $L$ only after the displayed diagonal readback route, coordinate exhaustion leaves no route that bypasses $W,D$.
\end{proof}

\begin{theorem}[Regular Cauchy tail fusion shared-threshold exhaustion]
\label{thm:regular-cauchy-tail-fusion-shared-threshold-exhaustion}
Every $\RegularCauchyTailFusionUp$ route that reaches the diagonal readback $R$ or the $\RealUp$ seal $L$ uses the single displayed shared-threshold row $M$ supplied by the $\RegularCauchyTailMeetUp$ sibling. The packet has no second tail oracle, quotient stream comparison, selected limit, ambient completeness row, or independent threshold coordinate.
\end{theorem}
\begin{proof}
Project
$$
F=(Q,X,W,D,T,M,R,L,H,C,P,N).
$$
The only shared-threshold coordinate in the carrier is $M$.

Apply \autoref{thm:regular-cauchy-tail-fusion-tail-meet-handoff}. It sends each cofinal tail witness to $R$ through $M$, with all movement recorded by the displayed transport, continuation, provenance, and local naming rows.

Use the sibling link to \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} to identify $M$ as the shared finite threshold surface. Use the link to \autoref{ch:concrete-instances-regularcauchylimittransport-namecert} for the subsequent route from $R$ to $L$.

Coordinate exhaustion closes the claim. Since no other carrier coordinate has the role of a threshold, tail oracle, quotient comparison, selected limit, or completeness witness, every route to $R$ or $L$ uses the displayed $M$ row.
\end{proof}

\begin{theorem}[Regular Cauchy tail fusion Real limit handoff]
\label{thm:regular-cauchy-tail-fusion-real-limit-handoff}
Every $\RealUp$ diagonal-limit consumer of an accepted $\RegularCauchyTailFusionUp$ packet reads the seal row $L$ only after the precision request, regular rational family, selected finite windows, dyadic tolerance ledger, finite tail witnesses, shared tail-meet row, diagonal readback, componentwise transports, continuation routes, package provenance, and local name have been displayed.

Equivalently, the completion-facing route factors through
$$
Q,X,W,D,T,M,R,H,C,P,N
$$
before the seal row $L$ is consumed, and it exports no quotient stream, host real equality, selected completed limit, unbounded diagonal object, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with a $\RealUp$ consumer of the seal row $L$. In the carrier, $L$ is downstream of the diagonal readback row $R$, so the consumer cannot inspect a seal before the finite readback route has been displayed.

Use \autoref{thm:regular-cauchy-tail-fusion-tail-meet-handoff}. It places every cofinal tail read through $Q,X,W,D,T,M,H,C,P,N$ before $R$ is reached.

Now append the seal route from $R$ to $L$ using the continuation rows in $C$ and the componentwise transports in $H$. The package provenance and local name record the same finite route. Since the carrier and classifier omit quotient streams, host equality, selected limits, unbounded diagonal objects, and ambient completeness, no completion-facing read can escape the displayed packet.
\end{proof}

\begin{theorem}[Regular Cauchy tail fusion limit-transport link]
\label{thm:regular-cauchy-tail-fusion-limit-transport-link}
Every accepted $\RegularCauchyTailFusionUp$ packet factors its $\RealUp$ seal handoff through the shared-threshold row of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} and the displayed seal transport surface of \autoref{ch:concrete-instances-regularcauchylimittransport-namecert} before any $\RealUp$ consumer can read the diagonal limit row.

In particular, for
$$
F=(Q,X,W,D,T,M,R,L,H,C,P,N),
$$
the rows $Q,X,W,D,T$ expose the finite precision, regular rational family, selected windows, dyadic ledger, and tail witnesses; $M$ supplies the shared cofinal threshold; $R,L$ supply the diagonal readback and seal handoff; and $H,C,P,N$ record transport, continuation, provenance, and the local name.
\end{theorem}
\begin{proof}
Project the existing packet rows $Q,X,W,D,T,M,R,L,H,C,P,N$. The carrier gives no direct route from the finite tail witnesses to the Real seal; the route must first pass through the displayed shared-threshold row $M$ and the diagonal readback row $R$.

Use the tail-meet sibling \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. Its shared cofinal threshold is exactly the row consumed at $M$, so the tail-fusion packet cannot read its finite witnesses as independent unmerged tails.

Now use the limit-transport sibling \autoref{ch:concrete-instances-regularcauchylimittransport-namecert}. Its displayed seal transport surface is the finite route that carries the diagonal readback toward the Real-facing seal row, so $L$ is reached only after $M$ and $R$ have been exposed.

Finally apply the Real limit handoff theorem above. Since the carrier omits quotient streams, host real equality, selected limits, unbounded diagonal data, and ambient completeness, the sibling-linked route has no non-displayed escape before the $\RealUp$ consumer reads the diagonal limit row.
\end{proof}

\closureat{RegularCauchyTailFusionUp}{seedStr}

\begin{closurestatus}{\RegularCauchyTailFusionUp}
  \constructivestory{A $\RegularCauchyTailFusionUp$ packet is generated from a precision request, $\RegSeqRatUp$ regular-family row, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, finite tail witnesses, a $\RegularCauchyTailMeetUp$ shared-threshold row, diagonal readback, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-tail-fusion-carrier}, \autoref{def:regular-cauchy-tail-fusion-classifier}, \autoref{thm:regular-cauchy-tail-fusion-namecert-obligations}, \autoref{thm:regular-cauchy-tail-fusion-tail-meet-handoff}, \autoref{thm:regular-cauchy-tail-fusion-window-monotone-handoff}, \autoref{thm:regular-cauchy-tail-fusion-shared-threshold-exhaustion}, \autoref{thm:regular-cauchy-tail-fusion-real-limit-handoff}, and \autoref{thm:regular-cauchy-tail-fusion-limit-transport-link} bind the finite cofinal tail-fusion packet from regular rational tail witnesses through the shared tail-meet row and limit-transport surface to the $\RealUp$ diagonal seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close quotient stream equality, host real equality, external limit selection, unbounded diagonal streams, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, shared tail-meet routing, diagonal readback routing, Real seal handoff, and non-escape for the same finite cofinal tail-fusion packet.}
\end{closurestatus}
