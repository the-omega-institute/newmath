\chapter{A Concrete Naming Certificate for $\FermatStationaryPointUp$}
\label{ch:concrete-instances-fermat-stationary-point-namecert}
\origin{human}

Following Fermat and Bishop--Bridges constructive calculus, the $\FermatStationaryPointUp$ packet records the finite route by which a local extremum witness and a derivative row at an interior point hand off to the stationary-point conclusion. It sits after the derivative quotient interface of \autoref{ch:concrete-instances-derivative-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the constructive interval-continuity route of \autoref{ch:concrete-instances-darboux-continuity-namecert}. The packet names a derivative-neighbour of the Real route; it is not an ambient Real-completeness principle, not an open-cover compactness theorem, not a selected minimizing point, and not quotient equality of functions.

\begin{definition}[Fermat stationary-point carrier]
\label{def:fermat-stationary-point-carrier}
A $\FermatStationaryPointUp$ carrier is a finite $\BHist$ packet
$$
S=(I,F,E,D,Z,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $I$ is the interval source row, carrying the two-sided interior-window data around the inspected point.
\item $F$ is the function graph row, exposing only the displayed values and finite observation windows consumed by the derivative row.
\item $E$ is the local extremum window row, recording that every admitted left and right comparison in the displayed neighbourhood is routed through the same interval and graph rows.
\item $D$ is the $\DerivativeUp$ row at the inspected point, including the finite difference-quotient ledger and nonzero-step boundary.
\item $Z$ is the zero-derivative handoff row, recording that the stationary conclusion reads the derivative row as a zero row after the two extremum-side comparison ledgers have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the interval, graph, extremum window, derivative row, zero handoff, replay, provenance, and local name.
\item $C$ stores displayed $\Cont$ replay from a stationary-point request through $I,F,E,D,Z,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FermatStationaryPointUp$, $\DerivativeUp$, $\RealUp$, $\DyadicRatCoreUp$, $\DarbouxContinuityUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FermatStationaryPointUp}$ row.
\end{enumerate}
The classifier compares accepted packets only after transporting all displayed rows to the same interval source, graph window, local extremum ledger, derivative quotient row, and zero-derivative handoff. The carrier contains no coordinate for a selected global optimum, trichotomy, host function equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Fermat stationary-point NameCert obligations]
\label{thm:fermat-stationary-point-namecert-obligations}
For an accepted $\FermatStationaryPointUp$ carrier $S=(I,F,E,D,Z,H,C,P,N)$, the local $\NameCert_{\FermatStationaryPointUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed stationary-point packet $S$;
\item interval-source exposure through $I$;
\item function-graph exposure through $F$;
\item local extremum window exactness through $E$;
\item derivative-row admission through $D$;
\item zero-derivative handoff through $Z$;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No obligation exports a selected global optimum, trichotomy, host function equality, open-cover compactness, quotient equality of functions, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fermat-stationary-point-carrier}. Its displayed rows are exactly the interval source, function graph, local extremum window, derivative row, zero-derivative handoff, transport, replay, provenance, and local naming row.

The NameCert obligations are therefore the admission and exactness checks for those coordinates. The interval source reads $I$; the graph read opens $F$; the local extremum comparisons read $E$; the derivative quotient data read $D$; and the stationary handoff reads $Z$.

The structural rows $H,C,P,N$ preserve this same finite packet under classifier transport and consumer replay. Since the carrier contains no selected global optimum, trichotomy, host function equality, open-cover compactness, quotient function equality, or Real-completeness coordinate, the displayed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Fermat stationary-point derivative-zero handoff]
\label{thm:fermat-stationary-point-derivative-zero-handoff}
Every stationary-point consumer of an accepted $\FermatStationaryPointUp$ carrier factors through the finite route
$$
I,F,E \longrightarrow D \longrightarrow Z.
$$
The consumer first reads the interval source, function graph, and local extremum window; it then reads the $\DerivativeUp$ quotient row and finally the zero-derivative handoff. No route out of the packet can bypass this carrier through a selected extremal point, trichotomy, host equality of functions, open-cover compactness, quotient function equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an accepted carrier and project its rows. The only interval coordinate is $I$, the only graph coordinate is $F$, and the only local extremum coordinate is $E$.

The derivative row $D$ is downstream from those three rows because its difference-quotient comparisons are read on the displayed interval and graph windows. The zero handoff $Z$ is downstream from $D$ because it names the stationary conclusion only as a readback of the derivative row after both extremum-side comparison ledgers have been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since no excluded host or compactness coordinate appears in $S$, every stationary-point consumer factors through the displayed finite handoff.
\end{proof}

\closureat{FermatStationaryPointUp}{seedStr}

\begin{closurestatus}{\FermatStationaryPointUp}
  \constructivestory{A $\FermatStationaryPointUp$ packet is generated from an interval source row, a function graph row, a local extremum window, a DerivativeUp row, a zero-derivative handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fermat-stationary-point-carrier}, \autoref{thm:fermat-stationary-point-namecert-obligations}, and \autoref{thm:fermat-stationary-point-derivative-zero-handoff} seed the finite local-extremum-to-zero-derivative route over $\DerivativeUp$, $\RealUp$, $\DyadicRatCoreUp$, $\DarbouxContinuityUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Real completeness, compactness, trichotomy, global extrema, quotient function equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, interval-source exposure, graph exposure, local-extremum window exactness, derivative-row admission, zero-derivative handoff, transport, replay, provenance, and local naming for BEDC.Derived.FermatStationaryPointUp.}
\end{closurestatus}
