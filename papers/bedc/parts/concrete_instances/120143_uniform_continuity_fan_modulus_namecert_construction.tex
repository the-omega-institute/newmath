\chapter{A Concrete Naming Certificate for $\UniformContinuityFanModulusUp$}
\label{ch:concrete-instances-uniform-continuity-fan-modulus-namecert}
\origin{human}

Following Brouwer and Bishop fan-theorem constructive analysis, the $\UniformContinuityFanModulusUp$ packet records the finite bar route by which pointwise continuity on a compact metric source yields one displayed uniform-modulus row. It sits over the compact finite-net data of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map pointwise rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational arithmetic of \autoref{ch:concrete-instances-rat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the downstream $\UniformModulusUp$ consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names finite fan-bar inspection data only; it does not import open-cover compactness, a selected infinite branch, semantic continuity, countable choice, quotient real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Uniform-continuity fan-modulus carrier]
\label{def:uniform-continuity-fan-modulus-carrier}
A $\UniformContinuityFanModulusUp$ carrier is a finite $\BHist$ packet
$$
F=(K,M,A,B,R,E,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row, exposing the inspected centers and cover radii.
\item $M$ is the $\ContinuousMapUp$ pointwise-continuity row for the map on that compact source.
\item $A$ is the finite fan-bar row whose nodes record the inspected source windows and their local continuity radii.
\item $B$ is the finite $\mathsf{ProbeBundle}$ induction ledger that certifies every accepted source request meets a bar node.
\item $R$ is the $\RatUp$ lower-bound fold row over the finitely many positive local radii.
\item $E$ is the $\RealUp$ seal row used only after the rational fold and target-value readbacks are displayed.
\item $U$ is the $\UniformModulusUp$ consumer row that receives the folded radius as the tolerance-only modulus boundary.
\item $H$ records componentwise $\hsame$ transport for compact-net, continuous-map, fan-bar, bundle, rational-fold, Real-seal, uniform-modulus, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from compact finite-net inspection through fan-bar induction and rational lower-bound folding to the uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\UniformContinuityFanModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RatUp$, $\RealUp$, $\UniformModulusUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformContinuityFanModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for an infinite branch selector, open-cover compactness, host equality of real functions, countable choice, quotient real equality, semantic continuity, or ambient completeness.
\end{definition}

\begin{theorem}[Uniform-continuity fan-modulus NameCert obligations]
\label{thm:uniform-continuity-fan-modulus-namecert-obligations}
For every accepted $\UniformContinuityFanModulusUp$ carrier $F=(K,M,A,B,R,E,U,H,C,P,N)$, the local $\NameCert_{\UniformContinuityFanModulusUp}$ surface consists exactly of compact finite-net exposure, continuous-map pointwise exposure, finite fan-bar admission, $\mathsf{ProbeBundle}$ induction, rational lower-bound folding, Real-seal readback, $\UniformModulusUp$ consumer handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A consumer may read the uniform modulus only after $K,M,A,B,R,E$ have all been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-continuity-fan-modulus-carrier}. The tuple lists the compact metric source, pointwise continuous-map row, fan-bar row, bundle-induction ledger, rational fold, Real seal, uniform-modulus output, and structural support rows as the whole accepted surface.

The finite fan-bar stage is $A,B$. The bar row lists the inspected nodes and local radii, while the $\mathsf{ProbeBundle}$ induction ledger proves that every accepted source request reaches one of those nodes. This is a finite induction over displayed bundle entries, not a selected infinite branch.

The modulus stage is $R,E,U$. The rational row folds the displayed positive radii to one lower-bound row, the Real row seals the scalar readback only after that fold is visible, and the uniform-modulus row receives exactly that finite boundary. The rows $H,C,P,N$ transport, replay, source, and name the same packet, so the local ledger cannot export open-cover compactness, semantic continuity, choice, quotient equality, or ambient completeness.
\end{proof}

\begin{theorem}[Uniform-continuity fan-modulus fan-bar route]
\label{thm:uniform-continuity-fan-modulus-fanbar-route}
Every $\UniformModulusUp$ consumer of an accepted $\UniformContinuityFanModulusUp$ packet factors through the finite fan-bar route
$$
\CompactMetricUp,\ \ContinuousMapUp
\longmapsto
A,B
\longmapsto
\RatUp,\ \RealUp
\longmapsto
\UniformModulusUp .
$$
Thus the consumer reads compact finite-net coverage, pointwise continuity rows, finite fan-bar nodes, $\mathsf{ProbeBundle}$ induction, rational lower-bound folds, the terminal Real seal, componentwise transport, replay, provenance, and the local NameCert ledger before exporting any modulus data.
\end{theorem}
\begin{proof}
Start with $K$ and $M$. The compact metric row supplies the finite source inspection surface, and the continuous-map row supplies only pointwise local estimates attached to that same source.

Next read $A$ and $B$. The fan-bar row stores the finite bar nodes, and the bundle induction ledger certifies coverage of the inspected source requests. Transport by $H$ and replay by $C$ keep these rows componentwise stable under the accepted classifier.

Finally fold through $R$, seal through $E$, and hand the result to $U$. The provenance row $P$ and local name row $N$ record that the exported modulus is the displayed finite route. Since no coordinate stores an infinite branch, open-cover compactness, selected representative, countable choice, host real equality, or ambient completeness, none of those objects can be read by the downstream $\UniformModulusUp$ consumer.
\end{proof}

\closureat{UniformContinuityFanModulusUp}{seedStr}

\begin{closurestatus}{\UniformContinuityFanModulusUp}
  \constructivestory{A $\UniformContinuityFanModulusUp$ packet is generated from CompactMetricUp finite-net data, ContinuousMapUp pointwise rows, a finite fan-bar row, a ProbeBundle induction ledger, RatUp lower-bound folds, a RealUp seal, a UniformModulusUp consumer row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-continuity-fan-modulus-carrier}, \autoref{thm:uniform-continuity-fan-modulus-namecert-obligations}, and \autoref{thm:uniform-continuity-fan-modulus-fanbar-route} seed the finite fan-bar route from compact finite-net and pointwise-continuity rows to a UniformModulus consumer boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, infinite branch selection, semantic continuity, quotient real equality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, continuous-map pointwise exposure, fan-bar coverage, ProbeBundle induction, rational lower-bound fold exactness, RealUp sealing, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.UniformContinuityFanModulusUp.}
\end{closurestatus}
