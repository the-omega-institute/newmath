\chapter{A Concrete Naming Certificate for $\BishopRealCompletionUp$}
\label{ch:concrete-instances-bishop-real-completion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopRealCompletionUp$ packet records real completion by regular rational names with located finite observation data. It sits on the dyadic arithmetic substrate of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence packet of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the located completion route of \autoref{ch:concrete-instances-bishop-located-real-completion-namecert}.

\begin{definition}[Bishop real completion carrier]
\label{def:bishop-real-completion-carrier}
A $\BishopRealCompletionUp$ carrier is a finite $\BHist$ packet
$$
R=(D,S,Q,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ precision ledger whose positive dyadic rows name the tolerances inspected by the completion route;
\item $S$ is the $\StreamNameUp$ window ledger naming the finite rational observations read from the candidate Cauchy name;
\item $Q$ is the $\RegSeqRatUp$ readback row exposing the regular rational values visible on the windows in $S$;
\item $L$ is the $\BishopLocatedRealCompletionUp$ located-completion row, recording the located brackets available for the same dyadic precision ledger and stream windows;
\item $E$ is the $\RealUp$ seal-facing row reached from $D,S,Q,L$;
\item $H$ records componentwise $\hsame$ transport for the dyadic ledger, stream windows, regular readbacks, located-completion row, Real-seal row, replay row, provenance row, and local naming row;
\item $C$ records displayed $\Cont$ replay from dyadic precision queries through stream readback, located completion, and Real sealing;
\item $P$ is the $\Pkg$ provenance over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BishopLocatedRealCompletionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\BishopRealCompletionUp}$ evidence.
\end{enumerate}
The carrier contains no quotient real object, no selected limiting representative, no countable-choice schedule, and no ambient completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Bishop real completion NameCert obligations]
\label{thm:bishop-real-completion-namecert-obligations}
For every accepted carrier $R=(D,S,Q,L,E,H,C,P,N)$, the local $\NameCert_{\BishopRealCompletionUp}$ surface consists exactly of carrier admission for the displayed rows; dyadic precision exactness through $D$; finite stream-window readback through $S$; regular rational Cauchy readback through $Q$; located completion handoff through $L$; Real-seal exposure through $E$; componentwise $\hsame$ stability through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is coordinate projection from \autoref{def:bishop-real-completion-carrier}. The packet displays the dyadic precision ledger, finite stream windows, regular rational readbacks, located-completion row, Real-seal row, transport, replay, provenance, and local name as one $\BHist$ surface.

The dyadic and stream obligations are read before the located-completion row. Thus every precision request uses a positive row of $D$, every rational observation enters through the finite window ledger $S$, and every rational value is exposed by the regular readback row $Q$.

The located-completion and Real-seal obligations are then forced through $L$ and $E$. Since $H$ and $C$ are the only transport and replay rows, any classified mate of the packet preserves the same finite route.

Finally $P$ and $N$ record provenance and local naming for exactly these rows. The carrier inventory has no quotient real, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness coordinate, so the listed obligations exhaust the certificate surface.
\end{proof}

\begin{theorem}[Bishop real completion Real-seal route]
\label{thm:bishop-real-completion-real-seal-route}
Every Real-facing consumer of an accepted $\BishopRealCompletionUp$ packet reaches $\RealUp$ only through the ordered route
$$
\DyadicRatCoreUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \BishopLocatedRealCompletionUp \longrightarrow \RealUp
$$
displayed by $D,S,Q,L,E,H,C,P,N$. The route supplies finite precision, window, readback, located-completion, and seal rows, and it supplies no quotient real equality, no selected limit, no countable-choice schedule, and no ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with an accepted carrier $R=(D,S,Q,L,E,H,C,P,N)$. The only Real-facing row is $E$, and the only rows feeding it are the dyadic ledger $D$, stream windows $S$, regular rational readbacks $Q$, and located-completion row $L$.

Use the NameCert obligations of \autoref{thm:bishop-real-completion-namecert-obligations}. They force precision queries, observations, regular readbacks, located completion, and Real sealing to pass through the same displayed carrier rows.

Transport and replay do not add a second route. The rows $H$ and $C$ preserve and replay the displayed path componentwise, while $P$ and $N$ record its provenance and local name.

The carrier excludes quotient real equality, selected limits, countable-choice schedules, and ambient completeness. Hence every Real-facing consumer receives exactly the finite Real-seal route stated above.
\end{proof}

\closureat{BishopRealCompletionUp}{seedStr}

\begin{closurestatus}{\BishopRealCompletionUp}
  \constructivestory{A $\BishopRealCompletionUp$ packet is generated from a dyadic precision ledger, finite StreamName windows, RegSeqRat readbacks, a BishopLocatedRealCompletion row, a Real seal row, componentwise transport, replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-completion-carrier}, \autoref{thm:bishop-real-completion-namecert-obligations}, and \autoref{thm:bishop-real-completion-real-seal-route} name the seed carrier, finite NameCert obligation surface, and Real-seal route for Bishop real completion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not assert quotient reals, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic precision exactness, finite stream-window readback, regular rational readback, located-completion handoff, Real-seal exposure, transport stability, replay, provenance, and local naming for the displayed Bishop real completion packet.}
\end{closurestatus}
