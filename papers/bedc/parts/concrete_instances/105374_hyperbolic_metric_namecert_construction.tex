\chapter{A Concrete Naming Certificate for $\HyperbolicMetricUp$}
\label{ch:concrete-instances-hyperbolic-metric-namecert}
\origin{ai}

The $\HyperbolicMetricUp$ packet realizes the Poincare-disk metric reading requested by \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite BEDC certificate data. It sits after the disk-coordinate surface of \autoref{ch:concrete-instances-unitdisk-namecert}, the metric readback of \autoref{ch:concrete-instances-metric-namecert}, the real and complex coordinate surfaces of \autoref{ch:concrete-instances-real-namecert} and \autoref{ch:concrete-instances-complex-namecert}, the finite flow rows of \autoref{ch:concrete-instances-dynsystem-namecert}, and the disk-phase transport packet of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}. The chapter names a licensed metric-read surface for hyperbolic geometry without turning the host disk, boundary circle, or ambient metric space into a primitive model.

\begin{definition}[Hyperbolic metric carrier]
\label{def:hyperbolic-metric-carrier}
A $\HyperbolicMetricUp$ carrier is a finite $\BHist$ packet
$$
M=(D,R,B,S,V,F,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the disk-map row, exposing the finite $\UnitDiskUp$ coordinate observations and their admissible radius comparisons.
\item $R$ is the metric-read row, naming the finite $\MetricUp$ distance observations and the $\RealUp$ readback rows used to compare them.
\item $B$ is the boundary row, recording which observations approach the disk boundary and refusing to treat the boundary as an interior point.
\item $S$ is the classifier-stability row, preserving disk-coordinate, metric-read, and boundary classifiers under the finite observations consumed by the packet.
\item $V$ is the $\DynSystemUp$ compatibility row for finite evolution of displayed disk-coordinate observations.
\item $F$ is the refusal row excluding host hyperbolic planes, quotient boundary equality, completed geodesic space, physical cosmology, and internal observer-substance readings.
\item $A$ is the audit row tying $D,R,B,S,V,F$ to the hyperbolic phase vision and to the disk-phase transport packet.
\item $H$ records componentwise $\hsame$ transport for disk-map, metric-read, boundary, classifier-stability, flow, refusal, and audit rows.
\item $C$ records displayed $\Cont$ replay through finite disk observations, metric reads, boundary checks, classifier stability, provenance, and refusal.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicMetricUp$, $\UnitDiskUp$, $\MetricUp$, $\RealUp$, $\ComplexUp$, $\DynSystemUp$, $\PoincareDiskPhaseTransportUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicMetricUp}$ row.
\end{enumerate}
The carrier exports a hyperbolic metric-read packet only through these rows. It contains no primitive hyperbolic plane, no quotient boundary, no completed geodesic model, no physical constant, no ambient Real completeness principle, and no object-level observer substance.
\end{definition}

\begin{theorem}[Hyperbolic metric NameCert obligations]
\label{thm:hyperbolic-metric-namecert-obligations}
For every accepted $\HyperbolicMetricUp$ carrier
$$
M=(D,R,B,S,V,F,A,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicMetricUp}$ surface consists exactly of disk-map admission, metric-read admission, boundary-row admission, classifier stability, finite flow compatibility, refusal exactness, audit provenance, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every metric consumer must factor through
$$
D,R,B \longmapsto S,V,F,A \longmapsto H,C,P,N.
$$
Thus hyperbolic geometry is read as a finite disk-map and metric-read certificate, not as a primitive ambient model.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-metric-carrier}. The disk-map row $D$ is visible before any metric value is read, and the metric-read row $R$ is tied to displayed $\MetricUp$ and $\RealUp$ observations.

Read the boundary and stability rows next. The row $B$ records boundary-facing observations without identifying them with interior disk points, while $S$ preserves the disk-map, metric-read, and boundary classifiers across the inspected data.

The finite flow row $V$ and refusal row $F$ keep the packet aligned with the hyperbolic phase reading without exporting host geometry. The audit row $A$ records that this is a metric-read certificate over the disk-phase transport route.

Finally $H,C,P,N$ transport, replay, source, and name only these displayed rows. Since the carrier has no coordinate for a primitive hyperbolic plane, quotient boundary, completed geodesic model, physical cosmology, or observer substance, no consumer can extract those readings from the NameCert surface.
\end{proof}

\falsifiablePrediction{Omitting the disk-map row, metric-read row, or boundary row prevents carrier acceptance because the packet can no longer distinguish finite Poincare-disk metric reading from a primitive host geometry.}

\independenceWitness{unitdisk, metric, real, complex, dynsystem: none is bijective to $\HyperbolicMetricUp$ because the packet jointly carries disk-map observations, metric-read rows, boundary checks, classifier stability, finite flow compatibility, refusal exactness, audit provenance, transport, replay, package sourcing, and local naming.}

The Lean follow-up for this packet must provide explicit $\mathsf{Nontrivial}\ \HyperbolicMetricUp$ and $\mathsf{FieldFaithful}\ \HyperbolicMetricUp$ TasteGate instances. Those instances must keep $D,R,B,S,V,F,A,H,C,P,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{HyperbolicMetricUp}{seedStr}

\begin{closurestatus}{\HyperbolicMetricUp}
  \constructivestory{A $\HyperbolicMetricUp$ packet is generated from UnitDisk disk-map observations, Metric and Real metric-read rows, boundary rows, classifier stability, DynSystem flow compatibility, refusal exactness, audit provenance, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-metric-carrier} and \autoref{thm:hyperbolic-metric-namecert-obligations} seed the finite Poincare-disk metric-read surface through disk-map, metric-read, boundary, classifier-stability, provenance, refusal, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close primitive hyperbolic plane geometry, quotient boundary equality, completed geodesic space, physical cosmology, observer-substance readings, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, disk-map rows, metric-read rows, boundary rows, classifier stability, finite flow compatibility, refusal exactness, TasteGate nontriviality, field faithfulness, and local naming for BEDC.Derived.HyperbolicMetricUp.}
\end{closurestatus}
