\chapter{A Concrete Naming Certificate for $\HyperbolicMetricUp$}
\label{ch:concrete-instances-hyperbolic-metric-namecert}
\origin{human}

Following Gromov's four-point metric viewpoint and Kelley's topology-side use of metric balls, the $\HyperbolicMetricUp$ packet realizes hyperbolic metric geometry as finite BEDC certificate data. It sits after the metric readback of \autoref{ch:concrete-instances-metric-namecert}, the center-radius surface of \autoref{ch:concrete-instances-metricball-namecert}, the ordered-real comparison rows of \autoref{ch:concrete-instances-real-namecert}, the topology handoff of \autoref{ch:concrete-instances-topology-namecert}, and the bounded-set ledger of \autoref{ch:concrete-instances-boundedset-namecert}. The chapter names a finite four-point metric surface without turning the host disk, smooth hyperbolic manifold, ideal boundary, or ambient metric completion into a primitive model.

\begin{definition}[Hyperbolic metric carrier]
\label{def:hyperbolic-metric-carrier}
A $\HyperbolicMetricUp$ carrier is a finite $\BHist$ packet
$$
M=(X,d,b,t,\beta,q,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the finite $\MetricUp$ carrier row whose displayed distance comparisons are the only metric data read by the packet.
\item $d$ is the $\RealUp$ valued distance row, naming the finite comparisons used by the four-point ledger.
\item $b$ is the $\MetricBallUp$ neighborhood row, exposing center-radius packets that can be transported without an ambient powerset.
\item $t$ is the $\TopologyUp$ handoff row, reading the ball-neighborhood surface as an indexed-open request.
\item $\beta$ is the $\BoundedSetUp$ boundary ledger, recording bounded containment data for the displayed ball rows.
\item $q$ is the four-point inequality row, comparing the three paired sums determined by four displayed metric observations.
\item $F$ is the refusal row excluding smooth hyperbolic manifolds, quotient ideal boundaries, host geodesic completions, ambient Real completeness, physical cosmology, and internal observer-substance readings.
\item $H$ records componentwise $\hsame$ transport for the metric carrier, distance, ball, topology, bounded-set, four-point, and refusal rows.
\item $C$ records displayed $\Cont$ replay through finite metric observations, ball-neighborhood transport, topology handoff, bounded containment, four-point comparison, provenance, and refusal.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicMetricUp$, $\MetricUp$, $\MetricBallUp$, $\RealUp$, $\TopologyUp$, $\BoundedSetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicMetricUp}$ row.
\end{enumerate}
The carrier exports a hyperbolic metric-read packet only through these rows. It contains no primitive smooth hyperbolic space, no quotient boundary, no completed geodesic model, no host powerset of balls, no ambient Real completeness principle, and no object-level observer substance.
\end{definition}

\begin{theorem}[Hyperbolic metric NameCert obligations]
\label{thm:hyperbolic-metric-namecert-obligations}
For every accepted $\HyperbolicMetricUp$ carrier
$$
M=(X,d,b,t,\beta,q,F,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicMetricUp}$ surface consists exactly of metric-carrier admission, real-valued distance readback, metric-ball neighborhood transport, topology handoff, bounded-set boundary ledger, four-point inequality exposure, refusal exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every metric consumer must factor through
$$
X,d,b \longmapsto t,\beta,q,F \longmapsto H,C,P,N.
$$
Thus hyperbolic geometry is read as a finite four-point metric certificate, not as a primitive ambient model.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-metric-carrier}. The metric carrier row $X$ is visible before any distance is read, and the distance row $d$ is tied to displayed $\MetricUp$ and $\RealUp$ observations.

Read the ball, topology, and bounded rows next. The row $b$ records center-radius neighborhoods, $t$ exposes the topology-side indexed-open request, and $\beta$ records the bounded-set boundary ledger without creating a host subset.

The four-point row $q$ is the only hyperbolic comparison row. The refusal row $F$ keeps smooth hyperbolic geometry, quotient boundary equality, completed geodesic space, physical cosmology, and ambient Real completeness outside the packet.

Finally $H,C,P,N$ transport, replay, source, and name only these displayed rows. Since the carrier has no coordinate for a primitive smooth hyperbolic space, quotient boundary, completed metric model, physical cosmology, or observer substance, no consumer can extract those readings from the NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic metric four-point obligation surface]
\label{thm:hyperbolic-metric-four-point-obligation-surface}
For every accepted $\HyperbolicMetricUp$ carrier $M=(X,d,b,t,\beta,q,F,H,C,P,N)$, carrier admission, four-point inequality exposure, ball-neighborhood transport, bounded-set boundary reading, topology handoff, and non-escape from smooth hyperbolic geometry or host metric completion are available only through the displayed rows
$$
X,d,b,t,\beta,q,F,H,C,P,N.
$$
The row $q$ may compare four displayed points by the usual hyperbolic four-point ledger, but it may not import a smooth manifold, an ideal-boundary compactification, a selected geodesic model, a host powerset of balls, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Carrier admission is the projection of \autoref{def:hyperbolic-metric-carrier}. It exposes $X$ as the metric carrier and $d$ as the real-valued distance row before any hyperbolic comparison is inspected.

The four-point surface is row $q$. It reads only the finite distance comparisons supplied by $d$, so the hyperbolic inequality is a ledger entry over displayed observations rather than a theorem about an ambient smooth model.

Ball-neighborhood transport uses $b$ together with $H$ and $C$. The same transported center-radius rows are handed to $t$ for the topology request and to $\beta$ for bounded-set boundary accounting.

The refusal row $F$ is then preserved by the structural rows $H,C,P,N$. Hence every consumer receives a finite NameCert surface over metric, ball, real, topology, bounded-set, transport, replay, provenance, and naming rows, with no coordinate from which smooth hyperbolic geometry or host metric completion can be recovered.
\end{proof}

\falsifiablePrediction{Omitting the metric carrier row, distance row, metric-ball row, or four-point row prevents carrier acceptance because the packet can no longer distinguish finite hyperbolic metric reading from a primitive host geometry.}

\independenceWitness{metric, metricball, real, topology, boundedset: none is bijective to $\HyperbolicMetricUp$ because the packet jointly carries metric carrier data, real-valued distance reads, ball-neighborhood rows, topology handoff, bounded-set boundary accounting, four-point comparison, refusal exactness, transport, replay, package sourcing, and local naming.}

The Lean follow-up for this packet must provide explicit $\mathsf{Nontrivial}\ \HyperbolicMetricUp$ and $\mathsf{FieldFaithful}\ \HyperbolicMetricUp$ TasteGate instances. Those instances must keep $X,d,b,t,\beta,q,F,H,C,P,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{HyperbolicMetricUp}{seedStr}

\begin{closurestatus}{\HyperbolicMetricUp}
  \constructivestory{A $\HyperbolicMetricUp$ packet is generated from Metric carrier observations, Real distance reads, MetricBall neighborhoods, Topology handoff, BoundedSet boundary accounting, a four-point comparison ledger, refusal exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-metric-carrier}, \autoref{thm:hyperbolic-metric-namecert-obligations}, and \autoref{thm:hyperbolic-metric-four-point-obligation-surface} seed the finite four-point metric surface through metric, ball, real, topology, bounded-set, refusal, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close smooth hyperbolic manifold geometry, quotient boundary equality, host metric completion, physical cosmology, observer-substance readings, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, metric rows, ball rows, real comparison rows, topology handoff, bounded-set boundary accounting, four-point ledger exactness, TasteGate nontriviality, field faithfulness, and local naming for BEDC.Derived.HyperbolicMetricUp.}
\end{closurestatus}
