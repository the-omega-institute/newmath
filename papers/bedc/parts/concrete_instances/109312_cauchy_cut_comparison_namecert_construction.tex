\chapter{A Concrete Naming Certificate for $\CauchyCutComparisonUp$}
\label{ch:concrete-instances-cauchy-cut-comparison-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyCutComparisonUp$ packet names the finite comparison surface between the located-cut presentation of \autoref{ch:concrete-instances-cauchy-cut-namecert} and the regular-Cauchy equality surface of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}. It reads the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal seal boundary of \autoref{ch:concrete-instances-real-namecert}. The packet compares presentations by displayed rows only; it does not identify quotient representatives, select limits, invoke countable choice, or claim ambient $\RealUp$ completeness.

\begin{definition}[Cauchy cut comparison carrier]
\label{def:cauchy-cut-comparison-carrier}
A $\CauchyCutComparisonUp$ carrier is a finite $\BHist$ packet
$$
J=(K,Q,S,R,D,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the cut-side exposure row. It records the accepted $\CauchyCutUp$ lower--upper located-cut route before any equality-facing comparison is read.
\item $Q$ is the setoid-side exposure row. It records the accepted $\CauchyEquivalenceSetoidUp$ regular-Cauchy comparison surface.
\item $S$ is the synchronized $\StreamNameUp$ window ledger shared by the cut-to-stream route and the setoid equality route.
\item $R$ is the $\RegSeqRatUp$ readback row, displaying the regular rational observations consumed by both sides.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger that records bracket, midpoint, and comparison tolerances at the same requested precision.
\item $A$ is the $\RealUp$ seal boundary reached only after $K,Q,S,R,D$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for the cut-side row, setoid-side row, synchronized windows, readback, dyadic tolerance, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a comparison request through the cut side, the setoid side, the synchronized window ledger, regular readback, dyadic tolerance, and then the seal row.
\item $P$ is the $\Pkg$ provenance over $\CauchyCutComparisonUp$, $\CauchyCutUp$, $\CauchyEquivalenceSetoidUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCutComparisonUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $K,Q,S,R,D,A,H,C,P,N$. It has no coordinate for quotient equality, selected representatives, host real equality, countable choice, or ambient Real completeness.
\end{definition}

\begin{theorem}[Cauchy cut comparison NameCert obligations]
\label{thm:cauchy-cut-comparison-namecert-obligations}
For every accepted $\CauchyCutComparisonUp$ carrier $J=(K,Q,S,R,D,A,H,C,P,N)$, the local $\NameCert_{\CauchyCutComparisonUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed cut-side exposure, setoid-side exposure, synchronized window ledger, regular readback, dyadic tolerance ledger, Real seal boundary, transport, continuation, provenance, and local naming row;
\item cut-side exposure, requiring the located lower--upper cut rows of $\CauchyCutUp$ to be visible before any comparison consumes the regular-Cauchy equality surface;
\item setoid-side exposure, requiring the $\CauchyEquivalenceSetoidUp$ windows, readbacks, tolerance tests, and equality-facing classifier row to remain displayed;
\item synchronized tolerance, requiring $S,R,D$ to provide the same finite precision request to the cut-to-stream route and the regular-Cauchy setoid route;
\item Real seal discipline, requiring $A$ to remain downstream of $K,Q,S,R,D$;
\item ledger refusal, excluding quotient equality, selected representatives, host real equality, countable choice, and ambient Real completeness.
\end{enumerate}
No NameCert obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-cut-comparison-carrier}. The projection exposes the cut-side row, setoid-side row, synchronized windows, regular readback, dyadic tolerance, Real seal, and the structural rows $H,C,P,N$.

The cut-side and setoid-side obligations are forced by the source rows $K$ and $Q$. The row $K$ displays the located-cut-to-stream route, while $Q$ displays the regular-Cauchy equality surface; neither row contains a quotient or selected representative coordinate.

The synchronized tolerance obligation is the shared route through $S,R,D$. Replay by $C$ sends a comparison request through the same finite windows, regular rational observations, and dyadic tolerance entries before the seal row $A$ is available.

Transport by $H$ preserves each displayed component, and $P,N$ source and name only this finite packet. Therefore the local certificate is exhausted by the listed obligations.
\end{proof}

\begin{theorem}[Cauchy cut comparison route]
\label{thm:cauchy-cut-comparison-route}
Let $J=(K,Q,S,R,D,A,H,C,P,N)$ be an accepted $\CauchyCutComparisonUp$ packet. Every comparison read between the located-cut presentation and the regular-Cauchy setoid presentation factors through the finite route
$$
K,Q \longmapsto S \longmapsto R \longmapsto D \longmapsto A \longmapsto H,C,P,N .
$$
Thus the located-cut side and the regular-Cauchy setoid side are compared only after their StreamName windows, RegSeqRat readback rows, DyadicRatCore tolerances, and Real seal boundary have been displayed. The route contains no quotient equality, selected limit, countable-choice selector, host real equality, or ambient Real completeness principle.
\end{theorem}
\begin{proof}
Begin with a comparison-facing consumer. Carrier admission gives access to $K$ and $Q$ only as displayed rows: $K$ exposes the located-cut-to-stream route from \autoref{ch:concrete-instances-cauchy-cut-namecert}, and $Q$ exposes the regular-Cauchy classifier route from \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}.

Replay the continuation row $C$. The request next consumes the synchronized StreamName ledger $S$, the RegSeqRat readback row $R$, and the DyadicRatCore tolerance ledger $D$ at the same precision boundary.

Only after this finite replay can the seal boundary $A$ be read. Componentwise transport, provenance, and local naming preserve the same dependency order, so the comparison remains a displayed presentation comparison rather than a quotient, selection, choice, host-equality, or completeness claim.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyCutComparisonUp$ read that compares located-cut and regular-Cauchy presentations factors through displayed $\CauchyCutUp$, $\CauchyEquivalenceSetoidUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.}

\independenceWitness{cauchy-cut, cauchy-equivalence-setoid, streamname, regseqrat, dyadicratcore, real: $\CauchyCutComparisonUp$ is not reducible to any listed sibling because it binds the cut-side exposure and setoid-side exposure to one synchronized tolerance ledger and one Real seal boundary.}

\closureat{CauchyCutComparisonUp}{seedStr}

\begin{closurestatus}{\CauchyCutComparisonUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-cut-comparison-carrier}, \autoref{thm:cauchy-cut-comparison-namecert-obligations}, and \autoref{thm:cauchy-cut-comparison-route} seed the comparison surface between located-cut and regular-Cauchy presentations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cut-side exposure, setoid-side exposure, synchronized tolerance, Real seal discipline, and ledger refusal for BEDC.Derived.CauchyCutComparisonUp.}
  \constructivestory{A $\CauchyCutComparisonUp$ packet is generated from displayed CauchyCut and CauchyEquivalenceSetoid rows, synchronized StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a Real seal, componentwise transport, displayed replay, package provenance, and a local NameCert row.}
\end{closurestatus}
