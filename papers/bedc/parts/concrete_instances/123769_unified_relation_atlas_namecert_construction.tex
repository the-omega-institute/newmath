\chapter{A Concrete Naming Certificate for $\UnifiedRelationAtlasUp$}
\label{ch:concrete-instances-unified-relation-atlas-namecert}
\origin{ai}

The $\UnifiedRelationAtlasUp$ packet realizes the unified relation packet of \autoref{def:unified-reality-relation-packet} as finite carrier, classifier, projection, transport, and ledger rows. It binds the arithmetic, algebraic-coordinate, shape-homology, and zeta-Dirichlet entries already present in the Lean atlas surface, while leaving later TasteGate work to provide the Nontrivial and FieldFaithful instances required for positive discovery classification.

\begin{definition}[Unified relation atlas carrier]
\label{def:unified-relation-atlas-carrier}
A $\UnifiedRelationAtlasUp$ carrier is a finite $\BHist$ packet
$$
A=(K,J,\Pi,T,L,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the carrier row, listing the arithmetic, algebraic-coordinate, shape-homology, and zeta-Dirichlet entry carriers.
\item $J$ is the classifier row, recording when two atlas reads preserve the same entry identity and the same ontic-axis marks.
\item $\Pi$ is the projection row, exposing arithmetic, algebraic-coordinate, shape-homology, and zeta-Dirichlet projections as distinct finite edges.
\item $T$ is the transport row, recording which projection reads may be replayed across the shared carrier boundary.
\item $L$ is the ledger row, recording the finite row-count and boundary bookkeeping attached to each projection entry.
\item $B$ is the boundary row, recording which vision-facing entries are licensed only as atlas entries and not as standalone proof of field faithfulness.
\item $H$ records componentwise $\hsame$ transport for $K,J,\Pi,T,L,B$ and the structural rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
K,J \longmapsto \Pi,T,L,B .
$$
\item $P$ is the $\Pkg$ provenance over $\UnifiedRelationAtlasUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UnifiedRelationAtlasUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the same carrier entries, ontic-axis marks, projection edges, transport row, ledger row, boundary row, componentwise transport, replay, provenance, and local name. It does not assert Nontrivial instances, FieldFaithful instances, analytic continuation, Euler-product validity, RH-facing zero control, or a complete bridge schema.
\end{definition}
\leandef{BEDC.Derived.UnifiedRelationAtlasUp.UnifiedRelationAtlas}

\begin{theorem}[Unified relation atlas projection obligations]
\label{thm:unified-relation-atlas-projection-obligations}
For the carrier of \autoref{def:unified-relation-atlas-carrier}, the local $\NameCert_{\UnifiedRelationAtlasUp}$ surface consists exactly of carrier-entry admission, classifier preservation, projection-edge exposure, transport replay, ledger bookkeeping, boundary non-escape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. In particular, every accepted atlas read must keep the arithmetic, algebraic-coordinate, shape-homology, and zeta-Dirichlet projection edges distinct before any transport-facing consumer may compare them.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:unified-relation-atlas-carrier}. The entry carrier $K$ and classifier $J$ fix the accepted atlas surface before any projection edge is read.

The projection row $\Pi$ displays the arithmetic, algebraic-coordinate, shape-homology, and zeta-Dirichlet edges. The transport and ledger rows $T,L$ then replay and account for those edges without identifying them.

The boundary row $B$ prevents a vision-facing entry from being read as a completed field-faithfulness or nontriviality theorem. It is an atlas boundary, not a replacement for the later TasteGate obligations.

Finally $H,C,P,N$ transport, replay, source, and name exactly the same finite packet. Thus the local NameCert surface is exhausted by the listed rows.
\end{proof}
\leanchecked{BEDC.Derived.UnifiedRelationAtlasUp.unifiedRelationAtlas\_projection\_rows}

\begin{theorem}[Unified relation atlas edge separation]
\label{thm:unified-relation-atlas-edge-separation}
Every accepted $\UnifiedRelationAtlasUp$ projection consumer that compares the arithmetic and algebraic-coordinate edges, or the algebraic-coordinate and zeta-Dirichlet edges, must factor through the displayed projection row $\Pi$ and the classifier row $J$. The atlas therefore supplies finite projection separation, not a claim of analytic continuation, Euler-product validity, RH-facing zero control, Nontrivial evidence, FieldFaithful evidence, or bridge checking.
\end{theorem}
\begin{proof}
Read the entry carrier and classifier rows first. They bind each projection to the same atlas packet and require comparison through the displayed identity and axis marks.

Apply the projection row $\Pi$. The arithmetic, algebraic-coordinate, shape-homology, and zeta-Dirichlet reads are named as separate finite edges, and transport replay cannot collapse them.

The boundary and structural rows $B,H,C,P,N$ preserve this separation. Since the excluded analytic and TasteGate witnesses are not carrier coordinates, they cannot be exported by the atlas edge comparison.
\end{proof}
\leanchecked{BEDC.Derived.UnifiedRelationAtlasUp.unifiedRelationAtlas\_distinct\_projection\_edges}

\falsifiablePrediction{An accepted $\UnifiedRelationAtlasUp$ consumer that identifies the arithmetic, algebraic-coordinate, shape-homology, or zeta-Dirichlet projection edges without displaying the carrier, classifier, projection, transport, ledger, boundary, componentwise hsame, Cont replay, provenance, and local NameCert rows refutes the atlas packet.}

\independenceWitness{pythagorean-common-carrier, circle-triangle-projection, algebraic-extension-dimension, mobius-inversion, and geometry-category-homology: none is bijective with $\UnifiedRelationAtlasUp$ because the atlas jointly carries classifier, projection, transport, ledger, boundary, transport, replay, provenance, and local naming rows over several projection entries.}

The atlas projection row consumes three displayed sibling routes: the arithmetic common-carrier route of \autoref{ch:concrete-instances-pythagorean-common-carrier-namecert}, the geometry projection route of \autoref{ch:concrete-instances-circle-triangle-projection-namecert}, and the zeta/Mobius ledger route of \autoref{ch:concrete-instances-mobius-inversion-namecert}. These are projection dependencies in the concrete-instance lattice, not evidence for analytic continuation, Euler-product validity, RH-facing zero control, Nontrivial evidence, or FieldFaithful evidence.

\closureat{UnifiedRelationAtlasUp}{seedStr}[BEDC.Derived.UnifiedRelationAtlasUp.unifiedRelationAtlas\_projection\_rows]

\begin{closurestatus}{\UnifiedRelationAtlasUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:unified-relation-atlas-carrier}, \autoref{thm:unified-relation-atlas-projection-obligations}, and \autoref{thm:unified-relation-atlas-edge-separation} seed the finite carrier, classifier, projection, transport, ledger, boundary, transport, replay, provenance, and local naming surface for the unified relation atlas.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.UnifiedRelationAtlasUp.unifiedRelationAtlas\_projection\_rows}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Nontrivial instances, FieldFaithful instances, analytic continuation, Euler-product validity, RH-facing zero control, positive discovery classification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier preservation, projection-edge exposure, transport replay, ledger bookkeeping, boundary non-escape, and TasteGate evidence for the Nontrivial and FieldFaithful instances.}
  \constructivestory{A $\UnifiedRelationAtlasUp$ packet is generated from atlas carrier entries, classifier marks, projection edges, transport rows, ledger rows, boundary rows, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
