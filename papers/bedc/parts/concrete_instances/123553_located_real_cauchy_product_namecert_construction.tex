\chapter{A Concrete Naming Certificate for $\LocatedRealCauchyProductUp$}
\label{ch:concrete-instances-located-real-cauchy-product-namecert}
\origin{human}

Following Bishop and Bridges, the $\LocatedRealCauchyProductUp$ packet records product Cauchy data for located real names as finite NameCert rows. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the located-real source of \autoref{ch:concrete-instances-locatedreal-namecert}, and the binary product topology of \autoref{ch:concrete-instances-product-topology-namecert}. The packet names paired located Cauchy reads only; it is not quotient equality of real names, a selected product limit, ambient completeness, or a choice principle.

\begin{definition}[LocatedRealCauchyProduct carrier]
\label{def:located-real-cauchy-product-carrier}
A $\LocatedRealCauchyProductUp$ carrier is a finite $\BHist$ packet
$$
P=(X,Y,L_X,L_Y,S_X,S_Y,R_X,R_Y,T,W,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two $\LocatedRealUp$ source-name rows.
\item $L_X$ and $L_Y$ are the located lower and upper rational cut witnesses read from the two source names.
\item $S_X$ and $S_Y$ are the finite $\StreamNameUp$ window rows that make the inspected Cauchy data visible.
\item $R_X$ and $R_Y$ are the $\RegSeqRatUp$ readback rows for the two coordinate names.
\item $T$ is the $\ProductTopologyUp$ row that exposes the binary coordinate product surface.
\item $W$ is the paired Cauchy-window ledger, recording the shared tolerance request and the two coordinate windows inspected for that request.
\item $H$ records componentwise $\hsame$ transport for the located-real, stream-window, regular-rational, product-topology, paired-window, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from located real names to stream windows, regular rational readbacks, product topology, and paired Cauchy windows.
\item $G$ is the $\Pkg$ provenance over $\LocatedRealCauchyProductUp$, $\LocatedRealUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\ProductTopologyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\LocatedRealCauchyProductUp}$ row.
\end{enumerate}
The carrier exports no quotient-name equality, selected representative, countable-choice row, ambient product completion, host real equality, or unscoped $\RealUp$ completeness.
\end{definition}

\begin{theorem}[LocatedRealCauchyProduct obligation surface]
\label{thm:located-real-cauchy-product-obligation-surface}
For every accepted $\LocatedRealCauchyProductUp$ carrier
$$
P=(X,Y,L_X,L_Y,S_X,S_Y,R_X,R_Y,T,W,H,C,G,N),
$$
the local $\NameCert_{\LocatedRealCauchyProductUp}$ surface consists exactly of carrier admission for the two located real source rows, located cut exposure, finite stream-window exposure, regular rational readback, product-topology exposure, paired Cauchy-window ledger exposure, componentwise transport, displayed replay, provenance, and local naming. Every product Cauchy consumer must read the two $\LocatedRealUp$ sources through $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, the $\ProductTopologyUp$ row, and the paired ledger $W$ before treating the product read as public.
\end{theorem}
\begin{proof}
Project the accepted packet $P$. The two located real source rows, their cut witnesses, stream windows, regular rational readbacks, product topology row, paired-window ledger, transport, replay, provenance, and local naming row are all visible coordinates of the same finite carrier.

Read the source siblings in order. The $\LocatedRealUp$ rows supply the located real names, the $\StreamNameUp$ rows expose only finite inspected windows, the $\RegSeqRatUp$ rows provide regular rational readback, and the $\RealUp$ seal is available only through those displayed readbacks.

Next read the product surface. The $\ProductTopologyUp$ row combines the coordinate reads as a binary product route, while $W$ records the shared Cauchy tolerance and the two coordinate windows used for that request.

Finally use $H,C,G,N$ to transport, replay, package, and name the same finite route. Since the carrier has no coordinate for quotient equality, selected representatives, countable choice, ambient completion, host real equality, or unscoped completeness, the obligation surface is exhausted by the displayed rows.
\end{proof}

\falsifiablePrediction{An accepted $\LocatedRealCauchyProductUp$ read is refuted whenever a product Cauchy consumer reaches the product surface without first displaying both located real sources, finite StreamName windows, RegSeqRat readbacks, the ProductTopology row, the paired Cauchy-window ledger, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{real, regseqrat, streamname, locatedreal, product-topology: $\LocatedRealCauchyProductUp$ is not bijective to any listed sibling because it binds two located real source rows, two finite stream-window rows, two regular rational readbacks, a product-topology row, and a paired Cauchy-window ledger into one local NameCert packet.}

\closureat{LocatedRealCauchyProductUp}{seedStr}

\begin{closurestatus}{\LocatedRealCauchyProductUp}
  \constructivestory{A $\LocatedRealCauchyProductUp$ packet is generated from two located real source names, located cut witnesses, finite StreamName windows, RegSeqRat readbacks, a ProductTopology row, a paired Cauchy-window ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-real-cauchy-product-carrier} and \autoref{thm:located-real-cauchy-product-obligation-surface} seed the finite product Cauchy route over located RealUp names, StreamName windows, RegSeqRat readbacks, and ProductTopology rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close ambient completeness, quotient equality of real names, selected representatives, countable choice, product limit existence, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located cut exposure, finite window discipline, RegSeqRat readback exactness, product-topology routing, paired Cauchy-window exactness, transport, replay, provenance, and local naming for BEDC.Derived.LocatedRealCauchyProductUp.}
\end{closurestatus}
