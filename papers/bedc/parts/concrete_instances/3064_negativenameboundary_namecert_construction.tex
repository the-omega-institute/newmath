\chapter{A Concrete Naming Certificate for $\NegativeNameBoundaryUp$}
\label{ch:concrete-instances-negative-name-boundary-namecert}
\origin{human}

The $\NegativeNameBoundaryUp$ packet realizes apophatic negative naming as a finite NameCert surface. It records the name that marks a requested external supply, the socket shape being refused, the refusal ledger that keeps the supply outside generated content, the audit gate that makes the boundary readable, the componentwise transports, the displayed continuation routes, the package provenance, and the local naming row. The apophatic handle row is consumed from \autoref{ch:concrete-instances-apophaticname-namecert}, while $\NegativeNameBoundaryUp$ adds the socket, refusal-ledger, and audit-gate rows that keep the requested supply outside generated content. The packet is a boundary certificate for named refusal, not a source of the requested supply.

The negative-name packet is the refusal-facing boundary between the socket-name surface of \autoref{ch:concrete-instances-apophaticname-namecert} and the visible refusal question of \autoref{ch:concrete-instances-apophaticgatequestion-namecert}. The certificate-side sibling is \autoref{ch:concrete-instances-negativecertificateboundary-namecert}; this chapter controls the name boundary by keeping the requested socket visible while refusing to turn it into supplied content.

\begin{definition}[NegativeNameBoundary carrier]
\label{def:negative-name-boundary-carrier}
A $\NegativeNameBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(a,s,r,g,h,c,p,n)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $a$ is the apophatic handle row naming the requested external supply without describing it as an internal object;
\item $s$ is the requested socket shape, recording which supply interface the surrounding argument asked to consume;
\item $r$ is the refusal and non-internalization ledger, recording each blocked route by which the requested supply might otherwise be treated as generated theorem content, quotient equality, host selector, axiom, or hidden witness;
\item $g$ is the audit gate row, recording the finite question that separates visible boundary use from hidden internalization;
\item $h$ records componentwise $\hsame$ transports for the handle, socket, refusal, gate, route, provenance, and local naming rows;
\item $c$ records the displayed $\Cont$ routes by which consumers inspect the handle, requested socket shape, refusal ledger, audit gate, provenance, and local certificate;
\item $p$ records $\Pkg$ provenance over $\BHist$, apophatic names, socket rows, refusal ledgers, audit gates, $\hsame$, $\Cont$, and $\NameCert$;
\item $n$ is the local $\NameCert_{\NegativeNameBoundaryUp}$ row.
\end{enumerate}
The carrier contains no coordinate for the requested external supply itself.
\end{definition}
\leanchecked{BEDC.Derived.NegativeNameBoundaryUp.NegativeNameBoundaryTasteGate\_single\_carrier\_alignment}

\begin{theorem}[NegativeNameBoundary NameCert obligations]
\label{thm:negative-name-boundary-namecert-obligations}
The local $\NameCert_{\NegativeNameBoundaryUp}$ surface for \autoref{def:negative-name-boundary-carrier} consists exactly of carrier admission for
$$
B=(a,s,r,g,h,c,p,n),
$$
apophatic handle exposure through $a$, requested socket display through $s$, refusal-ledger exhaustion through $r$, audit-gate readback through $g$, componentwise $\hsame$ stability through $h$, displayed $\Cont$ replay through $c$, $\Pkg$ provenance through $p$, and local naming through $n$.
\end{theorem}
\leanchecked{BEDC.Derived.NegativeNameBoundaryUp.NegativeNameBoundaryNameCert\_obligations}
\begin{proof}
Carrier admission is projection from the displayed tuple. The rows $a,s,r,g$ expose the boundary content: the name, the requested socket shape, the refusal ledger, and the audit gate.

Transport and consumer access are confined by $h$ and $c$. These rows replay the same boundary packet under componentwise $\hsame$ movement and expose only the recorded continuation routes.

The provenance and local naming rows $p,n$ account for package ancestry and the local certificate. Since the requested external supply has no coordinate in $B$, the NameCert surface can name the negative boundary without exporting the supply as internal content.
\end{proof}

\begin{theorem}[NegativeNameBoundary refusal ledger]
\label{thm:negative-name-boundary-refusal-ledger}
For an accepted $\NegativeNameBoundaryUp$ carrier, the refusal ledger $r$ records every public attempt to use the requested socket shape $s$ as generated theorem content, quotient equality, host selector, axiom, hidden witness, or anonymous supply route. Each such attempt is routed through $r$ and remains audit-visible through $g,c,p,n$.
\end{theorem}
\leanchecked{BEDC.Derived.NegativeNameBoundaryUp.NegativeNameBoundary\_refusal\_ledger}
\begin{proof}
Project the requested socket row $s$ and the refusal ledger $r$. The ledger is attached to exactly the routes by which the requested supply would be converted into internal content.

The audit gate row $g$ makes the refusal question public, while the continuation routes $c$ replay that gate and ledger for downstream consumers. Componentwise transport in $h$ preserves the same refusal rows when the packet is moved across equivalent presentations.

The provenance and local naming rows $p,n$ identify the packet as the certified source of the refusal. Therefore a public use of the requested socket shape must either pass through the displayed refusal ledger or have no accepted coordinate in the packet.
\end{proof}

\begin{theorem}[NegativeNameBoundary non-internalization]
\label{thm:negative-name-boundary-non-internalization}
Every public use of an accepted $\NegativeNameBoundaryUp$ packet factors through the rows
$$
a,s,r,g,h,c,p,n.
$$
The packet may expose the apophatic handle, the requested socket shape, the refusal ledger, the audit gate, transports, continuation routes, provenance, and local naming, but it cannot export the requested external supply as generated theorem content, quotient equality, host selector, axiom, hidden witness, or unrecorded internal object.
\end{theorem}
\begin{proof}
Begin with the carrier inventory of \autoref{def:negative-name-boundary-carrier}. The requested external supply itself is absent; only its handle, socket shape, refusal ledger, audit gate, transport, route, provenance, and local naming rows are present.

Let a public consumer of the packet be given. By the NameCert obligations of \autoref{thm:negative-name-boundary-namecert-obligations}, every displayed consumer route must read the packet through $c$ and preserve the same finite surface through $h$.

If the consumer tries to use the socket shape as generated theorem content, quotient equality, host selector, axiom, hidden witness, or anonymous supply, apply the refusal ledger of \autoref{thm:negative-name-boundary-refusal-ledger}. The attempt is routed to $r$ and remains visible through the audit gate.

Thus every accepted public use factors through the displayed rows, and no projection from the packet can return the requested external supply as internal substrate content.
\end{proof}

\begin{theorem}[NegativeNameBoundary quotient-soundness lattice link]\label{thm:negative-name-boundary-quotient-soundness-lattice-link}
Every $\QuotientSoundnessBoundaryUp$ representative-selection refusal first reads the $\NegativeNameBoundaryUp$ packet through its apophatic handle, requested socket shape, refusal ledger, audit gate, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row. The quotient-soundness boundary of \autoref{ch:concrete-instances-quotient-soundness-boundary-namecert} then specializes that refusal by replacing the blocked quotient request with local $\psame$ transport rather than a selected representative or a $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:negative-name-boundary-carrier}. Its handle, socket, refusal ledger, audit gate, transport, continuation, provenance, and local naming rows give the visible boundary packet through which a representative-selection request must be inspected.

Apply the refusal ledger theorem. A request to treat the socket as quotient equality, a host selector, an axiom, or a hidden witness is routed through $r$ and remains audit-visible through $g,c,p,n$.

Use non-internalization to remove the forbidden exits. Since the requested external supply is absent from the packet, the sibling quotient-soundness chapter can only specialize the visible refusal by routing surviving operations through its local relation and $\psame$ transport rows.

Thus the dependency on \autoref{ch:concrete-instances-quotient-soundness-boundary-namecert} is a lattice dependency between concrete refusal packets: the negative-name packet supplies the handle, socket, refusal, and gate discipline, and the quotient-soundness packet supplies the classifier-respecting transport that replaces representative selection.
\end{proof}

\begin{theorem}[NegativeNameBoundary obligation closure]
\label{thm:negative-name-boundary-obligation-closure}
For an accepted finite $\NegativeNameBoundaryUp$ packet
$B=(a,s,r,g,h,c,p,n)$, the carrier, refusal ledger, non-internalization row, and
quotient-soundness lattice link are the complete obligation surface. The packet
records the apophatic handle, requested socket, refusal gate, transport,
continuation, provenance, and local name, while excluding host equality,
$\mathsf{Quot.sound}$, propositional extensionality, and hidden negative-name
witnesses.
\end{theorem}
\begin{proof}
Start with \autoref{def:negative-name-boundary-carrier}. The requested external
supply is absent from the packet, while $a,s,r,g,h,c,p,n$ are the displayed
finite rows available to the certificate.

Apply the refusal ledger and non-internalization rows,
\autoref{thm:negative-name-boundary-refusal-ledger} and
\autoref{thm:negative-name-boundary-non-internalization}. They send attempted
uses of the requested socket to the visible refusal ledger and prevent the
requested supply from becoming generated theorem content.

Finally use the quotient-soundness lattice link
\autoref{thm:negative-name-boundary-quotient-soundness-lattice-link}. It routes
representative-selection pressure to local transport rather than to host
equality, $\mathsf{Quot.sound}$, propositional extensionality, or hidden
negative-name witnesses. These rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[NegativeNameBoundary public export]
\label{thm:negative-name-boundary-public-export}
Every public consumer of an accepted $\NegativeNameBoundaryUp$ packet must read the apophatic handle $a$, requested socket $s$, refusal ledger $r$, audit gate $g$, componentwise $\hsame$ transports $h$, displayed $\Cont$ routes $c$, $\Pkg$ provenance $p$, and local $\NameCert$ row $n$. The only allowed downstream specialization is the quotient-soundness lattice link through local $\psame$ transport; no public export may import external supply, $\mathsf{Quot.sound}$, host equality, a selector, an axiom, or hidden witness data.
\end{theorem}
\begin{proof}
Start with the carrier and certificate rows of \autoref{def:negative-name-boundary-carrier} and \autoref{thm:negative-name-boundary-namecert-obligations}. They display $a,s,r,g,h,c,p,n$ as the complete public packet surface.

Use the refusal ledger and non-internalization boundary, \autoref{thm:negative-name-boundary-refusal-ledger} and \autoref{thm:negative-name-boundary-non-internalization}. These rows send every attempted socket use through the visible refusal route and keep the requested external supply outside generated content.

The downstream specialization is exactly the one supplied by \autoref{thm:negative-name-boundary-quotient-soundness-lattice-link}. Combining it with \autoref{thm:negative-name-boundary-obligation-closure} leaves only local $\psame$ transport as the quotient-soundness route, with no public export of host equality, $\mathsf{Quot.sound}$, a selector, an axiom, or hidden witness data.
\end{proof}

\closureat{NegativeNameBoundaryUp}{seedStr}
\closureat{NegativeNameBoundaryUp}{scopedStr}
\closureat{NegativeNameBoundaryUp}{publicStr}
\closureat{NegativeNameBoundaryUp}{bridgedStr}
\closureat{NegativeNameBoundaryUp}{matureStr}

\begin{closurestatus}{\NegativeNameBoundaryUp}
  \constructivestory{A $\NegativeNameBoundaryUp$ packet is generated from an apophatic handle row, requested socket-shape row, refusal and non-internalization ledger, audit gate row, componentwise hsame transports, displayed Cont routes, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:negative-name-boundary-carrier}, \autoref{thm:negative-name-boundary-namecert-obligations}, \autoref{thm:negative-name-boundary-refusal-ledger}, \autoref{thm:negative-name-boundary-non-internalization}, \autoref{thm:negative-name-boundary-quotient-soundness-lattice-link}, \autoref{thm:negative-name-boundary-obligation-closure}, and \autoref{thm:negative-name-boundary-public-export} bind the finite carrier, local naming obligations, refusal ledger, non-internalization boundary, quotient-soundness lattice link, complete obligation surface, and public export route for apophatic negative naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close the requested external supply, positive characterization of the named source, Lean verification, bridge checking, quotient representatives, host selectors, axioms, or hidden witnesses.}
  \upgradepath{No further paper-side theory closure is pending; Lean verification and bridge checking remain on the independent verification axis.}
\end{closurestatus}
