\chapter{A Concrete Naming Certificate for $\FormalBallUp$}
\label{ch:concrete-instances-formalball-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and Vickers-style formal-ball presentations, the $\FormalBallUp$ packet records a radius-indexed observation interface for constructive completion. It sits between the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the dyadic radius ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational name surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the uniform-completion boundary of \autoref{ch:concrete-instances-uniformcompletion-namecert}. The packet gives completion consumers a finite formal-ball window without quotienting Cauchy names, selecting limits, using countable choice, invoking open-cover compactness, or assuming ambient completeness.

\begin{definition}[FormalBall carrier]
\label{def:formalball-carrier}
A $\FormalBallUp$ carrier is a finite $\BHist$ packet
$$
B=(M,R,D,W,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $M$ is the $\MetricSpaceUp$ source row, including the point classifier, distance witness surface, and finite observation histories.
\item $R$ is the $\RegSeqRatUp$ radius-name row read as a positive rational tolerance through finite requests.
\item $D$ is the $\DyadicRatCoreUp$ comparison ledger recording radius refinement, dyadic addition, and the strict comparison budget attached to $R$.
\item $W$ is the $\StreamNameUp$ finite-window row whose entries display the selected center observations and requested radii before any completion handoff.
\item $H$ records componentwise $\hsame$ transport for the metric source, radius row, dyadic ledger, finite window, replay row, provenance row, and local naming row.
\item $C$ records the displayed $\Cont$ replay from metric observations to radius refinements, window reads, $\RealUp$ tolerance seals, and the $\UniformCompletionUp$ handoff boundary.
\item $P$ is the $\Pkg$ provenance over $\FormalBallUp$, $\MetricSpaceUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\UniformCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FormalBallUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient completion equality, selected limits, countable choice, open-cover compactness, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallTasteGate\_single\_carrier\_alignment}

\begin{theorem}[FormalBall NameCert obligations]
\label{thm:formalball-namecert-obligations}
For the carrier of \autoref{def:formalball-carrier}, the local $\NameCert_{\FormalBallUp}$ surface consists exactly of carrier admission for $B=(M,R,D,W,H,C,P,N)$, metric source exposure through $M$, radius-name exposure through $R$, dyadic refinement exposure through $D$, finite-window exposure through $W$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

Every completion-facing read of an accepted carrier must pass through the displayed route
$$
M\longmapsto R\longmapsto D\longmapsto W\longmapsto H,C,P,N .
$$
No such read exports quotient completion equality, a chosen Cauchy representative, a selected limit, countable choice, open-cover compactness, or ambient completeness.
\end{theorem}
\begin{proof}
Carrier admission is tuple projection from \autoref{def:formalball-carrier}. The tuple displays the metric source, radius name, dyadic ledger, finite window, transport, replay, provenance, and local name.

The analytic obligations are exactly $M,R,D,W$. The metric row supplies point and distance reads, the radius row supplies the requested rational scale, the dyadic ledger records the refinement budget, and the stream window exposes the center observations available at that scale.

The structural obligations are exactly $H,C,P,N$. They transport the same rows, replay the same finite route, record package provenance, and bind the local certificate. Since the carrier contains no quotient, selected representative, selected limit, countable-choice schedule, compactness principle, or ambient-completeness coordinate, none can be exported by the NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_namecert\_obligations}

\begin{theorem}[FormalBall radius refinement]
\label{thm:formalball-radius-refinement}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. A refinement request from radius $r$ to a displayed smaller radius $s$ is admissible only when the dyadic ledger $D$ records the comparison and the replay row $C$ reads the corresponding finite center window $W$ over the same metric source $M$.

The refined formal ball is therefore a smaller window inside the same packet. It is not a second metric, not a quotient of centers, not a selected Cauchy representative, and not an appeal to ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallRadiusRefinement}
\leanvariant{BEDC.Derived.FormalBallUp.FormalBallCarrier\_radius\_refinement}
\begin{proof}
Project the accepted packet and read the metric source $M$. Any center comparison used by the refinement is interpreted through the point classifier and distance rows already displayed there.

Next read the radius row $R$ and the dyadic ledger $D$. The ledger is the only coordinate that records the comparison between $r$ and $s$, together with the finite arithmetic budget needed to keep the smaller radius inside the requested tolerance.

Finally replay the finite window through $W$ and $C$. The transport row $H$ preserves the same metric, radius, dyadic, and window rows, while $P$ and $N$ record provenance and naming. Thus refinement narrows an existing displayed formal-ball window and introduces no extra metric, quotient, representative choice, selected limit, or completeness principle.
\end{proof}

\begin{theorem}[FormalBall radius refinement chain]
\label{thm:formalball-radius-refinement-chain}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. For any finite displayed chain of radius refinements
$$
r_0 \ge r_1 \ge \cdots \ge r_k ,
$$
whose comparisons are all recorded in the same dyadic ledger $D$ and replayed through the same row $C$, every formal-ball window reached along the chain is read over the same metric source $M$ and the same finite stream-window row $W$.

The chain therefore refines only the displayed radius budget of the accepted packet. It does not identify quotient-completion classes, choose Cauchy representatives, select limits, use countable choice, invoke open-cover compactness, or assume ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_radius\_refinement\_chain}
\begin{proof}
For the empty chain there is no new refinement step. The accepted packet already exposes the metric source $M$, the radius row $R$, the dyadic ledger $D$, the stream-window row $W$, and the replay row $C$ through \autoref{def:formalball-carrier}.

For a nonempty chain, apply \autoref{thm:formalball-radius-refinement} to the first displayed comparison. The theorem keeps the smaller formal-ball window inside the same accepted packet, with its comparison recorded by $D$ and its finite center read through $W$ and $C$ over $M$.

Iterate the same argument over the remaining displayed comparisons. The transport row $H$ carries the metric, radius, dyadic, window, and replay rows componentwise at each step, while $P$ and $N$ only record provenance and local naming for the same packet.

No step introduces a field for quotient-completion equality, a selected Cauchy representative, a selected limit, a countable-choice schedule, an open-cover compactness principle, or an ambient complete space. Hence the whole finite chain preserves the same finite formal-ball window interface.
\end{proof}

\begin{theorem}[FormalBall completion-window handoff]
\label{thm:formalball-completion-window-handoff}
Every $\UniformCompletionUp$-facing consumer of an accepted $\FormalBallUp$ carrier $B=(M,R,D,W,H,C,P,N)$ receives only the finite window route through $\MetricSpaceUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, and $\UniformCompletionUp$ rows recorded by $B$. The handoff exposes radius-indexed formal balls as finite observation data before any completion boundary is read.

Consequently the handoff can support constructive completion arguments that use formal balls as observable neighborhoods, but it does not close quotient completion, selected limits, countable choice, open-cover compactness, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_completion\_window\_handoff}
\begin{proof}
Begin with the metric source and radius data. The rows $M$ and $R$ fix the center-observation surface and the requested radius scale before any completion-facing route is available.

Read the dyadic and stream windows next. The row $D$ records the arithmetic budget for the radius, and $W$ records the finite center windows inspected at that budget. Any $\RealUp$ tolerance seal reached by the consumer is read only through these rows.

Now pass to the completion boundary through $C$. The replay route carries the same finite formal-ball data to the $\UniformCompletionUp$ handoff, while $H,P,N$ preserve transport, provenance, and local naming.

The carrier fields are exhausted by this route. Since no field stores quotient equality, a selected limit, a countable-choice schedule, an open-cover compactness principle, or an ambient complete space, the completion-window handoff exports none of those principles.
\end{proof}

\begin{theorem}[FormalBall center-radius admission]
\label{thm:formalball-center-radius-admission}
Every accepted $\FormalBallUp$ carrier exposes its $\MetricSpaceUp$ center row, $\RegSeqRatUp$ radius row, $\DyadicRatCoreUp$ comparison ledger, and $\StreamNameUp$ finite window before the local $\NameCert_{\FormalBallUp}$ row is consumed. The admission route is a $\BHist$ route over
$$
M,R,D,W,H,C,P,N,
$$
and no consumer may read the formal ball as an accepted packet while omitting any one of $M,R,D,W$.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_center\_radius\_admission}
\begin{proof}
Project the carrier of \autoref{def:formalball-carrier}. Its first four analytic coordinates are exactly the metric source $M$, radius row $R$, dyadic ledger $D$, and finite window $W$.

The local naming row $N$ appears only after the structural rows $H,C,P$. Thus an accepted read must first display the center source, radius name, comparison ledger, and window route, and only then consume the local certificate.

Since the tuple has no alternate center or radius coordinate, a consumer that drops any of $M,R,D,W$ has no accepted $\BHist$ route to the same $\FormalBallUp$ packet.
\end{proof}

\begin{theorem}[FormalBall formal-order monotonicity]
\label{thm:formalball-formal-order-monotonicity}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. Any radius refinement or formal-order shrinkage of $B$ can only pass through the displayed dyadic ledger $D$ and the replay row $C$. It cannot introduce quotient completion equality, selected limits, countable choice, open-cover compactness, or ambient completeness as evidence for the shrinkage.
\end{theorem}
\begin{proof}
Read the accepted carrier and isolate the rows that can mention radii. The radius row is $R$, and the comparison data for shrinking that radius is recorded only in $D$.

Replay through $C$ preserves the same metric source and finite window while reading the smaller radius through the displayed ledger. The transport row $H$ keeps this shrinkage componentwise inside the same packet, and $P,N$ only source and name the route.

The remaining forbidden principles are absent by row exhaustion. There is no field for quotient completion equality, selected limits, countable choice, open-cover compactness, or ambient completeness, so none can be used to justify formal-order monotonicity.
\end{proof}

\begin{definition}[FormalBall rounded-filter boundary]
\label{def:formalball-rounded-filter-boundary}
For an accepted $\FormalBallUp$ carrier $B=(M,R,D,W,H,C,P,N)$, the rounded-filter boundary row is the finite row
$$
\partial_{\mathrm{rf}}(B)=(M,R,D,W,C,H,P,N)
$$
read after the metric source, radius row, dyadic refinement ledger, finite center-window row, replay row, componentwise transport, provenance, and local naming have all been displayed. The boundary records only rounded formal-ball windows generated by the same carrier; it does not contain quotient completion equality, selected Cauchy representatives, countable-choice schedules, open-cover compactness, or ambient completeness.
\end{definition}

\begin{theorem}[FormalBall completion dependency scope]
\label{thm:formalball-completion-dependency-scope}
Every completion-facing read of an accepted $\FormalBallUp$ carrier $B=(M,R,D,W,H,C,P,N)$ factors through the displayed rows $M,R,D,W,H,C,P,N$ and through the named $\MetricSpaceUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, and $\UniformCompletionUp$ surfaces. The read may consume the rounded-filter boundary of \autoref{def:formalball-rounded-filter-boundary}, but it cannot bypass the carrier rows or replace them by a quotient completion principle.
\end{theorem}
\begin{proof}
Start with carrier projection and the NameCert obligations. Theorem~\ref{thm:formalball-namecert-obligations} exposes the metric source, radius name, dyadic ledger, finite window, transport, replay, provenance, and local name as the only carrier rows available to a completion-facing consumer.

Next apply the radius-refinement chain. Theorem~\ref{thm:formalball-radius-refinement-chain} keeps every smaller-radius window inside the same dyadic ledger $D$, stream window $W$, and replay row $C$ over the same source $M$.

Finally apply formal-order monotonicity. Theorem~\ref{thm:formalball-formal-order-monotonicity} prevents a shrinkage read from introducing quotient completion equality, selected representatives, countable choice, compactness, or ambient completeness. Therefore the completion dependency scope is exactly the named finite route through $\MetricSpaceUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, and $\UniformCompletionUp$.
\end{proof}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_completion\_dependency\_scope}

\begin{theorem}[FormalBall rounded-filter non-escape]
\label{thm:formalball-rounded-filter-nonescape}
For an accepted $\FormalBallUp$ carrier, a rounded-filter read exposes no quotient completion equality, no selected Cauchy representative, no countable-choice schedule, no open-cover compactness argument, and no ambient completeness principle. It exposes only the finite boundary row $\partial_{\mathrm{rf}}(B)$ and the rows from which that boundary is projected.
\end{theorem}
\begin{proof}
Project the rounded-filter boundary of \autoref{def:formalball-rounded-filter-boundary}. Its entries are $M,R,D,W,C,H,P,N$, all inherited from the accepted carrier.

Use the NameCert obligations and the radius-refinement chain. The first theorem exhausts the accepted packet by the displayed carrier rows, and the second keeps every rounded refinement inside $D,W,C$ over the same metric source.

The formal-order monotonicity theorem supplies the final exclusion. Since every shrinkage remains inside the displayed dyadic and replay rows, the rounded-filter read has no coordinate from which to expose quotient equality, selected representatives, countable choice, compactness, or ambient completeness.
\end{proof}

\begin{theorem}[FormalBall Cauchy-filter handoff]
\label{thm:formalball-cauchy-filter-handoff}
Every $\CauchyFilterUp$ or $\UniformCompletionUp$ consumer that reads an accepted $\FormalBallUp$ packet must factor through the displayed $\MetricSpaceUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The handoff exports formal balls only as finite center-radius windows in the accepted $\BHist$ packet, not as quotient completions or selected limits.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_cauchy\_filter\_handoff}
\begin{proof}
Begin with the analytic rows. The metric source $M$ supplies the center surface, $R$ supplies the regular rational radius, $D$ supplies the dyadic comparison ledger, and $W$ supplies the finite center-window observations.

The Real-facing tolerance seal is reached only through the replay row $C$, which reads the same $M,R,D,W$ data before any completion-facing consumer can inspect the handoff. Componentwise transport is carried by $H$.

Finally $P$ and $N$ record provenance and local naming for this route. Therefore a $\CauchyFilterUp$ or $\UniformCompletionUp$ consumer receives exactly the displayed finite formal-ball packet and no quotient-completion equality, chosen Cauchy representative, selected limit, countable-choice schedule, compactness principle, or ambient complete space.
\end{proof}

\begin{theorem}[FormalBall rounded-filter Cauchy-filter boundary]
\label{thm:formalball-rounded-filter-cauchyfilter-boundary}
Every $\CauchyFilterUp$ consumer of the rounded-filter boundary of an accepted $\FormalBallUp$ packet must first read the same rows
$$
M,R,D,W,C,H,P,N
$$
displayed by $\partial_{\mathrm{rf}}(B)$ in \autoref{def:formalball-rounded-filter-boundary}. Equivalently, a filter-limit boundary may be consumed only after the metric source, radius row, dyadic refinement ledger, finite center window, replay row, componentwise transport, provenance, and local NameCert row of the accepted packet have been displayed. The route exports no quotient completion, selected limit, countable-choice schedule, open-cover compactness, or ambient completeness.
\end{theorem}
\begin{proof}
Start from the rounded-filter boundary definition. The boundary row is exactly the projection $\partial_{\mathrm{rf}}(B)=(M,R,D,W,C,H,P,N)$, so a consumer of that boundary already sits inside the displayed accepted carrier.

Apply rounded-filter non-escape and rounded-filter admission. The non-escape theorem excludes quotient completion, selected representatives, choice schedules, compactness, and ambient completeness from the rounded-filter read, while admission forces the completion-facing or Cauchy-facing route to display $M,R,D,W,C,H,P,N$ before the boundary is consumed.

Finally apply the Cauchy-filter handoff. It attaches every $\CauchyFilterUp$ consumer to the same metric, dyadic, regular-rational, finite-window, real-seal, transport, replay, provenance, and local naming rows. Thus a filter-limit boundary cannot be read before those rows, and cannot export any of the excluded completion or compactness data.
\end{proof}

\begin{theorem}[FormalBall located-radius window]
\label{thm:formalball-located-radius-window}
Every accepted $\FormalBallUp$ located-radius completion read must display the $\MetricSpaceUp$ source $M$, the $\RegSeqRatUp$ radius row $R$, the $\DyadicRatCoreUp$ comparison ledger $D$, the $\StreamNameUp$ finite window $W$, and the $\Cont$ replay row $C$ before any $\UniformCompletionUp$ or $\CauchyFilterUp$ handoff is consumed. The read is therefore a finite located-radius window in the carrier $B=(M,R,D,W,H,C,P,N)$, not a quotient completion, selected limit, countable-choice schedule, open-cover compactness argument, or ambient completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:formalball-carrier}. The located-radius data available to a completion-facing consumer is exactly $M,R,D,W$ together with transport, replay, provenance, and naming rows.

Apply the radius-refinement chain of \autoref{thm:formalball-radius-refinement-chain}. It keeps every displayed radius comparison inside the dyadic ledger $D$ and the replayed finite window $W,C$ over the same metric source $M$.

Finally apply the completion-window handoff of \autoref{thm:formalball-completion-window-handoff} and the Cauchy-filter handoff of \autoref{thm:formalball-cauchy-filter-handoff}. These handoffs force $\UniformCompletionUp$ and $\CauchyFilterUp$ consumers to read the finite center-radius packet before the completion boundary, so no quotient completion, selected limit, choice schedule, compactness principle, or ambient completeness row can enter.
\end{proof}

\begin{theorem}[FormalBall rounded-filter admission]
\label{thm:formalball-rounded-filter-admission}
Every $\UniformCompletionUp$ or $\CauchyFilterUp$ consumer admitted through an accepted $\FormalBallUp$ carrier must display the $\MetricSpaceUp$ source $M$, the $\RegSeqRatUp$ radius row $R$, the $\DyadicRatCoreUp$ refinement ledger $D$, the $\StreamNameUp$ finite window $W$, and the $\Cont$ replay row $C$ before reading a rounded-filter boundary. The admitted route also preserves componentwise transport, formal-order monotonicity, provenance, and local naming through $H,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_rounded\_filter\_admission}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:formalball-namecert-obligations}. They make carrier admission, metric exposure, radius exposure, dyadic refinement, finite-window exposure, transport, replay, provenance, and naming part of one packet.

Use the radius-refinement route of \autoref{thm:formalball-radius-refinement-chain} and the monotonicity boundary of \autoref{thm:formalball-formal-order-monotonicity}. They force every rounded shrinkage to remain inside the dyadic ledger $D$ and replay row $C$ over the same source $M$ and window $W$.

Finally apply the completion-window and Cauchy-filter handoffs, \autoref{thm:formalball-completion-window-handoff} and \autoref{thm:formalball-cauchy-filter-handoff}. A completion-facing consumer therefore reads the rounded-filter boundary only after the displayed finite rows $M,R,D,W,C,H,P,N$, and receives no quotient completion, selected representative, choice schedule, compactness principle, or ambient completeness row.
\end{proof}

\begin{theorem}[FormalBall rounded-filter stability]
\label{thm:formalball-rounded-filter-stability}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. Any rounded-filter read admitted by \autoref{thm:formalball-rounded-filter-admission} remains stable under componentwise $\hsame$ transport of the metric source $M$, $\RegSeqRatUp$ radius row $R$, $\DyadicRatCoreUp$ ledger $D$, $\StreamNameUp$ finite window $W$, and $\Cont$ replay row $C$. The transported read is again the rounded-filter boundary of \autoref{def:formalball-rounded-filter-boundary} for the same displayed packet route.

The stability route refuses quotient completion equality, selected limits, countable choice, open-cover compactness, and ambient completeness as evidence for the transported rounded-filter read.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_rounded\_filter\_stability}
\begin{proof}
Start with the boundary definition. The rounded-filter row of \autoref{def:formalball-rounded-filter-boundary} is projected from the displayed rows $M,R,D,W,C,H,P,N$, so every component available for transport is already named in the accepted packet.

Apply rounded-filter admission, \autoref{thm:formalball-rounded-filter-admission}. It forces any completion-facing consumer to read $M,R,D,W,C$ before the rounded-filter boundary, with $H,P,N$ preserving transport, provenance, and naming.

Finally apply directed radius transport, \autoref{thm:formal-ball-directed-radius-transport}. Componentwise $\hsame$ transport keeps radius refinements inside the same dyadic ledger $D$ and replay row $C$ over the transported metric and window rows. Since the carrier contains no quotient equality, selected limit, choice schedule, compactness principle, or ambient complete-space coordinate, none can justify or alter the transported rounded-filter read.
\end{proof}

\begin{theorem}[FormalBall carrier admission]
\label{thm:formal-ball-carrier-admission}
Every accepted $\FormalBallUp$ packet must display the metric source $M$, radius row $R$, dyadic comparison ledger $D$, and finite center-window row $W$ before the local $\NameCert_{\FormalBallUp}$ row $N$ is read. Thus the admitted carrier route has the visible analytic prefix
$$
M,R,D,W
$$
inside the $\BHist$ packet, and no completion consumer may treat $N$ as naming a formal ball without first reading those four rows.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:formalball-carrier}. Its displayed tuple is $B=(M,R,D,W,H,C,P,N)$, so the analytic rows $M,R,D,W$ occur before the structural transport, replay, provenance, and naming rows.

Apply \autoref{thm:formalball-namecert-obligations}. The local NameCert surface names carrier admission only for the same displayed route and records no alternate entry point into $N$.

The completion handoff theorems then keep every $\UniformCompletionUp$ or $\CauchyFilterUp$ consumer downstream of that prefix. Hence an accepted packet must expose $M,R,D,W$ before $N$ can be consumed as the local name.
\end{proof}

\begin{theorem}[FormalBall directed radius transport]
\label{thm:formal-ball-directed-radius-transport}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. If a displayed radius refinement is preserved componentwise by the $\hsame$ row $H$, then the transported refinement is still read through the same dyadic ledger $D$ and replay row $C$. In particular, radius refinements cannot be transported by quotient completion equality, selected representatives, countable choice, open-cover compactness, or an ambient complete-space principle.
\end{theorem}
\leanchecked{BEDC.Derived.FormalBallUp.FormalBallCarrier\_directed\_radius\_transport}
\begin{proof}
Start with a displayed refinement. By \autoref{thm:formalball-radius-refinement}, the comparison from the larger radius to the smaller one is recorded in $D$, and the corresponding center-window read is replayed through $C$ over the same metric source.

Now apply the componentwise transport row $H$ from the carrier. It transports the metric source, radius row, dyadic ledger, finite window, replay row, provenance row, and local naming row together; it does not supply an independent equality of completions or a selected representative.

The transported route therefore has the same radius-comparison evidence in $D$ and the same replay evidence in $C$. Since \autoref{thm:formalball-cauchy-filter-handoff} exhausts the consumer-facing fields by the displayed packet rows, no forbidden completion or compactness coordinate can justify the refinement.
\end{proof}

\begin{theorem}[FormalBall rounded ideal handoff]
\label{thm:formal-ball-rounded-ideal-handoff}
Every $\CauchyFilterUp$ and $\UniformCompletionUp$ consumer of an accepted $\FormalBallUp$ carrier reads only the rounded finite center-radius route generated by the metric source $M$, radius row $R$, dyadic ledger $D$, finite window $W$, componentwise transport $H$, replay row $C$, provenance row $P$, and local naming row $N$. The handoff exposes rounded formal-ball data, not quotient limits, selected Cauchy representatives, countable choice, open-cover compactness, or ambient Real completeness.
\end{theorem}
\begin{proof}
First read the carrier-admission route of \autoref{thm:formal-ball-carrier-admission}. It forces every consumer to expose $M,R,D,W$ before the local naming row can certify the packet.

Next use \autoref{thm:formalball-radius-refinement-chain} and \autoref{thm:formal-ball-directed-radius-transport}. Finite radius shrinkage remains inside the displayed dyadic ledger and replay route, while componentwise transport preserves the same center-radius packet.

Finally apply \autoref{thm:formalball-completion-window-handoff} and \autoref{thm:formalball-cauchy-filter-handoff}. These handoff statements exhaust the $\CauchyFilterUp$ and $\UniformCompletionUp$ reads by the finite rows $M,R,D,W,H,C,P,N$, so the exported rounded ideal interface contains no quotient limit, selected representative, choice schedule, compactness principle, or ambient completeness row.
\end{proof}

\begin{theorem}[FormalBall rounded-basis comparison]
\label{thm:formalball-rounded-basis-comparison}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. Any consumer comparing a formal-ball rounded basis must display the center source $M$, radius row $R$, dyadic refinement ledger $D$, finite center-window row $W$, componentwise $\hsame$ transport $H$, and displayed $\Cont$ replay $C$ before it reads the local $\NameCert_{\FormalBallUp}$ row. The comparison is therefore a row-level comparison of finite center-radius windows, not quotient equality of balls, a selected limit, countable choice, open-cover compactness, or ambient completion.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $M,R,D,W$ display the metric center surface, the regular rational radius, the dyadic refinement budget, and the finite observation window.

Rounded-basis comparison can only shrink or compare those displayed radii through $D$ and replay the corresponding center windows through $C$. Componentwise transport through $H$ preserves the same source, radius, dyadic, and window rows.

The local name and provenance rows $P,N$ do not add analytic data. Hence the comparison remains a finite formal-ball basis comparison and cannot become quotient equality of balls, a selected limit, a choice schedule, an open-cover compactness argument, or an ambient-completion principle.
\end{proof}

\begin{theorem}[FormalBall Cauchy-net admission]
\label{thm:formalball-cauchy-net-admission}
Every $\CauchyNetUp$ or $\CauchyFilterUp$ admission route that consumes an accepted $\FormalBallUp$ carrier must read the center-radius rows $M,R$, the rounded refinement ledger $D$, the finite window $W$, and the replay row $C$ before admitting Cauchy evidence to a completion-facing packet. The admitted Cauchy data is attached to the displayed $\BHist$ route
$$
M,R,D,W,H,C,P,N
$$
and not to a quotient of formal balls or a selected representative.
\end{theorem}
\begin{proof}
Begin with carrier admission. By \autoref{thm:formal-ball-carrier-admission}, every consumer must expose $M,R,D,W$ before the local NameCert row is read.

Use the rounded ideal handoff. The Cauchy-facing route reads rounded finite center-radius data through $D,W,C$ over the same metric source and radius row, and $H$ transports that data componentwise.

The admission row is therefore downstream of the finite packet. Since the carrier has no coordinate for quotient-ball equality, selected Cauchy representatives, selected limits, countable choice, open-cover compactness, or ambient completeness, neither a Cauchy net nor a Cauchy filter can obtain such evidence from the formal-ball packet.
\end{proof}

\begin{theorem}[FormalBall uniform-completion route]
\label{thm:formalball-uniform-completion-route}
Every $\UniformCompletionUp$ or $\MetricCompletionUp$ consumer of an accepted $\FormalBallUp$ carrier factors through the displayed rounded-basis and Cauchy-admission rows before reading any completion handoff. The route first exposes $M,R,D,W$, then transports and replays the finite window through $H,C$, then records provenance and local naming through $P,N$; it does not quotient formal balls, select limits, use countable choice, or appeal to ambient completeness.
\end{theorem}
\begin{proof}
First apply the rounded-basis comparison. It fixes the center source, radius row, dyadic refinement ledger, and finite window as the visible formal-ball basis data.

Next apply Cauchy-net admission. Completion-facing Cauchy evidence is admitted only after the finite rounded basis route is displayed and replayed through $C$ with componentwise transport $H$.

Finally use the existing completion-window and rounded-ideal handoffs. They send exactly the rows $M,R,D,W,H,C,P,N$ to uniform or metric completion consumers, so the route has no place for quotient formal-ball equality, selected limits, countable choice, open-cover compactness, or an ambient complete-space assumption.
\end{proof}

\begin{theorem}[FormalBall filter-basis route]
\label{thm:formalball-filter-basis-route}
Every $\CauchyFilterBasisUp$ consumer of an accepted $\FormalBallUp$ carrier must first read the center-radius rows $M,R$, the rounded refinement ledger $D$, the finite center window $W$, and the replay row $C$. The basis row is therefore admitted only after the rounded-basis comparison and Cauchy-net admission surface have been exposed; it cannot be consumed as a detached filter basis, a quotient of balls, a selected Cauchy representative, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:formalball-rounded-basis-comparison}. It fixes the center source, radius row, refinement ledger, finite window, transport, and replay rows as the only basis-facing data.

Apply \autoref{thm:formalball-cauchy-net-admission}. Any Cauchy-facing basis read is downstream of the displayed route $M,R,D,W,H,C,P,N$, so the basis cannot enter as an external filter object.

Finally use \autoref{thm:formalball-uniform-completion-route}. Completion consumers receive the basis only through the same formal-ball packet, with no row for quotient equality, selected representatives, choice schedules, or ambient completeness.
\end{proof}

\begin{theorem}[FormalBall radius-order handoff]
\label{thm:formalball-radius-order-handoff}
Every $\FormalBallRadiusOrderUp$ comparison handed off by an accepted $\FormalBallUp$ carrier is read through the center-radius rows $M,R$, the rounded refinement ledger $D$, and the replayed window $W,C$ before any $\UniformCompletionUp$ or $\MetricCompletionUp$ consumer may inspect it. The comparison is a finite radius-order row inside the same packet, not a host order on quotient balls or a selected completion representative.
\end{theorem}
\begin{proof}
Apply \autoref{thm:formalball-radius-refinement-chain}. The radius comparison is recorded in the dyadic ledger $D$ and is tied to the same center-radius route.

Use \autoref{thm:formal-ball-directed-radius-transport}. Componentwise $\hsame$ transport preserves the comparison together with $M,R,W,C,P,N$ rather than replacing it by a quotient-level order.

The completion route of \autoref{thm:formalball-uniform-completion-route} then forces every completion-facing consumer to read this radius-order handoff before it reads the terminal completion surface.
\end{proof}

\begin{theorem}[FormalBall completion-basis nonescape]
\label{thm:formalball-completion-basis-nonescape}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. Any $\UniformCompletionUp$ or $\MetricCompletionUp$ consumer that uses $B$ must first read the center-radius rows, the $\FormalBallRadiusOrderUp$ comparison row encoded by $D$, and the $\CauchyFilterBasisUp$ basis row encoded by the rounded Cauchy admission route. The consumer cannot bypass those rows to a quotient completion, selected limit, open-cover compactness proof, countable choice principle, or ambient complete-space witness.
\end{theorem}
\begin{proof}
First combine the filter-basis route with the radius-order handoff. The basis read is downstream of $M,R,D,W,H,C,P,N$, and the radius comparison is stored in $D$ over the same center-radius rows.

Next apply the rounded ideal and Cauchy-net admission theorems. They attach all completion-facing Cauchy evidence to the displayed finite packet and replay it through $C$.

Finally apply the uniform-completion route. Since that route exposes no coordinate beyond $M,R,D,W,H,C,P,N$, the completion consumer cannot escape to quotient equality, selected limits, compactness, choice, or ambient completeness.
\end{proof}

\begin{theorem}[FormalBall rounded-filter admission package]
\label{thm:formal-ball-rounded-filter-admission-package}
Every accepted $\FormalBallUp$ carrier $B=(M,R,D,W,H,C,P,N)$ whose center-radius row, directed radius transport row, rounded ideal handoff row, filter-basis route row, radius-order handoff row, and completion-basis nonescape row are displayed admits the rounded-filter package read by completion consumers. The package passes through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\DyadicRatCoreUp$, $\MetricSpaceUp$, $\CauchyFilterBasisUp$, $\CauchyNetUp$, and $\UniformCompletionUp$, and exports no quotient Cauchy name, selected limit, countable-choice schedule, or ambient completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:formalball-carrier}. The analytic prefix $M,R,D,W$ is visible by \autoref{thm:formal-ball-carrier-admission}, so the center-radius read starts from a displayed $\MetricSpaceUp$ source, a regular rational radius, a $\DyadicRatCoreUp$ ledger, and a finite window.

Apply directed radius transport and the rounded ideal handoff. Theorems~\ref{thm:formal-ball-directed-radius-transport} and \ref{thm:formal-ball-rounded-ideal-handoff} keep radius shrinkage inside the same dyadic ledger, componentwise $\hsame$ transport, and replay row $C$.

Next read the Cauchy-facing basis rows. Theorems~\ref{thm:formalball-cauchy-net-admission} and \ref{thm:formalball-filter-basis-route} attach $\CauchyNetUp$ and $\CauchyFilterBasisUp$ evidence to the displayed packet rather than to an external quotient or representative.

Finally apply the radius-order handoff and completion-basis nonescape, \autoref{thm:formalball-radius-order-handoff} and \autoref{thm:formalball-completion-basis-nonescape}. These rows exhaust the completion-facing package by $M,R,D,W,H,C,P,N$, so the admitted rounded-filter package exports no quotient Cauchy name, selected limit, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[FormalBall completion obligation triad]
\label{thm:formalball-completion-obligation-triad}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. A completion-facing read of $B$ is admitted only when the center-radius row $M,R$, the $\FormalBallRadiusOrderUp$ comparison row encoded by $D$, and the $\CauchyFilterBasisUp$ basis route through $W,H,C$ are all displayed before any $\UniformCompletionUp$ or $\MetricCompletionUp$ consumer reads the packet. The three rows form one finite BEDC route through $M,R,D,W,H,C,P,N$ and do not introduce a quotient Cauchy name, selected limit, countable-choice schedule, open-cover compactness theorem, or ambient completeness witness.
\end{theorem}
\begin{proof}
First project the rounded-filter package of \autoref{thm:formal-ball-rounded-filter-admission-package}. It exposes the center-radius source, dyadic refinement ledger, finite window, transport, replay, provenance, and local name before any completion handoff is available.

Next apply the radius-order handoff, \autoref{thm:formalball-radius-order-handoff}. The comparison read is stored in the same dyadic row $D$ over $M,R$ and is replayed with $W,C$ rather than with a quotient order on formal balls.

Finally use completion-basis nonescape, \autoref{thm:formalball-completion-basis-nonescape}. It forces the Cauchy-filter basis row and completion consumer to remain on $M,R,D,W,H,C,P,N$. Thus the center-radius row, radius-order comparison, and Cauchy basis route are a single finite obligation triad, with no selected limit, quotient name, choice schedule, compactness principle, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[FormalBall radius-order scope package]
\label{thm:formalball-radius-order-scope-package}
Let $B=(M,R,D,W,H,C,P,N)$ be an accepted $\FormalBallUp$ carrier. Its radius-order scope package consists of the center-radius source $M,R$, the $\FormalBallRadiusOrderUp$ comparison row stored in $D$, the $\MetricClosedBallUp$ boundary read available from the same center and radius window, the $\DyadicRatCoreUp$ refinement ledger, and the terminal $\RealUp$ tolerance rows replayed by $C$. A completion-facing consumer may inspect a radius-order comparison only through this package; it cannot replace the package by a host order on balls, quotient equality of completions, a selected center representative, or an ambient complete-space witness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The center and radius rows are $M,R$, the dyadic comparison ledger is $D$, and the finite window and replay rows are $W,C$.

Use the radius-order handoff, \autoref{thm:formalball-radius-order-handoff}. It places every $\FormalBallRadiusOrderUp$ comparison inside the same dyadic row $D$ and ties the comparison to the center-radius route rather than to a quotient order.

Now read the metric closed-ball boundary and real tolerance rows through the same center, radius, window, and replay data. Componentwise transport through $H$ and provenance through $P,N$ preserve that package, so the completion consumer receives only the displayed L10 dependency surface.
\end{proof}

\begin{theorem}[FormalBall Cauchy filter-basis triad]
\label{thm:formalball-cauchy-filter-basis-triad}
Every accepted $\FormalBallUp$ carrier that is consumed by a completion-facing route must expose three Cauchy-facing rows before the completion handoff: a rounded-filter route over the displayed center-radius packet, a $\CauchyFilterUp$ basis row read through the same finite windows, and a $\CauchyNetUp$ basis row with the same dyadic tolerance ledger. The triad is attached to $M,R,D,W,H,C,P,N$ and does not introduce detached filters, quotient Cauchy names, selected limits, countable-choice schedules, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the rounded ideal handoff, \autoref{thm:formal-ball-rounded-ideal-handoff}. It forces the rounded-filter route to read the finite center-radius data $M,R,D,W,H,C,P,N$.

Apply Cauchy-net admission and the filter-basis route. Theorems~\ref{thm:formalball-cauchy-net-admission} and \ref{thm:formalball-filter-basis-route} attach $\CauchyNetUp$, $\CauchyFilterUp$, and basis evidence to the same rounded formal-ball packet before any completion row is consumed.

Finally use the completion obligation triad. The completion-facing read sees the rounded-filter, filter-basis, and net-basis data as one finite L10 surface, with no coordinate for detached filters, quotient names, selected limits, choice schedules, or ambient completeness.
\end{proof}

\begin{theorem}[FormalBall uniform-completion ledger exhaustion]
\label{thm:formalball-uniform-completion-ledger-exhaustion}
Every $\UniformCompletionUp$ handoff from an accepted $\FormalBallUp$ carrier is exhausted by radius refinement, finite-window replay, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\FormalBallUp}$ row. Equivalently, the handoff consumes only $M,R,D,W,H,C,P,N$ together with the local radius-order, Cauchy-filter, Cauchy-net, $\MetricClosedBallUp$, $\DyadicRatCoreUp$, and $\RealUp$ tolerance rows already displayed by the packet. It exports no quotient completion equality, selected representative, countable-choice schedule, open-cover compactness, or ambient complete-space assumption.
\end{theorem}
\begin{proof}
Begin with the uniform-completion route, \autoref{thm:formalball-uniform-completion-route}. That route already factors the completion consumer through $M,R,D,W,H,C,P,N$ before any completion handoff is read.

Next add the L10 dependency rows. The radius-order scope package supplies the radius comparison, metric closed-ball boundary, dyadic ledger, and real tolerance seal, while the Cauchy filter-basis triad supplies the rounded-filter, $\CauchyFilterUp$, and $\CauchyNetUp$ basis data.

It remains to account for structural stability. Radius refinement is recorded in $D$, finite windows are replayed through $W,C$, componentwise $\hsame$ transport is carried by $H$, and provenance and local naming are exactly $P,N$.

Thus the displayed ledger is exhausted by the accepted packet itself. Since no field remains for quotient completion equality, selected representatives, countable choice, open-cover compactness, or ambient completeness, the $\UniformCompletionUp$ handoff cannot export those objects.
\end{proof}

\begin{theorem}[FormalBall obligation-closure export]
\label{thm:formalball-obligation-closure-export}
An accepted $\FormalBallUp$ packet exports an obligation-closure surface exactly when its carrier admission, radius refinement, rounded-filter boundary, Cauchy-filter handoff, radius-order handoff, completion-basis nonescape, and uniform-completion route all factor through the same finite rows
$$
M,R,D,W,H,C,P,N .
$$
The exported surface is the shared row-ordering of \autoref{thm:formal-ball-carrier-admission}, \autoref{thm:formalball-radius-refinement-chain}, \autoref{thm:formalball-rounded-filter-admission}, \autoref{thm:formalball-cauchy-filter-handoff}, \autoref{thm:formalball-radius-order-handoff}, \autoref{thm:formalball-completion-basis-nonescape}, and \autoref{thm:formalball-uniform-completion-route}; it contains no quotient Cauchy name, selected limit, countable-choice schedule, open-cover compactness row, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project carrier admission through \autoref{thm:formal-ball-carrier-admission}. It places every accepted read on the displayed formal-ball rows $M,R,D,W,H,C,P,N$ before local naming is consumed.

Next read the radius route. The radius-refinement chain and directed transport theorems keep every shrinkage and comparison inside the dyadic ledger $D$, replay row $C$, and the same center-window data.

Now attach the rounded-filter and Cauchy-facing boundaries. The rounded-filter admission theorem and the Cauchy-filter handoff theorem force filter consumers to read the same finite packet before any completion-facing row is available.

Finally apply the radius-order handoff, completion-basis nonescape, and uniform-completion route. These theorems order the remaining completion consumers over the same rows, so the packet exports one obligation surface precisely when all listed routes factor through that row list.
\end{proof}

\begin{closurestatus}{\FormalBallUp}
  \constructivestory{A $\FormalBallUp$ packet is generated from a displayed MetricSpaceUp source, a RegSeqRat radius row, a DyadicRatCore refinement ledger, a StreamName finite-window row, componentwise hsame transport, displayed Cont replay toward RealUp tolerance seals and UniformCompletionUp handoff, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:formalball-carrier}, \autoref{thm:formalball-namecert-obligations}, \autoref{thm:formalball-radius-refinement}, \autoref{thm:formalball-radius-refinement-chain}, \autoref{thm:formalball-completion-window-handoff}, \autoref{thm:formalball-cauchy-filter-handoff}, and \autoref{thm:formalball-rounded-filter-admission} seed the finite formal-ball observation and rounded-filter interface for constructive completion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion, selected limits, countable choice, open-cover compactness, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, radius-refinement stability, finite-window replay, RealUp tolerance seal compatibility, UniformCompletionUp handoff, transport stability, provenance, and local naming for the same packet.}
\end{closurestatus}
