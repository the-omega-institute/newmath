\chapter{A Concrete Naming Certificate for $\BishopRealFunctionSpaceUp$}
\label{ch:concrete-instances-bishop-real-function-space-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopRealFunctionSpaceUp$ packet records a finite L10 real-function surface whose inputs and outputs are read only through displayed real-name rows. It sits beside the general function-space packet of \autoref{ch:concrete-instances-real-function-space-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the finite window route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet is a finite Bishop-style real-function certificate; it is not a host set of all functions, a quotient equality principle for maps, a completeness theorem for $\RealUp$, a function-extensionality axiom, a selected limit operator, or a countable-choice schedule.

\begin{definition}[Bishop real-function-space carrier]\label{def:bishop-real-function-space-carrier}
A $\BishopRealFunctionSpaceUp$ carrier is a finite $\BHist$ packet
$$
B=(D,T,G,W,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the source $\RealUp$ domain row, listing the displayed input real names on which the function packet may be inspected.
\item $T$ is the target $\RealUp$ row, listing the displayed output real names returned by the inspected function windows.
\item $G$ is the finite graph-window row, binding each inspected source window in $D$ to the corresponding target window in $T$.
\item $W$ is the $\StreamNameUp$ window schedule for the source and target real names used by $G$.
\item $R$ is the $\RegSeqRatUp$ readback row reached from the windows in $W$ before any terminal target seal is consumed.
\item $Q$ is the local Bishop continuity row, requiring each graph-window read to pass through the same displayed source window, target window, and regular readback route.
\item $H$ records componentwise $\hsame$ transport for $D,T,G,W,R,Q$ and the structural rows below.
\item $C$ records displayed $\Cont$ replay from a real-function request through $D,T,G,W,R,Q,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopRealFunctionSpaceUp$, $\RealFunctionSpaceUp$, $\ContinuousMapUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealFunctionSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,T,G,W,R,Q,H,C,P,N$. It has no coordinate for host function extensionality, quotient equality of maps, an ambient function-space completion, selected pointwise limits, countable choice, real trichotomy, or completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Bishop real-function-space NameCert obligations]\label{thm:bishop-real-function-space-namecert-obligations}
For every accepted $\BishopRealFunctionSpaceUp$ carrier $B=(D,T,G,W,R,Q,H,C,P,N)$, the local $\NameCert_{\BishopRealFunctionSpaceUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item source and target $\RealUp$ exposure through $D$ and $T$;
\item finite graph-window exposure through $G$;
\item $\StreamNameUp$ scheduling through $W$;
\item $\RegSeqRatUp$ readback through $R$;
\item Bishop continuity replay through $Q$ over the same displayed windows;
\item componentwise $\hsame$ transport through $H$;
\item displayed $\Cont$ replay through $C$;
\item $\Pkg$ provenance through $P$ and local naming through $N$.
\end{enumerate}
No accepted obligation row is stored outside $D,T,G,W,R,Q,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:bishop-real-function-space-carrier}. The Real-facing rows are $D,T$, the graph and observation rows are $G,W,R,Q$, and the structural rows are $H,C,P,N$.

The graph-window obligations are read before the terminal target row is available. A consumer first displays a source real-name row from $D$, then the graph-window row $G$, then the target real-name row $T$ attached to that graph window.

The finite observation route is supplied by $W,R,Q$. The stream schedule $W$ lists the inspected windows, the regular rational row $R$ gives the readback, and the Bishop continuity row $Q$ records that the same displayed source and target windows are used by the local continuity replay.

Finally $H,C,P,N$ transport, replay, source, and name only this finite route. Since no other coordinate occurs in the carrier, the listed obligation rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Bishop real-function-space nonescape]\label{thm:bishop-real-function-space-nonescape}
Every Real or Completion consumer of an accepted $\BishopRealFunctionSpaceUp$ carrier factors through
$$
D,T,G,W,R,Q,H,C,P,N .
$$
Thus a target $\RealUp$ output row can be read only after the source $\RealUp$ domain row, graph-window row, $\StreamNameUp$ schedule, $\RegSeqRatUp$ readback, Bishop continuity replay, componentwise transport, displayed replay, provenance, and local naming rows have been exposed. The packet exports no host function extensionality, quotient equality of maps, ambient function-space completion, selected pointwise limit, countable-choice schedule, real trichotomy, or completeness theorem for $\RealUp$.
\end{theorem}
\begin{proof}
Begin with an accepted carrier and a Real- or Completion-facing consumer request. Carrier projection exposes only the rows named in \autoref{def:bishop-real-function-space-carrier}.

The request reaches the target real-name row through the graph route. The source row $D$ and graph-window row $G$ identify the inspected input window, while the target row $T$ records the output real-name window tied to that graph entry.

The L10 readback then passes through $W,R,Q$. The $\StreamNameUp$ schedule and $\RegSeqRatUp$ row make the inspected finite windows visible, and the Bishop continuity row replays the same source and target windows instead of replacing them by a host function.

The structural rows $H,C,P,N$ preserve, replay, source, and name the same packet. None of these rows introduces an equality principle for all functions, a quotient carrier, a selected limit, choice, trichotomy, or an ambient completeness coordinate. Hence every consumer read remains inside the displayed finite packet.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopRealFunctionSpaceUp$ consumer that reaches a target $\RealUp$ output without displaying $D,T,G,W,R,Q,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{real-function-space, continuousmap, real, streamname, regseqrat, dyadicratcore: no listed sibling supplies the whole packet $B=(D,T,G,W,R,Q,H,C,P,N)$, because each sibling contributes only a general function-space, continuous-map, real-name, stream-window, regular-readback, or dyadic-tolerance face while this chapter binds them as one finite Bishop real-function route.}

\closureat{BishopRealFunctionSpaceUp}{seedStr}

\begin{closurestatus}{\BishopRealFunctionSpaceUp}
  \constructivestory{A $\BishopRealFunctionSpaceUp$ packet is generated from source and target RealUp rows, finite graph-window rows, StreamName windows, RegSeqRat readbacks, a Bishop continuity replay row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-function-space-carrier}, \autoref{thm:bishop-real-function-space-namecert-obligations}, and \autoref{thm:bishop-real-function-space-nonescape} seed the finite L10 real-function route over RealUp, StreamNameUp, RegSeqRatUp, ContinuousMapUp, RealFunctionSpaceUp, and DyadicRatCoreUp rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function extensionality, quotient equality of maps, ambient function-space completion, selected pointwise limits, countable choice, real trichotomy, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, graph-window exactness, StreamName scheduling, RegSeqRat readback, Bishop continuity replay, transport, replay, provenance, and local naming for BEDC.Derived.BishopRealFunctionSpaceUp.}
\end{closurestatus}
