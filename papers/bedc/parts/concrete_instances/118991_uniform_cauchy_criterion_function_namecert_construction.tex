\chapter{A Concrete Naming Certificate for $\UniformCauchyCriterionFunctionUp$}
\label{ch:concrete-instances-uniform-cauchy-criterion-function-namecert}
\origin{human}

Following Bishop and Bridges, the $\UniformCauchyCriterionFunctionUp$ packet records the constructive uniform Cauchy criterion for a displayed sequence of functions before a ContinuousMap or completion consumer may read a terminal Real seal. It sits after the stream-name schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name carrier of \autoref{ch:concrete-instances-real-namecert}, the continuous-map route of \autoref{ch:concrete-instances-continuousmap-namecert}, and the metric-space carrier discipline of \autoref{ch:concrete-instances-metric-namecert}. The packet supplies a finite-window criterion for function sequences; it is not pointwise convergence by host functions, quotient stream equality, selected representatives, countable choice, trichotomy, or ambient completeness.

\begin{definition}[Uniform Cauchy criterion function carrier]
\label{def:uniform-cauchy-criterion-function-carrier}
A $\UniformCauchyCriterionFunctionUp$ carrier is a finite $\BHist$ packet
$$
U=(S,R,X,F,W,T,E,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ schedule for the function sequence and the finite family of inspected arguments.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational distance stream produced by the inspected function values.
\item $X$ is the displayed domain-window row whose metric comparisons are read through $\MetricSpaceUp$.
\item $F$ is the $\ContinuousMapUp$ row certifying that each inspected function value is read through the same displayed continuity route.
\item $W$ is the finite uniform tail window, listing the indices and argument windows needed for one requested tolerance.
\item $T$ is the dyadic tolerance ledger comparing the requested uniform bound with the distances generated by $S,R,X,F,W$.
\item $E$ is the terminal $\RealUp$ seal for the displayed uniform Cauchy bound.
\item $M$ is the target $\MetricSpaceUp$ distance row, consumed only after $S,R,X,F,W,T,E$ are visible.
\item $H$ records componentwise $\hsame$ transport for the stream schedule, regular readback, domain window, continuous-map row, uniform tail window, tolerance ledger, Real seal, metric row, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a uniform tolerance request through $S,R,X,F,W,T,E,M,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\UniformCauchyCriterionFunctionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ContinuousMapUp$, $\MetricSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\UniformCauchyCriterionFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for host function equality, pointwise-only convergence, quotient stream equality, selected representatives, countable-choice schedules, trichotomy, or ambient completeness.
\end{definition}

\begin{theorem}[Uniform Cauchy criterion function obligations]
\label{thm:uniform-cauchy-criterion-function-obligations}
For every accepted $\UniformCauchyCriterionFunctionUp$ carrier $U=(S,R,X,F,W,T,E,M,H,C,P,N)$, the local $\NameCert_{\UniformCauchyCriterionFunctionUp}$ surface consists exactly of carrier admission for the displayed stream schedule, regular readback, domain metric window, continuous-map row, finite uniform tail window, dyadic tolerance ledger, Real seal, metric row, componentwise transport, continuation replay, provenance, and local naming; uniform-tail exposure through $W$; Real-seal handoff through $T,E$; and ledger refusal for pointwise-only convergence, host function equality, quotient streams, selected representatives, countable choice, trichotomy, and ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-cauchy-criterion-function-carrier}. The rows $S,R,X,F,W,T,E,M$ are the only analytic rows available to a uniform Cauchy read, and $H,C,P,N$ are the only structural rows that may transport, replay, source, and name the packet.

Uniform-tail exposure is the role of $W$. It records one finite window for the requested tolerance, with the function-sequence schedule in $S$, the regular readback in $R$, the domain metric window in $X$, and the continuous-map row $F$ already displayed.

The tolerance ledger $T$ compares the requested bound with the distances generated by that finite window. The terminal Real row $E$ and metric row $M$ are therefore downstream of the finite uniform route, not detached pointwise evidence.

Since no carrier coordinate stores host function equality, quotient stream equality, selected representatives, countable choice, trichotomy, or ambient completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Uniform Cauchy criterion function finite-window induction]
\label{thm:uniform-cauchy-criterion-function-finite-window-induction}
Let $U=(S,R,X,F,W,T,E,M,H,C,P,N)$ be an accepted $\UniformCauchyCriterionFunctionUp$ carrier. Induction over the displayed finite uniform tail window preserves the route
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \ContinuousMapUp \longrightarrow \MetricSpaceUp \longrightarrow \RealUp .
$$
Strengthening or extending a finite prefix keeps the uniform Cauchy read inside $S,R,X,F,W,T,E,M,H,C,P,N$ and cannot introduce pointwise-only convergence, host function equality, quotient stream equality, selected representatives, countable choice, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
The base window is the carrier projection of \autoref{def:uniform-cauchy-criterion-function-carrier}. The displayed rows already expose the function-sequence schedule, regular rational readback, domain metric window, continuous-map row, finite tail window, dyadic tolerance ledger, terminal Real seal, and metric row.

For the induction step, extend the finite prefix by one accepted argument window or one accepted tail-index comparison. The obligation surface of \autoref{thm:uniform-cauchy-criterion-function-obligations} forces the extension to pass through $S,R,X,F,W$ before the tolerance ledger $T$ and Real seal $E$ are consumed.

If the prefix is strengthened without adding a new argument window, the same obligation surface keeps the readback and continuous-map rows fixed while only the displayed finite tolerance ledger is refined. The route therefore remains a uniform window route rather than a host-level pointwise convergence claim.

Finally inspect $H,C,P,N$. These structural rows transport, replay, source, and name only the same displayed packet, so the induction cannot acquire quotient stream equality, selected representatives, countable choice, trichotomy, host equality, or ambient completeness from outside the carrier.
\end{proof}

\falsifiablePrediction{An accepted $\UniformCauchyCriterionFunctionUp$ read is refuted if a ContinuousMap or completion consumer obtains a uniform Cauchy bound without displaying the StreamName schedule, RegSeqRat readback, ContinuousMap row, MetricSpace distance row, Real seal, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{streamname, regseqrat, real, continuous-map, metric-space: none alone is bijective with $\UniformCauchyCriterionFunctionUp$ because the packet binds a function-sequence schedule, regular readback, domain windows, continuous-map rows, finite uniform tail windows, dyadic tolerance accounting, terminal Real sealing, metric rows, transport, replay, provenance, and local NameCert rows in one uniform criterion surface.}

\closureat{UniformCauchyCriterionFunctionUp}{seedStr}
\leanstmt{BEDC.Derived.UniformCauchyCriterionFunctionUp}

\begin{closurestatus}{\UniformCauchyCriterionFunctionUp}
  \constructivestory{A $\UniformCauchyCriterionFunctionUp$ packet is generated from StreamName schedules, RegSeqRat readbacks, displayed metric windows, ContinuousMap rows, finite uniform tail windows, dyadic tolerance ledgers, RealUp seals, MetricSpace rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-cauchy-criterion-function-carrier}, \autoref{thm:uniform-cauchy-criterion-function-obligations}, and \autoref{thm:uniform-cauchy-criterion-function-finite-window-induction} seed the finite-window uniform Cauchy criterion for displayed function sequences over StreamNameUp, RegSeqRatUp, RealUp, ContinuousMapUp, MetricSpaceUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close pointwise-only convergence, host function equality, quotient stream equality, selected representatives, countable choice, trichotomy, ambient completeness, theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, uniform-tail exposure, Real-seal handoff, finite-window induction, transport, replay, provenance, and ledger refusal for BEDC.Derived.UniformCauchyCriterionFunctionUp.}
\end{closurestatus}
