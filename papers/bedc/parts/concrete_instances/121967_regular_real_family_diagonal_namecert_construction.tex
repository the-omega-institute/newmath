\chapter{A Concrete Naming Certificate for $\RegularRealFamilyDiagonalUp$}
\label{ch:concrete-instances-regular-real-family-diagonal-namecert}
\origin{human}

Bishop and Bridges constructive analysis treats diagonal arguments over regular real families through explicit finite observations, moduli, and null-tail comparisons rather than selected representatives of completed real names. The $\RegularRealFamilyDiagonalUp$ packet records that family-diagonal route as finite NameCert data. It sits over the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the zero-tail comparison service of \autoref{ch:concrete-instances-nullsequence-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-facing boundary of \autoref{ch:concrete-instances-real-cauchy-completion-namecert}. The packet names a finite diagonal family carrier for Real and Completion consumers; it is not quotient stream equality, a selected limit, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Regular real family diagonal carrier]
\label{def:regular-real-family-diagonal-carrier}
A $\RegularRealFamilyDiagonalUp$ carrier is a finite $\BHist$ packet
$$
F=(S,R,D,Z,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window family, listing the source-family coordinates and observation windows inspected by the diagonal request.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed family windows rather than from a selected stream representative.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the diagonal precision request, local dyadic slack, and comparison budget used by the readback.
\item $Z$ is the $\NullSequenceUp$ zero-tail row, displaying that the diagonal difference route is eventually absorbed by the named tolerance windows.
\item $E$ is the terminal $\RealUp$ seal row exposed only after $S,R,D,Z$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the stream-family, regular readback, dyadic tolerance, zero-tail, Real seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a diagonal-family request through $S,R,D,Z,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularRealFamilyDiagonalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\NullSequenceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularRealFamilyDiagonalUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one displayed regular-real family diagonal history. It has no coordinate for quotient streams, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.RegularRealFamilyDiagonalUp.RegularRealFamilyDiagonalTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular real family diagonal NameCert obligations]
\label{thm:regular-real-family-diagonal-namecert-obligations}
For every accepted $\RegularRealFamilyDiagonalUp$ carrier $F=(S,R,D,Z,E,H,C,P,N)$, the local $\NameCert_{\RegularRealFamilyDiagonalUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed stream-family windows, regular rational readback, dyadic tolerance ledger, zero-tail comparison, terminal Real seal, transport, replay, provenance, and local naming rows;
\item window-family exactness, requiring $S$ to expose only the finite source-family observations inspected by the diagonal request;
\item regular-readback handoff, requiring $R$ to be read from the displayed windows and not from an unrecorded representative of the source family;
\item dyadic tolerance exactness, requiring $D$ to bind the diagonal precision request to the readback and zero-tail rows;
\item null-tail handoff, requiring $Z$ to be displayed before the Real seal $E$ is consumed by any equality-facing or completion-facing reader;
\item ledger exhaustion, requiring $H,C,P,N$ to transport, replay, source, and name no quotient stream, selected limit, countable-choice schedule, trichotomy branch, host equality, ambient Real-completeness theorem, Lean verification, or checked bridge.
\end{enumerate}
No regular-real family diagonal obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-real-family-diagonal-carrier}. The accepted packet exposes exactly the finite stream-family windows, regular readback, dyadic tolerance row, zero-tail row, terminal seal, and structural rows.

Window-family exactness is read from $S$. A diagonal request names which family coordinates and finite windows are inspected before any regular readback can be consumed.

The regular-readback and dyadic clauses are supplied by $R$ and $D$. The readback row receives the displayed family windows, while the dyadic row records the precision and slack budget used by the diagonal comparison.

The null-tail handoff is the row $Z$. It must be displayed before the terminal Real seal $E$, so an equality-facing or completion-facing consumer receives the zero-tail evidence as part of the same finite route.

Finally $H,C,P,N$ preserve the route by componentwise transport, replay, provenance, and local naming. Since the carrier has no coordinate for the excluded quotient, selector, choice, trichotomy, host-equality, or completeness objects, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular real family diagonal seal route]
\label{thm:regular-real-family-diagonal-seal-route}
Every $\RealUp$-facing or $\RealCauchyCompletionUp$-facing consumer of an accepted $\RegularRealFamilyDiagonalUp$ carrier must read the displayed route
$$
S,R,D,Z,E,H,C,P,N
$$
before using the diagonal family as a real-name or completion-name input. The consumer therefore receives the finite StreamName family, RegSeqRat readback, DyadicRatCore tolerance, NullSequence zero-tail comparison, Real seal, transport, replay, provenance, and local naming rows as prior data. It cannot replace that route by quotient streams, selected limits, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-real-family-diagonal-carrier}. The only Real-facing endpoint is $E$, and the carrier places $E$ after the finite family windows, regular readback, dyadic tolerance ledger, and null-tail row.

Apply \autoref{thm:regular-real-family-diagonal-namecert-obligations}. The window-family, readback, dyadic, and null-tail obligation rows force every consumer to read $S,R,D,Z$ before the terminal Real seal can be used.

The completion-facing handoff is downstream of the same seal. A $\RealCauchyCompletionUp$ consumer may read the diagonal family only as the already named route $S,R,D,Z,E,H,C,P,N$; it receives no independent completion coordinate from the carrier.

The structural rows preserve this route and add no quotient stream, selected limit, countable-choice schedule, trichotomy branch, host equality, or ambient completeness theorem. Hence the displayed route is exhaustive for both Real-facing and completion-facing consumers.
\end{proof}

\closureat{RegularRealFamilyDiagonalUp}{seedStr}

\begin{closurestatus}{\RegularRealFamilyDiagonalUp}
  \constructivestory{A $\RegularRealFamilyDiagonalUp$ packet is generated from finite StreamName family windows, a RegSeqRat readback, a DyadicRatCore tolerance ledger, a NullSequence zero-tail comparison, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-real-family-diagonal-carrier}, \autoref{thm:regular-real-family-diagonal-namecert-obligations}, and \autoref{thm:regular-real-family-diagonal-seal-route} seed the finite diagonal-family route through StreamName, RegSeqRat, DyadicRatCore, NullSequence, Real, and RealCauchyCompletion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, countable choice, trichotomy, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window-family exactness, regular-readback handoff, dyadic tolerance exactness, null-tail handoff, Real seal ordering, transport, replay, provenance, and local naming for BEDC.Derived.RegularRealFamilyDiagonalUp.}
\end{closurestatus}
