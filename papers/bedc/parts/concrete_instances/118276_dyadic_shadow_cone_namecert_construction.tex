\chapter{A Concrete Naming Certificate for $\DyadicShadowConeUp$}
\label{ch:concrete-instances-dyadic-shadow-cone-namecert}
\origin{ai}

The $\DyadicShadowConeUp$ packet realizes the boundary-shadow and observer-expansion reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite dyadic radius windows. It sits after the boundary-shadow carrier of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the observer-expansion route of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, the visual metric surface of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the finite observation rows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. It names the dyadic cone of visible boundary shadows around an observer-side read; it does not complete the boundary, select a global observer horizon, or import host hyperbolic geometry.

\begin{definition}[Dyadic shadow cone carrier]
\label{def:dyadic-shadow-cone-carrier}
A $\DyadicShadowConeUp$ carrier is a finite $\BHist$ packet
$$
D=(Q,S,O,V,W,R,E,T,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the dyadic-radius row, listing the finite radius budget and the center of the observer-side window.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row exposing the boundary shadow read at that radius.
\item $O$ is the $\HyperbolicObserverExpansionUp$ row recording the observer-expansion source ledger.
\item $V$ is the $\HyperbolicVisualMetricUp$ row comparing the boundary shadow through a finite visual metric read.
\item $W$ is the $\StreamNameUp$ window row that records which finite observer and boundary coordinates have been inspected.
\item $R$ is the $\RegSeqRatUp$ readback row serving the dyadic radius and visual metric data.
\item $E$ is the terminal $\RealUp$ seal row for the finite radius or visual metric value.
\item $T$ is the transport row keeping the dyadic radius, boundary shadow, observer-expansion, visual metric, stream-window, readback, and Real-seal rows in one displayed route.
\item $L$ is the replay ledger that records the accepted read as a cone row rather than as an isolated shadow or isolated expansion row.
\item $H$ records componentwise $\hsame$ transport for the radius, shadow, observer, visual metric, window, readback, seal, transport, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from dyadic-radius requests through boundary-shadow reads, observer-expansion rows, visual-metric comparison, finite windows, regular readback, Real sealing, and transport.
\item $P$ is the $\Pkg$ provenance over $\DyadicShadowConeUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicObserverExpansionUp$, $\HyperbolicVisualMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicShadowConeUp}$ row.
\end{enumerate}
The classifier compares accepted cones only through $Q,S,O,V,W,R,E,T,L,H,C,P,N$. It has no coordinate for a completed hyperbolic boundary, selected observer horizon, quotient boundary equality, detached visual metric scalar, global disk model, or host geometric compactness.
\end{definition}

\begin{theorem}[Dyadic shadow cone NameCert obligations]
\label{thm:dyadic-shadow-cone-namecert-obligations}
For every accepted $\DyadicShadowConeUp$ carrier
$$
D=(Q,S,O,V,W,R,E,T,L,H,C,P,N),
$$
the local $\NameCert_{\DyadicShadowConeUp}$ surface consists exactly of dyadic-radius exposure, boundary-shadow exposure, observer-expansion exposure, visual-metric comparison, finite $\StreamNameUp$ window inspection, $\RegSeqRatUp$ readback, terminal $\RealUp$ sealing, route transport, cone replay, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation row may replace the dyadic radius window by a completed boundary point, detach the visual metric from the boundary-shadow route, or read observer expansion without the same finite cone packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-shadow-cone-carrier}. The dyadic radius row $Q$, boundary-shadow row $S$, observer-expansion row $O$, and visual metric row $V$ are simultaneous source rows for the cone.

Read the finite observation route next. The stream window $W$ fixes which observer and boundary coordinates are inspected, while $R$ and $E$ provide the regular rational and Real-facing values reached by that inspection.

The transport and replay rows $T,L$ keep the read as one cone route. They prevent a consumer from taking an isolated shadow, an isolated expansion row, or a detached visual scalar as the whole carrier.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no completed boundary, selected horizon, quotient equality, detached visual metric scalar, global disk model, or host compactness coordinate appears in the carrier, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Dyadic shadow cone observer-window nonescape]
\label{thm:dyadic-shadow-cone-observer-window-nonescape}
Every accepted observer-expansion read of a $\DyadicShadowConeUp$ packet factors through
$$
\HyperbolicBoundaryShadowUp
\longmapsto
\HyperbolicObserverExpansionUp
\longmapsto
\HyperbolicVisualMetricUp
\longmapsto
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
The read may narrow the dyadic radius or refine the finite boundary window, but it must keep the radius, boundary-shadow, observer-expansion, visual-metric, StreamName, RegSeqRat, Real-seal, transport, replay, provenance, and NameCert rows visible. It cannot escape to a completed boundary object, selected horizon, quotient boundary equality, detached visual scalar, global disk model, or host hyperbolic compactness.
\end{theorem}
\begin{proof}
Begin with the dyadic radius and boundary-shadow rows. They expose a finite cone window, not a completed boundary point.

Read the observer-expansion and visual-metric rows next. The observer row records the source ledger being expanded, and the visual metric row compares it only through the displayed boundary-shadow route.

The StreamName and RegSeqRat rows then expose the finite observation schedule and the regular rational readback. The Real seal is downstream of those rows and therefore cannot be consumed as an independent geometric scalar.

Transport, replay, provenance, and local naming preserve the same packet through $T,L,H,C,P,N$. Thus every observer-window read remains inside the displayed dyadic shadow cone.
\end{proof}

The Lean TasteGate file at \texttt{lean4/BEDC/Derived/DyadicShadowConeUp/TasteGate.lean} must include explicit \texttt{Nontrivial DyadicShadowConeUp} and \texttt{FieldFaithful DyadicShadowConeUp} instances preserving $Q,S,O,V,W,R,E,T,L,H,C,P,N$ as distinct fields before a checked carrier-alignment theorem can witness the gate.

\falsifiablePrediction{Every accepted $\DyadicShadowConeUp$ read exposes dyadic-radius, boundary-shadow, observer-expansion, visual-metric, StreamName, RegSeqRat, Real-seal, transport, replay, provenance, and NameCert rows before any boundary-shadow or observer-expansion consumer reads it.}

\independenceWitness{Nearest siblings: hyperbolic-boundary-shadow, hyperbolic-observer-expansion, hyperbolic-visual-metric, streamname, and regseqrat. $\DyadicShadowConeUp$ is not bijective to any one sibling because it jointly carries dyadic radius windows, boundary shadows, observer-expansion rows, visual-metric reads, finite observations, regular readback, Real sealing, transport, replay, provenance, and local naming as one packet.}

\closureat{DyadicShadowConeUp}{seedStr}

\begin{closurestatus}{\DyadicShadowConeUp}
  \constructivestory{A $\DyadicShadowConeUp$ packet is generated from dyadic-radius rows, boundary-shadow rows, observer-expansion rows, visual-metric comparison, StreamName windows, RegSeqRat readback, a Real seal, route transport, cone replay, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-shadow-cone-carrier}, \autoref{thm:dyadic-shadow-cone-namecert-obligations}, and \autoref{thm:dyadic-shadow-cone-observer-window-nonescape} seed the finite dyadic shadow-cone route for boundary-shadow and observer-expansion reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed boundary compactification, selected observer horizons, quotient boundary equality, detached visual metric scalars, global disk models, host hyperbolic compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked dyadic-radius exposure, boundary-shadow exposure, observer-expansion exposure, visual-metric comparison, StreamName window inspection, RegSeqRat readback, Real sealing, transport, replay, provenance, local naming, and TasteGate field faithfulness for BEDC.Derived.DyadicShadowConeUp.}
\end{closurestatus}
