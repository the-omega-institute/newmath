\chapter{A Concrete Naming Certificate for $\HyperbolicSectorCoverUp$}
\label{ch:concrete-instances-hyperbolic-sector-cover-namecert}
\origin{ai}

The $\HyperbolicSectorCoverUp$ packet realizes the hyperbolic observer-expansion sector-cover route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite boundary-shadow, visual-radius, overlap, refinement, transport, provenance, and local naming rows. It sits beside the sector-expansion packet of \autoref{ch:concrete-instances-hyperbolic-sector-expansion-namecert}, the sector-refinement packet of \autoref{ch:concrete-instances-hyperbolic-sector-refinement-namecert}, the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual-metric packet of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, and the observer-shell packet of \autoref{ch:concrete-instances-hyperbolic-observer-shell-namecert}. The packet names a finite cover of displayed observer sectors; it is not a completed boundary cover, a selected geodesic fan, host visual compactness, or a global hyperbolic model.

\begin{definition}[Hyperbolic sector-cover carrier]
\label{def:hyperbolic-sector-cover-carrier}
A $\HyperbolicSectorCoverUp$ carrier is a finite $\BHist$ packet
$$
S=(E,F,B,V,O,R,U,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\HyperbolicSectorExpansionUp$ source row, exposing finite sector aperture and observer-window expansion data.
\item $F$ is the $\HyperbolicSectorRefinementUp$ row, recording finite refinement of displayed sectors without completing a sector family.
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row, attaching each sector to a finite boundary-shadow window.
\item $V$ is the $\HyperbolicVisualMetricUp$ row, supplying visual-radius comparison data for the displayed sector windows.
\item $O$ is the overlap row, listing finite overlap witnesses between neighbouring sector windows.
\item $R$ is the refinement-stability row, requiring the cover rows to remain readable after the refinement row $F$ is applied.
\item $U$ is the $\HyperbolicObserverShellUp$ provenance row, tying the sector cover to the observer-shell boundary surface.
\item $K$ is the TasteGate request row requiring Lean's `TasteGate.lean` surface to provide $\mathsf{Nontrivial}\ \HyperbolicSectorCoverUp$ and $\mathsf{FieldFaithful}\ \HyperbolicSectorCoverUp$ instances for this row inventory.
\item $H$ records componentwise $\hsame$ transport for $E,F,B,V,O,R,U,K,C,P,N$.
\item $C$ records displayed $\Cont$ replay through sector expansion, sector refinement, boundary shadow, visual metric, overlap, refinement stability, observer shell, TasteGate requests, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicSectorCoverUp$, $\HyperbolicSectorExpansionUp$, $\HyperbolicSectorRefinementUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicObserverShellUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicSectorCoverUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,F,B,V,O,R,U,K,H,C,P,N$. It has no coordinate for a completed ideal-boundary cover, selected geodesic representatives, host compactness of shadows, ambient visual topology, or a physical observer law.
\end{definition}

\begin{theorem}[Hyperbolic sector-cover NameCert obligations]
\label{thm:hyperbolic-sector-cover-namecert-obligations}
For every accepted $\HyperbolicSectorCoverUp$ carrier
$$
S=(E,F,B,V,O,R,U,K,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicSectorCoverUp}$ surface consists exactly of sector-expansion exposure through $E$, sector-refinement exposure through $F$, boundary-shadow exposure through $B$, visual-radius comparison through $V$, finite overlap exposure through $O$, refinement stability through $R$, observer-shell provenance through $U$, TasteGate requests through $K$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-sector-cover-carrier}. Its public rows are exactly the sector expansion, sector refinement, boundary shadow, visual metric, overlap, refinement stability, observer shell, TasteGate request, transport, replay, provenance, and naming rows.

The cover route is finite at every read. The expansion row $E$ supplies displayed sector windows, $F$ refines those windows, $B$ attaches boundary-shadow data, $V$ supplies visual-radius comparisons, and $O$ lists only finite overlap witnesses.

Refinement stability and observer-shell provenance are then read through $R$ and $U$. The TasteGate row $K$ records the Lean-side requirement that `TasteGate.lean` provide Nontrivial and FieldFaithful instances for the same carrier rather than for a detached host cover.

Finally $H,C,P,N$ preserve, replay, source, and name the route. Since the carrier has no coordinate for completed boundary covers, selected geodesic fans, host visual compactness, ambient visual topology, or physical observer laws, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic sector-cover refinement route]
\label{thm:hyperbolic-sector-cover-refinement-route}
Every observer-expansion consumer of an accepted $\HyperbolicSectorCoverUp$ packet must factor through
$$
\HyperbolicSectorExpansionUp
\longmapsto
\HyperbolicSectorRefinementUp
\longmapsto
(\HyperbolicBoundaryShadowUp,\HyperbolicVisualMetricUp)
\longmapsto
O,R,U,K,H,C,P,N .
$$
Thus the readable sector cover is a finite route through boundary-shadow, visual-radius, overlap, refinement, observer-shell provenance, TasteGate requests, transport, replay, package provenance, and local NameCert rows.
\end{theorem}
\begin{proof}
Start with the expansion and refinement rows. The sector cover first reads finite sector windows through $E$ and then applies the displayed refinement row $F$, so it cannot begin from a completed cover family.

Next read the boundary-shadow and visual-metric rows. The rows $B$ and $V$ attach every sector window to a finite boundary shadow and visual-radius comparison before an overlap witness is public.

The overlap, stability, observer-shell, and TasteGate rows $O,R,U,K$ then bind the cover as an observer-expansion object. The row $K$ is part of the public obligation surface: Lean's `TasteGate.lean` must supply Nontrivial and FieldFaithful instances before this packet can be treated as field-faithful sector-cover data.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. No completed ideal-boundary cover, selected geodesic fan, host compactness theorem, or physical observer law is exported.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicSectorCoverUp$ consumer exposes sector-expansion, sector-refinement, boundary-shadow, visual-radius, finite-overlap, refinement-stability, observer-shell provenance, TasteGate request, transport, replay, provenance, and local naming rows before using the packet as a sector cover.}

\independenceWitness{hyperbolic-sector-expansion, hyperbolic-sector-refinement, hyperbolic-boundary-shadow, hyperbolic-visual-metric, hyperbolic-observer-shell: $\HyperbolicSectorCoverUp$ is not bijective to any listed sibling because it jointly binds expansion, refinement, boundary-shadow, visual-radius, overlap, observer-shell provenance, TasteGate requests, transport, replay, package provenance, and local NameCert rows.}

\closureat{HyperbolicSectorCoverUp}{seedStr}

\begin{closurestatus}{\HyperbolicSectorCoverUp}
  \constructivestory{A $\HyperbolicSectorCoverUp$ packet is generated from sector-expansion rows, sector-refinement rows, boundary-shadow windows, visual-radius comparisons, finite overlap witnesses, refinement stability, observer-shell provenance, TasteGate requests, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-sector-cover-carrier}, \autoref{thm:hyperbolic-sector-cover-namecert-obligations}, and \autoref{thm:hyperbolic-sector-cover-refinement-route} seed the finite observer-expansion sector-cover packet over expansion, refinement, boundary-shadow, visual-metric, overlap, observer-shell, TasteGate, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed ideal-boundary covers, selected geodesic fans, host visual compactness, ambient visual topology, physical observer laws, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, expansion exposure, refinement exposure, boundary-shadow exactness, visual-radius exactness, finite overlap, refinement stability, observer-shell provenance, TasteGate Nontrivial and FieldFaithful instances, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicSectorCoverUp.}
\end{closurestatus}
