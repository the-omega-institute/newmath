\chapter{A Concrete Naming Certificate for $\UniformLimitDerivativeUp$}
\label{ch:concrete-instances-uniform-limit-derivative-namecert}
\origin{human}

Following Bishop and Bridges, and the standard Rudin termwise-differentiation theorem, the $\UniformLimitDerivativeUp$ packet records the finite BEDC route by which a sequence of differentiable real-valued names with uniformly convergent derivative rows and one convergent base-point row yields a derivative handoff for the displayed limit. It sits after the derivative certificate of \autoref{ch:concrete-instances-derivative-namecert}, the regular-Cauchy uniform-limit route of \autoref{ch:concrete-instances-regular-cauchy-uniform-limit-namecert}, the continuity boundary of \autoref{ch:concrete-instances-uniform-limit-continuous-namecert}, and the integral-side neighbor of \autoref{ch:concrete-instances-uniform-limit-integral-namecert}. The packet names only the displayed termwise-differentiation route through finite histories; it does not import open-cover compactness, quotient function equality, selected limits, ambient $\RealUp$ completeness, or full calculus.

\begin{definition}[Uniform limit derivative carrier]
\label{def:uniform-limit-derivative-carrier}
A $\UniformLimitDerivativeUp$ carrier is a finite $\BHist$ packet
$$
D=(F,G,U,B,R,J,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the sequence row listing differentiable $\RealUp$ maps together with their common interval window and their local $\DerivativeUp$ certificates.
\item $G$ is the derivative-sequence row, recording the displayed derivative names produced componentwise from $F$.
\item $U$ is the uniform derivative-limit row through $\RegularCauchyUniformLimitUp$, carrying the shared modulus and derivative-limit readback.
\item $B$ is the base-point convergence row, read through $\RegSeqRatUp$ and $\RealUp$, recording that one displayed point has a regular-Cauchy value limit for the original sequence.
\item $R$ is the regular-Cauchy reconstruction row that combines $U$ and $B$ into the limit-function readback route.
\item $J$ is the derivative handoff row, exposing the derivative of the limit only after the derivative-limit and base-point rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for $F,G,U,B,R,J$.
\item $C$ records displayed $\Cont$ replay through the differentiability, uniform derivative-limit, base-point convergence, regular-Cauchy reconstruction, and derivative-handoff rows.
\item $P$ is the $\Pkg$ provenance over $\UniformLimitDerivativeUp$, $\DerivativeUp$, $\RegularCauchyUniformLimitUp$, $\UniformLimitContinuousUp$, $\UniformLimitIntegralUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformLimitDerivativeUp}$ row.
\end{enumerate}
The carrier has no coordinate for an open-cover compactness principle, quotient equality of functions, selected host limits, ambient completeness of $\RealUp$, unrestricted integration, Taylor expansion, inverse-function reasoning, or a full calculus library.
\end{definition}

\begin{theorem}[Uniform limit derivative NameCert obligations]
\label{thm:uniform-limit-derivative-namecert-obligations}
For the carrier of \autoref{def:uniform-limit-derivative-carrier}, the local $\NameCert_{\UniformLimitDerivativeUp}$ surface consists exactly of sequence admission through $F$, componentwise derivative exposure through $G$, uniform derivative-limit exposure through $U$, base-point regular-Cauchy convergence through $B$, limit readback through $R$, derivative handoff through $J$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, local naming through $N$, and non-escape for open-cover compactness, quotient function equality, selected limits, ambient $\RealUp$ completeness, and full calculus.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:uniform-limit-derivative-carrier}. The differentiable sequence, derivative sequence, uniform derivative-limit, base-point convergence, limit readback, and derivative handoff are separate displayed coordinates before the local name is consumed.

The structural rows $H,C,P,N$ preserve that same route under transport, replay, provenance, and local naming. Since the carrier has no coordinate for open-cover compactness, quotient function equality, selected limits, ambient $\RealUp$ completeness, or a full calculus interface, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Uniform limit derivative Cauchy route]
\label{thm:uniform-limit-derivative-cauchy-route}
Every accepted $\UniformLimitDerivativeUp$ packet exposes the termwise-differentiation read only through the finite route
$$
F \longmapsto G \longmapsto U \longmapsto B \longmapsto R \longmapsto J \longmapsto H,C,P,N .
$$
The derivative handoff $J$ is downstream of both the uniformly convergent derivative sequence and the base-point $\RegSeqRatUp/\RealUp$ convergence row; it is not available from a quotient equality of functions, a selected limiting function, ambient completeness, open-cover compactness, or an unrestricted calculus principle.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:uniform-limit-derivative-namecert-obligations}. They expose $F,G,U,B,R,J$ as ordered carrier rows and place the derivative handoff after the uniform derivative-limit row and the base-point convergence row.

Read the Cauchy data next. The row $U$ supplies the shared derivative-limit modulus, while $B$ supplies the single base-point regular-Cauchy convergence needed to reconstruct the displayed limit-function route $R$.

Finally replay through $H,C,P,N$. Transport and continuation keep the same finite route visible, and provenance records the derivative, uniform-limit, continuity, integral-neighbor, regular-Cauchy, and Real packets without adding host function equality, selected limits, ambient completeness, open-cover compactness, or a full calculus endpoint.
\end{proof}

\closureat{UniformLimitDerivativeUp}{seedStr}

\begin{closurestatus}{\UniformLimitDerivativeUp}
  \constructivestory{A $\UniformLimitDerivativeUp$ packet is generated from differentiable RealUp map rows, their derivative rows, a uniform derivative-limit row, a base-point regular-Cauchy convergence row, limit readback, derivative handoff, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-limit-derivative-carrier}, \autoref{thm:uniform-limit-derivative-namecert-obligations}, and \autoref{thm:uniform-limit-derivative-cauchy-route} seed the finite termwise-differentiation route over $\DerivativeUp$, $\RegularCauchyUniformLimitUp$, $\UniformLimitContinuousUp$, $\UniformLimitIntegralUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient function equality, selected limits, ambient $\RealUp$ completeness, unrestricted integration, Taylor expansion, inverse-function reasoning, full calculus, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative-sequence exposure, uniform derivative-limit exposure, base-point regular-Cauchy convergence, derivative handoff, transport, replay, provenance, and local naming for BEDC.Derived.UniformLimitDerivativeUp.}
\end{closurestatus}
