\chapter{A Concrete Naming Certificate for $\BishopRegularRealLocatorUp$}
\label{ch:concrete-instances-bishop-regular-real-locator-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\BishopRegularRealLocatorUp$ packet records the locator surface carried by a regular Cauchy real name before any Real-facing approximation consumer reads it. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window service of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet supplies located approximation windows for regular Cauchy data; it is not quotient real equality, countable choice, trichotomy, host equality, or an ambient $\RealUp$ completeness principle.

\begin{definition}[Bishop regular real locator carrier]
\label{def:bishop-regular-real-locator-carrier}
A $\BishopRegularRealLocatorUp$ carrier is a finite $\BHist$ packet
$$
L=(D,S,R,A,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance and rational-probe ledger, carrying the requested dyadic radius, lower and upper rational guards, and comparison slack.
\item $S$ is the $\StreamNameUp$ finite-window row opened from the regular Cauchy name at the requested precision.
\item $R$ is the $\RegSeqRatUp$ readback row that exposes the regular rational approximants attached to the same finite windows.
\item $A$ is the located approximation-window row, recording the finite rational interval or apartness window accepted for the requested dyadic tolerance.
\item $E$ is the terminal $\RealUp$ seal read only after $D,S,R,A$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the dyadic tolerance, stream window, regular rational readback, approximation-window, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an approximation request through $D,S,R,A,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopRegularRealLocatorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegularRealLocatorUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,S,R,A,E,H,C,P,N$. It has no coordinate for quotient real equality, countable-choice schedules, trichotomy of arbitrary real comparisons, host equality of completed objects, or ambient $\RealUp$ completeness.
\end{definition}
\leandef{BEDC.Derived.BishopRegularRealLocatorUp.BishopRegularRealLocatorUp}

\begin{theorem}[Bishop regular real locator NameCert obligations]
\label{thm:bishop-regular-real-locator-namecert-obligations}
For every accepted $\BishopRegularRealLocatorUp$ carrier $L=(D,S,R,A,E,H,C,P,N)$, the local $\NameCert_{\BishopRegularRealLocatorUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the dyadic tolerance ledger, finite stream window, regular rational readback, located approximation window, terminal Real seal, transport, replay, provenance, and local naming rows;
\item source agreement, requiring $D$, $S$, and $R$ to refer to the same displayed regular Cauchy name before $A$ is read;
\item locator exactness, requiring the approximation window in $A$ to be bounded by the dyadic tolerance and rational guards named in $D$;
\item regular readback binding, requiring $A$ to be justified by the displayed $\RegSeqRatUp$ row $R$ and not by a host real representative;
\item Real seal non-escape, requiring $E$ to be downstream of the finite dyadic, stream, regular-rational, and approximation-window rows;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through $H,C,P,N$;
\item refusal of quotient real equality, countable choice, trichotomy, host equality, and ambient $\RealUp$ completeness as certificate data.
\end{enumerate}
No locator obligation is stored outside these finite rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-regular-real-locator-carrier}. The only analytic rows exposed before the Real seal are the dyadic tolerance ledger $D$, the finite stream window $S$, the regular rational readback $R$, and the approximation window $A$.

Source agreement is read from the shared route $D,S,R$. The locator row $A$ can answer a rational approximation request only after the requested dyadic tolerance and the inspected regular-rational window have both been displayed.

The Real-facing row $E$ is downstream of that finite route. Transport through $H$ and replay through $C$ preserve the same dyadic, stream, readback, and approximation coordinates, while $P$ and $N$ package the local certificate.

Since the carrier has no coordinate for quotient equality, choice, trichotomy, host equality, or ambient completeness, none of those principles can enter the NameCert surface.
\end{proof}

\begin{theorem}[Bishop regular real locator approximation handoff]
\label{thm:bishop-regular-real-locator-approximation-handoff}
Every approximation consumer reading an accepted $\BishopRegularRealLocatorUp$ carrier factors through the finite route
$$
D,S,R \longmapsto A \longmapsto E \longmapsto H,C,P,N .
$$
The consumer receives a displayed rational window over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ data. It receives no quotient real equality witness, no countable-choice schedule, no trichotomy oracle, no host equality comparison, and no ambient $\RealUp$ completeness assertion.
\end{theorem}
\begin{proof}
Begin with an approximation request for the accepted carrier. The request first reads $D$ for the dyadic tolerance and rational guards, then opens the finite stream window $S$ and the regular rational readback $R$ attached to the same regular Cauchy name.

The located approximation window $A$ is therefore a finite consequence of displayed regular-rational data. It is not a selected representative of a quotient real and does not require a comparison of completed host objects.

The final Real-facing handoff passes through $E,H,C,P,N$. These rows seal, transport, replay, source, and name the same route, so a downstream consumer can inspect the approximation window without importing countable choice, trichotomy, host equality, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopRegularRealLocatorUp$ packet exposing a Real-facing approximation handoff must display dyadic tolerance, StreamName window, RegSeqRat readback, located approximation, Real seal, hsame transport, Cont replay, Pkg provenance, and NameCert rows.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real: $\BishopRegularRealLocatorUp$ is not bijective to any listed sibling because it jointly carries a dyadic tolerance request, finite stream observation, regular rational readback, located approximation window, terminal Real seal, and local naming rows.}

\closureat{BishopRegularRealLocatorUp}{seedStr}

\begin{closurestatus}{\BishopRegularRealLocatorUp}
  \constructivestory{A $\BishopRegularRealLocatorUp$ packet is generated from a DyadicRatCore tolerance ledger, a StreamName finite-window row, a RegSeqRat regular readback row, a located approximation-window row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-real-locator-carrier}, \autoref{thm:bishop-regular-real-locator-namecert-obligations}, and \autoref{thm:bishop-regular-real-locator-approximation-handoff} seed the finite locator surface for regular Cauchy real names and Real-facing approximation consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, countable choice, trichotomy, host equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source agreement, locator exactness, regular readback binding, Real seal non-escape, transport, replay, provenance, local naming, and approximation handoff for BEDC.Derived.BishopRegularRealLocatorUp.}
\end{closurestatus}
