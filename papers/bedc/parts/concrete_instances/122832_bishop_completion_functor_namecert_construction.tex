\chapter{A Concrete Naming Certificate for $\BishopCompletionFunctorUp$}
\label{ch:concrete-instances-bishop-completion-functor-namecert}
\origin{human}

Following Bishop and Bridges on constructive metric completion, the $\BishopCompletionFunctorUp$ packet records the functorial completion route as finite NameCert data. It sits over the metric completion carrier of \autoref{ch:concrete-instances-metriccompletion-namecert}, the operator surface of \autoref{ch:concrete-instances-cauchycompletionoperator-namecert}, the uniform-completion handoff of \autoref{ch:concrete-instances-uniformcompletion-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the complete separated landing object of \autoref{ch:concrete-instances-complete-separated-metric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet exports a constructive completion functor surface for downstream $\RealUp$ and completion-facing chapters, while refusing quotient completion equality, selected representatives, selected limits, countable choice, a host completion operator, and ambient $\RealUp$ completeness.

\begin{definition}[Bishop completion functor carrier]
\label{def:bishop-completion-functor-carrier}
A $\BishopCompletionFunctorUp$ carrier is a finite $\BHist$ packet
$$
B=(M,O,U,S,R,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricCompletionUp$ source row, naming the metric carrier and completion request.
\item $O$ is the $\CauchyCompletionOperatorUp$ row that turns displayed Cauchy data into a completion-facing handoff.
\item $U$ is the $\UniformCompletionUp$ row carrying the uniform completion request attached to the same metric source.
\item $S$ is the $\StreamNameUp$ finite-window schedule used by the Cauchy names.
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger controlling the window and readback rows.
\item $Q$ is the $\CompleteSeparatedMetricUp$ landing row for the separated completion target.
\item $E$ is the terminal $\RealUp$ seal row used by Real-facing consumers.
\item $H$ records componentwise $\hsame$ transport for metric completion, operator, uniform completion, stream, regular-rational, dyadic, complete-separated, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion request through
$$
M,O,U,S,R,D,Q,E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopCompletionFunctorUp$, $\MetricCompletionUp$, $\CauchyCompletionOperatorUp$, $\UniformCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CompleteSeparatedMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopCompletionFunctorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,O,U,S,R,D,Q,E,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, selected limits, countable-choice schedules, a host completion operator, or ambient Real completeness.
\end{definition}

\begin{theorem}[Bishop completion functor NameCert obligations]
\label{thm:bishop-completion-functor-namecert-obligations}
For every accepted $\BishopCompletionFunctorUp$ carrier
$$
B=(M,O,U,S,R,D,Q,E,H,C,P,N),
$$
the local $\NameCert_{\BishopCompletionFunctorUp}$ surface consists exactly of metric-completion source exposure through $M$, Cauchy-completion operator exposure through $O$, uniform-completion handoff through $U$, StreamName window exposure through $S$, RegSeqRat readback through $R$, DyadicRatCore tolerance exposure through $D$, complete-separated landing exposure through $Q$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-completion-functor-carrier}. The rows $M,O,U$ give the completion-facing source, operator, and uniform handoff, while $S,R,D$ give the finite L10 readback route.

The landing rows $Q,E$ are downstream of that finite route. They expose the complete separated metric object and the terminal Real seal only after the StreamName, RegSeqRat, and DyadicRatCore rows have been displayed.

The structural rows $H,C,P,N$ preserve, replay, source, and name the same packet. Since no quotient completion equality, selected representative, selected limit, countable-choice schedule, host completion operator, or ambient Real-completeness coordinate occurs in the carrier, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Bishop completion functor L10 handoff]
\label{thm:bishop-completion-functor-l10-handoff}
Every completion-facing or $\RealUp$ consumer of an accepted $\BishopCompletionFunctorUp$ packet must factor through the finite route
$$
\MetricCompletionUp,\ \CauchyCompletionOperatorUp,\ \UniformCompletionUp
\longmapsto
\StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp
\longmapsto
\CompleteSeparatedMetricUp,\ \RealUp
\longmapsto
H,C,P,N .
$$
The handoff supplies a constructive completion functor row over displayed Cauchy and regular-name data. It supplies no quotient completion equality, selected representative, selected limit, countable choice, host completion operator, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:bishop-completion-functor-namecert-obligations}. It places every completion-facing read behind $M,O,U$ and every L10 readback behind $S,R,D$.

The separated landing row $Q$ and the Real seal row $E$ are reached only after those displayed rows. Therefore a consumer cannot replace the route by a quotient completion class, a selected representative, or a selected limit.

The replay and provenance rows keep the route inside the finite packet. Thus downstream Real and completion-facing chapters consume a displayed Bishop completion functor surface, not a host completion operator or an ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopCompletionFunctorUp$ read must display MetricCompletion, CauchyCompletionOperator, UniformCompletion, StreamName, RegSeqRat, DyadicRatCore, CompleteSeparatedMetric, Real, hsame, Cont, Pkg, and NameCert rows before any completion-facing or Real-facing output is available.}

\independenceWitness{metriccompletion, cauchycompletionoperator, uniformcompletion, streamname, regseqrat, dyadicratcore, complete-separated-metric, real: $\BishopCompletionFunctorUp$ is not bijective to any listed sibling because it binds the completion operator, uniform handoff, L10 finite-window route, separated landing row, Real seal, transport, replay, provenance, and local naming as one functorial completion packet.}

\closureat{BishopCompletionFunctorUp}{seedStr}

\begin{closurestatus}{\BishopCompletionFunctorUp}
  \constructivestory{A $\BishopCompletionFunctorUp$ packet is generated from MetricCompletion, CauchyCompletionOperator, UniformCompletion, StreamName, RegSeqRat, DyadicRatCore, CompleteSeparatedMetric, Real, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-completion-functor-carrier}, \autoref{thm:bishop-completion-functor-namecert-obligations}, and \autoref{thm:bishop-completion-functor-l10-handoff} seed the constructive completion functor route over the L10 Cauchy, StreamName, RegSeqRat, DyadicRatCore, CompleteSeparatedMetric, and Real spine.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected representatives, selected limits, countable choice, host completion operators, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, completion operator exposure, uniform handoff, finite-window readback, separated landing, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.BishopCompletionFunctorUp.}
\end{closurestatus}
