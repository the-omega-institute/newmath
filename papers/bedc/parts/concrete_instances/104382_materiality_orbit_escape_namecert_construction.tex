\chapter{A Concrete Naming Certificate for $\MaterialityOrbitEscapeUp$}
\label{ch:concrete-instances-materiality-orbit-escape-namecert}
\origin{ai}

The $\MaterialityOrbitEscapeUp$ packet realizes the schema of \autoref{subsec:philosophy-materiality-orbit-escape} as finite NameCert data. It does not add a primitive subject, a context-free materiality predicate, or an absolute free-will predicate; it packages observer-history identity, feedback prefixes, identity closure, ledger-certified orbit rules, orbit closure, certified escape, transport, replay, provenance, and local naming as one finite carrier.

\begin{definition}[Materiality-orbit-escape carrier]
\label{def:materiality-orbit-escape-carrier}
A $\MaterialityOrbitEscapeUp$ carrier is a finite $\BHist$ packet
$$
M=(O,F,I,R,K,A,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the observer-history row, including the local observational context and the observer-history identity classifier.
\item $F$ is the feedback-prefix row, a finite list of response histories generated through the displayed probe route.
\item $I$ is the identity-closure row, recording positive classifier witnesses that the prefix remains traceable as one observed datum.
\item $R$ is the finite rule-family row, whose members are ledger-certified orbit rules with displayed $\Cont$ routes and state-classifier compatibility.
\item $K$ is the orbit-closure row, recording that at least one displayed rule in $R$ closes the prefix under the state classifier.
\item $A$ is the certified-escape row, recording for each displayed rule in $R$ a positive separation witness at a displayed feedback step.
\item $E$ is the appearance classifier row. It reads material appearance from $I$ and $K$, and freedom-like appearance from $I$ and $A$.
\item $H$ records componentwise $\hsame$ transport for observer histories, feedback histories, identity-closure witnesses, rule-family rows, orbit-closure rows, escape rows, and appearance rows.
\item $C$ records displayed $\Cont$ replay of the observer-history, feedback-prefix, rule-family, orbit-closure, and certified-escape routes.
\item $P$ is the $\Pkg$ provenance over $\MaterialityOrbitEscapeUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, observer-history identity, feedback prefixes, local clock budgets, layered relation certificates, finite witness routes, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MaterialityOrbitEscapeUp}$ row.
\end{enumerate}
The carrier admits only these rows. Its classifier compares accepted packets by transporting the displayed observer-history row, feedback prefix, identity closure, finite rule family, orbit-closure row, certified-escape row, appearance classifier, replay route, provenance, and local naming.
\end{definition}

\begin{theorem}[Materiality-orbit-escape NameCert obligations]
\label{thm:materiality-orbit-escape-namecert-obligations}
For every accepted $\MaterialityOrbitEscapeUp$ packet
$$
M=(O,F,I,R,K,A,E,H,C,P,N),
$$
the local $\NameCert_{\MaterialityOrbitEscapeUp}$ surface consists exactly of observer-history identity admission, feedback-prefix admission, identity-closure readback, rule-family admission, orbit-closure readback, certified-escape readback, appearance classification, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The packet exports no context-free materiality predicate, no primitive agency row, no global observer, no completed feedback stream, no rule selector outside the ledger, and no metaphysical free-will witness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:materiality-orbit-escape-carrier}. The observer row $O$, feedback row $F$, identity row $I$, rule-family row $R$, orbit row $K$, escape row $A$, and appearance row $E$ are the only semantic coordinates.

Identity closure is local to the displayed observer-history row. Transport through $H$ preserves the same feedback histories and identity witnesses, while replay through $C$ returns to the same finite prefix.

Material appearance is read only from $I$ and $K$. The orbit-closing rule must already be a member of the displayed rule-family row $R$, so no external dynamics or host-level predictor is introduced.

Freedom-like appearance is read only from $I$ and $A$. The escape row gives positive separation from each currently displayed rule in $R$, not absence of all possible rules.

The provenance and local naming rows source only this finite inventory. Hence the context-free, metaphysical, global-observer, completed-stream, and external-selector readings have no carrier coordinate and cannot be exported.
\end{proof}

\falsifiablePrediction{Every accepted $\MaterialityOrbitEscapeUp$ read that reports material or freedom-like appearance exposes \texttt{observer\_hist\_identity}, \texttt{inter\_hist\_locality}, \texttt{localclockbudget}, \texttt{layeredrelationcertificate}, \texttt{finitewitnessroute}, feedback-prefix, rule-family, orbit-closure or certified-escape, transport, replay, provenance, and local naming rows.}

\independenceWitness{\texttt{observer\_hist\_identity}, \texttt{inter\_hist\_locality}, \texttt{localclockbudget}, \texttt{layeredrelationcertificate}, \texttt{finitewitnessroute}: no listed sibling is bijective because this carrier stores observer identity, finite feedback prefix, rule-family closure, certified escape, and appearance classification in one NameCert packet, while each sibling supplies only one route or locality surface.}

\closureat{MaterialityOrbitEscapeUp}{seedStr}

\begin{closurestatus}{\MaterialityOrbitEscapeUp}
  \constructivestory{A $\MaterialityOrbitEscapeUp$ packet is generated from an observer-history identity row, a finite feedback prefix, identity-closure witnesses, ledger-certified orbit rules, orbit-closure rows, certified-escape rows, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:materiality-orbit-escape-carrier}, \autoref{thm:materiality-orbit-escape-namecert-obligations}, the falsifiable prediction, and the independence witness seed the finite orbit-closure and certified-escape route grounded in \autoref{subsec:philosophy-materiality-orbit-escape}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim context-free materiality, primitive agency, absolute free will, a global observer, completed feedback streams, external rule selection, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, observer-history identity transport, feedback-prefix replay, rule-family admission, orbit-closure readback, certified-escape readback, and appearance classifier non-escape for BEDC.Derived.MaterialityOrbitEscapeUp.}
\end{closurestatus}
