\chapter{A Concrete Naming Certificate for $\FiniteModulusLowerBoundLedgerUp$}
\label{ch:concrete-instances-finite-modulus-lower-bound-ledger-namecert}
\origin{human}

Following the compact-uniform-continuity construction of \autoref{ch:visions-compact-uniform-continuity}, the $\FiniteModulusLowerBoundLedgerUp$ packet records the finite $\mathsf{ProbeBundle}$ fold that turns bundled pointwise radii into one tolerance-only lower-bound history. It sits over the compact source data of \autoref{ch:concrete-instances-compact-namecert}, the metric distance rows of \autoref{ch:concrete-instances-metric-namecert}, the continuous pointwise modulus surface of \autoref{ch:concrete-instances-continuous-namecert}, the rational order readback of \autoref{ch:concrete-instances-rat-namecert}, and the subordinate cover packet of \autoref{ch:concrete-instances-subordinatemoduluscover-namecert}. The packet names only the finite lower-bound fold and its handoff to uniform-modulus consumers; it is not an infinite cover, a selected Lebesgue number, countable choice, a host compactness theorem, or an ambient real-completeness principle.

\begin{definition}[Finite modulus lower-bound ledger carrier]
\label{def:finite-modulus-lower-bound-ledger-carrier}
A $\FiniteModulusLowerBoundLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(K,M,F,Q,B,A,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row exposing the finite compact cover windows that supply the bundled probes.
\item $M$ is the $\MetricUp$ distance row used for source-ball and target-triangle comparisons in the compact-uniform route.
\item $F$ is the $\ContinuousUp$ pointwise-modulus row, assigning each bundled center its local positive rational precision history.
\item $Q$ is the $\RatUp$ order-readback row for positive rational histories and finite lower-bound comparisons.
\item $B$ is the nonempty finite $\mathsf{ProbeBundle}$ row whose bundled entries carry centers, half-radius cover rows, local modulus rows, and subordinate radius histories.
\item $A$ is the accumulator ledger produced by primitive recursion over $B$; each successor step compares the current accumulator with the next displayed half-radius history using $Q$.
\item $U$ is the uniform-modulus handoff row, recording the tolerance-only precision history certified below every bundled half-radius ledger.
\item $H$ records componentwise $\hsame$ transport for compact source, metric, continuous modulus, rational order, probe bundle, accumulator, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite fold
$$
K,M,F,Q,B,A \longmapsto U,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\FiniteModulusLowerBoundLedgerUp$, $\CompactUp$, $\MetricUp$, $\ContinuousUp$, $\RatUp$, $\SubordinateModulusCoverUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteModulusLowerBoundLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,F,Q,B,A,U,H,C,P,N$. It has no coordinate for an infinite cover, a host minimum operator, selected Lebesgue number, countable choice, quotient real equality, or ambient completeness.
\end{definition}

\begin{theorem}[Finite modulus lower-bound ledger fold obligation]
\label{thm:finite-modulus-lower-bound-ledger-fold-obligation}
For every accepted $\FiniteModulusLowerBoundLedgerUp$ carrier
$$
L=(K,M,F,Q,B,A,U,H,C,P,N),
$$
the local $\NameCert_{\FiniteModulusLowerBoundLedgerUp}$ surface consists exactly of compact-source exposure, metric-distance exposure, continuous pointwise-modulus exposure, rational order readback, nonempty finite $\mathsf{ProbeBundle}$ exposure, accumulator-step exactness, tolerance-only uniform-modulus handoff, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-modulus-lower-bound-ledger-carrier}. The compact, metric, continuous, rational, bundle, accumulator, handoff, and structural rows are exactly $K,M,F,Q,B,A,U,H,C,P,N$.

Read the source rows first. The compact row $K$ and subordinate cover data inside $B$ expose finitely many bundled centers and half-radius histories, while $F$ exposes the pointwise continuous-modulus rows attached to those centers.

Run the fold through $Q$ and $A$. At the initial and successor stages, the accumulator is compared with the displayed rational radius history for the next bundled probe, and the resulting lower-bound row is stored as finite BEDC history rather than as an external minimum.

The handoff row $U$ is downstream of that fold and records only the tolerance-only precision certified below every bundled half-radius ledger. Transport, replay, provenance, and the local name preserve the same route through $H,C,P,N$, so no excluded compactness, choice, quotient, or completeness principle is exported.
\end{proof}

\closureat{FiniteModulusLowerBoundLedgerUp}{seedStr}

\begin{closurestatus}{\FiniteModulusLowerBoundLedgerUp}
  \constructivestory{A $\FiniteModulusLowerBoundLedgerUp$ packet is generated from compact source rows, metric-distance rows, continuous pointwise-modulus rows, Rat order-readback rows, a nonempty finite ProbeBundle, a primitive-recursive accumulator ledger, a UniformModulus handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-modulus-lower-bound-ledger-carrier} and \autoref{thm:finite-modulus-lower-bound-ledger-fold-obligation} seed the finite rational lower-bound fold used by compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim an infinite cover, host minimum operator, selected Lebesgue number, countable choice, quotient real equality, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, metric-distance exposure, continuous-modulus exposure, Rat order readback, finite ProbeBundle recursion, accumulator exactness, uniform-modulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.FiniteModulusLowerBoundLedgerUp.}
\end{closurestatus}
