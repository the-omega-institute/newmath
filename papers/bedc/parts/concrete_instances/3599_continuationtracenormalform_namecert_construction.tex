\chapter{A Concrete Naming Certificate for $\ContinuationTraceNormalFormUp$}
\label{ch:concrete-instances-continuationtracenormalform-namecert}
\origin{human}

The $\ContinuationTraceNormalFormUp$ packet joins the finite small-step ledger of $\ContinuationTraceUp$ with the terminal-read surface of $\ContinuationBigStepUp$ and the typed normal-form boundary of $\TypedReductionNormalFormUp$. The terminal-read coordinate is consumed from \autoref{ch:concrete-instances-continuationbigstep-namecert} before the typed normal-form row is read. It records trace replay, terminal stability, typed normal-form readback, transports, routes, provenance, and local naming in one finite $\BHist$ carrier. It does not appeal to a host evaluator, a host conversion check, quotient behaviour, or a normalization oracle outside the displayed BEDC rows.

\begin{definition}[ContinuationTraceNormalForm carrier]
\label{def:continuation-trace-normal-form-carrier}
A $\ContinuationTraceNormalFormUp$ carrier is a finite $\BHist$ packet
$$
Z=(S,T,R,Q,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source history row from which replay begins;
\item $T$ is the finite $\ContinuationTraceUp$ small-step ledger, including its displayed source, mark-and-tail, target, and trace rows;
\item $R$ is the terminal history row reached by replaying $T$ through the displayed $\Cont$ routes;
\item $Q$ is the terminal-read witness exposed by the $\ContinuationBigStepUp$ surface;
\item $V$ is the $\TypedReductionNormalFormUp$ row naming the normal-form classifier for the same terminal history;
\item $H$ stores componentwise $\hsame$ transports for $S,T,R,Q,V$, provenance, and the local name;
\item $C$ stores the displayed $\Cont$ routes that replay the trace ledger, reach the terminal row, consume the terminal-read witness, and expose the typed normal-form row;
\item $P$ is the $\Pkg$ provenance over $\BHist$, $\BMark$, $\ContinuationTraceUp$, $\ContinuationBigStepUp$, $\TypedReductionNormalFormUp$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\ContinuationTraceNormalFormUp}$ row.
\end{enumerate}
The carrier is exactly this finite packet. It contains no host conversion relation, no quotient-normalization proof, no evaluator graph, no global confluence theorem, and no external normalizer.
\end{definition}

\begin{theorem}[ContinuationTraceNormalForm NameCert obligations]
\label{thm:continuation-trace-normal-form-namecert-obligations}
For the carrier of \autoref{def:continuation-trace-normal-form-carrier}, the local $\NameCert_{\ContinuationTraceNormalFormUp}$ surface consists exactly of carrier admission for
$$
Z=(S,T,R,Q,V,H,C,P,N),
$$
trace replay through the $\ContinuationTraceUp$ ledger, terminal-read exposure through the $\ContinuationBigStepUp$ row, typed normal-form readback through $V$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and non-escape from host conversion, quotient behaviour, evaluator graphs, global confluence, and normalization oracles.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:continuation-trace-normal-form-carrier}. The packet exposes exactly $S,T,R,Q,V,H,C,P,N$.

Trace replay is read from $T$ and $C$. The $\ContinuationTraceUp$ row supplies finite small-step entries, and the displayed continuation route replays those entries from the source row $S$ to the terminal row $R$.

Terminal exposure is then read through $Q$, and typed normal-form readback is read through $V$. The transport row $H$ moves the same displayed rows componentwise, while $P$ and $N$ record package provenance and local naming.

The non-escape clauses follow from the carrier inventory. Since the packet has no coordinate for host conversion, quotient behaviour, an evaluator graph, global confluence, or an external normalizer, no local certificate projection can export one.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm non-escape]
\label{thm:continuation-trace-normal-form-non-escape}
Every accepted $\ContinuationTraceNormalFormUp$ packet exposes terminal trace normalization only through the displayed rows
$$
S,T,R,Q,V,H,C,P,N.
$$
A downstream consumer may read the source history, finite continuation trace, terminal row, terminal-read witness, typed normal-form row, componentwise transports, displayed continuation replay, package provenance, and local naming row. It cannot replace those rows by host conversion, quotient behaviour, a selected evaluator, a global confluence theorem, or a normalization oracle.
\end{theorem}
\begin{proof}
Begin with the carrier projection. The only replay data are the finite trace row $T$ and the displayed continuation routes in $C$.

Use \autoref{thm:continuation-trace-normal-form-namecert-obligations}. The terminal row $R$, terminal-read witness $Q$, and normal-form row $V$ are reached only after the replayed trace has been consumed inside the same packet.

Finally inspect the excluded objects. Each would require a separate carrier coordinate or a second consumer route outside $S,T,R,Q,V,H,C,P,N$. Since the accepted packet has neither, the terminal normal-form surface is exactly the displayed finite NameCert packet.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm terminal scope]
\label{thm:continuation-trace-normal-form-terminal-scope}
Every accepted $\ContinuationTraceNormalFormUp$ packet exposes the terminal row only after consuming the $\ContinuationTraceUp$ ledger, the $\ContinuationBigStepUp$ terminal-read row, and the $\TypedReductionNormalFormUp$ row through the displayed $\Cont$ routes. No consumer can replace that sequence by host conversion, quotient normalization, evaluator graphs, or normalization oracles.
\end{theorem}
\begin{proof}
Project the accepted carrier $Z=(S,T,R,Q,V,H,C,P,N)$. The terminal row $R$ is a coordinate of the same finite packet as the trace ledger $T$, terminal-read row $Q$, typed normal-form row $V$, and displayed continuation routes $C$.

Apply \autoref{thm:continuation-trace-normal-form-namecert-obligations}. Trace replay is first consumed through $T$ and $C$, terminal-read exposure is then consumed through $Q$, and typed normal-form readback is consumed through $V$ before the local name $N$ is reached.

Apply \autoref{thm:continuation-trace-normal-form-non-escape}. Host conversion, quotient normalization, evaluator graphs, and normalization oracles have no coordinate in $Z$, so none can serve as an alternate terminal-read route.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm trace ledger exhaustion]
\label{thm:continuation-trace-normal-form-trace-ledger-exhaustion}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the finite row $T$ exhausts all trace reads exported by the packet. A consumer may replay only the displayed $\ContinuationTraceUp$ ledger through $C$ and may transport it only through $H$; it cannot read an additional trace from a host evaluator, quotient normalization, global confluence, or an external normalizer.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:continuation-trace-normal-form-carrier}. The only trace coordinate is $T$, and the only replay coordinate is the displayed continuation route row $C$.

Apply \autoref{thm:continuation-trace-normal-form-namecert-obligations}. Trace replay is certified through the $\ContinuationTraceUp$ ledger, componentwise transport is certified through $H$, and the package and local name are certified through $P,N$ for the same finite tuple.

Use \autoref{thm:continuation-trace-normal-form-non-escape}. Since host conversion, quotient behaviour, evaluator graphs, global confluence, and normalization oracles have no coordinate in $Z$, none can provide a second trace read outside $T$.

Finally apply \autoref{thm:continuation-trace-normal-form-terminal-scope}. Terminal exposure consumes the displayed trace before reaching $R,Q,V$, so every exported trace read is exhausted by $T$.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm terminal readback lock]
\label{thm:continuation-trace-normal-form-terminal-readback-lock}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the terminal row $R$ is locked behind the finite trace row $T$, terminal-read row $Q$, transport row $H$, displayed continuation row $C$, provenance row $P$, and local naming row $N$. No public read may expose $R$ without first consuming those displayed rows.
\end{theorem}
\begin{proof}
Start with the carrier projection of \autoref{def:continuation-trace-normal-form-carrier}. The row $R$ is not an isolated terminal object; it is part of the finite packet $S,T,R,Q,V,H,C,P,N$.

Apply \autoref{thm:continuation-trace-normal-form-namecert-obligations}. The terminal-read exposure through $Q$ is certified only after trace replay through $T$ and $C$, componentwise transport through $H$, and package/name recording through $P,N$.

Use \autoref{thm:continuation-trace-normal-form-terminal-scope}. It places the terminal row behind the $\ContinuationTraceUp$ ledger, the $\ContinuationBigStepUp$ terminal-read witness, and the $\TypedReductionNormalFormUp$ row.

Finally use \autoref{thm:continuation-trace-normal-form-non-escape}. A host conversion relation, quotient normalizer, evaluator graph, global confluence theorem, or external normalizer cannot unlock a terminal readback because none is a row of the accepted packet.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm typed boundary nonescape]
\label{thm:continuation-trace-normal-form-typed-boundary-nonescape}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the typed normal-form row $V$ cannot be replaced by host conversion, quotient normalization, evaluator graphs, or global confluence. The typed boundary is read only through the displayed trace, terminal, transport, route, package, and local naming rows of $Z$.
\end{theorem}
\begin{proof}
Read $V$ from \autoref{def:continuation-trace-normal-form-carrier}. It is the finite $\TypedReductionNormalFormUp$ row of the same packet as $S,T,R,Q,H,C,P,N$.

Apply \autoref{thm:continuation-trace-normal-form-namecert-obligations}. Typed normal-form readback is certified through $V$ after trace replay, terminal-read exposure, componentwise transport, displayed continuation replay, provenance, and local naming have been fixed.

Use \autoref{thm:continuation-trace-normal-form-terminal-scope}. The typed normal-form row is reached through the terminal route rather than through a host conversion check or evaluator graph.

Finally apply \autoref{thm:continuation-trace-normal-form-non-escape}. The packet has no coordinate for quotient normalization, global confluence, evaluator graphs, or external normalizers, so none can replace $V$ as the typed boundary.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm terminal route determinacy]
\label{thm:continuation-trace-normal-form-terminal-route-determinacy}
Let $Z=(S,T,R,Q,V,H,C,P,N)$ and $Z'=(S',T',R',Q',V',H',C',P',N')$ be accepted $\ContinuationTraceNormalFormUp$ packets. If two terminal reads have the same trace row, typed boundary row, componentwise $\hsame$ transport, displayed $\Cont$ route, $\Pkg$ provenance, and local $\NameCert$ row, then they determine the same normal-form consumer surface. No off-route evaluator state, host conversion relation, quotient-normalization datum, global confluence theorem, or external normalization oracle can distinguish the two reads.
\end{theorem}
\begin{proof}
First apply terminal scope from \autoref{thm:continuation-trace-normal-form-terminal-scope}. Each terminal read consumes the $\ContinuationTraceUp$ ledger, the $\ContinuationBigStepUp$ terminal-read row, and the $\TypedReductionNormalFormUp$ row through the displayed route before reaching a consumer surface.

Next use trace ledger exhaustion from \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion}. Since the trace rows agree and every trace read is exhausted by that finite ledger, neither packet can supply an additional trace from a host evaluator or quotient normalizer.

Apply the terminal readback lock of \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}. The common transport, route, provenance, and naming rows place the terminal row behind the same $T,Q,H,C,P,N$ surface in both packets.

Finally use typed boundary nonescape from \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}. The common typed row $V$ is the only normal-form boundary available to the consumer, and the excluded off-route evaluator state, host conversion relation, quotient-normalization datum, global confluence theorem, and external normalization oracle have no coordinate in either accepted packet.
\end{proof}

This terminal-scope row is the paper obligation needed before Lean can formalize the continuation trace normal-form handoff.

\begin{theorem}[ContinuationTraceNormalForm standard bridge surface]
\label{thm:continuation-trace-normal-form-standard-bridge-surface}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the public terminal trace-normal-form packet bridges exactly from $\ContinuationTraceUp$, $\ContinuationBigStepUp$, and $\TypedReductionNormalFormUp$ through the displayed source, trace, terminal, terminal-read, typed-boundary, transport, route, provenance, and naming rows $S,T,R,Q,V,H,C,P,N$.

The bridge surface reads no host conversion relation, quotient normalization datum, evaluator graph, global confluence theorem, or external normalizer. Its entire public object is the finite $\BHist$ route whose trace replay, terminal readback, typed boundary, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and $\NameCert$ row are already displayed in the carrier.
\end{theorem}
\begin{proof}
First apply \autoref{thm:continuation-trace-normal-form-namecert-obligations}. The local certificate surface is exactly the carrier admission, trace replay, terminal-read exposure, typed normal-form readback, componentwise transport, displayed continuation route, package provenance, and local naming row.

Next use \autoref{thm:continuation-trace-normal-form-terminal-scope}. It orders the public route through $\ContinuationTraceUp$, then $\ContinuationBigStepUp$, then $\TypedReductionNormalFormUp$, all within the same displayed packet.

Apply \autoref{thm:continuation-trace-normal-form-terminal-route-determinacy}. A consumer that reads the same trace row, typed boundary row, transport row, route row, provenance row, and local naming row obtains the same normal-form consumer surface.

Finally use \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}. The excluded host conversion, quotient normalization, evaluator graph, global confluence, and external normalizer have no coordinate in $Z$, so the standard bridge surface is precisely the displayed finite route.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm replay-prefix determinacy]
\label{thm:continuation-trace-normal-form-replay-prefix-determinacy}
Let $Z=(S,T,R,Q,V,H,C,P,N)$ and $Z'=(S',T',R',Q',V',H',C',P',N')$ be accepted $\ContinuationTraceNormalFormUp$ packets. Agreement on the source row, the $\ContinuationTraceUp$ ledger, and the displayed replay continuation determines the same terminal replay prefix before any terminal-read row or typed-boundary row is exposed.

Thus no host conversion relation, quotient normalization datum, evaluator graph, global confluence theorem, or external normalizer can split two replay prefixes whose visible source, trace, and continuation rows agree.
\end{theorem}
\begin{proof}
Project both packets through \autoref{def:continuation-trace-normal-form-carrier}. The only replay-prefix coordinates before terminal exposure are the source row $S$, the trace ledger $T$, and the displayed continuation route $C$.

Apply \autoref{thm:continuation-trace-normal-form-terminal-scope}. It places the terminal-read and typed-boundary rows strictly after the trace-ledger replay, so the prefix is determined before $Q$ or $V$ can be read.

Use \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion}. Since the trace ledger is exhausted by the displayed $T$ row and replayed through $C$, an agreeing source, ledger, and replay route leave no second prefix source.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm terminal typed-boundary exhaustion]
\label{thm:continuation-trace-normal-form-terminal-typed-boundary-exhaustion}
Every terminal typed-boundary consumer of an accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$ factors through the source row, trace ledger, terminal row, terminal-read row, typed normal-form boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row $S,T,R,Q,V,H,C,P,N$.

The consumer cannot read host conversion, quotient normalization, evaluator graphs, global confluence, or an external normalizer.
\end{theorem}
\begin{proof}
First apply \autoref{thm:continuation-trace-normal-form-terminal-scope}. It orders the terminal typed-boundary read through $S,T,R,Q,V,H,C,P,N$.

Next use \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}. The terminal row is locked behind the finite trace ledger, terminal-read row, transport, continuation, package, and naming rows.

Finally combine \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape} with \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}. The typed boundary and bridge surface expose exactly the displayed finite route, while the excluded host conversion, quotient normalization, evaluator graph, global confluence, and external normalizer have no carrier coordinate.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm root unblock package]
\label{thm:continuation-trace-normal-form-root-unblock-package}
The carrier of \autoref{def:continuation-trace-normal-form-carrier}, replay-prefix determinacy, terminal typed-boundary exhaustion, trace ledger exhaustion, terminal readback lock, and standard bridge surface form the root package consumed by downstream halting and normal-form chapters. The package consists only of the rows $S,T,R,Q,V,H,C,P,N$ and their displayed $\BHist$, $\BMark$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ structure.

It supplies no host conversion relation, quotient normalization datum, evaluator graph, global confluence theorem, external normalizer, or off-route terminal reader.
\end{theorem}
\begin{proof}
Start from the carrier definition and \autoref{thm:continuation-trace-normal-form-namecert-obligations}. They identify the displayed source, trace, terminal, terminal-read, typed-boundary, transport, route, package, and naming rows of the local packet.

Use \autoref{thm:continuation-trace-normal-form-replay-prefix-determinacy} for the prefix part of the package. The source, trace ledger, and replay continuation determine the terminal replay prefix before terminal-read exposure.

Use \autoref{thm:continuation-trace-normal-form-terminal-typed-boundary-exhaustion}, \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion}, and \autoref{thm:continuation-trace-normal-form-terminal-readback-lock} for the terminal part. These clauses exhaust the trace ledger, lock terminal readback, and force typed-boundary consumers through the displayed route.

Finally apply \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}. It packages the same finite surface for downstream halting and normal-form chapters, with no coordinate for the excluded host, quotient, evaluator, confluence, normalizer, or off-route terminal-reader objects.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm source-endpoint partition]
\label{thm:continuation-trace-normal-form-source-endpoint-partition}
Every accepted $\ContinuationTraceNormalFormUp$ packet partitions its public rows into source trace, terminal-read, typed-normal boundary, transport, route, provenance, and local $\NameCert$ data before any consumer reads the endpoint. For $Z=(S,T,R,Q,V,H,C,P,N)$, the source trace is $S,T$, the terminal-read surface is $R,Q$, the typed-normal boundary is $V$, the transport surface is $H$, the route surface is $C$, the provenance row is $P$, and the local naming row is $N$.
\end{theorem}
\begin{proof}
Start from the carrier definition and \autoref{thm:continuation-trace-normal-form-namecert-obligations}. They expose exactly the rows $S,T,R,Q,V,H,C,P,N$ and bind each row to carrier admission, trace replay, terminal-read exposure, typed normal-form readback, transport, route, provenance, and local naming.

Use \autoref{thm:continuation-trace-normal-form-terminal-scope}. It orders endpoint access after the trace ledger, terminal-read row, and typed normal-form row have been consumed through the displayed route.

Finally apply \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion} and \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}. The trace and typed-boundary reads cannot be replaced by host conversion, quotient normalization, evaluator graphs, global confluence, or external normalizers, so the displayed partition is the entire endpoint surface.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm terminal-result determinacy]
\label{thm:continuation-trace-normal-form-terminal-result-determinacy}
Two public terminal reads through the same displayed $T,Q,H,C,P,N$ rows of accepted $\ContinuationTraceNormalFormUp$ packets have the same BEDC result row under $\hsame$ transport and $\Cont$ replay. No host evaluator, quotient normalizer, global confluence proof, or off-route terminal reader can separate the two reads.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}. It locks each terminal row behind the trace ledger, terminal-read row, transport, route, provenance, and local naming rows.

Apply \autoref{thm:continuation-trace-normal-form-terminal-route-determinacy}. Agreement on the displayed trace, typed boundary, componentwise transport, route, provenance, and local naming rows determines the same normal-form consumer surface.

Use \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion}. Since trace reads are exhausted by the displayed ledger and replayed only through $C$, no second terminal result can be read through an external trace source.

Finally apply \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}. The excluded host conversion, quotient normalization, evaluator graph, global confluence, and external normalizer have no row from which to distinguish the BEDC result.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm typed-boundary ledger nonescape]
\label{thm:continuation-trace-normal-form-typed-boundary-ledger-nonescape}
Every typed-normal boundary read in an accepted $\ContinuationTraceNormalFormUp$ packet factors through the displayed source trace, terminal-read, typed boundary, transport, route, provenance, and local naming ledger $S,T,R,Q,V,H,C,P,N$. The boundary cannot import host conversion, quotient normalization, global confluence, evaluator graphs, or an external normalizer.
\end{theorem}
\begin{proof}
Project the packet through \autoref{def:continuation-trace-normal-form-carrier}. The typed-normal boundary is the row $V$ in the same finite carrier as the source, trace, terminal, terminal-read, transport, route, provenance, and local name.

Apply \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}. It reads $V$ only through the displayed trace, terminal, transport, route, package, and local naming rows.

Combine \autoref{thm:continuation-trace-normal-form-namecert-obligations}, \autoref{thm:continuation-trace-normal-form-terminal-scope}, and \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion}. These rows certify the carrier surface, order terminal access, and exhaust trace reads before the typed boundary reaches a consumer.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm route ledger totality]
\label{thm:continuation-trace-normal-form-route-ledger-totality}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the route ledger is total over the displayed source, trace, terminal, terminal-read, typed-boundary, transport, route, provenance, and naming rows. Every public normal-form consumer must read $S$, then the $\ContinuationTraceUp$ ledger $T$, then the terminal row $R$, then the $\ContinuationBigStepUp$ terminal-read row $Q$, then the $\TypedReductionNormalFormUp$ row $V$, with componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local $\NameCert_{\ContinuationTraceNormalFormUp}$ naming through $N$.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:continuation-trace-normal-form-terminal-scope}. It orders terminal exposure through the finite trace ledger, terminal-read row, and typed normal-form row inside the displayed packet.

Next use \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion}. The trace coordinate is exhausted by $T$ and replayed only through $C$, so no public consumer can supply a second trace ledger.

Apply \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}. The terminal row $R$ is locked behind $T,Q,H,C,P,N$, and therefore the route ledger reaches the terminal read only through the displayed rows.

Finally use \autoref{thm:continuation-trace-normal-form-terminal-route-determinacy} and \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}. Determinacy prevents off-route data from separating two reads with the same ledger rows, while the bridge surface exports exactly the public continuation trace normal-form route.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm normal boundary exhaustion]
\label{thm:continuation-trace-normal-form-normal-boundary-exhaustion}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the typed normal-form boundary is exhausted by the $\ContinuationTraceUp$, $\ContinuationBigStepUp$, and $\TypedReductionNormalFormUp$ rows of the same packet. The boundary exports the finite route $S,T,R,Q,V,H,C,P,N$ and does not export host conversion, quotient normalization, evaluator correctness, global confluence, or an external normalizer.
\end{theorem}
\begin{proof}
First apply \autoref{thm:continuation-trace-normal-form-route-ledger-totality}. It makes the public route ledger total over $S,T,R,Q,V,H,C,P,N$.

Use \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}. The typed boundary is read through the displayed trace, terminal, transport, route, package, and local naming rows, not through host conversion or quotient normalization.

Next combine \autoref{thm:continuation-trace-normal-form-terminal-readback-lock} with \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}. The terminal-read row is locked behind the finite ledger, and the public bridge exposes exactly the continuation trace, big-step terminal read, and typed normal-form rows.

The remaining exclusions follow from \autoref{thm:continuation-trace-normal-form-non-escape}. Evaluator correctness, global confluence, quotient behaviour, host conversion, and an external normalizer have no coordinate in the accepted packet.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm source route obligation package]
\label{thm:continuation-trace-normal-form-source-route-obligation-package}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the source, terminal, transport, route, provenance, and $\NameCert$ obligations are packaged by the finite route ledger. The source obligation is the displayed source row $S$ and trace ledger $T$; the terminal obligation is the displayed terminal row $R$ and terminal-read row $Q$; the transport obligation is $H$; the route obligation is $C$; the provenance obligation is $P$; and the naming obligation is $N$. These obligations are consumed before the typed normal-form boundary $V$ is available to a public consumer.
\end{theorem}
\begin{proof}
Start with \autoref{thm:continuation-trace-normal-form-namecert-obligations}. It identifies carrier admission, trace replay, terminal-read exposure, typed normal-form readback, transport, route, provenance, and local naming as the local certificate surface.

Apply \autoref{thm:continuation-trace-normal-form-route-ledger-totality}. The route ledger is total over $S,T,R,Q,V,H,C,P,N$, so each obligation is attached to a displayed coordinate of the accepted packet.

Use \autoref{thm:continuation-trace-normal-form-normal-boundary-exhaustion}. The typed normal-form boundary is exhausted by the continuation trace, big-step terminal read, and typed normal-form rows of the same finite route.

Finally combine \autoref{thm:continuation-trace-normal-form-terminal-route-determinacy} with \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}. The same source, terminal, transport, route, provenance, and naming rows determine the public normal-form consumer surface, so the obligation package has no hidden host conversion, quotient-normalization, evaluator, or oracle component.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm visible source route]
\label{thm:continuation-trace-normal-form-visible-source-route}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the public source route exposes the source history row $S$, the $\ContinuationTraceUp$ ledger $T$, the terminal row $R$, the $\ContinuationBigStepUp$ terminal-read row $Q$, the $\TypedReductionNormalFormUp$ boundary row $V$, the componentwise $\hsame$ transports $H$, the displayed $\Cont$ routes $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\ContinuationTraceNormalFormUp}$ row $N$ before any terminal consumer reads the normal-form endpoint.

No terminal consumer can read the endpoint by skipping the visible source route, adding an evaluator coordinate, importing host conversion, quotient trace equality, global confluence, or external normalization.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:continuation-trace-normal-form-carrier}. Its visible rows are exactly $S,T,R,Q,V,H,C,P,N$.

Apply \autoref{thm:continuation-trace-normal-form-non-escape} and \autoref{thm:continuation-trace-normal-form-terminal-scope}. They place terminal exposure behind the displayed trace ledger, terminal-read row, typed normal-form row, transports, routes, provenance, and local name.

Use \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion} and \autoref{thm:continuation-trace-normal-form-terminal-route-determinacy}. The finite trace ledger exhausts exported trace reads, and the displayed route determines the same public normal-form consumer surface whenever its route rows agree.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm typed-reduction handoff]
\label{thm:continuation-trace-normal-form-typed-reduction-handoff}
Every typed-reduction or normal-form consumer of an accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$ can read the terminal row only through $T,R,Q,V,H,C,P,N$ after the source row $S$ has been displayed. The handoff cannot bypass the $\ContinuationTraceUp$ ledger, cannot import host normalization, and cannot replace the $\TypedReductionNormalFormUp$ boundary by quotient normalization, evaluator correctness, or global confluence.
\end{theorem}
\begin{proof}
Begin with the terminal readback lock of \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}. It places $R$ behind the trace ledger, terminal-read row, transports, routes, provenance, and local name.

Apply \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}. The typed normal-form boundary is read through the displayed trace, terminal, transport, route, package, and naming rows rather than through host conversion or evaluator data.

Finally use \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}. The bridge exports exactly the continuation trace, terminal-read, typed-boundary, transport, route, provenance, and naming surface, so the typed-reduction handoff has no hidden host normalizer or off-route terminal reader.
\end{proof}

\begin{theorem}[ContinuationTraceNormalForm consumer readback envelope]
\label{thm:continuation-trace-normal-form-consumer-readback-envelope}
For every accepted $\ContinuationTraceNormalFormUp$ packet $Z=(S,T,R,Q,V,H,C,P,N)$, the downstream consumer envelope consists exactly of the visible source route of \autoref{thm:continuation-trace-normal-form-visible-source-route}, the typed-reduction handoff of \autoref{thm:continuation-trace-normal-form-typed-reduction-handoff}, the terminal readback lock of \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}, and the standard bridge surface of \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}.

The envelope exports no hidden evaluator, quotient trace equality, external normalizer, host conversion route, global confluence theorem, or unbounded fuel row beyond the displayed finite packet $S,T,R,Q,V,H,C,P,N$.
\end{theorem}
\begin{proof}
First combine \autoref{thm:continuation-trace-normal-form-visible-source-route} with \autoref{thm:continuation-trace-normal-form-typed-reduction-handoff}. The source route displays every row consumed by the typed-reduction handoff before a terminal consumer reads the normal-form endpoint.

Next apply \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}. It makes the terminal row available only behind the finite trace, terminal-read, transport, route, provenance, and local naming rows.

Finally apply \autoref{thm:continuation-trace-normal-form-standard-bridge-surface} and \autoref{thm:continuation-trace-normal-form-non-escape}. The standard bridge surface is the public consumer object, and the non-escape clause rules out hidden evaluator state, quotient trace equality, external normalization, host conversion, global confluence, and unbounded fuel.
\end{proof}

\closureat{ContinuationTraceNormalFormUp}{seedStr}
\closureat{ContinuationTraceNormalFormUp}{obligationStr}
\closureat{ContinuationTraceNormalFormUp}{scopedStr}
\closureat{ContinuationTraceNormalFormUp}{publicStr}
\closureat{ContinuationTraceNormalFormUp}{bridgedStr}
\closureat{ContinuationTraceNormalFormUp}{matureStr}

\begin{closurestatus}{\ContinuationTraceNormalFormUp}
  \constructivestory{A $\ContinuationTraceNormalFormUp$ packet is generated from a source history row, a finite $\ContinuationTraceUp$ ledger, a terminal row, a $\ContinuationBigStepUp$ terminal-read witness, a $\TypedReductionNormalFormUp$ row, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:continuation-trace-normal-form-carrier}, \autoref{thm:continuation-trace-normal-form-namecert-obligations}, \autoref{thm:continuation-trace-normal-form-non-escape}, \autoref{thm:continuation-trace-normal-form-terminal-scope}, \autoref{thm:continuation-trace-normal-form-trace-ledger-exhaustion}, \autoref{thm:continuation-trace-normal-form-terminal-readback-lock}, \autoref{thm:continuation-trace-normal-form-typed-boundary-nonescape}, \autoref{thm:continuation-trace-normal-form-standard-bridge-surface}, \autoref{thm:continuation-trace-normal-form-replay-prefix-determinacy}, \autoref{thm:continuation-trace-normal-form-terminal-typed-boundary-exhaustion}, and \autoref{thm:continuation-trace-normal-form-root-unblock-package} bind carrier admission, trace replay, terminal-read exposure, typed normal-form readback, non-escape, trace-ledger exhaustion, terminal readback lock, typed-boundary nonescape, the standard bridge surface, replay-prefix determinacy, terminal typed-boundary exhaustion, and the root package to the displayed $\BHist$, $\BMark$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, typed subject reduction, global confluence, host conversion, evaluator correctness, quotient behaviour, or machine-checked bridge verification.}
  \upgradepath{No further paper-side theory closure remains; Lean verification remains a separate formal-axis obligation.}
\end{closurestatus}
