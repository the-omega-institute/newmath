\chapter{A Concrete Naming Certificate for $\DyadicCompletionUp$}
\label{ch:concrete-instances-dyadiccompletion}
\origin{human}

The $\DyadicCompletionUp$ surface is the constructive completion handoff between
finite dyadic observations and the regular real-name layer. Following the
Bishop--Bridges constructive-analysis treatment of Cauchy completion for
dyadics, the chapter records only scheduled finite observations, explicit
regular-tail witnesses, and visible precision ledgers. It does not identify
streams by quotient classes, select limits by choice, or replace the stream
surface by Dedekind cuts.

\section{Carrier Surface}
\label{sec:dyadiccompletion-carrier-surface}

\begin{definition}[DyadicCompletion finite-window carrier]
\label{def:dyadiccompletion-carrier}
A $\DyadicCompletionUp$ carrier row is a finite $\mathsf{BHist}$ packet
$(d,w,r,s,L,P)$ with the following visible coordinates:
\begin{enumerate}
\item dyadic observation rows $d$ accepted by $\DyadicRatCoreUp$;
\item certified stream windows $w$ requested through $\StreamNameUp$;
\item a regular-tail classifier row $r$ whose comparisons are read through
$\RegSeqRatUp$;
\item a real-name boundary row $s$ naming the data consumed by $\RealUp$;
\item a precision ledger $L$ recording every finite schedule, tail bound, and
window restriction used by the packet;
\item a $\Pkg$ provenance row $P$ naming the $\DyadicRatCoreUp$,
$\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ certificates.
\end{enumerate}
The carrier stores finite windows and their ledgers. It contains no completed
host real, no quotient class of Cauchy streams, no hidden precision schedule,
and no Dedekind-cut classifier.
\end{definition}
\leanchecked{BEDC.Derived.DyadicCompletionUp.TasteGate.DyadicCompletionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[DyadicCompletion NameCert obligation surface]
\label{thm:dyadiccompletion-namecert-obligation-surface}
The local $\NameCert_{\DyadicCompletionUp}$ obligation surface consists exactly
of the carrier of \autoref{def:dyadiccompletion-carrier}, the componentwise
classifier that compares dyadic observations and stream-window rows, the
regular-tail stability row read through $\RegSeqRatUp$, the $\RealUp$ handoff
row for accepted real-name consumers, and the precision ledger boundary that
records every schedule consumed by the packet.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionPacket\_namecert\_obligation\_surface}
\begin{proof}
Project the carrier coordinates. The dyadic, stream, regular-tail, real-handoff,
ledger, and provenance rows are all stored as finite $\mathsf{BHist}$ data.

The classifier is componentwise because a consumer can compare only the rows it
can read: dyadic observations through $\DyadicRatCoreUp$, scheduled windows
through $\StreamNameUp$, and tail comparisons through $\RegSeqRatUp$.

The $\RealUp$ handoff is therefore a boundary projection rather than a selected
limit. Repacking these projections gives the listed obligation surface and no
additional completion object.
\end{proof}

\begin{theorem}[DyadicCompletion constant dyadic embedding]
\label{thm:dyadiccompletion-constant-embedding}
Every accepted $\DyadicRatCoreUp$ row $q$ determines a $\DyadicCompletionUp$
carrier whose scheduled stream windows all read $q$, whose regular-tail
classifier rows are reflexive dyadic comparisons, whose $\RealUp$ boundary row
is the constant real-name handoff, and whose precision ledger records only the
requested windows and their constant readback routes.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionPacket\_constant\_embedding}
\begin{proof}
Use the constant $\StreamNameUp$ schedule with value row $q$ at each requested
finite window. Each endpoint read is already accepted by $\DyadicRatCoreUp$.

For the regular-tail coordinate, compare any two displayed windows by the
reflexive dyadic classifier on the same row $q$. The bound rows are the trivial
constant-tail witnesses consumed by $\RegSeqRatUp$.

Finally attach the $\RealUp$ handoff row that reads the same constant windows.
The ledger records the schedule requests and the constant routes, so no
quotient, limit selector, or hidden precision choice is introduced.
\end{proof}

\begin{theorem}[DyadicCompletion regular-tail stability]
\label{thm:dyadiccompletion-regular-tail-stability}
Let $C$ be an accepted $\DyadicCompletionUp$ carrier. If a second carrier
$C'$ is obtained by $\hsame$ transport of the dyadic observation rows, certified
stream windows, regular-tail comparisons, real-name boundary row, ledger, and
provenance, then $C'$ is accepted and has the same regular-tail classifier
surface over $\RegSeqRatUp$.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionWindowCarrier\_regular\_tail\_stability}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionWindowPacket\_regular\_tail\_stability}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionPacket\_regular\_tail\_stability}
\begin{proof}
Transport the dyadic observation rows and certified stream windows along the
displayed $\hsame$ comparisons. The $\StreamNameUp$ schedule is finite, so each
requested window is repacked with the transported row.

The regular-tail classifier is a finite family of dyadic bound ledgers. Transport
each bound row and each comparison route componentwise through $\RegSeqRatUp$.

The real-name boundary and precision ledger are projections of the same windows.
Transporting those projections yields the accepted carrier $C'$ and preserves
the regular-tail surface.
\end{proof}

\begin{theorem}[DyadicCompletion StreamName handoff]
\label{thm:dyadiccompletion-streamname-handoff}
Every accepted $\DyadicCompletionUp$ carrier exposes a $\StreamNameUp$ handoff:
for each finite precision request, the handoff returns exactly the scheduled
dyadic observation row, its window certificate, the regular-tail comparison rows
visible at that precision, and the ledger entries proving that the same rows
are available to $\RegSeqRatUp$ and $\RealUp$ consumers.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionWindowPacket\_streamname\_handoff}
\begin{proof}
Unpack the carrier. A precision request first selects a finite stream window,
and the carrier stores the dyadic observation row and certificate attached to
that window.

The regular-tail row records the comparisons visible at the same precision. The
ledger points to the shared schedule entry, so the $\RegSeqRatUp$ consumer and
the $\RealUp$ boundary read the same finite rows.

Thus the handoff is only a projection from stored data. It does not synthesize a
global stream outside the certified windows.
\end{proof}

\begin{theorem}[DyadicCompletion ledger boundary]
\label{thm:dyadiccompletion-ledger-boundary}
The precision ledger of an accepted $\DyadicCompletionUp$ carrier is the whole
boundary between dyadic finite observations and real-name consumption: every
visible $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ row
used by the carrier is named in the ledger, and no consumer can project an
unstored modulus, quotient representative, Dedekind cut, or ambient completed
real from the packet.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionPacket\_ledger\_boundary}
\begin{proof}
Inspect the ledger coordinate in \autoref{def:dyadiccompletion-carrier}. It
lists the finite schedules, tail bounds, window restrictions, and continuation
routes consumed by each displayed row.

Any downstream read by $\RegSeqRatUp$ or $\RealUp$ must pass through one of
those listed routes. Since the carrier has no coordinate for an unstored
modulus, quotient representative, Dedekind cut, or completed host real, such
data cannot be projected from the packet.
\end{proof}

\begin{theorem}[DyadicCompletion scoped dependency packet]
\label{thm:dyadiccompletion-scoped-dependency-packet}
The carrier of \autoref{def:dyadiccompletion-carrier}, the componentwise
classifier of
\autoref{thm:dyadiccompletion-namecert-obligation-surface}, the constant dyadic
embedding of \autoref{thm:dyadiccompletion-constant-embedding}, the
regular-tail stability of
\autoref{thm:dyadiccompletion-regular-tail-stability}, the $\StreamNameUp$
handoff of \autoref{thm:dyadiccompletion-streamname-handoff}, the $\RealUp$
handoff boundary named in the carrier, and the ledger boundary of
\autoref{thm:dyadiccompletion-ledger-boundary} depend only on finite
$\mathsf{BHist}$ rows, displayed $\hsame$ comparisons, visible $\Cont$ routes,
the $\mathsf{Pkg}$ provenance row, and the named $\DyadicRatCoreUp$,
$\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ source certificates.

Thus every accepted $\DyadicCompletionUp$ packet is scoped by the finite BEDC
rows and source certificates listed above.
\end{theorem}
\begin{proof}
Project the finite packet coordinates of
\autoref{def:dyadiccompletion-carrier}. They are dyadic observation rows,
certified stream windows, regular-tail rows, a real-name handoff row, ledger
entries, and one $\mathsf{Pkg}$ provenance row naming the source certificates.

The componentwise classifier is the classifier component of
\autoref{thm:dyadiccompletion-namecert-obligation-surface}. The constant
embedding row is \autoref{thm:dyadiccompletion-constant-embedding}, and the
regular-tail row is transported by
\autoref{thm:dyadiccompletion-regular-tail-stability}; each cited block reads
only finite $\mathsf{BHist}$ rows through $\hsame$, $\Cont$, and the displayed
package provenance.

The consumer handoff is \autoref{thm:dyadiccompletion-streamname-handoff}. Its
stream-name and real-name reads are the same scheduled rows already present in
the carrier, with the $\RegSeqRatUp$ and $\RealUp$ source certificates named by
the package row.

Finally apply \autoref{thm:dyadiccompletion-ledger-boundary}. The ledger names
every $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ row
available to a consumer, and the carrier has no coordinate for a quotient
stream, Dedekind cut, host real, or chosen limit.
\end{proof}

\begin{theorem}[DyadicCompletion public RegSeqRat Real export]
\label{thm:dyadiccompletion-public-regseqrat-real-export}
The carrier of \autoref{def:dyadiccompletion-carrier}, the $\NameCert_{\DyadicCompletionUp}$ obligation surface of \autoref{thm:dyadiccompletion-namecert-obligation-surface}, the constant dyadic embedding of \autoref{thm:dyadiccompletion-constant-embedding}, regular-tail stability of \autoref{thm:dyadiccompletion-regular-tail-stability}, $\StreamNameUp$ handoff of \autoref{thm:dyadiccompletion-streamname-handoff}, ledger boundary of \autoref{thm:dyadiccompletion-ledger-boundary}, and scoped dependency packet of \autoref{thm:dyadiccompletion-scoped-dependency-packet} assemble the public $\DyadicCompletionUp$ export consumed by $\RegSeqRatUp$ and $\RealUp$.

The export exposes only finite dyadic observations, certified stream windows, regular-tail rows, real-boundary rows, precision ledger rows, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and source certificates. It exports no quotient stream, Dedekind cut, host real, choice-selected limit, or completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionWindowPacket\_public\_regseqrat\_real\_export}
\begin{proof}
Project the finite packet from \autoref{def:dyadiccompletion-carrier}. Its coordinates are scheduled dyadic observations, certified stream windows, regular-tail rows, real-name handoff rows, ledger entries, and package provenance.

Compose the public export from the cited blocks. The obligation surface supplies the carrier and classifier rows, the constant embedding supplies dyadic constants, regular-tail stability transports tail rows, and the $\StreamNameUp$ handoff fixes the downstream stream-window reads.

The ledger boundary names the $\RegSeqRatUp$ and $\RealUp$ consumer-facing rows, while the scoped dependency package binds all reads to finite $\mathsf{BHist}$ rows, $\hsame$, $\Cont$, $\mathsf{Pkg}$, $\NameCert$, and the listed source certificates.

The excluded principles are therefore outside the export. A quotient stream, Dedekind cut, host real, choice-selected limit, or completeness principle would require a coordinate not present in the finite packet.
\end{proof}

\begin{theorem}[DyadicCompletion finite-window standard bridge]
\label{thm:dyadiccompletion-finite-window-standard-bridge}
The carrier of \autoref{def:dyadiccompletion-carrier}, the
$\NameCert_{\DyadicCompletionUp}$ obligation surface of
\autoref{thm:dyadiccompletion-namecert-obligation-surface}, the
$\StreamNameUp$ handoff of
\autoref{thm:dyadiccompletion-streamname-handoff}, regular-tail stability of
\autoref{thm:dyadiccompletion-regular-tail-stability}, the ledger boundary of
\autoref{thm:dyadiccompletion-ledger-boundary}, the scoped dependency packet of
\autoref{thm:dyadiccompletion-scoped-dependency-packet}, and the public
$\RegSeqRatUp/\RealUp$ export of
\autoref{thm:dyadiccompletion-public-regseqrat-real-export} determine the
standard constructive finite-window reading of $\DyadicCompletionUp$.

Every consumer read factors through finite dyadic observation rows,
stream-window rows, regular-tail rows, real-boundary rows, precision ledger
rows, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and the listed
$\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ source
certificates. The bridge exports no quotient Cauchy stream, Dedekind cut, host
real limit, choice-selected limit, or completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicCompletionUp.DyadicCompletionWindowPacket\_finite\_window\_standard\_bridge}
\begin{proof}
Project the carrier from \autoref{def:dyadiccompletion-carrier}. The projected
coordinates are the dyadic observation rows, stream-window rows, regular-tail
rows, real-name boundary row, precision ledger, and package provenance. These
are the only rows available to the $\NameCert_{\DyadicCompletionUp}$ obligation
surface.

Use \autoref{thm:dyadiccompletion-streamname-handoff} for window reads. The
handoff sends each downstream stream access to a scheduled stream-window row,
and \autoref{thm:dyadiccompletion-regular-tail-stability} transports the
regular-tail rows along the displayed classifier rather than through an
unstored tail representative.

Apply \autoref{thm:dyadiccompletion-ledger-boundary}. The ledger lists every
precision, real-boundary, continuation, history, package, and source-certificate
row readable by a consumer, so no unstored modulus, quotient representative,
Dedekind cut, host real limit, choice-selected limit, or completeness witness is
available.

Finally use \autoref{thm:dyadiccompletion-public-regseqrat-real-export} to route
the same finite rows to $\RegSeqRatUp$ and $\RealUp$ consumers. Since the scoped
dependency packet of \autoref{thm:dyadiccompletion-scoped-dependency-packet}
binds those reads to $\mathsf{BHist}$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and
the listed source certificates, the resulting consumer schema is exactly the
standard constructive finite-window reading.
\end{proof}

\begin{theorem}[DyadicCompletion mature RegSeqRat Real consumer coverage]
\label{thm:dyadiccompletion-mature-consumer-coverage}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of a $\DyadicCompletionUp$ packet
receives precisely the $\NameCert_{\DyadicCompletionUp}$ obligation surface,
constant dyadic embedding, regular-tail stability, $\StreamNameUp$ handoff,
ledger boundary, scoped dependency packet, public $\RegSeqRatUp/\RealUp$
export, and finite-window standard bridge of
\autoref{thm:dyadiccompletion-finite-window-standard-bridge}.

The coverage contains no quotient completion, Dedekind cut, trichotomy, host
equality, chosen limit, or extra completion principle.
\end{theorem}
\begin{proof}
Project the packet through
\autoref{thm:dyadiccompletion-namecert-obligation-surface} and
\autoref{thm:dyadiccompletion-constant-embedding}. This exposes the carrier,
classifier, and constant dyadic rows available to any consumer.

Next apply \autoref{thm:dyadiccompletion-regular-tail-stability} and
\autoref{thm:dyadiccompletion-streamname-handoff}. These rows keep the regular
tail and stream-name reads inside the displayed finite schedules and
real-boundary handoff rows.

Use \autoref{thm:dyadiccompletion-ledger-boundary} together with
\autoref{thm:dyadiccompletion-scoped-dependency-packet}. The ledger and scoped
package list every $\mathsf{BHist}$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, source
certificate, precision, and real-boundary row readable by the consumer.

Finally apply \autoref{thm:dyadiccompletion-public-regseqrat-real-export} and
\autoref{thm:dyadiccompletion-finite-window-standard-bridge}. The public export
and standard bridge repack exactly the same finite rows for $\RegSeqRatUp$ and
$\RealUp$ consumers. Since none of the cited rows contains quotient completion,
Dedekind cuts, trichotomy, host equality, or a chosen limit, those data are not
part of the mature consumer coverage.
\end{proof}

\begin{theorem}[DyadicCompletion finite-window ledger source exhaustion]
\label{thm:dyadiccompletion-window-ledger-source-exhaustion}
Every finite-window read of an accepted $\DyadicCompletionUp$ packet by a
$\RegSeqRatUp$ or $\RealUp$ consumer factors through exactly the ledger-named
source rows of \autoref{def:dyadiccompletion-carrier}: dyadic observation
rows, $\StreamNameUp$ window rows, regular-tail comparison rows,
real-boundary rows, $\mathsf{BHist}$ history, $\hsame$ transport,
$\Cont$ continuation, and $\mathsf{Pkg}$ provenance.

Conversely, every ledger-named source row in that carrier is readable by the
corresponding $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer after the
displayed transport through the public finite-window handoff.

The exhaustion statement contains no quotient completion, Dedekind cut,
trichotomy, host equality, chosen limit, or hidden precision schedule.
\end{theorem}
\begin{proof}
First use \autoref{thm:dyadiccompletion-streamname-handoff}. A downstream
window read is sent to one of the displayed $\StreamNameUp$ window rows, and
the handoff provides no alternate stream carrier.

Next combine \autoref{thm:dyadiccompletion-ledger-boundary} with
\autoref{thm:dyadiccompletion-public-regseqrat-real-export}. The ledger
boundary lists the dyadic observation, regular-tail, real-boundary, history,
transport, continuation, package, and provenance rows, while the public export
reuses those same rows for $\RegSeqRatUp$ and $\RealUp$ consumers.

For the reverse direction, apply
\autoref{thm:dyadiccompletion-finite-window-standard-bridge}. Each
ledger-named row is repacked as a standard finite-window read along the
displayed transport, so the corresponding $\StreamNameUp$, $\RegSeqRatUp$, or
$\RealUp$ consumer can read it without adding data.

Finally invoke \autoref{thm:dyadiccompletion-mature-consumer-coverage}. The
mature coverage theorem identifies the complete consumer boundary, so the two
directions exhaust the same finite ledger rows and exclude quotient completion,
Dedekind cuts, trichotomy, host equality, chosen limits, and hidden precision
schedules.
\end{proof}

\begin{theorem}[DyadicCompletion RegSeqRat window exhaustion]
\label{thm:dyadiccompletion-regseqrat-window-exhaustion}
For any accepted $\DyadicCompletionUp$ finite-window packet, every
$\RegSeqRatUp$ consumer read exposed by the public finite-window export factors
through one of the ledger-named rows already listed by the packet: dyadic
observation rows, scheduled $\StreamNameUp$ window rows, regular-tail classifier
rows, real-boundary rows, $\hsame$ transports, $\Cont$ routes, and $\mathsf{Pkg}$
provenance rows.

Conversely, every ledger-named regular-tail or window row supplies the same
$\RegSeqRatUp$ consumer read after the displayed finite-window standard bridge.
This exhaustion exports no quotient Cauchy stream, Dedekind cut, host real
limit, hidden precision schedule, or choice-selected limit.
\end{theorem}
\begin{proof}
First apply \autoref{thm:dyadiccompletion-streamname-handoff}. It sends the
scheduled finite read to the displayed $\StreamNameUp$ window row, so the
consumer has no alternate stream schedule outside the packet.

Next use \autoref{thm:dyadiccompletion-regular-tail-stability}. The regular-tail
classifier is transported along the packet's $\hsame$ and $\Cont$ rows, and the
transport preserves exactly the ledger-named tail comparison row read by the
$\RegSeqRatUp$ consumer.

Combine \autoref{thm:dyadiccompletion-ledger-boundary} with
\autoref{thm:dyadiccompletion-public-regseqrat-real-export}. These theorems
identify the visible dyadic observation, real-boundary, history, transport,
continuation, and package provenance rows exported to public $\RegSeqRatUp$ and
$\RealUp$ consumers.

For the converse, apply
\autoref{thm:dyadiccompletion-finite-window-standard-bridge} and
\autoref{thm:dyadiccompletion-window-ledger-source-exhaustion}. A ledger-named
regular-tail or scheduled-window row is repacked through the same finite-window
standard bridge, and the source exhaustion theorem says that no further source
row is available to the consumer.

Thus the two readings have the same public $\RegSeqRatUp$ row boundary. The
excluded quotient streams, cuts, host limits, hidden schedules, and selected
limits are absent because none of the cited ledger, handoff, stability, export,
or bridge rows carries them.
\end{proof}

\begin{theorem}[DyadicCompletion terminal finite-window seal determinacy]\label{thm:dyadiccompletion-terminal-window-seal-determinacy}
Consider two $\DyadicCompletionUp$ finite-window packets whose dyadic
observation row, $\StreamNameUp$ window row, regular-tail row, $\RealUp$
boundary row, ledger row, provenance row, and downstream handoff route agree by
the displayed $\hsame$, $\Cont$, and $\Pkg$ witnesses. Then every terminal
$\RegSeqRatUp$ or $\RealUp$ seal consumer factors through the same public
finite-window row. No second terminal seal, hidden precision schedule, quotient
completion, host equality, or choice-selected limit is exported by the
DyadicCompletion boundary.
\end{theorem}
\begin{proof}
First apply \autoref{thm:dyadiccompletion-streamname-handoff}. The common
$\StreamNameUp$ window row fixes the visible stream handoff for both packets,
and the displayed $\hsame$ witness gives the same finite observation boundary.

Next combine \autoref{thm:dyadiccompletion-public-regseqrat-real-export} with
\autoref{thm:dyadiccompletion-ledger-boundary}. The regular-tail row,
$\RealUp$ boundary row, ledger row, provenance row, and $\Pkg$ source row are
therefore read from a single public packet surface, while the listed $\Cont$
routes give the same downstream handoff for $\RegSeqRatUp$ and $\RealUp$
consumers.

Now use \autoref{thm:dyadiccompletion-finite-window-standard-bridge}. The
shared ledger and transport rows repack the two finite-window packets as the
same standard public row, so transitivity of the displayed $\hsame$ comparisons
identifies the row consumed by either terminal seal.

Finally invoke \autoref{thm:dyadiccompletion-mature-consumer-coverage}. Mature
coverage says that the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ consumers
listed above exhaust the terminal boundary; hence no further seal, precision
schedule, quotient completion, host equality, or choice-selected limit can be
exported outside the identified finite-window row.
\end{proof}

\begin{theorem}[DyadicCompletion RealUp route exhaustion]\label{thm:dyadiccompletion-realup-route-exhaustion}
Every $\RealUp$ seal read exposed by an accepted $\DyadicCompletionUp$
finite-window packet is exhausted by the same finite route through the dyadic
observation rows, the $\StreamNameUp$ windows of
\autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$
regular-tail rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the
dyadic precision rows of \autoref{ch:concrete-instances-dyadicratcore-namecert},
the $\RealUp$ boundary of \autoref{ch:concrete-instances-real-namecert}, the
precision ledger, the displayed $\hsame$ and $\Cont$ transports, the
$\mathsf{Pkg}$ row, and the local $\NameCert$ provenance row.

Consequently no $\DyadicCompletionUp$ terminal $\RealUp$ seal can be read
through a second route outside the listed stream-window, regular-tail, dyadic,
real-boundary, ledger, transport, continuation, package, and provenance rows.
\end{theorem}
\begin{proof}
Project the accepted packet through \autoref{def:dyadiccompletion-carrier}.
The carrier exposes the finite tuple of dyadic observations, stream-window
names, regular-tail certificates, real-boundary handoff, precision ledger,
history comparison, continuation, package source, and local provenance rows on
which a $\DyadicCompletionUp$ consumer may depend.

Next apply \autoref{thm:dyadiccompletion-window-ledger-source-exhaustion}. The
ledger admits only the scheduled dyadic and $\StreamNameUp$ windows named by
the packet, so the $\RealUp$ seal cannot import an additional finite-window
source. Apply \autoref{thm:dyadiccompletion-regseqrat-window-exhaustion} to the
regular-tail side; the $\RegSeqRatUp$ read is forced through the same
window, ledger, $\hsame$, $\Cont$, and $\mathsf{Pkg}$ route.

The sibling chapters provide the route endpoints. The stream-window rows come
from \autoref{ch:concrete-instances-streamname-namecert}, the regular-tail
classifier rows from \autoref{ch:concrete-instances-regseqrat-namecert}, the
dyadic precision rows from \autoref{ch:concrete-instances-dyadicratcore-namecert},
and the terminal real-name boundary from
\autoref{ch:concrete-instances-real-namecert}. These are precisely the rows
already recorded by the local $\NameCert$ provenance surface.

Finally use
\autoref{thm:dyadiccompletion-terminal-window-seal-determinacy}. Once the
dyadic observation, stream-window, regular-tail, real-boundary, ledger,
transport, continuation, package, and provenance rows agree, the terminal
$\RealUp$ seal has no alternate public finite-window row. Hence the displayed
route exhausts every $\DyadicCompletionUp$ real-seal read.
\end{proof}

\begin{theorem}[DyadicCompletion regular-tail budget refinement]
\label{thm:dyadiccompletion-regular-tail-budget-refinement}
Let an accepted $\DyadicCompletionUp$ finite-window packet carry a certified
tail-radius seal whose regular-tail radius is refined along the
$\StreamNameUp$ handoff row. Then the refined seal induces the same
$\RegSeqRatUp$ classifier window as the original packet and preserves the
terminal $\RealUp$ seal route exhausted in
\autoref{thm:dyadiccompletion-realup-route-exhaustion}.

The induced budget witness is the one consumed by
\autoref{ch:concrete-instances-regseqobservationbudget-namecert}: it records
the same dyadic observation rows, scheduled $\StreamNameUp$ windows,
regular-tail comparison rows, precision ledger entries, displayed $\hsame$ and
$\Cont$ transports, $\mathsf{Pkg}$ provenance, and local $\NameCert$ source
certificate, with only the tail-radius ledger made sharper.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:dyadiccompletion-regular-tail-stability}. Refining the
tail-radius seal along the displayed $\hsame$ and $\Cont$ rows transports the
regular-tail comparison data without changing the $\RegSeqRatUp$ classifier
window read from the packet.

Next use \autoref{thm:dyadiccompletion-streamname-handoff}. The sharpened
radius is attached to the same scheduled $\StreamNameUp$ handoff row, so the
stream-window source and the regular-tail classifier still share the ledger row
visible to the downstream consumer.

Finally invoke \autoref{thm:dyadiccompletion-realup-route-exhaustion}. Since
the dyadic observation, stream-window, regular-tail, real-boundary, ledger,
transport, continuation, package, and provenance rows are the same rows after
refinement, the terminal $\RealUp$ seal is forced through the same finite
route. The only exported extra datum is the sharper budget witness used by
\autoref{ch:concrete-instances-regseqobservationbudget-namecert}.
\end{proof}

\closureat{DyadicCompletionUp}{obligationStr}
\closureat{DyadicCompletionUp}{scopedStr}
\closureat{DyadicCompletionUp}{publicStr}
\closureat{DyadicCompletionUp}{bridgedStr}
\closureat{DyadicCompletionUp}{matureStr}

\begin{closurestatus}{\DyadicCompletionUp}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:dyadiccompletion-mature-consumer-coverage} identifies the finite dyadic observation, stream-window, regular-tail, real-boundary, ledger, transport, continuation, package, public export, and standard bridge rows as the mature constructive $\RegSeqRatUp/\RealUp$ consumer boundary over the listed BEDC source certificates.}
  \constructivestory{A $\DyadicCompletionUp$ packet is generated by scheduled dyadic observations, certified stream windows, regular-tail comparison rows, a real-name handoff boundary, and a precision ledger that records every consumed schedule and tail bound.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.DyadicCompletionUp.DyadicCompletionPacket\_namecert\_obligation\_surface}
  \bridgestatus{paperBridge}
  \origin{human}
  \notclaimed{This chapter does not close the Lean namespace, a complete real construction, quotient equality of Cauchy streams, Dedekind real equivalence, host real limits, choice-selected limits, or completeness.}
  \upgradepath{The paper-side mature consumer coverage is complete; remaining work is Lean-side formalization and bridge checking for the same $\DyadicCompletionUp$ packet.}
\end{closurestatus}
