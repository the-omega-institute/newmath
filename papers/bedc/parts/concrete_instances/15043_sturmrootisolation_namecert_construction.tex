\chapter{A Concrete Naming Certificate for $\SturmRootIsolationUp$}
\label{ch:concrete-instances-sturmrootisolation-namecert}
\origin{human}

Following Sturm and Bishop--Bridges constructive analysis, the $\SturmRootIsolationUp$ packet reads a finite polynomial root-isolation surface through Sturm-chain sign-variation data rather than through host algebraic closure, real-root choice, or ambient completeness. It consumes the polynomial source of \autoref{ch:concrete-instances-polynomial-namecert}, the rational and dyadic interval ledgers of \autoref{ch:concrete-instances-rationalinterval-namecert} and \autoref{ch:concrete-instances-dyadicinterval-namecert}, and the adjacent sign-change bisection packet of \autoref{ch:concrete-instances-bisectionrootisolation-namecert}. Its downstream completion boundary is the finite window route through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Sturm root-isolation carrier]
\label{def:sturm-root-isolation-carrier}
A $\SturmRootIsolationUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)
$$
with the following visible rows:
\begin{enumerate}
\item $P$ is the $\PolynomialUp$ source row, including the displayed coefficient ledger and the finite derivative and pseudo-remainder inputs from which the Sturm chain is read;
\item $I$ is the $\RationalIntervalUp$ interval ledger carrying the inspected rational cells and their endpoint order rows;
\item $D$ is the $\DyadicIntervalUp$ refinement ledger used for finite cell subdivision and endpoint-window export;
\item $V$ is the Sturm-chain division and sign-variation ledger, recording each displayed division row, the endpoint sign rows for every chain entry, and the variation-count differences for the inspected cells;
\item $B$ is the selected isolating branch, represented as the retained cell together with the finite comparison rows showing that its variation drop is the accepted one-root branch and that discarded cells have the displayed alternative counts;
\item $W$ is the $\StreamNameUp$ window surface exposed after the selected rational or dyadic cell has been fixed;
\item $R$ is the $\RegSeqRatUp$ handoff row obtained from the selected interval windows and the requested unary precision;
\item $S$ is the $\RealUp$ seal for the retained endpoint names and for downstream real-facing consumers of the isolating window;
\item $H$ stores the componentwise $\hsame$ transports for the polynomial row, interval rows, Sturm ledger, branch row, stream windows, regular readbacks, real seal rows, package provenance, and local certificate;
\item $C$ stores the displayed $\Cont$ replay routes for polynomial inspection, Sturm-chain division replay, sign-variation counting, branch selection, window export, regular-sequence handoff, and real-seal consumption;
\item $Q_0$ is the $\Pkg$ provenance over $\PolynomialUp$, $\RationalIntervalUp$, $\DyadicIntervalUp$, $\BisectionRootIsolationUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\SturmRootIsolationUp}$ registration row.
\end{enumerate}
The carrier predicate says that these rows form one finite Sturm-chain root-isolation packet. It has no coordinate for an algebraic root object, a host factorization theorem, a classical completeness principle, a quotient real equality, or an ambient real-root existence theorem.
\end{definition}
\leandef{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier}

\begin{theorem}[Sturm root-isolation NameCert obligations]
\label{thm:sturm-root-isolation-namecert-obligations}
For every accepted $\SturmRootIsolationUp$ carrier $Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)$, the local $\NameCert_{\SturmRootIsolationUp}$ surface consists exactly of:
\begin{enumerate}
\item carrier admission for the finite packet of \autoref{def:sturm-root-isolation-carrier};
\item polynomial-source admission through the coefficient, derivative, and pseudo-remainder rows of $P$;
\item interval admission through the rational cell ledger $I$ and the dyadic refinement ledger $D$;
\item Sturm-ledger exactness, requiring every division row and every endpoint sign row used in $V$ to be one of the displayed finite rows;
\item branch isolation, requiring the selected row $B$ to be justified by the displayed sign-variation drop in $V$ and handed to the adjacent bisection packet only as a finite retained-cell surface;
\item retained-window handoff through $W$, $R$, and $S$;
\item componentwise transport, replay, provenance, and naming through $H$, $C$, $Q_0$, and $N$.
\end{enumerate}
No further analytic or algebraic obligation is part of the packet.
\end{theorem}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:sturm-root-isolation-carrier}. The polynomial row supplies only the displayed coefficient, derivative, and pseudo-remainder inputs, while the rational and dyadic interval rows supply only the inspected cells and endpoint windows.

The Sturm ledger $V$ accounts for the chain computation. Each sign row used in a variation count is attached to a displayed endpoint and a displayed chain entry, and each division row is recorded as finite ledger data rather than as an external factorization theorem.

The selected branch $B$ is then read from the displayed variation-drop ledger. Its retained cell may be handed to the bisection surface of \autoref{ch:concrete-instances-bisectionrootisolation-namecert}, but only as the already selected rational or dyadic window with its sign and variation evidence still attached.

Finally, $W$, $R$, and $S$ expose the finite window to stream, regular-rational, and real-facing consumers. The rows $H,C,Q_0$, and $N$ supply transport, replay, provenance, and local naming, so the packet has no remaining coordinate from which to extract an algebraic root object, host factorization, quotient real equality, or completeness principle.
\end{proof}

\begin{theorem}[Sturm root-isolation sign-variation handoff]
\label{thm:sturm-root-isolation-sign-variation-handoff}
For every accepted $\SturmRootIsolationUp$ carrier and every selected branch row $B$, the branch handed to downstream interval consumers factors through the $\PolynomialUp$ row $P$, the $\RationalIntervalUp$ and $\DyadicIntervalUp$ interval rows $I,D$, the Sturm-chain division and sign-variation ledger $V$, the retained-cell row of $B$, and the replay rows $H,C,Q_0,N$. The handoff exports a finite isolating cell with displayed variation-count evidence and exports no host root selection.
\end{theorem}
\begin{proof}
Read the selected branch coordinate. By construction, it is not independent of the Sturm ledger: the retained cell in $B$ is accepted only after the endpoint signs and variation-count difference have been read from $V$.

The interval coordinates $I$ and $D$ provide the cell on which those signs are inspected. The polynomial coordinate $P$ provides the finite chain entries whose signs are counted, and $H,C,Q_0,N$ replay the same rows under transport and package provenance.

Consequently any downstream interval consumer receives the retained cell together with its displayed variation-count evidence. Since no coordinate stores a chosen host root or a completed real-root witness, the handoff remains a finite NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_sign\_variation\_handoff}

\begin{theorem}[Sturm root-isolation squarefree route]
\label{thm:sturm-root-isolation-squarefree-route}
Every accepted $\SturmRootIsolationUp$ carrier reads the squarefree polynomial route before a sign-variation count can be consumed. Concretely, the polynomial source row $P$ and Sturm ledger $V$ must display the squarefree factor row, the pseudo-remainder or resultant compatibility row, the endpoint sign rows, and the retained interval rows from $I,D$ before the branch row $B$ is available. The interval-refinement certificate may cite \autoref{ch:concrete-instances-sturm-sequence-namecert}, \autoref{ch:concrete-instances-polynomial-resultant-namecert}, \autoref{ch:concrete-instances-rationalinterval-namecert}, \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-streamname-namecert} only through these displayed $\BHist$ and $\NameCert$ rows, and it exports no host squarefree factorization theorem.
\end{theorem}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_squarefree\_route}
\begin{proof}
Project the carrier of \autoref{def:sturm-root-isolation-carrier}. The polynomial source row $P$ contains the coefficient, derivative, pseudo-remainder, and squarefree-route data from which the finite Sturm sequence is read.

Use the sign-variation handoff of \autoref{thm:sturm-root-isolation-sign-variation-handoff}. It places every branch consumer downstream of $P,I,D,V,B$ and therefore downstream of the displayed squarefree and endpoint-sign rows that feed $V$.

The sibling chapters supply only their finite certificate surfaces: \autoref{ch:concrete-instances-sturm-sequence-namecert} supplies the chain rows, \autoref{ch:concrete-instances-polynomial-resultant-namecert} supplies compatibility data, and the rational, stream, regular-rational, and Real siblings supply interval and completion rows. Since no carrier coordinate stores a host factorization theorem, the squarefree route is exactly the displayed packet route.
\end{proof}

\begin{theorem}[Sturm root-isolation subresultant sign-variation route]
\label{thm:sturm-root-isolation-subresultant-sign-variation}
For every accepted $\SturmRootIsolationUp$ carrier $Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)$, the sign-variation count used by $B$ is downstream of the subresultant or pseudo-remainder compatibility rows displayed in $P$ and $V$. The interval-refinement certificate cannot bypass those rows: any refinement that reaches $W,R,S$ must first replay the polynomial resultant route, the Sturm sequence route, the endpoint sign rows, and the rational or dyadic interval rows through $H,C,Q_0,N$.
\end{theorem}
\begin{proof}
Read the Sturm ledger $V$. Its variation-count entries are tied to displayed chain entries and endpoint signs, so a count cannot be consumed before the subresultant or pseudo-remainder compatibility rows for those chain entries have been displayed.

Apply the squarefree route theorem \autoref{thm:sturm-root-isolation-squarefree-route}. It places the branch row $B$ downstream of the squarefree polynomial route and the resultant compatibility rows, while \autoref{ch:concrete-instances-sturm-sequence-namecert} and \autoref{ch:concrete-instances-polynomial-resultant-namecert} provide only named finite sibling surfaces.

Finally use interval refinement and Real seal nonescape, \autoref{thm:sturm-root-isolation-interval-refinement} and \autoref{thm:sturm-root-isolation-real-seal-nonescape}. The retained interval cannot reach $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ rows before the same polynomial, resultant, sign-variation, and interval rows have been replayed through $H,C,Q_0,N$.
\end{proof}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_subresultant\_sign\_variation}

\begin{theorem}[Sturm root-isolation sign-variation scope]
\label{thm:sturm-root-isolation-sign-variation-scope}
For every accepted $\SturmRootIsolationUp$ carrier $Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)$, every accepted branch selection reads the polynomial chain rows in $P$ and the endpoint sign rows in $V$ before the branch row $B$ is available. The branch classifier therefore acts only on displayed polynomial, endpoint-sign, and variation-count data, and it exports no root object or external sign oracle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sturm-root-isolation-carrier}. The polynomial source row $P$ stores the displayed coefficient, derivative, and pseudo-remainder inputs from which the finite Sturm chain is read.

Use the sign-variation handoff of \autoref{thm:sturm-root-isolation-sign-variation-handoff}. It places the selected branch downstream of the Sturm ledger $V$, so the endpoint sign rows and variation-count difference are already visible before $B$ can be consumed.

The structural rows $H,C,Q_0,N$ replay and name the same finite rows. Since the carrier has no coordinate for a host root, algebraic closure, or independent sign oracle, branch selection is scoped to the displayed polynomial chain and endpoint sign ledger.
\end{proof}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_sign\_variation\_scope}

\begin{theorem}[Sturm root-isolation interval refinement]
\label{thm:sturm-root-isolation-interval-refinement}
For every accepted $\SturmRootIsolationUp$ carrier $Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)$, rational interval cells from $I$ refine through the $\DyadicIntervalUp$ row $D$ before any $\StreamNameUp$ window $W$ or $\RegSeqRatUp$ readback $R$ is exposed. Thus a completion-facing consumer sees stream and regular-rational windows only after the retained rational cell has passed through the displayed dyadic refinement ledger.
\end{theorem}
\begin{proof}
Read the interval coordinates of \autoref{def:sturm-root-isolation-carrier}. The rational interval row $I$ carries the inspected cells, while $D$ records the dyadic refinement ledger used for finite subdivision and endpoint-window export.

Apply the branch handoff of \autoref{thm:sturm-root-isolation-sign-variation-handoff}. It keeps the retained cell attached to the rational and dyadic interval rows before the branch is handed to downstream interval consumers.

The Real seal nonescape theorem \autoref{thm:sturm-root-isolation-real-seal-nonescape} then places the $\StreamNameUp$ window $W$ and $\RegSeqRatUp$ readback $R$ downstream of that selected interval route. Hence no stream or regular-rational window is exposed before dyadic refinement has been displayed.
\end{proof}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_interval\_refinement}

\begin{theorem}[Sturm root-isolation certificate ledger]
\label{thm:sturm-root-isolation-certificate-ledger}
For every accepted $\SturmRootIsolationUp$ carrier $Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)$, the local certificate ledger is exhausted by the rows
$$
P,I,D,V,B,W,R,S,H,C,Q_0,N .
$$
It exports polynomial source data, rational and dyadic interval rows, sign-variation rows, a selected branch, stream windows, regular rational readback, a Real-facing seal, transport, replay, provenance, and local naming, but no host root, algebraic closure, quotient equality, real trichotomy, or completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_certificate\_ledger}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:sturm-root-isolation-namecert-obligations}. They list exactly the carrier, polynomial, interval, Sturm-ledger, branch, retained-window, transport, replay, provenance, and naming obligations.

The sign-variation scope theorem shows that branch selection consumes only $P,V,B$ together with the interval route. The interval refinement theorem shows that $I,D$ precede the stream and regular-rational rows $W,R$.

Finally use the Real seal nonescape theorem. It places the seal row $S$ downstream of $I,D,V,W,R,H,C,Q_0,N$ and excludes algebraic roots, host root existence, quotient real equality, trichotomy, and ambient completeness. The displayed tuple therefore exhausts the certificate ledger.
\end{proof}

\begin{theorem}[Sturm root-isolation Real seal nonescape]
\label{thm:sturm-root-isolation-real-seal-nonescape}
Every $\RealUp$ seal read exported by an accepted $\SturmRootIsolationUp$ carrier is reached only after the selected rational or dyadic interval row, the displayed Sturm-chain sign-variation ledger, the $\StreamNameUp$ window surface, the $\RegSeqRatUp$ readback row, and the transport, replay, provenance, and local naming rows have been read. The seal exposes the finite isolating window and exports no algebraic root object, host root existence theorem, quotient real equality, real trichotomy, or $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Inspect the seal coordinate $S$. It is stored with the selected interval row, the Sturm ledger, the stream windows, the regular-rational handoff, and the BEDC transport and replay rows; it is not stored as a free real object.

First the selected branch is fixed by \autoref{thm:sturm-root-isolation-sign-variation-handoff}. Then the retained interval windows pass through the $\StreamNameUp$ and $\RegSeqRatUp$ surfaces at the requested precision.

Only after that route is fixed can the $\RealUp$ seal be read. Thus the seal records the same finite isolating window and cannot export an algebraic root object, host root existence, quotient real equality, trichotomy data, or an ambient completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_real\_seal\_nonescape}

\begin{theorem}[Sturm root-isolation obligation closure route]
\label{thm:sturm-root-isolation-obligation-closure-route}
For every accepted $\SturmRootIsolationUp$ carrier $Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)$, the polynomial source row, rational and dyadic interval rows, Sturm sign-variation ledger, selected branch, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, and $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows exhaust the local obligation surface. The packet has no remaining obligation for a host root object, algebraic closure theorem, quotient real equality, real trichotomy, selected real root, or $\RealUp$ completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_obligation\_closure\_route}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:sturm-root-isolation-namecert-obligations}. They list carrier admission, polynomial-source admission, rational and dyadic interval admission, the Sturm ledger, branch isolation, retained-window handoff, transport, replay, provenance, and local naming.

Use the sign-variation handoff and scope rows, \autoref{thm:sturm-root-isolation-sign-variation-handoff} and \autoref{thm:sturm-root-isolation-sign-variation-scope}. They place every accepted branch downstream of the displayed polynomial chain rows, endpoint sign rows, and variation-count differences rather than a host root selector.

Apply interval refinement and certificate ledger exhaustion, \autoref{thm:sturm-root-isolation-interval-refinement} and \autoref{thm:sturm-root-isolation-certificate-ledger}. These force rational cells through the dyadic refinement ledger before any stream window, regular-rational readback, or real-facing seal is exposed.

Finally apply Real seal nonescape, \autoref{thm:sturm-root-isolation-real-seal-nonescape}. Since the seal is downstream of the selected interval, Sturm ledger, stream windows, regular-rational readback, transport, replay, provenance, and naming rows, no algebraic root object, host root theorem, quotient equality, trichotomy datum, selected root, or completeness principle remains.
\end{proof}

\begin{theorem}[Sturm root-isolation scoped kernel binding]
\label{thm:sturm-root-isolation-scoped-kernel-binding}
Every accepted $\SturmRootIsolationUp$ carrier
$$
Q=(P,I,D,V,B,W,R,S,H,C,Q_0,N)
$$
is scoped exactly by the displayed $\PolynomialUp$, $\RationalIntervalUp$, $\DyadicIntervalUp$, $\BisectionRootIsolationUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before any branch consumer or Real-seal consumer reads it. The scoped packet exposes no algebraic root object, host factorization theorem, quotient real equality, real trichotomy, selected real root, or $\RealUp$ completeness principle outside those rows.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:sturm-root-isolation-namecert-obligations}. They bind carrier admission to the displayed polynomial source, rational and dyadic interval ledgers, Sturm sign-variation ledger, selected branch, stream window, regular-rational readback, Real seal, transport, replay, provenance, and local naming rows.

Use the sign-variation handoff of \autoref{thm:sturm-root-isolation-sign-variation-handoff}. It places the branch row downstream of the polynomial, interval, and sign-variation data, with the adjacent $\BisectionRootIsolationUp$ consumer receiving only the retained finite cell and its variation-count evidence.

Apply interval refinement, \autoref{thm:sturm-root-isolation-interval-refinement}. It forces rational cells through the dyadic interval ledger before stream windows or regular-rational readback become visible.

Finally apply Real seal nonescape, \autoref{thm:sturm-root-isolation-real-seal-nonescape}. The seal remains downstream of the same displayed rows and cannot expose host algebraic roots, quotient equality, trichotomy, selected roots, or ambient completeness. Hence every branch or Real-seal consumer reads exactly the listed kernel rows before it can consume the packet.
\end{proof}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_scoped\_kernel\_binding}

\begin{theorem}[Sturm root-isolation scoped interval and Real-seal route]
\label{thm:sturm-root-isolation-scoped-interval-real-seal-route}
Every accepted $\SturmRootIsolationUp$ branch-selection consumer must pass
through the $\PolynomialUp$ source row, $\RationalIntervalUp$ cell ledger,
$\DyadicIntervalUp$ refinement ledger, Sturm sign-variation ledger,
$\BisectionRootIsolationUp$ branch handoff, $\StreamNameUp$ windows,
$\RegSeqRatUp$ readbacks, and $\RealUp$ seal rows before it can be used as a
scoped interval or real-facing root-isolation route. The route does not claim
algebraic root existence, factorization completeness, real trichotomy, quotient
equality, selected real roots, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of
\autoref{thm:sturm-root-isolation-namecert-obligations}. They expose the
polynomial source row, rational and dyadic interval rows, Sturm ledger, branch
row, stream windows, regular-rational readback, Real seal, transport, replay,
provenance, and local naming as the finite carrier surface.

Use the sign-variation handoff and sign-variation scope,
\autoref{thm:sturm-root-isolation-sign-variation-handoff} and
\autoref{thm:sturm-root-isolation-sign-variation-scope}. These place every
accepted branch downstream of the polynomial chain rows, endpoint sign rows,
variation-count differences, and the retained $\BisectionRootIsolationUp$
branch handoff.

Apply interval refinement and certificate ledger exhaustion,
\autoref{thm:sturm-root-isolation-interval-refinement} and
\autoref{thm:sturm-root-isolation-certificate-ledger}. The rational cell ledger
and dyadic refinement ledger therefore remain visible before any
$\StreamNameUp$ window, $\RegSeqRatUp$ readback, or real-facing consumer is
available.

Finally use Real seal nonescape, the obligation-closure route, and scoped
kernel binding:
\autoref{thm:sturm-root-isolation-real-seal-nonescape},
\autoref{thm:sturm-root-isolation-obligation-closure-route}, and
\autoref{thm:sturm-root-isolation-scoped-kernel-binding}. Together they keep
the branch and seal consumers inside the displayed polynomial, interval,
Sturm-ledger, bisection, stream, regular-rational, and real-seal rows. Since
the carrier has no coordinate for algebraic root existence, factorization
completeness, trichotomy, quotient equality, selected real roots, or Real
completeness, the consumer route is exactly the scoped interval and Real-seal
route stated above.
\end{proof}
\leanchecked{BEDC.Derived.SturmRootIsolationUp.SturmRootIsolationCarrier\_scoped\_interval\_real\_seal\_route}

\closureat{SturmRootIsolationUp}{seedStr}
\closureat{SturmRootIsolationUp}{obligationStr}
\closureat{SturmRootIsolationUp}{scopedStr}

\begin{closurestatus}{\SturmRootIsolationUp}
  \constructivestory{A $\SturmRootIsolationUp$ packet is generated from a $\PolynomialUp$ source row, $\RationalIntervalUp$ and $\DyadicIntervalUp$ interval rows, a finite Sturm-chain division and sign-variation ledger, a selected isolating branch, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\RealUp$ seal rows, componentwise transport, continuation replay, provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:sturm-root-isolation-carrier}, \autoref{thm:sturm-root-isolation-namecert-obligations}, \autoref{thm:sturm-root-isolation-sign-variation-handoff}, \autoref{thm:sturm-root-isolation-real-seal-nonescape}, and \autoref{thm:sturm-root-isolation-scoped-kernel-binding} bind finite Sturm-chain root isolation to the displayed polynomial, rational interval, dyadic interval, stream, regular-rational, real-seal, transport, replay, provenance, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close algebraic root existence, a host factorization theorem, quotient real equality, real trichotomy, or $\RealUp$ completeness.}
  \upgradepath{Scoped kernel binding is the next paper-theory step, tying the finite Sturm-chain obligation package to a public interval and real-seal consumer boundary.}
\end{closurestatus}
