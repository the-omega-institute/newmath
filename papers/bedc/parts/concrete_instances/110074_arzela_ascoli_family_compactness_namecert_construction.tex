\chapter{Arzela--Ascoli Family Compactness NameCert Construction}
\label{ch:concrete-instances-arzela-ascoli-family-compactness-namecert}
\origin{human}

Following Arzela, Ascoli, and Bishop--Bridges constructive compactness, the $\ArzelaAscoliFamilyCompactnessUp$ packet records family compactness for equicontinuous continuous-map families as finite BEDC NameCert data. It sits between the Arzela--Ascoli carrier of \autoref{ch:concrete-instances-arzelaascoli-namecert}, the equicontinuity family surface of \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map graph surface of \autoref{ch:concrete-instances-continuousmap-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names the finite family-compactness route; it does not assert an ambient compact-open topology, open-cover compactness, countable choice, selected infinite subsequence choice, or host function equality.

\begin{definition}[Arzela--Ascoli family-compactness carrier]
\label{def:arzela-ascoli-family-compactness-carrier}
An $\ArzelaAscoliFamilyCompactnessUp$ carrier is a finite $\BHist$ packet
$$
F=(A,E,K,G,M,W,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\ArzelaAscoliUp$ row naming the compact-continuity packet over source nets, target completion rows, finite family graphs, pointwise image rows, and handoff ledgers.
\item $E$ is the $\EquicontinuityFamilyUp$ row naming the shared finite-center equicontinuity modulus for the family.
\item $K$ is the $\CompactMetricUp$ source row whose finite probe bundle and compact-metric coverage supply the source windows.
\item $G$ is the $\ContinuousMapUp$ family graph row, storing each displayed family member as graph and pointwise-modulus data.
\item $M$ is the $\UniformModulusUp$ handoff row, receiving only the shared finite-radius and tolerance rows extracted from $E$ and $K$.
\item $W$ is the family window row, listing the finite source probes, family indices, target observations, and diagonal readback windows visible to the compactness consumer.
\item $B$ is the bounded image row, retaining the pointwise finite image bundles required before any family-compactness read is exported.
\item $H$ records componentwise $\hsame$ transport for Arzela--Ascoli, equicontinuity, compact-metric, continuous-map, uniform-modulus, window, bounded-image, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a family-compactness request through $A,E,K,G,M,W,B$.
\item $P$ is the $\Pkg$ provenance over $\ArzelaAscoliFamilyCompactnessUp$, $\ArzelaAscoliUp$, $\EquicontinuityFamilyUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ArzelaAscoliFamilyCompactnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,E,K,G,M,W,B,H,C,P,N$. It has no coordinate for compact-open topology, host function extensionality, selected subsequence choice, countable choice, open-cover compactness, or ambient completion outside the displayed rows.
\end{definition}

\begin{theorem}[Arzela--Ascoli family-compactness NameCert obligations]
\label{thm:arzela-ascoli-family-compactness-namecert-obligations}
For every accepted $\ArzelaAscoliFamilyCompactnessUp$ carrier
$$
F=(A,E,K,G,M,W,B,H,C,P,N),
$$
the local $\NameCert_{\ArzelaAscoliFamilyCompactnessUp}$ surface consists exactly of carrier admission, Arzela--Ascoli packet exposure, equicontinuity-family exposure, compact-metric source-window exposure, continuous-map graph exposure, uniform-modulus handoff, finite family-window readback, bounded-image retention, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arzela-ascoli-family-compactness-carrier}. The rows $A,E,K,G,M,W,B,H,C,P,N$ are the only readable coordinates of the packet.

The rows $A$ and $E$ supply the compact-continuity and equicontinuity-family surfaces. The compact-metric row $K$ supplies finite source probes, the continuous-map row $G$ supplies graph and pointwise-modulus reads, and the uniform-modulus row $M$ receives only the shared finite tolerance handoff.

The family-window row $W$ and bounded-image row $B$ keep the diagonal and image-bundle reads visible before any compactness consumer can cite the packet. These rows are finite BEDC histories rather than host-level choices of a subsequence or function-space cover.

Finally $H,C,P,N$ transport, replay, source, and name exactly that inventory. Since the carrier has no compact-open, function-extensionality, selected-subsequence, countable-choice, open-cover, or ambient-completion coordinate, none of those objects can be exported.
\end{proof}

\begin{theorem}[Arzela--Ascoli family-compactness finite route]
\label{thm:arzela-ascoli-family-compactness-finite-route}
Every family-compactness consumer of an accepted $\ArzelaAscoliFamilyCompactnessUp$ packet factors through the sequence of displayed sibling rows
$$
\ArzelaAscoliUp,\quad
\EquicontinuityFamilyUp,\quad
\CompactMetricUp,\quad
\ContinuousMapUp,\quad
\UniformModulusUp .
$$
The route reads finite source probes, family graph rows, shared equicontinuity radii, bounded target-image rows, and uniform-modulus handoff data before the local $\NameCert_{\ArzelaAscoliFamilyCompactnessUp}$ row is available.
\end{theorem}
\begin{proof}
Start with the obligation theorem,
\autoref{thm:arzela-ascoli-family-compactness-namecert-obligations}. It lists sibling exposure, source-window exposure, graph exposure, handoff, window readback, and bounded-image retention as required rows of the local NameCert surface.

Read the siblings in the displayed order. The Arzela--Ascoli row supplies the compact-continuity packet, the equicontinuity-family row supplies the shared radius ledger, the compact-metric row supplies finite source probes, the continuous-map row supplies graph observations, and the uniform-modulus row receives the finite tolerance handoff.

The structural rows $H,C,P,N$ preserve this sequence. A consumer therefore reaches family compactness only through the finite row inventory of the carrier and not through an ambient compactness theorem or selected host object.
\end{proof}

\falsifiablePrediction{Every accepted $\ArzelaAscoliFamilyCompactnessUp$ consumer read displays ArzelaAscoli, EquicontinuityFamily, CompactMetric, ContinuousMap, UniformModulus, finite family-window, bounded-image, transport, replay, provenance, and local NameCert rows before exporting a family-compactness packet.}

\independenceWitness{arzelaascoli, equicontinuityfamily, compactmetric, continuousmap, uniformmodulus: none is bijective to $\ArzelaAscoliFamilyCompactnessUp$ because the packet jointly records compact-continuity, shared equicontinuity, compact source windows, graph rows, uniform-modulus handoff, family-window readback, bounded-image retention, transport, replay, provenance, and local naming.}

\closureat{ArzelaAscoliFamilyCompactnessUp}{seedStr}

\begin{closurestatus}{\ArzelaAscoliFamilyCompactnessUp}
  \constructivestory{An $\ArzelaAscoliFamilyCompactnessUp$ packet is generated from ArzelaAscoli, EquicontinuityFamily, CompactMetric, ContinuousMap, UniformModulus, finite family-window, bounded-image, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:arzela-ascoli-family-compactness-carrier}, \autoref{thm:arzela-ascoli-family-compactness-namecert-obligations}, and \autoref{thm:arzela-ascoli-family-compactness-finite-route} seed the finite family-compactness route for equicontinuous continuous-map families over compact metric sources.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim compact-open topology, host function extensionality, selected infinite subsequences, countable choice, arbitrary open-cover compactness, ambient completion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sibling exposure, compact source-window exposure, continuous-map graph exposure, uniform-modulus handoff, family-window readback, bounded-image retention, transport, replay, provenance, and local naming for BEDC.Derived.ArzelaAscoliFamilyCompactnessUp.}
\end{closurestatus}
