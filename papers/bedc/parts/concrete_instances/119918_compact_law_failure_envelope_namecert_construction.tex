\chapter{A Concrete Naming Certificate for $\CompactLawFailureEnvelopeUp$}
\label{ch:concrete-instances-compact-law-failure-envelope-namecert}
\origin{ai}

The $\CompactLawFailureEnvelopeUp$ packet realizes the compact-uniform-modulus direction of \autoref{ch:visions-compact-uniform-continuity} as finite law-facing NameCert data. It sits after the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map row of \autoref{ch:concrete-instances-continuousmap-namecert}, the tolerance-only modulus handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the law-certificate surface of \autoref{ch:concrete-instances-law-certificate-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet binds compact source, continuous map, modulus, lawlike claim, failure surface, transport, replay, provenance, and local naming before a compact law can be consumed.

\begin{definition}[Compact law failure-envelope carrier]
\label{def:compact-law-failure-envelope-carrier}
A $\CompactLawFailureEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
K=(S,F,U,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\CompactMetricUp$ source row, naming the compact finite-net or compact-window surface on which the law is offered.
\item $F$ is the $\ContinuousMapUp$ row, naming the continuous map or observable transformation whose lawlike behavior is being inspected.
\item $U$ is the $\UniformModulusUp$ row, naming the tolerance-only modulus handoff extracted from compact and continuous data.
\item $L$ is the $\LawCertificateUp$ row, recording the lawlike equation, invariant, or classifier claim attached to the compact-uniform route.
\item $E$ is the failure-envelope row, listing compact windows, modulus refinements, source observations, or Real-facing reads that would defeat the law claim.
\item $H$ records componentwise $\hsame$ transport for the compact source, continuous map, modulus, law-certificate, failure-envelope, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from compact-source admission through continuous-map inspection, uniform-modulus handoff, law-certificate reading, and failure-envelope exposure.
\item $P$ is the $\Pkg$ provenance over $\CompactLawFailureEnvelopeUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\LawCertificateUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactLawFailureEnvelopeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a compact law without a modulus row, a law certificate without a failure envelope, a total prediction over all compact refinements, hidden compactness, selected real limits, or ledger-free law status.
\end{definition}

\begin{theorem}[Compact law failure-envelope NameCert obligations]
\label{thm:compact-law-failure-envelope-namecert-obligations}
For every accepted $\CompactLawFailureEnvelopeUp$ carrier
$$
K=(S,F,U,L,E,H,C,P,N),
$$
the local $\NameCert_{\CompactLawFailureEnvelopeUp}$ surface consists exactly of compact-source exposure through $S$, continuous-map exposure through $F$, uniform-modulus exposure through $U$, law-certificate exposure through $L$, failure-envelope exposure through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-law-failure-envelope-carrier}. The compact source, continuous map, uniform modulus, law certificate, and failure envelope are all displayed rows before the local name is public.

Read the compact-uniform route in order. The compact row supplies the source surface, the continuous-map row supplies the observable transformation, and the uniform-modulus row supplies the tolerance-only handoff. The law-certificate row then attaches the lawlike claim only after those rows are visible.

The failure-envelope row is part of the same NameCert surface. Componentwise transport, displayed replay, provenance, and local naming preserve this packet rather than replacing it by a compactness theorem or equation-only law, so the listed rows exhaust the local obligations.
\end{proof}

\begin{theorem}[Compact law failure-envelope classifier boundary]
\label{thm:compact-law-failure-envelope-classifier-boundary}
Every accepted $\CompactLawFailureEnvelopeUp$ consumer must preserve $S,F,U,L,E,H,C,P,N$ together. In particular, a compact law packet with a uniform modulus but no failure-envelope row is outside the classifier, and a route that keeps the law-certificate row while dropping compact source, continuous-map, modulus, or failure-surface data cannot export compact-law status.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:compact-law-failure-envelope-namecert-obligations}. They make $E$ a required row alongside the compact source, continuous-map, uniform-modulus, and law-certificate rows.

Apply the classifier clause in \autoref{def:compact-law-failure-envelope-carrier}. Accepted comparison preserves the displayed packet through $H,C,P,N$, so a consumer that omits $E$ or detaches $L$ from $S,F,U$ has left the accepted row surface.

The refusal clauses follow from absent coordinates. There is no hidden compactness row, selected real limit, total prediction, or ledger-free equation route from which the missing failure envelope could be reconstructed.
\end{proof}

The future Lean TasteGate surface for $\CompactLawFailureEnvelopeUp$ must provide explicit $\Nontrivial\ \CompactLawFailureEnvelopeUp$ and $\FieldFaithful\ \CompactLawFailureEnvelopeUp$ instances preserving $S,F,U,L,E,H,C,P,N$ as separate displayed rows.

\falsifiablePrediction{A compact law packet with a uniform modulus but no failure-envelope row is rejected by the classifier.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, lawcertificate: none is bijective because the new packet binds compact source, continuous map, modulus, lawlike claim, failure surface, and provenance rows in one carrier.}

\closureat{CompactLawFailureEnvelopeUp}{seedStr}

\begin{closurestatus}{\CompactLawFailureEnvelopeUp}
  \constructivestory{A $\CompactLawFailureEnvelopeUp$ packet is generated from CompactMetric source rows, ContinuousMap rows, UniformModulus handoff rows, LawCertificate rows, explicit failure-envelope rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-law-failure-envelope-carrier}, \autoref{thm:compact-law-failure-envelope-namecert-obligations}, and \autoref{thm:compact-law-failure-envelope-classifier-boundary} seed the compact law failure-envelope route over $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\LawCertificateUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, total compact-law prediction, hidden compactness, selected real limits, ledger-free equation status, or failure-envelope-free law export.}
  \upgradepath{Obligation closure requires checked carrier admission, compact source exposure, continuous-map exposure, uniform-modulus handoff, law-certificate exposure, failure-envelope exactness, transport, replay, provenance, and local naming for BEDC.Derived.CompactLawFailureEnvelopeUp.}
\end{closurestatus}
