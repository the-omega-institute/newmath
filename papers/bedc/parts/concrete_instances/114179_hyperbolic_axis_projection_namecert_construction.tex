\chapter{A Concrete Naming Certificate for $\HyperbolicAxisProjectionUp$}
\label{ch:concrete-instances-hyperbolic-axis-projection-namecert}
\origin{ai}

The $\HyperbolicAxisProjectionUp$ packet realizes an axis-facing branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite disk-metric, geodesic-flow, boundary-shadow, Busemann, visual-boundary, projection-readback, refusal, provenance, and NameCert rows. It sits after the Poincare disk metric packet of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the geodesic-flow carrier of \autoref{ch:concrete-instances-hyperbolic-geodesic-flow-namecert}, the visual boundary route of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the Busemann boundary packet of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, and the axis-shadow route of \autoref{ch:concrete-instances-hyperbolic-axis-shadow-namecert}. The projection is a BEDC-internal row discipline: it names a displayed axis read only after the boundary and Busemann evidence needed to keep the read non-selected has been opened.

\begin{definition}[Hyperbolic axis projection carrier]
\label{def:hyperbolic-axis-projection-carrier}
A $\HyperbolicAxisProjectionUp$ carrier is a finite $\BHist$ packet
$$
A=(D,M,G,V,B,S,X,R,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\PoincareDiskMetricUp$ disk-metric row that supplies the inspected disk coordinates and metric comparisons.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row over the same disk observations.
\item $G$ is the $\HyperbolicGeodesicFlowUp$ row naming the displayed oriented geodesic-flow segment whose axis projection is requested.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row exposing the boundary endpoints and visual-boundary readback used by the projection.
\item $B$ is the $\HyperbolicBusemannBoundaryUp$ row recording the Busemann-level boundary comparison attached to the same endpoints.
\item $S$ is the boundary-shadow row, read through the shadow or axis-shadow sibling route before an axis projection may be consumed.
\item $X$ is the axis-projection readback row, recording the finite projected axis window and its attachment to $D,M,G,V,B,S$.
\item $R$ is the refusal row excluding selected global axes, quotient ideal-boundary equality, completed hyperbolic geometry, host nearest-point projection, and physical observer-axis laws.
\item $F$ is the TasteGate-facing row requiring the future Lean $\HyperbolicAxisProjectionUp$ carrier to provide \texttt{Nontrivial} and \texttt{FieldFaithful} instances for the disk, metric, flow, boundary, Busemann, shadow, projection, refusal, replay, provenance, and naming fields.
\item $H$ records componentwise $\hsame$ transport for disk-metric, hyperbolic-metric, geodesic-flow, visual-boundary, Busemann-boundary, boundary-shadow, projection, refusal, TasteGate-facing, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from disk and geodesic-flow observations through $V,B,S$ to the projection row $X$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicAxisProjectionUp$, $\PoincareDiskMetricUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicFlowUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicBusemannBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicAxisProjectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,M,G,V,B,S,X,R,F,H,C,P,N$. It has no coordinate for selected axes, quotient boundary equality, completed disk geometry, host nearest-point projection, physical expansion dynamics, Lean verification, or a standard bridge.
\end{definition}

\begin{theorem}[Hyperbolic axis projection NameCert obligations]
\label{thm:hyperbolic-axis-projection-namecert-obligations}
For every accepted $\HyperbolicAxisProjectionUp$ carrier
$$
A=(D,M,G,V,B,S,X,R,F,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicAxisProjectionUp}$ surface consists exactly of Poincare disk metric admission through $D$, hyperbolic metric readback through $M$, geodesic-flow admission through $G$, visual-boundary exposure through $V$, Busemann-boundary exposure through $B$, boundary-shadow exposure through $S$, axis-projection readback through $X$, refusal exactness through $R$, TasteGate-facing nontriviality and field-faithfulness requirements through $F$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-axis-projection-carrier}. The metric and flow rows are $D,M,G$, the boundary rows are $V,B,S$, the projection row is $X$, and the structural rows are $R,F,H,C,P,N$.

The projection is not available from metric data alone. The disk and hyperbolic metric rows determine the inspected coordinates, and the geodesic-flow row names the oriented segment, but the carrier requires visual-boundary, Busemann-boundary, and boundary-shadow rows before $X$ may be consumed.

The refusal and TasteGate-facing rows keep the carrier separated. Row $R$ excludes selected global axes, quotient boundary equality, completed disk geometry, host nearest-point projection, and physical expansion dynamics, while $F$ records the future Lean requirement that the listed fields remain noncollapsed and faithful.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no row supplies an undisplayed axis or a host projection theorem, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic axis projection boundary dependency]
\label{thm:hyperbolic-axis-projection-boundary-dependency}
Every accepted $\HyperbolicAxisProjectionUp$ read that exports an axis-projection row must first display
$$
\PoincareDiskMetricUp,\quad \HyperbolicMetricUp,\quad \HyperbolicGeodesicFlowUp,\quad \HyperbolicVisualBoundaryUp,\quad \HyperbolicBusemannBoundaryUp
$$
together with the boundary-shadow row $S$. Consequently an axis-projection row cannot preserve its local $\NameCert$ surface when transported to a sibling packet lacking boundary-shadow and Busemann rows.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:hyperbolic-axis-projection-namecert-obligations}. They make $X$ downstream of $D,M,G,V,B,S$.

The visual-boundary row $V$ names the endpoints, while the Busemann-boundary row $B$ keeps the boundary comparison attached to the same endpoints. The shadow row $S$ then records the non-selected boundary-shadow witness needed before the axis projection can be read.

If a sibling lacks $B$ or $S$, the displayed route to $X$ is missing one of its carrier coordinates. Transport through $H,C,P,N$ can preserve an existing row, but it cannot create absent Busemann or boundary-shadow evidence.

Thus the accepted projection read remains local to the full axis-projection packet and does not become a host nearest-point projection or a boundary-only sibling certificate.
\end{proof}

\falsifiablePrediction{An accepted $\HyperbolicAxisProjectionUp$ read that preserves local NameCert evidence into a sibling lacking boundary-shadow and Busemann rows refutes the carrier surface of \autoref{def:hyperbolic-axis-projection-carrier}.}

\independenceWitness{hyperbolic-visual-boundary, hyperbolic-busemann-boundary, hyperbolic-geodesic-flow, poincare-disk-metric: $\HyperbolicAxisProjectionUp$ is not bijective to any listed sibling because each sibling lacks at least one obligation row among boundary-shadow evidence, Busemann comparison, geodesic-flow attachment, disk-metric provenance, axis-projection readback, refusal, TasteGate-facing field separation, transport, replay, provenance, and local naming.}

The future Lean TasteGate for $\HyperbolicAxisProjectionUp$ must provide \texttt{Nontrivial} and \texttt{FieldFaithful} instances that keep $D,M,G,V,B,S,X,R,F,H,C,P,N$ as separate fields before any carrier-alignment theorem can serve as a public TasteGate witness.

\closureat{HyperbolicAxisProjectionUp}{seedStr}

\begin{closurestatus}{\HyperbolicAxisProjectionUp}
  \constructivestory{A $\HyperbolicAxisProjectionUp$ packet is generated from PoincareDiskMetric rows, HyperbolicMetric reads, HyperbolicGeodesicFlow rows, HyperbolicVisualBoundary rows, HyperbolicBusemannBoundary rows, boundary-shadow evidence, axis-projection readback, refusal, TasteGate-facing field-separation requirements, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-axis-projection-carrier}, \autoref{thm:hyperbolic-axis-projection-namecert-obligations}, and \autoref{thm:hyperbolic-axis-projection-boundary-dependency} seed the finite route from disk metric and geodesic-flow rows through boundary-shadow and Busemann evidence to an axis-projection readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected global axes, quotient boundary equality, completed hyperbolic geometry, host nearest-point projection, physical observer-axis dynamics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, disk-metric visibility, geodesic-flow attachment, visual-boundary exposure, Busemann-boundary exposure, boundary-shadow dependency, axis-projection readback, refusal, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicAxisProjectionUp.}
\end{closurestatus}
