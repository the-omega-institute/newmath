\chapter{A Concrete Naming Certificate for $\NestedClosedIntervalSelectorUp$}
\label{ch:concrete-instances-nested-closed-interval-selector-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\NestedClosedIntervalSelectorUp$ packet records the finite selector route by which a displayed nested closed interval argument is read as regular Cauchy data before a terminal real-name seal is opened. It sits after the nested closed-interval surface of \autoref{ch:concrete-instances-closed-interval-nesting-namecert}, the dyadic endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the Cauchy naming surface of \autoref{ch:concrete-instances-cauchy-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the finite selector and handoff rows; it does not assert quotient real equality, a selected limit, countable choice, trichotomy, Heine--Borel compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Nested closed interval selector carrier]
\label{def:nested-closed-interval-selector-carrier}
A $\NestedClosedIntervalSelectorUp$ carrier is a finite $\BHist$ packet
$$
K=(I,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the nested closed-interval row, read through $\ClosedIntervalNestingUp$, carrying the inspected closed interval stages and successor-containment evidence.
\item $D$ is the $\DyadicRatCoreUp$ shrink ledger, recording endpoint arithmetic, displayed width budgets, and the finite stage bound at which the inspected closed interval is narrower than the requested tolerance.
\item $W$ is the $\StreamNameUp$ finite-window row exposing exactly the interval stages, endpoints, midpoint or endpoint selector values, and shrink witnesses inspected by the selector.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the selector values in $W$ after the shrink ledger $D$ has fixed the tolerance request.
\item $E$ is the terminal $\RealUp$ seal row available only after the nested interval row, shrink ledger, finite windows, and regular rational readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the nested interval row, shrink ledger, finite windows, readback, seal, replay, provenance, and local naming row.
\item $C$ records displayed $\Cont$ replay from a Cauchy-facing tolerance request through
$$
I,D,W,R \longmapsto E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\NestedClosedIntervalSelectorUp$, $\ClosedIntervalNestingUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\NestedClosedIntervalSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,D,W,R,E,H,C,P,N$. It has no coordinate for quotient real equality, a selected completed real, countable choice, trichotomy, Heine--Borel compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Nested closed interval selector NameCert obligations]
\label{thm:nested-closed-interval-selector-namecert-obligations}
For every accepted $\NestedClosedIntervalSelectorUp$ carrier
$$
K=(I,D,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\NestedClosedIntervalSelectorUp}$ surface consists exactly of carrier admission for the displayed packet, nested closed-interval exposure through $I$, dyadic shrink-ledger exactness through $D$, finite $\StreamNameUp$ window admission through $W$, $\RegSeqRatUp$ readback through $R$, terminal $\RealUp$ seal non-escape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:nested-closed-interval-selector-carrier}. The nested interval source, shrink ledger, finite windows, regular readback, terminal seal, and structural rows are exactly $I,D,W,R,E,H,C,P,N$.

The interval obligations are read first. The row $I$ exposes only inspected closed interval stages and successor containment, while $D$ attaches the dyadic endpoint arithmetic and width budget to those same stages.

The finite-window obligations are then forced by $W$. A selector value is visible only when its interval stage, endpoints, and shrink witness are all present in the displayed window, and the readback row $R$ is downstream of that window.

Finally, $E,H,C,P,N$ seal, transport, replay, source, and name the same route. Since no carrier coordinate stores quotient real equality, a selected completed real, countable choice, trichotomy, compactness, or ambient completeness, those principles cannot appear among the local NameCert obligations.
\end{proof}

\begin{theorem}[Nested closed interval selector Cauchy handoff]
\label{thm:nested-closed-interval-selector-cauchy-handoff}
Every Cauchy-facing or $\RealUp$-facing consumer of an accepted $\NestedClosedIntervalSelectorUp$ carrier
$$
K=(I,D,W,R,E,H,C,P,N)
$$
reaches a regular rational Cauchy read only through the finite route
$$
\ClosedIntervalNestingUp
\longmapsto
\DyadicRatCoreUp
\longmapsto
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
For each displayed tolerance request, the shrink ledger supplies an inspected stage whose closed interval is narrower than that tolerance, the window row exposes the selector value at that stage, and the readback row packages those values as the regular Cauchy handoff before the terminal seal is consumed.
\end{theorem}
\begin{proof}
Start with a displayed tolerance request. The shrink ledger $D$ gives a finite stage bound and a dyadic width inequality for the closed interval stage exposed by $I$.

Induct on the finite window prefix in $W$. The empty prefix exposes no selector value. In the successor step, the newly inspected stage carries its endpoints, containment witness, shrink witness, and selector value; successor containment keeps later inspected selector values inside the earlier displayed closed interval.

When two inspected stages are beyond the displayed bound for a tolerance, both selector values lie in a common closed interval whose dyadic width is below that tolerance. The row $R$ therefore reads the selector stream as regular rational Cauchy data without appealing to an unlisted limit object.

The terminal row $E$ is opened only after $R$. The transport, replay, provenance, and local naming rows preserve this order, so the handoff cannot export quotient equality, countable choice, trichotomy, Heine--Borel compactness, a selected completed real, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\NestedClosedIntervalSelectorUp$ Cauchy-facing read factors through a ClosedIntervalNesting row, DyadicRatCore shrink ledger, StreamName finite windows, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{closed-interval-nesting, dyadicratcore, streamname, regseqrat, cauchy, real: $\NestedClosedIntervalSelectorUp$ is not bijective to any listed sibling because it jointly carries the nested closed-interval row, dyadic shrink ledger, finite selector windows, regular Cauchy readback, terminal Real seal, transport, replay, provenance, and local naming.}

\closureat{NestedClosedIntervalSelectorUp}{seedStr}

\begin{closurestatus}{\NestedClosedIntervalSelectorUp}
  \constructivestory{A $\NestedClosedIntervalSelectorUp$ packet is generated from a ClosedIntervalNesting row, DyadicRatCore shrink ledger, StreamName finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nested-closed-interval-selector-carrier}, \autoref{thm:nested-closed-interval-selector-namecert-obligations}, and \autoref{thm:nested-closed-interval-selector-cauchy-handoff} seed the finite selector route from nested closed interval rows through shrink-ledger Cauchy readback and the terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected limits, countable choice, trichotomy, Heine--Borel compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nested closed-interval exposure, shrink-ledger exactness, finite-window induction, RegSeqRat readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.NestedClosedIntervalSelectorUp.}
\end{closurestatus}
