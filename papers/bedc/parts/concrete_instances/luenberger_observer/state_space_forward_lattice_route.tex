Every observer-error feedback read of an accepted $\LuenbergerObserverUp$ packet factors through the displayed $\StateSpaceModelUp$ row, $\ControlObservabilityUp$ row, observer-gain row, residual row, deterministic error-propagation ledger, and the existing anchors \autoref{ch:concrete-instances-statespacemodel-namecert} and \autoref{ch:concrete-instances-controlobservability-namecert} before it can be consumed by estimator or observer-controller routes. The lattice route exports the finite plant, observation, gain, residual, error, transport, replay, provenance, and local naming rows only; it exports no hidden trajectory semantics, stochastic covariance recursion, pole-placement theorem, or host convergence claim.
