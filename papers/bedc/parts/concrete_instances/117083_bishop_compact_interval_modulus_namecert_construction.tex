\chapter{A Concrete Naming Certificate for $\BishopCompactIntervalModulusUp$}
\label{ch:concrete-instances-bishop-compact-interval-modulus-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopCompactIntervalModulusUp$ packet records the finite compact-interval modulus route used by Real-and-Completion consumers. It sits between the closed bounded interval source of \autoref{ch:concrete-instances-closedboundedinterval-namecert}, the completion-modulus handoff of \autoref{ch:concrete-instances-bishopcompletionmodulus-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic threshold ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the displayed finite cover and modulus rows; it does not assert open-cover compactness, choose a subcover from an ambient family, quotient real names, or import ambient $\RealUp$ completeness.

\begin{definition}[Bishop compact interval modulus carrier]
\label{def:bishop-compact-interval-modulus-carrier}
A $\BishopCompactIntervalModulusUp$ carrier is a finite $\BHist$ packet
$$
K=(I,F,M,S,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\ClosedBoundedIntervalUp$ source row, including lower endpoint, upper endpoint, endpoint-order, containment, and terminal interval seal data.
\item $F$ is the dyadic finite-cover row over $I$, listing the inspected dyadic subintervals and their finite containment evidence.
\item $M$ is the compact interval modulus row, assigning each displayed precision request a finite cover depth and a dyadic threshold.
\item $S$ is the $\StreamNameUp$ window row used to inspect the endpoint names, finite cover cells, and modulus request.
\item $W$ is the StreamName window selected by $M$ at the requested depth.
\item $D$ is the $\DyadicRatCoreUp$ threshold ledger containing dyadic scale, endpoint comparison, finite-cover containment, and tolerance rows for $W$.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the selected window and dyadic ledger.
\item $E$ is the $\RealUp$ seal row reached only after $I,F,M,S,W,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the interval source, finite cover, modulus, window rows, dyadic ledger, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the interval source through the finite cover and modulus row to the selected StreamName window, then through the dyadic ledger and regular readback to the Real seal.
\item $P$ is the $\Pkg$ provenance over $\BishopCompactIntervalModulusUp$, $\ClosedBoundedIntervalUp$, $\BishopCompletionModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCompactIntervalModulusUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It contains no coordinate for an ambient open cover, selected subcover, host uniform-continuity theorem, quotient real equality, countable choice, or $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop compact interval modulus obligations]
\label{thm:bishop-compact-interval-modulus-obligations}
For every accepted $\BishopCompactIntervalModulusUp$ carrier $K=(I,F,M,S,W,D,R,E,H,C,P,N)$, the local $\NameCert_{\BishopCompactIntervalModulusUp}$ surface consists exactly of carrier admission for the displayed packet; closed bounded interval source exposure through $I$; dyadic finite-cover exposure through $F$; compact interval modulus readback through $M$; StreamName window selection through $S,W$; DyadicRatCore threshold compatibility through $D$; RegSeqRat readback through $R$; RealUp seal non-escape through $E$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-compact-interval-modulus-carrier}. The rows $I,F,M,S,W,D,R,E,H,C,P,N$ are the full public packet, so a consumer cannot introduce an unlisted open cover, selected subcover, quotient real representative, or completeness witness.

Read the analytic rows in order. The closed bounded interval source $I$ comes first, the finite dyadic cover $F$ is attached to that same source, and the modulus row $M$ selects the cover depth and threshold used for the requested precision.

The completion-facing rows are downstream. The selected window $W$ is read through $S$, the dyadic ledger $D$ records the same threshold and cover scale, and the regular readback $R$ is available before the seal $E$.

The structural rows close the NameCert surface. Componentwise transport, replay, provenance, and local naming preserve exactly the displayed route, so the listed obligations exhaust the carrier.
\end{proof}

\begin{theorem}[Bishop compact interval modulus completion handoff]
\label{thm:bishop-compact-interval-modulus-completion-handoff}
Every accepted $\BishopCompactIntervalModulusUp$ carrier hands a compact interval modulus to Real-and-Completion consumers through the finite route
$$
I,F,M \longmapsto S,W,D \longmapsto R \longmapsto E .
$$
The handoff first exposes the $\ClosedBoundedIntervalUp$ source and dyadic finite cover, then the compact interval modulus row, then the StreamName window, DyadicRatCore threshold ledger, RegSeqRat readback, and RealUp seal. It exports no ambient open-cover compactness, selected subcover, quotient real equality, countable-choice row, or Real completeness principle.
\end{theorem}
\begin{proof}
Begin with the source and cover rows. The accepted packet displays $I$ and $F$ before the modulus row can be consumed, so the interval and finite dyadic cover are fixed by carrier data rather than by an ambient compactness theorem.

Next read the modulus and window rows. The row $M$ selects the requested finite cover depth and threshold, $S$ names the finite observation schedule, and $W$ is the selected window attached to that request.

Finally read the completion-facing rows. The dyadic ledger $D$ keeps the threshold and cover scale visible, $R$ is the regular rational readback obtained from the displayed window, and $E$ is downstream from that readback. Since the carrier has no coordinate for the refused principles, the handoff exports only the finite route above.
\end{proof}

\falsifiablePrediction{An accepted $\BishopCompactIntervalModulusUp$ read fails if a Real-and-Completion consumer reaches a compact interval modulus without displaying the ClosedBoundedInterval source, dyadic finite cover, StreamName window, RegSeqRat readback, DyadicRatCore threshold ledger, RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}

\independenceWitness{closedboundedinterval, bishopcompletionmodulus, streamname, regseqrat, dyadicratcore, real: no listed sibling is bijective to $\BishopCompactIntervalModulusUp$ because this packet binds the closed interval source, finite dyadic cover, compact interval modulus row, completion window, threshold ledger, regular readback, Real seal, transport, replay, provenance, and local naming in one carrier.}

\closureat{BishopCompactIntervalModulusUp}{seedStr}

\begin{closurestatus}{\BishopCompactIntervalModulusUp}
  \constructivestory{A $\BishopCompactIntervalModulusUp$ packet is generated from a ClosedBoundedInterval source, a dyadic finite cover, a compact interval modulus row, StreamName windows, a DyadicRatCore threshold ledger, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-compact-interval-modulus-carrier}, \autoref{thm:bishop-compact-interval-modulus-obligations}, and \autoref{thm:bishop-compact-interval-modulus-completion-handoff} seed the compact-interval modulus route through the closed interval source, finite cover, completion window, and Real seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, quotient Real equality, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-cover exposure, compact modulus readback, StreamName and DyadicRatCore threshold compatibility, RegSeqRat handoff, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.BishopCompactIntervalModulusUp.}
\end{closurestatus}
