\chapter{A Concrete Naming Certificate for $\LocatedOrderIntervalUp$}
\label{ch:concrete-instances-located-order-interval-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\LocatedOrderIntervalUp$ packet names the finite order-facing interval data needed to compare located lower and upper boundary evidence before a real-completion consumer reads a terminal real seal. It sits between the interval carrier of \autoref{ch:concrete-instances-located-real-interval-namecert} and the two-sided cut surface of \autoref{ch:concrete-instances-lower-upper-real-cut-namecert}: the packet records lower-bound and upper-bound rows, a located-gap row, dyadic tolerance data, finite stream windows, regular-rational readback, and a terminal $\RealUp$ seal without importing trichotomy, a Dedekind construction of $\RealUp$, quotient real equality, selected representatives, countable choice, open-cover compactness, or $\RealUp$ completeness.

\begin{definition}[Located order interval carrier]
\label{def:located-order-interval-carrier}
A $\LocatedOrderIntervalUp$ carrier is a finite $\BHist$ packet
$$
O=(L,U,G,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower-bound row. It records the finite lower observations and the order-facing comparison requests visible to a completion route.
\item $U$ is the upper-bound row. It records the finite upper observations and the matching comparison requests.
\item $G$ is the located-gap row. It binds the lower and upper observations to one displayed apartness or overlap request before any real-facing seal is read.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row. It stores the dyadic endpoints, slack, radius, and comparison budget used by $L,U,G$.
\item $S$ is the $\StreamNameUp$ finite-window row. It schedules the visible lower and upper reads without selecting an infinite name.
\item $R$ is the $\RegSeqRatUp$ readback row. It translates the scheduled windows into regular-rational comparison evidence.
\item $E$ is the terminal $\RealUp$ seal. It is available only after $L,U,G,D,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the lower-bound row, upper-bound row, located-gap row, dyadic tolerance row, stream window, regular-rational readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for each order, interval, or real-completion request through $L,U,G,D,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LocatedOrderIntervalUp$, $\LocatedRealIntervalUp$, $\LowerUpperRealCutUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedOrderIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,U,G,D,S,R,E,H,C,P,N$. It has no coordinate for trichotomy, quotient real equality, selected representatives, countable choice, open-cover compactness, a Dedekind construction of $\RealUp$, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located order interval NameCert obligations]
\label{thm:located-order-interval-namecert-obligations}
For an accepted $\LocatedOrderIntervalUp$ carrier
$$
O=(L,U,G,D,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedOrderIntervalUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier exposure for the lower-bound row, upper-bound row, located-gap row, $\DyadicRatCoreUp$ tolerance row, $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ readback row, $\RealUp$ seal, componentwise transport, replay, provenance, and local naming row;
\item lower-bound stability, requiring every lower observation to be read through $L,D,S,R,H,C,P,N$ before a completion-facing consumer may inspect it;
\item upper-bound stability, requiring every upper observation to be read through $U,D,S,R,H,C,P,N$ by the same finite route;
\item located-gap exactness, requiring $G$ to bind the displayed lower and upper rows to one located request before the regular-rational readback and real seal are consumed;
\item tolerance-window compatibility, requiring $D$ and $S$ to expose the same finite comparison budget that $R$ reads back;
\item real-seal non-escape, requiring $E$ to remain downstream of $L,U,G,D,S,R$ and to export no trichotomy, quotient real equality, selected representative, countable-choice schedule, open-cover compactness witness, Dedekind construction of $\RealUp$, or ambient $\RealUp$ completeness.
\end{enumerate}
No local NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier exposure is projection from \autoref{def:located-order-interval-carrier}. The accepted packet displays exactly the lower-bound row, upper-bound row, located-gap row, dyadic tolerance row, stream window, regular-rational readback, real seal, and structural rows.

Lower-bound and upper-bound stability are componentwise. The row $D$ supplies the shared dyadic tolerance budget, $S$ schedules the finite windows, and $R$ reads the same windows back as regular-rational comparison evidence.

Located-gap exactness is read from $G$ against $L$ and $U$. The gap row binds the two order-facing ledgers to one displayed located request before any terminal real seal can be consumed.

The structural tail $H,C,P,N$ transports, replays, sources, and locally names the same packet. Since the carrier has no coordinate for trichotomy, quotient real equality, selected representatives, countable choice, open-cover compactness, a Dedekind construction of $\RealUp$, or ambient completeness, the listed obligation rows are exhaustive.
\end{proof}

\begin{theorem}[Located order interval real-completion route]
\label{thm:located-order-interval-real-completion-route}
Every completion-facing read of an accepted $\LocatedOrderIntervalUp$ carrier factors through the displayed route
$$
L,U,G \longmapsto D,S,R \longmapsto E,H,C,P,N .
$$
The route first exposes lower-bound, upper-bound, and located-gap evidence, then consumes the $\DyadicRatCoreUp$ tolerance row, the $\StreamNameUp$ finite window, and the $\RegSeqRatUp$ readback, and only then reaches the $\RealUp$ seal. Thus it gives Real and Completion consumers a finite comparison packet between located interval data and lower/upper real cut boundaries without asserting trichotomy, quotient real equality, selected representatives, countable choice, open-cover compactness, a Dedekind construction of $\RealUp$, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an accepted carrier $O=(L,U,G,D,S,R,E,H,C,P,N)$. The order-facing evidence is exactly the lower-bound row $L$, upper-bound row $U$, and located-gap row $G$.

Read the tolerance and name rows next. The dyadic row $D$ records the finite comparison budget, the stream row $S$ schedules the visible lower and upper windows, and the regular-rational row $R$ reads those windows back for the real-completion route.

Only after those rows are present may the terminal seal $E$ be consumed. The structural rows $H,C,P,N$ preserve the same finite packet under transport, replay, provenance, and local naming. No further carrier coordinate exists, so the route cannot export any of the excluded order, quotient, choice, compactness, or completeness principles.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedOrderIntervalUp$ completion-facing read factors through lower-bound, upper-bound, located-gap, dyadic tolerance, stream-window, regular-rational readback, $\RealUp$ seal, transport, replay, provenance, and local naming rows.}

\independenceWitness{locatedrealinterval, lowerupperrealcut, dyadicratcore, streamname, regseqrat, real: $\LocatedOrderIntervalUp$ is not bijective to any listed sibling because it jointly carries order-facing lower and upper boundary rows, a located-gap row, dyadic tolerance data, stream-window scheduling, regular-rational readback, a terminal real seal, transport, replay, provenance, and local naming.}

\closureat{LocatedOrderIntervalUp}{seedStr}

\begin{closurestatus}{\LocatedOrderIntervalUp}
  \constructivestory{A $\LocatedOrderIntervalUp$ packet is generated from lower-bound and upper-bound rows, a located-gap row, DyadicRatCore tolerance data, a StreamName finite window, RegSeqRat readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-order-interval-carrier}, \autoref{thm:located-order-interval-namecert-obligations}, and \autoref{thm:located-order-interval-real-completion-route} seed the finite order-facing comparison packet between located interval data and lower-upper real cut boundaries.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim trichotomy, a Dedekind construction of $\RealUp$, quotient real equality, selected representatives, countable choice, open-cover compactness, or $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier exposure, lower-bound stability, upper-bound stability, located-gap exactness, tolerance-window compatibility, and real-seal non-escape for BEDC.Derived.LocatedOrderIntervalUp.}
\end{closurestatus}
