\section{The certificate}

\begin{definition}[Compact source specification]
\label{def:compact-source-specification}
The source is a certified metric space plus a subset predicate over its carrier.
\end{definition}

\begin{definition}[Compact pattern specification]
\label{def:compact-pattern-specification}
The pattern is compact-subset formation by attaching locatedness and finite-net data for each rational precision.
\end{definition}

\begin{definition}[Compact classifier specification]
\label{def:compact-classifier-specification}
The classifier relates compact witnesses when their membership predicates agree by explicit bidirectional evidence under carrier sameness.
\end{definition}

\begin{definition}[Compact stability certificate]
\label{def:compact-stability-certificate}
The stability certificate consists of:
\begin{enumerate}
  \item membership stability under carrier classifier
  \item locatedness witness validity
  \item total-bounded finite-net validity
  \item monotonicity of precision witnesses where required
  \item stability under compact-witness classifier
\end{enumerate}
\end{definition}

\begin{theorem}[Compact net witness semantic name certificate]
\label{thm:compact-net-witness-semantic-namecert}
For unary histories $c$ and $\epsilon$, the compact-net witness predicate
$\mathsf{CompactNetWitness}(c,\epsilon,-)$ carries a semantic name certificate
with source, pattern, and ledger all equal to that concrete witness predicate,
and classifier $\hsame$:
$$
\mathsf{SemanticNameCert}
(\mathsf{CompactNetWitness}(c,\epsilon,-),
 \mathsf{CompactNetWitness}(c,\epsilon,-),
 \mathsf{CompactNetWitness}(c,\epsilon,-),
 \hsame).
$$
\end{theorem}
\begin{proof}
The carrier witness is the concrete history $c\cdot\epsilon$. Unary append
closure supplies its unary carrier, and the continuation ledger is
$c\cdot\epsilon=c\cdot\epsilon$. The classifier fields are the reflexive,
symmetric, and transitive laws of $\hsame$, with carrier transport by equality
case analysis. Pattern and ledger soundness are the identity projections
because all three semantic predicates are the compact-net witness predicate.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_semanticNameCert}

\begin{theorem}[Compact net witness prefix closure]
\label{thm:compact-net-witness-prefix-closed}
For unary histories $p,c,\epsilon,n$, if $n$ is a compact net witness obtained
by continuing center $c$ with precision $\epsilon$, then prefixing the center
and net by $p$ preserves the same precision witness:
$$
\mathsf{CompactNetWitness}(c,\epsilon,n)
\Longrightarrow
\mathsf{CompactNetWitness}(p \cdot c,\epsilon,p \cdot n).
$$
\end{theorem}
\begin{proof}
Unpack the witness into unary center, precision, and net histories together
with the continuation ledger $n=c\cdot\epsilon$. Unary closure gives unary
histories for $p\cdot c$ and $p\cdot n$. The prefixed continuation ledger is
the associativity equality $(p\cdot c)\cdot\epsilon=p\cdot(c\cdot\epsilon)$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_prefix\_closed}

\begin{theorem}[Compact net witness prefix exactness]
\label{thm:compact-net-witness-prefix-iff}
For histories $p,c,\epsilon,n$, a prefixed compact-net witness is equivalent
to unary prefix evidence together with the unprefixed witness:
$$
\mathsf{CompactNetWitness}(p \cdot c,\epsilon,p \cdot n)
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\wedge
\mathsf{CompactNetWitness}(c,\epsilon,n).
$$
\end{theorem}
\begin{proof}
For the forward direction, unpack the prefixed witness. Unary append
factorization recovers the unary prefix and the unary center and net histories,
while prefix cancellation on the continuation ledger recovers
$\mathsf{Cont}(c,\epsilon,n)$. For the reverse direction, apply prefix closure
to the unary prefix and the original compact-net witness.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_prefix\_iff}

\begin{theorem}[Compact net witness prefixed result determinacy]
\label{thm:compact-net-witness-prefixed-result-deterministic}
If two compact-net witnesses share the same prefixed center and precision,
$$
\mathsf{CompactNetWitness}(p\cdot c,\epsilon,p\cdot n)
\quad\text{and}\quad
\mathsf{CompactNetWitness}(p\cdot c,\epsilon,p\cdot n'),
$$
then the unprefixed net results are history-same:
$$
\hsame(n,n').
$$
\end{theorem}
\begin{proof}
The two compact-net witnesses contain continuation ledgers with the same source
$p\cdot c$ and precision $\epsilon$. Continuation determinacy identifies the
two prefixed results $p\cdot n$ and $p\cdot n'$, and left cancellation removes
the common prefix $p$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_prefixed\_result\_deterministic}

\begin{theorem}[Compact net witness empty net exactness]
\label{thm:compact-net-witness-empty-net-iff}
For histories $c$ and $\epsilon$, a compact-net witness with empty net is
equivalent to both the center and precision histories being empty:
$$
\mathsf{CompactNetWitness}(c,\epsilon,\emp)
\Longleftrightarrow
\hsame(c,\emp)\land\hsame(\epsilon,\emp).
$$
\end{theorem}
\begin{proof}
Unpack the compact-net witness and invert the continuation
$\Cont(c,\epsilon,\emp)$. This yields empty center and empty precision. The
reverse implication uses the empty unary carrier for both endpoints and the
canonical continuation from $\emp$ through $\emp$ to $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_empty\_net\_iff}

\begin{theorem}[Compact net witness endpoint cycle precision empty]
\label{thm:compact-net-witness-endpoint-cycle-precision-empty}
For histories $c,\epsilon,n$, a compact-net witness whose center and net are
history-same has empty precision:
$$
\mathsf{CompactNetWitness}(c,\epsilon,n)\Longrightarrow
\hsame(c,n)\Longrightarrow\hsame(\epsilon,\emp).
$$
\end{theorem}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_endpoint\_cycle\_precision\_empty}
\begin{proof}
Transport the witness continuation $\ContR(c,\epsilon,n)$ across
$\hsame(c,n)$ to obtain a self-extension of $c$. Right-unit uniqueness forces
the precision tail to be empty.
\end{proof}

\begin{theorem}[Compact empty-center nonempty precision shape]
\label{thm:compact-empty-center-nonempty-precision-shape}
For histories $net,\epsilon$, a compact-net witness with empty center and
nonempty precision has exactly one visible unary payload:
$$
\mathsf{CompactNetWitness}(\emp,\epsilon,net)\wedge\neg\hsame(\epsilon,\emp)
\Longleftrightarrow
\exists r:\mathsf{BHist}.\;
net=\Eone(r)\wedge \epsilon=\Eone(r)\wedge\mathsf{UnaryHistory}(r).
$$
\end{theorem}
\begin{proof}
The continuation field is $\Cont(\emp,\epsilon,net)$, so left-unit exactness
identifies $net$ with $\epsilon$. The nonempty precision premise removes the
empty case, and unary membership removes zero-headed precision. In the visible
case, the precision carrier exposes the unary payload and sameness fixes the
net to the same visible history. Conversely, the displayed visible payload
rebuilds the compact-net witness by the left-unit continuation.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_empty\_center\_nonempty\_precision\_shape\_iff}

\begin{theorem}[Compact net witness left e1 result cases]
\label{thm:compact-net-witness-left-e1-result-cases}
For histories $center,precision,net$, a compact-net witness from a visible
center to a visible net has only the empty precision case or a visible
precision tail:
$$
\mathsf{CompactNetWitness}(\Eone(center),precision,\Eone(net))
\Longrightarrow
\bigl(precision=\emp\land\mathsf{UnaryHistory}(center)\land
\hsame(center,net)\bigr)
\lor
\bigl(\exists precisionTail:\mathsf{BHist}.\;
precision=\Eone(precisionTail)\land
\mathsf{CompactNetWitness}(\Eone(center),precisionTail,net)\bigr).
$$
\end{theorem}
\begin{proof}
Unpack the compact-net witness to expose the unary carriers and the
continuation ledger. Inverting the ledger with visible result $\Eone(net)$
gives either empty precision or a visible precision tail. The empty branch
strips the visible constructor from the center-net equality and inverts the
center carrier to obtain $\mathsf{UnaryHistory}(center)$. The visible branch
inverts the precision and net carriers and repacks the tail continuation as a
compact-net witness.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_left\_e1\_result\_cases}

\begin{theorem}[Compact net witness left e1 result e1 precision tail]
\label{thm:compact-net-witness-left-e1-result-e1-precision-tail}
For histories $center,precisionTail,net$, a compact-net witness whose center,
precision, and result are all visible exposes the precision tail witness:
$$
\mathsf{CompactNetWitness}(\Eone(center),\Eone(precisionTail),\Eone(net))
\Longrightarrow
\mathsf{CompactNetWitness}(\Eone(center),precisionTail,net).
$$
\end{theorem}
\begin{proof}
Apply the visible-result cases theorem. The empty-precision branch would
identify $\Eone(precisionTail)$ with $\emp$, which constructor disjointness
excludes. The visible branch identifies the displayed precision tail with the
recovered tail, and the recovered tail witness is the required compact-net
witness.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_left\_e1\_result\_e1\_precision\_tail}

\begin{theorem}[Compact net witness right e1 result iff]
\label{thm:compact-net-witness-right-e1-result-iff}
For histories $center,precision,net$, visible precision and visible result
are equivalent to the tail compact-net witness:
$$
\mathsf{CompactNetWitness}(center,\Eone(precision),\Eone(net))
\Longleftrightarrow
\mathsf{CompactNetWitness}(center,precision,net).
$$
\end{theorem}
\begin{proof}
For the forward direction, unpack the compact-net witness. Unary inversion
removes the visible constructor from the precision and net carriers, and
right-step inversion removes it from the continuation ledger.

For the reverse direction, unary closure rebuilds the visible precision and
net carriers, while the one-step continuation rule lifts the tail ledger to
the visible result.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_right\_e1\_result\_iff}

\begin{theorem}[Compact net witness suffix exactness]
\label{thm:compact-net-witness-suffix-iff}
For histories $p,c,\epsilon,n$, a compact-net witness with the same right
suffix attached to precision and net is equivalent to unary suffix evidence
together with the unsuffixed witness:
$$
\mathsf{CompactNetWitness}(c,\epsilon\cdot p,n\cdot p)
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\wedge
\mathsf{CompactNetWitness}(c,\epsilon,n).
$$
\end{theorem}
\begin{proof}
In the forward direction, unary append factorization recovers the suffix and
the unsuffixed precision and net histories. The continuation ledger
$n\cdot p=c\cdot(\epsilon\cdot p)$ is associated to
$(c\cdot\epsilon)\cdot p$, and right cancellation by $p$ recovers
$n=c\cdot\epsilon$. In the reverse direction, unary append closure builds the
suffixed precision and net histories, while associativity supplies the
suffixed continuation ledger.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_suffix\_iff}

\begin{theorem}[Compact net witness visible context exactness]
\label{thm:compact-net-witness-visible-context-iff}
For histories $p,q,c,\epsilon,n$, a compact-net witness with visible left
context on the center and net and visible right context on the precision and
net is equivalent to the two unary context witnesses and the base compact-net
witness:
$$
\mathsf{CompactNetWitness}(p\cdot c,\epsilon\cdot q,(p\cdot n)\cdot q)
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\wedge
\mathsf{UnaryHistory}(q)\wedge
\mathsf{CompactNetWitness}(c,\epsilon,n).
$$
\end{theorem}
\begin{proof}
Peel the right suffix by Theorem~\ref{thm:compact-net-witness-suffix-iff}, then
peel the left prefix by Theorem~\ref{thm:compact-net-witness-prefix-iff}. The
reverse direction first applies prefix exactness and then reattaches the right
suffix by suffix exactness.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_visible\_context\_iff}
\begin{theorem}[Compact net witness visible-result context factorization]\label{thm:compact-net-witness-visible-result-context-factorizes}
For histories $p,q,c,\epsilon,n,result$, if
$\mathsf{CompactNetWitness}(p\cdot c,\epsilon\cdot q,result)$ and
$result\hsame(p\cdot n)\cdot q$, then
$\mathsf{UnaryHistory}(p)\wedge\mathsf{UnaryHistory}(q)\wedge\mathsf{CompactNetWitness}(c,\epsilon,n)$.
\end{theorem}
\begin{proof}
Transport the result carrier and continuation ledger along the displayed $\hsame$, then apply visible-context exactness.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_visible\_result\_context\_factorizes}
\begin{theorem}[Compact net witness visible-context result determinacy]
\label{thm:compact-net-witness-visible-context-result-deterministic}
For histories $p,q,c,\epsilon,n,n'$, two visible-context compact-net witnesses
with the same visible center and precision contexts determine the same central
net history:
$$
\mathsf{CompactNetWitness}(p\cdot c,\epsilon\cdot q,(p\cdot n)\cdot q)
\Longrightarrow
\mathsf{CompactNetWitness}(p\cdot c,\epsilon\cdot q,(p\cdot n')\cdot q)
\Longrightarrow
\hsame(n,n').
$$
\end{theorem}
\begin{proof}
Apply visible-context exactness to both witnesses. The recovered central
compact-net witnesses have the same center $c$ and precision $\epsilon$, so
continuation result determinacy identifies the two central nets.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_visible\_context\_result\_deterministic}

\begin{theorem}[Compact net witness visible-context precision determinacy]
\label{thm:compact-net-witness-visible-context-precision-deterministic}
For histories $p,q,c,\epsilon,\epsilon',n$, two visible-context compact-net
witnesses with the same visible center context and the same visible net context
determine the same central precision history:
$$
\mathsf{CompactNetWitness}(p\cdot c,\epsilon\cdot q,(p\cdot n)\cdot q)
\Longrightarrow
\mathsf{CompactNetWitness}(p\cdot c,\epsilon'\cdot q,(p\cdot n)\cdot q)
\Longrightarrow
\hsame(\epsilon,\epsilon').
$$
\end{theorem}
\begin{proof}
Apply visible-context exactness to both witnesses. The recovered central
compact-net witnesses have the same center $c$ and net $n$, so left
continuation cancellation identifies the two central precision histories.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_visible\_context\_precision\_deterministic}

\begin{theorem}[Compact net witness visible-context center determinacy]
\label{thm:compact-net-witness-visible-context-center-deterministic}
For histories $p,q,c,c',\epsilon,n$, two visible-context compact-net witnesses
with the same visible precision context and the same visible net context
determine the same central center history:
$$
\mathsf{CompactNetWitness}(p\cdot c,\epsilon\cdot q,(p\cdot n)\cdot q)
\Longrightarrow
\mathsf{CompactNetWitness}(p\cdot c',\epsilon\cdot q,(p\cdot n)\cdot q)
\Longrightarrow
\hsame(c,c').
$$
\end{theorem}
\begin{proof}
Apply visible-context exactness to both witnesses. The recovered central
compact-net witnesses have the same precision $\epsilon$ and net $n$, so right
continuation cancellation identifies the two central center histories.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_visible\_context\_center\_deterministic}

\begin{theorem}[Compact net witness common composite result prefix determinacy]
\label{thm:compact-net-witness-common-composite-result-prefix-deterministic}
For histories $p,p',c,\theta,out$, if two compact-net witnesses share the same
composite precision and result while their centers differ only by the visible
left prefix before the same $c$, then the two visible prefixes are $\hsame$:
$$
\mathsf{CompactNetWitness}(p\cdot c,\theta,out)
\Longrightarrow
\mathsf{CompactNetWitness}(p'\cdot c,\theta,out)
\Longrightarrow
p\hsame p'.
$$
\end{theorem}
\begin{proof}
Unpack the two compact-net witnesses. Their continuation ledgers have the same
precision $\theta$ and the same result $out$, so right cancellation identifies
$p\cdot c$ with $p'\cdot c$. Cancelling the common suffix $c$ gives
$p\hsame p'$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_common\_composite\_result\_prefix\_deterministic}

\begin{theorem}[Compact net witness empty-precision visible-context exactness]
\label{thm:compact-net-witness-empty-precision-visible-context-iff}
For histories $p,q,c,n$, a visible-context compact-net witness with empty
central precision is equivalent to unary evidence for the visible ledgers and
center, together with the center classifier for the net:
$$
\mathsf{CompactNetWitness}(p\cdot c,\emp\cdot q,(p\cdot n)\cdot q)
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\wedge
\mathsf{UnaryHistory}(q)\wedge
\mathsf{UnaryHistory}(c)\wedge
\hsame(n,c).
$$
\end{theorem}
\begin{proof}
Peel the visible prefix and suffix by
Theorem~\ref{thm:compact-net-witness-visible-context-iff}. The central witness
is $\mathsf{CompactNetWitness}(c,\emp,n)$, whose continuation field is exactly
the right-unit continuation classifier $\hsame(n,c)$; its carrier field gives
$\mathsf{UnaryHistory}(c)$.

Conversely, the unary center carrier and $\hsame(n,c)$ transport the center
carrier to the net and produce the right-unit continuation witness. This builds
$\mathsf{CompactNetWitness}(c,\emp,n)$, and visible-context exactness reattaches
the prefix $p$ and suffix $q$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_empty\_precision\_visible\_context\_iff}

\begin{theorem}[Compact visible result excludes empty center]
\label{thm:compact-net-witness-visible-context-e1-result-empty-center-absurd}
For histories $p,q,c,t$, a visible-context compact-net witness with empty
central precision and visible net $\Eone(t)$ cannot have an empty central
center:
$$
\mathsf{CompactNetWitness}(p\cdot c,\emp\cdot q,(p\cdot \Eone(t))\cdot q)
\Longrightarrow \hsame(c,\emp)\Longrightarrow\bot.
$$
\end{theorem}
\begin{proof}
Empty-precision visible-context exactness reads the central data back as
$\hsame(\Eone(t),c)$. If $c\hsame\emp$, transitivity identifies $\Eone(t)$
with $\emp$, contradicting BEDC constructor no-confusion.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_visible\_context\_e1\_result\_empty\_center\_absurd}

\begin{theorem}[Compact visible boundary excludes empty center]
\label{thm:compact-net-witness-visible-context-visible-result-empty-center-absurd}
If the net in a visible-context empty-precision compact witness is $\hsame$ to either $\Ezero(t)$ or $\Eone(t)$, then the center cannot be empty.
\end{theorem}
\begin{proof}
Empty-precision visible-context exactness identifies the net with the center; an empty center would identify the visible result with $\emp$, contradicting constructor no-confusion.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_visible\_context\_visible\_result\_empty\_center\_absurd}

\begin{theorem}[Compact net witness composite result determinacy]
\label{thm:compact-net-witness-composite-result-deterministic}
For histories $c,\epsilon,\eta,n,n',m,m'$, if $n$ and $n'$ are compact net
witnesses from the same center $c$ and precision $\epsilon$, and $m$ and $m'$
continue them with the same refinement precision $\eta$, then the refined nets
are the same BEDC history:
$$
\mathsf{CompactNetWitness}(c,\epsilon,n)
\Longrightarrow
\mathsf{CompactNetWitness}(n,\eta,m)
\Longrightarrow
\mathsf{CompactNetWitness}(c,\epsilon,n')
\Longrightarrow
\mathsf{CompactNetWitness}(n',\eta,m')
\Longrightarrow
\hsame(m,m').
$$
\end{theorem}
\begin{proof}
Unpack the first and third witnesses into continuation ledgers
$c\cdot\epsilon=n$ and $c\cdot\epsilon=n'$. Continuation determinacy gives
$\hsame(n,n')$. The second and fourth witnesses give ledgers
$n\cdot\eta=m$ and $n'\cdot\eta=m'$. Continuation compatibility with
$\hsame(n,n')$ and reflexivity of $\eta$ compares these second-step ledgers and
yields $\hsame(m,m')$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_composite\_result\_deterministic}

\begin{theorem}[Compact net witness composite middle continuation determinacy]
\label{thm:compact-net-witness-composite-middle-cont-deterministic}
For histories $c,\epsilon,\eta,n,n',m$, if
$\mathsf{CompactNetWitness}(c,\epsilon,n)$,
$\mathsf{CompactNetWitness}(n,\eta,m)$,
$\mathsf{CompactNetWitness}(c,\epsilon,n')$, and
$\mathsf{CompactNetWitness}(n',\eta,m)$ hold, then
$$
\ContR(c,\epsilon,n)\land \ContR(c,\epsilon,n')\land \hsame(n,n').
$$
\end{theorem}
\begin{proof}
The first and third compact-net witnesses expose the concrete first-stage
continuation ledgers $\ContR(c,\epsilon,n)$ and $\ContR(c,\epsilon,n')$.
Continuation determinacy compares those two ledgers with the same source and
precision, giving $\hsame(n,n')$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_composite\_middle\_cont\_deterministic}

\begin{theorem}[Compact net witness composite center determinacy]
\label{thm:compact-net-witness-composite-center-deterministic}
For histories $c,c',\epsilon,\eta,n,n',m$, if the first-stage net witnesses
share precision $\epsilon$ and the second-stage refinements share extra
precision $\eta$ and final net $m$, then their centers are the same BEDC
history:
$$
\mathsf{CompactNetWitness}(c,\epsilon,n)
\Longrightarrow
\mathsf{CompactNetWitness}(n,\eta,m)
\Longrightarrow
\mathsf{CompactNetWitness}(c',\epsilon,n')
\Longrightarrow
\mathsf{CompactNetWitness}(n',\eta,m)
\Longrightarrow
\hsame(c,c').
$$
\end{theorem}
\begin{proof}
Unpack the second-stage witnesses into continuation ledgers
$\ContR(n,\eta,m)$ and $\ContR(n',\eta,m)$. Continuation right cancellation
gives $\hsame(n,n')$. The first-stage witnesses provide
$\ContR(c,\epsilon,n)$ and $\ContR(c',\epsilon,n')$; cancellation over the
common suffix $\epsilon$ and the already identified net histories yields
$\hsame(c,c')$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_composite\_center\_deterministic}

\begin{theorem}[Compact net witness common precision center transport]
\label{thm:compact-net-witness-common-precision-center-transport}
For histories $c,c',\epsilon,n,n'$, if
$\mathsf{CompactNetWitness}(c,\epsilon,n)$,
$\mathsf{CompactNetWitness}(c',\epsilon,n')$, and $\hsame(n,n')$ hold, then
the second witness transports to the first net and the centers are the same
BEDC history:
$$
\ContR(c',\epsilon,n)\land \hsame(c,c').
$$
\end{theorem}
\begin{proof}
The second compact-net witness exposes $\ContR(c',\epsilon,n')$; endpoint
transport along $\hsame(n,n')$ gives $\ContR(c',\epsilon,n)$. The two
first-stage continuation ledgers now share the same precision and history-same
results, so common-suffix cancellation identifies $c$ and $c'$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_common\_precision\_center\_transport}

\begin{theorem}[Compact net witness common center precision transport]
\label{thm:compact-net-witness-common-center-precision-transport}
For histories $c,\epsilon,\epsilon',n,n'$, if
$\mathsf{CompactNetWitness}(c,\epsilon,n)$,
$\mathsf{CompactNetWitness}(c,\epsilon',n')$, and $\hsame(n,n')$ hold, then
the second witness transports to the first net and the precisions are the same
BEDC history:
$$
\ContR(c,\epsilon',n)\land \hsame(\epsilon,\epsilon').
$$
\end{theorem}
\begin{proof}
The second compact-net witness exposes $\ContR(c,\epsilon',n')$; endpoint
transport along $\hsame(n,n')$ gives $\ContR(c,\epsilon',n)$. Since the center
is common and the endpoints are history-same, compact-net precision
determinacy identifies $\epsilon$ and $\epsilon'$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_common\_center\_precision\_transport}

\begin{theorem}[Compact net witness precision determinacy under endpoint sameness]
\label{thm:compact-net-witness-precision-hsame-deterministic}
For histories $c,c',\epsilon,\epsilon',n,n'$, if $\hsame(c,c')$,
$\hsame(n,n')$, $\mathsf{CompactNetWitness}(c,\epsilon,n)$, and
$\mathsf{CompactNetWitness}(c',\epsilon',n')$ hold, then
$$
\hsame(\epsilon,\epsilon').
$$
\end{theorem}
\begin{proof}
Transport the two continuation ledgers to common center and net histories using
the endpoint $\hsame$ hypotheses. The two witnesses then have the same left
context and result, so continuation left cancellation identifies the precision
histories.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_precision\_hsame\_deterministic}

\begin{theorem}[Compact net witness composite precision continuation determinacy]
\label{thm:compact-net-witness-composite-precision-cont-deterministic}
For histories $c,\epsilon,\epsilon',\eta,n,n',m$, if
$\mathsf{CompactNetWitness}(c,\epsilon,n)$ and
$\mathsf{CompactNetWitness}(c,\epsilon',n')$ hold and both $n$ and $n'$ refine
through the same extra precision $\eta$ to the same history $m$, then
$$
\hsame(\epsilon,\epsilon').
$$
\end{theorem}
\begin{proof}
Continuation right cancellation on the two refinement ledgers identifies
$n$ and $n'$. With the center already common, compact-net precision
determinacy under endpoint sameness identifies $\epsilon$ and $\epsilon'$.
\end{proof}
\leanchecked{BEDC.Derived.CompactUp.CompactNetWitness\_composite\_precision\_cont\_deterministic}

\begin{corollary}[Compact source specification route determinacy]
\label{cor:compact-source-specification-route-determinacy}
Fix a consumer-facing $\CompactUp$ use whose source side is the compact source specification of \autoref{def:compact-source-specification}. For histories $p,c,\epsilon,n,n'$, suppose the displayed local rows are the same prefixed center, precision, continuation route, and compact-net result shape:
$$
\mathsf{CompactNetWitness}(p\cdot c,\epsilon,p\cdot n)
\quad\text{and}\quad
\mathsf{CompactNetWitness}(p\cdot c,\epsilon,p\cdot n').
$$
Then the unprefixed result rows selected by the two consumer uses are the same BEDC history:
$$
\hsame(n,n').
$$
Consequently every accepted consumer-facing use of the compact route, compact-net normal-form read, or exactness decision over these fixed source rows factors through the same finite route: the compact source row, the prefixed center row $p\cdot c$, the precision row $\epsilon$, the compact-net witness row, the continuation ledger $\ContR(p\cdot c,\epsilon,-)$, the result readback row $p\cdot n$, and the classifier row $\hsame$. There is no alternate hidden coordinate, second compact-net result source, or duplicate classifier source outside that displayed packet.
\end{corollary}
\begin{proof}
The compact source specification of \autoref{def:compact-source-specification} supplies only the certified metric source together with the subset predicate over its carrier. In this certificate, a consumer-facing compact-net use therefore reaches the compact source through the displayed compact-net witness row and not through an additional selector, hidden coordinate, or external classifier source.

The two displayed consumer uses have the same prefixed center $p\cdot c$ and the same precision $\epsilon$. Their only result rows are $p\cdot n$ and $p\cdot n'$, each carried by a compact-net witness over those fixed source rows. Thus they are exactly in the situation of \autoref{thm:compact-net-witness-prefixed-result-deterministic}. Applying that theorem gives $\hsame(n,n')$ after cancellation of the common prefix $p$.

It remains to account for the named consumer readings. The route read is the prefixed center and precision route just used. A compact-net normal-form read is a readback of the same result row, and an exactness decision named in the chapter is a decision about the same compact-net witness packet. Since the source specification adds no further coordinate and the determinacy theorem has already identified any competing unprefixed result, every accepted use over the fixed rows factors through the listed finite route and the same $\hsame$ classifier row.
\end{proof}
