\closureat{CompactUp}{publicStr}[BEDC.Derived.CompactUp.CompactNetWitness\_semanticNameCert]
\closureat{CompactUp}{bridgedStr}[BEDC.Derived.CompactUp.CompactNetWitness\_semanticNameCert]

\begin{closurestatus}{\CompactUp}
  \constructivestory{A compact witness in this scoped package is a finite refinement record over unary histories: a subset history, locatedness ledger, finite-net ledger, intermediate ledger, and compactness ledger are connected by displayed continuations, and the simpler net witness records a center, precision, and net with the net obtained by appending the precision to the center. The classifier is endpoint history-sameness on the exposed net or refinement endpoints, with determinacy rows showing that shared centers, precisions, composites, or final ledgers force the matching fields to be $\hsame$. The source is the BEDC compact-net or refinement predicate, the pattern records finite and located refinement chains, prefix closure, endpoint exactness, empty-net and nonempty-precision shapes, and composite refinement determinacy, and the ledger keeps every located, finite, intermediate, compact, precision, and net continuation visible. Because a unary center and precision always build the witness net $center\cdot precision$, $\hsame$ supplies the classifier equivalence, and the refinement predicates transport along deterministic continuation equalities, the five name-certificate obligations hold for the semantic compact-net certificate. A history $n$ lies in the signature gap of a $\CompactUp$ token $p$ when the source-domain accepts it as a compact net or final refinement witness, the signature relation pairs it with an answer net and evidence ledger, and token introduction names that answer as $p$; unfolded, $n$ is unary, is connected to its center and precision by the displayed continuation, and has the refinement endpoint data encoded by $p$. No standard bridge is claimed, so open-cover compactness, sequential compactness, products, and function-space compactness stay outside the closed surface. The chapter registers this finite-refinement net construction under the name $\CompactUp$; compactness is a ledger of explicit unary refinements, not an imported topological compactness axiom or host-space object.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{The compact-net-witness semantic name certificate, with located/finite refinement chain, endpoint classifier, and refinement-witness ledger over the BEDC source.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.CompactUp.CompactNetWitness\_semanticNameCert}
  \bridgestatus{none}
  \notclaimed{General theory of compact spaces: arbitrary open-cover compactness, sequential compactness in non-metric settings, products of compact spaces (Tychonoff), or compactness of function spaces.}
  \upgradepath{Mature closure requires downstream examples or completeness-style consumers of the explicit compact-net witness surface.}
\end{closurestatus}
