\chapter{A Concrete Naming Certificate for $\CauchyCondensationIntegralBoundUp$}
\label{ch:concrete-instances-cauchy-condensation-integral-bound-namecert}
\origin{human}

Following Hardy, \emph{Divergent Series}, the $\CauchyCondensationIntegralBoundUp$ packet records the finite integral-bound ledger that sits between the theorem-level condensation surface of \autoref{ch:concrete-instances-cauchy-condensation-theorem-namecert} and the bridge packet of \autoref{ch:concrete-instances-cauchy-condensation-integral-bridge-namecert}. It also shares the dyadic arithmetic, regular rational readback, and terminal real-name corridor of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The carrier names monotone positive term windows and dyadic integral-window majorants as BEDC rows before any Real or Completion consumer reads a bound.

\begin{definition}[CauchyCondensationIntegralBound carrier]
\label{def:cauchy-condensation-integral-bound-carrier}
A $\CauchyCondensationIntegralBoundUp$ carrier is a finite $\BHist$ packet
$$
J=(M,D,I,B,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the monotone positive term ledger, recording only inspected source windows, positivity witnesses, and monotonicity comparisons for the displayed tail.
\item $D$ is the $\DyadicRatCoreUp$ dyadic scale row, carrying powers-of-two windows, endpoint comparisons, and dyadic slack data.
\item $I$ is the integral-window ledger, recording finite lower and upper interval cells attached to the displayed dyadic scale.
\item $B$ is the dyadic integral-window majorant row, bounding each displayed condensation block by the corresponding integral-window data.
\item $W$ is the finite readback schedule for Real and Completion consumers.
\item $R$ is the $\RegSeqRatUp$ tail readback row obtained from $M,D,I,B,W$.
\item $E$ is the terminal $\RealUp$ seal row reached only after the majorant and regular-readback rows are visible.
\item $H$ records componentwise $\hsame$ transport for monotone terms, dyadic scales, integral windows, majorants, schedules, readback rows, seal rows, replay rows, provenance rows, and local names.
\item $C$ records displayed $\Cont$ replay from $M,D,I$ to $B$, from $B,W$ to $R$, and from $R$ to the seal row $E$.
\item $P$ is the $\Pkg$ provenance over $\CauchyCondensationIntegralBoundUp$, $\CauchyCondensationTheoremUp$, $\CauchyCondensationIntegralBridgeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCondensationIntegralBoundUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,D,I,B,W,R,E,H,C,P,N$. It has no coordinate for an ambient improper integral, selected infinite sum, quotient series equality, countable choice, trichotomy, host real equality, or completeness of $\RealUp$.
\end{definition}
\leanchecked{BEDC.Derived.CauchyCondensationIntegralBoundUp.CauchyCondensationIntegralBoundTasteGate\_single\_carrier\_alignment}

\begin{theorem}[CauchyCondensationIntegralBound NameCert obligations]
\label{thm:cauchy-condensation-integral-bound-namecert-obligations}
For every accepted $\CauchyCondensationIntegralBoundUp$ carrier $J=(M,D,I,B,W,R,E,H,C,P,N)$, the local $\NameCert_{\CauchyCondensationIntegralBoundUp}$ surface consists exactly of carrier admission for the displayed eleven-row packet; monotone positive term exposure through $M$; dyadic scale exposure through $D$; integral-window exposure through $I$; dyadic majorant exposure through $B$; finite schedule exposure through $W$; $\RegSeqRatUp$ readback through $R$; $\RealUp$ sealing through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier rows from \autoref{def:cauchy-condensation-integral-bound-carrier}. The packet exposes only monotone term windows, dyadic scale data, integral windows, majorant rows, finite schedules, regular readbacks, Real seals, and the structural rows.

Read the analytic handoff through the adjacent condensation chapters. The theorem packet supplies the monotone dyadic block route, while the integral bridge supplies the finite comparison corridor between condensation tails and integral windows.

The local $\NameCert$ row admits exactly those exposed rows and their structural transports. Since the carrier has no coordinate for an ambient integral, selected sum, quotient equality, countable choice, trichotomy, host equality, or Real completeness, the listed obligations exhaust the certificate surface.
\end{proof}

\begin{theorem}[CauchyCondensationIntegralBound dyadic majorant]
\label{thm:cauchy-condensation-integral-bound-dyadic-majorant}
Every accepted $\CauchyCondensationIntegralBoundUp$ carrier sends a monotone positive tail to a dyadic integral-window majorant only through the finite route
$$
M,D,I\longmapsto B\longmapsto W\longmapsto R.
$$
The route is anchored by $\CauchyCondensationTheoremUp$ on the condensation side and by $\CauchyCondensationIntegralBridgeUp$ on the integral-window side, and it exports no selected subseries, host integral object, quotient real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the source rows $M,D,I$. The monotone positive term ledger and dyadic scale rows determine which finite integral windows may be inspected.

Use the bridge route of \autoref{ch:concrete-instances-cauchy-condensation-integral-bridge-namecert}. It matches the condensation-side dyadic tail to the integral-window side before any regular rational readback is consumed.

The majorant row $B$ is then read through the finite schedule $W$ into $R$. Componentwise transport and continuation replay preserve the same route, so the majorant cannot be replaced by an unlisted selected sum, ambient integral, quotient equality, or completeness row.
\end{proof}

\begin{theorem}[CauchyCondensationIntegralBound Real seal]
\label{thm:cauchy-condensation-integral-bound-real-seal}
Every Real-facing read of an accepted $\CauchyCondensationIntegralBoundUp$ carrier reaches $E$ only after the displayed route
$$
M,D,I,B,W,R,E
$$
has exposed the monotone positive term ledger, dyadic integral-window majorant, $\RegSeqRatUp$ readback, and structural rows $H,C,P,N$. Thus the seal records a finite integral-bound certificate, not an ambient summability theorem or a completeness assertion for $\RealUp$.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchy-condensation-integral-bound-dyadic-majorant}. It places every bound read behind $M,D,I,B,W,R$ and keeps the dyadic and integral-window rows visible.

The terminal seal row $E$ is downstream of the regular readback row. A Real consumer therefore receives the bound only after the regular rational handoff is present.

Finally inspect the carrier exactness from \autoref{def:cauchy-condensation-integral-bound-carrier}. The structural rows transport, replay, source, and name the same finite route, while no coordinate supplies ambient integration, selected infinite sums, quotient equality, countable choice, trichotomy, host equality, or Real completeness.
\end{proof}

\falsifiablePrediction{An accepted $\CauchyCondensationIntegralBoundUp$ consumer that reaches a $\RealUp$ seal without first reading the monotone positive term row, dyadic scale row, integral-window row, dyadic majorant row, finite readback schedule, and $\RegSeqRatUp$ handoff refutes the carrier surface.}

\independenceWitness{cauchy-condensation-theorem, cauchy-condensation-integral-bridge, dyadicratcore, regseqrat, real: no listed sibling supplies the whole integral-bound packet $J=(M,D,I,B,W,R,E,H,C,P,N)$, because each sibling contributes only a theorem row, bridge row, dyadic ledger, regular readback, or terminal seal while this chapter binds them into one finite majorant certificate.}

\closureat{CauchyCondensationIntegralBoundUp}{seedStr}

\begin{closurestatus}{\CauchyCondensationIntegralBoundUp}
  \constructivestory{A $\CauchyCondensationIntegralBoundUp$ packet is generated from monotone positive term windows, DyadicRatCore scale rows, finite integral-window rows, dyadic majorant rows, readback schedules, RegSeqRat tail readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-condensation-integral-bound-carrier}, \autoref{thm:cauchy-condensation-integral-bound-namecert-obligations}, \autoref{thm:cauchy-condensation-integral-bound-dyadic-majorant}, and \autoref{thm:cauchy-condensation-integral-bound-real-seal} seed the finite integral-bound ledger over monotone tails, dyadic windows, RegSeqRat readback, and the Real seal.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.CauchyCondensationIntegralBoundUp.CauchyCondensationIntegralBoundTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient improper integration, selected infinite sums, quotient series equality, countable choice, trichotomy, host real equality, Real completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, monotone positive term exposure, dyadic integral-window majorant exposure, regular readback exactness, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyCondensationIntegralBoundUp.}
\end{closurestatus}
