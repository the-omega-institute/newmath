\chapter{A Concrete Naming Certificate for $\RealOrderWindowUp$}
\label{ch:concrete-instances-real-order-window-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, $\RealOrderWindowUp$ records a finite comparison-window certificate over \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-archimedean-window-namecert}. It is a local order-read packet for a displayed $\RealUp$ name, not a trichotomy principle, quotient stream equality test, selected supremum, host real equality, or ambient completeness theorem.

\begin{definition}[Real order window carrier]
\label{def:real-order-window-carrier}
A $\RealOrderWindowUp$ carrier is a finite $\BHist$ packet
$$
R=(X,S,Q,D,L,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the sealed $\RealUp$ row whose order-facing read is being inspected.
\item $S$ is the finite $\StreamNameUp$ window row attached to the inspected comparison request.
\item $Q$ is the $\RegSeqRatUp$ readback row for the same visible window.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the dyadic comparison radius and endpoint arithmetic.
\item $L$ is the lower comparison row exposed through $D,S,Q$.
\item $U$ is the upper comparison row exposed through the same $D,S,Q$ route.
\item $H$ records componentwise $\hsame$ transport for $X,S,Q,D,L,U,C,P,N$.
\item $C$ records displayed $\Cont$ replay from the finite comparison request through the readback, tolerance, lower, upper, seal, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\RealOrderWindowUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealArchimedeanWindowUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealOrderWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,S,Q,D,L,U,H,C,P,N$. It has no coordinate for trichotomy, host real equality, quotient stream equality, selected suprema, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real order window NameCert obligations]
\label{thm:real-order-window-namecert-obligations}
For every accepted $\RealOrderWindowUp$ carrier $R=(X,S,Q,D,L,U,H,C,P,N)$, the local $\NameCert_{\RealOrderWindowUp}$ surface consists exactly of carrier admission, sealed $\RealUp$ exposure through $X$, finite $\StreamNameUp$ window exposure through $S$, $\RegSeqRatUp$ readback through $Q$, $\DyadicRatCoreUp$ tolerance exactness through $D$, lower and upper comparison exposure through $L,U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-order-window-carrier}. The accepted packet exposes the real seal $X$, finite observation window $S$, regular readback $Q$, dyadic tolerance ledger $D$, and the two comparison rows $L,U$ before any structural row is consumed.

The lower and upper rows are not independent order primitives. They are read only after $D,S,Q$ have fixed the dyadic tolerance, inspected window, and regular rational readback for the same sealed real row.

The support rows $H,C,P,N$ transport, replay, source, and name exactly that displayed route. Since the carrier contains no coordinate for trichotomy, host real equality, quotient stream equality, selected suprema, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Real order window Archimedean handoff]
\label{thm:real-order-window-archimedean-handoff}
Every order-facing $\RealUp$ consumer of an accepted $\RealOrderWindowUp$ packet must pass through the $\RealArchimedeanWindowUp$ surface of \autoref{ch:concrete-instances-real-archimedean-window-namecert} before it uses the lower and upper comparison rows $L,U$. The exported order read is therefore the finite route
$$
\RealUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealArchimedeanWindowUp \longmapsto \RealOrderWindowUp,
$$
and it exports no trichotomy, host real equality, quotient stream equality, selected supremum, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier rows $X,S,Q,D,L,U,H,C,P,N$. The rows $L,U$ are downstream from the same dyadic tolerance ledger, finite stream window, and regular rational readback that expose the sealed real row $X$.

Now read the Archimedean window cited in $P$. Its bracketing surface supplies the finite lower-and-upper window through $\RealArchimedeanWindowUp$ before a consumer may interpret $L,U$ as order-facing comparison rows.

The remaining rows $H,C,P,N$ preserve the same handoff. Because neither the carrier nor the Archimedean handoff contains a coordinate for trichotomy, quotient stream equality, selected suprema, host real equality, or ambient completeness, the order consumer receives only the finite comparison-window certificate.
\end{proof}

\closureat{RealOrderWindowUp}{seedStr}

\begin{closurestatus}{\RealOrderWindowUp}
  \constructivestory{A $\RealOrderWindowUp$ packet is generated from a sealed Real row, a finite StreamName window, RegSeqRat readback, a DyadicRatCore tolerance ledger, lower and upper comparison rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-order-window-carrier}, \autoref{thm:real-order-window-namecert-obligations}, and \autoref{thm:real-order-window-archimedean-handoff} seed the finite comparison-window surface for order-facing Real reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim trichotomy, host real equality, quotient stream equality, selected suprema, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint exposure, dyadic tolerance exactness, Real seal non-escape, Archimedean handoff, transport, replay, provenance, and local naming for BEDC.Derived.RealOrderWindowUp.}
\end{closurestatus}
