\chapter{A Concrete Naming Certificate for $\KreinMilmanFiniteExtremeUp$}
\label{ch:concrete-instances-krein-milman-finite-extreme-namecert}
\origin{human}

Following Krein and Milman, the $\KreinMilmanFiniteExtremeUp$ packet records the finite extreme-point witness route that a convex-analysis consumer may inspect before any ambient locally convex compactness theorem is available. It sits over the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the convex-set interface of \autoref{ch:concrete-instances-convexset-namecert}, the scalar $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the weak functional-window surface of \autoref{ch:concrete-instances-weak-topology-namecert}, the operator-norm row of \autoref{ch:concrete-instances-operator-norm-namecert}, and the finite weak-star window of \autoref{ch:concrete-instances-banach-alaoglu-namecert}. The chapter names only a finite exposed-face and candidate-extreme certificate; it does not assert full locally convex duality, selected supporting functionals, unrestricted compactness, quotient convex bodies, or ambient $\RealUp$ completeness.

\begin{definition}[Krein--Milman finite extreme-point carrier]
\label{def:krein-milman-finite-extreme-carrier}
A $\KreinMilmanFiniteExtremeUp$ carrier is a finite $\BHist$ packet
$$
K=(M,V,X,F,E,S,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\CompactMetricUp$ row carrying the finite compact-window data through which the candidate convex body is inspected.
\item $V$ is the ambient vector or functional-analysis row, read through the weak-topology and operator-norm sibling packets.
\item $X$ is the $\ConvexSetUp$ row for the displayed convex subset, including the affine-combination and transport data available to the packet.
\item $F$ is the finite exposed-face ledger, listing the exposed faces, scalar thresholds, comparison budgets, and source probes that the certificate actually opens.
\item $E$ is the candidate extreme-point witness row, recording the displayed points and finite refusal rows for nontrivial convex decompositions inside the opened face ledger.
\item $S$ is the separating-functional ledger. Each functional is read only through finite weak-topology, operator-norm, and scalar $\RealUp$ observation rows.
\item $W$ records the scalar and metric windows used by $F,E,S$, including finite $\RealUp$ readbacks for values, gaps, and tolerance comparisons.
\item $H$ records componentwise $\hsame$ transport for the compact metric row, ambient row, convex-set row, exposed-face ledger, candidate extreme witnesses, separating-functional ledger, scalar windows, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact and convex reads through exposed-face and separating-functional ledgers to the candidate extreme witness rows.
\item $P$ is the $\Pkg$ provenance over $\KreinMilmanFiniteExtremeUp$, $\CompactMetricUp$, $\ConvexSetUp$, $\RealUp$, $\WeakTopologyUp$, $\OperatorNormUp$, $\BanachAlaogluUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KreinMilmanFiniteExtremeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,V,X,F,E,S,W,H,C,P,N$. It has no coordinate for a host choice of all extreme points, a completed dual space, full locally convex separation, unrestricted compactness, quotient convex-body equality, selected supporting functionals outside $S$, or ambient real completeness.
\end{definition}

\begin{theorem}[Krein--Milman finite extreme-point NameCert obligations]
\label{thm:krein-milman-finite-extreme-namecert-obligations}
For every accepted $\KreinMilmanFiniteExtremeUp$ carrier
$$
K=(M,V,X,F,E,S,W,H,C,P,N),
$$
the local $\NameCert_{\KreinMilmanFiniteExtremeUp}$ surface consists exactly of compact-window admission for $M$, ambient weak functional admission for $V$, convex-set admission for $X$, exposed-face ledger visibility for $F$, candidate extreme-point witness admission for $E$, separating-functional ledger visibility for $S$, scalar and metric window readback for $W$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.

Every consumer of the finite extreme-point certificate must read the route
$$
M,V,X \longmapsto F,S,W \longmapsto E \longmapsto H,C,P,N
$$
before treating a candidate as an extreme witness. The route exports finite certificate rows only; it exports no selected set of all extreme points, no full separation theorem, no host compact convex hull equality, no quotient convex-body object, and no ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:krein-milman-finite-extreme-carrier}. The compact and convex source rows are exactly $M$ and $X$, while the ambient functional-analysis reads are confined to $V$.

The exposed-face and separating-functional data are then forced through $F$ and $S$. Their scalar values, thresholds, and gap comparisons are opened through the finite window row $W$, so a consumer sees only displayed weak and normed scalar observations.

The candidate extreme witness row $E$ is downstream from those ledgers. It records the displayed point and the finite refusal of nontrivial convex decompositions inside the opened face route, not a global selector for all extreme points.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since no row contains a completed dual space, unrestricted compactness object, quotient convex body, host separation theorem, or ambient completeness principle, the listed rows exhaust the NameCert obligations.
\end{proof}

\closureat{KreinMilmanFiniteExtremeUp}{seedStr}

\begin{closurestatus}{\KreinMilmanFiniteExtremeUp}
  \constructivestory{A $\KreinMilmanFiniteExtremeUp$ packet is generated from compact metric rows, convex-set rows, weak functional-analysis windows, finite exposed-face ledgers, candidate extreme-point witnesses, separating-functional ledgers, scalar Real readbacks, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:krein-milman-finite-extreme-carrier} and \autoref{thm:krein-milman-finite-extreme-namecert-obligations} seed the finite exposed-face route for a Krein--Milman extreme-point witness over compact metric, convex-set, real scalar, weak-topology, operator-norm, and Banach--Alaoglu window rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full locally convex duality, unrestricted compactness, host separation theorems, selected supporting functionals, quotient convex-body equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-window routing, convex-set admission, finite exposed-face exactness, candidate extreme witness admission, separating-functional ledger visibility, scalar readback, transport, replay, provenance, and local naming for BEDC.Derived.KreinMilmanFiniteExtremeUp.}
\end{closurestatus}
