\chapter{A Concrete Naming Certificate for $\PreuniformityUp$}
\label{ch:concrete-instances-preuniformity-namecert}
\origin{human}

Following Bourbaki and Kelley on uniform structures and preuniform spaces, the $\PreuniformityUp$ packet records the finite entourage-base surface that sits between topology, metric data, and uniform completion. It is read beside the uniform-space certificate of \autoref{ch:concrete-instances-uniformspace-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the topology carrier of \autoref{ch:concrete-instances-topology-namecert}, the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a non-symmetric uniformity-like carrier before a separated or symmetric uniform-space object is available.

\falsifiablePrediction{Every accepted $\PreuniformityUp$ packet displays source, entourage, stability, base, transport, replay, provenance, and local naming rows before a UniformSpaceUp or UniformCompletionUp consumer may read it.}

\independenceWitness{topology, metric, uniformspace, streamname, real: no listed sibling is bijective to $\PreuniformityUp$. Topology records open-neighbourhood data without an entourage base; metric supplies symmetric distance rows; UniformSpaceUp already enforces the uniform-space surface; StreamNameUp records finite schedules; RealUp seals scalar names. The preuniform packet preserves the directed entourage route separately.}

\begin{definition}[Preuniformity carrier]
\label{def:preuniformity-carrier}
A $\PreuniformityUp$ carrier is a finite $\BHist$ packet
$$
U=(S,E,B,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source-row carrier whose points, observations, or names are being compared;
\item $E$ is a finite entourage ledger, recording displayed binary-neighbourhood rows without imposing symmetry;
\item $B$ is the finite filter-base ledger, recording upward closure and finite-intersection refinements for displayed entourage rows;
\item $T$ is the topology-readback row, recording the neighbourhoods induced by the displayed entourage base;
\item $H$ records componentwise $\hsame$ transport for $S,E,B,T$ and the local naming row;
\item $C$ records displayed $\Cont$ replay through source admission, entourage lookup, base refinement, topology readback, provenance, and local naming;
\item $P$ is the $\Pkg$ provenance over $\PreuniformityUp$, $\UniformSpaceUp$, $\MetricUp$, $\TopologyUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\PreuniformityUp}$ row.
\end{enumerate}
The carrier exports no symmetry axiom, separatedness theorem, quotient completion, selected Cauchy limit, host entourage filter, countable-choice base, or ambient $\RealUp$ completeness principle.
\end{definition}
\leanchecked{BEDC.Derived.PreuniformityUp.PreuniformityTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Preuniformity entourage stability]
\label{thm:preuniformity-entourage-stability}
For every accepted $\PreuniformityUp$ carrier $U=(S,E,B,T,H,C,P,N)$, an entourage read is stable only through the displayed ledger $E$, the base-refinement ledger $B$, and componentwise transport $H$. A downstream $\UniformSpaceUp$, $\MetricUp$, $\TopologyUp$, $\StreamNameUp$, or $\RealUp$ consumer may preserve the same directed row, but it may not replace it by a symmetric metric ball, a separated uniformity, a quotient completion row, or a selected completed point.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only binary-neighbourhood data in the packet is the displayed entourage ledger $E$, and the only refinement data is the base ledger $B$.

Transport through $H$ preserves rows componentwise. It therefore preserves the source, entourage, base, topology readback, and naming rows already present in $U$ without adding symmetry, separatedness, or completion data.

The sibling consumers may cite the preserved directed row only as a preuniform row. Since the carrier has no coordinate for metric symmetry, quotient completion, selected limits, host filters, countable choice, or ambient Real completeness, no such datum can be exported by the entourage read.
\end{proof}

\begin{theorem}[Preuniformity filter-base route]
\label{thm:preuniformity-filter-base}
For every accepted $\PreuniformityUp$ carrier $U=(S,E,B,T,H,C,P,N)$, the filter-base row $B$ is read after the entourage ledger $E$ and before topology readback $T$. The row records finite upward and intersection refinements for displayed entourages only; it does not assert a completed host filter, a symmetric uniformity, or a UniformCompletionUp limit object.
\end{theorem}
\begin{proof}
Read the carrier rows in order. The entourage ledger $E$ supplies the displayed binary-neighbourhood rows, and $B$ records the finite refinements allowed for those rows.

The topology row $T$ is downstream of the same finite base. It may expose neighbourhood data through \autoref{ch:concrete-instances-topology-namecert}, but it cannot turn the preuniform base into a host filter or a completed uniform space.

The continuation row $C$ and provenance row $P$ replay and source the same finite route. They do not add symmetry, separatedness, quotient completion, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{proof}
\leanchecked{BEDC.Derived.PreuniformityUp.PreuniformityCarrier\_filter\_base\_route}

\begin{theorem}[Preuniformity NameCert obligations]
\label{thm:preuniformity-namecert-obligations}
For the carrier of \autoref{def:preuniformity-carrier}, the local $\NameCert_{\PreuniformityUp}$ surface consists exactly of carrier admission for $U=(S,E,B,T,H,C,P,N)$, source exposure through $S$, directed entourage exposure through $E$, finite filter-base exposure through $B$, topology readback through $T$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission exposes the finite tuple $S,E,B,T,H,C,P,N$. The source row, entourage ledger, base ledger, topology readback, transport, replay, provenance, and local naming row are all visible coordinates of one packet.

Apply the entourage-stability theorem and the filter-base route theorem. Together they keep binary-neighbourhood reads directed, place base refinements between entourage lookup and topology readback, and prevent a consumer from replacing the preuniform carrier by a symmetric uniformity or completion object.

The listed rows therefore exhaust the local NameCert obligations. There is no additional coordinate from which to read separatedness, metric symmetry, quotient completion, selected limits, host filters, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\closureat{PreuniformityUp}{seedStr}

\begin{closurestatus}{\PreuniformityUp}
  \constructivestory{A $\PreuniformityUp$ packet is generated from a source row, a directed entourage ledger, a finite filter-base ledger, topology readback, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:preuniformity-carrier}, \autoref{thm:preuniformity-entourage-stability}, \autoref{thm:preuniformity-filter-base}, and \autoref{thm:preuniformity-namecert-obligations} seed the directed entourage-base route between topology, metric data, and uniform completion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, uniform-space symmetry, separatedness, quotient completion, selected Cauchy limits, countable choice, host filters, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, directed entourage stability, finite filter-base routing, topology readback, provenance, and local naming for BEDC.Derived.PreuniformityUp.}
\end{closurestatus}
