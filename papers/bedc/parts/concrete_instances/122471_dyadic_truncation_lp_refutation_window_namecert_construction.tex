\chapter{A Concrete Naming Certificate for $\DyadicTruncationLPRefutationWindowUp$}
\label{ch:concrete-instances-dyadic-truncation-lp-refutation-window-namecert}
\origin{ai}

The $\DyadicTruncationLPRefutationWindowUp$ packet realizes the sharp dyadic truncation route obstruction of \autoref{ch:visions-rh-dyadic-truncation-lp-route-refutation} as finite NameCert data. It sits beside the zeta-zero surface of \autoref{ch:concrete-instances-zeta-zeros-namecert}, the critical-strip boundary of \autoref{ch:concrete-instances-critical-strip-namecert}, the zero-count ledger of \autoref{ch:concrete-instances-riemann-zero-count-ledger-namecert}, the contour-operation route of \autoref{ch:concrete-instances-contourintegraloperation-namecert}, and the finite ordered-field comparison discipline of \autoref{ch:concrete-instances-lpduality-namecert}. The packet names the obstruction to the all-cutoff Laguerre--Polya premise; it does not prove or disprove $\mathsf{RH}$, classify all truncations, assert a smooth-deformation route, or import analytic continuation beyond displayed rows.

\begin{definition}[Dyadic truncation LP refutation window carrier]
\label{def:dyadic-truncation-lp-refutation-window-carrier}
A $\DyadicTruncationLPRefutationWindowUp$ carrier is a finite $\BHist$ packet
$$
W=(K,T,Z,Q,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the dyadic cutoff row, recording the sharp finite cutoff and the dyadic precision budget used to inspect the truncated integral.
\item $T$ is the truncation-evaluation row, recording the displayed finite approximation of the sharp cutoff function on the inspected contour windows.
\item $Z$ is the zero-count row, carrying the argument-principle or equivalent winding-count evidence that the inspected truncation has a non-real zero pair.
\item $Q$ is the Laguerre--Polya premise row, naming the all-real-zero condition that the sharp cutoff route would require at the inspected cutoff.
\item $F$ is the refusal row, recording that the displayed non-real zero-count row refutes the all-cutoff premise at this finite window.
\item $H$ records componentwise $\hsame$ transport for cutoff, truncation, zero-count, premise, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the cutoff and truncation rows through zero-count evidence, premise refusal, transport, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\DyadicTruncationLPRefutationWindowUp$, $\ZetaZerosUp$, $\CritStripUp$, $\LPDualityUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\DyadicTruncationLPRefutationWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,T,Z,Q,F,H,C,P,N$. It has no coordinate for a proof of $\mathsf{RH}$, a disproof of $\mathsf{RH}$, a global zero classifier, a hidden analytic-continuation theorem, a smoothed heat-flow replacement, or an all-cutoff real-rootedness oracle.
\end{definition}

\begin{theorem}[Dyadic truncation LP refutation window NameCert obligations]
\label{thm:dyadic-truncation-lp-refutation-window-obligations}
For every accepted $\DyadicTruncationLPRefutationWindowUp$ carrier $W=(K,T,Z,Q,F,H,C,P,N)$, the local $\NameCert_{\DyadicTruncationLPRefutationWindowUp}$ surface consists exactly of cutoff admission, truncation-evaluation exposure, zero-count evidence, Laguerre--Polya premise naming, finite refusal, componentwise transport, displayed replay, package provenance, and local naming. The packet makes the sharp-cutoff route obstruction inspectable as one finite window while leaving all global RH readings outside the carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-truncation-lp-refutation-window-carrier}. The cutoff row $K$ and truncation row $T$ identify the inspected sharp finite window, while $Z$ supplies the displayed zero-count evidence.

The premise row $Q$ names the all-real-zero condition required by the route. The refusal row $F$ is then finite: the displayed non-real zero-count evidence at the same window blocks that premise for the sharp-cutoff family.

The rows $H,C,P,N$ transport, replay, source, and name the same evidence. Since no coordinate stores an RH proof, an RH refutation, a global zero classifier, or a smoothed deformation route, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Dyadic truncation LP route nonclaim boundary]
\label{thm:dyadic-truncation-lp-refutation-window-nonclaim-boundary}
Every accepted $\DyadicTruncationLPRefutationWindowUp$ read exposes only the finite route
$$
K,T,Z,Q,F,H,C,P,N
$$
before a consumer may cite the Laguerre--Polya premise obstruction. The read cannot be reinterpreted as a proof of $\mathsf{RH}$, a disproof of $\mathsf{RH}$, a statement about every cutoff family, or a smooth de Bruijn--Newman deformation certificate.
\end{theorem}
\begin{proof}
Use the obligations theorem. The packet's positive content is the finite cutoff, truncation-evaluation, zero-count, premise, and refusal surface.

The obstruction is local to the displayed sharp cutoff row. Transport and replay preserve that local evidence; they do not quantify over all cutoffs and do not replace the sharp cutoff by a smooth heat-flow deformation.

Thus the consumer receives a finite route refutation window only. Any RH conclusion, global zero classification, or smooth-deformation certificate would require coordinates absent from the carrier.
\end{proof}

\falsifiablePrediction{An accepted sharp dyadic truncation window with displayed non-real zero-count evidence refutes the all-cutoff Laguerre--Polya premise for that sharp-cutoff route.}

\independenceWitness{zeta-zeros, critical-strip, riemann-zero-count-ledger, contourintegraloperation, lpduality: $\DyadicTruncationLPRefutationWindowUp$ is not bijective to any listed sibling because it jointly carries cutoff, truncation, zero-count, premise, refusal, transport, replay, provenance, and local naming rows while refusing all global RH readings.}

\closureat{DyadicTruncationLPRefutationWindowUp}{seedStr}

\begin{closurestatus}{\DyadicTruncationLPRefutationWindowUp}
  \constructivestory{A $\DyadicTruncationLPRefutationWindowUp$ packet is generated from a dyadic cutoff row, truncation-evaluation row, zero-count evidence row, Laguerre--Polya premise row, refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-truncation-lp-refutation-window-carrier}, \autoref{thm:dyadic-truncation-lp-refutation-window-obligations}, and \autoref{thm:dyadic-truncation-lp-refutation-window-nonclaim-boundary} seed the finite sharp-cutoff route obstruction over zeta-zero, critical-strip, zero-count, contour, LP-duality, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not prove RH, disprove RH, classify all truncations, close analytic continuation, replace sharp cutoffs by a smooth deformation, provide Lean verification, or bridge check the route.}
  \upgradepath{Obligation closure requires checked cutoff admission, truncation evaluation, zero-count evidence, Laguerre--Polya premise naming, finite refusal, transport, replay, provenance, and local naming for BEDC.Derived.DyadicTruncationLPRefutationWindowUp.}
\end{closurestatus}
