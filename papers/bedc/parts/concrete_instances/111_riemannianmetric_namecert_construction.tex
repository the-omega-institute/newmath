\chapter{A Concrete Naming Certificate for $\RiemannianMetricUp$}
\label{ch:concrete-instances-riemannianmetric-namecert}

The Riemannian-metric interface $\RiemannianMetricUp$ packages a smooth field of positive-definite inner products on the tangent fibres of a manifold carrier. It depends on $\NameCert_{\ManifoldUp}$ (\autoref{ch:concrete-instances-manifold-namecert}) and $\NameCert_{\InnerProductUp}$ (\autoref{ch:concrete-instances-innerproduct-namecert}), and supplies the metric substrate for length, geodesic, and curvature certificates.

\section{Carrier definition}
\label{sec:riemannianmetric-carrier}

\begin{theorem}[RiemannianMetricUp source fibre carrier row]
\label{thm:riemannianmetric-source-fibre-carrier-row}
A $\NameCert_{\RiemannianMetricUp}$ certificate carries the following
source-fibre rows. Its base carrier is a $\ManifoldUp$ point carrier over
$\mathsf{BHist}$. For each carried point history $x$, it displays a tangent
fibre row $T_x$ over $x$. For each displayed fibre $T_x$, it displays an
$\InnerProductUp$ row
$$
g_x:T_x\times T_x\to \mathsf{BHist}
$$
on that fibre. The visible ledger records only the manifold point provenance,
the tangent-witness provenance over the point, and the fibre inner-product
provenance; it contains no length, geodesic, curvature, or bridge row.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannianMetricUp.RiemannianMetricSingleton\_source\_fibre\_carrier\_row}
\leanchecked{BEDC.Derived.RiemannianMetricUp.RiemannianMetricSingletonFibreSurface\_carrier\_rows}
\begin{proof}
Read the certificate through the ten-field $\NameCert$ template. The only
outer carrier named by the template is the $\ManifoldUp$ carrier, so every
base point is a $\mathsf{BHist}$ point accepted by that source.

For a displayed point $x$, the fibre entry is a dependent row over the same
$\mathsf{BHist}$ witness. The row has no independent endpoint: its source is
the carried $\ManifoldUp$ point and its classifier is inherited through the
point row.

On each such fibre, the metric entry is exactly an $\InnerProductUp$ row. The
ledger projection of the ten-field template therefore has three visible
provenance classes: the $\ManifoldUp$ point row, the tangent row over that
point, and the $\InnerProductUp$ row on the displayed fibre.
\end{proof}

\begin{theorem}[RiemannianMetricUp classifier stability obligation]
\label{thm:riemannianmetric-classifier-stability-obligation}
For a $\NameCert_{\RiemannianMetricUp}$ certificate, classifier stability has
the following obligation shape. If point histories $x$ and $y$ are related by
$\hsame$ and by the $\ManifoldUp$ point classifier, then every tangent row over
$x$ transports to a tangent row over $y$. If tangent witnesses $u,v$ over $x$
transport to tangent witnesses $u',v'$ over $y$, then the $\InnerProductUp$
classifier transports the fibrewise metric endpoint
$$
g_x(u,v)
$$
to
$$
g_y(u',v').
$$
\end{theorem}
\begin{proof}
Start with the ten-field $\NameCert$ classifier field. Its base comparison is
the $\hsame$ comparison on $\mathsf{BHist}$ histories together with the
$\ManifoldUp$ point classifier, so the transported endpoint remains inside the
same carried point surface.

The tangent row is dependent on that transported point. Applying the
$\NameCert$ stability field moves the displayed tangent witness from the
source point to the target point without adding any classifier beyond
$\hsame$ and the $\ManifoldUp$ point classifier.

It remains to compare metric endpoints. The metric row on each fibre is an
$\InnerProductUp$ row, so its own classifier transports the endpoint through
the transported tangent witnesses and yields the displayed target endpoint.
\end{proof}

\begin{theorem}[RiemannianMetricUp fibrewise positivity and chart compatibility obligation]
\label{thm:riemannianmetric-fibrewise-positive-compatibility-obligation}
A $\NameCert_{\RiemannianMetricUp}$ certificate carries the following
fibrewise metric obligations. For every displayed point $x$ and nonzero
tangent witness $u:T_x$, the fibre row has a positive-definite entry
$$
g_x(u,u)>0.
$$
For every displayed fibre, symmetry and bilinearity are inherited from the
$\InnerProductUp$ row on that fibre. For every overlap in the $\ManifoldUp$
atlas, chart-change transport carries tangent witnesses through the atlas
classifier and preserves the displayed fibre metric endpoint under the
$\InnerProductUp$ classifier. These obligations do not assert length,
geodesic, curvature, or a standard bridge.
\end{theorem}
\begin{proof}
Unpack the ten-field $\NameCert$ template at a displayed $\mathsf{BHist}$
point. The point is supplied only by $\ManifoldUp$, and the tangent witness is
supplied only as a fibre row over that point.

The metric row on the fibre is an $\InnerProductUp$ row. Its positive,
symmetric, and bilinear entries give the positive-definite, symmetry, and
bilinearity obligations after restricting all arguments to the displayed
tangent fibre.

For a chart overlap, use the $\ManifoldUp$ atlas classifier to transport the
point and tangent witnesses across the overlap. Then apply the
$\InnerProductUp$ classifier on the target fibre to identify the metric
endpoint after transport.

The template has no row whose endpoint is a length functional, geodesic
equation, curvature tensor, or standard bridge. Hence those interfaces are
outside the stated obligation scope.
\end{proof}

\section{The certificate}
\label{sec:riemannianmetric-certificate}

\begin{theorem}[RiemannianMetricUp certificate surface assembly]
\label{thm:riemannianmetric-certificate-surface-assembly}
The source-fibre carrier row, classifier-stability row, and fibrewise
positivity and chart-compatibility row assemble a complete scoped
$\NameCert_{\RiemannianMetricUp}$ surface over $\mathsf{BHist}$. Its carrier
is the displayed $\ManifoldUp$ point surface together with tangent fibres over
accepted points and $\InnerProductUp$ metric rows over those fibres. Its
classifier is stable under the transported point and tangent witnesses, and
its metric obligations are exactly the fibrewise positivity, symmetry,
bilinearity, and chart-compatibility obligations displayed in the row data.
\end{theorem}
\begin{proof}
Use the carrier row of
\autoref{thm:riemannianmetric-source-fibre-carrier-row} as the outer carrier
of the certificate surface. This fixes the only visible base objects: point
histories, tangent witnesses over those points, and fibre inner-product rows.

The classifier-stability row of
\autoref{thm:riemannianmetric-classifier-stability-obligation} supplies the
transport field of the surface. It moves accepted points through $\hsame$ and
then moves the dependent tangent and metric endpoints along the same accepted
comparison.

Finally, the positivity and compatibility row of
\autoref{thm:riemannianmetric-fibrewise-positive-compatibility-obligation}
supplies the local metric laws and the chart-overlap transport law. These
three rows therefore fill the scoped $\NameCert_{\RiemannianMetricUp}$
surface without adding length, geodesic, curvature, or bridge data.
\end{proof}

\begin{theorem}[RiemannianMetricUp ledger scope]
\label{thm:riemannianmetric-ledger-scope}
For the scoped $\NameCert_{\RiemannianMetricUp}$ surface, the visible ledger
is exactly point provenance, tangent-witness provenance, and fibre
inner-product provenance. No ledger entry is generated for length,
geodesic equations, curvature tensors, or a standard bridge.
\end{theorem}
\begin{proof}
The carrier row of
\autoref{thm:riemannianmetric-source-fibre-carrier-row} lists the only
displayed source objects. They are the $\ManifoldUp$ point row, the tangent
row over that point, and the $\InnerProductUp$ row on the displayed fibre.

\autoref{thm:riemannianmetric-certificate-surface-assembly} uses exactly
those rows to assemble the scoped certificate surface. The classifier and
metric obligations act on the listed rows, but they do not introduce an
additional ledger endpoint.

The row data named in
\autoref{thm:riemannianmetric-fibrewise-positive-compatibility-obligation}
explicitly excludes length, geodesic, curvature, and standard-bridge
endpoints. Hence the ledger of the scoped surface is precisely the three
provenance classes stated above.
\end{proof}

\begin{theorem}[RiemannianMetricUp exactness surface]
\label{thm:riemannianmetric-exactness-surface}
The scoped $\NameCert_{\RiemannianMetricUp}$ surface is exact. Coverage holds
because every admitted row is generated by the displayed manifold point,
tangent fibre, and inner-product rows. Soundness holds because every generated
row satisfies the certificate classifier. Completeness holds because the
classifier identifies precisely those rows whose endpoints are the transported
point, transported tangent witness, and transported fibre metric endpoint of
the displayed surface.
\end{theorem}
\begin{proof}
For coverage, apply the assembled surface of
\autoref{thm:riemannianmetric-certificate-surface-assembly}. Its carrier has
no row source other than the displayed point, tangent fibre, and fibre
inner-product rows, so every admitted row is generated by those sources.

For soundness, use the classifier-stability obligation of
\autoref{thm:riemannianmetric-classifier-stability-obligation}. A generated
point row is accepted by the $\ManifoldUp$ classifier, a generated tangent row
is accepted over the transported point, and a generated metric row is accepted
by the transported $\InnerProductUp$ classifier.

For completeness, suppose a row is accepted by the scoped classifier. The
outer point comparison places its point endpoint in the transported
$\ManifoldUp$ point surface. The dependent tangent comparison places its
tangent endpoint over that transported point, and the fibre metric comparison
places its metric endpoint in the transported $\InnerProductUp$ row.

These are exactly the row generators named in the coverage step. Therefore
the classifier accepts no row outside the displayed point, tangent, and fibre
metric surface, and every row in that surface is accepted.
\end{proof}

\begin{closurestatus}{\RiemannianMetricUp}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The scoped surface is closed by \autoref{thm:riemannianmetric-certificate-surface-assembly}, \autoref{thm:riemannianmetric-ledger-scope}, and \autoref{thm:riemannianmetric-exactness-surface}, using the source-fibre carrier row of \autoref{thm:riemannianmetric-source-fibre-carrier-row}, the classifier-stability row of \autoref{thm:riemannianmetric-classifier-stability-obligation}, and the positivity and chart-compatibility row of \autoref{thm:riemannianmetric-fibrewise-positive-compatibility-obligation}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close length, geodesic, or curvature certificates, a standard bridge, public closure, or Lean verification.}
  \upgradepath{Public closure requires exposing the scoped $\NameCert_{\RiemannianMetricUp}$ surface as a public object with its public ledger boundary.}
\end{closurestatus}
