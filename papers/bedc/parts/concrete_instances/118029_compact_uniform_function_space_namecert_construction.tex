\chapter{A Concrete Naming Certificate for $\CompactUniformFunctionSpaceUp$}
\label{ch:concrete-instances-compact-uniform-function-space-namecert}
\origin{human}

Following Bishop--Bridges compact-uniform continuity and Kelley-style function-space usage, the $\CompactUniformFunctionSpaceUp$ packet names a finite BEDC carrier for compact-source continuous maps under uniform finite observation. It sits after the compact-uniform-continuity packet of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the compact metric source rows of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet is a finite function-space carrier consumed by compact-uniform continuity and Arzela--Ascoli style rows; it does not import host function equality, open-cover compactness, selected subsequences, or ambient Real completeness.

\begin{definition}[Compact-uniform function-space carrier]
\label{def:compact-uniform-function-space-carrier}
A $\CompactUniformFunctionSpaceUp$ carrier is a finite $\BHist$ packet
$$
\mathcal{F}=(K,M,U,W,R,D,E,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, including the finite $\mathsf{ProbeBundle}$ net and compact-metric distance observations available to function-space reads.
\item $M$ is the finite $\ContinuousMapUp$ ledger of displayed maps over the compact source.
\item $U$ is the $\CompactUniformContinuityUp$ handoff row that turns pointwise map data into compact-source uniform windows.
\item $W$ is the $\StreamNameUp$ schedule for source probes, map indices, tolerance requests, and target observations.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational precision histories attached to $W$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used for finite target-distance and modulus comparisons.
\item $E$ is the terminal $\RealUp$ seal reached by sampled target-distance rows after the window and readback coordinates have been displayed.
\item $H$ records componentwise $\hsame$ transport for $K,M,U,W,R,D,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a function-space request through compact finite-net inspection, continuous-map graph sampling, compact-uniform handoff, stream scheduling, rational readback, dyadic tolerance accounting, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformFunctionSpaceUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\CompactUniformContinuityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformFunctionSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,U,W,R,D,E,H,C,P,N$. There is no coordinate for host equality of functions, open-cover compactness, a selected subsequence, a quotient map space, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact-uniform function-space NameCert obligations]
\label{thm:compact-uniform-function-space-namecert-obligations}
For every accepted $\CompactUniformFunctionSpaceUp$ carrier
$$
\mathcal{F}=(K,M,U,W,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformFunctionSpaceUp}$ surface consists exactly of compact-source admission through $K$, displayed continuous-map admission through $M$, compact-uniform handoff through $U$, finite scheduling through $W$, regular-rational readback through $R$, dyadic tolerance accounting through $D$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-function-space-carrier}. Its coordinates list exactly the compact source, displayed maps, compact-uniform handoff, finite windows, rational readback, dyadic tolerance ledger, Real seal, transport, replay, provenance, and local naming rows.

The compact-source and map clauses are read through $K$ and $M$. The source side is a finite compact-metric observation surface, while the map side is a displayed continuous-map ledger; neither clause exposes host equality of functions or a topology on all maps.

The uniform and numerical clauses are then read through $U,W,R,D,E$. The compact-uniform row orders the source windows, the stream and regular-rational rows schedule and read them back, the dyadic row accounts for finite tolerances, and the Real row seals sampled target distances only after those finite rows have appeared.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for open-cover compactness, selected subsequences, quotient map equality, or ambient Real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact-uniform function-space finite-net handoff]
\label{thm:compact-uniform-function-space-finite-net-handoff}
Every compact-uniform or Arzela--Ascoli style consumer of an accepted $\CompactUniformFunctionSpaceUp$ packet must read the displayed finite route
$$
\CompactMetricUp \longmapsto \ContinuousMapUp \longmapsto \CompactUniformContinuityUp \longmapsto \StreamNameUp,\RegSeqRatUp,\DyadicRatCoreUp \longmapsto \RealUp .
$$
The handoff supplies compact finite-net observations, displayed map rows, uniform source-window control, finite precision schedules, rational readback, dyadic tolerance accounting, and terminal Real seals. It supplies no host function equality, open-cover compactness, selected subsequence, quotient function space, or ambient Real-completeness datum.
\end{theorem}
\begin{proof}
Start with an accepted carrier $\mathcal{F}=(K,M,U,W,R,D,E,H,C,P,N)$. The compact source is exposed only by $K$, and the maps available to the consumer are the displayed rows in $M$.

Next read the compact-uniform handoff $U$. It routes every tolerance request through the compact finite-net surface before any target observation can be sealed.

The schedule and numerical readback are then forced by $W,R,D,E$. The stream row names the finite windows, the regular rational row reads the precision histories, the dyadic row records the tolerance ledger, and the Real row seals only the sampled target-distance data produced by those finite rows.

The structural rows $H,C,P,N$ preserve exactly that route under transport and replay. Because no further coordinate is present, a downstream compact-uniform or Arzela--Ascoli style packet cannot replace the finite-net handoff by host function equality, open-cover compactness, selected subsequences, quotient map equality, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformFunctionSpaceUp$ read factors through one $\CompactMetricUp$ finite-net source row, one finite $\ContinuousMapUp$ ledger, one $\CompactUniformContinuityUp$ handoff row, one $\StreamNameUp$ schedule, one $\RegSeqRatUp$ readback row, one $\DyadicRatCoreUp$ tolerance ledger, one terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{compactuniformcontinuity, compactmetric, continuousmap, streamname, regseqrat, real, dyadicratcore: $\CompactUniformFunctionSpaceUp$ is not bijective to any listed sibling because it jointly carries compact finite-net source data, displayed continuous-map rows, compact-uniform handoff, finite schedules, rational readback, dyadic tolerance accounting, Real sealing, transport, replay, provenance, and local NameCert rows as one function-space packet.}

\closureat{CompactUniformFunctionSpaceUp}{seedStr}

\begin{closurestatus}{\CompactUniformFunctionSpaceUp}
  \constructivestory{A $\CompactUniformFunctionSpaceUp$ packet is generated from a CompactMetricUp finite-net source, ContinuousMapUp displayed map rows, a CompactUniformContinuityUp handoff, StreamNameUp windows, RegSeqRatUp readback, DyadicRatCoreUp tolerance accounting, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-function-space-carrier}, \autoref{thm:compact-uniform-function-space-namecert-obligations}, and \autoref{thm:compact-uniform-function-space-finite-net-handoff} seed the compact-uniform function-space packet over finite-net, map, uniform-handoff, window, readback, dyadic, seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, open-cover compactness, selected subsequences, quotient function spaces, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source coverage, displayed-map admission, compact-uniform handoff, window and readback exactness, dyadic tolerance exactness, Real-seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
