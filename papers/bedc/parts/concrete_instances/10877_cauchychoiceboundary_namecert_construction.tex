\chapter{A Concrete Naming Certificate for $\CauchyChoiceBoundaryUp$}
\label{ch:concrete-instances-cauchy-choice-boundary-namecert}
\origin{ai}

The $\CauchyChoiceBoundaryUp$ packet realizes the no-choice side of the Real completeness diagonal route of \autoref{ch:visions-real-completeness}. It sits after the limit selector of \autoref{ch:concrete-instances-limitselector-namecert}, the shared-threshold meet of \autoref{ch:concrete-instances-regular-cauchy-diagonal-meet-namecert}, the seal-budget synchronizer of \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert}, and the completion-seal classifier of \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}. Its role is to bind the explicit finite budget, selected index, tail witness, seal route, refusal row, transport, replay, provenance, and local NameCert surface into one packet without importing a host selected limit, quotient stream equality, countable choice, ambient completeness, or $\mathsf{Classical.choice}$.

\falsifiablePrediction{A Real-completion diagonal consumer that omits the selected finite budget row cannot claim the no-choice diagonal seal route.}

\independenceWitness{limitselector, regular-cauchy-diagonal-meet, cauchy-seal-budget-synchronizer, real-completion-seal-classifier, cauchy-witness-ledger: none is bijective to $\CauchyChoiceBoundaryUp$ because this carrier binds the selected finite index, tail-witness route, no-choice refusal row, seal route, transport, replay, provenance, and local naming in one boundary packet, while the siblings isolate selector admission, meet formation, budget synchronization, seal classification, or witness-ledger exposure.}

\begin{definition}[Cauchy choice-boundary carrier]
\label{def:cauchy-choice-boundary-carrier}
A $\CauchyChoiceBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(M,E,I,T,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the modulus row for the accepted regular Cauchy input, read through the explicit Real-completion diagonal route;
\item $E$ is the finite tolerance-budget row whose bound determines the diagonal scale being served;
\item $I$ is the selected finite index row produced from $M$ and $E$, not from a global selector or countable-choice principle;
\item $T$ is the tail-witness route row certifying that all downstream observations are read on the displayed finite tail determined by $I$;
\item $S$ is the Real seal route row reached through the same finite budget and tail-witness data;
\item $R$ is the no-choice refusal row, recording that the packet exports no selected limit object, no quotient stream equality, no ambient completeness row, no countable choice row, and no $\mathsf{Classical.choice}$ row;
\item $H$ stores componentwise $\hsame$ transport for $M,E,I,T,S,R$ and for the local packet shape;
\item $C$ stores the displayed $\Cont$ replay from modulus and tolerance admission to selected index, tail witness, Real seal, refusal, and local naming;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:concrete-instances-limitselector-namecert}, \autoref{ch:concrete-instances-regular-cauchy-diagonal-meet-namecert}, \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert}, \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}, \autoref{ch:concrete-instances-cauchy-witness-ledger-namecert}, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CauchyChoiceBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for host-side real equality, quotient regular streams, an unbounded selector, ambient metric completeness, countable choice, or $\mathsf{Classical.choice}$.
\end{definition}
\leanchecked{BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundaryTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Cauchy choice-boundary diagonal-budget factorization]
\label{thm:cauchy-choice-boundary-diagonal-budget-factorization}
Every accepted Real-completion diagonal consumer of a $\CauchyChoiceBoundaryUp$ packet
$$
B=(M,E,I,T,S,R,H,C,P,N)
$$
factors through the displayed route
$$
M,E,I,T,S,R,H,C,P,N.
$$
In particular, the consumer reads the explicit modulus before the tolerance budget, the selected finite index before the tail witness, the tail witness before the Real seal route, and the refusal row before any no-choice claim is exported.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundaryDiagonalBudgetFactorization}
\begin{proof}
Project the carrier of \autoref{def:cauchy-choice-boundary-carrier}. Its only modulus, budget, selected-index, tail-witness, seal, and refusal coordinates are $M,E,I,T,S,R$.

The selected-index row $I$ is displayed as a finite consequence of $M$ and $E$. Hence any diagonal consumer that reaches $T$ or $S$ has already passed through the modulus and tolerance rows rather than through a hidden selector.

The structural rows $H,C,P,N$ preserve and replay this same order. Componentwise $\hsame$ transport carries only the displayed rows, $\Cont$ replay follows the listed route, provenance names the sibling certificates, and local naming exposes the packet as the same finite boundary surface.

Thus every accepted diagonal consumer factors through $M,E,I,T,S,R,H,C,P,N$ before it can claim the Real seal route.
\end{proof}

\begin{theorem}[Cauchy choice-boundary no-choice non-escape]
\label{thm:cauchy-choice-boundary-no-choice-nonescape}
No selected limit, quotient stream equality, ambient completeness row, countable choice row, or $\mathsf{Classical.choice}$ row can be read from an accepted $\CauchyChoiceBoundaryUp$ packet. The packet exports only the modulus row, tolerance budget, selected finite index, tail-witness route, Real seal route, no-choice refusal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row listed in \autoref{def:cauchy-choice-boundary-carrier}.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundary\_no\_choice\_nonescape}
\begin{proof}
Inspect the accepted carrier. The row $R$ explicitly records refusal of selected limits, quotient stream equality, ambient completeness, countable choice, and $\mathsf{Classical.choice}$, while the carrier itself contains no additional coordinate from which such data could be reconstructed.

Apply \autoref{thm:cauchy-choice-boundary-diagonal-budget-factorization}. Any accepted consumer must pass through the finite route $M,E,I,T,S,R,H,C,P,N$.

The finite selected-index row $I$ is not a selected limit object; it is only the index admitted by the displayed modulus and tolerance budget. The tail row $T$ and seal row $S$ are likewise route witnesses, not quotient equalities or ambient completeness principles.

Therefore the only exported data are the ten displayed packet rows, and every attempted read of a selected limit, quotient stream equality, ambient completeness, countable choice, or $\mathsf{Classical.choice}$ leaves the accepted boundary packet.
\end{proof}

\begin{theorem}[Cauchy choice-boundary NameCert obligation surface]
\label{thm:cauchy-choice-boundary-namecert-obligation-surface}
The carrier of \autoref{def:cauchy-choice-boundary-carrier}, the diagonal-budget factorization of \autoref{thm:cauchy-choice-boundary-diagonal-budget-factorization}, and the no-choice boundary of \autoref{thm:cauchy-choice-boundary-no-choice-nonescape} assemble the five NameCert obligations for $\CauchyChoiceBoundaryUp$: the carrier row, classifier-stable visible route, diagonal-budget exactness, refusal of selected-limit or quotient exits, and local ledger provenance. Concretely, every accepted packet
$$
B=(M,E,I,T,S,R,H,C,P,N)
$$
exports exactly those five obligation rows through its ten displayed carrier fields.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundaryNameCertObligationSurface}
\begin{proof}
Project the ten fields of the carrier. The rows $M,E,I,T,S,R,H,C,P,N$ give the carrier row, and the structural rows $H$ and $C$ restrict comparison and replay to the visible route already listed in the definition.

The route exactness is supplied by \autoref{thm:cauchy-choice-boundary-diagonal-budget-factorization}: any accepted consumer reaches the seal row only through the displayed modulus, tolerance budget, selected index, tail witness, and refusal row.

The negative boundary is supplied by \autoref{thm:cauchy-choice-boundary-no-choice-nonescape}. It blocks selected-limit data, quotient stream equality, ambient completeness, countable choice, and $\mathsf{Classical.choice}$ as exits from the packet.

It remains to identify the local ledger. The provenance row $P$ records the sibling certificates and kernel primitives used by the construction, while $N$ is the local $\NameCert_{\CauchyChoiceBoundaryUp}$ row. These projections give the carrier, visible-route, exactness, refusal, and provenance obligations without adding any global Cauchy selector or host-side completeness principle.
\end{proof}

\begin{theorem}[Cauchy choice-boundary scoped consumer route]
\label{thm:cauchy-choice-boundary-scoped-consumer-route}
Every accepted Real-completion diagonal consumer of
$$
B=(M,E,I,T,S,R,H,C,P,N)
$$
factors through the five obligation rows of \autoref{thm:cauchy-choice-boundary-namecert-obligation-surface}: the carrier row, classifier-stable visible route, diagonal-budget exactness, no-choice refusal, and local ledger provenance. The scoped route is read as a finite $\BHist$ consumer whose componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\CauchyChoiceBoundaryUp}$ row pass through the sibling dependencies $\LimitSelectorUp$, $\RegularCauchyDiagonalMeetUp$, $\CauchySealBudgetSynchronizerUp$, $\RealCompletionSealClassifierUp$, and $\CauchyWitnessLedgerUp$.

Consequently the consumer may use only the displayed modulus, tolerance budget, selected finite index, tail-witness route, Real seal route, refusal row, transport, replay, provenance, and local naming. It preserves the exclusions on selected limits, quotient stream equality, ambient completeness, countable choice, and $\mathsf{Classical.choice}$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundaryScopedConsumerRoute}
\begin{proof}
Project the carrier definition. The packet supplies exactly the rows $M,E,I,T,S,R,H,C,P,N$, with $\BHist$ carrying the finite history, $\hsame$ transporting the displayed components, $\Cont$ replaying the route, $\Pkg$ recording provenance, and $\NameCert_{\CauchyChoiceBoundaryUp}$ naming the local surface.

Apply \autoref{thm:cauchy-choice-boundary-diagonal-budget-factorization} to fix the consumer order. The route reaches the selected finite index only after the modulus and tolerance budget, reaches the tail witness and seal route only through that selected index, and reaches the refusal row before any no-choice boundary is claimed.

Apply \autoref{thm:cauchy-choice-boundary-no-choice-nonescape}. Any attempted exit to a selected limit, quotient stream equality, ambient completeness row, countable choice row, or $\mathsf{Classical.choice}$ lies outside the accepted boundary packet.

Finally apply \autoref{thm:cauchy-choice-boundary-namecert-obligation-surface}. Its five rows package the carrier, classifier-stable visible route, diagonal-budget exactness, no-choice refusal, and local ledger provenance, with the sibling dependencies $\LimitSelectorUp$, $\RegularCauchyDiagonalMeetUp$, $\CauchySealBudgetSynchronizerUp$, $\RealCompletionSealClassifierUp$, and $\CauchyWitnessLedgerUp$ recorded through $P$ and exposed through $N$.
\end{proof}

\begin{theorem}[Cauchy choice-boundary public consumer seal]
\label{thm:cauchy-choice-boundary-public-consumer-seal}
Every public Real-completion consumer of an accepted $\CauchyChoiceBoundaryUp$ packet
$$
B=(M,E,I,T,S,R,H,C,P,N)
$$
must read the same ten rows $M,E,I,T,S,R,H,C,P,N$ through the five obligation rows of \autoref{thm:cauchy-choice-boundary-namecert-obligation-surface} and the scoped route of \autoref{thm:cauchy-choice-boundary-scoped-consumer-route} before reaching any Real seal claim. Consequently the public interface exports the finite no-choice boundary and excludes selected limits, quotient stream equality, ambient completeness, countable choice, and $\mathsf{Classical.choice}$.
\end{theorem}
\begin{proof}
Project the carrier definition. The accepted packet has only the displayed rows $M,E,I,T,S,R,H,C,P,N$, so a public consumer has no additional coordinate from which to read a selected limit, a quotient equality, an ambient completeness witness, countable choice, or $\mathsf{Classical.choice}$.

Apply \autoref{thm:cauchy-choice-boundary-diagonal-budget-factorization} to fix the public row order. The route reads the modulus and tolerance budget before the selected finite index, the selected index before the tail witness and Real seal route, and the refusal row before any no-choice claim is exported.

Apply \autoref{thm:cauchy-choice-boundary-no-choice-nonescape} to block the excluded exits. The public consumer can only keep the displayed no-choice boundary rows; every attempted selected-limit, quotient-stream, ambient-completeness, countable-choice, or $\mathsf{Classical.choice}$ read leaves the accepted packet.

Apply \autoref{thm:cauchy-choice-boundary-namecert-obligation-surface}. Its five obligation rows package the carrier row, classifier-stable visible route, diagonal-budget exactness, no-choice refusal, and local ledger provenance, so the public interface is already mediated by the finite NameCert surface.

Finally apply \autoref{thm:cauchy-choice-boundary-scoped-consumer-route}. The sibling dependencies $\LimitSelectorUp$, $\RegularCauchyDiagonalMeetUp$, $\CauchySealBudgetSynchronizerUp$, $\RealCompletionSealClassifierUp$, and $\CauchyWitnessLedgerUp$ are bound through the scoped route before any Real seal claim is made, which seals the public no-choice consumer surface.
\end{proof}

The Lean follow-up for this chapter is the namespace \texttt{BEDC.Derived.CauchyChoiceBoundaryUp}. Its \texttt{TasteGate.lean} surface must provide both a \texttt{Nontrivial CauchyChoiceBoundaryUp} instance and a \texttt{FieldFaithful CauchyChoiceBoundaryUp} instance for the carrier of \autoref{def:cauchy-choice-boundary-carrier}.

\begin{theorem}[Cauchy choice-boundary bridged Real seal]
\label{thm:cauchy-choice-boundary-bridged-real-seal}
Every accepted $\CauchyChoiceBoundaryUp$ public consumer whose rows factor through the displayed diagonal budget, selected finite index, tail-witness route, and Real seal route is a bridged Real-completion seal consumer. The bridge exports only the rows already named by \autoref{thm:cauchy-choice-boundary-public-consumer-seal}, \autoref{thm:cauchy-choice-boundary-scoped-consumer-route}, and \autoref{thm:cauchy-choice-boundary-no-choice-nonescape}. It exports no selected limit, quotient stream, ambient completeness, countable choice, host equality, or $\mathsf{Classical.choice}$ coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundaryUp\_StdBridge}
\begin{proof}
First apply \autoref{thm:cauchy-choice-boundary-scoped-consumer-route}. The accepted public consumer is forced through the finite carrier row, classifier-stable visible route, diagonal-budget exactness, no-choice refusal, and local ledger provenance before it reaches the Real seal route.

Next apply \autoref{thm:cauchy-choice-boundary-public-consumer-seal}. The public interface reads the same ten rows $M,E,I,T,S,R,H,C,P,N$ and reaches the seal only through the displayed budget, selected finite index, tail witness, refusal, transport, replay, provenance, and local naming surface.

Finally apply \autoref{thm:cauchy-choice-boundary-no-choice-nonescape}. The accepted boundary packet has no coordinate for a selected limit, quotient stream, ambient completeness, countable choice, host equality, or $\mathsf{Classical.choice}$, so the Real-completion seal consumer is bridged only by the already displayed no-choice rows.
\end{proof}

\closureat{CauchyChoiceBoundaryUp}{seedStr}
\closureat{CauchyChoiceBoundaryUp}{obligationStr}
\closureat{CauchyChoiceBoundaryUp}{scopedStr}[BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundary\_no\_choice\_nonescape]
\closureat{CauchyChoiceBoundaryUp}{publicStr}
\closureat{CauchyChoiceBoundaryUp}{bridgedStr}

\begin{closurestatus}{\CauchyChoiceBoundaryUp}
  \constructivestory{The Cauchy choice boundary is generated by starting with the explicit diagonal-budget rows recorded in $BHist$ and refusing to add a selector row for an arbitrary Cauchy subsequence. The carrier reads a Cauchy handoff only when it is already factored through the displayed budget ledger. The $hsame$ relation transports that boundary across equivalent histories, while $\Cont$ only extends the visible budget route and never licenses a new choice functional. Thus the chapter's object is a bottom-up refusal surface: it certifies the route from finite diagonal evidence to a Cauchy boundary and records where countable-choice-style exits leave the carrier.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{This chapter binds the Cauchy choice-boundary carrier, diagonal-budget factorization theorem, no-choice non-escape theorem, NameCert obligation-surface rows, scoped consumer route, public consumer seal, bridged Real-seal theorem \autoref{thm:cauchy-choice-boundary-bridged-real-seal}, and TasteGate instance \texttt{BEDC.Derived.CauchyChoiceBoundaryUp.taste\_gate}.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.CauchyChoiceBoundaryUp.CauchyChoiceBoundary\_no\_choice\_nonescape}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim a global Cauchy selector, choice over all tails, or host-level equality of Cauchy representatives.}
  \upgradepath{Reaching mature closure requires worked examples and cross-chapter consumers over the bridged Real-completion seal interface.}
\end{closurestatus}
