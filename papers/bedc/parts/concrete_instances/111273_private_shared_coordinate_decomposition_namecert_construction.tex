\chapter{A Concrete Naming Certificate for $\PrivateSharedCoordinateDecompositionUp$}
\label{ch:concrete-instances-private-shared-coordinate-decomposition-namecert}
\origin{ai}

The $\PrivateSharedCoordinateDecompositionUp$ packet realizes the private/shared coordinate decomposition of \autoref{def:philosophy-private-shared-coordinates} as finite NameCert data. It reads the quotient-level support family, private blocks, shared-coordinate row sets, and reconstruction equations through the support-census sibling of \autoref{ch:concrete-instances-support-nerve-census-namecert} and the reassignment-geometry sibling of \autoref{ch:concrete-instances-reassignment-mass-subcube-namecert}. The packet is a finite combinatorial carrier; it is not a biological mechanism claim, a hidden quotient of support labels, or an ambient uniqueness principle outside the displayed rows.

\begin{definition}[Private-shared coordinate decomposition carrier]
\label{def:private-shared-coordinate-decomposition-carrier}
A $\PrivateSharedCoordinateDecompositionUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,B,U,R,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite support-family row, displaying the quotient-level support family on the inspected coordinate set.
\item $B$ is the private-block row, listing the nonempty private coordinate blocks attached to the supports.
\item $U$ is the shared-coordinate row, listing the coordinates not owned by a single private block.
\item $R$ is the row-set ledger. For every shared coordinate it records the finite set of private blocks whose supports contain that coordinate.
\item $K$ is the reconstruction row, recording the displayed equations that recover each support from its private block and the shared coordinates whose row sets contain that block.
\item $H$ records componentwise $\hsame$ transport for support-family, private-block, shared-coordinate, row-set, reconstruction, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from support-family inspection through private-block extraction, shared-coordinate listing, row-set lookup, support reconstruction, transports, provenance, and local name.
\item $P$ is the $\Pkg$ provenance over $\PrivateSharedCoordinateDecompositionUp$, the source private/shared coordinate vision, $\SupportNerveCensusUp$, $\ReassignmentMassSubcubeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PrivateSharedCoordinateDecompositionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,B,U,R,K,H,C,P,N$. It has no coordinate for hidden support equality, unbounded coordinate universes, biological causality, stochastic dynamics, Lean verification, or a checked bridge.
\end{definition}

\begin{theorem}[PrivateSharedCoordinateDecomposition NameCert obligations]
\label{thm:private-shared-coordinate-decomposition-namecert-obligations}
For every accepted $\PrivateSharedCoordinateDecompositionUp$ carrier
$$
Q=(S,B,U,R,K,H,C,P,N),
$$
the local $\NameCert_{\PrivateSharedCoordinateDecompositionUp}$ surface consists exactly of support-family exposure, private-block exposure, shared-coordinate exposure, finite row-set exposure, reconstruction equation exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The reconstruction row must use only the displayed private block and shared-coordinate row-set data from $B,U,R$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:private-shared-coordinate-decomposition-carrier}. The rows $S,B,U,R$ display the support family, the nonempty private blocks, the shared coordinates, and the finite row sets attached to those shared coordinates.

The reconstruction row $K$ is downstream of those data. It may rebuild a support only by taking its private block from $B$ and adjoining exactly the shared coordinates from $U$ whose row-set entry in $R$ contains that block.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier contains no hidden support equality, unbounded coordinate universe, biological mechanism, stochastic dynamics, Lean verification row, or bridge credential, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Private-shared coordinate reconstruction handoff]
\label{thm:private-shared-coordinate-decomposition-reconstruction-handoff}
Every consumer of an accepted $\PrivateSharedCoordinateDecompositionUp$ carrier factors through the finite route
$$
S \longmapsto B,U,R \longmapsto K \longmapsto H,C,P,N .
$$
Consequently, the support corresponding to a private block is recovered only from the displayed private block, the shared-coordinate ledger, and the finite row-set membership data, exactly as in the source decomposition of \autoref{thm:philosophy-private-shared-decomposition}.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:private-shared-coordinate-decomposition-namecert-obligations}. They force the support-family row $S$ to expose the finite family before private and shared coordinates are read.

The rows $B,U,R$ then provide the decomposition data. Private blocks give the singleton-owned coordinates, while shared coordinates are paired with finite row sets naming the blocks whose supports contain them.

The reconstruction row $K$ consumes only those displayed rows. Transport, replay, provenance, and local naming preserve the same finite route, so a consumer cannot replace it by a hidden quotient of supports or by an ambient uniqueness principle.
\end{proof}

\falsifiablePrediction{Every accepted $\PrivateSharedCoordinateDecompositionUp$ consumer must expose finite support data, private blocks, shared coordinates, row-set membership, reconstruction equations, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence before reading a reconstructed support.}

\independenceWitness{support-nerve-census, reassignment-mass-subcube, private-shared-coordinate vision: $\PrivateSharedCoordinateDecompositionUp$ is not bijective to any listed sibling because it jointly binds private blocks, shared-coordinate row sets, reconstruction equations, transport, replay, provenance, and local naming as one finite coordinate-decomposition packet.}

\closureat{PrivateSharedCoordinateDecompositionUp}{seedStr}

\begin{closurestatus}{\PrivateSharedCoordinateDecompositionUp}
  \constructivestory{A $\PrivateSharedCoordinateDecompositionUp$ packet is generated from finite support-family rows, private-block rows, shared-coordinate rows, row-set ledgers, reconstruction equations, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:private-shared-coordinate-decomposition-carrier}, \autoref{thm:private-shared-coordinate-decomposition-namecert-obligations}, and \autoref{thm:private-shared-coordinate-decomposition-reconstruction-handoff} seed the finite NameCert packet for private/shared coordinate decomposition.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close hidden support equality, unbounded coordinate universes, biological causality, stochastic dynamics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked support-family exposure, private-block exposure, shared-coordinate exposure, row-set exposure, reconstruction exactness, transport stability, replay stability, provenance exhaustion, and local naming for BEDC.Derived.PrivateSharedCoordinateDecompositionUp.}
\end{closurestatus}
