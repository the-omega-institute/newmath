\chapter{A Concrete Naming Certificate for $\NestedCauchyIntervalSelectionUp$}
\label{ch:concrete-instances-nested-cauchy-interval-selection-namecert}
\origin{human}

Bishop--Bridges nested interval/regular Cauchy construction treats a decreasing located interval chain with a displayed diameter schedule as finite approximation data before any real point is read. The $\NestedCauchyIntervalSelectionUp$ packet records that handoff as a NameCert route from the nested-interval theorem surface of \autoref{ch:concrete-instances-nested-interval-theorem-namecert} and the located endpoint surface of \autoref{ch:concrete-instances-locatedinterval-namecert} through the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It names the L10 route from nested interval data to regular Cauchy real names; it is not an interval-intersection oracle, quotient real equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Nested Cauchy interval-selection carrier]
\label{def:nested-cauchy-interval-selection-carrier}
A $\NestedCauchyIntervalSelectionUp$ carrier is a finite $\BHist$ packet
$$
S=(I,L,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\NestedIntervalTheoremUp$ source row, exposing the finite descending interval windows, containment rows, and diameter controls.
\item $L$ is the $\LocatedIntervalUp$ endpoint row, recording located lower and upper endpoint reads for the inspected intervals.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare interval diameter bounds with the requested regular Cauchy precision.
\item $W$ is the $\StreamNameUp$ finite-window row through which endpoint and midpoint observations are requested.
\item $R$ is the $\RegSeqRatUp$ regular Cauchy handoff row extracted from the nested interval windows and tolerance ledger.
\item $E$ is the terminal $\RealUp$ seal row reached only after $I,L,D,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for interval, located endpoint, dyadic tolerance, stream-window, regular readback, real seal, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a nested interval request through located endpoint reads, dyadic tolerances, stream windows, regular readback, and real seal handoff.
\item $P$ is the $\Pkg$ provenance over $\NestedCauchyIntervalSelectionUp$, $\NestedIntervalTheoremUp$, $\LocatedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\NestedCauchyIntervalSelectionUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $I,L,D,W,R,E,H,C,P,N$. It has no coordinate for a completed host intersection point, a quotient real representative, countable choice, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Nested Cauchy interval-selection NameCert obligations]
\label{thm:nested-cauchy-interval-selection-namecert-obligations}
For every accepted $\NestedCauchyIntervalSelectionUp$ carrier $S=(I,L,D,W,R,E,H,C,P,N)$, the local $\NameCert_{\NestedCauchyIntervalSelectionUp}$ surface consists exactly of carrier admission for the displayed rows, nested-interval exposure through $I$, located endpoint exposure through $L$, dyadic tolerance exactness through $D$, finite-window exposure through $W$, regular Cauchy handoff through $R$, terminal real seal confinement through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:nested-cauchy-interval-selection-carrier}. The rows $I,L,D,W,R,E,H,C,P,N$ are exactly the data visible to the local classifier.

The interval source and located endpoint rows are separate. The source row $I$ carries the descending windows and diameter controls, while $L$ carries the endpoint locatedness evidence needed to read those windows as rational approximation data.

The regular Cauchy handoff is downstream. The tolerance ledger $D$ converts displayed diameter bounds into requested precision rows, the window row $W$ supplies finite observations, and $R$ packages the resulting observations as a $\RegSeqRatUp$ handoff.

The real seal and structural rows do not add a hidden selector. The row $E$ is reached only after $I,L,D,W,R$ have been replayed, and $H,C,P,N$ preserve and name the same route. Hence the excluded host intersection, quotient representative, choice, trichotomy, compactness, and ambient-completeness coordinates are absent from the NameCert surface.
\end{proof}

\begin{theorem}[Nested Cauchy interval-selection regular Cauchy handoff]
\label{thm:nested-cauchy-interval-selection-regular-cauchy-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\NestedCauchyIntervalSelectionUp$ packet factors through the finite route
$$
\NestedIntervalTheoremUp \longmapsto \LocatedIntervalUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The route exports a regular Cauchy name read from displayed interval windows and diameter controls, not a completed intersection point or a host equality of real presentations.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $S=(I,L,D,W,R,E,H,C,P,N)$. A regular Cauchy request first reads the nested interval theorem row $I$ and the located endpoint row $L$.

Next the request consumes the dyadic tolerance ledger $D$ and finite-window row $W$. These rows identify which displayed interval prefix supplies the requested Cauchy precision.

The $\RegSeqRatUp$ row $R$ is then read as the regular Cauchy handoff. A $\RealUp$ consumer may proceed to $E$, but only through the same route and the structural rows $H,C,P,N$.

Since no carrier coordinate names a completed host intersection point, quotient real representative, countable-choice schedule, trichotomy split, open-cover compactness proof, or ambient $\RealUp$ completeness theorem, the handoff remains choice-free and finite.
\end{proof}

\falsifiablePrediction{Every accepted $\NestedCauchyIntervalSelectionUp$ read exposes NestedIntervalTheorem rows, LocatedInterval endpoint rows, DyadicRatCore tolerance rows, StreamName finite windows, a RegSeqRat handoff, a Real seal, hsame transport, Cont replay, Pkg provenance, and local NameCert evidence before any Real-facing consumer can use the packet.}

\independenceWitness{nested-interval-theorem, locatedinterval, regseqrat, dyadicratcore, streamname, real: $\NestedCauchyIntervalSelectionUp$ is not bijective to any listed sibling because it jointly carries the nested interval source, located endpoint evidence, dyadic tolerance ledger, finite-window route, regular Cauchy handoff, Real seal, transport, replay, provenance, and local naming.}

\closureat{NestedCauchyIntervalSelectionUp}{seedStr}

\begin{closurestatus}{\NestedCauchyIntervalSelectionUp}
  \constructivestory{A $\NestedCauchyIntervalSelectionUp$ packet is generated from a NestedIntervalTheorem source row, a LocatedInterval endpoint row, a DyadicRatCore tolerance ledger, StreamName finite windows, a RegSeqRat regular Cauchy handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nested-cauchy-interval-selection-carrier}, \autoref{thm:nested-cauchy-interval-selection-namecert-obligations}, and \autoref{thm:nested-cauchy-interval-selection-regular-cauchy-handoff} seed the finite route from nested interval data to regular Cauchy real names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host interval intersection, quotient real equality, selected limit points, countable choice, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, nested interval exposure, located endpoint exposure, dyadic tolerance exactness, regular Cauchy handoff, Real seal confinement, and field-faithful TasteGate evidence for BEDC.Derived.NestedCauchyIntervalSelectionUp.}
\end{closurestatus}
