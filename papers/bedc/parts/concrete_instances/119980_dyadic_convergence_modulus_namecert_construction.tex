\chapter{A Concrete Naming Certificate for $\DyadicConvergenceModulusUp$}
\label{ch:concrete-instances-dyadic-convergence-modulus-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names, the $\DyadicConvergenceModulusUp$ packet records the finite tolerance ledger by which dyadic convergence budgets are read before a terminal real-name seal is exposed. It sits over the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name chapter of \autoref{ch:concrete-instances-real-namecert}. The packet names a displayed convergence-control object for Real-facing consumers; it does not assert quotient equality of real names, selected limits, countable choice, or ambient completeness of $\RealUp$.

\begin{definition}[Dyadic convergence modulus carrier]
\label{def:dyadic-convergence-modulus-carrier}
A $\DyadicConvergenceModulusUp$ carrier is a finite $\BHist$ packet
$$
M_{\mathrm{dy}}=(D,S,Q,E,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the dyadic tolerance row, storing the requested dyadic error bound, the comparison scale, and the finite arithmetic witnesses read from $\DyadicRatCoreUp$.
\item $S$ is the finite $\StreamNameUp$ window row, listing the observations used to compare the relevant prefixes of the candidate names.
\item $Q$ is the $\RegSeqRatUp$ readback row, turning the observed windows into regular rational comparison data.
\item $E$ is the terminal $\RealUp$ seal reached only after $D,S,Q$ have been displayed.
\item $M$ is the convergence modulus ledger, recording the finite index budget that makes the $D$-tolerance stable for every later inspected window.
\item $H$ records componentwise $\hsame$ transport for the dyadic tolerance, stream-window, regular readback, Real seal, modulus ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a convergence request through $D,S,Q,E,M,H$.
\item $P$ is the $\Pkg$ provenance over $\DyadicConvergenceModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicConvergenceModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,Q,E,M,H,C,P,N$. It has no coordinate for quotient Real equality, selected limits, countable choice, arbitrary Cauchy completeness, or a host convergence theorem detached from the displayed dyadic budget.
\end{definition}

\begin{theorem}[Dyadic convergence modulus NameCert obligations]
\label{thm:dyadic-convergence-modulus-namecert-obligations}
For every accepted $\DyadicConvergenceModulusUp$ carrier
$$
M_{\mathrm{dy}}=(D,S,Q,E,M,H,C,P,N),
$$
the local $\NameCert_{\DyadicConvergenceModulusUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item expose the dyadic tolerance row $D$ before any convergence budget is consumed;
\item route every inspected prefix through the finite stream windows $S$ and the regular rational readback row $Q$;
\item display the modulus ledger $M$ as the only source of later-index stability for the requested dyadic tolerance;
\item reach the terminal $\RealUp$ seal $E$ only after $D,S,Q,M$ have been displayed;
\item preserve the route by componentwise $\hsame$ transport and displayed $\Cont$ replay through $H,C$;
\item source and name the same finite packet through $\Pkg$ provenance $P$ and the local $\NameCert$ row $N$.
\end{enumerate}
No obligation row states quotient Real equality, selects a limiting Real name, supplies countable choice, proves ambient $\RealUp$ completeness, or exports Lean verification.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-convergence-modulus-carrier}. Its rows are exactly the dyadic tolerance data, finite stream windows, regular readback, terminal seal, modulus ledger, transport, replay, provenance, and local naming.

Read the numerical rows first. The dyadic tolerance row $D$ fixes the precision request; the stream row $S$ and the regular rational row $Q$ provide the finite observations against which that request is checked.

The convergence control is then the ledger row $M$. It records the finite index budget that makes the displayed tolerance stable for later inspected windows, without invoking a selected limit or an ambient completeness principle.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same route. Since the carrier has no coordinates for quotient Real equality, countable choice, selected limits, ambient completeness, or Lean verification, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Dyadic convergence modulus Real readback]
\label{thm:dyadic-convergence-modulus-real-readback}
Every Real-facing read of an accepted $\DyadicConvergenceModulusUp$ carrier factors through
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert_{\DyadicConvergenceModulusUp}.
$$
The read exposes a finite dyadic convergence budget before the $\RealUp$ seal is consumed. It supplies no quotient equality of real names, selected limit, countable-choice schedule, ambient Real completeness theorem, or Lean verification claim.
\end{theorem}
\begin{proof}
Begin with the obligation theorem \autoref{thm:dyadic-convergence-modulus-namecert-obligations}. It orders $D,S,Q,M$ before the terminal seal $E$, so a Real-facing consumer cannot read the seal without first seeing the dyadic tolerance, finite windows, regular readback, and modulus ledger.

The displayed route is finite at each stage. The $\DyadicRatCoreUp$ row supplies arithmetic tolerance data, $\StreamNameUp$ supplies inspected windows, and $\RegSeqRatUp$ supplies regular rational readback before $\RealUp$ is reached.

The structural rows $H,C,P,N$ preserve and name the same packet. They do not add quotient equality, selected limits, countable choice, ambient completeness, or Lean verification, so the Real readback remains the displayed finite dyadic convergence-control route.
\end{proof}

\closureat{DyadicConvergenceModulusUp}{seedStr}

\begin{closurestatus}{\DyadicConvergenceModulusUp}
  \constructivestory{A $\DyadicConvergenceModulusUp$ packet is generated from dyadic tolerance data, finite StreamName windows, RegSeqRat readback, a terminal Real seal, a convergence modulus ledger, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-convergence-modulus-carrier}, \autoref{thm:dyadic-convergence-modulus-namecert-obligations}, and \autoref{thm:dyadic-convergence-modulus-real-readback} seed the finite dyadic convergence-control route over DyadicRatCore, StreamName, RegSeqRat, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient Real equality, selected limits, countable choice, ambient $\RealUp$ completeness, arbitrary convergence theorems, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exactness, StreamName window routing, RegSeqRat readback, Real seal nonescape, modulus ledger stability, transport, replay, provenance, and local naming for BEDC.Derived.DyadicConvergenceModulusUp.}
\end{closurestatus}
