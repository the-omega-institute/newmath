\chapter{A Concrete Naming Certificate for $\RealSequenceClusterPointUp$}
\label{ch:concrete-instances-real-sequence-cluster-point-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\RealSequenceClusterPointUp$ packet records the finite carrier surface for a cluster point of a real sequence between the displayed limit interface of \autoref{ch:concrete-instances-real-sequence-limit-namecert} and the bounded-subsequence extraction route of \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}. It receives its sequence source from \autoref{ch:concrete-instances-boundedrealsequence-namecert}, its cofinal subsequence route from \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert}, and its downstream compactness consumer from \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}. The packet displays a local cluster seal without importing host subsequence choice, quotient equality of streams, or ambient $\RealUp$ completeness.

\begin{definition}[Real sequence cluster point carrier]\label{def:real-sequence-cluster-point-carrier}
A $\RealSequenceClusterPointUp$ carrier is a finite $\BHist$ packet
$$
K=(S,J,W,Q,D,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source real-sequence row, read only through the $\BoundedRealSequenceUp$ surface when a bounded source is required.
\item $J$ is the cofinal subsequence row, carrying the monotone index schedule and tail-access route supplied by the $\RegularCauchySubsequenceUp$ packet.
\item $W$ is the finite family of selected $\StreamNameUp$ windows for the source entries named by $J$ and for the proposed cluster real.
\item $Q$ is the $\RegSeqRatUp$ readback row for those selected windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger that records the inspected cluster comparisons.
\item $R$ is the $\RealUp$ cluster seal reached after the selected windows, regular readbacks, and dyadic tolerance rows have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the source sequence, cofinal subsequence route, selected windows, regular readbacks, tolerance ledger, cluster seal, continuation, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ routes from source indices to selected windows, from selected windows to regular readback, from regular readback to the dyadic tolerance ledger, and from the ledger to the $\RealUp$ cluster seal.
\item $P$ is the $\Pkg$ provenance over $\RealSequenceClusterPointUp$, $\BoundedRealSequenceUp$, $\RegularCauchySubsequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealSequenceClusterPointUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,J,W,Q,D,R,H,C,P,N$. It has no coordinate for host subsequence choice, quotient-stream equality, full sequence convergence, compactness of arbitrary covers, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real sequence cluster point NameCert obligations]\label{thm:real-sequence-cluster-point-namecert-obligations}
For every accepted $\RealSequenceClusterPointUp$ carrier $K=(S,J,W,Q,D,R,H,C,P,N)$, the local $\NameCert_{\RealSequenceClusterPointUp}$ surface consists exactly of carrier admission for the ten displayed rows, source-sequence access through $S$, cofinal subsequence access through $J$, selected $\StreamNameUp$ window coverage through $W$, $\RegSeqRatUp$ readback through $Q$, dyadic cluster tolerance accounting through $D$, the $\RealUp$ cluster seal through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.RealSequenceClusterPointUp.RealSequenceClusterPointTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the carrier of \autoref{def:real-sequence-cluster-point-carrier}. The only sequence data available to the classifier are the source row $S$, the cofinal subsequence row $J$, the selected stream windows $W$, the regular rational readback $Q$, and the tolerance ledger $D$.

The cluster seal is then read through $R$. This places the proposed cluster point behind the displayed $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ rows rather than behind a host convergence predicate.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since no other coordinate is present, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Real sequence cluster point subsequence handoff]\label{thm:real-sequence-cluster-point-subsequence-handoff}
Every accepted $\RealSequenceClusterPointUp$ carrier factors its cluster-point read through the finite route
$$
S\longmapsto J\longmapsto W\longmapsto Q\longmapsto D\longmapsto R
$$
together with $H,C,P,N$. Consequently a consumer may pass from the displayed sequence-limit surface of \autoref{ch:concrete-instances-real-sequence-limit-namecert} to the cofinal subsequence route of \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert} without importing host subsequence choice or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealSequenceClusterPointUp.RealSequenceClusterPointSubsequence\_handoff}
\begin{proof}
Start with the source sequence row $S$. The cofinal row $J$ restricts attention to the displayed subsequence indices supplied by the regular Cauchy subsequence route.

Next read the selected windows $W$ and their regular rational readback $Q$. The dyadic ledger $D$ records only the inspected cluster comparisons for those windows, so the route remains finite at every observation level.

Finally consume the real cluster seal $R$ and the structural rows $H,C,P,N$. The handoff therefore uses the subsequence certificate as a displayed carrier row and exports no host-level selector or completeness principle.
\end{proof}

\begin{theorem}[Real sequence cluster point public export]\label{thm:real-sequence-cluster-point-public-export}
The $\BolzanoWeierstrassUp$ consumer of \autoref{ch:concrete-instances-bolzanoweierstrass-namecert} may read a cluster candidate from an accepted $\RealSequenceClusterPointUp$ carrier only through the bounded source row of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the cofinal subsequence row of \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert}, and the displayed route
$$
W,Q,D,R,H,C,P,N.
$$
Thus the exported surface supplies a local cluster-point certificate for the real-and-completion boundary while leaving host subsequence choice, quotient-stream equality, open-cover compactness, trichotomy, and ambient $\RealUp$ completeness outside the packet.
\end{theorem}
\begin{proof}
Use \autoref{thm:real-sequence-cluster-point-subsequence-handoff}. It places every cluster read on the route from the source sequence through the cofinal subsequence row, selected windows, readback, dyadic ledger, and real cluster seal.

The bounded source row is read through \autoref{ch:concrete-instances-boundedrealsequence-namecert}. The downstream $\BolzanoWeierstrassUp$ carrier can therefore consume a bounded sequence source, a cofinal subsequence surface, and a cluster seal without receiving an independent compactness or choice coordinate.

The remaining rows $H,C,P,N$ preserve and name the displayed route. Since the carrier has no coordinate for host subsequence choice, quotient-stream equality, open-cover compactness, trichotomy, or ambient real completeness, none of those principles is part of the public export.
\end{proof}

\falsifiablePrediction{Every $\BolzanoWeierstrassUp$ cluster-candidate read factors through a $\RealSequenceClusterPointUp$ route displaying a bounded source row, cofinal subsequence row, selected StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealUp cluster seal, transport, continuation, provenance, and local naming.}

\independenceWitness{real-sequence-limit, regularcauchysubsequence, boundedrealsequence, bolzanoweierstrass: $\RealSequenceClusterPointUp$ is not bijective to any listed sibling because it jointly carries source sequence access, cofinal subsequence access, selected windows, regular readback, tolerance accounting, cluster sealing, transport, continuation, provenance, and local naming.}

\closureat{RealSequenceClusterPointUp}{seedStr}

\begin{closurestatus}{\RealSequenceClusterPointUp}
  \constructivestory{A $\RealSequenceClusterPointUp$ packet is generated from a source real-sequence row, a cofinal subsequence row, selected StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealUp cluster seal, componentwise transport, displayed continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-sequence-cluster-point-carrier}, \autoref{thm:real-sequence-cluster-point-namecert-obligations}, \autoref{thm:real-sequence-cluster-point-subsequence-handoff}, and \autoref{thm:real-sequence-cluster-point-public-export} bind the finite cluster-point certificate surface between displayed sequence limits and Bolzano-Weierstrass.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host subsequence choice, quotient equality of streams, open-cover compactness, trichotomy, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, NameCert obligations, subsequence handoff, public export, and TasteGate field faithfulness for BEDC.Derived.RealSequenceClusterPointUp.}
\end{closurestatus}
