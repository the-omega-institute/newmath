\begin{theorem}[SeparableSpace real-metric dependency route]
\label{thm:separable-space-real-metric-dependency-route}
Every completion, compactness, or real-facing consumer of an accepted $\SeparableSpaceUp$ dense schedule reads the metric carrier data of \autoref{ch:concrete-instances-metric-namecert} and the Real-density route of \autoref{ch:concrete-instances-real-separability-namecert} before it may inspect the downstream separability surface. The route exposes countable dense data only through displayed $\MetricUp$, $\RealSeparabilityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows; it exports no host enumeration, selected dense point, quotient point equality, countable-choice selector, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the accepted dense schedule. Its metric source is the row that supplies distance observations, while the real-separability row supplies the Real-facing density readback.

The existing forward route through \autoref{thm:separable-space-real-separability-forward-route} places the Real-density consumer after the metric, stream, regular rational, terminal Real, and countable-density ledgers. Thus the child dependency route is not a separate carrier; it records the horizontal lattice edge through which completion and compactness consumers must enter the same finite packet.

Since the carrier has no coordinate for a host enumeration, selected dense point, quotient equality, countable-choice selector, or ambient completeness theorem, every such consumer remains inside the displayed metric and Real-separability dependency route.
\end{proof}
