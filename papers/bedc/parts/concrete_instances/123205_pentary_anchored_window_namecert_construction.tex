\chapter{A Concrete Naming Certificate for $\PentaryAnchoredWindowUp$}
\label{ch:concrete-instances-pentary-anchored-window-namecert}
\origin{ai}

The $\PentaryAnchoredWindowUp$ packet realizes the anchored-window obstruction of \autoref{ch:visions-innovation-input-pentary-anchored-window-boundary} as finite NameCert data. It sits beside the pentary Hermitian rigidity packet of \autoref{ch:concrete-instances-pentary-hermitian-rigidity-namecert}, the gate route of \autoref{ch:concrete-instances-pentary-hermitian-rigidity-gate-namecert}, the recursive axis normal form of \autoref{ch:concrete-instances-recursive-orthogonal-axis-normal-form-namecert}, the finite-block reversibility boundary of \autoref{ch:concrete-instances-finite-block-reversibility-boundary-namecert}, and the interval-certificate rows of \autoref{ch:concrete-instances-lagarias-interval-certificate-namecert}. The packet names anchored finite-window correlation rows only; it does not turn the vision formulas into a global reflection-positivity theorem or a host search oracle.

\begin{definition}[Pentary anchored-window NameCert carrier]
\label{def:pentary-anchored-window-namecert-carrier}
A $\PentaryAnchoredWindowUp$ carrier is a finite $\BHist$ packet
$$
W=(Q,A,L_2,L_3,D,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the pentary Q3 row recording the displayed phase word and its finite row-sum certificate.
\item $A$ is the anchor row naming the reflection plane and the future-block offsets inspected by the window.
\item $L_2$ is the anchored $L=2$ neutral RP matrix row, including the displayed $\mu$, $D$, and $C$ correlation entries.
\item $L_3$ is the anchored $L=3$ extension row, carrying the next link diagonal and the finite obstruction witness.
\item $D$ is the diagonal-status row comparing the retained $L=2$ pass with the displayed $L=3$ failure.
\item $F$ is the finite formula row imported from the source vision chapter: it records the residue computations for the Pythagorean pentary row and the anchor-sensitive RP audit.
\item $H$ records componentwise $\hsame$ transport for the Q3, anchor, $L=2$, $L=3$, diagonal-status, formula, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the finite anchored-window formulas, diagonal checks, anchor-sensitive audit row, provenance, and local naming.
\item $P$ records $\Pkg$ provenance over $\PentaryAnchoredWindowUp$, $\PentaryHermitianRigidityUp$, $\PentaryHermitianRigidityGateUp$, $\RecursiveOrthogonalAxisNormalFormUp$, $\FiniteBlockReversibilityBoundaryUp$, $\LagariasIntervalCertificateUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\PentaryAnchoredWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,A,L_2,L_3,D,F,H,C,P,N$. It has no coordinate for a complete pentary feasibility solver, a global RP theorem, a host complex-number oracle, or an unanchored translation-invariant replacement of the displayed windows.
\end{definition}

\begin{theorem}[Pentary anchored-window NameCert obligations]
\label{thm:pentary-anchored-window-namecert-obligations}
For every accepted $\PentaryAnchoredWindowUp$ carrier
$$
W=(Q,A,L_2,L_3,D,F,H,C,P,N),
$$
the local $\NameCert_{\PentaryAnchoredWindowUp}$ surface consists exactly of carrier admission for the pentary Q3 row, anchor row, anchored $L=2$ matrix row, anchored $L=3$ extension row, diagonal-status row, finite formula row, componentwise transport, displayed replay, package provenance, and local naming. A consumer may read the obstruction only after the same anchor has been displayed for both the retained $L=2$ window and the failing next anchored link.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pentary-anchored-window-namecert-carrier}. The visible analytic rows are exactly $Q,A,L_2,L_3,D,F$.

The anchor row $A$ is shared by both window rows. Thus the $L=2$ pass and the $L=3$ obstruction are not separate translation-shape facts; they are two reads against the same displayed reflection plane.

The diagonal-status row $D$ records the retained nearest-window pass and the next-link failure. The formula row $F$ supplies the finite residue computations from the source vision chapter.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name that same finite surface. Since the carrier has no solver, global RP theorem, host oracle, or unanchored replacement row, the local certificate exports only the displayed anchored-window packet.
\end{proof}

\falsifiablePrediction{A claimed $\PentaryAnchoredWindowUp$ read is rejected if the $L=2$ matrix and the $L=3$ next-link diagonal are computed against different anchor rows.}

\independenceWitness{pentary-hermitian-rigidity, pentary-hermitian-rigidity-gate, recursive-orthogonal-axis-normal-form, finite-block-reversibility-boundary, lagarias-interval-certificate: none of these siblings carries the joint Q3 row, shared anchor row, $L=2$ pass row, $L=3$ obstruction row, finite formula row, transport, replay, provenance, and local naming as one packet.}

\closureat{PentaryAnchoredWindowUp}{seedStr}

\begin{closurestatus}{\PentaryAnchoredWindowUp}
  \constructivestory{A $\PentaryAnchoredWindowUp$ packet is generated from a pentary Q3 phase row, a shared anchor row, anchored L2 matrix data, anchored L3 extension data, a diagonal-status row, finite formula rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pentary-anchored-window-namecert-carrier} and \autoref{thm:pentary-anchored-window-namecert-obligations} seed the finite anchored-window packet for the pentary correlation rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a complete pentary feasibility solver, a global reflection-positivity theorem, a host complex-number oracle, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-anchor exposure, L2 matrix exactness, L3 extension exactness, diagonal-status nonescape, finite formula replay, transport, provenance, and local naming.}
\end{closurestatus}
