After the complete metric row of \autoref{def:frechet-space-carrier} has been displayed, any $\FrechetSpaceUp$ consumer that needs a Baire-category conclusion must leave the Frechet carrier through the theorem route of \autoref{ch:concrete-instances-baire-category-theorem-namecert} and the inspected complete-metric surface of \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert}. The Frechet packet supplies local convexity, topological-vector continuity, compatible metric data, and completion-facing rows; the dense-open theorem route is a separate sibling surface.

\begin{theorem}[Frechet Baire dependency route]
\label{thm:frechet-space-baire-dependency-route}
Every accepted $\FrechetSpaceUp$ read that asks for Baire-category evidence factors first through the complete metric row $C$ and the $\MetricCompletionUp$ handoff $Q$ of \autoref{def:frechet-space-carrier}, and then through the $\BaireCategoryTheoremUp$ and $\CompleteMetricBaireSurfaceUp$ chapters cited above. The route adds no Hahn--Banach separation, selected generic point, countable-choice schedule, host intersection object, or ambient complete locally convex space to the Frechet carrier.
\end{theorem}
\begin{proof}
Project the Frechet carrier as $F=(V,L,M,C,Q,H,R,P,N)$. The rows $V,L,M,C,Q$ expose topological-vector structure, local convexity, compatible metric data, completeness evidence, and the metric-completion boundary before any theorem consumer reads the packet.

A Baire-category request is not one of those Frechet rows. Route it through \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert} for the inspected complete-metric Baire surface and through \autoref{ch:concrete-instances-baire-category-theorem-namecert} for the theorem-level dense-open package.

The remaining rows $H,R,P,N$ only transport, replay, source, and name the displayed route. Since no row stores Hahn--Banach separation, a selected generic point, a countable-choice schedule, a host intersection object, or an ambient complete locally convex space, none can be exported by the dependency route.
\end{proof}

\begin{theorem}[Frechet Baire-category completeness route]
\label{thm:frechet-space-baire-category-completeness-route}
Every $\FrechetSpaceUp$ consumer that needs dense-open completeness must expose the full carrier
$$
F=(V,L,M,C,Q,H,R,P,N)
$$
and then pass through \autoref{ch:concrete-instances-baire-category-theorem-namecert} and \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert}. The Frechet chapter contributes the displayed vector, local-convex, compatible-metric, complete-metric, completion-handoff, transport, replay, provenance, and local naming rows; the dense-open conclusion remains a theorem-level sibling route rather than a hidden field of $F$.
\end{theorem}
\begin{proof}
Project the accepted carrier as $F=(V,L,M,C,Q,H,R,P,N)$. The rows $V,L,M,C,Q$ expose the topological-vector, local-convex, compatible-metric, complete-metric, and completion-handoff data needed before a dense-open consumer can even form its request.

Now apply the theorem route. The complete-metric surface is supplied by \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert}, while the dense-open theorem statement is supplied by \autoref{ch:concrete-instances-baire-category-theorem-namecert}. These two sibling chapters are therefore the only exits from the Frechet carrier toward Baire-category completeness.

The remaining rows $H,R,P,N$ preserve and name the route. Since they contain only componentwise transport, replay, provenance, and the local $\NameCert_{\FrechetSpaceUp}$ row, they cannot introduce a selected generic point, a countable-choice schedule, or an ambient complete locally convex space outside the displayed dependency route.
\end{proof}
