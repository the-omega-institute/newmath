Every $\FrechetSpaceUp$ consumer reads the locally convex linear-topology surface of \autoref{ch:concrete-instances-locally-convex-space-namecert} before it treats the compatible metric as complete. The route then passes through the metric-completion handoff of \autoref{ch:concrete-instances-metriccompletion-namecert}, so Baire-category and metric-completion readers receive the displayed locally convex, complete-metric, transport, replay, provenance, and local naming rows rather than a host Frechet space or an unbounded seminorm schedule.
