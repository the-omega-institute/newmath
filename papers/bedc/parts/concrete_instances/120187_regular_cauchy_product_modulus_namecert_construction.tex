\chapter{A Concrete Naming Certificate for $\RegularCauchyProductModulusUp$}
\label{ch:concrete-instances-regular-cauchy-product-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyProductModulusUp$ packet records the finite product-modulus boundary needed to read multiplication of displayed regular rational Cauchy names through bounded windows. It sits over the $\RegSeqRatUp$ source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the synchronized window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the $\DyadicRatCoreUp$ arithmetic rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the product carrier of \autoref{ch:concrete-instances-regular-cauchy-multiplication-namecert}. The packet is a finite modulus certificate for the product window; it is not quotient real multiplication, a selected limit, trichotomy, an ambient completeness theorem, or a host real norm.

\begin{definition}[RegularCauchyProductModulus carrier]
\label{def:regular-cauchy-product-modulus-carrier}
A $\RegularCauchyProductModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(X,Y,W,D,R,P,E,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two $\RegSeqRatUp$ source rows for regular rational Cauchy real names.
\item $W$ is the $\StreamNameUp$ product-window ledger that lists the finite source observations inspected for each requested output precision.
\item $D$ is the $\DyadicRatCoreUp$ error-budget row containing the displayed source tolerances, product tolerance, and slack allocation used by the window computation.
\item $R$ is the regular readback row that turns the windowed dyadic product data into a $\RegSeqRatUp$ product-name prefix.
\item $P$ is the product-modulus row. For each requested output precision it stores the finite source-window depth and dyadic slack sufficient for the product readback.
\item $E$ is the $\RealUp$ seal row reached only after $X,Y,W,D,R,P$ have supplied the regular product-modulus boundary.
\item $H$ records componentwise $\hsame$ transport for the source rows, product windows, dyadic error budget, regular readback, product modulus, real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from any product-modulus precision request through $X,Y,W,D,R,P,E,H,Q,N$.
\item $Q$ is the $\Pkg$ provenance over $\RegularCauchyProductModulusUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyMultiplicationUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyProductModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,W,D,R,P,E,H,C,Q,N$. The carrier has no coordinate for quotient real multiplication, selected limits, trichotomy, global completeness, host real norms, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Regular Cauchy product window bound]
\label{thm:regular-cauchy-product-modulus-window-bound}
For every accepted $\RegularCauchyProductModulusUp$ carrier
$$
M=(X,Y,W,D,R,P,E,H,C,Q,N),
$$
each product read requested at a displayed output precision factors through a finite window bound recorded in $P$ before the regular readback row $R$ or the $\RealUp$ seal row $E$ may be consumed.
\end{theorem}

\begin{proof}
Start with the precision request handled by the accepted carrier. The only row that assigns a product-facing source-window depth is $P$, and $P$ is keyed to the synchronized observations listed by $W$ and to the dyadic slack listed by $D$.

Once $P$ has named that finite bound, the readback row $R$ may use only the displayed source prefixes from $X$ and $Y$, the window ledger $W$, and the error-budget row $D$. Thus the product readback is determined before any terminal real-name seal is exposed.

The $\RealUp$ seal row $E$ is downstream from $X,Y,W,D,R,P$. Since the carrier contains no host norm, quotient Cauchy class, selected limit, or ambient completeness coordinate, every accepted product read reaches $E$ only through the finite window bound already recorded in $P$.
\end{proof}

\begin{theorem}[Regular Cauchy product-modulus NameCert obligations]
\label{thm:regular-cauchy-product-modulus-namecert-obligations}
For every accepted $\RegularCauchyProductModulusUp$ carrier
$$
M=(X,Y,W,D,R,P,E,H,C,Q,N),
$$
the local $\NameCert_{\RegularCauchyProductModulusUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ source rows, synchronized $\StreamNameUp$ product windows, $\DyadicRatCoreUp$ error-budget row, regular readback, product-modulus row, $\RealUp$ seal, and structural rows;
\item source-window exposure, requiring each product-modulus request to inspect only the finite source prefixes named by $W$;
\item dyadic error-budget exposure, requiring $D$ to carry the displayed source tolerances, product tolerance, and slack allocation used by the product bound;
\item product-modulus determinacy, requiring $P$ to name the finite bound before the readback row $R$ or the seal row $E$ is available;
\item regular readback compatibility, requiring $R$ to be the $\RegSeqRatUp$ prefix obtained from the rows $X,Y,W,D,P$;
\item Real seal nonescape, requiring every $\RealUp$ consumer to read through $X,Y,W,D,R,P$ before $E$ is exposed;
\item componentwise transport, replay, provenance, and local naming, requiring $H,C,Q,N$ to preserve and name exactly the same finite product-modulus packet;
\item refusal of quotient real multiplication, selected limits, trichotomy, global completeness, host real norms, Lean verification, and bridge checking as local NameCert data.
\end{enumerate}
No product-modulus obligation is stored outside these finite rows.
\end{theorem}

\falsifiablePrediction{An accepted $\RegularCauchyProductModulusUp$ read is refuted if a product-facing precision request reaches $R$ or $E$ without a finite source-window bound in $P$, a synchronized $\StreamNameUp$ window in $W$, and a $\DyadicRatCoreUp$ error-budget row in $D$.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real, regular-cauchy-multiplication: $\RegularCauchyProductModulusUp$ is not bijective to any listed sibling because it jointly carries two regular Cauchy source rows, product windows, dyadic error budgets, a regular readback row, a product-modulus row, a real seal, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyProductModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchyProductModulusUp}
  \constructivestory{A $\RegularCauchyProductModulusUp$ packet is generated from two RegSeqRat source rows, a finite StreamName product-window ledger, a DyadicRatCore error-budget row, a regular readback row, a product-modulus row, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-product-modulus-carrier}, \autoref{thm:regular-cauchy-product-modulus-window-bound}, and \autoref{thm:regular-cauchy-product-modulus-namecert-obligations} seed the finite product-modulus boundary consumed by regular Cauchy multiplication and Real-facing completion readers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient real multiplication, selected limits, trichotomy, host real norms, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-window exposure, dyadic error-budget exposure, product-modulus determinacy, regular readback compatibility, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyProductModulusUp.}
\end{closurestatus}
