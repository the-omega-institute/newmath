\chapter{A Concrete Naming Certificate for $\ParacompactPartitionOfUnityUp$}
\label{ch:concrete-instances-paracompact-partition-of-unity-namecert}
\origin{human}

Following Kelley and Bourbaki on paracompact refinements and subordinate partitions of unity, the $\ParacompactPartitionOfUnityUp$ packet records the finite BEDC handoff by which a displayed paracompact locally finite refinement is read as partition-of-unity weight rows. It sits between the paracompact refinement carrier of \autoref{ch:concrete-instances-paracompact-namecert}, the locally finite cover surface of \autoref{ch:concrete-instances-locally-finite-cover-namecert}, the Urysohn separation-function route of \autoref{ch:concrete-instances-urysohn-lemma-namecert}, the partition surface of \autoref{ch:concrete-instances-partition-of-unity-namecert}, and the chart-facing consumer rows of \autoref{ch:concrete-instances-manifold-namecert}. The packet names only displayed topology, refinement, locally finite, bump, weight, transport, replay, provenance, and local naming rows; it does not select an arbitrary host subcover, import countable choice, assert open-cover compactness, or identify analytic functions extensionally.

\begin{definition}[Paracompact partition of unity carrier]
\label{def:paracompact-partition-of-unity-carrier}
A $\ParacompactPartitionOfUnityUp$ carrier is a finite $\BHist$ packet
$$
U=(T,Q,L,R,W,F,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\TopologyUp$ source row, exposing the open-set and neighbourhood reads used by the cover and support data.
\item $Q$ is the $\ParacompactUp$ refinement row, carrying the displayed cover request, refinement assignment, and coverage ledger.
\item $L$ is the $\LocallyFiniteCoverUp$ row, recording locally finite refinement cells, finite neighbourhood witnesses, and assignment back to the original cover request.
\item $R$ is the $\UrysohnLemmaUp$ bump-row route, recording separation-compatible local functions for the displayed support windows.
\item $W$ is the $\PartitionOfUnityUp$ finite weight row, listing the finitely many nonzero weights visible at each displayed neighbourhood read.
\item $F$ is the displayed sum-to-one row over the finite support named by $W$.
\item $M$ is the $\ManifoldUp$ handoff row, exposing only the partition rows consumable by chart gluing and local-to-global analysis.
\item $H$ records componentwise $\hsame$ transport for $T,Q,L,R,W,F,M,C,P,N$.
\item $C$ records displayed $\Cont$ replay from the paracompact refinement request through the locally finite, bump, weight, normalization, and handoff rows.
\item $P$ is the $\Pkg$ provenance over $\ParacompactPartitionOfUnityUp$, $\TopologyUp$, $\ParacompactUp$, $\LocallyFiniteCoverUp$, $\UrysohnLemmaUp$, $\PartitionOfUnityUp$, $\ManifoldUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ParacompactPartitionOfUnityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,Q,L,R,W,F,M,H,C,P,N$. It has no coordinate for a selected host subcover, unrestricted function family, countable-choice schedule, open-cover compactness theorem, quotient equality of functions, or chart choice outside the displayed handoff row.
\end{definition}

\begin{theorem}[Paracompact partition of unity NameCert obligations]
\label{thm:paracompact-partition-of-unity-namecert-obligations}
For every accepted $\ParacompactPartitionOfUnityUp$ carrier
$$
U=(T,Q,L,R,W,F,M,H,C,P,N),
$$
the local $\NameCert_{\ParacompactPartitionOfUnityUp}$ surface consists exactly of carrier admission, topology-source exposure, paracompact refinement exposure, locally finite cover exactness, Urysohn-compatible bump exactness, finite weight and sum exactness, manifold handoff exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every partition consumer must read the finite route
$$
\TopologyUp \longrightarrow \ParacompactUp \longrightarrow \LocallyFiniteCoverUp \longrightarrow \UrysohnLemmaUp \longrightarrow \PartitionOfUnityUp \longrightarrow \ManifoldUp
$$
before treating the weight rows as gluing data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:paracompact-partition-of-unity-carrier}. The accepted tuple displays exactly the topology source, paracompact refinement, locally finite cover, Urysohn bump, finite weight, sum-to-one, manifold handoff, transport, replay, provenance, and local naming rows.

The cover side is read through $T,Q,L$. The topology row supplies the open-set interpretation, the paracompact row supplies the refinement assignment and coverage ledger, and the locally finite row supplies the finite neighbourhood witnesses without selecting a host subcover.

The partition side is read through $R,W,F,M$. The bump row passes through the Urysohn separation route, the weight row lists only finitely many visible nonzero weights, the sum row records the displayed unit total, and the manifold handoff exports only those rows needed by chart gluing.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no further coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Paracompact partition of unity topology handoff]
\label{thm:paracompact-partition-of-unity-topology-handoff}
Every topology-facing or manifold-facing consumer of an accepted $\ParacompactPartitionOfUnityUp$ packet must pass through the $\TopologyUp$ source row, the $\ParacompactUp$ refinement row, and the $\LocallyFiniteCoverUp$ row before it reads Urysohn bump rows, partition weights, the sum-to-one row, or chart-gluing data. The handoff is a finite route from displayed open-cover refinements to displayed weights, not an ambient theorem selecting functions over a host topology.
\end{theorem}
\begin{proof}
Use the obligation route in \autoref{thm:paracompact-partition-of-unity-namecert-obligations}. It places the topology source $T$, refinement row $Q$, and locally finite row $L$ before the partition rows $R,W,F,M$.

The bump and weight rows are downstream of that route. A bump is admitted only through $R$, a weight is visible only through the finite local witness in $W$, and the sum row $F$ records only the displayed finite support.

The handoff row $M$ exports this same route to manifold consumers. Therefore no consumer receives an unrestricted function family, host subcover selector, or ambient topology theorem from the packet.
\end{proof}

\begin{theorem}[Paracompact partition of unity nonescape]
\label{thm:paracompact-partition-of-unity-nonescape}
An accepted $\ParacompactPartitionOfUnityUp$ packet exports no selected host subcover, countable-choice schedule, open-cover compactness theorem, unrestricted partition-of-unity function family, quotient equality of functions, or unlogged chart choice. Any consumer of its partition rows must remain inside $T,Q,L,R,W,F,M,H,C,P,N$.
\end{theorem}
\begin{proof}
Read the classifier boundary in \autoref{def:paracompact-partition-of-unity-carrier}. Accepted packets are compared only through the displayed rows $T,Q,L,R,W,F,M,H,C,P,N$.

Apply \autoref{thm:paracompact-partition-of-unity-namecert-obligations}. It exhausts carrier admission, refinement exposure, local finiteness, bump exactness, finite weights, normalization, transport, replay, provenance, and local naming.

Finally use the topology handoff of \autoref{thm:paracompact-partition-of-unity-topology-handoff}. Since every consumer route is already forced through the displayed topology, refinement, locally finite, bump, weight, sum, and manifold rows, none of the refused host-level objects can be exported.
\end{proof}

\falsifiablePrediction{Every accepted $\ParacompactPartitionOfUnityUp$ read exposes Topology, Paracompact, LocallyFiniteCover, UrysohnLemma, PartitionOfUnity, Manifold, hsame, Cont, Pkg, and local NameCert rows before any partition or chart-gluing consumer reads it.}

\independenceWitness{topology, paracompact, locally-finite-cover, urysohn-lemma, partition-of-unity, manifold: $\ParacompactPartitionOfUnityUp$ is not bijective to any listed sibling because it jointly binds refinement, local finiteness, bump rows, finite weights, normalization, manifold handoff, transport, replay, provenance, and local naming in one finite certificate.}

\closureat{ParacompactPartitionOfUnityUp}{seedStr}

\begin{closurestatus}{\ParacompactPartitionOfUnityUp}
  \constructivestory{A $\ParacompactPartitionOfUnityUp$ packet is generated from a $\TopologyUp$ source row, $\ParacompactUp$ refinement row, $\LocallyFiniteCoverUp$ local-window ledger, $\UrysohnLemmaUp$ bump rows, $\PartitionOfUnityUp$ finite weight and sum rows, a $\ManifoldUp$ handoff row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:paracompact-partition-of-unity-carrier}, \autoref{thm:paracompact-partition-of-unity-namecert-obligations}, \autoref{thm:paracompact-partition-of-unity-topology-handoff}, and \autoref{thm:paracompact-partition-of-unity-nonescape} seed the finite handoff from paracompact locally finite refinements to partition-of-unity weight rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host-selected function families, arbitrary subcover selection, countable choice, open-cover compactness, analytic function extensionality, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, topology-source exposure, paracompact refinement exactness, locally finite cover exactness, Urysohn bump exactness, finite weight normalization, manifold handoff, transport, replay, provenance, and local naming for BEDC.Derived.ParacompactPartitionOfUnityUp.}
\end{closurestatus}
