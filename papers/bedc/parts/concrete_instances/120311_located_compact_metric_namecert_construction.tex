\chapter{A Concrete Naming Certificate for $\LocatedCompactMetricUp$}
\label{ch:concrete-instances-located-compact-metric-namecert}
\origin{human}

Following Bishop and Bridges constructive metric compactness, the $\LocatedCompactMetricUp$ packet records a compact metric source together with displayed located distance data and the finite-net readback route used by L10 Real and Completion consumers. It sits beside the metric-space source of \autoref{ch:concrete-instances-metric-space-namecert}, the compact-metric certificate of \autoref{ch:concrete-instances-compactmetric-namecert}, the finite epsilon-net route of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the complete-metric handoff of \autoref{ch:concrete-instances-completemetric-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite located compact metric surface only; it does not assert open-cover compactness, sequential compactness, countable choice, quotient completion, or ambient $\RealUp$ completeness.

\begin{definition}[Located compact metric carrier]
\label{def:located-compact-metric-carrier}
A $\LocatedCompactMetricUp$ carrier is a finite $\BHist$ packet
$$
L=(M,D,K,F,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricSpaceUp$ source row, naming the displayed metric carrier and metric-law ledger inspected by every distance request.
\item $D$ is the located distance witness row, recording finite lower and upper distance windows for each displayed point-to-source query.
\item $K$ is the $\CompactMetricUp$ row, exposing compact metric data only through displayed finite-net evidence.
\item $F$ is the $\FiniteEpsilonNetUp$ finite-net row, carrying center lists, tolerance labels, and coverage witnesses at inspected scales.
\item $T$ is the $\CompleteMetricUp$ route, naming the completion-facing Cauchy handoff without selecting a quotient representative or host limit.
\item $R$ is the $\DyadicRatCoreUp$ tolerance ledger used to measure radii, slack bounds, and finite comparison budgets.
\item $E$ is the paired $\RegSeqRatUp$ readback and $\RealUp$ seal row, exposing regular rational windows before any real-valued distance or compactness consumer can inspect the packet.
\item $H$ records componentwise $\hsame$ transport for the metric source, located distance witness, compact metric row, finite epsilon-net row, complete-metric route, dyadic ledger, readback seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a located compact metric request through $M,D,K,F,T,R,E,H$.
\item $P$ is the $\Pkg$ provenance over $\LocatedCompactMetricUp$, $\MetricSpaceUp$, $\CompactMetricUp$, $\FiniteEpsilonNetUp$, $\CompleteMetricUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCompactMetricUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,D,K,F,T,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, sequential compactness, countable choice, quotient completion equality, selected limits, host metric equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located compact metric NameCert obligations]
\label{thm:located-compact-metric-namecert-obligations}
For every accepted $\LocatedCompactMetricUp$ carrier
$$
L=(M,D,K,F,T,R,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedCompactMetricUp}$ surface consists exactly of MetricSpace source admission, located distance witness exposure, CompactMetric finite compactness data, FiniteEpsilonNet center and coverage rows, CompleteMetric completion-facing routing, DyadicRatCore tolerance accounting, RegSeqRat readback, RealUp sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-compact-metric-carrier}. The source metric row is $M$, the located distance row is $D$, and compactness is visible only through the compact metric row $K$ together with the finite epsilon-net row $F$.

The completion-facing side is then forced through $T,R,E$. The complete-metric row supplies the Cauchy handoff route, the dyadic ledger records the inspected tolerances, and the readback seal exposes regular rational data before the terminal real-name row is consumed.

Transport, replay, provenance, and local naming are exactly $H,C,P,N$. Since no listed row contains an open-cover theorem, a sequential-compactness selector, countable choice, quotient completion equality, selected limits, host metric equality, or ambient completeness, the displayed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Located compact metric finite-net completion route]
\label{thm:located-compact-metric-finite-net-completion-route}
Every completion-facing or real-facing consumer of an accepted $\LocatedCompactMetricUp$ packet must first read the route
$$
\MetricSpaceUp,\ \CompactMetricUp,\ \FiniteEpsilonNetUp,\ \CompleteMetricUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp
$$
through the same local $\NameCert_{\LocatedCompactMetricUp}$ carrier rows. The route exports finite centers, coverage witnesses, located distance windows, dyadic tolerance accounting, regular rational readback, and a terminal real seal; it exports no open-cover compactness, sequential compactness, countable choice, quotient completion, selected limit, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the metric and compact rows. The metric source $M$ gives the distance ledger, while $K$ and $F$ expose compactness only as displayed finite epsilon-net data.

Next read the located and completion rows. The located distance witness $D$ answers finite distance-window requests, and the complete-metric row $T$ names the handoff used by completion consumers without choosing a quotient representative or host limit.

Finally pass through the dyadic ledger and readback seal. Rows $R$ and $E$ keep the tolerance and real-facing data inside $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, and $H,C,P,N$ replay the same finite route. Thus the consumer receives exactly the listed finite surface and none of the refused endpoints.
\end{proof}

\falsifiablePrediction{Any accepted $\LocatedCompactMetricUp$ read that exposes compactness or completion-facing data without displaying MetricSpace source admission, located distance witness data, CompactMetric finite compactness rows, FiniteEpsilonNet coverage, CompleteMetric routing, DyadicRatCore tolerance accounting, RegSeqRat readback, RealUp sealing, hsame transport, Cont replay, Pkg provenance, and local NameCert data refutes the carrier.}

\independenceWitness{metricspace, compactmetric, finite-epsilon-net, completemetric, dyadicratcore, regseqrat, real: none is bijective with $\LocatedCompactMetricUp$ because this packet jointly binds metric source data, located distance windows, compact finite-net evidence, completion routing, dyadic tolerance accounting, regular readback, real sealing, transport, replay, provenance, and local naming.}

\closureat{LocatedCompactMetricUp}{seedStr}

\begin{closurestatus}{\LocatedCompactMetricUp}
  \constructivestory{A $\LocatedCompactMetricUp$ packet is generated from a MetricSpace source row, located distance witness, CompactMetric finite compactness data, FiniteEpsilonNet coverage rows, CompleteMetric routing, DyadicRatCore tolerance accounting, RegSeqRat readback, RealUp sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-compact-metric-carrier}, \autoref{thm:located-compact-metric-namecert-obligations}, and \autoref{thm:located-compact-metric-finite-net-completion-route} seed the located compact metric route over metric source, located distance, compact finite-net, completion, dyadic tolerance, regular readback, Real seal, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, sequential compactness, countable choice, quotient completion, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located distance exactness, finite-net coverage, completion routing, dyadic tolerance accounting, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.LocatedCompactMetricUp.}
\end{closurestatus}
