\chapter{A Concrete Naming Certificate for $\CantorSpaceBaireCategoryUp$}
\label{ch:concrete-instances-cantor-space-baire-category-namecert}
\origin{human}

Following Baire and Bishop--Bridges constructive analysis, the $\CantorSpaceBaireCategoryUp$ packet records the finite-cylinder face of the Baire-category argument on Cantor space. It sits beside the Cantor-space carrier of \autoref{ch:concrete-instances-cantorspace-namecert}, the Baire-category theorem packet of \autoref{ch:concrete-instances-baire-category-theorem-namecert}, the choice-free Baire route of \autoref{ch:concrete-instances-baire-category-choice-free-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, and the complete metric surface of \autoref{ch:concrete-instances-completemetric-namecert}. The chapter names only the finite cylinder and dense-window handoff; it does not import countable choice, open-cover compactness, quotient equality, or ambient Real completeness.

\begin{definition}[CantorSpaceBaireCategory carrier]
\label{def:cantor-space-baire-category-carrier}
A $\CantorSpaceBaireCategoryUp$ carrier is a finite $\BHist$ packet
$$
K=(C,T,F,S,M,H,A,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\CantorSpaceUp$ cylinder row, exposing finite Boolean prefixes, prefix projection, and cylinder stability;
\item $T$ is the $\BaireCategoryTheoremUp$ dense-open theorem row, exposing only the dense-window rows consumed by the category argument;
\item $F$ is the $\BaireCategoryChoiceFreeUp$ refusal row, recording that the route is read without a countable-choice selector or chosen generic point;
\item $S$ is the $\StreamNameUp$ finite-window row used to list exactly the Cantor coordinates and dense-open requests inspected by a consumer;
\item $M$ is the $\CompleteMetricUp$ service row for the Cantor metric surface, available only as the displayed complete-metric handoff used by $T$;
\item $H$ records componentwise $\hsame$ transport for the cylinder, dense-window, choice-free, stream-window, complete-metric, replay, provenance, and local naming rows;
\item $A$ records displayed $\Cont$ replay from cylinder admission through dense-window inspection and terminal NameCert publication;
\item $P$ is the $\Pkg$ provenance over $\CantorSpaceBaireCategoryUp$, $\CantorSpaceUp$, $\BaireCategoryTheoremUp$, $\BaireCategoryChoiceFreeUp$, $\StreamNameUp$, $\CompleteMetricUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CantorSpaceBaireCategoryUp}$ row.
\end{enumerate}
The carrier has no coordinate for a selected generic point, a countable-choice schedule, an open-cover compactness theorem, quotient equality of streams, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CantorSpaceBaireCategory NameCert obligations]
\label{thm:cantor-space-baire-category-namecert-obligations}
For every accepted $\CantorSpaceBaireCategoryUp$ carrier $K=(C,T,F,S,M,H,A,P,N)$, the local $\NameCert_{\CantorSpaceBaireCategoryUp}$ surface consists exactly of carrier admission for the Cantor cylinder row, Baire-category dense-open row, choice-free refusal row, finite stream-window row, complete-metric service row, componentwise transport, displayed replay, provenance, and local naming. Every dense-window read must expose the finite cylinder row $C$, the theorem row $T$, the refusal row $F$, and the window row $S$ before it can publish a Baire-category conclusion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cantor-space-baire-category-carrier}. The accepted packet displays exactly the rows $C,T,F,S,M,H,A,P,N$, so the local NameCert surface can read only those coordinates.

The analytic obligations are the first five rows. The Cantor cylinder row supplies finite Boolean prefixes, the Baire-category theorem row supplies dense-window evidence, the choice-free row refuses a generic-point selector, the StreamName row records inspected coordinates, and the complete-metric row supplies the service handoff consumed by the theorem route.

The structural obligations are $H,A,P,N$. They transport, replay, source, and name the same finite packet. Since no coordinate stores countable choice, open-cover compactness, quotient equality, or ambient Real completeness, those principles cannot discharge a local obligation.
\end{proof}

\begin{theorem}[CantorSpaceBaireCategory cylinder route]
\label{thm:cantor-space-baire-category-cylinder-route}
Every accepted $\CantorSpaceBaireCategoryUp$ packet routes a Baire-category dense-window read through the finite path
$$
\CantorSpaceUp,\quad
\StreamNameUp,\quad
\CompleteMetricUp,\quad
\BaireCategoryTheoremUp,\quad
\BaireCategoryChoiceFreeUp,\quad
\NameCert_{\CantorSpaceBaireCategoryUp}.
$$
The route may shrink Cantor cylinders and inspect dense-open rows along the finite window $S$, but it cannot export countable choice, open-cover compactness, quotient equality, selected generic points, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the Cantor cylinder row $C$. A consumer sees only finite Boolean prefix data, prefix projection, and cylinder stability inherited from the $\CantorSpaceUp$ surface.

Next read the StreamName window $S$ and the complete-metric service row $M$. The window records the coordinates and dense-open requests inspected by the consumer, while $M$ provides only the complete-metric handoff used by the Baire-category theorem row.

Finally apply the theorem row $T$ together with the choice-free row $F$. The Baire-category conclusion is published through $H,A,P,N$ only after the finite cylinder, stream window, and complete-metric handoff remain visible. The refused principles are absent from the route, so the packet exports a finite-cylinder Baire-category handoff and nothing stronger.
\end{proof}

\falsifiablePrediction{Every accepted $\CantorSpaceBaireCategoryUp$ dense-window read factors through one $\CantorSpaceUp$ finite cylinder row, one $\StreamNameUp$ finite-window row, one $\CompleteMetricUp$ service row, one $\BaireCategoryTheoremUp$ dense-open row, one $\BaireCategoryChoiceFreeUp$ refusal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{cantorspace, bairecategorytheorem, bairecategorychoicefree, streamname, completemetric: $\CantorSpaceBaireCategoryUp$ is not bijective to any listed sibling because it jointly carries the Cantor finite-cylinder row, the dense-open theorem row, the choice-free refusal row, the finite StreamName window, the complete-metric service handoff, transport, replay, provenance, and local NameCert row as one packet.}

\closureat{CantorSpaceBaireCategoryUp}{seedStr}

\begin{closurestatus}{\CantorSpaceBaireCategoryUp}
  \constructivestory{A $\CantorSpaceBaireCategoryUp$ packet is generated from CantorSpace finite cylinders, BaireCategoryTheorem dense-open rows, BaireCategoryChoiceFree refusal evidence, StreamName finite windows, CompleteMetric service rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-space-baire-category-carrier}, \autoref{thm:cantor-space-baire-category-namecert-obligations}, and \autoref{thm:cantor-space-baire-category-cylinder-route} seed the finite-cylinder Baire-category route from CantorSpace cylinders to dense-open Baire rows without choice or compactness imports.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close a public Cantor-space category theorem, Lean verification, a standard bridge, countable choice, open-cover compactness, quotient equality, selected generic points, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-window exposure, cylinder-route determinacy, choice-free refusal, transport, replay, provenance, and local naming for BEDC.Derived.CantorSpaceBaireCategoryUp.}
\end{closurestatus}
