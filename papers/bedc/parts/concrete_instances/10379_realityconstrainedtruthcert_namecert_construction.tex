\chapter{A Concrete Naming Certificate for $\RealityConstrainedTruthCertUp$}
\label{ch:concrete-instances-reality-constrained-truth-cert-namecert}
\origin{ai}

The $\RealityConstrainedTruthCertUp$ packet realizes the TruthCert field list of \autoref{def:certified-physical-truth} as a finite BEDC NameCert carrier. It sits beside the open-fit surface of \autoref{ch:concrete-instances-reality-constrained-open-fit-namecert}, the objectivity surface of \autoref{ch:concrete-instances-reality-constrained-objectivity-namecert}, the explanation surface of \autoref{ch:concrete-instances-reality-constrained-explanation-namecert}, the induction-stability seal of \autoref{ch:concrete-instances-physical-induction-stability-seal-namecert}, and the tower-compression ledger of \autoref{ch:concrete-instances-reality-constrained-tower-compression-namecert}. Those sibling chapters provide the finite fit, observer-invariant reading, explanatory handoff, continuation stability, and compression ledger rows; this carrier binds exactly the nine TruthCert fields before any reality-constrained truth claim is exported.

\begin{definition}[Reality-constrained TruthCert carrier]
\label{def:reality-constrained-truth-cert-carrier}
A $\RealityConstrainedTruthCertUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,\Sigma,K,T,U,D,I,L,F,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite source row recording observation traces, source bundles, and the claim site.
\item $\Sigma$ is the reality-facing signature row extracted from the source and compared against the model-facing signature.
\item $K$ is the classifier row naming the sameness relation used for signature comparison.
\item $T$ is the tower row recording approximation levels and the endpoint at which the claim is read.
\item $U$ is the stability row naming the continuation class over which finite fit may be reused.
\item $D$ is the descent row listing the operations that survive the tower route.
\item $I$ is the invariant row recording observer-anchor transport and public content.
\item $L$ is the ledger row recording compression, idealization, hidden residue, provenance, and packaging.
\item $F$ is the failure-surface row naming finite defeats, domain exits, classifier mismatches, and later observations that can overturn the claim.
\item $N$ is the local $\NameCert_{\RealityConstrainedTruthCertUp}$ row binding the nine displayed TruthCert fields as one packet.
\end{enumerate}
The carrier has no coordinate for model--reality identity, unbounded confirmation, unledgered compression, observer privilege, hidden classifier choice, or truth export outside the nine displayed TruthCert rows.
\end{definition}
\leandef{BEDC.Derived.RealityConstrainedTruthCertUp.taste\_gate}

\begin{definition}[Reality-constrained TruthCert classifier]
\label{def:reality-constrained-truth-cert-classifier}
For two $\RealityConstrainedTruthCertUp$ carrier packets
$$
Q=(S,\Sigma,K,T,U,D,I,L,F,N)
\quad\text{and}\quad
Q'=(S',\Sigma',K',T',U',D',I',L',F',N'),
$$
the classifier $Q\sim_{\RealityConstrainedTruthCertUp}Q'$ is the componentwise
agreement predicate asserting source agreement between $S$ and $S'$, signature
agreement between $\Sigma$ and $\Sigma'$, classifier-row agreement between $K$
and $K'$, tower agreement between $T$ and $T'$, continuation-stability agreement
between $U$ and $U'$, descent agreement between $D$ and $D'$, invariant-reading
agreement between $I$ and $I'$, ledger agreement between $L$ and $L'$, failure
surface agreement between $F$ and $F'$, and local-name agreement between $N$ and
$N'$. The $K$ component preserves exactly the accepted signature comparison
used by $\Sigma$ and $\Sigma'$. The $L$ and $F$ components preserve the ledger
rows and failure rows that determine whether an export remains readable through
the same local TruthCert packet.
\end{definition}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertClassifier\_row\_certificate}

\begin{theorem}[Reality-constrained TruthCert classifier obligation surface]
\label{thm:reality-constrained-truth-cert-classifier-obligation-surface}
The classifier of \autoref{def:reality-constrained-truth-cert-classifier}
is the full comparison surface for the carrier of
\autoref{def:reality-constrained-truth-cert-carrier}. In particular, an
accepted comparison between two $\RealityConstrainedTruthCertUp$ packets must
expose the componentwise rows
$$
S,\Sigma,K,T,U,D,I,L,F,N
$$
and their primed counterparts, with $K$ governing the accepted signature
comparison and with $L,F$ governing the ledger and failure-surface comparisons.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertClassifierObligationSurface}
\begin{proof}
Start from the carrier definition
\autoref{def:reality-constrained-truth-cert-carrier}. It exposes exactly the
source, signature, classifier, tower, stability, descent, invariant, ledger,
failure-surface, and local-name rows.

Now unfold the classifier definition
\autoref{def:reality-constrained-truth-cert-classifier}. Each displayed carrier
row is compared only with its counterpart in the second packet. The $K$ row is
therefore fixed before signature comparison is accepted, and the $L,F$ rows are
fixed before ledger or failure reads are transported.

Finally use the sibling-dependency theorem
\autoref{thm:reality-constrained-truth-cert-sibling-dependency}. The open-fit,
objectivity, explanation, physical-induction, and tower-compression siblings
supply the finite source comparison, invariant reading, source-to-claim handoff,
continuation stability, tower route, descent row, and ledger discipline consumed
by this componentwise classifier.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert carrier transport]
\label{thm:reality-constrained-truth-cert-carrier-transport}
If $Q=(S,\Sigma,K,T,U,D,I,L,F,N)$ is an accepted
$\RealityConstrainedTruthCertUp$ packet and
$Q\sim_{\RealityConstrainedTruthCertUp}Q'$ for
$$
Q'=(S',\Sigma',K',T',U',D',I',L',F',N'),
$$
then $Q'$ is accepted by the same carrier surface. Transport preserves the
accepted signature comparison through $K'$, preserves the ledger row through
$L'$, preserves the failure-surface row through $F'$, and leaves no truth
coordinate outside the transported packet.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertCarrier\_transport}
\begin{proof}
The carrier definition
\autoref{def:reality-constrained-truth-cert-carrier} lists all rows required for
admission. The classifier definition
\autoref{def:reality-constrained-truth-cert-classifier} supplies a comparison
for each such row, so every required component of $Q'$ is reached by the
matching component of $Q$.

Apply the classifier obligation surface of
\autoref{thm:reality-constrained-truth-cert-classifier-obligation-surface}. It
keeps $K'$ attached to the accepted signature comparison and keeps $L'$ and
$F'$ attached to the ledger and failure-surface rows rather than to an external
truth route.

The sibling-dependency theorem
\autoref{thm:reality-constrained-truth-cert-sibling-dependency} supplies the
ambient transport sources for finite fit, invariant reading, explanatory
handoff, continuation stability, and tower compression. Thus the transported
packet remains inside the same accepted carrier surface.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert NameCert obligation exhaustion]
\label{thm:reality-constrained-truth-cert-namecert-obligation-exhaustion}
For $\RealityConstrainedTruthCertUp$, the local $\NameCert$ obligation surface is
exhausted by the carrier of
\autoref{def:reality-constrained-truth-cert-carrier}, the classifier of
\autoref{def:reality-constrained-truth-cert-classifier}, the classifier
obligation surface of
\autoref{thm:reality-constrained-truth-cert-classifier-obligation-surface}, and
the carrier transport of
\autoref{thm:reality-constrained-truth-cert-carrier-transport}. Ledger
exactness, failure exactness, and export readiness add no second certificate
surface; they read the same rows $S,\Sigma,K,T,U,D,I,L,F,N$.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertNameCertObligationExhaustion}
\begin{proof}
Use the carrier definition and classifier definition first. Together they give
the admitted packet and the accepted packet comparison, including the signature
comparison controlled by $K$ and the ledger and failure comparisons controlled
by $L$ and $F$.

Next apply
\autoref{thm:reality-constrained-truth-cert-classifier-obligation-surface} and
\autoref{thm:reality-constrained-truth-cert-carrier-transport}. These results
make comparison and transport internal to the displayed packet rather than
external to the local $\NameCert_{\RealityConstrainedTruthCertUp}$ row.

Finally use the downstream readiness blocks
\autoref{thm:reality-constrained-truth-cert-ledger-exactness},
\autoref{thm:reality-constrained-truth-cert-failure-surface-exactness}, and
\autoref{thm:reality-constrained-truth-cert-export-readiness}. They consume the
same ledger, failure, classifier, sibling, and export rows already displayed in
the packet. Hence the local NameCert obligations are exhausted by the carrier,
classifier, and transport surface, not by any hidden certificate coordinate.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert field coverage]
\label{thm:reality-constrained-truth-cert-field-coverage}
For the carrier of \autoref{def:reality-constrained-truth-cert-carrier}, the local $\NameCert_{\RealityConstrainedTruthCertUp}$ surface covers exactly the source, signature, classifier, tower, stability, descent, invariant, ledger, and failure-surface fields of the certified physical truth packet. Any accepted export of a reality-constrained truth claim must read these fields through $S,\Sigma,K,T,U,D,I,L,F$ before the local name $N$ is exposed.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertFieldCoverage}
\begin{proof}
Carrier admission exposes exactly $Q=(S,\Sigma,K,T,U,D,I,L,F,N)$. The source field is the row $S$, and the signature field is the row $\Sigma$.

The classifier field is the row $K$. The tower field is the row $T$, while the stability and descent fields are $U$ and $D$.

The invariant field is the row $I$, the ledger field is the row $L$, and the failure-surface field is the row $F$. The local name $N$ binds these rows but adds no further truth coordinate.

Thus the NameCert surface covers the nine TruthCert fields and is exhausted by them. A consumer that omits any one row reads a projection of the packet, not an accepted $\RealityConstrainedTruthCertUp$ export.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert source-signature-classifier obligation]
\label{thm:reality-constrained-truth-cert-source-signature-classifier-obligation}
Every accepted $\RealityConstrainedTruthCertUp$ truth export must expose the source row $S$, the reality-facing signature row $\Sigma$, and the classifier row $K$ before the local truth claim is readable. The source may not be replaced by an unnamed observation route, the signature may not be replaced by a model-side assertion, and the classifier may not be chosen after export.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertSourceSignatureClassifierObligation}
\begin{proof}
Use the carrier of \autoref{def:reality-constrained-truth-cert-carrier}. The displayed packet contains $S,\Sigma,K$ as separate rows: source, reality-facing signature, and classifier.

Apply \autoref{thm:reality-constrained-truth-cert-field-coverage}. It makes these rows mandatory fields of the TruthCert surface before the local name $N$ is exposed, so omitting any one of them yields only a projection of the packet.

Finally use \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. The open-fit and explanation siblings supply finite source comparison and source-to-claim handoff, and the classifier row controls the accepted signature comparison. Thus truth export is blocked until $S,\Sigma,K$ have all been recorded.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert stability-descent-invariant obligation]
\label{thm:reality-constrained-truth-cert-stability-descent-invariant-obligation}
Every accepted $\RealityConstrainedTruthCertUp$ packet must preserve the tower route $T$, the continuation-stability row $U$, the descent row $D$, and the observer-invariant row $I$ as a joint obligation surface. These rows certify that the claim is read at the displayed endpoint, reused only over the named continuation class, transported only through the recorded descent operations, and exposed through an observer-invariant public reading.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertStabilityDescentInvariantObligation}
\begin{proof}
The carrier in \autoref{def:reality-constrained-truth-cert-carrier} displays the rows $T,U,D,I$ separately. They name the approximation tower, the continuation class, the surviving descent operations, and the observer-anchor transport.

Apply \autoref{thm:reality-constrained-truth-cert-field-coverage}. Since tower, stability, descent, and invariant reading are mandatory TruthCert fields, the local name $N$ can certify a truth export only after these four rows are readable.

Use \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. Physical induction supplies the continuation-stability seal, tower compression supplies tower route and descent discipline, and objectivity supplies observer-invariant reading. Therefore $T,U,D,I$ form one preservation surface rather than four optional annotations.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert ledger-failure-local-name obligation]
\label{thm:reality-constrained-truth-cert-ledger-failure-local-name-obligation}
Every accepted $\RealityConstrainedTruthCertUp$ local name must be preceded by the ledger row $L$ and the failure-surface row $F$. The ledger records compression, idealization, hidden residue, provenance, and packaging, while the failure row records finite defeats, domain exits, classifier mismatches, and later observations before $N$ binds the packet as a local $\NameCert_{\RealityConstrainedTruthCertUp}$.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertLedgerFailureLocalNameObligation}
\begin{proof}
Read the carrier of \autoref{def:reality-constrained-truth-cert-carrier}. It assigns compression, idealization, hidden residue, provenance, and packaging to $L$, assigns the defeat and exit surface to $F$, and reserves $N$ for the local NameCert row.

Apply \autoref{thm:reality-constrained-truth-cert-field-coverage}. The ledger and failure-surface fields are mandatory before the local name is exposed, while $N$ binds the already displayed rows without adding an extra truth coordinate.

Finally use \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. Tower compression controls descent, ledger, and hidden residue; open-fit controls finite defeat; and the remaining siblings route invariant reading, explanation, and stability. Hence local naming is accepted only after ledger and failure obligations have been recorded in $L$ and $F$.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert sibling dependency]
\label{thm:reality-constrained-truth-cert-sibling-dependency}
Every accepted $\RealityConstrainedTruthCertUp$ packet consumes the open-fit, objectivity, explanation, physical-induction, and tower-compression dependencies supplied by the sibling certificate family. The open-fit dependency controls finite fit and finite defeat; the objectivity dependency controls invariant observer reading; the explanation dependency controls source-to-claim handoff; the physical-induction dependency controls stability over continuations; and the tower-compression dependency controls descent, ledger, and hidden residue.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertSiblingDependency}
\begin{proof}
Read the carrier rows of \autoref{def:reality-constrained-truth-cert-carrier}. The rows $S,\Sigma,K,F$ require the finite source, signature comparison, classifier, and defeat surface supplied by the open-fit certificate family.

The invariant row $I$ requires the observer-anchor discipline supplied by the objectivity certificate family. Without that row, the packet can carry a private observation route but not a public reality-constrained truth claim.

The source-to-claim handoff is supplied by the explanation certificate family. It binds signatures, derivation routes, and admissible explanatory use before the local truth certificate can be read.

The stability row $U$ consumes the physical-induction seal, because finite fit can be reused only over the displayed continuation class. The tower, descent, and ledger rows $T,D,L$ consume the tower-compression certificate, because endpoint claims must retain their approximation route and hidden residue.

Consequently the packet is composite rather than atomic: its accepted route depends on the open-fit, objectivity, explanation, physical-induction, and tower-compression siblings, and no single sibling alone carries the full TruthCert surface.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert kernel truth route]
\label{thm:reality-constrained-truth-cert-kernel-truth-route}
Every accepted $\RealityConstrainedTruthCertUp$ truth export is routed only through
$$
S,\Sigma,K,T,U,D,I,L,F,N
$$
and through the sibling dependency surface of \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. It is not routed through model--reality identity, hidden observer privilege, unbounded confirmation, or an external truth coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertKernelTruthRoute}
\begin{proof}
Start from the carrier definition \autoref{def:reality-constrained-truth-cert-carrier}. It displays the source, signature, classifier, tower, stability, descent, invariant, ledger, failure, and local NameCert rows as the only rows of the packet.

Apply \autoref{thm:reality-constrained-truth-cert-classifier-obligation-surface} and \autoref{thm:reality-constrained-truth-cert-field-coverage}. These theorems make the classifier and all nine exported fields mandatory parts of the displayed route rather than optional side data.

Finally use \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. The open-fit, objectivity, explanation, physical-induction, and tower-compression siblings account for the admissible dependency surface, so no model--reality identity row or external truth coordinate remains available for export.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert failure readiness]
\label{thm:reality-constrained-truth-cert-failure-readiness}
In every accepted $\RealityConstrainedTruthCertUp$ export, the failure row $F$ is displayed before the truth certificate is readable. The same row remains part of transported classifier agreement whenever the packet is compared, replayed, or carried across a sibling route.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertFailureReadiness}
\begin{proof}
First read $F$ from \autoref{def:reality-constrained-truth-cert-carrier}. The carrier reserves it for finite defeats, domain exits, classifier mismatches, and later observations before local naming.

Use \autoref{thm:reality-constrained-truth-cert-ledger-failure-local-name-obligation}. It places $F$ before the local NameCert row $N$, so export cannot precede the displayed failure surface.

Then apply \autoref{thm:reality-constrained-truth-cert-carrier-transport} together with \autoref{thm:reality-constrained-truth-cert-classifier-obligation-surface}. Transported agreement must preserve the classifier-facing rows already exposed by the packet, and the failure row remains one of those rows rather than a removable annotation.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert public export boundary]
\label{thm:reality-constrained-truth-cert-public-export-boundary}
A public $\RealityConstrainedTruthCertUp$ export is accepted only after the source row $S$, classifier row $K$, tower row $T$, stability row $U$, invariant row $I$, ledger row $L$, failure row $F$, and local NameCert row $N$ are all present, with the signature and descent rows $\Sigma,D$ displayed in the same carrier.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertPublicExportBoundary}
\begin{proof}
Begin with \autoref{def:reality-constrained-truth-cert-carrier}. The public packet is $Q=(S,\Sigma,K,T,U,D,I,L,F,N)$, so each named row must be available inside the same finite carrier before any export can be read.

Next use \autoref{thm:reality-constrained-truth-cert-field-coverage}. It identifies the required source, signature, classifier, tower, stability, descent, invariant, ledger, failure, and local naming fields, and it leaves no public export row outside them.

Finally combine \autoref{thm:reality-constrained-truth-cert-ledger-failure-local-name-obligation} with \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. The ledger and failure rows precede local naming, while the sibling surface supplies the dependency discipline for source, classifier, tower, stability, invariant, ledger, and failure. Hence public export is accepted only at the full displayed boundary.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert carrier admission]
\label{thm:reality-constrained-truth-cert-carrier-admission}
An accepted $\RealityConstrainedTruthCertUp$ packet exposes exactly the carrier
$$
Q=(S,\Sigma,K,T,U,D,I,L,F,N).
$$
The accepted export may project the source, signature, classifier, tower,
stability, descent, invariant, ledger, failure-surface, and local-name rows,
but it has no additional TruthCert coordinate outside this displayed packet.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertCarrierAdmission\_eventflow\_boundary}
\begin{proof}
Use the carrier definition
\autoref{def:reality-constrained-truth-cert-carrier}. Its displayed packet is
exactly $Q=(S,\Sigma,K,T,U,D,I,L,F,N)$.

Apply the field-coverage theorem
\autoref{thm:reality-constrained-truth-cert-field-coverage}. It identifies the
source, signature, classifier, tower, stability, descent, invariant, ledger,
and failure-surface fields with the rows $S,\Sigma,K,T,U,D,I,L,F$, and it
reserves $N$ for local naming.

No remaining row is available for carrier admission. A consumer that asks for a
second truth coordinate is therefore asking outside the admitted packet rather
than reading an accepted $\RealityConstrainedTruthCertUp$ export.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert ledger exactness]
\label{thm:reality-constrained-truth-cert-ledger-exactness}
In every accepted $\RealityConstrainedTruthCertUp$ packet, the ledger row $L$
records compression, idealization, hidden residue, provenance, and package
descent before any truth export is readable. These records are routed through
the open-fit, objectivity, explanation, physical-induction stability, and
tower-compression siblings before the local name $N$ is exposed.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertLedgerExactness}
\begin{proof}
The carrier definition
\autoref{def:reality-constrained-truth-cert-carrier} assigns compression,
idealization, hidden residue, provenance, and packaging to the ledger row $L$.
The field-coverage theorem
\autoref{thm:reality-constrained-truth-cert-field-coverage} then makes $L$ a
mandatory TruthCert field rather than an optional annotation.

Now use the sibling-dependency theorem
\autoref{thm:reality-constrained-truth-cert-sibling-dependency}. Open-fit
controls finite fit and finite defeat; objectivity controls public observer
reading; explanation controls source-to-claim handoff; physical induction
controls continuation stability; and tower compression controls descent,
ledger, and hidden residue.

Consequently the ledger is exact at the point of export: compression,
idealization, hidden residue, provenance, and package descent must already be
visible in $L$ and routed through the named siblings before $N$ can certify the
truth packet.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert non-escape]
\label{thm:reality-constrained-truth-cert-nonescape}
No accepted $\RealityConstrainedTruthCertUp$ packet exports model--reality
identity, unbounded confirmation, observer privilege, hidden classifier choice,
or truth outside the displayed rows $S,\Sigma,K,T,U,D,I,L,F,N$.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertNonescape}
\begin{proof}
Inspect the carrier definition
\autoref{def:reality-constrained-truth-cert-carrier}. Its final sentence
excludes model--reality identity, unbounded confirmation, unledgered
compression, observer privilege, hidden classifier choice, and truth export
outside the nine displayed TruthCert rows.

The field-coverage theorem
\autoref{thm:reality-constrained-truth-cert-field-coverage} makes the displayed
rows exhaustive for accepted export. The sibling-dependency theorem
\autoref{thm:reality-constrained-truth-cert-sibling-dependency} further routes
finite fit, objectivity, explanation, induction stability, and compression
through their own certificate surfaces.

Therefore none of the excluded demands has a readable coordinate in the packet.
Such a demand can only be recorded at the failure-surface or ledger boundary,
not exported as part of the local TruthCert name.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert classifier stability]
\label{thm:reality-constrained-truth-cert-classifier-stability}
Let
$$
Q=(S,\Sigma,K,T,U,D,I,L,F,N)
\quad\text{and}\quad
Q'=(S',\Sigma',K',T',U',D',I',L',F',N')
$$
be accepted $\RealityConstrainedTruthCertUp$ packets. Any accepted comparison
between $Q$ and $Q'$ must preserve the signature, classifier, tower, stability,
descent, invariant, ledger, and failure-surface rows componentwise:
$\Sigma$ is compared only with $\Sigma'$, $K$ only with $K'$, $T$ only with
$T'$, $U$ only with $U'$, $D$ only with $D'$, $I$ only with $I'$, $L$ only
with $L'$, and $F$ only with $F'$. No comparison accepted by the local
$\NameCert_{\RealityConstrainedTruthCertUp}$ row may substitute one of these
rows for another.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertClassifier\_stability}
\begin{proof}
Project the two carriers through
\autoref{def:reality-constrained-truth-cert-carrier}. The displayed packets
expose the rows $S,\Sigma,K,T,U,D,I,L,F,N$ and
$S',\Sigma',K',T',U',D',I',L',F',N'$ with the same row order and the same
local naming role.

Use \autoref{thm:reality-constrained-truth-cert-field-coverage}. It makes the
signature, classifier, tower, stability, descent, invariant, ledger, and
failure-surface fields readable exactly through the displayed rows
$\Sigma,K,T,U,D,I,L,F$ and their primed counterparts.

Now use \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. The
open-fit sibling controls signature comparison and finite defeat, the
objectivity sibling controls invariant reading, the explanation sibling
controls source-to-claim handoff, the physical-induction sibling controls
stability, and the tower-compression sibling controls tower, descent, ledger,
and hidden residue.

Therefore an accepted comparison has no free classifier or cross-row transport
left to choose. It preserves each displayed obligation row against its
counterpart, while $N$ and $N'$ only name the already displayed packets.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert ledger non-escape]
\label{thm:reality-constrained-truth-cert-ledger-nonescape}
No accepted $\RealityConstrainedTruthCertUp$ truth export may bypass the
ledger row $L$. In particular, compression, idealization, provenance, and
hidden residue must be readable through $L$ before the local name $N$ can
certify the exported truth packet.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertLedgerNonescape}
\begin{proof}
Read the carrier row $L$ in
\autoref{def:reality-constrained-truth-cert-carrier}. It is the displayed
ledger coordinate for compression, idealization, hidden residue, provenance,
and packaging.

Apply \autoref{thm:reality-constrained-truth-cert-field-coverage}. The ledger
is one of the nine mandatory TruthCert fields, so an export that omits $L$
reads only a projection of the packet.

Apply \autoref{thm:reality-constrained-truth-cert-ledger-exactness}. The
ledger entries must already record compression, idealization, hidden residue,
provenance, and package descent before truth export is readable.

Finally use \autoref{thm:reality-constrained-truth-cert-sibling-dependency}.
The tower-compression sibling controls descent, ledger, and hidden residue,
while the other siblings route finite fit, invariant reading, explanatory
handoff, and continuation stability. Thus a truth export that bypasses $L$
bypasses a required sibling-routed row and is not accepted by the local
certificate.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert failure-surface exactness]
\label{thm:reality-constrained-truth-cert-failure-surface-exactness}
For every accepted $\RealityConstrainedTruthCertUp$ packet, later observations,
classifier mismatches, domain exits, and finite defeats are accepted exactly
through the displayed failure-surface row $F$. They are not hidden in the
source, classifier, stability, invariant, ledger, or local-name rows.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertFailureSurfaceExactness}
\begin{proof}
The carrier definition
\autoref{def:reality-constrained-truth-cert-carrier} assigns finite defeats,
domain exits, classifier mismatches, and later observations to the
failure-surface row $F$.

The field-coverage theorem
\autoref{thm:reality-constrained-truth-cert-field-coverage} makes $F$ a
mandatory TruthCert field and exhausts the accepted export by the displayed
rows. Therefore a claimed defeat route outside $F$ is outside the local
TruthCert surface.

The sibling-dependency theorem
\autoref{thm:reality-constrained-truth-cert-sibling-dependency} identifies the
dependencies that feed this surface. Open-fit supplies finite fit and finite
defeat, the classifier row supplies signature comparison, physical induction
supplies the continuation class, and tower compression supplies descent and
hidden residue.

Consequently later observations, classifier mismatches, domain exits, and
finite defeats have a single accepted public landing row: $F$. The other rows
may supply the data that makes a defeat meaningful, but they do not certify a
separate failure surface.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert export defeat exhaustion]
\label{thm:reality-constrained-truth-cert-export-defeat-exhaustion}
Let an accepted $\RealityConstrainedTruthCertUp$ packet expose the rows
$S,\Sigma,K,T,U,D,I,L,F,N$. If $F$ records a later observation, classifier
mismatch, domain exit, or finite defeat, then no truth export through the local
name $N$ is accepted unless that defeat is discharged through the same displayed
$S,\Sigma,K,T,U,D,I,L,F,N$ route. A purported export that hides $F$ or bypasses
it is outside the TruthCert packet.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertExportDefeatExhaustion}
\begin{proof}
The carrier coverage theorem
\autoref{thm:reality-constrained-truth-cert-field-coverage} first fixes the
only rows that the local $\NameCert_{\RealityConstrainedTruthCertUp}$ surface
can expose. Thus the name $N$ does not create a second export channel; it binds
the nine displayed rows after they are readable.

The ledger exactness theorem
\autoref{thm:reality-constrained-truth-cert-ledger-exactness} keeps
compression, residue, provenance, and package descent visible at $L$. Together
with the sibling-dependency theorem
\autoref{thm:reality-constrained-truth-cert-sibling-dependency}, this forces
open-fit, objectivity, explanation, physical-induction, and tower-compression
data to travel through the displayed route before a truth claim reaches $N$.

Finally apply
\autoref{thm:reality-constrained-truth-cert-failure-surface-exactness}. Later
observations, classifier mismatches, domain exits, and finite defeats land
only at $F$. Therefore a discharge of such a defeat must be visible through the
same carrier route that made $F$ visible. If a purported export hides or
bypasses $F$, it is not an accepted export through the local TruthCert packet.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert export readiness]
\label{thm:reality-constrained-truth-cert-export-readiness}
An accepted $\RealityConstrainedTruthCertUp$ truth export is readable through the
local name $N$ only after field coverage, sibling dependency routing, ledger
exactness, classifier stability, and failure-surface exactness have all been read
through the displayed carrier
$$
Q=(S,\Sigma,K,T,U,D,I,L,F,N).
$$
Thus the local $\NameCert_{\RealityConstrainedTruthCertUp}$ row certifies export
readiness only for the full ten-row packet, not for a projection of the packet.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertExportReadiness}
\begin{proof}
Start with \autoref{thm:reality-constrained-truth-cert-carrier-admission} and
\autoref{thm:reality-constrained-truth-cert-field-coverage}. They make
$Q=(S,\Sigma,K,T,U,D,I,L,F,N)$ the admitted carrier and make
$S,\Sigma,K,T,U,D,I,L,F$ the mandatory readable TruthCert rows before $N$ is
exposed.

Next use \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. It
routes finite fit, objectivity, explanation, physical-induction stability, and
tower compression through the displayed packet rather than through an external
export channel.

The ledger and classifier obligations are fixed by
\autoref{thm:reality-constrained-truth-cert-ledger-exactness} and
\autoref{thm:reality-constrained-truth-cert-classifier-stability}. The former
keeps compression, idealization, hidden residue, provenance, and package descent
visible at $L$, while the latter prevents signature, classifier, tower,
stability, descent, invariant, ledger, and failure-surface rows from being
substituted across comparisons.

Finally apply
\autoref{thm:reality-constrained-truth-cert-failure-surface-exactness}. Later
observations, classifier mismatches, domain exits, and finite defeats land at
$F$. Therefore an accepted export through $N$ is ready only when all these
readings have already passed through the displayed carrier rows.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert methodology-ledger handoff]
\label{thm:reality-constrained-truth-cert-methodology-ledger-handoff}
Every accepted $\RealityConstrainedTruthCertUp$ export consumed as methodology authority must route through $\RealityConstrainedMethodologyLedgerUp$ before the local name $N$ is read. The methodology consumer must preserve the TruthCert source, signature, classifier, tower, stability, descent, invariant, ledger, and failure rows together with the methodology-ledger export row, rather than reading a projection of the TruthCert packet as an independent methodological warrant.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertMethodologyLedgerHandoff}
\begin{proof}
Start with field coverage and export readiness, \autoref{thm:reality-constrained-truth-cert-field-coverage} and \autoref{thm:reality-constrained-truth-cert-export-readiness}. They force every accepted export through $S,\Sigma,K,T,U,D,I,L,F,N$ before the local name is exposed.

Next use sibling dependency and ledger exactness, \autoref{thm:reality-constrained-truth-cert-sibling-dependency} and \autoref{thm:reality-constrained-truth-cert-ledger-exactness}. These keep explanation, audit-facing source use, observer reading, physical-induction stability, tower compression, provenance, hidden residue, and package descent visible at the point where the TruthCert export is consumed.

The methodology-ledger chapter records exactly the downstream scope, domain-leakage refusal, upgrade route, and methodology failure surface for such consumption. Thus a methodology consumer cannot treat the TruthCert name as freestanding authority. It must first pass the accepted export through $\RealityConstrainedMethodologyLedgerUp$, where the export discipline is recorded before $N$ is read.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert open-fit export boundary]
\label{thm:reality-constrained-truth-cert-open-fit-export-boundary}
Every accepted $\RealityConstrainedTruthCertUp$ export whose failure row changes must route through the open-fit and failure-surface siblings before export. A changed row $F$ is accepted only when the finite-fit, defeat, classifier-mismatch, domain-exit, continuation-scope, and ledger reads remain visible through the displayed carrier $S,\Sigma,K,T,U,D,I,L,F,N$.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertOpenFitExportBoundary}
\begin{proof}
Use failure-surface exactness, \autoref{thm:reality-constrained-truth-cert-failure-surface-exactness}. Later observations, classifier mismatches, domain exits, and finite defeats have the single public landing row $F$.

Apply sibling dependency and classifier stability, \autoref{thm:reality-constrained-truth-cert-sibling-dependency} and \autoref{thm:reality-constrained-truth-cert-classifier-stability}. The open-fit sibling controls finite fit and finite defeat, while the classifier row prevents a changed failure surface from being compared by substituting another displayed row.

Finally use export readiness. Since $N$ is readable only after field coverage, sibling routing, ledger exactness, classifier stability, and failure-surface exactness have all been read, a changed $F$ must be routed through the open-fit and failure-surface surfaces before export. Otherwise the changed failure row is outside the accepted TruthCert packet.
\end{proof}

\input{parts/concrete_instances/realityconstrainedtruthcert/observer_invariant_ledger_route}

\begin{theorem}[Reality-constrained TruthCert failure routing]
\label{thm:reality-constrained-truth-cert-failure-routing}
For an accepted $\RealityConstrainedTruthCertUp$ packet, defeating observations,
domain exits, classifier mismatches, and later observations land only in the row
$F$ before the local name $N$ certifies export. They do not create a hidden truth
coordinate, an unledgered compression route, or a classifier choice outside
$S,\Sigma,K,T,U,D,I,L,F,N$.
\end{theorem}
\begin{proof}
Use \autoref{thm:reality-constrained-truth-cert-failure-surface-exactness}. It
assigns later observations, classifier mismatches, domain exits, and finite
defeats to the displayed failure-surface row $F$ and excludes separate failure
surfaces in the source, classifier, stability, invariant, ledger, and local-name
rows.

Now apply \autoref{thm:reality-constrained-truth-cert-nonescape}. The carrier has
no row for model--reality identity, unbounded confirmation, observer privilege,
hidden classifier choice, or truth outside the displayed packet.

Finally use \autoref{thm:reality-constrained-truth-cert-ledger-nonescape}. The
ledger row $L$ must already expose compression, idealization, hidden residue,
provenance, and package descent before $N$ names the export. Hence a failure
cannot be routed by hiding in the ledger or by bypassing $F$; it reaches the
local certificate only through the displayed failure row.
\end{proof}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertFailureRouting}

\begin{theorem}[Reality-constrained TruthCert sibling handoff totality]
\label{thm:reality-constrained-truth-cert-sibling-handoff-totality}
Every accepted $\RealityConstrainedTruthCertUp$ packet consumes the open-fit,
objectivity, explanation, physical-induction stability, and tower-compression
sibling handoffs before the local name $N$ exports its TruthCert packet. No
accepted export may skip one of these sibling routes while still claiming the
full rows $S,\Sigma,K,T,U,D,I,L,F,N$.
\end{theorem}
\begin{proof}
Expand \autoref{thm:reality-constrained-truth-cert-sibling-dependency}. Open-fit
controls finite fit and finite defeat, objectivity controls observer-invariant
reading, explanation controls source-to-claim handoff, physical induction
controls continuation stability, and tower compression controls tower route,
descent, ledger, and hidden residue.

Then apply \autoref{thm:reality-constrained-truth-cert-export-readiness}. Export
through $N$ is readable only after field coverage, sibling routing, ledger
exactness, classifier stability, and failure-surface exactness have all been read
through $S,\Sigma,K,T,U,D,I,L,F,N$.

If one sibling handoff were skipped, at least one required row would lose its
source of admissible data: finite fit or defeat, invariant reading, explanatory
handoff, continuation stability, or compression and descent. Such a packet would
be a projection rather than the accepted TruthCert carrier.

Therefore the five sibling routes are total for this export surface. They are all
consumed before $N$ certifies the local $\NameCert_{\RealityConstrainedTruthCertUp}$
packet.
\end{proof}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertSiblingHandoffTotality}

\begin{theorem}[Reality-constrained TruthCert root readiness packet]
\label{thm:reality-constrained-truth-cert-root-readiness-packet}
For the carrier
$$
Q=(S,\Sigma,K,T,U,D,I,L,F,N),
$$
root readiness of $\RealityConstrainedTruthCertUp$ is exactly the packet route
that passes through carrier admission, field coverage, sibling handoff
totality, ledger exactness, failure-surface exactness, and export readiness
over the displayed rows $S,\Sigma,K,T,U,D,I,L,F,N$. A root consumer receives a
TruthCert packet only along that route, not along a separate truth coordinate
or a projection of the carrier.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertRootReadinessPacket}
\begin{proof}
Start with carrier admission and field coverage,
\autoref{thm:reality-constrained-truth-cert-carrier-admission} and
\autoref{thm:reality-constrained-truth-cert-field-coverage}. They identify
$Q=(S,\Sigma,K,T,U,D,I,L,F,N)$ as the admitted carrier and make the source,
signature, classifier, tower, stability, descent, invariant, ledger, and
failure-surface rows mandatory before $N$ is readable.

Next use sibling handoff totality,
\autoref{thm:reality-constrained-truth-cert-sibling-handoff-totality}. It
forces the open-fit, objectivity, explanation, physical-induction stability,
and tower-compression routes to be consumed before the local name exports the
packet.

Finally apply ledger exactness, failure-surface exactness, and export
readiness:
\autoref{thm:reality-constrained-truth-cert-ledger-exactness},
\autoref{thm:reality-constrained-truth-cert-failure-surface-exactness}, and
\autoref{thm:reality-constrained-truth-cert-export-readiness}. These results
make $L$ and $F$ exact rows and allow $N$ to certify export only after the same
displayed packet has been read. Thus root readiness is neither more nor less
than this carrier route through $S,\Sigma,K,T,U,D,I,L,F,N$.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert root defeat projection]
\label{thm:reality-constrained-truth-cert-root-defeat-projection}
Any downstream consumer that changes or omits the failure row $F$ of an
accepted $\RealityConstrainedTruthCertUp$ packet reads only a projection of the
TruthCert carrier. Such a consumer does not receive the root-ready packet
unless the changed or disputed failure surface is routed through the displayed
open-fit, defeat, classifier, ledger, and export rows before $N$ is read.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertRootDefeatProjection}
\begin{proof}
Use failure routing,
\autoref{thm:reality-constrained-truth-cert-failure-routing}. Defeating
observations, domain exits, classifier mismatches, and later observations land
only in $F$ before $N$ certifies export; they do not create a hidden truth
coordinate or an unledgered compression route.

Now apply the open-fit export boundary,
\autoref{thm:reality-constrained-truth-cert-open-fit-export-boundary}. A
changed $F$ must remain visible through the open-fit and failure-surface
siblings together with the carrier rows $S,\Sigma,K,T,U,D,I,L,F,N$.

Finally use export defeat exhaustion,
\autoref{thm:reality-constrained-truth-cert-export-defeat-exhaustion}. A later
observation, classifier mismatch, domain exit, or finite defeat recorded by
$F$ must be discharged through the same displayed route. Omitting or changing
$F$ without that route leaves only a projection, so the downstream consumer has
not received the root-ready TruthCert packet.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert root consumer totality]
\label{thm:reality-constrained-truth-cert-root-consumer-totality}
Every methodology, open-fit, objectivity, physical-induction, or
tower-compression consumer of the $\RealityConstrainedTruthCertUp$ name must
consume all five sibling routes before $N$ exports the TruthCert packet. No
such consumer may read one sibling route as an independent root authority while
claiming the full rows $S,\Sigma,K,T,U,D,I,L,F,N$.
\end{theorem}\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertRootConsumerTotality}
\begin{proof}
Begin with sibling dependency and sibling handoff totality,
\autoref{thm:reality-constrained-truth-cert-sibling-dependency} and
\autoref{thm:reality-constrained-truth-cert-sibling-handoff-totality}. They
assign finite fit and defeat to open-fit, invariant reading to objectivity,
source-to-claim handoff to explanation, continuation stability to physical
induction, and tower route, descent, ledger, and hidden residue to tower
compression.

For methodology consumption, use
\autoref{thm:reality-constrained-truth-cert-methodology-ledger-handoff}. It
requires the methodology consumer to preserve the TruthCert source, signature,
classifier, tower, stability, descent, invariant, ledger, and failure rows
together with the methodology-ledger export row.

For the remaining root consumers, apply export readiness and root readiness,
\autoref{thm:reality-constrained-truth-cert-export-readiness} and
\autoref{thm:reality-constrained-truth-cert-root-readiness-packet}. The local
name $N$ is readable only after field coverage, sibling routing, ledger
exactness, classifier stability, and failure-surface exactness have all passed
through the displayed carrier. Therefore each named downstream consumer must
consume all five sibling routes before treating $N$ as the exported TruthCert
packet.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert root carrier obligation]
\label{thm:reality-constrained-truth-cert-root-carrier-obligation}
Every root-facing consumer of $\RealityConstrainedTruthCertUp$ must receive the
displayed carrier packet $S,\Sigma,K,T,U,D,I,L,F,N$. Omitting any one of these
rows leaves only a projection and cannot be read as the root-ready TruthCert
export named by $N$.
\end{theorem}\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertRootCarrierObligation}
\begin{proof}
Use the carrier and classifier definitions,
\autoref{def:reality-constrained-truth-cert-carrier} and
\autoref{def:reality-constrained-truth-cert-classifier}. They make the ten
displayed rows the whole accepted packet and the whole comparison surface.

Apply root consumer totality,
\autoref{thm:reality-constrained-truth-cert-root-consumer-totality}. Each
methodology, open-fit, objectivity, physical-induction, or tower-compression
consumer must consume the sibling routes through this same packet. Therefore a
consumer that omits one displayed row has not received the carrier required for
root export.
\end{proof}

\begin{theorem}[Reality-constrained TruthCert root classifier stability]
\label{thm:reality-constrained-truth-cert-root-classifier-stability}
If a root-facing transport compares two accepted
$\RealityConstrainedTruthCertUp$ packets, then the classifier rows $K,K'$ remain
the rows governing signature comparison for the transported packets
$S,\Sigma,K,T,U,D,I,L,F,N$ and
$S',\Sigma',K',T',U',D',I',L',F',N'$. The transport cannot replace the
classifier row while preserving root readiness.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedTruthCertUp.RealityConstrainedTruthCertRootClassifierStability}
\begin{proof}
Start with classifier stability and carrier transport,
\autoref{thm:reality-constrained-truth-cert-classifier-stability} and
\autoref{thm:reality-constrained-truth-cert-carrier-transport}. They keep the
classifier component attached to the accepted signature comparison across the
transported carrier.

Now use root readiness,
\autoref{thm:reality-constrained-truth-cert-root-readiness-packet}. Root
readiness is the carrier route through field coverage, sibling handoff, ledger
exactness, failure exactness, and export readiness. A changed classifier row
would leave that route, so the transported export remains root-ready only when
$K$ and $K'$ are the classifier rows being compared.
\end{proof}

\input{parts/concrete_instances/realityconstrainedtruthcert/root_ledger_nonescape}

\falsifiablePrediction{A purported reality-constrained truth export that omits the displayed source, signature, classifier, tower, stability, descent, invariant, ledger, or failure-surface row is not an accepted TruthCert packet.}

\independenceWitness{reality-constrained-open-fit, reality-constrained-objectivity, reality-constrained-explanation, physical-induction-stability-seal, reality-constrained-tower-compression: the full nine-field TruthCert packet is available only after these sibling dependencies are consumed together.}

\input{parts/concrete_instances/realityconstrainedtruthcert/l10_source_readiness}
\input{parts/concrete_instances/realityconstrainedtruthcert/root_tastegate_obligations}
\input{parts/concrete_instances/realityconstrainedtruthcert/root_l10_truth_obligations}
\input{parts/concrete_instances/realityconstrainedtruthcert/closure_status}
