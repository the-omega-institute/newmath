\chapter{A Concrete Naming Certificate for $\RealCompletionCauchyNetUp$}
\label{ch:concrete-instances-real-completion-cauchy-net-namecert}
\origin{human}

Following Bishop and Bridges on constructive Cauchy approximation and Kelley on nets over directed sets, the $\RealCompletionCauchyNetUp$ packet records the finite completion-facing route by which a directed Cauchy net is read through named rational windows before any terminal real-name seal is inspected. It sits after the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window source of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the explicit threshold discipline of \autoref{ch:concrete-instances-cauchymodulus-namecert}. The packet names only displayed directed indices, Cauchy-tail moduli, finite observation windows, regular rational readback, dyadic tolerances, Real sealing, transport, replay, provenance, and local naming.

\begin{definition}[RealCompletionCauchyNet carrier]
\label{def:real-completion-cauchy-net-carrier}
A $\RealCompletionCauchyNetUp$ carrier is a finite $\BHist$ packet
$$
N=(I,M,S,R,D,E,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the directed index row, naming the inspected finite upper-bound comparisons for the net indices.
\item $M$ is the $\CauchyModulusUp$ Cauchy-tail modulus row, assigning displayed tail thresholds to dyadic tolerance requests.
\item $S$ is the $\StreamNameUp$ finite-window row for the net entries visible at the inspected indices.
\item $R$ is the $\RegSeqRatUp$ readback row extracted from the same finite windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the rational scales consumed by $M,S,R$.
\item $E$ is the terminal $\RealUp$ seal boundary, readable only after $I,M,S,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the directed index, tail modulus, finite-window, readback, tolerance, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion-facing consumer through the directed Cauchy-net rows.
\item $P$ records
$$
\Pkg(N)=\Pkg(\RealCompletionCauchyNetUp,\RealUp,\RegSeqRatUp,\StreamNameUp,\DyadicRatCoreUp,\CauchyModulusUp,\BHist,\hsame,\Cont,\NameCert).
$$
\item $L$ is the local $\NameCert_{\RealCompletionCauchyNetUp}$ row.
\end{enumerate}
The classifier accepts only packets read through $I,M,S,R,D,E,H,C,P,L$. It has no coordinate for quotient nets, selected limits, countable choice, host net equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[RealCompletionCauchyNet NameCert obligations]
\label{thm:real-completion-cauchy-net-namecert-obligations}
For every accepted $\RealCompletionCauchyNetUp$ carrier $N=(I,M,S,R,D,E,H,C,P,L)$, the local $\NameCert_{\RealCompletionCauchyNetUp}$ surface consists exactly of directed-index exposure through $I$, Cauchy-tail modulus exposure through $M$, finite-window exposure through $S$, regular rational readback through $R$, dyadic tolerance accounting through $D$, Real seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-completion-cauchy-net-carrier}. Its tuple lists the directed index row, the Cauchy-tail modulus, the finite StreamName windows, the regular rational readback, the dyadic tolerance ledger, the Real seal, and the structural rows as the whole accepted surface.

Read the Cauchy request through $D$ and $M$. The dyadic tolerance ledger fixes the inspected scale, and the modulus row turns that scale into displayed tail thresholds over the directed index row $I$.

The finite windows $S$ and regular readback $R$ are then consumed only at the displayed tail indices. Finally $E,H,C,P,L$ seal, transport, replay, source, and name the same route. Since no quotient-net carrier, selected limit, countable-choice schedule, host equality principle, or ambient $\RealUp$ completeness coordinate appears in the packet, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[RealCompletionCauchyNet completion-facing route]
\label{thm:real-completion-cauchy-net-completion-facing-route}
Every L10 Real and Completion consumer of an accepted $\RealCompletionCauchyNetUp$ packet must factor through the displayed route
$$
\DyadicRatCoreUp
\longmapsto
\CauchyModulusUp
\longmapsto
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
The directed index row $I$ remains visible throughout the route, and the consumer receives no selected limit, quotient-net representative, countable-choice selector, host equality principle, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Begin with the dyadic tolerance request. The carrier requires that request to be read through $D$ before the Cauchy-tail modulus row $M$ can expose any tail threshold.

Next use the directed index row $I$ and the finite StreamName row $S$. They keep the inspected net entries tied to displayed upper-bound comparisons and finite windows, not to an unbounded host net.

The regular readback $R$ and terminal seal $E$ are then reached through the same displayed rows, with $H,C,P,L$ preserving transport, replay, provenance, and local naming. Thus the completion-facing route is a finite NameCert surface only.
\end{proof}

\falsifiablePrediction{Every accepted $\RealCompletionCauchyNetUp$ read exposes a directed index row, Cauchy-tail modulus row, StreamName finite-window row, RegSeqRat readback row, DyadicRatCore tolerance ledger, RealUp seal boundary, hsame transport, Cont replay, Pkg provenance, and local NameCert row before any completion-facing consumer may inspect it.}

\closureat{RealCompletionCauchyNetUp}{seedStr}

\begin{closurestatus}{\RealCompletionCauchyNetUp}
  \constructivestory{A $\RealCompletionCauchyNetUp$ packet is generated from directed index comparisons, Cauchy-tail modulus thresholds, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerances, a RealUp seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-completion-cauchy-net-carrier}, \autoref{thm:real-completion-cauchy-net-namecert-obligations}, and \autoref{thm:real-completion-cauchy-net-completion-facing-route} seed the finite Cauchy-net completion-facing route over directed indices, tail moduli, finite windows, regular rational readback, dyadic tolerance accounting, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient nets, countable choice, host net equality, ambient Real completeness, theorem-checked verification, axiom-clean audit, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, directed-index exposure, Cauchy-tail modulus exposure, finite-window exposure, regular rational readback, dyadic tolerance accounting, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RealCompletionCauchyNetUp.}
\end{closurestatus}
