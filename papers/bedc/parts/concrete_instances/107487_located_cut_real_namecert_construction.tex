\chapter{A Concrete Naming Certificate for $\LocatedCutRealUp$}
\label{ch:concrete-instances-located-cut-real-namecert}
\origin{human}

Following Bishop and Bridges, Constructive Analysis, the $\LocatedCutRealUp$ packet records the constructive comparison surface where located lower and upper cut data are read as finite real-name evidence before any endpoint comparison is exported. It sits between the regular Cauchy real-name route of $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, and $\DyadicRatCoreUp$ in \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the cut-facing siblings $\LocatedRealUp$, $\DedekindCutUp$, and $\DyadicLocatedCutUp$ in \autoref{ch:concrete-instances-locatedreal-namecert}, \autoref{ch:concrete-instances-dedekindcut-namecert}, and \autoref{ch:concrete-instances-dyadic-located-cut-namecert}. The packet exposes located comparison as a finite NameCert route; it does not claim Dedekind completeness, quotient-real equality, trichotomy, countable choice, selected suprema, or an ambient host real order.

\begin{definition}[LocatedCutReal carrier]
\label{def:located-cut-real-carrier}
A $\LocatedCutRealUp$ carrier is a finite $\BHist$ packet
$$
L=(D,G,W,R,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the located lower-and-upper cut ledger. It records finite lower cells, upper cells, gap requests, and comparison witnesses in the style of the located cut surface.
\item $G$ is the dyadic gap row over $\DyadicRatCoreUp$. It supplies the displayed dyadic endpoint and radius cells by which a finite gap request is read.
\item $W$ is the $\StreamNameUp$ schedule naming the finite observation windows opened by the comparison request.
\item $R$ is the $\RegSeqRatUp$ handoff row. It reads the endpoint-normalized windows as regular rational data before any real seal is consumed.
\item $Q$ is the $\LocatedRealUp$ comparison row. It records the finite locatedness evidence used by order, metric, and comparison consumers.
\item $E$ is the terminal $\RealUp$ seal. It can be read only after $D,G,W,R,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for cut cells, dyadic endpoints, schedules, regular-rational handoff, located comparison, real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay for every consumer read through the same ordered route $D,G,W,R,Q,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LocatedCutRealUp$, $\LocatedRealUp$, $\DedekindCutUp$, $\DyadicLocatedCutUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCutRealUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,G,W,R,Q,E,H,C,P,N$. It has no coordinate for quotient reals, selected cuts, Dedekind completeness, trichotomy, countable choice, or host analytic order.
\end{definition}

\begin{theorem}[LocatedCutReal NameCert obligations]
\label{thm:located-cut-real-namecert-obligations}
For an accepted $\LocatedCutRealUp$ carrier
$$
L=(D,G,W,R,Q,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedCutRealUp}$ surface consists exactly of carrier admission for the located cut ledger, dyadic gap row, finite stream schedule, regular-rational handoff, located real comparison row, terminal real seal, componentwise transport, displayed replay, provenance, and local naming.

The obligation surface may cite the $\DedekindCutUp$ lower-and-upper cut discipline, the $\DyadicLocatedCutUp$ endpoint route, the $\LocatedRealUp$ comparison row, and the $\RealUp$ seal route. It cannot add a selected supremum, quotient-real representative, ambient completeness proof, trichotomy branch, or external comparison oracle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-cut-real-carrier}. The rows $D,G,W,R,Q,E$ expose the located cut cells, dyadic endpoint budgets, finite schedules, regular-rational readback, located real comparison, and terminal real seal in order.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier stores no quotient real, selected cut, completeness witness, trichotomy branch, choice selector, or external order oracle, the local NameCert surface is exactly the displayed finite route.
\end{proof}

\begin{theorem}[LocatedCutReal Cauchy comparison]
\label{thm:located-cut-real-cauchy-comparison}
Every comparison consumer of an accepted $\LocatedCutRealUp$ packet factors through the finite Cauchy-facing route
$$
D,G,W,R,Q,E,H,C,P,N .
$$
The read may use $\DyadicRatCoreUp$ endpoint rows, $\StreamNameUp$ windows, $\RegSeqRatUp$ handoff data, the $\LocatedRealUp$ comparison row, and the terminal $\RealUp$ seal. It may not bypass those rows by invoking a host Dedekind-completeness theorem, quotient-real equality, selected supremum, trichotomy, countable choice, or an ambient real-order decision procedure.
\end{theorem}
\begin{proof}
Begin with the located cut ledger $D$ and dyadic gap row $G$. They provide only finite lower, upper, endpoint, radius, and gap cells, so the comparison request starts as a displayed packet route rather than as an ambient real-order statement.

Next read the schedule $W$ and handoff row $R$. These rows force endpoint data to pass through finite stream windows and regular-rational budgets before any real-facing comparison is available.

The located comparison row $Q$ and terminal seal $E$ are downstream of that Cauchy-facing route. The rows $H,C,P,N$ preserve the same carrier under transport and replay, so no consumer can export the excluded host objects while still consuming the accepted $\LocatedCutRealUp$ packet.
\end{proof}

\begin{theorem}[LocatedCutReal nonescape]
\label{thm:located-cut-real-nonescape}
The terminal $\RealUp$ seal of an accepted $\LocatedCutRealUp$ carrier exports only the displayed located cut, dyadic endpoint, stream schedule, regular-rational handoff, and located comparison rows. In particular, it exports no Dedekind completeness principle, quotient real, selected cut representative, trichotomy branch, countable-choice selector, or host analytic order.
\end{theorem}
\begin{proof}
Read the seal row $E$ through \autoref{thm:located-cut-real-cauchy-comparison}. The seal is available only after $D,G,W,R,Q$ have been displayed, and every transported or replayed use is still sourced and named by $H,C,P,N$.

The absent objects listed in the statement are not fields of \autoref{def:located-cut-real-carrier}. Therefore a terminal seal read can only expose the displayed finite comparison route and cannot be reinterpreted as an external completeness, quotient, choice, trichotomy, or host-order witness.
\end{proof}

\falsifiablePrediction{An accepted $\LocatedCutRealUp$ comparison read is refuted if the located cut ledger, dyadic endpoint row, StreamName schedule, RegSeqRat handoff, LocatedReal comparison row, Real seal, transport, replay, provenance, or local naming can be removed while preserving the same packet claim.}

\independenceWitness{real, regseqrat, streamname, dyadicratcore, locatedreal, dedekindcut, dyadic-located-cut: $\LocatedCutRealUp$ is not bijective to any listed sibling because it jointly carries located cut cells, dyadic endpoint budgets, finite schedules, regular-rational handoff, located comparison, terminal real sealing, transport, replay, provenance, and local naming.}

\closureat{LocatedCutRealUp}{seedStr}

\begin{closurestatus}{\LocatedCutRealUp}
  \constructivestory{A $\LocatedCutRealUp$ packet is generated from located lower-and-upper cut ledgers, dyadic endpoint and gap budgets, StreamName finite windows, RegSeqRat handoff rows, LocatedReal comparison evidence, a RealUp terminal seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-cut-real-carrier}, \autoref{thm:located-cut-real-namecert-obligations}, \autoref{thm:located-cut-real-cauchy-comparison}, and \autoref{thm:located-cut-real-nonescape} seed the finite constructive located-cut comparison surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Dedekind completeness, quotient reals, selected suprema, trichotomy, countable choice, ambient host real order, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located cut exactness, dyadic endpoint transport, regular-Cauchy handoff, located comparison preservation, and Real seal nonescape for BEDC.Derived.LocatedCutRealUp.}
\end{closurestatus}
