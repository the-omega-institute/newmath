\chapter{A Concrete Naming Certificate for $\RegularCauchyDifferenceModulusUp$}
\label{ch:concrete-instances-regular-cauchy-difference-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyDifferenceModulusUp$ packet records a finite modulus for reading the difference of two regular Cauchy rational names. It sits over the regular source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the displayed pointwise-difference route of \autoref{ch:concrete-instances-regularcauchydifference-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet supplies a Real and Completion consumer with a finite difference-modulus surface; it does not assert quotient stream equality, selected limits, countable choice, trichotomy, host real subtraction, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy difference-modulus carrier]
\label{def:regular-cauchy-difference-modulus-carrier}
A $\RegularCauchyDifferenceModulusUp$ carrier is a finite $\BHist$ packet
$$
D=(X,Y,W_X,W_Y,Q_X,Q_Y,\Delta,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two $\RegSeqRatUp$ source rows whose rational observations are compared only through finite requests.
\item $W_X$ and $W_Y$ are the aligned $\StreamNameUp$ window rows exposing the finite prefixes of $X$ and $Y$ at a requested output precision.
\item $Q_X$ and $Q_Y$ are the $\DyadicRatCoreUp$ tolerance rows read from the two source windows.
\item $\Delta$ is the displayed $\DyadicRatCoreUp$ difference row obtained by subtracting the two endpoint rows named by $Q_X$ and $Q_Y$.
\item $M$ is the finite modulus row assigning each requested difference precision to the source-window and dyadic-tolerance requests carried by $W_X,W_Y,Q_X,Q_Y,\Delta$.
\item $E$ is the terminal $\RealUp$ seal row for the difference readback after the modulus row has been consumed.
\item $H$ records componentwise $\hsame$ transport for the two regular sources, both window rows, both dyadic tolerance rows, the difference row, the modulus row, the Real seal, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay for reading windows, dyadic tolerances, difference rows, modulus entries, Real sealing, and structural transport.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyDifferenceModulusUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyDifferenceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyDifferenceModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,W_X,W_Y,Q_X,Q_Y,\Delta,M,E,H,C,P,N$. It has no coordinate for quotient real equality, selected representatives, countable-choice moduli, trichotomy, host subtraction, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy difference-modulus NameCert obligations]
\label{thm:regular-cauchy-difference-modulus-obligations}
For an accepted $\RegularCauchyDifferenceModulusUp$ packet $D=(X,Y,W_X,W_Y,Q_X,Q_Y,\Delta,M,E,H,C,P,N)$, the local NameCert surface consists exactly of carrier admission for the two regular rational sources, paired finite-window inspection, dyadic source tolerance reads, dyadic difference formation, modulus exactness, Real-seal handoff, componentwise transport, displayed replay, provenance, and local naming. Every accepted difference-modulus read follows
$$
X,Y\longmapsto W_X,W_Y\longmapsto Q_X,Q_Y\longmapsto \Delta\longmapsto M\longmapsto E
$$
together with $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-difference-modulus-carrier}. The source coordinates $X,Y$ are the only regular-rational inputs, and their observations are exposed through the finite window rows $W_X$ and $W_Y$.

The dyadic rows $Q_X,Q_Y$ record the source tolerances read at those windows. The difference row $\Delta$ is downstream of these endpoint reads, so the modulus row $M$ can assign a requested difference precision only to the displayed window and tolerance rows already present in the carrier.

The Real seal $E$ is read after $M$, while $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no quotient equality, selected representative, countable-choice, host-subtraction, or ambient-completeness coordinate, the listed obligations exhaust the local certificate.
\end{proof}

\begin{theorem}[Regular Cauchy difference-modulus Real-seal handoff]
\label{thm:regular-cauchy-difference-modulus-real-seal-handoff}
Every $\RealUp$ or completion-route consumer of an accepted $\RegularCauchyDifferenceModulusUp$ packet receives the difference readback only through the displayed route from $X,Y$ to $W_X,W_Y,Q_X,Q_Y,\Delta,M,E$. The handoff preserves the two $\RegSeqRatUp$ source rows, the paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ tolerance and difference rows, the finite modulus row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming; it exports no quotient stream equality, selected limit, countable-choice schedule, trichotomy, host real subtraction, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:regular-cauchy-difference-modulus-obligations}. They place every accepted read on the finite route $X,Y,W_X,W_Y,Q_X,Q_Y,\Delta,M,E,H,C,P,N$.

A completion-route consumer can inspect only the two source rows through the paired windows and the dyadic source tolerances. It must therefore read the displayed difference row $\Delta$ and the modulus row $M$ before any difference readback is available.

A $\RealUp$ consumer sees the terminal seal $E$ only after the same modulus row has been consumed. The structural rows preserve this route, so no excluded quotient, selector, choice schedule, trichotomy, host-subtraction, or completeness object can be exported.
\end{proof}

\falsifiablePrediction{Every $\RegularCauchyDifferenceModulusUp$ read factors through two RegSeqRat source rows, paired StreamName window rows, paired DyadicRatCore tolerance rows, a DyadicRatCore difference row, a finite modulus row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{regseqrat, streamname, dyadicratcore, regular-cauchy-difference, real: $\RegularCauchyDifferenceModulusUp$ is not bijective to any listed sibling because it jointly carries two regular sources, paired finite windows, dyadic source tolerances, a displayed dyadic difference, a finite modulus row, terminal Real sealing, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyDifferenceModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchyDifferenceModulusUp}
  \constructivestory{A $\RegularCauchyDifferenceModulusUp$ packet is generated from two RegSeqRat source rows, paired StreamName windows, paired DyadicRatCore tolerance rows, a DyadicRatCore difference row, a finite modulus row, a terminal RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-difference-modulus-carrier}, \autoref{thm:regular-cauchy-difference-modulus-obligations}, and \autoref{thm:regular-cauchy-difference-modulus-real-seal-handoff} seed the finite regular-Cauchy difference-modulus carrier, certificate surface, and Real-and-Completion handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected limits, countable choice, trichotomy, host real subtraction, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires checked carrier admission, paired regular-source exposure, window exactness, dyadic tolerance and difference budgeting, modulus exactness, Real sealing, transport, replay, provenance, and local NameCert non-escape for $\RegularCauchyDifferenceModulusUp$.}
\end{closurestatus}
