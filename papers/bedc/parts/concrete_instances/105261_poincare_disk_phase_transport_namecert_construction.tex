\chapter{A Concrete Naming Certificate for $\PoincareDiskPhaseTransportUp$}
\label{ch:concrete-instances-poincare-disk-phase-transport-namecert}
\origin{ai}

The $\PoincareDiskPhaseTransportUp$ packet realizes the Poincare disk reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite BEDC data. It reads visible phase coordinates through \autoref{ch:concrete-instances-complex-namecert}, disk coordinates and boundary rows through \autoref{ch:concrete-instances-unitdisk-namecert}, reversible transport through \autoref{ch:concrete-instances-unitarygroup-namecert}, flow-style evolution through \autoref{ch:concrete-instances-dynsystem-namecert}, and boundary-circle consumers through \autoref{ch:concrete-instances-s1-namecert}. The packet is a representation carrier over displayed histories, transports, flows, and provenance; it is not a host hyperbolic plane, not a global observer clock, not a physical cosmology, and not an internal point named $\mathsf{T}$.

\begin{definition}[Poincare disk phase-transport carrier]
\label{def:poincare-disk-phase-transport-carrier}
A $\PoincareDiskPhaseTransportUp$ carrier is a finite $\BHist$ packet
$$
P=(L,Z,U,B,\Gamma,V,F,O,A,H,C,R,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the focused finite multi-$\Hist$ ledger whose visible records are being represented.
\item $Z$ is the $\ComplexUp$ phase-coordinate row used for displayed amplitude, direction, or phase readback.
\item $U$ is the $\UnitDiskUp$ disk-coordinate row, including the finite product-metric radius ledger and the optional $\SOneUp$ boundary consumer row.
\item $B$ is the boundary-far-end row, read only as a representation boundary over the disk packet and never as an interior object.
\item $\Gamma$ is the $\UnitaryGroupUp$ reversible-transport row, recording classifier-preserving movement of the visible phase or disk surface.
\item $V$ is the $\DynSystemUp$ flow row for finite evolution of the displayed disk-coordinate records.
\item $F$ is the fiber and provenance row, retaining the hidden-side source distinctions behind each visible disk or phase readout.
\item $O$ is the observer-focus row tying the disk surface back to the local ledger $L$ rather than to a global observer or global clock.
\item $A$ is the audit-refusal row excluding internal $\mathsf{T}$, host hyperbolic geometry, physical light-speed derivation, global universe digest, and subject-substance readings.
\item $H$ records componentwise $\hsame$ transport for the ledger, phase, disk, boundary, reversible-transport, flow, fiber, focus, and audit rows.
\item $C$ records displayed $\Cont$ replay through finite disk coordinates, reversible transport, flow readback, fiber provenance, and refusal rows.
\item $R$ is the $\Pkg$ provenance over $\PoincareDiskPhaseTransportUp$, $\ComplexUp$, $\UnitDiskUp$, $\SOneUp$, $\UnitaryGroupUp$, $\DynSystemUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PoincareDiskPhaseTransportUp}$ row.
\end{enumerate}
The carrier compares packets only through these rows. It contains no coordinate for a completed Poincare disk, no host hyperbolic metric, no quotient boundary, no global physical evolution law, no subject-substance carrier, and no object-level $\mathsf{T}$ inside the disk.
\end{definition}

\begin{theorem}[Poincare disk phase-transport ledger]
\label{thm:poincare-disk-phase-transport-ledger}
For every accepted $\PoincareDiskPhaseTransportUp$ carrier
$$
P=(L,Z,U,B,\Gamma,V,F,O,A,H,C,R,N),
$$
every disk-phase transport read factors through the finite route
$$
L,Z,U \longmapsto B,\Gamma,V,F,O,A \longmapsto H,C,R,N.
$$
The route exports visible disk-coordinate, reversible-transport, flow, fiber-provenance, observer-focus, and refusal data, while refusing host hyperbolic geometry, quotient boundary equality, global observer time, physical light-speed derivation, internal $\mathsf{T}$, and subject-substance readings.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:poincare-disk-phase-transport-carrier}. The visible surface begins with the local ledger $L$, the complex phase row $Z$, and the disk-coordinate row $U$.

The representation rows are then read in order. The boundary row $B$ records a far-end representation boundary, $\Gamma$ supplies reversible classifier-preserving transport, $V$ supplies finite flow-style evolution, $F$ retains the fiber provenance behind the readout, $O$ localizes the surface to the focused ledger, and $A$ records the refused readings.

Transport and replay do not add new geometry. The rows $H$ and $C$ preserve and replay the same finite coordinates, while $R$ and $N$ record provenance and local naming.

Since the packet has no coordinate for a completed disk geometry, quotient boundary, global clock, physical constant, subject-substance, or internal $\mathsf{T}$, no consumer of the ledger can export those stronger readings.
\end{proof}

\falsifiablePrediction{Removing the fiber-provenance row or the audit-refusal row lets a disk-phase consumer identify visible disk-coordinate equality with source equality or with an internal $\mathsf{T}$ reading, so the packet is no longer accepted.}
\independenceWitness{complex, unitdisk, unitarygroup, dynsystem, s1: none is bijective to $\PoincareDiskPhaseTransportUp$ because the phase packet jointly carries visible complex coordinates, disk coordinates, optional boundary consumption, reversible transport, finite flow, fiber provenance, observer focus, refusal ledger, transport, replay, provenance, and local naming.}

The Lean follow-up for this packet must provide explicit $\mathsf{Nontrivial}\ \PoincareDiskPhaseTransportUp$ and $\mathsf{FieldFaithful}\ \PoincareDiskPhaseTransportUp$ TasteGate instances. Those instances must keep $L,Z,U,B,\Gamma,V,F,O,A,H,C,R,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{PoincareDiskPhaseTransportUp}{seedStr}

\begin{closurestatus}{\PoincareDiskPhaseTransportUp}
  \constructivestory{A $\PoincareDiskPhaseTransportUp$ packet is generated from a focused finite multi-Hist ledger, Complex phase coordinates, UnitDisk disk coordinates with optional SOne boundary consumption, UnitaryGroup reversible transport, DynSystem finite flow rows, fiber provenance, observer focus, refusal ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poincare-disk-phase-transport-carrier} and \autoref{thm:poincare-disk-phase-transport-ledger} seed the finite disk-coordinate, reversible-transport, flow, fiber-provenance, and refusal route for the hyperbolic phase representation.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed Poincare disk geometry, host hyperbolic metric, quotient boundary equality, global observer time, physical light-speed derivation, internal $\mathsf{T}$, subject-substance, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, disk-coordinate visibility, reversible transport, flow replay, fiber-provenance preservation, refusal exactness, TasteGate nontriviality, field faithfulness, and local naming for BEDC.Derived.PoincareDiskPhaseTransportUp.}
\end{closurestatus}
