\chapter{A Concrete Naming Certificate for $\InnovationInputGeometryBoundaryUp$}
\label{ch:concrete-instances-innovation-input-geometry-boundary-namecert}
\origin{ai}

The $\InnovationInputGeometryBoundaryUp$ packet realizes the geometry-boundary branch of \autoref{ch:visions-innovation-input-geometry-boundary} as finite observer-channel, boundary, response-slot, transport, replay, provenance, and local NameCert data. The observer-channel row is consumed only after the kernel acceptance boundary of \autoref{ch:concrete-instances-kernel-acceptance-boundary-namecert} admits the row, and it is routed as ledger-visible input in the sense of \autoref{ch:concrete-instances-vision-ledger-route-namecert}, so the geometry-boundary packet is downstream of both acceptance and ledger exposure before any metric-facing or dynamics-facing consumer reads it. It names the point at which positive innovation-input ledger rows become metric-facing and dynamics-facing certificates without asserting field equations, empirical matter coupling, a completed quantum-gravity model, or agreement between tick-reflected and reflection-positive propagation cones.

\begin{definition}[Innovation-input geometry boundary carrier]
\label{def:innovation-input-geometry-boundary-carrier}
An $\InnovationInputGeometryBoundaryUp$ carrier is a finite $\BHist$ packet
$$
I=(O,B,R,T,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the observer-channel row naming the input ledger, observer address, and visible channel through which innovation data enter the boundary.
\item $B$ is the geometry-boundary row recording the metric-facing and dynamics-facing certificate slots reached by that channel.
\item $R$ is the response-slot row, separating admissible response data from field equations or empirical coupling claims.
\item $T$ is the transport row that carries the displayed observer-channel and boundary rows into the response slot.
\item $E$ is the replay envelope row, recording which finite ledger entries can be replayed before a geometry-facing consumer reads the boundary.
\item $H$ records componentwise $\hsame$ transport for $O,B,R,T,E$ and the structural rows.
\item $C$ records displayed $\Cont$ replay from observer-channel input through geometry-boundary placement, response-slot admission, transport, and replay envelope.
\item $P$ is the $\Pkg$ provenance over $\InnovationInputGeometryBoundaryUp$, the source vision of \autoref{ch:visions-innovation-input-geometry-boundary}, observer-channel rows, geometry-boundary rows, response-slot rows, transport, replay, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\InnovationInputGeometryBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $O,B,R,T,E,H,C,P,N$. It has no coordinate for a physical field equation, empirical matter coupling, completed quantum-gravity model, hidden propagation cone, ambient metric semantics, or external observer oracle.
\end{definition}

\begin{theorem}[Innovation-input geometry boundary NameCert obligations]
\label{thm:innovation-input-geometry-boundary-namecert-obligations}
For every accepted $\InnovationInputGeometryBoundaryUp$ carrier $I=(O,B,R,T,E,H,C,P,N)$, the local $\NameCert_{\InnovationInputGeometryBoundaryUp}$ surface consists exactly of observer-channel admission through $O$, geometry-boundary placement through $B$, response-slot admission through $R$, transport through $T$, replay-envelope exposure through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, local naming through $N$, and non-escape from field equations, empirical matter coupling, completed quantum-gravity semantics, hidden propagation cones, ambient metric semantics, and external observer oracles.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:innovation-input-geometry-boundary-carrier}. The visible packet is $I=(O,B,R,T,E,H,C,P,N)$, so observer-channel input, boundary placement, response-slot admission, transport, replay envelope, and structural rows are all displayed.

The boundary side is finite. The row $O$ supplies the observer-channel ledger, $B$ places it at the metric-facing and dynamics-facing boundary, $R$ restricts the accepted response slot, and $T,E$ record transport and replay before a consumer reads the boundary.

Finally read $H,C,P,N$. These rows transport, replay, source, and name the same finite route, while the excluded physical and ambient semantics have no carrier coordinate. Thus the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Innovation-input geometry boundary response handoff]
\label{thm:innovation-input-geometry-boundary-response-handoff}
Every metric-facing or dynamics-facing consumer of an accepted $\InnovationInputGeometryBoundaryUp$ packet must read the finite route
$$
O \longmapsto B \longmapsto R \longmapsto T \longmapsto E \longmapsto H,C,P,N .
$$
The consumer receives observer-channel, geometry-boundary, response-slot, transport, replay, provenance, and NameCert data; it receives no field equation, empirical matter coupling, completed quantum-gravity model, hidden propagation cone, ambient metric semantics, or external observer oracle.
\end{theorem}
\begin{proof}
Start with the accepted carrier. The observer-channel row $O$ is read first, and the boundary row $B$ is the only route by which that input reaches a metric-facing or dynamics-facing certificate slot.

The response boundary is then recorded by $R,T,E$. The row $R$ admits the displayed response slot, $T$ transports the observer-channel and boundary data into it, and $E$ records the finite replay envelope available to downstream consumers.

Apply the obligation inventory of \autoref{thm:innovation-input-geometry-boundary-namecert-obligations}. The structural rows preserve the same route under transport and replay, so the handoff exports finite NameCert data rather than any of the excluded physical or ambient semantics.
\end{proof}

\falsifiablePrediction{A replay that reaches a metric-facing or dynamics-facing geometry-boundary consumer without displaying observer-channel row $O$, boundary row $B$, response-slot row $R$, transport row $T$, replay envelope $E$, and structural rows $H,C,P,N$ refutes this packet.}

\independenceWitness{innovation-input-modular-arithmetic-boundary, information-stratum-noncollapse, vision-ledger-route, kernel-acceptance-boundary: none is bijective to $\InnovationInputGeometryBoundaryUp$ because this packet binds observer-channel input, geometry-boundary placement, response-slot admission, transport, replay envelope, provenance, and local NameCert naming in one finite boundary route.}

The Lean TasteGate file \texttt{lean4/BEDC/Derived/InnovationInputGeometryBoundaryUp/TasteGate.lean} must include explicit $\Nontrivial\ \InnovationInputGeometryBoundaryUp$ and $\FieldFaithful\ \InnovationInputGeometryBoundaryUp$ instances preserving $O,B,R,T,E,H,C,P,N$ as distinct rows before any checked target can cite \texttt{BEDC.Derived.InnovationInputGeometryBoundaryUp}.

\closureat{InnovationInputGeometryBoundaryUp}{seedStr}

\begin{closurestatus}{\InnovationInputGeometryBoundaryUp}
  \constructivestory{An $\InnovationInputGeometryBoundaryUp$ packet is generated from an observer-channel row, geometry-boundary row, response-slot row, transport row, replay envelope, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:innovation-input-geometry-boundary-carrier}, \autoref{thm:innovation-input-geometry-boundary-namecert-obligations}, and \autoref{thm:innovation-input-geometry-boundary-response-handoff} seed the finite observer-channel to geometry-boundary response surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close field equations, empirical matter coupling, completed quantum-gravity semantics, hidden propagation cones, ambient metric semantics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, observer-channel exposure, boundary placement, response-slot exactness, transport and replay stability, provenance exhaustion, and TasteGate field faithfulness for BEDC.Derived.InnovationInputGeometryBoundaryUp.}
\end{closurestatus}
