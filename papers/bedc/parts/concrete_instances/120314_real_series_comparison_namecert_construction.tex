\chapter{A Concrete Naming Certificate for $\RealSeriesComparisonUp$}
\label{ch:concrete-instances-real-series-comparison-namecert}
\origin{human}

Following Bishop--Bridges comparison-test practice, the $\RealSeriesComparisonUp$ packet records a finite real-series comparison route that may read signed source terms only through displayed windows and bounded domination evidence. It consumes the positive comparison surface of \autoref{ch:concrete-instances-positive-series-comparison-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}. The packet names a comparison-test certificate for later $\RealUp$ series-tail estimates; it is not an ambient convergence theorem, a host infinite-sum construction, quotient equality of term streams, or a selected limit.

\begin{definition}[Real series comparison carrier]
\label{def:real-series-comparison-carrier}
A $\RealSeriesComparisonUp$ carrier is a finite $\BHist$ packet
$$
C=(S,B,P,W,D,Q,E,H,R,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source $\RealSeriesUp$ row whose finite term windows are being compared.
\item $B$ is the bounding $\RealSeriesUp$ row read through the positive comparison packet.
\item $P$ is the $\PositiveSeriesComparisonUp$ witness row, exposing the nonnegative domination surface already named by \autoref{ch:concrete-instances-positive-series-comparison-namecert}.
\item $W$ is the $\StreamNameUp$ window row listing the finite indices, signs, tail requests, and comparison requests under inspection.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording absolute-value bounds, domination checks, and requested error budgets for the displayed windows.
\item $Q$ is the $\RegSeqRatUp$ readback row used when the compared windows are routed toward regular rational data.
\item $E$ is the terminal $\RealUp$ seal available only after $S,B,P,W,D,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source series, bounding series, positive comparison witness, windows, dyadic ledger, readback, terminal seal, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from a comparison request through $S,B,P,W,D,Q,E,H,G,N$.
\item $G$ is the $\Pkg$ provenance over $\RealSeriesComparisonUp$, $\RealSeriesUp$, $\PositiveSeriesComparisonUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealSeriesComparisonUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,B,P,W,D,Q,E,H,R,G,N$. It has no coordinate for ambient convergence, host infinite sums, quotient equality of term streams, selected limits, countable choice, or detached $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real series comparison obligation surface]
\label{thm:real-series-comparison-obligation-surface}
For every accepted $\RealSeriesComparisonUp$ carrier
$$
C=(S,B,P,W,D,Q,E,H,R,G,N),
$$
the local $\NameCert_{\RealSeriesComparisonUp}$ surface consists exactly of source-series exposure, bounding-series exposure, positive comparison witness exposure, finite stream-window exposure, dyadic tolerance-ledger exposure, regular rational readback, terminal real sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-series-comparison-carrier}. The rows $S,B,P,W,D,Q,E,H,R,G,N$ are the complete finite packet, so carrier admission and classifier exactness are componentwise projections.

Read the comparison rows next. The source row $S$ and bounding row $B$ are inspected only through the finite window row $W$, while $P$ imports the already displayed positive comparison witness and $D$ records the dyadic domination ledger for those windows.

Finally inspect $Q,E,H,R,G,N$. These rows read back, seal, transport, replay, source, and name the same finite route. Since the carrier has no coordinate for host sums, selected limits, quotient equality, or ambient convergence, the obligation surface is exactly the stated packet.
\end{proof}

\begin{theorem}[Real series comparison non-escape]
\label{thm:real-series-comparison-nonescape}
Every $\RealUp$ or completion-facing consumer of an accepted $\RealSeriesComparisonUp$ carrier reaches the terminal seal only through
$$
\RealSeriesUp \longmapsto \PositiveSeriesComparisonUp \longmapsto \StreamNameUp,\DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
Thus a comparison-test read may export finite tail-estimate evidence, but it cannot export ambient convergence, a host infinite sum, quotient equality of term streams, selected limits, countable choice, or detached real-name completeness.
\end{theorem}
\begin{proof}
Start with the source and bounding series rows. They expose only finite $\RealSeriesUp$ observations, and the bounding row is consumable as comparison evidence only through the $\PositiveSeriesComparisonUp$ witness row.

Next read the window and tolerance rows. The $\StreamNameUp$ row supplies the inspected indices and requested tail windows, while the $\DyadicRatCoreUp$ row supplies the finite domination and error-budget checks used by the comparison route.

The readback and terminal rows $Q,E$ are downstream of those finite rows. Componentwise transport, displayed replay, provenance, and local naming preserve the same packet, so no consumer can escape to an unlisted convergence principle or selected limit.
\end{proof}

\falsifiablePrediction{Every accepted $\RealSeriesComparisonUp$ read exposes source and bounding $\RealSeriesUp$ rows, a $\PositiveSeriesComparisonUp$ witness, finite $\StreamNameUp$ windows, a $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before a comparison conclusion is consumed.}

\independenceWitness{positive-series-comparison, realseries, streamname, dyadicratcore, regseqrat, real: $\RealSeriesComparisonUp$ is not bijective to any listed sibling because it jointly carries signed source windows, bounding series windows, positive comparison evidence, dyadic domination ledgers, regular readback, terminal real sealing, transport, replay, provenance, and local naming.}

\closureat{RealSeriesComparisonUp}{seedStr}

\begin{closurestatus}{\RealSeriesComparisonUp}
  \constructivestory{A $\RealSeriesComparisonUp$ packet is generated from source and bounding RealSeries rows, a PositiveSeriesComparison witness row, finite StreamName windows, a DyadicRatCore tolerance ledger, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-series-comparison-carrier}, \autoref{thm:real-series-comparison-obligation-surface}, and \autoref{thm:real-series-comparison-nonescape} seed the finite real-series comparison route from source and bounding series windows through positive comparison evidence, dyadic tolerance accounting, regular readback, and terminal Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not claim ambient convergence, host sums, quotient equality of term streams, selected limits, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, comparison-window admission, dyadic domination exactness, positive-comparison witness routing, readback stability, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.RealSeriesComparisonUp.}
\end{closurestatus}
