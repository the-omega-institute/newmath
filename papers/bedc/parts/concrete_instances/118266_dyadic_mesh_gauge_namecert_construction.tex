\chapter{A Concrete Naming Certificate for $\DyadicMeshGaugeUp$}
\label{ch:concrete-instances-dyadic-mesh-gauge-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\DyadicMeshGaugeUp$ packet records the finite dyadic mesh gauges used to schedule regular Cauchy approximations before a real-name seal is read. It sits after the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite gauge ledger, not a quotient of streams, a selected limit, countable choice, open-cover compactness, or host real equality.

\begin{definition}[Dyadic mesh gauge carrier]
\label{def:dyadic-mesh-gauge-carrier}
A $\DyadicMeshGaugeUp$ carrier is a finite $\BHist$ packet
$$
G=(D,S,W,M,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, listing the dyadic precision requests and mesh widths inspected by the gauge.
\item $S$ is the $\StreamNameUp$ source row, listing the finite name windows opened by each precision request.
\item $W$ is the finite mesh-window ledger pairing dyadic widths with the stream windows that witness them.
\item $M$ is the gauge row, recording the displayed bound beyond which the mesh is read at the requested dyadic width.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from $D,S,W,M$.
\item $E$ is the terminal $\RealUp$ seal available only after the regular readback row has been displayed.
\item $H$ records componentwise $\hsame$ transport for dyadic tolerances, stream windows, mesh windows, gauge bounds, readbacks, real seals, replay, provenance, and naming.
\item $C$ records displayed $\Cont$ replay from a precision request through the same finite mesh-gauge route.
\item $P$ is the $\Pkg$ provenance over $\DyadicMeshGaugeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicMeshGaugeUp}$ row.
\end{enumerate}
The classifier compares accepted gauges only through $D,S,W,M,Q,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected host limit, countable-choice schedule, open-cover compactness, Heine--Borel, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.DyadicMeshGaugeUp.TasteGate.DyadicMeshGaugeTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Dyadic mesh gauge NameCert obligations]
\label{thm:dyadic-mesh-gauge-namecert-obligations}
For every accepted $\DyadicMeshGaugeUp$ carrier
$$
G=(D,S,W,M,Q,E,H,C,P,N),
$$
the local $\NameCert_{\DyadicMeshGaugeUp}$ surface consists exactly of dyadic tolerance admission through $D$, stream-window admission through $S$, finite mesh-window exposure through $W$, gauge-bound exactness through $M$, $\RegSeqRatUp$ readback through $Q$, terminal $\RealUp$ sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-mesh-gauge-carrier}. The rows $D,S,W,M,Q,E,H,C,P,N$ are the whole visible packet, so the local naming surface can only list those rows.

Read the approximation rows in order. The dyadic row $D$ supplies the precision request, the stream row $S$ supplies the finite windows, and the mesh ledger $W$ records how those windows are paired with dyadic widths.

The gauge row $M$ then supplies the finite bound used by the regular readback row $Q$. The terminal seal $E$ is downstream of that readback, while $H,C,P,N$ transport, replay, source, and name the same finite route.

Since no carrier coordinate stores quotient stream equality, selected limits, countable choice, open-cover compactness, Heine--Borel, or ambient real completeness, none of those principles belongs to the local obligation surface.
\end{proof}

\begin{theorem}[Dyadic mesh gauge regular-Cauchy handoff]
\label{thm:dyadic-mesh-gauge-regular-cauchy-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\DyadicMeshGaugeUp$ packet reads through the displayed route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \DyadicMeshGaugeUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
using $D,S,W,M,Q,E,H,C,P,N$. The handoff exports finite dyadic mesh-gauge data for regular Cauchy approximation schedules; it exports no host real equality, quotient stream equality, selected limit, countable-choice schedule, open-cover compactness, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $G=(D,S,W,M,Q,E,H,C,P,N)$. A regular-Cauchy read must expose the dyadic tolerance row $D$ and the finite stream-window row $S$ before a mesh window or gauge bound is available.

Use the mesh ledger $W$ and gauge row $M$. These rows bind each displayed dyadic width to the finite windows and the bound used by the approximation schedule, so a consumer cannot replace the route by a selected host limit or an unrecorded choice function.

The readback row $Q$ is then the only $\RegSeqRatUp$ row visible to downstream consumers, and the Real seal $E$ is reached only after $Q$. Componentwise transport and replay through $H,C$ preserve that same order, while $P,N$ source and name it.

Thus every regular-Cauchy or Real-facing read factors through the finite dyadic mesh-gauge route. The excluded host equality, quotient, choice, compactness, and completeness principles have no row from which to enter.
\end{proof}

\falsifiablePrediction{Every accepted $\DyadicMeshGaugeUp$ consumer displays $\DyadicRatCoreUp$, $\StreamNameUp$, finite mesh-window, gauge-bound, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows before it reads a terminal real-name approximation.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, regular-sequence-modulus: $\DyadicMeshGaugeUp$ is not bijective to any listed sibling because it jointly carries dyadic mesh widths, finite stream windows, gauge bounds, regular rational readback, terminal Real sealing, transport, replay, provenance, and local naming as one packet.}

\closureat{DyadicMeshGaugeUp}{seedStr}

\begin{closurestatus}{\DyadicMeshGaugeUp}
  \constructivestory{A $\DyadicMeshGaugeUp$ packet is generated from dyadic tolerance rows, finite StreamName windows, mesh-window ledgers, gauge bounds, RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-mesh-gauge-carrier}, \autoref{thm:dyadic-mesh-gauge-namecert-obligations}, and \autoref{thm:dyadic-mesh-gauge-regular-cauchy-handoff} seed the finite dyadic mesh-gauge route for regular-Cauchy and Real-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, open-cover compactness, Heine--Borel, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked dyadic tolerance admission, stream-window admission, mesh-window exposure, gauge-bound exactness, RegSeqRat readback, Real-seal routing, transport, replay, provenance, and local naming for BEDC.Derived.DyadicMeshGaugeUp.}
\end{closurestatus}
