\chapter{A Concrete Naming Certificate for $\BolzanoDedekindLocatedClusterUp$}
\label{ch:concrete-instances-bolzano-dedekind-located-cluster-namecert}
\origin{human}

Following Bolzano, Dedekind, and Bishop--Bridges constructive analysis, the $\BolzanoDedekindLocatedClusterUp$ packet records a finite located-cluster surface for bounded real sequences. It consumes the bounded sequence source of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the nested and located interval routes of \autoref{ch:concrete-instances-nestedinterval-namecert} and \autoref{ch:concrete-instances-locatedinterval-namecert}, the finite stream and regular-rational rows of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the boundary siblings \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}, \autoref{ch:concrete-instances-real-sequence-cluster-point-namecert}, and \autoref{ch:concrete-instances-sequentialcompact-namecert}. The packet is a located finite-window cluster certificate; it is not general sequential compactness, not a choice-selected subsequence theorem, and not an ambient completeness principle for $\RealUp$.

\begin{definition}[Bolzano--Dedekind located cluster carrier]
\label{def:bolzano-dedekind-located-cluster-carrier}
A $\BolzanoDedekindLocatedClusterUp$ carrier is a finite $\BHist$ packet
$$
K=(B,I,L,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the accepted $\BoundedRealSequenceUp$ source row, including only the inspected finite sequence windows and the displayed bound surface.
\item $I$ is the nested interval row, recording the retained finite interval chain used by the Bolzano--Dedekind search.
\item $L$ is the located interval row, carrying endpoint-apartness and retained-window location data for the intervals in $I$.
\item $S$ is the $\StreamNameUp$ schedule that names which sequence windows and interval windows are inspected.
\item $R$ is the $\RegSeqRatUp$ readback row for the scheduled sequence and interval windows.
\item $E$ is the $\RealUp$ cluster seal reached only after $B,I,L,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the bounded source, nested interval, located interval, stream, regular readback, and real-seal rows.
\item $C$ records displayed $\Cont$ replay from a cluster request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\BolzanoDedekindLocatedClusterUp$, $\BoundedRealSequenceUp$, $\NestedIntervalUp$, $\LocatedIntervalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BolzanoWeierstrassUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BolzanoDedekindLocatedClusterUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,I,L,S,R,E,H,C,P,N$. It has no coordinate for a globally selected subsequence, open-cover compactness, quotient real equality, real trichotomy, countable choice, general sequential compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bolzano--Dedekind located cluster NameCert obligations]
\label{thm:bolzano-dedekind-located-cluster-namecert-obligations}
For every accepted $\BolzanoDedekindLocatedClusterUp$ carrier
$$
K=(B,I,L,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\BolzanoDedekindLocatedClusterUp}$ surface consists exactly of carrier admission for the ten displayed rows, bounded-sequence source exposure through $B$, nested-window retention through $I$, located endpoint and interval comparison through $L$, finite stream scheduling through $S$, regular rational readback through $R$, terminal cluster sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. The obligation surface refuses general sequential compactness, choice-selected subsequences, open-cover compactness, quotient real equality, trichotomy, and ambient Real completeness as certificate rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bolzano-dedekind-located-cluster-carrier}. The bounded sequence can be read only through $B$, and $B$ itself supplies finite windows and a displayed bound surface rather than a global compactness theorem.

Next inspect the interval rows. The nested interval row $I$ records the retained finite chain, while the located interval row $L$ records the endpoint and apartness comparisons needed to keep the cluster window located. These rows are packet data, not consequences of an ambient interval-intersection theorem.

The readback and seal are downstream. The schedule row $S$ names the inspected windows, $R$ supplies the regular rational readback for those windows, and $E$ is visible only after those finite rows have been consumed.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since no excluded compactness, choice, quotient, trichotomy, or completeness coordinate occurs in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bolzano--Dedekind located cluster Real handoff]
\label{thm:bolzano-dedekind-located-cluster-real-handoff}
Every $\RealUp$-facing read of an accepted $\BolzanoDedekindLocatedClusterUp$ carrier factors through the finite route
$$
B,\ I,\ L,\ S,\ R,\ E,\ H,\ C,\ P,\ N .
$$
The handoff supplies a located cluster seal for bounded real sequence consumers and for the Bolzano--Weierstrass boundary siblings, but it does not supply general sequential compactness, a choice-selected subsequence, open-cover compactness, quotient real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the bounded source row. By \autoref{def:bolzano-dedekind-located-cluster-carrier}, the sequence source is the displayed $\BoundedRealSequenceUp$ row $B$, so any Real-facing consumer first reads finite sequence windows and the boundedness surface already present in $B$.

Use the NameCert obligations of \autoref{thm:bolzano-dedekind-located-cluster-namecert-obligations}. They place nested-window retention $I$ and located interval comparison $L$ before the stream schedule, regular rational readback, and terminal cluster seal.

Now read the seal row $E$. It is downstream of $S$ and $R$, so the cluster handoff reaches $\RealUp$ through finite stream and regular-rational data rather than through quotient real equality, trichotomy, or ambient completeness.

The structural rows $H,C,P,N$ preserve the same displayed route. Thus a Bolzano--Weierstrass or cluster-point consumer receives a located finite-window cluster seal, while the excluded compactness and choice principles remain outside the packet.
\end{proof}

\falsifiablePrediction{Every $\BolzanoWeierstrassUp$ boundary read that cites $\BolzanoDedekindLocatedClusterUp$ must display a $\BoundedRealSequenceUp$ source row, nested and located interval rows, StreamName windows, RegSeqRat readback, a RealUp seal, transport, replay, provenance, and local naming before any cluster consumer can proceed.}

\independenceWitness{boundedrealsequence, nestedinterval, locatedinterval, streamname, regseqrat, real, bolzanoweierstrass: $\BolzanoDedekindLocatedClusterUp$ is not bijective to any listed sibling because it jointly carries bounded source exposure, nested-window retention, located interval comparison, stream scheduling, regular readback, terminal cluster sealing, transport, replay, provenance, and local naming.}

\closureat{BolzanoDedekindLocatedClusterUp}{seedStr}

\begin{closurestatus}{\BolzanoDedekindLocatedClusterUp}
  \constructivestory{A $\BolzanoDedekindLocatedClusterUp$ packet is generated from a bounded real sequence row, nested and located interval rows, StreamName windows, RegSeqRat readback, a terminal RealUp cluster seal, componentwise transport, displayed continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bolzano-dedekind-located-cluster-carrier}, \autoref{thm:bolzano-dedekind-located-cluster-namecert-obligations}, and \autoref{thm:bolzano-dedekind-located-cluster-real-handoff} seed the finite located-cluster certificate for bounded real sequence windows through nested and located interval rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim general sequential compactness, choice-selected subsequences, open-cover compactness, quotient real equality, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-source exposure, nested-window retention, located interval comparison, stream scheduling, regular rational readback, real-seal handoff, transport, replay, provenance, and local naming for the same packet.}
\end{closurestatus}
