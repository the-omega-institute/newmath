\chapter{A Concrete Naming Certificate for $\HomologyUp$}
\label{ch:concrete-instances-homology-namecert}

The homology interface $\HomologyUp$ packages a chain complex over an abelian-group carrier together with the kernel/image quotients computing the homology groups. It depends on $\NameCert_{\AbGroupUp}$ (\autoref{ch:concrete-instances-abgroup-namecert}) and $\NameCert_{\QuotientGroupUp}$ (\autoref{ch:concrete-instances-quotientgroup-namecert}), and is the constructive substrate for both singular and simplicial homology.

\section{Carrier definition}
\label{sec:homology-carrier}

The cycle carrier of a BEDC chain differential $d:\mathsf{BHist}\to
\mathsf{BHist}$ is the kernel predicate
$$
Z_d(h) :\Longleftrightarrow \hsame(d(h),\mathsf{Empty}).
$$
The boundary carrier is the image predicate
$$
B_d(h) :\Longleftrightarrow \exists u:\mathsf{BHist},\ \hsame(h,d(u)).
$$
\leandef{BEDC.Derived.HomologyUp.HomologyCycleCarrier}
\leandef{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier}

\begin{theorem}[Homology boundary carrier nonempty preimage]
\label{thm:homology-boundary-carrier-nonempty-preimage}
If $B_d(h)$ and $h$ is not history-same to $\mathsf{Empty}$, then some
boundary preimage $u$ has a nonempty differential endpoint:
$$
\exists u:\mathsf{BHist},\
\hsame(h,d(u))\land \neg\hsame(d(u),\mathsf{Empty}).
$$
\end{theorem}
\begin{proof}
Choose the boundary witness $u$ with $\hsame(h,d(u))$. If $d(u)$ were
history-same to $\mathsf{Empty}$, transitivity of $\hsame$ would make $h$
history-same to $\mathsf{Empty}$, contradicting the nonempty hypothesis.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_nonempty\_preimage}

\begin{theorem}[Homology boundary carrier empty preimage]
\label{thm:homology-boundary-carrier-empty-preimage}
If $B_d(h)$ and $h\hsame\mathsf{Empty}$, then some boundary preimage $u$
has an empty differential endpoint:
$$
\exists u:\mathsf{BHist},\
\hsame(h,d(u))\land \hsame(d(u),\mathsf{Empty}).
$$
\end{theorem}
\begin{proof}
Choose the boundary witness $u$ with $\hsame(h,d(u))$. Symmetry of that
witness followed by the displayed empty endpoint comparison gives
$\hsame(d(u),\mathsf{Empty})$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_empty\_preimage}

\begin{theorem}[Homology boundary carrier cycle closure]
\label{thm:homology-boundary-carrier-cycle-closed}
Assume the BEDC chain differential is square-zero at every history:
$$
\forall u:\mathsf{BHist},\ \hsame(d(d(u)),\mathsf{Empty}).
$$
Every boundary history is a cycle history: if $B_d(h)$, then $Z_d(h)$.
\end{theorem}
\begin{proof}
Choose a boundary witness $u$ with $\hsame(h,d(u))$. Transporting the cycle
test along this witness reduces the claim to the displayed square-zero law at
the chosen boundary preimage, which is supplied by feeding that preimage to the
displayed universal square-zero law.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cycle\_closed}

\begin{theorem}[Homology boundary classifier refines cycle classifier]
\label{thm:homology-boundary-classifier-refines-cycle-classifier}
Assume the BEDC chain differential is square-zero at every history:
$$
\forall u:\mathsf{BHist},\ \hsame(d(d(u)),\mathsf{Empty}).
$$
If $B_d(h)$, $B_d(k)$, and $\hsame(h,k)$, then $Z_d(h)$, $Z_d(k)$, and
$\hsame(h,k)$.
\end{theorem}
\begin{proof}
Apply \autoref{thm:homology-boundary-carrier-cycle-closed} to the boundary
witness at $h$, using the displayed square-zero law at the chosen preimage.
This produces the cycle endpoint $Z_d(h)$.

Apply the same carrier-closure theorem to the boundary witness at $k$. The
existing classifier row $\hsame(h,k)$ is preserved unchanged, so the three
pieces repack as the cycle classifier row between the same endpoints.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cycle\_classifier\_refinement}

\begin{theorem}[Homology cycle carrier append closure]
\label{thm:homology-cycle-carrier-append-closed}
Assume the differential respects BEDC continuation append:
$$
\hsame(d(\mathsf{append}(u,v)),\mathsf{append}(d(u),d(v))).
$$
If $Z_d(h)$ and $Z_d(k)$, then
$Z_d(\mathsf{append}(h,k))$.
\end{theorem}
\begin{proof}
The append law transports the displayed differential to
$\mathsf{append}(d(h),d(k))$. The two cycle hypotheses identify both
endpoints with $\mathsf{Empty}$, and the BEDC append kernel has
$\mathsf{append}(\mathsf{Empty},\mathsf{Empty})=\mathsf{Empty}$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleCarrier\_append\_closed}

\begin{theorem}[Homology cycle carrier append visible differential absurd]
\label{thm:homology-cycle-carrier-append-visible-differential-absurd}
Assume the differential respects BEDC continuation append. If $Z_d(h)$ and
$Z_d(k)$, then the differential of the appended cycle cannot be history-same
to a visible zero-headed or one-headed endpoint:
$$
\neg\hsame(d(\mathsf{append}(h,k)),\mathsf{e0}(tail))
\qquad\text{and}\qquad
\neg\hsame(d(\mathsf{append}(h,k)),\mathsf{e1}(tail)).
$$
\end{theorem}
\begin{proof}
The homology cycle append closure theorem gives
$\hsame(d(\mathsf{append}(h,k)),\mathsf{Empty})$. A displayed zero-headed or
one-headed comparison transports this empty endpoint to the visible
constructor endpoint, contradicting BEDC constructor separation from
$\mathsf{Empty}$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleCarrier\_append\_visible\_differential\_absurd}

\begin{theorem}[Homology cycle carrier append components]
\label{thm:homology-cycle-carrier-append-components}
Assume the differential respects BEDC continuation append:
$$
\hsame(d(\mathsf{append}(u,v)),\mathsf{append}(d(u),d(v))).
$$
Then an appended history is a cycle exactly when both appended components are
cycles:
$$
Z_d(\mathsf{append}(h,k))
\quad\Longleftrightarrow\quad
Z_d(h)\wedge Z_d(k).
$$
\end{theorem}
\begin{proof}
The forward direction transports $d(\mathsf{append}(h,k))$ to
$\mathsf{append}(d(h),d(k))$. If this endpoint is empty, BEDC append-empty
inversion gives $d(h)\hsame\mathsf{Empty}$ and
$d(k)\hsame\mathsf{Empty}$. The reverse direction is the homology cycle
append-closure theorem.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleCarrier\_append\_components\_iff}

\begin{theorem}[Homology cycle carrier append differential nonempty component split]
\label{thm:homology-cycle-carrier-append-differential-nonempty-component-split}
Assume the differential respects BEDC continuation append. If the differential
of an appended history is not empty,
$$
\hsame(d(\mathsf{append}(h,k)),\mathsf{Empty})\to\bot,
$$
then at least one component is not a cycle:
$$
(Z_d(h)\to\bot)\vee(Z_d(k)\to\bot).
$$
\end{theorem}
\begin{proof}
Transport the displayed nonemptiness along the append law for $d$ to obtain
nonemptiness of $\mathsf{append}(d(h),d(k))$. BEDC append nonemptiness then
separates this obstruction into a nonempty $d(h)$ component or a nonempty
$d(k)$ component. Since $Z_d(h)$ and $Z_d(k)$ mean exactly that the
corresponding differential is empty, the selected obstruction negates the
matching cycle predicate.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleCarrier\_append\_differential\_nonempty\_component\_split}

\begin{theorem}[Homology boundary carrier append closure]
\label{thm:homology-boundary-carrier-append-closed}
Assume the differential respects BEDC continuation append:
$$
\hsame(d(\mathsf{append}(u,v)),\mathsf{append}(d(u),d(v))).
$$
If $B_d(h)$ and $B_d(k)$, then
$B_d(\mathsf{append}(h,k))$.
\end{theorem}
\begin{proof}
Choose boundary witnesses $u$ and $v$ with $\hsame(h,d(u))$ and
$\hsame(k,d(v))$. The BEDC witness for the appended boundary is
$\mathsf{append}(u,v)$. Endpoint congruence carries
$\mathsf{append}(h,k)$ to $\mathsf{append}(d(u),d(v))$, and the differential
append law identifies that history with $d(\mathsf{append}(u,v))$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_append\_closed}

\begin{theorem}[Homology boundary carrier append hsame transport]
\label{thm:homology-boundary-carrier-append-hsame-transport}
Assume the differential respects BEDC continuation append. If $B_d(h)$ and
$B_d(k)$ and $\hsame(\mathsf{append}(h,k),r)$, then $B_d(r)$.
\end{theorem}
\begin{proof}
Choose boundary witnesses $u$ and $v$ for $h$ and $k$. Their appended BEDC
preimage $\mathsf{append}(u,v)$ maps, by the differential append law, to the
same endpoint as $\mathsf{append}(d(u),d(v))$. The two boundary endpoint
comparisons identify $\mathsf{append}(h,k)$ with this value, and the final
$\hsame$ comparison transports the image witness to $r$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_append\_hsame\_transport}

\begin{theorem}[Homology boundary carrier continuation cycle closure]
\label{thm:homology-boundary-cont-cycle-closed}
Assume the differential respects BEDC continuation append and is square-zero.
If $B_d(h)$, $B_d(k)$, and $\ContR(h,k,r)$, then $Z_d(r)$.
\end{theorem}
\begin{proof}
Boundary append closure with endpoint transport first makes the continuation
result $r$ a boundary carrier. The square-zero differential condition turns any
boundary carrier into a cycle carrier, so $Z_d(r)$ follows.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_cycle\_closed}

\begin{theorem}[Homology boundary carrier continuation preimage]
\label{thm:homology-boundary-carrier-cont-preimage}
Assume the differential respects BEDC continuation append. If $B_d(h)$,
$B_d(k)$, and $\Cont(h,k,r)$, then the continuation result has compatible
boundary preimages:
$$
\exists u,v,uv:\mathsf{BHist},\
\Cont(u,v,uv)\wedge \hsame(r,d(uv))\wedge
\hsame(h,d(u))\wedge \hsame(k,d(v)).
$$
\end{theorem}
\begin{proof}
Choose boundary witnesses $u$ and $v$ for $h$ and $k$. Set
$uv=\mathsf{append}(u,v)$, giving $\Cont(u,v,uv)$ by the BEDC continuation
constructor. The input continuation reads $r$ as
$\mathsf{append}(h,k)$; the two boundary witnesses identify this with
$\mathsf{append}(d(u),d(v))$, and the differential append law identifies the
endpoint with $d(uv)$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_preimage}

\begin{theorem}[Homology boundary carrier continuation preimage append]
\label{thm:homology-boundary-carrier-cont-preimage-append}
Assume the differential respects BEDC continuation append. If $B_d(h)$,
$B_d(k)$, and $\ContR(h,k,r)$, then there are BEDC preimages $u$ and $v$
such that
$$
\hsame(h,d(u))\land
\hsame(k,d(v))\land
\hsame(r,d(\mathsf{append}(u,v))).
$$
\end{theorem}
\begin{proof}
Choose boundary witnesses $u$ and $v$ for $h$ and $k$. The continuation ledger
identifies $r$ with $\mathsf{append}(h,k)$. The witness endpoints identify
that appended history with $\mathsf{append}(d(u),d(v))$, and the differential
append law identifies this with $d(\mathsf{append}(u,v))$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_preimage\_append}

\begin{theorem}[Homology boundary carrier continuation preimage append $\hsame$ transport]
\label{thm:homology-boundary-carrier-cont-preimage-append-hsame-transport}
Assume the differential respects BEDC continuation append. If $B_d(h)$,
$B_d(k)$, $\ContR(h,k,r)$, and $r\hsame r'$, then the same BEDC preimages
$u$ and $v$ witness the transported endpoint:
$$
\hsame(h,d(u))\land
\hsame(k,d(v))\land
\hsame(r',d(\mathsf{append}(u,v))).
$$
\end{theorem}
\begin{proof}
The continuation preimage-append theorem supplies witnesses $u$ and $v$ for
the endpoint $r$. Composing that endpoint comparison with the symmetry of
$r\hsame r'$ transports the displayed differential image to $r'$ while keeping
the two boundary witness comparisons unchanged.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_preimage\_append\_hsame\_transport}

\begin{theorem}[Homology boundary continuation cycle-preimage package]
\label{thm:homology-boundary-continuation-cycle-preimage-package}
Assume the differential respects BEDC continuation append and is square-zero.
If $B_d(h)$, $B_d(k)$, and $\ContR(h,k,r)$, then
$$
Z_d(r)\land \exists u,v:\mathsf{BHist},\
\hsame(h,d(u))\land \hsame(k,d(v))\land
\hsame(r,d(\mathsf{append}(u,v))).
$$
\end{theorem}
\begin{proof}
The cycle component is exactly
\autoref{thm:homology-boundary-cont-cycle-closed}: boundary continuation first
produces a boundary carrier for $r$, and square-zero turns that carrier into
$Z_d(r)$.

The preimage component is supplied by
\autoref{thm:homology-boundary-carrier-cont-preimage-append}. It gives BEDC
histories $u$ and $v$ whose differential images match $h$ and $k$, and whose
appended differential image matches the continuation result $r$.

Conjoining these two conclusions gives the displayed cycle-preimage certificate
for the same continuation result.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_continuation\_cycle\_preimage\_package}

\begin{theorem}[Homology boundary carrier continuation nonempty preimage append]
\label{thm:homology-boundary-carrier-cont-nonempty-preimage-append}
Assume the differential respects BEDC continuation append. If $B_d(h)$,
$B_d(k)$, $\ContR(h,k,r)$, and $r$ is not history-same to $\mathsf{Empty}$,
then there are BEDC preimages $u$ and $v$ such that
$$
\hsame(h,d(u))\land
\hsame(k,d(v))\land
\neg\hsame(d(\mathsf{append}(u,v)),\mathsf{Empty}).
$$
\end{theorem}
\begin{proof}
The preimage-append theorem gives witnesses $u$ and $v$ with
$\hsame(r,d(\mathsf{append}(u,v)))$. If the displayed differential endpoint
were history-same to $\mathsf{Empty}$, transitivity would make $r$ history-same
to $\mathsf{Empty}$, contradicting the nonempty hypothesis.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_nonempty\_preimage\_append}

\begin{theorem}[Homology boundary carrier append empty preimages]
\label{thm:homology-boundary-carrier-append-empty-preimages}
If $B_d(h)$ and $B_d(k)$ and
$\hsame(\mathsf{append}(h,k),\mathsf{Empty})$, then the two boundary
witnesses can be chosen with empty differential endpoints:
$$
\exists u,v:\mathsf{BHist},\
\hsame(h,d(u))\land \hsame(k,d(v))\land
\hsame(d(u),\mathsf{Empty})\land\hsame(d(v),\mathsf{Empty}).
$$
\end{theorem}
\begin{proof}
BEDC append-empty inversion splits the appended endpoint comparison into
$h\hsame\mathsf{Empty}$ and $k\hsame\mathsf{Empty}$. Applying empty-preimage
inversion to the two boundary carrier hypotheses produces witnesses $u$ and
$v$ with the stated endpoint comparisons, which are then paired.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_append\_empty\_preimages}

\begin{theorem}[Homology boundary carrier continuation empty preimages]
\label{thm:homology-boundary-carrier-cont-empty-preimages}
If $B_d(h)$ and $B_d(k)$ and $\ContR(h,k,r)$ and
$r\hsame\mathsf{Empty}$, then the two boundary witnesses can be chosen with
empty differential endpoints:
$$
\exists u,v:\mathsf{BHist},\
\hsame(h,d(u))\land \hsame(k,d(v))\land
\hsame(d(u),\mathsf{Empty})\land\hsame(d(v),\mathsf{Empty}).
$$
\end{theorem}
\begin{proof}
The continuation equation identifies $r$ with $\mathsf{append}(h,k)$.
Transporting the empty result comparison along this equation gives
$\mathsf{append}(h,k)\hsame\mathsf{Empty}$, and the append empty preimage
theorem supplies the paired witnesses.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_empty\_preimages}

\begin{theorem}[Homology boundary carrier continuation empty-result preimage equivalence]
\label{thm:homology-boundary-carrier-cont-empty-result-preimages-iff}
If $B_d(h)$ and $B_d(k)$ and $\ContR(h,k,r)$, then $r$ is history-same to
$\mathsf{Empty}$ exactly when the two boundary witnesses can be chosen with
empty differential endpoints:
$$
\hsame(r,\mathsf{Empty})
\Longleftrightarrow
\exists u,v:\mathsf{BHist},\
\hsame(h,d(u))\land \hsame(k,d(v))\land
\hsame(d(u),\mathsf{Empty})\land\hsame(d(v),\mathsf{Empty}).
$$
\end{theorem}
\begin{proof}
The forward direction is the continuation empty-preimage theorem. Conversely,
the two displayed empty differential endpoints transport through the boundary
witnesses to give $h\hsame\mathsf{Empty}$ and $k\hsame\mathsf{Empty}$; append
empty-result exactness makes $\mathsf{append}(h,k)$ empty, and the continuation
ledger identifies this append endpoint with $r$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_empty\_result\_preimages\_iff}

\begin{theorem}[Homology boundary carrier append nonempty preimage]
\label{thm:homology-boundary-carrier-append-nonempty-preimage}
If $B_d(h)$ and $B_d(k)$ and
$\mathsf{append}(h,k)$ is not history-same to $\mathsf{Empty}$, then at least
one side has a boundary preimage with a nonempty differential endpoint:
$$
\begin{aligned}
&\left(\exists u:\mathsf{BHist},\
\hsame(h,d(u))\land\neg\hsame(d(u),\mathsf{Empty})\right)\\
&\quad{}\lor
\left(\exists v:\mathsf{BHist},\
\hsame(k,d(v))\land\neg\hsame(d(v),\mathsf{Empty})\right).
\end{aligned}
$$
\end{theorem}
\begin{proof}
BEDC append nonemptiness splits into a nonempty left component or a nonempty
right component. In the left case, boundary nonempty-preimage inversion
applied to $B_d(h)$ gives the left disjunct. In the right case, the same
argument applied to $B_d(k)$ gives the right disjunct.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_append\_nonempty\_preimage}

\begin{theorem}[Homology boundary carrier continuation nonempty side preimage]
\label{thm:homology-boundary-carrier-cont-nonempty-side-preimage}
If $B_d(h)$ and $B_d(k)$ and $\ContR(h,k,r)$ and $r$ is not
history-same to $\mathsf{Empty}$, then at least one side has a boundary
preimage with a nonempty differential endpoint:
$$
\begin{aligned}
&\left(\exists u:\mathsf{BHist},\
\hsame(h,d(u))\land\neg\hsame(d(u),\mathsf{Empty})\right)\\
&\quad{}\lor
\left(\exists v:\mathsf{BHist},\
\hsame(k,d(v))\land\neg\hsame(d(v),\mathsf{Empty})\right).
\end{aligned}
$$
\end{theorem}
\begin{proof}
The continuation equation transports nonemptiness of $r$ to nonemptiness of
$\mathsf{append}(h,k)$. The append nonempty preimage theorem then separates
the witness to the left boundary preimage or to the right boundary preimage.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_nonempty\_side\_preimage}

\begin{theorem}[Homology boundary carrier append nonempty cycle preimage]
\label{thm:homology-boundary-carrier-append-nonempty-cycle-preimage}
Assume the differential is square-zero. If $B_d(h)$ and $B_d(k)$ and
$\mathsf{append}(h,k)$ is not history-same to $\mathsf{Empty}$, then at least
one side has a boundary preimage with a nonempty differential endpoint, and
that side is a cycle:
$$
\begin{aligned}
&\left(\exists u:\mathsf{BHist},\
\hsame(h,d(u))\land Z_d(h)\land\neg\hsame(d(u),\mathsf{Empty})\right)\\
&\quad{}\lor
\left(\exists v:\mathsf{BHist},\
\hsame(k,d(v))\land Z_d(k)\land\neg\hsame(d(v),\mathsf{Empty})\right).
\end{aligned}
$$
\end{theorem}
\begin{proof}
The append nonempty preimage theorem splits the nonempty append boundary into a
left or right boundary preimage. The square-zero differential turns each
boundary carrier hypothesis into the corresponding cycle carrier, and that
cycle witness is attached to whichever disjunct supplies the nonempty
preimage.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_append\_nonempty\_cycle\_preimage}

\begin{theorem}[Homology boundary carrier continuation nonempty cycle preimage]
\label{thm:homology-boundary-carrier-cont-nonempty-cycle-preimage}
Assume the differential is square-zero. If $B_d(h)$ and $B_d(k)$, if
$\ContR(h,k,r)$, and if $r$ is not history-same to $\mathsf{Empty}$, then at
least one side has a boundary preimage with a nonempty differential endpoint,
and that side is a cycle:
$$
\begin{aligned}
&\left(\exists u:\mathsf{BHist},\
\hsame(h,d(u))\land Z_d(h)\land\neg\hsame(d(u),\mathsf{Empty})\right)\\
&\quad{}\lor
\left(\exists v:\mathsf{BHist},\
\hsame(k,d(v))\land Z_d(k)\land\neg\hsame(d(v),\mathsf{Empty})\right).
\end{aligned}
$$
\end{theorem}
\begin{proof}
The continuation equation transports nonemptiness of $r$ to nonemptiness of
$\mathsf{append}(h,k)$. The append nonempty cycle-preimage theorem then
separates the witness to the left boundary preimage or to the right boundary
preimage, carrying the corresponding cycle evidence supplied by square-zero.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_nonempty\_cycle\_preimage}

\begin{theorem}[Homology boundary carrier continuation cycle components]
\label{thm:homology-boundary-cont-cycle-components}
Assume the differential respects BEDC continuation append and is square-zero.
If $B_d(r)$ and $\ContR(h,k,r)$, then both continuation components are cycles:
$$
Z_d(h)\wedge Z_d(k).
$$
\end{theorem}
\begin{proof}
The square-zero law turns the boundary carrier on $r$ into a cycle carrier on
the continuation result. Since $\ContR(h,k,r)$ identifies $r$ with
$\mathsf{append}(h,k)$, the append-components theorem splits that cycle
predicate into the two component cycle predicates.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cont\_cycle\_components}

\begin{theorem}[Homology boundary carrier cycle from square-zero differential]
\label{thm:homology-boundary-carrier-cycle-of-d-squared-zero}
Assume the differential respects $\hsame$ and satisfies the square-zero
condition
$$
\hsame(d(d(u)),\mathsf{Empty})
$$
for every $u:\mathsf{BHist}$. If $B_d(h)$, then $Z_d(h)$.
\end{theorem}
\begin{proof}
Choose a boundary witness $u$ with $\hsame(h,d(u))$. Congruence for the
differential transports this witness to
$\hsame(d(h),d(d(u)))$. The square-zero row identifies $d(d(u))$ with
$\mathsf{Empty}$, and transitivity of $\hsame$ gives the cycle predicate.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_cycle\_of\_d\_squared\_zero}

\begin{theorem}[Homology cycle carrier append hsame transport]
\label{thm:homology-cycle-carrier-append-hsame-transport}
Assume the differential respects BEDC continuation append and is congruent
for $\hsame$. If $Z_d(h)$, $Z_d(k)$, and
$\hsame(\mathsf{append}(h,k),r)$, then $Z_d(r)$.
\end{theorem}
\begin{proof}
The append-closure theorem gives
$Z_d(\mathsf{append}(h,k))$. Congruence for the differential transports the
endpoint comparison to an $\hsame$ comparison between
$d(\mathsf{append}(h,k))$ and $d(r)$. Symmetry of that comparison, followed
by the cycle row for the append, proves that $d(r)$ is $\hsame$ to
$\mathsf{Empty}$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleCarrier\_append\_hsame\_transport}

\begin{theorem}[Homology cycle-boundary append cycle closure]
\label{thm:homology-cycle-boundary-append-cycle-closed}
Assume the differential respects BEDC continuation append and is square-zero.
If $Z_d(h)$ and $B_d(k)$, then $Z_d(\mathsf{append}(h,k))$.
\end{theorem}
\begin{proof}
The square-zero law turns the boundary hypothesis on $k$ into a cycle
hypothesis. The ordinary cycle append closure then applies to the two cycle
histories $h$ and $k$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleBoundary\_append\_cycle\_closed}

\begin{theorem}[Homology boundary-cycle append cycle closure]
\label{thm:homology-boundary-cycle-append-cycle-closed}
Assume the differential respects BEDC continuation append and is square-zero.
If $B_d(h)$ and $Z_d(k)$, then $Z_d(\mathsf{append}(h,k))$.
\end{theorem}
\begin{proof}
The square-zero law turns the boundary hypothesis on $h$ into a cycle
hypothesis. The ordinary cycle append closure then applies to the two cycle
histories $h$ and $k$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCycle\_append\_cycle\_closed}

\begin{theorem}[Homology boundary-cycle append hsame transport]
\label{thm:homology-boundary-cycle-append-hsame-transport}
Assume the differential respects BEDC continuation append, is square-zero,
and respects $\hsame$. If $B_d(h)$, $Z_d(k)$, and
$\hsame(\mathsf{append}(h,k),r)$, then $Z_d(r)$.
\end{theorem}
\begin{proof}
The boundary-cycle append closure theorem gives a cycle at
$\mathsf{append}(h,k)$. Differential congruence transports the final endpoint
comparison to $d(r)$, and symmetry followed by transitivity gives
$\hsame(d(r),\mathsf{Empty})$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCycle\_append\_hsame\_transport}

\begin{theorem}[Homology cycle-boundary append hsame transport]
\label{thm:homology-cycle-boundary-append-hsame-transport}
Assume the differential respects BEDC continuation append, is square-zero, and
respects $\hsame$. If $Z_d(h)$, $B_d(k)$, and
$\hsame(\mathsf{append}(h,k),r)$, then $Z_d(r)$.
\end{theorem}
\begin{proof}
The square-zero row turns the boundary hypothesis on $k$ into a cycle
hypothesis. Cycle-boundary append closure therefore gives a cycle at
$\mathsf{append}(h,k)$. Differential congruence transports the final endpoint
comparison to $d(r)$, and symmetry followed by transitivity gives
$\hsame(d(r),\mathsf{Empty})$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleBoundary\_append\_hsame\_transport}

\begin{theorem}[Homology cycle-boundary append cycle hsame transport]
\label{thm:homology-cycle-boundary-append-cycle-hsame-transport}
Assume the differential respects BEDC continuation append, is congruent for
$\hsame$, and is square-zero. If $Z_d(h)$, $B_d(k)$, and
$\hsame(\mathsf{append}(h,k),r)$, then $Z_d(r)$.
\end{theorem}
\begin{proof}
The square-zero and congruence laws turn the boundary hypothesis on $k$ into
a cycle hypothesis. The append endpoint transport theorem then applies to
the cycle histories $h$ and $k$ and transports the resulting cycle predicate
across the displayed endpoint comparison.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleBoundary\_append\_cycle\_hsame\_transport}

\section{The certificate}
\label{sec:homology-certificate}

\begin{theorem}[Homology boundary carrier semantic name certificate]
\label{thm:homology-boundary-carrier-semantic-namecert}
For any BEDC differential $d:\mathsf{BHist}\to\mathsf{BHist}$, the boundary
carrier $B_d$ forms a semantic naming certificate when source, pattern, and
ledger policy are all $B_d$, and the classifier requires both endpoints to be
boundaries together with $\hsame$ between them.
\end{theorem}
\begin{proof}
The carrier is inhabited by $d(\mathsf{Empty})$, using $\mathsf{Empty}$ as
its preimage and reflexivity of $\hsame$. The classifier equivalence laws
come from the stored endpoint boundary witnesses and the reflexive, symmetric,
and transitive laws for $\hsame$. Carrier transport along a classifier edge is
the right endpoint boundary witness, and the semantic pattern and ledger
fields are the source witness itself.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyBoundaryCarrier\_semanticNameCert}

\begin{theorem}[Homology cycle carrier empty semantic name certificate]
\label{thm:homology-cycle-carrier-empty-semantic-namecert}
If the empty history is a $d$-cycle, then the cycle carrier $Z_d$ forms a
semantic naming certificate when source, pattern, and ledger policy are all
$Z_d$, and the classifier requires both endpoints to be cycles together with
$\hsame$ between them.
\end{theorem}
\begin{proof}
The empty-cycle hypothesis inhabits the carrier at $\mathsf{Empty}$. The
classifier equivalence laws again follow by unpacking the two endpoint cycle
witnesses and composing the underlying $\hsame$ rows. Carrier transport is
read from the right endpoint cycle witness already stored in the classifier,
and the semantic pattern and ledger fields are identities on the source
witness.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleCarrier\_empty\_semanticNameCert}

\begin{theorem}[Homology singleton cycle semantic name certificate]
\label{thm:homology-singleton-cycle-semantic-namecert}
Let the singleton cycle carrier contain exactly the BEDC histories
$h\hsame\mathsf{BHist.Empty}$. Let its classifier require both endpoints to
lie in that carrier and require $h\hsame k$. This classifier is a semantic
naming certificate for the carrier used simultaneously as source, pattern,
and ledger policy.
\end{theorem}
\begin{proof}
The carrier is inhabited by $\mathsf{BHist.Empty}$ through reflexivity of
$\hsame$. Classifier reflexivity, symmetry, and transitivity are obtained by
unpacking the two endpoint-carrier witnesses and composing the underlying
$\hsame$ proofs. Carrier transport along a classifier edge is the right
endpoint carrier witness already stored in the classifier. The semantic
pattern and ledger fields are the source witness itself.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologySingletonCycle\_semanticNameCert}

\begin{theorem}[Homology singleton cycle standard bridge boundary]
\label{thm:homology-singleton-cycle-standard-bridge-boundary}
The singleton-cycle semantic certificate of \autoref{thm:homology-singleton-cycle-semantic-namecert} supplies the paper-side standard bridge boundary for the exported $\HomologyUp$ cycle interface. The BEDC source is $\mathsf{BHist}$ with $\hsame$, the carrier is the singleton cycle carrier, the classifier is endpoint carriage together with $\hsame$, the ledger is the source witness, and the standard readback is the one-cycle trivial homology object.
\end{theorem}
\begin{proof}
Project the semantic certificate of \autoref{thm:homology-singleton-cycle-semantic-namecert}. Its source, pattern, and ledger policy are all the singleton cycle carrier, and its classifier stores the two endpoint-carrier witnesses together with the $\hsame$ comparison.

Read the source as $\mathsf{BHist}$ equipped with $\hsame$. The carrier is inhabited by $\mathsf{BHist.Empty}$, so the standard readback is the one-cycle object whose only accepted cycle endpoint is the singleton empty-history class.

The ledger field is the source witness itself, and transport along the classifier uses the right endpoint-carrier witness. Repacking these rows gives the standard bridge boundary for the exported singleton-cycle $\HomologyUp$ interface.
\end{proof}

\closureat{HomologyUp}{matureStr}[BEDC.Derived.HomologyUp.HomologySingletonCycle\_semanticNameCert]

\begin{closurestatus}{\HomologyUp}
  \theoryclosure{\matureClosure}
  \scopeclosed{The public export is the singleton cycle semantic certificate together with the displayed cycle and boundary carrier readback and classifier transport rows.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.HomologyUp.HomologySingletonCycle\_semanticNameCert}
  \bridgestatus{paperBridge}
  \notclaimed{General homology theory: arbitrary chain complexes, boundary/image quotients, chain maps, exact sequences, long exact sequences, singular or simplicial homology, and derived functors.}
  \upgradepath{No further theory-axis upgrade remains; the remaining work is machine-checked bridge verification.}
\end{closurestatus}

\begin{theorem}[Homology cycle carrier $\hsame$ transport]
\label{thm:homology-cycle-carrier-hsame-transport}
Assume the differential is stable under history-sameness:
$$
\forall u,v:\mathsf{BHist},\ \hsame(u,v)\to\hsame(d(u),d(v)).
$$
If $Z_d(h)$ and $\hsame(h,k)$, then $Z_d(k)$.
\end{theorem}
\begin{proof}
By \autoref{sec:homology-carrier}, $Z_d(h)$ is the endpoint comparison
$\hsame(d(h),\mathsf{Empty})$. Applying the displayed stability row to
$\hsame(h,k)$ gives $\hsame(d(h),d(k))$. Symmetry of $\hsame$ gives
$\hsame(d(k),d(h))$, and transitivity with the cycle endpoint gives
$\hsame(d(k),\mathsf{Empty})$. By the same carrier definition, this is
$Z_d(k)$.
\end{proof}
\leanchecked{BEDC.Derived.HomologyUp.HomologyCycleCarrier\_hsame\_transport}
