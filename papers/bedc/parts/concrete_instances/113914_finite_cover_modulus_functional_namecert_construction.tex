\chapter{A Concrete Naming Certificate for $\FiniteCoverModulusFunctionalUp$}
\label{ch:concrete-instances-finite-cover-modulus-functional-namecert}
\origin{human}

Following the constructive Heine--Cantor proof through finite covers, the $\FiniteCoverModulusFunctionalUp$ packet records the finite compact-cover modulus functional that turns displayed local continuity radii into a tolerance-only uniform-modulus handoff. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is finite ProbeBundle and rational-radius data; it does not select a Lebesgue number outside the carrier, assert open-cover compactness, use quotient map equality, invoke countable choice, or import ambient $\RealUp$ completeness.

\begin{definition}[Finite cover modulus functional carrier]
\label{def:finite-cover-modulus-functional-carrier}
A $\FiniteCoverModulusFunctionalUp$ carrier is a finite $\BHist$ packet
$$
F=(K,G,B,L,D,U,W,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-cover source row, exposing only the displayed ProbeBundle cover data.
\item $G$ is the $\ContinuousMapUp$ graph and pointwise-modulus row for the map being read.
\item $B$ is the finite cover cell row carrying centers, local radii, and membership witnesses.
\item $L$ is the finite lower-bound fold row over the displayed radius candidates.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and comparison row for radius and oscillation checks.
\item $U$ is the $\UniformModulusUp$ handoff row produced from the finite fold.
\item $W$ is the finite StreamName window row for source and target observations.
\item $R$ is the RegSeqRat readback row for the same windows.
\item $S$ is the terminal $\RealUp$ seal row reached only after $K,G,B,L,D,U,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact-cover, continuous-map, radius, fold, tolerance, uniform-modulus, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-cover request through local modulus rows, dyadic radius comparison, finite lower-bound folding, uniform-modulus handoff, and terminal Real sealing.
\item $P$ is the $\Pkg$ provenance over $\FiniteCoverModulusFunctionalUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteCoverModulusFunctionalUp}$ row.
\end{enumerate}
Accepted packets are compared only through the displayed compact-cover, continuous-map, radius, fold, tolerance, uniform-modulus, readback, seal, transport, replay, provenance, and naming rows.
\end{definition}

\begin{theorem}[Finite cover modulus functional NameCert obligations]
\label{thm:finite-cover-modulus-functional-namecert-obligations}
For every accepted $\FiniteCoverModulusFunctionalUp$ carrier $F=(K,G,B,L,D,U,W,R,S,H,C,P,N)$, the local $\NameCert_{\FiniteCoverModulusFunctionalUp}$ surface consists exactly of CompactMetric finite-cover exposure, ContinuousMap pointwise-modulus exposure, finite cover-cell radius exposure, finite lower-bound folding, DyadicRatCore tolerance accounting, UniformModulus handoff, finite observation windows, RegSeqRat readback, Real sealing, componentwise transport, displayed replay, provenance, and local naming. Every compact-uniform-continuity consumer must read $K,G,B,L,D,U$ before it consumes $W,R,S,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-cover-modulus-functional-carrier}. Rows $K,G,B$ expose the compact source, continuous graph, and finite cover cells, while $L,D,U$ expose the lower-bound fold, dyadic comparison ledger, and uniform-modulus handoff.

The finite readback stays downstream of the cover route. StreamName windows and RegSeqRat readback in $W,R$ only observe the displayed compact-cover and continuous-map data, and the terminal Real seal $S$ is reached after the dyadic tolerance row and uniform-modulus row are already visible.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no row carries an external Lebesgue-number selector, open-cover compactness theorem, quotient map equality, countable-choice schedule, or ambient Real-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteCoverModulusFunctionalUp$ reader that claims a uniform-modulus handoff while omitting CompactMetric finite-cover exposure, ContinuousMap pointwise-modulus exposure, finite cover-cell radii, finite lower-bound folding, DyadicRatCore tolerance accounting, StreamName windows, RegSeqRat readback, Real sealing, hsame transport, Cont replay, Pkg provenance, or local naming refutes the packet.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, dyadicratcore, real: $\FiniteCoverModulusFunctionalUp$ is not bijective with any listed sibling because it joins compact finite covers, pointwise continuous-map moduli, radius lower-bound folding, dyadic tolerance accounting, uniform-modulus handoff, finite readback, Real sealing, transport, replay, provenance, and local naming as one packet.}

\closureat{FiniteCoverModulusFunctionalUp}{seedStr}

\begin{closurestatus}{\FiniteCoverModulusFunctionalUp}
  \constructivestory{A $\FiniteCoverModulusFunctionalUp$ packet is generated from CompactMetric finite-cover rows, ContinuousMap pointwise-modulus rows, finite cover-cell radius rows, lower-bound fold rows, DyadicRatCore tolerance rows, UniformModulus handoff rows, finite observation windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-cover-modulus-functional-carrier} and \autoref{thm:finite-cover-modulus-functional-namecert-obligations} seed the compact finite-cover modulus-functional route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, external Lebesgue-number selection, open-cover compactness, quotient map equality, countable choice, ambient $\RealUp$ completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-cover exposure, continuous-map pointwise-modulus exposure, cover-cell radius exposure, finite lower-bound folding, dyadic tolerance accounting, uniform-modulus handoff, finite readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteCoverModulusFunctionalUp.}
\end{closurestatus}
