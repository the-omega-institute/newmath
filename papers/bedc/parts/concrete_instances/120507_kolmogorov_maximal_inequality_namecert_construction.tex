\chapter{A Concrete Naming Certificate for $\KolmogorovMaximalInequalityUp$}
\label{ch:concrete-instances-kolmogorov-maximal-inequality-namecert}
\origin{human}

Following Kolmogorov and Bishop--Bridges finite probability estimates, the $\KolmogorovMaximalInequalityUp$ packet records the finite partial-sum maximal-window route behind Kolmogorov's maximal inequality. It sits after the probability source of \autoref{ch:concrete-instances-probspace-namecert}, the measurable random-variable rows of \autoref{ch:concrete-instances-randomvar-namecert}, the one-row Chebyshev tail carrier of \autoref{ch:concrete-instances-chebyshev-inequality-namecert}, the finite Boole ledger of \autoref{ch:concrete-instances-finite-union-bound-namecert}, the weak-law finite-average packet of \autoref{ch:concrete-instances-weak-law-large-numbers-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite maximal-window certificate for displayed prefixes; it is not a countable-choice selector, almost-sure quotient, measure-completion object, host limit theorem, or ambient $\RealUp$ completeness principle.

\begin{definition}[Kolmogorov maximal inequality carrier]
\label{def:kolmogorov-maximal-inequality-carrier}
A $\KolmogorovMaximalInequalityUp$ carrier is a finite $\BHist$ packet
$$
K=(\Omega,X,S,B,C,U,R,H,K_0,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $\Omega$ is the $\ProbSpaceUp$ source row carrying the event classifier and total-mass normalization.
\item $X$ is the finite $\RandomVarUp$ prefix row, listing the displayed variables $X_1,\ldots,X_n$ over the same probability source.
\item $S$ is the partial-sum window row, recording the displayed sums $S_k=X_1+\cdots+X_k$ for $1\le k\le n$.
\item $B$ is the maximum-threshold row, naming the positive $\RealUp$ threshold and the event $\max_{1\le k\le n}|S_k|\ge B$ as a finite window over $S$.
\item $C$ is the Chebyshev tail-row family, one displayed $\ChebyshevInequalityUp$ read for each stopped partial-sum window needed by the maximal estimate.
\item $U$ is the $\FiniteUnionBoundUp$ ledger that joins the finitely many exposed tail rows into the maximal-window event bound.
\item $R$ is the $\RealUp$ bound seal reached only after $\Omega,X,S,B,C,U$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, prefix, partial-sum, threshold, Chebyshev, finite-union, bound, replay, provenance, and local naming rows.
\item $K_0$ records displayed $\Cont$ replay of the finite maximal-window route.
\item $P$ is the $\Pkg$ provenance over $\KolmogorovMaximalInequalityUp$, $\ProbSpaceUp$, $\RandomVarUp$, $\ChebyshevInequalityUp$, $\FiniteUnionBoundUp$, $\WeakLawLargeNumbersUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KolmogorovMaximalInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $\Omega,X,S,B,C,U,R,H,K_0,P,N$. It has no coordinate for countable choice, almost-sure quotient equality, measure completion, a host-side limiting theorem, an infinite stopping-time selector, or ambient real completeness.
\end{definition}

\begin{theorem}[Kolmogorov maximal inequality NameCert obligations]
\label{thm:kolmogorov-maximal-inequality-namecert-obligations}
For every accepted $\KolmogorovMaximalInequalityUp$ carrier
$$
K=(\Omega,X,S,B,C,U,R,H,K_0,P,N),
$$
the local $\NameCert_{\KolmogorovMaximalInequalityUp}$ surface consists exactly of probability-source exposure through $\Omega$, finite random-variable prefix exposure through $X$, partial-sum window exactness through $S$, maximum-threshold exposure through $B$, Chebyshev tail-row exposure through $C$, finite-union ledger exposure through $U$, terminal real-bound sealing through $R$, componentwise transport through $H$, displayed replay through $K_0$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kolmogorov-maximal-inequality-carrier}. The projection exposes the probability source, finite random-variable prefix, partial-sum windows, maximum-threshold row, Chebyshev tail rows, finite-union ledger, Real bound seal, transport, replay, provenance, and local naming.

The prefix rows are ordered. The partial sums in $S$ can be read only from the displayed prefix $X_1,\ldots,X_n$ over the source $\Omega$, and the maximal event in $B$ is a finite event window over those displayed sums.

The tail rows are downstream from that window. Each row of $C$ is a Chebyshev read for one displayed stopped partial-sum prefix, while $U$ records the finite union-bound ledger over exactly those rows.

The endpoint $R$ is therefore a real-bound seal for the finite maximal event named by $B$. The structural rows $H,K_0,P,N$ transport, replay, source, and name the same route, so no unlisted measure completion, countable selector, almost-sure quotient, host limit, or ambient completeness coordinate remains.
\end{proof}

\begin{theorem}[Kolmogorov partial-sum window bound]
\label{thm:kolmogorov-maximal-inequality-partial-sum-window-bound}
Every accepted $\KolmogorovMaximalInequalityUp$ maximal-window read factors through the finite route
$$
\Omega,X,S,B \longmapsto C \longmapsto U \longmapsto R \longmapsto H,K_0,P,N .
$$
For each displayed prefix length $n$ and threshold row $B$, the route first opens the finite partial-sum windows $S_1,\ldots,S_n$, then applies the Chebyshev tail rows in $C$ to the stopped window pieces, then composes those finitely many rows through the $\FiniteUnionBoundUp$ ledger $U$, and only then exposes the $\RealUp$ bound seal $R$. Thus the maximal-window estimate is available to weak-law, concentration, and later strong-law-facing consumers as finite BEDC data, not as an almost-sure, countable-choice, measure-completion, host-limit, or ambient-completeness principle.
\end{theorem}
\begin{proof}
Use induction on the displayed prefix length in $X$. In the base case, the packet exposes the empty or one-term partial-sum window through $\Omega,X,S$, and the threshold row $B$ names the only finite maximal event that can be inspected.

For the successor step, extend the displayed prefix by one $\RandomVarUp$ row. The new partial-sum row is appended to $S$, while the already displayed prefix sums remain available through the same componentwise $\hsame$ transport.

Now open the tail-row family $C$. Each stopped prefix event is a finite Chebyshev request over the same probability source and the same displayed partial-sum data; no countable family or stopping-time selector is introduced.

The finite-union ledger $U$ then composes exactly the rows supplied by $C$. This is the Boole-inequality step of \autoref{ch:concrete-instances-finite-union-bound-namecert}, so the maximal event remains a finite union of displayed tail events.

Finally read the $\RealUp$ seal $R$ and the structural rows $H,K_0,P,N$. The seal is downstream from the finite prefix, Chebyshev rows, and union ledger, while the structural rows preserve the same route for weak-law and concentration consumers. Since the carrier contains no coordinate for almost-sure quotient equality, measure completion, countable choice, host limits, or ambient real completeness, the partial-sum maximal bound stays inside finite BEDC data.
\end{proof}

\falsifiablePrediction{Every accepted $\KolmogorovMaximalInequalityUp$ maximal-window read exposes a ProbSpace source row, finite RandomVar prefix, partial-sum window row, maximum-threshold row, Chebyshev tail rows, finite-union ledger, RealUp bound seal, hsame transport, Cont replay, Pkg provenance, and local NameCert row before the bound is consumed.}

\independenceWitness{probspace, randomvar, chebyshev-inequality, finite-union-bound, weak-law-large-numbers, real: $\KolmogorovMaximalInequalityUp$ is not bijective to any listed sibling because it jointly carries a finite probability source, random-variable prefix, partial-sum maximal window, Chebyshev tail family, finite-union ledger, terminal real-bound seal, transport, replay, provenance, and local naming.}

\closureat{KolmogorovMaximalInequalityUp}{seedStr}

\begin{closurestatus}{\KolmogorovMaximalInequalityUp}
  \constructivestory{A $\KolmogorovMaximalInequalityUp$ packet is generated from a probability source row, finite random-variable prefix, partial-sum window rows, a maximum-threshold row, Chebyshev tail rows, a finite-union ledger, a RealUp bound seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:kolmogorov-maximal-inequality-carrier}, \autoref{thm:kolmogorov-maximal-inequality-namecert-obligations}, and \autoref{thm:kolmogorov-maximal-inequality-partial-sum-window-bound} seed the finite partial-sum maximal-window route over probability, random-variable, Chebyshev, finite-union, RealUp, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close almost-sure convergence, countable-choice selection, measure completion, quotient event equality, host limiting theorems, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite prefix exposure, partial-sum window exactness, maximum-threshold exposure, Chebyshev tail-row stability, finite-union ledger composition, Real bound nonescape, transport, replay, provenance, and local naming for BEDC.Derived.KolmogorovMaximalInequalityUp.}
\end{closurestatus}
