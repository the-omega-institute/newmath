\chapter{A Concrete Naming Certificate for $\RegSeqRatIntervalIntersectionUp$}
\label{ch:concrete-instances-regseqrat-interval-intersection-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis of nested rational intervals, the $\RegSeqRatIntervalIntersectionUp$ packet records interval intersection as finite NameCert data over \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-cauchymodulus-namecert}, \autoref{ch:concrete-instances-regularcauchyseparation-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names the displayed endpoint, window, radius, descent, separation, and real-seal route for a nested regular-rational interval intersection; it is not quotient real equality, host completeness, trichotomy, a selected limit, countable choice, or open-cover compactness.

\begin{definition}[RegSeqRat interval-intersection carrier]
\label{def:regseqrat-interval-intersection-carrier}
A $\RegSeqRatIntervalIntersectionUp$ carrier is a finite $\BHist$ packet
$$
I=(L,R,W,D,M,S,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are endpoint $\RegSeqRatUp$ rows for the left and right sides of the displayed nested interval family.
\item $W$ is the finite $\StreamNameUp$ window ledger recording the endpoint observations actually read by the packet.
\item $D$ is the $\DyadicRatCoreUp$ interval and radius ledger, including the displayed inclusions, widths, and slack budgets.
\item $M$ is the $\CauchyModulusUp$ descent row that records how a requested precision is sent to a sufficiently narrow displayed interval.
\item $S$ is the $\RegularCauchySeparationUp$ sibling row, keeping endpoint separation and compatibility checks visible.
\item $E$ is the $\RealUp$ seal boundary reached only after the endpoint, window, dyadic, modulus, and separation rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for endpoint, window, dyadic, modulus, separation, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from endpoint windows and dyadic radius reads through the descent and separation rows to the seal boundary.
\item $P$ records $\Pkg$ provenance over $\RegSeqRatIntervalIntersectionUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, $\RegularCauchySeparationUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegSeqRatIntervalIntersectionUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient real equality, host completeness, trichotomy, a selected limit, countable choice, or open-cover compactness.
\end{definition}

\begin{theorem}[RegSeqRat interval-intersection NameCert obligations]
\label{thm:regseqrat-interval-intersection-namecert-obligations}
For every accepted $\RegSeqRatIntervalIntersectionUp$ carrier
$$
I=(L,R,W,D,M,S,E,H,C,P,N),
$$
the local $\NameCert_{\RegSeqRatIntervalIntersectionUp}$ surface consists exactly of endpoint $\RegSeqRatUp$ exposure, finite $\StreamNameUp$ window exposure, $\DyadicRatCoreUp$ interval and radius exposure, $\CauchyModulusUp$ descent admission, $\RegularCauchySeparationUp$ sibling compatibility, $\RealUp$ seal-boundary handoff, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regseqrat-interval-intersection-carrier}. The rows $L,R,W,D,M,S,E,H,C,P,N$ are the full displayed inventory.

The endpoint rows $L,R$ and finite window ledger $W$ expose only the regular-rational observations read by the packet. The dyadic ledger $D$ records the visible interval inclusions, radii, and slack budgets for those observations.

The modulus row $M$ is the constructive descent row: it sends a requested precision to a displayed interval narrow enough for the downstream read. The separation row $S$ keeps the sibling compatibility check attached to the same endpoint and dyadic data.

The real-facing row $E$ is therefore a seal boundary downstream of finite regular-rational, stream-window, dyadic, modulus, and separation data. The structural rows $H,C,P,N$ transport, replay, source, and name that route.

Since quotient real equality, host completeness, trichotomy, selected limits, countable choice, and open-cover compactness are absent from the carrier, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[RegSeqRat interval-intersection Real-seal route]
\label{thm:regseqrat-interval-intersection-real-seal-route}
Every $\RealUp$ consumer of an accepted $\RegSeqRatIntervalIntersectionUp$ carrier reaches the seal row $E$ only after reading the endpoint rows $L,R$, the finite window ledger $W$, the dyadic interval and radius ledger $D$, the modulus descent row $M$, and the regular-Cauchy separation row $S$. The exported route is a displayed seal-boundary route, not quotient real equality, host completeness, trichotomy, a selected limit, countable choice, or open-cover compactness.
\end{theorem}
\begin{proof}
Use \autoref{thm:regseqrat-interval-intersection-namecert-obligations}. It orders the consumer path so that endpoint, window, dyadic, modulus, and separation rows are exposed before the $\RealUp$ seal row can be consumed.

The descent row $M$ supplies the precision-to-window route, and the dyadic row $D$ supplies the corresponding interval and radius budget. The separation row $S$ ensures that the two endpoint streams are consumed as compatible siblings rather than as an external quotient object.

The rows $H,C,P,N$ do not add analytic or choice principles; they preserve the displayed route through transport, replay, provenance, and local naming. Thus the Real-facing read is exactly the finite seal-boundary handoff named in the statement.
\end{proof}

\begin{theorem}[RegSeqRat interval-intersection sibling dependency]
\label{thm:regseqrat-interval-intersection-sibling-dependency}
Any accepted $\RegSeqRatIntervalIntersectionUp$ carrier depends simultaneously on the endpoint $\RegSeqRatUp$ rows, the finite $\StreamNameUp$ window ledger, the $\DyadicRatCoreUp$ radius ledger, the $\CauchyModulusUp$ descent row, the $\RegularCauchySeparationUp$ sibling row, and the terminal $\RealUp$ seal boundary. Dropping any one of these sibling rows changes the local packet into a different NameCert surface.
\end{theorem}
\begin{proof}
Read the carrier coordinates from \autoref{def:regseqrat-interval-intersection-carrier}. The endpoint rows $L,R$ supply the two regular-rational sides, while $W$ supplies the finite observations through which those endpoints are read.

The dyadic row $D$ is the only row carrying displayed radius and inclusion budgets, and the modulus row $M$ is the only row carrying the precision descent. The separation row $S$ is the only row recording sibling compatibility for the two endpoints.

The seal row $E$ is the only Real-facing boundary, and $H,C,P,N$ only preserve and name the already displayed dependency route. Removing any endpoint, window, dyadic, modulus, separation, or seal row therefore removes a coordinate required by the local NameCert obligations.

The conclusion is constructive projection, not a uniqueness theorem about host reals. It refuses quotient real equality, host completeness, trichotomy, selected limits, countable choice, and open-cover compactness throughout.
\end{proof}

\falsifiablePrediction{Every $\RegSeqRatIntervalIntersectionUp$ seal read factors through endpoint $\RegSeqRatUp$ rows, a finite $\StreamNameUp$ window ledger, a $\DyadicRatCoreUp$ interval and radius ledger, a $\CauchyModulusUp$ descent row, a $\RegularCauchySeparationUp$ sibling row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before reaching a $\RealUp$ boundary.}

\independenceWitness{regseqrat, streamname, dyadicratcore, cauchymodulus, regularcauchyseparation, real: $\RegSeqRatIntervalIntersectionUp$ is not bijective to any listed sibling because it joins endpoint regular-rational rows, finite stream windows, dyadic interval budgets, modulus descent, sibling separation, and a real-seal boundary as one NameCert packet.}

\closureat{RegSeqRatIntervalIntersectionUp}{seedStr}

\begin{closurestatus}{\RegSeqRatIntervalIntersectionUp}
  \constructivestory{A $\RegSeqRatIntervalIntersectionUp$ packet is generated from endpoint RegSeqRat rows, finite StreamName window rows, DyadicRatCore interval and radius ledgers, CauchyModulus descent rows, RegularCauchySeparation sibling rows, a Real seal-boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regseqrat-interval-intersection-carrier}, \autoref{thm:regseqrat-interval-intersection-namecert-obligations}, \autoref{thm:regseqrat-interval-intersection-real-seal-route}, and \autoref{thm:regseqrat-interval-intersection-sibling-dependency} seed the nested RegSeqRat interval-intersection NameCert route up to the displayed Real seal boundary.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.RegSeqRatIntervalIntersectionUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, host completeness, trichotomy, selected limits, countable choice, open-cover compactness, theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint exposure, finite window exposure, dyadic radius exactness, modulus descent, sibling separation, Real seal-boundary routing, transport, replay, provenance, and local naming for BEDC.Derived.RegSeqRatIntervalIntersectionUp.}
\end{closurestatus}
