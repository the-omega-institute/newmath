\chapter{A Concrete Naming Certificate for $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$}
\label{ch:concrete-instances-rule110-cellular-trust-substrate-namecert}
\origin{ai}

The $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ packet realizes the Rule 110 axis of \autoref{ch:visions-cellular-automaton-substrate-reading} as finite NameCert data. It refines the generic cellular trust substrate of \autoref{ch:concrete-instances-cellulartrustsubstrate-namecert} by fixing the eight-entry Rule 110 transition table, manifest schema, orbit-window replay, classifier surface, frontier ledger, transport rows, and provenance rows before any cellular-pattern or cyclic-tag consumer can read a substrate observation.

\begin{definition}[Rule 110 cellular trust substrate carrier]
\label{def:rule110-cellular-trust-substrate-carrier}
A $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ carrier is a finite $\BHist$ packet
$$
R=(T,M,O,K,F,B,U,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the Rule 110 transition-table row, recording the eight three-cell cases and their output bits.
\item $M$ is the manifest row, recording the finite initial row, width, step count, expected observation claims, and fixed text-format schema.
\item $O$ is the orbit-window row, containing only finite prefixes of the Rule 110 evolution generated from $T$ and $M$.
\item $K$ is the classifier row, read through the cellular row classifier of \autoref{ch:concrete-instances-cellularrowclassifier-namecert}, the Rule 110 pattern classifier of \autoref{ch:concrete-instances-rule110-pattern-classifier-namecert}, and the glider catalog of \autoref{ch:concrete-instances-rule110glidercatalog-namecert}.
\item $F$ is the Cook-frontier ledger, read through \autoref{ch:concrete-instances-cook-frontier-witness-namecert} when a bounded cyclic-tag or compile-frontier consumer is present.
\item $B$ is the cellular-substrate boundary row, recording that the packet is a closed observational substrate rather than a host evaluator or universality theorem.
\item $U$ is the bounded cyclic-tag handoff row, read through \autoref{ch:concrete-instances-cyclic-tag-system-namecert} only after the finite orbit and frontier rows have been displayed.
\item $A$ is the local-stencil audit row, read through \autoref{ch:concrete-instances-rule110-local-stencil-observation-namecert} for each inspected transition.
\item $H$ records componentwise $\hsame$ transport for the rule table, manifest, orbit window, classifier, frontier ledger, boundary, bounded handoff, audit row, replay, provenance, and local name.
\item $C$ records displayed $\Cont$ replay of the route
$$
T,M,A \longmapsto O \longmapsto K,F,B,U .
$$
\item $P$ is the $\Pkg$ provenance over $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$, $\CellularTrustSubstrateUp$, $\CellularAutomatonUp$, $\CellularRowClassifierUp$, $\RuleOneTenPatternClassifierUp$, $\RuleOneTenGliderCatalogUp$, $\RuleOneTenLocalStencilObservationUp$, $\CyclicTagSystemUp$, $\CookFrontierWitnessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,M,O,K,F,B,U,A,H,C,P,N$. It has no coordinate for a host C evaluator, a complete infinite orbit, a detached universality theorem, unbounded halting, selected glider semantics, or an external proof of induction.
\end{definition}

\begin{theorem}[Rule 110 cellular trust substrate NameCert obligations]
\label{thm:rule110-cellular-trust-substrate-namecert-obligations}
For every accepted $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ carrier
$$
R=(T,M,O,K,F,B,U,A,H,C,P,N),
$$
the local $\NameCert_{\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}}$ surface consists exactly of carrier admission for \autoref{def:rule110-cellular-trust-substrate-carrier}, transition-table exactness through $T$, manifest schema admission through $M$, finite orbit-window replay through $O$, classifier exposure through $K$, frontier-ledger exposure through $F$, closed-substrate boundary through $B$, bounded cyclic-tag handoff through $U$, local-stencil audit through $A$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The tuple exposes exactly the transition table, manifest, orbit-window, classifier, frontier, boundary, cyclic-tag handoff, local-stencil audit, and structural rows.

The finite substrate rows are $T,M,O,A$. The transition table fixes the local rule, the manifest fixes the finite input and expected observations, the audit row checks each inspected local stencil, and the orbit window contains only the finite replay generated from those rows.

The consumer rows are $K,F,B,U$. Pattern consumers read $K$, frontier consumers read $F$, closed-substrate consumers read $B$, and bounded cyclic-tag consumers read $U$. Each consumer row is downstream of the displayed finite substrate route.

Finally $H,C,P,N$ transport, replay, source, and name that same route. Since the carrier has no coordinate for the excluded host evaluator, complete orbit, detached universality theorem, unbounded halting principle, selected glider semantics, or external induction proof, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Rule 110 cellular trust substrate observation boundary]
\label{thm:rule110-cellular-trust-substrate-observation-boundary}
Every accepted $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ observation factors through the finite route
$$
T,M,A \longmapsto O \longmapsto K,F,B,U \longmapsto H,C,P,N .
$$
Thus a pattern-classifier, frontier-ledger, bounded cyclic-tag, or closed-substrate consumer can read Rule 110 only after the finite rule table, manifest, orbit window, local-stencil audit, transport, replay, provenance, and local $\NameCert$ rows have been displayed.
\end{theorem}
\begin{proof}
Start with the local-stencil route. The transition table $T$ and audit row $A$ expose each inspected Rule 110 update as finite three-cell data, while the manifest row $M$ fixes the initial row and bounded step count.

Replay the orbit window $O$ from those rows. No consumer receives a whole infinite evolution; every read is a finite orbit prefix generated from the displayed table and manifest.

Now attach the public consumers. The classifier row $K$, frontier ledger $F$, closed-substrate boundary $B$, and bounded cyclic-tag handoff $U$ can inspect only the same finite orbit route.

The rows $H,C,P,N$ preserve and name the route. Any host evaluator, detached universality proof, unbounded halting claim, selected glider semantics, or external induction proof would require a coordinate outside the accepted packet.
\end{proof}

\falsifiablePrediction{Every $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ observation first exposes the finite Rule 110 transition table, manifest row, local-stencil audit, orbit-window replay, classifier row, frontier ledger, closed-substrate boundary, bounded cyclic-tag handoff, componentwise hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{cellulartrustsubstrate, cellularautomaton, cellularrowclassifier, rule110-pattern-classifier, cyclic-tag-system: $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ is not bijective to any listed sibling because it jointly fixes the Rule 110 table, manifest schema, orbit windows, classifier, Cook-frontier ledger, bounded cyclic-tag handoff, audit route, transport, replay, provenance, and local naming.}

The Lean TasteGate follow-up for \texttt{BEDC.Derived.Rule110CellularTrustSubstrateUp} must provide explicit $\mathsf{Nontrivial}\ \mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ and $\mathsf{FieldFaithful}\ \mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ instances over $T,M,O,K,F,B,U,A,H,C,P,N$. These instances keep the rule table, manifest, orbit-window, classifier, frontier, boundary, handoff, and audit rows as separate fields.

\closureat{Rule110CellularTrustSubstrateUp}{seedStr}

\begin{closurestatus}{\RuleOneTenCellularTrustSubstrateUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rule110-cellular-trust-substrate-carrier}, \autoref{thm:rule110-cellular-trust-substrate-namecert-obligations}, and \autoref{thm:rule110-cellular-trust-substrate-observation-boundary} seed the finite Rule 110 cellular trust substrate packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host evaluator correctness, complete infinite orbits, Cook universality, unbounded halting, selected glider semantics, internal induction, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, transition-table exactness, manifest admission, orbit-window replay, classifier exposure, Cook-frontier ledger exposure, bounded cyclic-tag handoff, local-stencil audit, transport, replay, provenance, TasteGate instances, and local naming for BEDC.Derived.Rule110CellularTrustSubstrateUp.}
  \constructivestory{A $\mathsf{Rule110CellularTrustSubstrate}^{\uparrow}$ packet is generated from a finite Rule 110 transition table, manifest row, orbit-window replay, classifier row, Cook-frontier ledger, closed-substrate boundary, bounded cyclic-tag handoff, local-stencil audit, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
