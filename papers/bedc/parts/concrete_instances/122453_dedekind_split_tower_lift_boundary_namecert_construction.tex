\chapter{A Concrete Naming Certificate for $\DedekindSplitTowerLiftBoundaryUp$}
\label{ch:concrete-instances-dedekind-split-tower-lift-boundary-namecert}
\origin{ai}

The $\DedekindSplitTowerLiftBoundaryUp$ packet realizes the boundary side of \autoref{sec:unified-reality-dedekind-split-tower-lift} as finite NameCert data. It records only displayed $q$-split Dedekind lift rows, local split-prime energy, tower provenance, zeta and number-field sources, refusal, transport, replay, and local naming. It sits beside the zeta carrier of \autoref{ch:concrete-instances-zeta-basic-namecert}, the prime carrier of \autoref{ch:concrete-instances-prime-namecert}, the number-field carrier of \autoref{ch:concrete-instances-numfield-namecert}, the Dedekind carrier of \autoref{ch:concrete-instances-dedekind-namecert}, and the prime-spectral fingerprint of \autoref{ch:concrete-instances-prime-spectral-fingerprint-namecert}. The packet is a boundary certificate for visible lift rows; it does not construct Dedekind cuts, form quotient completions, prove $\mathsf{RH}$, assert analytic continuation, or internalize an infinite tower.

\begin{definition}[Dedekind split-tower lift boundary carrier]
\label{def:dedekind-split-tower-lift-boundary-carrier}
A $\DedekindSplitTowerLiftBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(Z,P,K,D,E,F,Q,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the $\ZetaBasicUp$ source row naming the visible Riemann zeta read being routed into the lift boundary.
\item $P$ is the $\PrimeUp$ split-prime row naming the displayed rational prime $q$ and the finite split-prime windows inspected by the packet.
\item $K$ is the $\NumFieldUp$ tower-source row naming the displayed number-field levels and Galois-route data consumed by the boundary.
\item $D$ is the $\DedekindUp$ read row naming the displayed Dedekind zeta read at the inspected tower levels.
\item $E$ is the split-prime energy row, recording only displayed normal-energy terms for the listed prime-ideal rows.
\item $F$ is the refusal row: an off-route Dedekind lift row with no displayed split-prime energy is rejected by the carrier.
\item $Q$ is the $\PrimeSpectralFingerprintUp$ handoff row, naming the prime-local response fingerprint consumed by the boundary without claiming a spectral classification.
\item $H$ records componentwise $\hsame$ transport for zeta, prime, number-field, Dedekind, energy, refusal, fingerprint, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the same finite route.
\item $G$ is the $\Pkg$ provenance over $\DedekindSplitTowerLiftBoundaryUp$, $\ZetaBasicUp$, $\PrimeUp$, $\NumFieldUp$, $\DedekindUp$, $\PrimeSpectralFingerprintUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DedekindSplitTowerLiftBoundaryUp}$ row.
\end{enumerate}
The carrier has no coordinate for a Dedekind-cut real, quotient completion, selected tower limit, analytic-continuation proof, RH proof object, hidden prime-ideal family, or ambient spectral theorem.
\end{definition}

\begin{theorem}[Dedekind split-tower lift boundary NameCert obligations]
\label{thm:dedekind-split-tower-lift-boundary-namecert-obligations}
For every accepted $\DedekindSplitTowerLiftBoundaryUp$ carrier
$$
B=(Z,P,K,D,E,F,Q,H,C,G,N),
$$
the local $\NameCert_{\DedekindSplitTowerLiftBoundaryUp}$ surface consists exactly of zeta-source admission, split-prime admission, number-field tower-source admission, Dedekind-read exposure, split-prime energy exposure, off-route refusal, prime-spectral fingerprint handoff, componentwise transport, displayed replay, provenance, and local naming. The packet therefore records a finite boundary route for $q$-split Dedekind lift rows without constructing Dedekind cuts, quotient completions, hidden tower limits, analytic-continuation proofs, RH proof objects, or ambient spectral classifications.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dedekind-split-tower-lift-boundary-carrier}. The rows $Z,P,K,D,E,F,Q$ are the boundary-facing data, and $H,C,G,N$ are the structural rows.

The source route starts with the zeta row $Z$, split-prime row $P$, number-field tower row $K$, and Dedekind read row $D$. These rows display the finite tower and split-prime data that the boundary is allowed to inspect.

The energy row $E$ and refusal row $F$ make the boundary falsifiable. A lift row that is not accompanied by displayed split-prime energy cannot enter the accepted packet, and the fingerprint row $Q$ supplies only a finite prime-local handoff.

Transport, replay, provenance, and local naming preserve the same displayed rows through $H,C,G,N$. Since the carrier contains no Dedekind cut, quotient completion, selected tower limit, analytic-continuation proof, RH proof object, hidden prime-ideal family, or ambient spectral theorem, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Dedekind split-tower lift boundary nonescape]
\label{thm:dedekind-split-tower-lift-boundary-nonescape}
Every accepted $\DedekindSplitTowerLiftBoundaryUp$ read must pass through
$$
Z,P,K,D,E,F,Q,H,C,G,N
$$
before any boundary consumer may inspect the lift route. In particular, an off-route Dedekind lift row with no displayed split-prime energy row $E$ refutes the carrier rather than supplying a hidden tower escape.
\end{theorem}
\begin{proof}
Start with an accepted carrier $B=(Z,P,K,D,E,F,Q,H,C,G,N)$. The zeta, prime, number-field, and Dedekind rows display the only source route that the boundary recognizes.

The split-prime energy row $E$ is required before the lift is read as boundary data. The refusal row $F$ declares that a Dedekind lift row lacking such displayed energy is outside the accepted carrier.

The fingerprint row $Q$ and structural rows $H,C,G,N$ then carry only finite prime-local response, transport, replay, provenance, and local naming. They cannot introduce a hidden tower limit or RH-facing proof object because no such coordinate appears in the carrier.

Thus any boundary consumer must traverse the displayed rows in the stated order, and an off-route lift row without $E$ refutes the carrier surface.
\end{proof}

\falsifiablePrediction{Any off-route Dedekind lift row accepted without displayed split-prime energy refutes the $\DedekindSplitTowerLiftBoundaryUp$ carrier.}

\independenceWitness{zeta-basic, prime, numfield, dedekind, prime-spectral-fingerprint: $\DedekindSplitTowerLiftBoundaryUp$ is not bijective to any listed sibling because it jointly carries zeta-source admission, split-prime display, number-field tower source, Dedekind read, split-prime energy, off-route refusal, prime-spectral fingerprint handoff, transport, replay, provenance, and local naming as one finite boundary packet.}

The Lean follow-up for this chapter must provide \texttt{Nontrivial} and \texttt{FieldFaithful} TasteGate instances for the displayed zeta, prime, number-field, Dedekind, split-energy, refusal, fingerprint, transport, replay, provenance, and naming carrier.

\closureat{DedekindSplitTowerLiftBoundaryUp}{seedStr}

\begin{closurestatus}{\DedekindSplitTowerLiftBoundaryUp}
  \constructivestory{A $\DedekindSplitTowerLiftBoundaryUp$ packet is generated from zeta-source, split-prime, number-field tower, Dedekind read, split-prime energy, refusal, prime-spectral fingerprint, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dedekind-split-tower-lift-boundary-carrier}, \autoref{thm:dedekind-split-tower-lift-boundary-namecert-obligations}, \autoref{thm:dedekind-split-tower-lift-boundary-nonescape}, the falsifiable prediction, and the independence witness seed the finite boundary packet for displayed $q$-split Dedekind lift rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Dedekind-cut construction, quotient completion, selected tower limits, analytic continuation, $\mathsf{RH}$, ambient spectral classification, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, zeta-source admission, split-prime display, number-field tower-source admission, Dedekind-read exposure, split-prime energy exactness, off-route refusal, fingerprint handoff, transport, replay, provenance, local naming, and the required Nontrivial and FieldFaithful TasteGate instances for BEDC.Derived.DedekindSplitTowerLiftBoundaryUp.}
\end{closurestatus}
