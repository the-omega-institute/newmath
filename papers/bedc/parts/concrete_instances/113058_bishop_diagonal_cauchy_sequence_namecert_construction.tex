\chapter{A Concrete Naming Certificate for $\BishopDiagonalCauchySequenceUp$}
\label{ch:concrete-instances-bishop-diagonal-cauchy-sequence-namecert}
\origin{human}

Following Bishop and Bridges on diagonal arguments for regular Cauchy names, the $\BishopDiagonalCauchySequenceUp$ packet records the constructive diagonal readback of a displayed family of regular rational Cauchy names as finite NameCert data. It sits after the diagonalization route of \autoref{ch:concrete-instances-regular-cauchy-diagonalization-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives Real and Completion consumers one visible diagonal Cauchy sequence route without quotient streams, selected limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop diagonal Cauchy sequence carrier]
\label{def:bishop-diagonal-cauchy-sequence-carrier}
A $\BishopDiagonalCauchySequenceUp$ carrier is a finite $\BHist$ packet
$$
D=(F,I,S,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the family row, naming the finite inspected portion of a family of regular rational Cauchy names.
\item $I$ is the diagonal-index row, recording the unary stage selector used to read the family along the Bishop diagonal.
\item $S$ is the $\StreamNameUp$ window row, displaying the finite windows inspected from each selected source name.
\item $T$ is the $\DyadicRatCoreUp$ tolerance row, carrying the dyadic budgets and comparison ledgers used by the diagonal read.
\item $R$ is the $\RegSeqRatUp$ readback row for the diagonal sequence obtained from $F,I,S,T$.
\item $E$ is the $\RealUp$ seal row consumed only after the regular rational diagonal readback is visible.
\item $H$ records componentwise $\hsame$ transport for the family, index, stream, tolerance, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Real or Completion precision request through the diagonal selector into the regular rational readback.
\item $P$ is the $\Pkg$ provenance over $\BishopDiagonalCauchySequenceUp$, $\RegularCauchyDiagonalizationUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopDiagonalCauchySequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,I,S,T,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, countable choice, a selected host limit, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop diagonal Cauchy sequence NameCert obligations]
\label{thm:bishop-diagonal-cauchy-sequence-namecert-obligations}
For every accepted $\BishopDiagonalCauchySequenceUp$ carrier
$$
D=(F,I,S,T,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopDiagonalCauchySequenceUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item family and diagonal-index exposure through $F$ and $I$;
\item finite stream-window exposure through $S$;
\item dyadic tolerance and comparison exposure through $T$;
\item regular rational diagonal readback through $R$;
\item terminal Real-facing seal through $E$;
\item structural transport, replay, provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No diagonal Cauchy obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-diagonal-cauchy-sequence-carrier}. The displayed coordinates are exactly the family, diagonal-index, stream-window, dyadic-tolerance, regular-readback, Real-seal, transport, replay, provenance, and local naming rows.

The family and selector rows expose the diagonal source before any Cauchy read is available. The stream and tolerance rows then supply the finite inspected windows and dyadic budgets used by the diagonal readback.

The regular rational row $R$ is downstream of those rows, so the Real-facing seal $E$ can be consumed only after the $\RegSeqRatUp$ diagonal sequence is visible. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route.

Since the carrier contains no quotient stream, choice functional, selected host limit, trichotomy row, host equality row, or ambient-completeness row, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop diagonal Cauchy sequence Real handoff]
\label{thm:bishop-diagonal-cauchy-sequence-real-handoff}
Every Real or Completion consumer of an accepted $\BishopDiagonalCauchySequenceUp$ carrier must read the diagonal route
$$
\RegularCauchyDiagonalizationUp,\quad
\StreamNameUp,\quad
\DyadicRatCoreUp,\quad
\RegSeqRatUp,\quad
\RealUp
$$
before treating the diagonal sequence as a completed real-name boundary. The handoff exports only the displayed family row, diagonal-index row, finite stream windows, dyadic tolerance ledger, regular rational readback, Real seal, and structural rows.
\end{theorem}
\begin{proof}
Start from the obligation surface in \autoref{thm:bishop-diagonal-cauchy-sequence-namecert-obligations}. It orders the diagonal family and selector before stream windows, dyadic tolerances, regular readback, and Real seal data.

Apply the sibling diagonalization route \autoref{thm:regular-cauchy-diagonalization-real-seal-handoff}. The diagonal read becomes Real-facing only through the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ seal rows.

The replay row $C$ sends a Real or Completion precision request through exactly that route, while $H,P,N$ preserve, source, and name it. Therefore the handoff cannot bypass the four-object L10 seal or export quotient streams, selected limits, countable choice, trichotomy, host equality, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopDiagonalCauchySequenceUp$ read must expose a regular-Cauchy diagonalization row, StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, Real seal, hsame transport, Cont replay, Pkg provenance, and local NameCert row before any Real or Completion consumer receives it.}

\independenceWitness{regular-cauchy-diagonalization, streamname, regseqrat, dyadicratcore, real: no listed sibling supplies the whole ten-row packet $D=(F,I,S,T,R,E,H,C,P,N)$, because each sibling contributes only diagonal selection, finite windows, tolerance rows, regular readback, or terminal Real sealing while this chapter binds the route with transport, replay, provenance, and local naming.}

\closureat{BishopDiagonalCauchySequenceUp}{seedStr}

\begin{closurestatus}{\BishopDiagonalCauchySequenceUp}
  \constructivestory{A $\BishopDiagonalCauchySequenceUp$ packet is generated from a displayed family of regular rational names, a diagonal-index row, StreamName windows, DyadicRatCore tolerance ledgers, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-diagonal-cauchy-sequence-carrier}, \autoref{thm:bishop-diagonal-cauchy-sequence-namecert-obligations}, and \autoref{thm:bishop-diagonal-cauchy-sequence-real-handoff} seed the finite constructive diagonal Cauchy sequence route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected limits, countable choice, trichotomy, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, diagonal-index exposure, finite-window exposure, dyadic-tolerance exactness, RegSeqRat readback, Real-seal handoff, structural stability, and non-escape for BEDC.Derived.BishopDiagonalCauchySequenceUp.}
\end{closurestatus}
