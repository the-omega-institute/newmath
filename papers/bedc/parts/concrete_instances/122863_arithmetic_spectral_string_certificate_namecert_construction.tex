\chapter{A Concrete Naming Certificate for $\ArithmeticSpectralStringCertificateUp$}
\label{ch:concrete-instances-arithmetic-spectral-string-certificate-namecert}
\origin{ai}

The $\ArithmeticSpectralStringCertificateUp$ packet realizes the route of \autoref{ch:visions-rh-arithmetic-spectral-string-route} as finite normal-mode, prime-ledger, and unit-boundary NameCert data. It reads prime frequency registers through \autoref{ch:concrete-instances-prime-namecert}, finite exponent and support ledgers through \autoref{ch:concrete-instances-nat-namecert}, rational coefficient rows through \autoref{ch:concrete-instances-rat-namecert}, and the terminal real comparison surface through \autoref{ch:concrete-instances-real-namecert}. The packet names a BEDC certificate surface for the arithmetic string route; it does not prove the Riemann hypothesis, construct all zeta-zero certificates, import a physical string model, or assert a host spectral theorem.

\begin{definition}[ArithmeticSpectralStringCertificate carrier]
\label{def:arithmetic-spectral-string-certificate-carrier}
An $\ArithmeticSpectralStringCertificateUp$ carrier is a finite $\BHist$ packet
$$
S=(M,Q,L,U,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the finite normal-mode packet, listing the active logarithmic modes inspected by the certificate.
\item $Q$ is the rational coefficient row, read through $\RatUp$, for the finite amplitude data attached to the active modes.
\item $L$ is the prime ledger row, read through $\PrimeUp$ and $\NatUp$, recording the factorization data behind the displayed logarithmic heights.
\item $U$ is the unit-boundary row saying that the active centered channels are inspected only through unit phase data.
\item $Z$ is the zero-readout row, exposing the finite cancellation certificate before any global zero-coverage claim is available.
\item $H$ records componentwise $\hsame$ transport for modes, coefficients, prime ledgers, unit-boundary rows, zero readouts, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
M,Q,L,U \longmapsto Z .
$$
\item $P$ is the $\Pkg$ provenance over $\ArithmeticSpectralStringCertificateUp$, $\PrimeUp$, $\NatUp$, $\RatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ArithmeticSpectralStringCertificateUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,Q,L,U,Z,H,C,P,N$. It has no coordinate for a physical string model, host spectral operator, all-zero coverage theorem, radial scale reservoir, or completed zeta proof.
\end{definition}

\begin{theorem}[ArithmeticSpectralStringCertificate NameCert obligations]
\label{thm:arithmetic-spectral-string-certificate-namecert-obligations}
For every accepted $\ArithmeticSpectralStringCertificateUp$ carrier
$$
S=(M,Q,L,U,Z,H,C,P,N),
$$
the local $\NameCert_{\ArithmeticSpectralStringCertificateUp}$ surface consists exactly of finite normal-mode exposure through $M$, rational coefficient exposure through $Q$, prime-ledger exposure through $L$, unit-boundary exposure through $U$, zero-readout exposure through $Z$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arithmetic-spectral-string-certificate-carrier}. The rows $M,Q,L,U$ expose the finite mode packet, coefficient data, prime ledger, and unit-boundary discipline before the zero-readout row can be inspected.

The zero-readout row $Z$ is therefore a finite cancellation certificate, not a theorem that every accepted zeta zero has already been covered. The packet also has no carrier coordinate for a physical string model, host spectral operator, or radial scale reservoir.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. These rows exhaust the local NameCert obligations and leave global spectral coverage to separate source rows.
\end{proof}

\falsifiablePrediction{A completed formal route for $\ArithmeticSpectralStringCertificateUp$ must expose a finite normal-mode row, prime-ledger row, unit-boundary row, and zero-readout row before any RH-facing consumer can inspect the certificate; a purported proof that bypasses one of these rows is outside this NameCert surface.}

\independenceWitness{The packet is independent from the sibling surfaces prime, nat, rat, real, and zeta-zero-certificate-tower in the following sense: those chapters may supply source rows, but none of them alone supplies the combined finite normal-mode, prime-ledger, unit-boundary, and local NameCert packet carried by $\ArithmeticSpectralStringCertificateUp$.}

A Lean follow-up must provide $\mathrm{Nontrivial}$ and $\mathrm{FieldFaithful}$ instances for the carrier before the chapter can be promoted beyond statement-level paper evidence.

\closureat{ArithmeticSpectralStringCertificateUp}{seedStr}

\begin{closurestatus}{\ArithmeticSpectralStringCertificateUp}
  \constructivestory{An $\ArithmeticSpectralStringCertificateUp$ packet is generated from finite normal modes, rational coefficient rows, prime and Nat ledgers, unit-boundary rows, a zero-readout row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:arithmetic-spectral-string-certificate-carrier} and \autoref{thm:arithmetic-spectral-string-certificate-namecert-obligations} seed the arithmetic spectral string certificate route over finite modes, prime ledgers, unit-boundary data, and zero-readout rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not prove the Riemann hypothesis, construct all zeta-zero certificates, import a physical string model, supply a host spectral theorem, provide Lean verification, or bridge the packet to a public schema.}
  \upgradepath{Obligation closure requires checked finite-mode admission, prime-ledger admission, unit-boundary exposure, zero-readout exposure, transport, replay, provenance, local naming, and Nontrivial and FieldFaithful instances for BEDC.Derived.ArithmeticSpectralStringCertificateUp.}
\end{closurestatus}
