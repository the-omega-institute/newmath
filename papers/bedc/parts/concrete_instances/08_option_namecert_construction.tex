\input{parts/concrete_instances/option/01_nullable_option_namecert}
\input{parts/concrete_instances/option/02_tagged_option_namecert}
\input{parts/concrete_instances/option/02b_option_certificate_chains}
\input{parts/concrete_instances/option/03_tagged_option_right_visible_branch}
\input{parts/concrete_instances/option/04_tagged_option_nullable_bridge}
\input{parts/concrete_instances/option/05_tagged_option_branch_readback}
\input{parts/concrete_instances/option/06_tagged_option_payload_descent}
\input{parts/concrete_instances/option/07_tagged_option_payload_descent_image}
\input{parts/concrete_instances/option/08_tagged_option_payload_descent_image_classifier}
\input{parts/concrete_instances/option/08_option_namecert_construction_core}
\input{parts/concrete_instances/option/08_option_namecert_construction_readback}
\input{parts/concrete_instances/option/08_option_normalized_public_endpoint_readback}
\input{parts/concrete_instances/option/08_option_composite_image_classifier_public_branch}
\input{parts/concrete_instances/option/09_exclusive_classifier_branch_readback}
\input{parts/concrete_instances/option/10_payload_descent_image_classifier_public_package}
\input{parts/concrete_instances/option/10_payload_descent_image_classifier_readback_and_stability}
\input{parts/concrete_instances/option/11_payload_descent_image_classifier_visible_pair_coherence}
\input{parts/concrete_instances/option/12_composite_image_public_constructor_and_readback}
\input{parts/concrete_instances/option/13_composite_image_normalized_readback}
\input{parts/concrete_instances/option/14_composite_image_classifier_normalized_readback}
\input{parts/concrete_instances/option/15_composite_image_classifier_normalized_visible_classification}
\input{parts/concrete_instances/option/16_composite_image_classifier_normalized_selected_public_factorization}
\input{parts/concrete_instances/option/17_composite_image_classifier_normalized_one_sided_public_completion}
\input{parts/concrete_instances/option/18_composite_image_classifier_normalized_absent_public_factorization}

\section{Tagged option public endpoint visibility}
\label{sec:option-public-endpoint-visibility}

\begin{theorem}[Tagged option classifier endpoints expose visible certificate fields]
\label{thm:tagged-option-classifier-endpoint-visible-fields}
This theorem fits the ten-field template as follows: the traditional target is
public endpoint visibility for constructor-tagged $\OptionUp$; the BEDC source
is $\mathsf{BHist}$; the carrier is
$\mathsf{TaggedOptionHistoryCarrier}$; the classifier is
$\mathsf{TaggedOptionHistoryClassifier}$; the stability obligation is endpoint
carrier exposure plus visible-field projection; the ledger is visible
absent/present tag; the core theorem is the displayed endpoint field package;
exactness says classified endpoints expose public pattern and ledger witnesses;
the standard bridge is $\mathsf{none}$/$\mathsf{some}$ endpoint visibility; and
the status is $\mathsf{ProofTarget}$.

For every BEDC history source carrier $S:\mathsf{BHist}\to\mathsf{Prop}$,
source classifier $\sim_S$, and histories $h,k$, if
$\mathsf{TaggedOptionHistoryClassifier}_{S,\sim_S}(h,k)$ holds, then
$$
\mathsf{TaggedOptionHistoryPattern}_S(h)
\land
\mathsf{TaggedOptionHistoryLedger}_S(h)
\land
\mathsf{TaggedOptionHistoryPattern}_S(k)
\land
\mathsf{TaggedOptionHistoryLedger}_S(k).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:tagged-option-history-classifier-endpoint-carriers} to
the classifier witness. This gives the endpoint carrier witnesses
$\mathsf{TaggedOptionHistoryCarrier}_S(h)$ and
$\mathsf{TaggedOptionHistoryCarrier}_S(k)$.

Apply
Theorem~\ref{thm:tagged-option-history-carrier-pattern-ledger-equivalence} to
the carrier witness for $h$ and project the pattern and ledger components.
Apply the same theorem to the carrier witness for $k$ and project the same two
visible certificate fields. The four projected witnesses assemble into the
displayed conjunction.
\end{proof}

\section{Composite image classifier normalized factored public completion}
\label{sec:option-composite-image-classifier-normalized-factored-public-completion}

\begin{theorem}[Tagged option composite image classifier normalized factored public readback completion]
\label{thm:tagged-option-composite-image-classifier-normalized-factored-public-readback-completion}
Let $S,T,U:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source,
intermediate, and target carriers, let $\sim_S$, $\sim_T$, and $\sim_U$ be their
classifiers, and let
$$
\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)
$$
and
$$
\epsilon:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_T,\sim_U).
$$
Assume payload preservation fields
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a))
$$
and
$$
\rho_{\epsilon}:\forall b.\;T(b)\to U(\epsilon.\mathsf{map}(b)).
$$
Assume endpoint stability for the second descent:
$$
\eta_{\epsilon}:\forall x\,y.\;
T(x)\to T(y)\to \hsame(x,y)\to
\hsame(\epsilon.\mathsf{map}(x),\epsilon.\mathsf{map}(y)).
$$
Write $\epsilon\circ\delta$ for the descent certificate whose map field is
$a\mapsto\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and whose respects field
sends $r:a\sim_S b$ to
$\epsilon.\mathsf{respects}(\delta.\mathsf{respects}(r))$. Define
$$
\rho_{\epsilon\circ\delta}(a,Sa)=
\rho_{\epsilon}(\delta.\mathsf{map}(a),\rho_{\delta}(a,Sa)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$,
$\mathsf{cert}_T:\NameCert(T,\sim_T)$, and
$\mathsf{cert}_U:\NameCert(U,\sim_U)$. Assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$,
$\mathsf{TaggedOptionSourceHsameCompatible}(T,\sim_T)$, and
$\mathsf{TaggedOptionSourceHsameCompatible}(U,\sim_U)$. For final endpoints
$m,m':\mathsf{BHist}$ and normalized endpoints $u,u':\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\epsilon\circ\delta}
(S,U,\rho_{\epsilon\circ\delta};m,m')
$$
and
$$
\hsame(m,u)\land \hsame(m',u'),
$$
then either
\begin{enumerate}
\item $\hsame(u,\emp)\land \hsame(u',\emp)$;
\item $\neg\exists x,y:\mathsf{BHist}.\;
U(x)\land U(y)\land
\hsame(u,\Eone(x))\land
\hsame(u',\Eone(y))$;
\item there exist $k,k':\mathsf{BHist}$ such that
\begin{enumerate}
\item $\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k')$;
\item $\mathsf{TaggedOptionMapRel}_{\epsilon}(T,U,\rho_{\epsilon};k,u)$ and
$\mathsf{TaggedOptionMapRel}_{\epsilon}(T,U,\rho_{\epsilon};k',u')$;
\item $\hsame(k,\emp)\land \hsame(k',\emp)$;
\item $\neg\exists p,q:\mathsf{BHist}.\;
T(p)\land T(q)\land
\hsame(k,\Eone(p))\land
\hsame(k',\Eone(q))$.
\end{enumerate}
\end{enumerate}
or there exist $x,y,k,k',p,q,a,b:\mathsf{BHist}$ such that
\begin{enumerate}
\item $U(x)$, $U(y)$, $\hsame(u,\Eone(x))$, $\hsame(u',\Eone(y))$, and
$x\sim_U y$;
\item $\neg\hsame(u,\emp)$ and $\neg\hsame(u',\emp)$;
\item $\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k')$;
\item $\mathsf{TaggedOptionMapRel}_{\epsilon}(T,U,\rho_{\epsilon};k,u)$ and
$\mathsf{TaggedOptionMapRel}_{\epsilon}(T,U,\rho_{\epsilon};k',u')$;
\item $T(p)$, $T(q)$, $U(\epsilon.\mathsf{map}(p))$, and
$U(\epsilon.\mathsf{map}(q))$;
\item $\hsame(k,\Eone(p))$, $\hsame(k',\Eone(q))$,
$\hsame(x,\epsilon.\mathsf{map}(p))$, and
$\hsame(y,\epsilon.\mathsf{map}(q))$;
\item $S(a)$, $S(b)$, $a\sim_S b$, $T(\delta.\mathsf{map}(a))$, and
$T(\delta.\mathsf{map}(b))$;
\item $\hsame(p,\delta.\mathsf{map}(a))$ and
$\hsame(q,\delta.\mathsf{map}(b))$;
\item $p\sim_T q$;
\item for all $x',y':\mathsf{BHist}$, if $U(x')$, $U(y')$,
$\hsame(u,\Eone(x'))$, and $\hsame(u',\Eone(y'))$, then
$\hsame(x,x')\land \hsame(y,y')\land x'\sim_U y'$;
\item for every $x_L:\mathsf{BHist}$, if $U(x_L)$ and
$\hsame(u,\Eone(x_L))$, then $\hsame(x,x_L)$ and $x_L\sim_U y$;
\item for every $y_R:\mathsf{BHist}$, if $U(y_R)$ and
$\hsame(u',\Eone(y_R))$, then $\hsame(y,y_R)$ and $x\sim_U y_R$.
\end{enumerate}
\end{theorem}
\begin{proof}
Apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-public-readback-completion-package}
to the composite image-classifier witness, the endpoint normalization witnesses,
and the displayed descent, preservation, stability, certificate, and
compatibility data. This gives an exhaustive absent-or-present public readback
completion at $(u,u')$.

In the absent branch, retain the two normalized empty presentations and the
exclusion of every visible $U$-payload pair at $(u,u')$. Apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-absent-public-factorization}
to the same composite witness, endpoint normalizations, displayed data, and
absent endpoint presentations. This supplies intermediate endpoints $k,k'$ with
the $\delta$ classifier factor, the two $\epsilon$ public map-relation links,
the intermediate absent presentations, and the excluded intermediate visible
pair. Package these data as the absent factored branch.

In the present branch, retain the selected payloads $x,y$, their visible target
presentations, the target classification $x\sim_U y$, the two non-absence facts,
and the universal competing visible-pair uniqueness clause. Apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-selected-visible-public-factorization}
to the same composite witness, endpoint normalizations, displayed data, and
selected visible presentation. This supplies $k,k',p,q,a,b$ with the complete
selected visible public factorization and the one-sided competing-payload
uniqueness clauses. Package those witnesses together with the retained
completion uniqueness clause.
\end{proof}
\leanchecked{BEDC.Derived.OptionUp.TaggedOptionPayloadDescentImageClassifier\_composite\_normalized\_one\_sided\_visible\_public\_factorization}
