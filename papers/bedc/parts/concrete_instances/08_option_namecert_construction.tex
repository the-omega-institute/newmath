\input{parts/concrete_instances/option/01_nullable_option_namecert.tex}
\input{parts/concrete_instances/option/02_tagged_option_namecert.tex}
\input{parts/concrete_instances/option/02b_option_certificate_chains.tex}
\input{parts/concrete_instances/08_option_right_visible_branch_inversion.tex}
\input{parts/concrete_instances/option/08_tagged_option_payload_descent_image_classifier.tex}

\begin{theorem}[Tagged option payload descent image classifier mixed visible-tag separation]
\label{thm:tagged-option-payload-descent-image-classifier-mixed-visible-tag-separation}
This theorem fits the per-object template as follows: the traditional target is
image option no-confusion under payload descent; the BEDC source is
$\mathsf{BHist}$ with a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligation is
branch exactness plus generated-history no-confusion; the ledger is the visible
absent/present target tag; the core theorem is the two mixed-case
contradictions; exactness permits only absent/absent or present/present target
pairs; the standard bridge is $\mathsf{none}$/$\mathsf{some}$ under payload
descent; and the status is $\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, let
$\sim_S$ and $\sim_T$ be source classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}:\NameCert(S,\sim_S)$, assume source compatibility
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$, let
$k,k':\mathsf{BHist}$ be target endpoints, and let $u,v:\mathsf{BHist}$ be
target payloads. Both mixed visible-tag clauses hold:
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k')\to
\hsame(k,\emp)\to T(v)\to \hsame(k',\Eone(v))\to\bot
$$
and
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k')\to
T(u)\to \hsame(k,\Eone(u))\to \hsame(k',\emp)\to\bot.
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-branch-exactness}
to the image-classifier witness. Branch exactness leaves only the absent/absent
target pair or a transported present/present target pair.

For the clause with $\hsame(k,\emp)$ and $\hsame(k',\Eone(v))$, the
absent/absent branch contradicts the displayed present endpoint on the right by
empty-versus-$\Eone$ no-confusion. The present/present branch contradicts the
displayed absent endpoint on the left by the same no-confusion principle.

For the clause with $\hsame(k,\Eone(u))$ and $\hsame(k',\emp)$, the
absent/absent branch contradicts the displayed present endpoint on the left.
The present/present branch contradicts the displayed absent endpoint on the
right. These two contradictions exclude all cases supplied by branch exactness.
\end{proof}
\leanchecked{BEDC.Derived.OptionUp.TaggedOptionPayloadDescentImageClassifier\_mixed\_visible\_tag\_separation}
\begin{theorem}[Tagged option payload descent image classifier nonempty present-pair inversion]
\label{thm:tagged-option-payload-descent-image-classifier-nonempty-present-pair-inversion}
This theorem fits the per-object template as follows: the traditional target is
nonempty option image branch inversion; the BEDC source is $\mathsf{BHist}$ with
a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligation is
branch exactness plus endpoint non-absence; the ledger is the synchronized
transported payload pair; the core theorem is present-pair recovery; exactness is
elimination of the absent pair; the standard bridge is $\mathsf{none}$/$\mathsf{some}$
under payload descent; and the status is $\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, let
$\sim_S$ and $\sim_T$ be source classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}:\NameCert(S,\sim_S)$ and assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$. For target endpoints
$k,k':\mathsf{BHist}$, endpoint non-absence on either side forces the
synchronized transported present pair. The left non-absence clause is
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k')
\to
\neg\hsame(k,\emp)
\to
\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(k,\Eone(\delta.\mathsf{map}(a)))\land
\hsame(k',\Eone(\delta.\mathsf{map}(b))).
$$
The right non-absence clause is
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k')
\to
\neg\hsame(k',\emp)
\to
\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(k,\Eone(\delta.\mathsf{map}(a)))\land
\hsame(k',\Eone(\delta.\mathsf{map}(b))).
$$
\end{theorem}
\leanchecked{BEDC.Derived.OptionUp.TaggedOptionPayloadDescentImageClassifier\_nonempty\_present\_pair\_inversion}
\begin{proof}
Apply Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-branch-exactness}
to the image-classifier witness. Branch exactness supplies either the
absent/absent target pair or an exposed transported present/present target pair.

For the left non-absence clause, the absent/absent branch contains
$\hsame(k,\emp)$ and therefore contradicts the premise $\neg\hsame(k,\emp)$.
The present/present branch already contains payloads $a,b$ with $S(a)$, $S(b)$,
$a\sim_S b$, the two transported target-carrier witnesses, and the displayed
present endpoint presentations. Return those witnesses.

For the right non-absence clause, the same branch exactness split applies. The
absent/absent branch contains $\hsame(k',\emp)$ and contradicts the premise
$\neg\hsame(k',\emp)$, while the present/present branch is exactly the required
synchronized transported payload pair.
\end{proof}
\begin{theorem}[Tagged option payload descent image classifier visible target-payload classification]
\label{thm:tagged-option-payload-descent-image-classifier-visible-target-payload-classification}
This theorem fits the per-object template as follows: the traditional target is
target-side payload classification for a visible option image pair; the BEDC
source is $\mathsf{BHist}$ with a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligation is
visible payload readback plus descent respect; the ledger is the displayed
target present payload pair; the core theorem is target-payload classifier
recovery; exactness supplies transported source payloads behind the visible
targets; the standard bridge is $\mathsf{none}$/$\mathsf{some}$ under payload
descent; and the status is $\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history carriers, let
$\sim_S$ and $\sim_T$ be source and target classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$ and
$\mathsf{cert}_T:\NameCert(T,\sim_T)$. Assume source compatibility
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$ and target compatibility
$\mathsf{TaggedOptionSourceHsameCompatible}(T,\sim_T)$. For target endpoints
$k,k':\mathsf{BHist}$ and visible target payloads $u,v:\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k'),
\qquad
T(u),
\qquad
T(v),
\qquad
\hsame(k,\Eone(u)),
\qquad
\hsame(k',\Eone(v)),
$$
then $u\sim_T v$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-visible-payload-readback}
to the image-classifier witness and the two visible present-payload
presentations. This produces source payloads $a,b$ with $S(a)$, $S(b)$, and
$a\sim_S b$, together with target carrier witnesses for
$\delta.\mathsf{map}(a)$ and $\delta.\mathsf{map}(b)$ and the visible payload
readbacks $\hsame(u,\delta.\mathsf{map}(a))$ and
$\hsame(v,\delta.\mathsf{map}(b))$.

Use the $\mathsf{respects}$ field of $\delta$ on $a\sim_S b$ to obtain
$\delta.\mathsf{map}(a)\sim_T\delta.\mathsf{map}(b)$. Target compatibility turns
$T(u)$, $T(\delta.\mathsf{map}(a))$, and
$\hsame(u,\delta.\mathsf{map}(a))$ into
$u\sim_T\delta.\mathsf{map}(a)$; it also turns $T(v)$,
$T(\delta.\mathsf{map}(b))$, and $\hsame(v,\delta.\mathsf{map}(b))$ into
$v\sim_T\delta.\mathsf{map}(b)$.

Apply symmetry from $\mathsf{cert}_T$ to the second target-compatibility
relation, giving $\delta.\mathsf{map}(b)\sim_T v$. Transitivity from
$\mathsf{cert}_T$ first composes
$u\sim_T\delta.\mathsf{map}(a)$ with
$\delta.\mathsf{map}(a)\sim_T\delta.\mathsf{map}(b)$, and then composes the
result with $\delta.\mathsf{map}(b)\sim_T v$. The conclusion is $u\sim_T v$.
\end{proof}
\leanchecked{BEDC.Derived.OptionUp.TaggedOptionPayloadDescentImageClassifier\_visible\_target\_payload\_classification}
\input{parts/concrete_instances/option/09_exclusive_classifier_branch_readback.tex}
\input{parts/concrete_instances/option/10_payload_descent_image_classifier_public_package.tex}
\input{parts/concrete_instances/option/10_payload_descent_image_classifier_readback_and_stability.tex}

\section{Payload descent image classifier transported visible readback}
\label{sec:option-image-classifier-transported-visible-readback}

\begin{theorem}[Tagged option payload descent image classifier transported visible payload readback]
\label{thm:tagged-option-payload-descent-image-classifier-transported-visible-payload-readback}
Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, let
$\sim_S$ and $\sim_T$ be source classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}:\NameCert(S,\sim_S)$ and assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$. For target endpoints
$k,k',u,u':\mathsf{BHist}$ and visible target payloads $x,y:\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k'),
\qquad
\hsame(k,u),
\qquad
\hsame(k',u'),
$$
and
$$
T(x)\land T(y)\land
\hsame(u,\Eone(x))\land
\hsame(u',\Eone(y)),
$$
then
$$
\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(x,\delta.\mathsf{map}(a))\land
\hsame(y,\delta.\mathsf{map}(b)).
$$
\end{theorem}
\begin{proof}
Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-hsame-transport}
to the classifier witness at $(k,k')$ and to the two endpoint-normalizing
witnesses $\hsame(k,u)$ and $\hsame(k',u')$. This gives the same image
classifier at the normalized endpoint pair $(u,u')$.

Now apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-visible-payload-readback}
to that transported classifier and to the displayed visible payload data
$T(x)$, $T(y)$, $\hsame(u,\Eone(x))$, and $\hsame(u',\Eone(y))$. The returned
source histories $a,b$ carry $S(a)$, $S(b)$, $a\sim_S b$, the transported target
carrier witnesses, and the two payload readback witnesses. These are exactly the
fields of the required existential conclusion.
\end{proof}

\begin{theorem}[Tagged option payload descent image classifier transported visible payload pair coherence]
\label{thm:tagged-option-payload-descent-image-classifier-transported-visible-payload-pair-coherence}
This theorem fits the per-object template as follows: the traditional target is
transported visible readback coherence for constructor-tagged
$\mathsf{OptionUp}$ under payload descent; the BEDC source is $\mathsf{BHist}$
with a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligation
is endpoint transport plus visible endpoint determinacy; the ledger is the two
visible target payload pairs after endpoint normalization; the core theorem is
shared source-pair coherence; exactness says one transported image-classifier
witness explains both visible presentations; the standard bridge is
$\mathsf{none}$/$\mathsf{some}$ payload readback; and the status is
$\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, let
$\sim_S$ and $\sim_T$ be source classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}:\NameCert(S,\sim_S)$ and assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$. For target endpoints
$k,k',u,u':\mathsf{BHist}$ and visible target payloads
$x,y,x',y':\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,k'),
\qquad
\hsame(k,u),
\qquad
\hsame(k',u'),
$$
and
$$
T(x)\land T(y)\land
\hsame(u,\Eone(x))\land
\hsame(u',\Eone(y)),
$$
and
$$
T(x')\land T(y')\land
\hsame(u,\Eone(x'))\land
\hsame(u',\Eone(y')),
$$
then
$$
\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(x,\delta.\mathsf{map}(a))\land
\hsame(y,\delta.\mathsf{map}(b))\land
\hsame(x',\delta.\mathsf{map}(a))\land
\hsame(y',\delta.\mathsf{map}(b)).
$$
\end{theorem}
\begin{proof}
First transport the image-classifier witness. Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-hsame-transport}
to the classifier at $(k,k')$ and to the endpoint witnesses $\hsame(k,u)$ and
$\hsame(k',u')$. This yields the transported image classifier at the normalized
endpoint pair $(u,u')$.

Next apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-visible-payload-readback}
to the transported classifier and the first visible pair
$T(x)$, $T(y)$, $\hsame(u,\Eone(x))$, and $\hsame(u',\Eone(y))$. The resulting
source histories $a,b$ carry $S(a)$, $S(b)$, $a\sim_S b$, the two target carrier
witnesses, and the readback witnesses $\hsame(x,\delta.\mathsf{map}(a))$ and
$\hsame(y,\delta.\mathsf{map}(b))$.

Compare the two visible presentations of the left normalized endpoint $u$.
Apply
Theorem~\ref{thm:tagged-option-history-present-payload-determinism}
to $\hsame(u,\Eone(x))$ and $\hsame(u,\Eone(x'))$ to obtain $\hsame(x',x)$.
Composing this with $\hsame(x,\delta.\mathsf{map}(a))$ gives
$\hsame(x',\delta.\mathsf{map}(a))$.

Apply the same visible-payload determinacy at the right normalized endpoint
$u'$. The witnesses $\hsame(u',\Eone(y))$ and $\hsame(u',\Eone(y'))$ give
$\hsame(y',y)$, and composition with $\hsame(y,\delta.\mathsf{map}(b))$ gives
$\hsame(y',\delta.\mathsf{map}(b))$.

The original source pair $a,b$, together with the first readback witnesses and
the two transported primed readback witnesses, forms the required shared
source-pair witness.
\end{proof}
\input{parts/concrete_instances/option/11_payload_descent_image_classifier_visible_pair_coherence.tex}

\section{Payload descent image classifier reflective visible pair coherence}
\label{sec:option-payload-descent-image-reflective-visible-pair-coherence}

\begin{theorem}[Tagged option payload descent image classifier reflective transitive visible payload pair coherence]
\label{thm:tagged-option-payload-descent-image-classifier-reflective-transitive-visible-pair-coherence}
This theorem fits the per-object template as follows: the traditional target is
reflective transitive visible readback coherence for constructor-tagged
$\mathsf{OptionUp}$ under payload descent; the BEDC source is $\mathsf{BHist}$
with a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligation
is reflective transitivity plus visible endpoint determinacy; the ledger is the
two displayed target payload pairs; the core theorem is the shared-source-pair
coherence below; exactness says a transitive visible image-classifier path has a
single source payload pair for both visible presentations; the standard bridge
is $\mathsf{none}$/$\mathsf{some}$ payload readback; and the status is
$\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, let
$\sim_S$ and $\sim_T$ be source classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}:\NameCert(S,\sim_S)$, assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$, and assume
$$
\mathsf{TaggedOptionPayloadDescentReflectsSource}_{\delta}(S,\sim_S).
$$
For target endpoints $k,\ell,m:\mathsf{BHist}$ and visible target payloads
$u,v,u',v':\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,\ell)
$$
and
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};\ell,m)
$$
and
$$
T(u)\land T(v)\land
\hsame(k,\Eone(u))\land
\hsame(m,\Eone(v))
$$
and
$$
T(u')\land T(v')\land
\hsame(k,\Eone(u'))\land
\hsame(m,\Eone(v')),
$$
then
$$
\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(u,\delta.\mathsf{map}(a))\land
\hsame(v,\delta.\mathsf{map}(b))\land
\hsame(u',\delta.\mathsf{map}(a))\land
\hsame(v',\delta.\mathsf{map}(b)).
$$
\end{theorem}
\leanchecked{BEDC.Derived.OptionUp.TaggedOptionPayloadDescentImageClassifier\_reflective\_transitive\_visible\_pair\_coherence}
\begin{proof}
First combine the two image-classifier witnesses. Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-reflective-transitivity}
to the witnesses at $(k,\ell)$ and $(\ell,m)$, using the source hsame
compatibility and source-reflection hypotheses. This gives the direct
image-classifier witness from $k$ to $m$.

Next apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-visible-payload-readback}
to the direct witness and the first visible target payload pair. The result is a
source pair $a,b$ with $S(a)$, $S(b)$, $a\sim_S b$, transported target carrier
facts, and the readback identifications
$\hsame(u,\delta.\mathsf{map}(a))$ and
$\hsame(v,\delta.\mathsf{map}(b))$.

It remains to compare the primed visible presentation with the unprimed one.
At the left endpoint $k$, the two witnesses present $k$ as both $\Eone(u)$ and
$\Eone(u')$; $\Eone$ no-confusion, symmetry of $\hsame$, and transitivity of
$\hsame$ give $\hsame(u',\delta.\mathsf{map}(a))$. At the right endpoint $m$,
the same argument applied to $\Eone(v)$ and $\Eone(v')$ gives
$\hsame(v',\delta.\mathsf{map}(b))$. Repack the original source pair together
with these four payload identifications.
\end{proof}

\begin{theorem}[Tagged option payload descent image classifier reflective transitive transported visible payload pair coherence]
\label{thm:tagged-option-payload-descent-image-classifier-reflective-transitive-transported-visible-pair-coherence}
This theorem fits the per-object template as follows: the traditional target is
endpoint-normalized two-step readback coherence for constructor-tagged
$\mathsf{OptionUp}$ under payload descent; the BEDC source is $\mathsf{BHist}$
with a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligations
are reflective transitivity, $\hsame$ endpoint transport, and visible endpoint
determinacy; the ledger is the two normalized visible payload presentations; the
core theorem is that one source payload pair explains both visible
presentations; exactness says endpoint normalization does not change the
two-step readback; the standard bridge is $\mathsf{none}$/$\mathsf{some}$
payload readback; and the status is $\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, let
$\sim_S$ and $\sim_T$ be source classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}:\NameCert(S,\sim_S)$, assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$, and assume
$$
\mathsf{TaggedOptionPayloadDescentReflectsSource}_{\delta}(S,\sim_S).
$$
For target endpoints $k,\ell,m,p,q:\mathsf{BHist}$ and visible target payloads
$u,v,u',v':\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,\ell)
$$
and
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};\ell,m)
$$
and
$$
\hsame(k,p)\land \hsame(m,q)
$$
and
$$
T(u)\land T(v)\land
\hsame(p,\Eone(u))\land
\hsame(q,\Eone(v))
$$
and
$$
T(u')\land T(v')\land
\hsame(p,\Eone(u'))\land
\hsame(q,\Eone(v')),
$$
then
$$
\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(u,\delta.\mathsf{map}(a))\land
\hsame(v,\delta.\mathsf{map}(b))\land
\hsame(u',\delta.\mathsf{map}(a))\land
\hsame(v',\delta.\mathsf{map}(b)).
$$
\end{theorem}
\begin{proof}
First combine the two image-classifier witnesses. Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-reflective-transitivity}
to the witnesses at $(k,\ell)$ and $(\ell,m)$, together with source
compatibility and source reflection, to obtain the direct image classifier from
$k$ to $m$.

Next normalize the endpoints. Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-hsame-transport}
to the direct classifier and the endpoint witnesses $\hsame(k,p)$ and
$\hsame(m,q)$. This yields the same image-classifier witness at the normalized
endpoint pair $(p,q)$.

Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-visible-payload-readback}
to the normalized classifier and the first visible payload presentation at
$(p,q)$. It returns source histories $a,b$ with $S(a)$, $S(b)$, $a\sim_S b$,
target carrier facts for $\delta.\mathsf{map}(a)$ and $\delta.\mathsf{map}(b)$,
and the readbacks $\hsame(u,\delta.\mathsf{map}(a))$ and
$\hsame(v,\delta.\mathsf{map}(b))$.

It remains to compare the second normalized visible presentation with the first.
At endpoint $p$, apply
Theorem~\ref{thm:tagged-option-history-present-payload-determinism} to
$\hsame(p,\Eone(u))$ and $\hsame(p,\Eone(u'))$. By symmetry and transitivity of
$\hsame$, the resulting one-constructor inversion gives
$\hsame(u',\delta.\mathsf{map}(a))$.

At endpoint $q$, the same visible endpoint determinacy applied to
$\hsame(q,\Eone(v))$ and $\hsame(q,\Eone(v'))$ gives
$\hsame(v',v)$. Composing this with $\hsame(v,\delta.\mathsf{map}(b))$ yields
$\hsame(v',\delta.\mathsf{map}(b))$.

Repack the original source pair $a,b$ with the two source-carrier witnesses, the
source classifier relation, the transported target-carrier witnesses, and the
four visible payload readbacks. These are precisely the fields of the displayed
existential conclusion.
\end{proof}

\begin{theorem}[Tagged option payload descent image classifier reflective transitive visible target-payload classification]
\label{thm:tagged-option-payload-descent-image-classifier-reflective-transitive-visible-target-payload-classification}
This theorem fits the per-object template as follows: the traditional target is
target two-step image option target-payload classification; the BEDC source is
$\mathsf{BHist}$ with a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligation
is reflective transitivity plus target classifier descent; the ledger is the
two visible outer target payloads; the core theorem is the displayed
$u\sim_T v$; exactness is direct classifier recovery after composing the path;
the standard bridge is $\mathsf{none}$/$\mathsf{some}$ payload classification;
and the status is $\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history carriers, let
$\sim_S$ and $\sim_T$ be source and target classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$ and
$\mathsf{cert}_T:\NameCert(T,\sim_T)$. Assume source compatibility
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$ and target compatibility
$\mathsf{TaggedOptionSourceHsameCompatible}(T,\sim_T)$. Assume also
$$
\mathsf{TaggedOptionPayloadDescentReflectsSource}_{\delta}(S,\sim_S).
$$
For $k,\ell,m,u,v:\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,\ell)
$$
and
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};\ell,m)
$$
and
$$
T(u),
\qquad
T(v),
\qquad
\hsame(k,\Eone(u)),
\qquad
\hsame(m,\Eone(v)),
$$
then $u\sim_T v$.
\end{theorem}
\begin{proof}
First compose the visible image path. Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-reflective-transitivity}
to the two image-classifier witnesses, using source hsame compatibility and
source reflection. This gives a direct image-classifier witness from $k$ to
$m$.

Now apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-visible-target-payload-classification}
to the direct witness, the two target carrier witnesses $T(u)$ and $T(v)$, and
the visible endpoint presentations $\hsame(k,\Eone(u))$ and
$\hsame(m,\Eone(v))$. The conclusion is exactly $u\sim_T v$.
\end{proof}

\begin{theorem}[Tagged option payload descent image classifier reflective transitive pair readback is single-valued]
\label{thm:tagged-option-payload-descent-image-classifier-reflective-transitive-pair-readback-single-valued}
This theorem fits the per-object template as follows: the traditional target is
reflective two-step image option readback for constructor-tagged
$\mathsf{OptionUp}$ under payload descent; the BEDC source is $\mathsf{BHist}$
with a $\mathsf{DescentCertificate}$; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligation
is reflective transitivity plus paired branch exactness; the ledger is the outer
absent tag or transported outer present pair; the core theorem is the complete
absent/present split below; exactness says the outer endpoints inherit direct
pair readback; the standard bridge is $\mathsf{none}$/$\mathsf{some}$ payload
readback; and the status is $\mathsf{ProofTarget}$.

Let $S,T:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, let
$\sim_S$ and $\sim_T$ be source classifiers, let
$\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)$,
and assume payload preservation
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a)).
$$
Let $\mathsf{cert}:\NameCert(S,\sim_S)$, assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$, and assume
$$
\mathsf{TaggedOptionPayloadDescentReflectsSource}_{\delta}(S,\sim_S).
$$
For target endpoints $k,\ell,m:\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};k,\ell)
$$
and
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\delta}
(S,T,\rho_{\delta};\ell,m)
$$
hold, then either
$$
\hsame(k,\emp)\land
\hsame(m,\emp)\land
\neg\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(k,\Eone(\delta.\mathsf{map}(a)))\land
\hsame(m,\Eone(\delta.\mathsf{map}(b)))
$$
or
$$
\exists a,b:\mathsf{BHist}.\;
S(a)\land S(b)\land a\sim_S b\land
T(\delta.\mathsf{map}(a))\land T(\delta.\mathsf{map}(b))\land
\hsame(k,\Eone(\delta.\mathsf{map}(a)))\land
\hsame(m,\Eone(\delta.\mathsf{map}(b)))\land
\neg\hsame(k,\emp)\land
\neg\hsame(m,\emp)\land
\bigl(\forall a',b':\mathsf{BHist}.\;
S(a')\to S(b')\to a'\sim_S b'\to
T(\delta.\mathsf{map}(a'))\to T(\delta.\mathsf{map}(b'))\to
\hsame(k,\Eone(\delta.\mathsf{map}(a')))\to
\hsame(m,\Eone(\delta.\mathsf{map}(b')))\to
\hsame(\delta.\mathsf{map}(a),\delta.\mathsf{map}(a'))\land
\hsame(\delta.\mathsf{map}(b),\delta.\mathsf{map}(b'))\bigr).
$$
\end{theorem}
\begin{proof}
First combine the two image-classifier witnesses. Apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-reflective-transitivity}
to the witnesses at $(k,\ell)$ and $(\ell,m)$, using the source hsame
compatibility and source-reflection hypotheses. This gives the direct
image-classifier witness from $k$ to $m$.

Now apply
Theorem~\ref{thm:tagged-option-payload-descent-image-classifier-pair-readback-single-valued}
to the direct witness at the outer endpoint pair $(k,m)$. Its absent branch is
already the displayed $\hsame(k,\emp)$, $\hsame(m,\emp)$, and exclusion of every
transported present pair at $(k,m)$.

In the present branch, keep the source pair $a,b$, its source carrier witnesses,
the source classifier witness, the transported target carrier facts, the two
outer visible presentations, and the two non-absence clauses. The paired
single-valuedness clause is exactly the one returned by the direct pair
readback theorem for every competing transported pair at $(k,m)$, so the branch
repackages unchanged.
\end{proof}
