\input{parts/concrete_instances/option/01_nullable_option_namecert}
\input{parts/concrete_instances/option/02_tagged_option_namecert}
\input{parts/concrete_instances/option/02b_option_certificate_chains}
\input{parts/concrete_instances/option/03_tagged_option_right_visible_branch}
\input{parts/concrete_instances/option/04_tagged_option_nullable_bridge}
\input{parts/concrete_instances/option/05_tagged_option_branch_readback}
\input{parts/concrete_instances/option/06_tagged_option_payload_descent}
\input{parts/concrete_instances/option/07_tagged_option_payload_descent_image}
\input{parts/concrete_instances/option/08_tagged_option_payload_descent_image_classifier}
\input{parts/concrete_instances/option/08_option_namecert_construction_core}
\input{parts/concrete_instances/option/08_option_namecert_construction_readback}
\input{parts/concrete_instances/option/08_option_normalized_public_endpoint_readback}
\input{parts/concrete_instances/option/08_option_composite_image_classifier_public_branch}
\input{parts/concrete_instances/option/09_exclusive_classifier_branch_readback}
\input{parts/concrete_instances/option/10_payload_descent_image_classifier_public_package}
\input{parts/concrete_instances/option/10_payload_descent_image_classifier_readback_and_stability}
\input{parts/concrete_instances/option/11_payload_descent_image_classifier_visible_pair_coherence}
\input{parts/concrete_instances/option/12_composite_image_public_constructor_and_readback}
\input{parts/concrete_instances/option/13_composite_image_normalized_readback}
\input{parts/concrete_instances/option/14_composite_image_classifier_normalized_readback}

\section{Composite image classifier normalized visible target-payload classification}
\label{sec:option-composite-image-classifier-normalized-visible-target-classification}

\begin{theorem}[Tagged option composite image classifier normalized visible target-payload classification]
\label{thm:tagged-option-composite-image-classifier-normalized-visible-target-payload-classification}
This theorem fits the per-object template as follows: the traditional target is
endpoint-normalized visible target-payload classification for the composite
$\mathsf{OptionUp}$ image classifier; the BEDC source is $\mathsf{BHist}$ with
two composable $\mathsf{DescentCertificate}$ maps; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligations
are composite descent, endpoint $\hsame$ transport, visible-payload exactness,
descent respect, and target certificate transitivity; the ledger records
normalized visible endpoints and their target payload pair; the core theorem is
target-side classifier recovery; exactness supplies the source pair behind the
visible payloads; the standard bridge is $\mathsf{none}$/$\mathsf{some}$
visible readback; and the status is $\mathsf{ProofTarget}$.

Let $S,T,U:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source,
intermediate, and target carriers, let $\sim_S$, $\sim_T$, and $\sim_U$ be their
classifiers, and let
$$
\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)
$$
and
$$
\epsilon:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_T,\sim_U).
$$
Assume payload preservation fields
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a))
$$
and
$$
\rho_{\epsilon}:\forall b.\;T(b)\to U(\epsilon.\mathsf{map}(b)).
$$
Assume endpoint stability for the second descent:
$$
\eta_{\epsilon}:\forall x\,y.\;
T(x)\to T(y)\to \hsame(x,y)\to
\hsame(\epsilon.\mathsf{map}(x),\epsilon.\mathsf{map}(y)).
$$
Write $\epsilon\circ\delta$ for the descent certificate whose map field is
$a\mapsto\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and whose respects field
sends $r:a\sim_S b$ to
$\epsilon.\mathsf{respects}(\delta.\mathsf{respects}(r))$. Define
$$
\rho_{\epsilon\circ\delta}(a,Sa)=
\rho_{\epsilon}(\delta.\mathsf{map}(a),\rho_{\delta}(a,Sa)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$,
$\mathsf{cert}_T:\NameCert(T,\sim_T)$, and
$\mathsf{cert}_U:\NameCert(U,\sim_U)$. Assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$,
$\mathsf{TaggedOptionSourceHsameCompatible}(T,\sim_T)$, and
$\mathsf{TaggedOptionSourceHsameCompatible}(U,\sim_U)$. For final endpoints
$m,m':\mathsf{BHist}$, normalized endpoints $u,u':\mathsf{BHist}$, and visible
target payloads $x,y:\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\epsilon\circ\delta}
(S,U,\rho_{\epsilon\circ\delta};m,m')
$$
and
$$
\hsame(m,u)\land \hsame(m',u')
$$
and
$$
U(x)\land U(y)\land
\hsame(u,\Eone(x))\land \hsame(u',\Eone(y)),
$$
then $x\sim_U y$.
\end{theorem}
\begin{proof}
First apply the forward direction of
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-visible-payload-exactness}
to the composite image-classifier witness, the two endpoint normalization
witnesses, and the two visible target-payload presentations. This returns
source witnesses $a,b$ with $S(a)$, $S(b)$, and $a\sim_S b$, together with the
target carrier witnesses for
$\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and
$\epsilon.\mathsf{map}(\delta.\mathsf{map}(b))$ and the payload readbacks
$\hsame(x,\epsilon.\mathsf{map}(\delta.\mathsf{map}(a)))$ and
$\hsame(y,\epsilon.\mathsf{map}(\delta.\mathsf{map}(b)))$.

Use $\delta.\mathsf{respects}$ on $a\sim_S b$ to obtain
$\delta.\mathsf{map}(a)\sim_T\delta.\mathsf{map}(b)$. Then use
$\epsilon.\mathsf{respects}$ to derive
$\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))
\sim_U
\epsilon.\mathsf{map}(\delta.\mathsf{map}(b))$.

Target compatibility for $U$ turns the carrier facts for $x$ and
$\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and the first payload readback
into
$x\sim_U\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$. It also turns the
carrier facts for $y$ and $\epsilon.\mathsf{map}(\delta.\mathsf{map}(b))$ and
the second payload readback into
$y\sim_U\epsilon.\mathsf{map}(\delta.\mathsf{map}(b))$.

Apply symmetry from $\mathsf{cert}_U$ to the second target-compatibility
relation, giving
$\epsilon.\mathsf{map}(\delta.\mathsf{map}(b))\sim_U y$. Transitivity from
$\mathsf{cert}_U$ first composes
$x\sim_U\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ with the composite
descent relation, and then composes the result with
$\epsilon.\mathsf{map}(\delta.\mathsf{map}(b))\sim_U y$. The conclusion is
$x\sim_U y$.
\end{proof}
\leanchecked{BEDC.Derived.OptionUp.TaggedOptionPayloadDescentImageClassifier\_composite\_normalized\_visible\_target\_payload\_classification}

\begin{theorem}[Tagged option composite image classifier normalized visible payload determinacy]
\label{thm:tagged-option-composite-image-classifier-normalized-visible-payload-determinacy}
This theorem fits the per-object template as follows: the traditional target is
endpoint-normalized functorial $\mathsf{OptionUp}$ visible-payload determinacy;
the BEDC source is $\mathsf{BHist}$ with two composable
$\mathsf{DescentCertificate}$ maps; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligations
are composite descent, endpoint $\hsame$ transport, normalized target-payload
classification, $\Eone$ payload determinacy, and empty-versus-present
no-confusion; the ledger is the pair of normalized visible endpoints and two
visible target payload readings; the core theorem is the displayed
classification, determinacy, and non-absence conjunction; exactness says
normalized visible readback is single-valued and target-classified; the standard
bridge is $\mathsf{none}$/$\mathsf{some}$ normalized public readback; and the
status is $\mathsf{ProofTarget}$.

Let $S,T,U:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source,
intermediate, and target carriers, let $\sim_S$, $\sim_T$, and $\sim_U$ be their
classifiers, and let
$$
\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)
$$
and
$$
\epsilon:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_T,\sim_U).
$$
Assume payload preservation fields
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a))
$$
and
$$
\rho_{\epsilon}:\forall b.\;T(b)\to U(\epsilon.\mathsf{map}(b)).
$$
Assume endpoint stability for the second descent:
$$
\eta_{\epsilon}:\forall x\,y.\;
T(x)\to T(y)\to \hsame(x,y)\to
\hsame(\epsilon.\mathsf{map}(x),\epsilon.\mathsf{map}(y)).
$$
Write $\epsilon\circ\delta$ for the descent certificate whose map field is
$a\mapsto\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and whose respects field
sends $r:a\sim_S b$ to
$\epsilon.\mathsf{respects}(\delta.\mathsf{respects}(r))$. Define
$$
\rho_{\epsilon\circ\delta}(a,Sa)=
\rho_{\epsilon}(\delta.\mathsf{map}(a),\rho_{\delta}(a,Sa)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$,
$\mathsf{cert}_T:\NameCert(T,\sim_T)$, and
$\mathsf{cert}_U:\NameCert(U,\sim_U)$. Assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$,
$\mathsf{TaggedOptionSourceHsameCompatible}(T,\sim_T)$, and
$\mathsf{TaggedOptionSourceHsameCompatible}(U,\sim_U)$. For final endpoints
$m,m':\mathsf{BHist}$, normalized endpoints $u,u':\mathsf{BHist}$, and visible
target payloads $x,y,x',y':\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\epsilon\circ\delta}
(S,U,\rho_{\epsilon\circ\delta};m,m')
$$
and
$$
\hsame(m,u)\land \hsame(m',u')
$$
and
$$
U(x)\land U(y)\land
\hsame(u,\Eone(x))\land \hsame(u',\Eone(y))
$$
and
$$
U(x')\land U(y')\land
\hsame(u,\Eone(x'))\land \hsame(u',\Eone(y')),
$$
then
$$
x\sim_U y\land
x'\sim_U y'\land
\hsame(x,x')\land
\hsame(y,y')\land
\neg\hsame(u,\emp)\land
\neg\hsame(u',\emp).
$$
\end{theorem}
\begin{proof}
First classify the two normalized visible target payload pairs. Apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-visible-target-payload-classification}
to the composite image-classifier witness, the endpoint normalization witnesses,
and the presentation $U(x)$, $U(y)$, $\hsame(u,\Eone(x))$, and
$\hsame(u',\Eone(y))$. This gives $x\sim_U y$.

Apply the same theorem to the second visible presentation
$U(x')$, $U(y')$, $\hsame(u,\Eone(x'))$, and
$\hsame(u',\Eone(y'))$. This gives $x'\sim_U y'$.

Next compare the two readings of each normalized endpoint. Apply
Theorem~\ref{thm:tagged-option-history-present-payload-determinism} at $u$ to
$\hsame(u,\Eone(x))$ and $\hsame(u,\Eone(x'))$, giving $\hsame(x,x')$.
Apply the same theorem at $u'$ to $\hsame(u',\Eone(y))$ and
$\hsame(u',\Eone(y'))$, giving $\hsame(y,y')$.

It remains to rule out hidden absent tags at the normalized endpoints. If
$\hsame(u,\emp)$ held, composing its symmetry with $\hsame(u,\Eone(x))$ would
give $\hsame(\emp,\Eone(x))$, contradicting BEDC history no-confusion via
$not\_hsame\_emp\_e1$.

Likewise, if $\hsame(u',\emp)$ held, composing its symmetry with
$\hsame(u',\Eone(y))$ would give $\hsame(\emp,\Eone(y))$, again contradicting
BEDC history no-confusion via $not\_hsame\_emp\_e1$.

Collect the two target-classification conclusions, the two payload-determinacy
conclusions, and the two normalized non-absence conclusions in the displayed
conjunction.
\end{proof}
\leanchecked{BEDC.Derived.OptionUp.TaggedOptionPayloadDescentImageClassifier\_composite\_normalized\_visible\_payload\_determinacy}

\begin{theorem}[Tagged option composite image classifier normalized visible source-pair coherence]
\label{thm:tagged-option-composite-image-classifier-normalized-visible-source-pair-coherence}
Let $S,T,U:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source,
intermediate, and target carriers, let $\sim_S$, $\sim_T$, and $\sim_U$ be their
classifiers, and let
$$
\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)
$$
and
$$
\epsilon:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_T,\sim_U).
$$
Assume payload preservation fields
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a))
$$
and
$$
\rho_{\epsilon}:\forall b.\;T(b)\to U(\epsilon.\mathsf{map}(b)).
$$
Write $\epsilon\circ\delta$ for the descent certificate whose map field is
$a\mapsto\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and whose respects field
sends $r:a\sim_S b$ to
$\epsilon.\mathsf{respects}(\delta.\mathsf{respects}(r))$. Define
$$
\rho_{\epsilon\circ\delta}(a,Sa)=
\rho_{\epsilon}(\delta.\mathsf{map}(a),\rho_{\delta}(a,Sa)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$ and assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$. For final endpoints
$m,m':\mathsf{BHist}$, normalized endpoints $u,u':\mathsf{BHist}$, and visible
target payloads $x,y,x',y':\mathsf{BHist}$, assume
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\epsilon\circ\delta}
(S,U,\rho_{\epsilon\circ\delta};m,m')
$$
and
$$
\hsame(m,u)\land \hsame(m',u')
$$
and
$$
U(x)\land U(y)\land
\hsame(u,\Eone(x))\land \hsame(u',\Eone(y))
$$
and
$$
U(x')\land U(y')\land
\hsame(u,\Eone(x'))\land \hsame(u',\Eone(y')).
$$
Then there exist $a,b:\mathsf{BHist}$ such that
$$
S(a)\land S(b)\land a\sim_S b
$$
and
$$
U(\epsilon.\mathsf{map}(\delta.\mathsf{map}(a)))\land
U(\epsilon.\mathsf{map}(\delta.\mathsf{map}(b)))
$$
and
$$
\hsame(x,\epsilon.\mathsf{map}(\delta.\mathsf{map}(a)))\land
\hsame(y,\epsilon.\mathsf{map}(\delta.\mathsf{map}(b)))\land
\hsame(x',\epsilon.\mathsf{map}(\delta.\mathsf{map}(a)))\land
\hsame(y',\epsilon.\mathsf{map}(\delta.\mathsf{map}(b))).
$$
\end{theorem}
\begin{proof}
First apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-source-payload-exactness}
to the composite image-classifier witness, the endpoint normalizations, and the
unprimed visible presentation. The forward direction supplies source payloads
$a,b$ with $S(a)$, $S(b)$, $a\sim_S b$, the two composite target carrier
witnesses, and the readbacks
$\hsame(x,\epsilon.\mathsf{map}(\delta.\mathsf{map}(a)))$ and
$\hsame(y,\epsilon.\mathsf{map}(\delta.\mathsf{map}(b)))$.

Next compare the competing visible readings of the same normalized endpoints.
Apply Theorem~\ref{thm:tagged-option-history-present-payload-determinism} at
$u$ to $\hsame(u,\Eone(x))$ and $\hsame(u,\Eone(x'))$, and apply the same theorem
at $u'$ to $\hsame(u',\Eone(y))$ and $\hsame(u',\Eone(y'))$. These give
$\hsame(x,x')$ and $\hsame(y,y')$.

Use symmetry and transitivity of $\hsame$ to compose the endpoint-comparison
witnesses with the unprimed source readbacks. This yields
$\hsame(x',\epsilon.\mathsf{map}(\delta.\mathsf{map}(a)))$ and
$\hsame(y',\epsilon.\mathsf{map}(\delta.\mathsf{map}(b)))$.

Package the same source payloads $a,b$ with their source classification, the two
composite target carrier witnesses, and the four readback identifications.
\end{proof}

\begin{theorem}[Tagged option composite image classifier normalized selected visible public pair canonicality]
\label{thm:tagged-option-composite-image-classifier-normalized-selected-visible-public-pair-canonicality}
This theorem fits the per-object template as follows: the traditional target is
endpoint-normalized functorial $\mathsf{OptionUp}$ selected visible public-pair
canonicality; the BEDC source is $\mathsf{BHist}$ with two composable
$\mathsf{DescentCertificate}$ maps; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligations
are composite descent, endpoint $\hsame$ transport, normalized target-payload
classification, visible-payload determinacy, and empty-versus-present
no-confusion; the ledger is one selected normalized visible public pair
together with all one-sided competing readings; the core theorem is selected
pair classification, same-side uniqueness, cross-side target classification,
and non-absence; exactness says normalized visible readback is single-valued
around the selected public pair; the standard bridge is $\mathsf{none}$/
$\mathsf{some}$ normalized public readback; and the status is
$\mathsf{ProofTarget}$.

Let $S,T,U:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source,
intermediate, and target carriers, let $\sim_S$, $\sim_T$, and $\sim_U$ be their
classifiers, and let
$$
\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)
$$
and
$$
\epsilon:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_T,\sim_U).
$$
Assume payload preservation fields
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a))
$$
and
$$
\rho_{\epsilon}:\forall b.\;T(b)\to U(\epsilon.\mathsf{map}(b)).
$$
Assume endpoint stability for the second descent:
$$
\eta_{\epsilon}:\forall x\,y.\;
T(x)\to T(y)\to \hsame(x,y)\to
\hsame(\epsilon.\mathsf{map}(x),\epsilon.\mathsf{map}(y)).
$$
Write $\epsilon\circ\delta$ for the descent certificate whose map field is
$a\mapsto\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and whose respects field
sends $r:a\sim_S b$ to
$\epsilon.\mathsf{respects}(\delta.\mathsf{respects}(r))$. Define
$$
\rho_{\epsilon\circ\delta}(a,Sa)=
\rho_{\epsilon}(\delta.\mathsf{map}(a),\rho_{\delta}(a,Sa)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$,
$\mathsf{cert}_T:\NameCert(T,\sim_T)$, and
$\mathsf{cert}_U:\NameCert(U,\sim_U)$. Assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$,
$\mathsf{TaggedOptionSourceHsameCompatible}(T,\sim_T)$, and
$\mathsf{TaggedOptionSourceHsameCompatible}(U,\sim_U)$. For final endpoints
$m,m':\mathsf{BHist}$, normalized endpoints $u,u':\mathsf{BHist}$, and selected
visible target payloads $x,y:\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\epsilon\circ\delta}
(S,U,\rho_{\epsilon\circ\delta};m,m')
$$
and
$$
\hsame(m,u)\land \hsame(m',u')
$$
and
$$
U(x)\land U(y)\land
\hsame(u,\Eone(x))\land \hsame(u',\Eone(y)),
$$
then
$$
x\sim_U y
$$
and
$$
\forall x':\mathsf{BHist}.\;
U(x')\to
\hsame(u,\Eone(x'))\to
\hsame(x,x')\land x'\sim_U y
$$
and
$$
\forall y':\mathsf{BHist}.\;
U(y')\to
\hsame(u',\Eone(y'))\to
\hsame(y,y')\land x\sim_U y'
$$
and
$$
\neg\hsame(u,\emp)\land \neg\hsame(u',\emp).
$$
\end{theorem}
\begin{proof}
Apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-visible-target-payload-classification}
to the composite image-classifier witness, the endpoint normalization witnesses,
and the selected visible presentation $U(x)$, $U(y)$,
$\hsame(u,\Eone(x))$, and $\hsame(u',\Eone(y))$. This gives the selected target
classification $x\sim_U y$.

Let $x'$ be a competing left visible payload with $U(x')$ and
$\hsame(u,\Eone(x'))$. Apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-visible-payload-determinacy}
to the two complete visible pairs $(x,y)$ and $(x',y)$. The returned
conjunction contains $\hsame(x,x')$ and $x'\sim_U y$, which are exactly the
left-side canonicality and target-classification conclusions for $x'$.

Let $y'$ be a competing right visible payload with $U(y')$ and
$\hsame(u',\Eone(y'))$. Apply the same determinacy theorem to the two complete
visible pairs $(x,y)$ and $(x,y')$. The returned conjunction contains
$\hsame(y,y')$ and $x\sim_U y'$, which are exactly the right-side canonicality
and target-classification conclusions for $y'$.

It remains to record that the selected public endpoints are not absent. If
$\hsame(u,\emp)$ held, composing its symmetry with $\hsame(u,\Eone(x))$ would
give $\hsame(\emp,\Eone(x))$, contradicting BEDC history no-confusion via
$not\_hsame\_emp\_e1$.

Likewise, if $\hsame(u',\emp)$ held, composing its symmetry with
$\hsame(u',\Eone(y))$ would give $\hsame(\emp,\Eone(y))$, again contradicting
BEDC history no-confusion via $not\_hsame\_emp\_e1$.

Collect the selected target classification, the two universal one-sided
canonicality clauses, and the two non-absence conclusions.
\end{proof}

\begin{theorem}[Tagged option composite image classifier normalized one-sided visible public readback]
\label{thm:tagged-option-composite-image-classifier-normalized-one-sided-visible-public-readback}
This theorem fits the per-object template as follows: the traditional target is
endpoint-normalized functorial $\mathsf{OptionUp}$ one-sided visible public
readback; the BEDC source is $\mathsf{BHist}$ with two composable
$\mathsf{DescentCertificate}$ maps; the carrier is
$\mathsf{TaggedOptionPayloadDescentImageCarrier}$; the classifier is
$\mathsf{TaggedOptionPayloadDescentImageClassifier}$; the stability obligations
are endpoint transport, empty-versus-present no-confusion, normalized nonempty
public readback, and visible-payload determinacy; the ledger is one specified
normalized visible endpoint together with the matched classified payload on the
other endpoint; the core theorem is one-sided visible readback with uniqueness
against all competing visible payloads on the other side; exactness says the
public endpoint already read by a consumer determines the matching public
payload; the standard bridge is $\mathsf{none}$/$\mathsf{some}$ normalized
public readback; and the status is $\mathsf{ProofTarget}$.

Let $S,T,U:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source,
intermediate, and target carriers, let $\sim_S$, $\sim_T$, and $\sim_U$ be their
classifiers, and let
$$
\delta:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_S,\sim_T)
$$
and
$$
\epsilon:\mathsf{DescentCertificate}(\mathsf{BHist},\mathsf{BHist},\sim_T,\sim_U).
$$
Assume payload preservation fields
$$
\rho_{\delta}:\forall a.\;S(a)\to T(\delta.\mathsf{map}(a))
$$
and
$$
\rho_{\epsilon}:\forall b.\;T(b)\to U(\epsilon.\mathsf{map}(b)).
$$
Assume endpoint stability for the second descent:
$$
\eta_{\epsilon}:\forall x\,y.\;
T(x)\to T(y)\to \hsame(x,y)\to
\hsame(\epsilon.\mathsf{map}(x),\epsilon.\mathsf{map}(y)).
$$
Write $\epsilon\circ\delta$ for the descent certificate whose map field is
$a\mapsto\epsilon.\mathsf{map}(\delta.\mathsf{map}(a))$ and whose respects field
sends $r:a\sim_S b$ to
$\epsilon.\mathsf{respects}(\delta.\mathsf{respects}(r))$. Define
$$
\rho_{\epsilon\circ\delta}(a,Sa)=
\rho_{\epsilon}(\delta.\mathsf{map}(a),\rho_{\delta}(a,Sa)).
$$
Let $\mathsf{cert}_S:\NameCert(S,\sim_S)$,
$\mathsf{cert}_T:\NameCert(T,\sim_T)$, and
$\mathsf{cert}_U:\NameCert(U,\sim_U)$. Assume
$\mathsf{TaggedOptionSourceHsameCompatible}(S,\sim_S)$,
$\mathsf{TaggedOptionSourceHsameCompatible}(T,\sim_T)$, and
$\mathsf{TaggedOptionSourceHsameCompatible}(U,\sim_U)$. For final endpoints
$m,m':\mathsf{BHist}$ and normalized endpoints $u,u':\mathsf{BHist}$, if
$$
\mathsf{TaggedOptionPayloadDescentImageClassifier}_{\epsilon\circ\delta}
(S,U,\rho_{\epsilon\circ\delta};m,m')
$$
and
$$
\hsame(m,u)\land \hsame(m',u'),
$$
then the following two one-sided readback clauses hold:
\begin{enumerate}
\item for every $x:\mathsf{BHist}$, if $U(x)$ and $\hsame(u,\Eone(x))$, then
there exists $y:\mathsf{BHist}$ such that
$$
U(y)\land \hsame(u',\Eone(y))\land x\sim_U y
$$
and
$$
\forall y':\mathsf{BHist}.\;
U(y')\to
\hsame(u',\Eone(y'))\to
\hsame(y,y')\land x\sim_U y';
$$
\item for every $y:\mathsf{BHist}$, if $U(y)$ and $\hsame(u',\Eone(y))$, then
there exists $x:\mathsf{BHist}$ such that
$$
U(x)\land \hsame(u,\Eone(x))\land x\sim_U y
$$
and
$$
\forall x':\mathsf{BHist}.\;
U(x')\to
\hsame(u,\Eone(x'))\to
\hsame(x,x')\land x'\sim_U y.
$$
\end{enumerate}
\end{theorem}
\begin{proof}
Fix a left visible payload $x$ with $U(x)$ and $\hsame(u,\Eone(x))$. First rule
out absence of the left normalized endpoint: if $\hsame(u,\emp)$ held, composing
its symmetry with $\hsame(u,\Eone(x))$ would give $\hsame(\emp,\Eone(x))$,
contradicting $not\_hsame\_emp\_e1$.

Apply
Theorem~\ref{thm:tagged-option-composite-image-classifier-normalized-nonempty-public-readback-equivalence}
to the composite image-classifier witness, the endpoint normalization witnesses,
and the displayed descent, preservation, stability, certificate, and
compatibility data. Its nonempty classified-pair clause gives visible payloads
$x_0,y$ with $U(x_0)$, $U(y)$, endpoint presentations
$\hsame(u,\Eone(x_0))$ and $\hsame(u',\Eone(y))$, $x_0\sim_U y$, and the
universal competing-readback clause.

Specialize that universal clause to the specified left payload $x$ and the
selected right payload $y$. This gives $\hsame(x_0,x)$ and $x\sim_U y$, so the
same $y$ is a classified right-side readback for the specified $x$.

For any competing right payload $y'$ with $U(y')$ and
$\hsame(u',\Eone(y'))$, specialize the universal clause to $x$ and $y'$. The
returned conclusions are $\hsame(y,y')$ and $x\sim_U y'$, exactly the required
uniqueness and classification against the competitor.

The right-visible clause is symmetric. Fix $y$ with $U(y)$ and
$\hsame(u',\Eone(y))$, rule out $\hsame(u',\emp)$ by the same
empty-versus-present no-confusion argument, and apply the normalized nonempty
public readback equivalence to obtain a selected classified pair $x,y_0$ and
the universal competing-readback clause.

Specializing the universal clause to $x$ and the specified $y$ gives
$x\sim_U y$. Specializing it again to any competing left payload $x'$ and the
same specified $y$ gives $\hsame(x,x')$ and $x'\sim_U y$, which completes the
right-sided readback clause.
\end{proof}
