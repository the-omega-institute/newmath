The nullable tag surface of $\OptionUp$ is read through the finite $b0/b1$ sibling discipline of \autoref{ch:concrete-instances-bool-namecert}; the child file records the payload and classifier obligations after that tag route is fixed.

\input{parts/concrete_instances/option/map_relation_and_composite_rows}
