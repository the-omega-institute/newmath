\chapter{Sierpinski Continuity Classifier NameCert Construction}
\label{ch:concrete-instances-sierpinski-continuity-classifier-namecert}
\origin{human}

Following Sierpinski and standard constructive topology, the $\SierpinskiContinuityClassifierUp$ packet records continuity through the finite observable-open classifier rather than through a host open-set predicate. It sits beside the Sierpinski observable truth carrier of \autoref{ch:concrete-instances-sierpinski-space-namecert}, the finite observation topology of \autoref{ch:concrete-instances-finite-observation-topology-namecert}, the topology row discipline of \autoref{ch:concrete-instances-topology-namecert}, and the continuous-map naming surface of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names the route by which a finite observation of an open predicate is read as a Sierpinski-valued classifier and transported through a continuous map; it does not install host open-set extensionality, quotient topology, open-cover compactness, or an ambient classical evaluator.

\begin{definition}[Sierpinski continuity classifier carrier]
\label{def:sierpinski-continuity-classifier-carrier}
A $\SierpinskiContinuityClassifierUp$ carrier is a finite $\BHist$ packet
$$
K=(X,Y,F,O,S,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source $\TopologyUp$ row whose opens are inspected only through finite observation data.
\item $Y$ is the target $\TopologyUp$ row receiving the displayed open-predicate read.
\item $F$ is the $\ContinuousMapUp$ row, recording the map as a finite BEDC route from source observations to target observations.
\item $O$ is the target open-predicate row under inspection.
\item $S$ is the $\SierpinskiSpaceUp$ classifier row, turning the open-predicate read into displayed observable truth.
\item $W$ is the $\FiniteObservationTopologyUp$ window row, listing the finite observations through which $O$ and $S$ are tested.
\item $H$ records componentwise $\hsame$ transport for source topology, target topology, continuous-map, open-predicate, Sierpinski classifier, finite-window, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a continuity-classifier request through $X,Y,F,O,S,W,H$.
\item $P$ is the $\Pkg$ provenance over $\SierpinskiContinuityClassifierUp$, $\TopologyUp$, $\ContinuousMapUp$, $\SierpinskiSpaceUp$, $\FiniteObservationTopologyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SierpinskiContinuityClassifierUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,F,O,S,W,H,C,P,N$. It has no coordinate for a host open-set equality proof, a quotient topology on observations, an open-cover compactness principle, a hidden truth evaluator, or a detached classical continuity predicate.
\end{definition}
\leandef{BEDC.Derived.SierpinskiContinuityClassifierUp}

\begin{theorem}[Sierpinski continuity classifier NameCert obligations]
\label{thm:sierpinski-continuity-classifier-namecert-obligations}
For every accepted $\SierpinskiContinuityClassifierUp$ carrier
$$
K=(X,Y,F,O,S,W,H,C,P,N),
$$
the local $\NameCert_{\SierpinskiContinuityClassifierUp}$ surface consists exactly of source and target topology exposure through $X,Y$; continuous-map exposure through $F$; target open-predicate exposure through $O$; Sierpinski-valued observable truth through $S$; finite observation topology exposure through $W$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:sierpinski-continuity-classifier-carrier}. The public continuity-classifier rows are exactly $X,Y,F,O,S,W,H,C,P,N$.

The open-predicate read is represented by the Sierpinski-valued classifier row $S$. The finite observation row $W$ determines which displayed windows may test the target open row $O$, and the continuous-map row $F$ is the only route by which those observations are pulled back to the source topology row $X$.

Classifier stability is componentwise $\hsame$ transport through $H$. Transport preserves the topology rows, the continuous-map row, the open-predicate row, the Sierpinski classifier row, and the finite observation row before replay and provenance are consumed.

Since no row stores host open-set extensionality, quotient topology, open-cover compactness, hidden truth evaluation, or a detached classical continuity predicate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Finite observation route for Sierpinski continuity]
\label{thm:sierpinski-continuity-classifier-finite-observation-route}
Every finite-observation topology consumer of an accepted $\SierpinskiContinuityClassifierUp$ carrier must read the target open row $O$ as a $\SierpinskiSpaceUp$ classifier row $S$, test it only through the finite windows $W$, and replay the pullback route through $F,C,P,N$. The consumer receives a displayed finite-observation continuity classifier; it receives no host open-set extensionality principle, no quotient topology on unseen observations, and no classical open-cover compactness claim.
\end{theorem}
\begin{proof}
Read the carrier rows from \autoref{def:sierpinski-continuity-classifier-carrier}. The only open-predicate information available to a consumer is the pair $O,S$, with $S$ exposing observable truth in the Sierpinski packet.

Apply the finite observation row $W$. It restricts the continuity test to displayed windows, so the consumer cannot quantify over unseen opens or replace the observed classifier by a host predicate.

Transport through $H$ is componentwise. The topology rows, continuous-map row, open row, Sierpinski classifier row, and finite-window row are preserved before the route is replayed by $C$ and sourced by $P,N$.

Thus the route exposes a finite-observation classifier for continuity and nothing stronger. Host open-set extensionality, quotient topology, and classical open-cover compactness are outside the carrier.
\end{proof}

\falsifiablePrediction{No accepted $\SierpinskiContinuityClassifierUp$ consumer can cite continuity through finite observation topology unless it first exposes source and target topology rows, the continuous-map row, the target open-predicate row, the Sierpinski classifier row, finite observation windows, transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows.}

\independenceWitness{sierpinski-space, finite-observation-topology, topology, continuousmap: no listed sibling is bijective with $\SierpinskiContinuityClassifierUp$ because this carrier alone binds a continuous-map row, a target open-predicate row, Sierpinski-valued observable truth, finite observation windows, componentwise transport, replay, provenance, and local NameCert rows into one accepted continuity-classifier packet.}

\closureat{SierpinskiContinuityClassifierUp}{seedStr}

\begin{closurestatus}{\SierpinskiContinuityClassifierUp}
  \constructivestory{A $\SierpinskiContinuityClassifierUp$ packet is generated from source and target Topology rows, a ContinuousMap row, a target open-predicate row, a SierpinskiSpace classifier row, finite observation topology windows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sierpinski-continuity-classifier-carrier}, \autoref{thm:sierpinski-continuity-classifier-namecert-obligations}, and \autoref{thm:sierpinski-continuity-classifier-finite-observation-route} seed the finite-observation route from displayed open-predicate reads to Sierpinski-valued continuity classifiers.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.SierpinskiContinuityClassifierUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host open-set extensionality, quotient topology, open-cover compactness, selected limits, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Sierpinski classifier exactness, finite-window stability, continuous-map pullback replay, componentwise transport, provenance, and local naming for BEDC.Derived.SierpinskiContinuityClassifierUp.}
\end{closurestatus}
