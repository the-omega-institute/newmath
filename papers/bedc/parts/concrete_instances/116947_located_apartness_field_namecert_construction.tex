\chapter{A Concrete Naming Certificate for $\LocatedApartnessFieldUp$}
\label{ch:concrete-instances-located-apartness-field-namecert}
\origin{human}

Following Bishop and Bridges, the $\LocatedApartnessFieldUp$ packet records the ordered-field-facing real interface whose comparisons are read through located apartness data rather than through a trichotomy principle. It consumes the constructive real-line surface of \autoref{ch:concrete-instances-constructive-real-line-namecert}, the located real boundary of \autoref{ch:concrete-instances-locatedreal-namecert}, the located apartness boundary of \autoref{ch:concrete-instances-located-apartness-namecert}, the positive-order handoff of \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert}, and the L10 source route through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The chapter names the finite field-facing packet; it does not claim quotient real equality, selected limits, trichotomy, or ambient completeness for $\RealUp$.

\begin{definition}[Located apartness field carrier]
\label{def:located-apartness-field-carrier}
A $\LocatedApartnessFieldUp$ carrier is a finite $\BHist$ packet
$$
F=(D,S,R,E,L,A,O,M,I,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for radii, slack comparisons, and field-operation error bounds.
\item $S$ is the $\StreamNameUp$ finite-window schedule used to inspect each real-name input and each operation output.
\item $R$ is the $\RegSeqRatUp$ readback row that turns the inspected windows into regular rational approximants.
\item $E$ is the terminal $\RealUp$ seal row reached only after $D,S,R$ have been displayed.
\item $L$ is the $\LocatedRealUp$ interval-location row for the same readback surface.
\item $A$ is the $\LocatedApartnessUp$ positive-separation row used when a consumer requests apartness.
\item $O$ records the finite addition, negation, multiplication, and unit rows whose outputs are again read through $D,S,R,E$.
\item $M$ records the inverse row, available only with an $A$-witness separating the input from zero.
\item $I$ is the $\RealApartnessOrderBridgeUp$ comparison handoff from positive apartness data to order-facing finite comparison cells.
\item $H$ records componentwise $\hsame$ transport for $D,S,R,E,L,A,O,M,I,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a field-facing request to the finite tolerance, window, readback, seal, locatedness, apartness, operation, inverse, and order-handoff rows.
\item $P$ is the $\Pkg$ provenance over $\LocatedApartnessFieldUp$, $\ConstructiveRealLineUp$, $\LocatedRealUp$, $\LocatedApartnessUp$, $\RealApartnessOrderBridgeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedApartnessFieldUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through the rows $D,S,R,E,L,A,O,M,I,H,C,P,N$. It has no coordinate for trichotomy, quotient real equality, selected limits, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Located apartness field NameCert obligations]
\label{thm:located-apartness-field-namecert-obligations}
For every accepted $\LocatedApartnessFieldUp$ carrier
$$
F=(D,S,R,E,L,A,O,M,I,H,C,P,N),
$$
the local $\NameCert_{\LocatedApartnessFieldUp}$ surface consists exactly of carrier admission; L10 source admission through $D,S,R,E$; locatedness admission through $L$; apartness admission through $A$; operation admission through $O$; inverse admission through $M$ only under displayed apartness from zero; order-handoff admission through $I$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-apartness-field-carrier}. The L10 source rows $D,S,R,E$ are visible before any field-facing output can be read.

Read the located and apartness boundary rows next. The row $L$ supplies interval-location data, while $A$ supplies positive separation evidence for apartness-sensitive consumers.

The algebra rows are then forced to remain on the same route. The operation row $O$ reads its inputs and outputs through $D,S,R,E$, and the inverse row $M$ is available only after the zero-separation evidence is displayed through $A$.

Finally the order bridge $I$ and the structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no further coordinate appears in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Located apartness field real nonescape]
\label{thm:located-apartness-field-real-nonescape}
Every field-facing consumer exported by an accepted $\LocatedApartnessFieldUp$ carrier factors through
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
together with the displayed $\LocatedRealUp$, $\LocatedApartnessUp$, and $\RealApartnessOrderBridgeUp$ boundary rows. The consumer receives a finite located-apartness field interface, not trichotomy, quotient real equality, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the L10 source route $D,S,R,E$ in \autoref{def:located-apartness-field-carrier}. Every operation output and every comparison endpoint must pass through the dyadic tolerance ledger, stream windows, regular rational readback, and terminal real seal.

The located and apartness reads remain boundary reads. A location request must use $L$, a positive-separation request must use $A$, and an order-facing comparison must use the handoff row $I$ from \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert}.

The operation and inverse rows do not add a host real object. They only repack the same inspected rows and, in the inverse case, require displayed apartness from zero.

Transport and replay through $H$ and $C$, then read provenance and local naming through $P$ and $N$. The route contains no trichotomy branch, quotient real constructor, selected limit, or completeness witness, so none of those objects can escape through the field-facing interface.
\end{proof}

\falsifiablePrediction{Every $\LocatedApartnessFieldUp$ field-facing read factors through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedRealUp$, $\LocatedApartnessUp$, $\RealApartnessOrderBridgeUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{constructive-real-line, locatedreal, located-apartness, real-apartness-order-bridge, dyadicratcore, streamname, regseqrat, real: $\LocatedApartnessFieldUp$ is not bijective to any listed sibling because it jointly carries field-operation rows, inverse rows guarded by apartness from zero, locatedness rows, positive-separation rows, order-facing comparison rows, L10 readback rows, transport, replay, provenance, and local naming as one field-facing packet.}

\closureat{LocatedApartnessFieldUp}{seedStr}

\begin{closurestatus}{\LocatedApartnessFieldUp}
  \constructivestory{A $\LocatedApartnessFieldUp$ packet is generated from DyadicRatCore tolerance rows, StreamName windows, RegSeqRat readback, a terminal RealUp seal, LocatedReal interval rows, LocatedApartness positive-separation rows, field operation rows, inverse rows guarded by apartness from zero, RealApartnessOrderBridge comparison handoff rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-apartness-field-carrier}, \autoref{thm:located-apartness-field-namecert-obligations}, and \autoref{thm:located-apartness-field-real-nonescape} seed the finite located-apartness field interface over ConstructiveRealLineUp, LocatedRealUp, LocatedApartnessUp, RealApartnessOrderBridgeUp, DyadicRatCoreUp, StreamNameUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter refuses trichotomy, quotient real equality, selected limits, and ambient Real completeness.}
  \upgradepath{A formal route can encode the displayed carrier and prove source admission, locatedness and apartness boundaries, field operation closure, inverse guarding by apartness from zero, order-handoff discipline, transport, replay, provenance, and ledger nonescape for BEDC.Derived.LocatedApartnessFieldUp.}
\end{closurestatus}
