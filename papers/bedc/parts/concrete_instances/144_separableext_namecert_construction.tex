\chapter{A Concrete Naming Certificate for $\SeparableExtUp$}
\label{ch:concrete-instances-separableext-namecert}

The separable-extension interface $\SeparableExtUp$ packages a field extension whose minimal polynomials all have simple roots. It depends on $\NameCert_{\FieldExtUp}$ (\autoref{ch:concrete-instances-fieldext-namecert}) and $\NameCert_{\PolynomialUp}$ (\autoref{ch:concrete-instances-polynomial-namecert}).

\section{Carrier definition}
\label{sec:separableext-carrier}

\section{The certificate}
\label{sec:separableext-certificate}

\closureat{SeparableExtUp}{seedStr}

\begin{closurestatus}{\SeparableExtUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed binds exactly the FieldExtUp dependency, PolynomialUp dependency, minimal-polynomial simple-root row, carrier, classifier, stability, ledger, exactness, and dependency obligations named by the SeparableExtUp interface.}
  \formalstatus{\unformalizedV}
  \leantarget{}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a separable-extension carrier, classifier, simple-root proof, FieldExtUp bridge, PolynomialUp bridge, standard bridge, or Lean verification target.}
  \upgradepath{Obligation closure requires explicit carrier, classifier, minimal-polynomial simple-root, stability, ledger, exactness, and dependency rows for the certified separable-extension interface.}
\end{closurestatus}
