\chapter{A Concrete Naming Certificate for $\FanFunctionalUp$}
\label{ch:concrete-instances-fanfunctional-namecert}
\origin{human}

Following Brouwer, Troelstra--van Dalen, and Bishop constructive analysis, the
$\FanFunctionalUp$ packet records the finite modulus-extraction surface that a
continuous functional on Cantor space may expose after a finite fan argument.
It consumes the finite-prefix carrier of
\autoref{ch:concrete-instances-cantorspace-namecert}, the decidable finite-bar
and depth rows of \autoref{ch:concrete-instances-fantheorem-namecert}, the
tolerance-to-precision ledger of
\autoref{ch:concrete-instances-uniformcontinuity-namecert}, and the compact
metric finite-net surface of
\autoref{ch:concrete-instances-compactmetric-namecert}. The packet exports an
explicit $\UniformModulusUp$ row for Cantor-prefix consumers; it does not
export a host functional, a choice-selected branch, open-cover compactness, or
real completeness.

\begin{definition}[FanFunctional carrier surface]
\label{def:fan-functional-carrier-surface}
A $\FanFunctionalUp$ carrier is a finite $\BHist$ packet
$$
\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $C$ is the $\CantorSpaceUp$ finite-prefix row, including the
StreamName schedule, Boolean observation ledger, and List prefix spine;
\item $F$ is the $\FanTheoremUp$ finite fan row, including the decidable bar
classifier and the unary depth ledger for the displayed binary spread;
\item $\varepsilon$ is the positive tolerance request read by the downstream
metric or real-valued consumer;
\item $B$ is the finite branch-prefix window obtained by restricting the
Cantor prefix rows of $C$ to the depth ledger of $F$;
\item $D$ is the finite bar-depth evidence saying that every retained branch
prefix in $B$ reaches a barred prefix by the displayed unary depth;
\item $W$ is the finite witness window pairing each retained prefix with the
pointwise tolerance row requested at that prefix;
\item $M$ is the extracted $\UniformModulusUp$ row, read as a unary prefix
length sufficient for all Cantor histories sharing the retained prefix;
\item $H$ records componentwise $\hsame$ transport for the Cantor rows, fan
rows, tolerance request, witness window, depth evidence, and extracted modulus;
\item $K$ records the displayed $\Cont$ replay route from Cantor-prefix reads
through the finite fan rows into the modulus row;
\item $P$ is the $\Pkg$ provenance over $\CantorSpaceUp$, $\FanTheoremUp$,
$\UniformContinuityUp$, $\CompactMetricUp$, $\UniformModulusUp$, $\BHist$,
$\hsame$, $\Cont$, and the local naming row;
\item $N$ is the local $\NameCert_{\FanFunctionalUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite branch, a host functional
$2^{\NatUp}\to\RealUp$, an open cover, a selected bar proof, or a hidden
compactness principle.
\end{definition}

\begin{theorem}[FanFunctional NameCert obligations]
\label{thm:fan-functional-namecert-obligations}
The local $\NameCert_{\FanFunctionalUp}$ surface of
\autoref{def:fan-functional-carrier-surface} consists of six finite
obligations: carrier admission for the displayed packet
$\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)$; classifier stability for the
Cantor-prefix and finite-fan rows under componentwise $\hsame$ transport;
bar-depth exactness for the unary ledger $D$; witness-window coverage for all
retained branch prefixes in $B$; uniform-modulus extraction of the row $M$; and
non-escape, requiring every public read to factor through $H,K,P,N$ and no
host functional or choice principle.
\end{theorem}
\begin{proof}
Carrier admission is projection of the displayed coordinates of
\autoref{def:fan-functional-carrier-surface}. The Cantor row, fan row,
tolerance request, branch-prefix window, depth evidence, witness window, and
modulus row are all finite $\BHist$ data.

Classifier stability is componentwise. The Cantor rows are compared by the
finite-prefix classifier of
\autoref{ch:concrete-instances-cantorspace-namecert}, while the fan rows are
compared by the finite bar classifier of
\autoref{ch:concrete-instances-fantheorem-namecert}. Transport by $H$ keeps
the tolerance, witness window, depth evidence, and extracted modulus attached
to the same displayed packet.

Bar-depth exactness reads the unary depth ledger of the $\FanTheoremUp$ row.
For each retained prefix in $B$, the witness $D$ records that the displayed
decidable bar has already been met within the finite depth; no infinite branch
or bar-induction rule is introduced.

The witness-window and modulus clauses are obtained by replaying $K$. Each
retained prefix carries the local tolerance row in $W$, and the finite fan
depth bounds the largest prefix length that a downstream consumer must inspect.
Repacking this bounded inspection length as $M$ gives the
$\UniformModulusUp$ row.

Non-escape follows from coordinate exhaustion. The carrier contains only
$C,F,\varepsilon,B,D,W,M,H,K,P,N$, so a public read cannot produce a host
functional, a choice-selected branch, open-cover compactness, or real
completeness.
\end{proof}

\begin{theorem}[FanFunctional root carrier admission]
\label{thm:fan-functional-root-carrier-admission}
Every root-obligation read of an accepted $\FanFunctionalUp$ packet must admit the full finite carrier
$$
\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)
$$
as $\BHist$ data before any compact-uniform consumer reads it. The rows $C,F,\varepsilon,B,D,W,M,H,K,P,N$ expose the Cantor prefix, fan bar, epsilon request, branch window, finite depth, witness window, modulus handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ surface as one packet.
\end{theorem}
\begin{proof}
Project \autoref{def:fan-functional-carrier-surface}. Carrier admission reads the displayed tuple before any downstream consumer may inspect a modulus row.

The structural rows then bind the read. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row preserve exactly $C,F,\varepsilon,B,D,W,M,H,K,P,N$, so a consumer has no entry point outside the admitted carrier.
\end{proof}

\begin{theorem}[FanFunctional root finite bar depth]
\label{thm:fan-functional-root-finite-bar-depth}
For every accepted $\FanFunctionalUp$ carrier $\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)$, the finite bar-depth row $D$ is exposed after the $\CantorSpaceUp$ and $\FanTheoremUp$ rows $C,F$ and before any witness-window use of $W$ or modulus read of $M$. Thus every $\BHist$ route through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ sees finite depth before it sees witness-window coverage.
\end{theorem}
\begin{proof}
Use the NameCert obligations. Bar-depth exactness is a separate row obligation, and witness-window coverage is downstream from that exactness.

The order is therefore $C,F$ first, then $D$, then $W,M$. Transport through $H$, replay through $K$, provenance through $P$, and naming through $N$ keep this order inside the same finite packet and do not add a selected branch or infinite bar proof.
\end{proof}

\begin{theorem}[FanFunctional root uniform-modulus handoff]
\label{thm:fan-functional-root-uniform-modulus-handoff}
Every accepted $\FanFunctionalUp$ carrier $\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)$ routes its $\UniformModulusUp$ handoff through $\CantorSpaceUp$, $\FanTheoremUp$, $\CompactMetricUp$, and $\UniformContinuityUp$ before the row $M$ is exported. The route is a finite $\BHist$ package through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, not a host functional, open-cover compactness proof, selected branch, or ambient Real-completeness argument.
\end{theorem}
\begin{proof}
First read $C$ and $F$ to obtain the finite Cantor-prefix and fan-bar surfaces. The compact-metric row consumes only the finite windows $B,D,W$, and the uniform-continuity row consumes the epsilon request $\varepsilon$ over those same windows.

The extracted row $M$ is then the $\UniformModulusUp$ handoff. Rows $H,K,P,N$ transport, replay, source, and name the same finite route, so the exported modulus is exactly the Cantor, fan, compact-metric, and uniform-continuity route displayed by the carrier.
\end{proof}

\begin{theorem}[FanFunctional prefix-window carrier admission]
\label{thm:fan-functional-prefix-window-carrier-admission}
Every consumer-visible modulus read from an accepted $\FanFunctionalUp$ carrier must first admit the Cantor prefix row $C$, finite fan row $F$, tolerance row $\varepsilon$, branch-window row $B$, depth row $D$, witness-window row $W$, modulus row $M$, componentwise $\hsame$ transport row $H$, displayed $\Cont$ replay row $K$, $\Pkg$ provenance row $P$, and local $\NameCert_{\FanFunctionalUp}$ row $N$. No $\CompactMetricUp$, $\UniformContinuityUp$, or $\UniformModulusUp$ consumer may read a modulus before those rows have appeared on the same finite packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-functional-carrier-surface}. Its visible coordinates are exactly $C,F,\varepsilon,B,D,W,M,H,K,P,N$, so carrier admission exposes the Cantor prefix, finite fan, tolerance, branch-window, depth, witness, and modulus rows before any consumer-facing read.

Apply \autoref{thm:fan-functional-namecert-obligations}. The carrier admission, classifier stability, bar-depth exactness, witness-window coverage, uniform-modulus extraction, and non-escape clauses keep those rows on the same displayed $\hsame$, $\Cont$, $\Pkg$, and local naming route.

Coordinate exhaustion closes the boundary. Since no coordinate names a host functional, selected branch, open cover, choice schedule, or ambient real-completeness object, every consumer-visible modulus read is forced through the admitted prefix-window carrier.
\end{proof}

\begin{theorem}[FanFunctional finite bar-modulus obligations]
\label{thm:fan-functional-finite-bar-modulus-obligations}
For every accepted $\FanFunctionalUp$ carrier, the finite bar-depth row $D$ and witness-window coverage row $W$ jointly determine the $\UniformModulusUp$ row $M$ through the displayed $C,F,\varepsilon,B,D,W,M,H,K,P,N$ packet. The determination uses the finite fan and witness windows only; it does not select branches, evaluate a host functional, import bar induction, or appeal to open-cover compactness.
\end{theorem}
\begin{proof}
Project \autoref{def:fan-functional-carrier-surface}. The row $D$ records the finite depth at which every retained branch prefix in $B$ reaches the displayed bar, and $W$ records the tolerance witness attached to each retained prefix.

Use \autoref{thm:fan-functional-namecert-obligations}. Its bar-depth exactness and witness-window coverage clauses are the only clauses that feed the uniform-modulus extraction clause, so $M$ is read as the bounded prefix length supported by those finite rows.

The remaining rows $H,K,P,N$ transport, replay, source, and name the same extraction. Coordinate exhaustion excludes selected branches, host functionals, bar-induction rules, and open-cover compactness from the modulus determination.
\end{proof}

\begin{theorem}[FanFunctional uniform-modulus consumer boundary]
\label{thm:fan-functional-uniform-modulus-consumer-boundary}
Every $\CompactMetricUp$ or $\UniformContinuityUp$ consumer of an accepted $\FanFunctionalUp$ packet receives only the extracted finite prefix-modulus route through $\CantorSpaceUp$, $\FanTheoremUp$, $\CompactMetricUp$, $\UniformContinuityUp$, and $\UniformModulusUp$. The handoff exposes $C,F,\varepsilon,B,D,W,M,H,K,P,N$ and no host functional, selected branch, open-cover compactness theorem, countable-choice selector, or ambient Real-completeness principle.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:fan-functional-carrier-surface}. The $\CantorSpaceUp$ row $C$ and $\FanTheoremUp$ row $F$ supply the finite prefix and fan surfaces, while $\varepsilon,B,D,W,M$ supply the tolerance, branch-window, depth, witness, and modulus rows read by downstream consumers.

Apply \autoref{thm:fan-functional-namecert-obligations}. It forces the modulus extraction through the same carrier admission, classifier stability, bar-depth exactness, witness-window coverage, and non-escape clauses that govern the local NameCert surface.

Finally route the public handoff through $H,K,P,N$. These rows give only componentwise transport, displayed replay, provenance, and local naming, so $\CompactMetricUp$ and $\UniformContinuityUp$ consumers receive the finite $\UniformModulusUp$ route and no hidden compactness, choice, host-functional, or Real-completeness coordinate.
\end{proof}

\begin{theorem}[FanFunctional uniform modulus extraction]
\label{thm:fan-functional-uniform-modulus-extraction}
Let $\Phi$ be an accepted $\FanFunctionalUp$ carrier. The finite fan depth row
of $F$, the Cantor prefix window $B$, and the tolerance witness rows $W$
determine the extracted $\UniformModulusUp$ row $M$. For any two accepted
$\CantorSpaceUp$ histories whose Boolean ledgers agree through the prefix
length named by $M$, every consumer-visible value of the functional is routed
through the same barred prefix window and hence through the same tolerance
row. The extraction uses finite prefix inspection only.
\end{theorem}
\begin{proof}
Project the accepted carrier and read its $\FanTheoremUp$ row. The finite bar
handoff supplies a unary depth ledger and a finite branch-prefix window, so the
inspection bound is visible before any downstream modulus row is formed.

Restrict the $\CantorSpaceUp$ carrier to that window. Agreement of two Cantor
histories through the prefix length named by $M$ gives componentwise agreement
of their Boolean ledgers on every prefix in $B$.

Replay the witness rows $W$ through $K$. Each retained prefix already carries
the local tolerance request, and the bar-depth evidence $D$ says that no longer
branch read is required. The same prefix window therefore supports the same
consumer tolerance row for both histories.

Package the resulting bounded prefix length as the $\UniformModulusUp$ row
$M$. Since the construction reads only the finite Cantor rows, finite fan
depth, witness window, transports, continuation, provenance, and local
certificate, it introduces no host functional and no choice-selected branch.
\end{proof}

\begin{theorem}[FanFunctional Cantor-prefix modulus handoff]
\label{thm:fan-functional-cantor-prefix-modulus-handoff}
An accepted $\FanFunctionalUp$ carrier routes every Cantor-prefix finite-bar row through $\FanTheoremUp$, $\UniformContinuityUp$, $\CompactMetricUp$, and $\UniformModulusUp$ before exposing the extracted modulus row $M$. The handoff reads the Cantor prefix window $B$, finite bar and depth rows $F,D$, tolerance witnesses $W$, compact finite-prefix coverage, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row; it exports no selected branch, host functional, open-cover compactness principle, or countable-choice schedule.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-functional-carrier-surface}. Its Cantor-facing finite rows are the prefix window $B$, the fan row $F$, the depth ledger $D$, and the tolerance witness family $W$.

Apply the NameCert obligations of \autoref{thm:fan-functional-namecert-obligations}. They keep those rows on the same displayed route through $\hsame$, $\Cont$, $\Pkg$, and local naming before any consumer may inspect the extracted modulus.

Now use uniform modulus extraction, \autoref{thm:fan-functional-uniform-modulus-extraction}. The extracted row $M$ is determined by the finite fan depth, Cantor prefix window, and witness rows, so the $\UniformContinuityUp$ and $\CompactMetricUp$ consumers receive only the finite route that reaches $\UniformModulusUp$.

Thus every Cantor-prefix finite-bar read passes through $\FanTheoremUp$, $\UniformContinuityUp$, $\CompactMetricUp$, and $\UniformModulusUp$ before $M$ is exposed. Any selected branch, host functional, open-cover compactness proof, or countable-choice schedule would require a coordinate absent from the accepted $\FanFunctionalUp$ carrier.
\end{proof}

\begin{theorem}[FanFunctional compact-continuity consumer]
\label{thm:fan-functional-compact-continuity-consumer}
A compact-continuity consumer citing an accepted $\FanFunctionalUp$ packet may
read exactly the Cantor-prefix rows of $C$, the finite-bar and depth rows of
$F$, the tolerance request $\varepsilon$, the witness window $W$, the extracted
$\UniformModulusUp$ row $M$, and the support rows $H,K,P,N$. This is the
Cantor-space instance of the compact-metric and uniform-continuity consumer
surface: compactness is consumed only through finite prefix coverage and
finite bar depth, while continuity is consumed only through the displayed
tolerance-to-modulus ledger.
\end{theorem}
\begin{proof}
Start with the compact-metric dependency. In the Cantor-space case, the
compact carrier is presented by finite Boolean prefix coverage, so the consumer
can read compactness only as the prefix rows already present in $C$ and $B$.

Use the $\FanTheoremUp$ dependency to bound the prefix inspection. Its
decidable bar and unary depth rows supply the finite branch-depth evidence $D$,
and the witness window $W$ records the tolerance rows attached to those
prefixes.

Use the $\UniformContinuityUp$ dependency only at the modulus handoff. The row
$M$ is the explicit $\UniformModulusUp$ output supplied by the finite fan
inspection, and the continuation route $K$ is the only path by which a consumer
may read it.

Thus the compact-continuity consumer receives exactly the displayed finite
surface. Host compactness, open covers, selected branches, semantic continuity,
and real completeness are absent from the carrier and cannot be exported.
\end{proof}

\begin{theorem}[FanFunctional Cantor modulus ledger]
\label{thm:fan-functional-cantor-modulus-ledger}
Let $\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)$ be an accepted $\FanFunctionalUp$ carrier. The extracted $\UniformModulusUp$ row $M$ is determined only by the finite Cantor-prefix row $C$, the fan row $F$, the branch-prefix window $B$, the depth evidence $D$, the witness window $W$, and the support route $H,K,P,N$. Any two accepted packets with the same displayed rows $C,F,B,D,W,H,K,P,N$ expose the same consumer-visible modulus ledger, and no host functional, selected branch, open-cover compactness principle, or $\RealUp$ completeness datum contributes to the determination of $M$.
\end{theorem}
\begin{proof}
Project the carrier surface of \autoref{def:fan-functional-carrier-surface}. The Cantor row $C$, fan row $F$, branch-prefix window $B$, depth evidence $D$, and witness window $W$ are finite $\BHist$ rows, so the modulus read begins from displayed prefix and fan data rather than from a host functional.

Use \autoref{thm:fan-functional-namecert-obligations} to bind the carrier admission, classifier stability, bar-depth exactness, witness-window coverage, uniform-modulus extraction, and non-escape clauses. These obligations force $M$ to be read only after the finite prefix, fan, depth, and witness rows have been admitted.

Apply \autoref{thm:fan-functional-uniform-modulus-extraction} for the extraction route and \autoref{thm:fan-functional-compact-continuity-consumer} for the consumer scope. Transport by $H$, replay by $K$, provenance by $P$, and local naming by $N$ preserve the same finite route, so equal displayed ledger rows give the same public modulus read and the excluded compactness, branch-selection, host-functional, and Real-completeness data never enter.
\end{proof}

\begin{theorem}[FanFunctional compact-metric finite-net route]
\label{thm:fanfunctional-compactmetric-finite-net-route}
For an accepted $\FanFunctionalUp$ carrier
$\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)$, the compact-metric input read by any
consumer is the finite Cantor-prefix net $B$ together with the depth ledger
$D$ and witness window $W$. The route from $\CompactMetricUp$ to the
$\UniformModulusUp$ output is therefore
$$
\CompactMetricUp(B,D,W) \longmapsto \FanTheoremUp(F,D)
\longmapsto \UniformContinuityUp(\varepsilon,W)
\longmapsto \UniformModulusUp(M),
$$
with all transport and replay confined to $H,K,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only compact-metric data present are the
finite Cantor-prefix rows of $C$ restricted to the retained window $B$, the
bar-depth ledger $D$, and the tolerance witnesses $W$.

Replay the compact route through $K$. The finite-net read first exposes
$B,D,W$, then uses the fan row $F$ to certify that every retained prefix has
met the displayed decidable bar within the named depth.

Use the uniform-continuity row only after that finite coverage has been read.
The tolerance request $\varepsilon$ and witness window $W$ determine the output
row $M$, while $H,P,N$ preserve the same finite packet under transport,
provenance, and local naming. No open cover, selected branch, or host compact
space is introduced.
\end{proof}

\begin{theorem}[FanFunctional fan-theorem uniform depth]
\label{thm:fanfunctional-fan-theorem-uniform-depth}
Let $\Phi$ be an accepted $\FanFunctionalUp$ carrier. The $\FanTheoremUp$ row
$F$ supplies a single unary depth ledger $D$ for the displayed finite fan, and
the extracted modulus row $M$ may inspect Cantor histories only through
prefixes bounded by that ledger. Thus any two accepted $\CantorSpaceUp$
histories agreeing through the depth named by $D$ reach the same retained
prefix window before the $\UniformModulusUp$ row is read.
\end{theorem}
\begin{proof}
Read $F$ and $D$ from the carrier. The fan theorem row is already presented as
finite decidable-bar data, so the depth ledger is a displayed unary object and
not a host proof over all infinite branches.

Restrict the Cantor rows of $C$ to that depth. Agreement of two histories up
to the ledger named by $D$ gives the same retained prefix window $B$ and hence
the same indexed witness family $W$.

The modulus output $M$ is downstream from this window. Replay through $K$ and
transport through $H$ keep the read inside the displayed packet, while $P$ and
$N$ record provenance and local naming. The construction extracts a uniform
depth from finite fan data only.
\end{proof}

\begin{theorem}[FanFunctional uniform-modulus consumer scope]
\label{thm:fanfunctional-uniformmodulus-consumer-scope}
Every $\UniformModulusUp$ consumer of an accepted $\FanFunctionalUp$ packet
may read exactly the Cantor-prefix row $C$, finite prefix net $B$, fan-depth
ledger $D$, tolerance request $\varepsilon$, witness window $W$, extracted
modulus row $M$, and support rows $H,K,P,N$. The consumer scope excludes host
branch choice, open-cover compactness, semantic continuity over a function
space, and any modulus not obtained from the displayed finite fan route.
\end{theorem}
\begin{proof}
Start from the carrier coordinates. They expose $C,F,\varepsilon,B,D,W,M$ and
the structural rows $H,K,P,N$; there is no coordinate for a host function
space, a selected branch, or an open cover.

The consumer first reads the finite prefix net $B$ and the depth ledger $D$.
Those rows determine which Cantor prefixes are available before the modulus
handoff.

It then reads the tolerance witnesses $W$ and output row $M$ through the replay
route $K$. Componentwise transport, provenance, and local naming preserve this
same finite surface, so every consumer-visible modulus factors through the
displayed fan route.
\end{proof}

\begin{definition}[FanFunctional root bar carrier]
\label{def:fanfunctional-root-bar-carrier}
A root-unblock $\FanFunctionalUp$ carrier is the finite packet
$$
\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)
$$
where $C$ is the $\CantorSpaceUp$ prefix StreamName row, $K$ is the $\CompactMetricUp$ finite-prefix coverage row, $F$ is the $\FanTheoremUp$ decidable-bar row, $U$ is the $\UniformContinuityUp$ tolerance-to-prefix row, $M$ is the $\UniformModulusUp$ output row, $B$ is the finite root-bar window, $W$ is the retained $\ProbeBundle$ witness family, $D$ is the unary depth ledger, $H$ is componentwise $\hsame$ transport, $R$ is displayed $\Cont$ replay, $P$ is $\Pkg$ provenance, and $N$ is the local $\NameCert_{\FanFunctionalUp}$ row. The packet has no coordinate for countable choice, selected infinite branches, host functionals, or open-cover compactness.
\end{definition}

\begin{theorem}[FanFunctional root uniform-modulus obligation]
\label{thm:fanfunctional-root-uniform-modulus-obligation}
Every accepted root-unblock $\FanFunctionalUp$ packet $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$ exposes the $\UniformModulusUp$ row $M$ only after the $\CantorSpaceUp$ prefix row, $\CompactMetricUp$ finite-prefix coverage, $\FanTheoremUp$ decidable bar, $\UniformContinuityUp$ tolerance row, finite root-bar window, $\ProbeBundle$ witnesses, and unary depth ledger have all been displayed. The obligation is a finite $\BHist$ handoff and never a countable-choice selection of branch witnesses.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fanfunctional-root-bar-carrier}. The rows $C,K,F,U$ expose the Cantor prefix data, compact finite-prefix coverage, decidable fan bar, and tolerance-to-prefix ledger before any modulus output is available.

Read the finite root data next. The window $B$, witness family $W$, and depth ledger $D$ record the finite bar and prefix budget consumed by the modulus row.

The row $M$ is therefore downstream of finite displayed data. Transport, replay, provenance, and local naming through $H,R,P,N$ preserve that ordering, so no countable-choice selector, selected infinite branch, host functional, or open-cover compactness row can be read.
\end{proof}

\begin{theorem}[FanFunctional root Cantor-window handoff]
\label{thm:fanfunctional-root-cantor-window-handoff}
Every $\CantorSpaceUp$ consumer of an accepted root-unblock $\FanFunctionalUp$ packet receives only the finite prefix window
$$
C \longmapsto B,W,D
$$
before the $\FanTheoremUp$ and $\UniformModulusUp$ rows are inspected. The handoff is a StreamName and ProbeBundle window over Boolean prefixes, not an infinite branch, not a selected path through the fan, and not a countable-choice sequence.
\end{theorem}
\begin{proof}
Start with the Cantor prefix row $C$. It exposes Boolean observations through finite StreamName windows and contains no infinite branch coordinate.

The root-bar rows $B,W,D$ restrict those observations to the displayed finite prefix window. The witness family $W$ is indexed by the finite ProbeBundle data, and the depth ledger $D$ bounds the inspection.

Only after this window is displayed may $F$ and $M$ be read. Thus the Cantor consumer receives a finite handoff over $C,B,W,D$, with no selected branch or countable-choice sequence.
\end{proof}

\begin{theorem}[FanFunctional root choice-free nonescape]
\label{thm:fanfunctional-root-choice-free-nonescape}
No accepted root-unblock $\FanFunctionalUp$ packet can export countable choice, a selected infinite branch, or a host functional. Every public read factors through the finite rows $C,K,F,U,M,B,W,D,H,R,P,N$, and any attempted read that bypasses the finite root-bar window or the displayed $\NameCert_{\FanFunctionalUp}$ row leaves the packet.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates in \autoref{def:fanfunctional-root-bar-carrier}. They are finite Cantor-prefix, compact-prefix, fan-bar, uniform-continuity, uniform-modulus, root-window, witness-family, depth, transport, replay, provenance, and naming rows.

There is no coordinate whose type is a stream of choices, a selected branch, or a host-level functional. Componentwise $\hsame$ transport in $H$ can move only the displayed coordinates, and replay in $R$ reuses only the same finite route.

Thus every public read is exhausted by $C,K,F,U,M,B,W,D,H,R,P,N$. A reader that claims choice, branch selection, or a host functional has introduced data not present in the accepted packet.
\end{proof}

\begin{theorem}[FanFunctional root uniform-continuity consumer]
\label{thm:fanfunctional-root-uniformcontinuity-consumer}
Every $\UniformContinuityUp$ consumer of an accepted root-unblock $\FanFunctionalUp$ packet reads the tolerance-to-prefix row $U$ together with the $\CantorSpaceUp$ prefix row $C$, $\CompactMetricUp$ finite coverage row $K$, $\FanTheoremUp$ bar row $F$, finite root-bar window $B,W,D$, and the output $\UniformModulusUp$ row $M$. The consumer receives a finite modulus surface over Cantor prefixes and ProbeBundle witnesses, not semantic continuity over a host function space.
\end{theorem}
\begin{proof}
Project the accepted packet and read the uniform-continuity row $U$. Its tolerance request is attached to the same Cantor prefix and compact finite coverage rows $C,K$.

Use the fan row $F$ and root-bar window $B,W,D$ to bound the inspection. These rows provide the finite prefix budget that the consumer may use when replaying the tolerance-to-prefix ledger.

The output row $M$ is then the visible $\UniformModulusUp$ handoff, and $H,R,P,N$ preserve it as finite NameCert data. Since the packet has no host functional-space coordinate, the consumer obtains only the displayed finite modulus surface.
\end{proof}

\begin{theorem}[FanFunctional root Cantor compact modulus]
\label{thm:fanfunctional-root-cantor-compact-modulus}
For every accepted root-unblock $\FanFunctionalUp$ packet $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$, the row family
$$
C,\quad F,\quad U,\quad K,\quad M,\quad B,\quad D,\quad W,\quad H,\quad R,\quad P,\quad N
$$
forces the exported $\UniformModulusUp$ row $M$ through the displayed $\CantorSpaceUp$, $\FanTheoremUp$, $\UniformContinuityUp$, and $\CompactMetricUp$ rows only. A consumer reads $M$ by first reading the Cantor prefix row of \autoref{ch:concrete-instances-cantorspace-namecert}, the finite fan row of \autoref{ch:concrete-instances-fantheorem-namecert}, the compact finite-net row of \autoref{ch:concrete-instances-compactmetric-namecert}, and the tolerance row of \autoref{ch:concrete-instances-uniformcontinuity-namecert}; the modulus surface then lands at \autoref{ch:concrete-instances-uniformmodulus-namecert} as displayed $\BHist$ rows, not as a host compactness or continuity principle.
\end{theorem}
\begin{proof}
Project the root-bar carrier. The rows $C,F,U,K$ are the only Cantor, fan, uniform-continuity, and compact-metric source rows available before the modulus row $M$ is read.

The finite window rows $B,D,W$ bind those sources to displayed prefixes. They record the root-bar window, unary depth ledger, and retained ProbeBundle witnesses, so the compact metric input is a finite-net row over the same Cantor prefixes.

Finally transport and replay through $H,R,P,N$. These structural rows preserve the displayed $\BHist$ route and cannot add a host compactness theorem, a semantic continuity principle, or a hidden modulus selector. Thus $M$ is forced through the visible Cantor, fan, compact-net, and tolerance rows.
\end{proof}

\begin{theorem}[FanFunctional root finite-depth exhaustion]
\label{thm:fanfunctional-root-finite-depth-exhaustion}
Every finite-depth read of an accepted root-unblock $\FanFunctionalUp$ packet $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$ is exhausted by the displayed rows
$$
C,\quad F,\quad B,\quad D,\quad W,\quad K,\quad U,\quad M,\quad H,\quad R,\quad P,\quad N.
$$
The CantorSpace, FanTheorem, CompactMetric, UniformContinuity, and UniformModulus siblings are consumed only through these finite $\BHist$ rows: $C$ supplies Boolean prefix windows, $F$ supplies the decidable fan bar, $K$ supplies finite-net coverage, $U$ supplies tolerance-to-prefix evidence, and $M$ is exported only at the retained depth.
\end{theorem}
\begin{proof}
Start with the visible depth data. The rows $C,B,D,W$ are finite Cantor-prefix, root-window, unary-depth, and witness-family data, so they contain no selected infinite branch.

Read the fan and compact rows at the same depth. The row $F$ verifies the finite bar over the displayed prefixes, while $K$ gives compact finite-net coverage for that same bounded window.

The tolerance and modulus rows are then downstream. The row $U$ attaches the tolerance request to the bounded window, and $M$ is the resulting UniformModulus readback. Transport, replay, provenance, and local naming keep this route inside $H,R,P,N$, so finite depth is exhausted before any hidden selector could be claimed.
\end{proof}

\begin{theorem}[FanFunctional root uniform-modulus readback]
\label{thm:fanfunctional-root-uniform-modulus-readback}
For every accepted root-unblock $\FanFunctionalUp$ packet $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$, the exported $\UniformModulusUp$ row $M$ is read back from the $\UniformContinuityUp$ row $U$ only after the $\CantorSpaceUp$ row $C$, the $\FanTheoremUp$ row $F$, the $\CompactMetricUp$ row $K$, and the finite root rows $B,D,W$ have been displayed. The readback is a displayed $\BHist$ route through $H,R,P,N$, not a quotient of host functionals, an open-cover compactness argument, or a selected branch through Cantor space.
\end{theorem}
\begin{proof}
Project $\Theta$ and read the source tolerance row $U$. Its request is attached to the displayed Cantor prefix row $C$ and cannot be interpreted as semantic continuity over a host function space.

Next use the finite fan and compact rows. The row $F$ supplies the decidable bar depth, $K$ supplies finite-net coverage, and $B,D,W$ record the retained root-bar window and witnesses at that depth.

Only then replay the output row $M$. The rows $H,R,P,N$ transport, replay, source, and name the same finite route, so the UniformModulus export is a displayed readback from Cantor, FanTheorem, CompactMetric, and UniformContinuity rows rather than an external compactness or choice principle.
\end{proof}

\begin{theorem}[FanFunctional modulus-extraction obligations]
\label{thm:fanfunctional-modulus-extraction-obligations}
For an accepted root-unblock $\FanFunctionalUp$ packet $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$, the modulus-extraction obligation is the finite route that first reads the $\CantorSpaceUp$ prefix row $C$, then the $\FanTheoremUp$ decidable-bar row $F$, then the $\UniformContinuityUp$ tolerance row $U$, and only then the $\UniformModulusUp$ output row $M$. The extracted modulus is valid exactly for the Cantor prefixes retained by $B,W,D$ and transported by $H,R,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fanfunctional-root-bar-carrier}. The rows $C,F,U$ are displayed before $M$, and the finite window $B,W,D$ records which Cantor prefixes reach the decidable fan bar.

Replay the route through $R$. The tolerance row $U$ is consumed only after the bar window has been exposed, so the output $M$ is a bounded prefix row rather than a host functional-space modulus.

Componentwise transport through $H$ and provenance through $P,N$ keep the same finite packet attached to the modulus read. Therefore modulus extraction is exactly the displayed $\CantorSpaceUp$, $\FanTheoremUp$, $\UniformContinuityUp$, and $\UniformModulusUp$ handoff.
\end{proof}

\begin{theorem}[FanFunctional Cantor-window coverage obligations]
\label{thm:fanfunctional-cantor-window-coverage-obligations}
Every accepted root-unblock $\FanFunctionalUp$ packet covers its public Cantor-window reads through the $\CompactMetricUp$ finite-prefix row $K$, the retained window $B$, the witness family $W$, and the depth ledger $D$. A consumer may inspect only the finite-net rows displayed by $K,B,W,D$ before reading the modulus row $M$.
\end{theorem}
\begin{proof}
Read the compact row $K$ from $\Theta$. In this packet the compact-metric input is not an open cover; it is the finite Cantor-prefix net already recorded by the root-bar carrier.

The rows $B,W,D$ refine that net. They select the retained prefix window, attach the ProbeBundle witnesses, and give the unary depth ledger by which the fan row certifies coverage.

Replay through $R$ and transport through $H$. Since every public read of $M$ must pass through $K,B,W,D,P,N$, the Cantor-window coverage obligation is finite-net coverage and no stronger compactness principle is exported.
\end{proof}

\begin{theorem}[FanFunctional choice-free nonescape obligations]
\label{thm:fanfunctional-choice-free-nonescape-obligations}
The root-unblock $\FanFunctionalUp$ packet exports no selected infinite branch, no countable choice, no open-cover compactness principle, and no host continuity principle. Every public read factors through the displayed rows $C,K,F,U,M,B,W,D,H,R,P,N$ and through the local $\NameCert_{\FanFunctionalUp}$ row.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates. They are finite Cantor-prefix, compact finite-net, decidable fan-bar, tolerance, modulus, root-window, witness, depth, transport, replay, provenance, and naming rows.

There is no row for an infinite branch selector, countable-choice sequence, open cover, or host continuity axiom. The replay row $R$ can only revisit the displayed route, and $H$ can only transport the listed coordinates.

Thus any consumer that remains inside the packet reads through $C,K,F,U,M,B,W,D,H,R,P,N$. A claim of branch selection, countable choice, open-cover compactness, or host continuity would require data absent from the finite $\NameCert$ surface.
\end{proof}

\begin{theorem}[FanFunctional root depth exhaustion]
\label{thm:fanfunctional-root-depth-exhaustion}
For every accepted root-unblock $\FanFunctionalUp$ packet $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$, every root-depth read is exhausted by the displayed $\CantorSpaceUp$ windows $C,B$, the $\FanTheoremUp$ finite bar row $F$, the finite witness family $W$, the depth ledger $D$, the $\CompactMetricUp$ finite-net row $K$, and the structural rows $H,R,P,N$. The packet exports no countable choice sequence, host compactness principle, or hidden modulus selector.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fanfunctional-root-bar-carrier}. The rows $C,B,W,D$ are finite Cantor-window and depth data, while $F$ records the finite-bar route and $K$ records the compact finite-net input.

Replay the public read through $R$. The depth ledger is consumed only together with $C,K,F,B,W$, so a consumer cannot detach $D$ as an independent branch selector or as an open-cover compactness theorem.

Transport and provenance through $H,P,N$ preserve the same finite packet. Since no additional coordinate names countable choice or a hidden selector, root depth is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[FanFunctional Cantor-window modulus readback]
\label{thm:fanfunctional-cantor-window-modulus-readback}
For an accepted root-unblock $\FanFunctionalUp$ packet $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$, the $\UniformContinuityUp$ source rows $U$ may be read back into a $\UniformModulusUp$ consumer row $M$ only through the retained $\CantorSpaceUp$ windows $C,B$, the $\FanTheoremUp$ finite bar/depth rows $F,D$, the witness family $W$, and the $\CompactMetricUp$ finite-net row $K$. The readback is a finite NameCert route through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, not a host continuity or compactness export.
\end{theorem}
\begin{proof}
Start with the uniform-continuity row $U$. Its tolerance request is attached to displayed Cantor prefixes rather than to a semantic function space.

Use the Cantor-window rows $C,B$ and the fan rows $F,D$. These rows bound the prefix depth and identify the finite bar evidence used by the witness family $W$.

The compact row $K$ supplies only finite-net coverage for those displayed windows. Replaying through $R$ then delivers the modulus row $M$, while $H,P,N$ keep the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ structure visible.
\end{proof}

\begin{theorem}[FanFunctional UniformModulus consumer package]
\label{thm:fanfunctional-uniformmodulus-consumer-package}
Every $\UniformModulusUp$ consumer of an accepted root-unblock $\FanFunctionalUp$ packet receives the package
$$
C,K,F,U,M,B,W,D,H,R,P,N
$$
as visible finite carrier rows: $\CantorSpaceUp$ windows, $\FanTheoremUp$ finite bar and depth rows, $\UniformContinuityUp$ source rows, the $\UniformModulusUp$ consumer row, $\CompactMetricUp$ finite-net rows, $\BHist$ witnesses, componentwise $\hsame$, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. It receives no countable choice principle, hidden compactness theorem, or unrecorded modulus selector.
\end{theorem}
\begin{proof}
Inspect the displayed package. The consumer row $M$ is not read alone; it is attached to the source row $U$, the Cantor windows $C,B$, the fan/depth rows $F,D$, witness data $W$, and finite-net data $K$.

The replay row $R$ supplies the route from source tolerance to consumer modulus. Componentwise transport through $H$ and provenance through $P,N$ preserve the same finite carrier rows and do not add a selector coordinate.

Therefore a $\UniformModulusUp$ consumer gets exactly the visible NameCert package. Any claim of countable choice, host compactness, or a hidden modulus selector would require a row outside $\Theta$.
\end{proof}

\begin{theorem}[FanFunctional root finite-modulus route]
\label{thm:fanfunctional-root-finite-modulus-route}
Every accepted root-unblock $\FanFunctionalUp$ packet exposes a
$\UniformModulusUp$ read only through the finite route
$$
C,K,F,U,M,B,W,D,H,R,P,N.
$$
Here $C$ and $B$ are the $\CantorSpaceUp$ prefix-window rows, $K$ is the
$\CompactMetricUp$ finite-net row, $F$ is the $\FanTheoremUp$ finite-bar row,
$U$ is the $\UniformContinuityUp$ source row, $W$ is the $\ProbeBundle$
witness family, $D$ is the displayed depth ledger, $H$ transports the rows
componentwise by $\hsame$, $R$ replays the displayed $\Cont$ route, $P$
records provenance, and $N$ is the local $\NameCert$ row. No other coordinate
of the accepted packet can expose a selected branch, a countable-choice
operator, a host functional, or an open-cover compactness theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier $\Theta=(C,K,F,U,M,B,W,D,H,R,P,N)$. The only
rows that mention a modulus request are the $\UniformContinuityUp$ row $U$,
the $\UniformModulusUp$ row $M$, and the finite Cantor, fan, witness,
depth, transport, replay, provenance, and naming rows displayed with them.

The root-bar data are finite. The Cantor rows $C,B$ bound the prefix windows,
$F,D$ name the accepted bar and depth, $W$ supplies the displayed
$\ProbeBundle$ witnesses, and $K$ supplies only the compact finite-net input
needed by those windows.

Replay through $R$ and transport through $H$. These rows can only move the
same finite packet to the consumer row $M$, while $P,N$ record its source and
local name. Since the carrier has no row for a branch selector, countable
choice, host functional evaluation, or open-cover compactness, the
$\UniformModulusUp$ read is exhausted by the displayed finite route.
\end{proof}

\begin{theorem}[FanFunctional compact-uniform consumer route]
\label{thm:fanfunctional-compact-uniform-consumer-route}
Any compact-uniform-continuity consumer that receives an accepted
$\FanFunctionalUp$ root-unblock packet receives only the finite route of
\autoref{thm:fanfunctional-root-finite-modulus-route}: Cantor prefix windows,
the compact finite-net row, fan-bar and depth rows, uniform-continuity source
rows, the uniform-modulus consumer row, $\ProbeBundle$ witnesses,
componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$
provenance, and the local $\NameCert$ row. The handoff supplies no selected
branch, countable choice, host functional, or open-cover compactness object.
\end{theorem}
\begin{proof}
Start with a compact-uniform-continuity consumer read. By
\autoref{thm:fanfunctional-uniformmodulus-consumer-package}, the consumer
receives the package $C,K,F,U,M,B,W,D,H,R,P,N$ as visible finite carrier
rows, not as an ambient function-space principle.

Apply \autoref{thm:fanfunctional-root-finite-modulus-route}. The route from
$U$ to $M$ factors through the Cantor windows, finite-net row, fan-bar row,
witness bundle, depth ledger, replay row, transport row, provenance, and local
name certificate.

Thus the compact-uniform-continuity handoff is a finite NameCert route. A
selected branch, countable-choice schedule, host functional, or open-cover
compactness object would require an additional row outside the accepted
packet, so none is delivered to the consumer.
\end{proof}

\begin{theorem}[FanFunctional Cantor compact route]
\label{thm:fanfunctional-cantor-compact-route}
Every $\UniformModulusUp$ row exported by an accepted $\FanFunctionalUp$
root-unblock packet factors through the Cantor finite-prefix rows, the
$\FanTheoremUp$ finite-depth ledger, the $\CompactMetricUp$ finite-net row,
and the $\UniformModulusUp$ readback route before any $\RealUp$-facing or
compact-uniform consumer can read it. Thus the exported modulus is a finite
Cantor-compact NameCert route over
$$
C,K,F,U,M,B,W,D,H,R,P,N
$$
and not a selected branch, countable-choice schedule, host functional, or
open-cover compactness theorem.
\end{theorem}
\begin{proof}
Start from the compact-uniform consumer route of
\autoref{thm:fanfunctional-compact-uniform-consumer-route}. It already
places every consumer read behind the finite package
$C,K,F,U,M,B,W,D,H,R,P,N$.

Use \autoref{thm:fanfunctional-root-finite-modulus-route} to locate the
modulus row. The route from $U$ to $M$ passes through the Cantor prefix
windows $C,B$, the fan-bar and finite-depth rows $F,D$, the witness windows
$W$, and the compact finite-net row $K$ before the readback row $R$ is
available.

Finally apply
\autoref{thm:fanfunctional-uniformmodulus-consumer-package}. The package
delivered to a $\UniformModulusUp$ consumer is exactly the displayed finite
NameCert packet, so no branch selector, countable-choice schedule, host
functional, or open-cover compactness object can be exported to the
Real-facing consumer.
\end{proof}

\begin{definition}[FanFunctional finite-window carrier]
\label{def:fanfunctional-finite-window-carrier}
A $\FanFunctionalUp$ finite-window carrier is a finite $\BHist$ packet
$$
\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)
$$
where $B$ is the $\CantorSpaceUp$ finite-prefix row, $Q$ is the inspected prefix-query ledger, $F,D$ are the $\FanTheoremUp$ finite bar and depth rows, $U,T$ are the $\UniformContinuityUp$ tolerance ledger and requested tolerance row, $K$ is the $\CompactMetricUp$ finite-net consumer route, $W$ is the finite witness-window family, $H$ transports all displayed rows componentwise by $\hsame$, $R$ replays the displayed $\Cont$ route, $P$ is the $\Pkg$ provenance over the displayed packets, and $N$ is the local $\NameCert_{\FanFunctionalUp}$ row. The classifier compares accepted packets only through those rows and has no coordinate for a host functional, selected branch, open-cover compactness proof, countable choice, or Real completeness.
\end{definition}

\begin{theorem}[FanFunctional Cantor-window obligation]
\label{thm:fanfunctional-cantor-window-obligation}
Every accepted $\FanFunctionalUp$ finite-window carrier must expose its $\CantorSpaceUp$ information as finite $\BHist$ rows $B,Q,W$ before the $\FanTheoremUp$ bar/depth rows $F,D$ or the local $\NameCert_{\FanFunctionalUp}$ row may be consumed. No consumer may replace those rows by a selected infinite branch or host functional evaluation.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fanfunctional-finite-window-carrier}. The only Cantor-facing coordinates are $B,Q,W$, and they are finite rows in the same $\BHist$ packet.

The bar and naming rows are downstream. The $\FanTheoremUp$ rows $F,D$ read prefix coverage and depth only from the displayed Cantor windows, while $H,R,P,N$ transport, replay, source, and name that same finite route.

Since the carrier contains no selected branch or host functional coordinate, any consumer that bypasses $B,Q,W$ leaves the accepted packet.
\end{proof}

\begin{theorem}[FanFunctional uniform-modulus obligation]
\label{thm:fanfunctional-uniform-modulus-obligation}
For every accepted $\FanFunctionalUp$ finite-window carrier $\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)$, the uniform-modulus read must pass through the $\UniformContinuityUp$ tolerance ledger $U,T$, the finite $\FanTheoremUp$ bar/depth rows $F,D$, the witness-window row $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $R$, package provenance $P$, and the local $\NameCert$ row $N$. The packet exports no host functional, branch selector, countable-choice schedule, or Real-completeness coordinate.
\end{theorem}
\begin{proof}
Read the tolerance face first. The rows $U,T$ are the only $\UniformContinuityUp$ coordinates that name the requested tolerance and the ledger supporting it.

Attach the fan and window rows. The finite bar/depth rows $F,D$ and witness row $W$ provide the only bounded prefix information that can answer the tolerance request.

Transport and replay do not enlarge the surface. The rows $H,R,P,N$ preserve, replay, source, and name the same finite packet, so the excluded host and choice principles cannot be recovered from an accepted uniform-modulus read.
\end{proof}

\begin{theorem}[FanFunctional compact-metric consumer obligation]
\label{thm:fanfunctional-compactmetric-consumer-obligation}
Any $\CompactMetricUp$ finite-net consumer of a $\FanFunctionalUp$ finite-window carrier receives only the displayed route $K,B,Q,F,D,U,T,W,H,R,P,N$. In particular the compact-metric handoff consumes finite Cantor prefixes, finite bar/depth rows, the uniform-continuity tolerance ledger, witness windows, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, and it receives no open-cover compactness theorem, selected branch, countable-choice selector, host functional, or Real completeness.
\end{theorem}
\begin{proof}
Project the compact-facing row of the carrier. The row $K$ is the only $\CompactMetricUp$ coordinate, and it is stored beside the finite Cantor, fan, tolerance, witness, transport, replay, provenance, and naming rows.

The consumer route is therefore finite. It reads $B,Q$ for prefixes, $F,D$ for bar and depth, $U,T$ for the tolerance ledger, and $W$ for the witness windows before receiving $K$.

Finally $H,R,P,N$ keep the handoff on the same packet. Since no open-cover compactness proof, selected branch, choice selector, host functional, or Real-completeness row appears in $\Omega$, the compact-metric consumer receives only the displayed route.
\end{proof}

\begin{theorem}[FanFunctional NameCert nonescape]
\label{thm:fanfunctional-namecert-nonescape}
The local $\NameCert_{\FanFunctionalUp}$ row of an accepted finite-window carrier $\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)$ is exhausted by finite $\BHist$ rows for $\CantorSpaceUp$ prefixes, $\FanTheoremUp$ bar/depth data, $\UniformContinuityUp$ tolerance ledger, $\CompactMetricUp$ finite-net consumption, witness windows, componentwise $\hsame$, displayed $\Cont$, $\Pkg$ provenance, and local naming. It cannot expose a host functional, selected branch, open-cover compactness proof, countable choice, or Real-completeness principle.
\end{theorem}
\begin{proof}
Combine the three obligation blocks. The Cantor-window obligation, \autoref{thm:fanfunctional-cantor-window-obligation}, fixes the finite prefix rows that feed the fan bar/depth data.

The uniform-modulus obligation, \autoref{thm:fanfunctional-uniform-modulus-obligation}, fixes the tolerance ledger and witness-window route. The compact-metric consumer obligation, \autoref{thm:fanfunctional-compactmetric-consumer-obligation}, fixes the finite-net consumer route.

All three routes use the same structural rows $H,R,P,N$. Therefore the local NameCert row names exactly the displayed finite packet, and any host functional, selected branch, open-cover compactness proof, countable-choice selector, or Real-completeness principle would require a coordinate outside the accepted carrier.
\end{proof}

\begin{theorem}[FanFunctional root schema carrier]
\label{thm:fanfunctional-root-schema-carrier}
The root schema row for $\FanFunctionalUp$ is the finite Cantor--fan--modulus carrier
$$
\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)
$$
of \autoref{def:fanfunctional-finite-window-carrier}. Its root-facing fields are exactly the $\CantorSpaceUp$ finite-prefix rows $B,Q,W$, the $\FanTheoremUp$ bar and depth rows $F,D$, the $\UniformModulusUp$ and $\UniformContinuityUp$ tolerance rows $U,T$, the $\CompactMetricUp$ finite-net consumer row $K$, and the structural rows $H,R,P,N$.
\end{theorem}
\begin{proof}
Project \autoref{def:fanfunctional-finite-window-carrier}. The tuple displayed there already separates the Cantor prefix coordinates, fan bar-depth coordinates, uniform-modulus coordinates, compact-metric consumer coordinate, and structural coordinates.

No root schema row is added outside the tuple. The carrier has no coordinate for a host functional, selected branch, open-cover compactness theorem, Real completeness, or countable-choice schedule, so the listed rows are exactly the root-facing carrier fields.
\end{proof}

\begin{theorem}[FanFunctional root schema classifier]
\label{thm:fanfunctional-root-schema-classifier}
Two accepted $\FanFunctionalUp$ root schema carriers classify together whenever their $\CantorSpaceUp$ prefix windows, $\FanTheoremUp$ finite bar depths, $\UniformContinuityUp$ tolerance rows, $\UniformModulusUp$ witness windows, $\CompactMetricUp$ finite-net rows, and structural rows $H,R,P,N$ transport componentwise by $\hsame$ over the displayed finite windows. The classifier has no branch-selector, host-functional, open-cover, or Real-completeness coordinate.
\end{theorem}
\begin{proof}
Compare the two carriers coordinate by coordinate. Agreement on the Cantor rows fixes the inspected prefix windows, and agreement on the fan rows fixes the finite bar depth available to the modulus read.

Transport the tolerance and witness rows through the same windows. Componentwise $\hsame$ keeps $U,T,W$ aligned with the displayed bar depth, and the compact-metric row $K$ reads only that finite packet.

The structural rows preserve the comparison. Since $H,R,P,N$ transport, replay, source, and name exactly the displayed fields, a classifier disagreement would require a coordinate outside the root schema carrier.
\end{proof}

\begin{theorem}[FanFunctional root schema ledger]
\label{thm:fanfunctional-root-schema-ledger}
Every downstream root-schema consumer of $\FanFunctionalUp$ must pass through $\CompactMetricUp$, $\UniformContinuityUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ as displayed by $\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)$. The ledger records non-escape from finite Cantor windows and finite bar depth to the compact and uniform-modulus consumers, and it exports no host functional, selected infinite branch, open-cover compactness proof, countable-choice selector, or Real-completeness principle.
\end{theorem}
\begin{proof}
Start from the root schema carrier. The compact consumer row $K$ is downstream from $B,Q,F,D,U,T,W$, so it cannot be read before the Cantor window, fan bar depth, tolerance, and witness rows are displayed.

Next read the uniform-modulus side. The rows $U,T,W$ provide the finite tolerance and witness-window route, and the $\UniformModulusUp$ consumer receives that route through the same finite bar depth.

Finally attach the structural ledger. Rows $H,R,P,N$ keep the consumer inside the finite packet by componentwise transport, displayed continuation replay, package provenance, and local naming, so the excluded host and choice objects have no ledger slot.
\end{proof}

\input{parts/concrete_instances/fanfunctional/root_modulus_obligation_surface.tex}
\input{parts/concrete_instances/fanfunctional/root_unblock_retention_surface.tex}
\input{parts/concrete_instances/fanfunctional/root_obligation_completion_surface.tex}

\begin{theorem}[FanFunctional root bar-depth exactness]
\label{thm:fanfunctional-root-bar-depth-exactness}
For an accepted finite-window carrier $\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)$, the root read exposes the $\FanTheoremUp$ bar-depth row $D$ before any $\UniformModulusUp$ or $\UniformContinuityUp$ modulus row is consumed. The depth seen by a modulus consumer is exactly the finite depth displayed by $F,D$ over the Cantor prefix rows $B,Q$, not a selected branch, host functional, open-cover compactness proof, or Real-completeness witness.
\end{theorem}
\begin{proof}
Project the finite-window carrier. Rows $B,Q$ display the inspected Cantor prefixes, while $F,D$ display the decidable bar and its finite depth before the tolerance and witness rows are read.

The modulus rows $U,T,W$ are downstream from that display. Transport through $H,R$ and provenance through $P,N$ preserve the same finite depth, so a consumer cannot substitute an external branch selector or compactness proof for $D$.
\end{proof}

\begin{theorem}[FanFunctional root modulus-window coverage]
\label{thm:fanfunctional-root-modulus-window-coverage}
Every root-schema modulus read of $\FanFunctionalUp$ covers exactly the Cantor prefixes displayed in the finite windows $B,Q,W$ of $\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)$. The witness windows neither omit a displayed prefix before the modulus row is exported nor add a prefix outside the bar-depth boundary exposed by $F,D$.
\end{theorem}
\begin{proof}
Use the root schema carrier and classifier theorems. The carrier fixes the prefix rows $B,Q,W$, and the classifier compares only those finite windows together with the bar-depth and tolerance rows.

The root schema ledger keeps the compact and uniform-modulus consumers on that route. Since no coordinate names an infinite branch, countable choice schedule, or open-cover compactness proof, coverage is exactly the displayed finite prefix family.
\end{proof}

\begin{theorem}[FanFunctional root NameCert acceptance]
\label{thm:fanfunctional-root-namecert-acceptance}
The local root-facing $\NameCert_{\FanFunctionalUp}$ acceptance row receives only $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\CompactMetricUp$, $\UniformContinuityUp$, and $\UniformModulusUp$ rows carried by $\Omega=(B,Q,F,D,U,T,K,W,H,R,P,N)$. It does not receive a host functional, selected branch, arbitrary open-cover compactness row, choice selector, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Combine the root schema carrier, classifier, and ledger. They expose the finite Cantor prefix, bar-depth, tolerance, witness-window, compact-metric, uniform-continuity, and uniform-modulus rows before the local name is read.

The structural rows $H,R,P,N$ provide exactly displayed replay, componentwise transport, package provenance, and local naming. Because the carrier has no host-functional, branch-selector, open-cover, choice, or Real-completeness coordinate, local NameCert acceptance receives only the listed rows.
\end{proof}

\begin{closurestatus}{\FanFunctionalUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fan-functional-carrier-surface}, \autoref{thm:fan-functional-namecert-obligations}, \autoref{thm:fan-functional-uniform-modulus-extraction}, and \autoref{thm:fan-functional-compact-continuity-consumer} seed the finite Cantor-prefix fan-functional packet for constructive modulus extraction.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close host function spaces, Brouwer bar induction, open-cover compactness, choice-selected branches, real completeness, or a standard bridge.}
  \upgradepath{The next step is obligationClosure: carrier admission, classifier stability, bar-depth exactness, witness-window coverage, uniform-modulus extraction, and non-escape must be discharged as local rows.}
  \constructivestory{A $\FanFunctionalUp$ packet is generated from finite $\CantorSpaceUp$ prefix rows, a $\FanTheoremUp$ decidable finite-bar depth ledger, a tolerance request, finite witness windows, an extracted $\UniformModulusUp$ row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
