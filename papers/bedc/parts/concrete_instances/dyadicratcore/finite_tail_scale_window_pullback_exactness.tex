\begin{theorem}[DyadicRatCore finite-tail scale-window pullback exactness]
\label{thm:dyadicratcore-finite-tail-scale-window-pullback-exactness}
Let two finite-tail $\RealUp$ L10 reads have $\DyadicRatCoreUp$ faces obtained from displayed DyadicRatCore packets by finite positive-unary exponent widenings. Suppose the two reads share the same terminal scale-window packet of \autoref{def:dyadicratcore-scale-window-packet} and the same $\RegSeqRatUp$ finite-request section. Then their $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces determine the same finite pullback route over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

The exactness is only the finite BEDC pullback exactness of the displayed route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
It exports no quotient stream equality, selected limit, host rational equality, ambient completeness row, or $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
First apply scale-window terminal fusion, \autoref{thm:dyadicratcore-scale-window-terminal-fusion}. The common terminal scale-window packet identifies the widened DyadicRatCore comparison face through displayed scale transports, componentwise $\hsame$ rows, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ data.

Next apply finite-tail window factorization, \autoref{thm:dyadicratcore-finite-tail-window-factorization}. Each finite-tail $\RealUp$ L10 read factors through the same ordered four-face packet: the terminal $\DyadicRatCoreUp$ face, the fused $\StreamNameUp$ finite-window packet, the displayed $\RegSeqRatUp$ finite-request section, and the $\RealUp$ L10 finite-window boundary.

Use finite-tail window confluence, \autoref{thm:dyadicratcore-finite-tail-window-confluence}, on the two factorizations. The shared terminal scale-window packet already fixes the DyadicRatCore and StreamName faces, while the hypothesis fixes the RegSeqRat finite-request section.

Finally invoke the StreamName and Real completion pullback rows, \autoref{thm:streamname-real-completion-four-object-terminal-pullback}, \autoref{thm:streamname-real-completion-classifier-pullback}, and \autoref{thm:real-l10-finite-window-witness-determinacy}. These rows identify the Real-facing boundary exactly from the common four faces and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

The cited exclusion clauses remove quotient stream equality, selected limits, host rational equality, ambient completeness, and Real completeness from each step. Therefore the two reads determine the same finite pullback route and nothing outside the displayed BEDC rows.
\end{proof}

\begin{theorem}[DyadicRatCore finite-tail common scale-window reduction]
\label{thm:dyadicratcore-finite-tail-common-scale-window-reduction}
Let two finite-tail $\RealUp$ L10 reads have $\DyadicRatCoreUp$ faces obtained from displayed DyadicRatCore packets by finite positive-unary exponent widenings. Suppose both widened faces are read through the same displayed $\StreamNameUp$ finite-window packet and share a terminal scale-window packet of \autoref{def:dyadicratcore-scale-window-packet}. Then the two reads determine the same visible $\RegSeqRatUp$ finite-request section.

Consequently the hypotheses of \autoref{thm:dyadicratcore-finite-tail-scale-window-pullback-exactness} reduce to the common displayed scale-window route together with the common $\StreamNameUp$ finite-window packet. The reduction exports no quotient stream equality, selected limit, host rational equality, ambient completeness row, or $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
First apply scale-window terminal fusion, \autoref{thm:dyadicratcore-scale-window-terminal-fusion}. The common terminal packet identifies the widened $\DyadicRatCoreUp$ comparison face for both reads using only displayed scale transports, componentwise $\hsame$ rows, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ data.

Next use finite-tail window factorization, \autoref{thm:dyadicratcore-finite-tail-window-factorization}, on each read. Since both reads pass through the same $\StreamNameUp$ finite-window packet, the factorization exposes one downstream finite request surface for the $\RegSeqRatUp$ handoff rather than two independent request sections.

Now apply RegSeqRat bridge handoff exactness, \autoref{thm:dyadicratcore-regseqrat-bridge-handoff-exactness}. The handoff consumes exactly the public DyadicRatCore envelope visible through the shared StreamName packet, so the displayed signed mantissa row, positive unary exponent row, denominator ledger, common-exponent classifier, arithmetic window, distance window, radius window, $\BHist$ carrier data, $\hsame$ transport, $\Cont$ readback, and $\Pkg$ provenance are the same on both sides.

Therefore the visible $\RegSeqRatUp$ finite-request section is common. Feeding this derived section match, together with the already common terminal scale-window packet, into \autoref{thm:dyadicratcore-finite-tail-scale-window-pullback-exactness} gives the same finite pullback route over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and the local $\NameCert$ rows.

The exclusion clauses of terminal fusion, factorization, and bridge handoff are preserved by this reduction. Hence the argument does not assert quotient stream equality, selected limits, host rational equality, ambient completeness, or any $\RealUp$ completeness principle.
\end{proof}

\begin{theorem}[DyadicRatCore finite-tail cofinal section]
\label{thm:dyadicratcore-finite-tail-cofinal-section}
Let a finite-tail $\RealUp$ L10 read have a $\DyadicRatCoreUp$ face lying on a finite positive-unary cofinal scale-window route. Then the visible cofinal route determines one $\RegSeqRatUp$ finite-request section and one $\RealUp$ seal-visible source section through
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
The determined sections use only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows of the route.

No quotient stream equality, selected limit, host rational equality, ambient completion row, metric-completeness datum, or $\RealUp$ completeness principle is part of the section data.
\end{theorem}
\begin{proof}
Start from the finite positive-unary cofinal scale-window route. By scale-window cofinality, \autoref{thm:dyadicratcore-scale-window-cofinality}, the visible $\DyadicRatCoreUp$ face reaches the terminal scale-window packet through displayed scale transports, componentwise $\hsame$ rows, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ data.

Next apply finite-tail window factorization, \autoref{thm:dyadicratcore-finite-tail-window-factorization}. The finite-tail $\RealUp$ read is therefore routed through the fused $\StreamNameUp$ finite-window packet, then through the $\RegSeqRatUp$ finite-request section, and finally through the $\RealUp$ L10 finite-window boundary.

Use the common scale-window reduction, \autoref{thm:dyadicratcore-finite-tail-common-scale-window-reduction}, to identify the $\RegSeqRatUp$ section as the one forced by the visible cofinal route. The reduction removes any choice of a second request section once the displayed scale-window route and the displayed $\StreamNameUp$ finite-window packet are fixed.

Finally apply StreamName finite-request section budget exactness, \autoref{thm:streamname-finite-request-section-budget-exactness}, and StreamName finite-request section pullback, \autoref{thm:streamname-finite-request-section-pullback}. These rows identify the Real-facing section as the same displayed packet over the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ faces.

The cited nonescape clauses exclude quotient stream equality, selected limits, host rational equality, ambient completion, metric-completeness data, and RealUp completeness from every step. Thus the cofinal finite-tail route determines exactly the finite-request and seal-visible source sections described above.
\end{proof}

\begin{theorem}[DyadicRatCore RegSeqRat Real pullback composition]
\label{thm:dyadicratcore-regseqrat-real-pullback-composition}
Let a finite-tail $\RealUp$ L10 read have a $\DyadicRatCoreUp$ face lying on a finite positive-unary scale-window route. Suppose the route displays its terminal $\DyadicRatCoreUp$ scale-window packet, the induced $\StreamNameUp$ finite-window packet, the visible $\RegSeqRatUp$ finite-request section, and the $\RealUp$ seal-visible source section determined by the finite L10 boundary. Then these four faces compose as one BEDC pullback route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
over the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

The composed route is exactly the finite pullback handoff of the displayed packets. It does not identify quotient streams, select a limit, assert host rational equality, add ambient completion, import metric completeness, or prove $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the finite positive-unary scale-window route. By \autoref{thm:dyadicratcore-finite-tail-cofinal-section}, the visible route determines a single $\RegSeqRatUp$ finite-request section and a single $\RealUp$ seal-visible source section through the four displayed faces.

Next apply the common scale-window reduction, \autoref{thm:dyadicratcore-finite-tail-common-scale-window-reduction}. It turns the common terminal $\DyadicRatCoreUp$ packet and the induced $\StreamNameUp$ finite-window packet into the same visible $\RegSeqRatUp$ request section, so the middle face is not a second choice point.

Use StreamName finite-request section budget exactness, \autoref{thm:streamname-finite-request-section-budget-exactness}, to bind the StreamName packet to the same finite request budget. The request budget is therefore the one consumed by the RegSeqRat section already forced by the scale-window route.

Now invoke finite-tail scale-window pullback exactness, \autoref{thm:dyadicratcore-finite-tail-scale-window-pullback-exactness}. Its hypotheses are exactly the terminal scale-window packet, the induced StreamName packet, and the visible RegSeqRat section built above, so it packages the DyadicRatCore, StreamName, RegSeqRat, and Real faces as one finite pullback route over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

Finally apply Real L10 finite-window witness determinacy, \autoref{thm:real-l10-finite-window-witness-determinacy}. This fixes the seal-visible Real source section reached by the composed route and prevents a second Real-facing witness from entering the handoff.

The exclusion clauses in the cited results remove quotient stream equality, selected limits, host rational equality, ambient completion, metric completeness, and $\RealUp$ completeness at each step. The conclusion is therefore only the composed finite BEDC pullback route.
\end{proof}

\begin{theorem}[DyadicRatCore finite-tail endpoint invariance]
\label{thm:dyadicratcore-finite-tail-endpoint-invariance}
Let two finite-tail $\RealUp$ L10 reads have $\DyadicRatCoreUp$ faces on finite positive-unary scale-window routes. Suppose the two presentations display the same terminal $\DyadicRatCoreUp$ scale-window packet, the same induced $\StreamNameUp$ finite-window packet, the same $\RegSeqRatUp$ finite-request section, and the same $\RealUp$ seal-visible source section. Suppose also that their endpoint-facing rows share the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support over these four faces. Then every endpoint-facing $\RealUp$ consumer reads the same finite BEDC endpoint route from the two presentations.

The invariant endpoint is only the finite route exposed by the four L10 faces. It is not quotient stream equality, a selected limit, host rational equality, ambient completion, metric completeness, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First apply finite-tail scale-window pullback exactness, \autoref{thm:dyadicratcore-finite-tail-scale-window-pullback-exactness}. The shared terminal $\DyadicRatCoreUp$ packet and shared $\RegSeqRatUp$ finite-request section identify the same finite pullback route over the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

Next apply the common scale-window reduction, \autoref{thm:dyadicratcore-finite-tail-common-scale-window-reduction}. Since the two presentations also share the induced $\StreamNameUp$ finite-window packet, the RegSeqRat request section is forced by the visible scale-window route rather than supplied as an independent endpoint choice.

Now use the RegSeqRat--Real pullback composition, \autoref{thm:dyadicratcore-regseqrat-real-pullback-composition}. It composes the shared DyadicRatCore packet, StreamName finite-window packet, RegSeqRat finite-request section, and Real seal-visible source section as one BEDC pullback route through the endpoint-facing rows.

Finally invoke Real L10 finite-window witness determinacy, \autoref{thm:real-l10-finite-window-witness-determinacy}. Any $\RealUp$ consumer that only sees the finite L10 endpoint boundary must therefore read the same seal-visible endpoint route from either presentation.

The cited nonescape clauses exclude quotient stream equality, selected limits, host rational equality, ambient completion, metric completeness, and $\RealUp$ completeness. Hence the conclusion is endpoint invariance of the displayed finite BEDC route, not equality in any ambient completion.
\end{proof}

\begin{theorem}[DyadicRatCore finite-request Real-seal uniqueness]
\label{thm:dyadicratcore-finite-request-real-seal-uniqueness}
Let two finite-tail $\RealUp$ L10 reads have $\DyadicRatCoreUp$ faces on finite positive-unary scale-window routes. Suppose the two presentations share the same terminal $\DyadicRatCoreUp$ scale-window packet, the same induced $\StreamNameUp$ finite-window packet, the same visible $\RegSeqRatUp$ finite-request section, and the same seal-visible $\RealUp$ source section. Suppose also that the displayed consumer rows expose the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support over these four faces. Then every Real-seal consumer reads one unique finite request route through
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
over the displayed rows.

The unique route is only the finite Real-seal request route visible to the consumer. It does not identify quotient streams, choose a selected limit, assert host rational equality, add ambient completion, import metric completeness, or prove $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First apply the finite-tail common scale-window reduction, \autoref{thm:dyadicratcore-finite-tail-common-scale-window-reduction}. The shared terminal $\DyadicRatCoreUp$ packet and shared induced $\StreamNameUp$ packet force one visible $\RegSeqRatUp$ finite-request section, so the middle request surface cannot split into two consumer routes.

Next use the RegSeqRat--Real pullback composition, \autoref{thm:dyadicratcore-regseqrat-real-pullback-composition}. It composes the terminal DyadicRatCore packet, StreamName finite-window packet, RegSeqRat finite-request section, and seal-visible Real source section as one finite BEDC pullback route over the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

Finally apply finite-tail endpoint invariance, \autoref{thm:dyadicratcore-finite-tail-endpoint-invariance}. Since the two presentations expose the same endpoint-facing support over the same four faces, any Real-seal consumer that reads only the displayed finite boundary must read the same finite request route from both presentations.

The nonescape clauses in the three cited results exclude quotient stream equality, selected limits, host rational equality, ambient completion, metric completeness, and $\RealUp$ completeness. Therefore the uniqueness is exactly Real-seal finite-request uniqueness over the displayed BEDC rows.
\end{proof}

\begin{theorem}[DyadicRatCore Real-completion obstruction ledger]
\label{thm:dyadicratcore-real-completion-obstruction-ledger}
For a current Phase Real and Completion exit read whose dyadic face is displayed by a $\DyadicRatCoreUp$ packet, failure is localized exactly at one of the following finite sites:
\begin{enumerate}
\item the dyadic source row or its componentwise transport is absent;
\item the induced $\StreamNameUp$ finite-window face is absent or does not read the same finite window;
\item the $\RegSeqRatUp$ finite-request section is absent or leaves the displayed request surface;
\item the local $\RealUp$ seal is absent or does not seal the displayed finite request;
\item one of the support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, or $\NameCert$ is absent or is not transported with the four faces.
\end{enumerate}
Equivalently, once the dyadic source and transport row, the $\StreamNameUp$ finite-window face, the $\RegSeqRatUp$ finite-request section, the local $\RealUp$ seal, and the listed support rows are all present over the same displayed packet, the exit read has no further obstruction.

The ledger is only a finite obstruction account for the displayed BEDC route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
It exports no quotient stream equality, host real equality, selected limit, countable-choice selector, ambient completeness principle, metric-completeness row, or bridge row beyond the displayed dyadic-facing packet.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:dyadicratcore-regseqrat-handoff-failure-localization}. It localizes any failed public dyadic handoff to the dyadic source inventory, the $\StreamNameUp$ finite-window face, the $\RegSeqRatUp$ readback face, the local $\RealUp$ seal, or the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Next apply \autoref{thm:streamname-real-completion-failure-localization}. This keeps the StreamName-facing Real-completion read on the same four-object exit surface and rules out moving the failure into an undisplayed completion object or choice of a limit.

Use \autoref{thm:streamname-regseqrat-real-dyadic-completion-face-boundary} to align the four faces. The StreamName schedule rows, DyadicRatCore endpoint and radius rows, RegSeqRat classifier and regularity rows, and local Real seal row are necessary and sufficient for the same finite $\BHist$ route.

Finally use \autoref{thm:real-l10-finite-window-witness-determinacy}. Once the local $\RealUp$ seal-visible finite window is fixed by those faces, no second Real-facing witness remains from which a distinct failure site could be read.

For the reverse direction, suppose one of the five listed finite sites is missing or not transported componentwise. The corresponding local theorem blocks the route at exactly that face: the DyadicRatCore handoff theorem blocks source and support defects, the StreamName localization theorem blocks finite-window defects, the RegSeqRat boundary theorem blocks finite-request defects, and the Real L10 determinacy theorem blocks local seal defects.

Therefore absence of any listed site is an obstruction, while presence of all five sites composes the same finite exit route by \autoref{thm:dyadicratcore-finite-request-real-seal-uniqueness}. The exclusion clauses of the four cited results remove quotient stream equality, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, and extra bridge rows from the ledger.
\end{proof}

\begin{theorem}[DyadicRatCore finite-tail obstruction minimality]
\label{thm:dyadicratcore-finite-tail-obstruction-minimality}
For a current Phase Real and Completion exit read whose dyadic face is displayed by a $\DyadicRatCoreUp$ packet, each finite site in \autoref{thm:dyadicratcore-real-completion-obstruction-ledger} is independently necessary. The dyadic source and componentwise transport row, the induced $\StreamNameUp$ finite-window face, the $\RegSeqRatUp$ finite-request section, the local $\RealUp$ seal, and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support cannot be supplied by any combination of the other four sites.

When all five sites are present over the same displayed packet, the obstruction ledger and \autoref{thm:dyadicratcore-finite-request-real-seal-uniqueness} leave no further finite obstruction for the displayed route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
This minimality claim is only about the finite BEDC exit route. It does not identify quotient streams, select a limit, assert host rational or host real equality, add an ambient completion row, import a metric-completeness row, or add a bridge row.
\end{theorem}
\begin{proof}
The obstruction ledger, \autoref{thm:dyadicratcore-real-completion-obstruction-ledger}, gives the five-site classification. It says that every failed exit read is localized at exactly the dyadic source and transport row, the StreamName finite-window face, the RegSeqRat finite-request section, the local Real seal, or the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Independence follows by testing one missing site at a time. Without the dyadic source and componentwise transport row, the DyadicRatCore handoff cannot present the signed mantissa, positive unary exponent, denominator ledger, and common-exponent classifier consumed downstream, so the remaining four sites have no source inventory to transport.

Without the $\StreamNameUp$ finite-window face, finite-tail common scale-window reduction, \autoref{thm:dyadicratcore-finite-tail-common-scale-window-reduction}, has no displayed finite window through which the dyadic packet can expose the RegSeqRat request surface. Supplying the other four sites would have to replace that face by quotient stream equality or a selected limit, both excluded by the route theorems.

Without the $\RegSeqRatUp$ finite-request section, RegSeqRat--Real pullback composition, \autoref{thm:dyadicratcore-regseqrat-real-pullback-composition}, has no middle request row to compose with the StreamName packet and the Real seal. Replacing it would require host rational or host real equality outside the displayed BEDC rows.

Without the local $\RealUp$ seal, Real L10 finite-window witness determinacy cannot bind the endpoint-facing witness. The other four sites therefore do not create a Real-facing seal by themselves, and any substitute would amount to an ambient completion or metric-completeness row excluded from the finite route.

Without the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support, the four visible faces are not transported over one packet. Replacing those rows by the faces alone would require an extra bridge row rather than the displayed local support.

Conversely, when all five sites are present over the same displayed packet, \autoref{thm:dyadicratcore-real-completion-obstruction-ledger} removes every finite obstruction listed in the classification, and \autoref{thm:dyadicratcore-finite-request-real-seal-uniqueness} gives one finite request route through the displayed faces. Hence none of the five sites is replaceable by the other four, and together they are sufficient for the finite exit route claimed here.
\end{proof}

\begin{theorem}[DyadicRatCore finite-tail obstruction classifier determinacy]
\label{thm:dyadicratcore-finite-tail-obstruction-classifier-determinacy}
Let two current Phase Real and Completion exit reads have first visible face a finite-tail $\DyadicRatCoreUp$ scale-window packet. Suppose the two reads expose the same terminal $\DyadicRatCoreUp$ packet, the same induced $\StreamNameUp$ finite-window face, the same $\RegSeqRatUp$ finite-request section, the same local $\RealUp$ seal-facing section, and the same transported $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support rows over those four faces. Then the two obstruction classifiers agree exactly when the two reads fail at the same one of the five finite sites listed in \autoref{thm:dyadicratcore-real-completion-obstruction-ledger} over that shared support surface.

The classifier comparison is only the finite BEDC comparison attached to
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
It excludes quotient rational equality, quotient stream equality, selected limits, countable choice, metric-completeness rows, aggregate formalstatus rows, aggregate bridge rows, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
For the forward direction, apply the obstruction ledger, \autoref{thm:dyadicratcore-real-completion-obstruction-ledger}, to each failed read. Each read is classified by exactly one displayed finite site: the dyadic source and transport row, the $\StreamNameUp$ finite-window face, the $\RegSeqRatUp$ finite-request section, the local $\RealUp$ seal, or the transported support rows.

Now compare the two ledger classifications over the shared four-face support. If the classifiers agree while the sites differed, one of the five sites would be replaceable by the other four together with a different failed site. This contradicts finite-tail obstruction minimality, \autoref{thm:dyadicratcore-finite-tail-obstruction-minimality}, which makes each listed site independently necessary.

For the reverse direction, assume the two reads fail at the same listed finite site over the same $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. Replay the local blocking theorem for that site on both reads: the DyadicRatCore handoff block for source and transport defects, the StreamName localization block for finite-window defects, the RegSeqRat boundary block for finite-request defects, the Real L10 determinacy block for local seal defects, and the support-transport block for shared row defects.

The same local blocking theorem reads the same failed coordinate in both presentations, so the obstruction classifiers agree. The nonescape clauses of the ledger and minimality theorem keep the result finite: it is a BEDC classifier determinacy statement, not a quotient-equality, selected-limit, choice, bridge, or completeness theorem.
\end{proof}

\begin{theorem}[DyadicRatCore finite-tail exit-certificate equivalence]
\label{thm:dyadicratcore-finite-tail-exit-certificate-equivalence}
Let a current Phase Real and Completion exit read have first visible face a finite-tail $\DyadicRatCoreUp$ scale-window packet. Then the read satisfies the objectwise exit certificate exactly when the five finite sites of \autoref{thm:dyadicratcore-real-completion-obstruction-ledger} are present over the same four-face packet
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
and the four faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$ each supply their local scoped-closure and audit-clean threshold rows. The support surface is exactly the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows transported with those four faces.

This equivalence exports no quotient streams, selected limits, countable-choice selector, metric-completeness data, aggregate formalstatus row, aggregate bridge row, or $\RealUp$ completeness theorem. The local $\RealUp$ face remains a face of the finite packet, not an ambient completeness object.
\end{theorem}
\begin{proof}
For necessity, start with an accepted objectwise exit certificate. Apply the Real L10 objectwise face-status exit theorem, \autoref{thm:real-l10-objectwise-face-status-exit}, to read the certificate as four local face records over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$, together with the finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Now use the DyadicRatCore obstruction ledger, \autoref{thm:dyadicratcore-real-completion-obstruction-ledger}. Since the read has first visible face a finite-tail $\DyadicRatCoreUp$ scale-window packet, any missing dyadic source row, StreamName finite-window face, RegSeqRat finite-request section, local Real seal, or transported support row would be exactly one of the ledger obstructions. Therefore an accepted objectwise certificate contains all five finite sites over the same four-face packet.

For sufficiency, assume the five ledger sites are present over the same displayed packet and that each of the four faces supplies its local scoped-closure and audit-clean threshold rows. The ledger removes every finite obstruction, while finite-request Real-seal uniqueness, \autoref{thm:dyadicratcore-finite-request-real-seal-uniqueness}, gives a single route through
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$

Apply the Real L10 current-phase exit local threshold theorem, \autoref{thm:real-l10-current-phase-exit-local-threshold}. It identifies precisely those four local threshold records, read with the displayed support rows, as the current Phase Real and Completion objectwise exit read.

Minimality comes from \autoref{thm:dyadicratcore-finite-tail-obstruction-minimality}. Removing any one of the five finite sites leaves a ledger obstruction that the other four sites cannot replace, so the equivalence cannot be shortened by substituting a different face or support coordinate.

Finally, the Real L10 threshold theorem keeps the closure and verification axes facewise, and the DyadicRatCore nonescape clauses keep the route finite. Hence the accepted roadmap exit read is exactly the stated four-face certificate, with no quotient stream, selected limit, countable-choice selector, metric-completeness datum, aggregate formalstatus row, aggregate bridge row, or $\RealUp$ completeness coordinate.
\end{proof}
