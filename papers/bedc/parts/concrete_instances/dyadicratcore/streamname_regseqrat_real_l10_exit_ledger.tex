\begin{theorem}[DyadicRatCore StreamName RegSeqRat Real L10 exit ledger]
\label{thm:dyadicratcore-streamname-regseqrat-real-l10-exit-ledger}
Every accepted L10 exit read whose visible faces are $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ has a unique BEDC-visible dyadic source ledger. The forced ledger consists exactly of the signed mantissa row, refined positive-unary exponent row, denominator ledger, common-exponent comparison, distance and radius rows, $\StreamNameUp$ finite window, $\RegSeqRatUp$ classifier and readback rows, local $\RealUp$ seal row, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows.

The exit cannot be supplied by quotient rational equality, quotient stream equality, host real equality, a selected limit, countable choice, metric-completeness rows, standard bridge rows, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-common-scale-window-triangle-ledger} to the dyadic source faces visible in the L10 exit read. It supplies the common finite scale-window packet with signed mantissa, refined positive-unary exponent, denominator, comparison, distance, radius, transport, continuation, package, and local DyadicRatCore naming rows.

Next use \autoref{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence}. The same finite packet is read as the four displayed faces
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp ,
$$
so the StreamName finite window, RegSeqRat classifier and readback surface, Real seal row, and displayed support coordinates are not additional sources.

Finally apply \autoref{thm:dyadicratcore-scale-window-real-exit-source-uniqueness} and \autoref{thm:dyadicratcore-terminal-real-exit-no-fifth-source}. Once the four visible faces and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows agree, the dyadic scale-window source is forced to be the same common finite triangle ledger, and the exit inventory has no fifth source.

The non-escape clause is the combined exclusion inventory of the cited triangle ledger, four-face correspondence, source-uniqueness theorem, and no-fifth-source theorem. Those clauses leave no row for quotient rational equality, quotient stream equality, host real equality, selected limits, countable choice, metric completeness, standard bridges, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[DyadicRatCore L10 exit NameCert interface]
\label{thm:dyadicratcore-l10-exit-namecert-interface}
An accepted Phase Real and Completion exit packet is a local $\NameCert$ interface exactly when the following facewise rows are present on one BEDC-visible support surface: the $\DyadicRatCoreUp$ signed mantissa, exponent, denominator, comparison, distance, and radius rows; the $\StreamNameUp$ finite-window rows; the $\RegSeqRatUp$ classifier and readback rows; the local $\RealUp$ seal rows; and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support rows.

Equivalently, the four faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ jointly present the certificate surface consumed by the Phase Real and Completion exit criterion. Deleting any face, aggregating two faces into one status row, or replacing a face by a global equality datum fails to present that interface. The interface has no coordinate for quotient rational equality, quotient stream equality, host real equality, selected limits, countable choice, metric-completeness data, standard bridge rows, aggregate formalstatus rows, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
For sufficiency, start with the displayed four-face inventory and support rows. Apply \autoref{thm:dyadicratcore-streamname-regseqrat-real-l10-exit-ledger} to read the dyadic source surface as the unique BEDC-visible ledger of the L10 exit.

Next apply \autoref{thm:streamname-real-completion-four-object-terminal-pullback}. It repacks the same DyadicRatCore, StreamName, RegSeqRat, and Real rows as the terminal four-object boundary seen by every accepted Phase Real and Completion consumer.

Use \autoref{thm:regseqrat-terminal-four-face-correspondence} to identify the RegSeqRat classifier and readback face with that terminal boundary. The classifier is therefore not an external parameter transported into the packet; it is one of the four exposed faces of the same local certificate surface.

Finally apply \autoref{thm:real-l10-exit-certificate-face-minimality}. Minimality says that each of the four faces and each displayed support row is indispensable for the current Real L10 exit certificate, so the assembled packet is precisely the local $\NameCert$ interface.

For necessity, assume an accepted packet presents the local $\NameCert$ interface. The StreamName terminal pullback forces the packet to factor through the four-object boundary, and the RegSeqRat correspondence reads the classifier surface facewise inside that boundary.

The DyadicRatCore ledger theorem then recovers the signed mantissa, exponent, denominator, comparison, distance, and radius rows as the unique dyadic source ledger. Real-side face minimality prevents deletion or aggregation of any of the four faces, and it also rules out replacing a face by quotient rational equality, quotient stream equality, host real equality, selected limits, countable choice, metric-completeness data, standard bridge rows, aggregate formalstatus rows, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[DyadicRatCore L10 exit ledger facewise noncollapse]
\label{thm:dyadicratcore-l10-exit-ledger-facewise-noncollapse}
An accepted Phase Real and Completion exit ledger presents the $\DyadicRatCoreUp$ face if and only if the signed mantissa, exponent, denominator, comparison, distance, and radius rows remain a distinct face alongside the $\StreamNameUp$ finite window, $\RegSeqRatUp$ classifier readback, $\RealUp$ seal, and displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

In this correspondence no aggregate formalstatus row, bridge row, quotient rational equality, quotient stream equality, selected limit, metric-completeness datum, countable-choice row, or ambient $\RealUp$ completeness row can replace the $\DyadicRatCoreUp$ face.
\end{theorem}
\begin{proof}
For the forward direction, assume the accepted exit ledger presents the $\DyadicRatCoreUp$ face. Apply \autoref{thm:dyadicratcore-streamname-regseqrat-real-l10-exit-ledger}. The dyadic source surface is then forced to be the unique BEDC-visible ledger consisting of the signed mantissa, exponent, denominator, comparison, distance, and radius rows together with the displayed support rows.

Next apply \autoref{thm:dyadicratcore-l10-exit-namecert-interface}. Since the packet presents the local $\NameCert$ interface, those dyadic rows must appear facewise with the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows rather than as a collapsed status coordinate.

For the reverse direction, start with the distinct dyadic rows, the $\StreamNameUp$ finite window, the $\RegSeqRatUp$ readback, the $\RealUp$ seal, and the displayed support rows. Use \autoref{thm:dyadicratcore-l10-exit-namecert-interface} to read this inventory as the local $\NameCert$ interface for the accepted Phase Real and Completion exit packet.

Apply \autoref{thm:streamname-real-completion-four-object-terminal-pullback}. The four exposed faces form the terminal finite packet seen by downstream Real-and-completion consumers, so the distinct dyadic rows are presented as the $\DyadicRatCoreUp$ face of that packet.

It remains to rule out substitute rows. Apply \autoref{thm:real-l10-exit-certificate-face-minimality}. Each of the four faces and each displayed support row is indispensable for the current exit certificate; hence no aggregate formalstatus row, bridge row, quotient rational equality, quotient stream equality, selected limit, metric-completeness datum, countable-choice row, or ambient $\RealUp$ completeness row can stand in for the $\DyadicRatCoreUp$ face.
\end{proof}

\begin{theorem}[DyadicRatCore Phase Real and Completion objectwise exit equivalence]
\label{thm:dyadicratcore-phase-real-completion-objectwise-exit-equivalence}
An accepted L10 exit packet satisfies the current Phase Real and Completion objectwise exit criterion if and only if the four facewise rows are simultaneously present on one BEDC-visible support surface: the $\DyadicRatCoreUp$ signed mantissa, exponent, denominator, comparison, distance, and radius rows; the $\StreamNameUp$ finite-window rows; the $\RegSeqRatUp$ classifier and readback rows; the local $\RealUp$ seal rows; and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support rows.

No quotient rational equality, quotient stream equality, selected limit, countable choice, metric-completeness datum, bridge row, aggregate formalstatus row, or ambient $\RealUp$ completeness can replace any of the four faces or any shared support row in this criterion.
\end{theorem}
\begin{proof}
For the forward direction, assume the accepted L10 packet satisfies the current objectwise exit criterion. Apply \autoref{thm:real-l10-current-phase-exit-local-threshold}. The threshold reads the criterion through local Real exit rows, so the packet must expose the Real seal together with the support rows consumed by the Phase Real and Completion surface.

Apply \autoref{thm:streamname-real-completion-four-object-terminal-pullback}. The same local Real surface is the terminal four-object boundary with visible $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces. Use \autoref{thm:regseqrat-terminal-four-face-correspondence} to read the RegSeqRat classifier and readback rows as one of those exposed faces.

It remains to recover the dyadic source rows and the local certificate interface. Apply \autoref{thm:dyadicratcore-l10-exit-namecert-interface}. Then apply \autoref{thm:dyadicratcore-l10-exit-ledger-facewise-noncollapse}. These two results force the signed mantissa, exponent, denominator, comparison, distance, and radius rows to remain present as the distinct $\DyadicRatCoreUp$ face, with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

For the reverse direction, start with the simultaneous four-face inventory and the shared support rows. Apply \autoref{thm:dyadicratcore-l10-exit-namecert-interface} to package this inventory as the local $\NameCert$ interface for the accepted L10 exit packet.

Next apply \autoref{thm:dyadicratcore-l10-exit-ledger-facewise-noncollapse}. The dyadic rows do not collapse into an aggregate status coordinate. Apply \autoref{thm:streamname-real-completion-four-object-terminal-pullback} and \autoref{thm:regseqrat-terminal-four-face-correspondence} to transport the same inventory to the terminal four-object boundary.

Finally apply \autoref{thm:real-l10-current-phase-exit-local-threshold}. Since the local Real threshold sees exactly that four-object boundary and the displayed support surface, the packet satisfies the current Phase Real and Completion objectwise exit criterion.

The exclusion sentence follows from the noncollapse theorem together with the terminal pullback, the RegSeqRat correspondence, and the Real local threshold. None of those cited results exposes a coordinate for quotient rational equality, quotient stream equality, selected limits, countable choice, metric-completeness data, bridge rows, aggregate formalstatus rows, or ambient $\RealUp$ completeness, so none can replace any face or shared support row.
\end{proof}

\begin{theorem}[DyadicRatCore L10 face status pullback]
\label{thm:dyadicratcore-l10-face-status-pullback}
Let $P$ be an accepted Phase Real and Completion exit packet whose visible faces are $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Then $P$ pulls back exactly the four local status records from the four corresponding face chapters' own $\mathsf{closurestatus}$ and $\mathsf{formalstatus}$ records. The $\DyadicRatCoreUp$ face contributes the signed mantissa, exponent, denominator, comparison, distance, and radius ledger; the $\StreamNameUp$ face contributes the finite-window status; the $\RegSeqRatUp$ face contributes the classifier and readback status; and the $\RealUp$ face contributes the local seal status.

This pullback has no aggregate $\mathsf{formalstatus}$ row, aggregate bridge token, route-level Lean target, quotient rational equality, quotient stream equality, selected limit, countable-choice row, metric-completeness datum, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-phase-real-completion-objectwise-exit-equivalence}. It identifies the accepted packet with the simultaneous four-face inventory on one BEDC-visible support surface: the dyadic source ledger, the $\StreamNameUp$ finite window, the $\RegSeqRatUp$ classifier and readback rows, the $\RealUp$ seal rows, and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next apply \autoref{thm:dyadicratcore-l10-exit-ledger-facewise-noncollapse}. The signed mantissa, exponent, denominator, comparison, distance, and radius rows remain the distinct $\DyadicRatCoreUp$ face. They therefore cannot be re-read as a route status, bridge token, equality quotient, selected limit, choice datum, completeness datum, or ambient real-completion row.

Now apply \autoref{thm:streamname-real-completion-face-status-pullback}. Each visible face imports closure grade, verification token, Lean target, and bridge row only by reference to that face chapter's own $\mathsf{closurestatus}$ block. Hence the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ entries remain their own finite-window, classifier-readback, and seal status records, while the dyadic ledger is the local record supplied by the distinct DyadicRatCore face.

Finally apply \autoref{thm:real-l10-objectwise-exit-threshold-equivalence}. The Phase Real and Completion threshold is read object by object on these four local records. Thus the packet can be consumed as a finite paper-side boundary only through the four pulled-back face records, and never through aggregate $\mathsf{formalstatus}$, aggregate bridge, route-level Lean target, quotient rational equality, quotient stream equality, selected limit, countable choice, metric completeness, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[DyadicRatCore L10 exit Lean touchpoint completeness]
\label{thm:dyadicratcore-l10-exit-lean-touchpoint-completeness}
Let $P$ be an accepted Phase Real and Completion exit packet whose visible faces are $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Then the machine-facing support of $P$ is exhausted by the three route touchpoints named by \autoref{thm:streamname-open-phase-lean-touchpoint-triad} and \autoref{thm:streamname-real-completion-exit-lean-route-boundary}: the StreamName singleton finite-window handoff, the DyadicRatCore RegSeqRat bridge handoff exactness, and the RegSeqRat Real seal handoff. These three route sites are read together with the four local face records pulled back by \autoref{thm:dyadicratcore-l10-face-status-pullback}.

Thus the accepted four-face exit has no additional route-level Lean target, aggregate $\mathsf{formalstatus}$ row, bridge row, quotient stream equality, selected limit, countable-choice row, metric-completeness datum, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
First apply \autoref{thm:dyadicratcore-l10-face-status-pullback}. The accepted packet is read through the four local records carried by the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces, and the pullback already excludes aggregate status, aggregate bridge, route-level Lean targets, quotient stream equality, selected limits, countable choice, metric completeness, and ambient real completeness.

Next apply \autoref{thm:streamname-real-completion-exit-lean-route-boundary} together with \autoref{thm:streamname-open-phase-lean-touchpoint-triad}. These theorems identify the only machine-facing route sites for the same open-phase exit as the StreamName singleton finite-window handoff, the DyadicRatCore RegSeqRat bridge handoff exactness, and the RegSeqRat Real seal handoff.

Use \autoref{thm:regseqrat-terminal-four-face-correspondence} to identify the RegSeqRat classifier and readback rows with the terminal four-face boundary. The readback face is therefore one of the local records already pulled back from the accepted packet, not a separate quotient stream equality or selected limit.

Finally apply \autoref{thm:real-l10-objectwise-exit-threshold-equivalence}. The open-phase threshold is consumed object by object on the four local face records, while the three named Lean touchpoints merely expose the existing finite-window, dyadic-handoff, and seal-handoff route. Hence no machine support remains outside those sites, and none of the excluded rows can be added as a companion route coordinate.
\end{proof}

\begin{theorem}[DyadicRatCore L10 exit ledger source determinacy]
\label{thm:dyadicratcore-l10-exit-ledger-source-determinacy}
Let $P$ and $Q$ be accepted Phase Real and Completion objectwise audit reads. Suppose both reads expose the same $\DyadicRatCoreUp$ signed mantissa, exponent, denominator, comparison, distance, and radius rows; the same $\StreamNameUp$ finite-window rows; the same $\RegSeqRatUp$ classifier and readback rows; the same local $\RealUp$ seal rows; and the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. Then $P$ and $Q$ determine the same BEDC-visible dyadic source ledger and the same four-face exit packet.

This determinacy has no coordinate for quotient rational equality, quotient stream equality, selected limits, countable choice, metric-completeness data, aggregate formalstatus rows, aggregate bridge rows, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First apply \autoref{thm:dyadicratcore-l10-face-status-pullback} to both accepted reads. The hypotheses give the same four local status records on the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces, so both reads pull back the same facewise status packet.

Next use \autoref{thm:dyadicratcore-phase-real-completion-objectwise-exit-equivalence}. Since the objectwise audit criterion is exactly the simultaneous four-face inventory with the displayed support rows, $P$ and $Q$ have the same accepted objectwise exit record rather than two independent route credentials.

Now apply \autoref{thm:dyadicratcore-l10-exit-ledger-facewise-noncollapse}. The common signed mantissa, exponent, denominator, comparison, distance, and radius rows remain the distinct dyadic face inside each packet. Thus the dyadic source ledger read from $P$ is the same BEDC-visible ledger read from $Q$.

Finally apply \autoref{thm:streamname-real-completion-four-object-terminal-pullback} and \autoref{thm:real-l10-objectwise-exit-threshold-equivalence}. The common four faces and support rows factor through one terminal finite Real-completion boundary, and the threshold is consumed object by object on that boundary. Hence the two reads determine the same four-face exit packet.

The exclusions follow from the same citations: the pulled-back face-status packet, objectwise exit equivalence, facewise noncollapse, terminal pullback, and objectwise threshold expose no row for quotient rational equality, quotient stream equality, selected limits, countable choice, metric-completeness data, aggregate formalstatus, aggregate bridge data, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[DyadicRatCore L10 exit ledger restriction functoriality]
\label{thm:dyadicratcore-l10-exit-ledger-restriction-functoriality}
Let $P$ be an accepted Phase Real and Completion four-face packet, and let $P'$ be a sub-exit packet obtained by deleting only downstream consumer rows. Suppose the restriction preserves the displayed $\DyadicRatCoreUp$ signed mantissa, exponent, denominator, comparison, distance, and radius rows; the $\StreamNameUp$ finite-window rows; the $\RegSeqRatUp$ classifier and readback rows; the local $\RealUp$ seal rows; and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Then the DyadicRatCore ledger read from $P'$ is the restriction of the DyadicRatCore ledger read from $P$. The restriction introduces no quotient rational equality, quotient stream equality, selected limit, countable-choice row, metric-completeness datum, aggregate formalstatus row, aggregate bridge row, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
First apply \autoref{thm:dyadicratcore-l10-exit-ledger-source-determinacy} to the accepted read $P$ and to the restricted read $P'$. The preservation hypotheses give the same dyadic source rows, StreamName window rows, RegSeqRat classifier-readback rows, Real seal rows, and displayed support rows on the common part, so the two reads determine the same BEDC-visible dyadic source ledger there.

Next apply \autoref{thm:dyadicratcore-l10-face-status-pullback}. Both packets read their closure and verification data face by face from the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ records. Deleting downstream consumer rows therefore restricts the visible face-status packet; it does not create a new route-level status coordinate.

Now use \autoref{thm:dyadicratcore-phase-real-completion-objectwise-exit-equivalence} together with \autoref{thm:streamname-real-completion-four-object-terminal-pullback}. These theorems identify the shared source as the same terminal four-face boundary with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. Hence the restricted read factors through the restriction of the terminal boundary already read by $P$.

Finally apply \autoref{thm:dyadicratcore-l10-exit-ledger-facewise-noncollapse} and \autoref{thm:real-l10-objectwise-exit-threshold-equivalence}. The dyadic rows remain the distinct $\DyadicRatCoreUp$ face, and the Phase Real and Completion threshold is consumed objectwise on the four local records. Thus no quotient rational equality, quotient stream equality, selected limit, countable-choice row, metric-completeness datum, aggregate formalstatus row, aggregate bridge row, or ambient $\RealUp$ completeness row appears in the restriction.
\end{proof}
