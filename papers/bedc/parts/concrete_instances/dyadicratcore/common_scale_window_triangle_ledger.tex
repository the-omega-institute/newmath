\begin{theorem}[DyadicRatCore common scale-window triangle ledger]
\label{thm:dyadicratcore-common-scale-window-triangle-ledger}
Let three accepted L10 consumers among the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ routes expose dyadic faces that each factor through pairwise $\DyadicRatCoreUp$ scale-window packets. Then the three faces admit one common finite directed-meet scale window. That window carries the signed mantissa row, refined positive-unary exponent row, denominator ledger, common-exponent comparison row, distance and radius rows, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ rows for all three consumer faces.

The triangle ledger exports no quotient rational, host epsilon equality, selected $\RealUp$ endpoint, countable-choice selector, metric-completeness row, or $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
First place each pairwise dyadic face on a terminal scale-window packet by applying \autoref{thm:dyadicratcore-scale-window-terminal-fusion}. The pairwise packets therefore expose the same kind of signed mantissa, positive-unary exponent, denominator, comparison, transport, readback, provenance, and local NameCert rows.

Next apply \autoref{thm:dyadicratcore-finite-radius-window-directed-meet} to the finite family consisting of the three pairwise terminal packets. The directed meet supplies one common finite monotone radius window carrying the shared refined exponent, denominator ledger, common-exponent classifier, distance rows, carrier rows, $\hsame$ transports, $\Cont$ readbacks, $\Pkg$ provenance, and local NameCert row.

Use \autoref{thm:dyadicratcore-directed-refinement-radius-handoff} at each consumer boundary. It forces the $\RegSeqRatUp$ or $\RealUp$ read to pass through the displayed radius handoff before any terminal seal can consume the dyadic packet, so the three L10 faces cannot split into separate ambient dyadic sources.

Finally apply \autoref{thm:dyadicratcore-l10-sibling-dependency-totality}. It reads the same finite dyadic rows through the route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
and identifies those rows as the dyadic source consumed by the L10 real-and-completion boundary.

The exclusion clauses of the terminal fusion, directed meet, directed-refinement handoff, and L10 route leave no row for quotient rationals, host epsilon equality, selected real endpoints, countable-choice selectors, metric-completeness data, or $\RealUp$ completeness. Hence the common window is exactly the finite triangle ledger stated above.
\end{proof}

\begin{theorem}[DyadicRatCore four-face pullback correspondence]
\label{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence}
Let three accepted L10 consumers expose dyadic faces satisfying \autoref{thm:dyadicratcore-common-scale-window-triangle-ledger}. Suppose the same visible rows are also consumed by a terminal four-face route through
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Then the common scale-window triangle ledger and the terminal four-face pullback determine one another exactly on the signed mantissa, refined positive-unary exponent, denominator ledger, common-exponent comparison, distance and radius rows, StreamName finite window, RegSeqRat classifier and readback rows, Real seal row, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows.

This correspondence exports no quotient rationals, quotient streams, host $\varepsilon$ equality, selected $\RealUp$ endpoints, countable choice, metric completeness, ambient $\RealUp$ completeness, or standard bridge data.
\end{theorem}
\begin{proof}
Begin with the three accepted L10 dyadic faces. Apply \autoref{thm:dyadicratcore-common-scale-window-triangle-ledger} to obtain the common directed-meet scale window over the three consumers. The result supplies the signed mantissa row, refined exponent row, denominator ledger, comparison row, distance and radius rows, transports, continuations, package provenance, and local DyadicRatCore naming surface.

Next place that same finite window on the downstream route. Use \autoref{thm:streamname-regseqrat-real-dyadic-window-pullback} to read the DyadicRatCore window as the StreamName finite window, RegSeqRat classifier and readback surface, and Real seal boundary consumed by the ordered route. Then apply \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}. These identify the terminal presentation with the displayed four faces and leave no hidden terminal coordinate beyond the listed rows.

For the reverse direction, start from the terminal four-face pullback. Read its DyadicRatCore face together with the displayed StreamName, RegSeqRat, and Real rows. The pullback uniqueness fixes the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ coordinates, so the dyadic face restricts to the common finite scale window supplied by the triangle ledger.

The cited pullback and terminal results add no quotient rational carrier, quotient stream equality, host $\varepsilon$ equality, selected real endpoint, countable-choice selector, completeness datum, metric-completeness row, or standard bridge witness. Hence the two presentations determine exactly the same BEDC-visible finite surface.
\end{proof}

\begin{theorem}[DyadicRatCore terminal Real exit source uniqueness]
\label{thm:dyadicratcore-scale-window-real-exit-source-uniqueness}
Let two accepted Phase Real and Completion terminal exits expose the same $\StreamNameUp$ finite-window rows, $\RegSeqRatUp$ classifier and readback rows, local $\RealUp$ seal row, displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ coordinates. Suppose both exits are accepted through the terminal four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Then their $\DyadicRatCoreUp$ scale-window sources agree as the same common finite triangle ledger. In particular there is no independent dyadic source branch behind the shared terminal $\RealUp$ seal: the signed mantissa, refined positive-unary exponent, denominator ledger, common-exponent comparison, distance and radius rows, componentwise transports, displayed continuations, package provenance, and local DyadicRatCore naming rows are the same BEDC-visible source data for both exits.

This uniqueness exports no quotient rational, quotient stream, host $\varepsilon$ equality, selected $\RealUp$ endpoint, countable choice, metric completeness, ambient $\RealUp$ completeness, or standard bridge row.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence} to each accepted exit. Each terminal presentation is therefore determined by a common finite triangle ledger together with the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next use \autoref{thm:streamname-l10-four-step-source-lattice}. Since the two exits expose the same downstream $\StreamNameUp$ finite-window surface and the same $\RegSeqRatUp$ and $\RealUp$ rows, both routes pass through the same four ordered faces. The four-face source lattice leaves no extra source edge between the StreamName window and the Real seal.

Finally apply \autoref{thm:real-l10-terminal-pullback-uniqueness} to the shared terminal boundary. The two accepted terminal pullbacks over the same four faces and support coordinates are identified, so any difference in signed mantissa, exponent, denominator, distance, or radius rows would create a second four-face packet over the same terminal seal.

That second packet is impossible by the shared terminal pullback. Hence both exits read the same $\DyadicRatCoreUp$ scale-window source as the common finite triangle ledger, and the exclusion clause follows from the corresponding four-face and terminal uniqueness inventories.
\end{proof}

\begin{theorem}[DyadicRatCore four-face window-refinement cocycle]
\label{thm:dyadicratcore-four-face-window-refinement-cocycle}
Let three accepted finite positive-unary scale-window refinements expose pairwise $\DyadicRatCoreUp$ faces, and suppose each pairwise face maps through the displayed route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Then the two ways of passing from the pairwise common scale windows to a terminal four-face pullback agree on the signed mantissa, refined positive-unary exponent, denominator ledger, common-exponent comparison, distance and radius rows, $\StreamNameUp$ finite window, $\RegSeqRatUp$ classifier and readback rows, $\RealUp$ seal row, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows.

The cocycle exports no quotient rational or quotient stream equality, selected limit, choice row, metric-completeness datum, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-common-scale-window-triangle-ledger} to the three accepted pairwise scale-window refinements. This supplies one common finite dyadic window carrying the signed mantissa, refined exponent, denominator ledger, comparison, distance, radius, transport, continuation, package, and local NameCert rows for the three faces.

Next apply \autoref{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence}. The common dyadic window is identified with the terminal four-face pullback over the route through $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, so any comparison between two refinement orders is reduced to a comparison of the same four displayed faces.

Use \autoref{thm:streamname-regseqrat-triple-overlap-seal-cocycle} on the downstream triple overlap. It makes the $\StreamNameUp$ finite window and the $\RegSeqRatUp$ classifier/readback seal independent of the order in which the three finite overlaps are assembled.

Finally apply \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. The handoff exactness carries the downstream cocycle back across the $\DyadicRatCoreUp$ source and forward to the $\RealUp$ seal row, preserving the componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ coordinates. Thus both refinement paths determine the same terminal four-face surface, and the exclusion clause is inherited from the triangle ledger, four-face correspondence, triple-overlap seal, and handoff exactness results.
\end{proof}

\begin{theorem}[DyadicRatCore terminal Real exit has no fifth source]
\label{thm:dyadicratcore-terminal-real-exit-no-fifth-source}
Let an accepted Phase Real and Completion terminal $\RealUp$ exit be accepted through the route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Then the visible $\DyadicRatCoreUp$ scale window, the $\StreamNameUp$ finite window, the $\RegSeqRatUp$ readback and classifier surface, the local $\RealUp$ seal row, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support exhaust the source inventory of the exit.

No fifth source, aggregate formalstatus row, bridge row, quotient stream, selected limit, countable-choice row, metric-completeness row, or $\RealUp$ completeness coordinate can compensate for a missing one of those four faces or its displayed BEDC support rows.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence}. It identifies the terminal route with exactly the four visible faces: the dyadic scale-window face, the StreamName finite-window face, the RegSeqRat readback and classifier face, and the local Real seal face, together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next apply \autoref{thm:dyadicratcore-scale-window-real-exit-source-uniqueness}. Once the downstream StreamName, RegSeqRat, Real, and support rows are fixed, the dyadic source is forced to be the same common finite triangle ledger. Thus a different dyadic source would be a second terminal pullback over the same four-face surface.

Finally apply \autoref{thm:real-l10-incomplete-face-exit-obstruction} and \autoref{thm:real-l10-exit-facewise-audit-obstruction}. They make the L10 exit facewise: each of the four route positions must be present with its own support and local status rows, and no aggregate token or off-table Real-completeness datum can supply an absent face.

The exclusion inventories in the DyadicRatCore four-face correspondence, the terminal Real exit source uniqueness theorem, and the Real L10 route theorems list exactly the rows outside the route: quotient streams, selected limits, countable choice, metric completeness, bridge rows, and ambient $\RealUp$ completeness. Since none of those rows belongs to the four-face source surface, the displayed inventory is exhausted and there is no fifth compensating source.
\end{proof}

\begin{theorem}[DyadicRatCore common scale regular-name interface]
\label{thm:dyadicratcore-common-scale-regular-name-interface}
Let an accepted regular-name route expose a $\StreamNameUp$ finite-window face and a $\RegSeqRatUp$ regularity face, and suppose both faces consume the same $\DyadicRatCoreUp$ common scale window along the L10 route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Then the two regular-name consumers read the same signed mantissa row, positive-unary exponent row, denominator ledger, common-exponent comparison, distance and radius rows, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. The StreamName finite-window face and the RegSeqRat regularity face therefore form one BEDC-visible regular-name interface over the common DyadicRatCore scale window.

This interface exports no quotient rational carrier, quotient stream equality, selected $\RealUp$ endpoint, countable choice, metric-completeness row, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-common-scale-window-triangle-ledger} to the accepted route faces. The common finite scale window supplies the signed mantissa, positive-unary exponent, denominator ledger, comparison, distance, radius, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\DyadicRatCoreUp}$ rows before either downstream consumer reads the dyadic packet.

Next apply \autoref{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence}. It identifies that dyadic window with the ordered four-face route through $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, so the StreamName finite-window face and the RegSeqRat classifier and readback face are projections of the same pullback surface rather than independent regular-name sources.

Use the StreamName and RegSeqRat handoff rows \autoref{thm:streamname-regseqrat-real-dyadic-window-pullback}, \autoref{thm:regseqrat-streamname-dyadic-window-lock}, and \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. These rows force the finite-window consumer and the regularity consumer to preserve the displayed DyadicRatCore rows across the regular-name boundary, including the componentwise transports, continuations, package provenance, and local naming coordinates.

Finally apply \autoref{thm:dyadicratcore-terminal-real-exit-no-fifth-source}. Since the terminal L10 exit has only the dyadic scale window, the StreamName finite window, the RegSeqRat regularity/readback surface, the Real seal, and the displayed support rows, none of the excluded objects can supply another regular-name coordinate. The two consumers therefore read exactly the same common-scale interface, with no quotient rational, quotient stream, selected endpoint, choice row, metric-completeness row, or Real-completeness coordinate.
\end{proof}
