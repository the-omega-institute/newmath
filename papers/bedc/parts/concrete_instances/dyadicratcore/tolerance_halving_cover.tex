\begin{theorem}[DyadicRatCore tolerance-halving cover]\label{thm:dyadicratcore-tolerance-halving-cover}
For every positive dyadic tolerance row accepted by the $\DyadicRatCoreUp$ finite scale-window ledger, there is a halved positive dyadic tolerance row in a displayed finite refinement window whose doubled radius is classifier-below the original tolerance row. The refinement preserves the signed mantissa row, denominator ledger, common-exponent comparison, distance and radius rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ support.

Consequently every $\StreamNameUp$ finite-window request and every $\RegSeqRatUp$ regularity check that consumes a DyadicRatCore tolerance row may replace it by a carried half-tolerance row without invoking host rationals, quotient equality, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the accepted positive tolerance row in the finite scale-window packet. Apply scale-window monotonicity, \autoref{thm:dyadicratcore-scale-window-monotonicity}, to pass to the displayed successor exponent window while keeping the same signed mantissa, denominator ledger, comparison row, radius row, $\hsame$, $\Cont$, $\Pkg$, and local NameCert support.

Next use the common-scale triangle ledger, \autoref{thm:dyadicratcore-common-scale-window-triangle-ledger}. It compares the original radius row and the successor-window radius row inside one common finite denominator scale, so the doubled successor radius is classifier-below the original tolerance rather than compared by host rational order.

Finally apply the terminal four-face tolerance exhaustion theorem, \autoref{thm:dyadicratcore-terminal-four-face-tolerance-exhaustion}. The $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$ faces have no fifth tolerance source outside the displayed DyadicRatCore ledger and support rows. The halved row is therefore the finite-window handoff consumed by StreamName requests and RegSeqRat regularity checks, with the stated transport and nonescape clauses preserved.
\end{proof}

\begin{theorem}[DyadicRatCore halving-cover RegSeqRat handoff]
\label{thm:dyadicratcore-halving-cover-regseqrat-handoff}
Every $\RegSeqRatUp$ regularity check consuming a positive $\DyadicRatCoreUp$
tolerance row may replace that row by the halved finite refinement supplied by
\autoref{thm:dyadicratcore-tolerance-halving-cover}. The replacement preserves
the $\StreamNameUp$ request rows, the $\RegSeqRatUp$ classifier and regularity
rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$
provenance, and local $\NameCert$ support.

The refined read is still a finite regularity read over the displayed
DyadicRatCore ledger. It does not invoke host rational order, quotient stream
equality, selected limits, countable choice, host real equality, or ambient
$\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-tolerance-halving-cover} to the positive
tolerance row consumed by the regularity check. It supplies the halved positive
row in a displayed finite refinement window while preserving the dyadic ledger,
transport, replay, provenance, and local naming rows.

Then use the RegSeqRat common-window compatibility supplied by
\autoref{thm:regseqrat-tail-stable-cover-common-window-lock}. The original
regularity read and the halved refinement are compared on one accepted common
finite window, so the $\StreamNameUp$ request rows and the $\RegSeqRatUp$
classifier and regularity rows are unchanged as public source data.

Finally the terminal four-face tolerance exhaustion from
\autoref{thm:dyadicratcore-tolerance-halving-cover} rules out a second
tolerance source. The RegSeqRat check therefore consumes the halved row through
the same finite BEDC support rather than through host order, quotient streams,
selected limits, choice, host equality, or ambient completeness.
\end{proof}

\begin{theorem}[DyadicRatCore halving-cover StreamName handoff]
\label{thm:dyadicratcore-halving-cover-streamname-handoff}
Every $\StreamNameUp$ finite-window request consuming a positive
$\DyadicRatCoreUp$ tolerance row may replace that row by the halved finite
refinement supplied by \autoref{thm:dyadicratcore-tolerance-halving-cover}.
The replacement preserves the finite window schedule, componentwise $\hsame$
transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$
support.

The refined StreamName read remains a finite-window read over the displayed
DyadicRatCore ledger. It does not invoke host rational order, quotient streams,
selected limits, countable choice, host real equality, ambient
$\RealUp$ completeness, or a separate completion coordinate.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-tolerance-halving-cover} to the positive
tolerance row consumed by the StreamName request. It supplies a halved positive
dyadic row in a displayed finite refinement window while preserving the
DyadicRatCore comparison ledger, transport, replay, provenance, and local
naming rows.

Next read the StreamName finite-window request rows. The request schedule is
indexed by displayed windows and tolerance demands, so replacing the tolerance
row by its carried half-tolerance refinement changes no public schedule face
and adds no host rational comparison.

The same support rows transport the replacement through $\hsame$, replay it
through $\Cont$, source it through $\Pkg$, and name it through the local
$\NameCert$. Thus the StreamName handoff consumes the halved row through the
finite BEDC support.

Finally the nonescape clause in
\autoref{thm:dyadicratcore-tolerance-halving-cover} rules out host rational
order, quotient streams, selected limits, choice, host equality, ambient Real
completeness, and separate completion data as alternative tolerance sources.
\end{proof}

\begin{theorem}[DyadicRatCore common-exponent transport]
\label{thm:dyadicratcore-common-exponent-transport}
The halved tolerance refinement supplied by
\autoref{thm:dyadicratcore-tolerance-halving-cover} preserves the
common-exponent comparison ledger under componentwise $\hsame$ transport and
displayed $\Cont$ replay. If a $\RegSeqRatUp$ regularity check or a
$\StreamNameUp$ finite-window request consumes the original positive
$\DyadicRatCoreUp$ tolerance row, then replacing that row by the carried
half-tolerance row keeps the signed mantissa, denominator scale, doubled-radius
comparison, and common-exponent classifier in the same finite BEDC ledger.

The comparison is BEDC classifier data: it is replayed through displayed
common-exponent rows, not reopened as host rational order, quotient equality,
selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the positive tolerance row and its halved refinement from
\autoref{thm:dyadicratcore-tolerance-halving-cover}. The refinement already
carries the signed mantissa row, denominator ledger, common-exponent comparison,
transport, replay, provenance, and local naming rows.

Use \autoref{thm:dyadicratcore-scale-window-monotonicity} to move the read to
the displayed successor scale window without changing the public dyadic
components. Then apply
\autoref{thm:dyadicratcore-common-scale-window-triangle-ledger}; it keeps the
original row, the halved row, and the doubled-radius comparison inside one
common finite exponent ledger.

The downstream handoff is stable on both consuming faces.
\autoref{thm:dyadicratcore-halving-cover-regseqrat-handoff} carries the same
ledger to $\RegSeqRatUp$ regularity checks, while
\autoref{thm:dyadicratcore-halving-cover-streamname-handoff} carries it to
$\StreamNameUp$ finite-window requests. Componentwise $\hsame$ transport and
displayed $\Cont$ replay therefore repeat the common-exponent classifier
rather than creating a second comparison source.

Since every comparison row used by the replacement is one of the displayed
DyadicRatCore classifier rows, the construction never appeals to host rational
order, quotient stream equality, selected limits, countable choice, host real
equality, or ambient Real completeness.
\end{proof}

\begin{theorem}[DyadicRatCore iterated halving budget]
\label{thm:dyadicratcore-iterated-halving-budget}
For every positive $\DyadicRatCoreUp$ tolerance row and every finite unary schedule of halving requests, there is a displayed finite budget whose final row refines the original tolerance row and whose every predecessor doubles classifier-below the previous row. The budget preserves the signed mantissa row, denominator ledger, common-exponent comparison, distance and radius rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ support at each displayed step.

Every $\StreamNameUp$ finite-window request and every $\RegSeqRatUp$ regularity check may consume this iterated budget through the same finite dyadic ledger. The construction does not invoke host rational order, quotient stream equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, or a separate completion coordinate.
\end{theorem}
\begin{proof}
Induct over the finite unary schedule. In the empty case, the budget is the original positive tolerance row together with its displayed signed mantissa, denominator, common-exponent, distance, radius, transport, replay, provenance, and local naming rows.

For the successor step, apply \autoref{thm:dyadicratcore-tolerance-halving-cover} to the current positive tolerance row. It supplies the next halved row in a finite refinement window and records that doubling the new row is classifier-below the previous row inside the DyadicRatCore comparison ledger.

Use \autoref{thm:dyadicratcore-halving-cover-regseqrat-handoff} and \autoref{thm:dyadicratcore-halving-cover-streamname-handoff} to transport the accumulated budget to RegSeqRat regularity checks and StreamName finite-window requests. The induction preserves only displayed dyadic rows and support data, so no host rational order, quotient stream equality, selected limit, choice, host equality, ambient Real completeness, or completion coordinate is introduced.
\end{proof}

\begin{theorem}[DyadicRatCore halving-budget cofinality]
\label{thm:dyadicratcore-halving-budget-cofinality}
For any finite family of $\StreamNameUp$ finite-window requests and $\RegSeqRatUp$ regularity checks consuming a positive $\DyadicRatCoreUp$ tolerance row, the iterated halving budget of \autoref{thm:dyadicratcore-iterated-halving-budget} supplies a common cofinal tolerance window. The final budget row preserves the signed mantissa, denominator ledger, common-exponent comparison, distance and radius rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ support.

Every listed consumer factors through that common row by the same finite dyadic ledger. The cofinal window does not use host rational order, quotient stream equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, or a separate completion coordinate.
\end{theorem}
\begin{proof}
Enumerate the finite family of StreamName requests and RegSeqRat checks. Choose the unary halving schedule long enough to meet the largest displayed tolerance demand among those finite rows, and apply \autoref{thm:dyadicratcore-iterated-halving-budget} to the original positive tolerance row.

The resulting budget carries each predecessor and the final row inside one displayed dyadic ledger. Signed mantissas, denominator rows, common-exponent comparisons, distance and radius rows, transport, replay, provenance, and local naming are preserved at every step of the schedule.

Next apply \autoref{thm:dyadicratcore-halving-cover-streamname-handoff} to the StreamName requests and \autoref{thm:dyadicratcore-halving-cover-regseqrat-handoff} to the RegSeqRat checks. These handoffs let each consumer replace its original tolerance by the carried final row while keeping the same finite support rows.

Finally use \autoref{thm:dyadicratcore-common-exponent-transport}. It keeps all comparisons in the common-exponent ledger, so the common row is cofinal for the finite family without reopening the comparison as host rational order or importing quotient streams, selected limits, choice, host equality, ambient Real completeness, or completion data.
\end{proof}

\begin{theorem}[DyadicRatCore terminal halving-cover selection-free budget]
\label{thm:dyadicratcore-terminal-halving-cover-selection-free}
For any finite family of $\StreamNameUp$ finite-window requests and $\RegSeqRatUp$ regularity checks consuming a positive $\DyadicRatCoreUp$ tolerance row, there is one displayed terminal halving-cover budget over the common-exponent ledger through which every listed consumer factors. The terminal budget preserves the signed mantissa row, denominator ledger, common-exponent comparison, doubled-radius comparisons, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicRatCoreUp}$ support.

The budget is selection-free: it does not use host rational order, quotient stream equality, selected limits, countable choice, host $\RealUp$ equality, ambient $\RealUp$ completeness, or a hidden completion coordinate.
\end{theorem}
\begin{proof}
Start from the finite family of StreamName requests and RegSeqRat regularity checks. Apply the cofinality theorem \autoref{thm:dyadicratcore-halving-budget-cofinality} to obtain a common final tolerance row for the whole displayed family.

The cofinal row is built from iterated halving closure. By \autoref{thm:dyadicratcore-iterated-halving-budget}, each predecessor and the terminal row remain inside the same finite DyadicRatCore ledger, with the signed mantissa, denominator scale, common-exponent comparison, doubled-radius comparisons, transport, replay, provenance, and local naming rows preserved.

The two consumer faces are then routed through the existing handoffs. Use \autoref{thm:dyadicratcore-halving-cover-streamname-handoff} for finite-window requests and \autoref{thm:dyadicratcore-halving-cover-regseqrat-handoff} for regularity checks.

Finally apply \autoref{thm:dyadicratcore-common-exponent-transport}. It keeps every tolerance comparison in the displayed common-exponent ledger, so the terminal budget is one finite BEDC object rather than a selected limiting tolerance. The construction therefore refuses host rational order, quotient stream equality, selected limits, countable choice, host real equality, ambient Real completeness, and completion coordinates.
\end{proof}

\begin{theorem}[DyadicRatCore terminal halving-cover exhaustion]
\label{thm:dyadicratcore-terminal-halving-cover-exhaustion}
Every terminal four-face L10 tolerance read using $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ factors through the selection-free terminal halving-cover budget of \autoref{thm:dyadicratcore-terminal-halving-cover-selection-free}. No such consumer can introduce a separate tolerance source, host rational order, quotient stream equality, selected limit, countable choice, host $\RealUp$ equality, or ambient $\RealUp$ completeness.

The exhaustion route is the finite ledger
$$
\DyadicRatCoreUp \longrightarrow \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
together with the common-exponent transport and the two halving-cover handoffs. The terminal read therefore sees only the displayed dyadic budget, its common-exponent comparisons, the finite-window consumer face, the regular rational consumer face, and the downstream Real seal.
\end{theorem}
\begin{proof}
Start with a terminal four-face tolerance read. Apply \autoref{thm:dyadicratcore-terminal-halving-cover-selection-free} to obtain one displayed terminal budget through which the finite-window and regularity consumers factor.

Use \autoref{thm:dyadicratcore-common-exponent-transport} on that budget. It keeps every comparison in the same common-exponent ledger, so the terminal row is not replaced by host rational order or by an independent tolerance source.

Route the consumer faces through the two handoff theorems. The StreamName face uses \autoref{thm:dyadicratcore-halving-cover-streamname-handoff}, and the RegSeqRat face uses \autoref{thm:dyadicratcore-halving-cover-regseqrat-handoff}; the Real-facing read is downstream of those finite tolerance rows and cannot bypass them.

The accepted packet has no row for quotient stream equality, selected limits, countable choice, host Real equality, or ambient completeness. Hence every terminal four-face L10 read exhausts the available tolerance data at the selection-free halving-cover budget.
\end{proof}
