\chapter{A Concrete Naming Certificate for $\BishopLTenComparisonPrincipleUp$}
\label{ch:concrete-instances-bishop-l10-comparison-principle-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopLTenComparisonPrincipleUp$ packet records the finite L10 comparison surface used when two displayed real-name routes must be compared before a Real or Completion consumer can read an order-facing conclusion. It sits after the observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the Bishop real comparison surface of \autoref{ch:concrete-instances-bishoprealcomparison-namecert}. The packet names a located comparison route over finite rows; it does not import trichotomy, quotient equality, host equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[BishopLTenComparisonPrinciple carrier]
\label{def:bishop-l10-comparison-principle-carrier}
A $\BishopLTenComparisonPrincipleUp$ carrier is a finite $\BHist$ packet
$$
K=(S,R,D,E,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row listing the inspected source positions for the two comparison routes.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same finite windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger that records the dyadic scales, endpoint cells, and comparison budgets consumed by the readback.
\item $E$ is the terminal $\RealUp$ seal row reached only after the stream, regular-rational, and dyadic rows have been displayed.
\item $Q$ is the located comparison evidence row. It records a displayed separating witness or overlap certificate for the inspected window, tied to the sibling $\BishopRealComparisonUp$ route.
\item $H$ records componentwise $\hsame$ transport for the stream schedule, regular readback, dyadic ledger, Real seal, located comparison evidence, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from finite observation and dyadic tolerance data through regular readback, Real sealing, and located comparison use.
\item $P$ is the $\Pkg$ provenance over $\BishopLTenComparisonPrincipleUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BishopRealComparisonUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopLTenComparisonPrincipleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,D,E,Q,H,C,P,N$. No coordinate stores a global trichotomy branch, quotient real equality, selected representative, host equality test, countable-choice schedule, or ambient completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[BishopLTenComparisonPrinciple NameCert obligations]
\label{thm:bishop-l10-comparison-principle-namecert-obligations}
For every accepted $\BishopLTenComparisonPrincipleUp$ carrier
$$
K=(S,R,D,E,Q,H,C,P,N),
$$
the local $\NameCert_{\BishopLTenComparisonPrincipleUp}$ surface consists exactly of carrier admission for the StreamName finite-window row, RegSeqRat readback row, DyadicRatCore tolerance ledger, terminal Real seal, located comparison evidence, componentwise transport, displayed replay, package provenance, and local naming. Every comparison-facing consumer must expose $S,R,D$ before reading the Real seal $E$ or the located comparison row $Q$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-l10-comparison-principle-carrier}. Its analytic rows are exactly the finite StreamName window, regular rational readback, dyadic tolerance ledger, Real seal, and located comparison evidence.

The route begins with $S$. The observed source positions determine which finite prefixes of the two real-name routes are public. The readback row $R$ and the dyadic ledger $D$ then provide the rational and tolerance data on which the comparison evidence can be interpreted.

Only after those rows have been displayed may the terminal seal $E$ and the located comparison row $Q$ be consumed. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route.

The excluded principles are absent by coordinate inspection. Since the carrier has no row for global trichotomy, quotient equality, selected representatives, host equality, countable choice, or ambient Real completeness, the local certificate cannot export any of them.
\end{proof}

\begin{theorem}[BishopLTenComparisonPrinciple Real-completion handoff]
\label{thm:bishop-l10-comparison-principle-real-completion-handoff}
Every Real or Completion consumer of an accepted $\BishopLTenComparisonPrincipleUp$ packet receives located comparison evidence only through the displayed route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp \longmapsto \BishopRealComparisonUp .
$$
Thus the handoff may use the inspected window, regular readback, dyadic tolerance ledger, terminal Real seal, and located comparison row, but it exports no trichotomy principle, quotient equality of real names, host equality, selected representative, countable-choice functional, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bishop-l10-comparison-principle-namecert-obligations}. They force each comparison-facing consumer to read $S,R,D$ before the Real seal $E$ or located comparison evidence $Q$ is available.

The Real-facing part of the handoff is therefore downstream of the finite observation and tolerance rows. The Completion-facing part receives the same route: it may replay the inspected comparison through $H,C,P,N$, but it cannot replace the displayed rows by quotient names or host equality.

The sibling route through $\BishopRealComparisonUp$ records only located evidence for the inspected window. Since global trichotomy, selected representatives, countable choice, and ambient $\RealUp$ completeness are not coordinates of $K$, no Real or Completion consumer can project them from the handoff.
\end{proof}

\falsifiablePrediction{An accepted $\BishopLTenComparisonPrincipleUp$ read is refuted if it reaches a Real or Completion comparison consumer without displaying the $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, terminal $\RealUp$ seal, located $\BishopRealComparisonUp$ evidence, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, bishop-real-comparison: $\BishopLTenComparisonPrincipleUp$ is not bijective to any listed sibling because it jointly carries finite observation windows, regular rational readback, dyadic tolerance data, a Real seal, located comparison evidence, transport, replay, provenance, and local naming as one L10 comparison packet.}

\closureat{BishopLTenComparisonPrincipleUp}{seedStr}

\begin{closurestatus}{\BishopLTenComparisonPrincipleUp}
  \constructivestory{A $\BishopLTenComparisonPrincipleUp$ packet is generated from StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance data, a Real seal, located BishopRealComparison evidence, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-l10-comparison-principle-carrier}, \autoref{thm:bishop-l10-comparison-principle-namecert-obligations}, and \autoref{thm:bishop-l10-comparison-principle-real-completion-handoff} seed the L10 located comparison packet over StreamName, RegSeqRat, DyadicRatCore, Real, and BishopRealComparison rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, trichotomy, quotient real equality, host equality, selected representatives, countable choice, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, located comparison exactness, Real handoff, Completion handoff, transport, replay, provenance, and local naming for BEDC.Derived.BishopLTenComparisonPrincipleUp.}
\end{closurestatus}
