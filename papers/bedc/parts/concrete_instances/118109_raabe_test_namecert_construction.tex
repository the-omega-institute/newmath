\chapter{A Concrete Naming Certificate for $\RaabeTestUp$}
\label{ch:concrete-instances-raabe-test-namecert}
\origin{human}

Following Raabe and Duhamel on the ratio refinement for positive-term series, the $\RaabeTestUp$ packet records the finite ratio-window evidence by which a displayed first-order quotient defect is routed into constructive real-series Cauchy data. It sits beside the ratio corridor of \autoref{ch:concrete-instances-real-series-ratio-test-namecert}, the dyadic-block comparison of \autoref{ch:concrete-instances-cauchy-condensation-test-namecert}, the Abel boundary of \autoref{ch:concrete-instances-abel-test-namecert}, and the root-test majorant of \autoref{ch:concrete-instances-weierstrass-root-test-namecert}. The packet is a finite NameCert surface for Real and Completion consumers; it is not a selected sum, a countable-choice schedule, quotient stream equality, trichotomy, host summability, or ambient $\RealUp$ completeness.

\begin{definition}[Raabe test carrier]
\label{def:raabe-test-carrier}
A $\RaabeTestUp$ carrier is a finite $\BHist$ packet
$$
R=(A,Q,J,M,D,W,T,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the positive real-series source row, exposing only the inspected term windows and partial-sum windows used by the current tail request.
\item $Q$ is the adjacent positive ratio-window row, recording the finite quotient cells $a_{n+1}/a_n$ on the inspected tail.
\item $J$ is the unary index-threshold row, displaying the natural cutoff after which the Raabe defect is read.
\item $M$ is the first-order defect-margin row, displaying the rational margin by which $n(1-a_{n+1}/a_n)$ stays beyond the unit boundary on the opened window.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger that turns the finite defect row and threshold row into a tail-comparison budget.
\item $W$ is the $\StreamNameUp$ finite-window row for the source terms, quotient cells, partial sums, and comparison majorants.
\item $T$ is the ratio-window route row, binding $Q,J,M,D,W$ to the existing real-series ratio-test and adjacent comparison surfaces before endpoint data may be read.
\item $E$ is the $\RegSeqRatUp$ and $\RealUp$ handoff row, exposed only after the ratio-window route row has been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, quotient, threshold, defect, dyadic, stream, route, endpoint, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each tail request through $Q,J,M,D,W,T,E$.
\item $P$ is the $\Pkg$ provenance over $\RaabeTestUp$, $\RealSeriesRatioTestUp$, $\CauchyCondensationTestUp$, $\AbelTestUp$, $\WeierstrassRootTestUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RaabeTestUp}$ row naming exactly this finite ratio-refinement packet.
\end{enumerate}
The classifier compares accepted packets only through $A,Q,J,M,D,W,T,E,H,C,P,N$. It has no coordinate for selected sums, countable choice, quotient stream equality, trichotomy, host summability, ambient $\RealUp$ completeness, Lean theorem checking, or bridge checking.
\end{definition}
\leanchecked{BEDC.Derived.RaabeTestUp.RaabeTestTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Raabe test NameCert obligations]
\label{thm:raabe-test-namecert-obligations}
For every accepted $\RaabeTestUp$ carrier
$$
R=(A,Q,J,M,D,W,T,E,H,C,P,N),
$$
the local $\NameCert_{\RaabeTestUp}$ surface consists exactly of carrier admission for the positive source row, adjacent ratio windows, unary index threshold, first-order defect margin, dyadic tolerance ledger, finite stream windows, ratio-window route, regular rational and Real handoff, componentwise transport, displayed replay, package provenance, and local naming. Every obligation-facing read must pass through these rows and may not replace them by selected sums, countable choice, quotient stream equality, trichotomy, host summability, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:raabe-test-carrier}. The displayed rows are exactly $A,Q,J,M,D,W,T,E,H,C,P,N$, so carrier admission and local naming have no hidden analytic coordinate.

Read the finite ratio evidence first. The source row $A$ supplies only the visible positive-term and partial-sum windows, the quotient row $Q$ supplies adjacent positive ratio cells, and the threshold row $J$ records the unary cutoff at which the defect row is allowed to be inspected.

The defect and comparison route is then forced through $M,D,W,T$. The row $M$ records the first-order Raabe margin, $D$ translates that margin into dyadic tolerance cells, $W$ fixes the stream windows, and $T$ binds the route to the surrounding ratio-test, condensation, Abel, and root-test surfaces.

Finally, $E$ supplies the endpoint handoff through $\RegSeqRatUp$ and $\RealUp$. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet, so none of them can introduce selected sums, countable choice, quotient stream equality, trichotomy, host summability, ambient $\RealUp$ completeness, Lean theorem checking, or bridge data.
\end{proof}

\begin{theorem}[Raabe test ratio-window route]
\label{thm:raabe-test-ratio-window-route}
Every accepted $\RaabeTestUp$ read reaching a $\RegSeqRatUp$ or $\RealUp$ consumer factors through the finite route
$$
A,Q,J \longmapsto M \longmapsto D,W \longmapsto T \longmapsto E \longmapsto (H,C,P,N).
$$
Thus a constructive real-series Cauchy consumer receives ratio-refinement data only after the positive source windows, adjacent quotient windows, unary threshold, first-order defect margin, dyadic tolerance ledger, finite stream windows, ratio-window route, endpoint handoff, transport, replay, provenance, and local $\NameCert$ row have all been displayed.
\end{theorem}
\begin{proof}
Begin with an endpoint-facing read of an accepted carrier. The rows $A,Q,J$ are the only data available before the defect margin is read, so the consumer first sees the positive source windows, adjacent quotient windows, and the displayed tail threshold.

The margin row $M$ then records the Raabe first-order defect on the opened tail. It is a finite row tied to the current request, not a global choice of a sum or a detached convergence theorem.

Next inspect $D,W,T$. The dyadic ledger and stream window rows convert the displayed defect margin into the same finite-window discipline read by the ratio-test corridor and its condensation, Abel, and root-test sibling surfaces. The route row $T$ is therefore the only bridge from quotient evidence to the endpoint handoff.

Only after $T$ has been displayed may the packet expose $E$, the regular rational and Real-facing handoff. Transport, replay, provenance, and local naming preserve this order, so no endpoint consumer can bypass the route through selected sums, quotient stream equality, countable choice, trichotomy, host summability, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RaabeTestUp$ endpoint-facing read factors through positive source windows, adjacent quotient windows, a unary tail threshold, a first-order defect-margin row, DyadicRatCore tolerance cells, StreamName windows, a ratio-window route row, RegSeqRat and RealUp endpoint routing, hsame transport, Cont replay, Pkg provenance, and local NameCert data.}

\independenceWitness{real-series-ratio-test, cauchy-condensation-test, abel-test, weierstrass-root-test, streamname, regseqrat, real: no listed sibling supplies the whole packet $R=(A,Q,J,M,D,W,T,E,H,C,P,N)$, because each sibling contributes only a ratio corridor, dyadic block comparison, Abel boundary, root majorant, finite window surface, regular readback, or endpoint seal while this chapter binds the Raabe threshold, first-order defect margin, transport, replay, provenance, and local naming as one certificate.}

\closureat{RaabeTestUp}{seedStr}

\begin{closurestatus}{\RaabeTestUp}
  \constructivestory{A $\RaabeTestUp$ packet is generated from positive source windows, adjacent quotient windows, a unary index threshold, a first-order defect margin, DyadicRatCore tolerance data, StreamName windows, a ratio-window route row, RegSeqRat and RealUp endpoint routing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:raabe-test-carrier}, \autoref{thm:raabe-test-namecert-obligations}, and \autoref{thm:raabe-test-ratio-window-route} seed the finite Raabe ratio-refinement route from displayed positive-series quotient windows through regular rational readback and the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected sums, countable choice, quotient stream equality, trichotomy, host summability, ambient $\RealUp$ completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, quotient-window exactness, unary threshold stability, defect-margin coverage, dyadic tolerance routing, StreamName and RegSeqRat handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RaabeTestUp.}
\end{closurestatus}
