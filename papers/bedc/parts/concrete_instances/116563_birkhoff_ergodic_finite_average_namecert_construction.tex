\chapter{A Concrete Naming Certificate for $\BirkhoffErgodicFiniteAverageUp$}
\label{ch:concrete-instances-birkhoff-ergodic-finite-average-namecert}
\origin{human}

Following Birkhoff and von Neumann, the $\BirkhoffErgodicFiniteAverageUp$ packet records the finite orbit-average surface that precedes any pointwise or mean ergodic convergence theorem. It sits between the measure-preserving source of \autoref{ch:concrete-instances-ergodic-namecert}, the displayed transition rows of \autoref{ch:concrete-instances-dynsystem-namecert}, the probability-space ledger of \autoref{ch:concrete-instances-probspace-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives later mean-ergodic and recurrence-facing chapters a finite average row without importing measure-theoretic choice principles.

\begin{definition}[Birkhoff ergodic finite-average carrier]
\label{def:birkhoff-ergodic-finite-average-carrier}
A $\BirkhoffErgodicFiniteAverageUp$ carrier is a finite $\BHist$ packet
$$
F=(E,D,P,W,O,S,R,A,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\ErgodicUp$ source row, exposing the measure-preserving transformation, invariant-row ledger, and finite measure endpoint data consumed by the average.
\item $D$ is the $\DynSystemUp$ step row for the same transformation, recording the displayed source point, successor route, and finite iterate endpoints.
\item $P$ is the $\ProbSpaceUp$ averaging provenance row, recording the unit-mass probability source and the event or observable window through which the average is read.
\item $W$ is the finite orbit-window row, storing the inspected segment $x,Tx,\ldots,T^{n-1}x$ together with the visible window length $n$.
\item $O$ is the finite observation row, recording the observable values attached to the window entries and read only through $\StreamNameUp$ finite observations.
\item $S$ is the finite sum and denominator row used to form the displayed average of $O$ over $W$.
\item $R$ is the $\RegSeqRatUp$ readback row, exposing the rational regular-name route for the average value before the terminal real seal is read.
\item $A$ is the $\RealUp$ averaged endpoint seal, downstream of $E,D,P,W,O,S,R$ and never a selected almost-everywhere limit.
\item $H$ records componentwise $\hsame$ transport for the ergodic source, dynamic steps, probability provenance, orbit window, observations, finite sum, rational readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite averaging request through $E,D,P,W,O,S,R,A,H,Q,N$.
\item $Q$ is the $\Pkg$ provenance over $\BirkhoffErgodicFiniteAverageUp$, $\ErgodicUp$, $\DynSystemUp$, $\ProbSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BirkhoffErgodicFiniteAverageUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,D,P,W,O,S,R,A,H,C,Q,N$. It has no coordinate for the full almost-everywhere Birkhoff theorem, invariant sigma-algebra projection, countable choice, quotient a.e. equality, selected orbit limit, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Birkhoff finite-average NameCert obligations]
\label{thm:birkhoff-ergodic-finite-average-namecert-obligations}
For every accepted $\BirkhoffErgodicFiniteAverageUp$ carrier $F=(E,D,P,W,O,S,R,A,H,C,Q,N)$, the local $\NameCert_{\BirkhoffErgodicFiniteAverageUp}$ surface consists exactly of carrier admission, measure-preserving source exposure, dynamic-step window exactness, probability-provenance visibility, finite StreamName observation routing, finite sum and denominator exactness, RegSeqRat readback, Real seal ordering, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming for the finite carrier of \autoref{def:birkhoff-ergodic-finite-average-carrier}.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:birkhoff-ergodic-finite-average-carrier}. The accepted packet exposes exactly the ergodic source, dynamic step row, probability provenance row, finite orbit window, observation row, finite sum row, rational readback row, Real seal, transport, replay, provenance, and local name.

Window exactness is read from $D,W,O$. The dynamic row supplies the inspected iterates, the window row fixes the finite prefix, and the observation row records only the StreamName-visible values attached to that prefix.

The averaging obligation is read from $P,S,R,A$. The probability-space provenance fixes the averaging source, the finite sum and denominator row computes the displayed average, the RegSeqRat row exposes the rational regular-name readback, and the Real seal is visible only after those rows have been displayed.

The structural obligations are exhausted by $H,C,Q,N$. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming preserve the same finite route and add no convergence theorem, invariant sigma-algebra projection, quotient a.e. equality, countable-choice schedule, selected limit, or ambient Real completeness coordinate.
\end{proof}

\begin{theorem}[Birkhoff finite-average readback route]
\label{thm:birkhoff-ergodic-finite-average-readback-route}
Every average exported by an accepted $\BirkhoffErgodicFiniteAverageUp$ packet factors through the finite route
$$
\ErgodicUp,\ \DynSystemUp,\ \ProbSpaceUp \longmapsto W,O,S \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
Thus a downstream mean-ergodic or recurrence-facing consumer receives a displayed orbit window, finite observation ledger, finite average row, regular rational readback, and terminal real-name seal before it can consume the average.
\end{theorem}
\begin{proof}
Start with an averaging request. The request first opens the $\ErgodicUp$ and $\DynSystemUp$ rows, so the transformation and its finite iterates are read from displayed BEDC rows rather than from a host measurable dynamical system.

Next read the $\ProbSpaceUp$ provenance and the window rows $W,O,S$. These rows fix the probability source, the inspected orbit segment, the observed values, and the finite sum with its denominator.

The average then passes through $R$ before $A$ is visible. This forces the regular rational readback to appear before any $\RealUp$ seal is consumed, so the exported endpoint is a finite NameCert route rather than a hidden limit.

Finally transport and replay through $H,C,Q,N$. Since the carrier has no coordinate for the full almost-everywhere Birkhoff theorem, invariant sigma-algebra projection, countable choice, quotient a.e. equality, selected orbit limit, or ambient $\RealUp$ completeness, every exported average follows the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\BirkhoffErgodicFiniteAverageUp$ average read factors through one ErgodicUp source row, one DynSystemUp finite orbit-window row, one ProbSpaceUp averaging provenance row, finite StreamNameUp observations, a finite sum and denominator row, RegSeqRatUp readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{ergodic, dynsystem, probspace, streamname, regseqrat, real: $\BirkhoffErgodicFiniteAverageUp$ is not bijective to any listed sibling because it jointly carries a measure-preserving source, dynamic orbit windows, probability provenance, finite observations, finite average rows, rational readback, Real sealing, transport, replay, provenance, and local naming while excluding convergence, quotient, choice, and completeness coordinates.}

\closureat{BirkhoffErgodicFiniteAverageUp}{seedStr}

\begin{closurestatus}{\BirkhoffErgodicFiniteAverageUp}
  \constructivestory{A $\BirkhoffErgodicFiniteAverageUp$ packet is generated from ErgodicUp measure-preserving source rows, DynSystemUp finite orbit-window rows, ProbSpaceUp averaging provenance rows, StreamNameUp finite observation rows, RegSeqRatUp readback rows, RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:birkhoff-ergodic-finite-average-carrier}, \autoref{thm:birkhoff-ergodic-finite-average-namecert-obligations}, and \autoref{thm:birkhoff-ergodic-finite-average-readback-route} seed the finite orbit-average carrier, its local NameCert obligation surface, and its readback route through ErgodicUp, DynSystemUp, ProbSpaceUp, StreamNameUp, RegSeqRatUp, and RealUp rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim the full almost-everywhere Birkhoff theorem, invariant sigma-algebra projection, countable choice, quotient a.e. equality, selected orbit limits, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dynamic-window exactness, probability-provenance visibility, finite observation routing, finite average exactness, RegSeqRat readback, Real seal ordering, transport, replay, provenance, local naming, and TasteGate field faithfulness for BEDC.Derived.BirkhoffErgodicFiniteAverageUp.}
\end{closurestatus}
