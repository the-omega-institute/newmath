The Cauchy completion idempotent packet isolates the finite public interface for
repeated completion reads.
It consumes the Cauchy completion monad route of \autoref{ch:concrete-instances-cauchycompletionmonad-namecert} and the $\MetricCompletionUp$ completion surface of \autoref{ch:concrete-instances-metriccompletion-namecert} before any repeated completion read is accepted.

\input{parts/concrete_instances/cauchy_completion_idempotent/cauchy_completion_idempotent_namecert_construction.tex}
