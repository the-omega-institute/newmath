\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionUniversalPropertyUp$}
\label{ch:concrete-instances-regular-cauchy-completion-universal-property-namecert}
\origin{human}

Following Bishop--Bridges and Kelley-style completion theory, the $\RegularCauchyCompletionUniversalPropertyUp$ packet records the finite universal-property surface for regular Cauchy completion. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the metric-completion handoff of \autoref{ch:concrete-instances-metriccompletion-namecert}, the uniform-completion route of \autoref{ch:concrete-instances-uniformcompletion-namecert}, and the separated-metric comparison boundary of \autoref{ch:concrete-instances-separatedmetric-namecert}. The packet names only the displayed extension and uniqueness route available to completion consumers; it does not assert quotient completion, selected representatives, ambient completeness, countable choice, or $\RealUp$ completeness.

\begin{definition}[Regular Cauchy completion universal-property carrier]
\label{def:regular-cauchy-completion-universal-property-carrier}
A $\RegularCauchyCompletionUniversalPropertyUp$ carrier is a finite $\BHist$ packet
$$
U=(D,W,Q,R,M,F,S,E,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used by the regular Cauchy readback.
\item $W$ is the $\StreamNameUp$ finite-window schedule that makes the source observations available.
\item $Q$ is the $\RegSeqRatUp$ regular rational source row read through $D$ and $W$.
\item $R$ is the terminal $\RealUp$ seal reached from the same displayed regular Cauchy route.
\item $M$ is the $\MetricCompletionUp$ handoff row consumed by metric-completion readers.
\item $F$ is the $\UniformCompletionUp$ handoff row consumed by uniform-completion readers.
\item $S$ is the $\SeparatedMetricUp$ comparison row used by uniqueness of extensions.
\item $E$ is the extension-existence row: it records the displayed route from a regular Cauchy source read through $D,W,Q,R$ into the completion-facing target read through $M,F,S$.
\item $L$ is the uniqueness ledger: it compares two extension rows over the same $D,W,Q,R,M,F,S$ route by separatedness rather than by host equality.
\item $H$ records componentwise $\hsame$ transport for $D,W,Q,R,M,F,S,E,L$ and the local naming row.
\item $C$ records displayed $\Cont$ replay of the route
$$
D,W,Q,R \longmapsto M,F,S \longmapsto E,L,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionUniversalPropertyUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\MetricCompletionUp$, $\UniformCompletionUp$, $\SeparatedMetricUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionUniversalPropertyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,W,Q,R,M,F,S,E,L,H,C,P,N$. It has no coordinate for quotient completion, representative selection, host equality of completed points, hidden completeness of the target, countable choice, or completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Regular Cauchy completion universal-property NameCert obligations]
\label{thm:regular-cauchy-completion-universal-property-namecert-obligations}
For every accepted $\RegularCauchyCompletionUniversalPropertyUp$ carrier $U=(D,W,Q,R,M,F,S,E,L,H,C,P,N)$, the local $\NameCert_{\RegularCauchyCompletionUniversalPropertyUp}$ surface consists exactly of carrier admission; dyadic tolerance exposure through $D$; finite stream exposure through $W$; regular rational source exposure through $Q$; terminal real sealing through $R$; metric and uniform completion handoff through $M,F$; separated comparison through $S$; extension existence through $E$; uniqueness through $L$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; local naming through $N$; and non-escape from quotient completion, representative selection, host equality, hidden target completeness, countable choice, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-cauchy-completion-universal-property-carrier}. The accepted packet exposes exactly the dyadic tolerance row, stream-window row, regular rational source, real seal, metric-completion handoff, uniform-completion handoff, separated comparison row, extension row, uniqueness ledger, transport, replay, provenance, and local name.

The regular source route is finite. Rows $D,W,Q$ provide the dyadic, stream, and regular rational data before the terminal real seal $R$ is read, so the extension row cannot begin from a hidden completed point.

The completion-facing route is also finite. Rows $M,F,S$ record the metric, uniform, and separated target surfaces before $E$ and $L$ are inspected, so existence and uniqueness are read through displayed completion data rather than through a quotient or a selected representative.

Rows $H,C,P,N$ transport, replay, source, and name the same packet. Since no excluded host-side coordinate exists in the carrier, the listed rows exhaust the local $\NameCert$ surface.
\end{proof}

\begin{theorem}[Regular Cauchy completion universal-property core]
\label{thm:regular-cauchy-completion-universal-property-core}
Every extension-and-uniqueness read of an accepted $\RegularCauchyCompletionUniversalPropertyUp$ packet factors through
$$
D,W,Q,R \longmapsto M,F,S \longmapsto E,L,H,C,P,N .
$$
The route first exposes dyadic tolerance, stream windows, regular rational source data, and the terminal real seal; then it exposes metric completion, uniform completion, and separated comparison rows; and only then exports extension existence and uniqueness. No accepted read exports quotient completion, selected representatives, ambient completeness, countable choice, host equality of completed points, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the accepted carrier $U=(D,W,Q,R,M,F,S,E,L,H,C,P,N)$. Its continuation row $C$ displays the order in which the regular Cauchy source route is made available before the completion-facing rows are inspected.

The first stage is $D,W,Q,R$. It uses the dyadic tolerance ledger, the finite stream schedule, and the regular rational source to reach the real seal; none of these rows supplies a quotient object or a selected limit.

The second stage is $M,F,S$. The metric and uniform completion rows are consumed together with the separated comparison row, so the uniqueness ledger can compare extensions by separatedness inside the packet.

The final stage is $E,L,H,C,P,N$. Extension existence and uniqueness are transported, replayed, sourced, and named by the displayed structural rows. Because the carrier has no additional coordinate, every accepted universal-property read factors through the stated route.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyCompletionUniversalPropertyUp$ consumer that exports extension existence or uniqueness while omitting any of the dyadic tolerance, stream-window, regular rational source, real seal, metric-completion, uniform-completion, separated comparison, transport, replay, provenance, or local naming rows refutes the packet.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, metriccompletion, uniformcompletion, separatedmetric: none is bijective because $\RegularCauchyCompletionUniversalPropertyUp$ jointly carries tolerance, finite observation, regular source, terminal seal, metric handoff, uniform handoff, separated comparison, extension, uniqueness, transport, replay, provenance, and local NameCert rows in one universal-property packet.}

\closureat{RegularCauchyCompletionUniversalPropertyUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionUniversalPropertyUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-universal-property-carrier}, \autoref{thm:regular-cauchy-completion-universal-property-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-universal-property-core} seed the finite regular-Cauchy universal-property surface over dyadic, stream, regular rational, real, metric-completion, uniform-completion, separated-metric, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion, selected representatives, ambient completeness, countable choice, host equality of completed points, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, regular source exposure, completion handoff, separated uniqueness, structural transport, provenance non-escape, and TasteGate field faithfulness for BEDC.Derived.RegularCauchyCompletionUniversalPropertyUp.}
  \constructivestory{A $\RegularCauchyCompletionUniversalPropertyUp$ packet is generated from dyadic tolerance data, finite stream windows, regular rational source rows, a terminal Real seal, metric and uniform completion rows, a separated comparison row, extension and uniqueness rows, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
\end{closurestatus}
