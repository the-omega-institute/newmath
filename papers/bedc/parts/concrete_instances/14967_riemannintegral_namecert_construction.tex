\chapter{A Concrete Naming Certificate for $\RiemannIntegralUp$}
\label{ch:concrete-instances-riemannintegral-namecert}
\origin{human}

Following Riemann and Darboux, the $\RiemannIntegralUp$ packet records a tagged-mesh integration surface whose accepted sums are routed through the Darboux upper-lower gap and dyadic step-function mesh rows. It sits after the Darboux carrier of \autoref{ch:concrete-instances-darbouxintegral-namecert}, the stepwise approximation surface of \autoref{ch:concrete-instances-dyadicstepfunction-namecert}, and the dyadic mesh ledger of \autoref{ch:concrete-instances-dyadicmesh-namecert}. The packet names a finite constructive integration boundary over one displayed interval and integrand window; it does not assert host completeness, quotient equality of real functions, open-cover compactness, or a measure-theoretic integral.

\begin{definition}[Riemann integral carrier]
\label{def:riemann-integral-carrier}
A $\RiemannIntegralUp$ carrier is a finite $\BHist$ packet
$$
I=(M,T,F,S,D,G,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\DyadicMeshUp$ row naming the interval endpoints, cell subdivision, mesh bound, and cell adjacency ledger.
\item $T$ is the finite tag row, with each tag carried inside the corresponding mesh cell by an explicit dyadic or rational containment witness.
\item $F$ is the integrand-read window, exposing only the finitely displayed values needed at the tags and at the upper/lower step approximants.
\item $S$ is the finite Riemann-sum row computed from the tag values, cell widths, and displayed arithmetic ledger.
\item $D$ is the $\DarbouxIntegralUp$ comparison row, recording upper and lower dyadic step-function approximants that bracket the same displayed reads.
\item $G$ is the Darboux gap and mesh-refinement ledger that bounds the distance between the tagged sum and the Darboux interval determined by $D$.
\item $R$ is the $\RegSeqRatUp$ and $\RealUp$ handoff surface reached only after the displayed sums and gap ledger are present.
\item $H$ records componentwise $\hsame$ transport for mesh, tags, integrand reads, sums, Darboux comparison, gap, handoff, provenance, and the local naming row.
\item $C$ records displayed $\Cont$ replay from mesh cells to tag reads, from tag reads to finite sums, from sums to Darboux comparison, and from the gap ledger to the real-facing handoff.
\item $P$ is the $\Pkg$ provenance over $\RiemannIntegralUp$, $\DyadicMeshUp$, $\DyadicStepFunctionUp$, $\DarbouxIntegralUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RiemannIntegralUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,T,F,S,D,G,R,H,C,P,N$. It has no coordinate for a host function quotient, a completed real equality principle, a choice-selected tag family, open-cover compactness, a measure space, or an ambient completeness theorem.
\end{definition}
\leanchecked{BEDC.Derived.RiemannIntegralUp.RiemannIntegralTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Riemann integral NameCert obligations]
\label{thm:riemann-integral-namecert-obligations}
For an accepted $\RiemannIntegralUp$ carrier $I=(M,T,F,S,D,G,R,H,C,P,N)$, the local $\NameCert_{\RiemannIntegralUp}$ surface consists of carrier admission, mesh-tag containment, finite sum exactness, Darboux bracket compatibility, gap-to-real handoff, componentwise transport, continuation replay, provenance, and ledger non-escape. These obligations are discharged by the displayed route
$$
M\longmapsto T\longmapsto F\longmapsto S\longmapsto D\longmapsto G\longmapsto R
$$
together with $H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannIntegralUp.RiemannIntegralPacket\_namecert\_obligations}
\begin{proof}
Carrier admission is the projection of the tuple in \autoref{def:riemann-integral-carrier}. The mesh row $M$ displays the finite cells, and the tag row $T$ records containment inside those cells before any value of the integrand is read.

Finite sum exactness is then a ledger calculation over $F$ and $S$. Each summand uses only the carried tag value and the carried cell width, so the sum is a finite $\BHist$ computation rather than a host-level limiting assertion.

Darboux bracket compatibility is provided by the comparison row $D$. The upper and lower dyadic step-function approximants bound the same displayed reads, and the gap row $G$ records the visible estimate between the tagged sum and the Darboux bracket.

The rows $H$ and $C$ transport and replay the same route componentwise. The package row $P$ and name row $N$ source the packet, while $R$ is downstream of the mesh, sum, Darboux, and gap data. Since none of the excluded host objects appears as a carrier coordinate, the listed obligations exhaust the certificate surface.
\end{proof}

\begin{theorem}[Darboux and step scope of Riemann sums]
\label{thm:riemann-integral-darboux-step-scope}
Every accepted $\RiemannIntegralUp$ read factors through a $\DyadicMeshUp$ row, a finite tag row, a $\DyadicStepFunctionUp$ bracket supplied by the $\DarbouxIntegralUp$ comparison row, and the displayed gap ledger before the packet can be consumed as a real-facing integral. The exported scope is therefore a finite tagged-mesh comparison surface, not a quotient class of functions and not an ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:riemann-integral-carrier}. The mesh row $M$ supplies the finite cell surface, and the tag row $T$ is checked against those cells before the integrand-read row $F$ is available.

The finite sum row $S$ is computed from those displayed reads. The comparison row $D$ then supplies the Darboux upper and lower step-function bracket, so any step-function evidence used by the packet is already part of the visible Darboux route.

The gap row $G$ is read after the sum and bracket rows. Thus a real-facing consumer reaches $R$ only through the displayed mesh, tags, finite sum, Darboux bracket, and gap data. No carrier coordinate can export quotient function equality or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.RiemannIntegralUp.RiemannIntegralCarrier\_darboux\_step\_scope}

\begin{theorem}[Riemann integral real seal boundary]
\label{thm:riemann-integral-real-seal-boundary}
An accepted $\RiemannIntegralUp$ packet exports to $\RealUp$ only the real-facing handoff row $R$ produced from the finite route
$$
M\longmapsto T\longmapsto F\longmapsto S\longmapsto D\longmapsto G .
$$
The boundary seal records the tagged-mesh sum and Darboux-gap evidence already displayed in the packet, and it does not claim host completeness, quotient real equality, open-cover compactness, or a full measure-theoretic integral.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannIntegralUp.RiemannIntegralPacket\_real\_seal\_boundary}
\begin{proof}
The handoff row $R$ is a coordinate of the carrier, but it is downstream of the mesh, tag, read, sum, Darboux, and gap rows. A $\RealUp$ consumer therefore receives only a row whose construction has passed through the displayed finite route.

Componentwise transport through $H$ preserves the same coordinates, and continuation replay through $C$ rebuilds the same route when the packet is consumed. The package row $P$ and local name $N$ record this provenance without adding analytic structure.

The excluded objects are absent from the carrier: there is no host completion witness, no quotient equality of real functions, no open-cover compactness row, and no measure-theoretic carrier. Hence the real seal boundary is exactly the displayed tagged-mesh and Darboux-gap handoff.
\end{proof}

\begin{theorem}[Riemann integral scope closure package]
\label{thm:riemann-integral-scope-closure-package}
Every accepted $\RiemannIntegralUp$ read is exhausted by the dyadic mesh row, tag-containment row, integrand-read window, finite-sum row, Darboux bracket, gap ledger, regular-rational and $\RealUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local NameCert rows named in \autoref{def:riemann-integral-carrier}. The scope contains no quotient function equality, open-cover compactness, measure-integration carrier, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start from the carrier description in \autoref{def:riemann-integral-carrier}. Its coordinates are exactly $M,T,F,S,D,G,R,H,C,P,N$, so any accepted read first exposes a dyadic mesh, tag containment, integrand reads, finite sum data, Darboux comparison, gap estimate, and real-facing handoff.

Apply \autoref{thm:riemann-integral-namecert-obligations}. The NameCert surface names the same carrier admission, mesh-tag containment, finite sum exactness, Darboux bracket compatibility, gap-to-real handoff, transport, replay, provenance, and ledger non-escape rows.

Use \autoref{thm:riemann-integral-darboux-step-scope} and \autoref{thm:riemann-integral-real-seal-boundary}. The first confines every integration read to the tagged-mesh and Darboux-step route; the second makes the $\RealUp$ seal downstream of that finite route.

The excluded host objects have no row in the displayed carrier and cannot be introduced by $H,C,P$, or $N$. Therefore the listed rows form the scoped package for every accepted $\RiemannIntegralUp$ read.
\end{proof}
\leanchecked{BEDC.Derived.RiemannIntegralUp.RiemannIntegralCarrier\_scope\_closure\_package}

\begin{theorem}[Riemann integral tagged-sum totality]
\label{thm:riemann-integral-tagged-sum-totality}
For every accepted $\RiemannIntegralUp$ carrier $I=(M,T,F,S,D,G,R,H,C,P,N)$, the tagged-sum row $S$ is total only over the displayed $\DyadicMeshUp$ row $M$, tag-containment row $T$, and integrand-read window $F$. A scoped consumer cannot read $S$ before the mesh cells, tags, and finite values have been exposed and replayed through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:riemann-integral-carrier}. The rows $M,T,F,S$ are the only tagged-sum coordinates available before the Darboux comparison row $D$ is read.

Apply the NameCert obligations of \autoref{thm:riemann-integral-namecert-obligations}. They place mesh-tag containment and finite sum exactness on the route from $M$ through $T,F$ to $S$.

Use the Darboux-step scope and scope-closure package, \autoref{thm:riemann-integral-darboux-step-scope} and \autoref{thm:riemann-integral-scope-closure-package}. These results keep every scoped integration read inside the same $\DyadicMeshUp$ and finite tagged-sum boundary before a consumer reaches $\DarbouxIntegralUp$, $\RegSeqRatUp$, or $\RealUp$ rows.
\end{proof}

\begin{theorem}[Riemann integral Darboux-gap exactness]
\label{thm:riemann-integral-darboux-gap-exactness}
For every accepted $\RiemannIntegralUp$ carrier $I=(M,T,F,S,D,G,R,H,C,P,N)$, the gap ledger $G$ is exact only relative to the finite tagged sum $S$ and the $\DarbouxIntegralUp$ comparison row $D$ built from the same $M,T,F$ data. The scoped packet admits no Darboux bracket or gap estimate detached from the displayed $\DyadicStepFunctionUp$ and tagged-mesh route.
\end{theorem}
\begin{proof}
Start with the carrier and NameCert obligations. The Darboux comparison row $D$ is read after the tagged-sum row $S$, and the gap row $G$ is read after $D$ on the displayed route.

Apply Darboux-step scope. Theorem~\ref{thm:riemann-integral-darboux-step-scope} binds the step-function bracket to the same $\DyadicMeshUp$ row, tag row, integrand reads, and finite sum already displayed by the packet.

The scope-closure package then keeps the gap ledger in that chain. Since $G$ has no independent coordinate for host function equality, open-cover compactness, measure integration, or ambient completeness, its exactness is exactly the replayed relation between $S$ and $D$ through $H,C,P,N$.
\end{proof}

\begin{theorem}[Riemann integral RegSeqRat handoff totality]
\label{thm:riemann-integral-regseqrat-handoff-totality}
For every accepted $\RiemannIntegralUp$ carrier $I=(M,T,F,S,D,G,R,H,C,P,N)$, the $\RegSeqRatUp$ and $\RealUp$ handoff row $R$ is total only after the tagged-sum row $S$, $\DarbouxIntegralUp$ comparison row $D$, and Darboux-gap ledger $G$ have been displayed. A scoped consumer reaches $R$ through $M,T,F,S,D,G,H,C,P,N$ and cannot import a separate real-completion assertion or quotient equality of functions.
\end{theorem}
\begin{proof}
Read the real-facing boundary through \autoref{thm:riemann-integral-real-seal-boundary}. The theorem places $R$ downstream of the finite route from mesh and tags to finite sum, Darboux comparison, and gap ledger.

Apply the NameCert obligations and Darboux-gap exactness. The obligations expose the carried route $M,T,F,S,D,G,R$, while \autoref{thm:riemann-integral-darboux-gap-exactness} keeps $G$ attached to the same tagged-sum and Darboux rows before $R$ is read.

Finally use scope closure. Theorem~\ref{thm:riemann-integral-scope-closure-package} confines the $\RegSeqRatUp$ and $\RealUp$ handoff to $R$ with componentwise transport, replay, provenance, and local naming supplied by $H,C,P,N$, so the consumer receives no separate host completion, quotient function equality, open-cover compactness, or measure-integration carrier.
\end{proof}

\begin{theorem}[Riemann integral Darboux-RegSeqRat handoff]
\label{thm:riemann-integral-darboux-regseqrat-handoff}
For every accepted $\RiemannIntegralUp$ carrier $I=(M,T,F,S,D,G,R,H,C,P,N)$, any $\RegSeqRatUp$ or $\RealUp$ handoff read from $R$ factors through the same finite $\DarbouxIntegralUp$ bracket row $D$ and gap ledger $G$ that compare the tagged sum $S$ against the displayed mesh and tag data. A consumer cannot replace that route by a detached regular rational sequence, a host integral, or a completed real equality claim.
\end{theorem}
\begin{proof}
Start with the real-seal boundary and the RegSeqRat handoff totality theorem. Theorems~\ref{thm:riemann-integral-real-seal-boundary} and~\ref{thm:riemann-integral-regseqrat-handoff-totality} place $R$ downstream of $M,T,F,S,D,G$.

The Darboux bracket is fixed by the same displayed rows. Theorem~\ref{thm:riemann-integral-darboux-step-scope} supplies the step-function bracket row $D$, while \autoref{thm:riemann-integral-darboux-gap-exactness} keeps the gap ledger $G$ attached to that bracket and to the finite tagged sum $S$.

Finally apply the NameCert obligations and scope-closure package. Theorems~\ref{thm:riemann-integral-namecert-obligations} and~\ref{thm:riemann-integral-scope-closure-package} preserve the route through $H,C,P,N$, so a regular-rational or real-facing read has no coordinate outside the displayed Darboux and gap ledger chain.
\end{proof}

\begin{theorem}[Riemann integral mesh-refinement locality]
\label{thm:riemann-integral-mesh-refinement-locality}
Every mesh-refinement read admitted by a $\RiemannIntegralUp$ packet can only refine the displayed $\DyadicMeshUp$ row $M$ and tag-containment row $T$, then replay the corresponding finite reads through $F,S,D,G,R,H,C,P,N$. It cannot introduce host function equality, a new tag family selected outside the packet, an open-cover compactness assertion, or an ambient integration theorem.
\end{theorem}
\begin{proof}
Project the carrier through \autoref{def:riemann-integral-carrier}. The only mesh and tag coordinates are $M$ and $T$, so any refinement request begins by replacing or subdividing those displayed rows.

Use tagged-sum totality and Darboux-step scope. Theorem~\ref{thm:riemann-integral-tagged-sum-totality} requires $S$ to be computed from $M,T,F$, and \autoref{thm:riemann-integral-darboux-step-scope} keeps the Darboux comparison row $D$ on the same mesh and tag route.

The gap, handoff, and structural rows then replay the refined route. Theorems~\ref{thm:riemann-integral-darboux-gap-exactness} and~\ref{thm:riemann-integral-regseqrat-handoff-totality} keep $G$ and $R$ downstream, while $H,C,P,N$ preserve provenance and naming. Since no carrier row stores host function equality or an outside tag selector, locality is exactly the displayed refinement route.
\end{proof}

\begin{theorem}[Riemann integral tagged-sum ledger nonescape]
\label{thm:riemann-integral-tagged-sum-ledger-nonescape}
For every accepted $\RiemannIntegralUp$ carrier $I=(M,T,F,S,D,G,R,H,C,P,N)$, the tagged-sum ledger exposes only the displayed values in $F$, the cell widths carried by $M$, tag containment from $T$, the finite sum row $S$, componentwise transport $H$, replay $C$, provenance $P$, and local NameCert row $N$. The tagged sum cannot export a hidden integrand, a host equality of functions, a measure-theoretic integral, or an ambient completeness witness.
\end{theorem}
\begin{proof}
Read the tagged-sum row first. Theorem~\ref{thm:riemann-integral-tagged-sum-totality} makes $S$ total only over the displayed mesh row $M$, tag-containment row $T$, and integrand-read window $F$.

Attach the certificate surface. Theorem~\ref{thm:riemann-integral-namecert-obligations} lists finite sum exactness, transport, replay, provenance, and local naming as explicit obligations, so the ledger is exhausted by $M,T,F,S,H,C,P,N$ before any Darboux or real-facing handoff is read.

Finally use the scope package and real-seal boundary. Theorems~\ref{thm:riemann-integral-scope-closure-package} and~\ref{thm:riemann-integral-real-seal-boundary} exclude quotient function equality, open-cover compactness, measure-integration carriers, and ambient completeness from the packet. Hence the tagged-sum ledger has no escape beyond the displayed finite rows.
\end{proof}

\closureat{RiemannIntegralUp}{scopedStr}

\begin{closurestatus}{\RiemannIntegralUp}
  \constructivestory{A $\RiemannIntegralUp$ packet is generated from a dyadic mesh row, finite tag containment row, integrand-read window, finite Riemann-sum row, Darboux comparison row, gap ledger, regular-rational and real-facing handoff, componentwise transport, continuation replay, provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:riemann-integral-carrier}, \autoref{thm:riemann-integral-namecert-obligations}, \autoref{thm:riemann-integral-darboux-step-scope}, and \autoref{thm:riemann-integral-real-seal-boundary} bind the carrier, NameCert obligations, Darboux-step scope, and Real-seal boundary for the tagged-mesh Riemann integration surface over $\DyadicMeshUp$, $\DyadicStepFunctionUp$, $\DarbouxIntegralUp$, $\RegSeqRatUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host completeness, quotient real equality, open-cover compactness, measure-theoretic integration, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, mesh-tag containment, finite sum exactness, Darboux bracket compatibility, gap-to-real handoff, transport, replay, provenance, and ledger non-escape for BEDC.Derived.RiemannIntegralUp.}
\end{closurestatus}
