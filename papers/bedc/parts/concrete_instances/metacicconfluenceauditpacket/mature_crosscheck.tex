\begin{theorem}[MetaCIC confluence audit mature crosscheck]
\label{thm:metacic-confluence-auditpacket-mature-crosscheck}
Every mature read of an accepted $\MetaCICConfluenceAuditPacketUp$ carrier that consumes the candidate-normalization handoff factors through the audit anchor, candidate evidence, residual confluence envelope, decidable boundary, blocked-edge row, transport, replay, provenance, and local $\NameCert$ rows of the displayed packet. The handoff is therefore a bounded residual route; it does not export full confluence, subject reduction, evaluator equality, quotient equality of terms, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the candidate-normalization sibling route of \autoref{lem:metacic-confluence-audit-candidate-normalization-sibling-route}. It allows the neighboring handoff to attach only as a boundary route, not as a new carrier coordinate.

Read the candidate evidence through \autoref{thm:metacic-confluence-audit-candidate-normalization-handoff} and the residual envelope through \autoref{thm:metacic-confluence-candidate-residual-diamond-handoff}. These rows keep the candidate witness inside the displayed audit surface and route residual diamond use through the finite critical-pair and substitution windows.

Next expose the decidable boundary and blocked-edge row. The closed-face decider boundary, residual critical-pair boundary, and obstruction ledger force the read through $D,C,N,A_0,S,R,H,T,P,L$ before any confluence-facing conclusion can be consumed.

Finally project the audit carrier and its NameCert obligations. The rows $H,T,P,L$ supply componentwise transport, displayed replay, provenance, and local naming for the same packet, while the carrier has no coordinate for full confluence, subject reduction, evaluator equality, quotient equality of terms, or kernel consistency. The candidate-normalization handoff is therefore consumed only as a bounded residual route.
\end{proof}
