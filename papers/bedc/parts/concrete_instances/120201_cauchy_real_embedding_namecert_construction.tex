\chapter{A Concrete Naming Certificate for $\CauchyRealEmbeddingUp$}
\label{ch:concrete-instances-cauchy-real-embedding-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names, the $\CauchyRealEmbeddingUp$ packet records the general seal route from an accepted regular Cauchy source to the terminal $\RealUp$ interface. It sits after the regular Cauchy carrier of \autoref{ch:concrete-instances-regular-cauchy-name-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the stationary rational subcase of \autoref{ch:concrete-instances-stationary-rational-cauchy-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a general regular-Cauchy-to-Real handoff; it is not a quotient Cauchy class, a selected host limit, a stream equality theorem, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[CauchyRealEmbedding carrier]
\label{def:cauchy-real-embedding-carrier}
A $\CauchyRealEmbeddingUp$ carrier is a finite $\BHist$ packet
$$
E=(G,W,R,D,S,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $G$ is the accepted $\RegularCauchyNameUp$ source packet whose finite windows, dyadic-radius rows, regular rational readbacks, and real seal are visible to the consumer.
\item $W$ is the $\StreamNameUp$ finite-window handoff selected for the requested precision reads.
\item $R$ is the $\RegSeqRatUp$ readback boundary read through the displayed window.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the regularity and comparison rows.
\item $S$ is the terminal $\RealUp$ seal row reached only after $G,W,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the regular Cauchy source, stream window, regular rational readback, dyadic tolerance ledger, seal row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the same regular-Cauchy-to-Real route.
\item $P$ records $\Pkg$ provenance over $\CauchyRealEmbeddingUp$, $\RegularCauchyNameUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StationaryRationalCauchyUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CauchyRealEmbeddingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $G,W,R,D,S,H,C,P,N$. It has no coordinate for quotient Cauchy classes, host sequence equality, selected limits, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[CauchyRealEmbedding NameCert obligations]
\label{thm:cauchy-real-embedding-namecert-obligations}
For every accepted $\CauchyRealEmbeddingUp$ carrier $E=(G,W,R,D,S,H,C,P,N)$, the local $\NameCert_{\CauchyRealEmbeddingUp}$ surface consists exactly of carrier admission for the regular Cauchy source, finite StreamName window, regular rational readback, dyadic tolerance ledger, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming. The stationary rational route of \autoref{thm:stationary-rational-cauchy-real-embedding-exactness} is the constant-source subcase; the general route consumes the same kind of rows without restricting $G$ to a stationary rational window.
\end{theorem}
\begin{proof}
Project the carrier rows. The source row $G$ supplies the accepted $\RegularCauchyNameUp$ packet, while $W,R,D$ expose the stream window, regular rational readback, and dyadic tolerance information required before the seal row can be read.

Use the regular Cauchy seal boundary and factorization in \autoref{ch:concrete-instances-regular-cauchy-name-namecert}. They place every $\RealUp$ read downstream of the displayed $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ rows of the source packet.

The stationary rational theorem is then a visible subcase: when the regular Cauchy source is constant, \autoref{thm:stationary-rational-cauchy-real-embedding-exactness} identifies the same seal route through the hidden rational seed, constant stream windows, stationary regular rational member, and dyadic ledger. The carrier here keeps the general source row $G$, so no quotient class, selected host limit, or ambient completeness datum is added.
\end{proof}

\begin{theorem}[CauchyRealEmbedding Real seal scope]
\label{thm:cauchy-real-embedding-real-seal-scope}
Every $\RealUp$ consumer of an accepted $\CauchyRealEmbeddingUp$ packet must read the displayed route
$$
\RegularCauchyNameUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp
$$
through $G,W,R,D,S,H,C,P,N$ before the terminal seal is consumed. The route separates the general regular Cauchy embedding from the stationary rational subcase and exports no quotient stream equality, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with \autoref{def:cauchy-real-embedding-carrier}. The only source-facing row is $G$, and the only readback rows before the terminal seal are $W,R,D$.

Apply \autoref{thm:cauchy-real-embedding-namecert-obligations}. It requires the regular Cauchy source, finite window, regular rational readback, dyadic tolerance ledger, seal row, transport, replay, provenance, and local naming to be visible in the same packet.

Finally compare the constant-source branch with \autoref{thm:stationary-rational-cauchy-real-embedding-exactness}. That theorem supplies the stationary rational exactness row, while the present carrier records the general route from any accepted regular Cauchy source. Since no carrier coordinate names quotient stream equality, selected limits, countable choice, or ambient completeness, the seal scope is exactly the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyRealEmbeddingUp$ read is refuted if it consumes a terminal $\RealUp$ seal before displaying the $\RegularCauchyNameUp$ source, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{regular-cauchy-name, streamname, regseqrat, dyadicratcore, stationary-rational-cauchy, real: $\CauchyRealEmbeddingUp$ is not bijective to any listed sibling because it jointly records the general regular Cauchy source, finite window, regular rational readback, dyadic tolerance ledger, terminal seal, transport, replay, provenance, and local naming route.}

\closureat{CauchyRealEmbeddingUp}{seedStr}

\begin{closurestatus}{\CauchyRealEmbeddingUp}
  \constructivestory{A $\CauchyRealEmbeddingUp$ packet is generated from a RegularCauchyNameUp source, StreamNameUp finite window, RegSeqRatUp readback boundary, DyadicRatCoreUp tolerance ledger, RealUp terminal seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-real-embedding-carrier}, \autoref{thm:cauchy-real-embedding-namecert-obligations}, and \autoref{thm:cauchy-real-embedding-real-seal-scope} seed the general regular-Cauchy-to-Real seal route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient Cauchy classes, host sequence equality, selected limits, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, regular Cauchy source exposure, StreamName window exposure, RegSeqRat readback, DyadicRatCore tolerance accounting, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.CauchyRealEmbeddingUp.}
\end{closurestatus}
