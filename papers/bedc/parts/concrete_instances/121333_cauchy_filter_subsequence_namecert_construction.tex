\chapter{A Concrete Naming Certificate for $\CauchyFilterSubsequenceUp$}
\label{ch:concrete-instances-cauchy-filter-subsequence-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy sequences and Bourbaki--Kelley on filters, the $\CauchyFilterSubsequenceUp$ packet records the finite route by which Cauchy-filter convergence evidence is read as subsequence windows without invoking countable choice. It sits between the Cauchy-filter surface of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the regular-filter handoff of \autoref{ch:concrete-instances-regularcauchyfilter-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed extraction windows: it is not a quotient filter, not a selected infinite subsequence, not a countable-choice schedule, and not an appeal to ambient $\RealUp$ completeness.

\begin{definition}[CauchyFilterSubsequence carrier]
\label{def:cauchy-filter-subsequence-carrier}
A $\CauchyFilterSubsequenceUp$ carrier is a finite $\BHist$ packet
$$
S=(F,R,W,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the source $\CauchyFilterUp$ convergence row, including the finite filter witnesses and directed refinement data visible to the packet.
\item $R$ is the $\RegularCauchyFilterUp$ handoff row that regularizes the filter evidence before any sequence-facing read is exposed.
\item $W$ is the $\StreamNameUp$ finite-window row naming the inspected subsequence requests and their displayed positions.
\item $Q$ is the $\RegSeqRatUp$ readback row for the rational tolerance windows opened by $W$.
\item $E$ is the terminal $\RealUp$ seal row reached only after $F,R,W,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the filter, regular-filter, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a filter tolerance request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyFilterSubsequenceUp$, $\CauchyFilterUp$, $\RegularCauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyFilterSubsequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,R,W,Q,E,H,C,P,N$. It has no coordinate for a chosen infinite subsequence, quotient filter equality, host sequence equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CauchyFilterSubsequence NameCert obligations]
\label{thm:cauchy-filter-subsequence-namecert-obligations}
For every accepted $\CauchyFilterSubsequenceUp$ carrier $S=(F,R,W,Q,E,H,C,P,N)$, the local $\NameCert_{\CauchyFilterSubsequenceUp}$ surface consists exactly of carrier admission for the source $\CauchyFilterUp$ row $F$, the regular-filter handoff row $R$, the finite $\StreamNameUp$ extraction windows $W$, the $\RegSeqRatUp$ readback row $Q$, the terminal $\RealUp$ seal $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, provenance $P$, and local naming $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-filter-subsequence-carrier}. The analytic rows are precisely the Cauchy-filter source, regular-filter handoff, stream-window extraction surface, regular rational readback, and terminal real-name seal.

The structural rows $H,C,P,N$ transport, replay, source, and name that same finite tuple. Since the carrier contains no slot for a selected infinite subsequence, quotient filter equality, host sequence equality, countable choice, trichotomy, or ambient completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[CauchyFilterSubsequence finite-window route]
\label{thm:cauchy-filter-subsequence-finite-window-route}
Every $\CauchyFilterSubsequenceUp$ consumer that extracts sequence-facing evidence from a Cauchy filter must factor through the displayed route
$$
\CauchyFilterUp \longmapsto \RegularCauchyFilterUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The route exposes finite subsequence windows and tolerance readback, but it never exports a selected infinite subsequence, quotient filter, countable-choice schedule, host equality of streams, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with the source row $F$. It supplies only displayed filter convergence evidence, so a sequence-facing consumer must first read the regular-filter handoff $R$.

The handoff $R$ opens the finite window row $W$. The regular rational readback $Q$ can then inspect the tolerance windows named by $W$, and the terminal seal $E$ is reached only after those rows have been displayed.

Transport, replay, provenance, and local naming preserve the same route through $H,C,P,N$. No row of the carrier can replace the route by a chosen infinite sequence or by quotient equality of filters or streams.
\end{proof}

\begin{theorem}[CauchyFilterSubsequence nonescape]
\label{thm:cauchy-filter-subsequence-nonescape}
Let $S=(F,R,W,Q,E,H,C,P,N)$ be an accepted $\CauchyFilterSubsequenceUp$ carrier. Every downstream Real or Completion consumer that reads subsequence evidence from $S$ can read only the displayed rows $F,R,W,Q,E,H,C,P,N$ and their sibling chapter surfaces. In particular, no consumer can escape to a hidden selector, quotient filter equality, host stream equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:cauchy-filter-subsequence-namecert-obligations}. They bind the local certificate to the nine displayed rows and leave no extra consumer coordinate.

Apply the finite-window route of \autoref{thm:cauchy-filter-subsequence-finite-window-route}. It places every sequence-facing read on the path from the Cauchy-filter row through regular-filter, stream-window, regular-rational, and Real-seal rows.

Thus a Real or Completion consumer sees exactly the visible route named by the packet. Hidden selectors, quotient equalities, countable-choice schedules, trichotomy, and ambient completeness are outside the carrier and cannot be read from it.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyFilterSubsequenceUp$ read displays a CauchyFilter row, a RegularCauchyFilter handoff, StreamName finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert row before any Real or Completion consumer extracts subsequence evidence.}

\independenceWitness{cauchyfilter, regularcauchyfilter, streamname, regseqrat, real: $\CauchyFilterSubsequenceUp$ is not bijective to any listed sibling because it jointly binds filter convergence evidence, regular-filter handoff, finite subsequence windows, regular rational readback, terminal real sealing, transport, replay, provenance, and local naming while refusing selected infinite subsequences and countable choice.}

\closureat{CauchyFilterSubsequenceUp}{seedStr}

\begin{closurestatus}{\CauchyFilterSubsequenceUp}
  \constructivestory{A $\CauchyFilterSubsequenceUp$ packet is generated from CauchyFilter convergence rows, RegularCauchyFilter handoff rows, StreamName finite subsequence windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-filter-subsequence-carrier}, \autoref{thm:cauchy-filter-subsequence-namecert-obligations}, \autoref{thm:cauchy-filter-subsequence-finite-window-route}, and \autoref{thm:cauchy-filter-subsequence-nonescape} seed the finite bridge from Cauchy-filter convergence evidence to visible subsequence windows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected infinite subsequences, quotient filters, quotient streams, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, filter-source admission, regular-filter handoff, finite window extraction, regular rational readback, seal nonescape, transport, replay, provenance, and local naming for the same packet.}
\end{closurestatus}
