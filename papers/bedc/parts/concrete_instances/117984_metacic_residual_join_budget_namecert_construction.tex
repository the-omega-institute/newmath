\chapter{A Concrete Naming Certificate for $\MetaCICResidualJoinBudgetUp$}
\label{ch:concrete-instances-metacic-residual-join-budget-namecert}
\origin{ai}

The $\MetaCICResidualJoinBudgetUp$ packet realizes the confluence remaining-question surface of \autoref{sec:metacic-open-problems-confluence} as finite residual-join budget data. It sits beside the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the closed checker seal of \autoref{ch:concrete-instances-closed-type-checker-seal-namecert}, and the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}. The packet records typed-window, residual-pair, join-candidate, checker-status, premise-retention, obstruction, transport, replay, provenance, and local naming rows before any confluence consumer reads a local join.

\begin{definition}[MetaCIC residual-join budget carrier]
\label{def:metacic-residual-join-budget-carrier}
A $\MetaCICResidualJoinBudgetUp$ carrier is a finite $\BHist$ packet
$$
J=(W,P,R,Q,S,B,O,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the typed-window row for the closed MetaCIC source under inspection;
\item $P$ is the retained residual-substitution premise row read through $\MetaCICResidualSubstitutionBoundaryUp$;
\item $R$ is the residual-pair row, naming the two residual branches whose local join is budgeted;
\item $Q$ is the proposed join-candidate row, kept visible until the bounded checker row accepts it;
\item $S$ is the $\ClosedTypeCheckerSealUp$ row for the closed type-checker and finished-normal endpoint data used by the budget;
\item $B$ is the bounded local-join checker row, recording the finite checker status rather than an unrestricted confluence theorem;
\item $O$ is the obstruction-retention row for residual-substitution compatibility, premise-free parallel diamond, closed Church--Rosser, subject reduction, evaluator equality, quotient term equality, and kernel consistency endpoints;
\item $H$ is the componentwise $\hsame$ transport row for $W,P,R,Q,S,B,O$;
\item $C$ is the displayed $\Cont$ replay row for refining the same residual budget;
\item $G$ is the $\Pkg$ provenance over $\MetaCICResidualJoinBudgetUp$, $\MetaCICParallelDiamondFrontierUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\ClosedTypeCheckerSealUp$, $\MetaCICConfluenceAuditPacketUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\MetaCICResidualJoinBudgetUp}$ row.
\end{enumerate}
The carrier has no coordinate for residual-substitution compatibility, premise-free parallel diamond, arbitrary closed confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC residual-join budget NameCert obligations]
\label{thm:metacic-residual-join-budget-namecert-obligations}
For the carrier of \autoref{def:metacic-residual-join-budget-carrier}, the local $\NameCert_{\MetaCICResidualJoinBudgetUp}$ surface consists exactly of typed-window exposure through $W$, residual-substitution premise retention through $P$, residual-pair exposure through $R$, join-candidate exposure through $Q$, closed-checker sealing through $S$, bounded local-join status through $B$, obstruction retention through $O$, componentwise transport through $H$, displayed replay through $C$, provenance through $G$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier. The typed window, residual-substitution premise, residual pair, join candidate, closed checker, bounded checker, and obstruction rows are all explicit coordinates of $J$.

Read the sibling confluence rows next. The residual-substitution boundary supplies the retained premise row $P$, the parallel-diamond frontier supplies the conditional local-diamond context, the closed checker seal supplies $S$, and the confluence audit packet supplies the consumer-facing audit boundary.

Finally attach transport, replay, provenance, and local naming. Since the carrier contains no row for residual-substitution compatibility, premise-free parallel diamond, arbitrary closed confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency, the stated NameCert surface exhausts every accepted residual-join budget read.
\end{proof}

\begin{theorem}[MetaCIC residual-join budget confluence route]
\label{thm:metacic-residual-join-budget-confluence-route}
Every confluence consumer of an accepted $\MetaCICResidualJoinBudgetUp$ packet must first expose the finite route
$$
W,P,R,Q,S,B,O \longmapsto H,C,G,N
$$
before reading any local join handoff. The route may feed $\MetaCICParallelDiamondFrontierUp$ and $\MetaCICConfluenceAuditPacketUp$ consumers, but it does not discharge residual substitution or turn the bounded checker row into a closed Church--Rosser theorem.
\end{theorem}
\begin{proof}
Start with \autoref{def:metacic-residual-join-budget-carrier}. The typed window $W$ and residual-substitution premise $P$ are visible before the residual pair $R$ and join candidate $Q$ are inspected.

Next read the checker rows. The closed type-checker seal $S$ and bounded local-join checker $B$ record only finite checker status for the displayed residual pair. The obstruction row $O$ remains visible at the same stage.

Transport and replay preserve this route through $H$ and $C$, while $G$ and $N$ keep the read inside the local package and NameCert rows. Thus the consumer receives a residual-join budget surface, not a premise-free confluence theorem.
\end{proof}

\falsifiablePrediction{Every $\MetaCICResidualJoinBudgetUp$ local-join read that omits the retained premise row $P$ fails to distinguish bounded residual checking from a premise-free parallel diamond claim.}

\independenceWitness{metacic-parallel-diamond-frontier, metacic-residual-substitution-boundary, closed-type-checker-seal, metacic-confluence-audit-packet: $\MetaCICParallelDiamondFrontierUp$ carries the conditional diamond context but not this budget's closed checker and bounded status rows; $\MetaCICResidualSubstitutionBoundaryUp$ carries the retained premise but not the join candidate; $\ClosedTypeCheckerSealUp$ carries checker sealing but not the confluence audit boundary; $\MetaCICConfluenceAuditPacketUp$ carries audit status but not the local residual-pair budget. None is bijective to $\MetaCICResidualJoinBudgetUp$.}

\closureat{MetaCICResidualJoinBudgetUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualJoinBudgetUp}
  \constructivestory{A $\MetaCICResidualJoinBudgetUp$ packet is generated from a typed MetaCIC window, residual-substitution premise, residual-pair row, join-candidate row, closed type-checker seal, bounded local-join checker, obstruction-retention row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter binds the finite carrier, NameCert obligation surface, and confluence route for bounded MetaCIC residual-join budgets.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{It does not claim residual-substitution compatibility, premise-free parallel diamond, arbitrary closed confluence, subject reduction, evaluator equality, quotient term equality, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, typed-window exposure, retained residual-substitution premise, residual-pair coverage, join-candidate exactness, closed-checker sealing, bounded local-join status, obstruction retention, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICResidualJoinBudgetUp.}
\end{closurestatus}
