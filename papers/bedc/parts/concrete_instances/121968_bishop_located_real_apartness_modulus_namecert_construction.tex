\chapter{A Concrete Naming Certificate for $\BishopLocatedRealApartnessModulusUp$}
\label{ch:concrete-instances-bishop-located-real-apartness-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopLocatedRealApartnessModulusUp$ packet records the finite located-real apartness schedule that turns a displayed separation request into dyadic brackets and a readable modulus row. It sits over the located-real source of \autoref{ch:concrete-instances-located-real-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the located-apartness comparison row of \autoref{ch:concrete-instances-located-real-apartness-namecert}. The packet names only finite bracket and modulus data; it does not assert trichotomy, quotient real equality, selected real representatives, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop located-real apartness modulus carrier]
\label{def:bishop-located-real-apartness-modulus-carrier}
A $\BishopLocatedRealApartnessModulusUp$ carrier is a finite $\BHist$ packet
$$
A=(L,E,R,D,B,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\LocatedRealUp$ source row naming the located real datum whose apartness request is being read.
\item $E$ is the terminal $\RealUp$ seal row available only after the located source and its finite readbacks have been displayed.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational windows inspected by the apartness request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row carrying dyadic radii, half-radius comparisons, endpoint brackets, and bracket-refinement requests.
\item $B$ is the located-bracket transport row, requiring each apartness request to pass through the same displayed brackets before a modulus can be consumed.
\item $M$ is the apartness-modulus extraction row: it records the finite precision schedule that turns a positive dyadic separation witness into the requested apartness margin.
\item $H$ records componentwise $\hsame$ transport for the located source, Real seal, regular-rational readback, dyadic tolerance ledger, bracket transport, modulus extraction, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay along the same located-real, regular-rational, dyadic, bracket, and modulus route.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedRealApartnessModulusUp$, $\LocatedRealUp$, $\RealUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\LocatedRealApartnessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedRealApartnessModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,E,R,D,B,M,H,C,P,N$. It has no coordinate for trichotomy, quotient real equality, selected real representatives, hidden limit choice, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop located-real apartness modulus obligations]
\label{thm:bishop-located-real-apartness-modulus-obligations}
For every accepted $\BishopLocatedRealApartnessModulusUp$ carrier
$$
A=(L,E,R,D,B,M,H,C,P,N),
$$
the local $\NameCert_{\BishopLocatedRealApartnessModulusUp}$ surface consists exactly of five rows:
\begin{enumerate}
\item carrier admission through the displayed rows $L,E,R,D,B,M,H,C,P,N$;
\item classifier stability, requiring comparison only through the same located-real, real-seal, regular-rational, dyadic, bracket, modulus, and structural rows;
\item located bracket transport through $B$, so each apartness read consumes the same finite bracket route before reaching $\LocatedRealApartnessUp$ consumers;
\item apartness modulus extraction through $M$, turning a displayed positive dyadic separation witness into the finite precision schedule requested by the consumer;
\item ledger exactness through $D,H,C,P,N$, preserving the dyadic tolerance row, componentwise transport, replay, provenance, and local naming without adding a host real equality service.
\end{enumerate}
No obligation supplies trichotomy, quotient real equality, selected representatives, hidden limit choice, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\leanstmt{BEDC.Derived.BishopLocatedRealApartnessModulusUp}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-real-apartness-modulus-carrier}. Its visible rows are exactly the located source, Real seal, regular-rational readback, dyadic tolerance ledger, bracket transport, modulus extraction, and structural naming rows.

The classifier-stability clause is immediate from that projection. Since the carrier has no coordinate outside $L,E,R,D,B,M,H,C,P,N$, an accepted comparison can inspect only those rows.

The located-bracket and apartness-modulus clauses are the two value-facing obligations. The row $B$ forces each apartness request to pass through displayed brackets, and the row $M$ extracts the finite precision schedule from the positive dyadic separation witness recorded through $D$.

Finally $H,C,P,N$ preserve the same finite route under transport, replay, provenance, and local naming. Because none of the excluded principles is a row of the packet, the listed five rows exhaust the local NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopLocatedRealApartnessModulusUp$ apartness read exposes the located-real source, Real seal, RegSeqRat readback, DyadicRatCore tolerance ledger, bracket transport, modulus extraction, hsame transport, Cont replay, provenance, and local NameCert row before using a positive separation margin.}

\independenceWitness{located-real, real, regseqrat, dyadicratcore, located-real-apartness: $\BishopLocatedRealApartnessModulusUp$ is not bijective to any listed sibling because it jointly binds a located source, real seal, regular-rational readback, dyadic tolerance ledger, bracket transport, modulus extraction, transport, replay, provenance, and local naming in one packet.}

\closureat{BishopLocatedRealApartnessModulusUp}{seedStr}

\begin{closurestatus}{\BishopLocatedRealApartnessModulusUp}
  \constructivestory{A $\BishopLocatedRealApartnessModulusUp$ packet is generated from a LocatedReal source row, RealUp seal row, RegSeqRat readback, DyadicRatCore tolerance ledger, located-bracket transport row, apartness-modulus extraction row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-real-apartness-modulus-carrier} and \autoref{thm:bishop-located-real-apartness-modulus-obligations} seed the finite located-real apartness-modulus route through brackets, dyadic tolerances, regular-rational readback, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, quotient real equality, selected real representatives, hidden limit choice, countable choice, ambient Real completeness, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, located bracket transport, apartness modulus extraction, ledger exactness, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedRealApartnessModulusUp.}
\end{closurestatus}
