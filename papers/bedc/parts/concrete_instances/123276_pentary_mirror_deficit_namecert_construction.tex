\chapter{A Concrete Naming Certificate for $\PentaryMirrorDeficitUp$}
\label{ch:concrete-instances-pentary-mirror-deficit-namecert}
\origin{ai}

The $\PentaryMirrorDeficitUp$ packet realizes the mirror-defect branch of \autoref{ch:visions-innovation-input-pentary-mirror-decomposition-gate} as finite BEDC data. It reads pentary phase words through the circle carrier of \autoref{ch:concrete-instances-s1-namecert}, keeps compact search windows visible through \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-compact-namecert}, and records only the mirror decomposition, defect rows, rank-drop gate, transport, replay, provenance, and local naming surface. It does not prove pentary Hermitian rigidity, decide every nonpalindromic chart, or identify the finite carrier with a completed algebraic variety.

\begin{definition}[Pentary mirror-deficit carrier]
\label{def:pentary-mirror-deficit-carrier}
A $\PentaryMirrorDeficitUp$ carrier is a finite $\BHist$ packet
$$
M=(S,A,Q,U,D,K,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the pentary source row, read as five $\SOneUp$ phase entries before any mirror predicate is public;
\item $A$ is the symmetric and antisymmetric coordinate row storing the displayed $s_0,s_1,c,d_0,d_1$ decomposition;
\item $Q$ is the Q3 symmetric-sum row, recording that the Q3 equation reads only $2s_0+2s_1+c$;
\item $U$ is the unit-circle compatibility row for the two mirror pairs, sourced through \autoref{ch:concrete-instances-s1-namecert};
\item $D$ is the finite Hermitian-defect row whose cleared entries lie in the palindromic ideal when the mirror-odd condition is displayed;
\item $K$ is the coefficient-matrix row for the finite rank-drop test;
\item $R$ is the compact chart and search-window row, read through \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-compact-namecert};
\item $H$ records componentwise $\hsame$ transports for $S,A,Q,U,D,K,R,P,N$;
\item $C$ stores the displayed $\Cont$ routes from source phases to mirror coordinates, Q3 readback, defect extraction, rank-drop test, and compact chart handoff;
\item $P$ is the $\Pkg$ provenance over the pentary mirror-decomposition vision, $\SOneUp$, $\CompactMetricUp$, $\CompactUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\PentaryMirrorDeficitUp}$ row.
\end{enumerate}
The carrier has no coordinate for a completed solution variety, a global nonpalindromy decision procedure, a full Hermitian-rigidity theorem, or a host algebraic-geometry solver.
\end{definition}

\begin{theorem}[Pentary mirror-deficit NameCert obligations]
\label{thm:pentary-mirror-deficit-namecert-obligations}
The local $\NameCert_{\PentaryMirrorDeficitUp}$ surface for the carrier of \autoref{def:pentary-mirror-deficit-carrier} consists exactly of five obligation rows:
\begin{enumerate}
\item carrier admission for $M=(S,A,Q,U,D,K,R,H,C,P,N)$;
\item mirror-coordinate exactness, forcing every read of the symmetric and antisymmetric coordinates through the displayed pentary phase row $S$;
\item defect and rank-gate exactness, forcing every Hermitian-defect or rank-drop read through $A,Q,U,D,K$ before any compact chart row is consumed;
\item sibling handoff to \autoref{ch:concrete-instances-s1-namecert}, \autoref{ch:concrete-instances-compactmetric-namecert}, and \autoref{ch:concrete-instances-compact-namecert};
\item ledger non-escape from full pentary rigidity, global nonpalindromy exclusion, completed algebraic varieties, quotient phase equality, and host solver semantics.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pentary-mirror-deficit-carrier}. The rows $S,A,Q,U,D,K,R$ are the only source, coordinate, Q3, unit-circle, defect, rank-gate, and compact-window rows available to a consumer.

Apply mirror-coordinate exactness. It makes the symmetric and antisymmetric coordinates a read of the same displayed five-phase source row, with no separate hidden pentary point.

Next apply defect and rank-gate exactness. Hermitian defects are read only after the mirror coordinates, Q3 symmetric row, unit-circle compatibility row, and coefficient-matrix row are displayed; the compact chart row $R$ is a bounded search surface, not a completed variety.

Finally use sibling handoff and ledger non-escape. The circle, compact metric, and compact rows remain separate sibling dependencies, while $H,C,P,N$ transport, replay, source, and name the same finite packet. No full pentary rigidity theorem, global nonpalindromy decision, quotient phase equality, or host solver endpoint is exported.
\end{proof}

\falsifiablePrediction{Every accepted $\PentaryMirrorDeficitUp$ read displays one pentary phase source row, one mirror-coordinate row, one Q3 symmetric-sum row, one unit-circle compatibility row, one Hermitian-defect row, one coefficient-matrix rank-gate row, one compact chart row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before any mirror-defect conclusion is public.}

\independenceWitness{pentary-hermitian-rigidity, pentary-hermitian-rigidity-gate, s1, compactmetric, compact: no listed sibling is bijective to $\PentaryMirrorDeficitUp$ because this packet jointly carries mirror coordinates, Q3 symmetric readback, unit-circle pair compatibility, Hermitian-defect rows, rank-gate data, compact chart windows, transport, replay, provenance, and local naming as one finite carrier.}

The Lean TasteGate route for this packet must provide explicit $\mathsf{Nontrivial}\ \PentaryMirrorDeficitUp$ and $\FieldFaithful\ \PentaryMirrorDeficitUp$ instances. Those instances must keep $S,A,Q,U,D,K,R,H,C,P,N$ as separate fields before the touchpoint theorem $\mathsf{BEDC.Derived.PentaryMirrorDeficitUp}$ can serve as a checked carrier-alignment target.

\closureat{PentaryMirrorDeficitUp}{seedStr}

\begin{closurestatus}{\PentaryMirrorDeficitUp}
  \constructivestory{A $\PentaryMirrorDeficitUp$ packet is generated from pentary phase source, mirror-coordinate, Q3 symmetric-sum, unit-circle compatibility, Hermitian-defect, coefficient-matrix rank-gate, compact chart, componentwise transport, replay, provenance, and local $\NameCert$ rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pentary-mirror-deficit-carrier} and \autoref{thm:pentary-mirror-deficit-namecert-obligations} bind the finite mirror-defect carrier, sibling handoff, falsifiability, and non-escape surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close full pentary Hermitian rigidity, global nonpalindromy exclusion, completed algebraic varieties, quotient phase equality, host solver semantics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, mirror-coordinate exactness, defect and rank-gate exactness, sibling handoff, and ledger non-escape for the displayed packet.}
\end{closurestatus}
