\chapter{A Concrete Naming Certificate for $\FiniteWindowRealSealAuditUp$}
\label{ch:concrete-instances-finite-window-real-seal-audit-namecert}
\origin{ai}

The $\FiniteWindowRealSealAuditUp$ packet is the finite audit surface for the StreamName--RegSeqRat--Real seal route of \autoref{ch:visions-real-completeness}. It reads the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. Its role is to bind the displayed finite window, dyadic tolerance, RegSeqRat readback, RealUp seal, audit-refusal, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows into one NameCert carrier, not to supply quotient streams, selected host limits, host real equality, countable choice, or ambient completeness.

\falsifiablePrediction{A RealUp seal consumer that bypasses the displayed finite-window, dyadic-tolerance, and RegSeqRat readback rows refutes the FiniteWindowRealSealAuditUp packet.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, real-completion-seal-classifier: no sibling is bijective because only this packet binds finite windows, dyadic tolerance, RegSeqRat readback, Real seal, and audit-refusal rows into one NameCert carrier.}

\begin{definition}[Finite-window Real-seal audit carrier]
\label{def:finite-window-real-seal-audit-carrier}
A $\FiniteWindowRealSealAuditUp$ carrier is a finite $\BHist$ packet
$$
A=(W,D,R,E,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the $\StreamNameUp$ finite-window row naming the visible request window;
\item $D$ is the $\DyadicRatCoreUp$ dyadic tolerance and readback row attached to that same window;
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row reached from $W$ and $D$;
\item $E$ is the terminal $\RealUp$ seal row read only after $W,D,R$ have been displayed;
\item $F$ is the audit-refusal row stating that no accepted consumer may replace $W,D,R,E$ by a hidden completion route, quotient stream, selected limit, host real equality, countable-choice row, or ambient-completeness row;
\item $H$ records componentwise $\hsame$ transports for the displayed rows;
\item $C$ records the displayed $\Cont$ replay from finite-window admission through dyadic tolerance, RegSeqRat readback, audit refusal, and the terminal Real seal;
\item $P$ is the $\Pkg$ provenance over the cited sibling packets, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\FiniteWindowRealSealAuditUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for quotient streams, selected host limits, host real equality, detached Real seals, countable choice, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.FiniteWindowRealSealAuditUp.FiniteWindowRealSealAuditTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Finite-window Real-seal audit NameCert obligations]
\label{thm:finite-window-real-seal-audit-namecert-obligations}
For every accepted $\FiniteWindowRealSealAuditUp$ carrier
$$
A=(W,D,R,E,F,H,C,P,N),
$$
the local $\NameCert_{\FiniteWindowRealSealAuditUp}$ surface consists exactly of finite-window exposure through $W$, dyadic-tolerance exposure through $D$, RegSeqRat readback through $R$, terminal Real-seal exposure through $E$, audit-refusal exposure through $F$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-seal-audit-carrier}. Its displayed inventory is the StreamName window, DyadicRatCore tolerance ledger, RegSeqRat readback, terminal Real seal, audit-refusal row, and the structural support rows.

Read the seal route in dependency order. The row $W$ fixes the finite request window, the row $D$ fixes the dyadic tolerance ledger attached to that window, the row $R$ fixes the regular-rational readback, and the row $E$ is the RealUp seal reached by that displayed route.

The audit row $F$ blocks the absent completion, quotient, selected-limit, host-equality, choice, and ambient-completeness coordinates. The rows $H,C,P,N$ only transport, replay, package, and name the same finite packet, so the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite-window Real-seal audit non-escape]
\label{thm:finite-window-real-seal-audit-nonescape}
Every accepted $\FiniteWindowRealSealAuditUp$ consumer factors through the finite route
$$
W,D,R,E,F,H,C,P,N.
$$
The consumer may inspect the StreamName finite window, DyadicRatCore tolerance ledger, RegSeqRat readback, RealUp seal, audit-refusal row, componentwise transports, continuations, provenance, and local NameCert row. It receives no quotient stream equality, selected host limit, host real equality, detached Real seal, countable-choice row, hidden completion route, or ambient-completeness coordinate.
\end{theorem}
\begin{proof}
Start from \autoref{thm:finite-window-real-seal-audit-namecert-obligations}. The local certificate exposes the complete route inventory for the accepted packet and gives no second source for window, tolerance, readback, seal, refusal, transport, continuation, provenance, or naming data.

Attach the sibling faces. The cited StreamName, DyadicRatCore, RegSeqRat, and Real chapters supply the four route faces $W,D,R,E$ in the order needed by a RealUp seal consumer.

Inspect the refusal row. Since $F$ records the excluded coordinates and the carrier has no other coordinate through which to read them, any consumer that bypasses $W,D,R$ before reaching $E$ has left the accepted audit packet.
\end{proof}

\begin{theorem}[Finite-window Real-seal audit four-face minimality]
\label{thm:finite-window-real-seal-audit-four-face-minimality}
For every accepted $\FiniteWindowRealSealAuditUp$ carrier
$$
A=(W,D,R,E,F,H,C,P,N),
$$
each of the four source faces $W,D,R,E$ is individually necessary for the accepted route. The row $W$ supplies the $\StreamNameUp$ finite-window face, $D$ supplies the $\DyadicRatCoreUp$ tolerance and readback face, $R$ supplies the $\RegSeqRatUp$ readback face, and $E$ supplies the terminal $\RealUp$ seal face.

Replacing or omitting any one of $W,D,R,E$ leaves no accepted $\FiniteWindowRealSealAuditUp$ packet with the same route role. The rows $F,H,C,P,N$ can refuse, transport, replay, package, and name only after the four source faces have been displayed; they cannot manufacture a missing StreamName window, dyadic tolerance ledger, RegSeqRat readback, or terminal Real seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-seal-audit-carrier}. The four rows $W,D,R,E$ are not auxiliary annotations: they are the source faces from which the displayed route obtains its finite window, dyadic tolerance ledger, regular-rational readback, and terminal Real seal.

Apply \autoref{thm:finite-window-real-seal-audit-namecert-obligations}. The exact row inventory places $W,D,R,E$ in the local certificate before the audit-refusal, transport, continuation, provenance, and naming rows. Thus each of the four faces has a distinct certificate role not repeated by $F,H,C,P,N$.

Apply \autoref{thm:finite-window-real-seal-audit-nonescape}. An accepted consumer has no bypass coordinate through which a missing window, tolerance ledger, readback, or seal can be recovered. Therefore dropping or replacing any one of $W,D,R,E$ breaks the accepted finite-window Real-seal audit route componentwise.
\end{proof}

\begin{theorem}[Finite-window Real-seal audit refusal transport]
\label{thm:finite-window-real-seal-audit-refusal-transport}
Let
$$
A=(W,D,R,E,F,H,C,P,N)
$$
and
$$
A'=(W',D',R',E',F',H',C',P',N')
$$
be accepted $\FiniteWindowRealSealAuditUp$ carriers. If the rows of $A$ are transported componentwise by $\hsame$ to the corresponding rows of $A'$, then the audit-refusal row is preserved. Every transported Real-seal consumer must pass through $W',D',R'$ before reading $E'$.

The transported packet has no quotient stream, selected host limit, host real equality, countable-choice row, hidden completion route, or ambient-completeness coordinate. These coordinates are still refused by $F'$ and cannot be recovered from $H',C',P',N'$.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:finite-window-real-seal-audit-namecert-obligations}. They identify the complete local inventory of an accepted carrier and make $F$ the row that records the audit refusal.

Apply the componentwise $\hsame$ transport recorded by $H$ and $H'$. Since the transported rows are exactly $W',D',R',E',F',H',C',P',N'$, the refusal row travels with the same finite packet rather than opening a detached completion coordinate.

Use \autoref{thm:finite-window-real-seal-audit-nonescape} for the target carrier $A'$. Any transported consumer that reaches the terminal Real seal must read the displayed route through $W',D',R'$ before $E'$.

Finally apply \autoref{thm:finite-window-real-seal-audit-four-face-minimality}. The four source faces cannot be replaced by $F',H',C',P',N'$, so the refused quotient, selected-limit, host-equality, choice, hidden-completion, and ambient-completeness routes remain absent after transport.
\end{proof}

\begin{theorem}[Finite-window Real-seal audit obligation closure]
\label{thm:finite-window-real-seal-audit-obligation-closure}
The carrier definition, the local NameCert obligations, the non-escape theorem, four-face minimality, the refusal-transport theorem, and the displayed TasteGate alignment target together bind the carrier, classifier, stability, exactness, and ledger-refusal obligations for $\FiniteWindowRealSealAuditUp$.

Concretely, \autoref{def:finite-window-real-seal-audit-carrier} supplies the carrier and classifier rows; \autoref{thm:finite-window-real-seal-audit-namecert-obligations} supplies the exact local NameCert inventory; \autoref{thm:finite-window-real-seal-audit-nonescape} supplies ledger refusal for non-displayed Real-seal routes; \autoref{thm:finite-window-real-seal-audit-four-face-minimality} supplies exactness of the four displayed faces; and \autoref{thm:finite-window-real-seal-audit-refusal-transport} supplies stability under componentwise $\hsame$ transport.
\end{theorem}
\begin{proof}
The carrier and classifier obligations are read from \autoref{def:finite-window-real-seal-audit-carrier}. The definition lists the displayed rows and says that accepted comparison has no coordinate outside those rows.

The exactness obligation is supplied by \autoref{thm:finite-window-real-seal-audit-namecert-obligations} and \autoref{thm:finite-window-real-seal-audit-four-face-minimality}. Together they identify the complete NameCert inventory and show that the four source faces $W,D,R,E$ are all necessary.

The ledger-refusal obligation is supplied by \autoref{thm:finite-window-real-seal-audit-nonescape}. It rules out quotient streams, selected host limits, host real equality, detached Real seals, countable-choice rows, hidden completion routes, and ambient-completeness coordinates for accepted consumers.

The stability obligation is supplied by \autoref{thm:finite-window-real-seal-audit-refusal-transport}. Componentwise $\hsame$ transport carries the refusal row and preserves the forced route through the displayed finite-window, dyadic-tolerance, RegSeqRat, and Real-seal faces. The TasteGate alignment target records that the same single carrier is the Lean-side audit surface for this packet, so the listed rows form the obligation-closure boundary for $\FiniteWindowRealSealAuditUp$.
\end{proof}

Lean development for this route provides \texttt{Nontrivial FiniteWindowRealSealAuditUp} and \texttt{FieldFaithful FiniteWindowRealSealAuditUp} in \texttt{lean4/BEDC/Derived/FiniteWindowRealSealAuditUp/TasteGate.lean}.

\begin{closurestatus}{\FiniteWindowRealSealAuditUp}
  \constructivestory{The finite-window Real-seal audit is generated from a $BHist$ packet whose window rows, tolerance rows, audit witness, and Real-seal handoff are all visible before any consumer route is admitted. The carrier convention first names the finite window, then records the audit row that checks the handoff, and finally allows $\Cont$ only to continue that audited route. If the representing history changes, $hsame$ transports the same audit packet rather than creating a new window. The resulting NameCert object is a local audit surface: it certifies that Real-seal use is confined to the displayed finite packet and that forbidden coordinates remain absent.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{This chapter binds the finite-window Real-seal audit carrier, NameCert obligations, non-escape theorem, four-face minimality, refusal transport of \autoref{thm:finite-window-real-seal-audit-refusal-transport}, and obligation closure of \autoref{thm:finite-window-real-seal-audit-obligation-closure}.}
  \formalstatus{\axiomCleanV}
  \leanchecked{BEDC.Derived.FiniteWindowRealSealAuditUp.taste\_gate}
  \leantarget{BEDC.Derived.FiniteWindowRealSealAuditUp.FiniteWindowRealSealAuditTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim all Real windows are audited, that finite windows are complete, or that unaudited host limits are admissible.}
  \upgradepath{Reaching scopedClosure requires explicit dependency scopes over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
\end{closurestatus}
