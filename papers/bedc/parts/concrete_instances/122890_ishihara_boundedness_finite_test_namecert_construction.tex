\chapter{A Concrete Naming Certificate for $\IshiharaBoundednessFiniteTestUp$}
\label{ch:concrete-instances-ishihara-boundedness-finite-test-namecert}
\origin{human}

Following Bishop and Bridges' constructive real analysis, the $\IshiharaBoundednessFiniteTestUp$ packet records the finite inspection route by which an Ishihara-style argument may expose boundedness information for a real-name sequence without importing an omniscience principle. It sits beside the inspection boundary of \autoref{ch:concrete-instances-ishihara-trick-namecert}, the finite-test boundary of \autoref{ch:concrete-instances-ishihara-finite-test-boundary-namecert}, the stream window surface $\StreamNameUp$ in \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback $\RegSeqRatUp$ in \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger $\DyadicRatCoreUp$ in \autoref{ch:concrete-instances-dyadicratcore-namecert}, the located real route $\LocatedRealUp$ in \autoref{ch:concrete-instances-locatedreal-namecert}, and the terminal real-name seal $\RealUp$ in \autoref{ch:concrete-instances-real-namecert}. The packet supplies a finite boundedness test for L10 Real and Completion consumers; it does not supply WLPO, LLPO, LPO, trichotomy, a selected supremum, quotient real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Ishihara boundedness finite-test carrier]
\label{def:ishihara-boundedness-finite-test-carrier}
An $\IshiharaBoundednessFiniteTestUp$ carrier is a finite $\BHist$ packet
$$
F=(S,W,L,U,K,R,Q,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source sequence row whose boundedness-facing read is being inspected.
\item $W$ is the $\StreamNameUp$ finite-window row opened for the current boundedness request.
\item $L$ is the lower finite-test row, naming the inspected evidence that the displayed window stays within the lower side of the bound.
\item $U$ is the upper finite-test row, naming the inspected evidence that the displayed window stays within the upper side of the bound.
\item $K$ is the $\LocatedRealUp$ split row through which the boundedness request is read as a located comparison rather than as a global order decision.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same window.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance budget used by the lower and upper tests.
\item $D$ is the finite boundedness ledger saying which inspected coordinates, tolerances, and comparison margins have been exposed.
\item $E$ is the terminal $\RealUp$ seal row, available only after $W,L,U,K,R,Q,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, window, tests, located split, readback, tolerance, boundedness ledger, and seal rows.
\item $C$ records displayed $\Cont$ replay of the same finite boundedness inspection route.
\item $P$ records $\Pkg$ provenance over $\IshiharaBoundednessFiniteTestUp$, $\IshiharaTrickUp$, $\IshiharaFiniteTestBoundaryUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\LocatedRealUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\IshiharaBoundednessFiniteTestUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,W,L,U,K,R,Q,D,E,H,C,P,N$. It has no coordinate for WLPO, LLPO, LPO, trichotomy of real names, an externally selected bound, quotient stream equality, countable choice, or ambient completeness of $\RealUp$.
\end{definition}
\leanstmt{BEDC.Derived.IshiharaBoundednessFiniteTestUp}

\begin{theorem}[Ishihara boundedness NameCert obligations]
\label{thm:ishihara-boundedness-namecert-obligations}
For every accepted $\IshiharaBoundednessFiniteTestUp$ carrier $F=(S,W,L,U,K,R,Q,D,E,H,C,P,N)$, the local $\NameCert_{\IshiharaBoundednessFiniteTestUp}$ surface consists exactly of source-sequence admission, finite-window admission, lower-test admission, upper-test admission, located comparison split, regular rational readback, dyadic tolerance budgeting, boundedness-ledger exposure, terminal Real-seal exposure, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:ishihara-boundedness-finite-test-carrier}. The visible boundedness data are $S,W,L,U,K,R,Q,D,E,H,C,P,N$.

The boundedness read begins with the finite window $W$ over the source row $S$. The lower and upper test rows $L,U$ are then interpreted through the located split $K$, the regular readback $R$, and the dyadic budget $Q$ before the finite boundedness ledger $D$ is exposed.

The terminal Real seal $E$ is downstream of those rows. Transport, replay, provenance, and local naming preserve the same finite route, so no WLPO, LLPO, LPO, trichotomy, selected bound, quotient equality, choice schedule, or ambient Real-completeness coordinate can enter the local NameCert surface.
\end{proof}

\begin{theorem}[Ishihara boundedness located case split]
\label{thm:ishihara-boundedness-located-case-split}
Every boundedness-facing read of an accepted $\IshiharaBoundednessFiniteTestUp$ packet passes through the displayed located split row $K$ before it may consume the lower or upper finite-test rows. The split exposes only the finite comparison route $W,K,R,Q,D$ and does not decide a global order on real names.
\end{theorem}
\begin{proof}
Project the rows $W,L,U,K,R,Q,D$ from the carrier. The two test rows are meaningful only against the displayed window $W$ and the located comparison row $K$.

The regular readback $R$ and dyadic budget $Q$ attach the split to a finite rational request. The ledger $D$ then records the boundedness-facing comparison margins that were actually inspected.

Since the carrier has no global order row, no trichotomy row, and no quotient-real equality row, a consumer can read the case split only as located finite evidence. It cannot turn the lower and upper test rows into an omniscience principle.
\end{proof}

\begin{theorem}[Ishihara boundedness L10 route]
\label{thm:ishihara-boundedness-l10-route}
Any L10 Real or Completion consumer using an accepted $\IshiharaBoundednessFiniteTestUp$ packet must read the $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ tolerance budget $Q$, $\LocatedRealUp$ split $K$, finite boundedness ledger $D$, terminal $\RealUp$ seal $E$, and structural rows $H,C,P,N$ before naming a boundedness conclusion.
\end{theorem}
\begin{proof}
Apply \autoref{thm:ishihara-boundedness-namecert-obligations}. It exposes the finite source, window, test, located split, readback, tolerance, ledger, Real seal, transport, replay, provenance, and local naming rows.

Apply \autoref{thm:ishihara-boundedness-located-case-split}. The boundedness conclusion reaches the two test rows only through the located comparison route $W,K,R,Q,D$.

The terminal $\RealUp$ seal $E$ is therefore a handoff boundary for L10 Real and Completion consumers, not an ambient completeness theorem. The structural rows $H,C,P,N$ replay and name the same route, excluding WLPO, LLPO, LPO, trichotomy, selected suprema, quotient stream equality, and countable choice from the exported boundedness surface.
\end{proof}

\closureat{IshiharaBoundednessFiniteTestUp}{seedStr}

\begin{closurestatus}{\IshiharaBoundednessFiniteTestUp}
  \constructivestory{An $\IshiharaBoundednessFiniteTestUp$ packet is generated from a source sequence, a finite StreamName window, lower and upper finite-test rows, a LocatedReal split, RegSeqRat readback, DyadicRatCore tolerance budget, boundedness ledger, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:ishihara-boundedness-finite-test-carrier}, \autoref{thm:ishihara-boundedness-namecert-obligations}, \autoref{thm:ishihara-boundedness-located-case-split}, and \autoref{thm:ishihara-boundedness-l10-route} seed the finite boundedness test route for L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close WLPO, LLPO, LPO, trichotomy, selected suprema, quotient stream equality, countable choice, ambient Real completeness, Lean-checked obligations, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, lower and upper test exactness, located split stability, StreamName and RegSeqRat readback compatibility, DyadicRatCore tolerance preservation, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.IshiharaBoundednessFiniteTestUp.}
\end{closurestatus}
