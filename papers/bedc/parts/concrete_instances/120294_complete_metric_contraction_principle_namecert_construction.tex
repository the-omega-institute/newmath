\chapter{A Concrete Naming Certificate for $\CompleteMetricContractionPrincipleUp$}
\label{ch:concrete-instances-complete-metric-contraction-principle-namecert}
\origin{human}

Following Banach and Bishop--Bridges constructive analysis, the $\CompleteMetricContractionPrincipleUp$ packet records the fixed-point route in a complete metric space as finite NameCert data. It sits after the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy criterion surface of \autoref{ch:concrete-instances-cauchycriterion-namecert}, the Picard contraction route of \autoref{ch:concrete-instances-picardcontraction-namecert}, and the fixed-point certificate of \autoref{ch:concrete-instances-banach-fixed-point-namecert}. The packet names the contraction principle only when the metric source, contraction modulus, Picard orbit, Cauchy handoff, fixed-point seal, transport, replay, provenance, and local NameCert rows are all displayed; it is not a host selected-limit theorem.

\begin{definition}[Complete metric contraction principle carrier]
\label{def:complete-metric-contraction-principle-carrier}
A $\CompleteMetricContractionPrincipleUp$ carrier is a finite $\BHist$ packet
$$
K=(M,d,\lambda,O,Q,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\CompleteMetricUp$ source row naming the complete metric service consumed by the route.
\item $d$ is the $\MetricUp$ distance row used for finite iterate comparisons and contraction estimates.
\item $\lambda$ is the contraction-modulus row, including the displayed positivity and unit-bound data.
\item $O$ is the $\PicardContractionUp$ orbit row, exposing the initial point, successor applications, and finite Picard ledger.
\item $Q$ is the $\CauchyCriterionUp$ route that turns the contraction estimates along $O$ into named Cauchy data.
\item $F$ is the $\BanachFixedPointUp$ fixed-point seal, available only after the Picard and Cauchy rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for the complete metric source, distance, modulus, Picard orbit, Cauchy route, fixed-point seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from metric reads through contraction estimates, Picard iteration, Cauchy criterion, and fixed-point sealing.
\item $P$ is the $\Pkg$ provenance over $\CompleteMetricContractionPrincipleUp$, $\CompleteMetricUp$, $\MetricUp$, $\CauchyCriterionUp$, $\PicardContractionUp$, $\BanachFixedPointUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompleteMetricContractionPrincipleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected limit, quotient completion, countable choice, ambient real completeness, host fixed-point equality, Lean verification, or bridge checking.
\end{definition}
\leanchecked{BEDC.Derived.CompleteMetricContractionPrincipleUp.CompleteMetricContractionPrincipleTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Complete metric contraction principle NameCert obligations]
\label{thm:complete-metric-contraction-principle-namecert-obligations}
For every accepted $\CompleteMetricContractionPrincipleUp$ carrier
$$
K=(M,d,\lambda,O,Q,F,H,C,P,N),
$$
the local $\NameCert_{\CompleteMetricContractionPrincipleUp}$ surface consists exactly of complete-metric source exposure, metric-distance exposure, contraction-modulus visibility, Picard-orbit visibility, CauchyCriterion route exposure, fixed-point seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:complete-metric-contraction-principle-carrier}. The source side is $M,d,\lambda,O$: the complete metric row supplies the service boundary, the metric row supplies distance comparisons, the modulus row supplies the strict contraction ledger, and the Picard row supplies the finite orbit.

The Cauchy row $Q$ is downstream of those displayed data. It records the CauchyCriterion handoff for the Picard orbit, so the fixed-point seal $F$ cannot be read before the orbit and Cauchy route are visible.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier contains no selected limit, quotient completion, countable choice, ambient real completeness, host fixed-point equality, Lean verification, or bridge checking, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Complete metric contraction Picard-Cauchy route]
\label{thm:complete-metric-contraction-principle-picard-cauchy-route}
Every fixed-point read exported by an accepted $\CompleteMetricContractionPrincipleUp$ carrier factors through the finite route
$$
\CompleteMetricUp,\ \MetricUp,\ \PicardContractionUp,\ \CauchyCriterionUp,\ \BanachFixedPointUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert_{\CompleteMetricContractionPrincipleUp}.
$$
The route first exposes the complete metric source and distance row, then the contraction modulus and Picard orbit, then the CauchyCriterion handoff, and only then the BanachFixedPoint seal. It does not supply a host selected limit, a quotient completion, countable choice, ambient $\RealUp$ completeness, or fixed-point equality outside the displayed packet.
\end{theorem}
\begin{proof}
Begin with the accepted carrier. Its source rows $M,d,\lambda,O$ are the only complete-metric, distance, modulus, and Picard coordinates available to a consumer.

Use the obligation theorem \autoref{thm:complete-metric-contraction-principle-namecert-obligations}. It requires the CauchyCriterion row $Q$ to be read from those displayed source and orbit rows before a fixed-point seal is available.

Finally read $F,H,C,P,N$. The fixed-point seal is transported by $H$, replayed by $C$, sourced by $P$, and named by $N$ along the same finite route. Because no excluded coordinate occurs in the carrier, the exported principle is a NameCert handoff rather than a host selected-limit argument.
\end{proof}

\falsifiablePrediction{Every accepted $\CompleteMetricContractionPrincipleUp$ read exposes CompleteMetricUp, MetricUp, contraction modulus, PicardContractionUp, CauchyCriterionUp, BanachFixedPointUp, hsame transport, Cont replay, Pkg provenance, and local NameCert rows before any fixed-point consumer can use the seal.}

\independenceWitness{complete metric, metric, cauchy criterion, picard contraction, banach fixed point: no listed sibling carries the whole packet $K=(M,d,\lambda,O,Q,F,H,C,P,N)$ because each sibling supplies only a source, distance, orbit, Cauchy route, or fixed-point seal while this chapter binds all rows as one complete-metric contraction-principle certificate.}

\closureat{CompleteMetricContractionPrincipleUp}{seedStr}

\begin{closurestatus}{\CompleteMetricContractionPrincipleUp}
  \constructivestory{A $\CompleteMetricContractionPrincipleUp$ packet is generated from a CompleteMetric source row, a Metric distance row, a contraction-modulus row, a PicardContraction orbit row, a CauchyCriterion route, a BanachFixedPoint seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:complete-metric-contraction-principle-carrier}, \autoref{thm:complete-metric-contraction-principle-namecert-obligations}, and \autoref{thm:complete-metric-contraction-principle-picard-cauchy-route} seed the finite contraction-principle route over complete metric data.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a host selected-limit theorem, quotient completion, countable choice, ambient $\RealUp$ completeness, host fixed-point equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure asks for checked complete-metric source exposure, metric-distance exposure, contraction-modulus visibility, Picard-orbit visibility, CauchyCriterion handoff, fixed-point seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompleteMetricContractionPrincipleUp.}
\end{closurestatus}
