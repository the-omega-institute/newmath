\chapter{A Concrete Naming Certificate for $\RealUniformEntourageBasisUp$}
\label{ch:concrete-instances-real-uniform-entourage-basis-namecert}
\origin{human}

Following Bourbaki and Kelley on uniform spaces and Bishop--Bridges constructive real analysis, the $\RealUniformEntourageBasisUp$ packet records the metric-ball basis that realizes the Real-induced uniformity route of \autoref{ch:visions-compact-uniform-continuity}. It sits after the Real endpoint and seal surface of \autoref{ch:concrete-instances-real-namecert}, the metric witness of \autoref{ch:concrete-instances-metric-namecert}, the entourage discipline of \autoref{ch:concrete-instances-uniformspace-namecert}, the Real-induced uniformity of \autoref{ch:concrete-instances-real-uniform-structure-namecert}, and the Cauchy-filter consumer of \autoref{ch:concrete-instances-cauchyfilter-namecert}. Its finite readback path uses the StreamName surface of \autoref{ch:concrete-instances-streamname-namecert} and the RegSeqRat readback of \autoref{ch:concrete-instances-regseqrat-namecert}.

\begin{definition}[Real uniform entourage-basis carrier]
\label{def:real-uniform-entourage-basis-carrier}
A $\RealUniformEntourageBasisUp$ carrier is a finite $\BHist$ packet
$$
E=(R,M,U,S,F,W,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ endpoint and seal row naming the points whose metric balls generate the basis.
\item $M$ is the $\MetricUp$ distance witness row read before any entourage request is generated.
\item $U$ is the $\UniformSpaceUp$ entourage row generated by positive metric-ball requests.
\item $S$ is the $\RealUniformStructureUp$ source row that binds the metric-ball basis to the Real-induced uniformity.
\item $F$ is the $\CauchyFilterUp$ consumer row that reads Cauchy data through the generated basis.
\item $W$ is the $\StreamNameUp$ finite-window row used by completion-facing consumers.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback row attached to those windows.
\item $H$ records componentwise $\hsame$ transport for the Real, metric, uniform, Real-uniform, Cauchy-filter, window, readback, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
R,M \longmapsto U,S \longmapsto F,W,Q .
$$
\item $P$ is the $\Pkg$ provenance over $\RealUniformEntourageBasisUp$, $\RealUniformStructureUp$, $\RealUp$, $\MetricUp$, $\UniformSpaceUp$, $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RealUniformEntourageBasisUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,M,U,S,F,W,Q,H,C,P,N$. It has no coordinate for quotient real equality, selected limits, countable choice, open-cover compactness, quotient filters, or ambient Real completeness.
\end{definition}
\leanchecked{BEDC.Derived.RealUniformEntourageBasisUp.RealUniformEntourageBasisUpTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Real uniform entourage-basis NameCert obligations]
\label{thm:real-uniform-entourage-basis-namecert-obligations}
For every accepted $\RealUniformEntourageBasisUp$ carrier
$$
E=(R,M,U,S,F,W,Q,H,C,P,N),
$$
the local $\NameCert_{\RealUniformEntourageBasisUp}$ surface consists exactly of the following finite rows:
\begin{enumerate}
\item carrier admission for the eleven displayed rows;
\item Real endpoint exposure, requiring every basis request to read $R$ before it reads a metric ball;
\item metric-ball generation, requiring $M$ to generate each entourage row in $U$ by a displayed positive-radius request;
\item Real-uniform source compatibility, requiring $S$ to identify the basis as the $\RealUniformStructureUp$ metric-ball surface;
\item Cauchy-filter handoff, requiring $F$ to consume the generated basis before any completion-facing read;
\item finite readback, requiring $W$ and $Q$ to expose only StreamName windows and RegSeqRat readbacks downstream of the same basis route;
\item componentwise transport, displayed replay, provenance, and local naming for the route through $H,C,P,N$.
\end{enumerate}
No Real uniform entourage-basis obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted packet $E=(R,M,U,S,F,W,Q,H,C,P,N)$. The projection exposes exactly the Real endpoint row, metric witness row, generated uniform entourage row, Real-uniform source row, Cauchy-filter consumer, finite readback rows, and structural rows.

The basis starts from Real endpoints and metric data. The row $R$ names the endpoints, and $M$ supplies the distance witness from which the metric-ball entourage row $U$ is generated.

The row $S$ binds this generated basis to the $\RealUniformStructureUp$ route. A Cauchy-filter consumer can then read $F$ only after the same basis has been displayed.

The rows $W$ and $Q$ give finite StreamName and RegSeqRat readback downstream of the basis. The structural rows $H,C,P,N$ transport, replay, source, and name the same route, and the excluded objects do not occur as carrier coordinates.
\end{proof}

\begin{theorem}[Real uniform entourage-basis Cauchy-filter handoff]
\label{thm:real-uniform-entourage-basis-cauchy-filter-handoff}
Every completion-facing or Cauchy-filter consumer of an accepted $\RealUniformEntourageBasisUp$ carrier reads the Real-induced metric-ball basis only through
$$
\RealUp,\MetricUp,\UniformSpaceUp,\RealUniformStructureUp,\CauchyFilterUp,\StreamNameUp,\RegSeqRatUp,\hsame,\Cont,\Pkg,\NameCert .
$$
Consequently the packet exports no quotient real equality, selected limit, countable-choice selector, open-cover compactness, quotient filter, or ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with a completion-facing or Cauchy-filter read. Carrier admission gives access only to $R,M,U,S,F,W,Q,H,C,P,N$.

Use the metric-uniformity handoff of \autoref{thm:real-uniform-structure-metric-uniformity-handoff}. It forces the Real-induced uniformity to pass through endpoint rows, metric distance evidence, generated entourage rows, Cauchy-filter compatibility, finite windows, and regular rational readback.

The present carrier isolates the basis part of that route. The rows $R,M,U,S$ generate and identify the metric-ball basis, $F$ consumes it as a Cauchy-filter surface, and $W,Q$ provide the finite readback needed by completion-facing consumers.

Since quotient equality, selected limits, choice, quotient filters, open-cover compactness, and ambient Real completeness are not rows of $E$, the consumer obtains only the displayed NameCert route.
\end{proof}

\falsifiablePrediction{An accepted $\RealUniformEntourageBasisUp$ read is refuted if a Cauchy-filter or completion-facing consumer exports a Real-induced basis without displaying Real endpoint rows, Metric distance witnesses, UniformSpace entourage rows, RealUniformStructure source compatibility, StreamName windows, RegSeqRat readback, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{real-uniform-structure, real, metric, uniformspace, cauchyfilter, streamname, regseqrat: no listed sibling alone is bijective with $\RealUniformEntourageBasisUp$ because the packet jointly carries metric-ball basis generation, Real-uniform source compatibility, Cauchy-filter handoff, finite readback, transport, replay, provenance, and local NameCert.}

\closureat{RealUniformEntourageBasisUp}{seedStr}

\begin{closurestatus}{\RealUniformEntourageBasisUp}
  \constructivestory{A $\RealUniformEntourageBasisUp$ packet is generated from RealUp endpoint rows, MetricUp distance witnesses, UniformSpace entourage rows, RealUniformStructureUp source compatibility, CauchyFilterUp handoff, StreamNameUp windows, RegSeqRatUp readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-uniform-entourage-basis-carrier}, \autoref{thm:real-uniform-entourage-basis-namecert-obligations}, and \autoref{thm:real-uniform-entourage-basis-cauchy-filter-handoff} seed the Real-induced metric-ball entourage basis for Cauchy-filter and completion-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient real equality, selected limits, countable-choice selectors, open-cover compactness, quotient filters, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked Real endpoint exposure, metric-ball generation, RealUniformStructure compatibility, CauchyFilter handoff, finite readback, transport, replay, provenance, and local naming for BEDC.Derived.RealUniformEntourageBasisUp.}
\end{closurestatus}
