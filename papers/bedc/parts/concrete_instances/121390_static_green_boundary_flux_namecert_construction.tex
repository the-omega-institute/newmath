\chapter{A Concrete Naming Certificate for $\StaticGreenBoundaryFluxUp$}
\label{ch:concrete-instances-static-green-boundary-flux-namecert}
\origin{ai}

The $\StaticGreenBoundaryFluxUp$ packet realizes the static boundary-flux branch of \autoref{prin:zeckendorf-static-green-exact-right-boundary-flux} as finite graph-Laplacian NameCert data. It sits beside the finite-volume boundary ledger of \autoref{ch:concrete-instances-finite-volume-namecert}, the Gauss flux-boundary route of \autoref{ch:concrete-instances-gauss-divergence-theorem-namecert}, the displayed matrix surface of \autoref{ch:concrete-instances-matrix-namecert}, and the rational finite-place product carrier of \autoref{ch:concrete-instances-rational-finite-place-product-namecert}. The packet names static reduced-Laplacian exactness only; it does not identify the dynamic Green response, a running susceptibility, a host PDE solution, or a selected boundary trace.

\begin{definition}[Static Green boundary-flux carrier]
\label{def:static-green-boundary-flux-carrier}
A $\StaticGreenBoundaryFluxUp$ carrier is a finite $\BHist$ packet
$$
S=(G,I,B,L,A,E,Z,T,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the finite weighted graph row, listing retained vertices, boundary vertices, edge weights, and incidence windows.
\item $I$ is the retained-block row for the internal vertex set on which the reduced Dirichlet problem is read.
\item $B$ is the boundary-cut row, naming the deleted boundary block and the cut weights from retained vertices into that block.
\item $L$ is the reduced Laplacian row over $I$, with diagonal entries read from full weighted degree and off-diagonal entries read from retained-retained edge weights.
\item $A$ is the cut-source row, recording the vector whose retained coordinate is the total edge weight from that vertex into $B$.
\item $E$ is the exactness row, displaying the identity $L\mathbf{1}_I=A$ before any Green-class read is consumed.
\item $Z$ is the localized-denominator row. It records the finite determinant and the designated unit factors without turning the cokernel read into a dynamic Green response.
\item $T$ records componentwise $\hsame$ transport for graph, retained-block, boundary-cut, reduced-Laplacian, source, exactness, denominator, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay of the static cut-source route.
\item $P$ is the $\Pkg$ provenance over $\StaticGreenBoundaryFluxUp$, $\FiniteVolumeUp$, $\GaussDivergenceTheoremUp$, $\MatrixUp$, $\RationalFinitePlaceProductUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\StaticGreenBoundaryFluxUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for a dynamic Green operator, a Green--Kubo susceptibility, a host finite-volume theorem, quotient equality of sources, selected traces, or a physical running parameter.
\end{definition}

\begin{theorem}[Static Green boundary-flux NameCert obligations]
\label{thm:static-green-boundary-flux-namecert-obligations}
For every accepted $\StaticGreenBoundaryFluxUp$ carrier $S=(G,I,B,L,A,E,Z,T,R,P,N)$, the local $\NameCert_{\StaticGreenBoundaryFluxUp}$ surface consists exactly of finite graph exposure through $G$, retained-block exposure through $I$, boundary-cut exposure through $B$, reduced-Laplacian exposure through $L$, cut-source exposure through $A$, exactness exposure through $E$, localized-denominator exposure through $Z$, componentwise transport through $T$, displayed replay through $R$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:static-green-boundary-flux-carrier}. The finite graph and cut data are exactly $G,I,B$, while the algebraic readback rows are $L,A,E,Z$.

The reduced Laplacian and cut-source are not external objects. The row $L$ is computed from the retained graph incidence in $G,I$, and the row $A$ is computed from the boundary-cut row $B$.

The exactness claim is carried only by $E$. It displays $L\mathbf{1}_I=A$ before the localized-denominator row $Z$ is inspected, so the packet cannot claim Green-exactness while hiding the reduced Laplacian or the cut source.

Finally, $T,R,P,N$ transport, replay, source, and name the same finite route. Since no dynamic Green operator, susceptibility, host finite-volume theorem, quotient source equality, selected trace, or physical running parameter occurs in the carrier, the listed rows exhaust the local obligations.
\end{proof}

\begin{theorem}[Static Green boundary-flux exactness]
\label{thm:static-green-boundary-flux-exactness}
Let $S=(G,I,B,L,A,E,Z,T,R,P,N)$ be an accepted $\StaticGreenBoundaryFluxUp$ carrier. Any static boundary-flux read that claims Green-exactness must expose the reduced Laplacian row $L$ and the cut-source row $A$ before reading the exactness row $E$. The visible exactness is the finite identity $L\mathbf{1}_I=A$, and it exports no dynamic Green response, no Green--Kubo susceptibility, no selected boundary trace, and no host finite-volume theorem.
\end{theorem}
\begin{proof}
Read the graph, retained-block, and boundary-cut rows first. They determine which vertices remain internal and which edge weights contribute to the boundary source.

Project the reduced Laplacian row $L$. Its diagonal entries use the full weighted degree of each retained vertex, while its off-diagonal entries use only retained-retained edge weights.

Project the cut-source row $A$. Each coordinate records exactly the omitted boundary weights from the same retained vertex into $B$.

The row $E$ then displays the coordinatewise identity $L\mathbf{1}_I=A$: multiplying $L$ by the constant retained vector cancels retained-retained edge contributions and leaves precisely the boundary-cut coordinate. The transport, replay, provenance, and local naming rows preserve that static identity, and no row supplies the excluded dynamic or host-theorem data.
\end{proof}

\begin{theorem}[Static Green boundary-flux vision realization]
\label{thm:static-green-boundary-flux-vision-realization}
The $\StaticGreenBoundaryFluxUp$ carrier realizes the static part of \autoref{prin:zeckendorf-static-green-exact-right-boundary-flux}: a Window-six or finite graph boundary read is accepted only when the rows $G,I,B,L,A,E,Z,T,R,P,N$ expose the reduced Laplacian, cut source, exactness identity, localized denominator, transport, replay, provenance, and local naming. The realization is static and exact; the dynamic Green response of \autoref{thm:zeckendorf-static-right-boundary-source-green-zero} remains a separate readout surface.
\end{theorem}
\begin{proof}
Start with \autoref{def:static-green-boundary-flux-carrier}. Its rows bind the finite graph, retained block, deleted boundary block, reduced Laplacian, and cut-source vector before any exactness read is public.

Apply \autoref{thm:static-green-boundary-flux-exactness}. The exactness row is the displayed identity $L\mathbf{1}_I=A$, so the static boundary source is a reduced-Laplacian image inside the same finite packet.

The localized-denominator row $Z$ records which determinant factors remain visible after the designated finite-place localization. It does not create a running Green operator and does not turn the zero static cokernel class into a dynamic susceptibility.

Thus the vision is realized only as static finite NameCert data. The dynamic centered relaxation and Green-excess quantities cited by the vision require their own packet and cannot be recovered from $S$.
\end{proof}

\falsifiablePrediction{Any accepted $\StaticGreenBoundaryFluxUp$ read that claims static Green-exactness must expose the reduced Laplacian row and the cut-source row before the exactness row is consumed; a read that hides either row refutes the carrier of \autoref{def:static-green-boundary-flux-carrier}.}

\independenceWitness{finite-volume, gauss-divergence-theorem, matrix, rational-finite-place-product: $\StaticGreenBoundaryFluxUp$ is not bijective to any listed sibling because it jointly carries finite graph incidence, retained-block data, boundary-cut rows, reduced-Laplacian rows, cut-source rows, exactness, localized-denominator data, transport, replay, provenance, and local naming, while the siblings isolate conservation ledgers, flux-boundary calculus, matrix display, or finite-place arithmetic.}

The Lean-side taste surface for $\StaticGreenBoundaryFluxUp$ must eventually provide `Nontrivial StaticGreenBoundaryFluxUp` and `FieldFaithful StaticGreenBoundaryFluxUp` instances in `TasteGate.lean`, so that nontrivial boundary-flux content and finite-field faithful readback are checked before stronger closure is claimed.

\closureat{StaticGreenBoundaryFluxUp}{seedStr}

\begin{closurestatus}{\StaticGreenBoundaryFluxUp}
  \constructivestory{A $\StaticGreenBoundaryFluxUp$ packet is generated from a finite weighted graph row, retained-block row, boundary-cut row, reduced-Laplacian row, cut-source row, exactness row, localized-denominator row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:static-green-boundary-flux-carrier}, \autoref{thm:static-green-boundary-flux-namecert-obligations}, \autoref{thm:static-green-boundary-flux-exactness}, and \autoref{thm:static-green-boundary-flux-vision-realization} seed the finite static boundary-flux route over reduced Laplacians, cut sources, exactness, localized denominators, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close dynamic Green response, Green--Kubo susceptibility, host finite-volume theorems, selected traces, physical running parameters, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, reduced-Laplacian construction, cut-source construction, exactness, localized-denominator faithful readback, transport and replay stability, provenance, local naming, Nontrivial instance, and FieldFaithful instance for BEDC.Derived.StaticGreenBoundaryFluxUp.}
\end{closurestatus}
