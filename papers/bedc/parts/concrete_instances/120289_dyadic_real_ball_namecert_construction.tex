\chapter{A Concrete Naming Certificate for $\DyadicRealBallUp$}
\label{ch:concrete-instances-dyadic-real-ball-namecert}
\origin{human}

Following Bishop and Bridges, the $\DyadicRealBallUp$ packet records a finite dyadic centre-radius neighbourhood for regular real-name work. It reads dyadic arithmetic from \autoref{ch:concrete-instances-dyadicratcore-namecert}, finite observation windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal from \autoref{ch:concrete-instances-real-namecert}. The packet names a concrete ball row available to later metric, uniform, and completion arguments; it does not identify host real equality, quotient representatives, selected limits, countable choice, or ambient completeness.

\begin{definition}[Dyadic real ball carrier]
\label{def:dyadic-real-ball-carrier}
A $\DyadicRealBallUp$ carrier is a finite $\BHist$ packet
$$
B=(D,S,R,E,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ row carrying the dyadic centre, positive dyadic radius, denominator budget, and slack comparisons used by the ball.
\item $S$ is the $\StreamNameUp$ finite-window row through which the centre name is inspected.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row attached to those windows.
\item $E$ is the terminal $\RealUp$ seal row reached only after $D,S,R$ have been displayed.
\item $M$ is the membership-witness row: an admitted point can be read as lying in the ball only through the displayed centre, radius, window, readback, and seal rows.
\item $H$ records componentwise $\hsame$ transport for the dyadic data, finite windows, regular readback, Real seal, membership witness, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from a finite precision request through $D,S,R,E,M,H$.
\item $P$ is the $\Pkg$ provenance over $\DyadicRealBallUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicRealBallUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,S,R,E,M,H,C,P,N$. It has no coordinate for host real equality, quotient equality of real names, selected representatives, countable choice, hidden metric completion, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dyadic real ball NameCert obligations]
\label{thm:dyadic-real-ball-namecert-obligations}
For every accepted $\DyadicRealBallUp$ carrier
$$
B=(D,S,R,E,M,H,C,P,N),
$$
the local $\NameCert_{\DyadicRealBallUp}$ surface consists exactly of carrier admission for the dyadic centre-radius row, finite StreamName windows, RegSeqRat readback, terminal Real seal, membership-witness row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every ball-membership consumer must pass through
$$
D,S,R,E,M,H,C,P,N
$$
and through no host real equality, quotient representative, selected limit, countable-choice selector, hidden completion object, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-real-ball-carrier}. The row $D$ displays the dyadic centre, radius, denominator budget, and slack comparisons, while $S$ and $R$ display the finite observation and regular rational readback route for the centre name.

Read the seal and membership rows next. The terminal row $E$ is available only after $D,S,R$ have been displayed, and the row $M$ records membership only as a witness over the same centre-radius, window, readback, and seal route.

Finally inspect $H,C,P,N$. Transport, replay, provenance, and local naming preserve the same finite route and add no hidden equality or selector coordinate. Since the carrier contains no host real equality, quotient representative, selected limit, countable-choice selector, hidden completion object, or ambient completeness field, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\closureat{DyadicRealBallUp}{seedStr}

\begin{closurestatus}{\DyadicRealBallUp}
  \constructivestory{A $\DyadicRealBallUp$ packet is generated from a DyadicRatCore centre-radius row, StreamName finite windows, RegSeqRat readback, a RealUp seal, a membership-witness row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-real-ball-carrier} and \autoref{thm:dyadic-real-ball-namecert-obligations} seed the finite dyadic centre-radius ball route over dyadic arithmetic, finite windows, regular rational readback, terminal Real sealing, membership witnessing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close metric topology, uniform-space structure, completion theorems, host real equality, quotient representatives, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, centre-radius visibility, finite window readback, Real seal ordering, membership-witness exactness, transport, replay, provenance, and local naming for BEDC.Derived.DyadicRealBallUp.}
\end{closurestatus}
