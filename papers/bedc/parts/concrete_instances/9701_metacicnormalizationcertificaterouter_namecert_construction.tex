\chapter{A Concrete Naming Certificate for $\MetaCICNormalizationCertificateRouterUp$}
\label{ch:concrete-instances-metacicnormalizationcertificaterouter-namecert}
\origin{ai}

The $\MetaCICNormalizationCertificateRouterUp$ packet realizes the candidate-mediated route inventory of \autoref{ch:metacic-normalization-audit-map} as a finite NameCert router. It sits beside $\MetaCICNormalizationFrontierUp$, $\ClosedTermSubstitutionBoundaryUp$, $\AuthorizedGeneratorRecursorUp$, $\GroundCompilerRecognizerFlowUp$, and $\KernelInductiveAcceptanceUp$: those siblings expose frontier, closed-substitution, generator, compiler-flow, and inductive-acceptance rows, while this packet chooses which displayed normalization evidence is present before any closed-term normalization read is admitted.

The exact concrete carrier rows consumed by the router are supplied by \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}, \autoref{ch:concrete-instances-authorizedgeneratorrecursor-namecert}, \autoref{ch:concrete-instances-groundcompilerrecognizerflow-namecert}, and \autoref{ch:concrete-instances-kernelinductiveacceptance-namecert}.

Thus the router bundles the normalization-frontier, closed-substitution, authorized-generator, compiler-recognizer, and kernel-inductive-acceptance rows into one routing surface rather than claiming a new atomic carrier.

\begin{definition}[MetaCIC normalization certificate router carrier]
\label{def:metacic-normalization-certificate-router-carrier}
A $\MetaCICNormalizationCertificateRouterUp$ carrier is a finite $\BHist$ packet
$$
R=(A,K,S,P,D,U,G,L,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the audit-map anchor row for \autoref{ch:metacic-normalization-audit-map};
\item $K$ is the candidate-evidence row, including candidate, closed-candidate, and candidate-set evidence;
\item $S$ is the strong-normalization consequence row read only from $K$;
\item $P$ is the Pi and application adequacy row that keeps function, argument, and application candidates on the displayed route;
\item $D$ is the closed-term projection row consumed through $\ClosedTermSubstitutionBoundaryUp$;
\item $U$ is the authorized generator handoff row consumed through $\AuthorizedGeneratorRecursorUp$;
\item $G$ is the compiler recognizer row consumed through $\GroundCompilerRecognizerFlowUp$;
\item $L$ is the kernel inductive-acceptance row consumed through $\KernelInductiveAcceptanceUp$;
\item $H$ records componentwise $\hsame$ transport for the evidence, projection, adequacy, generator, compiler, acceptance, route, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from candidate evidence to strong normalization and from strong normalization to no-infinite-reduction projections;
\item $Q$ records $\Pkg$ provenance over $\MetaCICNormalizationFrontierUp$, $\ClosedTermSubstitutionBoundaryUp$, $\AuthorizedGeneratorRecursorUp$, $\GroundCompilerRecognizerFlowUp$, $\KernelInductiveAcceptanceUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\MetaCICNormalizationCertificateRouterUp}$ row.
\end{enumerate}
The carrier has no coordinate for evidence-free closed-term strong normalization, unconditional application normalization, host normalization, quotient reduction equality, or a normalization oracle outside the displayed router.
\end{definition}

\begin{theorem}[MetaCIC normalization certificate router obligation surface]
\label{thm:metacic-normalization-certificate-router-obligation-surface}
For the carrier of \autoref{def:metacic-normalization-certificate-router-carrier}, the local $\NameCert_{\MetaCICNormalizationCertificateRouterUp}$ surface consists exactly of candidate-evidence admission through $K$, strong-normalization readback through $S$, Pi and application adequacy through $P$, closed-term projection through $D$, authorized generator handoff through $U$, compiler recognizer handoff through $G$, kernel inductive-acceptance handoff through $L$, route replay through $C$, componentwise transport through $H$, provenance through $Q$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterUp\_single\_carrier\_alignment}
\begin{proof}
Project the carrier coordinates. The router admits normalization reads only after the audit anchor $A$ and candidate evidence $K$ have been displayed, and the row $S$ is downstream of that evidence rather than a separate global theorem.

The adequacy and closed-term rows are then finite consumers. The row $P$ records Pi and application adequacy, while $D$ records the closed-term projection through the sibling $\ClosedTermSubstitutionBoundaryUp$ surface.

Finally attach the producer and acceptance rows. The rows $U,G,L$ keep authorized generation, compiler recognition, and kernel inductive acceptance on the same packet, and $H,C,Q,N$ provide only transport, replay, provenance, and local naming. Since no other row is present, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router closed-read refusal]
\label{thm:metacic-normalization-certificate-router-closed-read-refusal}
A closed-term normalization read of an accepted $\MetaCICNormalizationCertificateRouterUp$ packet is refused unless the displayed route contains candidate evidence, closed-candidate evidence, or candidate-set evidence in $K$. The rows $P,D,U,G,L,H,C,Q,N$ may route, project, transport, replay, and package such evidence, but none of them can replace $K$ by an evidence-free normalization theorem.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterClosedReadRefusal}
\begin{proof}
Read the evidence side first. By \autoref{thm:metacic-normalization-certificate-router-obligation-surface}, the strong-normalization row $S$ is reachable only from the candidate-evidence row $K$.

Now inspect the sibling rows. $\MetaCICNormalizationFrontierUp$ supplies the candidate-frontier boundary, $\ClosedTermSubstitutionBoundaryUp$ supplies the closed projection row, $\AuthorizedGeneratorRecursorUp$ supplies the authorized generator handoff, $\GroundCompilerRecognizerFlowUp$ supplies the compiler recognizer flow, and $\KernelInductiveAcceptanceUp$ supplies the inductive acceptance row. Each is consumed as a displayed row of the router rather than as a source of evidence-free normalization.

The structural rows cannot change that dependency. Transport, replay, provenance, and local naming preserve the already displayed route, so a closed-term normalization request with no candidate, closed-candidate, or candidate-set evidence has no carrier coordinate from which to read $S$.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router candidate determinacy]
\label{thm:metacic-normalization-certificate-router-candidate-determinacy}
For an accepted $\MetaCICNormalizationCertificateRouterUp$ carrier, the strong-normalization readback row $S$ is determined by the displayed candidate, closed-candidate, or candidate-set evidence in $K$. The closed projection, Pi and application adequacy, generator, compiler, acceptance, transport, replay, provenance, and local naming rows may preserve and route that evidence, but they do not create a second source for $S$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterCandidate\_determinacy}
\begin{proof}
Project the carrier of \autoref{def:metacic-normalization-certificate-router-carrier}. The row $K$ is the only carrier coordinate containing candidate, closed-candidate, or candidate-set evidence, and $S$ is declared as the readback row downstream of it.

Apply the obligation surface and closed-read refusal theorems. Theorem~\ref{thm:metacic-normalization-certificate-router-obligation-surface} lists $K$ before the strong-normalization readback, and \autoref{thm:metacic-normalization-certificate-router-closed-read-refusal} refuses any closed read lacking such evidence.

The Lean TasteGate marker attached to the obligation surface records the single-carrier alignment for the same router. Thus the paper route and the checked carrier site agree that $S$ has no evidence-free source outside $K$.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router adequacy chain]
\label{thm:metacic-normalization-certificate-router-adequacy-chain}
The Pi and application adequacy rows $P$ of an accepted $\MetaCICNormalizationCertificateRouterUp$ carrier can route only evidence already admitted by $K$. They cannot turn function, argument, application, generator, compiler, or kernel-acceptance rows into evidence-free normalization reads.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterAdequacyChain}
\begin{proof}
Read the candidate row first. By \autoref{def:metacic-normalization-certificate-router-carrier}, the adequacy row $P$ is a consumer of the displayed candidate-evidence row $K$, not a replacement for it.

Apply candidate determinacy. Theorem~\ref{thm:metacic-normalization-certificate-router-candidate-determinacy} pins the strong-normalization readback $S$ to evidence in $K$, so adequacy can only preserve and transmit that already displayed evidence.

Now compare with the sibling separation theorem. Theorem~\ref{thm:metacic-normalization-certificate-router-sibling-separation} separates this router from the frontier, closed-substitution, generator, compiler-flow, and kernel-acceptance siblings, so none of those rows can be treated as an independent normalization source.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router projection coverage]
\label{thm:metacic-normalization-certificate-router-projection-coverage}
The closed-term projection row $D$ of an accepted $\MetaCICNormalizationCertificateRouterUp$ carrier covers exactly the displayed $\ClosedTermSubstitutionBoundaryUp$ consumer route. It projects candidate-mediated normalization evidence into the closed-term boundary and exposes no additional closed-term normalization principle outside the rows admitted by $K$ and routed by $P$.
\end{theorem}
\begin{proof}
Project the carrier row $D$. In \autoref{def:metacic-normalization-certificate-router-carrier}, $D$ is the closed-term projection row consumed through $\ClosedTermSubstitutionBoundaryUp$.

Use the obligation surface. Theorem~\ref{thm:metacic-normalization-certificate-router-obligation-surface} places $D$ beside the candidate-evidence row $K$ and adequacy row $P$, so the projection is a consumer route rather than a new source of evidence.

Apply closed-read refusal and sibling separation. Theorem~\ref{thm:metacic-normalization-certificate-router-closed-read-refusal} blocks any closed read without evidence in $K$, and \autoref{thm:metacic-normalization-certificate-router-sibling-separation} prevents collapsing the router to the closed-substitution sibling alone. Hence $D$ covers exactly the displayed closed-term substitution boundary route.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterProjectionCoverage}

\begin{theorem}[MetaCIC normalization certificate router route coverage]
\label{thm:metacic-normalization-certificate-router-route-coverage}
For an admitted $\MetaCICNormalizationCertificateRouterUp$ read, strong-normalization output is reachable only through the ordered finite route
$$
A,K,S,P,D,U,G,L,H,C,Q,N.
$$
Here $K$ supplies candidate, closed-candidate, or candidate-set evidence; $D$ supplies the closed projection; $U$ supplies authorized generation; $G$ supplies compiler recognition; and $L$ supplies kernel inductive acceptance. No sibling row replaces $K$ by an evidence-free normalization theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-normalization-certificate-router-carrier}. The row inventory is exactly $A,K,S,P,D,U,G,L,H,C,Q,N$, with $S$ downstream of $K$ rather than a standalone normalization fact.

Use the obligation-surface theorem, \autoref{thm:metacic-normalization-certificate-router-obligation-surface}, to exhaust the rows. It places candidate-evidence admission in $K$, Pi and application adequacy in $P$, the closed projection in $D$, authorized generation in $U$, compiler recognition in $G$, and kernel inductive acceptance in $L$.

Now apply closed-read refusal, \autoref{thm:metacic-normalization-certificate-router-closed-read-refusal}. A closed-term normalization read without candidate, closed-candidate, or candidate-set evidence has no route to $S$.

Finally compare the sibling rows. $\ClosedTermSubstitutionBoundaryUp$, $\AuthorizedGeneratorRecursorUp$, $\GroundCompilerRecognizerFlowUp$, and $\KernelInductiveAcceptanceUp$ contribute projection, generation, compiler, and acceptance evidence respectively, but none is the candidate-evidence row $K$. Transport, replay, provenance, and naming preserve the same ordered route, so the admitted read cannot bypass $K$.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterRouteCoverage}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterPacket\_route\_coverage}

\begin{theorem}[MetaCIC normalization certificate router kernel scope]
\label{thm:metacic-normalization-certificate-router-kernel-scope}
Every accepted $\MetaCICNormalizationCertificateRouterUp$ read must expose the audit anchor $A$, candidate evidence $K$, strong-normalization readback $S$, Pi and application adequacy $P$, closed projection $D$, authorized generator $U$, compiler recognizer $G$, inductive acceptance $L$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $Q$, and local $\NameCert$ row $N$ before any normalization output is read. Thus the router is scoped to the finite kernel packet carried by $A,K,S,P,D,U,G,L,H,C,Q,N$, not to a host normalization principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-normalization-certificate-router-carrier}. Its displayed rows are exactly $A,K,S,P,D,U,G,L,H,C,Q,N$, and the strong-normalization readback row $S$ is downstream of the candidate-evidence row $K$.

Apply route coverage. Theorem~\ref{thm:metacic-normalization-certificate-router-route-coverage} orders every admitted normalization output through the displayed route, while \autoref{thm:metacic-normalization-certificate-router-generator-compiler-route} keeps the generator and compiler recognizer rows inside the same packet.

Finally use the inductive-acceptance scope theorem. It binds the $L$ row to the audit anchor, candidate evidence, closed projection, generator, compiler, transport, replay, provenance, and local naming rows. Hence no normalization output can be read before the finite kernel-scope surface is displayed.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterKernelScope}

\begin{theorem}[MetaCIC normalization certificate router sibling separation]
\label{thm:metacic-normalization-certificate-router-sibling-separation}
The $\MetaCICNormalizationCertificateRouterUp$ carrier is not bijective, row for row, with any one of $\MetaCICNormalizationFrontierUp$, $\ClosedTermSubstitutionBoundaryUp$, $\AuthorizedGeneratorRecursorUp$, $\GroundCompilerRecognizerFlowUp$, or $\KernelInductiveAcceptanceUp$. A bijection preserving local NameCert obligations would have to preserve simultaneous candidate evidence, closed projection, Pi and application adequacy, authorized generator handoff, compiler recognition, kernel acceptance, and no-infinite-reduction routing, and no listed sibling carries that whole surface.
\end{theorem}
\begin{proof}
Compare first with $\MetaCICNormalizationFrontierUp$. The frontier sibling supplies candidate and endpoint boundary rows, but it does not also carry the closed-substitution projection, authorized generator handoff, compiler recognizer handoff, and kernel inductive acceptance rows.

Compare next with $\ClosedTermSubstitutionBoundaryUp$ and $\AuthorizedGeneratorRecursorUp$. These siblings supply closed projection and generator authority respectively, while neither contains the candidate-evidence router with Pi and application adequacy plus no-infinite-reduction replay.

Finally compare with $\GroundCompilerRecognizerFlowUp$ and $\KernelInductiveAcceptanceUp$. Those rows record compiler-flow and inductive-acceptance surfaces, not the simultaneous normalization evidence router. Since each sibling omits at least one of $K,S,P,D,U,G,L$, no row-preserving bijection can preserve the router obligations.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouter\_sibling\_separation}

\begin{theorem}[MetaCIC normalization certificate router obligation package]
\label{thm:metacic-normalization-certificate-router-obligation-closure-package}
For every accepted $\MetaCICNormalizationCertificateRouterUp$ carrier
$$
R=(A,K,S,P,D,U,G,L,H,C,Q,N),
$$
the obligation package exposed by $R$ consists exactly of the audit anchor $A$, candidate-evidence row $K$, strong-normalization readback row $S$, Pi and application adequacy row $P$, closed-term projection row $D$, authorized generator handoff row $U$, compiler recognizer row $G$, kernel inductive-acceptance row $L$, componentwise transport row $H$, displayed replay row $C$, provenance row $Q$, and local naming row $N$.
\end{theorem}
\begin{proof}
Project the carrier coordinates in the order displayed by \autoref{def:metacic-normalization-certificate-router-carrier}. The rows $A,K,S,P,D,U,G,L$ provide the audit anchor, evidence, readback, adequacy, projection, generator, compiler, and acceptance obligations.

The structural rows then close the package. Transport through $H$ is componentwise, replay through $C$ follows the displayed route, provenance through $Q$ records the sibling certificates named at the start of the chapter, and $N$ supplies the local NameCert row. Since no further coordinate is present, the listed rows are the complete obligation package.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterObligationClosurePackage}

\begin{theorem}[MetaCIC normalization certificate router generator-compiler route]
\label{thm:metacic-normalization-certificate-router-generator-compiler-route}
Every generator-to-compiler read exported by an accepted $\MetaCICNormalizationCertificateRouterUp$ carrier factors through the authorized generator handoff $U$, the compiler recognizer row $G$, the kernel inductive-acceptance row $L$, componentwise transport $H$, displayed replay $C$, provenance $Q$, and local naming $N$. It cannot be read as an evidence-free normalization theorem, a host compiler assertion, or a route that bypasses the candidate evidence $K$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterGeneratorCompilerRoute}
\begin{proof}
First inspect the producer rows. The authorized generator handoff is the row $U$, while the compiler recognizer handoff is the row $G$; both are visible only as coordinates of the same finite carrier that also displays $K$ and $S$.

Next attach the acceptance and structural rows. The row $L$ records kernel inductive acceptance, and $H,C,Q,N$ preserve, replay, source, and name the same route. Thus any generator-to-compiler read remains inside the displayed packet and has no coordinate from which to extract evidence-free normalization or a detached host compiler claim.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router inductive acceptance scope]
\label{thm:metacic-normalization-certificate-router-inductive-acceptance-scope}
The inductive-acceptance contribution of an accepted $\MetaCICNormalizationCertificateRouterUp$ carrier is scoped to the displayed $L$ row together with the audit anchor $A$, candidate evidence $K$, closed projection $D$, generator handoff $U$, compiler recognizer row $G$, transport $H$, replay $C$, provenance $Q$, and local naming $N$. It does not close global MetaCIC normalization, quotient reduction equality, host normalization, or unconditional closed-term normalization outside that finite route.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterInductiveAcceptanceScope}
\begin{proof}
Read the scope from the sibling dependencies. $\KernelInductiveAcceptanceUp$ contributes the row $L$, while $\MetaCICNormalizationFrontierUp$, $\ClosedTermSubstitutionBoundaryUp$, $\AuthorizedGeneratorRecursorUp$, and $\GroundCompilerRecognizerFlowUp$ contribute the candidate, closed-projection, generator, and compiler-facing rows named in the chapter introduction.

The router preserves those rows as one finite packet. Componentwise transport, displayed replay, provenance, and local naming do not add a global normalization coordinate, so the inductive-acceptance read remains scoped to the displayed route and refuses the excluded host and quotient claims.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router residual candidate coverage]
\label{thm:metacic-normalization-certificate-router-residual-candidate-coverage}
Every residual normalization read accepted by a $\MetaCICNormalizationCertificateRouterUp$ carrier exposes candidate, closed-candidate, or candidate-set evidence in the row $K$ before the Pi and application adequacy row $P$, closed projection row $D$, authorized generator row $U$, compiler recognizer row $G$, and kernel acceptance row $L$ may contribute to the displayed route. The structural rows $H,C,Q,N$ preserve that candidate-mediated route by componentwise $\hsame$, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming, but they do not introduce a residual normalization read outside $K$.
\end{theorem}
\begin{proof}
Start with the obligation surface. Theorem~\ref{thm:metacic-normalization-certificate-router-obligation-surface} lists candidate-evidence admission through $K$ before the readback, adequacy, projection, generator, compiler, and acceptance rows, so every accepted residual read has a displayed evidence coordinate.

Apply the refusal and determinacy clauses. Theorem~\ref{thm:metacic-normalization-certificate-router-closed-read-refusal} rejects closed reads with no candidate, closed-candidate, or candidate-set evidence, while \autoref{thm:metacic-normalization-certificate-router-candidate-determinacy} determines the strong-normalization readback from the evidence in $K$.

It remains to account for the route rows. Theorem~\ref{thm:metacic-normalization-certificate-router-route-coverage} orders the finite route through $A,K,S,P,D,U,G,L,H,C,Q,N$, and \autoref{thm:metacic-normalization-certificate-router-inductive-acceptance-scope} keeps the kernel-acceptance contribution scoped to that displayed packet. Hence $P,D,U,G,L,H,C,Q,N$ can only transmit a residual read already mediated by $K$.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterResidualCandidateCoverage}

\begin{theorem}[MetaCIC normalization certificate router residual determinacy]
\label{thm:metacic-normalization-certificate-router-residual-determinacy}
If two accepted $\MetaCICNormalizationCertificateRouterUp$ packets expose the same authorized-generator row $U$, closed-term substitution boundary $D$, normalization frontier rows $K,S,P$, and residual candidate rows carried by $K$, then every candidate-mediated normalization read selects the same displayed residual route up to the packet classifier. No hidden redex choice, host reduction equality, quotient reduction row, or evidence-free normalizer can separate the two reads.
\end{theorem}
\begin{proof}
Start with candidate determinacy. Theorem~\ref{thm:metacic-normalization-certificate-router-candidate-determinacy} determines the strong-normalization readback from the candidate evidence in $K$, and \autoref{thm:metacic-normalization-certificate-router-residual-candidate-coverage} requires every residual read to be mediated by that displayed evidence.

Project the shared rows. Projection coverage and route coverage identify the finite route through $A,K,S,P,D,U,G,L,H,C,Q,N$, while the common $D,U$ and normalization-frontier rows fix the closed-substitution boundary, authorized-generator handoff, and candidate frontier seen by the read.

It remains only to exclude hidden selectors. The carrier has no coordinate for an external redex choice, host reduction equality, quotient reduction, or evidence-free normalizer, and the structural rows $H,C,Q,N$ only transport, replay, source, and name the displayed route. Hence the two packets select the same residual route up to their packet classifier.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router residual readback boundary]
\label{thm:metacic-normalization-certificate-router-residual-readback-boundary}
Every residual readback consumed from an accepted $\MetaCICNormalizationCertificateRouterUp$ carrier must expose candidate evidence in $K$, closed projection through $D$, authorized generator evidence through $U$, compiler recognizer evidence through $G$, kernel acceptance through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $Q$, and the local $\NameCert$ row $N$ before any strong-normalization output in $S$ can be consumed. No residual readback can bypass those rows through a hidden redex selector, evidence-free normalizer, host reduction equality, or quotient reduction row.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterResidualReadbackBoundary}
\begin{proof}
Begin with the obligation surface and closed-read refusal. Theorem~\ref{thm:metacic-normalization-certificate-router-obligation-surface} lists candidate admission through $K$ before readback, and \autoref{thm:metacic-normalization-certificate-router-closed-read-refusal} rejects any closed normalization read without candidate, closed-candidate, or candidate-set evidence.

Next attach route and residual coverage. Theorem~\ref{thm:metacic-normalization-certificate-router-route-coverage} orders the accepted route through $A,K,S,P,D,U,G,L,H,C,Q,N$, while \autoref{thm:metacic-normalization-certificate-router-residual-candidate-coverage} requires every residual read to expose the candidate row before $P,D,U,G,L$ contribute.

Finally use residual determinacy. Theorem~\ref{thm:metacic-normalization-certificate-router-residual-determinacy} fixes the residual route once the displayed candidate, closed projection, authorized generator, and frontier rows agree. Transport, replay, provenance, and naming preserve that route, so the strong-normalization output cannot be consumed before $K,D,U,G,L,H,C,Q,N$ have been displayed.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router scoped kernel route]
\label{thm:metacic-normalization-certificate-router-scoped-kernel-route}
Every accepted $\MetaCICNormalizationCertificateRouterUp$ read exposes the ordered finite route
$$
A,K,S,P,D,U,G,L,H,C,Q,N
$$
through candidate evidence, closed projection, authorized generator, compiler recognizer, kernel acceptance, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ before any normalization output is read. Thus the router binds normalization output to the BEDC kernel-facing surface and not to an evidence-free host normalizer.
\end{theorem}
\begin{proof}
Begin with the carrier and route coverage. The carrier definition displays $A,K,S,P,D,U,G,L,H,C,Q,N$, and Theorem~\ref{thm:metacic-normalization-certificate-router-route-coverage} orders any admitted strong-normalization output through that finite route.

Next apply the kernel-scope and generator-compiler routes. Theorem~\ref{thm:metacic-normalization-certificate-router-kernel-scope} requires candidate evidence, closed projection, authorized generation, compiler recognition, and kernel inductive acceptance before a normalization output is read, while Theorem~\ref{thm:metacic-normalization-certificate-router-generator-compiler-route} keeps generator-to-compiler reads inside the same displayed packet.

Finally use the inductive acceptance scope. Theorem~\ref{thm:metacic-normalization-certificate-router-inductive-acceptance-scope} binds the $L$ row to the audit anchor, candidate evidence, closed projection, generator, compiler, transport, replay, provenance, and local naming rows. Hence every normalization read is scoped to the BEDC kernel route carried by the accepted packet.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router public export]
\label{thm:metacic-normalization-certificate-router-public-export}
An accepted $\MetaCICNormalizationCertificateRouterUp$ packet exports exactly the public router surface already scoped by its carrier: generator recursion through $U$, ground-compiler recognition through $G$, kernel-inductive acceptance through $L$, closed-substitution boundary reads through $D$, residual-candidate determinacy through $K,S,P$, and non-escape through $H,C,Q,N$. The public interface contains no new MetaCIC primitive, evidence-free normalizer, host reduction equality, quotient reduction row, or closed-term normalization read outside
$$
A,K,S,P,D,U,G,L,H,C,Q,N .
$$
\end{theorem}
\begin{proof}
Start with the carrier and obligation surface. The definition displays $A,K,S,P,D,U,G,L,H,C,Q,N$, and Theorem~\ref{thm:metacic-normalization-certificate-router-obligation-surface} identifies these rows as the complete local certificate surface.

Next use the coverage and producer routes. Theorems~\ref{thm:metacic-normalization-certificate-router-route-coverage} and~\ref{thm:metacic-normalization-certificate-router-generator-compiler-route} keep every admitted normalization read, generator read, and compiler-recognizer read inside the same finite router packet.

The acceptance and residual clauses supply the remaining public fields. Theorem~\ref{thm:metacic-normalization-certificate-router-inductive-acceptance-scope} scopes the $L$ row to the displayed kernel route, while Theorems~\ref{thm:metacic-normalization-certificate-router-residual-candidate-coverage} and~\ref{thm:metacic-normalization-certificate-router-residual-determinacy} force residual normalization reads to be mediated by the candidate rows $K,S,P$ and the closed-substitution boundary $D$.

Finally apply the scoped kernel route. Theorem~\ref{thm:metacic-normalization-certificate-router-scoped-kernel-route} binds normalization output to the BEDC kernel-facing surface, and the carrier has no coordinate for a new MetaCIC primitive, evidence-free normalizer, host equality, quotient reduction row, or closed-term normalization read outside the displayed packet. Thus the listed rows are exactly the public export surface.
\end{proof}

\begin{theorem}[MetaCIC normalization certificate router public consumer boundary]
\label{thm:metacic-normalization-certificate-router-public-consumer-boundary}
Every downstream consumer of the public $\MetaCICNormalizationCertificateRouterUp$ export must preserve the ordered route
$$
A,K,S,P,D,U,G,L,H,C,Q,N .
$$
The candidate row $K$ must supply candidate, closed-candidate, or candidate-set evidence before the generator row $U$, compiler row $G$, and acceptance row $L$ are consumed. The consumer boundary therefore admits only candidate-mediated normalization evidence over the displayed packet and refuses evidence-free normalizers, host reduction equality, quotient reduction rows, and unconditional closed-term normalization outside that route.
\end{theorem}
\begin{proof}
Start with route coverage and the public export. Theorem~\ref{thm:metacic-normalization-certificate-router-route-coverage} orders every accepted read through $A,K,S,P,D,U,G,L,H,C,Q,N$, and \autoref{thm:metacic-normalization-certificate-router-public-export} identifies that ordered packet as the public router surface.

Next enforce candidate visibility. The residual candidate coverage theorem, \autoref{thm:metacic-normalization-certificate-router-residual-candidate-coverage}, requires $K$ to display candidate, closed-candidate, or candidate-set evidence before $P,D,U,G,L$ can contribute to a residual normalization read. Residual determinacy, \autoref{thm:metacic-normalization-certificate-router-residual-determinacy}, then keeps the chosen residual route fixed by that displayed evidence.

The projection and producer rows do not create another source. Projection coverage, \autoref{thm:metacic-normalization-certificate-router-projection-coverage}, keeps $D$ as a closed-substitution consumer of candidate-mediated evidence, while the generator-compiler route, \autoref{thm:metacic-normalization-certificate-router-generator-compiler-route}, keeps $U,G,L$ inside the same packet.

Finally apply the scoped kernel route, \autoref{thm:metacic-normalization-certificate-router-scoped-kernel-route}. It binds normalization output to the BEDC kernel-facing rows and leaves no carrier coordinate for evidence-free normalizers, host reduction equality, quotient reduction rows, or unconditional closed-term normalization outside $A,K,S,P,D,U,G,L,H,C,Q,N$.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterPublicConsumerBoundary}

\begin{theorem}[MetaCIC normalization certificate router critical-pair discharge]
\label{thm:metacic-normalization-certificate-router-critical-pair-discharge}
Let $R=(A,K,S,P,D,U,G,L,H,C,Q,N)$ be an accepted $\MetaCICNormalizationCertificateRouterUp$ carrier. Suppose a router candidate read through $R$ passes through the sibling rows $\MetaCICNormalizationFrontierUp$, $\ClosedTermSubstitutionBoundaryUp$, $\MetaCICResidualSubstitutionBoundaryUp$, and $\MetaCICConfluenceCriticalPairBoundaryUp$. Then the read reaches a finite discharge row for the displayed residual-substitution and confluence-critical-pair boundary before any normalization certificate is exported through $S$ or through the public route. The discharge remains inside $A,K,S,P,D,U,G,L,H,C,Q,N$ together with the named sibling boundary rows; it does not export evidence-free strong normalization, host conversion equality, quotient reduction equality, or a hidden global confluence theorem.
\end{theorem}
\begin{proof}
Start from candidate determinacy and route coverage. Theorems~\ref{thm:metacic-normalization-certificate-router-candidate-determinacy} and~\ref{thm:metacic-normalization-certificate-router-route-coverage} force every accepted normalization read to expose candidate evidence in $K$ and to follow the ordered packet $A,K,S,P,D,U,G,L,H,C,Q,N$.

Next use the producer and residual rows. The generator-compiler route, \autoref{thm:metacic-normalization-certificate-router-generator-compiler-route}, keeps $U,G,L$ inside the same finite packet, while residual candidate coverage and residual determinacy, \autoref{thm:metacic-normalization-certificate-router-residual-candidate-coverage} and \autoref{thm:metacic-normalization-certificate-router-residual-determinacy}, keep the residual substitution read tied to the candidate, closed-substitution, and frontier rows already displayed.

Now apply the residual readback boundary and scoped kernel route. Theorem~\ref{thm:metacic-normalization-certificate-router-residual-readback-boundary} requires $K,D,U,G,L,H,C,Q,N$ before any residual readback can be consumed, and \autoref{thm:metacic-normalization-certificate-router-scoped-kernel-route} keeps the resulting normalization output kernel-facing.

Finally invoke the public export and consumer boundary. Theorems~\ref{thm:metacic-normalization-certificate-router-public-export} and~\ref{thm:metacic-normalization-certificate-router-public-consumer-boundary} allow downstream reads only through the displayed public router surface. Therefore a candidate route that also names $\MetaCICResidualSubstitutionBoundaryUp$ and $\MetaCICConfluenceCriticalPairBoundaryUp$ must discharge those finite boundary rows before export, and no evidence-free strong normalization, host conversion equality, quotient reduction equality, or hidden confluence theorem has a carrier coordinate.
\end{proof}

\falsifiablePrediction{A closed-term normalization read is refused unless candidate, closed-candidate, or candidate-set evidence is present on the displayed route.}

\independenceWitness{metacicnormalizationfrontier, closedtermsubstitutionboundary, authorizedgeneratorrecursor, groundcompilerrecognizerflow, kernelinductiveacceptance: the router has simultaneous candidate-evidence, closed projection, Pi/app adequacy, and no-infinite-reduction rows not bijective with any one sibling.}

The Lean TasteGate site for $\MetaCICNormalizationCertificateRouterUp$ must provide explicit $\Nontrivial\ \MetaCICNormalizationCertificateRouterUp$ and $\FieldFaithful\ \MetaCICNormalizationCertificateRouterUp$ instances. These instances preserve candidate evidence, closed projection, Pi and application adequacy, authorized generator handoff, compiler recognition, kernel acceptance, route replay, provenance, and local naming as distinguishable fields.

\closureat{MetaCICNormalizationCertificateRouterUp}{seedStr}
\closureat{MetaCICNormalizationCertificateRouterUp}{obligationStr}[BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterUp\_single\_carrier\_alignment]
\closureat{MetaCICNormalizationCertificateRouterUp}{scopedStr}[BEDC.Derived.MetaCICNormalizationCertificateRouterUp.MetaCICNormalizationCertificateRouterKernelScope]

\begin{closurestatus}{\MetaCICNormalizationCertificateRouterUp}
  \constructivestory{A $\MetaCICNormalizationCertificateRouterUp$ packet is generated from an audit-map anchor, candidate evidence, strong-normalization readback, Pi and application adequacy, closed-term projection, authorized generator handoff, compiler recognition, kernel inductive acceptance, componentwise transport, displayed replay, provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:metacic-normalization-certificate-router-carrier}, \autoref{thm:metacic-normalization-certificate-router-obligation-surface}, \autoref{thm:metacic-normalization-certificate-router-closed-read-refusal}, \autoref{thm:metacic-normalization-certificate-router-sibling-separation}, \autoref{thm:metacic-normalization-certificate-router-obligation-closure-package}, \autoref{thm:metacic-normalization-certificate-router-generator-compiler-route}, \autoref{thm:metacic-normalization-certificate-router-inductive-acceptance-scope}, and \autoref{thm:metacic-normalization-certificate-router-scoped-kernel-route} bind candidate-mediated normalization reads as a finite kernel-scoped router surface.}
  \leanchecked{BEDC.Derived.MetaCICNormalizationCertificateRouterUp.taste\_gate}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close evidence-free strong normalization, unconditional application normalization, host normalization, quotient reduction equality, Lean verification, or bridge checking.}
  \upgradepath{Public closure requires downstream consumers that preserve the scoped kernel route and its displayed candidate, projection, generator, compiler, acceptance, replay, provenance, and naming rows.}
\end{closurestatus}
