\chapter{A Concrete Naming Certificate for $\SeparatedCauchySequenceUp$}
\label{ch:concrete-instances-separated-cauchy-sequence-namecert}
\origin{human}

Following Bishop--Bridges style separated metric and Cauchy-sequence grounding, the $\SeparatedCauchySequenceUp$ packet records the finite route by which a displayed regular rational sequence is read as a separated completion-facing name before a Real consumer may inspect the terminal seal. It sits after the $\StreamNameUp$ finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the cofinal restriction route of \autoref{ch:concrete-instances-cauchy-subnet-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet names separated Cauchy-sequence data as finite NameCert rows, not as quotient sequence equality, a selected limit, host real equality, countable choice, or ambient Real completeness.

\begin{definition}[Separated Cauchy sequence carrier]
\label{def:separated-cauchy-sequence-carrier}
A $\SeparatedCauchySequenceUp$ carrier is a finite $\BHist$ packet
$$
S=(W,R,D,K,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the $\StreamNameUp$ finite-window row exposing the observed prefix and tail windows of the sequence.
\item $R$ is the $\RegSeqRatUp$ readback row attached to those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the separated tail comparisons used by the Cauchy test.
\item $K$ is the $\CauchySubnetUp$ cofinal-tail route used when the consumer restricts the read to a displayed subnet of tail requests.
\item $Q$ is the separated-comparison row, recording that zero-distance comparison is read only through displayed tail and tolerance rows.
\item $E$ is the terminal $\RealUp$ seal, available only after $W,R,D,K,Q$ remain visible together.
\item $H$ records componentwise $\hsame$ transport for the window, readback, tolerance, cofinal route, separated-comparison, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a precision request through $W,R,D,K,Q,E$ in that order.
\item $P$ is the $\Pkg$ provenance over $\SeparatedCauchySequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchySubnetUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SeparatedCauchySequenceUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient sequence equality, selected limits, countable choice, host real equality, trichotomy, or ambient Real completeness.
\end{definition}

\begin{theorem}[Separated Cauchy sequence NameCert obligations]
\label{thm:separated-cauchy-sequence-namecert-obligations}
For every accepted $\SeparatedCauchySequenceUp$ carrier $S=(W,R,D,K,Q,E,H,C,P,N)$, the local $\NameCert_{\SeparatedCauchySequenceUp}$ surface consists exactly of finite-window admission, regular rational readback, dyadic tolerance exposure, cofinal-tail route exposure, separated-comparison exposure, terminal Real sealing, componentwise transport, displayed replay, provenance, and local naming.

Every obligation is anchored in the displayed $\BHist$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchySubnetUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. No obligation row may replace the finite packet by quotient sequence equality, a selected limit, countable choice, host real equality, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:separated-cauchy-sequence-carrier}. The analytic data are $W,R,D,K,Q,E$, and the structural support is $H,C,P,N$.

Read the route in order. The StreamName window $W$ exposes the inspected finite tail data, the RegSeqRat row $R$ records the regular rational readback, and the DyadicRatCore row $D$ supplies the tolerance ledger for separated tail comparison.

The cofinal-tail row $K$ is the only subnet restriction route, and the separated-comparison row $Q$ is downstream of $W,R,D,K$. The terminal Real seal $E$ is downstream of all five displayed data rows.

Transport, replay, provenance, and local naming preserve the same rows through $H,C,P,N$. Since no carrier coordinate stores quotient sequence equality, selected limits, countable choice, host real equality, trichotomy, or ambient completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Separated Cauchy sequence real-completion handoff]
\label{thm:separated-cauchy-sequence-real-completion-handoff}
Every Real or completion-facing consumer of an accepted $\SeparatedCauchySequenceUp$ packet factors through the finite route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \CauchySubnetUp \longmapsto \RealUp
$$
together with the separated-comparison, $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert_{\SeparatedCauchySequenceUp}$ rows. The consumer receives a displayed separated Cauchy-sequence handoff and not quotient sequence equality, a selected limit, host real equality, countable choice, trichotomy, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with an accepted carrier $S=(W,R,D,K,Q,E,H,C,P,N)$. The terminal seal $E$ is not a free Real value; it is downstream of the window, readback, tolerance, cofinal route, and separated-comparison rows.

Use \autoref{thm:separated-cauchy-sequence-namecert-obligations} to exhaust the local surface. A consumer must expose $W,R,D,K,Q,E,H,C,P,N$ before the packet can be consumed by a Real or completion-facing route.

The cited sibling chapters supply the allowed components: StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, CauchySubnet cofinal-tail routing, and the terminal Real seal. Since the carrier refuses the excluded coordinates, the handoff is a finite NameCert route rather than an ambient completeness theorem or selected host limit.
\end{proof}

\falsifiablePrediction{An accepted $\SeparatedCauchySequenceUp$ read is refuted by any Real-facing consumer that reaches the terminal seal without first displaying the StreamName finite window, RegSeqRat readback, DyadicRatCore tolerance ledger, CauchySubnet cofinal route, and separated-comparison row.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, cauchysubnet: $\SeparatedCauchySequenceUp$ is not bijective to any listed sibling because it jointly carries finite windows, regular rational readback, dyadic separated-tail tolerance, cofinal-tail routing, terminal Real sealing, transport, replay, provenance, and local naming in one packet.}

\closureat{SeparatedCauchySequenceUp}{seedStr}

\begin{closurestatus}{\SeparatedCauchySequenceUp}
  \constructivestory{A $\SeparatedCauchySequenceUp$ packet is generated from StreamName finite windows, RegSeqRat readback, a DyadicRatCore separated-tail tolerance ledger, a CauchySubnet cofinal route, a separated-comparison row, a terminal Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:separated-cauchy-sequence-carrier}, \autoref{thm:separated-cauchy-sequence-namecert-obligations}, and \autoref{thm:separated-cauchy-sequence-real-completion-handoff} seed the separated Cauchy-sequence route through StreamName, RegSeqRat, DyadicRatCore, CauchySubnet, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient sequence equality, selected limits, countable choice, host real equality, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, separated-tail tolerance exposure, cofinal-tail routing, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.SeparatedCauchySequenceUp.}
\end{closurestatus}
