\chapter{A Concrete Naming Certificate for $\SubjectReductionRouteAuditUp$}
\label{ch:concrete-instances-subject-reduction-route-audit-namecert}
\origin{human}

The $\SubjectReductionRouteAuditUp$ packet is the finite audit surface for
the explicit bundle discharge route described in \autoref{ch:sr-discharge-routes}.
It keeps the obstruction boundary of
\autoref{ch:metacic-subject-reduction-obstruction} visible while consuming
the socket rows of
\autoref{ch:concrete-instances-subject-reduction-discharge-socket-namecert}.
The packet records route handoff data only; it does not inhabit the four
MetaCIC discharge hypotheses and does not close subject reduction.

\begin{definition}[Subject-reduction route-audit carrier]
\label{def:subject-reduction-route-audit-carrier}
A $\SubjectReductionRouteAuditUp$ carrier is a finite $\BHist$ packet
$$
A=(B,E,I,U,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the explicit bundle row naming the four subject-reduction discharge hypotheses as one $\mathrm{SubjectReductionDischargeBundle}$ packet;
\item $E$ is the route-exactness row comparing accepted bundle reads with the socket payload;
\item $I$ is the invocation row recording downstream explicit-bundle consumption;
\item $U$ is the socket handoff row through which the consumer reaches the displayed discharge socket surface;
\item $S$ is the $\SubjectReductionDischargeSocketUp$ row carrying the four named discharge sockets;
\item $H$ records componentwise $\hsame$ transports for the bundle row, exactness row, invocation row, socket row, handoff row, provenance row, and local name;
\item $C$ records the displayed $\Cont$ routes from route exactness to socket handoff and downstream subject-reduction consumption;
\item $P$ is the $\Pkg$ provenance over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\SubjectReductionDischargeSocketUp$, and the bundle route;
\item $N$ is the local $\NameCert_{\SubjectReductionRouteAuditUp}$ row.
\end{enumerate}
The carrier classifier compares only the displayed route rows, socket rows,
transports, continuations, provenance, and local naming data. It has no
coordinate for closed subject reduction, host conversion, quotient equality of
terms, erased semantics, or an inhabitant of any missing discharge hypothesis.
\end{definition}
\leanchecked{BEDC.Derived.SubjectReductionRouteAuditUp.SubjectReductionRouteAuditTasteGate\_single\_carrier\_alignment}

\begin{theorem}[SubjectReductionRouteAudit NameCert obligations]
\label{thm:subject-reduction-route-audit-namecert-obligations}
For the carrier of \autoref{def:subject-reduction-route-audit-carrier}, the
local $\NameCert_{\SubjectReductionRouteAuditUp}$ surface consists exactly of
carrier admission for
$$
A=(B,E,I,U,S,H,C,P,N),
$$
bundle-route visibility through $B$, route-exactness comparison through $E$,
invocation through $I$, socket handoff through $U,S$, componentwise transport
through $H$, displayed continuation reads through $C$, provenance through $P$,
and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed packet $A$. The bundle row $B$ is the public route
presentation, while $E$ records exactness against the socket payload.

Read the invocation row $I$. It records that the explicit bundle path is used,
but it does not add a fifth discharge row and does not alter the four-row
socket payload.

The handoff rows $U,S$ place the route in the socket surface. The transports
$H$ and continuations $C$ replay only the displayed route and socket
coordinates, and $P,N$ record provenance and local naming. These rows exhaust
the finite NameCert obligations.
\end{proof}
\leanchecked{BEDC.Derived.SubjectReductionRouteAuditUp.SubjectReductionRouteAudit\_namecert\_obligations}

\begin{theorem}[SubjectReductionRouteAudit socket handoff]
\label{thm:subject-reduction-route-audit-socket-handoff}
Every downstream consumer of an accepted $\SubjectReductionRouteAuditUp$
packet reaches the subject-reduction discharge socket only through
$$
B,E,I,U,S,H,C,P,N.
$$
The explicit bundle route hands off the socket payload without exporting a
closed subject-reduction theorem, a proof of the four discharge hypotheses, a
host conversion rule, or any route outside the displayed bundle comparison.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteAuditUp.SubjectReductionRouteAudit\_socket\_handoff}
\begin{proof}
Start with the carrier and the NameCert-obligation theorem. They identify
every permitted consumer read with one of the displayed rows of $A$.

The bundle route reaches the socket through $B,U,S$. The comparison row $E$
records that this path exposes the same four-row payload named by the socket.

Transport and replay are then forced through $H$ and $C$. Since the packet has
no coordinate for an inhabited discharge hypothesis, host conversion, closed
subject reduction, or an alternate route, the socket handoff remains a finite
audit row for bundle-route consumption alone.
\end{proof}

\begin{theorem}[SubjectReductionRouteAudit obligation surface]
\label{thm:subject-reduction-route-audit-obligation-surface}
Every accepted $\SubjectReductionRouteAuditUp$ packet exposes exactly its bundle route comparison row, socket handoff row, nonescape ledger, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before any MetaCIC discharge consumer can inspect it. The exposed surface remains an audit packet and does not prove subject reduction, closed subject reduction, or a public schema bridge.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteAuditUp.SubjectReductionRouteAudit\_obligation\_surface}
\begin{proof}
Project the carrier of \autoref{def:subject-reduction-route-audit-carrier}. The comparison row is $E$, the socket handoff is read through $U,S$, the nonescape information is the absence of any discharge-hypothesis coordinate beyond the displayed route rows, and $H,C,P,N$ supply transport, replay, provenance, and naming.

Apply the NameCert obligations of \autoref{thm:subject-reduction-route-audit-namecert-obligations}. They identify the accepted packet with exactly those carrier, comparison, handoff, transport, replay, provenance, and local naming rows.

Finally use the socket handoff theorem, \autoref{thm:subject-reduction-route-audit-socket-handoff}. It permits downstream inspection only through the displayed bundle comparison and socket rows, while explicitly withholding closed subject reduction, the missing discharge hypotheses, host conversion, and any route outside the displayed bundle comparison. Hence the obligation surface supports MetaCIC discharge work without asserting subject reduction, closed subject reduction, or a public schema bridge.
\end{proof}

\begin{theorem}[SubjectReductionRouteAudit discharge boundary]
\label{thm:subject-reduction-route-audit-discharge-boundary}
Every accepted $\SubjectReductionRouteAuditUp$ packet exposes the explicit bundle row, route-exactness row, invocation row, socket handoff row, discharge-socket row, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before any MetaCIC consumer may inspect it. The same packet still does not inhabit any of the four discharge hypotheses and does not prove closed subject reduction.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:subject-reduction-route-audit-carrier}. The coordinates $B,E,I,U,S,H,C,P,N$ are respectively the explicit bundle, exactness, invocation, handoff, socket, transport, replay, provenance, and local naming rows.

Apply \autoref{thm:subject-reduction-route-audit-namecert-obligations}. It forces every accepted inspection route to pass through exactly those displayed rows before a downstream MetaCIC consumer can read the packet.

Apply \autoref{thm:subject-reduction-route-audit-socket-handoff}. The socket handoff exposes the discharge-socket payload through $U,S$ while preserving the absence of coordinates for the four hypothesis inhabitants, host conversion, closed subject reduction, or an alternate route. Thus the discharge boundary is a finite audit surface and not a proof of subject reduction.
\end{proof}
\leanchecked{BEDC.Derived.SubjectReductionRouteAuditUp.SubjectReductionRouteAudit\_discharge\_boundary}

\begin{theorem}[SubjectReductionRouteAudit discharge socket scope]
\label{thm:subject-reduction-route-audit-discharge-socket-scope}
Every accepted $\SubjectReductionRouteAuditUp$ packet scopes MetaCIC discharge inspection to the explicit bundle row $B$, route exactness $E$, invocation $I$, socket handoff $U,S$, transport $H$, replay $C$, provenance $P$, and local $\NameCert$ row $N$. It does not provide the four discharge hypotheses.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteAuditUp.SubjectReductionRouteAudit\_discharge\_socket\_scope}
\begin{proof}
Use the carrier of \autoref{def:subject-reduction-route-audit-carrier}. It lists exactly the bundle, route-exactness, invocation, handoff, socket, transport, replay, provenance, and local naming rows.

Apply the NameCert obligations of \autoref{thm:subject-reduction-route-audit-namecert-obligations}. They force accepted inspection to preserve those rows as the packet surface rather than replacing them by inhabited discharge hypotheses.

The socket handoff of \autoref{thm:subject-reduction-route-audit-socket-handoff} and the discharge boundary of \autoref{thm:subject-reduction-route-audit-discharge-boundary} then confine the downstream MetaCIC read to $U,S$ and the displayed structural rows. Since no carrier coordinate supplies an inhabitant of any discharge hypothesis, the packet remains a scope theorem for inspection, not a subject-reduction proof.
\end{proof}

\begin{theorem}[SubjectReductionRouteAudit obligation closure]
\label{thm:subject-reduction-route-audit-obligation-closure}
The carrier admission row, bundle route comparison row, socket handoff exactness row, non-escape row, provenance row, and local naming row form the obligation-closed $\SubjectReductionRouteAuditUp$ surface for the same finite route-audit packet $A=(B,E,I,U,S,H,C,P,N)$. This closure records the explicit bundle route and its discharge-socket handoff; it does not inhabit the four discharge hypotheses and does not prove subject reduction.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteAuditUp.SubjectReductionRouteAudit\_obligation\_closure}
\begin{proof}
Carrier admission is \autoref{def:subject-reduction-route-audit-carrier}: the packet exposes exactly the rows $B,E,I,U,S,H,C,P,N$.

The bundle route comparison is supplied by \autoref{thm:subject-reduction-route-audit-namecert-obligations} and \autoref{thm:subject-reduction-route-audit-obligation-surface}. They identify $B,E,I$ as the displayed route, comparison, and invocation rows, with $H$ and $C$ carrying transport and replay.

Socket handoff exactness is \autoref{thm:subject-reduction-route-audit-socket-handoff}, refined by \autoref{thm:subject-reduction-route-audit-discharge-boundary} and \autoref{thm:subject-reduction-route-audit-discharge-socket-scope}. These theorems confine every downstream read to $U,S$ and the same structural rows.

The non-escape, provenance, and local naming clauses are then just the remaining coordinates $P,N$ together with the absence of any carrier field for the four discharge hypotheses, host conversion, closed subject reduction, or an alternate route. Hence the displayed rows form the obligation-closed audit surface without claiming subject reduction.
\end{proof}

\closureat{SubjectReductionRouteAuditUp}{seedStr}

\begin{closurestatus}{\SubjectReductionRouteAuditUp}
  \constructivestory{A $\SubjectReductionRouteAuditUp$ packet is generated from an explicit bundle row, route-exactness comparison, an invocation row, a socket handoff, the discharge socket row, componentwise $\hsame$ transports, displayed $\Cont$ routes, provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:subject-reduction-route-audit-carrier}, \autoref{thm:subject-reduction-route-audit-namecert-obligations}, and \autoref{thm:subject-reduction-route-audit-socket-handoff} bind the finite route-audit packet between bundle comparison and subject-reduction socket handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not prove any MetaCIC discharge hypothesis, construct closed subject reduction, provide Lean verification, or bridge the packet to a public schema.}
  \upgradepath{Obligation closure requires checked carrier admission, bundle route comparison, socket handoff exactness, non-escape, provenance, and local naming for the same finite route-audit packet.}
\end{closurestatus}
