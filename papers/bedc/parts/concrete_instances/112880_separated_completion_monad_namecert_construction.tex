\chapter{A Concrete Naming Certificate for $\SeparatedCompletionMonadUp$}
\label{ch:concrete-instances-separated-completion-monad-namecert}
\origin{human}

Following Bishop--Bridges constructive completion practice and Mac Lane's monad language, the $\SeparatedCompletionMonadUp$ packet records the L10 completion route as a separated unit-and-bind interface over finite BEDC rows. It sits after the separated completion carrier of \autoref{ch:concrete-instances-separatedcompletion-namecert}, the completion monad schedule of \autoref{ch:concrete-instances-cauchycompletionmonad-namecert}, the stream-name observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the separated monad interface needed by completion-functor and filter-completion consumers; it does not assert quotient completion, selected representatives, countable choice, or a host limit operator.

\begin{definition}[Separated completion monad carrier]
\label{def:separated-completion-monad-carrier}
A $\SeparatedCompletionMonadUp$ carrier is a finite $\BHist$ packet
$$
S=(A,M,U,B,W,R,D,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\SeparatedCompletionUp$ admission row, exposing the separated comparison boundary before any monad law is public.
\item $M$ is the $\CauchyCompletionMonadUp$ schedule row, naming the unit and bind requests as finite completion-facing data.
\item $U$ is the unit row, carrying the displayed source-name admission into the separated completion surface.
\item $B$ is the bind row, carrying a completion-facing map only through the same separated completion and Cauchy-completion schedule.
\item $W$ is the finite $\StreamNameUp$ window row read before any completion-facing Real seal is consumed.
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the stream and regular-rational reads.
\item $L$ is the terminal $\RealUp$ seal row, available only as a displayed name boundary.
\item $H$ records componentwise $\hsame$ transport for separated completion, monad schedule, unit, bind, stream, regular-rational, dyadic, Real-seal, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from separated admission through unit, bind, finite windows, regular-rational readback, dyadic tolerance, Real seal, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\SeparatedCompletionMonadUp$, $\SeparatedCompletionUp$, $\CauchyCompletionMonadUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SeparatedCompletionMonadUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,M,U,B,W,R,D,L,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, countable choice, a chosen host limit, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Separated completion monad NameCert obligations]
\label{thm:separated-completion-monad-namecert-obligations}
For every accepted $\SeparatedCompletionMonadUp$ carrier $S=(A,M,U,B,W,R,D,L,H,C,P,N)$, the local $\NameCert_{\SeparatedCompletionMonadUp}$ surface consists exactly of separated admission, Cauchy-completion monad schedule exposure, unit admission, bind replay, finite stream windows, regular-rational readback, dyadic tolerance accounting, terminal Real sealing, componentwise transport, displayed continuation replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:separated-completion-monad-carrier}. The separated row $A$ is read before the monad schedule $M$, so equality-facing completion reads cannot bypass the separated boundary.

The unit row $U$ and bind row $B$ are then inspected through the same schedule. Their finite observations are forced through $W,R,D,L$, which exposes stream windows, regular-rational readback, dyadic tolerance, and the terminal Real seal as displayed rows.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since no quotient completion, selected representative, countable-choice schedule, host limit, or ambient completeness coordinate is present, the NameCert obligations are exhausted by the listed rows.
\end{proof}

\begin{theorem}[Separated completion monad unit and bind laws]
\label{thm:separated-completion-monad-unit-bind-laws}
Every unit or bind law exported by an accepted $\SeparatedCompletionMonadUp$ packet is read as equality of the displayed finite ledger over $A,M,U,B,W,R,D,L,H,C,P,N$. The unit law admits source names only through $U$ after separated admission, and the bind law composes completion-facing reads only through $B$ after the same stream, regular-rational, dyadic, and Real-seal rows have been exposed.
\end{theorem}
\begin{proof}
For the unit side, read $A$ and $M$ before $U$. The source name enters the completion surface only after the separated boundary and Cauchy-completion schedule have been displayed.

For the bind side, read $B$ together with $W,R,D,L$. A completion-facing map can be replayed only against the visible finite windows, regular-rational readback, dyadic tolerance ledger, and Real seal already carried by the packet.

Componentwise transport by $H$ and replay by $C$ identify the two law reads only as equality of the same finite ledger. The provenance and local naming rows prevent the law from being replaced by quotient completion equality, selected representatives, or a host limit operation.
\end{proof}

\begin{theorem}[Separated completion monad idempotent reflection]
\label{thm:separated-completion-monad-idempotent-reflection}
An accepted $\SeparatedCompletionMonadUp$ packet may be consumed as an idempotent separated-reflection surface only after the separated completion row $A$, the monad schedule $M$, the unit row $U$, the bind row $B$, and the L10 observation rows $W,R,D,L$ have all been displayed. The reflection is therefore a finite NameCert route for repeated separated completion, not a quotient collapse of completion points and not a choice-selected limit operation.
\end{theorem}
\begin{proof}
Begin with separated admission. The row $A$ supplies the comparison boundary that repeated completion reads must share, while $M$ records that the read is governed by the same completion monad schedule.

The unit and bind rows $U,B$ supply the repeated-completion interface. Because both rows are tied to $W,R,D,L$, the second completion read has access only to the same finite stream windows, regular-rational readback, dyadic tolerances, and Real seal.

Transport, replay, provenance, and local naming keep the repeated read inside one packet. Thus the idempotent-reflection consumer receives a finite separated monad surface and receives no quotient collapse, selected representative, selected limit, countable-choice schedule, or ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\SeparatedCompletionMonadUp$ packet must expose separated admission, Cauchy-completion monad schedule, unit, bind, stream, regular-rational, dyadic, Real-seal, transport, replay, provenance, and NameCert rows before a completion-functor or filter-completion consumer may read it.}

\independenceWitness{separated-completion, cauchy-completion-monad, stream-name, regular-rational, dyadic-core, real-seal: $\SeparatedCompletionMonadUp$ is not bijective to any listed sibling because it jointly carries separated admission, unit-and-bind scheduling, L10 observation rows, transport, replay, provenance, and local naming.}

\closureat{SeparatedCompletionMonadUp}{seedStr}

\begin{closurestatus}{\SeparatedCompletionMonadUp}
  \constructivestory{A $\SeparatedCompletionMonadUp$ packet is generated from separated completion admission, a CauchyCompletionMonad unit-and-bind schedule, finite StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:separated-completion-monad-carrier}, \autoref{thm:separated-completion-monad-namecert-obligations}, \autoref{thm:separated-completion-monad-unit-bind-laws}, and \autoref{thm:separated-completion-monad-idempotent-reflection} seed the finite separated unit-and-bind route for L10 completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected representatives, selected limits, countable choice, ambient completeness of $\RealUp$, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, separated boundary routing, unit exactness, bind exactness, idempotent reflection routing, transport, replay, provenance, and local naming for BEDC.Derived.SeparatedCompletionMonadUp.}
\end{closurestatus}
