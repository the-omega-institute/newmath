\chapter{A Concrete Naming Certificate for $\RegularCauchyReverseTriangleUp$}
\label{ch:concrete-instances-regular-cauchy-reverse-triangle-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyReverseTriangleUp$ packet records the reverse-triangle estimate for displayed regular rational Cauchy real names as a finite route through subtraction, absolute-value readbacks, and a dyadic tolerance ledger. It sits over the regular source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic absolute-value surface of \autoref{ch:concrete-instances-dyadicabsolutevalue-namecert}, the regular absolute-value lift of \autoref{ch:concrete-instances-regularcauchyabs-namecert}, the subtraction route of \autoref{ch:concrete-instances-regular-cauchy-subtraction-namecert}, the real absolute-value boundary of \autoref{ch:concrete-instances-real-abs-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet exposes the finite inequality route behind $||x|-|y|| \leq |x-y|$; it is not host real order, quotient equality of Cauchy streams, a selected limit, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy reverse-triangle carrier]
\label{def:regular-cauchy-reverse-triangle-carrier}
A $\RegularCauchyReverseTriangleUp$ carrier is a finite $\BHist$ packet
$$
T=(X,Y,S,A_X,A_Y,D,A_D,L,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two source $\RegSeqRatUp$ rows for displayed regular rational Cauchy real names.
\item $S$ is the $\RegularCauchySubtractionUp$ route for $X-Y$, exposing only its source rows, sign-flip-plus-sum route, synchronized $\StreamNameUp$ windows, dyadic tolerance rows, and subtraction readback.
\item $A_X$ and $A_Y$ are the two $\RegularCauchyAbsUp$ routes for the source rows $X$ and $Y$.
\item $D$ is the $\RegularCauchySubtractionUp$ route for the displayed difference $|X|-|Y|$, using the readbacks exported by $A_X$ and $A_Y$.
\item $A_D$ is the $\RegularCauchyAbsUp$ route for the regular readback of $|X|-|Y|$.
\item $L$ is the dyadic comparison ledger whose finite rows read the endpoint-and-tolerance data from $A_X,A_Y,D,A_D,S$ and record the inequality window $||x|-|y|| \leq |x-y|$ at each requested dyadic tolerance.
\item $R$ is the resulting $\RegSeqRatUp$ readback row for the bounded absolute difference, assembled only after the subtraction and absolute-value rows have been displayed.
\item $E$ is the $\RealUp$ seal row reached only after $R$ and the finite comparison ledger $L$ have been exposed.
\item $H$ records componentwise $\hsame$ transport for the two source rows, subtraction routes, absolute-value routes, comparison ledger, readback, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from any reverse-triangle-facing precision request through $X,Y,S,A_X,A_Y,D,A_D,L,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyReverseTriangleUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicAbsoluteValueUp$, $\RegularCauchyAbsUp$, $\RegularCauchySubtractionUp$, $\RealAbsUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyReverseTriangleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,S,A_X,A_Y,D,A_D,L,R,E,H,C,P,N$. The carrier has no coordinate for host real order, quotient Cauchy equality, selected limits, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}
\leandef{BEDC.Derived.RegularCauchyReverseTriangleUp.RegularCauchyReverseTriangleUp}

\begin{theorem}[Regular Cauchy reverse-triangle NameCert obligations]
\label{thm:regular-cauchy-reverse-triangle-namecert-obligations}
For every accepted $\RegularCauchyReverseTriangleUp$ carrier
$$
T=(X,Y,S,A_X,A_Y,D,A_D,L,R,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyReverseTriangleUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ source rows, the $X-Y$ subtraction route, the absolute-value routes for both sources, the subtraction route for $|X|-|Y|$, the absolute-value route for that difference, the dyadic comparison ledger, the resulting readback, and the structural rows;
\item subtraction handoff, requiring $S$ and $D$ to expose their outputs only through the finite $\RegularCauchySubtractionUp$ route;
\item absolute-value handoff, requiring $A_X,A_Y,A_D$ to expose their outputs only through the finite $\RegularCauchyAbsUp$ route and its $\DyadicAbsoluteValueUp$ endpoint-and-tolerance ledger;
\item comparison exactness, requiring $L$ to compare the displayed dyadic windows for $||x|-|y||$ and $|x-y|$ before any $\RegSeqRatUp$ or $\RealUp$ consumer receives a bounded read;
\item Real seal nonescape, requiring $E$ to be downstream of $R$ and $L$ and to expose no host order, quotient equality, selected limit, trichotomy, or completeness row;
\item componentwise transport, replay, provenance, and local naming, requiring $H,C,P,N$ to preserve and name exactly the same finite reverse-triangle packet.
\end{enumerate}
No reverse-triangle obligation is stored outside these finite rows.
\end{theorem}

\begin{theorem}[Regular Cauchy reverse-triangle RegSeqRat handoff]
\label{thm:regular-cauchy-reverse-triangle-regseqrat-handoff}
For every accepted $\RegularCauchyReverseTriangleUp$ carrier $T=(X,Y,S,A_X,A_Y,D,A_D,L,R,E,H,C,P,N)$, the $\RegSeqRatUp$ row $R$ is assembled only after the packet displays the source rows $X,Y$, the subtraction readback for $X-Y$, the absolute-value readbacks for $X,Y$, the subtraction readback for $|X|-|Y|$, the absolute-value readback for that difference, and the dyadic comparison ledger $L$. Thus a $\RegSeqRatUp$ consumer of the reverse-triangle estimate reads the finite route
$$
X,Y \longmapsto S,A_X,A_Y \longmapsto D,A_D \longmapsto L \longmapsto R
$$
through $\StreamNameUp$, $\DyadicAbsoluteValueUp$, $\RegularCauchySubtractionUp$, and $\RegularCauchyAbsUp$, with componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data.
\end{theorem}

\begin{theorem}[Regular Cauchy reverse-triangle Real seal boundary]
\label{thm:regular-cauchy-reverse-triangle-real-seal-boundary}
Every $\RealUp$ or $\RealAbsUp$ consumer of an accepted $\RegularCauchyReverseTriangleUp$ carrier reaches the seal row $E$ only after the $\RegSeqRatUp$ handoff $R$ and the comparison ledger $L$ have been displayed. The real-facing boundary exposes the finite source rows, subtraction routes, absolute-value routes, dyadic tolerance comparison ledger for $||x|-|y|| \leq |x-y|$, regular readback, componentwise transport, continuation replay, provenance, and local naming. It exposes no host real order, quotient Cauchy equality, selected limit, trichotomy witness, ambient completeness principle, Lean verification, or public bridge row.
\end{theorem}

\closureat{RegularCauchyReverseTriangleUp}{seedStr}

\begin{closurestatus}{\RegularCauchyReverseTriangleUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-reverse-triangle-carrier}, \autoref{thm:regular-cauchy-reverse-triangle-namecert-obligations}, \autoref{thm:regular-cauchy-reverse-triangle-regseqrat-handoff}, and \autoref{thm:regular-cauchy-reverse-triangle-real-seal-boundary} seed the finite reverse-triangle route through two $\RegSeqRatUp$ source rows, subtraction and absolute-value routes, dyadic comparison ledger, resulting readback, and $\RealUp$ seal data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real order, quotient Cauchy equality, selected limits, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, subtraction handoff, absolute-value handoff, comparison exactness, Real seal nonescape, componentwise transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyReverseTriangleUp.}
  \constructivestory{A $\RegularCauchyReverseTriangleUp$ packet is generated from two RegSeqRat source rows, finite StreamName windows, subtraction routes for $X-Y$ and $|X|-|Y|$, RegularCauchyAbs routes for $X$, $Y$, and $|X|-|Y|$, DyadicAbsoluteValue tolerance rows, a comparison ledger for $||x|-|y|| \leq |x-y|$, a RegSeqRat readback, a RealUp seal, componentwise transport, continuation replay, package provenance, and local naming.}
\end{closurestatus}
