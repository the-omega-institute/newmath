\chapter{A Concrete Naming Certificate for $\MetaCICCandidateResidualJoinBudgetUp$}
\label{ch:concrete-instances-metacic-candidate-residual-join-budget-namecert}
\origin{ai}

The $\MetaCICCandidateResidualJoinBudgetUp$ packet realizes the finite candidate-residual join-budget branch of \autoref{ch:metacic-confluence-audit-map}. It sits between the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}, and the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}. The packet records typed residual windows, candidate join budgets, closed-shape guards, obstruction retention, TasteGate-facing instance requirements, transport, replay, provenance, and local naming before a confluence-facing consumer may read a local join attempt.

\begin{definition}[MetaCIC candidate residual join budget carrier]
\label{def:metacic-candidate-residual-join-budget-carrier}
A $\MetaCICCandidateResidualJoinBudgetUp$ carrier is a finite $\BHist$ packet
$$
J=(T,R,B,Q,D,O,I,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row, naming the closed or shape-limited MetaCIC source window being inspected.
\item $R$ is the residual-substitution row, recording the residual frontier that remains visible before a candidate join can be consumed.
\item $B$ is the finite budget row, recording unary fuel, inspected critical-pair identifiers, and the local residual-join search bound.
\item $Q$ is the candidate-join row, naming the displayed common-reduct attempt and its budgeted replay trace.
\item $D$ is the closed-shape guard row, separating the admitted closed-normal or bounded-shape branch from the unclosed residual-substitution boundary.
\item $O$ is the obstruction row, retaining any missing residual-substitution compatibility, parallel-diamond input, or closed-confluence premise as visible data.
\item $I$ is the TasteGate instance row, stating that Lean-side gate discharge must provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for the candidate residual-budget surface.
\item $H$ records componentwise $\hsame$ transport for typed-window, residual-substitution, budget, candidate-join, guard, obstruction, instance, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from typed-window admission through residual inspection, budgeted candidate join, obstruction retention, and the instance row.
\item $P$ is the $\Pkg$ provenance over $\MetaCICCandidateResidualJoinBudgetUp$, $\MetaCICParallelDiamondFrontierUp$, $\MetaCICConfluenceAuditPacketUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICCandidateResidualJoinBudgetUp}$ row.
\end{enumerate}
The classifier accepts exactly packets whose displayed residual window, budget, candidate join, guard, obstruction, instance, transport, replay, provenance, and naming rows agree componentwise. It has no coordinate for global confluence, unrestricted residual-substitution compatibility, subject reduction, quotient syntax equality, evaluator equality, or a hidden join oracle.
\end{definition}

\begin{theorem}[MetaCIC candidate residual join budget NameCert obligations]
\label{thm:metacic-candidate-residual-join-budget-namecert-obligations}
For every accepted $\MetaCICCandidateResidualJoinBudgetUp$ carrier $J=(T,R,B,Q,D,O,I,H,C,P,N)$, the local $\NameCert_{\MetaCICCandidateResidualJoinBudgetUp}$ surface consists exactly of typed-window exposure through $T$, residual-substitution exposure through $R$, finite budget exposure through $B$, candidate-join exposure through $Q$, closed-shape guard exposure through $D$, obstruction retention through $O$, TasteGate instance disclosure through $I$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-residual-join-budget-carrier}. The confluence-facing rows are $T,R,B,Q,D,O,I$, and the structural rows are $H,C,P,N$.

The typed-window row $T$ and residual-substitution row $R$ are read before the finite budget $B$ and candidate join $Q$. Thus a local join attempt remains attached to the residual frontier named by the confluence audit map.

The guard row $D$ and obstruction row $O$ then keep the closed-shape branch separate from missing substitution-compatibility and parallel-diamond inputs. The instance row $I$ records the $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ requirements that a Lean TasteGate must discharge before the packet can be promoted beyond a candidate budget.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since no carrier coordinate exports global confluence, subject reduction, quotient syntax equality, evaluator equality, or a hidden join oracle, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[MetaCIC candidate residual join budget non-escape]
\label{thm:metacic-candidate-residual-join-budget-nonescape}
Every confluence-facing consumer of an accepted $\MetaCICCandidateResidualJoinBudgetUp$ packet receives the finite route
$$
T,R,B,Q,D,O,I,H,C,P,N .
$$
The route may be handed to $\MetaCICParallelDiamondFrontierUp$, $\MetaCICConfluenceAuditPacketUp$, and $\MetaCICResidualSubstitutionBoundaryUp$ consumers only as candidate residual-join budget evidence with the obstruction row still visible. It cannot be repacked as full confluence, subject reduction, unrestricted residual-substitution compatibility, quotient syntax equality, or evaluator equality.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:metacic-candidate-residual-join-budget-namecert-obligations}. They expose the typed window, residual frontier, finite budget, candidate join, guard, obstruction, instance row, and structural rows as one packet.

The candidate row $Q$ is downstream of both the residual row $R$ and budget row $B$. Therefore a consumer sees a budgeted join attempt, not an unrestricted common-reduct theorem.

The obstruction row $O$ is transported and replayed together with $D$ and $I$. Hence the handoff to the parallel-diamond frontier, confluence audit packet, or residual-substitution boundary preserves the missing-premise boundary and the TasteGate instance requirements.
\end{proof}

\falsifiablePrediction{Any $\MetaCICCandidateResidualJoinBudgetUp$ consumer that exports a candidate join while omitting the residual-substitution row, budget row, obstruction row, or $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ TasteGate instance requirements is rejected by the local classifier.}

\independenceWitness{Nearest siblings: MetaCICParallelDiamondFrontier, MetaCICConfluenceAuditPacket, MetaCICResidualSubstitutionBoundary. The packet is not bijective to any sibling because it binds typed residual windows, finite candidate-join budgets, closed-shape guards, obstruction retention, TasteGate instance requirements, transport, replay, provenance, and local naming into one confluence-audit object.}

\closureat{MetaCICCandidateResidualJoinBudgetUp}{seedStr}

\begin{closurestatus}{\MetaCICCandidateResidualJoinBudgetUp}
  \constructivestory{A $\MetaCICCandidateResidualJoinBudgetUp$ packet is generated from typed-window rows, residual-substitution rows, finite budget rows, candidate-join rows, closed-shape guards, obstruction rows, TasteGate instance requirements, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-candidate-residual-join-budget-carrier}, \autoref{thm:metacic-candidate-residual-join-budget-namecert-obligations}, and \autoref{thm:metacic-candidate-residual-join-budget-nonescape} seed the finite candidate residual-join budget route for the MetaCIC confluence audit map.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, full confluence, subject reduction, unrestricted residual-substitution compatibility, quotient syntax equality, evaluator equality, or hidden join selection for $\MetaCICCandidateResidualJoinBudgetUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, typed-window exposure, residual-substitution exposure, budget replay, candidate-join admission, closed-shape guard retention, obstruction retention, TasteGate instance disclosure, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICCandidateResidualJoinBudgetUp.}
\end{closurestatus}
