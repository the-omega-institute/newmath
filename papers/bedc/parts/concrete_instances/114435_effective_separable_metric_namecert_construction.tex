\chapter{A Concrete Naming Certificate for $\EffectiveSeparableMetricUp$}
\label{ch:concrete-instances-effective-separable-metric-namecert}
\origin{human}

Following Bishop-Bridges/Weihrauch-style computable-analysis practice, the $\EffectiveSeparableMetricUp$ packet records the finite BEDC surface by which a metric space is read with an explicit dense schedule and computable distance readback. It sits after the metric observation carrier of \autoref{ch:concrete-instances-metric-namecert}, the Real separability route of \autoref{ch:concrete-instances-real-separability-namecert}, the finite window schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives Real and Completion consumers a displayed dense witness without countable choice, quotient stream equality, selected representatives, host completeness, or an ambient separability theorem.

\begin{definition}[EffectiveSeparableMetric carrier]
\label{def:effective-separable-metric-carrier}
An $\EffectiveSeparableMetricUp$ carrier is a finite $\BHist$ packet
$$
E=(M,D,W,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ distance-read ledger, carrying the displayed point rows and finite distance observations.
\item $D$ is the dense schedule row, listing the finite approximation requests by which a consumer reads a named dense family.
\item $W$ is the $\StreamNameUp$ window row through which finite precision requests are made.
\item $R$ is the $\RegSeqRatUp$ readback row for the metric comparisons attached to those windows.
\item $Q$ is the terminal $\RealUp$ seal row reached only after $M,D,W,R$ have been exposed.
\item $H$ records componentwise $\hsame$ transport for $M,D,W,R,Q$ and the structural rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
M,D \longmapsto W,R \longmapsto Q .
$$
\item $P$ is the $\Pkg$ provenance over $\EffectiveSeparableMetricUp$, $\MetricUp$, $\RealSeparabilityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EffectiveSeparableMetricUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,D,W,R,Q,H,C,P,N$. It has no coordinate for host separability, choice-selected dense sequences, quotient stream equality, selected representatives, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[EffectiveSeparableMetric NameCert obligations]
\label{thm:effective-separable-metric-namecert-obligations}
For every accepted $\EffectiveSeparableMetricUp$ carrier
$$
E=(M,D,W,R,Q,H,C,P,N),
$$
the local $\NameCert_{\EffectiveSeparableMetricUp}$ surface consists exactly of carrier admission for the metric ledger $M$, dense schedule $D$, StreamName window $W$, RegSeqRat readback $R$, Real seal $Q$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$; componentwise classifier compatibility for those rows; dense-schedule exposure through $D$ before any separability read; metric readback through $M,W,R,Q$; and non-escape from host separability, choice-selected enumeration, quotient stream equality, selected representatives, and ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:effective-separable-metric-carrier}. The accepted packet exposes exactly the rows $M,D,W,R,Q,H,C,P,N$.

The dense witness is read through $D$ before any Real-facing observation is available. The metric ledger $M$ supplies only displayed point and distance observations, and the finite window row $W$ fixes the precision at which those observations are sampled.

The numerical readback is then forced through $R$ and the terminal Real seal $Q$. These rows provide regular rational comparisons and Real-facing sealing, but no quotient stream equality or selected representative.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since host separability, countable choice, selected dense sequences, quotient equality, selected representatives, and ambient completeness have no coordinate in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[EffectiveSeparableMetric L10 source handoff]
\label{thm:effective-separable-metric-l10-source-handoff}
Every L10 Real or Completion consumer of an accepted $\EffectiveSeparableMetricUp$ carrier factors through
$$
\MetricUp,\RealSeparabilityUp,\StreamNameUp,\RegSeqRatUp,\RealUp
$$
in the displayed order. The handoff may export metric distance observations, an explicit dense schedule, finite StreamName windows, regular rational readback, and a Real seal, but it cannot export a host separability theorem, selected dense sequence, quotient stream equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:effective-separable-metric-namecert-obligations}. They require the dense schedule and metric ledger to be visible before a consumer reads any windowed numerical data.

The L10 read then consumes StreamName windows through $W$ and RegSeqRat readback through $R$. The Real seal $Q$ is downstream of those finite rows, so a Real or Completion route receives scheduled metric data rather than an unstructured separability assertion.

The structural rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and local naming. Because the carrier lacks choice, quotient, representative-selection, and ambient-completeness coordinates, the handoff remains the finite source route stated above.
\end{proof}

\falsifiablePrediction{Every accepted $\EffectiveSeparableMetricUp$ L10 read exposes a MetricUp ledger, an explicit dense schedule, StreamNameUp windows, RegSeqRatUp readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row before it can serve a Real or Completion consumer.}

\independenceWitness{metric, real-separability, streamname, regseqrat, real: none is bijective with $\EffectiveSeparableMetricUp$ because this packet jointly carries the metric ledger, dense schedule, finite windows, regular rational readback, Real seal, transport, replay, provenance, and local naming rows.}

\closureat{EffectiveSeparableMetricUp}{seedStr}

\begin{closurestatus}{\EffectiveSeparableMetricUp}
  \constructivestory{An $\EffectiveSeparableMetricUp$ packet is generated from a MetricUp ledger, an explicit dense schedule, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:effective-separable-metric-carrier}, \autoref{thm:effective-separable-metric-namecert-obligations}, and \autoref{thm:effective-separable-metric-l10-source-handoff} seed the effective separable metric source route over $\MetricUp$, $\RealSeparabilityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full separability, countable choice, quotient stream equality, selected representatives, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-schedule exposure, metric readback, Real-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.EffectiveSeparableMetricUp.}
\end{closurestatus}
