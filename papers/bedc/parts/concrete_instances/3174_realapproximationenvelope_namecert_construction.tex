\chapter{A Concrete Naming Certificate for $\RealApproximationEnvelopeUp$}
\label{ch:concrete-instances-realapproximationenvelope-namecert}
\origin{human}

The $\RealApproximationEnvelopeUp$ interface names the finite observation packet used by Real and Completion consumers before any completed real value is available. It packages a tolerance request, a finite stream window, a dyadic observation, a regular-sequence classifier row, and a $\RealUp$ seal handoff into one local $\NameCert$ surface. The packet is designed for diagonal representatives, stationary rational finite windows, Cauchy-modulus extraction, and RealUp classifier handoffs without importing host real equality, quotient stream equality, selected limits, or ambient completeness.

The envelope consumes the finite-window route of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic observation ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular-rational classifier of \autoref{ch:concrete-instances-regseqrat-namecert}, and the seal surface of \autoref{ch:concrete-instances-real-namecert}. These sibling surfaces are the rows $W,D,R,S$ of the carrier below, and the edge is a finite observation-envelope route rather than an independent completed real.

\begin{definition}[Real approximation-envelope carrier]
\label{def:real-approximation-envelope-carrier}
A $\RealApproximationEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $\varepsilon$ is the tolerance request row naming the precision scale to be answered by the envelope;
\item $W$ is the $\StreamNameUp$ finite-window row selected for that tolerance request;
\item $D$ is the $\DyadicRatCoreUp$ observation row read through $W$, including the displayed endpoint, radius, and finite arithmetic ledger;
\item $R$ is the $\RegSeqRatUp$ classifier row that reads the same window as a regular rational observation;
\item $S$ is the $\RealUp$ seal handoff row receiving the observation packet for downstream real-name consumers;
\item $H$ records componentwise $\hsame$ transports for the tolerance request, window, dyadic observation, classifier row, seal row, provenance, and local name;
\item $C$ records the displayed $\Cont$ routes from tolerance request to window, from window to dyadic observation, from dyadic observation to classifier row, and from classifier row to seal handoff;
\item $P$ is the $\Pkg$ provenance over $\BHist$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RealApproximationEnvelopeUp}$ row.
\end{enumerate}
The carrier stores no host real equality, no quotient stream equality, no selected limit, no completed-real inverse law, and no ambient completeness premise.
\end{definition}

\begin{theorem}[Real approximation-envelope NameCert obligations]
\label{thm:real-approximation-envelope-namecert-obligations}
The local $\NameCert_{\RealApproximationEnvelopeUp}$ surface for \autoref{def:real-approximation-envelope-carrier} consists exactly of tolerance-request admission, selected $\StreamNameUp$ window admission, $\DyadicRatCoreUp$ observation admission, $\RegSeqRatUp$ classifier readback, $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ route replay, $\Pkg$ provenance, and the local naming row.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet $E$. Its rows list the tolerance request, finite window, dyadic observation ledger, regular rational classifier row, RealUp seal handoff, transport witnesses, continuation routes, provenance, and local certificate.

The observation side first reads $\varepsilon$ and then follows the continuation route to $W$. The selected window exposes only finite stream data, and $D$ records the dyadic endpoint, radius, and arithmetic ledger obtained from that window.

The classifier side routes the same finite read through $R$ and then to the seal row $S$. The stored $\hsame$ and $\Cont$ rows ensure that the classifier handoff is tied to the same tolerance request and window, while $P$ and $N$ record the package provenance and local name.

No remaining coordinate can supply host real equality, quotient stream equality, selected limits, completed-real inverse laws, or ambient completeness. The obligations therefore exhaust the finite Real-and-Completion observation envelope.
\end{proof}
\leanchecked{BEDC.Derived.RealApproximationEnvelopeUp.RealApproximationEnvelopePacket\_namecert\_obligations}

\begin{theorem}[Real approximation-envelope tolerance-window admission]
\label{thm:real-approximation-envelope-tolerance-window-admission}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. Each accepted tolerance request in the row $\varepsilon$ reaches the selected $\StreamNameUp$ finite window $W$ through the same finite support rows $H,C,P,N$, and the admission path uses no stream coordinate outside the window row displayed in $E$.
\end{theorem}
\begin{proof}
Project the rows of $E$ from \autoref{def:real-approximation-envelope-carrier}. The row $\varepsilon$ stores the tolerance request, $W$ stores the selected $\StreamNameUp$ finite window, $H$ stores the componentwise $\hsame$ transport, $C$ stores the displayed $\Cont$ route, $P$ stores the $\Pkg$ provenance, and $N$ stores the local $\NameCert$ row.

Apply \autoref{thm:real-approximation-envelope-namecert-obligations}. Its first two obligations admit the tolerance request and then the selected finite window for the same local packet, while the transport, route, provenance, and naming obligations keep the path inside $H,C,P,N$.

Since the carrier lists no second stream read and no ambient stream equality, the admitted tolerance request can reach a consumer-visible stream observation only through $W$. Thus every accepted tolerance request reaches the selected finite window through the displayed rows $\varepsilon,W,H,C,P,N$.
\end{proof}

\begin{theorem}[Real approximation-envelope dyadic observation soundness]
\label{thm:real-approximation-envelope-dyadic-observation-soundness}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The selected finite window $W$ determines the $\DyadicRatCoreUp$ observation row $D$ and the $\RegSeqRatUp$ classifier row $R$ along the displayed route
$$
\begin{aligned}
W &\longrightarrow D,\\
D &\longrightarrow R.
\end{aligned}
$$
The determination is internal to the support rows $H,C,P,N$ and does not use quotient stream equality, host real equality, or a selected limit.
\end{theorem}
\leanchecked{BEDC.Derived.RealApproximationEnvelopeUp.RealApproximationEnvelope\_dyadic\_observation\_soundness}
\begin{proof}
Start with the window admission theorem \autoref{thm:real-approximation-envelope-tolerance-window-admission}. It places the accepted tolerance request at the selected $\StreamNameUp$ row $W$ through the local support rows of the envelope.

The NameCert obligations of \autoref{thm:real-approximation-envelope-namecert-obligations} then admit the $\DyadicRatCoreUp$ observation row $D$ and the $\RegSeqRatUp$ classifier row $R$ for the same packet. The continuation ledger $C$ records the route from $W$ to $D$ and from $D$ to $R$, while $H,P,N$ keep the route attached to the same finite carrier.

No coordinate of \autoref{def:real-approximation-envelope-carrier} records quotient stream equality, host real equality, or a selected limit. The dyadic observation and classifier row are therefore sound exactly as the displayed $W$ to $D$ to $R$ observation route.
\end{proof}

\begin{theorem}[Real approximation-envelope seal ledger exactness]
\label{thm:real-approximation-envelope-seal-ledger-exactness}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The $\RealUp$ seal row $S$ is admissible exactly as the downstream ledger endpoint of the finite route
$$
\varepsilon \longrightarrow W \longrightarrow D \longrightarrow R \longrightarrow S
$$
with support rows $H,C,P,N$. It exports no host real equality, quotient stream equality, selected limit, completed-real inverse law, or ambient completeness principle.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-approximation-envelope-tolerance-window-admission} and \autoref{thm:real-approximation-envelope-dyadic-observation-soundness}. These identify the admitted route from the tolerance request $\varepsilon$ through the selected window $W$, the $\DyadicRatCoreUp$ observation row $D$, and the $\RegSeqRatUp$ classifier row $R$.

Apply \autoref{thm:real-approximation-envelope-namecert-obligations} to the downstream row. It admits the $\RealUp$ seal handoff and records, through $C$, that the seal is reached after the classifier row for the same carrier, with $H,P,N$ preserving transport, provenance, and local naming.

The carrier definition lists the seal as a row of the finite packet rather than as a completed real value or an ambient equality principle. Since the negative boundary excludes host real equality, quotient stream equality, selected limits, completed-real inverse laws, and ambient completeness, the row $S$ is exact only as the endpoint of the displayed finite ledger.
\end{proof}

\begin{theorem}[Real approximation-envelope consumer boundary]
\label{thm:real-approximation-envelope-consumer-boundary}
Every diagonal-representative, stationary rational finite-window, Cauchy-modulus, or $\RealUp$ classifier consumer of an accepted $\RealApproximationEnvelopeUp$ packet must read the rows
$$
\varepsilon,W,D,R,S,H,C,P,N.
$$
Such a consumer may inspect the selected $\StreamNameUp$ window, the $\DyadicRatCoreUp$ observation, the $\RegSeqRatUp$ classifier row, and the $\RealUp$ seal handoff, but it cannot replace that route by host real equality, quotient stream equality, a selected limit, a completed-real inverse law, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:real-approximation-envelope-namecert-obligations}. They force every observation request to pass from $\varepsilon$ through the selected window $W$ before any dyadic or regular rational data is consumed.

Diagonal representatives and stationary rational finite-window consumers read the finite window side of the packet. The dyadic ledger $D$ records the observed endpoint data, and the classifier row $R$ records the regular rational readback for the same selected window.

Cauchy-modulus and $\RealUp$ classifier consumers then reach the real-facing boundary through $S$. The transport, continuation, provenance, and local naming rows keep the seal attached to the same finite observation route.

The carrier inventory is exhausted by the displayed rows. Since none of them is host real equality, quotient stream equality, a selected limit, a completed-real inverse law, or an ambient completeness theorem, no accepting consumer can export such data.
\end{proof}

\begin{theorem}[Real approximation-envelope window readback totality]
\label{thm:real-approximation-envelope-window-readback-totality}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. Every accepted tolerance request in $E$ reaches the selected $\StreamNameUp$ finite window by the displayed route through $\varepsilon$, $W$, $H$, $C$, $P$, and $N$, and the readback uses no stream coordinate except those recorded in that finite window row.
\end{theorem}
\leanchecked{BEDC.Derived.RealApproximationEnvelopeUp.RealApproximationEnvelope\_window\_readback\_totality}
\begin{proof}
The carrier definition in \autoref{def:real-approximation-envelope-carrier} displays $\varepsilon$ as the tolerance request row, $W$ as the selected $\StreamNameUp$ finite-window row, $H$ as the componentwise transport ledger, $C$ as the continuation route ledger, $P$ as provenance, and $N$ as the local certificate.

Apply the NameCert obligation inventory of \autoref{thm:real-approximation-envelope-namecert-obligations}. It requires tolerance-request admission, selected $\StreamNameUp$ window admission, componentwise $\hsame$ transport, displayed $\Cont$ route replay, provenance, and local naming for the same packet $E$.

Finally apply the consumer boundary in \autoref{thm:real-approximation-envelope-consumer-boundary}. A consumer may inspect the selected $\StreamNameUp$ window but cannot replace that finite route by unrecorded stream access, so the readback is total exactly along the displayed envelope rows.
\end{proof}

\begin{theorem}[Real approximation-envelope dyadic classifier handoff]
\label{thm:real-approximation-envelope-dyadic-classifier-handoff}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The selected $\StreamNameUp$ finite window $W$ determines the $\DyadicRatCoreUp$ observation row $D$, the $\RegSeqRatUp$ classifier row $R$, and the $\RealUp$ seal row $S$ along the displayed continuation route
$$
\begin{aligned}
\varepsilon &\longrightarrow W,\\
W &\longrightarrow D,\\
D &\longrightarrow R,\\
R &\longrightarrow S.
\end{aligned}
$$
\end{theorem}
\leanchecked{BEDC.Derived.RealApproximationEnvelopeUp.RealApproximationEnvelopeDyadicClassifierHandoff}
\begin{proof}
By \autoref{def:real-approximation-envelope-carrier}, the packet stores the $\StreamNameUp$ row $W$, the $\DyadicRatCoreUp$ row $D$, the $\RegSeqRatUp$ row $R$, the $\RealUp$ row $S$, and the continuation ledger $C$ in one finite $\BHist$ carrier.

The NameCert obligations in \autoref{thm:real-approximation-envelope-namecert-obligations} bind these rows as a single route: tolerance request to selected window, selected window to dyadic observation, dyadic observation to classifier row, and classifier row to seal handoff.

The boundary theorem \autoref{thm:real-approximation-envelope-consumer-boundary} then fixes the consumer-visible handoff. Any diagonal-representative, stationary rational finite-window, Cauchy-modulus, or $\RealUp$ classifier consumer sees the same $W,D,R,S$ chain, so the dyadic, classifier, and seal rows are the displayed continuation of the selected window.
\end{proof}

\begin{theorem}[Real approximation-envelope transport non-escape]
\label{thm:real-approximation-envelope-transport-nonescape}
For an accepted $\RealApproximationEnvelopeUp$ carrier
$$
E=(\varepsilon,W,D,R,S,H,C,P,N),
$$
componentwise $\hsame$ transport of $E$ preserves the tolerance request $\varepsilon$, the selected $\StreamNameUp$ finite window $W$, the $\DyadicRatCoreUp$ observation row $D$, the $\RegSeqRatUp$ classifier row $R$, and the $\RealUp$ seal handoff $S$. The transported packet exports no host real equality, quotient stream equality, selected limit, completed-real inverse law, or ambient completeness principle.
\end{theorem}
\begin{proof}
The transport coordinates are part of the carrier itself by \autoref{def:real-approximation-envelope-carrier}: $H$ records componentwise $\hsame$ transports for the tolerance request, window, dyadic observation, classifier row, seal row, provenance, and local name.

Use \autoref{thm:real-approximation-envelope-namecert-obligations} to replay those transport rows together with the displayed $\Cont$ routes and $\Pkg$ provenance. This keeps the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces attached to the same finite envelope packet after transport.

The non-escape claim is exactly the negative boundary of \autoref{thm:real-approximation-envelope-consumer-boundary}. Since every accepted consumer must read the displayed rows of $E$, and since those rows do not contain host real equality, quotient stream equality, selected limits, completed-real inverse laws, or ambient completeness, componentwise transport cannot export any such object.
\end{proof}

\begin{theorem}[Real approximation-envelope classifier handoff]
\label{thm:real-approximation-envelope-classifier-handoff}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The accepted packet routes the tolerance request $\varepsilon$ and the selected $\StreamNameUp$ finite window $W$ through the $\DyadicRatCoreUp$ observation row $D$ into the $\RegSeqRatUp$ classifier row $R$, using only the displayed rows $\varepsilon,W,D,R,H,C,P,N$ of the same finite packet.
\end{theorem}
\begin{proof}
Project the carrier coordinates from \autoref{def:real-approximation-envelope-carrier}. The packet displays the tolerance request, selected window, dyadic observation, classifier row, componentwise transport ledger, continuation ledger, provenance, and local certificate in one finite $\BHist$ object.

Apply the NameCert obligations in \autoref{thm:real-approximation-envelope-namecert-obligations}. They require admission of $\varepsilon$, $W$, $D$, and $R$ together with replay of the displayed $\Cont$ routes for the same local certificate.

The route is therefore the finite chain $\varepsilon$ to $W$, $W$ to $D$, and $D$ to $R$. No coordinate outside $\varepsilon,W,D,R,H,C,P,N$ is needed to hand the selected observation to the regular-rational classifier.
\end{proof}

\begin{theorem}[Real approximation-envelope seal routing]
\label{thm:real-approximation-envelope-seal-routing}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The $\RealUp$ seal row $S$ is reachable only after the classifier row $R$ for the same finite observation route, and this seal exports no selected limit, quotient stream equality, host real equality, completed-real inverse law, or ambient completeness premise.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-approximation-envelope-classifier-handoff}. It fixes the observation route from the tolerance request through the selected $\StreamNameUp$ window and $\DyadicRatCoreUp$ row into the $\RegSeqRatUp$ classifier row $R$.

The carrier and NameCert obligations then place the seal row after the classifier row by the displayed continuation route $R$ to $S$. Thus any accepted access to $S$ is tied to the same finite observation chain already used by the classifier.

Finally apply the consumer boundary theorem \autoref{thm:real-approximation-envelope-consumer-boundary}. Since accepted consumers may inspect the seal handoff but cannot replace the finite route by selected limits, quotient stream equality, host real equality, completed-real inverse laws, or ambient completeness, the seal row exports none of those objects.
\end{proof}

\begin{theorem}[Real approximation-envelope seal-classifier compatibility]
\label{thm:real-approximation-envelope-seal-classifier-compatibility}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The $\RegSeqRatUp$ classifier handoff row $R$ and the $\RealUp$ seal row $S$ are compatible exactly when both are read through the same displayed finite route
$$
\varepsilon \longrightarrow W \longrightarrow D \longrightarrow R \longrightarrow S
$$
and through the same support rows $H,C,P,N$. No second classifier, selected limit, quotient stream equality, host real equality, or ambient completeness coordinate may mediate the seal.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-approximation-envelope-classifier-handoff}. It identifies the unique classifier-facing route from the tolerance request $\varepsilon$ through the selected $\StreamNameUp$ window $W$ and the $\DyadicRatCoreUp$ observation row $D$ into the $\RegSeqRatUp$ classifier row $R$.

Apply \autoref{thm:real-approximation-envelope-seal-routing}. The seal-facing route reaches the $\RealUp$ row $S$ only after the already displayed classifier row $R$, so the classifier handoff and the seal handoff share the same finite boundary
$$
\varepsilon \longrightarrow W \longrightarrow D \longrightarrow R \longrightarrow S.
$$

Finally use \autoref{thm:real-approximation-envelope-transport-nonescape} and \autoref{thm:real-approximation-envelope-consumer-boundary}. These exclude selected limits, quotient stream equality, host real equality, completed-real inverse laws, ambient completeness, and unrecorded consumer coordinates, leaving only the displayed route together with $H,C,P,N$ as the finite packet boundary.
\end{proof}

\begin{theorem}[Real approximation-envelope window-classifier exactness]
\label{thm:real-approximation-envelope-window-classifier-exactness}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The finite window row $W$ is the unique public route by which the dyadic observation row $D$, the $\RegSeqRatUp$ classifier row $R$, and the $\RealUp$ seal row $S$ are exported. Every accepted classifier or seal read factors through the displayed chain
$$
\varepsilon \longrightarrow W \longrightarrow D \longrightarrow R \longrightarrow S
$$
together with the support rows $H,C,P,N$, and no selected limit, quotient stream equality, host real equality, completed-real inverse law, or ambient completeness premise can replace that route.
\end{theorem}
\leanchecked{BEDC.Derived.RealApproximationEnvelopeUp.RealApproximationEnvelope\_window\_classifier\_exactness}
\begin{proof}
The carrier definition \autoref{def:real-approximation-envelope-carrier} displays exactly the rows $\varepsilon,W,D,R,S,H,C,P,N$. Thus any public read of a dyadic observation, classifier row, or seal row must be a read of one of the named coordinates of the same finite $\BHist$ packet.

Apply \autoref{thm:real-approximation-envelope-window-readback-totality} to the first step. It makes $W$ the total finite-window readback for the tolerance request $\varepsilon$ and excludes unrecorded stream coordinates from that read.

Next apply \autoref{thm:real-approximation-envelope-dyadic-classifier-handoff} and \autoref{thm:real-approximation-envelope-classifier-handoff}. These identify the dyadic observation row $D$ and the classifier row $R$ as the displayed continuation of the same selected window, with only $H,C,P,N$ available as support.

Apply \autoref{thm:real-approximation-envelope-seal-routing} for the real-facing end of the route. The seal row $S$ is reached only after the classifier row $R$, so a seal read cannot bypass the window, dyadic observation, or classifier handoff.

Finally use \autoref{thm:real-approximation-envelope-consumer-boundary}. It rules out selected limits, quotient stream equality, host real equality, completed-real inverse laws, and ambient completeness as replacement routes, leaving the displayed finite chain as the exact public export path.
\end{proof}

\begin{theorem}[Real approximation-envelope regularity propagation]
\label{thm:real-approximation-envelope-regularity-propagation}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The $\RegSeqRatUp$ classifier row $R$ propagates regularity to the $\RealUp$ seal row $S$ exactly along the finite envelope route
$$
\varepsilon \longrightarrow W \longrightarrow D \longrightarrow R \longrightarrow S
$$
with support rows $H,C,P,N$. No selected limit, quotient stream equality, host real equality, completed-real inverse law, ambient completeness premise, or second classifier mediates this propagation.
\end{theorem}
\begin{proof}
The carrier definition \autoref{def:real-approximation-envelope-carrier} gives the only available rows of an accepted envelope. Thus a regularity read starting at $R$ can only move through coordinates already present in $E$.

Apply \autoref{thm:real-approximation-envelope-dyadic-observation-soundness} and \autoref{thm:real-approximation-envelope-classifier-handoff}. They place the $\RegSeqRatUp$ classifier row after the tolerance request, finite window, and dyadic observation rows, so the regularity obligation attached to $R$ is already tied to the route $\varepsilon \to W \to D \to R$.

Apply \autoref{thm:real-approximation-envelope-seal-classifier-compatibility} and \autoref{thm:real-approximation-envelope-window-classifier-exactness}. These force the real seal row $S$ to be read after the same classifier row and rule out every route that bypasses the finite window, dyadic observation, classifier handoff, and support rows $H,C,P,N$.

The sibling anchors \texttt{RegSeqRatStreamCarrier\_regularit\_obligation\_surface} and \texttt{RegSeqRatStreamCarrier\_real\_seal\_handoff} name the regularity and seal faces that this envelope exposes. The preceding exactness results show that those faces meet only through the displayed finite chain, not through a selected limit, stream quotient, host equality, completed-real inverse law, ambient completeness premise, or second classifier.
\end{proof}

\begin{theorem}[Real approximation-envelope obligation coverage]
\label{thm:real-approximation-envelope-obligation-coverage}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The carrier definition and the obligation theorems discharge exactly the seven obligation faces for the envelope package: carrier admission, finite-window readback, dyadic observation soundness, $\RegSeqRatUp$ classifier handoff, $\RealUp$ seal routing, componentwise $\hsame$ transport stability, and consumer non-escape. The discharged package is the finite route
$$
\varepsilon \longrightarrow W \longrightarrow D \longrightarrow R \longrightarrow S
$$
with support rows $H,C,P,N$, and it claims no selected limit, quotient stream equality, host real equality, completed-real inverse law, ambient completeness premise, Lean verification, or standard bridge.
\end{theorem}
\begin{proof}
Carrier admission is the displayed inventory of \autoref{def:real-approximation-envelope-carrier}. It supplies exactly the tolerance request, finite window, dyadic observation row, classifier row, seal row, componentwise transport ledger, continuation ledger, provenance row, and local certificate row.

Finite-window readback is \autoref{thm:real-approximation-envelope-window-readback-totality}. Dyadic observation soundness and the classifier-facing handoff are supplied by \autoref{thm:real-approximation-envelope-dyadic-classifier-handoff} and \autoref{thm:real-approximation-envelope-classifier-handoff}; together they force the selected $\StreamNameUp$ row $W$ to feed $D$ and then $R$ inside the same finite packet.

The real-facing endpoint is \autoref{thm:real-approximation-envelope-seal-routing}. It places the $\RealUp$ seal row $S$ after the $\RegSeqRatUp$ classifier row $R$ and prevents the seal from becoming an independent completed-real object.

Transport stability is exactly the componentwise $\hsame$ boundary of \autoref{thm:real-approximation-envelope-transport-nonescape}. The final consumer boundary is \autoref{thm:real-approximation-envelope-window-classifier-exactness}: every accepted classifier or seal read factors through the same finite window route, and no unrecorded equality, selected limit, inverse law, or completeness principle can replace it.
\end{proof}

\begin{theorem}[Real approximation-envelope primitive scope closure]
\label{thm:real-approximation-envelope-primitive-scope-closure}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. The seven obligation faces of \autoref{thm:real-approximation-envelope-obligation-coverage} are scoped exactly by the primitive rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, together with the four sibling surfaces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. Every tolerance-window read, dyadic observation, classifier handoff, seal route, componentwise transport, and consumer read factors through those rows. The package cannot use selected limits, quotient stream equality, host real equality, completed-real inverse laws, ambient completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
The carrier inventory is \autoref{def:real-approximation-envelope-carrier}. It exposes only the tolerance request $\varepsilon$, finite window $W$, dyadic observation $D$, classifier row $R$, seal row $S$, componentwise transport ledger $H$, continuation ledger $C$, provenance row $P$, and local certificate row $N$.

Admission is supplied by \autoref{thm:real-approximation-envelope-namecert-obligations}. Thus the available primitive rows are exactly the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, with the sibling surfaces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ named by the coordinates of $E$.

The route from tolerance to seal is forced by \autoref{thm:real-approximation-envelope-window-readback-totality}, \autoref{thm:real-approximation-envelope-dyadic-classifier-handoff}, \autoref{thm:real-approximation-envelope-seal-routing}, and \autoref{thm:real-approximation-envelope-seal-classifier-compatibility}. These results identify the finite chain $\varepsilon \to W \to D \to R \to S$ as the only route available for tolerance-window reads, dyadic observations, classifier handoffs, and seal reads.

Transport cannot leave that primitive scope by \autoref{thm:real-approximation-envelope-transport-nonescape}. Finally, \autoref{thm:real-approximation-envelope-obligation-coverage} repacks the carrier, admission, route, transport, and consumer faces into the seven-face package, with selected limits, quotient stream equality, host real equality, completed-real inverse laws, ambient completeness, Lean verification, and bridge checking outside the claim.
\end{proof}

\begin{theorem}[Real approximation-envelope four-face public export]
\label{thm:real-approximation-envelope-four-face-public-export}
Let
$$
E=(\varepsilon,W,D,R,S,H,C,P,N)
$$
be an accepted $\RealApproximationEnvelopeUp$ carrier. Its public Real-and-Completion export is exactly the four-face packet
$$
\StreamNameUp(W) \longrightarrow \DyadicRatCoreUp(D) \longrightarrow \RegSeqRatUp(R) \longrightarrow \RealUp(S)
$$
over the tolerance request $\varepsilon$, the componentwise $\hsame$ rows $H$, the displayed $\Cont$ routes $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\RealApproximationEnvelopeUp}$ row $N$. Every downstream diagonal-representative, stationary-window, Cauchy-modulus, Real-name, or Completion consumer reaches the L10 phase boundary through those rows, and no quotient stream, host real equality, selected limit, ambient completeness, metric-completeness, arithmetic/order package, or standard bridge row is exported by the envelope.
\end{theorem}
\begin{proof}
Project the accepted carrier by \autoref{def:real-approximation-envelope-carrier}. The only public face rows in the packet are the selected $\StreamNameUp$ window $W$, the $\DyadicRatCoreUp$ observation $D$, the $\RegSeqRatUp$ classifier $R$, and the $\RealUp$ seal $S$, with $\varepsilon,H,C,P,N$ supplying the tolerance, transport, route, provenance, and local certificate support.

Apply \autoref{thm:real-approximation-envelope-namecert-obligations}. It admits precisely those face rows and the support rows for the same local $\NameCert$ surface, so the export cannot acquire a detached quotient stream, host equality, selected limit, completeness witness, arithmetic/order package, or bridge coordinate.

Next apply \autoref{thm:real-approximation-envelope-window-classifier-exactness} and \autoref{thm:real-approximation-envelope-obligation-coverage}. These force every classifier or seal read to factor through the finite route from $\varepsilon$ to $W$, then to $D$, then to $R$, and finally to $S$, while preserving the componentwise $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The consumer boundary of \autoref{thm:real-approximation-envelope-consumer-boundary} and the primitive scope closure of \autoref{thm:real-approximation-envelope-primitive-scope-closure} place the same route at the public edge of the chapter. Downstream, \autoref{thm:real-completion-exact-boundary-four-face-exactness} and \autoref{thm:real-completion-exact-boundary-terminal-route-pullback} are the Real L10 four-face exactness and pullback boundary: they consume only the corresponding StreamName, DyadicRatCore, RegSeqRat, and Real faces through finite structural rows. Thus the envelope exports exactly the displayed four-face packet to that boundary and nothing outside it.
\end{proof}

\closureat{RealApproximationEnvelopeUp}{scopedStr}

\begin{closurestatus}{\RealApproximationEnvelopeUp}
  \constructivestory{A $\RealApproximationEnvelopeUp$ packet is generated from a tolerance request, selected $\StreamNameUp$ finite window, $\DyadicRatCoreUp$ observation row, $\RegSeqRatUp$ classifier row, $\RealUp$ seal handoff, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:real-approximation-envelope-carrier}, \autoref{thm:real-approximation-envelope-namecert-obligations}, \autoref{thm:real-approximation-envelope-obligation-coverage}, \autoref{thm:real-approximation-envelope-primitive-scope-closure}, and \autoref{thm:real-approximation-envelope-four-face-public-export} bind the primitive finite envelope from tolerance requests through StreamNameUp windows, DyadicRatCore observations, RegSeqRat classifier rows, RealUp seal handoffs, and the public Real-and-Completion export boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close RealUp completeness, uniqueness of completed limits, quotient stream equality, host real equality, completed-real inverse laws, Lean verification, or bridge checking.}
  \upgradepath{The remaining route to a higher closure grade is Lean verification and bridge checking for the finite StreamNameUp, DyadicRatCoreUp, RegSeqRatUp, and RealUp envelope export.}
\end{closurestatus}
