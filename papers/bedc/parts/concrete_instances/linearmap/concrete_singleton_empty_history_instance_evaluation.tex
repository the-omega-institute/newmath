\begin{theorem}[Singleton linear map composition associativity empty classifier]
\label{thm:linearmap-singleton-comp-assoc-empty-classifier}
For singleton linear map codes $f,g,h$, if each code lies in
$L_{\emp}$, then both associations of the composite code classify with
$\emp$, and the two associated composite codes classify with each other:
$$
\lambda_{\emp}(\mathsf{comp}_{\emp}(h,\mathsf{comp}_{\emp}(g,f)),\emp)
\land
\lambda_{\emp}(\mathsf{comp}_{\emp}(\mathsf{comp}_{\emp}(h,g),f),\emp)
\land
\lambda_{\emp}(\mathsf{comp}_{\emp}(h,\mathsf{comp}_{\emp}(g,f)),
  \mathsf{comp}_{\emp}(\mathsf{comp}_{\emp}(h,g),f)).
$$
\end{theorem}
\begin{proof}
The singleton composition code is definitionally $\emp$ for every pair of
inputs. Therefore both associated composites reduce to $\emp$, and each
displayed classifier is the reflexive singleton map-code classifier at the
empty history.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingleton\_comp\_assoc\_empty\_classifier}

\begin{theorem}[Singleton linear-map classifier append left cancellation]
\label{thm:singleton-linearmap-classifier-append-left-cancel}
For all $P,Q,R:\mathsf{BHist}$, if $L_{\emp}(P)$, then
$$
\lambda_{\emp}(\mathsf{append}(P,Q),\mathsf{append}(P,R))
\Longleftrightarrow
\lambda_{\emp}(Q,R).
$$
\end{theorem}
\begin{proof}
For the forward direction, unfold the singleton classifier. The carrier
clauses say that both appended histories are classified with $\emp$.
Applying the empty-append inversion to each carrier clause gives
$Q\hsame\emp$ and $R\hsame\emp$. These two endpoint witnesses compose, with
symmetry on the right one, to give $Q\hsame R$.

For the reverse direction, the hypothesis $L_{\emp}(P)$ identifies the left
prefix with $\emp$. The two appended histories therefore reduce through the
left-empty continuation law to $Q$ and $R$. The carrier clauses and classifier
comparison supplied by $\lambda_{\emp}(Q,R)$ transport along those reductions
to rebuild the prefixed singleton classifier.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonClassifier\_append\_left\_cancel\_iff}

\begin{theorem}[Singleton linear-map classifier append right cancellation]
\label{thm:linearmap-singleton-classifier-append-right-cancel-iff}
For all $P,Q,R:\mathsf{BHist}$, if $L_{\emp}(P)$, then
$$
\lambda_{\emp}(\mathsf{append}(Q,P),\mathsf{append}(R,P))
\Longleftrightarrow
\lambda_{\emp}(Q,R).
$$
\end{theorem}
\begin{proof}
For the forward direction, unfold the singleton classifier. The carrier
clauses classify both right-suffixed histories with $\emp$. Empty-append
inversion splits these clauses and gives $Q\hsame\emp$ and $R\hsame\emp$.
Composing the first endpoint witness with the symmetry of the second gives
$Q\hsame R$, which closes $\lambda_{\emp}(Q,R)$.

Conversely, the hypothesis $L_{\emp}(P)$ supplies the certified empty suffix.
Together with the two carrier fields of $\lambda_{\emp}(Q,R)$, empty-append
composition classifies both $\mathsf{append}(Q,P)$ and
$\mathsf{append}(R,P)$ with $\emp$. These two endpoint witnesses compose
through symmetry to rebuild the right-suffixed classifier.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonClassifier\_append\_right\_cancel\_iff}

\begin{theorem}[Singleton linear-map classifier append two-sided cancellation]
\label{thm:linearmap-singleton-classifier-append-two-sided-cancel}
For all $P,Q,R,S:\mathsf{BHist}$, if $L_{\emp}(P)$ and $L_{\emp}(S)$, then
$$
\lambda_{\emp}(\mathsf{append}(P,Q),\mathsf{append}(R,S))
\Longleftrightarrow
\lambda_{\emp}(Q,R).
$$
\end{theorem}
\begin{proof}
For the forward direction, unfold the singleton classifier. The two carrier
clauses classify $\mathsf{append}(P,Q)$ and $\mathsf{append}(R,S)$ with
$\emp$. Empty-append inversion gives $Q\hsame\emp$ and $R\hsame\emp$, and
these endpoint witnesses compose to close $\lambda_{\emp}(Q,R)$.

Conversely, $L_{\emp}(P)$ and $L_{\emp}(S)$ combine with the carrier fields of
$\lambda_{\emp}(Q,R)$ to classify the two appended histories with $\emp$.
The two resulting endpoint witnesses compose, with symmetry on the right, to
rebuild the two-sided appended classifier.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonClassifier\_append\_two\_sided\_cancel\_iff}

\begin{theorem}[Singleton evaluation append-input classifier readback]
\label{thm:linearmap-singleton-eval-append-input-classifier}
For histories $f,x,y,h$, classifying the singleton evaluation of an appended
input against $h$ is exactly the singleton carrier condition on $h$:
$$
\mathsf{LinearMapSingletonClassifier}
  (\mathsf{LinearMapSingletonEval}(f,x\cdot y),h)
\Longleftrightarrow
\mathsf{LinearMapSingletonCarrier}(h).
$$
\end{theorem}
\begin{proof}
The singleton evaluator always returns $\emp$. In the forward direction, the
classifier already contains the carrier witness for its right endpoint. In the
reverse direction, the carrier witness for $h$ combines with reflexivity at
$\emp$ and symmetry of the displayed carrier equality to form the restricted
singleton classifier.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_append\_input\_classifier\_iff}

\begin{theorem}[Singleton evaluation append-context classifier readback]
\label{thm:linearmap-singleton-eval-append-context-classifier}
For histories $p,f,x,h:\mathsf{BHist}$, classifying a singleton evaluation
after an appended context is exactly the pair of singleton carrier conditions
on the context and the target:
$$
\mathsf{LinearMapSingletonClassifier}
  (\mathsf{append}(p,\mathsf{LinearMapSingletonEval}(f,x)),h)
\Longleftrightarrow
\mathsf{LinearMapSingletonCarrier}(p)\land
\mathsf{LinearMapSingletonCarrier}(h).
$$
\end{theorem}
\begin{proof}
The singleton evaluator returns $\emp$. In the forward direction, the left
carrier field classifies $\mathsf{append}(p,\emp)$ with $\emp$, and
empty-append inversion exposes $p\hsame\emp$; the right classifier carrier
gives $h\hsame\emp$.

Conversely, $p\hsame\emp$ combines with reflexivity at the evaluator result to
classify $\mathsf{append}(p,\mathsf{LinearMapSingletonEval}(f,x))$ with
$\emp$. The target carrier for $h$ and symmetry on that carrier witness then
assemble the restricted singleton classifier.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_append\_context\_classifier\_iff}

\begin{theorem}[Singleton linear-map evaluation context pair classifier]
\label{thm:linearmap-singleton-classifier-eval-context-pair-iff}
For histories $p,q,f,x,g,y:\mathsf{BHist}$,
$$
\mathsf{LinearMapSingletonClassifier}
  (\mathsf{append}(p,\mathsf{LinearMapSingletonEval}(f,x)),
   \mathsf{append}(q,\mathsf{LinearMapSingletonEval}(g,y)))
\Longleftrightarrow
\mathsf{LinearMapSingletonCarrier}(p)\land
\mathsf{LinearMapSingletonCarrier}(q).
$$
\end{theorem}
\begin{proof}
Both singleton evaluation tails are definitionally $\emp$. The append-pair
classifier exactness splits the two appended endpoints into four singleton
carrier obligations; the two evaluation-tail obligations are closed by
reflexivity at $\emp$, leaving exactly the two context carriers.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonClassifier\_eval\_context\_pair\_iff}

\begin{theorem}[Singleton evaluation continuation classifier]
\label{thm:linearmap-singleton-eval-continuation-classifier}
For histories $f,x,y,r,h:\mathsf{BHist}$, if
$$
\ContR(\mathsf{LinearMapSingletonEval}(f,x),y,r),
$$
then classifying the continuation result $r$ against $h$ is equivalent to
the two exposed endpoints $y$ and $h$ lying in the singleton carrier:
$$
\mathsf{LinearMapSingletonClassifier}(r,h)
\Longleftrightarrow
\mathsf{LinearMapSingletonCarrier}(y)\land
\mathsf{LinearMapSingletonCarrier}(h).
$$
\end{theorem}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_continuation\_classifier\_iff}
\begin{proof}
The singleton evaluator returns $\emp$, so the continuation ledger displays
$r$ as $\mathsf{append}(\emp,y)$. In the forward direction, the classifier
field $r\hsame\emp$ and the left-unit append equation identify $y$ with
$\emp$, while the right classifier carrier gives the singleton witness for
$h$. Conversely, singleton witnesses for $y$ and $h$ identify $r$ with
$\emp$ through the same continuation ledger and left-unit append equation,
and the two singleton carrier witnesses assemble the restricted classifier.
\end{proof}

\begin{definition}[Concrete singleton linear-map evaluation graph carrier]
\label{def:linearmap-singleton-evaluation-graph-carrier}
For the concrete singleton empty-history $\LinearMapUp$ instance, the
evaluation graph row is the $\mathsf{BHist}$-anchored visible triple
$(f,x,y)$ satisfying
$$
L_{\emp}(f)\land A_{\emp}(x)\land
\rho_{\emp}(\mathsf{LinearMapSingletonEval}(f,x),y).
$$
Its ledger exposes exactly the map-code row $f$, the input-vector row $x$, the
output endpoint $y$, and the continuation provenance row that relates
$\mathsf{LinearMapSingletonEval}(f,x)$ to $y$ through $\ContR$ when such a
continuation is displayed. No host function equality or quotient witness is
part of the graph carrier.
\end{definition}

\begin{theorem}[Singleton linear-map evaluation graph package]
\label{thm:linearmap-singleton-evaluation-graph-package}
For the evaluation graph carrier of
\autoref{def:linearmap-singleton-evaluation-graph-carrier}, the following
finite singleton package holds.
\begin{enumerate}
  \item Output closure: every graph row has $A_{\emp}(y)$, and every admitted
  $L_{\emp}(f)$ and $A_{\emp}(x)$ determines the singleton output row
  $\rho_{\emp}(\mathsf{LinearMapSingletonEval}(f,x),\emp)$.
  \item Pointwise classifier transport: replacing $f$, $x$, or $y$ along
  $\lambda_{\emp}$ and $\rho_{\emp}$ preserves the graph row and its
  continuation provenance.
  \item Identity and zero-map evaluation collapse: for every carried input
  $x$, the identity and zero-map codes evaluate to the same singleton endpoint
  and classify with the displayed $x$-row.
  \item Composition evaluation collapse: if $f$, $g$, and $x$ are carried, the
  graph row for
  $\mathsf{LinearMapSingletonEval}(\mathsf{LinearMapSingletonComp}(g,f),x)$
  and the graph row read through the continuation of
  $\mathsf{LinearMapSingletonEval}(f,x)$ by $g$ expose the same singleton
  endpoint classifier.
\end{enumerate}
The package exports no host function equality, no quotient row, and no hidden
selector row.
\end{theorem}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEvaluationGraph\_package}
\begin{proof}
The carrier row is built over the same singleton empty-history table as
\autoref{thm:singleton-empty-history-linearmap-laws}. Output closure follows
because every evaluation endpoint is definitionally $\emp$, while
\autoref{thm:singleton-empty-history-linearmap-public-code-exactness} supplies
the public code, identity-code, zero-code, composite-code, and endpoint
exactness rows.

For pointwise transport, use the existing singleton evaluation classifier
readbacks in this file. They reduce each transported endpoint classifier to
the same singleton carrier obligations for the visible map code, input vector,
and output endpoint.

The identity and zero-map clauses are the identity and zero-map rows of the
public-code exactness theorem, read through the graph definition. The
continuation classifier
\autoref{thm:linearmap-singleton-eval-continuation-classifier} repackages the
same output endpoint as a continuation provenance row when a continuation is
displayed.

For composition, apply
\autoref{thm:singleton-empty-history-linearmap-laws} to the singleton
composition operation and then apply the existing composition continuation
classifier in this file. Both the direct composite evaluation and the
stagewise continuation evaluation reduce to $\emp$, so the endpoint classifier
is the singleton $\rho_{\emp}$ row. Since the construction only reads the
finite $\mathsf{BHist}$ graph ledger, no host function equality, quotient
carrier, or hidden selector can be exported.
\end{proof}

\begin{theorem}[Singleton evaluation composition continuation classifier]
\label{thm:linearmap-singleton-eval-comp-continuation-classifier}
If $g,f,x$ lie in the singleton linear-map carrier and
$\ContR(\mathsf{LinearMapSingletonEval}(f,x),g,r)$, then the composed
evaluation endpoint classifies against the continuation result:
$$
\mathsf{LinearMapSingletonClassifier}
(\mathsf{LinearMapSingletonEval}(\mathsf{LinearMapSingletonComp}(g,f),x),r).
$$
\end{theorem}
\begin{proof}
The continuation readback theorem applied to
$\ContR(\mathsf{LinearMapSingletonEval}(f,x),g,r)$ carries the result $r$
whenever $g$ is carried. The composed singleton evaluation endpoint is
$\emp$, so the singleton classifier is assembled from the endpoint carrier,
the result carrier, and symmetry of the result carrier witness.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_comp\_continuation\_classifier}

\begin{theorem}[Singleton evaluation context continuation classifier]
\label{thm:linearmap-singleton-eval-context-continuation-classifier}
For histories $p,f,x,y,r,h:\mathsf{BHist}$, if
$$
\ContR(\mathsf{append}(p,\mathsf{LinearMapSingletonEval}(f,x)),y,r),
$$
then classifying the continuation result $r$ against $h$ is equivalent to all
three exposed endpoints lying in the singleton carrier:
$$
\mathsf{LinearMapSingletonClassifier}(r,h)
\Longleftrightarrow
\mathsf{LinearMapSingletonCarrier}(p)\land
\mathsf{LinearMapSingletonCarrier}(y)\land
\mathsf{LinearMapSingletonCarrier}(h).
$$
\end{theorem}
\begin{proof}
In the forward direction, the result classifier transports the continuation to an empty result. Empty-result inversion splits the continuation into an empty context append and an empty continuation input, append empty-result inversion recovers the carrier witness for $p$, and the target carrier comes from the classifier.

Conversely, the singleton evaluator returns $\emp$, so the carrier witness for $p$ makes the context append carried by $\emp$. Together with the carrier witness for $y$, continuation transport carries $r$ by $\emp$, and the carrier witness for $h$ assembles the restricted singleton classifier.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_context\_continuation\_classifier\_iff}
\begin{theorem}[Singleton evaluation nonempty endpoint empty-result exclusion]
\label{thm:linearmap-singleton-eval-context-nonempty-endpoint-continuation-result-absurd}
For histories $p,f,x,y,r:\mathsf{BHist}$, if either $p$ or $y$ is not singleton-carried, then
$$
\ContR(\mathsf{append}(p,\mathsf{LinearMapSingletonEval}(f,x)),y,r)\land r\hsame\emp\Longrightarrow\bot.
$$
\end{theorem}
\begin{proof}
Transport the continuation result to $\emp$. Empty-result inversion makes both endpoints empty, and append empty-result inversion gives the singleton carrier witness for $p$; either non-carrier hypothesis is contradicted.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_context\_nonempty\_endpoint\_continuation\_result\_absurd}
\begin{theorem}[Singleton evaluation visible context continuation exclusion]
\label{thm:linearmap-singleton-eval-context-continuation-visible-context-absurd}
For histories $p,f,x,y,r,h:\mathsf{BHist}$, a singleton evaluation with a
visible context prefix cannot have a classified continuation result:
$$
\ContR(\mathsf{append}(\Ezero(p),\mathsf{LinearMapSingletonEval}(f,x)),y,r)
\Longrightarrow
\bigl(\mathsf{LinearMapSingletonClassifier}(r,h)\Longrightarrow\bot\bigr),
$$
and the same statement holds with $\Eone(p)$ in place of $\Ezero(p)$.
\end{theorem}
\begin{proof}
The context-continuation classifier readback says any such classifier forces
the visible context prefix to lie in the singleton carrier. That carrier is
$\hsame$ to $\emp$, contradicting constructor separation for the visible head.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_context\_continuation\_visible\_context\_absurd}

\begin{theorem}[Singleton evaluation continuation append target carrier]
\label{thm:linearmap-singleton-eval-continuation-append-target-carrier}
For histories $f,x,y,p,q:\mathsf{BHist}$, if
$$
\ContR(\mathsf{LinearMapSingletonEval}(f,x),y,\mathsf{append}(p,q)),
$$
then the exposed continuation input is in the singleton carrier exactly when
both append factors are:
$$
\mathsf{LinearMapSingletonCarrier}(y)
\Longleftrightarrow
\mathsf{LinearMapSingletonCarrier}(p)\land
\mathsf{LinearMapSingletonCarrier}(q).
$$
\end{theorem}
\begin{proof}
The singleton evaluator returns $\emp$, so the continuation ledger identifies
$\mathsf{append}(p,q)$ with $\mathsf{append}(\emp,y)$. The left-unit law for
append identifies this with $y$. Thus a singleton carrier witness for $y$
turns into an empty-result witness for $\mathsf{append}(p,q)$, which append
empty-result inversion splits into carrier witnesses for $p$ and $q$.
Conversely, carrier witnesses for both factors assemble an empty-result
witness for $\mathsf{append}(p,q)$, and the same continuation ledger and
left-unit law carry it back to $y$.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_continuation\_append\_target\_carrier\_iff}

\begin{theorem}[Singleton evaluation continuation visible result exclusion]
\label{thm:linearmap-singleton-eval-continuation-visible-result-absurd}
For histories $f,x,y,p:\mathsf{BHist}$, if
$\mathsf{LinearMapSingletonCarrier}(y)$, then the continuation result of the
singleton evaluator cannot be either visible endpoint:
$$
\ContR(\mathsf{LinearMapSingletonEval}(f,x),y,\Ezero(p))\Longrightarrow\bot,
\qquad
\ContR(\mathsf{LinearMapSingletonEval}(f,x),y,\Eone(p))\Longrightarrow\bot.
$$
\end{theorem}
\begin{proof}
The continuation ledger identifies each displayed result with
$\mathsf{append}(\emp,y)$. The append left-unit law and the carrier witness for
$y$ force that result to be history-same to $\emp$. Constructor no-confusion
for $\Ezero$ and $\Eone$ closes the two visible cases.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_continuation\_visible\_result\_absurd}

\begin{theorem}[Singleton evaluation context continuation visible result exclusion]
\label{thm:singleton-linearmap-eval-context-continuation-visible-result-absurd}
For histories $p,f,x,y,z:\mathsf{BHist}$, if both $p$ and $y$ lie in the
singleton linear-map carrier, then the continuation result of the prefixed
singleton evaluator cannot be either visible endpoint:
$$
\ContR(\mathsf{append}(p,\mathsf{LinearMapSingletonEval}(f,x)),y,\Ezero(z))
\Longrightarrow\bot,
\qquad
\ContR(\mathsf{append}(p,\mathsf{LinearMapSingletonEval}(f,x)),y,\Eone(z))
\Longrightarrow\bot.
$$
\end{theorem}
\begin{proof}
The singleton evaluator returns $\emp$, so the carrier witness for $p$ makes
the prefixed evaluator history empty. Transporting the displayed continuation
against the right-unit empty continuation forces each displayed result to be
$\hsame$ to $\emp$. Constructor no-confusion excludes both visible endpoint
results.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_context\_continuation\_visible\_result\_absurd}

\begin{theorem}[Singleton evaluation visible target classifier exclusion]
\label{thm:linearmap-singleton-eval-visible-target-classifier-absurd}
For histories $f,x,p:\mathsf{BHist}$, the singleton evaluator cannot classify
against either visible target endpoint:
$$
\mathsf{LinearMapSingletonClassifier}
(\mathsf{LinearMapSingletonEval}(f,x),\Ezero(p))\Longrightarrow\bot,
\qquad
\mathsf{LinearMapSingletonClassifier}
(\mathsf{LinearMapSingletonEval}(f,x),\Eone(p))\Longrightarrow\bot.
$$
\end{theorem}
\begin{proof}
The singleton evaluator returns $\emp$, but the classifier still requires its
right endpoint to lie in the singleton carrier. Thus a classifier witness
against $\Ezero(p)$ or $\Eone(p)$ would force that visible endpoint to be
history-same to $\emp$. Constructor no-confusion for the two visible
constructors closes both cases.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_visible\_target\_classifier\_absurd}

\begin{theorem}[Singleton evaluation continuation visible target exclusion]
\label{thm:linearmap-singleton-eval-continuation-visible-target-exclusion}
For histories $f,x,y,r,p:\mathsf{BHist}$, if
$$
\ContR(\mathsf{LinearMapSingletonEval}(f,x),y,r),
$$
then both visible target classifier claims are impossible:
$$
\mathsf{LinearMapSingletonClassifier}(r,\Ezero(p))\Longrightarrow\bot,
\qquad
\mathsf{LinearMapSingletonClassifier}(r,\Eone(p))\Longrightarrow\bot.
$$
The ten-field fit is: traditional target terminal linear-map continuation
result exclusion; BEDC source $\mathsf{BHist}$, $\ContR$, and the singleton
$\LinearMapUp$ evaluator; carrier $\mathsf{LinearMapSingletonCarrier}$;
classifier $\mathsf{LinearMapSingletonClassifier}$; stability from
continuation classifier readback and constructor no-confusion; ledger
continuation result endpoint; core theorem this block; exactness visible-target
impossibility; standard bridge terminal linear hom; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:linearmap-singleton-eval-continuation-classifier} to
the displayed continuation with target $\Ezero(p)$. A classifier witness for
$\Ezero(p)$ then forces
$\mathsf{LinearMapSingletonCarrier}(\Ezero(p))$.

Apply the same continuation classifier readback with target $\Eone(p)$. A
classifier witness for $\Eone(p)$ forces
$\mathsf{LinearMapSingletonCarrier}(\Eone(p))$.

Unfolding the singleton carrier makes each witness a history-sameness proof
from the visible endpoint to $\emp$. Constructor no-confusion for $\Ezero$ and
$\Eone$ contradicts those histories being empty, so both displayed classifier
claims imply $\bot$.
\end{proof}
\leanchecked{BEDC.Derived.LinearMapUp.LinearMapSingletonEval\_continuation\_visible\_target\_classifier\_absurd}

\input{parts/concrete_instances/linearmap/singleton_eval_continuation_visible_input_exclusion.tex}
\input{parts/concrete_instances/linearmap/concrete_singleton_eval_visible_input}
