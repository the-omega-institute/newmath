\chapter{A Concrete Naming Certificate for $\MetaCICConfluenceInventoryUp$}
\label{ch:concrete-instances-metacic-confluence-inventory-namecert}
\origin{ai}

The $\MetaCICConfluenceInventoryUp$ packet realizes the confluence audit map of \autoref{ch:metacic-confluence-audit-map} as a finite inventory separating checked fragments from retained conditional premises. It sits beside the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the critical-pair boundary of \autoref{ch:concrete-instances-metacic-confluence-critical-pair-boundary-namecert}, the closed-normal confluence seal of \autoref{ch:concrete-instances-closednormalconfluenceseal-namecert}, and the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}. The packet is an audit inventory, not an unconditional Church--Rosser theorem or a discharged substitution-compatibility proof.

\begin{definition}[MetaCIC confluence inventory carrier]\label{def:metacic-confluence-inventory-carrier}
A $\MetaCICConfluenceInventoryUp$ carrier is a finite $\BHist$ packet
$$
I=(K,A,NF,P,D,R,O,H,C,G,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the checked-fragment row, listing the closed-normal and atom-level confluence fragments already available to the audit.
\item $A$ is the audit-map row, recording the finite readback from \autoref{ch:metacic-confluence-audit-map} that classifies each confluence-facing component.
\item $NF$ is the closed-normal seal row, read through $\ClosedNormalConfluenceSealUp$ before any closed-source handoff is exposed.
\item $P$ is the parallel-diamond frontier row, retaining the premise boundary named by $\MetaCICParallelDiamondFrontierUp$.
\item $D$ is the critical-pair boundary row, recording inspected peaks and candidate common-reduct witnesses.
\item $R$ is the residual-substitution premise row, keeping the substitution-compatibility boundary visible.
\item $O$ is the conditional-obligation row, separating retained premises from checked fragments before any confluence handoff is consumed.
\item $H$ records componentwise $\hsame$ transport for checked fragments, audit-map rows, closed-normal seals, frontier rows, critical-pair rows, residual-substitution rows, conditional obligations, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a confluence-audit request through $K,A,NF,P,D,R,O$.
\item $G$ is the $\Pkg$ provenance over $\MetaCICConfluenceInventoryUp$, $\MetaCICParallelDiamondFrontierUp$, $\MetaCICConfluenceCriticalPairBoundaryUp$, $\ClosedNormalConfluenceSealUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $M$ is the local $\NameCert_{\MetaCICConfluenceInventoryUp}$ row.
\end{enumerate}
The classifier compares accepted inventories only through $K,A,NF,P,D,R,O,H,C,G,M$. It has no coordinate for an unconditional parallel diamond, a global Church--Rosser theorem, discharged residual-substitution compatibility, subject reduction, evaluator equality, quotient equality of terms, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC confluence inventory NameCert obligations]\label{thm:metacic-confluence-inventory-namecert-obligations}
For every accepted $\MetaCICConfluenceInventoryUp$ carrier $I=(K,A,NF,P,D,R,O,H,C,G,M)$, the local $\NameCert_{\MetaCICConfluenceInventoryUp}$ surface consists exactly of carrier admission for checked-fragment rows, confluence audit-map rows, closed-normal seal rows, parallel-diamond frontier rows, critical-pair boundary rows, residual-substitution premise rows, conditional-obligation rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The proposed Lean follow-up must provide Nontrivial $\MetaCICConfluenceInventoryUp$ and FieldFaithful $\MetaCICConfluenceInventoryUp$ instances in TasteGate.lean, so the inventory is nonempty and every displayed row is classifier-visible.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-confluence-inventory-carrier}. The projection exposes the checked-fragment inventory, audit-map row, closed-normal seal, parallel frontier, critical-pair boundary, residual-substitution premise, conditional-obligation row, and structural rows.

The checked side is read through $K,A,NF$. These rows may expose closed-normal and atom-level fragments, but they do not discharge the residual-substitution or closed-diamond premises retained by $P,D,R,O$.

The conditional side is read through the frontier, critical-pair, residual-substitution, and obligation rows. Since those rows remain visible before any handoff, the packet separates checked fragments from premises instead of promoting the audit map to an unconditional confluence theorem.

Finally $H,C,G,M$ transport, replay, source, and name the same inventory. Nontrivial and FieldFaithful TasteGate evidence must certify that the inventory carries genuine rows and that no row can be ignored by the classifier.
\end{proof}

\begin{theorem}[MetaCIC confluence inventory conditional boundary]\label{thm:metacic-confluence-inventory-conditional-boundary}
Every confluence-facing read of an accepted $\MetaCICConfluenceInventoryUp$ packet must expose the substitution-compatibility premise row $R$ and the closed-diamond premise retained by $P,D,O$ before any Church--Rosser handoff can be consumed. Checked fragments $K,A,NF$ remain available as finite rows, but the packet exports no unconditional parallel diamond, no global Church--Rosser theorem, no discharged residual-substitution compatibility, and no kernel-consistency endpoint.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:metacic-confluence-inventory-namecert-obligations}. They keep the checked-fragment rows and the retained premise rows in the same accepted carrier.

Use the parallel-diamond frontier and critical-pair boundary only as displayed dependencies. The rows $P$ and $D$ expose the closed-diamond and critical-pair boundary data, while $R$ keeps substitution compatibility as a premise rather than a theorem.

The conditional-obligation row $O$ then blocks a premise-free Church--Rosser handoff. The structural rows $H,C,G,M$ preserve that split, so a downstream confluence consumer can read the inventory but cannot read any refused unconditional endpoint.
\end{proof}

\falsifiablePrediction{Every confluence-facing read of $\MetaCICConfluenceInventoryUp$ exposes the substitution-compatibility and closed-diamond premise rows before any Church--Rosser handoff.}

\independenceWitness{metacic-parallel-diamond-frontier, metacic-confluence-critical-pair-boundary, closednormalconfluenceseal, metacic-residual-substitution-boundary: $\MetaCICConfluenceInventoryUp$ is not bijective to any listed sibling because it jointly carries checked-fragment, audit-map, closed-normal, parallel-frontier, critical-pair, residual-substitution, conditional-obligation, transport, replay, provenance, and local naming rows.}

\closureat{MetaCICConfluenceInventoryUp}{seedStr}

\begin{closurestatus}{\MetaCICConfluenceInventoryUp}
  \constructivestory{A $\MetaCICConfluenceInventoryUp$ packet is generated from checked confluence fragments, an audit-map row, a closed-normal seal row, a parallel-diamond frontier row, a critical-pair boundary row, a residual-substitution premise row, conditional-obligation data, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-confluence-inventory-carrier}, \autoref{thm:metacic-confluence-inventory-namecert-obligations}, and \autoref{thm:metacic-confluence-inventory-conditional-boundary} seed the finite inventory separating checked confluence fragments from retained premises.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, unconditional parallel diamond, global Church--Rosser, discharged substitution compatibility, subject reduction, evaluator equality, quotient equality of terms, kernel consistency, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, checked-fragment visibility, audit-map readback, closed-normal seal exposure, parallel-frontier premise retention, critical-pair boundary retention, residual-substitution boundary retention, conditional-obligation non-escape, transport, replay, provenance, local naming, Nontrivial TasteGate evidence, and FieldFaithful TasteGate evidence for BEDC.Derived.MetaCICConfluenceInventoryUp.}
\end{closurestatus}
