\chapter{A Concrete Naming Certificate for $\MarkovInequalityUp$}
\label{ch:concrete-instances-markov-inequality-namecert}
\origin{human}

Following Feller, Markov's inequality is read as a finite first-moment tail-bound route. It sits beside the second-moment packet of \autoref{ch:concrete-instances-chebyshev-inequality-namecert} and consumes the probability source of \autoref{ch:concrete-instances-probspace-namecert}, the random-variable surface of \autoref{ch:concrete-instances-randomvar-namecert}, the distribution readback of \autoref{ch:concrete-instances-distribution-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a nonnegative tail-bound certificate; it is not a weak-law theorem, a variance estimate, a selected exceptional event, a host measure-algebra quotient, or an ambient $\RealUp$ completeness principle.

\begin{definition}[Markov inequality carrier]\label{def:markov-inequality-carrier}
A $\MarkovInequalityUp$ carrier is a finite $\BHist$ packet
$$
M=(P,X,a,E,T,B,S,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\ProbSpaceUp$ source row carrying the normalized event classifier.
\item $X$ is the nonnegative $\RandomVarUp$ row whose finite observation windows are inspected over $P$.
\item $a$ is the positive threshold row, displayed as a $\RealUp$ comparison seal before any tail event is read.
\item $E$ is the expectation/readback row for $X$, with any rational approximation window displayed through the same probability source.
\item $T$ is the tail-event row naming the event where the observation of $X$ lies above the threshold $a$.
\item $B$ is the probability-bound comparison row, displaying the Markov estimate between the probability of $T$ and the first-moment ratio read from $E$ and $a$.
\item $S$ stores the paired $\RealUp$ and rational seals used to compare the threshold, expectation, tail probability, and ratio rows.
\item $H$ stores componentwise $\hsame$ transport for the probability source, random variable, threshold, expectation/readback, tail event, probability comparison, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite first-moment tail-bound route.
\item $Q$ is the $\Pkg$ provenance over $\MarkovInequalityUp$, $\ProbSpaceUp$, $\RandomVarUp$, $\DistributionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MarkovInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $P,X,a,E,T,B,S,H,C,Q,N$. It has no coordinate for a variance budget, almost-sure convergence, selected subevent, host integration theorem, quotient real equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Markov inequality NameCert obligations]\label{thm:markov-inequality-namecert-obligations}
For every accepted $\MarkovInequalityUp$ carrier $M=(P,X,a,E,T,B,S,H,C,Q,N)$, the local $\NameCert_{\MarkovInequalityUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the probability source, nonnegative random-variable row, positive threshold, expectation/readback row, tail event, probability-bound comparison, Real/Rat seals, transport, replay, provenance, and local naming;
\item source stability, requiring $X,T,E$ to remain attached to the event classifier displayed by $P$;
\item nonnegativity stability, requiring the random-variable read in $X$ to be exposed as nonnegative before the threshold comparison is consumed;
\item threshold stability, requiring $a$ to be displayed as a positive $\RealUp$ row before $T$ and $B$ are read;
\item first-moment stability, requiring $E$ to be displayed before the probability-bound comparison consumes the first-moment ratio;
\item seal nonescape, requiring $S$ to bind the Real and rational comparison rows without exporting quotient real equality, a host integral theorem, or ambient completeness;
\item componentwise transport, displayed replay, package provenance, and local naming through $H,C,Q,N$.
\end{enumerate}
These obligations certify only the finite first-moment tail-bound route. They do not close Chebyshev's inequality, a weak law, almost-sure convergence, host probability limits, selected exceptional events, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:markov-inequality-carrier}. The displayed rows are exactly the probability source, nonnegative random-variable row, threshold, expectation/readback, tail event, probability-bound comparison, seals, and structural rows.

Source, nonnegativity, threshold, and first-moment stability are read from $P,X,a,E,T,B$. The probability-bound comparison cannot be consumed until the event classifier, nonnegative observation row, positive threshold, and expectation/readback row have all been displayed.

The seal row $S$ records only the Real and rational endpoints used by that comparison. Since the carrier has no coordinate for quotient real equality, a host integral theorem, selected exceptional event, countable choice, or ambient completeness, the seal cannot export those objects.

The rows $H,C,Q,N$ transport, replay, source, and name the same finite route. Hence the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Markov--Chebyshev handoff]\label{thm:markov-inequality-chebyshev-handoff}
Every $\ChebyshevInequalityUp$ consumer that requests a first-moment tail-bound precursor can read a $\MarkovInequalityUp$ packet only through
$$
P\longmapsto X\longmapsto a\longmapsto E\longmapsto T\longmapsto B\longmapsto S\longmapsto H,C,Q,N.
$$
The handoff supplies a probability source, nonnegative random-variable row, positive threshold, expectation/readback row, tail event, probability-bound comparison, Real/Rat seals, transport, replay, provenance, and local naming. It does not supply a variance budget, centered second-moment estimate, weak-law limit, almost-sure convergence claim, selected subsequence, or ambient real-completeness principle.
\end{theorem}
\begin{proof}
Begin with a Chebyshev-facing request for a first-moment precursor. Projection of \autoref{def:markov-inequality-carrier} displays the probability source $P$ and the nonnegative random-variable row $X$ before any tail estimate can be consumed.

The request next reads the positive threshold $a$, the expectation/readback row $E$, and the tail-event row $T$. Only then can the probability-bound comparison $B$ and seal row $S$ be handed to the consumer.

The structural rows $H,C,Q,N$ preserve the same route. Because the Markov carrier has no variance-budget or centered second-moment coordinate, the consumer cannot treat this handoff as a Chebyshev proof, weak-law limit, selected subsequence, or ambient completeness claim.
\end{proof}

\begin{theorem}[Markov Real-seal nonescape]\label{thm:markov-inequality-real-seal-nonescape}
Every $\RealUp$ or rational seal read from an accepted $\MarkovInequalityUp$ carrier is downstream from the displayed probability source, nonnegative random-variable row, threshold, expectation/readback row, tail event, and probability-bound comparison. The seal exposes only the finite first-moment comparison route and cannot escape to quotient real equality, host expectation theory, selected events, countable choice, distributional convergence, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the seal row $S$ through the carrier coordinates $P,X,a,E,T,B$. Each endpoint used by the comparison is attached to either the threshold, expectation/readback, tail probability, or first-moment ratio row.

The rational windows in $S$ are displayed only as comparison seals, and the $\RealUp$ endpoints remain downstream from the same probability source and random-variable surface. No carrier coordinate stores a quotient real equality, host expectation theorem, selected event, countable-choice selector, or convergence principle.

Therefore every seal read remains inside the finite first-moment tail-bound route named by $\MarkovInequalityUp$.
\end{proof}

\falsifiablePrediction{Every $\MarkovInequalityUp$ tail-bound read factors through a ProbSpace source row, a nonnegative RandomVar row, a positive threshold, an expectation/readback row, a tail-event row, a probability-bound comparison, Real/Rat seals, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{probspace, randomvar, distribution, real, chebyshev-inequality: $\MarkovInequalityUp$ is not bijective to any listed sibling because it jointly carries a probability source, nonnegative random variable, threshold, expectation/readback, tail event, probability-bound comparison, Real/Rat seals, transport, replay, provenance, and local naming.}

\closureat{MarkovInequalityUp}{seedStr}

\begin{closurestatus}{\MarkovInequalityUp}
  \constructivestory{A $\MarkovInequalityUp$ packet is generated from a probability source row, a nonnegative random-variable row, a positive threshold row, an expectation/readback row, a tail-event row, a probability-bound comparison row, Real and rational seals, componentwise hsame transport, continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:markov-inequality-carrier}, \autoref{thm:markov-inequality-namecert-obligations}, \autoref{thm:markov-inequality-chebyshev-handoff}, and \autoref{thm:markov-inequality-real-seal-nonescape} seed the finite first-moment tail-bound route through ProbSpace, RandomVar, Distribution, RealUp, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Chebyshev's inequality, weak laws, almost-sure convergence, host probability limits, selected exceptional events, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source stability, nonnegativity stability, threshold stability, first-moment stability, seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MarkovInequalityUp.}
\end{closurestatus}
