\begin{theorem}[Heron square-root RegSeqRat Real dependency route]
\label{thm:heron-square-root-regseqrat-real-dependency-route}
Every accepted $\HeronSquareRootUp$ iteration budget consumed by the reciprocal forward link first reads the $\RegSeqRatUp$ regular windows and the $\RealUp$ seal rows already displayed by the Heron carrier. For
$$
H=(A,S,R,L,W,D,K,Q,E,T,C,P,N),
$$
the reciprocal route of \autoref{thm:heron-square-root-real-reciprocal-forward-link} exposes the square-root approximation only after the regular readback $Q$ and terminal seal $E$ are attached to the same positive radicand, seed, reciprocal ledger, Heron recurrence window, dyadic contraction estimate, and Newton provenance rows. Thus the visible dependency passes through \autoref{ch:concrete-instances-real-reciprocal-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert} without adding a host square-root function, quotient real equality, selected completed limit, arbitrary trichotomy, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the Heron carrier. The rows $A,S,R,L,W,D,K$ are the finite radicand, seed, reciprocal, recurrence, window, contraction, and Newton-provenance rows consumed before any square-root-facing read is public.

Apply \autoref{thm:heron-square-root-real-reciprocal-forward-link}. Its reciprocal forward path reads the existing reciprocal chapter through the same row $R$ and then returns to the Heron readback boundary rather than fabricating a separate inverse or square-root object.

Now read the regular-rational and real rows. The row $Q$ is the $\RegSeqRatUp$ regular-window readback produced from the displayed Heron iteration budget, and $E$ is the terminal $\RealUp$ seal visible only after that readback.

Finally transport and replay through $T,C,P,N$. These rows keep the reciprocal, RegSeqRat, and Real dependencies on the same finite Heron route, so the square-root approximation is exposed as a displayed NameCert dependency rather than a host operation, quotient equality, selected limit, trichotomy, or completeness principle.
\end{proof}
