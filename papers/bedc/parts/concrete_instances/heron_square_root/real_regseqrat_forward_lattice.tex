\begin{theorem}[HeronSquareRoot real and RegSeqRat forward lattice]
\label{thm:heron-square-root-real-regseqrat-forward-lattice}
Every $\HeronSquareRootUp$ square-root approximation read consumed by a $\RegSeqRatUp$ or $\RealUp$ endpoint first passes through the finite lattice
$$
\NewtonIterationUp,\ \RegularCauchySquareRootUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp
$$
already displayed by the sibling routes of \autoref{ch:concrete-instances-newtoniteration-namecert}, \autoref{ch:concrete-instances-regular-cauchy-square-root-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. For an accepted carrier
$$
H=(A,S,R,L,W,D,K,Q,E,T,C,P,N),
$$
the read exposes the Newton provenance row $K$, the regular-Cauchy square-root route over $A,S,R,L,W,D$, the regular-rational readback $Q$, the dyadic arithmetic rows $A,S,R,D$, and the terminal Real seal $E$ before $T,C,P,N$ may transport, replay, source, or name the result. It exports no host square-root function, quotient-real equality, selected completed limit, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $A,S,R,L,W,D$ display the positive dyadic radicand, seed, reciprocal ledger, Heron recurrence, finite window, and contraction estimate, while $K$ records the same finite recurrence as a $\NewtonIterationUp$ route.

Apply \autoref{thm:heron-square-root-regular-cauchy-square-root-forward-route}. The square-root-facing consumer must read the regular-Cauchy route before any $\RegSeqRatUp$ or $\RealUp$ endpoint is public.

Next apply \autoref{thm:heron-square-root-regular-cauchy-regseqrat-lattice} and \autoref{thm:heron-square-root-real-seal-boundary}. These place $Q$ before $E$ and keep both rows downstream of the finite dyadic and Newton lattice.

Finally attach $T,C,P,N$. Transport, replay, provenance, and local naming preserve the displayed route and add no coordinate for a host square-root function, quotient-real equality, selected completed limit, trichotomy, or ambient Real completeness.
\end{proof}
