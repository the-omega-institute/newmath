\begin{theorem}[Heron square-root DyadicRatCore anchor]
\label{thm:heron-square-root-dyadicratcore-anchor}
Every accepted $\HeronSquareRootUp$ packet that exposes a dyadic square-root iteration must read the $\DyadicRatCoreUp$ tolerance, denominator, reciprocal, halving, and squared-error rows of \autoref{ch:concrete-instances-dyadicratcore-namecert} before the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} is consumed. In carrier notation the Heron recurrence route remains
$$
A,S,R,L,W,D,K,Q,E,T,C,P,N ,
$$
where $A,S,R,L,W,D$ are the dyadic radicand, seed, reciprocal, recurrence, finite-window, and contraction rows; $Q$ is the regular rational readback; and $E$ is the Real seal. The route exports no host square-root function, quotient real equality, selected completed limit, arbitrary trichotomy, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the Heron carrier from the chapter spine. The radicand and seed rows are dyadic rows, and the reciprocal, halving, and squared-error comparisons in the visible recurrence are also read through $\DyadicRatCoreUp$.

Next use the regular-Cauchy handoff and finite-window contraction induction already present in this chapter. They place the $\RegSeqRatUp$ readback after the dyadic iteration window, so a Real-facing consumer cannot reach the seal before the dyadic tolerance ledger is displayed.

Finally read the terminal $\RealUp$ route. Since the seal is downstream of the dyadic and regular-rational rows, the Heron packet contributes a dependency edge through \autoref{ch:concrete-instances-dyadicratcore-namecert} and \autoref{ch:concrete-instances-real-namecert}, not a host square-root operator or an ambient completeness principle.
\end{proof}
