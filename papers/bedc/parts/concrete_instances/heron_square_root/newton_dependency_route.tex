\begin{theorem}[Heron square-root Newton dependency route]
\label{thm:heron-square-root-newton-dependency-route}
Every accepted $\HeronSquareRootUp$ read that seals a square-root approximation consumes the $\NewtonIterationUp$ provenance row of \autoref{ch:concrete-instances-newtoniteration-namecert}, the $\RegSeqRatUp$ readback row of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal $\RealUp$ seal, and the $\DyadicRatCoreUp$ arithmetic rows before the square-root read is public. For a carrier
$$
H=(A,S,R,L,W,D,K,Q,E,T,C,P,N),
$$
the dependency route projects $A,S,R,L,W,D$ as the finite dyadic Heron ledger, reads $K$ as the specialized Newton step, then exposes $Q$ and $E$ before transport, replay, provenance, and local naming through $T,C,P,N$. It exports no host square-root function, quotient-real equality, selected completed limit, arbitrary trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the Heron carrier. The rows $A,S,R,L,W,D$ contain the positive radicand, seed, reciprocal ledger, recurrence window, and dyadic contraction data needed before any square-root approximation can be consumed.

Apply \autoref{thm:heron-square-root-regular-cauchy-handoff}. It orders every Real-facing read through the finite dyadic ledger and the $\NewtonIterationUp$ provenance row before the $\RegSeqRatUp$ readback $Q$ and terminal seal $E$ are visible.

Use the sibling route in \autoref{thm:heron-square-root-regular-cauchy-sibling-route}. It identifies the same route through \autoref{ch:concrete-instances-newtoniteration-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and the Real-facing endpoint without adding a host square-root value.

Finally read the regular-Cauchy and Real dependency children already present in this chapter. They keep $Q$ and $E$ attached to the displayed Heron ledger, so the square-root read is sealed only after the Newton, regular rational, Real, and dyadic rows have all been displayed.
\end{proof}
