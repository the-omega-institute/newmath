The $\HeronSquareRootUp$ iteration route consumes $\RegSeqRatUp$ readback and $\RealUp$ sealing through the same finite tail-filter discipline used by \autoref{ch:concrete-instances-regular-cauchy-tail-filter-namecert} and \autoref{ch:concrete-instances-real-namecert}. Its positive radicand, reciprocal ledger, Heron window, contraction estimate, and Newton provenance are therefore visible before the regular rational readback and Real seal can be used, and no host square-root operation, quotient real equality, selected limit, countable choice, or ambient completeness row replaces that tail-filter handoff.
