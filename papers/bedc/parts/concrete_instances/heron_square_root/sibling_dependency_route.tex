\begin{theorem}[Heron square-root sibling dependency route]
\label{thm:heron-square-root-sibling-dependency-route}
Every $\HeronSquareRootUp$ consumer that exports a square-root approximation must first read the initial positive $\RealUp$ and $\RatUp$ rows, the $\NewtonIterationUp$ update route, the $\RegularCauchySquareRootUp$ convergence readback, and the $\DyadicIntervalNewtonBisectionUp$ interval discipline. These sibling rows are a dependency route for the Heron packet, not a new square-root existence theorem, not a host square-root function, and not a selected completed limit.
\end{theorem}
\begin{proof}
Project the Heron carrier through the already displayed radicand, seed, reciprocal ledger, recurrence window, dyadic contraction, readback, terminal seal, transport, replay, provenance, and local naming rows.

Read the initial numerical rows first. The positive $\RealUp$ and $\RatUp$ data supply the radicand and rational seed interface before any iterative square-root approximation can be consumed.

Next follow the sibling routes. The $\NewtonIterationUp$ row records the Heron update, $\RegularCauchySquareRootUp$ records the constructive convergence readback, and $\DyadicIntervalNewtonBisectionUp$ records the interval discipline that keeps the approximation inside finite dyadic windows.

The recurrence also reads the reciprocal-accounting route before the regular readback and Real seal become public. Its reciprocal ledger passes through \autoref{ch:concrete-instances-cauchyinversebudget-namecert}, \autoref{ch:concrete-instances-regular-cauchy-division-namecert}, and \autoref{ch:concrete-instances-real-reciprocal-namecert}, matching the forward link of \autoref{thm:heron-square-root-real-reciprocal-forward-link}.

Thus the public Heron read is a horizontal dependency route through the existing Newton, regular-Cauchy, Real, rational, and interval siblings. It exports only the displayed dependency order and adds no square-root theorem, selected limit, quotient-real equality, trichotomy, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Heron square-root sibling route]
\label{thm:heron-square-root-sibling-route}
Every accepted $\HeronSquareRootUp$ packet must read $\DyadicRatCoreUp$ radicand and tolerance data from \autoref{ch:concrete-instances-dyadicratcore-namecert}, $\NewtonIterationUp$ recurrence rows from \autoref{ch:concrete-instances-newtoniteration-namecert}, and the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert} together with the terminal $\RealUp$ seal rows used by \autoref{ch:concrete-instances-regular-cauchy-square-root-namecert} before it exports the square-root route. The Heron carrier therefore sits on the displayed sibling lattice rather than on a host square-root function, selected completed limit, quotient-real equality, trichotomy row, or ambient completeness coordinate.
\end{theorem}
