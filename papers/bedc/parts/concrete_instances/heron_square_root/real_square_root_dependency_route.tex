Every $\HeronSquareRootUp$ read consumes the dyadic-bisection dependency rows of \autoref{ch:concrete-instances-dyadicbisection-namecert} and the Newton interval rows of \autoref{thm:heron-square-root-newton-interval-dependency} before the regular Cauchy square-root row is handed to \autoref{ch:concrete-instances-regseqrat-namecert} and the terminal seal is read through \autoref{ch:concrete-instances-real-namecert}. This route keeps square-root dependency data inside the displayed L10 lattice and does not export selected limits or ambient $\RealUp$ completeness.
