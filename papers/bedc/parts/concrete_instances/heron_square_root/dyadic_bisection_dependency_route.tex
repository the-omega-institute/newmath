Every accepted $\HeronSquareRootUp$ consumer that reads a square-root endpoint through a bisection or interval-shrink route first consumes the $\DyadicRatCoreUp$ tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the dyadic interval cells of \autoref{ch:concrete-instances-dyadicinterval-namecert}, and the located bisection or nested-interval rows of \autoref{ch:concrete-instances-located-interval-bisection-namecert} and \autoref{ch:concrete-instances-located-real-nested-intervals-namecert}. Only after those interval rows have been displayed may the route pass through the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert} and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}; no host square-root endpoint, selected limit, quotient-real equality, trichotomy, or ambient completeness row is exported.
