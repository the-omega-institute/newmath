\begin{theorem}[Heron square-root Real route]
\label{thm:heron-square-root-real-route}
Every $\HeronSquareRootUp$ consumer that reaches a $\RealUp$ seal must first display the Newton--Heron recurrence route, the dyadic bisection or Newton-interval dependency route, the $\RegSeqRatUp$ readback, the reciprocal and nonnegative regular-Cauchy rows, componentwise transport, replay, provenance, and local naming. The route exports no host square-root function, selected limit, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the routed siblings for the carrier. The Newton and Heron recurrence rows expose the iterative rational window, while the dyadic bisection or Newton-interval rows identify the finite interval dependency used by that window.

The completion-facing read then passes through the $\RegSeqRatUp$ row and the reciprocal and nonnegative regular-Cauchy rows before the $\RealUp$ seal is available. Componentwise transport, displayed replay, provenance, and the local $\NameCert$ row preserve the same route without changing its endpoints.

Since none of these routed rows contains a host square-root function, a selected limit, quotient real equality, or an ambient completeness theorem for $\RealUp$, every Real-facing Heron square-root consumer remains inside the displayed finite dependency path.
\end{proof}
