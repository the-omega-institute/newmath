\begin{theorem}[Heron square-root regular-Cauchy square-root forward route]
\label{thm:heron-square-root-regular-cauchy-square-root-forward-route}
Every accepted $\HeronSquareRootUp$ read consumed as a $\RegularCauchySquareRootUp$ route must display the finite path
$$
\NewtonIterationUp,\NestedIntervalUp,\RegSeqRatUp,\RealUp,\hsame,\Cont,\Pkg,\NameCert
$$
before the square-root packet is exported. For a carrier
$$
H=(A,S,R,L,W,D,K,Q,E,T,C,P,N),
$$
the consumer first reads the positive radicand and seed rows $A,S$, the reciprocal ledger $R$, the Heron recurrence and window rows $L,W$, and the contraction estimate $D$; then it reads the $\NewtonIterationUp$ provenance row $K$, the nested-interval shrink route attached to the same window data, the $\RegSeqRatUp$ readback row $Q$, the terminal $\RealUp$ seal $E$, and finally the structural rows $T,C,P,N$. The route exports no host square-root function, quotient-real equality, selected completed limit, arbitrary trichotomy, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the Heron carrier. The rows $A,S,R,L,W,D$ are the finite numerical ledger for the radicand, seed, reciprocal accounting, recurrence window, and dyadic contraction estimate.

Read the iteration and interval support next. The row $K$ records the Heron recurrence as the displayed $\NewtonIterationUp$ step, while the nested-interval dependency route of \autoref{thm:heron-square-root-newton-interval-dependency} attaches the same contraction ledger to a finite interval-shrink path.

Apply \autoref{thm:heron-square-root-regular-cauchy-dependency}. It forces every $\RegularCauchySquareRootUp$ consumer through the sibling route before the square-root-facing read is public.

Apply \autoref{thm:heron-square-root-regular-cauchy-regseqrat-lattice}. The regular rational readback row $Q$ and terminal $\RealUp$ seal $E$ are available only after the displayed Heron ledger and Newton route have been opened.

Finally transport, replay, source, and name the packet through $T,C,P,N$. These are exactly the $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows needed before export, and they add no host square-root operation, quotient equality, selected limit, trichotomy, or ambient completeness coordinate.
\end{proof}
