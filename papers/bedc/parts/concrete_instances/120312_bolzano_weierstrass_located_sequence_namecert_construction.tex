\chapter{A Concrete Naming Certificate for $\BolzanoWeierstrassLocatedSequenceUp$}
\label{ch:concrete-instances-bolzano-weierstrass-located-sequence-namecert}
\origin{human}

Bolzano--Weierstrass and Bishop-style located compactness give the $\BolzanoWeierstrassLocatedSequenceUp$ packet its finite BEDC shape: a bounded located real sequence is inspected through stream windows, regular rational readback, compact interval data, and a monotone-subsequence theorem surface before any cluster-window seal is consumed. It sits after the $\RealUp$ carrier of \autoref{ch:concrete-instances-real-namecert}, the $\RegSeqRatUp$ route of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the selector theorem of \autoref{ch:concrete-instances-monotone-subsequence-theorem-namecert}, and the compact interval data of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert} and \autoref{ch:concrete-instances-closed-bounded-interval-compact-namecert}. The packet records a located finite-window substitute for sequential compactness; it does not assert open-cover compactness, countable choice, a selected host subsequence, quotient stream equality, or ambient $\RealUp$ completeness.

\begin{definition}[Bolzano-Weierstrass located-sequence carrier]
\label{def:bolzano-weierstrass-located-sequence-carrier}
A $\BolzanoWeierstrassLocatedSequenceUp$ carrier is a finite $\BHist$ packet
$$
B=(S,L,K,M,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the bounded located source-sequence row, read only through finite $\StreamNameUp$ windows and displayed lower and upper interval bounds.
\item $L$ is the located interval-membership row that records rational apartness windows for the source values.
\item $K$ is the compact interval data row, supplied by finite closed-bounded interval nets and closed-bounded compactness certificates.
\item $M$ is the $\MonotoneSubsequenceTheoremUp$ row that names the finite selector-theorem surface available to the bounded source.
\item $W$ is the selected finite subsequence-window ledger opened by $S,L,K,M$.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the selected windows.
\item $E$ is the terminal $\RealUp$ seal row for the located cluster-window read.
\item $H$ records componentwise $\hsame$ transport for $S,L,K,M,W,R,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from located source windows through compact interval data and monotone-subsequence windows to regular readback and the Real seal.
\item $P$ is the $\Pkg$ provenance over $\BolzanoWeierstrassLocatedSequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\MonotoneSubsequenceTheoremUp$, $\ClosedBoundedIntervalNetUp$, $\ClosedBoundedIntervalCompactUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BolzanoWeierstrassLocatedSequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for open-cover compactness, selected infinite subsequences, countable choice, quotient stream equality, trichotomy of host reals, or ambient completeness.
\end{definition}

\begin{theorem}[Bolzano-Weierstrass located-sequence NameCert obligations]
\label{thm:bolzano-weierstrass-located-sequence-namecert-obligations}
For every accepted $\BolzanoWeierstrassLocatedSequenceUp$ carrier
$$
B=(S,L,K,M,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\BolzanoWeierstrassLocatedSequenceUp}$ surface consists exactly of carrier admission, bounded located source-window exposure through $S,L$, compact interval data exposure through $K$, selector-theorem exposure through $M$, finite selected-window exposure through $W$, regular rational readback through $R$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier. The source side is $S,L,K$: finite stream windows, located interval membership, and compact interval data are the only rows available before a subsequence window can be read.

The selector-theorem row $M$ is then consumed as a finite BEDC surface. It supplies monotone selected-window data for the bounded source without adding a choice-selected infinite subsequence.

The readback rows are $W,R,E$. The selected windows $W$ determine the $\RegSeqRatUp$ readback $R$, and the terminal $\RealUp$ seal $E$ is visible only after that readback.

Finally $H,C,P,N$ preserve, replay, source, and name the same rows. Since no excluded compactness, choice, quotient, trichotomy, or ambient-completeness coordinate appears in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bolzano-Weierstrass located subsequence window]
\label{thm:bolzano-weierstrass-located-sequence-subsequence-window}
Every cluster-window read exported by an accepted $\BolzanoWeierstrassLocatedSequenceUp$ carrier factors through
$$
\StreamNameUp
\longmapsto
\MonotoneSubsequenceTheoremUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp
$$
together with the compact interval data row $K$ and located membership row $L$. Thus a downstream Real or Completion consumer receives only bounded located source windows, compact interval data, a finite monotone-subsequence theorem row, selected windows, regular readback, a Real seal, transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Start with the accepted carrier and read the source windows $S$ through the located row $L$. The compact interval data $K$ supplies the finite bounded interval surface in which those windows are inspected.

Next use the selector-theorem row $M$. Its output is the finite selected-window ledger $W$, not a host-level infinite subsequence.

The selected windows pass to $R$ and then to $E$. This is exactly the regular rational readback and terminal Real seal route consumed by downstream Real and Completion readers.

The structural rows $H,C,P,N$ keep the same finite route under transport and replay. No open-cover compactness theorem, selected subsequence functional, quotient stream equality, countable-choice row, or ambient $\RealUp$ completeness principle is exported.
\end{proof}

\closureat{BolzanoWeierstrassLocatedSequenceUp}{seedStr}

\begin{closurestatus}{\BolzanoWeierstrassLocatedSequenceUp}
  \constructivestory{A $\BolzanoWeierstrassLocatedSequenceUp$ packet is generated from bounded located StreamName windows, located interval membership, compact interval data, a MonotoneSubsequenceTheorem row, selected finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bolzano-weierstrass-located-sequence-carrier}, \autoref{thm:bolzano-weierstrass-located-sequence-namecert-obligations}, and \autoref{thm:bolzano-weierstrass-located-sequence-subsequence-window} seed the finite located sequence route from compact interval data to regular cluster-window readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, choice-selected infinite subsequences, countable choice, quotient stream equality, trichotomy of host reals, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located source-window exposure, compact interval data exposure, monotone-subsequence theorem routing, selected-window readback, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.BolzanoWeierstrassLocatedSequenceUp.}
\end{closurestatus}
