\chapter{A Concrete Naming Certificate for $\BishopCompactImageUp$}
\label{ch:concrete-instances-bishop-compact-image-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopCompactImageUp$ packet records the constructive continuous image of compact metric data as finite compact-source, continuous-map, uniform-modulus, image-witness, regular rational, dyadic tolerance, and terminal real-seal rows. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the regular sequence readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the finite image carrier, its NameCert rows, and its Real-facing handoff; it does not assert a quotient image space, an ambient compactness theorem, countable choice, or host-level function equality.

\falsifiablePrediction{A consumer that reads a compact-image Real endpoint without the displayed compact source, continuous-map row, uniform-modulus ledger, image witness, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance row, and $\RealUp$ seal has left the $\BishopCompactImageUp$ packet.}

\independenceWitness{compactmetric, continuousmap, regseqrat, dyadicratcore, real: no listed sibling supplies the whole compact-image packet, because $\CompactMetricUp$ names the compact source, $\ContinuousMapUp$ names the map row, $\RegSeqRatUp$ and $\DyadicRatCoreUp$ name value readback, and $\RealUp$ seals endpoints while this chapter binds their finite image-witness route.}

\begin{definition}[Bishop compact-image carrier]
\label{def:bishop-compact-image-carrier}
A $\BishopCompactImageUp$ carrier is a finite $\BHist$ packet
$$
I=(K,F,M,W,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the accepted $\CompactMetricUp$ compact-source row, including the finite cover or finite-net data exposed by that sibling packet.
\item $F$ is the accepted $\ContinuousMapUp$ row naming the map from the displayed compact source into the displayed metric target.
\item $M$ is the uniform-modulus ledger produced from the compact-source and continuous-map rows.
\item $W$ is the image-witness table: each displayed image request points back to a compact-source row, a map-evaluation row, and the modulus row used to certify the requested tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row for the displayed image values.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used by the modulus and readback comparisons.
\item $E$ is the terminal $\RealUp$ seal for the displayed image values.
\item $H$ records componentwise $\hsame$ transport for $K,F,M,W,R,D,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from compact-source requests through continuous-map evaluation, uniform-modulus use, image-witness inspection, regular rational readback, dyadic tolerance comparison, and the Real seal.
\item $P$ is the $\Pkg$ provenance row naming exactly $\BishopCompactImageUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCompactImageUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through these displayed rows. It has no coordinate for a quotient image set, a host subset equality, countable choice of preimages, or ambient compactness outside the named source.
\end{definition}

\begin{theorem}[Bishop compact-image NameCert obligations]
\label{thm:bishop-compact-image-namecert-obligations}
For every accepted $\BishopCompactImageUp$ carrier
$$
I=(K,F,M,W,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\BishopCompactImageUp}$ surface consists exactly of carrier exposure for $I$, compact-source exposure through $K$, continuous-map exposure through $F$, uniform-modulus handoff through $M$, image-witness exactness through $W$, regular rational readback through $R$, dyadic tolerance exposure through $D$, terminal Real-seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-compact-image-carrier}. The rows $K,F,M,W,R,D,E$ are the analytic route, and $H,C,P,N$ are the structural rows.

The source-facing read begins at $K$ and $F$. The compact metric row supplies only the displayed compact-source data, while the continuous-map row supplies only the named map-evaluation surface on that source.

The image-facing read then passes through $M,W,R,D,E$. The uniform-modulus ledger fixes the tolerance route, the witness table points each image request back to a displayed source and map-evaluation row, the regular rational row reads the requested values, the dyadic row supplies the tolerance arithmetic, and the Real row seals the displayed endpoint.

Transport, replay, provenance, and naming are exactly the rows $H,C,P,N$. Since no carrier coordinate names a quotient image set, a host subset equality, a choice function for all preimages, or ambient compactness beyond $K$, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop compact-image Real-completion handoff]
\label{thm:bishop-compact-image-real-completion-handoff}
Every Real-facing image consumer of an accepted $\BishopCompactImageUp$ carrier factors through the displayed compact source, continuous-map row, uniform-modulus ledger, image-witness table, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance row, and $\RealUp$ seal:
$$
K,F,M,W,R,D,E,H,C,P,N .
$$
The handoff exposes a finite constructive image route and no quotient image carrier, host-level function equality, countable preimage choice, ambient Heine--Borel principle, or completed image-space object.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bishop-compact-image-namecert-obligations}. They identify the compact-source, continuous-map, modulus, witness, readback, dyadic, and Real rows as the whole Real-facing route.

A consumer that requests an image endpoint must first inspect $K,F,M,W$. These rows determine the displayed compact source, the displayed map evaluation, the modulus used for the request, and the finite witness that links the image read to a source row.

The endpoint read then passes through $R,D,E$. Regular rational readback and dyadic tolerance comparison produce the value data that the terminal $\RealUp$ seal may expose.

Finally $H,C,P,N$ preserve the same route under transport, replay it under displayed continuation, name its sibling sources, and bind the local certificate. No listed row supplies an unbounded preimage selector, a quotient image set, or an ambient compactness theorem, so every Real-facing consumer remains inside the finite handoff.
\end{proof}

\closureat{BishopCompactImageUp}{seedStr}

\begin{closurestatus}{\BishopCompactImageUp}
  \constructivestory{A $\BishopCompactImageUp$ packet is generated from a displayed compact metric source, continuous-map row, uniform-modulus ledger, image-witness table, regular rational readback, dyadic tolerance arithmetic, terminal Real seal, componentwise transport, displayed continuation replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-compact-image-carrier}, \autoref{thm:bishop-compact-image-namecert-obligations}, and \autoref{thm:bishop-compact-image-real-completion-handoff} seed the finite constructive compact-image carrier and its Real-facing handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient image spaces, ambient compactness theorems, countable preimage choice, host function equality, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the finite carrier and prove compact-source exposure, continuous-map exposure, uniform-modulus handoff, image-witness exactness, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopCompactImageUp.}
\end{closurestatus}
