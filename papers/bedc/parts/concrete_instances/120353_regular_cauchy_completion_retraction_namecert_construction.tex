\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionRetractionUp$}
\label{ch:concrete-instances-regular-cauchy-completion-retraction-namecert}
\origin{human}

Following Bishop and Bridges constructive completion theory and the Kelley presentation of completion as a reflective boundary, the $\RegularCauchyCompletionRetractionUp$ packet records the finite retraction surface from displayed Cauchy-completion data back to regular Cauchy real names. It sits after the completion retraction packet of \autoref{ch:concrete-instances-cauchy-completion-retraction-namecert}, the finite name windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only a finite NameCert retraction route; it does not assert quotient completion, selected representatives, countable choice, host equality of completed objects, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy completion retraction carrier]
\label{def:regular-cauchy-completion-retraction-carrier}
A $\RegularCauchyCompletionRetractionUp$ carrier is a finite $\BHist$ packet
$$
T=(K,W,S,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CauchyCompletionRetractionUp$ source row, exposing the completion-facing retraction request before any regular readback is consumed.
\item $W$ is the $\StreamNameUp$ finite-window row inspected by the retraction.
\item $S$ is the $\RegSeqRatUp$ regular rational readback row extracted from those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger controlling the finite regularity and comparison budgets.
\item $E$ is the terminal $\RealUp$ seal reached only after $K,W,S,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the retraction row, windows, readback, dyadic ledger, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion-facing read back through $K,W,S,D,E$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionRetractionUp$, $\CauchyCompletionRetractionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionRetractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows and their replay paths. It has no coordinate for quotient completion equality, selected representatives, countable choice, host equality of completed points, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy completion retraction NameCert obligations]
\label{thm:regular-cauchy-completion-retraction-namecert-obligations}
For every accepted $\RegularCauchyCompletionRetractionUp$ carrier
$$
T=(K,W,S,D,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyCompletionRetractionUp}$ surface consists exactly of completion-retraction admission through $K$, finite StreamName window exposure through $W$, RegSeqRat readback through $S$, DyadicRatCore tolerance comparison through $D$, Real seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-completion-retraction-carrier}. The analytic rows are exactly $K,W,S,D,E$, while $H,C,P,N$ transport, replay, source, and name that same finite packet.

The retraction row is read first. Row $K$ supplies the completion-facing retraction request, but it is not a quotient-completion equality or a selected representative.

The regular source rows then control the readback. Rows $W,S,D$ expose finite stream windows, regular rational data, and dyadic tolerance budgets before the terminal seal row $E$ is available.

Finally $H,C,P,N$ preserve the same route. Since the excluded quotient, representative, choice, host-equality, and ambient-completeness objects have no carrier coordinate, none can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Regular Cauchy completion retraction Real seal route]
\label{thm:regular-cauchy-completion-retraction-real-seal-route}
Every accepted $\RegularCauchyCompletionRetractionUp$ completion-facing read reaches the terminal $\RealUp$ seal only through the displayed route
$$
\CauchyCompletionRetractionUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp .
$$
Thus a Real or Completion consumer reads the retraction as finite regular-Cauchy NameCert data through $K,W,S,D,E,H,C,P,N$, not as quotient completion, a selected representative, countable choice, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:regular-cauchy-completion-retraction-namecert-obligations}. It places every exported retraction read inside the rows $K,W,S,D,E,H,C,P,N$.

The source row $K$ attaches the route to \autoref{ch:concrete-instances-cauchy-completion-retraction-namecert}. The window and readback rows $W,S$ attach it to the finite StreamName and RegSeqRat surfaces of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}.

The dyadic ledger $D$ supplies the finite tolerance comparison used before the terminal seal $E$ is consumed. The structural rows only transport, replay, source, and name that route.

Therefore every accepted consumer reaches $\RealUp$ through the displayed regular-Cauchy surface and cannot replace it by quotient completion equality, selected representatives, countable choice, host equality, or ambient completeness.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyCompletionRetractionUp$ packet that exposes a terminal $\RealUp$ seal without first displaying the $\CauchyCompletionRetractionUp$ source row, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ tolerance ledger refutes the carrier surface.}

\independenceWitness{cauchy-completion-retraction, streamname, regseqrat, dyadicratcore, real: no listed sibling supplies the whole finite packet $T=(K,W,S,D,E,H,C,P,N)$, because each sibling contributes only the retraction request, finite windows, regular readback, tolerance comparison, or terminal seal while this chapter binds them into one regular-Cauchy completion retraction surface.}

\closureat{RegularCauchyCompletionRetractionUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionRetractionUp}
  \constructivestory{A $\RegularCauchyCompletionRetractionUp$ packet is generated from a CauchyCompletionRetraction source row, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance comparison, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-retraction-carrier}, \autoref{thm:regular-cauchy-completion-retraction-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-retraction-real-seal-route} seed the finite retraction route from Cauchy completion data to regular Cauchy real-name data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion, selected representatives, countable choice, host equality of completed objects, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, completion-retraction admission, finite-window exposure, regular readback, dyadic tolerance comparison, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyCompletionRetractionUp.}
\end{closurestatus}
