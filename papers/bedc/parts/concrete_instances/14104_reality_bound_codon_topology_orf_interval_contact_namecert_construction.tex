The $\RealityBoundCodonTopologyOrfIntervalContactUp$ chapter isolates an external curated interval-contact ledger from translation, structure, function, and law-level claims. Its interval-contact row consumes the displayed located endpoint and interval-membership carrier of \autoref{ch:concrete-instances-located-real-interval-namecert} before any codon-topology contact label is admitted.
