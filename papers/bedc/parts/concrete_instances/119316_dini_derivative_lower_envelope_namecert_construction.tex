\chapter{A Concrete Naming Certificate for $\DiniDerivativeLowerEnvelopeUp$}
\label{ch:concrete-instances-dini-derivative-lower-envelope-namecert}
\origin{human}

Following Dini and Bishop--Bridges constructive analysis, the $\DiniDerivativeLowerEnvelopeUp$ packet records the finite lower-envelope surface by which a displayed real-valued function exposes lower Dini rows before any derivative-envelope or semicontinuity consumer can inspect them. It sits beside the one-sided Dini carrier of \autoref{ch:concrete-instances-dini-derivative-namecert}, the upper envelope sibling of \autoref{ch:concrete-instances-upper-dini-derivative-namecert}, the lower-semicontinuity packet of \autoref{ch:concrete-instances-lowersemicontinuous-namecert}, the derivative quotient boundary of \autoref{ch:concrete-instances-derivative-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only finite lower-envelope windows and readback rows; it does not claim classical completeness, selected limiting slopes, quotient real equality, or a host lower semicontinuous hull.

\begin{definition}[DiniDerivativeLowerEnvelope carrier]
\label{def:dini-derivative-lower-envelope-carrier}
A $\DiniDerivativeLowerEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
G=(F,W,L,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the displayed real-valued function row, read through the same finite observation surface used by the sibling $\DiniDerivativeUp$ and $\DerivativeUp$ packets.
\item $W$ is the lower-envelope window row, listing the finite one-sided neighborhoods and lower secant comparisons available to the packet.
\item $L$ is the $\LowerSemicontinuousUp$ row that records the lower-envelope visibility surface for downstream epigraph or threshold consumers.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance row for radii, slope lower bounds, and comparison slack.
\item $R$ is the $\RegSeqRatUp$ readback row for endpoint values, secant numerators, step sizes, and dyadic lower-bound approximants.
\item $E$ is the terminal $\RealUp$ seal reached only after $F,W,L,Q,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the function, lower-envelope window, lower-semicontinuity, dyadic, readback, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for refinements of the same finite lower-envelope window.
\item $P$ is the $\Pkg$ provenance over $\DiniDerivativeLowerEnvelopeUp$, $\DiniDerivativeUp$, $\UpperDiniDerivativeUp$, $\LowerSemicontinuousUp$, $\DerivativeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DiniDerivativeLowerEnvelopeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $F,W,L,Q,R,E,H,C,P,N$. It has no coordinate for a classical liminf, selected derivative value, quotient equality of real names, countable choice, ambient $\RealUp$ completeness, or an external lower-semicontinuous hull.
\end{definition}

\begin{theorem}[DiniDerivativeLowerEnvelope NameCert obligations]
\label{thm:dini-derivative-lower-envelope-namecert-obligations}
For every accepted $\DiniDerivativeLowerEnvelopeUp$ carrier
$$
G=(F,W,L,Q,R,E,H,C,P,N),
$$
the local $\NameCert_{\DiniDerivativeLowerEnvelopeUp}$ surface consists exactly of carrier admission for the displayed function row, lower-envelope windows, lower-semicontinuity row, DyadicRatCore tolerance row, RegSeqRat readback, RealUp seal, componentwise transport, continuation replay, package provenance, and local naming; lower-envelope exactness requiring every lower-bound read to pass through $W,Q,R$; lower-semicontinuity compatibility requiring the row $L$ to remain visible before threshold consumers read the envelope; and nonescape from classical liminf data, selected derivative values, quotient real equality, countable choice, ambient Real completeness, and host lower-semicontinuous hulls.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dini-derivative-lower-envelope-carrier}. The accepted packet exposes only the displayed function row $F$, lower-envelope windows $W$, lower-semicontinuity row $L$, dyadic tolerance row $Q$, regular readback row $R$, Real seal $E$, and structural rows $H,C,P,N$.

Lower-envelope exactness is finite. A lower-bound request must first read the inspected window in $W$, then the dyadic tolerance and slack data in $Q$, and then the regular rational readback in $R$ before the Real seal can be consumed.

The lower-semicontinuity row remains attached to the same route. Thus an epigraph or threshold consumer can inspect the lower envelope only through the displayed row $L$, with transport and replay preserving the same finite packet.

Since no carrier coordinate stores a classical liminf, selected derivative value, quotient equality, choice schedule, ambient completeness theorem, or external hull, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[DiniDerivativeLowerEnvelope lower-envelope handoff]
\label{thm:dini-derivative-lower-envelope-handoff}
Every $\LowerSemicontinuousUp$-, $\DiniDerivativeUp$-, or derivative-facing consumer of an accepted $\DiniDerivativeLowerEnvelopeUp$ packet must read the finite route
$$
F,W,L,Q,R,E,H,C,P,N
$$
before using the lower-envelope comparison. The handoff supplies displayed function data, lower-envelope windows, lower-semicontinuity visibility, DyadicRatCore tolerances, RegSeqRat readback, the terminal RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming, but it supplies no upper-envelope row, two-sided derivative, selected limiting slope, quotient real equality, countable-choice selector, or ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with the obligation theorem \autoref{thm:dini-derivative-lower-envelope-namecert-obligations}. It identifies the lower envelope as a finite route through $F,W,L,Q,R,E,H,C,P,N$, not as an external limiting operator.

Read the sibling Dini and derivative surfaces next. The rows $F,W,Q,R$ provide the finite secant-window and tolerance data needed by a Dini or derivative-facing consumer, while the Real seal $E$ remains downstream of those rows.

Finally pass to lower-semicontinuity consumers through $L$. The row $L$ keeps threshold and epigraph reads on the same displayed packet, and the structural rows preserve that packet under transport, replay, provenance, and local naming. Therefore the handoff cannot export upper-envelope data, a two-sided derivative, selected limiting slopes, quotient equality, countable choice, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\DiniDerivativeLowerEnvelopeUp$ consumer reaching a lower-envelope read without the displayed function row, lower-envelope windows, LowerSemicontinuous row, DyadicRatCore tolerance row, RegSeqRat readback, RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming refutes the carrier.}

\independenceWitness{dini-derivative, upper-dini-derivative, lowersemicontinuous, derivative, real: no listed sibling supplies the whole lower-envelope packet $G=(F,W,L,Q,R,E,H,C,P,N)$, because each sibling contributes only one-sided Dini windows, upper rows, threshold visibility, quotient inspection, or a terminal real seal while this chapter binds the lower-envelope route in one finite surface.}

\closureat{DiniDerivativeLowerEnvelopeUp}{seedStr}

\begin{closurestatus}{\DiniDerivativeLowerEnvelopeUp}
  \constructivestory{A $\DiniDerivativeLowerEnvelopeUp$ packet is generated from displayed function data, lower-envelope windows, a LowerSemicontinuous visibility row, DyadicRatCore tolerance rows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dini-derivative-lower-envelope-carrier}, \autoref{thm:dini-derivative-lower-envelope-namecert-obligations}, and \autoref{thm:dini-derivative-lower-envelope-handoff} seed the finite lower-envelope packet from Dini derivative windows to lower-semicontinuity and derivative-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim classical liminf existence, selected derivative values, two-sided derivative existence, quotient real equality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, lower-envelope exactness, lower-semicontinuity compatibility, DyadicRatCore tolerance exposure, RegSeqRat readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DiniDerivativeLowerEnvelopeUp.}
\end{closurestatus}
