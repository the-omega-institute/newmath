\chapter{A Concrete Naming Certificate for $\CauchyProductCriterionUp$}
\label{ch:concrete-instances-cauchy-product-criterion-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\CauchyProductCriterionUp$ packet isolates the finite criterion under which synchronized Cauchy product data is sufficient for the $\RegSeqRatUp$ handoff and the terminal $\RealUp$ seal. It sits after the product route of \autoref{ch:concrete-instances-cauchyproduct-namecert}, the bounded-product ledger of \autoref{ch:concrete-instances-cauchy-product-bounded-namecert}, and the completion-facing product route of \autoref{ch:concrete-instances-cauchy-completion-product-namecert}; it reads synchronized windows from \autoref{ch:concrete-instances-streamname-namecert}, dyadic budget rows from \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational classifier of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite sufficiency surface, not multiplication of completed real objects, quotient equality of streams, a selected limit, countable choice, trichotomy, or an ambient completeness theorem.

\begin{definition}[Cauchy product criterion carrier]
\label{def:cauchy-product-criterion-carrier}
A $\CauchyProductCriterionUp$ carrier is a finite $\BHist$ packet
$$
Z=(X,Y,W,D,P,R,E,H,C,L,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two source rows. Each row exposes a Cauchy schedule, a regular-rational readback boundary, a real-seal boundary, and the local source certificate needed by the product route.
\item $W$ is the synchronized $\StreamNameUp$ window row, aligning the two source schedules with the product window consumed downstream.
\item $D$ is the $\DyadicRatCoreUp$ product-budget ledger, containing the source tolerances, magnitude bounds, cross-error budget, and the finite window budget used to certify the product read.
\item $P$ is the $\CauchyProductUp$ route, read only after $X$, $Y$, $W$, and $D$ have been displayed.
\item $R$ is the $\RegSeqRatUp$ classifier row receiving the product read from the synchronized product route.
\item $E$ is the $\RealUp$ seal row, consumed only after the regular-rational classifier row has accepted the product read.
\item $H$ records componentwise $\hsame$ transport for $X,Y,W,D,P,R,E,C,L,N$ and the local packet shape.
\item $C$ records displayed $\Cont$ replay from the two source rows through the synchronized window, dyadic product budget, product route, regular-rational classifier, and real-seal row.
\item $L$ is the $\Pkg$ provenance over $\CauchyProductCriterionUp$, $\CauchyProductUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyProductCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,W,D,P,R,E,H,C,L,N$. It has no coordinate for host multiplication of completed reals, quotient stream equality, selected limits, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Cauchy product criterion NameCert obligations]
\label{thm:cauchy-product-criterion-namecert-obligations}
For an accepted $\CauchyProductCriterionUp$ carrier $Z=(X,Y,W,D,P,R,E,H,C,L,N)$, the local $\NameCert_{\CauchyProductCriterionUp}$ obligations are exactly carrier admission for the two source rows, synchronized-window admission for $W$, dyadic product-budget exactness for $D$, product-route admission for $P$, regular-rational handoff for $R$, real-seal handoff for $E$, componentwise transport through $H$, displayed continuation replay through $C$, provenance through $L$, and ledger non-escape through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-product-criterion-carrier}. The displayed packet lists the two source rows, the synchronized window, the dyadic budget, the product route, the regular-rational classifier, the real seal, and the structural rows.

The source and window obligations are read from $X,Y,W$. These rows display the two schedules and the common product window before any product observation is sent to a downstream classifier.

The dyadic obligation is the row $D$. It contains the finite tolerance, magnitude, and cross-error budget needed by the product route, so product admission through $P$ is budgeted data rather than host multiplication of completed real names.

The rows $R$ and $E$ are downstream boundaries. Transport through $H$, replay through $C$, provenance through $L$, and local naming through $N$ preserve the same route and add no extra coordinate.

Since the carrier has no row for quotient streams, selected limits, countable choice, trichotomy, ambient completeness, Lean verification, or bridge checking, the displayed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Cauchy product criterion window-budget sufficiency]
\label{thm:cauchy-product-criterion-window-budget-sufficiency}
For an accepted $\CauchyProductCriterionUp$ carrier, synchronized windows and dyadic product-budget data suffice to route the product read to the regular-rational classifier:
$$
X,Y\longmapsto W\longmapsto D\longmapsto P\longmapsto R .
$$
Every $\RegSeqRatUp$ consumer of the product packet factors through this finite route and cannot bypass it by reading a quotient stream, selected product limit, host multiplication primitive, countable-choice row, trichotomy row, or ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with a consumer asking for the regular-rational product read. The only $\RegSeqRatUp$ row in the carrier is $R$.

The row $R$ is downstream of the displayed product route $P$. The route $P$ is downstream of the synchronized window $W$ and dyadic budget $D$, and those rows are in turn downstream of the two source rows $X$ and $Y$.

Thus the consumer must pass through the displayed chain $X,Y,W,D,P,R$. The structural rows $H,C,L,N$ transport, replay, source, and name this same chain without creating a second product route.

No other regular-rational classifier occurs in the packet. Therefore the synchronized window and dyadic product budget are sufficient for the $\RegSeqRatUp$ handoff, and the excluded objects cannot be exported by the criterion.
\end{proof}

\begin{theorem}[Cauchy product criterion source-window stability]
\label{thm:cauchy-product-criterion-stability-under-equivalent-source-windows}
Let $Z=(X,Y,W,D,P,R,E,H,C,L,N)$ be an accepted $\CauchyProductCriterionUp$ carrier. Suppose the source rows $X,Y$ and the synchronized window row $W$ are replaced only by displayed componentwise $\hsame$ rows and the replacement is replayed through the displayed $\Cont$ row $C$. Then the same dyadic product-budget row $D$, product route $P$, $\RegSeqRatUp$ handoff $R$, and $\RealUp$ seal $E$ remain the admissible criterion route:
$$
X,Y\longmapsto W\longmapsto D\longmapsto P\longmapsto R\longmapsto E .
$$
Thus source-window replacement along the classifier data preserves the product criterion and cannot introduce host multiplication, quotient stream equality, a selected product limit, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier to its displayed rows. By \autoref{thm:cauchy-product-criterion-window-budget-sufficiency}, the original $\RegSeqRatUp$ product read factors through $X,Y,W,D,P,R$.

The replacement hypothesis is internal to the carrier data: $H$ supplies componentwise $\hsame$ transport for the source rows and synchronized window, while $C$ replays the continuation from the transported source-window data through the same displayed product route.

Therefore the transported source-window presentation still reaches $D$, then $P$, then $R$, and the real-facing row $E$ reads the same admissible criterion route. No row in the carrier can insert host multiplication, a quotient-stream witness, a selected limit, countable choice, trichotomy, or ambient completeness between these displayed stages.
\end{proof}

\begin{theorem}[Cauchy product criterion real-seal boundary]
\label{thm:cauchy-product-criterion-real-seal-boundary}
Every $\RealUp$ seal read of an accepted $\CauchyProductCriterionUp$ carrier factors through
$$
X,Y\longmapsto W\longmapsto D\longmapsto P\longmapsto R\longmapsto E
$$
together with the displayed $\hsame$ transports, $\Cont$ replays, $\Pkg$ provenance, and local $\NameCert_{\CauchyProductCriterionUp}$ row. Hence the real-facing boundary is a finite synchronized Cauchy product criterion, not host multiplication, quotient stream equality, a selected limit, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
The real-facing consumer can read only $E$ as a $\RealUp$ seal row. In the carrier, $E$ is explicitly downstream of $R$, and $R$ is the regular-rational handoff produced by the product route.

Apply \autoref{thm:cauchy-product-criterion-window-budget-sufficiency}. The route to $R$ already passes through the two source rows, synchronized window, dyadic product-budget ledger, and product route.

The structural rows $H,C,L,N$ preserve that route under transport, replay, provenance, and local naming. They do not add a second seal source or a host-level product operation.

Since there is no coordinate for quotient streams, selected limits, countable choice, trichotomy, or ambient completeness, every real-seal read remains inside the displayed finite criterion.
\end{proof}

\closureat{CauchyProductCriterionUp}{seedStr}

\begin{closurestatus}{\CauchyProductCriterionUp}
  \constructivestory{A $\CauchyProductCriterionUp$ packet is generated from two Cauchy source rows, a synchronized StreamName window, a DyadicRatCore product-budget ledger, a CauchyProduct route, a RegSeqRat classifier row, a RealUp seal row, componentwise transport, displayed continuation replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-product-criterion-carrier}, \autoref{thm:cauchy-product-criterion-namecert-obligations}, \autoref{thm:cauchy-product-criterion-window-budget-sufficiency}, \autoref{thm:cauchy-product-criterion-stability-under-equivalent-source-windows}, and \autoref{thm:cauchy-product-criterion-real-seal-boundary} seed the finite criterion for synchronized Cauchy product data before RegSeqRat and Real seal consumers read the product.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host multiplication, quotient stream equality, selected limits, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, synchronized-window admission, dyadic product-budget exactness, product-route admission, regular-rational handoff, real-seal handoff, transport stability, continuation replay, and ledger non-escape for BEDC.Derived.CauchyProductCriterionUp.}
\end{closurestatus}
