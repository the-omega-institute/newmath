\chapter{A Concrete Naming Certificate for $\FiniteWitnessRouteUp$}
\label{ch:concrete-instances-finitewitnessroute-namecert}
\origin{human}

The $\FiniteWitnessRouteUp$ interface names the finite route packet that carries closed-observation witness records into Real-and-Completion consumers. It is a concrete $\NameCert$ surface for the passage from a selected observation request through $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ readbacks to a $\CauchyLimitSealUp$ or $\RealUp$ seal row, with every transport, continuation, and provenance step present in the finite history.

The route is downstream of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-cauchylimitseal-namecert}, and \autoref{ch:concrete-instances-real-namecert}. $\StreamNameUp$ supplies the finite window, $\RegSeqRatUp$ supplies the readback, $\DyadicRatCoreUp$ supplies the comparison ledger, $\CauchyLimitSealUp$ supplies the sealed completion boundary, and $\RealUp$ supplies the terminal real-facing seal.

\begin{definition}[Finite witness-route carrier]
\label{def:finite-witness-route-carrier}
A $\FiniteWitnessRouteUp$ carrier is a finite $\BHist$ packet
$$
R=(q,w,r,d,s,h,c,p,n)
$$
with the following displayed rows:
\begin{enumerate}
\item $q$ is the closed-observation request row naming the finite witness demand, tolerance scale, and downstream Real-and-Completion consumer;
\item $w$ is the selected $\StreamNameUp$ window inspected for that request;
\item $r$ is the $\RegSeqRatUp$ readback row exposed through the selected window;
\item $d$ is the $\DyadicRatCoreUp$ observation ledger containing the dyadic comparisons, radius splits, and finite arithmetic reads used by the route;
\item $s$ is the $\CauchyLimitSealUp$ or $\RealUp$ seal row that receives the completed finite witness route;
\item $h$ records componentwise $\hsame$ transports for the request row, selected window, regular rational readback, dyadic ledger, seal row, provenance, and local name;
\item $c$ records the displayed $\Cont$ routes from request to window, from window to regular rational readback, from readback to dyadic ledger, and from the dyadic ledger to the seal row;
\item $p$ is the $\Pkg$ provenance over $\BHist$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$, $\RealUp$, $\hsame$, $\Cont$, and $\NameCert$;
\item $n$ is the local $\NameCert_{\FiniteWitnessRouteUp}$ row.
\end{enumerate}
The carrier contains no host-level infinite trace, no quotient stream equality, no selected real value, no unrecorded limit, and no consumer route outside the displayed finite packet.
\end{definition}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Finite witness-route NameCert obligations]
\label{thm:finite-witness-route-namecert-obligations}
The local $\NameCert_{\FiniteWitnessRouteUp}$ surface for \autoref{def:finite-witness-route-carrier} consists exactly of carrier admission, request exposure, selected $\StreamNameUp$ window admission, $\RegSeqRatUp$ readback admission, $\DyadicRatCoreUp$ ledger admission, $\CauchyLimitSealUp$ or $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ route replay, $\Pkg$ provenance, and the local naming row.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet $R$. The rows name the request, selected observation window, rational readback, dyadic ledger, seal, transports, continuations, provenance, and local certificate.

The request and window obligations are read through $q$ and $w$. They ensure that a consumer sees a finite observation demand and exactly the selected $\StreamNameUp$ window used to answer it.

The readback and ledger obligations are read through $r$ and $d$. The regular rational row supplies the observed sequence data, and the dyadic row supplies the comparison and radius ledger used by the route.

The remaining rows $s,h,c,p,n$ expose the seal handoff, componentwise transports, route replay, package provenance, and local naming. Since every coordinate of the packet is accounted for, these obligations exhaust the local certificate surface.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteNameCert\_obligations}

\begin{theorem}[Finite witness-route non-escape]
\label{thm:finite-witness-route-non-escape}
Every accepted $\FiniteWitnessRouteUp$ carrier exports witness information only through the finite rows
$$
q,w,r,d,s,h,c,p,n.
$$
It exports no host-level infinite trace, no quotient stream equality, no selected real value, no unrecorded limit, and no Real-and-Completion consumer route that bypasses the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Project the accepted carrier as $R=(q,w,r,d,s,h,c,p,n)$. The definition lists every exported row of the finite witness route.

The observation side is exhausted by $q,w,r,d$. These rows record the request, selected stream window, regular rational readback, and dyadic comparison ledger.

The consumer side is exhausted by $s,h,c,p,n$. These rows record the seal handoff, transport, continuation, provenance, and local name.

No coordinate remains from which to read an infinite trace, quotient stream equality, selected real value, unrecorded limit, or bypass route. The accepted packet therefore has the stated non-escape boundary.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteNonEscape\_boundary}

\begin{theorem}[Finite witness-route transport stability]
\label{thm:finite-witness-route-transport-stability}
Let
$$
R=(q,w,r,d,s,h,c,p,n)
$$
be an accepted $\FiniteWitnessRouteUp$ carrier. Any $\hsame$ transport recorded in $h$ preserves the request row, selected $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, seal row, continuation routes, package provenance, and local name as one finite route packet for the same Real-and-Completion consumer.
\end{theorem}
\begin{proof}
Read the transport row $h$. Its components are indexed by the coordinates of the carrier, so each displayed row has a matching transport witness.

Transporting $q,w,r,d$ preserves the finite observation side of the route: the request, selected window, regular rational readback, and dyadic ledger stay attached to the same finite demand.

Transporting $s,c,p,n$ preserves the consumer side of the route: the seal handoff, continuation replay, provenance, and local certificate stay attached to that demand. Thus the transported data is again one finite witness-route packet for the same consumer boundary.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteTransport\_stability}

\begin{theorem}[Finite witness-route consumer factorization]
\label{thm:finite-witness-route-consumer-factorization}
Every $\CauchyLimitSealUp$ or $\RealUp$ consumer that accepts a $\FiniteWitnessRouteUp$ packet factors through the selected $\StreamNameUp$ window $w$, the $\RegSeqRatUp$ readback $r$, the $\DyadicRatCoreUp$ ledger $d$, the seal row $s$, the displayed transports $h$, the continuation routes $c$, the package provenance $p$, and the local $\NameCert_{\FiniteWitnessRouteUp}$ row $n$. The consumer receives the finite witness route, not an independent route ledger.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteConsumer\_factorization}
\begin{proof}
Begin with an accepted packet $R=(q,w,r,d,s,h,c,p,n)$. The request row $q$ names the finite demand and the downstream consumer boundary.

The only observation path in the carrier runs through $w,r,d$. The selected stream window exposes the inspected records, the regular rational row exposes their sequence readback, and the dyadic ledger exposes the comparison data.

The handoff to the consumer then runs through $s,h,c,p,n$. These rows seal, transport, replay, package, and name the same finite route. Hence any accepting consumer factors through the displayed packet rather than through an independent ledger.
\end{proof}

\begin{theorem}[Finite witness-route request-window admission]
\label{thm:finite-witness-route-request-window-admission}
Let
$$
R=(q,w,r,d,s,h,c,p,n)
$$
be an accepted $\FiniteWitnessRouteUp$ packet. Every finite witness demand read by a Real-and-Completion consumer must pass through the request row $q$ and the selected $\StreamNameUp$ window row $w$. No consumer may read a route from $R$ without first exposing those two rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-witness-route-carrier}. The row $q$ names the finite witness demand, tolerance scale, and downstream consumer, while $w$ names the selected stream window inspected for that request.

Apply \autoref{thm:finite-witness-route-namecert-obligations}. Its request exposure and selected-window admission clauses make $q$ and $w$ part of the local $\NameCert_{\FiniteWitnessRouteUp}$ surface.

Thus any accepted consumer route that reads the finite witness demand must open $q$ and then the selected window $w$. A route that omits either row is not a read of the displayed packet.
\end{proof}

\begin{theorem}[Finite witness-route regular readback admission]
\label{thm:finite-witness-route-regular-readback-admission}
For an accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
every accepted regular rational readback in the route is exposed through the same selected $\StreamNameUp$ window $w$ and through the $\RegSeqRatUp$ row $r$. The dyadic comparison ledger $d$ records the finite comparison data, and the route imports neither quotient stream equality nor a selected real.
\end{theorem}
\begin{proof}
Read the observation rows $w,r,d$ from \autoref{def:finite-witness-route-carrier}. The selected window $w$ is the inspection boundary, $r$ is the regular rational readback exposed through that window, and $d$ is the dyadic ledger for the finite comparisons.

Use \autoref{thm:finite-witness-route-namecert-obligations} to attach those three rows to the local certificate surface. The readback admission row is therefore tied to the same selected window that answers the request.

Finally apply \autoref{thm:finite-witness-route-non-escape}. It excludes quotient stream equality, selected real values, unrecorded limits, and host-level infinite traces from the accepted packet, so the regular readback remains a finite row of the displayed route.
\end{proof}

\begin{theorem}[Finite witness-route seal-budget exactness]
\label{thm:finite-witness-route-seal-budget-exactness}
For an accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
any $\CauchyLimitSealUp$ or $\RealUp$ seal-budget consumer factors through the request row $q$, the selected $\StreamNameUp$ window $w$, the $\RegSeqRatUp$ readback $r$, the $\DyadicRatCoreUp$ ledger $d$, the seal row $s$, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\FiniteWitnessRouteUp}$ rows before receiving the route. The packet exports no hidden observation budget, quotient stream equality, host real equality, selected host limit, countable-choice row, ambient completeness coordinate, independent dyadic ledger, or standard bridge row.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:finite-witness-route-carrier}. Its displayed coordinates are exactly $q,w,r,d,s,h,c,p,n$, with $q$ naming the request, $w$ the selected stream window, $r$ the regular rational readback, $d$ the dyadic ledger, $s$ the seal row, and $h,c,p,n$ the transport, continuation, package, and naming rows.

The local certificate surface is fixed by \autoref{thm:finite-witness-route-namecert-obligations}. Its clauses expose carrier admission, request exposure, selected-window admission, readback admission, dyadic ledger admission, seal handoff, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local naming row for the same finite packet.

Now apply non-escape, \autoref{thm:finite-witness-route-non-escape}. The route has no coordinate for host infinite traces, quotient stream equality, host real equality, selected host limits, ambient completeness, metric completion, countable choice, independent dyadic ledgers, or standard bridge data.

Finally use request-window admission, \autoref{thm:finite-witness-route-request-window-admission}, and regular readback admission, \autoref{thm:finite-witness-route-regular-readback-admission}. Every seal-budget read enters through $q,w$, then reads only $r,d$ below that selected window before reaching $s,h,c,p,n$. Thus every budget coordinate received by a $\CauchyLimitSealUp$ or $\RealUp$ consumer is already one of the displayed packet rows.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteSeal\_budget\_exactness}

\begin{theorem}[Finite witness-route consumer route exactness]
\label{thm:finite-witness-route-consumer-route-exactness}
Every Real-and-Completion consumer route that accepts a $\FiniteWitnessRouteUp$ packet factors through the displayed rows
$$
q,w,r,d,s,h,c,p,n
$$
in that dependency order. The seal row $s$ cannot be consumed unless the request, selected window, regular readback, and dyadic ledger rows $q,w,r,d$ have already been exposed in the same finite packet.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:finite-witness-route-consumer-factorization}. It sends every accepting $\CauchyLimitSealUp$ or $\RealUp$ consumer through the selected window, regular readback, dyadic ledger, seal, transport, continuation, provenance, and local naming rows of the packet.

Attach \autoref{thm:finite-witness-route-request-window-admission}. The consumer route cannot reach the observation side without the request row $q$ and the selected window row $w$.

Next use \autoref{thm:finite-witness-route-seal-budget-exactness}. The readback and dyadic comparison rows are the finite rows $r$ and $d$, and the seal-budget consumer receives no hidden observation budget, quotient stream equality, selected real value, countable-choice row, ambient completeness coordinate, independent dyadic ledger, or standard bridge row.

The seal row $s$ is therefore downstream of $q,w,r,d$, and the remaining rows $h,c,p,n$ preserve, replay, package, and name the same route. Hence the accepting consumer factors through the displayed dependency order.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteConsumer\_route\_exactness}

\begin{theorem}[Finite witness-route selected-window seal uniqueness]
\label{thm:finite-witness-route-selected-window-seal-uniqueness}
Let
$$
R=(q,w,r,d,s,h,c,p,n)
$$
be an accepted $\FiniteWitnessRouteUp$ packet. Any Real-and-Completion consumer route that reads the seal row $s$ must use the same request row $q$, selected $\StreamNameUp$ window $w$, $\RegSeqRatUp$ readback $r$, and $\DyadicRatCoreUp$ ledger $d$. Inside the packet there is no second selected window, readback, dyadic ledger, or seal handoff available for that consumer route.
\end{theorem}
\begin{proof}
The request-window admission theorem fixes the route entrance. By \autoref{thm:finite-witness-route-request-window-admission}, every accepted read of the finite witness demand opens the request row $q$ and the selected window row $w$.

The regular readback admission theorem then fixes the observation rows below that window. By \autoref{thm:finite-witness-route-regular-readback-admission}, the readback and dyadic comparison data are exactly the displayed rows $r$ and $d$, and the packet imports neither quotient stream equality nor a selected real value.

Finally apply \autoref{thm:finite-witness-route-consumer-route-exactness}. The consumer route reaches the seal row $s$ only after $q,w,r,d$ have been exposed in that dependency order. The non-escape boundary of \autoref{thm:finite-witness-route-non-escape} excludes an external equality, selected real, or route ledger that could furnish a second handoff, so the seal read is unique to the displayed packet rows.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteSelectedWindowSeal\_uniqueness}

\begin{theorem}[Finite witness-route scoped export factorization]
\label{thm:finite-witness-route-scoped-export-factorization}
Every Real-and-Completion consumer that accepts a $\FiniteWitnessRouteUp$ packet factors through the displayed request row $q$, the selected $\StreamNameUp$ window $w$, the $\RegSeqRatUp$ readback $r$, the $\DyadicRatCoreUp$ ledger $d$, the $\CauchyLimitSealUp$ or $\RealUp$ seal row $s$, and the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\FiniteWitnessRouteUp}$ support rows recorded by
$$
R=(q,w,r,d,s,h,c,p,n).
$$
Thus the consumer exports the finite witness route only along the open Real-and-Completion kernel and sibling surface named by those rows.
\end{theorem}
\begin{proof}
The route entrance is the request-window admission of \autoref{thm:finite-witness-route-request-window-admission}. It forces any accepting consumer read of the finite witness demand to expose the request row $q$ and the selected window $w$.

The observation layer is then fixed by \autoref{thm:finite-witness-route-regular-readback-admission}. The readback row is $r$, the comparison ledger is $d$, and both are tied to the same selected $\StreamNameUp$ window rather than to quotient stream equality or a selected real value.

The consumer handoff is supplied by \autoref{thm:finite-witness-route-consumer-route-exactness}. It sends the accepting route through $q,w,r,d,s,h,c,p,n$ in the displayed dependency order.

Finally, \autoref{thm:finite-witness-route-selected-window-seal-uniqueness} rules out a second selected window, readback, dyadic ledger, or seal handoff inside the packet. The only remaining support rows are the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\FiniteWitnessRouteUp}$ rows, so the consumer factors through exactly the stated scoped export surface.
\end{proof}

\begin{theorem}[Finite witness-route kernel scope lock]
\label{thm:finite-witness-route-kernel-scope-lock}
The scoped route of \autoref{thm:finite-witness-route-scoped-export-factorization} exposes no host infinite trace, quotient stream equality, selected real value, unrecorded limit, countable-choice row, or independent dyadic ledger. Its Real-and-Completion exit surface is only the finite packet
$$
R=(q,w,r,d,s,h,c,p,n)
$$
and the sibling rows named by $w,r,d,s,h,c,p,n$.
\end{theorem}
\begin{proof}
Apply \autoref{thm:finite-witness-route-non-escape}. It excludes host-level infinite traces, quotient stream equality, selected real values, unrecorded limits, and bypass consumer routes from every accepted $\FiniteWitnessRouteUp$ packet.

Now use \autoref{thm:finite-witness-route-scoped-export-factorization}. Any accepting Real-and-Completion consumer must factor through the request, selected window, readback, dyadic ledger, seal, transport, continuation, provenance, and local certificate rows of the displayed packet.

Since the factorization already names the only dyadic ledger row and the non-escape theorem removes every bypass route, there is no remaining coordinate for a choice row or independent ledger. The kernel scope is therefore locked to the finite route and to the sibling rows carried by that route.
\end{proof}

\begin{theorem}[Finite witness-route scoped support rows]
\label{thm:finite-witness-route-scoped-support-rows}
For every accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
the support scope is exactly the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows together with the $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$, and $\RealUp$ faces read by $w,r,d,s$. The scope contains no host-level infinite trace, quotient stream equality, selected real value, ambient Real completeness, metric completeness, countable choice, or standard bridge row.
\end{theorem}
\begin{proof}
Project the carrier through \autoref{def:finite-witness-route-carrier}. Its coordinates are the request row $q$, selected window $w$, regular rational readback $r$, dyadic ledger $d$, seal row $s$, transports $h$, routes $c$, provenance $p$, and local certificate $n$.

Apply \autoref{thm:finite-witness-route-namecert-obligations}. The certificate surface accounts for carrier admission, request exposure, selected-window admission, readback admission, ledger admission, seal handoff, transport, route replay, package provenance, and the local naming row.

The sibling faces are the ones named by those coordinates: $\StreamNameUp$ through $w$, $\RegSeqRatUp$ through $r$, $\DyadicRatCoreUp$ through $d$, and $\CauchyLimitSealUp$ or $\RealUp$ through $s$. Apply \autoref{thm:finite-witness-route-transport-stability} to keep those rows attached under the componentwise $\hsame$ transports.

Finally use \autoref{thm:finite-witness-route-non-escape}. It removes host infinite traces, quotient stream equality, selected real values, unrecorded limits, and bypass routes; the same packet has no coordinate for ambient Real completeness, metric completeness, countable choice, or a standard bridge row. The displayed rows therefore give the whole scoped support.
\end{proof}

\begin{theorem}[Finite witness-route bridged standard surface]
\label{thm:finite-witness-route-bridged-standard-surface}
For every accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
every bridge-facing consumer must first read the finite request $q$, the selected $\StreamNameUp$ window $w$, the $\RegSeqRatUp$ readback $r$, the $\DyadicRatCoreUp$ ledger $d$, the seal row $s$, and the support rows $q,w,r,d,s,h,c,p,n$. This is the standard-surface precondition for later bridge checking. It asserts no $\RealUp$ completeness theorem, no standard bridge theorem, and no bridgeChecked verification status.
\end{theorem}
\begin{proof}
First use scoped export factorization. It orders every accepted Real-and-Completion consumer through $q,w,r,d,s,h,c,p,n$ before the route can expose any public exit surface.

Next apply kernel scope lock and scoped support rows. These theorems identify the bridge-facing support as the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows together with the named $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$, and $\RealUp$ faces, while excluding host traces, quotient equality, selected real values, countable choice, metric completeness, and standard bridge rows.

Finally compare with the Real-and-Completion scope and public NameCert export. They package the same finite route as the public surface of the certificate. Therefore a later bridge checker may require this surface as input, but the theorem itself does not supply Real completeness or a checked standard bridge.
\end{proof}

\begin{theorem}[Finite witness-route Real and Completion scope]
\label{thm:finite-witness-route-real-completion-scope}
Every Phase Real or Completion consumer read that accepts a $\FiniteWitnessRouteUp$ packet factors through the finite route
$$
q,w,r,d,s,h,c,p,n.
$$
The read may use the named $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$, and $\RealUp$ faces carried by those rows, but it receives no independent real value, limit object, completeness principle, metric completion row, quotient stream equality, countable-choice selector, or standard bridge route.
\end{theorem}
\begin{proof}
Start with the consumer factorization theorem, \autoref{thm:finite-witness-route-consumer-factorization}. It sends every accepting $\CauchyLimitSealUp$ or $\RealUp$ consumer through the selected window, readback, ledger, seal, transport, continuation, provenance, and local naming rows.

The request and observation side is fixed by \autoref{thm:finite-witness-route-request-window-admission} and \autoref{thm:finite-witness-route-regular-readback-admission}. Thus the consumer first reads $q,w$ and then the regular rational and dyadic rows $r,d$.

The seal side is fixed by \autoref{thm:finite-witness-route-consumer-route-exactness}. The consumer reaches $s$ only after $q,w,r,d$, and the remaining rows $h,c,p,n$ preserve, replay, package, and name that same finite route.

Apply \autoref{thm:finite-witness-route-scoped-support-rows} for the scope boundary. Since the accepted packet has only the displayed support rows and sibling faces, no Phase Real or Completion read can acquire an independent real value, limit object, completeness principle, metric completion row, quotient equality, countable-choice selector, or standard bridge route.
\end{proof}

\begin{theorem}[Finite witness-route StreamName source lock]
\label{thm:finite-witness-route-streamname-source-lock}
Every accepted Real-and-Completion consumer read of a $\FiniteWitnessRouteUp$ packet first exposes the request row $q$ and the selected $\StreamNameUp$ window $w$ before any $\RegSeqRatUp$ readback row or $\DyadicRatCoreUp$ ledger row is readable. The read has no route that starts from $r$ or $d$, and it cannot bypass the selected window through a host trace, quotient stream equality, selected real value, countable-choice selector, or independent dyadic ledger.
\end{theorem}
\begin{proof}
Begin with request-window admission, \autoref{thm:finite-witness-route-request-window-admission}. It makes the entrance to the finite witness packet the displayed request row $q$ together with the selected window $w$.

Next apply regular readback admission, \autoref{thm:finite-witness-route-regular-readback-admission}. The regular rational readback $r$ and dyadic ledger $d$ are admitted only as rows below that selected window, so neither row can be the first readable Real-and-Completion coordinate.

Finally use non-escape, \autoref{thm:finite-witness-route-non-escape}. Since the carrier contains no host trace, quotient stream equality, selected real, choice selector, or separate dyadic ledger, every accepted consumer read is source-locked to $q,w$ before it reaches $r,d$.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteStreamName\_source\_lock}

\begin{theorem}[Finite witness-route RegSeq budget exhaustion]
\label{thm:finite-witness-route-regseq-budget-exhaustion}
For every accepted Real-and-Completion read of
$$
R=(q,w,r,d,s,h,c,p,n),
$$
the rows $r$ and $d$ exhaust the $\RegSeqRatUp$ and $\DyadicRatCoreUp$ budget segment after the selected window $w$. The consumer receives no quotient stream equality, selected real, countable choice, or independent dyadic ledger beyond the displayed readback and ledger rows.
\end{theorem}
\begin{proof}
Use regular readback admission, \autoref{thm:finite-witness-route-regular-readback-admission}, to identify the observation segment below $w$. The regular rational row is exactly $r$, and the dyadic comparison ledger is exactly $d$.

Now apply consumer route exactness, \autoref{thm:finite-witness-route-consumer-route-exactness}. A Real-and-Completion consumer reaches the seal side only after the displayed request, window, readback, and dyadic rows have been read in that order.

The kernel scope lock, \autoref{thm:finite-witness-route-kernel-scope-lock}, removes every attempt to extend that segment by an ambient equality or selector. Thus the budget after $w$ is exhausted by $r,d$, with no remaining coordinate for quotient stream equality, a selected real, countable choice, or a second dyadic ledger.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteRegSeq\_budget\_exhaustion}

\begin{theorem}[Finite witness-route Real seal factorization]
\label{thm:finite-witness-route-real-seal-factorization}
Every $\CauchyLimitSealUp$ or $\RealUp$ consumer of an accepted $\FiniteWitnessRouteUp$ packet factors through the ordered finite route
$$
q,w,r,d,s,h,c,p,n.
$$
The consumer first reads the request and selected $\StreamNameUp$ window, then the $\RegSeqRatUp$ readback and $\DyadicRatCoreUp$ ledger, then the seal row, transport, continuation, provenance, and local naming rows. Hence the consumer receives only the finite L10 route and no independent limit object, real value, completeness principle, quotient equality, countable-choice selector, host trace, metric-completion row, or standard bridge route.
\end{theorem}
\begin{proof}
First apply the StreamName source lock, \autoref{thm:finite-witness-route-streamname-source-lock}. It forces every accepted seal or real consumer to enter through $q,w$ before any regular or dyadic row is readable.

Next use RegSeq budget exhaustion, \autoref{thm:finite-witness-route-regseq-budget-exhaustion}. The rows $r,d$ are the whole observation budget below $w$, so the route cannot add quotient equality, a selected real, countable choice, or a second dyadic ledger before reaching the seal.

Now apply Real and Completion scope, \autoref{thm:finite-witness-route-real-completion-scope}, together with consumer route exactness, \autoref{thm:finite-witness-route-consumer-route-exactness}. These theorems place $s,h,c,p,n$ after $q,w,r,d$ and keep the consumer on the displayed CauchyLimitSeal and Real faces.

Finally invoke scoped support rows and kernel scope lock, \autoref{thm:finite-witness-route-scoped-support-rows} and \autoref{thm:finite-witness-route-kernel-scope-lock}. The accepted packet has no coordinate for an external limit, real, completeness principle, host trace, metric-completion row, or standard bridge, so the ordered finite route is the entire consumer factorization.
\end{proof}

\begin{theorem}[Finite witness-route seal boundary scope]
\label{thm:finite-witness-route-seal-boundary-scope}
For every accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
the seal row $s$ is downstream of the request row $q$, selected $\StreamNameUp$ window $w$, regular $\RegSeqRatUp$ readback $r$, dyadic $\DyadicRatCoreUp$ ledger $d$, componentwise transport row $h$, continuation row $c$, package provenance $p$, and local naming row $n$. No Real or Completion consumer may read $s$ as a boundary supplied by host traces, quotient equality, selected real values, ambient completeness, metric completeness, countable choice, or a standard bridge row.
\end{theorem}
\begin{proof}
The route entrance is determined by \autoref{thm:finite-witness-route-request-window-admission}. Any accepted finite witness demand read opens the request row $q$ and the selected window row $w$.

The observation data below the window is determined by \autoref{thm:finite-witness-route-regular-readback-admission}. The readback row is $r$, the comparison ledger is $d$, and neither quotient stream equality nor a selected real value is imported into the packet.

The consumer dependency order is determined by \autoref{thm:finite-witness-route-consumer-route-exactness} together with \autoref{thm:finite-witness-route-selected-window-seal-uniqueness}. The seal row $s$ is read only after $q,w,r,d$, and there is no second selected window, readback, dyadic ledger, or seal handoff inside the carrier.

Finally apply \autoref{thm:finite-witness-route-real-completion-scope}. The remaining rows $h,c,p,n$ are the only transport, continuation, provenance, and naming rows available to the seal consumer, and the excluded host, quotient, completeness, choice, and bridge coordinates have no public entry point.
\end{proof}

\begin{theorem}[Finite witness-route four-face exit exactness]
\label{thm:finite-witness-route-four-face-exit-exactness}
Every current Phase Real and Completion consumer that reaches a $\CauchyLimitSealUp$ or $\RealUp$ seal through an accepted $\FiniteWitnessRouteUp$ packet factors through exactly the selected $\StreamNameUp$ window, the $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ ledger, and the $\CauchyLimitSealUp$ or $\RealUp$ seal row carried by that packet. Its support is only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the same finite carrier.

No such consumer imports host infinite traces, quotient stream equality, selected real values, ambient Real completeness, metric completeness, countable choice, independent dyadic ledgers, or standard bridge rows.
\end{theorem}
\begin{proof}
Apply \autoref{thm:finite-witness-route-scoped-export-factorization}. It orders the route through the request row, selected window, regular readback, dyadic ledger, seal row, componentwise transport, continuation, package provenance, and local naming row.

Apply \autoref{thm:finite-witness-route-scoped-support-rows}. The only support rows available along that ordered route are the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows together with the named $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$, and $\RealUp$ faces.

Now use \autoref{thm:finite-witness-route-real-completion-scope}. It restricts Phase Real and Completion consumers to the same finite route and excludes independent real values, limit objects, completeness principles, metric completion rows, quotient equality, countable-choice selectors, and standard bridge routes.

Finally apply \autoref{thm:finite-witness-route-seal-boundary-scope}. The seal read is downstream of the selected window, readback, ledger, transport, continuation, provenance, and naming rows, so the entrance to the $\CauchyLimitSealUp$ or $\RealUp$ seal is exactly the four-face route displayed in the statement.
\end{proof}

\begin{theorem}[Finite witness-route public NameCert export]
\label{thm:finite-witness-route-public-namecert-export}
For every accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
the public certificate $\NameCert_{\FiniteWitnessRouteUp}$ exports exactly the finite packet $R$ and the four L10 faces $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\CauchyLimitSealUp$ or $\RealUp$ displayed by $w,r,d,s$. Its support is only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows of the same finite carrier.

No public consumer of this certificate receives a host infinite trace, quotient stream equality, selected real value, ambient Real completeness, metric completeness, countable-choice selector, independent dyadic ledger, or standard bridge row.
\end{theorem}
\begin{proof}
First read the public certificate obligations. By \autoref{thm:finite-witness-route-namecert-obligations}, the local naming row accounts for carrier admission, request exposure, selected-window admission, readback admission, dyadic ledger admission, seal handoff, componentwise transport, route replay, and package provenance for the same finite packet $R$.

Next restrict the support rows. Apply \autoref{thm:finite-witness-route-scoped-support-rows}; it identifies the available support as the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows together with the named $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$, and $\RealUp$ faces.

Now factor the seal side through the Real and Completion boundary. By \autoref{thm:finite-witness-route-real-seal-factorization}, every $\CauchyLimitSealUp$ or $\RealUp$ consumer follows the ordered finite route $q,w,r,d,s,h,c,p,n$ and gains no independent limit object, real value, completeness principle, quotient equality, countable-choice selector, host trace, metric-completion row, or standard bridge route.

Finally use \autoref{thm:finite-witness-route-four-face-exit-exactness}. The public exit surface is exactly the selected $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\CauchyLimitSealUp$ or $\RealUp$ seal row carried by the packet. Combining these four results repacks the same finite route as the public $\NameCert_{\FiniteWitnessRouteUp}$ export, with no coordinate left for the excluded host, quotient, real-value, completeness, choice, ledger, or bridge data.
\end{proof}

\begin{theorem}[Finite witness-route choice-refusal boundary]
\label{thm:finite-witness-route-choice-refusal-boundary}
Every accepted $\FiniteWitnessRouteUp$ Real-and-Completion consumer route remains on the displayed finite rows
$$
q,w,r,d,s,h,c,p,n.
$$
No countable-choice selector, selected host limit, quotient stream equality, or independent real value can be inserted between the selected $\StreamNameUp$ window $w$ and the $\CauchyLimitSealUp$ or $\RealUp$ seal row $s$.
\end{theorem}
\begin{proof}
Begin with request-window admission, \autoref{thm:finite-witness-route-request-window-admission}. It forces every accepted finite witness demand to expose $q$ and the selected $\StreamNameUp$ window $w$ before any downstream read.

Use regular readback admission and consumer route exactness next. By \autoref{thm:finite-witness-route-regular-readback-admission}, the route reads $r,d$ through the same selected window; by \autoref{thm:finite-witness-route-consumer-route-exactness}, the seal row $s$ is downstream of $q,w,r,d$ in the same finite packet.

Now apply public NameCert export, \autoref{thm:finite-witness-route-public-namecert-export}. The exported certificate is exactly the displayed packet and the four L10 faces carried by $w,r,d,s$.

Finally use non-escape, \autoref{thm:finite-witness-route-non-escape}. Since no countable-choice selector, selected host limit, quotient stream equality, or independent real value is a coordinate of the carrier, none can be inserted between the selected window and the seal.
\end{proof}

\begin{theorem}[Finite witness-route L10 terminal four-object handoff]
\label{thm:finite-witness-route-l10-terminal-four-object-handoff}
Every accepted $\FiniteWitnessRouteUp$ consumer that reaches a Real-facing witness route reaches it through the displayed L10 handoff
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \DyadicRatCoreUp \longrightarrow \CauchyLimitSealUp \text{ or } \RealUp .
$$
Equivalently, for every accepted packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
the consumer first reads the selected $\StreamNameUp$ finite window $w$, then the $\RegSeqRatUp$ readback $r$, then the $\DyadicRatCoreUp$ comparison ledger $d$, then the $\CauchyLimitSealUp$ seal boundary or $\RealUp$ terminal seal row $s$. The remaining rows $h,c,p,n$ transport, replay, package, and name that same finite route, and no Real-facing witness is exported outside this four-object path.
\end{theorem}
\begin{proof}
First apply consumer factorization, \autoref{thm:finite-witness-route-consumer-factorization}. It places every accepting $\CauchyLimitSealUp$ or $\RealUp$ consumer on the packet rows $w,r,d,s,h,c,p,n$ rather than on an external witness source.

The entrance is fixed by request-window admission, \autoref{thm:finite-witness-route-request-window-admission}. The selected finite window is $w$, so the route cannot begin at a quotient stream, host trace, selected real, or hidden selector.

The observation segment is fixed by regular readback admission, \autoref{thm:finite-witness-route-regular-readback-admission}, and RegSeq budget exhaustion, \autoref{thm:finite-witness-route-regseq-budget-exhaustion}. These identify $r$ as the regular readback and $d$ as the dyadic comparison ledger below the same selected window.

Finally use seal-budget exactness, \autoref{thm:finite-witness-route-seal-budget-exactness}, and public NameCert export, \autoref{thm:finite-witness-route-public-namecert-export}. The seal row $s$ is the downstream CauchyLimitSeal or Real terminal face, while $h,c,p,n$ only transport, replay, package, and name the already displayed route.
\end{proof}

\begin{theorem}[Finite witness-route no-choice Real completion boundary]
\label{thm:finite-witness-route-no-choice-real-completion-boundary}
The L10 handoff in \autoref{thm:finite-witness-route-l10-terminal-four-object-handoff} exports no quotient stream, hidden selector, countable-choice row, host real equality, ambient completeness row, selected host limit, independent dyadic ledger, or standard bridge route. Its Real-facing witness is only the public route through $w,r,d,s,h,c,p,n$.
\end{theorem}
\begin{proof}
Begin with the choice-refusal boundary, \autoref{thm:finite-witness-route-choice-refusal-boundary}. It keeps every accepted Real-and-Completion consumer on the displayed finite rows and blocks countable-choice selectors, selected host limits, quotient stream equality, and independent real values between $w$ and $s$.

Next apply seal boundary scope, \autoref{thm:finite-witness-route-seal-boundary-scope}. The seal row is downstream of $q,w,r,d,h,c,p,n$, so it cannot be replaced by an ambient completeness row, host real equality, metric completion row, or standard bridge.

Finally use scoped support rows, \autoref{thm:finite-witness-route-scoped-support-rows}, together with public NameCert export, \autoref{thm:finite-witness-route-public-namecert-export}. The certificate exposes only the finite packet and the four L10 faces, leaving no coordinate for the excluded quotient, selector, choice, host-equality, completeness, ledger, or bridge data.
\end{proof}

\begin{theorem}[Finite witness-route cofinal-window factorization]
\label{thm:finite-witness-route-cofinal-window-factorization}
Every cofinal-window read admitted by an accepted $\FiniteWitnessRouteUp$ packet factors through the same displayed L10 route
$$
q,w,r,d,s,h,c,p,n.
$$
The read enters through the $\StreamNameUp$ finite window $w$, obtains its $\RegSeqRatUp$ readback through $r$, checks its $\DyadicRatCoreUp$ comparisons through $d$, and reaches the $\CauchyLimitSealUp$ or $\RealUp$ terminal row through $s$. It cannot bypass the $\StreamNameUp$ or $\DyadicRatCoreUp$ rows through a quotient stream, ambient real, hidden selector, independent ledger, or host completeness principle.
\end{theorem}
\begin{proof}
The source of a cofinal-window read is controlled by the StreamName source lock, \autoref{thm:finite-witness-route-streamname-source-lock}. Any accepted Real-and-Completion read must expose $q,w$ before $r$ or $d$ is readable.

The regular and dyadic layers are then controlled by regular readback admission, \autoref{thm:finite-witness-route-regular-readback-admission}, and RegSeq budget exhaustion, \autoref{thm:finite-witness-route-regseq-budget-exhaustion}. They force the cofinal readback through $r$ and the comparison ledger through $d$ below the same selected window.

The seal side is controlled by Real seal factorization, \autoref{thm:finite-witness-route-real-seal-factorization}, and seal boundary scope, \autoref{thm:finite-witness-route-seal-boundary-scope}. The route reaches only the CauchyLimitSeal or Real row $s$ carried by the packet.

Apply the L10 terminal four-object handoff, \autoref{thm:finite-witness-route-l10-terminal-four-object-handoff}, to assemble the same ordered path. The preceding source, readback, ledger, and seal constraints leave no bypass around the $\StreamNameUp$ or $\DyadicRatCoreUp$ rows.
\end{proof}

\begin{theorem}[Finite witness-route CofinalModulusSeal handoff]
\label{thm:finite-witness-route-cofinal-modulus-handoff}
Every accepted $\FiniteWitnessRouteUp$ Real-facing packet that consumes a cofinal modulus route reads $\CofinalModulusSealUp$ through the same selected $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\CauchyLimitSealUp$ or $\RealUp$ seal row, $\BHist$ packet, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row already carried by
$$
R=(q,w,r,d,s,h,c,p,n).
$$
No cofinal modulus handoff may insert a quotient stream, selected host limit, countable-choice selector, ambient completeness row, detached window service, or separate completion operator between the finite witness route and the seal boundary.
\end{theorem}
\begin{proof}
Start with cofinal-window factorization, \autoref{thm:finite-witness-route-cofinal-window-factorization}. It places every cofinal-window read admitted by $R$ on the displayed rows $q,w,r,d,s,h,c,p,n$, with entrance through $w$, readback through $r$, dyadic comparison through $d$, and seal access through $s$.

Next apply the L10 terminal four-object handoff, \autoref{thm:finite-witness-route-l10-terminal-four-object-handoff}. The Real-facing consumer therefore reaches the terminal side only through $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\CauchyLimitSealUp$ or $\RealUp$, while $h,c,p,n$ transport, replay, package, and name that same finite route.

Finally use the CofinalModulusSeal L10 exit boundary, \autoref{thm:cofinal-modulus-seal-l10-exit-boundary}. Its public exit is the finite cofinal modulus route and it excludes quotient streams, selected host limits, countable choice, ambient completeness, detached windows, and separate completion operators. Combining the two finite route descriptions gives the stated handoff through the same packet rows and support structures.
\end{proof}

\begin{theorem}[Finite witness-route Stream-RegSeq budget factorization]
\label{thm:finite-witness-route-stream-regseq-budget-factorization}
For an accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n),
$$
the selected $\StreamNameUp$ window $w$ and the $\RegSeqRatUp$ readback row $r$ are the only budget-bearing entrance into the $\CofinalModulusSealUp$ consumer route. Any Real-facing or completion-facing cofinal modulus read admitted by $R$ must pass first through $q,w$, then through $r,d$, and then through the seal and support rows $s,h,c,p,n$.
\end{theorem}
\begin{proof}
Use request-window admission, \autoref{thm:finite-witness-route-request-window-admission}. It makes $q,w$ mandatory before a Real-and-Completion consumer reads any finite witness demand.

Then apply regular readback admission, \autoref{thm:finite-witness-route-regular-readback-admission}. The regular rational readback is $r$, the dyadic comparison ledger is $d$, and both sit below the same selected $\StreamNameUp$ window rather than below quotient stream equality or a selected real value.

It remains to connect this entrance with the cofinal modulus route. The handoff theorem \autoref{thm:finite-witness-route-cofinal-modulus-handoff} consumes $\CofinalModulusSealUp$ through the same $w,r,d,s,h,c,p,n$ rows. Thus the StreamName window and RegSeqRat readback are the only budget-bearing entry coordinates available to that route.
\end{proof}

\begin{theorem}[Finite witness-route Real Completion readiness]
\label{thm:finite-witness-route-real-completion-readiness}
The combined $\FiniteWitnessRouteUp$ route through $\CofinalModulusSealUp$ is ready as a Phase Real and Completion paper target over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$, and $\RealUp$: its consumer surface is the finite packet $q,w,r,d,s,h,c,p,n$ together with the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. This readiness does not claim quotient stream equality, selected host limits, countable choice, ambient completeness, or a $\StdBridge$ route.
\end{theorem}
\begin{proof}
First combine the CofinalModulusSeal handoff, \autoref{thm:finite-witness-route-cofinal-modulus-handoff}, with Stream-RegSeq budget factorization, \autoref{thm:finite-witness-route-stream-regseq-budget-factorization}. These results identify the selected window and readback entrance and keep the cofinal modulus consumer on the finite packet rows.

Now apply the no-choice Real completion boundary, \autoref{thm:finite-witness-route-no-choice-real-completion-boundary}. It states that the L10 handoff exports no quotient stream, hidden selector, countable-choice row, host real equality, ambient completeness row, selected host limit, independent dyadic ledger, or standard bridge route.

The two positive factorization results and the refusal boundary together give a precise paper target for later Real-and-Completion formalization: the route is exposed through the displayed finite rows and support structures, while the excluded quotient, choice, host-limit, completeness, and bridge claims remain outside the chapter.
\end{proof}

\begin{theorem}[Finite witness-route Real completion exact-boundary handoff]
\label{thm:finite-witness-route-real-completion-exact-boundary-handoff}
Let an accepted $\FiniteWitnessRouteUp$ packet
$$
R=(q,w,r,d,s,h,c,p,n)
$$
be consumed by an accepted $\RealCompletionExactBoundaryUp$ packet
$$
B=(L,K,J,S,W,R,D,E,H,C,P,N).
$$
After the governing seal, classifier, witness-ledger, and budget rows $L,K,J,S$ of $B$ have been displayed, the handoff identifies the route faces $w,r,d,s$ with the exact-boundary faces $W,R,D,E$. The consumer remains on the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the two packets. It receives no quotient stream, host real equality, selected limit, countable-choice row, ambient completeness row, metric-completeness row, independent dyadic ledger, or standard bridge row.
\end{theorem}
\begin{proof}
First use the L10 terminal four-object handoff, \autoref{thm:finite-witness-route-l10-terminal-four-object-handoff}. It places the finite route on the four displayed faces $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\CauchyLimitSealUp$ or $\RealUp$ through $w,r,d,s$, with $h,c,p,n$ carrying the transport, continuation, provenance, and local naming rows.

Next apply the Real Completion readiness theorem, \autoref{thm:finite-witness-route-real-completion-readiness}, together with the no-choice Real completion boundary, \autoref{thm:finite-witness-route-no-choice-real-completion-boundary}. These results keep the route on the same finite packet rows and exclude quotient streams, hidden selectors, countable-choice rows, host real equality, ambient completeness, selected host limits, independent dyadic ledgers, and bridge routes.

Finally consume the route through the exact-boundary packet. The four-face exactness theorem \autoref{thm:real-completion-exact-boundary-four-face-exactness} identifies the only boundary faces after $L,K,J,S$ as $W,R,D,E$. The terminal-consumer exactness theorem \autoref{thm:real-completion-exact-boundary-terminal-consumer-exactness} keeps the consumer on $H,C,P,N$, and the no-completeness-escape theorem \autoref{thm:real-completion-exact-boundary-no-completeness-escape} refuses quotient, host-equality, selected-limit, choice, completeness, and bridge rows. Composing these route descriptions gives the stated identification of $w,r,d,s$ with $W,R,D,E$ under the displayed structural rows.
\end{proof}

\closureat{FiniteWitnessRouteUp}{seedStr}
\closureat{FiniteWitnessRouteUp}{obligationStr}
\closureat{FiniteWitnessRouteUp}{scopedStr}
\closureat{FiniteWitnessRouteUp}{publicStr}
\closureat{FiniteWitnessRouteUp}{bridgedStr}[BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteRegSeq\_budget\_exhaustion]

\begin{closurestatus}{\FiniteWitnessRouteUp}
  \constructivestory{A $\FiniteWitnessRouteUp$ packet is generated from a finite witness request, selected $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ observation ledger, $\CauchyLimitSealUp$ or $\RealUp$ seal row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{\autoref{def:finite-witness-route-carrier}, \autoref{thm:finite-witness-route-namecert-obligations}, \autoref{thm:finite-witness-route-non-escape}, \autoref{thm:finite-witness-route-transport-stability}, \autoref{thm:finite-witness-route-consumer-factorization}, \autoref{thm:finite-witness-route-request-window-admission}, \autoref{thm:finite-witness-route-regular-readback-admission}, \autoref{thm:finite-witness-route-seal-budget-exactness}, \autoref{thm:finite-witness-route-consumer-route-exactness}, \autoref{thm:finite-witness-route-selected-window-seal-uniqueness}, \autoref{thm:finite-witness-route-scoped-export-factorization}, \autoref{thm:finite-witness-route-kernel-scope-lock}, \autoref{thm:finite-witness-route-scoped-support-rows}, \autoref{thm:finite-witness-route-real-completion-scope}, \autoref{thm:finite-witness-route-streamname-source-lock}, \autoref{thm:finite-witness-route-regseq-budget-exhaustion}, \autoref{thm:finite-witness-route-real-seal-factorization}, \autoref{thm:finite-witness-route-seal-boundary-scope}, \autoref{thm:finite-witness-route-four-face-exit-exactness}, \autoref{thm:finite-witness-route-public-namecert-export}, \autoref{thm:finite-witness-route-l10-terminal-four-object-handoff}, \autoref{thm:finite-witness-route-no-choice-real-completion-boundary}, \autoref{thm:finite-witness-route-cofinal-window-factorization}, \autoref{thm:finite-witness-route-cofinal-modulus-handoff}, \autoref{thm:finite-witness-route-stream-regseq-budget-factorization}, \autoref{thm:finite-witness-route-real-completion-readiness}, and \autoref{thm:finite-witness-route-real-completion-exact-boundary-handoff} bind the finite route packet from request exposure through selected-window readback, dyadic ledger admission, seal-budget exactness, scoped support, Real-and-Completion consumer factorization, kernel scope locking, seal-boundary consumption, four-face exit exactness, public $\NameCert_{\FiniteWitnessRouteUp}$ export, terminal four-object handoff, no-choice Real completion boundary, cofinal-window factorization, CofinalModulusSeal handoff, Stream-RegSeq budget entrance, Real Completion readiness, and exact-boundary handoff.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.FiniteWitnessRouteUp.FiniteWitnessRouteRegSeq\_budget\_exhaustion}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close RealUp completeness, uniqueness of completed limits, quotient stream equality, infinite traces, independent route ledgers, Lean verification, or bridge checking.}
  \upgradepath{Mature treatment requires example coverage, a completeness-facing consumer surface, and downstream route integration for the same $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyLimitSealUp$ or $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route.}
\end{closurestatus}
