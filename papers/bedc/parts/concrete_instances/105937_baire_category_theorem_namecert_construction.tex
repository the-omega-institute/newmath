\chapter{A Concrete Naming Certificate for $\BaireCategoryTheoremUp$}
\label{ch:concrete-instances-baire-category-theorem-namecert}
\origin{human}

Following Baire, Oxtoby, and Bishop--Bridges, the $\BaireCategoryTheoremUp$ packet records the theorem-level complete-metric Baire route as finite NameCert data. It sits over the dense-open schedule of $\BaireCategoryUp$ in \autoref{ch:concrete-instances-bairecategory-namecert}, the inspected complete-metric route of $\CompleteMetricBaireSurfaceUp$ in \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert}, the Cauchy service of $\CompleteMetricUp$ in \autoref{ch:concrete-instances-completemetric-namecert}, the carrier comparisons of $\MetricSpaceUp$ in \autoref{ch:concrete-instances-metric-namecert}, the finite observation windows of $\StreamNameUp$ in \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of $\RegSeqRatUp$ in \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of $\RealUp$ in \autoref{ch:concrete-instances-real-namecert}. The packet names the finite route by which a complete-metric dense-open construction is exposed to downstream theorem consumers; it does not assert a host intersection object, a countable-choice selector, an open-cover compactness theorem, or ambient completeness outside the displayed route.

\begin{definition}[Baire category theorem carrier]
\label{def:baire-category-theorem-carrier}
A $\BaireCategoryTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(B,U,K,M,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BaireCategoryUp$ dense-open schedule row, including the finite predecessor-ball and refinement data offered by the Baire-category packet.
\item $U$ is the $\CompleteMetricBaireSurfaceUp$ inspection row, exposing the complete-metric Baire surface through dense-open, Cauchy-thread, metric, stream, regular-rational, and real-seal reads.
\item $K$ is the $\CompleteMetricUp$ row naming the Cauchy-thread and limit-service data used by the theorem-level route.
\item $M$ is the $\MetricSpaceUp$ row naming the metric carrier, ball classifier, distance comparisons, and shrinkage checks consumed by $B,U,K$.
\item $S$ is the $\StreamNameUp$ finite-window row for the schedule, centers, radii, and requested tolerances.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular-rational center, radius, and tolerance data.
\item $E$ is the $\RealUp$ terminal seal reached only after $B,U,K,M,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the theorem route and all structural rows.
\item $C$ records displayed $\Cont$ replay of the dense-open, complete-metric, metric, finite-window, readback, and real-seal route.
\item $P$ is the $\Pkg$ provenance over $\BaireCategoryTheoremUp$, $\BaireCategoryUp$, $\CompleteMetricBaireSurfaceUp$, $\CompleteMetricUp$, $\MetricSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BaireCategoryTheoremUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host countable intersection, countable choice, a selected point outside the displayed readback, open-cover compactness, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Baire category theorem NameCert obligations]
\label{thm:baire-category-theorem-namecert-obligations}
For an accepted $\BaireCategoryTheoremUp$ carrier
$$
T=(B,U,K,M,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\BaireCategoryTheoremUp}$ surface consists exactly of carrier admission for the displayed theorem route, dense-open schedule exposure through $B$, complete-metric Baire inspection through $U$, Cauchy-thread service through $K$, metric comparison exposure through $M$, finite-window and regular-rational readback through $S,R$, terminal real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:baire-category-theorem-carrier}. Its analytic-facing rows are exactly $B,U,K,M,S,R,E$, followed by the structural rows $H,C,P,N$.

The dense-open schedule is exposed first through $B$ and then inspected through $U$. The complete-metric Baire surface row binds that schedule to a displayed Cauchy thread, so the theorem-level route cannot begin from an unrecorded host intersection.

The rows $K$ and $M$ provide the complete-metric service and metric comparisons consumed by the route. The rows $S$ and $R$ then record the finite windows and regular-rational readback before the terminal $\RealUp$ seal $E$ is available.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no other carrier coordinate exists, these rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Complete-metric Baire theorem nonescape]
\label{thm:baire-category-theorem-complete-metric-nonescape}
Every theorem-level consumer of an accepted $\BaireCategoryTheoremUp$ packet factors through the displayed route
$$
\BaireCategoryUp \longmapsto \CompleteMetricBaireSurfaceUp \longmapsto \CompleteMetricUp \longmapsto \MetricSpaceUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
and through the structural rows $\BHist,\hsame,\Cont,\Pkg,\NameCert$. The consumer receives the finite complete-metric Baire route carried by $T=(B,U,K,M,S,R,E,H,C,P,N)$, not a host countable intersection, countable-choice selector, ambient complete-metric theorem, open-cover compactness theorem, or ambient completeness theorem for $\RealUp$.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:baire-category-theorem-namecert-obligations}. They force the consumer to read the dense-open row $B$, the complete-metric Baire inspection row $U$, the complete-metric Cauchy service $K$, the metric comparison row $M$, the finite-window row $S$, the regular-rational readback $R$, and the terminal real seal $E$ in the displayed order.

The surface row $U$ imports only the inspected route already named by $\CompleteMetricBaireSurfaceUp$. Thus the theorem-level packet packages that route for downstream use without creating a second intersection or selector coordinate.

The structural rows $H,C,P,N$ preserve the same route under accepted transport, replay the same observations, source the same package, and name the same local certificate. Coordinate exhaustion then blocks every excluded host object in the statement.
\end{proof}

\begin{definition}[BaireCategoryTheorem finite certificate route]
\label{def:baire-category-theorem-finite-certificate-route}
For an accepted $\BaireCategoryTheoremUp$ carrier $T=(B,U,K,M,S,R,E,H,C,P,N)$, the finite certificate route is the ordered row
$$
\BaireCategoryUp \longmapsto \CompleteMetricBaireSurfaceUp \longmapsto \CompleteMetricUp \longmapsto \MetricSpaceUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp.
$$
The source coordinate $B$ supplies the $\BaireCategoryUp$ dense-open schedule, $U$ supplies the $\CompleteMetricBaireSurfaceUp$ inspection row, $K$ and $M$ supply the $\CompleteMetricUp$ and $\MetricSpaceUp$ service rows, $S$ and $R$ supply the finite $\StreamNameUp$ and $\RegSeqRatUp$ readback, and $E$ supplies the terminal $\RealUp$ seal. The structural rows $H,C,P,N$ transport, replay, source, and name only this route.
\end{definition}

\begin{theorem}[BaireCategoryTheorem route nonescape]
\label{thm:baire-category-theorem-route-nonescape}
Every theorem-level read admitted by an accepted $\BaireCategoryTheoremUp$ carrier is exhausted by the finite certificate route of \autoref{def:baire-category-theorem-finite-certificate-route}. In particular, any downstream complete-metric, compactness, or Baire-surface consumer receives the displayed chain through $\BaireCategoryUp$, $\CompleteMetricBaireSurfaceUp$, $\CompleteMetricUp$, $\MetricSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, together with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support, and no additional host intersection, choice selector, open-cover compactness row, or ambient real-completeness row.
\end{theorem}
\begin{proof}
Start with the carrier definition \autoref{def:baire-category-theorem-carrier}. It lists exactly the analytic rows $B,U,K,M,S,R,E$ and the structural rows $H,C,P,N$.

Apply the local NameCert obligations in \autoref{thm:baire-category-theorem-namecert-obligations}. Those obligations identify the dense-open schedule, complete-metric Baire inspection, Cauchy-thread service, metric comparison, finite-window readback, regular-rational readback, and terminal real seal as the only theorem-facing analytic rows.

Finally use \autoref{thm:baire-category-theorem-complete-metric-nonescape}. It already blocks host intersections, countable-choice selectors, ambient complete-metric theorems, open-cover compactness, and ambient $\RealUp$ completeness outside the displayed chain. Therefore every theorem-level read is exhausted by the route named in \autoref{def:baire-category-theorem-finite-certificate-route}.
\end{proof}

\closureat{BaireCategoryTheoremUp}{seedStr}

\begin{closurestatus}{\BaireCategoryTheoremUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:baire-category-theorem-carrier}, \autoref{thm:baire-category-theorem-namecert-obligations}, and \autoref{thm:baire-category-theorem-complete-metric-nonescape} seed the theorem-level complete-metric Baire route over dense-open schedules, complete-metric Baire inspection, Cauchy-thread service, metric comparison, finite windows, regular-rational readback, real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host intersections, countable choice, selected points outside the displayed readback, open-cover compactness, ambient complete-metric theorems, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-open schedule exposure, complete-metric Baire inspection, Cauchy-thread service, metric comparison exposure, finite-window readback, regular-rational readback, real sealing, and nonescape for BEDC.Derived.BaireCategoryTheoremUp.}
  \constructivestory{A $\BaireCategoryTheoremUp$ packet is generated from a BaireCategory dense-open schedule, a CompleteMetricBaireSurface inspection row, a CompleteMetric Cauchy-thread service row, MetricSpace comparison rows, StreamName finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
