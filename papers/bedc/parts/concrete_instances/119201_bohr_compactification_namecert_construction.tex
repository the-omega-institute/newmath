\chapter{A Concrete Naming Certificate for $\BohrCompactificationUp$}
\label{ch:concrete-instances-bohr-compactification-namecert}
\origin{human}

Following Bohr and Weil on compactification of topological groups, read through the finite-window discipline of BEDC naming, the $\BohrCompactificationUp$ packet records the compactification-facing data exposed by almost-periodic observables without taking a host compact group as primitive. It sits beside the translation ledger of \autoref{ch:concrete-instances-almost-periodic-namecert}, the topological-group source of \autoref{ch:concrete-instances-topgroup-namecert}, the compact target interface of \autoref{ch:concrete-instances-compact-namecert}, the uniform handoff of \autoref{ch:concrete-instances-uniformspace-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the finite compactification boundary visible through displayed translation and character rows; it does not assert host compactification, quotient hull equality, selected limits, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Bohr compactification carrier]
\label{def:bohr-compactification-carrier}
A $\BohrCompactificationUp$ carrier is a finite $\BHist$ packet
$$
B=(G,A,K,U,M,S,R,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $G$ is the $\TopGroupUp$ source row, naming the displayed topological-group multiplication, inverse, identity, neighbourhood, and translation windows consumed by the compactification-facing read.
\item $A$ is the $\AlmostPeriodicUp$ translation ledger row, recording the finite almost-periodic translation budgets and relative-density witnesses inspected before any compact target is exposed.
\item $K$ is the compact target row, read through $\CompactUp$ as a displayed finite-cover and finite-net target interface rather than as an external compact group.
\item $U$ is the $\UniformSpaceUp$ handoff row, carrying entourages and uniform-comparison data needed by character and translation consumers.
\item $M$ is the $\ContinuousMapUp$ handoff row for continuous real-valued character windows from the source side to the compact target side.
\item $S$ is the finite $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ readback row, used when real-valued characters are inspected through finite observation windows and regular rational seals.
\item $R$ is the refusal row, recording the absent quotient hull, host function extensionality, selected-limit operator, open-cover compactness theorem, and ambient real-completeness principle.
\item $H$ records componentwise $\hsame$ transport for $G,A,K,U,M,S,R,P,N$.
\item $C$ records displayed $\Cont$ replay of the route from topological-group source rows through almost-periodic translation data, uniform and continuous-map handoffs, finite real readback, refusal rows, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\BohrCompactificationUp$, $\TopGroupUp$, $\AlmostPeriodicUp$, $\CompactUp$, $\UniformSpaceUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BohrCompactificationUp}$ row.
\end{enumerate}
The carrier has no coordinate for a completed quotient hull, host equality of almost-periodic functions, an unrestricted character dual, a selected compactification limit, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bohr compactification NameCert obligations]
\label{thm:bohr-compactification-namecert-obligations}
For every accepted $\BohrCompactificationUp$ carrier $B=(G,A,K,U,M,S,R,H,C,P,N)$, the local $\NameCert_{\BohrCompactificationUp}$ surface consists exactly of carrier admission through the displayed packet; compact target exposure through $K$; translation-ledger exactness through $A$; uniform-continuous character handoff through $U,M$; finite real-valued character readback through $S$; refusal exactness through $R$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; and local naming through $N$. Every compactification-facing consumer must pass through those rows before inspecting a Bohr-style boundary.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bohr-compactification-carrier}. The topological-group source row $G$ and almost-periodic ledger row $A$ are displayed before the compact target row $K$, so the target cannot be read without the source translations that make it visible.

Next use the uniform and continuous-map handoffs. Rows $U$ and $M$ expose the uniform-continuous character route, while $S$ forces every real-valued character inspection through finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and the $\RealUp$ seal.

Finally attach the refusal, transport, replay, provenance, and local naming rows. The refusal row records that quotient hulls, host function extensionality, selected limits, open-cover compactness, and ambient real completeness are not exported, while $H,C,P,N$ preserve the same finite route under transport and public replay.
\end{proof}

\falsifiablePrediction{A consumer that reads a compact target for $\BohrCompactificationUp$ without first exposing the $\AlmostPeriodicUp$ translation ledger and the $\UniformSpaceUp$ handoff is outside the accepted carrier.}

\independenceWitness{almostperiodic, topgroup, compact, uniformspace, continuousmap, real: $\BohrCompactificationUp$ is not bijective to any listed sibling because it binds topological-group source rows, almost-periodic translation rows, compact target rows, uniform and continuous-map handoffs, finite real-valued character readback, refusal rows, transport, replay, provenance, and local NameCert into one compactification-facing packet.}

\closureat{BohrCompactificationUp}{seedStr}

\begin{closurestatus}{\BohrCompactificationUp}
  \constructivestory{A $\BohrCompactificationUp$ packet is generated from topological-group source rows, almost-periodic translation rows, compact target rows, uniform and continuous-map handoffs, finite real-valued character readback, refusal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bohr-compactification-carrier} and \autoref{thm:bohr-compactification-namecert-obligations} specify the compactification-facing seed surface visible from the AlmostPeriodic boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not prove Lean verification, bridge checking, host Bohr compactification, quotient hull equality, host function extensionality, selected limits, open-cover compactness, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact target exposure, translation-ledger exactness, uniform-continuous character handoff, finite real readback, refusal exactness, transport stability, and TasteGate evidence for BEDC.Derived.BohrCompactificationUp.}
\end{closurestatus}
