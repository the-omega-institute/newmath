\chapter{A Concrete Naming Certificate for $\FiniteOscillationPartitionUp$}
\label{ch:concrete-instances-finite-oscillation-partition-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\FiniteOscillationPartitionUp$ packet records a finite partition of a located real interval together with a displayed oscillation window for each cell. It sits after the closed bounded interval surface of \autoref{ch:concrete-instances-closedboundedinterval-namecert}, the located interval source of \autoref{ch:concrete-instances-located-real-interval-namecert}, the oscillation-modulus ledger of \autoref{ch:concrete-instances-oscillation-modulus-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational sequence readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the stream source of \autoref{ch:concrete-instances-streamname-namecert}. The packet gives later uniform-continuity, Darboux, Riemann, and regulated-integral certificates a finite-means carrier for oscillation-controlled partitions without invoking open-cover compactness, quotient equality of real functions, selected suprema, or ambient real completeness.

\begin{definition}[Finite oscillation partition carrier]
\label{def:finite-oscillation-partition-carrier}
A $\FiniteOscillationPartitionUp$ carrier is a finite $\BHist$ packet
$$
O=(I,M,A,B,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the closed bounded and located $\RealUp$ interval row, read through the endpoint, inclusion, and apartness surfaces of $\ClosedBoundedIntervalUp$ and $\LocatedRealIntervalPartitionUp$.
\item $M$ is the finite mesh row, listing ordered partition points, adjacent cell rows, endpoint containment, and a common finite index ledger.
\item $A$ is the oscillation-modulus row, recording the tolerance request and the finite cellwise radius or mesh constraint supplied by $\OscillationModulusUp$.
\item $B$ is the cell-bound row, assigning to each cell a displayed upper and lower real-name readback through $\StreamNameUp$ and $\RegSeqRatUp$ before the terminal $\RealUp$ seal is used.
\item $R$ is the refinement-readback row, recording how a later common refinement or integration consumer reads $I,M,A,B$ without changing the accepted classifier.
\item $H$ records componentwise $\hsame$ transport for $I,M,A,B,R$ and for the continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an oscillation-window request through
$$
I,M,A,B,R \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\FiniteOscillationPartitionUp$, $\ClosedBoundedIntervalUp$, $\LocatedRealIntervalPartitionUp$, $\OscillationModulusUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteOscillationPartitionUp}$ row.
\end{enumerate}
The carrier contains no open-cover compactness witness, no quotient equality of real-valued functions, no selected supremum or infimum, no unbounded partition search, and no ambient real-completeness principle.
\end{definition}

\begin{theorem}[Finite oscillation partition NameCert obligations]
\label{thm:finite-oscillation-partition-namecert-obligations}
For every accepted $\FiniteOscillationPartitionUp$ carrier $O=(I,M,A,B,R,H,C,P,N)$, the local $\NameCert_{\FiniteOscillationPartitionUp}$ surface consists exactly of carrier admission for the displayed rows, interval-source exposure through $I$, mesh exactness through $M$, oscillation-window accounting through $A$, cell-bound readback through $B$, refinement-readback stability through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-oscillation-partition-carrier}. The interval, mesh, oscillation, cell-bound, and refinement rows are the analytic part of the packet, while $H,C,P,N$ are the structural rows that transport, replay, source, and name it.

The interval and mesh obligations are finite. The row $I$ supplies the closed bounded located interval boundary, and $M$ lists the partition cells and endpoint containments over that same boundary.

The oscillation and bound obligations are also finite. The row $A$ records the tolerance and the cellwise oscillation constraint, while $B$ exposes upper and lower readbacks through $\StreamNameUp$ and $\RegSeqRatUp$ before the terminal $\RealUp$ seal can be consumed.

Refinement stability is read from $R$ and transported by $H$. A downstream consumer may refine the displayed cells only by replaying the same interval, mesh, oscillation, and bound rows through $C$ with provenance $P$ and local name $N$.

Since the carrier has no coordinate for open-cover compactness, quotient function equality, selected extrema, unbounded search, or ambient completeness, no accepted NameCert obligation can project one of those objects.
\end{proof}

\begin{theorem}[Finite oscillation partition uniform window]
\label{thm:finite-oscillation-partition-uniform-window}
Every uniform-continuity or integration-facing consumer of an accepted $\FiniteOscillationPartitionUp$ packet receives the finite route
$$
\ClosedBoundedIntervalUp,\ \LocatedRealIntervalPartitionUp,\ \OscillationModulusUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp \longmapsto H,C,P,N .
$$
In particular, the consumer reads a displayed interval source, finite mesh, oscillation tolerance ledger, cell-bound readback, and refinement route before any terminal real seal is used.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:finite-oscillation-partition-namecert-obligations}. They require every accepted consumer to open $I,M,A,B,R$ before the structural rows are available.

The uniform window is determined by $A$ over the cells of $M$. Thus a uniform-continuity consumer obtains a tolerance-indexed cell ledger rather than an open-cover argument.

An integration-facing consumer reads the same route through $B$ and $R$. The cell-bound readbacks expose finite upper and lower rows through stream and regular rational names, while the refinement row records how common refinements preserve the displayed classifier.

Therefore the accepted packet exports a finite oscillation-controlled partition window, and no hidden supremum, quotient-function comparison, open-cover compactness witness, or ambient completeness theorem is available to the consumer.
\end{proof}

\falsifiablePrediction{Any accepted $\FiniteOscillationPartitionUp$ consumer that claims a uniform oscillation window while omitting one of the interval source, finite mesh, oscillation-modulus, cell-bound, refinement, transport, replay, provenance, or local naming rows refutes the packet.}

\independenceWitness{closedboundedinterval, located-real-interval-partition, oscillationmodulus, real, regseqrat, streamname: none is bijective with $\FiniteOscillationPartitionUp$ because this packet jointly carries interval source, finite mesh, oscillation tolerance, cell-bound readback, refinement stability, transport, replay, provenance, and local naming rows.}

\closureat{FiniteOscillationPartitionUp}{seedStr}

\begin{closurestatus}{\FiniteOscillationPartitionUp}
  \constructivestory{A $\FiniteOscillationPartitionUp$ packet is generated from a closed bounded located interval row, a finite mesh row, an oscillation-modulus row, cell-bound readbacks through StreamName and RegSeqRat, a refinement-readback row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-oscillation-partition-carrier}, \autoref{thm:finite-oscillation-partition-namecert-obligations}, and \autoref{thm:finite-oscillation-partition-uniform-window} seed the finite oscillation-controlled partition route for uniform-continuity and integration-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient equality of real-valued functions, selected suprema, unbounded partition search, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, interval-source exposure, mesh exactness, oscillation-window accounting, cell-bound readback, refinement stability, replay stability, provenance exhaustion, and local naming for BEDC.Derived.FiniteOscillationPartitionUp.}
\end{closurestatus}
