\chapter{A Concrete Naming Certificate for $\RegularCauchyModulusTransferUp$}
\label{ch:concrete-instances-regular-cauchy-modulus-transfer-namecert}
\origin{human}

Following Bishop and Bridges, a regular Cauchy real name carries usable information only when its modulus data can be transferred along the displayed stream, rational-readback, and real-seal route. The $\RegularCauchyModulusTransferUp$ packet records that transfer as finite BEDC data from the $\StreamNameUp$ finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, through the $\DyadicRatCoreUp$ tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert} and the $\RegSeqRatUp$ readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, to the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. It consumes the sibling uniform-tail budget of $\RegularCauchyUniformModulusUp$ from \autoref{ch:concrete-instances-regular-cauchy-uniform-modulus-namecert} and names the non-quotient handoff that later completion consumers may inspect.

\begin{definition}[RegularCauchyModulusTransfer carrier]
\label{def:regular-cauchy-modulus-transfer-carrier}
A $\RegularCauchyModulusTransferUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,D,U,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row exposing the source name windows to which the modulus data applies;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the dyadic comparison budget for those windows;
\item $U$ is the $\RegularCauchyUniformModulusUp$ uniform-tail row that supplies one visible regular Cauchy modulus schedule;
\item $R$ is the $\RegSeqRatUp$ readback row that consumes the displayed windows and dyadic tolerances as a regular rational schedule;
\item $E$ is the $\RealUp$ seal row reached by the same readback route, with no selected limit or quotient representative;
\item $H$ records componentwise $\hsame$ transport for $S,D,U,R,E,P,N$;
\item $C$ records displayed $\Cont$ replay of the stream-window, dyadic-tolerance, modulus, readback, and real-seal rows;
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyUniformModulusUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\RegularCauchyModulusTransferUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through the displayed rows $S,D,U,R,E,H,C,P,N$. It therefore records transfer of modulus data across the StreamName-RegSeqRat-Real route, not equality of quotient completions, choice of a representative, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[RegularCauchyModulusTransfer NameCert obligations]
\label{thm:regular-cauchy-modulus-transfer-namecert-obligations}
For the carrier of \autoref{def:regular-cauchy-modulus-transfer-carrier}, the local $\NameCert_{\RegularCauchyModulusTransferUp}$ surface consists of carrier admission for
$$
Q=(S,D,U,R,E,H,C,P,N),
$$
source-window exposure through $S$, dyadic-tolerance exposure through $D$, uniform-modulus exposure through $U$, regular-rational readback through $R$, terminal real-seal exposure through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-cauchy-modulus-transfer-carrier}. The tuple displays the stream windows, dyadic tolerance ledger, uniform-tail modulus, regular rational readback, terminal real seal, transport data, replay data, provenance, and local naming row.

The transfer route is ordered. Row $S$ supplies the finite source windows, row $D$ supplies the dyadic precision ledger for those windows, row $U$ supplies the regular Cauchy tail budget, and row $R$ consumes that visible data as regular rational readback before row $E$ can be used as a real-seal endpoint.

The structural rows $H,C,P,N$ preserve exactly that route. Since the carrier has no coordinate for a quotient stream, selected representative, countable-choice schedule, host equality of completions, or ambient $\RealUp$ completeness principle, the listed rows are precisely the local NameCert obligations.
\end{proof}

\begin{theorem}[RegularCauchyModulusTransfer StreamName-RegSeqRat handoff]
\label{thm:regular-cauchy-modulus-transfer-streamname-handoff}
Let $Q=(S,D,U,R,E,H,C,P,N)$ be an accepted $\RegularCauchyModulusTransferUp$ carrier. Any consumer that reads the $\RegSeqRatUp$ row $R$ must first expose the $\StreamNameUp$ source windows $S$, the $\DyadicRatCoreUp$ tolerance row $D$, and the $\RegularCauchyUniformModulusUp$ budget $U$; only then may it pass the readback to the $\RealUp$ seal row $E$ through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The readback coordinate $R$ is part of the same finite packet as $S,D,U,E,H,C,P,N$, and the classifier compares packets only through those displayed rows.

Before $R$ can be consumed, the source windows and tolerance data must be visible. The rows $S$ and $D$ provide that finite input, while $U$ supplies the uniform regular Cauchy modulus that makes the readback route a transfer of displayed data rather than a hidden completion argument.

The real-seal row $E$ is reached only through the structural rows $H,C,P,N$. These rows transport, replay, source, and name the same packet, so the StreamName-RegSeqRat handoff cannot be replaced by a quotient stream, selected limit, host equality of completions, or ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyModulusTransferUp$ consumer displays the $\StreamNameUp$ source windows, $\DyadicRatCoreUp$ tolerance ledger, $\RegularCauchyUniformModulusUp$ budget, $\RegSeqRatUp$ readback row, $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before it claims a modulus-transfer handoff.}

\independenceWitness{StreamName, DyadicRatCore, RegSeqRat, RegularCauchyUniformModulus, Real: $\RegularCauchyModulusTransferUp$ is not bijective to any listed sibling because it packages the displayed transfer route between stream windows, dyadic tolerances, uniform modulus data, regular rational readback, and terminal real seal as one local NameCert surface.}

\closureat{RegularCauchyModulusTransferUp}{seedStr}

\begin{closurestatus}{\RegularCauchyModulusTransferUp}
  \constructivestory{A $\RegularCauchyModulusTransferUp$ packet is generated from stream windows, dyadic tolerance rows, a regular Cauchy uniform-modulus row, regular rational readback, terminal real-seal data, componentwise transport, displayed continuation replay, package provenance, and local naming evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{Only the seed carrier, local NameCert obligations, and StreamName-RegSeqRat handoff route in \autoref{def:regular-cauchy-modulus-transfer-carrier}, \autoref{thm:regular-cauchy-modulus-transfer-namecert-obligations}, and \autoref{thm:regular-cauchy-modulus-transfer-streamname-handoff} are recorded.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected representatives, countable choice, host equality of completions, ambient $\RealUp$ completeness, or Lean verification.}
  \upgradepath{Formal closure requires a Lean carrier and obligation theorem for the displayed rows $S,D,U,R,E,H,C,P,N$.}
\end{closurestatus}
