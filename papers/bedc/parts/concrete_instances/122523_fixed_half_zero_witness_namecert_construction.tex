\chapter{A Concrete Naming Certificate for $\FixedHalfZeroWitnessUp$}
\label{ch:concrete-instances-fixed-half-zero-witness-namecert}
\origin{ai}

The $\FixedHalfZeroWitnessUp$ packet names only the typed witness-function package of \autoref{def:rh-proof-obligation-witness-function}. It is not a proof of $\mathsf{RH}$, not a zero-location theorem, and not an analytic exclusion of off-line zeros. The packet sits beside the critical-line witness surface of \autoref{ch:concrete-instances-criticallinewitness-namecert}, the basic zeta source of \autoref{ch:concrete-instances-zeta-basic-namecert}, the zeta-zero carrier of \autoref{ch:concrete-instances-zeta-zeros-namecert}, and the zero-type factorization packet of \autoref{ch:concrete-instances-zeta-zero-type-factorization-namecert}.

\begin{definition}[FixedHalfZeroWitness carrier]
\label{def:fixed-half-zero-witness-carrier}
A $\FixedHalfZeroWitnessUp$ carrier is a finite $\BHist$ packet
$$
F=(Z,S,W,T,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the non-trivial zeta-zero source row, read through the zero and critical-strip typing data of the RH proof-obligation package;
\item $S$ is the source-classifier row, recording the projection back to the original zero source before any fixed-half witness is cited;
\item $W$ is the fixed-half witness-function row, displaying the typed function from the non-trivial-zero package to the fixed-half-zero package;
\item $T$ is the transport row witnessing that the projected source of $W$ is classifier-equal to the source row named by $S$;
\item $B$ is the refusal boundary row, recording that the packet exports no proof of $\mathsf{RH}$, no analytic zero exclusion, no global zero classifier, and no host truth value;
\item $H$ records componentwise $\hsame$ transport for source, classifier, witness-function, source-transport, refusal, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from a witness-function request through zero-source admission, classifier projection, fixed-half witness readback, refusal-boundary inspection, provenance, and local naming;
\item $P$ is the $\Pkg$ provenance over $\FixedHalfZeroWitnessUp$, $\CriticalLineWitnessUp$, $\ZetaBasicUp$, $\ZetaZerosUp$, $\ZetaZeroTypeFactorizationUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\FixedHalfZeroWitnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Z,S,W,T,B,H,C,P,N$. It has no coordinate for an RH proof, a zero-free strip theorem, a completed analytic continuation argument, an ambient truth predicate, Classical choice, quotient equality, or a standard bridge.
\end{definition}

\begin{theorem}[FixedHalfZeroWitness obligation surface]
\label{thm:fixed-half-zero-witness-obligation-surface}
For the carrier of \autoref{def:fixed-half-zero-witness-carrier}, the local $\NameCert_{\FixedHalfZeroWitnessUp}$ surface consists exactly of non-trivial-zero source admission, source-classifier projection, fixed-half witness-function readback, source-transport evidence, refusal-boundary exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the finite packet $F=(Z,S,W,T,B,H,C,P,N)$. The source row $Z$ and source-classifier row $S$ are visible before the witness-function row is read, so the package starts from typed zero data rather than an untyped analytic assertion.

The witness-function row $W$ supplies only the displayed function package named by \autoref{def:rh-proof-obligation-witness-function}. The transport row $T$ records the projection back to the same source classifier.

The refusal row $B$ is part of the same carrier. It prevents the read from being cited as an RH proof, an off-line zero exclusion, a global zero classifier, or a host truth value. The support rows $H,C,P,N$ preserve, replay, source, and name that finite package, and no further obligation is stored outside the displayed rows.
\end{proof}

\begin{theorem}[FixedHalfZeroWitness RH witness route]
\label{thm:fixed-half-zero-witness-rh-witness-route}
Every accepted $\FixedHalfZeroWitnessUp$ read routes the typed witness function of \autoref{def:rh-proof-obligation-witness-function} through $Z,S,W,T,B,H,C,P,N$. The route may be used as a concrete NameCert landing for the RH witness-function target, but it does not prove $\mathsf{RH}$, does not supply analytic zero exclusion, and does not turn fixed-half witness data into a bridge-checked theorem.
\end{theorem}
\begin{proof}
Read the source row $Z$ and classifier projection row $S$. They identify the typed non-trivial-zero input and its source before a fixed-half target can be named.

Next read $W$ and $T$. The witness-function row displays the fixed-half output package, and the transport row records that its projection returns to the source classifier.

Finally inspect $B,H,C,P,N$. The refusal boundary keeps the route inside the typed package and the structural rows transport, replay, source, and name the same data. Thus the route is a witness-function NameCert surface, not an RH proof or a bridge-checked analytic theorem.
\end{proof}

\falsifiablePrediction{If an accepted FixedHalfZeroWitnessUp packet exports a fixed-half witness without displaying the non-trivial-zero source row, source-classifier projection, witness-function row, source-transport row, and refusal boundary, then it is refuted as a NameCert carrier for the RH witness-function package.}

\independenceWitness{criticallinewitness, zeta-basic, zeta-zeros, zeta-zero-type-factorization: none is bijective to $\FixedHalfZeroWitnessUp$ because those siblings respectively bind critical-line witness rows, basic zeta source rows, zero carrier rows, or zero-type factorization rows, while this packet binds the typed witness-function route with source projection, refusal boundary, transport, replay, provenance, and local NameCert rows.}

The Lean follow-up file \texttt{lean4/BEDC/Derived/FixedHalfZeroWitnessUp/TasteGate.lean} must provide explicit $\Nontrivial\ \FixedHalfZeroWitnessUp$ and $\FieldFaithful\ \FixedHalfZeroWitnessUp$ instances. The nontriviality witness must distinguish an accepted witness-function packet from a packet missing the source-transport or refusal-boundary row, and the field-faithfulness witness must preserve $Z,S,W,T,B,H,C,P,N$ as separate rows.

\closureat{FixedHalfZeroWitnessUp}{seedStr}

\begin{closurestatus}{\FixedHalfZeroWitnessUp}
  \constructivestory{A $\FixedHalfZeroWitnessUp$ packet is generated from a non-trivial zero source row, source-classifier row, typed fixed-half witness-function row, source-transport row, refusal-boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fixed-half-zero-witness-carrier}, \autoref{thm:fixed-half-zero-witness-obligation-surface}, and \autoref{thm:fixed-half-zero-witness-rh-witness-route} seed the typed fixed-half witness-function package without proving RH.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not prove $\mathsf{RH}$, exclude off-line zeros, provide analytic continuation evidence, supply a global zero classifier, close Lean verification, audit cleanliness, or bridge checking.}
  \upgradepath{Obligation closure requires checked zero-source admission, source projection, witness-function readback, source transport, refusal-boundary exposure, provenance, local naming, and the required Nontrivial and FieldFaithful instances for BEDC.Derived.FixedHalfZeroWitnessUp.}
\end{closurestatus}
