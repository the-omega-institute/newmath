\chapter{A Concrete Naming Certificate for $\MetaCICResidualLocalConfluenceMatrixUp$}
\label{ch:concrete-instances-metacic-residual-local-confluence-matrix-namecert}
\origin{ai}

The $\MetaCICResidualLocalConfluenceMatrixUp$ packet realizes the bounded residual local-join frontier of \autoref{ch:metacic-confluence-audit-map} as finite matrix data. It sits beside the residual checker of \autoref{ch:concrete-instances-metacic-confluence-residual-checker-namecert}, the residual local-join determinacy packet of \autoref{ch:concrete-instances-metacic-residual-join-determinacy-namecert}, the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, and the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}. The matrix records typed windows, residual pairs, bounded checkers, obstruction retention, transport, replay, provenance, and local naming before any confluence consumer may cite a local join.

\begin{definition}[MetaCIC residual local-confluence matrix carrier]
\label{def:metacic-residual-local-confluence-matrix-carrier}
A $\MetaCICResidualLocalConfluenceMatrixUp$ carrier is a finite $\BHist$ packet
$$
M=(T,R,J,B,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row, naming the finite MetaCIC source terms and contexts inspected by the matrix.
\item $R$ is the residual-pair row, recording the bounded residual branch pairs whose local join status is under inspection.
\item $J$ is the candidate local-join row, displaying only the finite common-reduct candidates admitted by the checker.
\item $B$ is the bounded-checker row, recording the finite checker outcome for each displayed residual pair.
\item $O$ is the obstruction row, retaining residual-substitution compatibility, unrestricted closed parallel diamond, arbitrary closed Church--Rosser, subject-reduction discharge, evaluator equality, and kernel consistency as visible non-exports.
\item $H$ records componentwise $\hsame$ transport for typed windows, residual pairs, candidate joins, checker outcomes, obstruction rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each confluence-facing request through $T,R,J,B,O,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualLocalConfluenceMatrixUp$, the residual checker, residual local-join determinacy, residual-substitution boundary, parallel-diamond frontier, confluence audit packet, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICResidualLocalConfluenceMatrixUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,R,J,B,O,H,C,P,N$. It has no coordinate for unrestricted substitution compatibility, arbitrary closed parallel diamond, arbitrary closed Church--Rosser, subject reduction, evaluator equality, kernel consistency, or bridge checking.
\end{definition}

\begin{theorem}[MetaCIC residual local-confluence matrix NameCert obligations]
\label{thm:metacic-residual-local-confluence-matrix-namecert-obligations}
For every accepted $\MetaCICResidualLocalConfluenceMatrixUp$ carrier
$$
M=(T,R,J,B,O,H,C,P,N),
$$
the local $\NameCert_{\MetaCICResidualLocalConfluenceMatrixUp}$ surface consists exactly of typed-window exposure, residual-pair exposure, candidate local-join exposure, bounded-checker exactness, obstruction retention, componentwise transport, displayed replay, provenance, and local naming. Any accepted confluence-facing consumer must expose $T,R,J,B,O,H,C,P,N$ before treating a residual local join as available to the confluence audit route.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:metacic-residual-local-confluence-matrix-carrier}. The typed windows, residual pairs, candidate joins, checker outcomes, and obstruction rows are all explicit coordinates of $M$.

The local-join read first displays $T$ and $R$, so the matrix is tied to a bounded typed residual frontier. It then displays $J$ and $B$, so candidate joins are available only through the finite checker row.

The obstruction row $O$ remains visible after the checker outcome. Thus any missing residual-substitution compatibility or closed-diamond premise is retained as data rather than silently discharged.

Finally, $H,C,P,N$ transport, replay, package, and name the same finite rows. Since no carrier coordinate stores an unrestricted confluence theorem, the NameCert surface cannot export arbitrary closed parallel diamond or Church--Rosser.
\end{proof}

\begin{theorem}[MetaCIC residual local-confluence matrix budget handoff]
\label{thm:metacic-residual-local-confluence-matrix-budget-handoff}
Every bounded local-join handoff exported by an accepted $\MetaCICResidualLocalConfluenceMatrixUp$ carrier factors through the finite route
$$
T \longrightarrow R \longrightarrow J \longrightarrow B \longrightarrow O
$$
inside $M=(T,R,J,B,O,H,C,P,N)$. The route may pass checked residual-pair rows to the confluence audit map, but it must retain the obstruction row whenever residual-substitution compatibility, unrestricted closed parallel diamond, or arbitrary closed Church--Rosser has not been supplied.
\end{theorem}
\begin{proof}
The typed-window and residual-pair rows bound the source of every matrix entry. A handoff cannot name a candidate join until those finite source rows have been displayed.

The candidate local-join and bounded-checker rows then identify which entries passed the finite checker. These rows are budgeted by $B$, not by a global proof search or an unrestricted confluence theorem.

The obstruction row $O$ remains part of the exported route. Therefore the handoff can support bounded local joins while still refusing arbitrary closed parallel diamond, Church--Rosser, unchecked substitution compatibility, subject reduction, evaluator equality, and kernel consistency.
\end{proof}

\falsifiablePrediction{Every accepted $\MetaCICResidualLocalConfluenceMatrixUp$ consumer that cites a residual local join without first displaying typed-window, residual-pair, candidate local-join, bounded-checker, obstruction, transport, replay, provenance, and local NameCert rows refutes the matrix surface.}

\independenceWitness{metacic-confluence-residual-checker, metacic-residual-join-determinacy, metacic-residual-substitution-boundary, metacic-parallel-diamond-frontier, metacic-confluence-audit-packet: no listed sibling jointly carries typed windows, residual pairs, candidate local joins, bounded checker outcomes, obstruction retention, transport, replay, provenance, and local naming as one finite matrix packet.}

\closureat{MetaCICResidualLocalConfluenceMatrixUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualLocalConfluenceMatrixUp}
  \constructivestory{A $\MetaCICResidualLocalConfluenceMatrixUp$ packet is generated from typed-window rows, residual-pair rows, candidate local-join rows, bounded-checker rows, obstruction rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-local-confluence-matrix-carrier}, \autoref{thm:metacic-residual-local-confluence-matrix-namecert-obligations}, and \autoref{thm:metacic-residual-local-confluence-matrix-budget-handoff} seed the finite matrix route for bounded residual local joins.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close residual-substitution compatibility, arbitrary closed parallel diamond, arbitrary closed Church--Rosser, subject reduction, evaluator equality, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked typed-window exposure, residual-pair exposure, candidate local-join exposure, bounded-checker exactness, obstruction retention, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICResidualLocalConfluenceMatrixUp.}
\end{closurestatus}
