\chapter{A Concrete Naming Certificate for $\FiniteWindowRpBoundaryUp$}
\label{ch:concrete-instances-finite-window-rp-boundary-namecert}
\origin{ai}

The $\FiniteWindowRpBoundaryUp$ packet realizes the finite-window reflection-positive boundary of \autoref{ch:visions-innovation-input-finite-window-rp-boundary} as finite reflection, palindrome-window, charged/neutral split, deficit-ledger, transport, replay, provenance, and local NameCert data. It sits next to the geometry-boundary carrier of \autoref{ch:concrete-instances-innovation-input-geometry-boundary-namecert}, the finite envelope row of \autoref{ch:concrete-instances-finitewindowenvelope-namecert}, the phase-stability row of \autoref{ch:concrete-instances-golden-phase-projection-stability-namecert}, and the bounded oscillation ledger of \autoref{ch:concrete-instances-bounded-oscillation-namecert}. The packet names the finite obstruction surface only; it does not assert full OS/RP positivity, a limiting Hilbert space, empirical dynamics, or a completed quantum-gravity model.

\begin{definition}[Finite-window RP carrier]
\label{def:finite-window-rp-carrier}
A $\FiniteWindowRpBoundaryUp$ carrier is a finite $\BHist$ packet
$$
R=(W,J,M,Q,D,B,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the finite window row, naming the bounded future support, index span, and envelope used by the audit.
\item $J$ is the reflection-palindrome row, displaying the involutive reflection, reversed word, and palindrome comparison visible in the finite window.
\item $M$ is the finite RP matrix row assembled from reflected future monomials over $W$.
\item $Q$ is the charged/neutral split row, separating charged monomial windows from balanced neutral monomial windows.
\item $D$ is the deficit ledger, recording the finite witness row for a negative neutral value or the visible absence of such a deficit.
\item $B$ is the boundary row that distinguishes a charged obstruction, a neutral obstruction, and an accepted finite-window audit state.
\item $T$ records transport of $W,J,M,Q,D,B$ under componentwise $\hsame$.
\item $H$ records componentwise $\hsame$ transport for the finite window, reflection-palindrome row, RP matrix row, charged/neutral split, deficit ledger, boundary row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from a finite-window RP request through $W,J,M,Q,D,B,T,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteWindowRpBoundaryUp$, the source vision of \autoref{ch:visions-innovation-input-finite-window-rp-boundary}, $\InnovationInputGeometryBoundaryUp$, $\FiniteWindowEnvelopeUp$, $\GoldenPhaseProjectionStabilityUp$, $\BoundedOscillationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteWindowRpBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $W,J,M,Q,D,B,T,H,C,P,N$. It has no coordinate for an infinite reflection-positive completion, limiting Hilbert space, field equation, empirical coupling, hidden observer oracle, or ambient positivity theorem.
\end{definition}

\begin{theorem}[Finite-window RP independence boundary]
\label{thm:finite-window-rp-independence-boundary}
For every accepted $\FiniteWindowRpBoundaryUp$ carrier $R=(W,J,M,Q,D,B,T,H,C,P,N)$, the local $\NameCert_{\FiniteWindowRpBoundaryUp}$ surface consists exactly of finite-window admission through $W$, reflection-palindrome exposure through $J$, finite RP matrix assembly through $M$, charged/neutral split through $Q$, deficit-ledger exposure through $D$, boundary classification through $B$, transport through $T,H$, displayed replay through $C$, provenance through $P$, local naming through $N$, and non-escape from full OS/RP positivity, limiting Hilbert spaces, empirical dynamics, and completed quantum-gravity semantics.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-window-rp-carrier}. The finite window, reflection-palindrome row, RP matrix row, charged/neutral split, deficit ledger, boundary row, and structural rows are all explicit coordinates.

The independence boundary is carried by $Q,D,B$. The charged and neutral audits are not interchangeable because $Q$ records their split, $D$ records a reflection-deficit witness for the neutral side when present, and $B$ records which boundary state is visible to consumers.

Compare the sibling chapters named in the provenance row. The geometry-boundary carrier has observer-channel and response-slot rows, the finite envelope carrier has endpoint-window packaging, the phase-stability carrier has phase-projection stability rows, and the bounded oscillation carrier has oscillation ledgers; none carries the same reflection-deficit and charged/neutral split rows $J,M,Q,D,B$.

Finally $T,H,C,P,N$ transport, replay, source, and locally name that finite packet. Since no infinite positivity completion or ambient physical semantics is a carrier coordinate, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Finite-window RP deficit handoff]
\label{thm:finite-window-rp-deficit-handoff}
Every neutral-deficit read exported by an accepted $\FiniteWindowRpBoundaryUp$ carrier exposes a finite $\BHist$ witness distinguishing the neutral deficit row from the charged row before any boundary consumer reads it. The handoff is the finite route
$$
W,J,M,Q,D,B,T,H,C,P,N .
$$
It exports no full reflection-positive completion, limiting Hilbert space, empirical dynamics, or completed quantum-gravity model.
\end{theorem}
\begin{proof}
Start with the split row $Q$. It separates charged windows from neutral windows before a deficit can be interpreted.

Read the matrix and deficit rows $M,D$. The matrix row supplies the finite RP entries, and the deficit ledger records the neutral witness row or its visible absence.

Now apply the boundary row $B$ and the structural rows $T,H,C,P,N$. The charged/neutral distinction remains attached to the same finite window and reflection-palindrome data, so a consumer receives a visible finite witness rather than an ambient positivity claim.
\end{proof}

\falsifiablePrediction{If the finite-window RP carrier is accepted, then every neutral deficit row has a finite BHist witness distinguishing it from the charged row.}

\independenceWitness{Compare innovation-input-geometry-boundary, finitewindowenvelope, golden-phase-projection-stability, and bounded-oscillation: the RP carrier has a reflection-deficit row and charged/neutral split absent from those carriers, while they carry geometry, envelope, phase, or oscillation rows not bijective to it.}

The Lean TasteGate file \texttt{lean4/BEDC/Derived/FiniteWindowRpBoundaryUp/TasteGate.lean} must provide explicit $\Nontrivial\ \FiniteWindowRpBoundaryUp$ and $\FieldFaithful\ \FiniteWindowRpBoundaryUp$ instances preserving $W,J,M,Q,D,B,T,H,C,P,N$ as distinct rows before any checked target can cite \texttt{BEDC.Derived.FiniteWindowRpBoundaryUp}.

\closureat{FiniteWindowRpBoundaryUp}{seedStr}

\begin{closurestatus}{\FiniteWindowRpBoundaryUp}
  \constructivestory{A $\FiniteWindowRpBoundaryUp$ packet is generated from a finite window row, reflection-palindrome row, finite RP matrix row, charged/neutral split row, deficit ledger, boundary row, transport rows, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-window-rp-carrier}, \autoref{thm:finite-window-rp-independence-boundary}, and \autoref{thm:finite-window-rp-deficit-handoff} seed the finite reflection-palindrome boundary with a visible charged/neutral split and deficit ledger.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full OS/RP positivity, limiting Hilbert-space construction, empirical dynamics, completed quantum-gravity semantics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exposure, reflection-palindrome exactness, RP matrix assembly, charged/neutral split, deficit-ledger exactness, boundary classification, transport, replay, provenance, and TasteGate field faithfulness for BEDC.Derived.FiniteWindowRpBoundaryUp.}
\end{closurestatus}
