\chapter{A Concrete Naming Certificate for $\CompactApartnessLocatedSeparationUp$}
\label{ch:concrete-instances-compact-apartness-located-separation-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactApartnessLocatedSeparationUp$ packet records the finite route by which compact located metric data and apartness-sensitive separation data are read before a completion or Real-facing consumer asks for distinctness. It sits beside the located-limit route of \autoref{ch:concrete-instances-locatedlimit-namecert} and the separated-metric route of \autoref{ch:concrete-instances-separatedmetric-namecert}. The packet keeps the constructive distinction between positive apartness evidence and equality refusal: it names finite compact windows, located comparison rows, separated metric tests, transport, replay, provenance, and local NameCert data without importing classical trichotomy, quotient equality, selected limit points, host equality, or an ambient completed metric object.

\begin{definition}[Compact apartness located separation carrier]
\label{def:compact-apartness-located-separation-carrier}
A $\CompactApartnessLocatedSeparationUp$ carrier is a finite $\BHist$ packet
$$
L=(K,M,A,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the consumed $\CompactMetricUp$ or $\CompactUp$ finite-window row whose compact reads are presented by displayed probe or finite-net data.
\item $M$ is the underlying $\MetricSpaceUp$ row carrying the located metric-distance comparisons used by the packet.
\item $A$ is the positive apartness ledger, recording finite evidence that two displayed compact-window reads are separated by a rational lower-bound row.
\item $S$ is the $\SeparatedMetricUp$ comparison row that turns the positive apartness evidence into the separation-facing public test used by downstream consumers.
\item $R$ is the $\RegSeqRatUp$ or $\RealUp$ readback row for the rational lower bound and the terminal numerical seal.
\item $H$ records componentwise $\hsame$ transport for compact windows, metric rows, apartness ledgers, separated comparisons, readback rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from compact finite windows through located metric comparison, apartness evidence, separated comparison, and rational or Real readback.
\item $P$ is the $\Pkg$ provenance over $\CompactMetricUp$, $\CompactUp$, $\MetricSpaceUp$, $\SeparatedMetricUp$, $\LocatedLimitUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactApartnessLocatedSeparationUp}$ row.
\end{enumerate}
The carrier has no coordinate for classical trichotomy of equality and apartness, quotient equality, selected limit representatives, countable choice, host equality, or an ambient completed metric space.
\end{definition}

\begin{theorem}[CompactApartnessLocatedSeparation NameCert obligations]
\label{thm:compact-apartness-located-separation-namecert-obligations}
For every accepted $\CompactApartnessLocatedSeparationUp$ carrier $L=(K,M,A,S,R,H,C,P,N)$, the local $\NameCert_{\CompactApartnessLocatedSeparationUp}$ surface consists exactly of carrier admission, compact finite-window exposure, located metric comparison, positive apartness lower-bound exposure, separated-metric handoff, rational or Real readback, transport, replay, provenance, and local naming. A consumer may read a separated compact comparison only after the compact row $K$, metric row $M$, apartness row $A$, and separated row $S$ have been displayed.

The same surface exports no classical trichotomy, quotient equality, selected limit point, countable-choice schedule, host equality, ambient completion object, or Real completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-apartness-located-separation-carrier}. The displayed rows $K,M,A,S,R,H,C,P,N$ are the complete public inventory of the packet.

The compact and located rows are read first. The compact finite-window row $K$ supplies the bounded testing window, and the metric row $M$ supplies located distance comparisons internal to that window.

The apartness and separation rows are downstream. The row $A$ records positive rational lower-bound evidence, while $S$ packages the same evidence as the separated-metric handoff consumed by sibling separation routes such as \autoref{ch:concrete-instances-separatedmetric-namecert}.

Finally $R,H,C,P,N$ read back, transport, replay, source, and name the same finite route. Since the carrier contains no coordinate for trichotomy, quotient equality, selected limits, choice schedules, host equality, ambient completions, or Real completeness, none of those objects can become a NameCert obligation.
\end{proof}

\falsifiablePrediction{A compact separation consumer that reads apartness without first displaying the compact finite-window row, located metric comparison row, separated-metric handoff, and rational or Real readback row is not a $\CompactApartnessLocatedSeparationUp$ packet.}

\independenceWitness{compactmetric, locatedlimit, separatedmetric, regseqrat, real: the packet is not bijective to these siblings because it binds compact finite windows, located metric comparison, positive apartness lower bounds, separated-metric handoff, numerical readback, transport, replay, provenance, and local naming in one carrier.}

\closureat{CompactApartnessLocatedSeparationUp}{seedStr}

\begin{closurestatus}{\CompactApartnessLocatedSeparationUp}
  \constructivestory{A $\CompactApartnessLocatedSeparationUp$ packet is generated from compact finite-window data, located metric comparisons, positive apartness lower-bound rows, SeparatedMetric handoff data, RegSeqRat or Real readback, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-apartness-located-separation-carrier} and \autoref{thm:compact-apartness-located-separation-namecert-obligations} seed the compact apartness/separation carrier and its NameCert obligation surface over displayed CompactMetricUp, LocatedLimitUp, SeparatedMetricUp, MetricSpaceUp, RegSeqRatUp, and RealUp rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean theorem verification, bridge checking, classical trichotomy, quotient equality, selected limit representatives, countable choice, host equality, ambient completed metric spaces, or Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-window exposure, located metric comparison, apartness lower-bound exposure, separated-metric handoff, numerical readback, transport, replay, provenance, and local naming for the same packet.}
\end{closurestatus}
