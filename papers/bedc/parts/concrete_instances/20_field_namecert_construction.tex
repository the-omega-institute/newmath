\input{parts/concrete_instances/field/20_field_singleton_schema}
\input{parts/concrete_instances/field/20_field_rat_boundary}
\input{parts/concrete_instances/field/20_field_rat_continuation_closure}
\input{parts/concrete_instances/field/20_field_rat_denominator_context_exactness}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_obstruction}
\input{parts/concrete_instances/field/20_field_rat_denominator_adjoined_unit_laws}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_carrier_transport_closure_package}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_visible_carrier}
\input{parts/concrete_instances/field/20_field_inverse_laws}
\input{parts/concrete_instances/field/20_field_zero_product_laws}
\input{parts/concrete_instances/field/20_field_affine_equation_laws}
\input{parts/concrete_instances/field/20_field_product_right_equation_exactness}
\input{parts/concrete_instances/field/20_field_affine_explicit_inverse}
\input{parts/concrete_instances/field/20_field_affine_solution_uniqueness}
\input{parts/concrete_instances/field/20_field_affine_automorphism_package}
\input{parts/concrete_instances/field/20_field_signed_square_consequences}
\input{parts/concrete_instances/field/20_field_product_apartness_consequences}
\input{parts/concrete_instances/field/20_field_zero_apartness_and_square_descent}
\input{parts/concrete_instances/field/20_field_rat_denominator_concrete_continuation_instance}
\input{parts/concrete_instances/field/20_field_zero_apartness_checked_markers}
\input{parts/concrete_instances/field/20_field_square_apartness_consequences}
\input{parts/concrete_instances/field/20_field_square_apartness_and_affine_exactness}
\input{parts/concrete_instances/field/20_field_certificate_record}
\input{parts/concrete_instances/field/20_field_rat_denominator_empty_unit_endpoint_exactness}
\input{parts/concrete_instances/field/20_field_rat_denominator_external_unit_package}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_envelope_certificate}
\input{parts/concrete_instances/field/20_field_singleton_empty_concrete_instance}
\input{parts/concrete_instances/field/20_field_rat_denominator_external_unit_envelope}
\input{parts/concrete_instances/field/20_field_rat_denominator_external_unit_boundary}
\input{parts/concrete_instances/field/20_field_rat_denominator_strict_monoid_boundary}

\begin{theorem}[Rat denominator empty-unit commutative monoid laws]
\label{thm:field-rat-denominator-empty-unit-commutative-monoid-laws}
For the tuple $(C_D^u,R_D^u,\mu_D^u,e_D^u)$ of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance},
the five laws of
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws} hold,
and the multiplication is commutative up to the endpoint classifier: if
$C_D^u(h)$ and $C_D^u(k)$, then
$$
R_D^u(\mu_D^u(h,k),\mu_D^u(k,h)).
$$
\end{theorem}
\leanchecked{BEDC.Derived.FieldUp.field\_rat\_denominator\_empty\_unit\_continuation\_commutative\_monoid\_laws}
\begin{proof}
Unfold the concrete data of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance}.
The first five clauses are precisely
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws}.

It remains to prove the displayed commutativity classifier. Split the two
$\mathsf{RatDenomUnitCarrier}$ witnesses into their empty endpoint and genuine
Rat denominator endpoint alternatives.

In the genuine/genuine case, the two products are the canonical append
continuations $\Cont(h,k,\mathsf{append}(h,k))$ and
$\Cont(k,h,\mathsf{append}(k,h))$. Apply
\autoref{thm:field-rat-denominator-continuation-commutative-classifier} to
these two continuations and the two Rat denominator carriers. The resulting
$\mathsf{RatHistoryClassifier}$ supplies the carried endpoints and the
$\hsame$ comparison required to pack
$\mathsf{RatDenomUnitClassifier}(\mathsf{append}(h,k),\mathsf{append}(k,h))$.

In a mixed case, one endpoint is $\hsame\emp$ and the other is a genuine Rat
denominator endpoint. The append unit law on the empty side identifies both
products with the genuine endpoint, one directly and one after symmetry of
$\hsame$. Transitivity of $\hsame$ gives
$\mathsf{append}(h,k)\hsame\mathsf{append}(k,h)$, and append closure gives the
two empty-unit carrier witnesses. These data pack the required
$\mathsf{RatDenomUnitClassifier}$.

In the empty/empty case, both inputs are $\hsame\emp$. The left and right
append unit laws identify each product with $\emp$; symmetry and transitivity
of $\hsame$ then identify the two products with each other. The empty branch
of $\mathsf{RatDenomUnitCarrier}$ carries both products, so the classifier
packing again yields
$R_D^u(\mu_D^u(h,k),\mu_D^u(k,h))$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_rat\_denominator\_empty\_unit\_commutative\_monoid\_laws}

\begin{theorem}[Rat denominator empty-unit identity uniqueness]
\label{thm:field-rat-denominator-empty-unit-identity-unique}
For the tuple $(C_D^u,R_D^u,\mu_D^u,e_D^u)$ of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance},
let $u:\mathsf{BHist}$ be a carried endpoint with $C_D^u(u)$. If $u$
satisfies the two one-sided unit laws
$$
\forall h:\mathsf{BHist}.\; C_D^u(h)\to R_D^u(\mu_D^u(u,h),h)
$$
and
$$
\forall h:\mathsf{BHist}.\; C_D^u(h)\to R_D^u(\mu_D^u(h,u),h),
$$
then
$$
R_D^u(u,e_D^u).
$$
\end{theorem}
\begin{proof}
Unpack
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws}. Its
carried-unit clause gives $C_D^u(e_D^u)$, and its right-unit law with right
factor $e_D^u$ gives $R_D^u(\mu_D^u(u,e_D^u),u)$ from the assumed carrier
witness $C_D^u(u)$.

Now apply the assumed left-unit law for $u$ at the carried endpoint $e_D^u$.
This gives $R_D^u(\mu_D^u(u,e_D^u),e_D^u)$.

Project the two $R_D^u$ classifier witnesses to their $\hsame$ components. The
first comparison identifies $\mu_D^u(u,e_D^u)$ with $u$, and the second
comparison identifies the same product with $e_D^u$. Symmetry and transitivity
of $\hsame$ therefore give $u\hsame e_D^u$.

Finally, repack the carrier witnesses $C_D^u(u)$ and $C_D^u(e_D^u)$ together
with this endpoint comparison. The result is exactly $R_D^u(u,e_D^u)$.
\end{proof}

Per-object template: traditional target identity uniqueness in the Rat
denominator empty-unit monoid; BEDC source $\mathsf{RatDenomUnitCarrier}$,
$\mathsf{RatDenomUnitClassifier}$, $\mathsf{append}$, and $\emp$; carrier
$C_D^u$; classifier $R_D^u$; stability obligation concrete one-sided unit
collapse; ledger external-unit endpoint provenance; core theorem
\autoref{thm:field-rat-denominator-empty-unit-identity-unique}; exactness
predicate unit endpoints classify with $e_D^u$; standard bridge deferred;
status $\mathsf{ProofTarget}$.

\begin{definition}[Rat denominator empty-unit strict product support]
\label{def:field-rat-denominator-empty-unit-strict-product-support}
For the tuple $(C_D^u,R_D^u,\mu_D^u,e_D^u)$ of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance},
the strict product support predicate is the genuine Rat denominator endpoint
predicate
$$
N_D^u(h)\Longleftrightarrow\mathsf{RatHistoryCarrier}(h).
$$
Thus the empty endpoint is carried by $C_D^u$ but is not in the strict support.
\end{definition}

\begin{theorem}[Rat denominator empty-unit product nonempty iff]
\label{thm:field-rat-denominator-empty-unit-product-nonempty-iff}
For histories $h,k:\mathsf{BHist}$ with $C_D^u(h)$ and $C_D^u(k)$, the
empty-unit product is nonempty exactly when at least one input has strict Rat
denominator support:
$$
(\mu_D^u(h,k)\hsame\emp\to\mathsf{False})
\Longleftrightarrow
N_D^u(h)\lor N_D^u(k).
$$
\end{theorem}
\begin{proof}
Unfold $C_D^u$, $\mu_D^u$, and $N_D^u$. The product
$\mu_D^u(h,k)$ is $\mathsf{append}(h,k)$, with canonical continuation witness
$\Cont(h,k,\mathsf{append}(h,k))$.

For the forward direction, apply
\autoref{thm:rat-denom-unit-continuation-nonempty-rat-endpoint} to the two
$\mathsf{RatDenomUnitCarrier}$ witnesses and the canonical continuation. The
assumption that $\mathsf{append}(h,k)$ is not $\hsame$ to $\emp$ is exactly the
nonempty result condition, so the theorem returns
$\mathsf{RatHistoryCarrier}(h)\lor\mathsf{RatHistoryCarrier}(k)$.

For the reverse direction, split the strict-support disjunction. In the left
case, split the carried endpoint $k$ into its empty and genuine alternatives.
If $k\hsame\emp$, the right append unit law identifies
$\mathsf{append}(h,k)$ with $h$, so any empty product would make $h\hsame\emp$,
contradicting $\mathsf{RatHistoryCarrier\_not\_empty}$. If
$\mathsf{RatHistoryCarrier}(k)$, apply Rat denominator continuation result
nonemptiness to the canonical continuation.

In the right strict-support case, split the carried endpoint $h$ in the same
way. If $h\hsame\emp$, the left append unit law identifies
$\mathsf{append}(h,k)$ with $k$, and
$\mathsf{RatHistoryCarrier\_not\_empty}$ rejects an empty product. If
$\mathsf{RatHistoryCarrier}(h)$, the same Rat denominator continuation result
nonemptiness theorem applies to the canonical continuation.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_rat\_denominator\_empty\_unit\_product\_nonempty\_iff}

Per-object template: traditional target strict support for the empty-unit
denominator multiplication; BEDC source $\mathsf{RatHistoryCarrier}$,
$\mathsf{RatDenomUnitCarrier}$, and $\Cont$; carrier $C_D^u$; classifier
$R_D^u$; stability obligation product support exactness; ledger continuation
witness; core theorem
\autoref{thm:field-rat-denominator-empty-unit-product-nonempty-iff}; exactness
predicate nonempty product iff genuine denominator input; standard bridge
ordinary denominator monoid support; status $\mathsf{ProofTarget}$.

\begin{definition}[Rat denominator empty-unit multiplicative substrate]
\label{def:field-rat-denominator-empty-unit-multiplicative-substrate}
The denominator-side multiplicative substrate for
$\FieldUp(\mathsf{RatUp})$ is the concrete $\mathsf{BHist}$ tuple
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ with carrier
$\mathsf{RatDenomUnitCarrier}$, classifier
$\mathsf{RatDenomUnitClassifier}$, multiplication
$\mu_D^u(h,k):=\mathsf{append}(h,k)$ equipped with the canonical continuation
$\Cont(h,k,\mathsf{append}(h,k))$, unit $e_D^u:=\emp$, and strict support
$N_D^u(h)$ given by $\mathsf{RatHistoryCarrier}(h)$. Thus the denominator
schema instance is the concrete append substrate over $\mathsf{BHist}$, with
the empty endpoint adjoined only as the unit and not as an uninterpreted
multiplication input.
\end{definition}

\begin{theorem}[Rat denominator empty-unit multiplicative substrate laws]
\label{thm:field-rat-denominator-empty-unit-multiplicative-substrate-laws}
For the substrate
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ of
\autoref{def:field-rat-denominator-empty-unit-multiplicative-substrate}, the
following concrete laws hold.
\begin{enumerate}
  \item The unit is carried: $C_D^u(e_D^u)$.
  \item The carrier is closed under multiplication: if $C_D^u(h)$ and
  $C_D^u(k)$, then $C_D^u(\mu_D^u(h,k))$.
  \item The unit laws hold: if $C_D^u(h)$, then
  $R_D^u(\mu_D^u(e_D^u,h),h)$ and $R_D^u(\mu_D^u(h,e_D^u),h)$.
  \item Multiplication is associative up to the classifier: if $C_D^u(h)$,
  $C_D^u(k)$, and $C_D^u(\ell)$, then
  $R_D^u(\mu_D^u(\mu_D^u(h,k),\ell),\mu_D^u(h,\mu_D^u(k,\ell)))$.
  \item The operation respects binary classifier input: if $R_D^u(h,h')$ and
  $R_D^u(k,k')$, then $R_D^u(\mu_D^u(h,k),\mu_D^u(h',k'))$.
  \item Multiplication is commutative up to the classifier: if $C_D^u(h)$ and
  $C_D^u(k)$, then $R_D^u(\mu_D^u(h,k),\mu_D^u(k,h))$.
  \item Strict support is exact for products: if $C_D^u(h)$ and $C_D^u(k)$,
  then
  $$
  (\mu_D^u(h,k)\hsame\emp\to\mathsf{False})
  \Longleftrightarrow
  N_D^u(h)\lor N_D^u(k).
  $$
  \item Empty endpoint contexts preserve and reflect the classifier: if
  $p\hsame\emp$, $q\hsame\emp$, $p'\hsame\emp$, and $q'\hsame\emp$, then
  $$
  R_D^u(\mathsf{append}(p,\mathsf{append}(h,q)),
  \mathsf{append}(p',\mathsf{append}(k,q')))
  \Longleftrightarrow
  R_D^u(h,k).
  $$
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold the concrete substrate of
\autoref{def:field-rat-denominator-empty-unit-multiplicative-substrate}. The
carried unit, closure, left and right unit laws, associativity, and binary
classifier congruence are the five laws supplied by
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws}.

Commutativity is the additional classifier law of
\autoref{thm:field-rat-denominator-empty-unit-commutative-monoid-laws}; it uses
the same carried endpoints and the concrete append multiplication of the
substrate.

The strict support clause is exactly
\autoref{thm:field-rat-denominator-empty-unit-product-nonempty-iff}, because
$N_D^u$ is $\mathsf{RatHistoryCarrier}$ and the product is
$\mathsf{append}(h,k)$ with its canonical continuation witness.

For the empty-context classifier clause, replace $R_D^u$ by
$\mathsf{RatDenomUnitClassifier}$ and apply
\autoref{thm:field-rat-denominator-unit-classifier-empty-context-exactness}.
This proves preservation and reflection for the same concrete classifier used
by the substrate.
\end{proof}

\begin{theorem}[Rat denominator empty-unit classifier empty-context exactness]
\label{thm:field-rat-denominator-unit-classifier-empty-context-exactness}
For histories $p,q,p',q',h,k:\mathsf{BHist}$, if
$p\hsame\emp$, $q\hsame\emp$, $p'\hsame\emp$, and $q'\hsame\emp$, then
empty endpoint contexts do not change the empty-unit denominator classifier:
$$
\mathsf{RatDenomUnitClassifier}(\mathsf{append}(p,\mathsf{append}(h,q)),
\mathsf{append}(p',\mathsf{append}(k,q')))
\Longleftrightarrow
\mathsf{RatDenomUnitClassifier}(h,k).
$$
\end{theorem}
\begin{proof}
Unfold $\mathsf{RatDenomUnitClassifier}$ into the two
$\mathsf{RatDenomUnitCarrier}$ endpoints and the endpoint comparison by
$\hsame$. The carrier endpoints are handled by
\autoref{thm:rat-denom-unit-carrier-empty-context-iff}, once for
$(p,q,h)$ and once for $(p',q',k)$.

For the forward direction, the contextual classifier supplies the two
contextual carrier witnesses and the comparison between the contextual
endpoints. The two carrier equivalences transport the carrier witnesses back
to $h$ and $k$. The empty left and right append unit laws identify
$\mathsf{append}(p,\mathsf{append}(h,q))$ with $h$ and
$\mathsf{append}(p',\mathsf{append}(k,q'))$ with $k$; composing these
identifications by symmetry and transitivity with the contextual comparison
gives $h\hsame k$. These three pieces repack
$\mathsf{RatDenomUnitClassifier}(h,k)$.

For the reverse direction, start from the carrier witnesses for $h$ and $k$
and from $h\hsame k$. The same two carrier equivalences transport membership
to the contextual endpoints. The empty append identifications transport
$h\hsame k$ to the contextual endpoint comparison, again using symmetry and
transitivity of $\hsame$. The transported carriers and transported endpoint
comparison pack the contextual classifier.

This is not a variant of the carrier exactness theorem: it proves full
classifier preservation by combining carrier exactness at both endpoints with
transport of the endpoint $\hsame$ relation. Consequently the
$\FieldUp(\mathsf{RatUp})$ denominator envelope can read an empty external
unit context by the normalized classifier on the core endpoints.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.RatDenomUnitClassifier\_empty\_context\_iff}

\begin{definition}[Rat denominator empty-unit contextual commutative instance]
\label{def:field-rat-denominator-empty-unit-contextual-commutative-instance}
The concrete empty-unit contextual commutative instance over $\mathsf{BHist}$
uses the carrier
$C_D^u:=\mathsf{RatDenomUnitCarrier}$, the classifier
$R_D^u:=\mathsf{RatDenomUnitClassifier}$, multiplication
$\mu_D^u(h,k):=\mathsf{append}(h,k)$, and unit $e_D^u:=\emp$.
Its admissible external left and right contexts are exactly the histories
history-same to the empty endpoint, written $p\hsame\emp$ and $q\hsame\emp$.
\end{definition}

\begin{theorem}[Rat denominator empty-unit contextual commutativity]
\label{thm:field-rat-denominator-empty-unit-contextual-commutativity}
For histories $p,q,p',q',h,k:\mathsf{BHist}$, if
$p\hsame\emp$, $q\hsame\emp$, $p'\hsame\emp$, $q'\hsame\emp$,
$C_D^u(h)$, and $C_D^u(k)$, then denominator multiplication commutes inside
the admissible empty external contexts:
$$
R_D^u(\mathsf{append}(p,\mathsf{append}(\mu_D^u(h,k),q)),
\mathsf{append}(p',\mathsf{append}(\mu_D^u(k,h),q'))).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:field-rat-denominator-empty-unit-commutative-monoid-laws} to the
two carrier hypotheses $C_D^u(h)$ and $C_D^u(k)$. The commutativity clause gives
the core classifier
$$
R_D^u(\mu_D^u(h,k),\mu_D^u(k,h)).
$$

Now use the reverse direction of
\autoref{thm:field-rat-denominator-unit-classifier-empty-context-exactness}
with the four endpoint hypotheses
$p\hsame\emp$, $q\hsame\emp$, $p'\hsame\emp$, and $q'\hsame\emp$. It transports
the core classifier through the two empty left contexts and the two empty right
contexts.

Since the contextual instance of
\autoref{def:field-rat-denominator-empty-unit-contextual-commutative-instance}
has $\mu_D^u(x,y)=\mathsf{append}(x,y)$ and
$R_D^u=\mathsf{RatDenomUnitClassifier}$, the transported classifier is exactly
the displayed empty-context commutativity statement.
\end{proof}

\begin{theorem}[Rat denominator external-unit envelope commutative monoid laws]
\label{thm:field-rat-denominator-unit-envelope-commutative-monoid-laws}
For the tuple $(C_D^\ast,R_D^\ast,\mu_D^\ast,e_D^\ast)$ of
\autoref{def:field-rat-denominator-unit-envelope}, all laws of
\autoref{thm:field-rat-denominator-unit-envelope-monoid-laws} hold, and the
envelope multiplication is commutative up to its endpoint classifier: whenever
$C_D^\ast(h)$ and $C_D^\ast(k)$,
$$
R_D^\ast(\mu_D^\ast(h,k),\mu_D^\ast(k,h)).
$$
\end{theorem}
\begin{proof}
The carrier inhabitance, closure, associativity, left and right unit laws,
binary classifier congruence, carried restriction, and carried one-sided unit
obstruction are exactly the fields supplied by
\autoref{thm:field-rat-denominator-unit-envelope-monoid-laws}.

It remains to prove the displayed commutativity classifier. Split the two
carrier witnesses $C_D^\ast(h)$ and $C_D^\ast(k)$ into their carried Rat
denominator alternatives and their external-empty alternatives.

In the carried/carried case, $\mu_D^\ast(h,k)$ and $\mu_D^\ast(k,h)$ are the
canonical append continuations of the two carried denominators in opposite
orders. Apply
\autoref{thm:field-rat-denominator-continuation-commutative-classifier} to these
two continuations. The carried classifier exactness theorem
\autoref{thm:field-rat-denominator-unit-envelope-carried-classifier-exactness}
then repacks the resulting $\mathsf{RatHistoryClassifier}$ as the required
$R_D^\ast$ comparison.

In the carried/empty case, the append right-unit and left-unit laws identify
$\mu_D^\ast(h,k)$ and $\mu_D^\ast(k,h)$ with the same carried endpoint $h$,
up to $\hsame$. The envelope classifier normal form,
\autoref{thm:field-rat-denominator-unit-envelope-classifier-normal-form},
turns those two endpoint comparisons and the transported carried witness into
$R_D^\ast(\mu_D^\ast(h,k),\mu_D^\ast(k,h))$.

The empty/carried case is symmetric. The same append unit laws identify the two
products with the carried endpoint $k$, and the same normal form converts the
two transported carrier witnesses and the resulting $\hsame$ comparison into
the envelope classifier.

In the empty/empty case, both products are $\hsame$ to $\emp$ by the append unit
laws and transitivity of $\hsame$. Since $e_D^\ast=\emp$, the empty branch of
$R_D^\ast$ classifies the two products directly.
\end{proof}

Per-object template: traditional target commutative denominator monoid boundary
for $\FieldUp(\mathsf{RatUp})$; BEDC source $\mathsf{RatHistoryCarrier}$,
$\mathsf{RatHistoryClassifier}$, $\Cont$, $\mathsf{append}$, and $\emp$;
carrier $C_D^\ast$; classifier $R_D^\ast$; stability obligation monoid laws
plus commutativity; ledger external-unit bit plus continuation witness; core
theorem
\autoref{thm:field-rat-denominator-unit-envelope-commutative-monoid-laws};
exactness predicate envelope commutativity over the strict carried restriction;
standard bridge deferred; status $\mathsf{ProofTarget}$.

\begin{definition}[Rat denominator external-unit envelope strict support]
\label{def:field-rat-denominator-unit-envelope-strict-support}
For the tuple $(C_D^\ast,R_D^\ast,\mu_D^\ast,e_D^\ast)$ of
\autoref{def:field-rat-denominator-unit-envelope}, the strict support
predicate is the genuine Rat denominator endpoint predicate
$$
N_D^\ast(h)\Longleftrightarrow\mathsf{RatHistoryCarrier}(h).
$$
Thus the external endpoint is carried by $C_D^\ast$ but does not enter the
strict denominator support.
\end{definition}

\begin{theorem}[Rat denominator external-unit envelope strict support laws]
\label{thm:field-rat-denominator-unit-envelope-strict-support-laws}
For histories $h,k:\mathsf{BHist}$ with $C_D^\ast(h)$ and $C_D^\ast(k)$, the
strict support predicate of
\autoref{def:field-rat-denominator-unit-envelope-strict-support} satisfies:
\begin{enumerate}
  \item $R_D^\ast(h,k)\to
  (N_D^\ast(h)\Longleftrightarrow N_D^\ast(k))$.
  \item $N_D^\ast(\mu_D^\ast(h,k))\Longleftrightarrow
  N_D^\ast(h)\lor N_D^\ast(k)$.
  \item Together with
  \autoref{thm:field-rat-denominator-unit-envelope-commutative-monoid-laws},
  these clauses give a concrete $\mathsf{BHist}$ multiplicative-support
  boundary for $\FieldUp(\mathsf{RatUp})$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold $C_D^\ast$, $R_D^\ast$, $\mu_D^\ast$, and $N_D^\ast$. The carrier
assumptions split each input into a genuine Rat denominator endpoint or an
external empty endpoint.

For support transport along the classifier, split the defining alternatives of
$R_D^\ast(h,k)$. In the guarded carried branch, both strict-support predicates
are exactly the displayed $\mathsf{RatHistoryCarrier}$ witnesses, so the two
directions are projections. In the empty-empty branch, any strict-support
witness for either endpoint contradicts $\mathsf{RatHistoryCarrier\_not\_empty}$
after composing its endpoint comparison with the branch comparison to $\emp$.

For product support, first consider the carried/carried branch. The canonical
continuation witness for $\mathsf{append}(h,k)$ and carried denominator closure
give $\mathsf{RatHistoryCarrier}(\mu_D^\ast(h,k))$, while the right side holds
by either input support.

In the carried/empty branch, the append right-unit law identifies
$\mu_D^\ast(h,k)$ with $h$. Transport
$\mathsf{RatHistoryCarrier}(h)$ across this $\hsame$ comparison by
$\mathsf{RatHistoryCarrier\_hsame\_transport}$, and the left disjunct supplies
the reverse implication. The empty/carried branch is symmetric, using the
append left-unit law and the right strict-support disjunct.

In the empty/empty branch, the append unit laws identify
$\mu_D^\ast(h,k)$ with $\emp$. Therefore product strict support is impossible
by $\mathsf{RatHistoryCarrier\_not\_empty}$, and both input strict-support
predicates are impossible by the same theorem. This proves the displayed
product equivalence.

The final clause only packages these two support laws with the already stated
commutative monoid laws: the monoid theorem supplies the algebraic envelope,
and the present theorem supplies the exact descent of genuine denominator
support through classifier transport and multiplication.
\end{proof}

Per-object template: downstream target $\FieldUp(\mathsf{RatUp})$ denominator
support boundary; BEDC source $\mathsf{RatHistoryCarrier}$,
$\mathsf{RatHistoryClassifier}$, $\Cont$, $\mathsf{append}$, and $\emp$;
carrier $C_D^\ast$; classifier $R_D^\ast$; stability obligation strict support
transport and product support exactness; ledger external-unit bit plus
continuation witness; core theorem
\autoref{thm:field-rat-denominator-unit-envelope-strict-support-laws};
exactness predicate genuine Rat denominator support descends through
$R_D^\ast$ and $\mu_D^\ast$; standard bridge
$\FieldUp(\mathsf{RatUp})$ denominator support boundary; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Rat denominator empty-unit cancellative commutative monoid package]
\label{thm:field-rat-denominator-empty-unit-cancellative-commutative-monoid-package}
For the tuple $(C_D^u,R_D^u,\mu_D^u,e_D^u)$ of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance},
the laws of
\autoref{thm:field-rat-denominator-empty-unit-commutative-monoid-laws} hold.
Moreover, for carried histories $h,k,q:\mathsf{BHist}$, the endpoint classifier
is cancellative on both sides:
$$
C_D^u(h)\land C_D^u(k)\land C_D^u(q)\land
R_D^u(\mu_D^u(h,q),\mu_D^u(k,q))
\to R_D^u(h,k),
$$
and
$$
C_D^u(h)\land C_D^u(k)\land C_D^u(q)\land
R_D^u(\mu_D^u(q,h),\mu_D^u(q,k))
\to R_D^u(h,k).
$$
\end{theorem}
\begin{proof}
Unfold the concrete data of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance}.
The algebraic fields are exactly the commutative monoid laws supplied by
\autoref{thm:field-rat-denominator-empty-unit-commutative-monoid-laws}.

For right cancellation, split the three
$\mathsf{RatDenomUnitCarrier}$ witnesses for $h$, $k$, and $q$ into empty
endpoint and genuine Rat denominator endpoint alternatives.

In the genuine/genuine/genuine branch, the two classified products are displayed
continuation endpoints with the same genuine right context $q$. Apply
\autoref{thm:field-rat-denominator-continuation-common-context-cancellation}
with empty unary prefix and common suffix $q$, then use
\autoref{thm:field-rat-denominator-matched-context-classifier-exactness} to read
the obtained comparison as the denominator classifier $R_D^u(h,k)$.

If the common right factor $q$ is empty, the one-sided unit laws from
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws} identify
$\mu_D^u(h,q)$ with $h$ and $\mu_D^u(k,q)$ with $k$. Transporting the displayed
classifier across those endpoint identifications gives $R_D^u(h,k)$.

If one of $h$ and $k$ is empty and the other is genuine, the displayed product
classifier forces a mixed endpoint comparison after the same unit reduction on
the empty side. The empty-unit endpoint exactness clauses from
\autoref{sec:field-rat-denominator-unit-endpoint-exactness} separate the empty
endpoint from the genuine denominator endpoint, so the only classifiable mixed
case is the one already carrying the comparison needed for $R_D^u(h,k)$.

The empty/empty branch is immediate: both endpoints are $\hsame\emp$, and the
definition of $\mathsf{RatDenomUnitClassifier}$ packs the two carried witnesses
with the resulting $\hsame$ comparison.

Left cancellation follows from right cancellation and commutativity. Apply
\autoref{thm:field-rat-denominator-empty-unit-commutative-monoid-laws} to commute
both displayed left products into right products, use the right-cancellation
clause just proved, and transport the classifier back along the same
commutativity comparisons.
\end{proof}

\textbf{Traditional target}: cancellative commutative denominator monoid for
$\FieldUp(\mathsf{RatUp})$; \textbf{BEDC source}:
$\mathsf{RatHistoryCarrier}$, $\mathsf{RatDenomUnitCarrier}$, $\Cont$, and
$\mathsf{append}$; \textbf{carrier}: $C_D^u$; \textbf{classifier}: $R_D^u$;
\textbf{stability obligations}: monoid laws, commutativity, and cancellation;
\textbf{ledger}: external empty-unit provenance; \textbf{core theorem}:
\autoref{thm:field-rat-denominator-empty-unit-cancellative-commutative-monoid-package};
\textbf{exactness predicate}: cancellative endpoint classification;
\textbf{standard bridge}: ordinary positive-denominator multiplication with
adjoined unit; \textbf{status}: $\mathsf{ProofTarget}$.

\begin{theorem}[Rat denominator empty-unit right cancellation]
\label{thm:field-rat-denominator-empty-unit-right-cancellation}
For the tuple $(C_D^u,R_D^u,\mu_D^u,e_D^u)$ of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance},
right multiplication by a carried empty-unit denominator endpoint is exact for
the classifier. For all histories $h,k,t:\mathsf{BHist}$, if $C_D^u(h)$,
$C_D^u(k)$, $C_D^u(t)$, and
$$
R_D^u(\mu_D^u(h,t),\mu_D^u(k,t)),
$$
then
$$
R_D^u(h,k).
$$
\end{theorem}
\begin{proof}
Unfold the data of
\autoref{def:field-rat-denominator-empty-unit-continuation-monoid-instance}.
Thus $\mu_D^u$ is $\mathsf{append}$, $C_D^u$ is
$\mathsf{RatDenomUnitCarrier}$, and $R_D^u$ is
$\mathsf{RatDenomUnitClassifier}$.

First consider the branch where $t$ is the empty endpoint. The right-unit
clause in
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws}
classifies $\mathsf{append}(h,t)$ with $h$ and $\mathsf{append}(k,t)$ with
$k$. Transport the displayed classifier across these two right-unit
classifications, using symmetry and transitivity of $\hsame$ inside
$\mathsf{RatDenomUnitClassifier}$, to obtain
$\mathsf{RatDenomUnitClassifier}(h,k)$.

Now consider the branch where $t$ is a genuine Rat denominator endpoint. If one
of $h$ and $k$ is empty and the other is genuine, the displayed product
classifier is a mixed carried-empty endpoint after cancelling the visible
append presentation; this contradicts
\autoref{thm:field-rat-denominator-unit-envelope-mixed-endpoint-separation}.
The empty/empty subcase packs the classifier directly from the two empty
endpoint witnesses.

It remains to handle the genuine/genuine subcase. The products
$\mu_D^u(h,t)$ and $\mu_D^u(k,t)$ are the canonical append continuation
endpoints with common suffix $t$. Apply
\autoref{thm:field-rat-denominator-continuation-common-context-cancellation}
with the empty prefix, the common suffix $t$, and the two canonical
continuation witnesses for $\Cont(h,t,\mathsf{append}(h,t))$ and
$\Cont(k,t,\mathsf{append}(k,t))$. The result is
$\mathsf{RatHistoryClassifier}(h,k)$, and the original carrier hypotheses
repack it as $\mathsf{RatDenomUnitClassifier}(h,k)$.
\end{proof}

Per-object template: traditional target cancellation in the denominator monoid
boundary; BEDC source $\mathsf{RatDenomUnitCarrier}$,
$\mathsf{RatDenomUnitClassifier}$, $\Cont$, and $\mathsf{append}$; carrier
$C_D^u$; classifier $R_D^u$; stability obligation right cancellation; ledger
the common suffix continuation witness; core theorem
\autoref{thm:field-rat-denominator-empty-unit-right-cancellation}; exactness
predicate cancellation of a shared denominator factor; standard bridge
ordinary cancellative commutative monoids; status $\mathsf{ProofTarget}$.

\begin{theorem}[Field affine automorphism inverse is involutive]
\label{thm:field-affine-automorphism-inverse-involutive}
For a BEDC history-level $\FieldUp+\CommRingUp$ candidate over
$\mathsf{BHist}$ satisfying the hypotheses of
\autoref{thm:field-affine-automorphism-group-certificate}, let
$T=(a,b,d,p_a,p_b)$ be a certified affine automorphism and let
$T^{-1}_{\mathrm{aff}}$ be the explicit inverse certificate supplied by
\autoref{thm:field-affine-inverse-is-certified-affine}. Then applying the same
inverse construction again returns the original affine certificate under the
affine pointwise classifier:
$$
(T^{-1}_{\mathrm{aff}})^{-1}_{\mathrm{aff}}\equiv_{\mathrm{aff}}T.
$$
\end{theorem}
\begin{proof}
The inverse certificate $T^{-1}_{\mathrm{aff}}$ is closed among certified
affine automorphisms by
\autoref{thm:field-affine-inverse-is-certified-affine}. The identity and
inverse operations of the affine package are the ones recorded in
\autoref{thm:field-affine-automorphism-identity-and-inverse-package}, so both
$T$ and $T^{-1}_{\mathrm{aff}}$ inhabit the same $\GroupUp$ carrier of
certified affine automorphism histories.

The two inverse equations for $T$ are interpreted through the affine pointwise
classifier. Operation congruence from
\autoref{thm:field-affine-pointwise-classifier-operation-congruence}
transports those equations across classifier-equivalent representatives, so the
candidate obtained by inverting $T^{-1}_{\mathrm{aff}}$ is a left inverse of
$T^{-1}_{\mathrm{aff}}$ in the same group package.

Now apply the $\GroupUp$ inverse involutivity law from
\autoref{thm:group-left-inverse-involutive}; equivalently, use the
left-right inverse uniqueness principle of
\autoref{thm:group-left-right-inverse-uniqueness} to identify the second
inverse with the original certificate. Re-reading that identity through the
affine classifier gives the displayed conclusion.
\end{proof}

Per-object template: traditional target affine automorphism group inverse
involutivity; BEDC source $\FieldUp+\CommRingUp$, affine package, and
$\GroupUp$ inverse laws; carrier certified affine automorphism histories;
classifier $\equiv_{\mathrm{aff}}$; stability obligation inverse closure and
inverse uniqueness; ledger nonzero coefficient witnesses and
inverse-certificate provenance; core theorem
\autoref{thm:field-affine-automorphism-inverse-involutive}; exactness predicate
double-inverse pointwise classifier; standard bridge ordinary affine
automorphism group; status $\mathsf{ProofTarget}$.

\begin{theorem}[Field affine automorphism cancellation]
\label{thm:field-affine-automorphism-cancellation}
For a BEDC history-level $\FieldUp+\CommRingUp$ candidate over
$\mathsf{BHist}$ satisfying
\autoref{thm:field-affine-automorphism-group-certificate}, let $S,T,U$ be
certified affine automorphism histories in $\mathsf{AffAut}_F$, and let
$\circ_{\mathrm{aff}}$ be the coefficient-collected composition operation from
the affine package. Then the affine classifier is cancellative on both sides:
if
$$
(S\circ_{\mathrm{aff}}T)\equiv_{\mathrm{aff}}(U\circ_{\mathrm{aff}}T),
$$
then $S\equiv_{\mathrm{aff}}U$; and if
$$
(T\circ_{\mathrm{aff}}S)\equiv_{\mathrm{aff}}(T\circ_{\mathrm{aff}}U),
$$
then $S\equiv_{\mathrm{aff}}U$.
\end{theorem}
\begin{proof}
Instantiate the affine automorphism group law package supplied by
\autoref{thm:field-affine-automorphism-group-certificate}. Its carrier is the
class of certified affine automorphism histories, its operation is
$\circ_{\mathrm{aff}}$, and its classifier is the affine pointwise relation
$\equiv_{\mathrm{aff}}$.

For the common right factor, read
$(S\circ_{\mathrm{aff}}T)\equiv_{\mathrm{aff}}(U\circ_{\mathrm{aff}}T)$ as the
group classifier equality of the two products with the same right component.
Apply \autoref{thm:group-right-cancellation} in this instantiated
$\GroupUp$ package. The conclusion is the group classifier relation between
$S$ and $U$, which is exactly $S\equiv_{\mathrm{aff}}U$ under the pointwise
classifier interpretation.

For the common left factor, read
$(T\circ_{\mathrm{aff}}S)\equiv_{\mathrm{aff}}(T\circ_{\mathrm{aff}}U)$ as the
group classifier equality of the two products with the same left component.
Apply \autoref{thm:group-left-cancellation} in the same instantiated package.
Translating the resulting group classifier back through the affine pointwise
classifier gives $S\equiv_{\mathrm{aff}}U$.
\end{proof}

Per-object template: traditional target affine automorphism cancellativity;
BEDC source $\FieldUp+\CommRingUp$, the affine package, and $\GroupUp$
cancellation; carrier certified affine automorphism histories; classifier
$\equiv_{\mathrm{aff}}$; stability associativity, identity, inverse, and
operation congruence; ledger nonzero coefficient witnesses and composition
provenance; core theorem
\autoref{thm:field-affine-automorphism-cancellation}; exactness predicate left
and right cancellation under the affine classifier; standard bridge ordinary
affine automorphism group; status $\mathsf{ProofTarget}$.
