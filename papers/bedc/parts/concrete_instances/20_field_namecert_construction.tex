\input{parts/concrete_instances/field/20_field_singleton_schema}
\input{parts/concrete_instances/field/20_field_rat_boundary}
\input{parts/concrete_instances/field/20_field_rat_continuation_closure}
\input{parts/concrete_instances/field/20_field_rat_denominator_context_exactness}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_obstruction}
\input{parts/concrete_instances/field/20_field_rat_denominator_adjoined_unit_laws}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_carrier_transport_closure_package}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_visible_carrier}
\input{parts/concrete_instances/field/20_field_inverse_laws}
\input{parts/concrete_instances/field/20_field_zero_product_laws}
\input{parts/concrete_instances/field/20_field_affine_equation_laws}
\input{parts/concrete_instances/field/20_field_product_right_equation_exactness}
\input{parts/concrete_instances/field/20_field_affine_explicit_inverse}
\input{parts/concrete_instances/field/20_field_affine_solution_uniqueness}
\input{parts/concrete_instances/field/20_field_affine_automorphism_package}
\input{parts/concrete_instances/field/20_field_signed_square_consequences}
\input{parts/concrete_instances/field/20_field_product_apartness_consequences}
\input{parts/concrete_instances/field/20_field_zero_apartness_and_square_descent}
\input{parts/concrete_instances/field/20_field_rat_denominator_concrete_continuation_instance}
\input{parts/concrete_instances/field/20_field_zero_apartness_checked_markers}
\input{parts/concrete_instances/field/20_field_square_apartness_consequences}
\input{parts/concrete_instances/field/20_field_square_apartness_and_affine_exactness}
\input{parts/concrete_instances/field/20_field_certificate_record}
\input{parts/concrete_instances/field/20_field_rat_denominator_empty_unit_endpoint_exactness}
\input{parts/concrete_instances/field/20_field_rat_denominator_external_unit_package}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_envelope_laws}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_envelope_certificate}
\input{parts/concrete_instances/field/20_field_singleton_empty_concrete_instance}
\input{parts/concrete_instances/field/20_field_rat_denominator_external_unit_envelope}
\input{parts/concrete_instances/field/20_field_rat_denominator_external_unit_boundary}
\input{parts/concrete_instances/field/20_field_rat_denominator_strict_monoid_boundary}
\input{parts/concrete_instances/field/20_field_rat_denominator_empty_unit_commutative_support}
\input{parts/concrete_instances/field/20_field_contextual_and_affine_boundaries}
\input{parts/concrete_instances/field/20_field_rat_denominator_empty_unit_field_boundary}
\input{parts/concrete_instances/field/20_field_rat_denominator_empty_unit_contextual_multiplication_classifier_exactness}
\input{parts/concrete_instances/field/20_field_rat_denominator_fieldup_multiplicative_certificate_boundary}
\input{parts/concrete_instances/field/20_field_rat_denominator_parent_certificate_rows}
\input{parts/concrete_instances/field/20_field_rat_denominator_bilateral_strict_factor_cancellation}
\input{parts/concrete_instances/field/20_field_rat_denominator_contextual_action_certificate}
\input{parts/concrete_instances/field/20_field_rat_denominator_complete_contextual_row_package}

\begin{theorem}[RatUp FieldUp denominator contextual row closure package]
\label{thm:ratup-fieldup-denominator-contextual-row-closure-package}
For the concrete denominator tuple
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ given by
$$
C_D^u=\mathsf{RatDenomUnitCarrier},\qquad
R_D^u=\mathsf{RatDenomUnitClassifier},
$$
$$
\mu_D^u=\mathsf{append},\qquad
e_D^u=\emp,\qquad
N_D^u=\mathsf{RatHistoryCarrier},
$$
and for the action $\Gamma$ of
\autoref{def:ratup-fieldup-denominator-contextual-action-instance}, the
$\FieldUp(\mathsf{RatUp})$ denominator package contains the row laws and
assembly of
\autoref{thm:ratup-fieldup-denominator-complete-contextual-row-package}, the
total contextual action certificate of
\autoref{thm:field-rat-denominator-contextual-action-total-certificate}, the
two-layer composition closure of
\autoref{thm:ratup-fieldup-denominator-contextual-action-composition-closure},
the exact two-layer classifier and strict-support readback of
\autoref{thm:field-rat-denominator-contextual-action-composition}, the
normalized contextual action exactness of
\autoref{thm:field-rat-denominator-normalized-contextual-action-exactness},
the threefold contextual normalization of
\autoref{thm:field-rat-denominator-contextual-action-threefold-normalization},
and the envelope bilateral cancellation law of
\autoref{thm:field-rat-denominator-envelope-bilateral-context-cancellation}.
\end{theorem}
\begin{proof}
First cite
\autoref{thm:ratup-fieldup-denominator-complete-contextual-row-package}. It
fixes the concrete BHist carrier, classifier, append operation, empty unit,
and strict-support predicate as
$\mathsf{RatDenomUnitCarrier}$, $\mathsf{RatDenomUnitClassifier}$,
$\mathsf{append}$, $\emp$, and $\mathsf{RatHistoryCarrier}$, and it supplies
the row laws together with the assembled denominator subcertificate.

Next cite
\autoref{thm:field-rat-denominator-contextual-action-total-certificate}. This
is the one-step contextual action certificate for the same carrier,
classifier, append, unit, strict-support predicate, and action $\Gamma$.

The two carried-action rows are supplied by
\autoref{thm:ratup-fieldup-denominator-contextual-action-composition-closure}
and \autoref{thm:field-rat-denominator-contextual-action-composition}. The
first theorem gives closure of two carried contextual applications in
$C_D^u$; the second gives classifier reflection and the five-term strict
support readback for the exact two-layer action.

Normalized two-layer exactness is supplied by
\autoref{thm:field-rat-denominator-normalized-contextual-action-exactness}. It
composes the composition-closure row with the exact two-layer theorem: the
closure row identifies the nested action with the single action on appended
carried contexts, and the exactness row transports classifier equivalence and
strict-support readback to the normalized endpoints.

The threefold law is supplied by
\autoref{thm:field-rat-denominator-contextual-action-threefold-normalization}.
It applies the same closure twice and uses the concrete denominator
multiplicative rows to reassociate the append contexts, so three carried
contextual layers are classified by one normalized action with the displayed
seven-term strict-support readback.

Finally apply
\autoref{thm:field-rat-denominator-envelope-bilateral-context-cancellation}.
It supplies cancellation after strict carried denominator factors and empty
external endpoints, so the concrete denominator package records row laws,
contextual action, two-layer composition, exact readback, normalized exactness,
threefold normalization, and bilateral cancellation over the same BHist data.
\end{proof}

Per-object template: traditional target FieldUp denominator contextual row
closure for $\FieldUp(\mathsf{RatUp})$; BEDC source $\mathsf{BHist}$,
$\mathsf{RatHistoryCarrier}$, $\mathsf{RatDenomUnitCarrier}$,
$\mathsf{RatDenomUnitClassifier}$, $\mathsf{append}$, $\emp$, and the action
$\Gamma$; carrier $C_D^u$; classifier $R_D^u$; stability obligations
denominator row laws, one-step contextual action rows, two-layer contextual
composition, exact strict-support readback, normalized action exactness, three
carried-context normalization, and strict-factor cancellation; ledger empty
external endpoints, carried left and right denominator contexts, normalized
two-layer append contexts, and three carried left and right context layers; core
theorem \autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package}
with exactness rows
\autoref{thm:field-rat-denominator-normalized-contextual-action-exactness} and
\autoref{thm:field-rat-denominator-contextual-action-threefold-normalization};
exactness predicate denominator classifier reflection, contextual action
support readback, normalized classifier exactness, three carried-context
strict-support normalization, and envelope bilateral cancellation; standard
bridge FieldUp denominator boundary over $\RatUp$ histories; status
$\mathsf{ProofTarget}$.

\input{parts/concrete_instances/field/20_field_rat_denominator_contextual_action_unit_composition_laws}

\begin{theorem}[RatUp FieldUp denominator contextual row package with unit-composition laws]
\label{thm:ratup-fieldup-denominator-contextual-row-package-with-unit-composition}
For the concrete denominator tuple
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ given by
$$
C_D^u=\mathsf{RatDenomUnitCarrier},\qquad
R_D^u=\mathsf{RatDenomUnitClassifier},
$$
$$
\mu_D^u=\mathsf{append},\qquad
e_D^u=\emp,\qquad
N_D^u=\mathsf{RatHistoryCarrier},
$$
and for the contextual action $\Gamma$ of
\autoref{def:ratup-fieldup-denominator-contextual-action-instance}, the
$\FieldUp(\mathsf{RatUp})$ denominator package contains the rows of
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package}
together with the empty-action classifier unit, unchanged strict support,
context-multiplication composition, and five-term support law of
\autoref{thm:ratup-fieldup-denominator-contextual-action-unit-composition-laws}.
\end{theorem}
\begin{proof}
Apply
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package}. It
records the row laws, action exactness, two-layer composition readback, and
cancellation clauses for the concrete tuple
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ and the action $\Gamma$.

Apply
\autoref{thm:ratup-fieldup-denominator-contextual-action-unit-composition-laws}
over the same concrete data. Its hypotheses and conclusions use the same
$\mathsf{BHist}$ carrier, classifier, append operation, empty unit,
strict-support predicate, and contextual action fixed in
\autoref{def:ratup-fieldup-denominator-contextual-action-instance}.

Thus the two cited results occupy independent rows of the same denominator
interface: the first supplies row closure, action exactness, readback, and
cancellation, while the second supplies the empty-action unit, unchanged strict
support, context-multiplication composition, and five-term support law. The
package statement is their joint registration over the shared concrete RatUp
denominator data, without restating either theorem.
\end{proof}

\begin{theorem}[RatUp FieldUp denominator normalized contextual action package]
\label{thm:field-rat-denominator-normalized-contextual-action-package}
For the concrete tuple $(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ determined by
$\mathsf{RatDenomUnitCarrier}$, $\mathsf{RatDenomUnitClassifier}$,
$\mathsf{append}$, $\emp$, and $\mathsf{RatHistoryCarrier}$, and for the
concrete contextual action $\Gamma$ of
\autoref{def:ratup-fieldup-denominator-contextual-action-instance}, the
$\FieldUp(\mathsf{RatUp})$ denominator action package contains:
\begin{enumerate}
\item the row closure package of
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package};
\item the empty-action unit law and context-composition normalization law of
\autoref{thm:ratup-fieldup-denominator-contextual-action-unit-composition-laws};
\item the paired normalized classifier exactness and five-term strict-support
readbacks of
\autoref{thm:field-rat-denominator-normalized-contextual-action-exactness}.
\end{enumerate}
Thus the normalized denominator contextual action certificate is available as a
single non-parametric package over the same carried denominator contexts and
empty external endpoints.
\end{theorem}
\begin{proof}
Apply
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package}. This
supplies the concrete row closure package for the RatUp FieldUp denominator
carrier and classifier.

Apply
\autoref{thm:ratup-fieldup-denominator-contextual-action-unit-composition-laws}.
It gives the empty-action unit clause and the context-composition normalization
clause for the same action $\Gamma$.

Apply
\autoref{thm:field-rat-denominator-normalized-contextual-action-exactness}. It
supplies the paired normalized classifier exactness together with the five-term
strict-support readbacks.

The three cited results use the same concrete $\mathsf{BHist}$ carrier,
$\mathsf{RatDenomUnitClassifier}$, $\mathsf{append}$ operation, empty unit
$\emp$, strict-support predicate $N_D^u$, and action $\Gamma$. Therefore their
clauses assemble without transport into the stated denominator action package.
\end{proof}

Per-object template: traditional target normalized denominator contextual action
package for $\FieldUp(\mathsf{RatUp})$; BEDC source $\mathsf{BHist}$,
$\mathsf{RatHistoryCarrier}$, $\mathsf{RatDenomUnitCarrier}$,
$\mathsf{RatDenomUnitClassifier}$, $\mathsf{append}$, $\emp$, and $\Gamma$;
carrier $C_D^u$; classifier $R_D^u$; stability obligations row closure, action
unit, action composition normalization, normalized classifier exactness, and
strict-support readback; ledger carried left and right denominator contexts plus
empty external endpoints; core theorem
\autoref{thm:field-rat-denominator-normalized-contextual-action-package};
exactness predicate normalized classifier iff and five-term support iff;
standard bridge ordinary denominator context multiplication; status
$\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp denominator contextual action total normalization package]
\label{thm:ratup-fieldup-denominator-contextual-action-total-normalization-package}
For the concrete denominator tuple
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ and the contextual action $\Gamma$ of
\autoref{def:ratup-fieldup-denominator-contextual-action-instance}, the
$\FieldUp(\mathsf{RatUp})$ denominator action package contains the following
single certificate-level normalization record.

\begin{enumerate}
\item The empty action is the classifier unit. If $C_D^u(h)$, then
$$
C_D^u(\Gamma_{\emp,\emp,\emp,\emp}(h)),
$$
$$
R_D^u(\Gamma_{\emp,\emp,\emp,\emp}(h),h),
$$
and
$$
N_D^u(\Gamma_{\emp,\emp,\emp,\emp}(h))
\Longleftrightarrow
N_D^u(h).
$$

\item The two-layer contextual action normalizes to one collected carried action.
If $C_D^u(h)$, $C_D^u(k)$, $C_D^u(l)$, $C_D^u(r)$, $C_D^u(l')$, and
$C_D^u(r')$, and if all displayed external endpoints are $\hsame\emp$, then
$$
R_D^u(\Gamma_{p',q',l',r'}(\Gamma_{p,q,l,r}(h)),
\Gamma_{p',q',\mu_D^u(l',l),\mu_D^u(r,r')}(h)).
$$
For any second endpoint $k$ under the same collected carried contexts,
denominator classifier exactness is
$$
R_D^u(\Gamma_{s,t,\mu_D^u(l',l),\mu_D^u(r,r')}(h),
\Gamma_{s',t',\mu_D^u(l',l),\mu_D^u(r,r')}(k))
\Longleftrightarrow R_D^u(h,k).
$$
The nested endpoint has the five-term strict-support readback
$$
N_D^u(\Gamma_{p',q',l',r'}(\Gamma_{p,q,l,r}(h)))
\Longleftrightarrow
N_D^u(l')\lor N_D^u(l)\lor N_D^u(h)\lor N_D^u(r)\lor N_D^u(r').
$$

\item The threefold empty-external action normalizes to one collected action.
For $C_D^u(h)$ and carried contexts $l_0,r_0,l_1,r_1,l_2,r_2$, let
$$
H_3:=
\Gamma_{\emp,\emp,l_2,r_2}
(\Gamma_{\emp,\emp,l_1,r_1}(\Gamma_{\emp,\emp,l_0,r_0}(h))).
$$
Then
$$
C_D^u(H_3),
$$
$$
R_D^u\bigl(H_3,
\Gamma_{\emp,\emp,\mu_D^u(l_2,\mu_D^u(l_1,l_0)),
\mu_D^u(\mu_D^u(r_0,r_1),r_2)}(h)\bigr),
$$
and
$$
N_D^u(H_3)
\Longleftrightarrow
N_D^u(l_2)\lor N_D^u(l_1)\lor N_D^u(l_0)\lor N_D^u(h)
\lor N_D^u(r_0)\lor N_D^u(r_1)\lor N_D^u(r_2).
$$

\item The same record includes the surrounding row-closure assembly, the one-step
action-certificate rows, the exact readback rows, and the bilateral cancellation
fields listed in
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package}.
\end{enumerate}
\end{theorem}
\begin{proof}
The unit and two-layer action laws are exactly the concrete rows of
\autoref{thm:ratup-fieldup-denominator-contextual-action-unit-composition-laws}.
They supply the carried empty action, classifier identity, unchanged strict
support, the collected two-layer classifier comparison, and the five-term
strict-support readback.

For classifier exactness of the collected two-layer endpoint, apply
\autoref{thm:field-rat-denominator-normalized-contextual-action-exactness} with
the inner and outer carried contexts ordered so that the collected left context is
$\mu_D^u(l',l)$ and the collected right context is $\mu_D^u(r,r')$. Its
normalized row gives the displayed denominator classifier equivalence, and its
support rows agree with the five-term readback already recorded above.

The threefold row is
\autoref{thm:field-rat-denominator-contextual-action-threefold-normalization}.
It packages the carried threefold endpoint, the classifier comparison with the
single collected action, and the seven-term support readback for the empty
external layers.

Finally use
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package}. That
theorem supplies the surrounding row package, action certificate, exact readback,
and bilateral cancellation fields over the same concrete denominator tuple and
the same action $\Gamma$, so the displayed action-normalization clauses form one
certificate-level FieldUp denominator package.
\end{proof}

\section{Rat denominator context-pair action laws}
\label{sec:field-rat-denominator-context-pair-action-laws}

\begin{definition}[RatUp FieldUp denominator context-pair monoid]
\label{def:ratup-fieldup-denominator-context-pair-monoid}
The concrete denominator context-pair carrier for the RatUp FieldUp action is
the class of pairs $(l,r):\mathsf{BHist}\times\mathsf{BHist}$ such that
$C_D^u(l)$ and $C_D^u(r)$. Its unit is
$(e_D^u,e_D^u)$, and its pair product is
$$
(l,r)\star(l',r'):=(\mu_D^u(l',l),\mu_D^u(r,r')).
$$
The pair support predicate is
$$
\mathsf{PairSupp}_D^u(l,r)\Longleftrightarrow N_D^u(l)\lor N_D^u(r).
$$
The componentwise pair classifier is
$$
\mathsf{PairClass}_D^u((l,r),(l',r'))\Longleftrightarrow
R_D^u(l,l')\land R_D^u(r,r').
$$
\end{definition}

\begin{theorem}[RatUp FieldUp denominator context-pair operation stability]
\label{thm:ratup-fieldup-denominator-context-pair-operation-stability}
For the concrete denominator context-pair structure of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}, let $(l,r)$,
$(m,s)$, $(l',r')$, and $(m',s')$ be carried pairs. If
$\mathsf{PairClass}_D^u((l,r),(m,s))$ and
$\mathsf{PairClass}_D^u((l',r'),(m',s'))$, then the two products
$(l,r)\star(l',r')$ and $(m,s)\star(m',s')$ are carried pairs and satisfy
$$
\mathsf{PairClass}_D^u((l,r)\star(l',r'),(m,s)\star(m',s')).
$$
Moreover the support of the first product is exactly the support of its two
input pairs:
$$
\mathsf{PairSupp}_D^u((l,r)\star(l',r'))
\Longleftrightarrow
\mathsf{PairSupp}_D^u(l,r)\lor\mathsf{PairSupp}_D^u(l',r').
$$
\end{theorem}
\begin{proof}
Unfold the product. The first product is
$(\mu_D^u(l',l),\mu_D^u(r,r'))$, and the second is
$(\mu_D^u(m',m),\mu_D^u(s,s'))$. Since all four input pairs are carried,
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws}
gives carrier closure for each of the four displayed product components.
Thus both pair products are carried.

Unfold $\mathsf{PairClass}_D^u$. The hypotheses give
$R_D^u(l,m)$, $R_D^u(r,s)$, $R_D^u(l',m')$, and $R_D^u(r',s')$.
Apply
\autoref{thm:field-rat-denominator-continuation-binary-classifier-congruence}
to the left contexts $l'$, $l$, $m'$, and $m$ to obtain
$R_D^u(\mu_D^u(l',l),\mu_D^u(m',m))$.

Apply the same binary classifier congruence theorem to the right contexts $r$,
$r'$, $s$, and $s'$ to obtain
$R_D^u(\mu_D^u(r,r'),\mu_D^u(s,s'))$. These two component classifiers are
exactly the displayed product-pair classifier.

It remains to read support for the first product. Unfold
$\mathsf{PairSupp}_D^u$ and use
\autoref{thm:field-rat-denominator-empty-unit-product-nonempty-iff} on the
left component $\mu_D^u(l',l)$ and on the right component
$\mu_D^u(r,r')$. This rewrites the product support as
$N_D^u(l')\lor N_D^u(l)\lor N_D^u(r)\lor N_D^u(r')$.

Rebracket and commute the disjunctions to
$(N_D^u(l)\lor N_D^u(r))\lor(N_D^u(l')\lor N_D^u(r'))$. By the definition of
$\mathsf{PairSupp}_D^u$, this is precisely
$\mathsf{PairSupp}_D^u(l,r)\lor\mathsf{PairSupp}_D^u(l',r')$.
\end{proof}

\begin{theorem}[RatUp FieldUp denominator context-pair action laws]
\label{thm:ratup-fieldup-denominator-context-pair-action-laws}
For the concrete denominator context-pair structure of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}, the contextual
action $\Gamma$ is a unital action up to the denominator classifier and composes
by the product $\star$.

If $C_D^u(h)$, then the unit pair acts as the classifier identity:
$$
C_D^u(\Gamma_{\emp,\emp,e_D^u,e_D^u}(h)),
$$
$$
R_D^u(\Gamma_{\emp,\emp,e_D^u,e_D^u}(h),h),
$$
and its strict support is the support of $h$ together with the unit-pair
support:
$$
N_D^u(\Gamma_{\emp,\emp,e_D^u,e_D^u}(h))
\Longleftrightarrow
\mathsf{PairSupp}_D^u(e_D^u,e_D^u)\lor N_D^u(h).
$$

If $C_D^u(h)$, $C_D^u(l)$, $C_D^u(r)$, $C_D^u(l')$, and $C_D^u(r')$, and if
$p\hsame\emp$, $q\hsame\emp$, $p'\hsame\emp$, and $q'\hsame\emp$, then nested
actions are classified by the single action of the product pair:
$$
R_D^u(\Gamma_{p',q',l',r'}(\Gamma_{p,q,l,r}(h)),
\Gamma_{p',q',\mu_D^u(l',l),\mu_D^u(r,r')}(h)).
$$
The strict support of the nested action is exactly the support of the acted
history together with the supports of the two acting pairs:
$$
N_D^u(\Gamma_{p',q',l',r'}(\Gamma_{p,q,l,r}(h)))
\Longleftrightarrow
\mathsf{PairSupp}_D^u(l,r)\lor N_D^u(h)\lor\mathsf{PairSupp}_D^u(l',r').
$$
Equivalently, by the denominator row support laws for $\mu_D^u$, this is the
support of $h$ together with the support of the product pair $(l,r)\star(l',r')$.

The product $\star$ has $(e_D^u,e_D^u)$ as a two-sided unit and is associative
up to componentwise $R_D^u$. For every carried pair $(l,r)$, both
$(e_D^u,e_D^u)\star(l,r)$ and $(l,r)\star(e_D^u,e_D^u)$ are componentwise
$R_D^u$-classified with $(l,r)$. For carried pairs $(l,r)$, $(l',r')$, and
$(l'',r'')$, the pairs $((l,r)\star(l',r'))\star(l'',r'')$ and
$(l,r)\star((l',r')\star(l'',r''))$ are componentwise $R_D^u$-classified.
\end{theorem}
\begin{proof}
The carrier and product clauses are componentwise applications of the empty-unit
denominator continuation monoid laws of
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws}. The
unit pair is carried because $C_D^u(e_D^u)$ holds, and product closure follows
from closure of $\mu_D^u$ on each component.

For the unit action, specialize
\autoref{thm:ratup-fieldup-denominator-contextual-action-unit-composition-laws}
to the empty contextual action. Since $e_D^u=\emp$ in the concrete denominator
instance, its carrier, classifier-identity, and support clauses give exactly the
three displayed unit-action conclusions. The pair-support definition rewrites
the two empty context supports as $\mathsf{PairSupp}_D^u(e_D^u,e_D^u)$.

For composition, use the same unit-composition theorem with the carried context
pairs $(l,r)$ and $(l',r')$ and the four empty external endpoint hypotheses. Its
classifier clause is precisely the comparison with the action whose contexts are
$\mu_D^u(l',l)$ and $\mu_D^u(r,r')$, which is the pair product
$(l,r)\star(l',r')$.

The strict-support clause of that theorem gives the five-term readback
$N_D^u(l')\lor N_D^u(l)\lor N_D^u(h)\lor N_D^u(r)\lor N_D^u(r')$. Rebracketing
disjunction and unfolding $\mathsf{PairSupp}_D^u$ gives the displayed
pair-support form. The denominator row support laws for $\mu_D^u$ identify the
left two and right two support terms with the two components of the product pair.

Finally, unitality and associativity of $\star$ are the componentwise unit and
associativity clauses of
\autoref{thm:field-rat-denominator-empty-unit-continuation-monoid-laws}. The
left component uses the left-context order in the definition of $\star$, and the
right component uses the right-context order; both comparisons are exactly
componentwise $R_D^u$.
\end{proof}

\begin{theorem}[RatUp FieldUp denominator context-pair commutative support]
\label{thm:ratup-fieldup-denominator-context-pair-commutative-support}
For the concrete denominator context-pair structure of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}, let $(l,r)$ and
$(l',r')$ be carried pairs, so $C_D^u(l)$, $C_D^u(r)$, $C_D^u(l')$, and
$C_D^u(r')$. Then the two pair products
$(l,r)\star(l',r')$ and $(l',r')\star(l,r)$ are componentwise
$R_D^u$-classified:
$$
R_D^u(\mu_D^u(l',l),\mu_D^u(l,l'))\quad\text{and}\quad
R_D^u(\mu_D^u(r,r'),\mu_D^u(r',r)).
$$
Moreover, the support of the product pair is exactly the support of either
input pair:
$$
\mathsf{PairSupp}_D^u(\mu_D^u(l',l),\mu_D^u(r,r'))
\Longleftrightarrow
\mathsf{PairSupp}_D^u(l,r)\lor\mathsf{PairSupp}_D^u(l',r').
$$
\end{theorem}
\begin{proof}
Unfold the definition of $\star$. The left component of
$(l,r)\star(l',r')$ is $\mu_D^u(l',l)$, while the left component of
$(l',r')\star(l,r)$ is $\mu_D^u(l,l')$. Apply
\autoref{thm:field-rat-denominator-empty-unit-commutative-monoid-laws} to the
carried endpoints $l'$ and $l$ to obtain the displayed left-component
classifier.

The right component is identical in form, with $r$ and $r'$ in place of the two
left contexts. Applying the same commutative monoid law gives
$R_D^u(\mu_D^u(r,r'),\mu_D^u(r',r))$. Together the two component classifiers are
exactly componentwise $R_D^u$-classification of the two pair products.

It remains to read support. Unfold
$\mathsf{PairSupp}_D^u(\mu_D^u(l',l),\mu_D^u(r,r'))$ as the disjunction of the
left and right product supports. For each product component, use
\autoref{thm:field-rat-denominator-empty-unit-product-nonempty-iff} with the
corresponding carried factors, together with the strict-support interpretation
of $N_D^u$, to replace product support by the disjunction of the two input
supports.

The result is
$N_D^u(l')\lor N_D^u(l)\lor N_D^u(r)\lor N_D^u(r')$. Rebracket and commute the
outer disjunctions to obtain
$(N_D^u(l)\lor N_D^u(r))\lor(N_D^u(l')\lor N_D^u(r'))$, which is exactly
$\mathsf{PairSupp}_D^u(l,r)\lor\mathsf{PairSupp}_D^u(l',r')$ by the
pair-support definition.
\end{proof}

Per-object template: traditional target context-pair commutative action support;
BEDC source $\mathsf{BHist}$,
$\mathsf{RatDenomUnitCarrier}$, $\mathsf{RatDenomUnitClassifier}$,
$\mathsf{append}$, $\mathsf{RatHistoryCarrier}$, and action $\Gamma$; carrier
the carried context pairs; classifier componentwise $R_D^u$; stability
obligations component commutativity and support exactness; ledger left and right
denominator contexts; core theorem
\autoref{thm:ratup-fieldup-denominator-context-pair-commutative-support};
exactness predicate the displayed support iff; standard bridge denominator
context-pair monoid support; status $\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp denominator context-pair threefold action coherence]
\label{thm:ratup-fieldup-denominator-context-pair-threefold-action-coherence}
For the concrete denominator context-pair structure of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}, assume that
$(l_0,r_0)$, $(l_1,r_1)$, and $(l_2,r_2)$ are carried pairs and that
$C_D^u(h)$. Let
$$
H_3:=
\Gamma_{\emp,\emp,l_2,r_2}
(\Gamma_{\emp,\emp,l_1,r_1}(\Gamma_{\emp,\emp,l_0,r_0}(h))).
$$
Then the threefold context-pair action is carried:
$$
C_D^u(H_3).
$$
It is classified by the single action of the left-associated double product
$( (l_0,r_0)\star(l_1,r_1))\star(l_2,r_2)$:
$$
R_D^u\bigl(H_3,
\Gamma_{\emp,\emp,\mu_D^u(l_2,\mu_D^u(l_1,l_0)),
\mu_D^u(\mu_D^u(r_0,r_1),r_2)}(h)\bigr).
$$
It is also classified by the single action of the alternate bracketing
$(l_0,r_0)\star((l_1,r_1)\star(l_2,r_2))$:
$$
R_D^u\bigl(H_3,
\Gamma_{\emp,\emp,\mu_D^u(\mu_D^u(l_2,l_1),l_0),
\mu_D^u(r_0,\mu_D^u(r_1,r_2))}(h)\bigr).
$$
The strict-support readback is the seven-term denominator row
$$
N_D^u(H_3)
\Longleftrightarrow
N_D^u(l_2)\lor N_D^u(l_1)\lor N_D^u(l_0)\lor N_D^u(h)
\lor N_D^u(r_0)\lor N_D^u(r_1)\lor N_D^u(r_2).
$$
Equivalently, after reassociating the finite disjunction, the same readback is
$$
N_D^u(H_3)
\Longleftrightarrow
\mathsf{PairSupp}_D^u(l_2,r_2)\lor
\mathsf{PairSupp}_D^u(l_1,r_1)\lor
\mathsf{PairSupp}_D^u(l_0,r_0)\lor N_D^u(h).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:field-rat-denominator-contextual-action-threefold-normalization}
with the three carried denominator context pairs and the carried denominator
history $h$. This gives the carrier row for $H_3$, the classifier comparison with
the single action whose contexts are
$\mu_D^u(l_2,\mu_D^u(l_1,l_0))$ and
$\mu_D^u(\mu_D^u(r_0,r_1),r_2)$, and the seven-term strict-support readback.

Unfold
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}. The product
$(l_0,r_0)\star(l_1,r_1)$ is
$(\mu_D^u(l_1,l_0),\mu_D^u(r_0,r_1))$, so the next product with
$(l_2,r_2)$ has exactly the two normalized contexts displayed in the first
classifier row.

For the alternate bracketing, use the associativity clause of
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}. It identifies
the two double products componentwise up to $R_D^u$, with left component
$\mu_D^u(l_2,\mu_D^u(l_1,l_0))$ classified with
$\mu_D^u(\mu_D^u(l_2,l_1),l_0)$ and right component
$\mu_D^u(\mu_D^u(r_0,r_1),r_2)$ classified with
$\mu_D^u(r_0,\mu_D^u(r_1,r_2))$.

Transport the first single-action classifier across this componentwise
associativity comparison using the concrete denominator classifier laws recorded
in the same action-law theorem. Finally unfold $\mathsf{PairSupp}_D^u$ and
reassociate the finite disjunction to obtain the pair-support formulation of the
same strict-support readback.
\end{proof}

Per-object template: traditional target threefold context-pair action coherence
for $\FieldUp(\mathsf{RatUp})$; BEDC source $\mathsf{BHist}$,
$\mathsf{RatDenomUnitCarrier}$, $\mathsf{RatDenomUnitClassifier}$,
$\mathsf{RatHistoryCarrier}$, $\mathsf{append}$, $\Cont$, $\emp$, and $\Gamma$;
carrier carried context pairs; classifier componentwise $R_D^u$; stability
obligations closure, classifier normalization, support readback, and pair-product
associativity; ledger three carried pairs plus acted denominator history; core
theorem
\autoref{thm:ratup-fieldup-denominator-context-pair-threefold-action-coherence};
exactness predicate threefold normalized action classifier and support readback;
standard bridge FieldUp denominator context-pair action; status
$\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp denominator context-pair action commutation]
\label{thm:ratup-fieldup-denominator-context-pair-action-commutation}
For the concrete denominator context-pair structure of \autoref{def:ratup-fieldup-denominator-context-pair-monoid}, let $h$ be a carried
history and let $(l,r)$ and $(l',r')$ be carried denominator context pairs. Then
the two empty-external nested contextual actions commute up to the denominator classifier:
$$
R_D^u(\Gamma_{\emp,\emp,l',r'}(\Gamma_{\emp,\emp,l,r}(h)),\Gamma_{\emp,\emp,l,r}(\Gamma_{\emp,\emp,l',r'}(h))).
$$
Moreover, both nested actions have the same exact strict-support readback:
$$
N_D^u(\Gamma_{\emp,\emp,l',r'}(\Gamma_{\emp,\emp,l,r}(h)))\Longleftrightarrow\mathsf{PairSupp}_D^u(l,r)\lor N_D^u(h)\lor\mathsf{PairSupp}_D^u(l',r'),
$$
and
$$
N_D^u(\Gamma_{\emp,\emp,l,r}(\Gamma_{\emp,\emp,l',r'}(h)))\Longleftrightarrow\mathsf{PairSupp}_D^u(l,r)\lor N_D^u(h)\lor\mathsf{PairSupp}_D^u(l',r').
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:ratup-fieldup-denominator-context-pair-action-laws} to the
ordered pair $(l,r)$ followed by $(l',r')$. This identifies the first nested
action with the single action by $(l,r)\star(l',r')$, whose components are
$\mu_D^u(l',l)$ and $\mu_D^u(r,r')$.

Apply the same theorem in the opposite order. The second nested action is
identified with the single action by $(l',r')\star(l,r)$, whose components are
$\mu_D^u(l,l')$ and $\mu_D^u(r',r)$.

The componentwise comparison between those two product pairs is exactly
\autoref{thm:ratup-fieldup-denominator-context-pair-commutative-support}. Since
$R_D^u=\mathsf{RatDenomUnitClassifier}$, $\mu_D^u=\mathsf{append}$, and
$\Gamma$ is the concrete append-based contextual action, append congruence
transports the single-action endpoints across the component classifiers.
Transitivity of $R_D^u$ gives the displayed classifier comparison.

The support readback for the first nested action is the product strict-support
clause of \autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}. The
same clause in the opposite order gives
$\mathsf{PairSupp}_D^u(l',r')\lor N_D^u(h)\lor\mathsf{PairSupp}_D^u(l,r)$ for
the second nested action.

Finally, rebracket and commute the outer disjunctions. The support equivalence
of \autoref{thm:ratup-fieldup-denominator-context-pair-commutative-support}
identifies the product supports in either order, so both nested actions have
exactly the common strict support displayed above.
\end{proof}

\begin{theorem}[RatUp FieldUp denominator context-pair action certificate]
\label{thm:ratup-fieldup-denominator-context-pair-action-certificate}
For the concrete tuple
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ with
$$
C_D^u=\mathsf{RatDenomUnitCarrier},\qquad
R_D^u=\mathsf{RatDenomUnitClassifier},
$$
$$
\mu_D^u=\mathsf{append},\qquad e_D^u=\emp,\qquad
N_D^u=\mathsf{RatHistoryCarrier},
$$
and for the context-pair structure of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid} and the contextual
action $\Gamma$ of
\autoref{def:ratup-fieldup-denominator-contextual-action-instance}, the
denominator context-pair action certificate for
$\FieldUp(\mathsf{RatUp})$ consists of the following concrete rows:
\begin{enumerate}
\item $\mathsf{PairClass}_D^u$ is reflexive, symmetric, and transitive on
carried pairs, and it transports the carried pair predicate
$C_D^u(l)\land C_D^u(r)$.
\item The product $\star$ of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid} is closed on
carried pairs and congruent for $\mathsf{PairClass}_D^u$, with exact
$\mathsf{PairSupp}_D^u$ readback as in
\autoref{thm:ratup-fieldup-denominator-context-pair-operation-stability}.
\item The unit-pair action, product-pair composition law, and product
strict-support readback are exactly the laws of
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}.
\item The three-layer empty-endpoint contextual action has the normalized
single-action form
$$
\Gamma_{\emp,\emp,l_2,r_2}
(\Gamma_{\emp,\emp,l_1,r_1}(\Gamma_{\emp,\emp,l_0,r_0}(h)))
\sim_{R_D^u}
\Gamma_{\emp,\emp,\mu_D^u(l_2,\mu_D^u(l_1,l_0)),
\mu_D^u(\mu_D^u(r_0,r_1),r_2)}(h)
$$
and the seven-term strict-support readback of
\autoref{thm:field-rat-denominator-contextual-action-threefold-normalization}.
\item These rows, together with
\autoref{thm:ratup-fieldup-denominator-contextual-row-package-with-unit-composition},
form the concrete denominator context-pair action certificate for
$\FieldUp(\mathsf{RatUp})$.
\end{enumerate}
Equivalently, the certificate records exactly the pair-action interface of
\autoref{thm:ratup-fieldup-denominator-context-pair-operation-stability},
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}, and the
contextual row assembly of
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package} over the
same BHist carrier-classifier-operation tuple
$(\mathsf{RatDenomUnitCarrier},\mathsf{RatDenomUnitClassifier},
\mathsf{append},\emp,\mathsf{RatHistoryCarrier})$.
\end{theorem}
\begin{proof}
First build the classifier rows. The concrete denominator classifier
$R_D^u$ is an equivalence on carried histories and transports the carried
history predicate by the denominator row package. Applying those rows to the
left and right coordinates gives reflexivity, symmetry, transitivity, and
carrier transport for $\mathsf{PairClass}_D^u$ on carried pairs.

Next establish product stability. Closure of $\star$ is componentwise closure
of $\mu_D^u$. Congruence follows by applying the denominator append congruence
rows to the left components and to the right components, preserving the product
orders $\mu_D^u(l',l)$ and $\mu_D^u(r,r')$ from
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}. The exact
$\mathsf{PairSupp}_D^u$ readback is
\autoref{thm:ratup-fieldup-denominator-context-pair-operation-stability}.

Now apply
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}. It supplies
the unit-pair action, the product-pair composition law, the product
strict-support readback, and the two-sided unit and associativity clauses for
the same product $\star$ and componentwise classifier.

Apply
\autoref{thm:ratup-fieldup-denominator-contextual-action-classifier-congruence}
to the same carried left context, acted history, and carried right context
whenever the pair classifier transports an acting pair. This gives contextual
action stability under $\mathsf{PairClass}_D^u$ and keeps the external endpoint
contexts empty.

For the iterated action row, apply
\autoref{thm:field-rat-denominator-contextual-action-threefold-normalization}.
It gives the carried three-layer action, its normalized single-action classifier
comparison, and the seven-term strict-support readback for the same
$\Gamma$, $\mu_D^u$, $e_D^u$, and $N_D^u$.

Finally apply
\autoref{thm:ratup-fieldup-denominator-contextual-row-package-with-unit-composition}.
Its row closure, exactness, unit-composition, and support clauses occupy the
one-layer and two-layer rows of the same concrete denominator interface. The
pair classifier rows, product congruence, pair-action laws, and threefold
normalization therefore assemble as the stated FieldUp denominator
context-pair action certificate over $\mathsf{BHist}$.
\end{proof}

\begin{theorem}[RatUp FieldUp denominator action envelope closure]
\label{thm:ratup-fieldup-denominator-action-envelope-closure}
For $\FieldUp(\mathsf{RatUp})$, the concrete RatUp denominator data
$$
C_D^*=\mathsf{RatDenomUnitCarrier},\qquad
R_D^*=\mathsf{RatDenomUnitClassifier},
$$
$$
\mu_D^*=\mathsf{append},\qquad e_D^*=\emp,\qquad
N_D^*=\mathsf{RatHistoryCarrier},
$$
together with the empty-unit tuple
$$
(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)=
(\mathsf{RatDenomUnitCarrier},\mathsf{RatDenomUnitClassifier},
\mathsf{append},\emp,\mathsf{RatHistoryCarrier})
$$
supply one denominator action envelope. The envelope consists of
multiplicative obligation coverage, the contextual multiplicative boundary,
the unit-composition row package, and the context-pair action certificate over
the same BHist histories, append operation, empty endpoint, and denominator
carrier-classifier pair.
\end{theorem}
\begin{proof}
First use
\autoref{thm:field-rat-denominator-fieldup-multiplicative-obligation-coverage}
and
\autoref{thm:field-rat-denominator-fieldup-contextual-multiplicative-boundary}.
They supply the multiplicative obligation coverage and the contextual
multiplicative boundary for the concrete RatUp denominator data
$(C_D^*,R_D^*,\mu_D^*,e_D^*,N_D^*)$.

Next use
\autoref{thm:ratup-fieldup-denominator-contextual-row-package-with-unit-composition}.
It records the same denominator carrier, classifier, append operation, empty
unit, strict-support predicate, and contextual action $\Gamma$, and it adds the
empty-action unit row, unchanged support row, context-multiplication composition
row, and five-term support row.

Finally use
\autoref{thm:ratup-fieldup-denominator-context-pair-action-certificate}. It
packages the carried context-pair predicate, pair unit, product $\star$,
componentwise classifier, support readbacks, and star unit and associativity
laws for the identical empty-unit tuple
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$.

The four cited certificates are all stated over concrete $\mathsf{BHist}$
carriers and the concrete operation $\mathsf{append}$. Hence the envelope is a
single assembly of denominator action data for $\FieldUp(\mathsf{RatUp})$,
not a carrier or classifier transport from another instance.
\end{proof}

Per-object template: traditional target FieldUp(RatUp) denominator action
envelope; BEDC source $\mathsf{BHist}$, $\mathsf{RatHistoryCarrier}$,
$\mathsf{RatDenomUnitCarrier}$, $\mathsf{RatDenomUnitClassifier}$,
$\mathsf{append}$, $\emp$, and $\Gamma$; carrier
$\mathsf{RatDenomUnitCarrier}$; classifier
$\mathsf{RatDenomUnitClassifier}$; stability obligations multiplicative
coverage, contextual multiplicative boundary, unit-composition rows, and
context-pair action laws; ledger denominator histories, empty endpoint,
carried context pairs, and strict-support rows; core theorem
\autoref{thm:ratup-fieldup-denominator-action-envelope-closure}; exactness
predicate denominator classifier readback and strict-support readback; standard
bridge $\FieldUp(\mathsf{RatUp})$ denominator action envelope; status
$\mathsf{ProofTarget}$.

Per-object template: traditional target denominator context-pair action
certificate for $\FieldUp(\mathsf{RatUp})$; BEDC source $\mathsf{BHist}$,
$\mathsf{RatDenomUnitCarrier}$, $\mathsf{RatDenomUnitClassifier}$,
$\mathsf{RatHistoryCarrier}$, $\mathsf{append}$, $\Cont$, $\emp$, and
$\Gamma$; carrier carried denominator context pairs; classifier
$\mathsf{PairClass}_D^u$; stability obligations pair classifier equivalence,
carrier transport, $\star$ congruence, unit action, product composition, and
threefold normalization; ledger left and right carried denominator contexts
plus empty external endpoints; core theorem
\autoref{thm:ratup-fieldup-denominator-context-pair-action-certificate};
exactness predicate support readback and row closure; standard bridge FieldUp
denominator action; status $\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp denominator context-pair normalized row exhaustion]
\label{thm:ratup-fieldup-denominator-context-pair-normalized-row-exhaustion}
For the concrete denominator context-pair monoid of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid} and the concrete
contextual action $\Gamma$ of
\autoref{def:ratup-fieldup-denominator-contextual-action-instance}, every
denominator action row used by $\FieldUp(\mathsf{RatUp})$ is supplied by the
concrete BHist data
$$
\mathsf{RatDenomUnitCarrier},\quad
\mathsf{RatDenomUnitClassifier},\quad
\mathsf{append},\quad
\emp,\quad
\mathsf{RatHistoryCarrier},
$$
the empty external contexts, and the pair product $\star$. More precisely, the
action interface is exhausted by the row closure package of
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package}, the
unit-composition laws of
\autoref{thm:ratup-fieldup-denominator-contextual-row-package-with-unit-composition},
the normalized contextual action package of
\autoref{thm:field-rat-denominator-normalized-contextual-action-package}, the
context-pair action laws of
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}, and the
context-pair action certificate of
\autoref{thm:ratup-fieldup-denominator-context-pair-action-certificate}.
\end{theorem}
\begin{proof}
First invoke
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package} and
\autoref{thm:ratup-fieldup-denominator-contextual-row-package-with-unit-composition}.
These two rows provide the carried contextual row package, the unit action, and
the composition clauses over
$(\mathsf{RatDenomUnitCarrier},\mathsf{RatDenomUnitClassifier},
\mathsf{append},\emp,\mathsf{RatHistoryCarrier})$.

Next invoke
\autoref{thm:field-rat-denominator-normalized-contextual-action-package}. This
identifies the normalized contextual rows with the same concrete denominator
tuple and the same empty external endpoints, so no abstract carrier or
classifier transport is introduced.

Then invoke
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}. Its clauses
give the unit pair, composition by $\star$, support readback, and associativity
for the concrete pair product of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}.

Finally invoke
\autoref{thm:ratup-fieldup-denominator-context-pair-action-certificate}. It
identifies the preceding row, unit-composition, normalized, and pair-action
clauses as the concrete denominator action certificate consumed by
$\FieldUp(\mathsf{RatUp})$. Hence the displayed BHist objects, empty external
contexts, and $\star$ account for the whole denominator action interface.
\end{proof}

Per-object template: traditional target FieldUp(RatUp) denominator
context-pair normalized row exhaustion; BEDC source $\mathsf{BHist}$,
$\mathsf{RatHistoryCarrier}$, $\mathsf{RatDenomUnitCarrier}$,
$\mathsf{RatDenomUnitClassifier}$, $\mathsf{append}$, $\emp$, and $\Gamma$;
carrier denominator context pairs of
\autoref{def:ratup-fieldup-denominator-context-pair-monoid}; classifier
componentwise $R_D^u$; stability obligations row closure, unit composition,
normalized exactness, pair-action unit, pair-action composition, support
readback, and $\star$ associativity; ledger normalized contextual rows and
pair-action rows with empty external endpoints; core theorem
\autoref{thm:ratup-fieldup-denominator-context-pair-normalized-row-exhaustion};
exactness predicate denominator action row coverage by the concrete BHist data;
standard bridge FieldUp denominator action interface; status
$\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp assembled certificate boundary]
\label{thm:ratup-fieldup-assembled-certificate-boundary}
The concrete $\FieldUp(\mathsf{RatUp})$ source over $\mathsf{BHist}$ satisfies
\autoref{def:field-certificate-obligations}. Its carrier is
$\mathsf{RatHistoryCarrier}$, its classifier is
$\mathsf{RatHistoryClassifier}$, and its rows are the following jointly
registered obligations:
\begin{enumerate}
  \item the commutative-ring rows inherited from $\CommRingUp$ on the same
  history carrier and classifier;
  \item the inverse and nonzero rows supplied by
  \autoref{thm:field-inverse-classifier-exact-from-apartness},
  \autoref{thm:field-inverse-nonzero-domain-automorphism},
  \autoref{thm:field-inverse-product-classifier-exact-from-apartness}, and
  \autoref{thm:field-product-factor-map-classifier-exact-from-apartness};
  \item the denominator rows supplied by
  \autoref{thm:field-rat-denominator-fieldup-multiplicative-certificate-record}
  and \autoref{thm:ratup-fieldup-denominator-certificate-assembly};
  \item the contextual denominator rows supplied by
  \autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package} and
  \autoref{thm:ratup-fieldup-denominator-context-pair-action-laws};
  \item the affine automorphism closure supplied by
  \autoref{thm:field-affine-automorphism-group-certificate}.
\end{enumerate}
Consequently the current Phase Algebra Hierarchy exit object is the concrete
BHist-anchored $\FieldUp(\mathsf{RatUp})$ certificate assembled from these rows.
\end{theorem}
\begin{proof}
The carrier and classifier fields are fixed first. They are the concrete RatUp
history predicates $\mathsf{RatHistoryCarrier}$ and
$\mathsf{RatHistoryClassifier}$, so every row below is read over the same
$\mathsf{BHist}$ source rather than over detached parameters.

The algebraic spine is the inherited $\CommRingUp$ structure together with the
inverse block. Apply
\autoref{thm:field-inverse-classifier-exact-from-apartness},
\autoref{thm:field-inverse-nonzero-domain-automorphism},
\autoref{thm:field-inverse-product-classifier-exact-from-apartness}, and
\autoref{thm:field-product-factor-map-classifier-exact-from-apartness}. These
rows give classifier exactness for inverse formation, nonzero transport through
the inverse domain, product inverse exactness, and certified multiplication by
nonzero factors.

The denominator subcertificate is then read from
\autoref{thm:field-rat-denominator-fieldup-multiplicative-certificate-record}
and \autoref{thm:ratup-fieldup-denominator-certificate-assembly}. The first
theorem records the multiplicative denominator rows, and the second identifies
them as the denominator component consumed by the FieldUp certificate
obligations.

The contextual extension uses
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package} and
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}. These rows add
one-step and iterated contextual actions, pair composition, unit action,
classifier transport, strict-support readback, and componentwise product laws
for the same denominator carrier.

Finally, apply \autoref{thm:field-affine-automorphism-group-certificate}. Its
identity, composition, inverse, associativity, classifier-congruence, and
witness-choice descent rows close the affine automorphism part of the FieldUp
interface. Combining these disjoint rows gives exactly the carrier,
classifier, stability, exactness, and ledger obligations listed in
\autoref{def:field-certificate-obligations}.
\end{proof}

Per-object template: traditional target concrete constructive field certificate
for $\FieldUp(\mathsf{RatUp})$; BEDC source $\mathsf{BHist}$ with
$\mathsf{RatHistoryCarrier}$ and $\mathsf{RatHistoryClassifier}$; carrier
$\mathsf{RatHistoryCarrier}$; classifier $\mathsf{RatHistoryClassifier}$;
stability obligations commutative-ring laws, proof-indexed inverse closure,
nonzero transport, denominator multiplication, contextual action, context-pair
composition, and affine automorphism closure; ledger ring expression data,
nonzero witnesses, denominator histories, contextual pairs, and affine
coefficient witnesses; core theorem list
\autoref{thm:ratup-fieldup-assembled-certificate-boundary},
\autoref{thm:ratup-fieldup-denominator-certificate-assembly},
\autoref{thm:ratup-fieldup-denominator-contextual-row-closure-package},
\autoref{thm:ratup-fieldup-denominator-context-pair-action-laws}, and
\autoref{thm:field-affine-automorphism-group-certificate}; exactness predicate
classifier exactness for inverse, product, denominator, contextual action, and
affine automorphism rows; standard bridge ordinary constructive field over
rational histories with certified affine automorphisms; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Rat denominator context-pair product strict support]
\label{thm:field-rat-denominator-context-pair-product-strict-support}
For carried denominator context pairs $(l,r)$ and $(l',r')$, strict support of
their product is exactly the disjunction of the four component supports:
$$
N_D^u(\mu_D^u(\mu_D^u(l',l),\mu_D^u(r,r')))
\Longleftrightarrow
N_D^u(l')\lor N_D^u(l)\lor N_D^u(r)\lor N_D^u(r').
$$
\end{theorem}
\begin{proof}
Apply bilateral strict-support exactness to the left product
$\mu_D^u(l',l)$ and the carried right product $\mu_D^u(r,r')$. The remaining
right-product support term is then opened by denominator product support
exactness for $r$ and $r'$. Reassociating the resulting disjunction yields the
four displayed component supports, and the reverse implication closes the same
two product-support equivalences in the opposite order.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.RatDenomContextPair\_product\_strict\_support\_iff}

\begin{theorem}[Rat denominator context-pair product strict support transport]
\label{thm:field-rat-denominator-context-pair-product-strict-support-transport}
If $(l,r)$ and $(s,t)$ are componentwise $R_D^u$-classified, and
$(l',r')$ and $(s',t')$ are componentwise $R_D^u$-classified, then strict
support is invariant across the two product endpoints:
$$
N_D^u(\mu_D^u(\mu_D^u(l',l),\mu_D^u(r,r')))
\Longleftrightarrow
N_D^u(\mu_D^u(\mu_D^u(s',s),\mu_D^u(t,t'))).
$$
\end{theorem}
\begin{proof}
The component classifiers expose $\HSame(l,s)$, $\HSame(r,t)$,
$\HSame(l',s')$, and $\HSame(r',t')$. Continuation congruence composes these
through the two product layers, giving an $\HSame$ witness between the two
displayed product endpoints. Rat denominator support transports across that
endpoint witness in both directions.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.RatDenomContextPair\_product\_strict\_support\_transport}
