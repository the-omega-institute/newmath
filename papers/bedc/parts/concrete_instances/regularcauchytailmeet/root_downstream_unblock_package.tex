\begin{theorem}[RegularCauchyTailMeet root downstream window exhaustion]
\label{thm:regular-cauchy-tail-meet-root-downstream-window-exhaustion}
Let $T=(R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,N)$ be an accepted
$\RegularCauchyTailMeetUp$ packet. Every downstream $\RealUp$ or
$\CompletionUp$ root consumer that reads the TailMeet threshold must first
exhaust the paired window-source route through the displayed $\BHist$ rows
$R_0,R_1,W_0,W_1$, the componentwise $\hsame$ row $H$, the displayed $\Cont$
route $C$, the finite ledger $L$, the package row $\Pkg$, and the local
$\NameCert$ row $N$. No downstream read can reach $\tau$, $Q$, or a Real seal
handoff before those four source-window rows have been exposed.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTailMeetUp.RegularCauchyTailMeetPacket\_root\_downstream\_window\_exhaustion}
\begin{proof}
Start with TailMeet window-source coverage. It forces any threshold-facing read
to expose the two regular-Cauchy source rows $R_0,R_1$ and the two stream-window
rows $W_0,W_1$ before the shared dyadic threshold is available.

Next attach the structural rows. Shared-threshold transport carries the exposed
window-source route through the componentwise $\hsame$ row $H$ and the displayed
$\Cont$ route $C$, while consumer exactness records the same route in $L$,
$\Pkg$, and $N$.

Finally read the Real seal handoff. The handoff can consume only the already
displayed finite TailMeet route, so a root downstream consumer cannot bypass the
window-source rows to reach $\tau$, $Q$, or any completion-facing seal row.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet root downstream threshold stability]
\label{thm:regular-cauchy-tail-meet-root-downstream-threshold-stability}
Let $T=(R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,N)$ be an accepted
$\RegularCauchyTailMeetUp$ packet. If a downstream root consumer transports the
paired source-window-modulus route through the displayed $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ rows, then the shared threshold row $\tau$ and
comparison row $Q$ remain the TailMeet rows read by that consumer. The consumer
may request a stronger displayed dyadic threshold through $M_0$ and $M_1$, but
the stabilized route still factors through $R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,N$.
\end{theorem}
\begin{proof}
Use the shared-threshold transport theorem to move the paired source rows,
window rows, modulus rows, threshold row, comparison row, finite ledger, package
row, and local name together. The transported route therefore keeps $\tau$ and
$Q$ attached to the same TailMeet packet.

Now apply shared-threshold cofinality. A stronger displayed dyadic threshold can
serve the downstream request only through the same source modulus rows
$M_0,M_1$ and the same paired schedules $W_0,W_1$.

Consumer exactness and the Real seal handoff close the boundary. They record the
transported threshold route in $H,C,L,N$ and prevent the downstream consumer from
replacing it by a host equality, quotient stream, selected limit, independent
tail oracle, or ambient completeness row.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyTailMeetUp.RegularCauchyTailMeetPacket\_root\_downstream\_threshold\_stability}

\begin{theorem}[RegularCauchyTailMeet root downstream nonescape package]
\label{thm:regular-cauchy-tail-meet-root-downstream-nonescape-package}
For an accepted $\RegularCauchyTailMeetUp$ packet
$T=(R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,N)$, downstream root consumers receive
exactly the finite TailMeet route certified by
\autoref{thm:regular-cauchy-tail-meet-root-downstream-window-exhaustion} and
\autoref{thm:regular-cauchy-tail-meet-root-downstream-threshold-stability}. The
package exposes the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ rows and exports no quotient stream, selected limit, host real
equality, trichotomy row, independent tail oracle, or ambient completeness
witness.
\end{theorem}
\begin{proof}
First combine downstream window exhaustion with downstream threshold stability.
The first theorem forces each root consumer through $R_0,R_1,W_0,W_1$ before the
threshold is visible, and the second keeps the transported threshold and
comparison rows on $\tau$ and $Q$ through $M_0,M_1,H,C,L,N$.

Next compose with consumer exactness. That theorem identifies the listed rows as
the complete consumer-facing surface of the accepted TailMeet packet, including
the package provenance and local naming evidence.

Finally apply the Real seal handoff rows. Since every Real or Completion
consumer reaches the seal through the same TailMeet route, the downstream
package has no coordinate from which to export quotient streams, selected
limits, host equality, trichotomy, hidden tail data, or ambient completeness.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet root downstream seal route]
\label{thm:regular-cauchy-tail-meet-root-downstream-seal-route}
Every downstream $\RealUp$ or $\CompletionUp$ seal consumer of an accepted
$\RegularCauchyTailMeetUp$ packet
$T=(R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,N)$ first factors through the paired
source windows $R_0,R_1,W_0,W_1$, then through the modulus rows $M_0,M_1$, then
through the shared threshold and comparison rows $\tau,Q$, and finally through
$H,C,L,\Pkg,N$. The route exports no quotient stream, selected limit, host
equality, trichotomy row, independent tail oracle, or ambient completeness
witness.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTailMeetUp.RegularCauchyTailMeetPacket\_root\_downstream\_seal\_route}
\begin{proof}
Start with downstream window exhaustion. It forces every seal-facing consumer to
read the paired source rows and window rows $R_0,R_1,W_0,W_1$ before the
threshold or seal handoff can be visible.

Next apply downstream threshold stability. Any stronger displayed dyadic request
must pass through $M_0,M_1$ while keeping the shared threshold row $\tau$ and the
comparison row $Q$ attached to the same TailMeet packet.

Finally use the downstream nonescape package. It repacks the window,
modulus, threshold, comparison, transport, continuation, ledger, package, and
local naming rows as the complete seal route and excludes quotient streams,
selected limits, host equality, trichotomy, independent tail oracles, and
ambient completeness.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet downstream unblock package]
\label{thm:regular-cauchy-tail-meet-downstream-unblock-package}
For an accepted $\RegularCauchyTailMeetUp$ packet, every downstream $\RealUp$
or $\CompletionUp$ root consumer receives exactly the combined route certified
by window exhaustion, threshold stability, the nonescape package, and the seal
route. The public package is the finite tuple
$R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,\Pkg,N$, and it exports no quotient
stream, selected limit, host equality, trichotomy row, independent tail oracle,
or ambient completeness witness.
\end{theorem}
\begin{proof}
Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-window-exhaustion}. Every
root consumer first exposes the paired source-window rows
$R_0,R_1,W_0,W_1$ before a threshold-facing or seal-facing read is available.

Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-threshold-stability}. The
same route then carries the modulus rows $M_0,M_1$, preserves the shared
threshold row $\tau$, and keeps the comparison row $Q$ attached to the accepted
TailMeet packet.

Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-nonescape-package}. This
identifies the consumer-facing surface with the displayed $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ rows and excludes every unlisted root resource.

Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-seal-route}. The seal
consumer therefore factors through the same finite route and cannot bypass the
ledger or package evidence.

Ledger exhaustion leaves only
$R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,\Pkg,N$ in the exported package, while
non-escape removes quotient streams, selected limits, host equality, trichotomy,
independent tail oracles, and ambient completeness witnesses from the downstream
interface.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet budget-selector handoff]
\label{thm:regular-cauchy-tail-meet-budget-selector-handoff}
For an accepted $\RegularCauchyTailMeetUp$ packet, every
$\FiniteObservationBudgetSelectorUp$ or $\RegularCauchyTailSelectorUp$ consumer
must read the paired source windows $R_0,R_1,W_0,W_1$, modulus rows $M_0,M_1$,
shared threshold $\tau$, comparison row $Q$, $\hsame$ row $H$, $\Cont$ row $C$,
ledger $L$, package row $\Pkg$, and local $\NameCert$ row $N$ before selecting a
downstream budget. The handoff exports no selected limit, quotient stream, host
equality, trichotomy row, independent tail oracle, or ambient completeness
witness.
\end{theorem}
\begin{proof}
Start with the downstream unblock package. It identifies the complete public
route of an accepted TailMeet packet as the finite tuple
$R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,\Pkg,N$ and excludes every unlisted root
resource.

Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-window-exhaustion} and
\autoref{thm:regular-cauchy-tail-meet-root-downstream-threshold-stability}. A
budget selector must first read the paired source windows and modulus rows, and
the shared threshold and comparison row remain attached to the same accepted
packet.

Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-nonescape-package} and
\autoref{thm:regular-cauchy-tail-meet-root-downstream-seal-route}. These route
the selector through the displayed $\hsame$, $\Cont$, ledger, package, and
local naming rows.

Thus a $\FiniteObservationBudgetSelectorUp$ or
$\RegularCauchyTailSelectorUp$ consumer can select a downstream budget only
after consuming the already displayed finite route. Since that route contains no
selected limit, quotient stream, host equality, trichotomy row, independent tail
oracle, or ambient completeness witness, none is exported by the handoff.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet root consumer budget uniqueness]
\label{thm:regular-cauchy-tail-meet-root-consumer-budget-uniqueness}
For an accepted $\RegularCauchyTailMeetUp$ packet, any downstream $\RealUp$ or
$\CompletionUp$ root consumer has exactly the displayed budget route
$R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,\Pkg,N$. The route is the one obtained by
the source-window exhaustion, threshold stability, nonescape package, seal
route, and budget-selector handoff. No second downstream budget, quotient
stream, selected limit, host equality, independent tail oracle, or ambient
completeness witness is available to the consumer.
\end{theorem}
\begin{proof}
Start with the downstream unblock package. It displays the public root route as
$R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,\Pkg,N$ and records that the accepted
packet exports only the named rows.

Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-window-exhaustion} and
\autoref{thm:regular-cauchy-tail-meet-root-downstream-threshold-stability}. The
source windows, modulus rows, threshold, and comparison row are forced by the
accepted TailMeet packet before a downstream root request can be served.

Apply
\autoref{thm:regular-cauchy-tail-meet-root-downstream-nonescape-package} and
\autoref{thm:regular-cauchy-tail-meet-root-downstream-seal-route}. These attach
the same request to the displayed $\hsame$, $\Cont$, ledger, package, and local
name rows and prevent a $\RealUp$ or $\CompletionUp$ consumer from leaving that
route.

Finally use
\autoref{thm:regular-cauchy-tail-meet-budget-selector-handoff}. A downstream
budget selector can act only after reading the already displayed route, so any
root consumer budget is the route just constructed. The nonescape exclusions
remove every alternative carrier for a second budget.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet seal consumer tail-fiber factorization]
\label{thm:regular-cauchy-tail-meet-seal-consumer-tail-fiber-factorization}
For an accepted $\RegularCauchyTailMeetUp$ packet, every seal-facing consumer
that later reads the $\RegularCauchyTailFiberUp$ row of
\autoref{ch:concrete-instances-regular-cauchy-tail-fiber-namecert} must first
factor through the TailMeet shared threshold tuple
$R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,\Pkg,N$. Only after that finite route is
read may the tail-fiber row be made available. The factorization exports no
quotient stream, selected limit, host equality, independent tail oracle, or
ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with the root consumer budget uniqueness theorem. A seal-facing downstream
consumer has a unique finite TailMeet route, and that route is the displayed
tuple $R_0,R_1,W_0,W_1,M_0,M_1,\tau,Q,H,C,L,\Pkg,N$.

Use the seal route and the budget-selector handoff. They force the consumer to
read the same threshold tuple before any downstream budget or seal-facing read
is available.

Now pass to the tail-fiber chapter named in
\autoref{ch:concrete-instances-regular-cauchy-tail-fiber-namecert}. A
$\RegularCauchyTailFiberUp$ row can be consumed only as a later row attached to
the already routed TailMeet threshold service, because the present packet
exports no independent tail oracle and no ambient completeness witness.

The tail-fiber consumer therefore factors through the TailMeet threshold tuple
before the tail-fiber row is visible. Since the intermediate route contains no
quotient stream, selected limit, host equality, independent tail oracle, or
ambient completeness witness, none of those objects can be supplied by the
factorization.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet tail-fiber seal exactness]
\label{thm:regular-cauchy-tail-meet-tail-fiber-seal-exactness}
For an accepted $\RegularCauchyTailMeetUp$ packet whose seal-facing consumer
passes through $\RegularCauchyTailFiberUp$, the TailMeet threshold route and the
TailFiber paired-window route have the same finite support through the two
$\RegSeqRatUp$ source rows, the two $\StreamNameUp$ windows, the two dyadic
ledger rows, the shared fiber or threshold row, the
$\RealTailAgreementSealUp$ row, componentwise $\hsame$ rows, displayed $\Cont$
routes, $\Pkg$ provenance, and local $\NameCert$ rows. Conversely, any accepted
TailFiber paired-window seal route supplies the same TailMeet seal-consumer
entrance. The exactness statement exports no quotient streams, selected limits,
host real equality, hidden tail oracles, or ambient completeness.
\end{theorem}
\begin{proof}
For the forward direction, start with
\autoref{thm:regular-cauchy-tail-meet-seal-consumer-tail-fiber-factorization}.
It forces the seal-facing consumer through the displayed TailMeet threshold
tuple before the $\RegularCauchyTailFiberUp$ row is available.

Apply root consumer budget uniqueness together with the TailMeet root
downstream seal route. These identify the source windows, threshold data,
continuation rows, package row, and local name row as the only TailMeet entrance
for a seal-facing read.

Now use TailFiber paired-window exactness and TailFiber seal-consumer
exhaustion. The TailFiber route reads the two regular-rational source rows, two
stream windows, two dyadic ledgers, shared fiber row, seal row, componentwise
$\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows, and no further
consumer coordinate is present.

For the converse direction, begin with an accepted TailFiber paired-window seal
route. TailFiber paired-window exactness supplies the common finite support up
to the shared fiber row, and seal-consumer exhaustion keeps every listed
TailMeet, Real-tail-agreement, and Real-facing consumer inside the same packet.

Re-enter TailMeet through the seal-consumer factorization theorem. The
TailFiber route therefore supplies exactly the TailMeet seal-consumer entrance
already fixed by root consumer budget uniqueness and the TailMeet seal route.
The exclusions in the TailMeet nonescape package and TailFiber ledger
exhaustion rule out quotient streams, selected limits, host real equality,
hidden tail oracles, and ambient completeness.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet budget window service lock]
\label{thm:regular-cauchy-tail-meet-budget-window-service-lock}
For an accepted $\RegularCauchyTailMeetUp$ packet, every
$\FiniteObservationBudgetSelectorUp$, $\RegularCauchyTailSelectorUp$,
$\RealUp$, or $\CompletionUp$ consumer that requests the TailMeet threshold must
first read the paired $\StreamNameUp$ windows $W_0,W_1$, modulus rows
$M_0,M_1$, shared threshold $\tau$, comparison row $Q$, transport rows $H,C$,
ledger $L$, package row $\Pkg$, and local $\NameCert$ row $N$. The service is
locked to this displayed finite route and cannot be replaced by an uncapped
observation request, quotient stream, selected limit, host equality, trichotomy
row, independent tail oracle, or ambient completeness witness.
\end{theorem}
\begin{proof}
Start with window exhaustion. Any threshold-facing consumer must expose the
paired StreamName windows $W_0,W_1$ before $\tau$ is available, and the same
source-window route keeps the request inside the accepted TailMeet packet.

Apply threshold stability and the downstream unblock package. They attach the
modulus rows $M_0,M_1$, the shared threshold $\tau$, the comparison row $Q$, the
transport rows $H,C$, the ledger $L$, the package row $\Pkg$, and the local
name row $N$ to the same finite consumer-facing route.

Use the seal route for $\RealUp$ and $\CompletionUp$ consumers and the
budget-selector handoff for $\FiniteObservationBudgetSelectorUp$ and
$\RegularCauchyTailSelectorUp$ consumers. Both routes consume the displayed
tuple before a threshold service is available.

The remaining non-escape claim is ledger exhaustion for that tuple. Since the
route contains only the displayed windows, modulus rows, threshold, comparison,
transport, ledger, package, and local name, no uncapped observation request,
quotient stream, selected limit, host equality, trichotomy row, independent tail
oracle, or ambient completeness witness can replace the service.
\end{proof}

\begin{theorem}[RegularCauchyTailMeet quotient-refusal factorization]
\label{thm:regular-cauchy-tail-meet-quotient-refusal-factorization}
Every completion-facing consumer of an accepted $\RegularCauchyTailMeetUp$
packet that requests quotient-stream equality must first pass through the finite
TailMeet source-window route and then through the $\QuotientStreamRefusalUp$
route of \autoref{ch:concrete-instances-quotientstreamrefusal-namecert}. If
either route is missing, the request is routed to the refusal ledger. The
handoff exports no selected limit, quotient stream equality, host real equality,
trichotomy, independent tail oracle, or ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with the downstream unblock package. It exposes the paired source windows
$R_0,R_1,W_0,W_1$, modulus rows $M_0,M_1$, shared threshold $\tau$, comparison
row $Q$, and structural rows $H,C,L,\Pkg,N$ before any completion-facing read is
available.

Apply the threshold stability and budget-selector handoff theorems. They keep
the selected downstream budget attached to the same finite TailMeet packet and
prevent a selector from bypassing the source windows, modulus rows, threshold,
comparison, ledger, package, or local name.

Next use the $\QuotientStreamRefusalUp$ non-escape route of
\autoref{ch:concrete-instances-quotientstreamrefusal-namecert}. A
quotient-stream equality request has a route only after it is translated into
the displayed StreamName, RegSeqRat, CauchyLimitSeal, and RealUp seal rows; an
off-route request is sent to the refusal ledger.

The two routes therefore factor the completion-facing consumer through the
TailMeet finite source-window surface and then through the quotient-refusal
surface. Since neither packet contains selected limits, quotient stream
equality, host real equality, trichotomy, independent tail oracles, or ambient
completeness, none of those objects is exported.
\end{proof}
