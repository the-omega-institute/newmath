\chapter{A Concrete Naming Certificate for $\CompactUniformPointwiseRadiusChainUp$}
\label{ch:concrete-instances-compact-uniform-pointwise-radius-chain-namecert}
\origin{human}

Following Bishop--Bridges compact metric analysis, the $\CompactUniformPointwiseRadiusChainUp$ packet records the finite center-indexed radius chain that lies between local pointwise moduli and the rational lower-bound fold in the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity}. It consumes compact metric source rows from \autoref{ch:concrete-instances-compactmetric-namecert}, pointwise-modulus rows from \autoref{ch:concrete-instances-continuousmap-namecert}, finite lower-bound folding from \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, and the metric-triangle handoff of \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}. The packet names a finite radius-chain surface before a $\UniformModulusUp$ consumer reads the global handoff; it is not open-cover compactness, a selected Lebesgue number, countable choice, or ambient completeness.

\begin{definition}[CompactUniformPointwiseRadiusChain carrier]
\label{def:compact-uniform-pointwise-radius-chain-carrier}
A $\CompactUniformPointwiseRadiusChainUp$ carrier is a finite $\BHist$ packet
$$
R=(K,F,A,L,T,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row exposing the finite-net centers and their metric coverage data.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row attached to the displayed centers.
\item $A$ is the center-indexed radius-chain row that records, for each displayed center, the local radius budget and its successor comparison.
\item $L$ is the $\RatUp$ lower-bound fold boundary consumed by the finite-net minimum fold.
\item $T$ is the $\MetricTriangleUniformEstimateUp$ handoff row that turns centerwise estimates into the uniform comparison route.
\item $U$ is the $\UniformModulusUp$ export row reached only after $K,F,A,L,T$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the compact source, pointwise modulus, radius chain, lower-bound fold, triangle estimate, uniform export, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a compact-uniform query through $K,F,A,L,T,U,H$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformPointwiseRadiusChainUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\FiniteNetMinimumFoldUp$, $\MetricTriangleUniformEstimateUp$, $\UniformModulusUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformPointwiseRadiusChainUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows, their transports, continuation replay, package provenance, and local naming.
\end{definition}

\begin{theorem}[CompactUniformPointwiseRadiusChain obligations]
\label{thm:compact-uniform-pointwise-radius-chain-obligations}
For every accepted $\CompactUniformPointwiseRadiusChainUp$ carrier $R=(K,F,A,L,T,U,H,C,P,N)$, the local $\NameCert_{\CompactUniformPointwiseRadiusChainUp}$ surface consists exactly of carrier admission, compact-source exposure through $K$, pointwise-modulus exposure through $F$, center-indexed radius-chain exposure through $A$, rational lower-bound fold boundary through $L$, metric-triangle handoff through $T$, uniform-modulus export through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-pointwise-radius-chain-carrier}. The compact source, pointwise-modulus rows, radius chain, lower-bound fold, triangle estimate, and uniform export are the only analytic rows visible to a consumer.

Read the compact-uniform route in the displayed order. The compact source row $K$ supplies the finite centers, the pointwise-modulus row $F$ supplies local bounds at those centers, the radius-chain row $A$ records the centerwise budgets, the fold boundary $L$ exposes their rational lower bound, and the triangle handoff $T$ produces the uniform-modulus export $U$.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Because no coordinate stores open-cover compactness, a selected Lebesgue number, countable choice, quotient equality, or ambient completeness, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\closureat{CompactUniformPointwiseRadiusChainUp}{seedStr}

\begin{closurestatus}{\CompactUniformPointwiseRadiusChainUp}
  \constructivestory{A $\CompactUniformPointwiseRadiusChainUp$ packet is generated from compact metric source rows, continuous-map pointwise-modulus rows, center-indexed radius-chain rows, a RatUp lower-bound fold boundary, a MetricTriangleUniformEstimate handoff, a UniformModulus export row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-pointwise-radius-chain-carrier} and \autoref{thm:compact-uniform-pointwise-radius-chain-obligations} name the finite radius-chain packet between local moduli and the global compact-uniform modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected Lebesgue numbers, countable choice, quotient equality, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, pointwise-modulus exposure, radius-chain exposure, rational fold boundary, triangle handoff, uniform export, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformPointwiseRadiusChainUp.}
\end{closurestatus}
