\chapter{A Concrete Naming Certificate for $\HyperbolicDirichletDomainUp$}
\label{ch:concrete-instances-hyperbolic-dirichlet-domain-namecert}
\origin{human}

Following Poincare, Klein, and classical Fuchsian fundamental-domain theory, the $\HyperbolicDirichletDomainUp$ packet records a finite Dirichlet-domain surface inside displayed Poincare disk data. It sits after the metric-read surface of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the action-word surface of \autoref{ch:concrete-instances-fuchsian-group-action-namecert}, the visual-boundary packet of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, and the hyperbolic metric route of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}. The packet names finite side-pairing, metric-read, boundary-refusal, and local NameCert rows; it does not construct a quotient surface, a selected tessellation, a global moduli object, or host-level discreteness theorem.

\begin{definition}[Hyperbolic Dirichlet domain carrier]
\label{def:hyperbolic-dirichlet-domain-carrier}
A $\HyperbolicDirichletDomainUp$ carrier is a finite $\BHist$ packet
$$
D=(O,G,S,M,B,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the displayed basepoint and disk-coordinate row inside the $\PoincareDiskMetricUp$ surface.
\item $G$ is the finite $\FuchsianGroupActionUp$ action-word row whose inspected translates determine the displayed side candidates.
\item $S$ is the side-pairing ledger, recording the finite bisector or boundary-side rows, their paired action words, and the local incidence data read from the disk model.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row used to compare the basepoint with the inspected translates along the displayed side candidates.
\item $B$ is the boundary-refusal row: ideal endpoints, visual-boundary contacts, and exterior reads are exposed only as finite refusal or boundary-facing data, not as a completed quotient boundary.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row available to consumers that inspect visible boundary arcs of the displayed sides.
\item $H$ records componentwise $\hsame$ transport for basepoint, action-word, side-pairing, metric-read, boundary-refusal, visual-boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from basepoint inspection through action words, metric reads, side-pairing checks, boundary refusal, visual-boundary reads, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicDirichletDomainUp$, $\PoincareDiskMetricUp$, $\FuchsianGroupActionUp$, $\HyperbolicMetricUp$, $\HyperbolicVisualBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicDirichletDomainUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $O,G,S,M,B,V,H,C,P,N$. It has no coordinate for a quotient hyperbolic surface, selected global tessellation, host discreteness proof, moduli point, or completed ideal-boundary equality.
\end{definition}

\begin{theorem}[Hyperbolic Dirichlet domain NameCert obligations]
\label{thm:hyperbolic-dirichlet-domain-namecert-obligations}
For every accepted $\HyperbolicDirichletDomainUp$ carrier $D=(O,G,S,M,B,V,H,C,P,N)$, the local $\NameCert_{\HyperbolicDirichletDomainUp}$ surface consists exactly of basepoint admission through $O$, finite action-word admission through $G$, side-pairing ledger exactness through $S$, hyperbolic metric-read exactness through $M$, boundary-refusal exposure through $B$, visual-boundary exposure through $V$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-dirichlet-domain-carrier}. The packet displays exactly the basepoint, finite action-word, side-pairing, metric-read, boundary-refusal, visual-boundary, transport, replay, provenance, and naming rows.

The geometric obligations are finite. The basepoint row is read through the Poincare disk metric surface, the action words through the Fuchsian action surface, and the side-pairing ledger through the displayed bisector and incidence rows.

The metric and boundary rows are downstream of those displayed data. The row $M$ gives the hyperbolic metric comparisons, $B$ records refusal of quotient and ideal-boundary data, and $V$ is the only visual-boundary route available to boundary-facing consumers.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since the excluded global objects are absent from the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic Dirichlet domain side route]
\label{thm:hyperbolic-dirichlet-domain-side-route}
Every side-pairing or boundary-facing read from an accepted $\HyperbolicDirichletDomainUp$ carrier factors through
$$
\PoincareDiskMetricUp \longmapsto \FuchsianGroupActionUp \longmapsto \HyperbolicMetricUp \longmapsto \HyperbolicVisualBoundaryUp .
$$
Thus the Dirichlet-domain packet exposes finite basepoint, action-word, side-pairing, metric-read, boundary-refusal, and visual-boundary rows before any consumer can inspect a domain side.
\end{theorem}
\begin{proof}
Start with the basepoint row $O$. It is read through the Poincare disk metric packet, so the domain begins as displayed disk-coordinate and metric-read data rather than as a completed hyperbolic plane.

Next inspect the action row $G$ and side ledger $S$. The finite Fuchsian action words determine which displayed translates and side candidates are available to the packet.

Read the metric and boundary rows $M,B,V$. The metric row compares the displayed basepoint with inspected translates, while $B$ and $V$ restrict boundary-facing data to refusal and visual-boundary rows.

The remaining rows $H,C,P,N$ preserve, replay, source, and name the same route. No quotient surface, selected tessellation, host discreteness proof, moduli point, or completed boundary equality can be read from the carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicDirichletDomainUp$ side read displays a PoincareDiskMetric basepoint row, a FuchsianGroupAction word row, a side-pairing ledger, a HyperbolicMetric comparison row, a boundary-refusal row, a HyperbolicVisualBoundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{poincare-disk-metric, fuchsian-group-action, hyperbolic-metric, hyperbolic-visual-boundary: $\HyperbolicDirichletDomainUp$ is not bijective to any listed sibling because it jointly binds a basepoint, finite action words, side-pairing ledger, metric comparisons, boundary refusal, and visual-boundary exposure as one finite domain packet.}

\closureat{HyperbolicDirichletDomainUp}{seedStr}

\begin{closurestatus}{\HyperbolicDirichletDomainUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-dirichlet-domain-carrier}, \autoref{thm:hyperbolic-dirichlet-domain-namecert-obligations}, and \autoref{thm:hyperbolic-dirichlet-domain-side-route} seed the finite Dirichlet-domain packet over Poincare disk metric rows, Fuchsian action words, side-pairing data, hyperbolic metric reads, boundary refusal, visual-boundary exposure, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, quotient hyperbolic surfaces, selected tessellations, host discreteness theorems, global moduli objects, bridge checking, or completed ideal-boundary equality.}
  \upgradepath{Obligation closure requires checked basepoint admission, action-word admission, side-pairing exactness, metric-read exactness, boundary-refusal exposure, visual-boundary exposure, transport stability, replay stability, and local naming for BEDC.Derived.HyperbolicDirichletDomainUp.}
  \constructivestory{A $\HyperbolicDirichletDomainUp$ packet is generated from a PoincareDiskMetric basepoint row, a FuchsianGroupAction word row, a finite side-pairing ledger, HyperbolicMetric comparisons, boundary-refusal data, a HyperbolicVisualBoundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
