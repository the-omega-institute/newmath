\chapter{A Concrete Naming Certificate for $\CantorSpaceFanModulusUp$}
\label{ch:concrete-instances-cantor-space-fan-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\CantorSpaceFanModulusUp$ packet records the finite fan-bar route from Cantor-space continuity to a unary uniform modulus. It sits after the binary prefix carrier of $\CantorSpaceUp$, the bounded-bar surface of $\FanTheoremUp$, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the output interface of $\UniformModulusUp$, and the compact-uniform consumer surface of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}. The packet names a finite fan-bar modulus handoff; it does not use open-cover compactness, a selected infinite branch, quotient stream equality, countable choice, or host continuity.

\begin{definition}[CantorSpace fan modulus carrier]
\label{def:cantor-space-fan-modulus-carrier}
A $\CantorSpaceFanModulusUp$ carrier is a finite $\BHist$ packet
$$
F=(X,B,d,W,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CantorSpaceUp$ source row, recording binary prefix observations and the branch-facing source boundary;
\item $B$ is the finite fan bar row supplied by $\FanTheoremUp$, with each bar hit witnessed by a displayed binary prefix;
\item $d$ is the unary depth bound read from the bar, naming the maximum inspected prefix depth as finite $\NatUp$ data;
\item $W$ is the continuity window row, reading each required prefix through $\StreamNameUp$ and retaining only the finite observations below $d$;
\item $U$ is the modulus output row consumed by $\UniformModulusUp$ and by downstream $\CompactUniformModulusUp$ packets;
\item $H$ records componentwise $\hsame$ transport for the Cantor source, fan bar, depth, window, output, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from the Cantor source through the fan bar and continuity window to the modulus output;
\item $P$ is the $\Pkg$ provenance over $\CantorSpaceFanModulusUp$, $\CantorSpaceUp$, $\FanTheoremUp$, $\StreamNameUp$, $\UniformModulusUp$, $\CompactUniformModulusUp$, $\NatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CantorSpaceFanModulusUp}$ row.
\end{enumerate}
The carrier exports no open-cover compactness object, selected infinite branch, quotient stream equality, countable-choice functional, host modulus function, or ambient compact Hausdorff theorem.
\end{definition}

\begin{theorem}[CantorSpace fan modulus NameCert obligations]
\label{thm:cantor-space-fan-modulus-namecert-obligations}
For every accepted $\CantorSpaceFanModulusUp$ carrier
$$
F=(X,B,d,W,U,H,C,P,N),
$$
the local $\NameCert_{\CantorSpaceFanModulusUp}$ surface consists exactly of carrier admission for the Cantor source row $X$, finite fan-bar exactness for $B$, unary depth-bound exactness for $d$, continuity-window coverage for $W$, modulus-output admission for $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission projects the finite tuple $F$. The only source row is the displayed $\CantorSpaceUp$ prefix row $X$, and the only compactness-facing row is the finite fan bar $B$.

The fan bar supplies the unary depth bound $d$. Reading below $d$ gives the finite continuity window $W$, and the modulus output $U$ is admitted only after that window is available.

Finally $H,C,P,N$ transport, replay, package, and name the same finite route. Since no carrier coordinate names open-cover compactness, a selected infinite branch, quotient stream equality, countable choice, a host modulus, or ambient compactness, the listed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[CantorSpace fan modulus continuity handoff]
\label{thm:cantor-space-fan-modulus-continuity-handoff}
Every accepted $\CantorSpaceFanModulusUp$ carrier hands a $\UniformModulusUp$ or $\CompactUniformModulusUp$ consumer exactly the finite route
$$
\CantorSpaceUp \longmapsto \FanTheoremUp \longmapsto \StreamNameUp \longmapsto \UniformModulusUp .
$$
The route exposes the source prefix row $X$, finite fan bar $B$, unary depth bound $d$, continuity window $W$, modulus output $U$, and support rows $H,C,P,N$, and it exposes no open-cover compactness, selected branch, quotient stream, countable-choice schedule, host modulus, or ambient completeness row.
\end{theorem}
\begin{proof}
Project an accepted carrier. The consumer first reads the Cantor source row $X$, then the fan-bar row $B$ and its unary depth bound $d$.

The continuity window $W$ is read through the finite $\StreamNameUp$ observations below that bound. This is the only point at which branch data is inspected, and the inspection is prefix-bounded.

The output row $U$ then supplies the displayed modulus to $\UniformModulusUp$ and to the compact-uniform surface of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}. The support rows $H,C,P,N$ preserve the same finite route, so no excluded compactness or choice datum can be projected from the packet.
\end{proof}

\closureat{CantorSpaceFanModulusUp}{seedStr}

\begin{closurestatus}{\CantorSpaceFanModulusUp}
  \constructivestory{A $\CantorSpaceFanModulusUp$ packet is generated from a CantorSpaceUp source row, a FanTheoremUp finite fan bar, a unary depth bound, a StreamNameUp continuity window, a UniformModulusUp output, componentwise hsame transport, Cont replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-space-fan-modulus-carrier}, \autoref{thm:cantor-space-fan-modulus-namecert-obligations}, and \autoref{thm:cantor-space-fan-modulus-continuity-handoff} specify the finite Cantor fan-bar modulus carrier, local NameCert obligations, and compact-uniform handoff route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not prove FanTheoremUp, open-cover compactness, selected infinite branches, quotient stream equality, countable choice, host continuity, or bridge checking for $\CantorSpaceFanModulusUp$.}
  \upgradepath{Obligation closure requires carrier admission, fan-bar exactness, depth-bound exactness, continuity-window coverage, modulus-output admission, transport stability, replay, provenance, and non-escape rows.}
\end{closurestatus}
