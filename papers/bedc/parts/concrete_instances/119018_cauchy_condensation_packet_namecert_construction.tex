\chapter{A Concrete Naming Certificate for $\CauchyCondensationPacketUp$}
\label{ch:concrete-instances-cauchy-condensation-packet-namecert}
\origin{human}

Following Cauchy and Bishop--Bridges constructive analysis, the $\CauchyCondensationPacketUp$ packet records Cauchy's condensation test for positive decreasing real series as finite BEDC rows. It sits after the finite partial-sum surface of $\RealSeriesUp$ in \autoref{ch:concrete-instances-realseries-namecert}, the series-tail criterion of \autoref{ch:concrete-instances-cauchy-series-criterion-namecert}, the positive comparison route of \autoref{ch:concrete-instances-positive-series-comparison-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite condensation route; it is not a quotient of term streams, a selected infinite sum, countable choice, order trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy condensation packet carrier]
\label{def:cauchy-condensation-packet-carrier}
A $\CauchyCondensationPacketUp$ carrier is a finite $\BHist$ packet
$$
K=(A,S,L,M,R,E,H,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the positive decreasing source-series row. It records a $\RealSeriesUp$ term surface whose inspected terms are nonnegative and monotone decreasing on the accepted finite windows.
\item $S$ is the dyadic block schedule. It names the finite powers-of-two cut points and the requested block windows as $\DyadicRatCoreUp$ data rather than as a host exponentiation oracle.
\item $L$ is the block-sum ledger. It records the displayed comparison between each source block and its condensed representative block sum.
\item $M$ is the monotone comparison row. It exposes the finite inequalities that bound every term in a displayed dyadic block by the appropriate endpoint terms.
\item $R$ is the $\RegSeqRatUp$ handoff reached from the source windows, block schedule, ledger, and comparison row.
\item $E$ is the terminal $\RealUp$ seal available only after $A,S,L,M,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source series, dyadic schedule, block ledger, monotone comparison, regular handoff, real seal, replay, provenance, and local naming rows.
\item $Q$ records displayed $\Cont$ replay from a requested series-tail read through the dyadic block schedule, block-sum ledger, monotone comparison row, regular handoff, and real seal.
\item $P$ is the $\Pkg$ provenance over $\CauchyCondensationPacketUp$, $\RealSeriesUp$, $\CauchySeriesCriterionUp$, $\PositiveSeriesComparisonUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCondensationPacketUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $A,S,L,M,R,E,H,Q,P,N$. It has no coordinate for a host convergence theorem, quotient stream equality, a selected sum, countable choice, order trichotomy, or ambient completeness.
\end{definition}

\begin{theorem}[Cauchy condensation packet NameCert obligations]
\label{thm:cauchy-condensation-packet-namecert-obligations}
For every accepted $\CauchyCondensationPacketUp$ carrier $K=(A,S,L,M,R,E,H,Q,P,N)$, the local $\NameCert_{\CauchyCondensationPacketUp}$ surface consists exactly of carrier admission for the positive decreasing source series, dyadic block schedule, block-sum ledger, monotone comparison row, $\RegSeqRatUp$ handoff, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming row. No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-condensation-packet-carrier}. Its analytic inventory is exactly the source series row $A$, the dyadic block schedule $S$, the block ledger $L$, the monotone comparison row $M$, the regular readback $R$, and the real seal $E$.

The condensation obligations are finite. The source series is inspected only through displayed windows, while $S$ and $L$ record the finite powers-of-two blocks and their block sums as $\DyadicRatCoreUp$ data.

The monotone comparison row $M$ supplies the only route from source terms to block bounds. The handoff $R$ and seal $E$ are therefore available only after the source windows, schedule, ledger, and comparison rows have been displayed.

Finally $H,Q,P,N$ transport, replay, source, and name the same route. Since the carrier has no row for quotient streams, selected sums, countable choice, order trichotomy, host convergence, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cauchy condensation packet real handoff]
\label{thm:cauchy-condensation-packet-real-handoff}
Every $\RealUp$- or completion-facing read from an accepted $\CauchyCondensationPacketUp$ carrier factors through
$$
\StreamNameUp \longrightarrow \DyadicRatCoreUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp .
$$
In particular, the condensation read reaches the terminal real seal only by displaying the positive decreasing source-series windows, the dyadic block schedule, the block-sum ledger, the monotone comparison row, and the regular rational handoff.
\end{theorem}
\begin{proof}
Start with a real-facing read of an accepted carrier. The source series row $A$ exposes only finite term windows, so the read begins at the $\StreamNameUp$ surface.

Next read $S,L,M$. These rows provide the dyadic block schedule, block-sum ledger, and monotone comparison evidence as $\DyadicRatCoreUp$ data before any regular rational handoff may be consumed.

The row $R$ is the unique $\RegSeqRatUp$ handoff for the displayed condensation route. Once $R$ is present, the terminal seal $E$ may be read, and $H,Q,P,N$ preserve and name the same route.

Thus every real- or completion-facing condensation read factors through the displayed stream windows, dyadic block evidence, regular rational handoff, and real seal. No bypass supplies quotient stream equality, selected sums, countable choice, order trichotomy, host convergence, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyCondensationPacketUp$ read displays one positive decreasing RealSeries source row, one dyadic block schedule, one block-sum ledger, one monotone comparison row, one RegSeqRat handoff, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before a condensation conclusion is consumed.}

\independenceWitness{realseries, cauchy-series-criterion, positive-series-comparison, dyadicratcore, streamname, regseqrat, real: $\CauchyCondensationPacketUp$ is not bijective to any listed sibling because it jointly binds a positive decreasing source series, dyadic block scheduling, block-sum comparison, monotone endpoint control, regular rational handoff, and real sealing in one finite packet.}

\closureat{CauchyCondensationPacketUp}{seedStr}

\begin{closurestatus}{\CauchyCondensationPacketUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-condensation-packet-carrier}, \autoref{thm:cauchy-condensation-packet-namecert-obligations}, and \autoref{thm:cauchy-condensation-packet-real-handoff} seed the finite Cauchy condensation route over positive decreasing source series, dyadic block schedules, block-sum ledgers, monotone comparison rows, regular handoff, real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, host infinite-sum theorems, quotient stream equality, selected sums, order trichotomy, countable choice, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, block schedule admission, block-sum ledger exactness, monotone comparison stability, regular handoff nonescape, real-seal nonescape, transport stability, replay stability, and local naming for BEDC.Derived.CauchyCondensationPacketUp.}
  \constructivestory{A $\CauchyCondensationPacketUp$ packet is generated from a positive decreasing source series, a dyadic block schedule, a block-sum ledger, a monotone comparison row, a $\RegSeqRatUp$ handoff, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
