\chapter{A Concrete Naming Certificate for $\BishopEpsilonDeltaRealEqualityUp$}
\label{ch:concrete-instances-bishop-epsilon-delta-real-equality-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopEpsilonDeltaRealEqualityUp$ packet records equality of real names as a finite epsilon--delta request and response surface. It sits on the L10 corridor through the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the RealEquality classifier boundary of \autoref{ch:concrete-instances-real-equality-namecert}. The packet names the displayed equality route only; it does not turn equality into quotient real equality or into host equality of selected representatives.

\begin{definition}[Bishop epsilon-delta real equality carrier]
\label{def:bishop-epsilon-delta-real-equality-carrier}
A $\BishopEpsilonDeltaRealEqualityUp$ carrier is a finite $\BHist$ packet
$$
E=(Q,\Delta,D,S,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the epsilon-request row, recording the finite positive tolerance requested by the equality consumer;
\item $\Delta$ is the delta-response row, giving the finite modulus answer used to compare the two real-name windows at tolerance $Q$;
\item $D$ is the $\DyadicRatCoreUp$ tolerance row that carries the dyadic comparison budget used by the response;
\item $S$ is the pair of $\StreamNameUp$ finite windows inspected for the two real names at the requested tolerance;
\item $R$ is the pair of $\RegSeqRatUp$ readback rows obtained from the displayed stream windows;
\item $L$ is the $\RealUp$ seal row for the two terminal real-name boundaries, reached only after $Q,\Delta,D,S,R$ have been displayed;
\item $H$ records the componentwise $\hsame$ transports for epsilon requests, delta responses, dyadic tolerances, stream windows, regular readbacks, real seals, and the local packet shape;
\item $C$ records the displayed $\Cont$ replay from epsilon request through delta response, dyadic tolerance, finite windows, regular readbacks, Real seal, and local naming;
\item $P$ is the $\Pkg$ provenance over $\BishopEpsilonDeltaRealEqualityUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealEqualityUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\BishopEpsilonDeltaRealEqualityUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through these rows. It has no coordinate for quotient real equality, selected representatives, trichotomy, countable choice, ambient Real completeness, or host equality of completed real objects.
\end{definition}

\begin{theorem}[Bishop epsilon-delta real equality NameCert obligations]
\label{thm:bishop-epsilon-delta-real-equality-namecert-obligations}
For every accepted $\BishopEpsilonDeltaRealEqualityUp$ carrier
$$
E=(Q,\Delta,D,S,R,L,H,C,P,N),
$$
the local $\NameCert_{\BishopEpsilonDeltaRealEqualityUp}$ surface consists exactly of carrier admission for the epsilon request $Q$, delta response $\Delta$, dyadic tolerance row $D$, finite $\StreamNameUp$ windows $S$, $\RegSeqRatUp$ readbacks $R$, terminal $\RealUp$ seal $L$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, package provenance $P$, and local naming row $N$. Every equality-facing consumer must expose those rows before it can read the RealEquality boundary.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-epsilon-delta-real-equality-carrier}. Its equality data are the epsilon request, delta response, dyadic tolerance ledger, two finite stream windows, two regular rational readbacks, and the terminal Real seal row.

Read the route in dependency order. The request $Q$ determines which tolerance must be answered, $\Delta$ supplies the displayed response budget, $D$ records the dyadic tolerance used by that response, and $S$ records the finite windows inspected under that budget.

The readback row $R$ is then obtained from the displayed windows, and the seal row $L$ is reached only after the readbacks have been exposed. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet, so the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop epsilon-delta real equality Real seal nonescape]
\label{thm:bishop-epsilon-delta-real-equality-real-seal-nonescape}
Every Real-facing equality read of an accepted $\BishopEpsilonDeltaRealEqualityUp$ packet factors through the finite route
$$
Q,\Delta,D,S,R,L,H,C,P,N.
$$
The terminal $\RealUp$ seal row $L$ may be inspected only after the epsilon request, delta response, dyadic tolerance, StreamName windows, and RegSeqRat readbacks have been displayed. The packet exports no quotient real equality, selected representative, trichotomy, countable-choice schedule, ambient Real completeness, host equality, Lean verification claim, or bridge check.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bishop-epsilon-delta-real-equality-namecert-obligations}. They force every equality-facing consumer to expose the epsilon request, delta response, dyadic tolerance, finite windows, and regular readbacks before the Real seal row is available.

Now apply the sibling route through \autoref{ch:concrete-instances-real-equality-namecert}. That boundary accepts equality only through displayed finite windows, regular readbacks, and dyadic tolerance data, not through a quotient of completed streams or a host equality predicate.

Finally inspect the carrier coordinates. Since no coordinate names a selected representative, trichotomy principle, countable-choice schedule, ambient completeness theorem, Lean target, or standard bridge, none of those objects can be exported by the local Real seal route.
\end{proof}

\closureat{BishopEpsilonDeltaRealEqualityUp}{seedStr}

\begin{closurestatus}{\BishopEpsilonDeltaRealEqualityUp}
  \constructivestory{A $\BishopEpsilonDeltaRealEqualityUp$ packet is generated from a finite epsilon request, delta response, dyadic tolerance row, finite StreamName windows, RegSeqRat readbacks, terminal Real seal, componentwise transport, continuation replay, package provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter binds the finite epsilon--delta Real-equality carrier, its local NameCert obligations, and the Real-seal nonescape boundary along the L10 Real and Completion corridor.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{It does not close quotient real equality, selected representatives, trichotomy, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Reaching obligation closure requires a Lean carrier and checked proof that every accepted equality consumer exposes the same epsilon request, delta response, dyadic tolerance, finite-window, readback, and Real-seal route.}
\end{closurestatus}
