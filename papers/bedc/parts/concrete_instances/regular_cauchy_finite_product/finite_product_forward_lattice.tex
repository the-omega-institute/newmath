The forward lattice for $\RegularCauchyFiniteProductUp$ is consumed by \autoref{ch:concrete-instances-regular-cauchy-product-projection-namecert} and \autoref{ch:concrete-instances-cauchyproduct-namecert} through the shared finite index, synchronized $\StreamNameUp$ windows, dyadic product-budget ledger, and $\RegSeqRatUp$ readback. Downstream product and projection packets therefore reuse the displayed finite route rather than adding a host product, a selected component representative, quotient stream equality, trichotomy, or ambient completeness.
