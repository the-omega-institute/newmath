The genuine input route for $\RegularCauchyFiniteProductUp$ reads the finite product ledger of \autoref{ch:concrete-instances-prod-namecert}, then the synchronized finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, and then the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert} before any Real-facing projection or terminal seal is consumed. This order keeps $\ProductUp$, $\StreamNameUp$, and $\RegSeqRatUp$ visible as horizontal dependencies rather than hiding the finite product inside a kernel-only carrier or a downstream $\RealUp$ projection.
