The product-budget projection lattice for $\RegularCauchyFiniteProductUp$ is consumed by \autoref{ch:concrete-instances-regular-cauchy-product-projection-namecert}, \autoref{ch:concrete-instances-regularcauchyproduct-namecert}, and \autoref{ch:concrete-instances-cauchyproduct-namecert}. The route exports the finite index rows, synchronized $\StreamNameUp$ windows, $\DyadicRatCoreUp$ product-budget ledger, projection rows, and $\RegSeqRatUp$ readbacks as one displayed dependency surface, so downstream product consumers reuse the finite ledger rather than importing a host product coordinate, a selected component representative, quotient stream equality, trichotomy, or ambient completeness.
