The downstream projection packet of \autoref{ch:concrete-instances-regular-cauchy-product-projection-namecert} consumes the $\RegularCauchyFiniteProductUp$ carrier's finite index row, component source rows, synchronized window family, dyadic product-budget ledger, and final readback row as the shared route from which left and right projection readbacks are exposed. Thus projection consumers reuse the finite product's displayed source, window, budget, and readback rows rather than importing a host product or a selected component representative.
