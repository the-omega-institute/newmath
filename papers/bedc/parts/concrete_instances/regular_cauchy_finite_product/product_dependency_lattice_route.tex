\begin{theorem}[Regular Cauchy finite product dependency-lattice route]
\label{thm:regular-cauchy-finite-product-lattice-route}
Every accepted $\RegularCauchyFiniteProductUp$ packet reads its horizontal dependency lattice through $\ProductUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyProductUp$, and $\RealUp$ before a finite-product read is exported. The route consumes synchronized stream windows and dyadic budget rows for the displayed fold; it does not import host finite products, selected component representatives, quotient stream equality, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the product-facing request. The finite product source is opened through $\ProductUp$ only as a displayed dependency row, and the component regular Cauchy data are read through $\RegSeqRatUp$ sources.

The synchronized observation surface comes next. $\StreamNameUp$ supplies the shared finite windows for the components and intermediate reads, while $\DyadicRatCoreUp$ supplies the tolerance and product-budget rows used by the fold.

The binary product route through $\RegularCauchyProductUp$ is then replayed inside the finite-product packet, and the terminal $\RealUp$ seal is read only after the final regular readback is displayed. Because every step uses these finite rows, the dependency lattice exports no host finite product or selected coordinate object.
\end{proof}
