\chapter{A Concrete Naming Certificate for $\BishopCompletionFunctorialityUp$}
\label{ch:concrete-instances-bishop-completion-functoriality-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopCompletionFunctorialityUp$ packet records the constructive functoriality surface for uniformly continuous maps between displayed completion inputs. It reads the source completion row of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}, the target completion row of \autoref{ch:concrete-instances-cauchy-completion-functoriality-namecert}, the uniformly continuous map row of \autoref{ch:concrete-instances-uniform-continuous-extension-namecert}, the extension and uniqueness boundary of \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, the regular Cauchy readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only a finite Bishop-style completion functoriality route; it does not claim quotient completion, selected limits, countable choice, host function extensionality, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop completion functoriality carrier]
\label{def:bishop-completion-functoriality-carrier}
A $\BishopCompletionFunctorialityUp$ carrier is a finite $\BHist$ packet
$$
F=(S,T,U,L,E,Q,R,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source completion row, naming the regular Cauchy source packet before any map extension is read.
\item $T$ is the target completion row, naming the codomain completion packet reached by the same finite tolerance discipline.
\item $U$ is the uniformly continuous map row, recording the displayed source-to-target map and the modulus ledger that sends target tolerances to source tolerances.
\item $L$ is the Cauchy-control ledger, recording how source Cauchy windows are transported by $U$ into target Cauchy windows.
\item $E$ is the extension row, naming the completed map only after $S,T,U,L$ have been displayed.
\item $Q$ is the uniqueness row, requiring any two accepted extension reads over the same $S,T,U,L$ rows to agree through the displayed Bishop completion boundary.
\item $R$ is the $\RegSeqRatUp$ readback row serving the regular rational observations used by the source and target completion rows.
\item $W$ is the $\StreamNameUp$ finite-window row through which source and target Cauchy requests are opened.
\item $H$ records componentwise $\hsame$ transport for $S,T,U,L,E,Q,R,W$ and the structural rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,T,U,L \longmapsto W,R \longmapsto E,Q .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopCompletionFunctorialityUp$, $\CauchyCompletionFunctorialityUp$, $\CauchyCompletionFunctorUp$, $\UniformContinuousExtensionUp$, $\CompletionUniversalPropertyUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCompletionFunctorialityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient completion, selected limits, countable choice, host function extensionality, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop completion functoriality NameCert obligations]
\label{thm:bishop-completion-functoriality-namecert-obligations}
For the carrier of \autoref{def:bishop-completion-functoriality-carrier}, the local $\NameCert_{\BishopCompletionFunctorialityUp}$ surface consists exactly of source completion admission, target completion admission, uniformly continuous map admission, Cauchy-control ledger compatibility, extension row construction, uniqueness row comparison, regular rational readback, finite stream-window exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:bishop-completion-functoriality-carrier}. The rows $S,T,U,L$ fix the source completion, target completion, uniformly continuous map, and Cauchy-control ledger before any completed-map read is available.

Read the observation rows next. The finite windows $W$ and regular rational readbacks $R$ are the only source and target observations used by the extension route.

Construct the extension through $E$ and compare it through $Q$. The uniqueness row compares only accepted reads over the same $S,T,U,L,W,R$ data, so no host function extensionality or selected limit can enter the certificate.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the excluded principles have no carrier coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}
\leanchecked{BEDC.Derived.BishopCompletionFunctorialityUp.BishopCompletionFunctorialityNameCertObligations}

\begin{theorem}[Bishop completion functoriality route factorization]
\label{thm:bishop-completion-functoriality-route-factorization}
Every accepted $\BishopCompletionFunctorialityUp$ export factors through the finite route
$$
\CauchyCompletionFunctorUp \longmapsto \CauchyCompletionFunctorialityUp \longmapsto \UniformContinuousExtensionUp \longmapsto \CompletionUniversalPropertyUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The exported map is therefore a Bishop-style completion functoriality read over displayed Cauchy-control data, not a quotient completion, selected limit, countable-choice schedule, host function equality principle, or ambient completeness assertion for $\RealUp$.
\end{theorem}
\begin{proof}
Begin with the source and target completion rows. They are read through $S$ and $T$, so the completion-facing part of the packet is already bound to the displayed Cauchy completion functor rows.

Next read the map and control rows $U$ and $L$. Uniform continuity supplies the tolerance transport, and the Cauchy-control ledger records how source windows become target windows without selecting a completed limit.

The observation layer $W,R$ then opens finite windows and regular rational readbacks. Only after these rows are visible can the extension and uniqueness rows $E,Q$ be consumed.

The remaining structural rows $H,C,P,N$ preserve and name the same route. Thus every export factors through the displayed siblings and no excluded completion or choice principle is available.
\end{proof}

\falsifiablePrediction{Any accepted $\BishopCompletionFunctorialityUp$ consumer that exports a completed-map action without first displaying source completion, target completion, uniformly continuous map, Cauchy-control, StreamName window, RegSeqRat readback, extension, uniqueness, transport, replay, provenance, and local naming rows refutes the packet.}

\closureat{BishopCompletionFunctorialityUp}{seedStr}

\begin{closurestatus}{\BishopCompletionFunctorialityUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-completion-functoriality-carrier}, \autoref{thm:bishop-completion-functoriality-namecert-obligations}, and \autoref{thm:bishop-completion-functoriality-route-factorization} seed the finite Bishop completion functoriality route through displayed source completion, target completion, uniformly continuous map, Cauchy-control, extension, uniqueness, finite windows, and regular readback rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion, selected limits, countable choice, host function extensionality, host real equality, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source and target completion row visibility, uniformly continuous map routing, Cauchy-control compatibility, extension construction, uniqueness comparison, finite observation readback, transport stability, replay, provenance, and local NameCert registration for BEDC.Derived.BishopCompletionFunctorialityUp.}
  \constructivestory{A $\BishopCompletionFunctorialityUp$ packet is generated from source completion and target completion rows, a uniformly continuous map row, a Cauchy-control ledger, extension and uniqueness rows, StreamName windows, RegSeqRat readback, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
