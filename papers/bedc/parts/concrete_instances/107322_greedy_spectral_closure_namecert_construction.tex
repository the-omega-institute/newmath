\chapter{A Concrete Naming Certificate for $\GreedySpectralClosureUp$}
\label{ch:concrete-instances-greedy-spectral-closure-namecert}
\origin{ai}

The $\GreedySpectralClosureUp$ packet realizes the block-optimal spectral closure route of \autoref{thm:philosophy-block-optimal-budget-frustrated-spectral-closure} as finite BEDC NameCert data. It reads the active codon support, fixed-budget spectral frontiers, anchor and median layers, and symmetric inactive completion from the DNA-window spectral closure vision, then records only the finite trigger-spectrum rows needed by downstream finite-window and trigger-algebra consumers.

The trigger-spectrum route consumes the finite observation discipline of \autoref{ch:concrete-instances-finitewindowobserver-namecert}, the boolean trigger rows of \autoref{ch:concrete-instances-boolean-trigger-lattice-namecert}, the window-coverage trigger algebra of \autoref{ch:concrete-instances-window-coverage-trigger-algebra-namecert}, and the stabilization surface of \autoref{ch:concrete-instances-spectral-stabilization-namecert}. Thus the packet is not a kernel-only spectral table: its support, trigger, and layered-closure rows are read as sibling finite-window and trigger-algebra data before the local $\NameCert$ row can be consumed.

\begin{definition}[Greedy spectral closure carrier]
\label{def:greedy-spectral-closure-carrier}
A $\GreedySpectralClosureUp$ carrier is a finite $\BHist$ packet
$$
G=(S,A,M,T,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the active support row for $\mathcal{R}$ and the finite complement inside $Q_6$.
\item $A$ is the anchor-block row naming the unique three-point spectral maximizer $\{\mathrm{AAG},\mathrm{ACA},\mathrm{UGG}\}$.
\item $M$ is the median-block row naming the next three-point spectral maximizer $\{\mathrm{AUG},\mathrm{UCG},\mathrm{UUG}\}$.
\item $T$ is the terminal singleton row naming $\mathrm{ACG}$ as the next single-point spectral maximizer.
\item $R$ is the radius table row carrying the displayed killed-walk spectral values and multiplicity counts.
\item $L$ is the layered-closure row saying that the packet is block-optimal rather than a nested fixed-budget optimum from $\mathcal{R}$.
\item $H$ records componentwise $\hsame$ transport for support, anchor, median, terminal, radius, layered-closure, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a trigger-spectrum request through $S,A,M,T,R,L,H$.
\item $P$ is the $\Pkg$ provenance over $\GreedySpectralClosureUp$, $\FiniteWindowObserverUp$, $\BooleanTriggerLatticeUp$, $\WindowCoverageTriggerAlgebraUp$, $\SpectralStabilizationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\GreedySpectralClosureUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,A,M,T,R,L,H,C,P,N$. It has no coordinate for a host Perron-root oracle, biological-code truth, stochastic evolution model, quotient codon semantics, or unbounded spectral search.
\end{definition}

\begin{theorem}[Greedy spectral closure NameCert obligations]
\label{thm:greedy-spectral-closure-namecert-obligations}
For every accepted $\GreedySpectralClosureUp$ carrier $G=(S,A,M,T,R,L,H,C,P,N)$, the local $\NameCert_{\GreedySpectralClosureUp}$ surface consists exactly of finite support admission, anchor-block exposure, median-block exposure, terminal singleton exposure, spectral table exposure, layered-closure exposure, componentwise transport, displayed replay, provenance, and local naming.

The Lean follow-up for this packet should provide Nontrivial and FieldFaithful instances for BEDC.Derived.GreedySpectralClosureUp so that $S,A,M,T,R,L,H,C,P,N$ remain separated carrier fields.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:greedy-spectral-closure-carrier}. The rows $S,A,M,T,R,L$ expose the finite support, the three chosen blocks, the spectral table, and the layered-closure reading before any consumer can cite the packet.

The spectral data are finite table rows. They record the displayed optima and multiplicities from the vision file, but they do not introduce a Perron-root oracle or an unbounded search procedure.

The structural rows $H,C,P,N$ preserve that route. Since no host biological truth predicate, stochastic model, quotient codon equality, hidden spectral computation, or unbounded enumeration is a carrier coordinate, the local NameCert obligations are exactly the listed rows.
\end{proof}

\begin{theorem}[Greedy spectral closure trigger nonescape]
\label{thm:greedy-spectral-closure-trigger-nonescape}
Every trigger-spectrum consumer of an accepted $\GreedySpectralClosureUp$ packet factors through
$$
S\longmapsto A,M,T\longmapsto R,L\longmapsto H,C,P,N.
$$
The route may cite the finite anchor block, median block, terminal singleton, displayed spectral values, and layered-closure row. It cannot export a biological-code oracle, a stochastic evolutionary law, an ambient powerset closure, quotient codon equality, or an unbounded spectral optimizer.
\end{theorem}
\begin{proof}
Start with the finite support row $S$. It fixes the active support and complement inside the displayed codon window, so no later read can range over an unbounded search space.

Read the rows $A,M,T$ next. They name the anchor block, median block, and terminal singleton as the finite closure path consumed by trigger-spectrum clients.

The table and layered rows $R,L$ identify the route as block-optimal and budget-frustrated. They do not change the packet into a global optimizer, because the carrier stores only the displayed values and multiplicities.

Finally $H,C,P,N$ transport, replay, source, and name the same route. The absent host objects listed in the statement are not coordinates of the carrier and cannot be exported by a consumer read.
\end{proof}

\begin{theorem}[Greedy spectral closure sibling trigger route]
\label{thm:greedy-spectral-closure-sibling-trigger-route}
Let $G=(S,A,M,T,R,L,H,C,P,N)$ be an accepted $\GreedySpectralClosureUp$ carrier. Every trigger-spectrum consumer of $G$ first reads the finite-window observer, boolean trigger lattice, window-coverage trigger algebra, and spectral-stabilization sibling rows recorded in $P$ before it consumes the displayed rows
$$
S,A,M,T,R,L,H,C,P,N .
$$
Consequently the packet cannot be consumed as an isolated spectral kernel detached from finite observation, trigger algebra, coverage, or stabilization surfaces.
\end{theorem}
\begin{proof}
Project the provenance row $P$ in \autoref{def:greedy-spectral-closure-carrier}. It names $\FiniteWindowObserverUp$, $\BooleanTriggerLatticeUp$, $\WindowCoverageTriggerAlgebraUp$, and $\SpectralStabilizationUp$ as part of the same accepted packet source.

The finite-window observer fixes the public observation row from which the active support $S$ is read. The boolean trigger lattice and window-coverage trigger algebra then supply the trigger rows against which the anchor, median, and terminal rows $A,M,T$ are interpreted.

Only after those sibling rows are in place can the displayed spectral table and layered-closure rows $R,L$ be consumed. The structural rows $H,C,P,N$ transport, replay, source, and name that route, so no consumer can bypass the sibling finite-window and trigger-algebra surfaces while still reading $G$ as an accepted $\GreedySpectralClosureUp$ packet.
\end{proof}

\falsifiablePrediction{An accepted $\GreedySpectralClosureUp$ trigger-spectrum read is refuted if the anchor block, median block, terminal singleton, displayed spectral table, layered-closure row, transport, replay, provenance, or local naming can be removed while preserving the same packet claim.}

\independenceWitness{finite-window-observer, boolean-trigger-lattice, window-coverage-trigger-algebra, spectral-stabilization, dna-window spectral closure vision: none is bijective with $\GreedySpectralClosureUp$ because the packet jointly carries support, anchor, median, singleton, spectral table, layered closure, transport, replay, provenance, and local naming.}

\closureat{GreedySpectralClosureUp}{seedStr}

\begin{closurestatus}{\GreedySpectralClosureUp}
  \constructivestory{A $\GreedySpectralClosureUp$ packet is generated from active support rows, anchor-block rows, median-block rows, terminal singleton rows, displayed spectral table rows, layered-closure rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:greedy-spectral-closure-carrier}, \autoref{thm:greedy-spectral-closure-namecert-obligations}, and \autoref{thm:greedy-spectral-closure-trigger-nonescape} seed the finite trigger-spectrum closure packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim biological-code truth, stochastic evolutionary dynamics, ambient powerset closure, quotient codon equality, hidden spectral computation, unbounded optimization, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, trigger-spectrum exactness, displayed-table stability, Nontrivial and FieldFaithful instances, and trigger nonescape for BEDC.Derived.GreedySpectralClosureUp.}
\end{closurestatus}
