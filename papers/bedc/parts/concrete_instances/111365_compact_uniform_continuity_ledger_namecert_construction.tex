\chapter{A Concrete Naming Certificate for $\CompactUniformContinuityLedgerUp$}
\label{ch:concrete-instances-compact-uniform-continuity-ledger-namecert}
\origin{human}

Following Bishop and Bridges on compact metric uniform continuity, the $\CompactUniformContinuityLedgerUp$ packet records the finite NameCert ledger that assembles compact finite-net data, pointwise continuity rows, metric distance observations, rational lower-bound folds, and the final uniform-modulus output. It sits over the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous and continuous-map rows of \autoref{ch:concrete-instances-continuous-namecert} and \autoref{ch:concrete-instances-continuousmap-namecert}, the metric distance surface of \autoref{ch:concrete-instances-metric-namecert}, the rational ledger of \autoref{ch:concrete-instances-rat-namecert}, the total-order selector route of \autoref{ch:concrete-instances-totalorder-namecert}, the finite-net fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, and the $\UniformModulusUp$ consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names the full compactness-implies-uniform-continuity ledger as finite BEDC data; it is not an open-cover theorem, a host minimum, a selected subcover, or an ambient choice principle.

\begin{definition}[Compact uniform continuity ledger carrier]
\label{def:compact-uniform-continuity-ledger-carrier}
A $\CompactUniformContinuityLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(K,F,M,R,T,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row, including the compact source, the finite $\mathsf{ProbeBundle}$ of centers, and the located coverage witnesses read from the compact metric packet.
\item $F$ is the pointwise modulus row for the continuous map, carrying the graph reads, centerwise source radii, and target-tolerance certificates over the same compact source.
\item $M$ is the metric distance row, recording the source distances from an input point to a displayed center, from a second input point to that center, and the target distances used by the triangle estimate.
\item $R$ is the finite $\RatUp$ lower-bound fold row, read through the $\TotalOrderUp$ selector route over the bundled positive source radii.
\item $T$ is the finite triangle and tolerance ledger tying the lower-bound fold to the source and target metric comparisons.
\item $U$ is the output $\UniformModulusUp$ row, exposing the tolerance-only precision history obtained from $K,F,M,R,T$.
\item $H$ records componentwise $\hsame$ transport for compact finite nets, pointwise modulus rows, metric distance observations, rational folds, triangle ledgers, output rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a compact-uniform-continuity tolerance request through $K,F,M,R,T,U,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformContinuityLedgerUp$, $\CompactUp$, $\CompactMetricUp$, $\ContinuousUp$, $\ContinuousMapUp$, $\MetricUp$, $\RatUp$, $\TotalOrderUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformContinuityLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted ledgers only through these coordinates. It contains no coordinate for an arbitrary open cover, selected subcover, host minimum over points, quotient function equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact uniform continuity ledger NameCert obligations]
\label{thm:compact-uniform-continuity-ledger-namecert-obligations}
For every accepted $\CompactUniformContinuityLedgerUp$ carrier $L=(K,F,M,R,T,U,H,C,P,N)$, the local $\NameCert_{\CompactUniformContinuityLedgerUp}$ surface consists exactly of carrier admission for the compact finite-net row, pointwise modulus row, metric distance row, finite rational lower-bound fold, triangle and tolerance ledger, output uniform-modulus row, componentwise transport, displayed continuation replay, package provenance, and local naming; source compatibility between $K$ and $F$; metric-read compatibility between $M$ and the compact source; fold exactness through $R$ and the $\TotalOrderUp$ selector route; triangle exactness through $T$; uniform-modulus handoff through $U$; and nonescape from arbitrary open covers, selected subcovers, host minima, quotient function equality, countable choice, and ambient real completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:compact-uniform-continuity-ledger-carrier}. The visible rows are exactly the compact finite-net data, pointwise modulus data, metric distance observations, rational fold, triangle ledger, output row, and structural rows.

The source lock is read before any modulus row is consumed. The compact row $K$ and pointwise row $F$ name the same compact metric source, so the finite centers and local radii cannot move to another carrier.

The rational fold is the row $R$. It is computed over the displayed bundled radii through the $\TotalOrderUp$ selector route, and the metric row $M$ supplies only the distances later used by the source and target triangle estimates.

The triangle and tolerance row $T$ binds these reads into the output row $U$. Componentwise transport, continuation replay, package provenance, and local naming then preserve the same ledger, so no excluded open-cover, choice, quotient, host-minimum, or completeness coordinate is available to a consumer.
\end{proof}

\begin{theorem}[Compact uniform continuity ledger finite-fold route]
\label{thm:compact-uniform-continuity-ledger-finite-fold-route}
Every $\UniformModulusUp$ consumer of an accepted $\CompactUniformContinuityLedgerUp$ packet factors through the finite route
$$
\CompactMetricUp,\quad \ContinuousMapUp,\quad \MetricUp,\quad \RatUp,\quad \TotalOrderUp,\quad \UniformModulusUp .
$$
More explicitly, the consumer must first read compact finite-net centers and pointwise modulus rows, then the metric distance observations, then the finite $\RatUp$ lower-bound fold selected by the $\TotalOrderUp$ route, then the triangle and tolerance ledger, and only then the output uniform-modulus row. No compact-uniform-continuity read can bypass this finite fold by using an arbitrary open cover, selected subcover, host minimum, or choice schedule.
\end{theorem}
\begin{proof}
Start with the compact finite-net row $K$ and the pointwise modulus row $F$. They expose the bundled centers, local radii, graph reads, and source compatibility required by the compact-uniform-continuity request.

Next read the metric row $M$ and the rational fold row $R$. The metric row supplies the source and target distances used by the later estimate, while $R$ folds the displayed positive radius family through the $\TotalOrderUp$ selector route. The selected precision is therefore a finite history, not an external minimum.

Finally read $T$ and $U$. The triangle and tolerance ledger transports the selected lower bound into the two source estimates and the target triangle estimate, and the $\UniformModulusUp$ row receives only that tolerance-only precision history.

The rows $H,C,P,N$ keep the route stable under transport, replay, provenance, and naming. Since the carrier has no open-cover, selected-subcover, host-minimum, or choice coordinate, every compact-uniform-continuity consumer factors through the displayed finite fold.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformContinuityLedgerUp$ compact-uniform-continuity read must expose a compact finite-net row, pointwise continuous-map modulus rows, metric distance observations, a finite $\RatUp$ lower-bound fold, a $\TotalOrderUp$ selector route, a triangle and tolerance ledger, a terminal $\UniformModulusUp$ row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{compact, compactmetric, continuous, continuousmap, metric, rat, totalorder, finitenetminimumfold, uniformmodulus: $\CompactUniformContinuityLedgerUp$ is not bijective to any listed sibling because it binds the whole compactness-implies-uniform-continuity route from finite compact coverage through pointwise radii, metric observations, rational folding, triangle estimates, and the terminal UniformModulus handoff.}

\closureat{CompactUniformContinuityLedgerUp}{seedStr}

\begin{closurestatus}{\CompactUniformContinuityLedgerUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-continuity-ledger-carrier}, \autoref{thm:compact-uniform-continuity-ledger-namecert-obligations}, and \autoref{thm:compact-uniform-continuity-ledger-finite-fold-route} seed the finite NameCert ledger for compactness-implies-uniform-continuity over compact finite nets, pointwise modulus rows, metric distance rows, RatUp folds, TotalOrder selector routes, triangle ledgers, UniformModulus handoff, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, host minima, quotient function equality, countable choice, ambient real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source compatibility, metric-read compatibility, rational fold exactness, triangle exactness, uniform-modulus handoff, transport and replay stability, provenance exhaustion, and TasteGate field faithfulness for BEDC.Derived.CompactUniformContinuityLedgerUp.}
  \constructivestory{A $\CompactUniformContinuityLedgerUp$ packet is generated from compact finite-net rows, ContinuousMap pointwise modulus rows, Metric distance observations, RatUp lower-bound folds, TotalOrder selector routes, triangle and tolerance ledgers, UniformModulus output rows, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
