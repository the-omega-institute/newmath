\chapter{A Concrete Naming Certificate for $\PicardIterationCauchyUp$}
\label{ch:concrete-instances-picard-iteration-cauchy-namecert}
\origin{human}

Following Picard and Banach fixed-point iteration, the $\PicardIterationCauchyUp$ packet records the constructive route from a contractive Picard orbit to regular Cauchy completion data. It sits after the finite orbit surface of \autoref{ch:concrete-instances-picard-iteration-namecert}, the contraction endpoint surface of \autoref{ch:concrete-instances-banach-contraction-namecert}, the modulus route of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, the Cauchy completion reflector of \autoref{ch:concrete-instances-cauchycompletionreflector-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names the no-choice completion-neighbour exposed by the Picard residuals; it does not assert ambient $\RealUp$ completeness, select a fixed point, or identify quotient representatives.

\begin{definition}[PicardIterationCauchy carrier]
\label{def:picard-iteration-cauchy-carrier}
A $\PicardIterationCauchyUp$ carrier is a finite $\BHist$ packet
$$
P=(I,B,M,R,F,E,H,C,Q,N)
$$
whose coordinates are exactly:
\begin{enumerate}
\item $I$ is the accepted $\PicardIterationUp$ row displaying the finite orbit, residual-distance ledger, Cauchy-modulus extraction, and regular-sequence handoff.
\item $B$ is the $\BanachContractionUp$ row recording that the orbit is consumed only through the contraction fixed-point route and not through a selected host endpoint.
\item $M$ is the $\CauchyModulusUp$ row extracted from the Picard residual comparisons.
\item $R$ is the $\RegSeqRatUp$ row that exposes the regular rational name generated by $I$ and $M$.
\item $F$ is the $\CauchyCompletionReflectorUp$ row that admits the regular Cauchy name to the completion-facing reflector surface.
\item $E$ is the $\RealUp$ seal boundary available only after the regular handoff and reflector route have been displayed.
\item $H$ records componentwise $\hsame$ transport for $I,B,M,R,F,E$ and for the continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from Picard iteration through contraction residuals, Cauchy modulus extraction, regular rational readback, completion reflection, and the real seal boundary.
\item $Q$ is the $\Pkg$ provenance over $\PicardIterationCauchyUp$, $\PicardIterationUp$, $\BanachContractionUp$, $\CauchyModulusUp$, $\RegSeqRatUp$, $\CauchyCompletionReflectorUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PicardIterationCauchyUp}$ row.
\end{enumerate}
The carrier exposes a constructive completion-neighbour for the Picard orbit. It contains no coordinate for a selected fixed point, a selected limit, quotient completion equality, countable choice, host function extensionality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[PicardIterationCauchy NameCert obligations]
\label{thm:picard-iteration-cauchy-namecert-obligations}
For every accepted $\PicardIterationCauchyUp$ carrier $P=(I,B,M,R,F,E,H,C,Q,N)$, the local $\NameCert_{\PicardIterationCauchyUp}$ surface consists exactly of Picard-iteration admission, Banach-contraction route visibility, Cauchy-modulus exposure, $\RegSeqRatUp$ handoff visibility, $\CauchyCompletionReflectorUp$ admission, $\RealUp$ seal separation, componentwise transport, displayed replay, package provenance, and local naming for the finite packet of \autoref{def:picard-iteration-cauchy-carrier}.
\end{theorem}
\begin{proof}
Project the carrier. The mathematical rows $I,B,M,R,F,E$ are the only completion-facing coordinates, while $H,C,Q,N$ are the only structural coordinates.

The Picard and contraction rows are read first. The row $I$ exposes the finite orbit, residual-distance ledger, and Cauchy-modulus handoff of \autoref{ch:concrete-instances-picard-iteration-namecert}; the row $B$ keeps the same orbit inside the constructive Banach route of \autoref{ch:concrete-instances-banach-contraction-namecert}.

Next read the completion-neighbour rows. The modulus row $M$ is the source of the regular handoff $R$, the reflector row $F$ consumes that regular handoff, and the seal row $E$ is downstream of $F$.

Finally $H,C,Q,N$ transport, replay, source, and name exactly these displayed rows. Since the carrier inventory has no selected endpoint, quotient representative, countable-choice schedule, host extensionality row, or ambient real-completeness row, the NameCert surface cannot export one.
\end{proof}

\begin{theorem}[PicardIterationCauchy contraction route]
\label{thm:picard-iteration-cauchy-contraction-route}
Every completion-facing read of an accepted $\PicardIterationCauchyUp$ carrier must pass through the displayed chain
$$
\PicardIterationUp \longmapsto \BanachContractionUp \longmapsto \CauchyModulusUp \longmapsto \RegSeqRatUp \longmapsto \CauchyCompletionReflectorUp \longmapsto \RealUp .
$$
The $\RegSeqRatUp$ row is generated by the Picard residual-modulus route, the reflector row is generated by that regular handoff, and the $\RealUp$ seal is available only after the reflector has been displayed. No consumer may replace this route by a selected fixed point, selected limit, quotient stream, countable-choice schedule, host equality, or ambient real-completeness principle.
\end{theorem}
\begin{proof}
Begin with the Picard row $I$. By \autoref{thm:picard-iteration-cauchy-modulus-chain}, the regular handoff is downstream of the finite orbit ledger, residual-distance row, and Cauchy-modulus row.

Read the Banach row $B$ next. By \autoref{thm:banach-contraction-picard-tail-modulus}, a fixed-point-facing consumer must keep the same Picard tail modulus upstream of the regular rational completion boundary.

Now project $M,R,F,E$ from the carrier. The modulus row $M$ is the only source for $R$, the reflector row $F$ is the only completion-facing consumer of $R$, and $E$ is only a downstream real-name seal.

The structural rows $H,C,Q,N$ preserve this order under transport, replay, provenance, and local naming. Because no excluded object is a coordinate of the carrier, the displayed chain is the whole completion-neighbour route.
\end{proof}

\falsifiablePrediction{Every accepted $\PicardIterationCauchyUp$ read factors through one PicardIteration row, one BanachContraction row, one CauchyModulus row, one RegSeqRat handoff, one CauchyCompletionReflector row, one RealUp seal boundary, hsame transport, Cont replay, Pkg provenance, and NameCert row before any completion-facing consumer reads a Picard endpoint.}

\independenceWitness{picard-iteration, banach-contraction, cauchymodulus, regseqrat, cauchycompletionreflector, real: none is bijective with $\PicardIterationCauchyUp$ because the packet jointly stores the Picard orbit, contraction route, residual modulus, regular handoff, completion reflector, real seal boundary, transport, replay, provenance, and local naming.}

\closureat{PicardIterationCauchyUp}{seedStr}

\begin{closurestatus}{\PicardIterationCauchyUp}
  \constructivestory{A $\PicardIterationCauchyUp$ packet is generated from a PicardIteration row, a BanachContraction row, a CauchyModulus row, a RegSeqRat handoff, a CauchyCompletionReflector row, a RealUp seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:picard-iteration-cauchy-carrier}, \autoref{thm:picard-iteration-cauchy-namecert-obligations}, and \autoref{thm:picard-iteration-cauchy-contraction-route} seed the constructive route from contractive Picard iteration to regular Cauchy completion-neighbour data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim ambient $\RealUp$ completeness, selected fixed points, selected limits, quotient completion equality, countable choice, host equality, host function extensionality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure asks for checked Picard-iteration admission, Banach-contraction route visibility, Cauchy-modulus exposure, RegSeqRat handoff visibility, CauchyCompletionReflector admission, Real seal separation, transport, replay, provenance, and local naming.}
\end{closurestatus}
