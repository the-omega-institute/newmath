\chapter{A Concrete Naming Certificate for $\PoissonKernelUp$}
\label{ch:concrete-instances-poisson-kernel-namecert}
\origin{human}

Following Poisson and classical Fourier analysis, the $\PoissonKernelUp$ packet records the finite positive harmonic-analysis kernel by which a displayed radius row and angle-window row are read before any boundary or Abel-type consumer may compare it with Fejer and Dirichlet kernels. It sits beside the positive averaging surface $\FejerKernelUp$ of \autoref{ch:concrete-instances-fejer-kernel-namecert}, the partial-sum surface $\DirichletKernelUp$ of \autoref{ch:concrete-instances-dirichlet-kernel-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational sequence route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the dyadic arithmetic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter names a finite positive kernel boundary; it does not import host harmonic functions, quotient function equality, analytic completion, countable choice, or a selected boundary limit.

\begin{definition}[PoissonKernel carrier]\label{def:poisson-kernel-carrier}
A $\PoissonKernelUp$ carrier is a finite $\BHist$ packet
$$
K=(R,A,D,M,V,F,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the radius row, recording the displayed rational or dyadic radius request together with the comparison ledger asserting it lies inside the finite admissible radial window.
\item $A$ is the angle-window row, recording the finite circle or phase samples at which the kernel is inspected.
\item $D$ is the denominator row, recording the displayed expression $1-2r\cos(\theta)+r^{2}$ through finite real, regular-rational, and dyadic comparison rows.
\item $M$ is the numerator row, recording the displayed expression $1-r^{2}$ with the same radius ledger as $R$.
\item $V$ is the kernel-value readback row, exposing only the finite quotient read determined by $M,D,A,R$.
\item $F$ is the $\FejerKernelUp$ comparison row, recording the positive-kernel averaging corridor through which Fejer-facing consumers may compare displayed finite windows.
\item $S$ is the $\DirichletKernelUp$ comparison row, recording the partial-sum corridor through which Dirichlet-facing consumers may compare displayed finite windows.
\item $H$ records componentwise $\hsame$ transport for the radius, angle-window, denominator, numerator, value, comparison, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a boundary request through $R,A,D,M,V,F,S$ in that order.
\item $P$ is the $\Pkg$ provenance over $\PoissonKernelUp$, $\FejerKernelUp$, $\DirichletKernelUp$, $\RealUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PoissonKernelUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,A,D,M,V,F,S,H,C,P,N$. It has no coordinate for host harmonic-function equality, analytic completion, quotient boundary functions, countable choice, or a selected radial limit.
\end{definition}

\begin{theorem}[PoissonKernel NameCert obligations]\label{thm:poisson-kernel-namecert-obligations}
For every accepted $\PoissonKernelUp$ carrier $K=(R,A,D,M,V,F,S,H,C,P,N)$, the local $\NameCert_{\PoissonKernelUp}$ surface consists exactly of carrier admission for the radius row, angle-window row, denominator row, numerator row, kernel-value readback, Fejer comparison row, Dirichlet comparison row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every public kernel read must first expose the same radius and angle rows, then the denominator and numerator ledgers, then the value readback, and only then the Fejer or Dirichlet comparison route. No Poisson-kernel obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:poisson-kernel-carrier}. The displayed tuple exposes exactly the radius, angle-window, denominator, numerator, value, Fejer comparison, Dirichlet comparison, transport, replay, provenance, and local naming rows.

The value route is finite because $V$ is read only after $R,A,D,M$ have been displayed. The denominator and numerator ledgers share the radius row, so a consumer cannot replace the radial budget while preserving the accepted packet.

The comparison rows are downstream. Fejer-facing reads pass through $F$, Dirichlet-facing reads pass through $S$, and both routes inherit the same radius, angle, numerator, denominator, and value rows before any handoff is exposed.

Finally, $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier has no coordinate for host harmonic functions, analytic completion, quotient boundary functions, countable choice, or selected radial limits, the refusal clause is exhausted by the carrier inventory.
\end{proof}

\begin{theorem}[PoissonKernel Fejer and Dirichlet handoff]\label{thm:poisson-kernel-fejer-dirichlet-handoff}
Every accepted $\PoissonKernelUp$ comparison read factors through the finite route
$$
\RealUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \FejerKernelUp,\ \DirichletKernelUp .
$$
The radius and angle rows determine the displayed numerator, denominator, and kernel-value readback before either the positive Fejer corridor or the partial-sum Dirichlet corridor is exposed. The route exports no host boundary convergence theorem, analytic completion, quotient function equality, countable-choice selector, or selected limiting harmonic extension.
\end{theorem}
\begin{proof}
Let $K=(R,A,D,M,V,F,S,H,C,P,N)$ be accepted. Carrier projection exposes the real, regular-rational, and dyadic arithmetic rows through $R,A,D,M,V$ before any comparison row can be inspected.

Replay $C$. A comparison request first reads the radius row and angle window, then the denominator and numerator ledgers, and then the kernel-value readback. Thus both comparison routes receive the same finite kernel value rather than a host function.

The Fejer handoff is exactly $F$, and the Dirichlet handoff is exactly $S$. Componentwise transport through $H$ preserves the same displayed rows, while $P,N$ record provenance and local naming. The excluded analytic and choice-theoretic objects are absent from the carrier, so no comparison read can export them.
\end{proof}

\falsifiablePrediction{Every accepted $\PoissonKernelUp$ boundary-kernel read factors through a radius row, angle-window row, denominator row, numerator row, kernel-value readback, Fejer comparison row, Dirichlet comparison row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{fejer-kernel, dirichlet-kernel, real, regseqrat, dyadicratcore: $\PoissonKernelUp$ is not bijective to any listed sibling because it jointly carries a radius row, angle-window row, denominator ledger, numerator ledger, kernel-value readback, Fejer comparison, Dirichlet comparison, transport, replay, provenance, and local naming.}

\closureat{PoissonKernelUp}{seedStr}

\begin{closurestatus}{\PoissonKernelUp}
  \constructivestory{A $\PoissonKernelUp$ packet is generated from a radius row, angle-window row, denominator row, numerator row, kernel-value readback, Fejer and Dirichlet comparison rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poisson-kernel-carrier}, \autoref{thm:poisson-kernel-namecert-obligations}, and \autoref{thm:poisson-kernel-fejer-dirichlet-handoff} seed the finite positive Poisson-kernel surface for harmonic-analysis consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host harmonic-function equality, analytic completion, quotient boundary functions, countable choice, selected radial limits, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, radius exactness, denominator and numerator stability, value readback, Fejer and Dirichlet handoff, transport, replay, provenance, local naming, Nontrivial PoissonKernelUp, and FieldFaithful PoissonKernelUp for BEDC.Derived.PoissonKernelUp.}
\end{closurestatus}
