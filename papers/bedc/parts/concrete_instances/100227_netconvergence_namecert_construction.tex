\chapter{A Concrete Naming Certificate for $\NetConvergenceUp$}
\label{ch:concrete-instances-netconvergence-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley-style convergence of nets, the $\NetConvergenceUp$ packet names only the finite directed-tail evidence a completion-facing consumer can inspect. It sits between the Moore--Smith net carrier of \autoref{ch:concrete-instances-moore-smith-net-namecert}, the directed-window surface of \autoref{ch:concrete-instances-cauchynet-namecert}, the finite filterbase handoff of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the entourage discipline of \autoref{ch:concrete-instances-uniformspace-namecert}, and the L10 real-and-completion route of \autoref{sec:roadmap-l10-construction-order}. The Moore--Smith net row supplies the directed-tail and value-window route that this convergence interface consumes before filter and Real handoff rows are read. The packet is a convergence interface for Real and completion arguments; it is not a selected limit, a quotient of nets, or an ambient completeness theorem.

\begin{definition}[Net convergence carrier]
\label{def:net-convergence-carrier}
A $\NetConvergenceUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
N=(D,T,E,A,F,S,R,L,H,C,P,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the directed-index window, naming the finite index vertices, the displayed upper-window witnesses, and the finite tail cut used by every convergence read.
\item $T$ is the tail-neighbourhood ledger, recording the neighbourhood requests whose tails are inspected inside $D$.
\item $E$ is the $\UniformSpaceUp$ entourage row, giving the finite entourage request through which the tail-neighbourhood read is interpreted.
\item $A$ is an optional $\CauchyNetUp$ source row; when present, its common-window classifier is read only through the same $D,T,E$ rows, and when absent the packet records no Cauchy-net source.
\item $F$ is the $\CauchyFilterUp$ handoff row obtained from the displayed tail-neighbourhood family.
\item $S$ is the $\StreamNameUp$ schedule naming the finite observation windows used by the directed tail.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from those scheduled observations and dyadic comparisons.
\item $L$ is the terminal $\RealUp$ seal row exposed only after $D,T,E,F,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the directed window, tail-neighbourhood ledger, entourage row, optional source row, filter handoff, stream schedule, regular-rational readback, Real seal, provenance, and local naming rows.
\item $C$ records the displayed $\Cont$ replay for every consumer read through the same coordinates.
\item $P$ is the $\Pkg$ provenance over $\NetConvergenceUp$, $\CauchyNetUp$, $\CauchyFilterUp$, $\UniformSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, and $\NameCert$.
\item $M$ is the local $\NameCert_{\NetConvergenceUp}$ row.
\end{enumerate}
The carrier exports only these finite rows. It contains no selected limit point, quotient net, countable-choice selector, or ambient completeness witness.
\end{definition}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Net convergence NameCert obligations]
\label{thm:net-convergence-namecert-obligations}
For an accepted carrier $N=(D,T,E,A,F,S,R,L,H,C,P,M)$ of \autoref{def:net-convergence-carrier}, the local $\NameCert_{\NetConvergenceUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the directed-index window, tail-neighbourhood ledger, uniform-entourage row, optional CauchyNet source row, CauchyFilter handoff row, StreamName schedule, RegSeqRat readback, Real seal boundary, and structural rows;
\item tail classification, stating that every convergence read names a displayed tail in $D$ before it can consume a neighbourhood row in $T$;
\item entourage interpretation, stating that the neighbourhood read is routed through the finite $\UniformSpaceUp$ row $E$ rather than a host topology or ambient uniformity;
\item optional-source discipline, stating that a present $\CauchyNetUp$ row is read only through the same directed window and that an absent source row exports no hidden Cauchy-net data;
\item CauchyFilter handoff, stating that the filter-facing consumer sees only the tail-neighbourhood family and refinement rows displayed by $F$;
\item Real seal non-escape, stating that $L$ is consumed only after the stream schedule $S$ and regular-rational readback $R$ have been displayed;
\item componentwise transport, continuation replay, package provenance, and local naming through $H,C,P,M$.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceNameCert\_obligations}
\begin{proof}
Project the accepted packet of \autoref{def:net-convergence-carrier}. The analytic coordinates are $D,T,E,A,F,S,R,L$, and the structural coordinates are $H,C,P,M$.

A convergence read first names a directed tail in $D$. The tail-neighbourhood ledger $T$ can then be inspected only at that displayed tail, and the neighbourhood is interpreted by the finite entourage row $E$.

The optional source row does not add a second convergence surface. If $A$ is present, its common-window classifier is read through the same directed window and entourage data; if $A$ is absent, there is no coordinate from which source-net data can be exported.

The downstream routes are likewise fixed by visible rows. The Cauchy-filter consumer reads $F$, while Real-facing consumers read $S$ and $R$ before reaching the seal $L$. Transport, replay, provenance, and naming are componentwise through $H,C,P,M$, so the listed rows exhaust the local naming surface.
\end{proof}

\begin{theorem}[Moore--Smith dependency for net convergence]
\label{thm:net-convergence-moore-smith-dependency}
Every $\NetConvergenceUp$ consumer that is sourced from a $\MooreSmithNetUp$ packet reads the Moore--Smith directed-index row, value-window row, and finite tail ledger before it reads the $\CauchyFilterUp$ handoff row or the $\RealUp$ seal row. The dependency is confined to displayed finite directed-tail evidence and does not introduce selected subnets, cofinal choice, quotient nets, or ambient completeness.
\end{theorem}
\begin{proof}
Use the Moore--Smith net obligations of \autoref{thm:moore-smith-net-namecert-obligations}. They expose directed indices, value windows, and tail requests as the finite packet consumed by convergence-side readers.

Now apply the NetConvergence carrier discipline of \autoref{def:net-convergence-carrier}. The optional source row $A$ can contribute source-net material only through the same directed window, tail ledger, and entourage rows $D,T,E$.

The downstream rows are read after that directed-tail route. The $\CauchyFilterUp$ handoff row $F$ and the $\RealUp$ seal row $L$ therefore consume displayed Moore--Smith evidence only through the accepted $\NetConvergenceUp$ packet, leaving no coordinate for a selected subnet, cofinal-choice operator, quotient net, or ambient completeness theorem.
\end{proof}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceCarrier\_moore\_smith\_dependency}

\begin{theorem}[Net convergence CauchyFilter handoff]
\label{thm:net-convergence-cauchy-filter-handoff}
Every $\CauchyFilterUp$ handoff exported by an accepted $\NetConvergenceUp$ carrier factors through the displayed route
$$
D\longmapsto T\longmapsto E\longmapsto F
$$
and every $\RealUp$ read after that handoff factors through the same $F$ row together with the displayed $\StreamNameUp$ schedule $S$ and $\RegSeqRatUp$ readback $R$. No selected limit, quotient net, countable-choice selector, or ambient completeness row can alter the handoff.
\end{theorem}
\begin{proof}
Start from \autoref{thm:net-convergence-namecert-obligations}. The tail classification obligation places every convergence read on a displayed directed tail in $D$, and the tail-neighbourhood ledger $T$ supplies the finite neighbourhood family inspected at that tail.

The entourage interpretation obligation routes that neighbourhood family through the uniform row $E$. Thus the filter-facing datum is not a host filter; it is exactly the finite family that the handoff row $F$ exposes to $\CauchyFilterUp$ consumers.

For a Real-facing read, the packet must continue through $S$ and $R$ before reaching $L$. These rows are part of the same accepted carrier and are transported only by the displayed $\hsame$ and $\Cont$ data in $H$ and $C$.

There is no remaining coordinate in $N$ for a selected limit point, a quotient of nets, a countable-choice selector, or an ambient completeness theorem. Consequently the handoff is determined by the displayed route through $D,T,E,F,S,R,L$.
\end{proof}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceCarrier\_cauchy\_filter\_handoff}

\begin{theorem}[Net convergence directed-window admission]
\label{thm:net-convergence-directed-window-admission}
Every accepted $\NetConvergenceUp$ convergence read first exposes the directed-index window $D$, the tail-neighbourhood ledger $T$, and the $\UniformSpaceUp$ entourage row $E$ of \autoref{def:net-convergence-carrier}. No CauchyNet source row, CauchyFilter handoff row, StreamName schedule, RegSeqRat readback, or Real seal can be consumed before this directed window has been displayed.
\end{theorem}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceCarrier\_directed\_window\_admission}
\begin{proof}
Use the carrier projection of \autoref{def:net-convergence-carrier}. The rows $D,T,E$ are the only coordinates that name a directed tail, the neighbourhood requests inspected at that tail, and the finite entourage row interpreting those requests.

Apply the tail classification and entourage interpretation clauses of \autoref{thm:net-convergence-namecert-obligations}. They force every convergence read to pass through $D$ and $T$, then through $E$, before any downstream row can be inspected.

The optional source row $A$, the filter handoff $F$, the stream and rational readback rows $S,R$, and the Real seal $L$ are downstream coordinates. Since the carrier has no alternative directed-tail coordinate, they cannot be consumed before the displayed directed window is exposed.
\end{proof}

\begin{theorem}[Net convergence CauchyNetLimit handoff]
\label{thm:net-convergence-cauchy-net-limit-handoff}
Every CauchyNetLimit-facing consumer of an accepted $\NetConvergenceUp$ carrier receives the same directed tail $D,T,E$ before it can inspect the optional $\CauchyNetUp$ row $A$ or the terminal $\RealUp$ seal row $L$. The handoff is therefore a finite directed-tail route rather than a selected limit, host convergence predicate, quotient net, or ambient completeness witness.
\end{theorem}
\begin{proof}
Start from \autoref{thm:net-convergence-directed-window-admission}. It places $D,T,E$ before every convergence read, independently of which downstream consumer is asking for the data.

A CauchyNetLimit-facing consumer can use the optional CauchyNet source only through $A$. By the optional-source discipline of \autoref{thm:net-convergence-namecert-obligations}, a present $A$ is read through the same directed window, and an absent $A$ exports no source-net data.

The Real-facing part of the handoff reaches $L$ only after the stream schedule $S$ and the regular-rational readback $R$ have been displayed. Thus the consumer receives one finite directed-tail route before either the CauchyNet source or the Real seal is consumed.
\end{proof}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceCarrier\_cauchy\_net\_limit\_handoff}

\begin{theorem}[Net convergence Moore--Smith filter boundary]
\label{thm:net-convergence-moore-smith-filter-boundary}
Moore--Smith consumers and $\CauchyFilterUp$ consumers of an accepted $\NetConvergenceUp$ carrier meet only through the displayed finite directed-tail evidence $D,T,E,F$ and the structural rows $H,C,P,M$. The boundary exports no selected subnet, cofinal-choice operator, quotient net, host filter equality, or ambient completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceCarrier\_moore\_smith\_filter\_boundary}
\begin{proof}
Read the Moore--Smith dependency theorem \autoref{thm:net-convergence-moore-smith-dependency}. It says that source-net consumers contribute directed-index, value-window, and tail-ledger evidence through the finite directed-tail route.

Read the CauchyFilter handoff theorem \autoref{thm:net-convergence-cauchy-filter-handoff}. It routes every filter-facing datum through $D,T,E,F$ before any Real-facing rows are inspected.

The structural rows $H,C,P,M$ transport, replay, package, and locally name that same route. Since neither theorem supplies a coordinate for selected subnets, cofinal choice, quotient nets, host filter equality, or ambient completeness, the Moore--Smith and filter consumers meet only at the displayed finite boundary.
\end{proof}

\begin{theorem}[Net convergence filterbase finality]
\label{thm:net-convergence-filterbase-finality}
Every $\FilterBaseUp$ or $\CauchyFilterUp$ consumer of an accepted $\NetConvergenceUp$ carrier factors through the displayed directed tail $D,T,E$ and the handoff row $F$. No host filter equality, selected limit, quotient net, ambient completeness witness, or alternate filter coordinate can refine that handoff beyond the finite route already displayed by the carrier.
\end{theorem}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceCarrier\_filterbase\_finality}
\begin{proof}
Project the carrier of \autoref{def:net-convergence-carrier}. The rows $D,T,E$ are the directed-index window, tail-neighbourhood ledger, and entourage row, while $F$ is the only row that names the filter-facing handoff.

Apply \autoref{thm:net-convergence-cauchy-filter-handoff}. It sends every CauchyFilter-facing read through $D,T,E,F$ before any Real-facing rows can contribute. Apply also \autoref{thm:net-convergence-moore-smith-filter-boundary}, which identifies the Moore--Smith and filter boundary with the same displayed rows.

The carrier has no second filterbase coordinate, no selected limit coordinate, and no quotient-net coordinate. Thus every filterbase or CauchyFilter consumer factors through the displayed handoff, and that handoff is final among the routes accepted by the packet.
\end{proof}

\begin{theorem}[Net convergence Real seal nonescape]
\label{thm:net-convergence-real-seal-nonescape}
Let $N=(D,T,E,A,F,S,R,L,H,C,P,M)$ be an accepted $\NetConvergenceUp$ carrier. The terminal $\RealUp$ seal $L$ is reachable only after the directed window $D$, tail-neighbourhood ledger $T$, entourage row $E$, CauchyFilter handoff $F$, StreamName schedule $S$, and RegSeqRat readback $R$ have been displayed. The seal cannot be selected as a host limit, quotient-net representative, ambient convergence witness, or completeness object.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:net-convergence-carrier}. The rows preceding the Real seal are exactly the directed-tail rows $D,T,E$, the filter-facing row $F$, and the Real-facing readback rows $S,R$.

Use \autoref{thm:net-convergence-namecert-obligations}. Its carrier admission and Real seal non-escape clauses force $L$ to be read only after the displayed analytic rows have appeared.

Use also \autoref{thm:net-convergence-directed-window-admission}, \autoref{thm:net-convergence-cauchy-filter-handoff}, and \autoref{thm:net-convergence-cauchy-net-limit-handoff}. Together they leave no route from a host limit, quotient net, ambient convergence predicate, or completeness theorem into the seal coordinate.
\end{proof}

\begin{theorem}[Net convergence entourage-tail stability]
\label{thm:net-convergence-entourage-tail-stability}
Let $N=(D,T,E,A,F,S,R,L,H,C,P,M)$ and $N'=(D',T',E',A',F',S',R',L',H',C',P',M')$ be accepted $\NetConvergenceUp$ carriers related by the componentwise $\hsame$ transport recorded by their structural rows. Transport of $D,T,E$ preserves the same tail-neighbourhood and entourage route for every $\CauchyFilterUp$ consumer, so the handoff remains a read through $D,T,E,F$ rather than a new host filter or ambient uniformity.
\end{theorem}
\begin{proof}
Read the structural transport row in \autoref{def:net-convergence-carrier}. It transports the directed window, tail-neighbourhood ledger, entourage row, optional source row, filter handoff, stream schedule, readback, seal, provenance, and local naming component by component.

Apply \autoref{thm:net-convergence-namecert-obligations} and \autoref{thm:net-convergence-directed-window-admission}. A convergence read must first expose $D,T,E$, and the transported carrier exposes the corresponding $D',T',E'$ through the same structural coordinate.

Finally apply \autoref{thm:net-convergence-cauchy-filter-handoff} and \autoref{thm:net-convergence-cauchy-net-limit-handoff}. Since every filter-facing read is already routed through the directed tail and entourage rows before $F$ is consumed, componentwise $\hsame$ transport preserves the route and adds no host filter equality or ambient uniformity coordinate.
\end{proof}

\begin{theorem}[Net convergence stream readback route]
\label{thm:net-convergence-stream-readback-route}
Let $N=(D,T,E,A,F,S,R,L,H,C,P,M)$ be an accepted $\NetConvergenceUp$ carrier. After the CauchyFilter handoff row $F$ has been displayed, the only Real-facing route available to the packet is the $\StreamNameUp$ schedule $S$ followed by the $\RegSeqRatUp$ readback $R$ and the terminal seal $L$. No alternate real-name route, selected limit, quotient-net representative, or ambient completeness row is exported.
\end{theorem}
\leanchecked{BEDC.Derived.NetConvergenceUp.NetConvergenceCarrier\_stream\_readback\_route}
\begin{proof}
Start with the carrier projection of \autoref{def:net-convergence-carrier}. The rows downstream of $F$ on the Real-facing side are $S,R,L$, together with the structural rows $H,C,P,M$.

Apply \autoref{thm:net-convergence-namecert-obligations}. The listed obligations include the CauchyFilter handoff, Real seal non-escape, componentwise transport, replay, provenance, and local naming; none of them introduces a second real-name coordinate.

Apply \autoref{thm:net-convergence-directed-window-admission}, \autoref{thm:net-convergence-cauchy-filter-handoff}, and \autoref{thm:net-convergence-cauchy-net-limit-handoff}. They force the route through $D,T,E,F$ before any Real-facing read and then through $S,R,L$, so the stream schedule and regular readback are the only Real-facing route after the handoff.
\end{proof}

\begin{theorem}[Net convergence tail-window admission]
\label{thm:netconvergence-tail-window-admission}
Let $N=(D,T,E,A,F,S,R,L,H,C,P,M)$ be an accepted $\NetConvergenceUp$ carrier. Every accepted convergence read first exposes a directed tail window in $D$ together with the tail-neighbourhood request recorded in $T$ and the entourage interpretation row $E$. Only after that route is displayed may the packet read a present $\CauchyNetUp$ row $A$, the $\CauchyFilterUp$ handoff $F$, or the Real-facing schedule $S,R,L$.
\end{theorem}
\begin{proof}
Read the carrier rows of \autoref{def:net-convergence-carrier}. The only coordinates that name a directed tail and interpret a neighbourhood request are $D,T,E$.

Apply the tail classification and entourage interpretation clauses of \autoref{thm:net-convergence-namecert-obligations}. They put every convergence read on the displayed directed-tail route before the optional source row, filter handoff, stream schedule, regular-rational readback, or Real seal can be consumed.

The downstream rows therefore depend on the tail-window admission route. Since the carrier has no second directed-tail coordinate, no consumer can bypass $D,T,E$ by reading a host convergence predicate or a cofinal-choice operator.
\end{proof}

\begin{theorem}[Net convergence boundary readback]
\label{thm:netconvergence-boundary-readback}
For an accepted $\NetConvergenceUp$ carrier $N=(D,T,E,A,F,S,R,L,H,C,P,M)$, every Cauchy-facing read and every Real-facing read meet at the displayed boundary
$$
D,T,E
\longmapsto
F
\longmapsto
S,R,L .
$$
The $\CauchyFilterUp$ and $\CauchyNetUp$ handoff rows may be read only through the same directed tail, and the $\RealUp$ seal is read only after the $\RegSeqRatUp$ boundary row has been displayed.
\end{theorem}
\begin{proof}
Start with the Cauchy-facing side. By \autoref{thm:net-convergence-cauchy-filter-handoff}, the filter handoff $F$ is reached through $D,T,E$, and by \autoref{thm:net-convergence-cauchy-net-limit-handoff}, a present source-net row $A$ is read through that same directed route.

Now read the Real-facing side. The stream readback theorem \autoref{thm:net-convergence-stream-readback-route} forces every Real read after $F$ through $S$ and $R$ before it reaches the terminal seal $L$.

Thus the boundary is visible on both sides: directed tails supply the Cauchy handoff, and regular-rational readback supplies the Real seal. The packet does not expose a selected limit, quotient net, host convergence predicate, or ambient completeness row that could replace this boundary.
\end{proof}

\begin{theorem}[Net convergence ledger nonescape]
\label{thm:netconvergence-ledger-nonescape}
Every consumer of an accepted $\NetConvergenceUp$ packet factors through the finite ledger rows $D,T,E,A,F,S,R,L,H,C,P,M$. The packet refuses quotient nets, selected limits, cofinal-choice operators, host filter equality, ambient convergence predicates, and ambient completeness; none of these objects can be recovered from transport, replay, provenance, or local naming.
\end{theorem}
\begin{proof}
Project the accepted packet. Its analytic coordinates are the directed window, tail ledger, entourage row, optional source row, filter handoff, stream schedule, regular-rational readback, and Real seal; its structural coordinates are transport, replay, provenance, and local naming.

Apply \autoref{thm:netconvergence-tail-window-admission} and \autoref{thm:netconvergence-boundary-readback}. Together they place every accepted read on the finite directed-tail and Cauchy-to-Real boundary route.

The structural rows cannot add analytic content. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve only the listed coordinates, while $P$ and $M$ source and name only the same packet. Hence quotient nets, selected limits, cofinal choice, host filter equality, ambient convergence, and completeness remain outside the certificate.
\end{proof}

\begin{closurestatus}{\NetConvergenceUp}
  \constructivestory{A $\NetConvergenceUp$ packet is generated from a directed-index window, a tail-neighbourhood ledger, a UniformSpace entourage row, an optional CauchyNet source row, a CauchyFilter handoff row, StreamName windows, RegSeqRat readback, a Real seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:net-convergence-carrier}, \autoref{thm:net-convergence-namecert-obligations}, and \autoref{thm:net-convergence-cauchy-filter-handoff} seed the finite directed-tail convergence boundary for the Real-and-completion corridor.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected limits, quotient nets, countable choice, ambient completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, tail classification, entourage interpretation, optional-source discipline, CauchyFilter handoff, Real seal non-escape, package provenance, and local naming for the displayed convergence packet.}
\end{closurestatus}
