\chapter{A Concrete Naming Certificate for $\CompactUniformTailSelectorUp$}
\label{ch:concrete-instances-compact-uniform-tail-selector-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactUniformTailSelectorUp$ packet records the finite selector surface that turns compact finite-net data and pointwise modulus ledgers into a tolerance-only uniform tail ledger. It is grounded in the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity}, the compact-net packets of \autoref{ch:concrete-instances-compact-uniform-finite-net-namecert} and \autoref{ch:concrete-instances-compact-net-modulus-stability-namecert}, the continuous-map route of \autoref{ch:concrete-instances-continuousmap-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite $\mathsf{ProbeBundle}$ tail-radius handoff, not a global choice function, open-cover compactness theorem, or ambient completeness principle.

\begin{definition}[CompactUniformTailSelector carrier]
\label{def:compact-uniform-tail-selector-carrier}
A $\CompactUniformTailSelectorUp$ carrier is a finite $\BHist$ packet
$$
T=(K,F,Pb,L,D,U,R,E,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row, exposing only the inspected $\mathsf{ProbeBundle}$ of centers and coverage witnesses.
\item $F$ is the continuous-map row whose pointwise modulus entries are being folded.
\item $Pb$ is the finite $\mathsf{ProbeBundle}$ spine used by the selector.
\item $L$ is the local tail-radius ledger, one displayed radius and pointwise-modulus certificate for each bundled probe.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare the local radii and the requested output tolerance.
\item $U$ is the tolerance-only uniform tail threshold selected by finite fold over $Pb,L,D$.
\item $R$ is the $\RegSeqRatUp$ readback row for the selected threshold and the inspected finite net.
\item $E$ is the $\RealUp$ seal row reached only after $K,F,Pb,L,D,U,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact nets, pointwise modulus rows, bundle spines, local radii, dyadic tolerances, selected thresholds, readbacks, seals, provenance, and names.
\item $C$ records displayed $\Cont$ replay from compact finite-net rows and pointwise moduli to $U$, and from $U$ to $R,E$.
\item $Q$ is the $\Pkg$ provenance over $\CompactUniformTailSelectorUp$, $\CompactUniformFiniteNetUp$, $\CompactNetModulusStabilityUp$, $\ContinuousMapUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformTailSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a global point selector, arbitrary open cover, selected minimum outside the bundle fold, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CompactUniformTailSelector NameCert obligations]
\label{thm:compact-uniform-tail-selector-namecert-obligations}
For every accepted $\CompactUniformTailSelectorUp$ carrier $T=(K,F,Pb,L,D,U,R,E,H,C,Q,N)$, the local $\NameCert_{\CompactUniformTailSelectorUp}$ surface consists exactly of carrier admission for the displayed packet; compact finite-net exposure through $K$; continuous-map pointwise-modulus exposure through $F$; $\mathsf{ProbeBundle}$ spine exposure through $Pb$; local tail-radius exposure through $L$; dyadic tolerance comparison through $D$; uniform threshold exposure through $U$; $\RegSeqRatUp$ readback through $R$; $\RealUp$ sealing through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; provenance through $Q$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier rows from \autoref{def:compact-uniform-tail-selector-carrier}. The accepted packet exposes compact finite-net rows, pointwise-modulus rows, a finite bundle spine, local tail radii, dyadic tolerances, a selected uniform threshold, regular readback, Real seal, and structural rows.

The finite fold is internal to the displayed $\mathsf{ProbeBundle}$. It reads every bundled local radius and keeps the threshold row $U$ as a certificate over the same finite carrier.

The structural rows preserve this route under transport, replay, provenance, and local naming. Since no row supplies a global choice function, arbitrary open-cover compactness, quotient equality, selected external minimum, countable choice, or Real completeness, the listed obligations exhaust the NameCert surface.
\end{proof}

\begin{theorem}[CompactUniformTailSelector finite-net handoff]
\label{thm:compact-uniform-tail-selector-finite-net-handoff}
Every accepted $\CompactUniformTailSelectorUp$ packet hands compact finite-net data to the uniform selector only through the finite route
$$
K,F,Pb,L,D\longmapsto U.
$$
The handoff reads the $\mathsf{ProbeBundle}$ spine and pointwise modulus rows before producing a tolerance-only tail threshold, and it does not choose points, subcovers, or radii outside the displayed bundle.
\end{theorem}
\begin{proof}
Begin with the compact finite-net row $K$ and the pointwise modulus row $F$. These rows determine the inspected centers, local radius certificates, and coverage witnesses available to the selector.

Fold over the finite bundle spine $Pb$. Each local tail-radius row in $L$ is compared through the dyadic tolerance row $D$, and the fold exports only the threshold row $U$.

The transport and continuation rows preserve the same finite handoff. The packet therefore cannot import a global point selector, selected subcover, external minimum, quotient real equality, or completeness row.
\end{proof}

\begin{theorem}[CompactUniformTailSelector finite-net route]
\label{thm:compact-uniform-tail-selector-finite-net-route}
Every accepted $\CompactUniformTailSelectorUp$ packet receives compact finite-net data through $\CompactUniformFiniteNetUp$ and $\CompactNetModulusStabilityUp$ before the $\mathsf{ProbeBundle}$ spine, local radius ledger, $\DyadicRatCoreUp$ tolerance row, $\RegSeqRatUp$ readback, and $\RealUp$ seal are visible. For a carrier
$$
T=(K,F,Pb,L,D,U,R,E,H,C,Q,N),
$$
the route projects $K,F,Pb,L,D,U,R,E$ in that order and exports no open-cover compactness object, point-choice coordinate, external minimum selector, countable-choice schedule, quotient real equality, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the carrier rows $K,F,Pb,L,D,U,R,E,H,C,Q,N$ from \autoref{def:compact-uniform-tail-selector-carrier}. The rows $K$ and $F$ are the compact finite-net and pointwise-modulus inputs, while $Pb,L,D$ are the finite bundle spine, local radius ledger, and dyadic tolerance ledger inspected by the selector.

Apply \autoref{thm:compact-uniform-tail-selector-finite-net-handoff}. It forces the selector threshold $U$ to be produced only after the compact finite-net row, pointwise modulus row, bundle spine, local radius rows, and dyadic comparisons have been displayed.

Now apply \autoref{thm:compact-uniform-tail-selector-uniform-modulus-route}. The regular readback $R$ and terminal Real seal $E$ are downstream of the selected threshold, so the Real-facing route cannot be read before the finite-net and pointwise-modulus data have passed through the displayed ProbeBundle discipline.

Finally inspect the excluded coordinates in the carrier. Since open-cover compactness, point choice, external minima, countable choice, quotient real equality, and ambient completeness are absent from $K,F,Pb,L,D,U,R,E,H,C,Q,N$, every accepted packet consumes the compact finite-net route directly through $\CompactUniformFiniteNetUp$ and $\CompactNetModulusStabilityUp$ rather than through any external compactness principle.
\end{proof}

\begin{theorem}[CompactUniformTailSelector uniform modulus route]
\label{thm:compact-uniform-tail-selector-uniform-modulus-route}
Every Real-facing or uniform-modulus consumer of an accepted $\CompactUniformTailSelectorUp$ packet must read
$$
K,F,Pb,L,D,U,R,E
$$
in that order before treating the selected threshold as a uniform modulus. The route turns compact finite-net coverage and pointwise moduli into a tolerance-only ledger through $\RegSeqRatUp$ and $\RealUp$, not through open-cover compactness, sequential compactness, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:compact-uniform-tail-selector-finite-net-handoff}. It places the threshold $U$ behind compact finite-net rows, pointwise modulus rows, the finite bundle spine, local radii, and dyadic comparisons.

The readback row $R$ and seal row $E$ are downstream of $U$. A uniform-modulus consumer therefore receives the selected threshold only after regular rational readback and the Real-facing seal have preserved the same finite route.

Finally inspect the excluded rows in \autoref{def:compact-uniform-tail-selector-carrier}. There is no coordinate for an arbitrary open cover, sequential compactness, a global choice function, quotient real equality, selected external minimum, or Real completeness, so the uniform-modulus route remains the displayed finite handoff.
\end{proof}

\falsifiablePrediction{An accepted $\CompactUniformTailSelectorUp$ consumer that reads a tolerance-only uniform tail threshold without the compact finite-net row, pointwise modulus row, finite ProbeBundle spine, local tail-radius ledger, DyadicRatCore tolerance row, RegSeqRat readback row, RealUp seal row, and structural rows refutes the carrier surface.}

\independenceWitness{compact-uniform-finite-net, compact-net-modulus-stability, continuous-map, dyadicratcore, regseqrat, real: no listed sibling supplies the whole selector packet $T=(K,F,Pb,L,D,U,R,E,H,C,Q,N)$, because each sibling contributes only a compact-net row, pointwise-modulus row, dyadic ledger, regular readback, or terminal seal while this chapter binds them into one tolerance-only tail selector.}

\closureat{CompactUniformTailSelectorUp}{seedStr}

\begin{closurestatus}{\CompactUniformTailSelectorUp}
  \constructivestory{A $\CompactUniformTailSelectorUp$ packet is generated from compact finite-net rows, continuous-map pointwise modulus rows, a finite ProbeBundle spine, local tail-radius rows, DyadicRatCore tolerance comparisons, a tolerance-only uniform threshold, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-tail-selector-carrier}, \autoref{thm:compact-uniform-tail-selector-namecert-obligations}, \autoref{thm:compact-uniform-tail-selector-finite-net-handoff}, and \autoref{thm:compact-uniform-tail-selector-uniform-modulus-route} seed the finite compact-net tail selector from ProbeBundle radii to a tolerance-only uniform-modulus ledger.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, sequential compactness, global point selection, selected external minima, quotient real equality, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, pointwise modulus exposure, finite-bundle fold exactness, tolerance-threshold readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformTailSelectorUp.}
\end{closurestatus}
