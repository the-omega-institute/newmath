\chapter{A Concrete Naming Certificate for $\RealCompletenessUp$}
\label{ch:concrete-instances-realcompleteness-namecert}
\origin{human}

The $\RealCompletenessUp$ interface records the diagonal-limit target of \autoref{ch:visions-real-completeness} as a finite naming-certificate packet. It packages the displayed $\DyadicRatCoreUp$ observations, $\StreamNameUp$ windows, $\RegSeqRatUp$ regular sequence rows, and $\RealUp$ seal row through which a completion consumer sees the candidate limit. The packet is a seed carrier for the Real and Completion substrate; it contains no quotient real, selected host limit, choice principle, or host completeness theorem.

\begin{definition}[Real completeness finite carrier]
\label{def:real-completeness-carrier}
A $\RealCompletenessUp$ carrier is a finite $\BHist$ packet
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ is the displayed $\RegSeqRatUp$ family of rational regular sequences;
\item $\mu$ is the explicit Cauchy modulus row naming, for each requested rational tolerance, a finite stage after which the family is stable;
\item $\Delta$ is the diagonal selector row, choosing the finite stage and rational observation used at each precision request;
\item $D$ is the $\DyadicRatCoreUp$ observation ledger containing the dyadic endpoints, radii, comparison rows, and tolerance rows read by the diagonal selector;
\item $W$ is the finite $\StreamNameUp$ window family through which the selected rational names are inspected;
\item $Q$ is the $\RegSeqRatUp$ readback row tying the selected windows to the regular rational sequence data;
\item $S$ is the $\RealUp$ seal row that presents the same diagonal observations as the candidate limit name;
\item $H$ stores displayed $\hsame$ transports for the input family, modulus, selector, dyadic ledger, stream windows, readbacks, and seal row;
\item $C$ stores the $\Cont$ routes by which consumers read the modulus, selected diagonal observations, and sealed candidate;
\item $P$ is the $\Pkg$ provenance row over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RealCompletenessUp}$ registration row.
\end{enumerate}
The local classifier compares two carriers only through these displayed rows. It does not identify quotient representatives, does not choose an external real number, and does not appeal to ambient completeness.
\end{definition}

\begin{theorem}[Real completeness NameCert obligations]
\label{thm:real-completeness-namecert-obligations}
The local $\NameCert_{\RealCompletenessUp}$ surface consists of five finite obligations:
\begin{enumerate}
\item carrier formation for the finite packet of \autoref{def:real-completeness-carrier};
\item classifier stability for the transported family $X$, modulus $\mu$, diagonal selector $\Delta$, observation ledger $D$, windows $W$, readback row $Q$, and seal row $S$;
\item modulus exactness, requiring every convergence bound consumed by the diagonal selector to be the explicit row recorded by $\mu$;
\item diagonal readback exactness, requiring every selected rational observation to pass through the displayed $\StreamNameUp$ window, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ ledger;
\item package closure, requiring the $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows to contain exactly the provenance used by downstream $\RealUp$ consumers.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.RealCompletenessUp.RealCompletenessBHistCarrier\_namecert\_obligations}
\begin{proof}
Carrier formation is the tuple assembly of \autoref{def:real-completeness-carrier}. Each dependency row named by the diagonal-limit target is a visible coordinate of $R$.

Classifier stability is componentwise. Transport the regular sequence family and modulus first, then transport the selected stream windows and dyadic observation ledger, and finally transport the $\RealUp$ seal row reached by those observations.

The modulus and diagonal readback clauses are projections of the same packet. The row $\mu$ supplies the finite stage bounds, while $\Delta$, $W$, $Q$, and $D$ record exactly which rational observations are read at each precision request.

Package closure follows by projecting $H$, $C$, $P$, and $N$. No consumer route reaches a quotient real, external limit selector, or host completeness theorem because no such coordinate occurs in the carrier.
\end{proof}

\begin{theorem}[Real completeness diagonal limit candidate]
\label{thm:real-completeness-diagonal-limit-candidate}
Let
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N)
$$
be an accepted $\RealCompletenessUp$ carrier. The candidate limit exported to $\RealUp$ is the seal row $S$ obtained from the selected diagonal observations $\Delta$ through the finite $\StreamNameUp$ windows $W$, the $\RegSeqRatUp$ readback row $Q$, and the $\DyadicRatCoreUp$ ledger $D$. Every convergence read used by this candidate factors through the explicit modulus row $\mu$ and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:real-completeness-namecert-obligations}. They bind the modulus row, diagonal selector, finite windows, regular-sequence readbacks, dyadic ledger, and seal row to the same carrier.

The diagonal selector $\Delta$ chooses only recorded stages and observations. The selected reads therefore pass through $W$, $Q$, and $D$ before the seal row $S$ is formed.

The convergence-facing part is controlled by $\mu$. Since all consumer routes are stored in $H$, $C$, $P$, and $N$, the exported candidate is exactly the finite diagonal-limit surface seen by $\RealUp$.
\end{proof}

\begin{theorem}[Real completeness carrier transport]
\label{thm:real-completeness-carrier-transport}
Let
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N)
$$
be an accepted $\RealCompletenessUp$ carrier. Any componentwise displayed $\hsame$ transport of the concrete packet rows $X,\mu,\Delta,D,W,Q,S,H,C,P,N$ that preserves the $\Cont$ routes in $C$, the $\Pkg$ provenance in $P$, and the local $\NameCert$ row $N$ yields another accepted $\RealCompletenessUp$ carrier with the same public diagonal-limit surface.
\end{theorem}
\begin{proof}
Use the carrier definition to read every coordinate of $R$. The transported packet has the same row shape: regular sequence family, modulus row, diagonal selector, dyadic ledger, stream windows, readback row, $\RealUp$ seal, transport rows, continuation routes, package provenance, and local naming row.

Apply \autoref{thm:real-completeness-namecert-obligations}. Its classifier-stability clause transports $X,\mu,\Delta,D,W,Q,S$ componentwise by $\hsame$, while its package-closure clause keeps $H,C,P,N$ attached to the same public routes.

The conclusion is therefore a repacking of the displayed rows. No ambient completeness theorem is invoked, because acceptance is checked by the finite carrier and the local $\NameCert$ obligations.
\end{proof}

\begin{theorem}[Real completeness modulus diagonal exactness]
\label{thm:real-completeness-modulus-diagonal-exactness}
For every accepted packet
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N),
$$
each diagonal observation exported by $\RealCompletenessUp$ is selected by $\Delta$, bounded by the explicit modulus row $\mu$, read through the $\BHist$ ledgers $D,W,Q$, transported by $\hsame$ rows in $H$, routed through $\Cont$ rows in $C$, and packaged by $P$ and $N$. No exported diagonal read may use a modulus or rational observation absent from the concrete packet.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:real-completeness-diagonal-limit-candidate}. It identifies the exported $\RealUp$ candidate with the seal row $S$ obtained from $\Delta$ through $W$, $Q$, and $D$.

Apply \autoref{thm:real-completeness-namecert-obligations}. Its modulus exactness clause binds every convergence bound to $\mu$, and its diagonal readback exactness clause binds every selected rational observation to the displayed stream, readback, and dyadic rows.

The remaining rows $H,C,P,N$ record transport, continuation, package, and naming discipline. Hence exactness is a finite ledger property of $R$, not a claim about host completeness.
\end{proof}

\begin{theorem}[RealUp seal ledger discipline]
\label{thm:real-completeness-realup-seal-ledger}
For every accepted $\RealCompletenessUp$ carrier
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N),
$$
the $\RealUp$ seal row $S$ is the only completion-facing export. Its reads must pass through the displayed $\BHist$ packet rows $X,\mu,\Delta,D,W,Q$, the $\hsame$ transport row $H$, the $\Cont$ route row $C$, the $\Pkg$ row $P$, and the $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
Project the carrier through \autoref{def:real-completeness-carrier}. The only row typed as the candidate limit name is $S$, and the preceding rows record the family, modulus, selector, dyadic observations, stream windows, and regular-sequence readbacks that form it.

Use \autoref{thm:real-completeness-namecert-obligations} to keep the seal attached to the same classifier-stable rows. The package-closure clause says the public routes are exactly the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Finally apply \autoref{thm:real-completeness-diagonal-limit-candidate}. It identifies the seal with the finite diagonal observations, so the $\RealUp$ boundary contains no selected external limit, quotient real, host equality, or host completeness proof.
\end{proof}

\begin{theorem}[Real completeness no-escape boundary]
\label{thm:real-completeness-nonescape-boundary}
The public boundary of $\RealCompletenessUp$ exports only the finite diagonal-limit candidate over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. It exports no quotient real, no host equality on real numbers, no selected external limit, no countable-choice principle, and no host completeness theorem.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates of \autoref{def:real-completeness-carrier}. The only completion-facing rows are the modulus, diagonal selector, finite stream windows, regular-sequence readbacks, dyadic observation ledger, and $\RealUp$ seal row.

Apply \autoref{thm:real-completeness-diagonal-limit-candidate}. Every candidate-limit read is downstream of those displayed rows and their transport, continuation, package, and local naming data.

The excluded objects are absent from the tuple. Therefore the boundary cannot export a quotient real, host real equality, external selected limit, choice principle, or host completeness theorem.
\end{proof}

\begin{theorem}[Real completeness scoped finite seal pullback]
\label{thm:real-completeness-scoped-finite-seal-pullback}
Every $\RealCompletenessUp$ diagonal-limit read factors through the explicit modulus row, diagonal selector, $\DyadicRatCoreUp$ ledger, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback row, $\RealUp$ seal, and downstream $\CauchyLimitSealUp$ finite seal route displayed by the accepted carrier. The pullback exports no quotient stream, selected host limit, ambient completeness theorem, or host equality on completed reals.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:real-completeness-namecert-obligations}. They bind carrier formation, classifier stability, modulus exactness, diagonal readback exactness, and package closure to the finite rows of the accepted packet.

Apply \autoref{thm:real-completeness-diagonal-limit-candidate} and \autoref{thm:real-completeness-modulus-diagonal-exactness}. The candidate read is selected by $\Delta$, bounded by $\mu$, and read through $D,W,Q$ before it reaches the $\RealUp$ seal row.

Next use \autoref{thm:real-completeness-realup-seal-ledger}. It identifies the $\RealUp$ seal as the only completion-facing export and keeps every consumer route inside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The downstream $\CauchyLimitSealUp$ finite seal route consumes exactly that seal-facing row: explicit modulus, selected diagonal observations, stream windows, regular rational readback, dyadic ledger, and the same package provenance. The route is therefore a pullback along the finite seal consumer rather than a choice of an external completed object.

Finally apply \autoref{thm:real-completeness-nonescape-boundary}. Since quotient streams, selected host limits, ambient completeness, and host equality are absent from the carrier, they cannot appear in the scoped pullback.
\end{proof}

\begin{theorem}[Real completeness candidate-uniqueness boundary]
\label{thm:real-completeness-candidate-uniqueness-boundary}
Let
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N)
$$
and
$$
R'=(X',\mu',\Delta',D',W',Q',S',H,C,P,N)
$$
be accepted $\RealCompletenessUp$ packets whose $X,\mu,\Delta,D,W,Q$ rows are related by the displayed $\hsame$ transports named in the same $H$ row, and whose seal rows $S$ and $S'$ are produced through the same diagonal readback routes $C$, package provenance $P$, and local naming row $N$. Then every $\RealUp$ consumer classifies $S$ and $S'$ through the same displayed $\RealUp$ seal ledger. In particular, the packet cannot export two distinct completion-facing seal reads for the same transported diagonal family and the same readback routes.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-completeness-namecert-obligations}. Its classifier-stability clause transports the family, modulus, selector, dyadic ledger, windows, readback row, and seal row through the displayed rows of $H$.

Next use \autoref{thm:real-completeness-carrier-transport} and \autoref{thm:real-completeness-modulus-diagonal-exactness}. The transported packets have the same public diagonal-limit surface, and every convergence bound and selected rational observation is read from $\mu,\Delta,D,W,Q$ through the same routes.

Now apply \autoref{thm:real-completeness-realup-seal-ledger}. A $\RealUp$ consumer may inspect a seal only through the displayed packet rows and the shared $H,C,P,N$ ledger. Since both seal rows are produced through those same diagonal readback routes, the consumer reaches the same displayed $\RealUp$ seal ledger.

Finally invoke \autoref{thm:real-completeness-nonescape-boundary}. There is no quotient real, host equality, selected external limit, or host completeness row that could separate $S$ from $S'$ outside the displayed ledger. The only exported classification is therefore the common finite seal-ledger classification.
\end{proof}

\begin{theorem}[CauchyLimitSeal transport stability for real completeness]
\label{thm:real-completeness-cauchylimitseal-transport-stability}
Let
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N)
$$
and
$$
R'=(X',\mu',\Delta',D',W',Q',S',H,C,P,N)
$$
be accepted $\RealCompletenessUp$ packets whose $X,\mu,\Delta,D,W,Q$ rows are related by the displayed $\hsame$ transports named in the shared $H$ row, and whose seal rows $S$ and $S'$ are produced through the same continuation routes $C$, package provenance $P$, and local $\NameCert_{\RealCompletenessUp}$ row $N$. Then every downstream $\CauchyLimitSealUp$ consumer of either seal factors through the same finite route: the explicit modulus, diagonal selector, $\DyadicRatCoreUp$ ledger, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback row, $\RealUp$ seal, displayed $\hsame$ rows, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row. Hence the transported diagonal-limit seal is stable not only for $\RealUp$ consumers but also for the finite $\CauchyLimitSealUp$ route that consumes the seal.
\end{theorem}
\begin{proof}
First use \autoref{thm:real-completeness-carrier-transport}. The componentwise $\hsame$ rows in $H$ transport the visible family, modulus, selector, dyadic ledger, stream windows, readback row, and seal row while preserving the shared $C,P,N$ routing data, so both packets present the same public diagonal-limit surface.

Next apply \autoref{thm:real-completeness-modulus-diagonal-exactness}. Every selected read used by either seal is bounded by the corresponding explicit modulus row and passes through $\Delta,D,W,Q$ or through their transported counterparts. Since those rows are tied by the same $H$ and consumed through the same $C,P,N$ data, the diagonal selector and rational readback route are the same finite route seen through transport.

Use \autoref{thm:real-completeness-realup-seal-ledger} to identify the $\RealUp$ seal as the unique completion-facing export for each packet. Then use \autoref{thm:real-completeness-scoped-finite-seal-pullback}: any $\CauchyLimitSealUp$ consumer of that export reads the explicit modulus, selected diagonal observations, dyadic ledger, stream windows, regular-sequence readback, seal row, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

It remains to rule out a second downstream route. By \autoref{thm:real-completeness-candidate-uniqueness-boundary}, the two seal rows have the same displayed $\RealUp$ seal-ledger classification for the transported diagonal family and shared readback routes. Finally \autoref{thm:real-completeness-nonescape-boundary} excludes quotient streams, host real equality, selected external limits, choice principles, and host completeness rows. Thus any $\CauchyLimitSealUp$ consumer of either seal factors through the same finite route.
\end{proof}

\begin{theorem}[Real completeness seal classifier stability]
\label{thm:real-completeness-seal-classifier-stability}
Let
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N)
$$
and
$$
R'=(X',\mu',\Delta',D',W',Q',S',H,C,P,N)
$$
be accepted $\RealCompletenessUp$ packets. Suppose the family row, modulus row, diagonal selector, dyadic ledger, $\StreamNameUp$ windows, and $\RegSeqRatUp$ readback rows of $R$ and $R'$ are related by the displayed $\hsame$ transports named in the shared $H$ row, and suppose the seal rows $S$ and $S'$ are read through the same $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\RealCompletenessUp}$ route. Then the $\RealUp$ classifier class exported by $S$ is the same finite seal-ledger class exported by $S'$. Moreover, every downstream $\CauchyLimitSealUp$ read of either seal sees that same class through the displayed route consisting of $\mu,\Delta,D,W,Q,S$, the transported counterparts, $H,C,P,N$, and no ambient completion object.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-completeness-carrier-transport}. The displayed $\hsame$ rows give componentwise transport of the family, modulus, diagonal selector, dyadic ledger, stream windows, regular rational readback, and seal rows while keeping the shared continuation, package, and local naming data fixed.

Next use \autoref{thm:real-completeness-modulus-diagonal-exactness}. The finite observations that feed the two seal rows are exactly the bounded diagonal reads selected by $\Delta$ and $\Delta'$ and recovered through $D,W,Q$ and their transported counterparts.

Apply \autoref{thm:real-completeness-realup-seal-ledger}. The only $\RealUp$ classifier endpoint available to either packet is the seal ledger exposed by the accepted carrier, so the two seal rows export the same finite seal-ledger class under the shared $H,C,P,N$ route.

Now invoke \autoref{thm:real-completeness-cauchylimitseal-transport-stability}. A downstream $\CauchyLimitSealUp$ consumer reads no separate completion data; it consumes the explicit modulus, diagonal selector, dyadic ledger, stream windows, regular readback, seal row, displayed transports, continuations, package provenance, and local naming row already identified above.

Finally use \autoref{thm:real-completeness-nonescape-boundary}. Quotient streams, selected host limits, host equality, countable choice, and ambient completeness are not coordinates of the packet, so they cannot split the exported classifier class or create a second downstream seal view.
\end{proof}

\begin{theorem}[RealCompleteness shared-window handoff from CauchySequenceSpace]
\label{thm:real-completeness-cauchysequence-space-shared-window-handoff}
Let
$$
R=(X,\mu,\Delta,D,W,Q,S,H,C,P,N)
$$
be an accepted $\RealCompletenessUp$ packet whose regular-family row is presented by an accepted $\CauchySequenceSpaceUp$ packet
$$
T=(F,\sigma,W_c,\epsilon,Q_c,H_c,C_c,P_c,N_c).
$$
Then every $\RealUp$ or $\CauchyLimitSealUp$ consumer read of the seal row $S$ factors first through the displayed $\CauchySequenceSpaceUp$ rows
$$
F,\sigma,W_c,\epsilon,Q_c,H_c,C_c,P_c,N_c
$$
and then through the displayed $\RealCompletenessUp$ rows
$$
\mu,\Delta,D,W,Q,S,H,C,P,N.
$$
The handoff exports no quotient stream, selected host limit, host real equality, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Begin with the shared finite Cauchy window. By \autoref{thm:cauchysequence-space-shared-window-completion-consumer}, any completion-facing comparison supplied by $T$ reaches its handoff only after reading the regular-family row, schedule, cofinal-window ledger, dyadic tolerance row, completion handoff, transports, continuations, provenance, and local naming row.

Next exhaust the regular-family presentation. Theorem~\ref{thm:cauchysequence-space-regular-family-exhaustion} makes every regular-family consumer of $T$ factor through the same rows, and \autoref{thm:cauchysequence-space-uniformcriterion-consumption} records that a uniform-criterion presentation contributes only the shared threshold and real-seal handoff already displayed by $T$.

Now pass to the diagonal seal. Apply \autoref{thm:real-completeness-cauchylimitseal-transport-stability} and \autoref{thm:real-completeness-seal-classifier-stability}. The seal row $S$ is read through the explicit modulus, diagonal selector, dyadic ledger, stream windows, regular readback, $\RealUp$ seal, transported routes, continuations, package provenance, and local $\NameCert_{\RealCompletenessUp}$ row.

Finally use \autoref{thm:real-completeness-nonescape-boundary}. Since neither the CauchySequenceSpace packet nor the RealCompleteness packet contains quotient streams, selected host limits, host real equality, or an ambient completeness row, a consumer cannot bypass the two-stage displayed route.
\end{proof}

\closureat{RealCompletenessUp}{obligationStr}

\begin{closurestatus}{\RealCompletenessUp}
  \constructivestory{A $\RealCompletenessUp$ packet is generated by a displayed regular Cauchy family, explicit modulus row, diagonal selector, dyadic observation ledger, stream windows, regular-sequence readback, RealUp seal, transport routes, package provenance, and a local naming row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{thm:real-completeness-namecert-obligations}, \autoref{thm:real-completeness-carrier-transport}, \autoref{thm:real-completeness-modulus-diagonal-exactness}, and \autoref{thm:real-completeness-realup-seal-ledger} bind the finite diagonal-limit carrier, transport, modulus exactness, diagonal readback, and seal discipline over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close RealUp completeness, uniqueness of completed limits, quotient equality of streams, a public standard bridge, or a checked Lean target.}
  \upgradepath{Scoped closure requires Lean checking of carrier transport, modulus diagonal exactness, RealUp seal discipline, and ledger non-escape over the same displayed rows.}
\end{closurestatus}
