\chapter{A Concrete Naming Certificate for $\BoundaryGateUp$}
\label{ch:concrete-instances-boundary-gate-namecert}
\origin{ai}

The $\BoundaryGateUp$ packet realizes the boundary-gate construction of \autoref{ch:visions-external-supply-without-axioms} as a finite NameCert surface. It sits beside the external-supply boundary of \autoref{ch:concrete-instances-external-supply-boundary-namecert}, the socket ledger of \autoref{ch:concrete-instances-supplysocketledger-namecert}, the supply-kind router of \autoref{ch:concrete-instances-supply-kind-router-namecert}, the audit membrane of \autoref{ch:concrete-instances-auditmembrane-namecert}, and the audit-gate boundary of \autoref{ch:concrete-instances-auditgateboundary-namecert}. Those siblings isolate boundary records, socket ledgers, kind routing, refusal replay, or audit inventory; this packet binds the gate question and audit verdict to the visible boundary form and site route in one carrier.

\begin{definition}[BoundaryGate carrier]
\label{def:boundary-gate-carrier}
A $\BoundaryGateUp$ carrier is a finite $\BHist$ packet
$$
G=(B,Q,V,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the boundary-form row, recording the accepted external-supply boundary form being tested;
\item $Q$ is the gate-question row, recording the finite question asked of that boundary form;
\item $V$ is the audit-verdict row, recording the pass-or-refuse result for the question $Q$ at boundary form $B$;
\item $S$ is the visible site route, recording the socket, marker, dependency, certificate row, or import edge at which the boundary is read;
\item $H$ records componentwise $\hsame$ transports for $B,Q,V,S$ together with continuation, provenance, and naming rows;
\item $C$ records the displayed $\Cont$ routes by which consumers inspect the boundary form, gate question, audit verdict, and visible site route;
\item $P$ is the $\Pkg$ provenance over $\ExternalSupplyBoundaryUp$, $\SupplySocketLedgerUp$, $\SupplyKindRouterUp$, $\AuditMembraneUp$, $\AuditGateBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\BoundaryGateUp}$ row.
\end{enumerate}
The carrier exports no hidden boundary content, no verdict detached from the boundary form, no route that treats refused supply as substrate theorem content, and no host-side permission to answer the gate without displaying $B,Q,V,S,H,C,P,N$.
\end{definition}

\begin{theorem}[BoundaryGate NameCert obligations]
\label{thm:boundary-gate-namecert-obligations}
For the carrier of \autoref{def:boundary-gate-carrier}, the local $\NameCert_{\BoundaryGateUp}$ surface consists exactly of carrier admission for
$$
G=(B,Q,V,S,H,C,P,N),
$$
boundary-form exposure through $B$, gate-question exposure through $Q$, audit-verdict exposure through $V$, visible-site routing through $S$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.

Consequently, an accepted $\BoundaryGateUp$ packet cannot expose a verdict without the boundary form it judges, cannot expose a boundary form without the gate question and site route, and cannot route refused boundary content into substrate theorem content.
\end{theorem}
\leanchecked{BEDC.Derived.BoundaryGateUp.BoundaryGateTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the displayed carrier. The accepted packet lists $B,Q,V,S,H,C,P,N$ as its full inventory, so the local naming certificate has no coordinate from which to introduce a second boundary form, gate question, verdict, site route, transport, continuation, provenance, or local name.

Read the gate-facing rows first. The boundary form $B$ names what kind of external-supply boundary is being tested, $Q$ names the finite question asked of that form, and $V$ records the audit verdict for that question.

Next read the routing rows. The site route $S$ makes the tested location visible, while $H$ and $C$ transport and replay only the same displayed rows.

Finally inspect $P$ and $N$. Provenance ties the packet to its sibling surfaces, and the local name binds the finite inventory. Since the carrier has no coordinate for hidden boundary content or detached verdicts, every accepted read remains inside the displayed NameCert surface.
\end{proof}

\begin{theorem}[BoundaryGate non-internalization]
\label{thm:boundary-gate-noninternalization}
Every accepted $\BoundaryGateUp$ packet preserves the external-supply boundary it judges. A downstream consumer may read $B,Q,V,S,H,C,P,N$ as a finite audit surface for the boundary gate, but it may not read from the packet an internal witness of the requested supply, a replacement theorem for refused content, an off-site verdict, a selected boundary representative, a quotient collapse, a hidden host equality, or a bridge row.
\end{theorem}
\leanchecked{BEDC.Derived.BoundaryGateUp.BoundaryGate\_noninternalization}
\begin{proof}
Begin with the boundary-form and gate-question rows. The pair $B,Q$ fixes the boundary and the finite question as displayed certificate content rather than as an implicit host-side convention.

Use the audit-verdict row $V$ together with the visible site route $S$. A verdict is accepted only as the result of testing the displayed boundary form at the displayed site; it is not an independent source of requested supply.

The transport and replay rows $H$ and $C$ preserve that same packet under componentwise movement and consumer access. They do not add a selector, quotient representative, theorem object, host equality, or bridge coordinate.

The provenance and local naming rows $P,N$ bind the packet to $\ExternalSupplyBoundaryUp$, $\SupplySocketLedgerUp$, $\SupplyKindRouterUp$, $\AuditMembraneUp$, and $\AuditGateBoundaryUp$ as sibling sources. Since none of the displayed rows supplies the refused content itself, the accepted boundary gate keeps the judged supply external.
\end{proof}

\begin{theorem}[BoundaryGate carrier admission]
\label{thm:boundarygate-carrier-admission}
Every admitted $\BoundaryGateUp$ carrier is admitted only as a finite $\BHist$ packet
$$
G=(B,Q,V,S,H,C,P,N)
$$
whose rows display the boundary form $B$, gate question $Q$, audit verdict $V$, visible site route $S$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. In particular, the verdict $V$ cannot be admitted apart from the boundary form $B$ and visible site route $S$.
\end{theorem}
\leanchecked{BEDC.Derived.BoundaryGateUp.BoundaryGateCarrierAdmission}
\begin{proof}
Project the carrier of \autoref{def:boundary-gate-carrier}. The row inventory is exactly $B,Q,V,S,H,C,P,N$, so admission begins with the displayed boundary form, question, verdict, and site route.

The structural rows do not add a detached verdict. The $\hsame$ row $H$ transports the same displayed data, the $\Cont$ row $C$ replays access through the same packet, and $P,N$ bind provenance and local naming to that inventory.

Thus carrier admission is a row projection from the displayed packet. Since no carrier coordinate stores $V$ without $B$ and $S$, an admitted verdict remains attached to the boundary form and visible site route.
\end{proof}

\begin{theorem}[BoundaryGate classifier stability]
\label{thm:boundarygate-classifier-stability}
For any accepted $\BoundaryGateUp$ packet $G=(B,Q,V,S,H,C,P,N)$, the classifier is stable only under componentwise $\hsame$ transport of the displayed rows $B,Q,V,S,H,C,P,N$ and displayed $\Cont$ replay through the same $\Pkg$ and $\NameCert$ surface. A transported verdict remains the verdict for boundary form $B$ at visible site route $S$; it cannot become a route-free audit result.
\end{theorem}
\begin{proof}
Start with the classifier rows displayed by the carrier. The boundary form, question, verdict, and site route are compared as named coordinates of one finite packet.

Apply the transport row. Componentwise $\hsame$ preserves each displayed coordinate as the same row for classifier purposes, so a stable read carries $B,Q,V,S$ together rather than moving $V$ alone.

Finally use the replay and provenance rows. The $\Cont$ row $C$, $\Pkg$ row $P$, and local $\NameCert$ row $N$ replay only the same packet, so classifier stability cannot detach the verdict from the boundary form or visible site route.
\end{proof}

\begin{theorem}[BoundaryGate ledger nonescape]
\label{thm:boundarygate-ledger-nonescape}
Every ledger read from an accepted $\BoundaryGateUp$ carrier $G=(B,Q,V,S,H,C,P,N)$ must expose the boundary form $B$, gate question $Q$, audit verdict $V$, visible site route $S$, $\BHist$ transport row $H$, $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert$ row $N$ together. No ledger consumer can export $V$ as supplied content, as a hidden boundary witness, or as a verdict detached from $B$ and $S$.
\end{theorem}
\begin{proof}
Read the ledger-facing packet as displayed finite data. The rows $B,Q,V,S$ identify what is judged, which finite question is asked, what verdict is recorded, and where the boundary is read.

Now inspect the structural route. The $\BHist$ transport row $H$ and $\Cont$ replay row $C$ keep ledger access inside the same carrier, while $P$ and $N$ record provenance and local naming.

Because the carrier contains no extra row for supplied content, hidden boundary witnesses, or route-free verdicts, the ledger cannot export such data. The verdict remains a visible row of the boundary gate rather than an escaping object.
\end{proof}

\begin{theorem}[BoundaryGate verdict-site route stability]
\label{thm:boundarygate-verdict-site-route-stability}
For every accepted $\BoundaryGateUp$ carrier $G=(B,Q,V,S,H,C,P,N)$, the audit verdict row $V$ remains attached to both the boundary-form row $B$ and the visible-site route $S$ under componentwise $\hsame$ transport and displayed $\Cont$ replay. In particular, no stable replay can read $V$ as a route-free verdict, as supplied theorem content, or as a boundary decision detached from the site where $B$ is inspected.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:boundary-gate-carrier}. Its displayed packet has a single verdict coordinate $V$, and that coordinate is listed together with the boundary form $B$, gate question $Q$, visible site route $S$, transport row $H$, replay row $C$, provenance row $P$, and local name $N$.

Apply \autoref{thm:boundarygate-classifier-stability}. Componentwise $\hsame$ transport preserves the classifier only by moving the displayed rows together, so a transported read of $V$ still carries the same boundary form and visible site route.

Finally apply \autoref{thm:boundarygate-ledger-nonescape}. Ledger consumption cannot export $V$ as hidden supply or as a detached verdict. The displayed $\Cont$ replay therefore returns the verdict only through the same boundary-form and site-route rows that admitted it.
\end{proof}

\falsifiablePrediction{Any accepted BoundaryGateUp packet that admits boundary content without exposing both the boundary-form row and the audit-verdict row is rejected by the NameCert surface.}

\independenceWitness{Nearest siblings: external-supply-boundary, supply-socket-ledger, supply-kind-router, audit-membrane, audit-gate-boundary. None is bijective because BoundaryGateUp carries the gate question and verdict rows together with the visible boundary form and site route, while the siblings isolate boundary, socket kind, kind routing, refusal replay, or gate-boundary inventory.}

The Lean follow-up must provide $\mathsf{Nontrivial}\ \BoundaryGateUp$ and $\mathsf{FieldFaithful}\ \BoundaryGateUp$ in $\mathsf{lean4/BEDC/Derived/BoundaryGateUp/TasteGate.lean}$ before the taste-gate target $\mathsf{BEDC.Derived.BoundaryGateUp.TasteGate.boundaryGateTasteGate}$ can be used as a checked correspondence.

\closureat{BoundaryGateUp}{seedStr}
\closureat{BoundaryGateUp}{obligationStr}

\begin{closurestatus}{\BoundaryGateUp}
  \constructivestory{A $\BoundaryGateUp$ packet is generated from a boundary-form row, gate-question row, audit-verdict row, visible site route, componentwise $\hsame$ transport, displayed $\Cont$ routes, provenance, and local naming. The carrier asks a finite question of a visible external-supply boundary and records the verdict only with that boundary and site still present. The classifier therefore admits a gate read by projecting $B,Q,V,S,H,C,P,N$ together: transport preserves the same boundary form, and continuation replay prevents the verdict from becoming detached supply content.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{This chapter binds the BoundaryGate carrier, NameCert obligations, non-internalization theorem, prediction, and independence witness.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.BoundaryGateUp.BoundaryGateCarrierAdmission}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim hidden boundary content, detached verdicts, supplied theorem content, quotient representatives, host equality, or bridge rows.}
  \upgradepath{Scoped closure requires binding these obligations to the exact kernel scope for boundary forms, verdict rows, visible-site routes, and replay consumers.}
\end{closurestatus}
