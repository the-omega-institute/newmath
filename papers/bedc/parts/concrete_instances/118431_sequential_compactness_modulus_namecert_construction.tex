\chapter{A Concrete Naming Certificate for $\SequentialCompactnessModulusUp$}
\label{ch:concrete-instances-sequential-compactness-modulus-namecert}
\origin{human}

Following Bishop--Bridges compact metric arguments, the $\SequentialCompactnessModulusUp$ packet records the finite modulus data by which a compact metric source exposes bounded sequence windows, nested finite covers, dyadic tolerance requests, regular rational readback, and a terminal Real seal before any cluster-facing consumer may proceed. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, total boundedness data from \autoref{ch:concrete-instances-totallybounded-namecert}, the completion boundary of \autoref{ch:concrete-instances-completemetric-namecert}, finite stream windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, and the Real naming seal of \autoref{ch:concrete-instances-real-namecert}. The packet supplies explicit finite modulus data for sequential compactness evidence consumed by Bolzano--Weierstrass and compact-metric sequence arguments; it is not a proof of general sequential compactness.

\begin{definition}[SequentialCompactnessModulus carrier]
\label{def:sequential-compactness-modulus-carrier}
A $\SequentialCompactnessModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(K,T,C,S,D,R,E,Q,H,U,P,N)
$$
with the following rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the compact metric carrier used by the sequence argument.
\item $T$ is the $\TotallyBoundedUp$ finite-cover row, listing the cover cells inspected at the displayed tolerances.
\item $C$ is the $\CompleteMetricUp$ boundary row, recording that Cauchy-style readback may be sealed only through the displayed completion boundary.
\item $S$ is the $\StreamNameUp$ finite-window row for the sequence being inspected.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, listing the precision requests and dyadic margins used by the finite cover refinement.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the selected finite windows and dyadic tolerances.
\item $E$ is the terminal $\RealUp$ seal row available only after $K,T,C,S,D,R$ have been displayed.
\item $Q$ is the cluster handoff row, giving the downstream Bolzano--Weierstrass or compact-metric sequence consumer the displayed finite modulus route.
\item $H$ records componentwise $\hsame$ transport for the source, cover, completion-boundary, stream-window, tolerance, readback, seal, and handoff rows.
\item $U$ records displayed $\Cont$ replay for refinements of the finite tolerance and window budget.
\item $P$ is the $\Pkg$ provenance over $\SequentialCompactnessModulusUp$, $\CompactMetricUp$, $\TotallyBoundedUp$, $\CompleteMetricUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SequentialCompactnessModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,T,C,S,D,R,E,Q,H,U,P,N$. It has no coordinate for a general sequential compactness theorem, a selected infinite subsequence, countable choice, open-cover compactness, quotient stream equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[SequentialCompactnessModulus NameCert obligations]
\label{thm:sequential-compactness-modulus-namecert-obligations}
For every accepted $\SequentialCompactnessModulusUp$ carrier
$$
M=(K,T,C,S,D,R,E,Q,H,U,P,N),
$$
the local $\NameCert_{\SequentialCompactnessModulusUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the compact metric source $K$, total-bounded finite-cover row $T$, completion-boundary row $C$, stream-window row $S$, dyadic tolerance row $D$, regular readback row $R$, Real seal $E$, cluster handoff $Q$, and structural rows $H,U,P,N$;
\item finite-modulus exposure, requiring every tolerance request in $D$ to be matched with a displayed finite cover cell in $T$ and a finite stream window in $S$;
\item completion-boundary discipline, requiring every Cauchy-style readback to pass through $C$ before reaching $R$ and $E$;
\item cluster handoff nonescape, requiring any Bolzano--Weierstrass or compact-metric sequence consumer to read $Q$ only after the route
$$
K,T,C,S,D \longmapsto R \longmapsto E
$$
has been displayed;
\item provenance and local naming, requiring $H,U,P,N$ to transport, replay, source, and name only the displayed finite rows.
\end{enumerate}
No obligation row contains general sequential compactness, selected subsequences, countable choice, open-cover compactness, quotient stream equality, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sequential-compactness-modulus-carrier}. The rows $K,T,C,S,D,R,E,Q,H,U,P,N$ are the whole accepted packet.

For finite-modulus exposure, read $D$ together with $T$ and $S$. Each precision request is paired with a displayed finite cover cell and a finite stream window before any readback row is visible.

For the completion-boundary discipline, route the selected window and tolerance data through $C$ before $R$ and $E$. The packet therefore uses completion only as the displayed boundary row named by \autoref{ch:concrete-instances-completemetric-namecert}.

The cluster handoff $Q$ is downstream of the same finite route. Since $H,U,P,N$ only transport, replay, source, and name the displayed rows, the packet exports finite modulus data and no host sequential compactness, selected subsequence, countable-choice schedule, open-cover theorem, quotient stream equality, or ambient Real completeness.
\end{proof}
\leanstmt{BEDC.Derived.SequentialCompactnessModulusUp}

\falsifiablePrediction{A $\SequentialCompactnessModulusUp$ consumer that obtains a cluster handoff without reading compact metric source data, total-bounded finite covers, completion-boundary data, finite StreamName windows, dyadic tolerances, RegSeqRat readback, and the Real seal refutes the carrier surface.}

\independenceWitness{compactmetric, totallybounded, completemetric, streamname, regseqrat, real: no listed sibling supplies the whole finite modulus packet because each sibling contributes compact source data, cover finiteness, completion-boundary discipline, observation windows, regular readback, or sealing, while $\SequentialCompactnessModulusUp$ binds the cluster-facing route with transport, replay, provenance, and local naming.}

\closureat{SequentialCompactnessModulusUp}{seedStr}

\begin{closurestatus}{\SequentialCompactnessModulusUp}
  \constructivestory{A $\SequentialCompactnessModulusUp$ packet is generated from CompactMetric source data, TotallyBounded finite covers, CompleteMetric boundary rows, StreamName finite windows, DyadicRatCore tolerances, RegSeqRat readback, a RealUp seal, cluster handoff data, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sequential-compactness-modulus-carrier} and \autoref{thm:sequential-compactness-modulus-namecert-obligations} seed the finite modulus surface for sequential compactness evidence over compact-metric sequence consumers.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.SequentialCompactnessModulusUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close general sequential compactness, selected infinite subsequences, countable choice, open-cover compactness, quotient stream equality, ambient RealUp completeness, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-modulus exposure, completion-boundary discipline, cluster handoff nonescape, transport, replay, provenance, and local naming for BEDC.Derived.SequentialCompactnessModulusUp.}
\end{closurestatus}
