\chapter{A Concrete Naming Certificate for $\ClosedBoundedIntervalCompleteUp$}
\label{ch:concrete-instances-closed-bounded-interval-complete-namecert}
\origin{human}

Following Bishop--Bridges regular real analysis and Kelley on complete metric subspaces, the $\ClosedBoundedIntervalCompleteUp$ packet records the constructive completeness of a closed bounded real interval as finite BEDC NameCert data. It sits after the interval carrier of \autoref{ch:concrete-instances-closedboundedinterval-namecert}, the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet names the interval-completeness route used by L10 completion consumers without importing open-cover compactness, selected limits, quotient equality of real names, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Closed bounded interval complete carrier]
\label{def:closed-bounded-interval-complete-carrier}
A $\ClosedBoundedIntervalCompleteUp$ carrier is a finite $\BHist$ packet
$$
I=(B,A,Z,R,W,D,C,M,T,H,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\ClosedBoundedIntervalUp$ interval row, recording the closed endpoint pair, boundedness witnesses, and membership classifier for the interval being completed.
\item $A$ records the two endpoint $\RealUp$ seals, including the lower and upper endpoint names and their displayed endpoint-order evidence.
\item $Z$ is the $\RegSeqRatUp$ regular-Cauchy readback row for the candidate interval sequence.
\item $R$ is the $\RealUp$ terminal seal row reached by the readback after the tolerance and window rows have been exposed.
\item $W$ is the $\StreamNameUp$ finite-window row, recording exactly the finite observations of endpoint and sequence names requested by the consumer.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording dyadic radii, endpoint slack, interval-retention comparisons, and regular-Cauchy budget cells.
\item $C$ is the $\CompleteMetricUp$ limit row for the displayed Cauchy request.
\item $M$ records membership transport, carrying the interval-membership rows from the finite tail windows to the terminal Real seal.
\item $T$ records componentwise $\hsame$ transport for the interval row, endpoint seals, readback, StreamName windows, tolerance ledger, complete-metric limit row, membership transport, replay, provenance, and local naming.
\item $H$ records displayed $\Cont$ replay from a completion request through $B,A,Z,R,W,D,C,M,T$.
\item $Q$ is the local interval-completeness replay row, tying the Cauchy tail request, dyadic tolerance budget, and membership transport to the same closed interval.
\item $P$ is the $\Pkg$ provenance over $\ClosedBoundedIntervalCompleteUp$, $\ClosedBoundedIntervalUp$, $\CompleteMetricUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ClosedBoundedIntervalCompleteUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,A,Z,R,W,D,C,M,T,H,Q,P,N$. No coordinate stores open-cover compactness, a selected limit functional, quotient equality of real names, countable choice, trichotomy, or completeness for ambient $\RealUp$.
\end{definition}

\begin{theorem}[Closed bounded interval complete NameCert obligations]
\label{thm:closed-bounded-interval-complete-namecert-obligations}
For every accepted $\ClosedBoundedIntervalCompleteUp$ carrier
$$
I=(B,A,Z,R,W,D,C,M,T,H,Q,P,N),
$$
the local $\NameCert_{\ClosedBoundedIntervalCompleteUp}$ surface consists exactly of carrier admission for the $\ClosedBoundedIntervalUp$ row, endpoint $\RealUp$ seals, $\RegSeqRatUp$ regular-Cauchy readback, terminal $\RealUp$ seal, $\StreamNameUp$ finite windows, $\DyadicRatCoreUp$ tolerance ledger, $\CompleteMetricUp$ limit row, membership transport, componentwise $\hsame$ transport, displayed $\Cont$ replay, local replay, provenance, and local naming. Any completion-facing consumer must read those rows before treating a displayed regular Cauchy sequence in the closed bounded interval as having an interval member as its terminal seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:closed-bounded-interval-complete-carrier}. The accepted packet displays exactly the interval row, endpoint seals, regular-Cauchy readback, terminal seal, StreamName windows, dyadic tolerance ledger, complete-metric row, membership transport, and structural rows.

The analytic route begins with $B,A,W,D,Z$. These rows expose the interval endpoints, finite name observations, dyadic tolerance budget, and regular rational readback before any terminal Real seal can be consumed.

The completion-facing rows are downstream. The row $C$ supplies the complete-metric limit surface, while $M$ transports membership from finite retained tails to the terminal seal $R$ using only the displayed interval and tolerance data.

Finally $T,H,Q,P,N$ preserve, replay, source, and name the same finite route. Since the carrier contains no open-cover compactness row, selected-limit functional, quotient real-name equality, countable-choice schedule, trichotomy branch, or ambient $\RealUp$ completeness coordinate, none of those claims belongs to the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\ClosedBoundedIntervalCompleteUp$ completion read is refuted if it reaches a terminal $\RealUp$ seal without displaying the closed interval row, endpoint Real seals, regular-Cauchy readback, StreamName windows, dyadic tolerance ledger, complete-metric limit row, membership transport, componentwise transport, replay, provenance, and local NameCert data.}

\independenceWitness{closedboundedinterval, completemetric, real, regseqrat, streamname, dyadicratcore: $\ClosedBoundedIntervalCompleteUp$ is not bijective to any listed sibling because it jointly carries a closed interval row, endpoint seals, regular-Cauchy readback, finite name windows, dyadic tolerance ledger, complete-metric limit surface, membership transport, replay, provenance, and local naming as one interval-completeness packet.}

\closureat{ClosedBoundedIntervalCompleteUp}{seedStr}

\begin{closurestatus}{\ClosedBoundedIntervalCompleteUp}
  \constructivestory{A $\ClosedBoundedIntervalCompleteUp$ packet is generated from a ClosedBoundedIntervalUp row, endpoint RealUp seals, RegSeqRatUp regular-Cauchy readback, StreamNameUp finite windows, DyadicRatCoreUp tolerance ledger, CompleteMetricUp limit row, membership transport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:closed-bounded-interval-complete-carrier} and \autoref{thm:closed-bounded-interval-complete-namecert-obligations} seed the finite interval-completeness route from closed bounded interval data to a Real-facing terminal seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected limits, quotient equality of real names, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint seal exactness, regular-Cauchy readback, finite StreamName window inspection, DyadicRatCore tolerance exactness, CompleteMetric limit handoff, membership transport, replay, provenance, and local naming for BEDC.Derived.ClosedBoundedIntervalCompleteUp.}
\end{closurestatus}
