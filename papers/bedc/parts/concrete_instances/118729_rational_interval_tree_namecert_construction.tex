\chapter{A Concrete Naming Certificate for $\RationalIntervalTreeUp$}
\label{ch:concrete-instances-rational-interval-tree-namecert}
\origin{human}

Following Bishop--Bridges constructive real analysis and Farey interval-refinement practice, the $\RationalIntervalTreeUp$ packet records a finite tree of rational brackets whose boundary rows refine an interval without selecting a host real representative. It sits between the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the located interval boundary of \autoref{ch:concrete-instances-located-real-interval-namecert}, and the Farey tree source of \autoref{ch:concrete-instances-stern-brocot-namecert}. The packet names only finite rational interval nodes, bisection rows, tolerance ledgers, stream windows, regular-rational readback, Real sealing, transport, replay, provenance, and local naming.

\begin{definition}[Rational interval-tree carrier]
\label{def:rational-interval-tree-carrier}
A $\RationalIntervalTreeUp$ carrier is a finite $\BHist$ packet
$$
T=(A,L,U,M,B,Q,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the finite address row naming the inspected refinement path.
\item $L$ and $U$ are the lower and upper rational boundary rows for the current node.
\item $M$ is the displayed rational midpoint or Farey mediant row used to split the current bracket.
\item $B$ is the branch ledger recording which child interval is retained at each inspected refinement step.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger for endpoint width, midpoint comparison, and child-width budget.
\item $S$ is the $\StreamNameUp$ window row through which consumers inspect the finite bracket trace.
\item $R$ is the $\RegSeqRatUp$ readback row for rational endpoints and displayed tolerance comparisons.
\item $E$ is the terminal $\RealUp$ seal row reached only after the interval tree, stream window, readback, and dyadic tolerance rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for address, endpoint, midpoint, branch, tolerance, stream, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an interval request through finite refinement, branch selection, tolerance readback, stream exposure, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\RationalIntervalTreeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedRealIntervalUp$, $\SternBrocotUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RationalIntervalTreeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It contains no selected real representative, quotient-real equality, host trichotomy, unrestricted nested-interval principle, countable-choice selector, or ambient completeness witness.
\end{definition}

\begin{theorem}[Rational interval-tree NameCert obligations]
\label{thm:rational-interval-tree-namecert-obligations}
For every accepted $\RationalIntervalTreeUp$ carrier $T=(A,L,U,M,B,Q,S,R,E,H,C,P,N)$, the local $\NameCert_{\RationalIntervalTreeUp}$ surface consists exactly of carrier admission for the finite address row, rational boundary rows, midpoint or mediant row, branch ledger, dyadic tolerance ledger, stream window, regular-rational readback, Real seal, componentwise transport, displayed replay, package provenance, and local naming. No obligation is discharged by selected real representatives, quotient equality, host trichotomy, ambient completeness, or an unlisted infinite refinement object.
\end{theorem}
\begin{proof}
Project the accepted carrier. The analytic coordinates are $A,L,U,M,B,Q,S,R,E$, and the structural coordinates are $H,C,P,N$.

The interval-facing rows determine the local obligation surface. The address row names the finite path, the boundary rows name the current bracket, the midpoint or mediant row names the split, and the branch ledger records the retained child.

The readback rows determine every downstream approximation. The dyadic tolerance ledger supplies the width and comparison budgets; the stream row exposes finite windows; the regular-rational row reads the endpoints; and the Real seal is reached only after those rows have been displayed.

Transport, replay, provenance, and naming preserve the same inventory. Since the carrier has no coordinate for selected representatives, quotient equality, trichotomy, ambient completeness, or an unlisted infinite refinement object, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Rational interval-tree bisection window]
\label{thm:rational-interval-tree-bisection-window}
Every finite bisection or Farey-refinement read of an accepted $\RationalIntervalTreeUp$ carrier factors through the displayed route
$$
A,L,U,M,B,Q,S,R,E,H,C,P,N .
$$
Before any Real-facing approximation is public, the consumer has already read the boundary pair $L,U$, the split row $M$, the retained-child ledger $B$, the $\DyadicRatCoreUp$ tolerance row $Q$, the $\StreamNameUp$ window $S$, the $\RegSeqRatUp$ readback $R$, and the terminal $\RealUp$ seal $E$. The route exports no selected limit, quotient equality, host order trichotomy, or ambient Real completeness.
\end{theorem}
\begin{proof}
Induct over the finite address row $A$. At the root window, the packet displays the initial rational boundary pair $L,U$ together with its tolerance ledger $Q$.

For a successor window, read the split row $M$ and the branch ledger $B$. The retained child interval is one of the two displayed rational brackets, and the dyadic row records the corresponding width and comparison budget.

The public approximation boundary is then forced by the readback rows. The stream window $S$ exposes the inspected finite trace, the regular-rational row $R$ reads the rational endpoints, and the Real seal $E$ is downstream of both.

Thus every finite bisection or Farey-refinement consumer sees the displayed interval-tree window before it can read a Real-facing approximation, and the excluded host-level objects have no carrier coordinate from which they could be recovered.
\end{proof}

\falsifiablePrediction{Every accepted RationalIntervalTreeUp packet exposes a finite rational boundary pair, one split row, one retained-child ledger, one dyadic tolerance row, one StreamName window, one RegSeqRat readback row, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, located-real-interval, stern-brocot: $\RationalIntervalTreeUp$ is not bijective to any listed sibling because it jointly carries finite interval-tree addresses, rational boundary rows, split rows, retained-child ledgers, dyadic tolerances, finite observation windows, regular-rational readback, Real sealing, transport, replay, provenance, and local naming in one packet.}

\closureat{RationalIntervalTreeUp}{seedStr}

\begin{closurestatus}{\RationalIntervalTreeUp}
  \constructivestory{A $\RationalIntervalTreeUp$ packet is generated from a finite address row, rational lower and upper boundary rows, a midpoint or Farey-mediant split row, retained-child ledger, DyadicRatCore tolerance row, StreamName window, RegSeqRat readback, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rational-interval-tree-carrier}, \autoref{thm:rational-interval-tree-namecert-obligations}, and \autoref{thm:rational-interval-tree-bisection-window} seed the finite rational interval-refinement tree packet between dyadic interval arithmetic, regular-rational readback, located intervals, Stern--Brocot refinement, and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected real representatives, quotient-real equality, host trichotomy, unrestricted nested-interval completeness, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, split exactness, branch retention, dyadic tolerance readback, stream-window exposure, regular-rational readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.RationalIntervalTreeUp.}
\end{closurestatus}
