\chapter{A Concrete Naming Certificate for $\SemilocalRegularCauchyModulusUp$}
\label{ch:concrete-instances-semilocal-regular-cauchy-modulus-namecert}
\origin{human}

Following Bishop and Bridges, a regular Cauchy construction may glue local modulus schedules only after their overlap requests, dyadic tolerance rows, and regular-rational readbacks are visible. The $\SemilocalRegularCauchyModulusUp$ packet records that finite semilocal modulus surface for L10 Real-and-Completion consumers. It sits between the minimum-modulus route of \autoref{ch:concrete-instances-regular-cauchy-min-modulus-namecert} and the gluing route of \autoref{ch:concrete-instances-regular-cauchy-gluing-namecert}, while still reading the finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, and terminal $\RealUp$ seal before any glued Cauchy route is exported. The packet names a displayed semilocal modulus ledger, not quotient stream equality, selected representatives, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Semilocal regular Cauchy modulus carrier]
\label{def:semilocal-regular-cauchy-modulus-carrier}
A $\SemilocalRegularCauchyModulusUp$ carrier is a finite $\BHist$ packet
$$
S=(U,V,O,M,G,W,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ and $V$ are the two local regular-Cauchy modulus schedules whose overlap is being compared.
\item $O$ is the semilocal overlap ledger, naming the finite region on which the two schedules are jointly visible.
\item $M$ is the $\RegularCauchyMinModulusUp$ row that displays the common request budget used on the overlap.
\item $G$ is the $\RegularCauchyGluingUp$ source row waiting to consume the semilocal modulus budget.
\item $W$ is the $\StreamNameUp$ finite-window row opened by the common request budget.
\item $R$ is the $\RegSeqRatUp$ readback row for the same displayed windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger comparing the local schedules, overlap row, and common request budget.
\item $E$ is the terminal $\RealUp$ seal reached only after $U,V,O,M,G,W,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the local schedules, overlap, common budget, gluing source, windows, readback, dyadic ledger, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Real-and-Completion request through $U,V,O,M,G,W,R,D,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SemilocalRegularCauchyModulusUp$, $\RegularCauchyMinModulusUp$, $\RegularCauchyGluingUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SemilocalRegularCauchyModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient stream equality, a selected common representative, countable choice over local neighborhoods, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Semilocal regular Cauchy modulus NameCert obligations]
\label{thm:semilocal-regular-cauchy-modulus-namecert-obligations}
For every accepted $\SemilocalRegularCauchyModulusUp$ carrier
$$
S=(U,V,O,M,G,W,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\SemilocalRegularCauchyModulusUp}$ surface consists exactly of carrier admission for the two local modulus schedules, semilocal overlap ledger, minimum-modulus request budget, gluing source row, finite StreamName windows, RegSeqRat readback, DyadicRatCore tolerance ledger, Real seal, componentwise transport, displayed replay, package provenance, local naming, and non-escape from quotient stream equality, selected representatives, countable choice, host equality, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:semilocal-regular-cauchy-modulus-carrier}. The local schedules are $U,V$, and the semilocal overlap row is $O$.

The common request budget is read through $M$. By the sibling minimum-modulus chapter \autoref{ch:concrete-instances-regular-cauchy-min-modulus-namecert}, that row is a displayed finite schedule rather than a selected representative or choice functional.

The gluing-facing obligation is then $G,W,R,D,E$. The gluing row waits until the finite windows, regular readback, dyadic tolerance ledger, and terminal Real seal are visible, matching the L10 dependency route of \autoref{ch:concrete-instances-regular-cauchy-gluing-namecert}.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier omits quotient stream equality, selected representatives, countable choice, host equality, and ambient completeness, the displayed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Semilocal regular Cauchy modulus gluing scope]
\label{thm:semilocal-regular-cauchy-modulus-gluing-scope}
Every gluing consumer of an accepted $\SemilocalRegularCauchyModulusUp$ packet must read the finite route
$$
U,V,O \longmapsto M,D \longmapsto G,W,R \longmapsto E \longmapsto H,C,P,N .
$$
Thus the semilocal modulus data may feed $\RegularCauchyGluingUp$ only through the displayed overlap, minimum-modulus, dyadic tolerance, finite-window, regular-readback, and Real-seal rows. It does not export quotient stream equality, selected common representatives, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an accepted carrier $S=(U,V,O,M,G,W,R,D,E,H,C,P,N)$. The two local schedules and the semilocal overlap row are the only local inputs visible to a consumer.

Apply the obligation theorem above. It forces the common request budget to pass through $M$ and the dyadic comparisons to pass through $D$ before any gluing row is consumed.

The gluing read is then $G,W,R,E$, with the finite windows and regular-rational readback exposed before the terminal Real seal. The structural rows $H,C,P,N$ keep this route attached to the same packet, so none of the excluded quotient, choice, host-equality, or completeness coordinates can be projected from the carrier.
\end{proof}

\falsifiablePrediction{A Real-and-Completion consumer that cites a semilocal regular-Cauchy modulus for gluing without displaying $U,V,O,M,G,W,R,D,E,H,C,P,N$ is not reading a $\SemilocalRegularCauchyModulusUp$ packet.}

\independenceWitness{regularcauchyminmodulus, regularcauchygluing, streamname, regseqrat, dyadicratcore, real: no listed sibling is bijective to $\SemilocalRegularCauchyModulusUp$ because this packet jointly carries two local modulus schedules, a semilocal overlap ledger, a common request budget, a gluing source row, finite windows, regular readback, dyadic tolerance, Real sealing, transport, replay, provenance, and local naming.}

\closureat{SemilocalRegularCauchyModulusUp}{seedStr}

\begin{closurestatus}{\SemilocalRegularCauchyModulusUp}
  \constructivestory{A $\SemilocalRegularCauchyModulusUp$ packet is generated from two local regular-Cauchy modulus schedules, a semilocal overlap ledger, a RegularCauchyMinModulus common request row, a RegularCauchyGluing source row, finite StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:semilocal-regular-cauchy-modulus-carrier}, \autoref{thm:semilocal-regular-cauchy-modulus-namecert-obligations}, and \autoref{thm:semilocal-regular-cauchy-modulus-gluing-scope} seed the finite semilocal modulus route consumed by regular-Cauchy gluing and Real-facing completion readers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean theorem verification, bridge checking, quotient stream equality, selected representatives, countable choice, host real equality, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, semilocal overlap exactness, minimum-modulus compatibility, gluing-source routing, StreamName and RegSeqRat readback, dyadic tolerance comparison, Real sealing, transport, replay, provenance, and nonescape for BEDC.Derived.SemilocalRegularCauchyModulusUp.}
\end{closurestatus}
