\chapter{A Concrete Naming Certificate for $\FanCauchyModulusUp$}
\label{ch:concrete-instances-fan-cauchy-modulus-namecert}
\origin{human}

Following Brouwer and Bishop--Bridges, the $\FanCauchyModulusUp$ packet records the finite fan-bar extraction route by which a decidable bar on binary choice prefixes yields one displayed Cauchy-modulus request before any regular rational readback or real seal is consumed. It sits after the fan-bar surface of \autoref{ch:concrete-instances-fantheorem-namecert}, the Cauchy-modulus interface of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the finite observation window of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The certificate is a finite NameCert reconstruction of the fan-to-Cauchy-modulus handoff; it does not claim open-cover compactness, choice, quotient streams, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Fan-Cauchy modulus carrier]
\label{def:fan-cauchy-modulus-carrier}
A $\FanCauchyModulusUp$ carrier is a finite $\BHist$ packet
$$
F=(B,K,M,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite decidable $\FanTheoremUp$ bar row, listing the inspected binary prefixes and the local decision ledger for bar membership.
\item $K$ is the prefix-bound row extracted from $B$, recording one finite depth that meets every displayed branch through the bar.
\item $M$ is the $\CauchyModulusUp$ request row, translating the prefix bound into a precision-to-window demand for a Cauchy-facing consumer.
\item $W$ is the $\StreamNameUp$ window row through which the consumer inspects exactly the prefixes and tail windows licensed by $K$ and $M$.
\item $R$ is the $\RegSeqRatUp$ handoff row, carrying the regular rational readback obtained after the window and modulus rows have been displayed.
\item $E$ is the terminal $\RealUp$ seal route, readable only after $B,K,M,W,R$ have been opened.
\item $H$ records componentwise $\hsame$ transport for bar, bound, modulus, stream window, regular readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
\FanTheoremUp\longrightarrow \CauchyModulusUp\longrightarrow \StreamNameUp\longrightarrow \RegSeqRatUp\longrightarrow \RealUp .
$$
\item $P$ records provenance over $\FanCauchyModulusUp$, $\FanTheoremUp$, $\CauchyModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FanCauchyModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,K,M,W,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, a choice functional, quotient stream equality, a selected infinite branch, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Fan-Cauchy modulus NameCert obligations]
\label{thm:fan-cauchy-modulus-namecert-obligations}
For every accepted $\FanCauchyModulusUp$ carrier
$$
F=(B,K,M,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\FanCauchyModulusUp}$ surface consists exactly of finite bar admission through $B$, prefix-bound extraction through $K$, Cauchy-modulus request formation through $M$, StreamName window exposure through $W$, RegSeqRat handoff through $R$, Real seal routing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance exhaustion through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-cauchy-modulus-carrier}. The accepted packet exposes exactly the rows $B,K,M,W,R,E,H,C,P,N$, so carrier admission is the displayed finite list.

The fan-facing obligation is supplied by $B$ and $K$. The decidable bar row lists the inspected prefixes, and the bound row records the finite depth used by all downstream requests. No separate branch selector is present.

The Cauchy-facing obligation is supplied by $M,W,R$. The modulus request names the demanded precision window, the StreamName row opens only that finite observation surface, and the RegSeqRat row reads regular rational data from the same window.

Finally, $E,H,C,P,N$ seal, transport, replay, source, and name the displayed route. Since the carrier contains no row for open-cover compactness, choice, quotient streams, host real equality, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Fan-Cauchy finite-bar extraction]
\label{thm:fan-cauchy-modulus-finite-bar-extraction}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\FanCauchyModulusUp$ carrier must pass through the displayed finite route
$$
\FanTheoremUp\longmapsto \CauchyModulusUp\longmapsto \StreamNameUp\longmapsto \RegSeqRatUp\longmapsto \RealUp
$$
using the rows $B,K,M,W,R,E,H,C,P,N$. The prefix bound $K$ is the only source of the Cauchy-modulus request $M$, and the Real seal row $E$ is not readable until the bar, bound, modulus, window, and regular-readback rows have all been displayed.
\end{theorem}
\begin{proof}
Begin with the finite bar row $B$. It supplies the decidable prefix ledger read from the fan-theorem sibling, while $K$ records the finite bound extracted from that same ledger.

Read the Cauchy-modulus request $M$ through $K$. This forces any precision demand to be a displayed finite-window demand rather than a choice functional or a branchwise host argument.

Now open $W$ and $R$. The StreamName window exposes the inspected prefixes and tail windows, and the RegSeqRat handoff reads regular rational data only from those windows.

The Real seal $E$ is then available through $H,C,P,N$. These structural rows move, replay, source, and name the same finite route, so no open-cover compactness, quotient stream, selected branch, or ambient $\RealUp$ completeness claim can enter the extraction.
\end{proof}

\closureat{FanCauchyModulusUp}{seedStr}

\begin{closurestatus}{\FanCauchyModulusUp}
  \constructivestory{A $\FanCauchyModulusUp$ packet is generated from a finite decidable FanTheorem bar row, prefix-bound row, CauchyModulus request row, StreamName window row, RegSeqRat handoff, Real seal route, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fan-cauchy-modulus-carrier}, \autoref{thm:fan-cauchy-modulus-namecert-obligations}, and \autoref{thm:fan-cauchy-modulus-finite-bar-extraction} seed the finite fan-bar to Cauchy-modulus route consumed by later regular-Cauchy and Real-seal arguments.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, choice, quotient streams, selected infinite branches, host real equality, ambient $\RealUp$ completeness, Lean verification, axiom-clean audit, or bridge checking.}
  \upgradepath{Obligation closure requires checked finite-bar admission, prefix-bound extraction, Cauchy-modulus request formation, StreamName window exposure, RegSeqRat handoff, Real seal routing, transport, replay, provenance, and local naming for BEDC.Derived.FanCauchyModulusUp.}
\end{closurestatus}
