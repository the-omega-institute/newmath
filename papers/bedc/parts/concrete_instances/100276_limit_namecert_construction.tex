\chapter{A Concrete Naming Certificate for $\LimitUp$}
\label{ch:concrete-instances-limit-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley's finite-neighborhood reading of convergence, the $\LimitUp$ packet names the generic finite limit surface used by the Real and Completion chapters. It is not a host limit operator and not a quotient of convergent streams. It is the visible route through finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, a $\DyadicRatCoreUp$ tolerance ledger, and a downstream $\RealUp$ seal row, with transport, continuation replay, provenance, and local naming retained as BEDC rows.

The sibling dependency route is deliberately shared with the specialized limit chapters. The $\RealLimitUp$ packet of \autoref{ch:concrete-instances-reallimit-namecert} and the $\CauchyLimitSealUp$ boundary of \autoref{ch:concrete-instances-cauchylimitseal-namecert} consume the same finite faces supplied by \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The generic $\LimitUp$ carrier records that common surface before any specialized real-limit or completion-facing packet chooses a narrower role.

\begin{definition}[Limit carrier]
\label{def:limit-carrier}
A $\LimitUp$ carrier is a finite $\BHist$ packet
$$
L=(S,R,D,A,T,C,H,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ window row that supplies the finite observation schedule for the limit request.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from those displayed windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the requested radius, slack, and comparison budget for each finite observation.
\item $A$ is the downstream $\RealUp$ seal row, read only after $S,R,D$ have been replayed.
\item $T$ is the limit transport row binding the same $S,R,D,A$ surface to any specialized $\RealLimitUp$ or $\CauchyLimitSealUp$ consumer.
\item $C$ records displayed $\Cont$ replay from a finite limit request through the window row, rational readback, tolerance ledger, seal row, and transport row.
\item $H$ stores componentwise $\hsame$ transport for the window, readback, tolerance ledger, seal, limit transport, continuation, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RealLimitUp$, $\CauchyLimitSealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LimitUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows, their displayed transports, their continuation replay, package provenance, and local naming provenance. The carrier has no coordinate for quotient stream equality, selected representatives, host real equality, trichotomy, countable choice, ambient completeness, or bridge checking.
\end{definition}
\leanchecked{BEDC.Derived.LimitUp.LimitTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Limit NameCert obligations]
\label{thm:limit-namecert-obligations}
For every accepted $\LimitUp$ carrier $L=(S,R,D,A,T,C,H,P,N)$, the local $\NameCert_{\LimitUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed window, readback, tolerance ledger, seal, transport, continuation, provenance, and local naming rows;
\item finite-window exactness, requiring every limit-facing read to inspect $S$ before it can reach the regular rational readback row $R$;
\item tolerance-ledger exactness, requiring every readback comparison to pass through the same $\DyadicRatCoreUp$ ledger $D$ before the $\RealUp$ seal row $A$ is exposed;
\item specialized-consumer handoff, requiring every $\RealLimitUp$ or $\CauchyLimitSealUp$ consumer to reuse the same $S,R,D,A$ route through $T,C,H,P,N$;
\item non-escape from quotient stream equality, selected representatives, host real equality, trichotomy, countable choice, ambient completeness, Lean verification, and bridge checking.
\end{enumerate}
No NameCert obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:limit-carrier}. The projection exposes exactly the finite window row, regular rational readback, dyadic tolerance ledger, Real seal, limit transport, continuation replay, componentwise transport, package provenance, and local naming row.

Finite-window exactness follows from the route order stored in $C$. A limit-facing request enters through $S$ before it can obtain $R$, and the comparison budget is then read from $D$ before the seal row $A$ can be consumed.

The specialized handoff is supplied by $T,H,P,N$. These rows retain the same displayed route for $\RealLimitUp$ and $\CauchyLimitSealUp$ consumers, so the packet has no remaining coordinate from which quotient equality, selected representatives, host equality, trichotomy, choice, completeness, Lean verification, or bridge checking could be exported.
\end{proof}
\leanchecked{BEDC.Derived.LimitUp.LimitUpTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Limit Real and Completion handoff]
\label{thm:limit-real-completion-handoff}
Every Real-facing or Completion-facing consumer of an accepted $\LimitUp$ carrier factors through the finite route
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \DyadicRatCoreUp \longrightarrow \RealUp
$$
as displayed by the packet rows $S,R,D,A$, then through the transport, continuation, provenance, and local naming rows $T,C,H,P,N$. Consequently the generic limit surface can be cited by $\RealLimitUp$ and $\CauchyLimitSealUp$ chapters without claiming quotient stream equality, selected representatives, host real equality, trichotomy, countable choice, ambient completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Begin with a Real-facing or Completion-facing consumer. Carrier admission gives access to the seal row $A$ only as a row downstream of the finite window, readback, and tolerance rows.

Replay the continuation row $C$. The consumer first reads $S$, obtains the regular rational observations through $R$, consults the dyadic tolerance ledger $D$, and only then reaches the $\RealUp$ seal row $A$.

Finally apply the handoff row $T$ together with the structural rows $H,P,N$. They repack the same finite route for $\RealLimitUp$ and $\CauchyLimitSealUp$ consumers, while adding no quotient, representative selector, host equality, trichotomy, choice, completeness, Lean target, or bridge row.
\end{proof}

\begin{theorem}[Limit RegSeqRat Bishop handoff]
\label{thm:limit-regseqrat-bishop-handoff}
Any Bishop-style $\LimitUp$ read in the current L10 route must pass through finite $\StreamNameUp$ observations, $\RegSeqRatUp$ regular-rational readback, a $\DyadicRatCoreUp$ tolerance ledger, a downstream $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\LimitUp}$ naming. For an accepted carrier $L=(S,R,D,A,T,C,H,P,N)$, the only exported route is
$$
S \longmapsto R \longmapsto D \longmapsto A \longmapsto T,C,H,P,N .
$$
The packet cannot export selected representatives, quotient stream equality, trichotomy, countable choice, host real equality, ambient completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Unfold the finite carrier of \autoref{def:limit-carrier}. The window row $S$, regular rational readback row $R$, dyadic tolerance ledger $D$, Real seal $A$, and specialized handoff row $T$ are the only content rows available to a Bishop-style limit read.

Apply the NameCert obligations of \autoref{thm:limit-namecert-obligations}. They force finite-window exactness before $R$, tolerance-ledger exactness before $A$, and specialized-consumer handoff through $T,C,H,P,N$.

The Real and Completion handoff of \autoref{thm:limit-real-completion-handoff} identifies the same route as the shared L10 naming spine for downstream consumers. Since the carrier contains no selected representative, quotient stream equality, trichotomy, countable-choice, host-equality, completeness, Lean-verification, or bridge-checking coordinate, no such object can be exported by the handoff.
\end{proof}
\leanchecked{BEDC.Derived.LimitUp.LimitRegSeqRatBishopHandoff}

\begin{definition}[Limit obligation surface]
\label{def:limit-obligation-surface}
For an accepted $\LimitUp$ carrier $L=(S,R,D,A,T,C,H,P,N)$, the \emph{obligation surface} is the finite projection ledger consisting of:
\begin{enumerate}
\item the $\StreamNameUp$ window exactness row $S$ and its replay into the $\RegSeqRatUp$ readback row $R$;
\item the $\DyadicRatCoreUp$ tolerance ledger $D$ binding every displayed comparison before the downstream $\RealUp$ seal row $A$ can be consumed;
\item the consumer handoff row $T$ to $\RealLimitUp$ and $\CauchyLimitSealUp$;
\item the structural replay rows $C,H,P,N$ for $\Cont$, componentwise $\hsame$, $\Pkg$ provenance, and local $\NameCert_{\LimitUp}$ naming.
\end{enumerate}
This surface records only finite projection and replay obligations over the carrier rows. It is not quotient stream equality, a selected representative, a host limit operator, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Limit finite-window exactness]
\label{thm:limit-finite-window-exactness}
Let $L=(S,R,D,A,T,C,H,P,N)$ be an accepted $\LimitUp$ carrier. Every finite limit read that reaches the regular rational row $R$ factors through the same displayed $\StreamNameUp$ window row $S$, and the factorization is replayed by the $\Cont$ row $C$ and transported componentwise by $H$. Thus the window obligation of \autoref{def:limit-obligation-surface} is a finite projection route through \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}.
\end{theorem}
\begin{proof}
Project the carrier $L$ and isolate the rows $S,R,C,H$. The carrier definition exposes $R$ only as the readback row downstream of the displayed window row $S$.

Replay the request through $C$. The replay ledger opens the window schedule first, then reads the regular rational observations recorded by $R$. Componentwise transport by $H$ keeps the same $S$ and $R$ rows under $\hsame$.

No further row remains from which a host stream representative, quotient equality, or ambient limit operator could be read. The exactness statement is therefore the finite projection route from $\StreamNameUp$ to $\RegSeqRatUp$.
\end{proof}

\begin{theorem}[Limit tolerance-ledger exactness]
\label{thm:limit-tolerance-ledger-exactness}
Let $L=(S,R,D,A,T,C,H,P,N)$ be an accepted $\LimitUp$ carrier. Every readback comparison that reaches the $\RealUp$ seal row $A$ passes through the same $\DyadicRatCoreUp$ tolerance ledger $D$, with replay and transport recorded by $C,H,P,N$. Thus the tolerance obligation of \autoref{def:limit-obligation-surface} is a finite replay route through \autoref{ch:concrete-instances-dyadicratcore-namecert} and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Start from a comparison exported by the accepted carrier. The rows $R,D,A$ are ordered as readback, tolerance ledger, and downstream Real seal, so a comparison cannot inspect $A$ before the dyadic budget row $D$ is displayed.

Use $C$ to replay the comparison route and $H$ to transport the same row package. The provenance row $P$ records that the route is sourced in $\DyadicRatCoreUp$ and $\RealUp$, while $N$ names the local packet.

The carrier has no coordinate for trichotomy, countable choice, host real equality, or ambient completeness. The tolerance obligation is exactly the finite ledger route from $D$ to $A$.
\end{proof}

\begin{theorem}[Limit consumer handoff exactness]
\label{thm:limit-consumer-handoff-exactness}
Let $L=(S,R,D,A,T,C,H,P,N)$ be an accepted $\LimitUp$ carrier. Every $\RealLimitUp$ or $\CauchyLimitSealUp$ consumer of $L$ receives only the finite route
$$
S \longmapsto R \longmapsto D \longmapsto A \longmapsto T,C,H,P,N .
$$
Consequently the consumer obligation of \autoref{def:limit-obligation-surface} is a finite handoff through \autoref{ch:concrete-instances-reallimit-namecert} and \autoref{ch:concrete-instances-cauchylimitseal-namecert}, not a claim of quotient stream equality, selected representatives, host real equality, ambient completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Read the consumer through the transport row $T$. That row binds $\RealLimitUp$ and $\CauchyLimitSealUp$ consumers to the same upstream rows $S,R,D,A$ displayed by the generic $\LimitUp$ packet.

Replay the route using $C$ and transport it using $H$. The provenance and local naming rows $P,N$ record the same dependency spine through $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RealLimitUp$, and $\CauchyLimitSealUp$.

Because the carrier is exhausted by these rows, the consumer cannot acquire an external stream quotient, selected representative, host real equality proof, completeness principle, Lean target, or bridge row. The handoff exactness is therefore a finite replay obligation.
\end{proof}

\closureat{LimitUp}{obligationStr}

\begin{closurestatus}{\LimitUp}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:limit-carrier}, \autoref{thm:limit-namecert-obligations}, \autoref{def:limit-obligation-surface}, \autoref{thm:limit-finite-window-exactness}, \autoref{thm:limit-tolerance-ledger-exactness}, and \autoref{thm:limit-consumer-handoff-exactness} display the finite obligation surface shared by Real and Completion consumers.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.LimitUp.LimitRegSeqRatBishopHandoff}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected representatives, host real equality, trichotomy, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires binding these displayed obligations to BEDC primitives for StreamName, RegSeqRat, DyadicRatCore, Real, RealLimit, CauchyLimitSeal, hsame, Cont, Pkg, and NameCert.}
  \constructivestory{A $\LimitUp$ packet is generated from StreamName windows, RegSeqRat readbacks, a DyadicRatCore tolerance ledger, a RealUp seal row, a limit transport row for RealLimitUp and CauchyLimitSealUp consumers, continuation replay, componentwise transport, package provenance, and local naming.}
\end{closurestatus}
