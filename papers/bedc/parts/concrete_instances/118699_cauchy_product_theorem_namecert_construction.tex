\chapter{A Concrete Naming Certificate for $\CauchyProductTheoremUp$}
\label{ch:concrete-instances-cauchy-product-theorem-namecert}
\origin{human}

Following Bishop and Bridges on constructive real arithmetic, the $\CauchyProductTheoremUp$ packet records the finite L10 route saying that two displayed regular Cauchy rational sources admit a product name once the source windows, dyadic product ledger, regular readback, and terminal Real seal are all visible. It sits between the local product-budget packet of \autoref{ch:concrete-instances-regularcauchyproductbudget-namecert}, the regular rational source surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the finite product theorem corridor for regular Cauchy names; it does not assert quotient real multiplication, selected limits, trichotomy, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy product theorem carrier]
\label{def:cauchy-product-theorem-carrier}
A $\CauchyProductTheoremUp$ carrier is a finite $\BHist$ packet
$$
K=(A,B,S,T,D,L,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two $\RegSeqRatUp$ source rows. Each row exposes the finite rational windows and regularity witnesses consumed by the product request.
\item $S$ and $T$ are paired $\StreamNameUp$ window rows for the same two sources, indexed by the visible tolerance requests used by the product route.
\item $D$ is the $\DyadicRatCoreUp$ product row. It records the finite dyadic product cells, denominator refinements, and tolerance comparisons needed to multiply the inspected source windows.
\item $L$ is the $\FiniteErrorBudgetUp$ ledger binding the source moduli, dyadic product row, and terminal tolerance request into one displayed error account.
\item $R$ is the $\RegSeqRatUp$ product handoff row obtained from $A,B,S,T,D,L$.
\item $E$ is the terminal $\RealUp$ seal reached only after the product handoff row $R$ is visible.
\item $H$ records componentwise $\hsame$ transport for the source rows, stream windows, dyadic product row, error ledger, product handoff, Real seal, replay, provenance, and local naming row.
\item $C$ records displayed $\Cont$ replay from a product-theorem request through $A,B,S,T,D,L,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchyProductTheoremUp$, $\RegularCauchyProductBudgetUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\FiniteErrorBudgetUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyProductTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows and their replay path. It has no coordinate for quotient equality of completed reals, host real multiplication, selected limiting products, countable-choice schedules, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy product theorem finite route]
\label{thm:cauchy-product-theorem-finite-route}
For every accepted $\CauchyProductTheoremUp$ carrier $K=(A,B,S,T,D,L,R,E,H,C,P,N)$, every $\RealUp$-facing product read factors through the finite route
$$
A,B \longmapsto S,T \longmapsto D,L \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Thus the theorem packet exposes two $\RegSeqRatUp$ source rows, paired $\StreamNameUp$ windows, a $\DyadicRatCoreUp$ product row, a $\FiniteErrorBudgetUp$ ledger, the $\RegSeqRatUp$ product handoff, the terminal $\RealUp$ seal, and the $\hsame/\Cont/\Pkg/\NameCert$ support rows before any product name can be consumed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-product-theorem-carrier}. The accepted packet exposes exactly the two source rows, the paired stream windows, the dyadic product row, the finite error ledger, the product handoff row, and the terminal Real seal.

The source stage is $A,B,S,T$. The two $\RegSeqRatUp$ rows supply the rational Cauchy data, while the two $\StreamNameUp$ rows display the finite windows requested by the product consumer.

The arithmetic stage is $D,L$. The $\DyadicRatCoreUp$ row records the finite product cells and tolerance comparisons, and the $\FiniteErrorBudgetUp$ ledger binds those comparisons to the source moduli and the requested output tolerance.

The handoff stage is $R,E$. The product handoff row repacks the displayed budget as a regular rational product name, and the terminal $\RealUp$ seal is reached only after that row is visible.

Finally $H,C,P,N$ transport, replay, package, and locally name the same route. Since no excluded object is a carrier coordinate, a product consumer cannot bypass the displayed regular source rows, stream windows, dyadic product row, error ledger, handoff, or Real seal.
\end{proof}

\begin{theorem}[Cauchy product theorem budget support]
\label{thm:cauchy-product-theorem-budget-support}
Every accepted $\CauchyProductTheoremUp$ packet consumes the $\RegularCauchyProductBudgetUp$ surface before its product handoff row can be read as $\RegSeqRatUp$ data. In the carrier $K=(A,B,S,T,D,L,R,E,H,C,P,N)$, the budget surface is read through $A,B,S,T,D,L$, while $R,E,H,C,P,N$ only expose the resulting regular product name, Real seal, and structural support.
\end{theorem}
\begin{proof}
Start with the finite route of \autoref{thm:cauchy-product-theorem-finite-route}. Its first two stages contain the source regular Cauchy rows, paired stream windows, dyadic product arithmetic, and finite error ledger.

These are exactly the rows inspected by the $\RegularCauchyProductBudgetUp$ packet: source moduli, product cells, and tolerance bookkeeping appear before the product handoff row $R$ is available.

The remaining rows do not create a second product computation. They read back the displayed budget as a regular product name, seal it as $\RealUp$ data, and preserve the route under transport, replay, provenance, and local naming.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyProductTheoremUp$ consumer that reaches a terminal $\RealUp$ seal while omitting a $\RegSeqRatUp$ source row, a $\StreamNameUp$ window row, the $\DyadicRatCoreUp$ product row, the $\FiniteErrorBudgetUp$ ledger, the $\RegSeqRatUp$ product handoff, transport, replay, provenance, or local naming row refutes the packet.}

\independenceWitness{regularcauchyproductbudget, regseqrat, streamname, dyadicratcore, real: no listed sibling supplies the whole theorem packet $K=(A,B,S,T,D,L,R,E,H,C,P,N)$, because each sibling contributes only the budget, source readback, stream windows, arithmetic ledger, or terminal seal while this chapter binds them as one finite Cauchy product theorem certificate.}

\closureat{CauchyProductTheoremUp}{seedStr}

\begin{closurestatus}{\CauchyProductTheoremUp}
  \constructivestory{A $\CauchyProductTheoremUp$ packet is generated from two RegSeqRat source rows, paired StreamName windows, a DyadicRatCore product row, a FiniteErrorBudget ledger, a RegSeqRat product handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-product-theorem-carrier}, \autoref{thm:cauchy-product-theorem-finite-route}, and \autoref{thm:cauchy-product-theorem-budget-support} seed the finite Cauchy product theorem route from regular Cauchy source rows to stream windows, dyadic product arithmetic, finite error budgeting, regular product handoff, Real sealing, transport, replay, provenance, and local NameCert data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real multiplication, selected limits, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, source-window stability, dyadic product arithmetic, finite error budgeting, RegSeqRat product handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyProductTheoremUp.}
\end{closurestatus}
