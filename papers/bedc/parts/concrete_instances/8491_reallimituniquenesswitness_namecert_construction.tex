\chapter{A Concrete Naming Certificate for $\RealLimitUniquenessWitnessUp$}
\label{ch:concrete-instances-real-limit-uniqueness-witness-namecert}
\origin{human}

The $\RealLimitUniquenessWitnessUp$ packet realizes the separated uniqueness clause of \autoref{ch:visions-real-completeness} as a finite diagonal-comparison witness. It sits after the real naming surface of \autoref{ch:concrete-instances-real-namecert}, the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the two-seal uniqueness route of \autoref{ch:concrete-instances-real-limit-uniqueness-seal-namecert}. Its carrier records the point at which two diagonal limit candidates are compared through the same finite windows, not a quotient of regular streams and not a host equality of completed real numbers.

\begin{definition}[Real limit uniqueness witness carrier]
\label{def:real-limit-uniqueness-witness-carrier}
A $\RealLimitUniquenessWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(D_0,D_1,Q,S,R_0,R_1,E,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D_0$ and $D_1$ are the two diagonal $\RegSeqRatUp$ candidate rows read from the same regular-Cauchy source;
\item $Q$ is the finite diagonal-comparison row bounding the pointwise difference of the two candidates;
\item $S$ is the shared $\StreamNameUp$ window row through which the comparison is observed;
\item $R_0$ and $R_1$ are the two terminal $\RealUp$ seal rows reached from the compared regular-rational candidates;
\item $E$ is the separated RealUp classifier row witnessing the $\psame$ identification of the two seals;
\item $V$ is the sibling $\RealLimitUniquenessSealUp$ route row that packages the same two-seal comparison as a public uniqueness surface;
\item $H$ records componentwise $\hsame$ transport for the diagonal rows, comparison row, finite window, two RealUp seals, separated classifier, seal route, replay route, provenance row, and local naming row;
\item $C$ records the displayed $\Cont$ route from $D_0,D_1$ through $Q,S,R_0,R_1,E,V$;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:visions-real-completeness}, \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-limit-uniqueness-seal-namecert}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RealLimitUniquenessWitnessUp}$ row.
\end{enumerate}
The carrier has no coordinate for selected host limits, quotient stream equality, countable choice, ambient Real completeness, or host Real equality.
\end{definition}

\begin{theorem}[Real limit uniqueness witness obligation surface]
\label{thm:real-limit-uniqueness-witness-obligation-surface}
For the carrier of \autoref{def:real-limit-uniqueness-witness-carrier}, the local $\NameCert_{\RealLimitUniquenessWitnessUp}$ surface consists exactly of carrier admission for
$$
W=(D_0,D_1,Q,S,R_0,R_1,E,V,H,C,P,N),
$$
diagonal candidate exposure through $D_0,D_1$, finite comparison through $Q$, finite window exposure through $S$, terminal $\RealUp$ seal exposure through $R_0,R_1$, separated classifier exposure through $E$, the $\RealLimitUniquenessSealUp$ handoff through $V$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier rows. The tuple supplies exactly the two diagonal candidate rows, their comparison row, the observation window, the two RealUp seals, the separated classifier, the uniqueness-seal handoff, transport, replay, provenance, and local naming.

Read the comparison in dependency order. The diagonal candidates must be exposed before the finite comparison row can be interpreted, the window row must be exposed before the comparison is observed, and the two RealUp seals must be reached before the separated classifier row is consumed.

The remaining structural rows preserve and name the same route. Since the carrier has no coordinate for quotient streams, selected host limits, ambient completeness, choice, or host equality, the displayed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Real limit uniqueness witness nonescape]
\label{thm:real-limit-uniqueness-witness-nonescape}
Every accepted $\RealLimitUniquenessWitnessUp$ consumer that reads uniqueness of two Real-completion candidates factors through the finite route
$$
D_0,D_1,Q,S,R_0,R_1,E,V,H,C,P,N.
$$
The consumer cannot replace this route by a single $\RealUp$ seal, a quotient-stream equality, a selected host limit, an ambient Real-completeness theorem, or a host Real equality judgment.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:real-limit-uniqueness-witness-obligation-surface}. It gives every accepted uniqueness read the two diagonal candidates, their finite comparison row, the shared observation window, the two terminal RealUp seals, the separated classifier row, the uniqueness-seal handoff, and the structural rows.

Single-endpoint escape is impossible because neither $R_0$ nor $R_1$ alone contains the paired diagonal comparison $Q$ or the separated classifier row $E$. The witness is therefore a two-candidate packet, not a detached real-name seal.

Ambient equality escape is also impossible. Quotient streams, selected host limits, global completeness, and host Real equality are absent from the carrier, so every consumer read remains on the displayed diagonal-comparison route.
\end{proof}

\begin{theorem}[Real limit uniqueness witness consumer handoff]
\label{thm:real-limit-uniqueness-witness-consumer-handoff}
Every $\RealUp$-facing or completion-facing consumer of an accepted $\RealLimitUniquenessWitnessUp$ packet receives a finite diagonal-comparison, seal, classifier, and NameCert witness packet. The handoff exposes $D_0,D_1,Q,S,R_0,R_1,E,V,H,C,P,N$ and no selected limit, quotient stream, host equality, detached selector, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
Use the obligation surface to identify the packet given to the consumer. The consumer receives the two candidate diagonals, finite comparison, shared window, two RealUp seals, separated classifier, RealLimitUniquenessSeal route, transport, replay, provenance, and local naming.

Then apply nonescape. The packet's uniqueness read is already bounded by the paired diagonal-comparison route, so a downstream RealUp or completion consumer cannot import a host equality or quotient comparison after the handoff.

Thus the handoff is exactly the finite witness packet required by the Real-completeness uniqueness clause, with every dependency visible through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{proof}

\closureat{RealLimitUniquenessWitnessUp}{seedStr}

\begin{closurestatus}{\RealLimitUniquenessWitnessUp}
  \constructivestory{A $\RealLimitUniquenessWitnessUp$ packet is generated from two diagonal RegSeqRat candidate rows, a finite comparison row, a StreamName window row, two RealUp seals, a separated RealUp classifier, a RealLimitUniquenessSeal route, componentwise transports, displayed continuation, package provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-limit-uniqueness-witness-carrier}, \autoref{thm:real-limit-uniqueness-witness-obligation-surface}, \autoref{thm:real-limit-uniqueness-witness-nonescape}, and \autoref{thm:real-limit-uniqueness-witness-consumer-handoff} specify the finite diagonal-comparison witness surface and its nonescape boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not prove Real completeness, selected-limit existence, quotient stream equality, host Real equality, Lean verification, or bridge checking for $\RealLimitUniquenessWitnessUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, diagonal comparison exposure, finite window exposure, two RealUp seal exposure, separated classifier exposure, uniqueness-seal handoff, nontriviality, and field faithfulness for the same packet.}
\end{closurestatus}
