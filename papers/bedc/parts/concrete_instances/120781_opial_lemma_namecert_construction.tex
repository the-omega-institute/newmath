\chapter{A Concrete Naming Certificate for $\OpialLemmaUp$}
\label{ch:concrete-instances-opial-lemma-namecert}
\origin{human}

Following Opial's weak-convergence lemma, the $\OpialLemmaUp$ packet records the finite certificate surface by which a Hilbert-facing weak-limit row and a strict norm-distance separation ledger can be inspected without importing a host weak compactness selector. It sits after the Opial-condition packet of \autoref{ch:concrete-instances-opial-condition-namecert}, the Hilbert carrier of \autoref{ch:concrete-instances-hilbert-namecert}, the norm and metric surfaces of \autoref{ch:concrete-instances-norm-namecert} and \autoref{ch:concrete-instances-metric-namecert}, the scalar readback of \autoref{ch:concrete-instances-real-namecert}, and the weak compactness interface of \autoref{ch:concrete-instances-weak-compactness-namecert}. The packet names weak-limit and separation data only as displayed NameCert rows; it exports no selected weak cluster point, quotient equality, classical trichotomy, or ambient fixed-point theorem.

\begin{definition}[Opial lemma carrier]
\label{def:opial-lemma-carrier}
An $\OpialLemmaUp$ carrier is a finite $\BHist$ packet
$$
L=(H,N,M,R,W,S,B,A,D,T,C,P,K)
$$
with the following displayed rows:
\begin{enumerate}
\item $H$ is the $\HilbertUp$ source row, exposing only the inner-product, induced norm, and weak-observation surfaces visible to the packet.
\item $N$ is the $\NormUp$ row that reads norm distances for the displayed Hilbert endpoints.
\item $M$ is the $\MetricUp$ row induced by the norm-distance ledger.
\item $R$ is the $\RealUp$ scalar readback row for all displayed inequalities and strict-separation margins.
\item $W$ is the $\WeakCompactnessUp$ handoff row, naming the weak-observation window while refusing any selected weak compactness witness.
\item $S$ is the bounded sequence row whose entries are read through $H,N,M,R$.
\item $B$ is the candidate weak-limit row, displayed as a named endpoint of the same Hilbert carrier and not as the result of a choice principle.
\item $A$ is the alternative endpoint row used for comparison against the candidate weak limit.
\item $D$ is the strict separation ledger
$$
\liminf_n \|S_n-B\| < \liminf_n \|S_n-A\|
$$
read through the norm, metric, and Real rows.
\item $T$ records componentwise $\hsame$ transport for the Hilbert, norm, metric, Real, weak compactness, sequence, weak-limit, alternative endpoint, and strict-separation rows.
\item $C$ records displayed $\Cont$ replay from an Opial-lemma request through
$$
H,N,M,R,W,S,B,A,D \longmapsto T,P,K .
$$
\item $P$ is the $\Pkg$ provenance over $\OpialLemmaUp$, $\HilbertUp$, $\NormUp$, $\MetricUp$, $\RealUp$, $\WeakCompactnessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $K$ is the local $\NameCert_{\OpialLemmaUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for a host weak compactness selector, quotient equality of weak limits or Hilbert endpoints, classical trichotomy for arbitrary reals, a demiclosedness theorem, or a fixed-point theorem.
\end{definition}

\begin{theorem}[Opial lemma NameCert obligations]
\label{thm:opial-lemma-namecert-obligations}
For every accepted $\OpialLemmaUp$ carrier
$$
L=(H,N,M,R,W,S,B,A,D,T,C,P,K),
$$
the local $\NameCert_{\OpialLemmaUp}$ surface consists exactly of Hilbert source admission, norm-distance admission, metric-distance readback, Real scalar readback, weak-observation handoff, bounded-sequence exposure, candidate weak-limit exposure, alternative-endpoint exposure, strict-separation exposure, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:opial-lemma-carrier}. The packet exposes precisely the rows $H,N,M,R,W,S,B,A,D,T,C,P,K$, so every obligation must be read from that finite inventory.

The analytic source rows are read first. The $\HilbertUp$ row is interpreted through \autoref{ch:concrete-instances-hilbert-namecert}; its norm and metric observations are routed through \autoref{ch:concrete-instances-norm-namecert} and \autoref{ch:concrete-instances-metric-namecert}; and every comparison value is sealed by the $\RealUp$ readback of \autoref{ch:concrete-instances-real-namecert}.

The weak side is then confined to $W,S,B,A$. The $\WeakCompactnessUp$ handoff of \autoref{ch:concrete-instances-weak-compactness-namecert} supplies only a finite weak-observation window, while the sequence, candidate weak-limit row, and alternative endpoint remain displayed coordinates of the same packet.

Finally $D,T,C,P,K$ bind the strict-separation and structural obligations. The strict ledger records the displayed norm-distance inequality, and the transport, replay, provenance, and local naming rows preserve only the stated coordinates. Hence no host weak compactness selector, quotient equality, classical trichotomy, or ambient fixed-point theorem is exported.
\end{proof}

\begin{theorem}[Opial lemma weak-limit separation]
\label{thm:opial-lemma-weak-limit-separation}
Every $\OpialLemmaUp$ consumer that reads a candidate weak limit from an accepted carrier must also read the Hilbert source row $H$, norm-distance row $N$, metric row $M$, Real scalar row $R$, weak compactness handoff $W$, bounded sequence row $S$, alternative endpoint row $A$, and strict-separation ledger $D$. The exported conclusion is only the displayed weak-limit distance separation inside the packet; it is not a selected weak cluster point, quotient equality of endpoint representatives, classical real trichotomy, or host fixed-point principle.
\end{theorem}
\begin{proof}
Start with the candidate weak-limit read. By \autoref{thm:opial-lemma-namecert-obligations}, the candidate row $B$ is visible only together with the source rows $H,N,M,R,W,S,A,D$ and the structural rows $T,C,P,K$.

The distance comparison is routed through the named siblings. The Hilbert source provides the endpoint surface, the norm and metric chapters provide the distance observations, and the Real chapter supplies the scalar comparison row in which the strict inequality is read.

The weak compactness handoff stays local. The row $W$ refers to the finite weak-observation interface of \autoref{ch:concrete-instances-weak-compactness-namecert}, so the consumer may inspect the displayed weak-limit candidate but may not extract a selected weak compactness witness.

It remains to identify the export boundary. Since the carrier has no coordinate for quotient equality, classical trichotomy, demiclosedness, or fixed-point data, the public read is exactly the strict weak-limit separation ledger preserved by $T,C,P,K$.
\end{proof}

\falsifiablePrediction{Every accepted $\OpialLemmaUp$ read must display $\HilbertUp$, $\NormUp$, $\MetricUp$, $\RealUp$, $\WeakCompactnessUp$, bounded-sequence, candidate weak-limit, alternative-endpoint, strict-separation, transport, replay, provenance, and local $\NameCert$ rows before any weak-limit separation is public. A read that exports a host weak compactness selector, quotient equality, classical trichotomy, demiclosedness, or a fixed-point theorem from this packet refutes the carrier boundary.}

\independenceWitness{opial-condition, hilbert, norm, metric, real, weak-compactness: none is bijective with $\OpialLemmaUp$ because this carrier jointly binds a Hilbert source, norm-distance ledger, metric readback, Real strict comparison, weak-observation handoff, bounded sequence, candidate weak-limit row, alternative endpoint row, strict-separation ledger, transport, replay, provenance, and local NameCert row.}

\closureat{OpialLemmaUp}{seedStr}

\begin{closurestatus}{\OpialLemmaUp}
  \constructivestory{An $\OpialLemmaUp$ packet is generated from a Hilbert source, norm-distance and metric rows, Real scalar readback, weak compactness handoff, bounded sequence, candidate weak-limit row, alternative endpoint row, strict-separation ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:opial-lemma-carrier}, \autoref{thm:opial-lemma-namecert-obligations}, and \autoref{thm:opial-lemma-weak-limit-separation} seed the finite Opial-lemma route from displayed weak-limit data to strict norm-distance separation.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host weak compactness selector, quotient equality, classical trichotomy, demiclosedness, fixed-point existence, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, weak-limit row exposure, norm-distance and Real strict-separation exactness, weak compactness handoff stability, and BEDC.Derived.OpialLemmaUp formalization.}
\end{closurestatus}
