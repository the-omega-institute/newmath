\chapter{A Concrete Naming Certificate for $\BaireClassThreeRealFunctionUp$}
\label{ch:concrete-instances-baire-class-three-real-function-namecert}
\origin{human}

Following Baire and Bishop--Bridges constructive analysis, the $\BaireClassThreeRealFunctionUp$ packet records a real-valued function as a displayed pointwise limit of $\BaireClassTwoRealFunctionUp$ rows. It consumes the Baire class two sibling of \autoref{ch:concrete-instances-baire-class-two-real-function-namecert}, the pointwise-limit handoff of \autoref{ch:concrete-instances-pointwise-limit-namecert}, the continuous-map observation discipline of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite $\StreamNameUp$ schedules of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives later oscillation and rank consumers a third finite pointwise layer without importing quotient function equality, selected representatives, countable choice, arbitrary measurability, or ambient completeness of a host function space.

\begin{definition}[Baire class three real-function carrier]
\label{def:baire-class-three-real-function-carrier}
A $\BaireClassThreeRealFunctionUp$ carrier is a finite $\BHist$ packet
$$
F=(X,S,B,L,O,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source real-domain row whose point requests are read through $\RealUp$ names.
\item $S$ is the $\StreamNameUp$ schedule listing the inspected stages of the outer pointwise sequence.
\item $B$ assigns each inspected outer stage to a displayed $\BaireClassTwoRealFunctionUp$ source row.
\item $L$ is the $\PointwiseLimitUp$ handoff comparing the stage outputs only through finite pointwise windows.
\item $O$ is the observation row: it records the $\ContinuousMapUp$-compatible point request surface and the $\StreamNameUp$ windows inspected before an output value can be read.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the observed stage outputs and tolerance requests.
\item $E$ is the terminal $\RealUp$ seal for the displayed third-level pointwise value.
\item $H$ records componentwise $\hsame$ transport for the source, schedule, Baire-two stage, pointwise-limit, observation, readback, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a point request through $X,S,B,L,O,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BaireClassThreeRealFunctionUp$, $\BaireClassTwoRealFunctionUp$, $\PointwiseLimitUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BaireClassThreeRealFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for host equality of functions, quotient function spaces, selected representatives, countable choice, arbitrary measurability, or an ambient theorem identifying all Baire class three maps.
\end{definition}

\begin{theorem}[Baire class three real-function NameCert obligations]
\label{thm:baire-class-three-real-function-namecert-obligations}
For every accepted $\BaireClassThreeRealFunctionUp$ carrier $F=(X,S,B,L,O,R,E,H,C,P,N)$, the local $\NameCert_{\BaireClassThreeRealFunctionUp}$ surface consists exactly of carrier admission for the source real-domain row, the $\StreamNameUp$ outer stage schedule, the $\BaireClassTwoRealFunctionUp$ stage rows, the $\PointwiseLimitUp$ handoff, the $\ContinuousMapUp$-compatible observation row, the $\RegSeqRatUp$ readback, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every downstream oscillation, rank, Real, or descriptive-analysis consumer must consume those rows before treating the packet as a Baire class three real-valued function.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:baire-class-three-real-function-carrier}. The accepted packet exposes only the source row, outer stage schedule, Baire-two stage rows, pointwise-limit handoff, observation row, regular-rational readback, Real seal, transport, replay, provenance, and local naming.

The third pointwise layer starts with $X$ and $S$. A consumer first names the point request and the inspected outer stages, then reads the displayed $\BaireClassTwoRealFunctionUp$ row at each inspected stage.

The output route is then forced through $L,O,R,E$. The pointwise-limit row records the finite comparison windows, the observation row keeps the point request on the displayed $\ContinuousMapUp$ and $\StreamNameUp$ surfaces, the regular-rational row records the numerical readback, and the Real seal records the terminal pointwise value.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no coordinate stores host function equality, a quotient function space, selected representatives, countable choice, arbitrary measurability, or an ambient classification theorem, the listed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Baire class three pointwise ladder]
\label{thm:baire-class-three-real-function-pointwise-ladder}
Every $\RealUp$, pointwise-limit, oscillation-rank, or descriptive-analysis consumer of an accepted $\BaireClassThreeRealFunctionUp$ carrier must read the finite route
$$
\StreamNameUp
\longmapsto
\BaireClassTwoRealFunctionUp
\longmapsto
\PointwiseLimitUp
\longmapsto
\ContinuousMapUp,\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
Thus the Baire class three row is a displayed pointwise sequence of Baire class two real-valued maps with finite observation and readback, not a host function equality class, a selected third-level limit map, or an imported measurable-function hierarchy.
\end{theorem}
\begin{proof}
Begin with a downstream consumer. Carrier admission forces the consumer to read the $\StreamNameUp$ schedule before any stage of the outer approximating sequence can be inspected.

The inspected stage is then a $\BaireClassTwoRealFunctionUp$ row. Its output is not exposed as a host function value; it passes through the $\PointwiseLimitUp$ handoff and the observation row that binds point requests to $\ContinuousMapUp$ and $\StreamNameUp$ surfaces.

The readback route then reaches $R$ and $E$. The $\RegSeqRatUp$ row records the finite numerical windows, and the terminal $\RealUp$ seal is downstream of the same displayed pointwise ladder.

Transport and replay are supplied by $H$ and $C$, while $P$ and $N$ keep the provenance and local name attached to the same finite tuple. Consequently the consumer receives only the displayed pointwise route over named sibling packets.

The carrier inventory excludes host function equality, quotient function spaces, selected pointwise limits, countable choice, arbitrary measurability, imported hierarchy theorems, and ambient $\RealUp$ completeness, so the ladder is exactly the finite BEDC route stated above.
\end{proof}

\falsifiablePrediction{An accepted $\BaireClassThreeRealFunctionUp$ consumer is refuted if it treats a third-level pointwise limit as public before displaying the StreamName outer schedule, BaireClassTwo stage rows, PointwiseLimit handoff, ContinuousMap-compatible observation row, RegSeqRat readback, Real seal, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{baireclasstwo, pointwiselimit, continuousmap, streamname, regseqrat, real: none is bijective because $\BaireClassThreeRealFunctionUp$ binds a real-domain source, explicit outer stage schedule, Baire-two stage rows, pointwise-limit handoff, observation row, numerical readback, Real seal, transport, replay, provenance, and local NameCert rows in one packet. Each sibling supplies one constituent surface, while only this packet names the third pointwise layer.}

\closureat{BaireClassThreeRealFunctionUp}{seedStr}

\begin{closurestatus}{\BaireClassThreeRealFunctionUp}
  \constructivestory{A $\BaireClassThreeRealFunctionUp$ packet is generated from a RealUp source row, a StreamName outer stage schedule, BaireClassTwoRealFunction stage rows, a PointwiseLimit handoff, ContinuousMap-compatible observation rows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:baire-class-three-real-function-carrier}, \autoref{thm:baire-class-three-real-function-namecert-obligations}, and \autoref{thm:baire-class-three-real-function-pointwise-ladder} seed the explicit pointwise-sequence-of-Baire-two-functions route over BaireClassTwoRealFunctionUp, PointwiseLimitUp, ContinuousMapUp, StreamNameUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host function equality, quotient function spaces, selected pointwise limits, countable choice, arbitrary measurability, oscillation ranks, or a full Baire hierarchy.}
  \upgradepath{Obligation closure requires checked carrier admission, outer stage schedule exactness, Baire-two stage exposure, pointwise-limit handoff, observation-row compatibility, pointwise readback, Real seal nonescape, componentwise transport, displayed replay, provenance, and local naming for BEDC.Derived.BaireClassThreeRealFunctionUp.}
\end{closurestatus}
