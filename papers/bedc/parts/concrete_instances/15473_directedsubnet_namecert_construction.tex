\chapter{A Concrete Naming Certificate for $\DirectedSubnetUp$}
\label{ch:concrete-instances-directedsubnet-namecert}
\origin{human}

Following Kelley-style nets and Bourbaki general topology, the $\DirectedSubnetUp$ packet records cofinal subnet reindexing as finite BEDC rows rather than as a quotient of nets or a choice of subnet representatives. It sits between the source directed-window surface of \autoref{ch:concrete-instances-cauchynet-namecert} and the downstream limit consumer of \autoref{ch:concrete-instances-cauchynetlimit-namecert}. Its displayed reindexing route also reads the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal seal boundary of \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[DirectedSubnet carrier]
\label{def:directed-subnet-carrier}
A $\DirectedSubnetUp$ carrier is a finite $\BHist$ packet
$$
M=(I,J,\phi,K,L,S,R,D,A,H,C,P,N)
$$
over $\CauchyNetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite source directed-window ledger whose entries are the source windows inspected by the packet.
\item $J$ is the finite target directed-window ledger whose entries name the subnet windows requested by downstream consumers.
\item $\phi:J\to I$ is the displayed cofinal map from target directed windows into source directed windows. Its cofinal coverage row records, for each displayed source window in $I$, a target window in $J$ whose image lies above it inside the source directed order recorded by $K$.
\item $K$ is the accepted source $\CauchyNetUp$ row read on the source windows named by $I$.
\item $L$ is the target or subnet $\CauchyNetUp$ row whose windows are obtained by replaying $K$ along $\phi$.
\item $S$ records the $\StreamNameUp$ windows used to read both the source observations and their target replay.
\item $R$ records the $\RegSeqRatUp$ readback obtained from the scheduled windows.
\item $D$ records the $\DyadicRatCoreUp$ tolerance ledger consulted when the source and target readings are compared.
\item $A$ is the $\RealUp$ seal row exposed only after $K,\phi,L,S,R,D$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for the source, target, map, window, readback, tolerance, seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a target-window request through $\phi$ into the source CauchyNet row and then back into the target CauchyNet row.
\item $P$ is the $\Pkg$ provenance over the listed BEDC dependencies.
\item $N$ is the local $\NameCert_{\DirectedSubnetUp}$ row.
\end{enumerate}
The carrier exports no quotient of nets, no selected limit, no cofinal choice operator, no host real equality, and no ambient completeness theorem.
\end{definition}
\leanchecked{BEDC.Derived.DirectedSubnetUp.DirectedSubnetTasteGate\_single\_carrier\_alignment}

\begin{theorem}[DirectedSubnet NameCert obligations]
\label{thm:directed-subnet-namecert-obligations}
For every accepted $\DirectedSubnetUp$ carrier $M=(I,J,\phi,K,L,S,R,D,A,H,C,P,N)$, the local $\NameCert_{\DirectedSubnetUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed source ledger, target ledger, cofinal map, source CauchyNet row, target CauchyNet row, window, readback, tolerance, seal, transport, continuation, provenance, and local naming rows;
\item cofinal-map coverage, requiring every displayed source window requested by the packet to be reached by a target window through the row $\phi:J\to I$;
\item CauchyNet handoff, requiring the target row $L$ to be read only by replaying the source row $K$ along $\phi$ and the displayed window, readback, and tolerance rows $S,R,D$;
\item Real-seal non-escape, requiring the seal row $A$ to remain downstream of $K,\phi,L,S,R,D$;
\item ledger refusal, excluding quotient-net equality, selected subnet limits, cofinal choice outside $\phi$, host real equality, and ambient completeness.
\end{enumerate}
No obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.DirectedSubnetUp.DirectedSubnetNameCertObligations}
\begin{proof}
Carrier admission is projection from \autoref{def:directed-subnet-carrier}. The source and target ledgers, the displayed cofinal map, both CauchyNet rows, the StreamName windows, regular readback, dyadic tolerance ledger, Real seal, and structural rows are all visible coordinates of the same finite $\BHist$ packet.

Cofinal-map coverage is read from the $\phi$ row together with the source directed order recorded by $K$. For each source window the packet displays, the coverage row names a target window whose image lies above it, so the packet has no need for an ambient choice function selecting unseen target windows.

The CauchyNet handoff is then replayed through $C$ and transported by $H$. A target-window request first reads $J$, applies $\phi$ into the source ledger $I$, consumes the source row $K$, and only then exports the target row $L$ through the same $S,R,D$ evidence.

The seal row $A$ is downstream of this replay. Since the provenance and local naming rows package only those displayed coordinates, no quotient-net relation, selected limit, host real equality, or ambient completeness coordinate can be exported as a NameCert obligation.
\end{proof}

\begin{theorem}[DirectedSubnet CauchyNet handoff]
\label{thm:directed-subnet-cauchynet-handoff}
Let $M=(I,J,\phi,K,L,S,R,D,A,H,C,P,N)$ be an accepted $\DirectedSubnetUp$ packet. Every target $\CauchyNetUp$ row exported by $L$ factors through the displayed route
$$
J,\phi,I,K,S,R,D \longmapsto L .
$$
Thus the target CauchyNet packet is available only through the displayed cofinal map, the source CauchyNet row, the $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback, and the $\DyadicRatCoreUp$ tolerance ledger. The route is a projection and replay over finite rows, not a quotient-net construction and not a choice argument.
\end{theorem}
\leanchecked{BEDC.Derived.DirectedSubnetUp.DirectedSubnetCarrier\_cauchynet\_handoff}
\begin{proof}
Start with a consumer of the target row $L$. Carrier admission exposes $L$ only as a coordinate of the same packet that also contains $J,\phi,I,K,S,R,D$.

Replay the continuation row $C$. The consumer's target-window request first lands in $J$, then follows the displayed map $\phi$ into the source ledger $I$. The source $\CauchyNetUp$ row $K$ supplies the inspected source windows, while $S,R,D$ supply the scheduled windows, regular rational readback, and dyadic tolerance comparisons needed for the replay.

Transport by $H$ preserves this route componentwise. The package and local NameCert rows record exactly the same coordinates, so the target row $L$ has no exported path through quotient-net equality, hidden cofinal choice, a selected limit, host real equality, or ambient completeness.
\end{proof}

\begin{theorem}[DirectedSubnet cofinal replay]
\label{thm:directed-subnet-cofinal-replay}
Let $M=(I,J,\phi,K,L,S,R,D,A,H,C,P,N)$ be an accepted $\DirectedSubnetUp$ packet. Any consumer replaying a target directed window must expose the target ledger $J$, the displayed cofinal map $\phi$, the source ledger $I$, the source $\CauchyNetUp$ row $K$, the $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback $R$, and the $\DyadicRatCoreUp$ tolerance row $D$ before it reaches the target row $L$ and the terminal $\RealUp$ seal $A$.

Thus the cofinal replay route is the displayed finite chain
$$
J,\phi,I,K,S,R,D \longmapsto L \longmapsto A .
$$
It exports no quotient-net equality, hidden cofinal choice, selected subnet limit, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier tuple from \autoref{def:directed-subnet-carrier}. The target ledger $J$, map $\phi$, source ledger $I$, source row $K$, target row $L$, window row $S$, readback row $R$, tolerance row $D$, and seal row $A$ are all visible coordinates of the same finite packet.

Replay the continuation row $C$. A target-window request enters through $J$, follows $\phi$ into $I$, reads the source CauchyNet row $K$, and uses the same $S,R,D$ evidence before producing the target row $L$.

Transport by $H$ preserves each component of this chain. Since the package and local naming rows cite only the displayed coordinates, the terminal seal $A$ remains downstream of the replay and no absent quotient, choice, limit, equality, or completeness coordinate can be exported.
\end{proof}
\leanchecked{BEDC.Derived.DirectedSubnetUp.DirectedSubnetCofinalReplay}

\begin{theorem}[DirectedSubnet target Cauchy window]
\label{thm:directed-subnet-target-cauchy-window}
Every completion-facing consumer of an accepted $\DirectedSubnetUp$ packet must expose the target directed ledger $J$, the cofinal map $\phi$, the target $\CauchyNetUp$ row $L$, the $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback $R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the $\RealUp$ seal $A$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local $\NameCert_{\DirectedSubnetUp}$ row $N$ before it may read a limit or completion endpoint. The target window is therefore a displayed finite CauchyNet route, not a quotient net, selected subnet representative, countable-choice functional, or ambient Real completeness claim.
\end{theorem}
\leanchecked{BEDC.Derived.DirectedSubnetUp.DirectedSubnetTargetCauchyWindow}
\begin{proof}
Project the carrier tuple from \autoref{def:directed-subnet-carrier}. The target ledger $J$, cofinal map $\phi$, target row $L$, window row $S$, readback row $R$, tolerance row $D$, seal row $A$, and structural rows $H,C,P,N$ are all visible coordinates of the same finite packet.

Apply the CauchyNet handoff of \autoref{thm:directed-subnet-cauchynet-handoff}. Any read of $L$ must already pass through the target request in $J$, the displayed map $\phi$, the source route, and the same $S,R,D$ evidence that makes the target row Cauchy-facing.

It remains to inspect a completion endpoint. The seal row $A$ is downstream of the target CauchyNet route, while $H,C,P,N$ only transport, replay, cite, and name the displayed coordinates. Hence a completion consumer receives no quotient-net equality, selected subnet representative, countable-choice data, or ambient Real completeness row.
\end{proof}

\begin{theorem}[DirectedSubnet target-filter nonescape]
\label{thm:directed-subnet-target-filter-nonescape}
Every target-side Cauchy read exported by an accepted $\DirectedSubnetUp$ carrier must pass through the displayed source $\CauchyNetUp$ row, the cofinal replay row, the target Cauchy window, the $\RealUp$ seal, componentwise $\hsame/\Cont$ transport, $\Pkg$ provenance, and the local $\NameCert_{\DirectedSubnetUp}$ row. It exports no selected subnet limit, quotient net, cofinal choice outside the displayed map, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DirectedSubnetUp.DirectedSubnetTargetFilterNonescape}
\begin{proof}
Start with the carrier of \autoref{def:directed-subnet-carrier}. The source CauchyNet row $K$, target CauchyNet row $L$, displayed cofinal map $\phi$, stream windows $S$, readback $R$, tolerance ledger $D$, seal row $A$, and structural rows $H,C,P,N$ are visible coordinates of the same packet.

Use the NameCert obligations of \autoref{thm:directed-subnet-namecert-obligations}. They force a target-side read to cite the local naming row only after the carrier rows, transport row, replay row, and provenance row have all been exposed.

Apply the CauchyNet handoff of \autoref{thm:directed-subnet-cauchynet-handoff}, then the cofinal replay of \autoref{thm:directed-subnet-cofinal-replay}, and finally the target Cauchy window of \autoref{thm:directed-subnet-target-cauchy-window}. This route keeps the target read downstream of $K,\phi,L,S,R,D,A,H,C,P,N$, so the packet has no coordinate from which to export a selected subnet limit, quotient net, hidden cofinal choice, or ambient completeness claim.
\end{proof}

\begin{theorem}[DirectedSubnet obligation route]
\label{thm:directed-subnet-obligation-route}
Let $M=(I,J,\phi,K,L,S,R,D,A,H,C,P,N)$ be an accepted $\DirectedSubnetUp$ packet. Carrier admission, cofinal-map coverage, CauchyNet handoff, Real-seal nonescape, and ledger refusal are all projected from the same finite tuple $M$. Every obligation cites the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of that tuple, and no obligation is delegated to quotient-net equality, hidden cofinal choice, selected subnet limits, host real equality, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DirectedSubnetUp.DirectedSubnetObligationRoute}
\begin{proof}
Carrier admission is the projection of \autoref{def:directed-subnet-carrier}. The tuple displays the source ledger $I$, target ledger $J$, cofinal map $\phi$, source and target CauchyNet rows $K,L$, the stream, readback, tolerance, and seal rows $S,R,D,A$, and the structural rows $H,C,P,N$.

Cofinal-map coverage is the same coverage row used by \autoref{thm:directed-subnet-cofinal-replay}. It reads each requested source window through the displayed map $\phi:J\to I$ and never invokes an unseen cofinal selector.

The CauchyNet handoff is exactly the route of \autoref{thm:directed-subnet-cauchynet-handoff}. A target-window request must expose $J,\phi,I,K,S,R,D$ before it exports $L$.

The Real-seal boundary is the target-window route of \autoref{thm:directed-subnet-target-cauchy-window}. The seal row $A$ remains downstream of the target CauchyNet route, while $H,C,P,N$ only transport, replay, source, and name the displayed coordinates. Since the refused quotient, choice, limit, equality, and completeness rows are absent from $M$, ledger refusal is a projection from the same finite packet.
\end{proof}

\begin{theorem}[DirectedSubnet public NameCert export]
\label{thm:directed-subnet-public-namecert}
The public $\NameCert_{\DirectedSubnetUp}$ export is exactly the finite route through the source ledger $I$, target ledger $J$, displayed cofinal map $\phi$, source and target $\CauchyNetUp$ rows $K,L$, $\StreamNameUp$ windows $S$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ tolerance ledger $D$, $\RealUp$ seal $A$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local naming row $N$. A public consumer receives this route as the displayed $\DirectedSubnetUp$ certificate and receives no quotient-net equality, selected subnet limit, cofinal choice outside $\phi$, host real equality, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:directed-subnet-carrier}. The rows $I,J,\phi,K,L,S,R,D,A,H,C,P,N$ are the complete visible tuple.

Use the NameCert obligations of \autoref{thm:directed-subnet-namecert-obligations} and the obligation route of \autoref{thm:directed-subnet-obligation-route}. They bind carrier admission, cofinal-map coverage, CauchyNet handoff, Real-seal nonescape, and ledger refusal to those same displayed rows.

Apply the handoff, replay, target-window, and nonescape theorems of \autoref{thm:directed-subnet-cauchynet-handoff}, \autoref{thm:directed-subnet-cofinal-replay}, \autoref{thm:directed-subnet-target-cauchy-window}, and \autoref{thm:directed-subnet-target-filter-nonescape}. Their conclusions expose no public coordinate beyond the finite route already named by $N$, so the public export is precisely $\NameCert_{\DirectedSubnetUp}$ over the displayed packet.
\end{proof}

\begin{closurestatus}{\DirectedSubnetUp}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:directed-subnet-carrier}, \autoref{thm:directed-subnet-namecert-obligations}, \autoref{thm:directed-subnet-cauchynet-handoff}, \autoref{thm:directed-subnet-cofinal-replay}, \autoref{thm:directed-subnet-target-cauchy-window}, \autoref{thm:directed-subnet-obligation-route}, and \autoref{thm:directed-subnet-public-namecert} bind the finite carrier to the CauchyNet handoff, cofinal replay, target Cauchy window, target-filter nonescape, displayed NameCert obligation route, and public NameCert export.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient nets, selected limits, cofinal choice outside the displayed map, host real equality, ambient completeness, or Lean verification.}
  \upgradepath{Bridged closure requires a checked standard bridge from the public $\NameCert_{\DirectedSubnetUp}$ export to the acceptance schema.}
  \constructivestory{A $\DirectedSubnetUp$ packet is generated from source and target directed ledgers, a displayed cofinal map, source and target CauchyNet rows, StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealUp seal, componentwise transport, continuation replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
