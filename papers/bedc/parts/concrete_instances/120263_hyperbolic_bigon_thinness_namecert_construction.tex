\chapter{A Concrete Naming Certificate for $\HyperbolicBigonThinnessUp$}
\label{ch:concrete-instances-hyperbolic-bigon-thinness-namecert}
\origin{ai}

The $\HyperbolicBigonThinnessUp$ packet realizes the bigon-thinness branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite endpoint, side, metric, boundary-shadow, and Real-seal NameCert data. It sits beside the metric route of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary route of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the visual comparison route of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, and the ambient finite metric discipline of \autoref{ch:concrete-instances-metric-space-namecert}. The packet records a displayed two-sided geodesic boundary comparison; it does not assert global curvature, arbitrary geodesic uniqueness, host properness, or a selected geodesic representative outside the finite rows.

\begin{definition}[Hyperbolic bigon thinness carrier]\label{def:hyperbolic-bigon-thinness-carrier}
A $\HyperbolicBigonThinnessUp$ carrier is a finite $\BHist$ packet
$$
B=(M,G,\partial,V,E_0,E_1,L_0,L_1,D,R,\Sigma,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicMetricUp$ row carrying the hyperbolic metric read.
\item $G$ is the $\MetricSpaceUp$ row that keeps the underlying finite metric carrier visible.
\item $\partial$ is the $\HyperbolicGeodesicBoundaryUp$ row naming the boundary endpoints used by the bigon.
\item $V$ is the $\HyperbolicVisualMetricUp$ row used to compare the boundary shadows.
\item $E_0$ and $E_1$ are the two displayed endpoint rows of the bigon.
\item $L_0$ and $L_1$ are the two geodesic-side rows whose fellow-traveller comparison is being read.
\item $D$ is the finite distance ledger comparing side points through the hyperbolic metric.
\item $R$ is the $\RealUp$ seal row for the displayed thinness tolerance.
\item $\Sigma$ is the boundary-shadow ledger recording that the visual-metric read and the geodesic-boundary read refer to the same endpoints.
\item $H$ records componentwise $\hsame$ transport for metric, boundary, visual, endpoint, side, distance, Real-seal, shadow, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from an endpoint-pair request through $M,G,\partial,V,E_0,E_1,L_0,L_1,D,R,\Sigma,H$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicBigonThinnessUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicVisualMetricUp$, $\MetricSpaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicBigonThinnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for global CAT curvature, arbitrary geodesic uniqueness, host properness, selected geodesic representatives, quotient boundary equality, or an unbounded visual compactification.
\end{definition}

\begin{theorem}[Hyperbolic bigon thinness NameCert obligations]\label{thm:hyperbolic-bigon-thinness-namecert-obligations}
For every accepted $\HyperbolicBigonThinnessUp$ carrier
$$
B=(M,G,\partial,V,E_0,E_1,L_0,L_1,D,R,\Sigma,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicBigonThinnessUp}$ surface consists exactly of hyperbolic-metric exposure through $M$, ambient metric exposure through $G$, geodesic-boundary exposure through $\partial$, visual-metric exposure through $V$, endpoint exposure through $E_0,E_1$, geodesic-side exposure through $L_0,L_1$, distance-ledger exposure through $D$, Real thinness tolerance through $R$, boundary-shadow compatibility through $\Sigma$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-bigon-thinness-carrier}. The geometric rows are $M,G,\partial,V,E_0,E_1,L_0,L_1,D,R,\Sigma$, and the structural rows are $H,C,P,N$.

The metric and boundary rows are read before the sides. Thus the two geodesic-side rows are tied to the displayed hyperbolic metric, the underlying metric carrier, and the same geodesic-boundary endpoints.

The visual-metric and shadow rows then compare the endpoint data. The distance ledger $D$ and Real seal $R$ record only the finite thinness tolerance visible through those rows.

Finally the transport, replay, provenance, and local naming rows keep the same packet intact. Since no carrier coordinate stores global curvature, arbitrary geodesic uniqueness, host properness, selected geodesic representatives, quotient boundary equality, or visual compactification, the listed rows exhaust the local obligations.
\end{proof}

\begin{theorem}[Hyperbolic bigon thinness boundary route]\label{thm:hyperbolic-bigon-thinness-boundary-route}
Every boundary-facing consumer of an accepted $\HyperbolicBigonThinnessUp$ packet must expose
$$
M,G,\partial,V,E_0,E_1,L_0,L_1,D,R,\Sigma,H,C,P,N
$$
before using a bigon-thinness conclusion. The read reaches the conclusion only after the $\HyperbolicMetricUp$, $\MetricSpaceUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicVisualMetricUp$, and $\RealUp$ rows have been displayed in that route.
\end{theorem}
\begin{proof}
Begin with the NameCert obligation theorem. It makes the metric, boundary, visual, endpoint, geodesic-side, distance, Real-seal, shadow, transport, replay, provenance, and local naming rows the complete public surface.

The endpoint rows $E_0,E_1$ are read through $\partial$ and $V$. Thus a consumer cannot detach the boundary-shadow comparison from the displayed geodesic boundary or visual metric.

The side rows $L_0,L_1$ are then compared only through $M,G,D,R$. The hyperbolic metric, ambient metric carrier, finite distance ledger, and Real seal provide the thinness tolerance as a displayed row, not as a host global geometry fact.

Therefore the bigon-thinness conclusion remains a finite boundary route. It cannot be retyped as global curvature, arbitrary geodesic uniqueness, host properness, selected geodesic representation, quotient boundary equality, or an unbounded visual compactification.
\end{proof}

\falsifiablePrediction{An accepted $\HyperbolicBigonThinnessUp$ boundary consumer that reads bigon thinness without exposing $M,G,\partial,V,E_0,E_1,L_0,L_1,D,R,\Sigma,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{hyperbolic-metric, hyperbolic-geodesic-boundary, hyperbolic-visual-metric, metricspace, real: none of these siblings alone carries the joint endpoint, two-side, distance-ledger, boundary-shadow, Real-seal, transport, replay, provenance, and local NameCert packet required by $\HyperbolicBigonThinnessUp$.}

Future Lean verification for this packet must supply nontriviality and field-faithfulness taste gates for the same carrier surface before any bridge can advertise a schema-level hyperbolic-thinness export.

\closureat{HyperbolicBigonThinnessUp}{seedStr}

\begin{closurestatus}{\HyperbolicBigonThinnessUp}
  \constructivestory{A $\HyperbolicBigonThinnessUp$ packet is generated from HyperbolicMetric, MetricSpace, HyperbolicGeodesicBoundary, HyperbolicVisualMetric, endpoint, geodesic-side, distance-ledger, Real-seal, boundary-shadow, componentwise hsame, displayed Cont, Pkg, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-bigon-thinness-carrier}, \autoref{thm:hyperbolic-bigon-thinness-namecert-obligations}, and \autoref{thm:hyperbolic-bigon-thinness-boundary-route} seed the finite boundary route for hyperbolic bigon thinness.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean theorem verification, bridge checking, global curvature, arbitrary geodesic uniqueness, host properness, selected geodesic representatives, quotient boundary equality, or visual compactification.}
  \upgradepath{Obligation closure requires checked metric exposure, endpoint and side visibility, boundary-shadow compatibility, Real thinness tolerance, and taste-gate nonescape for BEDC.Derived.HyperbolicBigonThinnessUp.}
\end{closurestatus}
