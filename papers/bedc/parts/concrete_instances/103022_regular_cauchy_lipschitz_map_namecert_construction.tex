\chapter{A Concrete Naming Certificate for $\RegularCauchyLipschitzMapUp$}
\label{ch:concrete-instances-regular-cauchy-lipschitz-map-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and the standard Lipschitz continuity principle that a displayed bound transports Cauchy names to Cauchy names, the $\RegularCauchyLipschitzMapUp$ packet records the finite route by which a regular rational Cauchy name is read through a Lipschitz map and sealed as a Real-facing regular Cauchy output. It consumes the bound-carrying map surface of \autoref{ch:concrete-instances-lipschitzmap-namecert}, the input regular rational route of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite transport certificate for Real and Completion consumers; it is not a quotient map construction, a selected limit, host function extensionality, differentiability data, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy Lipschitz map carrier]
\label{def:regular-cauchy-lipschitz-map-carrier}
A $\RegularCauchyLipschitzMapUp$ carrier is a finite $\BHist$ packet
$$
T=(L,R_{\mathrm{in}},W_{\mathrm{in}},W_{\mathrm{out}},D,R_{\mathrm{out}},E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the source $\LipschitzMapUp$ row carrying the displayed graph and rational Lipschitz-bound ledger.
\item $R_{\mathrm{in}}$ is the input $\RegSeqRatUp$ regular Cauchy name row.
\item $W_{\mathrm{in}}$ is the finite $\StreamNameUp$ window opened on the input name for the requested tolerance.
\item $W_{\mathrm{out}}$ is the finite $\StreamNameUp$ window opened on the transported output readings.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and bound ledger, recording the requested output radius, the bound-scaled input radius, and the comparison budgets used by the Lipschitz inequality.
\item $R_{\mathrm{out}}$ is the output $\RegSeqRatUp$ readback row obtained after $L$, $R_{\mathrm{in}}$, $W_{\mathrm{in}}$, $W_{\mathrm{out}}$, and $D$ have been displayed.
\item $E$ is the terminal $\RealUp$ seal reached only after the output regular readback row is exposed.
\item $H$ records componentwise $\hsame$ transport for the Lipschitz map, input readback, both stream windows, dyadic ledger, output readback, Real seal, continuation replay, package provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each finite Real or Completion tolerance request through
$$
L,R_{\mathrm{in}},W_{\mathrm{in}},W_{\mathrm{out}},D,R_{\mathrm{out}} \longmapsto E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLipschitzMapUp$, $\LipschitzMapUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyLipschitzMapUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient stream equality, host function extensionality, selected limits, differentiability, countable choice, trichotomy, ambient $\RealUp$ completeness, or a detached output seal.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyLipschitzMapUp.RegularCauchyLipschitzMapTasteGate\_single\_carrier\_alignment}

\begin{definition}[Regular Cauchy Lipschitz map classifier]
\label{def:regular-cauchy-lipschitz-map-classifier}
The $\RegularCauchyLipschitzMapUp$ classifier compares accepted carriers only through componentwise $\hsame$ transport of the rows
$$
L,R_{\mathrm{in}},W_{\mathrm{in}},W_{\mathrm{out}},D,R_{\mathrm{out}},E,H,C,P,N.
$$
Thus the Lipschitz bound ledger, input regular name, finite input and output windows, dyadic tolerance conversion, output regular readback, terminal Real seal, continuation replay, provenance, and local NameCert row are all part of the visible comparison surface. The classifier does not compare host functions extensionally, quotient regular streams, select representatives, assert differentiability, or import ambient completeness.
\end{definition}

\begin{theorem}[Regular Cauchy Lipschitz map NameCert obligations]
\label{thm:regular-cauchy-lipschitz-map-namecert-obligations}
For every accepted $\RegularCauchyLipschitzMapUp$ carrier
$$
T=(L,R_{\mathrm{in}},W_{\mathrm{in}},W_{\mathrm{out}},D,R_{\mathrm{out}},E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyLipschitzMapUp}$ surface consists exactly of carrier exposure for the displayed packet, source-map exposure through $L$, input regular-name exposure through $R_{\mathrm{in}}$, finite input-window exposure through $W_{\mathrm{in}}$, finite output-window exposure through $W_{\mathrm{out}}$, dyadic tolerance and bound accounting through $D$, output regular readback through $R_{\mathrm{out}}$, terminal $\RealUp$ seal exposure through $E$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier tuple in \autoref{def:regular-cauchy-lipschitz-map-carrier}. Each listed coordinate is a finite row of the same $\BHist$ packet, and the last coordinate is the local NameCert row.

The analytic route is read in displayed order. A tolerance request first exposes the Lipschitz map row $L$ and its bound ledger, then the input regular Cauchy row $R_{\mathrm{in}}$, then the finite windows $W_{\mathrm{in}}$ and $W_{\mathrm{out}}$, and then the dyadic conversion ledger $D$ before the output readback $R_{\mathrm{out}}$ and terminal seal $E$ are available.

The structural rows $H,C,P,N$ transport, replay, source, and name only this route. Since the carrier contains no coordinate for quotient stream equality, host function extensionality, selected limits, differentiability, countable choice, trichotomy, ambient Real completeness, or a detached output seal, none of those principles can be part of the seed obligation surface.
\end{proof}

\begin{theorem}[Regular Cauchy Lipschitz map Real handoff]
\label{thm:regular-cauchy-lipschitz-map-real-handoff}
Every $\RealUp$ consumer read accepted by a $\RegularCauchyLipschitzMapUp$ carrier factors through the finite transport route
$$
\LipschitzMapUp,\ \RegSeqRatUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
Equivalently, an accepted terminal seal read may consume the displayed Lipschitz map, the input regular name, finite input and output stream windows, the dyadic tolerance and bound ledger, and the output regular readback, but it cannot bypass them by quotient stream equality, selected limits, host function extensionality, differentiability data, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use the obligation theorem \autoref{thm:regular-cauchy-lipschitz-map-namecert-obligations}. It exposes the terminal seal row $E$ only after the Lipschitz row, input regular-name row, both finite stream windows, dyadic tolerance ledger, and output regular readback have been displayed.

The classifier definition \autoref{def:regular-cauchy-lipschitz-map-classifier} then transports accepted reads only by the componentwise $\hsame$ row and replays them only through the displayed $\Cont$ route. Thus a Real-facing consumer cannot replace the finite transport route by a host equality of functions or streams.

Finally inspect the carrier definition. There is no coordinate for quotient stream equality, selected limits, host function extensionality, differentiability, countable choice, trichotomy, ambient Real completeness, or a second Real seal. A terminal read omitting one of the displayed transport rows is therefore not accepted by the local classifier.
\end{proof}

\closureat{RegularCauchyLipschitzMapUp}{seedStr}

\begin{closurestatus}{\RegularCauchyLipschitzMapUp}
  \constructivestory{A $\RegularCauchyLipschitzMapUp$ packet is generated from a LipschitzMap row, an input RegSeqRat name row, finite StreamName input and output windows, a DyadicRatCore tolerance and bound ledger, an output RegSeqRat readback row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-lipschitz-map-carrier}, \autoref{def:regular-cauchy-lipschitz-map-classifier}, \autoref{thm:regular-cauchy-lipschitz-map-namecert-obligations}, and \autoref{thm:regular-cauchy-lipschitz-map-real-handoff} seed the finite Lipschitz transport route from displayed regular Cauchy input names through dyadic bound accounting to output regular readback and the terminal RealUp seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected limits, host function extensionality, differentiability, countable choice, trichotomy, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, componentwise classifier transport, Lipschitz tolerance conversion, output regular readback, and Real-seal nonescape for BEDC.Derived.RegularCauchyLipschitzMapUp.}
\end{closurestatus}
