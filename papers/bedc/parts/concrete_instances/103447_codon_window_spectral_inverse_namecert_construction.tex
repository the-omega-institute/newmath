\chapter{A Concrete Naming Certificate for $\CodonWindowSpectralInverseUp$}
\label{ch:concrete-instances-codon-window-spectral-inverse-namecert}
\origin{ai}

The $\CodonWindowSpectralInverseUp$ packet realizes the codon-window spectral inverse vision of \autoref{subsec:philosophy-codon-window-inverse-reconstruction} as finite BEDC NameCert data. It sits beside the stop-face suffix packet of \autoref{ch:concrete-instances-stopcodonzeckendorfsuffix-namecert}, the finite bit-path carrier of \autoref{ch:concrete-instances-bitvector-namecert}, the zero-spine arithmetic route of \autoref{ch:concrete-instances-axisadd-namecert}, and the window-six dynamics bridge of \autoref{subsec:philosophy-dna-window-six-dynamics-bridge}. Its role is a finite inverse packet: it records the codon cube, two-axis atlas, local face spectra, stop-face reconstruction row, adjacency spectra, reassignment spectra, transport, replay, provenance, and local naming without identifying the biological code table with a BEDC kernel primitive.

\begin{definition}[Codon-window spectral inverse carrier]
\label{def:codon-window-spectral-inverse-carrier}
A $\CodonWindowSpectralInverseUp$ carrier is a finite $\BHist$ packet
$$
S=(C,A,F,H,R,T,Q,E,K,D,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the codon-cube row. It records the finite $Q_{6}$ carrier, the three two-bit base windows, and the displayed $\BitVectorUp$ paths used to read a codon window.
\item $A$ is the two-axis atlas row. It records the $\mathrm{Y}/\mathrm{R}$ and $\mathrm{W}/\mathrm{S}$ readback for $\mathrm{U},\mathrm{C},\mathrm{A},\mathrm{G}$ and keeps RNA/DNA spelling as bridge data rather than as a kernel equality.
\item $F$ is the local face enumeration row. It records the finite $Q_{2}$ faces, the selected stop/trp face, and the face labels consumed by the inverse spectrum.
\item $H$ is the local face spectrum row. It records multiplicity and class-pattern rows for each displayed face named by $F$.
\item $R$ is the Hamming adjacency spectrum row. It records one-bit adjacency boundaries, class changes, and local control-tile comparisons over the same $Q_{6}$ carrier.
\item $T$ is the reassignment spectrum row. It records finite comparison rows across displayed biological bridge tables or controlled perturbation rows.
\item $Q$ is the stop-face row. It records the $\mathrm{U}+\mathrm{RR}$ square, the family-suffix coordinates $00,01,10,11$, and the retained $X_{2}=\{00,01,10\}$ stop candidates.
\item $E$ records componentwise $\hsame$ transport for codon-cube, atlas, face, spectrum, adjacency, reassignment, stop-face, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a spectral inverse request through the cube, atlas, face enumeration, local spectra, adjacency spectra, reassignment spectra, stop-face row, provenance, and local name.
\item $D$ is the inverse-decision ledger. It records which finite rows are used to constrain a local coding mechanism and refuses any hidden global code-table reconstruction row.
\item $P$ is the $\Pkg$ provenance over $\CodonWindowSpectralInverseUp$, $\StopCodonZeckendorfSuffixUp$, $\BitVectorUp$, $\AxisAddUp$, the codon-window-six and DNA-window-six bridge rows, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CodonWindowSpectralInverseUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $C,A,F,H,R,T,Q,E,K,D,P,N$. It has no coordinate for a host genetic-code table, a selected global inverse, biological universality, quotient codon equality, probabilistic inference, or a choice principle.
\end{definition}

\begin{theorem}[Codon-window spectral inverse NameCert obligations]
\label{thm:codon-window-spectral-inverse-namecert-obligations}
For every accepted $\CodonWindowSpectralInverseUp$ carrier
$$
S=(C,A,F,H,R,T,Q,E,K,D,P,N),
$$
the local $\NameCert_{\CodonWindowSpectralInverseUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the codon cube, two-axis atlas, local face enumeration, local face spectra, Hamming adjacency spectra, reassignment spectra, stop-face row, transport, replay, inverse-decision ledger, provenance, and local naming row;
\item cube exactness, requiring every codon-window read to pass through the finite $Q_{6}$ row $C$ and its displayed $\BitVectorUp$ coordinates;
\item atlas compatibility, requiring all biological spelling reads to pass through $A$ before any face or spectrum row is consumed;
\item local spectrum coverage, requiring $F,H,R,T$ to expose the finite face, multiplicity, adjacency, and reassignment rows used by the inverse packet;
\item stop-face reconstruction, requiring $Q$ to display the $\mathrm{U}+\mathrm{RR}$ face and the retained $X_{2}$ suffix rows before a stop-candidate read is exported;
\item transport and replay stability, requiring $E$ and $K$ to move only the displayed rows and preserve the same carrier inventory under $\hsame$ and $\Cont$ replay;
\item provenance and non-escape, requiring $D,P,N$ to record the consumed sibling packets and to exclude host genetic-code tables, selected global inverses, biological universality, quotient codon equality, probabilistic inference, and choice.
\end{enumerate}
No codon-window spectral inverse obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier $S=(C,A,F,H,R,T,Q,E,K,D,P,N)$. The tuple inventory is the whole visible packet, so carrier admission is projection from the displayed finite $\BHist$ data.

Cube exactness is forced by $C$. Every codon-window request reads the finite $Q_{6}$ carrier and its three two-bit $\BitVectorUp$ windows before any biological or spectral classifier row is available.

Atlas compatibility is confined to $A$. The RNA/DNA spelling and the two-axis readback are bridge rows over the finite cube, not kernel equalities and not a host code table.

The local spectrum rows are $F,H,R,T$. They enumerate the finite faces, attach local face spectra, record one-bit adjacency boundaries, and compare reassignment rows, so the inverse packet can constrain local mechanisms without selecting a global table.

The stop-face row $Q$ supplies the finite $\mathrm{U}+\mathrm{RR}$ square and the retained $X_{2}$ suffix rows. Transport through $E$ and replay through $K$ preserve these rows componentwise, while $D,P,N$ record the consumed route and local name.
\end{proof}

\begin{theorem}[Codon-window stop-face reconstruction route]
\label{thm:codon-window-spectral-inverse-stop-face-reconstruction}
Let $S=(C,A,F,H,R,T,Q,E,K,D,P,N)$ be an accepted $\CodonWindowSpectralInverseUp$ carrier. Every stop-face reconstruction read exported by $S$ factors through the finite route
$$
C,A,F,H,R,T \longmapsto Q \longmapsto E,K,D,P,N .
$$
The exported read receives the codon cube, two-axis atlas, local face enumeration, local face spectra, Hamming adjacency spectra, reassignment spectra, stop-face row, componentwise transport, replay, inverse-decision ledger, provenance, and local name. It receives no host genetic-code table, selected global inverse, biological universality claim, quotient codon equality, probabilistic inference row, or choice principle.
\end{theorem}
\begin{proof}
Start with the accepted packet and read $C$ and $A$. These rows fix the finite codon carrier and the two-axis spelling bridge before any spectral data are visible.

Next read $F,H,R,T$. The local faces, local spectra, Hamming adjacency boundaries, and reassignment comparisons are all finite rows over the same displayed cube, so no global code-table search is introduced.

The stop-face row $Q$ then identifies the $\mathrm{U}+\mathrm{RR}$ square and the $X_{2}$ suffix subset. This is the only row through which a stop-face reconstruction may pass.

Finally the structural rows $E,K,D,P,N$ transport, replay, audit, source, and name the same route. Since the refused principles have no carrier coordinate and no replay edge, the stop-face reconstruction read exports only the displayed finite packet.
\end{proof}

\falsifiablePrediction{Every accepted $\CodonWindowSpectralInverseUp$ stop-face or local inverse read factors through one finite codon-cube row, one two-axis atlas row, finite local face spectra, Hamming adjacency spectra, reassignment spectra, one stop-face row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Empirical comparison may reject a chosen biological bridge by showing that its local face spectra, adjacency spectra, or reassignment spectra do not match observed genetic-code table rows.}

\independenceWitness{stopcodonzeckendorfsuffix, bitvector, axisadd, codon-window-six-bridge, dna-window-six-dynamics-bridge: $\CodonWindowSpectralInverseUp$ is not bijective to any listed sibling because it jointly carries the codon cube, two-axis atlas, local face enumeration, local face spectra, Hamming adjacency spectra, reassignment spectra, stop-face reconstruction row, inverse-decision ledger, transport, replay, provenance, and local naming.}

The Lean follow-up file \texttt{lean4/BEDC/Derived/CodonWindowSpectralInverseUp/TasteGate.lean} must provide explicit $\Nontrivial\ \CodonWindowSpectralInverseUp$ and $\FieldFaithful\ \CodonWindowSpectralInverseUp$ instances, with field-projection proofs preserving $C,A,F,H,R,T,Q,E,K,D,P,N$ as separate displayed fields.

\closureat{CodonWindowSpectralInverseUp}{seedStr}

\begin{closurestatus}{\CodonWindowSpectralInverseUp}
  \constructivestory{A $\CodonWindowSpectralInverseUp$ packet is generated from a codon-cube row, two-axis atlas row, local face enumeration, local face spectra, Hamming adjacency spectra, reassignment spectra, stop-face row, componentwise hsame transport, displayed Cont replay, inverse-decision ledger, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:codon-window-spectral-inverse-carrier}, \autoref{thm:codon-window-spectral-inverse-namecert-obligations}, and \autoref{thm:codon-window-spectral-inverse-stop-face-reconstruction} seed the finite codon-window spectral inverse packet over codon cube, atlas, face spectra, adjacency spectra, reassignment spectra, stop-face, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host genetic-code tables, selected global inverses, biological universality, quotient codon equality, probabilistic inference, choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cube exactness, atlas compatibility, local spectrum coverage, stop-face reconstruction, transport and replay stability, provenance non-escape, Nontrivial CodonWindowSpectralInverseUp, and FieldFaithful CodonWindowSpectralInverseUp for BEDC.Derived.CodonWindowSpectralInverseUp.}
\end{closurestatus}
