\chapter{A Concrete Naming Certificate for $\CholeskyDecompositionUp$}
\label{ch:concrete-instances-cholesky-decomposition-namecert}
\origin{human}

Following Cholesky and Golub--Van Loan, the $\CholeskyDecompositionUp$ packet records positive-definite matrix factorization as finite BEDC data over displayed matrix, inner-product, triangular-factor, and diagonal-positivity rows. It consumes the matrix carrier of \autoref{ch:concrete-instances-matrix-namecert}, the positive-definite inner-product route of \autoref{ch:concrete-instances-innerproduct-namecert}, the triangular ledger of \autoref{ch:concrete-instances-lu-decomposition-namecert}, the orthogonal factorization comparison of \autoref{ch:concrete-instances-qr-decomposition-namecert}, and the spectral comparison surface of \autoref{ch:concrete-instances-singular-value-decomposition-namecert}. The packet names only a displayed positive-definite factorization route; it does not import a host numerical algorithm, quotient matrix equality, pivot selection outside the certificate, an analytic square-root choice, or an ambient completeness principle.

\begin{definition}[Cholesky decomposition carrier]
\label{def:cholesky-decomposition-carrier}
A $\CholeskyDecompositionUp$ carrier is a finite $\BHist$ packet
$$
X=(M,P,L,D,R,H,C,Q,N)
$$
with the following visible rows:
\begin{enumerate}
\item $M$ is the displayed square matrix source, read through the finite row and column interface of $\MatrixUp$.
\item $P$ is the positive-definite witness row, recording the $\InnerProductUp$ comparison and the finite Gram positivity checks available to the factorization consumer.
\item $L$ is the lower-triangular factor row, carrying the finite entries and triangular-zero ledger.
\item $D$ is the diagonal-positivity row, recording that the diagonal entries used by the triangular factor are positive displayed scalars.
\item $R$ is the reconstruction ledger asserting that the displayed matrix source is recovered from the lower-triangular factor, its transpose-facing read, and the diagonal positivity data.
\item $H$ records componentwise $\hsame$ transport for the source, positivity, triangular, diagonal, reconstruction, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the matrix and positivity rows to the factor row, and from the factor row to the reconstruction ledger.
\item $Q$ is the $\Pkg$ provenance over $\CholeskyDecompositionUp$, $\MatrixUp$, $\InnerProductUp$, $\LUDecompositionUp$, $\QRDecompositionUp$, $\SingularValueDecompositionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CholeskyDecompositionUp}$ row.
\end{enumerate}
The classifier compares accepted packets componentwise by the displayed rows. No coordinate stores a host floating-point routine, quotient matrix equality, selected pivoting outside the packet, a global spectral theorem, or an unrestricted square-root operation.
\end{definition}

\begin{theorem}[Cholesky decomposition NameCert obligations]
\label{thm:cholesky-decomposition-namecert-obligations}
For every accepted $\CholeskyDecompositionUp$ carrier
$$
X=(M,P,L,D,R,H,C,Q,N),
$$
the local $\NameCert_{\CholeskyDecompositionUp}$ surface consists exactly of matrix-source admission through $M$, positive-definite exposure through $P$, lower-triangular factor exposure through $L$, diagonal positivity through $D$, reconstruction through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, sibling provenance through $Q$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cholesky-decomposition-carrier}. The only mathematical rows before structural transport are $M,P,L,D,R$, so every accepted read must expose the matrix source, positive-definite witness, lower-triangular factor, diagonal positivity, and reconstruction ledger.

The structural rows $H$ and $C$ preserve those same displayed rows under transport and replay. They do not introduce a second factorization, a host equality principle, or an algorithmic pivot choice.

Finally $Q$ and $N$ record sibling provenance and the local naming row. Since no other coordinate is present, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Cholesky decomposition positive-definite route]
\label{thm:cholesky-decomposition-positive-definite-route}
Every positive-definite matrix-factorization consumer of an accepted $\CholeskyDecompositionUp$ packet must read the finite route
$$
\MatrixUp \longrightarrow \InnerProductUp \longrightarrow \LUDecompositionUp \longrightarrow \QRDecompositionUp \longrightarrow \SingularValueDecompositionUp
$$
through the displayed rows $M,P,L,D,R,H,C,Q,N$. The Cholesky read therefore exports a lower-triangular positive-diagonal reconstruction certificate, not a host numerical routine, quotient matrix equality, selected pivoting rule, analytic square-root choice, or ambient spectral-completion theorem.
\end{theorem}
\begin{proof}
Start with the matrix source and positive-definite witness rows $M$ and $P$. They force the consumer to read a displayed finite matrix and the finite positivity checks before any triangular factor can be inspected.

Next read the triangular and diagonal rows $L$ and $D$. The lower-triangular shape is tied to the LU-style triangular ledger, while the positive diagonal row keeps the Cholesky factor distinct from a generic triangular decomposition.

Finally inspect the reconstruction and structural rows $R,H,C,Q,N$. They replay the same factorization route and record the QR and singular-value comparison surfaces only as sibling provenance. Thus the exported object is the displayed positive-definite Cholesky route and nothing outside the finite certificate.
\end{proof}

\falsifiablePrediction{An accepted $\CholeskyDecompositionUp$ consumer is refuted if it reads a positive-definite factorization without first exposing the MatrixUp source, InnerProductUp positivity witness, lower-triangular factor, diagonal-positivity row, reconstruction ledger, transport, replay, provenance, and local naming rows.}

\independenceWitness{matrix, innerproduct, lu-decomposition, qr-decomposition, singular-value-decomposition: none is bijective to $\CholeskyDecompositionUp$ because the Cholesky packet jointly carries positive-definite witness data, lower-triangular factor data, diagonal positivity, reconstruction, and structural certificate rows.}

\closureat{CholeskyDecompositionUp}{seedStr}

\begin{closurestatus}{\CholeskyDecompositionUp}
  \constructivestory{A $\CholeskyDecompositionUp$ packet is generated from a displayed MatrixUp source, an InnerProductUp positive-definite witness row, a lower-triangular factor row, a diagonal-positivity row, a reconstruction ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cholesky-decomposition-carrier}, \autoref{thm:cholesky-decomposition-namecert-obligations}, and \autoref{thm:cholesky-decomposition-positive-definite-route} seed the finite positive-definite triangular factorization route over MatrixUp, InnerProductUp, LUDecompositionUp, QRDecompositionUp, SingularValueDecompositionUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host numerical Cholesky algorithms, quotient matrix equality, selected pivoting outside the packet, analytic square-root choice, spectral completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-definite witness exposure, triangular-factor exposure, diagonal-positivity exposure, reconstruction replay, transport, provenance, and local naming for BEDC.Derived.CholeskyDecompositionUp.}
\end{closurestatus}
