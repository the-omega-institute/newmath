\chapter{A Concrete Naming Certificate for $\ZeroFibreNormalSheetUp$}
\label{ch:concrete-instances-zero-fibre-normal-sheet-namecert}
\origin{ai}

The $\ZeroFibreNormalSheetUp$ packet realizes the normal zero-sheet geometry of \autoref{sec:unified-reality-zero-fibre-normal-sheet-geometry} as finite BEDC NameCert data. It sits after the complex-coordinate carrier of \autoref{ch:concrete-instances-complex-namecert}, the continuation and zero-consumer ledgers of \autoref{ch:concrete-instances-zeta-continuation-witness-namecert} and \autoref{ch:concrete-instances-zeta-zeros-namecert}, the critical-strip window of \autoref{ch:concrete-instances-rh-critical-strip-window-certificate-namecert}, the zero-type factorization of \autoref{ch:concrete-instances-zeta-zero-type-factorization-namecert}, the finite prime-window gluing route of \autoref{ch:concrete-instances-finite-prime-window-gluing-namecert}, and the RH boundary separation of \autoref{ch:concrete-instances-rh-boundary-kind-separation-namecert}. The packet names finite normal-sheet support evidence; it does not assert RH, zero exclusion, analytic continuation completeness, zero simplicity, or an infinite spectral theorem.

\begin{definition}[Zero-fibre normal sheet carrier]
\label{def:zero-fibre-normal-sheet-carrier}
A $\ZeroFibreNormalSheetUp$ carrier is a finite $\BHist$ packet
$$
Z=(C,V,W,O,M,P,R,H,K,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\ComplexUp$ mirror-coordinate row, displaying a critical-strip point as $\frac12+\delta+i\tau$.
\item $V$ is the completed-zeta value row read through the existing zeta-continuation and zero-consumer ledgers.
\item $W$ is the $\RHCriticalStripWindowCertificateUp$ row that keeps the zero read inside a displayed finite critical-strip window.
\item $O$ is the $\ZetaZeroTypeFactorizationUp$ row separating zero-fibre, trivial-zero, generated-zero, and RH-facing support reads.
\item $M$ is the $\RHMirrorOrbitTargetUp$ row recording the mirror companion of an off-fixed coordinate without identifying it with the fixed line.
\item $P$ is the $\FinitePrimeWindowGluingUp$ support row used when a normal coordinate is compared with finite prime-window defect evidence.
\item $R$ is the support route row from a normal-coordinate read and completed-zero read to the finite no-extra-zero-sheet boundary.
\item $H$ records componentwise $\hsame$ transport for coordinate, completed-zeta, critical-window, zero-factorization, mirror, prime-window, support-route, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a zero-fibre query through $C,V,W,O,M,P,R,H$.
\item $Q$ is the $\Pkg$ provenance over $\ZeroFibreNormalSheetUp$, $\ComplexUp$, $\ZetaContUp$, $\ZetaZerosUp$, $\RHCriticalStripWindowCertificateUp$, $\ZetaZeroTypeFactorizationUp$, $\RHMirrorOrbitTargetUp$, $\FinitePrimeWindowGluingUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ZeroFibreNormalSheetUp}$ row.
\end{enumerate}
The classifier accepts a normal-sheet support read only through these rows. It has no coordinate for an RH proof, global zero exclusion, analytic continuation completeness, zero simplicity, host complex equality, an infinite prime window, or a spectral trace formula.
\end{definition}

\begin{theorem}[Zero-fibre normal sheet NameCert obligations]
\label{thm:zero-fibre-normal-sheet-namecert-obligations}
For every accepted $\ZeroFibreNormalSheetUp$ carrier $Z=(C,V,W,O,M,P,R,H,K,Q,N)$, the local $\NameCert_{\ZeroFibreNormalSheetUp}$ surface consists exactly of carrier admission for the mirror-coordinate row, completed-zeta value row, finite critical-strip window, zero-type factorization, mirror-orbit row, finite prime-window support row, normal-sheet support route, componentwise transport, displayed replay, provenance, and local naming. No accepted consumer may use the packet as an RH proof or zero-exclusion theorem before these finite rows are displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:zero-fibre-normal-sheet-carrier}. The rows $C,V,W,O,M,P,R,H,K,Q,N$ are the only coordinates available to the local certificate.

The coordinate row $C$ and value row $V$ expose the zero-fibre read, while $W$ keeps the read inside a finite critical-strip window. The factorization row $O$ prevents this zero read from being confused with trivial-zero, generated-zero, or global RH surfaces.

The mirror row $M$ records pairing under the functional-equation symmetry without collapsing the normal coordinate. The finite prime-window row $P$ supplies the support evidence consumed by $R$ when the packet is read as a no-extra-zero-sheet boundary.

Finally $H,K,Q,N$ transport, replay, source, and name the same finite route. Since the carrier contains no RH proof, global zero-exclusion coordinate, analytic-completion coordinate, zero-simplicity coordinate, host equality coordinate, infinite prime window, or spectral trace formula, none of those objects is exported by the local NameCert surface.
\end{proof}

\begin{theorem}[Zero-fibre normal sheet RH support route]
\label{thm:zero-fibre-normal-sheet-rh-support-route}
Every RH-facing support read from an accepted $\ZeroFibreNormalSheetUp$ packet factors through the displayed finite route
$$
\ComplexUp,\ZetaContUp,\ZetaZerosUp
\longmapsto
\RHCriticalStripWindowCertificateUp
\longmapsto
\ZetaZeroTypeFactorizationUp,\RHMirrorOrbitTargetUp
\longmapsto
\FinitePrimeWindowGluingUp
\longmapsto
\BHist,\hsame,\Cont,\Pkg,\NameCert .
$$
The route names a finite support certificate for the normal zero-sheet boundary; it does not decide RH, exclude all zeros off the fixed line, prove analytic continuation completeness, or replace finite prime-window evidence by an infinite spectral object.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:zero-fibre-normal-sheet-namecert-obligations}. It forces every accepted support read to expose the coordinate, completed-zeta, critical-window, zero-factorization, mirror, finite prime-window, and support-route rows before provenance and local naming are consumed.

The support row $R$ is downstream of both the mirror-orbit row and the finite prime-window row. Thus the packet may record normal-sheet support evidence only after the zero-fibre read has been separated from global RH claims and after the finite prime-window comparison has been displayed.

The non-escape statement is carrier inspection. The tuple contains no coordinate for an RH proof, global zero exclusion, analytic continuation completeness, or infinite spectral trace, so the accepted route remains a finite support certificate.
\end{proof}

\falsifiablePrediction{A $\ZeroFibreNormalSheetUp$ consumer that claims no-extra-zero-sheet support while omitting the critical-strip window $W$, zero-factorization row $O$, mirror-orbit row $M$, or finite prime-window support row $P$ is not reading an accepted packet of this chapter.}

\independenceWitness{zeta-continuation-witness, zeta-zeros, rh-critical-strip-window, zeta-zero-type-factorization, finite-prime-window-gluing: $\ZeroFibreNormalSheetUp$ is not bijective with any listed sibling because it jointly carries mirror coordinates, completed-zeta zero reads, finite critical-window support, zero-factorization, mirror-orbit, finite prime-window support, transport, replay, provenance, and local naming.}

The Lean TasteGate route for this packet must provide explicit $\mathsf{Nontrivial}\ \ZeroFibreNormalSheetUp$ and $\mathsf{FieldFaithful}\ \ZeroFibreNormalSheetUp$ instances. Those instances must keep $C,V,W,O,M,P,R,H,K,Q,N$ as separate displayed fields before any checked support-route theorem can serve as a public TasteGate realization.

\closureat{ZeroFibreNormalSheetUp}{seedStr}

\begin{closurestatus}{\ZeroFibreNormalSheetUp}
  \constructivestory{A $\ZeroFibreNormalSheetUp$ packet is generated from ComplexUp mirror coordinates, completed-zeta value rows, a finite critical-strip window, zero-type factorization, mirror-orbit rows, finite prime-window support, a normal-sheet support route, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:zero-fibre-normal-sheet-carrier}, \autoref{thm:zero-fibre-normal-sheet-namecert-obligations}, and \autoref{thm:zero-fibre-normal-sheet-rh-support-route} seed the finite normal-sheet support route over zeta, critical-window, mirror, prime-window, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, RH, global zero exclusion, analytic continuation completeness, zero simplicity, host complex equality, infinite prime windows, or a spectral trace formula.}
  \upgradepath{Obligation closure requires checked carrier admission, completed-zero visibility, critical-window support, zero-factorization separation, mirror-orbit preservation, finite prime-window support, Nontrivial ZeroFibreNormalSheetUp, and FieldFaithful ZeroFibreNormalSheetUp for BEDC.Derived.ZeroFibreNormalSheetUp.}
\end{closurestatus}
