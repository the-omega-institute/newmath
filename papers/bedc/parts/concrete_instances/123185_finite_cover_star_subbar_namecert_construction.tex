\chapter{A Concrete Naming Certificate for $\FiniteCoverStarSubbarUp$}
\label{ch:concrete-instances-finite-cover-star-subbar-namecert}
\origin{human}

Following the Heine--Cantor finite-cover proof in Bishop--Bridges constructive analysis, the $\FiniteCoverStarSubbarUp$ packet records the finite star-subbar route that turns compact cover incidence, local continuity moduli, rational radius folds, and metric-triangle handoff into a tolerance-only uniform-modulus ledger. It realizes the compact-uniform-continuity construction of \autoref{ch:visions-compact-uniform-continuity} after the compact source row of \autoref{ch:concrete-instances-compact-namecert}, the metric distance row of \autoref{ch:concrete-instances-metric-namecert}, the continuous pointwise-modulus row of \autoref{ch:concrete-instances-continuous-namecert}, the rational tolerance ledger of \autoref{ch:concrete-instances-rat-namecert}, and the finite-cover star-refinement sibling \autoref{ch:concrete-instances-finite-cover-star-refinement-modulus-namecert}. The packet exposes a finite subbar of a star refinement; it is not an open-cover compactness theorem, not a selected global modulus, and not an ambient completeness principle.

\begin{definition}[Finite cover star-subbar carrier]\label{def:finite-cover-star-subbar-carrier}
A $\FiniteCoverStarSubbarUp$ carrier is a finite $\BHist$ packet
$$
B=(K,M,F,E,I,S,R,T,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row, displaying the finite cover or finite-net data admitted by the compact naming certificate.
\item $M$ is the $\MetricUp$ distance-comparison row used to test cover membership and triangle handoff.
\item $F$ is the $\ContinuousUp$ local-modulus row attached to the displayed cover centers.
\item $E$ is the positive $\RatUp$ tolerance request.
\item $I$ is the finite cover-incidence row, recording which displayed probes meet which cover cells.
\item $S$ is the star-subbar row, a finite subbar of the star refinement whose entries retain their cover-incidence and local-modulus witnesses.
\item $R$ is the $\RatUp$ radius and lower-bound fold row over the displayed star-subbar radii.
\item $T$ is the metric-triangle handoff row, recording that any two points inside the retained star cell are close enough for the local modulus read.
\item $U$ is the tolerance-only uniform-modulus ledger exported after $K,M,F,E,I,S,R,T$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the cover, metric, continuity, rational, subbar, fold, handoff, and output rows.
\item $C$ records displayed $\Cont$ replay through finite cover incidence, local modulus reads, rational-radius folding, metric-triangle handoff, uniform-modulus output, transport, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\FiniteCoverStarSubbarUp$, $\CompactUp$, $\MetricUp$, $\ContinuousUp$, $\RatUp$, $\FiniteCoverStarRefinementModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteCoverStarSubbarUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,F,E,I,S,R,T,U,H,C,P,N$. It has no coordinate for arbitrary open covers, selected subcovers, quotient cover equality, a host function space, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite cover star-subbar NameCert obligations]\label{thm:finite-cover-star-subbar-namecert-obligations}
For every accepted $\FiniteCoverStarSubbarUp$ carrier
$$
B=(K,M,F,E,I,S,R,T,U,H,C,P,N),
$$
the local $\NameCert_{\FiniteCoverStarSubbarUp}$ surface consists exactly of compact finite-cover admission through $K$, metric comparison through $M$, continuous local-modulus exposure through $F$, positive rational tolerance exposure through $E$, finite cover incidence through $I$, star-subbar admission through $S$, rational-radius folding through $R$, metric-triangle handoff through $T$, uniform-modulus ledger output through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-cover-star-subbar-carrier}. The source rows $K,M,F,E$ make the compact cover, metric comparison, local continuity modulus, and requested rational tolerance visible before any subbar or uniform-modulus row can be consumed.

The finite cover rows are $I,S,R$. Incidence is read through $I$, the retained star-subbar is displayed by $S$, and the rational lower-bound fold over the retained radii is recorded by $R$. These rows are finite $\BHist$ data, not a host open-cover selection principle.

The output rows are downstream. The metric-triangle handoff $T$ uses the same metric row $M$ and folded rational bound $R$ before $U$ exports a tolerance-only uniform-modulus ledger. The structural rows $H,C,P,N$ preserve, replay, source, and name only that route.
\end{proof}

\begin{theorem}[Finite cover star-subbar uniform-modulus route]\label{thm:finite-cover-star-subbar-uniform-modulus-route}
Every uniform-modulus consumer of an accepted $\FiniteCoverStarSubbarUp$ packet factors through the finite route
$$
\CompactUp,\MetricUp,\ContinuousUp,\RatUp
\longmapsto
I,S,R,T
\longmapsto
U,H,C,P,N .
$$
The route reads compact finite-cover incidence, local moduli, a rational-radius fold, and a metric-triangle handoff before the tolerance-only ledger is public. It exports no arbitrary open cover, selected subcover, quotient cover equality, host function-space equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:finite-cover-star-subbar-namecert-obligations}. Any consumer of $U$ first sees the compact, metric, continuous, and rational rows $K,M,F,E$, so the tolerance request is interpreted inside the displayed compact metric source and its local modulus data.

Next the consumer reads $I,S,R$. The incidence row and star-subbar row display the finite cover entries retained by the argument, while the rational fold supplies the common lower-bound ledger used by every retained entry.

Finally $T$ performs the metric-triangle handoff and $U$ exports the tolerance-only ledger. Since $H,C,P,N$ only transport, replay, source, and name the same packet, the output cannot be detached into open-cover compactness, a selected global modulus, quotient cover equality, host function equality, countable choice, trichotomy, or ambient completeness.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\FiniteCoverStarSubbarUp$ uniform-modulus read while omitting the $\CompactUp$ source row, $\MetricUp$ comparison row, $\ContinuousUp$ local-modulus row, $\RatUp$ tolerance row, finite cover incidence, star-subbar row, rational-radius fold, metric-triangle handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, or local $\NameCert$ row refutes the carrier of \autoref{def:finite-cover-star-subbar-carrier}.}

\independenceWitness{compact, metric, continuous, rat, finitecoverstarrefinementmodulus: no listed sibling is bijective to $\FiniteCoverStarSubbarUp$ because this packet alone binds compact cover incidence, metric comparison, local continuity moduli, rational tolerance and lower-bound folding, a retained star-subbar, metric-triangle handoff, uniform-modulus output, transport, replay, provenance, and local naming as one finite cover certificate.}

\closureat{FiniteCoverStarSubbarUp}{seedStr}

\begin{closurestatus}{\FiniteCoverStarSubbarUp}
  \constructivestory{A $\FiniteCoverStarSubbarUp$ packet is generated from CompactUp finite-cover data, MetricUp distance comparisons, ContinuousUp local-modulus rows, RatUp tolerance and lower-bound folds, finite cover-incidence rows, a star-subbar row, a metric-triangle handoff, a tolerance-only uniform-modulus ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-cover-star-subbar-carrier}, \autoref{thm:finite-cover-star-subbar-namecert-obligations}, and \autoref{thm:finite-cover-star-subbar-uniform-modulus-route} seed the finite cover star-subbar route for compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim arbitrary open-cover compactness, selected subcovers, quotient cover equality, host function-space equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-cover exposure, metric comparison, continuous local-modulus exposure, rational tolerance folding, finite cover incidence, star-subbar admission, metric-triangle handoff, uniform-modulus output, transport, replay, provenance, and local naming for BEDC.Derived.FiniteCoverStarSubbarUp.}
\end{closurestatus}
