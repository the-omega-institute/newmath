\chapter{A Concrete Naming Certificate for $\LocatedZeroUniquenessUp$}
\label{ch:concrete-instances-located-zero-uniqueness-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\LocatedZeroUniquenessUp$ packet records the uniqueness boundary for two displayed located-zero reads over one shared constructive bracket route. It sits after the located-zero witness surface of \autoref{ch:concrete-instances-bishop-located-zero-namecert}, the finite stream and regular-rational readbacks of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the separated comparison boundary of \autoref{ch:concrete-instances-real-equality-namecert}. The packet names classifier equality over a displayed separated-zero route; it is not host equality of roots, a selected root, trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Located zero uniqueness carrier]
\label{def:located-zero-uniqueness-carrier}
A $\LocatedZeroUniquenessUp$ carrier is a finite $\BHist$ packet
$$
U=(B,M,S,Z_0,Z_1,W_0,W_1,R_0,R_1,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B,M,S$ are the shared $\BishopLocatedZeroUp$ bracket, modulus, and bisection-schedule rows, recording that both zero reads come from the same displayed sign-change route.
\item $Z_0$ and $Z_1$ are the two displayed zero-witness rows, each pointing back to the common $B,M,S$ route before any comparison is read.
\item $W_0$ and $W_1$ are the two $\StreamNameUp$ window rows used to expose the finite source observations for the displayed zero reads.
\item $R_0$ and $R_1$ are the two $\RegSeqRatUp$ readback rows for those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the comparison route.
\item $E$ is the $\RealEqualityUp$ comparison row, or equivalently a separated-metric zero-distance comparison row, read only after $B,M,S,Z_0,Z_1,W_0,W_1,R_0,R_1,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the shared bracket route, the two witness rows, the readback rows, the comparison row, and the local naming row.
\item $C$ records displayed $\Cont$ replay through the route $B,M,S,Z_0,Z_1,W_0,W_1,R_0,R_1,D,E,H$.
\item $P$ is the $\Pkg$ provenance over $\LocatedZeroUniquenessUp$, $\BishopLocatedZeroUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealEqualityUp$, $\SeparatedMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\LocatedZeroUniquenessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed separated-zero route. It has no coordinate for host equality of roots, a selected root, trichotomy, countable choice, open-cover compactness, quotient real equality, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.LocatedZeroUniquenessUp}

\begin{theorem}[Located zero uniqueness NameCert obligations]
\label{thm:located-zero-uniqueness-obligations}
For every accepted $\LocatedZeroUniquenessUp$ carrier $U=(B,M,S,Z_0,Z_1,W_0,W_1,R_0,R_1,D,E,H,C,P,N)$, the local $\NameCert_{\LocatedZeroUniquenessUp}$ surface is exhausted by shared located-zero bracket admission through $B,M,S$, two displayed zero-witness rows through $Z_0,Z_1$, StreamName exposure through $W_0,W_1$, RegSeqRat readback through $R_0,R_1$, dyadic tolerance accounting through $D$, separated comparison through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-zero-uniqueness-carrier}. The shared data $B,M,S$ is the same displayed located-zero route named in \autoref{ch:concrete-instances-bishop-located-zero-namecert}, so the comparison cannot begin from two unrelated host roots.

The two witness rows are then read symmetrically. Rows $Z_0,Z_1$ display the two zero witnesses, $W_0,W_1$ expose the finite stream windows, $R_0,R_1$ give the regular-rational readbacks, and $D$ supplies the dyadic tolerance ledger consumed by the comparison row $E$.

The structural rows $H,C,P,N$ transport, replay, source, and name that same finite route. Since the carrier has no root-selection, trichotomy, choice, compactness, host-equality, quotient-equality, or completeness coordinate, none of those objects can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Located zero separated comparison envelope]
\label{thm:located-zero-uniqueness-separation-envelope}
Let $U=(B,M,S,Z_0,Z_1,W_0,W_1,R_0,R_1,D,E,H,C,P,N)$ be an accepted $\LocatedZeroUniquenessUp$ carrier. Every downstream consumer that compares the two displayed zero reads must factor the comparison through the common $\BishopLocatedZeroUp$ route $B,M,S$, the two displayed zero-witness rows $Z_0,Z_1$, the two $\StreamNameUp$ and $\RegSeqRatUp$ readbacks $W_0,W_1,R_0,R_1$, the dyadic tolerance ledger $D$, and the $\RealEqualityUp$ or $\SeparatedMetricUp$ comparison row $E$ before it reaches transport, replay, provenance, and naming rows. The exported result is classifier equality over this displayed separated-zero route, not host equality of roots or a selected global zero.
\end{theorem}
\begin{proof}
Start with the shared located-zero route. Rows $B,M,S$ are the only admissible bracket, modulus, and bisection schedule for both displayed witnesses, so a consumer cannot substitute an ambient intermediate-value theorem or a separate root-selection principle.

Next read both witnesses through the same finite observation discipline. The rows $Z_0,Z_1$ provide the two zero-witness displays, $W_0,W_1$ and $R_0,R_1$ expose the finite stream and regular-rational routes, and $D$ records the tolerance ledger.

Finally apply the separated comparison row $E$ and carry the result through $H,C,P,N$. The comparison is the same kind of displayed equality boundary isolated by \autoref{ch:concrete-instances-real-equality-namecert} and the separated classifier discipline of \autoref{ch:concrete-instances-separatedmetric-namecert}; it does not export host root equality, trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedZeroUniquenessUp$ consumer that compares two displayed zero reads while omitting the shared BishopLocatedZero bracket route, one of the two zero-witness rows, one of the StreamName or RegSeqRat readbacks, the DyadicRatCore tolerance ledger, the separated comparison row, hsame, Cont, Pkg, or NameCert has left the packet.}

\independenceWitness{bishoplocatedzero, realequality, separatedmetric, streamname, regseqrat, dyadicratcore: $\LocatedZeroUniquenessUp$ is not bijective with any listed sibling because it joins one shared located-zero route, two displayed zero-witness rows, paired readbacks, a dyadic comparison ledger, separated classifier equality, transport, replay, provenance, and local naming as one finite packet.}

\closureat{LocatedZeroUniquenessUp}{seedStr}

\begin{closurestatus}{\LocatedZeroUniquenessUp}
  \constructivestory{A $\LocatedZeroUniquenessUp$ packet is generated from one shared BishopLocatedZeroUp bracket, modulus, and bisection schedule, two displayed zero-witness rows, paired StreamName and RegSeqRat readbacks, a DyadicRatCore tolerance ledger, a RealEqualityUp or SeparatedMetricUp comparison row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-zero-uniqueness-carrier}, \autoref{thm:located-zero-uniqueness-obligations}, and \autoref{thm:located-zero-uniqueness-separation-envelope} seed the finite classifier route for comparing two displayed located-zero reads over one shared L10 bracket surface.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.LocatedZeroUniquenessUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host equality of roots, a selected global zero, trichotomy, quotient real equality, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification of the obligation theorems, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared bracket admission, two zero-witness rows, paired readbacks, dyadic comparison accounting, separated classifier comparison, transport, replay, provenance, and local naming for BEDC.Derived.LocatedZeroUniquenessUp.}
\end{closurestatus}
