\chapter{A Concrete Naming Certificate for $\FinitePrimeWindowEnergyUp$}
\label{ch:concrete-instances-finite-prime-window-energy-namecert}
\origin{ai}

The $\FinitePrimeWindowEnergyUp$ packet realizes the finite Hamiltonian prime layer of \autoref{sec:unified-reality-finite-prime-tower-completed-readout} as concrete NameCert data. It reads displayed prime windows through \autoref{ch:concrete-instances-prime-namecert} and \autoref{ch:concrete-instances-finite-prime-window-gluing-namecert}, zeta-facing readout rows through \autoref{ch:concrete-instances-zeta-basic-namecert}, and spectral comparison demand through \autoref{ch:concrete-instances-spectraltheorem-namecert}. The packet names a finite occupation carrier, logarithmic prime-frequency weights, centered trace readout, and unitary fixed-half boundary; it does not claim $\mathsf{RH}$, an infinite spectral theorem, Euler-product completion, analytic continuation, or a selected limiting prime window.

\begin{definition}[Finite prime-window energy carrier]
\label{def:finite-prime-window-energy-carrier}
A $\FinitePrimeWindowEnergyUp$ carrier is a finite $\BHist$ packet
$$
E=(S,M,K,L,H,Z,U,B,T,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite nonempty $\PrimeUp$ window row, carrying the displayed prime list and the window-inclusion provenance consumed by $\FinitePrimeWindowGluingUp$.
\item $M$ is the finite occupation-depth row.
\item $K$ is the occupation carrier row for $\mathcal K_{S,M}$, storing one finite basis window for each prime row of $S$.
\item $L$ is the logarithmic frequency row, assigning the displayed weight $\log p$ to each prime row without adding an analytic completion.
\item $H$ is the Hamiltonian row for $H_{S,M}$, computed as the finite weighted sum of local occupation-number rows.
\item $Z$ is the partition-readout row for $Z_{S,M}(s)$ and the centered readout row for $\widetilde Z_{S,M}(s)$.
\item $U$ is the unitary fixed-half row: at $s=1/2+i\tau$ it records only phase channels $p^{-i\tau}$, while off the fixed-half surface it records the displayed radial bias $p^{-\delta}$.
\item $B$ is the boundary ledger separating finite energy unitarity from $\mathsf{RH}$, nontrivial zero exclusion, analytic continuation, Euler-product completion, and infinite spectral closure.
\item $T$ records componentwise $\hsame$ transport for the window, depth, occupation carrier, frequency, Hamiltonian, readout, unitary boundary, ledger, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from a finite prime-window energy request through $S,M,K,L,H,Z,U,B,T$.
\item $P$ is the $\Pkg$ provenance over $\FinitePrimeWindowEnergyUp$, $\PrimeUp$, $\FinitePrimeWindowGluingUp$, $\ZetaUp$, $\SpectralTheoremUp$, $\ComplexUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FinitePrimeWindowEnergyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a completed prime tower, global zeta recovery, host Hilbert-space completion, selected infinite tensor product, or proof of the Riemann hypothesis.
\end{definition}

\begin{theorem}[Finite prime-window energy NameCert obligations]
\label{thm:finite-prime-window-energy-namecert-obligations}
For every accepted $\FinitePrimeWindowEnergyUp$ carrier
$$
E=(S,M,K,L,H,Z,U,B,T,R,P,N),
$$
the local $\NameCert_{\FinitePrimeWindowEnergyUp}$ surface consists exactly of finite prime-window admission, occupation-depth admission, finite occupation carrier exposure, logarithmic frequency exposure, Hamiltonian finite-sum computation, partition and centered trace readout, unitary fixed-half boundary, explicit refusal of non-finite zeta conclusions, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Later Lean formalization must make the nonempty-window premise explicit as a `Nontrivial` instance for the finite occupation carrier and must make the readout-faithfulness premise explicit as a `FieldFaithful` instance tying the finite trace row to the displayed prime-frequency field.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-prime-window-energy-carrier}. The rows $S,M,K,L,H,Z,U,B,T,R,P,N$ are the full public inventory of the packet.

The energy stage is finite. The prime window $S$ and depth row $M$ determine the occupation basis row $K$, the frequency row $L$, and the Hamiltonian finite-sum row $H$.

The readout stage is $Z,U,B$. The trace row $Z$ exposes the partition and centered readouts, the unitary row $U$ separates the fixed-half phase channel from off-surface radial bias, and the boundary ledger $B$ prevents this finite readout from being consumed as a completed zeta theorem.

Finally $T,R,P,N$ transport, replay, source, and name the same rows. Since the packet contains no completed prime tower, host Hilbert completion, selected infinite tensor product, global zeta recovery, or RH proof coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Finite prime-window energy unitary boundary]
\label{thm:finite-prime-window-energy-unitary-boundary}
Every $\ZetaUp$-, $\SpectralTheoremUp$-, or finite-prime-window-facing consumer of an accepted $\FinitePrimeWindowEnergyUp$ packet must read $S,M,K,L,H,Z,U,B,T,R,P,N$ before using the centered Hamiltonian energy boundary. The consumer may use the fixed-half row as a finite unitary phase surface and may use the off-surface row as a finite radial-bias witness; it may not infer nontrivial zero exclusion, analytic continuation, Euler-product completion, or an infinite spectral model.
\end{theorem}
\begin{proof}
Start with the obligation theorem \autoref{thm:finite-prime-window-energy-namecert-obligations}. It forces every consumer to expose the finite prime window, occupation depth, occupation carrier, frequency row, Hamiltonian row, and readout row before any boundary interpretation is available.

At the fixed-half surface, $U$ contains only phase channels. Off that surface, $U$ records the displayed radial bias and leaves it in the boundary ledger $B$ rather than erasing it by appeal to a hidden endpoint.

Transport and replay preserve the same finite packet. Therefore a zeta-facing or spectral-facing consumer receives a finite energy-unitarity boundary, not a proof of any excluded global conclusion.
\end{proof}

\falsifiablePrediction{An accepted $\FinitePrimeWindowEnergyUp$ consumer that reaches a $\ZetaUp$ or $\SpectralTheoremUp$ readout without first displaying the finite prime-window row $S$, occupation carrier $K$, Hamiltonian row $H$, centered readout $Z$, and boundary ledger $B$ refutes the packet.}

\independenceWitness{PrimeUp, ZetaUp, SpectralTheoremUp, and FinitePrimeWindow-style siblings: $\FinitePrimeWindowEnergyUp$ is not bijective to any listed sibling because it jointly carries finite prime-window rows, occupation-depth rows, Hamiltonian finite-sum rows, centered trace readout, unitary fixed-half boundary, explicit radial-bias ledger, transport, replay, provenance, and local naming while refusing completed zeta and infinite spectral conclusions.}

\closureat{FinitePrimeWindowEnergyUp}{seedStr}

\begin{closurestatus}{\FinitePrimeWindowEnergyUp}
  \constructivestory{A $\FinitePrimeWindowEnergyUp$ packet is generated from a finite prime-window row, occupation-depth row, finite occupation carrier, logarithmic frequency row, Hamiltonian finite-sum row, partition and centered trace readout, unitary fixed-half boundary, boundary ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-prime-window-energy-carrier}, \autoref{thm:finite-prime-window-energy-namecert-obligations}, and \autoref{thm:finite-prime-window-energy-unitary-boundary} seed the finite Hamiltonian prime-window energy readout and its unitary boundary over prime, zeta, and spectral siblings.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RH, nontrivial zero exclusion, analytic continuation, Euler-product completion, infinite spectral completeness, selected limiting windows, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nonempty-window exposure, Hamiltonian finite-sum computation, centered trace readout, unitary boundary exactness, Nontrivial instance coverage, FieldFaithful instance coverage, transport, replay, provenance, and local naming for BEDC.Derived.FinitePrimeWindowEnergyUp.}
\end{closurestatus}
