\chapter{A Concrete Naming Certificate for $\FabryGapTheoremUp$}
\label{ch:concrete-instances-fabry-gap-theorem-namecert}
\origin{human}

Following Fabry and classical lacunary power-series theory, the $\FabryGapTheoremUp$ packet records a finite boundary route for a power series whose visible coefficients occur along a gap schedule. It sits beside the Hadamard gap series packet of \autoref{ch:concrete-instances-hadamard-gap-series-namecert}, the Cauchy--Hadamard radius row of \autoref{ch:concrete-instances-cauchy-hadamard-namecert}, the convergence-radius route of \autoref{ch:concrete-instances-convergence-radius-namecert}, the regular Cauchy root-test row of \autoref{ch:concrete-instances-regular-cauchy-root-test-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite route by which lacunary exponent windows, coefficient windows, radius data, root-test budgets, boundary probes, witness windows, exclusion rows, transport, replay, provenance, and local naming are consumed before any boundary conclusion can be read.

\begin{definition}[Fabry gap theorem carrier]
\label{def:fabry-gap-theorem-carrier}
A $\FabryGapTheoremUp$ carrier is a finite $\BHist$ packet
$$
F=(G,C,R,T,B,W,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the lacunary exponent-gap ledger, recording inspected exponent positions, adjacent growth comparisons, and the finite gap evidence used by the route.
\item $C$ is the coefficient-window row, naming the visible coefficient slots, zero-window declarations, and coefficient bounds attached to the inspected exponents.
\item $R$ is the radius row, read through the Cauchy--Hadamard and convergence-radius surfaces.
\item $T$ is the regular Cauchy root-test handoff, naming the finite root-bound and tail-budget rows used before a boundary request is answered.
\item $B$ is the boundary-probe ledger, listing the displayed boundary arc, direction, and tolerance requests that the finite route is allowed to inspect.
\item $W$ is the witness-window row, tying each accepted boundary request to the displayed lacunary exponent and coefficient windows.
\item $E$ is the exclusion row, recording that the carrier exports no host analytic continuation, no natural-boundary theorem, no completed complex function, and no selected infinite sum.
\item $H$ records componentwise $\hsame$ transport for the gap ledger, coefficient windows, radius row, root-test handoff, boundary probes, witness windows, exclusion row, replay, provenance, and local naming.
\item $K$ records displayed $\Cont$ replay from a boundary or coefficient request through $G,C,R,T,B,W,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FabryGapTheoremUp$, $\HadamardGapSeriesUp$, $\CauchyHadamardUp$, $\ConvergenceRadiusUp$, $\RegularCauchyRootTestUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FabryGapTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $G,C,R,T,B,W,E,H,K,P,N$. It has no coordinate for host analytic continuation, a natural-boundary theorem, a completed complex analytic function, quotient stream equality, selected infinite sums, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Fabry gap theorem NameCert obligations]
\label{thm:fabry-gap-theorem-namecert-obligations}
For every accepted $\FabryGapTheoremUp$ carrier
$$
F=(G,C,R,T,B,W,E,H,K,P,N),
$$
the local $\NameCert_{\FabryGapTheoremUp}$ surface consists exactly of carrier admission, lacunary exponent-gap exactness, coefficient-window stability, radius-row handoff, regular Cauchy root-test routing, boundary-probe scoping, witness-window exactness, exclusion-row nonescape, componentwise transport, displayed replay, provenance, and local naming through $G,C,R,T,B,W,E,H,K,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The gap ledger $G$ and coefficient row $C$ are the only rows that can name exponent spacing, visible coefficient slots, and zero-window declarations. The rows $R$ and $T$ then expose the radius and regular root-test handoff used by the finite route.

The boundary-facing data are downstream. Row $B$ supplies the displayed boundary request, row $W$ ties that request to the inspected lacunary windows, and row $E$ states the excluded host principles.

Finally, $H,K,P,N$ transport, replay, source, and name the same finite packet. Since the carrier contains no analytic-continuation or natural-boundary coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Fabry lacunary boundary route]
\label{thm:fabry-gap-theorem-lacunary-boundary-route}
Every boundary-facing read of an accepted $\FabryGapTheoremUp$ packet factors through the finite route
$$
G,C \longmapsto R,T \longmapsto B,W \longmapsto E .
$$
Thus the packet exposes a lacunary boundary route from displayed exponent and coefficient windows to radius, root-test, boundary-probe, witness, and exclusion rows, not a host analytic-continuation theorem, not a natural-boundary theorem, and not a completed power-series function.
\end{theorem}
\begin{proof}
Begin with a boundary-facing request. The carrier permits it to inspect the lacunary exponent ledger $G$ and coefficient-window row $C$ before any radius or root-test information is available.

The request then reaches the radius and root-test rows $R,T$. Only after those rows have been displayed can the boundary-probe row $B$ and witness-window row $W$ be consumed.

The exclusion row $E$ records the nonescape boundary of this route, while $H,K,P,N$ preserve, replay, source, and name the same finite data. No row supplies host analytic continuation, a natural-boundary theorem, or a completed power-series function.
\end{proof}

\falsifiablePrediction{Every accepted $\FabryGapTheoremUp$ endpoint read factors through lacunary exponent-gap data, coefficient windows, Cauchy--Hadamard radius rows, convergence-radius rows, regular Cauchy root-test handoffs, boundary-probe ledgers, witness windows, exclusion rows, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{hadamard-gap-series, cauchy-hadamard, convergence-radius, regular-cauchy-root-test, real: HadamardGapSeriesUp supplies lacunary coefficient windows without the Fabry boundary-probe and witness-window rows; CauchyHadamardUp and ConvergenceRadiusUp supply radius rows without a lacunary boundary route; RegularCauchyRootTestUp supplies root-test budgets without boundary probes; RealUp supplies terminal seals without exponent-gap evidence. None is bijective to $\FabryGapTheoremUp$.}

\closureat{FabryGapTheoremUp}{seedStr}

\begin{closurestatus}{\FabryGapTheoremUp}
  \constructivestory{A $\FabryGapTheoremUp$ packet is generated from lacunary exponent-gap ledgers, coefficient-window rows, Cauchy--Hadamard radius data, convergence-radius routing, regular Cauchy root-test handoffs, boundary-probe ledgers, witness windows, exclusion rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fabry-gap-theorem-carrier}, \autoref{thm:fabry-gap-theorem-namecert-obligations}, and \autoref{thm:fabry-gap-theorem-lacunary-boundary-route} seed the finite lacunary boundary route from exponent and coefficient windows to radius, root-test, boundary-probe, witness, and exclusion rows.}
  \formalstatus{\unformalizedV}
  \leanstmt{BEDC.Derived.FabryGapTheoremUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host analytic continuation, a natural-boundary theorem, a completed complex analytic function, quotient stream equality, selected infinite sums, countable choice, ambient RealUp completeness, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, lacunary exponent-gap exactness, coefficient-window stability, radius-row handoff, root-test routing, boundary-probe scoping, witness-window exactness, exclusion-row nonescape, transport, replay, provenance, and local naming for BEDC.Derived.FabryGapTheoremUp.}
\end{closurestatus}
