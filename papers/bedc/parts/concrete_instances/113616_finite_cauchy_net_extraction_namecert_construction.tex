\chapter{A Concrete Naming Certificate for $\FiniteCauchyNetExtractionUp$}
\label{ch:concrete-instances-finite-cauchy-net-extraction-namecert}
\origin{human}

Following Bishop and Bridges, the $\FiniteCauchyNetExtractionUp$ packet records the finite Cauchy-window extraction that a constructive Real and Completion route may inspect before any compactness or limit principle is available. It sits over the dyadic tolerance surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite schedule surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet extracts a displayed finite net row from a Cauchy window; it is not an open-cover compactness theorem, not a selected limit construction, and not a countable-choice schedule.

\begin{definition}[Finite Cauchy net extraction carrier]\label{def:finite-cauchy-net-extraction-carrier}
A $\FiniteCauchyNetExtractionUp$ carrier is a finite $\BHist$ packet
$$
F=(D,W,Q,M,T,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the precision request and the dyadic comparison ledger used to judge the finite net.
\item $W$ is the $\StreamNameUp$ finite-window schedule naming the observed stream positions available at that tolerance.
\item $Q$ is the $\RegSeqRatUp$ readback row through which the scheduled window is interpreted as regular rational Cauchy data.
\item $M$ is the $\RealModulusOfConvergenceUp$ ledger recording that the chosen window is large enough for the displayed tolerance request.
\item $T$ is the $\TotallyBoundedUp$ finite-net row extracted from the displayed Cauchy window and indexed only by the entries named in $W$.
\item $E$ is the $\RealUp$ seal handoff exposed only after $D,W,Q,M,T$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the dyadic tolerance, finite-window schedule, regular rational readback, modulus ledger, finite-net row, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite Cauchy-window request through $D,W,Q,M,T,E,H$ and into a Real or Completion consumer.
\item $P$ is the $\Pkg$ provenance over $\FiniteCauchyNetExtractionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealModulusOfConvergenceUp$, $\TotallyBoundedUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteCauchyNetExtractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,W,Q,M,T,E,H,C,P,N$. It has no coordinate for open-cover compactness, a selected Cauchy limit, quotient stream equality, countable choice, host real equality, or ambient Real completeness.
\end{definition}

\begin{theorem}[Finite Cauchy net extraction NameCert obligations]\label{thm:finite-cauchy-net-extraction-namecert-obligations}
For every accepted $\FiniteCauchyNetExtractionUp$ carrier $F=(D,W,Q,M,T,E,H,C,P,N)$, the local $\NameCert_{\FiniteCauchyNetExtractionUp}$ surface consists exactly of carrier admission, dyadic tolerance exposure, finite-window schedule readback, regular rational handoff, convergence-modulus ledger exactness, displayed finite-net extraction, Real seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every Real or Completion consumer must read the displayed route
$$
D,W,Q,M \longmapsto T \longmapsto E \longmapsto H,C,P,N
$$
before using a finite net as Cauchy-window evidence.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-cauchy-net-extraction-carrier}. The dyadic row $D$ supplies the tolerance request, and the finite schedule $W$ supplies the only stream entries that may be inspected.

Read $Q$ and $M$ next. The regular rational row $Q$ interprets the scheduled entries as Cauchy data, while the $\RealModulusOfConvergenceUp$ ledger $M$ records that the displayed window is large enough for the request named by $D$.

The finite-net row $T$ is then extracted from those displayed entries only. The Real seal $E$ and the structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since no carrier coordinate supplies open-cover compactness, selected limits, quotient stream equality, countable choice, host real equality, or ambient Real completeness, none of those objects can occur in the local NameCert surface.
\end{proof}

\begin{theorem}[Finite Cauchy net extraction L10 route]\label{thm:finite-cauchy-net-extraction-l10-route}
Every L10 Real and Completion consumer of an accepted $\FiniteCauchyNetExtractionUp$ packet reads the extracted net only through the finite route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealModulusOfConvergenceUp,\ \TotallyBoundedUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The route exposes the dyadic tolerance, the scheduled Cauchy window, the regular rational readback, the convergence-modulus ledger, the finite-net row, the terminal Real seal, componentwise transport, replay, provenance, and local naming. It exports no open-cover compactness, selected limit, quotient stream equality, countable-choice schedule, host real equality, detached metric-completion row, or ambient Real completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:finite-cauchy-net-extraction-namecert-obligations}. It fixes the only public route from the tolerance request through the scheduled window, regular rational readback, modulus ledger, finite-net row, and Real seal.

For a Real-facing consumer, the seal $E$ is reached only after $D,W,Q,M,T$ have already been displayed. Thus the consumer receives the finite net as an inspected Cauchy-window witness, not as a host-selected limit or quotient representative.

For a Completion-facing consumer, the same rows are replayed through $H,C,P,N$. The replay row can cite the displayed $\TotallyBoundedUp$ net row and the $\RealModulusOfConvergenceUp$ ledger, but it cannot add an open-cover compactness proof, countable choice, detached metric-completion row, or ambient Real completeness, because those data are absent from the carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteCauchyNetExtractionUp$ Real or Completion consumer factors through one $\DyadicRatCoreUp$ tolerance row, one $\StreamNameUp$ finite-window schedule, one $\RegSeqRatUp$ readback row, one $\RealModulusOfConvergenceUp$ ledger, one $\TotallyBoundedUp$ finite-net row, one $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before it may use finite Cauchy-window evidence.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real-modulus-of-convergence, totallybounded, real: $\FiniteCauchyNetExtractionUp$ is not bijective to any listed sibling because the siblings separately supply tolerance comparisons, finite schedules, rational readback, convergence ledgers, finite nets, and terminal Real sealing, while this packet binds them to the finite Cauchy-window extraction consumed by the L10 Real and Completion route.}

\closureat{FiniteCauchyNetExtractionUp}{seedStr}

\begin{closurestatus}{\FiniteCauchyNetExtractionUp}
  \constructivestory{A $\FiniteCauchyNetExtractionUp$ packet is generated from a DyadicRatCore tolerance row, a StreamName finite-window schedule, a RegSeqRat readback row, a RealModulusOfConvergence ledger, a TotallyBounded finite-net row, a Real seal handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-cauchy-net-extraction-carrier}, \autoref{thm:finite-cauchy-net-extraction-namecert-obligations}, and \autoref{thm:finite-cauchy-net-extraction-l10-route} seed the finite Cauchy-window extraction interface for the L10 Real and Completion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, selected limits, quotient stream equality, countable choice, host real equality, Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exposure, finite-window scheduling, RegSeqRat readback, convergence-modulus ledger exactness, finite-net extraction, Real seal handoff, structural transport, provenance non-escape, and local NameCert exactness for BEDC.Derived.FiniteCauchyNetExtractionUp.}
\end{closurestatus}
