\chapter{A Concrete Naming Certificate for $\GrussInequalityUp$}
\label{ch:concrete-instances-gruss-inequality-namecert}
\origin{human}

Following Gruss and Bishop--Bridges constructive analysis, the $\GrussInequalityUp$ packet records the finite BEDC route by which a bounded pair of integrable real-valued maps on a displayed compact metric source yields a covariance-bound certificate. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-name window of \autoref{ch:concrete-instances-streamname-namecert}, the continuous-map graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the integral carrier of \autoref{ch:concrete-instances-integral-namecert}, and the compact metric source rows of \autoref{ch:concrete-instances-compactmetric-namecert}. The packet names a finite covariance-bound route; it does not assert host equality of functions, measure completion, selected extrema, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Gruss inequality covariance-bound carrier]
\label{def:gruss-inequality-carrier}
A $\GrussInequalityUp$ carrier is a finite $\BHist$ packet
$$
G=(K,F,L,B,I,Q,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, naming the compact metric domain together with the finite observation covers consumed by the bound request.
\item $F$ is the paired $\ContinuousMapUp$ graph row for the two real-valued maps whose covariance is being bounded.
\item $L$ is the value-window row, recording displayed lower and upper Real bounds for each map.
\item $B$ is the bounded-oscillation budget row, exposing the four endpoint differences used by the Gruss constant.
\item $I$ is the $\IntegralUp$ row for the four integral reads: the two single-map reads, the product read, and the normalization read of the compact source.
\item $Q$ is the $\RegSeqRatUp$ readback row for the endpoint differences, integral values, product-integral comparison, and tolerance budget.
\item $W$ is the $\StreamNameUp$ finite-window row through which the endpoint, graph, and integral reads are observed.
\item $E$ is the terminal $\RealUp$ seal for the displayed covariance-bound value.
\item $H$ records componentwise $\hsame$ transport for the compact source, paired maps, bounds, oscillation budget, integral rows, readback, stream window, Real seal, replay, provenance, and local name.
\item $C$ records displayed $\Cont$ replay through $K,F,L,B,I,Q,W,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\GrussInequalityUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\IntegralUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\GrussInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,L,B,I,Q,W,E,H,C,P,N$. There is no coordinate for host function equality, selected extrema, a measure-completion object, countable choice, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Gruss inequality NameCert obligations]
\label{thm:gruss-inequality-namecert-obligations}
For every accepted $\GrussInequalityUp$ carrier $G=(K,F,L,B,I,Q,W,E,H,C,P,N)$, the local $\NameCert_{\GrussInequalityUp}$ surface consists exactly of carrier admission for the compact metric source, paired continuous-map graphs, displayed lower and upper value bounds, bounded-oscillation budget, integral readback, regular rational comparison, finite stream observation, terminal Real sealing, componentwise transport, displayed replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:gruss-inequality-carrier}. The analytic rows are the compact source $K$, the paired graph row $F$, the bound row $L$, the oscillation budget $B$, and the integral row $I$.

Readback and observation are downstream. The row $Q$ repacks only the displayed endpoint differences and integral comparisons, while $W$ fixes the finite observation window and $E$ seals the resulting Real-bound value.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no host function-equality, selected-extremum, measure-completion, countable-choice, or ambient-completeness coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Gruss inequality integral covariance bound]
\label{thm:gruss-inequality-integral-covariance-bound}
Every accepted $\GrussInequalityUp$ packet presents the covariance read through the finite route
$$
K,F,L,B,I \longmapsto Q,W,E,H,C,P,N .
$$
The bound compares the displayed integral covariance only with the Real-sealed budget
$$
\frac{1}{4}(L_f^{+}-L_f^{-})(L_g^{+}-L_g^{-}),
$$
where the four endpoint values are the rows stored in $L$ and the comparison is read through $Q,W,E$. A consumer receives no covariance estimate unless the compact metric source, the two continuous-map rows, the bounded windows, and the integral rows have all been exposed.
\end{theorem}
\begin{proof}
Start from $K,F,L,B,I$. The compact metric source and paired continuous graphs are displayed before any integral read is consumed, and the bound row records the lower and upper windows used by the Gruss constant.

Use the integral row $I$ to expose the two single-map integral reads, the product read, and the normalization read. The row $B$ supplies only the four endpoint differences already present in $L$.

The regular rational row $Q$ compares these integral reads with the displayed quarter-product budget, the stream row $W$ records the finite observation window, and $E$ seals the comparison as a Real-facing value. Transport, replay, provenance, and naming through $H,C,P,N$ preserve the same route for downstream consumers.
\end{proof}

\begin{theorem}[Gruss inequality L10 nonescape]
\label{thm:gruss-inequality-l10-nonescape}
Every L10 consumer of an accepted $\GrussInequalityUp$ packet must factor through
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \IntegralUp,\ \RegSeqRatUp,\ \StreamNameUp,\ \RealUp
\longmapsto
K,F,L,B,I,Q,W,E,H,C,P,N .
$$
Thus the packet exports a finite covariance-bound certificate, while excluding host function equality, selected extrema, detached measure completion, countable choice, quotient equality of real names, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply the obligation theorem \autoref{thm:gruss-inequality-namecert-obligations}. It forces any consumer to read the compact metric source, paired continuous-map graphs, bound rows, integral rows, regular rational comparisons, stream observation, Real sealing, transport, replay, provenance, and local name.

Apply the covariance-bound theorem \autoref{thm:gruss-inequality-integral-covariance-bound}. The only Real-facing estimate is the displayed quarter-product route; no independent equality or selection row appears in the carrier.

The excluded principles are absent from the carrier coordinates. Therefore an L10 consumer can replay the finite covariance-bound certificate but cannot extract host function equality, selected extrema, a detached measure-completion object, countable choice, quotient equality of real names, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\GrussInequalityUp$ read must display a CompactMetric source, paired ContinuousMap graphs, lower and upper value windows, an oscillation budget, Integral rows, RegSeqRat comparison, StreamName observation, Real sealing, hsame transport, Cont replay, Pkg provenance, and local NameCert before any covariance-bound value is consumed.}

\independenceWitness{real, regseqrat, streamname, continuousmap, integral, compactmetric: $\GrussInequalityUp$ is not bijective to any listed sibling because it jointly carries compact-source rows, paired graph rows, bound windows, covariance integral reads, rational comparison, stream observation, Real sealing, transport, replay, provenance, and local naming.}

\closureat{GrussInequalityUp}{seedStr}

\begin{closurestatus}{\GrussInequalityUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:gruss-inequality-carrier}, \autoref{thm:gruss-inequality-namecert-obligations}, \autoref{thm:gruss-inequality-integral-covariance-bound}, and \autoref{thm:gruss-inequality-l10-nonescape} seed the constructive covariance-bound route over compact metric source rows, paired continuous-map graphs, displayed bounds, integral reads, rational comparison, stream observation, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host equality of functions, selected extrema, detached measure completion, countable choice, quotient equality of real names, ambient Real completeness, Lean verification, or bridge checking for $\GrussInequalityUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, bound-window exactness, integral covariance readback, rational comparison, Real-seal nonescape, transport stability, and local NameCert export for BEDC.Derived.GrussInequalityUp.}
  \constructivestory{A $\GrussInequalityUp$ packet is generated from a compact metric source, paired continuous-map graphs, lower and upper value windows, a bounded-oscillation budget, integral rows, regular rational readback, stream observation, Real sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
