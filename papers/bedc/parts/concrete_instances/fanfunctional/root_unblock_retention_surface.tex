\begin{theorem}[FanFunctional root finite-cover retention]\label{thm:fan-functional-root-finite-cover-retention}
For every accepted $\FanFunctionalUp$ carrier $\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)$, the root finite-cover read retains the displayed $\CantorSpaceUp$ prefix row $C$, $\FanTheoremUp$ finite-bar row $F$, $\CompactMetricUp$ finite-net consumer row, $\UniformContinuityUp$ tolerance row, $\UniformModulusUp$ handoff row $M$, $\BHist$ packet route, componentwise $\hsame$ row $H$, displayed $\Cont$ replay row $K$, $\Pkg$ provenance row $P$, and local $\NameCert_{\FanFunctionalUp}$ row $N$ on the same packet before any modulus consumer may inspect $M$. The retention surface has no selected branch, host functional, open-cover compactness proof, countable-choice selector, or ambient Real-completeness coordinate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-functional-carrier-surface}. The rows $C,F,\varepsilon,B,D,W,M,H,K,P,N$ are the only available root coordinates, and the compact-metric and uniform-continuity consumers read them as finite $\BHist$ data.

The finite-cover side is retained by $F,B,D,W$. These rows expose the finite fan depth, retained prefix window, and witness window before the extracted $\UniformModulusUp$ row can be consumed.

Finally $H,K,P,N$ transport, replay, source, and name the same packet. Since selected branches, host functionals, open covers, choice schedules, and Real-completeness principles have no coordinate, the root read cannot leave the displayed finite-cover surface.
\end{proof}

\begin{theorem}[FanFunctional root modulus-row totality]\label{thm:fan-functional-root-modulus-row-totality}
Every accepted root read of $\FanFunctionalUp$ exposes a total displayed route from $\CantorSpaceUp$ and $\FanTheoremUp$ through $\CompactMetricUp$, $\UniformContinuityUp$, and $\UniformModulusUp$ before exporting the modulus row. Concretely, the packet route through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ must show $C,F,\varepsilon,B,D,W,M,H,K,P,N$ together; omitting any one of the Cantor, fan, compact-metric, uniform-continuity, or modulus rows leaves the accepted carrier.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:fan-functional-namecert-obligations}. They separate carrier admission, classifier stability, bar-depth exactness, witness-window coverage, uniform-modulus extraction, and non-escape as rows of one finite packet.

The modulus row is downstream from the other root rows. The Cantor and fan rows fix the finite prefix and bar-depth surface, while the compact-metric and uniform-continuity rows consume only the displayed finite windows and tolerance request.

The structural route through $H,K,P,N$ is total for that packet. A read missing one displayed row cannot reconstruct it from a selected branch, host functional, open-cover compactness proof, countable-choice schedule, or Real-completeness principle, because none of those objects is part of the carrier.
\end{proof}

\begin{theorem}[FanFunctional root choice boundary]\label{thm:fan-functional-root-choice-boundary}
No accepted $\FanFunctionalUp$ root obligation may replace the finite packet route through $\CantorSpaceUp$, $\FanTheoremUp$, $\CompactMetricUp$, $\UniformContinuityUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ by a branch selector, host functional, open-cover compactness theorem, countable-choice schedule, or ambient Real-completeness argument. The only admissible root boundary is the displayed carrier $\Phi=(C,F,\varepsilon,B,D,W,M,H,K,P,N)$ and its componentwise transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Use \autoref{def:fan-functional-carrier-surface}. The accepted carrier lists every coordinate available to a root obligation, including the Cantor prefix, finite fan, compact-metric, uniform-continuity, uniform-modulus, transport, replay, provenance, and naming rows.

Apply the root schema ledger of \autoref{thm:fanfunctional-root-schema-ledger}. Downstream consumers must pass through the compact and uniform-modulus route displayed by $\Omega$, hence through the same finite root boundary.

Coordinate exhaustion closes the choice boundary. A selected branch, host functional, open-cover compactness proof, countable-choice selector, or ambient Real-completeness argument would require a coordinate not present in $\Phi$, so such a read is outside the accepted $\FanFunctionalUp$ packet.
\end{proof}
