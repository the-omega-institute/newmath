\begin{theorem}[FanFunctional root-unblock Cantor prefix]
\label{thm:fanfunctional-root-unblock-cantor-prefix}
Every root-unblock read of an accepted $\FanFunctionalUp$ packet must first admit the displayed $\CantorSpaceUp$ finite prefix surface. Concretely, the read exposes the List spine, Bool-valued finite observation windows, StreamName schedule, and the surrounding $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before any fan-depth or modulus consumer can inspect the packet.
\end{theorem}
\leanchecked{BEDC.Derived.FanfunctionalUp.FanFunctionalRootUnblockCantorPrefix}
\begin{proof}
Project the carrier of \autoref{def:fan-functional-carrier-surface} and the finite-window carrier of \autoref{def:fanfunctional-finite-window-carrier}. Their Cantor-facing coordinates are finite prefix data: List spines, Bool ledgers, StreamName windows, and the structural transport rows.

The root schema carrier of \autoref{thm:fanfunctional-root-schema-carrier} reads those coordinates before the compact and uniform-modulus rows. Thus a root-unblock consumer receives the Cantor prefix as displayed finite data, not as an infinite branch or host functional.

Finally apply the root choice boundary of \autoref{thm:fan-functional-root-choice-boundary}. Since no selector, open-cover proof, or ambient Real-completeness row is present, the Cantor prefix admission remains inside the accepted packet.
\end{proof}

\begin{theorem}[FanFunctional root-unblock finite fan depth]
\label{thm:fanfunctional-root-unblock-fan-depth}
Every root-unblock read of $\FanFunctionalUp$ exposes the $\FanTheoremUp$ finite fan-depth row through the displayed UnaryHistory depth ledger before witness-window coverage or uniform-modulus handoff is consumed. The depth read is a finite carrier projection over the same Cantor-prefix rows; it is not a bar-induction principle, selected branch argument, or open-cover compactness claim.
\end{theorem}
\leanchecked{BEDC.Derived.FanfunctionalUp.FanFunctionalRootUnblockFanDepth}
\begin{proof}
Read the finite fan rows from \autoref{def:fan-functional-carrier-surface}. The row $F$ supplies the decidable bar classifier, and the row $D$ supplies the finite depth evidence attached to the retained branch-prefix window.

Use \autoref{thm:fan-functional-root-finite-bar-depth} and \autoref{thm:fanfunctional-root-bar-depth-exactness}. Together they place the UnaryHistory depth after the Cantor-prefix rows and before any witness-window or modulus read.

The route is preserved by componentwise $\hsame$, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. Coordinate exhaustion then excludes bar induction, selected branches, host functionals, and open-cover compactness from the fan-depth exposure.
\end{proof}

\begin{theorem}[FanFunctional root-unblock uniform modulus handoff]
\label{thm:fanfunctional-root-unblock-uniform-modulus}
Every root-unblock consumer that reads the $\UniformModulusUp$ row of an accepted $\FanFunctionalUp$ packet must keep the route through $\CompactMetricUp$, $\UniformContinuityUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ visible. The handoff uses the displayed Cantor prefix, finite fan depth, tolerance request, compact finite-window consumer row, and witness windows; it exports no selected branch, host functional, bar-induction principle, open-cover compactness proof, quotient stream equality, or ambient Real-completeness row.
\end{theorem}
\begin{proof}
Start with the Cantor-prefix admission of \autoref{thm:fanfunctional-root-unblock-cantor-prefix}. It fixes the finite prefix windows and the structural rows available to the root-unblock read.

Next apply the finite fan-depth exposure of \autoref{thm:fanfunctional-root-unblock-fan-depth}. The depth ledger bounds the retained prefix window before witness coverage or modulus handoff is consumed.

Use \autoref{thm:fan-functional-root-uniform-modulus-handoff} and \autoref{thm:fanfunctional-root-modulus-window-coverage}. They route the compact-metric and uniform-continuity consumers through the same finite windows before the $\UniformModulusUp$ row is exported.

Finally attach the root schema ledger. Rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ keep the handoff on the displayed packet, so none of the excluded branch, host, induction, compactness, quotient, or Real-completeness objects can be read from the root-unblock surface.
\end{proof}

\begin{theorem}[FanFunctional root-unblock compact route]
\label{thm:fanfunctional-root-unblock-compact-route}
Every root-unblock read of an accepted $\FanFunctionalUp$ packet routes through $\CantorSpaceUp$, $\FanTheoremUp$, $\CompactMetricUp$, $\UniformContinuityUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before exporting a modulus. The route exposes the Cantor prefix, finite fan-depth ledger, compact finite-window consumer, tolerance request, witness-window coverage, componentwise transport, displayed replay, package provenance, and local naming rows; it exports no selected branch, bar-induction principle, open-cover compactness proof, quotient stream equality, or ambient $\RealUp$ completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.FanfunctionalUp.FanFunctionalRootUnblockCompactRoute}
\begin{proof}
Project the carrier rows of \autoref{def:fan-functional-carrier-surface} and \autoref{def:fanfunctional-finite-window-carrier}. The visible compact-route data are the Cantor-prefix windows, the finite fan-depth row, the compact metric consumer row, the uniform-continuity request, and the modulus handoff rows.

Invoke \autoref{thm:fanfunctional-root-unblock-cantor-prefix} for the Cantor-facing prefix and \autoref{thm:fanfunctional-root-unblock-fan-depth} for the finite fan-depth exposure. These theorems place $\CantorSpaceUp$ and $\FanTheoremUp$ before any compact or modulus consumer can read the packet.

Use \autoref{thm:fanfunctional-root-unblock-uniform-modulus} to carry the compact-metric and uniform-continuity rows through the witness-window handoff to $\UniformModulusUp$. The structural rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ keep the read on the same accepted packet.

Coordinate exhaustion gives the boundary. Since the accepted packet contains no selected branch, bar-induction row, open-cover compactness proof, quotient stream equality, or ambient $\RealUp$ completeness coordinate, the compact route exports only the displayed finite modulus surface.
\end{proof}

\begin{theorem}[FanFunctional root-unblock witness-window totality]
\label{thm:fanfunctional-root-unblock-witness-window-totality}
Every root-unblock read that exposes the $\UniformModulusUp$ row of an accepted $\FanFunctionalUp$ packet must also expose the witness-window row pairing retained $\CantorSpaceUp$ prefixes with pointwise tolerance evidence. This window is read through $\CantorSpaceUp$, $\FanTheoremUp$, $\CompactMetricUp$, $\UniformContinuityUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$; it exports no selected branch, host functional, bar-induction row, open-cover compactness proof, quotient stream equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:fanfunctional-root-unblock-cantor-prefix}. It supplies the retained Cantor-prefix surface as finite List, Bool, and StreamName data before a modulus consumer can inspect the packet.

Apply \autoref{thm:fanfunctional-root-unblock-fan-depth}. The finite fan-depth ledger bounds the retained prefix window and places the witness-window row after the Cantor prefix but before the uniform-modulus handoff.

Use \autoref{thm:fanfunctional-root-unblock-uniform-modulus}. It routes the compact metric and uniform-continuity consumers through the witness-window handoff to $\UniformModulusUp$, so exposing the modulus row entails exposing the paired prefix and pointwise tolerance evidence.

Finally invoke \autoref{thm:fanfunctional-root-unblock-compact-route}. The structural rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ keep the totality read on the displayed packet. Coordinate exhaustion excludes selected branches, host functionals, bar-induction rows, open-cover compactness, quotient streams, and ambient $\RealUp$ completeness.
\end{proof}
