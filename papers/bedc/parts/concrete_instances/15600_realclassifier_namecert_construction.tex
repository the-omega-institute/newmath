\chapter{A Concrete Naming Certificate for $\RealClassifierUp$}
\label{ch:concrete-instances-real-classifier-namecert}
\origin{human}

Following Bishop and Bridges, the $\RealClassifierUp$ packet records the public classifier surface of $\RealUp$ as a finite NameCert construction rather than as host equality of completed real numbers. It consumes the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the common finite-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledgers of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the equality-inspection packet of \autoref{ch:concrete-instances-realnameclassifier-namecert}, and the regular-name equivalence boundary of \autoref{ch:concrete-instances-regularcauchyequivalence-namecert}. The chapter gives downstream metric, algebraic, and order-facing certificates one classifier boundary for real names while keeping equality as displayed classifier data.

\begin{definition}[RealClassifier carrier]
\label{def:real-classifier-carrier}
A $\RealClassifierUp$ carrier is a finite $\BHist$ packet
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two terminal $\RealUp$ seal rows whose public classifier relation is being exposed.
\item $S_X$ and $S_Y$ are the regular rational source rows read back from the two seals.
\item $R_X$ and $R_Y$ are the corresponding $\RegSeqRatUp$ regular-rational readback rows.
\item $W$ is the common $\StreamNameUp$ finite window through which both readbacks are inspected.
\item $D$ is the shared $\DyadicRatCoreUp$ tolerance ledger recording the finite precision, radius, and comparison budget used by the classifier read.
\item $C$ is the $\RealNameClassifierUp$ row comparing the two regular names through $W$ and $D$.
\item $E$ is the $\RegularCauchyEquivalenceUp$ row that records the regular-Cauchy equivalence boundary visible before the terminal real seals are consumed.
\item $H$ stores componentwise $\hsame$ transports for the two seals, readbacks, common window, dyadic ledger, classifier rows, provenance, and local name.
\item $K$ stores displayed $\Cont$ replay routes from $X,Y$ through $S_X,S_Y,R_X,R_Y,W,D,C,E$ and back to the public seal boundary.
\item $P$ records $\Pkg$ provenance over $\RealClassifierUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealNameClassifierUp$, $\RegularCauchyEquivalenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealClassifierUp}$ row.
\end{enumerate}
The packet has no coordinate for quotient equality of streams, host real equality, selected limits, trichotomy, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[RealClassifier NameCert obligations]
\label{thm:real-classifier-namecert-obligations}
For every accepted $\RealClassifierUp$ carrier $Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N)$, the local $\NameCert_{\RealClassifierUp}$ surface consists exactly of the following five rows:
\begin{enumerate}
\item carrier admission for the two $\RealUp$ seals, regular rational readbacks, common window, dyadic tolerance ledger, classifier rows, transport, replay, provenance, and local name;
\item tolerance classifier stability, requiring every classifier read to pass through the common window $W$, the dyadic ledger $D$, and the $\RealNameClassifierUp$ row $C$;
\item $\RegSeqRatUp$ handoff, requiring every regular-rational consumer to factor through $S_X,S_Y,R_X,R_Y,W,D,C,E$ before reaching the public classifier boundary;
\item real seal non-escape, requiring $X$ and $Y$ to be consumed only as displayed terminal seals rather than as host real values or quotient classes;
\item ledger exhaustion, requiring $H,K,P,N$ to transport, replay, source, and name only the displayed finite packet.
\end{enumerate}
No local NameCert obligation is stored outside these five rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-classifier-carrier}. The accepted packet exposes exactly the two seal rows, the two regular rational source rows, the two $\RegSeqRatUp$ readbacks, the common window, the dyadic ledger, the two classifier rows, and the structural rows $H,K,P,N$.

Tolerance classifier stability is the route through $W,D,C$. Since $W$ is the only common inspection schedule and $D$ is the only tolerance ledger, a classifier read cannot inspect either side independently of those rows.

The regular-rational handoff uses $S_X,S_Y,R_X,R_Y$ before it reaches $C$ and $E$. The real seal row is then read only after that finite handoff has been displayed.

The remaining rows transport, replay, source, and locally name the same packet. Because the carrier omits quotient stream equality, host real equality, selected limits, trichotomy, countable choice, and ambient completeness, no NameCert obligation can export them.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierCarrier\_namecert\_obligations}

\begin{theorem}[RealClassifier RegSeqRat handoff]
\label{thm:real-classifier-regseqrat-handoff}
Every $\RegSeqRatUp$-facing classifier read of an accepted $\RealClassifierUp$ carrier factors through the displayed regular readbacks $S_X,S_Y,R_X,R_Y$, the common window $W$, the dyadic tolerance ledger $D$, the $\RealNameClassifierUp$ row $C$, and the $\RegularCauchyEquivalenceUp$ row $E$. Thus the public real-classifier read is a finite route through regular rational data, not a direct equality test on terminal real seals.
\end{theorem}
\begin{proof}
Start with an accepted carrier $Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N)$. The two seal rows $X$ and $Y$ expose regular rational readbacks only through $S_X,S_Y,R_X,R_Y$.

The common window $W$ supplies the shared finite requests, and the ledger $D$ records the dyadic tolerance data for those requests. The classifier row $C$ compares the two regular names through exactly those displayed rows.

The row $E$ records the regular-Cauchy equivalence boundary before any real-facing consumer reaches the terminal seals. Hence every $\RegSeqRatUp$-facing read factors through the displayed finite chain and cannot be replaced by a host equality test on $X$ and $Y$.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierRegSeqRatHandoff}

\begin{theorem}[RealClassifier regular-real tail-equivalence readback]
\label{thm:real-classifier-regular-real-tail-equivalence-readback}
Every accepted $\RealClassifierUp$ packet comparing two $\RealUp$ seals through
$$
S_X,S_Y,R_X,R_Y,W,D,C,E
$$
factors any same-real read through the $\RegularRealTailEquivalenceUp$ finite tail route before it reaches the public classifier boundary. The readback therefore consumes the shared $\RegSeqRatUp$ readbacks, common $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance ledger, $\RealNameClassifierUp$ comparison row, and visible $\RealUp$ seal rows as tail-equivalence data, not as host equality of completed reals.
\end{theorem}
\begin{proof}
Apply the RegSeqRat handoff theorem \autoref{thm:real-classifier-regseqrat-handoff}. It forces every same-real read to expose $S_X,S_Y,R_X,R_Y,W,D,C,E$ before the public classifier boundary is reached.

Use the finite same-real tail route of \autoref{ch:concrete-instances-regular-real-tail-equivalence-namecert}. Its carrier reads two regular rational sources through one finite window and one dyadic tolerance ledger before the Real-facing seal is consumed.

Project the shared rows of the $\RealClassifierUp$ carrier. The rows $R_X,R_Y$ supply the two regular rational readbacks, $W$ supplies the common inspected window, $D$ supplies the tolerance ledger, $C$ records the name comparison, and $E$ records the visible regular-Cauchy boundary before $X,Y$ are read as terminal seals.

The structural rows $H,K,P,N$ only transport, replay, source, and name that same finite packet. Hence a same-real read cannot bypass the regular-real tail-equivalence route by appealing to quotient streams, host real equality, selected limits, countable choice, trichotomy, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierUp.RealClassifierRegularRealTailEquivalenceReadback}

\begin{theorem}[RealClassifier regular-sequence induction]
\label{thm:realclassifier-regular-sequence-induction}
Let
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N)
$$
be an accepted $\RealClassifierUp$ carrier. Every classifier read whose $\StreamNameUp$ requests lie in the common finite window $W$ is consumed by induction over the displayed request rows of $W$: the base read uses the first dyadic tolerance row through $D$, compares the two regular names through $C$, and records the visible regular-Cauchy boundary through $E$; the step appends the next inspected request in $W$ and replays the same finite packet through $H,K,P,N$. The induction exports only the rows of \autoref{def:real-classifier-carrier} and the obligations of \autoref{thm:real-classifier-namecert-obligations}; it does not export quotient stream equality, host real equality, trichotomy, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Apply the RegSeqRat handoff theorem \autoref{thm:real-classifier-regseqrat-handoff}. It forces every regular-rational classifier read to pass through $S_X,S_Y,R_X,R_Y,W,D,C,E$ before the public classifier boundary is reached.

For the base request, read the first displayed row of the finite window $W$. The dyadic tolerance information is the row $D$, the comparison of the two regular names is the row $C$, and the regular-Cauchy boundary visible before the terminal seals is the row $E$.

For the induction step, append one further inspected request from $W$. The NameCert obligations of \autoref{thm:real-classifier-namecert-obligations} keep the appended request inside the same carrier, while $H$ transports the displayed rows, $K$ replays the continuation route, $P$ records provenance, and $N$ locally names the same packet.

The carrier definition \autoref{def:real-classifier-carrier} has no coordinate for quotient stream equality, host real equality, trichotomy, countable choice, or ambient completeness. Thus the induction is a finite read over the common StreamName window and cannot be used as any of those ambient principles.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierRegularSequenceInduction}

\begin{theorem}[RealClassifier real seal non-escape]
\label{thm:real-classifier-real-seal-nonescape}
Every $\RealUp$ consumer of an accepted $\RealClassifierUp$ carrier sees only the finite rows $X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N$. The consumer obtains no quotient equality of streams, host real equality, selected limit, trichotomy witness, countable-choice schedule, or ambient completeness principle from the classifier packet.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:real-classifier-carrier}. The only real-facing rows are the displayed seals $X$ and $Y$, and their only exposed classifier route is the finite chain through the regular rational readbacks, common window, dyadic ledger, and classifier rows.

The structural rows $H,K,P,N$ do not add new analytic data. They identify, replay, source, and name the same finite packet.

Since no coordinate carries quotient stream equality, host real equality, selected limits, trichotomy, countable choice, or ambient completeness, a consumer of the public $\RealClassifierUp$ boundary cannot extract any of those principles.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierRealSealNonescape}

\begin{theorem}[RealClassifier ledger exhaustion]
\label{thm:real-classifier-ledger-exhaustion}
For every accepted $\RealClassifierUp$ carrier
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N),
$$
all public classifier reads are exhausted by the displayed structural rows $H,K,P,N$ together with the finite analytic rows $X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E$. In particular, no public read can export quotient stream equality, host real equality, selected limits, trichotomy, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:real-classifier-carrier}. The analytic access rows are exactly the two real seals, their regular rational sources and readbacks, the common stream window, the dyadic tolerance ledger, the classifier row, and the regular-Cauchy equivalence boundary.

Apply the NameCert obligations theorem \autoref{thm:real-classifier-namecert-obligations}. Its fifth row states that $H,K,P,N$ transport, replay, source, and locally name only the displayed finite packet, so there is no hidden classifier read outside the carrier.

Finally combine the RegSeqRat handoff of \autoref{thm:real-classifier-regseqrat-handoff} with the real seal non-escape theorem \autoref{thm:real-classifier-real-seal-nonescape}. The handoff forces every regular-rational read through $S_X,S_Y,R_X,R_Y,W,D,C,E$, and seal non-escape rules out the listed quotient, host-equality, choice, order, and completeness endpoints.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierLedgerExhaustion}

\begin{theorem}[RealClassifier tolerance-window determinacy]
\label{thm:real-classifier-tolerance-window-determinacy}
Let
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N)
$$
and
$$
Q'=(X',Y',S'_X,S'_Y,R'_X,R'_Y,W',D,C',E',H',K',P',N')
$$
be accepted $\RealClassifierUp$ carriers. Suppose the two packets share the same $\DyadicRatCoreUp$ tolerance ledger $D$, expose the same $\StreamNameUp$ window through $W$ and $W'$, use the same $\RegSeqRatUp$ handoff surface through $S_X,S_Y,R_X,R_Y$ and $S'_X,S'_Y,R'_X,R'_Y$, reach the same terminal $\RealUp$ seal boundary through $X,Y$ and $X',Y'$, and transport the displayed rows through the same $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ routes carried by $H,K,P,N$ and $H',K',P',N'$. Then $Q$ and $Q'$ expose the same public classifier decision surface: every accepted classifier read of either packet is the finite route through the common tolerance ledger, common window, regular rational readback, real seal boundary, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Apply the RegSeqRat handoff theorem \autoref{thm:real-classifier-regseqrat-handoff} to both carriers. The hypotheses identify the tolerance ledger, finite window, and regular rational readback surfaces, so each classifier read reaches the public boundary through the same displayed $D,W,S_X,S_Y,R_X,R_Y$ route.

Next apply the real seal non-escape theorem \autoref{thm:real-classifier-real-seal-nonescape}. It rules out quotient stream equality, host real equality, selected limits, trichotomy, countable choice, and ambient completeness as alternative endpoints, leaving only the displayed terminal seal boundary named in the hypotheses.

Finally apply ledger exhaustion from \autoref{thm:real-classifier-ledger-exhaustion}. The structural rows $H,K,P,N$ and $H',K',P',N'$ can only transport, replay, source, and locally name the displayed packet, so two accepted packets with the same tolerance, window, handoff, seal, transport, replay, provenance, and local naming routes expose the same classifier decision surface.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierToleranceWindowDeterminacy}

\begin{theorem}[RealClassifier window scope]
\label{thm:real-classifier-window-scope}
Every accepted $\RealClassifierUp$ public read factors through the common $\StreamNameUp$ window $W$ and the $\DyadicRatCoreUp$ ledger $D$ before a $\RealNameClassifierUp$ row or a $\RegularCauchyEquivalenceUp$ row can be consumed. In carrier notation, for
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N),
$$
the public classifier boundary is the finite route through $S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N$ over the two terminal $\RealUp$ seals $X,Y$, not a direct equality test on completed real names.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-classifier-carrier}. The only common inspection coordinate is $W$, and the only tolerance and comparison-budget coordinate is $D$.

Apply the RegSeqRat handoff theorem \autoref{thm:real-classifier-regseqrat-handoff}. It forces every regular-rational classifier read through $S_X,S_Y,R_X,R_Y,W,D,C,E$ before the public classifier boundary is reached.

Use real seal non-escape from \autoref{thm:real-classifier-real-seal-nonescape}. Since the terminal seals $X,Y$ do not expose host real equality, quotient stream equality, selected limits, trichotomy, countable choice, or ambient completeness, the read cannot bypass the common window and ledger by inspecting the seals directly.

Finally apply ledger exhaustion from \autoref{thm:real-classifier-ledger-exhaustion}. The structural rows $H,K,P,N$ only transport, replay, source, and name the displayed finite packet, so every accepted public read consumes $W$ and $D$ before it reaches $C$ or $E$.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierWindowScope}

\begin{theorem}[RealClassifier common-window quotient refusal]
\label{thm:real-classifier-common-window-quotient-refusal}
For accepted $\RealClassifierUp$ carriers, a public equality or classifier read is admissible only when both terminal $\RealUp$ seals are compared through the same $\StreamNameUp$ window $W$ and the same $\DyadicRatCoreUp$ ledger $D$. Any attempted read that identifies the terminal $\RealUp$ seals directly, without displaying both $W$ and $D$, leaves the carrier surface and cannot be cited as $\RealClassifierUp$ evidence.
\end{theorem}
\begin{proof}
Project an accepted carrier
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N).
$$
The carrier exposes the two terminal seals only together with their regular rational source rows, readbacks, common window, tolerance ledger, classifier row, equivalence boundary, and structural rows.

Apply the RegSeqRat handoff of \autoref{thm:real-classifier-regseqrat-handoff}. It forces every public classifier read through $S_X,S_Y,R_X,R_Y,W,D,C,E$ before the terminal boundary is consumed.

Apply real seal non-escape from \autoref{thm:real-classifier-real-seal-nonescape}. It rules out a direct host equality or quotient equality read on $X,Y$ as an endpoint supplied by the carrier.

Use ledger exhaustion from \autoref{thm:real-classifier-ledger-exhaustion} and window scope from \autoref{thm:real-classifier-window-scope}. These keep the structural rows $H,K,P,N$ tied to the same finite packet and require the common window $W$ and ledger $D$ before any $\RealNameClassifierUp$ or $\RegularCauchyEquivalenceUp$ row is consumed. A read that omits either row is therefore outside the accepted $\RealClassifierUp$ evidence surface.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierCommonWindowQuotientRefusal}

\begin{theorem}[RealClassifier source scope]
\label{thm:real-classifier-source-scope}
Every accepted $\RealClassifierUp$ public read factors through exactly the source scopes named by the carrier: the terminal $\RealUp$ seals $X,Y$, the regular rational source and readback rows $S_X,S_Y,R_X,R_Y$, the common $\StreamNameUp$ window $W$, the $\DyadicRatCoreUp$ ledger $D$, the $\RealNameClassifierUp$ row $C$, the $\RegularCauchyEquivalenceUp$ row $E$, and the structural rows $H,K,P,N$. No quotient stream equality, host real equality, trichotomy, countable choice, selected limit, or ambient completeness principle can bypass those scopes.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:real-classifier-carrier}. Its source rows are precisely the two terminal seals, the two regular rational source rows, the two readback rows, the common window, the dyadic ledger, the classifier row, the regular-Cauchy equivalence row, and the structural rows $H,K,P,N$.

Apply the NameCert obligations of \autoref{thm:real-classifier-namecert-obligations}. Carrier admission, tolerance classifier stability, the RegSeqRat handoff, real seal non-escape, and ledger exhaustion account for every accepted public read.

Use the RegSeqRat handoff of \autoref{thm:real-classifier-regseqrat-handoff} and the real seal non-escape theorem \autoref{thm:real-classifier-real-seal-nonescape}. These force the read through $S_X,S_Y,R_X,R_Y,W,D,C,E$ over the displayed seals $X,Y$ and rule out host equality or quotient-stream shortcuts.

Finally combine ledger exhaustion from \autoref{thm:real-classifier-ledger-exhaustion}, tolerance-window determinacy from \autoref{thm:real-classifier-tolerance-window-determinacy}, and window scope from \autoref{thm:real-classifier-window-scope}. The structural rows $H,K,P,N$ only transport, replay, source, and name the same finite packet, so no listed non-source principle remains as a bypass.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierSourceScope}

\begin{theorem}[RealClassifier public exactness surface]
\label{thm:realclassifier-public-exactness-surface}
Every accepted $\RealClassifierUp$ public classifier read is exactly the finite route through the two $\RealUp$ seals, the $\RegSeqRatUp$ readbacks, the shared $\StreamNameUp$ window, the $\DyadicRatCoreUp$ tolerance data, the $\RealNameClassifierUp$ row, the $\RegularCauchyEquivalenceUp$ row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RealClassifierUp}$ row. No accepted public read exports host real equality, quotient stream equality, a selected limit, trichotomy, or a completeness coordinate.
\end{theorem}
\begin{proof}
Start with the carrier and NameCert obligations from \autoref{def:real-classifier-carrier} and \autoref{thm:real-classifier-namecert-obligations}. They list the two terminal seals, regular rational source and readback rows, common StreamName window, dyadic tolerance ledger, classifier row, equivalence boundary, and structural rows as the complete local surface.

Use the RegSeqRat handoff of \autoref{thm:real-classifier-regseqrat-handoff} and the real seal non-escape theorem \autoref{thm:real-classifier-real-seal-nonescape}. These force every regular-rational read through $S_X,S_Y,R_X,R_Y,W,D,C,E$ over the displayed seals $X,Y$ and rule out host real equality, quotient stream equality, selected limits, trichotomy, countable choice, and ambient completeness as endpoints.

Finally apply ledger exhaustion from \autoref{thm:real-classifier-ledger-exhaustion}, tolerance-window determinacy from \autoref{thm:real-classifier-tolerance-window-determinacy}, and window scope from \autoref{thm:real-classifier-window-scope}. The shared window and tolerance ledger determine the public classifier route, while $H,K,P,N$ transport, replay, source, and locally name that exact finite packet.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierPublicExactnessSurface}

\begin{theorem}[RealClassifier completion-selector-seal readback]
\label{thm:realclassifier-completion-selector-seal-readback}
Every accepted $\RealClassifierUp$ public read that is consumed by a $\RealCompletionSelectorSealUp$ packet must pass through the same $\RealUp$ seals, $\RegSeqRatUp$ readbacks, $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance ledger, $\RealNameClassifierUp$ row, and $\RegularCauchyEquivalenceUp$ row before any selector-seal budget, window, or readback row is exposed. The handoff exports no selected limit, quotient stream equality, trichotomy, countable choice, or ambient-completeness coordinate.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:real-classifier-carrier} and the NameCert obligations of \autoref{thm:real-classifier-namecert-obligations}. These list the two terminal $\RealUp$ seals, regular rational rows, common $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger, classifier row, equivalence row, and structural rows as the full accepted source.

Apply the RegSeqRat handoff of \autoref{thm:real-classifier-regseqrat-handoff} and the real seal non-escape theorem \autoref{thm:real-classifier-real-seal-nonescape}. They force the read through $S_X,S_Y,R_X,R_Y,W,D,C,E$ before the terminal Real boundary can be used, and they rule out host equality, quotient-stream equality, selected limits, trichotomy, countable choice, and ambient completeness as endpoints.

Use ledger exhaustion from \autoref{thm:real-classifier-ledger-exhaustion} and the public exactness surface of \autoref{thm:realclassifier-public-exactness-surface}. The shared window and dyadic ledger are already exhausted before the public classifier boundary is reached, so a selector-seal consumer receives the same finite classifier route rather than a second tolerance or equality channel.

Finally attach the sibling $\RealCompletionSelectorSealUp$ route of \autoref{ch:concrete-instances-realcompletionselectorseal-namecert}. Its budget, window, and readback rows can only consume the displayed classifier read after the RealClassifier route has exposed the seals, readbacks, window, ledger, classifier row, equivalence row, and structural transport, replay, provenance, and naming rows. No coordinate remains from which to export a selected limit, quotient stream equality, trichotomy, countable choice, or ambient-completeness claim.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierCompletionSelectorSealReadback}

\begin{theorem}[RealClassifier completion-density consumer]
\label{thm:realclassifier-completion-density-consumer}
Every completion-density consumer of an accepted $\RealClassifierUp$ read must expose the terminal $\RealUp$ seal rows, the $\RegSeqRatUp$ readbacks, the shared $\StreamNameUp$ finite window, the $\DyadicRatCoreUp$ tolerance ledger, the rational-density route of \autoref{ch:concrete-instances-rational-dense-embedding-namecert} or the regular Cauchy dense-embedding route of \autoref{ch:concrete-instances-regular-cauchy-dense-embedding-namecert}, and the classifier transport rows before the read is used as a countable dense surface. In carrier notation, for
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N),
$$
the dense consumer may reuse only the displayed route through $X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N$ together with the sibling density rows. It may not substitute selected representatives, quotient stream equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply the public exactness surface of \autoref{thm:realclassifier-public-exactness-surface}. It binds every accepted classifier read to the two terminal Real seals, the regular rational readbacks, the common StreamName window, the DyadicRatCore ledger, the comparison row, the regular-Cauchy equivalence boundary, and the structural transport, replay, provenance, and naming rows.

Next use the regular-real tail-equivalence readback of \autoref{thm:real-classifier-regular-real-tail-equivalence-readback}. It keeps a same-real read inside the shared regular-rational, finite-window, dyadic-ledger, and comparison route before the public classifier boundary is reached.

Now pass to the density sibling. A rational-density consumer reads \autoref{ch:concrete-instances-rational-dense-embedding-namecert}; a completion-facing regular-Cauchy consumer reads \autoref{ch:concrete-instances-regular-cauchy-dense-embedding-namecert}. In either case, the dense surface receives the already displayed $\RegSeqRatUp$ readback, $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance ledger, and $\RealUp$ seal route from the classifier packet.

Finally read $H,K,P,N$ only as classifier transport, replay, provenance, and local naming. Since the carrier has no coordinate for selected representatives, quotient stream equality, countable choice, or ambient Real completeness, a completion-density consumer cannot obtain any of those endpoints from the accepted $\RealClassifierUp$ read.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierCompletionDensityConsumer}

\begin{theorem}[RealClassifier audit route]
\label{thm:real-classifier-audit-route}
For every accepted $\RealClassifierUp$ carrier
$$
Q=(X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N),
$$
the audit-facing classifier route is exactly the ordered read through the two $\RealUp$ seals $X,Y$, the $\RegSeqRatUp$ readbacks $R_X,R_Y$, the shared $\StreamNameUp$ window $W$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the $\RealNameClassifierUp$ row $C$, the $\RegularCauchyEquivalenceUp$ row $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $K$, $\Pkg$ provenance $P$, and the local $\NameCert_{\RealClassifierUp}$ row $N$. No audit read may replace that route by quotient stream equality, host real equality, selected limits, trichotomy, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Use the RegSeqRat handoff theorem \autoref{thm:real-classifier-regseqrat-handoff}. It forces every classifier read to expose the regular rational sources and readbacks before the public classifier boundary is reached.

Apply window scope and source scope, \autoref{thm:real-classifier-window-scope} and \autoref{thm:real-classifier-source-scope}. These identify the shared StreamName window, dyadic tolerance ledger, classifier row, equivalence row, terminal Real seals, and structural rows as the complete source list for the read.

Finally use the public exactness surface of \autoref{thm:realclassifier-public-exactness-surface}. Since that theorem already binds every public classifier read to the exact finite route through $X,Y,R_X,R_Y,W,D,C,E,H,K,P,N$, the audit route is the same ordered source read and has no ambient equality, choice, or completeness endpoint.
\end{proof}
\leanchecked{BEDC.Derived.RealClassifierAuditRoute}

\begin{theorem}[RealClassifier audit-density coherence]
\label{thm:real-classifier-audit-density-coherence}
Every audit-facing classifier read of an accepted $\RealClassifierUp$ carrier that is later consumed by the rational-density route of \autoref{ch:concrete-instances-rational-dense-embedding-namecert} or the regular-Cauchy dense-embedding route of \autoref{ch:concrete-instances-regular-cauchy-dense-embedding-namecert} must use the same ordered route through the $\RealUp$ seals, $\RegSeqRatUp$ readbacks, shared $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger, $\RealNameClassifierUp$ row, $\RegularCauchyEquivalenceUp$ row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\RealClassifierUp}$ row. No dense consumer may replace that route by selected representatives, quotient stream equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply the audit route theorem \autoref{thm:real-classifier-audit-route}. It fixes the audit-facing read as the ordered source route through the two terminal seals, regular readbacks, common window, dyadic ledger, classifier row, regular-Cauchy boundary, transport, replay, provenance, and local naming rows.

Apply the completion-density consumer theorem \autoref{thm:realclassifier-completion-density-consumer}. It says that rational-density and regular-Cauchy dense-embedding consumers receive the already displayed classifier route together with their sibling density rows, rather than a second representative or a detached completion endpoint.

Finally use the public exactness surface of \autoref{thm:realclassifier-public-exactness-surface}. Since every accepted public read is exhausted by the same finite rows, an audit-facing read and its dense consumer have one shared route through $X,Y,S_X,S_Y,R_X,R_Y,W,D,C,E,H,K,P,N$. The excluded selected representatives, quotient stream equality, countable choice, and ambient completeness coordinates are absent from both surfaces.
\end{proof}

\closureat{RealClassifierUp}{scopedStr}

\begin{closurestatus}{\RealClassifierUp}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:real-classifier-carrier}, \autoref{thm:real-classifier-namecert-obligations}, \autoref{thm:real-classifier-regseqrat-handoff}, \autoref{thm:real-classifier-real-seal-nonescape}, \autoref{thm:real-classifier-ledger-exhaustion}, \autoref{thm:real-classifier-tolerance-window-determinacy}, \autoref{thm:real-classifier-window-scope}, \autoref{thm:real-classifier-source-scope}, and \autoref{thm:realclassifier-public-exactness-surface} bind every public read to the exact RealUp seals, RegSeqRat readbacks, StreamName window, DyadicRatCore ledger, RealNameClassifier row, RegularCauchyEquivalence row, and structural hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\scaffoldCheckedV}
  \leantarget{BEDC.Derived.RealClassifierPublicExactnessSurface}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, host real equality, selected limits, trichotomy, countable choice, ambient completeness, or bridge checking.}
  \upgradepath{The remaining formal work is audit-clean and bridge-checked coverage for the same exact classifier route.}
  \constructivestory{A $\RealClassifierUp$ packet is generated from two RealUp seals, regular rational readbacks, one common StreamName window, DyadicRatCore tolerance data, a RealNameClassifier row, a RegularCauchyEquivalence row, componentwise transport, continuation replay, package provenance, and local naming.}
\end{closurestatus}
