\chapter{A Concrete Naming Certificate for $\FloorFunctionUp$}
\label{ch:concrete-instances-floor-function-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\FloorFunctionUp$ packet records the ordinary floor-function read as a finite certificate route rather than as a primitive operation on host real numbers. It consumes the real-name source of \autoref{ch:concrete-instances-real-namecert}, the integer bracket supplied by \autoref{ch:concrete-instances-archimedean-integer-part-namecert}, the Real-facing integer-part handoff of \autoref{ch:concrete-instances-real-integer-part-namecert}, the residual row of \autoref{ch:concrete-instances-fractional-part-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet names the displayed lower-integer value attached to a real name; it is not host floor, trichotomy, quotient real equality, selected representatives, countable choice, or ambient Real completeness.

\begin{definition}[FloorFunction carrier]
\label{def:floor-function-carrier}
A $\FloorFunctionUp$ carrier is a finite $\BHist$ packet
$$
K=(R,B,Z,F,D,S,G,I,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the source $\RealUp$ row whose floor read is being requested.
\item $B$ is the $\ArchimedeanIntegerPartUp$ bracket row attached to the same source real.
\item $Z$ is the $\IntUp$ candidate exposed by the bracket as the lower integer endpoint.
\item $F$ is the $\FractionalPartUp$ residual row tied to the same source real and integer candidate.
\item $D$ is the $\DyadicRatCoreUp$ scale row recording the finite tolerances used to inspect the source, bracket, and residual rows.
\item $S$ is the $\StreamNameUp$ window row opened for the finite real-name and residual observations.
\item $G$ is the $\RegSeqRatUp$ readback row that interprets the displayed windows before the floor value is exported.
\item $I$ is the $\RealIntegerPartUp$ handoff row that packages the same source, bracket, integer candidate, residual, dyadic scale, window, and readback route for Real-facing consumers.
\item $H$ records componentwise $\hsame$ transport for the source real, bracket, integer candidate, residual, dyadic scale, window, readback, RealIntegerPart handoff, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from every floor request through $R,B,Z,F,D,S,G,I$ without inserting another integer search.
\item $P$ is the $\Pkg$ provenance over $\FloorFunctionUp$, $\RealUp$, $\ArchimedeanIntegerPartUp$, $\IntUp$, $\FractionalPartUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealIntegerPartUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FloorFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for host floor, trichotomy over arbitrary real names, quotient real equality, selected representatives, countable choice, hidden Archimedean search, unbounded stream reads, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[FloorFunction NameCert obligations]
\label{thm:floor-function-namecert-obligations}
For every accepted $\FloorFunctionUp$ carrier $K=(R,B,Z,F,D,S,G,I,H,C,P,N)$, the local $\NameCert_{\FloorFunctionUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier exposure for the source $\RealUp$ row, $\ArchimedeanIntegerPartUp$ bracket row, $\IntUp$ candidate row, $\FractionalPartUp$ residual row, $\DyadicRatCoreUp$ scale row, $\StreamNameUp$ window row, $\RegSeqRatUp$ readback row, $\RealIntegerPartUp$ handoff row, and structural rows $H,C,P,N$;
\item bracket exactness, requiring the integer candidate $Z$ to be read only as the lower endpoint of the displayed $\ArchimedeanIntegerPartUp$ bracket $B$ for the same source row $R$;
\item residual compatibility, requiring $F$ to bind the same $R,B,Z$ route before any fractional residual observation is exposed;
\item handoff compatibility, requiring the $\RealIntegerPartUp$ row $I$ to consume exactly the displayed bracket, residual, dyadic, window, and readback rows;
\item transport and replay stability, requiring $H$ and $C$ to preserve the rows componentwise and to send each floor request back through $R,B,Z,F,D,S,G,I$;
\item ledger refusal, excluding host floor, trichotomy, quotient real equality, selected representatives, countable choice, hidden Archimedean search, unbounded stream reads, and ambient Real completeness from the certificate surface.
\end{enumerate}
No FloorFunction obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted packet $K=(R,B,Z,F,D,S,G,I,H,C,P,N)$. The projection exposes exactly the real source, bracket, integer candidate, residual, dyadic scale, stream window, regular readback, RealIntegerPart handoff, transport, replay, provenance, and naming rows.

Bracket exactness is read from \autoref{ch:concrete-instances-archimedean-integer-part-namecert}. The row $B$ binds the source real to a finite lower endpoint and successor comparison, and $Z$ is available only as the displayed integer candidate of that bracket.

Residual compatibility is read from \autoref{ch:concrete-instances-fractional-part-namecert}. The residual row $F$ attaches to the same source real and integer candidate, so it cannot provide an independent floor selector.

The Real-facing handoff is read through \autoref{ch:concrete-instances-real-integer-part-namecert}. The row $I$ packages the same bracket route together with the dyadic scale, finite window, and regular readback rows.

Finally $H,C,P,N$ transport, replay, source, and name the displayed packet. Since no excluded object appears as a carrier coordinate or classifier clause, the listed rows exhaust the local $\NameCert_{\FloorFunctionUp}$ obligation surface.
\end{proof}

\begin{theorem}[FloorFunction RealIntegerPart handoff]
\label{thm:floor-function-real-integer-part-handoff}
Every consumer that reads a floor value from an accepted $\FloorFunctionUp$ carrier factors through the finite route
$$
\RealUp \longmapsto \ArchimedeanIntegerPartUp \longmapsto \IntUp \longmapsto \FractionalPartUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealIntegerPartUp .
$$
In particular, every such read passes through \autoref{ch:concrete-instances-archimedean-integer-part-namecert} and \autoref{ch:concrete-instances-real-integer-part-namecert}. It receives no host floor function, trichotomy service, quotient real equality, selected representative, countable-choice row, or ambient Real completeness theorem.
\end{theorem}
\begin{proof}
Let a downstream consumer request the floor value. The replay row $C$ sends the request first to the source $\RealUp$ row $R$, so the route starts from a displayed real name rather than from a host real.

The bracket row $B$ then supplies the $\ArchimedeanIntegerPartUp$ certificate named by \autoref{ch:concrete-instances-archimedean-integer-part-namecert}. The integer candidate $Z$ is the lower endpoint exported by that finite bracket.

The residual and readback rows keep the endpoint attached to the same real-name route. The row $F$ records the $\FractionalPartUp$ residual, while $D,S,G$ expose only finite dyadic tolerances, finite windows, and regular rational readbacks.

The handoff row $I$ is exactly the $\RealIntegerPartUp$ consumer surface of \autoref{ch:concrete-instances-real-integer-part-namecert}. Transport by $H$ preserves the same rows componentwise, and $P,N$ record no additional operation. Therefore every exported floor read factors through the stated route.
\end{proof}

\falsifiablePrediction{Every $\FloorFunctionUp$ read factors through one $\RealUp$ source row, one $\ArchimedeanIntegerPartUp$ bracket row, one $\IntUp$ candidate row, one $\FractionalPartUp$ residual row, one $\DyadicRatCoreUp$ scale row, one $\StreamNameUp$ window row, one $\RegSeqRatUp$ readback row, one $\RealIntegerPartUp$ handoff row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{real, archimedean-integer-part, int, fractional-part, dyadicratcore, streamname, regseqrat, real-integer-part: no listed sibling is bijective to $\FloorFunctionUp$ because this packet jointly binds the floor-facing name, bracket endpoint, residual, finite tolerance scale, window read, regular readback, RealIntegerPart handoff, transport, replay, provenance, and local naming for the same real source.}

\closureat{FloorFunctionUp}{seedStr}

\begin{closurestatus}{\FloorFunctionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:floor-function-carrier}, \autoref{thm:floor-function-namecert-obligations}, and \autoref{thm:floor-function-real-integer-part-handoff} seed the finite floor-function certificate route from a displayed Real source through ArchimedeanIntegerPart bracket, Int candidate, FractionalPart residual, DyadicRatCore scale, StreamName window, RegSeqRat readback, RealIntegerPart handoff, and structural rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host floor, trichotomy, quotient real equality, selected representatives, countable choice, hidden Archimedean search, unbounded stream reads, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier exposure, bracket exactness, residual compatibility, RealIntegerPart handoff compatibility, transport replay stability, and ledger refusal for BEDC.Derived.FloorFunctionUp.}
  \constructivestory{A $\FloorFunctionUp$ packet is generated from a $\RealUp$ source row, an $\ArchimedeanIntegerPartUp$ bracket row, an $\IntUp$ candidate row, a $\FractionalPartUp$ residual row, a $\DyadicRatCoreUp$ scale row, a $\StreamNameUp$ window row, a $\RegSeqRatUp$ readback row, a $\RealIntegerPartUp$ handoff row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
