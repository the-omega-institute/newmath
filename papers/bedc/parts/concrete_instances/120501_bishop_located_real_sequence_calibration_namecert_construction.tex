\chapter{A Concrete Naming Certificate for $\BishopLocatedRealSequenceCalibrationUp$}
\label{ch:concrete-instances-bishop-located-real-sequence-calibration-namecert}
\origin{human}

Following Bishop and Bridges on located real sequences and regular Cauchy real names, the $\BishopLocatedRealSequenceCalibrationUp$ packet records the finite calibration row that lets a located real-sequence consumer pass from inspected stream windows to regular rational readback and then to a terminal real-name seal. It sits beside the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, the Bishop located sequence packet of \autoref{ch:concrete-instances-bishop-located-sequence-namecert}, and the located real sequence source of \autoref{ch:concrete-instances-located-real-sequence-namecert}. The calibration is finite L10 data; it is not quotient equality of streams, selected convergence, trichotomy, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop located real sequence calibration carrier]
\label{def:bishop-located-real-sequence-calibration-carrier}
A $\BishopLocatedRealSequenceCalibrationUp$ carrier is a finite $\BHist$ packet
$$
K=(S,B,L,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ row of inspected finite sequence windows.
\item $B$ is the $\BishopLocatedSequenceUp$ row naming the Bishop-style located sequence surface that supplies the locatedness request.
\item $L$ is the $\LocatedRealSequenceUp$ row recording the located real-name component windows used by the same request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the inspected windows and locatedness data.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from $S,B,L,D$.
\item $E$ is the terminal $\RealUp$ seal row, exposed only after the regular rational readback has been displayed.
\item $H$ records componentwise $\hsame$ transport for $S,B,L,D,R,E$ and for the structural rows.
\item $C$ records displayed $\Cont$ replay from a calibration request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedRealSequenceCalibrationUp$, $\StreamNameUp$, $\BishopLocatedSequenceUp$, $\LocatedRealSequenceUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedRealSequenceCalibrationUp}$ row.
\end{enumerate}
The classifier is rowwise over $S,B,L,D,R,E,H,C,P,N$. It compares only displayed stream windows, located-sequence rows, located-real component windows, dyadic tolerances, regular rational readbacks, real seals, transport, replay, provenance, and local naming.
\end{definition}

\begin{theorem}[Bishop located real sequence calibration obligations]
\label{thm:bishop-located-real-sequence-calibration-obligations}
For every accepted $\BishopLocatedRealSequenceCalibrationUp$ carrier $K=(S,B,L,D,R,E,H,C,P,N)$, the local $\NameCert_{\BishopLocatedRealSequenceCalibrationUp}$ surface consists of five finite obligation rows:
\begin{enumerate}
\item stream-window admission, requiring the consumer to begin at the displayed $\StreamNameUp$ row $S$;
\item located-sequence alignment, requiring the $\BishopLocatedSequenceUp$ row $B$ and the $\LocatedRealSequenceUp$ row $L$ to describe the same inspected request;
\item tolerance calibration, requiring the $\DyadicRatCoreUp$ row $D$ to bind the displayed windows and locatedness rows before any rational readback is consumed;
\item readback and seal routing, requiring the $\RegSeqRatUp$ row $R$ to precede the terminal $\RealUp$ row $E$;
\item structural replay, requiring $H,C,P,N$ to preserve the same finite route under componentwise transport, continuation replay, provenance, and local naming.
\end{enumerate}
No obligation row supplies quotient stream equality, selected convergence, trichotomy, countable choice, host real equality, or ambient real completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-located-real-sequence-calibration-carrier}. The analytic rows are $S,B,L,D,R,E$, and the structural rows are $H,C,P,N$.

The alignment obligations bind the two sequence views. The $\BishopLocatedSequenceUp$ row records the Bishop locatedness route, while the $\LocatedRealSequenceUp$ row records the located real-name component windows for the same inspected request.

The tolerance and readback rows are downstream. The dyadic ledger $D$ calibrates the displayed windows before the regular rational row $R$ is read, and the terminal seal $E$ is reached only after that readback.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since none of the excluded principles appears as a carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop located real sequence calibration L10 route]
\label{thm:bishop-located-real-sequence-calibration-l10-route}
Every Real-and-Completion consumer of an accepted $\BishopLocatedRealSequenceCalibrationUp$ carrier factors through the finite route
$$
\StreamNameUp\longmapsto\BishopLocatedSequenceUp\longmapsto\LocatedRealSequenceUp\longmapsto\DyadicRatCoreUp\longmapsto\RegSeqRatUp\longmapsto\RealUp
$$
together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The route is a calibration surface between finite windows, located sequence data, regular rational readback, and the real seal; it is not a completeness theorem or a quotient-real equality.
\end{theorem}
\begin{proof}
Start with the obligation rows of \autoref{thm:bishop-located-real-sequence-calibration-obligations}. They force each consumer to expose the stream window row, the two located sequence rows, the dyadic tolerance ledger, the regular rational readback, and the terminal real seal in that order.

Use the rowwise classifier of \autoref{def:bishop-located-real-sequence-calibration-carrier}. Componentwise transport preserves the displayed route, so a transported consumer cannot replace it by quotient streams or by a selected convergence witness.

The structural rows then close the finite replay. The route is sourced by $P$, replayed by $C$, transported by $H$, and named by $N$, so the consumer reaches only the displayed $\RealUp$ seal.
\end{proof}

\leanstmt{BEDC.Derived.BishopLocatedRealSequenceCalibrationUp}

\falsifiablePrediction{Every accepted $\BishopLocatedRealSequenceCalibrationUp$ consumer displays StreamName windows, Bishop located sequence data, LocatedRealSequence component windows, DyadicRatCore tolerances, RegSeqRat readback, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows before it reads a Real-facing result.}

\independenceWitness{streamname, regseqrat, real, bishoplocatedsequence, locatedrealsequence: $\BishopLocatedRealSequenceCalibrationUp$ is not bijective to any listed sibling because it packages the calibration between located sequence data, located real-sequence rows, dyadic tolerance, rational readback, and the terminal real seal as one finite NameCert surface.}

\closureat{BishopLocatedRealSequenceCalibrationUp}{seedStr}

\begin{closurestatus}{\BishopLocatedRealSequenceCalibrationUp}
  \constructivestory{A $\BishopLocatedRealSequenceCalibrationUp$ packet is generated from finite StreamName windows, BishopLocatedSequence data, LocatedRealSequence component windows, DyadicRatCore tolerances, RegSeqRat readback, a Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-real-sequence-calibration-carrier}, \autoref{thm:bishop-located-real-sequence-calibration-obligations}, and \autoref{thm:bishop-located-real-sequence-calibration-l10-route} seed the finite calibration route between located sequence data, regular rational readback, and a terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected convergence, trichotomy, countable choice, host real equality, ambient Real completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Lean closure requires checked carrier admission, located-sequence alignment, dyadic tolerance calibration, RegSeqRat handoff, Real-seal nonescape, transport replay, provenance, and local naming for BEDC.Derived.BishopLocatedRealSequenceCalibrationUp.}
\end{closurestatus}
