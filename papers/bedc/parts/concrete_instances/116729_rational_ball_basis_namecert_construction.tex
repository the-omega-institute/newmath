\chapter{A Concrete Naming Certificate for $\RationalBallBasisUp$}
\label{ch:concrete-instances-rational-ball-basis-namecert}
\origin{human}

Following Bishop and Bridges, the $\RationalBallBasisUp$ packet records the finite rational centre-radius basis used before a metric completion or Real-facing consumer reads a regular Cauchy name. It sits between the rational carrier of \autoref{ch:concrete-instances-rat-namecert}, the dyadic tolerance surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the metric test interface of \autoref{ch:concrete-instances-metric-namecert}, the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite rational-ball basis; it is not a host open-ball family, a quotient completion point, a selected limit, a countable-choice basis, or an ambient $\RealUp$ completeness assertion.

\begin{definition}[Rational ball basis carrier]
\label{def:rational-ball-basis-carrier}
A $\RationalBallBasisUp$ carrier is a finite $\BHist$ packet
$$
B=(C,R,D,M,Q,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the centre row, naming a finite list of $\RatUp$ centres.
\item $R$ is the positive rational radius row attached to those centres.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare a requested precision with the rational radius data.
\item $M$ is the $\MetricUp$ distance-test row, reading only centre-radius comparisons displayed by $C,R,D$.
\item $Q$ is the $\RegSeqRatUp$ handoff row obtained from the same rational-ball tests.
\item $E$ is the $\RealUp$ seal row reached only after the regular handoff has been displayed.
\item $H$ records componentwise $\hsame$ transport for centres, radii, dyadic tolerances, metric tests, regular handoff, Real seal, continuation, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from a rational-ball request through $C,R,D,M,Q,E,H$.
\item $P$ is the $\Pkg$ provenance over $\RationalBallBasisUp$, $\RatUp$, $\DyadicRatCoreUp$, $\MetricUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RationalBallBasisUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $C,R,D,M,Q,E,H,T,P,N$. It has no coordinate for host open balls, selected subbases, quotient completion points, chosen limits, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Rational ball basis NameCert obligations]
\label{thm:rational-ball-basis-namecert-obligations}
For every accepted $\RationalBallBasisUp$ carrier $B=(C,R,D,M,Q,E,H,T,P,N)$, the local $\NameCert_{\RationalBallBasisUp}$ surface consists exactly of carrier admission, centre exposure, positive-radius exposure, dyadic tolerance exactness, metric distance-test factorization, regular rational handoff, Real seal nonescape, componentwise transport, continuation replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:rational-ball-basis-carrier}. The centre row $C$ and positive radius row $R$ display the rational balls being inspected, and the dyadic tolerance row $D$ supplies the finite precision comparison used by those balls.

The metric row $M$ reads only the displayed centre-radius-tolerance data. The regular handoff $Q$ is therefore reached through the same finite distance tests, and the Real seal $E$ is downstream of $Q$.

The structural rows $H,T,P,N$ preserve, replay, source, and name that route. Since the excluded objects are absent from the carrier inventory, no additional NameCert obligation can expose host open balls, selected subbases, quotient completion points, chosen limits, countable choice, or ambient Real completeness.
\end{proof}

\begin{theorem}[Rational ball basis regular Cauchy handoff]
\label{thm:rational-ball-basis-regular-cauchy-handoff}
Every metric-completion, Real-separability, or finite-cover consumer of an accepted $\RationalBallBasisUp$ carrier reaches its regular Cauchy data only through the displayed route
$$
\RatUp,\DyadicRatCoreUp
\longmapsto
\MetricUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp
$$
inside the carrier rows $C,R,D,M,Q,E,H,T,P,N$. The exported basis supplies rational centres, positive rational radii, dyadic tolerances, metric distance tests, a regular rational handoff, and a Real seal, but it supplies no arbitrary open-ball basis, quotient completion point, selected real point, countable-choice enumeration, or ambient compactness principle.
\end{theorem}
\begin{proof}
Project the accepted carrier and read the rational-ball rows $C,R,D$. These rows bind the centres, positive radii, and dyadic tolerances before any metric consumer can inspect the packet.

Next use the metric test row $M$. It factors every distance read through the displayed rational data, so the regular handoff $Q$ receives only centre-radius tests already present in the carrier.

Finally consume the Real seal $E$ through the same handoff and structural rows. The transport, replay, provenance, and local naming rows keep the route attached to the displayed rational-ball packet, so completion, separability, and finite-cover consumers obtain the finite basis rather than any of the excluded host principles.
\end{proof}

\falsifiablePrediction{Every $\RationalBallBasisUp$ metric-completion, Real-separability, or finite-cover read factors through rational centres, positive rational radii, a DyadicRatCore tolerance row, a MetricUp distance-test row, RegSeqRat handoff, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{rat, dyadicratcore, metric, regseqrat, real: $\RationalBallBasisUp$ is not bijective to any listed sibling because it jointly carries rational centres, positive rational radii, dyadic tolerances, metric distance tests, regular handoff, Real seal, transport, replay, provenance, and local naming as one finite basis packet.}

\closureat{RationalBallBasisUp}{seedStr}

\begin{closurestatus}{\RationalBallBasisUp}
  \constructivestory{A $\RationalBallBasisUp$ packet is generated from RatUp centres, positive rational radii, a DyadicRatCore tolerance row, MetricUp distance tests, RegSeqRat handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rational-ball-basis-carrier}, \autoref{thm:rational-ball-basis-namecert-obligations}, and \autoref{thm:rational-ball-basis-regular-cauchy-handoff} seed the finite rational-ball route consumed by completion, separability, and finite-cover reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-ball bases, quotient completion points, selected real points, countable-choice enumerations, ambient compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked centre exposure, positive-radius exposure, dyadic tolerance exactness, metric distance-test factorization, regular handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RationalBallBasisUp.}
\end{closurestatus}
