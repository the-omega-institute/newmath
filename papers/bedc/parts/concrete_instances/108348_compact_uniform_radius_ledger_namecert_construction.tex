\chapter{A Concrete Naming Certificate for $\CompactUniformRadiusLedgerUp$}
\label{ch:concrete-instances-compact-uniform-radius-ledger-namecert}
\origin{human}

Following Lebesgue and Bishop--Bridges, the $\CompactUniformRadiusLedgerUp$ packet records the finite positive-radius ledger used in the Heine--Cantor route of \autoref{ch:visions-compact-uniform-continuity}. It sits between the compact finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the pointwise continuity surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the positive Lebesgue-radius packet of \autoref{ch:concrete-instances-finite-lebesgue-number-namecert}, and the downstream $\UniformModulusUp$ consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names the radius choices and their finite lower-bound fold as displayed BEDC rows; it does not export an open-cover compactness theorem, a selected subsequence, a host minimum over radii, quotient equality of reals, or an external analytic modulus.

\begin{definition}[Compact uniform radius ledger carrier]
\label{def:compact-uniform-radius-ledger-carrier}
A $\CompactUniformRadiusLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(K,M,G,F,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, carrying the finite $\mathsf{ProbeBundle}$ of centers and the source-distance coverage rows.
\item $M$ is the $\ContinuousMapUp$ pointwise-modulus ledger for the same source and target metrics.
\item $G$ is the $\FiniteLebesgueNumberUp$ positive-radius row, assigning a positive radius below the cover radius attached to each displayed center.
\item $F$ is the $\RatUp$ lower-bound fold row, produced by primitive recursion over the finite radius bundle.
\item $R$ records the radius-selection compatibility row saying that the folded lower bound is below every positive radius used by $G$.
\item $U$ is the $\UniformModulusUp$ output row that may be read only after $K,M,G,F,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact-net, pointwise-modulus, Lebesgue-radius, rational-fold, compatibility, and output rows.
\item $C$ records displayed $\Cont$ replay from finite-net inspection through pointwise-modulus reads, positive-radius extraction, rational folding, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformRadiusLedgerUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\FiniteLebesgueNumberUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactUniformRadiusLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted ledgers only through these rows. No coordinate records open-cover compactness, selected subsequences, external choice of a minimum radius, host real equality, or a semantic modulus function outside the displayed $\UniformModulusUp$ output.
\end{definition}

\begin{theorem}[Compact uniform radius ledger NameCert obligations]
\label{thm:compact-uniform-radius-ledger-namecert-obligations}
For every accepted $\CompactUniformRadiusLedgerUp$ carrier $L=(K,M,G,F,R,U,H,C,P,N)$, the local $\NameCert_{\CompactUniformRadiusLedgerUp}$ surface consists exactly of compact-net admission through $K$, pointwise-modulus admission through $M$, positive-radius exposure through $G$, finite rational lower-bound folding through $F$, compatibility exposure through $R$, $\UniformModulusUp$ output through $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-radius-ledger-carrier}. The rows $K,M,G,F,R,U,H,C,P,N$ are the complete displayed inventory.

Read the analytic input rows first. The compact-metric row $K$ supplies the finite net and its coverage witnesses, while $M$ supplies the pointwise-modulus rows at the displayed centers. The positive-radius row $G$ ties these local moduli to a finite Lebesgue-radius ledger.

Next read the finite fold. The $\RatUp$ row $F$ compares the positive radii by primitive recursion over the finite bundle, and $R$ records that the resulting lower bound is below each radius used by $G$. These rows are BEDC histories, not a host minimum.

Finally $U,H,C,P,N$ export, transport, replay, source, and name exactly this ledger. Since the carrier has no coordinate for open-cover compactness, selected subsequences, external minimum selection, host real equality, or a semantic modulus function, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact uniform radius ledger Heine--Cantor handoff]
\label{thm:compact-uniform-radius-ledger-heine-cantor-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactUniformRadiusLedgerUp$ packet reads the Heine--Cantor radius choice through the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \FiniteLebesgueNumberUp,\ \RatUp
\longmapsto
\UniformModulusUp .
$$
The handoff first displays the finite net, then the pointwise-modulus and positive-radius rows, then the rational lower-bound fold, and only then exports the uniform-modulus row. It supplies the finite-net lower-bound route described in \autoref{ch:visions-compact-uniform-continuity}, without importing open-cover compactness, sequential compactness, choice of a minimum radius, quotient real equality, or an ambient continuity theorem.
\end{theorem}
\begin{proof}
Use \autoref{thm:compact-uniform-radius-ledger-namecert-obligations}. It orders the compact-net row, pointwise-modulus row, positive-radius row, rational-fold row, compatibility row, and uniform-modulus output inside one accepted carrier.

The finite net in $K$ supplies the centers that $M$ may inspect. The row $G$ records a positive radius below the local cover radius at each center, so the local moduli are tied to the same finite bundle that compactness exposed.

The fold row $F$ and compatibility row $R$ then produce the common precision history consumed by $U$. Because $F$ is a $\RatUp$ fold over the displayed bundle, the lower bound is stable under the classifier and does not depend on an external minimum operator.

Thus the $\UniformModulusUp$ consumer receives a finite BEDC radius ledger. The refused analytic objects are absent from the carrier and cannot be reconstructed from $H,C,P,N$.
\end{proof}

\closureat{CompactUniformRadiusLedgerUp}{seedStr}

\begin{closurestatus}{\CompactUniformRadiusLedgerUp}
  \constructivestory{A $\CompactUniformRadiusLedgerUp$ packet is generated from CompactMetricUp finite-net rows, ContinuousMapUp pointwise-modulus rows, FiniteLebesgueNumberUp positive-radius rows, RatUp lower-bound folds, UniformModulusUp output rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-radius-ledger-carrier}, \autoref{thm:compact-uniform-radius-ledger-namecert-obligations}, and \autoref{thm:compact-uniform-radius-ledger-heine-cantor-handoff} seed the finite Lebesgue-radius ledger used by compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, open-cover compactness, sequential compactness, selected-subsequence extraction, host minimum selection, quotient real equality, or external analytic modulus choice.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net coverage, pointwise-modulus admission, positive-radius exposure, rational lower-bound folding, uniform-modulus output exactness, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformRadiusLedgerUp.}
\end{closurestatus}
