\chapter{A Concrete Naming Certificate for $\LocatedCompletionUniquenessUp$}
\label{ch:concrete-instances-located-completion-uniqueness-namecert}
\origin{human}

Following Bishop and Bridges on separated constructive completions, the $\LocatedCompletionUniquenessUp$ packet records the finite L10 surface where a located Cauchy-filter completion comparison is allowed to read uniqueness only after the separated completion route, completion uniqueness ledger, stream windows, regular rational readback, dyadic tolerance data, and terminal real seal have all been displayed. It sits over the located filter boundary of \autoref{ch:concrete-instances-located-cauchy-filter-namecert}, the separated-completion boundary of \autoref{ch:concrete-instances-separatedcompletion-namecert}, the completion-uniqueness ledger of \autoref{ch:concrete-instances-completion-uniqueness-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the located uniqueness boundary; it does not assert ambient $\RealUp$ completeness, quotient completion equality, selected limits, or host equality of completed points.

\begin{definition}[Located completion uniqueness carrier]
\label{def:located-completion-uniqueness-carrier}
A $\LocatedCompletionUniquenessUp$ carrier is a finite $\BHist$ packet
$$
U=(F,S,Q,W,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\LocatedCauchyFilterUp$ source row, exposing the located tail and basis-refinement data that any completion comparison must first read.
\item $S$ is the $\SeparatedCompletionUp$ boundary row, recording that equality-facing completion reads pass through separated zero-distance evidence rather than host equality.
\item $Q$ is the $\CompletionUniquenessUp$ comparison ledger for the two located completion presentations over the same displayed source route.
\item $W$ is the $\StreamNameUp$ finite-window row inspected by the comparison ledger.
\item $R$ is the $\RegSeqRatUp$ readback row for those windows.
\item $D$ is the positive $\DyadicRatCoreUp$ tolerance ledger used to compare the two located tails at the requested precision.
\item $E$ is the terminal $\RealUp$ seal row reached only after $F,S,Q,W,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for $F,S,Q,W,R,D,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a located completion uniqueness request back through the same filter, separated-completion, completion-uniqueness, window, readback, tolerance, and seal rows.
\item $P$ is the $\Pkg$ provenance over $\LocatedCompletionUniquenessUp$, $\LocatedCauchyFilterUp$, $\SeparatedCompletionUp$, $\CompletionUniquenessUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCompletionUniquenessUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for quotient completion equality, selected representatives, countable choice, host equality of completed points, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located completion uniqueness NameCert obligations]
\label{thm:located-completion-uniqueness-namecert-obligations}
For every accepted $\LocatedCompletionUniquenessUp$ carrier
$$
U=(F,S,Q,W,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedCompletionUniquenessUp}$ surface consists exactly of located Cauchy-filter source admission, separated-completion boundary admission, completion-uniqueness ledger exposure, StreamName window admission, RegSeqRat readback, DyadicRatCore tolerance comparison, Real seal nonescape, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-completion-uniqueness-carrier}. The analytic rows are exactly $F,S,Q,W,R,D,E$, and the structural rows are $H,C,P,N$.

The located source and separated boundary rows are read first. Row $F$ exposes the located filter data, while row $S$ forces equality-facing completion reads to pass through the separated completion boundary.

The uniqueness comparison is then a displayed finite ledger. Row $Q$ records the completion-uniqueness comparison, and rows $W,R,D$ provide the stream windows, regular rational readback, and dyadic tolerance data consumed by that ledger.

Only after those rows are visible may the route reach the real seal $E$. The rows $H,C,P,N$ transport, replay, source, and name the same route, so the local certificate cannot export quotient completion equality, selected representatives, host equality, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Located completion uniqueness Real seal nonescape]
\label{thm:located-completion-uniqueness-real-seal-nonescape}
Every $\RealUp$-facing read exported by an accepted $\LocatedCompletionUniquenessUp$ carrier factors through the finite route
$$
\LocatedCauchyFilterUp,\ \SeparatedCompletionUp,\ \CompletionUniquenessUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp
\longmapsto
\RealUp,H,C,P,N .
$$
Thus located completion uniqueness is available only as a displayed NameCert route through located filter data, separated completion, the completion uniqueness ledger, finite windows, regular readback, and dyadic tolerances. The packet exports no ambient $\RealUp$ completeness theorem, quotient completion equality, selected limit, host equality of completed points, or countable-choice schedule.
\end{theorem}
\begin{proof}
Use \autoref{thm:located-completion-uniqueness-namecert-obligations} to identify the complete local obligation surface. A real-facing read must first consume $F,S,Q,W,R,D$ before the seal row $E$ is available.

The separated and uniqueness rows are both visible. Row $S$ supplies the separated completion boundary, and row $Q$ supplies the completion uniqueness ledger over the same located route rather than replacing it by a quotient object.

The window, readback, and tolerance rows make the comparison finite. Rows $W,R,D$ expose the StreamName, RegSeqRat, and DyadicRatCore data used by the ledger, after which $E$ is only a terminal seal.

Finally $H,C,P,N$ preserve that route without adding host data. Since no excluded object is a carrier coordinate, every exported real-facing read remains inside the displayed located completion uniqueness boundary.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedCompletionUniquenessUp$ uniqueness read must expose a $\LocatedCauchyFilterUp$ source row, a $\SeparatedCompletionUp$ boundary row, a $\CompletionUniquenessUp$ ledger, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a $\DyadicRatCoreUp$ tolerance ledger, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.}

\independenceWitness{locatedcauchyfilter, separatedcompletion, completionuniqueness, streamname, regseqrat, dyadicratcore, real: $\LocatedCompletionUniquenessUp$ is not bijective to any listed sibling because it jointly carries the located filter source, separated completion boundary, completion uniqueness ledger, finite windows, regular readback, dyadic tolerance comparison, Real seal, transport, replay, provenance, and local naming while refusing quotient completion equality and ambient completeness.}

\closureat{LocatedCompletionUniquenessUp}{seedStr}

\begin{closurestatus}{\LocatedCompletionUniquenessUp}
  \constructivestory{A $\LocatedCompletionUniquenessUp$ packet is generated from a LocatedCauchyFilter row, SeparatedCompletion boundary, CompletionUniqueness ledger, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance ledger, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-completion-uniqueness-carrier}, \autoref{thm:located-completion-uniqueness-namecert-obligations}, and \autoref{thm:located-completion-uniqueness-real-seal-nonescape} seed the located uniqueness boundary over located Cauchy filters, separated completions, completion uniqueness, finite stream windows, regular rational readback, dyadic tolerances, and Real seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient $\RealUp$ completeness, quotient completion equality, selected limits, host equality of completed points, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located filter exposure, separated completion boundary, completion uniqueness ledger exactness, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance comparison, Real seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
