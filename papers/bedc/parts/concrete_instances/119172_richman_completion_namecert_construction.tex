\chapter{A Concrete Naming Certificate for $\RichmanCompletionUp$}
\label{ch:concrete-instances-richman-completion-namecert}
\origin{human}

Following Bishop and Richman, the $\RichmanCompletionUp$ packet records a constructive completion neighbour for the L10 Real and Completion corridor as finite BEDC NameCert data. It consumes the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, finite stream windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-functor handoff of \autoref{ch:concrete-instances-bishop-regular-completion-functor-namecert}. The packet names the inspected regular-Cauchy completion route; it does not quotient names, select limits, use countable choice, assert ambient $\RealUp$ completeness, choose finite subcovers, or import open-cover compactness.

\begin{definition}[Richman completion carrier]
\label{def:richman-completion-carrier}
A $\RichmanCompletionUp$ carrier is a finite $\BHist$ packet
$$
R=(D,S,Q,E,L,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ arithmetic row, recording dyadic endpoints, comparisons, and rational tolerance arithmetic used by the completion read.
\item $S$ is the $\StreamNameUp$ finite-window row, listing exactly the inspected rational-name windows visible to a consumer.
\item $Q$ is the $\RegSeqRatUp$ regular-Cauchy readback row obtained from the displayed stream windows and dyadic tolerance data.
\item $E$ is the $\RealUp$ seal row, readable only after $D,S,Q$ and the local comparison data have been displayed.
\item $L$ is the located comparison row, answering each admitted rational cut request by finite inspection of $D,S,Q$ before any terminal seal is consumed.
\item $B$ is the dyadic radius-budget row, recording the finite radii and contraction budgets used to pass from regular-Cauchy data to the completion handoff.
\item $H$ records componentwise $\hsame$ transport for the dyadic arithmetic, stream windows, regular readback, real seal, located comparison, radius budget, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same finite completion route.
\item $P$ is the $\Pkg$ provenance over $\RichmanCompletionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BishopRegularCompletionFunctorUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RichmanCompletionUp}$ row naming exactly this packet.
\end{enumerate}
The classifier compares accepted carriers only through $D,S,Q,E,L,B,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, selected limits, countable choice, host equality of real names, ambient $\RealUp$ completeness, open-cover compactness, or selected finite subcovers.
\end{definition}

\begin{theorem}[Richman completion NameCert obligations]
\label{thm:richman-completion-namecert-obligations}
For every accepted $\RichmanCompletionUp$ carrier
$$
R=(D,S,Q,E,L,B,H,C,P,N),
$$
the local $\NameCert_{\RichmanCompletionUp}$ surface consists exactly of carrier admission, $\DyadicRatCoreUp$ arithmetic visibility, finite $\StreamNameUp$ window admission, $\RegSeqRatUp$ regular readback, $\RealUp$ seal discipline, located comparison, dyadic radius-budget exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every completion-facing read must pass through those rows before the terminal seal is available.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:richman-completion-carrier}. The analytic rows $D,S,Q,E,L,B$ expose the dyadic arithmetic, finite windows, regular-Cauchy readback, terminal Real seal, located comparison, and radius-budget data in the order named by the tuple.

Read the constructive route through the sibling chapters. The dyadic row $D$ is the arithmetic source, the stream row $S$ fixes the finite observations, and the regular row $Q$ provides the Cauchy readback before $E$ can be consumed.

The comparison and budget rows $L,B$ keep the Richman-facing completion read local: each cut response and radius request is answered by visible finite data rather than by a quotient object or selected limiting representative.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for quotient completion equality, selected limits, countable choice, host equality, ambient $\RealUp$ completeness, open-cover compactness, or selected finite subcovers, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Richman completion regular-Cauchy handoff]
\label{thm:richman-completion-regular-cauchy-handoff}
Every $\RealUp$-facing or completion-functor-facing consumer of an accepted $\RichmanCompletionUp$ packet receives the finite handoff
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp \longmapsto \BishopRegularCompletionFunctorUp
$$
through the displayed rows $D,S,Q,E,L,B,H,C,P,N$. The handoff is a regular-Cauchy completion route with located comparison and dyadic radius budgets; it is not a quotient completion, selected limit, countable-choice schedule, host real equality theorem, ambient $\RealUp$ completeness claim, selected finite subcover, or open-cover compactness principle.
\end{theorem}
\begin{proof}
Start with the source side. The rows $D$ and $S$ determine the dyadic tolerance cells and the finite rational-name windows available to the read.

Next pass through $Q,E$. The $\RegSeqRatUp$ row reads the displayed windows as regular-Cauchy data, and the terminal $\RealUp$ seal is exposed only after that readback.

Then use $L,B$ to route the Richman completion interface. The located comparison row answers admitted cut requests from the same finite data, while the dyadic radius-budget row records the radius schedule consumed by the completion-functor handoff.

The structural rows $H,C,P,N$ preserve the sibling route and attach the local name. Thus any completion-facing consumer receives the displayed regular-Cauchy handoff and no quotient, selector, choice, host-equality, ambient-completeness, finite-subcover, or open-cover compactness coordinate.
\end{proof}

\falsifiablePrediction{Any accepted $\RichmanCompletionUp$ consumer that reaches a $\RealUp$ seal or a $\BishopRegularCompletionFunctorUp$ handoff without first reading the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, located comparison, dyadic radius-budget, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows is outside the packet surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, bishop-regular-completion-functor: no listed sibling is bijective to $\RichmanCompletionUp$ because this packet binds dyadic arithmetic, finite windows, regular-Cauchy readback, terminal Real sealing, located comparison, radius budgets, transport, replay, provenance, and local naming as one constructive completion surface.}

\closureat{RichmanCompletionUp}{seedStr}

\begin{closurestatus}{\RichmanCompletionUp}
  \constructivestory{A $\RichmanCompletionUp$ packet is generated from DyadicRatCore arithmetic rows, StreamName finite windows, RegSeqRat readback, a RealUp seal, located comparison, dyadic radius budgets, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:richman-completion-carrier}, \autoref{thm:richman-completion-namecert-obligations}, and \autoref{thm:richman-completion-regular-cauchy-handoff} seed the Richman constructive completion route over dyadic arithmetic, finite windows, regular-Cauchy readback, Real sealing, located comparison, radius budgets, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim RealUp completeness, quotient completion, quotient stream equality, selected limits, countable choice, selected finite subcovers, open-cover compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic arithmetic coverage, finite-window readback, regular-Cauchy handoff, located comparison exactness, radius-budget stability, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RichmanCompletionUp.}
\end{closurestatus}
