\chapter{A Concrete Naming Certificate for $\BishopRegularRealSealUp$}
\label{ch:concrete-instances-bishop-regular-real-seal-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopRegularRealSealUp$ packet records the regular-rational seal by which a displayed Cauchy name reaches the public $\RealUp$ boundary. It sits between the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the completion modulus discipline of \autoref{ch:concrete-instances-bishopcompletionmodulus-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite windows, regularity budgets, dyadic tolerances, seal transport, replay, provenance, and a local certificate; it does not allocate quotient streams, selected limits, countable-choice schedules, host real equality, or ambient completeness.

\begin{definition}[Bishop regular-real seal carrier]
\label{def:bishop-regular-real-seal-carrier}
A $\BishopRegularRealSealUp$ carrier is a finite $\BHist$ packet
$$
R=(W,G,D,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the $\StreamNameUp$ finite-window row exposing the rational-name observations available to the seal.
\item $G$ is the $\RegSeqRatUp$ regular rational readback row attached to the same displayed windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger that supplies the dyadic precision and endpoint comparisons consumed by $G$.
\item $M$ is the $\BishopCompletionModulusUp$ row recording the explicit completion modulus that makes the windows in $W$ reusable for all later seal reads.
\item $E$ is the terminal $\RealUp$ boundary seal, available only after $W,G,D,M$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the stream windows, regular readback, dyadic ledger, completion modulus, real seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay for every consumer route from $W,G,D,M$ to $E$.
\item $P$ is the $\Pkg$ provenance over $\BishopRegularRealSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BishopCompletionModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegularRealSealUp}$ row.
\end{enumerate}
The carrier stores no quotient stream, selected representative, host limit, countable-choice schedule, real trichotomy, or ambient completeness witness.
\end{definition}

\begin{theorem}[Bishop regular-real seal NameCert obligations]
\label{thm:bishop-regular-real-seal-namecert-obligations}
For every accepted $\BishopRegularRealSealUp$ carrier $R=(W,G,D,M,E,H,C,P,N)$, the local $\NameCert_{\BishopRegularRealSealUp}$ surface consists exactly of carrier admission for the displayed rows $W,G,D,M,E,H,C,P,N$, stream-window exposure through $W$, regular-rational readback through $G$, dyadic tolerance exposure through $D$, completion-modulus reuse through $M$, terminal real-seal exposure through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-regular-real-seal-carrier}. The analytic rows are exactly the stream windows, regular readback, dyadic ledger, completion modulus, and real seal, and the structural rows are exactly transport, replay, provenance, and local naming.

The window route begins at $W$. A consumer may inspect only the displayed finite observations named by the $\StreamNameUp$ row before any regular-rational or real-facing read is available.

The regularity route then reads $G,D,M$ together. The $\RegSeqRatUp$ readback consumes the dyadic tolerance row $D$, while the $\BishopCompletionModulusUp$ row records the completion budget that lets later consumers reuse the same windows.

Finally $E,H,C,P,N$ expose, transport, replay, source, and name the terminal $\RealUp$ seal over that finite route. Since no carrier coordinate stores quotient streams, selected limits, host equality, countable choice, trichotomy, or ambient completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop regular-real seal RegSeqRat route]
\label{thm:bishop-regular-real-seal-regseqrat-route}
Every accepted $\BishopRegularRealSealUp$ readback to $\RegSeqRatUp$ factors through the displayed route
$$
W \longmapsto D \longmapsto M \longmapsto G.
$$
The stream-window row $W$ supplies the finite observations, the $\DyadicRatCoreUp$ row $D$ supplies the tolerance ledger, the $\BishopCompletionModulusUp$ row $M$ supplies the reusable completion budget, and the $\RegSeqRatUp$ row $G$ is the only regular rational readback exported by the packet.
\end{theorem}
\begin{proof}
Start with \autoref{def:bishop-regular-real-seal-carrier}. The row $G$ is not a detached regular sequence; it appears only with the window row $W$, the dyadic ledger $D$, and the completion modulus $M$.

Apply \autoref{thm:bishop-regular-real-seal-namecert-obligations}. Stream-window exposure precedes regular readback, dyadic tolerance exposure supplies the comparison budget, and completion-modulus reuse records the stable tail discipline for later consumers.

Therefore any accepted $\RegSeqRatUp$ readback uses the route $W,D,M,G$. A quotient stream, selected representative, host limit, or choice schedule has no row from which to replace that route.
\end{proof}

\begin{theorem}[Bishop regular-real seal Real boundary]
\label{thm:bishop-regular-real-seal-real-boundary}
Every terminal $\RealUp$ boundary read exported by an accepted $\BishopRegularRealSealUp$ carrier factors through the finite route
$$
W \longmapsto G \longmapsto D \longmapsto M \longmapsto E \longmapsto H,C,P,N.
$$
In particular, the public real-name boundary is reached only after the $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, and $\BishopCompletionModulusUp$ row have all been displayed.
\end{theorem}
\begin{proof}
Read the terminal seal row in \autoref{def:bishop-regular-real-seal-carrier}. The row $E$ is stored downstream of $W,G,D,M$, so it cannot be consumed before the finite regularity route has been displayed.

Use \autoref{thm:bishop-regular-real-seal-regseqrat-route}. The regular rational readback already factors through the stream windows, dyadic tolerances, and completion modulus. Hence the terminal seal has the same preceding route whenever a real-facing consumer inspects it.

Finally apply \autoref{thm:bishop-regular-real-seal-namecert-obligations}. Transport, replay, provenance, and local naming preserve the same finite surface after $E$ is reached. The exported $\RealUp$ boundary therefore contains no quotient stream row, selected limit, host real equality, countable-choice schedule, trichotomy, or ambient completeness coordinate.
\end{proof}

\falsifiablePrediction{An accepted $\BishopRegularRealSealUp$ consumer that reaches the terminal $\RealUp$ boundary without displaying $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, and a $\BishopCompletionModulusUp$ row refutes the carrier.}

\independenceWitness{streamname, regseqrat, dyadicratcore, bishop-completion-modulus, real: no listed sibling is bijective to $\BishopRegularRealSealUp$ because the packet jointly carries finite windows, regular readback, dyadic tolerances, completion-modulus reuse, terminal Real seal exposure, transport, replay, provenance, and local naming.}

\closureat{BishopRegularRealSealUp}{seedStr}

\begin{closurestatus}{\BishopRegularRealSealUp}
  \constructivestory{A $\BishopRegularRealSealUp$ packet is generated from StreamName finite windows, a RegSeqRat readback row, a DyadicRatCore tolerance ledger, a BishopCompletionModulus row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-real-seal-carrier}, \autoref{thm:bishop-regular-real-seal-namecert-obligations}, \autoref{thm:bishop-regular-real-seal-regseqrat-route}, and \autoref{thm:bishop-regular-real-seal-real-boundary} seed the finite regular-real seal route from displayed windows and tolerances to the public Real boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking for $\BishopRegularRealSealUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, regular readback routing, completion-modulus reuse, Real boundary nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRegularRealSealUp.}
\end{closurestatus}
