\chapter{A Concrete Naming Certificate for $\LocatedWeakKonigUp$}
\label{ch:concrete-instances-located-weak-konig-namecert}
\origin{human}

Following Troelstra and van Dalen on choice sequences, bars, and constructive weak Konig readings, the $\LocatedWeakKonigUp$ packet records the finite located-branch surface used when completion and real-name consumers inspect binary trees through explicit prefix windows. It sits after the boundary refusal row of \autoref{ch:concrete-instances-weak-konig-boundary-namecert}, the finite-prefix route of \autoref{ch:concrete-instances-konig-lemma-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the Cantor prefix surface of \autoref{ch:concrete-instances-cantorspace-namecert}, and the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet names located finite branch evidence; it does not export countable choice, an open-cover compactness theorem, quotient stream equality, a selected infinite branch, or ambient $\RealUp$ completeness.

\begin{definition}[Located weak Konig carrier]
\label{def:located-weak-konig-carrier}
A $\LocatedWeakKonigUp$ carrier is a finite $\BHist$ packet
$$
L=(T,B,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the finite binary tree row, carrying decidable node membership, prefix closure, and finite successor checks through the $\KonigLemmaUp$ and $\CantorSpaceUp$ sibling routes.
\item $B$ is the decidable located bar row, recording for the current request which finite prefixes are already blocked, which remain open inside the displayed window, and which comparison rows are still unresolved.
\item $W$ is the finite prefix window row, read through $\StreamNameUp$ observations and $\RegSeqRatUp$ readback before any completion-facing consumer sees branch data.
\item $E$ is the branch witness row, storing a finite compatible prefix or a finite obstruction for the displayed window, not a completed infinite branch and not a choice-selected path.
\item $H$ records componentwise $\hsame$ transport for the tree, located bar, prefix window, branch witness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located branch request through $T,B,W,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LocatedWeakKonigUp$, $\WeakKonigBoundaryUp$, $\KonigLemmaUp$, $\StreamNameUp$, $\CantorSpaceUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedWeakKonigUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite located-branch packet. It has no coordinate for countable choice, open-cover compactness, quotient stream equality, selected infinite branches, host path equality, or ambient Real completeness.
\end{definition}
\leanstmt{BEDC.Derived.LocatedWeakKonigUp}

\begin{theorem}[Located weak Konig NameCert obligations]
\label{thm:located-weak-konig-namecert-obligations}
For every accepted $\LocatedWeakKonigUp$ carrier
$$
L=(T,B,W,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedWeakKonigUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the finite binary tree, decidable located bar, finite prefix window, branch witness, transport, replay, provenance, and local naming rows;
\item tree-window exactness, requiring every branch-facing read to expose $T$ and $W$ before the witness row $E$ is consumed;
\item located-bar decidability, requiring the bar row $B$ to display the current finite blocked/open alternatives inside the same prefix window;
\item branch-witness locality, requiring $E$ to be a finite compatible prefix or finite obstruction read through $T,B,W$;
\item transport and replay preservation through $H$ and $C$, with provenance and local naming through $P$ and $N$;
\item non-escape, excluding countable choice, open-cover compactness, quotient stream equality, selected infinite branches, host path equality, and ambient $\RealUp$ completeness.
\end{enumerate}
No located weak Konig obligation is stored outside $T,B,W,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-weak-konig-carrier}. The accepted packet displays exactly the finite tree, located bar, finite prefix window, finite witness, and structural rows.

Tree-window exactness reads the $\KonigLemmaUp$ and $\WeakKonigBoundaryUp$ sibling routes first. Those routes permit finite tree and prefix inspection while refusing branch selection and choice schedules.

The located bar row $B$ and prefix window row $W$ make the branch request finite. A consumer sees only the current blocked/open alternatives and the stream-window readback supplied by $\StreamNameUp$, $\CantorSpaceUp$, and $\RegSeqRatUp$ before reaching $E$.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since none of the carrier rows stores a selected infinite path, quotient stream equality, open-cover compactness object, choice schedule, or ambient Real-completeness witness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Located weak Konig finite branch route]
\label{thm:located-weak-konig-finite-branch-route}
Every Real, completion, or Cantor-facing consumer of an accepted $\LocatedWeakKonigUp$ carrier factors through the finite route
$$
\WeakKonigBoundaryUp,\ \KonigLemmaUp,\ \StreamNameUp,\ \CantorSpaceUp,\ \RegSeqRatUp,\ T,B,W,E,H,C,P,N .
$$
The consumer may inspect located finite branch evidence and finite obstructions inside the displayed prefix window. It receives no countable-choice schedule, no open-cover compactness theorem, no quotient stream equality, no selected infinite branch, and no ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with a consumer read of $L=(T,B,W,E,H,C,P,N)$. Carrier admission exposes a finite binary tree, a decidable located bar, a prefix window, and a finite branch witness or obstruction.

Use the $\WeakKonigBoundaryUp$ refusal surface and the $\KonigLemmaUp$ finite-prefix route. Together they force tree and branch reasoning to remain in finite prefix data before any path-like consumer can read a witness row.

Next read the stream and Cantor siblings. The finite prefix window $W$ is observed through $\StreamNameUp$ and $\CantorSpaceUp$, and the completion-facing readback goes through $\RegSeqRatUp$ before any Real or completion consumer uses the branch evidence.

The branch witness row $E$ is therefore located inside the displayed finite window. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve that route and add none of the excluded global principles.
\end{proof}

\falsifiablePrediction{A $\LocatedWeakKonigUp$ consumer that obtains a selected infinite branch, countable-choice schedule, quotient stream equality, open-cover compactness theorem, or ambient $\RealUp$ completeness witness without first displaying $T,B,W,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{weak-konig-boundary, konig-lemma, streamname, cantorspace, regseqrat: $\LocatedWeakKonigUp$ is not bijective to any listed sibling because it jointly carries a finite binary tree, a decidable located bar, a prefix window, a finite branch witness or obstruction, transport, replay, provenance, and local NameCert row in one packet.}

\closureat{LocatedWeakKonigUp}{seedStr}

\begin{closurestatus}{\LocatedWeakKonigUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-weak-konig-carrier}, \autoref{thm:located-weak-konig-namecert-obligations}, and \autoref{thm:located-weak-konig-finite-branch-route} seed the finite located-branch NameCert surface over binary trees, located bars, and prefix windows.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.LocatedWeakKonigUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close weak Konig's lemma as a global rule, countable choice, selected infinite branches, quotient stream equality, open-cover compactness, ambient $\RealUp$ completeness, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tree-window exactness, located-bar decidability, finite branch witness locality, transport, replay, and non-escape for BEDC.Derived.LocatedWeakKonigUp.}
  \constructivestory{A $\LocatedWeakKonigUp$ packet is generated from a finite binary tree row, decidable located bar, finite prefix window, finite branch witness or obstruction, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
