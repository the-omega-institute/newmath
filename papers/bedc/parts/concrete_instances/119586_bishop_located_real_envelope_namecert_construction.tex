\chapter{A Concrete Naming Certificate for $\BishopLocatedRealEnvelopeUp$}
\label{ch:concrete-instances-bishop-located-real-envelope-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopLocatedRealEnvelopeUp$ packet records a finite located envelope for a constructive real approximation before any quotient equality, trichotomy, choice-selected limit, or ambient completeness claim is available. It sits over the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the located interval boundary of \autoref{ch:concrete-instances-located-real-interval-namecert}. The packet gives Real and Completion consumers a named finite envelope surface rather than a completed real construction.

\begin{definition}[Bishop located real envelope carrier]
\label{def:bishop-located-real-envelope-carrier}
A $\BishopLocatedRealEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
E=(D,S,Q,R,L,U,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger carrying dyadic radii, endpoint comparisons, midpoint arithmetic, and slack witnesses for the inspected envelope.
\item $S$ is the $\StreamNameUp$ finite-window schedule listing exactly which approximation windows are exposed.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the displayed stream windows and dyadic tolerances.
\item $R$ is the terminal $\RealUp$ seal reached only after $D,S,Q$ have been displayed.
\item $L$ and $U$ are lower and upper dyadic envelope rows, with their order and width checked inside $D$.
\item $A$ is the $\LocatedRealIntervalUp$ adjacency row certifying that the displayed lower and upper rows form a located interval envelope around the inspected real name.
\item $H$ stores componentwise $\hsame$ transport for the dyadic ledger, stream windows, regular readback, Real seal, envelope endpoints, located interval row, replay, provenance, and local name.
\item $C$ stores displayed $\Cont$ replay from a precision request through $S,D,L,U,Q,A,R,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedRealEnvelopeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedRealIntervalUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedRealEnvelopeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,Q,R,L,U,A,H,C,P,N$. It has no coordinate for quotient equality of real names, trichotomy, countable choice, selected limits, supremum, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop located real envelope NameCert obligations]
\label{thm:bishop-located-real-envelope-namecert-obligations}
For every accepted $\BishopLocatedRealEnvelopeUp$ carrier $E=(D,S,Q,R,L,U,A,H,C,P,N)$, the local $\NameCert_{\BishopLocatedRealEnvelopeUp}$ surface consists exactly of carrier admission, dyadic tolerance exposure, finite-window scheduling, regular rational readback, lower and upper envelope exposure, located interval adjacency, terminal Real sealing, componentwise transport, displayed replay, package provenance, and local naming. Every Real-facing or Completion-facing read must pass through these rows before consuming the seal $R$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-real-envelope-carrier}. The analytic rows are $D,S,Q,R,L,U,A$, and the structural rows are $H,C,P,N$.

The dyadic and stream stages are finite. The row $D$ supplies endpoint arithmetic and tolerance comparisons, while $S$ names the inspected windows. The readback row $Q$ is therefore attached to displayed observations rather than to an ambient completed real.

The envelope rows are downstream of those observations. The lower and upper rows $L,U$ are checked by $D$, and the located interval row $A$ records their envelope adjacency before the terminal seal $R$ is available.

Finally $H,C,P,N$ only transport, replay, source, and name the same route. Since no omitted coordinate stores quotient equality, trichotomy, countable choice, selected limits, supremum, open-cover compactness, or host completeness, the local NameCert surface is exactly the displayed finite envelope surface.
\end{proof}

\begin{theorem}[Bishop located real envelope finite-window readback]
\label{thm:bishop-located-real-envelope-finite-window-readback}
Every accepted $\BishopLocatedRealEnvelopeUp$ read of a real approximation factors through the finite route
$$
\DyadicRatCoreUp\longmapsto \StreamNameUp\longmapsto \RegSeqRatUp\longmapsto \LocatedRealIntervalUp\longmapsto \RealUp
$$
as displayed by $D,S,Q,A,R,H,C,P,N$, with the lower and upper envelope rows $L,U$ read before the terminal seal. Thus the packet exposes located real approximation without quotient equality, trichotomy, countable choice over precision requests, selected limits, supremum, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with a consumer request for an accepted envelope. The finite schedule $S$ identifies the inspected windows, and the dyadic ledger $D$ identifies the tolerances and endpoint comparisons visible at those windows.

The regular readback $Q$ is the only row that turns those displayed windows into rational approximation data. The lower and upper envelope rows $L,U$ are then read against the same dyadic ledger, so the packet has no independent real-order oracle.

The located interval row $A$ binds the two endpoint rows as a finite located envelope. Only after that adjacency is displayed may the terminal seal $R$ be consumed.

Transport, replay, provenance, and local naming preserve the same dependency route. Hence every read stays inside the finite-window envelope and cannot export the excluded quotient, choice, order, compactness, or completeness principles.
\end{proof}

\falsifiablePrediction{Every $\BishopLocatedRealEnvelopeUp$ consumer that reaches a $\RealUp$ seal has already exposed $D,S,Q,L,U,A,H,C,P,N$ and the finite dyadic, stream, regular-readback, and located-interval route named by this chapter.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, locatedrealinterval: $\BishopLocatedRealEnvelopeUp$ is not bijective to any listed sibling because it jointly carries a dyadic tolerance ledger, finite StreamName windows, RegSeqRat readback, lower and upper envelope rows, a LocatedRealInterval adjacency row, a Real seal, transport, replay, provenance, and local naming.}

\closureat{BishopLocatedRealEnvelopeUp}{seedStr}

\begin{closurestatus}{\BishopLocatedRealEnvelopeUp}
  \constructivestory{A $\BishopLocatedRealEnvelopeUp$ packet is generated from a DyadicRatCore tolerance ledger, finite StreamName windows, RegSeqRat readback, lower and upper envelope rows, a LocatedRealInterval adjacency row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-real-envelope-carrier}, \autoref{thm:bishop-located-real-envelope-namecert-obligations}, and \autoref{thm:bishop-located-real-envelope-finite-window-readback} seed the finite located-envelope route from dyadic tolerance rows through finite windows, regular readback, located interval adjacency, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient equality of real names, trichotomy, countable choice, selected limits, supremum, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exposure, finite-window scheduling, regular rational readback, envelope exactness, located interval adjacency, terminal Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedRealEnvelopeUp.}
\end{closurestatus}
