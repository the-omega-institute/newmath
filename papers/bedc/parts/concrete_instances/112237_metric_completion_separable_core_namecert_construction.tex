\chapter{A Concrete Naming Certificate for $\MetricCompletionSeparableCoreUp$}
\label{ch:concrete-instances-metric-completion-separable-core-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley on metric completion, the $\MetricCompletionSeparableCoreUp$ packet records the finite L10 handoff by which countable dense data remains visible to completion consumers. It reads the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the indexed dense rows of \autoref{ch:concrete-instances-countable-dense-subset-namecert}, the separability surface of \autoref{ch:concrete-instances-separablemetric-namecert}, the completion interface of \autoref{ch:concrete-instances-metriccompletion-namecert}, the dense-image packet of \autoref{ch:concrete-instances-dense-image-completion-namecert}, the dense embedding of \autoref{ch:concrete-instances-metric-completion-dense-embedding-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a displayed separability-to-completion core; it does not use quotient completion equality, selected limits, countable choice, ambient $\RealUp$ completeness, or a host dense-image theorem.

\begin{definition}[Metric completion separable core carrier]
\label{def:metric-completion-separable-core-carrier}
A $\MetricCompletionSeparableCoreUp$ carrier is a finite $\BHist$ packet
$$
S=(M,D,W,E,I,J,Q,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the source $\MetricUp$ row, carrying the finite distance comparisons used by dense and completion-facing reads.
\item $D$ is the $\CountableDenseSubsetUp$ and $\SeparableMetricUp$ dense enumeration row over $M$.
\item $W$ is the finite $\StreamNameUp$ window row, listing the source indices and completion entries inspected by the current consumer.
\item $E$ is the $\MetricCompletionUp$ entry row, exposing completion names only as displayed finite Cauchy-entry data.
\item $I$ is the $\DenseImageCompletionUp$ row, recording that displayed source entries approximate the completion read.
\item $J$ is the $\MetricCompletionDenseEmbeddingUp$ row, tying the dense image to the source metric and separated comparison route.
\item $Q$ is the separated comparison row, recording zero-distance and apartness-facing comparisons only through finite displayed tolerances.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational observations opened by $W$.
\item $L$ is the $\RealUp$ tolerance ledger, sealing the finite distance and approximation budgets read by $R$.
\item $H$ records componentwise $\hsame$ transport for the metric, dense enumeration, completion-entry, dense-image, dense-embedding, separated comparison, readback, tolerance, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from source metric inspection through dense enumeration windows, completion-entry reads, dense-image exposure, separated comparison, rational readback, and Real tolerance sealing.
\item $P$ is the $\Pkg$ provenance over $\MetricCompletionSeparableCoreUp$, $\MetricUp$, $\CountableDenseSubsetUp$, $\SeparableMetricUp$, $\MetricCompletionUp$, $\DenseImageCompletionUp$, $\MetricCompletionDenseEmbeddingUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetricCompletionSeparableCoreUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient completion equality, a selected dense subsequence, countable-choice enumeration, an ambient completed metric object, or Real completeness outside the displayed tolerance ledger.
\end{definition}

\begin{theorem}[Metric completion separable core NameCert obligations]
\label{thm:metric-completion-separable-core-namecert-obligations}
For every accepted $\MetricCompletionSeparableCoreUp$ carrier $S=(M,D,W,E,I,J,Q,R,L,H,C,P,N)$, the local $\NameCert_{\MetricCompletionSeparableCoreUp}$ surface consists exactly of carrier admission for the source metric row, dense enumeration row, finite stream windows, completion entry row, dense-image row, dense-embedding row, separated comparison row, regular-rational readback, Real tolerance ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every completion-facing separability read must expose $M,D,W,E,I,J,Q,R,L,H,C,P,N$ before it can be consumed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metric-completion-separable-core-carrier}. The only source and density coordinates are $M,D,W$, so a separability read cannot inspect completion data before the metric and dense enumeration windows are displayed.

The completion-facing rows are then read in order. The entry row $E$ exposes completion names as finite Cauchy-entry data, while $I$ and $J$ expose the dense-image and dense-embedding routes that keep the source metric visible to completion consumers.

The comparison and readback rows are downstream of the same windows. The row $Q$ records separated comparison data, $R$ reads the regular rational observations opened by $W$, and $L$ seals the resulting Real tolerance ledger.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since no other coordinate occurs in the carrier, the local NameCert surface cannot import quotient completion equality, countable choice, selected limits, ambient Real completeness, or a host dense-image theorem.
\end{proof}

\begin{theorem}[Metric completion separable core density handoff]
\label{thm:metric-completion-separable-core-density}
Let $S=(M,D,W,E,I,J,Q,R,L,H,C,P,N)$ be an accepted $\MetricCompletionSeparableCoreUp$ carrier. Every $\MetricCompletionUp$ consumer that asks for separability of the completion receives the finite route
$$
\MetricUp,\ \CountableDenseSubsetUp,\ \SeparableMetricUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
\longmapsto
\DenseImageCompletionUp,\ \MetricCompletionDenseEmbeddingUp,\ \MetricCompletionUp .
$$
Thus the completion-side dense read is generated by displayed source dense data, finite observation windows, regular-rational readback, Real tolerance ledgers, dense-image exposure, and dense-embedding comparison rows, not by quotient representatives, selected completion points, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with a completion-facing density request. The source of dense data is $D$ over the metric row $M$, and the only observations available to the request are the finite stream windows $W$.

Read those windows through $R$ and $L$. Regular-rational readback supplies the finite rational observations, and the Real tolerance ledger records the approximation budgets used by the completion-side consumer.

The handoff to completion is through $E,I,J,Q$. The completion entry row opens the finite target entry, the dense-image row records the source approximation, the dense-embedding row keeps the source and target tied to one metric route, and the separated comparison row records only displayed tolerances.

The structural rows $H,C,P,N$ preserve this route. Consequently the completion consumer receives a finite separability handoff from the source dense enumeration to the completion read, with no coordinate for quotient equality, selected limits, countable-choice enumeration, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\MetricCompletionSeparableCoreUp$ completion-facing density read factors through the source metric, countable dense schedule, finite StreamName windows, completion entry row, dense-image row, dense-embedding row, separated comparison, RegSeqRat readback, Real tolerance ledger, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{metric, countable-dense-subset, separablemetric, metriccompletion, dense-image-completion, metric-completion-dense-embedding, streamname, regseqrat, real: $\MetricCompletionSeparableCoreUp$ is not bijective to any listed sibling because it carries the complete separability-to-completion handoff in one packet, with dense enumeration, completion entry, dense image, separated comparison, readback, and tolerance rows visible together.}

\closureat{MetricCompletionSeparableCoreUp}{seedStr}

\begin{closurestatus}{\MetricCompletionSeparableCoreUp}
  \constructivestory{A $\MetricCompletionSeparableCoreUp$ packet is generated from a source Metric row, CountableDenseSubset and SeparableMetric dense enumeration rows, finite StreamName windows, MetricCompletion entry rows, DenseImageCompletion and MetricCompletionDenseEmbedding rows, separated comparison rows, RegSeqRat readback, Real tolerance ledgers, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metric-completion-separable-core-carrier}, \autoref{thm:metric-completion-separable-core-namecert-obligations}, and \autoref{thm:metric-completion-separable-core-density} seed the finite separability-to-completion route from countable dense source data to completion-facing dense consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected limits, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense enumeration window replay, completion-entry routing, dense-image exactness, separated comparison stability, regular-rational readback, Real tolerance exactness, transport, replay, provenance, and local naming for BEDC.Derived.MetricCompletionSeparableCoreUp.}
\end{closurestatus}
