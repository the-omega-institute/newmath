\chapter{A Concrete Naming Certificate for $\LocatedCantorBendixsonUp$}
\label{ch:concrete-instances-located-cantor-bendixson-namecert}
\origin{human}

Following Cantor and Bendixson, the $\LocatedCantorBendixsonUp$ packet records the constructive derived-set route for a compact located real subset as finite derivative rows and perfect-kernel boundary data. It sits over the Cantor-set prefix surface of \autoref{ch:concrete-instances-cantorset-namecert}, the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the closed-set boundary of \autoref{ch:concrete-instances-closedset-namecert}, the finite intersection discipline of \autoref{ch:concrete-instances-finite-intersection-property-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The certificate names the located derivative search and residual perfect-kernel rows; it does not assert a host transfinite rank, a powerset decomposition, countable choice, or a classical isolated-point selector.

\begin{definition}[Located Cantor--Bendixson carrier]
\label{def:located-cantor-bendixson-carrier}
A $\LocatedCantorBendixsonUp$ carrier is a finite $\BHist$ packet
$$
L=(K,C,D,I,P,R,H,Q,S,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact located metric row, naming the compact metric presentation, its located distance read, and the finite cover budgets available to the derivative search.
\item $C$ is the closed real-set row, recording closedness tests through displayed complements and $\RealUp$ seals.
\item $D$ is the finite derived-set ledger, listing inspected neighborhoods, witnessed isolated exclusions, and the residual closed rows generated by the displayed search depth.
\item $I$ is the finite isolated-point ledger, carrying the local separation witness for each isolated read admitted by $D$.
\item $P$ is the residual perfect-kernel row, recording the no-isolated-point boundary for the remaining displayed closed subpacket.
\item $R$ is the $\RealUp$ membership and distance handoff row consumed only after $K,C,D,I,P$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact metric data, closed-set tests, derivative ledgers, isolated witnesses, residual rows, real handoff, continuation replay, provenance, and local naming.
\item $Q$ records displayed $\Cont$ replay from finite neighborhood requests to derivative rows and from residual rows to the perfect-kernel boundary.
\item $S$ is the $\Pkg$ provenance over $\LocatedCantorBendixsonUp$, $\CantorSetUp$, $\CompactMetricUp$, $\ClosedSetUp$, $\FiniteIntersectionPropertyUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCantorBendixsonUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for host ordinal rank, ambient powersets, a selected enumeration of all isolated points, quotient real equality, countable choice, or a classical Cantor--Bendixson theorem outside the displayed compact located route.
\end{definition}

\begin{theorem}[Located Cantor--Bendixson NameCert obligations]
\label{thm:located-cantor-bendixson-namecert-obligations}
For every accepted $\LocatedCantorBendixsonUp$ carrier $L=(K,C,D,I,P,R,H,Q,S,N)$, the local $\NameCert_{\LocatedCantorBendixsonUp}$ surface is exhausted by the following five obligation rows:
\begin{enumerate}
\item compact located admission through $K$, exposing only displayed finite cover and distance data;
\item closed real-set boundary through $C$, exposing only displayed complement and seal tests;
\item derived-set ledger exactness through $D$, relating every inspected neighborhood to the finite derivative row;
\item isolated-point separation through $I$, naming each admitted isolated read by a displayed local witness;
\item residual perfect-kernel stability through $P,R,H,Q,S,N$, keeping every surviving membership read inside the real handoff, transport, replay, provenance, and local naming route.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-cantor-bendixson-carrier}. The compact and closed inputs are rows $K$ and $C$, so the certificate starts from displayed located metric and closed-set data rather than from a host powerset.

The derivative work is finite at the named surface. Row $D$ records the inspected neighborhoods and residual closed rows, while $I$ stores only those isolated reads that come with displayed local separation evidence.

The residual row $P$ binds the remaining perfect-kernel boundary. The real handoff $R$, transport $H$, continuation replay $Q$, provenance $S$, and local naming row $N$ preserve the same route, so the five obligation rows exhaust the NameCert surface without importing an ordinal rank, a global isolated-point enumeration, quotient real equality, countable choice, or a classical decomposition theorem.
\end{proof}

\begin{theorem}[Located Cantor--Bendixson derivative stability]
\label{thm:located-cantor-bendixson-derivative-stability}
Let $L=(K,C,D,I,P,R,H,Q,S,N)$ be an accepted $\LocatedCantorBendixsonUp$ carrier. Every derivative read produced from the displayed compact located closed set factors through
$$
K\longmapsto C\longmapsto D\longmapsto I\longmapsto P\longmapsto R
$$
before it reaches transport, replay, provenance, and local naming rows. The residual closed packet is therefore stable under the displayed derived-set route, not under a host transfinite decomposition or a selected classical isolated-point enumeration.
\end{theorem}
\begin{proof}
Start with the compact located row $K$ and closed row $C$. Together they provide the only admissible neighborhoods, distance reads, finite cover budgets, complement tests, and real seals available to the derivative search.

Next read the derivative and isolated ledgers. The row $D$ records which neighborhoods have been inspected at the displayed depth, and $I$ attaches local separation evidence to the isolated reads admitted there. No row supplies a global enumeration of all isolated points.

Finally pass through the residual row and structural rows. The perfect-kernel boundary $P$ is consumed before the real handoff $R$, and $H,Q,S,N$ transport, replay, source, and name that same finite route. Hence derivative stability is a row-exhaustion statement for the displayed compact located closed packet.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedCantorBendixsonUp$ consumer that reads a derivative or residual perfect-kernel witness while omitting one of the compact located admission, closed real-set boundary, derivative ledger, isolated separation, residual kernel, Real handoff, hsame, Cont, Pkg, or NameCert rows has left the packet.}

\independenceWitness{cantorset, compactmetric, closedset, finiteintersectionproperty, real: $\LocatedCantorBendixsonUp$ is not bijective with any listed sibling because it joins compact located admission, closed real-set tests, finite derived-set rows, isolated separation, residual perfect-kernel stability, real handoff, transport, replay, provenance, and local naming as one finite packet.}

\closureat{LocatedCantorBendixsonUp}{seedStr}

\begin{closurestatus}{\LocatedCantorBendixsonUp}
  \constructivestory{A $\LocatedCantorBendixsonUp$ packet is generated from compact located metric data, closed real-set tests, finite derived-set ledgers, isolated-point separation witnesses, residual perfect-kernel rows, Real handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-cantor-bendixson-carrier}, \autoref{thm:located-cantor-bendixson-namecert-obligations}, and \autoref{thm:located-cantor-bendixson-derivative-stability} seed the constructive derived-set decomposition route for compact located closed real subsets.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host transfinite rank, ambient powerset decomposition, a selected enumeration of all isolated points, quotient real equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact located exactness, closed-set boundary exactness, derivative ledger exactness, isolated separation, residual perfect-kernel stability, route replay, provenance, local naming, and non-escape for BEDC.Derived.LocatedCantorBendixsonUp.}
\end{closurestatus}
