\chapter{A Concrete Naming Certificate for $\UniformLimitCauchyCriterionUp$}
\label{ch:concrete-instances-uniform-limit-cauchy-criterion-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\UniformLimitCauchyCriterionUp$ packet records the finite route by which a uniformly Cauchy family of real-valued names is admitted as a uniform-limit candidate before any endpoint, quotient function, selected limit, countable choice, or ambient completeness principle is available. It sits beside the finite family route of \autoref{ch:concrete-instances-real-function-uniform-cauchy-namecert}, the shared-tail criterion of \autoref{ch:concrete-instances-uniformcauchycriterion-namecert}, the uniform-limit endpoint of \autoref{ch:concrete-instances-uniformlimit-namecert}, and the continuity-facing handoff of \autoref{ch:concrete-instances-uniform-limit-continuous-namecert}. The chapter names only the Real-and-Completion corridor through displayed StreamName, RegSeqRat, Real, transport, replay, provenance, and local NameCert rows.

\begin{definition}[Uniform limit Cauchy criterion carrier]
\label{def:uniform-limit-cauchy-criterion-carrier}
A $\UniformLimitCauchyCriterionUp$ carrier is a finite $\BHist$ packet
$$
U=(F,W,M,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the function-family row, carrying the inspected family-window entries and the point requests to which the uniform criterion is applied.
\item $W$ is the finite family-window ledger, recording the shared windows for indices, source points, and comparison stages.
\item $M$ is the uniform Cauchy modulus row, requiring one displayed modulus request to govern every retained point window.
\item $S$ is the $\StreamNameUp$ observation row opened from the selected family windows before any regular rational readback is available.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same observations and dyadic tolerances.
\item $E$ is the terminal $\RealUp$ seal row, visible only after $F,W,M,S,R$ have been read.
\item $H$ records componentwise $\hsame$ transport for family windows, modulus rows, observations, regular readbacks, Real seals, replay rows, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a Real or Completion request through $M,W,S,R,E$.
\item $P$ is the $\Pkg$ provenance over $\UniformLimitCauchyCriterionUp$, $\UniformCauchyCriterionUp$, $\UniformLimitContinuousUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformLimitCauchyCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,W,M,S,R,E,H,C,P,N$. It has no coordinate for quotient function equality, pointwise-only convergence, selected family limits, countable choice, host completeness, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Uniform limit Cauchy criterion NameCert obligations]
\label{thm:uniform-limit-cauchy-criterion-namecert-obligations}
For every accepted $\UniformLimitCauchyCriterionUp$ carrier
$$
U=(F,W,M,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\UniformLimitCauchyCriterionUp}$ surface consists exactly of carrier admission for family-window rows, a shared uniform Cauchy modulus, $\StreamNameUp$ observations, $\RegSeqRatUp$ readbacks, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every Real or Completion consumer must read the uniform modulus $M$, then the displayed windows $W$, then the stream observations $S$ and regular readback $R$, before the Real seal $E$ is available.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-limit-cauchy-criterion-carrier}. Its rows list the only family-window, modulus, observation, readback, seal, transport, replay, provenance, and naming data admitted by the local classifier.

The uniformity obligation is the placement of $M$ before $W,S,R,E$. A consumer cannot replace it by a pointwise threshold, because the carrier stores one displayed modulus route for every retained family window.

The regular and Real-facing rows then follow the L10 route already used by $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. The structural rows $H,C,P,N$ transport, replay, source, and name that same finite route.

No excluded coordinate occurs in the packet. Thus quotient function equality, selected family limits, countable choice, host completeness, and ambient $\RealUp$ completeness cannot appear as local NameCert obligations.
\end{proof}

\begin{theorem}[Uniform limit Cauchy criterion L10 handoff]
\label{thm:uniform-limit-cauchy-criterion-l10-handoff}
Every endpoint-facing read of an accepted $\UniformLimitCauchyCriterionUp$ packet factors through the finite L10 route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
with the shared uniform Cauchy evidence supplied by $\UniformCauchyCriterionUp$ and the continuity-facing endpoint discipline compatible with $\UniformLimitContinuousUp$. In particular, the handoff exposes family-window rows, one uniform Cauchy modulus, stream observations, regular rational readbacks, Real seals, componentwise transport, replay, provenance, and local naming before any Real-and-Completion consumer may inspect the endpoint.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:uniform-limit-cauchy-criterion-namecert-obligations}. They force each endpoint-facing request to display the family window and the shared uniform modulus before the regular readback route is opened.

Read the stream and regular rows next. The $\StreamNameUp$ observations $S$ and $\RegSeqRatUp$ readback $R$ are tied to the same finite family-window ledger $W$ and the same modulus row $M$.

The terminal Real seal is downstream of that route. The $\UniformCauchyCriterionUp$ provenance supplies the shared-tail criterion, while $\UniformLimitContinuousUp$ may consume the resulting endpoint only as a displayed finite handoff.

Since $H,C,P,N$ preserve the same rows and add no external analytic object, the L10 handoff contains no quotient function equality, pointwise-only convergence witness, selected limit, countable choice, or ambient $\RealUp$ completeness datum.
\end{proof}

\closureat{UniformLimitCauchyCriterionUp}{seedStr}

\begin{closurestatus}{\UniformLimitCauchyCriterionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-limit-cauchy-criterion-carrier}, \autoref{thm:uniform-limit-cauchy-criterion-namecert-obligations}, and \autoref{thm:uniform-limit-cauchy-criterion-l10-handoff} seed the finite Real-and-Completion route for uniform Cauchy function-family convergence through displayed StreamName, RegSeqRat, Real, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient function equality, selected family limits, countable choice, pointwise-only convergence, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, uniform-modulus stability, StreamName observation routing, RegSeqRat readback, Real-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.UniformLimitCauchyCriterionUp.}
  \constructivestory{A $\UniformLimitCauchyCriterionUp$ packet is generated from family-window rows, one uniform Cauchy modulus, StreamName observations, RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
