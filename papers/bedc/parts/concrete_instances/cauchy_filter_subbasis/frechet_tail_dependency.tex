\paragraph{Frechet tail dependency.}
The $\FrechetFilterUp$ packet of \autoref{ch:concrete-instances-frechetfilter-namecert} supplies the cofinite-tail specialization of the subbasis-generated filter route. A $\CauchyFilterSubbasisUp$ read that is narrowed to eventual sequence tails therefore factors through the same finite threshold, filter-base, stream-schedule, regular-readback, and Real-seal rows exposed by $\FrechetFilterUp$, while the subbasis packet retains only the generated Cauchy-filter surface rather than quotient filter equality, selected limits, countable choice, or ambient completion.
