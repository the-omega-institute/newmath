\paragraph{Frechet tail dependency.}
The $\FrechetFilterUp$ packet of \autoref{ch:concrete-instances-frechetfilter-namecert} supplies the cofinite-tail specialization of the subbasis-generated filter route. The specialization is consumed through the displayed base discipline of \autoref{ch:concrete-instances-filterbase-namecert} before the generated route reaches \autoref{ch:concrete-instances-cauchyfilter-namecert} and the Real-facing seal.

A $\CauchyFilterSubbasisUp$ read that is narrowed to eventual sequence tails therefore factors through the same finite threshold, filter-base, stream-schedule, regular-readback, and Real-seal rows exposed by $\FrechetFilterUp$. This is a displayed sibling dependency in the tail-base factorization route, not a citation invented after the fact, and the subbasis packet retains only the generated Cauchy-filter surface rather than quotient filter equality, selected limits, countable choice, or ambient completion.
