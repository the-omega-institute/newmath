\chapter{A Concrete Naming Certificate for $\CompactUniformModulusLiftUp$}
\label{ch:concrete-instances-compact-uniform-modulus-lift-namecert}
\origin{human}

Following Bishop--Bridges and the Heine--Cantor theorem, the $\CompactUniformModulusLiftUp$ packet records the finite lift from pointwise continuity data on a compact metric source to a tolerance-only $\UniformModulusUp$ row. It realizes the compact finite-net, rational lower-bound fold, and metric-triangle handoff of \autoref{ch:visions-compact-uniform-continuity} as concrete NameCert data. The packet sits over $\CompactMetricUp$ in \autoref{ch:concrete-instances-compactmetric-namecert}, $\ContinuousMapUp$ in \autoref{ch:concrete-instances-continuousmap-namecert}, $\RatUp$ in \autoref{ch:concrete-instances-rat-namecert}, $\MetricSpaceUp$ in \autoref{ch:concrete-instances-metric-space-namecert}, the finite lower-bound fold of \autoref{ch:concrete-instances-finite-lower-bound-fold-namecert}, the triangle estimate of \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}, and the final $\UniformModulusUp$ surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}.

\begin{definition}[Compact uniform modulus lift carrier]
\label{def:compact-uniform-modulus-lift-carrier}
A $\CompactUniformModulusLiftUp$ carrier is a finite $\BHist$ packet
$$
U=(K,F,R,M,B,T,L,H,C,P,N).
$$
The rows have the following meanings:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row, exposing the displayed cover centers, cover radii, and inspected compact-source histories.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row for the displayed map on the compact source.
\item $R$ is the $\RatUp$ positive-radius ledger used by the lower-bound fold.
\item $M$ is the $\MetricSpaceUp$ source-and-target metric row used by the distance comparisons.
\item $B$ is the finite $\mathsf{ProbeBundle}$ lower-bound fold row, read through the finite lower-bound surface of \autoref{ch:concrete-instances-finite-lower-bound-fold-namecert}.
\item $T$ is the $\MetricTriangleUniformEstimateUp$ row that transports local pointwise bounds across the finite net by the metric triangle estimate.
\item $L$ is the tolerance-only $\UniformModulusUp$ handoff row obtained after $K,F,R,M,B,T$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact source, map, rational fold, metric, triangle, uniform-modulus, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusLiftUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RatUp$, $\MetricSpaceUp$, $\FiniteLowerBoundFoldUp$, $\MetricTriangleUniformEstimateUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformModulusLiftUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for open-cover compactness, selected subcovers outside the displayed finite net, host Heine--Cantor theorems, quotient point equality, external modulus selectors, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform modulus lift NameCert obligations]
\label{thm:compact-uniform-modulus-lift-namecert-obligations}
For every accepted $\CompactUniformModulusLiftUp$ carrier
$$
U=(K,F,R,M,B,T,L,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformModulusLiftUp}$ surface consists exactly of compact finite-net exposure, pointwise continuous-map modulus exposure, positive rational ledger admission, metric source-and-target exposure, finite $\mathsf{ProbeBundle}$ lower-bound folding, metric-triangle transport, tolerance-only $\UniformModulusUp$ handoff, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-lift-carrier}. The rows $K,F,R,M,B,T,L,H,C,P,N$ are the only rows available to the local certificate.

The compact and pointwise data are read before the global tolerance row. Row $K$ supplies the finite compact-net ledger, row $F$ supplies pointwise continuity data, row $R$ stores the positive rational candidates, and row $M$ fixes the metric comparisons used by the estimate.

The fold and triangle rows perform the handoff. Row $B$ computes the displayed finite lower bound over the $\mathsf{ProbeBundle}$ ledger, and row $T$ transports local bounds across net membership by the metric triangle estimate.

Finally $L,H,C,P,N$ expose the tolerance-only uniform modulus and preserve it under transport, replay, provenance, and local naming. Since no other coordinate is present, the packet cannot export open-cover compactness, external subcover selection, quotient point equality, a host Heine--Cantor theorem, or an ambient completeness principle.
\end{proof}

\begin{theorem}[Compact uniform modulus public handoff]
\label{thm:compact-uniform-modulus-lift-public-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactUniformModulusLiftUp$ packet receives a tolerance-only row $L$ only after the finite route
$$
\CompactMetricUp,\ContinuousMapUp,\RatUp,\MetricSpaceUp,\FiniteLowerBoundFoldUp,\MetricTriangleUniformEstimateUp,\UniformModulusUp,\hsame,\Cont,\Pkg,\NameCert
$$
has been displayed. The handoff records compact finite-net rows, pointwise-modulus rows, a rational lower-bound fold, and a metric-triangle estimate as NameCert data; it does not export an open-cover compactness theorem, a selected subcover oracle, quotient point equality, or an external modulus selector.
\end{theorem}
\begin{proof}
Start from an accepted carrier $U=(K,F,R,M,B,T,L,H,C,P,N)$. By \autoref{thm:compact-uniform-modulus-lift-namecert-obligations}, the compact finite net, pointwise map data, positive rational ledger, and metric rows are exposed before the fold and triangle rows can be read.

The row $B$ supplies the finite lower-bound fold over the displayed $\mathsf{ProbeBundle}$ data. The row $T$ then applies the metric triangle estimate to move from local pointwise bounds to the tolerance request consumed by $L$.

The structural rows $H,C,P,N$ preserve only that displayed route. Thus the $\UniformModulusUp$ consumer receives a finite tolerance-only modulus row, not open-cover compactness, a selected subcover, quotient point equality, a host Heine--Cantor theorem, or an external selector.
\end{proof}

\falsifiablePrediction{Every $\CompactUniformModulusLiftUp$ uniform-modulus read factors through compact finite-net rows, ContinuousMap pointwise-modulus rows, Rat positive-radius rows, MetricSpace distance rows, a finite ProbeBundle lower-bound fold, a metric-triangle estimate, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{compactmetric, continuousmap, rat, metric-space, finite-lower-bound-fold, metric-triangle-uniform-estimate, uniformmodulus: $\CompactUniformModulusLiftUp$ is not bijective to any listed sibling because it jointly carries compact finite-net data, pointwise modulus evidence, rational lower-bound folding, metric-triangle transport, and the tolerance-only UniformModulus handoff.}

\closureat{CompactUniformModulusLiftUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusLiftUp}
  \constructivestory{A $\CompactUniformModulusLiftUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise-modulus rows, Rat positive-radius rows, MetricSpace distance rows, a finite ProbeBundle lower-bound fold, a MetricTriangleUniformEstimate row, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-lift-carrier}, \autoref{thm:compact-uniform-modulus-lift-namecert-obligations}, and \autoref{thm:compact-uniform-modulus-lift-public-handoff} seed the compact finite-net route from pointwise moduli to a tolerance-only uniform modulus.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, selected subcovers outside the displayed net, host Heine--Cantor theorems, quotient point equality, external modulus selectors, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, pointwise-modulus exposure, rational lower-bound fold stability, metric-triangle transport, UniformModulus handoff, replay, provenance, and local naming for BEDC.Derived.CompactUniformModulusLiftUp.}
\end{closurestatus}
