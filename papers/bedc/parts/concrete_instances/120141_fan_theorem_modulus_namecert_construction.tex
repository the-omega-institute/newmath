\chapter{A Concrete Naming Certificate for $\FanTheoremModulusUp$}
\label{ch:concrete-instances-fan-theorem-modulus-namecert}
\origin{human}

Following Brouwer's fan theorem and Bishop--Bridges constructive uniform-continuity analysis, the $\FanTheoremModulusUp$ packet reads a displayed $\FanTheoremUp$ finite bound as a tolerance-only $\UniformModulusUp$ handoff. It sits after the finite fan certificate of \autoref{ch:concrete-instances-fantheorem-namecert}, the Bishop fan-modulus route of \autoref{ch:concrete-instances-bishop-fan-modulus-namecert}, the bar-modulus route of \autoref{ch:concrete-instances-fan-theorem-bar-modulus-namecert}, the compact-uniform fan packet of \autoref{ch:concrete-instances-compact-uniform-fan-modulus-namecert}, the downstream uniform-modulus surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records the theorem-level handoff from already displayed fan-bound data to a uniform modulus; it does not reprove the bar theorem, select an infinite branch, invoke open-cover compactness, choose a host modulus, quotient stream names, or assume ambient $\RealUp$ completeness.

\begin{definition}[Fan theorem modulus carrier]
\label{def:fan-theorem-modulus-carrier}
A $\FanTheoremModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(F,B,T,U,S,Q,D,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\FanTheoremUp$ finite-bound row, exposing a barred binary-spread depth already admitted by the fan-theorem packet.
\item $B$ is the $\BishopFanModulusUp$ schedule row, turning the displayed fan depth into finite observation windows for Real and Completion consumers.
\item $T$ is the $\FanTheoremBarModulusUp$ row, recording the bar-coverage-to-tolerance extraction already available at the bar-modulus surface.
\item $U$ is the $\UniformModulusUp$ handoff row, read only after $F,B,T$ have displayed the finite bound and its tolerance interpretation.
\item $S$ is the $\StreamNameUp$ finite-window row for the inspected prefixes and tolerance requests.
\item $Q$ is the $\RegSeqRatUp$ regular-rational readback row attached to the displayed windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare requested precision with the fan-bound schedule.
\item $R$ is the terminal $\RealUp$ seal reached after the stream, regular, and dyadic rows have been exposed.
\item $H$ records componentwise $\hsame$ transport for $F,B,T,U,S,Q,D,R$ and their structural support.
\item $C$ records displayed $\Cont$ replay from the fan-bound row through the Bishop schedule, bar-modulus row, tolerance ledger, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\FanTheoremModulusUp$, $\FanTheoremUp$, $\BishopFanModulusUp$, $\FanTheoremBarModulusUp$, $\CompactUniformFanModulusUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FanTheoremModulusUp}$ row.
\end{enumerate}
The classifier accepts only packets whose uniform-modulus read is routed through the displayed rows $F,B,T,U,S,Q,D,R,H,C,P,N$. It has no coordinate for a selected infinite branch, open-cover compactness theorem, host modulus function, quotient stream equality, countable-choice schedule, or ambient real completeness.
\end{definition}

\begin{theorem}[Fan theorem modulus NameCert obligations]
\label{thm:fan-theorem-modulus-namecert-obligations}
For every accepted $\FanTheoremModulusUp$ carrier $M=(F,B,T,U,S,Q,D,R,H,C,P,N)$, the local $\NameCert_{\FanTheoremModulusUp}$ surface consists exactly of carrier admission for the twelve displayed rows; fan-bound exposure through $F$; Bishop fan-modulus scheduling through $B$; bar-modulus extraction through $T$; tolerance-only uniform-modulus handoff through $U$; finite-window, regular-rational, dyadic-tolerance, and Real-seal readback through $S,Q,D,R$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-theorem-modulus-carrier}. Its fan and modulus rows are exactly $F,B,T,U$, its readback rows are exactly $S,Q,D,R$, and its structural rows are exactly $H,C,P,N$.

Read $F$ as the already displayed fan-theorem finite bound. The Bishop fan-modulus row $B$ turns that bound into finite observation windows, and the bar-modulus row $T$ records the tolerance extraction that is available before a uniform-modulus consumer reads $U$.

The rows $S,Q,D,R$ supply only finite windows, regular rational readback, dyadic tolerance comparison, and terminal Real sealing. The structural rows then transport, replay, source, and name the same packet. Since no additional coordinate appears in the carrier, the listed rows exhaust the local NameCert obligations and block branch selection, open-cover compactness, host modulus selection, quotient stream equality, countable choice, and ambient Real completeness.
\end{proof}

\begin{theorem}[Fan theorem modulus compact-uniform handoff]
\label{thm:fan-theorem-modulus-compact-uniform-handoff}
Every compact-uniform or Real-and-Completion consumer of an accepted $\FanTheoremModulusUp$ packet receives its uniform-modulus data through the finite route
$$
\FanTheoremUp
\longmapsto
\BishopFanModulusUp
\longmapsto
\FanTheoremBarModulusUp
\longmapsto
\UniformModulusUp
\longmapsto
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\DyadicRatCoreUp
\longmapsto
\RealUp .
$$
When the consumer is the compact-uniform fan route, the same handoff may be cited beside $\CompactUniformFanModulusUp$, but only as the displayed fan-bound-to-uniform-modulus package $F,B,T,U,S,Q,D,R,H,C,P,N$. It exports no open-cover compactness theorem, selected branch, selected cover center, external modulus oracle, quotient stream equality, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:fan-theorem-modulus-namecert-obligations}. They force every accepted read to expose the fan-bound row, Bishop schedule row, bar-modulus row, uniform-modulus handoff, readback rows, and structural rows in the displayed carrier.

The compact-uniform fan surface may consume this packet only after the fan-bound-to-uniform-modulus route has already been displayed. Thus $\CompactUniformFanModulusUp$ is a sibling consumer of the same finite handoff, not a source of an additional branch selector or host compactness object.

Finally inspect the excluded coordinates of \autoref{def:fan-theorem-modulus-carrier}. The carrier has no slot for branch selection, open-cover compactness, selected centers, external moduli, quotient stream equality, countable choice, or ambient Real completeness, so none of those objects can be exported by the handoff.
\end{proof}

\closureat{FanTheoremModulusUp}{seedStr}

\begin{closurestatus}{\FanTheoremModulusUp}
  \constructivestory{A $\FanTheoremModulusUp$ packet is generated from a FanTheoremUp finite-bound row, BishopFanModulusUp schedule row, FanTheoremBarModulusUp extraction row, UniformModulusUp handoff, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance comparison, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fan-theorem-modulus-carrier}, \autoref{thm:fan-theorem-modulus-namecert-obligations}, and \autoref{thm:fan-theorem-modulus-compact-uniform-handoff} seed the finite fan-bound-to-uniform-modulus handoff through fan, Bishop schedule, bar-modulus, uniform-modulus, stream, regular-rational, dyadic, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, open-cover compactness, branch selection, selected cover centers, external modulus oracles, quotient stream equality, countable choice, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, fan-bound exposure, Bishop schedule routing, bar-modulus extraction, UniformModulus handoff, finite-window readback, regular-rational readback, dyadic tolerance comparison, RealUp sealing, transport, replay, provenance, and local naming for BEDC.Derived.FanTheoremModulusUp.}
\end{closurestatus}
