\chapter{A Concrete Naming Certificate for $\RegularCauchyInterleaveUp$}
\label{ch:concrete-instances-regularcauchyinterleave-namecert}
\origin{human}

The $\RegularCauchyInterleaveUp$ interface names the finite merge schedule used when two regular Cauchy names are exposed through one visible stream of observations. The construction is not a quotient of completed real values and does not select a host limit; it records the rows that make every merged window, dyadic observation, modulus request, and real-name handoff inspectable.

\begin{definition}[Regular Cauchy interleave carrier]
\label{def:regular-cauchy-interleave-carrier}
A $\RegularCauchyInterleaveUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
K=(I,J,S,T,W,D,M,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $I$ and $J$ are the two $\RegSeqRatUp$ member schedules whose regular Cauchy observations may be read;
\item $S$ is the unary merge schedule naming, for each retained slot, whether the slot reads the $I$ side or the $J$ side;
\item $T$ is the interleaved $\StreamNameUp$ window table produced by applying $S$ to the two source schedules;
\item $W$ records the $\CauchyModulusUp$ precision requests attached to the selected windows;
\item $D$ records the $\DyadicRatCoreUp$ observation rows reached at the selected windows and precision requests;
\item $M$ records the $\RegSeqRatUp/\RealUp$ handoff rows by which the merged finite observations are exposed to real-name consumers;
\item $H$ displays the componentwise $\hsame$ transports for the two regular schedules, merge schedule, window table, modulus requests, dyadic observations, handoff rows, provenance, and local naming row;
\item $C$ displays the $\Cont$ routes for reading a selected side, forming the interleaved window, checking the modulus request, observing the dyadic row, and handing the result to $\RegSeqRatUp$ and $\RealUp$;
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchyInterleaveUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient equality of streams, host equality of completed reals, ambient completeness, or a choice-selected limit.
\end{definition}

\begin{definition}[Regular Cauchy interleave classifier]
\label{def:regular-cauchy-interleave-classifier}
Two accepted $\RegularCauchyInterleaveUp$ carriers are classified as the same finite merge packet exactly when all rows of \autoref{def:regular-cauchy-interleave-carrier} transport componentwise through the displayed $\hsame$ rows and all consumer reads factor through the displayed $\Cont$ routes. The classifier compares the two source $\RegSeqRatUp$ schedules, the unary merge schedule, the interleaved $\StreamNameUp$ table, the $\CauchyModulusUp$ request ledger, the $\DyadicRatCoreUp$ observation ledger, the $\RegSeqRatUp/\RealUp$ handoff rows, package provenance, and the local $\NameCert_{\RegularCauchyInterleaveUp}$ row.

The classifier has no generator for comparing completed streams or host real values. Equality is exhausted by the finite rows carried by the packet.
\end{definition}

\begin{theorem}[Regular Cauchy interleave NameCert obligations]
\label{thm:regular-cauchy-interleave-namecert-obligations}
The local $\NameCert_{\RegularCauchyInterleaveUp}$ obligation surface for the carrier of \autoref{def:regular-cauchy-interleave-carrier} consists of carrier admission, classifier transport, finite merge-window exactness, modulus-observation compatibility, and real-handoff boundary. Every row consumed by the certificate is one of
$$
I,J,S,T,W,D,M,H,C,P,N.
$$
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyInterleaveUp.RegularCauchyInterleavePacket\_semantic\_name\_certificate}
\begin{proof}
Carrier admission is the projection of the finite packet $K$. The rows $I$, $J$, $S$, $T$, $W$, $D$, and $M$ are stored coordinates, while $H$, $C$, $P$, and $N$ provide transport, continuation, package, and local naming evidence.

Classifier transport is exactly \autoref{def:regular-cauchy-interleave-classifier}. It moves source schedules, merge choices, window rows, precision requests, observations, and handoffs only through displayed $\hsame$ and $\Cont$ rows.

The merge-window and modulus-observation clauses are finite repackings. A selected slot of $S$ names one side, $T$ records the resulting window, $W$ records the requested precision, and $D$ records the dyadic observation at that request.

The real-handoff clause projects $M$. Since no carrier coordinate stores a quotient stream, host real equality, completeness proof, or selected limit, the certificate exports only the displayed finite handoff surface.
\end{proof}

\begin{theorem}[Regular Cauchy interleave carrier admission]
\label{thm:regular-cauchy-interleave-carrier-admission}
An accepted $\RegularCauchyInterleaveUp$ carrier is admitted exactly as the displayed finite $\mathsf{BHist}$ packet
$$
K=(I,J,S,T,W,D,M,H,C,P,N)
$$
of \autoref{def:regular-cauchy-interleave-carrier}. Carrier admission projects the two $\RegSeqRatUp$ schedules $I,J$, the merge schedule $S$, the interleaved $\StreamNameUp$ table $T$, the $\CauchyModulusUp$ request ledger $W$, the $\DyadicRatCoreUp$ observation ledger $D$, the $\RegSeqRatUp/\RealUp$ handoff rows $M$, and the rows $H,C,P,N$ for transport, continuation, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted packet by \autoref{def:regular-cauchy-interleave-carrier}. Each listed row is a coordinate of the same finite $\mathsf{BHist}$ object, so the admission witness is obtained by reading those coordinates and repacking them as the local $\NameCert_{\RegularCauchyInterleaveUp}$ carrier.

There is no further admission case to discharge. The carrier has no coordinate for quotient equality of streams, a host equality proof between completed names, ambient completeness, or a selected limit, so none of those objects can enter the admitted packet.
\end{proof}

\begin{theorem}[Regular Cauchy interleave classifier transport exactness]
\label{thm:regular-cauchy-interleave-classifier-transport-exactness}
Let $K$ and $K'$ be accepted $\RegularCauchyInterleaveUp$ carriers. If their rows $I,J,S,T,W,D,M,P,N$ are matched componentwise by the displayed $\hsame$ rows in $H$ and read through the displayed $\Cont$ routes in $C$, then the $\RegularCauchyInterleaveUp$ classifier transports exactly those rows and no other comparison data.
\end{theorem}
\begin{proof}
Use \autoref{def:regular-cauchy-interleave-classifier}. The classifier reads the two source schedules, the unary merge schedule, the interleaved window table, the modulus requests, the dyadic observations, the handoff rows, package provenance, and the local naming row.

The displayed $\hsame$ rows give the componentwise replacements, and the displayed $\Cont$ routes give the permitted consumer reads. Since the classifier has no generator for completed stream equality or host real equality, the transported comparison is exactly the one carried by $H$ and $C$ over the stored packet rows.
\end{proof}

\begin{theorem}[Regular Cauchy interleave dyadic observation handoff]
\label{thm:regular-cauchy-interleave-dyadic-observation-handoff}
For every accepted $\RegularCauchyInterleaveUp$ carrier $K=(I,J,S,T,W,D,M,H,C,P,N)$, each selected merge slot reaches its $\CauchyModulusUp$ request in $W$ and its $\DyadicRatCoreUp$ observation in $D$ before any $\RegSeqRatUp$ or $\RealUp$ handoff row in $M$ is consumed.
\end{theorem}
\begin{proof}
Start with a selected slot of the unary merge schedule $S$. The slot chooses one of the displayed source schedules $I$ or $J$ and then reads the corresponding finite $\StreamNameUp$ window row in $T$.

The same slot carries a precision request in $W$. Reading that request through the stored continuation route reaches the dyadic observation row in $D$, with the componentwise $\hsame$ rows preserving the selected side, window, request, and observation.

Only after these rows have been displayed does the handoff coordinate $M$ become available to $\RegSeqRatUp$ or $\RealUp$ consumers. Thus the handoff is a finite dyadic-observation route, not a shortcut through a quotient stream, completed real equality, completeness theorem, or selected limit.
\end{proof}

\begin{theorem}[Regular Cauchy interleave window merge exactness]
\label{thm:regular-cauchy-interleave-window-merge-exactness}
For every accepted $\RegularCauchyInterleaveUp$ carrier $K=(I,J,S,T,W,D,M,H,C,P,N)$, each retained row of the interleaved $\StreamNameUp$ window table $T$ is obtained by reading exactly one source schedule, selected by the corresponding unary merge row of $S$, and by pairing that selected window with the corresponding $\CauchyModulusUp$ request in $W$ and $\DyadicRatCoreUp$ observation in $D$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyInterleaveUp.RegularCauchyInterleaveWindowMergeExactness}
\begin{proof}
Project the selected slot of $S$. Its unary row has only the displayed left and right alternatives named by the carrier, so it chooses one of the two $\RegSeqRatUp$ member schedules $I$ and $J$.

Read the chosen schedule through the stored $\Cont$ route. The resulting finite window is the corresponding row of $T$, and the componentwise $\hsame$ rows in $H$ transport that row together with the chosen source schedule.

Next read the matching precision and observation rows. The ledger $W$ stores the $\CauchyModulusUp$ request for the same slot, and $D$ stores the $\DyadicRatCoreUp$ observation reached at that request.

Repacking these projections gives the asserted table row. No additional stream quotient or host equality comparison can alter the selected window, because the carrier contains no such coordinate.
\end{proof}

\begin{theorem}[Regular Cauchy interleave real handoff boundary]
\label{thm:regular-cauchy-interleave-real-handoff-boundary}
Every $\RegSeqRatUp$ or $\RealUp$ consumer read of a $\RegularCauchyInterleaveUp$ carrier factors through the finite merge schedule $S$, the interleaved window table $T$, the modulus request ledger $W$, the dyadic observation ledger $D$, and the handoff row $M$. The consumer cannot read a quotient of streams, a host equality proof between completed names, an ambient completeness theorem, or a choice-selected real value from the packet.
\end{theorem}
\begin{proof}
Start with a consumer read. The only real-facing coordinate of the carrier is $M$, and $M$ is stored together with the window, modulus, and dyadic observation rows that produce it.

Use \autoref{thm:regular-cauchy-interleave-window-merge-exactness} to identify the finite window consumed by the read. Each window row of $T$ is tied to one selected side of $S$, one precision request in $W$, and one observation row in $D$.

Use \autoref{def:regular-cauchy-interleave-classifier} for transport. It allows the consumer route to move these rows componentwise through $H$ and $C$, with the same package provenance $P$ and local naming row $N$.

Thus the read factors through the displayed finite packet. Since the carrier has no field for quotient streams, host equality of completed reals, completeness, or selected limits, none of those objects can be exported at the $\RegSeqRatUp/\RealUp$ boundary.
\end{proof}

\begin{definition}[Regular Cauchy interleave obligation surface]
\label{def:regular-cauchy-interleave-obligation-surface}
For a concrete packet
$$
K=(I,J,S,T,W,D,M,H,C,P,N)
$$
of \autoref{def:regular-cauchy-interleave-carrier}, the $\RegularCauchyInterleaveUp$ obligation surface consists exactly of carrier admission, classifier transport, finite merge-window exactness, modulus-observation compatibility, and the $\RegSeqRatUp/\RealUp$ handoff boundary. These obligations are read only from the source schedules $I,J$, unary merge schedule $S$, interleaved window table $T$, modulus request ledger $W$, dyadic observation ledger $D$, handoff rows $M$, transport rows $H$, continuation routes $C$, package row $P$, and local naming row $N$.
\end{definition}

\begin{theorem}[Regular Cauchy interleave carrier-classifier obligation package]
\label{thm:regular-cauchy-interleave-obligation-carrier-classifier-package}
For every accepted $\RegularCauchyInterleaveUp$ packet $K=(I,J,S,T,W,D,M,H,C,P,N)$, the carrier-admission and classifier-transport obligations of \autoref{def:regular-cauchy-interleave-obligation-surface} are exactly the concrete carrier and classifier obligations of the chapter.
\end{theorem}
\begin{proof}
Read the packet through \autoref{def:regular-cauchy-interleave-carrier}. This exposes precisely the rows $I,J,S,T,W,D,M,H,C,P,N$ and no additional analytic coordinate.

The carrier side is the admission statement of \autoref{thm:regular-cauchy-interleave-carrier-admission}. The classifier side is the componentwise transport described by \autoref{def:regular-cauchy-interleave-classifier} and proved in \autoref{thm:regular-cauchy-interleave-classifier-transport-exactness}.

Together these references give the carrier and classifier part of the obligation surface, with the same package row and local naming row retained for downstream reads.
\end{proof}

\begin{theorem}[Regular Cauchy interleave window-modulus obligation package]
\label{thm:regular-cauchy-interleave-obligation-window-modulus-package}
For every accepted $\RegularCauchyInterleaveUp$ packet $K=(I,J,S,T,W,D,M,H,C,P,N)$, the finite merge-window and modulus-observation obligations of \autoref{def:regular-cauchy-interleave-obligation-surface} are witnessed by the displayed rows $S,T,W,D,M,H,C,P,N$.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-interleave-window-merge-exactness}. It ties each selected merge slot in $S$ to the window table $T$, the request ledger $W$, the dyadic observation row $D$, and the handoff row $M$ before any consumer read is made.

Then apply \autoref{thm:regular-cauchy-interleave-dyadic-observation-handoff}. It keeps the modulus request and dyadic observation in the same finite read path under the displayed transport and continuation rows.

Thus the window and modulus obligations are fulfilled inside the concrete packet, without adding quotient streams, host real equality, or selected limits.
\end{proof}

\begin{theorem}[Regular Cauchy interleave real-handoff obligation package]
\label{thm:regular-cauchy-interleave-obligation-real-handoff-package}
For every accepted $\RegularCauchyInterleaveUp$ packet $K=(I,J,S,T,W,D,M,H,C,P,N)$, the $\RegSeqRatUp/\RealUp$ handoff obligation of \autoref{def:regular-cauchy-interleave-obligation-surface} is exactly the boundary route through $S,T,W,D,M,H,C,P,N$.
\end{theorem}
\begin{proof}
Use \autoref{thm:regular-cauchy-interleave-real-handoff-boundary}. It states that every regular-rational or real-facing consumer read factors through the finite merge schedule, interleaved window table, modulus request ledger, dyadic observation ledger, and handoff row.

The remaining rows $H,C,P,N$ preserve the same route as transport, continuation, package provenance, and local naming. Therefore the real-handoff obligation is a finite BEDC packet obligation, not an appeal to completion, quotient streams, or host equality.
\end{proof}

\closureat{RegularCauchyInterleaveUp}{seedStr}

\begin{closurestatus}{\RegularCauchyInterleaveUp}
  \constructivestory{A $\RegularCauchyInterleaveUp$ packet is generated from two $\RegSeqRatUp$ member schedules, a unary merge schedule, an interleaved $\StreamNameUp$ window table, $\CauchyModulusUp$ requests, $\DyadicRatCoreUp$ observations, $\RegSeqRatUp/\RealUp$ handoff rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope records the finite carrier, classifier, merge-window exactness, and real-handoff boundary for regular Cauchy interleaving over the displayed packet rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient equality of streams, host equality of completed real names, ambient completeness, uniqueness of a merged limit, or bridge checking.}
  \upgradepath{Obligation closure requires binding carrier admission, classifier transport, merge-window exactness, modulus-observation compatibility, and $\RegSeqRatUp/\RealUp$ handoff boundary to the displayed finite packet.}
\end{closurestatus}
