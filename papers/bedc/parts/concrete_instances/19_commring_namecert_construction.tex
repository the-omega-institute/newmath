\input{parts/concrete_instances/commring/19_commring_core}
\input{parts/concrete_instances/commring/19_commring_algebraic_consequences}
\input{parts/concrete_instances/commring/19_commring_quadratic_polarization_package}

\begin{theorem}[CommRing zero linear factor annihilator branch]
\label{thm:commring-zero-linear-factor-annihilator-branch}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
left distributivity, the $\RingUp$ negative-product laws, and the
$\CommRingUp$ multiplicative commutativity field used in
\autoref{thm:commring-difference-of-squares-zero-exact}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\bigl((a+b)\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+b)\cdot c\hsame0 \land c\cdot(a+b)\hsame0)\bigr)
\lor
\bigl((a+(-b))\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+(-b))\cdot c\hsame0 \land c\cdot(a+(-b))\hsame0)\bigr).
$$
\end{theorem}
\begin{proof}
First derive the right distributive law from the supplied left distributive law
by \autoref{thm:commring-right-distrib-from-left}. The zero-classifier factor
absorption theorem is then available with both distributive inputs.

If the displayed disjunction chooses $(a+b)\hsame0$, retain that branch witness.
For an arbitrary $c:\mathsf{BHist}$, apply
\autoref{thm:ring-zero-classifier-factor-absorption} with $x=a+b$ and $y=c$.
Its two components give $(a+b)\cdot c\hsame0$ and $c\cdot(a+b)\hsame0$, so the
left disjunct packages the original witness with the universal annihilator row.

If the displayed disjunction chooses $(a+(-b))\hsame0$, retain that branch
witness instead. For an arbitrary $c:\mathsf{BHist}$, apply
\autoref{thm:ring-zero-classifier-factor-absorption} with $x=a+(-b)$ and $y=c$.
The two components give $(a+(-b))\cdot c\hsame0$ and
$c\cdot(a+(-b))\hsame0$, yielding the right disjunct with the same
branch-preserving form.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_zero\_linear\_factor\_annihilator\_branch}

\begin{theorem}[CommRing zero linear factor signed product zero]
\label{thm:commring-zero-linear-factor-signed-product-zero}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
left distributivity, the $\RingUp$ negative-product laws, and the
$\CommRingUp$ multiplicative commutativity field used in
\autoref{thm:commring-difference-of-squares-zero-exact}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
(a+b)\cdot(a+(-b))\hsame0 .
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:commring-zero-linear-factor-annihilator-branch} to the chosen
zero linear factor. The result keeps the selected branch and supplies its
two-sided annihilator row for every multiplier.

In the $(a+b)\hsame0$ branch, specialize the universal row at
$c=a+(-b)$ and take its left component. This is exactly
$(a+b)\cdot(a+(-b))\hsame0$.

In the $(a+(-b))\hsame0$ branch, specialize the universal row at $c=a+b$ and
take its right component. The oriented product is
$(a+b)\cdot(a+(-b))$, giving the required zero classifier.
\end{proof}

\begin{theorem}[CommRing zero linear factor commuted signed product zero]
\label{thm:commring-zero-linear-factor-commuted-signed-product-zero}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
left distributivity, the $\RingUp$ negative-product laws, and the
$\CommRingUp$ multiplicative commutativity field used in
\autoref{thm:commring-difference-of-squares-zero-exact}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
(a+(-b))\cdot(a+b)\hsame0 .
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:commring-zero-linear-factor-annihilator-branch} to the
selected zero linear factor. In the $(a+b)\hsame0$ branch, specialize the
two-sided annihilator row at $c=a+(-b)$ and take the right multiplication
component. In the $(a+(-b))\hsame0$ branch, specialize at $c=a+b$ and take the
left multiplication component. Both cases produce the displayed commuted
signed product as an empty classifier.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_zero\_linear\_factor\_commuted\_signed\_product\_zero}

\begin{theorem}[CommRing zero linear factor two-sided signed product zero]
\label{thm:commring-zero-linear-factor-two-sided-signed-product-zero}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
left distributivity, the $\RingUp$ negative-product laws, and the
$\CommRingUp$ multiplicative commutativity field used in
\autoref{thm:commring-difference-of-squares-zero-exact}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\bigl((a+b)\cdot(a+(-b))\hsame0\bigr)
\land
\bigl((a+(-b))\cdot(a+b)\hsame0\bigr).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:commring-zero-linear-factor-signed-product-zero} to the
chosen zero linear factor. This gives the forward signed product
$(a+b)\cdot(a+(-b))\hsame0$.

Apply \autoref{thm:commring-zero-linear-factor-commuted-signed-product-zero} to
the same branch witness. This gives the commuted signed product
$(a+(-b))\cdot(a+b)\hsame0$.

Introduce the conjunction with these two oriented zero-product witnesses as its
left and right components.
\end{proof}

\begin{theorem}[CommRing zero linear factor signed product annihilator package]
\label{thm:commring-zero-linear-factor-signed-product-annihilator-package}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the same
hypotheses as \autoref{thm:commring-zero-linear-factor-exact-package}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\left(
(a+b)\cdot(a+(-b))\hsame0
\land
\forall c : \mathsf{BHist},\
\left(
((a+b)\cdot(a+(-b)))\cdot c\hsame0
\land
c\cdot((a+b)\cdot(a+(-b)))\hsame0
\right)
\right).
$$
\end{theorem}
\begin{proof}
Let $H$ be the displayed zero-linear-factor hypothesis. Apply
\autoref{thm:commring-zero-linear-factor-signed-product-zero} to $H$ to obtain
the signed product-zero witness
$(a+b)\cdot(a+(-b))\hsame0$.

Use \autoref{thm:commring-right-distrib-from-left} to recover the right
distributive row from the supplied left distributive row. Together with the
$\RingUp$ zero-factor absorption theorem, this gives the same two-sided
absorption mechanism used in
\autoref{thm:commring-zero-linear-factor-annihilator-branch}.

Now instantiate that absorption mechanism with
$x=(a+b)\cdot(a+(-b))$. For an arbitrary $c:\mathsf{BHist}$, the product-zero
witness gives both $((a+b)\cdot(a+(-b)))\cdot c\hsame0$ and
$c\cdot((a+b)\cdot(a+(-b)))\hsame0$. Pair this universal row with the original
signed product-zero witness to obtain the displayed package.
\end{proof}

\begin{theorem}[CommRing equal-squares signed-product annihilator]
\label{thm:commring-equal-squares-signed-product-annihilator}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
hypotheses of \autoref{thm:commring-difference-of-squares-zero-exact} and the
zero-factor absorption hypotheses of
\autoref{thm:ring-zero-classifier-factor-absorption}, every
$a,b : \mathsf{BHist}$ satisfies
$$
a\cdot a\hsame b\cdot b
\Longrightarrow
\left(
(a+b)\cdot(a+(-b))\hsame0
\land
\forall c : \mathsf{BHist},\
\left(
((a+b)\cdot(a+(-b)))\cdot c\hsame0
\land
c\cdot((a+b)\cdot(a+(-b)))\hsame0
\right)
\right).
$$
\end{theorem}
\begin{proof}
Let $Q$ be the square-equality hypothesis $a\cdot a\hsame b\cdot b$. Apply the
reverse direction of
\autoref{thm:commring-difference-of-squares-zero-exact} to $Q$. This produces
the signed product-zero witness $(a+b)\cdot(a+(-b))\hsame0$.

The zero-factor absorption hypotheses give
\autoref{thm:ring-zero-classifier-factor-absorption} for the signed product
$x=(a+b)\cdot(a+(-b))$. For an arbitrary $c:\mathsf{BHist}$, instantiate that
theorem at $y=c$.

The two components are $((a+b)\cdot(a+(-b)))\cdot c\hsame0$ and
$c\cdot((a+b)\cdot(a+(-b)))\hsame0$. Pair this universal two-sided absorption
row with the signed product-zero witness from the equal-squares route.
\end{proof}

The ten-field fit is: traditional target equal squares yields the signed-product
annihilator package in a commutative ring; BEDC source $\RingUp$ plus
$\CommRingUp$ over $\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier
$\hsame$; stability from difference-of-squares exactness and zero-factor
absorption; ledger the square-equality witness, signed product-zero witness, and
universal two-sided multiplier row; core theorem this block; exactness the same
annihilator certificate reached from square equality; downstream consumer
$\FieldUp$ quadratic and zero-factor exit packages; status
$\mathsf{ProofTarget}$.

The ten-field fit is: traditional target signed-product annihilator package in
a commutative ring; BEDC source $\RingUp$ plus $\CommRingUp$ over
$\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier $\hsame$; stability from
right distributivity and zero-factor absorption; ledger the selected zero
linear factor, signed product-zero witness, and universal two-sided multiplier
row; core theorem this block; exactness the paired product-zero and
signed-product annihilator certificate; downstream consumer $\FieldUp$
quadratic and zero-factor exit packages; status $\mathsf{ProofTarget}$.

The ten-field fit is: traditional target branch-preserving zero-product
certificate for both signed orientations; BEDC source $\RingUp$ plus
$\CommRingUp$ over $\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier
$\hsame$; stability from the two signed-product zero theorems; ledger the
chosen zero linear factor and the two oriented zero-product witnesses; core
theorem this block; exactness two product orientations at once; standard bridge
ordinary commutative-ring factor cancellation packaging; status
$\mathsf{ProofTarget}$ for downstream consumer $\FieldUp$ quadratic and
zero-factor exit packages.

\begin{theorem}[CommRing zero linear factor forces equal squares]
\label{thm:commring-zero-linear-factor-equal-squares}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
left distributivity, the $\RingUp$ negative-product laws, and the
$\CommRingUp$ multiplicative commutativity field used in
\autoref{thm:commring-difference-of-squares-zero-exact}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
a\cdot a\hsame b\cdot b .
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:commring-zero-linear-factor-signed-product-zero} to the
chosen zero linear factor. This produces
$(a+b)\cdot(a+(-b))\hsame0$.

The forward direction of
\autoref{thm:commring-difference-of-squares-zero-exact} sends this signed
product-zero witness to $a\cdot a\hsame b\cdot b$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_zero\_linear\_factor\_equal\_squares}
\leanvariant{BEDC.Derived.CommRingUp.commring\_empty\_linear\_factor\_equal\_squares}

The ten-field fit is: traditional target zero linear factor implies equal
squares in a commutative ring; BEDC source $\RingUp$ plus $\CommRingUp$ over
$\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier $\hsame$; stability
from zero-factor absorption and difference-of-squares exactness; ledger the
chosen zero factor and product-zero witness; core theorem this block; exactness
equality of squares; standard bridge ordinary commutative-ring factor argument;
status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing zero linear factor exact package]
\label{thm:commring-zero-linear-factor-exact-package}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
left distributivity, the $\RingUp$ negative-product laws, and the
$\CommRingUp$ multiplicative commutativity field used in
\autoref{thm:commring-difference-of-squares-zero-exact}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\bigl((a+b)\cdot(a+(-b))\hsame0 \land a\cdot a\hsame b\cdot b\bigr).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:commring-zero-linear-factor-signed-product-zero} to the
chosen zero linear factor. This gives the signed product-zero witness
$(a+b)\cdot(a+(-b))\hsame0$.

Feed that witness through the forward direction of
\autoref{thm:commring-difference-of-squares-zero-exact}. The resulting
square equality is $a\cdot a\hsame b\cdot b$, and pairing it with the
signed product-zero witness gives the displayed conjunction.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_zero\_linear\_factor\_exact\_package}

The ten-field fit is: traditional target zero linear factor exact package in a
commutative ring; BEDC source $\RingUp$ plus $\CommRingUp$ over
$\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier $\hsame$; stability
from zero-factor absorption and difference-of-squares exactness; ledger the
chosen linear factor and signed-product witness; core theorem this block;
exactness the paired conjunction of product zero and equal squares; standard
bridge ordinary commutative-ring square exit; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing zero linear factor total exact package]
\label{thm:commring-zero-linear-factor-total-exact-package}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
left distributivity, the $\RingUp$ negative-product laws, and the
$\CommRingUp$ multiplicative commutativity field used in
\autoref{thm:commring-difference-of-squares-zero-exact}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\Bigl(
\bigl((a+b)\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+b)\cdot c\hsame0 \land c\cdot(a+b)\hsame0)\bigr)
\lor
\bigl((a+(-b))\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+(-b))\cdot c\hsame0 \land c\cdot(a+(-b))\hsame0)\bigr)
\Bigr)
\land
\bigl((a+b)\cdot(a+(-b))\hsame0 \land a\cdot a\hsame b\cdot b\bigr).
$$
\end{theorem}
\begin{proof}
Let $H$ be the displayed zero-linear-factor assumption. Apply
\autoref{thm:commring-zero-linear-factor-annihilator-branch} to $H$ to obtain
the branch-preserving annihilator disjunction.

Apply \autoref{thm:commring-zero-linear-factor-exact-package} to the same
assumption $H$. This yields the paired signed product-zero witness and square
equality.

Introduce the final conjunction by placing the branch-preserving disjunction in
the first component and the paired product-zero and square-equality certificate
in the second component.
\end{proof}

The ten-field fit is: traditional target zero linear factor exact package in a
commutative ring; BEDC source $\RingUp$ plus $\CommRingUp$ over
$\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier $\hsame$; stability
from zero-factor absorption and difference-of-squares exactness; ledger the
chosen linear factor, branch annihilator row, and signed-product witness; core
theorem \autoref{thm:commring-zero-linear-factor-total-exact-package};
exactness branch annihilator coverage together with signed product zero and
equal squares; standard bridge ordinary commutative-ring square exit; status
$\mathsf{ProofTarget}$.

\begin{theorem}[CommRing zero linear factor branch exact package]
\label{thm:commring-zero-linear-factor-branch-exact-package}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the same
hypotheses as \autoref{thm:commring-zero-linear-factor-exact-package}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\left(
\left(
\left((a+b)\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+b)\cdot c\hsame0 \land c\cdot(a+b)\hsame0)\right)
\lor
\left((a+(-b))\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+(-b))\cdot c\hsame0 \land c\cdot(a+(-b))\hsame0)\right)
\right)
\land
a\cdot a\hsame b\cdot b
\right).
$$
\end{theorem}
\begin{proof}
Start with the displayed zero-linear-factor disjunction. Apply
\autoref{thm:commring-zero-linear-factor-annihilator-branch} to retain the
selected branch and its two-sided annihilator row for every
$c:\mathsf{BHist}$.

Apply \autoref{thm:commring-zero-linear-factor-equal-squares} to the same
disjunction. This gives the square equality $a\cdot a\hsame b\cdot b$ under
the identical hypotheses.

Pair the branch-preserving annihilator package with the square equality. The
result is exactly the displayed conjunction, so later selector arguments can
consume the chosen linear factor without reconstructing the branch.
\end{proof}

The ten-field fit is: traditional target branch-preserving zero-factor
exactness in a commutative ring; BEDC source $\RingUp$ plus $\CommRingUp$ over
$\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier $\hsame$; stability from
zero-factor absorption and difference-of-squares exactness; ledger the selected
linear factor and annihilator row; core theorem this block; exactness the
displayed conjunction; downstream consumer $\FieldUp$ quadratic and zero-factor
exit packages; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing zero linear factor bilateral exact package]
\label{thm:commring-zero-linear-factor-bilateral-exact-package}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the same
hypotheses as \autoref{thm:commring-zero-linear-factor-exact-package}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\left(
\left(
\left((a+b)\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+b)\cdot c\hsame0 \land c\cdot(a+b)\hsame0)\right)
\lor
\left((a+(-b))\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+(-b))\cdot c\hsame0 \land c\cdot(a+(-b))\hsame0)\right)
\right)
\land
(a+b)\cdot(a+(-b))\hsame0
\land
(a+(-b))\cdot(a+b)\hsame0
\land
a\cdot a\hsame b\cdot b
\right).
$$
\end{theorem}
\begin{proof}
Let $H$ be the displayed zero-linear-factor hypothesis. Apply
\autoref{thm:commring-zero-linear-factor-branch-exact-package} to $H$ to obtain
the branch-preserving annihilator disjunction together with the square equality
$a\cdot a\hsame b\cdot b$.

Apply \autoref{thm:commring-zero-linear-factor-signed-product-zero} to $H$.
This yields the forward signed product
$(a+b)\cdot(a+(-b))\hsame0$.

Apply \autoref{thm:commring-zero-linear-factor-commuted-signed-product-zero} to
$H$. This yields the reversed signed product
$(a+(-b))\cdot(a+b)\hsame0$.

Introduce the final conjunction by placing the branch annihilator package first,
the forward signed product second, the reversed signed product third, and the
square equality fourth. The result is the displayed bilateral certificate.
\end{proof}

The ten-field fit is: traditional target zero linear factor bilateral exact
package in a commutative ring; BEDC source $\RingUp$ plus $\CommRingUp$ over
$\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier $\hsame$; stability from
zero-factor absorption and difference-of-squares exactness; ledger the selected
linear factor, branch annihilator row, both signed product orientations, and
square equality; core theorem this block; exactness the displayed four-component
certificate; downstream consumer $\FieldUp$ quadratic and zero-factor exit
packages; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing quadratic zero-linear bridge package]
\label{thm:commring-quadratic-zero-linear-bridge-package}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
shared hypotheses of \autoref{thm:commring-quadratic-polarization-package} and
\autoref{thm:commring-zero-linear-factor-bilateral-exact-package}, every
$a,b : \mathsf{BHist}$ satisfies
$$
\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
\Longrightarrow
\left(
\left(
\Bigl(
((a+b)\cdot(a+b))+((a+(-b))\cdot(a+(-b)))\hsame
(a\cdot a+a\cdot a)+(b\cdot b+b\cdot b)
\Bigr)
\land
\Bigl(
((a+b)\cdot(a+b))+((-(a\cdot a))+(-(b\cdot b)))\hsame
(a\cdot b)+(a\cdot b)
\Bigr)
\land
\Bigl(
((a+(-b))\cdot(a+(-b)))+((-(a\cdot a))+(-(b\cdot b)))\hsame
(-(a\cdot b))+(-(a\cdot b))
\Bigr)
\land
\Bigl(
(a+(-b))\cdot(a+(-b))\hsame0
\Longleftrightarrow
a\cdot a+b\cdot b\hsame a\cdot b+a\cdot b
\Bigr)
\right)
\land
\left(
\left(
\left((a+b)\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+b)\cdot c\hsame0 \land c\cdot(a+b)\hsame0)\right)
\lor
\left((a+(-b))\hsame0 \land
\forall c : \mathsf{BHist},\ ((a+(-b))\cdot c\hsame0 \land c\cdot(a+(-b))\hsame0)\right)
\right)
\land
(a+b)\cdot(a+(-b))\hsame0
\land
(a+(-b))\cdot(a+b)\hsame0
\land
a\cdot a\hsame b\cdot b
\right)
\right).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:commring-quadratic-polarization-package} to the shared
hypotheses and the selected histories $a,b$. This produces the full
quadratic-polarization package in the displayed order.

Let $H$ be the zero-linear-factor branch witness. Apply
\autoref{thm:commring-zero-linear-factor-bilateral-exact-package} to $H$ under
the same operation hypotheses. This produces the full bilateral certificate:
the selected branch with its two-sided annihilator row, both signed product
orientations, and the square equality.

Introduce the final conjunction with the quadratic-polarization package as the
first component and the bilateral zero-linear-factor certificate as the second
component. The branch witness is consumed only by the bilateral component, so
the resulting certificate remains over the same concrete $\mathsf{BHist}$ source.
\end{proof}

The ten-field fit is: traditional target a commutative-ring package combining
quadratic polarization with zero-linear-factor exactness; BEDC source
$\RingUp$ plus $\CommRingUp$ over $\mathsf{BHist}$; carrier $\mathsf{BHist}$;
classifier $\hsame$; stability from the shared quadratic and zero-factor
operation laws; ledger the polarization identities, exact signed-square test,
selected linear factor, two-sided annihilator row, bilateral signed products,
and square equality; core theorem this block; exactness the displayed combined
certificate; downstream use by $\FieldUp$ quadratic normalization and
zero-factor exit packages; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing singleton empty square exact package]
\label{thm:commring-singleton-empty-square-exact-package}
For the singleton empty-history CommRing tuple of
Definition~\ref{def:singleton-empty-history-commring-instance}, every pair
$a,b:\mathsf{BHist}$ carried by $A_{\emp}$ satisfies
$$
\bigl((a+b)\cdot(a+(-b))\hsame0\bigr)\land
\bigl(a\cdot a\hsame b\cdot b\bigr),
$$
where $+$, $\cdot$, $-$, and $0$ are the displayed singleton operations.
\end{theorem}
\begin{proof}
Theorem~\ref{thm:singleton-empty-history-commring-laws} supplies the concrete
singleton $\CommRingUp$ laws for the operation tuple of
Definition~\ref{def:singleton-empty-history-commring-instance}. Since the
displayed addition and zero operations are constant at $\emp$, the carried
inputs identify the sum endpoint with the zero endpoint, giving
$(a+b)\hsame0$.

Choose the left branch of the zero-linear-factor disjunction:
$$
(a+b)\hsame0 \lor (a+(-b))\hsame0 .
$$
The same singleton law package supplies the additive, congruence,
distributive, negative-product, and multiplicative-commutativity fields needed
to instantiate Theorem~\ref{thm:commring-zero-linear-factor-exact-package}.

Applying that theorem to the selected left branch yields the signed product
zero witness together with the square equality. These are exactly
$((a+b)\cdot(a+(-b))\hsame0)$ and $(a\cdot a\hsame b\cdot b)$ for the
singleton empty-history CommRing tuple.
\end{proof}

The ten-field fit is: traditional target one-element commutative-ring square
exactness; BEDC source singleton empty-history $\CommRingUp$; carrier
$A_{\emp}$; classifier $\rho_{\emp}$; stability from singleton CommRing laws
plus the zero-linear-factor exact package; ledger the selected empty linear
factor and signed-product witness; core theorem this block; exactness the
paired product-zero and equal-square conclusion; standard bridge the
one-element commutative ring; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing singleton empty signed product annihilator package]
\label{thm:commring-singleton-empty-signed-product-annihilator-package}
For the singleton empty-history CommRing tuple of
Definition~\ref{def:singleton-empty-history-commring-instance}, every pair
$a,b:\mathsf{BHist}$ carried by $A_{\emp}$ satisfies
$$
\bigl((a+b)\cdot(a+(-b))\hsame0\bigr)\land
\forall c:\mathsf{BHist},\
\left(
(((a+b)\cdot(a+(-b)))\cdot c\hsame0)
\land
(c\cdot((a+b)\cdot(a+(-b)))\hsame0)
\right),
$$
where $+$, $\cdot$, $-$, and $0$ are the displayed singleton operations.
\end{theorem}
\begin{proof}
Theorem~\ref{thm:singleton-empty-history-commring-laws} supplies the concrete
singleton $\CommRingUp$ laws for the operation tuple of
Definition~\ref{def:singleton-empty-history-commring-instance}. Because the
displayed addition and zero operations are constant at $\emp$, carriedness of
$a$ and $b$ identifies the sum endpoint with the zero endpoint, giving
$(a+b)\hsame0$.

Choose the left branch of the zero-linear-factor disjunction:
$$
(a+b)\hsame0 \lor (a+(-b))\hsame0 .
$$
The same singleton law package supplies the additive, congruence,
distributive, negative-product, and multiplicative-commutativity fields needed
to instantiate
Theorem~\ref{thm:commring-zero-linear-factor-signed-product-annihilator-package}.

That theorem yields the signed product-zero witness together with the universal
two-sided annihilator row for the signed product. Unpacking those two components
gives exactly $((a+b)\cdot(a+(-b))\hsame0)$ and, for every
$c:\mathsf{BHist}$, both $(((a+b)\cdot(a+(-b)))\cdot c\hsame0)$ and
$(c\cdot((a+b)\cdot(a+(-b)))\hsame0)$ for the singleton empty-history CommRing
tuple.
\end{proof}

The ten-field fit is: traditional target one-element commutative-ring
signed-product annihilator; BEDC source singleton empty-history $\CommRingUp$;
carrier $A_{\emp}$; classifier $\rho_{\emp}$; stability from singleton CommRing
laws plus zero-factor absorption; ledger selected empty linear factor and
signed-product annihilator row; core theorem this block; exactness product-zero
plus universal two-sided annihilation; standard bridge one-element commutative
ring; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing singleton empty square and signed-product annihilator package]
\label{thm:commring-singleton-empty-square-signed-product-package}
For the singleton empty-history CommRing tuple of
Definition~\ref{def:singleton-empty-history-commring-instance}, every pair
$a,b:\mathsf{BHist}$ carried by $A_{\emp}$ satisfies
$$
\bigl(a\cdot a\hsame b\cdot b\bigr)
\land
\bigl((a+b)\cdot(a+(-b))\hsame0\bigr)
\land
\forall c:\mathsf{BHist},\
\left(
(((a+b)\cdot(a+(-b)))\cdot c\hsame0)
\land
(c\cdot((a+b)\cdot(a+(-b)))\hsame0)
\right),
$$
where $+$, $\cdot$, $-$, and $0$ are the displayed singleton operations.
\end{theorem}
\begin{proof}
Apply
Theorem~\ref{thm:commring-singleton-empty-square-exact-package} to the selected
histories $a,b$. This yields the signed-product zero row together with the
square-equality row for the singleton empty-history CommRing tuple.

Apply
Theorem~\ref{thm:commring-singleton-empty-signed-product-annihilator-package} to
the same selected histories. This yields the same signed-product zero row and
the universal two-sided annihilator row for that signed product.

Introduce the displayed triple conjunction. Use the square row from the
square-exact package as the first component, use either signed-product zero row
as the second component, and use the universal annihilator row from the
signed-product annihilator package as the final component.
\end{proof}

The ten-field fit is: traditional target one-element commutative-ring package
combining singleton square exactness with signed-product annihilation; BEDC
source singleton empty-history $\CommRingUp$; carrier $A_{\emp}$; classifier
$\rho_{\emp}$; stability from the singleton square-exact and signed-product
annihilator packages; ledger the equal-square row, signed-product zero row, and
universal two-sided annihilator row; core theorem this block; exactness the
displayed three-part certificate; downstream use by $\FieldUp$ quadratic and
zero-factor exit packages; status $\mathsf{ProofTarget}$.

\begin{definition}[CommRing left zero-divisor data]
\label{def:commring-left-zero-divisor-data}
For a BEDC history-level $\CommRingUp$ operation tuple with multiplication
$\cdot$ and zero $0$, write
$$
\mathsf{ApartZero}_R(x):=(x\hsame0\to\bot).
$$
The strict left zero-divisor predicate is
$$
\mathsf{LeftZD}_R(x):=
\mathsf{ApartZero}_R(x)\land
\exists c : \mathsf{BHist},\
\mathsf{ApartZero}_R(c)\land x\cdot c\hsame0.
$$
\end{definition}

\begin{theorem}[CommRing zero-divisor product closure]
\label{thm:commring-zero-divisor-product-closure}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, multiplicative associativity, addition
and multiplication congruence, left distributivity, and the $\CommRingUp$
multiplicative commutativity field, every $a,b:\mathsf{BHist}$ satisfies
$$
\mathsf{LeftZD}_R(a)
\Longrightarrow
\bigl((a\cdot b)\hsame0\to\bot\bigr)
\Longrightarrow
\mathsf{LeftZD}_R(a\cdot b).
$$
\end{theorem}
\begin{proof}
First derive right distributivity from the supplied left distributivity by
\autoref{thm:commring-right-distrib-from-left}. With this row, the additive
group laws, and multiplication congruence, the hypotheses of
\autoref{thm:ring-zero-classifier-factor-absorption} are available.

Unpack $\mathsf{LeftZD}_R(a)$. Its right component gives a history
$c:\mathsf{BHist}$ with $\mathsf{ApartZero}_R(c)$ and $a\cdot c\hsame0$. Let
$P$ be the displayed proof that $a\cdot b$ is apart from zero.

The same $c$ annihilates $a\cdot b$. Multiplicative associativity gives
$(a\cdot b)\cdot c\hsame a\cdot(b\cdot c)$. Multiplicative commutativity gives
$b\cdot c\hsame c\cdot b$, and multiplication congruence transports this inside
left multiplication by $a$. Symmetry of associativity then identifies
$a\cdot(c\cdot b)$ with $(a\cdot c)\cdot b$.

Apply \autoref{thm:ring-zero-classifier-factor-absorption} to the zero
classifier $a\cdot c\hsame0$ and multiplier $b$. Its left product component
gives $(a\cdot c)\cdot b\hsame0$. Transitivity through the associativity and
commutativity rows yields
$$
(a\cdot b)\cdot c\hsame0.
$$

The strict left zero-divisor data for $a\cdot b$ is therefore the pair
consisting of $P$ and the same annihilator witness $c$ with its apart-zero proof
and the displayed product-zero classifier.
\end{proof}

\begin{corollary}[CommRing zero-divisor product zero-or closure]
\label{cor:commring-zero-divisor-product-zero-or-closure}
Under the same hypotheses as
\autoref{thm:commring-zero-divisor-product-closure}, every
$a,b:\mathsf{BHist}$ satisfies
$$
\mathsf{LeftZD}_R(a)
\Longrightarrow
\left(
(a\cdot b)\hsame0
\lor
\bigl(((a\cdot b)\hsame0\to\bot)\to
\mathsf{LeftZD}_R(a\cdot b)\bigr)
\right).
$$
\end{corollary}
\begin{proof}
Assume $H:\mathsf{LeftZD}_R(a)$. Choose the right disjunct. Given a proof that
$a\cdot b$ is apart from zero, apply
\autoref{thm:commring-zero-divisor-product-closure} to $H$ and that apartness
proof.
\end{proof}

\begin{definition}[CommRing subring-inclusion name certificate]
\label{def:commring-subring-inclusion-name-certificate}
Let $S$ and $T$ be history-level $\CommRingUp$ packages as in \autoref{def:commring-certificate-obligations}. Write their carriers and classifiers as $C_S,\sim_S$ and $C_T,\sim_T$, and write their operations as $0_S,1_S,+_S,-_S,\cdot_S$ and $0_T,1_T,+_T,-_T,\cdot_T$. For a history map $\iota:\mathsf{BHist}\to\mathsf{BHist}$, define the target image carrier
$$
\mathsf{Im}_{\iota}(y)\Longleftrightarrow \exists x:\mathsf{BHist},\ C_S(x)\land \iota(x)\sim_T y.
$$
Define the target-image classifier by
$$
y\sim_{\iota} z\Longleftrightarrow \mathsf{Im}_{\iota}(y)\land\mathsf{Im}_{\iota}(z)\land y\sim_T z.
$$
A subring-inclusion name certificate for $\iota$ over $T$ is the naming certificate of \autoref{def:typed-naming-certificate} with source $\mathsf{Im}_{\iota}$, classifier $\sim_{\iota}$, stability rows inherited from the target $\CommRingUp$ package, and ledger rows recording the carrier reflection, classifier reflection, and operation-preservation data for $0$, $1$, $+$, and $\cdot$. Its stability component includes target-carrier inclusion, classifier transport, closure under $0_T$, $1_T$, $+_T$, $-_T$, and $\cdot_T$, and the inherited additive, multiplicative, distributive, congruence, and commutativity laws under $\sim_{\iota}$.
\end{definition}

\begin{theorem}[CommRing carrier reflection gives subring inclusion]
\label{thm:commring-carrier-reflection-subring-inclusion-certificate}
Let $S$ and $T$ be history-level $\CommRingUp$ packages as in \autoref{def:commring-certificate-obligations}, with notation as in \autoref{def:commring-subring-inclusion-name-certificate}. Let $\iota:\mathsf{BHist}\to\mathsf{BHist}$ satisfy the following rows:
\begin{enumerate}
  \item Carrier reflection: $C_S(x)\Longrightarrow C_T(\iota(x))$ and $C_T(\iota(x))\Longrightarrow C_S(x)$ for every $x$.
  \item Classifier reflection on carried source endpoints:
$$
C_S(x)\land C_S(y)\Longrightarrow \bigl(x\sim_S y\Longleftrightarrow \iota(x)\sim_T\iota(y)\bigr).
$$
  \item Constant preservation: $\iota(0_S)\sim_T0_T$ and $\iota(1_S)\sim_T1_T$.
  \item Operation preservation for carried inputs:
$$
\iota(x+_S y)\sim_T\iota(x)+_T\iota(y)\qquad\text{and}\qquad \iota(x\cdot_S y)\sim_T\iota(x)\cdot_T\iota(y).
$$
\end{enumerate}
Then the image carrier $\mathsf{Im}_{\iota}$ and classifier $\sim_{\iota}$ of \autoref{def:commring-subring-inclusion-name-certificate} form a subring-inclusion name certificate over the target package $T$.
\end{theorem}
\begin{proof}
The target classifier is a carrier naming certificate by \autoref{def:lean-carrier-naming-certificate}. Hence it supplies reflexivity, symmetry, transitivity, and carrier transport for $\sim_T$. If $\mathsf{Im}_{\iota}(y)$ is witnessed by a carried source endpoint $x$ with $\iota(x)\sim_T y$, carrier preservation gives $C_T(\iota(x))$, and target carrier transport gives $C_T(y)$. If also $y\sim_T y'$, transitivity gives $\iota(x)\sim_T y'$, so the same source witness proves $\mathsf{Im}_{\iota}(y')$.
The restricted classifier $\sim_{\iota}$ is a $\NameCert$ on $\mathsf{Im}_{\iota}$. Inhabitedness is witnessed by $0_T$: the source certificate carries $0_S$, and the constant row gives $\iota(0_S)\sim_T0_T$. Reflexivity, symmetry, and transitivity are the corresponding target classifier fields, with the image endpoints retained in the first two components of the definition of $\sim_{\iota}$. Carrier transport for $\mathsf{Im}_{\iota}$ is immediate from the right image component of a $\sim_{\iota}$ witness.
The constants are carried by the image for the same reason. The zero row uses $0_S$ and $\iota(0_S)\sim_T0_T$; the unit row uses $1_S$ and $\iota(1_S)\sim_T1_T$.
For addition closure, choose image witnesses $x,u$ for $y,z$, so $C_S(x)$, $C_S(u)$, $\iota(x)\sim_T y$, and $\iota(u)\sim_T z$. Source additive closure, part of the $\RingUp$ component in \autoref{def:ring-stability-certificate}, gives $C_S(x+_S u)$. Operation preservation gives
$$
\iota(x+_S u)\sim_T\iota(x)+_T\iota(u).
$$
Target additive congruence transports the two image comparisons to
$$
\iota(x)+_T\iota(u)\sim_T y+_T z.
$$
Transitivity gives $\iota(x+_S u)\sim_T y+_T z$, so $x+_S u$ witnesses $\mathsf{Im}_{\iota}(y+_T z)$. Multiplication closure is identical, using source multiplicative closure, target multiplication congruence, and the product-preservation row.
It remains to record closure under target additive inverse. Let $x$ witness $\mathsf{Im}_{\iota}(y)$. Source inverse closure gives $C_S(-_S x)$. The source left-inverse row gives $(-_S x)+_S x\sim_S0_S$, and classifier preservation sends this to
$$
\iota((-_S x)+_S x)\sim_T\iota(0_S).
$$
Additive operation preservation and zero preservation turn this into
$$
\iota(-_S x)+_T\iota(x)\sim_T0_T.
$$
Using target additive congruence with $\iota(x)\sim_T y$ gives
$$
\iota(-_S x)+_T y\sim_T0_T.
$$
The target right-inverse row from \autoref{thm:ring-add-right-inverse} gives $y+_T(-_T y)\sim_T0_T$. With target additive associativity, left zero, the right-zero row from \autoref{thm:ring-add-right-zero}, and target addition congruence, \autoref{thm:group-left-right-inverse-uniqueness} identifies the two inverse witnesses:
$$
\iota(-_S x)\sim_T -_T y.
$$
Thus $-_S x$ witnesses $\mathsf{Im}_{\iota}(-_T y)$. The additive inverse row is derived from $+$ and $0$ preservation, so no separate preservation assumption for $-_S$ is required.
All remaining $\CommRingUp$ laws restrict from $T$. Associativity, commutativity of addition, additive inverse laws, multiplicative unit and associativity, distributivity, multiplication congruence, and multiplicative commutativity are target stability rows from \autoref{def:commring-stability-certificate}; their endpoints stay inside $\mathsf{Im}_{\iota}$ by the closure rows just proved, and their classifier comparisons are exactly the third component of $\sim_{\iota}$. The reverse carrier and classifier reflection implications are recorded as the exactness part of the inclusion ledger, showing that the target image does not identify carried source endpoints beyond $\sim_S$.
These rows supply the source, classifier, stability, and ledger components required by \autoref{def:commring-subring-inclusion-name-certificate}, hence they induce the advertised subring-inclusion name certificate over $T$.
\end{proof}
