\chapter{A Concrete Naming Certificate for $\WeierstrassFunctionUp$}
\label{ch:concrete-instances-weierstrass-function-namecert}
\origin{human}

Following Weierstrass and Bishop--Bridges constructive analysis, the $\WeierstrassFunctionUp$ packet records a finite approximation route for the classical nowhere-smooth trigonometric series by using only displayed windows, dyadic ledgers, and regular real-name handoffs. It sits after the majorant route of \autoref{ch:concrete-instances-weierstrassmtest-namecert}, the uniform Cauchy series surface of \autoref{ch:concrete-instances-uniform-cauchy-series-namecert}, the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the constructive finite-window approximation route; it does not assert a host function space, differentiability failure, quotient equality of functions, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Weierstrass function finite-approximation carrier]
\label{def:weierstrass-function-carrier}
A $\WeierstrassFunctionUp$ carrier is a finite $\BHist$ packet
$$
W=(T,A,M,U,S,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the finite trigonometric-polynomial window, listing only the inspected frequencies, phase samples, and partial sums of the approximation.
\item $A$ is the dyadic amplitude and frequency ledger for the same window, including the finite amplitude weights and frequency growth entries read by $T$.
\item $M$ is the M-test majorant row that bounds the displayed tail requests through positive dyadic comparison data.
\item $U$ is the uniform Cauchy threshold row, stated as a finite request-to-tail ledger rather than as an ambient infinite sum.
\item $S$ is the $\StreamNameUp$ window surface through which sample points, partial sums, and tail requests are observed.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the visible rational approximants.
\item $R$ is the terminal $\RealUp$ seal row reached after the uniform Cauchy threshold and regular readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for trigonometric windows, dyadic ledgers, majorant rows, uniform thresholds, stream windows, readbacks, real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the same finite route.
\item $P$ is the $\Pkg$ provenance over $\WeierstrassFunctionUp$, $\WeierstrassMTestUp$, $\UniformCauchySeriesUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WeierstrassFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $T,A,M,U,S,Q,R,H,C,P,N$. It has no coordinate for a completed function space, a global infinite series object, differentiability failure, quotient equality of functions, countable choice, selected limits, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Weierstrass function finite-window NameCert obligations]
\label{thm:weierstrass-function-namecert-obligations}
For every accepted $\WeierstrassFunctionUp$ carrier
$$
W=(T,A,M,U,S,Q,R,H,C,P,N),
$$
the local $\NameCert_{\WeierstrassFunctionUp}$ surface consists exactly of finite trigonometric-polynomial window exposure through $T$, dyadic amplitude and frequency ledger exposure through $A$, M-test majorant exposure through $M$, uniform Cauchy threshold exposure through $U$, StreamName observation through $S$, RegSeqRat readback through $Q$, RealUp sealing through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:weierstrass-function-carrier}. The visible analytic rows are exactly $T,A,M,U,S,Q,R$, so every local certificate read must expose the finite partial polynomial window, its dyadic amplitude and frequency ledger, the majorant row, the uniform threshold, the observed stream windows, the regular rational readback, and the final real-name seal.

Read the dependencies in route order. The M-test row is the majorant interface of \autoref{ch:concrete-instances-weierstrassmtest-namecert}; the uniform Cauchy threshold is the series route of \autoref{ch:concrete-instances-uniform-cauchy-series-namecert}; the visible samples are $\StreamNameUp$ windows; and the rational approximants pass through $\RegSeqRatUp$ before reaching the $\RealUp$ seal.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for a completed function space, host infinite series, differentiability failure, quotient function equality, choice schedule, selected limit, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Weierstrass function uniform Cauchy route]
\label{thm:weierstrass-function-uniform-cauchy-route}
Every approximation or Real-facing consumer of an accepted $\WeierstrassFunctionUp$ packet factors through the finite route
$$
\WeierstrassMTestUp,\ \UniformCauchySeriesUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp .
$$
The consumer receives a finite trigonometric-polynomial window, dyadic amplitude and frequency data, a majorant row, and a uniform Cauchy threshold before any terminal Real seal can be inspected. The route exports no global infinite series object, quotient equality of functions, selected limit, countable-choice schedule, differentiability claim, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:weierstrass-function-namecert-obligations}. They force each consumer read through the finite rows $T,A,M,U,S,Q,R$ before the packet can be used as a named approximation object.

The M-test row supplies the majorant-tail control, and the uniform Cauchy threshold turns that control into a finite request-to-tail ledger. The stream and regular-rational rows then determine which approximants are visible before the terminal real-name row is consumed.

The structural rows $H,C,P,N$ preserve this route under transport, replay, provenance, and local naming. Because no excluded host-level coordinate is present in the carrier, every consumer factors through the displayed finite route.
\end{proof}

\begin{theorem}[Weierstrass function Real-seal nonescape]
\label{thm:weierstrass-function-real-seal-nonescape}
Let
$$
W=(T,A,M,U,S,Q,R,H,C,P,N)
$$
be an accepted $\WeierstrassFunctionUp$ carrier. Any read of the terminal $\RealUp$ seal $R$ from $W$ is preceded by the displayed rows $T,A,M,U,S,Q$ and is replayed only through $H,C,P,N$. Thus the real-name handoff cannot be used to infer a host function object, a completed ambient series, differentiability failure, quotient function equality, countable choice, a selected limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:weierstrass-function-uniform-cauchy-route}. A Real-facing read must first expose the trigonometric-polynomial window, dyadic amplitude and frequency ledger, majorant row, uniform Cauchy threshold, stream windows, and regular rational readback.

The seal $R$ is therefore terminal for this displayed route, not a source of additional host-level structure. The only remaining rows are transport, replay, provenance, and local naming rows, and each preserves the already displayed packet.

Since the carrier lacks coordinates for the excluded objects, reading $R$ cannot introduce them. The Real seal is a handoff boundary for the finite approximation route and not an ambient completeness principle.
\end{proof}

\falsifiablePrediction{Every accepted $\WeierstrassFunctionUp$ approximation read exposes finite trigonometric-polynomial windows, dyadic amplitude and frequency ledgers, M-test majorant rows, uniform Cauchy thresholds, StreamName windows, RegSeqRat readback, RealUp seals, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows before any Real-facing consumer is served.}

\independenceWitness{weierstrassmtest, uniformcauchyseries, streamname, regseqrat, dyadicratcore, real: none alone is bijective with $\WeierstrassFunctionUp$ because this packet alone binds the finite trigonometric window, dyadic amplitude and frequency ledger, majorant row, uniform threshold, observation route, readback, seal, transport, replay, provenance, and local naming as one certificate.}

\closureat{WeierstrassFunctionUp}{seedStr}

\begin{closurestatus}{\WeierstrassFunctionUp}
  \constructivestory{A $\WeierstrassFunctionUp$ packet is generated from finite trigonometric-polynomial windows, dyadic amplitude and frequency ledgers, M-test majorant rows, uniform Cauchy thresholds, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weierstrass-function-carrier}, \autoref{thm:weierstrass-function-namecert-obligations}, \autoref{thm:weierstrass-function-uniform-cauchy-route}, and \autoref{thm:weierstrass-function-real-seal-nonescape} seed the finite approximation route for the constructive Weierstrass function packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a completed function space, a global infinite series object, differentiability failure, quotient equality of functions, countable choice, selected limits, ambient $\RealUp$ completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, trigonometric-window exposure, dyadic ledger exactness, M-test majorant exposure, uniform Cauchy thresholding, StreamName observation, RegSeqRat readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.WeierstrassFunctionUp.}
\end{closurestatus}
