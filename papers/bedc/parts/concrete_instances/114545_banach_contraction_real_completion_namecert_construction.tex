\chapter{A Concrete Naming Certificate for $\BanachContractionRealCompletionUp$}
\label{ch:concrete-instances-banach-contraction-real-completion-namecert}
\origin{human}

Following Banach's contraction principle and Bishop--Bridges constructive completion, the $\BanachContractionRealCompletionUp$ packet records the finite route by which contraction iterates become a completion-facing real seal. It sits after the fixed-point endpoint discipline of \autoref{ch:concrete-instances-banach-fixed-point-namecert}, the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy-modulus surface of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names contraction iteration as displayed RegSeqRat and Real completion data; it does not select a host fixed point, quotient completion representative, or ambient completeness principle.

\begin{definition}[Banach contraction real-completion carrier]
\label{def:banach-contraction-real-completion-carrier}
A $\BanachContractionRealCompletionUp$ carrier is a finite $\BHist$ packet
$$
B=(X,F,x_0,I,D,M,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompleteMetricUp$ source row, carrying only the displayed metric and completion-facing Cauchy service available to the contraction route.
\item $F$ is the contraction self-map row, including the graph read and contraction-ratio ledger used to compare successive iterates.
\item $x_0$ is the initial-point row.
\item $I$ is the finite iterate ledger naming the inspected Picard stages $x_0,x_1,\ldots,x_n$.
\item $D$ is the adjacent-distance ledger, recording the contraction-controlled distance comparisons extracted from $I$ and $F$.
\item $M$ is the $\CauchyModulusUp$ row generated from $D$.
\item $W$ is the finite observation-window row used when a completion or real consumer asks for a displayed precision read.
\item $R$ is the $\RegSeqRatUp$ handoff row built from $M$ and $W$.
\item $E$ is the terminal $\RealUp$ seal row reached only after the contraction, modulus, window, and regular-rational rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for $X,F,x_0,I,D,M,W,R,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a contraction-iteration request through the metric, modulus, regular-rational, and real-seal rows.
\item $P$ is the $\Pkg$ provenance over $\BanachContractionRealCompletionUp$, $\CompleteMetricUp$, $\CauchyModulusUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BanachContractionRealCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for a selected fixed point, quotient real equality, host function extensionality, countable choice over iterates, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Banach contraction real-completion obligations]
\label{thm:banach-contraction-real-completion-obligations}
For every accepted $\BanachContractionRealCompletionUp$ carrier $B=(X,F,x_0,I,D,M,W,R,E,H,C,P,N)$, the local $\NameCert_{\BanachContractionRealCompletionUp}$ surface consists exactly of carrier admission, complete-metric source exposure, contraction self-map stability, initial-point visibility, finite iterate-ledger visibility, adjacent-distance exactness, Cauchy-modulus extraction, observation-window locality, $\RegSeqRatUp$ handoff, $\RealUp$ seal nonescape, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:banach-contraction-real-completion-carrier}. The rows $X,F,x_0,I,D$ expose the complete metric source, the contraction graph, the initial point, the inspected iterates, and the adjacent-distance comparisons before any completion-facing row is read.

The Cauchy data are downstream. The row $M$ is generated from the displayed distance ledger, and the row $W$ names the finite windows used by each requested precision read.

The real-completion boundary is terminal. The row $R$ is the only regular rational handoff, and $E$ is the only Real seal. The structural rows $H,C,P,N$ preserve and package the same route, so the certificate cannot export a selected fixed point, quotient representative, countable-choice schedule, or host completeness theorem.
\end{proof}

\begin{theorem}[Banach contraction iteration Cauchy route]
\label{thm:banach-contraction-real-completion-iteration-cauchy-route}
Every completion-facing consumer of an accepted $\BanachContractionRealCompletionUp$ carrier must read the contraction iteration through
$$
X,F,x_0,I,D,M,W,R,E,H,C,P,N .
$$
The adjacent-distance ledger $D$ produces the $\CauchyModulusUp$ row $M$, the finite windows $W$ expose the requested precision reads, the $\RegSeqRatUp$ row $R$ gives the regular rational handoff, and the $\RealUp$ row $E$ seals the completion-facing real datum. No public read bypasses this order through host completeness or a chosen fixed-point object.
\end{theorem}
\begin{proof}
Start with a completion-facing read. The carrier admits the candidate iteration only through the complete metric row $X$, contraction row $F$, initial point $x_0$, and finite iterate ledger $I$.

The Cauchy route is forced by the distance ledger. The row $D$ records the contraction-controlled adjacent distances, and $M$ is the only Cauchy-modulus row available to the consumer.

The regular and real rows are then read in order. The window row $W$ fixes the inspected precision budget, $R$ translates that budget into regular rational data, and $E$ seals the resulting real-facing read. Transport, replay, provenance, and local naming preserve the same route, leaving no coordinate for a host fixed point, selected limit, quotient equality, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\BanachContractionRealCompletionUp$ real-completion read factors through a complete metric source, contraction self-map, initial point, finite iterate ledger, adjacent-distance ledger, CauchyModulus row, finite observation windows, RegSeqRat handoff, RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{banachfixedpoint, completemetric, cauchymodulus, regseqrat, real: none is bijective with $\BanachContractionRealCompletionUp$ because the packet jointly stores contraction iteration, adjacent-distance control, Cauchy-modulus extraction, finite windows, regular rational handoff, terminal Real seal, transport, replay, provenance, and local naming.}

\closureat{BanachContractionRealCompletionUp}{seedStr}

\begin{closurestatus}{\BanachContractionRealCompletionUp}
  \constructivestory{A $\BanachContractionRealCompletionUp$ packet is generated from a CompleteMetricUp source, contraction self-map, initial point, finite iterate ledger, adjacent-distance ledger, CauchyModulus route, finite observation windows, RegSeqRat handoff, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:banach-contraction-real-completion-carrier}, \autoref{thm:banach-contraction-real-completion-obligations}, and \autoref{thm:banach-contraction-real-completion-iteration-cauchy-route} seed the finite contraction-iteration route from complete metric data to RegSeqRat readback and Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected fixed points, quotient completion representatives, host function extensionality, countable choice over iterates, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction stability, adjacent-distance exactness, Cauchy-modulus extraction, window locality, RegSeqRat handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BanachContractionRealCompletionUp.}
\end{closurestatus}
