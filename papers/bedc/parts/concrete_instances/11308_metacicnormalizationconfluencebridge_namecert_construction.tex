\chapter{A Concrete Naming Certificate for $\MetacicNormalizationConfluenceBridgeUp$}
\label{ch:concrete-instances-metacic-normalization-confluence-bridge-namecert}
\origin{ai}

The \MetacicNormalizationConfluenceBridgeUp{} packet binds the normalization audit route of \autoref{ch:concrete-instances-metacicnormalizationaudit-namecert} to the confluence audit route of \autoref{ch:concrete-instances-metacic-confluence-audit-namecert}, then records how the subject-reduction discharge ledger of \autoref{ch:concrete-instances-subjectreductiondischargeledger-namecert} and the closed-consistency assembly of \autoref{ch:concrete-instances-closed-consistency-assembly-namecert} may read the shared closed typed term boundary. It is a finite bridge packet for audit correspondence, not an unconditional closed subject-reduction theorem.

The packet is composite over the normalization-audit, confluence-audit, subject-reduction discharge-ledger, and closed-consistency assembly rows. Its bridge row records the correspondence among those sibling packets rather than presenting an atomic carrier discovery.

\begin{definition}[MetaCIC normalization-confluence bridge carrier]
\label{def:metacic-normalization-confluence-bridge-carrier}
A \MetacicNormalizationConfluenceBridgeUp{} carrier is a finite $\BHist$ packet
$$
B=(N,Q,D,A,T,H,C,P,L)
$$
with the following rows:
\begin{enumerate}
\item $N$ is the $\MetacicNormalizationAuditUp$ row carrying candidate, closed-candidate, candidate-set, application adequacy, Pi adequacy, and no-infinite-reduction reads;
\item $Q$ is the $\MetaCICConfluenceAuditUp$ row carrying checked confluence fragments and the named confluence-boundary inputs;
\item $D$ is the $\SubjectReductionDischargeLedgerUp$ row carrying the beta-substitution, application-argument, lambda-domain, and Pi-domain discharge surface;
\item $A$ is the $\ClosedConsistencyAssemblyUp$ row carrying closedness, typed-normal endpoint, normal false-proof exclusion, and subject-reduction boundary exposure;
\item $T$ is the closed typed term route-correspondence classifier, recording that the normalization row and confluence row speak about the same displayed closed typed term before any consistency consumer reads the packet;
\item $H$ is the componentwise $\hsame$ transport row over $N,Q,D,A,T$;
\item $C$ is the displayed $\Cont$ replay route for normalization, confluence, discharge-ledger, and assembly consumers;
\item $P$ is the $\Pkg$ provenance row naming the four sibling packets and the audit-map source route;
\item $L$ is the local $\NameCert_{\MetacicNormalizationConfluenceBridgeUp}$ row.
\end{enumerate}
The carrier exports no host evaluator, quotient equality over reductions, hidden typed-term selector, unconditional confluence theorem, unconditional closed subject-reduction theorem, or closed consistency proof detached from $N,Q,D,A,T,H,C,P,L$.
\end{definition}

\begin{theorem}[MetaCIC normalization-confluence bridge NameCert obligations]
\label{thm:metacic-normalization-confluence-bridge-namecert-obligations}
For an accepted \MetacicNormalizationConfluenceBridgeUp{} carrier, the local $\NameCert$ surface consists of the following finite obligations:
\begin{enumerate}
\item carrier admission for $B=(N,Q,D,A,T,H,C,P,L)$ as a $\BHist$ packet;
\item normalization-audit row readback through $N$, including Candidate, closed-candidate, candidate-set, App adequacy, and Pi adequacy entries;
\item confluence-audit row readback through $Q$, including checked fragment rows and named confluence-boundary inputs;
\item subject-reduction discharge-ledger readback through $D$, preserving the four preservation-obligation rows without turning them into a closed theorem;
\item closed-consistency assembly readback through $A$, preserving the normal false-proof exclusion and subject-reduction boundary rows;
\item closed typed term route correspondence through $T$, with agreement transported componentwise by $H$;
\item displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $L$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-normalization-confluence-bridge-carrier}. The rows $N,Q,D,A$ are already distinct sibling packets, and $T$ is the only classifier row that compares their closed typed term routes.

Use the normalization audit obligation surface, the confluence audit obligation surface, the subject-reduction discharge-ledger obligations, and the closed-consistency assembly obligations. These sibling results supply the rows consumed by the bridge, while $H,C,P,L$ preserve transport, replay, provenance, and local naming.

Because the bridge has no further coordinate, every accepted read is exhausted by the displayed finite packet. In particular the read cannot pass through host complex analysis, quotient equality, a hidden typed-term selector, an unconditional confluence theorem, or an unconditional closed subject-reduction theorem.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge route boundary]
\label{thm:metacic-normalization-confluence-bridge-route-boundary}
Any accepted bridge packet whose normalization and confluence rows agree on the closed typed term through $T,H,C,P,L$ has no route to an unconditional closed subject-reduction theorem. The packet may hand the displayed discharge-ledger row $D$ to the closed-consistency assembly row $A$, but every such handoff remains inside $N,Q,D,A,T,H,C,P,L$.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicNormalizationConfluenceBridgeUp.MetacicNormalizationConfluenceBridgeRouteBoundary}
\begin{proof}
Read the agreement first through $T$. It compares the normalization and confluence routes only as displayed closed typed term rows; it does not add a preservation theorem.

Next route the preservation side through $D$. The discharge ledger contains the four named preservation-obligation rows, so a consumer may cite those rows but cannot erase their boundary and obtain closed subject reduction for arbitrary closed proofs.

Finally pass to $A$. The closed-consistency assembly can read the normal false-proof exclusion and the subject-reduction boundary, but it receives them as finite sibling rows under $H,C,P,L$. Thus the bridge remains a route-correspondence certificate rather than an unconditional theorem.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge transport obligations]
\label{thm:metacic-normalization-confluence-bridge-transport-obligations}
For an accepted \MetacicNormalizationConfluenceBridgeUp{} carrier $B=(N,Q,D,A,T,H,C,P,L)$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ transport preserve the separate bridge rows $N,Q,D,A,T$. The transported read may replay the normalization audit row $N$, the confluence audit row $Q$, the discharge-ledger row $D$, the closed-consistency assembly row $A$, and the typed-term correspondence row $T$, but it cannot collapse those rows into a subject-reduction theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier into its nine displayed coordinates. The transport row $H$ is componentwise, so it acts on $N,Q,D,A,T$ as separate row data rather than as a quotient over the sibling packets.

The displayed $\Cont$ row $C$ replays the same bridge route after transport, while $\Pkg$ records the provenance of the four sibling packets. The local $\NameCert$ row $L$ names the transported certificate surface and does not add a new proof coordinate.

Thus transport preserves exactly the rows already present in $B$. Since no coordinate of $B$ is an unconditional subject-reduction theorem, preserving the packet cannot manufacture one.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge route exactness]
\label{thm:metacic-normalization-confluence-bridge-route-exactness}
Every accepted \MetacicNormalizationConfluenceBridgeUp{} bridge read is exhausted by $N,Q,D,A,T,H,C,P,L$. In particular, the read has no hidden typed-term selector, no quotient reduction equality, no host evaluator, no unconditional confluence theorem, no closed subject-reduction theorem, and no extra route outside the displayed packet.
\end{theorem}
\begin{proof}
Read the bridge through the carrier definition. Its displayed rows are exactly $N,Q,D,A,T,H,C,P,L$, with $T$ as the only row comparing the closed typed term routes and $H,C,P,L$ as structural transport, replay, provenance, and local naming rows.

Any accepted route must therefore factor through one of those coordinates. The carrier definition exports no further selector and no equality quotient over reduction routes, so there is no place for a hidden typed-term selector or host evaluator to enter.

The same exhaustion excludes unconditional metatheorems. Confluence is read only through $Q$, and preservation is read only through $D$ and the boundary row visible to $A$; neither read becomes a closed theorem detached from the packet.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge discharge boundary]
\label{thm:metacic-normalization-confluence-bridge-discharge-boundary}
Within an accepted \MetacicNormalizationConfluenceBridgeUp{} carrier, row $D$ can be handed to row $A$ only as the displayed subject-reduction discharge-ledger boundary. The handoff preserves the named discharge rows consumed by closed consistency, but it does not assert an unconditional closed subject-reduction theorem.
\end{theorem}
\begin{proof}
The discharge side of the bridge is the row $D$. It carries the named preservation-obligation rows from the subject-reduction discharge ledger and remains distinct from the closed-consistency assembly row $A$.

The assembly row may consume $D$ only through the displayed bridge route. This gives $A$ access to the ledger boundary that it cites, together with the normal false-proof exclusion already internal to the assembly packet.

Because the bridge has no additional preservation coordinate, the handoff cannot erase the ledger boundary. The result is a routed discharge boundary for the assembly consumer, not an unconditional closed subject-reduction theorem.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge TasteGate surface]
\label{thm:metacic-normalization-confluence-bridge-tastegate-surface}
The Lean file \texttt{TasteGate.lean} for \MetacicNormalizationConfluenceBridgeUp{} must provide $\mathrm{Nontrivial}\ \MetacicNormalizationConfluenceBridgeUp$ and $\mathrm{FieldFaithful}\ \MetacicNormalizationConfluenceBridgeUp$ instances. The nontrivial instance must distinguish bridge packets that agree on $N,Q$ but differ on $D$ or $A$, and the field-faithful instance must preserve the separate rows $N,Q,D,A,T,H,C,P,L$.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicNormalizationConfluenceBridgeUp.MetacicNormalizationConfluenceBridgeTasteGate\_single\_carrier\_alignment}
\begin{proof}
The carrier has four sibling packet rows and five structural rows. A TasteGate that identifies all carriers would collapse the bridge to one point and would lose the distinction between normalization, confluence, discharge-ledger, and assembly reads.

Field faithfulness is forced by the same packet shape. Preserving $N,Q,D,A,T,H,C,P,L$ separately is exactly what prevents the bridge from rereading a normalization or confluence agreement as a subject-reduction theorem or as closed consistency.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge obligation closure package]
\label{thm:metacic-normalization-confluence-bridge-obligation-closure-package}
The displayed rows $N,Q,D,A,T,H,C,P,L$, together with the local $\NameCert$ obligations, the route-boundary theorem, the TasteGate marker, the falsifiable prediction, and the independence witness below, form the complete $\obligationClosure$ package for \MetacicNormalizationConfluenceBridgeUp{}.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:metacic-normalization-confluence-bridge-carrier}. It fixes the only admissible packet shape $B=(N,Q,D,A,T,H,C,P,L)$ and states that no hidden evaluator, quotient equality, typed-term selector, global confluence theorem, unconditional subject-reduction theorem, or detached consistency proof is exported.

Apply \autoref{thm:metacic-normalization-confluence-bridge-namecert-obligations}. The seven obligation rows cover carrier admission, the normalization-audit row, the confluence-audit row, the discharge-ledger row, the closed-consistency assembly row, closed typed term route correspondence, and the replay/provenance/local-name rows.

Apply \autoref{thm:metacic-normalization-confluence-bridge-route-boundary} and \autoref{thm:metacic-normalization-confluence-bridge-tastegate-surface}. The route theorem keeps every preservation or consistency handoff inside $N,Q,D,A,T,H,C,P,L$, while the TasteGate theorem records that Lean supplies the nontrivial and field-faithful surface needed to keep those rows distinct.

The falsifiable prediction states the observable route failure for unconditional closed subject reduction, and the independence witness records why the four sibling packets do not already contain the simultaneous bridge classifier. Hence the chapter closes precisely the obligation package for this bridge. It does not claim global normalization, global confluence, global subject reduction, or closed consistency.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge scope index]
\label{thm:metacic-normalization-confluence-bridge-scope-index}
The scoped $\MetacicNormalizationConfluenceBridgeUp$ read consumes exactly the following displayed rows: the normalization-audit row $N$, the confluence-audit row $Q$, the subject-reduction obstruction and discharge row $D$, the closed-consistency assembly row $A$, the typed-term route-correspondence row $T$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming $L$. No scoped read may replace these rows by a host evaluator, quotient reduction equality, hidden typed-term selector, unconditional confluence theorem, unconditional subject-reduction theorem, or detached consistency proof.
\end{theorem}
\begin{proof}
Start with the carrier fixed in \autoref{def:metacic-normalization-confluence-bridge-carrier}. Its coordinates are exactly $N,Q,D,A,T,H,C,P,L$.

Apply \autoref{thm:metacic-normalization-confluence-bridge-namecert-obligations} to read the local obligation surface. The normalization audit, confluence audit, discharge-ledger boundary, closed-consistency assembly, typed-term correspondence, transport, replay, provenance, and local naming rows are all displayed there.

Apply \autoref{thm:metacic-normalization-confluence-bridge-route-boundary}, \autoref{thm:metacic-normalization-confluence-bridge-discharge-boundary}, and \autoref{thm:metacic-normalization-confluence-bridge-route-exactness}. These theorems exclude the host evaluator, quotient equality, hidden typed-term selector, unconditional confluence, unconditional subject reduction, and detached consistency routes, so the listed rows are the complete scoped index.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge candidate-mediated normalization]
\label{thm:metacic-normalization-confluence-bridge-candidate-mediated-normalization}
Every normalization conclusion exported by an accepted $\MetacicNormalizationConfluenceBridgeUp$ carrier is mediated by the displayed candidate and closed-candidate evidence in $N$. It may be transported through $H$ and replayed through $C$, and it may be compared with the confluence row $Q$ through $T$, but it cannot be read as strong normalization for all closed typed terms outside the candidate-mediated route.
\end{theorem}
\begin{proof}
Read the normalization side through $N$. By \autoref{def:metacic-normalization-confluence-bridge-carrier}, this row is the normalization-audit packet carrying candidate, closed-candidate, candidate-set, application adequacy, Pi adequacy, and no-infinite-reduction reads.

The comparison row $T$ only records that the normalization and confluence rows speak about the same displayed closed typed term. Transport through $H$ and replay through $C$ preserve that row structure; they do not add a universal normalization coordinate.

Finally apply \autoref{thm:metacic-normalization-confluence-bridge-route-boundary} and \autoref{thm:metacic-normalization-confluence-bridge-obligation-closure-package}. The bridge remains a finite route-correspondence certificate, so the normalization conclusion is candidate-mediated and not an unconditional strong-normalization theorem for all closed typed terms.
\end{proof}

\begin{theorem}[MetaCIC normalization-confluence bridge conditional Church-Rosser boundary]
\label{thm:metacic-normalization-confluence-bridge-conditional-cr-boundary}
Every Church-Rosser use exported by an accepted $\MetacicNormalizationConfluenceBridgeUp$ carrier remains conditional on the displayed confluence row $Q$, the typed-term correspondence row $T$, and the discharge-ledger boundary row $D$. In particular, such a use cannot discharge the beta-substitution, application-argument, lambda-domain, and Pi-domain subject-reduction hypotheses recorded in $D$.
\end{theorem}
\begin{proof}
The confluence side of the bridge is the row $Q$. It supplies only the checked confluence fragments and named confluence-boundary inputs recorded by the carrier.

The bridge can compare $Q$ with the normalization row only through $T$. That comparison is a route correspondence for the displayed closed typed term, not a replacement for the preservation rows stored in $D$.

Apply \autoref{thm:metacic-normalization-confluence-bridge-discharge-boundary}. The row $D$ is handed to the assembly row $A$ only as the displayed subject-reduction discharge-ledger boundary. Hence Church-Rosser use remains conditional on $Q,T,D$ and cannot discharge the four subject-reduction hypotheses.
\end{proof}

\falsifiablePrediction{Any accepted bridge packet whose normalization and confluence rows agree on the closed typed term has no route to an unconditional closed subject-reduction theorem.}
\independenceWitness{metacicnormalizationaudit, metacic-confluence-audit, subjectreductiondischargeledger, closed-consistency-assembly: the bridge has a simultaneous four-row carrier and route-correspondence classifier absent from each sibling alone.}

\begin{closurestatus}{\MetacicNormalizationConfluenceBridgeUp}
  \constructivestory{The MetaCIC normalization-confluence bridge is generated from a $BHist$ packet that records a normalization row, a confluence row, and the bridge route joining them to the local certificate surface. The carrier does not assert global metatheory; it reads only the named normalization and confluence evidence and transports that evidence along $hsame$ when the representing history changes. $\Cont$ supplies the route boundary: later uses of the bridge must extend the same packet rather than appeal to an external proof of adequacy. The TasteGate surface is then the visible bridge between these rows and the admitted NameCert obligation.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{This chapter binds the MetaCIC normalization-confluence bridge carrier, NameCert obligations, route-boundary theorem, TasteGate-surface theorem, scope index, candidate-mediated normalization boundary, and conditional Church-Rosser boundary.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.MetacicNormalizationConfluenceBridgeUp.MetacicNormalizationConfluenceBridgeTasteGate\_single\_carrier\_alignment}
  \leanchecked{BEDC.Derived.MetacicNormalizationConfluenceBridgeUp.taste\_gate}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim full MetaCIC normalization, global confluence, or kernel soundness outside the displayed bridge packet.}
  \upgradepath{Public closure requires a checked consumer-facing package for normalization, confluence, discharge, and assembly reads over the same scoped bridge carrier.}
\end{closurestatus}
