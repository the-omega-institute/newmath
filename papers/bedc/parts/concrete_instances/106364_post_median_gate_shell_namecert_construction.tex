\chapter{A Concrete Naming Certificate for $\PostMedianGateShellUp$}
\label{ch:concrete-instances-post-median-gate-shell-namecert}
\origin{ai}

The $\PostMedianGateShellUp$ packet realizes the post-median gate-shell object of \autoref{def:philosophy-post-median-gate-shell} as finite BEDC NameCert data. It reads the six-boundary-tile vision through codon-shell rows, Boolean-coordinate rows, spectral-optimum rows, first-exit rows, transport, replay, provenance, and local naming. The packet is a science-facing finite certificate: it does not treat the observed shell as a universal genetic-code law, an unbounded evolutionary optimizer, or an oracle for unseen codon tables.

\falsifiablePrediction{Every accepted $\PostMedianGateShellUp$ read must display the six shell rows $\mathrm{CAR}$, $\mathrm{CCR}$, $\mathrm{GUR}$, the median gate $M$, the first-exit target layer, and the retained $\mathrm{CUR}$ exit-closing row before any spectral-optimum claim is exported.}

\independenceWitness{six-boundary-tile-coverage, window-coverage-trigger-algebra, punctured-cube-skeleton, wrr-state-phase-space, median-closure-structure: six-boundary-tile coverage names the codon-window partition, window-coverage trigger algebra names activation gates, the punctured-cube skeleton names cube geometry, WRR state phase space names replay states, and median closure structure names the median set. None is bijective to $\PostMedianGateShellUp$ because this packet binds post-median shell rows, Boolean coordinates, first-exit rows, spectral optimality, transport, replay, provenance, and local naming in one accepted surface.}

\begin{definition}[Post-median gate shell carrier]
\label{def:post-median-gate-shell-carrier}
A $\PostMedianGateShellUp$ carrier is a finite $\BHist$ packet
$$
G=(K,B,S,F,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the codon-shell row. It records the median gate $M$, the shell rows $\mathrm{CAR}$, $\mathrm{CCR}$, $\mathrm{GUR}$, the retained $\mathrm{CUR}$ exit-closing row, and the finite read request.
\item $B$ is the Boolean-coordinate row. It records the six-boundary tile coordinates used to read the shell as a finite yes-or-no window rather than as a host-level biological assertion.
\item $S$ is the spectral-optimum row. It records the killed-walk Perron-root comparison for the displayed shell rows and refuses to export an optimizer outside the finite comparison table.
\item $F$ is the first-exit row. It records that $\mathrm{CAR}$, $\mathrm{CCR}$, and $\mathrm{GUR}$ are read as the top first-exit target layer after the median gate.
\item $T$ records componentwise $\hsame$ transport for codon-shell rows, Boolean coordinates, spectral rows, first-exit rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the post-median gate-shell request through $K,B,S,F,T,P,N$.
\item $P$ is the $\Pkg$ provenance over $\PostMedianGateShellUp$, the six-boundary-tile coverage, the window-coverage trigger algebra, the punctured-cube skeleton, the WRR state phase space, the median closure structure, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PostMedianGateShellUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these eight rows. It has no coordinate for arbitrary codon-table optimality, global evolutionary selection, unseen-window extrapolation, host biochemical causality, or bridge checking.
\end{definition}

\begin{theorem}[Post-median gate shell NameCert obligations]
\label{thm:post-median-gate-shell-namecert-obligations}
For every accepted $\PostMedianGateShellUp$ carrier
$$
G=(K,B,S,F,T,C,P,N),
$$
the local $\NameCert_{\PostMedianGateShellUp}$ surface consists exactly of carrier admission for the codon-shell row $K$, Boolean-coordinate row $B$, spectral-optimum row $S$, first-exit row $F$, componentwise transport $T$, continuation replay $C$, package provenance $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:post-median-gate-shell-carrier}. The projection exposes one finite codon-shell row, one Boolean-coordinate row, one spectral row, one first-exit row, and the structural rows.

The codon and coordinate obligations are $K$ and $B$. They force every read to display the six shell rows and the finite boundary-tile coordinates before the shell can be compared spectrally.

The spectral and first-exit obligations are $S$ and $F$. The spectral row is tied to the displayed finite comparison table, while the first-exit row ties $\mathrm{CAR}$, $\mathrm{CCR}$, and $\mathrm{GUR}$ to the post-median target layer.

Finally $T,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for arbitrary codon-table optimality, global evolutionary selection, unseen-window extrapolation, or host biochemical causality, the displayed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Post-median gate shell accepted-read frontier]
\label{thm:post-median-gate-shell-accepted-read-frontier}
Every accepted read of a $\PostMedianGateShellUp$ packet factors through
$$
K,B
\longmapsto
S,F
\longmapsto
T,C,P,N .
$$
Thus a consumer may export the post-median shell only after the six shell rows, Boolean-coordinate window, finite spectral comparison, and first-exit target layer have all been displayed.
\end{theorem}
\begin{proof}
Begin with the shell row $K$ and Boolean-coordinate row $B$. These rows display the median gate, $\mathrm{CAR}$, $\mathrm{CCR}$, $\mathrm{GUR}$, $\mathrm{CUR}$, and the finite six-boundary coordinates available to the read.

The spectral row $S$ then records the finite Perron-root comparison for that displayed shell. The first-exit row $F$ records the route from the median gate to the top target layer, so the consumer cannot replace the shell by an unrelated codon orbit.

The rows $T,C,P,N$ preserve the same finite surface under transport, replay, provenance, and local naming. Any read that claims an unbounded optimizer, unseen-window extrapolation, global evolutionary selection, or host biochemical causality leaves the packet.
\end{proof}

\closureat{PostMedianGateShellUp}{seedStr}

\begin{closurestatus}{\PostMedianGateShellUp}
  \constructivestory{A $\PostMedianGateShellUp$ packet is generated from finite codon-shell rows, Boolean-coordinate rows, spectral-optimum rows, first-exit rows, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:post-median-gate-shell-carrier}, \autoref{thm:post-median-gate-shell-namecert-obligations}, and \autoref{thm:post-median-gate-shell-accepted-read-frontier} seed the finite post-median shell certificate over displayed codon, Boolean-coordinate, spectral, and first-exit rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary codon-table optimality, global evolutionary selection, unseen-window extrapolation, host biochemical causality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, six-row shell exposure, Boolean-coordinate exactness, spectral-optimum exactness, first-exit exactness, Nontrivial and FieldFaithful TasteGate instances, transport, replay, provenance, and local naming for BEDC.Derived.PostMedianGateShellUp.TasteGate.}
\end{closurestatus}
