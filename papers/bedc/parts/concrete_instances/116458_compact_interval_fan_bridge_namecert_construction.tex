\chapter{A Concrete Naming Certificate for $\CompactIntervalFanBridgeUp$}
\label{ch:concrete-instances-compact-interval-fan-bridge-namecert}
\origin{human}

Following Bishop--Bridges compact-interval analysis and Brouwer fan-theorem grounding, the $\CompactIntervalFanBridgeUp$ packet records the finite route by which compact interval data may be consumed through fan and bar finite-modulus rows before a Real-and-Completion reader reaches a terminal seal. It sits between the Bishop compact-interval source of \autoref{ch:concrete-instances-bishop-compact-interval-namecert}, the finite fan route of \autoref{ch:concrete-instances-finite-fan-theorem-namecert}, the stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite compact-interval fan bridge; it is not open-cover compactness, sequential compactness, a selected cover, quotient real equality, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Compact interval fan bridge carrier]
\label{def:compact-interval-fan-bridge-carrier}
A $\CompactIntervalFanBridgeUp$ carrier is a finite $\BHist$ packet
$$
G=(K,F,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\BishopCompactIntervalUp$ source row, recording the displayed endpoints, interval order data, and compact-interval admission surface.
\item $F$ is the fan and bar finite-modulus row, recording a finite prefix ledger, bar boundary, branch-depth ledger, and modulus output for the displayed interval request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, storing the dyadic precision request and every dyadic comparison used by the finite-modulus row.
\item $S$ is the $\StreamNameUp$ finite-window row, listing exactly the stream windows inspected before readback.
\item $R$ is the $\RegSeqRatUp$ readback row produced from $S$ and $D$.
\item $E$ is the terminal $\RealUp$ seal available only after $K,F,D,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact source, finite-modulus row, dyadic ledger, stream windows, regular readback, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a compact-interval request through $K,F,D,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalFanBridgeUp$, $\BishopCompactIntervalUp$, $\FiniteFanTheoremUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactIntervalFanBridgeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,F,D,S,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, sequential compactness, choice-selected covers, quotient real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact interval fan bridge NameCert obligations]
\label{thm:compact-interval-fan-bridge-namecert-obligations}
For every accepted $\CompactIntervalFanBridgeUp$ carrier $G=(K,F,D,S,R,E,H,C,P,N)$, the local $\NameCert_{\CompactIntervalFanBridgeUp}$ surface consists exactly of compact-interval source exposure, fan and bar finite-modulus exposure, dyadic tolerance accounting, finite $\StreamNameUp$ window exposure, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal discipline, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-interval-fan-bridge-carrier}. The analytic route is exhausted by $K,F,D,S,R,E$, and the structural route is exhausted by $H,C,P,N$.

The compact source row $K$ displays the interval before any fan data is read. The finite-modulus row $F$ records only a finite prefix and bar ledger, while the dyadic ledger $D$ records the tolerance comparisons used by that finite row.

The windows $S$, readback $R$, and seal $E$ are downstream of the displayed finite data. Transport, replay, provenance, and local naming preserve exactly that inventory, so the NameCert surface contains the listed obligations and no hidden open-cover, sequential, choice, quotient, trichotomy, or completeness endpoint.
\end{proof}

\begin{theorem}[Compact interval fan bridge finite route]
\label{thm:compact-interval-fan-bridge-finite-route}
Every Real-and-Completion consumer of an accepted $\CompactIntervalFanBridgeUp$ packet must read the finite route
$$
\BishopCompactIntervalUp \longmapsto \FiniteFanTheoremUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
through the displayed rows $K,F,D,S,R,E,H,C,P,N$. The exported bridge is the finite fan and bar modulus route from compact interval data to regular-rational readback, not an open-cover compactness theorem, sequential compactness theorem, choice-selected cover, quotient real equality, trichotomy decision, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:compact-interval-fan-bridge-namecert-obligations}. A consumer must expose the compact source, finite-modulus ledger, dyadic tolerance ledger, stream windows, regular readback, terminal seal, transport, replay, provenance, and local naming before reading the bridge.

The route reaches $\StreamNameUp$ only after the dyadic tolerance ledger and finite bar boundary have been displayed. It reaches $\RegSeqRatUp$ only through those finite windows, and it reaches $\RealUp$ only through the terminal seal row.

Since the carrier has no row for open covers, sequential compactness, selected covers, quotient real equality, trichotomy, or ambient completeness, the finite route is exhausted by $K,F,D,S,R,E,H,C,P,N$.
\end{proof}

\falsifiablePrediction{An accepted $\CompactIntervalFanBridgeUp$ consumer that exports a $\RealUp$ seal without displaying $K,F,D,S,R,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{bishop compact interval, finite fan theorem, streamname, dyadicratcore, regseqrat, real: $\CompactIntervalFanBridgeUp$ is not bijective to any listed sibling because it packages compact-interval source data, finite fan and bar modulus data, dyadic tolerance accounting, finite stream windows, regular rational readback, Real seal, transport, replay, provenance, and local NameCert rows in one bridge packet.}

\closureat{CompactIntervalFanBridgeUp}{seedStr}

\begin{closurestatus}{\CompactIntervalFanBridgeUp}
  \constructivestory{A $\CompactIntervalFanBridgeUp$ packet is generated from BishopCompactIntervalUp source rows, fan and bar finite-modulus rows, DyadicRatCoreUp tolerance ledgers, finite StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-fan-bridge-carrier}, \autoref{thm:compact-interval-fan-bridge-namecert-obligations}, and \autoref{thm:compact-interval-fan-bridge-finite-route} seed the finite compact-interval fan bridge for Real-and-Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, sequential compactness, selected covers, quotient real equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Formal verification requires a Lean carrier and checked NameCert obligations for the displayed compact interval fan bridge route.}
\end{closurestatus}
