\chapter{A Concrete Naming Certificate for $\MultihistCouplingUp$}
\label{ch:concrete-instances-multihistcoupling-namecert}
\origin{ai}

The $\MultihistCouplingUp$ packet realizes the paired-history coupling step in the Inter-Hist Locality chain of \autoref{ch:visions-inter-hist-locality}. It sits after the no-subject observer boundary of \autoref{ch:concrete-instances-observersubjectboundary-namecert}, the observer-history identity surface of \autoref{ch:concrete-instances-observerhistidentity-namecert}, the multi-history configuration carrier of \autoref{ch:concrete-instances-multihistconfig-namecert}, and the invariant readback packet of \autoref{ch:concrete-instances-interhistinvariant-namecert}. Its role is to expose a symmetric finite coupling ledger between two displayed histories before a $\CrossHistCausalUp$ consumer reads causal dependence or a rate packet reads a maximum boundary.

\begin{definition}[MultiHistCoupling carrier]
\label{def:multihist-coupling-carrier}
A $\MultihistCouplingUp$ carrier is a finite $\BHist$ packet
$$
K=(H_0,H_1,J,S,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $H_0$ and $H_1$ are paired $\BHist$ rows read from the same $\MultiHistConfigUp$ surface, with the no-subject boundary inherited from $\ObserverSubjectBoundaryUp$ and the identity-facing read inherited from $\ObserverHistIdentityUp$;
\item $J$ is the finite coupling ledger that records the two directed reads $H_0 \to H_1$ and $H_1 \to H_0$ as one symmetric coupling packet;
\item $S$ is the observer-symmetry witness, stating that the coupling ledger is not oriented by a privileged observer, global frame, synchronized clock, or subject-substance coordinate;
\item $T$ stores componentwise $\hsame$ transport for the two $\BHist$ rows and displayed $\psame$ transport for the probe and signature rows used by the coupling ledger;
\item $C$ records the displayed $\Cont$ routes by which a consumer reads $H_0$, reads $H_1$, checks $J$, replays $S$, and transports the same coupling packet through $T$;
\item $P$ is the $\Pkg$ provenance over $\MultihistCouplingUp$, $\ObserverSubjectBoundaryUp$, $\ObserverHistIdentityUp$, $\MultiHistConfigUp$, $\InterHistInvariantUp$, $\CrossHistCausalUp$, $\BHist$, $\hsame$, $\psame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\MultihistCouplingUp}$ row.
\end{enumerate}
The carrier exports no global observer frame, no host equality of histories, no quotient of history pairs, no subject-substance coordinate, and no causal-dependence row outside the displayed coupling ledger.
\end{definition}
\leanchecked{BEDC.Derived.MultihistCouplingUp.TasteGate.MultihistCouplingTasteGate\_single\_carrier\_alignment}

\begin{theorem}[MultiHistCoupling NameCert obligations]
\label{thm:multihist-coupling-namecert-obligations}
For the carrier of \autoref{def:multihist-coupling-carrier}, the local $\NameCert_{\MultihistCouplingUp}$ surface consists exactly of five finite obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $K=(H_0,H_1,J,S,T,C,P,N)$;
\item paired-history source exactness, requiring $H_0$ and $H_1$ to be read from the displayed $\MultiHistConfigUp$ row after the no-subject boundary and observer-history identity surfaces are available;
\item symmetric coupling ledger exactness, requiring $J$ and $S$ to expose both coupling directions without privileging either observer history;
\item transport stability through componentwise $\hsame$, displayed $\psame$, and replay of the listed $\Cont$ routes in $T,C$;
\item non-escape, excluding global frames, synchronized clocks, host history equality, quotient pairs, subject-substance rows, and any causal-dependence read not routed through $H_0,H_1,J,S,T,C,P,N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:multihist-coupling-carrier}. The tuple lists the paired histories, coupling ledger, symmetry witness, transports, continuation routes, provenance, and local name.

The source rows are consumed from the sibling surfaces. The no-subject boundary of \autoref{ch:concrete-instances-observersubjectboundary-namecert} prevents a hidden observer-substance coordinate, \autoref{ch:concrete-instances-observerhistidentity-namecert} supplies the observer-history identity read, and \autoref{ch:concrete-instances-multihistconfig-namecert} supplies the common finite multi-history configuration.

The ledger rows $J$ and $S$ then make the coupling symmetric. They record both directed reads as one packet and refuse any global-frame or privileged-observer orientation.

Finally $T,C,P,N$ replay, transport, package, and name the same finite rows. Since the carrier contains no coordinate for a host equality, quotient pair, subject-substance row, or external causal predicate, the five obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[MultiHistCoupling invariant handoff]
\label{thm:multihist-coupling-invariant-handoff}
Every accepted $\MultihistCouplingUp$ carrier
$$
K=(H_0,H_1,J,S,T,C,P,N)
$$
hands an $\InterHistInvariantUp$ or $\CrossHistCausalUp$ consumer exactly the paired-history rows, symmetric coupling ledger, observer-symmetry witness, $\hsame$ and $\psame$ transports, displayed $\Cont$ routes, package provenance, and local naming row. The consumer receives no global frame, no host equality of histories, no quotient pair, and no causal-dependence coordinate outside the displayed coupling packet.
\end{theorem}
\begin{proof}
Project the accepted carrier to its displayed tuple. The only history rows available to the consumer are $H_0$ and $H_1$, and the only coupling row available between them is $J$ with the symmetry witness $S$.

Apply the local NameCert obligations of \autoref{thm:multihist-coupling-namecert-obligations}. Paired-history source exactness fixes the upstream configuration and observer-boundary reads, while symmetric coupling ledger exactness fixes the two directed reads as one finite coupling surface.

The invariant consumer of \autoref{ch:concrete-instances-interhistinvariant-namecert} may therefore read the paired histories through $J,S,T,C,P,N$. A $\CrossHistCausalUp$ consumer may use that same finite route as its input surface, but any additional causal-dependence row must be displayed by the downstream causal packet rather than projected from this carrier.
\end{proof}
\leanchecked{BEDC.Derived.MultihistCouplingUp.TasteGate.MultihistCoupling\_invariant\_handoff}

\begin{theorem}[MultiHistCoupling pair-symmetry obligation]
\label{thm:multihist-coupling-pair-symmetry-obligation}
Every accepted $\MultihistCouplingUp$ carrier $K=(H_0,H_1,J,S,T,C,P,N)$ admits only the displayed symmetric coupling read. Swapping the two history rows must transport through the same joint row $J$, the source rows $H_0,H_1$, the observer-symmetry row $S$, the coupling route $C$, the provenance row $P$, and the local $\NameCert$ row $N$. The swapped read cannot introduce $\CrossHistCausalUp$ dependence, $\MaxCausalRateUp$ bounds, Lorentz geometry, global frames, or quotient history equality.
\end{theorem}
\begin{proof}
Project the carrier. The two visible history rows are $H_0$ and $H_1$, and the only row relating them is the symmetric coupling ledger $J$ together with the observer-symmetry witness $S$.

Use the invariant handoff theorem. It already routes any invariant or causal consumer through the paired-history rows, $J,S,T,C,P,N$, and it leaves any additional causal-dependence data to downstream packets.

Therefore swapping the displayed source and target histories changes only the order in which $H_0$ and $H_1$ are read. The same ledger, transport, replay, provenance, and local naming rows remain in force, while causal dependence, rate bounds, Lorentz geometry, global frames, and quotient history equality have no carrier coordinate.
\end{proof}

\begin{theorem}[MultiHistCoupling source exactness]
\label{thm:multihist-coupling-source-exactness}
Every accepted $\MultihistCouplingUp$ carrier $K=(H_0,H_1,J,S,T,C,P,N)$ reads its two history rows only through the displayed $\ObserverSubjectBoundaryUp$, $\ObserverHistIdentityUp$, and $\MultiHistConfigUp$ sibling surfaces. The rows $H_0$ and $H_1$ must be admitted as paired histories from the same finite multi-history configuration after the no-subject boundary and observer-history identity rows have been exposed; no global observer frame, subject-substance coordinate, host history equality, or quotient-pair source can replace those reads.
\end{theorem}
\begin{proof}
Project the carrier to the two source rows $H_0,H_1$. The definition attaches both rows to the same $\MultiHistConfigUp$ surface, not to an ambient history universe.

Read the sibling boundaries next. The no-subject boundary blocks subject-substance coordinates, and the observer-history identity surface fixes the identity-facing route by which an observer row is read as a history row.

The remaining rows $J,S,T,C,P,N$ consume the two admitted sources but do not create new sources. Hence every accepted source read is exact at the displayed observer-boundary, identity, and multi-configuration rows.
\end{proof}

\begin{theorem}[MultiHistCoupling ledger transport]
\label{thm:multihist-coupling-ledger-transport}
Swapping the displayed order of $H_0$ and $H_1$ in an accepted $\MultihistCouplingUp$ carrier preserves the coupling ledger rows $J,S,T,C,P,N$. The swapped packet reads the same two directed coupling entries in $J$, the same observer-symmetry witness $S$, the same componentwise transport $T$, the same replay row $C$, the same provenance row $P$, and the same local naming row $N$.
\end{theorem}
\begin{proof}
Use pair-symmetry, \autoref{thm:multihist-coupling-pair-symmetry-obligation}. It states that the swapped read must still transport through $H_0,H_1,J,S,C,P,N$ and cannot introduce a causal, rate, Lorentz, global-frame, or quotient-history coordinate.

The transport row $T$ records the componentwise $\hsame$ and displayed $\psame$ data for the same two history rows and ledger entries. Thus the swap changes only presentation order; it leaves the ledger, symmetry, transport, replay, provenance, and local naming rows intact.
\end{proof}

\begin{theorem}[MultiHistCoupling carrier admission obligation]
\label{thm:multihist-coupling-carrier-admission-obligation}
For the concrete carrier tuple $K=(H_0,H_1,J,S,T,C,P,N)$, carrier admission for $\MultihistCouplingUp$ is the finite $\BHist$ admission of the two paired history rows $H_0,H_1$, the symmetric coupling ledger $J$, the observer-symmetry row $S$, the componentwise $\hsame$ and displayed $\psame$ transport row $T$, the $\Cont$ replay row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\MultihistCouplingUp}$ row $N$. No admitted carrier may replace these rows by a global observer frame, host history equality, quotient-pair source, or causal-dependence predicate outside $K$.
\end{theorem}
\begin{proof}
Project the tuple displayed in \autoref{def:multihist-coupling-carrier}. The two source coordinates are $\BHist$ rows, and every additional coordinate is listed as transport, replay, provenance, or local naming data over those rows.

The admission test is therefore finite and componentwise. The $\hsame$ and $\psame$ entries in $T$ preserve the displayed rows, $C$ replays only those rows, $P$ names their package provenance, and $N$ records the local NameCert boundary. Since no excluded coordinate is present in $K$, none can be admitted by carrier projection.
\end{proof}

\begin{theorem}[MultiHistCoupling symmetric ledger exactness]
\label{thm:multihist-coupling-symmetric-ledger-exactness}
For every accepted carrier $K=(H_0,H_1,J,S,T,C,P,N)$, the coupling ledger is exactly the symmetric $\BHist$ row $J$ read together with $H_0,H_1,S,T,C,P,N$. It records the two displayed directed reads $H_0 \to H_1$ and $H_1 \to H_0$ as one coupling surface, transports them only by the listed $\hsame$ and $\psame$ rows, replays them only through $\Cont$, and packages them only through the displayed $\Pkg$ provenance and local $\NameCert$ row.
\end{theorem}
\begin{proof}
Read the ledger row $J$ from the carrier. By definition it is the single row that contains both directed coupling entries, while $S$ states that neither displayed history is privileged.

Now apply ledger transport, \autoref{thm:multihist-coupling-ledger-transport}. Swapping the displayed order of $H_0$ and $H_1$ preserves $J,S,T,C,P,N$, so the ledger cannot split into two unrelated oriented rows.

The remaining structure only transports, replays, packages, and names this same row. Thus symmetric ledger exactness is fixed by $J$ together with the support rows of $K$.
\end{proof}

\begin{theorem}[MultiHistCoupling transport stability]
\label{thm:multihist-coupling-transport-stability}
Let $K=(H_0,H_1,J,S,T,C,P,N)$ be an accepted $\MultihistCouplingUp$ carrier. If the displayed history rows, probe rows, and signature rows are transported through the componentwise $\hsame$ and displayed $\psame$ data in $T$, then the same coupling packet is replayed through $C$, retains the same $\Pkg$ provenance $P$, and keeps the same local $\NameCert$ row $N$. Transport cannot create a new history source, a new coupling ledger, a privileged observer row, or a downstream causal-dependence row.
\end{theorem}
\begin{proof}
Start with the transport row $T$. It names only componentwise $\hsame$ transport for $H_0,H_1$ and displayed $\psame$ transport for the probe and signature rows used by $J$.

Replay after transport is forced through $C$. The replay row reads the same histories, ledger, symmetry witness, transports, provenance, and local name, so no fresh source or ledger coordinate is introduced.

Finally use $P$ and $N$. Provenance keeps the packet inside the listed sibling surfaces, and the local NameCert records exactly the transported carrier. Hence transport stability preserves $K$ and blocks all excluded coordinates.
\end{proof}

\begin{theorem}[MultiHistCoupling obligation closure]
\label{thm:multihist-coupling-obligation-closure}
The $\MultihistCouplingUp$ paper surface is obligation-closed for the displayed paired-history packet: carrier admission is fixed by \autoref{thm:multihist-coupling-carrier-admission-obligation}, source exactness is fixed by \autoref{thm:multihist-coupling-source-exactness}, symmetric ledger exactness is fixed by \autoref{thm:multihist-coupling-symmetric-ledger-exactness}, transport stability is fixed by \autoref{thm:multihist-coupling-transport-stability}, and non-escape is fixed by the exclusions in \autoref{thm:multihist-coupling-namecert-obligations}.
\end{theorem}
\begin{proof}
Carrier admission supplies the finite tuple $K=(H_0,H_1,J,S,T,C,P,N)$ and no other coordinates.

Source exactness forces both histories through the observer-subject boundary, observer-history identity, and multi-history configuration rows. Symmetric ledger exactness then records both directed reads in $J$ with the observer-symmetry witness $S$.

Ledger transport preserves $J,S,T,C,P,N$ under swapped history order. The NameCert obligations exclude global frames, synchronized clocks, host history equality, quotient pairs, subject-substance rows, and causal-dependence reads outside the displayed packet. These clauses are exactly the obligation rows needed by the chapter surface.
\end{proof}

\begin{theorem}[MultiHistCoupling TasteGate obligation]
\label{thm:multihist-coupling-tastegate-obligation}
The later Lean \texttt{Derived/MultiHistCouplingUp/TasteGate.lean} development must provide explicit $\mathsf{Nontrivial}\ \MultihistCouplingUp$ and $\mathsf{FieldFaithful}\ \MultihistCouplingUp$ instances. These instances must preserve the two paired $\BHist$ rows, symmetric coupling ledger, observer-symmetry witness, $\hsame$ and $\psame$ transports, displayed $\Cont$ routes, package provenance, and local naming row.
\end{theorem}
\begin{proof}
The packet is not determined by either single history alone. It contains two history rows and a separate symmetric coupling ledger between them.

Nontriviality prevents collapse of $H_0$, $H_1$, and $J$ into one indistinguishable row. Field faithfulness is the projection discipline for $K=(H_0,H_1,J,S,T,C,P,N)$, preserving each displayed coordinate and keeping the coupling ledger distinct from the sibling no-subject, identity, configuration, invariant, and causal-dependence surfaces.
\end{proof}

\falsifiablePrediction{If the coupling row is omitted, no CrossHistCausalUp consumer can read the paired histories through a symmetric finite route.}

\independenceWitness{observerhistidentity, observersubjectboundary, multihistconfig, interhistinvariant, crosshistcausal: MultiHistCouplingUp is not bijective to any of them because it contains both paired-history rows and a symmetric coupling ledger; each sibling lacks either the paired coupling row, the no-subject boundary, the invariant output row, or the causal-dependence row.}

\closureat{MultiHistCouplingUp}{obligationStr}

\begin{closurestatus}{\MultihistCouplingUp}
  \constructivestory{A $\MultihistCouplingUp$ packet is generated from two paired $\BHist$ rows, a symmetric coupling ledger, an observer-symmetry witness, componentwise $\hsame$ and displayed $\psame$ transports, $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:multihist-coupling-carrier}, \autoref{thm:multihist-coupling-carrier-admission-obligation}, \autoref{thm:multihist-coupling-symmetric-ledger-exactness}, \autoref{thm:multihist-coupling-transport-stability}, \autoref{thm:multihist-coupling-obligation-closure}, and \autoref{thm:multihist-coupling-tastegate-obligation} specify the finite paired-history coupling packet, carrier admission, symmetric ledger exactness, transport stability, obligation-closure surface, and TasteGate instance surface.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.MultihistCouplingUp.TasteGate.MultihistCouplingTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close CrossHistCausalUp dependence, MaxCausalRateUp bounds, Lorentz geometry, global frames, quotient history equality, or bridge checking for $\MultihistCouplingUp$.}
  \upgradepath{Scoped closure requires binding the paired-history source, symmetric ledger, transport, non-escape, and TasteGate rows to a single named consumer scope.}
\end{closurestatus}
