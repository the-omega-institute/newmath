\chapter{A Concrete Naming Certificate for $\CauchyPeanoExistenceUp$}
\label{ch:concrete-instances-cauchy-peano-existence-namecert}
\origin{human}

Following Cauchy's polygonal method and Peano's local existence theorem, the $\CauchyPeanoExistenceUp$ packet records the finite constructive ODE-existence route in which Euler-polygon approximants are read through Cauchy data before any $\RealUp$ consumer sees a terminal seal. It sits over the local Peano source of \autoref{ch:concrete-instances-peano-existence-namecert}, the Euler-polygon approximation route of \autoref{ch:concrete-instances-cauchy-peano-euler-polygon-namecert}, the located interval surface of \autoref{ch:concrete-instances-locatedinterval-namecert}, the compact interval carrier of \autoref{ch:concrete-instances-bishop-compact-interval-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a Cauchy-filtered polygonal approximation handoff; it is not a selected solution curve, not a uniqueness theorem, not a maximal-flow construction, not host compactness, not quotient stream equality, and not countable choice.

\begin{definition}[Cauchy--Peano existence carrier]
\label{def:cauchy-peano-existence-carrier}
A $\CauchyPeanoExistenceUp$ carrier is a finite $\BHist$ packet
$$
P=(I,L,K,F,E,S,Q,R,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\PeanoExistenceUp$ interval-source row, binding the initial time, initial state, local radius, and finite time window.
\item $L$ is the $\LocatedIntervalUp$ endpoint row, exposing located lower and upper interval reads used by the local ODE window.
\item $K$ is the compact interval row, read through $\BishopCompactIntervalUp$ as finite located endpoint, closed-bounded, finite-net, and completion-facing data.
\item $F$ is the $\ContinuousMapUp$ vector-field row, available only through displayed graph and continuity-modulus ledgers on the source window.
\item $E$ is the $\CauchyPeanoEulerPolygonUp$ approximation row, carrying finite mesh budgets, polygon vertices, containment checks, and dyadic comparison cells.
\item $S$ is the $\StreamNameUp$ window row for the inspected polygonal prefix schedules.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed polygonal prefix and its Cauchy comparison budget.
\item $R$ is the terminal $\RealUp$ seal row for the local-existence readback.
\item $H$ records componentwise $\hsame$ transport for interval data, compact-window data, vector-field data, polygonal approximation rows, stream windows, regular rational readback, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
(I,L,K,F,E,S)\longmapsto Q\longmapsto R .
$$
\item $G$ is the $\Pkg$ provenance over $\CauchyPeanoExistenceUp$, $\PeanoExistenceUp$, $\CauchyPeanoEulerPolygonUp$, $\LocatedIntervalUp$, $\BishopCompactIntervalUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyPeanoExistenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,L,K,F,E,S,Q,R,H,C,G,N$. It has no coordinate for a chosen solution path, uniqueness, maximal extension, host compactness, quotient equality of streams, countable choice, Lean evidence, or bridge credentials.
\end{definition}

\begin{theorem}[Cauchy--Peano existence NameCert obligations]
\label{thm:cauchy-peano-existence-namecert-obligations}
For every accepted $\CauchyPeanoExistenceUp$ carrier
$$
P=(I,L,K,F,E,S,Q,R,H,C,G,N),
$$
the local $\NameCert_{\CauchyPeanoExistenceUp}$ surface consists exactly of carrier admission for the Peano interval source, located interval endpoint row, compact interval window row, continuous vector-field row, Cauchy--Peano Euler-polygon approximation row, stream-window row, regular rational readback, terminal Real seal, componentwise transport, displayed replay, package provenance, and local naming. Every local existence read is therefore routed through $I,L,K,F,E,S,Q,R,H,C,G,N$ before a consumer may inspect the terminal seal.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:cauchy-peano-existence-carrier}. The row $I$ supplies the Peano local-existence request, while $L$ and $K$ display the located and compact interval windows in which the request is inspected.

The row $F$ supplies the continuous vector-field data. It can be consumed only through its graph and continuity-modulus ledgers, so the carrier has no separate analytic entrance for a host solution curve.

The approximation content is carried by $E,S,Q,R$. The Euler-polygon row $E$ exposes finite mesh budgets and containment checks, $S$ records the inspected stream windows, $Q$ supplies the regular rational Cauchy readback, and $R$ seals the read as a $\RealUp$ output.

The structural rows $H,C,G,N$ transport, replay, package, and name exactly that route. Since no coordinate names a chosen solution path, uniqueness, maximal extension, host compactness, quotient equality, countable choice, Lean evidence, or bridge credential, none of those objects can be exported by the local NameCert surface.
\end{proof}

\begin{theorem}[Polygonal approximation handoff]
\label{thm:cauchy-peano-existence-polygonal-approximation-handoff}
For every accepted $\CauchyPeanoExistenceUp$ carrier
$$
P=(I,L,K,F,E,S,Q,R,H,C,G,N),
$$
every $\RealUp$ consumer of the terminal seal $R$ factors through the finite polygonal approximation route
$$
\PeanoExistenceUp
\longmapsto \CauchyPeanoEulerPolygonUp
\longmapsto \StreamNameUp
\longmapsto \RegSeqRatUp
\longmapsto \RealUp
$$
together with the located interval row $L$, compact interval row $K$, continuous-map row $F$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The handoff certifies only the Cauchy-filtered polygonal approximation data needed for a local existence read; it does not certify a selected solution curve, uniqueness, ambient compactness, quotient stream equality, or countable choice.
\end{theorem}
\begin{proof}
Start with a Real-facing consumer of $R$. The carrier exposes $R$ only after the regular rational readback $Q$ has been displayed, and $Q$ is downstream of the polygonal approximation row $E$ and stream-window row $S$.

Trace the route farther back. The row $E$ is itself admitted only relative to the Peano source row $I$, the located endpoint row $L$, the compact interval row $K$, and the continuous vector-field row $F$.

The structural rows preserve this dependency order. Componentwise $\hsame$ transport cannot introduce a second readback route, displayed $\Cont$ replay records the same finite path, and the provenance and naming rows bind the packet to the stated dependencies.

Therefore every terminal Real read factors through the displayed polygonal approximation handoff. Since the carrier contains no coordinate for a chosen solution path, uniqueness, ambient compactness theorem, quotient equality, or countable-choice schedule, the handoff cannot export those claims.
\end{proof}

\falsifiablePrediction{Every $\CauchyPeanoExistenceUp$ Real-facing read factors through PeanoExistenceUp, LocatedIntervalUp, BishopCompactIntervalUp, ContinuousMapUp, CauchyPeanoEulerPolygonUp, StreamNameUp, RegSeqRatUp, RealUp, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}

\independenceWitness{peano-existence, cauchy-peano-euler-polygon, locatedinterval, bishop-compact-interval, continuousmap, streamname, regseqrat, real: $\CauchyPeanoExistenceUp$ is not bijective to any listed sibling because it binds Peano local-existence data, located and compact interval windows, continuous vector-field rows, Cauchy-filtered Euler-polygon approximations, stream windows, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming in one displayed NameCert packet.}

\closureat{CauchyPeanoExistenceUp}{seedStr}

\begin{closurestatus}{\CauchyPeanoExistenceUp}
  \constructivestory{A $\CauchyPeanoExistenceUp$ packet is generated from a Peano interval-source row, LocatedInterval endpoint data, BishopCompactInterval compact-window data, a ContinuousMap vector-field row, a CauchyPeanoEulerPolygon approximation row, StreamName windows, RegSeqRat readback, terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-peano-existence-carrier}, \autoref{thm:cauchy-peano-existence-namecert-obligations}, and \autoref{thm:cauchy-peano-existence-polygonal-approximation-handoff} seed the finite Cauchy-filtered polygonal route from Peano local-existence data to RealUp readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected solution curves, uniqueness, maximal extension, host compactness, quotient stream equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Peano source exposure, located interval exposure, compact interval window exposure, continuous-map routing, CauchyPeanoEulerPolygon handoff, StreamName and RegSeqRat readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyPeanoExistenceUp.}
\end{closurestatus}
