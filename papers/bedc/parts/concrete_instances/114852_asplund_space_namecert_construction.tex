\chapter{A Concrete Naming Certificate for $\AsplundSpaceUp$}
\label{ch:concrete-instances-asplund-space-namecert}
\origin{human}

Following Asplund and the standard Banach-space theory of separable dual restrictions, the $\AsplundSpaceUp$ packet records the finite BEDC surface by which a Banach carrier exposes separable-subspace dual witnesses without importing a host dual-space theorem. It sits over the Banach-space surface of \autoref{ch:concrete-instances-banachspace-namecert}, the norm row of \autoref{ch:concrete-instances-norm-namecert}, the vector-space source of \autoref{ch:concrete-instances-vecspace-namecert}, the functional-analysis handoff of \autoref{ch:concrete-instances-functionalanalysis-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite Asplund interface for downstream weak compactness, differentiability, and dual-separability consumers; it does not choose all separable subspaces, construct a global dual object, quotient functionals, assert Radon--Nikodym differentiability, or import a host weak topology.

\begin{definition}[Asplund space carrier]
\label{def:asplund-space-carrier}
An $\AsplundSpaceUp$ carrier is a finite $\BHist$ packet
$$
A=(B,V,N,F,S,D,R,T,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BanachSpaceUp$ carrier row, exposing the accepted complete normed vector-space source inspected by the packet.
\item $V$ is the $\VecSpaceUp$ row for the vector operations visible inside the accepted Banach carrier.
\item $N$ is the $\NormUp$ row recording the norm observations and comparison ledgers used by separable-subspace reads.
\item $F$ is the $\FunctionalAnalysisUp$ route row binding bounded-linear-functional requests to the same Banach, vector, and norm source.
\item $S$ is the separable-subspace window row, carrying only the finite named subspace probes and dense-window requests currently under inspection.
\item $D$ is the separable-dual witness row, recording the finite dual-probe windows read from $S$ through $F$.
\item $R$ is the terminal $\RealUp$ seal for norm and functional-value comparisons reached after $B,V,N,F,S,D$ have been displayed.
\item $T$ records componentwise $\hsame$ transport for Banach, vector, norm, functional-analysis, separable-window, dual-witness, Real-seal, and local certificate rows.
\item $C$ records displayed $\Cont$ replay from Asplund-space requests through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\AsplundSpaceUp$, $\BanachSpaceUp$, $\VecSpaceUp$, $\NormUp$, $\FunctionalAnalysisUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\AsplundSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for a host dual Banach space, quotient functionals, all separable subspaces, weak-star compactness, differentiability theorems, selected dense sequences, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Asplund space NameCert obligations]
\label{thm:asplund-space-namecert-obligations}
For every accepted $\AsplundSpaceUp$ carrier
$$
A=(B,V,N,F,S,D,R,T,C,P,L),
$$
the local $\NameCert_{\AsplundSpaceUp}$ surface consists exactly of carrier admission for the $\BanachSpaceUp$ row $B$, $\VecSpaceUp$ row $V$, $\NormUp$ row $N$, $\FunctionalAnalysisUp$ route $F$, separable-subspace window $S$, separable-dual witness row $D$, terminal $\RealUp$ seal $R$, componentwise transport $T$, displayed replay $C$, provenance $P$, and local naming $L$. Every Asplund consumer must read separable-dual data through those rows and cannot import a host dual space, a quotient functional universe, a selected dense sequence, a global weak topology, Radon--Nikodym differentiability, or ambient real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:asplund-space-carrier}. The accepted Banach, vector, and norm source is exactly $B,V,N$, so the packet has no independent host-space coordinate.

Next read the functional-analysis and separable-dual surface through $F,S,D$. These rows bind every dual-facing request to the displayed separable-subspace window before the terminal Real seal can be consumed.

Finally attach $R,T,C,P,L$. They seal, transport, replay, source, and name only the displayed rows, which leaves no route to a host dual Banach space, quotient functionals, selected dense sequences, weak-star compactness, differentiability theorems, or ambient real completeness.
\end{proof}

\begin{theorem}[Asplund separable dual route]
\label{thm:asplund-space-separable-dual-route}
Every separable-dual consumer of an accepted $\AsplundSpaceUp$ carrier must first expose the finite route
$$
\BanachSpaceUp \longrightarrow \VecSpaceUp \longrightarrow \NormUp \longrightarrow \FunctionalAnalysisUp \longrightarrow \RealUp
$$
through the rows $B,V,N,F,S,D,R,T,C,P,L$ of \autoref{def:asplund-space-carrier}. The route is a displayed separable-subspace dual witness surface, not a global dual-space construction, quotient-functional equality, selected dense sequence, weak compactness theorem, differentiability theorem, or host Banach-space theorem.
\end{theorem}
\begin{proof}
Start from \autoref{thm:asplund-space-namecert-obligations}. It places $B,V,N,F$ before the separable-subspace window and dual-witness rows can be inspected.

The rows $S$ and $D$ then localize the dual read to the named finite subspace window. The terminal Real seal $R$ records only norm and functional-value comparisons coming from that window.

The structural rows $T,C,P,L$ preserve this same route. Since no carrier coordinate names all separable subspaces, a host dual object, quotient equality of functionals, selected dense sequences, weak compactness, or differentiability, the separable-dual route exports only the displayed NameCert packet.
\end{proof}

\falsifiablePrediction{Every accepted $\AsplundSpaceUp$ separable-dual read factors through displayed $\BanachSpaceUp$, $\VecSpaceUp$, $\NormUp$, $\FunctionalAnalysisUp$, separable-window, separable-dual witness, $\RealUp$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows before any downstream weak-compactness or differentiability consumer output is visible.}

\independenceWitness{banachspace, vecspace, norm, functionalanalysis, real: none of the listed siblings alone is bijective with $\AsplundSpaceUp$ because the packet jointly carries a Banach source, vector operations, norm reads, functional-analysis routing, separable-subspace windows, separable-dual witnesses, a terminal Real seal, transport, replay, provenance, and local naming.}

\closureat{AsplundSpaceUp}{seedStr}

\begin{closurestatus}{\AsplundSpaceUp}
  \constructivestory{An $\AsplundSpaceUp$ packet is generated from BanachSpace carrier rows, VecSpace operation rows, Norm comparison rows, FunctionalAnalysis handoff rows, finite separable-subspace windows, separable-dual witness rows, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:asplund-space-carrier}, \autoref{thm:asplund-space-namecert-obligations}, and \autoref{thm:asplund-space-separable-dual-route} seed the finite Asplund-space NameCert surface over displayed Banach, vector, norm, functional-analysis, separable-window, dual-witness, real-seal, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host dual Banach space, quotient functionals, all separable subspaces, selected dense sequences, weak compactness, differentiability, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Banach-space exposure, vector and norm routing, functional-analysis handoff, separable-subspace window exactness, separable-dual witness readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.AsplundSpaceUp.}
\end{closurestatus}
