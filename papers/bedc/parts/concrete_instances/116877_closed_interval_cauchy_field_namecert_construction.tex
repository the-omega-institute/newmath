\chapter{A Concrete Naming Certificate for $\ClosedIntervalCauchyFieldUp$}
\label{ch:concrete-instances-closed-interval-cauchy-field-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\ClosedIntervalCauchyFieldUp$ packet records the interval-local regular Cauchy field rows available on a displayed closed bounded interval. It sits after the closed interval source of \autoref{ch:concrete-instances-closedboundedinterval-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the local Cauchy-field corridor without claiming general $\RealUp$ completeness, quotient stream equality, selected limits, countable choice, or an ambient field-completion principle.

\begin{definition}[Closed-interval Cauchy-field carrier]\label{def:closed-interval-cauchy-field-carrier}
A $\ClosedIntervalCauchyFieldUp$ carrier is a finite $\BHist$ packet
$$
F=(I,S,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the closed interval source row, exposing the accepted $\ClosedBoundedIntervalUp$ lower endpoint, upper endpoint, endpoint-order, containment, finite-window, readback, and Real-seal boundary.
\item $S$ is the finite $\StreamNameUp$ window row used to inspect only the displayed interval-local observations.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the requested precision, endpoint containment, and local comparison budget.
\item $Q$ is the $\RegSeqRatUp$ readback row produced from the same finite window and dyadic tolerance route.
\item $E$ is the terminal $\RealUp$ seal row, naming the real-facing value only after $I,S,D,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the interval source, stream window, dyadic tolerance ledger, regular rational readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from interval-source admission through finite windows, tolerance checks, readback, and terminal sealing.
\item $P$ is the $\Pkg$ provenance over $\ClosedIntervalCauchyFieldUp$, $\ClosedBoundedIntervalUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ClosedIntervalCauchyFieldUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only by $I,S,D,Q,E,H,C,P,N$. It has no coordinate for quotient streams, selected limit points, countable choice, host real-field completion, or an ambient proof that every Cauchy family on $\RealUp$ converges.
\end{definition}

\begin{theorem}[Closed-interval Cauchy-field NameCert obligations]\label{thm:closed-interval-cauchy-field-namecert-obligations}
For every accepted $\ClosedIntervalCauchyFieldUp$ carrier
$$
F=(I,S,D,Q,E,H,C,P,N),
$$
the local $\NameCert_{\ClosedIntervalCauchyFieldUp}$ surface consists exactly of closed interval source exposure, finite StreamName window admission, DyadicRatCore tolerance accounting, RegSeqRat readback, terminal RealUp sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and the local naming row. Every interval-local Cauchy-field read must pass through $I,S,D,Q,E,H,C,P,N$ before it can be consumed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:closed-interval-cauchy-field-carrier}. The rows $I,S,D,Q,E,H,C,P,N$ are the whole accepted packet.

The source and analytic rows are exposed first. The closed interval source $I$ fixes the endpoint and containment boundary, the window row $S$ exposes the finite observations, the dyadic row $D$ records the requested tolerance, and $Q$ reads those observations as regular rational data.

The remaining rows preserve the route. The Real seal $E$ is downstream of $I,S,D,Q$, while $H,C,P,N$ transport, replay, source, and name exactly those rows. Since no other coordinate is present, quotient streams, selected limits, countable choice, and ambient completeness cannot be exported by the local NameCert.
\end{proof}

\begin{theorem}[Closed-interval Cauchy-field window readback]\label{thm:closed-interval-cauchy-field-window-readback}
Every accepted $\ClosedIntervalCauchyFieldUp$ read that reaches the terminal $\RealUp$ seal first displays the finite lattice
$$
\ClosedBoundedIntervalUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The readback is interval-local and finite; it does not identify quotient streams, select a global limit, invoke countable choice, or import ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Let $F=(I,S,D,Q,E,H,C,P,N)$ be an accepted carrier. The source row $I$ is the only interval entry point, so the read begins with the closed bounded interval boundary.

The finite window and tolerance rows then determine the local request. The row $S$ exposes the inspected StreamName observations, $D$ records the DyadicRatCore precision and containment budget, and $Q$ replays the same data as RegSeqRat readback.

Only after this route is visible can the seal row $E$ be read. Transport, replay, provenance, and naming do not add a hidden stream quotient or chosen limit; they preserve the displayed finite lattice.
\end{proof}

\begin{theorem}[Closed-interval Cauchy-field Real-seal nonescape]\label{thm:closed-interval-cauchy-field-real-seal-nonescape}
An accepted $\ClosedIntervalCauchyFieldUp$ carrier exports the terminal $\RealUp$ row only as the seal of the displayed closed-interval Cauchy-field route. No consumer may read that seal as a proof of general Real completeness, a selected completion point for an arbitrary Cauchy stream, a countable-choice family, or a host field-completion operator.
\end{theorem}
\begin{proof}
Use \autoref{thm:closed-interval-cauchy-field-namecert-obligations}. Every consumer reaches the local naming row through the closed interval source, finite window, dyadic tolerance ledger, regular readback, and terminal Real seal.

Apply \autoref{thm:closed-interval-cauchy-field-window-readback}. The exported Real row is tied to the finite interval-local lattice and has no coordinate for a quotient stream, arbitrary Cauchy family, selected limit, or countable-choice schedule.

Thus the seal is a terminal row of this packet, not a global completeness theorem. Any consumer that treats it as an ambient field-completion principle leaves the displayed carrier.
\end{proof}

\closureat{ClosedIntervalCauchyFieldUp}{seedStr}

\begin{closurestatus}{\ClosedIntervalCauchyFieldUp}
  \constructivestory{A $\ClosedIntervalCauchyFieldUp$ packet is generated from a ClosedBoundedIntervalUp source row, finite StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:closed-interval-cauchy-field-carrier}, \autoref{thm:closed-interval-cauchy-field-namecert-obligations}, \autoref{thm:closed-interval-cauchy-field-window-readback}, and \autoref{thm:closed-interval-cauchy-field-real-seal-nonescape} seed the interval-local Cauchy-field packet over finite readback rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, general Real completeness, quotient stream equality, selected limits, countable choice, or an ambient field-completion operator.}
  \upgradepath{Obligation closure requires checked carrier admission, window readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ClosedIntervalCauchyFieldUp.}
\end{closurestatus}
