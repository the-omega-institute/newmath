\chapter{A Concrete Naming Certificate for $\ZNormalUp$}
\label{ch:concrete-instances-znormal-namecert}
\origin{ai}

The $\ZNormalUp$ packet realizes the total-host boundary named by \autoref{ch:visions-strong-normalization-vs-turing-completeness} as finite normal-form certificate data. It sits beside the typed endpoint packet of \autoref{ch:concrete-instances-typedreductionnormalform-namecert}, the finite Zeckendorf normal-form packet of \autoref{ch:concrete-instances-zeckendorf-normal-namecert}, and the terminal continuation packet of \autoref{ch:concrete-instances-continuationtracenormalform-namecert}. The packet records terminal normality, bounded fuel replay, and non-escape from total host evidence without exporting a divergence oracle, host conversion row, or global normalization procedure.

\falsifiablePrediction{Every accepted ZNormalUp packet exposes a finite terminal-normality row and admits no consumer route to a divergence oracle or host conversion row.}

\independenceWitness{Nearest siblings: typedreductionnormalform, zeckendorf-normal, zcarry, continuationtracenormalform, and normalformconsistencyseal. None is bijective to $\ZNormalUp$ because typedreductionnormalform records a typed endpoint route, zeckendorf-normal records a finite combinatorial normal-word classifier, zcarry records a local carry classifier, continuationtracenormalform records a terminal continuation trace, and normalformconsistencyseal records a consistency-facing seal; $\ZNormalUp$ keeps the total-host normal-form boundary itself as a finite packet.}

\begin{definition}[$\ZNormalUp$ carrier]
\label{def:znormal-carrier}
A $\ZNormalUp$ carrier is a finite $\BHist$ packet
$$
Z=(T,F,V,K,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed reduction normal-form row read through $\TypedReductionNormalFormUp$;
\item $F$ is the finite fuel row naming the bounded host reduction segment;
\item $V$ is the terminal-normality row asserting that the segment ends at a normal form;
\item $K$ is the Zeckendorf and carry-facing normal-form comparison row read through $\ZeckendorfNormalUp$ and $\ZCarryUp$ where a finite word boundary is present;
\item $R$ is the continuation terminal-read row read through $\ContinuationTraceNormalFormUp$;
\item $H$ records componentwise $\hsame$ transport for typed endpoint, fuel, terminal normality, finite word comparison, continuation read, provenance, and local naming rows;
\item $C$ records the displayed $\Cont$ routes from typed endpoint to fuel replay, from fuel replay to terminal normality, from terminal normality to the continuation read, and from those rows to the consumer-facing packet boundary;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:visions-strong-normalization-vs-turing-completeness}, \autoref{ch:concrete-instances-typedreductionnormalform-namecert}, \autoref{ch:concrete-instances-zeckendorf-normal-namecert}, \autoref{ch:concrete-instances-continuationtracenormalform-namecert}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\ZNormalUp}$ row.
\end{enumerate}
The carrier accepts only packets whose normal-form claim is read through a finite terminal row. It has no coordinate for a divergence oracle, host conversion row, quotient computation, unrestricted evaluator, or ambient proof that every external computation halts.
\end{definition}

\begin{theorem}[$\ZNormalUp$ NameCert obligations]
\label{thm:znormal-namecert-obligations}
For the carrier of \autoref{def:znormal-carrier}, the local $\NameCert_{\ZNormalUp}$ surface consists exactly of carrier admission for
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
typed endpoint readback through $T$, finite fuel exposure through $F$, terminal-normality exposure through $V$, finite normal-word comparison through $K$, continuation terminal-read exposure through $R$, componentwise transport through $H$, displayed route replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. The rows $T,F,V,K,R,H,C,P,N$ are the only fields of an accepted $\ZNormalUp$ packet.

Read the endpoint rows first. The typed reduction row $T$ is supplied by \autoref{ch:concrete-instances-typedreductionnormalform-namecert}; the finite fuel row $F$ and terminal-normality row $V$ bind that endpoint to a displayed total-host reduction segment.

Read the sibling normal-form rows next. The row $K$ may cite the finite word discipline of \autoref{ch:concrete-instances-zeckendorf-normal-namecert}, while $R$ may cite the terminal trace surface of \autoref{ch:concrete-instances-continuationtracenormalform-namecert}. These rows are auxiliary certificate coordinates, not routes to host conversion or general normalization.

Finally inspect $H,C,P,N$. They transport, replay, package, and name the same finite surface. Since no carrier coordinate names divergence, quotient computation, host conversion, or an unrestricted evaluator, the listed obligations exhaust the packet.
\end{proof}

\begin{theorem}[$\ZNormalUp$ carry normalization obligation]
\label{thm:znormal-carry-normalization-obligation}
For every accepted $\ZNormalUp$ packet
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
the row $K$ is obligated to expose finite normal-word evidence through the $\ZeckendorfNormalUp$ surface of \autoref{ch:concrete-instances-zeckendorf-normal-namecert} and the local carry evidence of \autoref{ch:concrete-instances-zcarry-namecert}. The exposure is a $\BHist$ row transported by $\hsame$, replayed by $\Cont$, packaged by $\Pkg$, and named by $\NameCert_{\ZNormalUp}$; it is not a host conversion row, quotient computation, unrestricted evaluator, or global normalizer.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:znormal-carrier}. The only finite normal-word comparison coordinate is $K$, and it is listed beside the typed endpoint, fuel, terminal-normality, and continuation rows rather than outside the packet.

Use the NameCert obligation surface of \autoref{thm:znormal-namecert-obligations}. It binds $K$ to the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local naming rows as the rest of the accepted packet.

The sibling evidence is then read through the Zeckendorf and carry-facing chapters named in the carrier. Since the carrier has no coordinate for host conversion, quotient computation, unrestricted evaluation, or global normalization, the $K$ obligation is exactly finite normal-word and carry evidence.
\end{proof}

\begin{theorem}[$\ZNormalUp$ continuation terminal route]
\label{thm:znormal-continuation-terminal-route}
For every accepted $\ZNormalUp$ packet, the continuation row $R$ is reached only after the typed endpoint $T$, finite fuel $F$, and terminal-normality row $V$ have been displayed. Its consumer route is the $\ContinuationTraceNormalFormUp$ terminal-read surface of \autoref{ch:concrete-instances-continuationtracenormalform-namecert} transported through $H$, replayed through $C$, packaged through $P$, and named through $N$.
\end{theorem}
\begin{proof}
Start from the finite packet $Z=(T,F,V,K,R,H,C,P,N)$. The row order places $R$ after $T,F,V$ and makes it a continuation terminal-read row, not a source of unbounded evaluation.

Apply \autoref{thm:znormal-namecert-obligations}. The theorem exposes typed endpoint readback, finite fuel, terminal normality, continuation terminal-read, and the structural rows in one local $\NameCert_{\ZNormalUp}$ surface.

Now read the sibling terminal trace through \autoref{ch:concrete-instances-continuationtracenormalform-namecert}. The transport, continuation, package, and naming rows are $H,C,P,N$, so no host conversion, quotient computation, unrestricted evaluator, or global normalizer can replace the terminal continuation route.
\end{proof}

\begin{theorem}[$\ZNormalUp$ typed boundary exhaustion]
\label{thm:znormal-typed-boundary-exhaustion}
Every consumer of the displayed $\ZNormalUp$ rows
$$
T,F,V,K,R,H,C,P,N
$$
must factor through the typed endpoint of \autoref{ch:concrete-instances-typedreductionnormalform-namecert}, the finite normal-word and carry row of \autoref{thm:znormal-carry-normalization-obligation}, the continuation terminal route of \autoref{thm:znormal-continuation-terminal-route}, and the local $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The boundary exports no host conversion, quotient computation, unrestricted evaluator, divergence oracle, or global normalizer.
\end{theorem}
\begin{proof}
First expose the typed side. The carrier definition and \autoref{thm:znormal-namecert-obligations} make $T,F,V$ the displayed route from typed endpoint through finite fuel to terminal normality.

Next expose the two downstream rows. The carry-normalization obligation fixes $K$ as finite Zeckendorf and carry evidence, while the continuation-terminal theorem fixes $R$ as the terminal read through the displayed continuation trace.

Finally collect $H,C,P,N$. These rows provide the only componentwise transport, replay, provenance, and local naming surface for all consumer reads. Since none of the cited rows contains host conversion, quotient computation, unrestricted evaluation, divergence detection, or global normalization, the typed boundary is exhausted by the displayed packet.
\end{proof}

\begin{theorem}[$\ZNormalUp$ fuel normality non-escape]
\label{thm:znormal-fuel-normality-nonescape}
Every accepted $\ZNormalUp$ packet exposes its terminal normal-form claim only through the finite route
$$
T \longmapsto F \longmapsto V \longmapsto R
$$
with the normal-word comparison row $K$ available only as displayed sibling evidence. No consumer can use the packet to read a divergence oracle, a host conversion row, a quotient computation, an unrestricted evaluator, or a global normalizer.
\end{theorem}
\begin{proof}
Start from the typed endpoint row $T$. By construction it is a packet-visible endpoint row rather than a host conversion judgment.

Next read $F$ and $V$. The fuel row names the finite replay segment, and the terminal-normality row records the endpoint of that segment. A consumer therefore receives a bounded normal-form packet, not an unbounded computation.

Use $R$ and $K$ only after the terminal row is displayed. The continuation trace row replays the terminal read, while the Zeckendorf and carry-facing row records finite word comparison where that sibling surface is relevant.

The excluded objects have no carrier coordinate. Thus transport, continuation replay, provenance, and local naming cannot manufacture a divergence oracle, host conversion row, quotient computation, unrestricted evaluator, or global normalizer from the accepted packet.
\end{proof}

\begin{theorem}[$\ZNormalUp$ total-host boundary]
\label{thm:znormal-total-host-boundary}
The $\ZNormalUp$ packet gives a concrete finite interface for the host-side total-language boundary of \autoref{ch:visions-strong-normalization-vs-turing-completeness}. A public consumer may read the typed endpoint, finite fuel, terminal normality, finite normal-word comparison, continuation terminal read, transports, routes, provenance, and local name of
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
but it may not reread this surface as Turing completeness of the host, as a proof that every substrate computation halts, or as a route from substrate universality to host-level partial functions.
\end{theorem}
\begin{proof}
First bind the host-side surface. The carrier of \autoref{def:znormal-carrier} records exactly the typed endpoint, finite replay, terminal-normality, sibling normal-word, continuation-read, transport, route, provenance, and naming rows.

Then compare this surface with \autoref{ch:visions-strong-normalization-vs-turing-completeness}. That chapter separates the total host from the universal substrate. The $\ZNormalUp$ packet inhabits the host side of that separation because every accepted read is through finite terminal-normality rows.

Finally apply \autoref{thm:znormal-fuel-normality-nonescape}. It excludes divergence, host conversion, quotient computation, unrestricted evaluation, and global normalization routes. Consequently no consumer can turn the finite normal-form packet into host Turing completeness or a substrate halting theorem.
\end{proof}

\begin{theorem}[$\ZNormalUp$ terminal readback exactness]
\label{thm:znormal-terminal-readback-exactness}
For any accepted $\ZNormalUp$ packet
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
every terminal read factors through the displayed rows $T,F,V,R,H,C,P,N$. The read may consume the sibling normal-word row $K$ only after the terminal-normality row $V$ is displayed, and it cannot consult host conversion, quotient computation, unrestricted evaluation, or a global normalizer.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:znormal-carrier}. The terminal read row is $R$, and the only rows before it are the typed endpoint $T$, the finite fuel row $F$, and the terminal-normality row $V$.

Use the displayed continuation routes in $C$. They replay the path from $T$ through $F$ and $V$ to $R$, while $H,P,N$ transport, package, and name that same route.

The row $K$ is a sibling normal-word coordinate rather than a terminal readback source. Since no coordinate names host conversion, quotient computation, unrestricted evaluation, or a global normalizer, every accepted terminal read is exactly the displayed finite readback surface.
\end{proof}

\begin{theorem}[$\ZNormalUp$ sibling normal-word handoff]
\label{thm:znormal-sibling-normalword-handoff}
The $K$ row of an accepted $\ZNormalUp$ packet may consume the sibling surfaces of \autoref{ch:concrete-instances-typedreductionnormalform-namecert}, \autoref{ch:concrete-instances-zeckendorf-normal-namecert}, \autoref{ch:concrete-instances-zcarry-namecert}, and \autoref{ch:concrete-instances-continuationtracenormalform-namecert} only as displayed finite evidence. It does not provide host computation, a conversion oracle, a quotient normalizer, or a proof that all external computations normalize.
\end{theorem}
\begin{proof}
Read the sibling handoff through the carrier coordinates. The typed endpoint row $T$, finite fuel row $F$, terminal row $V$, comparison row $K$, and continuation row $R$ are separate displayed fields.

The cited sibling chapters supply finite certificate surfaces: typed endpoint evidence, finite Zeckendorf normal-word evidence, local carry evidence, and terminal continuation-trace evidence. The $K$ row can package those surfaces only through the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the accepted packet.

No carrier coordinate supplies a host computation relation, conversion oracle, quotient normalizer, or external normalization theorem. Thus the sibling handoff remains finite evidence rather than a route to total host computation beyond the packet.
\end{proof}

\begin{theorem}[$\ZNormalUp$ obligation closure package]
\label{thm:znormal-obligation-closure-package}
The carrier admission of \autoref{def:znormal-carrier}, the NameCert obligations of \autoref{thm:znormal-namecert-obligations}, the fuel non-escape boundary of \autoref{thm:znormal-fuel-normality-nonescape}, the total-host boundary of \autoref{thm:znormal-total-host-boundary}, the terminal readback exactness of \autoref{thm:znormal-terminal-readback-exactness}, and the sibling normal-word handoff of \autoref{thm:znormal-sibling-normalword-handoff} assemble the five-row $\obligationClosure$ surface for $\ZNormalUp$ over $T,F,V,K,R,H,C,P,N$.
\end{theorem}
\begin{proof}
First fix the accepted carrier $Z=(T,F,V,K,R,H,C,P,N)$. Carrier admission and local naming expose only the rows listed in \autoref{def:znormal-carrier}.

Apply \autoref{thm:znormal-namecert-obligations} to bind the NameCert surface. Then use \autoref{thm:znormal-fuel-normality-nonescape} and \autoref{thm:znormal-total-host-boundary} to keep every consumer read inside finite fuel, terminal normality, continuation readback, and total-host boundary evidence.

Finally use \autoref{thm:znormal-terminal-readback-exactness} and \autoref{thm:znormal-sibling-normalword-handoff}. These identify the terminal read and the sibling normal-word handoff as displayed finite rows, so the carrier, NameCert, fuel, terminal readback, sibling handoff, and host-boundary components form one obligation package.
\end{proof}

\begin{theorem}[$\ZNormalUp$ route transport]
\label{thm:znormal-route-transport}
For every accepted $\ZNormalUp$ packet
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
the route from the typed endpoint row $T$ through finite fuel $F$, terminal normality $V$, the sibling normal-word row $K$, and the terminal continuation read $R$ is transported only by the displayed componentwise row $H$ and replayed only by the displayed continuation row $C$. No accepted route can insert host conversion, quotient computation, unrestricted evaluation, or a global normalizer between these coordinates.
\end{theorem}
\begin{proof}
Project the accepted packet through \autoref{def:znormal-carrier}. The only route-bearing coordinates exposed by the carrier are the componentwise transport row $H$ and the continuation replay row $C$.

Apply \autoref{thm:znormal-namecert-obligations} to bind these rows to the local $\NameCert_{\ZNormalUp}$ surface. Then use \autoref{thm:znormal-fuel-normality-nonescape} to keep the path from $T$ through $F$ and $V$ inside finite fuel and terminal-normality evidence.

Finally apply \autoref{thm:znormal-terminal-readback-exactness} and \autoref{thm:znormal-sibling-normalword-handoff}. The terminal read and sibling normal-word row are reached through the same displayed transport and replay rows, so no external normalizer or host computation route is available.
\end{proof}

\begin{theorem}[$\ZNormalUp$ root terminal non-escape interface]
\label{thm:znormal-root-terminal-nonescape-interface}
Every root consumer of an accepted $\ZNormalUp$ packet must read terminal normality through the displayed path
$$
T \to F \to V \to R,
$$
with $K$ available only as finite sibling normal-word and carry evidence from $\ZeckendorfNormalUp$ and $\ZCarryUp$. No host conversion, quotient computation, unrestricted evaluator, divergence oracle, or global normalizer can enter the root-facing interface.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:znormal-fuel-normality-nonescape}. It places the terminal normal-form claim on the finite route from typed endpoint $T$ through fuel $F$ and terminal row $V$ to continuation read $R$.

Apply \autoref{thm:znormal-terminal-readback-exactness}. The terminal read factors through $T,F,V,R,H,C,P,N$, and the row $K$ is consumed only after $V$ is already displayed.

Use \autoref{thm:znormal-sibling-normalword-handoff} to identify $K$ as finite sibling evidence from $\ZeckendorfNormalUp$ and $\ZCarryUp$. Then apply \autoref{thm:znormal-route-transport} to keep the whole root route inside $H,C,P,N$ without host conversion, quotient computation, unrestricted evaluation, divergence, or global normalization.
\end{proof}

\begin{theorem}[$\ZNormalUp$ root carrier exposure]
\label{thm:znormal-root-unblock-carrier-exposure}
Every root consumer of an accepted $\ZNormalUp$ packet first sees exactly the displayed tuple
$$
Z=(T,F,V,K,R,H,C,P,N).
$$
The rows expose the typed endpoint, finite fuel, terminal normality, sibling normal-word evidence, terminal readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local $\NameCert$ naming. No root-unblock consumer receives a divergence oracle, host conversion row, quotient computation, unrestricted evaluator, or global normalizer outside those coordinates.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:znormal-carrier}. It lists $T,F,V,K,R,H,C,P,N$ as the complete finite packet and gives no other coordinate to a root consumer.

Apply \autoref{thm:znormal-namecert-obligations}. The NameCert surface identifies the same fields as the complete local naming, transport, route, provenance, and package surface.

Use \autoref{thm:znormal-fuel-normality-nonescape} and the displayed TasteGate contract row. The finite fuel boundary and the carrier-faithful TasteGate reading keep the exposed tuple from becoming a route to host conversion, quotient computation, unrestricted evaluation, divergence, or global normalization.
\end{proof}

\begin{theorem}[$\ZNormalUp$ root terminal route]
\label{thm:znormal-root-unblock-terminal-route}
For every accepted $\ZNormalUp$ packet, the terminal normal-form route used by root consumers is
$$
T \to F \to V \to R,
$$
with $K$ available only as sibling normal-word evidence. The route is transported through $H$, replayed through $C$, packaged through $P$, and named through $N$; it cannot be replaced by host conversion, quotient computation, unrestricted evaluation, or global normalization.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:znormal-fuel-normality-nonescape}. It already binds terminal normality to the finite path from the typed endpoint $T$ through fuel $F$ and terminal row $V$ to the continuation read $R$.

Apply \autoref{thm:znormal-terminal-readback-exactness}. The theorem makes $R$ the exact terminal readback row and permits $K$ only after $V$ has been displayed.

Finally combine \autoref{thm:znormal-sibling-normalword-handoff} with \autoref{thm:znormal-route-transport}. The sibling row remains finite evidence, while $H,C,P,N$ transport, replay, package, and name the same terminal route without adding any excluded host or global normalization object.
\end{proof}

\begin{theorem}[$\ZNormalUp$ root non-escape ledger]
\label{thm:znormal-root-unblock-nonescape-ledger}
The root-unblock ledger of an accepted $\ZNormalUp$ packet is exhausted by carrier admission, NameCert obligations, finite fuel non-escape, terminal readback exactness, sibling normal-word handoff, and route transport. Thus every downstream normal-form row must pass through
$$
T,F,V,K,R,H,C,P,N
$$
and the finite sibling surfaces named by $K$, while host conversion, quotient computation, unrestricted evaluation, divergence detection, and global normalization remain absent.
\end{theorem}
\begin{proof}
First assemble the carrier and naming rows using \autoref{def:znormal-carrier} and \autoref{thm:znormal-namecert-obligations}. They expose the packet fields and no separate host computation surface.

Next apply \autoref{thm:znormal-fuel-normality-nonescape}, \autoref{thm:znormal-terminal-readback-exactness}, and \autoref{thm:znormal-sibling-normalword-handoff}. These rows bind finite fuel, terminal readback, and sibling evidence to the displayed packet.

Finally use \autoref{thm:znormal-route-transport} together with the displayed TasteGate contract row. Transport and replay stay inside $H,C,P,N$, so the ledger has no coordinate for host conversion, quotient computation, unrestricted evaluation, divergence detection, or global normalization.
\end{proof}

\begin{theorem}[$\ZNormalUp$ normal output boundary]
\label{thm:znormal-normal-output-boundary}
For every accepted $\ZNormalUp$ packet
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
the public normal-output row is exactly the terminal-normality row $V$ together with the terminal read row $R$. This output boundary exports no divergence oracle, host conversion, quotient computation, unrestricted evaluator, global normalizer, or total host-computation principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:znormal-carrier}. The normal-output data are displayed as $V$ and $R$, while $T,F,K,H,C,P,N$ only type, fuel, compare, transport, replay, package, and name that same finite packet.

Use \autoref{thm:znormal-namecert-obligations} to make the projection a local certificate read. The finite-output claim is then constrained by \autoref{thm:znormal-fuel-normality-nonescape}, which excludes divergence and unrestricted evaluation at the fuel boundary.

The terminal side is fixed by \autoref{thm:znormal-terminal-readback-exactness}, and the sibling side is fixed by \autoref{thm:znormal-sibling-normalword-handoff}. Together they show that $V$ with $R$ is the whole public normal-output boundary and that no host conversion, quotient computation, or global normalizer is exported.
\end{proof}

\begin{theorem}[$\ZNormalUp$ consumer non-escape]
\label{thm:znormal-consumer-nonescape}
Every downstream consumer of an accepted $\ZNormalUp$ packet
$$
Z=(T,F,V,K,R,H,C,P,N)
$$
factors through the displayed rows $T,F,V,K,R,H,C,P,N$ and the finite sibling surfaces cited in \autoref{thm:znormal-sibling-normalword-handoff}. The consumer obtains no route to host conversion, quotient computation, unrestricted evaluation, global normalization, divergence detection, or total host computation outside those rows.
\end{theorem}
\begin{proof}
Project $Z$ through \autoref{def:znormal-carrier}. This exposes the typed endpoint, fuel, terminal normality, sibling normal-word, terminal read, transport, continuation, provenance, and naming rows as the complete consumer surface.

Apply \autoref{thm:znormal-namecert-obligations}. The certificate admits consumer reads only through those displayed coordinates and their local $\NameCert_{\ZNormalUp}$ provenance.

The finite boundary is inherited from \autoref{thm:znormal-fuel-normality-nonescape} and \autoref{thm:znormal-terminal-readback-exactness}. The only sibling handoff available to downstream chapters is the finite handoff of \autoref{thm:znormal-sibling-normalword-handoff}. Hence every consumer factors through the displayed packet and cannot escape to any omitted host or global normalization object.
\end{proof}

The follow-up Lean TasteGate.lean contract for this chapter must provide \texttt{Nontrivial ZNormalUp} and \texttt{FieldFaithful ZNormalUp} instances for the same carrier.

\begin{theorem}[$\ZNormalUp$ carrier obligation row]
\label{thm:znormal-carrier-obligation-row}
For every accepted $\ZNormalUp$ packet
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
carrier admission binds the typed endpoint row $T$, finite fuel row $F$, terminal-normality row $V$, Zeckendorf and carry-facing comparison row $K$, continuation terminal-read row $R$, componentwise $\hsame$ row $H$, displayed $\Cont$ row $C$, package row $P$, and local $\NameCert_{\ZNormalUp}$ row $N$ to the same finite $\BHist$ surface. The carrier row exports no host conversion, quotient computation, unrestricted evaluator, divergence oracle, or global normalizer.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:znormal-carrier}. It displays exactly $T,F,V,K,R,H,C,P,N$ and gives no additional coordinate to a root consumer.

Apply \autoref{thm:znormal-namecert-obligations}. The local NameCert surface reads those fields as carrier admission, endpoint exposure, finite fuel, terminal normality, sibling comparison, terminal read, transport, route, provenance, and naming for the same packet.

Use \autoref{thm:znormal-fuel-normality-nonescape} and \autoref{thm:znormal-terminal-readback-exactness}. They keep the carrier read inside finite fuel and terminal readback rows, so the excluded host and global-normalization objects cannot be recovered from carrier admission.
\end{proof}

\begin{theorem}[$\ZNormalUp$ carry classifier row]
\label{thm:znormal-carry-classifier-row}
For every accepted $\ZNormalUp$ packet, the comparison coordinate $K$ is the only ZeckendorfNormal and ZCarry classifier surface. It is read as a $\BHist$ row transported by $H$, replayed by $C$, packaged by $P$, and named by $N$ after the typed endpoint, finite fuel, and terminal-normality rows have been displayed. It is not a host conversion row, quotient computation, unrestricted evaluator, divergence oracle, or global normalizer.
\end{theorem}
\begin{proof}
Start from \autoref{def:znormal-carrier} and \autoref{thm:znormal-namecert-obligations}. They place $K$ inside the same finite packet as $T,F,V,R,H,C,P,N$ and bind it to the local $\NameCert_{\ZNormalUp}$ route.

Apply \autoref{thm:znormal-carry-normalization-obligation} and \autoref{thm:znormal-sibling-normalword-handoff}. These theorems identify the $K$ row with the finite ZeckendorfNormal and ZCarry comparison surface rather than with host computation.

Finally use \autoref{thm:znormal-route-transport}. The comparison row is transported and replayed only through $H$ and $C$, then packaged and named by $P,N$, so no quotient computation, unrestricted evaluator, divergence oracle, or global normalizer can enter the classifier row.
\end{proof}

\begin{theorem}[$\ZNormalUp$ normal-form ledger row]
\label{thm:znormal-normal-form-ledger-row}
For every accepted $\ZNormalUp$ packet, terminal normality is recorded only by the ledger route through
$$
T,F,V,R,H,C,P,N,
$$
with $K$ available as the finite sibling comparison row named by \autoref{thm:znormal-carry-classifier-row}. The ledger binds the terminal-normality claim to $\BHist$, $\Cont$, $\hsame$, $\Pkg$, and $\NameCert$ rows and exports no host conversion, quotient computation, unrestricted evaluator, divergence oracle, or global normalizer.
\end{theorem}
\begin{proof}
Use \autoref{thm:znormal-carrier-obligation-row} to expose the displayed packet and no other terminal-normality coordinates. The terminal row is $V$, and the continuation read is $R$.

Apply \autoref{thm:znormal-fuel-normality-nonescape} and \autoref{thm:znormal-terminal-readback-exactness}. These theorems force terminal normality through the finite fuel path $T,F,V,R$ and keep readback inside the displayed packet.

Now apply \autoref{thm:znormal-carry-classifier-row} and \autoref{thm:znormal-route-transport}. The sibling comparison row remains finite evidence, while $H,C,P,N$ provide the only transport, replay, package, and naming surface for the normal-form ledger.
\end{proof}

\closureat{ZNormalUp}{seedStr}

\begin{closurestatus}{\ZNormalUp}
  \constructivestory{A $\ZNormalUp$ packet is generated from a typed reduction normal-form row, finite fuel row, terminal-normality row, finite Zeckendorf and carry-facing comparison row, continuation terminal-read row, componentwise transports, displayed continuations, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:znormal-carrier}, \autoref{thm:znormal-namecert-obligations}, \autoref{thm:znormal-carry-normalization-obligation}, \autoref{thm:znormal-continuation-terminal-route}, \autoref{thm:znormal-typed-boundary-exhaustion}, \autoref{thm:znormal-fuel-normality-nonescape}, \autoref{thm:znormal-total-host-boundary}, \autoref{thm:znormal-terminal-readback-exactness}, \autoref{thm:znormal-sibling-normalword-handoff}, \autoref{thm:znormal-obligation-closure-package}, \autoref{thm:znormal-route-transport}, \autoref{thm:znormal-normal-output-boundary}, and \autoref{thm:znormal-consumer-nonescape} bind the finite normal-form packet, carry normalization obligation, continuation terminal route, typed boundary exhaustion, terminal readback, sibling handoff, route transport, public normal-output boundary, consumer non-escape, and host-side total-language boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not prove host Turing completeness, substrate halting, global normalization for external computations, host conversion, quotient computation, Lean verification, or bridge checking for $\ZNormalUp$.}
  \upgradepath{Obligation closure requires binding the finite fuel, terminal-normality, terminal readback, sibling normal-word, non-escape, and total-host boundary rows to explicit BEDC primitives and checked TasteGate instances.}
\end{closurestatus}
