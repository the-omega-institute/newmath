\chapter{A Concrete Naming Certificate for $\HenstockKurzweilGaugeIntegralUp$}
\label{ch:concrete-instances-henstock-kurzweil-gauge-integral-namecert}
\origin{human}

Following Henstock and Kurzweil, the $\HenstockKurzweilGaugeIntegralUp$ packet records the constructive gauge-integral route in which finite tagged partitions are admitted by a displayed gauge and finite Riemann sums are compared through regular rational readback before any Real-facing integral value is exported. It sits beside the gauge-integral packet of \autoref{ch:concrete-instances-gauge-integral-namecert}, the tagged-partition surface of \autoref{ch:concrete-instances-tagged-real-partition-namecert}, the Riemann-sum gauge row of \autoref{ch:concrete-instances-riemann-sum-gauge-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter names one finite gauge-integral certificate; it does not import measure completion, countable choice, selected tags, quotient equality of functions, ambient completeness, or a standard bridge.

\begin{definition}[Henstock--Kurzweil gauge-integral carrier]
\label{def:henstock-kurzweil-gauge-integral-carrier}
A $\HenstockKurzweilGaugeIntegralUp$ carrier is a finite $\BHist$ packet
$$
K=(I,G,T,S,C,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the interval source row, carrying the located endpoint window on which the integrand is inspected.
\item $G$ is the gauge ledger, recording positive local gauge data and the dyadic tolerance requests used by the packet.
\item $T$ is the $\TaggedRealPartitionUp$ row, exposing only finite tagged subintervals that are $G$-fine.
\item $S$ is the $\RiemannSumGaugeUp$ row, carrying the finite Riemann-sum observations attached to the tagged partition.
\item $C$ is the Cauchy sum-window row, recording finite comparison windows for pairs of $G$-fine sums.
\item $H$ records componentwise $\hsame$ transport for the interval, gauge, partition, sum, Cauchy-window, readback, replay, provenance, and local naming rows.
\item $R$ is the $\RegSeqRatUp$ handoff row, read only after the finite Cauchy sum windows and dyadic tolerance ledger have been displayed.
\item $P$ is the $\Pkg$ provenance over $\HenstockKurzweilGaugeIntegralUp$, $\GaugeIntegralUp$, $\TaggedRealPartitionUp$, $\RiemannSumGaugeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HenstockKurzweilGaugeIntegralUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,G,T,S,C,H,R,P,N$. It has no coordinate for Lebesgue measure, countable choice, selected tags, quotient equality of functions, ambient $\RealUp$ completeness, or a standard bridge.
\end{definition}

\begin{theorem}[Henstock--Kurzweil gauge-fine partition obligation]
\label{thm:henstock-kurzweil-gauge-fine-partition-obligation}
For every accepted $\HenstockKurzweilGaugeIntegralUp$ carrier $K=(I,G,T,S,C,H,R,P,N)$, a partition-facing consumer must read the interval source $I$, the gauge ledger $G$, and the tagged finite partition row $T$ before any Riemann-sum or Cauchy-window row is available. Thus gauge fineness is stored as displayed finite packet data, not as a choice-selected partition family or an ambient compactness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:henstock-kurzweil-gauge-integral-carrier}. The only interval source is $I$, the only gauge data are in $G$, and the only partition data are the finite tagged windows in $T$.

The Riemann-sum row $S$ is downstream from those three rows. A consumer therefore cannot inspect a sum before the interval window, gauge ledger, and tagged partition are visible.

Transport through $H$ and provenance through $P,N$ preserve the same row order. Since the carrier contains no selected tag family, open-cover compactness row, countable-choice schedule, or host partition quotient, the obligation is exactly the displayed finite gauge-fineness route.
\end{proof}

\begin{theorem}[Henstock--Kurzweil Cauchy criterion obligation]
\label{thm:henstock-kurzweil-cauchy-criterion-obligation}
For every accepted $\HenstockKurzweilGaugeIntegralUp$ carrier $K=(I,G,T,S,C,H,R,P,N)$, a Real-facing integral read must pass through the finite Cauchy sum-window row $C$ and the regular rational handoff $R$ after the gauge-fine partition and Riemann-sum rows have been displayed. The packet exports only a Cauchy comparison route for displayed gauge-fine sums; it does not supply measure completion, quotient equality of functions, selected tags, countable choice, ambient completeness, or a standard bridge.
\end{theorem}
\begin{proof}
Start with the gauge-fine route in \autoref{thm:henstock-kurzweil-gauge-fine-partition-obligation}. It exposes $I,G,T$ before any sum row is read.

Next read the finite sum and Cauchy rows. The row $S$ records the displayed Riemann sums, while $C$ records the finite comparison windows used for the Cauchy criterion. Both rows remain tied to the same interval source, gauge ledger, and tagged partition.

Finally pass through $R,H,P,N$. The $\RegSeqRatUp$ handoff records regular rational readback before any $\RealUp$ consumer can interpret the integral value, and the structural rows transport, source, and name the same route. None of the excluded measure, quotient, selection, choice, completeness, or bridge coordinates occurs in the carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\HenstockKurzweilGaugeIntegralUp$ Real-facing read factors through interval source, gauge ledger, tagged fine partition, Riemann-sum gauge row, finite Cauchy sum windows, componentwise $\hsame$ transport, RegSeqRat handoff, provenance, and local NameCert rows.}

\independenceWitness{gauge-integral, tagged-real-partition, riemann-sum-gauge, regseqrat, real, dyadicratcore: no listed sibling is bijective with $\HenstockKurzweilGaugeIntegralUp$ because this carrier jointly binds gauge fineness, tagged finite partitions, Riemann-sum observations, Cauchy sum-window comparisons, regular rational readback, transport, provenance, and local naming.}

\closureat{HenstockKurzweilGaugeIntegralUp}{seedStr}

\begin{closurestatus}{\HenstockKurzweilGaugeIntegralUp}
  \constructivestory{A $\HenstockKurzweilGaugeIntegralUp$ packet is generated from an interval source, gauge ledger, tagged fine partition row, Riemann-sum gauge row, finite Cauchy sum-window row, componentwise hsame transport, RegSeqRat handoff, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:henstock-kurzweil-gauge-integral-carrier}, \autoref{thm:henstock-kurzweil-gauge-fine-partition-obligation}, and \autoref{thm:henstock-kurzweil-cauchy-criterion-obligation} seed the finite gauge-integral route over tagged partitions, Riemann-sum windows, Cauchy sum comparisons, and regular rational readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lebesgue measure, countable choice, selected tags, quotient equality of functions, ambient completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, gauge-fineness exposure, tagged-partition admission, Riemann-sum readback, Cauchy sum-window comparison, RegSeqRat handoff, transport, provenance, and local naming for BEDC.Derived.HenstockKurzweilGaugeIntegralUp.}
\end{closurestatus}
