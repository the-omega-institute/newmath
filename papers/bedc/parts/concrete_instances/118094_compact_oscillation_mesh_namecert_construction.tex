\chapter{A Concrete Naming Certificate for $\CompactOscillationMeshUp$}
\label{ch:concrete-instances-compact-oscillation-mesh-namecert}
\origin{human}

Following Bishop--Bridges compact metric uniform continuity, the $\CompactOscillationMeshUp$ packet realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} as a finite oscillation mesh. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the displayed continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite mesh of compact probes and oscillation bounds; it is not an open-cover compactness theorem, selected Lebesgue number, host modulus function, quotient equality of maps, or ambient $\RealUp$ completeness principle.

\begin{definition}[Compact oscillation mesh carrier]
\label{def:compact-oscillation-mesh-carrier}
A $\CompactOscillationMeshUp$ carrier is a finite $\BHist$ packet
$$
O=(K,F,Q,M,D,U,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row, exposing centers, mesh radius, and coverage witnesses.
\item $F$ is the $\ContinuousMapUp$ pointwise inspection row attached to the displayed compact source.
\item $Q$ is the finite oscillation ledger, recording for each mesh cell the source-radius and target-oscillation bound read from $F$.
\item $M$ is the rational lower-bound mesh fold, extracting a positive source precision from the finite oscillation ledger.
\item $D$ is the $\DyadicRatCoreUp$ tolerance-conversion row used by downstream dyadic budgets.
\item $U$ is the $\UniformModulusUp$ handoff row produced from the folded mesh precision.
\item $R$ is the $\RegSeqRatUp$ readback row for consumers that read the modulus through regular rational names.
\item $E$ is the $\RealUp$ seal reached only after $K,F,Q,M,D,U,R$ are displayed.
\item $H$ records componentwise $\hsame$ transport for compact source, continuous-map, oscillation, fold, dyadic, uniform-modulus, readback, Real, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through $K,F,Q,M,D,U,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactOscillationMeshUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactOscillationMeshUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for selected covers, selected Lebesgue numbers, host moduli, quotient map equality, countable choice, or ambient compactness.
\end{definition}

\begin{theorem}[Compact oscillation mesh NameCert obligations]
\label{thm:compact-oscillation-mesh-namecert-obligations}
For every accepted $\CompactOscillationMeshUp$ carrier $O=(K,F,Q,M,D,U,R,E,H,C,P,N)$, the local $\NameCert_{\CompactOscillationMeshUp}$ surface consists exactly of compact finite-net admission, continuous-map pointwise inspection, finite oscillation-ledger admission, rational lower-bound mesh folding, dyadic tolerance conversion, $\UniformModulusUp$ handoff, $\RegSeqRatUp$ readback, $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-oscillation-mesh-carrier}. The compact source row $K$, continuous-map row $F$, oscillation ledger $Q$, mesh fold $M$, dyadic row $D$, uniform-modulus row $U$, readback row $R$, and Real seal $E$ are the analytic coordinates.

The compact and continuity obligations are finite. The mesh centers and coverage witnesses come from $K$, while each oscillation row in $Q$ is attached to an inspected pointwise row in $F$.

The modulus obligation is the route through $Q,M,D,U$. The fold $M$ extracts one positive source precision from the finite oscillation ledger, $D$ converts it into the dyadic tolerance budget, and $U$ exports the tolerance-only uniform-modulus row.

Finally $R,E,H,C,P,N$ read, seal, transport, replay, source, and name the same route. Since the carrier has no coordinate for the excluded host objects, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact oscillation mesh uniform-modulus route]
\label{thm:compact-oscillation-mesh-uniform-modulus-route}
Every $\UniformModulusUp$ consumer of an accepted $\CompactOscillationMeshUp$ carrier must first read
$$
K,F,Q,M,D,U,H,C,P,N .
$$
If the consumer later reaches $\RegSeqRatUp$ or $\RealUp$ data, the read factors through the same mesh route before $R$ and $E$ are available. Thus the compact-uniform-continuity output is a finite oscillation-mesh realization, not a selected Lebesgue number, host modulus, open-cover compactness theorem, quotient map equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the compact source row. The finite mesh data in $K$ fixes the centers, mesh radius, and coverage witnesses before any oscillation estimate can be consumed.

Use the continuous-map inspection row $F$ and the oscillation ledger $Q$. Each target-oscillation bound is attached to a displayed mesh cell, so the consumer cannot replace the ledger by a host continuity theorem.

The lower-bound fold $M$ and dyadic row $D$ supply the source precision consumed by $U$. Componentwise transport and replay through $H$ and $C$ preserve exactly that route, and $P,N$ only source and name it.

When regular rational or Real-facing consumers appear, $R$ and $E$ are downstream of the same rows. No selected cover, selected Lebesgue number, host modulus, quotient equality, countable choice, or ambient completeness coordinate is present in the carrier.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\CompactOscillationMeshUp$ uniform-modulus read while omitting compact finite-net rows, continuous-map pointwise inspection, finite oscillation ledger, rational mesh fold, dyadic tolerance conversion, $\UniformModulusUp$ handoff, transport, replay, provenance, or local NameCert rows refutes the carrier of \autoref{def:compact-oscillation-mesh-carrier}.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, dyadicratcore, regseqrat, real: no listed sibling is bijective to $\CompactOscillationMeshUp$ because this packet jointly binds compact finite-net data, pointwise oscillation rows, finite mesh folding, dyadic tolerance conversion, uniform-modulus handoff, regular readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{CompactOscillationMeshUp}{seedStr}

\begin{closurestatus}{\CompactOscillationMeshUp}
  \constructivestory{A $\CompactOscillationMeshUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise inspection rows, finite oscillation ledger rows, rational mesh folds, DyadicRatCore tolerance conversion, UniformModulus handoff, RegSeqRat readback, Real sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-oscillation-mesh-carrier}, \autoref{thm:compact-oscillation-mesh-namecert-obligations}, and \autoref{thm:compact-oscillation-mesh-uniform-modulus-route} seed the finite oscillation-mesh route from compact metric pointwise inspection to uniform-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected Lebesgue numbers, host modulus functions, quotient map equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-net admission, continuous-map pointwise inspection, oscillation-ledger admission, rational mesh fold exactness, dyadic tolerance conversion, UniformModulus handoff, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactOscillationMeshUp.}
\end{closurestatus}
