\chapter{A Concrete Naming Certificate for $\KolmogorovUp$}
\label{ch:concrete-instances-kolmogorov-namecert}

The Kolmogorov-complexity interface $\KolmogorovUp$ packages a description-length predicate over a deterministic prefix-free decoder, parametrised by a displayed unary length budget. It depends on $\NameCert_{\NatUp}$ (\autoref{ch:concrete-instances-nat-namecert}), $\NameCert_{\TuringMachineUp}$ (\autoref{ch:concrete-instances-turingmachine-namecert}), and $\NameCert_{\ComputableUp}$ (\autoref{ch:concrete-instances-computable-namecert}).

\section{Carrier definition}
\label{sec:kolmogorov-carrier}

A $\KolmogorovUp$ token carries a deterministic prefix-free decoder $\TuringMachineUp$, a unary length budget $\ell$, and a target output history $h$. The carrier accepts a witness when there is a prefix-free description $d$ of unary length at most $\ell$ that, decoded by the decoder, halts and reads back $h$. The classifier is $\hsame$ on output histories at fixed budget $\ell$; the ledger keeps the description $d$, the unary budget, the decoded trace, and the readback row visible; the pattern records prefix-freeness, budget admissibility, decoded-readback exactness, and budget monotonicity.

\begin{closurestatus}{\KolmogorovUp}
  \constructivestory{A $\KolmogorovUp$ token names a deterministic prefix-free decoder $\TuringMachineUp$ together with a displayed unary length budget $\ell$ and a target output history $h$. The carrier accepts a witness when there is a prefix-free description history $d$ of unary length at most $\ell$ such that running the decoder on $d$, in the sense of $\NameCert_{\ComputableUp}$, halts and reads back $h$. The classifier is $\hsame$ on output histories at fixed unary budget; the source covers unary description lengths, budget values, decoder configurations, prefix-freeness rows, and decoded readback histories; the pattern records prefix-freeness admissibility, budget admissibility, decoded-readback exactness, budget monotonicity, and decoder-determinism rows; the ledger keeps the description $d$, the budget $\ell$, the decoded trace, and the readback row visible. The five name-certificate obligations are supplied by the bounded-simulation certificate plus the prefix-free description structure: the inhabited identity certificate is the empty-description trivial decoder; classifier stability is reflexive, symmetric, and transitive $\hsame$ at fixed budget; transport repacks descriptions through prefix-extension; ledger exactness identifies the accepted row with the decoded readback at the displayed budget; the extension boundary fixes budget monotonicity. A history $h$ lies in the signature gap of a $\KolmogorovUp$ token $p$ when the source-domain accepts the prefix-free decoded readback, the signature relation pairs it with the decoded output, and token introduction names that output as $p$. The chapter registers this prefix-free description-budget surface under the name $\KolmogorovUp$; it is a description-length object with no universal-decoder optimality, no resource-bounded variant ($K^{t}$, $K^{s}$), no randomness extraction, and no information-theoretic standard bridge.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope binds the deterministic prefix-free decoder, the unary description length, the budget value $\ell$, the decoded $\TuringMachineUp$ trace, and the prefix-free admissibility row.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close universal-decoder optimality, resource-bounded Kolmogorov complexity ($K^{t}$, $K^{s}$), randomness extraction, the invariance theorem, or a standard information-theoretic bridge.}
  \upgradepath{Obligation closure requires a checked $\KolmogorovUp$ prefix-free-decoder obligation theorem covering budget admissibility, decoded readback exactness, and budget monotonicity.}
\end{closurestatus}
