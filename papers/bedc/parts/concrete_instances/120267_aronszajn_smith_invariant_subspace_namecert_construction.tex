\chapter{A Concrete Naming Certificate for $\AronszajnSmithInvariantSubspaceUp$}
\label{ch:concrete-instances-aronszajn-smith-invariant-subspace-namecert}
\origin{human}

Following Aronszajn and Smith on invariant subspaces for compact operators, the $\AronszajnSmithInvariantSubspaceUp$ packet records the finite BEDC surface by which a compact operator on a Banach-facing scalar space exposes an invariant-subspace witness before any functional-analysis consumer reads the result. It sits over the compact-operator carrier of \autoref{ch:concrete-instances-compactoperator-namecert}, the bounded-linear-operator route of \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, the functional-analysis ledger of \autoref{ch:concrete-instances-functionalanalysis-namecert}, the Banach carrier of \autoref{ch:concrete-instances-banach-namecert}, the real scalar seal of \autoref{ch:concrete-instances-real-namecert}, and the complex scalar row of \autoref{ch:concrete-instances-complex-namecert}. The chapter names the invariant-subspace certificate as finite NameCert data; it does not assert a full spectral theorem, choose an eigenvector, quotient a function space, import Hilbert-space orthogonality, use countable choice, or claim an ambient completeness principle.

\begin{definition}[Aronszajn--Smith invariant-subspace carrier]
\label{def:aronszajn-smith-invariant-subspace-carrier}
An $\AronszajnSmithInvariantSubspaceUp$ carrier is a finite $\BHist$ packet
$$
A=(K,T,F,B,R,Z,W,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactOperatorUp$ row recording compactness of the operator route.
\item $T$ is the $\BoundedLinearOperatorUp$ row recording bounded linear action on the same displayed source.
\item $F$ is the $\FunctionalAnalysisUp$ ledger row that admits operator and subspace reads only through the finite packet.
\item $B$ is the $\BanachUp$ carrier row for the domain in which the invariant-subspace witness is read.
\item $R$ is the $\RealUp$ scalar-seal row used by norm and radius comparisons.
\item $Z$ is the $\ComplexUp$ scalar row, stored as finite BEDC scalar data rather than as an ambient algebraic closure claim.
\item $W$ is the invariant-subspace witness row, recording a displayed nonzero proper subspace candidate.
\item $L$ is the invariance ledger row saying that the bounded compact operator route sends the displayed witness row back through itself.
\item $H$ records componentwise $\hsame$ transport for $K,T,F,B,R,Z,W,L$ and for the package and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-operator admission through bounded linearity, Banach scalar sealing, witness exposure, and invariance readback.
\item $P$ is the $\Pkg$ provenance over $\AronszajnSmithInvariantSubspaceUp$, $\CompactOperatorUp$, $\BoundedLinearOperatorUp$, $\FunctionalAnalysisUp$, $\BanachUp$, $\RealUp$, $\ComplexUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AronszajnSmithInvariantSubspaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a full spectral theorem, selected eigenvectors, quotient function spaces, host Hilbert orthogonality, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Aronszajn--Smith invariant-subspace NameCert obligations]
\label{thm:aronszajn-smith-invariant-subspace-namecert-obligations}
For every accepted $\AronszajnSmithInvariantSubspaceUp$ carrier
$$
A=(K,T,F,B,R,Z,W,L,H,C,P,N),
$$
the local $\NameCert_{\AronszajnSmithInvariantSubspaceUp}$ surface consists exactly of compact-operator admission, bounded-linear-operator exposure, functional-analysis ledger exposure, Banach carrier exposure, real and complex-facing scalar sealing, invariant-subspace witness exposure, invariance-ledger readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored outside $K,T,F,B,R,Z,W,L,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:aronszajn-smith-invariant-subspace-carrier}. The analytic rows are $K,T,F,B,R,Z,W,L$, and the structural rows are $H,C,P,N$.

The compact and bounded-linear rows are read first. They force any operator-facing consumer to enter through \autoref{ch:concrete-instances-compactoperator-namecert} and \autoref{ch:concrete-instances-boundedlinearoperator-namecert} before the witness row is visible.

Next the functional-analysis, Banach, and scalar rows expose the source discipline. The rows $F,B,R,Z$ keep the normed carrier and scalar comparisons finite, with no coordinate for spectral decomposition or host-side algebraic closure.

Finally $W$ and $L$ provide the displayed witness and its invariance readback, while $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier has no row for selected eigenvectors, quotient spaces, Hilbert orthogonality, countable choice, or ambient completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Aronszajn--Smith compact-operator route]
\label{thm:aronszajn-smith-invariant-subspace-compact-operator-route}
Every functional-analysis or Banach-facing consumer of an accepted $\AronszajnSmithInvariantSubspaceUp$ packet must expose the compact-operator row $K$, the bounded-linear row $T$, the functional-analysis ledger $F$, the Banach row $B$, the scalar rows $R,Z$, the invariant-subspace witness $W$, the invariance ledger $L$, and the support rows $H,C,P,N$ before reading the invariant-subspace certificate. The route reaches the sibling surfaces of \autoref{ch:concrete-instances-compactoperator-namecert}, \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, \autoref{ch:concrete-instances-functionalanalysis-namecert}, \autoref{ch:concrete-instances-banach-namecert}, and \autoref{ch:concrete-instances-real-namecert} only through those displayed rows.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:aronszajn-smith-invariant-subspace-namecert-obligations}. They identify $K,T,F,B,R,Z,W,L,H,C,P,N$ as the complete public surface of the accepted packet.

The operator handoff passes through $K$ and $T$. Compactness is therefore consumed as a displayed compact-operator row, while bounded linearity is consumed as a displayed bounded-linear-operator row over the same packet.

The source and scalar handoff passes through $F,B,R,Z$. These rows expose the functional-analysis ledger, Banach carrier, and scalar comparison data before the witness row can be read.

The witness handoff is $W,L$ under $H,C,P,N$. Thus any consumer that reads the invariant-subspace certificate remains inside the finite packet and cannot replace it by a spectral theorem, eigenvector choice, quotient function space, Hilbert orthogonality, countable choice, or ambient completeness argument.
\end{proof}

\falsifiablePrediction{An accepted $\AronszajnSmithInvariantSubspaceUp$ consumer that reads the invariant-subspace witness without first exposing $K,T,F,B,R,Z,W,L,H,C,P,N$ refutes the carrier.}

\independenceWitness{compactoperator, boundedlinearoperator, functionalanalysis, banach, real: none of these siblings alone carries the joint packet containing compact-operator admission, bounded-linear action, functional-analysis ledger, Banach source, real and complex-facing scalar rows, invariant-subspace witness, invariance readback, transport, replay, provenance, and local NameCert rows.}

\closureat{AronszajnSmithInvariantSubspaceUp}{seedStr}

\begin{closurestatus}{\AronszajnSmithInvariantSubspaceUp}
  \constructivestory{An $\AronszajnSmithInvariantSubspaceUp$ packet is generated from a CompactOperator row, BoundedLinearOperator row, FunctionalAnalysis ledger, Banach source, Real scalar seal, Complex scalar row, invariant-subspace witness row, invariance ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:aronszajn-smith-invariant-subspace-carrier}, \autoref{thm:aronszajn-smith-invariant-subspace-namecert-obligations}, and \autoref{thm:aronszajn-smith-invariant-subspace-compact-operator-route} seed the finite compact-operator route from Aronszajn--Smith invariant-subspace data to functional-analysis and Banach-facing consumers.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean theorem verification, bridge checking, a full spectral theorem, selected eigenvectors, quotient function spaces, Hilbert orthogonality, countable choice, or ambient completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-operator exposure, bounded-linear exposure, scalar sealing, invariant-subspace witness exposure, invariance readback, transport, replay, provenance, and local naming for BEDC.Derived.AronszajnSmithInvariantSubspaceUp.}
\end{closurestatus}
