\chapter{A Concrete Naming Certificate for $\CompactCauchyFilterUp$}
\label{ch:concrete-instances-compact-cauchy-filter-namecert}
\origin{human}

Following Kelley, Bourbaki, and Bishop--Bridges on compactness, filters, and constructive completion, the $\CompactCauchyFilterUp$ packet records the finite compact-generated Cauchy-filter surface used by the Real and Completion route. It sits after the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the filter-base interface of \autoref{ch:concrete-instances-filterbase-namecert}, the Cauchy-filter surface of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the RegSeqRat readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the DyadicRatCore tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed compact finite-net and Cauchy-filter data; it does not select a limit, quotient filters, use countable choice, import open-cover compactness, or assert ambient $\RealUp$ completeness.

\begin{definition}[Compact Cauchy filter carrier]
\label{def:compact-cauchy-filter-carrier}
A $\CompactCauchyFilterUp$ carrier is a finite $\BHist$ packet
$$
F=(K,B,Q,W,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ or compact finite-net row, exposing the finite compact source windows and net witnesses consumed by the filter route.
\item $B$ is the $\FilterBaseUp$ row, listing the finite filter-base packets and directed-refinement witnesses visible to completion consumers.
\item $Q$ is the $\CauchyFilterUp$ row, recording the displayed Cauchy requests over the same base and compact source.
\item $W$ is the $\StreamNameUp$ finite-window row used when the compact filter route is inspected by sequential or real-facing consumers.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the finite windows and tolerance requests.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, bounding every displayed Cauchy request before the terminal seal is read.
\item $E$ is the $\RealUp$ seal row, reachable only after $K,B,Q,W,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact-source, filter-base, Cauchy-filter, window, readback, tolerance, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact filter request through $K,B,Q,W,R,D,E,H$.
\item $P$ is the $\Pkg$ provenance over $\CompactCauchyFilterUp$, $\CompactMetricUp$, $\FilterBaseUp$, $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactCauchyFilterUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,B,Q,W,R,D,E,H,C,P,N$. It has no coordinate for a selected limit, quotient filter equality, quotient real equality, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact Cauchy filter NameCert obligations]
\label{thm:compact-cauchy-filter-namecert-obligations}
For every accepted $\CompactCauchyFilterUp$ carrier $F=(K,B,Q,W,R,D,E,H,C,P,N)$, the local $\NameCert_{\CompactCauchyFilterUp}$ surface consists exactly of compact-source admission, filter-base admission, Cauchy-filter admission, finite-window exposure, regular-rational readback, dyadic tolerance exposure, Real sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-cauchy-filter-carrier}. The analytic rows are precisely $K,B,Q,W,R,D,E$: compact finite-net data, filter-base data, Cauchy evidence, finite windows, regular-rational readback, dyadic tolerances, and the terminal Real seal.

The structural rows $H,C,P,N$ preserve, replay, source, and name the same displayed data. They do not add a second filter object, a quotient relation, a selected limit, or an ambient compactness theorem.

Thus every local NameCert obligation is one of the displayed rows, and the excluded completion and compactness principles cannot be obligations of the carrier.
\end{proof}

\begin{theorem}[Compact Cauchy filter finite-net handoff]
\label{thm:compact-cauchy-filter-finite-net-handoff}
Every completion-facing consumer of an accepted $\CompactCauchyFilterUp$ carrier reads the compact finite-net row $K$, the $\FilterBaseUp$ row $B$, the $\CauchyFilterUp$ row $Q$, the $\StreamNameUp$ row $W$, the $\RegSeqRatUp$ row $R$, and the $\DyadicRatCoreUp$ row $D$ before the $\RealUp$ seal $E$ is available. The handoff exports compact-generated Cauchy-filter data only through finite BEDC rows, not through selected limits, quotient filter equality, countable choice, open-cover compactness, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the compact source row. The finite-net data in $K$ supply the compact windows that a completion-facing filter consumer may inspect.

Next read the filter route. The base row $B$ exposes the finite directed interface, and the Cauchy row $Q$ records the Cauchy requests only over that displayed base.

The real-facing portion is downstream. The rows $W,R,D$ translate the inspected filter requests into finite windows, regular-rational readback, and dyadic tolerance accounting before $E$ can be read.

Finally use the structural rows $H,C,P,N$. They preserve and name the same finite handoff, so no selected limit, quotient filter, choice schedule, open-cover compactness proof, or ambient completeness row is available to a consumer.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactCauchyFilterUp$ completion read must expose compact finite-net data, a FilterBase row, a CauchyFilter row, StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert before a Real-facing consumer may inspect the result.}

\independenceWitness{compactmetric, filterbase, cauchyfilter, streamname, regseqrat, dyadicratcore, real: no listed sibling is bijective to $\CompactCauchyFilterUp$ because this packet jointly carries compact finite-net data, filter-base refinements, Cauchy-filter requests, finite windows, rational readback, dyadic tolerances, Real sealing, transport, replay, provenance, and local naming as one completion-facing surface.}

\closureat{CompactCauchyFilterUp}{seedStr}

\begin{closurestatus}{\CompactCauchyFilterUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-cauchy-filter-carrier}, \autoref{thm:compact-cauchy-filter-namecert-obligations}, and \autoref{thm:compact-cauchy-filter-finite-net-handoff} seed the compact-generated Cauchy-filter route over CompactMetric, FilterBase, CauchyFilter, StreamName, RegSeqRat, DyadicRatCore, Real, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient filter equality, quotient real equality, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net admission, filter-base routing, Cauchy-filter routing, finite-window readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactCauchyFilterUp.}
  \constructivestory{A $\CompactCauchyFilterUp$ packet is generated from CompactMetric finite-net rows, FilterBase rows, CauchyFilter rows, StreamName windows, RegSeqRat readbacks, DyadicRatCore tolerance ledgers, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
