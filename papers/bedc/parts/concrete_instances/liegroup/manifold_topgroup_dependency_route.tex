\begin{theorem}[LieGroupUp manifold and topological-group route]
\label{thm:liegroup-manifold-topgroup-dependency-route}
Every accepted $\LieGroupUp$ carrier first exposes the $\ManifoldUp$ chart and atlas rows of \autoref{ch:concrete-instances-manifold-namecert}, then exposes the $\TopGroupUp$ multiplication and inversion continuity rows of \autoref{ch:concrete-instances-topgroup-namecert}, before it presents the combined group-manifold $\NameCert_{\LieGroupUp}$ surface. Thus a downstream smooth-group consumer reads chart domains, chart transport, group multiplication, inverse, continuity, smoothness, replay, provenance, and local naming through the displayed sibling rows, not through an ambient manifold object or an external topological group.
\end{theorem}
\begin{proof}
Project the accepted $\LieGroupUp$ packet to its smooth rows. The chart-domain, chart-readback, atlas-transition, and smoothness entries are exactly the $\ManifoldUp$ rows already carried by the singleton source surface, so the manifold side is read before any smooth-group export is available.

Project the same packet to its group-operation rows. Multiplication, inverse, unit, and their continuity readbacks are the $\TopGroupUp$-facing rows consumed by the topological-group sibling surface.

The local $\NameCert_{\LieGroupUp}$ row packages these two routes together with the shared $\mathsf{BHist}$ classifier, componentwise $\hsame$ transport, displayed $\Cont$ replay, and $\Pkg$ provenance. Since the carrier contains no separate ambient manifold object or external topological group coordinate, every smooth-group read factors through the named $\ManifoldUp$ and $\TopGroupUp$ dependency rows.
\end{proof}
