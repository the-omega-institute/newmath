\chapter{A Concrete Naming Certificate for $\CauchyWitnessPullbackUp$}
\label{ch:concrete-instances-cauchy-witness-pullback-namecert}
\origin{ai-composite}

The $\CauchyWitnessPullbackUp$ packet realizes the inverse readback side of the Real-completion witness route of \autoref{ch:visions-real-completeness}. It is paired with the forward gluing packet of \autoref{ch:concrete-instances-cauchy-witness-gluing-namecert} and reads back through the witness ledger of \autoref{ch:concrete-instances-cauchy-witness-ledger-namecert}, the tail-witness envelope of \autoref{ch:concrete-instances-cauchy-tail-witness-envelope-namecert}, the seal-budget synchronizer of \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert}, and the Real-completion seal classifier of \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}. The packet also records the $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows needed by a completion-facing consumer that must recover which witness ledger supported a glued seal.

\begin{definition}[CauchyWitnessPullback carrier]
\label{def:cauchy-witness-pullback-carrier}
A $\CauchyWitnessPullbackUp$ carrier is a finite $\BHist$ packet
$$
Q=(G,K,R,L,T,S,U,V,D,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the accepted $\CauchyWitnessGluingUp$ row whose Real-facing seal is being read back;
\item $K$ is the $\RealCompletionSealClassifierUp$ row that recognizes the glued completion seal;
\item $R$ is the terminal $\RealUp$ seal row served by the glued packet;
\item $L$ is the recovered $\CauchyWitnessLedgerUp$ row;
\item $T$ is the recovered $\CauchyTailWitnessEnvelopeUp$ row;
\item $S$ is the recovered $\CauchySealBudgetSynchronizerUp$ row;
\item $U$ is the $\StreamNameUp$ row visible at the recovered tail window;
\item $V$ is the $\RegSeqRatUp$ row reached by reading the recovered stream window;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the recovered stream and regular-sequence rows;
\item $A$ is the pullback agreement row saying that $G,K,R$ determine the displayed $L,T,S,U,V,D$ readback packet;
\item $H$ records componentwise $\hsame$ transports for all displayed rows;
\item $C$ records the $\Cont$ routes from the glued seal to the classifier, from the classifier to the recovered ledger, from the ledger through tail and synchronizer rows, and from the stream and dyadic rows to the regular-sequence handoff;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:concrete-instances-cauchy-witness-gluing-namecert}, \autoref{ch:concrete-instances-cauchy-witness-ledger-namecert}, \autoref{ch:concrete-instances-cauchy-tail-witness-envelope-namecert}, \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert}, \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-real-namecert}, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CauchyWitnessPullbackUp}$ row.
\end{enumerate}
The carrier exports no quotient stream equality, host real equality, detached external limit, countable-choice row, ambient completeness principle, or untracked witness replacement beyond the displayed pullback packet.
\end{definition}
\leanchecked{BEDC.Derived.CauchyWitnessPullbackUp.TasteGate.CauchyWitnessPullbackTasteGate\_single\_carrier\_alignment}

\begin{theorem}[CauchyWitnessPullback NameCert obligations]
\label{thm:cauchy-witness-pullback-namecert-obligations}
Let
$$
Q=(G,K,R,L,T,S,U,V,D,A,H,C,P,N)
$$
be an accepted $\CauchyWitnessPullbackUp$ packet. Every consumer that preserves the glued seal $G$, completion classifier $K$, Real seal $R$, recovered ledger $L$, tail envelope $T$, synchronizer $S$, stream row $U$, regular-sequence row $V$, dyadic ledger $D$, pullback agreement $A$, transports $H$, continuations $C$, provenance $P$, and local name $N$ preserves the same witness-readback packet. A consumer that preserves $G,K,R$ but changes $L$ is not a preservation of this carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-witness-pullback-carrier}. The displayed rows are the whole finite packet, and the agreement row $A$ is the coordinate that binds the glued seal to the recovered witness side.

The readback first passes through $G,K,R$. These rows identify the Real-facing seal and the classifier that accepts it, so a consumer cannot choose a second completion-seal source without changing the packet.

The readback then reaches $L,T,S,U,V,D$. These rows record which witness ledger, tail envelope, synchronizer, stream window, regular-sequence handoff, and dyadic tolerance ledger support the accepted seal.

Finally $H,C,P,N$ make the readback a NameCert surface rather than an ambient equality assertion. Thus preserving all displayed rows preserves the packet, while changing $L$ changes the coordinate that the agreement row recovers from the glued seal.
\end{proof}

\begin{theorem}[CauchyWitnessPullback Real seal factorization]
\label{thm:cauchy-witness-pullback-real-seal-factorization}
Every completion-facing read of an accepted $\CauchyWitnessPullbackUp$ packet factors through
$$
G \to K \to R \to A \to (L,T,S,U,D,V)
$$
with $H,C,P,N$ visible as structural rows. No consumer can preserve the glued seal while replacing the recovered witness-ledger row, tail-envelope row, synchronizer row, StreamName row, RegSeqRat row, DyadicRatCore row, or RealUp seal row.
\end{theorem}
\begin{proof}
Start with the Real-facing side. The rows $G,K,R$ present the accepted glued packet, the classifier that recognizes its completion seal, and the terminal $\RealUp$ seal.

Apply the pullback agreement row $A$. It records the inverse readback from that glued seal to the displayed witness-side packet, so the recovered rows are not optional annotations.

Read the recovered side. The ledger $L$, tail envelope $T$, synchronizer $S$, stream row $U$, dyadic ledger $D$, and regular-sequence row $V$ are the displayed coordinates through which the Real seal is supported.

The structural rows $H,C,P,N$ then repack these coordinates into the local NameCert surface. Replacing any recovered coordinate while keeping only the glued seal fixed therefore gives a different candidate packet, not the same $\CauchyWitnessPullbackUp$ factorization.
\end{proof}
\leanchecked{BEDC.Derived.CauchyWitnessPullbackUp.CauchyWitnessPullbackUp\_StdBridge}

\begin{theorem}[CauchyWitnessPullback recovered ledger determinacy]
\label{thm:cauchy-witness-pullback-recovered-ledger-determinacy}
Let
$$
Q=(G,K,R,L,T,S,U,V,D,A,H,C,P,N)
$$
be an accepted $\CauchyWitnessPullbackUp$ packet. Any second accepted pullback packet that agrees with $Q$ on $G,K,R,A,H,C,P,N$ has the same recovered witness-ledger row $L$. Thus the recovered ledger is determined by the glued seal, completion classifier, Real seal, pullback agreement, transport, continuation, provenance, and local NameCert rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyWitnessPullbackUp.CauchyWitnessPullbackCarrier\_recovered\_ledger\_determinacy}
\begin{proof}
Project the carrier of \autoref{def:cauchy-witness-pullback-carrier}. The row $L$ is a displayed coordinate of the same finite packet as $G,K,R,A,H,C,P,N$, not an ambient ledger chosen after the seal has been read.

Apply \autoref{thm:cauchy-witness-pullback-namecert-obligations}. A consumer that preserves the glued seal, classifier, Real seal, agreement, transports, continuations, provenance, and local name but changes $L$ fails to preserve the carrier.

Finally use \autoref{thm:cauchy-witness-pullback-real-seal-factorization}. The Real-facing read passes through $G \to K \to R \to A$ before it reaches the recovered witness side, so agreement on those structural rows fixes the ledger coordinate recovered by the pullback packet.
\end{proof}

\begin{theorem}[CauchyWitnessPullback tail synchronizer compatibility]
\label{thm:cauchy-witness-pullback-tail-synchronizer-compatibility}
For every accepted $\CauchyWitnessPullbackUp$ packet
$$
Q=(G,K,R,L,T,S,U,V,D,A,H,C,P,N),
$$
the recovered rows $T,S,U,V,D$ remain on the same $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ route that the agreement row reads back from $G,K,R$. In particular, a consumer cannot keep $G,K,R,L,A,H,C,P,N$ fixed while moving the recovered tail envelope, synchronizer, stream row, regular-sequence row, or dyadic ledger to an unrelated route.
\end{theorem}
\begin{proof}
Read the displayed coordinates in \autoref{def:cauchy-witness-pullback-carrier}. The tail envelope $T$, synchronizer $S$, stream row $U$, regular-sequence row $V$, and dyadic ledger $D$ are named together with the recovered ledger $L$ and the agreement row $A$.

Apply \autoref{thm:cauchy-witness-pullback-namecert-obligations}. Preserving the pullback carrier preserves the same recovered witness-readback packet, including the rows that route from the tail envelope through synchronizer, stream, regular-sequence, and dyadic data.

Use \autoref{thm:cauchy-witness-pullback-real-seal-factorization}. Since the Real seal factors through the displayed recovered side, replacing any one of $T,S,U,V,D$ by an unrelated route while keeping the structural rows fixed leaves the $\CauchyWitnessPullbackUp$ factorization.
\end{proof}

\begin{theorem}[CauchyWitnessPullback Real-seal readback exactness]
\label{thm:cauchy-witness-pullback-real-seal-readback-exactness}
Every completion-facing consumer of an accepted $\CauchyWitnessPullbackUp$ packet receives exactly the rows
$$
G,K,R,L,T,S,U,V,D,A,H,C,P,N
$$
as its readback surface. The readback exports no quotient stream, selected limit, choice row, ambient completeness datum, host real equality, or hidden witness replacement.
\end{theorem}
\begin{proof}
Start from \autoref{def:cauchy-witness-pullback-carrier}. The carrier lists precisely the fourteen displayed rows and explicitly excludes quotient stream equality, detached limits, choice rows, ambient completeness, and untracked witness replacement.

Apply \autoref{thm:cauchy-witness-pullback-namecert-obligations}. The local NameCert surface is preserved only by preserving those displayed coordinates together, so a completion-facing consumer has no additional row from which to read a selected limit or hidden witness.

Finally use \autoref{thm:cauchy-witness-pullback-real-seal-factorization}. The Real-seal read passes through the glued seal, classifier, Real seal, agreement, and recovered witness-side rows, with $H,C,P,N$ visible as structural rows. That factorization is exactly the exported readback surface.
\end{proof}

\begin{theorem}[CauchyWitnessPullback scoped primitive dependency binding]
\label{thm:cauchy-witness-pullback-scoped-primitive-dependency-binding}
Let
$$
Q=(G,K,R,L,T,S,U,V,D,A,H,C,P,N)
$$
be an accepted $\CauchyWitnessPullbackUp$ packet. The readback surface of $Q$ is scoped exactly by the displayed $\BHist$ packet, the componentwise $\hsame$ transports $H$, the displayed $\Cont$ routes $C$, the $\Pkg$ provenance $P$, the local $\NameCert_{\CauchyWitnessPullbackUp}$ row $N$, and the sibling certificate rows cited by the carrier: $\CauchyWitnessGluingUp$, $\CauchyWitnessLedgerUp$, $\CauchyTailWitnessEnvelopeUp$, $\CauchySealBudgetSynchronizerUp$, $\RealCompletionSealClassifierUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$. No quotient stream, selected limit, host real equality, choice row, or ambient completeness datum lies in this scoped primitive dependency surface.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-witness-pullback-carrier}. Its fourteen displayed coordinates are the whole accepted $\BHist$ packet, and the rows $H,C,P,N$ are already part of the carrier rather than external annotations.

Apply \autoref{thm:cauchy-witness-pullback-namecert-obligations}. The local NameCert obligations force preservation of $G,K,R,L,T,S,U,V,D,A,H,C,P,N$ together, so a consumer cannot keep the pullback packet while replacing any cited sibling certificate row.

Next use the factorization theorem \autoref{thm:cauchy-witness-pullback-real-seal-factorization}. It routes every completion-facing read through $G \to K \to R \to A$ before the recovered ledger, tail, synchronizer, stream, dyadic, and regular-sequence rows are consumed. Thus the displayed $\Cont$ replay and $\Pkg$ provenance are the only admissible dependency route from the glued seal to the recovered witness side.

Apply recovered-ledger determinacy from \autoref{thm:cauchy-witness-pullback-recovered-ledger-determinacy} and tail-synchronizer compatibility from \autoref{thm:cauchy-witness-pullback-tail-synchronizer-compatibility}. These results keep the ledger, tail envelope, synchronizer, stream row, regular-sequence row, and dyadic ledger on the same sibling-certified route.

Finally apply readback exactness from \autoref{thm:cauchy-witness-pullback-real-seal-readback-exactness}. Since the accepted packet exports exactly the displayed rows, quotient streams, selected limits, host real equality, choice rows, and ambient completeness remain outside the scoped primitive dependency boundary.
\end{proof}

The Lean TasteGate namespace $\mathsf{BEDC.Derived.CauchyWitnessPullbackUp.TasteGate}$ provides explicit $\mathsf{Nontrivial}\ \CauchyWitnessPullbackUp$ and $\mathsf{FieldFaithful}\ \CauchyWitnessPullbackUp$ instances for the displayed carrier, with the single-carrier alignment target named $\mathsf{BEDC.Derived.CauchyWitnessPullbackUp.TasteGate.CauchyWitnessPullbackTasteGate\_single\_carrier\_alignment}$.

\falsifiablePrediction{If the pullback packet is accepted, a Real-completion consumer preserving the glued seal but changing the witness-ledger row is rejected by the classifier.}

\independenceWitness{cauchywitnessgluing, cauchywitnessledger, cauchytailwitnessenvelope, cauchysealbudgetsynchronizer, realcompletionsealclassifier: no bijection preserves all pullback rows because the pullback packet carries the glued seal, classifier, Real seal, recovered ledger, recovered tail, recovered synchronizer, StreamName, RegSeqRat, DyadicRatCore, agreement, transport, continuation, provenance, and local NameCert rows together, while each sibling exposes only one direction or one face of that readback.}

\closureat{CauchyWitnessPullbackUp}{seedStr}
\closureat{CauchyWitnessPullbackUp}{obligationStr}[BEDC.Derived.CauchyWitnessPullbackUp.TasteGate.CauchyWitnessPullbackTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\CauchyWitnessPullbackUp}
  \constructivestory{A $\CauchyWitnessPullbackUp$ packet is generated from a glued seal row, completion classifier, Real seal, recovered witness ledger, recovered tail envelope, recovered synchronizer, StreamName row, RegSeqRat row, DyadicRatCore row, agreement row, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming. The carrier reads a Real-completion seal backwards through the witness data that produced it: the classifier exposes the recovered ledger, envelope, and synchronizer together. The agreement and transport rows keep that recovery tied to the same seal. A consumer preserving the glued seal but changing the witness ledger leaves the displayed pullback route.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:cauchy-witness-pullback-carrier}, \autoref{thm:cauchy-witness-pullback-namecert-obligations}, \autoref{thm:cauchy-witness-pullback-real-seal-factorization}, \autoref{thm:cauchy-witness-pullback-recovered-ledger-determinacy}, \autoref{thm:cauchy-witness-pullback-tail-synchronizer-compatibility}, \autoref{thm:cauchy-witness-pullback-real-seal-readback-exactness}, \autoref{thm:cauchy-witness-pullback-scoped-primitive-dependency-binding}, and \leanchecked{BEDC.Derived.CauchyWitnessPullbackUp.taste\_gate} bind the Cauchy witness pullback carrier, recovered-ledger determinacy, tail synchronizer compatibility, Real-seal readback exactness, scoped primitive dependency surface, prediction, and independence witness.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.CauchyWitnessPullbackUp.TasteGate.CauchyWitnessPullbackTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai-composite}
  \notclaimed{It does not claim arbitrary pullback of completed reals, quotient sequence equality, hidden witness recovery, or ambient completeness.}
  \upgradepath{To reach publicClosure, provide the public certificate export theorem for standard reflection consumers of the checked pullback carrier.}
\end{closurestatus}
