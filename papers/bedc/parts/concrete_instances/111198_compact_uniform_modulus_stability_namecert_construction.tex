\chapter{A Concrete Naming Certificate for $\CompactUniformModulusStabilityUp$}
\label{ch:concrete-instances-compact-uniform-modulus-stability-namecert}
\origin{human}

Following the Bishop--Bridges compact finite-net proof of uniform continuity, the $\CompactUniformModulusStabilityUp$ packet records the stability surface that keeps compact nets, pointwise moduli, lower-bound folds, and the final uniform-modulus handoff synchronized. It sits over the compact selector of \autoref{ch:concrete-instances-compactnetmodulusselector-namecert}, the compact uniform modulus row of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}, the finite lower-bound fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, the consumer surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the metric triangle estimate of \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}, and the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}. The packet supplies a finite stability object for compact-uniform consumers; it does not select a host modulus, a hidden center, a quotient equality, or an ambient compactness theorem.

\begin{definition}[Compact uniform modulus stability carrier]
\label{def:compact-uniform-modulus-stability-carrier}
A $\CompactUniformModulusStabilityUp$ carrier is a finite $\BHist$ packet
$$
S=(K,N,M,F,U,T,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row.
\item $N$ is the $\CompactNetModulusSelectorUp$ selector row carrying bundled centers, pointwise radii, and selected precision data.
\item $M$ is the $\CompactUniformModulusUp$ compact-uniform handoff row.
\item $F$ is the $\FiniteLowerBoundFoldUp$ row that synchronizes the finite radius ledger with the selected precision.
\item $U$ is the downstream $\UniformModulusUp$ consumer row.
\item $T$ is the $\MetricTriangleUniformEstimateUp$ row that transports the selected center estimates into the final source-target bound.
\item $H$ records componentwise $\hsame$ transport for compact, selector, modulus, fold, triangle, consumer, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-uniform tolerance request through $K,N,M,F,U,T,H,P,L$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusStabilityUp$, $\CompactMetricUp$, $\CompactNetModulusSelectorUp$, $\CompactUniformModulusUp$, $\FiniteLowerBoundFoldUp$, $\UniformModulusUp$, $\MetricTriangleUniformEstimateUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\CompactUniformModulusStabilityUp}$ row.
\end{enumerate}
The carrier compares accepted stability packets only through these rows. It contains no host compactness principle, selected center outside the finite net, external modulus function, quotient point equality, or choice schedule.
\end{definition}
\leanchecked{BEDC.Derived.CompactUniformModulusStabilityUp.CompactUniformModulusStabilityTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Compact uniform modulus stability NameCert obligations]
\label{thm:compact-uniform-modulus-stability-namecert-obligations}
For every accepted $\CompactUniformModulusStabilityUp$ carrier $S=(K,N,M,F,U,T,H,C,P,L)$, the local $\NameCert_{\CompactUniformModulusStabilityUp}$ surface consists exactly of compact finite-net admission through $K$, compact selector exposure through $N$, compact-uniform modulus handoff through $M$, lower-bound fold synchronization through $F$, uniform-modulus consumer export through $U$, metric-triangle estimate transport through $T$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-stability-carrier}. The compact source row $K$ and selector row $N$ provide the finite centers, pointwise modulus rows, subordinate radii, and selected precision before any consumer reads a modulus.

The lower-bound fold row $F$ synchronizes those finite radius rows with the selected precision. The compact-uniform row $M$ and uniform-modulus row $U$ therefore read the same finite ledger rather than an external modulus function.

The triangle row $T$ is the final estimate route. It uses the selected center and the two source-distance bounds already exposed by the selector and fold rows, so it cannot introduce a hidden center or host compactness argument.

Transport, replay, provenance, and local naming preserve the same packet through $H,C,P,L$. Since no excluded object appears among the coordinates, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Compact uniform modulus stability handoff]
\label{thm:compact-uniform-modulus-stability-handoff}
Every compact-uniform-continuity consumer of an accepted $\CompactUniformModulusStabilityUp$ packet reads the stable route
$$
\CompactMetricUp \longmapsto \CompactNetModulusSelectorUp \longmapsto \FiniteLowerBoundFoldUp \longmapsto \MetricTriangleUniformEstimateUp \longmapsto \CompactUniformModulusUp \longmapsto \UniformModulusUp .
$$
Thus the consumer receives one synchronized finite packet of compact-net rows, pointwise-modulus rows, lower-bound fold data, selected precision, metric-triangle estimates, transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Start from a tolerance request. The compact metric row $K$ and selector row $N$ expose the finite net, selected center candidates, pointwise radii, and compact selector data.

Apply the fold row $F$. It binds the selected precision to the same finite radius ledger, so the request cannot move to a different center family or a host minimum.

Use the triangle estimate row $T$ to transport the source bounds and pointwise estimates into the target bound consumed by $M$ and then by $U$. The compact-uniform and uniform-modulus rows therefore receive the same synchronized finite route.

The structural rows $H,C,P,L$ keep this route componentwise stable. No selected center outside the packet, quotient equality, choice schedule, host compactness, or external modulus function is exported.
\end{proof}

\closureat{CompactUniformModulusStabilityUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusStabilityUp}
  \constructivestory{A $\CompactUniformModulusStabilityUp$ packet is generated from CompactMetric finite-net rows, CompactNetModulusSelector rows, CompactUniformModulus rows, FiniteLowerBoundFold rows, UniformModulus consumer rows, MetricTriangleUniformEstimate rows, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-stability-carrier}, \autoref{thm:compact-uniform-modulus-stability-namecert-obligations}, and \autoref{thm:compact-uniform-modulus-stability-handoff} seed the stability certificate tying compact-net selectors, folds, triangle estimates, compact-uniform rows, and uniform-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host compactness, selected centers outside the finite packet, quotient point equality, choice schedules, external modulus functions, bridge checking, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, selector stability, fold synchronization, metric-triangle handoff, uniform-modulus export, replay stability, provenance exhaustion, and local naming for BEDC.Derived.CompactUniformModulusStabilityUp.}
\end{closurestatus}
