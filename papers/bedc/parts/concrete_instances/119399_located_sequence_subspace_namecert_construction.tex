\chapter{A Concrete Naming Certificate for $\LocatedSequenceSubspaceUp$}
\label{ch:concrete-instances-located-sequence-subspace-namecert}
\origin{human}

Following Bishop and Bridges, the $\LocatedSequenceSubspaceUp$ packet records the constructive carrier for located subspaces of real sequences as finite NameCert data. It sits after the located real sequence surface of \autoref{ch:concrete-instances-located-real-sequence-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the stream naming discipline of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a sequence-subspace membership classifier with finite-window stability and transport rows, without importing a host powerset of sequences, a quotient completion, a selected dense enumeration, or a Heine--Borel compactness principle.

\begin{definition}[LocatedSequenceSubspace carrier]
\label{def:located-sequence-subspace-carrier}
A $\LocatedSequenceSubspaceUp$ carrier is a finite $\BHist$ packet
$$
S=(A,M,W,T,L,H,C,P,N)
$$
whose rows have the following roles:
\begin{enumerate}
\item $A$ is the admitted located real sequence ambient row, routed through the sibling located-sequence carrier.
\item $M$ is the located membership classifier for the displayed subspace.
\item $W$ is the finite-window stability row for truncations and regular rational readback.
\item $T$ is the subspace transport row preserving membership under componentwise sequence equality.
\item $L$ is the ledger boundary recording the non-escape of membership reads from the displayed rows.
\item $H,C,P,N$ are componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\LocatedSequenceSubspaceUp}$ naming.
\end{enumerate}
The carrier has no coordinate for ambient powersets of sequence spaces, quotient equality of Cauchy representatives, selected representatives of Real values, countable choice, or compactness data.
\end{definition}

\begin{theorem}[LocatedSequenceSubspace NameCert obligations]
\label{thm:located-sequence-subspace-namecert-obligations}
For every accepted $\LocatedSequenceSubspaceUp$ carrier $S=(A,M,W,T,L,H,C,P,N)$, the local $\NameCert_{\LocatedSequenceSubspaceUp}$ surface consists exactly of the following five obligation rows:
\begin{enumerate}
\item carrier admission for the ambient located real sequence row $A$ together with provenance $P$ and local naming $N$;
\item located membership classifier exposure through $M$, read only after the stream and Real-seal rows cited above are visible;
\item finite-window stability through $W$, using $\StreamNameUp$ windows and $\RegSeqRatUp$ readback rather than host convergence;
\item subspace transport through $T,H,C$, preserving the displayed classifier under componentwise sequence equality and replay;
\item ledger non-escape through $L$, excluding host powersets, quotient completion, selected representatives, countable choice, and Heine--Borel compactness.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-sequence-subspace-carrier}. The row $A$ supplies the admitted ambient sequence surface, while $M$ is the only membership classifier made public.

The finite-window row $W$ routes every stability read through stream windows and regular rational readback before the Real seal is consumed. Thus no host convergence or quotient-completion row can replace the displayed finite data.

Transport is then componentwise through $T,H,C$. These rows preserve the same membership classifier under accepted sequence equality and replay, while $P,N$ source and name the finite packet.

Finally the ledger row $L$ records the boundary of the export. Since the carrier contains no coordinate for powersets, selected representatives, countable choice, or compactness, the listed five rows exhaust the local NameCert obligation surface.
\end{proof}

\closureat{LocatedSequenceSubspaceUp}{seedStr}

\begin{closurestatus}{\LocatedSequenceSubspaceUp}
  \constructivestory{A $\LocatedSequenceSubspaceUp$ packet is generated from an ambient located real sequence row, a located membership classifier, finite-window stability, subspace transport, ledger non-escape, componentwise transport, replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-sequence-subspace-carrier} and \autoref{thm:located-sequence-subspace-namecert-obligations} name the carrier and its five-row obligation surface for located subspaces of real sequences.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close separability, compactness, choice principles, quotient completion, Lean-side verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located membership, finite-window stability, subspace transport, and ledger non-escape routes for BEDC.Derived.LocatedSequenceSubspaceUp.}
\end{closurestatus}
