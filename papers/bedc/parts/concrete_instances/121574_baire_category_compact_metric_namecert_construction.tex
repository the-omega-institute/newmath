\chapter{A Concrete Naming Certificate for $\BaireCategoryCompactMetricUp$}
\label{ch:concrete-instances-baire-category-compact-metric-namecert}
\origin{human}

Following Baire, Oxtoby, and Kelley, the $\BaireCategoryCompactMetricUp$ packet records the finite compact-metric route by which a dense-open schedule is read as an input surface for the complete-metric Baire theorem. It sits between the Baire category theorem of \autoref{ch:concrete-instances-bairecategory-namecert}, the complete-metric Baire surface of \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert}, the compact-metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the displayed finite bridge from compact-metric data to the complete-metric Baire route; it does not claim countable choice, a host intersection point, open-cover compactness, quotient equality, or ambient $\RealUp$ completeness.

\begin{definition}[Baire category compact metric carrier]
\label{def:baire-category-compact-metric-carrier}
A $\BaireCategoryCompactMetricUp$ carrier is a finite $\BHist$ packet
$$
K=(D,F,B,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the dense-open schedule row, listing the finite prefix of dense-open tests consumed by the Baire read.
\item $F$ is the compact-metric finite-net source row, read through the $\CompactMetricUp$ carrier of \autoref{ch:concrete-instances-compactmetric-namecert}.
\item $B$ is the $\CompleteMetricBaireSurfaceUp$ inspection row, exposing the complete-metric Baire route of \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert} only after $D$ and $F$ are displayed.
\item $S$ is the $\StreamNameUp$ window row controlling the finite schedule prefix inspected by the consumer.
\item $Q$ is the $\RegSeqRatUp$ readback row for the rational tolerances and window bounds attached to $S$.
\item $E$ is the terminal $\RealUp$ seal row reached from the regular rational readback.
\item $H$ records componentwise $\hsame$ transport for the schedule, compact-metric source, Baire inspection, stream window, regular readback, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the dense-open schedule and compact finite-net source through the complete-metric Baire inspection row.
\item $P$ is the $\Pkg$ provenance over $\BaireCategoryCompactMetricUp$, $\BaireCategoryTheoremUp$, $\CompleteMetricBaireSurfaceUp$, $\CompactMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BaireCategoryCompactMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,F,B,S,Q,E,H,C,P,N$. It has no coordinate for countable choice, a host intersection witness, open-cover compactness, quotient equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[BaireCategoryCompactMetric NameCert obligations]
\label{thm:baire-category-compact-metric-namecert-obligations}
For every accepted $\BaireCategoryCompactMetricUp$ carrier
$$
K=(D,F,B,S,Q,E,H,C,P,N),
$$
the local $\NameCert_{\BaireCategoryCompactMetricUp}$ surface consists exactly of dense-open schedule admission for $D$, compact-metric finite-net source admission for $F$, $\CompleteMetricBaireSurfaceUp$ inspection for $B$, $\StreamNameUp$ finite-window exposure for $S$, $\RegSeqRatUp$ readback for $Q$, $\RealUp$ sealing for $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, sibling provenance through $P$, and local naming through $N$.

No Baire category compact-metric obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The dense-open schedule $D$ and compact-metric finite-net source $F$ are the only inputs available to the packet, and the complete-metric Baire inspection row $B$ is read only after both rows are displayed.

The finite-window and rational readback rows $S$ and $Q$ then expose the inspected prefix and its tolerance data before the terminal Real seal $E$ is consumed. This is a finite compact-metric route, not a selected point of the full intersection.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier contains no row for countable choice, host intersection, open-cover compactness, quotient equality, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[BaireCategoryCompactMetric route]
\label{thm:baire-category-compact-metric-route}
Every consumer that reads a compact-metric Baire handoff from an accepted $\BaireCategoryCompactMetricUp$ carrier must factor the read through
$$
D,F,B,S,Q,E,H,C,P,N .
$$
The handoff reaches the $\BaireCategoryTheoremUp$ surface of \autoref{ch:concrete-instances-bairecategory-namecert} only through the $\CompleteMetricBaireSurfaceUp$ inspection row, after the compact-metric finite-net source, stream window, regular rational readback, and Real seal have all been displayed. It exports no countable-choice schedule, host intersection point, open-cover compactness theorem, quotient equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:baire-category-compact-metric-carrier}. It fixes the public compact-metric Baire packet as the rows $D,F,B,S,Q,E,H,C,P,N$ and gives no consumer a separate complete-metric or Real-facing coordinate.

Apply the NameCert obligations of \autoref{thm:baire-category-compact-metric-namecert-obligations}. They require the dense-open schedule, compact finite-net source, complete-metric Baire inspection, stream window, rational readback, and Real seal to be displayed in that order before the local name row is consumed.

The structural rows then preserve the same route under $\hsame$ transport, $\Cont$ replay, and $\Pkg$ provenance. Thus the compact-metric Baire handoff is a finite bridge into the complete-metric Baire route, not an assertion of countable choice, host intersection, open-cover compactness, quotient equality, or ambient completeness.
\end{proof}

\closureat{BaireCategoryCompactMetricUp}{seedStr}

\begin{closurestatus}{\BaireCategoryCompactMetricUp}
  \constructivestory{A $\BaireCategoryCompactMetricUp$ packet is generated from a dense-open schedule row, a compact-metric finite-net source row, a CompleteMetricBaireSurface inspection row, a StreamName window row, a RegSeqRat readback row, a Real seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:baire-category-compact-metric-carrier}, \autoref{thm:baire-category-compact-metric-namecert-obligations}, and \autoref{thm:baire-category-compact-metric-route} seed the finite compact-metric Baire handoff through dense-open schedules, finite nets, complete-metric Baire inspection, stream windows, rational readback, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim countable choice, a host intersection point, open-cover compactness, quotient equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked dense-open schedule admission, compact-metric finite-net admission, complete-metric Baire inspection, stream-window exposure, regular rational readback, Real sealing, transport stability, replay stability, provenance, and non-escape for BEDC.Derived.BaireCategoryCompactMetricUp.}
\end{closurestatus}
