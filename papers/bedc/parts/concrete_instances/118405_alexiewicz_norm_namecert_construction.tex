\chapter{A Concrete Naming Certificate for $\AlexiewiczNormUp$}
\label{ch:concrete-instances-alexiewicz-norm-namecert}
\origin{human}

Following Alexiewicz and the Henstock--Kurzweil integral literature, the $\AlexiewiczNormUp$ packet records the finite BEDC surface by which a displayed primitive-window norm is read from gauge-integral data before any real-valued bound is exported. It sits beside the Henstock--Kurzweil route of \autoref{ch:concrete-instances-henstock-kurzweil-namecert}, the gauge-integral packet of \autoref{ch:concrete-instances-gauge-integral-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names a norm carrier for one displayed integration window; it does not import measure completion, quotient function equality, selected suprema, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[AlexiewiczNorm carrier]
\label{def:alexiewicz-norm-carrier}
An $\AlexiewiczNormUp$ carrier is a finite $\BHist$ packet
$$
A=(K,G,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\HenstockKurzweilUp$ integrability row that supplies the accepted gauge-integral source.
\item $G$ is the $\GaugeIntegralUp$ row recording the finite tagged-window integral route used by the primitive read.
\item $W$ is the primitive-window row, carrying the displayed interval endpoints and the finite list of subinterval reads on which the norm request is evaluated.
\item $D$ is the $\DyadicRatCoreUp$ endpoint and tolerance ledger for those windows.
\item $R$ is the $\RegSeqRatUp$ readback row assembling the visible rational bounds for the primitive-window values.
\item $E$ is the $\RealUp$ seal row reached only after $K,G,W,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the Henstock--Kurzweil, gauge-integral, primitive-window, dyadic, readback, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from an Alexiewicz-norm request through $K,G,W,D,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\AlexiewiczNormUp$, $\HenstockKurzweilUp$, $\GaugeIntegralUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AlexiewiczNormUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,G,W,D,R,E,H,C,P,N$. There is no coordinate for a measure space, quotient equality of functions, selected primitive, selected supremum, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[AlexiewiczNorm NameCert obligations]
\label{thm:alexiewicz-norm-namecert-obligations}
For every accepted $\AlexiewiczNormUp$ carrier $A=(K,G,W,D,R,E,H,C,P,N)$, the local $\NameCert_{\AlexiewiczNormUp}$ surface consists exactly of carrier admission, Henstock--Kurzweil exposure through $K$, gauge-integral exposure through $G$, primitive-window exactness through $W$, DyadicRatCore endpoint and tolerance exposure through $D$, RegSeqRat readback through $R$, Real seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

No obligation may be discharged by measure completion, quotient function equality, selected primitives, selected suprema, countable choice, host almost-everywhere equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:alexiewicz-norm-carrier}. The displayed rows are exactly $K,G,W,D,R,E,H,C,P,N$, so carrier admission exposes only Henstock--Kurzweil, gauge-integral, primitive-window, dyadic, readback, seal, transport, replay, provenance, and naming data.

The integration side starts with $K$ and $G$. The Henstock--Kurzweil row supplies the accepted integral source, while the gauge-integral row supplies the finite tagged-window route through which primitive-window values are read.

The numerical side then reads $W,D,R,E$ in order. The primitive-window row fixes the displayed interval reads, the dyadic row supplies endpoint and tolerance data, the regular rational row records the visible bounds, and the real seal is available only after those rows have been replayed.

Finally the structural rows $H,C,P,N$ preserve the same route. Since the excluded measure, quotient, selection, choice, host-equality, and completeness principles have no carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[AlexiewiczNorm Henstock handoff]
\label{thm:alexiewicz-norm-henstock-handoff}
Every completion-facing or Real-facing read of an accepted $\AlexiewiczNormUp$ carrier factors through the finite route
$$
\HenstockKurzweilUp \longmapsto \GaugeIntegralUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The primitive-window row $W$ is read before the dyadic tolerance ledger and the real seal, and the route cannot bypass $K,G,W,D,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Begin with an accepted carrier read. The carrier exposes the Henstock--Kurzweil source $K$ and the gauge-integral row $G$ before any norm-facing numerical read can be inspected.

Replay the continuation row $C$. The read moves from the integration rows through the primitive-window row $W$, then through the dyadic ledger $D$ and regular rational readback $R$ before it reaches the seal $E$.

The structural rows $H,P,N$ do not add another norm object. They preserve the same finite handoff, and no excluded measure, quotient, selection, choice, host-equality, or completeness coordinate is available in the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\AlexiewiczNormUp$ read factors through $\HenstockKurzweilUp$, $\GaugeIntegralUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\closureat{AlexiewiczNormUp}{seedStr}

\begin{closurestatus}{\AlexiewiczNormUp}
  \constructivestory{An $\AlexiewiczNormUp$ packet is generated from a HenstockKurzweil row, GaugeIntegral row, primitive-window row, DyadicRatCore endpoint and tolerance rows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:alexiewicz-norm-carrier}, \autoref{thm:alexiewicz-norm-namecert-obligations}, and \autoref{thm:alexiewicz-norm-henstock-handoff} seed the finite primitive-window norm route over HenstockKurzweilUp, GaugeIntegralUp, DyadicRatCoreUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, measure completion, quotient function equality, selected primitives, selected suprema, countable choice, host almost-everywhere equality, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, primitive-window exactness, HenstockKurzweil exposure, GaugeIntegral exposure, DyadicRatCore tolerance exactness, RegSeqRat readback, Real seal nonescape, and TasteGate field faithfulness for BEDC.Derived.AlexiewiczNormUp.}
\end{closurestatus}
