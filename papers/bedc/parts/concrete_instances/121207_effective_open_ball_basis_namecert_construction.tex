\chapter{A Concrete Naming Certificate for $\EffectiveOpenBallBasisUp$}
\label{ch:concrete-instances-effective-open-ball-basis-namecert}
\origin{human}

Following Bishop--Bridges constructive metric topology, the $\EffectiveOpenBallBasisUp$ packet records the finite center-radius basis rows by which metric open balls are exposed to topology consumers. It sits over the metric source of \autoref{ch:concrete-instances-metric-space-namecert}, the dyadic positive-radius ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the indexed open-set surface of \autoref{ch:concrete-instances-topology-namecert}. The packet is a finite basis object for Phase Real and Completion consumers; it does not import open-cover compactness, quotient equality, trichotomy, choice, or ambient $\RealUp$ completeness.

\begin{definition}[Effective open-ball basis carrier]
\label{def:effective-open-ball-basis-carrier}
An $\EffectiveOpenBallBasisUp$ carrier is a finite $\BHist$ packet
$$
B=(M,C,D,W,R,E,T,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricSpaceUp$ source row whose distance comparison supplies the ball predicate.
\item $C$ is the finite center schedule row, naming the displayed centers that may be used by basis consumers.
\item $D$ is the $\DyadicRatCoreUp$ positive-radius ledger, carrying the requested radii, slack rows, and comparison budgets.
\item $W$ is the $\StreamNameUp$ window row through which finite observations of centers and requested points are read.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same windows and dyadic tolerance ledger.
\item $E$ is the $\RealUp$ seal row for the finite real-name reads used in distance and radius comparison.
\item $T$ is the $\TopologyUp$ handoff row, exporting only the indexed open rows generated by displayed center-radius data.
\item $H$ records componentwise $\hsame$ transport for the metric source, centers, dyadic radii, windows, readback, real seal, topology handoff, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay of the metric-ball-to-topology route.
\item $P$ is the $\Pkg$ provenance over $\EffectiveOpenBallBasisUp$, $\MetricSpaceUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\TopologyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EffectiveOpenBallBasisUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,C,D,W,R,E,T,H,K,P,N$. It has no coordinate for open-cover compactness, quotient equality of points, real trichotomy, countable choice, selected completions, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.EffectiveOpenBallBasisUp}

\begin{theorem}[Effective open-ball basis NameCert obligations]
\label{thm:effective-open-ball-basis-namecert-obligations}
For every accepted $\EffectiveOpenBallBasisUp$ carrier
$$
B=(M,C,D,W,R,E,T,H,K,P,N),
$$
the local $\NameCert_{\EffectiveOpenBallBasisUp}$ surface consists exactly of metric-source admission, finite center schedule exposure, dyadic positive-radius exactness, stream-window readback, regular rational replay, real-name sealing, topology handoff, componentwise transport, displayed continuation replay, provenance, and non-escape. In particular, a topology consumer may read an open-ball basis row only after the rows $M,C,D,W,R,E,T,H,K,P,N$ have been displayed in the same finite packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:effective-open-ball-basis-carrier}. The metric source $M$, center schedule $C$, and dyadic radius ledger $D$ are the only rows that can generate a basis ball.

The readback rows $W,R,E$ then fix how finite observations and real-name comparisons are exposed. They keep distance and radius comparison inside displayed stream windows, regular rational data, and the terminal real seal.

The topology handoff row $T$ exports only the indexed open rows generated from those displayed center-radius data. Transport, replay, provenance, and local naming are carried by $H,K,P,N$ for the same packet.

Since no row stores open-cover compactness, quotient equality, trichotomy, choice, selected completions, or ambient Real completeness, the listed coordinates exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Effective open-ball basis metric topology route]
\label{thm:effective-open-ball-basis-metric-topology-route}
Every $\TopologyUp$ consumer of an accepted $\EffectiveOpenBallBasisUp$ packet must factor its open-set read through the displayed $\MetricSpaceUp$ source row $M$, the center schedule $C$, the $\DyadicRatCoreUp$ radius ledger $D$, the $\StreamNameUp$ and $\RegSeqRatUp$ readback rows $W,R$, the $\RealUp$ seal $E$, and the topology handoff row $T$. The route exports a finite center-radius basis surface and no open-cover compactness theorem, quotient point equality, real trichotomy, choice principle, or ambient completion of $\RealUp$.
\end{theorem}
\begin{proof}
Start with the carrier projection. The rows $M,C,D$ display the metric source, the available centers, and the positive radii before any topological read occurs.

Next read $W,R,E$. These rows make the finite distance and radius comparisons visible through stream windows, regular rational readback, and the Real seal, so the basis route is not a host-side predicate over hidden points.

Finally consume $T$ together with $H,K,P,N$. The topology handoff is therefore the image of the displayed metric-ball rows under recorded transport and replay, and the consumer receives no additional compactness, quotient, trichotomy, choice, or ambient-completeness coordinate.
\end{proof}

\falsifiablePrediction{Every accepted $\EffectiveOpenBallBasisUp$ consumer must display a $\MetricSpaceUp$ source row, a finite center schedule, a $\DyadicRatCoreUp$ radius ledger, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a $\RealUp$ seal, a $\TopologyUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row before an open-ball basis row can be consumed.}

\independenceWitness{metricspace, dyadicratcore, streamname, regseqrat, real, topology: $\EffectiveOpenBallBasisUp$ is not bijective to any listed sibling because it binds metric centers and dyadic radii into a topology-facing basis handoff before any indexed open row becomes public.}

\closureat{EffectiveOpenBallBasisUp}{seedStr}

\begin{closurestatus}{\EffectiveOpenBallBasisUp}
  \constructivestory{An $\EffectiveOpenBallBasisUp$ packet is generated from a MetricSpace source row, finite center schedule, DyadicRatCore positive-radius ledger, StreamName windows, RegSeqRat readback, Real seal, Topology handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:effective-open-ball-basis-carrier}, \autoref{thm:effective-open-ball-basis-namecert-obligations}, and \autoref{thm:effective-open-ball-basis-metric-topology-route} seed the finite metric open-ball basis route from displayed center-radius data into indexed topology rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient equality, trichotomy, choice, selected completions, ambient $\RealUp$ completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, radius-ledger exactness, metric-ball readback, topology handoff, transport, replay, provenance, and non-escape for BEDC.Derived.EffectiveOpenBallBasisUp.}
\end{closurestatus}
