\chapter{A Concrete Naming Certificate for $\HadamardThreeCircleUp$}
\label{ch:concrete-instances-hadamard-three-circle-namecert}
\origin{human}

Following Hadamard's three-circle theorem and the classical complex-analysis reading of maximum-modulus bounds on annuli, the $\HadamardThreeCircleUp$ packet records a finite annulus ledger whose radius reads, boundary maxima, and logarithmic-convexity row are all displayed before a holomorphic-boundary consumer may read a bound. It sits over the complex carrier of \autoref{ch:concrete-instances-complex-namecert}, the circle boundary surface of \autoref{ch:concrete-instances-s1-namecert}, the Real seal of \autoref{ch:concrete-instances-real-namecert}, the holomorphic carrier of \autoref{ch:concrete-instances-holomorphic-namecert}, the finite integral row of \autoref{ch:concrete-instances-integral-namecert}, and the metric annulus packet of \autoref{ch:concrete-instances-metric-annulus-namecert}. The chapter names the finite three-radius certificate only; it does not assert an ambient analytic continuation theorem, a selected maximizer, quotient function equality, or a bridge certificate.

\begin{definition}[Hadamard three-circle carrier]
\label{def:hadamard-three-circle-carrier}
A $\HadamardThreeCircleUp$ carrier is a finite $\BHist$ packet
$$
H=(A,F,R_0,R_1,R_2,B,M,L,Hs,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the displayed annulus row, read through $\MetricAnnulusUp$ and carrying the two boundary circles and the inspected middle radius.
\item $F$ is the $\HolomorphicUp$ map row over the annulus, with values in the $\ComplexUp$ carrier.
\item $R_0,R_1,R_2$ are the three positive $\RealUp$ radius reads, ordered by the annulus row and sealed before any maximum row is consumed.
\item $B$ is the boundary-circle readback row, using $\SOneUp$ to expose the inner, middle, and outer circle probes.
\item $M$ is the maximum-modulus row for the displayed boundary reads, recording only finite upper-bound data on the inspected circles.
\item $L$ is the logarithmic-convexity ledger tying the three radius reads and the corresponding maximum-modulus rows to a single finite inequality certificate.
\item $Hs$ records componentwise $\hsame$ transport for the annulus, holomorphic map, radius reads, boundary reads, maximum rows, logarithmic-convexity ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an annulus-bound request through $A,F,R_0,R_1,R_2,B,M,L,Hs$.
\item $P$ is the $\Pkg$ provenance over $\HadamardThreeCircleUp$, $\MetricAnnulusUp$, $\HolomorphicUp$, $\ComplexUp$, $\SOneUp$, $\RealUp$, $\IntegralUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HadamardThreeCircleUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for a host supremum theorem, an uninspected boundary point, a selected maximizer, quotient function equality, analytic continuation outside the annulus, or countable choice.
\end{definition}

\begin{theorem}[Hadamard three-circle NameCert obligations]
\label{thm:hadamard-three-circle-namecert-obligations}
For every accepted $\HadamardThreeCircleUp$ carrier $H=(A,F,R_0,R_1,R_2,B,M,L,Hs,C,P,N)$, the local $\NameCert_{\HadamardThreeCircleUp}$ surface is exhausted by annulus admission, holomorphic-map exposure, three sealed radius reads, boundary-circle readback, maximum-modulus rows, logarithmic-convexity ledger compatibility, componentwise transport, displayed replay, provenance, and local naming. A consumer of the packet may not infer an ambient maximum principle, a selected extremal point, analytic continuation, quotient function equality, or any infinite covering principle from the local certificate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hadamard-three-circle-carrier}. The rows $A,F,R_0,R_1,R_2$ display the annulus, the holomorphic map, and the three radius reads before a bound can be consumed.

The boundary computation is then finite. The row $B$ exposes the inspected $\SOneUp$ circle probes, $M$ records the corresponding maximum-modulus upper-bound rows, and $L$ binds those rows to the logarithmic-convexity ledger for the same three radii.

Finally the structural rows $Hs,C,P,N$ transport, replay, cite provenance, and name that same finite route. Since none of these rows adds a host supremum theorem, a selected maximizer, an uninspected boundary point, analytic continuation, quotient equality, or countable choice, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hadamard annulus maximum handoff]
\label{thm:hadamard-three-circle-annulus-maximum-handoff}
Every holomorphic-boundary consumer of an accepted $\HadamardThreeCircleUp$ carrier factors through
$$
\MetricAnnulusUp \longmapsto \HolomorphicUp \longmapsto \ComplexUp \longmapsto \SOneUp \longmapsto \RealUp \longmapsto \IntegralUp
$$
together with the displayed rows $A,F,R_0,R_1,R_2,B,M,L,Hs,C,P,N$. The handoff exports a finite three-radius annulus ledger and its logarithmic-convexity certificate, not a global maximum-modulus theorem or an ambient analytic object outside the displayed annulus.
\end{theorem}
\begin{proof}
Start from the annulus row. The packet reads the displayed $\MetricAnnulusUp$ data $A$ and the $\HolomorphicUp$ map row $F$ before any boundary maximum is available.

Next seal the radius and boundary rows. The three $\RealUp$ reads $R_0,R_1,R_2$ determine the inspected circles, and $B$ turns those circles into finite $\SOneUp$ boundary probes over the same annulus.

The maximum handoff is then local. The row $M$ records the finite boundary upper bounds, while $L$ ties them to the logarithmic-convexity inequality for the same radii. The $\IntegralUp$ dependency is consumed only as a displayed finite contour or averaging row, not as an ambient selection principle.

The transport and naming rows $Hs,C,P,N$ preserve this route. Therefore a consumer receives exactly the finite annulus ledger and no global maximum principle, selected extremal point, analytic continuation coordinate, quotient equality, or infinite covering data.
\end{proof}

\falsifiablePrediction{An accepted $\HadamardThreeCircleUp$ read is refuted if the logarithmic-convexity row $L$ is preserved while the displayed annulus row $A$, holomorphic map row $F$, sealed radii $R_0,R_1,R_2$, boundary-circle row $B$, and maximum-modulus row $M$ are removed.}

\independenceWitness{complex, sone, real, holomorphic, integral, metric-annulus: none is bijective with $\HadamardThreeCircleUp$ because the packet jointly carries annulus rows, holomorphic map rows, three sealed radius reads, boundary circle probes, maximum-modulus rows, a logarithmic-convexity ledger, transport, replay, provenance, and local naming.}

\closureat{HadamardThreeCircleUp}{seedStr}

\begin{closurestatus}{\HadamardThreeCircleUp}
  \constructivestory{A $\HadamardThreeCircleUp$ packet is generated from a displayed annulus, a holomorphic map row, three Real radius reads, boundary circle probes, maximum-modulus rows, a logarithmic-convexity ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hadamard-three-circle-carrier}, \autoref{thm:hadamard-three-circle-namecert-obligations}, and \autoref{thm:hadamard-three-circle-annulus-maximum-handoff} seed the finite annulus ledger for three-circle maximum-modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a global maximum-modulus theorem, selected extremal points, analytic continuation beyond the annulus, quotient function equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked annulus admission, holomorphic-map exposure, three-radius ordering, boundary-circle readback, maximum-modulus rows, logarithmic-convexity compatibility, and structural transport for BEDC.Derived.HadamardThreeCircleUp.}
\end{closurestatus}
