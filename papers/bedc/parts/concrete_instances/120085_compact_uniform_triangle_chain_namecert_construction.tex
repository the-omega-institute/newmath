\chapter{A Concrete Naming Certificate for $\CompactUniformTriangleChainUp$}
\label{ch:concrete-instances-compact-uniform-triangle-chain-namecert}
\origin{human}

Following Heine--Cantor and Bishop--Bridges compact-uniform reasoning, the $\CompactUniformTriangleChainUp$ packet records the finite source-and-target triangle chain by which compact metric coverage, pointwise continuity, rational tolerance folds, and target metric estimates are read before a $\UniformModulusUp$ consumer receives a tolerance-only handoff. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map graph of \autoref{ch:concrete-instances-continuousmap-namecert}, the metric law surface of \autoref{ch:concrete-instances-metric-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It shares the final estimate boundary with \autoref{ch:concrete-instances-compact-uniform-source-target-triangle-ledger-namecert} and \autoref{ch:concrete-instances-compact-uniform-final-triangle-closure-namecert}, but this packet names the whole chain from source coverage to target estimate as one finite NameCert carrier.

\begin{definition}[Compact-uniform triangle-chain carrier]
\label{def:compact-uniform-triangle-chain-carrier}
A $\CompactUniformTriangleChainUp$ carrier is a finite $\BHist$ packet
$$
T=(K,F,U,M_X,M_Y,D,S,A,R,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-source row, listing the selected compact-net center, source ball, and inspected source point pair.
\item $F$ is the $\ContinuousMapUp$ graph row whose pointwise-modulus reads are attached to the center named by $K$.
\item $U$ is the $\UniformModulusUp$ handoff row produced only after the triangle chain is complete.
\item $M_X$ and $M_Y$ are the source and target $\MetricUp$ law rows, including the triangle witnesses used on each side of $F$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance fold row, splitting the requested tolerance among source-radius, pointwise, and target-triangle contributions.
\item $S$ is the source-triangle row, showing that both source points remain in the displayed local ball around the selected center.
\item $A$ is the target-triangle row, joining the two pointwise target estimates through the center image.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational tolerance and metric-window data, and $E$ is the terminal $\RealUp$ seal.
\item $H$ records componentwise $\hsame$ transport for compact-net, continuous-map, uniform-modulus, source metric, target metric, dyadic fold, source triangle, target triangle, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
K,F,M_X,M_Y,D,S,A,R,E \longmapsto U,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactUniformTriangleChainUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\MetricUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformTriangleChainUp}$ row.
\end{enumerate}
The classifier compares accepted triangle-chain packets only through $K,F,U,M_X,M_Y,D,S,A,R,E,H,C,P,N$. It has no coordinate for a host compactness theorem, an external modulus selector, hidden center choice, quotient function equality, selected limits, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact-uniform triangle-chain obligations]
\label{thm:compact-uniform-triangle-chain-namecert-obligations}
For every accepted $\CompactUniformTriangleChainUp$ carrier
$$
T=(K,F,U,M_X,M_Y,D,S,A,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformTriangleChainUp}$ surface consists exactly of compact-source admission, continuous-map pointwise admission, source metric triangle admission, target metric triangle admission, dyadic tolerance-fold exactness, source-triangle compatibility, target-triangle compatibility, regular rational readback, Real seal routing, UniformModulus handoff, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-triangle-chain-carrier}. The compact-source and continuous-map data are $K$ and $F$, so the chain begins with a displayed center and a pointwise graph read at that center.

Read the metric and tolerance rows $M_X,M_Y,D$. The source and target metric rows provide the two triangle laws, while the dyadic fold row records the finite rational split of the requested tolerance.

The triangle rows are $S$ and $A$. The source triangle keeps both source points inside the selected local ball, and the target triangle composes the two pointwise target estimates through the center image.

Finally $R,E,U,H,C,P,N$ read back, seal, export, transport, replay, source, and name the same finite route. Since the carrier has no coordinate for host compactness, external modulus selection, hidden center choice, quotient function equality, selected limits, or ambient completeness, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Compact-uniform triangle-chain UniformModulus handoff]
\label{thm:compact-uniform-triangle-chain-uniform-modulus-handoff}
Every $\UniformModulusUp$-facing read of an accepted $\CompactUniformTriangleChainUp$ carrier factors through
$$
\CompactMetricUp,\quad
\ContinuousMapUp,\quad
\MetricUp,\quad
\DyadicRatCoreUp,\quad
\RegSeqRatUp,\quad
\RealUp
$$
before the handoff row $U$ is available. In carrier notation, the consumer must read $K,F,M_X,M_Y,D,S,A,R,E$ before $U,H,C,P,N$ can export the tolerance-only modulus route, and it receives no host compactness theorem, hidden center choice, quotient equality, selected limit, or ambient real completeness principle.
\end{theorem}
\begin{proof}
Start with the source packet $K$ and graph packet $F$. These rows display the compact-net center, the source point pair, and the pointwise continuity read consumed by the triangle chain.

Apply the metric rows $M_X,M_Y$ and the dyadic fold row $D$. The tolerance split is finite, and both triangle estimates are read as metric-law rows rather than as an ambient continuity theorem.

Use the source and target triangle rows $S,A$. They turn the compact-source coverage and pointwise center estimate into the target-distance estimate, after which $R$ and $E$ read the regular rational and Real-facing windows.

Only then can the handoff row $U$ be exported. The structural rows $H,C,P,N$ preserve and name this route, so a downstream consumer obtains a finite UniformModulus handoff and none of the refused host-level objects.
\end{proof}

\closureat{CompactUniformTriangleChainUp}{seedStr}

\begin{closurestatus}{\CompactUniformTriangleChainUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-triangle-chain-carrier}, \autoref{thm:compact-uniform-triangle-chain-namecert-obligations}, and \autoref{thm:compact-uniform-triangle-chain-uniform-modulus-handoff} seed the finite source-and-target triangle chain from CompactMetric, ContinuousMap, Metric, DyadicRatCore, RegSeqRat, Real, and UniformModulus rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host compactness, external modulus selection, hidden center choice, quotient function equality, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact-source admission, pointwise-continuity admission, source and target metric triangle exactness, dyadic tolerance-fold exactness, RegSeqRat readback, Real seal routing, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformTriangleChainUp.}
  \constructivestory{A $\CompactUniformTriangleChainUp$ packet is generated from CompactMetric finite-source rows, ContinuousMap pointwise rows, UniformModulus handoff rows, Metric source and target triangle laws, DyadicRatCore tolerance folds, RegSeqRat readbacks, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
