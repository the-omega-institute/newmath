\chapter{A Concrete Naming Certificate for $\WitnessedDescentLedgerUp$}
\label{ch:concrete-instances-witnessed-descent-ledger-namecert}
\origin{ai}

\falsifiablePrediction{Every public downward use of an upward name factors through the displayed witness row of an accepted $\WitnessedDescentLedgerUp$ packet.}

\independenceWitness{russellboundary, definitedescriptionboundary, contextualclassreading, russellanalysisladder: the witnessed-descent ledger is not bijective to any sibling because it isolates the source, bridge, descent request, witness, transport, provenance, and local NameCert rows for downward use, while the siblings bind the full Russell boundary, description-expansion boundary, contextual-class reading, or analysis ladder surface.}

The $\WitnessedDescentLedgerUp$ packet realizes the witnessed-descent restriction of \autoref{ch:visions-russell-problem-boundary} as a concrete NameCert surface. It sits beside the definite-description boundary of \autoref{ch:concrete-instances-definite-description-boundary-namecert}: that chapter controls when a description may be consumed as denoting, while this chapter controls when an already licensed upward name may be read downward by a consumer.

The packet also consumes the neighboring Russell surfaces. $\RussellBoundaryUp$ supplies the fivefold boundary, $\ContextualClassReadingUp$ supplies scoped classifier reading, and $\RussellAnalysisLadderUp$ supplies the analysis-spine context in which witnessed descent is one row among the full Russell discipline. The ledger below isolates the descent row itself so later real, rational-sequence, and bridge-readback consumers can cite a finite witness route rather than a slogan about harmless level passage.

\begin{definition}[Witnessed-descent ledger carrier]
\label{def:witnessed-descent-ledger-carrier}
A $\WitnessedDescentLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(S,B,D,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source row naming the upward interface whose public name is being consumed;
\item $B$ is the bridge row naming the stratum passage from the upward interface to the downward consumer boundary;
\item $D$ is the descent-request row recording the requested downward use of the upward name;
\item $W$ is the witness row certifying that this particular downward use is licensed;
\item $H$ records componentwise $\hsame$ transport for source, bridge, descent request, witness, continuation, provenance, and local name rows;
\item $C$ records the displayed $\Cont$ replay by which a consumer moves from $S$ through $B,D,W$ and returns to the local ledger;
\item $P$ is the $\Pkg$ provenance over $\BHist$, upward names, bridge rows, descent requests, witnesses, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\WitnessedDescentLedgerUp}$ row.
\end{enumerate}
The carrier has no coordinate for a stratum collapse, host identity between source and target, reducibility principle, quotient equality, proof-irrelevance shortcut, or unrecorded downward read.
\end{definition}
\leandef{BEDC.Derived.WitnessedDescentLedgerUp.WitnessedDescentLedgerUp}
\leanchecked{BEDC.Derived.WitnessedDescentLedgerUp.WitnessedDescentLedgerTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Witnessed-descent NameCert obligations]
\label{thm:witnessed-descent-ledger-namecert-obligations}
For the carrier of \autoref{def:witnessed-descent-ledger-carrier}, the local $\NameCert_{\WitnessedDescentLedgerUp}$ surface consists exactly of carrier admission for
$$
L=(S,B,D,W,H,C,P,N),
$$
source exposure through $S$, bridge exposure through $B$, descent-request accountability through $D$, witness exposure through $W$, componentwise transport through $H$, continuation replay through $C$, package provenance through $P$, local naming through $N$, and non-collapse of the upward and downward strata.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:witnessed-descent-ledger-carrier}. The source, bridge, request, witness, transport, replay, provenance, and local name are all displayed rows of the same finite packet.

The source and bridge obligations are $S$ and $B$. They identify which upward name is being consumed and which stratum passage is in scope before any downward read can proceed.

The request and witness obligations are $D$ and $W$. A consumer may ask for a downward read only through $D$, and the read is licensed only by the displayed witness row $W$.

Finally $H,C,P,N$ keep the read local to the packet. They transport, replay, cite, and name the same descent route, and no row remains from which a collapse, quotient equality, proof-irrelevance shortcut, or unrecorded downward use could be extracted.
\end{proof}

\begin{theorem}[Witnessed-descent public readback]
\label{thm:witnessed-descent-ledger-public-readback}
Every public downward read of an accepted $\WitnessedDescentLedgerUp$ packet factors through the rows
$$
S,B,D,W,H,C,P,N.
$$
In particular, the read exposes the source upward name, the bridge row, the descent request, the witness row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before the downward consumer receives the name.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:witnessed-descent-ledger-namecert-obligations}. They list the complete row surface available to a public consumer.

The downward read must first identify its source through $S$ and its bridge through $B$. It must then record the requested use through $D$ and expose the certificate witness through $W$.

The structural rows $H,C,P,N$ preserve that route as a finite BEDC packet. Since the carrier has no hidden coordinate for source-target identity, external reducibility, quotient equality, or unrecorded witness selection, every public downward read factors through the displayed ledger.
\end{proof}

\begin{theorem}[Witnessed-descent sibling boundary]
\label{thm:witnessed-descent-ledger-sibling-boundary}
The $\WitnessedDescentLedgerUp$ packet is a sibling of $\RussellBoundaryUp$, $\DefiniteDescriptionBoundaryUp$, $\ContextualClassReadingUp$, and $\RussellAnalysisLadderUp$, not a replacement for them. It consumes their boundary, description, contextual, and ladder surfaces only as neighboring rows and exports exactly the descent ledger rows $S,B,D,W,H,C,P,N$ for downstream bridge-readback consumers.
\end{theorem}
\begin{proof}
Read the neighboring chapters as separate NameCert surfaces. $\RussellBoundaryUp$ binds the fivefold Russell discipline, $\DefiniteDescriptionBoundaryUp$ binds description-expansion use, $\ContextualClassReadingUp$ binds scoped class-like readings, and $\RussellAnalysisLadderUp$ binds the analysis-spine packet.

The witnessed-descent ledger projects only the descent route. Its carrier names the source, bridge, request, witness, transport, replay, provenance, and local name rows needed by a downward consumer.

Therefore no sibling is collapsed into this packet and this packet is not a full Russell boundary. It is the concrete descent ledger that later consumers may cite when an upward name is read downward.
\end{proof}

\begin{theorem}[Displayed witness accountability]
\label{thm:witnessed-descent-ledger-displayed-witness-accountability}
Every accepted $\WitnessedDescentLedgerUp$ carrier $L=(S,B,D,W,H,C,P,N)$ accounts for a downward read only through the displayed witness row $W$. The TasteGate-facing carrier alignment records that the single carrier exposed to downstream consumers is the same finite packet whose rows display the source, bridge, descent request, witness, transport, replay, provenance, and local naming data.
\end{theorem}
\begin{proof}
Use the carrier of \autoref{def:witnessed-descent-ledger-carrier}. The descent request row $D$ records the requested downward use, while $W$ is the only row licensing that particular use.

The checked TasteGate alignment keeps the public carrier from being replaced by a detached event-flow presentation. Thus every consumer that reads the carrier reads the same displayed $W$ row before any downward name is available.

The structural rows $H,C,P,N$ then transport, replay, source, and name this route. Since the carrier omits unrecorded downward reads and proof-irrelevance shortcuts, witness accountability is exactly the displayed-row obligation.
\end{proof}

\begin{theorem}[Witnessed-descent noncollapse transport]
\label{thm:witnessed-descent-ledger-noncollapse-transport}
Componentwise $\hsame$ transport for an accepted $\WitnessedDescentLedgerUp$ packet preserves the separation between the source row $S$, bridge row $B$, descent-request row $D$, and witness row $W$. It never identifies the upward source stratum with the downward consumer boundary, and it never converts the witness row into quotient equality or proof-irrelevant reducibility.
\end{theorem}
\begin{proof}
Read transport through the row $H$ in \autoref{def:witnessed-descent-ledger-carrier}. It is componentwise: source, bridge, request, witness, continuation, provenance, and local name rows are transported as separate displayed coordinates.

The non-collapse claim follows from this row separation. Transport can preserve the finite route from $S$ through $B,D,W$, but it has no coordinate that turns the bridge into identity between strata.

Finally replay through $C$ and source through $P,N$. The omitted coordinates exclude quotient equality, proof irrelevance, and reducibility, so the transported packet remains a witnessed descent ledger rather than a collapse principle.
\end{proof}

\begin{theorem}[Witnessed-descent bridge-readback exhaustion]
\label{thm:witnessed-descent-ledger-bridge-readback-exhaustion}
Every bridge-readback consumer of an accepted $\WitnessedDescentLedgerUp$ packet must inspect the finite route
$$
S,B,D,W,H,C,P,N
$$
before it can consume the downward name. The route exhausts the readback surface: there is no additional bridge row, hidden witness selector, global descent rule, quotient equality, or identity between upward and downward strata available to the consumer.
\end{theorem}
\begin{proof}
Start from the public readback theorem \autoref{thm:witnessed-descent-ledger-public-readback}. It already forces every public downward read through the source, bridge, descent request, witness, transport, replay, provenance, and local NameCert rows.

For a bridge-readback consumer, the bridge row $B$ is therefore not optional. The consumer reaches the requested downward use only after $D$ and the displayed witness $W$ have been inspected.

The remaining rows $H,C,P,N$ close the route inside the packet. Since no second bridge row, selector, global rule, quotient equality, or stratum identity coordinate exists, the listed route exhausts the bridge-readback surface.
\end{proof}

\closureat{WitnessedDescentLedgerUp}{obligationStr}

\begin{closurestatus}{\WitnessedDescentLedgerUp}
  \constructivestory{A $\WitnessedDescentLedgerUp$ packet is generated from a source upward-name row, a bridge row, a descent-request row, a witness row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:witnessed-descent-ledger-carrier}, \autoref{thm:witnessed-descent-ledger-namecert-obligations}, \autoref{thm:witnessed-descent-ledger-public-readback}, \autoref{thm:witnessed-descent-ledger-sibling-boundary}, \autoref{thm:witnessed-descent-ledger-displayed-witness-accountability}, \autoref{thm:witnessed-descent-ledger-noncollapse-transport}, and \autoref{thm:witnessed-descent-ledger-bridge-readback-exhaustion} bind the finite source, bridge, request, witness, noncollapse transport, readback exhaustion, provenance, and local naming rows for downward use of upward names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not prove Lean-side formal verification, a standard bridge, a global descent rule, a reducibility principle, quotient equality, proof irrelevance, or identity between upward and downward strata.}
  \upgradepath{Scoped closure requires Lean-side verification of carrier admission, witness-row exactness, public readback factorization, sibling noncollapse, noncollapse transport, and bridge-readback exhaustion for the displayed packet.}
\end{closurestatus}
