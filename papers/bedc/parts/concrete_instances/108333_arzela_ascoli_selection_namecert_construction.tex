\chapter{A Concrete Naming Certificate for $\ArzelaAscoliSelectionUp$}
\label{ch:concrete-instances-arzela-ascoli-selection-namecert}
\origin{human}

Following Arzela--Ascoli, Kelley, and Bishop--Bridges constructive analysis, the $\ArzelaAscoliSelectionUp$ packet records the finite selection surface by which an equicontinuous bounded family on a compact metric source exposes a regular convergent subsequence. It sits after the family ledger of \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, the classical compactness packet of \autoref{ch:concrete-instances-arzelaascoli-namecert}, the bounded-family surface of \autoref{ch:concrete-instances-bounded-function-family-namecert}, and the diagonal extraction route of \autoref{ch:concrete-instances-ascoli-diagonal-namecert}. The packet names the selection route through displayed finite rows only; it does not export a host compact-open topology, an arbitrary sequence choice, quotient equality of functions, or ambient completeness outside the named certificates.

\begin{definition}[ArzelaAscoliSelection carrier]
\label{def:arzela-ascoli-selection-carrier}
An $\ArzelaAscoliSelectionUp$ carrier is a finite $\BHist$ packet
$$
A=(X,E,B,D,M,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ source row supplying finite net and compact inspection data.
\item $E$ is the $\EquicontinuityFamilyUp$ ledger, including shared-center modulus rows for the displayed family.
\item $B$ is the $\BoundedFunctionFamilyUp$ row, recording pointwise and sup-norm boundedness for the same family.
\item $D$ is the $\AscoliDiagonalUp$ row selecting compatible finite windows across the source net and target readback.
\item $M$ is the finite modulus row read through $\ArzelaAscoliFiniteModulusUp$ and the compact uniform-continuity route.
\item $Q$ is the $\RegSeqRatUp$ readback row for the selected finite windows.
\item $R$ is the $\RealUp$ seal row attached to the regular readback of the selected subsequence values.
\item $H$ records componentwise $\hsame$ transport for $X,E,B,D,M,Q,R$.
\item $C$ records displayed $\Cont$ replay from compact-source inspection through equicontinuity, boundedness, diagonal selection, regular readback, and real sealing.
\item $P$ is the $\Pkg$ provenance over $\ArzelaAscoliSelectionUp$, $\CompactMetricUp$, $\EquicontinuityFamilyUp$, $\BoundedFunctionFamilyUp$, $\AscoliDiagonalUp$, $\ArzelaAscoliFiniteModulusUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ArzelaAscoliSelectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host-selected subsequence, a compact-open completion, quotient equality of functions, countable choice, or an unstated completeness principle.
\end{definition}

\begin{theorem}[ArzelaAscoliSelection NameCert obligations]
\label{thm:arzela-ascoli-selection-namecert-obligations}
For every accepted $\ArzelaAscoliSelectionUp$ carrier $A=(X,E,B,D,M,Q,R,H,C,P,N)$, the local $\NameCert_{\ArzelaAscoliSelectionUp}$ surface consists exactly of compact-source admission through $\CompactMetricUp$, equicontinuity-family admission through $\EquicontinuityFamilyUp$, bounded-family admission through $\BoundedFunctionFamilyUp$, diagonal-window admission through $\AscoliDiagonalUp$, finite modulus readback through $\ArzelaAscoliFiniteModulusUp$, regular rational readback through $\RegSeqRatUp$, terminal real sealing through $\RealUp$, carrier storage in $\BHist$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through $\NameCert$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arzela-ascoli-selection-carrier}. The rows $X,E,B,D,M,Q,R,H,C,P,N$ are the full displayed inventory.

Read the source and family rows first. The compact metric row $X$ supplies finite source nets, the equicontinuity row $E$ supplies shared modulus ledgers, and the bounded-family row $B$ supplies the bound surface needed before any diagonal selection is public.

Read the selection rows next. The diagonal row $D$ consumes the finite net and family ledgers, while $M$ records the finite modulus compatibility that keeps the selected windows inside the Arzela--Ascoli route. The row $Q$ then exposes the selected windows as regular rational readback, and $R$ seals the resulting values as $\RealUp$ data.

Finally $H,C,P,N$ transport, replay, source, and name exactly those rows. Since the carrier has no coordinate for host subsequence choice, compact-open completion, quotient equality of functions, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[ArzelaAscoliSelection selected regular subsequence route]
\label{thm:arzela-ascoli-selection-regular-subsequence-route}
Every accepted $\ArzelaAscoliSelectionUp$ packet exposes a selected regular subsequence only through the finite route
$$
\CompactMetricUp,\ \EquicontinuityFamilyUp,\ \BoundedFunctionFamilyUp,\ \AscoliDiagonalUp,\ \ArzelaAscoliFiniteModulusUp,\ \RegSeqRatUp,\ \RealUp .
$$
The route first displays compact source nets, family equicontinuity, boundedness, and diagonal finite windows; only then does it expose regular rational readback and the real seal. It exports no arbitrary subsequence selector, compact-open host topology, quotient function equality, countable choice, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Use \autoref{thm:arzela-ascoli-selection-namecert-obligations}. It orders the compact source, equicontinuity, bounded-family, diagonal, finite-modulus, regular-readback, and Real-seal rows inside one accepted carrier.

The diagonal row can be consumed only after $X,E,B$ are displayed. The finite modulus row $M$ ties the selected windows back to the shared equicontinuity ledger and the bounded-family surface, so the selection does not detach from the family certificate.

The readback rows $Q,R$ are downstream. They convert the displayed windows into $\RegSeqRatUp$ and then $\RealUp$ data, while $H,C,P,N$ preserve the same route.

Thus the packet supplies a finite BEDC selection route. None of the refused host-level objects is present in the carrier inventory.
\end{proof}

\falsifiablePrediction{An accepted $\ArzelaAscoliSelectionUp$ consumer that reads a selected regular subsequence without first exposing CompactMetric, EquicontinuityFamily, BoundedFunctionFamily, AscoliDiagonal, finite modulus, RegSeqRat, Real, hsame, Cont, Pkg, and local NameCert rows refutes the carrier surface.}

\independenceWitness{compactmetric, equicontinuityfamily, bounded-function-family, ascoli-diagonal, arzelaascoli: $\ArzelaAscoliSelectionUp$ is not bijective to any listed sibling because it jointly carries compact-source nets, equicontinuity ledgers, bounded-family rows, diagonal selection windows, finite modulus readback, regular rational data, Real seals, transport, replay, provenance, and local naming.}

\closureat{ArzelaAscoliSelectionUp}{seedStr}

\begin{closurestatus}{\ArzelaAscoliSelectionUp}
  \constructivestory{An $\ArzelaAscoliSelectionUp$ packet is generated from CompactMetricUp source nets, EquicontinuityFamilyUp modulus ledgers, BoundedFunctionFamilyUp boundedness rows, AscoliDiagonalUp finite windows, ArzelaAscoliFiniteModulusUp compatibility rows, RegSeqRat readback, RealUp seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:arzela-ascoli-selection-carrier}, \autoref{thm:arzela-ascoli-selection-namecert-obligations}, and \autoref{thm:arzela-ascoli-selection-regular-subsequence-route} seed the finite constructive selection route for equicontinuous bounded families over compact metric sources.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, compact-open topology, arbitrary subsequence choice, quotient equality of functions, countable choice, or ambient completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, family ledger exactness, boundedness readback, diagonal-window compatibility, finite modulus transport, regular readback, Real sealing, provenance, and local naming for BEDC.Derived.ArzelaAscoliSelectionUp.}
\end{closurestatus}
