\begin{lemma}[Cofinal tail normal-form diagonal dependency]
\label{lem:cofinal-tail-normal-form-diagonal-dependency}
Every $\CofinalTailNormalFormUp$ consumer first reads the common-tail and finite-window route of \autoref{ch:concrete-instances-diagonalcofinaltail-namecert} before exporting a normal-form row. The diagonal cofinal tail supplies the shared tail window and terminal common-tail descent that this normal-form packet normalizes, so the normal-form read is a finite dependency route rather than a new selected limit, quotient stream equality, or ambient Real-completeness principle.
\end{lemma}
