\begin{lemma}[Cofinal-tail normal-form RegSeqRat and Real dependency route]
\label{lem:cofinal-tail-normal-form-regseqrat-real-dependency-route}
Every $\CofinalTailNormalFormUp$ consumer that exports a Real-facing tail read first passes through the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert} and then through the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The cofinal normal form is therefore consumed as a finite route from displayed tail windows to $\RegSeqRatUp$ and $\RealUp$ rows, not as quotient stream equality, a hidden selected tail, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{lemma}
