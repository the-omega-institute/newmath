\chapter{A Concrete Naming Certificate for $\CofinalTailNormalFormUp$}
\label{ch:concrete-instances-cofinal-tail-normal-form-namecert}
\origin{human}

Bishop--Bridges style regular-Cauchy tail arguments replace eventual agreement talk by displayed cofinal windows, explicit tolerance rows, and regular readbacks before a real-name seal is read. The $\CofinalTailNormalFormUp$ packet records that finite normal-form route for the Real-completeness vision of \autoref{ch:visions-real-completeness}. It consumes the $\StreamNameUp$ finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ regular readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and the paired cofinal-tail route of \autoref{ch:concrete-instances-cofinaltailagreement-namecert}. It names a finite tail normal form, not quotient stream equality, a selected host limit, countable choice, or ambient Real completeness.

\begin{definition}[Cofinal tail normal-form carrier]
\label{def:cofinal-tail-normal-form-carrier}
A $\CofinalTailNormalFormUp$ carrier is a finite $\BHist$ packet
$$
T=(A,S,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the accepted $\CofinalTailAgreementUp$ row supplying the paired cofinal-tail agreement source.
\item $S$ is the $\StreamNameUp$ tail-selector window row recording the finite indices inspected by the normal form.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the selected cofinal windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and comparison ledger attached to the same selected tail windows.
\item $E$ is the terminal $\RealUp$ seal row reached only after $A,S,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for agreement, window, readback, tolerance, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from each tail request through $A,S,R,D,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CofinalTailNormalFormUp$, $\CofinalTailAgreementUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CofinalTailNormalFormUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,S,R,D,E,H,C,P,N$. It has no coordinate for quotient stream equality, hidden tail selectors, selected representatives, host-limit equality, countable choice, or an ambient completeness principle.
\end{definition}

\begin{theorem}[Cofinal tail normal-form NameCert obligations]
\label{thm:cofinal-tail-normal-form-namecert-obligations}
For every accepted $\CofinalTailNormalFormUp$ carrier
$$
T=(A,S,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\CofinalTailNormalFormUp}$ surface consists exactly of carrier admission for the cofinal-tail agreement source, finite StreamName tail-selector windows, RegSeqRat readback, DyadicRatCore tolerance display, terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming. Every Real-completeness consumer of the normal form must read those rows before it can use the tail agreement.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cofinal-tail-normal-form-carrier}. The displayed route is $A,S,R,D,E,H,C,P,N$, and the classifier has no coordinate outside that route.

The source row $A$ supplies the cofinal-tail agreement. The finite selector windows $S$ determine which tail observations are inspected, while $R$ and $D$ record the regular readback and dyadic tolerance data attached to those same windows.

The terminal seal $E$ is downstream of $A,S,R,D$. The rows $H,C,P,N$ then preserve the route under transport, replay, provenance, and local naming.

Because no hidden selector, quotient stream, selected limit, countable-choice schedule, or ambient completeness row occurs in the carrier, none can be part of the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every $\CofinalTailNormalFormUp$ Real-completeness read factors through one CofinalTailAgreement source row, finite StreamName tail-selector windows, RegSeqRat readback, DyadicRatCore tolerance display, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, cofinaltailagreement: $\CofinalTailNormalFormUp$ is not bijective to any listed sibling because it carries a cofinal-tail agreement source together with the finite selector-window, regular-readback, tolerance, Real-seal, transport, replay, provenance, and naming rows as one normal-form packet.}

\closureat{CofinalTailNormalFormUp}{seedStr}

\begin{closurestatus}{\CofinalTailNormalFormUp}
  \constructivestory{A $\CofinalTailNormalFormUp$ packet is generated from a CofinalTailAgreement source row, StreamName tail-selector windows, RegSeqRat readback, DyadicRatCore tolerance display, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cofinal-tail-normal-form-carrier} and \autoref{thm:cofinal-tail-normal-form-namecert-obligations} seed the finite cofinal-tail normal form consumed by later Real-completeness tail and diagonal-limit routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, hidden tail selectors, selected representatives, host-limit equality, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cofinal-tail agreement source exactness, StreamName selector-window coverage, RegSeqRat readback, DyadicRatCore tolerance exactness, RealUp seal ordering, transport, replay, provenance, and local naming for BEDC.Derived.CofinalTailNormalFormUp.}
\end{closurestatus}
