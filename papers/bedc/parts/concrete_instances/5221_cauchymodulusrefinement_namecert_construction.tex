\chapter{A Concrete Naming Certificate for $\CauchyModulusRefinementUp$}
\label{ch:concrete-instances-cauchy-modulus-refinement-namecert}
\origin{ai-composite}

The $\CauchyModulusRefinementUp$ packet concretizes the Real-completeness diagonal-modulus route of \autoref{ch:visions-real-completeness}. It is assembled from the comparison and meet surfaces of \autoref{ch:concrete-instances-cauchymoduluscomparison-namecert} and \autoref{ch:concrete-instances-cauchymodulusmeet-namecert}, then routes extraction, selector-budget, finite-window, rational-readback, dyadic-tolerance, and $\RealUp$ seal data through one displayed NameCert carrier.

\begin{definition}[Cauchy modulus refinement carrier]
\label{def:cauchy-modulus-refinement-carrier}
A $\CauchyModulusRefinementUp$ carrier is a finite $\BHist$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $M_0$ and $M_1$ are the two source $\CauchyModulusUp$ schedules whose refinement is being compared;
\item $U$ is the shared $\CauchyModulusMeetUp$ row consumed by both source schedules;
\item $V$ is the $\CauchyModulusExtractionUp$ refinement witness row, read only after $M_0,M_1$ and $U$ have been displayed;
\item $T$ is the $\RegularCauchySelectorBudgetUp$ row that budgets the selected finite windows for the refinement handoff;
\item $W$ is the shared tail-threshold and finite $\StreamNameUp$ window ledger selected by $U,V,T$;
\item $Q$ stores the $\DyadicRatCoreUp$ tolerance rows and $\RegSeqRatUp$ readback ledger attached to the same selected windows;
\item $E$ is the $\RealUp$ seal row reached after the shared meet, refinement witness, selector budget, windows, tolerance rows, and regular readbacks have been displayed;
\item $H$ stores componentwise $\hsame$ transports for $M_0,M_1,U,V,T,W,Q,E,P,N$ and for the local packet shape;
\item $C$ stores displayed $\Cont$ routes from the source schedules to $U$, from $U$ to $V$, from $V$ to $T$, from $T$ to $W$, from $W$ to $Q$, from $Q$ to $E$, and from the packet rows to the local naming row;
\item $P$ is the $\Pkg$ provenance over $\CauchyModulusUp$, $\CauchyModulusComparisonUp$, $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CauchyModulusRefinementUp}$ row.
\end{enumerate}
The carrier contains no quotient stream, no selected limit, no host real equality, no hidden selector, no second meet row, and no ambient completeness coordinate.
\end{definition}
\leandef{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier}

\begin{theorem}[Cauchy modulus refinement NameCert obligations]
\label{thm:cauchy-modulus-refinement-namecert-obligations}
The local $\NameCert_{\CauchyModulusRefinementUp}$ surface for \autoref{def:cauchy-modulus-refinement-carrier} consists of five finite obligation rows:
\begin{enumerate}
\item carrier admission for $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$;
\item classifier stability under the displayed $\hsame$ transports and $\Cont$ routes;
\item refinement-source exactness stating that the row $V$ is read only from the two source schedules $M_0,M_1$ through the shared meet row $U$;
\item shared-window tolerance exactness forcing every selected-window read through $T,W,Q$;
\item $\RealUp$ seal non-escape stating that $E$ is reached only after the shared meet, refinement witness, selector budget, windows, dyadic tolerance rows, regular readbacks, transports, routes, provenance, and local name have been displayed.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_cofinal\_window\_factorization}
\begin{proof}
Carrier admission is projection from the finite packet in \autoref{def:cauchy-modulus-refinement-carrier}. The tuple lists the two source schedules, shared meet row, refinement witness, selector-budget row, selected windows, tolerance and readback ledger, Real seal, transports, routes, provenance, and local name.

Classifier stability uses $H$ and $C$. These rows are componentwise over the packet coordinates, so a transported refinement read remains a read of the same source schedules, meet row, witness row, budget row, window ledger, tolerance ledger, seal row, package row, and local certificate.

Refinement-source exactness follows from the order of the displayed coordinates and routes. The witness $V$ is downstream of $M_0,M_1,U$, and no carrier coordinate stores an external modulus ordering, a second meet, or a hidden selector that could replace that source.

Finally, the shared-window and Real-seal clauses use $T,W,Q,E$. Every Real-facing read reaches $E$ only through the selector-budget row, the shared finite-window ledger, and the tolerance-readback ledger, so quotient streams, selected limits, host real equality, and ambient completeness remain outside the packet.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_namecert\_obligations}

\begin{theorem}[Cauchy modulus refinement carrier admission row]
\label{thm:cauchy-modulus-refinement-carrier-admission}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
carrier admission is exactly the finite $\BHist$ admission of the displayed rows $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ together with their local $\NameCert$ row $N$. The admitted carrier cites the sibling $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces only through the coordinates displayed in \autoref{def:cauchy-modulus-refinement-carrier}.
\end{theorem}
\begin{proof}
Project the carrier definition. Its coordinate list is a finite $\BHist$ packet, and the final coordinate $N$ is the local $\NameCert_{\CauchyModulusRefinementUp}$ row naming that same packet.

The bundled obligation theorem supplies the carrier-admission clause for exactly this tuple. It admits the two source schedules, the shared meet row $U$, the extraction witness $V$, the selector budget $T$, the finite window ledger $W$, the tolerance-readback ledger $Q$, the Real seal $E$, and the transport, route, package, and name rows.

The non-escape clause removes every unlisted carrier source. Hence carrier admission for $R$ is projection from the displayed packet and not an admission of a quotient stream, selected limit, host real equality, hidden selector, second meet row, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Cauchy modulus refinement shared meet dependency row]
\label{thm:cauchy-modulus-refinement-shared-meet-dependency}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the refinement witness row $V$ depends on the shared $\CauchyModulusMeetUp$ row $U$ by the displayed $\Cont$ route
$$
M_0,M_1 \longrightarrow U \longrightarrow V.
$$
Thus any read of $V$ in the local $\NameCert$ surface also reads the two source schedules and the shared meet row before it reaches the $\CauchyModulusExtractionUp$ witness.
\end{theorem}
\begin{proof}
Read the source portion of the packet. The coordinates place $M_0$ and $M_1$ before $U$, and the continuation row $C$ records the routes from both source schedules to $U$ and from $U$ to $V$.

The bundled refinement-source exactness clause states that $V$ is read only from the two displayed source schedules through $U$. This is the same dependency row used by the sibling $\CauchyModulusMeetUp$ and $\CauchyModulusExtractionUp$ surfaces.

Apply non-escape to the source side. Since no second meet row, external modulus ordering, hidden selector, or host equality coordinate belongs to the carrier, the shared meet row $U$ is the only admitted dependency route into $V$.
\end{proof}

\begin{theorem}[Cauchy modulus refinement selector budget dependency row]
\label{thm:cauchy-modulus-refinement-selector-budget-dependency}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, each selected-window read and each tolerance-readback read depends on the selector budget row $T$ through
$$
V \longrightarrow T \longrightarrow W \longrightarrow Q.
$$
The row $T$ is therefore the displayed $\RegularCauchySelectorBudgetUp$ handoff into the $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ rows used by the refinement.
\end{theorem}
\begin{proof}
Project the window side of the carrier. The only selected-window coordinates are $T$ for the selector budget, $W$ for the finite $\StreamNameUp$ ledger, and $Q$ for the $\DyadicRatCoreUp$ tolerance rows together with the $\RegSeqRatUp$ readback ledger.

The bundled shared-window tolerance exactness clause forces each selected-window read through $T,W,Q$. The route row $C$ places these rows after the extraction witness $V$ and before the seal row $E$.

Use the readback non-escape content already stated for the packet. Because there is no hidden selector, second window ledger, quotient stream, selected limit, or ambient completeness coordinate, every selected-window and tolerance-readback read depends on the displayed selector budget row.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_selector\_budget\_dependency}

\input{parts/concrete_instances/cauchymodulusrefinement/selector_budget_meet_exhaustion.tex}

\begin{theorem}[Cauchy modulus refinement Real seal route row]
\label{thm:cauchy-modulus-refinement-real-seal-route}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the $\RealUp$ seal row $E$ is reached only along the displayed route
$$
M_0,M_1 \longrightarrow U \longrightarrow V \longrightarrow T \longrightarrow W \longrightarrow Q \longrightarrow E.
$$
Consequently a completion-facing consumer may use $E$ only after the shared meet, extraction witness, selector budget, finite window ledger, dyadic tolerance rows, and regular readbacks have been displayed.
\end{theorem}
\begin{proof}
First combine the source and selector routes. The shared meet dependency row places $U$ before $V$, and the selector budget dependency row places $T,W,Q$ after $V$.

The bundled $\RealUp$ seal non-escape clause then places $E$ after the shared meet, refinement witness, selector budget, selected windows, tolerance rows, regular readbacks, transports, routes, provenance, and local name.

Thus the Real seal is not an independent Real-facing object. It is the final row of the displayed $\Cont$ chain from the source schedules through $Q$, stabilized by the componentwise $\hsame$ row $H$ and recorded by the package row $P$ and local name $N$.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_seal\_route}

\begin{theorem}[Cauchy modulus refinement Real seal scope package]
\label{thm:cauchy-modulus-refinement-real-seal-scope-package}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, any $\RealUp$ or completion-facing consumer of the refinement seal must pass through the two source schedules $M_0,M_1$, the shared $\CauchyModulusMeetUp$ row $U$, the extraction witness $V$, the $\RegularCauchySelectorBudgetUp$ row $T$, the finite $\StreamNameUp$ window ledger $W$, the $\DyadicRatCoreUp/\RegSeqRatUp$ readback ledger $Q$, the $\RealUp$ seal $E$, and the structural rows $H,C,P,N$. The package exports no quotient stream, selected limit, host real equality, hidden selector, second meet row, or ambient completeness datum.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_seal\_scope\_package}
\begin{proof}
Project the carrier row in \autoref{def:cauchy-modulus-refinement-carrier}. The accepted packet has exactly the displayed coordinates $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, with $H,C,P,N$ recording componentwise transport, displayed route data, provenance, and the local name over the same finite carrier.

Apply Theorem~\ref{thm:cauchy-modulus-refinement-shared-meet-dependency}. The shared meet row $U$ is the only meet dependency between the two source schedules and the extraction witness $V$.

Apply Theorem~\ref{thm:cauchy-modulus-refinement-selector-budget-dependency}. The selected-window and tolerance-readback side then passes through $T,W,Q$, so the selector budget, finite window ledger, dyadic tolerance rows, and regular readbacks are all explicit rows of the same route.

Apply Theorem~\ref{thm:cauchy-modulus-refinement-real-seal-route}. The seal row $E$ is reachable only after the route through $M_0,M_1,U,V,T,W,Q$ has been displayed and stabilized by the structural rows.

Finally use Theorem~\ref{thm:cauchy-modulus-refinement-nonescape}. Since the carrier has no coordinate for a quotient stream, selected limit, host real equality, hidden selector, second meet row, or ambient completeness datum, a $\RealUp$ or completion-facing consumer of the refinement seal receives only the public scope package listed above.
\end{proof}

\begin{theorem}[Cauchy modulus refinement NameScope package row]
\label{thm:cauchy-modulus-refinement-namescope-package}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the local $\NameCert_{\CauchyModulusRefinementUp}$ namescope package consists exactly of the carrier-admission row, the componentwise $\hsame$ transport row $H$, the displayed $\Cont$ route row $C$, the provenance row $P$, and the local naming row $N$ over the same sibling surfaces. It exports no name for a row outside $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the naming coordinates of the carrier. The rows $H,C,P,N$ are displayed alongside the source, meet, extraction, selector, window, tolerance, readback, and seal rows, so the local namescope is attached to the same finite packet as carrier admission.

Use classifier stability from the bundled obligation theorem. The componentwise $\hsame$ transports and $\Cont$ routes replay accepted reads of the same rows rather than creating a separate classifier or hidden carrier.

Finally combine the shared meet, selector budget, and Real seal route rows. They show that every named downstream consumer reaches $E$ through the sibling $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces displayed in $R$. Non-escape then excludes every unlisted namescope target.
\end{proof}

\begin{theorem}[Cauchy modulus refinement non-escape]
\label{thm:cauchy-modulus-refinement-nonescape}
An accepted $\CauchyModulusRefinementUp$ packet exposes only the finite route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
If the refinement packet is accepted, every $\RealUp$-facing read must factor through the displayed shared meet and selector-budget rows. Thus a counterpacket with the same seal row $E$ but no shared meet row $U$ refutes the chapter's concrete carrier claim.
\end{theorem}
\begin{proof}
Read the carrier inventory. The only route from the two source schedules to the Real-facing seal passes through $U,V,T,W,Q,E$ and is stabilized by $H,C,P,N$.

The shared meet row $U$ is required before the refinement witness $V$ can be read, and the selector-budget row $T$ is required before the selected window ledger $W$ and tolerance-readback ledger $Q$ can be consumed. Hence a Real-facing read cannot bypass either $U$ or $T$.

If a packet presents the same seal row but omits the shared meet row, its displayed coordinate list is not the carrier of \autoref{def:cauchy-modulus-refinement-carrier}. Such a packet therefore witnesses failure of the concrete carrier claim rather than a second accepted refinement surface.
\end{proof}

\begin{theorem}[Cauchy modulus refinement source meet budget chain]
\label{thm:cauchy-modulus-refinement-source-meet-budget-chain}
Every accepted refinement read of a $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)
$$
factors through the two source schedules $M_0,M_1$, the shared meet row $U$, the refinement witness row $V$, and the selector-budget row $T$ before any finite window ledger $W$ is exposed. The route consumes the sibling $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows named in the carrier, and it exports no selected window read before the source-meet and selector-budget chain is displayed.
\end{theorem}
\begin{proof}
Project the packet $R$. The carrier lists the two source schedules first, then the shared meet row, then the refinement witness, then the selector-budget row.

Read the displayed continuation routes in $C$. They place $M_0$ and $M_1$ before $U$, place $U$ before $V$, and place $V$ before $T$.

Only after that route is displayed can $W$ be read. The provenance row $P$ names the meet, extraction, selector-budget, StreamName, DyadicRatCore, RegSeqRat, and Real rows as sibling dependencies, so a window read outside this route is not a read of the accepted refinement packet.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinement\_source\_meet\_budget\_chain}

\begin{theorem}[Cauchy modulus refinement window readback exhaustion]
\label{thm:cauchy-modulus-refinement-window-readback-exhaustion}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, every selected-window read and every $\RegSeqRatUp$ readback consumed by the refinement is exhausted by the displayed selector-budget, finite-window, and tolerance-readback rows
$$
T,\ W,\ Q.
$$
The packet has no second window ledger, hidden selector, quotient stream, selected host limit, or ambient completeness coordinate from which a selected window or regular-rational readback may be recovered.
\end{theorem}
\begin{proof}
Start from the selected-window portion of the carrier. The only coordinates devoted to selected observation are $T$ for the budget, $W$ for the finite $\StreamNameUp$ windows, and $Q$ for the $\DyadicRatCoreUp$ tolerance rows together with the $\RegSeqRatUp$ readback ledger.

The continuation route forces these rows into the order $T,W,Q$. A readback request therefore reaches $\RegSeqRatUp$ only after the selected windows have already been read from the displayed StreamName ledger.

Apply the non-escape theorem. Since the carrier omits hidden selectors, quotient streams, selected limits, and ambient completeness, the $T,W,Q$ rows exhaust all accepted selected-window and regular-readback access.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinement\_window\_readback\_exhaustion}

\begin{theorem}[Cauchy modulus refinement Real seal refusal]
\label{thm:cauchy-modulus-refinement-real-seal-refusal}
The $\RealUp$ seal row $E$ of an accepted $\CauchyModulusRefinementUp$ packet refuses any packet that bypasses either the shared meet route through $U$ or the selector-budget route through $T$. A purported seal read that presents $E$ without the source schedules, shared meet, refinement witness, selector budget, selected windows, dyadic tolerance rows, regular readbacks, transports, routes, provenance, and local name is not a carrier read of \autoref{def:cauchy-modulus-refinement-carrier}.
\end{theorem}
\begin{proof}
First read the source side. The source meet budget chain places $M_0,M_1,U,V,T$ before any selected-window or readback row can be exposed.

Next read the window and tolerance side. Window readback exhaustion forces the selected $\StreamNameUp$ windows and $\RegSeqRatUp$ readbacks through $T,W,Q$.

The only route into the $\RealUp$ seal is the displayed continuation from $Q$ to $E$, stabilized by $H$ and recorded by $P,N$. A packet that omits $U$ or $T$ therefore omits a required coordinate of the seal route and is refused as a $\CauchyModulusRefinementUp$ carrier.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_seal\_refusal}

\begin{theorem}[Cauchy modulus refinement shared-route totality]
\label{thm:cauchy-modulus-refinement-shared-route-totality}
Every $\RealUp$-facing consumer of an accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ factors through the single displayed route
$$
M_0,M_1 \longrightarrow U \longrightarrow V \longrightarrow T \longrightarrow W \longrightarrow Q \longrightarrow E.
$$
Along this route the consumer obtains the shared meet, refinement witness, selector budget, selected windows, tolerance readback, and Real seal rows, but obtains no quotient stream, selected limit, host real equality, hidden selector, second meet row, or ambient completeness datum.
\end{theorem}
\begin{proof}
Use shared-meet dependency, \autoref{thm:cauchy-modulus-refinement-shared-meet-dependency}, and selector-budget dependency, \autoref{thm:cauchy-modulus-refinement-selector-budget-dependency}, to bind the source side. They force every accepted consumer route to read $M_0,M_1$ before the shared meet $U$, and then to pass through the refinement witness $V$ and selector-budget row $T$.

Apply window readback exhaustion, \autoref{thm:cauchy-modulus-refinement-window-readback-exhaustion}. It places every selected-window and $\RegSeqRatUp$ readback through $T,W,Q$ and leaves no hidden selector, quotient stream, selected limit, or second window ledger outside those rows.

Finally apply Real seal refusal, \autoref{thm:cauchy-modulus-refinement-real-seal-refusal}. Since the seal row $E$ refuses reads that bypass $U$ or $T$, the only accepted $\RealUp$-facing route is the displayed chain ending in $E$, with host real equality and ambient completeness still outside the carrier.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_shared\_route\_totality}

\begin{theorem}[Cauchy modulus refinement cofinal-window factorization]
\label{thm:cauchy-modulus-refinement-cofinal-window-factorization}
Every accepted $\CauchyModulusRefinementUp$ Real-facing or Completion-facing consumer factors through the same cofinal finite-window chain
$$
M_0,M_1 \longrightarrow U \longrightarrow V \longrightarrow T \longrightarrow W \longrightarrow Q \longrightarrow E.
$$
The cofinal-window read is exhausted by the displayed $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows of the accepted packet. No quotient stream, selected host limit, host real equality, hidden selector, or ambient completeness datum supplies a second cofinal route.
\end{theorem}
\begin{proof}
First bind the source side by the shared-meet dependency and selector-budget dependency, \autoref{thm:cauchy-modulus-refinement-shared-meet-dependency} and \autoref{thm:cauchy-modulus-refinement-selector-budget-dependency}. They force both source moduli $M_0,M_1$ to meet at $U$, pass through the extraction row $V$, and enter the selector-budget row $T$ before any finite-window read is available.

Next apply window readback exhaustion, \autoref{thm:cauchy-modulus-refinement-window-readback-exhaustion}. It accounts for the $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ rows by routing selected windows and tolerance reads through $T,W,Q$.

Finally use the Real seal route and the non-escape boundary, \autoref{thm:cauchy-modulus-refinement-real-seal-refusal} and \autoref{thm:cauchy-modulus-refinement-nonescape}. The seal row $E$ accepts only the displayed route, while non-escape removes quotient streams, selected host limits, host real equality, hidden selectors, and ambient completeness data from the packet. Hence Real-facing and Completion-facing consumers share the same cofinal finite-window factorization.
\end{proof}

\begin{theorem}[Cauchy modulus refinement classifier transport]
\label{thm:cauchy-modulus-refinement-classifier-transport}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
classifier stability is transported componentwise by the $\hsame$ row $H$ and replayed by the displayed $\Cont$ routes in $C$:
$$
\begin{gathered}
\Cont(M_0,U),\quad \Cont(M_1,U),\quad \Cont(U,V),\quad \Cont(V,T),\\
\Cont(T,W),\quad \Cont(W,Q),\quad \Cont(Q,E),\quad
\Cont(M_0,M_1,U,V,T,W,Q,E,H,C,P,N).
\end{gathered}
$$
Thus any accepted classifier read of $R$ remains a read of the same two source schedules, shared meet row, refinement witness, selector-budget row, finite-window ledger, tolerance ledger, $\RealUp$ seal row, provenance row, and local $\NameCert$ row.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinement\_classifier\_transport}
\begin{proof}
Project the packet $R$. The only transport data in the carrier are the componentwise $\hsame$ entries stored in $H$, and the only replay data are the displayed $\Cont$ routes stored in $C$.

The first two routes place the source schedules $M_0,M_1$ before the shared meet row $U$. The next five routes carry the same read through $V,T,W,Q,E$, so a transported classifier cannot switch to a different witness, budget, window ledger, tolerance ledger, or seal row.

The final packet route authenticates the whole displayed tuple with $P$ and $N$. Since the carrier has no hidden classifier coordinate, componentwise $\hsame$ transport and the displayed $\Cont$ route list exhaust the classifier transport surface.
\end{proof}

\begin{definition}[Cauchy modulus refinement pullback spine]
\label{def:cauchy-modulus-refinement-pullback-spine}
The pullback spine of an accepted $\CauchyModulusRefinementUp$ packet is the finite $\BHist$ tuple
$$
(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)
$$
read as a single displayed route from the two source Cauchy-modulus schedules to the Real-facing seal. The row $U$ is pulled back from the $\CauchyModulusMeetUp$ surface over $M_0,M_1$; the row $V$ is pulled back from the $\CauchyModulusExtractionUp$ witness over that meet; the row $T$ is pulled back from the $\RegularCauchySelectorBudgetUp$ selector budget over $V$; the row $W$ is the finite $\StreamNameUp$ window surface opened by $T$; the row $Q$ is the joint $\DyadicRatCoreUp$ tolerance and $\RegSeqRatUp$ readback ledger over $W$; and $E$ is the $\RealUp$ seal read only after $Q$. The structural rows $H,C,P,N$ store the componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row for precisely this route.
\end{definition}

\begin{theorem}[Cauchy modulus refinement pullback spine admission]
\label{thm:cauchy-modulus-refinement-pullback-spine-admission}
An accepted $\CauchyModulusRefinementUp$ packet is admitted exactly through the pullback spine of \autoref{def:cauchy-modulus-refinement-pullback-spine}. Equivalently, carrier admission for
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)
$$
is the same finite $\BHist$ admission obtained by reading the $\CauchyModulusMeetUp$ row $U$, $\CauchyModulusExtractionUp$ row $V$, $\RegularCauchySelectorBudgetUp$ row $T$, $\StreamNameUp$ finite windows $W$, $\DyadicRatCoreUp$ and $\RegSeqRatUp$ ledger $Q$, $\RealUp$ seal $E$, and structural rows $H,C,P,N$ in the displayed order.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_pullback\_spine\_admission}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-refinement-carrier}. Its visible coordinates are exactly the rows listed in the pullback spine, and the NameCert obligations of \autoref{thm:cauchy-modulus-refinement-namecert-obligations} attach the local naming row to that same finite packet.

Read the source side through the shared meet dependency. The source-meet budget chain and source-meet exactness force the witness row $V$ to enter only after $M_0,M_1,U$, so the meet row is not an external parameter of the accepted packet.

Read the downstream side through classifier transport. The classifier-transport theorem keeps every accepted read on the componentwise $\hsame$ and displayed $\Cont$ routes, while window readback exhaustion and Real seal factorization identify the only later rows as $T,W,Q,E$.

The reverse direction is repacking. A tuple admitted through the displayed meet, extraction, selector-budget, window, tolerance-readback, seal, transport, route, provenance, and naming rows supplies every coordinate required by the carrier and no coordinate outside it. Thus accepted packets and pullback-spine admissions coincide.
\end{proof}

\begin{theorem}[Cauchy modulus refinement shared-budget pullback]
\label{thm:cauchy-modulus-refinement-shared-budget-pullback}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the meet row $U$ and selector-budget row $T$ jointly determine every selected-window read in $W$ and every tolerance-readback row in $Q$. No selected window may be read from $T$ without the same shared meet row $U$, and no $\DyadicRatCoreUp$ or $\RegSeqRatUp$ readback may be read from $Q$ without the window budget determined by $U$ through $V,T$.
\end{theorem}
\begin{proof}
First fix the shared source. Source-meet exactness identifies $U$ as the only meet row over the two schedules $M_0,M_1$, and the source-meet budget chain places the refinement witness $V$ after that meet.

Next fix the selector budget. Selector budget totality makes $T$ the only route from $V$ to the selected finite-window surface, and window readback exhaustion makes $W,Q$ the only rows from which selected StreamName, dyadic tolerance, and regular-rational readback data can be consumed.

Finally use the classifier-transport and Real-seal route theorems. They keep the same $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows attached to the route, so a selected-window consumer cannot detach $T$ from $U$ or replace $Q$ by a tolerance ledger outside the displayed pullback spine.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real seal pullback non-escape]
\label{thm:cauchy-modulus-refinement-real-seal-pullback-nonescape}
A $\RealUp$-facing consumer of an accepted $\CauchyModulusRefinementUp$ packet cannot bypass any of the rows
$$
U,\ V,\ T,\ W,\ Q.
$$
Every such consumer reaches the seal row $E$ only after the shared meet, extraction witness, selector budget, finite StreamName windows, and dyadic and rational readback ledger have been read through the pullback spine, and it receives no quotient stream, selected host limit, hidden selector, second meet row, host real equality, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Begin at the seal. Real seal refusal rejects any attempted seal read that omits the shared meet route or the selector-budget route, and Real seal factorization places $E$ after $U,V,T,W,Q$.

Now bind the consumer path. Classifier transport supplies the componentwise $\hsame$ and displayed $\Cont$ route, while the shared-budget pullback theorem fixes the selected-window and readback rows as the ones determined by $U,V,T$.

It remains to exclude side routes. The Real seal consumer exhaustion, completion boundary, and non-escape theorems remove quotient streams, selected limits, hidden selectors, second meet rows, host real equality, and ambient completeness from the carrier surface. Hence a Real-facing consumer can reach $E$ only through the displayed pullback spine.
\end{proof}

\begin{theorem}[Cauchy modulus refinement source-meet exactness]
\label{thm:cauchy-modulus-refinement-source-meet-exactness}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the refinement witness row $V$ is forced through the displayed source-meet spine
$$
M_0,M_1 \longrightarrow U \longrightarrow V.
$$
No accepted read of $V$ may replace $U$ by a second meet row, bypass either source schedule, or import an external modulus comparison not displayed by $R$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_meet\_exactness}
\begin{proof}
Start from the carrier definition. The packet has exactly two source schedules, one shared meet row, and one refinement witness row.

Use classifier transport to follow the displayed routes. The $\Cont(M_0,U)$ and $\Cont(M_1,U)$ entries bind both source schedules to $U$, and $\Cont(U,V)$ is the only route that reaches the refinement witness.

The non-escape theorem removes the remaining alternatives. A second meet row, hidden selector, external ordering, or host completeness witness is not a coordinate of $R$, so it cannot supply an accepted source for $V$.
\end{proof}

\begin{theorem}[Cauchy modulus refinement window tolerance exactness]
\label{thm:cauchy-modulus-refinement-window-tolerance-exactness}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, all selected-window and dyadic-tolerance reads are forced through the route
$$
V \longrightarrow T \longrightarrow W \longrightarrow Q.
$$
The selector-budget row $T$, finite $\StreamNameUp$ window ledger $W$, and $\DyadicRatCoreUp$ and $\RegSeqRatUp$ tolerance ledger $Q$ are the only packet coordinates that may feed the selected-window tolerance surface.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_window\_tolerance\_exactness}
\begin{proof}
Project the selected-window portion of $R$. The carrier stores one selector-budget row $T$, one finite-window ledger $W$, and one tolerance-readback ledger $Q$.

Follow the displayed continuation rows. The route $\Cont(V,T)$ makes the selector budget downstream of the refinement witness, $\Cont(T,W)$ selects the finite windows, and $\Cont(W,Q)$ attaches the dyadic and regular-rational readbacks to those same windows.

Apply non-escape to exclude hidden routes. Since $R$ contains no second window ledger, hidden selector, selected host limit, or ambient completeness coordinate, every tolerance read accepted by the packet is the $T,W,Q$ read.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real seal factorization]
\label{thm:cauchy-modulus-refinement-real-seal-factorization}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the $\RealUp$ seal row $E$ factors through the displayed refinement and tolerance route
$$
U \longrightarrow V \longrightarrow T \longrightarrow W \longrightarrow Q \longrightarrow E.
$$
Consequently $E$ cannot be accepted from a quotient stream, host real equality, selected limit, hidden selector, second meet row, or ambient completeness witness outside the packet.
\end{theorem}
\begin{proof}
First bind the source of the seal route. Source-meet exactness places the refinement witness $V$ downstream of the shared meet row $U$ and the two source schedules.

Next bind the tolerance surface. Window tolerance exactness forces the selected-window and dyadic-tolerance data through $T,W,Q$.

The route $\Cont(Q,E)$ is the only displayed continuation into the $\RealUp$ seal row. Together with the componentwise transports in $H$ and the provenance and naming rows $P,N$, this makes $E$ a factor of the displayed $U,V,T,W,Q$ spine rather than an independent Real-facing object.
\end{proof}

\begin{theorem}[Cauchy modulus refinement common source lock]
\label{thm:cauchy-modulus-refinement-common-source-lock}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
any accepted refinement read keeps the source schedules $M_0$ and $M_1$ locked to the same shared meet row $U$ before the refinement witness row $V$ is read. A consumer cannot read $V$ from one source schedule alone, from a second meet row, or from an external modulus comparison not displayed by $R$.
\end{theorem}
\begin{proof}
Start with source-meet exactness, \autoref{thm:cauchy-modulus-refinement-source-meet-exactness}. It identifies the route to the refinement witness as
$$
M_0,M_1 \longrightarrow U \longrightarrow V.
$$

Use classifier transport to keep this route componentwise stable through $H$ and $C$. The transports preserve both source schedules and the same shared meet row.

Finally apply non-escape, \autoref{thm:cauchy-modulus-refinement-nonescape}. Since $R$ carries no second meet row, hidden selector, external comparison, or ambient completeness coordinate, the common source lock is exactly the displayed $M_0,M_1,U$ surface.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_common\_source\_lock}

\begin{theorem}[Cauchy modulus refinement shared threshold transport]
\label{thm:cauchy-modulus-refinement-shared-threshold-transport}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
the shared comparison and meet threshold is transported only through the displayed rows
$$
M_0,\ M_1,\ U,\ V,\ T,\ H,\ C,\ P,\ N.
$$
Thus no accepted threshold read may replace the two source schedules, the shared meet row, the refinement witness, the selector-budget row, the componentwise transports, the displayed continuation routes, the package provenance, or the local naming row by an external comparison, second meet, quotient stream, hidden selector, or host completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_shared\_threshold\_transport}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-refinement-carrier}. The packet displays exactly the two source schedules $M_0,M_1$, one shared meet row $U$, one refinement witness $V$, one selector-budget row $T$, one transport row $H$, one continuation row $C$, one provenance row $P$, and one local certificate row $N$.

Apply the source-meet-budget chain. It forces every accepted threshold read to move from $M_0,M_1$ through $U,V,T$ before any finite selected window can be exposed.

Apply classifier transport. The componentwise $\hsame$ entries in $H$ and the displayed $\Cont$ entries in $C$ replay the same threshold through the named rows, while $P$ and $N$ authenticate the packet as the local $\CauchyModulusRefinementUp$ surface.

The carrier contains no external comparison, second meet, quotient stream, hidden selector, or host completeness coordinate. Hence the shared threshold can be transported only along the displayed rows.
\end{proof}

\begin{theorem}[Cauchy modulus refinement selector budget totality]
\label{thm:cauchy-modulus-refinement-selector-budget-totality}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the selector-budget row $T$ is the only route from the refinement witness row $V$ to the selected finite-window ledger $W$ and the tolerance-readback ledger $Q$. Thus every selected-window read and every $\DyadicRatCoreUp$ or $\RegSeqRatUp$ readback consumed by the refinement factors through
$$
V \longrightarrow T \longrightarrow W \longrightarrow Q.
$$
\end{theorem}
\begin{proof}
Read window-tolerance exactness, \autoref{thm:cauchy-modulus-refinement-window-tolerance-exactness}. It already forces every selected-window and dyadic-tolerance read through $V,T,W,Q$.

The Real seal factorization theorem then shows why this budget route is total for downstream consumers: the seal row $E$ is reachable only after $U,V,T,W,Q$ have been displayed.

Apply non-escape to remove alternate selectors and window ledgers. The carrier has one selector-budget row, one selected-window ledger, and one tolerance-readback ledger, so $T$ exhausts the route from $V$ to $W,Q$.
\end{proof}

\begin{theorem}[Cauchy modulus refinement selector budget non-escape]
\label{thm:cauchy-modulus-refinement-selector-budget-nonescape}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the selector-budget row $T$ is the only route from the source-meet threshold into the $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ window-readback surface. In particular, no hidden selector, quotient stream, selected host limit, or countable-choice coordinate can supply an accepted window, rational-readback, or dyadic-tolerance read for $R$.
\end{theorem}
\begin{proof}
Start with window readback exhaustion. It identifies $T,W,Q$ as the complete selected-window, regular-rational readback, and dyadic-tolerance surface of the packet.

Use source-meet exactness to bind the source threshold. The refinement witness $V$ is reached only from the two source schedules through the shared meet row $U$, and the displayed continuation route then moves from $V$ to $T$.

Apply non-escape to the remaining alternatives. Since the carrier has no hidden selector, quotient stream, selected host limit, or countable-choice coordinate, the route through $T$ is the sole accepted handoff from the source-meet threshold into $W$ and $Q$.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real seal obligation surface]
\label{thm:cauchy-modulus-refinement-real-seal-obligation-surface}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
the rows $U,V,T,W,Q,E,H,C,P,N$ form the complete $\RealUp$ seal obligation surface for the refinement route. The surface contains the shared meet row, refinement witness, selector-budget row, finite windows, tolerance and readback ledger, Real seal, componentwise transports, displayed continuation routes, provenance, and local naming row, and it excludes quotient streams, host real equality, hidden selectors, selected limits, second meet rows, and ambient completeness.
\end{theorem}
\begin{proof}
First bind the source of the surface. Common source lock fixes $M_0$ and $M_1$ to the shared meet row $U$ before the witness $V$ is read.

Next bind the selected-window route. Selector-budget totality forces the only route from $V$ to the selected windows and tolerance-readback ledger through $T,W,Q$.

Now apply Real seal factorization. It places $E$ downstream of $U,V,T,W,Q$ and keeps the seal route inside the displayed transport, continuation, provenance, and naming rows.

Finally use non-escape. Quotient streams, host real equality, hidden selectors, selected limits, second meet rows, and ambient completeness are absent from the carrier, so the listed rows are the whole Real-seal obligation surface.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real seal consumer exhaustion]
\label{thm:cauchy-modulus-refinement-real-seal-consumer-exhaustion}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, every $\RealUp$ or completion-facing consumer reaches the seal row $E$ only after consuming the shared threshold, selector-budget, finite-window, rational-readback, dyadic-tolerance, transport, continuation, package, and local $\NameCert$ rows displayed by
$$
M_0,M_1,U,V,T,W,Q,H,C,P,N.
$$
No accepted consumer may read $E$ directly from a quotient stream, selected limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or unbudgeted completion object.
\end{theorem}
\begin{proof}
First apply Real seal factorization. The seal row $E$ factors through the displayed route $U,V,T,W,Q,E$, so a Real-facing consumer reaches the seal only after the shared meet, refinement witness, selector budget, finite windows, and tolerance-readback ledger have been consumed.

Next apply Real seal refusal. A purported seal read that omits the source schedules, meet, witness, selector budget, windows, tolerances, readbacks, transports, routes, provenance, and local name is not an accepted carrier read.

Use window tolerance exactness to identify the finite-window, rational-readback, and dyadic-tolerance coordinates as $T,W,Q$. The same route attaches the selected $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, and $\DyadicRatCoreUp$ tolerance rows to the seal.

Finally apply non-escape. Quotient streams, selected limits, host real equality, hidden selectors, second meet rows, ambient completeness coordinates, and unbudgeted completion objects are absent from the carrier, so every accepted Real or completion-facing consumer must pass through the displayed rows before reading $E$.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_seal\_consumer\_exhaustion}

\begin{theorem}[Cauchy modulus refinement obligation closure package]
\label{thm:cauchy-modulus-refinement-obligation-closure-package}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the carrier of \autoref{def:cauchy-modulus-refinement-carrier}, the NameCert obligation rows of \autoref{thm:cauchy-modulus-refinement-namecert-obligations}, the source-meet-budget chain of \autoref{thm:cauchy-modulus-refinement-source-meet-budget-chain}, window readback exhaustion of \autoref{thm:cauchy-modulus-refinement-window-readback-exhaustion}, Real seal refusal of \autoref{thm:cauchy-modulus-refinement-real-seal-refusal}, classifier transport of \autoref{thm:cauchy-modulus-refinement-classifier-transport}, source-meet exactness of \autoref{thm:cauchy-modulus-refinement-source-meet-exactness}, window tolerance exactness of \autoref{thm:cauchy-modulus-refinement-window-tolerance-exactness}, Real seal factorization of \autoref{thm:cauchy-modulus-refinement-real-seal-factorization}, common source lock of \autoref{thm:cauchy-modulus-refinement-common-source-lock}, shared threshold transport of \autoref{thm:cauchy-modulus-refinement-shared-threshold-transport}, selector budget totality of \autoref{thm:cauchy-modulus-refinement-selector-budget-totality}, selector budget non-escape of \autoref{thm:cauchy-modulus-refinement-selector-budget-nonescape}, Real seal obligation surface of \autoref{thm:cauchy-modulus-refinement-real-seal-obligation-surface}, Real seal consumer exhaustion of \autoref{thm:cauchy-modulus-refinement-real-seal-consumer-exhaustion}, and non-escape of \autoref{thm:cauchy-modulus-refinement-nonescape} jointly form the $\obligationClosure$ surface for $\CauchyModulusRefinementUp$.

The package exports exactly the two $\CauchyModulusUp$ source schedules, shared $\CauchyModulusMeetUp$ row, $\CauchyModulusExtractionUp$ witness row, $\RegularCauchySelectorBudgetUp$ row, finite $\StreamNameUp$ window ledger, $\DyadicRatCoreUp$ and $\RegSeqRatUp$ tolerance ledger, $\RealUp$ seal row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row.
\end{theorem}
\begin{proof}
Project the displayed packet $R$. The carrier and NameCert obligation theorem supply the finite row inventory and the five required local certificate clauses.

Read the classifier and source rows. Classifier transport keeps every accepted read on the componentwise $\hsame$ and displayed $\Cont$ routes, while source-meet exactness fixes $V$ as the witness reached from $M_0,M_1$ through $U$.

Read the selected-window and Real-facing rows. Window tolerance exactness binds the tolerance surface to $T,W,Q$, and Real seal factorization places $E$ after the shared meet, refinement witness, selector budget, windows, and tolerance ledger.

Use the intermediate obligations to expose the selector-budget route. Common source lock fixes $M_0,M_1,U$, shared threshold transport keeps the comparison and meet threshold on $M_0,M_1,U,V,T,H,C,P,N$, selector budget totality and selector budget non-escape make $T$ the sole handoff into $W,Q$, and the two Real-seal obligations force every Real or completion-facing consumer through the displayed rows before $E$ is read.

Finally combine these rows with non-escape. All excluded objects are absent from the carrier, so the assembled package is precisely the written obligation surface and no external Real-completeness or host-equality route remains.
\end{proof}

\begin{theorem}[Cauchy modulus refinement primitive scope]
\label{thm:cauchy-modulus-refinement-primitive-scope}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
the exact primitive scope of the packet is the displayed $\BHist$ carrier row $R$, the componentwise $\hsame$ transports stored in $H$, the displayed $\Cont$ route row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\CauchyModulusRefinementUp}$ row $N$, together with the named sibling rows
$$
\CauchyModulusUp,\ \CauchyModulusComparisonUp,\ \CauchyModulusMeetUp,\ \CauchyModulusExtractionUp,\ \RegularCauchySelectorBudgetUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp.
$$
No primitive outside these rows is part of the accepted $\CauchyModulusRefinementUp$ surface.
\end{theorem}
\begin{proof}
Project the carrier and NameCert obligations. The displayed packet supplies the $\BHist$ row, the componentwise $\hsame$ row, the $\Cont$ row, the $\Pkg$ row, and the local naming row, while \autoref{thm:cauchy-modulus-refinement-namecert-obligations} lists the finite certificate clauses attached to those rows.

Bind the source and selector side. Source-meet exactness fixes the refinement witness to the route $M_0,M_1,U,V$, and selector-budget totality fixes every selected-window handoff to $V,T,W,Q$.

Bind the Real-facing side. Window-tolerance exactness identifies the $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ rows as the selected tolerance surface, and Real seal factorization places the $\RealUp$ seal after the same route.

Finally apply Real seal consumer exhaustion and non-escape. Since the carrier has no quotient stream, selected limit, host real equality, hidden selector, second meet row, or ambient completeness coordinate, the displayed rows and named siblings are the whole primitive scope.
\end{proof}

\begin{theorem}[Cauchy modulus refinement sibling dependency lattice]
\label{thm:cauchy-modulus-refinement-sibling-dependency-lattice}
Every accepted $\CauchyModulusRefinementUp$ packet consumes its sibling surfaces in the dependency order
$$
\CauchyModulusComparisonUp,\ \CauchyModulusMeetUp,\ \CauchyModulusExtractionUp,\ \RegularCauchySelectorBudgetUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp.
$$
The comparison rows bind the two source schedules, the meet row supplies the shared source threshold, the extraction row supplies the refinement witness, the selector-budget row selects the finite windows, the $\StreamNameUp$ row records those windows, the $\DyadicRatCoreUp$ and $\RegSeqRatUp$ rows attach tolerance and rational readback data, and the $\RealUp$ row seals the downstream Real surface.
\end{theorem}
\begin{proof}
Start with the source-meet chain. Source-meet exactness and common source lock place the comparison and meet rows before the extraction witness $V$ can be read.

Next expose the selected-window route. Selector-budget totality makes $T$ the sole handoff from $V$ into $W,Q$, and window-tolerance exactness identifies $W$ as the $\StreamNameUp$ window ledger and $Q$ as the $\DyadicRatCoreUp$ and $\RegSeqRatUp$ tolerance-readback ledger.

Now read the Real seal. Real seal factorization places $E$ after $U,V,T,W,Q$, and Real seal consumer exhaustion forces every Real or completion-facing consumer through the same sibling order before the seal can be consumed.

Non-escape removes all alternate lattices. The packet carries no second meet row, hidden selector, quotient stream, selected limit, host equality, or ambient completeness coordinate, so the dependency order written above is the accepted sibling lattice.
\end{proof}

\begin{theorem}[Cauchy modulus refinement completion boundary]
\label{thm:cauchy-modulus-refinement-completion-boundary}
No $\RealUp$ or completion-facing consumer of an accepted $\CauchyModulusRefinementUp$ packet may read quotient streams, selected limits, host real equality, hidden selectors, second meet rows, or ambient completeness outside the displayed packet. Such a consumer must factor through
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N
$$
and through the sibling dependency lattice of \autoref{thm:cauchy-modulus-refinement-sibling-dependency-lattice}.
\end{theorem}
\begin{proof}
First bind the route to the Real seal. Real seal factorization places $E$ downstream of the shared meet, refinement witness, selector budget, selected windows, and tolerance-readback ledger.

Use Real seal consumer exhaustion to control downstream reads. A Real or completion-facing consumer reaches $E$ only after consuming the displayed source, meet, witness, budget, window, tolerance, transport, route, provenance, and naming rows.

Use the primitive-scope theorem and sibling dependency lattice to identify the whole surface available to that consumer. They restrict the primitive rows and sibling rows to the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, comparison, meet, extraction, selector-budget, StreamName, DyadicRatCore, RegSeqRat, and Real surfaces.

Finally apply non-escape. The forbidden quotient, selected-limit, host-equality, hidden-selector, second-meet, and ambient-completeness objects are absent from the packet, so no completion-facing read may obtain them outside the displayed route.
\end{proof}

\begin{theorem}[Cauchy modulus refinement BHist source scope]
\label{thm:cauchy-modulus-refinement-bhist-source-scope}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
the rows $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ are finite $\BHist$-facing rows over $\CauchyModulusUp$, $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. No quotient stream, host-completeness object, ambient completeness coordinate, selected limit, hidden selector, or host real equality coordinate belongs to the accepted source scope.
\end{theorem}
\begin{proof}
Project the displayed packet through the carrier. The carrier row gives the finite $\BHist$ spine $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, and the NameCert obligations attach the local naming clause to exactly that spine.

Bind the primitive side. Primitive scope identifies the componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row, while the sibling dependency lattice identifies the named $\CauchyModulusUp$, meet, extraction, selector-budget, StreamName, dyadic, rational, and Real sibling rows.

Use the completion boundary and non-escape to remove external sources. Since quotient streams, selected limits, host real equality, hidden selectors, second meet rows, host-completeness objects, and ambient completeness coordinates are absent from the carrier, no accepted source row can be read outside the displayed $\BHist$-facing surface.
\end{proof}

\begin{theorem}[Cauchy modulus refinement continuation route scope]
\label{thm:cauchy-modulus-refinement-cont-route-scope}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the displayed continuation order
$$
M_0,M_1 \longrightarrow U \longrightarrow V \longrightarrow T \longrightarrow W \longrightarrow Q \longrightarrow E
$$
is the only consumer route for selector-window and $\RealUp$-seal reads. A consumer of selected windows, rational readbacks, dyadic tolerances, or the Real seal must therefore pass through the source schedules, shared meet, extraction witness, selector-budget row, finite-window ledger, tolerance-readback ledger, and seal row in this order.
\end{theorem}
\begin{proof}
Start with the source route. Source-meet exactness fixes the witness row $V$ as downstream of $M_0,M_1$ through $U$, so a consumer cannot reach the refinement witness without first reading the two source schedules and the shared meet.

Next read the selected-window surface. Window readback exhaustion and selector budget totality place the selector handoff at $T$ and the finite-window and tolerance-readback rows at $W,Q$.

Finally read the seal. Real seal factorization places $E$ after $U,V,T,W,Q$, and Real seal consumer exhaustion prevents a downstream consumer from reaching the seal by a route that omits those rows. Non-escape removes quotient-stream, selected-limit, host-equality, hidden-selector, second-meet, and ambient-completeness alternatives, so the displayed order is the only accepted consumer route.
\end{proof}

\begin{theorem}[Cauchy modulus refinement NameCert scope]
\label{thm:cauchy-modulus-refinement-namecert-scope}
For every accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the local $\NameCert_{\CauchyModulusRefinementUp}$ scope is exhausted by carrier admission, classifier transport, source-meet exactness, selector-budget and window-tolerance exactness, and $\RealUp$-seal non-escape. Equivalently, the certificate can name only the admitted finite $\BHist$ rows, their componentwise $\hsame$ transports, the displayed $\Cont$ routes, the $\Pkg$ provenance row, and the Real-facing seal route certified by the obligation closure package.
\end{theorem}
\begin{proof}
First admit the named object. The carrier and NameCert obligation theorem bind the local certificate to the finite packet rows and require the naming row to travel with the displayed $\BHist$, $\hsame$, $\Cont$, and $\Pkg$ fields.

Next transport the classifier through the source and window rows. Classifier transport keeps accepted reads on the displayed transports and routes, source-meet exactness fixes the $M_0,M_1,U,V$ source-meet witness, and window tolerance exactness fixes the $T,W,Q$ selector, window, dyadic-tolerance, and rational-readback surface.

Finally seal the Real-facing edge. The BHist source scope supplies the permitted primitive rows, the continuation route scope supplies the only consumer order into $E$, and the obligation closure package assembles those rows with Real-seal factorization and non-escape. Thus the local NameCert scope has no remaining quotient stream, hidden selector, selected limit, host equality, ambient completeness, or external completion coordinate to name.
\end{proof}

The closest siblings are $\CauchyModulusComparisonUp$, $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, and $\RegularCauchyLimitTransportUp$. The packet is composite because it consumes their rows as coordinates rather than presenting an atomic independent carrier. A later Lean TasteGate module should still provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances over the packet fields, since the finite carrier separates the comparison, meet, witness, budget, window, ledger, seal, transport, route, provenance, and local certificate coordinates.

\begin{theorem}[Cauchy modulus refinement budget-selector source lock]
\label{thm:cauchy-modulus-refinement-budget-selector-source-lock}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
every accepted refinement route reads the source schedules, shared meet row, extraction witness, and selector budget
$$
M_0,M_1,U,V,T
$$
before it reads the selected window ledger $W$, tolerance-readback ledger $Q$, or Real seal row $E$. Thus the budget selector is source-locked to the displayed $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, and $\RegularCauchySelectorBudgetUp$ rows before any downstream $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, or $\RealUp$ consumer can be served.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-refinement-carrier}. The displayed tuple places $M_0,M_1,U,V,T$ before the window, tolerance, readback, and seal rows, and no second source, second meet, hidden selector, or host-completeness coordinate appears in the packet.

Apply source-meet exactness from \autoref{thm:cauchy-modulus-refinement-source-meet-exactness}. It forces the witness row $V$ to be read only through $M_0,M_1,U$, so a selected-window route cannot choose its source after the fact.

Next apply selector-budget totality from \autoref{thm:cauchy-modulus-refinement-selector-budget-totality}. The selector budget row $T$ is the sole route from $V$ into the selected finite-window and tolerance surface.

Finally use window-tolerance exactness from \autoref{thm:cauchy-modulus-refinement-window-tolerance-exactness}. It identifies $T,W,Q$ as the exact selected-window and dyadic-tolerance route, so every later read of $W,Q,E$ is already locked to $M_0,M_1,U,V,T$.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_budget\_selector\_source\_lock}

\begin{theorem}[Cauchy modulus refinement diagonal-consumer boundary]
\label{thm:cauchy-modulus-refinement-diagonal-consumer-boundary}
Any $\RealUp$ consumer or diagonal-completion consumer of an accepted $\CauchyModulusRefinementUp$ packet factors through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows of
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
No such consumer may read a quotient stream, selected limit, host real equality, hidden selector, second meet row, side channel completion object, or ambient completeness coordinate outside that displayed route.
\end{theorem}
\begin{proof}
Start with the completion boundary of \autoref{thm:cauchy-modulus-refinement-completion-boundary}. It already requires every Real or completion-facing consumer to factor through the displayed packet and the sibling dependency lattice before reading the seal.

Use the BHist source scope and continuation route scope, \autoref{thm:cauchy-modulus-refinement-bhist-source-scope} and \autoref{thm:cauchy-modulus-refinement-cont-route-scope}. These theorems identify the primitive rows as the finite $\BHist$ packet, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, $\NameCert$, and the ordered consumer route from the source schedules to $E$.

Then apply the budget-selector source lock. It places $M_0,M_1,U,V,T$ before the selected-window and tolerance rows, so a diagonal consumer reaches $W,Q,E$ only through the same source-locked route.

Finally apply non-escape from \autoref{thm:cauchy-modulus-refinement-nonescape}. Since all forbidden quotient, selected-limit, host-equality, hidden-selector, second-meet, and ambient-completeness rows are absent from the carrier, the displayed rows exhaust the diagonal-consumer boundary.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root-unblock package]
\label{thm:cauchy-modulus-refinement-root-unblock-package}
The root-unblock obligation package for downstream Real-completion consumers of $\CauchyModulusRefinementUp$ is exactly the conjunction of the local $\NameCert$ scope, the budget-selector source lock, and the diagonal-consumer boundary. An accepted downstream route may cite $\CauchyModulusRefinementUp$ only through the rows
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N
$$
and the sibling $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces displayed by those rows.
\end{theorem}
\begin{proof}
First use \autoref{thm:cauchy-modulus-refinement-namecert-scope}. It binds the local naming certificate to carrier admission, classifier transport, source-meet exactness, selector-budget and window-tolerance exactness, and Real-seal non-escape.

Next use \autoref{thm:cauchy-modulus-refinement-budget-selector-source-lock}. This supplies the source-lock portion of the package by placing $M_0,M_1,U,V,T$ before $W,Q,E$.

Finally use \autoref{thm:cauchy-modulus-refinement-diagonal-consumer-boundary}. It supplies the downstream Real and diagonal-completion boundary, excluding quotient streams, selected limits, host real equality, hidden selectors, side channel completion objects, second meet rows, and ambient completeness.

The three clauses use the same displayed carrier and the same sibling rows. Therefore they assemble one root-unblock package rather than three independent routes, and every downstream consumer must cite that package through the displayed finite packet.
\end{proof}

\begin{theorem}[Cauchy modulus refinement diagonal budget cofinality]
\label{thm:cauchy-modulus-refinement-diagonal-budget-cofinality}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
every diagonal Real-completion consumer that has passed through the shared-meet and selector-budget rows factors through a cofinal finite budget route before reaching the $\RealUp$ seal. The route uses the $\CauchyModulusUp$, $\CauchyModulusMeetUp$, $\CauchySharedTailModulusUp$, $\RegularCauchySelectorBudgetUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows displayed by the packet, and it exports no choice-selected tail, quotient stream, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with source-meet exactness from \autoref{thm:cauchy-modulus-refinement-source-meet-exactness}. It binds the two source modulus rows and the shared meet row to the refinement witness before any selector or Real-facing consumer can be read.

Apply selector budget totality from \autoref{thm:cauchy-modulus-refinement-selector-budget-totality}. The selector row $T$ supplies the finite budget route into the selected-window and tolerance-readback rows, so a diagonal consumer receives a cofinal finite budget before it can ask for the Real seal.

Next use Real seal factorization and the diagonal-consumer boundary, \autoref{thm:cauchy-modulus-refinement-real-seal-factorization} and \autoref{thm:cauchy-modulus-refinement-diagonal-consumer-boundary}. These results force every Real-completion consumer through $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ and through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\CauchyModulusUp$, $\CauchyModulusMeetUp$, $\CauchySharedTailModulusUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces.

Finally use the root-unblock package of \autoref{thm:cauchy-modulus-refinement-root-unblock-package}. The package assembles the local NameCert scope, budget-selector source lock, and diagonal boundary into one route, so no choice-selected tail, quotient stream, or ambient completeness theorem is exported by the accepted packet.
\end{proof}
\leanvariant{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_diagonal\_budget\_cofinality\_route}

\begin{theorem}[Cauchy modulus refinement common-refinement selector lock]
\label{thm:cauchy-modulus-refinement-common-refinement-selector-lock}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
any Real-completion consumer preserving the common meet row $U$, extraction row $V$, selector-budget row $T$, finite-window row $W$, dyadic ledger $Q$, and Real seal row $E$ must read the same finite common-refinement route before it reaches $\RealUp$. The packet makes no alternate selected tail, quotient stream, second meet row, or ambient completeness coordinate available to that consumer.
\end{theorem}
\begin{proof}
Begin with source-meet exactness from \autoref{thm:cauchy-modulus-refinement-source-meet-exactness}. It identifies the common meet row $U$ as the unique displayed meet through which the two source moduli $M_0,M_1$ can reach the extraction row $V$.

Next apply selector-budget totality from \autoref{thm:cauchy-modulus-refinement-selector-budget-totality}. The row $T$ routes the extraction row into the finite-window and tolerance surface $W,Q$, so a Real-completion consumer preserving $U,V,T,W,Q$ preserves the same finite selector budget.

Use Real-seal factorization from \autoref{thm:cauchy-modulus-refinement-real-seal-factorization} and the diagonal-budget cofinality route of \autoref{thm:cauchy-modulus-refinement-diagonal-budget-cofinality}. These place the Real seal row $E$ after the same cofinal finite route through $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$.

Finally repack the route through the structural rows of $R$. The $\BHist$ carrier, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row name only the preserved rows $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$. Since no other row is exported, there is no alternate selected tail, quotient stream, second meet row, or ambient completeness coordinate to read.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_common\_refinement\_selector\_lock}

\begin{theorem}[Cauchy modulus refinement RealObservationBudget handoff]
\label{thm:cauchy-modulus-refinement-real-observation-budget-handoff}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
any $\RealObservationBudgetUp$ request that uses the same selected finite $\StreamNameUp$ windows, dyadic tolerance rows, $\RegSeqRatUp$ readback ledger, and $\RealUp$ seal row factors through
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N
$$
before any Real-facing observation is consumed. The handoff exports no quotient stream, selected limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or unbudgeted observation selector.
\end{theorem}
\begin{proof}
First apply the budget-selector source lock of \autoref{thm:cauchy-modulus-refinement-budget-selector-source-lock}. It places the source schedules, shared meet row, extraction witness, and selector budget in the prefix $M_0,M_1,U,V,T$ before the selected finite-window, tolerance-readback, or Real-seal rows can be read.

Next use window readback exhaustion and diagonal budget cofinality, \autoref{thm:cauchy-modulus-refinement-window-readback-exhaustion} and \autoref{thm:cauchy-modulus-refinement-diagonal-budget-cofinality}. These bind the selected $\StreamNameUp$ windows, dyadic tolerance rows, and $\RegSeqRatUp$ readback ledger to $T,W,Q,E$, and they route every diagonal Real-completion consumer through the displayed cofinal finite budget before the $\RealUp$ seal is observed.

Finally apply the completion boundary and non-escape, \autoref{thm:cauchy-modulus-refinement-completion-boundary} and \autoref{thm:cauchy-modulus-refinement-nonescape}. The completion boundary keeps the observation request on the displayed packet and sibling route, while non-escape removes quotient streams, selected limits, host real equality, hidden selectors, second meet rows, ambient completeness coordinates, and unbudgeted observation selectors from the exported surface.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_observation\_budget\_handoff}

\begin{theorem}[Cauchy modulus refinement RealLimitUniqueness handoff]
\label{thm:cauchy-modulus-refinement-real-limit-uniqueness-handoff}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
any $\RealLimitUniquenessSealUp$-facing consumer preserving the same selected finite $\StreamNameUp$ windows, dyadic tolerance ledger, $\RegSeqRatUp$ readback, and $\RealUp$ seal row must first factor through
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N
$$
and then through the two-seal packet rows of \autoref{ch:concrete-instances-real-limit-uniqueness-seal-namecert}. The handoff exports no selected host limit, quotient stream equality, hidden selector, second meet, or ambient completeness row.
\end{theorem}
\begin{proof}
Start with the RealObservationBudget handoff of \autoref{thm:cauchy-modulus-refinement-real-observation-budget-handoff}. It binds the selected $\StreamNameUp$ windows, dyadic tolerance rows, $\RegSeqRatUp$ readback ledger, and $\RealUp$ seal row to the displayed refinement packet before any Real-facing observation is consumed.

Apply the common-refinement selector lock of \autoref{thm:cauchy-modulus-refinement-common-refinement-selector-lock}. The consumer preserving those rows must therefore keep the same source moduli, common meet, extraction row, selector budget, finite-window route, tolerance ledger, and Real seal row in the order $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$.

Finally use the RealLimitUniquenessSeal public route theorem \autoref{thm:real-limit-uniqueness-bridged-seal-surface}. Its two-seal surface exposes only the two $\CauchyLimitSealUp$ rows, shared comparison row, finite window, dyadic ledger, $\RealUp$ classifier row, uniqueness handoff, transports, replay, provenance, and local name. Since neither packet exports a selected host limit, quotient stream equality, hidden selector, second meet, or ambient completeness row, the consumer factors through the displayed refinement route and then through the two-seal packet.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_limit\_uniqueness\_handoff}

\input{parts/concrete_instances/cauchymodulusrefinement/root_consumer_surfaces}
\input{parts/concrete_instances/cauchymodulusrefinement/root_unblock_obligation_surface}
\input{parts/concrete_instances/cauchymodulusrefinement/root_source_window_locks}

\closureat{CauchyModulusRefinementUp}{seedStr}
\closureat{CauchyModulusRefinementUp}{obligationStr}
\closureat{CauchyModulusRefinementUp}{publicStr}
\closureat{CauchyModulusRefinementUp}{bridgedStr}
\closureat{CauchyModulusRefinementUp}{matureStr}

\input{parts/concrete_instances/5221_cauchymodulusrefinement_root_threshold_surfaces}
\input{parts/concrete_instances/cauchymodulusrefinement/bridged_route_surface}

\begin{closurestatus}{\CauchyModulusRefinementUp}
  \constructivestory{A $\CauchyModulusRefinementUp$ packet is generated from displayed modulus comparison, meet, extraction, selector-budget, finite-window, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:cauchy-modulus-refinement-carrier}, \autoref{thm:cauchy-modulus-refinement-namecert-obligations}, \autoref{thm:cauchy-modulus-refinement-source-meet-budget-chain}, \autoref{thm:cauchy-modulus-refinement-window-readback-exhaustion}, \autoref{thm:cauchy-modulus-refinement-real-seal-refusal}, \autoref{thm:cauchy-modulus-refinement-classifier-transport}, \autoref{thm:cauchy-modulus-refinement-source-meet-exactness}, \autoref{thm:cauchy-modulus-refinement-window-tolerance-exactness}, \autoref{thm:cauchy-modulus-refinement-real-seal-factorization}, \autoref{thm:cauchy-modulus-refinement-common-source-lock}, \autoref{thm:cauchy-modulus-refinement-shared-threshold-transport}, \autoref{thm:cauchy-modulus-refinement-selector-budget-totality}, \autoref{thm:cauchy-modulus-refinement-selector-budget-nonescape}, \autoref{thm:cauchy-modulus-refinement-real-seal-obligation-surface}, \autoref{thm:cauchy-modulus-refinement-real-seal-consumer-exhaustion}, \autoref{thm:cauchy-modulus-refinement-obligation-closure-package}, \autoref{thm:cauchy-modulus-refinement-primitive-scope}, \autoref{thm:cauchy-modulus-refinement-sibling-dependency-lattice}, \autoref{thm:cauchy-modulus-refinement-completion-boundary}, \autoref{thm:cauchy-modulus-refinement-bhist-source-scope}, \autoref{thm:cauchy-modulus-refinement-cont-route-scope}, \autoref{thm:cauchy-modulus-refinement-namecert-scope}, \autoref{thm:cauchy-modulus-refinement-nonescape}, and the bridge witness \autoref{thm:cauchy-modulus-refinement-bridged-route-surface} bind the displayed modulus comparison, meet, extraction, selector-budget, finite-window, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows into one scoped Real and completion obligation surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, hidden selectors, second meet rows, ambient completeness, Lean verification, or bridge checking for $\CauchyModulusRefinementUp$.}
  \upgradepath{No further paper-side theory closure is required; remaining work is Lean verification and any future bridge checking for the displayed $\CauchyModulusRefinementUp$ route.}
\end{closurestatus}
