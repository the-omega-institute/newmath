\begin{theorem}[ParallelDiamondResidual residual-join consumer route]
\label{thm:parallel-diamond-residual-join-matrix-forward-route}
Every accepted $\ParallelDiamondResidualUp$ local-join row used by the MetaCIC residual audit is consumed by \autoref{ch:concrete-instances-metacic-residual-join-matrix-namecert} and \autoref{ch:concrete-instances-metacic-residual-join-grid-namecert} before any confluence handoff is read. The route exposes residual branch rows, candidate joins, retained premise data, obstruction rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming, and it does not export a premise-free parallel diamond or closed Church--Rosser theorem.
\end{theorem}
\begin{proof}
Project the residual local-join carrier through the sibling routes already named by the chapter. The branch rows and candidate joins are retained as finite $\BHist$ coordinates, while the transport, replay, provenance, and naming rows remain local to the $\ParallelDiamondResidualUp$ packet.

The matrix consumer of \autoref{ch:concrete-instances-metacic-residual-join-matrix-namecert} reads two residual branches together with retained residual-substitution premise data. The grid consumer of \autoref{ch:concrete-instances-metacic-residual-join-grid-namecert} reads the same residual handoff as a finite residual-vertex and candidate-join grid.

Thus the forward route is a consumer dependency for the residual audit surface. Since neither consumer receives a coordinate discharging the retained premises or the obstruction row, the route cannot be retyped as a premise-free confluence theorem.
\end{proof}
