\chapter{A Concrete Naming Certificate for $\ParallelDiamondResidualUp$}
\label{ch:concrete-instances-parallel-diamond-residual-namecert}
\origin{ai}

The $\ParallelDiamondResidualUp$ packet realizes the MetaCIC confluence audit boundary of \autoref{ch:metacic-confluence-audit-map} as finite residual-join data. It sits after the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the residual-substitution compatibility packet of \autoref{ch:concrete-instances-metacic-residual-substitution-compatibility-namecert}, and the local-join ledger of \autoref{ch:concrete-instances-metacic-local-join-ledger-namecert}. The chapter records the concrete residual frontier that a later conditional parallel-diamond bridge may consume; it does not claim full Church--Rosser, closed arbitrary-term confluence, or unconditional substitution compatibility.

\begin{definition}[ParallelDiamondResidual carrier]
\label{def:parallel-diamond-residual-carrier}
A $\ParallelDiamondResidualUp$ carrier is a finite $\BHist$ packet
$$
J=(P,F,S,L,B,C,O,H,T,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the parallel peak row. It records a source MetaCIC term and the two parallel reducts whose residuals must be compared.
\item $F$ is the residual frontier row. It records the finite redex-frontier cells, binder depths, and residual tags visible at the peak.
\item $S$ is the $\MetacicResidualSubstitutionBoundaryUp$ row naming the substitution-sensitive boundary consumed by the residual comparison.
\item $L$ is the $\MetacicLocalJoinLedgerUp$ row recording candidate common reducts for the displayed frontier cells.
\item $B$ is the bounded checker row. It records the finite checks that the candidate joins respect the residual tags and binder depths displayed in $F$.
\item $C$ is the $\MetacicResidualSubstitutionCompatibilityUp$ handoff row. It is a named compatibility interface, not a proof that the interface is globally available.
\item $O$ is the obstruction row. It records any remaining residual-substitution cell that is not discharged by $S,L,B,C$.
\item $H$ records componentwise $\hsame$ transport for peaks, residual frontiers, substitution boundaries, local joins, bounded checks, compatibility handoffs, obstruction rows, replay, provenance, and local naming.
\item $T$ records displayed $\Cont$ replay for the same residual-join packet.
\item $G$ is the $\Pkg$ provenance over $\ParallelDiamondResidualUp$, $\MetacicResidualSubstitutionBoundaryUp$, $\MetacicResidualSubstitutionCompatibilityUp$, $\MetacicLocalJoinLedgerUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ParallelDiamondResidualUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $P,F,S,L,B,C,O,H,T,G,N$. It has no coordinate for full Church--Rosser, arbitrary closed confluence, unconditional substitution compatibility, quotienting of terms, or a global parallel-diamond theorem.
\end{definition}

\begin{theorem}[ParallelDiamondResidual NameCert obligations]
\label{thm:parallel-diamond-residual-namecert-obligations}
For every accepted $\ParallelDiamondResidualUp$ carrier
$$
J=(P,F,S,L,B,C,O,H,T,G,N),
$$
the local $\NameCert_{\ParallelDiamondResidualUp}$ surface consists exactly of parallel peak exposure, residual frontier exposure, residual-substitution boundary exposure, local-join ledger exposure, bounded residual checking, compatibility-interface handoff, obstruction-row exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A consumer may read the residual local-join packet only after the frontier $F$, boundary row $S$, ledger row $L$, and bounded checker $B$ are displayed.
\end{theorem}
\begin{proof}
Project the carrier $J=(P,F,S,L,B,C,O,H,T,G,N)$. The analytic rows are the peak, residual frontier, substitution boundary, local-join ledger, bounded checker, compatibility handoff, and obstruction row.

The compatibility row is downstream of the displayed frontier and checker. It names the interface consumed by the conditional parallel-diamond route, while the obstruction row keeps undiscovered residual-substitution cells visible.

The structural rows $H,T,G,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for full Church--Rosser or an unconditional global diamond, the NameCert surface stops at residual local joins and their boundary rows.
\end{proof}

\begin{theorem}[ParallelDiamondResidual conditional handoff]
\label{thm:parallel-diamond-residual-conditional-handoff}
Every $\ParallelDiamondResidualUp$ residual-join read offered to the MetaCIC confluence route factors through
$$
\MetacicResidualSubstitutionBoundaryUp \longmapsto \MetacicLocalJoinLedgerUp \longmapsto \MetacicResidualSubstitutionCompatibilityUp .
$$
The handoff supplies finite residual-frontier, local-join, bounded-checker, and obstruction data for the conditional parallel-diamond bridge; it does not assert the bridge, full Church--Rosser, or arbitrary closed-term confluence.
\end{theorem}
\begin{proof}
Start with the residual frontier $F$ and the substitution boundary row $S$. These rows display the finite cells whose residual algebra must be inspected.

The local-join ledger $L$ and bounded checker $B$ then record candidate common reducts and the finite checks against the displayed frontier. The compatibility handoff $C$ can only be read after these rows have been exposed.

Finally the obstruction row $O$ and structural rows $H,T,G,N$ preserve the same conditional boundary. They do not add a global confluence theorem, so the export remains a residual local-join packet for a later bridge.
\end{proof}

\begin{theorem}[ParallelDiamondResidual MetaCIC sibling route]
\label{thm:parallel-diamond-residual-metacic-sibling-route}
Every $\ParallelDiamondResidualUp$ read is a downstream residual-frontier consumer of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. Such a read must preserve the sibling frontier's residual-substitution premise, critical-pair row, candidate-join row, bounded-checker row, compatibility-handoff row, and obstruction row before it may export a residual local-join packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:parallel-diamond-residual-carrier}. Its rows $P,F,S,L,B,C,O,H,T,G,N$ display the residual frontier, residual-substitution boundary, local-join ledger, bounded checker, compatibility handoff, obstruction row, and structural certificate data.

Apply the NameCert obligations theorem \autoref{thm:parallel-diamond-residual-namecert-obligations}. It requires any accepted read to expose $F,S,L,B,C,O$ before the residual local-join packet can be consumed, so the sibling frontier's premise, critical-pair, candidate-join, checker, handoff, and obstruction rows cannot be dropped.

Finally use the conditional handoff theorem \autoref{thm:parallel-diamond-residual-conditional-handoff}. The handoff reaches the MetaCIC confluence route only as a residual-frontier consumer, and the rows $H,T,G,N$ preserve the same displayed boundary for \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}.
\end{proof}

\begin{theorem}[ParallelDiamondResidual obstruction retention]
\label{thm:parallel-diamond-residual-obstruction-retention}
While the obstruction row $O$ remains displayed in an accepted $\ParallelDiamondResidualUp$ packet
$$
J=(P,F,S,L,B,C,O,H,T,G,N),
$$
no consumer may read the packet as a premise-free parallel diamond, a full Church--Rosser theorem, an arbitrary closed-term confluence theorem, an unconditional substitution-compatibility theorem, a quotienting of terms, or a global parallel-diamond theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:parallel-diamond-residual-carrier}. The rows $P,F,S,L,B,C,O,H,T,G,N$ are displayed coordinates of the same accepted packet, and $O$ is one of the confluence-facing rows rather than a surrounding note.

Apply the NameCert obligations theorem \autoref{thm:parallel-diamond-residual-namecert-obligations}. It requires the residual frontier $F$, substitution boundary $S$, local-join ledger $L$, bounded checker $B$, compatibility handoff $C$, and obstruction row $O$ to remain visible before the residual local-join packet can be consumed.

Finally use the conditional handoff theorem \autoref{thm:parallel-diamond-residual-conditional-handoff}. Since that handoff supplies only finite residual-frontier, local-join, bounded-checker, compatibility, and obstruction data, any consumer that removes $O$ and reads the packet as one of the refused endpoints has left the displayed $\NameCert_{\ParallelDiamondResidualUp}$ surface.
\end{proof}

\begin{theorem}[ParallelDiamondResidual frontier obstruction route]
\label{thm:parallel-diamond-residual-frontier-obstruction-route}
An accepted $\ParallelDiamondResidualUp$ packet
$$
J=(P,F,S,L,B,C,O,H,T,G,N)
$$
can be consumed by $\MetaCICParallelDiamondFrontierUp$ from \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} only while the obstruction row $O$, bounded checker $B$, residual frontier $F$, and substitution boundary $S$ remain displayed. The frontier consumer receives a residual-frontier dependency packet over $F,S,B,O,H,T,G,N$, not a premise-free diamond, full Church--Rosser theorem, closed arbitrary-term confluence theorem, or discharged substitution boundary.
\end{theorem}
\begin{proof}
Project the residual packet $J=(P,F,S,L,B,C,O,H,T,G,N)$. The rows $F,S,B,O$ expose the residual frontier, substitution-sensitive boundary, bounded checker, and obstruction data that a frontier consumer must inspect.

Apply obstruction retention, \autoref{thm:parallel-diamond-residual-obstruction-retention}. It forbids any consumer from reading the packet as a premise-free parallel diamond while $O$ is still a displayed coordinate.

Now read the MetaCIC frontier chapter named in the statement. Its residual-join surface consumes finite frontier, checker, substitution-boundary, and obstruction rows before any candidate diamond read is public, so the same $F,S,B,O$ rows remain part of the dependency rather than surrounding prose.

Finally transport and replay the packet through $H,T,G,N$. These structural rows preserve the displayed dependency for $\MetaCICParallelDiamondFrontierUp$ and add no global confluence, quotienting, or substitution-discharge coordinate.
\end{proof}

\begin{theorem}[ParallelDiamondResidual seed obligation]
\label{thm:parallel-diamond-residual-seed-obligation}
Every seed-level $\ParallelDiamondResidualUp$ consumer must preserve the rows
$$
P,F,S,L,B,C,O,H,T,G,N
$$
before any local join is exported. In particular, the residual frontier $F$, substitution boundary $S$, local join ledger $L$, bounded checker $B$, compatibility handoff $C$, and obstruction row $O$ remain visible as finite obligations of the seed packet.

The seed obligation refuses premise-free parallel diamond, full Church--Rosser, arbitrary closed-term confluence, unconditional substitution compatibility, quotienting of terms, and a global parallel-diamond theorem.
\end{theorem}
\begin{proof}
Start with \autoref{thm:parallel-diamond-residual-namecert-obligations}. It identifies $P,F,S,L,B,C,O,H,T,G,N$ as the displayed local NameCert surface and requires $F,S,L,B,C,O$ before a residual local-join packet can be consumed.

Use \autoref{thm:parallel-diamond-residual-conditional-handoff}. The handoff reaches the MetaCIC confluence route only through the residual-substitution boundary, local-join ledger, bounded checker, compatibility interface, and obstruction data.

Apply \autoref{thm:parallel-diamond-residual-metacic-sibling-route} and \autoref{thm:parallel-diamond-residual-frontier-obstruction-route}. The sibling and frontier consumers keep the residual frontier, substitution boundary, bounded checker, compatibility handoff, and obstruction rows visible before any local join is exported.

Finally invoke \autoref{thm:parallel-diamond-residual-obstruction-retention}. Since $O$ remains a displayed coordinate, the packet cannot be read as a premise-free parallel diamond, full Church--Rosser theorem, arbitrary closed-term confluence theorem, unconditional substitution-compatibility theorem, quotienting of terms, or global parallel-diamond theorem.
\end{proof}

\falsifiablePrediction{Every $\ParallelDiamondResidualUp$ handoff reaches $\MetacicResidualSubstitutionCompatibilityUp$ only through displayed parallel peaks, residual frontiers, $\MetacicResidualSubstitutionBoundaryUp$, $\MetacicLocalJoinLedgerUp$, bounded residual checks, obstruction rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{metacic-residual-substitution-boundary, metacic-residual-substitution-compatibility, metacic-local-join-ledger: $\ParallelDiamondResidualUp$ is not bijective to any listed sibling because it jointly carries parallel peaks, residual frontiers, boundary rows, local-join ledgers, bounded checks, compatibility handoffs, obstruction rows, transport, replay, provenance, and local naming.}

\closureat{ParallelDiamondResidualUp}{seedStr}

\begin{closurestatus}{\ParallelDiamondResidualUp}
  \constructivestory{A $\ParallelDiamondResidualUp$ packet is generated from a parallel peak row, residual frontier, $\MetacicResidualSubstitutionBoundaryUp$ row, $\MetacicLocalJoinLedgerUp$ row, bounded checker, $\MetacicResidualSubstitutionCompatibilityUp$ handoff, obstruction row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:parallel-diamond-residual-carrier}, \autoref{thm:parallel-diamond-residual-namecert-obligations}, and \autoref{thm:parallel-diamond-residual-conditional-handoff} seed the finite residual local-join route for the conditional MetaCIC parallel-diamond boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full Church--Rosser, arbitrary closed-term confluence, unconditional substitution compatibility, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, residual-frontier exactness, substitution-boundary routing, local-join ledger exposure, bounded-checker exactness, compatibility handoff nonescape, obstruction-row exposure, transport, replay, provenance, and local naming for BEDC.Derived.ParallelDiamondResidualUp.}
\end{closurestatus}
