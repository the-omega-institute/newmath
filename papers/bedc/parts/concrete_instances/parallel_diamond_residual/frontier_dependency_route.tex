\begin{theorem}[ParallelDiamondResidual frontier dependency]
\label{thm:parallel-diamond-residual-frontier-dependency}
Every accepted $\ParallelDiamondResidualUp$ residual-join read consumes \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} before exporting any residual local-join packet. The read preserves the residual-substitution premise, critical-pair row, candidate-join row, bounded-checker row, compatibility-handoff row, and obstruction row through the same residual frontier dependency route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:parallel-diamond-residual-carrier}. The rows $P,F,S,L,B,C,O,H,T,G,N$ display the peak, frontier, substitution boundary, local join, bounded checker, compatibility handoff, obstruction, and structural certificate data.

Apply the MetaCIC sibling route of \autoref{thm:parallel-diamond-residual-metacic-sibling-route}. It makes the residual packet a downstream consumer of the frontier chapter before the local-join packet can be exported.

Use the conditional handoff theorem \autoref{thm:parallel-diamond-residual-conditional-handoff}. The handoff preserves the residual-substitution premise, local-join ledger, bounded checker, compatibility interface, and obstruction row, so the frontier dependency is a displayed packet route rather than an ambient confluence claim.
\end{proof}
