The $\ParallelDiamondResidualUp$ packet is a residual-frontier consumer of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. It preserves the residual-substitution boundary, local-join ledger, compatibility handoff, bounded checker, and obstruction row before any confluence-facing consumer reads the residual-diamond packet.
