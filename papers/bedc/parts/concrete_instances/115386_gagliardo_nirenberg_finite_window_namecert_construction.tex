\chapter{A Concrete Naming Certificate for $\GagliardoNirenbergFiniteWindowUp$}
\label{ch:concrete-instances-gagliardo-nirenberg-finite-window-namecert}
\origin{human}

Following Gagliardo and Nirenberg on interpolation inequalities, the $\GagliardoNirenbergFiniteWindowUp$ packet records the finite BEDC surface by which a Sobolev derivative window, a norm ledger, and a metric or complete-metric source window hand an interpolation estimate to compactness-facing consumers. It sits after the $\SobolevUp$ carrier of \autoref{ch:concrete-instances-sobolev-namecert}, the norm surface of \autoref{ch:concrete-instances-norm-namecert}, the compact-metric route of \autoref{ch:concrete-instances-compactmetric-namecert}, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet names only finite rows: derivative windows, exponent comparisons, norm estimates, compact handoff data, transport, replay, provenance, and local $\NameCert$ evidence.

\begin{definition}[Gagliardo-Nirenberg finite-window carrier]
\label{def:gagliardo-nirenberg-finite-window-carrier}
A $\GagliardoNirenbergFiniteWindowUp$ carrier is a finite $\BHist$ packet
$$
G=(S,N,M,D,E,R,K,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\SobolevUp$ source row, carrying the displayed function, weak-derivative budget, integrability row, and local domain window.
\item $N$ is the $\NormUp$ target row, recording the lower-order and higher-order norm reads used by the interpolation estimate.
\item $M$ is the $\MetricUp$ or $\CompleteMetricUp$ base row whose finite balls and Cauchy-boundary data supply the inspection scale.
\item $D$ is the finite derivative-window row, listing exactly the derivative orders, test windows, and source tolerances inspected by the estimate.
\item $E$ is the interpolation exponent ledger, recording the dimension, derivative orders, norm exponents, interpolation weight, and admissibility comparisons as finite rational or regular-rational data.
\item $R$ is the $\RegSeqRatUp$ readback row for tolerance, exponent, and norm-bound observations.
\item $K$ is the compact handoff row, exposing only the $\CompactMetricUp$ or compact-window data needed by L10/L11 compactness consumers.
\item $H$ records componentwise $\hsame$ transport for the Sobolev, norm, metric, derivative-window, exponent-ledger, readback, compact-handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from requested precision to derivative windows, norm ledgers, regular-rational readback, and compact handoff rows.
\item $P$ is the $\Pkg$ provenance over $\GagliardoNirenbergFiniteWindowUp$, $\SobolevUp$, $\NormUp$, $\MetricUp$, $\CompleteMetricUp$, $\CompactMetricUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\GagliardoNirenbergFiniteWindowUp}$ evidence.
\end{enumerate}
The classifier compares accepted packets only through $S,N,M,D,E,R,K,H,C,P,L$. It has no coordinate for a host Sobolev embedding theorem, nonconstructive compact embedding, quotient function equality, selected representative, countable-choice schedule, or ambient completed function space.
\end{definition}

\begin{theorem}[Gagliardo-Nirenberg finite-window NameCert obligations]
\label{thm:gagliardo-nirenberg-finite-window-namecert-obligations}
For every accepted $\GagliardoNirenbergFiniteWindowUp$ carrier
$$
G=(S,N,M,D,E,R,K,H,C,P,L),
$$
the local $\NameCert_{\GagliardoNirenbergFiniteWindowUp}$ surface consists exactly of carrier admission for the $\SobolevUp$ source row, norm-row exposure through $\NormUp$, metric or complete-metric inspection through $M$, finite derivative-window exposure through $D$, interpolation exponent-ledger exactness through $E$, $\RegSeqRatUp$ readback through $R$, compact handoff exposure through $K$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:gagliardo-nirenberg-finite-window-carrier}. Its analytic rows are $S,N,M,D,E,R,K$, and its structural rows are $H,C,P,L$.

The interpolation estimate is finite at every read. A consumer first sees the $\SobolevUp$ derivative source $S$, the $\NormUp$ target ledger $N$, the metric or complete-metric inspection row $M$, and the derivative-window row $D$ before it may inspect the exponent ledger $E$.

The readback and handoff rows keep the estimate attached to finite data. The $\RegSeqRatUp$ row $R$ records tolerance and bound observations, while $K$ exposes only the compact-window information needed by downstream compactness routes.

Finally $H,C,P,L$ transport, replay, source, and name the same packet. Since no row carries host embedding, quotient-function equality, selected representatives, countable choice, or completed function-space data, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Gagliardo-Nirenberg finite-window interpolation handoff]
\label{thm:gagliardo-nirenberg-finite-window-interpolation-handoff}
Every L10/L11 compactness or completion consumer of an accepted $\GagliardoNirenbergFiniteWindowUp$ packet receives the interpolation estimate only through the finite route
$$
\SobolevUp\longmapsto\NormUp\longmapsto(\MetricUp,\CompleteMetricUp)\longmapsto\RegSeqRatUp\longmapsto\CompactMetricUp .
$$
The handoff may pass a displayed derivative-window estimate, exponent ledger, regular-rational tolerance readback, and compact-window row to later finite-window compactness chapters, but it exports no nonconstructive compact embedding theorem, global Sobolev embedding strength, quotient function equality, selected subsequence, countable choice, or ambient completed Sobolev carrier.
\end{theorem}
\begin{proof}
Start from the obligations of \autoref{thm:gagliardo-nirenberg-finite-window-namecert-obligations}. They force the Sobolev source, norm target, metric inspection row, derivative windows, exponent ledger, readback row, and compact handoff row to appear in the same finite packet.

Read the sibling route in order. The $\SobolevUp$ row supplies weak-derivative data, $\NormUp$ supplies lower-order and higher-order norm observations, $\MetricUp$ or $\CompleteMetricUp$ supplies inspection scale, and $\RegSeqRatUp$ supplies rational tolerance readback before the compact handoff row $K$ is exposed.

The structural rows $H,C,P,L$ keep that route stable under componentwise transport and replay. The compactness consumer receives a finite interpolation handoff only, not an ambient compact embedding theorem or any selected representative absent from the carrier.
\end{proof}

\closureat{GagliardoNirenbergFiniteWindowUp}{seedStr}

\begin{closurestatus}{\GagliardoNirenbergFiniteWindowUp}
  \constructivestory{A $\GagliardoNirenbergFiniteWindowUp$ packet is generated from a Sobolev source row, norm target row, metric or complete-metric inspection row, finite derivative-window row, interpolation exponent ledger, RegSeqRat readback, compact handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:gagliardo-nirenberg-finite-window-carrier}, \autoref{thm:gagliardo-nirenberg-finite-window-namecert-obligations}, and \autoref{thm:gagliardo-nirenberg-finite-window-interpolation-handoff} seed the finite interpolation route for L10/L11 compactness and completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close nonconstructive compact embedding strength, global Sobolev embedding theorems, quotient function equality, selected subsequences, countable choice, ambient completed Sobolev carriers, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite derivative-window exposure, exponent-ledger exactness, RegSeqRat readback, compact handoff exactness, transport, replay, provenance, and local naming for BEDC.Derived.GagliardoNirenbergFiniteWindowUp.}
\end{closurestatus}
