\chapter{A Concrete Naming Certificate for $\CauchyCompletionSeparatedUniversalUp$}
\label{ch:concrete-instances-cauchy-completion-separated-universal-namecert}
\origin{human}

Following Bishop and Bridges and the categorical reflection reading of completion, the $\CauchyCompletionSeparatedUniversalUp$ packet records the separated universal-property boundary for Cauchy completion as finite BEDC NameCert data. It sits after the functorial completion surface of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}, the adjunction row of \autoref{ch:concrete-instances-cauchy-completion-adjunction-namecert}, the dense embedding of \autoref{ch:concrete-instances-metric-completion-dense-embedding-namecert}, the separated-reflection boundary of \autoref{ch:concrete-instances-separatedmetricreflection-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names exactly the rows through which a separated target may consume a completion universal arrow; it is not a quotient completion, a selected limit, a host equality principle, or an assertion of ambient $\RealUp$ completeness.

\begin{definition}[Cauchy completion separated universal carrier]
\label{def:cauchy-completion-separated-universal-carrier}
A $\CauchyCompletionSeparatedUniversalUp$ carrier is a finite $\BHist$ packet
$$
U=(F,A,D,S,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\CauchyCompletionFunctorUp$ row exposing the source completion surface before any universal-property consumer reads the packet.
\item $A$ is the $\CauchyCompletionAdjunctionUp$ row carrying the unit and extension handoff over the same source data.
\item $D$ is the $\MetricCompletionDenseEmbeddingUp$ dense-source row, visible before a separated target is allowed to inspect the completion value.
\item $S$ is the $\SeparatedMetricReflectionUp$ row witnessing that the target-side comparison is separated at the NameCert boundary.
\item $W$ is the finite $\StreamNameUp$ window surface through which source and image observations are requested.
\item $R$ is the $\RegSeqRatUp$ readback row attached to those windows.
\item $E$ is the terminal $\RealUp$ seal row reached only after $W$ and $R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the functor, adjunction, dense embedding, separated reflection, window, readback, seal, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a universal-property request through $F,A,D,S,W,R,E$.
\item $P$ is the $\Pkg$ provenance over $\CauchyCompletionSeparatedUniversalUp$, $\CauchyCompletionFunctorUp$, $\CauchyCompletionAdjunctionUp$, $\MetricCompletionDenseEmbeddingUp$, $\SeparatedMetricReflectionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCompletionSeparatedUniversalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient equality of completion points, selected representatives, choice-selected limits, host equality of maps, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy completion separated universal obligations]
\label{thm:cauchy-completion-separated-universal-obligations}
For every accepted $\CauchyCompletionSeparatedUniversalUp$ carrier
$$
U=(F,A,D,S,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyCompletionSeparatedUniversalUp}$ surface consists exactly of carrier admission, functor-row exposure, adjunction handoff, dense-embedding exposure, separated-reflection exposure, finite window readback, terminal Real sealing, componentwise transport, continuation replay, provenance, and local naming. No obligation may replace these rows by quotient equality, selected limits, host map equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-separated-universal-carrier}. Its public rows are exactly $F,A,D,S,W,R,E,H,C,P,N$.

The universal-property part is read through $F,A,D,S$. The functor and adjunction rows expose the completion-facing source and extension route, the dense embedding row supplies the source image inside the completion surface, and the separated-reflection row is the only target-side separatedness datum.

The numerical readback is read through $W,R,E$. Stream windows and regular rational readback are displayed before the terminal Real seal is available.

Finally $H,C,P,N$ transport, replay, source, and name the same rows. Since the carrier has no quotient, selected-limit, host-map-equality, choice, or ambient-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Cauchy completion separated universal factorization]
\label{thm:cauchy-completion-separated-universal-factorization}
Every separated-target consumer of an accepted $\CauchyCompletionSeparatedUniversalUp$ packet factors through
$$
\CauchyCompletionFunctorUp,\ \CauchyCompletionAdjunctionUp,\ \MetricCompletionDenseEmbeddingUp,\ \SeparatedMetricReflectionUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
before any public universal-property read is exposed. The factorization supplies a displayed completion functor row, adjunction handoff, dense-source row, separated-reflection row, finite windows, regular readback, Real seal, transport, replay, provenance, and local naming; it exports no quotient completion, selected representative, choice-selected limit, host map equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the functor and adjunction rows $F,A$. They identify the completion-facing source and extension route by finite NameCert data.

Next read the dense embedding and separated-reflection rows $D,S$. The dense image must be visible before the target consumes the completion value, and separatedness is recorded only as the row $S$.

Then replay the observation route through $W,R,E$. The consumer receives finite stream windows, regular rational readback, and the terminal Real seal in that order.

The structural rows $H,C,P,N$ preserve and name the same route. Thus a separated-target consumer can only factor through the displayed rows and cannot obtain quotient equality, selected limits, host map equality, choice, or ambient completeness from the packet.
\end{proof}

\begin{theorem}[Cauchy completion separated universal nonescape]
\label{thm:cauchy-completion-separated-universal-nonescape}
Every public read from an accepted $\CauchyCompletionSeparatedUniversalUp$ packet exposes separatedness only as the $\SeparatedMetricReflectionUp$ row $S$ attached to the displayed completion route. The read cannot escape to quotient equality of completion points, chosen representatives, choice-selected limits, host equality of maps, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:cauchy-completion-separated-universal-obligations}. It fixes the packet inventory to the finite rows $F,A,D,S,W,R,E,H,C,P,N$.

Apply \autoref{thm:cauchy-completion-separated-universal-factorization}. Any separated-target consumer reaches the universal-property boundary only after the functor, adjunction, dense embedding, separated-reflection, finite-window, readback, and Real-seal rows are displayed.

The separatedness datum is therefore the visible row $S$, not an equality principle outside the carrier. Because no excluded object is a coordinate of the packet, no public read can export it.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyCompletionSeparatedUniversalUp$ read must expose $F,A,D,S,W,R,E,H,C,P,N$ before a separated universal-property consumer may inspect the completion boundary.}

\independenceWitness{cauchycompletionfunctor, cauchycompletionadjunction, metric-completion-dense-embedding, separatedmetricreflection, streamname, regseqrat, real: no listed sibling jointly carries the separated universal-property packet with functor, adjunction, dense embedding, separated reflection, finite observation, regular readback, Real seal, transport, replay, provenance, and local NameCert rows.}

\closureat{CauchyCompletionSeparatedUniversalUp}{seedStr}

\begin{closurestatus}{\CauchyCompletionSeparatedUniversalUp}
  \constructivestory{A $\CauchyCompletionSeparatedUniversalUp$ packet is generated from CauchyCompletionFunctor, CauchyCompletionAdjunction, MetricCompletionDenseEmbedding, SeparatedMetricReflection, StreamName, RegSeqRat, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-completion-separated-universal-carrier}, \autoref{thm:cauchy-completion-separated-universal-obligations}, and \autoref{thm:cauchy-completion-separated-universal-factorization} seed the separated universal-property boundary for Cauchy completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient $\RealUp$ completeness, quotient equality, choice-selected limits, host map equality, selected representatives, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, functor-row exposure, adjunction handoff, dense embedding, separated-reflection visibility, finite readback, Real sealing, and ledger nonescape for BEDC.Derived.CauchyCompletionSeparatedUniversalUp.}
\end{closurestatus}
