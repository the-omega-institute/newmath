\chapter{A Concrete Naming Certificate for $\FiniteIntersectionPropertyUp$}
\label{ch:concrete-instances-finite-intersection-property-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and Kelley-style compactness practice, the $\FiniteIntersectionPropertyUp$ packet records only the finite closed-family certificate used by the L10 Real-and-Completion route. It reads the closed-interval completion surface of \autoref{ch:concrete-instances-closed-interval-cauchy-completion-namecert}, the nested-window compactness handoff of \autoref{ch:concrete-instances-nestedintervalcompactness-namecert}, the Cantor nested-intersection consumer of \autoref{ch:concrete-instances-cantor-intersection-namecert}, the finite epsilon-net witness of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names finite closed-family intersection data; it does not import open-cover compactness, arbitrary closed-family compactness, countable choice, selected endpoints, quotient real equality, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Finite intersection property carrier]
\label{def:finite-intersection-property-carrier}
A $\FiniteIntersectionPropertyUp$ carrier is a finite $\BHist$ packet
$$
K=(J,F,A,Q,N,C,E,R,H,T,P,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $J$ is the finite family index row, recording the inspected closed-family cells and the finite subfamily request consumed by the packet.
\item $F$ is the closed-family row, giving each displayed member as a closed interval or closed interval subpacket over the same Real-and-Completion route.
\item $A$ is the finite-acceptance ledger, requiring every index in $J$ to have a displayed closed-family member in $F$ and requiring the finite subfamily request to be read only through those cells.
\item $Q$ is the $\ClosedIntervalCauchyCompletionUp$ row, supplying the endpoint-bounded Cauchy completion handoff for the displayed closed interval cells.
\item $N$ is the $\NestedIntervalCompactnessUp$ row, used when the finite family is routed through a nested-window refinement rather than through a general cover argument.
\item $C$ is the $\CantorIntersectionUp$ row, recording the finite Cantor-style nested-intersection handoff downstream of the displayed nested-window data.
\item $E$ is the $\FiniteEpsilonNetUp$ row or interval-net witness row, recording the finite net, mesh, or interval-window data used to keep the finite family request inside visible compactness-shaped data.
\item $R$ is the terminal $\RealUp$ seal row reached only after $J,F,A,Q,N,C,E$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the finite index, closed-family, completion, nested-window, Cantor handoff, finite-net, Real seal, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from a finite closed-family request through $J,F,A,Q,N,C,E,R,H,P,M$.
\item $P$ is the $\Pkg$ provenance over $\FiniteIntersectionPropertyUp$, $\ClosedIntervalCauchyCompletionUp$, $\NestedIntervalCompactnessUp$, $\CantorIntersectionUp$, $\FiniteEpsilonNetUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $M$ is the local $\NameCert_{\FiniteIntersectionPropertyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $J,F,A,Q,N,C,E,R,H,T,P,M$. The carrier has no coordinate for open-cover compactness, arbitrary closed-family compactness, countable choice, selected endpoints, quotient real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}
\leandef{BEDC.Derived.FiniteIntersectionPropertyUp.FiniteIntersectionPropertyUp}

\begin{theorem}[Finite intersection property NameCert obligations]
\label{thm:finite-intersection-property-namecert-obligations}
For every accepted $\FiniteIntersectionPropertyUp$ carrier
$$
K=(J,F,A,Q,N,C,E,R,H,T,P,M),
$$
the local $\NameCert_{\FiniteIntersectionPropertyUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the finite family index, closed-family cells, finite-acceptance ledger, closed-interval Cauchy completion row, nested-window compactness row, Cantor handoff row, finite epsilon-net or interval-net witness, Real seal, transport, replay, provenance, and local naming rows;
\item finite-family exactness, requiring every accepted intersection request to name a finite subfamily through $J$ and to read each member through the displayed closed-family row $F$;
\item closed-interval completion handoff, requiring $Q$ to be consumed before any nested-window, Cantor, finite-net, or Real-facing conclusion is exported;
\item compactness-shape locality, requiring $N,C,E$ to be read as displayed finite nested-window, Cantor handoff, and finite-net or interval-net rows rather than as an open-cover compactness theorem;
\item Real seal routing, requiring $R$ to remain downstream of $J,F,A,Q,N,C,E$ and upstream only of $H,T,P,M$;
\item componentwise classifier transport, displayed replay, provenance, and local naming for the same rows through $H,T,P,M$;
\item ledger non-escape, requiring the packet to export no open-cover compactness, arbitrary closed-family compactness, countable-choice schedule, selected endpoint, quotient real equality, trichotomy branch, or ambient $\RealUp$ completeness theorem.
\end{enumerate}
No finite intersection property obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:finite-intersection-property-carrier}. The visible packet consists exactly of the finite family index $J$, the closed-family row $F$, the finite-acceptance ledger $A$, the completion row $Q$, the nested-window row $N$, the Cantor handoff row $C$, the finite-net or interval-net witness $E$, the Real seal $R$, and the structural rows $H,T,P,M$.

Finite-family exactness is rowwise. Every accepted request first names its finite subfamily through $J$, and every named member is read through $F$ before any downstream intersection certificate is visible.

The completion and compactness-shaped handoffs are then ordered. The row $Q$ supplies the closed-interval Cauchy completion boundary; the rows $N,C,E$ consume that displayed route as nested-window, Cantor-style, and finite-net data rather than as a general open-cover theorem.

Finally, $R,H,T,P,M$ seal, transport, replay, source, and name the same finite route. Since none of the excluded compactness, choice, endpoint-selection, quotient-equality, trichotomy, or ambient-completeness objects occurs in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Closed interval completion finite intersection handoff]
\label{thm:closed-interval-cauchy-completion-finite-intersection-property}
Every Real-and-Completion consumer that reads an accepted $\FiniteIntersectionPropertyUp$ carrier factors its closed-family intersection request through the finite route
$$
J,F,A \longmapsto Q \longmapsto N,C,E \longmapsto R \longmapsto H,T,P,M .
$$
Consequently the consumer receives a displayed finite family index, displayed closed-family rows, finite-acceptance data, a closed-interval Cauchy completion handoff, nested-window and Cantor intersection handoffs, a finite epsilon-net or interval-net witness, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row. The route supplies no open-cover compactness, arbitrary closed-family compactness, countable choice, selected endpoint, quotient real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier $K=(J,F,A,Q,N,C,E,R,H,T,P,M)$. The finite family index $J$, closed-family row $F$, and acceptance ledger $A$ are the only source coordinates available to a finite intersection request.

The completion boundary is downstream of those rows. By the closed-interval completion handoff obligation of \autoref{thm:finite-intersection-property-namecert-obligations}, the row $Q$ must be consumed before the packet exposes nested-window, Cantor, finite-net, or Real-facing data.

The compactness-shaped data remain finite. The rows $N,C,E$ route the request through displayed nested-window, Cantor-style, and finite-net or interval-net witnesses, and none of these rows contains an open-cover compactness theorem or an arbitrary closed-family principle.

The Real-facing seal and structural rows preserve that order. The row $R$ is reachable only after $J,F,A,Q,N,C,E$, while $H,T,P,M$ transport, replay, source, and name the same displayed route. Thus every consumer factors through the stated finite handoff.
\end{proof}

\falsifiablePrediction{Every $\FiniteIntersectionPropertyUp$ read factors through one displayed finite family index, one closed-family row, one finite-acceptance ledger, one ClosedIntervalCauchyCompletion row, NestedIntervalCompactness and CantorIntersection handoff rows, one FiniteEpsilonNet or interval-net witness, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{closed-interval-cauchy-completion, nestedintervalcompactness, cantor-intersection, finite-epsilon-net, real: $\FiniteIntersectionPropertyUp$ is not bijective to any listed sibling because it jointly carries finite closed-family indexing, closed-interval completion, compactness-shaped nested and Cantor handoffs, finite-net or interval-net witnesses, and the terminal Real seal as one finite intersection packet.}

\closureat{FiniteIntersectionPropertyUp}{seedStr}

\begin{closurestatus}{\FiniteIntersectionPropertyUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-intersection-property-carrier}, \autoref{thm:finite-intersection-property-namecert-obligations}, and \autoref{thm:closed-interval-cauchy-completion-finite-intersection-property} seed the finite closed-family intersection certificate from displayed finite family indices and closed interval rows through closed-interval Cauchy completion, nested-window and Cantor handoffs, finite-net or interval-net witnesses, Real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, arbitrary closed-family compactness, countable choice, selected endpoints, quotient real equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-family exactness, closed-interval completion handoff, compactness-shape locality, Real seal routing, classifier transport, replay, provenance, local naming, and finite route factorization for BEDC.Derived.FiniteIntersectionPropertyUp.}
  \constructivestory{A $\FiniteIntersectionPropertyUp$ packet is generated from a finite family index, displayed closed-family rows, a finite-acceptance ledger, a ClosedIntervalCauchyCompletion row, NestedIntervalCompactness and CantorIntersection handoff rows, a FiniteEpsilonNet or interval-net witness, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
