\chapter{A Concrete Naming Certificate for $\UniformContinuityGraphModulusUp$}
\label{ch:concrete-instances-uniform-continuity-graph-modulus-namecert}
\origin{human}

Following Heine--Cantor and Bishop--Bridges compact metric analysis, the $\UniformContinuityGraphModulusUp$ packet realizes the compact uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} as graph-level finite NameCert data. It sits beside the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map packet of \autoref{ch:concrete-instances-continuousmap-namecert}, the metric-distance rows of \autoref{ch:concrete-instances-metric-namecert}, the rational tolerance fold of \autoref{ch:concrete-instances-rat-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names the finite graph and modulus ledger; it is not open-cover compactness, host function extensionality, quotient map equality, a selected external modulus, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Uniform-continuity graph modulus carrier]
\label{def:uniform-continuity-graph-modulus-carrier}
A $\UniformContinuityGraphModulusUp$ carrier is a finite $\BHist$ packet
$$
(K,G,L,Q,S,T,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row, exposing bundled source centers, coverage witnesses, and finite $\ProbeBundle$ membership data.
\item $G$ is the continuous graph row, pairing source and target metric reads through $\ContinuousMapUp$ before any modulus is exported.
\item $L$ is the pointwise-modulus ledger attached to the graph row at each bundled center.
\item $Q$ is the $\RatUp$ lower-bound fold over the positive radii carried by $L$.
\item $S$ is the source $\MetricSpaceUp$ or $\MetricUp$ triangle row used to move nearby source points into a common local graph neighbourhood.
\item $T$ is the target metric-triangle row used to combine the two pointwise target estimates.
\item $U$ is the $\UniformModulusUp$ handoff row, exposing only a tolerance request and the folded precision obtained from $Q$.
\item $H$ records componentwise $\hsame$ transport for compact nets, graph rows, local modulus ledgers, rational folds, metric-triangle rows, uniform-modulus rows, replay, provenance, and naming.
\item $C$ records displayed $\Cont$ replay from tolerance request through finite-net coverage, graph lookup, pointwise-modulus readback, rational fold, source and target triangle rows, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\UniformContinuityGraphModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\MetricSpaceUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformContinuityGraphModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for open-cover compactness, host function extensionality, quotient maps, selected external moduli, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Uniform-continuity graph modulus NameCert obligations]
\label{thm:uniform-continuity-graph-modulus-namecert-obligations}
For every accepted $\UniformContinuityGraphModulusUp$ carrier $(K,G,L,Q,S,T,U,H,C,P,N)$, the local $\NameCert_{\UniformContinuityGraphModulusUp}$ surface consists exactly of carrier admission for compact finite nets, continuous graph rows, pointwise-modulus ledgers, rational lower-bound folds, source and target metric-triangle rows, uniform-modulus handoff, componentwise transport, displayed continuation replay, provenance, and local naming. Every compact-uniform-continuity read must pass through those rows before the public $\UniformModulusUp$ output is available.
\end{theorem}
\begin{proof}
Project the accepted packet. Its analytic rows are the compact finite-net row $K$, continuous graph row $G$, pointwise-modulus ledger $L$, rational fold $Q$, source and target metric rows $S,T$, and uniform-modulus handoff $U$.

Finite-net coverage comes first. A source point is matched to a bundled center through $K$, and the graph row $G$ supplies only the displayed continuous-map relation at that center.

The local modulus ledger $L$ provides positive radii and target estimates for the graph row. The rational fold $Q$ is read by primitive recursion over those finitely many radii, yielding a single precision below every displayed local radius.

The source triangle row $S$ moves a nearby pair of source points into the same local graph neighbourhood, and the target triangle row $T$ combines the resulting target estimates. Only then does $U$ expose the tolerance-only $\UniformModulusUp$ handoff.

Rows $H,C,P,N$ transport, replay, source, and name the same route. Since the packet has no coordinate for open-cover compactness, host function extensionality, quotient maps, selected external moduli, countable choice, or ambient completeness, those objects cannot escape through the NameCert surface.
\end{proof}

\closureat{UniformContinuityGraphModulusUp}{seedStr}

\begin{closurestatus}{\UniformContinuityGraphModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-continuity-graph-modulus-carrier} and \autoref{thm:uniform-continuity-graph-modulus-namecert-obligations} seed the compact finite-net, continuous graph, pointwise-modulus, ProbeBundle fold, metric-triangle, and uniform-modulus handoff route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, host function extensionality, quotient map equality, selected external moduli, countable choice, ambient real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-net coverage, graph lookup, pointwise-modulus exactness, rational-fold lower bounds, source-target triangle handoff, transport, replay, provenance, and local naming for BEDC.Derived.UniformContinuityGraphModulusUp.}
  \constructivestory{A $\UniformContinuityGraphModulusUp$ packet is generated from compact finite-net rows, continuous graph rows, pointwise-modulus ledgers, RatUp lower-bound folds, source and target MetricSpace triangle rows, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
