\chapter{A Concrete Naming Certificate for $\RiemannMappingUp$}
\label{ch:concrete-instances-riemann-mapping-namecert}
\origin{human}

Following Riemann, Koebe, and Ahlfors, the $\RiemannMappingUp$ packet records the simply connected proper planar-domain uniformization route as finite NameCert data. It is grounded in the holomorphic-map surface of \autoref{ch:concrete-instances-holomorphic-namecert}, the disk target of \autoref{ch:concrete-instances-unitdisk-namecert}, the complex carrier of \autoref{ch:concrete-instances-complex-namecert}, the normal-family compactness route of \autoref{ch:concrete-instances-normal-family-namecert}, the pointed disk bound of \autoref{ch:concrete-instances-schwarz-lemma-namecert}, and the disk-contraction comparison of \autoref{ch:concrete-instances-schwarz-pick-disk-contraction-namecert}. The certificate names the classical route without importing a host theorem about all complex domains, selected boundary prime ends, quotient function equality, or ambient compact-open function-space completeness.

\begin{definition}[Riemann mapping carrier]
\label{def:riemann-mapping-carrier}
A $\RiemannMappingUp$ carrier is a finite $\BHist$ packet
$$
R=(D,U,F,I,B,NF,SL,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the simply connected proper complex-domain row over $\ComplexUp$, recording the finite source-domain observations and the refusal of the whole-plane case.
\item $U$ is the $\UnitDiskUp$ target row, including the named disk origin and disk boundary observations consumed by the mapping route.
\item $F$ is the holomorphic conformal-map row read through $\HolomorphicUp$, recording injective local coordinate preservation from $D$ to $U$.
\item $I$ is the inverse-map row, displaying the holomorphic inverse on the accepted image route rather than a host inverse function.
\item $B$ is the boundary-refusal row, saying that the packet exports no selected boundary extension, Caratheodory boundary theorem, prime-end classifier, or boundary homeomorphism.
\item $NF$ is the $\NormalFamilyUp$ route that supplies the compactness-style search surface for the extremal disk-normalized map.
\item $SL$ is the Schwarz route through $\SchwarzLemmaUp$ and $\SchwarzPickDiskContractionUp$, recording pointed normalization, disk contraction, uniqueness up to displayed disk automorphism data, and nonexpansion comparisons.
\item $H$ records componentwise $\hsame$ transport for the domain, disk, map, inverse, boundary refusal, normal-family route, Schwarz route, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for the same rows, so a consumer replays the finite route rather than consulting an ambient analytic-function space.
\item $P$ is the $\Pkg$ provenance over $\RiemannMappingUp$, $\HolomorphicUp$, $\UnitDiskUp$, $\ComplexUp$, $\NormalFamilyUp$, $\SchwarzLemmaUp$, $\SchwarzPickDiskContractionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\RiemannMappingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,U,F,I,B,NF,SL,H,C,P,L$. It has no coordinate for a selected conformal map outside the displayed normalization route, no quotient equality of holomorphic functions, no ambient compact-open completion, no boundary correspondence theorem, and no appeal to a choice principle over domains.
\end{definition}

\begin{theorem}[Riemann mapping NameCert obligations]
\label{thm:riemann-mapping-namecert-obligations}
For every accepted $\RiemannMappingUp$ carrier $R=(D,U,F,I,B,NF,SL,H,C,P,L)$, the local $\NameCert_{\RiemannMappingUp}$ surface consists exactly of the simply connected proper complex-domain row, the $\UnitDiskUp$ target row, the holomorphic conformal-map row, the inverse-map row, the boundary-refusal row, the $\NormalFamilyUp$ route, the $\SchwarzLemmaUp$ and $\SchwarzPickDiskContractionUp$ route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:riemann-mapping-carrier}. The analytic rows are $D,U,F,I,B,NF,SL$, and the structural rows are $H,C,P,L$.

The domain row $D$ is the only row that names a simply connected proper complex source, while $U$ is the only row that names the disk target. Thus any accepted read of the mapping claim must display both endpoints before it reaches the map or inverse rows.

The map rows $F,I$ are routed through the normal-family and Schwarz rows. The normal-family row supplies the extremal compactness-style route, and the Schwarz and Schwarz--Pick rows supply the pointed disk comparison and contraction discipline used by the uniqueness read.

Finally $H,C,P,L$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for selected boundary values, quotient function equality, ambient compact-open completion, or a global choice over domains, those objects are outside the NameCert obligations.
\end{proof}

\begin{theorem}[Riemann mapping normal-family route]
\label{thm:riemann-mapping-normal-family-route}
Every accepted $\RiemannMappingUp$ consumer that reads a disk uniformization claim must pass through the ordered route
$$
\ComplexUp,\ \HolomorphicUp,\ \UnitDiskUp,\ \NormalFamilyUp,\ \SchwarzLemmaUp,\ \SchwarzPickDiskContractionUp .
$$
The route first displays the proper simply connected complex-domain source, then the disk target and holomorphic conformal-map rows, then the normal-family extremal surface, and finally the Schwarz comparison rows. It exports the displayed mapping packet and inverse-map row, not a boundary extension theorem, quotient equality of maps, host analytic continuation principle, or ambient function-space completeness.
\end{theorem}
\begin{proof}
Start with the source and target rows. The complex carrier and holomorphic-map surface provide only the displayed domain observations and the map row, while the UnitDisk row fixes the codomain used by the disk-normalized route.

Next apply the normal-family route. It is the only compactness-style row in the packet, so the extremal-map read must pass through $NF$ before any disk uniformization conclusion is named.

The Schwarz rows then control the disk comparison. $\SchwarzLemmaUp$ supplies the pointed disk bound, and $\SchwarzPickDiskContractionUp$ supplies the contraction comparison, so uniqueness and normalization are read through the finite disk route rather than an ambient analytic-function equality.

The transport, replay, provenance, and local naming rows preserve this route unchanged. Therefore a consumer that skips the normal-family row, the Schwarz rows, or the displayed boundary-refusal row is not reading an accepted $\RiemannMappingUp$ packet.
\end{proof}

\falsifiablePrediction{A purported $\RiemannMappingUp$ consumer that claims a disk uniformization without displaying the $\NormalFamilyUp$ route and the $\SchwarzLemmaUp$--$\SchwarzPickDiskContractionUp$ comparison route is outside the carrier surface.}

\independenceWitness{holomorphic, unitdisk, complex, normal-family, schwarz-lemma, schwarz-pick-disk-contraction: none is bijective to $\RiemannMappingUp$ because only this packet binds the simply connected proper domain, disk target, conformal map, inverse row, boundary refusal, normal-family route, Schwarz comparison route, transport, replay, provenance, and local NameCert rows into one accepted carrier.}

\closureat{RiemannMappingUp}{seedStr}

\begin{closurestatus}{\RiemannMappingUp}
  \constructivestory{A $\RiemannMappingUp$ packet is generated from a simply connected proper complex-domain row, UnitDisk target row, holomorphic conformal-map row, inverse-map row, boundary-refusal row, NormalFamily route, SchwarzLemma and SchwarzPickDiskContraction route, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:riemann-mapping-carrier}, \autoref{thm:riemann-mapping-namecert-obligations}, and \autoref{thm:riemann-mapping-normal-family-route} seed the finite Riemann mapping carrier, obligation surface, and normal-family route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, boundary extension theorems, quotient equality of holomorphic maps, selected conformal maps outside the displayed normalization route, or ambient compact-open function-space completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, domain properness, simple-connectedness routing, disk target exactness, conformal-map and inverse-map exactness, boundary refusal, normal-family handoff, Schwarz comparison, transport, replay, provenance, and local naming for BEDC.Derived.RiemannMappingUp.}
\end{closurestatus}
