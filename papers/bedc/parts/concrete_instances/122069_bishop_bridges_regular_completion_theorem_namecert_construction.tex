\chapter{A Concrete Naming Certificate for $\BishopBridgesRegularCompletionTheoremUp$}
\label{ch:concrete-instances-bishop-bridges-regular-completion-theorem-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopBridgesRegularCompletionTheoremUp$ packet records the regular-Cauchy completion theorem as finite BEDC naming data rather than as a quotient construction on streams. It reads dyadic tolerance arithmetic from \autoref{ch:concrete-instances-dyadicratcore-namecert}, finite observation windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal from \autoref{ch:concrete-instances-real-namecert}, and the extension-and-uniqueness route of \autoref{ch:concrete-instances-regular-cauchy-completion-universal-property-namecert}. The theorem packet names a capstone handoff for regular completion consumers; it does not identify quotient streams, select limits, invoke countable choice, assert host real equality, or claim ambient $\RealUp$ completeness.

\begin{definition}[Bishop--Bridges regular completion theorem carrier]
\label{def:bishop-bridges-regular-completion-theorem-carrier}
A $\BishopBridgesRegularCompletionTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(D,W,R,A,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare regular Cauchy observations.
\item $W$ is the $\StreamNameUp$ finite-window row opened by the inspected completion request.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row visible through $D$ and $W$.
\item $A$ is the terminal $\RealUp$ seal row available only after the dyadic, stream, and regular-rational rows have been displayed.
\item $U$ is the $\RegularCauchyCompletionUniversalPropertyUp$ handoff row, read as an extension-and-uniqueness surface over the same finite completion data.
\item $H$ records componentwise $\hsame$ transport for $D,W,R,A,U,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a completion theorem request through the dyadic, stream, regular-rational, real-seal, and universal-property rows.
\item $P$ is the $\Pkg$ provenance over $\BishopBridgesRegularCompletionTheoremUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RegularCauchyCompletionUniversalPropertyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopBridgesRegularCompletionTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,W,R,A,U,H,C,P,N$. It has no coordinate for quotient stream equality, selected representatives, selected limits, countable choice, host real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Bishop--Bridges regular completion theorem NameCert obligations]
\label{thm:bishop-bridges-regular-completion-theorem-namecert-obligations}
For every accepted $\BishopBridgesRegularCompletionTheoremUp$ packet
$$
T=(D,W,R,A,U,H,C,P,N),
$$
the local $\NameCert_{\BishopBridgesRegularCompletionTheoremUp}$ obligation sketch has exactly five rows:
\begin{enumerate}
\item carrier: $D,W,R,A,U,H,C,P,N$ must be displayed before the theorem packet is public;
\item classifier: accepted packets are compared only by the dyadic tolerance, stream-window, regular-rational, real-seal, universal-property, transport, replay, provenance, and local naming rows;
\item stability: componentwise $\hsame$ transport in $H$ and displayed $\Cont$ replay in $C$ preserve the same completion route;
\item ledger: $P$ records the theorem packet as sourced from the dyadic, stream, regular-rational, real, and regular-completion universal-property siblings;
\item public theorem: the exported theorem handoff is the finite route through those rows, not a quotient completion, a selected limit, a choice principle, host real equality, or ambient $\RealUp$ completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-bridges-regular-completion-theorem-carrier}. The rows $D,W,R,A,U$ expose the dyadic tolerance ledger, finite stream windows, regular rational readback, real-name seal, and universal-property handoff before any theorem-facing consumer can inspect the packet.

The classifier and stability rows are structural. A consumer can compare packets only by the displayed coordinates, while $H$ and $C$ transport and replay the same ordered route without adding quotient equality or a selected representative.

Finally read the provenance and local naming rows. They bind the packet to the sibling chapters named in the opening paragraph and export only the finite theorem handoff. Since no excluded coordinate occurs in the carrier, the five listed rows exhaust the local NameCert obligation sketch.
\end{proof}

\begin{theorem}[Bishop--Bridges regular completion theorem handoff]
\label{thm:bishop-bridges-regular-completion-theorem-handoff}
Every completion-facing consumer of an accepted $\BishopBridgesRegularCompletionTheoremUp$ packet reads the route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \RegularCauchyCompletionUniversalPropertyUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert
$$
as the finite regular-completion theorem surface. The handoff may be consumed by Real and Completion chapters only after the dyadic tolerance, finite observation, regular rational readback, real seal, extension-and-uniqueness, transport, replay, provenance, and local naming rows have all been displayed.
\end{theorem}
\begin{proof}
Begin with the dyadic and stream rows. They determine the tolerance request and the finite windows that are visible to the theorem packet.

Next read the regular rational and real rows. The $\RegSeqRatUp$ row supplies the regular readback, and the $\RealUp$ row is reached only as the terminal seal over the same displayed data.

The universal-property row is then consumed as a finite extension-and-uniqueness ledger, through \autoref{ch:concrete-instances-regular-cauchy-completion-universal-property-namecert}. Transport, replay, provenance, and local naming preserve that route for downstream consumers. No step introduces quotient stream equality, selected limits, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopBridgesRegularCompletionTheoremUp$ read exposes dyadic tolerance, finite StreamName windows, RegSeqRat readback, a RealUp seal, a regular-Cauchy universal-property row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data before the theorem handoff is public.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, regular-cauchy-completion-universal-property: none is bijective with $\BishopBridgesRegularCompletionTheoremUp$ because the theorem packet binds all five rows as one completion-facing capstone surface.}

\closureat{BishopBridgesRegularCompletionTheoremUp}{seedStr}

\begin{closurestatus}{\BishopBridgesRegularCompletionTheoremUp}
  \constructivestory{A $\BishopBridgesRegularCompletionTheoremUp$ packet is generated from dyadic tolerance rows, finite StreamName windows, RegSeqRat readback, a RealUp seal, a regular-Cauchy universal-property row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-bridges-regular-completion-theorem-carrier}, \autoref{thm:bishop-bridges-regular-completion-theorem-namecert-obligations}, and \autoref{thm:bishop-bridges-regular-completion-theorem-handoff} seed the finite regular-completion theorem route over dyadic, stream, regular rational, real-seal, universal-property, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier locality, transport stability, provenance non-escape, and theorem handoff for BEDC.Derived.BishopBridgesRegularCompletionTheoremUp.}
\end{closurestatus}
