\chapter{A Concrete Naming Certificate for $\DyadicTailBoundUp$}
\label{ch:concrete-instances-dyadictailbound-namecert}
\origin{human}

The $\DyadicTailBoundUp$ interface records the finite tail-budget certificate used by Bishop--Bridges constructive analysis when a real-facing construction presents explicit rational tail control. It sits between $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$: the packet names the displayed precision row, the modulus row that selects the inspected tail, the dyadic tolerance used for the comparison, the finite ledger of tail estimates, and the seal-facing readback route. It does not form quotient streams, select limits, identify host real equality, or assume ambient completeness.

The tail-budget packet consumes the CauchyModulus schedule of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the regular-rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}. These sibling dependencies explain how later regular-Cauchy and $\RealUp$ packets read the same finite threshold, tolerance, and ledger rows without adding a quotient stream or an ambient completeness input.

\begin{definition}[DyadicTailBound BHist carrier]
\label{def:dyadic-tail-bound-carrier}
A $\DyadicTailBoundUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $p$ is a unary precision row naming the displayed request;
\item $\mu$ is the $\CauchyModulusUp$ schedule row that assigns the finite tail threshold used for $p$;
\item $\epsilon$ is the $\DyadicRatCoreUp$ tolerance row whose dyadic value is the comparison budget attached to $p$;
\item $L$ is the finite tail comparison ledger: every inspected tail entry is paired with the threshold supplied by $\mu$, the tolerance supplied by $\epsilon$, and the displayed rational comparison row;
\item $R$ is the $\RegSeqRatUp$ readback route obtained by reading the same finite tail ledger as regular rational sequence evidence;
\item $E$ is the $\RealUp$ seal-facing row reached through $R$ and through no other real-facing coordinate;
\item $H$ records the componentwise $\hsame$ transports for precision, schedule, tolerance, tail-ledger, readback, and seal rows;
\item $C$ records the displayed $\Cont$ routes by which consumers reach the schedule, tolerance, ledger, regular-sequence readback, and real seal;
\item $P$ is the $\Pkg$ provenance over $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and the rational source rows used by the ledger;
\item $N$ is the local $\NameCert_{\DyadicTailBoundUp}$ provenance row.
\end{enumerate}
The classifier compares two accepted carriers only by these displayed rows, their componentwise $\hsame$ transports, their $\Cont$ read routes, their $\Pkg$ provenance, and their local $\NameCert$ rows.
\end{definition}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundTasteGate\_single\_carrier\_alignment}

\begin{theorem}[DyadicTailBound NameCert obligations]
\label{thm:dyadic-tail-bound-namecert-obligations}
The local $\NameCert_{\DyadicTailBoundUp}$ surface consists of five finite obligations:
\begin{enumerate}
\item carrier habitation by the zero-tail packet, where the precision row is arbitrary, the schedule threshold is the displayed zero threshold, the dyadic tolerance is the zero-compatible $\DyadicRatCoreUp$ budget, the tail ledger records only zero comparisons, the $\RegSeqRatUp$ readback is the constant zero row, and the $\RealUp$ seal-facing row is the corresponding zero seal;
\item classifier transport under componentwise $\hsame$ and displayed $\Cont$ routes for the precision, $\CauchyModulusUp$ schedule, $\DyadicRatCoreUp$ tolerance, finite tail ledger, $\RegSeqRatUp$ readback, $\RealUp$ seal, $\Pkg$, and local $\NameCert$ rows;
\item dyadic tolerance exactness: every accepted tail comparison uses exactly the tolerance row $\epsilon$ displayed in the carrier and cannot substitute an unrecorded rational radius;
\item regular-sequence handoff: the finite ledger $L$ repacks as the visible $\RegSeqRatUp$ readback row $R$ without dropping the schedule or tolerance rows that justify the tail budget;
\item $\RealUp$ seal non-escape: the seal-facing row $E$ reads only through $R$, $L$, $\mu$, $\epsilon$, $H$, $C$, $P$, and $N$, and exports no quotient stream, selected limit, host real equality, or ambient completeness object.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_namecert\_obligations}
\begin{proof}
Carrier habitation is witnessed by the zero-tail packet. The schedule row supplies the zero threshold, the dyadic tolerance row is the displayed zero-compatible budget, and every ledger entry is the visible zero comparison. Repacking those rows gives the constant zero $\RegSeqRatUp$ readback and the matching $\RealUp$ seal-facing row.

Classifier transport is componentwise. Transport the precision request, schedule threshold, dyadic tolerance, ledger entry, readback row, and seal row along the stored $\hsame$ comparisons, then replay the recorded $\Cont$ routes. Since the classifier reads only the listed packet coordinates, the transported carrier is accepted by the same finite shape.

Dyadic tolerance exactness follows by projecting the ledger coordinate $L$. Each accepted comparison names the tolerance coordinate $\epsilon$ of the same carrier, so a consumer cannot replace it with an unrecorded rational radius or an ambient metric estimate.

The regular-sequence handoff is the projection to $R$. It keeps the finite ledger together with the schedule and tolerance evidence needed by $\RegSeqRatUp$, so no tail choice or quotient of streams is introduced during readback.

Finally the $\RealUp$ seal-facing row is reached only after the regular-sequence handoff. The carrier has no coordinate for selected limits, host real equality, quotient streams, or completeness principles, and therefore none of those objects can be exported by the local certificate.
\end{proof}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_semantic\_name\_certificate}

\begin{theorem}[DyadicTailBound zero packet carrier]
\label{thm:dyadic-tail-bound-zero-packet-carrier}
For every displayed precision row $p$, the zero-tail packet
$$
T_0=(p,\mu_0,\epsilon_0,L_0,R_0,E_0,H_0,C_0,P_0,N_0)
$$
inhabits the $\DyadicTailBoundUp$ carrier of \autoref{def:dyadic-tail-bound-carrier}. The row $\mu_0$ is the displayed schedule threshold for the zero request, $\epsilon_0$ is the zero-compatible $\DyadicRatCoreUp$ tolerance, $L_0$ is the finite ledger of zero tail comparisons at that threshold and tolerance, $R_0$ is the corresponding $\RegSeqRatUp$ zero readback, $E_0$ is the $\RealUp$ zero seal reached through $R_0$, and $H_0,C_0,P_0,N_0$ are the matching $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DyadicTailBoundUp}$ rows.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_zero\_packet\_carrier}
\begin{proof}
Build the packet by the displayed carrier coordinates in \autoref{def:dyadic-tail-bound-carrier}. The precision coordinate is $p$, and the schedule, tolerance, ledger, readback, and seal coordinates are the named zero rows.

The ledger $L_0$ contains only zero tail comparisons, each paired with the stored threshold $\mu_0$ and tolerance $\epsilon_0$. Thus the packet satisfies the same finite ledger condition used by the classifier.

Apply \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} to read the ledger as the constant zero $\RegSeqRatUp$ row $R_0$, and apply \autoref{thm:dyadic-tail-bound-real-seal-factorization} to route the $\RealUp$ seal through that readback. The transport, continuation, package, and local certificate rows are all displayed coordinates of the same packet, so no extra real-facing object is introduced.
\end{proof}

\begin{theorem}[DyadicTailBound precision window restriction]
\label{thm:dyadic-tail-bound-precision-window-restriction}
Let
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
be an accepted $\DyadicTailBoundUp$ carrier. Every consumer read of the precision row $p$ factors through the stored schedule threshold $\mu$, the displayed dyadic tolerance row $\epsilon$, the finite ledger $L$, and the displayed $\Cont$ routes in $C$. In particular, no consumer precision read may bypass the carrier by using an unrecorded threshold, a separate tolerance, or a ledger outside $L$.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_precision\_window\_restriction}
\begin{proof}
Project the packet through \autoref{def:dyadic-tail-bound-carrier}. The precision row is stored in the same finite carrier as the schedule threshold, tolerance, ledger, readback, seal, transport, continuation, package, and naming rows.

Use \autoref{thm:dyadic-tail-bound-classifier-transport-exactness} to identify accepted consumer transport with componentwise $\hsame$ movement and displayed $\Cont$ reads. The classifier has no coordinate for an external threshold or tolerance.

The readback and real-facing routes are fixed by \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} and \autoref{thm:dyadic-tail-bound-real-seal-factorization}. Hence a precision consumer reaches the schedule, tolerance, ledger, and seal only through the stored packet routes.
\end{proof}

\begin{theorem}[DyadicTailBound ledger append stability]
\label{thm:dyadic-tail-bound-ledger-append-stability}
Let
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
be an accepted $\DyadicTailBoundUp$ carrier. If a displayed tail-comparison row $q$ is appended to $L$ with the same schedule threshold $\mu$, the same dyadic tolerance row $\epsilon$, and the same $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DyadicTailBoundUp}$ provenance rows, then the enlarged packet is accepted by the same carrier/classifier surface and its $\RegSeqRatUp$ handoff still reads through the enlarged displayed ledger.
\end{theorem}
\begin{proof}
Start from the carrier coordinates in \autoref{def:dyadic-tail-bound-carrier}. The appended row $q$ is not a new kind of evidence: it is another displayed tail comparison paired with the same threshold $\mu$ and tolerance $\epsilon$ already used by $L$.

Apply \autoref{thm:dyadic-tail-bound-classifier-transport-exactness}. Because the schedule, tolerance, transport, continuation, package, and local certificate rows are preserved, classifier acceptance remains the same finite carrier condition with the enlarged ledger coordinate.

Apply \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} to repack the enlarged ledger as the $\RegSeqRatUp$ handoff, and then \autoref{thm:dyadic-tail-bound-real-seal-factorization} for the seal route. The handoff still factors through the displayed ledger, so the append operation does not create an unrecorded tail estimate or a separate real-facing route.
\end{proof}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_ledger\_append\_stability}

\begin{theorem}[DyadicTailBound classifier transport exactness]
\label{thm:dyadic-tail-bound-classifier-transport-exactness}
Let
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
be an accepted $\DyadicTailBoundUp$ $\mathsf{BHist}$ carrier. If another displayed packet $T'$ is obtained from $T$ by the componentwise $\hsame$ transports recorded in $H$ and by the displayed $\Cont$ routes recorded in $C$, while preserving the $\Pkg$ row $P$ and the local $\NameCert_{\DyadicTailBoundUp}$ row $N$, then the classifier accepts $T'$ exactly when the transported precision row, schedule row $\mu$, tolerance row $\epsilon$, ledger row $L$, $\RegSeqRatUp$ row $R$, and $\RealUp$ seal row $E$ are the corresponding transported coordinates. In particular, classifier transport preserves accepted carriers and cannot replace the displayed tolerance, ledger, readback, or seal by unrecorded rows.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_classifier\_transport\_exactness}
\begin{proof}
Project the carrier coordinates of \autoref{def:dyadic-tail-bound-carrier}. The classifier reads only the precision row, the $\CauchyModulusUp$ schedule, the $\DyadicRatCoreUp$ tolerance, the tail ledger, the regular-sequence readback, the real seal, and the carried transport and provenance rows.

Apply the $\hsame$ witnesses in $H$ component by component, and use the $\Cont$ routes in $C$ only to read the transported displayed rows. The package row $P$ and local $\NameCert$ row $N$ are preserved as provenance for the same finite packet shape.

Since the classifier has no coordinate outside those projections, the transported packet is accepted precisely by the transported carrier rows. This rules out substitution of an ambient rational radius, an unlisted ledger, or a separate real-facing endpoint.
\end{proof}

\begin{theorem}[DyadicTailBound regular-sequence handoff exactness]
\label{thm:dyadic-tail-bound-regseqrat-handoff-exactness}
For every accepted $\DyadicTailBoundUp$ carrier
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N),
$$
the $\RegSeqRatUp$ row $R$ is the handoff obtained from the same schedule row $\mu$, dyadic tolerance row $\epsilon$, and finite tail ledger $L$. Any consumer of $R$ through the displayed $\Cont$ routes must therefore retain the schedule, tolerance, and ledger data certified by the $\Pkg$ row $P$ and the local $\NameCert_{\DyadicTailBoundUp}$ row $N$.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_regseqrat\_handoff\_exactness}
\begin{proof}
Read the schedule, tolerance, ledger, and readback coordinates from the carrier. The row $L$ records the inspected tail comparisons, and each comparison is indexed by the threshold named by $\mu$ and the dyadic budget named by $\epsilon$.

The readback row $R$ is the regular rational view of exactly those finite comparisons. Repacking $L$ as $R$ forgets no schedule or tolerance evidence, because those rows are part of the same displayed $\mathsf{BHist}$ packet and are reached by the same $\Cont$ routes.

The provenance rows $P$ and $N$ certify the dependency package and the local naming surface. They add no hidden stream quotient or selected limit, so the handoff remains the finite $\RegSeqRatUp$ row determined by $\mu$, $\epsilon$, and $L$.
\end{proof}

\begin{theorem}[DyadicTailBound real-seal factorization]
\label{thm:dyadic-tail-bound-real-seal-factorization}
For an accepted $\DyadicTailBoundUp$ carrier
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N),
$$
the $\RealUp$ seal row $E$ factors through the $\RegSeqRatUp$ row $R$, the finite tail ledger $L$, the schedule row $\mu$, and the dyadic tolerance row $\epsilon$. The rows $H$, $C$, $P$, and $N$ certify transport, readback, package provenance, and local naming for that factorization, but they do not supply a quotient of streams, a selected limit, host real equality, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_real\_seal\_factorization}
\begin{proof}
Project the seal coordinate $E$ from the carrier. By the carrier definition, the only real-facing route first reaches the $\RegSeqRatUp$ row $R$ obtained from the displayed tail data.

Use \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} to identify $R$ with the repacking of $L$, $\mu$, and $\epsilon$. The transport and continuation rows certify that consumers reach these coordinates through the displayed $\mathsf{BHist}$ packet.

No coordinate of the carrier names a quotient stream, a chosen limiting real, host equality of real numbers, or a completeness principle. Therefore the seal row factors only through the finite readback and tail-budget rows listed in the statement.
\end{proof}

\begin{theorem}[DyadicTailBound precision refinement stability]
\label{thm:dyadic-tail-bound-precision-refinement-stability}
Let
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
be an accepted $\DyadicTailBoundUp$ carrier. If a displayed refinement replaces the precision row $p$ by $p'$ through the componentwise $\hsame$ transport recorded in $H$ and through the displayed $\Cont$ routes recorded in $C$, then the same refinement transports the schedule row $\mu$, dyadic tolerance row $\epsilon$, ledger row $L$, $\RegSeqRatUp$ readback row $R$, $\RealUp$ seal row $E$, package row $P$, and local $\NameCert_{\DyadicTailBoundUp}$ row $N$ as one classifier-facing packet.

The transported packet is again read only through the displayed rows of \autoref{def:dyadic-tail-bound-carrier}; in particular, the refined precision request cannot detach the schedule, tolerance, ledger, readback, seal, package, or naming row from the original finite tail-budget certificate.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_precision\_refinement\_stability}
\begin{proof}
Project the carrier of \autoref{def:dyadic-tail-bound-carrier}. The precision row is not an isolated parameter: it is stored in the same finite packet as the schedule, tolerance, ledger, readback, seal, transport, continuation, package, and local certificate rows.

Apply the componentwise $\hsame$ witnesses recorded in $H$ to the precision row and then to each dependent row listed in the statement. The displayed $\Cont$ routes in $C$ give the only consumer reads of the transported schedule, tolerance, ledger, regular-sequence readback, and real seal.

Now use \autoref{thm:dyadic-tail-bound-classifier-transport-exactness} to identify classifier acceptance with those transported coordinates. The regular-sequence and real-facing projections remain the routes described by \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} and \autoref{thm:dyadic-tail-bound-real-seal-factorization}.

Thus the refinement moves the whole finite certificate at once. There is no coordinate from which a consumer could keep the refined precision while replacing the tail schedule, tolerance, ledger, readback, seal, package, or local naming row by unrecorded data.
\end{proof}

\begin{theorem}[DyadicTailBound ledger threshold exhaustion]
\label{thm:dyadic-tail-bound-ledger-threshold-exhaustion}
For every accepted $\DyadicTailBoundUp$ carrier
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N),
$$
each comparison consumed from the ledger $L$ factors through the displayed threshold row $\mu$ and the displayed dyadic tolerance row $\epsilon$. The carrier has no additional tail-estimate row, no ambient metric estimate, and no separate rational radius from which a ledger consumer can justify the same comparison.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_ledger\_comparison\_determinacy}
\begin{proof}
Read the ledger coordinate $L$ from \autoref{def:dyadic-tail-bound-carrier}. By definition, every inspected tail entry is paired with the threshold supplied by $\mu$ and the tolerance supplied by $\epsilon$.

The handoff theorem \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} keeps the ledger together with exactly those two rows when it repacks the data as the $\RegSeqRatUp$ readback $R$. The seal theorem \autoref{thm:dyadic-tail-bound-real-seal-factorization} then reaches the $\RealUp$ row only through that readback and the same finite ledger.

The remaining rows $H$, $C$, $P$, and $N$ record transport, consumer reads, package provenance, and local naming. They certify access to the displayed rows, but none of them carries an extra estimate.

Therefore any ledger comparison is exhausted by $\mu$, $\epsilon$, and the visible ledger entry. A downstream consumer cannot cite an unrecorded tail estimate without leaving the carrier surface.
\end{proof}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_ledger\_threshold\_exhaustion}

\begin{theorem}[DyadicTailBound seal consumer determinacy]
\label{thm:dyadic-tail-bound-seal-consumer-determinacy}
Suppose two consumers read an accepted $\DyadicTailBoundUp$ carrier
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
through the same displayed rows $R$, $L$, $\mu$, $\epsilon$, $H$, $C$, $P$, and $N$. Then both consumers obtain the same classifier-facing $\RealUp$ seal row $E$. This determinacy is only a statement about the visible seal row of the carrier; it does not export host equality of real numbers, quotient equality of streams, a selected limit, or a completeness datum.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_seal\_consumer\_determinacy}
\begin{proof}
Project the seal coordinate $E$ from the carrier. The definition gives a single real-facing row attached to the regular-sequence readback route.

By \autoref{thm:dyadic-tail-bound-real-seal-factorization}, every consumer route to $E$ factors through $R$, $L$, $\mu$, and $\epsilon$, with $H$, $C$, $P$, and $N$ supplying only transport, readback, provenance, and local naming. The two consumers in the statement therefore traverse the same displayed factorization.

Apply \autoref{thm:dyadic-tail-bound-classifier-transport-exactness} to compare the resulting reads. Since the classifier sees only the listed rows, the two reads have the same classifier-facing seal coordinate.

The conclusion does not add a host real carrier or an equality principle. It records determinacy of the finite $\RealUp$ seal row already present in the packet.
\end{proof}

\begin{theorem}[DyadicTailBound zero-tail carrier habitation]
\label{thm:dyadic-tail-bound-zero-tail-carrier-habitation}
For every displayed precision row $p$, the zero-tail packet
$$
T_0=(p,\mu_0,\epsilon_0,L_0,R_0,E_0,H_0,C_0,P_0,N_0)
$$
inhabits the $\DyadicTailBoundUp$ carrier. Here $\mu_0$ is the displayed zero threshold, $\epsilon_0$ is the zero-compatible $\DyadicRatCoreUp$ tolerance, $L_0$ is the finite ledger whose accepted comparisons are zero comparisons at that threshold and tolerance, $R_0$ is the constant zero $\RegSeqRatUp$ readback, $E_0$ is the corresponding $\RealUp$ zero seal, $H_0$ and $C_0$ are the componentwise $\hsame$ and $\Cont$ rows for those displayed coordinates, and $P_0,N_0$ are the matching $\Pkg$ and local $\NameCert_{\DyadicTailBoundUp}$ provenance rows.
\end{theorem}
\begin{proof}
Build the packet by the carrier coordinates of \autoref{def:dyadic-tail-bound-carrier}. The precision coordinate is the given row $p$, while the schedule, tolerance, ledger, readback, and seal coordinates are the displayed zero rows.

The ledger coordinate contains only zero comparisons, so every comparison is already indexed by the zero threshold $\mu_0$ and the zero-compatible dyadic tolerance $\epsilon_0$. Repacking this ledger gives the constant zero $\RegSeqRatUp$ readback $R_0$.

The $\RealUp$ row $E_0$ is reached through that readback. The transport, continuation, package, and local naming rows are the trivial componentwise rows for the same displayed packet, so the classifier accepts $T_0$ without adding quotient streams, selected limits, host real equality, or ambient completeness.
\end{proof}

\begin{theorem}[DyadicTailBound tolerance ledger exhaustion]
\label{thm:dyadic-tail-bound-tolerance-ledger-exhaustion}
Let
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
be an accepted $\DyadicTailBoundUp$ carrier. Every accepted comparison consumed from $L$ is exhausted by the displayed tolerance row $\epsilon$, the displayed threshold row $\mu$, the $\RegSeqRatUp$ readback $R$, the componentwise $\hsame$ rows in $H$, the $\Cont$ routes in $C$, the package row $P$, and the local $\NameCert_{\DyadicTailBoundUp}$ row $N$. No accepted comparison in $L$ can be justified by an unrecorded tolerance, external metric estimate, host real equality, quotient stream, or selected limit.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_tolerance\_ledger\_exhaustion}
\begin{proof}
Project the ledger coordinate $L$ from the carrier. By \autoref{def:dyadic-tail-bound-carrier}, each inspected tail entry is paired with the same threshold coordinate $\mu$ and the same dyadic tolerance coordinate $\epsilon$ displayed in the packet.

Apply \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} to keep $L$ attached to the readback row $R$. The $\hsame$ rows in $H$ and the $\Cont$ routes in $C$ only transport and read these displayed coordinates.

The provenance rows $P$ and $N$ certify the dependency package and local naming surface for this finite packet. Since no other comparison coordinate exists, the ledger is exhausted by $\mu$, $\epsilon$, $R$, $H$, $C$, $P$, and $N$.
\end{proof}

\begin{theorem}[DyadicTailBound CauchyRate consumer route]
\label{thm:dyadic-tail-bound-cauchy-rate-consumer-route}
For every accepted $\DyadicTailBoundUp$ carrier
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
$$
every $\CauchyRateUp$ consumer read of $T$ factors through the precision row $p$, schedule $\mu$, dyadic tolerance $\epsilon$, ledger $L$, $\RegSeqRatUp$ readback $R$, $\RealUp$ seal $E$, and displayed rows $H$, $C$, $P$, and $N$.

The route exports no hidden rate selector, quotient stream, selected limit, host real equality, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_cauchy\_rate\_consumer\_route}
\begin{proof}
Project the accepted carrier. Its consumer-visible coordinates are exactly the precision row, schedule threshold, tolerance row, finite ledger, regular-sequence readback, real seal, transport rows, continuation routes, package provenance, and local naming row.

Use \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness}. Any $\CauchyRateUp$ read that consumes a regular rational rate must pass through $R$, and $R$ is obtained from the same schedule row $\mu$, dyadic tolerance $\epsilon$, and finite ledger $L$.

Apply \autoref{thm:dyadic-tail-bound-real-seal-factorization}. A real-facing consumer reaches $E$ only through the displayed $\RegSeqRatUp$ readback and the same tail-budget coordinates.

Use \autoref{thm:dyadic-tail-bound-ledger-threshold-exhaustion} and \autoref{thm:dyadic-tail-bound-tolerance-ledger-exhaustion}. These rows keep every consumed comparison tied to the threshold $\mu$, tolerance $\epsilon$, ledger $L$, transports $H$, continuations $C$, package row $P$, and local naming row $N$.

Since the carrier contains no hidden rate selector, quotient stream, selected limit, host equality, or ambient completeness field, every $\CauchyRateUp$ consumer route is exhausted by $p$, $\mu$, $\epsilon$, $L$, $R$, $E$, $H$, $C$, $P$, and $N$.
\end{proof}

\begin{theorem}[DyadicTailBound consumer obligation package]
\label{thm:dyadic-tail-bound-consumer-obligation-package}
The carrier of \autoref{def:dyadic-tail-bound-carrier}, classifier transport exactness, zero-tail carrier habitation, tolerance ledger exhaustion, $\RegSeqRatUp$ handoff exactness, and $\RealUp$ seal factorization assemble the $\DyadicTailBoundUp$ consumer obligation package over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The package exposes only the displayed precision row, schedule threshold, dyadic tolerance, finite ledger, regular-sequence readback, real seal, componentwise transport rows, continuation routes, package provenance, and local naming row. It does not export quotient streams, selected limits, host real equality, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_consumer\_obligation\_package}
\begin{proof}
Start with the finite carrier. It supplies the displayed rows $p,\mu,\epsilon,L,R,E,H,C,P,N$ and therefore fixes the consumer-visible packet shape.

Classifier transport is \autoref{thm:dyadic-tail-bound-classifier-transport-exactness}. The inhabited base case is \autoref{thm:dyadic-tail-bound-zero-tail-carrier-habitation}, and the ledger exactness row is \autoref{thm:dyadic-tail-bound-tolerance-ledger-exhaustion}.

The readback and real-facing rows are supplied by \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} and \autoref{thm:dyadic-tail-bound-real-seal-factorization}. These theorems force every consumer route through the finite ledger, the displayed tolerance and threshold, and the regular rational readback.

Assembling the listed rows gives the obligation package. Since each component is a projection or repacking of the same $\mathsf{BHist}$ packet, no extra carrier for quotient streams, selected limits, host real equality, or ambient completeness is introduced.
\end{proof}

\begin{theorem}[DyadicTailBound budget composition]
\label{thm:dyadic-tail-bound-budget-composition}
Let
$$
T_1=(p,\mu,\epsilon,L_1,R_1,E_1,H,C,P,N)
\quad\text{and}\quad
T_2=(p,\mu,\epsilon,L_2,R_2,E_2,H,C,P,N)
$$
be accepted $\DyadicTailBoundUp$ packets sharing the displayed precision row, schedule threshold, dyadic tolerance, componentwise $\hsame$ rows, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row. If the second displayed tail ledger refines or extends the first through those same rows, then the concatenated ledger $L_1L_2$ is an accepted $\DyadicTailBoundUp$ packet whose $\RegSeqRatUp$ handoff and $\RealUp$ seal factor through the composite displayed budget. The conclusion exports only the composed finite ledger surface and no quotient stream, selected limit, host real equality, or ambient completeness datum.
\end{theorem}
\begin{proof}
Begin with the two accepted carriers and project their common rows. The precision, threshold, tolerance, transport, continuation, package, and local naming coordinates agree by hypothesis, so the only displayed change is the refinement from $L_1$ to $L_2$ and the concatenated ledger $L_1L_2$.

Apply \autoref{thm:dyadic-tail-bound-ledger-append-stability} along the displayed refinement rows to accept each appended comparison with the same $\mu$, $\epsilon$, $H$, $C$, $P$, and $N$. Then use \autoref{thm:dyadic-tail-bound-ledger-threshold-exhaustion} to keep every comparison in the composite ledger tied to the displayed threshold and tolerance.

Classifier transport is controlled by \autoref{thm:dyadic-tail-bound-classifier-transport-exactness}. It prevents the composite packet from replacing the shared provenance by an unrecorded tolerance, external estimate, or separate ledger surface.

Finally apply \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness} and \autoref{thm:dyadic-tail-bound-real-seal-factorization}. The regular-sequence readback factors through the concatenated ledger, and the real-facing seal factors through that readback and the same composite displayed budget.
\end{proof}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_budget\_composition}

\begin{theorem}[DyadicTailBound schedule monotone consumer]
\label{thm:dyadic-tail-bound-schedule-monotone-consumer}
Let
$$
T=(p,\mu,\epsilon,L,R,E,H,C,P,N)
\quad\text{and}\quad
T'=(p',\mu',\epsilon,L',R',E',H,C,P,N)
$$
be accepted $\DyadicTailBoundUp$ packets sharing the displayed tolerance, componentwise $\hsame$ rows, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row. Suppose the displayed refinement from $p$ to $p'$ transports the schedule threshold from $\mu$ to $\mu'$ through the same rows $H$ and $C$, and suppose the refined ledger $L'$ is read as the carried tail ledger for $\mu'$ and $\epsilon$.

Then every $\RegSeqRatUp$ or $\RealUp$ consumer of $T'$ factors through the transported schedule row $\mu'$, the displayed dyadic tolerance row $\epsilon$, the finite ledger $L'$, and the shared transport, continuation, package, and naming rows. Such a consumer cannot reuse the old ledger through an unrecorded schedule, external radius, selected limit, quotient stream, host real equality, or ambient completeness datum.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_schedule\_monotone\_consumer}
\begin{proof}
Start with the accepted packet $T$ and apply \autoref{thm:dyadic-tail-bound-precision-refinement-stability}. The refinement of $p$ to $p'$ is not a free precision change; it transports the schedule, tolerance, ledger, readback, seal, package, and naming coordinates through the same $H$ and $C$ rows.

Next use \autoref{thm:dyadic-tail-bound-ledger-threshold-exhaustion}. Every comparison consumed from the refined ledger is exhausted by the transported threshold $\mu'$ and the displayed tolerance $\epsilon$, so the ledger cannot be replayed through a schedule row absent from the packet.

For a $\RegSeqRatUp$ consumer, apply \autoref{thm:dyadic-tail-bound-regseqrat-handoff-exactness}. The readback row $R'$ is obtained only by repacking $L'$, $\mu'$, and $\epsilon$ along the displayed transport and continuation routes.

For a $\RealUp$ consumer, apply \autoref{thm:dyadic-tail-bound-real-seal-factorization}. The seal row $E'$ is reached only through that regular-sequence readback and the same finite tail-budget data.

Finally use \autoref{thm:dyadic-tail-bound-budget-composition} to keep composed refinements on the displayed budget surface. Since the carrier has no coordinate for an external radius, selected limit, quotient stream, host real equality, or ambient completeness, every refined consumer route must carry the schedule row together with the ledger.
\end{proof}

\closureat{DyadicTailBoundUp}{seedStr}
\closureat{DyadicTailBoundUp}{obligationStr}
\closureat{DyadicTailBoundUp}{scopedStr}
\closureat{DyadicTailBoundUp}{publicStr}
\closureat{DyadicTailBoundUp}{bridgedStr}
\closureat{DyadicTailBoundUp}{matureStr}[BEDC.Derived.DyadicTailBoundUp.DyadicTailBoundCarrier\_budget\_composition]

\begin{closurestatus}{\DyadicTailBoundUp}
  \constructivestory{A $\DyadicTailBoundUp$ packet is generated from a unary precision row, a $\CauchyModulusUp$ threshold schedule, a $\DyadicRatCoreUp$ tolerance row, a finite tail comparison ledger, a $\RegSeqRatUp$ readback route, a $\RealUp$ seal-facing row, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature paper scope is the finite tail-budget interface through budget composition, ledger threshold exhaustion, the $\RegSeqRatUp$ handoff, $\RealUp$ seal factorization, classifier transport, consumer obligation packaging, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, ambient completeness, or bridge checking.}
  \upgradepath{Machine bridge verification requires a Lean bridge theorem for the same public tail-budget interface and downstream completion schema.}
\end{closurestatus}
