\chapter{A Concrete Naming Certificate for $\RegularCauchyLiminfUp$}
\label{ch:concrete-instances-regular-cauchy-liminf-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyLiminfUp$ packet records the finite lower-tail envelope available from a displayed regular Cauchy rational source before any Real-facing consumer reads a terminal value. It sits over the lower-envelope route of \autoref{ch:concrete-instances-liminf-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the upper-tail companion of \autoref{ch:concrete-instances-regular-cauchy-limsup-namecert}. The packet names an L10 completion-facing lower-tail envelope; it does not import host infimum, quotient stream equality, countable choice, selected subsequences, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy liminf carrier]
\label{def:regular-cauchy-liminf-carrier}
A $\RegularCauchyLiminfUp$ carrier is a finite $\BHist$ packet
$$
I=(S,W,R,D,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source sequence row, recording the displayed regular Cauchy rational name whose lower tail is being inspected.
\item $W$ is the $\StreamNameUp$ window row, listing the finite tail windows made visible to the lower-envelope request.
\item $R$ is the $\RegSeqRatUp$ tail readback row, binding the windows from $W$ to regular rational values and tail tolerances.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, carrying dyadic precision, slack, and comparison budgets for the lower-envelope read.
\item $L$ is the $\LiminfUp$ lower-envelope row, exposing the finite tail-lower surface available at the displayed precision.
\item $E$ is the $\RealUp$ seal row reached only after $S,W,R,D,L$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, windows, readback, dyadic ledger, lower-envelope, Real-seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a lower-tail request through
$$
S \longrightarrow W \longrightarrow R \longrightarrow D \longrightarrow L \longrightarrow E \longrightarrow H \longrightarrow P \longrightarrow N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLiminfUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\LiminfUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyLiminfUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,W,R,D,L,E,H,C,P,N$. It has no coordinate for quotient stream equality, a host-selected infimum value, a chosen cofinal subsequence, countable choice, or an ambient completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Regular Cauchy liminf NameCert obligations]
\label{thm:regular-cauchy-liminf-namecert-obligations}
For every accepted $\RegularCauchyLiminfUp$ carrier
$$
I=(S,W,R,D,L,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyLiminfUp}$ surface consists exactly of carrier admission for the displayed source sequence, $\StreamNameUp$ windows, $\RegSeqRatUp$ tail readback, $\DyadicRatCoreUp$ tolerance ledger, $\LiminfUp$ lower-envelope row, terminal $\RealUp$ seal, componentwise transport, displayed continuation replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:regular-cauchy-liminf-carrier}. The analytic rows $S,W,R,D,L,E$ display the source name, finite windows, regular rational readback, dyadic tolerance ledger, lower-envelope request, and terminal Real seal in that order.

The structural rows $H,C,P,N$ transport, replay, source, and name the same packet. They do not add a quotient stream, a selected cofinal subsequence, a host infimum, countable choice, or ambient Real completeness.

Since the classifier has no coordinate outside $S,W,R,D,L,E,H,C,P,N$, every local NameCert obligation is one of the listed rows, and every listed row is visible in the accepted carrier.
\end{proof}

\begin{theorem}[Regular Cauchy liminf Real seal route]
\label{thm:regular-cauchy-liminf-real-seal}
Every terminal $\RealUp$ seal of an accepted $\RegularCauchyLiminfUp$ carrier is reached through displayed regular-Cauchy lower-tail data. More precisely, every Real or Completion-facing read factors through the finite route
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \DyadicRatCoreUp \longrightarrow \LiminfUp \longrightarrow \RealUp ,
$$
with the same source sequence row $S$ and the same displayed tail windows $W$ governing the lower-envelope comparison.
\end{theorem}
\begin{proof}
Let a Real or Completion-facing consumer read an accepted carrier $I$. The terminal value is available only as the row $E$, and $E$ is downstream of the source row $S$, window row $W$, readback row $R$, dyadic tolerance ledger $D$, and lower-envelope row $L$.

Replay the continuation row $C$. The consumer first opens the finite $\StreamNameUp$ windows, then reads regular rational tail values through $\RegSeqRatUp$, and then consults the dyadic ledger before the lower-envelope row is available.

Componentwise transport $H$ and provenance $P$ preserve the same order. Therefore the Real seal cannot bypass the displayed regular-Cauchy tail rows or import a host infimum, quotient stream equality, countable choice, selected subsequence, or ambient completeness witness.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyLiminfUp$ Real-facing read exposes a source sequence, StreamName windows, RegSeqRat tail readbacks, a DyadicRatCore tolerance ledger, Liminf lower-envelope data, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before any lower-tail endpoint is visible.}

\independenceWitness{liminf, regular-cauchy-limsup, limsup-liminf-gap, streamname, regseqrat, dyadicratcore, real: $\RegularCauchyLiminfUp$ is not bijective to any listed sibling because it jointly carries a regular Cauchy source, finite windows, rational tail readback, dyadic tolerances, lower-envelope data, Real sealing, transport, replay, provenance, and local NameCert in one packet.}

\closureat{RegularCauchyLiminfUp}{seedStr}

\begin{closurestatus}{\RegularCauchyLiminfUp}
  \constructivestory{A $\RegularCauchyLiminfUp$ packet is generated from a regular Cauchy source sequence, StreamName windows, RegSeqRat tail readbacks, a DyadicRatCore tolerance ledger, a Liminf lower-envelope row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-liminf-carrier}, \autoref{thm:regular-cauchy-liminf-namecert-obligations}, and \autoref{thm:regular-cauchy-liminf-real-seal} seed the finite regular-Cauchy lower-tail envelope route through displayed windows, rational readback, dyadic tolerances, Liminf lower-envelope data, and terminal Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host infimum, quotient stream equality, selected subsequences, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window coverage, RegSeqRat readback, DyadicRatCore tolerance exposure, lower-envelope exactness, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyLiminfUp.}
\end{closurestatus}
