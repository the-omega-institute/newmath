\chapter{A Concrete Naming Certificate for $\PentaryHermitianRigidityUp$}
\label{ch:concrete-instances-pentary-hermitian-rigidity-namecert}
\origin{ai}

The $\PentaryHermitianRigidityUp$ packet realizes the pentary Hermitian rigidity target of \autoref{prin:innovation-input-pentary-hermitian-rigidity-target} as finite NameCert data. It packages a pentary phase-word source, the anchored three-link Hermitian defect rows, the two nonpalindromic coordinate rows, the finite-window RP boundary source of \autoref{ch:concrete-instances-finite-window-rp-boundary-namecert}, the golden-palindrome boundary of \autoref{ch:concrete-instances-golden-palindrome-boundary-namecert}, the innovation-input geometry boundary, the golden-triadic positive-kernel boundary, and the local transport, replay, provenance, and naming rows. The packet asserts only the finite rigidity certificate; it does not assert a global spectral theorem, a host positivity principle, an infinite RP classifier, or a completed TasteGate proof.

\begin{definition}[PentaryHermitianRigidity carrier]
\label{def:pentary-hermitian-rigidity-carrier}
A $\PentaryHermitianRigidityUp$ carrier is a finite $\BHist$ packet
$$
P=(Q,V,D,R,G,B,K,T,H,C,L,N)
$$
with the following rows:
\begin{enumerate}
\item $Q$ is the pentary source row naming the admitted finite $\mathcal Q_5$ phase-word window.
\item $V$ is the accepted-coordinate row, recording the displayed pentary coordinates under the local classifier.
\item $D$ is the anchored three-link Hermitian defect row, exposing the finite entries of $\mathcal D_3(v)$.
\item $R$ is the nonpalindromic-coordinate row, exposing $R_0(v)$ and $R_1(v)$ as the only obstruction coordinates.
\item $G$ is the golden-palindrome boundary row, connecting the accepted packet to the finite palindrome boundary surface.
\item $B$ is the finite-window RP boundary row, recording the local RP boundary data consumed by the rigidity read.
\item $K$ is the golden-triadic positive-kernel row, recording the finite positive-kernel comparison data used by the boundary check.
\item $T$ is the TasteGate follow-up row, naming the future Lean obligations $\texttt{Nontrivial PentaryHermitianRigidityUp}$ and $\texttt{FieldFaithful PentaryHermitianRigidityUp}$ in $\texttt{TasteGate.lean}$.
\item $H$ records componentwise $\hsame$ transport for the source, accepted-coordinate, defect, obstruction, boundary, kernel, follow-up, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from rigidity consumers through the same finite rows.
\item $L$ is the $\Pkg$ provenance over $\PentaryHermitianRigidityUp$, $\FiniteWindowRpBoundaryUp$, $\GoldenPalindromeBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PentaryHermitianRigidityUp}$ row.
\end{enumerate}
The classifier has no coordinate for a nonpalindromic accepted packet with zero displayed Hermitian defect, a hidden positivity theorem, an infinite symbolic-dynamics object, ambient spectral completeness, or bridge checking.
\end{definition}

\begin{theorem}[PentaryHermitianRigidity obligation surface]
\label{thm:pentary-hermitian-rigidity-obligation-surface}
For every accepted $\PentaryHermitianRigidityUp$ carrier
$$
P=(Q,V,D,R,G,B,K,T,H,C,L,N),
$$
the local $\NameCert_{\PentaryHermitianRigidityUp}$ surface consists exactly of carrier admission for the pentary source row, accepted-coordinate row, displayed Hermitian defect row, nonpalindromic-coordinate obstruction row, golden-palindrome boundary row, finite-window RP boundary row, golden-triadic positive-kernel row, TasteGate follow-up row, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pentary-hermitian-rigidity-carrier}. The finite algebraic part of the packet is displayed by $Q,V,D,R$: the source window, accepted coordinates, Hermitian defect entries, and nonpalindromic obstruction coordinates.

The boundary rows are also explicit. The rows $G,B,K$ connect the same accepted packet to the golden-palindrome boundary, finite-window RP boundary, and golden-triadic positive-kernel data without replacing them by an ambient positivity theorem.

Finally $T,H,C,L,N$ record the TasteGate follow-up, transport, replay, provenance, and naming obligations. Since no excluded nonpalindromic zero-defect packet, hidden positivity theorem, infinite symbolic-dynamics object, spectral completeness principle, or bridge certificate appears as a row, the listed obligations exhaust the NameCert surface.
\end{proof}

\begin{theorem}[PentaryHermitianRigidity obstruction nonescape]
\label{thm:pentary-hermitian-rigidity-obstruction-nonescape}
Let $P=(Q,V,D,R,G,B,K,T,H,C,L,N)$ be an accepted $\PentaryHermitianRigidityUp$ carrier. Any consumer that accepts a pentary packet with zero displayed Hermitian defect must still read the nonpalindromic-coordinate row $R$ before exporting the packet. Thus a nonpalindromic accepted packet with zero displayed Hermitian defect is outside the carrier surface unless it refutes the certificate through the displayed rows.
\end{theorem}
\begin{proof}
Begin with the accepted carrier. The zero-defect request reads $D$, but $D$ is not exported alone; the carrier places it next to the obstruction row $R$ and the accepted-coordinate row $V$.

The boundary rows $G,B,K$ preserve this pairing. They can be consumed only after the accepted coordinates, defect entries, and obstruction coordinates have been displayed in the same finite packet.

The structural rows $H,C,L,N$ replay the same dependency route. Therefore a consumer cannot erase $R$ while keeping an accepted zero-defect read, and any nonpalindromic zero-defect packet must appear as a visible refutation rather than as hidden accepted data.
\end{proof}

\falsifiablePrediction{A nonpalindromic accepted packet with zero displayed Hermitian defect refutes the $\PentaryHermitianRigidityUp$ certificate.}

\independenceWitness{finite-window-rp-boundary, golden-palindrome-boundary, innovation-input-geometry-boundary, golden-triadic-positive-kernel: no listed sibling is bijective to $\PentaryHermitianRigidityUp$ because this packet jointly binds pentary coordinates, three-link Hermitian defect rows, nonpalindromic obstruction coordinates, the three boundary sources, TasteGate follow-up, transport, replay, provenance, and local NameCert naming in one carrier.}

\closureat{PentaryHermitianRigidityUp}{seedStr}
\begin{closurestatus}{\PentaryHermitianRigidityUp}
  \constructivestory{A $\PentaryHermitianRigidityUp$ packet is generated from pentary source data, accepted coordinates, displayed Hermitian defect rows, nonpalindromic obstruction coordinates, golden-palindrome and finite-window RP boundary rows, golden-triadic positive-kernel data, TasteGate follow-up, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pentary-hermitian-rigidity-carrier}, \autoref{thm:pentary-hermitian-rigidity-obligation-surface}, and \autoref{thm:pentary-hermitian-rigidity-obstruction-nonescape} seed the finite pentary Hermitian rigidity certificate.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a global spectral theorem, host positivity, an infinite RP classifier, ambient spectral completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, defect-row exactness, nonpalindromic obstruction nonescape, boundary provenance, TasteGate nontriviality, field-faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.PentaryHermitianRigidityUp.}
\end{closurestatus}
