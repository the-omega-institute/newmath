\chapter{A Concrete Naming Certificate for $\LocatedTotalBoundednessUp$}
\label{ch:concrete-instances-located-total-boundedness-namecert}
\origin{human}

Following Bishop-style constructive metric compactness, the $\LocatedTotalBoundednessUp$ packet records total boundedness with located finite-net selection data rather than with open-cover compactness, a host compactness theorem, or a choice-selected net. It sits over the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the total-boundedness packet of \autoref{ch:concrete-instances-totallybounded-namecert}, the finite $\varepsilon$-net interface of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. A consumer sees explicit finite centers, tolerance windows, and location rows; it does not receive an unbounded cover family or a hidden selector.

\begin{definition}[LocatedTotalBoundedness carrier]
\label{def:located-total-boundedness-carrier}
A $\LocatedTotalBoundednessUp$ carrier is a finite $\BHist$ packet
$$
L=(X,T,F,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the ambient $\MetricUp$ point and distance row for the requested finite window.
\item $T$ is the $\TotallyBoundedUp$ coverage row, naming the displayed tolerance and the finite family of candidate centers.
\item $F$ is the $\FiniteEpsilonNetUp$ row whose entries list the actual finite net centers and their coverage ledger.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare the requested radius, center distances, and coverage slack.
\item $S$ is the $\StreamNameUp$ finite-observation row recording which center and point windows are opened.
\item $R$ is the $\RegSeqRatUp$ readback row for the displayed distance and radius comparisons.
\item $E$ is the $\RealUp$ seal row for the distance comparisons after $D,S,R$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for metric data, total-boundedness data, finite-net data, dyadic tolerances, stream windows, regular readback, Real sealing, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay for the same finite route.
\item $P$ is the $\Pkg$ provenance over $\LocatedTotalBoundednessUp$, $\MetricUp$, $\TotallyBoundedUp$, $\FiniteEpsilonNetUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedTotalBoundednessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,T,F,D,S,R,E,H,C,P,N$. There is no coordinate for open-cover compactness, a host compactness theorem, an unrecorded center selector, quotient stream equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[LocatedTotalBoundedness NameCert obligations]
\label{thm:located-total-boundedness-namecert-obligations}
For every accepted $\LocatedTotalBoundednessUp$ carrier
$$
L=(X,T,F,D,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedTotalBoundednessUp}$ surface is exhausted by carrier exposure, total-bounded coverage, finite-net center listing, dyadic tolerance exactness, stream-window observation, regular rational readback, Real-seal nonescape, componentwise transport, displayed replay, package provenance, and local naming. The certificate refuses open-cover compactness, host compactness, hidden center selection, quotient stream equality, countable choice, and ambient completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only ambient metric data are $X$, and the only total-bounded coverage data are the displayed rows $T$ and $F$.

The dyadic and stream rows $D$ and $S$ fix the finite tolerance windows used to test the listed centers. The regular readback row $R$ records those comparisons before the terminal real seal $E$ is available.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no open-cover, host-compactness, selector, quotient, countable-choice, or ambient-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[LocatedTotalBoundedness finite-net location route]
\label{thm:located-total-boundedness-finite-net-location-route}
Every compact-metric or completion-facing consumer of an accepted $\LocatedTotalBoundednessUp$ carrier must read located finite-net data through the route
$$
X,T,F \longmapsto D,S \longmapsto R \longmapsto E,H,C,P,N .
$$
Thus a finite-net location claim is available only after the Metric row, TotallyBounded row, FiniteEpsilonNet row, DyadicRatCore tolerance row, StreamName observation row, RegSeqRat readback row, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row have all been displayed.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:located-total-boundedness-carrier}. The rows $X,T,F$ expose the metric source, the total-bounded coverage request, and the finite list of net centers.

The rows $D$ and $S$ then determine the dyadic tolerances and finite observation windows used to locate a center for the request. The regular readback row $R$ turns these finite comparisons into the rational data consumed by the downstream Real seal $E$.

The structural rows $H,C,P,N$ preserve this same route under transport, replay, provenance, and local naming. A compact-metric or completion-facing consumer therefore cannot replace the displayed finite-net route by open-cover compactness, host compactness, hidden selection, quotient stream equality, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedTotalBoundednessUp$ compact-metric or completion-facing read exposes a Metric row, a TotallyBounded coverage row, a FiniteEpsilonNet center row, a DyadicRatCore tolerance row, a StreamName observation row, a RegSeqRat readback row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before it may use located finite-net data.}

\independenceWitness{metric, totallybounded, finiteepsilonnet, dyadicratcore, streamname, regseqrat, real: $\LocatedTotalBoundednessUp$ is not bijective to any listed sibling because it jointly carries metric source data, total-bounded coverage, located finite-net centers, dyadic tolerance comparison, stream observation, regular rational readback, Real sealing, transport, replay, provenance, and local naming as one finite compactness-facing certificate.}

\closureat{LocatedTotalBoundednessUp}{seedStr}

\begin{closurestatus}{\LocatedTotalBoundednessUp}
  \constructivestory{A $\LocatedTotalBoundednessUp$ packet is generated from a Metric row, TotallyBounded coverage row, FiniteEpsilonNet center row, DyadicRatCore tolerance row, StreamName observation row, RegSeqRat readback row, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-total-boundedness-carrier}, \autoref{thm:located-total-boundedness-namecert-obligations}, and \autoref{thm:located-total-boundedness-finite-net-location-route} seed the located finite-net route for compact-metric and completion-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, host compactness, hidden center selection, quotient stream equality, countable choice, ambient real completeness, Lean verification, or bridge checking.}
  \upgradepath{Formal verification requires a Lean carrier and checked NameCert obligations for BEDC.Derived.LocatedTotalBoundednessUp.}
\end{closurestatus}
