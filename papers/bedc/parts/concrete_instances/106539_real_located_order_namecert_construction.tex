\chapter{A Concrete Naming Certificate for $\RealLocatedOrderUp$}
\label{ch:concrete-instances-real-located-order-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RealLocatedOrderUp$ packet names located comparison for two displayed real names by reading only regular Cauchy data, finite stream windows, dyadic tolerance budgets, a located comparison row, and an apartness handoff before any completion-facing consumer sees an order claim. It consumes the terminal $\RealUp$ seals of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the positive-separation boundary of \autoref{ch:concrete-instances-apartnessreal-namecert}, and the finite located-order test of \autoref{ch:concrete-instances-cauchy-real-located-order-test-namecert}. The packet is a public constructive located-order certificate over $\RealUp$ names; it is not host trichotomy, host real equality, quotient stream equality, selected limits, countable choice, or ambient real completeness.

\begin{definition}[Real located-order carrier]
\label{def:real-located-order-carrier}
A $\RealLocatedOrderUp$ carrier is a finite $\BHist$ packet
$$
L=(X,Y,R,S,D,O,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two $\RealUp$ seal rows whose order relation is being read.
\item $R$ stores the paired $\RegSeqRatUp$ readbacks for the two real names.
\item $S$ stores the shared $\StreamNameUp$ finite windows used to inspect the paired approximants.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger that fixes the visible precision budget.
\item $O$ is the located comparison row imported from the $\CauchyRealLocatedOrderTestUp$ surface.
\item $A$ is the $\ApartnessRealUp$ handoff row, present only when the comparison exposes positive separation.
\item $H$ stores componentwise $\hsame$ transport for the two real seals, readbacks, windows, dyadic tolerances, located comparison, apartness handoff, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from Real-name reads through regular Cauchy readbacks, stream windows, dyadic tolerances, located comparison, and apartness handoff.
\item $P$ is the $\Pkg$ provenance over $\RealLocatedOrderUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyRealLocatedOrderTestUp$, $\ApartnessRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealLocatedOrderUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,R,S,D,O,A,H,C,P,N$. It has no coordinate for host trichotomy, host real equality, quotient stream equality, selected limits, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Real located-order NameCert obligations]
\label{thm:real-located-order-namecert-obligations}
For every accepted $\RealLocatedOrderUp$ carrier $L=(X,Y,R,S,D,O,A,H,C,P,N)$, the local $\NameCert_{\RealLocatedOrderUp}$ surface consists exactly of carrier admission for the two $\RealUp$ seals, paired $\RegSeqRatUp$ readbacks, shared $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance budget, located comparison row, optional $\ApartnessRealUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-located-order-carrier}. The two real names are visible only as the seal rows $X,Y$, and their numerical reads pass through the paired regular rational row $R$.

The finite windows $S$ and dyadic tolerance ledger $D$ are then the only inspection surface available to the located comparison row $O$. Thus the local NameCert reads order evidence as the displayed CauchyRealLocatedOrderTest route rather than as a host order theorem.

Finally read $A,H,C,P,N$. The apartness row is a handoff when positive separation is displayed, while the structural rows transport, replay, source, and name the same finite route. Since no remaining coordinate stores trichotomy, host equality, quotient stream equality, selected limits, choice, or ambient completeness, the listed rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[Real located-order apartness handoff]
\label{thm:real-located-order-apartness-handoff}
For every accepted $\RealLocatedOrderUp$ carrier $L=(X,Y,R,S,D,O,A,H,C,P,N)$, an apartness-facing consumer may read positive separation only through the route
$$
X,Y\longmapsto R\longmapsto S\longmapsto D\longmapsto O\longmapsto A
$$
together with $H,C,P,N$. The handoff exports an $\ApartnessRealUp$ row when the located comparison displays a positive gap, but it exports no host real equality test, quotient-stream equality, trichotomy, selected limit, countable-choice selector, or ambient completeness witness.
\end{theorem}
\begin{proof}
Start with the located comparison row in \autoref{def:real-located-order-carrier}. The row $O$ is downstream of the two Real seals, paired regular readbacks, finite windows, and dyadic tolerance ledger.

Use the apartness clause of the carrier. The row $A$ is not an independent equality or order oracle; it is the ApartnessReal handoff available only when the displayed located comparison has exposed positive separation.

The structural rows $H,C,P,N$ preserve this same packet route. Therefore a consumer can export the apartness handoff, but cannot bypass the displayed rows to obtain host equality, quotient stream equality, trichotomy, selected limits, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RealLocatedOrderUp$ consumer must exhibit two RealUp seals, paired RegSeqRat readbacks, StreamName finite windows, a DyadicRatCore tolerance budget, a CauchyRealLocatedOrderTest row, an ApartnessReal handoff when positive separation is claimed, componentwise transport, displayed replay, provenance, and local naming.}

\independenceWitness{real, regseqrat, streamname, dyadicratcore, apartnessreal, cauchy-real-located-order-test: no listed sibling carries the whole packet $L=(X,Y,R,S,D,O,A,H,C,P,N)$ because each sibling supplies only real sealing, rational readback, finite windows, tolerance budgets, apartness handoff, or located comparison while this chapter binds all rows as one Real-facing located-order certificate.}

\closureat{RealLocatedOrderUp}{seedStr}

\begin{closurestatus}{\RealLocatedOrderUp}
  \constructivestory{A $\RealLocatedOrderUp$ packet is generated from two RealUp seals, paired RegSeqRat readbacks, StreamName finite windows, a DyadicRatCore tolerance budget, a CauchyRealLocatedOrderTest located comparison row, an ApartnessReal handoff when positive separation is displayed, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-located-order-carrier}, \autoref{thm:real-located-order-namecert-obligations}, and \autoref{thm:real-located-order-apartness-handoff} seed the finite constructive located-order certificate over two displayed RealUp names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, host real equality, quotient stream equality, selected limits, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, NameCert obligations, apartness handoff, and TasteGate field faithfulness for BEDC.Derived.RealLocatedOrderUp.}
\end{closurestatus}
