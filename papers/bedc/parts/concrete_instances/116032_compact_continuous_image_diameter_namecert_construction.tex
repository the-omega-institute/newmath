\chapter{A Concrete Naming Certificate for $\CompactContinuousImageDiameterUp$}
\label{ch:concrete-instances-compact-continuous-image-diameter-namecert}
\origin{human}

Following Heine--Cantor's compact finite-net proof in the compact-uniform-continuity vision of \autoref{ch:visions-compact-uniform-continuity}, the $\CompactContinuousImageDiameterUp$ packet records the finite image-diameter consumer that reads compact nets, pointwise continuity rows, a uniform modulus, metric-distance comparisons, and a terminal real seal before exporting a bounded-image estimate. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map row of \autoref{ch:concrete-instances-continuousmap-namecert}, the metric-distance carrier of \autoref{ch:concrete-instances-metric-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite image-diameter read only; it does not choose a host finite subcover, select a global modulus, quotient functions, invoke countable choice, or import ambient $\RealUp$ completeness.

\begin{definition}[Compact continuous image-diameter carrier]
\label{def:compact-continuous-image-diameter-carrier}
A $\CompactContinuousImageDiameterUp$ carrier is a finite $\BHist$ packet
$$
G=(K,F,U,M,A,D,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, including the finite net windows inspected by the packet.
\item $F$ is the $\ContinuousMapUp$ row, exposing the displayed function graph and pointwise continuity reads on those net windows.
\item $U$ is the $\UniformModulusUp$ row consumed after compact-net and pointwise-continuity data have been displayed.
\item $M$ is the $\MetricUp$ distance row for target-space diameter comparisons.
\item $A$ is the finite image-sample ledger, recording the displayed net centers, their image reads, and the pairwise distance windows inspected by the packet.
\item $D$ is the image-diameter bound row, recording the rational or real-sealed upper diameter requested by downstream compact-image consumers.
\item $R$ is the $\RealUp$ seal boundary for the numeric diameter and distance comparisons.
\item $H$ records componentwise $\hsame$ transport for compact source, continuous map, uniform modulus, metric distance, image samples, diameter bound, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-net requests through pointwise map reads, uniform-modulus routing, metric-distance comparison, image-diameter bounding, and real sealing.
\item $P$ records $\Pkg$ provenance over $\CompactContinuousImageDiameterUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\MetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactContinuousImageDiameterUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,U,M,A,D,R,H,C,P,N$. It has no coordinate for a selected host subcover, a chosen global modulus outside the displayed row, quotient equality of functions, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact continuous image-diameter NameCert obligations]
\label{thm:compact-continuous-image-diameter-namecert-obligations}
For every accepted $\CompactContinuousImageDiameterUp$ carrier $G=(K,F,U,M,A,D,R,H,C,P,N)$, the local $\NameCert_{\CompactContinuousImageDiameterUp}$ surface consists exactly of compact finite-net exposure, continuous-map exposure, uniform-modulus visibility, metric-distance comparison, finite image-sample ledger visibility, image-diameter bound separation, $\RealUp$ seal routing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-continuous-image-diameter-carrier}. The compact source row $K$ and continuous-map row $F$ expose the finite net windows and their image reads before a diameter bound is visible.

Read the modulus and metric rows next. The row $U$ is the uniform-modulus consumer produced from the compact finite-net route, and $M$ is the metric-distance row used to compare displayed image samples.

The rows $A,D,R$ package the finite image ledger, the diameter upper-bound row, and the terminal real seal. They are downstream of $K,F,U,M$, so the packet cannot replace the finite image read by a host compactness theorem.

Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same rows. Since the carrier has no other coordinate, the NameCert obligations are exactly the displayed finite-net, modulus, metric, image-diameter, and real-seal obligations.
\end{proof}

\begin{theorem}[Compact continuous image-diameter uniform bound]
\label{thm:compact-continuous-image-diameter-uniform-bound}
Every bounded-image consumer of an accepted $\CompactContinuousImageDiameterUp$ packet must first read the finite route
$$
\CompactMetricUp \longmapsto \ContinuousMapUp \longmapsto \UniformModulusUp \longmapsto \MetricUp \longmapsto \RealUp .
$$
Only after that route is displayed may the consumer inspect the finite image-sample ledger and the image-diameter bound row. The handoff exports a reusable compact-image diameter estimate without selected finite subcovers, hidden global moduli, quotient function equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the compact metric source. The row $K$ supplies only displayed finite net windows, so a consumer receives a finite compact source rather than an open-cover compactness oracle.

Use the continuous-map and uniform-modulus rows. The image reads in $F$ are tied to the displayed net windows, and $U$ gives the tolerance route consumed by the diameter comparison.

Apply the target metric and real seal. The metric row $M$ compares the displayed image samples, while $D$ and $R$ record the diameter bound and its real-name boundary.

The structural rows $H,C,P,N$ then preserve this finite route for downstream compact-image consumers. No selected subcover, host modulus, quotient function equality, countable-choice selector, or ambient real-completeness principle is introduced.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactContinuousImageDiameterUp$ read displays one CompactMetricUp finite-net source, one ContinuousMapUp image-read row, one UniformModulusUp row, one MetricUp distance row, one finite image-sample ledger, one image-diameter bound row, one RealUp seal, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, metric, real: none is bijective with $\CompactContinuousImageDiameterUp$ because the packet jointly stores compact finite-net windows, continuous image reads, uniform-modulus routing, metric-distance comparisons, finite image samples, a diameter bound, Real seal, transport, replay, provenance, and local naming.}

\closureat{CompactContinuousImageDiameterUp}{seedStr}

\begin{closurestatus}{\CompactContinuousImageDiameterUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-continuous-image-diameter-carrier}, \autoref{thm:compact-continuous-image-diameter-namecert-obligations}, and \autoref{thm:compact-continuous-image-diameter-uniform-bound} seed the finite compact-image diameter packet over compact-net, continuous-map, uniform-modulus, metric-distance, image-ledger, bound, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected finite subcovers, hidden global moduli, quotient equality of functions, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact-source exposure, continuous-map exposure, uniform-modulus visibility, metric-distance comparison, image-sample ledger visibility, image-diameter bound separation, Real seal routing, transport, replay, provenance, and local naming.}
  \constructivestory{A $\CompactContinuousImageDiameterUp$ packet is generated from CompactMetricUp finite-net rows, ContinuousMapUp image reads, UniformModulusUp rows, MetricUp distance rows, a finite image-sample ledger, an image-diameter bound, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
