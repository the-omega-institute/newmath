\chapter{A Concrete Naming Certificate for $\DyadicCauchyApartnessUp$}
\label{ch:concrete-instances-dyadic-cauchy-apartness-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\DyadicCauchyApartnessUp$ packet records a positive separation witness between regular Cauchy real names as finite BEDC NameCert data. It sits over the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the located real apartness boundary of \autoref{ch:concrete-instances-located-real-apartness-namecert}. The packet supplies a constructive apartness boundary for Real and Completion consumers without importing quotient stream equality, trichotomy, selected representatives, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Dyadic Cauchy apartness carrier]
\label{def:dyadic-cauchy-apartness-carrier}
A $\DyadicCauchyApartnessUp$ carrier is a finite $\BHist$ packet
$$
K=(X,Y,W_X,W_Y,D,R_X,R_Y,A,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two regular Cauchy name rows whose observations are read only through displayed finite windows.
\item $W_X$ and $W_Y$ are the $\StreamNameUp$ window rows inspected for the two names at the separating precision.
\item $D$ is the $\DyadicRatCoreUp$ positive-bound row; it records the dyadic radius and strict positive gap used by the apartness witness.
\item $R_X$ and $R_Y$ are the $\RegSeqRatUp$ readback rows obtained from the two stream windows and the dyadic tolerance ledger.
\item $A$ is the $\LocatedRealApartnessUp$ handoff row reading the displayed positive gap as located apartness evidence.
\item $E$ is the terminal $\RealUp$ seal row reached only after the two readbacks and the positive dyadic gap have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two names, windows, dyadic gap, readbacks, apartness handoff, Real seal, replay, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from a precision request through $W_X,W_Y,D,R_X,R_Y,A,E$.
\item $P$ is the $\Pkg$ provenance over $\DyadicCauchyApartnessUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedRealApartnessUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicCauchyApartnessUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these finite rows. It has no coordinate for quotient streams, a selected limit, real trichotomy, host real equality, or an ambient completeness principle.
\end{definition}

\begin{theorem}[Dyadic Cauchy apartness NameCert obligations]
\label{thm:dyadic-cauchy-apartness-namecert-obligations}
For every accepted $\DyadicCauchyApartnessUp$ carrier
$$
K=(X,Y,W_X,W_Y,D,R_X,R_Y,A,E,H,C,P,N),
$$
the local $\NameCert_{\DyadicCauchyApartnessUp}$ surface consists exactly of carrier admission for the two regular Cauchy names, paired $\StreamNameUp$ window admission, $\DyadicRatCoreUp$ positive-gap exactness, $\RegSeqRatUp$ readback stability, $\LocatedRealApartnessUp$ handoff compatibility, $\RealUp$ seal ordering, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-cauchy-apartness-carrier}. The analytic rows are the two names $X,Y$, the two finite windows $W_X,W_Y$, the positive dyadic row $D$, the two readbacks $R_X,R_Y$, the apartness handoff $A$, and the Real seal $E$.

The positive witness is forced through $D$. Since $D$ is the only row carrying a strict dyadic gap, no apartness consumer can read a separation assertion before the finite windows and dyadic positive-bound data have been displayed.

The regular readbacks $R_X,R_Y$ attach the two inspected windows to the regular rational source surface, and $A$ reads exactly that displayed gap as located real apartness evidence. The Real seal $E$ is downstream of those rows rather than a source of equality or trichotomy.

Finally $H,C,P,N$ transport, replay, source, and name the same route. No further coordinate is present in the carrier, so the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Dyadic Cauchy apartness located-comparison boundary]
\label{thm:dyadic-cauchy-apartness-located-comparison-boundary}
Every Real or Completion consumer of an accepted $\DyadicCauchyApartnessUp$ carrier must read the finite route
$$
\StreamNameUp,\StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp,\RegSeqRatUp \longmapsto \LocatedRealApartnessUp \longmapsto \RealUp .
$$
The consumer may use the displayed positive dyadic gap as located apartness evidence, but it cannot extract quotient stream equality, trichotomy, selected representatives, countable choice, host real equality, or ambient $\RealUp$ completeness from the packet.
\end{theorem}
\begin{proof}
Begin with a Real or Completion consumer of the accepted packet. The carrier exposes no comparison row before the two finite stream windows and the positive dyadic ledger have been read.

The consumer then obtains $R_X$ and $R_Y$ from the displayed windows. These rows keep the comparison attached to regular rational readback rather than to a quotient of completed streams.

Next the row $A$ turns the same positive dyadic separation into located apartness evidence, and the row $E$ seals the route for Real-facing consumers. The seal is therefore reached only after the window, gap, and readback rows have been displayed.

The structural rows $H,C,P,N$ preserve this ordering under transport, replay, provenance, and local naming. Since the carrier has no row for the excluded host objects, the consumer receives only the stated finite located-comparison boundary.
\end{proof}

\begin{theorem}[Dyadic Cauchy apartness window induction]
\label{thm:dyadic-cauchy-apartness-window-induction}
Let
$$
K=(X,Y,W_X,W_Y,D,R_X,R_Y,A,E,H,C,P,N)
$$
be an accepted $\DyadicCauchyApartnessUp$ carrier. If the paired finite windows $W_X,W_Y$ are extended by one displayed $\StreamNameUp$ observation and the positive dyadic gap row $D$ transports through the corresponding $\RegSeqRatUp$ readbacks $R_X,R_Y$, then the same $\DyadicCauchyApartnessUp$ packet remains accepted and the located-apartness handoff still reaches the $\RealUp$ seal row $E$.
\end{theorem}
\begin{proof}
Proceed by finite induction on the displayed paired window extension. The base case is the accepted carrier itself, where $W_X,W_Y,D,R_X,R_Y,A,E$ already expose the positive dyadic gap, the two regular readbacks, the located-apartness handoff, and the Real seal.

For the successor step, append one displayed $\StreamNameUp$ observation to each of $W_X$ and $W_Y$. The transported positive dyadic gap remains the row $D$, and its two readbacks remain the corresponding $R_X$ and $R_Y$ rows after the window extension.

The handoff row $A$ reads that same transported positive gap as located apartness evidence. Since the extension changes only displayed finite windows and their readbacks, $A$ still feeds the terminal seal row $E$.

Finally $H,C,P,N$ transport, replay, source, and name the enlarged finite route. Thus the accepted packet is stable under the displayed one-step window extension, and the Real-facing consumer still receives the located-apartness handoff only through $E$.
\end{proof}

\closureat{DyadicCauchyApartnessUp}{seedStr}

\begin{closurestatus}{\DyadicCauchyApartnessUp}
  \constructivestory{A $\DyadicCauchyApartnessUp$ packet is generated from two regular Cauchy name rows, paired StreamName windows, a DyadicRatCore positive-gap row, RegSeqRat readbacks, a LocatedRealApartness handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-cauchy-apartness-carrier}, \autoref{thm:dyadic-cauchy-apartness-namecert-obligations}, \autoref{thm:dyadic-cauchy-apartness-located-comparison-boundary}, and \autoref{thm:dyadic-cauchy-apartness-window-induction} seed the finite constructive apartness boundary over dyadic positive gaps, stream windows, regular rational readbacks, located apartness, window extension, and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, trichotomy, selected representatives, countable choice, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped treatment asks for checked carrier admission, dyadic positive-gap exactness, paired window readback, located-apartness compatibility, Real-seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
