\chapter{A Concrete Naming Certificate for $\HausdorffMetricUp$}
\label{ch:concrete-instances-hausdorffmetric-namecert}
\origin{human}

Following Hausdorff and Blaschke, the $\HausdorffMetricUp$ packet records the finite distance interface between compact located subsets of a metric space. It consumes the ambient metric surface of \autoref{ch:concrete-instances-metric-namecert}, compact finite-net data of \autoref{ch:concrete-instances-compactmetric-namecert}, the located-distance interface of \autoref{ch:concrete-instances-locatedmetric-namecert}, the positive rational ball discipline of \autoref{ch:concrete-instances-rational-ball-namecert}, the separated completion boundary of \autoref{ch:concrete-instances-hausdorffcompletion-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The carrier names finite directed cover-distance ledgers rather than host hyperspaces, quotient subsets, selected closest points, or ambient completeness.

\begin{definition}[Hausdorff metric carrier]
\label{def:hausdorff-metric-carrier}
A $\HausdorffMetricUp$ carrier is a finite $\BHist$ packet
$$
H=(X,A,B,R,D,T,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ is the ambient $\MetricUp$ row, including point classifiers, distance witnesses, triangle ledgers, and observation histories.
\item $A$ and $B$ are compact located subset rows over the same ambient metric route. Each row exposes compact finite-net data through $\CompactMetricUp$ and located-distance data through $\LocatedMetricUp$.
\item $R$ is the positive rational tolerance row read through the $\RationalBallUp$ interface.
\item $D$ contains two directed finite cover-distance ledgers: one from $A$ to $B$ and one from $B$ to $A$. Each ledger records the finite net cells, located-distance comparisons, rational tolerance windows, and $\RealUp$ readback route used for the directed bound.
\item $T$ records triangle handoff rows tying ambient metric triangles in $X$ to the two directed ledgers in $D$ and to the separated boundary named by $\HausdorffCompletionUp$.
\item $C$ records $\Cont$ replay from ambient metric observations to compact located rows, from positive rational tolerance requests to directed cover-distance ledgers, and from the two directed ledgers to the final real distance seal.
\item $P$ is the $\Pkg$ provenance over $\HausdorffMetricUp$, $\MetricUp$, $\CompactMetricUp$, $\LocatedMetricUp$, $\RationalBallUp$, $\HausdorffCompletionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HausdorffMetricUp}$ row.
\end{enumerate}
The carrier compares compact located subset packets only through the displayed tuple $X,A,B,R,D,T,C,P,N$. It has no coordinate for open-cover compactness, a hyperspace compactness theorem, quotient subset equality, selected closest points, host suprema, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[HausdorffMetric NameCert obligations]
\label{thm:hausdorff-metric-namecert-obligations}
For an accepted $\HausdorffMetricUp$ carrier $H=(X,A,B,R,D,T,C,P,N)$, the local $\NameCert_{\HausdorffMetricUp}$ obligations are exactly carrier admission, shared ambient metric compatibility, compact located subset compatibility for both sides, positive rational tolerance readback, two directed cover-distance ledgers, triangle handoff stability, real seal non-escape, and ledger exactness. These obligations are discharged by the displayed rows
$$
X,\quad A,\quad B,\quad R,\quad D,\quad T,\quad C,\quad P,\quad N .
$$
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:hausdorff-metric-carrier}. The tuple displays the ambient metric row, the two compact located subset rows, the positive rational tolerance row, the directed cover-distance ledgers, triangle handoff rows, continuation replay, provenance, and local naming row.

Shared ambient metric compatibility reads $X$ before either subset ledger is inspected. The compact located subset obligations then read $A$ and $B$ through the $\CompactMetricUp$ and $\LocatedMetricUp$ rows attached to the same ambient metric route.

The tolerance and distance obligations factor through $R$ and $D$. The positive rational request is read by the $\RationalBallUp$ row, and the two directed ledgers record finite cover-distance comparisons in both directions before any $\RealUp$ seal is exported.

Triangle handoff stability is the role of $T$. It reuses only the ambient metric triangle rows in $X$, the directed ledgers in $D$, and the separated boundary made visible through $\HausdorffCompletionUp$.

Finally, $C,P,N$ replay and name the same finite route. Since the carrier has no open-cover, hyperspace compactness, quotient subset, selected closest point, host supremum, or ambient completeness coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[HausdorffMetric compact-subset distance route]
\label{thm:hausdorff-metric-compact-subset-distance-route}
Every accepted $\HausdorffMetricUp$ packet exposes the distance between its compact located subset rows only through the finite route
$$
X\longmapsto (A,B)\longmapsto R\longmapsto D\longmapsto \RealUp .
$$
The route reads the ambient $\MetricUp$ row, both $\CompactMetricUp$ and $\LocatedMetricUp$ subset rows, a positive $\RationalBallUp$ tolerance row, and the two directed finite cover-distance ledgers before a real distance name can be consumed.
\end{theorem}
\begin{proof}
Start with a distance request between the displayed subset rows. The ambient metric row $X$ supplies the point classifiers, distance witnesses, and metric-law ledgers that both subset rows use.

The rows $A$ and $B$ then expose compact located data over the same ambient route. Compact finite-net data provides the finite witnesses for cover inspection, while located-distance data provides the comparison route from an ambient point to the opposite displayed subset.

The positive rational request $R$ fixes the tolerance window. The directed ledgers in $D$ answer the two cover-distance questions using only finite rows from $A$, $B$, $R$, and $X$.

The $\RealUp$ readback is therefore downstream of both directed ledgers and of the structural rows $T,C,P,N$. No consumer can bypass this route through quotient subsets, selected closest points, host suprema, hyperspace compactness, open-cover compactness, or ambient real completeness.
\end{proof}

\begin{theorem}[HausdorffMetric triangle handoff]
\label{thm:hausdorff-metric-triangle-handoff}
For accepted $\HausdorffMetricUp$ carriers over the same ambient $\MetricUp$ row, every triangle-consumer read of the Hausdorff distance factors through the stored triangle handoff rows $T$, the two directed cover-distance ledgers $D$, and the displayed $\Cont$, $\Pkg$, and $\NameCert$ provenance. The handoff uses no host hyperspace metric, quotient subset equality, selected nearest points, host suprema, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the three compact located subset packets through the same ambient metric row $X$. The only triangle data available to the Hausdorff packet are the metric triangle rows displayed by $X$ and the finite directed cover-distance ledgers displayed by $D$.

The handoff rows $T$ bind those ambient triangles to the directed ledgers. A consumer first follows the directed comparison from the first subset to the second, then the directed comparison from the second to the third, and then replays the ambient triangle rows through the same compact located and rational tolerance data.

The continuation and provenance rows $C,P,N$ keep the handoff attached to the accepted packet. The separated completion boundary supplied through $\HausdorffCompletionUp$ records only the visible finite seal route and does not add a host hyperspace metric.

Thus the triangle consumer receives exactly the displayed finite handoff. Since the carrier has no coordinate for quotient subsets, selected nearest points, host suprema, open-cover compactness, hyperspace compactness, or ambient real completeness, no additional triangle source can be exported.
\end{proof}

\falsifiablePrediction{Every $\HausdorffMetricUp$ read from an accepted packet factors through the ambient $\MetricUp$ row, the two compact located subset rows, the positive $\RationalBallUp$ tolerance row, both directed finite cover-distance ledgers, triangle handoff rows, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ before any $\RealUp$ distance seal is consumed.}

\independenceWitness{metric, compactmetric, locatedmetric, rationalball, hausdorffcompletion, real: $\HausdorffMetricUp$ is not bijective to any listed sibling because it jointly carries two compact located subset rows, a positive rational tolerance row, paired directed cover-distance ledgers, triangle handoff rows, continuation replay, provenance, and a local naming certificate.}

\closureat{HausdorffMetricUp}{seedStr}

\begin{closurestatus}{\HausdorffMetricUp}
  \constructivestory{A $\HausdorffMetricUp$ packet starts from a displayed Metric row, reads two compact located subset rows, records a positive rational tolerance, builds two directed finite cover-distance ledgers, attaches triangle handoff rows, and preserves the route by continuation replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hausdorff-metric-carrier}, \autoref{thm:hausdorff-metric-namecert-obligations}, \autoref{thm:hausdorff-metric-compact-subset-distance-route}, and \autoref{thm:hausdorff-metric-triangle-handoff} seed the finite compact-subset distance interface through ambient metric data, compact located subset rows, rational tolerance windows, directed cover-distance ledgers, and triangle handoff rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, hyperspace compactness, quotient subsets, selected closest points, host suprema, ambient $\RealUp$ completeness, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, shared ambient metric compatibility, compact located subset compatibility, positive rational tolerance readback, directed cover-distance ledgers, triangle handoff stability, real seal non-escape, and ledger exactness for BEDC.Derived.HausdorffMetricUp.}
\end{closurestatus}
