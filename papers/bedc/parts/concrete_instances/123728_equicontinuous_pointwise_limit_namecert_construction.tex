\chapter{A Concrete Naming Certificate for $\EquicontinuousPointwiseLimitUp$}
\label{ch:concrete-instances-equicontinuous-pointwise-limit-namecert}
\origin{human}

Following Bishop and Bridges on constructive compact metric spaces and the Arzela--Ascoli and Dini traditions, the $\EquicontinuousPointwiseLimitUp$ packet records the finite route by which an equicontinuous family with pointwise finite-window convergence presents a uniform Real-facing limit read. It sits over the compact finite-net source of \autoref{ch:concrete-instances-compactmetric-namecert}, the displayed map-family surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-limit handoff of \autoref{ch:concrete-instances-uniformlimit-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names finite net propagation from pointwise windows to a uniform limit route; it does not import an open-cover compactness theorem, quotient equality of functions, countable choice, hidden host limits, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Equicontinuous pointwise-limit carrier]
\label{def:equicontinuous-pointwise-limit-carrier}
An $\EquicontinuousPointwiseLimitUp$ carrier is a finite $\BHist$ packet
$$
E=(K,F,M,W,R,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row, read through $\CompactMetricUp$, naming the inspected centers, mesh radius, and cell-membership evidence.
\item $F$ is the continuous-family row, read through $\ContinuousMapUp$, naming the finite family members and the displayed source and target windows.
\item $M$ is the equicontinuity modulus row shared by the accepted family over the displayed finite-net cells.
\item $W$ is the pointwise $\StreamNameUp$ window row, recording for each displayed net center the finite stages at which the family observations are compared.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the pointwise windows and to the propagated finite-net tolerance.
\item $A$ is the terminal $\RealUp$ seal row exposed only after $K,F,M,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact-net data, continuous-family rows, equicontinuity moduli, pointwise stream windows, regular rational readback, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the compact-net request through
$$
K\longrightarrow F\longrightarrow M\longrightarrow W\longrightarrow R\longrightarrow A .
$$
\item $P$ is the $\Pkg$ provenance over $\EquicontinuousPointwiseLimitUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EquicontinuousPointwiseLimitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,M,W,R,A,H,C,P,N$. It has no coordinate for arbitrary open covers, host function equality, a chosen limiting function outside the displayed rows, countable choice, trichotomy, or an ambient completeness theorem for $\RealUp$.
\end{definition}
\leanchecked{BEDC.Derived.EquicontinuousPointwiseLimitUp.TasteGate.EquicontinuousPointwiseLimitTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Equicontinuous pointwise-limit NameCert obligations]
\label{thm:equicontinuous-pointwise-limit-namecert-obligations}
For every accepted $\EquicontinuousPointwiseLimitUp$ carrier
$$
E=(K,F,M,W,R,A,H,C,P,N),
$$
the local $\NameCert_{\EquicontinuousPointwiseLimitUp}$ surface consists exactly of compact finite-net admission, continuous-family exposure, shared equicontinuity-modulus exposure, pointwise finite-window exposure, $\RegSeqRatUp$ readback, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier to the displayed tuple of \autoref{def:equicontinuous-pointwise-limit-carrier}. The mathematical rows before transport and naming are precisely $K,F,M,W,R,A$.

The compact row $K$ supplies only a finite net and its mesh evidence. The continuous-family row $F$ names the displayed maps on the inspected source and target windows, and the modulus row $M$ is the only row allowed to propagate pointwise information from net centers to neighboring cells.

The pointwise convergence data are finite-window data. The row $W$ opens the relevant $\StreamNameUp$ windows at the displayed centers, while $R$ reads them as regular rational observations before $A$ can be sealed as a $\RealUp$ value.

Finally $H,C,P,N$ transport, replay, source, and name that same finite route. Since no carrier coordinate stores an open-cover compactness principle, quotient function equality, countable choice, hidden host limit, trichotomy, or ambient completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Equicontinuous pointwise finite-net route]
\label{thm:equicontinuous-pointwise-limit-finite-net-route}
Every $\UniformLimitUp$, $\RealUp$, or Completion-facing consumer of an accepted $\EquicontinuousPointwiseLimitUp$ packet factors through the finite route
$$
\CompactMetricUp\longrightarrow \ContinuousMapUp\longrightarrow M\longrightarrow \StreamNameUp\longrightarrow \RegSeqRatUp\longrightarrow \RealUp .
$$
The consumer may read the terminal Real-facing limit row only after the compact finite-net row, continuous-family row, shared equicontinuity modulus, pointwise finite windows, and regular rational readback have been displayed.
\end{theorem}
\begin{proof}
Start with the accepted carrier $E=(K,F,M,W,R,A,H,C,P,N)$. The finite-net row $K$ is the only compactness datum in the packet, and the family row $F$ is the only displayed continuous-map datum.

Use the NameCert obligations of \autoref{thm:equicontinuous-pointwise-limit-namecert-obligations}. They force the shared modulus row $M$ to be visible before pointwise windows may be propagated from net centers to cells.

The pointwise windows then pass through $W$ and $R$. Thus any uniform-limit or Real-facing read reaches the terminal seal $A$ only by the displayed $\StreamNameUp$ and $\RegSeqRatUp$ readback route.

The structural rows $H,C,P,N$ preserve the same ordering and provenance. They add no open-cover compactness theorem, function quotient, choice principle, host limit, trichotomy, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\EquicontinuousPointwiseLimitUp$ uniform-limit or Real-facing read displays one compact finite-net row, one continuous-family row, one shared equicontinuity modulus, pointwise StreamName windows, RegSeqRat readback, RealUp sealing, hsame transport, Cont replay, Pkg provenance, and local NameCert row before exporting the limit route.}

\independenceWitness{compactmetric, continuousmap, uniformlimit, streamname, regseqrat, real: $\EquicontinuousPointwiseLimitUp$ is not bijective to any listed sibling because it jointly binds compact finite nets, continuous-family rows, equicontinuity modulus data, pointwise finite windows, regular rational readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{EquicontinuousPointwiseLimitUp}{seedStr}

\begin{closurestatus}{\EquicontinuousPointwiseLimitUp}
  \constructivestory{An $\EquicontinuousPointwiseLimitUp$ packet is generated from compact finite-net rows, continuous-family rows, a shared equicontinuity modulus, pointwise StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:equicontinuous-pointwise-limit-carrier}, \autoref{thm:equicontinuous-pointwise-limit-namecert-obligations}, and \autoref{thm:equicontinuous-pointwise-limit-finite-net-route} seed the finite-net route from pointwise windows and equicontinuity to a uniform Real-facing limit handoff.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.EquicontinuousPointwiseLimitUp.TasteGate.EquicontinuousPointwiseLimitTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient function equality, countable choice, hidden host limits, trichotomy, ambient RealUp completeness, Lean checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, continuous-family exposure, equicontinuity-modulus exposure, pointwise window exactness, RegSeqRat readback, RealUp seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.EquicontinuousPointwiseLimitUp.}
\end{closurestatus}
