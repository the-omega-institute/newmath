The $\MeasureUp$ event-value and premeasure rows consume the L10 completion surface through the $\RealUp$ value route of \autoref{ch:concrete-instances-real-namecert} and the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}. The same displayed rows then feed the probability consumer of \autoref{ch:concrete-instances-probspace-namecert}: normalization may read a total-mass row only after the event algebra, Real-valued mass row, $\BHist$ ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\MeasureUp}$ route are visible.
