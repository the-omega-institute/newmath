The $\MeasureUp$ event-value and countable-premise rows are consumed by \autoref{ch:concrete-instances-probspace-namecert} and \autoref{ch:concrete-instances-integral-namecert}. The route reads the finite event algebra, $\RealUp$ value row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before either probability normalization or integral readback is available.
