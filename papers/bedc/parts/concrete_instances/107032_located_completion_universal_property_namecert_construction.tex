\chapter{A Concrete Naming Certificate for $\LocatedCompletionUniversalPropertyUp$}
\label{ch:concrete-instances-located-completion-universal-property-namecert}
\origin{human}

Following Bishop and Bridges constructive completion theory, the $\LocatedCompletionUniversalPropertyUp$ packet records the extension-and-uniqueness surface for located completion as finite BEDC NameCert data. It sits after the located completion seal of \autoref{ch:concrete-instances-located-completion-seal-namecert}, the generic completion universal-property packet of \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, the stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names located extension and uniqueness only through displayed source, route, modulus, transport, replay, provenance, and local naming rows; it is not a quotient completion, a selected host limit, countable choice, ambient real completeness, or function extensionality.

\begin{definition}[Located completion universal-property carrier]
\label{def:located-completion-universal-property-carrier}
A $\LocatedCompletionUniversalPropertyUp$ carrier is a finite $\BHist$ packet
$$
K=(L,U,E,Q,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the located completion source row, exposing the source metric, locatedness request surface, dense unit, and completion seal read from \autoref{ch:concrete-instances-located-completion-seal-namecert}.
\item $U$ is the universal extension row, read against the target row and the generic completion-universal interface of \autoref{ch:concrete-instances-completionuniversalproperty-namecert}.
\item $E$ is the extension uniqueness row, comparing two displayed extensions only over the same located source, dense unit, and target route.
\item $Q$ is the request route through $\RegSeqRatUp$ and $\StreamNameUp$, carrying finite windows, regular rational readback, and the observations used by located requests.
\item $M$ is the uniform-modulus compatibility row, tying source moduli, target moduli, and located request tolerances to the same extension route.
\item $H$ records componentwise $\hsame$ transport for the located source, universal extension, uniqueness row, request route, modulus row, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from located source requests through finite stream windows, regular rational readback, uniform-modulus compatibility, extension, and uniqueness.
\item $P$ is the $\Pkg$ provenance over $\LocatedCompletionUniversalPropertyUp$, $\LocatedCompletionSealUp$, $\CompletionUniversalPropertyUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCompletionUniversalPropertyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,U,E,Q,M,H,C,P,N$. It has no coordinate for quotient completion representatives, selected limits, ambient complete-space theorems, function extensionality, countable choice, or unlocated real equality.
\end{definition}

\begin{theorem}[Located completion universal-property NameCert obligations]
\label{thm:located-completion-universal-property-namecert-obligations}
For every accepted $\LocatedCompletionUniversalPropertyUp$ carrier $K=(L,U,E,Q,M,H,C,P,N)$, the local $\NameCert_{\LocatedCompletionUniversalPropertyUp}$ surface consists exactly of located-source admission through $L$, universal extension exposure through $U$, extension uniqueness through $E$, $\RegSeqRatUp$ and $\StreamNameUp$ request routing through $Q$, uniform-modulus compatibility through $M$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-completion-universal-property-carrier}. The located completion source row $L$ is visible before any extension row is consumed, and it reads the dense unit and completion seal through \autoref{ch:concrete-instances-located-completion-seal-namecert}.

Read the universal-property rows next. The row $U$ supplies the displayed extension route, while $E$ compares two such routes only along the same located source and dense unit. The generic obligation surface is therefore the one already named by \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, refined by located request data rather than by a new host completion object.

The request and modulus rows keep the route finite. The row $Q$ exposes the $\StreamNameUp$ windows and $\RegSeqRatUp$ readback consumed by located requests, and $M$ ties those requests to the same uniform-modulus compatibility ledger before the $\RealUp$ seal is read.

Finally $H,C,P,N$ transport, replay, source, and name only these displayed rows. Since the carrier has no quotient representative, selected host limit, ambient completeness theorem, function-extensionality row, countable-choice row, or unlocated real-equality coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedCompletionUniversalPropertyUp$ extension or uniqueness read factors through the displayed located source row, finite request route, uniform-modulus compatibility row, transport, replay, provenance, and local naming before reaching a $\RealUp$ consumer.}

\independenceWitness{located-completion-seal, completionuniversalproperty, streamname, regseqrat, real: $\LocatedCompletionUniversalPropertyUp$ is not bijective to any listed sibling because it jointly carries a located completion source, universal extension row, extension uniqueness row, finite request route, uniform-modulus compatibility, transport, replay, provenance, and local NameCert row.}

\closureat{LocatedCompletionUniversalPropertyUp}{seedStr}

\begin{closurestatus}{\LocatedCompletionUniversalPropertyUp}
  \constructivestory{A $\LocatedCompletionUniversalPropertyUp$ packet is generated from a located completion source row, universal extension row, extension uniqueness row, RegSeqRat and StreamName request route, uniform-modulus compatibility row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-completion-universal-property-carrier} and \autoref{thm:located-completion-universal-property-namecert-obligations} seed the finite located-completion universal-property certificate surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, selected limits, ambient complete-space theorems, unlocated real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located request routing, extension existence, extension uniqueness, uniform-modulus compatibility, transport replay, provenance, and TasteGate field faithfulness for BEDC.Derived.LocatedCompletionUniversalPropertyUp.}
\end{closurestatus}
