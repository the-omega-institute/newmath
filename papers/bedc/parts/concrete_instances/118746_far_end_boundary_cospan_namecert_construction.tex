\chapter{A Concrete Naming Certificate for $\FarEndBoundaryCospanUp$}
\label{ch:concrete-instances-far-end-boundary-cospan-namecert}
\origin{ai}

The $\FarEndBoundaryCospanUp$ packet realizes the boundary-cospan branch of the apophatic far-end diagrammatics route in \autoref{ch:visions-apophatic-far-end-diagrammatics} as finite NameCert data. It sits beside the far-end diagram packet of \autoref{ch:concrete-instances-far-end-diagram-namecert}, the far-end diagrammatics packet of \autoref{ch:concrete-instances-far-end-diagrammatics-namecert}, and the boundary-style transport rows used by adjacent hyperbolic and observer chapters. The packet names a finite cospan of left boundary, right boundary, apex far-end name, leg provenance, and compatibility rows; it is not a global universe, not an infinite object, not an observer, and not a selected terminal object in a host category.

\begin{definition}[FarEndBoundaryCospan carrier]
\label{def:far-end-boundary-cospan-carrier}
A $\FarEndBoundaryCospanUp$ carrier is a finite $\BHist$ packet
$$
F=(L,R,A,\lambda,\rho,B_L,B_R,J,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are the left and right boundary-name rows read from admissible far-end diagram data.
\item $A$ is the apex far-end name row common to the two boundary legs.
\item $\lambda$ and $\rho$ are the left and right leg rows from $L$ and $R$ into $A$.
\item $B_L$ and $B_R$ are the boundary-provenance rows recording which observation-fiber or socket route produced the two boundary names.
\item $J$ is the cospan-compatibility row asserting that both legs name the same apophatic far end only through the displayed apex row.
\item $H$ records componentwise $\hsame$ transport for the boundary rows, apex row, leg rows, provenance rows, compatibility row, replay, package, and local naming.
\item $C$ records displayed $\Cont$ replay from a boundary comparison request through $B_L,B_R,\lambda,\rho,J$ and the apex row $A$.
\item $P$ is the $\Pkg$ provenance over $\FarEndBoundaryCospanUp$, $\FarEndDiagramUp$, $\FarEndDiagrammaticsUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FarEndBoundaryCospanUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,R,A,\lambda,\rho,B_L,B_R,J,H,C,P,N$. It has no coordinate for an ambient terminal object, global category of observers, selected boundary quotient, or infinite far-end completion.
\end{definition}

\begin{theorem}[FarEndBoundaryCospan NameCert obligations]
\label{thm:far-end-boundary-cospan-namecert-obligations}
For every accepted $\FarEndBoundaryCospanUp$ carrier
$$
F=(L,R,A,\lambda,\rho,B_L,B_R,J,H,C,P,N),
$$
the local $\NameCert_{\FarEndBoundaryCospanUp}$ surface consists exactly of carrier admission for the displayed rows; left and right boundary exposure through $L,R$; apex far-end exposure through $A$; leg exposure through $\lambda,\rho$; provenance exposure through $B_L,B_R$; cospan compatibility through $J$; componentwise transport through $H$; boundary-comparison replay through $C$; package provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted packet. The only boundary data are the left row, right row, apex row, two legs, two provenance rows, and compatibility row.

Carrier admission is therefore exhausted by those rows. The two legs cannot be compared without the apex $A$, and the two boundary names cannot be sourced without the provenance rows $B_L$ and $B_R$.

The compatibility row $J$ records the cospan condition inside the displayed packet. The structural rows transport, replay, source, and name the same finite route, so no ambient terminal object or observer category is added by the certificate.
\end{proof}

\begin{theorem}[FarEndBoundaryCospan diagrammatic route]
\label{thm:far-end-boundary-cospan-diagrammatic-route}
Every boundary comparison exported by an accepted $\FarEndBoundaryCospanUp$ packet factors through the finite route
$$
L,R,A,\lambda,\rho,B_L,B_R,J,H,C,P,N.
$$
The consumer must read the two boundary names, their provenance rows, the two legs, and the apex far-end name before the compatibility row can be consumed.
\end{theorem}
\begin{proof}
Start with a boundary comparison request. The only replay row is $C$, so the request must follow the displayed cospan route.

The boundary rows $L$ and $R$ are read together with their provenance rows $B_L$ and $B_R$. The leg rows $\lambda$ and $\rho$ then map those boundary names into the apex row $A$.

Only after both legs have reached $A$ can the compatibility row $J$ be read. Transport and provenance remain componentwise through $H$ and $P$, and the local name $N$ records the same finite cospan. Thus the exported comparison is diagrammatic finite data, not a host categorical terminality claim.
\end{proof}

\begin{theorem}[FarEndBoundaryCospan non-escape boundary]
\label{thm:far-end-boundary-cospan-nonescape}
An accepted $\FarEndBoundaryCospanUp$ packet exports no global universe, no infinite far-end object, no observer identity, no selected terminal object, and no quotient of boundary names. It exports only the displayed boundary rows, apex row, leg rows, provenance rows, compatibility row, transport, replay, package provenance, and local naming rows of \autoref{def:far-end-boundary-cospan-carrier}.
\end{theorem}
\begin{proof}
Inspect the carrier inventory. Every row is either a boundary name, an apex far-end name, a leg, a provenance row, a compatibility row, or structural support.

A global universe or infinite far-end object would require a completion or totality coordinate absent from the carrier. An observer identity or terminal-object claim would require host category data absent from the packet.

The only equality-like information is the displayed cospan compatibility through $J$, and it is scoped by the apex row $A$. Therefore no consumer can escape from the finite cospan surface to an ambient quotient or terminality principle.
\end{proof}

\falsifiablePrediction{An accepted $\FarEndBoundaryCospanUp$ consumer that compares two far-end boundary names without displaying both boundary provenance rows, both cospan legs, the apex far-end name, and the compatibility row refutes the carrier surface.}

\independenceWitness{far-end-diagram, far-end-diagrammatics, hyperbolic-boundary-transport, poincare-disk-boundary-transport, vision-coverage-index: no listed sibling supplies the whole packet $F=(L,R,A,\lambda,\rho,B_L,B_R,J,H,C,P,N)$, because far-end diagram rows name socket and fiber data, far-end diagrammatics names observation stratification, hyperbolic and Poincare boundary transports name geometric boundary movement, and the vision coverage index names marker coverage, while this chapter binds a two-leg boundary cospan with apex compatibility as one finite NameCert surface.}

\closureat{FarEndBoundaryCospanUp}{seedStr}

\begin{closurestatus}{\FarEndBoundaryCospanUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:far-end-boundary-cospan-carrier}, \autoref{thm:far-end-boundary-cospan-namecert-obligations}, \autoref{thm:far-end-boundary-cospan-diagrammatic-route}, and \autoref{thm:far-end-boundary-cospan-nonescape} seed the finite boundary-cospan route over two boundary names, an apex far-end name, two legs, provenance, compatibility, transport, replay, package, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a global universe, infinite far-end object, observer identity, selected terminal object, boundary quotient, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires BEDC.Derived.FarEndBoundaryCospanUp carrier admission, boundary provenance, cospan-leg compatibility, diagrammatic replay, and future TasteGate Nontrivial and FieldFaithful instances.}
  \constructivestory{A $\FarEndBoundaryCospanUp$ packet is generated from left and right boundary-name rows, an apex far-end name, two leg rows, boundary-provenance rows, a compatibility row, componentwise transport, displayed replay, package provenance, and a local NameCert row.}
\end{closurestatus}
