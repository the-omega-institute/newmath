\chapter{A Concrete Naming Certificate for $\IntermediateValueUp$}
\label{ch:concrete-instances-intermediatevalue-namecert}
\origin{human}

Following Bishop and Bridges' constructive intermediate-value reading, the $\IntermediateValueUp$ packet records the finite bisection route by which opposite endpoint signs, a displayed continuity modulus, and located interval windows generate a real-facing root-location seal. The certificate is a NameCert reconstruction of the bisection argument: it consumes the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the finite branch ledger of \autoref{ch:concrete-instances-dyadicbisection-namecert}, and the nested-window carrier of \autoref{ch:concrete-instances-nestedinterval-namecert}, but it does not claim open-cover compactness, quotient reals, host equality of real values, or ambient $\RealUp$ completeness.

The packet also reads its endpoint cells through the located interval surface of \autoref{ch:concrete-instances-locatedinterval-namecert} and its function data through the pointwise-modulus carrier of \autoref{ch:concrete-instances-continuousmap-namecert}. Thus the intermediate-value route is a finite ledger over displayed source rows, not a global theorem about all continuous maps on a host interval.

\begin{definition}[IntermediateValue located-sign carrier]
\label{def:intermediate-value-located-sign-carrier}
An $\IntermediateValueUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
V=(J,A,B,F,M,D,N,E,H,C,P,Q)
$$
with the following visible rows:
\begin{enumerate}
\item $J$ is the $\LocatedIntervalUp$ row naming the inspected endpoint interval and its rational and dyadic endpoint cells;
\item $A$ and $B$ are the endpoint sign rows, one recording the displayed negative-side read and one recording the displayed positive-side read after both endpoints have been transported into the same located interval packet;
\item $F$ is the $\ContinuousMapUp$ row carrying the map graph, pointwise modulus, and metric-distance witness used by the sign inspections;
\item $M$ is the finite modulus budget row selecting the dyadic precision at which the sign reads and midpoint tests are performed;
\item $D$ is the $\DyadicBisectionUp$ branch ledger whose midpoint choices retain an interval with opposite displayed signs at the requested finite window;
\item $N$ is the $\NestedIntervalUp$ row obtained by threading the retained intervals of the bisection ledger;
\item $E$ is the $\RealUp$ seal row offered to downstream root-location consumers;
\item $H$ stores the $\hsame$ transports for the located interval, sign rows, continuous-map row, modulus budget, bisection ledger, nested interval row, and real seal;
\item $C$ stores the displayed $\Cont$ read routes by which a consumer reaches exactly those rows;
\item $P$ is the $\Pkg$ provenance over $\LocatedIntervalUp$, $\DyadicBisectionUp$, $\NestedIntervalUp$, $\ContinuousMapUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, and $\NameCert$;
\item $Q$ is the local $\NameCert_{\IntermediateValueUp}$ evidence row.
\end{enumerate}
The carrier predicate asserts that these rows form one finite located-sign bisection packet. The packet has no field for a host real root, a quotient of real names, an order-completeness principle, a real trichotomy oracle, or an open-cover compactness argument.
\end{definition}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueTasteGate\_single\_carrier\_alignment}

\begin{theorem}[IntermediateValue dyadic bisection obligations]
\label{thm:intermediate-value-dyadic-bisection-obligations}
The seed $\NameCert_{\IntermediateValueUp}$ surface has five obligations:
\begin{enumerate}
\item carrier: admission of the finite $\mathsf{BHist}$ packet of \autoref{def:intermediate-value-located-sign-carrier};
\item endpoint sign exactness: the displayed negative and positive endpoint reads are routed through $J$, $F$, $M$, $H$, and $C$, and every transported sign read remains attached to the same located interval row;
\item bisection ledger exactness: each midpoint decision in $D$ is the displayed branch row whose retained subinterval preserves the transported opposite-sign boundary;
\item nested-window stability: the retained branch prefix in $D$ repacks as the $\NestedIntervalUp$ row $N$ through the displayed $\hsame$ transports and $\Cont$ routes;
\item real-seal handoff: the $\RealUp$ seal $E$ is exposed only after the located interval row, endpoint signs, continuous-map modulus row, bisection ledger, nested interval row, provenance row, and local NameCert row have all been read.
\end{enumerate}
These obligations are internal to the displayed packet and do not use compactness, quotient equality of real names, host real trichotomy, or completeness of $\RealUp$.
\end{theorem}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_dyadic\_bisection\_obligations}
\begin{proof}
Carrier admission is projection from the tuple $V$. Its coordinates are exactly the located interval, endpoint sign, continuous-map, modulus, bisection, nested-window, real-seal, transport, continuation, provenance, and local naming rows listed in \autoref{def:intermediate-value-located-sign-carrier}.

Endpoint sign exactness reads the two endpoint signs only through the located interval and continuous-map rows. The stored $\hsame$ and $\Cont$ data move both endpoint reads into the same finite window before any midpoint branch is inspected.

Bisection ledger exactness is the finite branch invariant inherited from the displayed $\DyadicBisectionUp$ row. Each retained subinterval is selected by a visible midpoint row, and the opposite-sign boundary is carried forward as part of the same packet.

Nested-window stability repacks the retained branch prefix as the $\NestedIntervalUp$ row. The repacking uses only the branch ledger, endpoint cells, and transport routes already present in $V$.

The real-seal handoff then exposes $E$ through the same displayed provenance. Since no coordinate of $V$ contains compactness data, quotient-real equality, a trichotomy oracle, or a completeness theorem, none of these can participate in the seed certificate.
\end{proof}

\begin{theorem}[IntermediateValue bisection-window scope]
\label{thm:intermediatevalue-bisection-window-scope}
Let $V=(J,A,B,F,M,D,N,E,H,C,P,Q)$ be an accepted $\IntermediateValueUp$ carrier. Every accepted root-bracket export from $V$ factors through the displayed $\DyadicBisectionUp$ branch ledger $D$, the retained $\NestedIntervalUp$ window $N$, and the source $\LocatedIntervalUp$ row $J$ before any $\RealUp$ seal or root-location consumer can read $E$.

The factorization is stable under the componentwise $\hsame$ rows in $H$, is replayed only through the displayed $\Cont$ row $C$, and remains attached to the same $\BHist$, $\Pkg$, and $\NameCert_{\IntermediateValueUp}$ evidence. It does not use unrestricted completeness, open-cover compactness, quotient real equality, a selected root, or classical trichotomy.
\end{theorem}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_bisection\_window\_scope}
\begin{proof}
Project the accepted carrier and read the bisection coordinates. The only row that stores midpoint decisions is $D$, and the only retained-window row obtained from those decisions is $N$.

The source interval is fixed by $J$. Each branch export therefore has to pass from $J$ to $D$ and then to $N$ before the seal row $E$ can be exposed to a root-location consumer.

Transport and replay do not create a second route. The rows $H$ and $C$ move and replay the same coordinates, while $P$ and $Q$ package and name the same finite carrier.

Since the carrier has no coordinate for unrestricted completeness, open-cover compactness, quotient equality, a selected root, or classical trichotomy, the bisection-window scope cannot export any of these objects.
\end{proof}

\begin{theorem}[IntermediateValue sign-change ledger]
\label{thm:intermediatevalue-sign-change-ledger}
Let $V=(J,A,B,F,M,D,N,E,H,C,P,Q)$ be an accepted $\IntermediateValueUp$ carrier. Before $V$ exports a root bracket, the endpoint sign rows $A,B$ must be visible through the same $\LocatedIntervalUp$ row $J$, the same $\ContinuousMapUp$ row $F$, and the same finite modulus budget $M$. Each retained branch row in $D$ records the transported sign-change boundary used to choose the next displayed subinterval.

Thus the sign-change information is a ledger inside the finite carrier, not a host predicate over all real points. It is transported by $H$, replayed by $C$, sourced by $P$, and named by $Q$, with no appeal to real trichotomy, selected roots, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_sign\_change\_ledger}
\begin{proof}
Begin with the endpoint sign rows. The carrier stores them as $A$ and $B$, and both are attached to the located interval row $J$ through the continuous-map and modulus rows $F,M$.

The bisection ledger can read only those displayed signs. Each midpoint decision in $D$ retains the subinterval whose endpoint rows still carry the transported opposite-sign boundary.

The structural rows preserve the ledger. The transport row $H$ moves the sign rows componentwise, $C$ replays the same route, and $P,Q$ source and name the packet.

No carrier coordinate supplies a global sign predicate, trichotomy oracle, selected root, quotient equality, or completeness theorem. The sign-change evidence is therefore exactly the finite ledger visible before the bracket is exported.
\end{proof}

\begin{theorem}[IntermediateValue Real seal nonescape]
\label{thm:intermediatevalue-real-seal-nonescape}
Every accepted $\IntermediateValueUp$ real-facing consumer factors through
$$
\LocatedIntervalUp \longmapsto \ContinuousMapUp \longmapsto \DyadicBisectionUp \longmapsto \NestedIntervalUp \longmapsto \RealUp
$$
by first reading $J$, then $F,M,A,B$, then the branch ledger $D$, then the retained window $N$, and only then the seal row $E$ with $H,C,P,Q$.

Consequently the $\RealUp$ seal is a nonescape boundary for the finite bisection route. A consumer cannot replace it by unrestricted completeness of $\RealUp$, an infinite nested intersection, quotient equality of real names, classical trichotomy, compactness, or an externally selected root.
\end{theorem}
\begin{proof}
Read the consumer route from the continuation row $C$. The route starts at the located interval row $J$ and the continuous-map data $F,M$ because the endpoint sign rows are meaningful only over those displayed source cells.

Next inspect the branch and retained-window rows. The ledger $D$ records the finite bisection choices, and $N$ repacks the retained branch prefix as the nested-window row.

The seal row $E$ is downstream from those rows. The structural rows $H,C,P,Q$ transport, replay, source, and name the same route, so there is no separate Real-facing coordinate.

Since the packet contains neither a completeness principle nor an infinite intersection, quotient equality, trichotomy decision, compactness theorem, or selected root, every real-facing consumer remains inside the displayed bisection route.
\end{proof}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_real\_seal\_nonescape}

\begin{theorem}[IntermediateValue real seal handoff]
\label{thm:intermediate-value-real-seal-handoff}
For every accepted $\IntermediateValueUp$ carrier, every $\RealUp$ or root-location consumer factors through the $\LocatedIntervalUp$ endpoint row, the $\DyadicBisectionUp$ branch ledger, the $\NestedIntervalUp$ retained window, the $\ContinuousMapUp$ modulus row, the displayed $\mathsf{BHist}$ packet, the $\hsame$ transport rows, the $\Cont$ read routes, the $\Pkg$ provenance, and the local $\NameCert_{\IntermediateValueUp}$ row.

Consequently the consumer route cannot bypass the finite located-sign bisection packet by reading a host root, an infinite nested intersection, quotient equality of real names, real trichotomy, open-cover compactness, or an ambient completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_real\_seal\_handoff}
\begin{proof}
Start from the accepted carrier $V=(J,A,B,F,M,D,N,E,H,C,P,Q)$. A root-location consumer can reach the real-facing row only through the continuation route $C$, so its first readable data are the located interval row $J$, the endpoint sign rows $A,B$, and the continuous-map row $F$ with its finite modulus budget $M$.

Next route the midpoint decisions through the bisection ledger. The ledger $D$ supplies the retained branch prefix, and \autoref{thm:intermediate-value-dyadic-bisection-obligations} keeps every branch choice attached to the transported endpoint signs and the displayed modulus row.

Then read the nested window. The retained branch prefix repacks as the $\NestedIntervalUp$ row $N$, so the real-facing request reaches a finite nested interval window before it reaches the seal row.

Finally expose $E$ with its structural rows. The transports $H$, routes $C$, provenance $P$, and local certificate row $Q$ bind the $\RealUp$ read to the finite packet. Every named consumer therefore factors through the stated rows, and there is no available coordinate for a host root, infinite intersection, quotient equality, trichotomy decision, compactness theorem, or completeness principle.
\end{proof}

\begin{theorem}[IntermediateValue bisection convergence handoff]
\label{thm:intermediate-value-bisection-convergence-handoff}
For every accepted $\IntermediateValueUp$ carrier
$$
V=(J,A,B,F,M,D,N,E,H,C,P,Q),
$$
if the retained branch prefix $D$ and nested window $N$ are read through the $\BisectionConvergenceUp$ packet supplied by \autoref{def:bisection-convergence-carrier}, then every root-location or $\RealUp$ consumer factors through $\LocatedIntervalUp$, $\DyadicBisectionUp$, $\NestedIntervalUp$, $\BisectionConvergenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before reading $E$.

Consequently the intermediate-value route can consume the named finite convergence certificate without introducing a selected root, quotient real equality, real trichotomy, countable choice, compactness, or an ambient completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_bisection\_convergence\_handoff}
\begin{proof}
Project the accepted carrier $V=(J,A,B,F,M,D,N,E,H,C,P,Q)$. By \autoref{thm:intermediate-value-real-seal-handoff}, any root-location or $\RealUp$ consumer first reaches the located interval row $J$, the dyadic branch ledger $D$, the retained nested window $N$, the displayed packet rows, and the local $\NameCert_{\IntermediateValueUp}$ evidence before it can read the seal row $E$.

Now read $D$ and $N$ through the $\BisectionConvergenceUp$ carrier of \autoref{def:bisection-convergence-carrier}. The carrier places the located interval source, dyadic branch ledger, retained nested windows, tail-diameter schedule, StreamName windows, RegSeqRat readback, and terminal Real seal in one displayed finite route.

Apply \autoref{thm:bisection-convergence-tail-diameter} to the retained branch prefix and nested window. The convergence packet forces every tolerance request to pass through the displayed branch depth and retained-window diameter bound before any $\StreamNameUp$ or $\RegSeqRatUp$ readback is exposed.

Finally apply \autoref{thm:bisection-convergence-real-seal-handoff}. The StreamName and RegSeqRat stages precede the terminal $\RealUp$ seal, so the original intermediate-value consumer factors through the stated $\LocatedIntervalUp$, $\DyadicBisectionUp$, $\NestedIntervalUp$, $\BisectionConvergenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ route before reading $E$. Since neither carrier has a coordinate for a selected root, quotient real equality, trichotomy, choice, compactness, or ambient completeness, the composite handoff exports none of them.
\end{proof}

\begin{theorem}[IntermediateValue located sign scope]
\label{thm:intermediatevalue-located-sign-scope}
Every root-bracket consumer of an accepted $\IntermediateValueUp$ carrier must pass through the located interval row $J$, the endpoint sign rows $A,B$, the $\ContinuousMapUp$ modulus row $F,M$, the $\DyadicBisectionUp$ branch ledger $D$, the $\NestedIntervalUp$ retained window $N$, the $\BisectionConvergenceUp$ convergence packet when that packet is supplied, the $\StreamNameUp$ and $\RegSeqRatUp$ readbacks of the convergence route, and only then the $\RealUp$ seal row $E$ with $H,C,P,Q$.

Thus the Real-facing route is finite and L10-safe: it is a displayed BEDC path through located signs, finite bisection, retained nested windows, convergence readback, and the terminal seal, not a route through selected roots, quotient real equality, real trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the carrier definition and the dyadic bisection obligations. They place the endpoint signs inside the same located interval row $J$ and the same continuous-map modulus row $F,M$ before any branch ledger can be inspected.

Apply the real seal handoff. It forces every root-location consumer to read the dyadic branch ledger $D$ and the retained nested window $N$ before the seal row $E$ is reachable.

Use the bisection convergence handoff for the completion-facing part of the route. When the retained branch prefix and nested window are consumed through $\BisectionConvergenceUp$, the StreamName and RegSeqRat readbacks precede the terminal $\RealUp$ seal.

The structural rows $H,C,P,Q$ transport, replay, source, and name exactly this route. Since the carrier and the convergence packet contain no selected root, quotient equality, trichotomy oracle, choice selector, compactness theorem, or ambient completeness row, the located-sign scope is finite and L10-safe.
\end{proof}

\begin{theorem}[IntermediateValue sign-bracket carrier]
\label{thm:intermediate-value-sign-bracket-carrier}
For every accepted $\IntermediateValueUp$ carrier
$$
V=(J,A,B,F,M,D,N,E,H,C,P,Q),
$$
the located sign-bracket surface is exactly the displayed packet whose endpoint cells are read through $\LocatedIntervalUp$, whose sign inspections are read through $\ContinuousMapUp$ and the finite modulus row $M$, whose branch history is read through $\DyadicBisectionUp$, and whose retained interval stream is read through $\NestedIntervalUp$. The structural rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ attach these reads to one local packet before any $\RealUp$ seal is exposed.

Thus the sign-bracket carrier has no separate coordinate for a host root, quotient real equality, real trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_sign\_bracket\_carrier}
\begin{proof}
Project the carrier of \autoref{def:intermediate-value-located-sign-carrier}. The located interval row $J$ supplies the two endpoint cells, while $A$ and $B$ record the opposite displayed sign reads over the same transported source interval.

The function row $F$ and modulus row $M$ are the only rows that can justify those sign inspections. Hence every endpoint sign read is anchored in $\ContinuousMapUp$ data before the branch ledger is consulted.

The branch row $D$ and retained row $N$ then record the finite bisection history and its nested-window image. The rows $H,C,P,Q$ transport, replay, source, and name the same packet, so the carrier exposes one located sign bracket rather than independent host data.

Since no coordinate of $V$ stores a root selector, quotient equality, trichotomy oracle, compactness theorem, or completeness theorem, no such object can be exported by the carrier.
\end{proof}

\begin{theorem}[IntermediateValue bisection nesting]
\label{thm:intermediate-value-bisection-nesting}
Let $V=(J,A,B,F,M,D,N,E,H,C,P,Q)$ be an accepted $\IntermediateValueUp$ carrier. Every retained branch read in $D$ that is offered to a root-location consumer must pass through the dyadic bisection route of $\DyadicBisectionUp$ and then through the nested-window route of $\NestedIntervalUp$ before the $\RealUp$ seal $E$ is reachable. The route is stable under the displayed $\hsame$ transports and is replayed only by the $\Cont$ row attached to the same $\BHist$ packet.
\end{theorem}
\begin{proof}
Begin with a retained branch read. The row $D$ is the only carrier coordinate that records midpoint choices, retained subintervals, and finite branch prefixes.

The nested row $N$ is downstream of $D$. It repacks the retained branch prefix as a nested interval window, and the real-facing consumer cannot reach $E$ until that nested-window row has been displayed.

Transport stability is componentwise. The row $H$ transports the located interval, sign rows, bisection ledger, nested window, and seal together, while $C$ replays the same ordered route for consumers.

The provenance row $P$ and local certificate row $Q$ bind this route to one $\NameCert_{\IntermediateValueUp}$ packet. Thus the bisection nesting path is $D$ followed by $N$ inside the displayed $\BHist$ carrier, with no bypass through a selected root or ambient completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_bisection\_nesting}

\begin{theorem}[IntermediateValue located-sign window exactness]
\label{thm:intermediate-value-located-sign-window-exactness}
Let $V=(J,A,B,F,M,D,N,E,H,C,P,Q)$ be an accepted $\IntermediateValueUp$ carrier. Every endpoint sign read exported by $V$ factors through the same $\LocatedIntervalUp$ row $J$, the same $\ContinuousMapUp$ modulus row $F,M$, the same $\DyadicBisectionUp$ branch ledger $D$, and the same $\NestedIntervalUp$ retained window $N$ before any $\RealUp$ seal is reachable. The factorization is stable under the displayed $\BHist$ packet, componentwise $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\IntermediateValueUp}$ route.

Thus the two endpoint signs are exact reads of one located-sign window rather than independent host predicates, selected roots, quotient real equalities, trichotomy decisions, compactness witnesses, or ambient $\RealUp$ completeness data.
\end{theorem}
\begin{proof}
Start with the accepted carrier and project the rows listed in \autoref{def:intermediate-value-located-sign-carrier}. The endpoint sign rows $A$ and $B$ are attached to the located interval row $J$, and \autoref{thm:intermediate-value-sign-bracket-carrier} identifies this attachment as the displayed sign-bracket surface.

The function and modulus data are read through $F$ and $M$. By the dyadic bisection obligations of \autoref{thm:intermediate-value-dyadic-bisection-obligations}, every sign inspection used by the branch ledger remains attached to that same located interval and continuous-map route.

Apply \autoref{thm:intermediate-value-bisection-nesting}. The retained branch read then passes through $D$ and $N$ before the seal row $E$ can be consumed, with $H$ and $C$ preserving the same rows componentwise.

Finally use \autoref{thm:intermediate-value-real-seal-handoff}. The real-facing route includes the provenance row $P$ and local naming row $Q$, so there is no coordinate from which a selected root, quotient equality, trichotomy oracle, compactness theorem, or ambient completeness principle can alter the located-sign window.
\end{proof}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_located\_sign\_window\_exactness}

\begin{theorem}[IntermediateValue modulus branch transport]
\label{thm:intermediate-value-modulus-branch-transport}
Let $V=(J,A,B,F,M,D,N,E,H,C,P,Q)$ be an accepted $\IntermediateValueUp$ carrier. Transporting the continuous-map modulus row $F,M$ along the displayed $\hsame$ data preserves the retained branch ledger $D$ and its nested-window image $N$. Consequently every transported root-location request still reads the same $\LocatedIntervalUp$ endpoint cells, the same dyadic midpoint choices, the same retained branch window, and the same $\RealUp$ handoff route.

The transport does not introduce a selected root, quotient real equality, trichotomy, compactness, or ambient $\RealUp$ completeness; it only replays the existing finite branch packet through $H$, $C$, $P$, and $Q$.
\end{theorem}
\begin{proof}
Read the transport row $H$ of the carrier. Its component for $F,M$ is stored with the same located interval row $J$ and the same endpoint sign rows $A,B$, so a transported modulus request cannot detach the sign test from the source window.

The dyadic branch ledger is already bound to those sign tests by \autoref{thm:intermediate-value-dyadic-bisection-obligations}. Therefore transporting the modulus row preserves the branch choices recorded in $D$ rather than recomputing them from host-level sign predicates.

Next apply \autoref{thm:intermediate-value-bisection-nesting}. The retained branch prefix is repacked as $N$ through the same continuation route $C$, and the route to the seal row $E$ still passes through $D$ followed by $N$.

Finally \autoref{thm:intermediate-value-real-seal-handoff} identifies the only real-facing export. Since the carrier has no coordinate for a root selector, quotient equality, trichotomy, compactness, or completeness, modulus transport cannot add any of these objects to the retained branch packet.
\end{proof}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_modulus\_branch\_transport}

\begin{theorem}[IntermediateValue NameCert obligations]
\label{thm:intermediate-value-namecert-obligations}
The local $\NameCert_{\IntermediateValueUp}$ obligation surface for the carrier of \autoref{def:intermediate-value-located-sign-carrier} consists exactly of:
\begin{enumerate}
\item admission of the finite located sign-bracket carrier over $\LocatedIntervalUp$, $\ContinuousMapUp$, $\DyadicBisectionUp$, $\NestedIntervalUp$, and $\RealUp$;
\item endpoint sign transport through $\BHist$, $\hsame$, and $\Cont$ without detaching the two signs from the same located interval row;
\item bisection branch nesting through $\DyadicBisectionUp$ and $\NestedIntervalUp$ before the real-facing seal is exposed;
\item provenance and local naming through $\Pkg$ and $\NameCert$ for the same displayed rows.
\end{enumerate}
No additional obligation asks for a selected root, quotient real equality, trichotomy, open-cover compactness, countable choice, or ambient completeness of $\RealUp$.
\end{theorem}
\leanchecked{BEDC.Derived.IntermediateValueUp.IntermediateValueCarrier\_namecert\_obligations}
\begin{proof}
The carrier obligation is \autoref{thm:intermediate-value-sign-bracket-carrier}. It identifies the displayed rows and excludes every host-level datum not present in the finite packet.

Endpoint sign transport is the second clause of \autoref{thm:intermediate-value-dyadic-bisection-obligations}. The two sign rows move only through $H$ and are replayed only through $C$ over the same located interval and continuous-map data.

Bisection branch nesting is \autoref{thm:intermediate-value-bisection-nesting}. It forces every retained branch read through $D$ and then $N$ before the $\RealUp$ seal can be consumed.

Finally, $P$ and $Q$ supply the provenance and local naming rows for exactly those carrier coordinates. Since the listed rows exhaust the packet, the excluded host objects cannot become local obligations.
\end{proof}

\begin{theorem}[IntermediateValue obligation closure package]
\label{thm:intermediate-value-obligation-closure-package}
The displayed located-sign carrier of \autoref{def:intermediate-value-located-sign-carrier}, the dyadic bisection obligations of \autoref{thm:intermediate-value-dyadic-bisection-obligations}, the real seal handoff of \autoref{thm:intermediate-value-real-seal-handoff}, the bisection convergence handoff of \autoref{thm:intermediate-value-bisection-convergence-handoff}, the modulus branch transport of \autoref{thm:intermediate-value-modulus-branch-transport}, and the NameCert obligation surface of \autoref{thm:intermediate-value-namecert-obligations} form the complete obligation package for the current finite $\IntermediateValueUp$ surface.

Every root-location or $\RealUp$ consumer must therefore read the same located interval, endpoint sign, continuous-map modulus, dyadic branch ledger, nested window, real seal, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows before any real-facing handoff is exposed.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:intermediate-value-located-sign-carrier}. The dyadic bisection theorem fixes the carrier, endpoint signs, branch ledger, nested-window stability, and real-seal obligation clauses.

The handoff theorems then bind every consumer route. The real seal handoff prevents a direct read of $E$, while the bisection convergence handoff routes the retained branch and nested-window rows through the displayed convergence packet when that packet is supplied.

Finally apply modulus branch transport and the NameCert obligations theorem. These results account for the transported modulus branch and the local naming rows, so the listed clauses exhaust the finite obligation surface and leave no coordinate for a selected root, quotient real equality, trichotomy, compactness, choice, or ambient completeness.
\end{proof}

\begin{closurestatus}{\IntermediateValueUp}
  \constructivestory{An $\IntermediateValueUp$ packet is generated from a located interval row, two endpoint sign rows, a $\ContinuousMapUp$ modulus row, a finite $\DyadicBisectionUp$ branch ledger, a $\NestedIntervalUp$ retained window, a $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ evidence.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{The obligation scope is the finite located-sign carrier of \autoref{def:intermediate-value-located-sign-carrier}, the bisection, handoff, transport, and local naming obligations of \autoref{thm:intermediate-value-dyadic-bisection-obligations}, \autoref{thm:intermediate-value-real-seal-handoff}, \autoref{thm:intermediate-value-bisection-convergence-handoff}, \autoref{thm:intermediate-value-modulus-branch-transport}, \autoref{thm:intermediate-value-namecert-obligations}, and \autoref{thm:intermediate-value-obligation-closure-package}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close open-cover compactness, quotient reals, host equality of real values, real trichotomy, selected roots, infinite nested intersections, or completeness of $\RealUp$.}
  \upgradepath{A checked carrier theorem and obligation package for the same $\LocatedIntervalUp$, $\DyadicBisectionUp$, $\NestedIntervalUp$, $\ContinuousMapUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route would raise the verification axis.}
\end{closurestatus}
