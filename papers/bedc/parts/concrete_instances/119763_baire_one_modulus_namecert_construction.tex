\chapter{A Concrete Naming Certificate for $\BaireOneModulusUp$}
\label{ch:concrete-instances-baire-one-modulus-namecert}
\origin{human}

Following Baire's function hierarchy and Bishop--Bridges constructive analysis, the $\BaireOneModulusUp$ packet records finite oscillation-window moduli for displayed Baire-one pointwise-limit names. It sits after the pointwise-limit carrier of \autoref{ch:concrete-instances-baireonefunction-namecert}, the continuous approximant rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite stream schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the lower-semicontinuous handoff of \autoref{ch:concrete-instances-lowersemicontinuous-namecert}, and the source prefix context of \autoref{ch:concrete-instances-bairespace-namecert}. The packet names a finite oscillation-modulus companion to the Baire-one function ledger; it is not an ambient space of functions, not a selected approximating sequence, not quotient equality of real names, not a full measurability theorem, and not a completeness principle for $\RealUp$.

\begin{definition}[Baire-one modulus carrier]
\label{def:baire-one-modulus-carrier}
A $\BaireOneModulusUp$ carrier is a finite $\BHist$ packet
$$
K=(X,F,S,O,M,Q,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\BaireSpaceUp$ source-context row naming the inspected source points and finite prefix information;
\item $F$ is the finite family ledger of $\ContinuousMapUp$ approximants read by the companion $\BaireOneFunctionUp$ packet;
\item $S$ is the $\StreamNameUp$ schedule row selecting the finite approximant and source windows inspected by the modulus request;
\item $O$ records the finite oscillation windows over the selected source and approximant rows;
\item $M$ records modulus witnesses that turn each displayed oscillation window into a tolerance-controlled finite read;
\item $Q$ is the $\RegSeqRatUp$ readback row for the rational tolerances and approximant-value windows selected by $S,O,M$;
\item $R$ is the terminal $\RealUp$ value-seal row reached only after the displayed modulus windows and readbacks have been inspected;
\item $L$ is the $\LowerSemicontinuousUp$ handoff row receiving the same finite oscillation-modulus ledger;
\item $H$ records componentwise $\hsame$ transport for the source, approximant, schedule, oscillation, modulus, readback, real-seal, handoff, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from a source-point and tolerance request through $X,F,S,O,M,Q,R,L$;
\item $P$ is the $\Pkg$ provenance over $\BaireOneModulusUp$, $\BaireOneFunctionUp$, $\BaireSpaceUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LowerSemicontinuousUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\BaireOneModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for an ambient function space, arbitrary approximant choice, quotient real equality, full measurability, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Baire-one modulus NameCert obligations]
\label{thm:baire-one-modulus-obligations}
For every accepted $\BaireOneModulusUp$ carrier
$$
K=(X,F,S,O,M,Q,R,L,H,C,P,N),
$$
the local $\NameCert_{\BaireOneModulusUp}$ surface consists exactly of carrier admission, continuous approximant admission, pointwise schedule exposure, oscillation-window induction, modulus witness exposure, real-seal nonescape, lower-semicontinuous handoff, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier admission projects the twelve displayed rows of \autoref{def:baire-one-modulus-carrier}. The source row $X$, approximant ledger $F$, and schedule row $S$ are the only entries by which a consumer reaches the Baire-one pointwise-limit surface.

Oscillation-window induction is finite because $O$ is indexed by the displayed schedule. Each step exposes a selected source window and approximant window before the modulus row $M$ may answer the tolerance request.

The readback and value rows $Q,R$ are downstream of the same finite route, and the lower-semicontinuous handoff $L$ consumes only that route. The structural rows $H,C,P,N$ transport, replay, source, and name this packet, so no excluded ambient function-space, choice, quotient, measurability, or completeness principle is available.
\end{proof}

\falsifiablePrediction{Every accepted $\BaireOneModulusUp$ read exposes BaireSpace source data, ContinuousMap approximant rows, StreamName schedules, oscillation windows, modulus witnesses, RegSeqRat tolerance readbacks, RealUp seals, LowerSemicontinuous handoff rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming before any oscillation-modulus consumer receives the packet.}

\independenceWitness{baireonefunction, bairespace, continuousmap, streamname, regseqrat, real, lowersemicontinuous: no listed sibling supplies the full twelve-row packet $K=(X,F,S,O,M,Q,R,L,H,C,P,N)$, because each sibling contributes only the pointwise-limit ledger, source context, approximant admission, finite schedule, rational readback, real seal, or lower-semicontinuous handoff while this chapter binds oscillation windows and modulus witnesses into one local NameCert carrier.}

\closureat{BaireOneModulusUp}{seedStr}

\begin{closurestatus}{\BaireOneModulusUp}
  \constructivestory{A $\BaireOneModulusUp$ packet is generated from a BaireSpace source row, ContinuousMap approximant rows, StreamName schedules, finite oscillation windows, modulus witnesses, RegSeqRat tolerance readbacks, RealUp seals, a LowerSemicontinuous handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:baire-one-modulus-carrier} and \autoref{thm:baire-one-modulus-obligations} seed the finite oscillation-modulus companion to the Baire-one pointwise-limit packet over displayed source, approximant, schedule, oscillation, modulus, readback, seal, handoff, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient function spaces, arbitrary approximant choice, quotient real equality, full measurability, $\RealUp$ completeness, Lean verification, axiom purity, or bridge checking.}
  \upgradepath{Obligation closure asks for checked carrier admission, continuous approximant admission, pointwise schedule exposure, oscillation-window induction, modulus witness exposure, real-seal nonescape, lower-semicontinuous handoff, transport, replay, provenance, and local naming for BEDC.Derived.BaireOneModulusUp.}
\end{closurestatus}
