$\CauchySequentialCriterionUp$ records the L10 finite Cauchy-window route consumed by \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-real-namecert}; the child files below keep the NameCert body and sibling routes separate.

\input{parts/concrete_instances/cauchy_sequential_criterion/namecert_construction.tex}
