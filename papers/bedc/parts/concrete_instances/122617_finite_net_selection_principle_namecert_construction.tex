\chapter{A Concrete Naming Certificate for $\FiniteNetSelectionPrincipleUp$}
\label{ch:concrete-instances-finite-net-selection-principle-namecert}
\origin{human}

Following Bishop and Bridges on constructive compactness and total boundedness, the $\FiniteNetSelectionPrincipleUp$ packet records finite net selection as displayed interval or metric data rather than as an open-cover compactness principle. It sits after the finite interval mesh discipline of \autoref{ch:concrete-instances-finite-interval-mesh-refinement-namecert}, the modulus-controlled completion route of \autoref{ch:concrete-instances-cauchy-uniform-continuation-namecert}, and the density window surface of \autoref{ch:concrete-instances-real-separability-namecert}. The packet supplies the Real and Completion corridor with an L10 selector object for finite dyadic nets, without claiming a host choice function, selected Cauchy limits, Heine--Borel compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Finite net selection principle carrier]
\label{def:finite-net-selection-principle-carrier}
A $\FiniteNetSelectionPrincipleUp$ carrier is a finite $\BHist$ packet
$$
S=(X,L,M,Q,W,R,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the certified located interval or located metric source row whose points may be inspected only through displayed finite probes.
\item $L$ is the locatedness row, giving the interval endpoint or metric-ball witness used to decide finite covering incidence.
\item $M$ is the finite dyadic mesh or finite metric-net row, read through $\FiniteIntervalMeshRefinementUp$ when the source is an interval and through the accepted total-bounded or compact-metric net row when the source is metric.
\item $Q$ is the finite selector table assigning each requested dyadic or metric tolerance to a displayed finite list of net centers.
\item $W$ is the mesh monotonicity row, saying that a sharper requested tolerance refines the previous finite selector table through displayed parent-cell or parent-ball incidence.
\item $R$ is the cover soundness row, recording that every admitted source probe is covered by one displayed net center at the requested tolerance.
\item $T$ is the selector stability row, requiring $\hsame$ transport to preserve source rows, located witnesses, mesh rows, selector tables, monotonicity rows, cover rows, replay, provenance, and local naming.
\item $H$ records componentwise $\hsame$ transport for the displayed selector route.
\item $C$ records displayed $\Cont$ replay from a finite-net request through $X,L,M,Q,W,R,T,H$.
\item $P$ is the $\Pkg$ provenance over $\FiniteNetSelectionPrincipleUp$, $\FiniteIntervalMeshRefinementUp$, $\CauchyUniformContinuationUp$, $\RealSeparabilityUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteNetSelectionPrincipleUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite open cover, a global choice function, a selected limit point, quotient completion equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.FiniteNetSelectionPrincipleUp}

\begin{theorem}[Finite net selection principle obligations]
\label{thm:finite-net-selection-principle-obligation}
For every accepted $\FiniteNetSelectionPrincipleUp$ carrier
$$
S=(X,L,M,Q,W,R,T,H,C,P,N),
$$
the local $\NameCert_{\FiniteNetSelectionPrincipleUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the source $X$, locatedness witness $L$, mesh or net row $M$, selector table $Q$, monotonicity row $W$, cover row $R$, selector stability row $T$, transport row $H$, replay row $C$, provenance row $P$, and local name $N$;
\item classifier locality, comparing accepted selectors only through the displayed located source, finite mesh or net, selector table, monotonicity, cover, stability, transport, replay, provenance, and naming rows;
\item mesh monotonicity, requiring sharper tolerances to refine the selected finite net through the parent-cell or parent-ball incidence recorded in $W$;
\item cover soundness, requiring every admitted located source probe to be covered by a displayed net center before a Real or Completion consumer reads the selector;
\item selector stability, requiring $T,H,C,P,N$ to preserve, replay, source, and locally name the same finite selector route under accepted renamings.
\end{enumerate}
No finite net selector obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-net-selection-principle-carrier}. The source row, locatedness row, mesh or net row, selector table, monotonicity row, cover row, and structural rows are the whole displayed packet.

Carrier admission and classifier locality are therefore row inventory facts. A consumer can compare two accepted selectors only by reading $X,L,M,Q,W,R,T,H,C,P,N$, so no hidden open-cover or choice coordinate can enter the classifier.

Mesh monotonicity is read from $W$. A sharper tolerance may subdivide interval cells or metric balls, but the refinement must point back to displayed parent incidence in the same finite route.

Cover soundness is read from $R$. The selected net is usable only after every admitted located probe has a displayed center at the requested tolerance.

Finally $T,H,C,P,N$ transport, replay, source, and locally name the same finite route. Since the carrier contains no row for infinite covers, global choice functions, selected limits, quotient completion equality, countable choice, or ambient Real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite net selection principle ledger]
\label{thm:finite-net-selection-principle-ledger}
Every Real or Completion consumer of an accepted $\FiniteNetSelectionPrincipleUp$ packet must read the finite route
$$
\FiniteIntervalMeshRefinementUp \longmapsto \FiniteNetSelectionPrincipleUp \longmapsto \CauchyUniformContinuationUp \longmapsto \RealSeparabilityUp
$$
through the displayed rows $X,L,M,Q,W,R,T,H,C,P,N$ before it treats a finite net as available selector data. The ledger exports a finite dyadic or metric net handoff; it exports no classical open-cover compactness theorem, no choice-based Heine--Borel principle, no selected Cauchy limit, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:finite-net-selection-principle-obligation}. They force every selector read to expose the located source, finite mesh or net, selector table, monotonicity, cover, stability, transport, replay, provenance, and local naming rows.

The interval-facing branch reads $M$ through \autoref{ch:concrete-instances-finite-interval-mesh-refinement-namecert}. Thus dyadic refinement data remain finite parent-cell data before any selector table is public.

The completion-facing branch then reads continuation requests through \autoref{ch:concrete-instances-cauchy-uniform-continuation-namecert}. The selector table can be used only as a finite window for a modulus-controlled Cauchy route, not as a detached completed value.

The Real-facing density branch reads \autoref{ch:concrete-instances-real-separability-namecert} only after the selector has displayed its net centers and cover row. Hence the consumer receives finite density windows and not a host dense subset, a countable-choice enumeration, or an ambient compactness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteNetSelectionPrincipleUp$ read exposes a located interval or metric source row, locatedness witness, finite dyadic mesh or metric-net row, selector table, mesh monotonicity row, cover soundness row, selector stability row, hsame transport, Cont replay, provenance, and local NameCert before a Real or Completion consumer reads finite-net selector data.}

\independenceWitness{finite-interval-mesh-refinement, cauchy-uniform-continuation, real-separability: no listed sibling simultaneously carries located source witnesses, finite selector tables, mesh monotonicity, cover soundness, selector stability, completion continuation handoff, density-window handoff, transport, replay, provenance, and local NameCert rows for constructive finite-net selection.}

\closureat{FiniteNetSelectionPrincipleUp}{seedStr}

\begin{closurestatus}{\FiniteNetSelectionPrincipleUp}
  \constructivestory{A $\FiniteNetSelectionPrincipleUp$ packet is generated from a certified located interval or metric source, a locatedness witness, a finite dyadic mesh or metric-net row, a finite selector table, mesh monotonicity, cover soundness, selector stability, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-net-selection-principle-carrier}, \autoref{thm:finite-net-selection-principle-obligation}, and \autoref{thm:finite-net-selection-principle-ledger} seed the finite selector route for located interval and metric finite-net reads in the Real and Completion corridor.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.FiniteNetSelectionPrincipleUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim classical open-cover compactness, choice-based Heine--Borel selection, global choice functions, selected Cauchy limits, quotient completion equality, ambient RealUp completeness, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier locality, mesh monotonicity, cover soundness, selector stability, transport, replay, provenance, and ledger refusal for BEDC.Derived.FiniteNetSelectionPrincipleUp.}
\end{closurestatus}
