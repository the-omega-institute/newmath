\chapter{A Concrete Naming Certificate for $\KatetovTongInsertionUp$}
\label{ch:concrete-instances-katetov-tong-insertion-namecert}
\origin{human}

Following Katetov, Tong, and Dowker, the $\KatetovTongInsertionUp$ packet records the finite normal-space insertion route by which a lower semicontinuous row and an upper semicontinuous row are separated by a displayed continuous real-valued function. It sits over the normal separation surface of \autoref{ch:concrete-instances-normalspace-namecert}, the Urysohn separation route of \autoref{ch:concrete-instances-urysohn-lemma-namecert}, the extension handoff of \autoref{ch:concrete-instances-tietze-extension-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite insertion certificate only; it does not import host choice, quotient function equality, selected pointwise suprema, or ambient $\RealUp$ completeness.

\begin{definition}[Katetov--Tong insertion carrier]
\label{def:katetov-tong-insertion-carrier}
A $\KatetovTongInsertionUp$ carrier is a finite $\BHist$ packet
$$
I=(N,L,U,S,T,C,E,A,H,Q,P,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $N$ is the $\NormalSpaceUp$ source row whose separated closed-neighborhood data are inspected.
\item $L$ is the lower-row ledger for the displayed lower semicontinuous boundary.
\item $U$ is the upper-row ledger for the displayed upper semicontinuous boundary.
\item $S$ records the strict pointwise separation budget between the $L$ and $U$ rows through terminal $\RealUp$ seals.
\item $T$ is the local Urysohn/Tietze route that turns the separated neighborhood data into a continuous real-valued candidate.
\item $C$ is the $\ContinuousMapUp$ row of the inserted function.
\item $E$ records the endpoint inequalities saying that the inserted function lies between the lower and upper rows on every displayed probe.
\item $A$ records finite approximation windows used to read the lower row, upper row, candidate map, and endpoint inequalities through the same $\RealUp$ seal.
\item $H$ records componentwise $\hsame$ transport for the normal source, semicontinuity rows, separation budget, Urysohn/Tietze route, continuous map, endpoint inequalities, approximation windows, replay, provenance, and local name.
\item $Q$ records displayed $\Cont$ replay for the same insertion route.
\item $P$ is the $\Pkg$ provenance over $\KatetovTongInsertionUp$, $\NormalSpaceUp$, $\UrysohnLemmaUp$, $\TietzeExtensionUp$, $\ContinuousMapUp$, $\RealUp$, finite insertion ledgers, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $M$ is the local $\NameCert_{\KatetovTongInsertionUp}$ row.
\end{enumerate}
The carrier exports no host choice principle, quotient equality of functions, untracked real-valued map, selected supremum construction, or ambient completeness theorem.
\end{definition}

\begin{theorem}[Katetov--Tong insertion NameCert obligations]
\label{thm:katetov-tong-insertion-namecert-obligations}
For every accepted $\KatetovTongInsertionUp$ carrier
$$
I=(N,L,U,S,T,C,E,A,H,Q,P,M),
$$
the local $\NameCert_{\KatetovTongInsertionUp}$ surface consists exactly of carrier admission, normal-source exposure, lower-row and upper-row visibility, strict real-valued separation, Urysohn/Tietze route discipline, continuous-map insertion, endpoint inequality exactness, approximation-window replay, componentwise transport, package provenance, and local naming. No insertion obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:katetov-tong-insertion-carrier}. The rows $N,L,U,S,T,C,E,A,H,Q,P,M$ are the whole public surface, so carrier admission is exactly admission of those displayed coordinates.

The normal-source and separation obligations are read through $N,L,U,S$. The lower and upper rows are compared only through the displayed $\RealUp$ seals in $S$, and the route $T$ records the Urysohn/Tietze separation data used to build the inserted function.

The inserted function itself is the $\ContinuousMapUp$ row $C$, with endpoint exactness recorded by $E$ and finite readback windows recorded by $A$. Transport, replay, provenance, and local naming are then confined to $H,Q,P,M$, leaving no coordinate for host choice, quotient function equality, selected pointwise suprema, or ambient real completeness.
\end{proof}

\begin{theorem}[Katetov--Tong normal-space route]
\label{thm:katetov-tong-insertion-normal-space-route}
Every accepted $\KatetovTongInsertionUp$ packet factors any insertion consumer through the finite route
$$
\NormalSpaceUp \longmapsto \UrysohnLemmaUp \longmapsto \TietzeExtensionUp \longmapsto \ContinuousMapUp \longmapsto \RealUp .
$$
Thus the consumer first exposes normal separation, then the local Urysohn/Tietze construction route, then the continuous inserted function and its real endpoint inequalities. It receives neither a host choice functional nor quotient equality of functions.
\end{theorem}
\begin{proof}
Start with the normal-space source row $N$ and the separated lower and upper ledgers $L,U,S$. These rows provide the only closed-neighborhood and real-valued comparison data visible to the insertion request.

Read the route row $T$ next. It records the local Urysohn and Tietze handoff through which the separated normal-space data can be turned into a continuous candidate without adding a host selector.

Finally read the continuous-map row $C$, the endpoint row $E$, and the approximation row $A$. The endpoint inequalities are checked through the same $\RealUp$ seal used by the lower and upper ledgers, while $H,Q,P,M$ keep transport, replay, provenance, and naming attached to the same finite carrier. Hence every insertion consumer stays on the displayed normal-space route and cannot claim quotient function equality, a selected supremum, host choice, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\KatetovTongInsertionUp$ read exposes the $\NormalSpaceUp$ source row, lower and upper semicontinuity ledgers, strict $\RealUp$ separation budget, Urysohn/Tietze route row, $\ContinuousMapUp$ inserted function, endpoint inequality ledger, finite approximation windows, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming before any insertion consumer may inspect the real-valued function.}

\independenceWitness{normalspace, urysohn-lemma, tietze-extension, continuousmap, real: $\KatetovTongInsertionUp$ is not bijective to any listed sibling because it jointly binds lower and upper semicontinuity ledgers, strict real-valued separation budgets, Urysohn/Tietze construction rows, inserted continuous-map rows, endpoint inequalities, finite approximation windows, componentwise transport, replay, provenance, and local NameCert rows while refusing host choice and quotient function equality.}

\closureat{KatetovTongInsertionUp}{seedStr}

\begin{closurestatus}{\KatetovTongInsertionUp}
  \constructivestory{A $\KatetovTongInsertionUp$ packet is generated from a normal-space source row, lower and upper semicontinuity ledgers, a strict real-valued separation budget, a Urysohn/Tietze route row, a continuous-map inserted function, endpoint inequalities, finite approximation windows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:katetov-tong-insertion-carrier}, \autoref{thm:katetov-tong-insertion-namecert-obligations}, and \autoref{thm:katetov-tong-insertion-normal-space-route} seed the finite normal-space insertion certificate for L10 continuous real-valued consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host choice, quotient equality of functions, selected pointwise suprema, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, normal-source exposure, lower and upper row visibility, strict real-valued separation, Urysohn/Tietze route discipline, continuous-map insertion, endpoint exactness, approximation replay, transport, provenance, and nonescape for BEDC.Derived.KatetovTongInsertionUp.}
\end{closurestatus}
