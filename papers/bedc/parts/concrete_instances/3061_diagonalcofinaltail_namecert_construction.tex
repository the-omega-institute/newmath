\chapter{Diagonal Cofinal Tail Name Certificate Construction}
\label{ch:concrete-instances-diagonalcofinaltail-namecert}
\origin{ai}

The $\DiagonalCofinalTailUp$ packet realizes the cofinal-tail reading of the transported stationary diagonal window as a finite $\NameCert$ surface. It keeps the rational seed classifier, the constant stream windows, the regular-rational tail readbacks, the dyadic finite-window ledgers, the real-facing seal row, transport rows, continuation routes, provenance, and local naming in one displayed packet. It does not export an external equality identification, a selected limit, an unrecorded window selector, or an ambient completeness principle.

\begin{definition}[Diagonal cofinal-tail carrier]
\label{def:diagonal-cofinal-tail-carrier}
A $\DiagonalCofinalTailUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
T=(Q,S,G,D,R,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the paired stationary $\RatUp$ seed row, including the two rational history carriers and their $\mathsf{RatHistoryClassifier}$ row;
\item $S$ is the paired constant $\StreamNameUp$ window row induced by $Q$;
\item $G$ is the $\RegSeqRatUp$ tail readback row for the selected diagonal window;
\item $D$ is the $\DyadicRatCoreUp$ finite-window ledger for the same selected tail window;
\item $R$ is the $\RealUp$ diagonal representative row read only through $S,G,D$;
\item $W$ records the transported stationary window seal whose RatUp, StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, and RealUp readings are the four public readings of \autoref{thm:real-completeness-transported-stationary-window-seal-exhaustion};
\item $H$ records componentwise $\hsame$ transports for $Q,S,G,D,R,W$ and the structural rows;
\item $C$ records the displayed $\Cont$ routes from $Q$ to $S$, from $S$ to $G$, from $G$ to $D$, from $D$ to $R$, and from $W$ to each public window reading;
\item $P$ is the $\Pkg$ provenance over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\DiagonalCofinalTailUp}$ row.
\end{enumerate}
The carrier is finite. It has no coordinate for an infinite window family, external equality identification, selected real limit, unrecorded selector, or ambient completeness theorem.
\end{definition}

\begin{theorem}[Diagonal cofinal-tail NameCert obligations]
\label{thm:diagonal-cofinal-tail-namecert-obligations}
The local $\NameCert_{\DiagonalCofinalTailUp}$ surface for \autoref{def:diagonal-cofinal-tail-carrier} consists exactly of carrier admission for $T=(Q,S,G,D,R,W,H,C,P,N)$, stationary $\RatUp$ seed exposure through $Q$, constant $\StreamNameUp$ window exposure through $S$, $\RegSeqRatUp$ cofinal-tail readback exposure through $G$, $\DyadicRatCoreUp$ finite-window ledger exposure through $D$, $\RealUp$ diagonal representative exposure through $R$, the transported stationary seal row $W$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ route replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet $T$. The rows $Q,S,G,D,R$ expose the rational seeds, constant stream windows, regular-rational readbacks, dyadic window ledgers, and real-facing representatives.

The seal row $W$ is read through the finite-window seal exhaustion theorem in \autoref{thm:real-completeness-transported-stationary-window-seal-exhaustion}. Its public readings are already the RatUp seed classifier, the StreamNameUp constant-window classifier, the RegSeqRatUp finite-window seal consumption row, the DyadicRatCoreUp observation ledger, and the RealUp finite-window classifier.

The rows $H,C,P,N$ account for transport, route replay, package provenance, and local naming. Since the excluded analytic objects have no coordinate in $T$, the listed rows exhaust the local $\NameCert$ obligations.
\end{proof}

\begin{theorem}[Diagonal cofinal-tail window exhaustion]
\label{thm:diagonal-cofinal-tail-window-exhaustion}
Every certified finite cofinal-tail observation exported by an accepted $\DiagonalCofinalTailUp$ carrier factors through the finite rows
$$
Q,S,G,D,R,W,H,C,P,N.
$$
In particular, the consumer may inspect only the displayed RatUp seed classifier, the constant StreamNameUp window, the RegSeqRatUp tail readback, the DyadicRatCoreUp finite-window ledger, the RealUp diagonal representative, the transported seal row, transports, continuations, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier and its local naming surface. The observation rows are $Q,S,G,D,R,W$, and the structural rows are $H,C,P,N$.

Apply \autoref{thm:real-completeness-transported-stationary-window-seal-exhaustion} to the window read by $W$. It says that the finite StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, and RealUp readings all agree with the same RatUp seed classifier.

The continuations in $C$ replay only those readings, and $H$ transports them componentwise. No carrier coordinate remains for an unbounded window family, so every cofinal-tail observation is exhausted by the displayed finite packet.
\end{proof}

\begin{theorem}[Diagonal cofinal-tail selector lock]
\label{thm:diagonal-cofinal-tail-tail-selector-lock}
For an accepted $\DiagonalCofinalTailUp$ carrier
$$
T=(Q,S,G,D,R,W,H,C,P,N),
$$
any cofinal-tail selector visible to a $\RegSeqRatUp$ or $\RealUp$ consumer is determined by the displayed stationary $\RatUp$ seed row $Q$, the constant $\StreamNameUp$ window row $S$, the $\RegSeqRatUp$ tail readback row $G$, the $\DyadicRatCoreUp$ finite-window ledger $D$, the transported seal row $W$, and the route rows $H,C,P,N$. No unrecorded selector, quotient-stream equality, selected limit, or ambient completeness datum can select a different tail for that consumer.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only tail-bearing rows are the stationary seed row, the constant window row, the regular-rational readback row, the dyadic finite-window ledger, and the transported seal row, with $H,C,P,N$ giving transport, route replay, provenance, and local naming.

Apply \autoref{thm:diagonal-cofinal-tail-window-exhaustion}. It forces every finite cofinal-tail observation read by a downstream consumer through the same displayed packet $Q,S,G,D,R,W,H,C,P,N$.

Now use \autoref{thm:diagonal-cofinal-tail-real-seal-handoff}. The $\RealUp$-facing read is the transported stationary diagonal row named by the $\RatUp$ seed classifier, so the real-facing selector and the $\RegSeqRatUp$ tail readback are the same finite route.

Since the carrier contains no coordinate for an unrecorded selector, quotient-stream equality, selected limit, or ambient completeness datum, no consumer-visible route remains that could choose a different cofinal tail.
\end{proof}

\begin{theorem}[Diagonal cofinal-tail Real seal handoff]
\label{thm:diagonal-cofinal-tail-real-seal-handoff}
For an accepted $\DiagonalCofinalTailUp$ carrier, the $\RealUp$ finite-window classifier exported by $R$ and $W$ is reached only through the displayed StreamNameUp window, RegSeqRatUp seal consumption row, and DyadicRatCoreUp finite-window ledger. Thus the real-facing seal handoff is the same transported stationary diagonal row named by the RatUp seed classifier in $Q$.
\end{theorem}
\begin{proof}
Read the seal row $W$ and the representative row $R$. The transported stationary diagonal theorem identifies the real-facing diagonal representatives with the constant StreamNameUp row induced by $Q$.

Now restrict to the window selected by $D$. The finite-window seal exhaustion theorem forces the RegSeqRatUp seal consumption row and the DyadicRatCoreUp ledger to be the public middle rows through which the RealUp classifier is read.

Therefore the real-facing handoff cannot introduce another comparison datum. It is the same transported stationary diagonal row replayed through $S,G,D,R,W$ and the structural routes $H,C,P,N$.
\end{proof}

\begin{theorem}[Diagonal cofinal-tail non-escape]
\label{thm:diagonal-cofinal-tail-nonescape}
An accepted $\DiagonalCofinalTailUp$ packet has no observation path outside the finite cofinal-tail rows of \autoref{def:diagonal-cofinal-tail-carrier}. Any downstream route that consumes its RealUp seal, RegSeqRatUp readback, DyadicRatCoreUp ledger, StreamNameUp window, or RatUp seed classifier must factor through $Q,S,G,D,R,W,H,C,P,N$.
\end{theorem}
\begin{proof}
The NameCert obligations identify every row that may be exported from the packet. The window exhaustion theorem shows that the finite cofinal-tail observations factor through those rows.

The Real seal handoff theorem then covers the only real-facing consumer path. It routes the RealUp classifier through the StreamNameUp, RegSeqRatUp, and DyadicRatCoreUp rows already named by the RatUp seed classifier.

Since no further coordinate is present, a downstream consumer cannot leave the packet to obtain an external equality identification, selected limit, unrecorded selector, or ambient completeness witness.
\end{proof}

\closureat{DiagonalCofinalTailUp}{seedStr}

\begin{closurestatus}{\DiagonalCofinalTailUp}
  \constructivestory{A $\DiagonalCofinalTailUp$ packet is generated from paired stationary RatUp seed rows, constant StreamNameUp windows, RegSeqRatUp cofinal-tail readbacks, DyadicRatCoreUp finite-window ledgers, RealUp diagonal representative rows, a transported stationary seal row, componentwise transports, continuation routes, package provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:diagonal-cofinal-tail-carrier}, \autoref{thm:diagonal-cofinal-tail-namecert-obligations}, \autoref{thm:diagonal-cofinal-tail-window-exhaustion}, \autoref{thm:diagonal-cofinal-tail-real-seal-handoff}, and \autoref{thm:diagonal-cofinal-tail-nonescape} bind the finite cofinal-tail NameCert packet for the transported stationary diagonal window over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close RealUp completeness, external equality identification, selected real limits, unbounded diagonal families, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cofinal-tail window exhaustion, Real seal handoff, and non-escape for the same finite packet.}
\end{closurestatus}
