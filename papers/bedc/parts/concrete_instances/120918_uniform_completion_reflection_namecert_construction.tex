\chapter{A Concrete Naming Certificate for $\UniformCompletionReflectionUp$}
\label{ch:concrete-instances-uniform-completion-reflection-namecert}
\origin{human}

Following Bishop and Bridges and Kelley-style constructive uniform completion, the $\UniformCompletionReflectionUp$ packet records the finite reflector route by which a uniform source is sent to a separated completion-facing object without quotienting Cauchy filters or selecting limits. It sits beside the dense-source route of \autoref{ch:concrete-instances-cauchy-completion-density-source-namecert}, the metric dense embedding of \autoref{ch:concrete-instances-metric-completion-dense-embedding-namecert}, the separated completion reflection of \autoref{ch:concrete-instances-separated-completion-reflection-namecert}, and the uniform-space completion packet of \autoref{ch:concrete-instances-uniform-space-completion-namecert}. Its finite readback rows are anchored by \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Uniform completion reflection carrier]
\label{def:uniform-completion-reflection-carrier}
A $\UniformCompletionReflectionUp$ carrier is a finite $\BHist$ packet
$$
U=(S,F,D,E,R,W,Q,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source uniformity row, exposing the uniform entourage data admitted by the reflector.
\item $F$ is the Cauchy-filter window row, recording the finite filter requests inspected before a completion-facing read is accepted.
\item $D$ is the dense embedding row, read through $\CauchyCompletionDensitySourceUp$ and $\MetricCompletionDenseEmbeddingUp$ before any reflected completion point is public.
\item $E$ is the separated reflection row, read through $\SeparatedCompletionReflectionUp$ before the completion-facing seal can be compared.
\item $R$ is the universal arrow row, recording the $\UniformSpaceCompletionUp$ handoff available to uniform-continuous consumers.
\item $W$ is the $\StreamNameUp$ finite-window row used to inspect Cauchy-filter evidence.
\item $Q$ is the $\RegSeqRatUp$ readback row for regular rational observations extracted from the displayed windows.
\item $Z$ is the $\DyadicRatCoreUp$ tolerance ledger and terminal $\RealUp$ sealing route for the inspected comparison.
\item $H$ records componentwise $\hsame$ transport for $S,F,D,E,R,W,Q,Z,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a reflector request through the source uniformity, Cauchy-filter windows, dense embedding, separated reflection, universal arrow, numerical readback, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\UniformCompletionReflectionUp$, $\CauchyCompletionDensitySourceUp$, $\MetricCompletionDenseEmbeddingUp$, $\SeparatedCompletionReflectionUp$, $\UniformSpaceCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformCompletionReflectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,F,D,E,R,W,Q,Z,H,C,P,N$. It has no coordinate for quotient completion classes, selected filter representatives, selected limits, countable choice, host equality of completed points, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Uniform completion reflection NameCert obligations]
\label{thm:uniform-completion-reflection-namecert-obligations}
For every accepted $\UniformCompletionReflectionUp$ carrier $U=(S,F,D,E,R,W,Q,Z,H,C,P,N)$, the local $\NameCert_{\UniformCompletionReflectionUp}$ surface consists exactly of source uniformity admission, Cauchy-filter window exposure, dense embedding exposure through $\CauchyCompletionDensitySourceUp$ and $\MetricCompletionDenseEmbeddingUp$, separated reflection exposure through $\SeparatedCompletionReflectionUp$, universal-arrow exposure through $\UniformSpaceCompletionUp$, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance and $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:uniform-completion-reflection-carrier}. The tuple displays the source uniformity, Cauchy-filter windows, dense embedding, separated reflection, universal arrow, finite windows, regular rational readback, dyadic and Real sealing, transport, replay, provenance, and local naming rows.

The completion-facing obligations are downstream from the finite source evidence. A consumer first reads $S$ and $F$, then the dense-source rows $D$, then the separated reflection row $E$, and only then the universal arrow row $R$.

The numerical and structural rows $W,Q,Z,H,C,P,N$ preserve the same route. Since the packet contains no quotient class, selected representative, selected limit, countable-choice schedule, host equality, or ambient completeness coordinate, the NameCert surface is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[Uniform completion reflection density route]
\label{thm:uniform-completion-reflection-density}
Every density read exported by an accepted $\UniformCompletionReflectionUp$ packet factors through $\CauchyCompletionDensitySourceUp$ and $\MetricCompletionDenseEmbeddingUp$ before the separated reflection or universal-arrow rows are consumed. The density route is carried by $S,F,D,W,Q,Z,H,C,P,N$ and remains a finite BEDC packet, not a quotient completion or selected representative.
\end{theorem}
\begin{proof}
Read the source uniformity row $S$ and Cauchy-filter window row $F$. These rows bound the request before any dense image can be inspected.

The dense embedding row $D$ is then supplied by the dense-source and metric dense-embedding packets. Its readback still passes through the StreamName, RegSeqRat, DyadicRatCore, and Real rows $W,Q,Z$.

Transport, replay, provenance, and local naming keep the route within $H,C,P,N$. Thus density is exported only as displayed finite evidence, with no quotient completion class or selected representative available to the consumer.
\end{proof}

\begin{theorem}[Uniform completion reflection universal route]
\label{thm:uniform-completion-reflection-universal-route}
Every universal-arrow read from an accepted $\UniformCompletionReflectionUp$ packet factors through
$$
S,F,D,E,W,Q,Z,H,C,P,N \longmapsto R .
$$
The row $R$ may be consumed by $\UniformSpaceCompletionUp$-facing maps only after the source uniformity, Cauchy-filter window, dense embedding, separated reflection, numerical readback, transport, replay, provenance, and local naming rows remain visible in the same packet.
\end{theorem}
\begin{proof}
Start with \autoref{thm:uniform-completion-reflection-namecert-obligations}. It places the universal arrow after the source uniformity, Cauchy-filter windows, dense embedding, separated reflection, and numerical readback rows.

The density route of \autoref{thm:uniform-completion-reflection-density} supplies the source-to-completion side. The separated reflection row $E$ then prevents the consumer from treating zero-distance evidence as host equality before the reflection boundary is displayed.

Finally project $R$ through $H,C,P,N$. The universal route is therefore a replayable NameCert row for $\UniformSpaceCompletionUp$ consumers, not a selected-limit operator, quotient completion theorem, countable-choice principle, or ambient separated completion.
\end{proof}

\falsifiablePrediction{Every accepted $\UniformCompletionReflectionUp$ consumer that reaches the universal arrow while omitting source uniformity, Cauchy-filter windows, dense embedding, separated reflection, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, Real sealing, transport, replay, provenance, or local naming refutes the packet.}

\independenceWitness{cauchy-completion-density-source, metric-completion-dense-embedding, separated-completion-reflection, uniform-space-completion, streamname, regseqrat, dyadicratcore, real: no listed sibling is bijective with $\UniformCompletionReflectionUp$ because the packet jointly carries source uniformity, Cauchy-filter windows, dense embedding, separated reflection, universal-arrow routing, finite windows, regular rational readback, dyadic tolerance, Real sealing, transport, replay, provenance, and local naming.}

\closureat{UniformCompletionReflectionUp}{seedStr}

\begin{closurestatus}{\UniformCompletionReflectionUp}
  \constructivestory{A $\UniformCompletionReflectionUp$ packet is generated from a source uniformity row, Cauchy-filter windows, dense embedding rows, separated reflection rows, a universal-arrow row, StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-completion-reflection-carrier}, \autoref{thm:uniform-completion-reflection-namecert-obligations}, \autoref{thm:uniform-completion-reflection-density}, and \autoref{thm:uniform-completion-reflection-universal-route} seed the constructive uniform-completion reflector route from source uniformity and Cauchy-filter windows through dense embedding, separated reflection, and universal-arrow rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion, selected filter representatives, selected limits, countable choice, trichotomy, host equality of completed points, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-uniformity exactness, Cauchy-filter window exposure, dense embedding handoff, separated reflection non-escape, universal-arrow routing, finite numerical readback, transport, replay, provenance, and local naming for BEDC.Derived.UniformCompletionReflectionUp.}
\end{closurestatus}
