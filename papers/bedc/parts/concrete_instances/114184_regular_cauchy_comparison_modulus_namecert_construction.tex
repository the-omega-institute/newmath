\chapter{A Concrete Naming Certificate for $\RegularCauchyComparisonModulusUp$}
\label{ch:concrete-instances-regular-cauchy-comparison-modulus-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names with explicit moduli, the $\RegularCauchyComparisonModulusUp$ packet records the finite comparison-modulus data by which two regular rational names can be compared before equality, convergence-modulus, or completion-facing consumers read a terminal real boundary. It sits after the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the common-tail comparison surface of \autoref{ch:concrete-instances-cauchy-tail-comparison-namecert}. The packet is a finite L10 comparison-modulus surface for RealEquality and RealModulusOfConvergence consumers; it is not quotient stream equality, a selected limit, trichotomy, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy comparison modulus carrier]
\label{def:regular-cauchy-comparison-modulus-carrier}
A $\RegularCauchyComparisonModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(S_0,S_1,R_0,R_1,D,\mu,W,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S_0$ and $S_1$ are the two $\StreamNameUp$ finite-window rows opened for the compared regular Cauchy names.
\item $R_0$ and $R_1$ are the corresponding $\RegSeqRatUp$ readback rows obtained from those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording requested radii, slack splits, and comparison budgets.
\item $\mu$ is the finite comparison-modulus row assigning each displayed tolerance request in $D$ a common tail stage.
\item $W$ is the shared window row obtained by reading both regular names beyond the stage recorded by $\mu$.
\item $Q$ is the $\CauchyTailComparisonUp$ row that checks the two readbacks through the shared window $W$ and the dyadic ledger $D$.
\item $E$ is the terminal $\RealUp$ seal row, available only after $S_0,S_1,R_0,R_1,D,\mu,W,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two window rows, two readback rows, dyadic ledger, comparison modulus, shared window, tail-comparison row, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S_0,S_1 \longmapsto R_0,R_1 \longmapsto D,\mu,W \longmapsto Q \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyComparisonModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyTailComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyComparisonModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. There is no coordinate for quotient classes of streams, selected representatives, trichotomy, countable-choice schedules, host real equality, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyComparisonModulusUp.RegularCauchyComparisonModulusTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy comparison modulus NameCert obligations]
\label{thm:regular-cauchy-comparison-modulus-namecert-obligations}
For every accepted $\RegularCauchyComparisonModulusUp$ carrier $M=(S_0,S_1,R_0,R_1,D,\mu,W,Q,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyComparisonModulusUp}$ surface consists exactly of carrier admission, two $\StreamNameUp$ window exposures, two $\RegSeqRatUp$ readbacks, one $\DyadicRatCoreUp$ tolerance ledger, one finite comparison-modulus row, one shared tail-window row, one $\CauchyTailComparisonUp$ check, ordered $\RealUp$ seal access, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No NameCert obligation may replace these rows by quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted packet. The carrier exposes exactly two finite stream windows, two regular-rational readbacks, the dyadic tolerance ledger, the comparison-modulus row, the shared tail window, the tail-comparison check, the terminal Real seal, and the structural rows $H,C,P,N$.

The comparison-modulus row is read after the dyadic request is displayed. Thus each common tail stage is attached to an explicit radius and slack budget rather than to an ambient convergence assertion.

The shared window $W$ and comparison row $Q$ then compare the two regular readbacks through the same visible tail data. This keeps the comparison inside $\CauchyTailComparisonUp$ and prevents a quotient or host-equality route from entering.

Finally the Real seal $E$ and the structural rows transport, replay, source, and name the same route. Since the carrier has no coordinate for the excluded principles, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy comparison modulus RegSeqRat handoff]
\label{thm:regular-cauchy-comparison-modulus-regseqrat-handoff}
Every $\RegSeqRatUp$, $\RealEqualityUp$, or $\RealModulusOfConvergenceUp$ consumer of an accepted $\RegularCauchyComparisonModulusUp$ packet must read the two $\StreamNameUp$ window rows, two $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ tolerance ledger, the finite comparison-modulus row, the shared tail-window row, and the $\CauchyTailComparisonUp$ check before it may use the terminal $\RealUp$ seal. The handoff supplies a finite comparison-modulus route for regular Cauchy names; it does not supply quotient streams, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier projection of \autoref{def:regular-cauchy-comparison-modulus-carrier}. A consumer first receives $S_0,S_1$ and then $R_0,R_1$, so the comparison begins from displayed finite windows and regular-rational readbacks.

Next read the dyadic ledger $D$ and the comparison-modulus row $\mu$. These rows bind each tolerance request to a finite common tail stage before any Real-facing boundary is visible.

Use the shared window $W$ and the tail-comparison row $Q$. They compare the two readbacks through the same finite tail data and the same dyadic budget, matching the common-tail discipline of $\CauchyTailComparisonUp$.

Only after this route is displayed may the terminal seal $E$ be consumed by $\RegSeqRatUp$, $\RealEqualityUp$, or $\RealModulusOfConvergenceUp$ readers. The structural rows $H,C,P,N$ preserve that route and contain no quotient, selection, choice, order-decision, host-equality, or completeness coordinate.
\end{proof}

\closureat{RegularCauchyComparisonModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchyComparisonModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-comparison-modulus-carrier}, \autoref{thm:regular-cauchy-comparison-modulus-namecert-obligations}, and \autoref{thm:regular-cauchy-comparison-modulus-regseqrat-handoff} seed the finite comparison-modulus route over displayed dyadic tolerances, regular readbacks, common tail windows, and terminal Real seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, quotient stream equality, selected limits, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, public closure, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, paired window exposure, paired RegSeqRat readback, dyadic tolerance accounting, comparison-modulus exactness, shared tail-window exactness, CauchyTailComparison handoff, ordered Real seal access, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyComparisonModulusUp.}
  \constructivestory{A $\RegularCauchyComparisonModulusUp$ packet is generated from two StreamName windows, two RegSeqRat readback rows, a DyadicRatCore tolerance ledger, a finite comparison-modulus row, a shared tail window, a CauchyTailComparison row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
