\chapter{A Concrete Naming Certificate for $\RealPrefixDiagonalUp$}
\label{ch:concrete-instances-real-prefix-diagonal-namecert}
\origin{ai}

The $\RealPrefixDiagonalUp$ packet realizes the finite prefix-window diagonal route of \autoref{ch:visions-real-completeness} as NameCert data. It consumes the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the diagonal-limit route of \autoref{ch:concrete-instances-regular-cauchy-diagonal-limit-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet is paper-side TasteGate aware: a future gate must preserve nontrivial prefix variation and field-faithful visibility for the displayed rows before any positive discovery claim can be made.

\falsifiablePrediction{An accepted $\RealPrefixDiagonalUp$ read is refuted by any diagonal-limit consumer that reaches the terminal RealUp seal without displaying a finite prefix window, RegSeqRat readback, DyadicRatCore tolerance ledger, diagonal selector row, transport row, replay row, provenance row, and local NameCert row.}

\independenceWitness{streamname, regseqrat, dyadicratcore, regular-cauchy-diagonal-limit, real: $\RealPrefixDiagonalUp$ is not bijective to any listed sibling because it jointly binds the finite prefix-window diagonal selector, regular rational readback, dyadic tolerance ledger, terminal Real seal, transport, replay, provenance, and local naming as one packet.}

\begin{definition}[Real prefix diagonal carrier]
\label{def:real-prefix-diagonal-carrier}
A $\RealPrefixDiagonalUp$ carrier is a finite $\BHist$ packet
$$
P=(B,W,R,D,G,E,H,C,K,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite prefix-budget row naming the admitted observation depth for the diagonal read.
\item $W$ is the $\StreamNameUp$ window row exposed under that prefix budget.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the displayed window.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the diagonal error budget.
\item $G$ is the diagonal selector row, recording which displayed prefix coordinate is read at the current precision.
\item $E$ is the terminal $\RealUp$ seal, available only after $B,W,R,D,G$ have been displayed together.
\item $H$ records componentwise $\hsame$ transport for the budget, window, readback, tolerance, selector, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a precision request through $B,W,R,D,G,E$.
\item $K$ is the $\Pkg$ provenance over $\RealPrefixDiagonalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealPrefixDiagonalUp}$ row.
\end{enumerate}
The carrier exports only finite prefix-window diagonal data. It has no coordinate for a host-selected subsequence, quotient stream equality, ambient Real completeness, countable choice, or a bridge schema.
\end{definition}

\begin{theorem}[Real prefix diagonal NameCert obligations]
\label{thm:real-prefix-diagonal-namecert-obligations}
For every accepted $\RealPrefixDiagonalUp$ carrier $P=(B,W,R,D,G,E,H,C,K,N)$, the local $\NameCert_{\RealPrefixDiagonalUp}$ surface consists exactly of prefix-budget admission, finite StreamName window exposure, RegSeqRat readback, DyadicRatCore tolerance exposure, diagonal selector exposure, terminal Real sealing, componentwise transport, displayed replay, package provenance, and local naming.

The paper-side TasteGate boundary is the same row list: any future gate must preserve visible variation in $B,W,R,D,G,E$ and field-faithful access to $H,C,K,N$. Until such a gate exists, the chapter claims only the finite NameCert packet and not a positive discovery result.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-prefix-diagonal-carrier}. The finite data rows are $B,W,R,D,G,E$, and the structural rows are $H,C,K,N$.

Read the diagonal route in order. The prefix budget $B$ admits a bounded observation, the StreamName row $W$ exposes the corresponding window, the RegSeqRat row $R$ supplies regular rational readback, and the DyadicRatCore row $D$ records the tolerance attached to the readback.

The selector row $G$ chooses only among displayed prefix coordinates, and the terminal Real seal $E$ is downstream of that selector. Transport, replay, provenance, and naming preserve this route without adding host-selected subsequences, quotient stream equality, countable choice, ambient Real completeness, or a bridge schema.
\end{proof}

\begin{theorem}[Real prefix diagonal limit route]
\label{thm:real-prefix-diagonal-limit-route}
Every Real-completeness diagonal consumer of an accepted $\RealPrefixDiagonalUp$ packet factors through the finite route
$$
B,W,R,D,G,E,H,C,K,N .
$$
Equivalently, the consumer must expose a prefix budget, StreamName finite window, RegSeqRat readback, DyadicRatCore tolerance ledger, diagonal selector row, terminal Real seal, componentwise transport, displayed replay, package provenance, and local NameCert row before it can read the diagonal-limit handoff named by \autoref{ch:visions-real-completeness}.
\end{theorem}
\begin{proof}
Begin with \autoref{def:real-prefix-diagonal-carrier}. The terminal seal $E$ is not accessible until the budget, window, readback, tolerance, and selector rows are present in the same packet.

Use \autoref{thm:real-prefix-diagonal-namecert-obligations} to exhaust the local ledger. The listed rows account for every data-bearing and structural coordinate of the packet.

The vision route of \autoref{ch:visions-real-completeness} can therefore consume this chapter only as a finite prefix-window handoff. Because the carrier has no coordinate for host-selected subsequences, quotient stream equality, countable choice, ambient Real completeness, or a bridge schema, none of those objects can be projected from the packet.
\end{proof}

\closureat{RealPrefixDiagonalUp}{seedStr}

\begin{closurestatus}{\RealPrefixDiagonalUp}
  \constructivestory{A $\RealPrefixDiagonalUp$ packet is generated from a finite prefix budget, StreamName window, RegSeqRat readback, DyadicRatCore tolerance ledger, diagonal selector row, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-prefix-diagonal-carrier}, \autoref{thm:real-prefix-diagonal-namecert-obligations}, \autoref{thm:real-prefix-diagonal-limit-route}, the falsifiable prediction, and the independence witness seed the finite prefix-window diagonal route of the Real-completeness vision.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full Real completeness, quotient stream equality, host-selected subsequences, countable choice, positive discovery status, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, prefix-window exposure, diagonal selector exactness, tolerance-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.RealPrefixDiagonalUp.}
\end{closurestatus}
