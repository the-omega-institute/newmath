\chapter{A Concrete Naming Certificate for $\CodonLiftOrbitClassifierUp$}
\label{ch:concrete-instances-codon-lift-orbit-classifier-namecert}
\origin{human}

Following Burnside's lemma and finite orbit enumeration, the $\CodonLiftOrbitClassifierUp$ packet realizes the codon-level lift-orbit theorem of \autoref{thm:philosophy-codon-level-lift-orbit-classification}, the orbit table of \autoref{thm:philosophy-codon-level-lift-orbit-table}, and the Burnside formula of \autoref{thm:philosophy-burnside-lift-orbit-formula} as concrete finite NameCert data. The packet classifies binary lift colorings over quotient-level trigger supports under support automorphisms and global $f_{3}$ complement. It does not assert a biological mechanism, probabilistic fitness law, host group quotient, or an unrecorded search oracle.

\begin{definition}[Codon lift-orbit classifier carrier]
\label{def:codon-lift-orbit-classifier-carrier}
A $\CodonLiftOrbitClassifierUp$ carrier is a finite $\BHist$ packet
$$
O=(A,E,\Gamma,T,B,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the support normal-form row, naming one quotient-level minimal trigger support and its displayed support rows.
\item $E$ is the finite binary lift row over $A$, recording the carried $f_{3}$ lift vector and its Hamming-weight ledger.
\item $\Gamma$ is the support-automorphism row, listing the finite row permutations that preserve $A$.
\item $T$ is the global complement row, recording the $C_{2}^{(f_{3})}$ action on the displayed lift vector.
\item $B$ is the Burnside ledger, listing the fixed-point counts for each displayed permutation and complemented permutation.
\item $K$ is the classifier row, assigning the displayed support normal form and lift pattern to one of the twenty codon-level orbit types.
\item $H$ records componentwise $\hsame$ transport for supports, lift vectors, automorphism rows, complement rows, Burnside counts, classifier rows, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the support normal form through lift coloring, group action, Burnside counting, and classifier assignment.
\item $P$ is the $\Pkg$ provenance over $\CodonLiftOrbitClassifierUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and the finite orbit-enumeration rows cited above.
\item $N$ is the local $\NameCert_{\CodonLiftOrbitClassifierUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,E,\Gamma,T,B,K,H,C,P,N$. It has no coordinate for a hidden biological interpretation, an unstated orbit representative, a host quotient object, or a search result detached from the displayed Burnside ledger.
\end{definition}

\begin{theorem}[Codon lift-orbit classifier NameCert obligations]
\label{thm:codon-lift-orbit-classifier-namecert-obligations}
For every accepted $\CodonLiftOrbitClassifierUp$ carrier $O=(A,E,\Gamma,T,B,K,H,C,P,N)$, the local $\NameCert_{\CodonLiftOrbitClassifierUp}$ surface consists exactly of support-normal-form admission through $A$, binary lift admission through $E$, support-automorphism admission through $\Gamma$, complement-action admission through $T$, Burnside ledger exactness through $B$, orbit-classifier readback through $K$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:codon-lift-orbit-classifier-carrier}. The rows $A,E,\Gamma,T$ display the finite support, lift vector, support automorphisms, and complement action before any classifier output is read.

The Burnside ledger is then computed from the displayed action. The row $B$ records fixed-point counts for ordinary and complemented action elements, matching the finite formula in \autoref{thm:philosophy-burnside-lift-orbit-formula}. The classifier row $K$ is downstream of that ledger.

The structural rows preserve the same enumeration route. Componentwise transport and replay cannot introduce an unstated representative or biological interpretation, while provenance and local naming source the packet. Thus the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Codon lift-orbit vision landing]
\label{thm:codon-lift-orbit-classifier-vision-landing}
The twenty codon-level orbit types of \autoref{thm:philosophy-codon-level-lift-orbit-classification} are consumed by $\CodonLiftOrbitClassifierUp$ only through the finite route
$$
A,E,\Gamma,T \longmapsto B \longmapsto K \longmapsto H,C,P,N .
$$
Consequently every downstream BioReality ledger read must expose the support normal form, binary lift vector, support automorphism action, global complement action, Burnside fixed-point ledger, classifier row, transport, replay, provenance, and local NameCert rows before treating a codon-level lift orbit as classified.
\end{theorem}
\begin{proof}
Read the source theorem through its finite components. The support normal forms and lift patterns supply $A$ and $E$, while support automorphisms and the global complement action supply $\Gamma$ and $T$.

Apply the Burnside formula. The row $B$ records exactly the fixed-point count needed to classify the displayed support-lift pair, and the row $K$ names the resulting orbit type among the twenty displayed rows.

Finally apply the local certificate obligations. Theorem~\ref{thm:codon-lift-orbit-classifier-namecert-obligations} keeps transport, replay, provenance, and local naming attached to the same finite enumeration. Thus a downstream read cannot consume an orbit class without the displayed Burnside and classifier evidence.
\end{proof}

\begin{theorem}[Codon window and Burnside dependency]
\label{thm:codon-lift-orbit-classifier-codon-window-dependency}
Every accepted $\CodonLiftOrbitClassifierUp$ read of an orbit class first consumes the codon-window carrier of \autoref{ch:concrete-instances-codon-six-bit-window-namecert} and the finite Burnside count route of \autoref{ch:concrete-instances-burnside-orbit-count-namecert}. Only after those sibling surfaces have supplied the source window and orbit-count ledger may the rows $A,E,\Gamma,T,B,K,H,C,P,N$ export a classifier value.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:codon-lift-orbit-classifier-carrier}. The support row $A$ and binary lift row $E$ are not free coordinates; they are read as codon-window data before the automorphism row $\Gamma$ and complement row $T$ act on them.

Next project the Burnside ledger row $B$. The classifier row $K$ is downstream of the fixed-point counts recorded in $B$, so its value cannot be read before the finite orbit-count route supplied by \autoref{ch:concrete-instances-burnside-orbit-count-namecert}.

Finally apply the NameCert obligations of \autoref{thm:codon-lift-orbit-classifier-namecert-obligations}. They keep $H,C,P,N$ attached to the same source-window and Burnside-ledger rows, and therefore prevent the classifier from exporting an orbit value detached from \autoref{ch:concrete-instances-codon-six-bit-window-namecert} or \autoref{ch:concrete-instances-burnside-orbit-count-namecert}.
\end{proof}

\falsifiablePrediction{Deleting the Burnside ledger row $B$ prevents an accepted $\CodonLiftOrbitClassifierUp$ packet from exporting any codon-level lift-orbit class, even when support, lift, automorphism, complement, transport, replay, provenance, and local naming rows remain displayed.}

\independenceWitness{codon-level lift-orbit classification: $\CodonLiftOrbitClassifierUp$ binds support normal forms, binary lift vectors, support automorphisms, global complement action, Burnside fixed-point counts, classifier readback, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ into one finite orbit-enumeration packet.}

\closureat{CodonLiftOrbitClassifierUp}{seedStr}

\begin{closurestatus}{\CodonLiftOrbitClassifierUp}
  \constructivestory{A $\CodonLiftOrbitClassifierUp$ packet is generated from support normal forms, binary lift vectors, support automorphism rows, global complement rows, Burnside fixed-point ledgers, classifier rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:codon-lift-orbit-classifier-carrier}, \autoref{thm:codon-lift-orbit-classifier-namecert-obligations}, and \autoref{thm:codon-lift-orbit-classifier-vision-landing} seed the finite codon lift-orbit classifier over support normal forms, lift vectors, group-action rows, Burnside counts, and classifier readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a biological mechanism, probabilistic fitness law, host quotient object, hidden search oracle, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked support admission, lift-vector admission, automorphism action, complement action, Burnside ledger exactness, classifier readback, transport, replay, provenance, and local naming for BEDC.Derived.CodonLiftOrbitClassifierUp.}
\end{closurestatus}
