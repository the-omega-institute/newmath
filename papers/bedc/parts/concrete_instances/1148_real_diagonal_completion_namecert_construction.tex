\chapter{A Concrete Naming Certificate for $\RealDiagonalCompletionUp$}
\label{ch:concrete-instances-real-diagonal-completion-namecert}
\origin{human}

The $\RealDiagonalCompletionUp$ interface names the concrete diagonal completion packet that sits between the regular Cauchy family used in the real-completeness capstone and the finite $\RealUp$ seal surface. Following the Bishop--Bridges constructive-analysis treatment of Cauchy completion, the packet keeps the regular rational input, its explicit modulus, the chosen diagonal windows, and the real-seal ledger as visible certificate data. It does not form a quotient of regular streams, select a limit by choice, or import host real completeness.

The diagonal completion packet consumes the finite $\StreamNameUp$ window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence carrier of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real seal boundary of \autoref{ch:concrete-instances-real-namecert}. This chapter is the finite ledger that routes those sibling surfaces through the displayed diagonal selector without quotient streams, host limits, or choice-selected representatives.

The packet consumes the diagonal-window source of \autoref{ch:concrete-instances-regularcauchydiagonal-namecert} and instantiates the finite diagonal route from \autoref{ch:visions-real-completeness} before exposing the $\RealUp$ seal. Thus its carrier records the substantive path from regular Cauchy diagonal data to completion-facing seal data rather than standing as a kernel-only packet.

\begin{definition}[RealDiagonalCompletion source packet]
\label{def:real-diagonal-completion-source}
A $\RealDiagonalCompletionUp$ source packet is a finite $\mathsf{BHist}$ row
$$
D=(X,M,\Delta,S,R,E,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $X$ is a $\RegSeqRatUp$-indexed regular Cauchy input family, with every inspected member read as a regular rational sequence;
\item $M$ is an explicit unary modulus row selecting a late enough member of $X$ for each requested precision;
\item $\Delta$ is the diagonal selector row that sends a precision request to the selected member of $X$ and to the regular-sequence window read inside that member;
\item $S$ is the finite $\StreamNameUp$ schedule of all windows requested by the diagonal selector;
\item $R$ is the $\RegSeqRatUp$ readback ledger for the selected diagonal windows;
\item $E$ is the $\RealUp$ classifier and seal ledger attached to the diagonal representative;
\item $P$ is the $\Pkg$ provenance naming the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RatUp$, and $\NameCert$ source certificates;
\item $N$ is the local $\NameCert_{\RealDiagonalCompletionUp}$ provenance row.
\end{enumerate}
The classifier compares two accepted source packets only by the displayed modulus rows, selected diagonal windows, $\RegSeqRatUp$ readbacks, and $\RealUp$ seal ledgers, transporting each component through the visible $\hsame$ and $\Cont$ routes.
\end{definition}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[RealDiagonalCompletion carrier habitation]
\label{thm:real-diagonal-completion-carrier}
Every regular Cauchy input family over $\RegSeqRatUp$ with an explicit unary modulus row and displayed finite readbacks determines an accepted $\RealDiagonalCompletionUp$ carrier by placing the input family, modulus, diagonal selector, $\StreamNameUp$ schedule, $\RegSeqRatUp$ readback ledger, $\RealUp$ seal ledger, package provenance, and local $\NameCert$ row into the source packet of \autoref{def:real-diagonal-completion-source}.
\end{theorem}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_carrier\_habitation}
\begin{proof}
Start with the finite rows supplied by the regular Cauchy input. The family row gives the $\RegSeqRatUp$-indexed source, and the modulus row gives a displayed unary bound for each requested precision.

Use the diagonal selector to choose the visible late member and the visible regular-sequence window inside that member. The selected windows form the finite $\StreamNameUp$ schedule, and the corresponding rational observations form the $\RegSeqRatUp$ readback ledger.

Attach the $\RealUp$ seal ledger to the same selected rows. The provenance row names only the displayed source certificates, and the local $\NameCert$ row records the packet assembly, so the resulting carrier contains no quotient, selected host limit, or hidden completion datum.
\end{proof}

\begin{theorem}[RealDiagonalCompletion classifier stability]
\label{thm:real-diagonal-completion-classifier-stability}
Let $D$ and $D'$ be accepted $\RealDiagonalCompletionUp$ packets. If their modulus rows, diagonal selector rows, selected $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\RealUp$ seal ledgers, $\Pkg$ provenance, and local $\NameCert$ rows are transported componentwise through displayed $\hsame$ comparisons and $\Cont$ routes, then the transported packet is accepted and the local classifier relates it to the original packet.
\end{theorem}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_classifier\_stability}
\begin{proof}
Project the packet coordinates of \autoref{def:real-diagonal-completion-source}. Each coordinate is finite certificate data, so a displayed $\hsame$ comparison transports the row without changing the carrier kind.

The continuation routes identify which finite windows are read after transport. The diagonal selector is therefore transported together with the $\StreamNameUp$ schedule, and the selected readbacks remain the same $\RegSeqRatUp$ observations seen through the transported routes.

Finally repack the $\RealUp$ seal ledger and the package provenance. Since the classifier compares exactly these rows, the transported packet is accepted and classified with the original one.
\end{proof}

\begin{theorem}[RealDiagonalCompletion ledger exactness]
\label{thm:real-diagonal-completion-ledger-exactness}
For every accepted $\RealDiagonalCompletionUp$ packet, each $\RealUp$ consumer read of the diagonal representative factors through one displayed diagonal selector row, one finite $\StreamNameUp$ window, one $\RegSeqRatUp$ readback row, the associated $\DyadicRatCoreUp$ rational observation ledger, and the visible $\Pkg$ and $\NameCert$ provenance.
\end{theorem}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_ledger\_exactness}
\begin{proof}
Read the accepted packet through its source definition. The $\RealUp$ seal coordinate is not independent data; it is attached to the selected diagonal windows and their regular rational readbacks.

A consumer precision request first meets the unary modulus row, then the diagonal selector row, and then the scheduled finite stream window. The readback ledger records the $\RegSeqRatUp$ observation and its $\DyadicRatCoreUp$ rational evidence at that same window.

Thus the visible ledger exhausts the read. There is no remaining coordinate from which a quotient representative, host real endpoint, hidden tail choice, or ambient completeness proof could be exported.
\end{proof}

\begin{theorem}[RealDiagonalCompletion diagonal window extraction]
\label{thm:real-diagonal-completion-window-extraction}
For every accepted $\RealDiagonalCompletionUp$ packet $D$ and every unary precision request $n$, the consumer read of the diagonal representative factors through a displayed modulus row $M(n)$, a displayed diagonal selector row $\Delta(n)$, one scheduled $\StreamNameUp$ window $S(n)$, and one $\RegSeqRatUp$ readback row $R(n)$. These rows are transported only by the visible $\hsame$ comparisons and $\Cont$ routes carried by the packet provenance.
\end{theorem}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_window\_extraction}
\begin{proof}
Project $D$ by \autoref{def:real-diagonal-completion-source}. The modulus coordinate supplies the finite member of the regular Cauchy input family selected for the unary request $n$.

Use the diagonal selector coordinate to identify the displayed window inside that member. The scheduled $\StreamNameUp$ row is exactly the finite window named by this selector, and the $\RegSeqRatUp$ readback row is the corresponding regular rational observation.

Repack these rows with the visible $\Pkg$ and $\NameCert$ provenance. Since the packet classifier transports exactly these coordinates through $\hsame$ and $\Cont$, the extracted window is a consumer-facing factor of the diagonal read rather than an ambient limit selection.
\end{proof}

\begin{theorem}[RealDiagonalCompletion real seal factorization]
\label{thm:real-diagonal-completion-seal-factorization}
The $\RealUp$ seal ledger $E$ of an accepted $\RealDiagonalCompletionUp$ packet is determined by the family of extracted diagonal windows of \autoref{thm:real-diagonal-completion-window-extraction} together with the displayed $\RegSeqRatUp$ readback ledger. No host limit, quotient representative, or ambient completeness proof is exported by this factorization.
\end{theorem}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_seal\_factorization}
\begin{proof}
Combine \autoref{thm:real-diagonal-completion-ledger-exactness} with the window extraction surface of \autoref{thm:real-diagonal-completion-window-extraction}. Every consumer read of the diagonal representative has already passed through the modulus row, selector row, scheduled stream window, and regular rational readback.

The source packet attaches the $\RealUp$ seal coordinate to exactly those finite readbacks. Therefore two accepted packets with the same extracted windows and the same displayed $\RegSeqRatUp$ readback ledger expose the same $\RealUp$ seal ledger to consumers.

The conclusion is only a finite ledger factorization. It does not form a quotient of regular streams, choose a host real limit, or appeal to an ambient completeness theorem.
\end{proof}

\begin{theorem}[RealDiagonalCompletion common-refinement seal invariance]
\label{thm:real-diagonal-completion-common-refinement-seal-invariance}
Let $D_0$ and $D_1$ be accepted $\RealDiagonalCompletionUp$ packets. Suppose the diagonal selectors of $D_i$ read selected $\StreamNameUp$ windows $S_i(n)$ with displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support, and suppose that for the inspected boundary requests these windows both transport to one common-refinement $\StreamNameUp$ packet $S_\ast(n)$. Suppose further that the transported $\RegSeqRatUp$ readback rows and the transported $\DyadicRatCoreUp$ observation ledgers of $D_0$ and $D_1$ agree on $S_\ast(n)$ for each such boundary request.

Then the attached $\RealUp$ seal ledgers of $D_0$ and $D_1$ are classifier-equal at the shared boundary. The equality is only the finite seal-ledger equality carried by the displayed $\hsame$ and $\Cont$ transports through the two $\Pkg$ and $\NameCert$ rows. It does not assert quotient stream equality, host real equality, a selected host limit, countable choice, ambient completeness, or a host-level equality of completed real numbers.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-diagonal-completion-window-extraction} to both accepted packets. For each inspected boundary request this exposes the modulus row, diagonal selector, selected $\StreamNameUp$ window, and $\RegSeqRatUp$ readback used by the $\RealUp$-facing consumer read.

Next apply \autoref{thm:real-diagonal-completion-seal-factorization} to both packets. The attached $\RealUp$ seal ledger is read only through those extracted windows and the displayed regular-rational readbacks, so the comparison of seal ledgers reduces to the comparison of the transported finite-window data.

Use \autoref{thm:real-streamname-regseqrat-common-refinement-seal-uniqueness} on the shared common-refinement window $S_\ast(n)$. The hypotheses give agreement of the transported $\RegSeqRatUp$ readbacks and $\DyadicRatCoreUp$ observation ledgers on that window, so the terminal $\RealUp$ seal route is unique at the shared boundary.

Finally transport this terminal route back through the displayed $\Pkg$ and $\NameCert$ rows of $D_0$ and $D_1$. Componentwise $\hsame$ supplies the classifier equality of the seal ledgers, while the $\Cont$ routes record that the consumer reads pass through the same selected boundary data. None of the cited rows contains quotient stream equality, host real equality, selected limits, countable choice, or ambient completeness, so the conclusion is exactly the finite classifier equality stated above.
\end{proof}

\begin{theorem}[RealDiagonalCompletion constant rational handoff]
\label{thm:real-diagonal-completion-constant-rational-handoff}
If the input regular Cauchy family is the stationary family generated by a $\RatUp$ row $q$, then the accepted $\RealDiagonalCompletionUp$ packet classifies its diagonal representative with the constant $\RealUp$ embedding of $q$. The diagonal selector always reads the stationary $\RegSeqRatUp$ member, and every selected $\StreamNameUp$ window returns the same rational row through the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance.
\end{theorem}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_constant\_rational\_handoff}
\begin{proof}
The stationary family has a constant regular rational source row. Its modulus can be the displayed unary zero-threshold row, because every selected member of the family reads the same rational value.

The diagonal selector therefore chooses a window of the same stationary $\RegSeqRatUp$ member at every requested precision. The $\StreamNameUp$ schedule may vary the requested window, but the readback ledger always returns the row $q$ with its $\RatUp$ and $\DyadicRatCoreUp$ evidence.

The $\RealUp$ seal ledger is the constant rational handoff for those same rows. Componentwise $\hsame$ transport and the displayed $\Cont$ routes preserve the stationary readback, so the classifier identifies the diagonal representative with the constant embedding of $q$.
\end{proof}

\begin{theorem}[RealDiagonalCompletion NameCert obligation surface]
\label{thm:real-diagonal-completion-namecert-obligation-surface}
The local $\NameCert_{\RealDiagonalCompletionUp}$ obligation surface consists of five rows:
\begin{enumerate}
\item carrier habitation: \autoref{thm:real-diagonal-completion-carrier} assembles the finite source packet from the regular Cauchy input, unary modulus, diagonal selector, finite stream schedule, readback ledger, real seal, provenance, and local ledger;
\item classifier stability: \autoref{thm:real-diagonal-completion-classifier-stability} transports exactly the modulus, selector, selected windows, readbacks, and seal ledgers through $\hsame$ and $\Cont$;
\item diagonal-window exactness: \autoref{thm:real-diagonal-completion-ledger-exactness} forces each consumer read through a displayed selector window and its $\RegSeqRatUp$ readback;
\item constant rational handoff: \autoref{thm:real-diagonal-completion-constant-rational-handoff} identifies stationary rational inputs with their constant $\RealUp$ embeddings;
\item ledger non-escape: the packet exposes only $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RatUp$, $\Pkg$, and $\NameCert$ rows, and contains no quotient stream, host real equality, choice-selected limit, or ambient completion object.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_namecert\_obligation\_surface}
\begin{proof}
The first four rows are exactly the preceding carrier, stability, exactness, and stationary-handoff theorems. They list the finite source coordinates and describe how a consumer reaches every displayed read.

For the ledger boundary, inspect the source packet. Its coordinates name only the dependency certificates and the local provenance rows, so a consumer cannot obtain quotient, choice, or host-completion data from the carrier.

These five rows are precisely the naming-certificate obligations for the diagonal completion object. They turn the diagonal representative into a concrete $\mathsf{BHist}$ packet surface while this chapter leaves machine-checked real completeness unclaimed.
\end{proof}

\begin{theorem}[RealDiagonalCompletion scoped consumer seal package]
\label{thm:real-diagonal-completion-scoped-consumer-seal-package}
Every accepted $\RealDiagonalCompletionUp$ packet satisfying the source row of \autoref{def:real-diagonal-completion-source}, the carrier construction, classifier stability, ledger exactness, diagonal window extraction, seal factorization, constant rational handoff, and $\NameCert$ obligation surface determines a scoped dependency package over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, $\NameCert$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RatUp$.

Every $\RealUp$-facing consumer read of that package factors through the displayed diagonal selector, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal ledger, and provenance rows. No quotient stream, choice-selected limit, host real equality, or ambient completeness object is part of the scoped package.
\end{theorem}
\begin{proof}
Project the source packet of \autoref{def:real-diagonal-completion-source}. Its visible coordinates are the finite $\mathsf{BHist}$ carrier row, the displayed transport routes through $\hsame$ and $\Cont$, the package and local $\NameCert$ provenance, and the source certificates for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RatUp$.

The carrier row is supplied by \autoref{thm:real-diagonal-completion-carrier}. Classifier transport is supplied by \autoref{thm:real-diagonal-completion-classifier-stability}, and each consumer read is forced through the displayed diagonal-window ledger by \autoref{thm:real-diagonal-completion-ledger-exactness} and \autoref{thm:real-diagonal-completion-window-extraction}.

The seal route is the factorization of \autoref{thm:real-diagonal-completion-seal-factorization}. For stationary rational inputs, \autoref{thm:real-diagonal-completion-constant-rational-handoff} keeps the handoff inside the same $\RatUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows.

The complete local obligation surface is the five-row $\NameCert$ packet of \autoref{thm:real-diagonal-completion-namecert-obligation-surface}. These rows exhaust the available fields of the accepted packet.

Since none of the cited rows contains a quotient stream, choice-selected limit, host real equality, or ambient completeness object, the scoped consumer seal package has exactly the dependency surface stated above.
\end{proof}

\begin{theorem}[RealDiagonalCompletion public RealUp handoff]
\label{thm:real-diagonal-completion-public-realup-handoff}
Every accepted $\RealDiagonalCompletionUp$ packet is repacked as a consumer-facing $\RealUp$ seal read through the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RatUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows only. The repacking is exactly the composition of diagonal-window extraction, real-seal factorization, constant rational handoff, and scoped primitive dependency over the accepted packet surface.

The handoff exports no $\RealUp$ completeness theorem, quotient equality of regular streams, selected limit, uniqueness of limits, or host real construction.
\end{theorem}
\begin{proof}
Begin with the extracted consumer window of \autoref{thm:real-diagonal-completion-window-extraction}. It routes each requested read through the displayed modulus, diagonal selector, finite $\StreamNameUp$ window, and $\RegSeqRatUp$ readback row.

Next apply the seal factorization of \autoref{thm:real-diagonal-completion-seal-factorization}. The $\RealUp$ seal is read from those extracted windows and their displayed rational observations, with $\hsame$ and $\Cont$ accounting for the permitted transports.

For stationary rational inputs, \autoref{thm:real-diagonal-completion-constant-rational-handoff} identifies the same surface with the constant $\RatUp$ handoff inside the $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ dependency rows.

Finally use the scoped dependency package of \autoref{thm:real-diagonal-completion-scoped-consumer-seal-package}. That package exhausts the accepted packet fields by $\mathsf{BHist}$ carrier data, package provenance, local $\NameCert$ rows, and the named dependency certificates. Since none of these rows contains completeness, quotient equality, a selected limit, uniqueness of limits, or a host real carrier, the public read has exactly the stated boundary.
\end{proof}

\begin{theorem}[RealDiagonalCompletion public finite completeness interface]
\label{thm:real-diagonal-completion-public-finite-completeness-interface}
Every accepted $\RealDiagonalCompletionUp$ packet satisfying the source packet, carrier construction, classifier stability, ledger exactness, diagonal-window extraction, seal factorization, constant rational handoff, $\NameCert$ obligation surface, and scoped consumer seal package exports a public $\RealUp$-facing finite completeness interface.

For every requested consumer read, the interface factors the read through an explicit unary modulus, the displayed diagonal selector, a finite $\StreamNameUp$ window, a $\RegSeqRatUp$ readback, a $\DyadicRatCoreUp$ observation ledger, a $\RealUp$ seal row, $\Pkg$ provenance, and a local $\NameCert$ row. It exports no quotient stream, selected host limit, uniqueness theorem, or ambient real completeness theorem.
\end{theorem}
\begin{proof}
Start from the source packet of \autoref{def:real-diagonal-completion-source}. The carrier theorem \autoref{thm:real-diagonal-completion-carrier} supplies the finite rows, and \autoref{thm:real-diagonal-completion-classifier-stability} transports those rows only along the displayed $\hsame$ and $\Cont$ routes.

The read itself is the diagonal-window readback of \autoref{thm:real-diagonal-completion-ledger-exactness} and \autoref{thm:real-diagonal-completion-window-extraction}. These two results exhibit the unary modulus, diagonal selector, finite stream window, regular-sequence readback, and dyadic observation ledger used by the consumer.

The $\RealUp$ output row is the seal factorization of \autoref{thm:real-diagonal-completion-seal-factorization}. For constant rational inputs, \autoref{thm:real-diagonal-completion-constant-rational-handoff} keeps the read inside the same $\RatUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ dependency rows.

Finally, \autoref{thm:real-diagonal-completion-namecert-obligation-surface}, \autoref{thm:real-diagonal-completion-scoped-consumer-seal-package}, and \autoref{thm:real-diagonal-completion-public-realup-handoff} repack these finite rows into the public consumer interface. Since the cited packet rows contain no quotient stream, selected host limit, uniqueness theorem, or ambient real-completeness object, the public interface has exactly the stated boundary.
\end{proof}

\begin{theorem}[RealDiagonalCompletion standard finite bridge]
\label{thm:real-diagonal-completion-standard-finite-bridge}
Any accepted $\RealDiagonalCompletionUp$ packet satisfying the source packet of \autoref{def:real-diagonal-completion-source} and the public finite completeness interface of \autoref{thm:real-diagonal-completion-public-finite-completeness-interface} repacks as the standard BEDC bridge surface over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RatUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

Every bridge read factors through the displayed modulus, diagonal selector, finite stream window, regular-sequence readback, real seal, and provenance rows. The bridge exports no quotient stream, host real equality, selected limit, uniqueness theorem, or ambient completeness object.
\end{theorem}
\begin{proof}
The carrier habitation theorem \autoref{thm:real-diagonal-completion-carrier} supplies the accepted finite packet, and \autoref{thm:real-diagonal-completion-ledger-exactness} identifies the exact ledger through which a consumer read must pass. Together these fix the bridge carrier as a $\mathsf{BHist}$ surface rather than an external completion object.

For each requested read, use \autoref{thm:real-diagonal-completion-window-extraction} to expose the modulus, selector, finite $\StreamNameUp$ window, and $\RegSeqRatUp$ readback. Then apply \autoref{thm:real-diagonal-completion-seal-factorization} to route the same read into the displayed $\RealUp$ seal, with the constant rational case constrained by \autoref{thm:real-diagonal-completion-constant-rational-handoff}.

The scoped consumer seal package of \autoref{thm:real-diagonal-completion-scoped-consumer-seal-package} records the permitted $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ routes. The public $\RealUp$ handoff of \autoref{thm:real-diagonal-completion-public-realup-handoff} and the finite completeness interface of \autoref{thm:real-diagonal-completion-public-finite-completeness-interface} then repack those routes as the public bridge surface.

It remains to check that the bridge has no extra coordinate. The source packet lists exactly $X$, $M$, $\Delta$, $S$, $R$, $E$, $P$, and $N$; the preceding theorems account for each of these rows. Coordinate exhaustion therefore rules out any quotient stream, host real equality, selected limit, uniqueness theorem, or ambient completeness object.
\end{proof}

\begin{theorem}[RealDiagonalCompletion bridge readback roundtrip]
\label{thm:real-diagonal-completion-bridge-readback-roundtrip}
Reading the standard finite packet of \autoref{thm:real-diagonal-completion-standard-finite-bridge} back into BEDC recovers the same accepted $\RealDiagonalCompletionUp$ rows: the unary modulus row, diagonal selector row, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ observation ledger, $\RealUp$ seal row, package provenance, and local $\NameCert$ row.

The readback is conservative over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RatUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. It exports no quotient stream, selected host limit, uniqueness theorem, host real equality, or ambient real completeness.
\end{theorem}
\begin{proof}
Begin with the bridge packet of \autoref{thm:real-diagonal-completion-standard-finite-bridge}. Its coordinates are the displayed packet rows of \autoref{def:real-diagonal-completion-source}; there is no additional host coordinate to interpret.

Apply \autoref{thm:real-diagonal-completion-window-extraction} to read back the modulus, diagonal selector, finite $\StreamNameUp$ window, and $\RegSeqRatUp$ row. The same projection also recovers the dyadic observation ledger because \autoref{thm:real-diagonal-completion-ledger-exactness} identifies the accepted read ledger with those displayed coordinates.

Next use \autoref{thm:real-diagonal-completion-seal-factorization} and \autoref{thm:real-diagonal-completion-public-realup-handoff}. These repack the recovered window and ledger as the same $\RealUp$ seal row, with all transports accounted for by the displayed $\hsame$ and $\Cont$ routes.

Finally apply \autoref{thm:real-diagonal-completion-public-finite-completeness-interface}. The bridge readback forgets only the wrapper naming the standard finite packet and keeps the same $\Pkg$ provenance and local $\NameCert$ row. Projection followed by repacking therefore returns the accepted BEDC packet with no quotient stream, selected host limit, uniqueness theorem, host real equality, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[RealDiagonalCompletion mature consumer package]
\label{thm:real-diagonal-completion-mature-consumer-package}
The carrier, classifier stability, ledger exactness, diagonal-window extraction, seal factorization, public finite completeness interface, standard finite bridge, and bridge readback for accepted $\RealDiagonalCompletionUp$ packets assemble a mature consumer package for downstream $\RealUp$ completion consumers.

Every read in the package is routed through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RatUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The package exports no quotient stream, host limit, uniqueness theorem, or ambient completeness object.
\end{theorem}
\begin{proof}
Start with the accepted source packet of \autoref{def:real-diagonal-completion-source}. The carrier theorem \autoref{thm:real-diagonal-completion-carrier} supplies the finite packet rows, while \autoref{thm:real-diagonal-completion-classifier-stability} records that consumer transport uses only displayed $\hsame$ and $\Cont$ routes.

Next recover the read coordinates. The ledger exactness theorem \autoref{thm:real-diagonal-completion-ledger-exactness} fixes the observable row, and \autoref{thm:real-diagonal-completion-window-extraction} exposes the modulus, diagonal selector, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ observation ledger used by each read.

The $\RealUp$ output is then the seal of \autoref{thm:real-diagonal-completion-seal-factorization}. The constant rational boundary of \autoref{thm:real-diagonal-completion-constant-rational-handoff} keeps the $\RatUp$ case inside the same visible rational, regular-sequence, dyadic, and real rows.

Finally apply the public package results. The $\NameCert$ obligation surface of \autoref{thm:real-diagonal-completion-namecert-obligation-surface}, the scoped consumer seal package of \autoref{thm:real-diagonal-completion-scoped-consumer-seal-package}, the public finite completeness interface of \autoref{thm:real-diagonal-completion-public-finite-completeness-interface}, the standard finite bridge of \autoref{thm:real-diagonal-completion-standard-finite-bridge}, and the bridge readback of \autoref{thm:real-diagonal-completion-bridge-readback-roundtrip} together account for every exported coordinate. Since those cited rows contain only the named BEDC dependency surfaces, the mature consumer package has exactly the stated boundary.
\end{proof}

\closureat{RealDiagonalCompletionUp}{seedStr}
\closureat{RealDiagonalCompletionUp}{obligationStr}
\closureat{RealDiagonalCompletionUp}{scopedStr}
\closureat{RealDiagonalCompletionUp}{publicStr}
\closureat{RealDiagonalCompletionUp}{bridgedStr}
\closureat{RealDiagonalCompletionUp}{matureStr}

\begin{closurestatus}{\RealDiagonalCompletionUp}
  \constructivestory{A $\RealDiagonalCompletionUp$ packet is generated from a $\RegSeqRatUp$-indexed regular Cauchy input, an explicit unary modulus, a diagonal selector, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, a $\RealUp$ seal ledger, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:real-diagonal-completion-scoped-consumer-seal-package}, \autoref{thm:real-diagonal-completion-public-finite-completeness-interface}, \autoref{thm:real-diagonal-completion-standard-finite-bridge}, \autoref{thm:real-diagonal-completion-bridge-readback-roundtrip}, and \autoref{thm:real-diagonal-completion-mature-consumer-package} bind the finite diagonal completion carrier, classifier transport, diagonal-window readback exactness, constant rational handoff, public $\RealUp$ finite completeness interface, standard finite bridge, BEDC readback surface, and mature consumer package over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RatUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.RealDiagonalCompletionUp.RealDiagonalCompletionSourcePacket\_ledger\_exactness}
  \bridgestatus{paperBridge}
  \origin{human}
  \notclaimed{This chapter does not close $\RealUp$ completeness, quotient equality of regular streams, uniqueness of limits, host real construction, or machine bridge checking.}
  \upgradepath{No further paper-side theory-grade target is open; verification-axis audit cleanliness and machine bridge checking remain separate obligations.}
\end{closurestatus}
