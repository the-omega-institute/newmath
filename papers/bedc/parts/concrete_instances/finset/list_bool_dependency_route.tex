$\FinSetUp$ reads finite membership through the $\BoolUp$ classifier of \autoref{ch:concrete-instances-bool-namecert} and finite enumeration through the $\ListUp$ spine of \autoref{ch:concrete-instances-list-namecert}. Thus the finite-set packet exposes its sibling dependency route before any consumer treats membership or enumeration as local kernel-only data.
