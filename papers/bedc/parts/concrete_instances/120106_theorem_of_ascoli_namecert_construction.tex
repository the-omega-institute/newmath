\chapter{A Concrete Naming Certificate for $\TheoremOfAscoliUp$}
\label{ch:concrete-instances-theorem-of-ascoli-namecert}
\origin{human}

Following Arzela, Ascoli, Kelley, and Bishop--Bridges, the $\TheoremOfAscoliUp$ packet records the compact-open theorem surface as finite BEDC data. It sits after the compact-open topology route of \autoref{ch:concrete-instances-compact-open-topology-namecert}, the Arzela--Ascoli family surface of \autoref{ch:concrete-instances-arzelaascoli-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, and the real-name readback of \autoref{ch:concrete-instances-real-namecert}. The packet names the theorem-facing handoff from compact metric data, compact-open topology, equicontinuity, bounded-family control, finite stream windows, regular rational readback, and a terminal real seal; it does not add a host function space or a subsequence selector.

\begin{definition}[TheoremOfAscoli carrier]
\label{def:theorem-of-ascoli-carrier}
A $\TheoremOfAscoliUp$ carrier is a finite $\BHist$ packet
$$
T=(K,O,E,B,A,W,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the compact metric observations consumed by the theorem surface.
\item $O$ is the $\CompactOpenTopologyUp$ row, naming the compact-open neighbourhood and finite subbasic-window route read by consumers.
\item $E$ is the $\EquicontinuityFamilyUp$ row, carrying the shared equicontinuity ledger for the displayed family.
\item $B$ is the $\BoundedFunctionFamilyUp$ row, carrying the pointwise and sup-norm bound ledger visible to the compact-open consumer.
\item $A$ is the $\ArzelaAscoliUp$ row, recording the finite extraction and compactness-facing dependency already present in the Arzela--Ascoli packet.
\item $W$ is the $\StreamNameUp$ window row, listing the finite observation schedule for compact-open convergence reads.
\item $R$ is the $\RegSeqRatUp$ readback row, carrying regular rational comparison data for the displayed windows.
\item $S$ is the $\RealUp$ seal, recording only terminal real values already read through $R$.
\item $H$ records componentwise $\hsame$ transport for all displayed theorem rows.
\item $C$ records displayed $\Cont$ replay through
$$
K,O,E,B,A,W,R,S \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\TheoremOfAscoliUp$, $\CompactMetricUp$, $\CompactOpenTopologyUp$, $\EquicontinuityFamilyUp$, $\BoundedFunctionFamilyUp$, $\ArzelaAscoliUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\TheoremOfAscoliUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,O,E,B,A,W,R,S,H,C,P,N$. It has no coordinate for host function-space compactness, a selected subsequence, quotient function equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[TheoremOfAscoli NameCert obligations]
\label{thm:theorem-of-ascoli-namecert-obligations}
For every accepted $\TheoremOfAscoliUp$ carrier $T=(K,O,E,B,A,W,R,S,H,C,P,N)$, the local $\NameCert_{\TheoremOfAscoliUp}$ surface consists exactly of the following five rows:
\begin{enumerate}
\item carrier admission for the compact metric source and compact-open topology rows $K,O$;
\item family-control admission for the equicontinuity, bounded-family, and Arzela--Ascoli rows $E,B,A$;
\item value-window admission for the stream-window, regular-rational, and real-seal rows $W,R,S$;
\item componentwise transport and replay through $H,C$;
\item provenance and terminal local naming through $P,N$.
\end{enumerate}
No accepted packet can export host function-space compactness, selected subsequences, quotient function equality, open-cover compactness, ambient real completeness, Lean verification, or bridge checking as part of this local surface.
\end{theorem}

\begin{theorem}[TheoremOfAscoli compact-open handoff]
\label{thm:theorem-of-ascoli-compact-open-handoff}
Every compact-open Ascoli consumer of an accepted $\TheoremOfAscoliUp$ carrier must first pass through
$$
\CompactMetricUp,\quad
\CompactOpenTopologyUp,\quad
\EquicontinuityFamilyUp,\quad
\BoundedFunctionFamilyUp,\quad
\ArzelaAscoliUp,\quad
\StreamNameUp,\quad
\RegSeqRatUp,\quad
\RealUp
$$
before exporting a compact-open convergence read. The exported read is therefore a finite route through $K,O,E,B,A,W,R,S,H,C,P,N$, not a function-space compactness theorem, subsequence choice, quotient function equality, open-cover compactness principle, or ambient completeness assertion.
\end{theorem}

\closureat{TheoremOfAscoliUp}{seedStr}

\begin{closurestatus}{\TheoremOfAscoliUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:theorem-of-ascoli-carrier}, \autoref{thm:theorem-of-ascoli-namecert-obligations}, and \autoref{thm:theorem-of-ascoli-compact-open-handoff} seed the finite compact-open Ascoli packet from compact metric, compact-open topology, family-control, value-window, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function-space compactness, selected subsequences, quotient function equality, open-cover compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-open topology routing, family-control routing, value-window exactness, transport, replay, provenance, and local naming for BEDC.Derived.TheoremOfAscoliUp.}
  \constructivestory{A $\TheoremOfAscoliUp$ packet is generated from CompactMetricUp, CompactOpenTopologyUp, EquicontinuityFamilyUp, BoundedFunctionFamilyUp, ArzelaAscoliUp, StreamNameUp, RegSeqRatUp, RealUp, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
