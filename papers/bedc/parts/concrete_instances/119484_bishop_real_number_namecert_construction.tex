\chapter{A Concrete Naming Certificate for $\BishopRealNumberUp$}
\label{ch:concrete-instances-bishop-real-number-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\BishopRealNumberUp$ packet records the ordinary real-number interface as a finite BEDC NameCert surface rather than as a host quotient or completed object. It is a sibling of the finite stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the located Cauchy modulus route of \autoref{ch:concrete-instances-locatedcauchymodulus-namecert}. The chapter names a traditional constructive real-number carrier for Real and Completion consumers without asserting trichotomy, quotient stream equality, Dedekind-cut completeness, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[BishopRealNumber carrier]
\label{def:bishop-real-number-carrier}
A $\BishopRealNumberUp$ carrier is a finite $\BHist$ packet
$$
B=(S,R,D,E,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite $\StreamNameUp$ schedule that opens the inspected approximation windows;
\item $R$ is the $\RegSeqRatUp$ readback assembled from those windows;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger governing the displayed rational errors;
\item $E$ is the terminal $\RealUp$ seal reached only after the regular rational readback and dyadic ledger have been exposed;
\item $M$ is the $\LocatedCauchyModulusUp$ row recording located tail control for the same readback route;
\item $H$ records componentwise $\hsame$ transport for the schedule, readback, tolerance ledger, seal, modulus, and structural rows;
\item $C$ records the displayed $\Cont$ replay for window inspection, rational readback, tolerance comparison, Real seal delivery, located modulus use, and non-escape inspection;
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\LocatedCauchyModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\BishopRealNumberUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through the rows $S,R,D,E,M,H,C,P,N$.
\end{definition}

\begin{theorem}[BishopRealNumber NameCert obligations]
\label{thm:bishop-real-number-namecert-obligations}
For every accepted $\BishopRealNumberUp$ carrier $B=(S,R,D,E,M,H,C,P,N)$, the local $\NameCert_{\BishopRealNumberUp}$ surface consists exactly of carrier admission for the StreamName schedule, RegSeqRat readback, DyadicRatCore tolerance ledger, RealUp seal, located Cauchy modulus row, componentwise transport, displayed replay, provenance, and local naming row. Any Real or Completion consumer of the packet must read those rows before treating the carrier as a constructive real-number name.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-number-carrier}. The analytic rows are the displayed schedule $S$, regular readback $R$, dyadic ledger $D$, terminal seal $E$, and located modulus $M$.

The structural rows $H,C,P,N$ transport, replay, package, and name those same analytic rows. Since no coordinate stores quotient stream equality, a selected host limit, trichotomy, Dedekind-cut completeness, countable choice, open-cover compactness, or ambient RealUp completeness, the NameCert obligation surface is exactly the displayed finite packet.
\end{proof}

\begin{theorem}[BishopRealNumber RegSeqRat seal]
\label{thm:bishop-real-number-regseqrat-seal}
Every $\RealUp$ seal read from an accepted $\BishopRealNumberUp$ carrier factors first through the finite $\StreamNameUp$ schedule $S$, the $\RegSeqRatUp$ readback $R$, and the $\DyadicRatCoreUp$ tolerance ledger $D$. The located Cauchy modulus row $M$ records tail control for that same route, while $H,C,P,N$ preserve it by transport, replay, provenance, and naming.
\end{theorem}
\begin{proof}
Read the accepted carrier $B=(S,R,D,E,M,H,C,P,N)$. The only rows before the Real-facing endpoint are the finite schedule, regular rational readback, dyadic tolerance ledger, and located modulus.

The seal row $E$ is displayed after those rows in the carrier. Therefore a consumer reaches $\RealUp$ only through the route supplied by $S,R,D,M$, with $R$ providing the regular rational handoff and $D$ providing the dyadic error ledger.

The structural rows do not add another Real seal route. Componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve the same finite readback route, so the packet supplies a RegSeqRat-facing constructive real-number seal and nothing stronger.
\end{proof}

\closureat{BishopRealNumberUp}{seedStr}

\begin{closurestatus}{\BishopRealNumberUp}
  \constructivestory{A $\BishopRealNumberUp$ packet is generated from a StreamName schedule, RegSeqRat readback, DyadicRatCore tolerance ledger, RealUp seal, located Cauchy modulus row, componentwise transport, displayed replay, provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-number-carrier}, \autoref{thm:bishop-real-number-namecert-obligations}, and \autoref{thm:bishop-real-number-regseqrat-seal} bind the finite constructive real-number carrier and its RegSeqRat-to-Real seal route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close trichotomy, quotient stream equality, Dedekind-cut completeness, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or a public standard bridge.}
  \upgradepath{Formal closure requires checked carrier admission, StreamName scheduling, RegSeqRat readback, dyadic tolerance compatibility, RealUp seal delivery, located modulus stability, transport stability, route replay, provenance, and non-escape for the same finite packet.}
\end{closurestatus}
