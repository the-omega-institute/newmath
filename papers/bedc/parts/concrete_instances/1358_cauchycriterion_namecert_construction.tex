\chapter{A Concrete Naming Certificate for $\CauchyCriterionUp$}
\label{ch:concrete-instances-cauchycriterion-namecert}
\origin{human}

The $\CauchyCriterionUp$ interface records the explicit constructive Cauchy criterion used by Bishop and Bridges, \emph{Constructive Analysis}, when a real-facing packet is accepted only after a displayed dyadic tail budget has been checked. It is a finite consumer object over $\CauchyModulusUp$, $\DyadicTailBoundUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$: a precision request is read through an explicit modulus, a dyadic tolerance row, a finite tail-bound ledger, a regular rational handoff, and finally a real seal. The certificate does not form quotient streams, identify host real equality, select a limit, or appeal to ambient completeness.

The precision route consumes the sibling carrier rows of \autoref{ch:concrete-instances-cauchymodulus-namecert}, \autoref{ch:concrete-instances-dyadictailbound-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-regseqrat-namecert} before reaching the $\RealUp$ seal. Thus $M$, $T$, $Q$, and $W$ are not local free parameters: they are imported sibling rows whose finite route is repacked by the $\CauchyCriterionUp$ carrier.

\begin{definition}[CauchyCriterion carrier]
\label{def:cauchycriterion-carrier}
A $\CauchyCriterionUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
K=(W,M,T,Q,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $W$ is the $\RegSeqRatUp$ source window being tested, including the finite rational observations that the consumer may inspect;
\item $M$ is the $\CauchyModulusUp$ schedule that assigns to each displayed precision request the tail threshold used by the test;
\item $T$ is the $\DyadicTailBoundUp$ ledger recording the finite comparisons between tail observations beyond the threshold selected by $M$;
\item $Q$ is the $\DyadicRatCoreUp$ tolerance row whose dyadic budget is used by every comparison in $T$;
\item $E$ is the $\RealUp$ seal row reached after the regular rational handoff has been accepted;
\item $H$ records the componentwise $\hsame$ transports for the source window, modulus schedule, tail ledger, dyadic tolerance, regular-sequence handoff, and seal rows;
\item $C$ records the displayed $\Cont$ routes by which a consumer reads the modulus, tolerance, finite tail ledger, $\RegSeqRatUp$ handoff, and $\RealUp$ seal;
\item $P$ is the $\Pkg$ provenance naming the $\CauchyModulusUp$, $\DyadicTailBoundUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ source certificates;
\item $N$ is the local $\NameCert_{\CauchyCriterionUp}$ provenance row.
\end{enumerate}
The classifier compares two accepted carriers only by these displayed rows, their componentwise $\hsame$ transports, their $\Cont$ read routes, their $\Pkg$ provenance, and their local $\NameCert$ rows.
\end{definition}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_visible\_packet\_terminal\_closure}

\begin{theorem}[CauchyCriterion tail-bound RealUp seal factorization]
\label{thm:cauchycriterion-tail-bound-real-seal}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. Every $\RealUp$ seal read exported from $K$ factors through the displayed Cauchy criterion route:
$$
\mathsf{precision}
\longmapsto
M(\mathsf{precision})
\longmapsto
Q
\longmapsto
T
\longmapsto
W
\longmapsto
E.
$$
Equivalently, the consumer must pass through the $\CauchyModulusUp$ schedule, the $\DyadicRatCoreUp$ tolerance row, the finite $\DyadicTailBoundUp$ ledger, the $\RegSeqRatUp$ source handoff, and the $\RealUp$ seal. No exported read may use a quotient stream, host real equality, a selected limit, or an ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_tail\_bound\_real\_seal}
\begin{proof}
Project the carrier of \autoref{def:cauchycriterion-carrier}. The consumer precision request first reaches the modulus row $M$, which supplies the displayed tail threshold for the requested precision.

The tolerance coordinate $Q$ and tail ledger $T$ are stored beside that threshold. Reading $T$ therefore uses exactly the dyadic budget named by $Q$ and the finite tail entries selected by $M$, not an unrecorded rational radius or an external metric estimate.

The source window $W$ is the regular rational handoff for the same inspected tail. Transport along the recorded $\hsame$ comparisons and replay the $\Cont$ routes in $C$ to keep the modulus, tolerance, ledger, and handoff rows in one visible $\mathsf{BHist}$ packet.

Finally the seal coordinate $E$ is reached only after this regular-sequence handoff. The package row $P$ and local certificate row $N$ record the dependency certificates used by the route, and the carrier has no coordinate for quotient streams, host real equality, selected limits, or ambient completeness. Hence every seal read factors through the displayed criterion route.
\end{proof}

\begin{theorem}[CauchyCriterion NameCert obligation sketch]
\label{thm:cauchycriterion-namecert-obligation-sketch}
The local $\NameCert_{\CauchyCriterionUp}$ sketch has five rows:
\begin{enumerate}
\item carrier habitation: the packet of \autoref{def:cauchycriterion-carrier} stores a $\RegSeqRatUp$ source window, a $\CauchyModulusUp$ schedule, a $\DyadicTailBoundUp$ ledger, a $\DyadicRatCoreUp$ tolerance row, a $\RealUp$ seal, transport routes, package provenance, and local certificate provenance;
\item classifier transport: accepted packets are compared only by componentwise $\hsame$ and displayed $\Cont$ routes over the source window, modulus, tolerance, tail ledger, handoff, seal, $\Pkg$, and $\NameCert$ rows;
\item tail-budget exactness: every accepted tail comparison uses the threshold supplied by $M$ and the tolerance supplied by $Q$ in the same carrier;
\item $\RegSeqRatUp$ handoff: the finite tail ledger repacks as the visible regular rational source window $W$ before any real-facing seal read is exposed;
\item $\RealUp$ seal non-escape: \autoref{thm:cauchycriterion-tail-bound-real-seal} exhausts every exported seal read and withholds quotient streams, host real equality, selected limits, and ambient completeness.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_namecert\_obligations}
\begin{proof}
The carrier and classifier rows are direct projections of the displayed packet. Since the classifier reads only the named coordinates, componentwise $\hsame$ transport and the recorded $\Cont$ routes preserve the accepted carrier shape.

Tail-budget exactness is read from the paired coordinates $M$, $Q$, and $T$. The threshold and tolerance used by a comparison are the rows displayed in the same finite packet, so the criterion cannot substitute hidden bounds.

The regular-sequence handoff is the projection to $W$ after the finite tail ledger has been checked. The handoff keeps the inspected observations, threshold, tolerance, and ledger evidence visible to $\RegSeqRatUp$ consumers.

The seal row is supplied by \autoref{thm:cauchycriterion-tail-bound-real-seal}. It is reached only through the displayed criterion route, so the certificate exports no quotient, host-real, selected-limit, or completeness datum.
\end{proof}

\begin{theorem}[CauchyCriterion RealObservationBudget tail-budget lock]
\label{thm:cauchy-criterion-tail-budget-real-observation-lock}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ packet. Let an accepted $\RealObservationBudgetUp$ read use the same precision request, the same finite $\RegSeqRatUp$ source window, and the same $\DyadicRatCoreUp$ tolerance row as the readback of $K$. Then the observation-budget read factors through the finite CauchyCriterion route $M,Q,T,W,E,H,C,P,N$ before any Real-facing seal is public.

Consequently every such budget read sees the displayed modulus threshold, dyadic tolerance, finite tail ledger, regular rational handoff, real seal, transport, continuation, package, and local naming rows. It receives no quotient stream, host real equality, selected limit, hidden family limit, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_tail\_budget\_real\_observation\_lock}
\begin{proof}
First read the CauchyCriterion seal route. By \autoref{thm:cauchycriterion-tail-bound-real-seal}, any Real-facing seal exposed by $K$ is reached only after the precision request has passed through the modulus row $M$, dyadic tolerance row $Q$, finite tail ledger $T$, regular rational source row $W$, and seal row $E$.

Next read the local naming surface. \autoref{thm:cauchycriterion-namecert-obligation-sketch} records tail-budget exactness, the $\RegSeqRatUp$ handoff, and seal non-escape as rows of the same local $\NameCert$, so the shared precision request, source window, and tolerance row cannot be detached from $H,C,P,N$.

Finally compose with the observation-budget consumer exactness row of \autoref{thm:real-observation-budget-consumer-exactness}. Since that consumer reads only its displayed precision, window, dyadic, regular handoff, seal, transport, route, provenance, and naming rows, the shared rows force the budget read to pass through the same $M,Q,T,W,E,H,C,P,N$ route before the seal is public. The non-escape clauses on both packets exclude quotient streams, host real equality, selected limits, hidden family limits, and ambient completeness.
\end{proof}

\begin{theorem}[CauchyCriterion modulus threshold stability]
\label{thm:cauchycriterion-modulus-threshold-stability}
Let $K=(W,M,T,Q,E,H,C,P,N)$ and $K'=(W',M',T',Q',E',H',C',P',N')$ be accepted $\CauchyCriterionUp$ carriers. Suppose the classifier relates them by the componentwise $\hsame$ rows and displayed $\Cont$ routes for the source window, modulus schedule, tail ledger, dyadic tolerance, regular-sequence handoff, seal row, package provenance, and local $\NameCert$ provenance. Then every precision request read from either carrier has the same displayed threshold coordinate selected by the transported modulus row and the same dyadic tolerance coordinate selected by the transported $Q$ row. In particular, componentwise transport of the $W,M,T,Q,E,H,C,P,N$ packet preserves the threshold chosen by $M$ and the dyadic tolerance row read by $Q$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_modulus\_threshold\_stability}
\begin{proof}
Project both carriers using \autoref{def:cauchycriterion-carrier}. The only modulus coordinate visible to the classifier is $M$ on the first packet and $M'$ on the transported packet, and the only tolerance coordinate visible to the classifier is $Q$ and $Q'$.

The $\hsame$ rows in $H$ and $H'$ identify the corresponding coordinates, while the $\Cont$ routes in $C$ and $C'$ describe how a consumer reaches them. Since the classifier has no access to any further modulus or tolerance carrier, a transported precision request can only be evaluated through the displayed modulus row and the displayed dyadic tolerance row.

The obligation sketch of \autoref{thm:cauchycriterion-namecert-obligation-sketch} makes this the classifier-transport row for the local $\NameCert$. Repacking those projections shows that the selected threshold and tolerance survive transport unchanged as named rows of the accepted $\mathsf{BHist}$ packet.
\end{proof}

\begin{theorem}[CauchyCriterion dyadic tail ledger exactness]
\label{thm:cauchycriterion-dyadic-tail-ledger-exactness}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. Every accepted tail comparison recorded in $T$ is read only through the threshold displayed by $M$, the dyadic tolerance displayed by $Q$, the finite $\DyadicTailBoundUp$ ledger row $T$, and the $\DyadicRatCoreUp$ tolerance certificate named in $P$. No comparison in $T$ may be justified by an undisplayed modulus threshold, an ambient rational radius, a host-real metric estimate, or a tail-bound certificate outside the same carrier.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_dyadic\_tail\_ledger\_exactness}
\begin{proof}
Read the tail comparison from the ledger coordinate $T$ of \autoref{def:cauchycriterion-carrier}. The carrier stores $T$ beside the modulus row $M$ and the dyadic tolerance row $Q$, so the comparison is indexed by the displayed threshold and the displayed dyadic budget.

The package row $P$ names the $\DyadicTailBoundUp$ and $\DyadicRatCoreUp$ source certificates used by this ledger. The $\Cont$ routes in $C$ then replay the same read path from the consumer to the threshold, tolerance, and ledger rows.

By \autoref{thm:cauchycriterion-namecert-obligation-sketch}, tail-budget exactness is one of the local $\NameCert$ rows. Thus an accepted comparison can be repacked only with the displayed $M$, $Q$, $T$, and $P$ data, and there is no carrier coordinate from which an undisplayed radius, host-real estimate, or external ledger certificate could be exported.
\end{proof}

\begin{theorem}[CauchyCriterion RegSeqRat handoff exhaustion]
\label{thm:cauchycriterion-regseqrat-handoff-exhaustion}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. Any $\RegSeqRatUp$-to-$\RealUp$ consumer read exported from $K$ factors through the finite route
$$
W \longmapsto M \longmapsto Q \longmapsto T \longmapsto E.
$$
Consequently the public read exports only the displayed source window, modulus threshold, dyadic tolerance, tail ledger, and seal row. It exports no quotient stream, selected limit, host real equality, uniqueness-of-limit claim, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_regseqrat\_handoff\_exhaustion}
\begin{proof}
Begin with the $\RegSeqRatUp$ source window $W$ in the carrier of \autoref{def:cauchycriterion-carrier}. A real-facing consumer must then pass through the modulus row $M$, because the accepted tail entries are defined only beyond the threshold selected by that schedule.

Next the read passes through $Q$ and $T$. The dyadic row supplies the tolerance, and \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness} says that the ledger accepts comparisons only through this displayed threshold and tolerance.

Finally the seal coordinate $E$ is reached by the factorization of \autoref{thm:cauchycriterion-tail-bound-real-seal}. The obligation sketch records this as the regular rational handoff and $\RealUp$ seal non-escape rows, so repacking the route exhausts every exported consumer read without adding quotient streams, selected limits, host real equality, uniqueness of limits, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyCriterion budgeted shared-tail seal compatibility]
\label{thm:cauchycriterion-budgeted-shared-tail-seal-compatibility}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier whose finite regular rational source window is also presented through the $\StreamNameUp$ shared-tail budget route of \autoref{thm:streamname-real-regseq-shared-tail-budget-route}. Then every $\RealUp$-facing seal read exported from $K$ factors through one common finite route consisting of the Cauchy modulus schedule $M$, the dyadic tolerance row $Q$, the tail ledger $T$, the $\RegSeqRatUp$ source row $W$, the finite $\StreamNameUp$ window rows, the $\DyadicTailRadiusLedgerUp$ tail-budget coverage row of \autoref{thm:dyadic-tail-radius-ledger-tail-budget-coverage}, the $\RegularCauchyTailMeetUp$ public shared-threshold certificate of \autoref{thm:regular-cauchy-tail-meet-public-shared-threshold-certificate}, and the seal row $E$.

The common route exports no quotient stream, selected limit, host real equality, countable-choice tail selector, independent tail oracle, metric completeness theorem, or ambient $\RealUp$ completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_budgeted\_shared\_tail\_seal\_compatibility}
\begin{proof}
First apply \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}. It places each consumer route from the regular rational source to the seal inside the displayed $W,M,Q,T,E$ packet and excludes quotient streams, selected limits, host real equality, uniqueness of limits, and ambient completeness.

Next apply \autoref{thm:cauchycriterion-tail-bound-real-seal}. The seal row $E$ is therefore reached only after the modulus schedule, dyadic tolerance row, finite tail ledger, and regular rational handoff have all been read from the same accepted $\CauchyCriterionUp$ carrier.

Now use \autoref{thm:streamname-real-regseq-shared-tail-budget-route} on the finite StreamName presentation of $W$. This identifies the same public readback and seal service with the finite $\StreamNameUp$ window rows, $\mathsf{ProbeBundle}$, $\mathsf{InBundle}$, $\mathsf{UnaryHistory}$, the dyadic tail-radius ledger, the shared threshold, the $\RegSeqRatUp$ readback, and the $\RealUp$ finite-window ledger boundary.

Finally cite \autoref{thm:dyadic-tail-radius-ledger-tail-budget-coverage} and \autoref{thm:regular-cauchy-tail-meet-public-shared-threshold-certificate}. The first fixes the admitted tail-radius read inside the finite precision, tail-window, StreamName-window, and dyadic readback rows; the second fixes the public shared-threshold row used by both the regular rational readback and the seal-facing route. Repacking these identifications gives the single finite route named in the statement, and none of the cited carriers contains a quotient stream, selected limit, host real equality, countable-choice selector, independent tail oracle, metric completeness theorem, or ambient $\RealUp$ completeness principle.
\end{proof}

\begin{theorem}[CauchyCriterion tail-budget coherence consumption]
\label{thm:cauchy-criterion-tail-budget-coherence-consumption}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier whose shared-tail seal route is presented as in \autoref{thm:cauchycriterion-budgeted-shared-tail-seal-compatibility}. If a $\TailBudgetCoherenceUp$ packet consumes this carrier at the Real-completion handoff, then the consumer must read the handoff as one finite boundary through the displayed CauchyCriterion rows $M,Q,T,W,E$, the finite $\StreamNameUp$ window rows, the $\DyadicTailRadiusLedgerUp$ coverage row, the $\RegularCauchyTailMeetUp$ shared-threshold row, and the displayed $\TailBudgetCoherenceUp$ rows
$$
M_B,O_B,S_B,W_B,D_B,R_B,A_B,L_B,H_B,C_B,P_B,N_B.
$$

The consumption route exports no quotient stream, selected limit, host real equality, countable-choice selector, detached tail oracle, metric-completeness theorem, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchycriterion-budgeted-shared-tail-seal-compatibility}. It binds every seal-facing read from $K$ to the finite route through the Cauchy modulus schedule, dyadic tolerance row, tail ledger, regular rational source row, StreamName windows, dyadic tail-radius coverage row, shared threshold row, and seal row $E$.

Next apply the primitive scope and sibling-scope lock of \autoref{thm:tail-budget-coherence-primitive-scope} and \autoref{thm:tail-budget-coherence-sibling-scope-lock}. These theorems say that a $\TailBudgetCoherenceUp$ consumer has no carrier rows beyond the named TailMeet, observation-budget, selector-budget, agreement-seal, limit-seal, StreamName, regular readback, dyadic-tolerance, transport, continuation, provenance, and local NameCert coordinates.

Finally use the route-scope and seal boundary results \autoref{thm:tail-budget-coherence-real-route-scope} and \autoref{thm:tail-budget-coherence-seal-nonescape}. They force the Real-completion consumer to pass through the TailBudgetCoherence rows only after the finite threshold, budget, window, tolerance, and readback surface has been displayed. Combining this with the CauchyCriterion shared-tail seal route gives one finite boundary, and none of the cited packets supplies quotient streams, selected limits, host real equality, countable-choice selectors, detached tail oracles, metric completeness, or ambient $\RealUp$ completeness.
\end{proof}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_tail\_budget\_coherence\_consumption}

\begin{theorem}[CauchyCriterion tail-budget observer route]
\label{thm:cauchycriterion-tail-budget-observer-route}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier whose shared-tail route is presented by \autoref{thm:cauchycriterion-budgeted-shared-tail-seal-compatibility}. Any observer-budget or finite-window observation consumer of $K$ must factor through the displayed Cauchy modulus schedule $M$, dyadic tolerance row $Q$, tail ledger $T$, $\RegSeqRatUp$ source row $W$, finite $\StreamNameUp$ window rows, shared-threshold certificate, and Real seal $E$ before observing the public Real-facing endpoint.

The route exports no quotient stream, selected limit, host real equality, detached tail oracle, or ambient completeness principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchycriterion-budgeted-shared-tail-seal-compatibility}. It places every Real-facing seal read on the shared finite route through $M,Q,T,W$, the StreamName windows, the dyadic tail-radius coverage row, the regular shared-threshold certificate, and the seal row $E$.

Next use \autoref{thm:cauchy-criterion-tail-budget-coherence-consumption}. A downstream observation consumer can attach to that route only after the TailBudgetCoherence rows have made the finite threshold, budget, window, tolerance, readback, transport, continuation, package, and local naming data visible.

Finally apply RealObservationBudget consumer exactness, \autoref{thm:real-observation-budget-consumer-exactness}. It reads observation budgets and finite windows through the same displayed precision and source rows, so the public endpoint is observed only after the Cauchy modulus schedule, tolerance row, tail ledger, source row, shared-threshold certificate, and seal have been exposed. The excluded quotient streams, selected limits, host real equalities, detached tail oracles, and ambient completeness principles are absent from the cited routes.
\end{proof}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_tail\_budget\_observer\_route}

\begin{theorem}[CauchyCriterion uniform-family quotient-refusal factorization]
\label{thm:cauchycriterion-uniform-family-quotient-refusal-factorization}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier whose shared-tail route is presented through the budgeted seal theorem of \autoref{thm:cauchycriterion-budgeted-shared-tail-seal-compatibility}. Any finite-family or uniform-family consumer that asks for quotient-name equality, a selected limit, host real equality, hidden tail data, or ambient completeness must first consume the displayed CauchyCriterion rows $M,Q,T,W,E$, the finite $\StreamNameUp$ window rows, the $\DyadicTailRadiusLedgerUp$ coverage row, the $\RegularCauchyTailMeetUp$ shared-threshold row, the displayed $\TailBudgetCoherenceUp$ rows, and then the $\QuotientStreamRefusalUp$ refusal row of \autoref{thm:quotient-stream-refusal-regular-tail-factorization}.

Consequently the accepted carrier exports no quotient stream, selected limit, host equality, tail oracle, metric-completeness row, or ambient $\RealUp$ completeness principle to such a family consumer.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchycriterion-budgeted-shared-tail-seal-compatibility}. This places every seal-facing read from $K$ on the finite shared-tail route through $M,Q,T,W,E$, the StreamName window rows, the dyadic tail-radius coverage row, and the regular shared-threshold row.

Next apply \autoref{thm:cauchy-criterion-tail-budget-coherence-consumption}. A Real-completion family consumer can therefore attach to the route only after the TailBudgetCoherence rows have been displayed as threshold, budget, window, tolerance, readback, transport, continuation, package, and local naming data.

Now route the family request through \autoref{thm:quotient-stream-refusal-regular-tail-factorization}. A regular-tail read that reaches Real-completion must pass through the displayed StreamName, RegSeqRat, CauchyLimitSeal, RealUp seal, and refusal-ledger rows before any quotient-style request is interpreted.

Finally use \autoref{thm:quotient-stream-refusal-offroute-nonescape}. Requests for quotient streams, selected limits, host real equality, hidden tail data, metric-completeness rows, or ambient $\RealUp$ completeness are off the displayed route and are sent to the refusal ledger rather than exported as witnesses.
\end{proof}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_uniform\_family\_quotient\_refusal\_factorization}

\begin{definition}[CauchyCriterion classifier]
\label{def:cauchycriterion-classifier}
For accepted $\CauchyCriterionUp$ carriers
$$
K=(W,M,T,Q,E,H,C,P,N)
\qquad\text{and}\qquad
K'=(W',M',T',Q',E',H',C',P',N'),
$$
the $\CauchyCriterionUp$ classifier relates $K$ and $K'$ exactly when the accepted packets agree componentwise on the displayed source window, modulus schedule, tail ledger, dyadic tolerance, seal row, transport ledger, continuity-route ledger, package provenance, and local certificate provenance.

Thus the comparison is the finite conjunction of the corresponding $\hsame$ rows and displayed $\Cont$ route comparisons over $W,M,T,Q,E,H,C,P,N$. It has no additional coordinate for quotient streams, hidden real equality, selected limits, uniqueness of limits, or ambient completeness.
\end{definition}
\leandef{BEDC.Derived.CauchyCriterionUp.CauchyCriterionClassifier}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionClassifier\_endpoint\_transport\_closure}

\begin{theorem}[CauchyCriterion precision route normal form]
\label{thm:cauchycriterion-precision-route-normal-form}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. Every accepted precision read exported from $K$ has the normal form
$$
\mathsf{precision}
\longmapsto
M
\longmapsto
Q
\longmapsto
T
\longmapsto
W
\longmapsto
E.
$$
The only rows used by this factorization are the displayed $\mathsf{BHist}$ packet, the componentwise $\hsame$ transports in $H$, the displayed $\Cont$ routes in $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\CauchyCriterionUp}$ row $N$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionRoute\_precision\_endpoint\_closure}
\begin{proof}
Start with the carrier projection of \autoref{def:cauchycriterion-carrier}. A precision request first reads the modulus schedule $M$, which selects the finite threshold at which the tail ledger is inspected.

The dyadic tolerance row $Q$ supplies the comparison budget, and the ledger row $T$ records the accepted finite tail comparisons for that threshold and tolerance. The exactness statement of \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness} prevents replacing these rows by an undisplayed radius or an outside tail certificate.

The regular rational source row $W$ is then the only handoff available before the seal row $E$. The exhaustion theorem \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion} and the seal factorization of \autoref{thm:cauchycriterion-tail-bound-real-seal} show that every real-facing read passes through these rows.

Transport along $H$ and replay the routes in $C$ to keep the read inside the same finite $\mathsf{BHist}$ packet. The package row $P$ and the certificate row $N$ record the source certificates; no remaining carrier coordinate can export quotient streams, selected limits, host real equality, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyCriterion obligation closure package]
\label{thm:cauchycriterion-obligation-closure-package}
The $\CauchyCriterionUp$ chapter presents an obligation-complete paper package for the explicit constructive Cauchy criterion over the displayed finite carrier. The package consists of the carrier of \autoref{def:cauchycriterion-carrier}, the classifier of \autoref{def:cauchycriterion-classifier}, tail-budget exactness from \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness}, the $\RegSeqRatUp$ handoff exhaustion from \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}, the $\RealUp$ seal factorization from \autoref{thm:cauchycriterion-tail-bound-real-seal}, and the non-escape boundary forbidding quotient streams, selected limits, host real equality, uniqueness-of-limit claims, and ambient completeness.

Consequently a future formal target for $\CauchyCriterionUp$ may use this chapter as the paper-side obligation surface for carrier transport, classifier stability, dyadic tail-budget exactness, regular rational handoff exactness, and $\RealUp$ seal factorization, without claiming Lean verification or real completeness here.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_obligation\_closure\_package}
\begin{proof}
The carrier and classifier are displayed by \autoref{def:cauchycriterion-carrier} and \autoref{def:cauchycriterion-classifier}. They identify the finite packet and the componentwise comparison rows on which every accepted read depends.

Tail-budget exactness is supplied by \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness}. It binds each comparison to the same displayed modulus threshold, dyadic tolerance, tail ledger, and package provenance.

The regular rational handoff is exhausted by \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}. The precision-route normal form of \autoref{thm:cauchycriterion-precision-route-normal-form} then names the only route by which a precision request can reach a real-facing seal.

Finally \autoref{thm:cauchycriterion-tail-bound-real-seal} supplies the $\RealUp$ seal factorization and the non-escape boundary. Repacking these clauses gives the stated paper-side obligation package while leaving Lean verification, quotient construction, selected limits, host real equality, uniqueness of limits, and ambient completeness outside the claim.
\end{proof}

\begin{theorem}[CauchyCriterion scoped tail-seal transport]
\label{thm:cauchycriterion-scoped-tail-seal-transport}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. Any transported precision read that preserves the displayed $\mathsf{BHist}$ packet, the componentwise $\hsame$ rows in $H$, the displayed $\Cont$ routes in $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\CauchyCriterionUp}$ row $N$ factors through the kernel-visible route
$$
M \longmapsto Q \longmapsto T \longmapsto W \longmapsto E.
$$
This factorization is stable under the classifier of \autoref{def:cauchycriterion-classifier}: classifier-related carriers transport the same modulus row, dyadic tolerance row, finite tail ledger, regular rational handoff, and $\RealUp$ seal row as a single scoped packet.

The scoped route introduces no quotient stream, selected limit, host real equality, uniqueness-of-limit claim, or ambient completeness row.
\end{theorem}
\begin{proof}
First project the precision route normal form of \autoref{thm:cauchycriterion-precision-route-normal-form}. It identifies the only displayed route from a precision request to the real-facing seal, namely the passage through $M$, $Q$, $T$, $W$, and $E$ inside the same carrier.

The dyadic tail ledger is exact by \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness}. Hence the transported read cannot replace $M$, $Q$, or $T$ by an undisplayed threshold, an outside radius, or an external tail certificate.

Next use \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}. The finite ledger reaches the regular rational source window $W$ before any $\RealUp$ seal row is exposed, so the handoff is part of the same scoped packet rather than a separate stream quotient.

Finally apply the seal factorization of \autoref{thm:cauchycriterion-tail-bound-real-seal}. The classifier of \autoref{def:cauchycriterion-classifier} preserves exactly the displayed carrier rows and their $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ witnesses, so the same $M \longmapsto Q \longmapsto T \longmapsto W \longmapsto E$ route is transported to any classifier-related carrier. Since none of the excluded analytic data is a coordinate of the carrier, none is introduced by the scoped transport.
\end{proof}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_scoped\_tail\_seal\_transport}

\begin{theorem}[CauchyCriterion classifier stability scope]
\label{thm:cauchycriterion-classifier-stability-scope}
Let $K=(W,M,T,Q,E,H,C,P,N)$ and $K'=(W',M',T',Q',E',H',C',P',N')$ be accepted $\CauchyCriterionUp$ carriers. If the classifier of \autoref{def:cauchycriterion-classifier} relates $K$ and $K'$, then the comparison is scoped by one finite dependency packet: the carrier rows $W,M,T,Q,E$, the transport rows in $H$ and $H'$, the displayed $\Cont$ routes in $C$ and $C'$, the $\Pkg$ rows $P,P'$, and the local $\NameCert_{\CauchyCriterionUp}$ rows $N,N'$. Under this packet, classifier stability preserves the same $\mathsf{BHist}$ source, $\hsame$ transport data, $\Cont$ read routes, $\Pkg$ provenance, $\NameCert$ provenance, dyadic tail ledger, $\RegSeqRatUp$ handoff, and $\RealUp$ seal rows, and it introduces no quotient stream, selected limit, host real equality, uniqueness-of-limit claim, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the classifier definition. Its only comparison clauses are the componentwise rows for $W,M,T,Q,E,H,C,P,N$ and the corresponding primed rows, so the carrier and classifier are already bound to the same displayed $\mathsf{BHist}$ packet.

The modulus threshold and tolerance coordinates are stable by \autoref{thm:cauchycriterion-modulus-threshold-stability}. These clauses identify the route by which a consumer reads $M$ and $Q$ after transport.

The tail ledger and regular rational handoff remain inside the same packet by \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness} and \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}. The seal row is then reached by \autoref{thm:cauchycriterion-tail-bound-real-seal}.

Thus every classifier-related read is repacked through $H,H'$, $C,C'$, $P,P'$, and $N,N'$ over the displayed carrier rows. Since the classifier has no field for the excluded analytic data, stability cannot export such a field.
\end{proof}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_classified\_tail\_budget\_transport}

\begin{theorem}[CauchyCriterion tail-budget scope]
\label{thm:cauchycriterion-tail-budget-scope}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. Every scoped tail-budget read in $K$ is bound to the same finite packet as the carrier and classifier: the displayed $\mathsf{BHist}$ row for $K$, the $\hsame$ transports in $H$, the $\Cont$ routes in $C$, the $\Pkg$ provenance row $P$, the local $\NameCert_{\CauchyCriterionUp}$ row $N$, the dyadic threshold and tolerance rows $M,Q$, the tail ledger $T$, the $\RegSeqRatUp$ handoff row $W$, and the $\RealUp$ seal row $E$. Consequently tail-budget exactness is not a separate analytic assumption; it is the packet-local claim that each accepted comparison uses the threshold selected by $M$ and the dyadic budget recorded by $Q$ before reaching $W$ and $E$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionTasteGate\_single\_carrier\_alignment}
\begin{proof}
Begin with the ledger projection $T$. The exactness theorem \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness} says that each accepted comparison in $T$ is read through the displayed threshold $M$, tolerance $Q$, ledger row $T$, and package provenance $P$.

The $\hsame$ and $\Cont$ rows keep that ledger read in the same carrier. The transport row $H$ records componentwise sameness for the carrier rows, while $C$ records the consumer route by which the threshold, tolerance, and ledger are reached.

The handoff to $W$ is supplied by \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}, and the seal row $E$ is supplied by \autoref{thm:cauchycriterion-tail-bound-real-seal}. These two results bind the tail budget to the regular rational source and the real-facing seal without adding a new carrier coordinate.

Repacking the displayed clauses with the local $\NameCert$ row $N$ gives the stated scoped tail-budget packet. No outside modulus, rational radius, host metric estimate, selected limit, or ambient completeness datum can enter, because none is a row of $K$.
\end{proof}

\begin{theorem}[CauchyCriterion shared tail-budget seal comparison]
\label{thm:cauchycriterion-shared-tail-budget-seal-comparison}
Let $K=(W,M,T,Q,E,H,C,P,N)$ and $K'=(W',M',T',Q',E',H',C',P',N')$ be accepted $\CauchyCriterionUp$ carriers related by the classifier of \autoref{def:cauchycriterion-classifier}. Suppose their modulus rows and dyadic tolerance rows select the same displayed tail-budget packet. Then the two $\RealUp$ seal reads factor through the same finite route
$$
M \longmapsto Q \longmapsto T \longmapsto W \longmapsto E
$$
and
$$
M' \longmapsto Q' \longmapsto T' \longmapsto W' \longmapsto E',
$$
with the two routes compared only by the shared tail-budget rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local \NameCert{} rows. The comparison exports no selected limit, uniqueness-of-limit claim, host metric estimate, quotient stream equality, host real equality, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_shared\_tail\_budget\_seal\_comparison}
\begin{proof}
First apply the classifier stability scope of \autoref{thm:cauchycriterion-classifier-stability-scope}. The comparison of $K$ and $K'$ is already confined to the displayed carrier rows, the componentwise $\hsame$ rows, the $\Cont$ routes, provenance, and local certificates.

Use the tail-budget scope of \autoref{thm:cauchycriterion-tail-budget-scope}. The shared modulus and dyadic tolerance hypothesis identifies the tail-budget packet read by $M,Q,T$ with the packet read by $M',Q',T'$.

Next apply the scoped tail-seal transport theorem \autoref{thm:cauchycriterion-scoped-tail-seal-transport}. It forces each transported precision read to pass through the finite route from modulus to dyadic tolerance, tail ledger, regular rational handoff, and seal row.

Finally use the precision route normal form of \autoref{thm:cauchycriterion-precision-route-normal-form}. Repacking the two displayed routes through the shared tail budget gives the stated comparison. None of the cited carrier rows contains selected limits, uniqueness of limits, host metric estimates, quotient equality, host real equality, or ambient completeness, so those objects cannot be exported.
\end{proof}

\begin{theorem}[CauchyCriterion RealUp seal scope]
\label{thm:cauchycriterion-real-seal-scope}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. The $\RealUp$ seal row $E$ is scoped by the same finite packet that supports the carrier, classifier, dyadic tail ledger, and $\RegSeqRatUp$ handoff. In particular, every exported seal read factors through $M$, $Q$, $T$, and $W$ while retaining the displayed $\mathsf{BHist}$ carrier, $\hsame$ transport rows, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\CauchyCriterionUp}$ row. The seal scope therefore records a packet-local endpoint for the constructive Cauchy criterion and does not assert real completeness, quotient equality of streams, a selected limit, host real equality, or uniqueness of limits.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_real\_seal\_scope}
\begin{proof}
Use the precision route normal form of \autoref{thm:cauchycriterion-precision-route-normal-form}. It gives the displayed path from a precision request through $M$, $Q$, $T$, $W$, and $E$ inside the carrier.

The scoped tail-seal transport theorem \autoref{thm:cauchycriterion-scoped-tail-seal-transport} keeps this path stable under the carrier transports, $\Cont$ read routes, package provenance, and local certificate row. Thus the seal is not read from a separate real object or an ambient limit construction.

The classifier stability scope of \autoref{thm:cauchycriterion-classifier-stability-scope} then shows that classifier-related carriers preserve the same seal-facing dependency packet. The tail-budget scope of \autoref{thm:cauchycriterion-tail-budget-scope} supplies the exact threshold, tolerance, and ledger rows used before the handoff reaches $W$.

Finally apply \autoref{thm:cauchycriterion-tail-bound-real-seal}. The seal row is the endpoint of the displayed constructive criterion route, and the carrier has no coordinate for completeness, quotient equality, selected limits, host real equality, or uniqueness of limits.
\end{proof}

\begin{theorem}[CauchyCriterion public certificate]
\label{thm:cauchycriterion-public-certificate}
The carrier, classifier, tail-bound $\RealUp$ seal factorization, $\NameCert$ obligation sketch, modulus threshold stability, dyadic tail ledger exactness, $\RegSeqRatUp$ handoff exhaustion, precision route normal form, obligation closure package, scoped tail-seal transport, classifier stability scope, tail-budget scope, and $\RealUp$ seal scope assemble one exported $\CauchyCriterionUp$ public certificate over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\CauchyModulusUp$, $\DyadicTailBoundUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.

This certificate is exactly the finite constructive Cauchy criterion packet of \autoref{def:cauchycriterion-carrier} and \autoref{def:cauchycriterion-classifier}. It preserves the exclusions on quotient streams, selected limits, host real equality, uniqueness of limits, ambient completeness, and bridge checking.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_public\_certificate}
\begin{proof}
First take the carrier and classifier from \autoref{def:cauchycriterion-carrier} and \autoref{def:cauchycriterion-classifier}. These two displays provide the finite $\mathsf{BHist}$ packet, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\CauchyCriterionUp}$ row.

Next attach the obligation rows. The tail-bound seal factorization is \autoref{thm:cauchycriterion-tail-bound-real-seal}, the local naming obligations are \autoref{thm:cauchycriterion-namecert-obligation-sketch}, and the modulus threshold stability is \autoref{thm:cauchycriterion-modulus-threshold-stability}.

The dyadic and rational handoff data come from \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness}, \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}, and \autoref{thm:cauchycriterion-precision-route-normal-form}. Together they force every precision route through the displayed modulus, dyadic tolerance, finite tail ledger, regular rational source window, and seal row.

Finally repack the scoped surface using \autoref{thm:cauchycriterion-obligation-closure-package}, \autoref{thm:cauchycriterion-scoped-tail-seal-transport}, \autoref{thm:cauchycriterion-classifier-stability-scope}, \autoref{thm:cauchycriterion-tail-budget-scope}, and \autoref{thm:cauchycriterion-real-seal-scope}. Since each cited theorem stays inside the same visible packet and records the same non-escape boundary, the assembled public certificate exports no quotient stream, selected limit, host real equality, uniqueness-of-limit claim, ambient completeness principle, or checked bridge.
\end{proof}

\begin{theorem}[CauchyCriterion Real-completion consumer bridge]
\label{thm:cauchycriterion-real-completion-consumer-bridge}
Every exported $\CauchyCriterionUp$ public packet consumed by a $\RealUp$ or completion-facing interface factors through the displayed modulus, dyadic tolerance, finite tail ledger, $\RegSeqRatUp$ window, $\RealUp$ seal row, $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row of the same finite carrier. The bridge exports no quotient stream, selected limit, host real equality, uniqueness-of-limit claim, ambient completeness principle, or bridge-checked verification.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_real\_completion\_consumer\_bridge}
\begin{proof}
Start from the public packet of \autoref{thm:cauchycriterion-public-certificate}. It already assembles the carrier, classifier, tail-bound seal factorization, local naming obligations, modulus threshold stability, dyadic tail ledger exactness, $\RegSeqRatUp$ handoff, precision route normal form, scoped transport, classifier stability, tail-budget scope, and $\RealUp$ seal scope.

The consumer-facing route is the same finite route. The modulus threshold is fixed by \autoref{thm:cauchycriterion-modulus-threshold-stability}, the dyadic tolerance and tail ledger are fixed by \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness}, and the regular rational handoff is fixed by \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}.

The $\RealUp$ endpoint is then read through \autoref{thm:cauchycriterion-tail-bound-real-seal}, \autoref{thm:cauchycriterion-precision-route-normal-form}, \autoref{thm:cauchycriterion-scoped-tail-seal-transport}, and \autoref{thm:cauchycriterion-real-seal-scope}. These theorems keep the endpoint inside the carrier rows rather than turning it into a host completeness assertion.

Repack those rows as the downstream bridge. Since every cited clause remains inside the displayed packet and the public certificate explicitly preserves the same exclusions, a Real-facing or completion-facing consumer receives the constructive criterion route but no quotient stream, selected limit, host real equality, uniqueness-of-limit theorem, ambient completeness principle, or checked bridge status.
\end{proof}

\begin{theorem}[CauchyCriterion modulus-tail consumer exactness]
\label{thm:cauchycriterion-modulus-tail-consumer-exactness}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ carrier. The modulus row $M$, dyadic tail ledger $T$, $\RegSeqRatUp$ handoff $W$, and $\RealUp$ seal row $E$ determine the same consumer-visible tail bound for every $\RealUp$ or completion-facing readback route through $H,C,P,N$.

Equivalently, any such consumer route reads the tail bound by passing through the displayed modulus threshold, the same dyadic tolerance row, the same finite tail ledger, the same regular rational handoff, and the same real seal. It cannot obtain a different bound from quotient streams, selected limits, host real equality, uniqueness of limits, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_modulus\_tail\_consumer\_exactness}
\begin{proof}
Start with \autoref{thm:cauchycriterion-modulus-threshold-stability}. It fixes the threshold row $M$ as the only modulus-facing entry point for a precision request in the carrier.

Apply \autoref{thm:cauchycriterion-dyadic-tail-ledger-exactness}. The dyadic tolerance row and finite tail ledger $T$ read the bound attached to that threshold, so a consumer cannot switch to a different tail comparison after the modulus row has been fixed.

Use \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}. The handoff row $W$ exhausts the regular rational source window that can feed the tail ledger, and the route still passes through the displayed transport, continuation, package, and local naming rows.

Finally apply \autoref{thm:cauchycriterion-real-completion-consumer-bridge}. The $\RealUp$ and completion-facing consumer routes read the same seal row $E$ after the modulus, ledger, and handoff rows have been fixed, so the consumer-visible tail bound is exact for every displayed readback route.
\end{proof}

\begin{theorem}[CauchyCriterion tail-budget seal determinacy]
\label{thm:cauchycriterion-tail-budget-seal-determinacy}
Let $K=(W,M,T,Q,E,H,C,P,N)$ and $K'=(W',M',T',Q',E',H',C',P',N')$ be
accepted $\CauchyCriterionUp$ packets. If they display the same modulus row,
dyadic tolerance row, finite tail ledger, and $\RegSeqRatUp$ handoff, then
they expose the same $\RealUp$ seal route through $H,C,P,N$. The seal route
cannot be changed by quotient stream equality, a selected limit, host real
equality, uniqueness of limits, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_tail\_budget\_seal\_determinacy}
\begin{proof}
Start with the shared tail-budget seal comparison of
\autoref{thm:cauchycriterion-shared-tail-budget-seal-comparison}. The seal
row is read from the displayed modulus threshold, dyadic tail ledger, regular
rational handoff, and real endpoint, not from an ambient limit construction.

Use \autoref{thm:cauchycriterion-real-seal-scope}. The $\RealUp$ seal remains
inside the carrier rows together with the displayed transports,
continuations, package provenance, and local naming row.

Finally apply
\autoref{thm:cauchycriterion-modulus-tail-consumer-exactness}. Once the
modulus row, dyadic tolerance row, tail ledger, and $\RegSeqRatUp$ handoff are
the same, every completion-facing readback route sees the same consumer-visible
tail bound and hence the same seal route through $H,C,P,N$.
\end{proof}

\begin{theorem}[CauchyCriterion RegSeqRat Real route lock]
\label{thm:cauchycriterion-regseqrat-real-route-lock}
Every accepted $\CauchyCriterionUp$ public $\RealUp$-seal consumer factors
through the Cauchy tail-bound row, the $\RegSeqRatUp$ handoff row, the
$\StreamNameUp$ finite-window row, the dyadic tolerance ledger, and the
$\RealUp$ seal row of the displayed packet. No such consumer may pass through
host real equality, a selected external limit, a quotient stream, or an ambient
completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_regseqrat\_real\_route\_lock}
\begin{proof}
Start with \autoref{thm:cauchycriterion-tail-bound-real-seal}. It places the
tail-bound read inside the displayed modulus, dyadic tolerance, tail ledger,
regular rational source, and real seal rows of the accepted packet.

Next use \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}. The
$\RegSeqRatUp$ handoff exhausts the regular rational source rows available to
the CauchyCriterion consumer, so the route cannot replace them by quotient
stream data or a selected external limit.

The finite-window input is supplied by
\autoref{thm:streamname-real-regseq-finite-window-handoff}, and the real
endpoint is kept inside the packet by
\autoref{thm:cauchycriterion-real-seal-scope}. Thus the consumer route reaches
$\RealUp$ through the same finite window, source row, dyadic ledger, and seal
row.

Finally apply \autoref{thm:cauchycriterion-tail-budget-seal-determinacy}. Any
route with the same modulus row, dyadic tolerance row, finite tail ledger, and
$\RegSeqRatUp$ handoff exposes the same $\RealUp$ seal route through
$H,C,P,N$, so alternate routes using host equality, external limits, quotient
streams, or ambient completeness coordinates are ruled out.
\end{proof}

\begin{theorem}[CauchyCriterion completion-facing readback normal form]
\label{thm:cauchycriterion-completion-facing-readback-normal-form}
Every completion-facing consumer route for an accepted $\CauchyCriterionUp$
packet normalizes to the displayed route
$$
M \to Q \to T \to W \to E
$$
together with the transport, continuation, provenance, and local naming rows
$H,C,P,N$. No normal form may use a selected limit, quotient stream, host real
equality, uniqueness-of-limit theorem, ambient completeness principle, or
unrecorded completion object.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_completion\_facing\_readback\_normal\_form}
\begin{proof}
First use the public certificate
\autoref{thm:cauchycriterion-public-certificate}. It assembles the carrier,
classifier, modulus row, dyadic tolerance row, finite tail ledger,
$\RegSeqRatUp$ handoff, $\RealUp$ seal, transports, continuations,
provenance, and local certificate into one finite packet.

Next apply
\autoref{thm:cauchycriterion-real-completion-consumer-bridge}. Every
completion-facing consumer factors through the same displayed modulus,
dyadic tolerance, finite tail ledger, regular rational window, and real seal
route.

The exactness theorem
\autoref{thm:cauchycriterion-modulus-tail-consumer-exactness} fixes the order
of the readback: the modulus row feeds the dyadic tolerance and tail ledger,
the ledger feeds the regular rational handoff, and the handoff reaches the
$\RealUp$ seal through $H,C,P,N$.

Thus the consumer route has the stated normal form and has no coordinate from
which to read selected limits, quotient stream equality, host real equality,
uniqueness of limits, ambient completeness, or an unrecorded completion
object.
\end{proof}

\begin{theorem}[CauchyCriterion RegSeqRat StreamName RealUp handoff]
\label{thm:cauchycriterion-regseqrat-streamname-realup-handoff}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$
carrier. Suppose the $W$ row is read from a finite public
$\StreamNameUp$ observation window and is accepted by the $\RegSeqRatUp$
handoff. Then every $\RealUp$ seal read of $K$ factors through exactly the
route
$$
\StreamNameUp\text{ finite window}
\to
\RegSeqRatUp\text{ source row}
\to
\CauchyModulusUp,\DyadicTailBoundUp,\DyadicRatCoreUp\text{ budget rows}
\to
\RealUp\text{ seal}.
$$
No quotient stream, selected host limit, host real equality, or ambient
completeness row can enter this route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_regseqrat\_streamname\_realup\_handoff}
\begin{proof}
Start with the carrier definition. Its displayed rows expose the regular
rational handoff $W$, modulus row $M$, tail ledger $T$, dyadic tolerance
row $Q$, real seal row $E$, and the transport, continuation, provenance,
and local naming rows $H,C,P,N$.

Apply \autoref{thm:cauchycriterion-tail-bound-real-seal}. The tail-bound
budget is read through the modulus, dyadic tolerance, tail ledger, and
real seal rows already present in $K$.

Use \autoref{thm:cauchycriterion-regseqrat-handoff-exhaustion}. The
$\RegSeqRatUp$ handoff exhausts the finite source rows available from the
displayed observation window, so the source cannot be replaced by a
quotient stream or by a selected host limit.

Finally apply
\autoref{thm:cauchycriterion-completion-facing-readback-normal-form}. The
completion-facing route normalizes to the same displayed rows. Together
with the sibling inputs named in the chapter introduction for
$\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$, this
binds every seal read to the stated finite handoff route and excludes host
real equality and ambient completeness rows.
\end{proof}

\begin{theorem}[CauchyCriterion diagonal-budget readback compatibility]
\label{thm:cauchycriterion-diagonal-budget-readback-compatibility}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$
carrier, and let
$B=(\epsilon,M_B,W_B,D_B,K_B,S_B,H_B,C_B,P_B,N_B)$ be an accepted
$\CauchyDiagonalBudgetUp$ packet through which the $W,Q,E$ rows of $K$ are
read by a completion-facing consumer. Then every such readback first
normalizes to the CauchyCriterion route
$$
M \to Q \to T \to W \to E
$$
with $H,C,P,N$, and then to the diagonal-budget route
$$
\epsilon \to M_B \to W_B \to D_B \to K_B \to S_B
$$
with $H_B,C_B,P_B,N_B$. No quotient stream, selected limit, host real
equality, uniqueness-of-limit theorem, trichotomy row, or ambient
completeness coordinate may enter either side of the readback.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_diagonal\_budget\_readback\_compatibility}
\begin{proof}
First apply
\autoref{thm:cauchycriterion-completion-facing-readback-normal-form}. It
normalizes every completion-facing read of $K$ to
$M \to Q \to T \to W \to E$ with the displayed transport, continuation,
provenance, and naming rows.

Next use
\autoref{thm:cauchycriterion-regseqrat-streamname-realup-handoff}. The
$W$ row is read through the finite $\StreamNameUp$ and $\RegSeqRatUp$
handoff, while $Q$ supplies the dyadic budget row and $E$ supplies the
$\RealUp$ seal.

Now pass to the diagonal-budget selector. The route factorization theorem
\autoref{thm:cauchy-diagonal-budget-route-factorization} identifies every
completion-facing read through $B$ with the finite route from $\epsilon$
through $M_B,W_B,D_B,K_B$ to $S_B$.

Finally apply
\autoref{thm:cauchy-diagonal-budget-regseq-real-route-lock} and
\autoref{thm:cauchy-diagonal-budget-shared-tail-modulus-seal-factorization}.
These keep the RegSeqRat-to-Real handoff and the shared-tail modulus seal on
the displayed diagonal-budget route.

The non-escape scope theorem
\autoref{thm:cauchy-diagonal-budget-nonescape-scope} then rules out
quotient streams, selected limits, host real equality, uniqueness-of-limit
inputs, trichotomy rows, and ambient completeness coordinates.
\end{proof}

\begin{theorem}[CauchyCriterion RealTailAgreementSeal route correspondence]
\label{thm:cauchycriterion-real-tail-agreement-seal-route-correspondence}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$
packet. Every accepted completion-facing consumer of $K$ whose $\RealUp$
seal is mediated by an accepted $\RealTailAgreementSealUp$ packet factors
through the same finite route
$$
\ObservationBudgetLimiterUp,\ \RegularCauchyTailBudgetUp,\
\RegularCauchyTailMeetUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\
\RegSeqRatUp,\ \RealUp,\ \CompletionUp.
$$
Conversely, every terminal $\RealTailAgreementSealUp$ route supplies the
CauchyCriterion tail-budget, $\RegSeqRatUp$, and $\RealUp$ seal rows needed
by \autoref{thm:cauchycriterion-regseqrat-streamname-realup-handoff}. The
correspondence exports no selected limit, quotient-stream equality,
uncapped observation, host real equality, ambient completeness principle, or
second agreement coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_real\_tail\_agreement\_seal\_route\_correspondence}
\begin{proof}
Start from the CauchyCriterion handoff theorem
\autoref{thm:cauchycriterion-regseqrat-streamname-realup-handoff}. It fixes
the $\StreamNameUp$ source window, the $\RegSeqRatUp$ source row, the dyadic
budget rows, and the $\RealUp$ seal as the only displayed handoff route for
the accepted packet.

The tail-budget seal determinacy theorem
\autoref{thm:cauchycriterion-tail-budget-seal-determinacy} and the
completion-facing readback normal form
\autoref{thm:cauchycriterion-completion-facing-readback-normal-form} then
force every completion-facing consumer to read the same budget, regular
rational, and real-seal rows before any $\CompletionUp$ surface is exposed.

For the mediated case, apply
\autoref{thm:real-tail-agreement-seal-terminal-consumer-route-exhaustion}
and \autoref{thm:real-tail-agreement-seal-budget-route-uniqueness}. These
identify the terminal $\RealTailAgreementSealUp$ consumer route as the
finite path through the observation cap, regular Cauchy tail budget,
shared tail meet, finite stream window, dyadic readback, agreement row, and
the displayed $\RealUp$ and $\CompletionUp$ seal rows.

Projecting this terminal route back to the CauchyCriterion rows gives the
forward factorization. Reading it in the other direction supplies exactly
the tail-budget, $\RegSeqRatUp$, and $\RealUp$ seal rows required by the
handoff theorem, while the non-escape clauses on both packets exclude every
host, quotient, uncapped, completeness, or second-agreement route.
\end{proof}

\begin{theorem}[CauchyCriterion and finite-family UniformCauchyCriterion route agreement]
\label{thm:cauchycriterion-uniformcriterion-finite-family-reduction}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ packet. Let
$U$ be an accepted $\UniformCauchyCriterionUp$ packet whose finite-family
ledger is either singleton or a fixed finite family with the same displayed
shared window row $W$, shared modulus row $M$, dyadic tolerance row $Q$, tail
ledger $T$, and real seal row $E$. Assume that both packets consume the same
$\StreamNameUp$ source route, the same $\RegSeqRatUp$ tail route, and the same
$\RealUp$ seal route as displayed by
\autoref{ch:concrete-instances-streamname-namecert},
\autoref{ch:concrete-instances-regseqrat-namecert}, and
\autoref{ch:concrete-instances-real-namecert}. Then the
$\UniformCauchyCriterionUp$ family route restricts to the same finite
$\CauchyCriterionUp$ route
$$
M\to Q\to T\to W\to E.
$$
Equivalently, the singleton or fixed finite family packet exposes no additional
route beyond the CauchyCriterion modulus, tolerance, tail, window, and seal
rows. The correspondence uses
\autoref{thm:cauchycriterion-real-tail-agreement-seal-route-correspondence},
\autoref{thm:uniform-cauchy-criterion-tail-ledger-exactness}, and
\autoref{thm:uniform-cauchy-criterion-regseqrat-tail-equivalence}, and exports
no quotient stream, selected limit, host real equality, ambient completeness
principle, or independently chosen family limit.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_uniformcriterion\_finite\_family\_reduction}
\begin{proof}
Project the CauchyCriterion packet first. By
\autoref{thm:cauchycriterion-real-tail-agreement-seal-route-correspondence},
the accepted $K$ packet exposes the finite seal route through its displayed
$\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows, and this route is exactly
the CauchyCriterion handoff through $M,Q,T,W,E$.

Now project the uniform packet along its singleton or fixed finite family
ledger. The hypotheses identify the shared window row with $W$, the shared
modulus row with $M$, the dyadic tolerance row with $Q$, the tail ledger with
$T$, and the seal row with $E$ componentwise.

Apply \autoref{thm:uniform-cauchy-criterion-tail-ledger-exactness}. It says
that every accepted family tail comparison in $U$ is read through the displayed
uniform tail ledger after the shared window, shared modulus, and dyadic
tolerance rows have already been fixed.

Apply \autoref{thm:uniform-cauchy-criterion-regseqrat-tail-equivalence}. The
$\RegSeqRatUp$ consumer therefore sees the same finite tail evidence as the
ledger route, so the family-side readback cannot insert a second stream source,
a second modulus schedule, or an unrecorded tolerance row.

The cited $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ construction chapters
fix the source, tail, and seal carriers used by both packets. After the
componentwise identifications above, the uniform route and the single
CauchyCriterion route expose the same finite path $M\to Q\to T\to W\to E$.
The non-export clauses on the two packets then exclude quotient streams,
selected limits, host real equality, ambient completeness, and any additional
family-limit object.
\end{proof}

\begin{theorem}[CauchyCriterion CompleteMetric consumer boundary]
\label{thm:cauchycriterion-complete-metric-consumer-boundary}
Every $\CompleteMetricUp$ consumer of an accepted $\CauchyCriterionUp$ packet
$K=(W,M,T,Q,E,H,C,P,N)$ must pass through the displayed tail-budget route, the
$\RegSeqRatUp$ handoff, and the $\RealUp$ seal before any completion-facing use.
The consumer cannot import quotient streams, selected limits, host real
equality, ambient completeness, or uniqueness of limits.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchycriterion-uniformcriterion-finite-family-reduction}
and \autoref{thm:cauchycriterion-real-tail-agreement-seal-route-correspondence}.
They force the finite tail route through $M,Q,T,W,E$ and place the
$\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows before completion-facing
readback.

The terminal completion routes below then expose only the same sealed endpoint
to $\CompleteMetricUp$ consumers. Since the packet has no quotient stream,
selected limit, host equality, ambient completeness, or uniqueness-of-limit
coordinate, the displayed tail-budget and Real-seal route is the whole
consumer boundary.
\end{proof}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_complete\_metric\_consumer\_boundary}

\begin{theorem}[CauchyCriterion four-face pullback correspondence]
\label{thm:cauchycriterion-streamname-four-face-pullback-correspondence}
Let $K=(W,M,T,Q,E,H,C,P,N)$ be an accepted $\CauchyCriterionUp$ packet. Suppose
its $\StreamNameUp$ finite window row, $\DyadicRatCoreUp$ tolerance and budget
row, $\RegSeqRatUp$ tail and readback row, and $\RealUp$ seal row agree
componentwise with a terminal four-face finite section. Then the
$\CauchyCriterionUp$ consumer route
$$
M\to Q\to T\to W\to E
$$
corresponds exactly to that four-face terminal pullback. Conversely, any
terminal four-face pullback with those four rows supplies the same displayed
$\CauchyCriterionUp$ route. The correspondence exports no quotient stream
equality, selected limit, host sequence equality, ambient Real completeness,
metric-completeness datum, countable-choice row, or bridge row.
\end{theorem}
\begin{proof}
For the forward direction, apply
\autoref{thm:cauchycriterion-real-tail-agreement-seal-route-correspondence} and
\autoref{thm:cauchycriterion-complete-metric-consumer-boundary}. These identify
every CauchyCriterion consumer with the displayed finite route through
$M,Q,T,W,E$ before any completion-facing readback is allowed.

Next apply \autoref{thm:streamname-four-face-terminal-completeness} and
\autoref{thm:streamname-finite-section-pullback-exactness}. Since the
StreamName window, dyadic tolerance and budget, RegSeqRat tail readback, and
Real seal rows are the same displayed rows, terminality reads the
CauchyCriterion route as the unique four-face pullback route.

For the reverse direction, start with such a terminal four-face pullback and
project its four faces. The projected rows are precisely $W,Q,T,E$, while the
accepted packet supplies the modulus row $M$ and the existing $H,C,P,N$
certificates. Repacking these displayed rows gives the same route
$M\to Q\to T\to W\to E$.

The final exclusions are coordinate exclusions. None of the cited terminality
or boundary results adds a quotient equality, a selected limit, host sequence
equality, Real completeness, metric completeness, countable choice, or a bridge
certificate, so the correspondence is only the finite BEDC pullback described
above.
\end{proof}
\leanchecked{BEDC.Derived.CauchyCriterionUp.CauchyCriterionCarrier\_streamname\_four\_face\_pullback\_correspondence}

\input{parts/concrete_instances/cauchycriterion/terminal_completion_route}
\input{parts/concrete_instances/cauchycriterion/streamname_real_budget_selector_boundary}
\input{parts/concrete_instances/cauchycriterion/finite_window_real_seal_handoff}

\closureat{CauchyCriterionUp}{seedStr}
\closureat{CauchyCriterionUp}{obligationStr}
\closureat{CauchyCriterionUp}{scopedStr}
\closureat{CauchyCriterionUp}{publicStr}
\closureat{CauchyCriterionUp}{bridgedStr}
\closureat{CauchyCriterionUp}{matureStr}

\begin{closurestatus}{\CauchyCriterionUp}
  \constructivestory{A $\CauchyCriterionUp$ packet is generated from a $\RegSeqRatUp$ source window, a $\CauchyModulusUp$ schedule, a $\DyadicTailBoundUp$ finite tail ledger, a $\DyadicRatCoreUp$ tolerance row, a $\RealUp$ seal row, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:cauchycriterion-classifier-stability-scope}, \autoref{thm:cauchycriterion-tail-budget-scope}, \autoref{thm:cauchycriterion-real-seal-scope}, \autoref{thm:cauchycriterion-real-completion-consumer-bridge}, and \autoref{thm:cauchycriterion-mature-downstream-examples} bind the carrier, classifier, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, dyadic tail ledger, $\RegSeqRatUp$ handoff, $\RealUp$ seal rows, completion-facing bridge, and representative downstream consumers to one finite constructive Cauchy criterion packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, ambient completeness, uniqueness of limits, or bridge checking.}
  \upgradepath{No further paper-axis theory step remains for this packet; formal verification and bridge checking remain separate verification-axis work.}
\end{closurestatus}
