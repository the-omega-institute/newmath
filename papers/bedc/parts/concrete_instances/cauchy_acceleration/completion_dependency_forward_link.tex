Every completion-facing consumer of $\CauchyAccelerationUp$ reads the displayed acceleration route after \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert} and before the regular rational and Real-facing seals of \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}. The concrete dependency is the finite route from the source $\StreamNameUp$ windows through the $\DyadicRatCoreUp$ tolerance schedule and cofinal acceleration ledger into $\RegSeqRatUp$ readback and the terminal $\RealUp$ boundary; it does not export quotient completion or selected subsequences.
