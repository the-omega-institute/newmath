\begin{theorem}[Cauchy acceleration completion forward link]
\label{thm:cauchy-acceleration-completion-forward-link}
Every completion-facing consumer of $\CauchyAccelerationUp$ reads through \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert} before the accelerated route exports a terminal real boundary. The dependency is the finite route from source $\StreamNameUp$ windows through the $\DyadicRatCoreUp$ tolerance schedule and cofinal acceleration ledger into $\RegSeqRatUp$ readback, the Bishop regular Cauchy completion surface, and the terminal $\RealUp$ seal.
\end{theorem}
\begin{proof}
Start with the displayed source windows. The accelerated route has no completion-facing input until the $\StreamNameUp$ inspection windows and $\DyadicRatCoreUp$ tolerance schedule have been exposed.

Next read the regular rational and completion surfaces. The cofinal acceleration ledger feeds $\RegSeqRatUp$ readback and then passes through the Bishop regular Cauchy completion chapter before any terminal real boundary is visible.

Finally attach the $\RealUp$ seal. Since the route is finite and displayed at each sibling boundary, it exports no quotient completion, selected subsequence, countable choice principle, or ambient $\RealUp$ completeness.
\end{proof}
