\begin{theorem}[Cauchy acceleration sequence-space route]
\label{thm:cauchy-acceleration-sequence-space-route}
Every $\CauchyAccelerationUp$ consumer reads the common-window classifier of $\CauchySequenceSpaceUp$ from \autoref{ch:concrete-instances-cauchysequencespace-namecert} before exporting the accelerated $\RegSeqRatUp$ and $\RealUp$ handoff. The route passes through the regularization boundary of \autoref{ch:concrete-instances-cauchyregularization-namecert}: the source $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, accelerated cofinal ledger, regular rational readback, terminal Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local $\NameCert$ rows are visible before any Real-facing consumer may inspect the acceleration result. No quotient representative, selected subsequence outside the displayed ledger, host equality, or ambient completeness row is exported.
\end{theorem}
