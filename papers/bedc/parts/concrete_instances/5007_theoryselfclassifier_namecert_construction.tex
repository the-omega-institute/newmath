\chapter{A Concrete Naming Certificate for $\TheorySelfClassifierUp$}
\label{ch:concrete-instances-theoryselfclassifier-namecert}
\origin{ai}

The $\TheorySelfClassifierUp$ packet realizes the own-theory reading of \autoref{ch:bedc-is-its-own-theory} as a finite classifier surface. It sits beside \autoref{ch:concrete-instances-internaltheoryboundary-namecert}, \autoref{ch:concrete-instances-metacic-theory-identity-namecert}, \autoref{ch:concrete-instances-axiom-dependency-audit-map-namecert}, and \autoref{ch:concrete-instances-bedcselftheory-namecert}: those siblings expose boundary, identity, dependency-audit, and self-theory rows, while this chapter packages the row comparison that lets a consumer read the five commitments as one self-theory classifier.

\begin{definition}[Theory self-classifier carrier]
\label{def:theory-self-classifier-carrier}
A $\TheorySelfClassifierUp$ carrier is a finite $\BHist$ packet
$$
T=(G,E,R,P,A,L,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the closed-generator row, naming the finite constructor and declaration inventory used by the theory surface;
\item $E$ is the literal equality row, recording that comparison is syntactic equality of the displayed object rows rather than semantic equality in a host calculus;
\item $R$ is the structural recursor row, exposing the eliminators attached to the closed generators named by $G$;
\item $P$ is the purity row, recording zero hidden axiom supply and strict proof-ancestry accountability for internal reads;
\item $A$ is the apophatic boundary row, recording visible sockets and setup fields for requests outside the internal substrate;
\item $L$ is the classifier ledger that compares $G,E,R,P,A$ as one self-theory surface;
\item $H$ records componentwise $\hsame$ transports for generator, equality, recursor, purity, boundary, ledger, route, provenance, and local naming rows;
\item $C$ records the displayed $\Cont$ routes by which consumers read each commitment and then return to the classifier ledger;
\item $Q$ is the $\Pkg$ provenance over \autoref{ch:bedc-is-its-own-theory}, \autoref{ch:concrete-instances-internaltheoryboundary-namecert}, \autoref{ch:concrete-instances-metacic-theory-identity-namecert}, \autoref{ch:concrete-instances-axiom-dependency-audit-map-namecert}, \autoref{ch:concrete-instances-bedcselftheory-namecert}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\TheorySelfClassifierUp}$ row.
\end{enumerate}
The carrier has no coordinate for a classical-CIC parent theory, semantic equality oracle, hidden recursor supply, unrecorded axiom dependency, or boundary row promoted to internal theorem content.
\end{definition}

\begin{theorem}[Theory self-classifier NameCert obligations]
\label{thm:theory-self-classifier-namecert-obligations}
For every $\TheorySelfClassifierUp$ carrier
$$
T=(G,E,R,P,A,L,H,C,Q,N),
$$
the local $\NameCert_{\TheorySelfClassifierUp}$ surface consists exactly of carrier admission, closed-generator exposure, literal-equality exposure, structural-recursor exposure, purity exposure, apophatic-boundary exposure, classifier-ledger comparison, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The ledger row $L$ is required to compare the five commitment rows as one self-theory surface before a consumer may export a self-theory reading.
\end{theorem}
\begin{proof}
Project the finite carrier of \autoref{def:theory-self-classifier-carrier}. The rows $G,E,R,P,A$ are the five commitment coordinates, while $L,H,C,Q,N$ provide comparison, transport, replay, provenance, and local naming.

The NameCert obligations for the five commitments are read componentwise. The generator row supplies the closed inventory, the equality row supplies literal comparison, the recursor row supplies only generator-local eliminators, the purity row supplies strict audit visibility, and the apophatic row keeps external supply visible as boundary data.

The classifier obligation is the additional row $L$. It compares those five rows as one surface, so a consumer cannot cite a self-theory reading by projecting only one commitment or by replacing the row comparison with a sibling chapter.

Since the carrier contains no further coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifierCarrier\_namecert\_obligations}

\begin{theorem}[Theory self-classifier carrier obligation]
\label{thm:theoryselfclassifier-carrier-obligation}
Every accepted $\TheorySelfClassifierUp$ read exposes the finite $\BHist$ carrier
$$
T=(G,E,R,P,A,L,H,C,Q,N)
$$
before a consumer may inspect the self-theory classifier. The carrier rows are closed-generator data, literal equality data, structural recursor data, purity data, apophatic-boundary data, classifier-ledger data, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:theory-self-classifier-carrier}. Its ten displayed rows are exactly $G,E,R,P,A,L,H,C,Q,N$, and no row outside that tuple belongs to the local packet.

Apply the carrier-admission clause of \autoref{thm:theory-self-classifier-namecert-obligations}. Since the classifier row $L$, transport row $H$, replay row $C$, provenance row $Q$, and local naming row $N$ are all carrier coordinates, a consumer cannot inspect the self-theory classifier before the finite $\BHist$ packet has been exposed.
\end{proof}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_carrier\_obligation}

\begin{theorem}[Theory self-classifier classifier obligation]
\label{thm:theoryselfclassifier-classifier-obligation}
The classifier row $L$ of an accepted $\TheorySelfClassifierUp$ carrier compares exactly the five commitment rows $G,E,R,P,A$. It may export a self-theory reading only after those rows have been displayed as closed-generator, literal-equality, structural-recursor, purity, and apophatic-boundary data.
\end{theorem}
\begin{proof}
Read the row order in \autoref{def:theory-self-classifier-carrier}. The ledger row $L$ is not a separate theory object; it is the comparison row attached to the five commitment rows $G,E,R,P,A$.

Use the classifier-ledger clause of \autoref{thm:theory-self-classifier-namecert-obligations}. It requires $L$ to compare the five commitments as one self-theory surface, so no strict subtuple and no external parent-theory row can carry the same classifier read.
\end{proof}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_classifier\_obligation}

\begin{theorem}[Theory self-classifier ledger obligation]
\label{thm:theoryselfclassifier-ledger-obligation}
Every public self-theory read from $\TheorySelfClassifierUp$ keeps the ledger row $L$ visible together with componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $Q$, and local $\NameCert$ row $N$. The read cannot replace $L$ by a hidden semantic equality oracle, hidden recursor supply, unrecorded axiom dependency, or boundary row promoted to internal theorem content.
\end{theorem}
\begin{proof}
Project the structural rows $H,C,Q,N$. They transport, replay, source, and name the classifier row; they do not introduce another classifier coordinate.

Apply \autoref{thm:theory-self-classifier-namecert-obligations}. The public read must pass through carrier admission, five commitment exposures, and classifier-ledger comparison before the structural rows can hand it to a consumer. Finite row projection then leaves no coordinate for a semantic equality oracle, hidden recursor, unrecorded axiom, or promoted boundary theorem.
\end{proof}

\begin{theorem}[Theory self-classifier obligation triple]
\label{thm:theory-self-classifier-obligation-triple}
Every accepted $\TheorySelfClassifierUp$ carrier
$$
T=(G,E,R,P,A,L,H,C,Q,N)
$$
exposes the boundary, identity, dependency-audit, self-theory, and row-comparison obligations as one finite $\NameCert_{\TheorySelfClassifierUp}$ surface. The obligations are read through the displayed commitment rows $G,E,R,P,A$ and the classifier ledger $L$, with $H,C,Q,N$ supplying transport, replay, provenance, and local naming. No consumer may project any single row, or any strict subtuple of the five commitment rows, as the whole self-theory classifier.
\end{theorem}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_obligation\_triple}
\begin{proof}
Project the accepted carrier. The five commitment rows are displayed in the carrier itself: $G$ names the closed-generator boundary, $E$ names literal identity comparison, $R$ names structural recursor exposure, $P$ names dependency audit and purity, and $A$ names the apophatic boundary.

Read the classifier ledger row $L$. Its role is to compare those five rows as one self-theory surface, while $H,C,Q,N$ only transport, replay, source, and name that surface.

If a consumer projects a single row as the classifier, then the remaining commitment rows have no place in the projected packet. If a consumer projects a strict subtuple, the omitted commitment has no alternate coordinate in $T$.

Thus the accepted carrier exposes the five obligation rows and the ledger comparison as one finite NameCert surface, and no proper projection is the whole self-theory classifier.
\end{proof}

\begin{theorem}[Theory self-classifier ledger row inversion]
\label{thm:theory-self-classifier-ledger-row-inversion}
Any accepted public self-theory read through the ledger row $L$ of a $\TheorySelfClassifierUp$ carrier
$$
T=(G,E,R,P,A,L,H,C,Q,N)
$$
determines visible reads of the five commitment rows $G,E,R,P,A$. The same public self-theory read cannot be reproduced from any strict subtuple of those five rows, because no proper subtuple contains the ledger comparison that binds closed generators, literal equality, structural recursors, purity, and apophatic boundary as one self-theory surface.
\end{theorem}
\begin{proof}
Project the carrier $T$. The only row whose role is to compare the five commitments as one public self-theory surface is $L$.

Use the NameCert obligations of \autoref{thm:theory-self-classifier-namecert-obligations}. They force carrier admission for $G,E,R,P,A$ before the ledger row may export a self-theory reading, and they keep $H,C,Q,N$ as structural transport, replay, provenance, and naming rows rather than replacement commitment rows.

It remains to rule out strict subtuples. If one of $G,E,R,P,A$ is absent, then the missing row has no alternate coordinate in the carrier: closed generators, literal equality, structural recursors, purity, and apophatic boundary each occur exactly once in the displayed tuple. The ledger comparison cannot be reconstructed from a tuple that omits one of the rows it compares.

Therefore an accepted public read through $L$ determines all five visible commitment reads and cannot be reproduced from any strict subtuple of them.
\end{proof}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_ledger\_row\_inversion}

\begin{theorem}[Theory self-classifier obligation row exhaustion]
\label{thm:theory-self-classifier-obligation-row-exhaustion}
For every accepted $\TheorySelfClassifierUp$ carrier
$$
T=(G,E,R,P,A,L,H,C,Q,N),
$$
the public obligation inventory exposes exactly the rows $G,E,R,P,A,L,H,C,Q,N$. It exposes no hidden parent-theory row, semantic equality row, external recursor row, unrecorded axiom row, or classifier coordinate outside the displayed ledger.
\end{theorem}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_obligation\_row\_exhaustion}
\begin{proof}
Project the accepted carrier of \autoref{def:theory-self-classifier-carrier}. The tuple displays ten rows: closed generators, literal equality, structural recursors, purity, apophatic boundary, classifier ledger, componentwise transport, displayed replay, provenance, and local naming.

Use the NameCert obligations of \autoref{thm:theory-self-classifier-namecert-obligations}. Each obligation is attached to one of those displayed rows, and the ledger row $L$ is the only row that compares the five commitments as a self-theory surface.

The excluded resources would require further coordinates. A parent theory, semantic equality oracle, external recursor supply, hidden axiom supply, or detached classifier would have to be read outside $G,E,R,P,A,L,H,C,Q,N$. Since the carrier has no such coordinate, the displayed rows exhaust the public obligation inventory.
\end{proof}

\begin{theorem}[Theory self-classifier five-row determinacy]
\label{thm:theory-self-classifier-five-row-determinacy}
Let
$$
T=(G,E,R,P,A,L,H,C,Q,N)
$$
and
$$
T'=(G',E',R',P',A',L',H',C',Q',N')
$$
be accepted $\TheorySelfClassifierUp$ carriers. Equality of their accepted public self-theory reads is determined by equality of the five commitment rows $G,E,R,P,A$ with $G',E',R',P',A'$ together with equality of the ledger comparisons $L$ and $L'$. No strict subtuple of the five commitment rows determines the same public read.
\end{theorem}
\begin{proof}
Read the public self-theory surface through the ledger row. By \autoref{thm:theory-self-classifier-ledger-row-inversion}, any accepted read through $L$ determines visible reads of $G,E,R,P,A$, and the same holds for $L'$ and $G',E',R',P',A'$.

When the five commitment rows agree and the ledger comparisons agree, the public read has the same closed-generator inventory, literal equality policy, structural recursor exposure, purity boundary, apophatic boundary, and ledger comparison. The remaining rows $H,C,Q,N$ transport, replay, source, and name that same read; they do not supply an additional commitment coordinate.

Conversely, if a strict subtuple omits one of $G,E,R,P,A$, the missing commitment has no alternate source in the carrier. The ledger comparison cannot determine a public self-theory surface without the omitted row, so no strict subtuple determines equality of accepted public reads.
\end{proof}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_five\_row\_determinacy}

\begin{theorem}[Theory self-classifier non-escape]
\label{thm:theory-self-classifier-nonescape}
Every public read from an accepted $\TheorySelfClassifierUp$ carrier factors through
$$
G,E,R,P,A,L,H,C,Q,N.
$$
A consumer may read the five commitment rows, their classifier ledger, componentwise transports, displayed routes, provenance, and local name. It cannot read a classical-CIC parent theory, a host semantic equality oracle, a hidden recursor, an unrecorded axiom dependency, or an apophatic boundary promoted to internal theorem content.
\end{theorem}
\begin{proof}
Start from the displayed carrier. The only self-theory commitments are $G,E,R,P,A$, and the only row that compares them as one surface is $L$.

Routes through $C$ and transports through $H$ preserve those rows componentwise. They can replay a commitment read or move the packet, but they cannot create a new substrate principle.

The provenance and naming rows $Q,N$ cite the sibling surfaces and bind the local certificate. Citation gives a consumer context for the five rows; it does not add a parent theory, host equality, hidden recursor, axiom supply, or internal theorem extracted from a boundary row.

Thus every public read remains inside $T=(G,E,R,P,A,L,H,C,Q,N)$, and the excluded objects have no route through the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_nonescape}

\begin{theorem}[Theory self-classifier sibling independence]
\label{thm:theory-self-classifier-sibling-independence}
\independenceWitness{Nearest siblings: internaltheoryboundary, metacic-theory-identity, axiomdependencyauditmap, and bedcselftheory. No one of these is bijective to the $\TheorySelfClassifierUp$ carrier because each lacks the classifier row $L$ that compares closed generators, literal equality, structural recursors, purity, and apophatic boundary rows as one self-theory surface.}
\end{theorem}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifier\_sibling\_independence}
\begin{proof}
Compare row inventories. The internal-theory-boundary packet exposes the five commitments as a boundary, the MetaCIC theory-identity packet records identity rows, the axiom-dependency-audit-map packet records mode and supply rows, and the BEDC self-theory packet packages the commitments as a self-theory certificate.

The $\TheorySelfClassifierUp$ carrier requires the same five commitment rows together with the classifier ledger $L$ that compares them as one surface. None of the sibling carriers has that row with the displayed comparison role.

Therefore a consumer may cite those siblings as provenance, but no cited sibling is a bijective replacement for this classifier packet.
\end{proof}

\begin{theorem}[Theory self-classifier TasteGate target]
\label{thm:theory-self-classifier-tastegate-target}
The later Lean `TasteGate.lean' development for $\TheorySelfClassifierUp$ must provide a $\mathsf{Nontrivial}$ witness with two explicit carrier values and a $\mathsf{FieldFaithful}$ witness by field projection, match analysis, and injection over the displayed rows $G,E,R,P,A,L,H,C,Q,N$.
\end{theorem}
\leanchecked{BEDC.Derived.TheorySelfClassifierUp.TheorySelfClassifierTasteGate\_single\_carrier\_alignment}
\begin{proof}
The carrier has distinguishable commitment and classifier fields, so a singleton event-flow encoding would not show that the self-theory surface is genuinely classified.

Nontriviality supplies two explicit packets. Field faithfulness then checks the event-flow encoding by projecting each displayed row, matching on the carrier constructor, and using injection to recover equality of all fields from equality of encodings.

These two gate obligations are the Lean-side conditions needed before the classifier can be treated as a checked TasteGate object.
\end{proof}

\falsifiablePrediction{A TheorySelfClassifierUp packet cannot export a self-theory reading unless the five commitment rows are all present as displayed BHist coordinates.}

\closureat{TheorySelfClassifierUp}{seedStr}

\begin{closurestatus}{\TheorySelfClassifierUp}
  \constructivestory{A $\TheorySelfClassifierUp$ packet is generated from closed-generator, literal-equality, structural-recursor, purity, apophatic-boundary, classifier-ledger, transport, route, provenance, and local naming rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:theory-self-classifier-carrier}, \autoref{thm:theory-self-classifier-namecert-obligations}, \autoref{thm:theory-self-classifier-nonescape}, \autoref{thm:theory-self-classifier-sibling-independence}, and \autoref{thm:theory-self-classifier-tastegate-target} specify the seed carrier, finite NameCert surface, non-escape boundary, sibling independence witness, and gate-facing target.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not provide Lean-side verification, bridge checking, a classical-CIC embedding, a semantic equality oracle, a hidden axiom route, or a completed TasteGate instance.}
  \upgradepath{Obligation closure requires Lean encoding of the carrier, checked five-row classifier obligations, non-escape, sibling independence, and a TasteGate route with Nontrivial and FieldFaithful instances.}
\end{closurestatus}
