\chapter{A Concrete Naming Certificate for $\SetUp$}
\label{ch:concrete-instances-set-namecert}

The set interface $\SetUp$ packages a carrier together with a $\psame$-classifier interpretable as set-membership equality. It is the type-layer counterpart of $\NameCert_{\PreorderUp}$ (\autoref{ch:concrete-instances-preorder-namecert}) at the discrete level, and the substrate for $\NameCert_{\FinSetUp}$ (\autoref{ch:concrete-instances-finset-namecert}) and $\NameCert_{\SubtypeUp}$ (\autoref{ch:concrete-instances-subtype-namecert}).

\section{Carrier definition}
\label{sec:set-carrier}

\section{The certificate}
\label{sec:set-certificate}

\begin{closurestatus}{\SetUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{The chapter names the $\Hist$ set-carrier predicate, $\psame$-style membership classifier, membership transport, extensionality row, finite-substrate consumers $\FinSetUp$ and $\SubtypeUp$, exact ledger boundary, and standard-bridge row as the binding scope for the certificate obligations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This seed does not close carrier construction, classifier laws, extensionality, exactness, standard bridge data, or Lean verification for $\SetUp$.}
  \upgradepath{Obligation closure requires explicit carrier, classifier, membership-transport, extensionality, exact-ledger, and dependency theorem blocks for $\SetUp$.}
\end{closurestatus}
