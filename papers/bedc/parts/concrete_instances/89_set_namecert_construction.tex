\chapter{A Concrete Naming Certificate for $\SetUp$}
\label{ch:concrete-instances-set-namecert}

The set interface $\SetUp$ packages a carrier together with a $\psame$-classifier interpretable as set-membership equality. It is the type-layer counterpart of $\NameCert_{\PreorderUp}$ (\autoref{ch:concrete-instances-preorder-namecert}) at the discrete level, and the substrate for $\NameCert_{\FinSetUp}$ (\autoref{ch:concrete-instances-finset-namecert}) and $\NameCert_{\SubtypeUp}$ (\autoref{ch:concrete-instances-subtype-namecert}).

\section{Carrier definition}
\label{sec:set-carrier}

\section{The certificate}
\label{sec:set-certificate}

\begin{closurestatus}{\SetUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed binds the BEDC history carrier predicate, the $\psame$ classifier, the membership-equality reading, stability rows, ledger boundary, and the $\FinSetUp$ and $\SubtypeUp$ consumer interfaces as obligations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close the carrier, classifier, stability fields, exactness rows, ledger rows, standard bridge, $\FinSetUp$ bridge, $\SubtypeUp$ bridge, or Lean verification.}
  \upgradepath{Obligation closure requires explicit carrier and classifier definitions, classifier laws, carrier transport, membership-exactness rows, ledger entries, and consumer boundary rows.}
\end{closurestatus}
