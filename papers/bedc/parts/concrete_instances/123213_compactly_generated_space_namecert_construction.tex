\chapter{A Concrete Naming Certificate for $\CompactlyGeneratedSpaceUp$}
\label{ch:concrete-instances-compactly-generated-space-namecert}
\origin{human}

Following Kelley and compactly generated topology, the $\CompactlyGeneratedSpaceUp$ packet records the finite compact-test final-topology route by which a displayed topology is read through compact sources and classifier-respecting continuous maps. It sits after the compact source surface of \autoref{ch:concrete-instances-compact-namecert}, the indexed-open topology row of \autoref{ch:concrete-instances-topology-namecert}, the continuous-map interface of \autoref{ch:concrete-instances-continuousmap-namecert}, and the product-topology rectangle ledger of \autoref{ch:concrete-instances-product-topology-namecert}. The packet realizes the common-carrier projection discipline of \autoref{ch:visions-unified-reality-common-carrier-projection} as finite compact-source, topology, continuous-map, transport, and boundary-ledger NameCert data without importing host powersets, arbitrary compact families, quotient topology, choice-selected covers, or ambient spaces.

\begin{definition}[CompactlyGeneratedSpace carrier]\label{def:compactly-generated-space-carrier}
A $\CompactlyGeneratedSpaceUp$ carrier is a finite $\BHist$ packet
$$
K=(X,O,\mathcal K,F,\Phi,\Pi,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the underlying carrier row, equipped only with the classifier visible through the packet.
\item $O$ is the $\TopologyUp$ row on $X$, exposing indexed opens and finite generated-open operations.
\item $\mathcal K$ is the finite compact-source ledger, listing the $\CompactUp$ rows whose maps into $X$ are used for compact tests.
\item $F$ is the finite $\ContinuousMapUp$ ledger from compact sources in $\mathcal K$ into the topology row $O$.
\item $\Phi$ is the compact-test final-topology row: a candidate open row is accepted exactly when its pullbacks along the displayed maps in $F$ are open on the corresponding compact sources.
\item $\Pi$ is the common-carrier projection row, recording the classifier-respecting read from the carrier $X$ into each compact-test surface.
\item $B$ is the boundary ledger, refusing reverse readings from compact-test success to host powersets, arbitrary compact families, quotient topology, unlisted maps, or ambient space equality.
\item $H$ records componentwise $\hsame$ transport for the carrier, topology, compact-source ledger, continuous-map ledger, final-topology row, projection row, boundary ledger, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a compactly-generated-space request through $X,O,\mathcal K,F,\Phi,\Pi,B,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactlyGeneratedSpaceUp$, $\CompactUp$, $\TopologyUp$, $\ContinuousMapUp$, $\ProductTopologyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactlyGeneratedSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,O,\mathcal K,F,\Phi,\Pi,B,H,C,P,N$. It has no coordinate for host powersets of opens, arbitrary compact-family quantification, quotient topology, choice-selected covers, external function-space topology, or ambient equality of spaces.
\end{definition}

\begin{theorem}[CompactlyGeneratedSpace NameCert obligations]\label{thm:compactly-generated-space-namecert-obligations}
For every accepted $\CompactlyGeneratedSpaceUp$ carrier $K=(X,O,\mathcal K,F,\Phi,\Pi,B,H,C,P,N)$, the local $\NameCert_{\CompactlyGeneratedSpaceUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the underlying carrier row, topology row, compact-source ledger, continuous-map ledger, compact-test final-topology row, common-carrier projection row, boundary ledger, transport row, replay row, provenance row, and local naming row;
\item topology visibility, requiring every open read to inspect $O$ before any compact-test final-topology claim is exported;
\item compact-source accountability, requiring every compact test to be listed in $\mathcal K$ and every map used by that test to be listed in $F$;
\item compact-test exactness, requiring $\Phi$ to accept a candidate open row only through displayed pullback-open rows along the finite maps in $F$;
\item projection compatibility, requiring $\Pi$ to preserve the classifier on $X$ when a compact-source read is projected into the common-carrier surface;
\item boundary non-escape, requiring $B$ to refuse host powersets, arbitrary compact families, quotient topology, unlisted maps, choice-selected covers, external function-space topology, and ambient equality of spaces;
\item structural stability, provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No $\CompactlyGeneratedSpaceUp$ obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:compactly-generated-space-carrier}. The accepted packet exposes exactly the carrier, topology, compact-source, continuous-map, final-topology, projection, boundary, transport, replay, provenance, and naming rows.

Topology visibility and compact-source accountability determine the route. A candidate open read first inspects $O$; every compact source used to test it must then appear in $\mathcal K$, and every map used for the pullback test must appear in $F$.

The compact-test row $\Phi$ is therefore exact. It can accept a candidate open row only by replaying the displayed pullback-open rows along the maps in $F$, so it cannot quantify over arbitrary compact families or host powersets.

Projection compatibility is supplied by $\Pi$, while $B$ records the refused reverse readings. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the excluded host data have no carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[CompactlyGeneratedSpace compact-test route]\label{thm:compactly-generated-space-compact-test-route}
Every open-set or continuity-facing read exported by an accepted $\CompactlyGeneratedSpaceUp$ carrier factors through the finite route
$$
\TopologyUp \longmapsto \CompactUp \longmapsto \ContinuousMapUp \longmapsto \Phi \longmapsto \Pi,B,H,C,P,N .
$$
The route reads candidate opens by compact-source pullback tests and reads maps by classifier-respecting projections from the common carrier. It exports no host powerset of opens, arbitrary compact-family quantifier, quotient topology, selected cover, external function-space topology, or ambient equality of spaces.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:compactly-generated-space-namecert-obligations}. It places the topology row $O$ before any compact-test claim and requires all compact sources and maps to be displayed in $\mathcal K$ and $F$.

A candidate open is then tested by pullback along the finite maps in $F$. The final-topology row $\Phi$ records exactly those compact-source tests and does not add an unlisted compact family or host powerset coordinate.

Continuity-facing reads use the same displayed maps. The projection row $\Pi$ records classifier respect from the carrier into the compact-test surfaces, and the boundary row $B$ refuses reverse readings that would identify the packet with an ambient topological space.

Transport, replay, provenance, and naming through $H,C,P,N$ preserve this finite route. Thus every exported read factors through the displayed compact-test ledger.
\end{proof}

\begin{theorem}[CompactlyGeneratedSpace final topology boundary]\label{thm:compactly-generated-space-final-topology-boundary}
For every accepted $\CompactlyGeneratedSpaceUp$ carrier $K=(X,O,\mathcal K,F,\Phi,\Pi,B,H,C,P,N)$, the final-topology row $\Phi$ is a boundary row over the displayed compact-source tests. It may certify that candidate opens are stable under the listed compact pullbacks, but it does not export a host final topology, an arbitrary compact-family closure theorem, a quotient topology, choice-selected compact covers, external function-space topology, or ambient equality of spaces.
\end{theorem}
\begin{proof}
Read $\Phi$ from the carrier of \autoref{def:compactly-generated-space-carrier}. Its inputs are the topology row $O$, compact-source ledger $\mathcal K$, and continuous-map ledger $F$.

Apply \autoref{thm:compactly-generated-space-namecert-obligations}. Compact-source accountability keeps each test inside $\mathcal K,F$, and compact-test exactness keeps each accepted candidate open tied to displayed pullback-open rows.

The common-carrier projection row $\Pi$ and boundary ledger $B$ mark the projection surface and the refused reverse readings. They allow compact-test evidence to be consumed as finite NameCert data, but they do not license replacement by a host final-topology object.

The transport, replay, provenance, and local naming rows preserve the same packet. Therefore $\Phi$ is a final-topology boundary over the displayed compact tests, with no carrier coordinate for the excluded host principles.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactlyGeneratedSpaceUp$ compact-test read factors through a displayed TopologyUp row, finite CompactUp source ledger, finite ContinuousMapUp map ledger, final-topology boundary row, classifier-respecting projection row, boundary ledger, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{compact, topology, continuousmap, product-topology: $\CompactlyGeneratedSpaceUp$ is not bijective to any listed sibling because it jointly carries the topology row, compact-source ledger, continuous-map ledger, compact-test final-topology row, common-carrier projection row, boundary ledger, transport, replay, provenance, and local NameCert rows in one packet.}

\closureat{CompactlyGeneratedSpaceUp}{seedStr}

\begin{closurestatus}{\CompactlyGeneratedSpaceUp}
  \constructivestory{A $\CompactlyGeneratedSpaceUp$ packet is generated from an underlying carrier row, a TopologyUp row, a finite CompactUp source ledger, a finite ContinuousMapUp ledger, a compact-test final-topology row, a classifier-respecting common-carrier projection row, a boundary ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compactly-generated-space-carrier}, \autoref{thm:compactly-generated-space-namecert-obligations}, \autoref{thm:compactly-generated-space-compact-test-route}, and \autoref{thm:compactly-generated-space-final-topology-boundary} seed the compact-test final-topology route as finite common-carrier projection data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host powersets, arbitrary compact-family quantification, quotient topology, choice-selected covers, external function-space topology, ambient equality of spaces, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, topology visibility, compact-source accountability, compact-test exactness, projection compatibility, boundary non-escape, transport, replay, provenance, and local naming for BEDC.Derived.CompactlyGeneratedSpaceUp.}
\end{closurestatus}
