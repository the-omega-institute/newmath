\chapter{A Concrete Naming Certificate for \(\SOneUp\)}
\label{ch:concrete-instances-s1-namecert}

The circle interface $\SOneUp$ is a certificate-obligation schema over the product carrier $\RealUp \times \RealUp$ from \autoref{ch:concrete-instances-real-namecert}. Its carrier is the unit-circle subspace: pairs $(x,y)$ equipped with a certified equation witness that $x^2 + y^2$ is classified as the real unit. The interface is not a primitive topological space; it is admitted only when the product real witnesses, equation witness, componentwise classifier, and equation transport data are supplied.

\input{parts/concrete_instances/s1/carrier_readbacks}
\input{parts/concrete_instances/s1/certificate}

\begin{theorem}[SOne Real metric bridge boundary]
\label{thm:sone-real-metric-bridge-boundary}
The public $\SOneUp$ bridge boundary over the $\RealUp$ product carrier
consists only of the two $\RealUp$ component rows, the product carrier ledger,
the unit-equation witness, the componentwise classifier, and equation
transport. It does not expose angle parametrisation, winding data, covering
quotients, compactness, or connectedness.
\end{theorem}
\begin{proof}
Read the carrier through the child file
\autoref{sec:s1-carrier}. The carrier readbacks expose the two real component
histories, the product-history row, the continuation ledger, and the displayed
unit-equation witness.

Read the certificate through the child file certificate surface. The component
classifier exposes only $\hsame$ on the two real coordinates, while the hidden
equation and point ledgers are determined and transported by the equation
transport and carrier transport rows.

Repack those rows as the public bridge boundary. Every exported field is one
of the RealUp component, product carrier, unit-equation, component classifier,
or equation-transport rows already displayed in this chapter, so no angle,
winding, covering, compactness, or connectedness datum is part of the boundary.
\end{proof}

\closureat{SOneUp}{scopedStr}[BEDC.Derived.S1Up.sone\_history\_semantic\_name\_certificate]

\begin{closurestatus}{\SOneUp}
  \constructivestory{A circle point is generated by placing two $\RealUp$ constant histories side by side, using the product continuation ledger to assemble the point history $p$, and carrying a separate equation history $e$ that certifies $x^2+y^2$ against the real unit; empty points, zero-headed points, and pairs without that unit-equation witness are outside the carrier. The classifier deliberately exposes only the two component comparisons $\hsame(x,x')$ and $\hsame(y,y')$: once those are present, the hidden equation ledger and point ledger are determined and transported, so the construction never quotients by angles or by a host $\mathbb R/\mathbb Z$ presentation. The source specification is the product of two real witnesses together with the real-algebra rows needed to form the unit equation, the pattern specification is unit-circle formation from that pair and its certified equation object, and the ledger keeps the rational tails of the two real constants, the product continuation, the equation witness, and the transported component rows visible. The five name-certificate obligations reduce to componentwise history-equality: an admitted pair with its unit equation inhabits the carrier, reflexivity, symmetry, and transitivity are inherited from the two real components, and full carrier transport works because the hidden equation and point witnesses can be recovered from those component comparisons. A point history $h$ lies in the signature gap of a $\SOneUp$ token $p$ when the source-domain accepts $h$ as such a real pair with unit-equation ledger, the signature relation pairs it with an answer circle point and evidence ledger, and the token-introduction relation names that answer as $p$; unfolded, the two real components and their equation witness match the point encoded by $p$. The chapter registers this component-and-equation construction under the name $\SOneUp$; it introduces no trigonometric library, winding theory, covering-space quotient, topological-group structure, or standard bridge.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The seed scope is the RealUp product carrier, unit-circle equation witness, componentwise classifier, and equation-transport rows displayed in this chapter.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.S1Up.sone\_history\_semantic\_name\_certificate}
  \bridgestatus{none}
  \notclaimed{The chapter does not claim connectedness, compactness, angle parametrisation, or a standard topological circle bridge.}
  \upgradepath{The next theory-axis step is a public-facing $\SOneUp$ certificate or bridge theorem.}
\end{closurestatus}
