\chapter{A Concrete Naming Certificate for $\RegularCauchyWindowAssociatorUp$}
\label{ch:concrete-instances-regular-cauchy-window-associator-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy sequence arithmetic, the $\RegularCauchyWindowAssociatorUp$ packet records the finite window reassociation route used when a regular rational product or paired completion read is regrouped before reaching the terminal Real seal. It sits over the finite schedule surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the product rows of \autoref{ch:concrete-instances-cauchyproduct-namecert} and \autoref{ch:concrete-instances-regularcauchyproduct-namecert}, and the value-facing boundary of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite reassociation certificate for displayed windows; it does not assert associativity of completed real multiplication, quotient stream equality, selected representatives, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy window associator carrier]
\label{def:regular-cauchy-window-associator-carrier}
A $\RegularCauchyWindowAssociatorUp$ carrier is a finite $\BHist$ packet
$$
A=(W_L,W_M,W_R,D_L,D_M,D_R,Q_L,Q_M,Q_R,P_L,P_R,S,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W_L,W_M,W_R$ are the three finite $\StreamNameUp$ windows being reassociated.
\item $D_L,D_M,D_R$ are the corresponding $\DyadicRatCoreUp$ tolerance rows at the shared requested scale.
\item $Q_L,Q_M,Q_R$ are the $\RegSeqRatUp$ readback rows served from those windows and tolerances.
\item $P_L$ and $P_R$ are the left- and right-parenthesized product-window rows read through $\CauchyProductUp$ and $\RegularCauchyProductUp$.
\item $S$ is the terminal $\RealUp$ seal reached only after the three windows, three tolerance rows, three readbacks, and both parenthesized product rows remain visible.
\item $H$ records componentwise $\hsame$ transport for the windows, tolerances, readbacks, product rows, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a reassociation request through the same finite rows.
\item $G$ is the $\Pkg$ provenance over $\RegularCauchyWindowAssociatorUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyProductUp$, $\RegularCauchyProductUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyWindowAssociatorUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient sequence equality, selected representatives, hidden infinite reassociation, countable choice, host multiplication, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Regular Cauchy window associator NameCert obligations]
\label{thm:regular-cauchy-window-associator-namecert-obligations}
For every accepted $\RegularCauchyWindowAssociatorUp$ carrier
$$
A=(W_L,W_M,W_R,D_L,D_M,D_R,Q_L,Q_M,Q_R,P_L,P_R,S,H,C,G,N),
$$
the local $\NameCert_{\RegularCauchyWindowAssociatorUp}$ surface consists exactly of carrier admission for the displayed packet, three finite $\StreamNameUp$ windows, three $\DyadicRatCoreUp$ tolerance rows, three $\RegSeqRatUp$ readbacks, the two parenthesized product-window rows $P_L,P_R$, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, local naming, and nonescape from quotient stream equality, selected representatives, hidden infinite reassociation, countable choice, host multiplication, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-window-associator-carrier}. The tuple displays the three windows, three dyadic tolerance rows, three regular rational readbacks, two parenthesized product rows, terminal seal, transport, replay, provenance, and local naming as separate coordinates.

The product rows are downstream of the finite windows. Row $P_L$ reads the left-parenthesized window route, and row $P_R$ reads the right-parenthesized route, both through the existing CauchyProduct and RegularCauchyProduct sibling surfaces.

Finally inspect $S,H,C,G,N$. The Real seal, componentwise transport, replay, provenance, and local naming preserve the same displayed rows. Since no carrier coordinate stores quotient stream equality, selected representatives, hidden infinite reassociation, countable choice, host multiplication, trichotomy, or ambient completeness, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy window associator RegSeqRat handoff]
\label{thm:regular-cauchy-window-associator-regseqrat-handoff}
Every RegSeqRat or Real-facing consumer of an accepted $\RegularCauchyWindowAssociatorUp$ carrier must expose the three $\StreamNameUp$ windows, the three $\DyadicRatCoreUp$ tolerance rows, the three $\RegSeqRatUp$ readbacks, and both parenthesized product-window rows before it may inspect the terminal $\RealUp$ seal. The reassociation handoff is therefore a finite displayed route through StreamName, RegSeqRat, DyadicRatCore, CauchyProduct, RegularCauchyProduct, and Real rows, not an ambient completeness or quotient-stream principle.
\end{theorem}
\begin{proof}
Start with a RegSeqRat-facing reassociation request. The readback rows $Q_L,Q_M,Q_R$ are meaningful only after the corresponding windows $W_L,W_M,W_R$ and tolerance rows $D_L,D_M,D_R$ have been exposed.

Use the product rows $P_L$ and $P_R$ to compare the two parenthesized routes. Both rows are product-window data attached to the same three displayed windows and dyadic scales, so neither can introduce a hidden product representative or a host multiplication coordinate.

The terminal seal $S$ is then read only through the preserved finite route. Transport, replay, provenance, and local naming keep that route attached to the accepted packet, leaving no coordinate for quotient stream equality, selected representatives, countable choice, trichotomy, host multiplication, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyWindowAssociatorUp$ read displays three $\StreamNameUp$ windows, three $\DyadicRatCoreUp$ tolerance rows, three $\RegSeqRatUp$ readbacks, two parenthesized product-window rows, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, cauchyproduct, regularcauchyproduct, real: $\RegularCauchyWindowAssociatorUp$ is not bijective to any listed sibling because it carries both parenthesized product-window routes over one three-window regular Cauchy readback surface.}

\closureat{RegularCauchyWindowAssociatorUp}{seedStr}

\begin{closurestatus}{\RegularCauchyWindowAssociatorUp}
  \constructivestory{A $\RegularCauchyWindowAssociatorUp$ packet is generated from three StreamName windows, three DyadicRatCore tolerance rows, three RegSeqRat readbacks, two parenthesized product-window rows, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-window-associator-carrier}, \autoref{thm:regular-cauchy-window-associator-namecert-obligations}, and \autoref{thm:regular-cauchy-window-associator-regseqrat-handoff} seed the finite reassociation route over StreamName, RegSeqRat, DyadicRatCore, product, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, associativity of completed real multiplication, quotient stream equality, selected representatives, hidden infinite reassociation, countable choice, trichotomy, host multiplication, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, product-window exactness, RegSeqRat handoff determinacy, transport stability, replay stability, provenance non-escape, and local naming for BEDC.Derived.RegularCauchyWindowAssociatorUp.}
\end{closurestatus}
