\chapter{A Concrete Naming Certificate for $\MetaCICLocalConfluenceFrontierUp$}
\label{ch:concrete-instances-metacic-local-confluence-frontier-namecert}
\origin{ai}

The $\MetaCICLocalConfluenceFrontierUp$ packet realizes the conditional local-diamond boundary of \autoref{ch:metacic-confluence-audit-map} as finite critical-pair, conditional-join, substitution-premise, obstruction, transport, replay, provenance, and local naming data. It sits between the critical-path surface of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the normal-form consistency seal of \autoref{ch:concrete-instances-normal-form-consistency-seal-namecert}, the decidable-conversion boundary of \autoref{ch:concrete-instances-metacic-decidable-conversion-frontier-namecert}, and the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. The packet records the conditional local confluence surface without claiming full Church--Rosser, subject reduction, evaluator equality, quotient equality of terms, residual-substitution compatibility, or kernel consistency.

\falsifiablePrediction{Every accepted $\MetaCICLocalConfluenceFrontierUp$ read exposes the critical-pair, conditional-join, residual-substitution premise, obstruction, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows before any local-diamond handoff can be consumed.}

\independenceWitness{metacic-parallel-diamond-frontier, normal-form-consistency-seal, metaciccriticalpath, metacicdecidableboundary, subject-reduction-discharge-socket: the parallel-diamond frontier retains a stronger residual-diamond route without closing the local confluence premise; the normal-form consistency seal records normal-source consistency without the critical-pair frontier; the critical path records reduction-budget routing without the conditional local-join packet; the decidable boundary records conversion-frontier data without local diamond discharge; and the subject-reduction discharge socket names a typing-preservation boundary rather than a confluence frontier. None is bijective to $\MetaCICLocalConfluenceFrontierUp$.}

\begin{definition}[MetaCIC local confluence frontier carrier]
\label{def:metacic-local-confluence-frontier-carrier}
A $\MetaCICLocalConfluenceFrontierUp$ carrier is a finite $\BHist$ packet
$$
F=(K,J,S,O,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the critical-pair row, exposing the local overlap, source term, two one-step residual branches, and bounded redex-window data consumed by $\MetaCICCriticalPathUp$.
\item $J$ is the conditional-join row, recording the candidate common reduct and the finite local-join check that may be read only after $K$ and $S$ are displayed.
\item $S$ is the substitution-premise row, retaining the residual-substitution compatibility boundary required by the conditional local-diamond read.
\item $O$ is the obstruction row, recording non-discharge of full parallel diamond, full closed Church--Rosser, subject reduction, evaluator equality, quotient equality of terms, and kernel consistency.
\item $H$ records componentwise $\hsame$ transport for critical pairs, conditional joins, substitution premises, obstruction data, replay, provenance, and local naming.
\item $T$ records displayed $\Cont$ replay of the route
$$
K,S \longmapsto J \longmapsto O,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\MetaCICLocalConfluenceFrontierUp$, $\MetaCICParallelDiamondFrontierUp$, $\NormalFormConsistencySealUp$, $\MetaCICCriticalPathUp$, $\MetaCICDecidableConversionFrontierUp$, $\SubjectReductionDischargeSocketUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICLocalConfluenceFrontierUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,J,S,O,H,T,P,N$. It has no coordinate for an unconditional diamond theorem, a subject-reduction theorem, evaluator equality, quotient syntax equality, global normalization, or kernel consistency.
\end{definition}
\leandef{BEDC.Derived.MetaCICLocalConfluenceFrontierUp.MetaCICLocalConfluenceFrontierUp}

\begin{theorem}[MetaCIC local confluence frontier NameCert obligations]
\label{thm:metacic-local-confluence-frontier-namecert-obligations}
For every accepted $\MetaCICLocalConfluenceFrontierUp$ carrier
$$
F=(K,J,S,O,H,T,P,N),
$$
the local $\NameCert_{\MetaCICLocalConfluenceFrontierUp}$ surface consists exactly of critical-pair exposure through $K$, conditional-join exposure through $J$, residual-substitution premise retention through $S$, obstruction nonescape through $O$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $T$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The finite tuple displays the critical-pair, conditional-join, substitution-premise, obstruction, transport, replay, provenance, and local naming rows, and the classifier compares only those rows.

The confluence-facing data are $K,J,S$. A reader first exposes the critical pair $K$ and retained substitution premise $S$; only then may it inspect the candidate local join $J$. This preserves the conditional boundary named in \autoref{ch:metacic-confluence-audit-map}.

The obstruction and structural rows prevent nonescape. The row $O$ records that the packet has not supplied full parallel diamond, closed Church--Rosser, subject reduction, evaluator equality, quotient equality, global normalization, or kernel consistency, while $H,T,P,N$ transport, replay, source, and name the same finite rows.
\end{proof}

\begin{theorem}[MetaCIC local confluence frontier conditional handoff]
\label{thm:metacic-local-confluence-frontier-conditional-handoff}
Every confluence-audit consumer that reads an accepted $\MetaCICLocalConfluenceFrontierUp$ packet must expose the route
$$
\MetaCICCriticalPathUp,\ S,\ J,\ O
\longmapsto
H,T,P,N
$$
before using a local-diamond handoff. The handoff may feed the conditional surface of $\MetaCICParallelDiamondFrontierUp$ and the consistency-facing rows of $\NormalFormConsistencySealUp$, but it does not prove residual-substitution compatibility, full Church--Rosser, subject reduction, evaluator equality, quotient equality of terms, decidable conversion, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the critical-pair row $K$. It exposes only the local overlap and bounded residual branches consumed by the MetaCIC critical path, so the reader has not yet received any global confluence theorem.

Next read the retained substitution premise $S$ and conditional join row $J$. The join is public only relative to the displayed premise, which keeps the residual-substitution boundary visible at the handoff.

Finally read $O,H,T,P,N$. The obstruction row records the endpoints that remain outside the packet, and the structural rows transport, replay, source, and name the same conditional frontier. Therefore the consumer receives a local confluence frontier, not a proof of full confluence, subject reduction, evaluator equality, quotient equality, decidable conversion, or kernel consistency.
\end{proof}

\closureat{MetaCICLocalConfluenceFrontierUp}{seedStr}

\begin{closurestatus}{\MetaCICLocalConfluenceFrontierUp}
  \constructivestory{A $\MetaCICLocalConfluenceFrontierUp$ packet is generated from critical-pair rows, conditional-join rows, residual-substitution premise rows, obstruction rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-local-confluence-frontier-carrier}, \autoref{thm:metacic-local-confluence-frontier-namecert-obligations}, and \autoref{thm:metacic-local-confluence-frontier-conditional-handoff} seed the finite local confluence frontier over critical-pair, conditional-join, substitution-premise, obstruction, transport, replay, provenance, and local naming rows.}
  \formalstatus{\encodedDefV}
  \leantarget{BEDC.Derived.MetaCICLocalConfluenceFrontierUp.MetaCICLocalConfluenceFrontierUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close residual-substitution compatibility, full parallel diamond, full Church--Rosser, subject reduction, evaluator equality, quotient equality of terms, decidable conversion, global normalization, kernel consistency, or standard bridges.}
  \upgradepath{Obligation closure requires checked carrier admission, critical-pair exposure, conditional-join exposure, residual-substitution premise retention, obstruction nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICLocalConfluenceFrontierUp.}
\end{closurestatus}
