\chapter{A Concrete Naming Certificate for $\RealInverseUp$}
\label{ch:concrete-instances-realinverse-namecert}
\origin{human}

The $\RealInverseUp$ interface names the finite reciprocal packet for a real source whose nonzero domain is witnessed by apartness. It follows the Bishop--Bridges discipline: the reciprocal route is available only after an $\ApartnessRealUp$ witness supplies the constructive guard, and the resulting packet hands a product-seal route to $\RealUp$ without importing trichotomy, host division, quotient real equality, a selected limit, or completeness.

The reciprocal packet records its sibling route explicitly. It consumes the nonzero-domain witness from \autoref{ch:concrete-instances-apartnessreal-namecert}, the product verification route from \autoref{ch:concrete-instances-cauchyproduct-namecert}, the finite-window route from \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, and the seal surface from \autoref{ch:concrete-instances-real-namecert}. This is the dependency route for the displayed rows $x,a,p,w,r,s,h,c,l,n$; it is not another theorem and not a host division principle.

\begin{definition}[Real inverse carrier]
\label{def:real-inverse-carrier}
A $\RealInverseUp$ carrier is a finite $\BHist$ packet
$$
I=(x,a,p,w,r,s,h,c,l,n)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $x$ is the source $\RealUp$ row whose reciprocal is being named;
\item $a$ is an $\ApartnessRealUp$ nonzero-domain witness licensing the reciprocal route for $x$;
\item $p$ is the $\CauchyProductUp$ product route that checks the candidate inverse against the source;
\item $w$ is the $\StreamNameUp$ finite-window row used to read the guarded source and candidate reciprocal;
\item $r$ is the $\RegSeqRatUp$ regular rational window row exposed through $w$;
\item $s$ is the $\RealUp$ seal row receiving the guarded reciprocal packet;
\item $h$ stores componentwise $\hsame$ transports for the source, apartness witness, product route, windows, regular rational row, seal, provenance, and local name;
\item $c$ stores the displayed $\Cont$ routes from the apartness witness to the reciprocal window, from the window to the regular rational row, from the product route to the seal, and from the finite packet to the local name;
\item $l$ records $\Pkg$ provenance over $\RealUp$, $\ApartnessRealUp$, $\CauchyProductUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $n$ is the local $\NameCert_{\RealInverseUp}$ row.
\end{enumerate}
The carrier has no coordinate for trichotomy, host division, quotient real equality, selected real values, infinite tails, or $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseTasteGate\_single\_carrier\_alignment}
\leandef{BEDC.Derived.RealInverseUp.RealInverseCarrier}

\begin{theorem}[Real inverse NameCert obligations]
\label{thm:real-inverse-namecert-obligations}
The local $\NameCert_{\RealInverseUp}$ surface for \autoref{def:real-inverse-carrier} consists exactly of source $\RealUp$ admission, $\ApartnessRealUp$ nonzero-domain admission, $\CauchyProductUp$ product-route exposure, $\StreamNameUp$ finite-window readback, $\RegSeqRatUp$ regular rational exposure, $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ routes, $\Pkg$ provenance, and the local naming row.
\end{theorem}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseNameCertObligations}
\begin{proof}
Carrier admission is projection from the displayed packet $I$. Its rows list the real source, apartness guard, product route, finite window, regular rational readback, seal, transport, continuation, provenance, and local certificate.

The reciprocal obligation is guarded by the apartness row. The certificate first reads $a$, then uses the continuation rows to expose the finite window $w$ and the regular rational row $r$ that describe the candidate reciprocal.

The product route $p$ supplies the only multiplication-facing check before the seal $s$ is exposed. The remaining rows $h,c,l,n$ transport and name this finite route, so the certificate contains no trichotomy, host division, quotient real equality, selected limit, infinite tail, or completeness coordinate.
\end{proof}

\begin{theorem}[Real inverse apartness domain]
\label{thm:real-inverse-apartness-domain}
Every accepted $\RealInverseUp$ packet licenses reciprocal use only through its displayed $\ApartnessRealUp$ witness $a$. A consumer that cannot read $a$ receives no reciprocal window, no regular rational candidate row, and no product-seal route.
\end{theorem}
\begin{proof}
Project the carrier $I=(x,a,p,w,r,s,h,c,l,n)$. The only coordinate naming a nonzero domain is the apartness row $a$.

Read the continuation routes in $c$. They place the reciprocal window $w$ and the regular rational row $r$ downstream of the apartness witness, and the product route $p$ is exposed only along the same guarded path.

Thus the reciprocal is not a total host operation on $\RealUp$. Without the constructive apartness witness, the packet has no route to a reciprocal candidate, product check, or seal handoff.
\end{proof}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseApartnessDomain}

\begin{theorem}[Real inverse apartness denominator handoff]
\label{thm:real-inverse-apartness-denominator-handoff}
For an accepted $\RealInverseUp$ carrier $I=(x,a,p,w,r,s,h,c,l,n)$, every denominator or reciprocal-candidate read must pass through the displayed $\ApartnessRealUp$ witness $a$, then the $\StreamNameUp$ window $w$, then the $\RegSeqRatUp$ row $r$, before the $\CauchyProductUp$ route $p$ may certify the product seal. The handoff exposes no trichotomy, host division, quotient real equality, selected limit, infinite tail, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier $I=(x,a,p,w,r,s,h,c,l,n)$. The only row licensing a denominator read is the displayed apartness witness $a$.

Apply \autoref{thm:real-inverse-apartness-domain}. It forces the nonzero guard before any reciprocal window, regular rational candidate row, or product-seal route can be consumed.

The denominator read is then ordered through $w$ and $r$. The $\StreamNameUp$ window supplies the bounded observation of the source and candidate reciprocal, and the $\RegSeqRatUp$ row supplies the regular rational readback attached to that window.

Only after those rows are visible may the $\CauchyProductUp$ route $p$ certify the product seal. The structural rows $h,c,l,n$ replay and name this same guarded path, so the packet refuses trichotomy, host division, quotient real equality, selected limits, infinite tails, and ambient $\RealUp$ completeness.
\end{proof}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseApartnessDenominatorHandoff}

\begin{theorem}[Real inverse product seal handoff]
\label{thm:real-inverse-product-seal-handoff}
Every $\RealUp$ consumer of an accepted $\RealInverseUp$ packet factors through the $\CauchyProductUp$ product route $p$, the $\StreamNameUp$ window $w$, the $\RegSeqRatUp$ row $r$, the displayed transport and continuation rows, the provenance ledger, and the seal row $s$. The handoff exposes reciprocal information only as the apartness-guarded product certificate named by the packet.
\end{theorem}
\begin{proof}
Begin with the apartness-domain theorem. It places every reciprocal read downstream of the nonzero-domain witness $a$.

The finite observation then runs through $w$ and $r$. The $\StreamNameUp$ row supplies the bounded read, and the $\RegSeqRatUp$ row is the regular rational surface passed to the product check.

Finally the $\CauchyProductUp$ route $p$ supplies the product certificate consumed by the seal row $s$. The transport, continuation, provenance, and local naming rows record this route, so the real-facing handoff receives only the guarded finite packet.
\end{proof}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseProductSealHandoff}

\begin{theorem}[Real inverse guarded product uniqueness]
\label{thm:real-inverse-guarded-product-uniqueness}
Let $I=(x,a,p,w,r,s,h,c,l,n)$ and $I'=(x,a',p',w',r',s,h',c',l',n')$ be accepted $\RealInverseUp$ packets for the same $\RealUp$ source row $x$. If the apartness witnesses $a,a'$ name the same $\ApartnessRealUp$ guard, the product routes $p,p'$ expose $\hsame$-compatible $\CauchyProductUp$ certificates, and both packets hand off to the same terminal $\RealUp$ seal row $s$, then $I$ and $I'$ are classifier-equal as $\RealInverseUp$ packets.
\end{theorem}
\begin{proof}
Project the two carriers through \autoref{def:real-inverse-carrier}. The source row and seal row are shared by assumption, while the only reciprocal-domain coordinates are the apartness witnesses.

Apply the apartness-domain theorem, \autoref{thm:real-inverse-apartness-domain}. Since both witnesses name the same constructive guard, both reciprocal reads must pass through the same guarded route before any reciprocal window or product certificate is visible.

Next use the product-seal handoff of \autoref{thm:real-inverse-product-seal-handoff}. The $\hsame$-compatible product routes align the $\CauchyProductUp$ rows, the finite windows $w,w'$, the $\RegSeqRatUp$ readbacks $r,r'$, and the common seal $s$ along the displayed continuation rows.

It remains only to compare the bookkeeping rows. Componentwise transport in $h,h'$ carries the aligned analytic rows, $c,c'$ replay the same guarded route, $l,l'$ record the same provenance surface, and $n,n'$ name the local certificate. The non-escape theorem then rules out any hidden trichotomy, host division, quotient real equality, selected limit, infinite tail, or completeness coordinate, so classifier equality is componentwise.
\end{proof}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseGuardedProductUniqueness}

\begin{theorem}[Real inverse non-escape]
\label{thm:real-inverse-non-escape}
The carrier, NameCert obligations, apartness domain, and product seal handoff of \autoref{def:real-inverse-carrier}, \autoref{thm:real-inverse-namecert-obligations}, \autoref{thm:real-inverse-apartness-domain}, and \autoref{thm:real-inverse-product-seal-handoff} assemble one constructive reciprocal certificate over $\RealUp$, $\ApartnessRealUp$, $\CauchyProductUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The certificate exports exactly the rows
$$
x,a,p,w,r,s,h,c,l,n.
$$
It exports no trichotomy, host division, quotient real equality, selected limit, infinite tail, or completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseNonEscape}
\begin{proof}
The carrier definition lists every row in the reciprocal packet. The NameCert obligations bind source admission, apartness admission, product-route exposure, finite-window readback, regular rational exposure, seal handoff, transport, continuation, provenance, and naming to those rows.

The apartness-domain theorem makes the reciprocal route partial in the constructive sense: it is available only after the nonzero-domain witness has been read. The product seal handoff then routes every real-facing consumer through the finite window, regular rational row, product certificate, and seal.

No other coordinate remains from which to read trichotomy, host division, quotient real equality, a selected limit, an infinite tail, or completeness. The displayed rows therefore exhaust the $\RealInverseUp$ observation surface.
\end{proof}

\begin{theorem}[Real inverse obligation closure package]
\label{thm:real-inverse-obligation-closure-package}
The carrier of \autoref{def:real-inverse-carrier}, the NameCert obligations of \autoref{thm:real-inverse-namecert-obligations}, the apartness-domain theorem \autoref{thm:real-inverse-apartness-domain}, the product-seal handoff \autoref{thm:real-inverse-product-seal-handoff}, guarded product uniqueness \autoref{thm:real-inverse-guarded-product-uniqueness}, and non-escape \autoref{thm:real-inverse-non-escape} assemble the obligation-closure surface for $\RealInverseUp$ over $\RealUp$, $\ApartnessRealUp$, $\CauchyProductUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The package remains apartness-guarded and refuses trichotomy, total reciprocal on all real rows, host division, quotient equality, selected limits, infinite tails, and $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the carrier and NameCert obligations. They display the source, apartness guard, product route, StreamName window, RegSeqRat readback, Real seal, transport, continuation, provenance, and local naming rows as the full reciprocal packet.

Apply the apartness-domain and product-seal handoff theorems. The reciprocal route is available only after the constructive nonzero witness is read, and the Real-facing seal is reached only through the finite product certificate, window, and regular rational rows.

Next use guarded product uniqueness to make the package stable under matching apartness witnesses, compatible product certificates, and the same terminal Real seal. The non-escape theorem then removes trichotomy, total reciprocal, host division, quotient equality, selected limits, infinite tails, and Real completeness from the available surface, so the named rows form the obligation-closure package.
\end{proof}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseObligationClosurePackage}

\begin{theorem}[Real inverse apartness-product scoped factorization]
\label{thm:real-inverse-apartness-product-scope}
Every $\RealInverseUp$ consumer factors through the displayed $\ApartnessRealUp$ nonzero guard, $\CauchyProductUp$ product route, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ regular rational row, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\RealInverseUp}$ row before any reciprocal read is public.

The factorization is guarded by the apartness row and product route. It does not produce a total reciprocal on all real rows, trichotomy, host division, quotient real equality, selected limits, infinite tails, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-inverse-carrier} and the NameCert obligations of \autoref{thm:real-inverse-namecert-obligations}. They make the rows $x,a,p,w,r,s,h,c,l,n$ exhaustive for an accepted reciprocal packet.

Apply the apartness-domain theorem \autoref{thm:real-inverse-apartness-domain}. It places every reciprocal read downstream of the displayed $\ApartnessRealUp$ nonzero guard, so no consumer can reach a reciprocal window without first reading $a$.

Next apply the product-seal handoff of \autoref{thm:real-inverse-product-seal-handoff}. The handoff orders the consumer through the $\CauchyProductUp$ route, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal, with transport and continuation preserving the same route.

Guarded product uniqueness in \autoref{thm:real-inverse-guarded-product-uniqueness} identifies compatible guarded product routes, and non-escape in \autoref{thm:real-inverse-non-escape} removes trichotomy, total reciprocal, host division, quotient equality, selected limits, infinite tails, and completeness from the available surface. The obligation-closure package of \autoref{thm:real-inverse-obligation-closure-package} then repacks exactly this route with $\Pkg$ provenance and the local $\NameCert$ row.
\end{proof}
\leanchecked{BEDC.Derived.RealInverseUp.RealInverseApartnessProductScopedFactorization}

\begin{theorem}[Real inverse public inverse domain]
\label{thm:realinverse-public-inverse-domain}
The public $\RealInverseUp$ interface is a partial reciprocal domain. A consumer may read a reciprocal only after the displayed $\ApartnessRealUp$ guard, $\CauchyProductUp$ product route, $\StreamNameUp$ finite window, $\RegSeqRatUp$ row, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\RealInverseUp}$ rows are visible.

The public route is therefore
$$
\ApartnessRealUp \longmapsto \CauchyProductUp \longmapsto
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
\longmapsto \NameCert_{\RealInverseUp},
$$
with the structural rows carried by $h,c,l,n$. It does not claim trichotomy, total inverse on all real rows, host division, quotient real equality, selected limits, infinite tails, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the apartness-domain theorem, \autoref{thm:real-inverse-apartness-domain}. It places every reciprocal read downstream of the constructive nonzero guard, so the public interface cannot be total on all $\RealUp$ rows.

Apply the denominator handoff and product-seal handoff, \autoref{thm:real-inverse-apartness-denominator-handoff} and \autoref{thm:real-inverse-product-seal-handoff}. Together they order the read through the $\StreamNameUp$ window, $\RegSeqRatUp$ rational row, $\CauchyProductUp$ certificate, and terminal $\RealUp$ seal.

Guarded product uniqueness, \autoref{thm:real-inverse-guarded-product-uniqueness}, identifies compatible guarded product routes only after the same apartness guard, product certificate, and seal have been displayed. Non-escape, \autoref{thm:real-inverse-non-escape}, removes every coordinate for trichotomy, host division, quotient equality, selected limits, infinite tails, and completeness.

Finally use the scoped factorization theorem \autoref{thm:real-inverse-apartness-product-scope}. It repacks the public route with componentwise transport, continuation replay, provenance, and local naming, so the exported interface is exactly the apartness-guarded partial reciprocal domain.
\end{proof}

\closureat{RealInverseUp}{seedStr}

\begin{closurestatus}{\RealInverseUp}
  \constructivestory{A $\RealInverseUp$ packet is generated from a $\RealUp$ source, an $\ApartnessRealUp$ nonzero-domain witness, a $\CauchyProductUp$ product route, $\StreamNameUp$ and $\RegSeqRatUp$ finite readback rows, a $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:real-inverse-carrier}, \autoref{thm:real-inverse-namecert-obligations}, \autoref{thm:real-inverse-apartness-domain}, \autoref{thm:real-inverse-product-seal-handoff}, \autoref{thm:real-inverse-guarded-product-uniqueness}, \autoref{thm:real-inverse-non-escape}, \autoref{thm:real-inverse-obligation-closure-package}, and the scoped factorization witness \autoref{thm:real-inverse-apartness-product-scope} bind the apartness-guarded reciprocal carrier, product route, finite windows, regular rational row, seal handoff, uniqueness, refusal ledger, obligation package, and public consumer route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close trichotomy, total reciprocal on all real rows, host division, quotient real equality, selected limits, infinite tails, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires a public transport and consumer-factorization theorem for the apartness guard, product route, finite windows, regular rational row, Real seal, and local NameCert primitives.}
\end{closurestatus}
