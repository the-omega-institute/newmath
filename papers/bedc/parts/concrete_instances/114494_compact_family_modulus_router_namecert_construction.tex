\chapter{A Concrete Naming Certificate for $\CompactFamilyModulusRouterUp$}
\label{ch:concrete-instances-compact-family-modulus-router-namecert}
\origin{human}

Following Bishop--Bridges and the finite-net proof of uniform continuity on compact metric spaces, the $\CompactFamilyModulusRouterUp$ packet realizes the route of \autoref{ch:visions-compact-uniform-continuity} as finite NameCert data for families of maps. It sits over the compact source packet of \autoref{ch:concrete-instances-compactmetric-namecert}, the map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the tolerance-only handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the family stability surface of \autoref{ch:concrete-instances-equicontinuity-namecert}, and the rational fold carrier of \autoref{ch:concrete-instances-rat-namecert}. The packet records a finite router from compact nets and local family moduli to a public uniform-modulus row; it does not import open-cover compactness, sequential compactness, host function equality, or a choice-selected center family.

\begin{definition}[Compact family modulus router carrier]
\label{def:compact-family-modulus-router-carrier}
A $\CompactFamilyModulusRouterUp$ carrier is a finite $\BHist$ packet
$$
R=(K,F,E,L,Q,D,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, including displayed centers, cover radii, and located source membership witnesses.
\item $F$ is the $\ContinuousMapUp$ family row, listing the inspected map names and their pointwise modulus rows over the centers named by $K$.
\item $E$ is the $\EquicontinuityUp$ compatibility row, requiring the family pointwise moduli to share the same tolerance request and source metric comparison.
\item $L$ is the local finite-net lookup row sending a tolerance history to the displayed center-radius rows relevant to the family request.
\item $Q$ is the finite $\RatUp$ lower-bound fold over the displayed centerwise radii.
\item $D$ is the dyadic tolerance ledger used for halving, positivity, lower-bound transport, and metric-triangle budget checks.
\item $T$ is the public $\UniformModulusUp$ handoff row, depending only on the tolerance history and the displayed router packet.
\item $H$ records componentwise $\hsame$ transport for compact nets, family map rows, equicontinuity rows, local lookups, rational folds, dyadic budgets, uniform-modulus handoffs, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a tolerance request through $K,F,E,L,Q,D,H,P,N$ to the handoff row $T$.
\item $P$ is the $\Pkg$ provenance over $\CompactFamilyModulusRouterUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\EquicontinuityUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactFamilyModulusRouterUp}$ row.
\end{enumerate}
The carrier has no coordinate for arbitrary open covers, selected centers, external function equality, countable choice over a map family, or an ambient completeness principle.
\end{definition}

\begin{theorem}[Compact family modulus router NameCert obligations]
\label{thm:compact-family-modulus-router-namecert-obligations}
For every accepted $\CompactFamilyModulusRouterUp$ carrier $R=(K,F,E,L,Q,D,T,H,C,P,N)$, the local $\NameCert_{\CompactFamilyModulusRouterUp}$ surface consists exactly of compact finite-net exposure, family ContinuousMap pointwise-modulus exposure, Equicontinuity compatibility, finite lookup exactness, RatUp lower-bound fold exactness, dyadic tolerance transport, UniformModulus handoff, componentwise transport, displayed continuation replay, package provenance, and local naming. Every theorem-facing read must display
$$
K,F,E,L,Q,D,H,C,P,N
$$
before it may consume $T$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-family-modulus-router-carrier}. Its accepted rows are exactly the compact finite-net, family pointwise-modulus, equicontinuity, lookup, rational fold, dyadic budget, uniform handoff, and support rows.

The source of the router is $K,F,E,L$. These rows display the compact centers, the inspected family map names, the pointwise radii attached to those centers, and the compatibility data that lets all maps in the displayed family consume the same tolerance request.

The numerical route is $Q,D$. The finite $\RatUp$ fold records a lower-bound history for the displayed radii, while the dyadic ledger transports positivity, halving, and metric-triangle budget checks.

Finally $T,H,C,P,N$ export the tolerance-only handoff and preserve the packet under transport and replay. No open-cover, choice-selected, host-function, or completeness coordinate exists outside those rows.
\end{proof}

\begin{theorem}[Compact family finite-net routing]
\label{thm:compact-family-modulus-router-finite-net-route}
Every accepted $\CompactFamilyModulusRouterUp$ packet turns the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} into a finite router
$$
\CompactMetricUp
\longmapsto
\ContinuousMapUp
\longmapsto
\EquicontinuityUp
\longmapsto
\RatUp
\longmapsto
\UniformModulusUp .
$$
The output $\UniformModulusUp$ row is a tolerance-only read from the displayed compact finite-net, family pointwise-modulus rows, equicontinuity compatibility, rational lower-bound fold, and dyadic tolerance ledger. It exports no open-cover compactness, selected center family, host equality of maps, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with an accepted router $R=(K,F,E,L,Q,D,T,H,C,P,N)$. The compact row $K$ supplies displayed centers and finite coverage data, and the family row $F$ attaches pointwise modulus rows to those centers.

Use $E$ to synchronize those pointwise rows across the inspected family. The lookup row $L$ then selects only displayed center-radius data for the current tolerance request.

Fold the displayed radii through $Q$ and transport the tolerance budget through $D$. This produces a lower-bound history depending on the tolerance and the finite router packet, not on an external center selector.

The handoff row $T$ is therefore a public $\UniformModulusUp$ row. The support rows preserve the same finite route, so the excluded compactness, choice, host-equality, and completeness objects are not exported.
\end{proof}

\begin{theorem}[Compact family tolerance nonescape]
\label{thm:compact-family-modulus-router-tolerance-nonescape}
Every tolerance read exported by an accepted $\CompactFamilyModulusRouterUp$ packet reaches $\UniformModulusUp$ only through the displayed compact finite-net, ContinuousMap family, Equicontinuity compatibility, RatUp fold, dyadic budget, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. A downstream consumer cannot replace the router by a host choice of centers, a pointwise family selector, quotient equality of maps, sequential compactness, or ambient Real completeness.
\end{theorem}
\begin{proof}
Read the requested tolerance through the replay row $C$. The replay exposes $K,F,E,L,Q,D$ before the handoff row $T$ is visible.

The compact and family rows display only finite center and pointwise-modulus data. The equicontinuity row synchronizes that data for the inspected family, while the rational and dyadic rows compute the lower-bound budget inside the packet.

Transport and provenance through $H,P,N$ keep the same finite route attached to the local name. Since the carrier has no coordinate for host choices, quotient equality of maps, sequential compactness, or ambient completeness, a UniformModulus consumer cannot escape the displayed router.
\end{proof}

\falsifiablePrediction{A $\CompactFamilyModulusRouterUp$ consumer that reads a $\UniformModulusUp$ row without first displaying $K,F,E,L,Q,D,H,C,P,N$ refutes the router surface.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, equicontinuity, rat: $\CompactFamilyModulusRouterUp$ is not bijective to any listed sibling because it jointly carries compact finite-net rows, family pointwise-modulus rows, equicontinuity compatibility, finite lookup, RatUp fold, dyadic tolerance budget, UniformModulus handoff, transport, replay, provenance, and local naming while refusing host choice of centers and map equality.}

\closureat{CompactFamilyModulusRouterUp}{seedStr}

\begin{closurestatus}{\CompactFamilyModulusRouterUp}
  \constructivestory{A $\CompactFamilyModulusRouterUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap family pointwise-modulus rows, Equicontinuity compatibility, finite lookup rows, RatUp lower-bound folds, dyadic tolerance budgets, UniformModulus handoff rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-family-modulus-router-carrier}, \autoref{thm:compact-family-modulus-router-namecert-obligations}, \autoref{thm:compact-family-modulus-router-finite-net-route}, and \autoref{thm:compact-family-modulus-router-tolerance-nonescape} seed the finite family-router boundary for compact-uniform-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, sequential compactness, host function equality, selected center families, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, family pointwise-modulus coverage, equicontinuity compatibility, lookup exactness, RatUp fold exactness, dyadic budget transport, UniformModulus handoff, and non-escape for BEDC.Derived.CompactFamilyModulusRouterUp.}
\end{closurestatus}
