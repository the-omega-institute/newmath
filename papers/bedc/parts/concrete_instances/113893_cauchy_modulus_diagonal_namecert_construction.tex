\chapter{A Concrete Naming Certificate for $\CauchyModulusDiagonalUp$}
\label{ch:concrete-instances-cauchy-modulus-diagonal-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy reals and diagonal arguments for countable families, the $\CauchyModulusDiagonalUp$ packet records the finite schedule that turns a displayed family of Cauchy-modulus requests into one diagonal tail witness. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal rows of \autoref{ch:concrete-instances-real-namecert}, and the modulus algebra surface of \autoref{ch:concrete-instances-regular-cauchy-modulus-algebra-namecert}. The packet names a local diagonal route for completion-facing consumers; it does not select a global real representative, identify quotient real names, or assert a completed real-line choice principle.

\begin{definition}[CauchyModulusDiagonal carrier]
\label{def:cauchy-modulus-diagonal-carrier}
A $\CauchyModulusDiagonalUp$ carrier is a finite $\BHist$ packet
$$
D=(F,S,Q,E,M,T,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is a finite family row of $\RegSeqRatUp$ names whose members expose regular rational readback only through named windows;
\item $S$ is the $\StreamNameUp$ schedule assigning the finite input windows inspected for each family member and each requested dyadic tolerance;
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic scales consumed by the diagonal comparison;
\item $E$ is the $\RealUp$ seal row reached only after the displayed regular readback and dyadic tolerance rows have been replayed;
\item $M$ is the family of accepted $\CauchyModulusUp$ rows controlling the ordinary tail windows for the displayed regular rational names;
\item $T$ is the diagonal threshold row. Given a finite tolerance request and a finite family window, it records one displayed tail index large enough for every modulus row visible in that family window;
\item $W$ is the diagonal tail-witness row, storing the finite comparison traces showing that all scheduled members remain within the requested dyadic tolerance after the threshold named by $T$;
\item $H$ records componentwise $\hsame$ transport for $F,S,Q,E,M,T,W$ and for the replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from a requested diagonal tail comparison through $S$, then through $F,Q,M,T,W$, and only then into the Real-facing seal row $E$;
\item $P$ is the $\Pkg$ provenance over $\CauchyModulusDiagonalUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and local naming rows;
\item $N$ is the local $\NameCert_{\CauchyModulusDiagonalUp}$ row.
\end{enumerate}
The carrier has no coordinate for a chosen infinite tail, equality of completed Real names, countable choice over all precision requests, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[CauchyModulusDiagonal tail witness]
\label{thm:cauchy-modulus-diagonal-tail-witness}
Let
$$
D=(F,S,Q,E,M,T,W,H,C,P,N)
$$
be an accepted $\CauchyModulusDiagonalUp$ carrier. Every diagonal tail read exported by $D$ factors through the finite route
$$
S,F,Q,M \longmapsto T \longmapsto W \longmapsto E .
$$
Thus the witness for a requested dyadic tolerance is a displayed threshold over the scheduled finite family window, read from the existing Cauchy-modulus rows and dyadic tolerance ledger before any $\RealUp$ seal can be consumed.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:cauchy-modulus-diagonal-carrier}. The stream schedule $S$ fixes the finite family window and the inspected regular rational windows in $F$ before the dyadic tolerance row $Q$ or any modulus row in $M$ is read.

The threshold row $T$ is downstream of those displayed rows. It records a single finite tail index that dominates the visible modulus thresholds for the requested dyadic tolerance, so the row is a finite maximum over displayed certificate entries rather than a choice over completed sequences.

The witness row $W$ then replays the tail comparisons after that threshold and hands the result to the seal row $E$. Transport by $H$, replay by $C$, provenance by $P$, and local naming by $N$ preserve the same route, so no consumer can bypass the scheduled family, dyadic tolerance, or Cauchy-modulus coordinates.
\end{proof}

\begin{theorem}[CauchyModulusDiagonal NameCert obligations]
\label{thm:cauchy-modulus-diagonal-namecert-obligations}
For every accepted $\CauchyModulusDiagonalUp$ carrier
$$
D=(F,S,Q,E,M,T,W,H,C,P,N),
$$
the local $\NameCert_{\CauchyModulusDiagonalUp}$ surface consists exactly of carrier admission for $D$, regular rational family exposure through $F$, scheduled window exposure through $S$, dyadic tolerance exposure through $Q$, Real seal exposure through $E$, Cauchy-modulus exposure through $M$, diagonal threshold exposure through $T$, tail-witness exposure through $W$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

Consequently a $\RegSeqRatUp$ or $\RealUp$ completion-facing consumer may use the diagonal only after reading the displayed finite family schedule, dyadic tolerance ledger, modulus rows, diagonal threshold, and tail witness. The certificate does not claim completed-stream equality, quotient real equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Carrier admission lists exactly the rows $F,S,Q,E,M,T,W,H,C,P,N$. The analytic rows expose the regular rational family, finite stream schedule, dyadic tolerance request, Cauchy-modulus thresholds, diagonal threshold, tail-witness comparisons, and terminal Real-facing seal.

The structural rows do not add analytic content. Componentwise $\hsame$ transport keeps the same carrier coordinates, displayed $\Cont$ replay preserves the order of reads, $\Pkg$ provenance records the dependency ledger, and the local $\NameCert$ row names precisely this finite surface.

Apply \autoref{thm:cauchy-modulus-diagonal-tail-witness}. Any completion-facing read must pass through the finite diagonal route before it reaches a $\RegSeqRatUp$ or $\RealUp$ consumer. Since the carrier has no coordinate for completed-stream equality, quotient real equality, choice over all requests, or ambient completeness, none of those claims belongs to the local obligation surface.
\end{proof}

\falsifiablePrediction{A consumer that claims $\CauchyModulusDiagonalUp$ tail control while omitting the regular rational family row, StreamName schedule, dyadic tolerance ledger, CauchyModulus rows, diagonal threshold, tail witness, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, or $\NameCert$ row refutes the carrier of \autoref{def:cauchy-modulus-diagonal-carrier}.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, regular-cauchy-modulus-algebra: $\CauchyModulusDiagonalUp$ binds a finite family schedule, dyadic tolerance ledger, existing CauchyModulus rows, one diagonal threshold, and a tail-witness route in one local NameCert packet; each named sibling lacks at least one of these diagonal-tail coordinates as a local carrier field.}

\closureat{CauchyModulusDiagonalUp}{seedStr}

\begin{closurestatus}{\CauchyModulusDiagonalUp}
  \constructivestory{A $\CauchyModulusDiagonalUp$ packet is generated from a regular rational family, StreamName schedule, dyadic tolerance ledger, Real seal row, CauchyModulus rows, diagonal threshold, tail witness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-modulus-diagonal-carrier}, \autoref{thm:cauchy-modulus-diagonal-tail-witness}, and \autoref{thm:cauchy-modulus-diagonal-namecert-obligations} seed the finite diagonal modulus packet for scheduled regular Cauchy families.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed-stream equality, quotient real equality, selected global real representatives, countable choice, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, diagonal threshold replay, tail-witness replay, Real seal handoff, and a Lean realization for BEDC.Derived.CauchyModulusDiagonalUp.}
\end{closurestatus}
