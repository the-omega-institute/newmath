\chapter{A Concrete Naming Certificate for $\DirichletTestUp$}
\label{ch:concrete-instances-dirichlet-test-namecert}
\origin{human}

Following Dirichlet and Bishop--Bridges constructive analysis, the $\DirichletTestUp$ packet records the bounded-partial-sum convergence test as finite BEDC data before a completion-facing consumer may read a regular rational handoff or a real seal. Its source series face sits beside $\SeriesUp$ in \autoref{ch:concrete-instances-series-namecert}; its summation-by-parts sibling is $\AbelTestUp$ in \autoref{ch:concrete-instances-abel-test-namecert}; its Dirichlet-series source discipline is \autoref{ch:concrete-instances-dirichlet-series-namecert}; its uniform tail boundary is \autoref{ch:concrete-instances-uniformlimit-namecert}; and its bounded-row discipline is \autoref{ch:concrete-instances-boundedrealsequence-namecert}. The finite observation route also uses $\StreamNameUp$ windows from \autoref{ch:concrete-instances-streamname-namecert}, $\RegSeqRatUp$ readback from \autoref{ch:concrete-instances-regseqrat-namecert}, dyadic estimates from \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the $\RealUp$ seal boundary of \autoref{ch:concrete-instances-real-namecert}. The packet is a constructive Dirichlet-test surface for a displayed scalar coefficient route over a bounded partial-sum source; it is not a selected series sum, a quotient series equality, host real completeness, countable choice, or compactness by open covers.

\begin{definition}[DirichletTest carrier]
\label{def:dirichlet-test-carrier}
A $\DirichletTestUp$ carrier is a finite $\BHist$ packet
$$
D=(S,A,B,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the bounded partial-sum row, displaying finite partial sums of the source series and the bound used in the Dirichlet tail estimate.
\item $A$ is the coefficient stream row, displaying the inspected scalar coefficient values on the finite windows read by the packet.
\item $B$ is the null-tail ledger, recording the displayed thresholds at which the coefficient tail lies below a requested dyadic tolerance and the finite variation rows needed by the estimate.
\item $T$ is the Dirichlet tail estimate schedule, assigning each precision request to a finite source window and a coefficient threshold used to bound the displayed tail.
\item $R$ is the $\RegSeqRatUp$ handoff row obtained from $S,A,B,T$.
\item $E$ is the $\RealUp$ seal row attached to the regular rational handoff.
\item $H$ records componentwise $\hsame$ transport for the partial-sum, coefficient, null-tail, tail-estimate, regular-handoff, real-seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a precision request through $T$ to the $\StreamNameUp$ windows, through $S,A,B$ to the Dirichlet estimate, then through $R$ to the $\RealUp$ seal row $E$.
\item $P$ is the $\Pkg$ provenance over $\DirichletTestUp$, $\SeriesUp$, $\AbelTestUp$, $\DirichletSeriesUp$, $\UniformLimitUp$, $\BoundedRealSequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DirichletTestUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these ten displayed rows. It has no coordinate for a chosen infinite sum, quotient series equality, choice-selected limit, open-cover compactness, host series summation, or ambient completeness.
\end{definition}

\begin{theorem}[DirichletTest NameCert obligations]
\label{thm:dirichlet-test-namecert-obligations}
For every accepted $\DirichletTestUp$ carrier $D=(S,A,B,T,R,E,H,C,P,N)$, the local $\NameCert_{\DirichletTestUp}$ surface consists exactly of the following obligations:
\begin{enumerate}
\item carrier admission for the displayed ten-row packet;
\item bounded-partial-sum transport for $S$, preserving the displayed bound and inspected partial sums along the componentwise $\hsame$ route;
\item coefficient-stream exposure for $A$, preserving the inspected scalar coefficient rows on the admitted $\StreamNameUp$ windows;
\item null-tail and finite-variation exposure for $B$, preserving the displayed thresholds and variation rows used by the Dirichlet estimate;
\item tail-estimate schedule exactness for $T$, stating that each requested tolerance is answered by the finite source window and coefficient threshold recorded in the packet;
\item $\RegSeqRatUp$ handoff and $\RealUp$ seal exposure for $R$ and $E$;
\item transport stability, continuation replay, provenance, and local naming for exactly the route recorded by $H,C,P,N$.
\end{enumerate}
These obligations are finite-row obligations over the sibling surfaces of \autoref{ch:concrete-instances-series-namecert}, \autoref{ch:concrete-instances-abel-test-namecert}, \autoref{ch:concrete-instances-dirichlet-series-namecert}, \autoref{ch:concrete-instances-uniformlimit-namecert}, \autoref{ch:concrete-instances-boundedrealsequence-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Project the accepted packet $D=(S,A,B,T,R,E,H,C,P,N)$. The projection exposes exactly the bounded partial-sum row, coefficient stream row, null-tail ledger, tail-estimate schedule, regular handoff, real seal, transport rows, continuation replay, provenance, and local naming row.

Read a displayed precision request through $T$. The schedule supplies a finite source window and a coefficient threshold, while $S$ supplies the bounded partial sums and $A,B$ supply the inspected coefficient values together with the null-tail and finite-variation data visible on that window.

The estimate is therefore a finite Dirichlet tail calculation on the displayed rows. It is not an appeal to an external infinite series sum, a hidden quotient stream equality, or a choice-selected limiting process.

Finally apply the structural rows. The componentwise transports in $H$ preserve the analytic rows together, while $C,P,N$ replay, source, and name only that displayed route. Since the carrier has no further coordinate, the seven listed obligations exhaust the local $\NameCert_{\DirichletTestUp}$ surface.
\end{proof}

\begin{theorem}[DirichletTest Cauchy handoff]
\label{thm:dirichlet-test-cauchy-handoff}
Every accepted $\DirichletTestUp$ carrier sends the displayed partial-sum family to the Cauchy-facing route only through the finite handoff
$$
S,A,B,T \longmapsto R \longmapsto E,H,C,P,N.
$$
Thus a $\RegSeqRatUp$ consumer can use the Dirichlet test only after reading the bounded partial-sum row, coefficient stream row, null-tail ledger, and tail-estimate schedule that bind the finite tail estimate to the regular rational handoff.
\end{theorem}
\begin{proof}
Begin with an accepted carrier and a displayed precision request. The schedule $T$ supplies a finite source window and a coefficient threshold, while the source row $S$ supplies only the partial sums and bound visible on that window.

Read the coefficient rows. The stream row $A$ displays the inspected coefficient values, and the ledger $B$ supplies the null-tail threshold and finite variation rows needed by the Dirichlet estimate.

These rows are exactly the data consumed by the Cauchy-facing route: sufficiently late partial-sum tails are bounded by the displayed bounded-partial-sum constant and the coefficient tail data named by $B$ and $T$. The handoff row $R$ records that regular rational readback and does not receive any separate completed real or quotient series object.

Transport and replay preserve this handoff componentwise. The rows $H,C,P,N$ attach the route to the local certificate without creating an alternate path through host convergence, selected sums, quotient equality, or $\RealUp$ completeness.
\end{proof}

\begin{theorem}[DirichletTest Real-seal boundary]
\label{thm:dirichlet-test-real-seal-boundary}
For every accepted $\DirichletTestUp$ carrier, any $\RealUp$ seal-facing read factors through the $\RegSeqRatUp$ handoff row $R$ produced from the displayed bounded partial sums, coefficient stream, null-tail ledger, and tail-estimate schedule. The seal row $E$ receives only $R$ together with the componentwise transports, continuation replay, provenance, and local naming rows; it receives no selected sum, quotient series equality, host real completeness, countable choice, or open-cover compactness.
\end{theorem}
\begin{proof}
Project the seal-facing rows $R$ and $E$ from the accepted carrier. The regular handoff $R$ is downstream of $S,A,B,T$, and the seal row $E$ is downstream of $R$.

Apply \autoref{thm:dirichlet-test-cauchy-handoff}. It identifies the only route from bounded partial sums and coefficient-tail data to the regular readback as the finite path through the displayed source, coefficient, null-tail, and schedule rows.

Read the seal row. Its analytic input is exactly the regular handoff $R$, together with $H,C,P,N$. Since the carrier has no coordinate for a selected sum, quotient series equality, host real completeness, countable choice, or open-cover compactness, the seal-facing read cannot export any of those objects.
\end{proof}

\falsifiablePrediction{An accepted $\DirichletTestUp$ consumer that reaches a $\RegSeqRatUp$ handoff or $\RealUp$ seal without reading the bounded partial-sum row, coefficient stream row, null-tail ledger, and Dirichlet tail estimate schedule refutes the carrier surface.}

\independenceWitness{series, abel-test, dirichlet-series, uniformlimit, boundedrealsequence, streamname, regseqrat, dyadicratcore, real: no listed sibling supplies the whole ten-row packet $D=(S,A,B,T,R,E,H,C,P,N)$, because each sibling contributes only a source, summation-by-parts route, Dirichlet-series face, uniform tail, bounded-row, window, regular-readback, dyadic-estimate, or seal face while this chapter binds the coefficient null-tail route with transport, replay, provenance, and local naming.}

\closureat{DirichletTestUp}{seedStr}

\begin{closurestatus}{\DirichletTestUp}
  \constructivestory{A $\DirichletTestUp$ packet is generated from bounded partial-sum rows, a displayed coefficient stream, a null-tail ledger, a finite Dirichlet tail estimate schedule, a $\RegSeqRatUp$ handoff, a $\RealUp$ seal row, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dirichlet-test-carrier}, \autoref{thm:dirichlet-test-namecert-obligations}, \autoref{thm:dirichlet-test-cauchy-handoff}, and \autoref{thm:dirichlet-test-real-seal-boundary} seed the finite Dirichlet-test boundary over $\SeriesUp$, $\AbelTestUp$, $\DirichletSeriesUp$, $\UniformLimitUp$, $\BoundedRealSequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected sums, quotient series equality, host real completeness, countable choice, open-cover compactness, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the ten-row carrier and prove the finite NameCert obligation surface, Cauchy handoff theorem, and Real-seal boundary theorem.}
\end{closurestatus}
