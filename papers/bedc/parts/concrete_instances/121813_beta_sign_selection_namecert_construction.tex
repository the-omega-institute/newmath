\chapter{A Concrete Naming Certificate for $\BetaSignSelectionUp$}
\label{ch:concrete-instances-beta-sign-selection-namecert}
\origin{ai}

The $\BetaSignSelectionUp$ packet realizes the beta-sign selection principle of \autoref{prin:zeckendorf-beta-sign-selection} as finite NameCert data. It reads the centered Green-excess branch through \autoref{ch:concrete-instances-static-green-boundary-flux-namecert}, the phase-shell route through \autoref{ch:concrete-instances-zeckendorf-phase-shell-namecert}, and the window-six seed through \autoref{ch:concrete-instances-golden-mean-shift-window-six-namecert}. The packet records the sign of the denominator deformation before any physical running law is consumed; it is not a fitted infrared value, a continuous beta function, or a host analytic flow.

\falsifiablePrediction{Every accepted $\BetaSignSelectionUp$ read exposes the denominator-deformation sign row before treating a Zeckendorf window-six branch as physically running. A branch whose displayed sign raises the inverse coupling is rejected as the primary running direction.}

\independenceWitness{Nearest siblings: ZeckendorfNormal, ZCarry, ZNormal, StaticGreenBoundaryFlux, ZeckendorfPhaseShell, and GoldenMeanShiftWindowSix. None is bijective to $\BetaSignSelectionUp$ because this packet binds denominator sign, activation order, admissible branch tag, finite window-six provenance, transport, replay, and local naming into one selection surface.}

\begin{definition}[Beta-sign selection carrier]
\label{def:beta-sign-selection-carrier}
A $\BetaSignSelectionUp$ carrier is a finite $\BHist$ packet
$$
B=(D,A,G,W,Z,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the denominator-deformation row, displaying the inspected source denominator, target denominator, and monotone activation coordinate.
\item $A$ is the beta-sign admissibility row, recording whether the displayed denominator deformation lowers inverse coupling along the activation interval.
\item $G$ is the centered Green-excess row consumed from $\StaticGreenBoundaryFluxUp$.
\item $W$ is the window-six beta-seed row consumed from $\GoldenMeanShiftWindowSixUp$.
\item $Z$ is the Zeckendorf normal-and-carry row, naming ZeckendorfNormal, ZCarry, and ZNormal as the finite normal-word siblings that must be visible before a branch tag is accepted.
\item $S$ is the selected-branch row, retaining the accepted local running seed and the refused walking-branch tag.
\item $H$ records componentwise $\hsame$ transport for $D,A,G,W,Z,S$ and for the structural rows of the packet.
\item $C$ records displayed $\Cont$ replay for denominator inspection, sign selection, Green-excess read, window-six seed read, sibling normal-word read, and branch retention.
\item $P$ is the $\Pkg$ provenance over $\BetaSignSelectionUp$, $\StaticGreenBoundaryFluxUp$, $\ZeckendorfPhaseShellUp$, $\GoldenMeanShiftWindowSixUp$, ZeckendorfNormal, ZCarry, ZNormal, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BetaSignSelectionUp}$ row.
\end{enumerate}
The Lean TasteGate surface for this packet will require \texttt{Nontrivial} and \texttt{FieldFaithful} instances for the displayed carrier rows before the sign-selection classifier can be checked.
\end{definition}

\begin{theorem}[Beta-sign selection NameCert obligation surface]
\label{thm:beta-sign-selection-namecert-obligation-surface}
For every accepted $\BetaSignSelectionUp$ carrier
$$
B=(D,A,G,W,Z,S,H,C,P,N),
$$
the local $\NameCert_{\BetaSignSelectionUp}$ surface consists exactly of denominator-deformation admission through $D$, beta-sign admissibility through $A$, centered Green-excess exposure through $G$, window-six beta-seed exposure through $W$, ZeckendorfNormal--ZCarry--ZNormal sibling visibility through $Z$, selected-branch retention through $S$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$. It exports no fitted infrared value, no continuous beta function, no host analytic flow, no uninspected walking branch, and no physical running claim without the displayed sign row.
\end{theorem}
\begin{proof}
Project the carrier rows from \autoref{def:beta-sign-selection-carrier}. The denominator row $D$ and sign row $A$ are the only entries that decide whether a branch may be read as physically running.

Read the source siblings next. The Green-excess row $G$, window-six row $W$, and normal-word row $Z$ display the finite Zeckendorf sources named by \autoref{prin:zeckendorf-beta-sign-selection}; the selected-branch row $S$ can retain a running seed only after those rows are visible.

The transport, replay, provenance, and naming rows $H,C,P,N$ preserve the same finite packet. Since no coordinate stores a fitted infrared value, a continuous beta function, a host analytic flow, or an uninspected walking branch, the NameCert surface exports only the displayed beta-sign selection route.
\end{proof}

\begin{closurestatus}{\BetaSignSelectionUp}
  \constructivestory{A $\BetaSignSelectionUp$ packet is generated from a denominator-deformation row, beta-sign admissibility row, Green-excess source, window-six beta seed, Zeckendorf sibling visibility, selected-branch retention, componentwise hsame transport, displayed Cont replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:beta-sign-selection-carrier} and \autoref{thm:beta-sign-selection-namecert-obligation-surface} bind the finite denominator-sign, Green-excess, window-six beta-seed, Zeckendorf sibling, transport, replay, provenance, and local naming rows of the BetaSignSelection packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a continuous beta function, a fitted infrared value, a host analytic flow, Lean verification, bridge checking, or a running-law conclusion without the displayed sign row.}
  \upgradepath{A higher closure grade requires Lean carrier rows plus Nontrivial and FieldFaithful instances for the displayed sign-selection classifier.}
\end{closurestatus}
