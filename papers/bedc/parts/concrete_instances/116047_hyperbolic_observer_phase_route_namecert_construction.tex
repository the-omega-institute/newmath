\chapter{A Concrete Naming Certificate for $\HyperbolicObserverPhaseRouteUp$}
\label{ch:concrete-instances-hyperbolic-observer-phase-route-namecert}
\origin{ai}

The $\HyperbolicObserverPhaseRouteUp$ packet realizes the observer-expansion direction of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It binds boundary-shadow, visual-boundary, phase-distance, reversible transport, refusal, provenance, and local naming rows into one concrete route. The chapter is a concrete carrier site; the vision chapter remains an expressive source, not a proof surface.

\begin{definition}[Hyperbolic observer phase route carrier]
\label{def:hyperbolic-observer-phase-route-carrier}
A $\HyperbolicObserverPhaseRouteUp$ carrier is a finite $\BHist$ packet
$$
Q=(B,V,D,T,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the boundary-shadow row read through $\HyperbolicBoundaryShadowUp$.
\item $V$ is the visual-boundary row read through $\HyperbolicVisualBoundaryUp$.
\item $D$ is the phase-distance row read through $\PhaseDistanceObserverExpansionUp$ and the terminal real-facing distance seal used there.
\item $T$ is the reversible disk or phase transport row, read through $\PoincareBoundaryFixedTransportUp$ and $\PoincareDiskPhaseTransportUp$.
\item $F$ is the refusal row blocking interior fixed-center readings, hidden global geometry, and unlogged expansion claims.
\item $H$ stores componentwise $\hsame$ transport for $B,V,D,T,F$.
\item $C$ is the displayed $\Cont$ replay of the observer phase route.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverPhaseRouteUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\PhaseDistanceObserverExpansionUp$, $\PoincareBoundaryFixedTransportUp$, $\PoincareDiskPhaseTransportUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicObserverPhaseRouteUp}$ row.
\end{enumerate}
The carrier contains no coordinate for an ambient hyperbolic manifold, a selected ideal boundary, a global geodesic completion, an empirical cosmology claim, or a hidden observer center.
\end{definition}

\begin{theorem}[Hyperbolic observer phase route NameCert obligations]
\label{thm:hyperbolic-observer-phase-route-namecert-obligations}
For the carrier of \autoref{def:hyperbolic-observer-phase-route-carrier}, the local $\NameCert_{\HyperbolicObserverPhaseRouteUp}$ surface consists exactly of carrier admission for $Q=(B,V,D,T,F,H,C,P,N)$, boundary-shadow coverage by $B$, visual-boundary coverage by $V$, phase-distance readback by $D$, reversible transport by $T$, refusal coverage by $F$, componentwise transport by $H$, displayed replay by $C$, package provenance by $P$, and local naming by $N$.
\end{theorem}
\begin{proof}
Project the packet. The geometric input rows are $B,V,D,T,F$, and each is a displayed row of the finite carrier rather than a theorem about an ambient disk or manifold.

Read $B$ through \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert} and $V$ through \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}. These rows supply the boundary-facing surface before any observer expansion is consumed.

Read $D$ through \autoref{ch:concrete-instances-phase-distance-observer-expansion-namecert}, then read $T$ through \autoref{ch:concrete-instances-poincare-boundary-fixed-transport-namecert} and \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}. The refusal row $F$ prevents those readings from becoming an interior fixed-center claim or an unlogged global-geometry assertion.

Finally $H,C,P,N$ transport, replay, package, and name the same rows. Since the carrier has no further coordinate, the NameCert obligations are exactly the displayed route.
\end{proof}

\begin{theorem}[Hyperbolic observer phase route finite handoff]
\label{thm:hyperbolic-observer-phase-route-finite-handoff}
Every downstream read of an accepted $\HyperbolicObserverPhaseRouteUp$ packet factors through $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\PhaseDistanceObserverExpansionUp$, $\PoincareBoundaryFixedTransportUp$, $\PoincareDiskPhaseTransportUp$, the refusal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperbolicObserverPhaseRouteUp}$ row. It exports no hidden global hyperbolic space, no selected ideal boundary, no empirical observer-expansion law, and no private phase-distance oracle.
\end{theorem}
\begin{proof}
Use the obligation theorem \autoref{thm:hyperbolic-observer-phase-route-namecert-obligations}. It exposes the accepted route as $B,V,D,T,F,H,C,P,N$.

The boundary side is supplied only by the boundary-shadow and visual-boundary siblings. The phase-distance and reversible-transport sides are supplied only by their displayed concrete-instance siblings, so the route cannot bypass them through a private geometry reading.

The refusal row $F$ blocks the strong interpretations: an interior observer center, selected ideal boundary, empirical cosmology claim, and hidden global hyperbolic space. The rows $H,C,P,N$ preserve only the displayed carrier evidence for downstream consumers.
\end{proof}

Future Lean coverage for this packet should require Nontrivial and FieldFaithful TasteGate instances for the boundary-shadow, visual-boundary, phase-distance, reversible-transport, and refusal rows, so that no zero-row or collapsed-field carrier can satisfy the route.

\falsifiablePrediction{Every accepted $\HyperbolicObserverPhaseRouteUp$ read must display boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, hsame, Cont, Pkg, and local NameCert rows before it exports an observer phase route.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-visual-boundary, phase-distance-observer-expansion, poincare-boundary-fixed-transport, poincare-disk-phase-transport: no listed sibling is bijective to $\HyperbolicObserverPhaseRouteUp$ because this packet jointly carries boundary shadows, visual-boundary readback, phase-distance comparison, reversible transport, refusal, replay, provenance, and local naming.}

\closureat{HyperbolicObserverPhaseRouteUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverPhaseRouteUp}
  \constructivestory{A $\HyperbolicObserverPhaseRouteUp$ packet is generated from boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-phase-route-carrier}, \autoref{thm:hyperbolic-observer-phase-route-namecert-obligations}, and \autoref{thm:hyperbolic-observer-phase-route-finite-handoff} describe the finite observer phase route and its downstream handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, global hyperbolic geometry, empirical cosmology, selected ideal boundaries, or observer-center existence.}
  \upgradepath{Scoped closure requires checked carrier coverage, Nontrivial and FieldFaithful TasteGate instances, refusal exactness, and downstream handoff nonescape for $B,V,D,T,F,H,C,P,N$.}
\end{closurestatus}
