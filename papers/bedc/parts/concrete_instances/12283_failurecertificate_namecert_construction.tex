\chapter{A Concrete Naming Certificate for $\FailureCertificateUp$}
\label{ch:concrete-instances-failurecertificate-namecert}
\origin{ai}

The $\FailureCertificateUp$ packet realizes the typed failure-certificate discipline of \autoref{subsec:philosophy-failure-certificates} as a finite $\NameCert$ carrier for rejected gate attempts. It also sits beside \autoref{ch:concrete-instances-auditgatefailurewitness-namecert}, \autoref{ch:concrete-instances-realityconstrainedfailuresurface-namecert}, \autoref{ch:concrete-instances-registry-export-consistency-gate-namecert}, \autoref{ch:concrete-instances-axiszeckendorfcannotclaim-namecert}, and \autoref{ch:concrete-instances-machine-interface-socket-namecert}: those siblings expose audit witnesses, reality-constrained defeat surfaces, registry export gates, cannot-claim rows, and machine-facing sockets, while this packet binds a typed failure to the exact theory axis, verification axis, and combined gate it blocks.

\begin{definition}[Failure certificate carrier]
\label{def:failure-certificate-carrier}
A $\FailureCertificateUp$ carrier is a finite $\BHist$ packet
$$
F=(N_0,C,V,A,B,D,H,K,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $N_0$ is the named claim, chapter, or packet whose gate attempt is being rejected;
\item $C$ is the theory-closure row, recording the claimed theory grade and the denied source, classifier, exactness, descent, ledger, stability, scope, bridge, or no-host-leak clause;
\item $V$ is the verification-status row, recording the claimed formal grade and the denied checked theorem, encoded definition, audit-clean, axiom-clean, or bridge-checked clause;
\item $A$ is the failed-axis row, choosing theory-side failure, verification-side failure, or axis-confusion failure as typed audit data;
\item $B$ is the blocked-gate row, naming the theory gate, formal-status gate, or verified gate that the failed-axis row blocks;
\item $D$ is the diagnostic row, importing a concrete witness from $\AuditGateFailureWitnessUp$, $\RealityConstrainedFailureSurfaceUp$, $\RegistryExportConsistencyGateUp$, or $\AxisZeckendorfCannotClaimUp$ when the rejected packet crosses those surfaces;
\item $H$ stores componentwise $\hsame$ transport for claim name, closure grade, formal grade, failed axis, blocked gate, diagnostic witness, replay, provenance, and local naming rows;
\item $K$ stores displayed $\Cont$ replay for reading $N_0,C,V,A,B,D$ without adding a host repair route;
\item $P$ is the $\Pkg$ provenance over $\FailureCertificateUp$, $\AuditGateFailureWitnessUp$, $\RealityConstrainedFailureSurfaceUp$, $\RegistryExportConsistencyGateUp$, $\AxisZeckendorfCannotClaimUp$, $\MachineInterfaceSocketUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $L$ is the local $\NameCert_{\FailureCertificateUp}$ row.
\end{enumerate}
The carrier contains no coordinate for a private gate override, prose repair, host equality, hidden verifier result, or untyped failure reason outside $A,B,D$.
\end{definition}
\leandef{BEDC.Derived.FailureCertificateUp.FailureCertificateUp}

\begin{theorem}[Failure certificate gate blocking]
\label{thm:failure-certificate-gate-blocking}
For every accepted $\FailureCertificateUp$ packet $F=(N_0,C,V,A,B,D,H,K,P,L)$, the failed-axis row $A$ and blocked-gate row $B$ jointly determine which gate is blocked for the named claim $N_0$. A theory-side failure blocks the theory gate named by $B$, a verification-side failure blocks the formal-status gate named by $B$, and an axis-confusion failure blocks the combined verified gate named by $B$. No accepted packet can recover the blocked gate from $D,H,K,P,L$ alone.
\end{theorem}
\leanchecked{BEDC.Derived.FailureCertificateUp.FailureCertificate\_gate\_blocking}
\begin{proof}
Project the finite carrier of \autoref{def:failure-certificate-carrier}. The row $A$ records the typed failure branch, while $B$ records the gate whose acceptance is denied. These rows are read before diagnostic replay, provenance, or local naming can be consumed.

For a theory-side failure, the denied clause lies in $C$ and the gate named by $B$ is the theory gate. For a verification-side failure, the denied clause lies in $V$ and the gate named by $B$ is the formal-status gate. For axis confusion, $A$ records that the two axes have been collapsed, so the verified gate is blocked even when one row is read without the other.

Finally inspect $D,H,K,P,L$. The diagnostic row only records the concrete witness, and the remaining rows only transport, replay, package, and name the same packet. Since none of them supplies a fresh axis row or a fresh blocked-gate row, the gate-blocking conclusion depends on $A$ and $B$ together.
\end{proof}

\begin{theorem}[Failure certificate axis separation]
\label{thm:failure-certificate-axis-separation}
The $\FailureCertificateUp$ carrier is not bijective, under NameCert-row preservation, to $\AuditGateFailureWitnessUp$, $\RealityConstrainedFailureSurfaceUp$, $\RegistryExportConsistencyGateUp$, $\AxisZeckendorfCannotClaimUp$, or $\MachineInterfaceSocketUp$.
\end{theorem}
\leanchecked{BEDC.Derived.FailureCertificateUp.FailureCertificate\_axis\_separation}
\begin{proof}
Compare the displayed rows. $\AuditGateFailureWitnessUp$ supplies concrete audit failure evidence, $\RealityConstrainedFailureSurfaceUp$ supplies finite defeat surfaces, $\RegistryExportConsistencyGateUp$ supplies export consistency checks, $\AxisZeckendorfCannotClaimUp$ supplies refusal rows, and $\MachineInterfaceSocketUp$ supplies machine-facing target sockets.

None of those siblings carries the joint two-axis gate-blocking row of $A$ together with $B$ for the same named claim, while also preserving $C,V,D,H,K,P,L$. A row-preserving bijection would have to preserve that joint failure-axis and blocked-gate pair, so no such bijection exists.
\end{proof}
\leanchecked{BEDC.Derived.FailureCertificateUp.FailureCertificate\_axis\_row\_separation}

\begin{theorem}[Failure certificate public axis export]
\label{thm:failurecertificate-public-axis-export}
The exported $\FailureCertificateUp$ public row consists exactly of the typed failure branch, the blocked axis, the diagnostic evidence row, the non-collapse of theory and verification axes, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. A public consumer may read $A,B,D,H,K,P,L$ together with the named claim $N_0$ and the separate closure rows $C,V$, but it cannot reinterpret the packet as a private gate override, hidden verifier result, host equality, prose repair route, or untyped failure reason.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:failure-certificate-carrier}. The rows $A$ and $B$ expose the typed failure branch and blocked gate, while $C$ and $V$ keep the theory and verification axes separate.

Apply gate blocking and axis separation, \autoref{thm:failure-certificate-gate-blocking} and \autoref{thm:failure-certificate-axis-separation}. These force the failure evidence to remain tied to the selected axis and prevent sibling audit packets from collapsing the two-axis certificate surface.

The remaining rows $D,H,K,P,L$ provide diagnostic evidence, transport, replay, provenance, and local naming for the same packet. Since no private override, hidden verifier, host equality, prose repair, or untyped reason is a carrier coordinate, the listed rows are exactly the public export surface.
\end{proof}

\begin{theorem}[Failure certificate bridged interface]
\label{thm:failurecertificate-bridged-interface}
Every bridge-facing consumer of an accepted $\FailureCertificateUp$ packet must preserve the named claim row $N_0$, the separate theory row $C$, the separate verification row $V$, the failed-axis row $A$, the blocked-gate row $B$, the diagnostic row $D$, componentwise transport $H$, displayed replay $K$, provenance $P$, and the local $\NameCert$ row $L$. The bridge-facing surface may transport those rows to a standard bridge consumer, but it cannot collapse theory and verification status, drop the failed-axis row, replace the blocked-gate row by a private override, omit the diagnostic witness, or detach the local NameCert row.
\end{theorem}
\begin{proof}
Start with gate blocking in \autoref{thm:failure-certificate-gate-blocking}. It makes the failed-axis row $A$ and blocked-gate row $B$ jointly responsible for the refused gate of the named claim $N_0$, so a bridge consumer cannot preserve the failure certificate after dropping either row.

Use axis separation in \autoref{thm:failure-certificate-axis-separation}. The rows $C$ and $V$ remain distinct from the sibling audit packets, and the failed-axis row records whether the refusal is theory-side, verification-side, or axis-confusion data.

Apply the public axis export of \autoref{thm:failurecertificate-public-axis-export}. The export surface already names $N_0,C,V,A,B,D,H,K,P,L$ as the public rows and excludes private overrides, hidden verifier results, host equality, prose repair, and untyped failure reasons.

Finally read the checked TasteGate witness. Field faithfulness keeps $N_0,C,V,A,B,D,H,K,P,L$ as distinct carrier rows, while nontriviality prevents the bridge-facing packet from degenerating into a vacuous single-row object. Hence any bridge consumer must preserve exactly the listed interface.
\end{proof}

The Lean TasteGate file provides explicit $\Nontrivial\ \FailureCertificateUp$ and $\FieldFaithful\ \FailureCertificateUp$ instances in \texttt{lean4/BEDC/Derived/FailureCertificateUp/TasteGate.lean}. The field-faithfulness witness keeps $N_0,C,V,A,B,D,H,K,P,L$ distinct so the failed-axis row and blocked-gate row cannot be merged.
\leanchecked{BEDC.Derived.FailureCertificateUp.taste\_gate}

\falsifiablePrediction{Removing either the failed-axis row or blocked-gate row breaks the gate-blocking theorem for an accepted packet.}

\independenceWitness{auditgatefailurewitness, realityconstrainedfailuresurface, registryexportconsistencygate, axiszeckendorfcannotclaim, machineinterfacesocket: none is bijective to $\FailureCertificateUp$ because the joint two-axis gate-blocking row $A,B$ is preserved only by the finite failure-certificate packet.}

\closureat{FailureCertificateUp}{seedStr}
\closureat{FailureCertificateUp}{obligationStr}
\closureat{FailureCertificateUp}{scopedStr}
\closureat{FailureCertificateUp}{publicStr}
\closureat{FailureCertificateUp}{bridgedStr}

\begin{closurestatus}{\FailureCertificateUp}
  \constructivestory{A $\FailureCertificateUp$ packet is generated from a named claim row, separate theory-closure and verification-status rows, a failed-axis row, a blocked-gate row, a diagnostic witness row, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{\autoref{def:failure-certificate-carrier}, \autoref{thm:failure-certificate-gate-blocking}, \autoref{thm:failure-certificate-axis-separation}, \autoref{thm:failurecertificate-public-axis-export}, \autoref{thm:failurecertificate-bridged-interface}, and the checked TasteGate witness bind the carrier, gate-blocking row, axis-separation row, public axis export, bridge-facing interface, TasteGate, Nontrivial, and FieldFaithful obligation surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, bridge checking, private gate overrides, host equality, hidden verifier results, or untyped failure reasons.}
  \upgradepath{Bridge closure requires a checked standard bridge for the public failure-certificate carrier and its gate-blocking consumers.}
\end{closurestatus}
