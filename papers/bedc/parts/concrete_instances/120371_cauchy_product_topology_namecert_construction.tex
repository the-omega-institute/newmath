\chapter{A Concrete Naming Certificate for $\CauchyProductTopologyUp$}
\label{ch:concrete-instances-cauchy-product-topology-namecert}
\origin{human}

Following Bourbaki and Kelley on finite product topologies, the $\CauchyProductTopologyUp$ packet records the product-topology read available when each coordinate is presented by a constructive Cauchy real name. It sits after the finite topology certificate of \autoref{ch:concrete-instances-product-topology-namecert}, the Cauchy coordinate-projection certificate of \autoref{ch:concrete-instances-regular-cauchy-product-projection-namecert}, and the completion-projection certificate of \autoref{ch:concrete-instances-cauchy-product-completion-projection-namecert}. The packet gives finite Cauchy-name product consumers a topology-facing coordinate read without importing quotient product equality, selected stream representatives, ambient completeness, or host product spaces.

\begin{definition}[Cauchy product topology carrier]
\label{def:cauchy-product-topology-carrier}
A $\CauchyProductTopologyUp$ carrier is a finite $\BHist$ packet
$$
T=(P,R,Q,W,A,S,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\ProductTopologyUp$ finite product-topology row naming the coordinate-window topology surface.
\item $R$ is the $\RegularCauchyProductProjectionUp$ coordinate-projection readback row.
\item $Q$ is the $\CauchyProductCompletionProjectionUp$ completion-facing projection row.
\item $W$ is the finite family of $\StreamNameUp$ windows used by the coordinate Cauchy names.
\item $A$ is the $\RegSeqRatUp$ regular-rational readback row attached to the displayed windows.
\item $S$ is the pair of $\RealUp$ seal rows reached from the regular readbacks.
\item $H$ records componentwise $\hsame$ transport for topology, projection, completion, stream-window, regular-readback, real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each product-coordinate topology request through $P,R,Q,W,A,S,H,G,N$.
\item $G$ is the $\Pkg$ provenance over $\CauchyProductTopologyUp$, $\ProductTopologyUp$, $\RegularCauchyProductProjectionUp$, $\CauchyProductCompletionProjectionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyProductTopologyUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite product-topology read over Cauchy real-name coordinates. It has no coordinate for quotient product equality, selected stream representatives, host product spaces, global completeness, choice of representatives, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Cauchy product topology NameCert obligations]
\label{thm:cauchy-product-topology-namecert-obligations}
For every accepted $\CauchyProductTopologyUp$ carrier
$$
T=(P,R,Q,W,A,S,H,C,G,N),
$$
the local $\NameCert_{\CauchyProductTopologyUp}$ surface consists exactly of carrier admission for the displayed finite packet, product-topology exposure through $P$, regular Cauchy product projection exposure through $R$, completion projection exposure through $Q$, finite stream-window exposure through $W$, regular-rational readback through $A$, real-name sealing through $S$, componentwise transport through $H$, displayed replay through $C$, provenance through $G$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-product-topology-carrier}. Its rows are precisely the topology row, projection rows, stream-window rows, regular-rational readback, real seals, and structural certificate rows listed in the statement.

Read the three sibling certificates. The $\ProductTopologyUp$ chapter supplies the finite product-window topology row, \autoref{ch:concrete-instances-regular-cauchy-product-projection-namecert} supplies the coordinate Cauchy projection row, and \autoref{ch:concrete-instances-cauchy-product-completion-projection-namecert} supplies the completion-facing projection row.

Finally replay through $H,C,G,N$. These rows transport, replay, source, and name the already displayed route, so the local certificate cannot add quotient product equality, selected stream representatives, host product spaces, global completeness, or a Lean-side verification claim.
\end{proof}

\begin{theorem}[Cauchy product topology finite product readback]
\label{thm:cauchy-product-topology-finite-product-readback}
Every finite product-coordinate topology read exported by an accepted $\CauchyProductTopologyUp$ carrier factors through
$$
P,R,Q,W,A,S,H,C,G,N
$$
in that order: the $\ProductTopologyUp$ window is exposed before the $\RegularCauchyProductProjectionUp$ and $\CauchyProductCompletionProjectionUp$ projection rows are consumed, the $\StreamNameUp$ windows and $\RegSeqRatUp$ readbacks are displayed before the $\RealUp$ seals, and the final route is preserved only by $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.
\end{theorem}
\begin{proof}
Start with the accepted packet $T$ from \autoref{def:cauchy-product-topology-carrier}. Since $P$ is the only product-topology row, every coordinate topology read must expose $P$ before any Cauchy projection or completion projection can be consumed.

Use the projection certificates cited in the introduction. The regular Cauchy projection row $R$ reads finite coordinate windows, and the completion projection row $Q$ preserves that coordinate route at the completion-facing boundary.

Then read the real-name source. The finite windows $W$ feed the regular-rational row $A$, and $A$ feeds the terminal real seals $S$; no selected stream representative or quotient product equality is present in the carrier.

The structural rows $H,C,G,N$ preserve and name the same finite route. Hence the exported read is the displayed Cauchy-name product topology route, not an ambient product space, host completeness theorem, or bridge-checked object.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyProductTopologyUp$ read displays the $\ProductTopologyUp$ finite product window, $\RegularCauchyProductProjectionUp$ coordinate readback, $\CauchyProductCompletionProjectionUp$ completion projection, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\RealUp$ seals, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before a product-coordinate topology consumer can use the route.}

\independenceWitness{product-topology, regular-cauchy-product-projection, cauchy-product-completion-projection, streamname, regseqrat, real: $\CauchyProductTopologyUp$ is not bijective to any listed sibling because it jointly carries product-topology windows, regular Cauchy projection readback, completion projection, finite stream windows, regular-rational readback, real seals, transport, replay, provenance, and local naming.}

\closureat{CauchyProductTopologyUp}{seedStr}

\begin{closurestatus}{\CauchyProductTopologyUp}
  \constructivestory{A $\CauchyProductTopologyUp$ packet is generated from a ProductTopology finite window row, RegularCauchyProductProjection coordinate readback, CauchyProductCompletionProjection completion-facing row, StreamName windows, RegSeqRat readback, RealUp seals, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-product-topology-carrier}, \autoref{thm:cauchy-product-topology-namecert-obligations}, and \autoref{thm:cauchy-product-topology-finite-product-readback} seed the finite product-topology route over Cauchy real-name coordinates.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient product equality, selected stream representatives, ambient product spaces, global completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite product-window exposure, coordinate projection readback, completion projection exposure, stream-window accounting, regular-rational readback, real sealing, transport, replay, provenance, and local naming for BEDC.Derived.CauchyProductTopologyUp.}
\end{closurestatus}
