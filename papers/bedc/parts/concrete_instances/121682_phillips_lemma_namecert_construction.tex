\chapter{A Concrete Naming Certificate for $\PhillipsLemmaUp$}
\label{ch:concrete-instances-phillips-lemma-namecert}
\origin{human}

Following Phillips on lifting/local-reflexivity phenomena in Banach-space theory, the $\PhillipsLemmaUp$ packet records the finite BEDC surface by which a bounded lifting request is read through Banach endpoints, dual-pair windows, finite approximation data, and an explicit obstruction row. It sits after the functional-analysis surface of \autoref{ch:concrete-instances-functionalanalysis-namecert}, the Banach carrier of \autoref{ch:concrete-instances-banach-namecert}, the bounded-operator route of \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, and the operator-ideal consumer boundary of \autoref{ch:concrete-instances-operatorideal-namecert}. The certificate names the local lifting packet only; it does not assert a quotient dual space, a globally selected local inverse, reflexivity of the source, exactness of every short sequence, or ambient choice over finite-dimensional subspaces.

\begin{definition}[PhillipsLemma carrier]
\label{def:phillips-lemma-carrier}
A $\PhillipsLemmaUp$ carrier is a finite $\BHist$ packet
$$
P=(S,T,B,D,A,O,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source $\BanachUp$ row, including the norm, metric, completion, and real-seal reads available to the lifting request.
\item $T$ is the target $\BanachUp$ row, displayed with the same Banach-facing endpoint discipline.
\item $B$ is the bounded lifting row. It records the bounded-operator surface through which a local lift is inspected, including its action window, norm-bound row, and compatibility with the Banach endpoints.
\item $D$ is the dual-pair readback row. It displays the finite dual tests used by the lifting request before any operator-ideal or functional-analysis consumer reads the packet.
\item $A$ is the finite approximation witness row, listing the inspected finite-dimensional source window, tolerance ledger, and bounded image data.
\item $O$ is the obstruction/refusal row. It records which global lifting, quotient-dual, or selector claims are not exported by the packet.
\item $H$ records componentwise $\hsame$ transport for the Banach endpoint rows, bounded lifting row, dual-pair readback, approximation witness, obstruction row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a lifting request through $S,T,B,D,A,O,H$.
\item $Q$ is the $\Pkg$ provenance over $\PhillipsLemmaUp$, $\FunctionalAnalysisUp$, $\BanachUp$, $\BoundedLinearOperatorUp$, $\OperatorIdealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\PhillipsLemmaUp}$ row.
\end{enumerate}
The carrier has no coordinate for a quotient-dual equality, a selected complement, a global right inverse, countable choice over subspaces, host operator equality, or reflexivity beyond the displayed Banach endpoint rows.
\end{definition}

\begin{theorem}[PhillipsLemma NameCert obligations]
\label{thm:phillips-lemma-namecert-obligations}
For every accepted $\PhillipsLemmaUp$ carrier
$$
P=(S,T,B,D,A,O,H,C,Q,N),
$$
the local $\NameCert_{\PhillipsLemmaUp}$ surface is exhausted by five grouped rows: Banach source and target admission through $S,T$; bounded lifting exposure through $B$; dual-pair readback and finite approximation exposure through $D,A$; obstruction and refusal visibility through $O$; and structural closure through componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through $H,C,Q,N$. Every functional-analysis, Banach, bounded-operator, or operator-ideal consumer must read those grouped rows before treating a local Phillips lifting witness as public certificate data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:phillips-lemma-carrier}. The Banach endpoint rows $S,T$ are the only source and target data available to the packet, and the bounded lifting row $B$ is the only row carrying an operator action and norm-bound ledger.

Read the dual and approximation data next. The row $D$ supplies the displayed dual-pair tests, while $A$ supplies the finite-dimensional window and tolerance ledger used by the local lifting witness.

The obstruction row $O$ blocks the packet from exporting a global right inverse, selected complement, quotient-dual equality, or choice schedule. Thus the lifting read remains local to the displayed finite window.

Finally replay $H,C,Q,N$. Componentwise transport, continuation replay, provenance, and local naming preserve the same rows, so the five grouped obligations exhaust the NameCert surface before any consumer in \autoref{ch:concrete-instances-functionalanalysis-namecert}, \autoref{ch:concrete-instances-banach-namecert}, \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, or \autoref{ch:concrete-instances-operatorideal-namecert} can cite the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\PhillipsLemmaUp$ read exposes Banach source and target rows, a bounded lifting row, a dual-pair readback row, a finite approximation witness row, an obstruction/refusal row, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row before any functional-analysis or operator-ideal consumer is served.}

\independenceWitness{functionalanalysis, banach, boundedlinearoperator, operatorideal: none alone is bijective with $\PhillipsLemmaUp$ because this packet alone binds the Banach endpoint rows, bounded lifting window, dual-pair readback, finite approximation witness, obstruction/refusal row, transport, replay, provenance, and local naming as one certificate.}

\closureat{PhillipsLemmaUp}{seedStr}

\begin{closurestatus}{\PhillipsLemmaUp}
  \constructivestory{A $\PhillipsLemmaUp$ packet is generated from Banach source and target rows, a bounded lifting row, a dual-pair readback row, a finite approximation witness row, an obstruction/refusal row, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:phillips-lemma-carrier} and \autoref{thm:phillips-lemma-namecert-obligations} seed the local Banach lifting certificate route over functional-analysis, bounded-operator, operator-ideal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient-dual equality, selected complements, global right inverses, exactness of arbitrary short sequences, countable choice over subspaces, host operator equality, reflexivity beyond displayed endpoint rows, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked Banach endpoint admission, bounded lifting exposure, dual-pair readback, finite approximation witness exactness, obstruction/refusal visibility, transport, replay, provenance, and local naming for BEDC.Derived.PhillipsLemmaUp.}
\end{closurestatus}
