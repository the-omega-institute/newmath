\begin{theorem}[GeneratorClosureRobustness forward-kernel consumer]
\label{thm:generator-closure-robustness-forward-kernel-consumer}
Let $G=(A,E,L,Q,P,T,H,C,K,N)$ be an accepted $\GeneratorClosureRobustnessUp$ carrier. Any forward socket or kernel-audit consumer citing the robustness packet must read the active-generator row $A$, deletion-loss rows $L$, $Q_{2}$ completion row $Q$, minimal-seed row $P$, trigger row $T$, componentwise transport row $H$, replay row $C$, provenance row $K$, and local naming row $N$ before invoking the $\ForwardSocketLedgerUp$ route of \autoref{ch:concrete-instances-forward-socket-ledger-namecert}. The forward-kernel route is a displayed finite dependency route; it does not export biological necessity, probabilistic selection, host optimality, hidden codon classifiers, or an unbounded search principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:generator-closure-robustness-carrier}. The rows $A,E,L,Q,P,T,H,C,K,N$ are the only coordinates a robustness consumer can inspect.

Use the NameCert obligations of \autoref{thm:generator-closure-robustness-namecert-obligations}. They require active-generator exactness, deletion-loss exactness, $Q_{2}$ completion exactness, seed minimality, and the local non-escape clauses before any public robustness read is accepted.

Apply \autoref{thm:generator-closure-robustness-q2-completion-exactness} and \autoref{thm:generator-closure-robustness-trigger-closure-boundary}. These force the completion and trigger reads to pass through $A,L,Q,P,T,H,C,K,N$ as one transported packet.

Only after those rows are visible may the consumer cite \autoref{ch:concrete-instances-forward-socket-ledger-namecert}. The $\ForwardSocketLedgerUp$ chapter records the downstream socket ledger, while the robustness rows remain the finite kernel evidence that the consumer is auditing.
\end{proof}
