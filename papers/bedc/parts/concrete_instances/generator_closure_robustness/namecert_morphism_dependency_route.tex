\begin{theorem}[GeneratorClosureRobustness NameCertMorphism dependency route]
\label{thm:generator-closure-robustness-namecert-morphism-dependency-route}
Let $G=(A,E,L,Q,P,T,H,C,K,N)$ be an accepted $\GeneratorClosureRobustnessUp$ carrier. Every morphism-facing consumer of this packet must preserve the active-generator row $A$, the deletion-loss rows $L$, the $Q_{2}$ completion row $Q$, the minimal-seed row $P$, the trigger row $T$, the componentwise transport row $H$, the replay row $C$, the provenance row $K$, and the local naming row $N$ before the read is transported through \autoref{ch:concrete-instances-namecertmorphism-namecert}. The route is a horizontal NameCert dependency route; it exports no biological necessity, probabilistic selection principle, host optimum, hidden codon classifier, unbounded search oracle, or morphism detached from the displayed robustness packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:generator-closure-robustness-carrier}. The finite rows $A,E,L,Q,P,T,H,C,K,N$ are the only data available to a morphism-facing robustness consumer.

Apply the NameCert obligations of \autoref{thm:generator-closure-robustness-namecert-obligations}. They force carrier admission, active-generator exactness, deletion-loss exactness, $Q_{2}$ completion exactness, seed minimality, trigger comparison, transport, replay, provenance, and local naming to remain visible before any robustness read is accepted.

Use \autoref{thm:generator-closure-robustness-q2-completion-exactness} and \autoref{thm:generator-closure-robustness-trigger-closure-boundary}. These keep the completion and trigger rows inside the same transported packet, so a consumer cannot replace the robustness evidence by a hidden classifier or an unbounded search principle.

Only after those rows are preserved may the consumer cite \autoref{ch:concrete-instances-namecertmorphism-namecert}. The NameCertMorphism route transports the displayed naming dependency, while $\GeneratorClosureRobustnessUp$ still supplies the finite active-generator, deletion-loss, completion, seed, trigger, transport, replay, provenance, and local naming rows being transported.
\end{proof}
