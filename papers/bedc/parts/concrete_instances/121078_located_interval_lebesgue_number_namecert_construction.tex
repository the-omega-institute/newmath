\chapter{A Concrete Naming Certificate for $\LocatedIntervalLebesgueNumberUp$}
\label{ch:concrete-instances-located-interval-lebesgue-number-namecert}
\origin{human}

Following Bishop and Bridges, the $\LocatedIntervalLebesgueNumberUp$ packet records the located-interval Lebesgue-number handoff at the point where the $\LocatedClosedIntervalFiniteNetUp$ surface, finite interval nets, $\DyadicRatCoreUp$ comparison budgets, $\RealUp$ endpoint seals, and $\HeineBorelFiniteNetUp$ finite-subcover radius ledgers are still visible. It sits after \autoref{ch:concrete-instances-located-closed-interval-finite-net-namecert}, \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-heine-borel-finite-net-namecert}, where the finite subcover radius ledger already appears in \autoref{thm:heine-borel-finite-net-subcover-extraction} and \autoref{thm:heine-borel-finite-net-obligation-closure-package}. The located interval packet consumes the finite-radius ledger of \autoref{ch:concrete-instances-finite-lebesgue-number-namecert} and the interval finite-net extraction of \autoref{ch:concrete-instances-heine-borel-finite-net-namecert} before exporting the located Lebesgue-number handoff. The packet gives L10 compact-uniform and finite-subcover consumers a located Lebesgue-number handoff without claiming arbitrary open-cover compactness, selected subcovers, countable choice, quotient interval equality, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Located interval Lebesgue-number carrier]
\label{def:located-interval-lebesgue-number-carrier}
A $\LocatedIntervalLebesgueNumberUp$ carrier is a finite $\BHist$ packet
$$
L=(I,F,L,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the located interval row. It exposes the located endpoints and interval-order data used by the cover request.
\item $F$ is the finite cover row. It records the displayed finite interval net and cover witnesses attached to the located interval.
\item $L$ is the positive Lebesgue radius row. It gives the radius ledger that makes the displayed finite cover effective.
\item $D$ is the $\DyadicRatCoreUp$ comparison ledger. It records dyadic slack, mesh refinement, and endpoint comparison budgets for the cover route.
\item $E$ is the terminal $\RealUp$ seal row. It is read only after the located interval, finite cover, positive radius, and dyadic comparison rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for the located interval, finite cover, positive radius, dyadic ledger, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the finite cover request through $I,F,L,D,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LocatedIntervalLebesgueNumberUp$, $\LocatedClosedIntervalFiniteNetUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedIntervalLebesgueNumberUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,F,L,D,E,H,C,P,N$. It has no coordinate for arbitrary open-cover compactness, a selected subcover, countable choice, quotient interval equality, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Located interval Lebesgue-number NameCert obligations]
\label{thm:located-interval-lebesgue-number-namecert-obligations}
For every accepted $\LocatedIntervalLebesgueNumberUp$ carrier $L=(I,F,L,D,E,H,C,P,N)$, the local $\NameCert_{\LocatedIntervalLebesgueNumberUp}$ surface consists exactly of carrier admission, interval exposure, radius compatibility, finite-net transfer, and nonescape:
\begin{enumerate}
\item carrier admission for the located interval row, finite cover row, positive Lebesgue radius row, dyadic comparison ledger, Real seal, transport, replay, provenance, and local naming rows;
\item interval exposure, requiring the cover request to display $I$ and $F$ before any cover witness is read;
\item radius compatibility, requiring the positive radius row $L$ and the dyadic comparison row $D$ to control mesh width, endpoint containment, and finite cover slack;
\item finite-net transfer, requiring every finite-cover read to pass through the route $I \longmapsto F \longmapsto L \longmapsto D \longmapsto E$ before any uniform-modulus or subcover route is visible;
\item nonescape, requiring $H,C,P,N$ to transport, replay, source, and name only the displayed packet.
\end{enumerate}
The obligation surface may cite \autoref{ch:concrete-instances-located-closed-interval-finite-net-namecert}, \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-heine-borel-finite-net-namecert}; it does not add arbitrary open-cover compactness, selected subcovers, countable choice, quotient interval equality, host real equality, or bridge checking.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-interval-lebesgue-number-carrier}. The visible rows are exactly the located interval, finite cover, positive radius, dyadic comparison, Real seal, transport, replay, provenance, and local naming rows.

The interval exposure is immediate. A finite cover consumer must read $I$ and $F$ before any witness is inspected, so the located interval and the displayed finite interval net are already public before a radius is used.

The radius compatibility is then forced through $L$ and $D$. The positive Lebesgue radius row and the dyadic comparison ledger control the mesh width, endpoint containment, and finite-cover slack before the terminal seal is opened.

Finally the finite-net transfer and nonescape are carried by the same packet. The route goes through $I \longmapsto F \longmapsto L \longmapsto D \longmapsto E$ and then through $H,C,P,N$, so the chapter cannot export arbitrary compactness, selected subcovers, countable choice, quotient interval equality, host real equality, ambient completeness, or bridge checking.
\end{proof}

\begin{theorem}[Located interval Lebesgue-number finite-cover handoff]
\label{thm:located-interval-lebesgue-number-finite-cover-handoff}
Every finite-cover consumer of an accepted $\LocatedIntervalLebesgueNumberUp$ packet reads through the route
$$
I \longmapsto F \longmapsto L \longmapsto D \longmapsto E \longmapsto H,C,P,N .
$$
The handoff is the located-interval, finite-cover, positive-radius, DyadicRatCore, and RealUp route that later feeds compact-uniform-modulus and finite-subcover consumers, but it does not export a selected subcover, arbitrary open-cover compactness, quotient interval equality, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:located-interval-lebesgue-number-carrier}. The consumer first sees the located interval row $I$ and the finite cover row $F$, so the read is already confined to the displayed interval data.

Apply \autoref{thm:located-interval-lebesgue-number-namecert-obligations}. It forces the positive radius row $L$ and dyadic comparison row $D$ to mediate every cover witness before the Real seal $E$ can be consumed.

Finally replay the structural rows. The transport, replay, provenance, and local naming rows preserve the same finite route, so every finite-cover consumer factors through the displayed handoff before any uniform-modulus or subcover route can be used.
\end{proof}

\closureat{LocatedIntervalLebesgueNumberUp}{seedStr}

\begin{closurestatus}{\LocatedIntervalLebesgueNumberUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-interval-lebesgue-number-carrier}, \autoref{thm:located-interval-lebesgue-number-namecert-obligations}, and \autoref{thm:located-interval-lebesgue-number-finite-cover-handoff} seed the located-interval Lebesgue-number route for finite-cover consumers over located intervals, positive radii, dyadic comparison budgets, and Real seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, selected subcovers, countable choice, quotient interval equality, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, interval exposure, radius compatibility, finite-net transfer, nonescape, transport, replay, provenance, and local naming for BEDC.Derived.LocatedIntervalLebesgueNumberUp.}
  \constructivestory{A $\LocatedIntervalLebesgueNumberUp$ packet is generated from a located interval row, a finite cover row, a positive Lebesgue radius row, a DyadicRatCore comparison ledger, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
