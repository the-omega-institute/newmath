\chapter{A Concrete Naming Certificate for $\NestedLocatedIntervalConvergenceUp$}
\label{ch:concrete-instances-nested-located-interval-convergence-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, nested located intervals converge here only through displayed finite interval windows, dyadic diameter ledgers, and regular rational readback. The $\NestedLocatedIntervalConvergenceUp$ packet binds the located bisection route of \autoref{ch:concrete-instances-located-interval-bisection-namecert}, the nested dyadic Cauchy handoff of \autoref{ch:concrete-instances-nested-dyadic-cauchy-namecert}, and the tail-diameter Real seal of \autoref{ch:concrete-instances-bisection-convergence-namecert} to the finite StreamName, RegSeqRat, and Real surfaces of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. It names the L10 finite-means route from located interval bisection data to a regular Cauchy real seal; it is not ambient $\RealUp$ completeness, a quotient-stream representative, trichotomy, countable choice, or a completed intersection point.

\begin{definition}[Nested located interval convergence carrier]
\label{def:nested-located-interval-convergence-carrier}
A $\NestedLocatedIntervalConvergenceUp$ carrier is a finite $\BHist$ packet
$$
L=(I,B,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\LocatedIntervalBisectionUp$ source row. It records the located endpoint data, midpoint or locator decisions, retained child intervals, and the Real-facing seal discipline supplied by the located bisection packet.
\item $B$ is the $\BisectionConvergenceUp$ tail-diameter row. It assigns each requested tolerance to a displayed retained-window depth and a diameter bound already present in the finite bisection route.
\item $D$ is the $\NestedDyadicCauchyUp$ handoff row. It repacks the retained located windows as dyadic Cauchy windows with an explicit shrinking-width budget.
\item $S$ is the $\StreamNameUp$ schedule row opened from the retained window depth selected by $B$ and the dyadic endpoint observations exposed by $D$.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row formed from the StreamName windows, dyadic endpoint ledger, and shrinking-diameter budget.
\item $E$ is the terminal $\RealUp$ seal row reached only after $I,B,D,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the located bisection source, convergence budget, nested dyadic handoff, StreamName schedule, RegSeqRat readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located-interval convergence request through retained bisection windows, tail-diameter scheduling, dyadic Cauchy handoff, finite window opening, regular readback, and Real-seal consumption.
\item $P$ is the $\Pkg$ provenance over $\NestedLocatedIntervalConvergenceUp$, $\LocatedIntervalBisectionUp$, $\NestedDyadicCauchyUp$, $\BisectionConvergenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\NestedLocatedIntervalConvergenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,B,D,S,R,E,H,C,P,N$. It has no coordinate for ambient $\RealUp$ completeness, host closed intervals, quotient stream equality, selected representatives, trichotomy, countable choice, completed intersections, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Nested located interval convergence NameCert obligations]
\label{thm:nested-located-interval-convergence-namecert-obligations}
For every accepted $\NestedLocatedIntervalConvergenceUp$ carrier
$$
L=(I,B,D,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\NestedLocatedIntervalConvergenceUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the located bisection source, tail-diameter budget, nested dyadic Cauchy handoff, StreamName schedule, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming;
\item located-source discipline, requiring every endpoint, locator, midpoint, and retained-window read to factor through $I$ before any convergence budget is inspected;
\item diameter exactness, requiring every requested tolerance to be answered by the displayed retained-window depth and diameter bound stored in $B$;
\item dyadic Cauchy compatibility, requiring $D$ to reuse the same retained windows and expose only the dyadic endpoint observations admitted by the located source and diameter budget;
\item finite readback discipline, requiring $S$ and $R$ to be formed only after $I,B,D$ have fixed the visible windows and dyadic budget;
\item Real-seal nonescape, requiring $E,H,C,P,N$ to export no ambient $\RealUp$ completeness, host closed interval, quotient stream equality, selected representative, trichotomy, countable choice, completed intersection, Lean verification, or bridge data.
\end{enumerate}
No nested located interval convergence obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:nested-located-interval-convergence-carrier}. The projection exposes exactly the located bisection source $I$, the tail-diameter budget $B$, the nested dyadic handoff $D$, the finite schedule $S$, the regular readback $R$, the terminal seal $E$, and the structural rows $H,C,P,N$.

Located-source discipline is supplied by $I$. Endpoint, locator, midpoint, branch-retention, and retained-window requests all enter through the $\LocatedIntervalBisectionUp$ row before a convergence budget or Cauchy handoff can be read.

Diameter exactness is supplied by $B$. Each tolerance request is answered by a displayed retained-window depth and a displayed diameter bound, so no hidden completeness theorem or countable-choice selector chooses a limit.

Dyadic Cauchy compatibility is supplied by $D$. The handoff reuses the retained located windows and endpoint observations already exposed by $I$ and $B$, then records the shrinking-width data needed by the nested dyadic Cauchy route.

Finally $S,R,E,H,C,P,N$ open finite windows, form regular rational readback, expose the Real seal, and preserve the same route under transport, replay, provenance, and local naming. Since the excluded objects have no carrier coordinate, the local NameCert surface cannot export them.
\end{proof}

\begin{theorem}[Nested located interval convergence Cauchy-seal route]
\label{thm:nested-located-interval-convergence-cauchy-seal-route}
Let
$$
L=(I,B,D,S,R,E,H,C,P,N)
$$
be an accepted $\NestedLocatedIntervalConvergenceUp$ carrier. Every $\RegSeqRatUp$ or $\RealUp$ consumer of $L$ factors through the displayed route
$$
I \longmapsto B \longmapsto D \longmapsto S \longmapsto R \longmapsto E .
$$
The consumer receives the located interval bisection rows, tail-diameter budget, nested dyadic Cauchy handoff, finite StreamName windows, regular rational readback, and terminal Real seal, but receives no ambient $\RealUp$ completeness, quotient representative, trichotomy, countable choice, host interval equality, or completed intersection point.
\end{theorem}
\begin{proof}
Start with an accepted consumer read. The only source interval coordinate is $I$, so the route first displays the located bisection rows of \autoref{ch:concrete-instances-located-interval-bisection-namecert}.

Next read the convergence budget $B$. This is the tail-diameter stage supplied by \autoref{ch:concrete-instances-bisection-convergence-namecert}; it fixes the retained-window depth and diameter bound used by the requested tolerance.

The nested Cauchy stage is $D$. It transports the same retained windows into the dyadic Cauchy handoff of \autoref{ch:concrete-instances-nested-dyadic-cauchy-namecert}, so the subsequent finite windows and regular readback cannot use an independent interval chain.

The readback stage is $S$ followed by $R$. The StreamName row opens only the scheduled finite observations, and the RegSeqRat row repacks those observations under the displayed dyadic budget.

The terminal seal is $E$, with $H,C,P,N$ preserving the same route as structural data. Because no excluded principle appears in the carrier inventory or replay path, every RegSeqRat or Real-facing consumer factors through the displayed Cauchy-seal route.
\end{proof}

\falsifiablePrediction{Every $\NestedLocatedIntervalConvergenceUp$ consumer read factors through one $\LocatedIntervalBisectionUp$ source row, one $\BisectionConvergenceUp$ tail-diameter row, one $\NestedDyadicCauchyUp$ handoff row, one $\StreamNameUp$ finite-window schedule, one $\RegSeqRatUp$ readback row, one terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}

\independenceWitness{located-interval-bisection, nested-dyadic-cauchy, bisection-convergence, streamname, regseqrat, real: $\NestedLocatedIntervalConvergenceUp$ is not bijective to any listed sibling because it binds located bisection source data, tail-diameter scheduling, dyadic Cauchy handoff, finite window opening, regular readback, and terminal Real sealing as one convergence certificate.}

\closureat{NestedLocatedIntervalConvergenceUp}{seedStr}

\begin{closurestatus}{\NestedLocatedIntervalConvergenceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nested-located-interval-convergence-carrier}, \autoref{thm:nested-located-interval-convergence-namecert-obligations}, and \autoref{thm:nested-located-interval-convergence-cauchy-seal-route} seed the finite L10 located-interval convergence route from located bisection data through tail-diameter scheduling, nested dyadic Cauchy handoff, StreamName windows, RegSeqRat readback, and the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient $\RealUp$ completeness, quotient stream equality, selected representatives, trichotomy, countable choice, host interval equality, completed intersections, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-source discipline, diameter exactness, dyadic Cauchy compatibility, finite readback discipline, Real-seal nonescape, and Cauchy-seal route factorization for BEDC.Derived.NestedLocatedIntervalConvergenceUp.}
  \constructivestory{A $\NestedLocatedIntervalConvergenceUp$ packet is generated from a $\LocatedIntervalBisectionUp$ source row, a $\BisectionConvergenceUp$ tail-diameter row, a $\NestedDyadicCauchyUp$ handoff row, a $\StreamNameUp$ finite-window schedule, a $\RegSeqRatUp$ readback row, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
