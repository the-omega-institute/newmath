\begin{definition}[CommRing left zero-divisor data]
\label{def:commring-left-zero-divisor-data}
For a BEDC history-level $\CommRingUp$ operation tuple with multiplication
$\cdot$ and zero $0$, write
$$
\mathsf{ApartZero}_R(x):=(x\hsame0\to\bot).
$$
The strict left zero-divisor predicate is
$$
\mathsf{LeftZD}_R(x):=
\mathsf{ApartZero}_R(x)\land
\exists c : \mathsf{BHist},\
\mathsf{ApartZero}_R(c)\land x\cdot c\hsame0.
$$
\end{definition}
\leandef{BEDC.Derived.CommRingUp.CommRingApartZero}
\leandef{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor}

\begin{theorem}[CommRing apart-zero visible pair]
\label{thm:commring-apartzero-visible-pair}
For every history $h:\mathsf{BHist}$, both visible constructors are apart from
the empty zero history:
$$
\mathsf{ApartZero}_R(\Ezero(h))\land
\mathsf{ApartZero}_R(\Eone(h)).
$$
\end{theorem}
\begin{proof}
Each conjunct is the corresponding BEDC history no-confusion law: neither an
$\Ezero$-headed history nor an $\Eone$-headed history can be history-same as
$\emp$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.CommRingApartZero\_visible\_pair}

\begin{definition}[CommRing strict two-sided zero-divisor data]
\label{def:commring-strict-zero-divisor-data}
For the same BEDC history-level $\CommRingUp$ operation tuple, the strict
two-sided zero-divisor predicate is
$$
\mathsf{StrictZD}_R(x):=
\mathsf{ApartZero}_R(x)\land
\exists c:\mathsf{BHist},\
\mathsf{ApartZero}_R(c)\land x\cdot c\hsame0\land c\cdot x\hsame0.
$$
\end{definition}
\leandef{BEDC.Derived.CommRingUp.CommRingStrictZeroDivisor}

\begin{theorem}[CommRing singleton multiplication visible strict zero-divisors]
\label{thm:commring-singleton-mul-visible-strict-zero-divisor}
For every BEDC history $h$, both visible singleton histories
$\Ezero(h)$ and $\Eone(h)$ are strict two-sided zero-divisors for the singleton
commutative-ring multiplication:
$$
\mathsf{StrictZD}_{\mathsf{singleton}}(\Ezero(h))
\land
\mathsf{StrictZD}_{\mathsf{singleton}}(\Eone(h)).
$$
\end{theorem}
\begin{proof}
The singleton multiplication sends every pair of histories to $\emp$. For
$\Ezero(h)$ use $\Eone(h)$ as the displayed annihilator, and for $\Eone(h)$ use
$\Ezero(h)$. Constructor disjointness gives the apart-from-zero clauses for
both visible histories, while both ordered products reduce to $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.CommRingSingletonMul\_visible\_strictZeroDivisor}

\begin{theorem}[CommRing left zero-divisor strictness from commutativity]
\label{thm:commring-left-zero-divisor-strict-of-mul-comm}
For any BEDC history multiplication $mul$ satisfying multiplicative
commutativity under $\hsame$, every left zero-divisor carries a two-sided
annihilator witness:
$$
\mathsf{LeftZD}_R(x)
\Longrightarrow
\mathsf{ApartZero}_R(x)\land
\exists c:\mathsf{BHist},\
\mathsf{ApartZero}_R(c)\land x\cdot c\hsame0\land c\cdot x\hsame0.
$$
\end{theorem}
\begin{proof}
Unpack the left zero-divisor data. It gives the apartness of $x$ and a witness
$c$ with $\mathsf{ApartZero}_R(c)$ and $x\cdot c\hsame0$. Apply multiplicative
commutativity to obtain $c\cdot x\hsame x\cdot c$, then compose by transitivity
with the displayed left annihilation row. The same witness $c$ therefore supplies
both annihilator orientations.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_strict\_of\_mul\_comm}

\begin{theorem}[CommRing left zero-divisor strictness iff under commutativity]
\label{thm:commring-left-zero-divisor-strict-iff-of-mul-comm}
For any BEDC history multiplication $mul$, written as $\cdot$, satisfying
multiplicative commutativity under $\hsame$, every $x:\mathsf{BHist}$
satisfies
$$
(\forall u v:\mathsf{BHist},\,u\cdot v\hsame v\cdot u)
\Longrightarrow
\bigl(\mathsf{LeftZD}_R(x)\Longleftrightarrow\mathsf{StrictZD}_R(x)\bigr).
$$
\end{theorem}
\begin{proof}
For the forward implication, assume $\mathsf{LeftZD}_R(x)$ and the displayed
multiplicative commutativity row. Apply
\autoref{thm:commring-left-zero-divisor-strict-of-mul-comm}. Its conclusion is
the apartness of $x$ together with an apart annihilator $c$ satisfying both
$x\cdot c\hsame0$ and $c\cdot x\hsame0$, which is exactly
$\mathsf{StrictZD}_R(x)$ by
\autoref{def:commring-strict-zero-divisor-data}.

For the reverse implication, unfold $\mathsf{StrictZD}_R(x)$. The strict
two-sided data gives $\mathsf{ApartZero}_R(x)$ and a witness
$c:\mathsf{BHist}$ with $\mathsf{ApartZero}_R(c)$, $x\cdot c\hsame0$, and
$c\cdot x\hsame0$. Dropping the right-annihilation conjunct leaves precisely
the apartness of $x$, the same witness $c$, its apartness, and
$x\cdot c\hsame0$, hence $\mathsf{LeftZD}_R(x)$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_strictZeroDivisor\_of\_mul\_comm\_iff}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_strict\_iff\_of\_mul\_comm}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_strict\_expansion\_iff\_of\_mul\_comm}
Ten-field fit: traditional target left and two-sided zero-divisor collapse in
commutative rings; BEDC source $\CommRingUp$ over $\mathsf{BHist}$; carrier
$\mathsf{BHist}$; classifier $\hsame$; stability multiplicative commutativity
and transitivity; ledger selected annihilator witness; core theorem
\autoref{thm:commring-left-zero-divisor-strict-iff-of-mul-comm}; exactness
equivalence of one-sided and two-sided annihilator data; standard bridge
ordinary commutative-ring zero divisors; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing zero-divisor product closure]
\label{thm:commring-zero-divisor-product-closure}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, multiplicative associativity, addition
and multiplication congruence, left distributivity, and the $\CommRingUp$
multiplicative commutativity field, every $a,b:\mathsf{BHist}$ satisfies
$$
\mathsf{LeftZD}_R(a)
\Longrightarrow
\bigl((a\cdot b)\hsame0\to\bot\bigr)
\Longrightarrow
\mathsf{LeftZD}_R(a\cdot b).
$$
\end{theorem}
\begin{proof}
First derive right distributivity from the supplied left distributivity by
\autoref{thm:commring-right-distrib-from-left}. With this row, the additive
group laws, and multiplication congruence, the hypotheses of
\autoref{thm:ring-zero-classifier-factor-absorption} are available.

Unpack $\mathsf{LeftZD}_R(a)$. Its right component gives a history
$c:\mathsf{BHist}$ with $\mathsf{ApartZero}_R(c)$ and $a\cdot c\hsame0$. Let
$P$ be the displayed proof that $a\cdot b$ is apart from zero.

The same $c$ annihilates $a\cdot b$. Multiplicative associativity gives
$(a\cdot b)\cdot c\hsame a\cdot(b\cdot c)$. Multiplicative commutativity gives
$b\cdot c\hsame c\cdot b$, and multiplication congruence transports this inside
left multiplication by $a$. Symmetry of associativity then identifies
$a\cdot(c\cdot b)$ with $(a\cdot c)\cdot b$.

Apply \autoref{thm:ring-zero-classifier-factor-absorption} to the zero
classifier $a\cdot c\hsame0$ and multiplier $b$. Its left product component
gives $(a\cdot c)\cdot b\hsame0$. Transitivity through the associativity and
commutativity rows yields
$$
(a\cdot b)\cdot c\hsame0.
$$

The strict left zero-divisor data for $a\cdot b$ is therefore the pair
consisting of $P$ and the same annihilator witness $c$ with its apart-zero proof
and the displayed product-zero classifier.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_product\_closed}

\begin{theorem}[CommRing annihilator product left zero-divisor]
\label{thm:commring-annihilator-product-left-zero-divisor}
Under the same hypotheses as
\autoref{thm:commring-zero-divisor-product-closure}, every
$a,b,c:\mathsf{BHist}$ satisfies
$$
\mathsf{ApartZero}_R(c)
\Longrightarrow
\bigl(a\cdot c\hsame0\bigr)
\Longrightarrow
\bigl((a\cdot b)\hsame0\to\bot\bigr)
\Longrightarrow
\mathsf{LeftZD}_R(a\cdot b).
$$
\end{theorem}
\begin{proof}
Assume $\mathsf{ApartZero}_R(c)$, $a\cdot c\hsame0$, and
$P:(a\cdot b)\hsame0\to\bot$. We reuse the displayed $c$ as the annihilator
witness and show that it annihilates $a\cdot b$.

Multiplicative associativity gives
$(a\cdot b)\cdot c\hsame a\cdot(b\cdot c)$. Multiplicative commutativity gives
$b\cdot c\hsame c\cdot b$, and multiplication congruence transports this
comparison inside left multiplication by $a$. Symmetry of associativity then
identifies $a\cdot(c\cdot b)$ with $(a\cdot c)\cdot b$.

As in \autoref{thm:commring-zero-divisor-product-closure}, derive right
distributivity from \autoref{thm:commring-right-distrib-from-left} and use the
zero-classifier factor absorption theorem. Applied to $a\cdot c\hsame0$ and
the multiplier $b$, it yields $(a\cdot c)\cdot b\hsame0$. Transitivity through
the associativity, commutativity, and congruence chain gives
$$
(a\cdot b)\cdot c\hsame0.
$$

The strict left zero-divisor data for $a\cdot b$ is the apart-zero component
$P$ together with the same witness $c$, its apart-zero proof, and the displayed
product-zero classifier.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_annihilator\_product\_closed}
\leanchecked{BEDC.Derived.CommRingUp.CommRingAnnihilatorProduct\_leftZeroDivisor}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_product\_closed\_from\_annihilator}
Ten-field fit: traditional target nonzero multiple of an element with a
nonzero annihilator; BEDC source $\CommRingUp$ over $\mathsf{BHist}$; carrier
$\mathsf{BHist}$; classifier $\hsame$; stability associativity,
commutativity, congruence, and zero absorption; ledger selected annihilator
witness; core theorem \autoref{thm:commring-annihilator-product-left-zero-divisor};
exactness explicit witness preservation; standard bridge ordinary
commutative-ring zero-divisor propagation; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing strict zero-divisor product closure]
\label{thm:commring-strict-zero-divisor-product-closure}
Under the same operation hypotheses as
\autoref{thm:commring-zero-divisor-product-closure}, every
$a,b:\mathsf{BHist}$ satisfies
$$
\mathsf{StrictZD}_R(a)
\Longrightarrow
\bigl((a\cdot b)\hsame0\to\bot\bigr)
\Longrightarrow
\mathsf{StrictZD}_R(a\cdot b).
$$
\end{theorem}
\begin{proof}
Unpack $\mathsf{StrictZD}_R(a)$. The strict witness supplies
$c:\mathsf{BHist}$ together with $\mathsf{ApartZero}_R(c)$,
$a\cdot c\hsame0$, and $c\cdot a\hsame0$. Let $P$ be the given proof that
$a\cdot b$ is apart from zero.

The left annihilation row for the product is the same chain as in
\autoref{thm:commring-zero-divisor-product-closure}. Multiplicative
associativity rewrites $(a\cdot b)\cdot c$ to $a\cdot(b\cdot c)$,
commutativity rewrites $b\cdot c$ to $c\cdot b$, and congruence transports the
comparison under left multiplication by $a$.

Associativity in the other direction identifies $a\cdot(c\cdot b)$ with
$(a\cdot c)\cdot b$. The zero-classifier factor absorption theorem then sends
$a\cdot c\hsame0$ to $(a\cdot c)\cdot b\hsame0$, so transitivity gives
$$
(a\cdot b)\cdot c\hsame0.
$$

For the right annihilation row, apply the $\CommRingUp$ multiplicative
commutativity field to the two histories $(a\cdot b)\cdot c$ and
$c\cdot(a\cdot b)$. Transport the displayed zero classifier across this
comparison to obtain
$$
c\cdot(a\cdot b)\hsame0.
$$

Package $P$, the same apart-zero witness $c$, its apartness proof, and the two
displayed annihilation rows. These are exactly the components of
$\mathsf{StrictZD}_R(a\cdot b)$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_strict\_zero\_divisor\_empty\_product\_closed}

\begin{theorem}[CommRing left zero-divisor product strict closure]
\label{thm:commring-left-zero-divisor-product-strict-closure}
Under the same operation hypotheses as
\autoref{thm:commring-zero-divisor-product-closure}, together with the
multiplicative commutativity row used in
\autoref{thm:commring-left-zero-divisor-strict-of-mul-comm}, every
$a,b:\mathsf{BHist}$ satisfies
$$
\mathsf{LeftZD}_R(a)
\Longrightarrow
\bigl((a\cdot b)\hsame0\to\bot\bigr)
\Longrightarrow
\mathsf{StrictZD}_R(a\cdot b).
$$
\end{theorem}
\begin{proof}
Assume $\mathsf{LeftZD}_R(a)$ and let $P:(a\cdot b)\hsame0\to\bot$ be the
displayed apartness proof for the product. Apply
\autoref{thm:commring-zero-divisor-product-closure} to $a$, $b$, the left
zero-divisor witness for $a$, and $P$. This gives
$\mathsf{LeftZD}_R(a\cdot b)$ with the preserved annihilator.

Next apply \autoref{thm:commring-left-zero-divisor-strict-of-mul-comm} to the
product $a\cdot b$, using the same multiplicative commutativity row. The result
unpacks into the apartness of $a\cdot b$ and a history
$c:\mathsf{BHist}$ with $\mathsf{ApartZero}_R(c)$,
$(a\cdot b)\cdot c\hsame0$, and $c\cdot(a\cdot b)\hsame0$.

These are precisely the fields of $\mathsf{StrictZD}_R(a\cdot b)$. Repack the
apartness component and the two-sided annihilator witness $c$ to obtain the
claimed strict product closure.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.CommRingLeftZeroDivisor\_product\_strict\_closed}
The ten-field fit for
\autoref{thm:commring-left-zero-divisor-product-strict-closure} is:
traditional target nonzero multiple of a left zero-divisor; BEDC source
$\CommRingUp$ over $\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier
$\hsame$; stability from associativity, commutativity, congruence, and zero
absorption; ledger the preserved annihilator and product apartness; core theorem
\autoref{thm:commring-left-zero-divisor-product-strict-closure}; exactness the
two-sided strict witness recovered from the left witness; downstream consumer
the $\FieldUp$ zero-product obstruction and commutative-ring localization
boundary; status $\mathsf{ProofTarget}$.

The ten-field fit for
\autoref{thm:commring-strict-zero-divisor-product-closure} is: traditional
target strict zero-divisor closure in a
commutative ring; BEDC source $\CommRingUp$ over $\mathsf{BHist}$; carrier
$\mathsf{BHist}$; classifier $\hsame$; stability from associativity,
commutativity, congruence, and zero absorption; ledger the apartness witness
plus both annihilator orientations; core theorem
\autoref{thm:commring-strict-zero-divisor-product-closure}; exactness the
two-sided annihilator witness; standard bridge ordinary commutative-ring
zero-divisor closure; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing left zero-divisor two-step product strict closure]
\label{thm:commring-left-zero-divisor-two-step-product-strict-closure}
Under the same operation hypotheses as
\autoref{thm:commring-left-zero-divisor-product-strict-closure}, every
$a,b,c:\mathsf{BHist}$ satisfies
$$
\mathsf{LeftZD}_R(a)
\Longrightarrow
\bigl((a\cdot b)\hsame0\to\bot\bigr)
\Longrightarrow
\bigl(((a\cdot b)\cdot c)\hsame0\to\bot\bigr)
\Longrightarrow
\mathsf{StrictZD}_R((a\cdot b)\cdot c).
$$
\end{theorem}
\begin{proof}
Assume $\mathsf{LeftZD}_R(a)$ and let
$P:(a\cdot b)\hsame0\to\bot$ be the apartness proof for the first product.
Apply \autoref{thm:commring-left-zero-divisor-product-strict-closure} to
$a$, $b$, the left zero-divisor witness for $a$, and $P$. This gives
$\mathsf{StrictZD}_R(a\cdot b)$.

Let $Q:((a\cdot b)\cdot c)\hsame0\to\bot$ be the apartness proof for the
second product. Apply
\autoref{thm:commring-strict-zero-divisor-product-closure} to the strict
zero-divisor $a\cdot b$, the factor $c$, and $Q$.

The resulting package is exactly
$\mathsf{StrictZD}_R((a\cdot b)\cdot c)$: its apartness component is $Q$, and
its two-sided annihilator witness is the one transported by the cited strict
product-closure theorem.
\end{proof}

The ten-field fit for
\autoref{thm:commring-left-zero-divisor-two-step-product-strict-closure} is:
traditional target iterated nonzero multiple of a zero divisor; BEDC source
$\CommRingUp$ over $\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier
$\hsame$; stability from the two cited product-closure theorems; ledger the two
apartness hypotheses and preserved annihilator; core theorem
\autoref{thm:commring-left-zero-divisor-two-step-product-strict-closure};
exactness the strict two-sided witness for the second product; standard bridge
ordinary commutative-ring zero-divisor propagation; status
$\mathsf{ProofTarget}$.
