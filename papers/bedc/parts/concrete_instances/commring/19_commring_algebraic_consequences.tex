The $\CommRingUp$ schema is therefore represented by this concrete
$\mathsf{BHist}$ certificate rather than by abstract operator fields.

\section{The certificate}

\begin{definition}[CommRing source specification]
\label{def:commring-source-specification}
The source is a certified ring carrier plus multiplicative commutativity.
\end{definition}

\begin{definition}[CommRing pattern specification]
\label{def:commring-pattern-specification}
The pattern is ring expression formation where multiplication may be exchanged by the commutativity field.
\end{definition}

\begin{definition}[CommRing classifier specification]
\label{def:commring-classifier-specification}
The classifier is the ring carrier classifier.
\end{definition}

\begin{definition}[CommRing stability certificate]
\label{def:commring-stability-certificate}
The stability certificate consists of:
\begin{enumerate}
  \item all ring stability fields
  \item multiplicative commutativity
  \item compatibility of multiplication commutativity with ring congruence
\end{enumerate}
\end{definition}

\begin{theorem}[CommRing right distributivity from left distributivity]
\label{thm:commring-right-distrib-from-left}
For BEDC history addition and multiplication, multiplicative commutativity,
additive congruence, and left distributivity imply right distributivity:
$$
(x+y)\cdot z \hsame x\cdot z + y\cdot z .
$$
\end{theorem}
\begin{proof}
Commute \((x+y)\cdot z\) to \(z\cdot(x+y)\). Apply left distributivity, then
use additive congruence with multiplicative commutativity on both summands.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_right\_distrib\_from\_left}

\begin{theorem}[CommRing left distributivity from right distributivity]
\label{thm:commring-left-distrib-from-right}
For BEDC history addition and multiplication, multiplicative commutativity,
additive congruence, and right distributivity imply left distributivity:
$$
x\cdot(y+z)\hsame x\cdot y + x\cdot z .
$$
\end{theorem}
\begin{proof}
Commute $x\cdot(y+z)$ to $(y+z)\cdot x$. Apply right distributivity, then
use additive congruence with multiplicative commutativity on both summands.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_left\_distrib\_from\_right}

\begin{theorem}[CommRing one-sided distributivity equivalence]
\label{thm:commring-one-sided-distributivity-equivalence}
For BEDC history operations $add$ and $mul$, multiplicative commutativity and
additive congruence imply that left distributivity is equivalent to right
distributivity:
$$
\left(\forall x\,y\,z : \mathsf{BHist},\ x\cdot(y+z)\hsame x\cdot y+x\cdot z\right)
\leftrightarrow
\left(\forall x\,y\,z : \mathsf{BHist},\ (x+y)\cdot z\hsame x\cdot z+y\cdot z\right).
$$
\end{theorem}
\begin{proof}
For the forward direction, assume the displayed left distributivity law. Applying
Theorem~\ref{thm:commring-right-distrib-from-left} with the shared
multiplicative commutativity and additive congruence fields gives the displayed
right distributivity law for all $x,y,z : \mathsf{BHist}$.

For the reverse direction, assume the displayed right distributivity law.
Applying Theorem~\ref{thm:commring-left-distrib-from-right} with the same
commutativity and congruence fields gives the displayed left distributivity law
for all $x,y,z : \mathsf{BHist}$.
\end{proof}

Per object, the traditional target is one-sided distributivity collapse in
commutative rings; the BEDC source is $\CommRingUp$ over $\mathsf{BHist}$; the
carrier is $\mathsf{BHist}$; the classifier is $\hsame$; the stability
obligations are multiplicative commutativity, additive congruence, and one
chosen distributivity law; the ledger records which side is primitive and which
side is derived; the core theorem list records this equivalence; the exactness
predicate is the displayed iff; the standard bridge is ordinary
commutative-ring distributivity; and the status is $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing square of a sum expansion]
\label{thm:commring-square-add-expand}
For BEDC history addition and multiplication, multiplicative commutativity,
additive congruence, and both distributive laws imply
$$
(a+b)\cdot(a+b)\hsame (a\cdot a+a\cdot b)+(a\cdot b+b\cdot b).
$$
\end{theorem}
\begin{proof}
Apply right distributivity to the outer product. Apply left distributivity to
both exposed factors, then use additive congruence and multiplicative
commutativity to replace \(b\cdot a\) by \(a\cdot b\).
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_square\_add\_expand}

\begin{theorem}[CommRing square of a signed difference]
\label{thm:commring-square-signed-difference-expand}
For BEDC history operations $add$, $mul$, and $neg$ satisfying the $\RingUp$
additive abelian-group laws, addition and multiplication congruence, both
distributive laws, multiplicative commutativity, and the $\RingUp$
negative-product laws, every $a,b : \mathsf{BHist}$ satisfies
$$
(a+(-b))\cdot(a+(-b))\hsame
(a\cdot a+(-(a\cdot b)))+((-(a\cdot b))+(b\cdot b)).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-square-add-expand} with second input $-b$.
This gives the ledger expression
$$
(a\cdot a+a\cdot(-b))+(a\cdot(-b)+(-b)\cdot(-b)).
$$

Use Theorem~\ref{thm:ring-mul-neg-right-eq-neg-mul} on each exposed
$a\cdot(-b)$ term, and use Theorem~\ref{thm:ring-mul-neg-neg-eq-mul} on
$(-b)\cdot(-b)$.

Nested additive congruence transports these three product rewrites through the
two additions. Transitivity of $\hsame$ composes the square expansion with the
transported ledger, yielding the stated signed-difference square form.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_square\_signed\_difference\_expand}

For the $\CommRingUp$ per-object template, this theorem is a Core theorem list
item for traditional target commutative-ring polynomial algebra. Its BEDC source
is $\RingUp$ plus $\CommRingUp$ over $\mathsf{BHist}$, its carrier is
$\mathsf{BHist}$, its classifier is $\hsame$, its stability obligations are
distributivity, congruence, commutativity, and negative-product laws, and its
ledger records polynomial expression provenance with status ProofTarget.

\begin{theorem}[CommRing parallelogram square sum]
\label{thm:commring-parallelogram-square-sum}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
both distributive laws, multiplicative commutativity, and the $\RingUp$
negative-product laws, every $a,b : \mathsf{BHist}$ satisfies
$$
((a+b)\cdot(a+b))+((a+(-b))\cdot(a+(-b)))\hsame
(a\cdot a+a\cdot a)+(b\cdot b+b\cdot b).
$$
\end{theorem}
\begin{proof}
Use \autoref{thm:commring-square-add-expand} for the first square and
\autoref{thm:commring-square-signed-difference-expand} for the signed square.
Addition congruence combines the two normalized ledgers into one sum of
four additive blocks.

Apply additive associativity, additive commutativity, and
\autoref{thm:abgroup-middle-four-interchange} to place the two $a\cdot b$
terms next to the two $-(a\cdot b)$ terms. The middle ledger then contains
the paired mixed terms as inverse pairs.

Collapse those pairs with \autoref{thm:ring-add-right-inverse} and the
additive left-inverse law. The ring zero laws remove the resulting zero
summands, leaving exactly the two $a\cdot a$ terms and the two $b\cdot b$
terms in the displayed order.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_parallelogram\_square\_sum}

For the $\CommRingUp$ per-object template, the traditional target is the
commutative-ring parallelogram square-sum identity; the BEDC source is
$\RingUp$ plus $\CommRingUp$ over $\mathsf{BHist}$; the carrier is
$\mathsf{BHist}$; the classifier is $\hsame$; the stability obligations are
square expansion, signed-square expansion, additive inverse laws, additive
congruence, and middle-four interchange; the ledger records the two square
ledgers and mixed-term cancellation; the core theorem list records this block;
the exactness predicate is the displayed $\hsame$; the standard bridge is the
ordinary parallelogram identity in a commutative ring; and the status is
$\mathsf{ProofTarget}$.

\begin{theorem}[CommRing square cross-term extraction pair]
\label{thm:commring-square-cross-term-extraction-pair}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication congruence,
both distributive laws, multiplicative commutativity, and the $\RingUp$
negative-product laws, every $a,b : \mathsf{BHist}$ satisfies both identities
$$
((a+b)\cdot(a+b))+((-(a\cdot a))+(-(b\cdot b)))\hsame
(a\cdot b)+(a\cdot b)
$$
and
$$
((a+(-b))\cdot(a+(-b)))+((-(a\cdot a))+(-(b\cdot b)))\hsame
(-(a\cdot b))+(-(a\cdot b)).
$$
\end{theorem}
\begin{proof}
For the positive square, apply \autoref{thm:commring-square-add-expand} and
transport the appended $(-(a\cdot a))+(-(b\cdot b))$ ledger through addition
congruence. Additive associativity, additive commutativity, and
\autoref{thm:abgroup-middle-four-interchange} move the $a\cdot a$ term next to
its inverse and the $b\cdot b$ term next to its inverse.

\autoref{thm:ring-add-right-inverse} cancels the two exposed diagonal pairs.
The additive zero laws remove the resulting zero summands, leaving exactly the
two positive mixed terms in the displayed order.

For the signed square, apply
\autoref{thm:commring-square-signed-difference-expand} and use addition
congruence to append the same diagonal inverse ledger. The same associativity,
commutativity, and middle-four interchange rearrangement exposes the
$a\cdot a$ and $b\cdot b$ inverse pairs without changing the two signed mixed
terms.

Apply \autoref{thm:ring-add-right-inverse} again, then use the additive zero
laws to erase the cancelled diagonal ledger. The remaining terms are
$-(a\cdot b)$ and $-(a\cdot b)$, which gives the second displayed identity.
\end{proof}

For the $\CommRingUp$ per-object template, the traditional target is
commutative-ring cross-term extraction for quadratic algebra; the BEDC source is
$\RingUp$ plus $\CommRingUp$ over $\mathsf{BHist}$; the carrier is
$\mathsf{BHist}$; the classifier is $\hsame$; the stability obligations are
square expansion, signed-square expansion, additive congruence, additive
associativity, additive commutativity, diagonal inverse cancellation, additive
zero laws, and middle-four interchange; the ledger records diagonal removal from
the two normalized square ledgers; the core theorem is this block; the exactness
predicate is the two displayed $\hsame$ identities; the downstream use is
$\FieldUp$ quadratic normalization and later polarization-style algebra; and the
status is $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing product of binary sums]
\label{thm:commring-mul-add-add-expand}
For BEDC history addition and multiplication, multiplicative commutativity,
additive congruence, and left distributivity imply the expansion
$$
(a+b)\cdot(c+d)\hsame
(a\cdot c+a\cdot d)+(b\cdot c+b\cdot d).
$$
\end{theorem}
\begin{proof}
First derive right distributivity from multiplicative commutativity and left
distributivity, then apply it to the outer product. Apply left distributivity
to the two inner products $a\cdot(c+d)$ and $b\cdot(c+d)$ and transport both
equalities through additive congruence.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_mul\_add\_add\_expand}

\begin{theorem}[CommRing square of a binary sum]
\label{thm:commring-square-of-sum-duplicate-middle}
For BEDC history addition and multiplication satisfying additive
associativity, additive congruence, multiplicative commutativity, and left
distributivity, every $a,b : \mathsf{BHist}$ satisfies
$$
(a+b)\cdot(a+b)\hsame
(a\cdot a+a\cdot b)+(a\cdot b+b\cdot b).
$$
\end{theorem}
\begin{proof}
Specialize Theorem~\ref{thm:commring-mul-add-add-expand} with $c:=a$ and
$d:=b$. This gives the row-major expansion
$$
(a+b)\cdot(a+b)\hsame
(a\cdot a+a\cdot b)+(b\cdot a+b\cdot b).
$$

Multiplicative commutativity rewrites the exposed middle product
$b\cdot a$ to $a\cdot b$. Additive congruence transports this product rewrite
through the right inner sum, then through the outer sum.

Transitivity of $\hsame$ composes the specialized expansion with the
transported right-hand ledger, yielding the duplicate-middle square form.
\end{proof}

\begin{theorem}[CommRing column expansion of binary sums]
\label{thm:commring-mul-add-add-column-expand}
For BEDC history addition and multiplication, additive associativity,
additive commutativity, additive congruence, multiplicative commutativity,
and left distributivity imply, for every $a,b,c,d : \mathsf{BHist}$,
$$
(a+b)\cdot(c+d)\hsame
(a\cdot c+b\cdot c)+(a\cdot d+b\cdot d).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-mul-add-add-expand} to obtain the row-major
expansion of $(a+b)\cdot(c+d)$ as
$$
(a\cdot c+a\cdot d)+(b\cdot c+b\cdot d).
$$

On the additive operation, apply
Theorem~\ref{thm:abgroup-middle-four-interchange} with the four summands
$a\cdot c$, $a\cdot d$, $b\cdot c$, and $b\cdot d$. Additive congruence then
composes this interchange with the row-major expansion and yields the
column-major normal form.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_mul\_add\_add\_column\_expand}

\begin{theorem}[CommRing left distributivity with commuted terms]
\label{thm:commring-left-distrib-commuted-terms}
For BEDC history addition and multiplication, multiplicative commutativity,
additive congruence, and left distributivity imply
$$
x\cdot(y+z)\hsame y\cdot x + z\cdot x .
$$
\end{theorem}
\begin{proof}
Apply left distributivity to obtain the two summands $x\cdot y$ and
$x\cdot z$. Additive congruence then transports both summands across
multiplicative commutativity.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_left\_distrib\_commuted\_terms}

\begin{theorem}[CommRing difference of squares]
\label{thm:commring-difference-of-squares}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b : \mathsf{BHist}$ satisfies
$$
(a+b)\cdot(a+(-b))\hsame (a\cdot a)+(-(b\cdot b)).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-mul-add-add-expand} to expand
$(a+b)\cdot(a+(-b))$ into the four-term ledger expression
$(a\cdot a+a\cdot(-b))+(b\cdot a+b\cdot(-b))$.

Use Theorem~\ref{thm:ring-mul-neg-right-eq-neg-mul} to rewrite
$a\cdot(-b)$ as $-(a\cdot b)$. Commute $b\cdot a$ to $a\cdot b$ by
multiplicative commutativity, and use the same right-negative product theorem
to rewrite $b\cdot(-b)$ as $-(b\cdot b)$.

The expression is therefore transported by additive congruence to
$(a\cdot a+(-(a\cdot b)))+(a\cdot b+(-(b\cdot b)))$. Additive associativity
and commutativity move the middle pair next to each other without changing the
outer square terms.

Finally, additive inverse collapse gives $(-(a\cdot b))+(a\cdot b)\hsame0$.
The zero laws remove this neutral summand, leaving
$(a\cdot a)+(-(b\cdot b))$ under $\hsame$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_difference\_of\_squares}

\begin{theorem}[CommRing equal squares force signed factor zero]
\label{thm:commring-equal-squares-signed-factor-zero}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b : \mathsf{BHist}$ satisfies
$$
a\cdot a\hsame b\cdot b
\Longrightarrow
(a+b)\cdot(a+(-b))\hsame0 .
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-difference-of-squares}
to rewrite the signed factor product as $(a\cdot a)+(-(b\cdot b))$. Transport
the assumed square equality $a\cdot a\hsame b\cdot b$ through additive
congruence while keeping the right summand $-(b\cdot b)$ fixed. The right side
is therefore identified with $(b\cdot b)+(-(b\cdot b))$.

Theorem~\ref{thm:ring-add-right-inverse} collapses this final additive inverse
pair to $0$. Transitivity of $\hsame$ composes the difference-of-squares
normal form, the transported square equality, and the inverse collapse.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_equal\_squares\_signed\_factor\_zero}

\begin{theorem}[CommRing difference-of-squares zero exactness]
\label{thm:commring-difference-of-squares-zero-exact}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b : \mathsf{BHist}$ satisfies
$$
(a+b)\cdot(a+(-b))\hsame0
\Longleftrightarrow
a\cdot a\hsame b\cdot b .
$$
\end{theorem}
\begin{proof}
For the forward direction, apply Theorem~\ref{thm:commring-difference-of-squares}
to identify the signed product with $(a\cdot a)+(-(b\cdot b))$. Compose this
identification, using symmetry where needed, with the zero-product hypothesis.
The difference term is therefore classified as $0$.

Add $b\cdot b$ on the right by additive congruence. On the left endpoint,
additive associativity and commutativity move the inverse pair into the form
$(b\cdot b)+(-(b\cdot b))$; Theorem~\ref{thm:ring-add-right-inverse}
collapses that pair to $0$, and the zero laws leave $a\cdot a$. On the right
endpoint, the zero law collapses $0+(b\cdot b)$ to $b\cdot b$. Transitivity
gives $a\cdot a\hsame b\cdot b$.

For the reverse direction, apply
Theorem~\ref{thm:commring-equal-squares-signed-factor-zero} to the square
equality $a\cdot a\hsame b\cdot b$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_difference\_of\_squares\_zero\_exact}

\begin{theorem}[CommRing signed binary product normal form]
\label{thm:commring-signed-binary-product-normal-form}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ satisfies
$$
(a+(-b))\cdot(c+(-d))\hsame
(a\cdot c+(-(a\cdot d)))+((-(b\cdot c))+(b\cdot d)).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-mul-add-add-expand} with the four inputs
$a$, $-b$, $c$, and $-d$. The left side expands to
$$
(a\cdot c+a\cdot(-d))+((-b)\cdot c+(-b)\cdot(-d)).
$$

Next normalize the signed products. Theorem~\ref{thm:ring-mul-neg-right-eq-neg-mul}
rewrites $a\cdot(-d)$ as $-(a\cdot d)$,
Theorem~\ref{thm:ring-mul-neg-left-eq-neg-mul} rewrites $(-b)\cdot c$ as
$-(b\cdot c)$, and Theorem~\ref{thm:ring-mul-neg-neg-eq-mul} rewrites
$(-b)\cdot(-d)$ as $b\cdot d$.

Use nested additive congruence to transport the three rewritten summands
through the outer two additions. Transitivity of $\hsame$ composes the
expansion step with this transport, giving the stated four-term normal form.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_binary\_product\_normal\_form}

\begin{theorem}[CommRing signed product balance]
\label{thm:commring-signed-product-balance}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ satisfies
$$
((a+(-b))\cdot(c+(-d)))+((a\cdot d)+(b\cdot c))
\hsame
(a\cdot c)+(b\cdot d).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-signed-binary-product-normal-form} to replace
the signed product by the four-term row expansion
$$
(a\cdot c+(-(a\cdot d)))+((-(b\cdot c))+(b\cdot d)).
$$

Transport the added mixed-term sum through this identification by additive
congruence. The expression is then an additive composite containing the two
ordinary mixed terms $a\cdot d$ and $b\cdot c$ together with their signed
counterparts $-(a\cdot d)$ and $-(b\cdot c)$.

Use additive associativity and commutativity, with
Theorem~\ref{thm:abgroup-middle-four-interchange} for the middle summands, to
place $-(a\cdot d)$ adjacent to $a\cdot d$ and $-(b\cdot c)$ adjacent to
$b\cdot c$. The unmixed terms $a\cdot c$ and $b\cdot d$ remain as the
external summands.

Theorem~\ref{thm:ring-add-right-inverse} collapses the right-inverse mixed
pair, and the additive left-inverse law collapses the left-inverse mixed
pair. The ring zero laws remove the two neutral summands, leaving
$(a\cdot c)+(b\cdot d)$ under $\hsame$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_product\_balance}

The ten-field fit is: traditional target bilinear subtraction balance; BEDC
source $\RingUp$ plus $\CommRingUp$ over $\mathsf{BHist}$; carrier
$\mathsf{BHist}$; classifier $\hsame$; stability obligations distributivity,
congruence, commutativity, negative-product laws, and additive cancellation;
ledger polynomial mixed-term provenance; core theorem this balance identity;
exactness the displayed $\hsame$; standard bridge commutative-ring bilinear
expansion; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing signed column expansion of binary sums]
\label{thm:commring-signed-column-binary-product-normal-form}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ satisfies
$$
(a+(-b))\cdot(c+(-d))\hsame
(a\cdot c+(-(b\cdot c)))+((-(a\cdot d))+(b\cdot d)).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-signed-binary-product-normal-form} to obtain
the row-major signed expansion
$$
(a\cdot c+(-(a\cdot d)))+((-(b\cdot c))+(b\cdot d)).
$$

On the additive operation, apply
Theorem~\ref{thm:abgroup-middle-four-interchange} with the four summands
$a\cdot c$, $-(a\cdot d)$, $-(b\cdot c)$, and $b\cdot d$. Additive congruence
transports the middle-four interchange through the displayed signed row form.

Transitivity of $\hsame$ composes the row-major normalization with this
column reordering, yielding the stated column-major signed normal form.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_column\_binary\_product\_normal\_form}

\begin{theorem}[CommRing signed column cancellations force zero product]
\label{thm:commring-signed-column-cancellations-zero}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ with
$a\cdot c\hsame b\cdot c$ and $a\cdot d\hsame b\cdot d$ satisfies
$$
(a+(-b))\cdot(c+(-d))\hsame0.
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-signed-column-binary-product-normal-form} to
rewrite the signed product as
$$
(a\cdot c+(-(b\cdot c)))+((-(a\cdot d))+(b\cdot d)).
$$

The first hypothesis is transported by additive congruence through the left
signed column. It identifies $a\cdot c+(-(b\cdot c))$ with
$(b\cdot c)+(-(b\cdot c))$, and Theorem~\ref{thm:ring-add-right-inverse}
collapses this right-inverse pair to $0$.

For the second signed column, apply additive inverse congruence to
$a\cdot d\hsame b\cdot d$ and identify $-(a\cdot d)$ with $-(b\cdot d)$.
Additive congruence transports $(-(a\cdot d))+(b\cdot d)$ to
$(-(b\cdot d))+(b\cdot d)$, and the additive left-inverse law collapses this
left-inverse pair to $0$.

The column normal form is therefore transported to $0+0$. The additive
left-zero law collapses $0+0$ to $0$, and transitivity of $\hsame$ composes
the normal-form step, the two signed-column collapses, and the final zero
collapse.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_column\_cancellations\_zero}

For the $\CommRingUp$ per-object template, the traditional target is signed
column cancellation in commutative-ring algebra. Its BEDC source is $\RingUp$
plus $\CommRingUp$ over $\mathsf{BHist}$, its carrier is $\mathsf{BHist}$, its
classifier is $\hsame$, its stability obligations are signed-product
normalization, additive inverse laws, additive congruence, and transitivity, and
its ledger records column-paired product provenance. The core theorem list
contains Theorem~\ref{thm:commring-signed-column-cancellations-zero}; exactness
is zero classification under column-paired cancellations; the standard bridge
remains outside this proof site; and the status is $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing signed row cancellations force zero product]
\label{thm:commring-signed-row-cancellations-zero}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ with
$a\cdot c\hsame a\cdot d$ and $b\cdot c\hsame b\cdot d$ satisfies
$$
(a+(-b))\cdot(c+(-d))\hsame0.
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-signed-binary-product-normal-form} to rewrite
the signed product as
$$
(a\cdot c+(-(a\cdot d)))+((-(b\cdot c))+(b\cdot d)).
$$

The first row equality is transported by additive congruence through the left
signed row. It identifies $a\cdot c+(-(a\cdot d))$ with
$(a\cdot d)+(-(a\cdot d))$, and Theorem~\ref{thm:ring-add-right-inverse}
collapses this right-inverse pair to $0$.

For the second signed row, apply additive inverse congruence to
$b\cdot c\hsame b\cdot d$ and identify $-(b\cdot c)$ with $-(b\cdot d)$.
Additive congruence transports $(-(b\cdot c))+(b\cdot d)$ to
$(-(b\cdot d))+(b\cdot d)$, and the additive left-inverse law collapses this
left-inverse pair to $0$.

The row normal form is therefore transported to $0+0$. The additive left-zero
law collapses $0+0$ to $0$, and transitivity of $\hsame$ composes the
normal-form step, the two signed-row collapses, and the final zero collapse.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_row\_cancellations\_zero}

The ten-field fit is: traditional target signed row cancellation in
commutative-ring algebra; BEDC source $\RingUp$ plus $\CommRingUp$ over
$\mathsf{BHist}$; carrier $\mathsf{BHist}$; classifier $\hsame$; stability
additive abelian-group laws, congruence, distributivity, commutativity, and
negative-product laws; ledger signed row product provenance; core theorem
this row-cancellation result; exactness row-zero soundness; standard bridge
the ordinary commutative-ring identity; status $\mathsf{ProofTarget}$.

\begin{theorem}[CommRing signed product zero exactness]
\label{thm:commring-signed-product-zero-exact}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ satisfies
$$
(a+(-b))\cdot(c+(-d))\hsame0
\Longleftrightarrow
a\cdot c+b\cdot d\hsame a\cdot d+b\cdot c .
$$
\end{theorem}
\begin{proof}
For the forward direction, apply
Theorem~\ref{thm:commring-signed-binary-product-normal-form} to rewrite the
signed product as
$$
(a\cdot c+(-(a\cdot d)))+((-(b\cdot c))+(b\cdot d)).
$$

Apply Theorem~\ref{thm:abgroup-middle-four-interchange} to regroup the
four-term additive ledger as
$(a\cdot c+b\cdot d)+((-(a\cdot d))+(-(b\cdot c)))$. The negative-product
laws and additive inverse congruence identify the second summand with the
additive inverse of $(a\cdot d+b\cdot c)$. The signed product hypothesis
therefore presents a balanced difference between the two cross sums.

Add $a\cdot d+b\cdot c$ to both sides by additive congruence. Additive
associativity moves the inverse pair next to its positive partner, and
Theorem~\ref{thm:ring-add-right-inverse} collapses the pair to $0$. The zero
laws remove the neutral summand and leave
$a\cdot c+b\cdot d\hsame a\cdot d+b\cdot c$.

For the reverse direction, start from the assumed cross-sum equality and
transport it through additive congruence while keeping the inverse of the
right cross sum fixed. The right endpoint becomes the sum of
$a\cdot d+b\cdot c$ and its additive inverse.

Theorem~\ref{thm:ring-add-right-inverse} collapses that endpoint to $0$.
Symmetry and transitivity compose this collapse with the signed binary
normal form, yielding
$ (a+(-b))\cdot(c+(-d))\hsame0 $.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_product\_empty\_zero\_exact}

\begin{theorem}[CommRing signed square zero exactness]
\label{thm:commring-signed-square-zero-exact}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b : \mathsf{BHist}$ satisfies
$$
(a+(-b))\cdot(a+(-b))\hsame0
\Longleftrightarrow
a\cdot a+b\cdot b\hsame a\cdot b+a\cdot b .
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-signed-product-zero-exact} with $c=a$ and
$d=b$. The signed product side becomes the signed square, and the cross-sum
criterion becomes
$$
a\cdot a+b\cdot b\hsame a\cdot b+b\cdot a .
$$

For the forward direction, multiplicative commutativity gives
$b\cdot a\hsame a\cdot b$. Additive congruence transports the right summand
of $a\cdot b+b\cdot a$ along this equality, so the cross sum obtained from
the specialized exactness theorem is identified with
$a\cdot b+a\cdot b$.

For the reverse direction, use symmetry of the same commutativity instance
and additive congruence to transport $a\cdot b+a\cdot b$ back to
$a\cdot b+b\cdot a$. The reverse implication of the specialized
signed-product exactness theorem then returns
$(a+(-b))\cdot(a+(-b))\hsame0$.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_square\_empty\_zero\_exact}
\leanchecked{BEDC.Derived.CommRingUp.commring\_signed\_square\_zero\_exact}

\begin{theorem}[CommRing column cancellations balance cross sums]
\label{thm:commring-column-cancellations-cross-sum-balance}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ with
$a\cdot c\hsame b\cdot c$ and $a\cdot d\hsame b\cdot d$ satisfies
$$
a\cdot c+b\cdot d\hsame a\cdot d+b\cdot c .
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-signed-column-cancellations-zero} to the two
column hypotheses. This produces the signed zero product
$$
(a+(-b))\cdot(c+(-d))\hsame0.
$$

Apply the forward direction of
Theorem~\ref{thm:commring-signed-product-zero-exact}. The signed zero product
is exactly the left side of that exactness statement, so its conclusion is the
displayed cross-sum balance.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_column\_cancellations\_cross\_sum\_balance}

\begin{theorem}[CommRing row cancellations balance cross sums]
\label{thm:commring-row-cancellations-cross-sum-balance}
For BEDC history operations $add$, $mul$, $neg$, and $zero$ satisfying the
$\RingUp$ additive abelian-group laws, addition and multiplication
congruence, left distributivity, multiplicative commutativity, and the
$\RingUp$ negative-product laws, every $a,b,c,d : \mathsf{BHist}$ with
$a\cdot c\hsame a\cdot d$ and $b\cdot c\hsame b\cdot d$ satisfies
$$
a\cdot c+b\cdot d\hsame a\cdot d+b\cdot c .
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:commring-signed-row-cancellations-zero} to the two
row hypotheses. This produces the signed zero product
$$
(a+(-b))\cdot(c+(-d))\hsame0.
$$

Apply the forward direction of
Theorem~\ref{thm:commring-signed-product-zero-exact}. The signed zero product
is exactly the left side of that exactness statement, so its conclusion is the
displayed cross-sum balance.
\end{proof}
\leanchecked{BEDC.Derived.CommRingUp.commring\_row\_cancellations\_cross\_sum\_balance}

For the $\CommRingUp$ per-object template, the traditional target is row
cancellation balance in commutative-ring algebra. Its BEDC source is
$\RingUp$ plus $\CommRingUp$ over $\mathsf{BHist}$, its carrier is
$\mathsf{BHist}$, its classifier is $\hsame$, its stability obligations are
signed row cancellation and signed-product zero exactness, and its ledger
records row-paired product provenance. The core theorem is
Theorem~\ref{thm:commring-row-cancellations-cross-sum-balance}; exactness is
cross-sum balance; the standard bridge is ordinary commutative-ring
cancellation balance; and the status is $\mathsf{ProofTarget}$.

\begin{definition}[CommRing ledger policy]
\label{def:commring-ledger-policy}
Record ring expression provenance and the multiplicative commutativity law when used.
\end{definition}

\begin{definition}[\(\CommRingUp\) certificate obligations]
\label{def:commring-certificate-obligations}
The carrier, source, pattern, classifier, stability, and ledger data listed above are the obligations required for the interface name \(\CommRingUp\).
\end{definition}

\paragraph{Primitive boundary.}
\(\CommRingUp\) is not a BEDC primitive. It is admitted only when those obligations are supplied.
