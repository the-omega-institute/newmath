\chapter{A Concrete Naming Certificate for $\CompactUniformCoverRefinementUp$}
\label{ch:concrete-instances-compact-uniform-cover-refinement-namecert}
\origin{human}

Following Bishop and Bridges constructive metric compactness, the $\CompactUniformCoverRefinementUp$ packet records the finite uniform-cover refinement route used by compact-uniform-continuity arguments. It sits over the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the metric-space rows of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite ProbeBundle cover surface of \autoref{ch:concrete-instances-probe-bundle-cover-fold-namecert}, the rational lower-bound rows of \autoref{ch:concrete-instances-rat-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names finite refinement evidence for a compact cover; it is not open-cover compactness as an ambient principle, a selected Lebesgue number, countable choice, quotient equality, or ambient $\RealUp$ completeness.

\begin{definition}[Compact uniform-cover refinement carrier]
\label{def:compact-uniform-cover-refinement-carrier}
A $\CompactUniformCoverRefinementUp$ carrier is a finite $\BHist$ packet
$$
U=(K,M,F,B,R,T,V,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ finite compact source row.
\item $M$ is the $\MetricSpaceUp$ row containing the source and target triangle comparisons used by the refinement.
\item $F$ is the $\ContinuousMapUp$ row carrying pointwise modulus data over the displayed finite cover.
\item $B$ is the $\ProbeBundleUp$ finite-cover row listing centers, cover membership, and local radius evidence.
\item $R$ is the $\RatUp$ lower-bound fold row for the displayed cover radii.
\item $T$ is the metric-triangle row that compares source points through the selected cover member and its local modulus.
\item $V$ is the exported $\UniformModulusUp$ row consumed after the refinement and rational fold.
\item $H$ records componentwise $\hsame$ transport for compact, metric, continuous-map, ProbeBundle, rational-fold, triangle, and uniform-modulus rows.
\item $C$ records displayed $\Cont$ replay from finite cover membership through pointwise moduli, rational folds, and triangle estimates to the uniform-modulus export.
\item $P$ records $\Pkg$ provenance over $\CompactUniformCoverRefinementUp$, $\CompactUp$, $\MetricSpaceUp$, $\ContinuousMapUp$, $\ProbeBundleUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformCoverRefinementUp}$ row.
\end{enumerate}
The carrier has no coordinate for an external open-cover compactness theorem, selected Lebesgue number, countable choice, quotient equality, or ambient completeness.
\end{definition}

\begin{theorem}[Compact uniform-cover refinement NameCert obligations]
\label{thm:compact-uniform-cover-refinement-namecert-obligations}
For every accepted $\CompactUniformCoverRefinementUp$ carrier $U=(K,M,F,B,R,T,V,H,C,P,N)$, the local $\NameCert_{\CompactUniformCoverRefinementUp}$ surface consists exactly of compact source exposure, metric source-and-target exposure, continuous-map local-modulus exposure, finite ProbeBundle cover exposure, rational lower-bound folding, metric-triangle handoff, uniform-modulus export, componentwise transport, displayed replay, provenance, and local naming. Every compact-uniform consumer reads the route
$$
\CompactUp,\quad \MetricSpaceUp,\quad \ContinuousMapUp,\quad \ProbeBundleUp,\quad \RatUp,\quad \UniformModulusUp,\quad \BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert
$$
through those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-cover-refinement-carrier}. The rows $K,M,F,B,R,T,V,H,C,P,N$ are the full visible inventory.

The compact source row $K$ and the ProbeBundle row $B$ expose the finite cover before any uniform modulus is public. The continuous-map row $F$ supplies local moduli on the displayed cover members, while the rational row $R$ folds their radii into a visible positive lower bound.

The metric row $M$ and triangle row $T$ then transport the local estimates to arbitrary source points covered by $B$. The exported row $V$ is downstream of the cover membership, local-modulus, rational-fold, and triangle data.

Finally $H,C,P,N$ preserve and name the same finite route. Since the carrier contains no row for external open-cover compactness, selected Lebesgue numbers, countable choice, quotient equality, or ambient completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Compact uniform-cover refinement uniform-modulus export]
\label{thm:compact-uniform-cover-refinement-uniform-modulus-export}
Every $\UniformModulusUp$ read exported by an accepted $\CompactUniformCoverRefinementUp$ packet factors through finite ProbeBundle cover membership, a $\RatUp$ radius fold, $\ContinuousMapUp$ local moduli, $\MetricSpaceUp$ triangle rows, and the compact source row $K$ before the row $V$ is consumed. The export contains no selected global modulus, open-cover compactness oracle, quotient equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:compact-uniform-cover-refinement-namecert-obligations}. They order the route so that the compact source, metric rows, continuous-map rows, ProbeBundle cover data, rational fold, and triangle handoff all precede $V$.

The ProbeBundle row gives the finite cover member used for the local estimate, and the rational fold row supplies the shared radius bound. The metric triangle row then transfers the local estimate to the requested source pair.

Thus the exported uniform modulus is a finite packet read rather than a selected object. The excluded principles cannot appear because there is no coordinate for them in the carrier.
\end{proof}

\begin{theorem}[Compact uniform-cover refinement vision route]
\label{thm:compact-uniform-cover-refinement-vision-route}
The finite uniform-cover refinement branch of \autoref{ch:visions-compact-uniform-continuity} is realized by an accepted $\CompactUniformCoverRefinementUp$ packet as ProbeBundle cover rows, $\RatUp$ radius folds, $\ContinuousMapUp$ local-modulus rows, $\MetricSpaceUp$ triangle rows, and $\UniformModulusUp$ export rows. The route is finite NameCert data, not a detached Heine--Cantor theorem or an ambient compactness principle.
\end{theorem}
\begin{proof}
Read the carrier rows $B,R,F,M,T,V$. They are exactly the finite cover, lower-bound fold, local-modulus, triangle-estimate, and uniform-modulus export rows named by the compact-uniform-continuity route.

The compact row $K$ supplies the source surface, while $H,C,P,N$ transport, replay, source, and name the same finite packet. Therefore the vision branch is represented as one local NameCert carrier rather than as an external compactness or selection theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformCoverRefinementUp$ compact-uniform consumer reads Compact, MetricSpace, ContinuousMap, ProbeBundle finite-cover, Rat radius-fold, metric-triangle, UniformModulus, hsame, Cont, Pkg, and local NameCert rows before using the exported uniform modulus.}

\independenceWitness{compact, metricspace, continuousmap, probebundle, rat, uniformmodulus: no listed sibling carries the full packet $U=(K,M,F,B,R,T,V,H,C,P,N)$, because the siblings separately supply compact source data, metric comparison, local continuity, finite cover membership, rational folding, and uniform-modulus export while this packet binds them into one refinement route.}

\closureat{CompactUniformCoverRefinementUp}{seedStr}

\begin{closurestatus}{\CompactUniformCoverRefinementUp}
  \constructivestory{A $\CompactUniformCoverRefinementUp$ packet is generated from Compact source rows, MetricSpace triangle rows, ContinuousMap local-modulus rows, ProbeBundle finite-cover rows, Rat radius-fold rows, UniformModulus export rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-cover-refinement-carrier}, \autoref{thm:compact-uniform-cover-refinement-namecert-obligations}, \autoref{thm:compact-uniform-cover-refinement-uniform-modulus-export}, and \autoref{thm:compact-uniform-cover-refinement-vision-route} seed the finite compact uniform-cover refinement route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a full Heine--Cantor theorem, open-cover compactness, selected Lebesgue numbers, countable choice, quotient equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact source exposure, ProbeBundle cover exactness, rational fold exactness, continuous-map local-modulus exposure, metric-triangle handoff, UniformModulus export, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformCoverRefinementUp.}
\end{closurestatus}
