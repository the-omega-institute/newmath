\chapter{A Concrete Naming Certificate for $\FiniteDimensionalBanachUp$}
\label{ch:concrete-instances-finite-dimensional-banach-namecert}
\origin{human}

Following Banach-space and finite-dimensional normed-space treatments, the $\FiniteDimensionalBanachUp$ packet records the coordinate route by which a displayed finite-dimensional normed vector space is consumed as complete. It sits after the Banach certificate of \autoref{ch:concrete-instances-banach-namecert}, the vector-space carrier of \autoref{ch:concrete-instances-vecspace-namecert}, the norm interface of \autoref{ch:concrete-instances-norm-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational sequence surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite norm-comparison route of \autoref{ch:concrete-instances-finite-dimensional-norm-equivalence-namecert}, and the finite-net compactness packet of \autoref{ch:concrete-instances-finite-dimensional-compactness-namecert}. The packet names finite coordinate completeness as displayed NameCert data; it is not general Hahn--Banach separation, arbitrary open-cover compactness, quotient vector equality, or a selected completion carrier.

\begin{definition}[FiniteDimensionalBanach carrier]
\label{def:finite-dimensional-banach-carrier}
A $\FiniteDimensionalBanachUp$ carrier is a finite $\BHist$ packet
$$
F=(V,N,R,S,E,K,L,W,H,C,P,Q)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the finite $\VecSpaceUp$ row, including the displayed coordinate-window dimension, basis-window ledger, zero, addition, negation, scalar source, and scalar action.
\item $N$ is the $\NormUp$ row attached to $V$, naming the norm read used by every Cauchy and limit comparison.
\item $R$ is the $\RealUp$ scalar seal row through which norm values, comparison constants, and error budgets are read.
\item $S$ is the $\RegSeqRatUp$ row recording the regular rational error schedule for the inspected Cauchy sequence.
\item $E$ is the $\FiniteDimensionalNormEquivalenceUp$ row tying the norm comparison to the same finite coordinate window.
\item $K$ is the coordinate-Cauchy ledger, listing the finitely many coordinate sequences extracted from the norm-Cauchy read.
\item $L$ is the coordinate-limit ledger, recording the regular rational names that serve as limits for the displayed coordinate sequences.
\item $W$ is the reconstructed limit-window row, repacking the coordinate limits as a vector row over $V$ before any Banach-facing read is exported.
\item $H$ stores componentwise $\hsame$ transport for $V,N,R,S,E,K,L,W$ and the local naming row.
\item $C$ stores displayed $\Cont$ replay from a finite-dimensional completeness request through the coordinate extraction, coordinate limits, and reconstructed limit window.
\item $P$ is the $\Pkg$ provenance over $\FiniteDimensionalBanachUp$, $\BanachUp$, $\VecSpaceUp$, $\NormUp$, $\RealUp$, $\RegSeqRatUp$, $\FiniteDimensionalNormEquivalenceUp$, $\FiniteDimensionalCompactnessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $Q$ is the local $\NameCert_{\FiniteDimensionalBanachUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for a host completion functor, quotient vector equality, unrestricted Hahn--Banach separation, arbitrary open-cover compactness, selected bases outside the packet, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[FiniteDimensionalBanach NameCert obligations]
\label{thm:finite-dimensional-banach-namecert-obligations}
For every accepted $\FiniteDimensionalBanachUp$ carrier $F=(V,N,R,S,E,K,L,W,H,C,P,Q)$, the local $\NameCert_{\FiniteDimensionalBanachUp}$ surface consists exactly of carrier admission for the displayed vector-space, norm, real-seal, regular-rational error, finite norm-equivalence, coordinate-Cauchy, coordinate-limit, reconstructed limit-window, transport, replay, provenance, and local naming rows. A completeness-facing read is admitted only after the coordinate Cauchy ledger $K$, coordinate-limit ledger $L$, and reconstructed limit window $W$ are visible in the same finite packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-dimensional-banach-carrier}. The vector, norm, scalar, and regular-rational source rows are exactly $V,N,R,S$.

Read the finite-dimensional comparison row next. The row $E$ keeps norm comparisons on the displayed coordinate window, so Cauchy control can be transferred only through named finite coordinates.

The completeness data is then $K,L,W$. The coordinate-Cauchy ledger $K$ exposes finitely many coordinate sequences, $L$ records their regular rational limits, and $W$ repacks those limits as the vector read consumed by Banach-facing users.

Finally $H,C,P,Q$ preserve the same packet under componentwise transport, displayed replay, provenance, and local naming. Since the carrier has no row for a host completion functor, quotient vector equality, unrestricted separation, arbitrary compactness, selected external bases, countable choice, or ambient real completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[FiniteDimensionalBanach coordinate completeness handoff]
\label{thm:finite-dimensional-banach-coordinate-completeness-handoff}
Every Banach-facing read of an accepted $\FiniteDimensionalBanachUp$ carrier factors through the finite coordinate route
$$
\VecSpaceUp,\quad \NormUp,\quad \RealUp,\quad \RegSeqRatUp,\quad \FiniteDimensionalNormEquivalenceUp,\quad \FiniteDimensionalCompactnessUp .
$$
The route first displays the finite vector and norm rows, then reads the regular rational Cauchy budget, transfers it to finitely many coordinate sequences, records their coordinate limits, and repacks the resulting vector through the reconstructed limit window. It exports finite-coordinate completeness only for the named packet and does not export general Hahn--Banach, choice-based compactness, quotient completion, or ambient real completeness.
\end{theorem}
\begin{proof}
Start from the obligation surface of \autoref{thm:finite-dimensional-banach-namecert-obligations}. A Banach-facing consumer must open $V,N,R,S$ before any Cauchy or limit read is available.

Next use the finite norm-comparison row $E$. It is the only route from the norm-Cauchy budget to coordinate Cauchy data, so the handoff remains tied to the displayed finite coordinate window.

The coordinate ledgers $K$ and $L$ supply the finite limit data, and $W$ repacks those limits as the vector-window read. The compactness-facing sibling of \autoref{ch:concrete-instances-finite-dimensional-compactness-namecert} may supply bounded-window consumers, but no compactness or choice principle is added to this packet.

The structural rows $H,C,P,Q$ replay and name the same route for downstream Banach consumers. Therefore the exported completeness claim is finite-coordinate completeness over the listed rows, not a host completion theorem, a quotient construction, general Hahn--Banach separation, arbitrary open-cover compactness, or ambient $\RealUp$ completeness.
\end{proof}

\closureat{FiniteDimensionalBanachUp}{seedStr}

\begin{closurestatus}{\FiniteDimensionalBanachUp}
  \constructivestory{A $\FiniteDimensionalBanachUp$ packet is generated from a displayed VecSpaceUp row, NormUp row, RealUp scalar seal, RegSeqRat error schedule, finite-dimensional norm-equivalence row, coordinate-Cauchy ledger, coordinate-limit ledger, reconstructed limit-window row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-dimensional-banach-carrier}, \autoref{thm:finite-dimensional-banach-namecert-obligations}, and \autoref{thm:finite-dimensional-banach-coordinate-completeness-handoff} seed the finite-coordinate completeness route over displayed vector, norm, real, regular-rational, coordinate-limit, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close general Hahn--Banach, arbitrary open-cover compactness, quotient vector equality, selected external bases, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite vector and norm exposure, coordinate Cauchy extraction, coordinate-limit readback, reconstructed limit-window exactness, transport stability, replay stability, provenance, and local naming for BEDC.Derived.FiniteDimensionalBanachUp.}
\end{closurestatus}
