\chapter{A Concrete Naming Certificate for $\BertrandTestUp$}
\label{ch:concrete-instances-bertrand-test-namecert}
\origin{human}

Following Bertrand and Hardy's classical treatment of logarithmic refinements of ratio-type convergence tests, the $\BertrandTestUp$ packet records the finite positive-series tail data by which a logarithmic first-order defect is routed into the existing BEDC convergence-test lattice. It sits beside the Raabe--Duhamel surface of \autoref{ch:concrete-instances-raabe-duhamel-test-namecert}, the auxiliary multiplier route of \autoref{ch:concrete-instances-kummer-test-namecert}, the Gauss quotient-defect surface of \autoref{ch:concrete-instances-gauss-test-namecert}, the ratio-test corridor of \autoref{ch:concrete-instances-real-series-ratio-test-namecert}, and the regular-rational and Real endpoint seals of \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}. The packet names only inspected source windows, adjacent quotient windows, logarithmic tail ledgers, dyadic comparison budgets, regular rational readback, Real sealing, transport, replay, provenance, and local naming; it is not host summability, quotient series equality, a selected sum, countable choice, trichotomy, ambient $\RealUp$ completeness, or bridge checking.

\begin{definition}[Bertrand test carrier]
\label{def:bertrand-test-carrier}
A $\BertrandTestUp$ carrier is a finite $\BHist$ packet
$$
B=(S,Q,L,D,T,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source positive-series tail row, exposing only the finite term and partial-sum windows inspected by the test.
\item $Q$ is the adjacent quotient-window row, recording the displayed neighboring-term ratio data.
\item $L$ is the Bertrand logarithmic tail ledger, storing the inspected index window, logarithmic scale row, rational margin, and first-order defect comparison.
\item $D$ is the $\DyadicRatCoreUp$ comparison-budget row used to route the logarithmic defect into a finite domination statement.
\item $T$ is the Raabe--Duhamel tail handoff row, consumed only after $S,Q,L,D$ have been displayed.
\item $W$ is the $\StreamNameUp$ finite-window row for the source, quotient, logarithmic, domination, and handoff rows.
\item $R$ is the $\RegSeqRatUp$ readback row reached only after the tail handoff and stream windows have been replayed.
\item $E$ is the terminal $\RealUp$ endpoint seal.
\item $H$ stores componentwise $\hsame$ transport for the source, quotient, logarithmic, domination, handoff, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from each tail request through $Q,L,D,T,W,R,E$.
\item $P$ is the $\Pkg$ provenance over $\BertrandTestUp$, $\RaabeDuhamelTestUp$, $\KummerTestUp$, $\GaussTestUp$, $\RealSeriesRatioTestUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BertrandTestUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,Q,L,D,T,W,R,E,H,C,P,N$. It has no coordinate for host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}
\leanstmt{BEDC.Derived.BertrandTestUp}

\begin{theorem}[Bertrand test NameCert obligations]
\label{thm:bertrand-test-namecert-obligations}
For every accepted $\BertrandTestUp$ carrier
$$
B=(S,Q,L,D,T,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\BertrandTestUp}$ surface consists exactly of carrier admission for the displayed rows; quotient-window exactness over $S,Q$; logarithmic-tail stability over $L$; dyadic comparison routing through $D$; Raabe--Duhamel tail handoff through $T$; regular rational and Real endpoint routing through $W,R,E$; and transport, replay, provenance, and ledger non-escape through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bertrand-test-carrier}. Carrier admission is exactly the displayed row inventory $S,Q,L,D,T,W,R,E,H,C,P,N$, with no detached host sum, quotient-series equality, selector, choice, trichotomy, completeness, Lean-verification, or bridge row.

Read the analytic evidence before the handoff. The source row $S$ and quotient row $Q$ expose only finite adjacent-tail windows, while the logarithmic ledger $L$ records the inspected scale and defect margin attached to the same tail request.

The comparison and endpoint rows are then forced in order. The dyadic budget $D$ turns the displayed logarithmic defect into finite domination evidence, the tail handoff $T$ routes that evidence to the Raabe--Duhamel surface, and $W,R,E$ expose only stream windows, regular rational readback, and the terminal Real seal.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier contains no further coordinate, the listed host and quotient principles remain outside the local NameCert surface.
\end{proof}

\begin{theorem}[Bertrand test Raabe tail handoff]
\label{thm:bertrand-test-raabe-tail-handoff}
Every endpoint-facing read of an accepted $\BertrandTestUp$ carrier factors through the finite tail route
$$
\BertrandTestUp \longrightarrow \RaabeDuhamelTestUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp .
$$
Concretely, the read consumes source series windows, adjacent quotient windows, a logarithmic tail ledger, dyadic comparison data, and a Raabe--Duhamel handoff before any $\RegSeqRatUp$ readback or $\RealUp$ seal is exposed. The handoff may cite the neighboring $\KummerTestUp$, $\GaussTestUp$, and $\RealSeriesRatioTestUp$ surfaces as dependency anchors, but it exports no host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Begin with an endpoint-facing read of the accepted carrier. The only pre-handoff source rows are $S,Q,L,D$: finite source windows, adjacent quotient windows, the logarithmic tail ledger, and the dyadic comparison budget.

Apply the carrier order from \autoref{def:bertrand-test-carrier}. The handoff row $T$ is downstream of $S,Q,L,D$, so a consumer cannot reach regular rational or Real endpoint data before the Raabe--Duhamel tail surface has consumed the displayed Bertrand evidence.

The neighboring Kummer, Gauss, and ratio-test chapters provide sibling anchors for the same quotient-defect lattice, but the exported route remains the finite sequence through $T,W,R,E$. The structural rows $H,C,P,N$ keep that sequence attached to its transport, replay, provenance, and local naming data.

Because the accepted packet is exhausted by these rows, no host summability, quotient series equality, selected sum, countable-choice coordinate, trichotomy, ambient completeness principle, Lean target, or bridge row can be exported by the handoff.
\end{proof}

\closureat{BertrandTestUp}{seedStr}

\begin{closurestatus}{\BertrandTestUp}
  \constructivestory{A $\BertrandTestUp$ packet is generated from source positive-series windows, adjacent quotient windows, a logarithmic tail ledger, dyadic comparison data, a Raabe--Duhamel handoff, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bertrand-test-carrier}, \autoref{thm:bertrand-test-namecert-obligations}, and \autoref{thm:bertrand-test-raabe-tail-handoff} seed the finite Bertrand convergence-test boundary over displayed quotient, logarithmic, comparison, regular-rational, and Real rows.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.BertrandTestUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires Lean theorems for carrier admission, quotient-window exactness, logarithmic-tail stability, dyadic comparison routing, Raabe--Duhamel handoff, endpoint routing, transport, replay, provenance, and ledger non-escape for BEDC.Derived.BertrandTestUp.}
\end{closurestatus}
