\chapter{A Concrete Naming Certificate for $\UniformCompletionUp$}
\label{ch:concrete-instances-uniformcompletion-namecert}
\origin{human}

Following Kelley, \emph{General Topology}, and Bishop--Bridges constructive
completion, the $\UniformCompletionUp$ packet records uniform completion as
finite Cauchy-filter and Cauchy-net evidence over BEDC rows, not quotient
completion, selected limits, ambient completeness, or choice. The packet
threads the filter boundary of
\autoref{ch:concrete-instances-cauchyfilter-namecert}, the directed witness
boundary of \autoref{ch:concrete-instances-cauchynet-namecert}, the extension
surface of \autoref{ch:concrete-instances-completionuniversalproperty-namecert},
the regular rational handoff of
\autoref{ch:concrete-instances-regseqrat-namecert}, and the real readback
boundary of \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[UniformCompletion carrier]
\label{def:uniform-completion-carrier}
A $\UniformCompletionUp$ carrier is a finite $\BHist$ packet
$$
(F,D,U,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $F$ is the $\CauchyFilterUp$ data row recording the finite filterbase,
late-window, and cofinality evidence consumed by completion-facing readers;
\item $D$ is the directed $\CauchyNetUp$ witness row recording the finite net
indices, common-window witnesses, and extracted directed subwindows;
\item $U$ is the $\CompletionUniversalPropertyUp$ universal-extension row for
uniformly continuous consumers served by the same $F$ and $D$ rows;
\item $E$ is the completion equality classifier row, comparing two packet reads
only through the displayed filter, net, and universal-extension coordinates;
\item $H$ is the componentwise $\hsame$ row over $F,D,U,E$;
\item $C$ is the displayed $\Cont$ route used to replay the Cauchy-filter,
directed-net, extension, and classifier rows under finite observation;
\item $P$ is the $\Pkg$ provenance over $\CauchyFilterUp$, $\CauchyNetUp$,
$\CompletionUniversalPropertyUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$,
$\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\UniformCompletionUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient completion equality, selected
representatives, ambient complete uniform spaces, open-cover compactness, or
choice.
\end{definition}
\leanchecked{BEDC.Derived.UniformCompletionUp.TasteGate.UniformCompletionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[UniformCompletion NameCert obligation surface]
\label{thm:uniform-completion-namecert-obligation-surface}
The local $\NameCert_{\UniformCompletionUp}$ surface of
\autoref{def:uniform-completion-carrier} consists of five obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $(F,D,U,E,H,C,P,N)$;
\item Cauchy-filter factorization, requiring every completion-facing consumer
to read the displayed $\CauchyFilterUp$ row $F$ before any completion handoff;
\item directed-net witness extraction, requiring directed witnesses to be
projected from the $\CauchyNetUp$ row $D$ rather than from a selected limit;
\item universal-extension uniqueness, requiring uniformly continuous extension
comparisons to pass through the $\CompletionUniversalPropertyUp$ row $U$ and
the classifier row $E$;
\item ledger non-escape, stating that the packet exports only the rows named in
$P$ and never a quotient completion, selected representative, ambient
complete uniform space, open-cover compactness principle, or choice principle.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.UniformCompletionUp.TasteGate.UniformCompletion\_namecert\_obligation\_surface}
\begin{proof}
Carrier admission is projection from the finite tuple
$(F,D,U,E,H,C,P,N)$ in \autoref{def:uniform-completion-carrier}. Each named
coordinate is already a BEDC row, and the local naming row $N$ records that
these coordinates are the public $\UniformCompletionUp$ surface.

Cauchy-filter factorization is the obligation that completion-facing reads
start with $F$. Directed-net witness extraction is the parallel obligation
that directed witnesses start with $D$, so neither route can introduce a
selected representative or an ambient limit point.

Universal-extension uniqueness is then confined to $U$ and $E$. The
componentwise $\hsame$ row $H$ and displayed $\Cont$ route $C$ replay these
same finite coordinates, while $P$ records that all downstream handoffs stay
inside the named Cauchy-filter, Cauchy-net, universal-property, RegSeqRat, and
Real rows.
\end{proof}

\begin{theorem}[UniformCompletion Cauchy-filter factorization]
\label{thm:uniform-completion-cauchy-filter-factorization}
For an accepted $\UniformCompletionUp$ carrier
$(F,D,U,E,H,C,P,N)$, every completion-facing consumer factors through the
displayed $\CauchyFilterUp$ row $F$ and the $\CauchyNetUp$ witness row $D$
before reading any $\CompletionUniversalPropertyUp$ extension data or
$\RealUp$ handoff data. Thus the completion boundary is exhausted by
$F,D,U,E,H,C,P,N$ and exports no quotient completion, selected representative,
ambient complete uniform space, open-cover compactness principle, or choice
principle.
\end{theorem}
\leanchecked{BEDC.Derived.UniformCompletionUp.TasteGate.UniformCompletion\_cauchy\_filter\_factorization}
\begin{proof}
Project an accepted carrier $(F,D,U,E,H,C,P,N)$. The first completion-facing
coordinate is $F$, so a consumer reaches Cauchy-filter evidence before it can
ask for any completion extension or real handoff.

The next witness coordinate is $D$. Its directed Cauchy-net subwindows provide
the directed evidence used by the completion route, and the packet contains no
separate selected-limit coordinate from which such witnesses could be read.

Only after $F$ and $D$ have been consumed can the route read the
$\CompletionUniversalPropertyUp$ row $U$, the classifier row $E$, or the
$\RealUp$ handoff data named by $P$. The transport and replay rows $H$ and $C$
preserve this order because they are componentwise rows over the same finite
packet.
\end{proof}

\begin{theorem}[UniformCompletion directed-net witness extraction]
\label{thm:uniform-completion-directed-net-witness}
For an accepted $\UniformCompletionUp$ carrier $(F,D,U,E,H,C,P,N)$, every
directed witness consumed by a completion-facing route is extracted from the
displayed $\CauchyNetUp$ row $D$ after the Cauchy-filter row $F$ has fixed the
request. The witness is therefore a finite directed-net subwindow, not a
selected limit, quotient representative, or ambient complete-uniform-space
point.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:uniform-completion-carrier}. The
Cauchy-filter row $F$ names the completion request, and the only directed row
available to answer it is $D$.

The existing factorization route of
\autoref{thm:uniform-completion-cauchy-filter-factorization} already forces
completion-facing consumers to read $F$ and $D$ before extension or Real handoff
data. Thus a directed witness is obtained by inspecting the finite subwindow
stored in $D$.

The remaining rows $U,E,H,C,P,N$ transport, replay, source, classify, and name
the same packet. Since none of them stores a selected limit, quotient
representative, or ambient point, the extracted witness cannot escape the
displayed directed-net row.
\end{proof}

\begin{theorem}[UniformCompletion universal-extension uniqueness]
\label{thm:uniform-completion-universal-extension-uniqueness}
For an accepted $\UniformCompletionUp$ carrier $(F,D,U,E,H,C,P,N)$, any two
displayed extension routes that read the same $\RegSeqRatUp$ handoff data and
the same $\RealUp$ seal data agree under the classifier row $E$. The comparison
passes through the $\CompletionUniversalPropertyUp$ row $U$ and the displayed
classifier, and it adds no parameter-transport equivalence or host equality of
completion objects.
\end{theorem}
\begin{proof}
Read both extension routes through the same accepted packet. By
\autoref{thm:uniform-completion-cauchy-filter-factorization}, both routes
first consume $F$ and $D$, so their completion-facing source data are already
the displayed Cauchy-filter and directed-net rows.

The universal-extension coordinate is $U$. When the two routes present the
same RegSeqRat and Real seal rows, $U$ supplies the common extension surface
attached to those rows, and $E$ is the only classifier coordinate allowed to
compare the two reads.

Componentwise transport and replay through $H$ and $C$ preserve the same
classifier input, while $P$ and $N$ source and name only the displayed packet.
Therefore agreement is a classifier agreement under $E$, not an added
parameter-transport equivalence or host completion equality.
\end{proof}
\leanchecked{BEDC.Derived.UniformCompletionUp.TasteGate.UniformCompletion\_universal\_extension\_uniqueness}

\begin{theorem}[UniformCompletion ledger non-escape]
\label{thm:uniform-completion-ledger-nonescape}
Every $\UniformCompletionUp$ consumer read factors through the displayed rows
$$
F,D,U,E,H,C,P,N .
$$
In particular, the read enters through the Cauchy-filter and Cauchy-net rows,
continues through universal-extension and classifier rows, and is preserved by
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data only. It exports no
quotient completion, selected representative, ambient complete uniform space,
open-cover compactness principle, or choice principle.
\end{theorem}
\begin{proof}
Start with a consumer read of the accepted carrier. Projection gives exactly
the rows $F,D,U,E,H,C,P,N$.

The factorization theorem forces the analytic part of the read through $F$ and
$D$ before any extension or Real-facing handoff is visible. The directed witness
extraction row then remains inside the displayed Cauchy-net evidence.

The rows $U$ and $E$ are the only extension and classifier rows. The structural
rows $H,C,P,N$ preserve, replay, source, and name those same coordinates, and
the carrier contains no further ledger entry.

Thus every consumer read factors through the displayed BHist, transport,
continuation, provenance, and naming rows, and none of the excluded quotient,
selection, ambient-completeness, open-cover, or choice data can be exported.
\end{proof}

\begin{theorem}[UniformCompletion filter-net obligations]
\label{thm:uniform-completion-filter-net-obligations}
An accepted $\UniformCompletionUp$ carrier exposes exactly the $\CauchyFilterUp$, $\CauchyNetUp$, $\CompletionUniversalPropertyUp$, $\RealUp$, $\RegSeqRatUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows needed for carrier admission and filter-to-net factorization. Carrier admission reads the packet as $(F,D,U,E,H,C,P,N)$, the filter row $F$ fixes the completion-facing request, the directed-net row $D$ supplies the finite witness route for that request, and the universal, Real, and RegSeqRat rows are readable only after $F$ and $D$ have been displayed.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:uniform-completion-carrier}. The displayed coordinates already list the Cauchy-filter row $F$, the Cauchy-net row $D$, the universal-property row $U$, the classifier row $E$, and the structural rows $H,C,P,N$.

The filter-to-net factorization is the local route from $F$ to $D$. The filter row fixes the finite request and the directed-net row provides the corresponding finite witness, so the construction has no place to read a selected limit or a quotient completion object.

The RegSeqRat and Real handoff data are named in the provenance row $P$ and replayed by $C$ after $F,D,U,E$ have been fixed. Componentwise $\hsame$ transport through $H$ preserves the same displayed rows, and the local $\NameCert$ row $N$ admits exactly this packet.
\end{proof}

\begin{theorem}[UniformCompletion universal ledger non-escape]
\label{thm:uniform-completion-universal-ledger-nonescape}
Any universal-extension read of an accepted $\UniformCompletionUp$ packet factors through the displayed universal-extension uniqueness row and gains no host quotient, selected limit, choice, or ambient completeness coordinate. The only extension comparison route is
$$
F,D \longmapsto U,E \longmapsto H,C,P,N ,
$$
so a consumer may compare extension reads only through $U$ and the displayed classifier row $E$.
\end{theorem}
\begin{proof}
Begin with a universal-extension read. By \autoref{thm:uniform-completion-cauchy-filter-factorization}, the read first passes through the Cauchy-filter row $F$ and the directed-net witness row $D$.

The universal ledger is then $U$, with comparison confined to the classifier row $E$. This is exactly the uniqueness route stated in \autoref{thm:uniform-completion-universal-extension-uniqueness}.

Finally $H$ and $C$ transport and replay the same finite rows, while $P$ and $N$ source and name the packet. None of those structural rows contains a quotient completion, selected limit, choice principle, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[UniformCompletion Real-RegSeqRat handoff]
\label{thm:uniform-completion-real-regseqrat-handoff}
Every $\RealUp$-facing consumer of an accepted $\UniformCompletionUp$ packet reads the completion seal through $\RegSeqRatUp$ and the displayed Cauchy filter and Cauchy net rows. The consumer route is therefore confined to the finite chain
$$
F,D \longmapsto \RegSeqRatUp \longmapsto \RealUp \longmapsto H,C,P,N
$$
named by the carrier provenance, not to a host completed-real object or a selected representative.
\end{theorem}
\begin{proof}
Project the packet and read its provenance row $P$. The only Real-facing source named there passes through the displayed Cauchy-filter row $F$, the Cauchy-net row $D$, the regular rational handoff, and the terminal Real seal.

The already established filter-net obligations force $F$ and $D$ to be consumed before the RegSeqRat and Real rows are public. Thus a Real-facing consumer cannot bypass the finite filter and net witness route.

Transport through $H$ and replay through $C$ are componentwise over the same rows, and $N$ only names that local packet. Consequently the handoff exports no host completed-real object, selected representative, quotient stream, choice principle, or ambient completeness premise.
\end{proof}

\begin{theorem}[UniformCompletion scope lock]
\label{thm:uniform-completion-scope-lock}
The $\UniformCompletionUp$ filter-net obligations are scoped exactly by the $\CauchyFilterUp$ row $F$, the $\CauchyNetUp$ row $D$, the $\CompletionUniversalPropertyUp$ row $U$, the $\RegSeqRatUp$ and $\RealUp$ handoff rows named by $P$, and the structural rows $H,C,P,N$ for $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Within that scope there is no quotient completion, selected representative, ambient complete uniform space, open-cover compactness principle, or choice coordinate.
\end{theorem}
\begin{proof}
Apply \autoref{thm:uniform-completion-filter-net-obligations}. It identifies carrier admission and filter-to-net factorization with the finite rows $F,D,U,E,H,C,P,N$, and it places the RegSeqRat and Real handoff data after the Cauchy-filter and Cauchy-net rows.

Apply \autoref{thm:uniform-completion-universal-ledger-nonescape}. The universal-extension comparison route is confined to
$$
F,D \longmapsto U,E \longmapsto H,C,P,N .
$$
Thus the extension ledger can compare displayed reads only through the universal-property row and the classifier row.

Finally apply \autoref{thm:uniform-completion-real-regseqrat-handoff}. The Real-facing route remains a finite RegSeqRat-to-Real handoff named by the provenance row, with transport and replay preserving the same carrier coordinates. The listed quotient, representative-selection, ambient-completeness, compactness, and choice data are outside the carrier and outside every continuation route.
\end{proof}

\begin{theorem}[UniformCompletion root source admission]
\label{thm:uniform-completion-root-source-admission}
Every accepted $\UniformCompletionUp$ carrier admits its root source through the displayed $\CauchyFilterUp$ row $F$, the $\CauchyNetUp$ row $D$, and the $\CompletionUniversalPropertyUp$ row $U$ before any extension consumer can read the structural rows $H,C,P,N$ for $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Equivalently, a $\BHist$ read into the $\UniformCompletionUp$ packet is root-admissible only when its source route is witnessed by
$$
F,D,U \longmapsto H,C,P,N .
$$
No root source is admitted through a quotient completion, a selected representative, or an ambient complete uniform space.
\end{theorem}
\begin{proof}
Project the carrier and begin with an arbitrary root read. The only source rows able to expose completion data are $F$, $D$, and $U$, so the read must first pass through the displayed Cauchy filter, Cauchy net, and universal-property coordinates.

The structural rows are downstream. The $\hsame$ transports $H$ preserve the same source coordinates, the $\Cont$ replay $C$ exposes only those finite routes, and $P,N$ source and name the resulting packet.

Since the carrier has no coordinate for quotient completion, representative selection, or an ambient complete uniform space, none of those objects can discharge root admission. The displayed route is therefore the complete admission boundary.
\end{proof}

\begin{theorem}[UniformCompletion root branch coverage]
\label{thm:uniform-completion-root-branch-coverage}
For an accepted $\UniformCompletionUp$ packet, every root branch relevant to extension data is covered by one of the displayed $\CauchyFilterUp$ or $\CauchyNetUp$ entries before the $\CompletionUniversalPropertyUp$ row $U$ is read. In BEDC rows, each branch read in $\BHist$ is classified through $F$ or $D$, transported by $\hsame$ in $H$, replayed by $\Cont$ in $C$, sourced by $\Pkg$ in $P$, and named by $\NameCert$ in $N$; no extension datum may bypass the filter-net branch coverage.
\end{theorem}
\begin{proof}
Read a branch request in the packet. If it is filter-shaped, the classifier sends it to $F$; if it is directed-net-shaped, the classifier sends it to $D$. These are exactly the two branch sources displayed before the universal-property row.

The universal row $U$ can compare extension data only after one of those branch witnesses is present. Transport and replay through $H$ and $C$ preserve the selected filter or net coordinate rather than adding a third branch.

The provenance row $P$ and naming row $N$ record the same two-branch boundary. Thus extension data has no route around the displayed filter-net coverage surface.
\end{proof}

\begin{theorem}[UniformCompletion root readback nonescape]
\label{thm:uniform-completion-root-readback-nonescape}
Every $\RegSeqRatUp$ or $\RealUp$ readback from an accepted $\UniformCompletionUp$ carrier is confined to the displayed finite route through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows already named by $F,D,U,H,C,P,N$. The readback ledger excludes quotient completion, selected representatives, an ambient complete uniform space, open-cover compactness, and choice: none of those data types is a carrier coordinate, a classifier branch, a continuation target, or a provenance source for the packet.
\end{theorem}
\begin{proof}
Start with a RegSeqRat-facing read. By the filter-net scope theorem, it is sourced by the displayed Cauchy-filter and Cauchy-net rows and then replayed through the same structural transport and continuation rows.

The Real-facing read is downstream of that ledger. It can only consume the regular rational handoff already named by the packet, so it cannot introduce a quotient completion or a selected representative.

Finally inspect the remaining excluded data. Ambient complete uniform spaces, open-cover compactness principles, and choice operators occur neither as carrier coordinates nor as classifier branches, continuation targets, or provenance sources. The readback therefore stays inside the finite BEDC ledger.
\end{proof}

\begin{theorem}[UniformCompletion filter-net canonical readback]
\label{thm:uniform-completion-filter-net-canonical-readback}
Every canonical readback from an accepted $\UniformCompletionUp$ carrier factors first through the displayed Cauchy-filter row $F$ and directed-net row $D$, then through the universal row $U$ and structural rows $H,C,P,N$. The readback boundary is
$$
F,D \longmapsto U \longmapsto H,C,P,N ,
$$
with any $\RegSeqRatUp$ or $\RealUp$ seal visible only through the provenance already carried by $P$.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:uniform-completion-cauchy-filter-factorization}. It identifies $F$ as the finite filter request and $D$ as the directed witness route for that request.

Use \autoref{thm:uniform-completion-directed-net-witness} and \autoref{thm:uniform-completion-filter-net-obligations}. The directed-net witness is the public canonical readback source, and the structural rows preserve that source rather than adding a quotient completion coordinate.

Finally apply \autoref{thm:uniform-completion-root-readback-nonescape}. The RegSeqRat and Real seals are downstream provenance reads, so canonical readback remains inside $F,D,U,H,C,P,N$ before any seal handoff.
\end{proof}

\begin{theorem}[UniformCompletion separated Real-seal uniqueness]
\label{thm:uniform-completion-separated-real-seal-uniqueness}
If two accepted $\UniformCompletionUp$ packets expose the same displayed Cauchy-filter row, directed-net witness row, universal-extension comparison row, and RegSeqRat-to-Real provenance route by componentwise $\hsame$ transport, then their $\RealUp$ seal handoffs are unique inside the displayed separated completion scope. The comparison is read only through
$$
F,D,U,E,H,C,P,N ,
$$
and never through quotient representatives, host equality of completed points, selected limits, or ambient complete-uniform-space extensionality.
\end{theorem}
\begin{proof}
Project the two accepted packets and compare their displayed rows componentwise. The rows $F$ and $D$ fix the filter-net source data, while $U$ fixes the universal-extension comparison route.

Apply \autoref{thm:uniform-completion-universal-extension-uniqueness}. Within the carrier, extension comparison is already confined to the universal row and classifier row $E$.

Use \autoref{thm:uniform-completion-real-regseqrat-handoff}. The Real-facing seal is downstream of the same RegSeqRat route named by $P$, and $H,C,N$ only transport, replay, and name that route.

Therefore the separated Real-seal comparison is unique relative to the displayed rows. The excluded quotient, selected-limit, host-equality, and ambient-completeness principles are not coordinates of either packet.
\end{proof}

\begin{theorem}[UniformCompletion universal property scope]
\label{thm:uniform-completion-universal-property-scope}
The universal property exported by an accepted $\UniformCompletionUp$ carrier is scoped exactly to finite filter-net rows before any RegSeqRat or Real seal handoff. A consumer may read
$$
F,D,U,E,H,C,P,N
$$
as carrier admission, filter-to-net factorization, directed witness extraction, universal comparison, classifier stability, continuation replay, provenance, and local naming, but may not read a quotient completion object, a selected representative, an ambient complete uniform space, open-cover compactness, or choice.
\end{theorem}
\begin{proof}
Apply \autoref{thm:uniform-completion-filter-net-canonical-readback}. It places every canonical readback behind the finite filter-net rows $F,D$ and the universal row $U$.

Apply \autoref{thm:uniform-completion-separated-real-seal-uniqueness}. The Real-seal comparison remains a separated handoff through $E,H,C,P,N$, not an equality principle for an ambient completed object.

Finally use \autoref{thm:uniform-completion-universal-ledger-nonescape}. The universal ledger has no field for quotient completions, selected representatives, ambient complete uniform spaces, open-cover compactness, or choice. The displayed finite scope is therefore the whole public universal-property surface.
\end{proof}

\begin{closurestatus}{\UniformCompletionUp}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:uniform-completion-carrier}, \autoref{thm:uniform-completion-namecert-obligation-surface}, \autoref{thm:uniform-completion-cauchy-filter-factorization}, \autoref{thm:uniform-completion-directed-net-witness}, \autoref{thm:uniform-completion-universal-extension-uniqueness}, \autoref{thm:uniform-completion-ledger-nonescape}, \autoref{thm:uniform-completion-filter-net-obligations}, \autoref{thm:uniform-completion-universal-ledger-nonescape}, \autoref{thm:uniform-completion-real-regseqrat-handoff}, and \autoref{thm:uniform-completion-scope-lock} close the local obligation surface and scoped filter-net boundary for carrier admission, filter-net factorization, directed-net witness extraction, universal-extension uniqueness, Real-RegSeqRat handoff, and ledger non-escape.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close quotient completions, selected representatives, ambient complete uniform spaces, open-cover compactness, or choice principles.}
  \upgradepath{The next step is Lean alignment of the scoped obligation rows with a canonical target and bridge-ready TasteGate surface for BEDC.Derived.UniformCompletionUp.}
  \constructivestory{The construction starts from displayed Cauchy-filter and directed Cauchy-net rows, then routes finite completion consumers through CompletionUniversalProperty, RegSeqRat, and Real handoff data without adding quotient or choice principles.}
\end{closurestatus}
