\chapter{A Concrete Naming Certificate for $\CompactUniformDiagonalNetUp$}
\label{ch:concrete-instances-compact-uniform-diagonal-net-namecert}
\origin{human}

Following Bishop and Bridges on compact metric uniform continuity, the $\CompactUniformDiagonalNetUp$ packet records the finite diagonal schedule that synchronizes compact-net centers, pointwise modulus rows, rational lower-bound folds, and Real-facing readback windows. It realizes the compact-uniform finite-net route of \autoref{ch:visions-compact-uniform-continuity} beside the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus boundary of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite Lebesgue-number ledger of \autoref{ch:concrete-instances-finite-lebesgue-number-namecert}, the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite schedule, not an open-cover compactness theorem, selected subcover, host diagonal argument, countable choice principle, or ambient completeness theorem.

\begin{definition}[Compact uniform diagonal net carrier]
\label{def:compact-uniform-diagonal-net-carrier}
A $\CompactUniformDiagonalNetUp$ carrier is a finite $\BHist$ packet
$$
G=(K,F,L,U,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, including inspected centers, coverage windows, and metric source data.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row over the same compact source and target metric surface.
\item $L$ is the $\FiniteLebesgueNumberUp$ or rational lower-bound ledger that turns the displayed finite cover into a positive radius budget.
\item $U$ is the $\UniformModulusUp$ handoff row reached only after $K,F,L$ have been displayed.
\item $S$ is the $\StreamNameUp$ diagonal schedule row listing the finite source windows, center indices, target tolerances, and output windows inspected together.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the scheduled windows and uniform-modulus handoff.
\item $E$ is the terminal $\RealUp$ seal row for Real-facing reads of the scheduled outputs.
\item $H$ records componentwise $\hsame$ transport for compact-net, continuous-map, radius-ledger, uniform-modulus, stream-schedule, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through $K,F,L,U,S,Q,E$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformDiagonalNetUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\FiniteLebesgueNumberUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformDiagonalNetUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,F,L,U,S,Q,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected subcovers outside $K$, host diagonal choice, quotient stream equality, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform diagonal net NameCert obligations]
\label{thm:compact-uniform-diagonal-net-namecert-obligations}
For every accepted $\CompactUniformDiagonalNetUp$ carrier $G=(K,F,L,U,S,Q,E,H,C,P,N)$, the local $\NameCert_{\CompactUniformDiagonalNetUp}$ surface consists exactly of carrier admission, compact finite-net exposure, continuous-map pointwise-modulus exposure, finite radius-ledger exposure, uniform-modulus handoff, diagonal StreamName schedule exposure, RegSeqRat readback, Real-seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through the displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-diagonal-net-carrier}. The compact finite-net row $K$, continuous-map row $F$, radius ledger $L$, and uniform-modulus row $U$ are the only compact-uniform source data available to the classifier.

Read the diagonal schedule row $S$. It lists the finite windows inspected together, so the schedule is a StreamName row rather than a host diagonal choice over infinitely many requests.

The readback row $Q$ and seal row $E$ are downstream of that schedule. They can inspect only the windows and tolerances already displayed by $S$ and the uniform-modulus handoff $U$.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for open-cover compactness, selected subcovers, host diagonal choice, quotient stream equality, selected limits, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact uniform diagonal net schedule handoff]
\label{thm:compact-uniform-diagonal-net-schedule-handoff}
Every Real-facing read of an accepted $\CompactUniformDiagonalNetUp$ carrier factors through the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \FiniteLebesgueNumberUp,\ \UniformModulusUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The read first displays the compact-net and pointwise-modulus rows, then the finite radius ledger and uniform-modulus handoff, then the StreamName diagonal schedule, regular rational readback, and Real seal.
\end{theorem}
\begin{proof}
Start with the carrier rows $K,F,L,U,S,Q,E,H,C,P,N$. The uniform-modulus handoff $U$ is downstream of the compact metric row, pointwise modulus row, and finite radius ledger.

Next read the diagonal schedule. The schedule row $S$ synchronizes center indices, source windows, tolerances, and output windows inside one finite $\StreamNameUp$ surface.

The regular readback $Q$ and Real seal $E$ consume only that displayed schedule and the handoff $U$. The structural rows preserve the same finite path under transport, replay, provenance, and local naming.

Thus every Real-facing read remains inside the compact-uniform finite-net diagonal schedule and cannot add an open-cover compactness theorem, selected subcover, host diagonal selector, quotient stream equality, selected limit, or ambient real completeness.
\end{proof}

\falsifiablePrediction{An accepted $\CompactUniformDiagonalNetUp$ read is refuted if a RealUp seal is reached without displaying the CompactMetricUp finite-net row, ContinuousMapUp pointwise-modulus row, FiniteLebesgueNumberUp radius ledger, UniformModulusUp handoff, StreamNameUp diagonal schedule, RegSeqRatUp readback, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, finite-lebesgue-number, streamname, regseqrat, real: none is bijective to $\CompactUniformDiagonalNetUp$, because only this packet binds compact finite-net coverage, pointwise moduli, finite radius ledgers, a uniform-modulus handoff, a diagonal StreamName schedule, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming into one finite carrier.}

\closureat{CompactUniformDiagonalNetUp}{seedStr}

\begin{closurestatus}{\CompactUniformDiagonalNetUp}
  \constructivestory{A $\CompactUniformDiagonalNetUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise-modulus rows, FiniteLebesgueNumber radius ledgers, UniformModulus handoff rows, StreamName diagonal schedules, RegSeqRat readback, RealUp seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-diagonal-net-carrier}, \autoref{thm:compact-uniform-diagonal-net-namecert-obligations}, and \autoref{thm:compact-uniform-diagonal-net-schedule-handoff} seed the compact-uniform finite-net diagonal schedule over compact metric, continuous-map, finite-radius, uniform-modulus, stream, regular readback, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, host diagonal choice, quotient stream equality, selected limits, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net exposure, pointwise-modulus exposure, finite-radius exposure, uniform-modulus handoff, diagonal StreamName scheduling, RegSeqRat readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformDiagonalNetUp.}
\end{closurestatus}
