\chapter{A Concrete Naming Certificate for $\RecursionAuthorizationLedgerUp$}
\label{ch:concrete-instances-recursion-authorization-ledger-namecert}
\origin{human}

The $\RecursionAuthorizationLedgerUp$ packet turns an accepted recursor call into finite NameCert ledger data. It records the inductive signature accepted by the kernel, the emitted eliminator, the checked motive, the branch package, the structural descent row, the generated output row, transport and continuation routes, package provenance, and the local naming row that later audit and generator-closure consumers can inspect.

\begin{definition}[RecursionAuthorizationLedger carrier]
\label{def:recursion-authorization-ledger-carrier}
A $\RecursionAuthorizationLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the accepted inductive signature row, including the constructor inventory and the recursive argument positions admitted by the kernel;
\item $R$ is the emitted eliminator row for $I$, read as the authorized recursor generated from that signature;
\item $M$ is the motive row checked for the eliminator call;
\item $B$ is the branch-package row, with one displayed branch for each constructor in $I$;
\item $D$ is the structural descent ledger recording that every recursive subcall is made only on constructor-subdata admitted by $I$;
\item $O$ is the generated output row whose public name is being certified by the accepted recursor call;
\item $H$ records componentwise $\hsame$ transports for the signature, eliminator, motive, branch package, descent ledger, output row, provenance row, and local naming row;
\item $C$ records the displayed $\Cont$ routes by which consumers read the signature, eliminator, motive, branch, descent, output, audit, and naming coordinates;
\item $P$ is the $\Pkg$ provenance over $\BHist$, $\hsame$, $\Cont$, the kernel-accepted signature, emitted eliminator, branch-completeness check, structural descent check, purity audit boundary, and neighboring generator certificates;
\item $N$ is the local $\NameCert_{\RecursionAuthorizationLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through the displayed rows $I,R,M,B,D,O,H,C,P,N$.
\end{definition}

\begin{theorem}[RecursionAuthorizationLedger NameCert obligations]
\label{thm:recursion-authorization-ledger-namecert-obligations}
For the carrier of \autoref{def:recursion-authorization-ledger-carrier}, the local $\NameCert_{\RecursionAuthorizationLedgerUp}$ surface consists of carrier admission for
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
kernel-acceptance of the constructor signature $I$, emitted-eliminator exactness for $R$, motive admission for $M$, branch completeness for $B$, structural descent exactness for $D$, output-row exposure for $O$, componentwise $\hsame$ transport, displayed $\Cont$ route replay, $\Pkg$ provenance, local naming, and purity-audit non-escape.
\end{theorem}
\leanchecked{BEDC.Derived.RecursionAuthorizationLedgerUp.RecursionAuthorizationLedgerTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is projection from \autoref{def:recursion-authorization-ledger-carrier}. The signature, emitted eliminator, motive, branch package, descent ledger, output row, transport row, continuation row, provenance row, and local naming row are all coordinates of one finite packet.

The kernel-facing obligations are the accepted signature and emitted recursor rows. The recursor-as-authorized-generator vision supplies the boundary: a constructor signature is usable only after kernel acceptance, and the eliminator row is the kernel output for that signature rather than a separate recursion principle.

Branch completeness and structural descent are read from $B$ and $D$. The branch package is indexed by the displayed constructors of $I$, and every recursive call recorded by $D$ descends through constructor-subdata accepted by the same signature.

The output and audit obligations are the remaining rows. Consumers reach $O$ only along $C$, transport it only through $H$, and cite only the provenance in $P$ and $N$. The source gate, build gate, and purity-audit boundary named by $P$ exclude axiom channels and forbidden host principles from the ledger surface.
\end{proof}

\begin{theorem}[RecursionAuthorizationLedger generator-closure sibling route]
\label{thm:recursion-authorization-ledger-generator-closure-sibling-route}
For an accepted $\RecursionAuthorizationLedgerUp$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
the accepted signature row $I$, emitted eliminator row $R$, branch package $B$, and structural descent ledger $D$ are the finite authorization ledger consumed by \autoref{ch:concrete-instances-generatorclosure-namecert} and \autoref{ch:concrete-instances-generatorinductionseal-namecert}. The consumer route also transports, exposes, packages, and names that ledger through $H,C,P,N$, but it exports no unrestricted recursion principle and no eliminator outside the accepted signature.
\end{theorem}
\begin{proof}
Project the carrier tuple $L=(I,R,M,B,D,O,H,C,P,N)$ from \autoref{def:recursion-authorization-ledger-carrier}. The rows $I,R,B,D$ are the signature, emitted eliminator, branch package, and structural descent data that authorize the generated output row.

Use \autoref{thm:recursion-authorization-ledger-namecert-obligations}. Kernel acceptance, emitted-eliminator exactness, branch completeness, and structural descent exactness are obligations of the same finite packet, so a generator-closure consumer reads only those rows before inspecting the output route.

Finally route the sibling consumers through the displayed structural rows $H,C,P,N$. These rows transport, expose, package, and name the same finite ledger for \autoref{ch:concrete-instances-generatorclosure-namecert} and \autoref{ch:concrete-instances-generatorinductionseal-namecert}; they do not add an unrestricted recursion principle or a second eliminator.
\end{proof}

\begin{theorem}[RecursionAuthorizationLedger signature acceptance]
\label{thm:recursion-authorization-ledger-signature-acceptance}
In an accepted $\RecursionAuthorizationLedgerUp$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
the emitted eliminator row $R$ and the branch package $B$ are readable only after the same accepted inductive signature row $I$. No unlisted constructor row can enter the ledger.
\end{theorem}
\begin{proof}
Project the signature, eliminator, and branch coordinates $I,R,B$ from \autoref{def:recursion-authorization-ledger-carrier}. The carrier records $R$ as emitted from $I$ and $B$ as indexed by the constructor inventory displayed by $I$.

Apply \autoref{thm:recursion-authorization-ledger-namecert-obligations}. Kernel-acceptance of $I$, emitted-eliminator exactness for $R$, and branch completeness for $B$ are local obligations of the same finite packet.

The remaining coordinates $M,D,O,H,C,P,N$ do not add constructor rows. They check the motive, descent, output, transport, replay, provenance, and local naming of the accepted packet, so an unlisted constructor has no route into the ledger.
\end{proof}

\begin{theorem}[RecursionAuthorizationLedger output audit boundary]
\label{thm:recursion-authorization-ledger-output-audit-boundary}
For an accepted $\RecursionAuthorizationLedgerUp$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
the output row $O$ is exposed only through the structural descent row $D$, componentwise $\hsame$ transport $H$, displayed $\Cont$ routes $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. The packet exports no unrestricted recursion principle and no hidden eliminator.
\end{theorem}
\begin{proof}
Project the descent, output, and audit coordinates $D,O,H,C,P,N$. The carrier makes $O$ a generated output row whose recursive calls are certified by $D$ and whose public reads are transported, replayed, packaged, and named through $H,C,P,N$.

Use \autoref{thm:recursion-authorization-ledger-namecert-obligations} for output-row exposure and purity-audit non-escape. Then use \autoref{thm:recursion-authorization-ledger-obligation-branch-completeness} to keep the output tied to the displayed branch package and structural descent ledger.

Since the carrier has no coordinate for an unrestricted recursion principle or a second eliminator, the output audit boundary is exactly the route through $D,H,C,P,N$.
\end{proof}

\begin{theorem}[RecursionAuthorizationLedger branch completeness and descent exactness]
\label{thm:recursion-authorization-ledger-obligation-branch-completeness}
For an accepted $\RecursionAuthorizationLedgerUp$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
every constructor row displayed by $I$ has exactly one branch row in $B$, and every recursive call certified by the output row $O$ is justified by the structural descent ledger $D$. Thus the exported ledger contains no omitted branch and no non-structural recursive call.
\end{theorem}
\begin{proof}
Project the signature and branch coordinates $I$ and $B$ from the carrier. The inventory in \autoref{def:recursion-authorization-ledger-carrier} records $B$ as the branch-package row with one displayed branch for each constructor in $I$.

Project the descent and output coordinates $D$ and $O$. The same carrier records $D$ as the structural descent ledger and $O$ as the generated output row whose recursive calls are certified by that ledger.

Use the NameCert obligations of \autoref{thm:recursion-authorization-ledger-namecert-obligations}. Branch completeness and structural descent exactness are local obligations for the same finite packet, so no consumer can read $O$ without the constructor-indexed branch package and the descent rows that justify its recursive subcalls.

The remaining rows $H,C,P,N$ only transport, expose, package, and name those displayed coordinates. Since they add no branch coordinate and no recursion coordinate, the public ledger exports neither an omitted branch nor a non-structural recursive call.
\end{proof}
\leanchecked{BEDC.Derived.RecursionAuthorizationLedgerUp.RecursionAuthorizationLedgerPacket\_branch\_descent\_exactness}

\begin{theorem}[RecursionAuthorizationLedger motive-output route]
\label{thm:recursion-authorization-ledger-motive-output-route}
For an accepted $\RecursionAuthorizationLedgerUp$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
the motive row $M$ and generated output row $O$ are consumed only through the finite ledger. In particular, a public output read first uses the accepted signature $I$, the emitted eliminator $R$, the checked motive $M$, the branch package $B$, and the descent ledger $D$, and only then exposes $O$ through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier. The rows $I,R,M,B,D,O$ occur in the displayed order of the ledger packet, with $M$ recorded as the checked motive for the emitted eliminator and $O$ recorded as the generated output row.

Use \autoref{thm:recursion-authorization-ledger-namecert-obligations}. Motive admission, branch completeness, structural descent exactness, and output-row exposure are all local obligations for the same finite packet.

Finally apply \autoref{thm:recursion-authorization-ledger-obligation-branch-completeness}. The output row cannot be read apart from the branch package and descent ledger that justify the recursive subcalls, so the motive-output route stays inside $L$.
\end{proof}

\begin{theorem}[RecursionAuthorizationLedger descent transport stability]
\label{thm:recursion-authorization-ledger-descent-transport-stability}
For an accepted $\RecursionAuthorizationLedgerUp$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
componentwise $\hsame$ transport preserves the structural descent ledger $D$, the branch package $B$, and the output row $O$ as one finite authorization route. A transported read cannot replace $D$ by a non-structural recursion row or replace $O$ by a hidden eliminator output.
\end{theorem}
\begin{proof}
Read the transport row $H$ from the carrier. Its componentwise clauses cover the signature, emitted eliminator, motive, branch package, descent ledger, output row, provenance row, and local naming row.

Apply the NameCert obligations. Structural descent exactness is attached to $D$, branch completeness is attached to $B$, and output-row exposure is attached to $O$, all within the same packet $L$.

The continuation and provenance rows $C,P,N$ replay and name the transported route. Since the carrier has no coordinate for a non-structural recursive call or a second eliminator, transport preserves the finite descent-output surface rather than producing an escape route.
\end{proof}

\begin{theorem}[RecursionAuthorizationLedger audit provenance exhaustion]
\label{thm:recursion-authorization-ledger-audit-provenance-exhaustion}
For an accepted $\RecursionAuthorizationLedgerUp$ packet
$$
L=(I,R,M,B,D,O,H,C,P,N),
$$
all audit provenance for the signature, motive, descent, output, and purity boundary is exhausted by the rows $P$ and $N$ together with the displayed read routes $C$. The ledger exports no unlisted constructor, no omitted branch, no non-structural recursive call, no hidden eliminator, and no output provenance outside $L$.
\end{theorem}
\begin{proof}
Project $P,N$ and the continuation row $C$. The carrier describes $P$ as provenance over the accepted signature, emitted eliminator, branch completeness check, structural descent check, purity audit boundary, and neighboring generator certificates.

Use \autoref{thm:recursion-authorization-ledger-namecert-obligations}. Package provenance, local naming, displayed route replay, and purity-audit non-escape are obligations of the same finite packet as $I,R,M,B,D,O,H$.

Apply the branch-completeness theorem and the non-escape theorem. They rule out omitted branches, non-structural recursive calls, hidden eliminators, unrestricted recursion principles, and outside audit channels, so the provenance ledger is exhausted by $C,P,N$ over $L$.
\end{proof}

\begin{theorem}[RecursionAuthorizationLedger non-escape]
\label{thm:recursion-authorization-ledger-nonescape}
Every consumer read of an accepted $\RecursionAuthorizationLedgerUp$ packet factors through
$$
I,R,M,B,D,O,H,C,P,N.
$$
It exports no unlisted constructor, no omitted branch, no branch outside the accepted signature, no non-structural recursive call, no hidden eliminator, no unrestricted recursion principle, and no output whose dependency route escapes the build, source-audit, and purity-audit boundary recorded in the packet.
\end{theorem}
\begin{proof}
Start with the obligation theorem. It places the generated output downstream of the accepted signature, emitted eliminator, motive, branch package, structural descent ledger, transport rows, continuation routes, provenance, and local naming row.

For constructors and branches, inspect $I$ and $B$. The visible signature supplies the only constructor inventory, and the branch package supplies exactly the branch rows available to the eliminator call.

For recursion, inspect $D$. A recursive subcall can be consumed only when the descent ledger records it as constructor-subdata of the accepted signature. There is no coordinate for a non-structural recursive call or a second hidden eliminator.

Finally inspect $P$ and $N$. They record the build, source-audit, and purity-audit boundary for the named output. Since the carrier contains no axiom channel, unrestricted recursion principle, or forbidden host dependency row, the public ledger cannot export any of those objects.
\end{proof}

\closureat{RecursionAuthorizationLedgerUp}{seedStr}

\begin{closurestatus}{\RecursionAuthorizationLedgerUp}
  \constructivestory{A $\RecursionAuthorizationLedgerUp$ packet is generated from an accepted inductive signature, emitted eliminator, checked motive, complete branch package, structural descent ledger, output row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:recursion-authorization-ledger-carrier}, \autoref{thm:recursion-authorization-ledger-namecert-obligations}, and \autoref{thm:recursion-authorization-ledger-nonescape} bind the finite authorization ledger for accepted recursor calls.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, global kernel soundness, all positivity checking, all termination checking, bridge checking, or any external classical principle.}
  \upgradepath{Obligation closure requires checked carrier admission, signature acceptance, branch completeness, structural descent exactness, output exposure, audit boundary, and non-escape for the same finite ledger packet.}
\end{closurestatus}
