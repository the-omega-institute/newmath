\begin{theorem}[Effective cover compactness sibling route]
\label{thm:effective-cover-compactness-sibling-route}
Every accepted $\EffectiveCoverCompactnessUp$ packet consumes the compact metric source of $\CompactMetricUp$, the finite-cover refinement route, the Lebesgue-number finite-cover route, and the $\FiniteEpsilonNetUp$ finite coverage pattern before it exports compact-uniform modulus data. Its visible dependency route is therefore
$$
\CompactMetricUp \longmapsto \FiniteCoverRefinementUp \longmapsto \LebesgueNumberFiniteCoverUp \longmapsto \FiniteEpsilonNetUp \longmapsto \CompactUniformModulusUp .
$$
No consumer may bypass these finite rows by appealing to arbitrary open-cover compactness, hidden centers, countable subcover extraction, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:effective-cover-compactness-carrier}. The rows $K,O,R,L,M,H,C,P,N$ force the compact metric source and displayed cover to be read before the finite refinement and Lebesgue-number witnesses.

The finite coverage step follows the same visible pattern as $\FiniteEpsilonNetUp$: the consumer only sees finitely listed coverage data and the named transport rows that preserve it. The compact-uniform row $M$ is downstream of that finite route, while $H,C,P,N$ only transport, replay, source, and name the displayed certificate.

Thus the chapter exports compact-uniform modulus data through the sibling route recorded above, and not through any hidden compactness principle outside the effective cover packet.
\end{proof}
