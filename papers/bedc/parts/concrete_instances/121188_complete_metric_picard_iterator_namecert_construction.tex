\chapter{A Concrete Naming Certificate for $\CompleteMetricPicardIteratorUp$}
\label{ch:concrete-instances-complete-metric-picard-iterator-namecert}
\origin{human}

Following Banach's fixed-point theorem and Bishop--Bridges constructive complete-metric analysis, the $\CompleteMetricPicardIteratorUp$ packet records the finite Picard-iteration surface available before a complete-metric consumer reads a fixed-point approximation. It sits after the complete-metric carrier of \autoref{ch:concrete-instances-completemetric-namecert}, the separated-reflector unit of \autoref{ch:concrete-instances-metric-separated-reflector-unit-namecert}, the separated-reflection route of \autoref{ch:concrete-instances-separated-reflection-namecert}, and the contraction interface of \autoref{ch:concrete-instances-banach-contraction-namecert}. The packet names a constructive orbit-and-tail ledger; it does not select a limit point, quotient representatives, host-level function equality, countable choice, or ambient classical completeness.

\begin{definition}[CompleteMetricPicardIterator carrier]
\label{def:complete-metric-picard-iterator-carrier}
A $\CompleteMetricPicardIteratorUp$ carrier is a finite $\BHist$ packet
$$
I=(M,S,T,Q,O,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\CompleteMetricUp$ source row, exposing point admission, metric readback, completeness witness data, and the separated target surface through which distances are read.
\item $S$ is the separated-reflection row consumed before the iterator is allowed to treat zero-distance agreement as a public metric equality.
\item $T$ is the contraction row, displaying the self-map, contraction budget, and compatibility with the metric row.
\item $Q$ is the dyadic tail-control row, recording finite bounds that make the Picard tails Cauchy in the displayed complete metric.
\item $O$ is the orbit row, listing the finite initial point, iterate window, and successor routing for the Picard chain.
\item $W$ is the endpoint-window row. It records which finite windows may be exported to downstream fixed-point or Real-facing consumers before any completed endpoint is claimed.
\item $H$ records componentwise $\hsame$ transport for the complete metric, separated reflection, contraction, tail-control, orbit, endpoint-window, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a metric request through $M,S,T,Q,O,W,H$ before an endpoint-facing consumer reads the packet.
\item $P$ is the $\Pkg$ provenance over $\CompleteMetricPicardIteratorUp$, $\CompleteMetricUp$, $\MetricSeparatedReflectorUnitUp$, $\SeparatedReflectionUp$, $\BanachContractionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompleteMetricPicardIteratorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,S,T,Q,O,W,H,C,P,N$. It has no coordinate for a chosen completed point, quotient representative, host equality of functions, countable-choice schedule, or classical fixed-point theorem.
\end{definition}

\begin{theorem}[CompleteMetricPicardIterator NameCert obligations]
\label{thm:complete-metric-picard-iterator-namecert-obligations}
For every accepted $\CompleteMetricPicardIteratorUp$ carrier $I=(M,S,T,Q,O,W,H,C,P,N)$, the local $\NameCert_{\CompleteMetricPicardIteratorUp}$ surface consists exactly of complete-metric admission, separated-reflection exposure, contraction exposure, finite tail-control exposure, Picard-orbit exposure, endpoint-window exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every fixed-point or completion-facing consumer must read
$$
M,S,T,Q,O,W,H,C,P,N
$$
before treating the Picard iterator as endpoint evidence.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:complete-metric-picard-iterator-carrier}. The rows $M,S,T,Q,O,W,H,C,P,N$ are exactly the displayed coordinates available to the local certificate.

The complete-metric and separated-reflection rows are read first. They expose the metric request surface and the zero-distance discipline before the contraction row is allowed to generate an orbit.

Next read the contraction, tail-control, orbit, and endpoint-window rows. These rows supply the constructive Picard data: a self-map with a displayed contraction budget, finite tail estimates, finite iterate windows, and the bounded endpoint windows that downstream consumers may inspect.

Finally transport and replay preserve the same route. Componentwise $\hsame$ aligns each row, $\Cont$ replay sends the request through the displayed coordinates, and $P,N$ source and name the packet. Since no carrier coordinate stores a selected limit, quotient representative, host function equality, countable-choice schedule, or ambient classical completeness principle, no NameCert obligation can export one.
\end{proof}

\closureat{CompleteMetricPicardIteratorUp}{seedStr}

\begin{closurestatus}{\CompleteMetricPicardIteratorUp}
  \constructivestory{A $\CompleteMetricPicardIteratorUp$ packet is generated from complete-metric data, separated-reflection exposure, a contraction row, finite tail-control data, Picard orbit windows, endpoint-window rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:complete-metric-picard-iterator-carrier} and \autoref{thm:complete-metric-picard-iterator-namecert-obligations} seed the constructive Picard-iteration interface over complete metric data, separated reflection, contraction budgets, finite tails, orbit windows, endpoint windows, transport, replay, provenance, and naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, selected fixed points, quotient representatives, host function equality, countable choice, ambient classical completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, complete-metric exposure, separated-reflection exposure, contraction exposure, tail-control stability, orbit-window replay, endpoint-window routing, transport, provenance, and local naming for BEDC.Derived.CompleteMetricPicardIteratorUp.}
\end{closurestatus}
