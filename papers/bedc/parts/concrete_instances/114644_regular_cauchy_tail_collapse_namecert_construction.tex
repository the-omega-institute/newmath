\chapter{A Concrete Naming Certificate for $\RegularCauchyTailCollapseUp$}
\label{ch:concrete-instances-regular-cauchy-tail-collapse-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyTailCollapseUp$ packet records the finite tail-collapse surface used when a regular Cauchy rational name is read only through sufficiently late windows. It sits over the finite observation service of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the paired-tail uniqueness packet of \autoref{ch:concrete-instances-regular-cauchy-limit-uniqueness-namecert}. The packet supplies Real and completion routes with displayed tail-collapse data; it does not identify quotient streams, select host limits, invoke countable choice, use trichotomy, or assume ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy tail-collapse carrier]
\label{def:regular-cauchy-tail-collapse-carrier}
A $\RegularCauchyTailCollapseUp$ carrier is a finite $\BHist$ packet
$$
T=(R,S,D,W,Q,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the source $\RegSeqRatUp$ regular rational name whose late tail is being collapsed.
\item $S$ is the $\StreamNameUp$ schedule exposing the finite tail windows used by the collapse request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger naming the displayed scale and the finite tail threshold.
\item $W$ is the selected finite tail window after the threshold named by $D$.
\item $Q$ is the $\RegSeqRatUp$ readback row for the collapsed tail data.
\item $U$ is the $\RegularCauchyLimitUniquenessUp$ sibling row used only to compare paired tail windows after the same dyadic threshold has been displayed.
\item $E$ is the downstream $\RealUp$ seal row exposed after $R,S,D,W,Q,U$ have been read.
\item $H$ records componentwise $\hsame$ transport for the source name, schedule, dyadic threshold, tail window, readback, uniqueness sibling row, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay for any Real or completion consumer through the same finite route.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyTailCollapseUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RegularCauchyLimitUniquenessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyTailCollapseUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,S,D,W,Q,U,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, host real equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy tail-collapse NameCert obligations]
\label{thm:regular-cauchy-tail-collapse-namecert-obligations}
For every accepted $\RegularCauchyTailCollapseUp$ carrier $T=(R,S,D,W,Q,U,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyTailCollapseUp}$ surface consists exactly of source $\RegSeqRatUp$ admission, finite $\StreamNameUp$ tail-window exposure, $\DyadicRatCoreUp$ threshold accounting, collapsed-tail $\RegSeqRatUp$ readback, the $\RegularCauchyLimitUniquenessUp$ paired-tail witness, the downstream $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:regular-cauchy-tail-collapse-carrier}. The analytic rows are $R,S,D,W,Q,U,E$: the source regular name, the displayed schedule, the dyadic threshold, the finite tail window, the collapsed regular readback, the paired-tail uniqueness witness, and the terminal Real-facing seal.

The structural rows are $H,C,P,N$. They transport, replay, source, and name the same finite route, so the certificate surface has no remaining coordinate from which a quotient stream equality, selected host limit, choice schedule, trichotomy decision, or ambient completeness principle could be read.
\end{proof}

\begin{theorem}[Regular Cauchy tail-collapse handoff]
\label{thm:regular-cauchy-tail-collapse-handoff}
Every Real or completion consumer of an accepted $\RegularCauchyTailCollapseUp$ carrier must read the finite route
$$
\StreamNameUp+\DyadicRatCoreUp+\RegSeqRatUp \longmapsto \RegularCauchyLimitUniquenessUp \longmapsto \RealUp
$$
through the displayed rows $S,D,R,W,Q,U,E,H,C,P,N$. The handoff therefore supplies RegSeqRat and RealUp completion arguments with a named tail-collapse packet while refusing quotient stream equality, selected limits, countable choice, trichotomy, and ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with a consumer request. The schedule row $S$ and tolerance row $D$ display the finite threshold before any late tail entries are available, and the window row $W$ records exactly the exposed tail segment.

Read the regular rational data through $R$ and $Q$. The source name and collapsed-tail readback are both $\RegSeqRatUp$ rows, so no host stream quotient or selected limit is introduced at this stage.

Use the sibling $\RegularCauchyLimitUniquenessUp$ row $U$ only after the same threshold and window have been displayed. The route can then expose the $\RealUp$ seal row $E$, and $H,C,P,N$ preserve, replay, source, and name that finite packet for downstream Real and completion routes.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyTailCollapseUp$ consumer read factors through a StreamName schedule, DyadicRatCore threshold ledger, RegSeqRat source row, finite tail window, collapsed RegSeqRat readback, RegularCauchyLimitUniqueness paired-tail witness, RealUp seal, transport, replay, provenance, and local NameCert row.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, regular-cauchy-limit-uniqueness: $\RegularCauchyTailCollapseUp$ is not bijective to any listed sibling because it jointly carries a source regular name, finite tail schedule, displayed dyadic threshold, collapsed readback, paired-tail uniqueness witness, Real seal boundary, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyTailCollapseUp}{seedStr}

\begin{closurestatus}{\RegularCauchyTailCollapseUp}
  \constructivestory{A $\RegularCauchyTailCollapseUp$ packet is generated from a RegSeqRat source row, StreamName tail schedule, DyadicRatCore threshold ledger, finite tail window, collapsed RegSeqRat readback, RegularCauchyLimitUniqueness paired-tail witness, RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-tail-collapse-carrier}, \autoref{thm:regular-cauchy-tail-collapse-namecert-obligations}, and \autoref{thm:regular-cauchy-tail-collapse-handoff} seed the finite tail-collapse route consumed by RegSeqRat and RealUp completion arguments.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, host real equality, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, threshold accounting, collapsed-tail readback, paired-tail uniqueness handoff, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyTailCollapseUp.}
\end{closurestatus}
