\chapter{A Concrete Naming Certificate for $\SequentialRealCompactnessUp$}
\label{ch:concrete-instances-sequential-real-compactness-namecert}
\origin{human}

Following Bolzano and Weierstrass, the $\SequentialRealCompactnessUp$ packet records the finite route by which a bounded real sequence exposes a regular cluster subsequence through displayed interval-tree data. It sits after the subsequence extraction certificate of \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}, the bounded source surface of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the interval-net carrier of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a sequential compactness interface for bounded real sequences, not an open-cover compactness theorem, a host completeness principle, quotient equality of real names, or a choice-selected arbitrary subsequence.

\begin{definition}[Sequential real compactness carrier]
\label{def:sequential-real-compactness-carrier}
A $\SequentialRealCompactnessUp$ carrier is a finite $\BHist$ packet
$$
K=(S,B,I,E,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source sequence window schedule exposed through $\StreamNameUp$.
\item $B$ is the $\BoundedRealSequenceUp$ source row naming the bounded real sequence interface consumed by the extraction route.
\item $I$ is the $\ClosedBoundedIntervalNetUp$ interval-tree row carrying dyadic retained cells and finite mesh comparisons.
\item $E$ is the $\BolzanoWeierstrassUp$ extraction row, including the monotone-cofinal subsequence schedule and retained finite windows.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the extracted windows and dyadic tolerances.
\item $R$ is the terminal $\RealUp$ cluster seal reached only after $S,B,I,E,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source schedule, bounded source row, interval tree, extraction row, regular readback, real seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from bounded sequence inspection through interval-tree retention, Bolzano--Weierstrass extraction, RegSeqRat readback, and RealUp sealing.
\item $P$ is the $\Pkg$ provenance over $\SequentialRealCompactnessUp$, $\BolzanoWeierstrassUp$, $\BoundedRealSequenceUp$, $\ClosedBoundedIntervalNetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SequentialRealCompactnessUp}$ row.
\end{enumerate}
The carrier stores no open-cover compactness object, no arbitrary subsequence selector, no quotient equality of real names, no host supremum, no ambient $\RealUp$ completeness theorem, and no branch outside the displayed interval-tree extraction route.
\end{definition}

\begin{theorem}[Sequential real compactness NameCert obligations]
\label{thm:sequential-real-compactness-namecert-obligations}
For every accepted $\SequentialRealCompactnessUp$ carrier $K=(S,B,I,E,Q,R,H,C,P,N)$, the local $\NameCert_{\SequentialRealCompactnessUp}$ surface consists exactly of carrier admission for the ten displayed rows, bounded-source admission through $B$, finite interval-tree retention through $I$, Bolzano--Weierstrass extraction through $E$, finite stream-window and dyadic-tolerance readback through $S$ and $Q$, terminal real sealing through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sequential-real-compactness-carrier}. The bounded sequence source is available only through $S$ and $B$, while the interval-tree row $I$ supplies the retained dyadic cells before extraction is read.

Use the bounded-source handoff of \autoref{ch:concrete-instances-boundedrealsequence-namecert} and the interval-net surface of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert}. These rows expose finite sequence windows, regular readback, real-name seals, and interval containment without adding an open-cover compactness object or a host completeness theorem.

Read the extraction row through \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}. The Bolzano--Weierstrass packet supplies the monotone-cofinal subsequence schedule and finite retained windows, which are then read through $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ before the terminal $\RealUp$ seal is available.

The structural rows $H,C,P,N$ transport, replay, source, and name only this finite route. Since the carrier has no coordinate for arbitrary subsequence choice, quotient real equality, host supremum, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Sequential real compactness Bolzano--Weierstrass handoff]
\label{thm:sequential-real-compactness-bolzano-weierstrass-handoff}
For every accepted $\SequentialRealCompactnessUp$ carrier $K=(S,B,I,E,Q,R,H,C,P,N)$, the terminal compactness-facing read factors through the finite route
$$
\BoundedRealSequenceUp \longmapsto \ClosedBoundedIntervalNetUp \longmapsto \BolzanoWeierstrassUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The handoff composes the bounded-source row with Bolzano--Weierstrass finite interval-tree extraction and the RegSeqRat/Real seal readback. It exports a displayed sequential cluster route, not open-cover compactness, choice-like subsequence selection, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:sequential-real-compactness-namecert-obligations}. It places the bounded source row $B$, interval-tree row $I$, extraction row $E$, readback row $Q$, and terminal seal $R$ in a single finite carrier route.

The bounded source is read through \autoref{ch:concrete-instances-boundedrealsequence-namecert}. Its finite windows and interval bound supply the source surface consumed by the Bolzano--Weierstrass certificate, while \autoref{ch:concrete-instances-closedboundedintervalnet-namecert} supplies the dyadic retained-cell discipline.

Now compose with \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}. The extraction row reads only the finite interval-tree schedule and then hands the selected windows to $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ before the terminal $\RealUp$ cluster seal can be consumed.

Transport and replay through $H$ and $C$, then source and name the same route through $P$ and $N$. The packet therefore records a finite sequential compactness handoff and refuses the excluded open-cover, choice, quotient, and ambient-completeness exits.
\end{proof}

\closureat{SequentialRealCompactnessUp}{seedStr}

\begin{closurestatus}{\SequentialRealCompactnessUp}
  \constructivestory{A $\SequentialRealCompactnessUp$ packet is generated from finite StreamName windows, a bounded real sequence source row, a closed bounded interval-net tree, a Bolzano--Weierstrass extraction row, a RegSeqRat readback row, a RealUp cluster seal, componentwise transport, displayed continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sequential-real-compactness-carrier}, \autoref{thm:sequential-real-compactness-namecert-obligations}, and \autoref{thm:sequential-real-compactness-bolzano-weierstrass-handoff} seed the finite bounded-sequence compactness route through interval-tree extraction and regular real readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, arbitrary subsequence choice, quotient real equality, host supremum, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-source handoff, interval-tree extraction, RegSeqRat readback, Real seal routing, and NameCert field faithfulness for BEDC.Derived.SequentialRealCompactnessUp.}
\end{closurestatus}
