\chapter{A Concrete Naming Certificate for $\MoreraTheoremUp$}
\label{ch:concrete-instances-morera-theorem-namecert}
\origin{human}

Following Morera and Bishop--Bridges constructive complex analysis, the $\MoreraTheoremUp$ packet names the finite theorem-shaped route by which triangle-contour integral vanishing is read as a holomorphic handoff. It sits beside the complex-analysis carrier of \autoref{ch:concrete-instances-complex-analytic-namecert}, the holomorphic output surface of \autoref{ch:concrete-instances-holomorphic-namecert}, the finite contour window of \autoref{ch:concrete-instances-contoursumwindow-namecert}, the complex limit rows of \autoref{ch:concrete-instances-complex-limit-namecert}, and the real tolerance ledger of \autoref{ch:concrete-instances-real-namecert}. The packet records only displayed triangle contours, finite contour-integral rows, local complex-limit evidence, real tolerance comparisons, transport, replay, provenance, and local naming; it does not import analytic continuation, a host path-integral theory, open-cover compactness, selected primitives, quotient path equality, or classical choice.

\begin{definition}[Morera theorem carrier]
\label{def:morera-theorem-carrier}
A $\MoreraTheoremUp$ carrier is a finite $\BHist$ packet
$$
M=(A,H,T,W,L,R,Z,E,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\ComplexAnalyticUp$ context row, naming the complex neighbourhood and the displayed analytic-read request.
\item $H$ is the $\HolomorphicUp$ handoff row that may be exported only after the triangle-contour vanishing rows have been inspected.
\item $T$ is the finite triangle-contour row, recording three $\ComplexUp$ vertex histories, oriented edge rows, and the bounded family of triangle windows to be tested.
\item $W$ is the $\ContourSumWindowUp$ row, recording the finite contour-window ledger and the local sum requests for the triangle boundaries.
\item $L$ is the $\ComplexLimitUp$ row, recording the local limiting comparison that turns the finite contour-window data into the analytic handoff.
\item $R$ is the $\RealUp$ tolerance row, recording positive rational bounds, modulus comparisons, and real-valued estimates used by the finite windows.
\item $Z$ is the zero-contour row, displaying that each accepted triangle-window integral request is sealed at $\mathsf{0}_{\ComplexUp}$ inside the finite ledger.
\item $E$ records componentwise $\hsame$ transport for analytic context, holomorphic handoff, triangle contours, contour windows, complex limits, real tolerances, zero rows, provenance, and naming.
\item $C$ records displayed $\Cont$ replay from triangle-window admission through zero-contour sealing and the holomorphic handoff.
\item $P$ is the $\Pkg$ provenance over $\MoreraTheoremUp$, $\ComplexAnalyticUp$, $\HolomorphicUp$, $\ContourSumWindowUp$, $\ComplexLimitUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MoreraTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,H,T,W,L,R,Z,E,C,P,N$. It has no coordinate for a selected primitive, analytic continuation, an ambient path integral, a quotient path space, an open-cover compactness proof, or a classical-choice schedule.
\end{definition}

\begin{theorem}[Morera theorem NameCert obligations]
\label{thm:morera-theorem-namecert-obligations}
For every accepted $\MoreraTheoremUp$ carrier $M=(A,H,T,W,L,R,Z,E,C,P,N)$, the local $\NameCert_{\MoreraTheoremUp}$ surface consists exactly of $\ComplexAnalyticUp$ context admission through $A$, $\HolomorphicUp$ handoff exposure through $H$, finite triangle-contour display through $T$, $\ContourSumWindowUp$ ledger display through $W$, $\ComplexLimitUp$ limiting comparison through $L$, $\RealUp$ tolerance comparison through $R$, zero-contour sealing through $Z$, componentwise $\hsame$ transport through $E$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:morera-theorem-carrier}. The analytic rows are $A,H,T,W,L,R,Z$, and the structural rows are $E,C,P,N$.

The triangle-contour row $T$ and contour-window row $W$ expose the finite integral requests before the holomorphic handoff is read. The zero row $Z$ seals those requests at $\mathsf{0}_{\ComplexUp}$ inside the displayed window, rather than by an ambient path-integral theorem.

The complex-limit row $L$ and real tolerance row $R$ supply the constructive limiting and estimate data consumed by the handoff $H$. Transport, replay, provenance, and local naming preserve the same packet, so no selected primitive, analytic-continuation principle, quotient path equality, or classical-choice schedule can be read from the carrier.
\end{proof}

\begin{theorem}[Morera theorem contour-window route]
\label{thm:morera-theorem-contour-window-route}
Every complex-analysis consumer that reads an accepted $\MoreraTheoremUp$ packet must factor the read through the finite route
$$
A,T,W,R,Z,L \longmapsto H \longmapsto E,C,P,N .
$$
The theorem-shaped content is the displayed passage from finite triangle-contour vanishing to the $\HolomorphicUp$ handoff over the named $\ComplexAnalyticUp$ context. It is not an analytic-continuation theorem, a hidden primitive selector, a host contour-integral object, a quotient path equality, or an open-cover compactness principle.
\end{theorem}
\begin{proof}
Begin with the accepted carrier. The rows $A,T,W$ display the complex-analysis context, triangle contours, and contour-window ledger before any handoff is exposed.

The real tolerance row $R$ and zero-contour row $Z$ then bind the finite estimates and vanishing certificates for the same triangle windows. The complex-limit row $L$ is read after those rows, so the limiting comparison remains attached to the finite contour-window surface.

Finally $H$ exports the holomorphic handoff, while $E,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for the excluded ambient objects, every accepted consumer remains on the finite contour-window route.
\end{proof}

\falsifiablePrediction{Any $\MoreraTheoremUp$ consumer that exports a $\HolomorphicUp$ handoff while omitting the displayed $\ContourSumWindowUp$, $\ComplexLimitUp$, and $\RealUp$ rows is rejected by the local classifier.}

\independenceWitness{complexanalytic, holomorphic, contoursumwindow, complexlimit, real: $\MoreraTheoremUp$ is not bijective to any listed sibling because it jointly binds finite triangle-contour vanishing, contour-window ledgers, complex-limit comparisons, real tolerance rows, holomorphic handoff, transport, replay, provenance, and local naming into one theorem-shaped packet.}

\closureat{MoreraTheoremUp}{seedStr}

\begin{closurestatus}{\MoreraTheoremUp}
  \constructivestory{A $\MoreraTheoremUp$ packet is generated from ComplexAnalytic context rows, Holomorphic handoff rows, triangle-contour rows, ContourSumWindow ledgers, ComplexLimit comparisons, Real tolerance rows, zero-contour seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:morera-theorem-carrier}, \autoref{thm:morera-theorem-namecert-obligations}, and \autoref{thm:morera-theorem-contour-window-route} seed the finite Morera theorem contour-window route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, analytic continuation, selected primitives, host path integration, open-cover compactness, quotient path equality, or classical choice for $\MoreraTheoremUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, triangle-contour display, contour-window exactness, ComplexLimit comparison, Real tolerance replay, zero-contour sealing, Holomorphic handoff, transport, replay, provenance, and local naming for BEDC.Derived.MoreraTheoremUp.}
\end{closurestatus}
