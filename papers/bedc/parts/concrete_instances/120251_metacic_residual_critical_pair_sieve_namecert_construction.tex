\chapter{A Concrete Naming Certificate for $\MetacicResidualCriticalPairSieveUp$}
\label{ch:concrete-instances-metacic-residual-critical-pair-sieve-namecert}
\origin{ai}

The $\MetacicResidualCriticalPairSieveUp$ packet realizes the confluence audit map of \autoref{ch:metacic-confluence-audit-map} as a finite residual sieve surface. It sits beside the critical-path carrier of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}, the candidate-normalization handoff of \autoref{ch:concrete-instances-metacic-candidate-normalization-confluence-handoff-namecert}, and the decidable boundary of \autoref{ch:concrete-instances-metacicdecidableboundary-namecert}. The sieve records bounded residual candidates and blocked edges before any confluence-facing read is public; it does not assert full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or consistency.

\begin{definition}[Metacic residual critical-pair sieve carrier]
\label{def:metacic-residual-critical-pair-sieve-carrier}
A $\MetacicResidualCriticalPairSieveUp$ carrier is a finite $\BHist$ packet
$$
S=(A,Q,C,H,D,R,B,T,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the MetaCIC audit anchor read through $\MetaCICCriticalPathUp$.
\item $Q$ is the finite residual critical-pair candidate list, naming the inspected source, left residual edge, right residual edge, and local overlap code for each candidate.
\item $C$ is the $\MetaCICConfluenceAuditPacketUp$ row that records how the candidate list is read by the confluence audit surface.
\item $H$ is the $\MetacicCandidateNormalizationConfluenceHandoffUp$ row, retaining candidate-normalization evidence before any bounded endpoint is cited.
\item $D$ is the $\MetaCICDecidableBoundaryUp$ row used only as a bounded checker and not as global conversion decidability.
\item $R$ is the residual-window row, recording the finite reduction neighborhood in which each critical pair is inspected.
\item $B$ is the blocked-edge ledger, recording candidates whose local join is not discharged by the displayed window and checker rows.
\item $T$ records componentwise $\hsame$ transport for the anchor, candidate list, audit packet, handoff, decidable boundary, residual window, blocked-edge ledger, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a sieve request through $A,Q,C,H,D,R,B,T,P,N$.
\item $P$ records $\Pkg$ provenance over $\MetacicResidualCriticalPairSieveUp$, $\MetaCICCriticalPathUp$, $\MetaCICConfluenceAuditPacketUp$, $\MetacicCandidateNormalizationConfluenceHandoffUp$, $\MetaCICDecidableBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\MetacicResidualCriticalPairSieveUp}$ row.
\end{enumerate}
The packet has no coordinate for a global confluence theorem, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or consistency.
\end{definition}

\begin{theorem}[Metacic residual critical-pair sieve NameCert obligations]
\label{thm:metacic-residual-critical-pair-sieve-namecert-obligations}
For every accepted $\MetacicResidualCriticalPairSieveUp$ carrier $S=(A,Q,C,H,D,R,B,T,K,P,N)$, the local $\NameCert_{\MetacicResidualCriticalPairSieveUp}$ surface consists exactly of carrier admission, residual-candidate exposure, audit-packet routing, candidate-normalization handoff retention, decidable-boundary boundedness, blocked-edge retention, componentwise transport, displayed replay, package provenance, and local naming. Every public read factors through
$$
A,Q,C,H,D,R,B,T,K,P,N
$$
before it may be cited as sieve evidence.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:metacic-residual-critical-pair-sieve-carrier}. The accepted packet exposes the audit anchor, finite residual candidate list, confluence audit row, candidate-normalization handoff, decidable boundary, residual window, blocked-edge ledger, and structural rows.

Residual-candidate exposure is the row $Q$ together with the residual window $R$. These rows name the inspected critical pairs and the bounded neighborhood in which their residual edges are compared.

Audit routing and handoff retention use $C,H,D,B$. The confluence audit packet reads the candidate list, the candidate-normalization handoff retains its premise surface, the decidable boundary supplies only bounded checking, and the blocked-edge row keeps non-discharged candidates visible.

The structural rows $T,K,P,N$ transport, replay, source, and locally name the same finite packet. Since the carrier omits global confluence, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, and consistency, no local NameCert obligation can export those endpoints.
\end{proof}

\begin{theorem}[Metacic residual critical-pair sieve boundary]
\label{thm:metacic-residual-critical-pair-sieve-boundary}
Every confluence-facing read of an accepted $\MetacicResidualCriticalPairSieveUp$ packet must first display the $\MetaCICCriticalPathUp$ anchor, the finite residual critical-pair candidate list, the $\MetaCICConfluenceAuditPacketUp$ row, the $\MetacicCandidateNormalizationConfluenceHandoffUp$ row, the $\MetaCICDecidableBoundaryUp$ bounded checker row, residual-window data, blocked-edge retention, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. The read is a finite sieve over residual critical pairs, not full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or consistency.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:metacic-residual-critical-pair-sieve-carrier}. The anchor $A$ fixes the critical-path surface, while $Q$ and $R$ name the finite residual critical-pair window being inspected.

The confluence audit row $C$ and the candidate-normalization handoff $H$ keep the read attached to the audit and candidate premise surfaces. The decidable boundary row $D$ is only a bounded checker for the displayed window, and the blocked-edge row $B$ records candidates not discharged by that bounded check.

Apply the NameCert obligations of \autoref{thm:metacic-residual-critical-pair-sieve-namecert-obligations}. They exhaust transport, replay, provenance, and local naming through $T,K,P,N$.

Because none of the carrier coordinates is a global Church--Rosser proof, unrestricted subject-reduction proof, global conversion decider, quotient syntax equality, host evaluator soundness principle, or consistency theorem, no public read of the sieve may cite such an endpoint.
\end{proof}

\falsifiablePrediction{Every accepted $\MetacicResidualCriticalPairSieveUp$ read is refuted if it exports a confluence-facing candidate while omitting the critical-path anchor, finite residual candidate list, confluence audit packet, candidate-normalization handoff, decidable-boundary checker, residual window, blocked-edge ledger, transport, replay, provenance, or local NameCert rows.}

\independenceWitness{metaciccriticalpath, metacicconfluenceauditpacket, metacic-candidate-normalization-confluence-handoff, metacicdecidableboundary, metacic-residual-critical-pair: none is bijective to $\MetacicResidualCriticalPairSieveUp$ because the sieve jointly carries residual critical-pair candidates, residual windows, bounded checker status, blocked-edge retention, audit routing, candidate-normalization handoff, transport, replay, provenance, and local naming in one finite packet.}

\closureat{MetacicResidualCriticalPairSieveUp}{seedStr}

\begin{closurestatus}{\MetacicResidualCriticalPairSieveUp}
  \constructivestory{A $\MetacicResidualCriticalPairSieveUp$ packet is generated from a MetaCICCriticalPath anchor, finite residual critical-pair candidates, a MetaCICConfluenceAuditPacket row, a MetacicCandidateNormalizationConfluenceHandoff row, a MetaCICDecidableBoundary bounded checker, residual windows, blocked-edge retention, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-critical-pair-sieve-carrier}, \autoref{thm:metacic-residual-critical-pair-sieve-namecert-obligations}, and \autoref{thm:metacic-residual-critical-pair-sieve-boundary} seed the finite residual critical-pair sieve route for the MetaCIC confluence audit map.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, residual-candidate exposure, audit routing, candidate-normalization handoff retention, decidable-boundary boundedness, blocked-edge retention, transport, replay, provenance, and local naming for BEDC.Derived.MetacicResidualCriticalPairSieveUp.}
\end{closurestatus}
