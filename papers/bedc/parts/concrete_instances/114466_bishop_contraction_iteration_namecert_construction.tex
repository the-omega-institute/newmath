\chapter{A Concrete Naming Certificate for $\BishopContractionIterationUp$}
\label{ch:concrete-instances-bishop-contraction-iteration-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopContractionIterationUp$ packet records the finite iteration-budget surface for a displayed contraction before any fixed-point endpoint or host limit is claimed. It sits after the complete-metric carrier of \autoref{ch:concrete-instances-completemetric-namecert}, the one-step Picard contraction surface of \autoref{ch:concrete-instances-picardcontraction-namecert}, the Cauchy-modulus schedule of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the finite stream route of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the visible budget by which contraction iterates become completion-facing data; it does not select a fixed point, quotient an orbit, or import countable choice.

\begin{definition}[BishopContractionIteration carrier]\label{def:bishop-contraction-iteration-carrier}
A $\BishopContractionIterationUp$ carrier is a finite $\BHist$ packet
$$
B=(X,T,x_0,W,I,A,M,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompleteMetricUp$ source row carrying only the displayed distance reads needed by the contraction route.
\item $T$ is the $\PicardContractionUp$ one-step row, exposing the accepted contraction graph and its adjacent-iterate comparison surface.
\item $x_0$ is the initial-point row from which the displayed orbit windows are generated.
\item $W$ is the finite $\StreamNameUp$ window schedule requested by a downstream completion or fixed-point consumer.
\item $I$ is the finite iterate ledger, listing the visible orbit rows $x_0,x_1,\ldots,x_n$ and the replayed applications of $T$ used to obtain them.
\item $A$ is the adjacent-distance ledger, storing the contraction-bound comparisons and tail estimates read from $I$ inside the metric row $X$.
\item $M$ is the $\CauchyModulusUp$ budget row extracted from $A$ for the requested windows $W$.
\item $R$ is the $\RegSeqRatUp$ readback row for the finite orbit budget and its regular rational tail presentation.
\item $E$ is the $\RealUp$ seal row reached only after $W,I,A,M,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the metric source, contraction row, initial point, window schedule, iterate ledger, adjacent-distance ledger, modulus row, readback row, seal row, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from complete-metric and Picard-contraction admission through finite iteration, tail-budget extraction, regular readback, and real-seal separation.
\item $P$ is the $\Pkg$ provenance over $\BishopContractionIterationUp$, $\CompleteMetricUp$, $\PicardContractionUp$, $\CauchyModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopContractionIterationUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $X,T,x_0,W,I,A,M,R,E,H,C,P,N$. It has no coordinate for a selected limit, a Banach fixed point, host function extensionality, quotient orbit equality, countable choice, or ambient completeness beyond the displayed $\CompleteMetricUp$ row.
\end{definition}

\begin{theorem}[BishopContractionIteration NameCert obligations]\label{thm:bishop-contraction-iteration-namecert-obligations}
For every accepted $\BishopContractionIterationUp$ carrier $B=(X,T,x_0,W,I,A,M,R,E,H,C,P,N)$, the local $\NameCert_{\BishopContractionIterationUp}$ surface consists exactly of carrier admission for the displayed complete-metric, Picard-contraction, initial-point, stream-window, iterate-ledger, adjacent-distance, Cauchy-modulus, regular-readback, Real-seal, transport, replay, provenance, and local naming rows; iteration visibility through $I$; tail-budget visibility through $A$ and $M$; readback visibility through $R$; Real seal non-escape through $E$; and componentwise preservation through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-contraction-iteration-carrier}. Carrier admission is exactly the displayed finite inventory $X,T,x_0,W,I,A,M,R,E,H,C,P,N$.

The iterate ledger $I$ is generated from the initial row $x_0$ by replaying the Picard-contraction row $T$ inside the complete-metric source $X$. The adjacent-distance ledger $A$ reads only those displayed orbit rows and the contraction comparisons attached to them.

The modulus row $M$ is therefore a budget extracted from visible finite comparisons for the requested windows $W$. The regular readback $R$ and terminal seal $E$ are downstream rows, so they cannot add a selected endpoint or a fixed-point theorem.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since every readable coordinate has been listed, the certificate exports the contraction-iteration budget and not host function equality, quotient orbit equality, countable choice, or ambient completion.
\end{proof}

\begin{theorem}[BishopContractionIteration budget extraction]\label{thm:bishop-contraction-iteration-budget-extraction}
For every accepted $\BishopContractionIterationUp$ carrier $B=(X,T,x_0,W,I,A,M,R,E,H,C,P,N)$, every requested finite window in $W$ receives its contraction budget only through the route
$$
\PicardContractionUp \longmapsto I \longmapsto A \longmapsto \CauchyModulusUp .
$$
The row $M$ is read after the finite orbit ledger and adjacent-distance comparisons have been displayed, and no budget is read from a selected fixed point, completed orbit endpoint, or host metric completion.
\end{theorem}
\begin{proof}
Start with the rows $T,x_0,W,I$. The only source of successive iterates is the displayed Picard-contraction row $T$ replayed from the initial point $x_0$ for the finite windows requested in $W$.

The row $A$ then records the adjacent-distance comparisons over those visible iterates. Because $A$ is tied to $I$ and the metric row $X$, it cannot read a completed endpoint or an external fixed-point equation.

The Cauchy-modulus row $M$ is extracted from $A$ for the same finite windows. Thus the budget route passes through $\PicardContractionUp$, the finite iterate ledger, the adjacent-distance ledger, and $\CauchyModulusUp$ before any regular readback or Real seal is available.
\end{proof}

\begin{theorem}[BishopContractionIteration completion boundary]\label{thm:bishop-contraction-iteration-completion-boundary}
Every completion-facing or fixed-point-facing consumer of an accepted $\BishopContractionIterationUp$ carrier must first read the displayed rows
$$
X,\ T,\ x_0,\ W,\ I,\ A,\ M,\ R,\ E,\ H,\ C,\ P,\ N .
$$
The consumer receives a finite $\StreamNameUp$ window schedule, a $\RegSeqRatUp$ readback, and a $\RealUp$ seal boundary for the contraction orbit, but it receives no Banach fixed point, selected limit, quotient orbit equality, host function extensionality, countable choice, or ambient completeness beyond the named complete-metric row.
\end{theorem}
\begin{proof}
Read the carrier inventory from \autoref{def:bishop-contraction-iteration-carrier}. The complete-metric row $X$, Picard-contraction row $T$, initial row $x_0$, requested windows $W$, iterate ledger $I$, and adjacent-distance row $A$ are all visible before the Cauchy-modulus row can be consumed.

Apply \autoref{thm:bishop-contraction-iteration-budget-extraction}. It places the modulus row $M$ downstream of the finite orbit and contraction comparisons rather than downstream of a selected endpoint.

The readback and seal rows $R,E$ are therefore boundary rows for the displayed budget. Transport, replay, provenance, and local naming preserve that same route through $H,C,P,N$, so a downstream endpoint theorem must supply its own fixed-point and uniqueness obligations.
\end{proof}

\begin{theorem}[BishopContractionIteration Real seal nonescape]\label{thm:bishop-contraction-iteration-real-seal-nonescape}
For every accepted $\BishopContractionIterationUp$ carrier $B=(X,T,x_0,W,I,A,M,R,E,H,C,P,N)$, the $\RealUp$ seal row $E$ seals only the finite contraction-iteration readback built from $W,I,A,M,R$. It excludes Banach fixed points, selected limits, host function equality, quotient orbit equality, countable choice, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier. The seal row $E$ is downstream of the stream windows, finite iterate ledger, adjacent-distance comparisons, modulus row, and regular readback.

Use \autoref{thm:bishop-contraction-iteration-completion-boundary}. Any completion-facing consumer must read the same finite rows before it can inspect $E$.

Since the classifier has no coordinate for fixed points, selected limits, quotient orbit equality, host function equality, countable choice, or ambient real completeness, none of those objects can escape through the terminal seal.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopContractionIterationUp$ read factors through one CompleteMetric row, one PicardContraction row, one initial-point row, one StreamName window schedule, one finite iterate ledger, one adjacent-distance ledger, one CauchyModulus budget, one RegSeqRat readback, one RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming before any endpoint theorem consumes it.}

\independenceWitness{completemetric, picardcontraction, cauchymodulus, streamname, regseqrat, real: $\BishopContractionIterationUp$ is not bijective to any listed sibling because it jointly carries the initial point, finite orbit windows, contraction-distance budget, modulus extraction, regular readback, Real seal, transport, replay, provenance, and local naming.}

\closureat{BishopContractionIterationUp}{seedStr}

\begin{closurestatus}{\BishopContractionIterationUp}
  \constructivestory{A $\BishopContractionIterationUp$ packet is generated from a CompleteMetric source row, a PicardContraction row, an initial point, StreamName windows, a finite iterate ledger, adjacent-distance comparisons, a CauchyModulus budget, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-contraction-iteration-carrier}, \autoref{thm:bishop-contraction-iteration-namecert-obligations}, \autoref{thm:bishop-contraction-iteration-budget-extraction}, and \autoref{thm:bishop-contraction-iteration-completion-boundary} seed the finite contraction-iteration budget consumed by later completion and fixed-point routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Banach fixed points, selected limits, quotient orbit equality, host function extensionality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, iteration visibility, tail-budget visibility, Cauchy-modulus extraction, RegSeqRat readback, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.BishopContractionIterationUp.}
\end{closurestatus}
