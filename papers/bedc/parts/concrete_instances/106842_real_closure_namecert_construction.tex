\chapter{A Concrete Naming Certificate for $\RealClosureUp$}
\label{ch:concrete-instances-real-closure-namecert}
\origin{human}

Following Kelley and Bourbaki topology, the $\RealClosureUp$ packet names a closure operation by displayed finite sequential data rather than by ambient completeness of a host real line. It sits over the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the topological carrier of \autoref{ch:concrete-instances-topology-namecert}, and the sequential closure packet of \autoref{ch:concrete-instances-sequentialclosure-namecert}. The chapter uses these rows to expose an L10 closure object for Real-facing and completion-facing consumers without asserting quotient reals, host equality, countable choice, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Real closure carrier]
\label{def:real-closure-carrier}
A $\RealClosureUp$ carrier is a finite $\BHist$ packet
$$
R=(X,T,S,Q,A,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\RealUp$ point-name row whose accepted entries are inspected only through terminal real-name seals.
\item $T$ is the $\TopologyUp$ neighbourhood row for the displayed Real-facing carrier.
\item $S$ stores the $\StreamNameUp$ finite windows used to observe candidate closure witnesses.
\item $Q$ stores the $\RegSeqRatUp$ regular rational readback of the same windows.
\item $A$ is the $\SequentialClosureUp$ approach row, displaying a sequence or net-free sequential route from the named source rows toward the closure point.
\item $E$ is the exactness and refusal row stating that the closure verdict is read from $X,T,S,Q,A$ and not from quotient representatives, host real equality, choice-selected subsequences, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.
\item $H$ stores componentwise $\hsame$ transport for Real names, topological neighbourhood rows, stream windows, regular rational readbacks, sequential approach rows, exactness rows, replay rows, provenance rows, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from finite Real-name observations through topology, stream, regular rational, sequential closure, and refusal rows.
\item $P$ is the $\Pkg$ provenance over $\RealClosureUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\TopologyUp$, $\SequentialClosureUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealClosureUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,T,S,Q,A,E,H,C,P,N$. It has no coordinate for completed subsets of a host real line, quotient real equality, selected limits, countable-choice schedules, trichotomy, open-cover compactness, or ambient Real completeness.
\end{definition}

\begin{theorem}[Real closure NameCert obligations]
\label{thm:real-closure-namecert-obligations}
For every accepted $\RealClosureUp$ carrier
$$
R=(X,T,S,Q,A,E,H,C,P,N),
$$
the local $\NameCert_{\RealClosureUp}$ surface consists exactly of carrier admission for the displayed $\RealUp$ point names, $\TopologyUp$ neighbourhood rows, $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\SequentialClosureUp$ approach row, exactness and refusal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-closure-carrier}. The projection exposes Real names $X$, neighbourhood rows $T$, stream windows $S$, regular rational readbacks $Q$, and the sequential approach row $A$.

The closure verdict is then forced through $E$. That row records exactness of the displayed sequential route and refuses any replacement by quotient real representatives, host equality, countable-choice selection, trichotomy, open-cover compactness, or an ambient completeness theorem.

Finally transport and replay are only the componentwise rows $H$ and $C$, while $P$ and $N$ record provenance and local naming for the same finite surface. Since no other coordinate is present, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Real closure stream-regular handoff]
\label{thm:real-closure-stream-regseqrat-handoff}
Every Real-facing or completion-facing consumer of an accepted $\RealClosureUp$ packet reaches the closure verdict only through the ordered handoff
$$
\RealUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \TopologyUp \longrightarrow \SequentialClosureUp
$$
displayed by $X,S,Q,T,A,E,H,C,P,N$. The handoff supplies finite observation windows, regular rational readback, neighbourhood membership, sequential approach evidence, transport, replay, provenance, and local naming; it supplies no quotient real, host equality, countable-choice schedule, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $R=(X,T,S,Q,A,E,H,C,P,N)$. A consumer may inspect a Real name only through $X$, and every finite observation of that name is recorded by the stream-window and regular-rational rows $S,Q$.

The topological and sequential closure rows then consume the same readback. The neighbourhood row $T$ records the local closure test, while $A$ records the displayed sequential route that meets the test.

Use the exactness row $E$ and the transport-replay rows $H,C$. They preserve the ordered handoff under classifier comparison and prevent a consumer from replacing the finite route by quotient equality, selected representatives, countable choice, trichotomy, compactness, or ambient completeness.

The provenance and local name rows $P,N$ package exactly this route. Hence every downstream closure read receives the stated L10 handoff and nothing stronger.
\end{proof}

\closureat{RealClosureUp}{seedStr}

\begin{closurestatus}{\RealClosureUp}
  \constructivestory{A $\RealClosureUp$ packet is generated from Real point names, topology neighbourhood rows, finite StreamName windows, RegSeqRat readbacks, a SequentialClosure approach row, exactness refusal, componentwise transport, replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-closure-carrier}, \autoref{thm:real-closure-namecert-obligations}, and \autoref{thm:real-closure-stream-regseqrat-handoff} seed the constructive L10 closure carrier, finite NameCert obligation surface, and stream-to-regular-rational handoff for Real-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient reals, host real equality, countable choice, trichotomy, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, topology-neighbourhood exactness, finite stream-window readback, regular rational readback, sequential closure handoff, refusal, transport stability, replay, provenance, and local naming for BEDC.Derived.RealClosureUp.}
\end{closurestatus}
