\chapter{A Concrete Naming Certificate for $\CauchyNetTailFilterUp$}
\label{ch:concrete-instances-cauchy-net-tail-filter-namecert}
\origin{human}

Following Moore and Smith, Kelley, and Bishop--Bridges on nets, filters, and constructive completion, the $\CauchyNetTailFilterUp$ packet records the finite handoff from displayed Cauchy-net evidence to displayed tail-filter evidence. It sits beside the completion route of \autoref{ch:concrete-instances-cauchynetcompletion-namecert}, the finite filter-base surface of \autoref{ch:concrete-instances-filterbase-namecert}, the cofinite sequence-tail specialization of \autoref{ch:concrete-instances-frechetfilter-namecert}, the finite schedule interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a BEDC-native bridge from Cauchy-net windows to tail-filter windows; it does not choose a limit, quotient nets, assume countable choice, decide trichotomy, or assert ambient $\RealUp$ completeness.

\begin{definition}[CauchyNetTailFilter carrier]
\label{def:cauchy-net-tail-filter-carrier}
A $\CauchyNetTailFilterUp$ carrier is a finite $\BHist$ packet
$$
T=(J,W,K,B,F,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $J$ is the $\CauchyNetUp$ index-window row, naming the directed indices and inspected pairs used by the net-side Cauchy request.
\item $W$ is the finite tail-threshold row, recording the displayed index after which the inspected net windows are admitted.
\item $K$ is the Cauchy-net tolerance ledger attached to $J$ and $W$, so every tail request keeps the original Cauchy evidence visible.
\item $B$ is the $\FilterBaseUp$ meet row generated from finite tails and finite intersections of tail events.
\item $F$ is the $\FrechetFilterUp$ cofinite handoff row used when the directed tail is narrowed to a sequence-tail read.
\item $S$ is the $\StreamNameUp$ schedule row exposing the finite observation windows passed to downstream Real or completion consumers.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same schedule and tolerance ledger.
\item $E$ is the terminal $\RealUp$ seal, readable only after $J,W,K,B,F,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the Cauchy-net, threshold, filter-base, cofinite handoff, schedule, readback, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tail-filter request through $J,W,K,B,F,S,R,E,H$.
\item $P$ is the $\Pkg$ provenance over $\CauchyNetTailFilterUp$, $\CauchyNetUp$, $\CauchyNetCompletionUp$, $\FilterBaseUp$, $\FrechetFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyNetTailFilterUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through these rows. It has no coordinate for selected limits, quotient nets, quotient filters, countable-choice schedules, trichotomy, host equality of tails, or ambient Real completeness.
\end{definition}

\begin{theorem}[CauchyNetTailFilter NameCert obligations]
\label{thm:cauchy-net-tail-filter-namecert-obligations}
For every accepted $\CauchyNetTailFilterUp$ carrier $T=(J,W,K,B,F,S,R,E,H,C,P,N)$, the local $\NameCert_{\CauchyNetTailFilterUp}$ surface consists exactly of carrier admission for the Cauchy-net index windows $J$, finite tail threshold $W$, Cauchy tolerance ledger $K$, $\FilterBaseUp$ meet row $B$, $\FrechetFilterUp$ cofinite handoff $F$, $\StreamNameUp$ schedule $S$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local naming $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-net-tail-filter-carrier}. The net-side evidence is exactly $J,W,K$: displayed index windows, a finite threshold, and the Cauchy tolerance ledger attached to those windows.

The filter-facing handoff is then $B,F$. The $\FilterBaseUp$ row records finite meets of tail events, while the $\FrechetFilterUp$ row records the cofinite sequence-tail specialization available to sequential consumers.

Downstream Real and completion reads must pass through $S,R,E$. The $\StreamNameUp$ schedule exposes only finite windows, the $\RegSeqRatUp$ row records regular rational readback for those windows, and the $\RealUp$ seal is reached only after the schedule and readback have been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since there is no additional coordinate, the local NameCert surface cannot export selected limits, quotient nets, countable choice, trichotomy, or Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyNetTailFilterUp$ completion-facing read exposes CauchyNet index windows, a finite tail threshold, a Cauchy tolerance ledger, a FilterBase meet row, a FrechetFilter cofinite handoff, StreamName windows, RegSeqRat readback, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert naming before it can serve a completion or Real consumer.}

\independenceWitness{cauchynetcompletion, filterbase, frechetfilter, streamname, regseqrat, real: $\CauchyNetTailFilterUp$ is not bijective to any listed sibling because it jointly carries net index windows, tail thresholds, filter-base meets, cofinite sequence-tail handoff, finite schedules, regular readback, Real sealing, transport, replay, provenance, and local naming in one packet.}

\closureat{CauchyNetTailFilterUp}{seedStr}

\begin{closurestatus}{\CauchyNetTailFilterUp}
  \constructivestory{A $\CauchyNetTailFilterUp$ packet is generated from CauchyNet index windows, finite tail thresholds, a Cauchy tolerance ledger, a FilterBase meet row, a FrechetFilter cofinite handoff, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-net-tail-filter-carrier} and \autoref{thm:cauchy-net-tail-filter-namecert-obligations} seed the finite Cauchy-net tail-filter bridge over CauchyNet, CauchyNetCompletion, FilterBase, FrechetFilter, StreamName, RegSeqRat, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected limits, quotient nets, quotient filters, countable choice, trichotomy, host equality of tails, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Cauchy-net index-window exposure, finite tail-threshold exactness, FilterBase meet generation, FrechetFilter cofinite handoff, StreamName scheduling, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.CauchyNetTailFilterUp.}
\end{closurestatus}
