\chapter{A Concrete Naming Certificate for $\DyadicIntervalContinuityModulusUp$}
\label{ch:concrete-instances-dyadic-interval-continuity-modulus-namecert}
\origin{human}

Following Bishop--Bridges constructive practice, the $\DyadicIntervalContinuityModulusUp$ packet names a finite dyadic interval modulus for a continuous map on a located closed interval. It sits beside the dyadic interval cells of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, the located finite-net interval surface of \autoref{ch:concrete-instances-located-closed-interval-finite-net-namecert}, the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records finite modulus data only; it is not an open-cover compactness theorem, a choice-selected modulus, quotient-real equality, or ambient completeness coordinate for $\RealUp$.

\begin{definition}[Dyadic interval continuity modulus carrier]
\label{def:dyadic-interval-continuity-modulus-carrier}
A $\DyadicIntervalContinuityModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(I,N,L,S,R,E,B,H,C,P,Q)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the located closed interval row carrying the two dyadic endpoint certificates and the located membership test used by the packet.
\item $N$ is the $\DyadicIntervalNetUp$ row listing the dyadic subinterval cells inspected at the chosen mesh scale.
\item $L$ is the $\LocatedClosedIntervalFiniteNetUp$ row certifying that the listed cells are the finite located interval net consumed by the packet.
\item $S$ is the $\StreamNameUp$ finite-window row opened for the function reads at the cell endpoints and selected interior probes.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from those finite stream windows.
\item $E$ is the terminal $\RealUp$ seal row for the displayed readbacks.
\item $B$ is the modulus-budget row: for each requested output dyadic tolerance it displays a finite input mesh scale and the cell-window ledger that witnesses the corresponding oscillation bound.
\item $H$ records componentwise $\hsame$ transport for the interval, net, located-net, stream-window, rational-readback, real-seal, budget, replay, provenance, and local-name rows.
\item $C$ records displayed $\Cont$ replay from a requested tolerance through $I,N,L,S,R,E,B$.
\item $P$ is the $\Pkg$ provenance over $\DyadicIntervalContinuityModulusUp$, $\DyadicIntervalNetUp$, $\LocatedClosedIntervalFiniteNetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $Q$ is the local $\NameCert_{\DyadicIntervalContinuityModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for open-cover compactness, choice-selected subcovers, quotient-real equality, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Dyadic interval modulus window stability]
\label{thm:dyadic-interval-continuity-modulus-window-stability}
Every accepted $\DyadicIntervalContinuityModulusUp$ packet preserves its modulus decision under componentwise $\hsame$ transport of the displayed interval, net, finite-window, readback, real-seal, budget, replay, provenance, and local-name rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-interval-continuity-modulus-carrier}. The modulus budget row $B$ is not a standalone host function; it is read only together with the interval row $I$, the dyadic net $N$, the located finite-net certificate $L$, the StreamName windows $S$, the RegSeqRat readback $R$, and the RealUp seal $E$.

Now transport the packet componentwise along the displayed $\hsame$ row $H$. Because $H$ compares exactly the rows listed in the carrier, it preserves the cell ledger, the opened stream windows, the rational readback, and the requested output tolerance attached to $B$.

The replay row $C$ then replays the same finite route after transport. Hence a consumer that reads the transported packet sees the same finite modulus decision, and no new open cover, selected subcover, quotient-real equality, or completeness principle is introduced.
\end{proof}

\begin{theorem}[Dyadic interval modulus regular-Cauchy handoff]
\label{thm:dyadic-interval-continuity-modulus-regular-cauchy-handoff}
Every accepted $\DyadicIntervalContinuityModulusUp$ packet exports completion-facing data only through the finite route
$$
\DyadicIntervalNetUp \longrightarrow \LocatedClosedIntervalFiniteNetUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp .
$$
The modulus budget is therefore a finite regular-Cauchy window handoff, not an appeal to open-cover compactness or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
The source of a completion-facing read is the dyadic interval net $N$ together with the located finite-net row $L$. These rows determine the finite cells and probes available to the packet.

The only observation route then passes through the StreamName windows $S$ and the RegSeqRat readback row $R$. The terminal seal $E$ records that the displayed rational windows have been presented as a RealUp-facing name, but it does not add a selected limit or a completeness theorem.

Finally the budget row $B$ is tied to those same finite windows by the replay row $C$ and the local NameCert row $Q$. Since no excluded principle is a coordinate of the carrier, every completion consumer receives exactly the finite regular-Cauchy handoff displayed in the statement.
\end{proof}

\begin{closurestatus}{\DyadicIntervalContinuityModulusUp}
  \constructivestory{A $\DyadicIntervalContinuityModulusUp$ packet is generated from a located closed interval row, a DyadicIntervalNet row, a LocatedClosedIntervalFiniteNet row, StreamName finite windows, RegSeqRat readback, a RealUp terminal seal, a finite modulus-budget ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-interval-continuity-modulus-carrier}, \autoref{thm:dyadic-interval-continuity-modulus-window-stability}, and \autoref{thm:dyadic-interval-continuity-modulus-regular-cauchy-handoff} seed the finite dyadic interval modulus route from located interval nets to regular-Cauchy and RealUp consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, choice-selected modulus extraction, quotient-real equality, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window stability, regular-Cauchy handoff, transport, replay, provenance, and local naming for BEDC.Derived.DyadicIntervalContinuityModulusUp.}
\end{closurestatus}
