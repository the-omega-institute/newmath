\chapter{A Concrete Naming Certificate for $\BishopContinuityModulusUp$}
\label{ch:concrete-instances-bishop-continuity-modulus-namecert}
\origin{human}

Following Bishop and Bridges, Constructive Analysis, the $\BishopContinuityModulusUp$ packet records the local continuity-modulus handoff for a displayed real-valued map at a displayed source point. It sits after the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the continuity carrier of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names a local modulus request as finite BEDC data; it is not compactness, uniform continuity, trichotomy, quotient real equality, a selected limit, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop continuity modulus carrier]
\label{def:bishop-continuity-modulus-carrier}
A $\BishopContinuityModulusUp$ carrier is a finite $\BHist$ packet
$$
B=(F,x,e,m,S,D,R,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\ContinuousMapUp$ source row, recording the displayed real-valued map and its local continuity surface.
\item $x$ is the source-point row, naming the point at which the local continuity request is read.
\item $e$ is the $\DyadicRatCoreUp$ tolerance request row, recording the positive dyadic target precision.
\item $m$ is the local modulus witness row, giving the finite neighbourhood budget whose use is licensed only for the source point $x$ and tolerance request $e$.
\item $S$ is the $\StreamNameUp$ window row, listing the finite observation windows used to test the source and image names.
\item $D$ is the dyadic tolerance ledger comparing the request $e$ with the windows selected by $m$.
\item $R$ is the $\RegSeqRatUp$ readback row, translating the window observations into regular rational data.
\item $W$ is the terminal $\RealUp$ seal row, exposing the target real-name read only after $S,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for $F,x,e,m,S,D,R,W$ and the local naming row.
\item $C$ records displayed $\Cont$ replay through the continuity source, source point, tolerance request, local modulus, windows, dyadic comparisons, regular readback, real seal, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\BishopContinuityModulusUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopContinuityModulusUp}$ row.
\end{enumerate}
The classifier compares carriers only through $F,x,e,m,S,D,R,W,H,C,P,N$. It has no coordinate for a compact domain, a uniform modulus over all source points, real trichotomy, quotient real equality, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop continuity modulus NameCert obligations]
\label{thm:bishop-continuity-modulus-namecert-obligations}
For the carrier of \autoref{def:bishop-continuity-modulus-carrier}, the local $\NameCert_{\BishopContinuityModulusUp}$ surface consists exactly of continuity-source exposure through $F$, source-point exposure through $x$, dyadic tolerance request through $e$, local modulus witness through $m$, StreamName windows through $S$, dyadic comparison through $D$, RegSeqRat readback through $R$, terminal Real seal through $W$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier to its displayed rows. The continuity-source row $F$, source point $x$, tolerance request $e$, and local modulus witness $m$ determine the local request before any real-facing data can be read.

Read the observation side next. The StreamName windows $S$ expose the finite source and image windows, the dyadic ledger $D$ compares those windows with the requested tolerance, and the RegSeqRat row $R$ supplies the regular rational readback used by the endpoint.

Finally apply the terminal seal and structural rows. The Real row $W$ is reached only after the window, dyadic, and regular-rational rows are displayed, while $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no further coordinate occurs in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop continuity modulus real handoff]
\label{thm:bishop-continuity-modulus-real-handoff}
Let $B=(F,x,e,m,S,D,R,W,H,C,P,N)$ be an accepted $\BishopContinuityModulusUp$ carrier. Any downstream consumer that reads the local continuity output as $\RealUp$ data must first factor through the source point $x$, tolerance request $e$, local modulus witness $m$, $\StreamNameUp$ window row $S$, $\DyadicRatCoreUp$ tolerance ledger $D$, $\RegSeqRatUp$ readback row $R$, and terminal $\RealUp$ seal $W$ on the same packet route.

This handoff is local at $x$ and exports no compactness theorem, no uniform-continuity modulus over all points, no trichotomy principle, no quotient real equality, no selected limit, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from \autoref{thm:bishop-continuity-modulus-namecert-obligations}. It places the source point, tolerance request, local modulus witness, windows, dyadic comparisons, regular readback, and real seal in one local certificate surface.

Use the sibling routes named in the chapter opening. The continuity source is read through \autoref{ch:concrete-instances-continuousmap-namecert}; finite observations are read through \autoref{ch:concrete-instances-streamname-namecert}; dyadic tolerance data through \autoref{ch:concrete-instances-dyadicratcore-namecert}; regular readback through \autoref{ch:concrete-instances-regseqrat-namecert}; and the endpoint seal through \autoref{ch:concrete-instances-real-namecert}.

The route remains local because $m$ is tied to the displayed source point $x$ and request $e$. A downstream consumer therefore reaches $\RealUp$ only through $S,D,R,W$ together with $H,C,P,N$, and no excluded compactness, uniformity, trichotomy, quotient-equality, selected-limit, or completeness coordinate is available.
\end{proof}

\falsifiablePrediction{Any accepted BishopContinuityModulusUp packet that exports a Real-facing continuity read without displaying the source point, tolerance request, local modulus witness, StreamName windows, DyadicRatCore tolerance ledger, RegSeqRat readback, and terminal Real seal refutes the carrier.}

\independenceWitness{continuousmap, streamname, dyadicratcore, regseqrat, real: none is bijective with $\BishopContinuityModulusUp$ because this carrier binds a local source-point request, tolerance, modulus witness, finite windows, dyadic comparison, regular readback, terminal seal, transport, replay, provenance, and local NameCert rows in one packet.}

\closureat{BishopContinuityModulusUp}{seedStr}

\begin{closurestatus}{\BishopContinuityModulusUp}
  \constructivestory{A $\BishopContinuityModulusUp$ packet is generated from a continuity-source row, source point, dyadic tolerance request, local modulus witness, StreamName windows, DyadicRatCore tolerance ledger, RegSeqRat readback, terminal Real seal, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-continuity-modulus-carrier}, \autoref{thm:bishop-continuity-modulus-namecert-obligations}, \autoref{thm:bishop-continuity-modulus-real-handoff}, the falsifiable prediction, and the independence witness record the local continuity-modulus seed surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not claim compactness, uniform continuity, trichotomy, quotient real equality, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires grounding the source point, tolerance request, local modulus witness, StreamName windows, DyadicRatCore tolerance, RegSeqRat readback, Real seal, transport, replay, provenance, and local NameCert rows against explicit kernel primitives.}
\end{closurestatus}
