\begin{theorem}[Critical-line witness modulus source totality]
\label{thm:critical-line-witness-modulus-source-totality}
Let $W=(Z,S,M,R,Q,H,C,P,N)$ be an accepted $\CriticalLineWitnessUp$ packet. Before any zero-facing consumer reads $W$, the packet exposes its zeta source row, critical-strip row, rational-modulus row, zero-window row, componentwise transports, displayed routes, package provenance, and local naming row through the finite surface
$$
Z,S,M,R,Q,H,C,P,N.
$$
Thus the consumer first receives $\BHist$ source data, $\hsame$ transport, $\Cont$ locality, $\Pkg$ provenance, and the local $\NameCert_{\CriticalLineWitnessUp}$ row, not a late analytic source or an unlisted zero-location coordinate.
\end{theorem}
\begin{proof}
Begin with the source exposure package of \autoref{thm:critical-line-witness-root-source-exposure}. It opens the zero-strip, rational-height, real-part, and modulus rows as finite $\BHist$ data before downstream use.

Next apply the zero-strip source totality theorem, \autoref{thm:critical-line-witness-zero-strip-source-totality}. The $Z,S$ source side and the $H,C,P,N$ structural side are read together, so the zero-facing consumer cannot bypass the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Finally use the RH-interface carrier and the RH-refusal readback totality theorem, \autoref{thm:critical-line-witness-rh-interface-carrier} and \autoref{thm:critical-line-witness-rh-refusal-readback-totality}. These identify the same packet route as the only RH-facing readback surface, with $M,R,Q$ carrying the modulus and rational-comparison data before any consumer sees the packet.
\end{proof}

\begin{theorem}[Critical-line witness zero-strip ledger exhaustion]
\label{thm:critical-line-witness-zero-strip-ledger-exhaustion}
Every zero-strip read of an accepted $\CriticalLineWitnessUp$ packet is exhausted by the displayed critical-strip row, rational-modulus row, zero-window row, continuation rows, componentwise transports, package provenance, and local $\NameCert$ row of
$$
Z,S,M,R,Q,H,C,P,N.
$$
The read cannot import a global zero locator, an ambient zero-search oracle, an analytic-continuation theorem, or a source row outside the finite $\BHist$, $\hsame$, $\Cont$, and $\Pkg$ ledger.
\end{theorem}
\leanchecked{BEDC.Derived.CriticalLineWitnessUp.CriticalLineWitnessCarrier\_zero\_strip\_ledger\_exhaustion}
\begin{proof}
Read the zero-strip request through \autoref{thm:critical-line-witness-zero-strip-source-totality}. The supplied zero and strip rows are already total on the finite route $Z,S,H,C,P,N$.

Use the modulus-depth ledger of \autoref{thm:critical-line-witness-modulus-depth-ledger} together with the modulus ledger exhaustion of \autoref{thm:critical-line-witness-modulus-ledger-exhaustion}. These force the rational-modulus, real-part, and rational-comparison reads through $M,R,Q,H,C,P,N$ before the zero-strip request is consumed.

Apply the RH-refusal readback totality theorem. Since every refusal readback is the same finite packet route, no global zero locator or unlisted analytic branch can be projected from the zero-strip ledger.
\end{proof}

\begin{theorem}[Critical-line witness RH-boundary refusal totality]
\label{thm:critical-line-witness-rh-boundary-refusal-totality}
Every RH-facing read of an accepted $\CriticalLineWitnessUp$ packet remains a refusal-boundary read over the finite rows
$$
Z,S,M,R,Q,H,C,P,N.
$$
It exports no $\mathsf{RH}$ predicate, no global zero-location theorem, no completed $\GammaUp$ certificate, and no hidden analytic branch. The exported object is only the displayed $\BHist$ refusal surface stabilized by $\hsame$, routed by $\Cont$, carried by $\Pkg$, and named by the local $\NameCert$.
\end{theorem}
\begin{proof}
Start from the RH-interface carrier, \autoref{thm:critical-line-witness-rh-interface-carrier}. It exposes the critical-line, zero-strip, modulus, source-lock, transport, route, provenance, and naming rows while refusing RH truth, nontrivial-zero location, and completed Gamma data.

Next use \autoref{thm:critical-line-witness-rh-refusal-readback-totality}. Every RH-refusal readback factors through $Z,S,M,R,Q,H,C,P,N$, so the boundary read is not a separate analytic theorem.

Finally combine the source exposure package with the zero-strip ledger exhaustion theorem above. The consumer-facing route is already exhausted by the displayed source, modulus, zero-window, transport, continuation, provenance, and naming rows, leaving no coordinate for an $\mathsf{RH}$ predicate, global zero-location theorem, completed $\GammaUp$ certificate, or hidden analytic branch.
\end{proof}

\begin{theorem}[Critical-line witness root formal-target ledger]
\label{thm:critical-line-witness-root-formal-target-ledger}
A future formal target for the $\CriticalLineWitnessUp$ root-modulus zero-strip surface must verify exactly the finite route
$$
Z,S,M,R,Q,H,C,P,N .
$$
Here $Z$ is source exposure, $S$ is the critical-strip row, $M$ is the modulus-depth ledger, $R$ is the real-part row, $Q$ is the rational-comparison row, $H$ is componentwise $\hsame$ transport, $C$ is displayed $\Cont$ replay, $P$ is $\Pkg$ provenance, and $N$ is the local $\NameCert_{\CriticalLineWitnessUp}$ row. The target must refuse RH truth, global zero-location data, analytic continuation, completed $\GammaUp$ data, and hidden analytic branches.
\end{theorem}
\leanchecked{BEDC.Derived.CriticalLineWitnessUp.CriticalLineWitnessRootFormalTargetLedger}
\begin{proof}
The source-exposure package fixes the source row $Z$ before any zero-facing consumer reads the packet. The zero-strip ledger exhaustion theorem then identifies the displayed rows $Z,S,M,R,Q,H,C,P,N$ as the whole zero-strip surface.

The RH-boundary refusal totality theorem supplies the negative side of the ledger. It states that every RH-facing read remains a refusal-boundary read over the same route, with no $\mathsf{RH}$ predicate, global zero-location theorem, completed $\GammaUp$ certificate, or hidden analytic branch.

Thus the formal target is not a global analytic theorem. It is the finite verification obligation that the listed source, strip, modulus, rational-comparison, transport, replay, provenance, and local naming rows exhaust the accepted root-modulus zero-strip surface.
\end{proof}
