The $\CriticalLineWitnessUp$ packet consumes the zero-location and continuation witness surfaces through \autoref{ch:concrete-instances-zeta-zeros-namecert} and \autoref{ch:concrete-instances-zeta-continuation-witness-namecert} before it can expose any critical-line certificate row. This dependency keeps the critical-line readback downstream of displayed zeta-zero and continuation evidence rather than treating the line certificate as an isolated source.
