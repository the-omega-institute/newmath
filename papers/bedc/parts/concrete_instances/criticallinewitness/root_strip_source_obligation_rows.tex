\begin{theorem}[CriticalLineWitness root strip source totality]
\label{thm:criticalline-witness-root-strip-source-totality}
Let $W=(Z,S,M,R,Q,H,C,P,N)$ be an accepted $\CriticalLineWitnessUp$ packet anchored in $\BHist$. Every root consumer that asks for the zero-strip source must read the supplied $\ZetaZerosUp$ row $Z$, the critical-strip row $S$, the $\NatUp$ depth row $M$, the $\RatUp$ comparison ledger $Q$, the $\RealUp$ real-part row $R$, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\CriticalLineWitnessUp}$ rows $H,C,P,N$. The source route may cite the sibling $\ZetaContinuationWitnessUp$ surface, but it does not export analytic continuation, a global $\ZetaZerosUp$ classifier, host equality, quotient representatives, an $\mathsf{RH}$ proof, or ambient completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $Z,S,M,R,Q,H,C,P,N$ are finite $\BHist$ coordinates, with $H$ carrying componentwise $\hsame$ transport, $C$ carrying the displayed $\Cont$ route, $P$ carrying $\Pkg$ provenance, and $N$ carrying the local naming row.

Use the supplied zero-strip route and the zero-strip source totality theorem, \autoref{thm:critical-line-witness-supplied-zero-strip-route} and \autoref{thm:critical-line-witness-zero-strip-source-totality}. They bind the zero and strip source side to $Z,S,H,C,P,N$ before any downstream read can consult the modulus or comparison rows.

Next attach the numerical rows through the root comparison ledger and the modulus source totality theorem, \autoref{thm:critical-line-witness-root-comparison-ledger} and \autoref{thm:critical-line-witness-modulus-source-totality}. These rows force the $\NatUp$, $\RatUp$, and $\RealUp$ data to remain inside $M,R,Q,H,C,P,N$.

Finally read the zeta side only as a named sibling boundary. The $\ZetaContinuationWitnessUp$ and $\ZetaZerosUp$ rows are consumed as displayed source coordinates, while the refusal ledger excludes analytic continuation, host equality, quotient representatives, global zero classification, $\mathsf{RH}$ proof data, and ambient completeness.
\end{proof}

\begin{theorem}[CriticalLineWitness root Gamma Real dependency]
\label{thm:criticalline-witness-root-gamma-real-dependency}
Let $W=(Z,S,M,R,Q,H,C,P,N)$ be an accepted $\CriticalLineWitnessUp$ packet anchored in $\BHist$. Any root read that mentions a $\GammaUp$-facing or $\RealUp$-facing dependency must pass through the displayed real-part row $R$, rational comparison ledger $Q$, depth row $M$, supplied zero-strip rows $Z,S$, componentwise $\hsame$ row $H$, displayed $\Cont$ row $C$, $\Pkg$ row $P$, and local $\NameCert$ row $N$. The dependency is a finite packet route over $\NatUp$, $\RatUp$, $\RealUp$, $\ZetaContinuationWitnessUp$, and $\ZetaZerosUp$ rows, not an analytic Gamma completion or a host real-equality shortcut.
\end{theorem}
\leanchecked{BEDC.Derived.CriticalLineWitnessUp.CriticalLineWitnessCarrier\_root\_gamma\_real\_dependency}
\begin{proof}
Begin with the root carrier classifier boundary, \autoref{thm:critical-line-witness-root-carrier-classifier-boundary}. It orders the displayed carrier rows before a critical-line read of $M$ or $R$ is available.

Next use the root zero-strip supported handoff, \autoref{thm:critical-line-witness-root-zero-strip-supported-handoff}. The handoff sends the supplied $\ZetaZerosUp$ and critical-strip rows through $R,Q,M,H,C,P,N$, so a $\RealUp$ dependency is still a finite carrier dependency.

Now apply the RH-boundary refusal totality theorem, \autoref{thm:critical-line-witness-rh-boundary-refusal-totality}. It records that completed $\GammaUp$ data and hidden analytic branches are not exported by the packet.

The remaining dependency is therefore exactly the finite route through $\NatUp$, $\RatUp$, $\RealUp$, $\ZetaContinuationWitnessUp$, $\ZetaZerosUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Since host real equality and analytic Gamma completion have no displayed coordinate in $W$, they cannot replace $R,Q,M,H,C,P,N$.
\end{proof}

\begin{theorem}[CriticalLineWitness root refusal ledger exactness]
\label{thm:criticalline-witness-root-refusal-ledger-exactness}
Let $W=(Z,S,M,R,Q,H,C,P,N)$ be an accepted $\CriticalLineWitnessUp$ packet anchored in $\BHist$. Every root refusal ledger entry for $\mathsf{RH}$-facing, $\GammaUp$-facing, $\ZetaContinuationWitnessUp$-facing, or $\ZetaZerosUp$-facing demand is exactly the finite ledger over $Z,S,M,R,Q,H,C,P,N$ with componentwise $\hsame$, displayed $\Cont$, $\Pkg$ provenance, and local $\NameCert$ rows. The ledger records refusal of analytic continuation export, host equality, quotient representatives, ambient completeness, global zero classification, zero search, and $\mathsf{RH}$ proof data; it does not turn any refused item into a proof object.
\end{theorem}
\begin{proof}
First project the root refusal route, \autoref{thm:critical-line-witness-root-refusal-route}. It sends off-route root demands to the displayed refusal boundary rather than to analytic or host-level data.

Apply RH-refusal ledger completeness, \autoref{thm:critical-line-witness-rh-refusal-ledger-completeness}. This accounts for $\mathsf{RH}$ proof requests, global zero classifiers, analytic-continuation certificates, quotient representatives, host equality, and ambient completeness within the same finite ledger.

Then use the root zeta ledger non-escape theorem, \autoref{thm:critical-line-witness-root-zeta-ledger-nonescape}. It keeps $\ZetaContinuationWitnessUp$ and $\ZetaZerosUp$ demands attached to the named packet and the sibling continuation boundary rather than exporting a new analytic theorem.

Together these finite carrier projections exhaust the refusal ledger. The exact rows are $Z,S,M,R,Q,H,C,P,N$, and every refused $\GammaUp$, zeta, or $\mathsf{RH}$ object remains absent as an exported proof or host-level coordinate.
\end{proof}
