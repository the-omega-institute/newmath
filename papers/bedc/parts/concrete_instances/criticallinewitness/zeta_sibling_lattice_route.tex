The $\CriticalLineWitnessUp$ sibling lattice consumes only displayed zeta rows before any critical-line obstruction is read. Its visible route passes through the finite source and continuation surfaces of \autoref{ch:concrete-instances-zeta-basic-namecert}, \autoref{ch:concrete-instances-zeta-continuation}, and \autoref{ch:concrete-instances-zeta-continuation-witness-namecert}, then returns to the local $\CriticalLineWitnessUp$ classifier, replay, provenance, and naming rows. This dependency edge supplies no Riemann-hypothesis conclusion, no analytic-continuation closure, no global zero-free region, and no host equality of zeta functions.
