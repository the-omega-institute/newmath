\chapter{A Concrete Naming Certificate for $\TailWindowFilterRefinementUp$}
\label{ch:concrete-instances-tail-window-filter-refinement-namecert}
\origin{human}

Following Bishop and Bridges / Kelley filter terminology, the $\TailWindowFilterRefinementUp$ packet records the finite common-filter refinement by which two displayed Cauchy tail-window schedules can be read through one downstream Real-facing route. It sits after the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the dyadic tail envelope of \autoref{ch:concrete-instances-dyadic-cauchy-tail-envelope-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite refinement carrier for completion consumers; it is not a quotient stream, a selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness theorem.

\begin{definition}[TailWindowFilterRefinement carrier]
\label{def:tail-window-filter-refinement-carrier}
A $\TailWindowFilterRefinementUp$ carrier is a finite $\BHist$ packet
$$
F=(S_0,S_1,R_0,R_1,D,E,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S_0$ and $S_1$ are $\StreamNameUp$ tail-window rows naming the two schedules being compared.
\item $R_0$ and $R_1$ are $\RegSeqRatUp$ readback rows attached to those windows.
\item $D$ is the shared $\DyadicRatCoreUp$ tolerance row requested by the downstream comparison.
\item $E$ is a $\DyadicCauchyTailEnvelopeUp$ row witnessing that both readbacks enter the same finite tail-envelope route.
\item $W$ is the common refinement row, displaying a tail window through which both $S_0,R_0$ and $S_1,R_1$ factor before any $\RealUp$ seal is read.
\item $H$ records componentwise $\hsame$ transport for the two schedules, two readbacks, dyadic tolerance, envelope, common refinement, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a refinement request through $S_0,S_1,R_0,R_1,D,E,W,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\TailWindowFilterRefinementUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\DyadicCauchyTailEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\TailWindowFilterRefinementUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S_0,S_1,R_0,R_1,D,E,W,H,C,P,N$. There is no coordinate for quotient equality of streams, a selected limit representative, countable choice, trichotomy, host real equality, or an ambient Real completeness principle.
\end{definition}

\begin{theorem}[TailWindowFilterRefinement NameCert obligations]
\label{thm:tail-window-filter-refinement-namecert-obligations}
For every accepted $\TailWindowFilterRefinementUp$ carrier $F=(S_0,S_1,R_0,R_1,D,E,W,H,C,P,N)$, the local $\NameCert_{\TailWindowFilterRefinementUp}$ surface consists exactly of carrier admission, two StreamName tail-window exposures, two RegSeqRat readback exposures, shared DyadicRatCore tolerance exposure, DyadicCauchyTailEnvelope exposure, common-refinement window exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.

No obligation may be discharged by quotient stream equality, selected limits, countable-choice selectors, trichotomy, host real equality, detached completion objects, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:tail-window-filter-refinement-carrier}. The displayed rows are exactly $S_0,S_1,R_0,R_1,D,E,W,H,C,P,N$, so carrier admission exposes only the two schedules, two readbacks, shared dyadic tolerance, tail envelope, common refinement, transport, replay, provenance, and local naming data.

The two schedule branches are then read through the same tolerance request. Rows $S_0,R_0$ and $S_1,R_1$ provide the two finite tail-window presentations, while $D$ fixes the dyadic scale at which the comparison is made.

The envelope and refinement rows are the only meeting point. Row $E$ certifies that both branches enter a displayed $\DyadicCauchyTailEnvelopeUp$ route, and $W$ records the common tail window consumed before any $\RealUp$ seal-facing read.

Finally the structural rows preserve that route. Since the excluded quotient, selection, choice, trichotomy, host-equality, detached-completion, and ambient-completeness principles have no carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[TailWindowFilterRefinement Real seal route]
\label{thm:tail-window-filter-refinement-real-seal-route}
Every $\RealUp$ or completion-facing read of an accepted $\TailWindowFilterRefinementUp$ carrier factors through the ordered finite route
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \DyadicRatCoreUp \longrightarrow \DyadicCauchyTailEnvelopeUp \longrightarrow \RealUp .
$$
The common refinement row $W$ is read before the Real seal, and no downstream consumer can bypass $S_0,S_1,R_0,R_1,D,E,W,H,C,P,N$.
\end{theorem}
\begin{proof}
Begin with an accepted carrier read. The two StreamName windows and two RegSeqRat readbacks are exposed as displayed rows before a downstream consumer can inspect the shared dyadic tolerance.

Read the dyadic and envelope rows next. The tolerance row $D$ fixes the comparison scale, and the envelope row $E$ places both branches inside the same finite dyadic tail-envelope surface.

The common window row $W$ supplies the refinement consumed by the Real-facing route. Transport, replay, provenance, and naming preserve that route, and the carrier has no coordinate from which a quotient stream, selected limit, countable-choice schedule, trichotomy datum, host equality, detached completion object, or ambient completeness theorem could be read.
\end{proof}

\falsifiablePrediction{Every accepted $\TailWindowFilterRefinementUp$ read factors two StreamName tail windows and two RegSeqRat readbacks through one DyadicRatCore tolerance, one DyadicCauchyTailEnvelope row, one common refinement window, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming before any RealUp seal-facing consumer reads the route.}

\closureat{TailWindowFilterRefinementUp}{seedStr}

\begin{closurestatus}{\TailWindowFilterRefinementUp}
  \constructivestory{A $\TailWindowFilterRefinementUp$ packet is generated from two StreamName tail-window rows, two RegSeqRat readback rows, a shared DyadicRatCore tolerance row, a DyadicCauchyTailEnvelope route, a common refinement window, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:tail-window-filter-refinement-carrier}, \autoref{thm:tail-window-filter-refinement-namecert-obligations}, and \autoref{thm:tail-window-filter-refinement-real-seal-route} seed the finite common-filter refinement surface over StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, DyadicCauchyTailEnvelopeUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, quotient stream equality, selected limits, countable choice, trichotomy, host real equality, detached completion objects, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, paired StreamName exposure, RegSeqRat readback, DyadicRatCore tolerance exactness, DyadicCauchyTailEnvelope exposure, common-refinement exactness, and TasteGate field faithfulness for BEDC.Derived.TailWindowFilterRefinementUp.}
\end{closurestatus}
