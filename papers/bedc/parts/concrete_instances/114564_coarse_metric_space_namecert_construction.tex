\chapter{A Concrete Naming Certificate for $\CoarseMetricSpaceUp$}
\label{ch:concrete-instances-coarse-metric-space-namecert}
\origin{human}

Following Roe and Bourbaki/Kelley large-scale metric practice, the $\CoarseMetricSpaceUp$ packet records coarse control as finite BEDC NameCert data over displayed metric and uniform-space rows. It reads the metric source of \autoref{ch:concrete-instances-metric-namecert}, the entourage discipline of \autoref{ch:concrete-instances-uniformspace-namecert}, and the terminal distance readback surface of \autoref{ch:concrete-instances-real-namecert}. The packet gives later large-scale consumers a controlled-entourage and bounded-set route without constructing a quotient coarse category, selecting representatives, importing countable choice, or asserting an external large-scale theorem.

\begin{definition}[CoarseMetricSpace carrier]
\label{def:coarse-metric-space-carrier}
A $\CoarseMetricSpaceUp$ carrier is a finite $\BHist$ packet
$$
K=(M,U,E,B,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row, including the admitted point classifier, finite distance-query surface, and metric observation histories.
\item $U$ is the $\UniformSpaceUp$ entourage row, recording the finite entourage requests whose small-scale control is visible to the packet.
\item $E$ is the controlled large-scale entourage budget, assigning displayed coarse-radius or tolerance rows to the finite entourages allowed for downstream coarse consumers.
\item $B$ is the bounded-set ledger, recording the finite named bounded rows and the radius/readback witnesses by which a consumer may use them.
\item $R$ is the $\RegSeqRatUp$ and $\RealUp$ readback row for metric distances, controlled radii, and bounded-set witnesses.
\item $H$ records componentwise $\hsame$ transport for the metric source, uniform entourage row, controlled-entourage budget, bounded-set ledger, readback, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from metric-distance and entourage requests through $U,E,B,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CoarseMetricSpaceUp$, $\MetricUp$, $\UniformSpaceUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CoarseMetricSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,U,E,B,R,H,C,P,N$. It has no coordinate for quotient coarse-space equality, a selected net, a choice-selected bornology, a host large-scale category, or ambient metric completion.
\end{definition}

\begin{theorem}[CoarseMetricSpace NameCert obligations]
\label{thm:coarse-metric-space-namecert-obligations}
For every accepted $\CoarseMetricSpaceUp$ carrier $K=(M,U,E,B,R,H,C,P,N)$, the local $\NameCert_{\CoarseMetricSpaceUp}$ surface consists exactly of carrier admission for the displayed $\MetricUp$ source, $\UniformSpaceUp$ entourage row, controlled large-scale entourage budget, bounded-set ledger, $\RegSeqRatUp$ and $\RealUp$ readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every coarse consumer must read $M,U,E,B,R,H,C,P,N$ before treating the packet as large-scale metric data, and every request for quotient completion, selected representatives, countable choice, host coarse categories, or ambient completeness is refused by absence from the carrier.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:coarse-metric-space-carrier}. The tuple displays the metric source, uniform entourage row, controlled-entourage budget, bounded-set ledger, readback, transport, replay, provenance, and local name.

The control obligations are finite route inspections. A consumer first reads $M$ for metric distances, $U$ for entourage shape, $E$ for large-scale control, and $B$ for bounded-set access.

Readback and stability then pass through $R,H,C,P,N$. These rows transport, replay, source, and name the same finite route, so the NameCert surface contains no quotient coarse category, selected representative, countable-choice schedule, host bornology, or ambient completeness principle.
\end{proof}

\begin{theorem}[CoarseMetricSpace controlled entourage route]
\label{thm:coarse-metric-space-controlled-entourage-route}
Let $K=(M,U,E,B,R,H,C,P,N)$ be an accepted $\CoarseMetricSpaceUp$ carrier. Every controlled-entourage consumer must first display the $\MetricUp$ source $M$, the $\UniformSpaceUp$ entourage row $U$, the large-scale budget $E$, the bounded-set ledger $B$, and the $\RegSeqRatUp/\RealUp$ readback row $R$ before it can replay a coarse relation. The replay is transported only through $\hsame$, continued only through $\Cont$, sourced only through $\Pkg$, and named only by the local $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only metric and uniform data available to a controlled-entourage consumer are $M$ and $U$.

Read the large-scale rows next. The budget $E$ records which displayed entourage rows are allowed at the requested scale, while $B$ records the bounded-set ledger used by coarse consumers.

The readback row $R$ converts the displayed distance and radius requests through $\RegSeqRatUp$ and $\RealUp$ before replay. The structural rows $H,C,P,N$ then preserve that same route and add no selected net, quotient equality, countable choice, host large-scale category, or ambient completion object.
\end{proof}

\falsifiablePrediction{An accepted $\CoarseMetricSpaceUp$ read is refuted if a consumer obtains controlled entourage data or bounded-set evidence without displaying the MetricUp source, UniformSpace entourage row, controlled large-scale budget, bounded-set ledger, RegSeqRat/Real readback, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}

\independenceWitness{metric, uniformspace, real: none is bijective to $\CoarseMetricSpaceUp$ because this packet jointly carries a metric source, an entourage row, a controlled large-scale budget, bounded-set ledger data, rational and real readback, transport, replay, provenance, and local naming.}

\closureat{CoarseMetricSpaceUp}{seedStr}

\begin{closurestatus}{\CoarseMetricSpaceUp}
  \constructivestory{A $\CoarseMetricSpaceUp$ packet is generated from a displayed MetricUp source, UniformSpace entourage row, controlled large-scale entourage budget, bounded-set ledger, RegSeqRat/Real readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:coarse-metric-space-carrier}, \autoref{thm:coarse-metric-space-namecert-obligations}, and \autoref{thm:coarse-metric-space-controlled-entourage-route} seed the finite coarse metric certificate over displayed metric, uniform entourage, large-scale control, boundedness, readback, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, quotient coarse-space equality, selected nets, host coarse categories, countable-choice schedules, ambient completeness, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, controlled-entourage exactness, bounded-set ledger stability, readback transport, provenance non-escape, and local naming for BEDC.Derived.CoarseMetricSpaceUp.}
\end{closurestatus}
