\chapter{A Concrete Naming Certificate for $\IsometricCompletionExtensionUp$}
\label{ch:concrete-instances-isometric-completion-extension-namecert}
\origin{human}

Following Bourbaki and Kelley on metric completion, the $\IsometricCompletionExtensionUp$ packet records the finite dense-isometry extension route that begins with the completion certificate of \autoref{ch:concrete-instances-isometric-completion-namecert}, passes through the dense-source extension surface of \autoref{ch:concrete-instances-dense-isometry-extension-namecert}, and reads uniqueness only through the complete-metric comparison boundary of \autoref{ch:concrete-instances-complete-metric-limit-uniqueness-namecert}. The packet names the universal extension theorem as finite BEDC completion data; it does not construct quotient completions, select completion points, invoke countable choice, or import an ambient complete metric space.

\begin{definition}[Isometric completion extension carrier]
\label{def:isometric-completion-extension-carrier}
An $\IsometricCompletionExtensionUp$ carrier is a finite $\BHist$ packet
$$
X=(M,D,K,G,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the source metric carrier row, read through the same public metric surface used by $\IsometricCompletionUp$.
\item $D$ is the dense isometry row, recording the displayed dense-source isometry and its finite approximation witnesses through $\DenseIsometryExtensionUp$.
\item $K$ is the completion target row, exposing the completion handoff named by $\IsometricCompletionUp$ before any extension value is public.
\item $G$ is the unique extension graph row, recording source agreement, completion-facing extension reads, and uniqueness only as finite graph data.
\item $R$ is the $\RealUp$ tolerance readback row used for distance preservation, dense approximation, and zero-distance comparison.
\item $H$ records componentwise $\hsame$ transport for $M,D,K,G,R$ and the local naming row.
\item $C$ records displayed $\Cont$ replay of metric admission, dense-isometry handoff, completion handoff, extension-graph readback, and Real tolerance sealing.
\item $P$ is the $\Pkg$ provenance over $\IsometricCompletionExtensionUp$, $\IsometricCompletionUp$, $\DenseIsometryExtensionUp$, $\CompleteMetricLimitUniquenessUp$, $\MetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\IsometricCompletionExtensionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for a selected completion point, quotient metric equality, countable choice, host function equality, or an ambient complete metric space.
\end{definition}
\leanstmt{BEDC.Derived.IsometricCompletionExtensionUp}

\begin{theorem}[Isometric completion extension NameCert obligations]
\label{thm:isometric-completion-extension-namecert-obligations}
For every accepted $\IsometricCompletionExtensionUp$ carrier $X=(M,D,K,G,R,H,C,P,N)$, the local $\NameCert_{\IsometricCompletionExtensionUp}$ surface consists exactly of five obligation rows before structural replay is consumed:
\begin{enumerate}
\item source metric carrier exposure through $M$;
\item dense isometry exposure through $D$;
\item completion target handoff through $K$;
\item unique extension graph exposure through $G$;
\item $\RealUp$ tolerance readback through $R$.
\end{enumerate}
The remaining rows $H,C,P,N$ transport, replay, source, and locally name that five-row surface, and no accepted carrier exports selected completion points, quotient metric equality, countable choice, host function equality, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:isometric-completion-extension-carrier}. The rows $M,D,K,G,R$ are displayed before a completion-facing extension value can be read.

The source side is $M,D$. The metric carrier fixes the distance surface, while the dense isometry row supplies only finite dense-source observations and their Real tolerance readback.

The completion side is $K,G,R$. The completion target row is read before the graph row, and uniqueness of graph reads is routed through the same Real tolerance row and the complete-metric limit uniqueness boundary.

Finally $H,C,P,N$ preserve the displayed route under transport, continuation replay, provenance, and local naming. Since the carrier contains no further coordinate, the excluded completion point, quotient, choice, host equality, and ambient completeness services cannot be exported.
\end{proof}

\begin{theorem}[Isometric completion extension unique factorization]
\label{thm:isometric-completion-extension-unique-factorization}
Every completion-facing extension read from an accepted $\IsometricCompletionExtensionUp$ carrier factors through the displayed route
$$
\DenseIsometryExtensionUp,\ \IsometricCompletionUp,\ \CompleteMetricLimitUniquenessUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
In carrier notation, a consumer must first expose the dense isometry row $D$, the completion handoff row $K$, the complete-metric uniqueness comparison attached to $G$, and the Real tolerance row $R$ before it reads any extension value. The factorization exports no selected completion point, quotient metric equality, countable-choice schedule, host function equality, or ambient complete metric space.
\end{theorem}
\begin{proof}
Start with an accepted packet $X=(M,D,K,G,R,H,C,P,N)$. The dense-source portion is read through $D$ over the metric source $M$, so the consumer begins with the finite dense-isometry surface rather than a host map on a completion.

Next read the completion handoff $K$. This is the only coordinate that names a completion target, and it is supplied by the finite $\IsometricCompletionUp$ route.

The graph row $G$ is then interpreted through complete-metric limit uniqueness and the terminal Real tolerance row $R$. This gives the unique factorization route for extension reads while keeping equality-like comparison inside displayed zero-distance and Real readback data.

The structural suffix $H,C,P,N$ transports, replays, sources, and names the same route. It adds no selected point, quotient equality, choice schedule, host function equality, or ambient completeness principle.
\end{proof}

\falsifiablePrediction{Every accepted $\IsometricCompletionExtensionUp$ extension read exposes source metric, dense isometry, completion handoff, unique extension graph, Real tolerance, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before any completion-facing value is public.}

\independenceWitness{isometriccompletion, denseisometryextension, completemetriclimituniqueness, real: none is bijective to $\IsometricCompletionExtensionUp$ because the packet jointly carries the source metric row, dense isometry row, completion handoff, unique extension graph, Real tolerance readback, transport, replay, provenance, and local NameCert rows.}

\closureat{IsometricCompletionExtensionUp}{seedStr}

\begin{closurestatus}{\IsometricCompletionExtensionUp}
  \constructivestory{An $\IsometricCompletionExtensionUp$ packet is generated from a source MetricUp row, a DenseIsometryExtensionUp row, an IsometricCompletionUp completion target row, a CompleteMetricLimitUniquenessUp uniqueness comparison, a RealUp tolerance row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:isometric-completion-extension-carrier}, \autoref{thm:isometric-completion-extension-namecert-obligations}, and \autoref{thm:isometric-completion-extension-unique-factorization} seed the finite dense-isometry completion-extension route over metric, completion, uniqueness, and Real tolerance rows.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.IsometricCompletionExtensionUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion construction, selected completion points, quotient metric equality, countable choice, host function equality, ambient completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-isometry exposure, completion handoff, unique extension graph exactness, Real tolerance preservation, transport and replay stability, provenance non-escape, and field faithfulness for BEDC.Derived.IsometricCompletionExtensionUp.}
\end{closurestatus}
