\chapter{A Concrete Naming Certificate for $\IntervalBisectionSearchUp$}
\label{ch:concrete-instances-interval-bisection-search-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\IntervalBisectionSearchUp$ packet records the finite bisection-search surface for a located real interval whose endpoint signs, apartness data, dyadic midpoint rows, and shrinking brackets are inspected before a regular Cauchy readback or terminal real seal is consumed. It sits beside the dyadic rational carrier of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window source of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the located endpoint ledger of \autoref{ch:concrete-instances-locatedinterval-namecert}. The packet names constructive interval bisection search as displayed BEDC rows; it is not host real trichotomy, not a selected root, not quotient real equality, and not ambient $\RealUp$ completeness.

\begin{definition}[Interval bisection search carrier]
\label{def:interval-bisection-search-carrier}
An $\IntervalBisectionSearchUp$ carrier is a finite $\BHist$ packet
$$
B=(I,M,A,L,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\LocatedIntervalUp$ source row, exposing located lower and upper endpoint names, endpoint comparison budgets, and interval membership rows.
\item $M$ is the $\DyadicRatCoreUp$ midpoint row, recording the dyadic midpoint request, its endpoint placement witnesses, and the finite tolerance ledger used to split the current bracket.
\item $A$ is the sign and apartness witness row. It records the finite sign-read or apartness-read data used to retain one child bracket without invoking real trichotomy.
\item $L$ is the shrinking bracket ledger, listing the retained located interval at each inspected bisection stage together with the displayed diameter or tolerance decrease row.
\item $S$ is the $\StreamNameUp$ finite-window row obtained from the retained bracket endpoints at the requested precisions.
\item $R$ is the $\RegSeqRatUp$ readback row for the midpoint or retained-endpoint schedule selected by the finite ledger.
\item $E$ is the terminal $\RealUp$ seal reached only after $I,M,A,L,S,R$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for located endpoints, midpoint rows, sign or apartness witnesses, bracket ledgers, stream windows, regular readback, Real seal, replay, provenance, and local naming.
\item $C$ stores displayed $\Cont$ replay of the bisection-search request through $I,M,A,L,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\IntervalBisectionSearchUp$, $\LocatedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\IntervalBisectionSearchUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite bisection-search history. It has no coordinate for a host root selector, unrestricted sign trichotomy, quotient equality of real names, selected limit, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Interval bisection search NameCert obligations]
\label{thm:interval-bisection-search-namecert-obligations}
For every accepted $\IntervalBisectionSearchUp$ carrier $B=(I,M,A,L,S,R,E,H,C,P,N)$, the local $\NameCert_{\IntervalBisectionSearchUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the located interval row, dyadic midpoint row, sign or apartness witness row, shrinking bracket ledger, stream windows, regular rational readback, terminal Real seal, transport, replay, provenance, and local naming;
\item classifier stability under componentwise $\hsame$ transport of $I,M,A,L,S,R,E$;
\item midpoint exactness, requiring every retained child bracket to cite the displayed $\DyadicRatCoreUp$ midpoint row before it is inspected;
\item sign and apartness locality, requiring every branch decision to be read from $A$ and not from host trichotomy of real values;
\item bracket shrinkage, requiring the ledger $L$ to expose a smaller located interval or tighter tolerance row at each accepted stage;
\item $\StreamNameUp$ window exposure using only retained endpoint windows from $L$;
\item $\RegSeqRatUp$ readback from the same retained windows or midpoint schedule;
\item $\RealUp$ seal nonescape through $S,R,E$;
\item displayed $\Cont$ replay and $\Pkg$ provenance for the whole route;
\item ledger refusal for any consumer that omits a located endpoint, midpoint, sign or apartness, shrinking bracket, stream window, regular readback, Real seal, transport, replay, provenance, or local naming row.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:interval-bisection-search-carrier}. The visible rows are exactly $I,M,A,L,S,R,E,H,C,P,N$, and the packet has no hidden coordinate for a host root selector, real trichotomy, quotient equality, selected limit, or completeness theorem.

The classifier is rowwise. Componentwise $\hsame$ transport preserves the located interval, midpoint, sign or apartness, bracket ledger, stream, readback, and seal rows together, so a transported bisection-search read remains inside the same finite route.

Midpoint exactness and branch locality are read from $M$ and $A$. The midpoint row is the only split source, and the sign or apartness row is the only branch-retention source, so the carrier cannot replace them by a host comparison of real values.

The readback obligations follow from $L,S,R,E$. The shrinking bracket ledger fixes the retained windows, the stream row exposes finite endpoint observations, the regular rational row repacks the same observations, and the Real seal is terminal. Repacking with $H,C,P,N$ gives the stated local NameCert surface.
\end{proof}

\begin{theorem}[Interval bisection search L10 handoff]
\label{thm:interval-bisection-search-l10-handoff}
Every Real-and-Completion consumer reading an accepted $\IntervalBisectionSearchUp$ carrier must pass through the finite L10 route
$$
\LocatedIntervalUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
Concretely, the consumer first reads the located endpoint row $I$, then the dyadic midpoint and sign-apartness rows $M,A$, then the shrinking bracket ledger $L$, then the stream and regular rational readback rows $S,R$, and only then the terminal Real seal $E$. The handoff exports no host real equality, selected root, quotient stream equality, unrestricted sign trichotomy, chosen limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the accepted carrier $B=(I,M,A,L,S,R,E,H,C,P,N)$. The source interval is displayed by $I$, so every endpoint or membership read begins in the $\LocatedIntervalUp$ row.

Next read the bisection rows. The dyadic midpoint row $M$ and the sign or apartness row $A$ determine the retained child bracket as finite BEDC evidence, while $L$ records the shrinking bracket history that downstream consumers may inspect.

Then pass to L10 readback. The finite windows $S$ and regular rational row $R$ are built only from the retained bracket ledger, and the terminal seal $E$ is read only after those rows are present.

Finally replay the route through $H,C,P,N$. These structural rows transport, replay, source, and name the same finite packet, so none of the refused host-level or completion-level claims can be exported by the bisection-search handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\IntervalBisectionSearchUp$ consumer must expose located endpoints, dyadic midpoint data, sign or apartness witnesses, shrinking bracket rows, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming before using a bisection-search result.}

\independenceWitness{locatedinterval, dyadicratcore, streamname, regseqrat, real: no listed sibling alone supplies the joint packet containing located endpoint rows, dyadic midpoint splitting, sign or apartness branch retention, shrinking bracket ledger, finite windows, regular rational readback, terminal Real seal, transport, replay, provenance, and local NameCert row.}

\closureat{IntervalBisectionSearchUp}{seedStr}

\begin{closurestatus}{\IntervalBisectionSearchUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:interval-bisection-search-carrier}, \autoref{thm:interval-bisection-search-namecert-obligations}, and \autoref{thm:interval-bisection-search-l10-handoff} seed the finite bisection-search route over located interval endpoints, dyadic midpoint rows, sign or apartness witnesses, shrinking brackets, StreamName windows, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close root existence, root uniqueness, host real trichotomy, quotient real equality, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, midpoint exactness, sign and apartness locality, bracket shrinkage, L10 readback, transport, replay, provenance, and local naming for BEDC.Derived.IntervalBisectionSearchUp.}
  \constructivestory{An $\IntervalBisectionSearchUp$ packet is generated from a located interval source, dyadic midpoint rows, sign or apartness branch witnesses, a shrinking bracket ledger, StreamName windows, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
