\chapter{A Concrete Naming Certificate for $\ZeckendorfPhaseShellUp$}
\label{ch:concrete-instances-zeckendorf-phase-shell-namecert}
\origin{human}

Following Zeckendorf and the Fibonacci symbolic-dynamics tradition, the $\ZeckendorfPhaseShellUp$ packet realizes the stable-window, residual-fiber, and external golden-phase projection rows of \autoref{ch:visions-zeckendorf-golden-phase-projection} as finite NameCert data. It sits beside the carry-normalization surface of \autoref{ch:concrete-instances-zeckendorf-carry-normalization-namecert} and the finite golden-mean window of \autoref{ch:concrete-instances-golden-mean-shift-window-six-namecert}. The packet names the visible phase shell only; it does not turn golden phase into a kernel object, a quotient of histories, a physical law, or a real-completion theorem.

\begin{definition}[ZeckendorfPhaseShell carrier]
\label{def:zeckendorf-phase-shell-carrier}
A $\ZeckendorfPhaseShellUp$ carrier is a finite $\BHist$ packet
$$
Z=(m,\Omega,X,F,R,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $m$ is the finite window-width row.
\item $\Omega$ is the raw binary-window row $\{0,1\}^{m}$.
\item $X$ is the stable Zeckendorf-window row, admitting exactly the visible words with no adjacent $11$ pattern.
\item $F$ is the fold row from raw windows to stable visible windows, read through the finite Fibonacci-value ledger of the vision chapter.
\item $R$ is the residual-fiber row. For each visible word $x$, it records the finite fiber of raw windows that fold to $x$ and the refusal to identify that fiber with a quotient class of histories.
\item $G$ is the external golden-phase projection row. It records the phase readout attached to $X$ and $R$ without placing that readout in the BEDC kernel.
\item $H$ records componentwise $\hsame$ transport for $m,\Omega,X,F,R,G,C,P,N$.
\item $C$ records displayed $\Cont$ replay through the stable window, fold, residual-fiber, and phase rows.
\item $P$ is the $\Pkg$ provenance over $\ZeckendorfPhaseShellUp$, $\ZeckendorfCarryNormalizationUp$, $\GoldenMeanShiftWindowSixUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ZeckendorfPhaseShellUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotienting raw histories by the fold, no kernel equality between visible phase and raw microstate, no invariant-measure claim, no physical-law assertion, and no completed real limit.
\end{definition}

\begin{theorem}[ZeckendorfPhaseShell NameCert boundary]
\label{thm:zeckendorf-phase-shell-namecert-boundary}
For every accepted $\ZeckendorfPhaseShellUp$ carrier
$$
Z=(m,\Omega,X,F,R,G,H,C,P,N),
$$
the local $\NameCert_{\ZeckendorfPhaseShellUp}$ surface is exhausted by five obligation rows:
\begin{enumerate}
\item stable-window admission, requiring $X$ to expose the no-adjacent-$11$ classifier over the finite raw window $\Omega$;
\item fold exactness, requiring $F$ to display the finite Fibonacci-value fold before any visible word is consumed;
\item residual-fiber preservation, requiring $R$ to remain visible whenever a consumer reads the folded window;
\item external phase transport, requiring the golden-phase row $G$ to be replayed as projection data rather than as a kernel object or quotient of histories;
\item structural exhaustion, requiring $H,C,P,N$ to transport, replay, source, and name only the displayed rows $m,\Omega,X,F,R,G$.
\end{enumerate}
No Zeckendorf phase-shell obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:zeckendorf-phase-shell-carrier}. The raw window, stable visible window, fold, residual-fiber ledger, and phase projection are all finite rows before transport or replay is applied.

Stable-window admission and fold exactness come from $X$ and $F$. The visible word can be consumed only after the no-adjacent-$11$ classifier and the Fibonacci fold have been displayed.

The residual-fiber row $R$ prevents the fold readout from erasing raw histories. Thus a consumer of the visible phase shell receives a stable word together with its finite fiber ledger, not a quotient equality of histories.

Finally read $G,H,C,P,N$. The golden phase row is transported and replayed only as external projection data, while provenance names the Zeckendorf carry-normalization and golden-mean window sources. Therefore the five rows exhaust the local NameCert boundary without promoting phase projection to a kernel object.
\end{proof}

\falsifiablePrediction{Every accepted $\ZeckendorfPhaseShellUp$ read exposes a finite window width, raw binary window, stable Zeckendorf window, Fibonacci fold, residual-fiber ledger, external golden-phase projection row, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert row before any visible phase-shell consumer is served.}

\independenceWitness{zeckendorfcarrynormalization, goldenmeanshiftwindowsix: neither is bijective with $\ZeckendorfPhaseShellUp$ because this packet alone binds raw windows, stable folds, residual fibers, external phase projection, transport, replay, provenance, and local naming as one certificate.}

\closureat{ZeckendorfPhaseShellUp}{seedStr}

\begin{closurestatus}{\ZeckendorfPhaseShellUp}
  \constructivestory{A $\ZeckendorfPhaseShellUp$ packet is generated from a finite window-width row, raw binary-window row, stable Zeckendorf-window row, Fibonacci fold row, residual-fiber row, external golden-phase projection row, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:zeckendorf-phase-shell-carrier} and \autoref{thm:zeckendorf-phase-shell-namecert-boundary} seed the finite Zeckendorf phase-shell certificate over stable-window, residual-fiber, golden-phase projection, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a kernel golden-phase object, quotient equality of histories, invariant measure, physical law, real-completion theorem, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked stable-window admission, fold exactness, residual-fiber preservation, external phase transport, and structural exhaustion for BEDC.Derived.ZeckendorfPhaseShellUp.}
\end{closurestatus}
