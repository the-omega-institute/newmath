\chapter{A Concrete Naming Certificate for $\HyperbolicBoundaryShadowTransportUp$}
\label{ch:concrete-instances-hyperbolic-boundary-shadow-transport-namecert}
\origin{ai}

The $\HyperbolicBoundaryShadowTransportUp$ packet realizes the boundary-shadow
transport branch of
\autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite
NameCert rows. It sits after the boundary-shadow carrier of
\autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the
boundary-transport route of
\autoref{ch:concrete-instances-hyperbolic-boundary-transport-namecert}, the
ray-boundary surface of \autoref{ch:concrete-instances-gromov-boundary-namecert},
the disk metric readback of
\autoref{ch:concrete-instances-poincare-disk-metric-namecert}, and the terminal
Real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet
names a transported boundary shadow through displayed disk, boundary, metric,
and Real-facing rows; it is not an interior-center collapse, a selected ideal
endpoint, quotient boundary equality, physical observer motion, or global
hyperbolic compactification.

\falsifiablePrediction{Every accepted $\HyperbolicBoundaryShadowTransportUp$ read is refuted if it collapses a boundary-shadow transport to an interior-center row without displaying $\HyperbolicBoundaryShadowUp$, $\HyperbolicBoundaryTransportUp$, $\GromovBoundaryUp$, $\PoincareDiskMetricUp$, $\RealUp$, nontriviality, field-faithful metric readback, componentwise $\hsame$ transport, $\Cont$ replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-boundary-transport, gromov-boundary, poincare-disk-metric, real: the boundary-shadow sibling records shadow readout without transport alignment; the boundary-transport sibling records fixed-boundary transport without shadow readout; the Gromov-boundary sibling records ray-boundary equivalence without disk-shadow transport; the Poincare disk metric sibling records metric readback without transported boundary shadows; and the Real sibling seals numerical data without a hyperbolic boundary carrier. None is bijective to $\HyperbolicBoundaryShadowTransportUp$.}

\begin{definition}[Hyperbolic boundary shadow transport carrier]
\label{def:hyperbolic-boundary-shadow-transport-carrier}
A $\HyperbolicBoundaryShadowTransportUp$ carrier is a finite $\BHist$ packet
$$
T=(S,U,G,D,E,L,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row naming the boundary-shadow
readout attached to displayed disk observations.
\item $U$ is the $\HyperbolicBoundaryTransportUp$ row transporting the boundary
commitment while preserving the visible far-end boundary reading.
\item $G$ is the $\GromovBoundaryUp$ row carrying the ray-boundary token through
the transported shadow.
\item $D$ is the $\PoincareDiskMetricUp$ disk-metric readback row used to refuse
interior-center collapse.
\item $E$ is the $\RealUp$ seal row for the metric and boundary-shadow readback.
\item $L$ is the nontrivial and field-faithful ledger: it records that the disk
metric read is nonzero on the transported boundary-shadow separation and that
the Real-facing metric coordinate is read through the displayed field-faithful
route rather than through a collapsed center.
\item $F$ is the refusal row, excluding interior-center collapse, selected ideal
endpoints, quotient boundary equality, physical observer motion, and global
hyperbolic compactification.
\item $H$ records componentwise $\hsame$ transport for boundary-shadow,
boundary-transport, Gromov-boundary, disk-metric, Real-seal, ledger, refusal,
provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from shadow readout through
boundary transport, Gromov-boundary token, disk-metric readback, Real seal,
nontrivial field-faithful ledger, refusal, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over
$\HyperbolicBoundaryShadowTransportUp$, $\HyperbolicBoundaryShadowUp$,
$\HyperbolicBoundaryTransportUp$, $\GromovBoundaryUp$,
$\PoincareDiskMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and
$\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicBoundaryShadowTransportUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through
$S,U,G,D,E,L,F,H,C,P,N$. It has no coordinate for an interior-center collapse,
selected ideal endpoint, quotient boundary equality, physical observer motion,
completed hyperbolic compactification, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Hyperbolic boundary shadow transport NameCert obligations]
\label{thm:hyperbolic-boundary-shadow-transport-namecert-obligations}
For every accepted $\HyperbolicBoundaryShadowTransportUp$ carrier
$T=(S,U,G,D,E,L,F,H,C,P,N)$, the local
$\NameCert_{\HyperbolicBoundaryShadowTransportUp}$ surface consists exactly of
boundary-shadow admission, boundary-transport preservation, Gromov-boundary
token exposure, Poincare disk metric readback, Real-seal handoff, nontrivial and
field-faithful metric ledger, refusal exactness, componentwise transport,
displayed replay, package provenance, and local naming through
$S,U,G,D,E,L,F,H,C,P,N$.

Every public read of the transported shadow factors through
$$
\HyperbolicBoundaryShadowUp \longmapsto
\HyperbolicBoundaryTransportUp \longmapsto
\GromovBoundaryUp \longmapsto
\PoincareDiskMetricUp \longmapsto
\RealUp
$$
together with the nontrivial field-faithful ledger and the structural
$\BHist,\hsame,\Cont,\Pkg,\NameCert$ rows.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:hyperbolic-boundary-shadow-transport-carrier}. Rows $S,U,G,D,E$
are the visible geometric and Real-facing route, while $L,F,H,C,P,N$ record
ledger, refusal, transport, replay, provenance, and naming.

Read the boundary-shadow row $S$ first and transport it through $U$. The
Gromov-boundary token $G$ is then the only boundary token available to the
transported shadow route.

The disk-metric and Real-facing rows are downstream. Row $D$ supplies the
$\PoincareDiskMetricUp$ readback, row $E$ seals the numerical boundary, and row
$L$ records the nontrivial field-faithful route expected by the future TasteGate
surface.

Finally use $F,H,C,P,N$ to repack the public certificate. Because the excluded
objects have no coordinate in the carrier, the packet cannot collapse the
transported shadow to an interior center or export quotient boundary equality,
physical observer motion, selected ideal endpoints, or global compactification.
\end{proof}

\closureat{HyperbolicBoundaryShadowTransportUp}{seedStr}

\begin{closurestatus}{\HyperbolicBoundaryShadowTransportUp}
  \constructivestory{A $\HyperbolicBoundaryShadowTransportUp$ packet is generated from HyperbolicBoundaryShadow, HyperbolicBoundaryTransport, GromovBoundary, PoincareDiskMetric, RealUp seal, nontrivial field-faithful ledger, refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-boundary-shadow-transport-carrier}, \autoref{thm:hyperbolic-boundary-shadow-transport-namecert-obligations}, the falsifiable prediction, and the independence witness seed the finite boundary-shadow transport route over shadow, transport, boundary, disk-metric, Real-seal, ledger, refusal, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close interior-center collapse, selected ideal endpoints, quotient boundary equality, physical observer motion, global hyperbolic compactification, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shadow admission, boundary-transport preservation, Gromov-boundary token exposure, Poincare disk metric readback, Real-seal handoff, nontrivial and field-faithful ledger exactness, refusal coverage, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicBoundaryShadowTransportUp.}
\end{closurestatus}
