The $\EffectiveRealCompactIntervalSearchUp$ route hands its finite bisection rows, interval-value rows, and compact-window certificates to the compatible-window route of \autoref{ch:concrete-instances-regular-cauchy-gluing-namecert} before any $\RealUp$ seal consumer reads a glued local search window. This dependency keeps each L10 finite-window consumer downstream from the displayed compact search packet, so the gluing step reads certified local windows rather than an ambient completeness or choice principle.
