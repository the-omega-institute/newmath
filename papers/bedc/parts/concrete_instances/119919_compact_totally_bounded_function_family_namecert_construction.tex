\chapter{A Concrete Naming Certificate for $\CompactTotallyBoundedFunctionFamilyUp$}
\label{ch:concrete-instances-compact-totally-bounded-function-family-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactTotallyBoundedFunctionFamilyUp$ packet records finite-net compactness data for a displayed family of continuous maps over a compact metric source. It sits beside the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the equicontinuity surface of \autoref{ch:concrete-instances-equicontinuity-namecert}, the continuous-map graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite data consumed by compact-uniform and Arzela--Ascoli routes; it does not assert open-cover compactness, selected subsequences, quotient function-space equality, countable choice, or ambient $\RealUp$ completeness.

\falsifiablePrediction{Any compact-family consumer that reads a $\CompactTotallyBoundedFunctionFamilyUp$ packet while bypassing its compact source row, continuous-map family row, equicontinuity modulus row, finite-net row, RegSeqRat/Real readback rows, hsame transport, Cont replay, Pkg provenance, or local NameCert row refutes the classifier.}

\independenceWitness{compactmetric, equicontinuity, continuousmap, streamname, regseqrat, real: $\CompactTotallyBoundedFunctionFamilyUp$ is not bijective to any listed sibling because it binds compact source inspection, a continuous-map family ledger, equicontinuity modulus data, finite totally-bounded nets, regular rational readback, Real endpoint seals, transport, replay, provenance, and local naming in one packet.}

\begin{definition}[Compact totally bounded function-family carrier]
\label{def:compact-totally-bounded-function-family-carrier}
A $\CompactTotallyBoundedFunctionFamilyUp$ carrier is a finite $\BHist$ packet
$$
K=(X,F,E,B,W,R,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ source row, including the compact finite-net windows used to inspect source points.
\item $F$ is the finite $\ContinuousMapUp$ family ledger whose graph rows expose the admissible maps on the source windows.
\item $E$ is the $\EquicontinuityUp$ modulus row, recording one displayed source-radius response for the family at each requested target tolerance.
\item $B$ is the totally-bounded finite-net row for the image family, including the finite list of net centers and radius budgets visible to compact-family consumers.
\item $W$ is the $\StreamNameUp$ observation row through which source windows, family indices, and value samples are requested.
\item $R$ is the $\RegSeqRatUp$ value-readback row obtained from the displayed windows and dyadic tolerance ledgers.
\item $V$ is the $\RealUp$ value-seal row reached only after the regular readback is exposed.
\item $H$ records componentwise $\hsame$ transport for compact source rows, continuous-map rows, equicontinuity rows, finite-net rows, stream windows, readbacks, Real seals, replay rows, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a family compactness request through compact source inspection, graph evaluation, equicontinuity radius use, finite-net comparison, regular readback, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\CompactTotallyBoundedFunctionFamilyUp$, $\CompactMetricUp$, $\EquicontinuityUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactTotallyBoundedFunctionFamilyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,F,E,B,W,R,V,H,C,P,N$. No coordinate names open-cover compactness, selected subsequences, quotient function-space equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact totally bounded function-family NameCert obligations]
\label{thm:compact-totally-bounded-function-family-namecert-obligations}
For every accepted $\CompactTotallyBoundedFunctionFamilyUp$ carrier $K=(X,F,E,B,W,R,V,H,C,P,N)$, the local $\NameCert_{\CompactTotallyBoundedFunctionFamilyUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the compact source, continuous-map family, equicontinuity modulus, totally-bounded finite-net, stream-window, regular-readback, Real-seal, transport, replay, provenance, and local naming rows;
\item compact source visibility, requiring every sampled source point to enter through the $\CompactMetricUp$ row $X$ and the observation row $W$;
\item family graph visibility, requiring every sampled value to be read through the $\ContinuousMapUp$ ledger $F$ before regular readback;
\item equicontinuity stability, requiring the modulus row $E$ to mediate tolerance changes before a finite-net comparison is read;
\item finite-net exactness, requiring the totally-bounded row $B$ to supply the only admissible net centers and radius budgets for the family image;
\item value readback, requiring every terminal value to pass through $W,R,V$ before any Real-facing consumer reads it;
\item non-escape, requiring $H,C,P,N$ to preserve the same displayed rows and to export no open-cover compactness, selected subsequence, quotient function-space equality, countable-choice selector, or ambient $\RealUp$ completeness.
\end{enumerate}
No NameCert obligation for the compact function family is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-totally-bounded-function-family-carrier}. Its analytic coordinates are exactly the compact source row, the continuous-map family ledger, the equicontinuity modulus row, the totally-bounded finite-net row, the stream windows, the regular readback, and the Real seal.

Compact source visibility comes from $X$ and $W$. A sampled source point has no independent coordinate, so it must be requested through the displayed observation row and then interpreted by the compact-metric source packet.

Family graph visibility comes from $F$. The carrier stores graph reads only as continuous-map rows, hence a sampled value cannot be read before the relevant family graph row is displayed.

Equicontinuity stability and finite-net exactness are the rows $E$ and $B$. The modulus row translates tolerance requests into source-window control, and the finite-net row is the only place where net centers and radius budgets are named.

Finally inspect $H,C,P,N$. These rows transport, replay, record provenance for, and name exactly the displayed packet, so the excluded principles have no row from which a local certificate could read them.
\end{proof}

\begin{theorem}[Compact totally bounded function-family finite-net consumer]
\label{thm:compact-totally-bounded-function-family-finite-net-consumer}
Every compact-uniform or Arzela--Ascoli-facing consumer of an accepted $\CompactTotallyBoundedFunctionFamilyUp$ carrier factors through the finite route
$$
\CompactMetricUp,\ContinuousMapUp,\EquicontinuityUp,\StreamNameUp,\RegSeqRatUp,\RealUp .
$$
The route exposes compact source windows, graph samples, equicontinuity moduli, finite totally-bounded net centers, regular value readbacks, and terminal Real seals. It exports no open-cover compactness, selected subsequence, quotient function-space equality, countable-choice selector, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from an accepted carrier $K=(X,F,E,B,W,R,V,H,C,P,N)$. A compact-uniform consumer first reads the source windows through $X$ and $W$, because the packet has no host source-point selector.

The value side then reads $F$, $R$, and $V$. The continuous-map family ledger supplies graph samples, the regular-rational row records their finite readback, and the Real seal is reached only after that readback is visible.

The compact-family comparison is confined by $E$ and $B$. Equicontinuity supplies the tolerance-to-window handoff, while the finite-net row supplies the displayed centers and radius budgets consumed by the downstream compactness route.

The structural rows $H,C,P,N$ preserve exactly this route. Since the carrier has no row for open covers, selected subsequences, quotient function-space equality, countable choice, or ambient Real completeness, the consumer cannot export any of those objects.
\end{proof}

\closureat{CompactTotallyBoundedFunctionFamilyUp}{seedStr}

\begin{closurestatus}{\CompactTotallyBoundedFunctionFamilyUp}
  \constructivestory{A $\CompactTotallyBoundedFunctionFamilyUp$ packet is generated from a CompactMetricUp source row, ContinuousMapUp family rows, an EquicontinuityUp modulus row, a totally-bounded finite-net row, StreamNameUp observation rows, RegSeqRatUp value readback rows, RealUp value seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-totally-bounded-function-family-carrier}, \autoref{thm:compact-totally-bounded-function-family-namecert-obligations}, and \autoref{thm:compact-totally-bounded-function-family-finite-net-consumer} seed the finite-net compactness data for displayed continuous-map families.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subsequences, quotient function-space equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact source visibility, family graph visibility, equicontinuity stability, finite-net exactness, value readback, transport, replay, and non-escape rows for BEDC.Derived.CompactTotallyBoundedFunctionFamilyUp.}
\end{closurestatus}
