\chapter{A Concrete Naming Certificate for $\HyperbolicBarycenterUp$}
\label{ch:concrete-instances-hyperbolic-barycenter-namecert}
\origin{human}

Following Bridson--Haefliger and the standard finite comparison view of hyperbolic metric geometry, the $\HyperbolicBarycenterUp$ packet records a weighted barycenter readback as finite NameCert data. It sits after the metric rows of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the four-point coarse-negative-curvature surface of \autoref{ch:concrete-instances-gromov-hyperbolic-metric-namecert}, the boundary-direction rows of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, and the terminal scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite averaging-style route over displayed metric, boundary, weight, and seal rows; it is not a completed hyperbolic space, a quotient boundary measure, a selected minimizer in an ambient space, countable choice, or a global convexity theorem.

\begin{definition}[Hyperbolic barycenter carrier]
\label{def:hyperbolic-barycenter-carrier}
A $\HyperbolicBarycenterUp$ carrier is a finite $\BHist$ packet
$$
B=(M,G,W,U,R,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicMetricUp$ metric-read row for the finite family being averaged.
\item $G$ is the $\GromovHyperbolicMetricUp$ comparison row, recording the four-point or coarse hyperbolicity budget used by the read.
\item $W$ is the finite weight ledger, recording the nonnegative displayed weights and their finite normalization row.
\item $U$ is the $\HyperbolicBusemannBoundaryUp$ boundary-direction row used when the barycenter is read through a boundary-facing shadow.
\item $R$ is the $\RealUp$ seal row for the scalar comparison budget attached to the weighted readback.
\item $T$ is the transport row binding the same metric, hyperbolicity, weight, boundary, and scalar rows under componentwise comparison.
\item $H$ records componentwise $\hsame$ transport for the metric, comparison, weight, boundary, scalar, transport, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a barycenter request through $M,G,W,U,R,T,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicBarycenterUp$, $\HyperbolicMetricUp$, $\GromovHyperbolicMetricUp$, $\HyperbolicBusemannBoundaryUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicBarycenterUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,G,W,U,R,T,H,C,P,N$. It has no coordinate for a host minimizer, quotient boundary probability measure, selected global convexity center, countable-choice family, or ambient hyperbolic completion.
\end{definition}

\begin{theorem}[Hyperbolic barycenter NameCert obligations]
\label{thm:hyperbolic-barycenter-namecert-obligations}
For every accepted $\HyperbolicBarycenterUp$ carrier
$$
B=(M,G,W,U,R,T,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicBarycenterUp}$ surface consists exactly of metric-read admission, Gromov-hyperbolic comparison exposure, finite weight-ledger admission, Busemann-boundary handoff, Real-seal scalar budgeting, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-barycenter-carrier}. The rows $M,G,W,U,R,T,H,C,P,N$ are the complete accepted packet.

The metric and comparison rows supply the hyperbolic geometry that the barycenter read may inspect. The finite weight ledger records the displayed coefficients, while the Busemann-boundary and Real-seal rows expose boundary direction and scalar budget only as named rows.

Transport, replay, provenance, and local naming preserve this inventory. Since no carrier coordinate stores a host minimizer, quotient measure, selected center, countable-choice family, or completed hyperbolic space, those objects are outside the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic barycenter boundary handoff]
\label{thm:hyperbolic-barycenter-boundary-handoff}
Every boundary-facing read of an accepted $\HyperbolicBarycenterUp$ packet factors through
$$
\HyperbolicMetricUp,\ \GromovHyperbolicMetricUp,\ \HyperbolicBusemannBoundaryUp,\ \RealUp,\ H,C,P,N .
$$
The exported barycenter row is therefore a finite weighted readback over displayed metric, coarse-hyperbolic, boundary, and scalar rows, not a quotient boundary measure or selected global center.
\end{theorem}
\begin{proof}
Start with the NameCert obligations. They require the metric row $M$, comparison row $G$, weight ledger $W$, boundary row $U$, and scalar seal $R$ before any barycenter read is exported.

The boundary-facing consumer can read $U$ only as part of that same packet. It cannot detach the Busemann row from the metric and hyperbolicity rows, because replay through $C$ preserves the carried order.

Thus the handoff remains finite and row-scoped. The structural rows $H,P,N$ transport, source, and name the same route, leaving no surface for a quotient measure, selected center, or ambient completion.
\end{proof}

\begin{theorem}[Hyperbolic barycenter non-escape]
\label{thm:hyperbolic-barycenter-nonescape}
No accepted $\HyperbolicBarycenterUp$ consumer reaches a barycenter read without first displaying the hyperbolic metric row, Gromov comparison row, finite weight ledger, Busemann-boundary row, Real scalar seal, componentwise transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Use the carrier inventory of \autoref{def:hyperbolic-barycenter-carrier}. A barycenter read is exposed only through $M,G,W,U,R,T,H,C,P,N$.

Apply \autoref{thm:hyperbolic-barycenter-boundary-handoff}. It keeps boundary-facing and scalar-facing reads attached to the same finite weighted route. Omitting any listed row changes the carrier rather than preserving the accepted packet.

Therefore a claimed barycenter read that bypasses metric, comparison, weight, boundary, scalar, transport, replay, provenance, or local naming data has no local NameCert row and is rejected.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicBarycenterUp$ packet exposes HyperbolicMetric, GromovHyperbolicMetric, finite weight-ledger, HyperbolicBusemannBoundary, Real-seal, hsame, Cont, Pkg, and NameCert rows before any barycenter read is exported.}

\independenceWitness{hyperbolic-metric, gromov-hyperbolic-metric, hyperbolic-busemann-boundary, real: none is bijective with $\HyperbolicBarycenterUp$ because the barycenter packet jointly binds metric reads, coarse-hyperbolic comparison, finite weights, boundary direction, scalar seal, transport, replay, provenance, and local naming as one finite weighted readback route.}

\closureat{HyperbolicBarycenterUp}{seedStr}

\begin{closurestatus}{\HyperbolicBarycenterUp}
  \constructivestory{A $\HyperbolicBarycenterUp$ packet is generated from HyperbolicMetric rows, GromovHyperbolicMetric comparison rows, finite weight ledgers, HyperbolicBusemannBoundary rows, a RealUp scalar seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-barycenter-carrier}, \autoref{thm:hyperbolic-barycenter-namecert-obligations}, \autoref{thm:hyperbolic-barycenter-boundary-handoff}, and \autoref{thm:hyperbolic-barycenter-nonescape} seed the finite weighted hyperbolic barycenter route over metric, coarse-hyperbolic, boundary, scalar, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a completed hyperbolic space, quotient boundary measure, selected global center, countable-choice family, ambient convexity theorem, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the displayed carrier and prove metric exposure, finite weight-ledger admission, Busemann-boundary handoff, scalar-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicBarycenterUp.}
\end{closurestatus}
