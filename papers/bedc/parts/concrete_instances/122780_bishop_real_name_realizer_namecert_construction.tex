\chapter{A Concrete Naming Certificate for $\BishopRealNameRealizerUp$}
\label{ch:concrete-instances-bishop-real-name-realizer-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopRealNameRealizerUp$ packet records the finite route by which a rational source window is realized as a constructive real name before any host real equality, quotient class, countable-choice selector, or ambient completeness theorem is available. It sits beside the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the located-real witness surface of \autoref{ch:concrete-instances-bishop-located-real-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite real-name realization layer for later Real and RegSeqRat consumers, not a completed host real.

\begin{definition}[Bishop real-name realizer carrier]
\label{def:bishop-real-name-realizer-carrier}
A $\BishopRealNameRealizerUp$ carrier is a finite $\BHist$ packet
$$
R=(Q,W,D,G,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the rational source-window row, listing the finite rational observations and requested precision bounds before any real-facing read is allowed.
\item $W$ is the $\StreamNameUp$ finite observation window opened for exactly the positions named by $Q$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic radii, slack bounds, and comparison budgets used by the window.
\item $G$ is the $\RegSeqRatUp$ readback row, exposing the same window as regular rational data.
\item $L$ is the $\BishopLocatedRealUp$ witness row, recording located lower and upper tests only through the displayed rational and dyadic rows.
\item $E$ is the terminal $\RealUp$ seal, exposed only after $Q,W,D,G,L$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for the source window, stream window, dyadic ledger, regular readback, located witness, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a real-name request through $Q,W,D,G,L,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopRealNameRealizerUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\BishopLocatedRealUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealNameRealizerUp}$ row.
\end{enumerate}
The classifier compares accepted packets only by $Q,W,D,G,L,E,H,C,P,N$. It has no coordinate for host real equality, quotient classes of names, countable choice, trichotomy, selected limits, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.BishopRealNameRealizerUp}

\begin{theorem}[Bishop real-name realizer NameCert obligations]
\label{thm:bishop-real-name-realizer-namecert-obligations}
For every accepted $\BishopRealNameRealizerUp$ carrier $R=(Q,W,D,G,L,E,H,C,P,N)$, the local $\NameCert_{\BishopRealNameRealizerUp}$ surface consists exactly of rational source-window exposure through $Q$, finite $\StreamNameUp$ observation through $W$, $\DyadicRatCoreUp$ tolerance accounting through $D$, $\RegSeqRatUp$ readback through $G$, $\BishopLocatedRealUp$ witness exposure through $L$, terminal $\RealUp$ sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-name-realizer-carrier}. The finite analytic rows are exactly $Q,W,D,G,L,E$, and the structural rows are exactly $H,C,P,N$.

The source window $Q$ fixes the rational data being realized. The finite observation row $W$ supplies only the named stream positions, while the dyadic ledger $D$ supplies the comparison budget for those observations.

The real-facing rows remain downstream. The regular rational row $G$ reads the displayed observations, the located witness $L$ records only the located tests supported by the same rational and dyadic budget, and the terminal seal $E$ is reached only after those rows have been replayed.

Finally $H,C,P,N$ preserve, replay, source, and name the same packet. Since no excluded host equality, quotient, choice, trichotomy, selected-limit, or ambient-completeness object is a carrier coordinate, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop real-name realizer finite-window readback]
\label{thm:bishop-real-name-realizer-finite-window-readback}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\BishopRealNameRealizerUp$ carrier reads the real name through the finite route
$$
Q \longmapsto W \longmapsto D \longmapsto G \longmapsto L \longmapsto E \longmapsto H,C,P,N .
$$
The exported read is a finite real-name realization from displayed rational windows; it is not an equality class of streams, a choice-selected Cauchy limit, host real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:bishop-real-name-realizer-namecert-obligations}. It requires every real-name read to expose the rational source window, StreamName observation window, dyadic tolerance ledger, regular rational readback, located witness, Real seal, transport, replay, provenance, and local naming rows.

The readback route is finite because $Q$ and $W$ name only displayed windows. The dyadic row $D$ fixes the tolerance ledger for the window, and $G$ is the only regular-rational readback row available to the consumer.

The located witness $L$ and real seal $E$ are downstream of that finite route. Componentwise transport, replay, provenance, and naming through $H,C,P,N$ preserve the same order and do not add a detached tail or completed host object.
\end{proof}

\begin{theorem}[Bishop real-name realizer Real-seal nonescape]
\label{thm:bishop-real-name-realizer-real-seal-nonescape}
For every accepted $\BishopRealNameRealizerUp$ carrier $R=(Q,W,D,G,L,E,H,C,P,N)$, the terminal $\RealUp$ seal $E$ is readable only after the rational source window $Q$, finite observation window $W$, dyadic tolerance ledger $D$, regular rational readback $G$, located-real witness $L$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming $N$ have been exposed. Thus $E$ cannot be consumed as host real equality, quotient-name identity, countable choice, a selected limit, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-name-realizer-carrier}. The only Real-facing coordinate is $E$, and it appears in the same finite packet as $Q,W,D,G,L,H,C,P,N$.

Apply finite-window readback, \autoref{thm:bishop-real-name-realizer-finite-window-readback}. It places $E$ after the rational source, StreamName window, dyadic tolerance ledger, RegSeqRat readback, and Bishop located witness.

The refusal follows from the absence of the excluded objects in the carrier. Since no row stores host real equality, quotient-name identity, countable choice, selected limits, trichotomy, or ambient completeness, the Real seal has no escape route outside the displayed NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopRealNameRealizerUp$ read that reaches a Real seal factors through one rational source window, one StreamName finite observation window, one DyadicRatCore tolerance ledger, one RegSeqRat readback, one BishopLocatedReal witness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{streamname, dyadicratcore, regseqrat, bishoplocatedreal, real: $\BishopRealNameRealizerUp$ is not bijective to any listed sibling because it jointly carries a rational source window, finite observation window, dyadic tolerance ledger, regular readback, located witness, Real seal, componentwise transport, replay, provenance, and local naming as one realization packet.}

\closureat{BishopRealNameRealizerUp}{seedStr}

\begin{closurestatus}{\BishopRealNameRealizerUp}
  \constructivestory{A $\BishopRealNameRealizerUp$ packet is generated from a rational source window, StreamName finite observation window, DyadicRatCore tolerance ledger, RegSeqRat readback, BishopLocatedReal witness, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-name-realizer-carrier}, \autoref{thm:bishop-real-name-realizer-namecert-obligations}, \autoref{thm:bishop-real-name-realizer-finite-window-readback}, and \autoref{thm:bishop-real-name-realizer-real-seal-nonescape} seed the finite constructive real-name realization route from rational windows to a downstream RealUp seal.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.BishopRealNameRealizerUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real equality, quotient-name identity, countable choice, selected limits, trichotomy, ambient $\RealUp$ completeness, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window readback, located-witness exposure, Real-seal nonescape, componentwise transport, replay, provenance, and local naming for BEDC.Derived.BishopRealNameRealizerUp.}
\end{closurestatus}
