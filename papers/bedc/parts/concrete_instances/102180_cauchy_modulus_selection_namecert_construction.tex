\chapter{A Concrete Naming Certificate for $\CauchyModulusSelectionUp$}
\label{ch:concrete-instances-cauchy-modulus-selection-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\CauchyModulusSelectionUp$ packet records the finite threshold-selection surface that a regular Cauchy request exposes before any completed real object or quotient stream is consulted. It sits on the L10 route through the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational source row of \autoref{ch:concrete-instances-regseqrat-namecert}, the Real boundary of \autoref{ch:concrete-instances-real-namecert}, and the extraction discipline of \autoref{ch:concrete-instances-cauchymodulusextraction-namecert}. The packet records threshold selection for displayed requests only; it is not host choice, quotient stream equality, a selected limit, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Cauchy modulus-selection carrier]
\label{def:cauchy-modulus-selection-carrier}
A $\CauchyModulusSelectionUp$ carrier is a finite $\BHist$ packet
$$
X=(R,D,W,M,T,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row naming the regular rational sequence surface whose requests are being served.
\item $D$ is the $\DyadicRatCoreUp$ tolerance request row, including the displayed dyadic radius, slack, comparison, and scale data used by the selection.
\item $W$ is the finite $\StreamNameUp$ observation-window family available for the request.
\item $M$ is the selected unary threshold row, read as a modulus value for the displayed tolerance and source row.
\item $T$ is the request-threshold exactness ledger, recording that the threshold in $M$ answers the tolerance request in $D$ using only the inspected windows in $W$ over the source row $R$.
\item $E$ is the $\RealUp$ request boundary, exposed only after $R,D,W,M,T$ have been displayed and never as a completed limit or ambient completeness witness.
\item $H$ records componentwise $\hsame$ transport for the regular source, dyadic request, finite windows, selected threshold, exactness ledger, Real boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from every Real-facing request through $D,W,R,M,T,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchyModulusSelectionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyModulusExtractionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyModulusSelectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,D,W,M,T,E,H,C,P,N$. It has no coordinate for host-level choice, quotient stream equality, selected limits, trichotomy, ambient $\RealUp$ completeness, Lean verification, or standard bridge data.
\end{definition}
\leanchecked{BEDC.Derived.CauchyModulusSelectionUp.CauchyModulusSelectionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Cauchy modulus-selection NameCert obligations]
\label{thm:cauchy-modulus-selection-namecert-obligations}
For every accepted $\CauchyModulusSelectionUp$ carrier
$$
X=(R,D,W,M,T,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyModulusSelectionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the $\RegSeqRatUp$ source row, $\DyadicRatCoreUp$ tolerance request, finite $\StreamNameUp$ windows, selected unary threshold, exactness ledger, Real request boundary, transport, replay, provenance, and local naming;
\item selected-threshold exactness, requiring $T$ to bind the selected row $M$ to the displayed tolerance request in $D$ over the source row $R$;
\item dyadic tolerance compatibility, requiring every comparison and slack row used by $M$ and $T$ to be read through $D$;
\item finite-window handoff, requiring every source observation used by the selection to occur inside the displayed window family $W$;
\item $\RegSeqRatUp$ readback, requiring the source data consumed by the selector to be read through $R$ before $M$ is exported;
\item $\RealUp$ non-escape, requiring the boundary row $E$ to remain downstream of $R,D,W,M,T$ and to add no selected limit, quotient equality, trichotomy, or ambient completeness principle;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No local modulus-selection obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-selection-carrier}. The analytic coordinates are exactly the regular rational source $R$, the dyadic tolerance request $D$, the finite windows $W$, the selected threshold $M$, the exactness ledger $T$, and the Real request boundary $E$.

Threshold exactness is the role of $T$. It records that $M$ answers the tolerance request in $D$ by reading the source row $R$ only through the finite windows $W$.

Dyadic compatibility is forced by the absence of any second tolerance coordinate. Every radius, slack row, and comparison used by the selection is therefore read through $D$.

The handoff to Real-facing consumers is terminal. The row $E$ is downstream of $R,D,W,M,T$, while $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for host choice, quotient equality, selected limits, trichotomy, ambient completeness, Lean verification, or bridge data, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cauchy modulus-selection Real request handoff]
\label{thm:cauchy-modulus-selection-real-request-handoff}
Every $\RealUp$-facing finite request accepted by a $\CauchyModulusSelectionUp$ carrier
$$
X=(R,D,W,M,T,E,H,C,P,N)
$$
factors through the finite route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
as recorded by the displayed rows $D,W,R,E$, with the selected threshold row $M$ and exactness ledger $T$ visible before the Real seal is reached.
\end{theorem}
\begin{proof}
Begin with a Real-facing request. By the carrier definition, the request boundary is the row $E$, and $E$ is available only after the dyadic request, finite windows, regular source, selected threshold, and exactness ledger have been displayed.

Read the route backward from $E$. The tolerance side is the $\DyadicRatCoreUp$ row $D$, the finite observation surface is the $\StreamNameUp$ row $W$, and the source readback is the $\RegSeqRatUp$ row $R$.

The selected threshold $M$ is not external to this route. The ledger $T$ binds it to the same $D,W,R$ data before any consumer can inspect the Real boundary.

Finally, $H,C,P,N$ preserve and name the displayed route. Hence the accepted request factors through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ in that order, with the selected threshold visible before the Real seal and with no bypass through host choice, quotient stream equality, selected limits, trichotomy, ambient completeness, Lean verification, or bridge checking.
\end{proof}

\begin{theorem}[Cauchy modulus-selection threshold uniqueness]
\label{thm:cauchy-modulus-selection-threshold-uniqueness}
For every accepted $\CauchyModulusSelectionUp$ carrier
$$
X=(R,D,W,M,T,E,H,C,P,N),
$$
every exported threshold for the displayed request is read through the single selected row $M$ and certified by the exactness ledger $T$. No consumer of the packet can introduce a second modulus row outside the displayed coordinates $R,D,W,M,T,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-selection-carrier}. Its only coordinate whose role is a selected unary threshold is $M$, and its only coordinate whose role is request-threshold exactness is $T$.

Apply \autoref{thm:cauchy-modulus-selection-namecert-obligations}. The obligation surface binds selected-threshold exactness to $T$ and requires $T$ to bind $M$ to the displayed dyadic request in $D$ over the source row $R$.

The classifier has no further threshold coordinate. Therefore any exported threshold visible to a consumer must be the row already displayed as $M$, certified by $T$, and transported or replayed only through $H,C,P,N$.
\end{proof}

\begin{theorem}[Cauchy modulus-selection dyadic monotonicity]
\label{thm:cauchy-modulus-selection-dyadic-monotonicity}
For every accepted $\CauchyModulusSelectionUp$ carrier
$$
X=(R,D,W,M,T,E,H,C,P,N),
$$
any refinement of a dyadic tolerance request must be routed through the same $\DyadicRatCoreUp$ tolerance ledger $D$ before the selected threshold row $M$ and exactness ledger $T$ are visible. In particular, refined requests cannot bypass the dyadic tolerance ledger or the finite $\StreamNameUp$ windows.
\end{theorem}
\begin{proof}
Read the analytic route from the carrier. The tolerance request is $D$, the finite observation surface is $W$, the regular rational source row is $R$, and the selected threshold and exactness rows are $M,T$.

Apply \autoref{thm:cauchy-modulus-selection-real-request-handoff}. Every Real-facing request reaches the boundary $E$ only after passing through the displayed $D,W,R$ route with $M$ and $T$ visible before the Real seal.

A refined dyadic tolerance request is still a dyadic request. Since the carrier has no second tolerance ledger and no external window coordinate, refinement can only change the data read inside $D$ and then pass through $W,R,M,T$ in the same order.
\end{proof}

\begin{theorem}[Cauchy modulus-selection transport closure]
\label{thm:cauchy-modulus-selection-transport-closure}
If two accepted $\CauchyModulusSelectionUp$ packets are classifier-related through their displayed rows, then their selected threshold rows transport componentwise through $H$ and replay the same finite route through $C$. The transported route preserves the order $D,W,R,M,T,E$ and introduces no external modulus row, tolerance ledger, Real limit, or quotient stream equality.
\end{theorem}
\begin{proof}
Use the carrier coordinates for the two accepted packets. Classifier relatedness is measured only through the displayed rows $R,D,W,M,T,E,H,C,P,N$, so componentwise comparison has a threshold component, an exactness component, a Real-boundary component, and the transport and replay components.

The row $H$ records the componentwise $\hsame$ transport for the regular source, dyadic request, finite windows, selected threshold, exactness ledger, Real boundary, replay, provenance, and local naming rows. Transport of the selected threshold is therefore the $M$ component of the same displayed relation, not a new selector.

The row $C$ records displayed $\Cont$ replay from every Real-facing request through $D,W,R,M,T,E,H,P,N$. Replaying after transport follows that route in the same order, so the transported packet preserves the finite handoff and remains inside the original classifier surface.
\end{proof}

\closureat{CauchyModulusSelectionUp}{seedStr}

\begin{closurestatus}{\CauchyModulusSelectionUp}
  \constructivestory{A $\CauchyModulusSelectionUp$ packet is generated from a RegSeqRat source row, a DyadicRatCore tolerance request, finite StreamName observation windows, a selected unary threshold row, a request-threshold exactness ledger, a RealUp request boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-modulus-selection-carrier}, \autoref{thm:cauchy-modulus-selection-namecert-obligations}, \autoref{thm:cauchy-modulus-selection-real-request-handoff}, \autoref{thm:cauchy-modulus-selection-threshold-uniqueness}, \autoref{thm:cauchy-modulus-selection-dyadic-monotonicity}, and \autoref{thm:cauchy-modulus-selection-transport-closure} seed the finite threshold-selection certificate for regular Cauchy requests on the L10 route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host choice, quotient stream equality, selected limits, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, selected-threshold exactness, dyadic tolerance compatibility, finite-window handoff, RegSeqRat readback, RealUp non-escape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyModulusSelectionUp.}
\end{closurestatus}
