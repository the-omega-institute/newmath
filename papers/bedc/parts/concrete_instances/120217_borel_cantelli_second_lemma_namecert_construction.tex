\chapter{A Concrete Naming Certificate for $\BorelCantelliSecondLemmaUp$}
\label{ch:concrete-instances-borel-cantelli-second-lemma-namecert}
\origin{human}

Following Borel, Cantelli, and standard probability texts, the $\BorelCantelliSecondLemmaUp$ packet records the independence-side converse deliberately absent from the first-lemma and finite-window packets of \autoref{ch:concrete-instances-borel-cantelli-namecert} and \autoref{ch:concrete-instances-finite-borel-cantelli-namecert}. It sits over the normalized probability source of \autoref{ch:concrete-instances-probspace-namecert}, the finite independence ledger of \autoref{ch:concrete-instances-independence-namecert}, and the terminal scalar readback of \autoref{ch:concrete-instances-real-namecert}. The packet names only the finite event-sequence, divergent partial-sum, and independence witness rows needed to expose the converse route as BEDC data; it is not a completed almost-sure quotient, a countable-choice selector, a host product-measure theorem, or an ambient probability-limit principle.

\begin{definition}[Borel--Cantelli second-lemma carrier]
\label{def:borel-cantelli-second-lemma-carrier}
A $\BorelCantelliSecondLemmaUp$ carrier is a finite $\BHist$ packet
$$
S=(\Omega,E,W,I,A,D,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $\Omega$ is the $\ProbSpaceUp$ source row naming the probability carrier from which all event reads are taken.
\item $E$ is the event-sequence row, recording the displayed event names, membership readbacks, and measurable-event witnesses.
\item $W$ is the finite repeated-hit window row, exposing the inspected prefix and tail windows before any limiting conclusion is visible.
\item $I$ is the $\IndependenceUp$ row for the same displayed event family.
\item $A$ is the pairwise or finite-family independence audit row consumed by the converse route.
\item $D$ is the divergent partial-probability-sum row for the inspected event probabilities.
\item $R$ is the repeated-hit request row, recording that the consumer asks for infinitely many displayed hits only through finite windows.
\item $L$ is the converse ledger row, recording the route from independence and divergent partial sums to the repeated-hit surface.
\item $H$ records componentwise $\hsame$ transport for $\Omega,E,W,I,A,D,R,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay from $\Omega$ through $E,W,I,A,D,R,L$ before the local name is exposed.
\item $P$ is the $\Pkg$ provenance over $\BorelCantelliSecondLemmaUp$, $\BorelCantelliUp$, $\FiniteBorelCantelliUp$, $\ProbSpaceUp$, $\IndependenceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BorelCantelliSecondLemmaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $\Omega,E,W,I,A,D,R,L,H,C,P,N$. It has no coordinate for a completed sigma algebra, quotient almost-sure equality, selected infinite subsequence, host product measure, or ambient real convergence theorem.
\end{definition}

\begin{theorem}[Borel--Cantelli second-lemma NameCert obligations]
\label{thm:borel-cantelli-second-lemma-namecert-obligations}
For every accepted $\BorelCantelliSecondLemmaUp$ carrier $S=(\Omega,E,W,I,A,D,R,L,H,C,P,N)$, the local $\NameCert_{\BorelCantelliSecondLemmaUp}$ surface consists exactly of probability-source admission through $\Omega$, event-sequence exposure through $E$, repeated-hit window exposure through $W$, independence exposure through $I,A$, divergent partial-sum exposure through $D$, repeated-hit request exposure through $R$, converse ledgering through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:borel-cantelli-second-lemma-carrier}. The displayed tuple exposes the probability source, event sequence, finite windows, independence rows, divergent partial-sum row, repeated-hit request, converse ledger, transport, replay, provenance, and local naming.

The event route must pass through $\Omega,E,W$ before any converse ledger is read. The independence rows $I,A$ and the divergent partial-sum row $D$ are therefore attached to the same displayed event family and finite windows, rather than to an ambient host sequence.

Finally $R,L,H,C,P,N$ record the repeated-hit request, the converse route, structural transport, replay, source, and local name. Since the packet has no coordinate for a completed sigma algebra, quotient almost-sure equality, selected infinite subsequence, host product measure, or ambient convergence theorem, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Borel--Cantelli second-lemma independence route]
\label{thm:borel-cantelli-second-lemma-independence-route}
Every accepted $\BorelCantelliSecondLemmaUp$ converse read factors through
$$
\BorelCantelliUp,\ \FiniteBorelCantelliUp,\ \IndependenceUp,\ \ProbSpaceUp,\ \RealUp .
$$
The probability source and event-sequence rows are exposed before the finite repeated-hit windows; the independence audit and divergent partial-sum row are then read over that same displayed event family; only after those rows are visible may the converse ledger expose the repeated-hit request. The route exports no first-lemma summability conclusion, no finite-window-only bound, no countable-choice tail selector, no quotient almost-sure equality, and no host product-measure completion.
\end{theorem}
\begin{proof}
Start from the sibling first-lemma and finite-window packets. They provide the event-sequence and bounded-window boundaries named in \autoref{ch:concrete-instances-borel-cantelli-namecert} and \autoref{ch:concrete-instances-finite-borel-cantelli-namecert}, while leaving the independence converse off their public surfaces.

Next read $\Omega,E,W,I,A,D$ in order. The probability source of \autoref{ch:concrete-instances-probspace-namecert} supplies event probabilities, the independence packet of \autoref{ch:concrete-instances-independence-namecert} supplies the displayed independence audit, and the $\RealUp$ surface of \autoref{ch:concrete-instances-real-namecert} supplies the scalar partial-sum readback. These rows share the same event family.

The repeated-hit request $R$ and ledger $L$ are therefore local consumers of the displayed independence and divergence evidence. Componentwise transport, replay, provenance, and local naming through $H,C,P,N$ preserve that route and do not add a quotient almost-sure object, selected infinite subsequence, host product measure, or ambient convergence principle.
\end{proof}

\closureat{BorelCantelliSecondLemmaUp}{seedStr}

\begin{closurestatus}{\BorelCantelliSecondLemmaUp}
  \constructivestory{A $\BorelCantelliSecondLemmaUp$ packet is generated from a probability source row, event-sequence row, finite repeated-hit windows, displayed independence rows, divergent partial-probability sums, a repeated-hit request, a converse ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:borel-cantelli-second-lemma-carrier}, \autoref{thm:borel-cantelli-second-lemma-namecert-obligations}, and \autoref{thm:borel-cantelli-second-lemma-independence-route} seed the independence-side Borel--Cantelli converse route as a finite NameCert surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed sigma algebras, quotient almost-sure equality, countable-choice tail selection, host product-measure completion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, event-window exactness, independence audit exposure, divergent partial-sum routing, repeated-hit request non-escape, converse ledger exactness, transport, replay, provenance, and local naming for BEDC.Derived.BorelCantelliSecondLemmaUp.}
\end{closurestatus}
