\chapter{A Concrete Naming Certificate for $\MooreOsgoodUp$}
\label{ch:concrete-instances-moore-osgood-namecert}
\origin{human}

Following Moore and Osgood on iterated-limit interchange, the $\MooreOsgoodUp$ packet records the finite route by which a two-parameter real-name window may hand a first-index limit row through a uniform second-index tail ledger before a downstream consumer reads the opposite iterated limit. It sits between the double-sequence surface of \autoref{ch:concrete-instances-cauchy-double-sequence-namecert}, the uniform-limit discipline of \autoref{ch:concrete-instances-uniformlimit-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite certificate for an iterated-limit handoff; it is not a host double-limit convergence theorem, quotient equality of arrays, a selected limiting value, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Moore-Osgood carrier]\label{def:moore-osgood-carrier}
A $\MooreOsgoodUp$ carrier is a finite $\BHist$ packet
$$
K=(W,F,S,U,Q,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the two-parameter window family, listing the finite rectangle of inspected indices together with the requested first-index and second-index tolerances.
\item $F$ is the first-limit row, recording the displayed regular readback of the first iterated direction over the windows in $W$.
\item $S$ is the second-limit row, recording the displayed regular readback of the second iterated direction over the same window family.
\item $U$ is the uniform tail ledger in one index, binding a finite tolerance request to a tail bound that is stable across the displayed cofinal side of the other index.
\item $Q$ is the cofinal schedule row, naming the finite schedule that routes window enlargement, tail entry, and iterated-limit handoff.
\item $R$ is the $\RegSeqRatUp$ handoff row for the two iterated regular-rational reads exposed by $F$ and $S$.
\item $D$ is the $\DyadicRatCoreUp$ ledger for rectangle tolerances, uniform tails, cofinal schedule bounds, and accumulated arithmetic error.
\item $E$ is the $\RealUp$ seal reached only after the window family, iterated rows, uniform ledger, schedule, regular readback, and dyadic accounting have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the window, iterated-limit, uniform-ledger, schedule, readback, dyadic, real-seal, continuation, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from rectangle inspection to first-limit and second-limit rows, from the uniform tail ledger to the cofinal schedule, from regular readback and dyadic accounting to the real seal, and from the seal to the local NameCert row.
\item $P$ is the $\Pkg$ provenance over $\MooreOsgoodUp$, $\CauchyDoubleSequenceUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MooreOsgoodUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $W,F,S,U,Q,R,D,E,H,C,P,N$. It has no coordinate for a completed double limit, quotient array equality, a selected limiting representative, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Moore-Osgood NameCert obligations]\label{thm:moore-osgood-namecert-obligations}
For every accepted $\MooreOsgoodUp$ carrier $K=(W,F,S,U,Q,R,D,E,H,C,P,N)$, the local $\NameCert_{\MooreOsgoodUp}$ surface consists exactly of the following six finite rows:
\begin{enumerate}
\item carrier admission for the displayed two-parameter window family, first-limit row, second-limit row, uniform tail ledger, cofinal schedule, regular readback, dyadic ledger, real seal, transport, continuation, provenance, and naming rows;
\item rectangle-window stability, requiring every first-index or second-index observation to pass through the same $W$ row before either iterated read is exposed;
\item uniform-tail control in one index, requiring $U$ to bind the tail bound before the cofinal schedule $Q$ may route an interchange handoff;
\item iterated regular readback, requiring the first-limit and second-limit rows $F$ and $S$ to be exported only through the $\RegSeqRatUp$ row $R$ and the dyadic tolerance ledger $D$;
\item real-seal admission, requiring the $\RealUp$ seal $E$ to occur after $W,F,S,U,Q,R,D$ have all been displayed;
\item ledger nonescape, excluding host double-limit convergence, quotient equality of arrays, selected limiting values, countable choice, and ambient $\RealUp$ completeness from the packet surface.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.MooreOsgoodUp.MooreOsgoodTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the carrier of \autoref{def:moore-osgood-carrier}. Carrier admission is exactly the inventory of $W,F,S,U,Q,R,D,E,H,C,P,N$, and the only available two-parameter observations are the finite rectangle windows listed by $W$.

The stability and uniform-tail clauses are then forced by the placement of $U$ and $Q$. The packet does not allow either iterated row to choose an unrecorded tail; the uniform ledger must first bind the relevant tolerance, and the cofinal schedule must record the finite route by which the chosen rectangle is enlarged.

The two iterated rows pass through $R$ and $D$ before the real seal $E$ is visible. The structural rows $H,C,P,N$ transport, replay, source, and name this same route, while the carrier contains no coordinate for the excluded host principles.
\end{proof}

\begin{theorem}[Moore-Osgood iterated-limit handoff]\label{thm:moore-osgood-iterated-limit-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an iterated-limit interchange exported by an accepted $\MooreOsgoodUp$ carrier factors through
$$
W,F,S,U,Q,R,D,E,H,C,P,N.
$$
Thus the consumer reads a finite two-parameter window family, first-limit row, second-limit row, uniform tail ledger, cofinal schedule, regular rational handoff, dyadic accounting, real seal, transport, continuation, provenance, and local naming before it can use the interchanged iterated-limit surface.
\end{theorem}
\begin{proof}
Start with a consumer of the interchange surface. By \autoref{def:moore-osgood-carrier}, the only visible array data is $W$, and the only visible iterated rows are $F$ and $S$.

A $\RegSeqRatUp$ read is therefore forced through $R$ after $U$ and $Q$ have supplied the uniform tail and cofinal schedule data. The dyadic ledger $D$ accounts for the displayed tolerances and schedule bounds, so no consumer can bypass the finite ledger route while reading either iterated row.

For a $\RealUp$ read, the same route continues to the seal $E$. The rows $H,C,P,N$ preserve the handoff under transport, replay the displayed continuations, record package provenance, and bind the local NameCert, so every downstream consumer factors through the displayed packet.
\end{proof}

\begin{theorem}[Moore-Osgood uniform tail lock]\label{thm:moore-osgood-uniform-tail-lock}
For every accepted $\MooreOsgoodUp$ carrier $K=(W,F,S,U,Q,R,D,E,H,C,P,N)$, any consumer that uses both the first-limit row $F$ and the second-limit row $S$ must read the uniform tail ledger $U$ and the cofinal schedule $Q$ before reaching the $\RegSeqRatUp$ handoff row $R$.

If the source two-parameter window is served by $\CauchyDoubleSequenceUp$, this read factors through \autoref{thm:cauchy-double-sequence-schedule-tail-exactness} and \autoref{thm:cauchy-double-sequence-completion-consumer-scope} before the $\RealUp$ seal $E$ is visible. Thus no unscheduled rectangle, quotient array, selected limit, countable-choice row, or ambient real-completeness row can enter the Moore-Osgood handoff.
\end{theorem}
\begin{proof}
Project $K$ along \autoref{def:moore-osgood-carrier}. The iterated rows $F$ and $S$ are exposed as rows of the same finite packet, while the carrier places the uniform tail ledger $U$ and the cofinal schedule $Q$ between the two-parameter window family $W$ and the regular readback row $R$.

Apply \autoref{thm:moore-osgood-iterated-limit-handoff}. A consumer of the interchanged surface can reach $R$ only through the displayed route $W,F,S,U,Q,R,D,E,H,C,P,N$, so the row pair $F,S$ has no independent path around $U$ or $Q$.

When the window family is supplied by $\CauchyDoubleSequenceUp$, use \autoref{thm:cauchy-double-sequence-schedule-tail-exactness} to force every rectangle read through the scheduled tail, then use \autoref{thm:cauchy-double-sequence-completion-consumer-scope} to keep completion-facing consumption inside that scheduled surface. Composing these two constraints with the Moore-Osgood handoff leaves only the displayed ledger and schedule route before $E$, excluding the non-carrier rows named in the statement.
\end{proof}

\begin{theorem}[Moore-Osgood Real seal nonescape]\label{thm:moore-osgood-real-seal-nonescape}
In every accepted $\MooreOsgoodUp$ carrier, the $\RealUp$ seal row $E$ is downstream of the two-parameter window family, both iterated rows, the uniform tail ledger, the cofinal schedule, the regular readback row, and the dyadic tolerance ledger. Consequently a Real-facing consumer cannot obtain from the packet a host double-limit theorem, quotient equality of arrays, a selected limiting value, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the carrier order in \autoref{def:moore-osgood-carrier}. The seal row $E$ is not an independent coordinate; it is exposed after $W,F,S,U,Q,R,D$ have already supplied the finite observations, iterated rows, uniform tail data, schedule data, regular readback, and dyadic accounting.

The nonescape statement follows from the classifier boundary. Accepted packets are compared only through $W,F,S,U,Q,R,D,E,H,C,P,N$, and none of those rows stores a completed double limit, quotient array equality, a selected limit representative, countable-choice selector, or ambient completeness witness.
\end{proof}

\falsifiablePrediction{Every $\MooreOsgoodUp$ iterated-limit read factors through a finite two-parameter window family, first-limit and second-limit rows, a uniform tail ledger in one index, a cofinal schedule, RegSeqRat handoff, DyadicRatCore accounting, a RealUp seal, transport, continuation, provenance, and local naming.}

\independenceWitness{cauchydoublesequence, uniformlimit, streamname, regseqrat, dyadicratcore, real: $\MooreOsgoodUp$ is not bijective to any listed sibling because it jointly carries two-parameter windows, paired iterated-limit rows, uniform tail control, cofinal scheduling, regular readback, dyadic tolerance accounting, real sealing, transport, continuation, provenance, and local naming.}

\closureat{MooreOsgoodUp}{seedStr}

\begin{closurestatus}{\MooreOsgoodUp}
  \constructivestory{A $\MooreOsgoodUp$ packet is generated from finite two-parameter windows, first-limit and second-limit rows, a uniform tail ledger in one index, a cofinal schedule, RegSeqRat handoff, a DyadicRatCore tolerance ledger, a RealUp seal, componentwise transport, displayed continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:moore-osgood-carrier}, \autoref{thm:moore-osgood-namecert-obligations}, \autoref{thm:moore-osgood-iterated-limit-handoff}, \autoref{thm:moore-osgood-uniform-tail-lock}, and \autoref{thm:moore-osgood-real-seal-nonescape} bind the finite iterated-limit interchange certificate surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host double-limit convergence, quotient array equality, selected limiting values, countable choice, ambient $\RealUp$ completeness, bridge checking, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, rectangle-window stability, uniform-tail control, cofinal scheduling, iterated regular readback, real-seal nonescape, and TasteGate field faithfulness for BEDC.Derived.MooreOsgoodUp.}
\end{closurestatus}
