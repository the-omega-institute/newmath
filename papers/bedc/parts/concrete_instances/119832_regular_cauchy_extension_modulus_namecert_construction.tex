\chapter{A Concrete Naming Certificate for $\RegularCauchyExtensionModulusUp$}
\label{ch:concrete-instances-regular-cauchy-extension-modulus-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy completions and uniformly continuous extension, the $\RegularCauchyExtensionModulusUp$ packet records the finite modulus data by which a uniformly continuous map on displayed regular-Cauchy dense input extends to a separated complete target. It sits after the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the unit row of \autoref{ch:concrete-instances-cauchycompletionunit-namecert}, the uniform-continuity surface of \autoref{ch:concrete-instances-uniformcontinuity-namecert}, and the universal-property packet of \autoref{ch:concrete-instances-cauchy-completion-universal-property-namecert}. The packet names the completion-extension handoff as displayed BEDC rows; it does not quotient Cauchy names, select representatives, import countable choice, or use ambient completeness outside the target metric row.

\begin{definition}[RegularCauchyExtensionModulus carrier]
\label{def:regular-cauchy-extension-modulus-carrier}
A $\RegularCauchyExtensionModulusUp$ carrier is a finite $\BHist$ packet
$$
E=(R,S,X,U,T,M,G,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row carrying the regular rational readback of the dense input name.
\item $S$ is the $\StreamNameUp$ finite-window row recording exactly which source observations are inspected before an extension value is requested.
\item $X$ is the $\RealUp$ seal row reached from the displayed regular Cauchy input.
\item $U$ is the $\CauchyCompletionUnitUp$ row embedding the dense source into the completion-facing surface.
\item $T$ is the separated target metric row, read through $\SeparatedMetricUp$ and the target's displayed complete-metric evidence.
\item $M$ is the $\UniformContinuityUp$ modulus row for the source map on the displayed dense data.
\item $G$ is the extension graph row, pairing completion-input windows with target values using only $R,S,X,U,T,M$.
\item $L$ is the uniqueness row, comparing two extension graph rows over the same source, unit, target, and modulus data by separatedness of $T$.
\item $H$ records componentwise $\hsame$ transport for $R,S,X,U,T,M,G,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay of dense-input reading, unit handoff, modulus use, graph construction, and uniqueness comparison.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyExtensionModulusUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\CauchyCompletionUnitUp$, $\UniformContinuityUp$, $\SeparatedMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyExtensionModulusUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $R,S,X,U,T,M,G,L,H,C,P,N$. It has no coordinate for quotient completion equality, selected limits, host function equality, countable-choice schedules, or ambient completeness outside the displayed separated target metric row.
\end{definition}

\begin{theorem}[RegularCauchyExtensionModulus NameCert obligations]
\label{thm:regular-cauchy-extension-modulus-namecert-obligations}
For every accepted $\RegularCauchyExtensionModulusUp$ carrier $E=(R,S,X,U,T,M,G,L,H,C,P,N)$, the local $\NameCert_{\RegularCauchyExtensionModulusUp}$ surface consists exactly of regular Cauchy source admission, finite stream-window inspection, terminal $\RealUp$ sealing, Cauchy-completion unit handoff, separated target metric exposure, uniform-continuity modulus use, extension graph construction, separated uniqueness comparison, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier exposure is projection from \autoref{def:regular-cauchy-extension-modulus-carrier}. The tuple displays the source name, inspected finite windows, terminal Real seal, completion unit, separated target metric, modulus, graph, uniqueness, transport, replay, provenance, and local naming rows.

The dense input is read through $R,S,X,U$. The regular rational row and stream windows are visible before the terminal Real seal and before the completion unit can be consumed by an extension request.

The target-facing part is read through $T,M,G,L$. The separated target metric supplies the comparison surface, the uniform-continuity modulus bounds how finite source windows determine target windows, the graph row records the extension values, and the uniqueness row compares graph rows only over the same displayed data.

The structural boundary is fixed by $H,C,P,N$. These rows transport, replay, source, and name the same finite packet, while the carrier lists no quotient completion equality, selected limit, host function equality, countable-choice schedule, or ambient completeness coordinate outside $T$.
\end{proof}

\begin{theorem}[RegularCauchyExtensionModulus factorization]
\label{thm:regular-cauchy-extension-modulus-factorization}
Every extension consumer of an accepted $\RegularCauchyExtensionModulusUp$ packet factors through
$$
\RegSeqRatUp,\ \StreamNameUp,\ \RealUp
\longmapsto
\CauchyCompletionUnitUp
\longmapsto
\UniformContinuityUp,\ \SeparatedMetricUp
\longmapsto
\BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The consumer receives a finite extension graph and a separated uniqueness row only after the source regular Cauchy data, inspected stream windows, completion unit, target metric, and uniform-continuity modulus are displayed. It obtains no quotient completion equality, selected representative, host function equality, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with an extension request for an accepted carrier $E=(R,S,X,U,T,M,G,L,H,C,P,N)$. The request can first inspect only the regular rational source row $R$, the finite window row $S$, and the terminal seal row $X$.

The completion handoff is then forced through $U$. This row is the only displayed passage from the dense regular-Cauchy data to the completion-facing input, so the request cannot substitute a quotient representative or selected limit.

Next the request reads $T,M,G,L$. The target metric row supplies separated comparison, the modulus row explains how finite source windows control target windows, the graph row records the extension values, and the uniqueness row compares only graph rows over the same visible source, unit, target, and modulus data.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since no carrier row stores host function equality, countable-choice schedules, or an ambient completeness principle, every extension consumer factors through the displayed route.
\end{proof}

\closureat{RegularCauchyExtensionModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchyExtensionModulusUp}
  \constructivestory{A $\RegularCauchyExtensionModulusUp$ packet is generated from RegSeqRat source data, StreamName finite windows, a RealUp seal, a CauchyCompletionUnit row, separated target metric evidence, a UniformContinuity modulus, an extension graph, separated uniqueness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-extension-modulus-carrier}, \autoref{thm:regular-cauchy-extension-modulus-namecert-obligations}, and \autoref{thm:regular-cauchy-extension-modulus-factorization} seed the finite completion-extension route over regular Cauchy, stream, Real, completion-unit, uniform-modulus, separated-target, graph, uniqueness, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient completion equality, selected representatives, host function equality, countable-choice schedules, or ambient completeness outside the displayed target metric row.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window inspection, unit handoff, uniform-modulus use, extension graph construction, separated uniqueness, transport replay, provenance nonescape, and local naming for BEDC.Derived.RegularCauchyExtensionModulusUp.}
\end{closurestatus}
