\chapter{A Concrete Naming Certificate for $\RegularCauchyMinmaxLatticeUp$}
\label{ch:concrete-instances-regular-cauchy-minmax-lattice-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyMinmaxLatticeUp$ packet records finite min--max lattice operations on displayed regular Cauchy rational names before any completed-real order principle is exposed. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the located/order L10 sibling of \autoref{ch:concrete-instances-regular-cauchy-located-order-namecert}. The packet names the pointwise meet and join surface for two displayed regular Cauchy names; it does not assert trichotomy, quotient stream equality, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy minmax lattice carrier]
\label{def:regular-cauchy-minmax-lattice-carrier}
A $\RegularCauchyMinmaxLatticeUp$ carrier is a finite $\BHist$ packet
$$
M=(L,R,W,D,O,m,j,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are the left and right $\RegSeqRatUp$ regular rational name rows.
\item $W$ is the shared $\StreamNameUp$ finite-window ledger used to inspect both names at the requested dyadic radius.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the comparison radius, slack, and rational cells consumed by the lattice read.
\item $O$ is the $\RegularCauchyLocatedOrderUp$ sibling row that supplies the located comparison boundary for the same windows.
\item $m$ and $j$ are the displayed meet and join rows, respectively naming the regular Cauchy readbacks for $\min(L,R)$ and $\max(L,R)$ over the inspected window.
\item $E$ is the $\RealUp$ seal row reached only after $L,R,W,D,O,m,j$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two source names, the shared window, dyadic tolerance, located-order row, meet row, join row, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each lattice request through $L,R,W,D,O,m,j,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyMinmaxLatticeUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyLocatedOrderUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyMinmaxLatticeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows and their displayed transports. It has no coordinate for a host lattice on completed reals, quotient stream equality, trichotomy, selected representatives, countable choice, or ambient completeness for $\RealUp$.
\end{definition}

\begin{theorem}[Regular Cauchy minmax lattice NameCert obligations]
\label{thm:regular-cauchy-minmax-lattice-namecert-obligations}
For every accepted $\RegularCauchyMinmaxLatticeUp$ carrier
$$
M=(L,R,W,D,O,m,j,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyMinmaxLatticeUp}$ surface consists exactly of carrier admission for the two source $\RegSeqRatUp$ rows, shared $\StreamNameUp$ windows, dyadic tolerance data, the located-order sibling row, meet and join readback rows, the terminal $\RealUp$ seal, transport, replay, provenance, and local naming; comparison visibility requiring every meet or join read to inspect $O$ after $W,D$; and nonescape excluding trichotomy, host real lattice operations, quotient stream equality, selected limits, countable choice, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-minmax-lattice-carrier}. The analytic rows are $L,R,W,D,O,m,j,E$, while $H,C,P,N$ transport, replay, source, and name the same finite packet.

Read the comparison route in order. A lattice request first inspects the two regular rational names $L,R$, then aligns them over the common finite window $W$, then consumes the dyadic tolerance row $D$, and only then reads the located-order sibling row $O$.

The meet and join rows are downstream of that comparison route. The row $m$ records the lower regular Cauchy readback and the row $j$ records the upper regular Cauchy readback, both over the same windows and tolerance data. Componentwise transport through $H$ and replay through $C$ preserve that shared input surface.

Since the carrier has no coordinate for trichotomy, quotient stream equality, selected limits, countable choice, host lattice operations on completed reals, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy minmax lattice RegSeqRat stability]
\label{thm:regular-cauchy-minmax-lattice-regseqrat-stability}
For every accepted $\RegularCauchyMinmaxLatticeUp$ packet $M=(L,R,W,D,O,m,j,E,H,C,P,N)$, the meet row $m$ and join row $j$ remain $\RegSeqRatUp$ readbacks over the same finite windows $W$ and dyadic tolerance row $D$ as the source rows $L$ and $R$. Every $\RealUp$ consumer reaches $E$ only after the route
$$
(L,R)\longmapsto W\longmapsto D\longmapsto O\longmapsto (m,j)
$$
has been displayed.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:regular-cauchy-minmax-lattice-namecert-obligations}. They force every lattice read to expose the source regular rational names, the shared stream window, and the dyadic tolerance row before either operation row is available.

Use the located-order sibling row $O$ as the comparison boundary. It is read over the same $W,D$ surface, so the lower and upper operation rows cannot switch to a different observation schedule or a different tolerance ledger.

The rows $m$ and $j$ are therefore regular rational readbacks attached to the displayed input surface. The structural rows $H,C,P,N$ preserve and name that attachment, and the terminal seal $E$ is downstream of it. Thus every Real-facing read receives the stated finite route and no extra completeness or quotient datum.
\end{proof}

\closureat{RegularCauchyMinmaxLatticeUp}{seedStr}

\begin{closurestatus}{\RegularCauchyMinmaxLatticeUp}
  \constructivestory{A $\RegularCauchyMinmaxLatticeUp$ packet is generated from two RegSeqRat source rows, a shared StreamName window, DyadicRatCore tolerance data, a RegularCauchyLocatedOrder sibling row, meet and join RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-minmax-lattice-carrier}, \autoref{thm:regular-cauchy-minmax-lattice-namecert-obligations}, and \autoref{thm:regular-cauchy-minmax-lattice-regseqrat-stability} seed the finite min--max lattice-operation surface over regular Cauchy rational names and the located/order L10 sibling.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim trichotomy, host real lattice operations, quotient stream equality, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, comparison visibility, meet and join RegSeqRat readback stability, Real-seal handoff, transport, replay, provenance, and local naming for the displayed min--max lattice packet.}
\end{closurestatus}
