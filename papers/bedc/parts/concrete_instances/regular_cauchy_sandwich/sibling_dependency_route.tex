\begin{theorem}[Regular Cauchy sandwich sibling dependency route]
\label{thm:regular-cauchy-sandwich-sibling-dependency-route}
Every accepted $\RegularCauchySandwichUp$ sandwich packet depends on the $\RegSeqRatUp$ regular readback rows of \autoref{ch:concrete-instances-regseqrat-namecert} and the $\StreamNameUp$ pointwise window rows of \autoref{ch:concrete-instances-streamname-namecert} before it exposes a terminal $\RealUp$ seal. The route consumes those siblings only through the displayed lower and upper windows, lower and upper regular readbacks, common tail row, shared dyadic ledger, transport, replay, provenance, and local naming rows of \autoref{def:regular-cauchy-sandwich-carrier}.
\end{theorem}
\begin{proof}
Read the carrier rows from \autoref{def:regular-cauchy-sandwich-carrier}. The lower and upper windows $W_L,W_U$ are the pointwise $\StreamNameUp$ windows, and the rows $R_L,R_U$ are the regular rational readbacks supplied by $\RegSeqRatUp$.

The bracketing theorem \autoref{thm:regular-cauchy-sandwich-window-bracketing} orders every precision request through those windows and readbacks before the seal row is consumed. The Real-seal nonescape theorem \autoref{thm:regular-cauchy-sandwich-real-seal-nonescape} prevents bypass through quotient stream equality, selected limits, host real equality, or ambient completeness.

Thus the chapter's sibling dependency is the concrete route through $\StreamNameUp$ and $\RegSeqRatUp$, not an external analytic carrier.
\end{proof}
