\chapter{A Concrete Naming Certificate for $\StreamNameTailWindowRouterUp$}
\label{ch:concrete-instances-streamname-tail-window-router-namecert}
\origin{human}

Following Bishop--Bridges constructive sequence bookkeeping, the $\StreamNameTailWindowRouterUp$ packet records the finite route by which a regular-Cauchy tail request is routed through stream windows, regular rational readback, dyadic tolerance arithmetic, and the terminal Real seal. It concretizes the regular-Cauchy diagonal-limit route of \autoref{ch:visions-real-completeness} and sits over the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The router is a finite precision-and-tail packet; it does not choose a limit, quotient streams by host equality, supply countable choice, or assert ambient completeness of $\RealUp$.

\begin{definition}[StreamName tail-window router carrier]\label{def:streamname-tail-window-router-carrier}
A $\StreamNameTailWindowRouterUp$ carrier is a finite $\BHist$ packet
$$
T=(r,W,Q,D,E,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
  \item $r$ is the finite request row naming the precision budget, tail threshold, and diagonal-window demand.
  \item $W$ is the $\StreamNameUp$ window row opened by that request.
  \item $Q$ is the $\RegSeqRatUp$ tail readback row for the displayed window.
  \item $D$ is the $\DyadicRatCoreUp$ tolerance row comparing the requested precision with the inspected tail cells.
  \item $E$ is the terminal $\RealUp$ seal reached after $W,Q,D$ are displayed.
  \item $H$ is componentwise $\hsame$ transport for $r,W,Q,D,E$.
  \item $C$ is displayed $\Cont$ replay for the same route.
  \item $P$ is the $\Pkg$ provenance over $\StreamNameTailWindowRouterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
  \item $N$ is the local $\NameCert_{\StreamNameTailWindowRouterUp}$ row.
\end{enumerate}
The classifier compares accepted routers only through $r,W,Q,D,E,H,C,P,N$. It has no coordinate for a selected limit, quotient stream equality, countable-choice schedule, hidden tail selector, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[StreamName tail-window router NameCert obligations]\label{thm:streamname-tail-window-router-namecert-obligations}
For every accepted $\StreamNameTailWindowRouterUp$ carrier $T=(r,W,Q,D,E,H,C,P,N)$, the local $\NameCert_{\StreamNameTailWindowRouterUp}$ surface consists exactly of carrier admission for the displayed request, StreamName window coverage, RegSeqRat tail readback, DyadicRatCore tolerance exactness, Real-seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming. Every regular-Cauchy diagonal-limit consumer must read those rows in that order before it treats $E$ as a Real-facing seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:streamname-tail-window-router-carrier}. The request row $r$ names the only precision and tail data available to the router.

The stream-window row $W$ is downstream of $r$, and the regular rational readback $Q$ is downstream of that displayed window. Thus a tail read cannot reach $\RegSeqRatUp$ without first exposing the finite StreamName window it uses.

The dyadic row $D$ supplies the tolerance arithmetic for the same request and window. Once $W,Q,D$ are present, the Real seal $E$ is available as a terminal handoff rather than as a selected limit or quotient-stream equality.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no carrier coordinate stores a hidden tail selector, countable-choice schedule, host real equality, or ambient completeness, those objects cannot be exported by the local NameCert surface.
\end{proof}

\falsifiablePrediction{Every regular-Cauchy diagonal-limit read through $\StreamNameTailWindowRouterUp$ must display the finite request, StreamName window, RegSeqRat tail readback, DyadicRatCore tolerance row, RealUp seal, transport, replay, provenance, and local NameCert rows before the Real-facing output is consumed.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real: $\StreamNameTailWindowRouterUp$ is not bijective to any listed sibling because it jointly carries a finite tail request, one StreamName window, RegSeqRat tail readback, DyadicRatCore tolerance arithmetic, and a terminal Real seal as one router packet.}

\closureat{StreamNameTailWindowRouterUp}{seedStr}

\begin{closurestatus}{\StreamNameTailWindowRouterUp}
  \constructivestory{A $\StreamNameTailWindowRouterUp$ packet is generated from a finite precision-and-tail request, a StreamName window row, RegSeqRat tail readback, DyadicRatCore tolerance arithmetic, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:streamname-tail-window-router-carrier} and \autoref{thm:streamname-tail-window-router-namecert-obligations} seed the regular-Cauchy diagonal-limit router as finite request, window, tail, tolerance, readback, and Real-seal data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient stream equality, hidden tail selectors, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{The next closure step requires checked carrier admission, window coverage, tolerance exactness, regular readback, and Real-seal handoff for the displayed router packet.}
\end{closurestatus}
