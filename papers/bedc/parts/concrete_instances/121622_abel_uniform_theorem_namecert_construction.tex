\chapter{A Concrete Naming Certificate for $\AbelUniformTheoremUp$}
\label{ch:concrete-instances-abel-uniform-theorem-namecert}
\origin{human}

Following Abel, Hardy, and Bishop--Bridges constructive analysis, the $\AbelUniformTheoremUp$ packet records the finite theorem endpoint by which Abel uniform convergence data are handed to Real-and-Completion consumers. It sits after the uniform Abel route of \autoref{ch:concrete-instances-abel-uniform-convergence-namecert}, the Abel boundary theorem of \autoref{ch:concrete-instances-abel-limit-theorem-namecert}, the Abel mean boundary of \autoref{ch:concrete-instances-abel-mean-boundary-namecert}, the uniform-limit packet of \autoref{ch:concrete-instances-uniformlimit-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter packages the traditional theorem as a finite NameCert route; it does not introduce host power-series semantics, selected sums, quotient streams, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[AbelUniformTheorem carrier]
\label{def:abel-uniform-theorem-carrier}
An $\AbelUniformTheoremUp$ carrier is a finite $\BHist$ packet
$$
A=(R,U,L,M,T,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the finite radius-window row listing the displayed radial cells and endpoint-approach windows inspected by the theorem consumer.
\item $U$ is the $\AbelUniformConvergenceUp$ boundary row, carrying the already named uniform Abel convergence surface over the same radius windows.
\item $L$ is the $\AbelLimitTheoremUp$ endpoint row, recording the boundary-limit handoff after $R$ and $U$ have been accepted.
\item $M$ is the $\AbelMeanBoundaryUp$ mean-boundary row, exposing the finite Abel-mean compatibility data used by the endpoint theorem.
\item $T$ is the $\UniformLimitUp$ tail ledger, assigning one displayed tail discipline to every radius cell in $R$ before endpoint readback.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the synchronized Abel endpoint windows.
\item $E$ is the terminal $\RealUp$ seal row available only after $R,U,L,M,T,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for radius windows, Abel uniform convergence data, Abel endpoint data, mean-boundary data, uniform-tail data, readback, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each theorem request through $R,U,L,M,T,Q,E$.
\item $P$ is the $\Pkg$ provenance over $\AbelUniformTheoremUp$, $\AbelUniformConvergenceUp$, $\AbelLimitTheoremUp$, $\AbelMeanBoundaryUp$, $\UniformLimitUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AbelUniformTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,U,L,M,T,Q,E,H,C,P,N$. It has no coordinate for host power-series semantics, selected sums, quotient streams, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[AbelUniformTheorem NameCert obligations]
\label{thm:abel-uniform-theorem-namecert-obligations}
For every accepted $\AbelUniformTheoremUp$ carrier
$$
A=(R,U,L,M,T,Q,E,H,C,P,N),
$$
the local $\NameCert_{\AbelUniformTheoremUp}$ surface consists exactly of carrier admission for the displayed rows, radius-window admission through $R$, Abel uniform convergence boundary admission through $U$, Abel endpoint admission through $L$, Abel mean-boundary compatibility through $M$, uniform-tail exactness through $T$, regular rational readback and Real seal nonescape through $Q$ and $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:abel-uniform-theorem-carrier}. Its theorem rows are $R,U,L,M,T,Q,E$, and its structural rows are $H,C,P,N$.

The radius and Abel boundary obligations are finite-row projections. The row $R$ fixes the displayed radius windows, $U$ imports the sibling $\AbelUniformConvergenceUp$ surface over those windows, $L$ records the Abel endpoint route, and $M$ records the Abel mean-boundary compatibility data.

The tail and readback obligations are downstream. The $\UniformLimitUp$ row $T$ binds one finite tail discipline to the displayed windows before the $\RegSeqRatUp$ readback $Q$ is available, and the terminal $\RealUp$ seal $E$ is read only after $R,U,L,M,T,Q$ have appeared.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no further coordinate, the listed rows exhaust the local NameCert surface and exclude host power-series semantics, selected sums, quotient streams, countable choice, and ambient completeness.
\end{proof}

\begin{theorem}[AbelUniformTheorem real-seal handoff]
\label{thm:abel-uniform-theorem-real-seal-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\AbelUniformTheoremUp$ packet reads the endpoint only through the finite route
$$
\AbelUniformConvergenceUp,\ \AbelLimitTheoremUp,\ \AbelMeanBoundaryUp,\ \UniformLimitUp,\ \RegSeqRatUp,\ \RealUp .
$$
The consumer first receives displayed radius windows, the Abel uniform convergence boundary, the endpoint Abel limit row, the Abel mean-boundary row, and one uniform-tail ledger; only then may it consume the regular rational readback and terminal Real seal. The route exports no host power-series semantics, selected sum, quotient stream equality, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from an accepted consumer read and project the carrier. The rows $R,U,L,M$ expose the radius windows, Abel uniform convergence boundary, Abel endpoint theorem row, and Abel mean-boundary row before any real-seal read can occur.

Apply the uniform-tail row $T$. It fixes the displayed tail discipline for the same radius windows and prevents a consumer from choosing a separate endpoint schedule outside the packet.

Now pass through the $\RegSeqRatUp$ readback $Q$ and the terminal $\RealUp$ seal $E$. These rows are downstream of $R,U,L,M,T$, so the Real-facing handoff cannot bypass the finite Abel theorem route.

The structural rows $H,C,P,N$ preserve the same route componentwise and add no analytic coordinate. Thus the packet exposes exactly the finite real-seal handoff stated above and none of the excluded host or choice principles.
\end{proof}

\falsifiablePrediction{Every accepted AbelUniformTheoremUp read exposes radius-window rows, an AbelUniformConvergenceUp boundary row, an AbelLimitTheoremUp endpoint row, an AbelMeanBoundaryUp compatibility row, a UniformLimitUp tail ledger, RegSeqRatUp readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert before a Real-facing consumer can inspect the endpoint.}

\independenceWitness{abel-uniform-convergence, abel-limit-theorem, abel-mean-boundary, uniformlimit, regseqrat, real: $\AbelUniformTheoremUp$ is not bijective to any listed sibling because it jointly carries radius windows, the uniform Abel boundary, endpoint Abel theorem data, Abel mean-boundary compatibility, one uniform tail ledger, regular rational readback, terminal Real sealing, transport, replay, provenance, and local naming.}

\closureat{AbelUniformTheoremUp}{seedStr}

\begin{closurestatus}{\AbelUniformTheoremUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:abel-uniform-theorem-carrier}, \autoref{thm:abel-uniform-theorem-namecert-obligations}, and \autoref{thm:abel-uniform-theorem-real-seal-handoff} seed the finite Real-and-Completion Abel theorem route over AbelUniformConvergence, AbelLimitTheorem, AbelMeanBoundary, UniformLimit, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host power-series semantics, selected sums, quotient streams, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, radius-window admission, Abel uniform convergence boundary admission, Abel endpoint admission, Abel mean-boundary compatibility, uniform-tail exactness, readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.AbelUniformTheoremUp.}
  \constructivestory{An $\AbelUniformTheoremUp$ packet is generated from radius-window rows, an AbelUniformConvergence boundary row, an AbelLimitTheorem endpoint row, an AbelMeanBoundary compatibility row, a UniformLimit tail ledger, RegSeqRat readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
