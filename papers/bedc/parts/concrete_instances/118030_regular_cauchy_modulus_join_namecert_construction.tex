\chapter{A Concrete Naming Certificate for $\RegularCauchyModulusJoinUp$}
\label{ch:concrete-instances-regular-cauchy-modulus-join-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyModulusJoinUp$ packet records the finite shared-tail modulus join used when two regular Cauchy rational names must be read at one downstream tolerance. It sits beside the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window schedule of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the meet-side comparison packet of \autoref{ch:concrete-instances-cauchymodulusmeet-namecert}. The packet names the selected common threshold as displayed BEDC data; it does not import host maxima, quotient Cauchy classes, selected limits, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy modulus join carrier]
\label{def:regular-cauchy-modulus-join-carrier}
A $\RegularCauchyModulusJoinUp$ carrier is a finite $\BHist$ packet
$$
J=(M_0,M_1,T,W_0,W_1,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M_0$ and $M_1$ are the two modulus-input rows whose tail demands must be served by one shared threshold.
\item $T$ is the selected join-threshold row, recording a displayed bound large enough for both modulus inputs at the requested dyadic tolerance.
\item $W_0$ and $W_1$ are the $\StreamNameUp$ window rows opened only after $T$ is displayed.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger carrying the requested radius, the two inherited tolerance budgets, and the slack accounting for the shared threshold.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the two windows and the dyadic ledger.
\item $E$ is the terminal $\RealUp$ seal reached only after the modulus inputs, join threshold, windows, tolerance ledger, and readback row have been replayed.
\item $H$ records componentwise $\hsame$ transport for $M_0,M_1,T,W_0,W_1,Q,R,E$ and the local naming row.
\item $C$ records displayed $\Cont$ replay from a downstream tolerance request through the two modulus inputs, selected threshold, windows, ledger, readback, seal, provenance, and local name.
\item $P$ records $\Pkg$ provenance over $\RegularCauchyModulusJoinUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyModulusMeetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyModulusJoinUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M_0,M_1,T,W_0,W_1,Q,R,E,H,C,P,N$. It has no coordinate for an ambient maximum operation, quotient-stream equality, a selected limit, trichotomy, countable choice, or real completeness outside the displayed packet.
\end{definition}

\begin{theorem}[Regular Cauchy modulus join NameCert obligations]
\label{thm:regular-cauchy-modulus-join-namecert-obligations}
For every accepted $\RegularCauchyModulusJoinUp$ carrier
$$
J=(M_0,M_1,T,W_0,W_1,Q,R,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyModulusJoinUp}$ surface consists exactly of carrier admission for the two modulus-input rows, the selected join threshold, the paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ tolerance ledger, the $\RegSeqRatUp$ readback, the $\RealUp$ seal, componentwise transport, displayed replay, provenance, and local naming row. Every obligation is read through those rows and no obligation is stored in host maxima, quotient streams, selected limits, trichotomy, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-modulus-join-carrier}. The tuple displays the two modulus inputs, the selected threshold, the two window rows, the dyadic ledger, the regular rational readback, the terminal real seal, and the structural rows.

The tail side is exhausted by $M_0,M_1,T,W_0,W_1,Q$. The threshold row $T$ is the only common bound available to both modulus inputs, and the window rows are opened only through the displayed tolerance ledger.

The readback side is exhausted by $R$ and $E$. The regular rational row receives the paired windows and the ledger, while the real seal appears only after that readback has been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no additional coordinate, the excluded host maximum, quotient, selector, choice, trichotomy, and ambient-completeness data cannot become NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy modulus join shared-tail handoff]
\label{thm:regular-cauchy-modulus-join-shared-tail-handoff}
Every $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, or $\RealUp$ consumer of an accepted $\RegularCauchyModulusJoinUp$ carrier reads the finite route
$$
(M_0,M_1) \longmapsto T \longmapsto (W_0,W_1) \longmapsto Q \longmapsto R \longmapsto E .
$$
The route exposes a single shared threshold before either stream window or regular rational readback is consumed. It exports no host maximum operation, quotient Cauchy class, selected limit, trichotomy, countable-choice selector, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-modulus-join-namecert-obligations}. It fixes the public handoff surface at the rows $M_0,M_1,T,W_0,W_1,Q,R,E,H,C,P,N$.

Read the modulus side first. The two input rows state the tail demands, and the threshold row $T$ is the displayed common bound consumed by both of them.

Next read the window and real-name side. The rows $W_0,W_1$ open the finite observations under that threshold, $Q$ records the tolerance ledger, $R$ supplies the regular rational readback, and $E$ seals the route for real-facing consumers.

The structural rows keep the handoff confined to the displayed packet. Therefore a consumer that reaches a stream, rational readback, or real seal without the stated route is not reading the accepted $\RegularCauchyModulusJoinUp$ carrier.
\end{proof}

\begin{theorem}[Regular Cauchy modulus join real seal route]
\label{thm:regular-cauchy-modulus-join-real-seal-route}
The $\RealUp$ seal of an accepted $\RegularCauchyModulusJoinUp$ packet is downstream of the two modulus inputs, the selected join threshold, the paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ tolerance ledger, and the $\RegSeqRatUp$ readback. Hence later L10 regular-Cauchy comparison routes may cite this packet as the concrete shared-tail join certificate, but they receive no additional real-completeness witness from it.
\end{theorem}
\begin{proof}
Use the shared-tail handoff of \autoref{thm:regular-cauchy-modulus-join-shared-tail-handoff}. The displayed route reaches the real seal only after the threshold, windows, ledger, and readback rows have been consumed in order.

The meet-side sibling \autoref{ch:concrete-instances-cauchymodulusmeet-namecert} supplies the comparison-facing common-modulus row, while this packet supplies the join-facing selected threshold used by downstream L10 regular Cauchy routes. Both are read as finite NameCert rows, not as ambient host operations.

Thus the real seal is a terminal boundary for the displayed shared-tail certificate. It cannot be used as a hidden proof of real completeness, selected limits, quotient equality, or trichotomy.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyModulusJoinUp$ consumer that reaches a $\RealUp$ seal without first reading both modulus inputs, the selected join threshold, paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ tolerance ledger, the $\RegSeqRatUp$ readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row refutes the packet surface.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real, cauchymodulusmeet: no listed sibling is bijective to $\RegularCauchyModulusJoinUp$ because this packet binds two modulus inputs, one selected shared threshold, paired finite windows, a tolerance ledger, regular rational readback, real seal, transport, replay, provenance, and local naming as one L10 shared-tail route.}

\closureat{RegularCauchyModulusJoinUp}{seedStr}

\begin{closurestatus}{\RegularCauchyModulusJoinUp}
  \constructivestory{A $\RegularCauchyModulusJoinUp$ packet is generated from two modulus-input rows, one selected join-threshold row, paired StreamName windows, a DyadicRatCore tolerance ledger, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-modulus-join-carrier}, \autoref{thm:regular-cauchy-modulus-join-namecert-obligations}, \autoref{thm:regular-cauchy-modulus-join-shared-tail-handoff}, and \autoref{thm:regular-cauchy-modulus-join-real-seal-route} seed the L10 shared-tail modulus join route over StreamNameUp, DyadicRatCoreUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host maxima, quotient Cauchy classes, selected limits, trichotomy, countable-choice selectors, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the displayed carrier and prove two-input admission, selected-threshold exposure, paired window scheduling, tolerance-ledger readback, RegSeqRat handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyModulusJoinUp.}
\end{closurestatus}
