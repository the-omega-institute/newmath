\chapter{A Concrete Naming Certificate for $\CompactUniformContinuityRealizerUp$}
\label{ch:concrete-instances-compact-uniform-continuity-realizer-namecert}
\origin{ai}

The $\CompactUniformContinuityRealizerUp$ packet realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} as finite NameCert data. It sits over the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map graph surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}. The packet names a finite compact-net modulus realizer: it does not assert an open-cover theorem, a choice-selected modulus, quotient function equality, selected limits, or ambient completeness.

\begin{definition}[Compact uniform-continuity realizer carrier]
\label{def:compact-uniform-continuity-realizer-carrier}
A $\CompactUniformContinuityRealizerUp$ carrier is a finite $\BHist$ packet
$$
U=(K,M,X,W,R,E,G,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row naming the compact finite-net surface.
\item $M$ is the $\ContinuousMapUp$ graph and pointwise-modulus row consumed by the realizer.
\item $X$ is the $\MetricSpaceUp$ distance and triangle ledger shared by the compact source and continuous map.
\item $W$ is the $\StreamNameUp$ finite-window row used to inspect compact-net centers and nearby inputs.
\item $R$ is the finite radius-fold row that extracts a tolerance-only modulus from the compact net and pointwise modulus rows.
\item $E$ is the terminal $\RealUp$ seal row reached after the finite modulus has been displayed.
\item $G$ is the uniform-modulus handoff row consumed by downstream $\UniformModulusUp$ and compact-uniform-continuity readers.
\item $L$ is the local metric-triangle replay row showing that the selected radius works for all inspected source pairs.
\item $H$ records componentwise $\hsame$ transport for $K,M,X,W,R,E,G,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay for the compact-net read, continuous-map read, metric-triangle read, finite-window inspection, radius fold, Real seal, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformContinuityRealizerUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\MetricSpaceUp$, $\RealUp$, $\StreamNameUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformContinuityRealizerUp}$ row.
\end{enumerate}
The carrier has no coordinate for an open-cover compactness proof, an external modulus selector, quotient equality of maps, hidden points of the compact space, selected limits, countable choice, host equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform-continuity realizer NameCert obligations]
\label{thm:compact-uniform-continuity-realizer-namecert-obligations}
For the carrier of \autoref{def:compact-uniform-continuity-realizer-carrier}, the local $\NameCert_{\CompactUniformContinuityRealizerUp}$ surface consists exactly of carrier admission for $U=(K,M,X,W,R,E,G,L,H,C,P,N)$, compact-metric exposure through $K$, continuous-map exposure through $M$, metric-ledger exposure through $X$, finite-window exposure through $W$, finite radius-fold exposure through $R$, Real sealing through $E$, uniform-modulus handoff through $G$, metric-triangle replay through $L$, componentwise transport through $H$, continuation replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier rows. The compact source $K$, continuous-map row $M$, and metric ledger $X$ are the only analytic source rows. The finite-window row $W$ records which compact-net centers and source inputs are inspected before the realizer can fold local radii.

Read the finite-modulus rows next. The radius fold $R$ is built from the displayed compact net and pointwise modulus rows, the metric-triangle replay $L$ verifies the resulting tolerance-only radius, the uniform-modulus row $G$ exports that radius to consumers, and the Real seal $E$ is downstream of this finite route.

Finally use $H,C,P,N$. These rows keep the same compact-net realizer stable under componentwise transport, displayed replay, provenance, and local naming. Since the carrier has no coordinate for an open-cover theorem, external modulus selector, quotient map equality, hidden compact points, selected limits, countable choice, host equality, or ambient completeness, the listed rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[Compact uniform-continuity realizer finite-net route]
\label{thm:compact-uniform-continuity-realizer-finite-net-route}
Every accepted $\CompactUniformContinuityRealizerUp$ handoff to a compact-uniform-continuity consumer factors through
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \MetricSpaceUp,\ \StreamNameUp,\ \RealUp
$$
as the displayed rows $K,M,X,W,R,E,G,L,H,C,P,N$ before the uniform-modulus row is public. The route supplies a finite compact-net modulus realizer, not open-cover compactness, not an external modulus choice, not quotient function equality, and not ambient completeness.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:compact-uniform-continuity-realizer-namecert-obligations}. They force any consumer to read the compact metric source, continuous-map row, metric ledger, finite-window row, radius fold, Real seal, uniform-modulus handoff, metric-triangle replay, transport, continuation, provenance, and local naming as one packet.

The finite-net route is determined by $K,W,R,L$. The compact source provides finite centers, the stream row records the inspected windows, the radius fold extracts the tolerance-only candidate, and the metric-triangle replay verifies it for the displayed source pair reads.

The continuous-map row $M$ supplies only pointwise modulus data, and the public uniform row $G$ appears only after the finite fold and triangle replay have been read. The Real seal $E$ and structural rows $H,C,P,N$ preserve the same route. No row remains for open-cover compactness, choice-selected moduli, quotient function equality, selected limits, hidden points, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted CompactUniformContinuityRealizerUp packet must expose compact-metric finite-net data, a continuous-map pointwise-modulus row, a metric ledger, finite StreamName windows, a radius-fold row, Real sealing, uniform-modulus handoff, triangle replay, transport, continuation, provenance, and local naming before a uniform-continuity consumer may read it.}

\independenceWitness{compactmetric, continuousmap, metric, real, streamname: $\CompactUniformContinuityRealizerUp$ is not bijective to any listed sibling because it jointly carries compact finite-net data, continuous-map local moduli, metric-triangle replay, finite-window inspection, radius-fold extraction, uniform-modulus export, Real sealing, transport, replay, provenance, and local naming.}

\closureat{CompactUniformContinuityRealizerUp}{seedStr}

\begin{closurestatus}{\CompactUniformContinuityRealizerUp}
  \constructivestory{A $\CompactUniformContinuityRealizerUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise-modulus rows, MetricSpace distance and triangle ledgers, StreamName finite-window rows, a finite radius-fold row, a RealUp seal, a uniform-modulus handoff row, metric-triangle replay, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-continuity-realizer-carrier}, \autoref{thm:compact-uniform-continuity-realizer-namecert-obligations}, and \autoref{thm:compact-uniform-continuity-realizer-finite-net-route} seed the finite compact-net realizer route from compact metric and continuous-map data to a public uniform-modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, external modulus selection, quotient map equality, hidden compact points, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net exposure, continuous-map exposure, metric-ledger exposure, finite-window exactness, radius-fold exactness, Real seal nonescape, uniform-modulus handoff, triangle replay, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformContinuityRealizerUp.}
\end{closurestatus}
