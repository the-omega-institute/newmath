\chapter{RealNameBallFilter NameCert Construction}
\label{ch:concrete-instances-real-name-ball-filter-namecert}
\origin{human}

Following Bishop--Bridges constructive real analysis and Kelley filter terminology, the $\RealNameBallFilterUp$ packet records the finite ball-filter reads by which a displayed $\RealUp$ name is inspected through dyadic neighbourhoods. It sits between the dyadic centre-radius surface of \autoref{ch:concrete-instances-dyadicball-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the principal Cauchy filter route of \autoref{ch:concrete-instances-cauchy-principal-filter-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet supplies a finite neighbourhood interface for Real and Completion consumers; it exports no quotient real equality, selected nearest point, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Real name ball-filter carrier]
\label{def:real-name-ball-filter-carrier}
A $\RealNameBallFilterUp$ carrier is a finite $\BHist$ packet
$$
B=(E,S,R,D,Q,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\RealUp$ name seal whose finite neighbourhood reads are being exposed.
\item $S$ is the $\StreamNameUp$ finite-window row naming the requested prefix and precision window of the real name.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed stream window.
\item $D$ is the $\DyadicBallUp$ centre-radius row that records the displayed dyadic neighbourhood and its radius budget.
\item $Q$ is the dyadic tolerance and containment ledger comparing the readback row $R$ with the ball row $D$ at the requested precision.
\item $F$ is the $\CauchyPrincipalFilterUp$ principal-neighbourhood row generated from the same displayed real-name window and dyadic ball.
\item $H$ records componentwise $\hsame$ transport for the seal, window, readback, ball, tolerance, principal-filter, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite ball request through $S,R,D,Q,F$ before the terminal seal $E$ is consumed.
\item $P$ is the $\Pkg$ provenance over $\RealNameBallFilterUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicBallUp$, $\CauchyPrincipalFilterUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealNameBallFilterUp}$ row naming exactly this finite ball-filter packet.
\end{enumerate}
The classifier compares accepted packets only through $E,S,R,D,Q,F,H,C,P,N$. It has no coordinate for quotient real equality, selected nearest point, countable choice, open-cover compactness, hidden infinite filter bases, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[RealNameBallFilter NameCert obligations]
\label{thm:real-name-ball-filter-namecert-obligations}
For every accepted $\RealNameBallFilterUp$ carrier $B=(E,S,R,D,Q,F,H,C,P,N)$, the local $\NameCert_{\RealNameBallFilterUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed real seal, stream window, regular rational readback, dyadic ball, tolerance ledger, principal-filter row, transport, replay, provenance, and local naming row;
\item window locality, requiring every ball request to inspect the real name only through the finite $\StreamNameUp$ row $S$;
\item readback exactness, requiring the regular rational row $R$ to be the only readback surface generated from $S$;
\item ball containment exactness, requiring the dyadic centre-radius row $D$ and tolerance ledger $Q$ to bind the requested neighbourhood before the principal-filter row $F$ is exposed;
\item principal-filter compatibility, requiring $F$ to use the same displayed ball-filter data rather than a hidden basis or selected nearest point;
\item transport, replay, provenance, and name exhaustion through $H,C,P,N$.
\end{enumerate}
No local NameCert obligation is exported for quotient real equality, selected nearest points, countable choice, open-cover compactness, hidden infinite filter bases, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-name-ball-filter-carrier}. The visible analytic rows are exactly the real seal $E$, the finite stream window $S$, the regular rational readback $R$, the dyadic ball $D$, the tolerance ledger $Q$, and the principal-filter row $F$.

Window locality and readback exactness follow from the ordered route in the carrier. A finite neighbourhood request first opens $S$ and then reads the same displayed entries through $R$; there is no second stream, hidden representative, or quotient-real equality coordinate.

Ball containment is then checked by $D$ and $Q$. These rows record the centre, radius, and tolerance comparison before $F$ may package the displayed neighbourhood as a principal filter row.

Finally, $H$ and $C$ preserve the same route under componentwise transport and replay, while $P$ and $N$ source and name only the displayed packet. Since the excluded objects are absent from the carrier inventory, they cannot appear as additional NameCert obligations.
\end{proof}

\begin{theorem}[RealNameBallFilter Real seal nonescape]
\label{thm:real-name-ball-filter-real-seal-nonescape}
Every Real or Completion-facing consumer of an accepted $\RealNameBallFilterUp$ carrier reaches the terminal $\RealUp$ seal only through the finite route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicBallUp \longmapsto \CauchyPrincipalFilterUp \longmapsto \RealUp .
$$
Consequently the exported read is a finite ball-filter neighbourhood of the displayed real name, not quotient real equality, not a selected nearest point, not countable choice, not open-cover compactness, and not ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a consumer read of the seal $E$. By \autoref{def:real-name-ball-filter-carrier}, $E$ is packaged with the displayed stream window $S$, regular rational readback $R$, dyadic ball row $D$, tolerance ledger $Q$, and principal-filter row $F$.

Apply the obligation surface of \autoref{thm:real-name-ball-filter-namecert-obligations}. The request must first inspect $S$, then read $R$, then bind the requested ball through $D$ and $Q$ before $F$ can be exposed.

Only after that route is replayed may the Real-facing consumer use the seal row. The structural rows $H,C,P,N$ transport, continue, source, and name the same finite packet, so the exported object remains a displayed ball-filter read around the real name.

The forbidden principles are not coordinates of the carrier. Hence no path through the local certificate can produce quotient real equality, a selected nearest point, countable choice, open-cover compactness, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RealNameBallFilterUp$ read can be audited by checking that the consumer first displays a $\StreamNameUp$ finite window, then a $\RegSeqRatUp$ readback, then a $\DyadicBallUp$ neighbourhood and tolerance ledger, then the $\CauchyPrincipalFilterUp$ row, before the $\RealUp$ seal is consumed.}

\independenceWitness{dyadicball, streamname, regseqrat, real, cauchy-principal-filter: $\RealNameBallFilterUp$ is not bijective to any listed sibling because it jointly carries the real-name seal, finite stream window, regular rational readback, dyadic ball, tolerance ledger, principal-filter handoff, transport, replay, provenance, and local naming rows.}

\closureat{RealNameBallFilterUp}{seedStr}

\begin{closurestatus}{\RealNameBallFilterUp}
  \constructivestory{A $\RealNameBallFilterUp$ packet is generated from a RealUp seal, finite StreamName window, RegSeqRat readback, DyadicBall neighbourhood, tolerance ledger, CauchyPrincipalFilter row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-name-ball-filter-carrier}, \autoref{thm:real-name-ball-filter-namecert-obligations}, and \autoref{thm:real-name-ball-filter-real-seal-nonescape} seed the finite ball-filter read route around displayed RealUp names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected nearest points, countable choice, open-cover compactness, hidden infinite filter bases, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite window locality, regular rational readback, dyadic ball containment, principal-filter compatibility, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RealNameBallFilterUp.}
\end{closurestatus}
