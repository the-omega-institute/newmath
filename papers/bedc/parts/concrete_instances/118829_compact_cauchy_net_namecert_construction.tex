\chapter{A Concrete Naming Certificate for $\CompactCauchyNetUp$}
\label{ch:concrete-instances-compact-cauchy-net-namecert}
\origin{human}

Following Bishop--Bridges compact metric arguments, the $\CompactCauchyNetUp$ packet records the finite-net and Cauchy-tail route used to turn compact-source inspection into a regular rational and Real-facing handoff. It sits over compact metric finite-net data, the Cauchy-net source surface, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal. The packet names the finite compact-net Cauchy-tail route; it is not open-cover compactness, not a selected limit theorem, and not an ambient completeness principle.

\begin{definition}[Compact Cauchy-net carrier]
\label{def:compact-cauchy-net-carrier}
A $\CompactCauchyNetUp$ carrier is a finite $\BHist$ packet
$$
K=(M,F,T,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the compact metric source row whose finite-net coverage is inspected by the consumer.
\item $F$ is the finite-net row, recording the selected finite cover cells and mesh witnesses used by the compact argument.
\item $T$ is the Cauchy-tail row, recording the finite tail-window evidence over the net.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare mesh size, tail radius, and readback precision.
\item $S$ is the $\StreamNameUp$ finite-window schedule for the inspected net and tail observations.
\item $R$ is the $\RegSeqRatUp$ readback row produced from the same finite-net and tail-window surface.
\item $E$ is the terminal $\RealUp$ seal row reached only after $M,F,T,D,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact source, finite net, Cauchy tail, dyadic tolerance, stream window, readback, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from compact-source inspection through finite-net coverage, tail-window evidence, tolerance rows, readback, and Real seal.
\item $P$ is the $\Pkg$ provenance over $\CompactCauchyNetUp$, $\CompactMetricUp$, $\CauchyNetUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactCauchyNetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed finite-net, tail-window, readback, seal, and structural rows. It has no coordinate for open-cover compactness, selected subnet, selected limit, quotient net equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact Cauchy-net NameCert obligations]
\label{thm:compact-cauchy-net-namecert-obligations}
For every accepted $\CompactCauchyNetUp$ carrier $K=(M,F,T,D,S,R,E,H,C,P,N)$, the local $\NameCert_{\CompactCauchyNetUp}$ surface consists exactly of compact-source carrier exposure, finite-net coverage exposure, Cauchy-tail evidence, dyadic tolerance exposure, finite $\StreamNameUp$ scheduling, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-cauchy-net-carrier}. The compact source, finite-net row, Cauchy-tail row, dyadic tolerance row, stream window, regular rational readback, and Real seal are the only analytic rows.

The finite-net and Cauchy-tail rows are read before any regular rational or Real-facing handoff. The dyadic tolerance row binds the mesh and tail comparisons, while the stream window records exactly which finite observations have been inspected.

Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve that same finite route. Since the carrier has no row for open-cover compactness, selected subnets, selected limits, quotient net equality, countable choice, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact Cauchy-net finite-tail handoff]
\label{thm:compact-cauchy-net-finite-tail-handoff}
Every compact uniform-continuity or completion-facing consumer of an accepted $\CompactCauchyNetUp$ packet must read the finite-net row $F$, the Cauchy-tail row $T$, the $\DyadicRatCoreUp$ tolerance row $D$, the $\StreamNameUp$ window $S$, and the $\RegSeqRatUp$ readback $R$ before the $\RealUp$ seal $E$ is available. Thus the consumer route factors through finite-net coverage, Cauchy-tail evidence, regular rational readback, dyadic tolerance, and Real sealing as NameCert data.
\end{theorem}
\begin{proof}
Start with compact-source exposure. The row $M$ supplies the compact metric source only through the finite-net row $F$, so a consumer cannot replace the packet by an open-cover compactness theorem.

Read the Cauchy-tail and tolerance rows next. The tail evidence $T$ and tolerance ledger $D$ determine the finite comparison surface, and the stream window $S$ records the observations that feed the readback row $R$.

The terminal seal $E$ is downstream of the same route. Componentwise transport, continuation replay, provenance, and naming preserve the displayed rows, so no selected subnet, selected limit, quotient net equality, choice principle, or ambient completeness coordinate is exported.
\end{proof}

\closureat{CompactCauchyNetUp}{seedStr}

\begin{closurestatus}{\CompactCauchyNetUp}
  \constructivestory{A $\CompactCauchyNetUp$ packet is generated from a compact metric source, finite-net coverage rows, Cauchy-tail evidence, DyadicRatCore tolerance data, StreamName finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-cauchy-net-carrier}, \autoref{thm:compact-cauchy-net-namecert-obligations}, and \autoref{thm:compact-cauchy-net-finite-tail-handoff} seed the finite compact-net Cauchy-tail route used by compact uniform-continuity and completion-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, open-cover compactness, selected subnets, selected limits, quotient net equality, countable choice, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, finite-net coverage, Cauchy-tail evidence, dyadic tolerance exactness, readback routing, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactCauchyNetUp.}
\end{closurestatus}
