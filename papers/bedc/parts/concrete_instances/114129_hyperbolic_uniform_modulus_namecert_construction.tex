\chapter{A Concrete Naming Certificate for $\HyperbolicUniformModulusUp$}
\label{ch:concrete-instances-hyperbolic-uniform-modulus-namecert}
\origin{ai}

The $\HyperbolicUniformModulusUp$ packet realizes the uniform-modulus branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite hyperbolic metric, cross-ratio, visual-boundary, and tolerance-only modulus rows. It sits after the Poincare disk metric packet of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the hyperbolic metric surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary comparison packet of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, the visual boundary carrier of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, and the compact uniform selector of \autoref{ch:concrete-instances-compact-uniform-tail-selector-namecert}. The carrier is a BEDC-internal synthesis: it binds hyperbolic boundary geometry to a tolerance-only modulus route without turning visual expansion into a host compactness theorem or a completed boundary object.

\begin{definition}[Hyperbolic uniform modulus carrier]
\label{def:hyperbolic-uniform-modulus-carrier}
A $\HyperbolicUniformModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(M,X,B,V,Q,T,D,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicMetricUp$ metric-read row over the displayed disk or boundary-shadow observations.
\item $X$ is the $\HyperbolicCrossRatioUp$ comparison row, recording the finite four-point or boundary-ratio data used by the modulus read.
\item $B$ is the $\HyperbolicVisualBoundaryUp$ boundary row, exposing the visual-boundary source of the observer-expansion tolerance request.
\item $V$ is the $\PoincareDiskMetricUp$ disk-metric provenance row tying the metric and boundary observations to the disk model used by the vision.
\item $Q$ is the $\UniformModulusUp$ tolerance-only modulus row delivered to observer-expansion consumers.
\item $T$ is the compact-uniform handoff row, read through $\CompactUniformModulusUp$ or the finite selector route of \autoref{ch:concrete-instances-compact-uniform-tail-selector-namecert}.
\item $D$ is the dyadic or rational tolerance ledger used to compare cross-ratio, visual-boundary, and modulus requests.
\item $R$ is the refusal row excluding host boundary compactness, completed hyperbolic plane geometry, quotient ideal points, selected visual metrics, and physical observer expansion.
\item $H$ records componentwise $\hsame$ transport for metric, cross-ratio, visual-boundary, disk-metric, uniform-modulus, compact-uniform, tolerance, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from hyperbolic boundary observations through $M,X,B,V,T,D$ to the tolerance-only row $Q$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicUniformModulusUp$, $\HyperbolicMetricUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicVisualBoundaryUp$, $\PoincareDiskMetricUp$, $\UniformModulusUp$, $\CompactUniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicUniformModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host compactness theorem, quotient boundary equality, selected visual metric, completed hyperbolic geometry, physical expansion law, or observer substance.
\end{definition}

\begin{theorem}[Hyperbolic uniform modulus NameCert obligations]
\label{thm:hyperbolic-uniform-modulus-namecert-obligations}
For every accepted $\HyperbolicUniformModulusUp$ carrier
$$
U=(M,X,B,V,Q,T,D,R,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicUniformModulusUp}$ surface consists exactly of hyperbolic metric admission, hyperbolic cross-ratio admission, visual-boundary admission, Poincare disk metric provenance, tolerance-only uniform modulus exposure, compact-uniform handoff, tolerance-ledger compatibility, refusal exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier. The hyperbolic geometry rows are $M,X,B,V$, and the tolerance rows are $Q,T,D$.

The metric and cross-ratio rows are read before the modulus row. This keeps the boundary comparison visible when the consumer receives a tolerance-only threshold.

The compact-uniform handoff is finite. The row $T$ records the selector or compact uniform modulus route, while $D$ records the tolerance ledger used to compare the geometric and modulus requests.

The refusal and structural rows preserve the boundary. Since no coordinate supplies host compactness, quotient boundary equality, selected visual metrics, completed hyperbolic geometry, physical expansion, or observer substance, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic uniform modulus cross-ratio route]
\label{thm:hyperbolic-uniform-modulus-cross-ratio-route}
Every accepted $\HyperbolicUniformModulusUp$ read delivers its tolerance-only $\UniformModulusUp$ row only after the displayed route
$$
\HyperbolicMetricUp,\ \HyperbolicCrossRatioUp,\ \HyperbolicVisualBoundaryUp,\ \PoincareDiskMetricUp,\ \CompactUniformModulusUp
\longmapsto
\UniformModulusUp
$$
has been opened. The observer-expansion consumer receives metric, cross-ratio, visual-boundary, disk-metric, compact-uniform, and tolerance-ledger rows before any modulus threshold can be used.
\end{theorem}
\begin{proof}
Begin with $U=(M,X,B,V,Q,T,D,R,H,C,P,N)$. The row $Q$ is the only tolerance-only uniform modulus coordinate.

The route to $Q$ is ordered by the carrier inventory. The consumer first reads the metric row $M$, cross-ratio row $X$, visual-boundary row $B$, disk-metric provenance $V$, compact-uniform handoff $T$, and tolerance ledger $D$.

Only then can $Q$ be replayed by $C$ and transported by $H$. The provenance row $P$ and local naming row $N$ record the same finite route.

Thus the modulus threshold is never detached from its hyperbolic boundary source, and no host compactness, selected visual metric, quotient boundary, completed geometry, or physical expansion law is exported.
\end{proof}

\begin{theorem}[Hyperbolic uniform modulus boundary nonescape]
\label{thm:hyperbolic-uniform-modulus-boundary-nonescape}
For an accepted $\HyperbolicUniformModulusUp$ carrier $U=(M,X,B,V,Q,T,D,R,H,C,P,N)$, the visual-boundary row $B$ and tolerance-only row $Q$ export no completed boundary compactification, quotient ideal point, selected visual metric, host compactness theorem, or physical observer-expansion datum. Every boundary-to-modulus read remains inside the displayed metric, cross-ratio, disk-metric, compact-uniform, tolerance, refusal, transport, replay, provenance, and naming rows.
\end{theorem}
\begin{proof}
Inspect the carrier surface. The visual-boundary row $B$ is accompanied by metric row $M$, cross-ratio row $X$, disk-metric provenance $V$, compact-uniform handoff $T$, tolerance ledger $D$, and refusal row $R$.

Apply the NameCert obligations of \autoref{thm:hyperbolic-uniform-modulus-namecert-obligations}. They require the tolerance-only row $Q$ to be exposed only through the same displayed rows.

The refusal row rules out stronger host readings, and $H,C,P,N$ preserve the route under transport, replay, provenance, and local naming.

Consequently a consumer cannot extract completed boundary compactification, quotient ideal points, selected visual metrics, host compactness, or physical expansion from the packet. Such data are absent from the carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicUniformModulusUp$ read must expose $\HyperbolicMetricUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicVisualBoundaryUp$, and $\UniformModulusUp$ rows before any observer-expansion consumer receives a tolerance-only modulus.}

\independenceWitness{hyperbolic\_metric, hyperbolic\_cross\_ratio, hyperbolic\_visual\_boundary, compact\_uniform\_modulus, poincare\_disk\_metric: no listed sibling is bijective to $\HyperbolicUniformModulusUp$ because this packet jointly binds metric reads, cross-ratio comparisons, visual-boundary rows, disk-metric provenance, compact-uniform handoff, a tolerance-only modulus, refusal, transport, replay, provenance, and local naming.}

\closureat{HyperbolicUniformModulusUp}{seedStr}

\begin{closurestatus}{\HyperbolicUniformModulusUp}
  \constructivestory{A $\HyperbolicUniformModulusUp$ packet is generated from HyperbolicMetric reads, HyperbolicCrossRatio comparisons, HyperbolicVisualBoundary rows, PoincareDiskMetric provenance, CompactUniformModulus handoff, UniformModulus tolerance rows, refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-uniform-modulus-carrier}, \autoref{thm:hyperbolic-uniform-modulus-namecert-obligations}, \autoref{thm:hyperbolic-uniform-modulus-cross-ratio-route}, and \autoref{thm:hyperbolic-uniform-modulus-boundary-nonescape} seed the finite route from hyperbolic metric and boundary comparison rows to a tolerance-only uniform modulus.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host boundary compactness, quotient ideal-boundary equality, selected visual metrics, completed hyperbolic geometry, physical expansion, observer substance, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, metric-read exposure, cross-ratio route, visual-boundary nonescape, compact-uniform handoff, tolerance-only modulus exactness, refusal, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicUniformModulusUp.}
\end{closurestatus}
