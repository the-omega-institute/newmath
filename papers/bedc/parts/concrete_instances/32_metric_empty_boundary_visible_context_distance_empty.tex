\begin{theorem}[Metric distance witness empty-boundary visible-context distance empty]
\label{thm:metric-distance-witness-empty-boundary-visible-context-distance-empty}
For histories $p,q,d$, a visible-context distance witness whose central left and
right endpoints are both empty identifies the central distance with the empty
history and gives zero central distance depth:
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\hsame(d,\emp)\wedge\mathsf{MetricDistanceDepth}(d)=0.
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:metric-distance-witness-visible-context-iff} to peel away
the visible prefix and suffix. The remaining central witness is
$\mathsf{MetricDistanceWitness}(\emp,\emp,d)$. The empty-left boundary theorem
identifies $d$ with $\emp$; case analysis on that classifier gives the zero
depth equation.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_distance\_empty}

\begin{theorem}[Metric prefix-independent symmetric zero-depth empty-boundary forward]
\label{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-empty-boundary-forward}
For histories $p,q,p',q',x,y,d_{xy},d_{yx}$, suppose the two symmetric
visible-context distance witnesses
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q),
\qquad
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
$$
hold. If $\mathsf{MetricDistanceDepth}(d_{xy})=0$, then the original visible
context carries the empty-boundary witness
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot\emp)\cdot q).
$$
\end{theorem}
\begin{proof}
Peel both visible-context witnesses to their central witnesses. Symmetric
classification compares $d_{xy}$ and $d_{yx}$. Case analysis on the zero-depth
central distance leaves only $d_{xy}=\emp$; the empty-distance visible-context
readback then identifies the two central endpoints as empty. Reattaching the
original prefix and suffix gives the displayed empty-boundary witness.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_empty\_boundary\_forward}

\begin{theorem}[Metric empty-boundary visible context append unit laws]
\label{thm:metric-distance-witness-empty-boundary-visible-context-append-unit-laws}
For histories $p,q,d,l,r$, a visible-context distance witness whose central
endpoints are both empty makes the central distance act as a left and right
append unit:
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\mathsf{append}(d,r)=r\ \wedge\ \mathsf{append}(l,d)=l.
$$
\end{theorem}
\begin{proof}
The empty-boundary distance theorem identifies $d$ with $\emp$. Case analysis
on that classifier reduces the two displayed equations to the left and right
unit laws for finite-kernel append.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_append\_unit\_laws}

\begin{theorem}[Metric empty-boundary visible context unit splice commute]
\label{thm:metric-distance-witness-empty-boundary-visible-context-unit-splice-commute}
For histories $p,q,d,l,r,out_L,out_R$, an empty-boundary visible-context
distance witness makes left and right continuation through the central distance
commute after one ordinary append splice:
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\ContR(l,d,out_L)\Longrightarrow
\ContR(d,r,out_R)\Longrightarrow
\hsame(out_L\cdot r,l\cdot out_R).
$$
\end{theorem}
\begin{proof}
The empty-boundary distance theorem identifies $d$ with $\emp$. The left
continuation then has right-unit result $out_L\hsame l$, and the right
continuation has left-unit result $out_R\hsame r$. Substituting these two unit
identifications reduces the displayed splice comparison to reflexivity.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_unit\_splice\_commute}

\begin{theorem}[Metric visible-context empty-distance unit splice commute]
\label{thm:metric-distance-witness-visible-context-empty-distance-unit-splice-commute}
For histories $p,q,x,y,l,r,out_L,out_R$, a visible-context empty-distance
witness makes left continuation through $x$ and right continuation through $y$
commute after one ordinary append splice:
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q)
\Longrightarrow
\ContR(l,x,out_L)\Longrightarrow
\ContR(y,r,out_R)\Longrightarrow
\hsame(out_L\cdot r,l\cdot out_R).
$$
\end{theorem}
\begin{proof}
The visible-context empty-distance exactness theorem identifies both central
endpoints $x$ and $y$ with $\emp$. The left continuation then has right-unit
result $out_L\hsame l$, and the right continuation has left-unit result
$out_R\hsame r$. Substituting these unit identifications reduces the splice
comparison to reflexivity.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_unit\_splice\_commute}

\begin{theorem}[Metric empty-boundary visible context left-unit witness]
\label{thm:metric-distance-witness-empty-boundary-visible-context-left-unit-witness}
For histories $p,q,d,r,out$, an empty-boundary visible-context distance witness
turns any continuation $\ContR(d,r,out)$ into a metric witness from the empty
source:
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\mathsf{UnaryHistory}(r)\Longrightarrow
\ContR(d,r,out)\Longrightarrow
\mathsf{MetricDistanceWitness}(\emp,r,out).
$$
\end{theorem}
\begin{proof}
The empty-boundary distance theorem identifies $d$ with $\emp$. Substituting
that classifier makes the continuation a left-unit continuation from $\emp$ to
$r$. The empty source is unary, the target is unary by hypothesis, and unary
continuation closure supplies the output carrier, so the three fields package
the displayed metric distance witness.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_left\_unit\_witness}

\begin{theorem}[Metric empty-boundary visible context right-unit witness]
\label{thm:metric-distance-witness-empty-boundary-visible-context-right-unit-witness}
For histories $p,q,d,l,out$, an empty-boundary visible-context distance witness
turns any continuation $\ContR(l,d,out)$ into a metric witness into the empty
target:
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\mathsf{UnaryHistory}(l)\Longrightarrow
\ContR(l,d,out)\Longrightarrow
\mathsf{MetricDistanceWitness}(l,\emp,out).
$$
\end{theorem}
\begin{proof}
The empty-boundary distance theorem identifies $d$ with $\emp$. Substituting
that classifier makes the continuation a right-unit continuation into $\emp$.
The source is unary by hypothesis, the empty target is unary, and unary
continuation closure supplies the output carrier, so the three fields package
the displayed metric distance witness.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_right\_unit\_witness}

\begin{theorem}[Metric empty-boundary visible context continuation depth add]
\label{thm:metric-distance-witness-empty-boundary-visible-context-continuation-depth-add}
For histories $p,q,d,l,r,mid,out$, an empty-boundary visible-context distance
witness lets a continuation through $d$ splice into the direct continuation
from $l$ to $r$, so the output depth is the sum of the endpoint depths:
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\mathsf{UnaryHistory}(l)\Longrightarrow\mathsf{UnaryHistory}(r)\Longrightarrow
\ContR(l,d,mid)\Longrightarrow\ContR(mid,r,out)\Longrightarrow
\mathsf{MetricDistanceDepth}(out)=
\mathsf{MetricDistanceDepth}(l)+\mathsf{MetricDistanceDepth}(r).
$$
\end{theorem}
\begin{proof}
The empty-boundary continuation splice removes the central empty distance from
the two-step ledger and gives $\ContR(l,r,out)$. Together with the unary
endpoint hypotheses, unary continuation closure supplies the output carrier,
so these fields form a concrete metric distance witness from $l$ to $r$.
Metric distance depth additivity then gives the displayed equation.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_continuation\_depth\_add}

\begin{theorem}[Metric empty-boundary visible context continuation witness depth package]
\label{thm:metric-distance-witness-empty-boundary-visible-context-continuation-witness-depth}
For histories $p,q,d,l,r,mid,out$, an empty-boundary visible-context distance
witness, unary visible endpoints, and the two-step continuation through $d$
determine both the visible-endpoint metric witness and its depth ledger:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\mathsf{UnaryHistory}(l)\Longrightarrow\mathsf{UnaryHistory}(r)\Longrightarrow\\
\qquad
\ContR(l,d,mid)\Longrightarrow\ContR(mid,r,out)\Longrightarrow\\
\qquad
\mathsf{MetricDistanceWitness}(l,r,out)\wedge
\mathsf{MetricDistanceDepth}(out)=
\mathsf{MetricDistanceDepth}(l)+\mathsf{MetricDistanceDepth}(r).
\end{array}
$$
\end{theorem}
\begin{proof}
The witness conjunct is supplied by
\autoref{thm:metric-distance-witness-empty-boundary-visible-context-witness-splice}
from the empty-boundary witness, the endpoint unary histories, and the two
continuation rows.

The depth conjunct is supplied by
\autoref{thm:metric-distance-witness-empty-boundary-visible-context-continuation-depth-add}
from the same hypotheses. Conjoining the two conclusions gives the displayed
package.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_continuation\_witness\_depth}

\begin{theorem}[Metric empty-boundary visible context canonical splice package]
\label{thm:metric-empty-boundary-visible-context-canonical-splice-package}
For histories $p,q,d,l,r,mid,out,out'$, an empty-boundary visible-context
distance witness, unary endpoint carriers, a two-step continuation through
$d$, and a displayed direct continuation from $l$ to $r$ determine the
canonical splice package:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\mathsf{UnaryHistory}(l)\Longrightarrow\mathsf{UnaryHistory}(r)\Longrightarrow\\
\qquad
\ContR(l,d,mid)\Longrightarrow\ContR(mid,r,out)\Longrightarrow
\ContR(l,r,out')\Longrightarrow\\
\qquad
\mathsf{MetricDistanceWitness}(l,r,out)\land
\hsame(out,out')\land\\
\qquad
\mathsf{MetricDistanceDepth}(out)=
\mathsf{MetricDistanceDepth}(l)+\mathsf{MetricDistanceDepth}(r).
\end{array}
$$
\end{theorem}
\begin{proof}
The witness row is supplied by
Theorem~\ref{thm:metric-distance-witness-empty-boundary-visible-context-witness-splice}
from the empty-boundary visible-context witness, the unary carriers, and the
two displayed continuation rows.

The comparison row is supplied by
Theorem~\ref{thm:metric-empty-boundary-visible-context-continuation-splice-hsame},
which compares the spliced output with the displayed direct continuation
$\ContR(l,r,out')$.

The depth row is supplied by
Theorem~\ref{thm:metric-distance-witness-empty-boundary-visible-context-continuation-depth-add}.
Conjoining these three rows gives the displayed certificate package.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_visible\_context\_canonical\_splice\_package}

\begin{closurestatus}{\MetricUp}
  \constructivestory{}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The empty-boundary visible-context metric distance witness package, where central empty endpoints force empty distance, zero depth, append-unit behavior, and the cited semantic certificate.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.MetricUp.MetricDistanceWitness\_semanticNameCert}
  \bridgestatus{none}
  \notclaimed{General metric-space theory: arbitrary point carriers, real-valued distances, triangle inequality for all points, topology induced by the metric, completeness, compactness, or continuity.}
  \upgradepath{$\publicClosure$ requires a public metric-space carrier, distance readback, symmetry and triangle laws, identity-of-indiscernibles under the classifier, and no-hidden-leakage for distance witnesses.}
\end{closurestatus}
