\chapter{A Concrete Naming Certificate for $\BishopBarInductionBoundaryUp$}
\label{ch:concrete-instances-bishop-bar-induction-boundary-namecert}
\origin{human}

Following Brouwer, Troelstra--van Dalen, and Bishop--Bridges, the $\BishopBarInductionBoundaryUp$ packet records the finite boundary at which bar-induction evidence becomes available to Real and Completion consumers without turning into a choice or compactness principle. It sits beside the barred-spread source of \autoref{ch:concrete-instances-brouwer-bar-induction-namecert}, the finite fan route of \autoref{ch:concrete-instances-fantheorem-namecert}, the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite bar handoff for analytic neighbours; it is not countable choice, a selected infinite branch, quotient stream equality, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop bar induction boundary carrier]
\label{def:bishop-bar-induction-boundary-carrier}
A $\BishopBarInductionBoundaryUp$ carrier is a finite $\BHist$ packet
$$
Y=(B,F,S,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BrouwerBarInductionUp$ barred-spread row, recording the finite bar decisions, monotone closure, and local induction replay visible at the retained prefix boundary.
\item $F$ is the $\FanTheoremUp$ finite fan row, recording the bounded prefix cover and depth ledger consumed before a boundary read is public.
\item $S$ is the $\StreamNameUp$ finite-window row for Real and Completion-facing observations of the retained prefixes.
\item $R$ is the $\RegSeqRatUp$ readback row attached to those finite windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the dyadic scales and comparison cells used to interpret the finite windows and regular rational readback.
\item $E$ is the terminal $\RealUp$ seal row reached only after $B,F,S,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the barred-spread row, finite fan row, StreamName windows, RegSeqRat readback, dyadic tolerances, Real seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a boundary request through $B,F,S,R,D,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopBarInductionBoundaryUp$, $\BrouwerBarInductionUp$, $\FanTheoremUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopBarInductionBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,F,S,R,D,E,H,C,P,N$. It has no coordinate for countable choice, a selected infinite branch, quotient stream equality, open-cover compactness, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop bar induction boundary NameCert obligations]
\label{thm:bishop-bar-induction-boundary-namecert-obligations}
For every accepted $\BishopBarInductionBoundaryUp$ carrier
$$
Y=(B,F,S,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\BishopBarInductionBoundaryUp}$ surface consists exactly of carrier admission, barred-spread ancestry, finite fan-boundary exposure, finite StreamName window admission, RegSeqRat readback, dyadic tolerance exactness, Real seal ordering, componentwise transport, displayed continuation replay, provenance, and local naming for the carrier of \autoref{def:bishop-bar-induction-boundary-carrier}.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its public rows are precisely $B,F,S,R,D,E,H,C,P,N$, so carrier admission and local naming are fixed by \autoref{def:bishop-bar-induction-boundary-carrier}.

The barred-spread ancestry row $B$ supplies the finite bar decisions and induction replay, while the fan row $F$ supplies only bounded prefix data. Thus the boundary is finite before any analytic read can inspect StreamName, RegSeqRat, dyadic, or Real-facing rows.

The readback rows are ordered by the carrier itself: $S$ displays the finite windows, $R$ reads those windows as regular rational data, $D$ supplies the dyadic tolerance ledger, and $E$ is reached only after those rows are present. The transport, replay, provenance, and naming rows preserve this order through $H,C,P,N$.

Since no coordinate stores countable choice, selected branches, quotient stream equality, open-cover compactness, host real equality, or ambient Real completeness, none of those principles can be a local NameCert obligation.
\end{proof}

\begin{theorem}[Bishop bar induction boundary finite-bar handoff]
\label{thm:bishop-bar-induction-boundary-finite-bar-handoff}
Every $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, or Completion-facing consumer of an accepted $\BishopBarInductionBoundaryUp$ packet reads the finite bar route
$$
\BrouwerBarInductionUp,\FanTheoremUp \longmapsto \StreamNameUp,\RegSeqRatUp,\DyadicRatCoreUp \longmapsto \RealUp
$$
before the boundary is public. Consequently the handoff exports finite barred-spread and fan-window data to Real-and-Completion neighbours without exporting countable choice, selected infinite branches, quotient stream equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $Y=(B,F,S,R,D,E,H,C,P,N)$. The only bar source is $B$, and the only finite fan source is $F$, so a boundary consumer must display these rows before it can request analytic data.

Next the consumer reads the finite observation rows. The $\StreamNameUp$ window $S$ gives the inspected prefixes, the $\RegSeqRatUp$ row $R$ reads those prefixes as regular rational data, and the $\DyadicRatCoreUp$ row $D$ records the displayed tolerance comparisons.

The terminal $\RealUp$ row $E$ is downstream of that finite route. Componentwise transport, continuation replay, provenance, and local naming only preserve the same path through $H,C,P,N$.

There is no remaining insertion point for countable choice, a selected infinite branch, quotient stream equality, open-cover compactness, or ambient Real completeness. The handoff is therefore exactly the finite boundary route displayed in the statement.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopBarInductionBoundaryUp$ Real or Completion-facing read displays BrouwerBarInductionUp barred-spread data, FanTheoremUp finite fan rows, StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before the boundary is public.}

\independenceWitness{brouwer-bar-induction, fantheorem, streamname, regseqrat, dyadicratcore, real: $\BishopBarInductionBoundaryUp$ is not bijective to any listed sibling because it jointly carries finite bar ancestry, finite fan-boundary exposure, finite windows, regular rational readback, dyadic tolerance accounting, Real sealing, transport, replay, provenance, and local naming.}

\closureat{BishopBarInductionBoundaryUp}{seedStr}

\begin{closurestatus}{\BishopBarInductionBoundaryUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-bar-induction-boundary-carrier}, \autoref{thm:bishop-bar-induction-boundary-namecert-obligations}, and \autoref{thm:bishop-bar-induction-boundary-finite-bar-handoff} seed the finite bar boundary route from Brouwer bar induction and finite fan rows through StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, terminal Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close countable choice, selected infinite branches, quotient stream equality, open-cover compactness, ambient $\RealUp$ completeness, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, barred-spread ancestry, finite fan-boundary exposure, window admission, readback ordering, dyadic exactness, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopBarInductionBoundaryUp.}
  \constructivestory{A $\BishopBarInductionBoundaryUp$ packet is generated from BrouwerBarInductionUp barred-spread rows, FanTheoremUp finite fan rows, StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
