\chapter{A Concrete Naming Certificate for $\PoincareDiskDynamicsUp$}
\label{ch:concrete-instances-poincare-disk-dynamics-namecert}
\origin{ai}

The $\PoincareDiskDynamicsUp$ packet realizes the disk-dynamics fixed-end reading of \autoref{sec:hyperbolic-disk-dynamics-fixed-end} as finite BEDC data. It consumes the phase-transport route of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, the metric surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, disk coordinates from \autoref{ch:concrete-instances-unitdisk-namecert}, finite evolution rows from \autoref{ch:concrete-instances-dynsystem-namecert}, and boundary-circle consumers from \autoref{ch:concrete-instances-s1-namecert}. Its distinguishing carrier binds disk-map evolution to a preserved far-end boundary row; it is not a completed hyperbolic plane, physical cosmology, global clock, quotient boundary, or internal object named $\mathsf{T}$.

\begin{definition}[Poincare disk dynamics carrier]
\label{def:poincare-disk-dynamics-carrier}
A $\PoincareDiskDynamicsUp$ carrier is a finite $\BHist$ packet
$$
D=(U,\Phi,G,B,F,V,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ coordinate row for the inspected disk points.
\item $\Phi$ is the disk-map row, recording the finite boundary-preserving transport applied to those points.
\item $G$ is the $\HyperbolicMetricUp$ metric-read row used only as displayed four-point or distance-comparison data.
\item $B$ is the $\SOneUp$ boundary-consumer row naming the fixed far-end commitment.
\item $F$ is the $\PoincareDiskPhaseTransportUp$ provenance row linking visible phase movement, disk coordinates, reversible transport, fiber provenance, and refusal data.
\item $V$ is the $\DynSystemUp$ finite-evolution row for replaying the disk-map orbit.
\item $A$ is the audit-refusal row excluding an interior fixed point requirement, host hyperbolic geometry, physical light-speed derivation, global observer time, subject-substance, and an internal $\mathsf{T}$ object.
\item $H$ records componentwise $\hsame$ transport for the disk, map, metric, boundary, phase-provenance, dynamics, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through disk coordinates, disk-map evolution, metric readback, boundary preservation, phase provenance, and refusal.
\item $P$ is the $\Pkg$ provenance over $\PoincareDiskDynamicsUp$, $\PoincareDiskPhaseTransportUp$, $\HyperbolicMetricUp$, $\UnitDiskUp$, $\DynSystemUp$, $\SOneUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PoincareDiskDynamicsUp}$ row.
\end{enumerate}
The carrier compares packets only through these rows. It has no coordinate for a host Poincare disk model, quotient ideal boundary, global physical evolution law, completed metric geometry, or object-level $\mathsf{T}$.
\end{definition}

\begin{theorem}[Poincare disk dynamics NameCert obligations]
\label{thm:poincare-disk-dynamics-namecert-obligations}
For every accepted $\PoincareDiskDynamicsUp$ carrier
$$
D=(U,\Phi,G,B,F,V,A,H,C,P,N),
$$
the local $\NameCert_{\PoincareDiskDynamicsUp}$ surface consists exactly of disk-coordinate admission, disk-map exposure, hyperbolic metric readback, fixed far-end boundary preservation, phase-transport provenance, finite dynamic replay, refusal exactness, componentwise transport, displayed continuation, package provenance, and local naming through the rows $U,\Phi,G,B,F,V,A,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:poincare-disk-dynamics-carrier}. The visible geometric data are the disk coordinates $U$, disk-map row $\Phi$, metric-read row $G$, boundary row $B$, phase-provenance row $F$, and finite-evolution row $V$.

Read the fixed-end condition next. The boundary row $B$ is preserved by the displayed disk-map route, while $F$ records the phase-transport provenance that keeps visible movement distinct from source collapse or an internal $\mathsf{T}$ object.

The dynamic replay is finite. The row $V$ supplies only the inspected orbit cells, and $C$ replays those cells together with the disk, map, metric, boundary, and refusal rows.

Finally attach $H,P,N$. Componentwise transport, provenance, and local naming preserve exactly the displayed rows, so the NameCert surface cannot export host hyperbolic geometry, a quotient boundary, global time, physical constants, or subject-substance.
\end{proof}

\falsifiablePrediction{If a disk-dynamics consumer preserves visible disk coordinates but drops either the boundary row, the phase-transport provenance row, or the refusal row, it is not reading a $\PoincareDiskDynamicsUp$ packet.}

\independenceWitness{poincare-disk-phase-transport, hyperbolic-metric, unitdisk, dynsystem, s1: no listed sibling is bijective to $\PoincareDiskDynamicsUp$ because this packet jointly carries disk coordinates, disk-map evolution, metric readback, fixed far-end boundary preservation, phase provenance, dynamic replay, refusal, transport, provenance, and local naming.}

The Lean follow-up for this packet must provide explicit $\mathsf{Nontrivial}\ \PoincareDiskDynamicsUp$ and $\mathsf{FieldFaithful}\ \PoincareDiskDynamicsUp$ TasteGate instances. Those instances must keep $U,\Phi,G,B,F,V,A,H,C,P,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{PoincareDiskDynamicsUp}{seedStr}

\begin{closurestatus}{\PoincareDiskDynamicsUp}
  \constructivestory{A $\PoincareDiskDynamicsUp$ packet is generated from UnitDisk coordinates, a disk-map row, HyperbolicMetric readback, SOne boundary consumption, PoincareDiskPhaseTransport provenance, DynSystem finite evolution, refusal exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poincare-disk-dynamics-carrier} and \autoref{thm:poincare-disk-dynamics-namecert-obligations} seed the finite disk-map, metric, fixed-boundary, phase-provenance, and dynamic-replay route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed Poincare disk geometry, host hyperbolic metric models, quotient boundary equality, global observer time, physical light-speed derivation, internal $\mathsf{T}$, subject-substance, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, disk-map exposure, metric readback, fixed-boundary preservation, phase-provenance routing, dynamic replay, TasteGate nontriviality, field faithfulness, and local naming for BEDC.Derived.PoincareDiskDynamicsUp.}
\end{closurestatus}
