Every accepted $\CompletelyRegularSpaceUp$ read that is consumed by a uniform-space-facing route first exposes the $\TopologyUp$ open-set classifier rows of \autoref{ch:concrete-instances-topology-namecert} and the $\UniformSpaceUp$ entourage rows of \autoref{ch:concrete-instances-uniformspace-namecert}. Only after those rows are visible may the packet export the completely-regular separation predicate through the finite carrier route $T,R,C,F,H,K,P,N$ of \autoref{def:completely-regular-space-carrier}.

The route is dependency-visible rather than metrization-visible. A consumer may read open-set classification, entourage transport, the displayed real-valued separator, componentwise $\hsame$, $\Cont$ replay, $\Pkg$ provenance, and local naming, but it receives no host family of separating functions, selected uniformity base, ambient normality theorem, metrization, compactness principle, or unrestricted product construction.
