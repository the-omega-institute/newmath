Every completely regular separator route factors through the topology source of \autoref{ch:concrete-instances-topology-namecert}, the point-versus-closed-set regularity source of \autoref{ch:concrete-instances-regular-topological-space-namecert}, the real seal of \autoref{ch:concrete-instances-real-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuous-namecert}, and the Urysohn consumer of \autoref{ch:concrete-instances-urysohn-lemma-namecert}. The top-level $\CompletelyRegularSpaceUp$ hub therefore routes its finite separator rows through those siblings before any Urysohn-style reader can consume the displayed real-valued function.
