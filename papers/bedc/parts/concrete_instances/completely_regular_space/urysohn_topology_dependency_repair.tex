The real-valued separator route for $\CompletelyRegularSpaceUp$ is read through the topology carrier of \autoref{ch:concrete-instances-topology-namecert}, the point-versus-closed-set regularity carrier of \autoref{ch:concrete-instances-regular-topological-space-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the Urysohn separation consumer of \autoref{ch:concrete-instances-urysohn-lemma-namecert}. A consumer therefore receives a displayed topology source, regularity request, real-valued separator, transport, provenance, and local naming route; it receives no selected family of all real-valued maps, ambient normality theorem, host powerset separation, or untracked separator.
