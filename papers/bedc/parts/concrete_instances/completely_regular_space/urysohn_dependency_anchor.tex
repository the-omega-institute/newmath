\begin{theorem}[Completely regular space Urysohn dependency anchor]
\label{thm:completely-regular-space-urysohn-link}
Every $\CompletelyRegularSpaceUp$ separator route consumes the $\TopologyUp$ open-row carrier of \autoref{ch:concrete-instances-topology-namecert} and the Urysohn-style separator rows of \autoref{ch:concrete-instances-urysohn-lemma-namecert} before it exports its regularity surface. The exported route is therefore the finite row sequence of \autoref{def:completely-regular-space-carrier} together with the topology--Urysohn sibling lattice of \autoref{thm:completely-regular-space-topology-urysohn-sibling-lattice}; it is not an ambient normality theorem, an unrestricted family of real-valued maps, a host powerset separator, or a selected complement principle.
\end{theorem}

\begin{proof}
Project the carrier $Q=(T,R,C,F,H,K,P,N)$ of \autoref{def:completely-regular-space-carrier}. The row $T$ supplies the displayed topology source, while $R$ and $C$ bind the point-versus-closed-set request before the separator row $F$ can be read.

Now attach the Urysohn-facing sibling route. The topology--Urysohn sibling lattice forces the separator to remain downstream of the $\TopologyUp$ open-row carrier and the Urysohn-style separation rows before compact-open, uniformity, or Kelleyfication consumers inspect it.

Thus the regularity surface is exported only after the topology source, separator request, real-valued separator, componentwise transport, replay, provenance, and local naming rows are visible. No omitted ambient principle can be projected from the finite packet.
\end{proof}
