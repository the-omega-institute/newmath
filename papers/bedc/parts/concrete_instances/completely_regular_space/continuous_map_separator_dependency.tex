Every $\CompletelyRegularSpaceUp$ separator read is dependency-visible as a $\TopologyUp$ source row followed by the $\UrysohnLemmaUp$ separation route and the $\ContinuousMapUp$ real-valued map row. Thus a downstream compact-open, uniformity, or Kelleyfication consumer receives the displayed point-versus-closed-set separator only through the existing topology and Urysohn sibling lattice of \autoref{thm:completely-regular-space-topology-urysohn-sibling-lattice}; it cannot replace that route by a host family of functions, a selected separator, or an ambient compactness principle.
