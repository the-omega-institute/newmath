\begin{theorem}[Completely regular space compact-open forward route]
\label{thm:completely-regular-space-compact-open-forward-route}
Every $\CompactOpenTopologyUp$ or $\KelleyficationUp$ consumer that reads a $\CompletelyRegularSpaceUp$ packet first passes through the displayed topology source of \autoref{ch:concrete-instances-topology-namecert}, the point-versus-closed-set regularity source of \autoref{ch:concrete-instances-regular-topological-space-namecert}, the $\RealUp$-valued $\ContinuousMapUp$ separator route of \autoref{ch:concrete-instances-continuousmap-namecert}, and the Urysohn-facing route of \autoref{ch:concrete-instances-urysohn-lemma-namecert}. The forward read then preserves componentwise transport, displayed replay, provenance, and local naming before any compact-open or Kelleyfication window may consume the separator.
\end{theorem}
\begin{proof}
Read the completely regular packet through the local topology--Urysohn dependency route and the sibling dependency theorem. These rows expose the topology carrier, the regular topological source, the point-versus-closed-set separation data, and the continuous real-valued separator before any later function-space consumer appears.

A compact-open reader can therefore inspect only the displayed $\TopologyUp$ source, $\RegularTopologicalSpaceUp$ boundary, $\RealUp$-valued $\ContinuousMapUp$ separator, and Urysohn-facing separation route. The $\CompactOpenTopologyUp$ window receives this separator as finite graph and target-open data, not as a host function space or an unrestricted family of separating maps.

A Kelleyfication reader uses the same route after the compact-open ledger has been displayed. Transport, replay, provenance, and local naming remain the rows attached to the original $\CompletelyRegularSpaceUp$ packet, so the consumer gains no quotient-open equality, choice-selected separator, ambient compactness principle, or external topology oracle.
\end{proof}
