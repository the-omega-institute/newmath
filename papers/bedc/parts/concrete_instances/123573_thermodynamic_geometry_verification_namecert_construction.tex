\chapter{A Concrete Naming Certificate for $\ThermodynamicGeometryVerificationUp$}
\label{ch:concrete-instances-thermodynamic-geometry-verification-namecert}
\origin{ai}

The $\ThermodynamicGeometryVerificationUp$ packet realizes \autoref{ch:visions-innovation-input-thermodynamic-geometry-verification} as finite NameCert data. It binds the cone coefficient slot, paired curvature instruments, regulator comparison, Ricci tensor read, tensor-coefficient ledger, and grid-scaling discriminator into one displayed verification carrier before any thermodynamic-geometry conclusion is public. The packet names a verification surface for the source vision; it is not continuum quantum field theory, a complete heat-kernel coefficient system, a regulator-independent continuum theorem, or an ambient differential-geometric construction.

\begin{definition}[Thermodynamic geometry verification carrier]
\label{def:thermodynamic-geometry-verification-carrier}
A $\ThermodynamicGeometryVerificationUp$ carrier is a finite $\BHist$ packet
$$
V=(K,C,R,M,T,G,H,E,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the cone-slot row, recording the Sommerfeld coefficient, curvature-mass slot comparison, and two-twelfths agreement from the source vision.
\item $C$ is the curvature-instrument row, carrying the geometric and entropy curvature reads and their generic-point agreement data.
\item $R$ is the regulator comparison row, recording the scheme-dependent area intercept, scheme-stable mass slope, and cross-scheme differencing discipline.
\item $M$ is the Ricci tensor row, exposing the three-dimensional directional reads and tensor-ratio target before any continuum tensor claim is public.
\item $T$ is the tensor-coefficient ledger, binding the homogeneous trace channel, two-dimensional coefficient basis, spherical-family constraints, and three-dimensional rank read.
\item $G$ is the grid-scaling discriminator row, separating $dx^2/T$ lattice residue from a continuum $T^0$ correction.
\item $H$ records componentwise $\hsame$ transport for $K,C,R,M,T,G,E,P,N$.
\item $E$ records displayed $\Cont$ replay from a verification request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\ThermodynamicGeometryVerificationUp$, the source vision, thermodynamic-geometry boundary data, curvature rows, entanglement rows, metric rows, heat-kernel rows, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ThermodynamicGeometryVerificationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It stores no continuum field theory, no regulator-independent theorem outside the displayed comparison, no hidden Ricci tensor, no complete heat-kernel coefficient system, and no lattice-to-continuum limit outside the grid-scaling row.
\end{definition}

\begin{theorem}[Thermodynamic geometry verification NameCert obligations]
\label{thm:thermodynamic-geometry-verification-namecert-obligations}
For every accepted $\ThermodynamicGeometryVerificationUp$ carrier
$$
V=(K,C,R,M,T,G,H,E,P,N),
$$
the local $\NameCert_{\ThermodynamicGeometryVerificationUp}$ surface consists exactly of cone-slot exposure through $K$, curvature-instrument exposure through $C$, regulator-scheme comparison through $R$, Ricci tensor read through $M$, tensor-coefficient ledger exposure through $T$, grid-scaling discrimination through $G$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $E$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:thermodynamic-geometry-verification-carrier}. The verification rows $K,C,R,M,T,G$ are the only numerical and geometric data available to a consumer.

The cone and curvature rows are read first. Row $K$ fixes the coefficient slot, while row $C$ records the paired curvature instruments and their agreement away from critical points.

The regulator, Ricci, tensor, and grid rows are then separate checks. Row $R$ prevents a scheme-dependent area intercept from being mistaken for a universal slope; row $M$ exposes the tensor directional read; row $T$ records the coefficient ledger; and row $G$ separates lattice residue from continuum correction.

Finally $H,E,P,N$ transport, replay, source, and name the same finite packet. Since no carrier coordinate contains a hidden continuum theorem or complete heat-kernel system, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Thermodynamic geometry verification curvature-slot route]
\label{thm:thermodynamic-geometry-verification-curvature-slot-route}
Every accepted curvature-slot, regulator, Ricci, tensor-coefficient, or grid-scaling read of a $\ThermodynamicGeometryVerificationUp$ packet must pass through
$$
K \longmapsto C \longmapsto R \longmapsto M \longmapsto T \longmapsto G \longmapsto H \longmapsto E \longmapsto P \longmapsto N .
$$
The route exports a finite verification packet for the source vision, not a continuum completion theorem, a regulator-independent quantum-field-theory assertion, a hidden curvature oracle, or an ambient lattice-limit construction.
\end{theorem}
\begin{proof}
Use \autoref{thm:thermodynamic-geometry-verification-namecert-obligations}. A curvature-slot consumer starts from $K$ and $C$, because those rows are the only displayed coefficient and instrument reads.

Next read the regulator comparison and tensor rows. The scheme row $R$ records which slope survives regulator comparison, $M$ carries the Ricci tensor target, and $T$ records the coefficient ledger used by tensor-facing consumers.

The grid row $G$ is the final numerical discriminator. It decides whether the residual is the displayed $dx^2/T$ lattice term or a continuum correction, so no continuum claim is public before $G$ is visible.

Transport, replay, provenance, and local naming then preserve the same route. They add no continuum field theory, hidden curvature oracle, or complete heat-kernel coefficient system.
\end{proof}

\falsifiablePrediction{Every accepted thermodynamic-geometry verification read exposes the cone-slot, curvature-instrument, regulator-scheme, tensor-read, grid-scaling, hsame, Cont, Pkg, and local NameCert rows before any continuum verification claim is public.}

\independenceWitness{curvature, entanglement, metric, heatkernel, thermodynamic-geometry-boundary: no listed sibling is bijective to $\ThermodynamicGeometryVerificationUp$ because the packet jointly carries cone-slot, curvature-instrument, regulator-scheme, Ricci tensor, tensor-coefficient, grid-scaling, transport, replay, provenance, and local naming rows, while each sibling supplies only a curvature, entropy, metric, coefficient, or boundary surface without the complete verification classifier.}

The later Lean TasteGate draft must provide $\Nontrivial$ and $\FieldFaithful$ instances for the displayed carrier.

\closureat{ThermodynamicGeometryVerificationUp}{seedStr}

\begin{closurestatus}{\ThermodynamicGeometryVerificationUp}
  \constructivestory{A $\ThermodynamicGeometryVerificationUp$ packet is generated from cone-slot rows, curvature-instrument rows, regulator comparison rows, Ricci tensor rows, tensor-coefficient ledgers, grid-scaling discriminator rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:thermodynamic-geometry-verification-carrier}, \autoref{thm:thermodynamic-geometry-verification-namecert-obligations}, and \autoref{thm:thermodynamic-geometry-verification-curvature-slot-route} seed the finite thermodynamic-geometry verification packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close continuum quantum field theory, complete heat-kernel coefficient systems, regulator-independent continuum theorems, ambient differential geometry, checked Lean obligations, or bridge checking.}
  \upgradepath{Formal target registration requires checked carrier admission, cone-slot exposure, curvature-instrument replay, regulator comparison, Ricci tensor readback, tensor-coefficient ledger replay, grid-scaling discrimination, nontriviality, and field faithfulness for BEDC.Derived.ThermodynamicGeometryVerificationUp.}
\end{closurestatus}
