The $\RealDiagonalNonSurjectionUp$ packet consumes $\RealBaireCylinderUp$ as the finite prefix-cylinder readback surface for Real-facing diagonal rows. The sibling route through \autoref{ch:concrete-instances-real-baire-cylinder-namecert} first exposes the $\StreamNameUp$ finite-window schedule, then the $\RegSeqRatUp$ regular rational readback, and only then the terminal $\RealUp$ seal; the diagonal row therefore reads the Baire cylinder as displayed Real-facing data rather than as a host stream, quotient real equality, selected branch, or ambient completeness principle.
