The $\RealDiagonalNonSurjectionUp$ dependency route combines the Cantor-style diagonal cylinder, the $\RegSeqRatUp$ finite readback, the terminal $\RealUp$ seal, and the $\MetricUp$ separation surface in one visible order. A consumer first reads the finite binary disagreement through the Cantor route of \autoref{ch:concrete-instances-constructive-cantor-theorem-namecert} and the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}; only then does the route expose the Real seal of \autoref{ch:concrete-instances-real-namecert} and the metric comparison boundary of \autoref{ch:concrete-instances-metric-namecert}. The packet therefore supplies a concrete dependency edge from diagonal cylinders to metric separation without adding host cardinality, quotient real equality, selected branches, countable choice, or ambient completeness.
