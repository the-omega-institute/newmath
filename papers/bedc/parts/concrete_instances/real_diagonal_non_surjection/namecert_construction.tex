\chapter{A Concrete Naming Certificate for $\RealDiagonalNonSurjectionUp$}
\label{ch:concrete-instances-real-diagonal-non-surjection-namecert}
\origin{human}

Following Cantor's diagonal argument, the $\RealDiagonalNonSurjectionUp$ packet records the finite BEDC boundary that prevents a displayed countable $\StreamNameUp$ enumeration from being read as exhaustive $\RealUp$ data. It sits over the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic bit ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the comparison setoid boundary of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}. The packet names only the inspected enumeration rows, the diagonal bit-flip row, the regular readback route, transport, replay, provenance, and local naming data; it is not a host real-uncountability theorem, not a quotient-stream principle, and not a completed powerset of reals. The downstream Cantor diagonal real-uncountability packet of \autoref{ch:concrete-instances-cantor-diagonal-real-uncountability-namecert} consumes this finite non-surjection boundary as its Real-facing diagonal obstruction before adding CantorSpace and CantorSet rows.

\begin{definition}[Real diagonal non-surjection carrier]
\label{def:real-diagonal-non-surjection-carrier}
A $\RealDiagonalNonSurjectionUp$ carrier is a finite $\BHist$ packet
$$
D=(E,W,B,F,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the countable-enumeration row, displaying the finite slice of the proposed $\StreamNameUp$ enumeration inspected by the packet.
\item $W$ is the row-index schedule, listing the inspected diagonal rows and the finite windows used for each row.
\item $B$ is the dyadic-bit observation row, read through $\DyadicRatCoreUp$ before any real-name seal is exposed.
\item $F$ is the diagonal bit-flip row, recording for each inspected row the opposite dyadic bit at the matching diagonal window.
\item $R$ is the $\RegSeqRatUp$ readback row that turns the displayed bit-flip data into regular rational real-name observations.
\item $S$ is the terminal $\RealUp$ seal row for the diagonal witness, exposed only after $E,W,B,F,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for $E,W,B,F,R,S$ and for the replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an enumeration request through
$$
E,W,B,F,R \longmapsto S \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RealDiagonalNonSurjectionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchyEquivalenceSetoidUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealDiagonalNonSurjectionUp}$ row.
\end{enumerate}
The carrier contains no host real equality test, quotient stream equality, completed powerset of real names, countable-choice schedule, selected global enumeration inverse, ambient $\RealUp$ completeness theorem, Lean verification row, or bridge credential.
\end{definition}

\begin{theorem}[Real diagonal non-surjection NameCert obligations]
\label{thm:real-diagonal-non-surjection-namecert-obligations}
For every accepted $\RealDiagonalNonSurjectionUp$ carrier $D=(E,W,B,F,R,S,H,C,P,N)$, the local $\NameCert_{\RealDiagonalNonSurjectionUp}$ surface consists exactly of carrier admission for the displayed rows, enumeration-slice exposure through $E$, finite diagonal-window accounting through $W$, dyadic-bit readback through $B$, bit-flip exactness through $F$, regular rational readback through $R$, terminal real-name sealing through $S$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-diagonal-non-surjection-carrier}. The analytic rows are the enumeration slice $E$, the finite diagonal schedule $W$, the dyadic observation row $B$, the flipped-bit row $F$, the regular rational readback $R$, and the terminal seal $S$.

The enumeration obligations are finite. A consumer sees only the rows named by $W$, and for each such row it sees only the dyadic window displayed in $B$ before the corresponding flipped bit is recorded in $F$.

The real-name handoff is routed through $R$ and then $S$. Thus the diagonal witness is read as regular rational data before it is sealed as $\RealUp$ data, and the Cauchy-equivalence boundary remains a displayed comparison surface rather than a quotient of streams.

Transport and replay preserve the same rows componentwise. Since the carrier has no coordinate for host real equality, quotient streams, completed powersets, countable choice, selected enumeration inverses, ambient real completeness, Lean verification, or bridge checking, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Real diagonal non-surjection diagonal witness]
\label{thm:real-diagonal-non-surjection-diagonal-witness}
Every accepted $\RealDiagonalNonSurjectionUp$ packet exports a displayed diagonal witness route
$$
\begin{gathered}
\text{inspected row } n,\quad \text{dyadic bit } b_n,\\
b_n \longmapsto 1-b_n \longmapsto \RegSeqRatUp \longmapsto \RealUp .
\end{gathered}
$$
For each inspected enumeration row, the witness differs from that row at the matching displayed dyadic window before the terminal $\RealUp$ seal is exposed. Therefore a countable $\StreamNameUp$ enumeration cannot be used as an exhaustive Real-facing packet unless it supplies an additional displayed row that defeats the bit-flip witness through the same $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, and $\CauchyEquivalenceSetoidUp$ route.
\end{theorem}
\begin{proof}
Start with an accepted carrier $D=(E,W,B,F,R,S,H,C,P,N)$. For a row listed in $W$, the enumeration slice $E$ exposes the row and $B$ exposes the dyadic bit observed at the matching diagonal window.

The row $F$ records the opposite bit at that window. The displayed step is
$$
b_n \longmapsto 1-b_n .
$$
This is a finite dyadic row, not an appeal to host real equality or to a completed set of all real names.

The readback row $R$ repacks the flipped bits as regular rational observations, and $S$ is the only terminal $\RealUp$ seal available to the witness. Any comparison with the inspected row must therefore pass through the displayed Cauchy-equivalence setoid route, not through quotient streams or ambient completeness.

Consequently every inspected row is separated from the diagonal witness at its own displayed dyadic window. The packet refuses host real equality, quotient streams, completed powersets, countable choice, and ambient Real completeness as sources of surjectivity data.
\end{proof}

\closureat{RealDiagonalNonSurjectionUp}{seedStr}

\begin{closurestatus}{\RealDiagonalNonSurjectionUp}
  \constructivestory{A $\RealDiagonalNonSurjectionUp$ packet is generated from a finite StreamName enumeration slice, a diagonal row-index schedule, a DyadicRatCore bit-observation row, a flipped-bit row, a RegSeqRat readback route, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-diagonal-non-surjection-carrier}, \autoref{thm:real-diagonal-non-surjection-namecert-obligations}, and \autoref{thm:real-diagonal-non-surjection-diagonal-witness} seed the finite diagonal boundary for L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.RealDiagonalNonSurjectionUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real equality, quotient streams, completed powersets of real names, countable choice, selected enumeration inverses, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, enumeration-slice exposure, diagonal-window accounting, dyadic-bit readback, bit-flip exactness, RegSeqRat readback, RealUp sealing, Cauchy-equivalence comparison routing, replay stability, provenance exhaustion, and local naming for BEDC.Derived.RealDiagonalNonSurjectionUp.}
\end{closurestatus}
