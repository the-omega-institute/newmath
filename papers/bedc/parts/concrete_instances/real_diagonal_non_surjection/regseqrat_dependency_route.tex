\begin{theorem}[Real diagonal non-surjection RegSeqRat dependency route]
\label{thm:real-diagonal-non-surjection-regseqrat-dependency-route}
Every $\RealDiagonalNonSurjectionUp$ read that constructs the anti-diagonal $\RealUp$ row first exposes the finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance rows, and $\RealUp$ seal used by the source sequence. The consumer cannot bypass those rows with host stream equality, selected limits, quotient reals, ambient completeness, countable choice, or a hidden real-surjectivity principle.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:real-diagonal-non-surjection-carrier}. The rows $E$ and $W$ expose only the inspected finite enumeration slice and diagonal windows, so the anti-diagonal read begins as finite $\StreamNameUp$ data.

Next apply the diagonal witness route of \autoref{thm:real-diagonal-non-surjection-diagonal-witness}. The dyadic bit read and bit flip pass through $B$ and $F$, then the regular-rational readback $R$ repacks the displayed windows before the terminal seal $S$ is exposed.

Use the existing sibling dependency route of this chapter to keep the same boundary visible to downstream consumers. The sibling route already records that the diagonal packet is Real-facing, and \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert} supply the named readback and seal surfaces.

Since the carrier has no coordinate for host stream equality, selected limits, quotient reals, ambient completeness, countable choice, or hidden surjectivity data, every anti-diagonal Real row must follow the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ route.
\end{proof}
