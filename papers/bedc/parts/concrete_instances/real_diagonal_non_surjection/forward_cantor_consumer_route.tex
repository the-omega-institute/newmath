\begin{theorem}[RealDiagonalNonSurjection forward Cantor consumer route]
\label{thm:real-diagonal-non-surjection-forward-cantor-consumer-route}
Every $\CantorDiagonalRealUncountabilityUp$ consumer of an accepted $\RealDiagonalNonSurjectionUp$ carrier must first read the finite diagonal obstruction through the displayed $\StreamNameUp$ enumeration slice, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ bit-observation and bit-flip rows, the terminal $\RealUp$ seal, and the sibling Cantor route of \autoref{thm:real-diagonal-non-surjection-cantor-sibling-route}. The consumer may then pass to \autoref{ch:concrete-instances-cantor-diagonal-real-uncountability-namecert}, but only with the Real-facing seal and comparison boundary already constrained by \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:real-diagonal-non-surjection-carrier}. Its enumeration slice, row-index schedule, dyadic bit-observation row, flipped-bit row, regular rational readback, and Real seal are all displayed before the local diagonal witness is available.

Apply the StreamName dependency theorem and the Cantor sibling route. Theorems~\ref{thm:real-diagonal-non-surjection-streamname-dependency} and~\ref{thm:real-diagonal-non-surjection-cantor-sibling-route} force the consumer through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ before the disagreement ledger can be interpreted as Cantor-facing non-enumeration data.

The downstream chapter \autoref{ch:concrete-instances-cantor-diagonal-real-uncountability-namecert} therefore receives the diagonal obstruction as a finite Real-facing route, not as a free-standing host uncountability principle. The Real seal remains governed by \autoref{ch:concrete-instances-real-namecert}, so the consumer imports no quotient stream equality, completed powerset, countable choice, selected enumeration inverse, or ambient completeness.
\end{proof}
