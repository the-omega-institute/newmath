\begin{theorem}[Real diagonal non-surjection Cantor--RegSeqRat lock]
\label{thm:real-diagonal-non-surjection-cantor-regseqrat-lock}
Every accepted $\RealDiagonalNonSurjectionUp$ packet that claims a non-surjectivity read must factor through the Cantor diagonal row of \autoref{ch:concrete-instances-constructive-cantor-theorem-namecert}, the regular rational readback row of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal $\RealUp$ seal row of \autoref{ch:concrete-instances-real-namecert}, and the local $\NameCert_{\RealDiagonalNonSurjectionUp}$ ledger before the conclusion is exposed. In carrier coordinates this means that the enumeration slice $E$, diagonal schedule $W$, dyadic bit row $B$, flipped-bit row $F$, RegSeqRat row $R$, Real seal $S$, transport row $H$, replay row $C$, provenance row $P$, and naming row $N$ of \autoref{def:real-diagonal-non-surjection-carrier} remain visible in that order; no classical choice principle, host powerset, quotient stream equality, selected inverse, or ambient real-completeness row is introduced by the lock.
\end{theorem}
\begin{proof}
Start with the carrier and diagonal witness rows of \autoref{def:real-diagonal-non-surjection-carrier} and \autoref{thm:real-diagonal-non-surjection-diagonal-witness}. They expose $E,W,B,F,R,S,H,C,P,N$ before any non-surjectivity read is available.

Next use the Cantor sibling route of \autoref{thm:real-diagonal-non-surjection-cantor-sibling-route}. The Cantor-facing consumer receives the same displayed disagreement row and not a host powerset, completed binary stream, or selected inverse.

Then apply the RegSeqRat dependency route of \autoref{thm:real-diagonal-non-surjection-regseqrat-dependency-route} and the Real source route of \autoref{thm:real-diagonal-non-surjection-real-source-route}. The flipped bit row is read through $R$ as regular rational data before $S$ exposes the terminal Real-facing seal.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for classical choice, quotient stream equality, selected enumeration inverse, host powerset, or ambient real completeness, every accepted non-surjection claim remains locked to the Cantor, RegSeqRat, RealUp, and local NameCert rows.
\end{proof}
