The $\RealDiagonalNonSurjectionUp$ completion-facing dependency route consumes the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert} and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert} before a diagonal non-surjection packet may refuse a surjective enumeration. This anchor keeps the L10 readback lattice visible: the inspected enumeration slice and finite diagonal windows are read first, the disagreement is repacked through $\RegSeqRatUp$, the resulting row is sealed by $\RealUp$, and only then can the packet deny that the displayed enumeration is exhaustive.
