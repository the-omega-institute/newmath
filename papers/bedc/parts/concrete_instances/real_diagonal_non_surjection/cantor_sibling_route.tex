\begin{theorem}[RealDiagonalNonSurjection Cantor sibling route]
\label{thm:real-diagonal-non-surjection-cantor-sibling-route}
Every accepted $\RealDiagonalNonSurjectionUp$ carrier consumed by a Cantor-style diagonal argument remains a Real-facing sibling route through $\CantorSpaceUp$, $\ConstructiveCantorTheoremUp$, and the terminal $\RealUp$ seal. The consumer first reads the finite enumeration slice, diagonal row-index schedule, dyadic bit-observation row, flipped-bit row, $\RegSeqRatUp$ readback, $\CauchyEquivalenceSetoidUp$ comparison boundary, transport, replay, provenance, and local naming rows of \autoref{def:real-diagonal-non-surjection-carrier}; only then may the Cantor route of \autoref{ch:concrete-instances-constructive-cantor-theorem-namecert} interpret the same disagreement ledger as a Cantor-space non-enumeration witness.
\end{theorem}
\begin{proof}
Start with the carrier and diagonal witness rows of \autoref{def:real-diagonal-non-surjection-carrier} and \autoref{thm:real-diagonal-non-surjection-diagonal-witness}. They expose the inspected enumeration row, finite diagonal window, dyadic bit read, flipped-bit row, regular-rational readback, and terminal $\RealUp$ seal before any non-surjection conclusion is read.

Next route the displayed disagreement through the Cantor-space handoff of \autoref{ch:concrete-instances-constructive-cantor-theorem-namecert}. That sibling consumes the same finite diagonal classifier over $\CantorSpaceUp$ and carries it toward the Real-facing non-surjection boundary; it does not introduce a completed binary stream, host powerset, or ambient surjectivity principle.

Finally use the sibling dependency route already recorded for this chapter. The $\CauchyEquivalenceSetoidUp$ comparison and $\RealUp$ seal remain visible on the Real side, so the packet is a Cantor/Real diagonal sibling route rather than a kernel-only packet.
\end{proof}
