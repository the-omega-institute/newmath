The $\RealDiagonalNonSurjectionUp$ packet is a Real-facing diagonal boundary rather than a kernel-only island. Its diagonal witness consumes the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} for the displayed regular-rational readback, and the Cantor diagonal real-uncountability route of \autoref{ch:concrete-instances-cantor-diagonal-real-uncountability-namecert} consumes this chapter as its Real-facing non-surjection input before adding CantorSpace and CantorSet rows.

The non-surjection carrier first reads the reusable diagonal schedule of \autoref{ch:concrete-instances-diagonal-sequence-namecert}: finite stream windows select the inspected enumeration row, the diagonal bit disagreement is recorded at the selected coordinate, the $\RegSeqRatUp$ readback exposes the rational window that realizes the disagreement, and the terminal $\RealUp$ seal packages that readback as the Real-facing obstruction. Only after this route is visible may \autoref{ch:concrete-instances-cantor-diagonal-real-uncountability-namecert} and \autoref{ch:concrete-instances-constructive-cantor-theorem-namecert} consume the packet as a Cantor-facing diagonal obstruction.
