\chapter{A Concrete Naming Certificate for $\FiniteProofDagFrontierUp$}
\label{ch:concrete-instances-finite-proof-dag-frontier-namecert}
\origin{ai}

The $\FiniteProofDagFrontierUp$ packet realizes a MetaCIC open-problem route as finite proof-DAG bookkeeping. It sits beside the critical-path surface of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the public residual frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, and the normal-form consistency seal of \autoref{ch:concrete-instances-normal-form-consistency-seal-namecert}. The packet is a finite frontier carrier for nodes, edges, checker rows, normalization-frontier rows, obstructions, transport, replay, provenance, and local naming; it does not export subject reduction, unrestricted confluence, evaluator equality, quotient proof equality, global normalization, residual-substitution discharge, or kernel consistency.

\begin{definition}[Finite proof-DAG frontier carrier]
\label{def:finite-proof-dag-frontier-carrier}
A $\FiniteProofDagFrontierUp$ carrier is a finite $\BHist$ packet
$$
D=(V,A,K,F,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the finite node row, recording the proof objects, typing windows, and local source names inspected by the packet.
\item $A$ is the finite edge row, recording premise-to-conclusion dependencies and residual links between displayed proof-DAG nodes.
\item $K$ is the checker row, recording the bounded local verifier that reads only the displayed nodes and edges.
\item $F$ is the normalization-frontier row, recording candidate normal endpoints, unfinished normalization windows, and the boundary between them.
\item $O$ is the obstruction row, retaining absent subject-reduction, residual-substitution, global-normalization, evaluator-equality, quotient-proof-equality, and kernel-consistency endpoints.
\item $H$ records componentwise $\hsame$ transport for nodes, edges, checker rows, normalization frontiers, obstructions, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay through the proof-DAG node, edge, checker, frontier, and obstruction route.
\item $P$ is $\Pkg$ provenance over $\FiniteProofDagFrontierUp$, $\MetaCICCriticalPathUp$, $\MetaCICParallelDiamondFrontierUp$, $\MetaCICNormalizationFrontierUp$, $\NormalFormConsistencySealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteProofDagFrontierUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $V,A,K,F,O,H,C,P,N$. No coordinate stores subject reduction, unrestricted confluence, evaluator equality, quotient proof equality, global normalization, residual-substitution discharge, or kernel consistency.
\end{definition}

\begin{theorem}[Finite proof-DAG frontier NameCert obligations]
\label{thm:finite-proof-dag-frontier-namecert-obligations}
For every accepted $\FiniteProofDagFrontierUp$ carrier $D=(V,A,K,F,O,H,C,P,N)$, the local $\NameCert_{\FiniteProofDagFrontierUp}$ surface consists exactly of finite node admission, finite edge admission, bounded checker exposure, normalization-frontier exposure, obstruction retention, componentwise transport, displayed replay, provenance, local naming, and ledger refusal for subject reduction, unrestricted confluence, evaluator equality, quotient proof equality, global normalization, residual-substitution discharge, and kernel consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-proof-dag-frontier-carrier}. The node and edge rows $V,A$ are the only proof-DAG shape data available to the packet, while $K$ is the only checker row and $F$ is the only normalization-frontier row.

The obstruction row $O$ records the endpoints that remain absent. Transport through $H$ and replay through $C$ preserve the displayed DAG route without adding subject reduction, unrestricted confluence, evaluator equality, quotient proof equality, global normalization, residual-substitution discharge, or kernel consistency.

The provenance and local naming rows $P,N$ source and name the same finite route. Hence the local obligations are exactly the displayed node, edge, checker, frontier, obstruction, transport, replay, provenance, and naming rows.
\end{proof}

\begin{theorem}[Finite proof-DAG frontier non-escape]
\label{thm:finite-proof-dag-frontier-nonescape}
An accepted $\FiniteProofDagFrontierUp$ packet exports no subject reduction, no unrestricted confluence, no evaluator equality, no quotient proof equality, no global normalization, no residual-substitution discharge, and no kernel consistency. Every MetaCIC consumer of the packet must read only the displayed route
$$
V,A \longmapsto K,F,O \longmapsto H,C,P,N .
$$
\end{theorem}
\begin{proof}
Begin with the carrier inventory. The packet lists finite nodes, finite edges, a bounded checker, a normalization frontier, an obstruction row, transport, replay, provenance, and local naming.

Now inspect the consumer route. Any downstream read must pass through $V,A$ before reaching the checker and frontier rows $K,F$, and the obstruction row $O$ remains attached to the same route.

Since the carrier has no additional coordinate beyond $V,A,K,F,O,H,C,P,N$, none of subject reduction, unrestricted confluence, evaluator equality, quotient proof equality, global normalization, residual-substitution discharge, or kernel consistency can be projected from an accepted packet.
\end{proof}

\falsifiablePrediction{A $\FiniteProofDagFrontierUp$ read is refuted whenever a MetaCIC consumer treats a finite proof-DAG frontier as subject reduction, unrestricted confluence, evaluator equality, quotient proof equality, global normalization, residual-substitution discharge, or kernel consistency without displaying the node, edge, checker, normalization-frontier, obstruction, transport, replay, provenance, and local naming rows.}

\independenceWitness{metaciccriticalpath, metacic-parallel-diamond-frontier, metacicnormalizationfrontier, normalformconsistencyseal: none is bijective with $\FiniteProofDagFrontierUp$ because the packet jointly records finite nodes, finite edges, a bounded checker, a normalization frontier, obstruction retention, transport, replay, provenance, and local naming.}

The Lean follow-up path is \texttt{lean4/BEDC/Derived/FiniteProofDagFrontierUp/TasteGate.lean}. It must provide explicit $\mathsf{Nontrivial}\ \FiniteProofDagFrontierUp$ and $\mathsf{FieldFaithful}\ \FiniteProofDagFrontierUp$ instances preserving node, edge, checker, normalization-frontier, obstruction, transport, replay, provenance, and local naming rows.

\closureat{FiniteProofDagFrontierUp}{seedStr}

\begin{closurestatus}{\FiniteProofDagFrontierUp}
  \constructivestory{A $\FiniteProofDagFrontierUp$ packet is generated from finite proof-DAG nodes, finite edges, bounded checker rows, normalization-frontier rows, obstruction rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-proof-dag-frontier-carrier}, \autoref{thm:finite-proof-dag-frontier-namecert-obligations}, and \autoref{thm:finite-proof-dag-frontier-nonescape} seed the finite proof-DAG frontier route for MetaCIC bookkeeping consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close subject reduction, unrestricted confluence, evaluator equality, quotient proof equality, global normalization, residual-substitution discharge, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked node admission, edge admission, bounded checker exposure, normalization-frontier exposure, obstruction retention, transport, replay, provenance, local naming, and ledger non-escape for BEDC.Derived.FiniteProofDagFrontierUp.}
\end{closurestatus}
