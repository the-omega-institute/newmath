\chapter{A Concrete Naming Certificate for $\CauchyRealSpreadUp$}
\label{ch:concrete-instances-cauchy-real-spread-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names and Brouwer-style spread presentations, the $\CauchyRealSpreadUp$ packet records the finite spread interface through which a countable family of regular Cauchy real-name windows is read without importing choice sequences or quotient reals. It sits after the prefix discipline of \autoref{ch:concrete-instances-brouwer-spread-namecert}, the finite window schedule of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a spread-indexed real-name boundary, not a completed branch, not a selected Cauchy representative, not countable choice, not quotient real equality, and not a completeness principle for $\RealUp$.

The spread-indexed real-name boundary consumes \autoref{ch:concrete-instances-brouwer-spread-namecert} for prefix-tree admission, \autoref{ch:concrete-instances-streamname-namecert} for finite window scheduling, \autoref{ch:concrete-instances-regseqrat-namecert} for regular rational readback, and \autoref{ch:concrete-instances-real-namecert} only as the terminal seal.

\begin{definition}[CauchyRealSpread carrier]
\label{def:cauchy-real-spread-carrier}
A $\CauchyRealSpreadUp$ carrier is a finite $\BHist$ packet
$$
S=(R,W,E,T,Q,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the spread root row inherited from $\BrouwerSpreadUp$, recording the displayed root prefix and its admitted prefix-tree boundary.
\item $W$ is the finite window row, listing the spread prefixes and request windows whose regular Cauchy real-name data may be inspected.
\item $E$ is the extension law row, requiring every displayed extension of an admitted prefix to remain inside the same spread discipline before any real-name read is public.
\item $T$ is the $\StreamNameUp$ row that schedules finite observations over the admitted prefixes.
\item $Q$ is the $\RegSeqRatUp$ row obtained from the scheduled windows and the $\DyadicRatCoreUp$ tolerance ledger.
\item $A$ is the terminal $\RealUp$ seal row, available only after the spread root, finite window, extension law, stream schedule, dyadic ledger, and regular rational readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the spread root, finite window, extension law, StreamName, RegSeqRat, Real seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from spread-prefix admission through finite window inspection, extension-law checking, stream scheduling, regular rational readback, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\CauchyRealSpreadUp$, $\BrouwerSpreadUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyRealSpreadUp}$ row.
\end{enumerate}
The carrier has no coordinate for completed infinite branches, choice-sequence selection, quotient real equality, selected Cauchy representatives, host real equality, countable choice, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[CauchyRealSpread NameCert obligations]
\label{thm:cauchy-real-spread-namecert-obligations}
For an accepted $\CauchyRealSpreadUp$ carrier $S=(R,W,E,T,Q,A,H,C,P,N)$, the local $\NameCert_{\CauchyRealSpreadUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the spread root, finite window, extension law, StreamName row, RegSeqRat row, Real seal, componentwise transport, continuation replay, provenance, and local naming rows;
\item root compatibility, requiring every public real-name request to start from the displayed $\BrouwerSpreadUp$ root row $R$;
\item window discipline, requiring the finite request window $W$ to be read before any stream, rational, or real-seal row is consumed;
\item extension stability, requiring each admitted prefix extension to pass through the law row $E$ before the scheduled stream row $T$ is public;
\item regular readback exactness, requiring every finite real-name observation to pass through the $\DyadicRatCoreUp$ tolerance ledger and the $\RegSeqRatUp$ row $Q$ before the $\RealUp$ seal $A$ is read;
\item transport and replay, requiring $H$ and $C$ to preserve the same ordered route under $\hsame$ transport and displayed $\Cont$ replay;
\item ledger non-escape, excluding completed branches, choice-sequence selection, quotient real equality, selected Cauchy representatives, host real equality, countable choice, and ambient completeness of $\RealUp$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the accepted carrier. The tuple exposes exactly the spread root $R$, finite window $W$, extension law $E$, stream schedule $T$, regular rational readback $Q$, Real seal $A$, and structural rows $H,C,P,N$.

Root compatibility and window discipline follow from the tuple order. A public real-name request begins with the displayed $\BrouwerSpreadUp$ root and then reads only the finite windows stored in $W$.

Extension stability is recorded by $E$. The row checks each displayed prefix extension before the scheduled $\StreamNameUp$ observations in $T$ can be consumed, so no completed branch or choice-sequence selection is introduced.

Regular readback exactness then routes every tolerance request through the $\DyadicRatCoreUp$ ledger and the $\RegSeqRatUp$ row $Q$ before the terminal $\RealUp$ seal $A$ is available. The rows $H$ and $C$ transport and replay this same ordered route, while $P$ and $N$ record provenance and local naming.

Since the carrier has no further coordinate, quotient real equality, selected representatives, host real equality, countable choice, and ambient completeness of $\RealUp$ cannot be read from the packet.
\end{proof}

\begin{theorem}[CauchyRealSpread root and window discipline]
\label{thm:cauchy-real-spread-root-window-discipline}
For every accepted $\CauchyRealSpreadUp$ carrier
$$
S=(R,W,E,T,Q,A,H,C,P,N),
$$
each public spread-indexed real-name request must preserve the $\BrouwerSpreadUp$ root row $R$ and the finite window row $W$ before it reads the $\StreamNameUp$ schedule $T$, the $\DyadicRatCoreUp$ tolerance data, the $\RegSeqRatUp$ readback row $Q$, or the terminal $\RealUp$ seal $A$. The route records only root admission and finite window discipline; it does not select completed branches, choice sequences, quotient real equality, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-real-spread-carrier} and the obligation list of \autoref{thm:cauchy-real-spread-namecert-obligations}. The rows $R$ and $W$ are displayed before the stream schedule, regular readback, Real seal, and structural rows.

The root row $R$ supplies the admitted $\BrouwerSpreadUp$ prefix surface. A public request that omits $R$ has no displayed spread root from which a finite real-name window can be read.

The window row $W$ then lists the finite spread prefixes and request windows available to the consumer. Only after this row is visible may the route pass to the $\StreamNameUp$ schedule $T$, the $\DyadicRatCoreUp$ tolerance ledger, the $\RegSeqRatUp$ readback row $Q$, and the terminal $\RealUp$ seal $A$.

The rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and local naming. Since no row stores a completed branch, choice sequence, quotient equality, or completeness theorem, the discipline is exactly the finite root-window route.
\end{proof}

\begin{theorem}[CauchyRealSpread sibling route]
\label{thm:cauchy-real-spread-sibling-route}
For every accepted $\CauchyRealSpreadUp$ carrier
$$
S=(R,W,E,T,Q,A,H,C,P,N),
$$
any read exposing a spread of regular Cauchy names must factor through the sibling route consisting of the $\StreamNameUp$ window rows, the $\RegSeqRatUp$ readback rows, the terminal $\RealUp$ seal, and the diagonal-selection boundary of \autoref{ch:concrete-instances-cauchy-diagonal-selection-namecert}. The route is a finite dependency surface: it adds no selected host subsequence, no quotient stream equality, and no ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the carrier rows from \autoref{def:cauchy-real-spread-carrier}. The rows $W,T$ display the finite windows and StreamName schedule, while $Q$ is the regular rational readback row and $A$ is the terminal Real seal.

Use the route discipline of \autoref{thm:cauchy-real-spread-root-window-discipline}. A spread-indexed request begins from the root and window rows before it can expose StreamName observations.

The regular readback then passes through $Q$ and reaches $A$ only after the finite schedule is visible. The diagonal-selection sibling of \autoref{ch:concrete-instances-cauchy-diagonal-selection-namecert} may consume this surface as a boundary for comparing displayed Cauchy windows, but it does not supply an external subsequence selector.

Transport through $H$, replay through $C$, and naming through $P,N$ preserve the same sibling route. Since the carrier has no coordinate for a selected host subsequence, quotient stream equality, or ambient completeness, the exposed spread of regular Cauchy names remains inside the finite L10 route.
\end{proof}

\begin{theorem}[CauchyRealSpread extension readback stability]
\label{thm:cauchy-real-spread-extension-readback-stability}
For an accepted $\CauchyRealSpreadUp$ carrier $S=(R,W,E,T,Q,A,H,C,P,N)$, every readback along an admitted prefix extension is stable only when the extension law row $E$ remains visible between the root-window route $R,W$ and the $\StreamNameUp$ schedule $T$. The resulting readback passes through $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ as finite rows and never becomes a completed branch, a selected Cauchy representative, quotient real equality, or a Real-completeness assertion.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:cauchy-real-spread-namecert-obligations}. Its extension-stability clause places $E$ before the scheduled stream observations and after the displayed root-window route.

For an admitted prefix extension, read $R$ and $W$ first. The row $E$ checks that the displayed extension remains inside the same spread discipline before any observation in $T$ is public.

After $E$ has been read, the route may consume the $\StreamNameUp$ schedule $T$, the $\DyadicRatCoreUp$ tolerance ledger, the $\RegSeqRatUp$ row $Q$, and finally the $\RealUp$ seal $A$. Transport through $H$ and replay through $C$ preserve this order for the same finite packet.

There is no carrier coordinate for an infinite branch, choice-sequence selector, selected representative, quotient equality, or completeness principle. Thus extension readback stability is a finite routing condition, not an external real-analysis closure claim.
\end{proof}

\begin{theorem}[CauchyRealSpread regular-seal route]
\label{thm:cauchy-real-spread-regular-seal-route}
Every accepted $\CauchyRealSpreadUp$ route to the terminal $\RealUp$ seal $A$ must pass through the ordered rows
$$
R,W,E,T,Q,A,H,C,P,N.
$$
The route starts at the $\BrouwerSpreadUp$ root, keeps the finite window and extension-law rows visible, reads the $\StreamNameUp$ schedule, consumes the $\DyadicRatCoreUp$ tolerance ledger through the $\RegSeqRatUp$ readback row $Q$, and only then exposes the Real seal. It exports no completed branch, no choice sequence, no quotient real equality, no selected representative, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use the carrier definition and \autoref{thm:cauchy-real-spread-root-window-discipline}. They force every public real-name request to begin with the $\BrouwerSpreadUp$ root row $R$ and finite window row $W$.

Next apply extension readback stability, \autoref{thm:cauchy-real-spread-extension-readback-stability}. The extension law row $E$ remains visible before the schedule $T$ can expose finite observations along admitted prefixes.

The regular readback exactness clause of \autoref{thm:cauchy-real-spread-namecert-obligations} then routes tolerance and rational data through $\DyadicRatCoreUp$ and $\RegSeqRatUp$ in $Q$ before the terminal $\RealUp$ seal $A$ is public.

Finally the structural rows $H,C,P,N$ carry the same route under componentwise transport, displayed replay, provenance, and local naming. Because the accepted carrier has no coordinate for the excluded objects, the regular-seal route cannot be retyped as a completed branch, choice sequence, quotient equality, selected representative, or Real-completeness theorem.
\end{proof}

\closureat{CauchyRealSpreadUp}{seedStr}

\begin{closurestatus}{\CauchyRealSpreadUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-real-spread-carrier} and \autoref{thm:cauchy-real-spread-namecert-obligations} seed the spread-indexed regular Cauchy real-name surface through $\BrouwerSpreadUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed infinite branches, choice-sequence selection, quotient real equality, selected Cauchy representatives, host real equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, spread-root compatibility, finite-window discipline, extension stability, regular readback exactness, transport replay, provenance, and non-escape for BEDC.Derived.CauchyRealSpreadUp.}
  \constructivestory{A $\CauchyRealSpreadUp$ packet is generated from a BrouwerSpread root, finite request windows, an extension law, a StreamName schedule, DyadicRatCore tolerance data, RegSeqRat readback, a Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
