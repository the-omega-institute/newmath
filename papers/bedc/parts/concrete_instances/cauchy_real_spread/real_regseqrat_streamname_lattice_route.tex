The $\CauchyRealSpreadUp$ child route through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert} is the displayed L10 completion stack for spread-indexed Cauchy reads. The $\StreamNameUp$ row supplies finite prefix windows, the $\RegSeqRatUp$ row reads those windows as regular rational observations, and the $\RealUp$ row is only the terminal seal after the displayed readback has been consumed; the route is not an ambient $\RealUp$ completeness principle, a quotient real equality, or a selected completed branch.
