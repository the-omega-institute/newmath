Every downstream $\CauchyRealSpreadUp$ consumer that reaches a Real-completion read factors through the displayed spread root of \autoref{ch:concrete-instances-brouwer-spread-namecert}, the finite-window schedule of \autoref{ch:concrete-instances-streamname-namecert}, the tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. This is a forward dependency route for spread-indexed Cauchy reads; it does not assert completion, select an infinite branch, identify quotient real names, or add an ambient $\RealUp$ completeness principle.
