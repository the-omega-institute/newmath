\begin{theorem}[CauchyRealSpread StreamName and RegSeqRat dependency anchor]
\label{thm:cauchy-real-spread-streamname-regseqrat-dependency-anchor}
Every $\CauchyRealSpreadUp$ consumer that reads a public spread-indexed real-name row must pass through the sibling $\StreamNameUp$ scheduling route, the $\RegSeqRatUp$ regular readback route, the terminal $\RealUp$ seal, and the $\DyadicRatCoreUp$ tolerance ledger before any Cauchy-real spread datum is exposed. Thus the dependency route factors through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}, with the dyadic tolerance row retained as finite NameCert data rather than as a host completeness or quotient-real principle.
\end{theorem}
