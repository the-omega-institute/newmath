\chapter{A Concrete Naming Certificate for $\CertificatePipelineStageBoundaryUp$}
\label{ch:concrete-instances-certificate-pipeline-stage-boundary-namecert}
\origin{human}

The $\CertificatePipelineStageBoundaryUp$ interface names the finite boundary between two adjacent certificate compiler stages. It keeps both compiler packets visible, records the shared middle-stage compatibility row, and exposes only the classifier, continuation, package, and local naming rows needed by downstream compiler-pipeline consumers.

The boundary is a consumer of \autoref{ch:concrete-instances-certificatecompiler-namecert} because the adjacent packets $K_{SM}$ and $K_{MT}$ are accepted $\CertificateCompilerUp$ routes. This chapter adds only the displayed middle-row compatibility ledger $J$ between those two compiler packets.

\begin{definition}[CertificatePipelineStageBoundary carrier]
\label{def:certificate-pipeline-stage-boundary-carrier}
A $\CertificatePipelineStageBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(S,M,T,K_{SM},K_{MT},J,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $S$ is the source $\NameCert$ row for the first compiler stage;
\item $M$ is the middle $\NameCert$ row that receives the first stage and supplies the second stage;
\item $T$ is the target $\NameCert$ row for the second compiler stage;
\item $K_{SM}$ is an accepted $\CertificateCompilerUp$ packet from $S$ to $M$;
\item $K_{MT}$ is an accepted $\CertificateCompilerUp$ packet from $M$ to $T$;
\item $J$ is the middle-stage compatibility row asserting that the target row read by $K_{SM}$ is the source row read by $K_{MT}$;
\item $H$ records componentwise $\hsame$ transports for the source, middle, target, adjacent compiler packets, compatibility row, package row, and local naming row;
\item $C$ records the displayed $\Cont$ routes for the first compiler stage, the middle-stage handoff, the second compiler stage, and the composed boundary read;
\item $P$ names $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\CertificateCompilerUp$, and the adjacent compiler-stage boundary shape;
\item $N$ is the local $\NameCert_{\CertificatePipelineStageBoundaryUp}$ row.
\end{enumerate}
The packet contains no host compiler semantics, program equivalence theorem, global optimisation proof, or unrecorded intermediate-representation interpretation.
\end{definition}

\begin{theorem}[CertificatePipelineStageBoundary NameCert obligations]
\label{thm:certificate-pipeline-stage-boundary-namecert-obligations}
For the carrier of \autoref{def:certificate-pipeline-stage-boundary-carrier}, the local $\NameCert_{\CertificatePipelineStageBoundaryUp}$ surface consists of five finite obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $B=(S,M,T,K_{SM},K_{MT},J,H,C,P,N)$;
\item middle landing and source compatibility, requiring the target certificate row consumed by $K_{SM}$ to be the same displayed middle row $M$ used as the source certificate row of $K_{MT}$;
\item classifier transport across the boundary, first through $K_{SM}$ and then through $K_{MT}$, with no classifier row outside $S,M,T$;
\item continuation composition, composing the first-stage route, middle-stage handoff, and second-stage route through the displayed $C$ row;
\item non-escape from host compiler semantics, excluding program equality, semantic models, global optimisation correctness, and unrecorded target behavior.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection of \autoref{def:certificate-pipeline-stage-boundary-carrier}. The rows $S,M,T,K_{SM},K_{MT},J,H,C,P,N$ are visible coordinates of one finite $\BHist$ packet.

The middle compatibility obligation is the row $J$. It states that the first accepted $\CertificateCompilerUp$ packet lands in the same middle naming certificate that the second accepted compiler packet reads as its source.

Classifier transport is read stage by stage. The first packet transports source classifier rows from $S$ to $M$, the second transports those middle rows from $M$ to $T$, and the componentwise transports in $H$ preserve the same displayed boundary rows.

Continuation composition is the corresponding route in $C$. It concatenates the first-stage continuation route, the middle-stage handoff route, and the second-stage continuation route before the package row $P$ and local naming row $N$ expose the boundary to consumers.

No further coordinate remains in the carrier. Hence host compiler semantics, program equality, global optimisation correctness, and unrecorded target behavior cannot witness the adjacent-stage boundary.
\end{proof}

\begin{theorem}[CertificatePipelineStageBoundary middle-row determinacy]
\label{thm:certificate-pipeline-stage-boundary-middle-row-determinacy}
Let
$$
B=(S,M,T,K_{SM},K_{MT},J,H,C,P,N)
$$
and
$$
B'=(S',M',T',K_{SM},K_{MT},J',H',C',P',N')
$$
be accepted $\CertificatePipelineStageBoundaryUp$ carriers in the sense of \autoref{def:certificate-pipeline-stage-boundary-carrier} and \autoref{thm:certificate-pipeline-stage-boundary-namecert-obligations}. Assume that both displayed compatibility rows identify the target $\NameCert$ row read by the shared first compiler packet $K_{SM}$ with the source $\NameCert$ row read by the shared second compiler packet $K_{MT}$. Then the displayed middle rows $M$ and $M'$ are classifier-same as the common middle $\NameCert$ row of the adjacent compiler pair, and every composed boundary read exported by either $B$ or $B'$ factors through that common middle row before it reaches the target row.
\end{theorem}
\begin{proof}
Project the shared first compiler packet as
$$
K_{SM}=(S_{0},M_{0},G_{SM},E_{SM},C_{SM},H_{SM},P_{SM},N_{SM}).
$$
By \autoref{thm:certificate-compiler-endpoint-certificate-availability}, every accepted read of $K_{SM}$ exposes its endpoint $\NameCert$ rows before any landing or classifier comparison is consumed. Since the same packet $K_{SM}$ occurs in both boundary carriers, the target endpoint available to both boundary reads is the single displayed row $M_{0}$.

Project the shared second compiler packet as
$$
K_{MT}=(M_{1},T_{0},G_{MT},E_{MT},C_{MT},H_{MT},P_{MT},N_{MT}).
$$
The same endpoint-availability result says that the source endpoint available to every accepted read of $K_{MT}$ is the single displayed row $M_{1}$.

Now read the two compatibility rows. In $B$, the row $J$ asserts that the target row read from $K_{SM}$ is the source row read from $K_{MT}$. Hence $J$ identifies $M_{0}$ and $M_{1}$ as the displayed middle certificate used by the boundary carrier, so $M$ is classified with that common middle row. The same argument with $J'$ shows that $M'$ is classified with the same common middle row. By transitivity of the local $\NameCert$ classifier comparison, $M$ and $M'$ are classifier-same.

It remains to identify the route used by a composed boundary read. The boundary obligation theorem makes the composed read the continuation row in $C$ obtained by replaying the first compiler route, the middle-stage handoff, and the second compiler route. The first route reaches the target endpoint of $K_{SM}$, the second route starts from the source endpoint of $K_{MT}$, and the compatibility row is exactly the displayed identification between those two endpoint reads. Thus the composed read in $B$ factors through the common middle row before any target read in $T$ is available.

The same reasoning applies to $B'$ with $C'$ and $T'$. No alternative middle row can be inserted, because \autoref{thm:certificate-compiler-middle-row-handoff} forces a composite of the two accepted compiler packets to hand off only through their displayed middle $\NameCert$ row, and \autoref{thm:certificate-compiler-edge-classifier-exactness} makes classifier transport pass through the displayed landing, $\hsame$, and $\Cont$ rows of those same packets. Since the boundary carrier has no coordinate for an independent middle certificate, host compiler semantics, program equivalence theorem, or unrecorded target behavior, every composed boundary read of either accepted packet factors through that common middle row.
\end{proof}

\begin{closurestatus}{\CertificatePipelineStageBoundaryUp}
  \constructivestory{A $\CertificatePipelineStageBoundaryUp$ packet is generated from finite $\BHist$ rows for source, middle, and target certificate stages, two compiler-edge packets, compatibility rows at the shared middle endpoint, and the composed boundary read. The pipeline boundary is built by replaying the first compiler route, the middle-stage handoff, and the second compiler route through $\Cont$ before any target-facing row is available. Componentwise $\hsame$ transports source, middle, target, landing, and classifier rows, while $\Pkg$ records the certificate-compiler dependencies. The common middle row is therefore a displayed boundary coordinate, not an inferred host compiler equivalence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter binds the stage-boundary carrier, middle-row handoff, edge-classifier exactness, and composed-boundary factorization for $\CertificatePipelineStageBoundaryUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{It does not claim host compiler semantics, program equivalence, an independent hidden middle certificate, or unrecorded target behavior.}
  \upgradepath{Obligation closure requires Lean encodings of the boundary carrier, middle compatibility row, edge classifier transport, and composed-read factorization.}
\end{closurestatus}
