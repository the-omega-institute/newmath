\chapter{A Concrete Naming Certificate for $\HostPrimitiveLeakageUp$}
\label{ch:concrete-instances-hostprimitiveleakage-namecert}
\origin{ai}

The $\HostPrimitiveLeakageUp$ packet realizes the host-smuggling branch of \autoref{ch:visions-philosophy-auditable-generativity} as a finite diagnostic refusal carrier. It sits beside \autoref{ch:concrete-instances-auditgatefailurewitness-namecert}, \autoref{ch:concrete-instances-quotient-soundness-boundary-namecert}, \autoref{ch:concrete-instances-negativecertificateboundary-namecert}, \autoref{ch:concrete-instances-auditgateboundary-namecert}, and \autoref{ch:concrete-instances-kernel-audit-witness-namecert}. Those siblings expose failed audit rows, quotient-boundary refusals, negative certificates, audit-gate verdicts, and kernel acceptance witnesses; this packet isolates the public-surface import of host primitives as the refused item.

\begin{definition}[HostPrimitiveLeakage carrier]
\label{def:host-primitive-leakage-carrier}
A $\HostPrimitiveLeakageUp$ carrier is a finite $\BHist$ packet
$$
L=(S,Q,E,D,F,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the public-surface site where a consumer requests host equality, host numerals, host lists, host function spaces, quotient soundness, choice, or propositional extensionality;
\item $Q$ is the requested primitive row, naming the host feature that would otherwise enter as substrate content;
\item $E$ is the exposed BEDC replacement row, containing the finite $\BHist$, $\msame$, $\hsame$, $\psame$, $\Cont$, $\Pkg$, or local $\NameCert$ route offered instead;
\item $D$ is the diagnostic row explaining which public route attempted the import;
\item $F$ is the failed-gate row consumed from $\AuditGateFailureWitnessUp$;
\item $G$ is the audit-boundary row consumed from $\AuditGateBoundaryUp$;
\item $H$ records componentwise $\hsame$ transport for the site, request, replacement, diagnostic, failed-gate, audit-boundary, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay for the refusal route and for the replacement route when one is available;
\item $P$ is the $\Pkg$ provenance over $\HostPrimitiveLeakageUp$, $\AuditGateFailureWitnessUp$, $\QuotientSoundnessBoundaryUp$, $\NegativeCertificateBoundaryUp$, $\AuditGateBoundaryUp$, $\KernelAuditWitnessUp$, $\BHist$, $\msame$, $\hsame$, $\psame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\HostPrimitiveLeakageUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host equality token, host-level numeric object, host list eliminator, host function-space extensionality principle, quotient soundness dependency, choice principle, or propositional-extensionality transport.
\end{definition}
\leandef{BEDC.Derived.HostPrimitiveLeakageUp.HostPrimitiveLeakageUp}

\begin{theorem}[HostPrimitiveLeakage NameCert obligations]
\label{thm:host-primitive-leakage-namecert-obligations}
For the carrier of \autoref{def:host-primitive-leakage-carrier}, the local $\NameCert_{\HostPrimitiveLeakageUp}$ surface consists exactly of carrier admission for
$$
L=(S,Q,E,D,F,G,H,C,P,N),
$$
public-site exposure through $S$, requested-host-primitive exposure through $Q$, BEDC replacement exposure through $E$, diagnostic readback through $D$, failed-gate witness exposure through $F$, audit-boundary verdict exposure through $G$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:host-primitive-leakage-carrier}. The rows $S,Q,E,D,F,G,H,C,P,N$ account for the public site, requested host primitive, BEDC replacement, diagnostic readback, failed-gate witness, audit boundary, transport, replay, provenance, and local naming surface.

The failed-gate and audit rows are consumed from the sibling certificates \autoref{ch:concrete-instances-auditgatefailurewitness-namecert} and \autoref{ch:concrete-instances-auditgateboundary-namecert}. The quotient-specific branch is read through \autoref{ch:concrete-instances-quotient-soundness-boundary-namecert}, and the named refusal branch is read through \autoref{ch:concrete-instances-negativecertificateboundary-namecert}.

The kernel witness sibling \autoref{ch:concrete-instances-kernel-audit-witness-namecert} supplies the accepted-declaration readback surface, not a permission to import host primitives. Repacking these rows gives the local naming certificate, and every public consumer route is forced to display the requested host feature before the route can be refused or replaced.
\end{proof}
\leanchecked{BEDC.Derived.HostPrimitiveLeakageUp.HostPrimitiveLeakageTasteGate\_single\_carrier\_alignment}

\begin{theorem}[HostPrimitiveLeakage non-escape]
\label{thm:host-primitive-leakage-nonescape}
Every accepted $\HostPrimitiveLeakageUp$ packet $L=(S,Q,E,D,F,G,H,C,P,N)$ admits public reads only through the displayed diagnostic route $S,Q,D,F,G,H,C,P,N$ and, when present, the replacement route $E,H,C,P,N$. A consumer that reads host equality, host numerals, host lists, host function spaces, quotient soundness, choice, or propositional extensionality as BEDC-internal content has left the accepted packet.
\end{theorem}
\leanchecked{BEDC.Derived.HostPrimitiveLeakageUp.HostPrimitiveLeakageNonescape}
\begin{proof}
Inspect the carrier. The requested host feature is present only as the request row $Q$, and the diagnostic row $D$ records the public route that tried to expose it.

Apply \autoref{thm:host-primitive-leakage-namecert-obligations}. The failed-gate row $F$ and audit-boundary row $G$ keep the request on the refusal side of the packet, while $H,C,P,N$ preserve only the displayed diagnostic and replacement routes.

Finally use the excluded coordinates in \autoref{def:host-primitive-leakage-carrier}. Since no host primitive is a packet coordinate, every accepted consumer route either reads it as a refused request through $Q,D,F,G$ or uses the visible BEDC replacement row $E$.
\end{proof}

\falsifiablePrediction{An accepted $\HostPrimitiveLeakageUp$ packet is refuted by any public consumer route that obtains host equality, host numerals, host lists, host function spaces, quotient soundness, choice, or propositional extensionality without first exposing the requested primitive row and the failed-gate diagnostic row.}

\independenceWitness{auditgatefailurewitness, quotientsoundnessboundary, negativecertificateboundary, auditgateboundary, kernelauditwitness: $\HostPrimitiveLeakageUp$ has the simultaneous public-site row, requested-host-primitive row, replacement row, diagnostic row, failed-gate row, and audit-boundary row. The siblings each expose a gate, quotient, negative, audit, or kernel face, but none carries the complete finite leakage witness.}

The Lean follow-up for this chapter must provide \texttt{Derived/HostPrimitiveLeakageUp/TasteGate.lean} with $\mathsf{Nontrivial}\ \HostPrimitiveLeakageUp$ and $\mathsf{FieldFaithful}\ \HostPrimitiveLeakageUp$ instances for the displayed carrier fields.

\begin{theorem}[HostPrimitiveLeakage public export]
\label{thm:host-primitive-leakage-public-export}
Every public $\HostPrimitiveLeakageUp$ leakage read must expose the public site $S$, requested primitive $Q$, replacement row $E$, diagnostic row $D$, failed-gate row $F$, audit-boundary row $G$, transport row $H$, replay row $C$, provenance row $P$, local naming row $N$, and the displayed \texttt{TasteGate} marker before a consumer can use either the refusal route or the replacement route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:host-primitive-leakage-carrier}. The public leakage surface is exactly $S,Q,E,D,F,G,H,C,P,N$, with $Q$ naming the requested host primitive and $E$ naming only the BEDC replacement route when one is available.

Apply \autoref{thm:host-primitive-leakage-namecert-obligations} and \autoref{thm:host-primitive-leakage-nonescape}. The first theorem lists the local NameCert obligations for every displayed row, and the second prevents a public consumer from using a host primitive as accepted BEDC content.

The falsifiable prediction records the observable failure condition, while the independence witness separates this packet from the audit-gate and boundary siblings. Together with the displayed \texttt{BEDC.Derived.HostPrimitiveLeakageUp.taste\_gate} marker, these rows expose the refusal or replacement route before any public consumer may use it.
\end{proof}

\begin{theorem}[HostPrimitiveLeakage report-row refusal]
\label{thm:host-primitive-leakage-report-row-refusal}
Let $L=(S,Q,E,D,F,G,H,C,P,N)$ be an accepted $\HostPrimitiveLeakageUp$ packet. Any public report that identifies the requested primitive $Q$ as host equality, host numerals, host lists, host function spaces, quotient soundness, choice, or propositional extensionality must pass through the diagnostic report row $D$, failed-gate row $F$, audit-boundary row $G$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$ before the refusal or replacement route is exported.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:host-primitive-leakage-carrier}. The requested host feature is the row $Q$, and the only row that reports where the public import was attempted is $D$.

Apply \autoref{thm:host-primitive-leakage-nonescape}. It keeps every accepted public read on the displayed diagnostic route $S,Q,D,F,G,H,C,P,N$ or on the replacement route $E,H,C,P,N$.

Finally use \autoref{thm:host-primitive-leakage-public-export}. The public export must display all ten rows before a consumer can use the refusal or replacement route, so a report that omits $D,F,G,H,C,P,N$ cannot be an accepted $\HostPrimitiveLeakageUp$ export.
\end{proof}

\begin{theorem}[HostPrimitiveLeakage quotient-soundness non-escape]
\label{thm:host-primitive-leakage-quot-soundness-nonescape}
Let $L=(S,Q,E,D,F,G,H,C,P,N)$ be an accepted $\HostPrimitiveLeakageUp$ packet whose requested primitive row $Q$ names quotient soundness. The packet can expose that request only as a refused host primitive routed through $S,Q,D,F,G,H,C,P,N$ and through the sibling quotient-boundary provenance in $P$; it does not export quotient soundness as BEDC-internal evidence.
\end{theorem}
\begin{proof}
Use the carrier projection from \autoref{def:host-primitive-leakage-carrier}. Quotient soundness can occur only as the requested primitive row $Q$, while the provenance row $P$ records the quotient-boundary sibling route.

Apply \autoref{thm:host-primitive-leakage-namecert-obligations}. The local NameCert surface requires the request row, diagnostic row, failed-gate row, audit-boundary row, replay, provenance, and naming rows to be displayed together.

Now apply \autoref{thm:host-primitive-leakage-nonescape}. Since quotient soundness is listed among the host primitives that cannot be accepted as BEDC-internal content, the only accepted read is the refusal route, with any BEDC replacement separated into $E,H,C,P,N$.
\end{proof}

\begin{theorem}[HostPrimitiveLeakage audit-gate factorization]
\label{thm:host-primitive-leakage-audit-gate-factorization}
Every accepted public export of a $\HostPrimitiveLeakageUp$ packet $L=(S,Q,E,D,F,G,H,C,P,N)$ factors its leakage refusal through the failed-gate row $F$ and audit-boundary row $G$ before the structural rows $H,C,P,N$ can name the exported route. Thus a consumer cannot bypass the audit gate by reading $Q$ or $E$ alone.
\end{theorem}
\begin{proof}
Start with \autoref{thm:host-primitive-leakage-public-export}. It states that a public leakage read displays $S,Q,E,D,F,G,H,C,P,N$ before either the refusal route or replacement route is available.

The NameCert obligations of \autoref{thm:host-primitive-leakage-namecert-obligations} identify $F$ as the failed-gate witness row and $G$ as the audit-boundary verdict row. These rows occur before the structural transport, replay, provenance, and local naming rows are consumed.

Finally apply \autoref{thm:host-primitive-leakage-nonescape}. Reading $Q$ alone would expose only a refused request, and reading $E$ alone would expose only a replacement row; neither read can bypass the displayed audit-gate factorization through $F,G,H,C,P,N$.
\end{proof}

\closureat{HostPrimitiveLeakageUp}{matureStr}[BEDC.Derived.HostPrimitiveLeakageUp.taste\_gate]

\begin{closurestatus}{\HostPrimitiveLeakageUp}
  \leanchecked{BEDC.Derived.HostPrimitiveLeakageUp.taste\_gate}
  \constructivestory{A $\HostPrimitiveLeakageUp$ packet is generated from a public-surface site, requested host primitive, BEDC replacement row, diagnostic readback, failed-gate witness, audit-boundary row, componentwise transports, continuation replay, package provenance, and local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:host-primitive-leakage-carrier}, \autoref{thm:host-primitive-leakage-namecert-obligations}, \autoref{thm:host-primitive-leakage-nonescape}, \autoref{thm:host-primitive-leakage-public-export}, the displayed falsifiable prediction, the displayed independence witness, the audit-boundary sibling chapters, and the displayed \texttt{BEDC.Derived.HostPrimitiveLeakageUp.taste\_gate} marker bind the refusal packet through $\BHist$, $\msame$, $\hsame$, $\psame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \closureledger{BEDC.Meta.DiscoveryDeltaLedger.hostPrimitiveLeakageDeltaLedger}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, bridge checking, or every host-leakage case.}
  \upgradepath{Machine bridge checking requires a checked standard interface row that preserves the refusal packet across standard BEDC interfaces.}
\end{closurestatus}
