\chapter{A Concrete Naming Certificate for $\StepFunctionUniformApproximationUp$}
\label{ch:concrete-instances-step-function-uniform-approximation-namecert}
\origin{human}

Following Bishop and Bridges on regulated functions and uniform approximation, the $\StepFunctionUniformApproximationUp$ packet records the finite certificate by which a regulated target is read through displayed step approximants and uniform tolerance rows. It sits between the dyadic step approximation source of \autoref{ch:concrete-instances-dyadic-step-approximation-namecert}, the regulated step-function surface of \autoref{ch:concrete-instances-regulated-step-function-namecert}, the uniform limit route of \autoref{ch:concrete-instances-uniformlimit-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names uniform approximation as finite BEDC data before any host function equality, quotient stream equality, selected limiting function, countable-choice schedule, or ambient real-completeness theorem is available.

\begin{definition}[Step-function uniform approximation carrier]
\label{def:step-function-uniform-approximation-carrier}
A $\StepFunctionUniformApproximationUp$ carrier is a finite $\BHist$ packet
$$
S=(D,R,U,W,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicStepApproximationUp$ source row, recording the displayed dyadic mesh, finite cell ledger, step values, and oscillation bounds used by the approximation.
\item $R$ is the regulated-function target row, read through the $\RegulatedStepFunctionUp$ surface and recording the regulated target boundary to which the step data is compared.
\item $U$ is the shared uniform tolerance ledger. It records one finite tolerance budget whose dyadic reads are shared by the step source, regulated target, finite windows, and terminal real seal.
\item $W$ is the $\StreamNameUp$ finite-window row through which the target and approximants are inspected.
\item $Q$ is the $\RegSeqRatUp$ readback row for the displayed window comparisons and tolerance witnesses.
\item $E$ is the $\RealUp$ seal row, reached only after $D,R,U,W,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the step source, regulated target, tolerance ledger, finite windows, readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the dyadic step source and regulated target through the shared uniform tolerance ledger, finite windows, readback, real seal, transports, provenance, and local name.
\item $P$ is the $\Pkg$ provenance over $\StepFunctionUniformApproximationUp$, $\DyadicStepApproximationUp$, $\RegulatedStepFunctionUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\StepFunctionUniformApproximationUp}$ row.
\end{enumerate}
The carrier predicate asserts that these rows form one finite uniform-approximation surface. It contains no host function equality, quotient stream equality, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness coordinate.
\end{definition}

\begin{theorem}[Step-function uniform approximation NameCert obligations]
\label{thm:step-function-uniform-approximation-namecert-obligations}
For every accepted $\StepFunctionUniformApproximationUp$ carrier $S=(D,R,U,W,Q,E,H,C,P,N)$, the local $\NameCert_{\StepFunctionUniformApproximationUp}$ surface consists exactly of carrier admission, dyadic step-source admission through $D$, regulated-target admission through $R$, shared uniform-tolerance exactness through $U$, finite-window readback through $W$ and $Q$, $\RealUp$ seal nonescape through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:step-function-uniform-approximation-carrier}. The analytic rows are the dyadic step source $D$, the regulated target row $R$, the shared tolerance ledger $U$, the finite window row $W$, the regular rational readback row $Q$, and the terminal real seal $E$.

Read the source side first. The dyadic step approximation chapter supplies finite cells, displayed values, and oscillation data, while the regulated step-function chapter supplies the target-facing regulated row. These two rows are the only places where step data and regulated-target data enter the packet.

The uniform comparison is then finite. The tolerance ledger $U$ supplies one shared budget, $W$ selects the finite windows on which that budget is inspected, and $Q$ records the regular rational readback of the displayed comparisons before $E$ is available.

Finally, $H,C,P,N$ preserve and name the same route. Since the carrier has no host-function coordinate, quotient-stream coordinate, selected-limit coordinate, choice schedule, or ambient completeness row, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Step-function uniform approximation regulated route]
\label{thm:step-function-uniform-approximation-regulated-route}
Every $\RealUp$-facing regulated approximation read of an accepted $\StepFunctionUniformApproximationUp$ carrier factors through
$$
D,R,U,W,Q,E,H,C,P,N .
$$
The route exports only the displayed dyadic step source, regulated target, shared uniform tolerance ledger, finite windows, regular rational readback, real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. It exports no host function equality, quotient stream equality, selected limit, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the obligation surface of \autoref{thm:step-function-uniform-approximation-namecert-obligations}. It confines every accepted packet to the rows $D,R,U,W,Q,E,H,C,P,N$.

Apply the step-source and regulated-target sibling chapters. The source row $D$ supplies finite dyadic step data, and the target row $R$ keeps the regulated function boundary displayed before the uniform comparison is read.

The Real-facing handoff is available only after $U,W,Q$. The shared tolerance ledger fixes the finite budget, the stream windows expose the finite observations, and the regular rational readback records the comparison before the $\RealUp$ seal is public.

The remaining rows are structural. Transport, replay, provenance, and local naming repack the same finite route, while the refused host equality, quotient equality, selected limit, countable choice, and ambient completeness objects have no coordinate through which to enter.
\end{proof}

\closureat{StepFunctionUniformApproximationUp}{seedStr}

\begin{closurestatus}{\StepFunctionUniformApproximationUp}
  \constructivestory{A $\StepFunctionUniformApproximationUp$ packet is generated from a dyadic step approximation source, a regulated target row, a shared uniform tolerance ledger, finite StreamName windows, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:step-function-uniform-approximation-carrier}, \autoref{thm:step-function-uniform-approximation-namecert-obligations}, and \autoref{thm:step-function-uniform-approximation-regulated-route} seed the finite uniform-approximation packet connecting dyadic step data and regulated-function consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, quotient stream equality, selected limits, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, step-source admission, regulated-target admission, uniform-tolerance exactness, finite-window readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.StepFunctionUniformApproximationUp.}
\end{closurestatus}
