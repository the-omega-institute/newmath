\chapter{A Concrete Naming Certificate for $\RadstromEmbeddingUp$}
\label{ch:concrete-instances-radstrom-embedding-namecert}
\origin{human}

Following Radstrom's embedding of convex compact sets into an ordered vector group, the $\RadstromEmbeddingUp$ packet records the finite BEDC surface on which convex compact-set addition and Hausdorff-distance reads become a cancellative embedding route. It sits beside the compact-subset hyperspace carrier of \autoref{ch:concrete-instances-hyperspace-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the convex-set carrier of \autoref{ch:concrete-instances-convexset-namecert}, the Hausdorff metric route of \autoref{ch:concrete-instances-hausdorff-metric-namecert}, the Vietoris topology route of \autoref{ch:concrete-instances-vietoris-topology-namecert}, and the real seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only finite compact, convex, Hausdorff, Vietoris, and real-valued rows; it is not a host Banach-space completion, quotient equality of compact sets, selected support functional, or ambient compactness theorem.

\begin{definition}[Radstrom embedding carrier]\label{def:radstrom-embedding-carrier}
A $\RadstromEmbeddingUp$ carrier is a finite $\BHist$ packet
$$
R=(K,V,H,T,E,D,S,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row carrying the finite compact metric presentation from which compact-set names are read.
\item $V$ is the $\ConvexSetUp$ row recording the inspected convex compact-set operations and finite Minkowski-sum windows.
\item $H$ is the $\HausdorffMetricUp$ row recording the finite Hausdorff comparison ledger on compact subsets.
\item $T$ is the $\VietorisTopologyUp$ row recording the hit--miss compatibility windows used before the embedding row is exposed.
\item $E$ is the embedding row, displaying the cancellative difference-class name determined by $K,V,H,T$.
\item $D$ is the distance-readback row into $\RealUp$, exposed only after the compact, convex, Hausdorff, Vietoris, and embedding rows have been displayed.
\item $S$ stores componentwise $\hsame$ transport for compact, convex, Hausdorff, Vietoris, embedding, distance, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from compact-set inspection to convex operation windows, Hausdorff comparison, Vietoris compatibility, embedding exposure, and real-valued distance readback.
\item $P$ is the $\Pkg$ provenance over $\RadstromEmbeddingUp$, $\CompactMetricUp$, $\ConvexSetUp$, $\HausdorffMetricUp$, $\VietorisTopologyUp$, $\HyperspaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RadstromEmbeddingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,V,H,T,E,D,S,C,P,N$. It has no coordinate for host quotient spaces, selected support functions, external Banach completions, powerset equality, countable choice, or open-cover compactness.
\end{definition}

\begin{theorem}[Radstrom embedding NameCert obligations]\label{thm:radstrom-embedding-namecert-obligations}
For every accepted $\RadstromEmbeddingUp$ carrier $R=(K,V,H,T,E,D,S,C,P,N)$, the local $\NameCert_{\RadstromEmbeddingUp}$ surface consists exactly of carrier admission for the compact metric source, convex compact-set operations, Hausdorff comparison ledger, Vietoris compatibility row, embedding row, real-valued distance readback, componentwise transport, replay, provenance, and local naming rows. The obligations require the embedding row $E$ to be downstream of $K,V,H,T$, and they require the $\RealUp$ distance readback $D$ to be downstream of $E$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:radstrom-embedding-carrier}. Carrier admission is the displayed tuple $K,V,H,T,E,D,S,C,P,N$, so the local certificate has no detached coordinate for an ambient quotient space or external completion.

The compact, convex, Hausdorff, and Vietoris obligations are read from $K,V,H,T$. Since $E$ is the only embedding row, every embedding exposure is downstream of those four rows.

The real-valued readback is $D$, and its only source is the displayed embedding row. The rows $S,C,P,N$ transport, replay, source, and name the same route, so the NameCert surface is exhausted by the listed finite rows.
\end{proof}

\begin{theorem}[Radstrom embedding hyperspace route]\label{thm:radstrom-embedding-hyperspace-route}
Every accepted $\RadstromEmbeddingUp$ consumer that reads the embedding row or the $\RealUp$ distance readback factors through the finite hyperspace route
$$
\CompactMetricUp \longmapsto \HyperspaceUp \longmapsto \ConvexSetUp \longmapsto \HausdorffMetricUp \longmapsto \VietorisTopologyUp \longmapsto E \longmapsto D.
$$
The route uses the compact-subset carrier of \autoref{ch:concrete-instances-hyperspace-namecert} as the neighbour through which convex compact-set and Hausdorff-metric requests are inspected. It exposes no quotient equality of compact sets, host linear-space completion, selected support functional, choice principle, or open-cover compactness proof.
\end{theorem}
\begin{proof}
Begin with the compact-subset side. The carrier row $K$ supplies the compact metric presentation, and the provenance row $P$ routes compact-subset inspection through the sibling hyperspace chapter before any convex operation is read.

Next read the convex and metric rows. The rows $V,H,T$ display convex compact-set operations, finite Hausdorff comparisons, and Vietoris compatibility windows. Thus the embedding row $E$ cannot be inspected before the compact hyperspace, convex, Hausdorff, and Vietoris rows are all present.

Finally read the real-facing row $D$. Since $D$ is downstream of $E$ and the transport and replay rows preserve the same order, any consumer of the Radstrom surface factors through the displayed hyperspace route and cannot recover any excluded host principle.
\end{proof}

\falsifiablePrediction{Any accepted $\RadstromEmbeddingUp$ carrier that reaches the embedding row or the $\RealUp$ readback while omitting the compact metric row, hyperspace route, convex compact-set row, Hausdorff metric row, Vietoris compatibility row, transport, replay, provenance, or local naming row refutes the packet.}

\independenceWitness{hyperspace, compactmetric, convexset, hausdorffmetric, vietoristopology, real: $\RadstromEmbeddingUp$ is not bijective to any listed sibling because it jointly carries convex compact-set operations, Hausdorff comparison, Vietoris compatibility, a cancellative embedding row, real-valued readback, transport, replay, provenance, and local naming.}

\closureat{RadstromEmbeddingUp}{seedStr}

\begin{closurestatus}{\RadstromEmbeddingUp}
  \constructivestory{A $\RadstromEmbeddingUp$ packet is generated from CompactMetric, Hyperspace, ConvexSet, HausdorffMetric, VietorisTopology, embedding, Real readback, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:radstrom-embedding-carrier}, \autoref{thm:radstrom-embedding-namecert-obligations}, and \autoref{thm:radstrom-embedding-hyperspace-route} seed the finite convex compact hyperspace route into Radstrom embedding data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality of compact sets, host Banach completions, selected support functionals, countable choice, open-cover compactness, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, convex compact-set operation exposure, Hausdorff comparison exactness, Vietoris compatibility, embedding nonescape, Real readback, provenance, and local NameCert exhaustion for BEDC.Derived.RadstromEmbeddingUp.}
\end{closurestatus}
