\chapter{A Concrete Naming Certificate for $\TaoMetastableCauchyUp$}
\label{ch:concrete-instances-tao-metastable-cauchy-namecert}
\origin{human}

Following Tao's metastability/no-counterexample interpretation of Cauchy convergence, the $\TaoMetastableCauchyUp$ packet records a finite control-window answer for a Cauchy read before any completed limit, quotient stream, or countable-choice selector is available. It sits on the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-facing boundary of \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert}. The point of the packet is not to prove ambient $\RealUp$ completeness; it is to make the finite Cauchy window demanded by a control function into named BEDC data.

\begin{definition}[Tao metastable Cauchy carrier]\label{def:tao-metastable-cauchy-carrier}
A $\TaoMetastableCauchyUp$ carrier is a finite $\BHist$ packet
$$
T=(q,F,W,D,S,R,E,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $q$ is the dyadic precision request, read through the $\DyadicRatCoreUp$ tolerance ledger.
\item $F$ is the finite control row, naming the inspected control-function values only through displayed requests.
\item $W$ is the metastable window row, displaying a bound $n$ and the finite interval from $n$ to $F(n)$.
\item $D$ is the $\DyadicRatCoreUp$ ledger used by every comparison inside the window.
\item $S$ is the $\StreamNameUp$ finite-observation row opened for the named stream positions in $W$.
\item $R$ is the $\RegSeqRatUp$ rational readback row for the observed values.
\item $E$ is the downstream $\RealUp$ seal, exposed only after $q,F,W,D,S,R$ have been displayed.
\item $B$ is the $\BishopRegularCauchyCompletionUp$ boundary row, recording that the window is completion-facing without selecting a completed limit.
\item $H$ records componentwise $\hsame$ transport for all displayed rows.
\item $C$ records displayed $\Cont$ replay from the precision request and control row through the finite window, dyadic ledger, stream observations, rational readback, terminal seal, and completion boundary.
\item $P$ records $\Pkg$ provenance over $\TaoMetastableCauchyUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BishopRegularCauchyCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\TaoMetastableCauchyUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only by $q,F,W,D,S,R,E,B,H,C,P,N$. It has no coordinate for countable choice, quotient stream equality, selected limits, host real equality, trichotomy, or an ambient $\RealUp$ completeness theorem.
\end{definition}
\leanchecked{BEDC.Derived.TaoMetastableCauchyUp.TaoMetastableCauchyTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Tao metastable Cauchy NameCert obligations]\label{thm:tao-metastable-cauchy-namecert-obligations}
For every accepted $\TaoMetastableCauchyUp$ carrier
$$
T=(q,F,W,D,S,R,E,B,H,C,P,N),
$$
the local $\NameCert_{\TaoMetastableCauchyUp}$ surface consists exactly of finite carrier admission, precision request exposure, finite control-row exposure, metastable window extraction, dyadic tolerance accounting, StreamName observation, RegSeqRat readback, Real seal boundary, Bishop regular Cauchy completion boundary, componentwise transport, displayed replay, provenance, and local naming. Every public read factors through those rows before it can cite a metastable Cauchy answer.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:tao-metastable-cauchy-carrier}. The accepted packet exposes exactly $q,F,W,D,S,R,E,B,H,C,P,N$, so no hidden limit selector or quotient stream coordinate is available.

The request, control, and window rows supply the finite no-counterexample data. The row $q$ fixes the tolerance, $F$ fixes the displayed control values, and $W$ names the finite interval in which all comparisons are inspected.

The analytic rows remain on the same route. The dyadic ledger $D$, StreamName row $S$, and RegSeqRat row $R$ provide the only comparison data before the terminal $\RealUp$ seal $E$ and the $\BishopRegularCauchyCompletionUp$ boundary row $B$ are read.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no row for countable choice, quotient stream equality, selected limits, host real equality, trichotomy, or ambient completeness, the displayed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Tao metastable Cauchy window extraction]\label{thm:tao-metastable-cauchy-window-extraction}
Every accepted $\TaoMetastableCauchyUp$ public read extracts a finite window before any real-facing or completion-facing output is consumed: the precision request $q$ and control row $F$ determine the displayed window $W$, every comparison in $W$ is read through the common $\DyadicRatCoreUp$ ledger $D$, the inspected values are read through $\StreamNameUp$ and $\RegSeqRatUp$ rows $S,R$, and the downstream $\RealUp$ seal $E$ and $\BishopRegularCauchyCompletionUp$ boundary row $B$ remain after that finite route. The read exports no countable choice, quotient stream equality, selected limit, host real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:tao-metastable-cauchy-namecert-obligations}. A public read must open $q,F,W$ before it can reach the analytic rows, so the control request is answered by a displayed finite interval rather than by an infinite selection principle.

The comparison data are then forced through $D,S,R$. The dyadic ledger records the tolerance, StreamName supplies the finite observations named by the window, and RegSeqRat supplies the rational readbacks actually compared.

The endpoint rows are downstream only. The $\RealUp$ seal $E$ and $\BishopRegularCauchyCompletionUp$ boundary row $B$ can be cited only after the finite window route has been displayed, while $H,C,P,N$ preserve the same route as transport, replay, provenance, and local naming.

Therefore the extracted object is a finite metastable Cauchy window. Since no carrier row stores a selected limit, quotient equality, countable-choice witness, host equality, trichotomy, or ambient completeness principle, none of those objects is exported by the read.
\end{proof}

\closureat{TaoMetastableCauchyUp}{seedStr}

\begin{closurestatus}{\TaoMetastableCauchyUp}
  \constructivestory{A $\TaoMetastableCauchyUp$ packet is generated from a dyadic precision request, finite control row, metastable window row, dyadic tolerance ledger, StreamName observations, RegSeqRat readback, Real seal boundary, Bishop regular Cauchy completion boundary, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:tao-metastable-cauchy-carrier}, \autoref{thm:tao-metastable-cauchy-namecert-obligations}, and \autoref{thm:tao-metastable-cauchy-window-extraction} seed the choice-free metastable Cauchy window route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim countable choice, quotient stream equality, selected limits, host real equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite control-window extraction, dyadic tolerance accounting, StreamName observation, RegSeqRat readback, Real seal boundary, Bishop regular Cauchy completion boundary, transport, replay, provenance, and local naming for BEDC.Derived.TaoMetastableCauchyUp.}
\end{closurestatus}
