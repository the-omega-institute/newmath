\chapter{A Concrete Naming Certificate for $\SkorokhodSpaceUp$}
\label{ch:concrete-instances-skorokhod-space-namecert}
\origin{human}

Following Skorokhod and Billingsley, the $\SkorokhodSpaceUp$ packet records the finite path-space interface for cadlag real-valued paths before any host topology, quotient path equality, weak convergence theorem, or stochastic-process compactness principle is available. It sits over the cadlag path rows of \autoref{ch:concrete-instances-cadlag-real-function-namecert}, the metric and Polish-space carriers of \autoref{ch:concrete-instances-metric-namecert} and \autoref{ch:concrete-instances-polishspace-namecert}, the countable-name windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives later path and compactness consumers one finite NameCert surface for Skorokhod-style reads while refusing selected time changes, quotient path equality, ambient completeness, and probabilistic tightness claims.

\begin{definition}[Skorokhod space carrier]
\label{def:skorokhod-space-carrier}
A $\SkorokhodSpaceUp$ carrier is a finite $\BHist$ packet
$$
S=(D,L,O,W,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\CadlagRealFunctionUp$ path carrier, exposing right-continuous real-valued paths with displayed left-limit rows.
\item $L$ is the finite increasing time-change ledger, recording the inspected monotone clock changes and their endpoint alignment.
\item $O$ is the dyadic oscillation-window row, recording the finite jump and modulus checks used before any path comparison is public.
\item $W$ is the $\StreamNameUp$ observation schedule for time, path value, clock, and oscillation windows.
\item $Q$ is the $\RegSeqRatUp$ readback row for rational time stamps, clock values, and dyadic tolerances.
\item $R$ is the $\RealUp$ seal for path values and endpoint comparisons reached only after $W$ and $Q$ are displayed.
\item $H$ records componentwise $\hsame$ transport for cadlag path, time-change, oscillation, stream, regular-rational, real-seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a path-space request through cadlag admission, finite time-change inspection, dyadic oscillation, stream observation, regular-rational readback, and real sealing.
\item $P$ is the $\Pkg$ provenance over $\SkorokhodSpaceUp$, $\CadlagRealFunctionUp$, $\MetricUp$, $\PolishSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SkorokhodSpaceUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host Skorokhod topology, quotient path equality, selected subsequence or time change, ambient $\RealUp$ completeness, probability measure, tightness theorem, or bridge credential.
\end{definition}
\leanstmt{BEDC.Derived.SkorokhodSpaceUp}

\begin{theorem}[Skorokhod space NameCert obligations]
\label{thm:skorokhod-space-namecert-obligations}
For every accepted $\SkorokhodSpaceUp$ packet $S=(D,L,O,W,Q,R,H,C,P,N)$, the local $\NameCert_{\SkorokhodSpaceUp}$ surface consists exactly of cadlag path admission through $D$, finite increasing time-change inspection through $L$, dyadic oscillation-window inspection through $O$, stream observation through $W$, regular-rational readback through $Q$, real sealing through $R$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$. Any consumer that asks for a Skorokhod path-space read must display those rows before the read is public.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:skorokhod-space-carrier}. The rows $D,L,O,W,Q,R,H,C,P,N$ are the whole accepted packet, so local naming can inspect only cadlag path data, finite clock data, oscillation windows, stream windows, regular-rational readback, real seals, transport, replay, provenance, and the local certificate.

The path-space read first consumes $D,L,O$. The cadlag row supplies the path carrier, the time-change ledger supplies only finite increasing clock data, and the dyadic oscillation row supplies the finite jump and modulus checks needed for a comparison window.

The countable-name and real-value route is then fixed by $W,Q,R$. Stream windows expose observations, regular-rational rows read the displayed rational data, and the terminal real seal is reached only after those rows are visible.

Finally $H,C,P,N$ preserve and name the same finite packet. Since there is no coordinate for quotient path equality, selected time changes, ambient completeness, probability measures, tightness, or a bridge credential, those surfaces are not part of the local NameCert obligations.
\end{proof}

\begin{theorem}[Skorokhod space time-change stability]
\label{thm:skorokhod-space-time-change-stability}
Replacing the finite increasing time-change ledger $L$ of an accepted $\SkorokhodSpaceUp$ packet along the corresponding $\hsame$ transport in $H$ preserves the public path-space classifier only when the cadlag path row $D$, dyadic oscillation row $O$, stream window row $W$, regular-rational readback row $Q$, real seal $R$, replay row $C$, provenance row $P$, and local naming row $N$ are transported with it. A time-change read cannot be consumed as a detached clock row.
\end{theorem}
\begin{proof}
Read the componentwise transport row $H$. It relates the time-change ledger to the cadlag path, oscillation, stream, readback, real-seal, replay, provenance, and local naming rows as one accepted packet.

Transporting $L$ alone would expose only clock data. The classifier also needs the cadlag row $D$, the oscillation row $O$, and the $W,Q,R$ observation and seal route before any path-space comparison is public.

Replay and provenance keep the transported packet tied to the same NameCert row. Therefore stability holds only for the displayed componentwise transport, not for a detached selected time change or quotient clock equality.
\end{proof}

\begin{theorem}[Skorokhod space cadlag consumer route]
\label{thm:skorokhod-space-cadlag-consumer-route}
Every $\CadlagRealFunctionUp$ consumer that reads a path through $\SkorokhodSpaceUp$ must pass through the finite route
$$
\CadlagRealFunctionUp \longmapsto \MetricUp \longmapsto \PolishSpaceUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The route reads cadlag path rows, metric and Polish carrier rows, countable-name windows, regular-rational readback, and real sealing before path-space comparison is public. It exports no stochastic-process law, selected subsequence, quotient path equality, host Skorokhod topology, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start with the cadlag consumer. It can read path values only through the $\CadlagRealFunctionUp$ row $D$ and the finite clock and oscillation rows $L,O$.

The metric and Polish carriers provide the path-space interface in which those finite rows are compared. They do not add quotient path equality or a host topology; they only name the carrier route required before a countable-name read is meaningful.

Now apply the countable-name readback route. The $\StreamNameUp$ row opens the observation windows, $\RegSeqRatUp$ reads the rational time and tolerance data, and $\RealUp$ seals the path values and endpoint comparisons.

The structural rows $H,C,P,N$ preserve this route for downstream consumers. Hence every cadlag consumer route through $\SkorokhodSpaceUp$ remains finite and displayed, with no stochastic law, selected subsequence, quotient path equality, host topology, or ambient completeness principle added.
\end{proof}

\falsifiablePrediction{Every accepted $\SkorokhodSpaceUp$ read exposes a cadlag path row, a finite increasing time-change ledger, a dyadic oscillation window, StreamName observation, RegSeqRat readback, and a Real seal before any path-space comparison is public.}

\independenceWitness{cadlag-real-function, metric, polish-space, streamname, regseqrat, real: none is bijective to SkorokhodSpaceUp because the path-space packet binds cadlag paths, finite time-change ledgers, dyadic oscillation windows, countable-name observation, regular-rational readback, real sealing, transport, replay, provenance, and local NameCert rows as one carrier.}

\closureat{SkorokhodSpaceUp}{seedStr}

\begin{closurestatus}{\SkorokhodSpaceUp}
  \constructivestory{A $\SkorokhodSpaceUp$ packet is generated from a cadlag real-function path row, a finite increasing time-change ledger, dyadic oscillation windows, StreamName observation, RegSeqRat readback, Real sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:skorokhod-space-carrier}, \autoref{thm:skorokhod-space-namecert-obligations}, \autoref{thm:skorokhod-space-time-change-stability}, and \autoref{thm:skorokhod-space-cadlag-consumer-route} seed the finite path-space NameCert surface over cadlag, metric, Polish, StreamName, RegSeqRat, and Real rows.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.SkorokhodSpaceUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host Skorokhod topology, quotient path equality, selected subsequences or time changes, stochastic-process laws, tightness, ambient completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, time-change stability, oscillation-window readback, cadlag consumer routing, transport, replay, provenance, and local naming for BEDC.Derived.SkorokhodSpaceUp.}
\end{closurestatus}
