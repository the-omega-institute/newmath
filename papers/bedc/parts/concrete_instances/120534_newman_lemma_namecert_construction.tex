\chapter{A Concrete Naming Certificate for $\NewmanLemmaUp$}
\label{ch:concrete-instances-newman-lemma-namecert}
\origin{human}

Following Newman on terminating abstract reduction systems, the $\NewmanLemmaUp$ packet records the finite BEDC surface by which local confluence and normalization evidence can be routed toward confluence-facing MetaCIC consumers. It sits beside the traditional Church--Rosser packet of \autoref{ch:concrete-instances-church-rosser-namecert} and reads the MetaCIC normalization-confluence bridge of \autoref{ch:concrete-instances-metacic-normalization-confluence-bridge-namecert}, the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, and the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. The packet names a handoff from terminating local peaks to a bounded confluence route; it is not an unconditional Church--Rosser theorem, a subject-reduction theorem, quotient syntax equality, evaluator equality, or kernel consistency.

\begin{definition}[Newman lemma carrier]
\label{def:newman-lemma-carrier}
A $\NewmanLemmaUp$ carrier is a finite $\BHist$ packet
$$
N=(T,L,W,J,B,O,H,C,P,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the terminating or strongly normalizing measure row for the displayed reduction source.
\item $L$ is the local-confluence row, exposing only finite one-step peak data for the same source.
\item $W$ is the well-founded descent ledger that forces every iterated peak analysis to consume a smaller displayed row.
\item $J$ is the local join budget extracted from $L$ under the descent row $W$.
\item $B$ is the MetaCIC bridge row, read through $\MetacicNormalizationConfluenceBridgeUp$ and carrying only normalization-confluence correspondence data.
\item $O$ is the obstruction row recording that subject reduction, quotient syntax equality, host evaluator equality, and unrestricted Church--Rosser remain outside the packet.
\item $H$ records componentwise $\hsame$ transport for $T,L,W,J,B,O$ and the structural rows.
\item $C$ records displayed $\Cont$ replay from a confluence-facing request through $T,L,W,J,B,O,H,P,M$.
\item $P$ is the $\Pkg$ provenance over $\NewmanLemmaUp$, $\ChurchRosserUp$, $\MetacicNormalizationConfluenceBridgeUp$, $\MetaCICNormalizationFrontierUp$, $\MetaCICParallelDiamondFrontierUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $M$ is the local $\NameCert_{\NewmanLemmaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,L,W,J,B,O,H,C,P,M$. There is no coordinate for a global confluence oracle, a subject-reduction discharge, quotient equality of terms, evaluator equality, or a consistency endpoint.
\end{definition}

\begin{theorem}[Newman lemma NameCert obligations]
\label{thm:newman-lemma-namecert-obligations}
For every accepted $\NewmanLemmaUp$ carrier $N=(T,L,W,J,B,O,H,C,P,M)$, the local $\NameCert_{\NewmanLemmaUp}$ surface consists exactly of termination evidence, local-confluence exposure, well-founded descent, local-join budgeting, MetaCIC bridge routing, obstruction retention, componentwise transport, displayed replay, provenance, and local naming through the displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:newman-lemma-carrier}. The rows $T,L,W,J$ expose the traditional Newman ingredients: termination, local confluence, descent, and local joins for the same displayed source.

The bridge row $B$ then fixes the only MetaCIC-facing route. It reads normalization-confluence correspondence data through the existing bridge packet rather than inventing a detached confluence theorem.

Finally use $O,H,C,P,M$. The obstruction row records the missing global claims, while transport, replay, provenance, and local naming preserve the same finite route. Since no further coordinate is present, the NameCert surface cannot supply subject reduction, quotient term equality, evaluator equality, unrestricted Church--Rosser, or kernel consistency.
\end{proof}

\begin{theorem}[Newman local-confluence normalization handoff]
\label{thm:newman-lemma-local-confluence-normalization-handoff}
Every confluence-facing consumer of an accepted $\NewmanLemmaUp$ carrier first reads the finite handoff
$$
T,L \longmapsto W \longmapsto J \longmapsto B \longmapsto O,H,C,P,M .
$$
Thus local confluence is consumed only under the displayed normalization or termination row, and the resulting join budget is handed to the MetaCIC normalization-confluence bridge before any confluence conclusion is visible.
\end{theorem}
\begin{proof}
Start with the rows $T$ and $L$. The local peak row is readable only for the same source whose terminating measure is displayed by $T$.

Next consume $W$ and $J$. The descent ledger orders the finite peak analysis, and the local join budget records the bounded join data produced by that analysis.

The bridge and structural rows then close the handoff route. Row $B$ passes the bounded join budget to the normalization-confluence bridge, while $O,H,C,P,M$ retain the obstruction boundary, replay order, provenance, and local naming.
\end{proof}

\begin{theorem}[Newman MetaCIC route]
\label{thm:newman-lemma-metacic-route}
Any MetaCIC use of $\NewmanLemmaUp$ factors through the sibling route
$$
\ChurchRosserUp,\ \MetacicNormalizationConfluenceBridgeUp,\ \MetaCICNormalizationFrontierUp,\ \MetaCICParallelDiamondFrontierUp .
$$
The Newman packet may therefore serve as a finite local-confluence and normalization handoff, but it does not discharge subject reduction, residual substitution compatibility, full closed confluence, quotient syntax equality, evaluator equality, or kernel consistency.
\end{theorem}
\begin{proof}
Read the provenance row $P$ of \autoref{def:newman-lemma-carrier}. It names the Church--Rosser sibling, the normalization-confluence bridge, the normalization frontier, and the parallel-diamond frontier as the only MetaCIC-facing dependencies.

The analytic rows $T,L,W,J,B$ are then consumed along that provenance route. They can produce a bounded local-confluence handoff under termination, and they can feed the bridge row, but they do not add the missing subject-reduction or residual-substitution premises.

The obstruction and structural rows preserve this boundary. Hence the route remains a finite Newman-style handoff inside the displayed MetaCIC dependency lattice.
\end{proof}

\closureat{NewmanLemmaUp}{seedStr}

\begin{closurestatus}{\NewmanLemmaUp}
  \constructivestory{A $\NewmanLemmaUp$ packet is generated from termination evidence, local-confluence exposure, well-founded descent, local-join budgeting, MetaCIC bridge routing, obstruction retention, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:newman-lemma-carrier}, \autoref{thm:newman-lemma-namecert-obligations}, \autoref{thm:newman-lemma-local-confluence-normalization-handoff}, and \autoref{thm:newman-lemma-metacic-route} seed the finite Newman handoff from terminating local confluence into the MetaCIC normalization-confluence route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close subject reduction, residual substitution compatibility, full closed confluence, quotient syntax equality, evaluator equality, Lean verification, bridge checking, or kernel consistency.}
  \upgradepath{Obligation closure requires checked carrier admission, local-confluence exposure, well-founded descent, local-join budgeting, MetaCIC bridge routing, obstruction retention, transport, replay, provenance, and local naming for BEDC.Derived.NewmanLemmaUp.}
\end{closurestatus}
