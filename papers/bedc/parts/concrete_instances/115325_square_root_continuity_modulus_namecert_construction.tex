\chapter{A Concrete Naming Certificate for $\SquareRootContinuityModulusUp$}
\label{ch:concrete-instances-square-root-continuity-modulus-namecert}
\origin{human}

Following Bishop and Bridges on constructive square-root continuity, the $\SquareRootContinuityModulusUp$ packet records the finite uniform-modulus certificate that lets a bounded nonnegative interval consumer use Heron's square-root route without importing a host square-root function or a completed real continuity theorem. It sits downstream of the value-level square-root certificates in \autoref{ch:concrete-instances-heron-square-root-namecert} and \autoref{ch:concrete-instances-regular-cauchy-square-root-namecert}, and it binds that route to the continuity and compact-interval surfaces of \autoref{ch:concrete-instances-continuous-namecert}, \autoref{ch:concrete-instances-uniformmodulus-namecert}, and \autoref{ch:concrete-instances-closedboundedinterval-namecert}.

\begin{definition}[Square-root continuity-modulus carrier]
\label{def:square-root-continuity-modulus-carrier}
A $\SquareRootContinuityModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(I,F,U,R,H,S,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\ClosedBoundedIntervalUp$ row naming the bounded nonnegative source interval and the rational lower-bound data used by the square-root route.
\item $F$ is the $\ContinuousUp$ row for the square-root read on the displayed interval, with continuity evidence read only through finite regular rational windows.
\item $U$ is the $\UniformModulusUp$ row assigning each requested output tolerance a finite interval-window threshold.
\item $R$ is the $\RegularCauchySquareRootUp$ row giving the regular rational square-root readback available at the chosen threshold.
\item $H$ is the $\HeronSquareRootUp$ row giving the displayed Heron recurrence, reciprocal ledger, contraction window, and Real seal handoff used by the modulus witness.
\item $S$ records componentwise $\hsame$ transport for the interval, continuity row, modulus row, regular square-root row, Heron route, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from a tolerance request through $I,F,U,R,H,S,P,N$.
\item $C$ is the compatibility row saying that the threshold chosen by $U$ is exactly the threshold at which $R$ and $H$ expose the same square-root window on the source interval.
\item $P$ is the $\Pkg$ provenance over $\SquareRootContinuityModulusUp$, $\HeronSquareRootUp$, $\RegularCauchySquareRootUp$, $\ContinuousUp$, $\UniformModulusUp$, $\ClosedBoundedIntervalUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SquareRootContinuityModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,F,U,R,H,S,T,C,P,N$. It has no coordinate for a host square-root operation, a selected completed limit, quotient-real equality, global real completeness, or a pointwise continuity proof that bypasses the displayed uniform-modulus row.
\end{definition}

\begin{theorem}[Square-root continuity-modulus NameCert obligations]
\label{thm:square-root-continuity-modulus-namecert-obligations}
For every accepted $\SquareRootContinuityModulusUp$ carrier
$$
M=(I,F,U,R,H,S,T,C,P,N),
$$
the local $\NameCert_{\SquareRootContinuityModulusUp}$ surface consists exactly of carrier admission for the bounded nonnegative interval, displayed continuity row, uniform-modulus row, regular Cauchy square-root readback, Heron recurrence route, componentwise transport, continuation replay, modulus-route compatibility, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier rows. The interval row $I$ supplies the bounded nonnegative source scope, while $F$ and $U$ record the continuity assertion and the uniform tolerance-to-window service on that scope.

The numerical square-root read is not independent data. The row $R$ provides the regular rational square-root readback, and the row $H$ provides the Heron recurrence route, reciprocal ledger, contraction window, and terminal Real seal handoff used to justify the same finite window.

The structural rows $S,T,C,P,N$ preserve and replay exactly that packet. Since no further carrier coordinate is available, the listed rows exhaust the local NameCert surface, and no host square-root operation or global completeness theorem can replace the displayed modulus certificate.
\end{proof}

\begin{theorem}[Square-root continuity-modulus Heron route]
\label{thm:square-root-continuity-modulus-heron-route}
Every accepted $\SquareRootContinuityModulusUp$ packet factors a uniform square-root tolerance request through the route
$$
I \longmapsto F \longmapsto U \longmapsto R \longmapsto H \longmapsto C .
$$
Thus a consumer first sees the bounded nonnegative interval, the displayed continuity row, the uniform-modulus threshold, the regular Cauchy square-root readback, the Heron recurrence route, and the compatibility row tying the threshold to the same finite square-root window. The route exports no host square-root function, selected completed limit, quotient-real equality, or ambient real completeness principle.
\end{theorem}
\begin{proof}
Start with an accepted carrier $M=(I,F,U,R,H,S,T,C,P,N)$. A tolerance request is replayed through $T$, so the first numerical datum it can read is the interval scope $I$ together with the continuity and modulus rows $F,U$.

The modulus row chooses a finite threshold. At that threshold, $R$ exposes the regular Cauchy square-root readback and $H$ exposes the Heron recurrence window with its reciprocal and contraction ledger.

The compatibility row $C$ identifies these finite windows as the same square-root read on the displayed interval. Transport and provenance through $S,P,N$ preserve the route but add no extra numerical coordinate.

Consequently every uniform square-root tolerance request factors through the stated Heron route, and the packet cannot be read as a host square-root function, a selected limit, quotient-real equality, or an ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\SquareRootContinuityModulusUp$ read of a square-root tolerance request factors through a ClosedBoundedInterval source row, displayed Continuous row, UniformModulus threshold row, RegularCauchySquareRoot readback, HeronSquareRoot recurrence route, componentwise hsame transport, Cont replay, compatibility, Pkg provenance, and local NameCert row.}

\independenceWitness{heron-square-root, regular-cauchy-square-root, continuous, uniform-modulus, closed-bounded-interval: $\SquareRootContinuityModulusUp$ is not bijective to any listed sibling because it jointly binds a bounded nonnegative interval, a displayed continuity row, a uniform tolerance-to-window threshold, regular square-root readback, Heron recurrence data, compatibility, transport, replay, provenance, and local naming.}

\closureat{SquareRootContinuityModulusUp}{seedStr}

\begin{closurestatus}{\SquareRootContinuityModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:square-root-continuity-modulus-carrier}, \autoref{thm:square-root-continuity-modulus-namecert-obligations}, and \autoref{thm:square-root-continuity-modulus-heron-route} seed the finite uniform-modulus certificate connecting Heron's square-root route to bounded nonnegative interval continuity.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a host square-root function, selected completed limits, quotient-real equality, global real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, interval nonnegativity, continuity-row exactness, uniform-modulus threshold exactness, regular Cauchy readback, Heron route compatibility, transport, replay, provenance, and local naming for BEDC.Derived.SquareRootContinuityModulusUp.}
  \constructivestory{A $\SquareRootContinuityModulusUp$ packet is generated from a bounded nonnegative ClosedBoundedInterval row, displayed Continuous and UniformModulus rows, RegularCauchySquareRoot readback, HeronSquareRoot recurrence route, componentwise hsame transport, Cont replay, compatibility, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
