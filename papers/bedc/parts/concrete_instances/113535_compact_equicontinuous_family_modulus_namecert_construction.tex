\chapter{A Concrete Naming Certificate for $\CompactEquicontinuousFamilyModulusUp$}
\label{ch:concrete-instances-compact-equicontinuous-family-modulus-namecert}
\origin{human}

Following Arzela-Ascoli and Bishop--Bridges, the $\CompactEquicontinuousFamilyModulusUp$ packet records the finite BEDC route that turns a compact metric source and an equicontinuous family of continuous maps into one displayed uniform budget for the whole family. It sits over the compact finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the family surface of \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, the precision consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite-window row of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite-net modulus route; it does not claim open-cover compactness, a selected global modulus outside the carrier, or a choice principle over the family.

\begin{definition}[Compact equicontinuous family modulus carrier]
\label{def:compact-equicontinuous-family-modulus-carrier}
A $\CompactEquicontinuousFamilyModulusUp$ carrier is a finite $\BHist$ packet
$$
\mathcal{E}=(X,F,M,U,W,R,S,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ source row, including its finite-net schedules and tolerance coverage data.
\item $F$ is the $\EquicontinuousFamilyUp$ row, displaying the common pointwise budget for the family over the compact source.
\item $M$ is the $\ContinuousMapUp$ family-member row used when a particular displayed map is inspected.
\item $U$ is the $\UniformModulusUp$ output row that receives the compact-family budget.
\item $W$ is the $\StreamNameUp$ finite-window row through which the source and target observations are scheduled.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational precision histories attached to the windows.
\item $S$ is the terminal $\RealUp$ seal reached only after the finite-net, family, map, window, and readback rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for $X,F,M,U,W,R,S,C,P,N$.
\item $C$ records displayed $\Cont$ replay of the same finite-net route for downstream consumers.
\item $P$ is the $\Pkg$ provenance over $\CompactEquicontinuousFamilyModulusUp$, $\CompactMetricUp$, $\EquicontinuousFamilyUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactEquicontinuousFamilyModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. There is no coordinate for open-cover compactness, host function-space compactness, selected subnet, quotient equality of maps, or a modulus chosen outside the finite packet.
\end{definition}

\begin{theorem}[Compact equicontinuous family modulus NameCert obligations]
\label{thm:compact-equicontinuous-family-modulus-namecert-obligations}
For every accepted $\CompactEquicontinuousFamilyModulusUp$ carrier
$$
\mathcal{E}=(X,F,M,U,W,R,S,H,C,P,N),
$$
the local $\NameCert_{\CompactEquicontinuousFamilyModulusUp}$ surface consists exactly of compact finite-net admission through $X$, equicontinuous-family admission through $F$, displayed family-member inspection through $M$, uniform-budget output through $U$, finite-window and regular-rational readback through $W,R$, terminal Real sealing through $S$, componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-equicontinuous-family-modulus-carrier}. Its visible coordinates list exactly the compact source, equicontinuity family, inspected continuous map, uniform-modulus output, finite windows, regular rational readback, Real seal, transport, replay, provenance, and local naming rows.

The compact and family clauses are read through $X$ and $F$. The finite-net schedule belongs to the compact metric row, while the common family budget belongs to the equicontinuity row; neither clause exposes an open cover, a selected subnet, or a host function-space object.

The output and seal clauses are then read through $M,U,W,R,S$ and preserved by $H,C,P,N$. Since no other coordinate is present, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact equicontinuous family uniform budget route]
\label{thm:compact-equicontinuous-family-modulus-uniform-budget-route}
Every uniform-budget consumer of an accepted $\CompactEquicontinuousFamilyModulusUp$ carrier factors through the finite route
$$
\CompactMetricUp \longmapsto \EquicontinuousFamilyUp \longmapsto \ContinuousMapUp \longmapsto \StreamNameUp,\RegSeqRatUp \longmapsto \UniformModulusUp \longmapsto \RealUp .
$$
Concretely, the compact finite-net row $X$ and the family row $F$ are public before any member row $M$ or output row $U$ is read, and the terminal seal $S$ is public only after the window and readback rows $W,R$ have been replayed.
\end{theorem}
\begin{proof}
Start with the carrier row $\mathcal{E}=(X,F,M,U,W,R,S,H,C,P,N)$. The source-side information is exactly $X$, and the common family budget is exactly $F$.

A consumer inspecting a displayed family member then reads $M$ over the same compact source and family budget. The finite-window row $W$ schedules the visible source and target observations, while $R$ supplies the regular rational readback for the precision history used by the output row.

Finally $U$ packages the uniform budget and $S$ seals the Real-facing read. The rows $H,C,P,N$ transport, replay, source, and name the same route, so no open-cover compactness theorem, selected global modulus, quotient map equality, or choice-selected family element can replace the displayed finite-net route.
\end{proof}

\begin{theorem}[Compact equicontinuous family finite-net nonescape]
\label{thm:compact-equicontinuous-family-modulus-finite-net-nonescape}
The $\CompactEquicontinuousFamilyModulusUp$ packet exports no modulus data outside the compact finite-net, equicontinuity-family, continuous-map, finite-window, regular-rational, and Real-seal rows
$$
X,F,M,W,R,S .
$$
Every downstream $\UniformModulusUp$ read is therefore a finite BEDC read over those rows together with $H,C,P,N$, not a host Arzela--Ascoli compactness argument, an open-cover proof, a selected subnet, or an external modulus selector.
\end{theorem}
\begin{proof}
Use \autoref{thm:compact-equicontinuous-family-modulus-namecert-obligations} to identify the carrier surface. The only rows that can supply the budget are $X,F,M,W,R,S$ together with the structural rows $H,C,P,N$.

Apply \autoref{thm:compact-equicontinuous-family-modulus-uniform-budget-route}. It orders the compact source, family budget, inspected map, finite windows, readback, output, and seal before any downstream consumer can read a uniform modulus.

Because the carrier of \autoref{def:compact-equicontinuous-family-modulus-carrier} contains no host compactness, selected subnet, quotient equality, or external selector coordinate, the finite-net route is nonescaping.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactEquicontinuousFamilyModulusUp$ budget read factors through one $\CompactMetricUp$ source row, one $\EquicontinuousFamilyUp$ family row, one $\ContinuousMapUp$ member row, one $\StreamNameUp$ finite-window row, one $\RegSeqRatUp$ readback row, one $\UniformModulusUp$ output row, one terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{compactmetric, continuousmap, equicontinuityfamily, uniformmodulus, streamname, regseqrat, real: $\CompactEquicontinuousFamilyModulusUp$ is not bijective to any listed sibling because it jointly carries compact finite-net data, equicontinuity-family data, inspected map data, precision windows, rational readback, uniform output, Real sealing, transport, replay, provenance, and local NameCert rows as one packet.}

\closureat{CompactEquicontinuousFamilyModulusUp}{seedStr}

\begin{closurestatus}{\CompactEquicontinuousFamilyModulusUp}
  \constructivestory{A $\CompactEquicontinuousFamilyModulusUp$ packet is generated from a CompactMetricUp finite-net source, an EquicontinuousFamilyUp common-budget row, a ContinuousMapUp inspected-member row, StreamNameUp windows, RegSeqRatUp readback, a UniformModulusUp output row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-equicontinuous-family-modulus-carrier}, \autoref{thm:compact-equicontinuous-family-modulus-namecert-obligations}, and \autoref{thm:compact-equicontinuous-family-modulus-uniform-budget-route} seed the compact-family uniform-budget route over finite-net, family, map, window, readback, output, and seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, host function-space compactness, selected subnets, quotient map equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net coverage, family-budget stability, member inspection, window and readback exactness, uniform-output exactness, Real-seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
