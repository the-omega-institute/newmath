\chapter{A Concrete Naming Certificate for $\UniformConvergenceTheoremUp$}
\label{ch:concrete-instances-uniform-convergence-theorem-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\UniformConvergenceTheoremUp$ packet records the finite certificate surface for the standard theorem that a uniformly convergent family of continuous displayed maps has a continuous Real-facing limit read. It sits after the continuous-map source of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-limit endpoint route of \autoref{ch:concrete-instances-uniformlimit-namecert}, the shared uniform-Cauchy family row of \autoref{ch:concrete-instances-uniform-cauchy-family-namecert}, the finite stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet exposes the theorem as a finite BEDC handoff; it does not assert ambient $\RealUp$ completeness, host equality of functions, quotient equality of maps, countable choice, trichotomy, or a selected external limit.

\begin{definition}[Uniform convergence theorem carrier]
\label{def:uniform-convergence-theorem-carrier}
A $\UniformConvergenceTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(D,F,U,L,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the source-domain row inspected by the continuous maps and by the limit read.
\item $F$ is the finite family row of $\ContinuousMapUp$ carriers, each read through the same source-domain surface.
\item $U$ is the $\UniformCauchyFamilyUp$ or $\UniformLimitUp$ ledger row that supplies one displayed uniform tail discipline over the family row.
\item $L$ is the limiting graph row whose endpoint reads are admitted only after $F$ and $U$ have been displayed.
\item $W$ is the $\StreamNameUp$ finite-window row for source points, family indices, continuity-modulus requests, and value names.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the value windows and their rational tolerance budgets.
\item $E$ is the terminal $\RealUp$ seal row reached only after the uniform ledger, stream windows, and regular rational readback have been consumed.
\item $H$ records componentwise $\hsame$ transport for source rows, continuous-map rows, uniform-tail rows, limit graph rows, windows, readbacks, Real seals, provenance, and local names.
\item $C$ records displayed $\Cont$ replay from source admission through continuity inspection, uniform-tail inspection, finite-window readback, graph comparison, and Real-seal handoff.
\item $P$ is the $\Pkg$ provenance over $\UniformConvergenceTheoremUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\UniformCauchyFamilyUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformConvergenceTheoremUp}$ row.
\end{enumerate}
The classifier relates accepted packets only by the displayed componentwise rows $D,F,U,L,W,R,E,H,C,P,N$. It has no coordinate for host function equality, quotient map equality, a selected limit outside the packet, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Uniform convergence theorem NameCert obligations]
\label{thm:uniform-convergence-theorem-namecert-obligations}
For every accepted $\UniformConvergenceTheoremUp$ carrier
$$
T=(D,F,U,L,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\UniformConvergenceTheoremUp}$ surface consists exactly of carrier admission for the displayed rows, continuous-source compatibility for $D$ and $F$, uniform-tail exactness through $U$, limit-graph admission through $L$, finite-window readback through $W$ and $R$, Real-seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. No obligation row supplies host equality of functions, quotient equality of maps, a selected external limit, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-convergence-theorem-carrier}. The visible rows are the source-domain row, the continuous-map family row, the uniform-tail row, the limiting graph row, the stream-window row, the regular rational readback, the Real seal, transport, replay, provenance, and local naming.

Continuous-source compatibility is read from $D$ and $F$. Each family member is a displayed $\ContinuousMapUp$ row over the same source-domain surface, so a downstream continuity consumer has no separate host function space from which to read equality.

Uniform-tail exactness is read from $U$. Whether the row is consumed through the $\UniformCauchyFamilyUp$ surface or through the endpoint-facing $\UniformLimitUp$ surface, the same finite ledger supplies the shared threshold discipline for every displayed source window.

The limit graph and endpoint read are downstream. The row $L$ is admitted only after the continuous family and the uniform ledger have been exposed, while $W,R,E$ turn the scheduled value windows into regular rational data and then into the terminal Real seal.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite route. Since the excluded principles are absent from the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Uniform convergence theorem Real completion handoff]
\label{thm:uniform-convergence-theorem-real-completion-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\UniformConvergenceTheoremUp$ packet factors through the finite route
$$
\ContinuousMapUp,\ \UniformCauchyFamilyUp,\ \UniformLimitUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
The consumer first reads the displayed continuous-map family over the common source row, then the shared uniform-tail ledger, then the finite stream windows and regular rational readbacks, and only then the terminal Real seal. The handoff exports no ambient completeness theorem, no host equality of functions, no quotient equality of maps, no countable-choice schedule, no trichotomy principle, and no selected external limit.
\end{theorem}
\begin{proof}
Begin with a completion-facing consumer asking for the limiting endpoint. The packet admits such a read only through the displayed source and continuous-map rows $D,F$, so the consumer must inspect the same finite source surface used by the family.

Next use the uniform ledger $U$. It binds all requested source windows to one displayed tail discipline, matching the uniform-limit endpoint route of \autoref{ch:concrete-instances-uniformlimit-namecert} and the shared-family route of \autoref{ch:concrete-instances-uniform-cauchy-family-namecert}.

The limiting graph row $L$ then records the theorem handoff: continuity data from the family and the uniform tail data are read before any endpoint row is visible. The stream windows $W$ and regular rational row $R$ provide the only value readback accepted by the packet.

Finally the Real seal $E$ is reached through the same transport, replay, provenance, and local naming rows $H,C,P,N$. Because no refused object is a coordinate of the carrier, every Real or completion-facing read remains inside this finite route.
\end{proof}

\falsifiablePrediction{Every accepted UniformConvergenceTheoremUp read exposes a source-domain row, a ContinuousMapUp family row, a UniformCauchyFamilyUp or UniformLimitUp tail ledger, a limiting graph row, StreamNameUp finite windows, RegSeqRatUp readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert before a completion-facing consumer can inspect the endpoint.}

\independenceWitness{continuousmap, uniformlimit, uniform-cauchy-family, streamname, regseqrat, real: $\UniformConvergenceTheoremUp$ is not bijective to any listed sibling because it jointly carries continuous-map source compatibility, shared uniform-tail exactness, limiting graph admission, finite-window readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{UniformConvergenceTheoremUp}{seedStr}

\begin{closurestatus}{\UniformConvergenceTheoremUp}
  \constructivestory{A $\UniformConvergenceTheoremUp$ packet is generated from a common source-domain row, ContinuousMap family rows, a UniformCauchyFamily or UniformLimit tail ledger, a limiting graph row, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-convergence-theorem-carrier}, \autoref{thm:uniform-convergence-theorem-namecert-obligations}, and \autoref{thm:uniform-convergence-theorem-real-completion-handoff} seed the finite theorem route from displayed continuous-map families and uniform-tail ledgers to Real-facing completion reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient Real completeness, host function equality, quotient map equality, selected limits, countable choice, trichotomy, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires a Lean declaration for BEDC.Derived.UniformConvergenceTheoremUp covering carrier admission, continuous-source compatibility, uniform-tail exactness, limit-graph admission, readback, Real-seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
