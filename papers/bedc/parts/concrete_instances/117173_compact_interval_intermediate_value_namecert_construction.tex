\chapter{A Concrete Naming Certificate for $\CompactIntervalIntermediateValueUp$}
\label{ch:concrete-instances-compact-interval-intermediate-value-namecert}
\origin{human}

Following Bishop and Bridges, the $\CompactIntervalIntermediateValueUp$ packet records the constructive intermediate-value bisection route over a closed real interval as finite NameCert data. It consumes the compact interval finite-net surface of \autoref{ch:concrete-instances-closed-bounded-interval-compact-namecert}, the dyadic sign-change route of \autoref{ch:concrete-instances-dyadic-intermediate-value-namecert}, the finite sign ledger of \autoref{ch:concrete-instances-intermediate-value-bisection-namecert}, the located real interval bisection route of \autoref{ch:concrete-instances-located-real-interval-bisection-namecert}, the displayed graph surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names a finite bisection certificate; it does not assert classical Heine--Borel compactness, selected roots, quotient equality of real names, real trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Compact interval intermediate-value carrier]
\label{def:compact-interval-intermediate-value-carrier}
A $\CompactIntervalIntermediateValueUp$ carrier is a finite $\BHist$ packet
$$
V=(I,F,B,D,R,E,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $I$ is the closed bounded interval source row, read through $\ClosedBoundedIntervalCompactUp$ and located endpoint data;
\item $F$ is the $\ContinuousMapUp$ graph row, exposing only finite graph windows and a displayed continuity budget;
\item $B$ is the sign-bracket and bisection row, consuming $\IntermediateValueBisectionUp$ and $\LocatedRealIntervalBisectionUp$ branch data;
\item $D$ is the dyadic tolerance row, recording the $\DyadicRatCoreUp$ mesh and sign-read budget;
\item $R$ is the $\RegSeqRatUp$ readback row for the retained midpoint and interval-window schedule;
\item $E$ is the terminal $\RealUp$ seal for the displayed limiting name;
\item $H$ records componentwise $\hsame$ transport of $I,F,B,D,R,E$;
\item $C$ records displayed $\Cont$ replay of the same bisection route;
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalIntermediateValueUp$, $\ClosedBoundedIntervalCompactUp$, $\DyadicIntermediateValueUp$, $\IntermediateValueBisectionUp$, $\LocatedRealIntervalBisectionUp$, $\ContinuousMapUp$, $\RegSeqRatUp$, $\RealUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CompactIntervalIntermediateValueUp}$ row.
\end{enumerate}
The carrier has no coordinate for an open-cover compactness principle, a selected root, quotient real equality, excluded middle over signs of arbitrary reals, or an ambient completeness theorem.
\end{definition}
\leanstmt{BEDC.Derived.CompactIntervalIntermediateValueUp}

\begin{theorem}[Compact interval intermediate-value NameCert obligations]
\label{thm:compact-interval-intermediate-value-namecert-obligations}
For the carrier of \autoref{def:compact-interval-intermediate-value-carrier}, the local $\NameCert_{\CompactIntervalIntermediateValueUp}$ surface consists exactly of carrier admission for $V=(I,F,B,D,R,E,H,C,P,N)$, interval-source admission through $I$, continuous graph admission through $F$, bracket stability through $B$, dyadic descent and tolerance control through $D$, regular rational readback through $R$, $\RealUp$ seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the finite packet. The interval row $I$ supplies only the closed bounded interval and its finite compactness-facing data, while the graph row $F$ supplies the displayed continuity budget needed to interpret finite sign reads.

Read the bisection rows next. The sign-bracket row $B$ keeps the lower and upper sign entries attached to the retained subinterval, and the dyadic row $D$ records the mesh, midpoint, and tolerance descent used by the branch ledger. These rows are finite and do not select an unbounded branch outside the displayed schedule.

The readback and seal rows then close the real-facing surface. Row $R$ turns the retained windows into a $\RegSeqRatUp$ schedule, and row $E$ seals only the resulting displayed real name. Finally $H,C,P,N$ transport, replay, source, and name the same packet, so no excluded host principle can be introduced after the finite bisection route has been named.
\end{proof}

\falsifiablePrediction{Every accepted compact-interval intermediate-value read must display a closed interval source, continuous graph row, sign-bracket bisection row, dyadic tolerance row, regular rational readback, Real seal, transport, replay, provenance, and local NameCert evidence before any zero-facing real name is exported.}

\independenceWitness{closed-bounded-interval-compact, dyadic-intermediate-value, intermediate-value-bisection, located-real-interval-bisection, continuousmap, regseqrat, real, dyadicratcore: $\CompactIntervalIntermediateValueUp$ is not bijective to any listed sibling because it jointly carries interval source, graph, bracket, dyadic descent, readback, seal, transport, replay, provenance, and local naming rows.}

\closureat{CompactIntervalIntermediateValueUp}{seedStr}

\begin{closurestatus}{\CompactIntervalIntermediateValueUp}
  \constructivestory{A $\CompactIntervalIntermediateValueUp$ packet is generated from a closed interval source, continuous graph row, sign-bracket bisection row, dyadic tolerance row, regular rational readback, Real seal, componentwise transport, displayed continuation replay, provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-intermediate-value-carrier} and \autoref{thm:compact-interval-intermediate-value-namecert-obligations} seed the finite compact-interval bisection certificate.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.CompactIntervalIntermediateValueUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close classical Heine--Borel compactness, selected roots, quotient equality of real names, real trichotomy, ambient $\RealUp$ completeness, or theorem-level Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, bracket stability, bisection descent, dyadic readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactIntervalIntermediateValueUp.}
\end{closurestatus}
