\chapter{A Concrete Naming Certificate for $\CauchyRealArchimedeanUp$}
\label{ch:concrete-instances-cauchy-real-archimedean-namecert}
\origin{human}

Following Bishop and Bridges, Constructive Analysis, the $\CauchyRealArchimedeanUp$ packet records the finite Archimedean witness consumed by L10 Cauchy-real readers before an order, located-comparison, or completion-facing consumer opens a real-name seal. It sits over the regular rational source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic scale rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, the Cauchy-real presentation route of \autoref{ch:concrete-instances-cauchy-real-presentation-namecert}, and the rational-bound witness surface of \autoref{ch:concrete-instances-archimedeanreal-namecert}. The packet names a displayed finite bound route; it is not a host least-bound principle, not quotient equality of real names, not a choice-selected integer part, and not ambient real completeness.

\begin{definition}[CauchyRealArchimedean carrier]
\label{def:cauchy-real-archimedean-carrier}
A $\CauchyRealArchimedeanUp$ carrier is a finite $\BHist$ packet
$$
A=(G,D,E,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the $\RegSeqRatUp$ source row whose finite regular-rational observations are the only Cauchy-real reads admitted by the packet.
\item $D$ is the $\DyadicRatCoreUp$ scale row recording the dyadic tolerance, comparison budget, and rational arithmetic used to inspect $G$.
\item $E$ is the $\RealUp$ seal row reached after the displayed route through $G$ and $D$.
\item $W$ is the Archimedean bound witness row, read as the rational or dyadic bound surface supplied by \autoref{ch:concrete-instances-archimedeanreal-namecert} for the sealed Cauchy-real name.
\item $H$ records componentwise $\hsame$ transport for $G,D,E,W,C,P,N$.
\item $C$ records displayed $\Cont$ replay from regular-rational windows through dyadic scale inspection, the terminal real seal, and the Archimedean witness row:
$$
G \longmapsto D \longmapsto E \longmapsto W .
$$
\item $P$ is the $\Pkg$ provenance over $\CauchyRealArchimedeanUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchyRealPresentationUp$, $\ArchimedeanRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyRealArchimedeanUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $G,D,E,W,H,C,P,N$. It has no coordinate for a host supremum, a selected least bound, quotient stream equality, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CauchyRealArchimedean NameCert obligations]
\label{thm:cauchy-real-archimedean-namecert-obligations}
For every accepted $\CauchyRealArchimedeanUp$ carrier $A=(G,D,E,W,H,C,P,N)$, the local $\NameCert_{\CauchyRealArchimedeanUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed regular-rational source, dyadic scale, real seal, Archimedean witness, transport, replay, provenance, and local naming rows;
\item classifier transport, requiring comparisons of accepted packets to inspect matching $G,D,E,W$ rows through the componentwise $\hsame$ data in $H$;
\item dyadic-scale exactness, requiring every bound or comparison request to pass through the displayed scale row $D$ before the real seal $E$ is consumed;
\item Archimedean witness exposure, requiring the bound witness $W$ to be read after the same $G,D,E$ route and before any order or located-comparison consumer uses it;
\item continuation, provenance, and naming stability through $C,P,N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:cauchy-real-archimedean-carrier}. The regular-rational observations, dyadic scale data, real-name seal, bound witness, transport, replay, provenance, and local name are the only displayed coordinates.

The analytic route is supplied by the sibling Cauchy-real chapters. The $\RealUp$ seal is read through \autoref{ch:concrete-instances-real-namecert}, the regular rational source through \autoref{ch:concrete-instances-regseqrat-namecert}, the scale row through \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the presentation face through \autoref{ch:concrete-instances-cauchy-real-presentation-namecert}.

The Archimedean part is then the displayed witness row. Apply the rational-bound surface of \autoref{ch:concrete-instances-archimedeanreal-namecert}: it supplies a finite bound route for the same real-name seal, while this carrier records that route as $W$ downstream of $G,D,E$.

Finally, $H,C,P,N$ transport, replay, source, and name the same packet. Since no field stores a host supremum, selected least bound, quotient stream equality, countable-choice schedule, host equality row, or ambient completeness object, the listed obligations exhaust the local NameCert surface.
\end{proof}

\closureat{CauchyRealArchimedeanUp}{seedStr}

\begin{closurestatus}{\CauchyRealArchimedeanUp}
  \constructivestory{A $\CauchyRealArchimedeanUp$ packet is generated from displayed $\RegSeqRatUp$ source rows, $\DyadicRatCoreUp$ scale rows, a terminal $\RealUp$ seal row, an Archimedean bound witness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-real-archimedean-carrier} and \autoref{thm:cauchy-real-archimedean-namecert-obligations} seed the finite Archimedean witness route for Cauchy-real names over RegSeqRat, DyadicRatCore, Real, CauchyRealPresentation, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected least bounds, integer-part selection, countable choice, host real equality, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, dyadic-scale exactness, Archimedean witness exposure, replay stability, provenance stability, and local naming for the same displayed packet.}
\end{closurestatus}
