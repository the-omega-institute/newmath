\chapter{A Concrete Naming Certificate for $\CompactUniformSequenceModulusUp$}
\label{ch:concrete-instances-compact-uniform-sequence-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\CompactUniformSequenceModulusUp$ packet records the compact sequence-modulus route behind \autoref{ch:visions-compact-uniform-continuity} as finite NameCert data. It sits over the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map row of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Compact uniform sequence-modulus carrier]
\label{def:compact-uniform-sequence-modulus-carrier}
A $\CompactUniformSequenceModulusUp$ carrier is a finite $\BHist$ packet
$$
K=(X,F,U,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source $\CompactMetricUp$ row, including the finite compact windows used by the packet.
\item $F$ is the $\ContinuousMapUp$ sequence row whose pointwise modulus data are inspected on the compact source.
\item $U$ is the $\UniformModulusUp$ output row, exposed only after the compact windows and pointwise rows have been read.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the requested precision and radius comparisons.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the inspected sequence windows.
\item $E$ is the terminal $\RealUp$ seal for the resulting real-valued estimates.
\item $H$ records componentwise $\hsame$ transport for compact source, continuous-map sequence, uniform modulus, dyadic tolerance, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
X,F,D,R \longmapsto U \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactUniformSequenceModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformSequenceModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed compact windows, pointwise sequence-modulus rows, dyadic tolerance ledger, regular readback, uniform-modulus handoff, Real seal, and structural rows. It has no coordinate for open-cover compactness, host-selected subcovers, semantic continuity, countable choice, quotient function equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform sequence-modulus NameCert obligations]
\label{thm:compact-uniform-sequence-modulus-namecert-obligations}
For every accepted $\CompactUniformSequenceModulusUp$ carrier $K=(X,F,U,D,R,E,H,C,P,N)$, the local $\NameCert_{\CompactUniformSequenceModulusUp}$ surface consists exactly of carrier admission for the displayed compact metric source, continuous-map sequence row, uniform-modulus output, dyadic tolerance ledger, regular rational readback, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every compact-uniform consumer must read the compact window and pointwise sequence-modulus rows before the uniform-modulus row is exposed; the route exports no open-cover compactness theorem, selected subcover, semantic continuity principle, quotient function equality, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-sequence-modulus-carrier}. The compact source $X$ supplies finite compact windows, and the continuous-map sequence row $F$ supplies the pointwise modulus observations inspected on those windows.

The modulus handoff is ordered by the carrier. The dyadic tolerance row $D$ and regular readback row $R$ are read before the $\UniformModulusUp$ output $U$, and the terminal Real seal $E$ is downstream of that same route.

Transport and replay preserve the compact-uniform surface. The row $H$ moves each coordinate componentwise, and $C$ repeats the inspection route for downstream consumers without adding a second source of compactness or continuity.

Finally, $P$ and $N$ record provenance and local naming. Since the carrier contains no open-cover compactness theorem, selected subcover, semantic continuity principle, quotient function equality, countable-choice schedule, or ambient $\RealUp$ completeness row, the listed obligations exhaust the local NameCert surface.
\end{proof}

\closureat{CompactUniformSequenceModulusUp}{seedStr}

\begin{closurestatus}{\CompactUniformSequenceModulusUp}
  \constructivestory{A $\CompactUniformSequenceModulusUp$ packet is generated from CompactMetric source windows, ContinuousMap sequence rows, UniformModulus output rows, DyadicRatCore tolerance ledgers, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-sequence-modulus-carrier} and \autoref{thm:compact-uniform-sequence-modulus-namecert-obligations} seed the finite compact sequence-modulus packet over CompactMetric, ContinuousMap, UniformModulus, DyadicRatCore, RegSeqRat, Real, hsame, Cont, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, semantic continuity, quotient function equality, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-window exposure, pointwise sequence-modulus visibility, dyadic tolerance readback, uniform-modulus handoff, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformSequenceModulusUp.}
\end{closurestatus}
