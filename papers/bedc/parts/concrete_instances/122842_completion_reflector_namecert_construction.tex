\chapter{A Concrete Naming Certificate for $\CompletionReflectorUp$}
\label{ch:concrete-instances-completion-reflector-namecert}
\origin{human}

Following Bourbaki and Mac Lane, the $\CompletionReflectorUp$ packet records the reflector surface by which a displayed source carrier is routed to a completed carrier through unit, extension, universal comparison, separatedness, transport, replay, provenance, and NameCert rows. It abstracts the metric route of \autoref{ch:concrete-instances-metric-completion-reflector-namecert}, the regular Cauchy route of \autoref{ch:concrete-instances-regular-cauchy-completion-reflector-namecert}, and the uniform route of \autoref{ch:concrete-instances-uniform-completion-reflector-namecert} into one L10 boundary. The packet names only finite BEDC data; it does not import quotient completion equality, a selected limit, ambient completeness, or an external reflector theorem.

\begin{definition}[Completion-reflector carrier]
\label{def:completion-reflector-carrier}
A $\CompletionReflectorUp$ carrier is a finite $\BHist$ packet
$$
R=(S,T,U,E,Q,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source-carrier row, recording the object whose completion request is visible to downstream consumers.
\item $T$ is the completed-carrier row, recording the displayed completion target without identifying it with any host quotient.
\item $U$ is the unit row from the source carrier into the completed carrier.
\item $E$ is the extension row, recording how an accepted compatible map is replayed across the completed target.
\item $Q$ is the universal-comparison row, exposing the reflector comparison as finite request-and-response data.
\item $A$ is the separatedness row, recording the boundary at which the completed target may be read by separated or Real-facing consumers.
\item $H$ records componentwise $\hsame$ transport for source, completed target, unit, extension, universal comparison, separatedness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion-reflector request through
$$
S,T,U,E,Q,A,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CompletionReflectorUp$, the cited completion-reflector siblings, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompletionReflectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,T,U,E,Q,A,H,C,P,N$. It has no coordinate for quotient equality of completions, selected Cauchy representatives, chosen limits, ambient completeness, or host-level reflector equality.
\end{definition}

\begin{theorem}[Completion-reflector NameCert obligations]
\label{thm:completion-reflector-namecert-obligations}
For every accepted $\CompletionReflectorUp$ carrier
$$
R=(S,T,U,E,Q,A,H,C,P,N),
$$
the local $\NameCert_{\CompletionReflectorUp}$ surface consists exactly of source-carrier exposure through $S$, completed-carrier exposure through $T$, unit exposure through $U$, extension replay through $E$, universal comparison through $Q$, separatedness through $A$, componentwise transport through $H$, displayed continuation through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:completion-reflector-carrier}. The displayed tuple lists the source, completed target, unit, extension, universal comparison, separatedness, transport, replay, provenance, and naming rows, so every public read of the packet first exposes those finite coordinates.

The unit row $U$ and extension row $E$ give the constructive arrow data. The comparison row $Q$ is downstream of those rows, and the separatedness row $A$ records the boundary required by separated or Real-facing reflector consumers.

The structural rows $H,C,P,N$ preserve the same route under $\hsame$, replay it through $\Cont$, source it through $\Pkg$, and name it by $\NameCert$. Since no quotient equality, selected representative, selected limit, ambient completeness theorem, or host reflector equality appears as a coordinate, no such object belongs to the local obligation surface.
\end{proof}

\begin{theorem}[Completion-reflector L10 route]
\label{thm:completion-reflector-l10-route}
Every metric, regular Cauchy, uniform, separated, or Real-facing consumer of an accepted $\CompletionReflectorUp$ packet must factor its reflector read through the finite route
$$
S \longmapsto U \longmapsto T \longmapsto E \longmapsto Q \longmapsto A \longmapsto H,C,P,N .
$$
The route supplies the common L10 reflector surface consumed by completion-reflector specializations. It supplies no quotient completion, selected limit, ambient completeness theorem, or host equality between completed objects.
\end{theorem}
\begin{proof}
Apply \autoref{thm:completion-reflector-namecert-obligations}. It places the source row $S$, unit row $U$, completed target row $T$, extension row $E$, universal-comparison row $Q$, and separatedness row $A$ before any structural read is available.

The sibling reflector chapters instantiate this same order with metric, regular Cauchy, or uniform source data. Their consumers may refine the rows, but they still read the common unit, extension, comparison, separatedness, transport, replay, provenance, and local NameCert boundary named here.

Finally, the absence clauses in the carrier prevent a consumer from extracting quotient completion equality, selected representatives, chosen limits, ambient completeness, or host reflector equality from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\CompletionReflectorUp$ read displays source, completed-target, unit, extension, universal-comparison, separatedness, hsame, Cont, Pkg, and NameCert rows before any completion-facing output is accepted.}

\independenceWitness{metric-completion-reflector, regular-cauchy-completion-reflector, uniform-completion-reflector: $\CompletionReflectorUp$ is not bijective to any listed sibling because it binds the common source-to-completion reflector route across unit, extension, comparison, separatedness, transport, replay, provenance, and local naming rows.}

\closureat{CompletionReflectorUp}{seedStr}

\begin{closurestatus}{\CompletionReflectorUp}
  \constructivestory{A $\CompletionReflectorUp$ packet is generated from source carrier, completed target, unit, extension, universal comparison, separatedness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:completion-reflector-carrier}, \autoref{thm:completion-reflector-namecert-obligations}, and \autoref{thm:completion-reflector-l10-route} seed the common L10 reflector route consumed by metric, regular Cauchy, uniform, separated, and Real-facing completion chapters.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected representatives, selected limits, ambient completeness, host reflector equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source and completed-target exposure, unit exactness, extension replay, universal comparison, separatedness, transport, replay, provenance, and local naming for BEDC.Derived.CompletionReflectorUp.}
\end{closurestatus}
