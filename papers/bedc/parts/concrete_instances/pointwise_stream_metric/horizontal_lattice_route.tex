The $\PointwiseStreamMetricUp$ packet sits horizontally between the completion route of \autoref{ch:concrete-instances-metriccompletion-namecert} and the convergence-budget route of \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}. MetricCompletion consumers read pointwise distance through the certified stream-name rows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the displayed metric carrier rows of \autoref{ch:concrete-instances-metric-namecert}. RealModulusOfConvergence consumers read the same finite window and dyadic ledger as a convergence budget before any pointwise stream metric row is available. The route exports no host function equality, quotient stream equality, selected representative, countable-choice schedule, or ambient completeness principle.
