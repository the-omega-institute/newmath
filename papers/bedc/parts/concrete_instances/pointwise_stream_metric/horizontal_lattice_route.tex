The $\PointwiseStreamMetricUp$ packet reads pointwise distance windows only after the certified stream-name rows of \autoref{ch:concrete-instances-streamname-namecert}, the displayed metric carrier rows of \autoref{ch:concrete-instances-metric-namecert}, the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert} have been displayed. This horizontal lattice route keeps $\StreamNameUp$, $\MetricSpaceUp$, $\RegSeqRatUp$, and $\RealUp$ visible before any completion or modulus consumer inspects the pointwise stream metric row; it exports no host function equality, quotient stream equality, selected representative, countable-choice schedule, or ambient completeness principle.
