\begin{theorem}[PointwiseStreamMetric L10 dependency route]
\label{thm:pointwise-stream-metric-l10-route}
Every completion-facing $\PointwiseStreamMetricUp$ read consumes the certified $\StreamNameUp$ pointwise windows of \autoref{ch:concrete-instances-streamname-namecert}, the displayed $\MetricUp$ distance rows of \autoref{ch:concrete-instances-metric-namecert}, the $\RealUniformStructureUp$ entourage reads of \autoref{ch:concrete-instances-real-uniform-structure-namecert}, and the terminal $\RealUp$ seal boundary of \autoref{ch:concrete-instances-real-namecert} before it can expose a pointwise metric packet to a downstream Real or completion consumer.
\end{theorem}
\begin{proof}
Start with the certified stream source and finite pointwise windows in the carrier of \autoref{def:pointwise-stream-metric-carrier}. These rows force every read to pass through $\StreamNameUp$ before any distance datum is visible.

Next read the metric row and the Real-uniform entourage row. The sibling chapters cited above provide the displayed distance and entourage surfaces used by the pointwise packet; no host function equality, quotient stream equality, selected representative, or countable-choice schedule is available in the carrier.

Finally apply the Real-completion anchor theorem \autoref{thm:pointwise-stream-metric-real-completion-anchor}. It keeps the route inside the already displayed stream windows, metric rows, entourage reads, and terminal Real seal, so the L10 dependency route is exactly the concrete sibling route stated above.
\end{proof}
