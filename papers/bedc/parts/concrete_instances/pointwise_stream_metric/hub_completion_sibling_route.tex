\begin{theorem}[Pointwise stream metric hub completion sibling route]
\label{thm:pointwise-stream-metric-orphan-lattice}
Every hub-level $\PointwiseStreamMetricUp$ completion read first consumes the certified stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, the metric carrier surface of \autoref{ch:concrete-instances-metric-namecert}, and the finite completion boundary of \autoref{ch:concrete-instances-metriccompletionfinite-namecert} before exporting the pointwise distance packet. The hub exports only the displayed stream, readback, Real, metric, and finite-completion lattice; it exports no detached stream quotient, selected representative, host function equality, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\begin{proof}
Read the hub through the body carrier and its existing sibling routes. The stream-name windows, regular rational readback, Real seal, and metric distance rows are visible before any pointwise distance packet is exported.

The finite completion boundary is then supplied by $\MetricCompletionFiniteUp$. It consumes the same displayed rows as completion-facing data and adds no quotient stream equality, selected representative, countable-choice schedule, host function equality, or ambient completeness theorem.

Thus every hub-level completion read follows the stated sibling lattice before exporting the pointwise stream metric distance packet.
\end{proof}
