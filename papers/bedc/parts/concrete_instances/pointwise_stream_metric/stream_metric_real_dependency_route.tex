The $\PointwiseStreamMetricUp$ packet depends substantively on the finite $\StreamNameUp$ observation route of \autoref{ch:concrete-instances-streamname-namecert}, the $\MetricUp$ distance-witness interface of \autoref{ch:concrete-instances-metric-namecert}, and the terminal $\RealUp$ seal row of \autoref{ch:concrete-instances-real-namecert}. The pointwise metric route is not an isolated kernel float: it consumes stream windows, metric distance rows, and Real-facing seal rows before any pointwise classifier read is public.
