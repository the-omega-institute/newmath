Every $\PointwiseStreamMetricUp$ consumer reads pointwise metric data only after the finite-window source of \autoref{ch:concrete-instances-streamname-namecert}, the distance row of \autoref{ch:concrete-instances-metric-namecert}, and the regular rational tolerance readback of \autoref{ch:concrete-instances-regseqrat-namecert} are all visible. This route keeps the pointwise stream metric surface attached to displayed StreamName, Metric, and RegSeqRat rows before any downstream argument treats the stream as metric data.
