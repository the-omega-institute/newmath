The $\PointwiseStreamMetricUp$ hub has a substantive horizontal dependency on \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-real-namecert}: compact-metric finite-net rows and $\RealUp$ distance rows may enter the pointwise stream metric route only through the displayed $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ carriers already named by the packet. The dependency is therefore a finite stream-and-rational route, not ambient compactness, selected subsequences, quotient stream equality, or completeness of $\RealUp$.
