When a $\PointwiseStreamMetricUp$ distance read is used as a completion-facing convergence witness, the pointwise comparison stream leaves the carrier through the convergence ledger of \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}. The route first projects the carrier of \autoref{def:pointwise-stream-metric-carrier} and the obligation surface of \autoref{thm:pointwise-stream-metric-namecert-obligations}: the certified stream source, finite pointwise windows, dyadic tolerance ledger, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, and displayed metric row are consumed before the RealModulusOfConvergenceUp ledger may read the sequence as completion data.
