Every $\PointwiseStreamMetricUp$ consumer that reads a pointwise metric value must pass through the finite $\StreamNameUp$ schedule of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback row of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} before the metric comparison is public. The route exposes a finite stream, rational readback, and Real-facing seal boundary; it does not export quotient stream equality, a selected representative, countable choice, or ambient completeness for $\RealUp$.
