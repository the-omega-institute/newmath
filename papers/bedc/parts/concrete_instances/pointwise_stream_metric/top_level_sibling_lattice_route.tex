\begin{theorem}[Pointwise stream metric top-level sibling lattice route]
\label{thm:pointwise-stream-metric-top-level-sibling-lattice-route}
Every top-level read exported by the $\PointwiseStreamMetricUp$ hub first consumes the certified stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the metric carrier surface of \autoref{ch:concrete-instances-metric-namecert}. Only after those sibling rows are visible may a pointwise stream metric read expose its local distance packet; the hub exports no detached stream quotient, selected representative, host function equality, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\begin{proof}
Read the hub through its body chapter, \autoref{thm:pointwise-stream-metric-sibling-route}. That route already places $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\MetricSpaceUp$ before the pointwise metric row.

The present top-level route records the same dependency at the hub boundary. Since the hub has no substantive rows of its own, any exported pointwise stream metric read is exactly the sibling lattice route supplied by the body chapter and its Real-modulus dependency file.
\end{proof}
