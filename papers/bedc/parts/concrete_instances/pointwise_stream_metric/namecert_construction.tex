\chapter{A Concrete Naming Certificate for $\PointwiseStreamMetricUp$}
\label{ch:concrete-instances-pointwise-stream-metric-namecert}
\origin{human}

Following Bishop and Bridges' constructive metric-space treatment, the $\PointwiseStreamMetricUp$ packet records the standard pointwise distance interface for certified streams as finite BEDC data. It sits after the stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the metric-space carrier discipline of \autoref{ch:concrete-instances-metric-namecert}. The packet exposes pointwise metric data for completion and continuous-map consumers without host functions, quotient stream equality, selected representatives, or choice.

\begin{definition}[Pointwise stream metric carrier]
\label{def:pointwise-stream-metric-carrier}
A $\PointwiseStreamMetricUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,W,D,R,E,M,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $S$ is the certified stream source row, read through the $\StreamNameUp$ finite-window interface.
\item $W$ records the finite pointwise window schedule used when two certified streams are compared at a displayed index.
\item $D$ is the dyadic tolerance and triangle ledger governing the finite comparisons of pointwise observations.
\item $R$ is the $\RegSeqRatUp$ readback row that converts the finite rational comparison stream into a regular rational name.
\item $E$ is the terminal $\RealUp$ seal for the distance value obtained from the displayed pointwise route.
\item $M$ is the $\MetricSpaceUp$ metric-structure row, used only after the pointwise stream route has displayed $S,W,D,R,E$.
\item $H$ stores componentwise $\hsame$ transport for stream names, window schedules, dyadic ledgers, regular readbacks, real seals, metric rows, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a consumer request through $S,W,D,R,E,M,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\MetricSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\PointwiseStreamMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these coordinates. It contains no host function equality, quotient stream object, selected stream representative, countable-choice schedule, or ambient completeness assertion.
\end{definition}

\begin{theorem}[Pointwise stream metric pointwise classifier]
\label{thm:pointwise-stream-metric-pointwise-classifier}
For an accepted $\PointwiseStreamMetricUp$ carrier $Q=(S,W,D,R,E,M,H,C,P,N)$, every pointwise distance classifier read factors through the certified stream source $S$, the finite window schedule $W$, the dyadic tolerance ledger $D$, the regular-rational readback $R$, and the terminal $\RealUp$ seal $E$ before the $\MetricSpaceUp$ row $M$ may be inspected. The classifier therefore names a pointwise metric route on certified streams and does not read host functions, quotient streams, selected representatives, countable choice, or an ambient completion theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pointwise-stream-metric-carrier}. The source stream, window schedule, dyadic ledger, regular readback, Real seal, metric row, transport, replay, provenance, and local naming row are all displayed coordinates.

The pointwise route begins with $S$ and $W$. A consumer must name the stream source and the finite observation windows before any rational readback or metric row can be inspected.

The distance read then passes through $D,R,E$. The dyadic ledger bounds the finite comparisons, the regular-rational row gives the readback discipline, and the Real seal is the terminal distance value. Since no coordinate stores a host function or quotient stream, the classifier cannot export one.
\end{proof}

\begin{theorem}[Pointwise stream metric constant stream]
\label{thm:pointwise-stream-metric-constant-stream}
Let $Q=(S,W,D,R,E,M,H,C,P,N)$ be an accepted $\PointwiseStreamMetricUp$ carrier whose stream source row $S$ is displayed as a constant certified stream. Then every distance read between two displayed copies of that source is routed through the same finite window schedule $W$, dyadic zero-tolerance ledger $D$, regular-rational readback $R$, terminal Real seal $E$, and metric row $M$. The packet records the constant-stream zero-distance route as displayed NameCert data, not as host equality of functions or quotient stream identity.
\end{theorem}
\begin{proof}
Start with the displayed constant stream row in $S$. Because the stream is certified by its $\StreamNameUp$ windows, the two copies are compared only through the finite schedule $W$.

Use the dyadic ledger $D$ for the zero-tolerance comparison. The readback row $R$ and the terminal seal $E$ record the regular-rational and Real distance route generated by that finite ledger.

Finally inspect $M$ only after the pointwise route has been displayed. The metric row reads the zero-distance route as finite certificate data, while host function equality and quotient stream identity are absent from the carrier.
\end{proof}

\begin{theorem}[Pointwise stream metric reindexing stability]
\label{thm:pointwise-stream-metric-reindexing-stability}
Let $Q=(S,W,D,R,E,M,H,C,P,N)$ be an accepted $\PointwiseStreamMetricUp$ carrier. Any accepted finite reindexing of the certified stream windows transports the pointwise metric classifier only through the displayed $\hsame$ rows $H$ and the replay row $C$: the reindexed route must preserve the stream source row, window schedule, dyadic ledger, regular-rational readback, Real seal, metric row, provenance, and local naming row. It cannot add a hidden stream quotient, selected representative, or host-level function extensionality principle.
\end{theorem}
\begin{proof}
Read the transport coordinate $H$. It is componentwise: stream sources, window schedules, dyadic ledgers, readback rows, Real seals, metric rows, replay, provenance, and naming rows are transported as displayed rows.

Replay the reindexed request with $C$. Since $C$ follows the same order $S,W,D,R,E,M,H,P,N$, the pointwise classifier is preserved only by the finite route already present in the carrier.

The exclusions are an inventory check. No coordinate of $Q$ stores a stream quotient, selected representative, or host-level function extensionality principle, so reindexing stability cannot export such data.
\end{proof}

\begin{theorem}[Pointwise stream metric NameCert obligations]
\label{thm:pointwise-stream-metric-namecert-obligations}
For an accepted $\PointwiseStreamMetricUp$ carrier $Q=(S,W,D,R,E,M,H,C,P,N)$, the local $\NameCert_{\PointwiseStreamMetricUp}$ surface consists exactly of carrier admission for the certified stream source, finite pointwise windows, dyadic tolerance ledger, regular-rational readback, Real seal, metric row, componentwise transport, continuation replay, provenance, and local naming row; pointwise classifier exposure; constant-stream zero-distance exposure; reindexing stability; and ledger non-escape for host functions, quotient streams, selected representatives, countable choice, and ambient completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:pointwise-stream-metric-carrier}. The accepted packet displays exactly the stream, window, tolerance, readback, Real, metric, transport, replay, provenance, and naming rows.

Pointwise classifier exposure is \autoref{thm:pointwise-stream-metric-pointwise-classifier}. Constant-stream zero-distance exposure is \autoref{thm:pointwise-stream-metric-constant-stream}. Reindexing stability is \autoref{thm:pointwise-stream-metric-reindexing-stability}.

The ledger non-escape clause follows from the same carrier inventory. Since no row stores host functions, quotient streams, selected representatives, countable-choice schedules, or ambient completeness, the local NameCert surface is exhausted by the displayed finite rows.
\end{proof}

\begin{theorem}[Pointwise stream metric sibling route]
\label{thm:pointwise-stream-metric-sibling-route}
Every accepted $\PointwiseStreamMetricUp$ consumer reaches pointwise distance data only after the certified $\StreamNameUp$ finite window, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, and $\MetricSpaceUp$ carrier rows named in the introduction have been displayed. The pointwise metric row is therefore a sibling route through the finite stream, regular-rational, Real-seal, and metric surfaces, not a host function metric, quotient stream comparison, selected representative, countable-choice schedule, or ambient completeness assertion.
\end{theorem}
\begin{proof}
Apply \autoref{thm:pointwise-stream-metric-namecert-obligations}. The obligation surface lists carrier admission for the certified stream source, finite pointwise windows, dyadic tolerance ledger, regular-rational readback, Real seal, metric row, componentwise transport, continuation replay, provenance, and local naming row.

The opening paragraph identifies the sibling chapters that supply those rows: \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-namecert}, and \autoref{ch:concrete-instances-metric-namecert}. Reading the pointwise distance surface therefore requires the stream window before the regular readback, the regular readback before the Real seal, and the Real seal before the metric row may be used.

Since the same obligation theorem excludes host functions, quotient streams, selected representatives, countable choice, and ambient completeness, the sibling route is exhausted by the displayed finite packet.
\end{proof}

\begin{theorem}[Pointwise stream metric Real-completion anchor]
\label{thm:pointwise-stream-metric-real-completion-anchor}
Every completion-facing read of an accepted $\PointwiseStreamMetricUp$ packet factors through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and the displayed $\MetricSpaceUp$ row before it reaches any Real-completion consumer. The route exports the finite pointwise metric packet and excludes host functions, quotient streams, selected representatives, countable choice, and ambient completeness.
\end{theorem}
\begin{proof}
Start with the pointwise classifier of \autoref{thm:pointwise-stream-metric-pointwise-classifier}. It forces a distance read to pass through the certified stream source, finite window schedule, dyadic tolerance ledger, regular-rational readback, and terminal $\RealUp$ seal before the metric row is inspected.

Use \autoref{thm:pointwise-stream-metric-reindexing-stability} to transport any completion-facing reindexing request. The transport coordinate $H$ and replay coordinate $C$ preserve the same stream windows, dyadic ledger, readback row, Real seal, metric row, provenance, and local naming row.

Apply \autoref{thm:pointwise-stream-metric-namecert-obligations} and then \autoref{thm:pointwise-stream-metric-sibling-route}. The NameCert obligations exhaust the accepted packet, and the sibling route identifies the supplying chapters for $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\MetricSpaceUp$; the dyadic ledger is already a displayed coordinate of the carrier and obligations.

Therefore a Real-completion consumer receives only the finite route
$$
\StreamNameUp \longrightarrow \DyadicRatCoreUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp \longrightarrow \MetricSpaceUp.
$$
Since none of the cited surfaces stores host functions, quotient streams, selected representatives, countable-choice schedules, or ambient completeness, the completion-facing read cannot acquire them.
\end{proof}

\begin{theorem}[Pointwise stream metric dossier lattice route]
\label{thm:pointwise-stream-metric-dossier-lattice-route}
Every $\RealModulusOfConvergenceUp$ or $\MetricCompletionUp$ consumer of an accepted $\PointwiseStreamMetricUp$ carrier
$$
Q=(S,W,D,R,E,M,H,C,P,N)
$$
must read the same finite pointwise window $W$, dyadic ledger $D$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, and displayed $\MetricSpaceUp$ row $M$ before any completion-facing route is consumed. The downstream lattice is therefore the convergence-ledger route of \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert} together with the completion route of \autoref{ch:concrete-instances-metriccompletion-namecert}, not an ambient completeness theorem, host function equality, quotient stream equality, selected representative, or countable-choice schedule.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pointwise-stream-metric-carrier}. The only rows available before the structural data are the certified stream source $S$, finite window schedule $W$, dyadic ledger $D$, regular-rational readback $R$, terminal Real seal $E$, and metric row $M$.

Apply the Real-completion anchor \autoref{thm:pointwise-stream-metric-real-completion-anchor}. It already forces every completion-facing read through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and the displayed $\MetricSpaceUp$ row before a consumer receives the pointwise metric packet.

Now split the downstream request by destination. A $\RealModulusOfConvergenceUp$ consumer reads the convergence ledger only through \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}, so the same $W,D,R,E,M$ rows remain visible before convergence data is public. A $\MetricCompletionUp$ consumer reads the completion route only through \autoref{ch:concrete-instances-metriccompletion-namecert}, so the same finite pointwise dossier is still the source of completion-facing distance data.

Because the carrier lacks coordinates for ambient completeness, host function equality, quotient stream equality, selected representatives, and countable-choice schedules, neither downstream route can acquire those objects from $\PointwiseStreamMetricUp$.
\end{proof}

The downstream completion lattice consumes this anchor through the convergence-ledger route of \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}. Every $\RealModulusOfConvergenceUp$ consumer of an accepted $\PointwiseStreamMetricUp$ packet must read the same finite windows $W$, dyadic ledger $D$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, and displayed metric row $M$ before any convergence ledger is public. The consumer therefore receives the pointwise stream metric only as the finite route already exposed by the carrier, not as a detached completion principle.

\begin{theorem}[Pointwise stream metric finite-window consumption]
\label{thm:pointwise-stream-metric-finite-window-consumption}
Every metric-distance read from an accepted $\PointwiseStreamMetricUp$ packet
first consumes the displayed $\StreamNameUp$ finite window, the $\RegSeqRatUp$
readback row, the terminal $\RealUp$ seal, and the $\MetricSpaceUp$ distance
row. The consumed route is the finite packet
$$
S,W,D,R,E,M,H,C,P,N
$$
of \autoref{def:pointwise-stream-metric-carrier}, with the dyadic ledger $D$
bounding the window comparison before the Real seal and metric row are read.

The route is invariant under accepted scheduled window replacement transported
by $H$ and replayed by $C$. It exposes no quotient stream equality, selected
stream representative, host function equality, countable-choice schedule, or
ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pointwise-stream-metric-carrier}. The
metric-distance read has only the stream source, finite window schedule,
dyadic ledger, regular readback, Real seal, metric row, transport, replay,
provenance, and local naming coordinates available.

Apply \autoref{thm:pointwise-stream-metric-sibling-route}. It orders the
reader through the $\StreamNameUp$ finite window, the $\RegSeqRatUp$ readback,
the terminal $\RealUp$ seal, and then the $\MetricSpaceUp$ row.

Use \autoref{thm:pointwise-stream-metric-real-completion-anchor} to connect
the same route to completion-facing consumers. The dyadic ledger remains a
displayed coordinate of the packet, so the read cannot bypass the finite
window and regular readback.

Finally invoke \autoref{thm:pointwise-stream-metric-reindexing-stability}. Any
scheduled replacement transported by $H$ and replayed by $C$ preserves the
same stream windows, dyadic ledger, readback, Real seal, metric row,
provenance, and local naming row. Since the carrier has no quotient stream,
selected representative, host function equality, choice schedule, or ambient
completeness coordinate, the finite-window consumption route is exhausted by
the displayed packet.
\end{proof}

\begin{theorem}[Pointwise stream metric completion separation]
\label{thm:pointwise-stream-metric-completion-separation}
Every completion-facing read of an accepted $\PointwiseStreamMetricUp$ carrier
$$
Q=(S,W,D,R,E,M,H,C,P,N)
$$
must expose $S,W,D,R,E,M,H,C,P,N$ before a completion consumer may inspect the metric-distance row. The read cannot infer ambient completeness, quotient stream equality, selected representatives, countable-choice schedules, or host function equality unless the displayed $\RegSeqRatUp$ readback row $R$ and terminal $\RealUp$ seal row $E$ are present in the same packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pointwise-stream-metric-carrier}. The only available rows are the certified stream source, finite window schedule, dyadic tolerance ledger, regular-rational readback, terminal Real seal, metric row, transport, replay, provenance, and local naming row.

Apply the pointwise classifier theorem \autoref{thm:pointwise-stream-metric-pointwise-classifier}. It forces the distance read through $S,W,D,R,E$ before $M$ is inspected, so a completion consumer cannot start from an ambient completed stream space.

Use the Real-completion anchor \autoref{thm:pointwise-stream-metric-real-completion-anchor} and the finite-window consumption theorem \autoref{thm:pointwise-stream-metric-finite-window-consumption}. Together they place every completion-facing request on the displayed stream-window, dyadic-ledger, RegSeqRat readback, Real-seal, and metric route.

The conclusion is the carrier inventory. Since no coordinate stores ambient completeness, quotient stream equality, selected representatives, countable choice, or host function equality, the completion-facing read can use such data only by leaving the accepted $\PointwiseStreamMetricUp$ packet.
\end{proof}

\begin{theorem}[Pointwise stream metric source separation]
\label{thm:pointwise-stream-metric-source-separation}
Let $Q=(S,W,D,R,E,M,H,C,P,N)$ be an accepted $\PointwiseStreamMetricUp$ carrier. If two completion-facing reads agree on the metric row $M$ but differ in any of the source rows $S,W,D,R,E$, then the packet cannot identify them by $\PointwiseStreamMetricUp$. Source separation requires the same $\StreamNameUp$ finite window, dyadic ledger, $\RegSeqRatUp$ readback, $\RealUp$ seal, and $\MetricSpaceUp$ row; no quotient stream equality or host function equality can collapse different source rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pointwise-stream-metric-carrier}. A completion-facing read exposes the ordered packet $S,W,D,R,E,M,H,C,P,N$, so agreement of the metric row $M$ alone does not determine the source route.

Apply the pointwise classifier theorem \autoref{thm:pointwise-stream-metric-pointwise-classifier}. It places the certified stream source, finite window schedule, dyadic ledger, regular readback, and Real seal before the metric row is inspected.

Use finite-window consumption, \autoref{thm:pointwise-stream-metric-finite-window-consumption}, and completion separation, \autoref{thm:pointwise-stream-metric-completion-separation}. Together they require a completion-facing metric read to keep the same $S,W,D,R,E$ rows in the packet, not merely the same metric row.

Finally apply reindexing stability, \autoref{thm:pointwise-stream-metric-reindexing-stability}. Transport by $H$ and replay by $C$ preserve displayed rows componentwise, and the carrier has no quotient stream or host function equality coordinate. Therefore two reads differing in any of $S,W,D,R,E$ remain source-separated inside $\PointwiseStreamMetricUp$.
\end{proof}

\begin{theorem}[Pointwise stream metric modulus ledger pullback]
\label{thm:pointwise-stream-metric-modulus-ledger-pullback}
Every $\RealModulusOfConvergenceUp$ consumer of an accepted $\PointwiseStreamMetricUp$ carrier
$$
Q=(S,W,D,R,E,M,H,C,P,N)
$$
must factor through the displayed certified stream source $S$, finite pointwise window $W$, dyadic ledger $D$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, and metric row $M$ before it may read convergence data. The pullback reaches the convergence ledger only through the sibling route of \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}; it exports no detached modulus, quotient stream equality, selected representative, countable-choice schedule, host function equality, or ambient completion principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pointwise-stream-metric-carrier}. The only completion-facing analytic rows available before the structural data are $S,W,D,R,E,M$.

Apply the NameCert obligations, \autoref{thm:pointwise-stream-metric-namecert-obligations}, together with completion separation, \autoref{thm:pointwise-stream-metric-completion-separation}. These force any completion consumer to expose the certified stream source, finite windows, dyadic ledger, regular-rational readback, Real seal, metric row, transport, replay, provenance, and local naming before a metric-distance read is public.

Now use the sibling $\RealModulusOfConvergenceUp$ chapter. Its carrier and route theorems read convergence-modulus data only after the finite $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, and terminal $\RealUp$ seal are displayed. Therefore the pointwise metric contribution enters the convergence ledger through the same $S,W,D,R,E,M$ route.

Since neither carrier stores a detached modulus selector, quotient stream equality, selected representative, choice schedule, host function equality, or ambient completion theorem, the pullback is exhausted by the displayed rows.
\end{proof}

\closureat{PointwiseStreamMetricUp}{seedStr}

\begin{closurestatus}{\PointwiseStreamMetricUp}
  \constructivestory{A $\PointwiseStreamMetricUp$ packet is generated from a certified stream source, finite pointwise windows, a dyadic tolerance ledger, regular-rational readback, a Real distance seal, a displayed metric-space row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pointwise-stream-metric-carrier}, \autoref{thm:pointwise-stream-metric-pointwise-classifier}, \autoref{thm:pointwise-stream-metric-constant-stream}, \autoref{thm:pointwise-stream-metric-reindexing-stability}, and \autoref{thm:pointwise-stream-metric-namecert-obligations} seed the finite pointwise metric route for certified streams over StreamNameUp, DyadicRatCoreUp, RegSeqRatUp, RealUp, MetricSpaceUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, quotient stream equality, selected representatives, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, pointwise classifier exposure, constant-stream zero-distance exposure, reindexing stability, and ledger non-escape for BEDC.Derived.PointwiseStreamMetricUp.}
\end{closurestatus}
