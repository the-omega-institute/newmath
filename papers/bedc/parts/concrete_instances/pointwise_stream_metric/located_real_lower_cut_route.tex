The $\PointwiseStreamMetricUp$ route also admits located-real lower-cut rows as completion-facing input data. A metric comparison first reads the certified $\StreamNameUp$ windows, passes the rational readback through $\RegSeqRatUp$, consumes the lower-cut row before the terminal $\RealUp$ consumer, and only then exposes the pointwise metric packet to Real and Completion dependents.
