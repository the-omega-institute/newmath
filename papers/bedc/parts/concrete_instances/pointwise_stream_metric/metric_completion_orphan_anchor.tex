\begin{theorem}[Pointwise stream metric sibling repair route]
\label{thm:pointwise-stream-metric-sibling-orphan-repair-route}
Every $\MetricCompletionUp$ or $\RealModulusOfConvergenceUp$ consumer of $\PointwiseStreamMetricUp$ must first read the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and $\MetricSpaceUp$ rows from \autoref{def:pointwise-stream-metric-carrier} and \autoref{thm:pointwise-stream-metric-namecert-obligations}. Only then may the consumer enter the downstream completion ledgers of \autoref{ch:concrete-instances-metriccompletion-namecert} and \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}.
\end{theorem}
\begin{proof}
Project the carrier. The stream source, dyadic tolerance ledger, regular-rational readback, terminal Real seal, and metric row are displayed before any completion-facing row can be consumed.

Read the obligation surface. It requires carrier admission, pointwise classifier exposure, constant-stream zero-distance exposure, reindexing stability, and ledger non-escape over the same finite rows.

The downstream references therefore consume the pointwise metric packet as a displayed BEDC route. No host completion theorem, quotient stream equality, selected representative, or countable-choice schedule is introduced.
\end{proof}
