Every $\MetricCompletionUp$ consumer of $\PointwiseStreamMetricUp$ reads the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and $\MetricSpaceUp$ rows before completion-facing convergence data are exposed through \autoref{ch:concrete-instances-metriccompletion-namecert}. Completion-facing convergence data are also read through \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert} when the pointwise metric packet is used as a Real convergence ledger. The route is the displayed metric-stream dossier itself, not a host completion theorem, quotient stream equality, selected representative, or countable-choice schedule.
