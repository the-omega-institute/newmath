\begin{theorem}[Pointwise stream metric StreamName completion sibling route]
\label{thm:pointwise-stream-metric-completion-sibling-route}
Every $\PointwiseStreamMetricUp$ completion-facing consumer first reads the certified $\StreamNameUp$ finite observations of \autoref{ch:concrete-instances-streamname-namecert} and the $\MetricCompletionUp$ existence boundary of \autoref{ch:concrete-instances-metric-completion-existence-namecert}. The pointwise packet reaches that boundary only through the displayed route
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp \longrightarrow \MetricSpaceUp \longrightarrow \MetricCompletionUp .
$$
Thus the completion read consumes pointwise stream observations, regular rational readback, the terminal Real classifier, and the metric row already displayed in $\PointwiseStreamMetricUp$; it does not introduce host stream equality, a selected representative, quotient completion, countable-choice scheduling, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pointwise-stream-metric-carrier}. The stream source $S$, finite window schedule $W$, dyadic ledger $D$, regular readback row $R$, terminal Real seal $E$, and metric row $M$ are visible before any structural row can be consumed.

Apply the pointwise classifier and Real-completion anchor, \autoref{thm:pointwise-stream-metric-pointwise-classifier} and \autoref{thm:pointwise-stream-metric-real-completion-anchor}. They force every distance read through the StreamName window, RegSeqRat readback, Real seal, and metric row before a completion-facing consumer receives the packet.

Finally read the completion existence boundary through \autoref{ch:concrete-instances-metric-completion-existence-namecert}. Since the incoming route has already exhausted $S,W,D,R,E,M,H,C,P,N$, the completion consumer receives only the displayed sibling dependency surface and cannot acquire host stream equality, quotient completion, selected representatives, countable-choice schedules, or ambient completeness from $\PointwiseStreamMetricUp$.
\end{proof}
