\chapter{A Concrete Naming Certificate for $\HyperbolicHoroballShadowLedgerUp$}
\label{ch:concrete-instances-hyperbolic-horoball-shadow-ledger-namecert}
\origin{ai}

The $\HyperbolicHoroballShadowLedgerUp$ packet realizes the observer-expansion branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as a finite ledger that places Busemann, visual-shadow, boundary, metric, observer-window, transport, provenance, and local naming rows on one route. It sits after the cross-ratio boundary comparison of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, the geodesic-lamination surface of \autoref{ch:concrete-instances-hyperbolic-geodesic-lamination-namecert}, the Patterson--Sullivan shadow packet of \autoref{ch:concrete-instances-hyperbolic-patterson-sullivan-shadow-namecert}, the visual-metric certificate of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, and the Busemann-boundary certificate of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}. The packet names a finite horoball-shadow ledger; it is not a quotient ideal boundary, a selected geodesic ray, a completed conformal density, an ambient hyperbolic group action, a physical observer trajectory, or a global measure construction.

\begin{definition}[Hyperbolic horoball-shadow ledger carrier]
\label{def:hyperbolic-horoball-shadow-ledger-carrier}
A $\HyperbolicHoroballShadowLedgerUp$ carrier is a finite $\BHist$ packet
$$
Z=(B,V,W,M,O,X,L,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\HyperbolicBusemannBoundaryUp$ row recording the horoball threshold and boundary-level Busemann read.
\item $V$ is the $\HyperbolicVisualMetricUp$ row recording the visible shadow metric used by the boundary read.
\item $W$ is the visual-shadow window row, tying the Busemann threshold and visual metric to a finite aperture.
\item $M$ is the metric comparison row that keeps the horoball threshold attached to displayed hyperbolic distance data.
\item $O$ is the observer-window row recording the finite observer-expansion read that consumes the shadow.
\item $X$ is the cross-ratio and lamination sibling row, recording the boundary comparison route through $\HyperbolicCrossRatioUp$ and $\HyperbolicGeodesicLaminationUp$ when those rows are present.
\item $L$ is the Patterson--Sullivan shadow sibling row, recording the counting and boundary-weight ledger consumed by the same shadow aperture when present.
\item $T$ is the refusal row excluding quotient ideal-boundary equality, selected rays, completed conformal densities, host group actions, physical observer trajectories, and unlogged global measures.
\item $H$ records componentwise $\hsame$ transport for $B,V,W,M,O,X,L,T,C,P,N$.
\item $C$ records displayed $\Cont$ replay through Busemann readback, visual-shadow exposure, metric comparison, observer-window routing, sibling ledger rows, refusal, transport, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicHoroballShadowLedgerUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicGeodesicLaminationUp$, $\HyperbolicPattersonSullivanShadowUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicHoroballShadowLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,V,W,M,O,X,L,T,H,C,P,N$.
\end{definition}

\begin{theorem}[Hyperbolic horoball-shadow ledger NameCert obligations]
\label{thm:hyperbolic-horoball-shadow-ledger-namecert-obligations}
For every accepted $\HyperbolicHoroballShadowLedgerUp$ carrier $Z=(B,V,W,M,O,X,L,T,H,C,P,N)$, the local $\NameCert_{\HyperbolicHoroballShadowLedgerUp}$ surface consists exactly of Busemann-boundary admission through $B$; visual-shadow metric exposure through $V,W$; metric comparison through $M$; observer-window exposure through $O$; cross-ratio and geodesic-lamination sibling exposure through $X$; Patterson--Sullivan sibling exposure through $L$; refusal exactness through $T$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-horoball-shadow-ledger-carrier}. The displayed tuple exposes the Busemann row, visual metric row, shadow window, metric comparison, observer window, sibling rows, refusal row, and structural rows.

Read the geometric rows first. Rows $B,V,W,M,O$ bind the horoball threshold, visual-shadow metric, finite aperture, metric comparison, and observer-window route to one finite ledger.

The sibling rows $X,L$ then record the boundary comparison, lamination, and Patterson--Sullivan shadow routes without replacing the local carrier. Finally $T,H,C,P,N$ refuse unlogged host geometry, transport the displayed fields, replay the route, record provenance, and name the packet. No further NameCert obligation is stored outside these rows.
\end{proof}

\begin{theorem}[Hyperbolic horoball-shadow ledger sibling independence]
\label{thm:hyperbolic-horoball-shadow-ledger-sibling-independence}
The $\HyperbolicHoroballShadowLedgerUp$ packet is not bijective to any one of the cross-ratio, geodesic-lamination, Patterson--Sullivan shadow, visual-metric, or Busemann-boundary sibling packets. Each sibling may supply one row of $Z$, but the accepted ledger requires the joint Busemann threshold, visual-shadow window, metric comparison, observer-window route, sibling ledger rows, refusal row, transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Compare the carrier fields. A $\HyperbolicCrossRatioUp$ packet supplies boundary comparison data but not the Busemann threshold, observer window, or Patterson--Sullivan ledger row. A $\HyperbolicGeodesicLaminationUp$ packet supplies lamination data but not the horoball threshold or visual-shadow aperture.

The $\HyperbolicPattersonSullivanShadowUp$ sibling supplies a counting and boundary-weight route, while $\HyperbolicVisualMetricUp$ and $\HyperbolicBusemannBoundaryUp$ supply visual and Busemann rows. None of those packets alone contains all rows $B,V,W,M,O,X,L,T,H,C,P,N$.

Therefore each sibling is a dependency row rather than a replacement carrier, and no listed sibling is bijective to the accepted horoball-shadow ledger.
\end{proof}

\begin{theorem}[Hyperbolic horoball-shadow ledger observer-window nonescape]
\label{thm:hyperbolic-horoball-shadow-ledger-observer-window-nonescape}
Every observer-expansion consumer of an accepted $\HyperbolicHoroballShadowLedgerUp$ packet must read the Busemann row $B$, visual-shadow rows $V,W$, metric row $M$, observer-window row $O$, and sibling ledger rows $X,L$ before it can use the structural rows $H,C,P,N$. Consequently the packet exports no quotient ideal-boundary equality, selected geodesic ray, completed conformal density, ambient hyperbolic group action, physical observer trajectory, or global measure construction.
\end{theorem}
\begin{proof}
Start with the observer-expansion consumer. Projection of $Z$ gives the finite route through $B,V,W,M,O,X,L$ before the structural rows are available.

The refusal row $T$ is part of the same route. It blocks interpretations that replace the displayed horoball-shadow ledger by quotient boundary equality, selected rays, conformal densities, host group actions, physical trajectories, or global measures.

Rows $H,C,P,N$ then transport, replay, source, and name only the displayed ledger. Since none of those rows adds a hidden geometry coordinate, the consumer cannot escape the finite observer-window surface.
\end{proof}

\falsifiablePrediction{Any accepted $\HyperbolicHoroballShadowLedgerUp$ packet that omits the Busemann threshold row, the visual-shadow window row, or the observer-window row fails to produce the observer-window nonescape route of \autoref{thm:hyperbolic-horoball-shadow-ledger-observer-window-nonescape}.}

\independenceWitness{hyperbolic-cross-ratio, hyperbolic-geodesic-lamination, hyperbolic-patterson-sullivan-shadow, hyperbolic-visual-metric, hyperbolic-busemann-boundary: no listed sibling is bijective to $\HyperbolicHoroballShadowLedgerUp$ because the ledger jointly carries Busemann threshold, visual-shadow aperture, metric comparison, observer-window, sibling ledger, refusal, transport, replay, provenance, and local NameCert rows.}

The future Lean TasteGate route must provide explicit $\Nontrivial\ \HyperbolicHoroballShadowLedgerUp$ and $\FieldFaithful\ \HyperbolicHoroballShadowLedgerUp$ instances in \texttt{lean4/BEDC/Derived/HyperbolicHoroballShadowLedgerUp/TasteGate.lean}. Those instances must preserve $B,V,W,M,O,X,L,T,H,C,P,N$ as distinct fields before a checked carrier-alignment theorem can serve as the TasteGate witness.

\closureat{HyperbolicHoroballShadowLedgerUp}{seedStr}

\begin{closurestatus}{\HyperbolicHoroballShadowLedgerUp}
  \constructivestory{A $\HyperbolicHoroballShadowLedgerUp$ packet is generated from Busemann-boundary rows, visual-shadow metric rows, a finite shadow window, metric comparison rows, an observer-window route, cross-ratio and geodesic-lamination sibling rows, a Patterson--Sullivan shadow sibling row, refusal exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-horoball-shadow-ledger-carrier}, \autoref{thm:hyperbolic-horoball-shadow-ledger-namecert-obligations}, \autoref{thm:hyperbolic-horoball-shadow-ledger-sibling-independence}, and \autoref{thm:hyperbolic-horoball-shadow-ledger-observer-window-nonescape} seed the finite horoball-shadow ledger through Busemann, visual-shadow, metric, observer-window, sibling, refusal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient ideal-boundary equality, selected geodesic rays, completed conformal densities, ambient hyperbolic group actions, physical observer trajectories, global measure constructions, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Busemann exposure, visual-shadow exposure, metric comparison, observer-window nonescape, sibling independence, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicHoroballShadowLedgerUp.}
\end{closurestatus}
