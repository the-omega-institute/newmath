\chapter{A Concrete Naming Certificate for $\RealBolzanoWeierstrassUp$}
\label{ch:concrete-instances-real-bolzano-weierstrass-namecert}
\origin{human}

Following Bishop and Bridges, Constructive Analysis, the $\RealBolzanoWeierstrassUp$ packet records the bounded real-sequence subsequence route as finite NameCert data. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, and the located nested-interval limit surface of \autoref{ch:concrete-instances-located-nested-interval-limit-namecert}. The packet is a constructive boundary object for bounded real sequences: it exposes a finite subsequence ledger and a terminal Real seal, not classical sequential compactness, open-cover compactness, countable choice, a host subsequence selector, or ambient $\RealUp$ completeness.

\begin{definition}[Real Bolzano--Weierstrass carrier]
\label{def:real-bolzano-weierstrass-carrier}
A $\RealBolzanoWeierstrassUp$ carrier is a finite $\BHist$ packet
$$
W=(S,D,I,L,R,E,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the bounded $\RealUp$ sequence row, exposing only the finite source coordinates inspected by the packet.
\item $D$ is the $\DyadicRatCoreUp$ subdivision ledger, recording the dyadic interval cuts, diameter budgets, and containment checks used at each finite stage.
\item $I$ is the finite nested-window row, storing the selected interval windows and their inclusion witnesses.
\item $L$ is the subsequence ledger, recording the monotone index schedule produced by the same finite windows.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the dyadic subdivisions and the subsequence ledger.
\item $E$ is the terminal $\RealUp$ seal reached only after $S,D,I,L,R$ have been displayed.
\item $Q$ is the refusal row, recording that no classical compactness theorem, countable-choice selector, host subsequence, or ambient completeness object is part of the carrier.
\item $H$ records componentwise $\hsame$ transport for the bounded source, dyadic subdivision ledger, nested windows, subsequence ledger, RegSeqRat readback, Real seal, refusal row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from bounded-sequence inspection through dyadic subdivision, nested-window refinement, subsequence ledgering, readback, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\RealBolzanoWeierstrassUp$, $\RealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedNestedIntervalLimitUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealBolzanoWeierstrassUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,D,I,L,R,E,Q,H,C,P,N$.
\end{definition}

\begin{theorem}[Real Bolzano--Weierstrass NameCert obligations]
\label{thm:real-bolzano-weierstrass-namecert-obligations}
For every accepted $\RealBolzanoWeierstrassUp$ carrier
$$
W=(S,D,I,L,R,E,Q,H,C,P,N),
$$
the local $\NameCert_{\RealBolzanoWeierstrassUp}$ surface consists exactly of bounded Real sequence admission through $S$, dyadic interval subdivision through $D$, finite nested-window discipline through $I$, subsequence ledger exactness through $L$, RegSeqRat readback through $R$, terminal Real sealing through $E$, refusal of the excluded compactness and choice rows through $Q$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-bolzano-weierstrass-carrier}. The rows $S,D,I,L,R,E,Q,H,C,P,N$ are the only coordinates exposed to a consumer.

The bounded sequence is read first through $S$. The dyadic subdivision ledger $D$ and nested-window row $I$ then fix the finite interval route before any subsequence or Real-facing read is available.

The ledger row $L$ records the monotone index schedule selected by those same windows. The readback row $R$ consumes the dyadic and subsequence evidence, and the Real seal $E$ is downstream of that readback.

The refusal and structural rows add no hidden analytic principle. Row $Q$ blocks the excluded compactness, choice, host-selector, and completeness coordinates, while $H,C,P,N$ transport, replay, source, and name exactly the displayed finite packet.
\end{proof}

\begin{theorem}[Real subsequence readback route]
\label{thm:real-bolzano-weierstrass-subsequence-readback-route}
Every $\RealBolzanoWeierstrassUp$ consumer that exports a cluster-facing real read must factor through the finite route
$$
\RealUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \LocatedNestedIntervalLimitUp \longmapsto \RealUp .
$$
The exported read is supported by bounded source rows, dyadic interval subdivision, finite nested windows, a subsequence ledger, RegSeqRat readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-bolzano-weierstrass-namecert-obligations}. It places bounded source admission, dyadic subdivisions, nested windows, subsequence ledgering, and regular rational readback before the terminal Real seal can be consumed.

The source and target occurrences of $\RealUp$ are not quotient representatives. The first supplies bounded sequence names through finite StreamName windows, while the second is the terminal seal reached after the dyadic and regular-rational route has been displayed.

Transport and replay preserve the same packet. Thus any cluster-facing read follows the stated finite route and cannot bypass it through classical sequential compactness, open-cover compactness, countable choice, a host subsequence selector, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RealBolzanoWeierstrassUp$ cluster-facing read factors through one bounded RealUp sequence row, one DyadicRatCore subdivision ledger, one finite nested-window row, one subsequence ledger, one RegSeqRat readback row, one terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{real, regseqrat, dyadicratcore, streamname, located-nested-interval-limit: $\RealBolzanoWeierstrassUp$ is not bijective to any listed sibling because it jointly carries bounded real-sequence rows, dyadic interval subdivision, finite nested windows, a subsequence ledger, regular rational readback, and a terminal Real seal for cluster-facing consumers.}

\closureat{RealBolzanoWeierstrassUp}{seedStr}

\begin{closurestatus}{\RealBolzanoWeierstrassUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-bolzano-weierstrass-carrier}, \autoref{thm:real-bolzano-weierstrass-namecert-obligations}, and \autoref{thm:real-bolzano-weierstrass-subsequence-readback-route} seed the bounded real-sequence subsequence route over dyadic subdivision, finite nested windows, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close classical sequential compactness, open-cover compactness, countable choice, host subsequence selection, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded sequence visibility, dyadic interval subdivision, finite nested-window exactness, subsequence-ledger exactness, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RealBolzanoWeierstrassUp.}
  \constructivestory{A $\RealBolzanoWeierstrassUp$ packet is generated from a bounded RealUp sequence row, a DyadicRatCore subdivision ledger, finite nested windows, a subsequence ledger, RegSeqRat readback, a terminal RealUp seal, a refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
