The $\RegularCauchyUniformContinuityUp$ route consumes $\RegSeqRatUp$ readbacks through \autoref{ch:concrete-instances-regseqrat-namecert}, $\ContinuousMapUp$ modulus-facing pointwise rows through \autoref{ch:concrete-instances-continuousmap-namecert}, and the $\UniformModulusUp$ handoff through \autoref{ch:concrete-instances-uniformmodulus-namecert}. This child route exports no quotient stream, selected limit, host continuity, or ambient $\RealUp$ completeness.
