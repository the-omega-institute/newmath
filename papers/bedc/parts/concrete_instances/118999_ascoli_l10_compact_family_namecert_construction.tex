\chapter{A Concrete Naming Certificate for $\AscoliLTenCompactFamilyUp$}
\label{ch:concrete-instances-ascoli-l10-compact-family-namecert}
\origin{human}

Following Arzela, Ascoli, and Bishop--Bridges constructive analysis, the $\AscoliLTenCompactFamilyUp$ packet records the reusable compact-uniform family carrier used before any selection theorem is invoked. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the bounded-family ledger of \autoref{ch:concrete-instances-bounded-function-family-namecert}, the equicontinuity family surface of \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, the finite diagonal schedule of \autoref{ch:concrete-instances-ascoli-diagonal-namecert}, and the terminal readback rows of \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}. The chapter names the compact family carrier only; it does not assert a host function-space compactness theorem, a selected subsequence outside the displayed schedule, quotient equality of functions, or ambient completeness.

\begin{definition}[Ascoli L10 uniform compact-family carrier]\label{def:ascoli-l10-uniform-compact-family-carrier}
An $\AscoliLTenCompactFamilyUp$ carrier is a finite $\BHist$ packet
$$
\mathcal{A}_{10}=(K,B,E,F,U,W,Q,R,H,C,P,N),
$$
where $K$ is the compact source row read through $\CompactMetricUp$, $B$ is the bounded-family row read through $\BoundedFunctionFamilyUp$, $E$ is the equicontinuity row read through $\EquicontinuityFamilyUp$, $F$ is the displayed compact finite-net row, $U$ is the uniform-modulus handoff row, $W$ is the finite window schedule read through $\StreamNameUp$, $Q$ is the $\RegSeqRatUp$ readback row, $R$ is the terminal $\RealUp$ seal, $H$ is componentwise $\hsame$ transport across source, family, net, window, readback, and seal rows, $C$ is displayed $\Cont$ replay for the same finite route, $P$ is $\Pkg$ provenance over the listed sibling packets, and $N$ is the local $\NameCert_{\AscoliLTenCompactFamilyUp}$ row.
\end{definition}

\begin{theorem}[Ascoli L10 finite-net subsequence obligation]\label{thm:ascoli-l10-finite-net-subsequence-obligation}
Every accepted $\AscoliLTenCompactFamilyUp$ consumer that later reads a compact-family or subsequence-facing conclusion must first display the finite route
$$
\CompactMetricUp,\ \BoundedFunctionFamilyUp,\ \EquicontinuityFamilyUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
The route exposes the compact finite-net row $F$, the uniform modulus row $U$, the window schedule $W$, the regular rational readback $Q$, and the Real seal $R$ before any downstream compact-uniform argument may consume the packet. It exports no host function-space compactness theorem, no selected representative outside $W$, no quotient stream equality, and no ambient completeness principle.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:ascoli-l10-uniform-compact-family-carrier}. The compact source, bounded family, equicontinuity family, finite-net, uniform-modulus, window, readback, and seal coordinates are all explicit rows of the tuple.

The finite-net and window rows determine the only subsequence-facing schedule available to later Ascoli consumers. The $\RegSeqRatUp$ row reads that schedule as regular rational data, and the $\RealUp$ row seals only the displayed readback.

Transport and replay are componentwise through $H$ and $C$, while provenance and local naming are carried by $P$ and $N$. Since the tuple contains no coordinate for host compactness, selected representatives outside $W$, quotient stream equality, or ambient completeness, every downstream read factors through the listed finite route.
\end{proof}

\begin{theorem}[Ascoli L10 NameCert ledger]\label{thm:ascoli-l10-namecert-ledger}
The local $\NameCert_{\AscoliLTenCompactFamilyUp}$ surface consists exactly of carrier admission for the bounded-family row, equicontinuity row, compact finite-net row, uniform-modulus row, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local NameCert row $N$. The ledger is a reusable compact-uniform family carrier for downstream Ascoli and completion arguments, not a replacement for the selection theorem of \autoref{ch:concrete-instances-arzela-ascoli-selection-namecert} or the diagonal packet of \autoref{ch:concrete-instances-ascoli-diagonal-namecert}.
\end{theorem}
\begin{proof}
Read the accepted packet coordinate by coordinate. The rows $B,E,F,U,Q,R$ supply the bounded-family, equicontinuity, compact finite-net, uniform modulus, regular rational readback, and terminal seal obligations.

The rows $H,C,P,N$ then record transport, replay, provenance, and local naming for exactly those obligations. No further NameCert clause can be read, because no further row occurs in the carrier.

Thus the ledger exposes a reusable compact-uniform family surface. Selection, diagonal extraction, quotient equality, and host compactness remain outside the local packet unless a downstream chapter supplies its own displayed rows.
\end{proof}

\closureat{AscoliLTenCompactFamilyUp}{seedStr}

\begin{closurestatus}{\AscoliLTenCompactFamilyUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:ascoli-l10-uniform-compact-family-carrier}, \autoref{thm:ascoli-l10-finite-net-subsequence-obligation}, and \autoref{thm:ascoli-l10-namecert-ledger} seed the finite compact-uniform family carrier over bounded-family, equicontinuity, compact finite-net, uniform-modulus, regular rational readback, Real seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host function-space compactness, selected subsequences outside the displayed schedule, quotient function equality, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net route, readback exactness, transport, replay, provenance, and local naming for BEDC.Derived.AscoliLTenCompactFamilyUp.}
  \constructivestory{An $\AscoliLTenCompactFamilyUp$ packet is generated from a compact source row, bounded-family row, equicontinuity-family row, compact finite-net row, uniform-modulus handoff, finite StreamName window, RegSeqRat readback, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
