\chapter{A Concrete Naming Certificate for $\RationalCauchyTailBoundUp$}
\label{ch:concrete-instances-rational-cauchy-tail-bound-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RationalCauchyTailBoundUp$ packet records the finite rational tail-bound ledger used before a regular Cauchy read is handed to a Real or Completion consumer. It sits after the interval localization of \autoref{ch:concrete-instances-rational-interval-locator-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names only the displayed tail-bound evidence; it is not quotient equality of rational streams, a selected limit, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Rational Cauchy tail-bound carrier]
\label{def:rational-cauchy-tail-bound-carrier}
A $\RationalCauchyTailBoundUp$ carrier is a finite $\BHist$ packet
$$
T=(L,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\RationalIntervalLocatorUp$ source row, exposing the rational center, interval endpoints, and the finite index threshold at which the tail read begins.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, storing the displayed dyadic radius, slack row, and comparison budget used to test late rational observations.
\item $W$ is the $\StreamNameUp$ finite-window row opened by $L$ and budgeted by $D$.
\item $R$ is the $\RegSeqRatUp$ readback row, recording the regular rational tail estimate obtained from the displayed windows and dyadic budget.
\item $E$ is the terminal $\RealUp$ seal row, visible only after $L,D,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the locator, dyadic tolerance ledger, finite windows, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a requested precision threshold through $L,D,W,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RationalCauchyTailBoundUp$, $\RationalIntervalLocatorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RationalCauchyTailBoundUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,D,W,R,E,H,C,P,N$. It has no coordinate for quotient equality of rational streams, a selected real limit, countable choice, host real equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Rational Cauchy tail-bound NameCert obligations]
\label{thm:rational-cauchy-tail-bound-namecert-obligations}
For every accepted $\RationalCauchyTailBoundUp$ carrier
$$
T=(L,D,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\RationalCauchyTailBoundUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the rational locator row, dyadic tolerance ledger, finite stream windows, regular rational readback, terminal Real seal, transport, replay, provenance, and local naming rows;
\item locator exactness, requiring $L$ to display the rational center, interval endpoints, and finite tail threshold that the bound is allowed to inspect;
\item dyadic tolerance exactness, requiring $D$ to store the radius, slack, and comparison budget used for late observations;
\item window compatibility, requiring $W$ to expose only the StreamName observations opened by $L$ and budgeted by $D$;
\item regular readback exactness, requiring $R$ to consume $L,D,W$ before any Real seal is visible;
\item Real-seal nonescape, requiring $E$ to be terminal with respect to the route $L,D,W,R$ and to export no quotient stream equality, selected limit, countable-choice schedule, host real equality, trichotomy, ambient completeness theorem, Lean verification, or bridge data;
\item componentwise transport, displayed replay, provenance, and local naming for the same displayed rows through $H,C,P,N$.
\end{enumerate}
No rational Cauchy tail-bound obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:rational-cauchy-tail-bound-carrier}. The accepted packet displays exactly the rational locator, dyadic tolerance ledger, finite stream windows, regular rational readback, terminal Real seal, transport, replay, provenance, and local naming rows.

The analytic clauses are forced by the route. The row $L$ is the only coordinate that carries the rational center, interval endpoints, and finite tail threshold, while $D$ is the only coordinate that carries the dyadic radius, slack, and comparison budget.

The window and readback clauses are downstream. The row $W$ reads only the observations opened by $L$ and budgeted by $D$, and $R$ is the first row that repacks those finite observations as regular rational tail evidence.

Finally, $E,H,C,P,N$ seal, transport, replay, source, and name the same finite route. Since quotient stream equality, selected limits, countable choice, host real equality, trichotomy, ambient completeness, Lean verification, and bridge data are absent from the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Rational Cauchy tail-bound regular Real handoff]
\label{thm:rational-cauchy-tail-bound-regular-real-handoff}
Every accepted rational tail-bound read that reaches a $\RegSeqRatUp$ or $\RealUp$ consumer must factor through the finite route
$$
L \longmapsto D \longmapsto W \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Thus the consumer receives the rational locator row, dyadic tolerance ledger, finite StreamName windows, regular rational readback, terminal Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. The consumer receives no quotient equality of rational streams, selected limit, countable-choice schedule, host real equality, trichotomy, ambient $\RealUp$ completeness theorem, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Project the accepted carrier $T=(L,D,W,R,E,H,C,P,N)$. The tail-bound data starts in $L$, and the only tolerance data available to the read is the dyadic ledger $D$.

The observation route is finite. The StreamName row $W$ exposes the bounded windows licensed by $L$ and $D$, and the RegSeqRat row $R$ is the first point where those windows are repacked as regular rational tail evidence.

The Real-facing consumer reaches the terminal seal only through $E$ and then through the structural rows $H,C,P,N$. These rows preserve and replay the same route; they do not add quotient stream equality, a selected limit, countable choice, host real equality, trichotomy, ambient completeness, Lean verification, or bridge checking.
\end{proof}

\falsifiablePrediction{Every $\RationalCauchyTailBoundUp$ read into RegSeqRat, Real, or Completion factors through one RationalIntervalLocator row, one DyadicRatCore tolerance ledger, one StreamName finite-window row, one RegSeqRat readback row, one terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{rational-interval-locator, dyadicratcore, streamname, regseqrat, real: $\RationalCauchyTailBoundUp$ is not bijective to any listed sibling because it jointly carries a rational interval locator, finite tail threshold, dyadic tolerance ledger, finite stream windows, regular rational readback, and the terminal RealUp seal that witnesses a bounded tail route rather than a completed quotient object.}

\closureat{RationalCauchyTailBoundUp}{seedStr}

\begin{closurestatus}{\RationalCauchyTailBoundUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rational-cauchy-tail-bound-carrier}, \autoref{thm:rational-cauchy-tail-bound-namecert-obligations}, and \autoref{thm:rational-cauchy-tail-bound-regular-real-handoff} seed the finite rational tail-bound route through RationalIntervalLocator source data, DyadicRatCore tolerance ledger, StreamName finite windows, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality of rational streams, selected limits, countable choice, host real equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, locator exactness, dyadic tolerance exactness, window compatibility, regular readback exactness, Real-seal nonescape, transport, replay, provenance, and handoff factorization for BEDC.Derived.RationalCauchyTailBoundUp.}
  \constructivestory{A $\RationalCauchyTailBoundUp$ packet is generated from one RationalIntervalLocator row, one DyadicRatCore tolerance ledger, one StreamName finite-window row, one RegSeqRat readback row, one terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
