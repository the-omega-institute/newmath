\chapter{A Concrete Naming Certificate for $\RegularCauchyApartnessBudgetUp$}
\label{ch:concrete-instances-regular-cauchy-apartness-budget-namecert}
\origin{ai}

The $\RegularCauchyApartnessBudgetUp$ packet realizes the Real and Completion direction of \autoref{ch:visions-real-completeness} as a finite nonzero-apartness budget certificate for regular Cauchy consumers. It consumes the regular source surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic lower-bound readback of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the Real seal boundary of \autoref{ch:concrete-instances-real-namecert}, and the reciprocal-window budget surface of \autoref{ch:concrete-instances-cauchyinversebudget-namecert}. The packet isolates the finite lower-bound row shared by reciprocal and apartness consumers before either consumer reaches a Real-facing seal.

\begin{definition}[Regular Cauchy apartness-budget carrier]
\label{def:regular-cauchy-apartness-budget-carrier}
A $\RegularCauchyApartnessBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(X,A,M,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the accepted $\RegSeqRatUp$ source whose nonzero lower-bound window is being read;
\item $A$ is the nonzero-apartness budget row, recording a finite positive lower-bound request for $X$;
\item $M$ is the finite modulus row selecting the observation precision at which the lower-bound request is inspected;
\item $W$ is the finite $\StreamNameUp$ window selected from $X$ by the budget and modulus rows;
\item $D$ is the $\DyadicRatCoreUp$ lower-bound readback row obtained from that window;
\item $R$ is the $\RegSeqRatUp$ readback row tying the dyadic lower-bound data back to the regular source;
\item $E$ is the $\RealUp$ seal row exposed only after $A,M,W,D,R$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for the source, apartness budget, modulus, window, dyadic lower bound, regular readback, Real seal, provenance, and local name;
\item $C$ records the displayed $\Cont$ routes for source inspection, apartness-budget exposure, finite-window coverage, dyadic lower-bound soundness, regular readback, and Real-seal handoff;
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, $\CauchyInverseBudgetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchyApartnessBudgetUp}$ row.
\end{enumerate}
The local classifier compares accepted packets only through the displayed rows $X,A,M,W,D,R,E,H,C,P,N$.
\end{definition}

\begin{theorem}[Regular Cauchy apartness-budget NameCert obligations]
\label{thm:regular-cauchy-apartness-budget-namecert-obligations}
For the carrier of \autoref{def:regular-cauchy-apartness-budget-carrier}, the local $\NameCert_{\RegularCauchyApartnessBudgetUp}$ surface consists of carrier admission, classifier stability, apartness-budget exposure, dyadic lower-bound soundness, and Real-seal non-escape for the same finite packet $B=(X,A,M,W,D,R,E,H,C,P,N)$. Carrier admission exposes exactly the listed rows. Classifier stability transports those rows componentwise through $H$ and replays the same routes through $C$. Apartness-budget exposure requires $A$ before any lower-bound or seal read. Dyadic lower-bound soundness requires $D$ to be read from the finite window $W$ selected by $M$. Real-seal non-escape requires every $\RealUp$ consumer to reach $E$ only after the regular readback $R$ has been displayed.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the displayed packet. The carrier rows are exactly $X,A,M,W,D,R,E,H,C,P,N$, so admission has no hidden coordinate outside the tuple.

Classifier stability is componentwise. The transport row $H$ compares the same source, budget, modulus, window, lower-bound, readback, seal, provenance, and name fields, while $C$ replays the displayed route in the transported packet.

The apartness and dyadic clauses follow from the order of the finite route. The budget row $A$ is read before the selected window and lower-bound row, and $D$ is a read of the finite StreamName window $W$ under the modulus row $M$.

The Real-seal clause then uses the regular readback row $R$. Since $E$ is the only Real seal coordinate and it is reached only through $R,H,C,P,N$, a seal-facing consumer cannot bypass the displayed apartness-budget and dyadic lower-bound rows.
\end{proof}

\begin{theorem}[Regular Cauchy apartness-budget non-escape]
\label{thm:regular-cauchy-apartness-budget-nonescape}
An accepted $\RegularCauchyApartnessBudgetUp$ packet exports only the rows
$$
X,A,M,W,D,R,E,H,C,P,N.
$$
It exports no host nonzero real, no trichotomy principle, no quotient stream, no selected representative, no total reciprocal operation, and no ambient completeness theorem. Any reciprocal-window, apartness, regular-Cauchy, dyadic, StreamName, or Real-facing consumer of the packet must read the displayed apartness-budget row before consuming the dyadic lower-bound row or the Real seal row.
\end{theorem}
\begin{proof}
Inspect the carrier definition. The packet has one regular source, one nonzero-apartness budget, one modulus row, one finite window, one dyadic lower-bound readback, one regular readback, one Real seal, and the structural rows $H,C,P,N$.

Apply the NameCert obligations of \autoref{thm:regular-cauchy-apartness-budget-namecert-obligations}. They force carrier admission, componentwise classifier stability, apartness-budget exposure, dyadic lower-bound soundness, and Real-seal non-escape to use those same fields.

The excluded objects are not packet fields. A host nonzero real, trichotomy branch, quotient stream, selected representative, total reciprocal operation, or ambient completeness theorem would require an additional coordinate or an additional route, neither of which is present in the accepted carrier.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudget\_nonescape}

\begin{theorem}[Regular Cauchy apartness-budget transport stability]
\label{thm:regular-cauchy-apartness-budget-transport-stability}
For accepted $\RegularCauchyApartnessBudgetUp$ packets, the lower-bound row is stable only componentwise through the displayed transport and continuation surface. If
$$
B=(X,A,M,W,D,R,E,H,C,P,N),
$$
then $H$ transports the regular source, apartness budget, modulus row, StreamName window, DyadicRatCore lower-bound row, RegSeqRat readback, Real seal, provenance, and local name componentwise, while $C$ records the corresponding displayed route stability
$$
\Cont(M,W),\qquad \Cont(W,D),\qquad \Cont(W,R),\qquad \Cont(R,E).
$$
No transported consumer may detach the lower-bound row $D$ from the apartness budget $A$ and the finite window $W$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudget\_transport\_stability}
\begin{proof}
Project the carrier rows. The only lower-bound coordinate is $D$, and it is placed after the apartness budget $A$, modulus row $M$, and StreamName window $W$.

Use the NameCert obligations. They make classifier stability componentwise through $H$ and replay the lower-bound and readback routes through $C$.

The non-escape theorem then blocks any transported read that asks for a detached lower bound. Such a read would require a lower-bound coordinate outside $X,A,M,W,D,R,E,H,C,P,N$, which the accepted packet does not contain.
\end{proof}

\begin{theorem}[Regular Cauchy apartness-budget lower-bound scope]
\label{thm:regular-cauchy-apartness-budget-lower-bound-scope}
Every accepted $\RegularCauchyApartnessBudgetUp$ packet exposes its dyadic lower-bound row only through the finite route
$$
A,M,W,D,R,H,C,P,N .
$$
Here $A$ is the apartness budget, $M$ is the modulus row, $W$ is the $\StreamNameUp$ window, $D$ is the $\DyadicRatCoreUp$ lower-bound read, and $R$ is the $\RegSeqRatUp$ readback. A $\RealUp$ or reciprocal-window consumer may therefore read $D$ only after the displayed route has fixed those rows, and cannot read $D$ outside the finite packet $X,A,M,W,D,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The lower-bound coordinate is the single displayed row $D$, and the rows immediately controlling it are the apartness budget $A$, modulus $M$, finite window $W$, and regular readback $R$.

Apply the NameCert obligations of \autoref{thm:regular-cauchy-apartness-budget-namecert-obligations}. They require apartness-budget exposure before the lower-bound read, force $D$ to be read from the selected finite window, and keep the regular readback on the same displayed packet.

Apply transport stability, \autoref{thm:regular-cauchy-apartness-budget-transport-stability}. The rows $H$ and $C$ preserve the lower-bound route componentwise and replay only the displayed continuations. Thus any $\RealUp$ or reciprocal-window consumer that tries to read $D$ without $A,M,W,R,H,C,P,N$ leaves the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetCarrier\_lower\_bound\_scope}

\begin{theorem}[Regular Cauchy apartness-budget lower-bound determinacy]
\label{thm:regular-cauchy-apartness-budget-lower-bound-determinacy}
Let
$$
B=(X,A,M,W,D,R,E,H,C,P,N)
$$
and
$$
B'=(X',A,M,W,D,R,E',H',C',P',N')
$$
be accepted $\RegularCauchyApartnessBudgetUp$ packets. If the two packets share the displayed apartness row $A$, modulus row $M$, $\StreamNameUp$ window $W$, $\DyadicRatCoreUp$ lower-bound row $D$, $\RegSeqRatUp$ readback row $R$, and the structural rows governing the lower-bound route, then they expose the same dyadic lower-bound handoff to every $\RealUp$ and reciprocal-window consumer. The handoff cannot be changed by a different source seal, host nonzero witness, trichotomy branch, quotient stream, selected representative, or ambient completeness principle.
\end{theorem}
\begin{proof}
Apply lower-bound scope, \autoref{thm:regular-cauchy-apartness-budget-lower-bound-scope}. It confines every lower-bound read to the displayed route $A,M,W,D,R,H,C,P,N$ and does not admit an additional lower-bound coordinate.

Use transport stability, \autoref{thm:regular-cauchy-apartness-budget-transport-stability}. The shared structural rows keep the apartness budget, modulus, window, dyadic lower bound, regular readback, provenance, and local naming componentwise fixed, while the displayed continuations replay the same route.

Apply the reciprocal-window boundary, \autoref{thm:regular-cauchy-apartness-budget-reciprocal-window-boundary}. A reciprocal consumer can only consume the lower-bound handoff through the same finite window and readback rows, and a $\RealUp$ consumer reaches the seal only after that handoff is displayed.

Finally use non-escape, \autoref{thm:regular-cauchy-apartness-budget-nonescape}. The alternative objects listed in the statement are not fields of either accepted packet, so they cannot distinguish the handoff determined by the shared lower-bound route.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetCarrier\_lower\_bound\_determinacy}
\leanvariant{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetLowerBoundDeterminacy}

\begin{theorem}[Regular Cauchy apartness-budget Real seal refusal]
\label{thm:regular-cauchy-apartness-budget-real-seal-refusal}
No $\RealUp$ seal or reciprocal-window read is public from a $\RegularCauchyApartnessBudgetUp$ packet unless the apartness-budget row $A$ has already been read. A route that exposes $E$ or a reciprocal lower-bound consumer while omitting $A$ is not a carrier read of the accepted packet.
\end{theorem}
\begin{proof}
Read the order of the displayed packet. The apartness-budget row $A$ is the only public nonzero-budget coordinate, and the lower-bound and seal rows occur downstream of the finite window route.

Apply transport stability for the lower-bound row. It keeps $D$ attached to $A,M,W$ under $H$ and $C$, so a reciprocal-window consumer cannot read $D$ as an independent public datum.

Finally apply non-escape. The Real seal row $E$ is exposed only through $R,H,C,P,N$ after the same apartness and lower-bound route, so a consumer omitting $A$ leaves the public surface.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetCarrier\_real\_seal\_refusal}

\begin{theorem}[Regular Cauchy apartness-budget reciprocal-window boundary]
\label{thm:regular-cauchy-apartness-budget-reciprocal-window-boundary}
Every reciprocal-window consumer of an accepted $\RegularCauchyApartnessBudgetUp$ packet factors through the $\DyadicRatCoreUp$ lower-bound row, the $\StreamNameUp$ finite-window row, the $\RegSeqRatUp$ readback row, and the local $\NameCert$ rows
$$
A,M,W,D,R,H,C,P,N.
$$
The boundary exposes a finite apartness budget and finite readback route, not a total reciprocal operation, host nonzero real, quotient stream, selected representative, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start from the NameCert obligations. They require apartness-budget exposure before the dyadic lower-bound row and require that lower bound to be read from the finite StreamName window selected by the modulus row.

Use transport stability to keep the $\DyadicRatCoreUp$, $\StreamNameUp$, and $\RegSeqRatUp$ rows on the same displayed route under $H$ and $C$.

Apply the Real seal refusal and non-escape theorems. A reciprocal-window consumer that asks for a total reciprocal or host-level nonzero fact asks for data not carried by $A,M,W,D,R,H,C,P,N$, so the public boundary is exactly the finite route stated above.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetCarrier\_reciprocal\_window\_boundary}

\begin{theorem}[Regular Cauchy apartness-budget public interface]
\label{thm:regular-cauchy-apartness-budget-public-interface}
Every public reciprocal-window or $\RealUp$-seal consumer of an accepted $\RegularCauchyApartnessBudgetUp$ packet
$$
B=(X,A,M,W,D,R,E,H,C,P,N)
$$
factors through the same finite interface
$$
A,M,W,D,R,E,H,C,P,N.
$$
This interface composes lower-bound scope, lower-bound determinacy, Real-seal refusal, and reciprocal-window boundary. It exports no host nonzero real, trichotomy principle, quotient stream, selected representative, total reciprocal operation, or ambient completeness theorem.
\end{theorem}
\begin{proof}
First apply lower-bound scope, \autoref{thm:regular-cauchy-apartness-budget-lower-bound-scope}. It confines every lower-bound read to the apartness row, modulus row, finite window, dyadic lower-bound row, regular readback, transport, replay, provenance, and local naming rows.

Next apply lower-bound determinacy, \autoref{thm:regular-cauchy-apartness-budget-lower-bound-determinacy}. Once $A,M,W,D,R$ and the structural rows are fixed, a public consumer has no second carrier coordinate from which to read a different dyadic lower-bound handoff.

Use Real-seal refusal, \autoref{thm:regular-cauchy-apartness-budget-real-seal-refusal}, before any $\RealUp$-seal consumer reaches $E$. The seal is public only after the same apartness and lower-bound route has been displayed.

Finally apply the reciprocal-window boundary, \autoref{thm:regular-cauchy-apartness-budget-reciprocal-window-boundary}. A reciprocal consumer reads exactly the finite route $A,M,W,D,R,H,C,P,N$, and a Real-seal consumer adds only the downstream seal row $E$ on that route. Since non-escape excludes host nonzero reals, trichotomy, quotient streams, selected representatives, total reciprocals, and ambient completeness, the listed interface is the full public surface.
\end{proof}

\begin{theorem}[Regular Cauchy apartness-budget positive-window determinacy]
\label{thm:regular-cauchy-apartness-budget-positive-window-determinacy}
Let $B=(X,A,M,W,D,R,E,H,C,P,N)$ and $B'=(X',A,M,W,D,R,E',H',C',P',N')$ be accepted $\RegularCauchyApartnessBudgetUp$ packets with the same displayed apartness row $A$, modulus row $M$, $\StreamNameUp$ window $W$, $\DyadicRatCoreUp$ lower-bound row $D$, $\RegSeqRatUp$ readback $R$, and structural rows for the lower-bound route. Then every reciprocal-window consumer reads the same positive lower-bound handoff from $B$ and $B'$. No host nonzero real, trichotomy branch, quotient stream, or selected representative can alter that read.
\end{theorem}
\begin{proof}
Project both accepted carriers. The lower-bound route in each packet is displayed through the same rows $A,M,W,D,R$ together with the structural transport, replay, provenance, and naming rows for that route.

Apply lower-bound scope, \autoref{thm:regular-cauchy-apartness-budget-lower-bound-scope}. It makes the dyadic lower-bound read depend only on the apartness budget, modulus, StreamName window, DyadicRatCore row, regular readback, and local structural rows.

Use transport stability, \autoref{thm:regular-cauchy-apartness-budget-transport-stability}, to keep this route componentwise fixed. Then apply the reciprocal-window boundary theorem \autoref{thm:regular-cauchy-apartness-budget-reciprocal-window-boundary}: a reciprocal consumer has no remaining coordinate from which to read a different positive lower bound.

The non-escape and Real-seal refusal clauses remove host nonzero reals, trichotomy branches, quotient streams, and selected representatives from the packet. Hence those external objects cannot change the finite lower-bound handoff determined by the shared window rows.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetCarrier\_positive\_window\_determinacy}

\begin{theorem}[Regular Cauchy apartness-budget lower-bound modulus monotonicity]
\label{thm:regular-cauchy-apartness-budget-lower-bound-modulus-monotonicity}
Let
$$
B=(X,A,M,W,D,R,E,H,C,P,N)
$$
and
$$
B'=(X',A,M',W',D,R,E',H',C',P',N')
$$
be accepted $\RegularCauchyApartnessBudgetUp$ packets sharing the apartness row $A$, $\DyadicRatCoreUp$ lower-bound row $D$, and $\RegSeqRatUp$ readback row $R$. If the modulus and window pair $(M',W')$ refines $(M,W)$ through the displayed $\StreamNameUp$ and $\Cont$ route, then every $\RealUp$ or reciprocal-window consumer reads from $B'$ a lower-bound handoff no weaker than the one read from $B$. The refinement may add displayed window information, but it cannot replace the apartness row, weaken the dyadic lower-bound row, detach the regular readback, or import a host nonzero real, trichotomy branch, quotient stream, selected representative, total reciprocal operation, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier rows of both packets. The shared lower-bound data are the apartness row $A$, the dyadic lower-bound row $D$, and the regular readback $R$, while the only changing rows in the lower-bound route are the modulus and window pair.

Apply lower-bound scope, \autoref{thm:regular-cauchy-apartness-budget-lower-bound-scope}. It binds every lower-bound read to the finite route $A,M,W,D,R,H,C,P,N$ in the coarser packet and to the analogous route $A,M',W',D,R,H',C',P',N'$ in the refined packet.

Use transport stability to replay the refinement through $H,H'$ and $C,C'$. The displayed $\StreamNameUp$ and $\Cont$ route preserves the apartness row, dyadic lower-bound row, and regular readback, so a consumer can only receive the same lower-bound handoff or one exposed through a finer finite window.

Apply positive-window determinacy, \autoref{thm:regular-cauchy-apartness-budget-positive-window-determinacy}. With $A,D,R$ fixed and the refined window routed through the same displayed lower-bound data, there is no carrier coordinate from which a changed or weaker handoff can be read.

Finally use non-escape. Host nonzero reals, trichotomy, quotient streams, selected representatives, total reciprocals, and ambient completeness are not packet fields, so they cannot reverse the monotone refinement of the lower-bound route.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetCarrier\_lower\_bound\_modulus\_monotonicity}

\begin{theorem}[Regular Cauchy apartness-budget L10 scope binding]
\label{thm:regular-cauchy-apartness-budget-l10-scope-binding}
Every accepted apartness-budget read exported by a $\RegularCauchyApartnessBudgetUp$ packet factors through \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-real-namecert}, and \autoref{ch:concrete-instances-cauchyinversebudget-namecert} before any Real-facing apartness consumer may use the budget. In carrier terms, the source row $X$, apartness row $A$, modulus row $M$, window row $W$, dyadic lower-bound row $D$, regular readback row $R$, Real seal row $E$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$ must all remain on the same finite L10 route. No consumer may detach the budget from this dependency lattice into a host nonzero real, trichotomy branch, quotient stream, selected representative, total reciprocal operation, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-apartness-budget-carrier}. The accepted packet places the regular source, apartness budget, modulus, finite window, dyadic lower bound, regular readback, Real seal, transport, replay, provenance, and local naming rows in one tuple.

Apply the NameCert obligations and lower-bound scope. These force the apartness row to precede the StreamName window and DyadicRatCore lower-bound read, and they keep the RegSeqRat readback on the same route before any Real seal is consumed.

Use the reciprocal-window boundary and positive-window determinacy. A Cauchy inverse or reciprocal-window consumer reads the same positive lower-bound handoff only through the displayed finite window and readback rows.

Finally apply non-escape and Real-seal refusal. The excluded host objects are not carrier coordinates, so every Real-facing apartness consumer remains bound to the stated L10 dependency lattice.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyApartnessBudgetUp.RegularCauchyApartnessBudgetCarrier\_l10\_scope\_binding}

\falsifiablePrediction{Any accepted reciprocal-window consumer that omits the displayed apartness-budget row is not a RegularCauchyApartnessBudgetUp carrier read.}

\independenceWitness{Nearest siblings: cauchyinversebudget, apartnessreal, regseqrat, dyadicratcore, streamname. The carrier is not bijective to CauchyInverseBudgetUp because it has no reciprocal readback row; not to ApartnessRealUp because it keeps finite regular-Cauchy window budgets rather than a completed Real apartness predicate; not to RegSeqRatUp because it adds nonzero lower-bound rows; not to DyadicRatCoreUp because it includes stream-window and Real-seal routes; not to StreamNameUp because it includes dyadic lower-bound and apartness ledger rows.}

The Lean TasteGate route is expected to provide \texttt{Nontrivial RegularCauchyApartnessBudgetUp} with an explicit witness pair and \texttt{FieldFaithful RegularCauchyApartnessBudgetUp} with \texttt{.mk}-derived fields and a match/injection proof.

\closureat{RegularCauchyApartnessBudgetUp}{scopedStr}

\begin{closurestatus}{\RegularCauchyApartnessBudgetUp}
  \constructivestory{A $\RegularCauchyApartnessBudgetUp$ packet is generated from a regular-Cauchy source, a finite nonzero-apartness budget, a modulus row, a finite StreamName window, a DyadicRatCore lower-bound readback, a RegSeqRat readback, a RealUp seal, componentwise transports, displayed routes, provenance, and local naming.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:regular-cauchy-apartness-budget-l10-scope-binding} gives the exact L10 dependency scope over $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, $\CauchyInverseBudgetUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before Real-seal or reciprocal-window consumption.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, host nonzero reals, trichotomy, quotient streams, selected representatives, total reciprocal operations, ambient completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, apartness-budget exposure, dyadic lower-bound soundness, Real-seal non-escape, provenance, and TasteGate alignment for the same finite packet.}
\end{closurestatus}
