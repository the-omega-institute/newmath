\chapter{A Concrete Naming Certificate for $\RealCompletionAdjunctionUp$}
\label{ch:concrete-instances-real-completion-adjunction-namecert}
\origin{human}

Following Bishop and Bridges on constructive completion and Mac Lane on adjunctions, the $\RealCompletionAdjunctionUp$ packet records the finite unit/counit surface by which dyadic, stream, regular-rational, and real-name rows support a completion-facing universal route. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-name discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names Real completion as displayed unit and counit data rather than as quotient completion equality, selected limits, ambient $\RealUp$ completeness, or open-cover compactness.

\begin{definition}[Real completion adjunction carrier]
\label{def:real-completion-adjunction-carrier}
A $\RealCompletionAdjunctionUp$ carrier is a finite $\BHist$ packet
$$
A=(D,S,Q,R,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording precision cells, refinement slack, and comparison budgets used by the completion request.
\item $S$ is the $\StreamNameUp$ finite-window ledger, listing the source windows inspected before any real-name seal or completion endpoint is read.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to those windows, exposing the regular rational data consumed by the completion route.
\item $R$ is the terminal $\RealUp$ seal row reached only after $D,S,Q$ have been displayed.
\item $U$ is the unit row, routing the displayed rational or regular-rational source into the Real-completion surface without choosing a quotient representative.
\item $E$ is the counit and extension row, recording the finite handoff from a completion-facing consumer back through the same $\RealUp$ seal and readback data.
\item $H$ records componentwise $\hsame$ transport for the dyadic, stream, readback, Real seal, unit, counit, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
D,S,Q \longmapsto R \longmapsto U,E .
$$
\item $P$ is the $\Pkg$ provenance over $\RealCompletionAdjunctionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealCompletionAdjunctionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,Q,R,U,E,H,C,P,N$. It has no coordinate for quotient completion equality, selected limits, a host Cauchy-real quotient, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real completion adjunction NameCert obligations]
\label{thm:real-completion-adjunction-namecert-obligations}
For every accepted $\RealCompletionAdjunctionUp$ carrier
$$
A=(D,S,Q,R,U,E,H,C,P,N),
$$
the local $\NameCert_{\RealCompletionAdjunctionUp}$ surface consists exactly of dyadic tolerance exposure through $D$, stream-window exposure through $S$, regular-rational readback through $Q$, terminal Real seal handoff through $R$, unit exposure through $U$, counit and extension exposure through $E$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The projection exposes the dyadic tolerance ledger, finite stream windows, regular-rational readback, Real seal, unit, counit, transport, replay, provenance, and naming rows.

The completion-facing obligations are read in that order. The dyadic ledger fixes the precision budget, the stream ledger fixes the inspected finite windows, and the regular-rational readback is the only route by which the terminal Real seal is reached.

The unit and counit rows then record the finite adjunction surface over the same data. Componentwise transport and displayed replay preserve the route, while provenance and the local NameCert row bind it as one packet. Since no quotient representative, selected limit, open-cover compactness row, or ambient completeness theorem occurs in the carrier, the listed rows exhaust the local obligation surface.
\end{proof}

\begin{theorem}[Real completion adjunction unit-counit scope]
\label{thm:real-completion-adjunction-unit-counit-scope}
Every completion-facing consumer of an accepted $\RealCompletionAdjunctionUp$ packet reads the unit and counit only after the route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
has been displayed through $D,S,Q,R,H,C,P,N$. The handoff supplies finite Real-completion adjunction data; it does not supply quotient completion equality, selected limits, ambient $\RealUp$ completeness, open-cover compactness, host real equality, or countable choice.
\end{theorem}
\begin{proof}
Start with the source rows $D,S,Q$. They provide the finite tolerance, window, and regular-rational readback data needed before a Real-completion request is admitted.

Next read the terminal seal $R$. The $\RealUp$ row is downstream of those source rows, so a consumer cannot detach the unit or counit from the dyadic, stream, and readback ledgers.

Finally inspect $U$ and $E$ with $H,C,P,N$. These rows bind the unit and counit to the same displayed carrier and replay route. No excluded quotient, selected-limit, compactness, choice, host-equality, or ambient-completeness coordinate is present, so the unit-counit scope is exactly the finite NameCert surface named above.
\end{proof}

\falsifiablePrediction{Every accepted $\RealCompletionAdjunctionUp$ consumer reads dyadic tolerance, finite stream windows, regular-rational readback, Real seal, unit, counit, hsame transport, Cont replay, Pkg provenance, and local NameCert rows before using a Real-completion adjunction endpoint.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real: $\RealCompletionAdjunctionUp$ is not bijective to any listed sibling because it binds the four L10 faces to explicit unit and counit rows with transport, replay, provenance, and local naming in one finite packet.}

\closureat{RealCompletionAdjunctionUp}{seedStr}

\begin{closurestatus}{\RealCompletionAdjunctionUp}
  \constructivestory{A $\RealCompletionAdjunctionUp$ packet is generated from a DyadicRatCore tolerance ledger, finite StreamName windows, RegSeqRat readback, RealUp seal, unit row, counit row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-completion-adjunction-carrier}, \autoref{thm:real-completion-adjunction-namecert-obligations}, and \autoref{thm:real-completion-adjunction-unit-counit-scope} seed the finite Real-completion adjunction packet over DyadicRatCore, StreamName, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected limits, ambient RealUp completeness, open-cover compactness, host real equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exposure, stream-window exposure, regular-rational readback, Real seal handoff, unit and counit exactness, transport, replay, provenance, and local naming for BEDC.Derived.RealCompletionAdjunctionUp.}
\end{closurestatus}
