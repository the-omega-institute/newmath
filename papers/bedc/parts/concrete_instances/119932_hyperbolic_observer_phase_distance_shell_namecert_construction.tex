\chapter{A Concrete Naming Certificate for $\HyperbolicObserverPhaseDistanceShellUp$}
\label{ch:concrete-instances-hyperbolic-observer-phase-distance-shell-namecert}
\origin{ai}

The $\HyperbolicObserverPhaseDistanceShellUp$ packet realizes the observer phase-distance shell route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite boundary-shadow, visual-boundary, observer-window, phase-distance, shell, provenance, and local NameCert rows. It sits beside the observer shell of \autoref{ch:concrete-instances-hyperbolic-observer-shell-namecert}, the phase-distance observer-expansion packet of \autoref{ch:concrete-instances-phase-distance-observer-expansion-namecert}, the phase-space packet of \autoref{ch:concrete-instances-hyperbolic-phase-space-namecert}, the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, and the visual-boundary packet of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}. The packet names the joint observer shell and phase-distance boundary rather than a host hyperbolic space, physical observer, quotient ideal boundary, selected geodesic ray, or ambient $\RealUp$ completeness theorem.

\begin{definition}[Hyperbolic observer phase-distance shell carrier]
\label{def:hyperbolic-observer-phase-distance-shell-carrier}
A $\HyperbolicObserverPhaseDistanceShellUp$ carrier is a finite $\BHist$ packet
$$
S=(B,V,O,D,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row exposing the boundary-shadow window seen by the observer.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row reading the same shadow window as visual-boundary data.
\item $O$ is the $\HyperbolicObserverShellUp$ row that records the observer-window shell over boundary and expansion data.
\item $D$ is the $\PhaseDistanceObserverExpansionUp$ row exposing the finite phase-distance comparison for the same observer window.
\item $Q$ is the $\HyperbolicPhaseSpaceUp$ row that keeps the reversible phase-space route tied to displayed disk, boundary, refusal, and provenance rows.
\item $R$ is the shell-radius and provenance ledger, recording which boundary-shadow, visual-boundary, observer-window, and phase-distance rows are jointly read.
\item $H$ records componentwise $\hsame$ transport for boundary-shadow, visual-boundary, observer-shell, phase-distance, phase-space, ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an observer phase-distance request through $B,V,O,D,Q,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverPhaseDistanceShellUp$, $\HyperbolicObserverShellUp$, $\PhaseDistanceObserverExpansionUp$, $\HyperbolicPhaseSpaceUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicObserverPhaseDistanceShellUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,V,O,D,Q,R,H,C,P,N$. It has no coordinate for a completed hyperbolic space, physical observer trajectory, quotient ideal-boundary equality, selected geodesic ray, hidden visual-boundary completion, or ambient real completeness.
\end{definition}

\begin{theorem}[Hyperbolic observer phase-distance shell NameCert obligations]
\label{thm:hyperbolic-observer-phase-distance-shell-namecert-obligations}
For every accepted $\HyperbolicObserverPhaseDistanceShellUp$ carrier $S=(B,V,O,D,Q,R,H,C,P,N)$, the local $\NameCert_{\HyperbolicObserverPhaseDistanceShellUp}$ surface consists exactly of boundary-shadow admission, visual-boundary readback, observer-shell admission, phase-distance comparison, hyperbolic phase-space routing, shell-radius provenance, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-observer-phase-distance-shell-carrier}. The visible geometric rows are $B,V,O,D,Q,R$, and each is displayed before transport, replay, provenance, or local naming is consumed.

The boundary rows $B,V$ anchor the shell at the visual boundary. The observer row $O$ and phase-distance row $D$ force the observer-window and distance comparison to be read together, while $Q$ keeps the route inside the finite phase-space packet.

The structural rows $H,C,P,N$ preserve that same finite packet. Since no excluded coordinate occurs in $S$, the NameCert obligations cannot export a completed hyperbolic space, physical observer trajectory, quotient ideal boundary, selected geodesic ray, hidden visual-boundary completion, or ambient real completeness.
\end{proof}

\begin{theorem}[Hyperbolic observer phase-distance shell vision handoff]
\label{thm:hyperbolic-observer-phase-distance-shell-vision-handoff}
The $\HyperbolicObserverPhaseDistanceShellUp$ packet realizes the boundary-shadow, phase-distance, observer-window, and provenance rows of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as one finite NameCert carrier. Every consumer of the shell must first read $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicObserverShellUp$, $\PhaseDistanceObserverExpansionUp$, and $\HyperbolicPhaseSpaceUp$ rows, then the ledger, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:hyperbolic-observer-phase-distance-shell-namecert-obligations}. It orders every shell read through the boundary-shadow, visual-boundary, observer-shell, phase-distance, phase-space, and shell-radius rows before any consumer receives a result.

This route is a vision handoff because the cited vision chapter names exactly the observer boundary, phase-distance, shell, and provenance rows that the carrier displays. The handoff is therefore concrete finite NameCert data rather than a prose-only observer geometry.

Finally inspect the refused coordinates in \autoref{def:hyperbolic-observer-phase-distance-shell-carrier}. The carrier contains no completed hyperbolic space, physical observer path, quotient ideal-boundary equality, selected geodesic ray, hidden visual-boundary completion, or ambient completeness row, so no such object can be exported by the handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicObserverPhaseDistanceShellUp$ read exposes HyperbolicBoundaryShadow, HyperbolicVisualBoundary, HyperbolicObserverShell, PhaseDistanceObserverExpansion, HyperbolicPhaseSpace, shell-radius provenance, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows before the phase-distance shell handoff is consumed.}

\independenceWitness{hyperbolic-observer-shell, phase-distance-observer-expansion, hyperbolic-phase-space, hyperbolic-boundary-shadow, hyperbolic-visual-boundary: $\HyperbolicObserverShellUp$ lacks the phase-distance row, $\PhaseDistanceObserverExpansionUp$ lacks the observer shell and boundary-shadow ledger, $\HyperbolicPhaseSpaceUp$ lacks the observer-window shell, $\HyperbolicBoundaryShadowUp$ lacks the phase-distance comparison, and $\HyperbolicVisualBoundaryUp$ lacks the joint observer-shell provenance. None is bijective to $\HyperbolicObserverPhaseDistanceShellUp$ under displayed NameCert-row preservation.}

\closureat{HyperbolicObserverPhaseDistanceShellUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverPhaseDistanceShellUp}
  \constructivestory{A $\HyperbolicObserverPhaseDistanceShellUp$ packet is generated from HyperbolicBoundaryShadow rows, HyperbolicVisualBoundary rows, HyperbolicObserverShell rows, PhaseDistanceObserverExpansion rows, HyperbolicPhaseSpace rows, a shell-radius provenance ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-phase-distance-shell-carrier}, \autoref{thm:hyperbolic-observer-phase-distance-shell-namecert-obligations}, and \autoref{thm:hyperbolic-observer-phase-distance-shell-vision-handoff} seed the finite observer phase-distance shell over boundary-shadow, visual-boundary, observer-shell, phase-distance, phase-space, ledger, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic spaces, physical observer trajectories, quotient ideal-boundary equality, selected geodesic rays, hidden visual-boundary completion, ambient real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow admission, visual-boundary readback, observer-shell admission, phase-distance comparison, phase-space routing, shell-radius provenance, transport, replay, and local naming for BEDC.Derived.HyperbolicObserverPhaseDistanceShellUp.}
\end{closurestatus}
