\begin{theorem}[Helly finite convex-family budget]
\label{thm:helly-theorem-finite-convex-family-budget}
Every accepted $\HellyTheoremUp$ finite-family read must expose $\ConvexSetUp$, $\PolytopeUp$, $\CompactUp$, $\VecSpaceUp$, $\AffineSpaceUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before it exports a finite intersection witness. In particular, the convex-set membership rows of \autoref{ch:concrete-instances-convexset-namecert}, the finite witness rows of \autoref{ch:concrete-instances-polytope-namecert}, the compact finite-read rows of \autoref{ch:concrete-instances-compact-namecert}, and the ambient affine and vector rows of \autoref{ch:concrete-instances-affinespace-namecert} and \autoref{ch:concrete-instances-vecspace-namecert} form the displayed dependency route for the local intersection ledger.
\end{theorem}
\begin{proof}
Project the $\HellyTheoremUp$ carrier from \autoref{def:helly-theorem-carrier}. The convex-family coordinate $F$ is read through the $\ConvexSetUp$ surface, the witness coordinate $W$ is read through $\PolytopeUp$, and the ambient rows $A,V$ are read through $\AffineSpaceUp$ and $\VecSpaceUp$ before the finite subfamily ledger $S$ can be inspected.

The compact finite-read discipline supplies the bounded inspection surface for those finite subfamilies. Transport, continuation replay, provenance, and local naming are then carried by the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Therefore the exported finite intersection witness is downstream of the sibling convex, polytope, compact, vector, and affine chapters rather than a detached host convexity fact.
\end{proof}
