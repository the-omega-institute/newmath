The $\HellyTheoremUp$ packet consumes the finite-intersection lattice of \autoref{ch:concrete-instances-finite-intersection-property-namecert} before any Helly carrier can export an intersection witness. The route also reads the convex carrier rows of \autoref{ch:concrete-instances-convexset-namecert} and the compact finite-cover boundary of \autoref{ch:concrete-instances-compactmetric-namecert}; together with the local sibling route of \autoref{thm:helly-theorem-finite-subfamily-ledger-route}, these rows force the witness to be a finite NameCert consequence rather than a kernel-only intersection assertion.
