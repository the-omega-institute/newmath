The finite subfamily ledger exported by $\HellyTheoremUp$ is consumed through a concrete sibling route: $\ConvexSetUp$ from \autoref{ch:concrete-instances-convexset-namecert} supplies the convex-family membership rows, $\PolytopeUp$ from \autoref{ch:concrete-instances-polytope-namecert} supplies the finite witness ledger, $\AffineSpaceUp$ from \autoref{ch:concrete-instances-affinespace-namecert} and $\VecSpaceUp$ from \autoref{ch:concrete-instances-vecspace-namecert} supply the ambient affine and displacement rows, $\CompactMetricUp$ from \autoref{ch:concrete-instances-compactmetric-namecert} supplies the finite compact-read boundary, and $\CaratheodoryConvexHullUp$ from \autoref{ch:concrete-instances-caratheodory-convex-hull-namecert} consumes the resulting bounded support surface.
