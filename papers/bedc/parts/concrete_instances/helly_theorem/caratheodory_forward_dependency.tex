The finite subfamily ledger exported by $\HellyTheoremUp$ is consumed by \autoref{ch:concrete-instances-caratheodory-convex-hull-namecert} through a concrete dependency route: first the convex-family membership rows are read from \autoref{ch:concrete-instances-convexset-namecert}, then the finite witness ledger is read from \autoref{ch:concrete-instances-polytope-namecert}, then the Helly full-family intersection row is exposed, and only after those rows are fixed may the Caratheodory support-reduction packet read a bounded support surface. This route records a downstream use of $\HellyTheoremUp$ without adding a theorem environment to the hub.
