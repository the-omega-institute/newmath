The finite subfamily ledger exported by $\HellyTheoremUp$ is consumed by \autoref{ch:concrete-instances-caratheodory-convex-hull-namecert} through a concrete dependency route: first the convex-family membership rows are read from \autoref{ch:concrete-instances-convexset-namecert}, then the finite witness ledger is read from \autoref{ch:concrete-instances-polytope-namecert}, then the Helly full-family intersection row is exposed, and only after those rows are fixed may the Caratheodory support-reduction packet read a bounded support surface. This route records a downstream use of $\HellyTheoremUp$ without adding a theorem environment to the hub.

\begin{theorem}[Helly Caratheodory forward dependency]
\label{thm:helly-theorem-caratheodory-forward-dependency}
Every $\HellyTheoremUp$ finite-subfamily read that is exported to a convex-hull consumer must first consume the $\CaratheodoryConvexHullUp$ support-reduction route of \autoref{ch:concrete-instances-caratheodory-convex-hull-namecert} and the convex-hull carrier rows already displayed in \autoref{thm:helly-theorem-convex-hull-forward-link}. Only after those rows are visible may the packet export the Helly finite-intersection route of \autoref{thm:helly-theorem-convex-polytope-dependency-route}. The dependency is a finite forward read through the displayed convex-family, polytope, Caratheodory, and local $\NameCert_{\HellyTheoremUp}$ rows, not a host separation theorem, selected intersection point, or ambient convexity principle.
\end{theorem}
\begin{proof}
Project the existing Helly dependency route. The convex-family rows and finite witness rows are displayed before the Helly subfamily ledger is inspected.

Next read the Caratheodory convex-hull surface. The support-reduction packet consumes the displayed subfamily ledger only as finite bounded-support data, so the convex-hull carrier remains upstream of the Helly finite-intersection export.

Finally repack the rows into the local Helly NameCert route. Since no row names a host separation theorem, selected intersection point, or ambient convexity principle, the exported dependency is exactly the finite forward route named in the statement.
\end{proof}

\begin{theorem}[Helly Caratheodory sibling route]
\label{thm:helly-theorem-caratheodory-sibling-route}
The $\HellyTheoremUp$ finite-subfamily ledger is available to the $\CaratheodoryConvexHullUp$ consumer of \autoref{ch:concrete-instances-caratheodory-convex-hull-namecert} only after the $\ConvexSetUp$, $\PolytopeUp$, $\AffineSpaceUp$, $\VecSpaceUp$, and $\CompactMetricUp$ rows expose the finite convex-family boundary. The consumed route is the displayed finite ledger through those rows and the local $\NameCert_{\HellyTheoremUp}$ packet; it is not a detached convexity principle, a selected intersection point, or a host support-reduction theorem.
\end{theorem}
\begin{proof}
Project the Helly ledger as a finite subfamily packet. Its convex-family membership, polytope witness, affine and vector-space coordinates, and compactmetric boundary rows must be visible before a convex-hull consumer can read the finite-intersection surface.

Then pass the exposed ledger to the Caratheodory consumer. The consumer receives bounded-support data from the displayed packet, so the route remains tied to the same local NameCert rows.

No additional coordinate names host convexity, selected intersection points, or an external support theorem. The sibling route is therefore the finite dependency stated above.
\end{proof}

\begin{theorem}[Helly Caratheodory finite-dependency audit]
\label{thm:helly-theorem-caratheodory-finite-dependency-audit}
Any $\HellyTheoremUp$ finite-subfamily ledger exported to $\CaratheodoryConvexHullUp$ must display $\ConvexSetUp$ membership, $\PolytopeUp$ finite witnesses, $\AffineSpaceUp$ and $\VecSpaceUp$ ambient rows, the $\CompactMetricUp$ finite-read boundary, and the local $\NameCert_{\HellyTheoremUp}$ row before the Caratheodory bounded-support surface is consumed. The route exports no host separation theorem, selected intersection point, quotient convex-set equality, or ambient convexity principle.
\end{theorem}
\begin{proof}
Start with the forward dependency theorem, \autoref{thm:helly-theorem-caratheodory-forward-dependency}. It places the convex-family and finite-witness rows upstream of any convex-hull consumer.

Apply the sibling route theorem, \autoref{thm:helly-theorem-caratheodory-sibling-route}. It refines the dependency surface to $\ConvexSetUp$, $\PolytopeUp$, $\AffineSpaceUp$, $\VecSpaceUp$, $\CompactMetricUp$, and the local Helly naming row before the Caratheodory surface is read.

The carrier non-escape boundary in \autoref{ch:concrete-instances-helly-theorem-namecert} excludes host convex-set equality, choice-selected intersection points, separation theorems, unlisted compactness principles, and infinite-family claims. Hence the Caratheodory handoff is exactly the finite dependency audit stated above.
\end{proof}
