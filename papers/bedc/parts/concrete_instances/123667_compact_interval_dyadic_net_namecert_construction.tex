\chapter{A Concrete Naming Certificate for $\CompactIntervalDyadicNetUp$}
\label{ch:concrete-instances-compact-interval-dyadic-net-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactIntervalDyadicNetUp$ packet records the finite dyadic-net surface of a compact rational interval as displayed NameCert data. It consumes the dyadic arithmetic spine of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal seal-facing rows of \autoref{ch:concrete-instances-real-namecert}, and the located endpoint discipline of \autoref{ch:concrete-instances-locatedinterval-namecert}. The packet names only a finite mesh and its readback route; it does not import open-cover compactness, selected subcovers, quotient real equality, trichotomy, countable choice, or $\RealUp$ completeness.

\begin{definition}[Compact interval dyadic-net carrier]
\label{def:compact-interval-dyadic-net-carrier}
A $\CompactIntervalDyadicNetUp$ carrier is a finite $\BHist$ packet
$$
I=(A,B,M,F,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the $\LocatedIntervalUp$ endpoint rows, including the lower and upper rational endpoint reads and their located-order witness.
\item $M$ is the $\DyadicRatCoreUp$ mesh row naming the denominator scale, radius ledger, and adjacent dyadic-step comparison used by the net.
\item $F$ is the finite net row listing the dyadic mesh cells that cover the displayed interval endpoints at the requested scale.
\item $S$ is the $\StreamNameUp$ schedule row that chooses only the finite observation windows needed to read the endpoint and mesh data.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational endpoint observations and dyadic cell centers.
\item $E$ is the $\RealUp$ seal-facing row reached after the endpoint, mesh, finite-net, schedule, and readback rows are visible.
\item $H$ records componentwise $\hsame$ transport for endpoint rows, mesh rows, finite-net rows, schedules, rational readbacks, seal rows, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from endpoints to mesh, from mesh to the finite net, from the schedule to readback, and from readback to the seal-facing row.
\item $P$ records $\Pkg$ provenance over $\CompactIntervalDyadicNetUp$, $\LocatedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactIntervalDyadicNetUp}$ row.
\end{enumerate}
The classifier compares packets only through these displayed rows and their componentwise transports. It has no coordinate for an open cover, a chosen finite subcover, quotient equality of real names, trichotomy of real endpoints, countable choice over mesh cells, or a completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Compact interval dyadic-net NameCert obligations]
\label{thm:compact-interval-dyadic-net-namecert-obligations}
For every accepted $\CompactIntervalDyadicNetUp$ carrier
$$
I=(A,B,M,F,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactIntervalDyadicNetUp}$ surface consists exactly of endpoint admission through $A,B$, dyadic mesh admission through $M$, finite net exposure through $F$, StreamName schedule exposure through $S$, RegSeqRat readback through $R$, RealUp seal handoff through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-interval-dyadic-net-carrier}. The displayed inventory is exactly $A,B,M,F,S,R,E,H,C,P,N$.

The endpoint rows are read first. They supply the located rational interval boundary, after which the dyadic mesh row chooses a finite scale and the finite net row lists the mesh cells visible at that scale.

The schedule and readback rows keep the route finite. $\StreamNameUp$ supplies only the finite observation windows needed for this interval and mesh, while $\RegSeqRatUp$ reads back the rational endpoint and cell-center data used by the net.

Finally the RealUp seal-facing row, transport rows, replay rows, provenance row, and local naming row repack the same finite route. Since the carrier has no row for open covers, subcover selection, quotient real equality, trichotomy, countable choice, or completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact interval dyadic-net total-bounded route]
\label{thm:compact-interval-dyadic-net-total-bounded-route}
Every total-boundedness consumer of an accepted $\CompactIntervalDyadicNetUp$ carrier reaches a finite interval net only through the route
$$
A,B\longmapsto M\longmapsto F\longmapsto S\longmapsto R\longmapsto E\longmapsto H,C,P,N .
$$
The consumer may read the located endpoint rows, dyadic mesh row, finite net row, StreamName schedule row, RegSeqRat readback row, RealUp seal row, componentwise transport, replay, provenance, and local NameCert rows. It receives no open-cover compactness principle, selected subcover, quotient real equality, trichotomy, countable-choice schedule, or $\RealUp$ completeness object.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:compact-interval-dyadic-net-namecert-obligations}. They order the endpoint rows, mesh row, finite net row, schedule row, readback row, seal handoff, transport, replay, provenance, and local naming rows as the public finite-net surface.

The route is finite at each step. The mesh row $M$ is a dyadic denominator and radius ledger, the net row $F$ is a finite list of displayed mesh cells, the schedule row $S$ selects only finite observation windows, and the readback row $R$ exposes rational data rather than a completed real representative.

The total-boundedness consumer therefore reads only the displayed mesh package before the $\RealUp$ seal-facing row is available. The excluded compactness, subcover, quotient, trichotomy, choice, and completeness objects are absent from the carrier and cannot be exported by the local NameCert.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactIntervalDyadicNetUp$ read exposes located endpoint rows, a DyadicRatCore mesh row, a finite dyadic-net row, a StreamName schedule row, a RegSeqRat readback row, a RealUp seal row, hsame transport, Cont replay, Pkg provenance, and a local NameCert row before any interval total-boundedness consumer reads the packet.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, locatedinterval: no listed sibling is bijective with $\CompactIntervalDyadicNetUp$ because this packet jointly binds located endpoints, dyadic mesh data, a finite net, finite schedule, rational readback, RealUp seal handoff, transport, replay, provenance, and local naming.}

\closureat{CompactIntervalDyadicNetUp}{seedStr}

\begin{closurestatus}{\CompactIntervalDyadicNetUp}
  \constructivestory{A $\CompactIntervalDyadicNetUp$ packet is generated from located interval endpoints, a dyadic mesh row, a finite net row, StreamName schedule data, RegSeqRat readback, a RealUp seal-facing row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-dyadic-net-carrier}, \autoref{thm:compact-interval-dyadic-net-namecert-obligations}, and \autoref{thm:compact-interval-dyadic-net-total-bounded-route} seed the finite dyadic-net certificate for compact interval consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, quotient real equality, trichotomy, countable choice, RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint admission, dyadic mesh admission, finite net exposure, StreamName schedule exposure, RegSeqRat readback, RealUp seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactIntervalDyadicNetUp.}
\end{closurestatus}
