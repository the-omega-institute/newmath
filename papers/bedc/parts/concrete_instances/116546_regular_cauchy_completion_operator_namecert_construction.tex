\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionOperatorUp$}
\label{ch:concrete-instances-regular-cauchy-completion-operator-namecert}
\origin{human}

Following Bishop and Bridges on Cauchy completion, the $\RegularCauchyCompletionOperatorUp$ packet names the finite operator route that turns displayed regular rational Cauchy data into a Real-facing completion seal. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and the neighboring Cauchy completion operator of \autoref{ch:concrete-instances-cauchycompletionoperator-namecert}. The packet records the regular-operator handoff only; it does not quotient Cauchy names, select representatives, use countable choice, assert ambient completeness of $\RealUp$, or replace the broader Bishop completion operator of \autoref{ch:concrete-instances-bishop-completion-operator-namecert}.

\begin{definition}[RegularCauchyCompletionOperator carrier]
\label{def:regular-cauchy-completion-operator-carrier}
A $\RegularCauchyCompletionOperatorUp$ carrier is a finite $\BHist$ packet
$$
O=(D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used for every precision request and every regularity comparison.
\item $S$ is the $\StreamNameUp$ finite-window row naming the observed windows of the Cauchy name.
\item $R$ is the $\RegSeqRatUp$ regularity and readback row attached to those windows.
\item $E$ is the terminal $\RealUp$ seal row reached only after the tolerance, window, and readback rows have been exposed.
\item $H$ records componentwise $\hsame$ transport for the dyadic tolerance, stream window, regular readback, Real seal, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from a precision request through $D,S,R$, then through the Real seal $E$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionOperatorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionOperatorUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient Cauchy equality, a selected representative, a chosen limit point, countable choice, host real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[RegularCauchyCompletionOperator NameCert obligations]
\label{thm:regular-cauchy-completion-operator-namecert-obligations}
For every accepted $\RegularCauchyCompletionOperatorUp$ carrier $O=(D,S,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyCompletionOperatorUp}$ surface consists exactly of carrier admission, dyadic tolerance exposure through $D$, finite-window exposure through $S$, regular rational readback and regularity exposure through $R$, ordered $\RealUp$ seal access through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. A completion-facing operator read must pass through those rows before it may cite the Real-facing seal.
\end{theorem}
\begin{proof}
Project the accepted finite $\BHist$ packet. The visible carrier rows are precisely $D,S,R,E,H,C,P,N$, so carrier admission exposes tolerance, window, readback, terminal seal, transport, replay, provenance, and naming data.

The operator read is ordered. A precision request first reaches the $\DyadicRatCoreUp$ tolerance row, then the $\StreamNameUp$ finite windows, then the $\RegSeqRatUp$ readback and regularity row, and only afterward the terminal $\RealUp$ seal.

Transport and replay are structural. The row $H$ moves the listed coordinates componentwise by $\hsame$, while $C$ replays the displayed continuation route and $P,N$ source and name the same packet. Since no quotient Cauchy equality, selected representative, chosen limit, countable-choice schedule, host real equality, or $\RealUp$ completeness coordinate appears in the tuple, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[RegularCauchyCompletionOperator seal route]
\label{thm:regular-cauchy-completion-operator-seal-route}
Every $\RegularCauchyCompletionOperatorUp$ consumer that reads the Real-facing output of an accepted carrier $O=(D,S,R,E,H,C,P,N)$ factors through the finite route
$$
D\longmapsto S\longmapsto R\longmapsto E\longmapsto H,C,P,N .
$$
Thus the operator output reaches $\RealUp$ only through DyadicRatCore tolerances, finite StreamName windows, RegSeqRat readback, the displayed seal row, componentwise transport, continuation replay, package provenance, and local NameCert data. It does not export quotient Cauchy equality, selected representatives, selected completion points, countable choice, host real equality, or any claim that $\RealUp$ is complete.
\end{theorem}
\begin{proof}
Start with a consumer request for the regular completion output. The carrier definition gives the request access to the dyadic tolerance row $D$ and the finite window row $S$, so the request begins with displayed Cauchy-name observations rather than an external quotient.

Next read the regular row. The readback row $R$ supplies the regular rational data attached to the same windows and tolerance ledger, so the terminal seal $E$ is reached only after the regularity evidence has been displayed.

Finally replay the structural rows $H,C,P,N$. The route is therefore the displayed finite chain from dyadic tolerances and stream windows to the regular readback and Real seal. Because the packet contains no additional coordinate, the factorization cannot produce quotient Cauchy equality, selected representatives, selected completion points, countable choice, host real equality, or completeness of $\RealUp$.
\end{proof}

\closureat{RegularCauchyCompletionOperatorUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionOperatorUp}
  \constructivestory{A $\RegularCauchyCompletionOperatorUp$ packet is generated from DyadicRatCore tolerances, StreamName windows, RegSeqRat regular readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-operator-carrier}, \autoref{thm:regular-cauchy-completion-operator-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-operator-seal-route} seed the finite regular Cauchy completion-operator route from dyadic tolerances and finite windows to the Real-facing seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient Cauchy equality, selected representatives, selected completion points, countable choice, host real equality, Lean verification, bridge checking, or any assertion that $\RealUp$ is complete.}
  \upgradepath{Obligation closure requires checked carrier admission, regular readback ordering, Real-seal factorization, transport stability, and non-escape for BEDC.Derived.RegularCauchyCompletionOperatorUp.}
\end{closurestatus}
