\chapter{A Concrete Naming Certificate for $\HausdorffMomentProblemUp$}
\label{ch:concrete-instances-hausdorff-moment-problem-namecert}
\origin{human}

Following Hausdorff on moment sequences over a compact interval, the $\HausdorffMomentProblemUp$ packet records the finite BEDC surface for a truncated interval moment table before any consumer asks for a representing measure. It specializes the finite-window route of \autoref{ch:concrete-instances-moment-problem-namecert} by fixing the compact support row through \autoref{ch:concrete-instances-compact-real-interval-search-basis-namecert}, reading polynomial tests through \autoref{ch:concrete-instances-real-polynomial-namecert}, sealing scalar values through \autoref{ch:concrete-instances-real-namecert}, and routing nonnegative polynomial checks through \autoref{ch:concrete-instances-positive-cone-namecert}. The certificate names only displayed compact-interval handoff data, finite polynomial windows, Hankel and localizing positivity ledgers, transport, replay, provenance, and local naming; it does not export a representing measure, determinacy, uniqueness, selected support, or ambient compactness theorem.

\begin{definition}[Hausdorff moment problem carrier]
\label{def:hausdorff-moment-problem-carrier}
A $\HausdorffMomentProblemUp$ carrier is a finite $\BHist$ packet
$$
H=(M,I,R,P,V,Q,L,S,T,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the inherited $\MomentProblemUp$ finite moment-window row, including the rational and real readbacks of the inspected moment entries.
\item $I$ is the $\CompactRealIntervalUp$ support row, displaying the lower and upper $\RealUp$ endpoint seals for the compact interval on which the Hausdorff read is requested.
\item $R$ is the $\RealUp$ scalar readback row used for endpoint values, moment entries, and tolerance budgets.
\item $P$ is the $\RealPolynomialUp$ finite polynomial-test row, naming only the displayed coefficient lists and evaluation windows consumed by the packet.
\item $V$ is the $\PositiveConeUp$ nonnegative-read row for the finite polynomial tests and scalar comparison witnesses.
\item $Q$ is the finite Hankel positivity row inherited from $\MomentProblemUp$, restricted to the displayed compact interval support.
\item $L$ is the localizing positivity row for the endpoint constraints, recording the finite windows by which $(x-a)p(x)$ and $(b-x)p(x)$ are inspected through $P,V,R,I$.
\item $S$ records componentwise $\hsame$ transport for the moment window, compact interval support, real readbacks, polynomial tests, positivity rows, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from a Hausdorff finite-window request through $M,I,R,P,V,Q,L,S$.
\item $C$ is the $\Pkg$ provenance over $\HausdorffMomentProblemUp$, $\MomentProblemUp$, $\CompactRealIntervalUp$, $\RealPolynomialUp$, $\PositiveConeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $G$ is the compact-interval handoff ledger recording that every support-facing read uses the displayed $I$ row rather than an ambient compactness principle.
\item $N$ is the local $\NameCert_{\HausdorffMomentProblemUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for a representing measure, infinite Hausdorff sequence, determinacy theorem, selected support measure, host Riesz representation theorem, or ambient completeness principle.
\end{definition}

\begin{theorem}[Hausdorff moment problem NameCert obligations]
\label{thm:hausdorff-moment-problem-namecert-obligations}
For every accepted $\HausdorffMomentProblemUp$ carrier
$$
H=(M,I,R,P,V,Q,L,S,T,C,G,N),
$$
the local $\NameCert_{\HausdorffMomentProblemUp}$ surface consists exactly of MomentProblem finite-window admission, compact real interval support admission, real scalar readback, finite real-polynomial test admission, positive-cone nonnegative readback, finite Hankel positivity, localizing endpoint positivity, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, compact-interval handoff, and local naming for the displayed rows
$$
M,I,R,P,V,Q,L,S,T,C,G,N .
$$
No consumer of the certificate receives a representing measure, uniqueness, determinacy, selected support, an infinite positivity theorem, or a host compact-interval existence theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:hausdorff-moment-problem-carrier}. The inherited moment data are confined to the finite $\MomentProblemUp$ row $M$, while the compact support is confined to the endpoint row $I$ and the real readback row $R$.

Polynomial tests are not host functions. They are read through the finite $\RealPolynomialUp$ row $P$, and their nonnegative comparisons are read through the $\PositiveConeUp$ row $V$ before the finite Hankel row $Q$ or localizing row $L$ may be consumed.

The structural rows $S,T,C,G,N$ transport, replay, source, hand off, and name exactly the same finite rows. Since the carrier contains no representing measure, infinite moment sequence, selected support measure, determinacy proof, Riesz theorem, or ambient completeness coordinate, those objects cannot be exported by the local certificate.
\end{proof}

\begin{theorem}[Hausdorff moment problem Hankel positivity route]
\label{thm:hausdorff-moment-problem-hankel-positivity-route}
Every Hankel-positivity read exported by an accepted $\HausdorffMomentProblemUp$ packet first passes through the $\MomentProblemUp$ finite-window row, the $\RealUp$ scalar readback row, the $\RealPolynomialUp$ finite test row, and the $\PositiveConeUp$ nonnegative-read row before the localizing endpoint ledger may be consumed. In carrier terms, the route is
$$
M,R,P,V \longmapsto Q,L \longmapsto S,T,C,G,N .
$$
It exports no infinite Hankel operator, representing measure, determinacy theorem, or host compact-support solver.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:hausdorff-moment-problem-carrier}. The only Hankel source is the inherited finite-window row $M$, and its scalar entries are sealed by the real readback row $R$.

The endpoint and polynomial tests used by a Hausdorff read are finite. They pass through $P$ and $V$ before the packet opens the Hankel row $Q$ and the localizing endpoint row $L$.

Finally $S,T,C,G,N$ preserve the same route for transport, replay, provenance, compact-interval handoff, and naming. Because no carrier row stores an infinite operator, representing measure, selected support, determinacy theorem, or ambient solver, the displayed finite positivity route is the whole exported surface.
\end{proof}

\begin{theorem}[Hausdorff moment problem compact interval handoff]
\label{thm:hausdorff-moment-problem-compact-interval-handoff}
Every compact-support read of an accepted $\HausdorffMomentProblemUp$ packet is handed off through the displayed $\CompactRealIntervalUp$ row before any moment or polynomial positivity ledger is consumed. The read must expose the support row $I$, the endpoint $\RealUp$ seals in $R$, the finite $\RealPolynomialUp$ test row $P$, the $\PositiveConeUp$ nonnegative row $V$, the Hankel and localizing ledgers $Q,L$, and the compact-interval handoff ledger $G$ before a Hausdorff moment consumer may use the packet.
\end{theorem}
\begin{proof}
Project the carrier. The support row $I$ is the only compact interval coordinate, and $R$ is the only endpoint readback coordinate available to the packet.

Any support-facing polynomial test therefore enters through $I,R,P,V$ before it can reach $Q$ or $L$. The handoff row $G$ records that the same compact interval row, not an ambient compactness theorem, supplies the support boundary.

Transport, replay, provenance, and local naming through $S,T,C,N$ preserve this displayed handoff. A consumer that skips $I$, $R$, or $G$ is not reading the $\HausdorffMomentProblemUp$ carrier, and a consumer that adds a representing measure or determinacy proof leaves the named packet.
\end{proof}

\closureat{HausdorffMomentProblemUp}{seedStr}

\begin{closurestatus}{\HausdorffMomentProblemUp}
  \constructivestory{A $\HausdorffMomentProblemUp$ packet is generated from a MomentProblemUp finite moment-window row, a CompactRealIntervalUp support row, RealUp endpoint and scalar readbacks, RealPolynomialUp finite polynomial tests, PositiveConeUp nonnegative rows, finite Hankel and localizing positivity ledgers, componentwise hsame transport, displayed Cont replay, Pkg provenance, compact-interval handoff, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hausdorff-moment-problem-carrier}, \autoref{thm:hausdorff-moment-problem-namecert-obligations}, \autoref{thm:hausdorff-moment-problem-hankel-positivity-route}, and \autoref{thm:hausdorff-moment-problem-compact-interval-handoff} seed the finite Hausdorff moment route over MomentProblemUp, CompactRealIntervalUp, RealPolynomialUp, PositiveConeUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a representing measure, determinacy, uniqueness, selected support, infinite Hausdorff sequence, ambient compactness theorem, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Hankel positivity routing, localizing endpoint positivity, compact-interval handoff, transport, replay, provenance, and local naming for BEDC.Derived.HausdorffMomentProblemUp.}
\end{closurestatus}
