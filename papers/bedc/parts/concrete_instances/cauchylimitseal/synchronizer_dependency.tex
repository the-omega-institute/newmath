The CauchyLimitSeal seal row is consumed downstream by \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert}, where it is locked to the displayed budget and selector rows before a $\RealUp$-facing seal read. Thus a consumer that cites the seal through the synchronizer reads the same finite seal row together with the synchronizer's budget, selector, shared-tail, compatibility, transport, route, provenance, and local naming rows.
