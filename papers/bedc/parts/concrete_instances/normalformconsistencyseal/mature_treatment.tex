\begin{theorem}[Normal-form consistency seal mature treatment]
\label{thm:normal-form-consistency-seal-mature-treatment}
Let $S=(T,F,N,K,X,H,C,P,L)$ be an accepted $\NormalFormConsistencySealUp$ packet. Its mature treatment is the route that first exposes the candidate route of \autoref{thm:normal-form-consistency-seal-candidate-route}, then the residual schedule of \autoref{thm:normal-form-consistency-seal-residual-schedule-discharge}, and then the MetaCIC handoff of \autoref{thm:normal-form-consistency-seal-metacic-critical-path-handoff}. The route permits MetaCIC and confluence-facing consumers to inspect the closed-normal theorem row only through the displayed candidate, residual, schedule, transport, replay, provenance, and local naming rows. It refuses arbitrary normal endpoints, global closed consistency, hidden subject reduction, evaluator equality, quotient syntax equality, host equality, and any completion of the seal outside those rows.
\end{theorem}
\leanchecked{BEDC.Derived.NormalFormConsistencySealUp.NormalFormConsistencySealMatureTreatment}
\begin{proof}
Read the candidate route first. The cited route fixes the only admissible normal-endpoint evidence available to $S$ and keeps that evidence attached to the displayed rows $T,F,N,K,X$.

Next read the residual schedule. The residual schedule discharge theorem supplies the single schedule used for candidate-mediated strong-normalization and confluence checks, while the finite-window determinacy theorem prevents a second residual route from being opened.

Finally use the MetaCIC handoff and the public export surface. They package the same candidate route, residual schedule, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row for downstream consumers. Because none of these rows contains global closed consistency, hidden subject reduction, evaluator equality, quotient syntax equality, or host equality, the mature treatment is exactly the stated refused-endpoint boundary.
\end{proof}
