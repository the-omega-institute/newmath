\chapter{A Concrete Naming Certificate for $\MichaelSelectionFiniteRefinementUp$}
\label{ch:concrete-instances-michael-selection-finite-refinement-namecert}
\origin{human}

Following Michael's selection theorem in its finite closed-convex refinement reading, the $\MichaelSelectionFiniteRefinementUp$ packet records the BEDC surface where one displayed paracompact cover cell family, one lower-semicontinuous closed-convex value row, and one finite refinement ledger are inspected before any local continuous-selection handoff is public. It is a finite sibling of \autoref{ch:concrete-instances-michael-selection-namecert}; it consumes the paracompact refinement rows of \autoref{ch:concrete-instances-paracompact-namecert}, the lower-semicontinuous value boundary of \autoref{ch:concrete-instances-lowersemicontinuous-namecert}, the convex target rows of \autoref{ch:concrete-instances-convexset-namecert}, the continuous-map handoff of \autoref{ch:concrete-instances-continuousmap-namecert}, and the metric observation discipline of \autoref{ch:concrete-instances-metric-namecert}. The packet names only finite refinement data; it does not assert a global selector, arbitrary choice over fibres, Banach-space completeness, host function equality, or ambient point-set topology.

\begin{definition}[Michael finite closed-convex refinement carrier]
\label{def:michael-selection-finite-carrier}
A $\MichaelSelectionFiniteRefinementUp$ carrier is a finite $\BHist$ packet
$$
F=(D,V,K,R,S,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\ParacompactUp$ domain-cover row whose cells are being refined.
\item $V$ is the $\LowerSemicontinuousUp$ closed-value row exposed over the displayed cover cells.
\item $K$ is the $\ConvexSetUp$ target row, carrying the closed-convex witnesses used by each inspected value row.
\item $R$ is the finite refinement ledger, listing the retained cover cells, their refinement addresses, and the value-row incidence data.
\item $S$ is the local finite selection row, recording only the selections attached to the retained refinement cells.
\item $T$ is the $\ContinuousMapUp$ handoff row available after $D,V,K,R,S$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the domain, value, convex-target, refinement, local selection, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
D,V,K,R,S \longmapsto T \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\MichaelSelectionFiniteRefinementUp$, $\MichaelSelectionUp$, $\ParacompactUp$, $\LowerSemicontinuousUp$, $\ConvexSetUp$, $\ContinuousMapUp$, $\MetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\MichaelSelectionFiniteRefinementUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,V,K,R,S,T,H,C,P,N$. It has no coordinate for a host global selector, arbitrary choice over all fibres, quotient equality of functions, Banach-space completeness, or an ambient topology theorem.
\end{definition}

\begin{theorem}[Michael finite refinement NameCert obligations]
\label{thm:michael-selection-finite-namecert-obligations}
For every accepted $\MichaelSelectionFiniteRefinementUp$ carrier $F=(D,V,K,R,S,T,H,C,P,N)$, the local $\NameCert_{\MichaelSelectionFiniteRefinementUp}$ surface consists exactly of carrier admission, paracompact cover exposure through $D$, lower-semicontinuous closed-convex value exposure through $V$, convex target exposure through $K$, finite refinement incidence through $R$, local finite selection rows through $S$, continuous-map handoff through $T$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. Every continuous-map consumer must read the refinement route $D,V,K,R,S$ before treating $T$ as public, and no obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:michael-selection-finite-carrier}. The rows $D,V,K,R,S,T,H,C,P,N$ are the whole finite packet, so carrier admission and the visible obligation rows are read directly from that tuple.

The selection data are local to the displayed refinement ledger. The paracompact row supplies the cover cells, the lower-semicontinuous row supplies the closed-value windows, the convex row supplies target witnesses, and $R,S$ bind the retained finite refinement cells to the visible local selections.

The continuous-map row $T$ is downstream of those rows. Transport and replay through $H$ and $C$ preserve the same finite route, while $P,N$ source and name it. Since the excluded selector, choice, completeness, host-function, and ambient-topology objects have no carrier coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Michael finite refinement sibling boundary]
\label{thm:michael-selection-finite-sibling-boundary}
Every accepted $\MichaelSelectionFiniteRefinementUp$ packet is a finite refinement read of the $\MichaelSelectionUp$ surface of \autoref{ch:concrete-instances-michael-selection-namecert}. Its public handoff factors through
$$
\ParacompactUp,\ \LowerSemicontinuousUp,\ \ConvexSetUp,\ \ContinuousMapUp,\ \MetricUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The handoff names one displayed finite closed-convex refinement ledger and no global selection theorem.
\end{theorem}
\begin{proof}
Start from \autoref{thm:michael-selection-finite-namecert-obligations}. The visible route exposes the paracompact domain cover, the lower-semicontinuous closed-value row, the convex target row, the finite refinement ledger, and the local selection rows before the continuous-map handoff.

Compare this route with the sibling carrier in \autoref{def:michael-selection-carrier}. The finite packet narrows the selection ledger to the retained refinement cells $R,S$ while keeping the same paracompact, lower-semicontinuous, convex, continuous-map, metric, transport, replay, provenance, and naming dependencies.

Thus a consumer may read a finite closed-convex refinement handoff, but it cannot promote that read to arbitrary choice, a host global selector, quotient equality of functions, Banach-space completeness, or an ambient point-set selection theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\MichaelSelectionFiniteRefinementUp$ read exposes a displayed $\ParacompactUp$ cover row, $\LowerSemicontinuousUp$ closed-value row, $\ConvexSetUp$ target row, finite refinement ledger, local selection row, terminal $\ContinuousMapUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{michael-selection, paracompact, lowersemicontinuous, convexset, continuousmap, metric: $\MichaelSelectionFiniteRefinementUp$ is not bijective to any listed sibling because it binds one finite refinement ledger and its local closed-convex selection rows into a separate NameCert packet.}

\closureat{MichaelSelectionFiniteRefinementUp}{seedStr}

\begin{closurestatus}{\MichaelSelectionFiniteRefinementUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:michael-selection-finite-carrier}, \autoref{thm:michael-selection-finite-namecert-obligations}, and \autoref{thm:michael-selection-finite-sibling-boundary} seed the finite closed-convex refinement surface over paracompact covers, lower-semicontinuous value rows, convex target rows, continuous-map handoff, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global selection, arbitrary choice over fibres, host function equality, Banach-space completeness, ambient point-set topology, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite refinement incidence, local selection-row exactness, continuous-map handoff boundary, transport and replay stability, provenance exhaustion, and TasteGate field faithfulness for BEDC.Derived.MichaelSelectionFiniteRefinementUp.}
  \constructivestory{A $\MichaelSelectionFiniteRefinementUp$ packet is generated from a paracompact cover row, a lower-semicontinuous closed-value row, a closed-convex target row, finite refinement and local selection rows, a continuous-map handoff, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
\end{closurestatus}
