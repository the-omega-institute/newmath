\chapter{A Concrete Naming Certificate for $\CompactIntervalOscillationModulusUp$}
\label{ch:concrete-instances-compact-interval-oscillation-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactIntervalOscillationModulusUp$ packet records the finite oscillation-modulus data by which a located compact interval read can hand a uniform tail-tolerance bound to Real-facing consumers. It sits after the located interval surface of \autoref{ch:concrete-instances-locatedinterval-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal seal discipline of \autoref{ch:concrete-instances-real-namecert}. The packet names an interval-local oscillation ledger and modulus handoff without asserting ambient $\RealUp$ completeness, a selected supremum, host interval compactness, or quotient real equality.

\begin{definition}[Compact interval oscillation-modulus carrier]
\label{def:compact-interval-oscillation-modulus-carrier}
A $\CompactIntervalOscillationModulusUp$ carrier is a finite $\BHist$ packet
$$
I=(L,W,R,Q,O,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\LocatedIntervalUp$ source row, exposing lower and upper endpoint windows together with the interval-cell ledger used by the oscillation read.
\item $W$ is the finite $\StreamNameUp$ window row over the displayed interval cells and endpoint observations.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the finite stream window.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance row that stores the dyadic budget requested by the oscillation test.
\item $O$ is the oscillation ledger, recording finite comparisons of the displayed interval reads under the same $W,R,Q$ rows.
\item $M$ is the modulus handoff row, giving the finite interval-local threshold returned to downstream Cauchy or compact-interval consumers.
\item $E$ is the $\RealUp$ seal row, reached only after $L,W,R,Q,O,M$ have been accepted.
\item $H$ records componentwise $\hsame$ transport for the interval source, window, readback, tolerance, oscillation ledger, modulus handoff, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from located interval reads through stream windows, regular rational readback, dyadic tolerance, oscillation ledger, modulus handoff, and real sealing.
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalOscillationModulusUp$, $\LocatedIntervalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactIntervalOscillationModulusUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $L,W,R,Q,O,M,E,H,C,P,N$. It has no coordinate for a host compact interval, selected endpoint value, quotient real equality, selected supremum, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact interval oscillation-modulus NameCert obligations]
\label{thm:compact-interval-oscillation-modulus-namecert-obligations}
For every accepted $\CompactIntervalOscillationModulusUp$ carrier
$$
I=(L,W,R,Q,O,M,E,H,C,P,N),
$$
the local $\NameCert_{\CompactIntervalOscillationModulusUp}$ surface consists exactly of located interval admission, finite stream-window exposure, regular rational readback, dyadic tolerance budgeting, oscillation-ledger exactness, modulus handoff, real-seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The certificate exports no host compact interval, selected endpoint value, quotient real equality, selected supremum, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-interval-oscillation-modulus-carrier}. The analytic rows are $L,W,R,Q,O,M,E$, while $H,C,P,N$ preserve, replay, source, and name the same finite packet.

The located interval admission row $L$ supplies the interval source and endpoint-window data. The finite window row $W$ and regular readback row $R$ expose the only interval reads available to the oscillation ledger.

The dyadic tolerance row $Q$ fixes the comparison budget before $O$ is read. The oscillation ledger $O$ records only finite comparisons over the same $W,R,Q$ rows, and the modulus handoff $M$ is the only downstream threshold exported by the packet.

Finally the seal row $E$ is downstream of the accepted finite prefix, while $H,C,P,N$ preserve the same route under transport and replay. Since no displayed coordinate names a host interval, selected endpoint, quotient real equality, selected supremum, countable choice, or ambient completeness principle, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact interval oscillation-modulus handoff]
\label{thm:compact-interval-oscillation-modulus-handoff}
Every downstream Cauchy, compact-interval, or $\RealUp$ consumer of an accepted $\CompactIntervalOscillationModulusUp$ carrier receives the finite route
$$
L,W,R,Q,O,M,E,H,C,P,N .
$$
The handoff displays the $\LocatedIntervalUp$ source, the $\StreamNameUp$ window, the $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ tolerance row, the oscillation ledger, the modulus handoff, and the terminal $\RealUp$ seal before any consumer may use the interval-local modulus.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:compact-interval-oscillation-modulus-namecert-obligations}. They place located interval admission, stream-window exposure, regular rational readback, dyadic tolerance budgeting, oscillation-ledger exactness, modulus handoff, and real sealing in one displayed packet.

The route to $M$ must pass through $L,W,R,Q,O$. The located interval row supplies the interval source, the window and readback rows supply the finite observations, the dyadic row supplies the tolerance, and the oscillation ledger records the finite comparisons from which the modulus handoff is read.

The route to $E$ must pass through the same prefix. Componentwise transport and replay in $H$ and $C$ preserve that prefix, while $P$ and $N$ source and name it locally. Thus any downstream Cauchy, compact-interval, or Real-facing consumer receives the interval-local oscillation modulus only as the displayed finite handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactIntervalOscillationModulusUp$ read must expose $\LocatedIntervalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, the oscillation ledger, the modulus handoff, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows.}

\independenceWitness{locatedinterval, streamname, regseqrat, dyadicratcore, real: $\CompactIntervalOscillationModulusUp$ is not bijective to any listed sibling because it binds interval-local oscillation comparisons to a finite modulus handoff before Real-facing consumers may read the seal.}

\closureat{CompactIntervalOscillationModulusUp}{seedStr}

\begin{closurestatus}{\CompactIntervalOscillationModulusUp}
  \constructivestory{A $\CompactIntervalOscillationModulusUp$ packet is generated from a LocatedInterval source row, finite StreamName windows, RegSeqRat readback, DyadicRatCore tolerance data, an oscillation ledger, a modulus handoff, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-oscillation-modulus-carrier}, \autoref{thm:compact-interval-oscillation-modulus-namecert-obligations}, and \autoref{thm:compact-interval-oscillation-modulus-handoff} seed the finite interval-local route from located compact reads to oscillation-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host compact intervals, selected endpoint values, quotient real equality, selected suprema, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, interval-source exactness, finite-window readback, dyadic tolerance exactness, oscillation-ledger exactness, modulus handoff, seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactIntervalOscillationModulusUp.}
\end{closurestatus}
