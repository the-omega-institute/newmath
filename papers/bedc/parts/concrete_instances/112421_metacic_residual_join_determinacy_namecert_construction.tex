\chapter{A Concrete Naming Certificate for $\MetaCICResidualJoinDeterminacyUp$}
\label{ch:concrete-instances-metacic-residual-join-determinacy-namecert}
\origin{ai}

The $\MetaCICResidualJoinDeterminacyUp$ packet realizes the residual-substitution boundary of \autoref{ch:metacic-confluence-audit-map} as finite local-join data. It sits beside the confluence-budget witness of \autoref{ch:concrete-instances-metacic-confluence-budget-witness-namecert}, the residual-substitution compatibility boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-compatibility-namecert}, the local-diamond witness of \autoref{ch:concrete-instances-metacic-local-diamond-witness-namecert}, the residual envelope of \autoref{ch:concrete-instances-metacic-confluence-residual-envelope-namecert}, and the closed parallel-diamond premise ledger of \autoref{ch:concrete-instances-closed-parallel-diamond-premise-ledger-namecert}. It names a budgeted local join packet for residual branches; it does not claim full Church--Rosser, unguarded substitution compatibility, global strong normalization, subject reduction, or host reduction semantics.

\begin{definition}[MetaCIC residual-join determinacy carrier]
\label{def:metacic-residual-join-determinacy-carrier}
A $\MetaCICResidualJoinDeterminacyUp$ carrier is a finite $\BHist$ packet
$$
J=(T,R,B,K,E,S,L,D,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed residual-window row, naming the closed MetaCIC source, the two residual branches, and the inspected redex-frontier fragments.
\item $R$ is the residual-budget row, read from the confluence-budget witness and bounding residual addresses, replay fuel, and candidate-join search.
\item $B$ is the bounded local-join row, recording the candidate common reduct accepted inside the displayed residual budget.
\item $K$ is the determinacy row, requiring any accepted candidate for the same displayed residual branch data to be compared through the same budgeted row $B$.
\item $E$ is the residual-envelope row, preserving the residual branches and the local envelope in which the bounded join is inspected.
\item $S$ is the residual-substitution boundary row, recording that substitution compatibility is a guarded input shape, not an unguarded theorem.
\item $L$ is the closed-premise ledger row consumed when closed local-diamond evidence is read.
\item $D$ is the local-diamond witness row for the displayed peak and its bounded join.
\item $O$ is the obstruction row retaining the refusal of full Church--Rosser, full parallel diamond, arbitrary residual substitution compatibility, subject reduction, and host semantics.
\item $H$ records componentwise $\hsame$ transport for typed windows, residual budgets, bounded joins, determinacy rows, envelopes, substitution boundaries, closed-premise ledgers, local diamonds, obstruction rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a residual-join request through $T,R,B,K,E,S,L,D,O,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualJoinDeterminacyUp$, $\MetaCICConfluenceBudgetWitnessUp$, $\MetaCICResidualSubstitutionCompatibilityUp$, $\MetaCICLocalDiamondWitnessUp$, $\MetaCICConfluenceResidualEnvelopeUp$, $\ClosedParallelDiamondPremiseLedgerUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICResidualJoinDeterminacyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,R,B,K,E,S,L,D,O,H,C,P,N$. It has no coordinate for full parallel diamond, arbitrary closed Church--Rosser, unguarded substitution compatibility, global strong normalization, subject reduction, or host reduction semantics.
\end{definition}

\begin{theorem}[MetaCIC residual-join determinacy NameCert obligations]
\label{thm:metacic-residual-join-determinacy-obligations}
For every accepted $\MetaCICResidualJoinDeterminacyUp$ carrier $J=(T,R,B,K,E,S,L,D,O,H,C,P,N)$, the local $\NameCert_{\MetaCICResidualJoinDeterminacyUp}$ surface consists exactly of typed residual-window admission, residual-budget accounting, bounded local-join exposure, candidate determinacy inside the displayed budget, residual-envelope handoff, guarded residual-substitution boundary visibility, closed-premise ledger handoff, local-diamond handoff, obstruction retention, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-join-determinacy-carrier}. The displayed mathematical rows are $T,R,B,K,E,S,L,D,O$, followed by the structural rows $H,C,P,N$.

The local join is budgeted before it is deterministic. Rows $T$ and $R$ first bind the residual window and the residual budget; row $B$ then records only the candidate common reduct accepted inside that budget.

The determinacy row $K$ compares accepted candidates for the same displayed residual data through $B$. It does not supply a global confluence theorem or an unguarded substitution compatibility theorem.

Rows $E,S,L,D,O$ keep the sibling handoffs and the obstruction boundary visible. Rows $H,C,P,N$ transport, replay, source, and name the same finite route, so the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[MetaCIC residual-join determinacy budgeted local join]
\label{thm:metacic-residual-join-determinacy-budgeted-local-join}
Every accepted $\MetaCICResidualJoinDeterminacyUp$ packet exposes a local join only through the finite route
$$
T,R \longrightarrow B,K \longrightarrow E,S,L,D,O \longrightarrow H,C,P,N .
$$
The route may compare bounded candidate joins for the same displayed residual branch data, but it cannot be read as full parallel diamond, arbitrary closed Church--Rosser, unguarded substitution compatibility, global strong normalization, subject reduction, or host reduction semantics.
\end{theorem}
\begin{proof}
Start with the typed residual window $T$ and the residual budget $R$. These rows determine which branch data and which candidate addresses can be inspected.

The bounded join row $B$ is downstream of that budget, and the determinacy row $K$ compares only candidates accepted for the same displayed data. Thus determinacy is local to the packet.

The envelope, substitution-boundary, closed-premise, local-diamond, and obstruction rows remain visible after the local comparison. The structural rows preserve this finite route without adding endpoint claims.
\end{proof}

\begin{theorem}[MetaCIC residual-join determinacy boundary]
\label{thm:metacic-residual-join-determinacy-boundary}
Every confluence-facing consumer of $\MetaCICResidualJoinDeterminacyUp$ receives only a budgeted residual-local join packet. In particular, the consumer must keep the residual-substitution compatibility row $S$ as a guarded boundary and the obstruction row $O$ as a visible refusal of full Church--Rosser, full parallel diamond, unguarded substitution compatibility, subject reduction, global strong normalization, and host reduction semantics.
\end{theorem}
\begin{proof}
Read the carrier and apply \autoref{thm:metacic-residual-join-determinacy-budgeted-local-join}. The route exposes typed residual windows, a residual budget, bounded candidate joins, and local determinacy before any sibling handoff is consumed.

The substitution boundary is not discharged by this packet. It is stored as row $S$, while the obstruction row $O$ records the endpoint claims that remain unavailable to the consumer.

Transport, replay, provenance, and naming preserve the displayed rows only. Hence the consumer obtains a finite residual-local join boundary, not an endpoint confluence theorem.
\end{proof}

\falsifiablePrediction{A $\MetaCICResidualJoinDeterminacyUp$ packet that omits the residual-budget row $R$, the bounded local-join row $B$, the guarded residual-substitution boundary row $S$, or the obstruction row $O$ is rejected even if it carries typed residual windows and local-diamond evidence.}

\independenceWitness{metacic-confluence-budget-witness, metacic-residual-substitution-compatibility, metacic-local-diamond-witness, metacic-confluence-residual-envelope, closed-parallel-diamond-premise-ledger: $\MetaCICResidualJoinDeterminacyUp$ is not bijective to any listed sibling because it jointly binds typed residual windows, residual budgets, bounded local joins, determinacy rows, residual envelopes, guarded substitution-boundary rows, closed-premise ledgers, local-diamond evidence, obstruction retention, transport, replay, provenance, and local NameCert data.}

The Lean-side hardening surface requires future $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for the residual-join determinacy packet. The nontrivial witness separates an accepted packet with displayed residual-budget, bounded-join, guarded-boundary, and obstruction rows from a refused packet missing one of those rows; field faithfulness compares $T,R,B,K,E,S,L,D,O,H,C,P,N$ componentwise so that equal displayed fields are indistinguishable to the local NameCert classifier.

\closureat{MetaCICResidualJoinDeterminacyUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualJoinDeterminacyUp}
  \constructivestory{A $\MetaCICResidualJoinDeterminacyUp$ packet is generated from typed residual-window rows, residual-budget rows, bounded local-join rows, determinacy rows, residual-envelope rows, guarded residual-substitution boundary rows, closed-premise ledger rows, local-diamond rows, obstruction rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-join-determinacy-carrier}, \autoref{thm:metacic-residual-join-determinacy-obligations}, \autoref{thm:metacic-residual-join-determinacy-budgeted-local-join}, and \autoref{thm:metacic-residual-join-determinacy-boundary} seed the finite residual-local join determinacy boundary for MetaCIC confluence consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full parallel diamond, arbitrary closed Church--Rosser, unguarded substitution compatibility, subject reduction, global strong normalization, host reduction semantics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, residual-budget accounting, bounded local-join exposure, candidate determinacy, residual-envelope handoff, guarded substitution-boundary visibility, local-diamond handoff, obstruction retention, and Nontrivial and FieldFaithful TasteGate instances for BEDC.Derived.MetaCICResidualJoinDeterminacyUp.}
\end{closurestatus}
