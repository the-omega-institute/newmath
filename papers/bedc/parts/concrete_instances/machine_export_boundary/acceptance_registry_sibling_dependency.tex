The $\MachineExportBoundaryUp$ acceptance route consumes both the machine-interface boundary of \autoref{ch:concrete-instances-machine-interface-boundary-namecert} and the registry export rows of \autoref{ch:concrete-instances-registry-export-surface-namecert} before an export boundary can be read as machine-facing evidence. The accepted packet therefore reaches local export naming only after interface exposure, registry status, audit readback, refusal preservation, componentwise transport, displayed replay, package provenance, and the local $\NameCert$ row remain visible together.
