\chapter{A Concrete Naming Certificate for $\SeparatedLimitUniquenessUp$}
\label{ch:concrete-instances-separated-limit-uniqueness-namecert}
\origin{human}

Following Bishop and Bridges constructive metric-space analysis, the $\SeparatedLimitUniquenessUp$ packet records the familiar uniqueness of metric limits in a separated metric space as finite BEDC certificate data. It sits over the separated classifier of \autoref{ch:concrete-instances-separatedmetric-namecert}, the complete-metric limit service of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy-sequence comparison route of \autoref{ch:concrete-instances-cauchysequenceequivalence-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}. The packet names only the displayed zero-distance handoff between two limit rows; it is not a quotient completion equality, a selected representative theorem, countable choice, host equality of sequences, or an ambient completeness principle for $\RealUp$.

\begin{definition}[SeparatedLimitUniqueness carrier]\label{def:separated-limit-uniqueness-carrier}
A $\SeparatedLimitUniquenessUp$ carrier is a finite $\BHist$ packet
$$
U=(L_{0},L_{1},Q,M,Z,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L_{0}$ and $L_{1}$ are $\CompleteMetricUp$ limit rows for the same displayed Cauchy datum, each exposing only the finite limit-service surface available in the complete metric packet.
\item $Q$ is the common $\CauchySequenceEquivalenceUp$ comparison route through which the two limit reads are compared before any endpoint classifier is inspected.
\item $M$ is the $\MetricUp$ distance-witness row carrying the displayed distance comparison between the two candidate limits.
\item $Z$ is the $\RealUp$ zero-distance row, read only after the distance witness has passed through finite regular rational and stream-window readback.
\item $S$ is the $\SeparatedMetricUp$ classifier handoff saying that the zero-distance row is the only route by which the two displayed limit reads may be identified.
\item $H$ records componentwise $\hsame$ transport for $L_{0},L_{1},Q,M,Z,S,C,P,N$.
\item $C$ records displayed $\Cont$ replay from the two limit rows through the common Cauchy comparison, the metric distance witness, the zero-distance row, and the separated classifier.
\item $P$ is the $\Pkg$ provenance over $\CompleteMetricUp$, $\CauchySequenceEquivalenceUp$, $\MetricUp$, $\RealUp$, $\SeparatedMetricUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SeparatedLimitUniquenessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L_{0},L_{1},Q,M,Z,S,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, host sequence equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[SeparatedLimitUniqueness NameCert obligations]\label{thm:separated-limit-uniqueness-namecert-obligations}
For every accepted $\SeparatedLimitUniquenessUp$ carrier
$$
U=(L_{0},L_{1},Q,M,Z,S,H,C,P,N),
$$
the local $\NameCert_{\SeparatedLimitUniquenessUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item two-limit admission, requiring both displayed limit reads to pass through the $\CompleteMetricUp$ rows $L_{0}$ and $L_{1}$;
\item common Cauchy comparison, requiring the two limit rows to be compared through the single $\CauchySequenceEquivalenceUp$ route $Q$;
\item distance-witness exactness, requiring the metric comparison to be the displayed $\MetricUp$ distance row $M$;
\item zero-distance sealing, requiring the zero comparison to be the $\RealUp$ row $Z$ reached after the finite readback route;
\item separated classifier handoff, requiring identification of the two limit reads to pass through the $\SeparatedMetricUp$ row $S$;
\item transport, replay, provenance, and local naming stability through $H,C,P,N$ for the same rows;
\item refusal of quotient completion equality, selected representatives, host sequence equality, countable choice, trichotomy, and ambient $\RealUp$ completeness as certificate data.
\end{enumerate}
No SeparatedLimitUniqueness obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:separated-limit-uniqueness-carrier}. The two candidate limits are visible only as the displayed complete-metric limit rows $L_{0}$ and $L_{1}$, so the certificate has no separate host endpoint from which to read equality.

The common comparison route is $Q$. It aligns the two limit reads as Cauchy data before the distance witness is inspected, keeping the route inside the finite $\CauchySequenceEquivalenceUp$ surface.

The metric comparison then passes through $M$ and reaches the zero-distance row $Z$. The row $Z$ is a terminal $\RealUp$ seal over the finite readback route, not a detached assertion of equality in a host real line.

Finally the separated classifier row $S$ is consumed. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve the same displayed path, so the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[SeparatedLimitUniqueness zero-distance handoff]\label{thm:separated-limit-uniqueness-zero-distance-handoff}
Every consumer that identifies two accepted $\CompleteMetricUp$ limit reads through an accepted $\SeparatedLimitUniquenessUp$ packet must factor the identification through
$$
L_{0},L_{1},Q,M,Z,S,H,C,P,N .
$$
The handoff reads the common Cauchy comparison $Q$, the $\MetricUp$ distance witness $M$, the $\RealUp$ zero-distance row $Z$, and then the $\SeparatedMetricUp$ classifier $S$ before any named limit uniqueness claim is visible. It exports no quotient sequence equality, selected representative, host equality of metric points, countable-choice schedule, trichotomy split, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with an accepted carrier $U$. A uniqueness-facing consumer first sees only the two complete-metric rows $L_{0}$ and $L_{1}$ and the shared comparison route $Q$.

The metric witness $M$ is downstream of that route. Thus the consumer cannot compare the endpoints by host equality or by selecting representatives before the finite Cauchy comparison has been displayed.

The zero-distance row $Z$ is then read as a $\RealUp$ seal, and $S$ translates that displayed zero-distance evidence into the separated classifier handoff. The structural rows $H,C,P,N$ transport, replay, source, and name the same route.

Therefore every limit-uniqueness read is the stated finite zero-distance handoff, with no additional quotient, choice, or ambient-completeness coordinate.
\end{proof}

\closureat{SeparatedLimitUniquenessUp}{seedStr}

\begin{closurestatus}{\SeparatedLimitUniquenessUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:separated-limit-uniqueness-carrier}, \autoref{thm:separated-limit-uniqueness-namecert-obligations}, and \autoref{thm:separated-limit-uniqueness-zero-distance-handoff} seed the finite separated-limit uniqueness packet over complete-metric limit rows, Cauchy-sequence comparison, metric distance witnesses, Real zero-distance rows, separated classifiers, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, quotient completion equality, selected representatives, host sequence equality, countable choice, trichotomy, standard bridge checking, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, common Cauchy comparison, distance-witness exactness, zero-distance sealing, separated classifier handoff, transport stability, replay stability, and non-escape for BEDC.Derived.SeparatedLimitUniquenessUp.}
  \constructivestory{A $\SeparatedLimitUniquenessUp$ packet is generated from two CompleteMetricUp limit rows, a common CauchySequenceEquivalenceUp comparison route, a MetricUp distance witness, a RealUp zero-distance row, a SeparatedMetricUp classifier handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
