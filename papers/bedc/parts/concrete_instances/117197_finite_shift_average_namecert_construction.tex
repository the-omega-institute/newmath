\chapter{A Concrete Naming Certificate for $\FiniteShiftAverageUp$}
\label{ch:concrete-instances-finite-shift-average-namecert}
\origin{human}

Following Banach, Lorentz, and Bishop--Bridges finite averaging practice, the $\FiniteShiftAverageUp$ packet records a finite shift-window average over L10 real-name data as one NameCert carrier. It consumes the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the Cauchy-name route of \autoref{ch:concrete-instances-cauchy-namecert}. The packet names only the finite averaging route for a displayed window; it does not select a Banach limit, quotient sequence class, ultrafilter, countable-choice subsequence, or ambient $\RealUp$ completeness witness.

\begin{definition}[Finite shift average carrier]
\label{def:finite-shift-average-carrier}
A $\FiniteShiftAverageUp$ carrier is a finite $\BHist$ packet
$$
F=(S,W,A,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ source row naming the finite stream windows whose shifted entries are inspected.
\item $W$ is the finite shift-window row, recording the window length, shift offsets, and local inclusion of every sampled entry.
\item $A$ is the average ledger, storing the finite rational sum and denominator schedule before any real seal is consumed.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the dyadic precision and rounding budget for the average readback.
\item $R$ is the $\RegSeqRatUp$ readback row that turns the average ledger into a regular rational name.
\item $E$ is the terminal $\RealUp$ seal for the displayed averaged name.
\item $H$ records componentwise $\hsame$ transport for $S,W,A,D,R,E$.
\item $C$ records displayed $\Cont$ replay from stream windows through finite averaging, dyadic tolerance, regular readback, and real seal.
\item $P$ is the $\Pkg$ provenance over $\FiniteShiftAverageUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteShiftAverageUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for an invariant mean, a Banach-limit value, quotient equality of sequences, an ultrafilter, a choice-selected subsequence, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[Finite shift average NameCert obligations]
\label{thm:finite-shift-average-namecert-obligations}
For every accepted $\FiniteShiftAverageUp$ carrier $F=(S,W,A,D,R,E,H,C,P,N)$, the local $\NameCert_{\FiniteShiftAverageUp}$ surface consists exactly of carrier admission for the $\StreamNameUp$ source row, finite shift-window admission through $W$, finite average ledger exactness through $A$, dyadic tolerance control through $D$, $\RegSeqRatUp$ readback through $R$, terminal $\RealUp$ seal discipline through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-shift-average-carrier}. The stream source $S$ and shift-window row $W$ list the only entries that can be sampled, so every average is finite before any real-facing read is available.

Read the average ledger next. Row $A$ records the rational sum and denominator schedule, while $D$ supplies the dyadic tolerance budget used to compare the finite sum with the requested precision.

The readback and seal rows then expose the L10 real-name route. Row $R$ turns the finite ledger into a regular rational name, and $E$ seals only that displayed name. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same rows, so no invariant mean or completion principle can enter after the finite average has been named.
\end{proof}

\begin{theorem}[Finite shift average Banach-limit boundary]
\label{thm:finite-shift-average-banach-limit-boundary}
Every $\RealUp$- or $\RegSeqRatUp$-facing consumer of an accepted $\FiniteShiftAverageUp$ packet reads the finite route
$$
\StreamNameUp,\ W,\ A,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp
$$
through the rows $S,W,A,D,R,E,H,C,P,N$ of \autoref{def:finite-shift-average-carrier}. The consumer may use the finite shift-window average as L10 real-name data, but it cannot replace that route by a Banach limit, quotient sequence class, ultrafilter, countable-choice subsequence, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:finite-shift-average-namecert-obligations}. They require the stream window, shift window, finite average ledger, dyadic tolerance row, regular rational readback, and real seal to be displayed before any consumer reads the averaged name.

The finite route is already closed under the sibling readbacks. The $\StreamNameUp$ and $\CauchyUp$ source rows expose the sampled entries, $\DyadicRatCoreUp$ supplies the tolerance budget, $\RegSeqRatUp$ supplies the regular rational readback, and $\RealUp$ supplies only the terminal seal.

Since the carrier has no coordinate for an invariant mean, quotient sequence class, ultrafilter, selected subsequence, or ambient completeness witness, none of those objects can be substituted for the finite ledger. The boundary is therefore a finite shift-window average route rather than a Banach-limit selection principle.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteShiftAverageUp$ consumer must expose StreamName windows, the finite shift-window row, average ledger, DyadicRatCore tolerance row, RegSeqRat readback, Real seal, transport, replay, provenance, and local NameCert evidence before any averaged real-name read is public.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, cauchy: $\FiniteShiftAverageUp$ is not bijective to any listed sibling because it jointly binds source windows, finite shifts, averaging, dyadic tolerance, regular readback, real sealing, transport, replay, provenance, and local naming rows.}

\closureat{FiniteShiftAverageUp}{seedStr}

\begin{closurestatus}{\FiniteShiftAverageUp}
  \constructivestory{A $\FiniteShiftAverageUp$ packet is generated from StreamName source windows, a finite shift-window row, a finite average ledger, a DyadicRatCore tolerance row, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-shift-average-carrier}, \autoref{thm:finite-shift-average-namecert-obligations}, and \autoref{thm:finite-shift-average-banach-limit-boundary} seed the finite shift-average certificate for L10 real-name consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Banach-limit existence, quotient sequence equality, ultrafilters, countable-choice subsequences, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exactness, average-ledger exactness, dyadic tolerance control, regular readback, Real-seal discipline, transport, replay, provenance, and local naming for BEDC.Derived.FiniteShiftAverageUp.}
\end{closurestatus}
