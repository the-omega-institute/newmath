\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseHolonomyIndexUp$}
\label{ch:concrete-instances-hyperbolic-phase-holonomy-index-namecert}
\origin{ai}

The $\HyperbolicPhaseHolonomyIndexUp$ packet realizes the hyperbolic observer phase route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as a finite index surface for closed phase-transport classes. It is distinct from the carrier-level $\HyperbolicPhaseHolonomyUp$ packet of \autoref{ch:concrete-instances-hyperbolic-phase-holonomy-namecert}: the holonomy packet records an accepted closed reversible transport row, while the index packet records the finite comparison class, boundary anchor, disk-metric readback, observer groupoid route, and provenance rows that allow consumers to compare such closed transports without importing a host holonomy group. It sits beside $\ObserverPhaseGroupoidUp$ in \autoref{ch:concrete-instances-observer-phase-groupoid-namecert}, $\HyperbolicMetricUp$ in \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, $\PoincareDiskMetricUp$ in \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, and $\HyperbolicBoundaryShadowUp$ in \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}.

\section{Carrier and vision route}
\label{sec:hyperbolic-phase-holonomy-index-carrier}

\begin{definition}[Hyperbolic phase-holonomy index carrier]
\label{def:hyperbolic-phase-holonomy-index-carrier}
A $\HyperbolicPhaseHolonomyIndexUp$ carrier is a finite $\BHist$ packet
$$
I=(D,B,M,O,L,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
  \item $D$ is the $\PoincareDiskMetricUp$ disk-metric readback row for the finite disk observations used by the closed transport.
  \item $B$ is the $\HyperbolicBoundaryShadowUp$ boundary-anchor row saying which visible boundary shadow is preserved or crossed by the transport loop.
  \item $M$ is the $\HyperbolicMetricUp$ comparison row attached to the same displayed disk and boundary observations.
  \item $O$ is the $\ObserverPhaseGroupoidUp$ reversible phase-route row, including source, target, inverse, and composition evidence for the observer-facing phase surface.
  \item $L$ is the accepted $\HyperbolicPhaseHolonomyUp$ closed transport row whose finite loop data is being indexed.
  \item $Q$ is the holonomy-index row, comparing two accepted loops only by $D,B,M,O,L$ and by the displayed classifier rows already present in those packets.
  \item $H$ records componentwise $\hsame$ transport for disk, boundary, metric, groupoid, holonomy, index, replay, provenance, and naming rows.
  \item $C$ records displayed $\Cont$ replay of the same finite route.
  \item $P$ is the $\Pkg$ provenance over $\HyperbolicPhaseHolonomyIndexUp$, $\HyperbolicPhaseHolonomyUp$, $\ObserverPhaseGroupoidUp$, $\HyperbolicMetricUp$, $\PoincareDiskMetricUp$, $\HyperbolicBoundaryShadowUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
  \item $N$ is the local $\NameCert_{\HyperbolicPhaseHolonomyIndexUp}$ row.
\end{enumerate}
The classifier compares accepted index carriers only through $D,B,M,O,L,Q,H,C,P,N$. It has no coordinate for a host holonomy group, quotient loop space, global curvature invariant, selected representative loop, physical observer law, or completed hyperbolic boundary.
\end{definition}

\begin{theorem}[Hyperbolic phase-holonomy index obligations]
\label{thm:hyperbolic-phase-holonomy-index-namecert-obligations}
For every accepted $\HyperbolicPhaseHolonomyIndexUp$ carrier $I=(D,B,M,O,L,Q,H,C,P,N)$, the local $\NameCert_{\HyperbolicPhaseHolonomyIndexUp}$ surface consists exactly of disk-metric readback, boundary-shadow anchoring, hyperbolic metric comparison, observer phase-groupoid routing, closed holonomy-row exposure, finite index comparison, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-phase-holonomy-index-carrier}. The disk, boundary, metric, groupoid, and holonomy rows $D,B,M,O,L$ must be displayed before the index row can be consumed.

The row $Q$ compares accepted closed transports only through those displayed rows. It therefore records a finite index surface rather than a host quotient of loops or a global holonomy group.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier contains no host holonomy group, quotient loop space, global curvature invariant, selected representative loop, physical observer law, or completed boundary coordinate, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic phase-holonomy index vision route]
\label{thm:hyperbolic-phase-holonomy-index-vision-route}
The $\HyperbolicPhaseHolonomyIndexUp$ packet realizes the observer-expansion holonomy-index route only as finite disk, boundary, metric, observer-groupoid, closed-holonomy, index, transport, replay, provenance, and NameCert rows. Every consumer of the index must first read $\PoincareDiskMetricUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicMetricUp$, $\ObserverPhaseGroupoidUp$, and $\HyperbolicPhaseHolonomyUp$ rows; the consumer receives no generic phase-transport packet, host holonomy group, quotient loop space, global curvature invariant, physical observer law, selected representative loop, or completed boundary object.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:hyperbolic-phase-holonomy-index-namecert-obligations}. It orders every index read through the disk, boundary, metric, observer-groupoid, and closed-holonomy rows before $Q$ is exposed.

The vision route is therefore a concrete finite ledger for comparing closed phase transports. It refines $\HyperbolicPhaseHolonomyUp$ by adding the comparison index and its boundary and metric anchors, but it does not duplicate the underlying holonomy carrier.

Finally project the refusal coordinates from \autoref{def:hyperbolic-phase-holonomy-index-carrier}. Since the packet has no host quotient, global holonomy group, curvature invariant, physical observer law, selected loop, or completed-boundary coordinate, no consumer can extract those objects from the index route.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicPhaseHolonomyIndexUp$ read exposes PoincareDiskMetric, HyperbolicBoundaryShadow, HyperbolicMetric, ObserverPhaseGroupoid, HyperbolicPhaseHolonomy, index comparison, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows before any holonomy-index consumer is served.}

\independenceWitness{hyperbolic-phase-holonomy, observer-phase-groupoid, hyperbolic-metric, poincare-disk-metric, hyperbolic-boundary-shadow: $\HyperbolicPhaseHolonomyUp$ carries a closed transport without the index row, $\ObserverPhaseGroupoidUp$ carries reversible phase routing without the closed holonomy comparison, $\HyperbolicMetricUp$ and $\PoincareDiskMetricUp$ carry metric readback without the phase loop, and $\HyperbolicBoundaryShadowUp$ carries the boundary anchor without the index comparison. None is bijective to $\HyperbolicPhaseHolonomyIndexUp$ under NameCert-row preservation.}

\closureat{HyperbolicPhaseHolonomyIndexUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseHolonomyIndexUp}
  \constructivestory{A $\HyperbolicPhaseHolonomyIndexUp$ packet is generated from PoincareDiskMetric disk rows, HyperbolicBoundaryShadow boundary anchors, HyperbolicMetric comparison rows, ObserverPhaseGroupoid reversible phase-route rows, HyperbolicPhaseHolonomy closed transport rows, a finite index comparison row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-holonomy-index-carrier}, \autoref{thm:hyperbolic-phase-holonomy-index-namecert-obligations}, and \autoref{thm:hyperbolic-phase-holonomy-index-vision-route} seed the finite holonomy-index route for hyperbolic observer-expansion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host holonomy groups, quotient loop spaces, global curvature invariants, physical observer laws, selected representative loops, completed hyperbolic boundaries, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, disk and boundary row exposure, metric comparison, observer-groupoid routing, closed-holonomy handoff, index comparison, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicPhaseHolonomyIndexUp.}
\end{closurestatus}
