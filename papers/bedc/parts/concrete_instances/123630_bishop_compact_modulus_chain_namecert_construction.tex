\chapter{A Concrete Naming Certificate for $\BishopCompactModulusChainUp$}
\label{ch:concrete-instances-bishop-compact-modulus-chain-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopCompactModulusChainUp$ packet records the finite route from compact finite-net data to a displayed uniform-modulus row. It sits beside the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus carrier of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the rational tolerance carrier of \autoref{ch:concrete-instances-rat-namecert}, and the finite epsilon-net carrier of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}. The packet names the displayed finite-net-to-modulus chain without importing open-cover compactness, selected Lebesgue numbers, sequential compactness, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop compact modulus chain carrier]
\label{def:bishop-compact-modulus-chain-carrier}
A $\BishopCompactModulusChainUp$ carrier is a finite $\BHist$ packet
$$
B=(K,E,M,F,R,U,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row, read only through the finite compact surface used by the packet.
\item $E$ is the $\FiniteEpsilonNetUp$ row that displays a finite net for the chosen rational tolerance.
\item $M$ is the $\MetricUp$ ledger that records the source and target distance comparisons used by the finite net.
\item $F$ is the $\ContinuousMapUp$ row carrying pointwise continuity data at the displayed finite-net centers.
\item $R$ is the $\RatUp$ tolerance row that keeps every radius, shrink, and comparison inside rational data.
\item $U$ is the exported $\UniformModulusUp$ row produced by the displayed finite chain.
\item $H$ records componentwise $\hsame$ transport for compact, finite-net, metric, continuous-map, rational, and modulus rows.
\item $C$ records displayed $\Cont$ replay from finite-net coverage and pointwise continuity through the rational metric comparisons to the modulus export.
\item $P$ records $\Pkg$ provenance over $\CompactUp$, $\MetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\RatUp$, $\FiniteEpsilonNetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopCompactModulusChainUp}$ row.
\end{enumerate}
The carrier has no coordinate for open-cover compactness, selected Lebesgue numbers, sequential compactness, countable choice, or a completeness principle for $\RealUp$.
\end{definition}

\begin{theorem}[Bishop compact modulus chain NameCert obligations]
\label{thm:bishop-compact-modulus-chain-namecert-obligations}
For every accepted $\BishopCompactModulusChainUp$ carrier $B=(K,E,M,F,R,U,H,C,P,N)$, the local $\NameCert_{\BishopCompactModulusChainUp}$ surface is exhausted by the following five obligation rows:
\begin{enumerate}
\item compact finite-net exposure through $K$ and $E$;
\item metric and rational tolerance exposure through $M$ and $R$;
\item pointwise continuous-map exposure through $F$;
\item finite chain replay and uniform-modulus export through $C$ and $U$;
\item componentwise transport, provenance, and local naming through $H$, $P$, and $N$.
\end{enumerate}
Every modulus consumer must read these rows in the displayed order and cannot replace them by an open-cover proof, a selected Lebesgue number, a sequential compactness argument, countable choice, or ambient real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-compact-modulus-chain-carrier}. The rows $K,E,M,F,R,U,H,C,P,N$ are the full visible inventory.

The compact side is read through $K$ only after the finite-net row $E$ has displayed the net used by the requested rational tolerance. The metric and rational rows $M,R$ keep the comparison data visible before the continuity rows are inspected.

Next read $F$ at the displayed net centers and replay the chain through $C$. The exported row $U$ is therefore downstream of finite-net coverage, pointwise continuity, rational tolerance comparison, and metric transport.

Finally $H,P,N$ preserve and name the same packet. Since the carrier definition contains no coordinate for the excluded compactness or choice principles, the five listed rows exhaust the local certificate surface.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopCompactModulusChainUp$ consumer that reads a uniform-modulus row must first expose compact finite-net data, metric comparison data, continuous-map pointwise data, rational tolerance data, finite replay, transport, provenance, and local naming.}

\independenceWitness{compact, metric, continuousmap, uniformmodulus, rat, finiteepsilonnet: no listed sibling carries the full packet $B=(K,E,M,F,R,U,H,C,P,N)$, because the compact sibling supplies the source surface, the metric sibling supplies distance comparison, the continuous-map sibling supplies pointwise continuity, the rational sibling supplies tolerance arithmetic, the finite epsilon-net sibling supplies finite coverage, and the uniform-modulus sibling supplies the exported target row.}

\closureat{BishopCompactModulusChainUp}{seedStr}

\begin{closurestatus}{\BishopCompactModulusChainUp}
  \constructivestory{A $\BishopCompactModulusChainUp$ packet is generated from compact finite-net rows, metric comparison rows, continuous-map pointwise rows, rational tolerance rows, a uniform-modulus export row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-compact-modulus-chain-carrier} and \autoref{thm:bishop-compact-modulus-chain-namecert-obligations} seed the finite Bishop compact-modulus chain over Compact, Metric, ContinuousMap, UniformModulus, Rat, and FiniteEpsilonNet rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected Lebesgue numbers, sequential compactness, countable choice, Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net exposure, metric and rational tolerance exposure, pointwise continuity exposure, uniform-modulus export, transport, replay, provenance, and local naming for BEDC.Derived.BishopCompactModulusChainUp.}
\end{closurestatus}
