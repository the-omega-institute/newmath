\chapter{A Concrete Naming Certificate for $\CompactContinuityRadiusChoiceUp$}
\label{ch:concrete-instances-compact-continuity-radius-choice-namecert}
\origin{human}

Following Bishop and Bridges, the $\CompactContinuityRadiusChoiceUp$ packet records the finite radius-choice ledger used in the Heine--Cantor route. It reads compact finite-net data from \autoref{ch:concrete-instances-compactmetric-namecert}, pointwise continuity rows from \autoref{ch:concrete-instances-continuousmap-namecert}, finite lower-bound folds from \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, dyadic tolerance bookkeeping from \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the uniform-modulus consumer surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The chapter is a finite NameCert carrier for the radius handoff; it is not an open-cover compactness proof, a semantic continuity theorem, or a choice function over all points.

\begin{definition}[Compact continuity radius-choice carrier]
\label{def:compact-continuity-radius-choice-carrier}
A $\CompactContinuityRadiusChoiceUp$ carrier is a finite $\BHist$ packet
$$
R=(K,F,M,Q,D,L,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact metric finite-net row, read through $\CompactMetricUp$;
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row at the displayed finite centers;
\item $M$ is the finite center-to-radius matching ledger;
\item $Q$ is the positive rational radius ledger attached to the selected center rows;
\item $D$ is the $\DyadicRatCoreUp$ tolerance and comparison ledger;
\item $L$ is the $\FiniteNetMinimumFoldUp$ lower-bound fold over the displayed radius rows;
\item $U$ is the $\UniformModulusUp$ consumer handoff row reached from the finite lower-bound ledger;
\item $H$ records componentwise $\hsame$ transport for compact-net, continuous-map, radius, dyadic, fold, and handoff rows;
\item $C$ records displayed $\Cont$ replay from finite-net centers and pointwise radii to the uniform-modulus handoff;
\item $P$ is the $\Pkg$ provenance over $\CompactContinuityRadiusChoiceUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\FiniteNetMinimumFoldUp$, $\DyadicRatCoreUp$, $\HeineCantorUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CompactContinuityRadiusChoiceUp}$ row.
\end{enumerate}
The carrier exposes the finite radius ledger and its consumer handoff. It has no coordinate for an external radius-selection function, open-cover compactness, semantic function equality, or a point chosen outside the finite compact-net bundle.
\end{definition}

\begin{theorem}[Compact continuity radius-choice NameCert obligations]
\label{thm:compact-continuity-radius-choice-namecert-obligations}
For the carrier of \autoref{def:compact-continuity-radius-choice-carrier}, the local $\NameCert_{\CompactContinuityRadiusChoiceUp}$ surface consists exactly of carrier admission for $R=(K,F,M,Q,D,L,U,H,C,P,N)$; compact finite-net admission through $\CompactMetricUp$; pointwise-radius admission through $\ContinuousMapUp$; radius matching through $M,Q,D$; finite lower-bound folding through $\FiniteNetMinimumFoldUp$; the $\UniformModulusUp$ handoff through $U$; classifier preservation under $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. The compact finite-net row, continuous-map pointwise rows, radius ledgers, dyadic comparisons, lower-bound fold, uniform-modulus handoff, transport, replay, provenance, and local name are all visible coordinates.

The radius choice is finite. Each radius row is attached to a displayed compact-net center and a displayed pointwise-modulus row; the lower-bound fold consumes only those finitely many positive rational and dyadic comparisons.

The consumer handoff is therefore confined to the displayed route. Componentwise transport and replay preserve the same rows, while provenance and local naming record the route without adding a global selector, open-cover compactness object, semantic continuity theorem, or point outside the finite bundle.
\end{proof}

\begin{theorem}[Compact continuity radius-choice uniform-modulus handoff]
\label{thm:compact-continuity-radius-choice-uniform-modulus-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactContinuityRadiusChoiceUp$ carrier factors through the finite route
$$
\CompactMetricUp,\quad \ContinuousMapUp,\quad \DyadicRatCoreUp,\quad \FiniteNetMinimumFoldUp,\quad \UniformModulusUp .
$$
Equivalently, the handoff receives the compact finite-net centers, pointwise radius rows, dyadic tolerance comparisons, the finite lower-bound ledger, and the displayed transport, replay, provenance, and local naming rows. It receives no open-cover compactness object, no external radius-selection function, no semantic continuity theorem, and no point selected outside the finite net.
\end{theorem}
\begin{proof}
Start with the compact-net and pointwise-modulus rows. The carrier exposes $K$ and $F$ before any radius row can be folded, so every radius is tied to a displayed finite center and a displayed continuous-map modulus row.

Next read the finite lower-bound ledger. The rows $M,Q,D,L$ package the radius matching, positive rational radius data, dyadic comparisons, and finite minimum fold. This is the only source of a tolerance-only handoff to $\UniformModulusUp$.

Finally apply the structural rows. The route $H,C,P,N$ transports, replays, sources, and names exactly the finite radius-choice ledger, so a uniform-modulus consumer cannot add open-cover compactness, a global radius-selection function, semantic continuity, or an off-net selected point.
\end{proof}

\falsifiablePrediction{Every $\UniformModulusUp$ read of a $\CompactContinuityRadiusChoiceUp$ packet factors through CompactMetric finite-net centers, ContinuousMap pointwise radii, DyadicRatCore tolerance comparisons, a FiniteNetMinimumFold lower-bound ledger, transport, replay, provenance, and local naming.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, finitenetminimumfold, dyadicratcore, heinecantor: $\CompactContinuityRadiusChoiceUp$ is not bijective to any listed sibling because it jointly carries the finite compact-net radius ledger and the UniformModulus-facing lower-bound handoff.}

\closureat{CompactContinuityRadiusChoiceUp}{seedStr}

\begin{closurestatus}{\CompactContinuityRadiusChoiceUp}
  \constructivestory{A $\CompactContinuityRadiusChoiceUp$ packet is generated from compact finite-net centers, ContinuousMap pointwise radius rows, radius matching ledgers, dyadic tolerance comparisons, a finite lower-bound fold, a UniformModulus handoff row, hsame transports, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-continuity-radius-choice-carrier}, \autoref{thm:compact-continuity-radius-choice-namecert-obligations}, and \autoref{thm:compact-continuity-radius-choice-uniform-modulus-handoff} bind the finite radius-choice carrier feeding the Heine--Cantor uniform-modulus route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, open-cover compactness, semantic continuity, global radius selection, or off-net point choice.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier preservation, finite radius fold, and UniformModulus handoff for BEDC.Derived.CompactContinuityRadiusChoiceUp.}
\end{closurestatus}
