\chapter{A Concrete Naming Certificate for $\ConstructiveApartnessSpaceUp$}
\label{ch:concrete-instances-constructive-apartness-space-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\ConstructiveApartnessSpaceUp$ packet records an apartness-space interface in which positive separation is supplied as finite evidence rather than as the negation of equality. It sits between the located positive-gap route of \autoref{ch:concrete-instances-located-apartness-namecert}, the separated metric handoff of \autoref{ch:concrete-instances-apartness-metric-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter gives Real and Completion consumers a constructive separation certificate without importing trichotomy, quotient equality, selected representatives, or ambient $\RealUp$ completeness.

\begin{definition}[Constructive apartness-space carrier]
\label{def:constructive-apartness-space-carrier}
A $\ConstructiveApartnessSpaceUp$ carrier is a finite $\BHist$ packet
$$
S=(X,A,M,R,L,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the carried-object row with the local point classifier that will receive positive separation evidence.
\item $A$ is the $\LocatedApartnessUp$ row exposing two located observations, their common finite window, and the positive-gap data attached to that window.
\item $M$ is the optional $\MetricUp$ source row used when the separation is read through a metric distance.
\item $R$ is the $\RealUp$ seal row for the positive real witness that certifies the displayed gap.
\item $L$ is the $\ApartnessMetricUp$ handoff row tying a metric distance, positive real readback, and located witness data to the apartness read.
\item $T$ records stability of the apartness evidence under componentwise transport of the two observations, the common window, and the positive witness rows.
\item $H$ stores componentwise $\hsame$ transport for $X,A,M,R,L,T$, replay, provenance, and local naming.
\item $C$ stores displayed $\Cont$ replay from located positive-gap evidence to the local apartness classifier and, when present, through the metric handoff.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveApartnessSpaceUp$, $\LocatedApartnessUp$, $\ApartnessMetricUp$, $\MetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ConstructiveApartnessSpaceUp}$ row.
\end{enumerate}
The carrier records positive information only. It has no row for deciding equality of all pairs, quotient equality of real names, host inequality, selected limits, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Constructive apartness-space NameCert obligations]
\label{thm:constructive-apartness-space-namecert-obligations}
For every accepted $\ConstructiveApartnessSpaceUp$ carrier $S=(X,A,M,R,L,T,H,C,P,N)$, the local $\NameCert_{\ConstructiveApartnessSpaceUp}$ surface consists exactly of carrier admission, located positive-gap exposure through $A$, metric-source exposure through $M$ when the metric handoff is used, positive $\RealUp$ witness exposure through $R$, $\ApartnessMetricUp$ handoff through $L$, transport stability through $T$ and $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:constructive-apartness-space-carrier}. The rows $X,A,M,R,L,T,H,C,P,N$ are the whole accepted packet, so the NameCert surface has no source outside them.

The positive-separation data are read in constructive order. The located row $A$ supplies the inspected observations and finite window, the $\RealUp$ row $R$ supplies the positive witness, and the optional metric handoff $L$ ties that witness to the displayed $\MetricUp$ source row $M$.

Finally $T,H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier omits trichotomy, quotient equality, host inequality, selected limits, countable choice, and ambient completeness, none of those principles can become local obligations.
\end{proof}

\begin{theorem}[Apartness-space constructive separation]
\label{thm:apartness-space-constructive-separation}
Every separation exported by an accepted $\ConstructiveApartnessSpaceUp$ packet factors through the finite route
$$
\LocatedApartnessUp \longmapsto \RealUp \longmapsto \ApartnessMetricUp \longmapsto \ConstructiveApartnessSpaceUp .
$$
If the metric row is absent, the route still exposes the located positive-gap row, positive real witness, componentwise transport, replay, provenance, and local naming before the local apartness classifier is read. Thus the exported separation is positive finite evidence, not a negated equality test or a trichotomy decision.
\end{theorem}
\begin{proof}
Start with the located apartness row $A$. It displays the two observations, their common window, and the positive-gap evidence before any consumer can read the classifier of $X$.

Read the positive witness through $R$. When the separation is metric-facing, the rows $M$ and $L$ expose the metric source and the $\ApartnessMetricUp$ handoff before the public apartness read is available.

Use the transport and replay rows $T,H,C$. They preserve the same positive-gap route under componentwise movement of observations, windows, witnesses, and handoff rows.

The carrier has no row for negated equality, total comparison, quotient equality, selected representative, choice schedule, or ambient completion. The exported separation is therefore exactly the displayed positive route.
\end{proof}

\begin{theorem}[Constructive apartness-space nonescape]
\label{thm:apartness-space-nonescape}
No accepted $\ConstructiveApartnessSpaceUp$ consumer can extract trichotomy, quotient equality of real names, host inequality, selected representatives, countable choice, or ambient $\RealUp$ completeness from the packet. Any Real-facing or Completion-facing read must consume only the displayed located-apartness, metric, positive-real, transport, replay, provenance, and local NameCert rows.
\end{theorem}
\begin{proof}
Read the obligation surface of \autoref{thm:constructive-apartness-space-namecert-obligations}. It lists all rows that a consumer may inspect before receiving a separation claim.

Apply the constructive separation route of \autoref{thm:apartness-space-constructive-separation}. It forces every exported separation through located positive-gap evidence, positive real witness data, and the optional metric handoff.

The remaining rows $H,C,P,N$ only transport, replay, source, and locally name that route. Since no excluded principle has a carrier coordinate, no accepted consumer can extract one from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\ConstructiveApartnessSpaceUp$ consumer displays located-apartness evidence, positive Real witness data, optional Metric and ApartnessMetric handoff rows, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows before it reads a constructive separation.}

\independenceWitness{located-apartness, apartness-metric, metric, real: $\ConstructiveApartnessSpaceUp$ binds located positive evidence, optional metric handoff, real witness data, transport, replay, provenance, and local naming as one constructive separation packet; it is not reducible to any single listed sibling.}

\closureat{ConstructiveApartnessSpaceUp}{seedStr}

\begin{closurestatus}{\ConstructiveApartnessSpaceUp}
  \constructivestory{A $\ConstructiveApartnessSpaceUp$ packet is generated from a carried-object row, located positive-gap evidence, an optional Metric source, a positive Real witness, an ApartnessMetric handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-apartness-space-carrier}, \autoref{thm:constructive-apartness-space-namecert-obligations}, \autoref{thm:apartness-space-constructive-separation}, and \autoref{thm:apartness-space-nonescape} seed the constructive positive-separation route over $\LocatedApartnessUp$, $\ApartnessMetricUp$, $\MetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim real trichotomy, quotient equality, selected representatives, countable choice, ambient real completion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located positive-gap exposure, metric-source handoff, positive Real witness exactness, transport, replay, provenance, and local naming for BEDC.Derived.ConstructiveApartnessSpaceUp.}
\end{closurestatus}
