\chapter{A Concrete Naming Certificate for $\MetaCICResidualDiamondFrontierUp$}
\label{ch:concrete-instances-metacic-residual-diamond-frontier-namecert}
\origin{ai}

The $\MetaCICResidualDiamondFrontierUp$ packet realizes the residual-diamond frontier of \autoref{ch:metacic-confluence-audit-map} as finite confluence-facing NameCert data. It sits beside the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the confluence critical-pair boundary of \autoref{ch:concrete-instances-metacic-confluence-critical-pair-boundary-namecert}, and the closed-normal confluence seal of \autoref{ch:concrete-instances-closednormalconfluenceseal-namecert}. The packet records candidate residual diamonds as bounded finite evidence while keeping $\mathrm{BetaParallelSubstitutionCompatibility}$, $\mathrm{BetaParallelDiamondClosed}$, arbitrary closed-term Church--Rosser, subject reduction, evaluator equality, quotient term equality, and kernel consistency outside the carrier.

\falsifiablePrediction{Every accepted $\MetaCICResidualDiamondFrontierUp$ read exposes the residual-substitution premise and obstruction rows before any residual-diamond handoff is consumed.}

\independenceWitness{metacic-parallel-diamond-frontier, metacic-residual-substitution-boundary, metacic-confluence-critical-pair-boundary, closednormalconfluenceseal: the parallel-diamond frontier records a conditional parallel handoff without this packet's residual-diamond candidate grid; the residual-substitution boundary names the missing compatibility premise without candidate residual joins; the critical-pair boundary records local peak rows without the residual-diamond handoff; and the closed-normal confluence seal supplies normal-source joins without residual substitution. None is bijective to $\MetaCICResidualDiamondFrontierUp$.}

\begin{definition}[MetaCIC residual-diamond frontier carrier]
\label{def:metacic-residual-diamond-frontier-carrier}
A $\MetaCICResidualDiamondFrontierUp$ carrier is a finite $\BHist$ packet
$$
F=(K,R,J,B,O,X,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the finite critical-pair row, recording the inspected peak, branch labels, and local source window.
\item $R$ is the residual-substitution premise row retained from the residual-substitution boundary.
\item $J$ is the candidate-join row, recording the proposed residual-diamond common target and branch-local join routes.
\item $B$ is the bounded-checker row, recording the finite checker budget for residual descendants, closedness, support, and branch compatibility.
\item $O$ is the obstruction row naming every endpoint not discharged by the packet: residual-substitution compatibility, premise-free parallel diamond, arbitrary closed-term Church--Rosser, subject reduction, evaluator equality, quotient term equality, and kernel consistency.
\item $X$ is the residual-diamond handoff row exposed only after $K,R,J,B,O$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for critical-pair, residual-substitution, candidate-join, bounded-checker, obstruction, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a confluence query through $K,R,J,B,O$ and then to $X$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualDiamondFrontierUp$, $\MetaCICParallelDiamondFrontierUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\MetaCICConfluenceCriticalPairBoundaryUp$, $\ClosedNormalConfluenceSealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICResidualDiamondFrontierUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,R,J,B,O,X,H,C,P,N$. It has no coordinate for a discharged substitution-compatibility theorem, a premise-free parallel diamond, an arbitrary closed Church--Rosser theorem, subject reduction, evaluator equality, quotient equality of terms, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC residual-diamond frontier obligations]
\label{thm:metacic-residual-diamond-frontier-obligations}
For every accepted $\MetaCICResidualDiamondFrontierUp$ carrier $F=(K,R,J,B,O,X,H,C,P,N)$, the local $\NameCert_{\MetaCICResidualDiamondFrontierUp}$ surface consists exactly of finite critical-pair exposure through $K$, residual-substitution premise retention through $R$, candidate-join exposure through $J$, bounded-checker accounting through $B$, obstruction retention through $O$, residual-diamond handoff non-escape through $X$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.

The Lean-side TasteGate surface must provide \texttt{Nontrivial} $\MetaCICResidualDiamondFrontierUp$ and \texttt{FieldFaithful} $\MetaCICResidualDiamondFrontierUp$ instances in \texttt{TasteGate.lean}: the first separates genuine frontier packets from the empty carrier, and the second asserts that the classifier reads every displayed field $K,R,J,B,O,X,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-diamond-frontier-carrier}. The projection exposes exactly the critical-pair, residual-substitution, candidate-join, bounded-checker, obstruction, handoff, transport, replay, provenance, and local naming rows.

Read the confluence rows in order. The critical-pair row $K$ fixes the inspected peak, the residual-substitution row $R$ remains visible as a retained premise, the candidate-join row $J$ records the proposed residual diamond, and the bounded checker $B$ accounts for the finite branch checks.

The obstruction row $O$ is retained before $X$ can be consumed. Thus the residual-diamond handoff is a bounded frontier row, not a discharge of residual substitution, a premise-free parallel diamond, or a closed Church--Rosser endpoint.

Finally $H,C,P,N$ transport, replay, source, and locally name the same finite route. The stated TasteGate obligations are precisely the classifier-readiness conditions for nonempty, field-faithful packet data.
\end{proof}

\begin{theorem}[MetaCIC residual-diamond frontier confluence handoff]
\label{thm:metacic-residual-diamond-frontier-confluence-handoff}
Every confluence consumer of an accepted $\MetaCICResidualDiamondFrontierUp$ packet must display the route
$$
K \longmapsto R \longmapsto J \longmapsto B \longmapsto O \longmapsto X \longmapsto H,C,P,N
$$
before consuming the residual-diamond handoff. The handoff may feed the conditional confluence boundary of \autoref{ch:metacic-confluence-audit-map}, but it does not prove $\mathrm{BetaParallelSubstitutionCompatibility}$, does not prove $\mathrm{BetaParallelDiamondClosed}$, and does not prove arbitrary closed-term Church--Rosser for $\mathrm{BetaStarStep}$.
\end{theorem}
\begin{proof}
Let $F=(K,R,J,B,O,X,H,C,P,N)$ be accepted. By \autoref{def:metacic-residual-diamond-frontier-carrier}, the handoff row $X$ is downstream of critical-pair exposure, residual-substitution premise retention, candidate-join exposure, bounded checking, and obstruction retention.

Apply the obligation surface of \autoref{thm:metacic-residual-diamond-frontier-obligations}. It forces every residual-diamond read to keep $K,R,J,B,O$ visible before the handoff row is available.

The structural rows preserve the same route. Componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ do not add any of the omitted confluence endpoints. Therefore the handoff remains a finite residual-diamond frontier certificate.
\end{proof}

\begin{closurestatus}{\MetaCICResidualDiamondFrontierUp}
  \constructivestory{A $\MetaCICResidualDiamondFrontierUp$ packet is generated from critical-pair rows, residual-substitution premise retention, candidate-join rows, bounded-checker rows, obstruction retention, a residual-diamond handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-diamond-frontier-carrier}, \autoref{thm:metacic-residual-diamond-frontier-obligations}, and \autoref{thm:metacic-residual-diamond-frontier-confluence-handoff} seed the residual-diamond frontier over critical-pair, residual-substitution, candidate-join, bounded-checker, obstruction, handoff, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not claim residual-substitution compatibility, premise-free parallel diamond, arbitrary closed-term Church--Rosser, subject reduction, evaluator equality, quotient term equality, kernel consistency, public closure, or a standard bridge.}
  \upgradepath{Formalization can encode BEDC.Derived.MetaCICResidualDiamondFrontierUp as carrier admission, residual-diamond obligation rows, confluence handoff non-escape, Nontrivial TasteGate evidence, FieldFaithful TasteGate evidence, transport, replay, provenance, and local naming.}
\end{closurestatus}
