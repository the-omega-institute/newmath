\chapter{A Concrete Naming Certificate for $\BishopEquicontinuityModulusUp$}
\label{ch:concrete-instances-bishop-equicontinuity-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopEquicontinuityModulusUp$ packet records the finite modulus data by which an equicontinuous family over a compact metric source is read as a uniform finite-window handoff. It sits beside the shared-radius surface of \autoref{ch:concrete-instances-equicontinuity-namecert}, the finite-net handoff of \autoref{ch:concrete-instances-finite-net-equicontinuity-namecert}, the compact-uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the constructive compact-metric neighbour of the L10 real and completion route; it does not import open-cover compactness, selected subsequences, host function-space compactness, quotient equality of real names, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop equicontinuity modulus carrier]
\label{def:bishop-equicontinuity-modulus-carrier}
A $\BishopEquicontinuityModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(E,N,U,W,R,S,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\EquicontinuityUp$ shared-radius row, read over the same compact metric source and finite family ledger.
\item $N$ is the $\FiniteNetEquicontinuityUp$ handoff row, recording the compact finite-net centers and the family-radius comparisons available at the inspected scale.
\item $U$ is the $\CompactUniformContinuityUp$ row consumed after the finite-net and shared-radius data are displayed.
\item $W$ is the $\StreamNameUp$ finite-window row for source and target observations.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same finite windows and tolerance histories.
\item $S$ is the terminal $\RealUp$ seal reached only after $E,N,U,W,R$ have fixed the finite modulus route.
\item $H$ records componentwise $\hsame$ transport for the shared-radius, finite-net, compact-uniform, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the finite route
$$
E,N,U \longmapsto W,R,S,H,P,L .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopEquicontinuityModulusUp$, $\EquicontinuityUp$, $\FiniteNetEquicontinuityUp$, $\CompactUniformContinuityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\BishopEquicontinuityModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,N,U,W,R,S,H,C,P,L$. It has no coordinate for open-cover compactness, selected subsequences, host function-space compactness, quotient equality of real names, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop equicontinuity modulus NameCert obligations]
\label{thm:bishop-equicontinuity-modulus-namecert-obligations}
For every accepted $\BishopEquicontinuityModulusUp$ carrier
$$
M=(E,N,U,W,R,S,H,C,P,L),
$$
the local $\NameCert_{\BishopEquicontinuityModulusUp}$ surface consists exactly of shared-radius admission through $E$, finite-net equicontinuity admission through $N$, compact-uniform-continuity admission through $U$, finite-window exposure through $W$, regular rational readback through $R$, terminal Real sealing through $S$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-equicontinuity-modulus-carrier}. The analytic rows are $E,N,U,W,R,S$, and the structural rows are $H,C,P,L$.

The equicontinuity and finite-net clauses are finite. The row $E$ supplies the shared family-radius data, while $N$ supplies the compact finite-net handoff on the same source route.

The compact-uniform and L10 clauses are then read through $U,W,R,S$. The finite windows and regular rational readback determine the Real-facing seal without adding a quotient real, a selected limit, or an ambient completeness principle.

Finally $H,C,P,L$ transport, replay, source, and name exactly the displayed rows. Since none of the excluded host objects is a carrier coordinate, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Bishop equicontinuity modulus finite-net handoff]
\label{thm:bishop-equicontinuity-modulus-finite-net-handoff}
Every $\CompactUniformContinuityUp$ or L10 real-facing consumer of an accepted $\BishopEquicontinuityModulusUp$ carrier must first read the shared route
$$
\EquicontinuityUp,\FiniteNetEquicontinuityUp,\CompactUniformContinuityUp,\StreamNameUp,\RegSeqRatUp,\RealUp .
$$
The handoff consists of the shared-radius row $E$, finite-net row $N$, compact-uniform row $U$, window row $W$, readback row $R$, seal row $S$, and the structural rows $H,C,P,L$.
\end{theorem}
\begin{proof}
Start with the obligation theorem. It identifies $E,N,U,W,R,S,H,C,P,L$ as the complete public surface of the carrier.

The finite-net stage is downstream of the shared-radius row. A consumer reads $E$ and then $N$ before the compact-uniform row $U$ is public, so the route cannot be replaced by a selected finite net outside the packet.

The L10-facing stage is downstream of that compact-uniform handoff. Rows $W$ and $R$ expose finite windows and regular rational readback before $S$ seals the Real-facing output.

The rows $H,C,P,L$ preserve this order by componentwise transport, replay, provenance, and local naming. Therefore every compact-uniform or real-facing consumer receives the displayed finite-net handoff rather than open-cover compactness, host function-space compactness, selected subsequences, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Bishop equicontinuity modulus nonescape]
\label{thm:bishop-equicontinuity-modulus-nonescape}
No accepted $\BishopEquicontinuityModulusUp$ packet exports open-cover compactness, selected subsequences, host function-space compactness, quotient equality of real names, countable choice, or ambient $\RealUp$ completeness. Its public modulus content is exactly the finite row package
$$
E,N,U,W,R,S,H,C,P,L .
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:bishop-equicontinuity-modulus-namecert-obligations}. Every public read factors through the displayed tuple $E,N,U,W,R,S,H,C,P,L$.

The shared-radius input is confined to $E$, the finite-net input to $N$, the compact-uniform handoff to $U$, and the real-facing route to $W,R,S$. The structural rows only transport, replay, package, and name these displayed rows.

None of the excluded principles is present as a carrier coordinate or as a structural row. Exporting any of them would leave the local $\BishopEquicontinuityModulusUp$ NameCert surface.
\end{proof}

\closureat{BishopEquicontinuityModulusUp}{seedStr}

\begin{closurestatus}{\BishopEquicontinuityModulusUp}
  \constructivestory{A $\BishopEquicontinuityModulusUp$ packet is generated from an EquicontinuityUp shared-radius row, a FiniteNetEquicontinuityUp finite-net handoff, a CompactUniformContinuityUp consumer row, StreamNameUp windows, RegSeqRatUp readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-equicontinuity-modulus-carrier}, \autoref{thm:bishop-equicontinuity-modulus-namecert-obligations}, \autoref{thm:bishop-equicontinuity-modulus-finite-net-handoff}, and \autoref{thm:bishop-equicontinuity-modulus-nonescape} seed the finite modulus route over equicontinuity, finite-net, compact-uniform, window, readback, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.BishopEquicontinuityModulusUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, selected subsequences, host function-space compactness, quotient equality of real names, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-radius admission, finite-net handoff, compact-uniform handoff, finite-window replay, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopEquicontinuityModulusUp.}
\end{closurestatus}
