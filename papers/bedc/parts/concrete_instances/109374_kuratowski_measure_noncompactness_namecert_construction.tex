\chapter{A Concrete Naming Certificate for $\KuratowskiMeasureNoncompactnessUp$}
\label{ch:concrete-instances-kuratowski-measure-noncompactness-namecert}
\origin{human}

Following Kuratowski, the $\KuratowskiMeasureNoncompactnessUp$ packet records a constructive compactness-defect ledger by finite diameter-cover and finite-net rows rather than by a host infimum over all covers. It sits over the complete metric boundary of \autoref{ch:concrete-instances-completemetric-namecert}, the total-bounded finite-net route of \autoref{ch:concrete-instances-totallybounded-namecert}, the compact metric consumer of \autoref{ch:concrete-instances-compactmetric-namecert}, the finite epsilon-net witness of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet names a finite compactness-defect surface for later compactness and functional-analysis consumers; it is not an open-cover compactness theorem, not a choice-selected cover, not a quotient hyperspace, and not an ambient real infimum principle.

\begin{definition}[Kuratowski measure of noncompactness carrier]
\label{def:kuratowski-measure-noncompactness-carrier}
A $\KuratowskiMeasureNoncompactnessUp$ carrier is a finite $\BHist$ packet
$$
K=(X,C,T,E,D,V,H,R,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ is the displayed $\MetricSpaceUp$ or $\CompleteMetricUp$ source row whose bounded subset is being inspected.
\item $C$ is the $\CompleteMetricUp$ boundary row exposed before completion-facing consumers read any defect claim.
\item $T$ is the $\TotallyBoundedUp$ finite-net comparison row, recording the tolerance-indexed route by which defect zero may be compared with total boundedness.
\item $E$ is the $\FiniteEpsilonNetUp$ witness row, listing the displayed centers, tolerance cell, and coverage ledger available at the inspected scale.
\item $D$ is the finite diameter-cover row, recording the listed cover pieces and their displayed dyadic diameter bounds.
\item $V$ is the compactness-defect value ledger, comparing the requested dyadic threshold with the finite diameter and finite-net rows without taking a host infimum.
\item $H$ records componentwise $\hsame$ transport for the source, completeness, total-boundedness, finite-net, diameter-cover, value, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from a compactness-defect request through $X,C,T,E,D,V,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\KuratowskiMeasureNoncompactnessUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, $\TotallyBoundedUp$, $\CompactMetricUp$, $\FiniteEpsilonNetUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KuratowskiMeasureNoncompactnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,C,T,E,D,V,H,R,P,N$. It has no coordinate for an open cover, a selected compactness proof, a host powerset, a quotient compactum, a chosen finite subcover, countable choice, or an ambient real infimum.
\end{definition}

\begin{theorem}[Kuratowski measure of noncompactness NameCert obligations]
\label{thm:kuratowski-measure-noncompactness-namecert-obligations}
For every accepted $\KuratowskiMeasureNoncompactnessUp$ carrier $K=(X,C,T,E,D,V,H,R,P,N)$, the local $\NameCert_{\KuratowskiMeasureNoncompactnessUp}$ surface consists exactly of carrier admission, complete-metric source exposure, total-bounded finite-net comparison, finite epsilon-net witness visibility, finite diameter-cover exactness, compactness-defect value readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.

Every compactness or functional-analysis consumer of the packet must read the displayed route
$$
X,C \longmapsto T,E,D,V \longmapsto H,R,P,N
$$
before treating the defect ledger as compactness-facing evidence.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kuratowski-measure-noncompactness-carrier}. The only source and completion-facing rows are $X$ and $C$, and the only compactness-defect rows are the finite total-boundedness, finite-net, diameter-cover, and value ledgers $T,E,D,V$.

The finite-net and diameter-cover rows are displayed before the value ledger is read. Thus a consumer sees centers, coverage data, cover pieces, dyadic diameter bounds, and the requested threshold as finite rows rather than as a hidden open-cover or infimum argument.

Finally $H,R,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no open-cover, selected subcover, host powerset, quotient compactum, countable-choice, or ambient-infimum coordinate, those objects cannot be exported by the local classifier.
\end{proof}

\begin{theorem}[Kuratowski measure complete-metric handoff]
\label{thm:kuratowski-measure-noncompactness-complete-metric-handoff}
Let $K=(X,C,T,E,D,V,H,R,P,N)$ be an accepted $\KuratowskiMeasureNoncompactnessUp$ packet. Any completion-facing consumer that reads the compactness-defect value ledger $V$ must first expose the complete-metric row $C$, the total-boundedness comparison row $T$, and the finite epsilon-net row $E$, and must then replay the same finite diameter-cover row $D$ through $H,R,P,N$ before reaching a $\CompactMetricUp$ or functional-analysis consumer.

The handoff exports only a finite compactness-defect packet over complete metric and finite-net rows. It does not claim open-cover compactness, selected finite subcovers, quotient compacta, countable choice, or an ambient $\RealUp$ infimum.
\end{theorem}
\begin{proof}
Start from the accepted carrier in \autoref{def:kuratowski-measure-noncompactness-carrier}. The value row $V$ is downstream of $X,C,T,E,D$, so it cannot be read before the complete-metric source and finite compactness rows have been displayed.

Use the finite epsilon-net route of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}. The row $E$ supplies centers, tolerance, and coverage data, while $T$ records the total-bounded comparison and $D$ records the finite diameter cover at the inspected scale.

The replay rows $H,R,P,N$ preserve that route when a $\CompactMetricUp$ or functional-analysis consumer asks for compactness-facing evidence. Because no carrier coordinate stores an open cover, selected subcover, quotient compactum, countable-choice schedule, or ambient infimum, the consumer receives only the displayed compactness-defect handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\KuratowskiMeasureNoncompactnessUp$ compactness-defect read factors through one complete metric source row, one total-boundedness comparison row, one finite epsilon-net witness, one finite diameter-cover ledger, one dyadic value ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before it can be consumed by a compact metric or functional-analysis route.}

\independenceWitness{completemetric, totallybounded, compactmetric, finite-epsilon-net, dyadicratcore: $\KuratowskiMeasureNoncompactnessUp$ is not bijective to any listed sibling because those siblings separately supply completeness, finite-net coverage, compact-metric consumption, and dyadic tolerance rows, while this packet binds them to one compactness-defect value ledger.}

\closureat{KuratowskiMeasureNoncompactnessUp}{seedStr}

\begin{closurestatus}{\KuratowskiMeasureNoncompactnessUp}
  \constructivestory{A $\KuratowskiMeasureNoncompactnessUp$ packet is generated from a metric or complete-metric source row, total-bounded finite-net comparison, finite epsilon-net witness, finite diameter-cover ledger, dyadic compactness-defect value row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:kuratowski-measure-noncompactness-carrier}, \autoref{thm:kuratowski-measure-noncompactness-namecert-obligations}, and \autoref{thm:kuratowski-measure-noncompactness-complete-metric-handoff} seed the constructive compactness-defect packet over complete metric, finite-net, diameter-cover, dyadic value, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, choice-selected finite subcovers, quotient compacta, countable choice, host powersets, ambient Real infima, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, complete-metric handoff, total-bounded finite-net comparison, finite epsilon-net witness visibility, finite diameter-cover exactness, compactness-defect value readback, structural transport, and non-escape for BEDC.Derived.KuratowskiMeasureNoncompactnessUp.}
\end{closurestatus}
