\chapter{A Concrete Naming Certificate for $\CompactOscillationTreeUp$}
\label{ch:concrete-instances-compact-oscillation-tree-namecert}
\origin{human}

Following Bishop--Bridges compact uniform-continuity proofs, the $\CompactOscillationTreeUp$ packet records the finite cover-refinement tree by which local oscillation windows are folded into a uniform-modulus handoff. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite cover nerve of \autoref{ch:concrete-instances-finite-cover-nerve-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the Real seal of \autoref{ch:concrete-instances-real-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names only displayed finite tree data; it is not open-cover compactness, an external Lebesgue number selector, a host modulus function, quotient equality of maps, or ambient $\RealUp$ completeness.

\begin{definition}[Compact oscillation tree carrier]
\label{def:compact-oscillation-tree-carrier}
A $\CompactOscillationTreeUp$ carrier is a finite $\BHist$ packet
$$
T=(K,F,V,D,Q,R,O,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the finite compact-net cells and coverage witnesses.
\item $F$ is the $\ContinuousMapUp$ pointwise inspection row attached to the displayed compact source.
\item $V$ is the $\FiniteCoverNerveUp$ cover-refinement row, listing finite parent-child cover incidences.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the dyadic oscillation scale used at each node.
\item $Q$ is the $\RegSeqRatUp$ readback row for rational lower-bound and oscillation-window values.
\item $R$ is the terminal $\RealUp$ seal row for the displayed real-valued bounds.
\item $O$ is the compact oscillation tree row. It records the finite rooted tree, node windows, child refinements, and leaf oscillation bounds.
\item $U$ is the $\UniformModulusUp$ handoff row exported only after $K,F,V,D,Q,R,O$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for source, pointwise map, cover refinement, dyadic tolerance, rational readback, Real seal, oscillation tree, uniform modulus, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a compact-uniform query through $K,F,V,D,Q,R,O,U,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactOscillationTreeUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\FiniteCoverNerveUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactOscillationTreeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,F,V,D,Q,R,O,U,H,C,P,N$. It has no coordinate for arbitrary open-cover compactness, a selected Lebesgue number, a host modulus function, quotient equality of continuous maps, or ambient completeness.
\end{definition}

\begin{theorem}[Compact oscillation tree NameCert obligations]
\label{thm:compact-oscillation-tree-namecert-obligations}
For every accepted $\CompactOscillationTreeUp$ carrier
$$
T=(K,F,V,D,Q,R,O,U,H,C,P,N),
$$
the local $\NameCert_{\CompactOscillationTreeUp}$ surface consists exactly of compact-metric source exposure through $K$, continuous-map pointwise inspection through $F$, finite cover-refinement exposure through $V$, dyadic tolerance exposure through $D$, rational readback through $Q$, Real sealing through $R$, finite oscillation-tree exposure through $O$, uniform-modulus handoff through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-oscillation-tree-carrier}. The source rows $K,F,V$ expose the compact finite net, the pointwise continuous-map checks, and the finite cover-refinement incidence data.

Rows $D,Q,R$ attach the numerical readback. They display dyadic tolerances, rational oscillation-window values, and Real value seals before any uniform-modulus consumer can inspect the tree.

The tree row $O$ is therefore read as finite combinatorial data over already displayed cover and tolerance rows. The handoff row $U$ can only consume that finite route, while $H,C,P,N$ transport, replay, source, and name it.
\end{proof}

\begin{theorem}[Compact oscillation tree uniform-modulus handoff]
\label{thm:compact-oscillation-tree-uniform-modulus-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactOscillationTreeUp$ carrier first reads the finite route
$$
\CompactMetricUp \longmapsto \ContinuousMapUp \longmapsto \FiniteCoverNerveUp \longmapsto \DyadicRatCoreUp,\RegSeqRatUp,\RealUp \longmapsto \CompactOscillationTreeUp .
$$
Thus the exported modulus is a finite cover-refinement and oscillation-window handoff, not an external compactness theorem, selected Lebesgue number, host modulus function, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with a compact-uniform query. It must expose the compact-metric row $K$ and pointwise continuous-map row $F$ before it can read any refinement data.

The finite cover route is then $V,D,Q,R$. These rows display the cover-refinement incidences, dyadic oscillation scales, rational lower-bound reads, and Real seals needed at the tree nodes.

Only after those rows are present can the query read $O$ and then $U$. Componentwise transport, replay, provenance, and local naming preserve the same finite order, so no hidden compactness or host modulus coordinate is exported.
\end{proof}

\closureat{CompactOscillationTreeUp}{seedStr}

\begin{closurestatus}{\CompactOscillationTreeUp}
  \constructivestory{A $\CompactOscillationTreeUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise checks, FiniteCoverNerve refinement rows, DyadicRatCore tolerances, RegSeqRat rational readback, Real seals, a finite oscillation tree, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-oscillation-tree-carrier}, \autoref{thm:compact-oscillation-tree-namecert-obligations}, and \autoref{thm:compact-oscillation-tree-uniform-modulus-handoff} seed the finite cover-refinement and oscillation-window route for compact uniform-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected Lebesgue numbers, host modulus functions, quotient equality of continuous maps, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-metric source exposure, continuous-map pointwise inspection, finite cover-refinement exactness, dyadic tolerance exposure, rational readback, Real sealing, oscillation-tree handoff, UniformModulus export, transport, replay, provenance, and local naming for BEDC.Derived.CompactOscillationTreeUp.}
\end{closurestatus}
