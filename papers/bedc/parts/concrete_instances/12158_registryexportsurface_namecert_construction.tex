\chapter{A Concrete Naming Certificate for $\RegistryExportSurfaceUp$}
\label{ch:concrete-instances-registry-export-surface-namecert}
\origin{ai}

The $\RegistryExportSurfaceUp$ packet realizes the export-control surface of \autoref{def:philosophy-export-control-surface} inside the auditable-generativity programme of \autoref{ch:visions-philosophy-auditable-generativity}. It sits beside the cannot-claim registry packet of \autoref{ch:concrete-instances-axiszeckendorfcannotclaim-namecert}, the verified-output boundary of \autoref{ch:concrete-instances-largemodeloutputverifier-namecert}, the focused finite ledger of \autoref{ch:concrete-instances-focused-multi-hist-ledger-namecert}, the GroundCompiler recognition surface of \autoref{ch:concrete-instances-groundcompilerrecognition-namecert}, and the NameCert audit pattern of \autoref{ch:concrete-instances-physical-namecert-audit-namecert}. Those siblings supply refusal rows, checker-facing export, focused ledger scope, machine recognition, and naming-audit discipline; this carrier binds claim registration, blocking, status, machine target, audit route, and non-smuggling rows before a registry claim may leave the philosophy package.

\begin{definition}[Registry export surface carrier]
\label{def:registry-export-surface-carrier}
A $\RegistryExportSurfaceUp$ carrier is a finite $\BHist$ packet
$$
E=(R,B,S,M,A,U,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $R$ is the registration row, recording the registry entry in which the claim is allowed to appear;
\item $B$ is the blocking row, recording the cannot-claim or refusal entry that prevents export when the claim is not licensed;
\item $S$ is the status row, recording whether the claim is exported, blocked, horizon-facing, or held as a formalization target;
\item $M$ is the machine-target row, recording the exact machine-facing consumer of the registry entry;
\item $A$ is the audit row, recording the finite verification, closure-status, or ledger route that the machine target may inspect;
\item $U$ is the non-smuggling row, recording the forbidden readings that the export must not import from prose, host semantics, model output, or hidden registry state;
\item $H$ stores componentwise $\hsame$ transport for the registration, blocking, status, machine-target, audit, non-smuggling, continuation, provenance, and local naming rows;
\item $C$ stores displayed $\Cont$ replay for registration lookup, blocking lookup, status inspection, machine-target export, audit replay, and non-smuggling refusal;
\item $P$ is the $\Pkg$ provenance over $\RegistryExportSurfaceUp$, $\AxisZeckendorfCannotClaimUp$, $\LargeModelOutputVerifierUp$, $\FocusedMultiHistLedgerUp$, $\GroundCompilerRecognitionUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\RegistryExportSurfaceUp}$ row.
\end{enumerate}
The carrier contains no coordinate for an unregistered claim, a private override of $B$, a hidden machine target, or a prose-only export route.
\end{definition}
\leandef{BEDC.Derived.RegistryExportSurfaceUp.RegistryExportSurfaceUp}

\begin{theorem}[Registry export surface NameCert obligations]
\label{thm:registry-export-surface-namecert-obligations}
For the carrier of \autoref{def:registry-export-surface-carrier}, the local $\NameCert_{\RegistryExportSurfaceUp}$ surface consists exactly of six obligation rows:
\begin{enumerate}
\item carrier admission for $E=(R,B,S,M,A,U,H,C,P,N)$;
\item registration and blocking coverage through $R$ and $B$;
\item status exactness through $S$, requiring exported, blocked, horizon-facing, and formalization-target readings to remain distinct;
\item machine-target and audit exposure through $M$ and $A$;
\item non-smuggling accountability through $U$, requiring every forbidden reading to remain visible at the export boundary;
\item transport, replay, provenance, and local naming through $H,C,P,N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the finite packet of \autoref{def:registry-export-surface-carrier}. The rows $R,B,S,M,A,U$ already separate registration, refusal, status, machine target, audit route, and forbidden readings.

Registration and blocking coverage are read first. A consumer may export a claim through $R$ or block it through $B$, but it cannot erase the blocked case by reclassifying it as an exported registry entry.

The status row $S$ then determines which registry reading is available to the machine target $M$. The audit row $A$ supplies the finite route that the target may inspect, while the non-smuggling row $U$ records the readings that remain forbidden.

Finally $H,C,P,N$ transport, replay, package, and name the same rows. Since the carrier has no further coordinate, the six obligation rows exhaust the local $\NameCert_{\RegistryExportSurfaceUp}$ surface.
\end{proof}

\begin{theorem}[Registry export surface non-escape]
\label{thm:registry-export-surface-nonescape}
Let
$$
E=(R,B,S,M,A,U,H,C,P,N)
$$
be an accepted $\RegistryExportSurfaceUp$ packet. Any public consumer that exports a registry claim must read the registration row $R$, blocking row $B$, status row $S$, machine-target row $M$, audit row $A$, non-smuggling row $U$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$. The export cannot be recovered from prose synthesis, model output, hidden registry state, or a machine target that bypasses the blocking row.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:registry-export-surface-namecert-obligations}. They place registration, blocking, status, machine-target export, audit replay, and non-smuggling accountability inside one finite packet.

If a route omits $B$, the route no longer knows whether the claim is blocked by a registry entry. If it omits $U$, the route no longer records which prose, host-semantic, model-output, or hidden-state readings are forbidden.

The rows $H,C,P,N$ bind these checks to the same displayed packet. Hence every accepted export remains inside the listed rows, and any route that bypasses them exits the $\RegistryExportSurfaceUp$ carrier rather than exporting a BEDC registry claim.
\end{proof}

\begin{theorem}[Registry export surface status exactness]
\label{thm:registry-export-surface-status-exactness}
Let
$$
E=(R,B,S,M,A,U,H,C,P,N)
$$
be an accepted $\RegistryExportSurfaceUp$ $\BHist$ packet with $\hsame$ transport $H$, $\Cont$ replay $C$, provenance package $P$, and local $\NameCert_{\RegistryExportSurfaceUp}$ row $N$. The status row $S$ keeps the exported, blocked, horizon-facing, and formalization-target readings as separate registry statuses: no $\NameCert_{\RegistryExportSurfaceUp}$ audit may identify any two of these readings while preserving the displayed registration row $R$, blocking row $B$, machine-target row $M$, audit row $A$, and non-smuggling row $U$.
\end{theorem}
\begin{proof}
Unpack the carrier of \autoref{def:registry-export-surface-carrier}. The row $S$ is the only displayed coordinate assigned to export status, and the definition lists exported, blocked, horizon-facing, and formalization-target readings as the alternatives recorded there.

Apply the NameCert obligations of \autoref{thm:registry-export-surface-namecert-obligations}. Their third row requires status exactness through $S$, and their surrounding rows keep $R,B,M,A,U,H,C,P,N$ in the same finite packet.

Finally use the non-escape theorem, \autoref{thm:registry-export-surface-nonescape}. A consumer that tries to merge two status readings must still read $B$ and $U$ through the same transport and replay rows; if it bypasses either row, it no longer exports from the $\RegistryExportSurfaceUp$ carrier. Thus the four status readings remain distinct for every accepted packet audit.
\end{proof}

\begin{theorem}[Registry export surface machine-target audit route]
\label{thm:registry-export-surface-machine-target-audit-route}
Let
$$
E=(R,B,S,M,A,U,H,C,P,N)
$$
be an accepted $\RegistryExportSurfaceUp$ $\BHist$ packet. Every machine-target export from $E$ factors through the displayed machine target $M$, audit row $A$, transport row $H$, replay row $C$, provenance package $P$, and local $\NameCert_{\RegistryExportSurfaceUp}$ row $N$, and this factorization cannot bypass the blocking row $B$.
\end{theorem}
\begin{proof}
Read the carrier in \autoref{def:registry-export-surface-carrier}. The machine-facing consumer is not an ambient parameter: it is the row $M$, and the route it may inspect is the row $A$ together with $H,C,P,N$.

The NameCert obligations of \autoref{thm:registry-export-surface-namecert-obligations} place machine-target and audit exposure in one row, and they place transport, replay, provenance, and local naming in the final row. These rows therefore describe the finite route from the machine target back to the named packet.

The blocking row is still part of the same export. By \autoref{thm:registry-export-surface-nonescape}, any public consumer exporting a registry claim must read $B$ as well as $M,A,H,C,P,N$. A purported machine target that reaches the claim without $B$ is outside the accepted carrier rather than a machine-target export from $E$.
\end{proof}

\begin{theorem}[Registry export surface non-smuggling ledger exhaustion]
\label{thm:registry-export-surface-nonsmuggling-ledger-exhaustion}
Let
$$
E=(R,B,S,M,A,U,H,C,P,N)
$$
be an accepted $\RegistryExportSurfaceUp$ $\BHist$ packet. The prose reading, host-semantic reading, model-output reading, and hidden-registry-state reading are refused by the non-smuggling row $U$ together with the displayed $\hsame$ transport $H$, $\Cont$ replay $C$, provenance package $P$, and local $\NameCert_{\RegistryExportSurfaceUp}$ row $N$; no further ledger coordinate is available for importing any of these readings into the registry export.
\end{theorem}
\begin{proof}
The carrier definition names $U$ as the row that records forbidden readings from prose, host semantics, model output, and hidden registry state. It also states that the packet contains no coordinate for a prose-only export route, hidden machine target, or private override of the blocking row.

The NameCert obligations of \autoref{thm:registry-export-surface-namecert-obligations} make this refusal visible at the export boundary. They assign non-smuggling accountability to $U$ and bind it to the transport, replay, provenance, and local naming rows $H,C,P,N$.

It remains to exclude a hidden ledger extension. Apply \autoref{thm:registry-export-surface-nonescape}: every accepted public export must read $U,H,C,P,N$ along with the registration, blocking, status, machine-target, and audit rows. Since the carrier has no additional coordinate, the listed rows exhaust the ledger route and refuse exactly the four forbidden readings named in the theorem.
\end{proof}

\begin{theorem}[Registry export surface TasteGate obligation]
\label{thm:registry-export-surface-tastegate-obligation}
The Lean follow-up for $\RegistryExportSurfaceUp$ must provide $\Nontrivial\ \RegistryExportSurfaceUp$ and $\FieldFaithful\ \RegistryExportSurfaceUp$ in $\mathsf{lean4/BEDC/Derived/RegistryExportSurfaceUp/TasteGate.lean}$, with the nontriviality witness exposing at least one accepted registration-or-blocking packet and the field-faithfulness witness preserving the distinct rows $R,B,S,M,A,U,H,C,P,N$.
\end{theorem}
\begin{proof}
The carrier in \autoref{def:registry-export-surface-carrier} is not a terminal tag: it has separate rows for registration, blocking, status, machine target, audit route, and non-smuggling accountability. A TasteGate witness must therefore exhibit a packet in which these rows are inspectable.

Field faithfulness is exactly the requirement that the classifier not collapse the rows that the NameCert obligations keep apart. In particular, $R$ cannot substitute for $B$, $M$ cannot substitute for $A$, and $U$ cannot be erased after a successful export.

Thus the Lean surface named in the theorem is the appropriate follow-up site for the same finite carrier, with nontriviality and field-faithfulness aligned to the displayed rows.
\end{proof}

\falsifiablePrediction{If the blocking row is absent, no accepted RegistryExportSurfaceUp packet exports a blocked-claim reading.}

\independenceWitness{cannotclaim, largemodeloutputverifier, focused-multi-hist-ledger, groundcompilerrecognizerflow: the cannot-claim packet records refusal rows without machine-target export, the large-model verifier records checked output without registry status coverage, the focused multi-Hist ledger records a finite focus ledger without claim blocking, and the GroundCompiler recognizer flow records machine recognition without the full registration, blocking, audit, and non-smuggling packet. None is bijective to $\RegistryExportSurfaceUp$.}

\begin{theorem}[Registry export surface public bridge route]
\label{thm:registry-export-surface-public-bridge-route}
The public $\RegistryExportSurfaceUp$ route bridges registration, blocking, status, machine-target, audit, and non-smuggling rows to the sibling surfaces \autoref{ch:concrete-instances-axiszeckendorfcannotclaim-namecert}, \autoref{ch:concrete-instances-largemodeloutputverifier-namecert}, \autoref{ch:concrete-instances-focused-multi-hist-ledger-namecert}, \autoref{ch:concrete-instances-groundcompilerrecognition-namecert}, and \autoref{ch:concrete-instances-physical-namecert-audit-namecert}. The bridge-facing route is paper-level: it records the public dependency surface while leaving Lean bridge checking and $\StdBridge$ discharge outside this chapter.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:registry-export-surface-namecert-obligations}. It displays the registration row $R$, blocking row $B$, status row $S$, machine-target row $M$, audit row $A$, non-smuggling row $U$, and structural rows $H,C,P,N$.

Use \autoref{thm:registry-export-surface-nonescape}, \autoref{thm:registry-export-surface-status-exactness}, and \autoref{thm:registry-export-surface-machine-target-audit-route}. These theorems keep export, blocking, status, machine target, and audit reads inside the public carrier instead of prose or hidden registry state.

Finally apply \autoref{thm:registry-export-surface-nonsmuggling-ledger-exhaustion}. The non-smuggling row connects the public route to the cannot-claim, large-model-output-verifier, focused-ledger, GroundCompiler-recognition, and physical NameCert audit sibling surfaces named in the chapter introduction. Since no checked $\StdBridge$ row is displayed, the result is a bridged paper route while the bridge verification axis remains unchanged.
\end{proof}

\begin{theorem}[Registry export surface index coverage]
\label{thm:registryexportsurface-index-coverage}
For every accepted $\RegistryExportSurfaceUp$ packet $E=(R,B,S,M,A,U,H,C,P,N)$, the registry index visible to a downstream consumer is covered exactly by the $\BHist$ registration row $R$, the blocking row $B$, the status row $S$, the machine-target row $M$, the audit route row $A$, the non-smuggling row $U$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\RegistryExportSurfaceUp}$ row $N$. In particular, no BHist registry row may be exported unless the matching blocking, status, machine-target, audit, provenance, and naming rows are present in the same packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:registry-export-surface-carrier}. The visible registry coordinates are $R,B,S,M,A,U,H,C,P,N$, with $R$ naming the BHist registry row and $N$ naming the local NameCert row.

Apply the NameCert obligations of \autoref{thm:registry-export-surface-namecert-obligations}. They place registration and blocking coverage in $R,B$, status exactness in $S$, machine-target and audit exposure in $M,A$, and non-smuggling accountability in $U$.

Finally use \autoref{thm:registry-export-surface-public-bridge-route}. The public route reads those same rows before any downstream export, so the index has no hidden entry outside the displayed $\BHist$, $\Pkg$, and $\NameCert$ surface.
\end{proof}

\begin{theorem}[Registry export surface export determinacy]
\label{thm:registryexportsurface-export-determinacy}
For every accepted $\RegistryExportSurfaceUp$ packet $E=(R,B,S,M,A,U,H,C,P,N)$, the exported registry reading is determined by the displayed status row $S$ together with the blocking row $B$, the machine-target row $M$, the audit route row $A$, the non-smuggling row $U$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\RegistryExportSurfaceUp}$ row $N$. Two consumers that preserve these rows cannot produce two different export outcomes from the same BHist registry row $R$.
\end{theorem}
\begin{proof}
Start with status exactness, \autoref{thm:registry-export-surface-status-exactness}. It keeps exported, blocked, horizon-facing, and formalization-target readings separate while preserving $R,B,M,A,U,H,C,P,N$.

Now read the machine-target audit route of \autoref{thm:registry-export-surface-machine-target-audit-route}. The machine target $M$ receives only the displayed audit row $A$ and cannot bypass the blocking row $B$.

It remains to rule out a second export by hidden state. Apply nonescape, \autoref{thm:registry-export-surface-nonescape}, and the non-smuggling ledger exhaustion theorem. Since prose synthesis, model output, hidden registry state, and detached machine targets are refused by $U,H,C,P,N$, the preserved rows determine the public export outcome.
\end{proof}

\begin{theorem}[Registry export surface consumer provenance]
\label{thm:registryexportsurface-consumer-provenance}
Every downstream consumer of an accepted $\RegistryExportSurfaceUp$ packet must carry the $\Pkg$ provenance row $P$ and the local $\NameCert_{\RegistryExportSurfaceUp}$ row $N$ together with the BHist registry row $R$, blocking row $B$, status row $S$, machine-target row $M$, audit route row $A$, and non-smuggling row $U$. A consumer that drops $P$ or $N$ is no longer a registry-export consumer of the packet.
\end{theorem}
\begin{proof}
Read the structural rows in \autoref{def:registry-export-surface-carrier}. The row $P$ records provenance over $\RegistryExportSurfaceUp$ and its sibling export surfaces, while $N$ records local NameCert naming for the same finite packet.

Apply \autoref{thm:registry-export-surface-namecert-obligations}. Its final obligation row binds transport, replay, provenance, and local naming to $H,C,P,N$, so the consumer cannot detach the registration, blocking, status, machine-target, audit, or non-smuggling rows from their package and certificate.

Finally use the public bridge route. Downstream export passes through the sibling cannot-claim, verifier, focused-ledger, recognition, and audit surfaces only with the same displayed rows. Therefore a consumer without $P$ or $N$ is a hidden or prose-only route, not a $\RegistryExportSurfaceUp$ consumer.
\end{proof}

\begin{theorem}[Registry export surface obligation rows]
\label{thm:registry-export-surface-obligation-rows}
For every accepted $\RegistryExportSurfaceUp$ packet $E=(R,B,S,M,A,U,H,C,P,N)$, the registration row $R$, blocking row $B$, status row $S$, machine-target row $M$, audit row $A$, and non-smuggling row $U$, together with $H,C,P,N$, exhaust the public obligation surface. No registry export obligation remains outside registration, blocking, status exactness, machine-target exposure, audit replay, non-smuggling accountability, componentwise transport, continuation replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:registry-export-surface-carrier}. Its visible coordinates are exactly $R,B,S,M,A,U,H,C,P,N$, and the definition excludes unregistered claims, private blocking overrides, hidden machine targets, and prose-only export routes.

Apply \autoref{thm:registry-export-surface-namecert-obligations}. The obligation list already assigns carrier admission, registration and blocking coverage, status exactness, machine-target and audit exposure, non-smuggling accountability, and transport/replay/provenance/name rows to these coordinates.

It remains to exclude a public obligation outside the list. Use \autoref{thm:registry-export-surface-public-bridge-route}: the public bridge route reads the same registration, blocking, status, machine-target, audit, and non-smuggling rows before connecting to sibling surfaces. Hence every public obligation is already one of the displayed rows.
\end{proof}

\begin{theorem}[Registry export surface ledger nonescape]
\label{thm:registry-export-surface-ledger-nonescape}
No accepted $\RegistryExportSurfaceUp$ export route can bypass the blocking and non-smuggling ledger. Any route that exports a registry claim must read $B$ and $U$ through the same status, machine-target, audit, transport, replay, provenance, and local $\NameCert$ rows that name the carrier.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:registry-export-surface-nonescape}. It states that every public consumer exporting a registry claim reads $R,B,S,M,A,U,H,C,P,N$ and cannot recover the export from prose synthesis, model output, hidden registry state, or a machine target that bypasses $B$.

The status exactness theorem, \autoref{thm:registry-export-surface-status-exactness}, keeps blocked and exported readings separate while preserving $R,B,M,A,U$. Thus the blocking row cannot be erased by relabeling a blocked status as an exported status.

Finally use \autoref{thm:registry-export-surface-nonsmuggling-ledger-exhaustion}. It identifies $U,H,C,P,N$ as the ledger route refusing prose, host-semantic, model-output, and hidden-registry-state readings. A route that bypasses $B$ or $U$ is therefore outside the accepted carrier, not an export route from it.
\end{proof}

\begin{theorem}[Registry export surface machine-readable blocking route]
\label{thm:registry-export-surface-machine-readable-blocking-route}
Every machine-readable $\RegistryExportSurfaceUp$ export factors through blocking, audit, transport, replay, provenance, and local $\NameCert$ rows:
$$
B,A,H,C,P,N .
$$
The machine target $M$ may read a registry claim only along this route, with status supplied by $S$ and non-smuggling accountability supplied by $U$.
\end{theorem}
\begin{proof}
Use \autoref{thm:registry-export-surface-machine-target-audit-route}. It already factors every machine-target export through $M,A,H,C,P,N$ and states that the factorization cannot bypass the blocking row $B$.

Next apply \autoref{thm:registry-export-surface-ledger-nonescape}. The blocking row $B$ and non-smuggling row $U$ remain in the same ledger as the status row $S$, so a machine-readable export cannot read $M$ and $A$ while discarding the rows that decide whether the claim is blocked or forbidden.

The bridge route of \autoref{thm:registry-export-surface-public-bridge-route} supplies the public consumer side. It connects the displayed registry rows to the sibling export surfaces without adding a hidden machine target. Therefore every machine-readable export factors through the listed blocking, audit, transport, replay, provenance, and local naming rows.
\end{proof}

\begin{theorem}[Registry export surface blocking-audit factorization]
\label{thm:registry-export-surface-blocking-audit-factorization}
For every accepted $\RegistryExportSurfaceUp$ packet
$$
E=(R,B,S,M,A,U,H,C,P,N),
$$
each exported machine target factors through the registration row $R$, blocking row $B$, status row $S$, machine-target row $M$, audit row $A$, non-smuggling row $U$, transport row $H$, replay row $C$, provenance row $P$, and local $\NameCert$ row $N$. In particular, the audit route for a machine target is not a standalone $M,A$ projection: it is the displayed blocking-audit route
$$
R,B,S,M,A,U \longmapsto H,C,P,N .
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:registry-export-surface-carrier}. Its visible rows already list registration, blocking, status, machine target, audit, non-smuggling, transport, replay, provenance, and local naming as the complete export surface.

Apply \autoref{thm:registry-export-surface-machine-readable-blocking-route}. It forces every machine-readable export through $B,A,H,C,P,N$, with $M$ supplying the machine target and $S,U$ supplying status and non-smuggling accountability.

Now use \autoref{thm:registry-export-surface-ledger-nonescape}. A route that deletes $B$ or $U$ is not an accepted registry export, while \autoref{thm:registry-export-surface-namecert-obligations} binds $R,S,M,A$ to the same local certificate rows.

Finally replay $H,C,P,N$. Transport, continuation replay, provenance, and local naming keep the displayed blocking-audit route componentwise, so no hidden registry state, prose-only export, or detached machine target remains.
\end{proof}

\begin{theorem}[Registry export surface status machine-target coverage]
\label{thm:registry-export-surface-status-machine-target-coverage}
For every accepted $\RegistryExportSurfaceUp$ packet $E=(R,B,S,M,A,U,H,C,P,N)$, every exported, blocked, horizon-facing, or formalization-target machine read is covered by the same displayed rows $R,B,S,M,A,U,H,C,P,N$. The status row $S$ chooses the public status, the machine-target row $M$ chooses the machine-facing consumer, and the audit row $A$ supplies the route that consumer may inspect; none of these rows can cover a target after removing blocking, non-smuggling, transport, replay, provenance, or local naming.
\end{theorem}
\begin{proof}
Start with status exactness, \autoref{thm:registry-export-surface-status-exactness}. It keeps exported, blocked, horizon-facing, and formalization-target readings distinct while preserving $R,B,M,A,U,H,C,P,N$.

Apply the blocking-audit factorization theorem above. It places the machine-facing read inside the complete route $R,B,S,M,A,U,H,C,P,N$, so status coverage and machine-target coverage are the same displayed packet rather than two independent projections.

Finally use the non-smuggling ledger exhaustion theorem, \autoref{thm:registry-export-surface-nonsmuggling-ledger-exhaustion}. It refuses prose, host-semantic, model-output, and hidden-registry-state readings, so every covered machine target remains local to the accepted carrier and its local $\NameCert$ row.
\end{proof}

\begin{theorem}[Registry export surface machine-audit totality]
\label{thm:registry-export-surface-machine-audit-totality}
For every accepted $\RegistryExportSurfaceUp$ packet
$$
E=(R,B,S,M,A,U,H,C,P,N),
$$
each machine audit export reads the machine-target row $M$, audit row $A$, transport row $H$, continuation row $C$, provenance row $P$, and local name row $N$ together with the blocking row $B$ and non-smuggling row $U$. No machine audit export is available from $M,A,H,C,P,N$ after deleting $B$ or $U$.
\end{theorem}
\begin{proof}
Project the carrier $E$. The machine-readable route of \autoref{thm:registry-export-surface-machine-readable-blocking-route} gives the displayed audit path through $B,A,H,C,P,N$, with $M$ supplying the machine target and $S,U$ supplying status and non-smuggling accountability.

Now apply the ledger nonescape theorem. It keeps $B$ and $U$ attached to any route that exposes a registry claim to a machine reader.

Finally use status exactness. The status row $S$ may classify the claim as exported, blocked, horizon, or formal target, but the machine audit export remains a read of the same displayed rows. Thus $M,A,H,C,P,N$ never form a standalone export after $B$ or $U$ is erased.
\end{proof}

\begin{theorem}[Registry export surface status-blocking exhaustion]
\label{thm:registry-export-surface-status-blocking-exhaustion}
For every accepted $\RegistryExportSurfaceUp$ packet $E=(R,B,S,M,A,U,H,C,P,N)$, the exported, blocked, horizon, and formal-target statuses are exhausted by the status row $S$. Changing among these four public statuses cannot erase the blocking row $B$, and any export route that lacks $B$ is outside the accepted registry surface.
\end{theorem}
\begin{proof}
Read the status row through \autoref{thm:registry-export-surface-status-exactness}. It is the displayed row that separates exported and blocked registry reads while preserving the registration, machine-target, audit, and non-smuggling context.

The machine-readable blocking route extends the same separation to machine consumers. It requires $B$ before $M$ can be read as an export target.

The ledger nonescape theorem excludes the remaining case. A route that keeps a status label but removes the blocking row is not an accepted export route from $E$; it is a hidden-registry-state or prose-only route, both of which the carrier refuses.
\end{proof}

\begin{theorem}[Registry export surface formal-target boundary]
\label{thm:registry-export-surface-formal-target-export-boundary}
For every accepted $\RegistryExportSurfaceUp$ packet $E=(R,B,S,M,A,U,H,C,P,N)$, a formalization-target status in $S$ exports only target metadata together with the audit route through $M,A,H,C,P,N$ and the blocking and non-smuggling rows $B,U$. It does not export a checked theorem, a checked bridge, or any theorem-level evidence not already present as registry metadata.
\end{theorem}
\begin{proof}
Begin with the status row $S$. A formalization-target status is a registry status, so it is read through the same carrier rows as exported, blocked, and horizon statuses.

Apply machine-audit totality. The target metadata can be exposed to a machine reader only with $M,A,H,C,P,N$ and with $B,U$ still attached.

There is no carrier coordinate for a checked theorem or checked bridge in $E$. The public bridge route is explicitly paper-level, and the closurestatus leaves bridge checking outside the chapter. Therefore the formal-target export boundary is metadata plus audit route, not theorem or bridge evidence.
\end{proof}

\begin{theorem}[Registry export surface public discharge]
\label{thm:registry-export-surface-public-discharge}
For every accepted $\RegistryExportSurfaceUp$ packet
$$
E=(R,B,S,M,A,U,H,C,P,N),
$$
the public registry-export route is unique: it is the route through the displayed $\BHist$ packet, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\RegistryExportSurfaceUp}$ row that keeps the status row $S$, machine-target row $M$, blocking row $B$, audit row $A$, and non-smuggling row $U$ together. Any putative public route that deletes one of $S,M,B,A,U$ or bypasses $H,C,P,N$ is outside the accepted carrier rather than a second registry-export discharge.
\end{theorem}
\begin{proof}
Project the accepted carrier. By \autoref{def:registry-export-surface-carrier}, the only public coordinates available to an export are $R,B,S,M,A,U,H,C,P,N$, with $N$ naming the local $\NameCert_{\RegistryExportSurfaceUp}$ row.

Apply status-blocking exhaustion and machine-audit totality. The status row $S$ decides the public registry status, while the machine reader can inspect $M,A,H,C,P,N$ only with the blocking and non-smuggling rows $B,U$ still attached.

Finally use the formal-target boundary together with ledger nonescape. A route that claims public export while dropping $B$, $U$, or the structural rows $H,C,P,N$ is a prose-only or hidden-registry-state route, not an accepted export from $E$. Hence the displayed path through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ is the single public discharge route.
\end{proof}

\closureat{RegistryExportSurfaceUp}{seedStr}
\closureat{RegistryExportSurfaceUp}{obligationStr}
\closureat{RegistryExportSurfaceUp}{scopedStr}
\closureat{RegistryExportSurfaceUp}{publicStr}
\closureat{RegistryExportSurfaceUp}{bridgedStr}
\closureat{RegistryExportSurfaceUp}{matureStr}

\begin{closurestatus}{\RegistryExportSurfaceUp}
  \constructivestory{A $\RegistryExportSurfaceUp$ packet is generated from registration, blocking, status, machine-target, audit, non-smuggling, transport, replay, provenance, and local naming rows.}
  \leanchecked{BEDC.Derived.RegistryExportSurfaceUp.taste\_gate}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature export surface is closed by the carrier rows $R,B,S,M,A,U,H,C,P,N$, the local $\NameCert$ obligations, nonescape, status exactness, machine-target audit route, non-smuggling ledger exhaustion, the TasteGate obligation, and \autoref{thm:registry-export-surface-public-bridge-route}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, bridge checking, hidden registry state, prose-only export, or machine targets that bypass blocking.}
  \upgradepath{Lean closure requires checked registration, blocking, status, machine-target, audit, non-smuggling, transport, replay, provenance, and local naming rows.}
\end{closurestatus}
