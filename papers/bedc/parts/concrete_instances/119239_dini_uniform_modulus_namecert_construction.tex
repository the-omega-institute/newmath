\chapter{A Concrete Naming Certificate for $\DiniUniformModulusUp$}
\label{ch:concrete-instances-dini-uniform-modulus-namecert}
\origin{human}

Following Dini and Bishop--Bridges constructive analysis, the $\DiniUniformModulusUp$ packet records the finite route by which monotone pointwise continuity data on a compact metric source become an explicit uniform-modulus certificate. It sits beside the monotone Dini route of \autoref{ch:concrete-instances-dini-uniform-monotone-namecert}, the uniformly continuous sequence surface of \autoref{ch:concrete-instances-uniformly-continuous-sequence-namecert}, the uniform-modulus packet of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the compact-metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the constructive finite modulus handoff; it does not assert open-cover compactness, countable choice, a host supremum principle, or ambient $\RealUp$ completeness.

\begin{definition}[Dini uniform-modulus carrier]
\label{def:dini-uniform-modulus-carrier}
A $\DiniUniformModulusUp$ carrier is a finite $\BHist$ packet
$$
D=(K,F,S,M,U,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing only the finite metric observations and compact-source identifiers used by the packet.
\item $F$ is the $\ContinuousMapUp$ pointwise-continuity row for the inspected family over the compact source.
\item $S$ is the $\UniformlyContinuousSequenceUp$ sequence row, recording the displayed monotone approximation schedule before any terminal modulus is read.
\item $M$ is the $\DiniUniformMonotoneUp$ monotone-convergence row that binds pointwise convergence to the finite compact route.
\item $U$ is the $\UniformModulusUp$ output row reached only after $K,F,S,M$ have been displayed.
\item $R$ is the $\RealUp$ tolerance and value-readback row used by the monotone comparison; it is terminal and does not export real completeness.
\item $H$ records componentwise $\hsame$ transport for the compact source, continuous-map rows, sequence schedule, monotone route, uniform-modulus output, real readback, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through compact finite inspection, pointwise continuity, monotone schedule, and final uniform-modulus output.
\item $P$ is the $\Pkg$ provenance over $\DiniUniformModulusUp$, $\DiniUniformMonotoneUp$, $\UniformlyContinuousSequenceUp$, $\UniformModulusUp$, $\ContinuousMapUp$, $\CompactMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DiniUniformModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected infinite subcover, countable choice, quotient real equality, a host supremum, full compactness outside the displayed finite source, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dini uniform-modulus NameCert obligations]
\label{thm:dini-uniform-modulus-namecert-obligations}
For every accepted $\DiniUniformModulusUp$ carrier $D=(K,F,S,M,U,R,H,C,P,N)$, the local $\NameCert_{\DiniUniformModulusUp}$ surface consists exactly of carrier admission for the compact metric source, continuous-map pointwise rows, uniformly continuous sequence schedule, monotone Dini route, terminal uniform-modulus output, real tolerance readback, componentwise transport, displayed continuation replay, provenance, and local naming. Every uniform-modulus read must pass through $K,F,S,M,U,R,H,C,P,N$, and no obligation is stored outside those finite rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The analytic rows are precisely the compact source $K$, pointwise continuous-map data $F$, the uniformly continuous sequence schedule $S$, the monotone Dini route $M$, the uniform-modulus output $U$, and the real tolerance readback $R$.

The NameCert obligations follow that order. A tolerance request first enters the compact finite source, then the pointwise continuity row, then the sequence schedule, and only then the monotone Dini row that licenses the uniform-modulus output.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since no excluded object appears in the carrier, the local certificate cannot export a selected infinite subcover, countable choice, quotient real equality, a host supremum, open-cover compactness, or ambient real completeness.
\end{proof}

\begin{theorem}[Dini monotone uniform route]
\label{thm:dini-uniform-modulus-monotone-uniform-route}
Every $\UniformModulusUp$ consumer of an accepted $\DiniUniformModulusUp$ carrier receives exactly the finite route
$$
\CompactMetricUp,\quad \ContinuousMapUp,\quad \UniformlyContinuousSequenceUp,\quad \DiniUniformMonotoneUp,\quad \UniformModulusUp,\quad \RealUp
$$
through the displayed rows $K,F,S,M,U,R,H,C,P,N$. A consumer cannot bypass the monotone schedule, replace it by a host supremum argument, or import open-cover compactness, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the compact source and pointwise-continuity rows. They fix the finite metric observations and the continuous-map requests that the later sequence and modulus rows may read.

Next use the uniformly continuous sequence schedule and the $\DiniUniformMonotoneUp$ row. These rows bind monotone pointwise convergence to the displayed compact route before the terminal $\UniformModulusUp$ row is visible.

Finally inspect the real readback and structural rows. The $\RealUp$ row supplies only the tolerance read needed by the displayed route, while $H,C,P,N$ preserve the same route under transport, replay, provenance, and local naming. The absent host supremum, selected subcover, quotient equality, choice, and ambient-completeness objects therefore cannot be recovered from the accepted packet.
\end{proof}

\falsifiablePrediction{An accepted $\DiniUniformModulusUp$ consumer that reaches a $\UniformModulusUp$ output without displaying the compact source row $K$, continuous-map row $F$, sequence schedule $S$, monotone Dini row $M$, and real tolerance row $R$ refutes the carrier surface.}

\independenceWitness{dini-uniform-monotone, uniformly-continuous-sequence, uniformmodulus, continuousmap, compactmetric, real: no listed sibling supplies the whole ten-row packet $D=(K,F,S,M,U,R,H,C,P,N)$, because each sibling contributes only monotone convergence, sequence scheduling, modulus output, pointwise continuity, compact metric source, or real readback while this chapter binds the route through transport, replay, provenance, and local naming.}

\closureat{DiniUniformModulusUp}{seedStr}

\begin{closurestatus}{\DiniUniformModulusUp}
  \constructivestory{A $\DiniUniformModulusUp$ packet is generated from a compact metric source row, ContinuousMap pointwise rows, a UniformlyContinuousSequence schedule, a DiniUniformMonotone route, a UniformModulus output, Real tolerance readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dini-uniform-modulus-carrier}, \autoref{thm:dini-uniform-modulus-namecert-obligations}, and \autoref{thm:dini-uniform-modulus-monotone-uniform-route} seed the finite Dini route from compact metric source rows, continuous-map pointwise data, uniformly continuous sequence schedules, monotone Dini rows, uniform-modulus output, real tolerance readback, transport, replay, provenance, and naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected infinite subcovers, countable choice, quotient real equality, host supremum arguments, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Formal closure requires Lean coverage for carrier admission, pointwise-continuity exposure, sequence-schedule exactness, monotone Dini handoff, uniform-modulus output, real-readback nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DiniUniformModulusUp.}
\end{closurestatus}
