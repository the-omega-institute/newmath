\chapter{A Concrete Naming Certificate for $\CompactModulusExtractionLatticeUp$}
\label{ch:concrete-instances-compact-modulus-extraction-lattice-namecert}
\origin{human}

Following Heine--Cantor and Bishop's finite-net proof of compact uniform continuity, the $\CompactModulusExtractionLatticeUp$ packet records the lattice-shaped finite ProbeBundle data that turns centerwise pointwise moduli into one tolerance-only uniform modulus. It refines the compact-modulus extraction surface of \autoref{ch:concrete-instances-compact-modulus-extraction-namecert} by making the ProbeBundle fold, rational lower-bound row, and uniform-modulus row visible as separate lattice coordinates. It sits over the compact source rows of \autoref{ch:concrete-instances-compact-namecert}, the compact metric rows of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite lower-bound fold of \autoref{ch:concrete-instances-finite-lower-bound-fold-namecert}, the metric rows of \autoref{ch:concrete-instances-metric-namecert}, and the terminal Real and uniform-modulus surfaces of \autoref{ch:concrete-instances-real-namecert} and \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet is finite NameCert data, not an open-cover theorem, selected subcover, external modulus oracle, quotient function equality, or ambient compactness principle.

\begin{definition}[CompactModulusExtractionLattice carrier]
\label{def:compact-modulus-extraction-lattice-carrier}
A $\CompactModulusExtractionLatticeUp$ carrier is a finite $\BHist$ packet
$$
L=(K,M,A,\mathcal{P},B,Q,U,E,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ or $\CompactMetricUp$ finite-net row naming the inspected compact source, centers, and coverage radius.
\item $M$ is the $\MetricUp$ comparison row used to route a source point through a displayed center.
\item $A$ is the $\ContinuousMapUp$ pointwise-modulus ledger attached to the displayed centers.
\item $\mathcal{P}$ is the finite $\mathsf{ProbeBundle}$ row listing center probes, overlap checks, and admissible tolerance requests.
\item $B$ is the ProbeBundle fold row that gathers the centerwise moduli into one finite lattice of positive rational candidates.
\item $Q$ is the $\FiniteLowerBoundFoldUp$ rational lower-bound row selected from the finite lattice.
\item $U$ is the $\UniformModulusUp$ row produced from $K,M,A,\mathcal{P},B,Q$.
\item $E$ is the $\RealUp$ seal boundary for target-distance and tolerance reads consumed by the uniform-modulus route.
\item $H$ records componentwise $\hsame$ transport for compact, metric, pointwise-modulus, ProbeBundle, fold, rational-bound, uniform-modulus, Real-seal, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact finite-net admission through pointwise moduli, ProbeBundle folding, rational lower-bound extraction, metric comparison, and uniform-modulus handoff.
\item $G$ is the $\Pkg$ provenance over $\CompactModulusExtractionLatticeUp$, $\CompactUp$, $\CompactMetricUp$, $\MetricUp$, $\ContinuousMapUp$, $\FiniteLowerBoundFoldUp$, $\UniformModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactModulusExtractionLatticeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,A,\mathcal{P},B,Q,U,E,H,C,G,N$. It has no coordinate for selected infinite covers, open-cover compactness, host continuity theorems, quotient function equality, arbitrary suprema, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CompactModulusExtractionLattice NameCert obligations]
\label{thm:compact-modulus-extraction-lattice-namecert-obligations}
For every accepted $\CompactModulusExtractionLatticeUp$ carrier $L=(K,M,A,\mathcal{P},B,Q,U,E,H,C,G,N)$, the local $\NameCert_{\CompactModulusExtractionLatticeUp}$ surface consists exactly of compact finite-net admission, metric comparison admission, pointwise-modulus rows, finite ProbeBundle exposure, ProbeBundle fold exactness, rational lower-bound extraction, uniform-modulus handoff, Real-seal boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier. The mathematical rows are the compact finite-net data $K$, metric comparison $M$, centerwise pointwise-modulus ledger $A$, ProbeBundle spine $\mathcal{P}$, fold row $B$, rational lower-bound row $Q$, uniform-modulus output $U$, and Real seal $E$.

The ProbeBundle obligations are finite. Each probe listed by $\mathcal{P}$ is attached to a displayed center and a displayed tolerance request, and the fold row $B$ gathers only those listed candidates.

The rational lower-bound row $Q$ is downstream of the fold. It computes a positive rational bound from the finite lattice before the output row $U$ can be consumed by a uniform-modulus reader.

Finally $H,C,G,N$ transport, replay, source, and name this route. Since no selected infinite cover, open-cover theorem, host continuity proof, quotient function equality, arbitrary supremum, or ambient completeness row is part of the carrier, no local obligation can read one.
\end{proof}

\begin{theorem}[CompactModulusExtractionLattice ProbeBundle fold]
\label{thm:compact-modulus-extraction-lattice-probebundle-fold}
Every accepted $\CompactModulusExtractionLatticeUp$ packet folds pointwise moduli only through the finite route
$$
K,M,A,\mathcal{P} \longrightarrow B \longrightarrow Q .
$$
The row $B$ is a fold over the displayed ProbeBundle lattice, and $Q$ is the resulting positive rational lower-bound row consumed before any $\UniformModulusUp$ handoff is public.
\end{theorem}
\begin{proof}
Start with the compact and metric rows. They supply a finite list of centers and the comparison route by which nearby source points are checked against those centers.

Read the pointwise-modulus row $A$ only at the probes listed by $\mathcal{P}$. The ProbeBundle spine stores the admissible center, overlap, and tolerance requests, so the fold has a finite domain.

The fold row $B$ gathers those displayed candidates into a finite lattice. The lower-bound row $Q$ then reads the lattice output as a positive rational bound rather than as an external infimum or selected subcover.

Thus any downstream uniform-modulus consumer receives the folded lower bound through the displayed ProbeBundle route and not through open-cover compactness, countable choice, quotient function equality, or ambient completeness.
\end{proof}

\begin{theorem}[CompactModulusExtractionLattice uniform-modulus route]
\label{thm:compact-modulus-extraction-lattice-uniform-modulus-route}
Every $\UniformModulusUp$ consumer of an accepted $\CompactModulusExtractionLatticeUp$ packet reaches the output row through
$$
K,M,A,\mathcal{P},B,Q \longrightarrow U \longrightarrow E .
$$
The uniform-modulus route is therefore a finite compact-net, ProbeBundle fold, rational lower-bound, metric-comparison, and Real-seal route; it exports no selected open subcover, host continuity theorem, quotient function equality, arbitrary supremum, or ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with a uniform-modulus consumer. The carrier places $U$ downstream of compact finite-net admission, metric comparison, pointwise-modulus rows, ProbeBundle folding, and rational lower-bound extraction.

Apply the ProbeBundle fold theorem. It forces the lower-bound row $Q$ to be read from the displayed finite lattice $K,M,A,\mathcal{P},B$ before the output row is available.

The Real-seal row $E$ supplies the target-distance and tolerance boundary used by the same route, while $H$ and $C$ preserve the route under componentwise transport and replay. Provenance and local naming record no further analytic coordinate.

Consequently the uniform-modulus output is the displayed finite NameCert route. The excluded open-cover, selected-subcover, host-continuity, quotient-equality, arbitrary-supremum, and ambient-completeness objects are absent from the carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactModulusExtractionLatticeUp$ uniform-modulus read factors through compact finite-net rows, Metric comparison rows, ContinuousMap pointwise-modulus rows, a finite ProbeBundle spine, a ProbeBundle fold, a FiniteLowerBoundFold rational row, UniformModulus output, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}

\independenceWitness{compact-modulus-extraction, compact, compactmetric, continuousmap, finite-lower-bound-fold, metric, uniformmodulus, real: $\CompactModulusExtractionLatticeUp$ is not bijective with any listed sibling because this packet alone exposes the ProbeBundle spine, fold row, rational lower-bound row, and uniform-modulus handoff as one finite lattice certificate.}

\closureat{CompactModulusExtractionLatticeUp}{seedStr}

\begin{closurestatus}{\CompactModulusExtractionLatticeUp}
  \constructivestory{A $\CompactModulusExtractionLatticeUp$ packet is generated from compact finite-net rows, metric comparison rows, ContinuousMap pointwise-modulus rows, a finite ProbeBundle spine, a ProbeBundle fold row, a FiniteLowerBoundFold rational lower-bound row, UniformModulus output, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-modulus-extraction-lattice-carrier}, \autoref{thm:compact-modulus-extraction-lattice-namecert-obligations}, \autoref{thm:compact-modulus-extraction-lattice-probebundle-fold}, and \autoref{thm:compact-modulus-extraction-lattice-uniform-modulus-route} seed the finite ProbeBundle lattice route from compact finite-net and pointwise-modulus data to a tolerance-only uniform-modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, external modulus oracles, host continuity theorems, quotient function equality, arbitrary suprema, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-net admission, metric comparison, pointwise-modulus exposure, ProbeBundle fold exactness, rational lower-bound extraction, uniform-modulus handoff, Real-seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.CompactModulusExtractionLatticeUp.}
\end{closurestatus}
