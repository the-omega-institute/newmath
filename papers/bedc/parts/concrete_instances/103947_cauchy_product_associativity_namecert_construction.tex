\chapter{A Concrete Naming Certificate for $\CauchyProductAssociativityUp$}
\label{ch:concrete-instances-cauchy-product-associativity-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyProductAssociativityUp$ packet records the finite threefold source-window route by which two parenthesized Cauchy-product readings expose the same regular-rational product boundary before any completed-real multiplication law is available. It sits after the product-modulus route of \autoref{ch:concrete-instances-cauchy-product-modulus-namecert}, the bounded-product route of \autoref{ch:concrete-instances-cauchy-product-bounded-namecert}, the source-swap route of \autoref{ch:concrete-instances-cauchy-product-commutativity-namecert}, the regular multiplication route of \autoref{ch:concrete-instances-regular-cauchy-multiplication-namecert}, the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names associativity as displayed BEDC route data over finite windows and dyadic product ledgers; it does not import host multiplication, quotient streams, selected limits, trichotomy, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy product associativity carrier]
\label{def:cauchy-product-associativity-carrier}
A $\CauchyProductAssociativityUp$ carrier is a finite $\BHist$ packet
$$
A=(P_{(xy)z},P_{x(yz)},W_x,W_y,W_z,M_{xy},M_{yz},B_{xy},B_{yz},D_{(xy)z},D_{x(yz)},R_{(xy)z},R_{x(yz)},E_{(xy)z},E_{x(yz)},H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P_{(xy)z}$ and $P_{x(yz)}$ are the two parenthesized $\CauchyProductUp$ source rows.
\item $W_x,W_y,W_z$ are the three $\StreamNameUp$ finite source-window rows shared by both parenthesizations.
\item $M_{xy}$ and $M_{yz}$ are the $\CauchyProductModulusUp$ rows that allocate the two adjacent product budgets before the outer product row is read.
\item $B_{xy}$ and $B_{yz}$ are the $\CauchyProductBoundedUp$ rows that keep the adjacent intermediate product reads inside displayed finite magnitude ledgers.
\item $D_{(xy)z}$ and $D_{x(yz)}$ are the $\DyadicRatCoreUp$ threefold product ledgers for the left- and right-associated product observations.
\item $R_{(xy)z}$ and $R_{x(yz)}$ are the $\RegSeqRatUp$ classifier rows reached from the two threefold dyadic product ledgers.
\item $E_{(xy)z}$ and $E_{x(yz)}$ are the $\RealUp$ seal rows exposed only after the shared windows, adjacent modulus rows, bounded-product rows, dyadic ledgers, and regular-rational handoffs have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two parenthesized product rows, shared windows, modulus rows, bounded rows, dyadic ledgers, regular handoffs, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from every associativity-facing request through the three shared windows, the adjacent product budgets, the bounded-product ledgers, the two dyadic threefold ledgers, and the two regular and Real-facing handoffs.
\item $Q$ is the $\Pkg$ provenance over $\CauchyProductAssociativityUp$, $\CauchyProductUp$, $\CauchyProductModulusUp$, $\CauchyProductBoundedUp$, $\CauchyProductCommutativityUp$, $\RegularCauchyMultiplicationUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyProductAssociativityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. The carrier has no coordinate for host multiplication, quotient stream equality, selected product limits, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Cauchy product associativity NameCert obligations]
\label{thm:cauchy-product-associativity-namecert-obligations}
For every accepted $\CauchyProductAssociativityUp$ carrier
$$
A=(P_{(xy)z},P_{x(yz)},W_x,W_y,W_z,M_{xy},M_{yz},B_{xy},B_{yz},D_{(xy)z},D_{x(yz)},R_{(xy)z},R_{x(yz)},E_{(xy)z},E_{x(yz)},H,C,Q,N),
$$
the local $\NameCert_{\CauchyProductAssociativityUp}$ surface consists exactly of carrier admission for the two parenthesized $\CauchyProductUp$ rows, the three shared $\StreamNameUp$ source windows, adjacent $\CauchyProductModulusUp$ budgets, adjacent $\CauchyProductBoundedUp$ ledgers, two $\DyadicRatCoreUp$ threefold product ledgers, two $\RegSeqRatUp$ classifier rows, two $\RealUp$ seal rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every obligation is read through the sibling routes named in the opening dependency paragraph; no obligation is stored in host multiplication or completed-real algebra.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only source windows are $W_x,W_y,W_z$, and both parenthesized product rows are downstream of those three windows.

Read the adjacent product-budget rows. The modulus rows $M_{xy},M_{yz}$ allocate the finite tolerances, while the bounded-product rows $B_{xy},B_{yz}$ keep the intermediate product observations inside displayed magnitude ledgers before either outer product read is exposed.

Now inspect the two dyadic ledgers. The rows $D_{(xy)z}$ and $D_{x(yz)}$ are the only coordinates where the three source windows become product observations, and the regular rows $R_{(xy)z},R_{x(yz)}$ and seal rows $E_{(xy)z},E_{x(yz)}$ are downstream of them.

Finally $H,C,Q,N$ transport, replay, source, and name the same finite packet. Since no omitted row contains host multiplication, quotient stream equality, selected limits, trichotomy, choice, or ambient completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Cauchy product associativity threefold route]
\label{thm:cauchy-product-associativity-threefold-route}
Every $\RegSeqRatUp$ read exported by an accepted $\CauchyProductAssociativityUp$ carrier factors through the shared finite route
$$
W_x,W_y,W_z \longmapsto M_{xy},M_{yz} \longmapsto B_{xy},B_{yz} \longmapsto D_{(xy)z},D_{x(yz)} \longmapsto R_{(xy)z},R_{x(yz)} .
$$
Thus the two parenthesized product reads consume the same three source windows, adjacent modulus budgets, and bounded-product ledgers before the regular-rational product boundary is public.
\end{theorem}
\begin{proof}
Begin with a regular-rational consumer. By \autoref{def:cauchy-product-associativity-carrier}, the only regular classifier rows are $R_{(xy)z}$ and $R_{x(yz)}$.

Those rows are not source data. Each is downstream of a threefold dyadic product ledger, and both dyadic ledgers are downstream of the same source windows together with the adjacent modulus and bounded-product rows.

The two parenthesized source rows therefore determine different readings of one displayed three-window route. No regular-rational consumer can bypass $W_x,W_y,W_z,M_{xy},M_{yz},B_{xy},B_{yz},D_{(xy)z},D_{x(yz)}$ and still read either classifier row from this packet.
\end{proof}

\begin{theorem}[Cauchy product associativity Real seal boundary]
\label{thm:cauchy-product-associativity-real-seal-boundary}
Every $\RealUp$ seal read of an accepted $\CauchyProductAssociativityUp$ carrier factors through the threefold route of \autoref{thm:cauchy-product-associativity-threefold-route} before either seal row $E_{(xy)z}$ or $E_{x(yz)}$ is exposed. Hence the packet supplies the certificate-level associativity surface needed by later $\RegularCauchyMultiplicationUp$ and $\RealUp$ multiplication consumers, while exporting no host real multiplication law, quotient stream equality, selected product limit, trichotomy, countable choice, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start from a seal-facing read. The carrier exposes exactly the two seal rows $E_{(xy)z}$ and $E_{x(yz)}$, and each appears after the corresponding regular-rational classifier row.

Apply \autoref{thm:cauchy-product-associativity-threefold-route}. Both classifier rows are downstream of the same three source windows, adjacent modulus budgets, bounded-product ledgers, and dyadic threefold product observations.

The structural rows $H,C,Q,N$ preserve and name this displayed route. Since the packet has no coordinate for host multiplication, quotient streams, selected limits, trichotomy, choice, or ambient completeness, no Real-facing read can export any of those objects.
\end{proof}

\falsifiablePrediction{Every $\RegSeqRatUp$ or $\RealUp$ consumer of a $\CauchyProductAssociativityUp$ packet factors through three shared source windows, adjacent CauchyProductModulus budgets, adjacent CauchyProductBounded ledgers, two dyadic threefold product rows, and the two regular-rational handoffs before either parenthesized product seal is consumed.}

\independenceWitness{cauchyproductmodulus, cauchyproductbounded, cauchyproductcommutativity, regularcauchymultiplication, regseqrat, real: $\CauchyProductAssociativityUp$ is not bijective to any listed sibling because it jointly binds three source windows, two adjacent product budgets, two bounded-product ledgers, two parenthesized product observations, two regular handoffs, two Real seals, transport, replay, provenance, and local naming.}

\closureat{CauchyProductAssociativityUp}{seedStr}

\begin{closurestatus}{\CauchyProductAssociativityUp}
  \constructivestory{A $\CauchyProductAssociativityUp$ packet is generated from two parenthesized CauchyProduct rows, three shared StreamName windows, adjacent CauchyProductModulus budgets, adjacent CauchyProductBounded ledgers, two DyadicRatCore threefold product rows, two RegSeqRat classifier rows, two RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-product-associativity-carrier}, \autoref{thm:cauchy-product-associativity-namecert-obligations}, \autoref{thm:cauchy-product-associativity-threefold-route}, and \autoref{thm:cauchy-product-associativity-real-seal-boundary} seed the finite threefold product associativity packet for the regular-Cauchy product route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real multiplication, quotient stream equality, selected product limits, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, adjacent product-budget handoff, bounded-product ledger preservation, threefold dyadic product routing, regular handoff preservation, Real seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.CauchyProductAssociativityUp.}
\end{closurestatus}
