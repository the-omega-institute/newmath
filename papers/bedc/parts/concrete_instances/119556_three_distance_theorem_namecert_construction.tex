\chapter{A Concrete Naming Certificate for $\ThreeDistanceTheoremUp$}
\label{ch:concrete-instances-three-distance-theorem-namecert}
\origin{human}

Following Steinhaus and the classical three-distance theorem, the $\ThreeDistanceTheoremUp$ packet records the finite orbit-window certificate for rotations on the circle. It sits beside the circle-name surface $\SOneUp$, the approximation route of \autoref{ch:concrete-instances-kronecker-approximation-namecert}, the distribution route of \autoref{ch:concrete-instances-weyl-equidistribution-namecert}, the dyadic comparison core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite circle-rotation windows, adjacent gap ledgers, dyadic tolerance rows, StreamName and RegSeqRat readback, Real and $\SOneUp$ sealing, transport, replay, provenance, and local naming.

\begin{definition}[Three-distance theorem carrier]
\label{def:three-distance-theorem-carrier}
A $\ThreeDistanceTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(A,W,O,G,D,S,Q,R,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the rotation-angle source row, carried only through displayed rational and real-name observations.
\item $W$ is the finite orbit-window row for the points $\{0,A,2A,\ldots,nA\}$ read on the circle.
\item $O$ is the cyclic ordering row for the inspected orbit window.
\item $G$ is the adjacent gap ledger obtained from consecutive rows of $O$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger comparing the displayed gaps with the accepted tolerance rows.
\item $S$ is the $\StreamNameUp$ observation schedule used to expose the finite real-name windows.
\item $Q$ is the $\RegSeqRatUp$ readback row for the gap lengths and tolerance budgets.
\item $R$ is the terminal $\RealUp$ seal for the inspected gap lengths.
\item $U$ is the $\SOneUp$ seal stating that the cyclic order is read on the circle surface.
\item $E$ is the local three-distance classifier row: the adjacent gap ledger has at most three displayed gap classes through $D,Q,R,U$.
\item $H$ records componentwise $\hsame$ transport for the angle, window, cyclic order, gap ledger, dyadic tolerance, stream schedule, readback, seals, classifier, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from any theorem-facing query back through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\ThreeDistanceTheoremUp$, $\SOneUp$, $\KroneckerApproximationUp$, $\WeylEquidistributionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ThreeDistanceTheoremUp}$ row.
\end{enumerate}
The carrier contains no measure argument, quotient-circle equality oracle, selected limiting subsequence, ambient real completeness principle, or countable-choice route.
\end{definition}

\begin{theorem}[Three-distance theorem NameCert obligations]
\label{thm:three-distance-theorem-namecert-obligations}
For every accepted $\ThreeDistanceTheoremUp$ carrier
$$
T=(A,W,O,G,D,S,Q,R,U,E,H,C,P,N),
$$
the local $\NameCert_{\ThreeDistanceTheoremUp}$ surface consists exactly of carrier admission for the rotation-angle row, finite orbit-window row, cyclic ordering row, adjacent gap ledger, dyadic tolerance ledger, StreamName schedule, RegSeqRat readback, Real and $\SOneUp$ seals, three-gap classifier, componentwise transport, displayed replay, provenance, and local naming. A consumer may cite the theorem only after the finite window has been inspected and the adjacent gap ledger has been classified into at most three displayed gap classes.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:three-distance-theorem-carrier}. The only circle data available to a theorem-facing consumer are the angle source $A$, the finite orbit window $W$, and the cyclic order $O$.

Inspect the finite window. Consecutive entries of $O$ determine the adjacent gap ledger $G$, and the dyadic row $D$ compares the displayed gap lengths against the finite tolerance rows read through $S,Q,R$.

Classify the ledger. The row $E$ records that the displayed adjacent gaps fall into at most three gap classes after the $\SOneUp$ seal $U$ and the $\RealUp$ seal $R$ are visible.

Finally transport and replay through $H,C,P,N$. These rows preserve and name the inspected finite packet, so the certificate cannot import measure, quotient-circle equality, a selected limit, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Three-distance finite window inspection]
\label{thm:three-distance-finite-window-inspection}
Every accepted $\ThreeDistanceTheoremUp$ read factors through the finite route
$$
A\longmapsto W\longmapsto O\longmapsto G\longmapsto D\longmapsto(S,Q,R,U)\longmapsto E.
$$
The route supplies the three-gap statement by inspection of the displayed orbit window and adjacent gap ledger; it does not use measure, a quotient equality test for circle points, an infinite equidistribution theorem, a selected subsequence, or ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Read the rotation source and finite window first. The cyclic order row $O$ is part of the packet, so the adjacent gap ledger $G$ is generated from displayed neighboring rows rather than from a host quotient of the circle.

Next read the numeric side. The dyadic ledger $D$, stream schedule $S$, regular rational readback $Q$, Real seal $R$, and circle seal $U$ are the only routes by which gap lengths become visible.

The classifier row $E$ consumes exactly those rows and records the at-most-three gap classes. Componentwise transport, replay, provenance, and local naming expose no additional analytic principle, so the theorem remains a finite window certificate.
\end{proof}

\falsifiablePrediction{An accepted $\ThreeDistanceTheoremUp$ read is refuted if the adjacent gap ledger $G$ or dyadic tolerance ledger $D$ is removed while the three-gap classifier row $E$ is still claimed.}

\independenceWitness{sone, kroneckerapproximation, weylequidistribution, dyadicratcore, regseqrat, real: $\ThreeDistanceTheoremUp$ is not bijective to any listed sibling because it jointly carries finite rotation windows, cyclic ordering, adjacent gap classification, dyadic tolerance rows, StreamName scheduling, RegSeqRat readback, Real and circle seals, transport, replay, provenance, and local naming.}

\closureat{ThreeDistanceTheoremUp}{seedStr}

\begin{closurestatus}{\ThreeDistanceTheoremUp}
  \constructivestory{A $\ThreeDistanceTheoremUp$ packet is generated from a rotation-angle row, finite orbit-window row, cyclic ordering row, adjacent gap ledger, dyadic tolerance ledger, StreamName schedule, RegSeqRat readback, Real and circle seals, three-gap classifier, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:three-distance-theorem-carrier}, \autoref{thm:three-distance-theorem-namecert-obligations}, and \autoref{thm:three-distance-finite-window-inspection} seed the finite three-distance route over circle windows, adjacent gap ledgers, dyadic tolerance, regular rational readback, Real and circle seals, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close measure-theoretic equidistribution, quotient-circle equality, selected subsequences, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, orbit-window inspection, cyclic-order exposure, adjacent gap ledger exactness, dyadic tolerance readback, Real and circle sealing, transport, replay, provenance, and local naming for BEDC.Derived.ThreeDistanceTheoremUp.}
\end{closurestatus}
