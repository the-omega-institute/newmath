\chapter{A Concrete Naming Certificate for $\ContractionMappingPicardUp$}
\label{ch:concrete-instances-contraction-mapping-picard-namecert}
\origin{human}

Following Banach and Bishop--Bridges constructive analysis, the $\ContractionMappingPicardUp$ packet records the finite route from displayed contraction data through Picard iteration into completion-facing regular-real seals. It sits over the contraction-map surface of \autoref{ch:concrete-instances-contractionmapping-namecert}, the finite iterate ledger of \autoref{ch:concrete-instances-picard-iteration-namecert}, the complete-metric service row of \autoref{ch:concrete-instances-completemetric-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the route that a constructive fixed-equation consumer may inspect; it does not select a fixed point, quotient Picard streams, use countable choice, assert ambient $\RealUp$ completeness, or supply host equality of maps.

\begin{definition}[ContractionMappingPicard carrier]
\label{def:contraction-mapping-picard-carrier}
A $\ContractionMappingPicardUp$ carrier is a finite $\BHist$ packet
$$
P=(K,I,C_m,R,E,H,Q,S,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the accepted $\ContractionMappingUp$ source row, including the metric carrier, graph-exact self-map, rational bound below one, and one-step contraction surface.
\item $I$ is the $\PicardIterationUp$ finite orbit row generated from the displayed initial point by repeated reads of the same contraction graph.
\item $C_m$ is the $\CompleteMetricUp$ service row, kept separate from the contraction packet and consumed only after the finite Picard ledger is visible.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the Picard residual and tail-modulus route.
\item $E$ is the terminal $\RealUp$ seal row reached only after the regular-rational readback has been displayed.
\item $H$ stores componentwise $\hsame$ transport for the contraction, Picard, complete-metric, regular-rational, real-seal, replay, provenance, and local naming rows.
\item $Q$ stores displayed $\Cont$ replay from contraction admission through Picard iteration, complete-metric service, regular-rational readback, and real-seal exposure.
\item $S$ is the $\Pkg$ provenance over $\ContractionMappingPicardUp$, $\ContractionMappingUp$, $\PicardIterationUp$, $\CompleteMetricUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ContractionMappingPicardUp}$ row.
\end{enumerate}
The carrier has no coordinate for a selected fixed point, quotient equality of Picard streams, countable-choice schedules, host function extensionality, ambient $\RealUp$ completeness, Lean verification, or standard bridge data.
\end{definition}

\begin{theorem}[ContractionMappingPicard NameCert obligations]
\label{thm:contraction-mapping-picard-namecert-obligations}
For an accepted $\ContractionMappingPicardUp$ carrier $P=(K,I,C_m,R,E,H,Q,S,N)$, the local $\NameCert_{\ContractionMappingPicardUp}$ surface consists exactly of contraction-source admission, finite Picard-iteration visibility, complete-metric service separation, $\RegSeqRatUp$ readback visibility, $\RealUp$ seal nonescape, componentwise transport, displayed replay, provenance, and local naming for the carrier of \autoref{def:contraction-mapping-picard-carrier}.
\end{theorem}
\begin{proof}
Project the carrier rows. The contraction source $K$ supplies the graph-exact self-map, rational contraction bound, and one-step Picard comparison through \autoref{ch:concrete-instances-contractionmapping-namecert}.

Read the finite iteration row next. The $\PicardIterationUp$ sibling supplies only displayed finite iterates, residual comparisons, and a regular-rational handoff route; it does not supply an endpoint or a quotient stream.

The complete-metric row $C_m$ remains a separate service coordinate. It may be consumed only after the contraction and Picard rows are visible, and it does not turn the contraction packet itself into an ambient completeness theorem.

Finally inspect $R,E,H,Q,S,N$. These rows read the regular-rational tail, expose the real seal, transport and replay the same route, record provenance, and name the local packet. Since no listed row is a selector, quotient, choice schedule, host equality proof, or ambient real-completeness coordinate, the displayed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[ContractionMappingPicard fixed-equation route]
\label{thm:contraction-mapping-picard-fixed-equation-route}
Every fixed-equation consumer of an accepted $\ContractionMappingPicardUp$ carrier must read the finite route
$$
\ContractionMappingUp \longmapsto \PicardIterationUp \longmapsto \CompleteMetricUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
through the displayed rows $K,I,C_m,R,E,H,Q,S,N$ before it may cite endpoint-facing data. The route exposes contraction data, Picard iteration, complete-metric service, regular-rational readback, and a terminal real seal boundary; it does not assert that a selected fixed point exists, that two Picard streams are quotient-equal, that countable choice is available, or that $\RealUp$ is ambiently complete.
\end{theorem}
\begin{proof}
Start from the contraction row $K$. The consumer first reads the accepted $\ContractionMappingUp$ surface: metric carrier, graph-exact self-map, rational contraction bound, and adjacent Picard comparison data.

Next apply the Picard iteration row $I$. The iteration sibling turns the contraction data into a finite orbit ledger and residual-distance route, so the consumer receives only displayed finite approximants and their regular-tail budget.

Now read $C_m$ and $R$. The complete-metric service is separate from the contraction source, and the regular-rational handoff is downstream of the Picard residual route. Thus completion-facing information enters only through the named service and readback rows.

Finally expose the terminal seal and structural rows $E,H,Q,S,N$. They preserve the same route and mark where real-facing consumers may read a seal after the regular handoff. Because no row selects an endpoint, quotients streams, invokes countable choice, imports host equality, or asserts ambient $\RealUp$ completeness, the fixed-equation consumer receives exactly the displayed route and no stronger theorem.
\end{proof}

\closureat{ContractionMappingPicardUp}{seedStr}

\begin{closurestatus}{\ContractionMappingPicardUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:contraction-mapping-picard-carrier}, \autoref{thm:contraction-mapping-picard-namecert-obligations}, and \autoref{thm:contraction-mapping-picard-fixed-equation-route} seed the finite route from contraction data through Picard iteration, complete-metric service, regular-rational readback, and real-seal exposure.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a selected fixed point, quotient equality of Picard streams, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure asks for checked carrier admission, contraction-source admission, Picard-iteration visibility, complete-metric service separation, RegSeqRat readback visibility, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ContractionMappingPicardUp.}
  \constructivestory{A $\ContractionMappingPicardUp$ packet is generated from a ContractionMapping source row, PicardIteration finite orbit row, CompleteMetric service row, RegSeqRat readback, RealUp seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
