The completion-theorem forward route makes the horizontal L10 edge explicit: every $\BishopCompletionIdempotenceWitnessUp$ read that serves a completion-facing consumer first passes through the theorem-level completion surface of \autoref{ch:concrete-instances-bishop-completion-theorem-namecert} and the universal regular-Cauchy route of \autoref{ch:concrete-instances-regular-cauchy-completion-universal-namecert} before the terminal $\RealUp$ seal is consumed. Thus idempotence is read as a displayed route through the Bishop theorem packet and the regular-Cauchy universal packet, not as a second completed object, a quotient representative, selected limit data, countable choice, host equality of completions, or ambient real completeness.
