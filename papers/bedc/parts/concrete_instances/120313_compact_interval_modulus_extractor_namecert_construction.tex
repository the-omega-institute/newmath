\chapter{A Concrete Naming Certificate for $\CompactIntervalModulusExtractorUp$}
\label{ch:concrete-instances-compact-interval-modulus-extractor-namecert}
\origin{human}

Following Bishop and Bridges on compact intervals and uniform continuity, the $\CompactIntervalModulusExtractorUp$ packet records the finite extraction surface promised by \autoref{ch:visions-compact-uniform-continuity}: compact interval probes, rational lower-bound folds, and a uniform-modulus handoff are carried as displayed NameCert rows. It sits beside the compact interval modulus route of \autoref{ch:concrete-instances-compact-interval-modulus-namecert}, the bridge packet of \autoref{ch:concrete-instances-compact-interval-modulus-bridge-namecert}, the finite interval net of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert}, the compact source rows of \autoref{ch:concrete-instances-compactmetric-namecert}, and the output surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names only finite extraction data; it does not assert open-cover compactness, a selected subcover functional, host semantic continuity, quotient real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Compact interval modulus extractor carrier]
\label{def:compact-interval-modulus-extractor-carrier}
A $\CompactIntervalModulusExtractorUp$ carrier is a finite $\BHist$ packet
$$
X=(I,K,Q,F,R,U,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the compact interval source row, read through closed bounded interval endpoint and finite-net data.
\item $K$ is the $\CompactMetricUp$ or interval-compactness row that supplies the finite $\mathsf{ProbeBundle}$ cover.
\item $Q$ is the tolerance-query row whose requested precision is displayed before any radius fold is read.
\item $F$ is the finite lower-bound fold row, recording rational radius comparisons over the displayed probe bundle.
\item $R$ is the regular rational readback row attached to the selected radius and interval-window data.
\item $U$ is the $\UniformModulusUp$ handoff row exported after $I,K,Q,F,R$ have been displayed.
\item $S$ records the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ seal rows used by interval endpoints, probe centers, and modulus readback.
\item $H$ records componentwise $\hsame$ transport for $I,K,Q,F,R,U,S,C,P,N$.
\item $C$ records displayed $\Cont$ replay from compact interval probes through the rational lower-bound fold to the uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalModulusExtractorUp$, $\CompactIntervalModulusUp$, $\CompactIntervalModulusBridgeUp$, $\CompactMetricUp$, $\ClosedBoundedIntervalNetUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactIntervalModulusExtractorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these finite rows. It has no coordinate for arbitrary open covers, selected subcovers, host continuity theorems, quotient real equality, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Compact interval modulus extractor NameCert obligations]
\label{thm:compact-interval-modulus-extractor-namecert-obligations}
For every accepted $\CompactIntervalModulusExtractorUp$ carrier
$$
X=(I,K,Q,F,R,U,S,H,C,P,N),
$$
the local $\NameCert_{\CompactIntervalModulusExtractorUp}$ surface consists exactly of carrier admission, compact interval source exposure through $I$, finite probe-bundle exposure through $K$, tolerance-query exposure through $Q$, rational lower-bound fold exposure through $F$, regular readback through $R$, $\UniformModulusUp$ handoff through $U$, stream, regular, and Real seal exposure through $S$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The source side is $I,K,Q$: the interval, compact finite probe bundle, and requested precision must all be visible before any extracted radius is read.

The finite lower-bound fold is $F$. It ranges only over the displayed probe rows and rational comparisons, so it is not an open-cover compactness theorem or a selected subcover functional.

The output side is $R,U,S$. The regular readback records the extracted rational data, the $\UniformModulusUp$ row is downstream of that readback, and $S$ carries the stream, regular, and Real seal rows used by the handoff.

Finally $H,C,P,N$ preserve, replay, source, and name the same finite route. Since the carrier has no host continuity, quotient, choice, or ambient-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact interval modulus extractor vision handoff]
\label{thm:compact-interval-modulus-extractor-vision-handoff}
Every compact-uniform-continuity read of an accepted $\CompactIntervalModulusExtractorUp$ carrier factors through the finite route
$$
\mathsf{ProbeBundle}
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp
\longmapsto
\UniformModulusUp .
$$
The route realizes the compact interval branch of \autoref{ch:visions-compact-uniform-continuity} as finite ProbeBundle cover rows, rational lower-bound fold data, regular readback, Real sealing, and UniformModulus handoff rows.
\end{theorem}
\begin{proof}
Begin with the compact interval source and finite probe bundle rows $I,K$. These rows supply the only cover data visible to the extractor.

Read the tolerance query $Q$ and the lower-bound fold $F$. The fold is finite and rational, so every selected radius remains attached to a displayed probe and comparison row.

The regular readback and seal rows $R,S$ then prepare the uniform-modulus handoff $U$. This is the precise finite route named by the compact-uniform-continuity vision: probe coverage, lower-bound fold, readback, Real seal, and uniform modulus.

Transport, replay, provenance, and local naming through $H,C,P,N$ keep the route inside the carrier. No open-cover compactness, selected subcover, semantic continuity theorem, quotient real equality, countable choice, or ambient completeness is exported.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactIntervalModulusExtractorUp$ uniform-modulus read factors through compact interval source rows, a finite ProbeBundle cover, a tolerance query, rational lower-bound fold data, RegSeqRat readback, RealUp sealing, UniformModulus handoff, hsame transport, Cont replay, Pkg provenance, and local NameCert naming.}

\independenceWitness{compact-interval-modulus, compact-interval-modulus-bridge, closedboundedintervalnet, compactmetric, uniformmodulus: $\CompactIntervalModulusExtractorUp$ is not bijective to any listed sibling because it jointly binds the compact interval source, probe-bundle cover, tolerance query, rational lower-bound fold, regular readback, Real seal, UniformModulus handoff, transport, replay, provenance, and local naming.}

\closureat{CompactIntervalModulusExtractorUp}{seedStr}

\begin{closurestatus}{\CompactIntervalModulusExtractorUp}
  \constructivestory{A $\CompactIntervalModulusExtractorUp$ packet is generated from compact interval source rows, CompactMetric finite ProbeBundle cover data, tolerance queries, rational lower-bound folds, RegSeqRat readback, UniformModulus handoff, StreamName and RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-modulus-extractor-carrier}, \autoref{thm:compact-interval-modulus-extractor-namecert-obligations}, and \autoref{thm:compact-interval-modulus-extractor-vision-handoff} seed the compact interval modulus extraction route from finite ProbeBundle covers to UniformModulus handoff data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, host semantic continuity, quotient real equality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact interval source exposure, finite ProbeBundle coverage, rational lower-bound fold exactness, RegSeqRat readback, RealUp sealing, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactIntervalModulusExtractorUp.}
\end{closurestatus}
