\chapter{A Concrete Naming Certificate for $\HilbertInequalityUp$}
\label{ch:concrete-instances-hilbert-inequality-namecert}
\origin{human}

Following Hardy--Littlewood--Polya style Hilbert inequality, the $\HilbertInequalityUp$ packet records a finite real-series inequality route rather than an ambient double-series theorem. It sits next to the absolute-domination surface of \autoref{ch:concrete-instances-absolute-convergence-namecert}, the finite square-bound route of \autoref{ch:concrete-instances-cauchy-schwarz-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet exposes the Hilbert kernel bound only as displayed BEDC rows over finitely inspected series windows, not as quotient stream equality, selected sums, countable choice, host real order, or ambient $\RealUp$ completeness.

\begin{definition}[Hilbert inequality carrier]
\label{def:hilbert-inequality-carrier}
A $\HilbertInequalityUp$ carrier is a finite $\BHist$ packet
$$
H=(A,B,K,S,Q,E,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two real-series source rows whose inspected terms are read through finite $\RealUp$ seals.
\item $K$ is the Hilbert-kernel ledger. Each row records a displayed denominator window, the dyadic comparison budget for the kernel coefficient, and the finite multiplication read attached to the selected term pair.
\item $S$ is the finite summation-window row. It records the rectangular or triangular window actually inspected by the packet and no infinite double-sum selector.
\item $Q$ is the $\RegSeqRatUp$ readback row for the term observations, kernel coefficients, partial rectangular sums, and comparison budgets opened by $A,B,K,S$.
\item $E$ is the terminal $\RealUp$ seal for the exported inequality boundary, reached only after $A,B,K,S,Q$ have been displayed.
\item $T$ stores componentwise $\hsame$ transport for the two source series, Hilbert-kernel ledger, finite windows, regular readback, Real seal, continuation, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a finite window request through the kernel ledger, regular readback, and Real seal.
\item $P$ is the $\Pkg$ provenance over $\HilbertInequalityUp$, $\AbsoluteConvergenceUp$, $\CauchySchwarzRealUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HilbertInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,K,S,Q,E,T,C,P,N$. It has no coordinate for an ambient Hilbert-space theorem, selected infinite sums, quotient stream equality, countable choice, host real order, or ambient completeness.
\end{definition}

\begin{theorem}[Hilbert inequality NameCert obligations]
\label{thm:hilbert-inequality-namecert-obligations}
For every accepted $\HilbertInequalityUp$ carrier $H=(A,B,K,S,Q,E,T,C,P,N)$, the local $\NameCert_{\HilbertInequalityUp}$ surface consists exactly of carrier admission for the two source series, the Hilbert-kernel ledger, the finite summation windows, the $\RegSeqRatUp$ readback, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The same surface records kernel-window exactness, rectangular finite-sum exposure, absolute-domination compatibility with \autoref{ch:concrete-instances-absolute-convergence-namecert}, square-bound compatibility with \autoref{ch:concrete-instances-cauchy-schwarz-real-namecert}, and nonescape from selected sums, quotient stream equality, countable choice, host real order, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hilbert-inequality-carrier}. The available rows are exactly the two source series $A,B$, the kernel ledger $K$, the finite summation window $S$, the regular readback $Q$, the Real seal $E$, and the structural rows $T,C,P,N$.

The kernel and window obligations are the same finite route. The packet must display the denominator window and dyadic comparison budget in $K$ for each inspected pair, and $S$ names the finite rectangular or triangular window in which those pair reads occur.

The compatibility obligations use the sibling surfaces only as finite routes. The absolute-convergence chapter supplies the domination shape for series tails, while the Cauchy--Schwarz real chapter supplies the square-bound route for real-valued finite rows. Both are consumed through the same regular readback and Real seal rows $Q,E$.

Finally, the structural rows transport, replay, source, and name that displayed route. Since none of the excluded analytic objects is a carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hilbert inequality Real-series handoff]
\label{thm:hilbert-inequality-real-series-handoff}
Every real-series consumer of an accepted $\HilbertInequalityUp$ packet receives the Hilbert inequality boundary only through
$$
A,B \longmapsto K,S \longmapsto Q \longmapsto E \longmapsto T,C,P,N .
$$
The handoff exposes the two source series, finite kernel-denominator comparisons, inspected summation windows, regular rational readback, and the terminal Real seal. It does not expose a host double-series theorem, quotient stream equality, selected sums, countable choice, host real order, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a real-series consumer request. The carrier first exposes the source rows $A$ and $B$, so every term read is tied to a displayed finite series observation.

The Hilbert kernel is then read through $K$ inside the finite window $S$. This is the only place where denominator comparisons and pairwise multiplication budgets appear, so the consumer cannot bypass the packet to obtain an independent double series.

The readback and seal are forced by $Q$ and $E$. The $\RegSeqRatUp$ row records the rational observations for the selected terms, kernel coefficients, and partial finite sums before the $\RealUp$ seal is available.

The rows $T,C,P,N$ preserve and replay the same route. Because the carrier has no coordinate for the excluded host or quotient data, every exported Hilbert inequality read is the displayed finite real-series handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\HilbertInequalityUp$ read exposes two real-series source rows, a Hilbert-kernel denominator ledger, finite summation windows, RegSeqRat readback, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert before a real-series consumer receives the inequality boundary.}

\independenceWitness{absolute-convergence, cauchy-schwarz-real, regseqrat, real: $\HilbertInequalityUp$ is not bijective to any listed sibling because it jointly carries two source series, a Hilbert-kernel ledger, finite summation windows, regular rational readback, Real sealing, transport, replay, provenance, and local naming for the same inequality route.}

\closureat{HilbertInequalityUp}{seedStr}

\begin{closurestatus}{\HilbertInequalityUp}
  \constructivestory{A $\HilbertInequalityUp$ packet is generated from two real-series source rows, a Hilbert-kernel denominator ledger, finite summation windows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hilbert-inequality-carrier}, \autoref{thm:hilbert-inequality-namecert-obligations}, and \autoref{thm:hilbert-inequality-real-series-handoff} seed the finite Hilbert inequality boundary for displayed real-series rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host double-series convergence, quotient stream equality, selected sums, countable choice, host real order, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, kernel-window exactness, finite summation exposure, absolute-domination compatibility, square-bound compatibility, regular readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.HilbertInequalityUp.}
\end{closurestatus}
