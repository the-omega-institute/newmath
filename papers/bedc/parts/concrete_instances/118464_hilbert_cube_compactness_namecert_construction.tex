\chapter{A Concrete Naming Certificate for $\HilbertCubeCompactnessUp$}
\label{ch:concrete-instances-hilbert-cube-compactness-namecert}
\origin{human}

Following Kelley and Engelking on the Hilbert cube and compact metric products, the $\HilbertCubeCompactnessUp$ packet records compactness through finite coordinate-window nets over $\HilbertCubeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, and $\CompactMetricUp$. It consumes the finite Hilbert-cube carrier of \autoref{ch:concrete-instances-hilbert-cube-namecert}, the stream and regular-rational readback routes of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the compact-metric row of \autoref{ch:concrete-instances-compactmetric-namecert}. The packet names finite coordinate-window net data; it does not claim open-cover compactness, a choice-selected subnet, host product completeness, quotient stream equality, or ambient $\RealUp$ completeness.

\begin{definition}[Hilbert cube compactness carrier]
\label{def:hilbert-cube-compactness-carrier}
A $\HilbertCubeCompactnessUp$ carrier is a finite $\BHist$ packet
$$
C=(H,W,R,D,E,K,B,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $H$ is the accepted $\HilbertCubeUp$ source row, read through the finite coordinate-window carrier of \autoref{ch:concrete-instances-hilbert-cube-namecert}.
\item $W$ is the finite $\StreamNameUp$ coordinate-window schedule naming exactly the coordinates inspected by the current net request.
\item $R$ is the $\RegSeqRatUp$ readback row for those inspected coordinate windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic mesh radii and comparison cells used by the finite net.
\item $E$ is the $\RealUp$ seal row, attaching each displayed coordinate readback to a real interval boundary.
\item $K$ is the $\CompactMetricUp$ finite-metric support row for the inspected product window.
\item $B$ is the finite coordinate-window net ledger, listing the displayed centers, mesh radii, and containment rows for the inspected window.
\item $T$ records componentwise $\hsame$ transport and displayed $\Cont$ replay for $H,W,R,D,E,K,B$.
\item $P$ is the $\Pkg$ provenance over $\HilbertCubeCompactnessUp$, $\HilbertCubeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CompactMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HilbertCubeCompactnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $H,W,R,D,E,K,B,T,P,N$. It has no coordinate for open-cover compactness, host product completeness, a selected subnet, quotient stream equality, countable choice, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Hilbert cube compactness NameCert obligations]
\label{thm:hilbert-cube-compactness-namecert-obligations}
For every accepted $\HilbertCubeCompactnessUp$ carrier
$$
C=(H,W,R,D,E,K,B,T,P,N),
$$
the local $\NameCert_{\HilbertCubeCompactnessUp}$ surface consists exactly of carrier admission, Hilbert-cube source exposure, finite coordinate-window scheduling, regular-rational readback, dyadic mesh tolerance exposure, real interval sealing, compact-metric finite-window support, finite net containment, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:hilbert-cube-compactness-carrier}. The accepted tuple exposes the Hilbert-cube source row, stream schedule, regular-rational readback, dyadic tolerance ledger, real seal row, compact-metric support row, finite net ledger, transport and replay row, provenance row, and local name.

The finite coordinate-window obligations are read from $H,W,R,D,E$. The source row $H$ supplies only the displayed Hilbert-cube carrier, $W$ names the inspected coordinates, $R$ reads their rational data, $D$ fixes the dyadic mesh, and $E$ seals the inspected coordinates as real interval data.

The compact-metric support and net obligations are read from $K$ and $B$. The row $K$ records the finite metric support for the inspected product window, while $B$ lists the centers, radii, and containment checks used by the net consumer.

Finally, $T,P,N$ exhaust transport, replay, provenance, and local naming. Since the carrier has no additional coordinate, the certificate cannot store open-cover compactness, host product completeness, a selected subnet, quotient stream equality, countable choice, Lean verification, or bridge checking.
\end{proof}

\begin{theorem}[Hilbert cube compactness finite coordinate net]
\label{thm:hilbert-cube-compactness-finite-coordinate-net}
Every accepted $\HilbertCubeCompactnessUp$ consumer read factors through the finite coordinate-window net route
$$
\HilbertCubeUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp \longmapsto \CompactMetricUp .
$$
The exported net is therefore a finite ledger over inspected coordinates, dyadic mesh radii, sealed real intervals, compact-metric support, and displayed containment rows, not an open-cover compactness proof or a choice-selected countable product argument.
\end{theorem}
\begin{proof}
Start with an accepted carrier $C=(H,W,R,D,E,K,B,T,P,N)$. The Hilbert-cube source row $H$ can be read only through the finite coordinate-window surface already displayed by \autoref{ch:concrete-instances-hilbert-cube-namecert}.

The consumer then reads $W,R,D,E$ in order. These rows name the inspected coordinates, recover regular rational data, choose the dyadic mesh budget, and seal the inspected coordinates into real interval rows.

The net itself is exported only after $K$ and $B$ are visible. The compact-metric support row fixes the finite metric window, and the net ledger lists the displayed centers, radii, and containment checks for that same window.

Transport, replay, provenance, and local naming preserve this route componentwise through $T,P,N$. No row remains from which the consumer could obtain an arbitrary open-cover argument, a selected subnet, host product completeness, quotient stream equality, or countable choice.
\end{proof}

\begin{theorem}[Hilbert cube compactness real-seal boundary]
\label{thm:hilbert-cube-compactness-real-seal-boundary}
Every $\RealUp$-facing read of an accepted $\HilbertCubeCompactnessUp$ packet is bounded by the displayed finite route through $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ mesh tolerances, $\RealUp$ interval seals, $\CompactMetricUp$ finite support, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HilbertCubeCompactnessUp}$ row. The packet exports no ambient real completeness principle and no equality of host streams.
\end{theorem}
\begin{proof}
Project the carrier. A real-facing consumer reaches the terminal real rows only after the finite stream schedule, regular rational readback, and dyadic tolerance ledger have all been displayed.

The compactness read does not alter that boundary. The compact-metric support row and finite net ledger consume the sealed interval data already present in $E$ and do not add a separate real-completion or host-product coordinate.

The structural rows $T,P,N$ preserve the same finite route. Therefore every real-facing read remains inside the displayed coordinate-window seal and cannot be reread as ambient $\RealUp$ completeness, quotient stream equality, host product completeness, open-cover compactness, or a selected subnet.
\end{proof}

\closureat{HilbertCubeCompactnessUp}{seedStr}

\begin{closurestatus}{\HilbertCubeCompactnessUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hilbert-cube-compactness-carrier}, \autoref{thm:hilbert-cube-compactness-namecert-obligations}, \autoref{thm:hilbert-cube-compactness-finite-coordinate-net}, and \autoref{thm:hilbert-cube-compactness-real-seal-boundary} seed the finite coordinate-window net route from $\HilbertCubeUp$ through stream windows, regular-rational readback, dyadic mesh tolerances, real interval seals, compact-metric support, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, host product completeness, quotient stream equality, selected subnets, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite coordinate-window scheduling, dyadic mesh exposure, real interval sealing, compact-metric support, finite net containment, transport, replay, provenance, and local naming for BEDC.Derived.HilbertCubeCompactnessUp.}
  \constructivestory{A $\HilbertCubeCompactnessUp$ packet is generated from a $\HilbertCubeUp$ source row, finite $\StreamNameUp$ coordinate windows, $\RegSeqRatUp$ readback rows, $\DyadicRatCoreUp$ mesh ledgers, $\RealUp$ interval seals, $\CompactMetricUp$ finite support rows, a finite net containment ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
