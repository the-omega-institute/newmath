The $\ContinuousMapUp$ entry point reads its metric distance witness through
\autoref{ch:concrete-instances-metric-namecert}, its pointwise-modulus rows
through \autoref{ch:concrete-instances-continuous-namecert}, and its
categorical graph and composition rows through
\autoref{ch:concrete-instances-category-namecert}; compact-uniform consumers
read the resulting horizontal lattice through the child files below.

\input{parts/concrete_instances/continuousmap/carrier_definition.tex}
\input{parts/concrete_instances/continuousmap/top_level_lattice_pointer}
\input{parts/concrete_instances/continuousmap/certificate.tex}
\input{parts/concrete_instances/continuousmap/sibling_lattice_surface}
