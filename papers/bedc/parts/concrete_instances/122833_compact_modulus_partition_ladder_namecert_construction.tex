\chapter{A Concrete Naming Certificate for $\CompactModulusPartitionLadderUp$}
\label{ch:concrete-instances-compact-modulus-partition-ladder-namecert}
\origin{human}

Following Bishop and Bridges on compact metric uniform continuity, the $\CompactModulusPartitionLadderUp$ packet realizes the finite partition-refinement route described in \autoref{ch:visions-compact-uniform-continuity}. It sits over the compact source of \autoref{ch:concrete-instances-compact-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map pointwise rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational tolerance rows of \autoref{ch:concrete-instances-rat-namecert}, the $\ProbeBundleUp$ finite partition spine, and the uniform-modulus handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names a finite ladder from compact nets and pointwise moduli to uniform-modulus selection; it does not import open-cover compactness, a selected Lebesgue number, countable choice, quotient point equality, or an external modulus oracle.

\begin{definition}[Compact modulus partition ladder carrier]
\label{def:compact-modulus-partition-ladder-carrier}
A $\CompactModulusPartitionLadderUp$ carrier is a finite $\BHist$ packet
$$
L=(K,M,F,R,B,A,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row exposing the compact finite-net or finite-cover data.
\item $M$ is the $\MetricUp$ distance row used by the partition comparisons.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row attached to the compact source.
\item $R$ is the $\RatUp$ tolerance ledger for positive local radii, lower-bound comparisons, and refinement thresholds.
\item $B$ is the $\ProbeBundleUp$ partition spine listing the finite probes and membership rows.
\item $A$ is the ladder row, recording refinement steps from coarse probes to the subpartition carrying the local modulus selected at each probe.
\item $U$ is the $\UniformModulusUp$ handoff row produced after the finite ladder has been folded.
\item $H$ records componentwise $\hsame$ transport for compact, metric, continuous-map, rational, probe-bundle, ladder, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through
$$
K,M,F,R,B,A,U,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactModulusPartitionLadderUp$, $\CompactUp$, $\MetricUp$, $\ContinuousMapUp$, $\RatUp$, $\ProbeBundleUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactModulusPartitionLadderUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,F,R,B,A,U,H,C,P,N$. It has no coordinate for open-cover compactness, a selected Lebesgue number, an infinite refinement tree, countable choice, quotient point equality, or an external uniform-modulus selector.
\end{definition}

\begin{theorem}[Compact modulus partition ladder NameCert obligations]
\label{thm:compact-modulus-partition-ladder-namecert-obligations}
For every accepted $\CompactModulusPartitionLadderUp$ carrier
$$
L=(K,M,F,R,B,A,U,H,C,P,N),
$$
the local $\NameCert_{\CompactModulusPartitionLadderUp}$ surface consists exactly of compact source exposure through $K$, metric distance exposure through $M$, continuous-map pointwise-modulus exposure through $F$, rational tolerance exposure through $R$, finite $\ProbeBundleUp$ partition exposure through $B$, partition-refinement ladder exposure through $A$, uniform-modulus handoff through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-modulus-partition-ladder-carrier}. The compact, metric, continuous-map, rational, probe-bundle, ladder, uniform-modulus, transport, replay, provenance, and naming rows are exactly the displayed coordinates.

The compact and metric rows provide the finite partition context. The continuous-map and rational rows attach local moduli and positive tolerance comparisons to each finite probe.

The $\ProbeBundleUp$ row and ladder row record the finite refinement route before the uniform-modulus handoff is available. Transport, replay, provenance, and local naming preserve that same finite route.

No carrier coordinate names open-cover compactness, a selected Lebesgue number, an infinite refinement tree, countable choice, quotient point equality, or an external selector, so none of those objects is part of the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Compact modulus partition ladder handoff]
\label{thm:compact-modulus-partition-ladder-handoff}
Every compact-uniform-continuity consumer that reads a $\CompactModulusPartitionLadderUp$ packet must factor its uniform-modulus request through the finite route
$$
\CompactUp,\ \MetricUp,\ \ContinuousMapUp,\ \RatUp,\ \ProbeBundleUp
\longmapsto
A
\longmapsto
\UniformModulusUp
\longmapsto
H,C,P,N .
$$
The ladder supplies finite partition-refinement data from compact nets and pointwise moduli to a uniform-modulus handoff. It supplies no open-cover compactness theorem, selected Lebesgue number, infinite refinement tree, countable choice, quotient point equality, or external modulus selector.
\end{theorem}
\begin{proof}
Apply \autoref{thm:compact-modulus-partition-ladder-namecert-obligations}. It places compact, metric, continuous-map, rational, and finite ProbeBundle rows before the ladder row $A$.

The ladder row records the finite refinement steps that feed the uniform-modulus handoff $U$. Because $U$ is downstream of $A$, the consumer receives a tolerance-only result only after the finite partition route has been displayed.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the excluded host objects are absent from the carrier, they cannot be exported by the handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactModulusPartitionLadderUp$ read displays Compact, Metric, ContinuousMap, Rat, ProbeBundle, partition ladder, UniformModulus, hsame, Cont, Pkg, and NameCert rows before any uniform-modulus handoff is accepted.}

\independenceWitness{compact, metric, continuousmap, rat, probebundle, uniformmodulus: $\CompactModulusPartitionLadderUp$ is not bijective to any listed sibling because it binds finite compact partitions, metric distance comparisons, pointwise continuous-map moduli, rational tolerance ledgers, ProbeBundle membership, partition refinement, uniform-modulus handoff, transport, replay, provenance, and local naming as one ladder packet.}

\closureat{CompactModulusPartitionLadderUp}{seedStr}

\begin{closurestatus}{\CompactModulusPartitionLadderUp}
  \constructivestory{A $\CompactModulusPartitionLadderUp$ packet is generated from Compact, Metric, ContinuousMap, Rat, ProbeBundle, partition-refinement ladder, UniformModulus, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-modulus-partition-ladder-carrier}, \autoref{thm:compact-modulus-partition-ladder-namecert-obligations}, and \autoref{thm:compact-modulus-partition-ladder-handoff} seed the finite partition ladder from compact nets and pointwise moduli to uniform-modulus handoff rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected Lebesgue numbers, infinite refinement trees, countable choice, quotient point equality, external modulus selectors, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, metric-distance exposure, pointwise ContinuousMap exposure, rational tolerance exactness, ProbeBundle partition coverage, refinement ladder stability, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactModulusPartitionLadderUp.}
\end{closurestatus}
