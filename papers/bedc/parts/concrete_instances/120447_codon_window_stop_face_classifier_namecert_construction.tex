\chapter{A Concrete Naming Certificate for $\CodonWindowStopFaceClassifierUp$}
\label{ch:concrete-instances-codon-window-stop-face-classifier-namecert}
\origin{ai}

The $\CodonWindowStopFaceClassifierUp$ packet realizes the local stop-face classifier route of \autoref{subsec:philosophy-codon-window-six-bridge} as finite BEDC NameCert data. It consumes the six-coordinate source atlas of \autoref{ch:concrete-instances-codon-six-bit-window-namecert}, the stop-face suffix surface of \autoref{ch:concrete-instances-stopcodonzeckendorfsuffix-namecert}, the codon-window spectral inverse sibling of \autoref{ch:concrete-instances-codon-window-spectral-inverse-namecert}, and the deterministic transition-matrix rows of \autoref{ch:concrete-instances-codon-transition-matrix-namecert}. The packet records the $\mathrm{U}+\mathrm{RR}$ face, the local suffix classifier $00,01,10,11$, the exclusion of $\mathrm{UGG}$ as the $11$ corner, and the surviving stop rows $\mathrm{UAA}$, $\mathrm{UAG}$, and $\mathrm{UGA}$; it is not a global no-$11$ predicate, a full genetic-code table, an amino-acid classifier, a stochastic readthrough model, or a quotient codon semantics.

\begin{definition}[Codon-window stop-face classifier carrier]
\label{def:codon-window-stop-face-classifier-carrier}
A $\CodonWindowStopFaceClassifierUp$ carrier is a finite $\BHist$ packet
$$
F=(W,A,U,S,X,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the $\CodonSixBitWindowUp$ source-window row exposing the six-bit codon coordinates and Flat6 readback.
\item $A$ is the two-axis atlas row naming the $\mathrm{U},\mathrm{C},\mathrm{A},\mathrm{G}$ base reads used by the codon-window bridge.
\item $U$ is the face-selection row. It selects the local $\mathrm{U}+\mathrm{RR}$ square before any stop decision is inspected.
\item $S$ is the suffix-coordinate row. It displays the four local family suffixes $00,01,10,11$ inside the selected face.
\item $X$ is the $\StopCodonZeckendorfSuffixUp$ legality row, retaining exactly the $X_{2}=\{00,01,10\}$ suffixes.
\item $D$ is the stop-decision row. It maps $00,01,10$ to the stop rows $\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}$ and maps $11$ to the excluded $\mathrm{UGG}$ corner.
\item $E$ records componentwise $\hsame$ transport for source windows, atlas rows, face rows, suffix rows, legality rows, decision rows, replay, provenance, and local naming.
\item $H$ records displayed $\Cont$ replay for the route
$$
W,A,U,S \longmapsto X,D \longmapsto E,C,P,N .
$$
\item $C$ is the classifier handoff row used by local stop-face consumers that accept only the displayed finite route.
\item $P$ is the $\Pkg$ provenance over $\CodonWindowStopFaceClassifierUp$, $\CodonSixBitWindowUp$, $\StopCodonZeckendorfSuffixUp$, $\CodonWindowSpectralInverseUp$, $\CodonTransitionMatrixUp$, the codon-window-six bridge, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CodonWindowStopFaceClassifierUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these eleven rows. It has no coordinate for a global flattened no-$11$ predicate, a full translation table, amino-acid class identity, stochastic readthrough, quotient codon equality, selected biological code tables, or global biological universality.
\end{definition}

\begin{theorem}[Codon-window stop-face classifier NameCert obligations]
\label{thm:codon-window-stop-face-classifier-namecert-obligations}
For every accepted $\CodonWindowStopFaceClassifierUp$ carrier
$$
F=(W,A,U,S,X,D,E,H,C,P,N),
$$
the local $\NameCert_{\CodonWindowStopFaceClassifierUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for six-bit source windows, two-axis atlas rows, local face selection, suffix-coordinate rows, suffix-legality rows, stop-decision rows, transport, replay, classifier handoff, provenance, and local naming;
\item source-atlas exactness, requiring every local stop-face read to pass through $W$ and $A$ before a suffix is classified;
\item face locality, requiring the decision row to be attached to the selected $\mathrm{U}+\mathrm{RR}$ square $U$ rather than to the whole flattened six-bit window;
\item suffix exactness, requiring $S$ and $X$ to retain $00,01,10$ and exclude only $11$ inside that face;
\item stop-decision determinacy, requiring $D$ to export exactly $\mathrm{UAA}$, $\mathrm{UAG}$, and $\mathrm{UGA}$ as stop rows while keeping $\mathrm{UGG}$ outside the stop class;
\item non-escape of global no-$11$ classification, full genetic-code tables, amino-acid classifiers, stochastic readthrough, quotient codon equality, selected biological code tables, and biological universality claims.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:codon-window-stop-face-classifier-carrier}. The visible analytic rows are $W,A,U,S,X,D$, and the structural rows $E,H,C,P,N$ preserve, replay, hand off, source, and locally name the same finite route.

Source-atlas exactness comes first. The six-bit source row $W$ and atlas row $A$ expose the codon window before any face or suffix classifier can be read.

Face locality is then enforced by $U$. The suffix rows $S$ and $X$ are attached only to the selected $\mathrm{U}+\mathrm{RR}$ square, so the classifier cannot become a global no-$11$ predicate on the flattened six-bit string.

The decision row $D$ is deterministic over the displayed suffixes: it exports $00,01,10$ as $\mathrm{UAA}$, $\mathrm{UAG}$, and $\mathrm{UGA}$ stop rows and keeps $11$ as the excluded $\mathrm{UGG}$ corner. Since the refused biological objects have no carrier coordinate and no replay edge, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Codon-window stop-face exclusion route]
\label{thm:codon-window-stop-face-classifier-exclusion-route}
Every accepted $\CodonWindowStopFaceClassifierUp$ read factors through
$$
\CodonSixBitWindowUp,\ \StopCodonZeckendorfSuffixUp,\ \CodonWindowSpectralInverseUp,\ \CodonTransitionMatrixUp
$$
as sibling evidence before it may export a local stop decision. The exported decision is the finite $\mathrm{U}+\mathrm{RR}$ face classifier that excludes $\mathrm{UGG}$ and retains $\mathrm{UAA}$, $\mathrm{UAG}$, and $\mathrm{UGA}$, not the global no-adjacent predicate of \autoref{thm:philosophy-global-no-eleven-not-stop-classifier}.
\end{theorem}
\begin{proof}
Apply \autoref{thm:codon-window-stop-face-classifier-namecert-obligations}. Carrier admission forces the read through $W,A,U,S,X,D$ before any handoff row can be consumed.

The sibling route is finite. The source row $W$ is the six-coordinate atlas packet, the legality row $X$ is the stop-suffix packet, and the provenance row $P$ names the spectral-inverse and transition-matrix consumers that inspect the same stop face.

By suffix exactness and stop-decision determinacy, only $00,01,10$ are exported as stop rows. The $11$ suffix is the $\mathrm{UGG}$ corner and remains outside the stop class.

Thus the classifier is local to the selected face. It records the cannot-claim boundary of \autoref{thm:philosophy-global-no-eleven-not-stop-classifier} as concrete NameCert data, rather than importing a global flattened predicate or a full biological translation table.
\end{proof}

\falsifiablePrediction{Every accepted $\CodonWindowStopFaceClassifierUp$ packet exposes CodonSixBitWindowUp source rows, two-axis atlas rows, U+RR face selection, suffix coordinates, StopCodonZeckendorfSuffixUp legality, stop-decision rows, transport, replay, classifier handoff, provenance, and local naming. A packet that classifies stop codons by global no-adjacent-$11$ on the flattened six-bit window fails the face-locality row.}

\independenceWitness{codon-six-bit-window, stopcodonzeckendorfsuffix, codon-window-spectral-inverse, codon-transition-matrix: $\CodonWindowStopFaceClassifierUp$ is not identical to any listed sibling because it binds source atlas, local face selection, suffix legality, and deterministic UGG exclusion into one local stop-face classifier packet.}

\closureat{CodonWindowStopFaceClassifierUp}{seedStr}

\begin{closurestatus}{\CodonWindowStopFaceClassifierUp}
  \constructivestory{A $\CodonWindowStopFaceClassifierUp$ packet is generated from CodonSixBitWindowUp source rows, two-axis atlas rows, a U+RR face-selection row, suffix-coordinate rows, StopCodonZeckendorfSuffixUp legality rows, a deterministic stop-decision row, componentwise hsame transport, displayed Cont replay, a classifier handoff row, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:codon-window-stop-face-classifier-carrier}, \autoref{thm:codon-window-stop-face-classifier-namecert-obligations}, and \autoref{thm:codon-window-stop-face-classifier-exclusion-route} seed the local U+RR stop-face classifier and its UGG exclusion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global flattened no-adjacent classification, full genetic-code table identity, amino-acid classifiers, stochastic readthrough, quotient codon equality, selected biological code tables, biological universality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-atlas exactness, face locality, suffix exactness, stop-decision determinacy, transport, replay, provenance, and local naming for BEDC.Derived.CodonWindowStopFaceClassifierUp.}
\end{closurestatus}
