\chapter{A Concrete Naming Certificate for $\SequentiallyCompactRealIntervalUp$}
\label{ch:concrete-instances-sequentially-compact-real-interval-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\SequentiallyCompactRealIntervalUp$ packet records an explicit-subsequence-selector compactness surface for located real intervals. It sits after the real-name carrier of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the selector discipline of \autoref{ch:concrete-instances-cauchy-subsequence-selector-namecert}, and the located interval sequence route of \autoref{ch:concrete-instances-located-compact-interval-sequence-namecert}. The packet does not claim open-cover Heine--Borel compactness, quotient real equality, selected representatives, countable choice, or general sequential compactness without displayed selector data.

\begin{definition}[Sequentially compact real interval carrier]
\label{def:sequentially-compact-real-interval-carrier}
A $\SequentiallyCompactRealIntervalUp$ carrier is a finite $\BHist$ packet
$$
K=(I,S,Q,M,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the located real interval row, recording endpoint names, locator data, and the closed interval boundary read through $\RealUp$.
\item $S$ is the $\StreamNameUp$ row for the input sequence of interval points and the finite windows inspected before a subsequence is accepted.
\item $Q$ is the explicit selector row, naming the monotone subsequence index function and the finite evidence that each selected index is read from the input stream.
\item $M$ is the Cauchy-modulus corridor row, recording the displayed tolerance schedule, interval-nesting or finite-net corridor, and the modulus data attached to the selected subsequence.
\item $R$ is the $\RegSeqRatUp$ and $\RealUp$ handoff row, converting regular rational readback and terminal real-name sealing into the same interval packet.
\item $H$ records componentwise $\hsame$ transport for interval, stream, selector, modulus, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located interval and sequence request through the selector and Cauchy-modulus corridor.
\item $P$ is the $\Pkg$ provenance over $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\CauchySubsequenceSelectorUp$, $\LocatedCompactIntervalSequenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SequentiallyCompactRealIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,S,Q,M,R,H,C,P,N$. It has no coordinate for open-cover compactness, quotient equality of real names, hidden selected representatives, countable choice, or a general sequential-compactness principle detached from the displayed selector row.
\end{definition}

\begin{theorem}[Sequentially compact real interval NameCert obligations]
\label{thm:sequentially-compact-real-interval-obligation}
For every accepted $\SequentiallyCompactRealIntervalUp$ carrier
$$
K=(I,S,Q,M,R,H,C,P,N),
$$
the local $\NameCert_{\SequentiallyCompactRealIntervalUp}$ surface consists of five finite obligations:
\begin{enumerate}
\item interval exposure: the located interval row $I$ is displayed before any compactness read;
\item sequence exposure: the input sequence and inspected finite windows are exposed through $S$;
\item explicit selector exposure: the subsequence selector row $Q$ displays the chosen index map and its monotonicity-facing data;
\item Cauchy-modulus corridor: the selected subsequence is routed through the modulus and finite corridor row $M$ before convergence may be read;
\item Real-seal handoff: the regular rational readback and terminal real-name seal travel through $R,H,C,P,N$ as the same local packet.
\end{enumerate}
Thus every accepted compactness consumer reads located interval data, sequence windows, an explicit selector, a Cauchy-modulus corridor, and a Real-facing seal in one finite route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sequentially-compact-real-interval-carrier}. The rows $I,S,Q,M,R,H,C,P,N$ are the only accepted coordinates, so each consumer read begins with the located interval, sequence, selector, modulus corridor, and Real handoff rows.

Use the selector and located interval siblings next. The $\CauchySubsequenceSelectorUp$ route supplies displayed selector data, while \autoref{ch:concrete-instances-located-compact-interval-sequence-namecert} supplies the located finite-window interval corridor consumed by $M$.

Finally read the regular rational and Real rows through $R,H,C,P,N$. These structural rows transport, replay, source, and name the same packet; they do not add open-cover compactness, quotient equality, hidden representatives, countable choice, or selector-free sequential compactness.
\end{proof}

\falsifiablePrediction{An accepted $\SequentiallyCompactRealIntervalUp$ consumer that reaches a Real seal or Cauchy subsequence without displaying $I,S,Q,M,R,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{real, regseqrat, streamname, cauchy-subsequence-selector, located-compact-interval-sequence: no listed sibling supplies the joint finite packet containing located interval data, stream windows, explicit subsequence selector rows, Cauchy-modulus corridors, regular rational readback, Real sealing, transport, replay, provenance, and local NameCert rows.}

\closureat{SequentiallyCompactRealIntervalUp}{seedStr}

\begin{closurestatus}{\SequentiallyCompactRealIntervalUp}
  \constructivestory{A $\SequentiallyCompactRealIntervalUp$ packet is generated from located real interval rows, stream-window sequence rows, explicit subsequence selector rows, Cauchy-modulus corridor rows, RegSeqRat and Real handoff rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sequentially-compact-real-interval-carrier} and \autoref{thm:sequentially-compact-real-interval-obligation} seed the explicit-selector compactness route for located real intervals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover Heine--Borel compactness, quotient real equality, selected representatives, countable choice, general sequential compactness without displayed selector data, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located interval exposure, sequence-window exposure, explicit selector exactness, Cauchy-modulus corridor preservation, Real-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.SequentiallyCompactRealIntervalUp.}
\end{closurestatus}
