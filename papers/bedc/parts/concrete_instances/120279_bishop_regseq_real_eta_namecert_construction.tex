\chapter{A Concrete Naming Certificate for $\BishopRegSeqRealEtaUp$}
\label{ch:concrete-instances-bishop-regseq-real-eta-namecert}
\origin{human}

Following Bishop--Bridges constructive completion, the $\BishopRegSeqRealEtaUp$ packet records the finite eta route by which a displayed regular rational sequence name is sealed as a $\RealUp$ name and then read back through the same finite windows. It sits over the regular-rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the finite window service of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the completion handoff of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}. The packet names the eta surface from a $\RegSeqRatUp$ display to a $\RealUp$ seal and back to the same displayed route; it does not identify quotient representatives, select a limit stream, invoke countable choice, or assert ambient $\RealUp$ completeness.

\begin{definition}[Bishop RegSeqRat-to-Real eta carrier]
\label{def:bishop-regseq-real-eta-carrier}
A $\BishopRegSeqRealEtaUp$ carrier is a finite $\BHist$ packet
$$
E=(Q,W,D,R,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the $\RegSeqRatUp$ source row, recording the regular rational sequence name and its displayed Cauchy modulus data.
\item $W$ is the $\StreamNameUp$ window row, naming the finite observations through which the source sequence is inspected.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording dyadic radii, denominator budgets, and slack comparisons used by the windows.
\item $R$ is the terminal $\RealUp$ seal, available only after $Q,W,D$ have been displayed.
\item $A$ is the eta readback row, requiring any read from the sealed real name back toward regular rational data to expose the same $Q,W,D,R$ route.
\item $H$ records componentwise $\hsame$ transport for the source row, windows, dyadic ledger, Real seal, eta readback, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from a finite precision request through $Q,W,D,R,A,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopRegSeqRealEtaUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchyCompletionFunctorUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegSeqRealEtaUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $Q,W,D,R,A,H,C,P,N$. It has no coordinate for quotient equality of real names, selected representatives, countable-choice schedules, hidden completion objects, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop RegSeqRat-to-Real eta NameCert obligations]
\label{thm:bishop-regseq-real-eta-namecert-obligations}
For every accepted $\BishopRegSeqRealEtaUp$ carrier
$$
E=(Q,W,D,R,A,H,C,P,N),
$$
the local $\NameCert_{\BishopRegSeqRealEtaUp}$ surface consists exactly of carrier admission for the $\RegSeqRatUp$ source row, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, $\RealUp$ seal, eta readback row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming row; seal ordering requiring $R$ to be read only after $Q,W,D$; eta readback requiring $A$ to expose the same $Q,W,D,R$ route; and non-escape excluding quotient representatives, selected limits, countable choice, hidden completion objects, and ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-regseq-real-eta-carrier}. The accepted packet displays $Q,W,D,R,A,H,C,P,N$, so carrier admission and local naming are finite row projections.

Read the seal ordering next. The $\RegSeqRatUp$ source row $Q$ supplies the regular rational name, $W$ supplies the finite $\StreamNameUp$ observations, and $D$ supplies the $\DyadicRatCoreUp$ tolerance ledger before the terminal $\RealUp$ seal $R$ can be consumed.

Read the eta row $A$. It is not a second representative of the real name; it is the displayed return route that exposes the same source, window, tolerance, and seal rows when a consumer reads back from $R$ toward regular rational data.

Finally inspect $H,C,P,N$. These rows transport, replay, source, and name the same finite route. Since no carrier coordinate stores quotient equality, a selected limit, a choice schedule, a hidden completion object, or Real completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop RegSeqRat-to-Real eta readback nonescape]
\label{thm:bishop-regseq-real-eta-readback-nonescape}
Every consumer that reads the eta row of an accepted $\BishopRegSeqRealEtaUp$ packet must carry the same $\RegSeqRatUp$ source, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, and $\RealUp$ seal through $H,C,P,N$. A readback that omits any one of $Q,W,D,R$ leaves the accepted carrier and cannot cite the packet as a RegSeqRat-to-Real eta certificate.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bishop-regseq-real-eta-namecert-obligations}. They require the eta row $A$ to expose the same $Q,W,D,R$ route that produced the Real seal.

Transport and replay do not add another route. The row $H$ preserves the displayed components under $\hsame$, while $C$ replays the finite precision request through those components. The provenance row $P$ and local name $N$ only record the same package boundary.

Therefore a consumer that omits $Q$, $W$, $D$, or $R$ is no longer reading the accepted eta packet. It may describe a partial source, partial window, partial tolerance ledger, or sealed real name, but it is not the $\BishopRegSeqRealEtaUp$ readback route.
\end{proof}

\falsifiablePrediction{An accepted $\BishopRegSeqRealEtaUp$ readback exposes a RegSeqRatUp source row, StreamNameUp finite windows, DyadicRatCoreUp tolerance ledger, RealUp seal, eta row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row before any consumer treats the sealed real name as returning to regular rational data.}

\independenceWitness{regseqrat, real, streamname, dyadicratcore, cauchycompletionfunctor: none is bijective to $\BishopRegSeqRealEtaUp$ because the packet jointly carries the source regular sequence, finite windows, dyadic tolerance ledger, Real seal, eta readback, transport, replay, provenance, and local NameCert rows while refusing quotient representatives, selected limits, countable choice, hidden completion objects, and ambient Real completeness.}

\closureat{BishopRegSeqRealEtaUp}{seedStr}

\begin{closurestatus}{\BishopRegSeqRealEtaUp}
  \constructivestory{A $\BishopRegSeqRealEtaUp$ packet is generated from a RegSeqRatUp source row, StreamNameUp windows, a DyadicRatCoreUp tolerance ledger, a RealUp seal, an eta readback row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regseq-real-eta-carrier}, \autoref{thm:bishop-regseq-real-eta-namecert-obligations}, and \autoref{thm:bishop-regseq-real-eta-readback-nonescape} seed the finite RegSeqRat-to-Real eta route through source rows, finite windows, dyadic tolerance data, Real seals, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality of real names, selected representatives, selected limits, countable choice, hidden completion objects, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, seal ordering, eta readback exactness, non-escape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRegSeqRealEtaUp.}
\end{closurestatus}
