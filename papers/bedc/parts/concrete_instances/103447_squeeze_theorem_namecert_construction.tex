\chapter{A Concrete Naming Certificate for $\SqueezeTheoremUp$}
\label{ch:concrete-instances-squeeze-theorem-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\SqueezeTheoremUp$ packet records the constructive comparison sandwich as a finite lower-middle-upper $\RealUp$ comparison packet over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealSequenceLimitUp$, and $\RealUp$ rows. It sits after the real seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the displayed sequence-limit route of \autoref{ch:concrete-instances-real-sequence-limit-namecert}. The packet gives L10 Real and Completion consumers one named sandwich surface without importing host limits, quotient streams, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Squeeze theorem carrier]
\label{def:squeeze-theorem-carrier}
A $\SqueezeTheoremUp$ carrier is a finite $\BHist$ packet
$$
S=(L,M,U,W,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$, $M$, and $U$ are the lower, middle, and upper source rows. Each source is read only through the same displayed sequence-limit and real-comparison route before any conclusion is exported.
\item $W$ is the finite $\StreamNameUp$ window row. It records the inspected tail indices, lower-middle comparison windows, middle-upper comparison windows, and limit-window requests.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger. It records the split tolerance used to compare the lower and upper bounds with the common limit row and the middle row with the displayed sandwich windows.
\item $Q$ is the $\RegSeqRatUp$ readback row for the scheduled lower, middle, upper, and limit observations.
\item $E$ is the $\RealSequenceLimitUp$ and terminal $\RealUp$ seal row. It becomes available only after $L,M,U,W,D,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the three source rows, window row, dyadic ledger, rational readback row, seal row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
L,M,U,W,D,Q \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\SqueezeTheoremUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealSequenceLimitUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SqueezeTheoremUp}$ row.
\end{enumerate}
The carrier predicate admits exactly these rows. Its classifier compares accepted packets only through $L,M,U,W,D,Q,E,H,C,P,N$ and has no coordinate for host sequence limits, quotient stream equality, trichotomy, countable choice, selected representatives, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Squeeze theorem NameCert obligations]
\label{thm:squeeze-theorem-namecert-obligations}
For every accepted $\SqueezeTheoremUp$ carrier
$$
S=(L,M,U,W,D,Q,E,H,C,P,N),
$$
the local $\NameCert_{\SqueezeTheoremUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the lower, middle, and upper source rows, finite window row, dyadic tolerance ledger, regular-rational readback row, displayed sequence-limit and real-seal row, transport, replay, provenance, and local naming row;
\item sandwich-window discipline, requiring every lower-middle and middle-upper comparison to be read through the same scheduled $W$ row before the middle limit can be exported;
\item dyadic tolerance exactness, requiring $D$ to record the tolerance split consumed by the two bounding convergence reads and the middle comparison read;
\item rational readback discipline, requiring $Q$ to expose the scheduled $\RegSeqRatUp$ observations for the lower, middle, upper, and limit rows before the terminal seal $E$ is available;
\item real-seal nonescape, requiring $E$ to remain downstream of the displayed sequence-limit route and forbidding direct host-limit or completeness reads;
\item transport, replay, provenance, and naming stability, requiring $H,C,P,N$ to preserve and source exactly the displayed rows componentwise;
\item ledger refusal, excluding host sequence limits, quotient stream equality, trichotomy, countable choice, selected representatives, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{enumerate}
No squeeze theorem obligation is stored outside these rows.
\end{theorem}

\begin{proof}
Project the accepted packet $S=(L,M,U,W,D,Q,E,H,C,P,N)$. The carrier exposes the three source rows, one shared finite-window row, one dyadic tolerance ledger, one regular-rational readback row, one terminal sequence-limit and real-seal row, and the structural rows.

The comparison route starts with the common window. The row $W$ fixes which lower-middle, middle-upper, and limit windows are inspected, so the middle source cannot use an unrelated comparison schedule.

The dyadic and rational data are then forced. The row $D$ supplies the tolerance split, and $Q$ supplies the scheduled $\RegSeqRatUp$ observations attached to that split before any seal is visible.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same finite rows. Since none of the excluded host objects has a carrier coordinate, replay edge, provenance entry, or local naming row, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Squeeze theorem comparison sandwich]
\label{thm:squeeze-theorem-comparison-sandwich}
Every accepted $\SqueezeTheoremUp$ carrier exposes the middle real-sequence limit only through the finite route
$$
L,M,U,W,D,Q \longmapsto E \longmapsto H,C,P,N .
$$
Thus the consumer receives a displayed lower-middle-upper comparison sandwich, a shared $\StreamNameUp$ tail-window schedule, $\DyadicRatCoreUp$ tolerance accounting, $\RegSeqRatUp$ readbacks, the $\RealSequenceLimitUp$ handoff, the terminal $\RealUp$ seal boundary, componentwise transport, continuation replay, package provenance, and local naming. It receives no host sequence limit, quotient stream equality, trichotomy, countable choice, selected representative, or ambient $\RealUp$ completeness principle.
\end{theorem}

\begin{proof}
Start with an accepted squeeze-theorem read. The source rows are $L$, $M$, and $U$, and the carrier requires all comparison evidence to be observed through the shared finite-window row $W$.

The dyadic ledger $D$ fixes the tolerance split for the bounding convergence reads and the middle comparison read. The regular-rational row $Q$ then exposes the scheduled lower, middle, upper, and limit observations before the real seal can be reached.

Only after those rows have been displayed can $E$ expose the $\RealSequenceLimitUp$ handoff and terminal $\RealUp$ seal boundary. The structural rows $H,C,P,N$ preserve that same finite route under componentwise transport, continuation replay, provenance, and local naming.

Induction over the displayed route order proves that no later consumer can skip a row: the base stage reads $L,M,U,W,D,Q$, the terminal step reads $E$, and the structural step reads $H,C,P,N$. Because there is no coordinate outside this route, the consumer cannot obtain host sequence limits, quotient stream equality, trichotomy, countable choice, selected representatives, or ambient $\RealUp$ completeness from the packet.
\end{proof}

\begin{theorem}[Squeeze theorem real-seal nonescape]
\label{thm:squeeze-theorem-real-seal-nonescape}
An accepted $\SqueezeTheoremUp$ consumer that reaches the terminal $\RealUp$ seal must have already displayed the lower row, middle row, upper row, shared StreamName window row, dyadic tolerance ledger, RegSeqRat readback row, and RealSequenceLimit handoff. Consequently the real seal cannot be used as a shortcut to host limit existence, quotient stream equality, trichotomy, countable choice, selected representatives, or ambient $\RealUp$ completeness.
\end{theorem}

\begin{proof}
Read the seal row $E$ from an accepted carrier. By carrier admission, $E$ is not an independent coordinate; it is downstream of $L,M,U,W,D,Q$ and records only the sequence-limit and real-seal boundary reached from those displayed rows.

The replay row $C$ has no branch that starts at $E$. It replays the route from the three source rows through the window schedule, tolerance ledger, and rational readback before the seal can be consumed.

The transport and provenance rows $H$ and $P$ preserve the same dependency order. The local name $N$ therefore names only the displayed sandwich packet, so no host limit, quotient stream, trichotomy, countable-choice selector, selected representative, or ambient completeness witness can escape through the seal.
\end{proof}

\closureat{SqueezeTheoremUp}{seedStr}

\begin{closurestatus}{\SqueezeTheoremUp}
  \constructivestory{A $\SqueezeTheoremUp$ packet is generated from lower, middle, and upper source rows, one shared StreamName tail-window schedule, a DyadicRatCore tolerance ledger, RegSeqRat readbacks, a RealSequenceLimit handoff, a terminal RealUp seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:squeeze-theorem-carrier}, \autoref{thm:squeeze-theorem-namecert-obligations}, \autoref{thm:squeeze-theorem-comparison-sandwich}, and \autoref{thm:squeeze-theorem-real-seal-nonescape} seed the finite constructive comparison sandwich over StreamName, RegSeqRat, DyadicRatCore, RealSequenceLimit, and RealUp rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host sequence limits, quotient stream equality, trichotomy, countable choice, selected representatives, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, sandwich-window discipline, dyadic tolerance exactness, rational readback discipline, real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.SqueezeTheoremUp.}
\end{closurestatus}
