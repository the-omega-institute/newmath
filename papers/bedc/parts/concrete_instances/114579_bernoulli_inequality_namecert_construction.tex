\chapter{A Concrete Naming Certificate for $\BernoulliInequalityUp$}
\label{ch:concrete-instances-bernoulli-inequality-namecert}
\origin{human}

Following Bernoulli and Hardy--Littlewood--Polya, the $\BernoulliInequalityUp$ packet records the finite binomial lower-bound route used before Real algebra and calculus estimates read a power comparison. It sits beside the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the dyadic arithmetic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet exports the finite comparison route for a nonnegative base and unary exponent; it is not a host real-power theorem, a global ordered-field library, trichotomy, quotient stream equality, or ambient $\RealUp$ completeness.

\begin{definition}[Bernoulli inequality carrier]\label{def:bernoulli-inequality-carrier}
A $\BernoulliInequalityUp$ carrier is a finite $\BHist$ packet
$$
B=(X,N,D,Q,E,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the nonnegative $\RealUp$ base row, displaying the accepted real seal for the term $1+x$ together with the proof-relevant nonnegative boundary for $x$.
\item $N$ is the unary exponent row, recording the finite successor spine used for the power read.
\item $D$ is the $\DyadicRatCoreUp$ binomial ledger, containing the displayed coefficient rows, nonnegative remainder rows, and tolerance budget used by the finite binomial comparison.
\item $Q$ is the $\RegSeqRatUp$ readback row for the inspected base, coefficient, product, and remainder observations.
\item $E$ is the terminal $\RealUp$ comparison seal for the lower bound between the finite power read and $1+Nx$.
\item $H$ stores componentwise $\hsame$ transport for the base, exponent, dyadic binomial ledger, regular-rational readback, comparison seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the base and exponent rows through the dyadic binomial ledger, RegSeqRat readback, and Real comparison seal.
\item $P$ is the $\Pkg$ provenance over $\BernoulliInequalityUp$, $\RealUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $L$ is the local $\NameCert_{\BernoulliInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,N,D,Q,E,H,C,P,L$. It has no coordinate for host exponentiation, a selected infinite binomial series, trichotomy, quotient real equality, global ordered-field automation, or ambient Real completeness.
\end{definition}

\begin{theorem}[Bernoulli inequality NameCert obligations]\label{thm:bernoulli-inequality-namecert-obligations}
For every accepted $\BernoulliInequalityUp$ carrier $B=(X,N,D,Q,E,H,C,P,L)$, the local $\NameCert_{\BernoulliInequalityUp}$ surface consists exactly of carrier admission, nonnegative $\RealUp$ base exposure, unary exponent exposure, dyadic binomial-ledger exposure, $\RegSeqRatUp$ readback, terminal Real comparison sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No consumer may read a Bernoulli comparison from the packet before $X,N,D,Q,E,H,C,P,L$ are displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bernoulli-inequality-carrier}. The accepted rows are exactly the nonnegative base, unary exponent, dyadic ledger, regular-rational readback, Real comparison seal, and the structural rows.

The base and exponent obligations are read from $X$ and $N$. The packet therefore exposes a finite successor spine and a nonnegative Real boundary before any power comparison is public.

The binomial comparison is read from $D,Q,E$. The dyadic ledger records the coefficients and nonnegative remainder budget, the regular-rational row packages the inspected arithmetic observations, and the Real seal is consumed only after those rows are present.

Rows $H,C,P,L$ transport, replay, source, and name the same finite route. Since the carrier lacks host exponentiation, global order automation, quotient equality, selected infinite series, and ambient completeness coordinates, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bernoulli dyadic binomial handoff]\label{thm:bernoulli-inequality-dyadic-binomial-handoff}
Every accepted $\BernoulliInequalityUp$ comparison handoff factors through
$$
\RealUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
inside the displayed carrier rows $X,D,Q,E,H,C,P,L$. The dyadic binomial ledger $D$ supplies the finite coefficient and nonnegative remainder budget, the RegSeqRat readback $Q$ packages the inspected rational observations, and the terminal Real seal $E$ exports only the finite Bernoulli lower-bound comparison for the unary exponent row $N$.
\end{theorem}
\begin{proof}
Start with an accepted comparison request. The base row $X$ supplies the nonnegative Real input, and the exponent row $N$ supplies the finite successor spine.

The request then enters the dyadic binomial ledger $D$. That row records the coefficient and remainder budget needed for the finite binomial comparison and keeps every comparison at a displayed dyadic tolerance.

The readback row $Q$ packages the visible arithmetic observations before the Real seal $E$ is available. Transport, replay, provenance, and local naming are supplied by $H,C,P,L$ and do not add any host arithmetic principle.

Thus the handoff is the finite route through the Real, DyadicRatCore, RegSeqRat, and Real rows. It does not export host powers, quotient real equality, trichotomy, an infinite binomial series, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every $\BernoulliInequalityUp$ comparison read exposes a nonnegative RealUp base row, a unary exponent row, a DyadicRatCore binomial ledger, RegSeqRat readback, a terminal RealUp comparison seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert before any lower-bound comparison is exported.}

\independenceWitness{real, dyadicratcore, regseqrat, real-triangle-inequality, cauchy-product-tail-budget: $\BernoulliInequalityUp$ is not bijective to any listed sibling because it jointly carries a nonnegative base, unary exponent, dyadic binomial coefficient and remainder ledger, regular-rational readback, terminal Real comparison seal, transport, replay, provenance, and local NameCert in one inequality packet.}

\closureat{BernoulliInequalityUp}{seedStr}

\begin{closurestatus}{\BernoulliInequalityUp}
  \constructivestory{A $\BernoulliInequalityUp$ packet is generated from a nonnegative RealUp base row, a unary exponent row, a DyadicRatCore binomial ledger, RegSeqRat readback, a terminal RealUp comparison seal, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bernoulli-inequality-carrier}, \autoref{thm:bernoulli-inequality-namecert-obligations}, and \autoref{thm:bernoulli-inequality-dyadic-binomial-handoff} seed the finite Bernoulli comparison route over RealUp, DyadicRatCoreUp, RegSeqRatUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host exponentiation, global ordered-field automation, trichotomy, quotient real equality, selected infinite binomial series, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nonnegative base exposure, unary exponent exposure, dyadic binomial-ledger exposure, RegSeqRat readback, Real comparison sealing, transport, replay, provenance, and local naming for BEDC.Derived.BernoulliInequalityUp.}
\end{closurestatus}
