\chapter{A Concrete Naming Certificate for $\CompactUniformModulusNerveUp$}
\label{ch:concrete-instances-compact-uniform-modulus-nerve-namecert}
\origin{human}

Following Heine--Cantor and Bishop compactness machinery, the $\CompactUniformModulusNerveUp$ packet records the finite nerve route by which compact finite covers, local continuity moduli, rational lower-bound folds, and a $\UniformModulusUp$ handoff are read as one NameCert object. It realizes the finite-cover branch of \autoref{ch:visions-compact-uniform-continuity} and sits beside the finite cover nerve of \autoref{ch:concrete-instances-finite-cover-nerve-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational comparison row of \autoref{ch:concrete-instances-rat-namecert}, and the downstream uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}.

\begin{definition}[Compact uniform modulus nerve carrier]
\label{def:compact-uniform-modulus-nerve-carrier}
A $\CompactUniformModulusNerveUp$ carrier is a finite $\BHist$ packet
$$
M=(K,F,L,R,B,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-cover source row, displaying the inspected $\mathsf{ProbeBundle}$ centers and cover-radius histories.
\item $F$ is the finite nerve row, recording cover membership, overlap compatibility, and the accepted finite faces used by the modulus extraction.
\item $L$ is the local-modulus row read from the $\ContinuousMapUp$ surface, assigning displayed source neighborhoods to displayed target tolerances.
\item $R$ is the $\RatUp$ lower-bound comparison row for the cover radii and local modulus radii.
\item $B$ is the finite rational-fold row that computes the uniform positive lower-bound ledger.
\item $U$ is the $\UniformModulusUp$ handoff row consumed after $K,F,L,R,B$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for cover, nerve, local-modulus, rational-fold, handoff, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay through finite covers, nerve faces, local moduli, rational folds, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusNerveUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\FiniteCoverNerveUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformModulusNerveUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,L,R,B,U,H,C,P,N$. It has no coordinate for arbitrary open-cover compactness, selected subcovers, host function equality, countable choice, quotient equality of covers, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact uniform modulus nerve NameCert obligations]
\label{thm:compact-uniform-modulus-nerve-namecert-obligations}
For every accepted $\CompactUniformModulusNerveUp$ carrier
$$
M=(K,F,L,R,B,U,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformModulusNerveUp}$ surface consists exactly of compact finite-cover exposure through $K$, finite nerve exposure through $F$, local-modulus exposure through $L$, rational comparison through $R$, finite lower-bound folding through $B$, uniform-modulus handoff through $U$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-nerve-carrier}. The compact-cover, nerve, local-modulus, rational-fold, uniform-modulus, transport, replay, provenance, and naming rows are the complete inventory of the packet.

The cover and nerve obligations are read through $K,F$. These rows expose finite $\mathsf{ProbeBundle}$ data and accepted faces, not an arbitrary open cover or selected subcover.

The modulus obligations are read through $L,R,B,U$. Local continuity data is displayed before the rational lower-bound fold, and the uniform-modulus handoff is downstream of that fold.

Finally $H,C,P,N$ preserve and name only these rows. No host function equality, countable choice principle, quotient cover equality, or ambient completeness theorem is exported.
\end{proof}

\begin{theorem}[Compact uniform modulus nerve vision handoff]
\label{thm:compact-uniform-modulus-nerve-vision-handoff}
The compact finite-cover, local-modulus, $\RatUp$ lower-bound fold, and $\UniformModulusUp$ handoff route of \autoref{ch:visions-compact-uniform-continuity} is realized by an accepted $\CompactUniformModulusNerveUp$ carrier as the finite row sequence
$$
K,F,L,R,B \longmapsto U,H,C,P,N .
$$
The handoff is a concrete NameCert packet and not an external Heine--Cantor theorem.
\end{theorem}
\begin{proof}
Read the vision chapter through its finite-cover branch. The compact $\mathsf{ProbeBundle}$ and cover-radius histories land in $K$, and the finite nerve membership and overlap checks land in $F$.

The local continuity witnesses land in $L$. Their radii are compared through $R$ and folded by $B$ before any uniform-modulus row is exposed.

The row $U$ is therefore downstream of finite cover, nerve, local-modulus, and rational-fold data. Transport, replay, provenance, and local naming keep the handoff inside the same packet, so the construction does not invoke an external compactness theorem or selector.
\end{proof}

\begin{theorem}[Compact uniform modulus nerve no-choice boundary]
\label{thm:compact-uniform-modulus-nerve-no-choice-boundary}
Every accepted $\CompactUniformModulusNerveUp$ read keeps the finite nerve and rational lower-bound fold visible before the $\UniformModulusUp$ handoff. Consequently the packet exports no arbitrary open-cover compactness principle, selected subcover, countable choice functional, quotient equality of covers, host function equality, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Start from the carrier rows $K,F,L,R,B,U,H,C,P,N$. The only route to $U$ passes through the finite cover row, nerve row, local-modulus row, rational comparison row, and finite fold row.

The sibling finite cover nerve chapter supplies a displayed cover-nerve route, while the compact metric, continuous-map, rational, and uniform-modulus chapters supply the rows consumed by the packet. None of these rows is a choice functional or host equality principle.

Since transport and replay preserve only the displayed carrier coordinates, a consumer cannot bypass $F,R,B$ to obtain a modulus from an external compactness theorem. The refused choice, quotient, host-function, and completeness claims remain outside the packet.
\end{proof}

\closureat{CompactUniformModulusNerveUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusNerveUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-nerve-carrier}, \autoref{thm:compact-uniform-modulus-nerve-namecert-obligations}, \autoref{thm:compact-uniform-modulus-nerve-vision-handoff}, and \autoref{thm:compact-uniform-modulus-nerve-no-choice-boundary} seed the finite cover-nerve, local-modulus, rational-fold, and uniform-modulus handoff surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, selected subcovers, countable choice, quotient cover equality, host function equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite nerve exactness, local-modulus replay, rational-fold stability, uniform-modulus handoff, and no-choice boundary proofs for BEDC.Derived.CompactUniformModulusNerveUp.}
  \constructivestory{A $\CompactUniformModulusNerveUp$ packet is generated from CompactMetric finite-cover rows, finite nerve rows, ContinuousMap local-modulus rows, Rat lower-bound comparison rows, rational-fold rows, UniformModulus handoff rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
