\chapter{A Concrete Naming Certificate for $\FiniteObservationTopologyUp$}
\label{ch:concrete-instances-finite-observation-topology-namecert}
\origin{ai}

The $\FiniteObservationTopologyUp$ packet realizes the observer-expansion window of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as a concrete NameCert carrier. It sits beside the finite observation substrate of \autoref{ch:concrete-instances-streamname-namecert}, the topology carrier of \autoref{ch:concrete-instances-topology-namecert}, and the hyperbolic observer-expansion packets referenced by the vision. The packet names a finite observation topology only through displayed windows, residue-boundary rows, transport, replay, provenance, and local naming; it does not install an ambient observer space, a completed boundary, or a host topological quotient.

\begin{definition}[Finite observation topology carrier]
\label{def:finite-observation-topology-carrier}
A $\FiniteObservationTopologyUp$ carrier is a finite $\BHist$ packet
$$
F=(S,T,E,B,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ observation source row, recording the finite windows through which an observer-expansion ledger may be inspected.
\item $T$ is the $\TopologyUp$ row whose open-incidence data are restricted to the finite observation surface exposed by $S$.
\item $E$ is the observer-expansion ledger row read from \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion}; it records the finite expansion coordinates without turning them into a global observer object.
\item $B$ is the residue-boundary row, naming the visible boundary of the finite observation window and the hidden remainder left outside that window.
\item $W$ is the finite topology-window ledger, listing which observations from $S$ are used to test the topology row $T$ and the expansion row $E$.
\item $H$ records componentwise $\hsame$ transport for observation source, topology row, expansion ledger, residue boundary, topology-window ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite observation-topology request through $S,T,E,B,W,H$.
\item $P$ is the $\Pkg$ provenance over $\FiniteObservationTopologyUp$, $\StreamNameUp$, $\TopologyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteObservationTopologyUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,T,E,B,W,H,C,P,N$. It has no coordinate for a completed observer manifold, a selected far-end point, a quotient topology on unseen observations, a global boundary compactification, or an unledgered hidden residue.
\end{definition}

\begin{theorem}[Finite observation topology NameCert obligations]
\label{thm:finite-observation-topology-namecert-obligations}
For every accepted $\FiniteObservationTopologyUp$ carrier $F=(S,T,E,B,W,H,C,P,N)$, the local $\NameCert_{\FiniteObservationTopologyUp}$ surface consists exactly of finite observation source exposure through $S$; topology-row exposure through $T$; observer-expansion ledger exposure through $E$; residue-boundary exposure through $B$; finite topology-window exposure through $W$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$. Lean follow-up requires \texttt{Nontrivial} and \texttt{FieldFaithful} instances before the corresponding TasteGate surface can be checked.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:finite-observation-topology-carrier}. The public observation-topology rows are exactly $S,T,E,B,W,H,C,P,N$.

The topology row is finite-facing. It can be inspected only through the windows listed by $S$ and $W$, while $E$ records the observer-expansion ledger and $B$ records the residue boundary left outside the visible window.

Transport and replay preserve those rows componentwise. Provenance and local naming then bind the same finite observation surface as a $\FiniteObservationTopologyUp$ certificate. Since no completed observer space, selected far-end point, quotient topology, global compactification, or unledgered residue row occurs in the carrier, the listed rows exhaust the NameCert obligations.
\end{proof}

\falsifiablePrediction{No accepted $\FiniteObservationTopologyUp$ consumer can cite observer-expansion topology unless it first exposes the StreamName observation source, topology row, observer-expansion ledger, residue-boundary row, finite topology-window ledger, transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows.}

\independenceWitness{finiteobservationresidueboundary, sierpinski-space, topology, streamname: no listed sibling is bijective with $\FiniteObservationTopologyUp$ because this carrier alone binds finite observation windows, observer-expansion ledger rows, residue-boundary data, topology-window tests, transport, replay, provenance, and local NameCert rows into one accepted packet.}

\closureat{FiniteObservationTopologyUp}{seedStr}

\begin{closurestatus}{\FiniteObservationTopologyUp}
  \constructivestory{A $\FiniteObservationTopologyUp$ packet is generated from StreamName finite observation windows, a Topology row, an observer-expansion ledger, a residue-boundary row, finite topology-window tests, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-observation-topology-carrier} and \autoref{thm:finite-observation-topology-namecert-obligations} seed the finite observer-expansion topology surface over observation, topology, residue-boundary, and provenance rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a completed observer space, a selected far-end point, quotient topology on unseen observations, global boundary compactification, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked finite observation source exposure, topology-window exactness, observer-expansion ledger stability, residue-boundary nonescape, transport, replay, provenance, local naming, and Nontrivial and FieldFaithful instances for BEDC.Derived.FiniteObservationTopologyUp.}
\end{closurestatus}
