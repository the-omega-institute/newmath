\chapter{A Concrete Naming Certificate for $\WarsawCircleUp$}
\label{ch:concrete-instances-warsaw-circle-namecert}
\origin{human}

Following classical continuum theory, the $\WarsawCircleUp$ packet records the Warsaw circle as a compact metric continuum assembled from the topologist sine curve, a closing arc, and the displayed topology and real-coordinate carriers already present in the BEDC library. It sits beside the topologist sine curve of \autoref{ch:concrete-instances-topologist-sine-curve-namecert}, the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the topology carrier of \autoref{ch:concrete-instances-topology-namecert}, the real-coordinate carrier of \autoref{ch:concrete-instances-real-namecert}, and the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names the finite row surface by which a continuum-theory consumer reads the Warsaw circle; it does not assert planar embedding uniqueness, local connectedness, path connectedness, selected parametrizations, quotient equality of point names, or a Lean-checked standard bridge.

\begin{definition}[Warsaw circle carrier]
\label{def:warsaw-circle-carrier}
A $\WarsawCircleUp$ carrier is a finite $\BHist$ packet
$$
W=(S,A,T,M,R,F,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $S$ is the $\TopologistSineCurveUp$ spine row naming the displayed sine-curve component.
\item $A$ is the finite closing-arc row recording the added compact connector between the two endpoint sides of the displayed spine.
\item $T$ is the $\TopologyUp$ subspace-topology row on the assembled carrier.
\item $M$ is the $\CompactMetricUp$ metric row for the closed bounded carrier surface.
\item $R$ is the $\RealUp$ coordinate row used by the planar point names and endpoint witnesses.
\item $F$ is the $\ContinuousMapUp$ inclusion-and-attachment row recording the displayed continuous maps from the sine spine and the closing arc into the assembled carrier.
\item $H$ records componentwise $\hsame$ transport for the spine, arc, topology, compact metric, real coordinate, continuous-map, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Warsaw-circle request through the sine-curve spine, closing arc, topology, compact metric, real-coordinate, and continuous-map rows.
\item $P$ is the $\Pkg$ provenance over $\WarsawCircleUp$, $\TopologistSineCurveUp$, $\CompactMetricUp$, $\TopologyUp$, $\RealUp$, $\ContinuousMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\WarsawCircleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected parametrization of the continuum, planar embedding uniqueness, local connectedness, path connectedness, quotient equality of point names, or an ambient theorem of continuum theory.
\end{definition}

\begin{theorem}[Warsaw circle NameCert obligations]
\label{thm:warsaw-circle-namecert-obligations}
For every accepted $\WarsawCircleUp$ carrier $W=(S,A,T,M,R,F,H,C,P,N)$, the local $\NameCert_{\WarsawCircleUp}$ surface consists exactly of sine-curve spine exposure through $S$, closing-arc exposure through $A$, topology exposure through $T$, compact metric exposure through $M$, real-coordinate exposure through $R$, continuous attachment exposure through $F$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. A consumer therefore reads the Warsaw circle only through the finite route
$$
S,A,T,M,R,F \longmapsto H,C,P,N .
$$
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:warsaw-circle-carrier}. The sine-curve row $S$ and closing-arc row $A$ display the two geometric pieces before any assembled continuum read is public.

The topology row $T$, compact metric row $M$, real-coordinate row $R$, and continuous attachment row $F$ then expose the carrier surface used by a continuum-theory consumer. These rows are the only rows available before structural replay.

Finally replay $H,C,P,N$. Transport, continuation replay, provenance, and local naming preserve the same finite packet, so no selected parametrization, embedding uniqueness theorem, local connectedness claim, path connectedness claim, quotient equality of point names, or ambient continuum theorem enters the NameCert surface.
\end{proof}

\begin{theorem}[Warsaw circle sine-curve closure handoff]
\label{thm:warsaw-circle-sine-curve-closure-handoff}
Every continuum-facing read from an accepted $\WarsawCircleUp$ carrier factors through the $\TopologistSineCurveUp$ spine row $S$ before it may consume the closing arc $A$, topology row $T$, compact metric row $M$, real-coordinate row $R$, and continuous attachment row $F$. The handoff from the sine-curve carrier to the Warsaw-circle carrier is therefore the displayed finite route
$$
\TopologistSineCurveUp \longmapsto A,T,M,R,F,H,C,P,N ,
$$
not a new proof of local connectedness, path connectedness, selected planar parametrization, embedding uniqueness, quotient equality, or ambient continuum classification.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:warsaw-circle-carrier}. Its first row is the displayed $\TopologistSineCurveUp$ spine, so any accepted Warsaw-circle read exposes the inherited sine-curve component before the closing arc or assembled surface is available.

Apply the NameCert obligations of \autoref{thm:warsaw-circle-namecert-obligations}. They place the closing arc, topology, compact metric, real-coordinate, and continuous-map rows downstream of that displayed spine and upstream of the structural rows.

The handoff is therefore finite and carrier-local. Since $H,C,P,N$ only transport, replay, source, and name the same packet, the route cannot be reread as local connectedness, path connectedness, selected parametrization, planar embedding uniqueness, quotient equality of point names, or a general continuum-classification theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\WarsawCircleUp$ consumer must display the $\TopologistSineCurveUp$ spine row, closing-arc row, $\TopologyUp$ row, $\CompactMetricUp$ row, $\RealUp$ coordinate row, $\ContinuousMapUp$ attachment row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before the Warsaw-circle certificate can be consumed.}

\independenceWitness{topologist-sine-curve, compactmetric, topology, real, continuousmap: $\WarsawCircleUp$ is not bijective to any listed sibling because it carries the joint sine spine, closing arc, topology, compact metric, real-coordinate, continuous-attachment, transport, replay, provenance, and local naming rows. The sine-curve sibling lacks the closing arc and assembled compact-metric carrier; the compact metric sibling lacks the sine-spine and attachment rows; the topology sibling lacks compact-metric and continuum rows; the real sibling supplies only coordinate names; and the continuous-map sibling records maps without the named Warsaw-circle carrier.}

\closureat{WarsawCircleUp}{seedStr}

\begin{closurestatus}{\WarsawCircleUp}
  \constructivestory{A $\WarsawCircleUp$ packet is generated from a topologist-sine-curve spine, a finite closing-arc row, topology and compact metric rows, real-coordinate rows, continuous attachment rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:warsaw-circle-carrier}, \autoref{thm:warsaw-circle-namecert-obligations}, and \autoref{thm:warsaw-circle-sine-curve-closure-handoff} seed the finite Warsaw-circle carrier over the existing sine-curve, topology, compact metric, real-coordinate, and continuous-map anchors.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close local connectedness, path connectedness, planar embedding uniqueness, selected parametrizations, quotient equality of point names, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sine-spine exposure, closing-arc exposure, topology and compact metric routing, real-coordinate exposure, continuous attachment routing, structural nonescape, and local naming for BEDC.Derived.WarsawCircleUp.}
\end{closurestatus}
