\chapter{A Concrete Naming Certificate for $\IntermediateValueRealSealUp$}
\label{ch:concrete-instances-intermediate-value-real-seal-namecert}
\origin{human}

Following Bolzano and Bishop--Bridges constructive analysis, the $\IntermediateValueRealSealUp$ packet records the finite handoff from displayed intermediate-value bisection data to the terminal $\RealUp$ seal. It sits after the constructive intermediate-value surface of \autoref{ch:concrete-instances-intermediatevalue-namecert}, the bisection route of \autoref{ch:concrete-instances-intermediate-value-bisection-namecert}, the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite sign-change route into a real seal; it does not claim unrestricted intermediate value, selected roots, real trichotomy, quotient real equality, countable choice, or ambient completeness.

\begin{definition}[IntermediateValueRealSeal carrier]
\label{def:intermediate-value-real-seal-carrier}
An $\IntermediateValueRealSealUp$ carrier is a finite $\BHist$ packet
$$
Z=(I,B,A,S,W,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the located interval endpoint row, exposing the lower and upper rational endpoint cells and their located-order witness.
\item $B$ is the $\IntermediateValueBisectionUp$ branch row, recording the retained finite bisection prefix and the interval chosen at the requested tolerance.
\item $A$ is the intermediate-value source row, tying the branch to the displayed function, endpoint signs, and local continuity budget.
\item $S$ is the sign-change ledger, containing the initial endpoint signs and the transported midpoint signs used by $B$.
\item $W$ is the $\StreamNameUp$ window surface for the retained endpoint names and the bisection readback window.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from $W$ and the displayed dyadic tolerance request.
\item $R$ is the terminal $\RealUp$ seal row exposed only after $Q$ has been read.
\item $H$ records componentwise $\hsame$ transport for $I,B,A,S,W,Q,R$ and the structural rows.
\item $C$ records displayed $\Cont$ replay from endpoint inspection through bisection retention, stream windows, regular readback, and real sealing.
\item $P$ is the $\Pkg$ provenance over $\IntermediateValueRealSealUp$, $\IntermediateValueBisectionUp$, $\IntermediateValueUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\IntermediateValueRealSealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for an unrestricted intermediate-value principle, a selected root, quotient equality of real names, real trichotomy, countable choice, a completed nested intersection, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[IntermediateValueRealSeal obligations]
\label{thm:intermediate-value-real-seal-obligations}
For every accepted $\IntermediateValueRealSealUp$ carrier $Z=(I,B,A,S,W,Q,R,H,C,P,N)$, the local $\NameCert_{\IntermediateValueRealSealUp}$ surface consists exactly of located endpoint admission, intermediate-value source exposure, sign-change ledger admission, bisection branch retention, $\StreamNameUp$ window admission, $\RegSeqRatUp$ readback admission, terminal $\RealUp$ seal admission, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored outside $I,B,A,S,W,Q,R,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:intermediate-value-real-seal-carrier}. The endpoint, source, branch, sign, window, readback, seal, transport, replay, provenance, and naming rows are the whole visible packet.

Read the analytic route first. The located endpoints in $I$ and the source row $A$ provide the finite sign-change request, while the sign ledger $S$ and bisection row $B$ record the retained branch at the requested tolerance.

The Real-facing handoff is then forced through $W,Q,R$. The stream windows expose only finite endpoint observations, the regular rational readback seals those observations as $Q$, and the terminal real-name row $R$ is available only after that readback.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier contains no other analytic coordinate, the listed obligations exhaust the local certificate and the excluded root-selection, quotient, trichotomy, choice, and completeness principles remain unavailable.
\end{proof}

\begin{theorem}[IntermediateValueRealSeal route]
\label{thm:intermediate-value-real-seal-route}
Every $\RealUp$ consumer of an accepted $\IntermediateValueRealSealUp$ packet factors through the ordered finite route
$$
\IntermediateValueUp,\IntermediateValueBisectionUp,\StreamNameUp,\RegSeqRatUp,\RealUp
\longmapsto
\BHist,\hsame,\Cont,\Pkg,\NameCert .
$$
The consumer receives the terminal real seal only as the endpoint of the displayed bisection, window, and regular-readback chain. It receives no unrestricted intermediate-value theorem, selected root, real trichotomy, quotient real equality, countable-choice selector, hidden completeness principle, or independent real representative.
\end{theorem}
\begin{proof}
Begin with the source and branch rows. The intermediate-value row $A$ supplies only the displayed sign-change and continuity data, and the bisection row $B$ selects only the retained finite branch justified by the sign ledger $S$.

Next read the finite observation surface. The stream row $W$ records the windows through which the retained endpoints are inspected, and the regular rational row $Q$ is the only readback exported from those windows.

Now seal the Real-facing endpoint. The row $R$ is downstream of $Q$, so a $\RealUp$ consumer cannot bypass the bisection branch or the regular readback by reading a host root, quotient stream, selected representative, or ambient completeness witness.

The structural rows $H,C,P,N$ preserve and name that same route. Composing these projections gives exactly the ordered handoff in the statement and no further real-facing coordinate.
\end{proof}

\closureat{IntermediateValueRealSealUp}{seedStr}

\begin{closurestatus}{\IntermediateValueRealSealUp}
  \constructivestory{An $\IntermediateValueRealSealUp$ packet is generated from located interval endpoint rows, intermediate-value source rows, a sign-change ledger, a bisection branch row, StreamName windows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:intermediate-value-real-seal-carrier}, \autoref{thm:intermediate-value-real-seal-obligations}, and \autoref{thm:intermediate-value-real-seal-route} seed the finite route from bisection-style intermediate-value data through regular rational readback to a terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close unrestricted intermediate value, selected roots, real trichotomy, quotient real equality, countable choice, completed nested intersections, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source exposure, sign-ledger retention, bisection branch routing, StreamName window admission, RegSeqRat readback, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.IntermediateValueRealSealUp.}
\end{closurestatus}
