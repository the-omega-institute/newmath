\chapter{A Concrete Naming Certificate for $\FiniteContractionOrbitErrorUp$}
\label{ch:concrete-instances-finite-contraction-orbit-error-namecert}
\origin{human}

Following Banach fixed-point theory and Bishop--Bridges constructive analysis, a $\FiniteContractionOrbitErrorUp$ packet records the finite Picard-orbit error budget used before a contraction route is sealed for Real-facing consumers. It sits beside the contraction data of \autoref{ch:concrete-instances-banach-contraction-namecert}, the Cauchy orbit route of \autoref{ch:concrete-instances-cauchy-contraction-orbit-namecert}, and the iterator packet of \autoref{ch:concrete-instances-contractive-cauchy-iterator-namecert}. The packet gives downstream fixed-point and Picard chapters a finite non-choice error certificate rather than an ambient completeness or selected-limit assertion.

\begin{definition}[Finite contraction orbit error carrier]
\label{def:finite-contraction-orbit-error-carrier}
A $\FiniteContractionOrbitErrorUp$ carrier is a finite $\BHist$ packet
$$
E=(K,X,F,W,D,Q,E_0,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the contraction-modulus and contractive-map row read from $\BanachContractionUp$ data.
\item $X$ is the metric carrier and starting-point row for the finite orbit window.
\item $F$ is the self-map replay row for the displayed finite Picard iterates.
\item $W$ is the finite iterate window that records the source iterate, terminal iterate, and observed step bound.
\item $D$ is the $\DyadicRatCoreUp$ budget row carrying the dyadic tolerance and finite arithmetic ledger.
\item $Q$ is the $\RegSeqRatUp$ and $\CauchyContractionOrbitUp$ route that turns the finite orbit window into a regular Cauchy readback.
\item $E_0$ is the $\RealUp$ seal row reached from the displayed dyadic and regular-sequence route.
\item $H$ records componentwise $\hsame$ transports for the contraction, orbit, budget, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from contraction data through the finite window, dyadic budget, Cauchy route, and Real seal.
\item $P$ is the $\Pkg$ provenance over $\FiniteContractionOrbitErrorUp$, $\BanachContractionUp$, $\CauchyContractionOrbitUp$, $\ContractiveCauchyIteratorUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteContractionOrbitErrorUp}$ row.
\end{enumerate}
The carrier has no coordinate for a selected fixed point, quotient orbit equality, host completeness proof, trichotomy comparison, or hidden Real error estimate.
\end{definition}
\leanstmt{BEDC.Derived.FiniteContractionOrbitErrorUp}

\begin{theorem}[Finite contraction orbit error NameCert obligations]
\label{thm:finite-contraction-orbit-error-namecert-obligations}
For every accepted $\FiniteContractionOrbitErrorUp$ carrier
$$
E=(K,X,F,W,D,Q,E_0,H,C,P,N),
$$
the local $\NameCert_{\FiniteContractionOrbitErrorUp}$ surface consists of exactly five finite rows of obligation:
\begin{enumerate}
\item carrier admission for the displayed packet $E=(K,X,F,W,D,Q,E_0,H,C,P,N)$;
\item contraction and finite-orbit exposure through $K,X,F,W$;
\item dyadic error-budget exactness through $D$;
\item $\RegSeqRatUp$ and $\CauchyContractionOrbitUp$ readback through $Q$ followed by the $\RealUp$ seal row $E_0$;
\item componentwise transport, replay, provenance, local naming, and non-escape through $H,C,P,N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-contraction-orbit-error-carrier}. The rows $K,X,F,W$ are the only contraction and finite-orbit rows, $D$ is the only dyadic budget row, $Q$ is the only Cauchy and regular-sequence readback row, and $E_0$ is the only Real-facing seal.

The structural rows $H,C,P,N$ preserve, replay, source, and name those displayed rows. Since the carrier contains no selected fixed point, quotient orbit equality, host completeness proof, trichotomy comparison, or hidden Real error estimate, the five listed duties exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite contraction orbit error Cauchy route]
\label{thm:finite-contraction-orbit-error-cauchy-route}
Every $\RealUp$ or completion-facing consumer of an accepted $\FiniteContractionOrbitErrorUp$ packet reaches the error budget only through the displayed route
$$
\BanachContractionUp,\ \CauchyContractionOrbitUp,\ \ContractiveCauchyIteratorUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp .
$$
In carrier coordinates this route is $K,X,F,W,D,Q,E_0,H,C,P,N$, and no consumer can substitute ambient completeness, a selected limit, quotient orbit equality, or an unrecorded Real comparison for that route.
\end{theorem}
\begin{proof}
Start with an accepted carrier $E=(K,X,F,W,D,Q,E_0,H,C,P,N)$. The contraction data are read through $K,X,F,W$, so the finite orbit window is visible before any error budget is exported.

Read the budget row next. The only error-budget coordinate is $D$, and it is a $\DyadicRatCoreUp$ ledger tied to the same finite iterate window.

Read the Cauchy and Real-facing rows. The row $Q$ carries the $\CauchyContractionOrbitUp$ and $\RegSeqRatUp$ handoff, and $E_0$ seals the displayed read through $\RealUp$.

Finally inspect $H,C,P,N$. These rows transport, replay, source, and name the same packet; they do not add selected limits, quotient equality, ambient completeness, or hidden comparison data. Therefore the error budget reaches Real-facing consumers only through the displayed Cauchy, regular-sequence, dyadic, and Real route.
\end{proof}

\falsifiablePrediction{A candidate packet that omits either the finite iterate window $W$, the dyadic budget row $D$, or the Cauchy readback row $Q$ cannot export the same $\RealUp$ seal row $E_0$ as an accepted $\FiniteContractionOrbitErrorUp$ packet.}

\independenceWitness{banach-contraction, cauchy-contraction-orbit, contractive-cauchy-iterator, dyadicratcore, regseqrat, real: none is bijective to $\FiniteContractionOrbitErrorUp$ because this packet jointly carries contraction data, a finite iterate window, dyadic error budget, regular Cauchy readback, Real seal, transport, replay, provenance, and local naming.}

\closureat{FiniteContractionOrbitErrorUp}{seedStr}

\begin{closurestatus}{\FiniteContractionOrbitErrorUp}
  \constructivestory{A $\FiniteContractionOrbitErrorUp$ packet is generated from contraction data, a metric starting row, self-map replay, a finite iterate window, a dyadic error budget, RegSeqRat and Cauchy contraction-orbit readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-contraction-orbit-error-carrier}, \autoref{thm:finite-contraction-orbit-error-namecert-obligations}, and \autoref{thm:finite-contraction-orbit-error-cauchy-route} seed the finite contraction-orbit error packet and its Cauchy, dyadic, regular-sequence, and Real-facing route.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Banach fixed-point existence, selected limits, ambient completeness, quotient orbit equality, trichotomy comparison, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction-window exposure, dyadic budget exactness, Cauchy and RegSeqRat handoff, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteContractionOrbitErrorUp.}
\end{closurestatus}
