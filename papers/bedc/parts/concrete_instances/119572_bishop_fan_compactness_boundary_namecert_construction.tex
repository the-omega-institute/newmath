\chapter{A Concrete Naming Certificate for $\BishopFanCompactnessBoundaryUp$}
\label{ch:concrete-instances-bishop-fan-compactness-boundary-namecert}
\origin{human}

Following Brouwer, Bishop, and Bridges, the $\BishopFanCompactnessBoundaryUp$ packet records the finite fan compactness boundary at which bar-depth data may feed compact-uniform-continuity consumers without becoming open-cover compactness. It sits after the fan theorem route of \autoref{ch:concrete-instances-fantheorem-namecert}, the finite fan theorem route of \autoref{ch:concrete-instances-finite-fan-theorem-namecert}, the compact metric source rows of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet also borders the compact fan handoff of \autoref{ch:concrete-instances-compact-uniform-bishop-fan-namecert}; its role is to expose the exact finite boundary consumed by L10 compact and continuous-map readers, not to supply selected infinite branches or ambient completeness.

\begin{definition}[Bishop fan compactness boundary carrier]
\label{def:bishop-fan-compactness-boundary-carrier}
A $\BishopFanCompactnessBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(F,D,V,K,Q,M,U,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\FanTheoremUp$ bar row naming the fan bar whose inspected prefixes are available to the packet.
\item $D$ is the fan-bar depth ledger, recording the finite depth bound at which the visible bar inspection stops.
\item $V$ is the finite prefix coverage row supplied by $\FiniteFanTheoremUp$; it lists exactly the prefixes covered below depth $D$.
\item $K$ is the $\CompactMetricUp$ source-probe row, exposing the compact finite-net observations that the fan prefixes are allowed to inspect.
\item $Q$ is the $\ContinuousMapUp$ pointwise-modulus window row, tying each displayed compact-source probe to a finite continuity read.
\item $M$ is the $\StreamNameUp$ and $\RegSeqRatUp$ readback window package used to inspect the prefix, compact-source, and pointwise-modulus rows without quotienting streams.
\item $U$ is the $\UniformModulusUp$ handoff row exported to compact-uniform consumers after $F,D,V,K,Q,M$ have been displayed.
\item $R$ is the $\RealUp$ seal boundary for the value and tolerance reads that enter $Q$ and $U$.
\item $H$ records componentwise $\hsame$ transport for the fan-bar, depth, prefix-coverage, compact-source, pointwise-modulus, window, uniform-handoff, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-uniform request through
$$
\FanTheoremUp,\FiniteFanTheoremUp,\CompactMetricUp,\ContinuousMapUp,\StreamNameUp,\RegSeqRatUp,\RealUp,\UniformModulusUp .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopFanCompactnessBoundaryUp$, $\FanTheoremUp$, $\FiniteFanTheoremUp$, $\BishopFanCompactUniformUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopFanCompactnessBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,D,V,K,Q,M,U,R,H,C,P,N$. It has no coordinate for arbitrary open covers, selected infinite branches, countable choice, quotient stream equality, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop fan compactness boundary NameCert obligations]
\label{thm:bishop-fan-compactness-boundary-namecert-obligations}
For every accepted $\BishopFanCompactnessBoundaryUp$ carrier
$$
B=(F,D,V,K,Q,M,U,R,H,C,P,N),
$$
the local $\NameCert_{\BishopFanCompactnessBoundaryUp}$ surface consists exactly of fan-bar admission for $F$, fan-bar depth admission for $D$, finite prefix coverage for $V$, compact-source probe admission for $K$, pointwise-modulus window exposure for $Q$, StreamName and RegSeqRat readback for $M$, uniform-modulus handoff for $U$, Real-seal boundary exposure for $R$, componentwise $\hsame$ transport for $H$, displayed $\Cont$ replay for $C$, $\Pkg$ provenance for $P$, and local naming for $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-fan-compactness-boundary-carrier}. The fan rows are $F,D,V$: the bar is named, the inspected depth is finite, and the covered prefixes are exactly those displayed by the finite fan theorem route.

Read the compact and continuity rows next. The compact-source probes $K$ are finite metric observations, while $Q$ records only pointwise-modulus windows tied to those probes.

The window and value rows are downstream. The row $M$ keeps the prefix, compact-source, and modulus reads inside finite StreamName and RegSeqRat windows, while $R$ seals the displayed value and tolerance reads as $\RealUp$ data.

Finally, $U,H,C,P,N$ package the uniform-modulus handoff, transport, replay, provenance, and local naming. Since the carrier has no coordinate for open-cover compactness, selected branches, countable choice, quotient streams, selected limits, or ambient completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop fan compactness boundary handoff]
\label{thm:bishop-fan-compactness-boundary-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\BishopFanCompactnessBoundaryUp$ packet must first read the finite route
$$
\FanTheoremUp,\FiniteFanTheoremUp,\CompactMetricUp,\ContinuousMapUp,\StreamNameUp,\RegSeqRatUp,\RealUp .
$$
The exported uniformity data are exactly the handoff row $U$ together with the transport, replay, provenance, and naming rows $H,C,P,N$. The route exports no open-cover compactness, selected infinite branch, countable-choice schedule, quotient stream equality, selected limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the fan boundary rows. The bar row $F$, depth ledger $D$, and prefix coverage row $V$ expose a finite inspection boundary before any compact-source read is allowed.

Apply the compact-source and pointwise-modulus rows. The compact probes $K$ and continuity windows $Q$ show which finite observations are being used by the downstream uniform-modulus consumer.

Pass through the stream, regular-rational, and Real rows $M,R$. These rows seal the inspected windows and value comparisons without selecting an infinite branch, quotienting a stream, or appealing to a selected limit.

The handoff row $U$ can now be exported through $H,C,P,N$. Because no host compactness or choice coordinate appears in the packet, every accepted consumer remains inside the finite boundary route stated above.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopFanCompactnessBoundaryUp$ read must expose fan-bar depth, finite prefix coverage, compact-source probes, pointwise-modulus windows, StreamName and RegSeqRat readback, a UniformModulus handoff, a Real-seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row before a compact-uniform consumer may read the packet.}

\independenceWitness{fan-theorem, finite-fan-theorem, compactmetric, continuousmap, uniformmodulus, streamname, regseqrat, real: $\BishopFanCompactnessBoundaryUp$ binds fan-bar depth, finite prefix coverage, compact-source probes, pointwise-modulus windows, finite readback, uniform handoff, Real sealing, transport, replay, provenance, and local naming in one boundary packet; each listed sibling lacks at least one of these local coordinates.}

\closureat{BishopFanCompactnessBoundaryUp}{seedStr}

\begin{closurestatus}{\BishopFanCompactnessBoundaryUp}
  \constructivestory{A $\BishopFanCompactnessBoundaryUp$ packet is generated from FanTheorem bar rows, finite fan-depth and prefix-coverage rows, CompactMetric source probes, ContinuousMap pointwise-modulus windows, StreamName and RegSeqRat readback, a UniformModulus handoff, a RealUp seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-fan-compactness-boundary-carrier}, \autoref{thm:bishop-fan-compactness-boundary-namecert-obligations}, and \autoref{thm:bishop-fan-compactness-boundary-handoff} seed the finite fan compactness boundary over bar depth, prefix coverage, compact probes, pointwise modulus windows, finite readback, Real sealing, and uniform-modulus handoff rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, selected infinite branches, countable choice, quotient streams, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\BishopFanCompactnessBoundaryUp$.}
  \upgradepath{Obligation closure requires checked fan-bar admission, depth and prefix coverage, compact-source probe exposure, pointwise-modulus window exposure, finite readback, uniform-modulus handoff, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.BishopFanCompactnessBoundaryUp.}
\end{closurestatus}
