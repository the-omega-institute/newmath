\chapter{A Concrete Naming Certificate for $\CauchyTailThresholdNormalizerUp$}
\label{ch:concrete-instances-cauchy-tail-threshold-normalizer-namecert}
\origin{ai}

The $\CauchyTailThresholdNormalizerUp$ packet is the finite Real and Completion carrier that turns a shared-tail threshold into a single normal-form row before any $\RealUp$ seal consumer may read a tail window. It sits between the shared tail data of \autoref{ch:concrete-instances-cauchy-shared-tail-modulus-namecert}, the seal-facing row of \autoref{ch:concrete-instances-cauchy-tail-modulus-seal-namecert}, the readback surfaces of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal consumers \autoref{ch:concrete-instances-realtailagreementseal-namecert} and \autoref{ch:concrete-instances-real-namecert}. Its role is to make the common finite threshold a displayed route object rather than a private parameter carried separately by each downstream seal.

\begin{definition}[Cauchy tail threshold normalizer carrier]
\label{def:cauchy-tail-threshold-normalizer-carrier}
A $\CauchyTailThresholdNormalizerUp$ carrier is a finite $\BHist$ packet
$$
T=(S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\CauchySharedTailModulusUp$ row naming the shared tail source whose threshold is being normalized.
\item $M$ is the $\CauchyTailModulusSealUp$ row that exposes the seal-facing modulus bound.
\item $\Theta$ is the normalized shared finite threshold row.
\item $W_0$ and $W_1$ are the two $\StreamNameUp$ window readbacks cut from the same threshold $\Theta$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger read on the same threshold.
\item $R$ is the paired $\RegSeqRatUp$ readback row attached to $W_0,W_1,D$.
\item $A$ is the $\RealTailAgreementSealUp$ row reached after the two threshold-indexed windows have been compared.
\item $E$ is the terminal $\RealUp$ consumer row available only after $S,M,\Theta,W_0,W_1,D,R,A$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the shared-tail, seal, threshold, window, tolerance, readback, agreement, Real, provenance, ledger, and naming rows.
\item $C$ stores displayed $\Cont$ replay from the shared-tail source through the seal row, normalized threshold, both window readbacks, tolerance ledger, regular readback, agreement seal, and Real consumer row.
\item $P$ is the $\Pkg$ provenance over $\CauchyTailThresholdNormalizerUp$, $\CauchySharedTailModulusUp$, $\CauchyTailModulusSealUp$, $\RealTailAgreementSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the route-exhaustion ledger recording that every seal-facing consumer has already read the same threshold $\Theta$.
\item $N$ is the local $\NameCert_{\CauchyTailThresholdNormalizerUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient streams, selected host limits, host real equality, trichotomy, ambient completeness, private side thresholds, or a second Real seal route.
\end{definition}

\begin{definition}[Cauchy tail threshold normalizer classifier]
\label{def:cauchy-tail-threshold-normalizer-classifier}
Two accepted $\CauchyTailThresholdNormalizerUp$ carriers classify together exactly when their shared-tail source, modulus seal, normalized threshold, two StreamName windows, dyadic tolerance ledger, RegSeqRat readback, RealTailAgreementSeal route, RealUp consumer row, componentwise transports, displayed continuation replay, provenance, route-exhaustion ledger, and local naming row agree componentwise over the same threshold $\Theta$. The classifier does not identify carriers by comparing quotient streams, by choosing host limits, by imposing host real equality, by invoking trichotomy, by importing ambient completeness, or by allowing each seal consumer to carry a private threshold.
\end{definition}
\leandef{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerClassifier}

\begin{theorem}[Cauchy tail threshold normalizer NameCert obligations]
\label{thm:cauchy-tail-threshold-normalizer-namecert-obligations}
For every accepted $\CauchyTailThresholdNormalizerUp$ carrier
$$
T=(S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N),
$$
the local $\NameCert_{\CauchyTailThresholdNormalizerUp}$ surface consists exactly of carrier admission, shared-tail source exposure through $S$, modulus-seal exposure through $M$, normalized threshold exposure through $\Theta$, paired StreamName window readback through $W_0,W_1$, dyadic tolerance exposure through $D$, RegSeqRat readback through $R$, RealTailAgreementSeal handoff through $A$, RealUp consumer exposure through $E$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, route-exhaustion ledgering through $L$, local naming through $N$, and non-escape for quotient streams, selected host limits, host real equality, trichotomy, ambient completeness, private side thresholds, and second Real seal routes.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-tail-threshold-normalizer-carrier}. The packet displays one shared-tail source, one seal-facing modulus row, one normalized threshold, two windows cut from that threshold, one dyadic tolerance ledger, one regular readback row, one agreement seal, one terminal Real consumer row, and five structural support rows.

The threshold row is read before any seal consumer. The source $S$ and seal row $M$ expose the tail data, then $\Theta$ fixes the finite threshold that both windows must use.

The readback rows are downstream of that threshold. The windows $W_0,W_1$, tolerance ledger $D$, and regular readback $R$ are all indexed by the same $\Theta$, so neither side can substitute a private finite window before the Real-facing handoff.

Finally $A,E,H,C,P,L,N$ package the route as a NameCert surface. The carrier lacks quotient streams, selected host limits, host real equality, trichotomy, ambient completeness, private side thresholds, and second Real seal routes, so the listed rows exhaust the local obligations.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerCarrier\_namecert\_obligations}

\begin{theorem}[Cauchy tail threshold normalizer route exhaustion]
\label{thm:cauchy-tail-threshold-normalizer-route-exhaustion}
Every accepted $\RealUp$ or $\RealTailAgreementSealUp$ consumer of a $\CauchyTailThresholdNormalizerUp$ carrier factors through the finite route
$$
S\longmapsto M\longmapsto\Theta\longmapsto(W_0,W_1,D,R)\longmapsto(A,E,H,C,P,L,N).
$$
In particular, no such consumer can read two incompatible finite tail windows through private thresholds.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerCarrier\_route\_exhaustion}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:cauchy-tail-threshold-normalizer-namecert-obligations}. They expose $S$ and $M$ before any Real-facing row appears.

Next read the normalized threshold $\Theta$. The two StreamName windows, dyadic ledger, and regular readback are attached to $\Theta$, so the threshold-indexed data rows are $W_0,W_1,D,R$ and not side-private alternatives.

The agreement and Real rows are therefore downstream of the same finite threshold. The rows $A,E,H,C,P,L,N$ transport, replay, source, ledger, and name the displayed route, and the carrier contains no coordinate for a second route around $\Theta$.
\end{proof}

\begin{theorem}[Cauchy tail threshold normalizer tail-window induction]
\label{thm:cauchy-tail-threshold-normalizer-tail-window-induction}
Every accepted $\CauchyTailThresholdNormalizerUp$ packet normalizes a threshold read by finite induction over the displayed $\StreamNameUp$ tail windows. The base window is the threshold row $\Theta$ exposed between $S,M$ and $W_0,W_1$; each successor window preserves the $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, route-exhaustion ledger, and local $\NameCert$ row.

Consequently every Real-facing consumer must pass through the same finite induction surface before reading a tail window. The induction exports no quotient stream equality, selected limit, trichotomy, countable choice, ambient Real completeness, private side threshold, or second Real seal route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerTailWindowInduction}
\begin{proof}
Project the carrier of \autoref{def:cauchy-tail-threshold-normalizer-carrier}. Its threshold row $\Theta$ is displayed before the two $\StreamNameUp$ windows, so the base step is the finite threshold read itself, not a hidden limit or quotient-stream representative.

For the successor step, use the componentwise transport row $H$. Moving from one displayed tail window to the next preserves the shared-tail source, seal row, threshold row, tolerance ledger, regular readback, agreement-seal handoff, Real consumer row, continuation replay, provenance, route ledger, and local naming row component by component.

The $\DyadicRatCoreUp$ and $\RegSeqRatUp$ rows supply the numerical readback discipline at each displayed window. The $\RealUp$ seal row is downstream of those rows, so no successor step can inspect a Real-facing value before the window, tolerance, and readback data have been presented.

Finally apply route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. Every accepted Real-facing consumer already factors through
$$
S\longmapsto M\longmapsto\Theta\longmapsto(W_0,W_1,D,R)\longmapsto(A,E,H,C,P,L,N),
$$
so the induction has no coordinate for quotient equality, selected limits, trichotomy, countable choice, ambient completeness, private side thresholds, or a second Real seal route.
\end{proof}

\begin{theorem}[Cauchy tail threshold normalizer shared-threshold uniqueness]
\label{thm:cauchy-tail-threshold-normalizer-shared-threshold-uniqueness}
Let $T$ and $T'$ be accepted $\CauchyTailThresholdNormalizerUp$ carriers read by $\RealUp$ or $\RealTailAgreementSealUp$ consumers. If the two consumers read the same shared-tail source row $S$ and the same seal-facing modulus row $M$ through classified normalizer packets, then both consumers expose the same normalized threshold row $\Theta$ before any $W_0,W_1,D,R,A,E$ row is available.

In particular, the classifier cannot admit two downstream seal reads with the same $S,M$ source data and different normalized tail thresholds.
\end{theorem}
\begin{proof}
Project both accepted carriers through \autoref{def:cauchy-tail-threshold-normalizer-carrier}. In each carrier, the source and seal rows $S,M$ are followed by a single normalized threshold row before any window, tolerance, readback, agreement, or Real-seal row appears.

Apply route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. A $\RealUp$ or $\RealTailAgreementSealUp$ consumer has no route around $\Theta$, so the downstream rows $W_0,W_1,D,R,A,E$ can be read only after the normalized threshold has been displayed.

Finally use the classifier of \autoref{def:cauchy-tail-threshold-normalizer-classifier}. Classified packets are compared componentwise on the shared-tail source, seal row, normalized threshold, two windows, dyadic tolerance ledger, regular readback, agreement seal, and Real row. Since the two reads agree on $S$ and $M$ inside that classified normalizer surface, the threshold component exposed before the window rows is the same $\Theta$ in both reads.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerSharedThresholdUniqueness}

\begin{theorem}[Cauchy tail threshold normalizer window cofinality]
\label{thm:cauchy-tail-threshold-normalizer-window-cofinality}
Let $T$ be an accepted $\CauchyTailThresholdNormalizerUp$ packet with shared source row $S$, seal row $M$, and normalized threshold row $\Theta$. Every later finite $\StreamNameUp$ window read by a $\RealTailAgreementSealUp$ or $\RealUp$ consumer factors through a finite successor of the same $\Theta$. No such consumer can introduce a private later threshold outside the displayed normalizer route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-tail-threshold-normalizer-carrier}. The rows $S,M,\Theta$ appear before $W_0,W_1,D,R,A,E$, so every seal-facing window read is downstream of the normalized threshold already displayed by the packet.

Use route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. A $\RealTailAgreementSealUp$ or $\RealUp$ consumer has no route around
$$
S\longmapsto M\longmapsto\Theta\longmapsto(W_0,W_1,D,R)\longmapsto(A,E,H,C,P,L,N),
$$
and therefore any later finite StreamName window must be a successor window in that same finite route.

Apply shared-threshold uniqueness from \autoref{thm:cauchy-tail-threshold-normalizer-shared-threshold-uniqueness}. If a later consumer tried to introduce a private threshold while keeping the same $S,M$ rows, the classified normalizer surface would expose two thresholds before the downstream window rows, contradicting the uniqueness row.

Finally transport the displayed successor window componentwise through $H$ and replay it through $C$. The rows $P,L,N$ keep the provenance, route-exhaustion ledger, and local NameCert attached to the same $\Theta$, so the consumer receives a cofinal finite-window route rather than a hidden side threshold.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerWindowCofinality}

\begin{theorem}[Cauchy tail threshold normalizer public Real-seal route]
\label{thm:cauchy-tail-threshold-normalizer-real-seal-public-route}
Every public $\RealUp$ seal read of an accepted $\CauchyTailThresholdNormalizerUp$ carrier
$$
T=(S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N)
$$
passes through the same displayed route
$$
S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N .
$$
It cannot use quotient streams, selected host limits, host equality of real names, trichotomy, ambient completeness, private side thresholds, or a second seal route.
\end{theorem}
\begin{proof}
Start with route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. It already forces every $\RealUp$ or $\RealTailAgreementSealUp$ consumer through the finite route from $S,M$ to $\Theta$, then through $W_0,W_1,D,R$, and finally through $A,E,H,C,P,L,N$.

Apply shared-threshold uniqueness from \autoref{thm:cauchy-tail-threshold-normalizer-shared-threshold-uniqueness}. A public seal read with the same shared source $S$ and modulus seal $M$ cannot split into a private side threshold before the window and readback rows are exposed.

Use window cofinality from \autoref{thm:cauchy-tail-threshold-normalizer-window-cofinality}. Every later finite $\StreamNameUp$ window remains downstream of a finite successor of the same $\Theta$, so the public route has no opening for quotient streams, selected host limits, host equality, trichotomy, or ambient completeness.

Finally read the structural rows. Transport, replay, provenance, route ledger, and local naming preserve the same carrier surface, and no listed row contains a second seal route. Therefore every public $\RealUp$ seal read factors through the displayed route and nothing else.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerRealSealPublicRoute}

\begin{theorem}[Cauchy tail threshold normalizer Real-facing determinacy]
\label{thm:cauchy-tail-threshold-normalizer-real-facing-determinacy}
Let two accepted $\RealUp$ or $\RealTailAgreementSealUp$ consumers classify through the same $\CauchyTailThresholdNormalizerUp$ source row $S$ and seal row $M$. Then their $\StreamNameUp$ windows $W_0,W_1$, $\DyadicRatCoreUp$ tolerance ledger $D$, $\RegSeqRatUp$ readback $R$, and $\RealTailAgreementSealUp$ row $A$ are all read downstream of the same normalized threshold row $\Theta$.

Equivalently, the Real-facing classifier surface exposes one finite route
$$
S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N
$$
before either consumer can inspect a tail window, tolerance ledger, regular readback, or agreement-seal row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-tail-threshold-normalizer-carrier}. The normalized threshold row $\Theta$ is a displayed coordinate between the shared source and seal rows $S,M$ and the downstream $W_0,W_1,D,R,A,E$ rows.

Apply route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. Any accepted $\RealUp$ or $\RealTailAgreementSealUp$ consumer must pass through $\Theta$ before reading the two StreamName windows, the dyadic tolerance ledger, the RegSeqRat readback, or the agreement-seal row.

Apply shared-threshold uniqueness from \autoref{thm:cauchy-tail-threshold-normalizer-shared-threshold-uniqueness}. Since the two consumers classify through the same $S,M$ normalizer source and seal rows, the threshold exposed before the window rows is the same normalized row $\Theta$.

Finally use the classifier of \autoref{def:cauchy-tail-threshold-normalizer-classifier}. It compares the downstream rows componentwise over the shared threshold surface, so $W_0,W_1,D,R,A$ are determined as reads below that same $\Theta$ rather than as private tail windows or side thresholds.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerRealFacingDeterminacy}

\begin{theorem}[Cauchy tail threshold normalizer window scope]
\label{thm:cauchy-tail-threshold-normalizer-window-scope}
Every $\RealTailAgreementSealUp$ or $\RealUp$ consumer that classifies through a $\CauchyTailThresholdNormalizerUp$ packet must expose
$$
S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N
$$
in that order before inspecting a tail-window row. Consequently a private side threshold or second seal route is outside the classifier surface.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerWindowScope}
\begin{proof}
Project the carrier of \autoref{def:cauchy-tail-threshold-normalizer-carrier}. The displayed coordinates are exactly $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$, so any classified read begins with the shared-tail source, seal row, and normalized threshold before it reaches a window.

Apply route exhaustion and shared-threshold uniqueness. The route-exhaustion theorem forces every $\RealTailAgreementSealUp$ or $\RealUp$ consumer through the same threshold path, and uniqueness prevents two classified consumers with the same $S,M$ rows from introducing separate threshold coordinates.

Use window cofinality and Real-facing determinacy next. They place $W_0,W_1,D,R,A,E$ downstream of the same $\Theta$, so the two windows, dyadic ledger, regular readback, agreement row, and Real row are not available as side reads.

Finally inspect the classifier surface. It has no coordinate for a private threshold or a second seal route, and the structural rows $H,C,P,L,N$ only transport, replay, source, ledger, and name the same displayed packet. Thus any consumer that tries to inspect a tail-window row before the ordered route is outside the accepted classifier.
\end{proof}

\begin{theorem}[Cauchy tail threshold composition scope]
\label{thm:cauchy-tail-threshold-composition-scope}
Every accepted $\CauchyTailThresholdNormalizerUp$ carrier
$$
T=(S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N)
$$
composes the shared-tail source $S$, modulus seal $M$, normalized threshold $\Theta$, StreamName windows $W_0,W_1$, DyadicRatCore tolerance row $D$, RegSeqRat readback $R$, RealTailAgreementSeal row $A$, RealUp row $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, route-exhaustion ledger $L$, and local $\NameCert$ row $N$ into one finite route. No Real-facing consumer may bypass that route with private thresholds, quotient streams, selected host limits, trichotomy, host real equality, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdCompositionScope}
\begin{proof}
Project the carrier of \autoref{def:cauchy-tail-threshold-normalizer-carrier}. The displayed coordinates are exactly $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$, so the route starts at the shared-tail and seal rows before any Real-facing read is exposed.

Apply the NameCert obligations of \autoref{thm:cauchy-tail-threshold-normalizer-namecert-obligations}. Those obligations make each analytic row available only as part of the same local certificate surface, with the normalized threshold placed before the paired StreamName windows, tolerance ledger, regular readback, agreement seal, and Real row.

Apply route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. Every accepted $\RealUp$ or $\RealTailAgreementSealUp$ consumer already factors through the displayed threshold route, so a private threshold, quotient stream, selected host limit, trichotomy, host real equality, or ambient completeness principle has no public coordinate to occupy.

Finally use componentwise $\hsame$ transport and displayed $\Cont$ replay. They preserve the same route under transport and continuation, while $P,L,N$ source, ledger, and name the packet without adding a second Real-facing path.
\end{proof}

\begin{theorem}[Cauchy tail threshold normalizer noninternalization]
\label{thm:cauchy-tail-threshold-normalizer-noninternalization}
The $\CauchyTailThresholdNormalizerUp$ packet does not internalize quotient streams, selected host limits, host real equality, trichotomy, ambient completeness, or hidden threshold choice. Its public surface is exactly the finite shared-threshold route displayed by
$$
S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N.
$$
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerCarrier\_noninternalization}
\begin{proof}
Inspect the carrier of \autoref{def:cauchy-tail-threshold-normalizer-carrier}. Its data coordinates are the shared-tail source, seal row, normalized threshold, two windows, dyadic ledger, regular readback, agreement seal, and Real consumer row.

Inspect the structural coordinates next. Transport, continuation, provenance, route-exhaustion ledgering, and local naming preserve the displayed route; none supplies quotient equality, a host limit, host real equality, trichotomy, completeness, or hidden threshold selection.

Apply route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. Every downstream seal consumer already factors through $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$, so there is no remaining public coordinate in which an excluded object could be read.
\end{proof}

\begin{theorem}[Cauchy tail threshold normalizer obligation ledger]
\label{thm:cauchy-tail-threshold-normalizer-obligation-ledger}
For every accepted $\CauchyTailThresholdNormalizerUp$ packet
$$
T=(S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N),
$$
the Real-facing route exposes exactly the shared-tail source $S$, modulus seal $M$, normalized threshold $\Theta$, two $\StreamNameUp$ windows $W_0,W_1$, $\DyadicRatCoreUp$ ledger $D$, $\RegSeqRatUp$ readback $R$, $\RealTailAgreementSealUp$ row $A$, $\RealUp$ row $E$, componentwise transport $H$, displayed replay $C$, provenance $P$, route-exhaustion ledger $L$, and local $\NameCert$ row $N$ before any Real-facing consumer reads a tail window.

No accepted route adds quotient-stream equality, selected host limits, host real equality, trichotomy, ambient completeness, a private side threshold, or a second Real seal route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerObligationLedger}
\begin{proof}
Project the carrier of \autoref{def:cauchy-tail-threshold-normalizer-carrier} and its classifier in \autoref{def:cauchy-tail-threshold-normalizer-classifier}. The exposed coordinates are precisely $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$, compared componentwise over the same threshold surface.

Apply the NameCert obligations of \autoref{thm:cauchy-tail-threshold-normalizer-namecert-obligations} and route exhaustion of \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. They place the shared-tail source and modulus seal before $\Theta$, and place the two StreamName windows, dyadic ledger, RegSeqRat readback, RealTailAgreementSeal row, and Real row downstream of that threshold.

Apply shared-threshold uniqueness and Real-facing determinacy from \autoref{thm:cauchy-tail-threshold-normalizer-shared-threshold-uniqueness} and \autoref{thm:cauchy-tail-threshold-normalizer-real-facing-determinacy}. These rows prevent two Real-facing consumers with the same source and seal rows from reading private threshold windows or incompatible downstream readbacks.

Finally use noninternalization, \autoref{thm:cauchy-tail-threshold-normalizer-noninternalization}. The excluded quotient, limit, equality, trichotomy, completeness, private-threshold, and second-seal objects have no carrier coordinate, so the listed rows form the complete L10 obligation ledger before any Real-facing tail-window read.
\end{proof}

\begin{theorem}[Cauchy tail threshold encoded obligation readback]
\label{thm:cauchy-tail-threshold-normalizer-encoded-obligation-readback}
The carrier row of \autoref{def:cauchy-tail-threshold-normalizer-carrier}, the classifier row of \autoref{def:cauchy-tail-threshold-normalizer-classifier}, the route-exhaustion row of \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}, the shared-threshold uniqueness row of \autoref{thm:cauchy-tail-threshold-normalizer-shared-threshold-uniqueness}, and the Real-facing determinacy row of \autoref{thm:cauchy-tail-threshold-normalizer-real-facing-determinacy} together provide the full finite obligation readback needed for the next Lean target over $\mathrm{BEDC.Derived.CauchyTailThresholdNormalizerUp}$. The readback exports carrier admission, classifier comparison, route coverage, unique shared-threshold exposure, and Real-facing downstream determinacy, and it exports no quotient-stream equality, selected host limit, host real equality, trichotomy, ambient completeness, private side threshold, or second Real seal route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerEncodedObligationReadback}
\begin{proof}
Read the carrier and classifier first. The carrier theorem displays the rows $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$, while the classifier requires those rows to be compared componentwise over the same threshold surface.

Next apply route exhaustion. It forces every $\RealUp$ and $\RealTailAgreementSealUp$ consumer through
$$
S\longmapsto M\longmapsto\Theta\longmapsto(W_0,W_1,D,R)\longmapsto(A,E,H,C,P,L,N),
$$
so the encoded target has a finite route to read rather than an ambient completion premise.

The constructor induction is over threshold-window rows. The base constructor reads $\Theta$ after the shared source and modulus rows; the successor constructors preserve the two StreamName windows, dyadic tolerance ledger, regular readback, agreement seal, Real row, transport, replay, provenance, route ledger, and local naming.

Finally use shared-threshold uniqueness and Real-facing determinacy. They prevent two classified reads with the same $S,M$ rows from splitting into private thresholds, and they force $W_0,W_1,D,R,A$ to remain downstream of the same $\Theta$. These cited rows are exactly the finite obligation surface required by the Lean target.
\end{proof}

\falsifiablePrediction{Removing the normalized shared-tail row permits two RealUp seal consumers to read incompatible finite windows through private thresholds.}

\independenceWitness{cauchysharedtailmodulus, cauchytailmodulusseal, realtailagreementseal, regseqrat, real: not bijective because the normalizer carries the common threshold, both window readbacks, both seal-facing routes, and the route-exhaustion ledger simultaneously.}

Lean follow-up provides explicit $\mathsf{Nontrivial}\ \CauchyTailThresholdNormalizerUp$ and $\mathsf{FieldFaithful}\ \CauchyTailThresholdNormalizerUp$ instances in \texttt{lean4/BEDC/Derived/CauchyTailThresholdNormalizerUp/TasteGate.lean}.

\begin{theorem}[Cauchy tail threshold normalizer obligation scope]
\label{thm:cauchy-tail-threshold-normalizer-obligation-scope}
The finite obligation surface of $\CauchyTailThresholdNormalizerUp$ is exactly the carrier row of \autoref{def:cauchy-tail-threshold-normalizer-carrier}, the classifier row of \autoref{def:cauchy-tail-threshold-normalizer-classifier}, route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}, tail-window induction from \autoref{thm:cauchy-tail-threshold-normalizer-tail-window-induction}, shared-threshold uniqueness from \autoref{thm:cauchy-tail-threshold-normalizer-shared-threshold-uniqueness}, window cofinality from \autoref{thm:cauchy-tail-threshold-normalizer-window-cofinality}, Real-facing determinacy from \autoref{thm:cauchy-tail-threshold-normalizer-real-facing-determinacy}, composition scope from \autoref{thm:cauchy-tail-threshold-composition-scope}, noninternalization from \autoref{thm:cauchy-tail-threshold-normalizer-noninternalization}, and the obligation ledger of \autoref{thm:cauchy-tail-threshold-normalizer-obligation-ledger}. These rows bind the obligationClosure surface over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealTailAgreementSealUp$, and $\RealUp$ without exporting quotient streams, selected host limits, host real equality, trichotomy, ambient completeness, private side thresholds, or a second Real seal route.
\end{theorem}
\begin{proof}
Project the carrier and classifier first. They display the finite packet $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$ and require classified reads to compare those rows componentwise over the same threshold surface.

Use route exhaustion, tail-window induction, shared-threshold uniqueness, window cofinality, and Real-facing determinacy to bind the analytic rows. These theorems force every $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance row, $\RealTailAgreementSealUp$ handoff, and $\RealUp$ seal read to pass through the same normalized threshold $\Theta$.

Apply composition scope and noninternalization next. They show that the displayed packet composes as one finite route and that no quotient stream, selected host limit, host real equality, trichotomy, ambient completeness, private side threshold, or second seal row can be projected from it.

Finally read the obligation ledger. It names exactly the same rows as the finite L10 surface, so the chapter reaches obligationClosure on the paper axis while leaving later verification to kernel-scoped dependency wording over those rows.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerObligationScope}

\begin{theorem}[Cauchy tail threshold normalizer uniform-threshold induction]
\label{thm:cauchy-tail-threshold-normalizer-uniform-threshold-induction}
Every $\StreamNameUp$ tail-window successor read of an accepted $\CauchyTailThresholdNormalizerUp$ carrier preserves the single normalized threshold row $\Theta$ across the $\DyadicRatCoreUp$ tolerance ledger, the $\RegSeqRatUp$ readback, the $\RealTailAgreementSealUp$ handoff, and every $\RealUp$ consumer row. In particular, no successor window may replace the shared $S,M,\Theta$ surface by a private threshold before exposing $W_0,W_1,D,R,A,E,H,C,P,L,N$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerUniformThresholdInduction}
\begin{proof}
Use the tail-window induction row of \autoref{thm:cauchy-tail-threshold-normalizer-tail-window-induction}. Its base case reads $\Theta$ immediately after the shared source $S$ and seal row $M$; its successor step preserves the displayed windows $W_0,W_1$ together with the dyadic, regular-rational, agreement-seal, and Real rows.

Apply shared-threshold uniqueness and window cofinality. These rows force every later $\StreamNameUp$ window to remain downstream of the same $\Theta$, and they prevent a $\RealTailAgreementSealUp$ or $\RealUp$ consumer from introducing a side threshold.

Finally read Real-facing determinacy and the obligation ledger. The ledger lists only $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$, so the induction is uniform over all displayed downstream consumers and exports no quotient stream, selected host limit, trichotomy, ambient completeness, or second Real seal route.
\end{proof}

\begin{theorem}[Cauchy tail threshold normalizer Real-seal handoff]
\label{thm:cauchy-tail-threshold-normalizer-real-seal-handoff}
Every $\RealUp$ seal handoff of an accepted $\CauchyTailThresholdNormalizerUp$ carrier factors first through
$$
S,\ M,\ \Theta,\ W_0,\ W_1,\ D,\ R,\ A
$$
before any row among
$$
E,\ H,\ C,\ P,\ L,\ N
$$
is available to the consumer. Thus the Real-facing route reads the shared source, modulus seal, normalized threshold, two StreamName windows, dyadic tolerance ledger, RegSeqRat readback, and RealTailAgreementSeal handoff before it may expose the Real consumer row, structural transport, replay, provenance, route ledger, or local naming.
\end{theorem}
\begin{proof}
Start with route exhaustion from \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion}. It presents every $\RealUp$ or $\RealTailAgreementSealUp$ consumer through the same finite row order and prohibits a detached Real seal.

Use Real-facing determinacy next. Once $S$ and $M$ are fixed, the windows $W_0,W_1$, dyadic row $D$, regular readback $R$, and agreement handoff $A$ are all read downstream of $\Theta$; the $\RealUp$ consumer row $E$ cannot precede that threshold path.

The composition and noninternalization rows then keep $H,C,P,L,N$ as transport, replay, provenance, ledger, and naming data for the same route. They do not create an additional seal handoff, so every Real-facing consumer factors through $S,M,\Theta,W_0,W_1,D,R,A$ before exposing $E,H,C,P,L,N$.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerRealSealHandoff}

\begin{theorem}[Cauchy tail threshold normalizer scoped kernel route]
\label{thm:cauchy-tail-threshold-normalizer-scoped-kernel-route}
Every accepted $\CauchyTailThresholdNormalizerUp$ carrier
$$
T=(S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N)
$$
scopes its public threshold-normalizer route over exactly $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealTailAgreementSealUp$, and $\RealUp$. Any $\RealUp$-facing read must expose those rows before using the normalized threshold $\Theta$, and the scoped route exports no quotient stream, selected host limit, trichotomy, ambient completeness, side threshold, or second $\RealUp$ seal.
\end{theorem}
\begin{proof}
Project the carrier and route-exhaustion rows. The accepted packet displays $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$, and \autoref{thm:cauchy-tail-threshold-normalizer-route-exhaustion} forces every public read through that finite route.

Use tail-window induction, shared-threshold uniqueness, and window cofinality. These theorems keep both $\StreamNameUp$ windows and the $\DyadicRatCoreUp$ tolerance ledger downstream of the same normalized threshold, so no side threshold can be selected by a later consumer.

Apply Real-facing determinacy and the Real-seal handoff. They require the $\RegSeqRatUp$ readback and $\RealTailAgreementSealUp$ row to be exposed before the terminal $\RealUp$ seal, and they prevent a second seal route from being projected out of the carrier.

Finally read transport, replay, provenance, ledger, and local naming. The rows $H,C,P,L,N$ are exactly the $\hsame$, $\Cont$, $\Pkg$, route-ledger, and $\NameCert$ boundary over the same finite packet, so the scoped route has no quotient stream, selected host limit, trichotomy, ambient completeness, or hidden threshold coordinate to export.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerScopedKernelRoute}

\begin{theorem}[Cauchy tail threshold normalizer public certificate]
\label{thm:cauchy-tail-threshold-normalizer-public-certificate}
Every public $\RealUp$, $\RealTailAgreementSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, or $\DyadicRatCoreUp$ consumer of an accepted $\CauchyTailThresholdNormalizerUp$ packet receives exactly the scoped route
$$
S,\ M,\ \Theta,\ W_0,\ W_1,\ D,\ R,\ A,\ E,\ H,\ C,\ P,\ L,\ N
$$
from \autoref{thm:cauchy-tail-threshold-normalizer-scoped-kernel-route}. The public certificate exposes the same shared threshold $\Theta$, the two window rows $W_0,W_1$, the dyadic tolerance ledger $D$, the regular-rational readback $R$, the agreement-seal handoff $A$, the Real row $E$, and the structural rows $H,C,P,L,N$; it exports no quotient stream, selected host limit, trichotomy, ambient completeness, private side threshold, or second Real seal route.
\end{theorem}
\begin{proof}
Start with the scoped kernel route of \autoref{thm:cauchy-tail-threshold-normalizer-scoped-kernel-route}. It already fixes the accepted packet as the finite row list $S,M,\Theta,W_0,W_1,D,R,A,E,H,C,P,L,N$ over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealTailAgreementSealUp$, and $\RealUp$.

For a $\RealUp$ or $\RealTailAgreementSealUp$ consumer, apply the Real-seal handoff of \autoref{thm:cauchy-tail-threshold-normalizer-real-seal-handoff}. The handoff requires $S,M,\Theta,W_0,W_1,D,R,A$ before the public Real-facing row is available, and the remaining structural rows $E,H,C,P,L,N$ are read only as part of the same route.

For a $\StreamNameUp$, $\RegSeqRatUp$, or $\DyadicRatCoreUp$ consumer, use window cofinality from \autoref{thm:cauchy-tail-threshold-normalizer-window-cofinality}. It keeps both window rows and the dyadic tolerance ledger downstream of the shared threshold, so the regular readback and agreement seal cannot be separated from $S,M,\Theta$.

The public certificate is therefore only a consumer-facing packaging of the scoped route. Since each admissible public read is obtained by composing these rows, no consumer can add a quotient stream, selected host limit, trichotomy, ambient completeness, private side threshold, or second Real seal route without leaving the displayed packet.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailThresholdNormalizerUp.CauchyTailThresholdNormalizerPublicCertificate}

\closureat{CauchyTailThresholdNormalizerUp}{seedStr}
\closureat{CauchyTailThresholdNormalizerUp}{obligationStr}
\closureat{CauchyTailThresholdNormalizerUp}{scopedStr}

\begin{closurestatus}{\CauchyTailThresholdNormalizerUp}
  \constructivestory{A $\CauchyTailThresholdNormalizerUp$ packet is generated from two regular-Cauchy tail presentations by making the common tail threshold explicit before any agreement or real-seal read is allowed. The finite $\BHist$ carrier stores the threshold normalizer, the tail-agreement row, the eventual-equality row, componentwise $\hsame$ transport, displayed $\Cont$ routes, package provenance, ledger entries, and local naming. Its classifier reads the same threshold surface in both presentations and rejects side thresholds or second real-seal routes. The constructive content is the normalization discipline: every downstream RealUp or agreement read is generated bottom-up from the displayed threshold and its replay ledger, not from a quotient stream or hidden host limit.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{This chapter binds \autoref{thm:cauchy-tail-threshold-normalizer-scoped-kernel-route}: the $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealTailAgreementSealUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the finite $\CauchyTailThresholdNormalizerUp$ kernel route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{It does not claim quotient-stream equality, selected host limits, trichotomy, ambient completeness, private side thresholds, or a second Real seal route.}
  \upgradepath{Public closure requires a public-object consumer surface over the same scoped kernel route, including its StreamName, RegSeqRat, DyadicRatCore, RealTailAgreementSeal, Real, BHist, transport, replay, provenance, and local naming rows.}
\end{closurestatus}
