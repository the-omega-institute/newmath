\chapter{A Concrete Naming Certificate for $\ProximalPointAlgorithmUp$}
\label{ch:concrete-instances-proximal-point-algorithm-namecert}
\origin{human}

Following Rockafellar and Bauschke--Combettes, the $\ProximalPointAlgorithmUp$ packet records the finite BEDC surface for a proximal iteration over a displayed convex functional and metric descent ledger. It sits after the proximal candidate route of \autoref{ch:concrete-instances-proximal-map-namecert}, the lower-envelope boundary of \autoref{ch:concrete-instances-moreau-envelope-namecert}, the convex-domain row of \autoref{ch:concrete-instances-convexset-namecert}, the metric source of \autoref{ch:concrete-instances-metric-namecert}, the completeness service of \autoref{ch:concrete-instances-completemetric-namecert}, and the scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed resolvent steps and finite descent windows; it does not assert arbitrary minimizers, selected limits, quotient orbit equality, or ambient completeness.

\section{Proximal Point Algorithm Carrier}
\label{sec:proximal-point-algorithm-carrier}

\begin{definition}[Proximal point algorithm carrier]
\label{def:proximal-point-algorithm-carrier}
A $\ProximalPointAlgorithmUp$ carrier is a finite $\BHist$ packet
$$
A=(F,X,R,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the convex functional row, carrying the $\ConvexSetUp$ domain, $\RealUp$ value ledger, and finite subgradient or stationarity rows available to the request;
\item $X$ is the metric carrier row, read through $\MetricUp$ and the $\CompleteMetricUp$ service rows only as displayed dependency data;
\item $R$ is the finite resolvent-step row, listing the inspected proximal requests, their $\ProximalMapUp$ handoffs, and their $\MoreauEnvelopeUp$ comparison budgets;
\item $D$ is the descent certificate row, recording the finite window of value and distance comparisons attached to the displayed steps in $R$;
\item $H$ stores componentwise $\hsame$ transports for the functional, metric carrier, resolvent steps, descent ledger, replay, provenance, and local naming rows;
\item $C$ stores displayed $\Cont$ replay from a step request through convex admission, metric distance readback, proximal handoff, lower-envelope comparison, descent-window inspection, and local NameCert exposure;
\item $P$ is the $\Pkg$ provenance over $\ProximalPointAlgorithmUp$, $\ProximalMapUp$, $\MoreauEnvelopeUp$, $\ConvexSetUp$, $\MetricUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\ProximalPointAlgorithmUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,X,R,D,H,C,P,N$. It has no coordinate for a host sequence, an arbitrary minimizer, a selected limit point, quotient orbit equality, or a global convergence theorem.
\end{definition}

\section{Proximal Point Algorithm Certificate}
\label{sec:proximal-point-algorithm-certificate}

\begin{theorem}[Proximal point algorithm NameCert obligations]
\label{thm:proximal-point-algorithm-namecert-obligations}
For every accepted $\ProximalPointAlgorithmUp$ carrier $A=(F,X,R,D,H,C,P,N)$, the local $\NameCert_{\ProximalPointAlgorithmUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the convex functional, metric carrier, resolvent-step ledger, descent certificate, transport, replay, provenance, and local naming rows;
\item convex-metric compatibility, requiring every displayed step to read the same $\ConvexSetUp$, $\MetricUp$, $\CompleteMetricUp$, and $\RealUp$ dependency rows before the proximal handoff is public;
\item resolvent-step exactness, requiring each step in $R$ to pass through the displayed $\ProximalMapUp$ packet and the corresponding $\MoreauEnvelopeUp$ comparison budget;
\item descent-window exactness, requiring the ledger $D$ to list only finite value and distance comparisons already displayed by $F,X,R$;
\item transport and continuation replay, requiring $H$ and $C$ to preserve the same finite step order and the same descent-window rows;
\item non-escape from arbitrary minimizer existence, selected limits, quotient orbit equality, hidden tail selectors, and ambient completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier. The rows $F,X,R,D$ are the complete analytic surface, and the rows $H,C,P,N$ are the structural route that transports, replays, sources, and names the same finite packet.

Read convex-metric compatibility first. The functional row $F$ supplies the convex domain and value ledger, while $X$ supplies the metric and completeness dependency rows as displayed data. No step can be consumed before these rows are visible.

Next inspect the resolvent row. Each listed step in $R$ passes through the proximal handoff of \autoref{ch:concrete-instances-proximal-map-namecert} and the envelope budget of \autoref{ch:concrete-instances-moreau-envelope-namecert}; hence the row records a finite resolvent trace rather than a host minimization principle.

Finally read the descent ledger and structural rows. The ledger $D$ stores only finite comparisons already exposed by $F,X,R$, and $H,C,P,N$ preserve and name that same trace. Since no carrier coordinate stores arbitrary minimizers, selected limits, quotient orbit equality, hidden tail selectors, or ambient completeness, those objects cannot be exported by the local certificate.
\end{proof}

\begin{theorem}[Proximal point algorithm resolvent-step window]
\label{thm:proximal-point-algorithm-resolvent-step-window}
Every accepted $\ProximalPointAlgorithmUp$ packet exposes each algorithmic read through the finite route
$$
F \longmapsto X \longmapsto R \longmapsto D \longmapsto H,C,P,N.
$$
The resolvent row $R$ may cite $\ProximalMapUp$ and $\MoreauEnvelopeUp$ only for displayed requests in the finite window, and the descent row $D$ may compare only the values and distances already carried by $F,X,R$. The route exports no arbitrary minimizer, selected limit, quotient orbit equality, hidden tail selector, host convergence theorem, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the route displayed in the carrier. The functional and metric rows $F,X$ are read before any resolvent step, so a consumer cannot begin with an external sequence or a host convergence claim.

Apply the NameCert obligations above. Resolvent-step exactness forces every step in $R$ through the proximal-map and Moreau-envelope siblings, and descent-window exactness forces every comparison in $D$ to use only values and distances already displayed in the same finite packet.

Now use transport and continuation replay. The rows $H$ and $C$ preserve the order $F,X,R,D$, while $P$ records the sibling provenance and $N$ names the local certificate. The accepted packet is therefore a finite resolvent-step window, not an existence theorem for an infinite proximal orbit.

The excluded objects would require coordinates not present in $A=(F,X,R,D,H,C,P,N)$. Thus the route refuses arbitrary minimizers, selected limits, quotient orbit equality, hidden tail selectors, host convergence, and ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\ProximalPointAlgorithmUp$ consumer read factors through a ConvexSetUp functional row, MetricUp and CompleteMetricUp carrier rows, RealUp value comparisons, displayed ProximalMapUp and MoreauEnvelopeUp step budgets, a finite descent ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert.}

\independenceWitness{proximal-map, moreau-envelope, convexset, metric, completemetric, real: $\ProximalPointAlgorithmUp$ is not bijective to any listed sibling because it jointly carries a convex functional row, metric carrier row, finite resolvent-step ledger, descent-window certificate, transport, replay, provenance, and local naming in one packet.}

\closureat{ProximalPointAlgorithmUp}{seedStr}

\begin{closurestatus}{\ProximalPointAlgorithmUp}
  \constructivestory{A $\ProximalPointAlgorithmUp$ packet is generated from a convex functional row, metric and completeness dependency rows, finite resolvent-step rows, a displayed descent-window ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:proximal-point-algorithm-carrier}, \autoref{thm:proximal-point-algorithm-namecert-obligations}, and \autoref{thm:proximal-point-algorithm-resolvent-step-window} seed the finite proximal-point algorithm certificate over convex, metric, complete-metric, real, proximal-map, and Moreau-envelope rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim arbitrary minimizer existence, selected limits, quotient orbit equality, hidden tail selectors, host convergence, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, convex-metric compatibility, resolvent-step exactness, descent-window exactness, transport, replay, provenance, and local naming for BEDC.Derived.ProximalPointAlgorithmUp.}
\end{closurestatus}
