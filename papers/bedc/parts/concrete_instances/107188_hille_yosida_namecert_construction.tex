\chapter{A Concrete Naming Certificate for $\HilleYosidaUp$}
\label{ch:concrete-instances-hille-yosida-namecert}
\origin{human}

Following Hille and Yosida, the $\HilleYosidaUp$ packet records the finite BEDC surface by which a densely read linear generator graph is accepted as strongly continuous contraction-semigroup generator data. It consumes the functional-analytic source of \autoref{ch:concrete-instances-functionalanalysis-namecert}, the Banach carrier of \autoref{ch:concrete-instances-banach-namecert}, the linear graph rows of \autoref{ch:concrete-instances-linearmap-namecert}, the norm ledgers of \autoref{ch:concrete-instances-norm-namecert}, and the complete-metric convergence surface of \autoref{ch:concrete-instances-completemetric-namecert}. The packet names the generator and resolvent route used by later evolution equations; it is not a spectral theorem, a PDE solver, a selected semigroup object outside the displayed rows, or a global regularity principle.

\begin{definition}[Hille--Yosida carrier]
\label{def:hille-yosida-carrier}
A $\HilleYosidaUp$ carrier is a finite $\BHist$ packet
$$
Y=(B,A,R,S,N,H,C,P,L)
$$
with the following visible rows:
\begin{enumerate}
\item $B$ is the $\BanachUp$ and $\FunctionalAnalysisUp$ source row, recording the ambient vector space, normed structure, and complete state classifier in which generator reads are interpreted.
\item $A$ is the linear generator graph row, a $\LinearMapUp$-facing finite graph with its displayed domain row and graph-compatibility observations.
\item $R$ is the positive-parameter resolvent ledger, recording the displayed $\lambda>0$ requests, resolvent graph rows, domain-return rows, and resolvent identity comparisons used by the generator test.
\item $S$ is the contraction semigroup row, recording the finite time-parameter observations, identity-at-zero row, composition replay row, and contraction comparisons visible to a strongly continuous semigroup consumer.
\item $N$ is the norm-bound ledger, joining the $\NormUp$ contraction bounds for $S$ and the positive-parameter resolvent bounds for $R$.
\item $H$ records componentwise $\hsame$ transport for the Banach source, generator graph, resolvent ledger, semigroup row, norm bounds, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from finite positive-parameter and finite time requests through
$$
B,A,R,N \longmapsto S,H,P,L .
$$
\item $P$ is the $\Pkg$ provenance over $\HilleYosidaUp$, $\FunctionalAnalysisUp$, $\BanachUp$, $\LinearMapUp$, $\NormUp$, $\CompleteMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\HilleYosidaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a chosen global semigroup outside $S$, a spectral calculus, differentiability beyond the displayed graph, maximal PDE regularity, excluded middle, quotient equality, or choice-selected limits.
\end{definition}

\begin{theorem}[Hille--Yosida NameCert obligations]
\label{thm:hille-yosida-namecert-obligations}
For an accepted $\HilleYosidaUp$ carrier
$$
Y=(B,A,R,S,N,H,C,P,L),
$$
the local $\NameCert_{\HilleYosidaUp}$ surface consists exactly of carrier admission for the displayed rows, Banach and functional-analysis source compatibility for $B$, linear generator graph exposure for $A$, positive-parameter resolvent accounting for $R$, contraction semigroup visibility for $S$, norm-bound accounting for $N$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:hille-yosida-carrier}. The accepted packet displays the Banach source, generator graph, resolvent ledger, semigroup row, norm ledger, transport row, replay row, provenance row, and local name.

The analytic obligations are read in the displayed order. The source row $B$ fixes the complete normed state space, the generator row $A$ supplies the linear graph, and the resolvent row $R$ records the positive-parameter reads whose estimates are accounted for by $N$.

The semigroup-facing obligation is then restricted to $S$. A consumer may read finite time observations, the identity-at-zero row, composition replay, and contraction comparisons, but it cannot import an additional selected semigroup object.

Finally $H,C,P,L$ transport, replay, source, and name exactly the same route. Since no row stores spectral calculus, maximal PDE regularity, quotient equality, excluded middle, or choice-selected limits, those principles are outside the local certificate surface.
\end{proof}

\begin{theorem}[Hille--Yosida semigroup generator handoff]
\label{thm:hille-yosida-semigroup-generator-handoff}
Every strongly continuous semigroup generator read accepted by a $\HilleYosidaUp$ carrier factors through
$$
\FunctionalAnalysisUp,\ \BanachUp,\ \LinearMapUp,\ \NormUp,\ \CompleteMetricUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
Equivalently, an evolution-equation consumer may inspect the Banach source $B$, generator graph $A$, positive-parameter resolvent ledger $R$, contraction semigroup row $S$, norm-bound ledger $N$, and structural rows $H,C,P,L$, but it cannot bypass them by selecting a global semigroup, invoking spectral calculus, or reading a host completion principle not displayed by the packet.
\end{theorem}
\begin{proof}
Use \autoref{thm:hille-yosida-namecert-obligations}. It exposes a generator-facing read only after $B,A,R,S,N$ have been projected from the finite carrier.

The resolvent side is controlled by $R$ and $N$. Positive-parameter requests, domain-return rows, identity comparisons, and norm bounds are visible only as finite ledger entries in those coordinates.

The semigroup side is controlled by $S$. Strong continuity and contraction information are read as the displayed time-row observations and replay rows, not as an external choice of a completed semigroup.

The structural rows $H,C,P,L$ then preserve the same data under transport, replay, provenance, and local naming. Thus a later ODE or PDE evolution packet receives precisely the finite generator handoff and no stronger global regularity or spectral conclusion.
\end{proof}

\falsifiablePrediction{Every $\HilleYosidaUp$ generator handoff read factors through one Banach and FunctionalAnalysis source row, one linear generator graph row, one positive-parameter resolvent ledger, one contraction semigroup row, one norm-bound ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{functionalanalysis, banach, linearmap, norm, completemetric: $\HilleYosidaUp$ is not bijective to any listed sibling because it jointly carries a generator graph, positive-parameter resolvent ledger, contraction semigroup row, norm-bound ledger, and the transport/replay/provenance rows tying them into a semigroup-generator certificate.}

\closureat{HilleYosidaUp}{seedStr}

\begin{closurestatus}{\HilleYosidaUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hille-yosida-carrier}, \autoref{thm:hille-yosida-namecert-obligations}, and \autoref{thm:hille-yosida-semigroup-generator-handoff} seed the finite generator certificate from Banach and functional-analysis source rows through linear graph, resolvent, semigroup, norm-bound, transport, replay, provenance, and local naming data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim spectral calculus, maximal PDE regularity, a selected global semigroup outside the displayed row, quotient equality, choice principles, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, generator graph exposure, resolvent positive-parameter accounting, contraction semigroup visibility, norm-bound stability, complete-metric convergence visibility, transport stability, replay stability, and ledger non-escape for BEDC.Derived.HilleYosidaUp.}
  \constructivestory{A $\HilleYosidaUp$ packet is generated from one Banach and FunctionalAnalysis source row, one LinearMap generator graph, one positive-parameter resolvent ledger, one contraction semigroup row, one Norm ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
