\chapter{A Concrete Naming Certificate for $\CantorSpaceCompletionUp$}
\label{ch:concrete-instances-cantor-space-completion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CantorSpaceCompletionUp$ packet records the completion of Cantor space by finite prefix Cauchy names. It sits after the binary-prefix carrier of \autoref{ch:concrete-instances-cantorspace-namecert}, the finite prefix stream certificate of \autoref{ch:concrete-instances-finite-prefix-stream-certificate-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the Cantor-intersection route of \autoref{ch:concrete-instances-cantor-intersection-namecert}, and the interval coding surface of \autoref{ch:concrete-instances-bishop-interval-cantor-namecert}. The packet names constructive prefix completion as displayed BEDC rows; it is not quotient stream equality, not open-cover compactness, not trichotomy, and not ambient $\RealUp$ completeness.

\begin{definition}[CantorSpaceCompletion carrier]
\label{def:cantor-space-completion-carrier}
A $\CantorSpaceCompletionUp$ carrier is a finite $\BHist$ packet
$$
K=(A,W,D,R,E,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\CantorSpaceUp$ prefix row, naming the inspected binary cylinders and their extension order.
\item $W$ is the $\StreamNameUp$ finite window row, displaying the prefix observations available at the requested depths.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic diameter bound attached to each displayed cylinder.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same prefix windows and dyadic bounds.
\item $E$ is the terminal $\RealUp$ seal row, available only after $A,W,D,R$ have been displayed.
\item $L$ is the prefix Cauchy ledger, recording that later prefix windows extend the earlier cylinders and shrink their displayed dyadic diameters.
\item $H$ records componentwise $\hsame$ transport for prefix rows, windows, dyadic ledgers, regular readbacks, terminal seals, the prefix Cauchy ledger, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the same finite prefix Cauchy route.
\item $P$ is the $\Pkg$ provenance over $\CantorSpaceCompletionUp$, $\CantorSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CantorIntersectionUp$, $\BishopIntervalCantorUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CantorSpaceCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,W,D,R,E,L,H,C,P,N$. It has no coordinate for quotient stream equality, selected infinite representatives, trichotomy, open-cover compactness, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CantorSpaceCompletion NameCert obligations]
\label{thm:cantor-space-completion-namecert-obligations}
For every accepted $\CantorSpaceCompletionUp$ carrier
$$
K=(A,W,D,R,E,L,H,C,P,N),
$$
the local $\NameCert_{\CantorSpaceCompletionUp}$ surface consists exactly of carrier admission for the $\CantorSpaceUp$ prefix row, $\StreamNameUp$ finite-window exposure, $\DyadicRatCoreUp$ diameter accounting, $\RegSeqRatUp$ readback exposure, terminal $\RealUp$ seal routing, prefix Cauchy ledger exposure, componentwise transport, displayed replay, package provenance, and local naming. A Real or Completion consumer may cite completed Cantor-space data only after those rows have been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cantor-space-completion-carrier}. The visible completion-facing rows are exactly the binary prefix row $A$, finite windows $W$, dyadic diameter ledger $D$, regular readback $R$, terminal seal $E$, and prefix Cauchy ledger $L$.

Read the prefix Cauchy ledger $L$ after the source rows. It says only that later displayed windows extend earlier binary cylinders and tighten the dyadic diameter bounds already recorded in $D$.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since none of the rows stores quotient stream equality, a selected representative stream, trichotomy, open-cover compactness, countable choice, host real equality, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[CantorSpaceCompletion prefix limit route]
\label{thm:cantor-space-completion-prefix-limit-route}
Every Real or Completion-facing consumer of an accepted $\CantorSpaceCompletionUp$ carrier must factor its read through the displayed route
$$
\CantorSpaceUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
together with the prefix Cauchy ledger $L$ and the structural rows $H,C,P,N$. The route may be compared with $\CantorIntersectionUp$ and $\BishopIntervalCantorUp$ sibling packets as a finite prefix handoff, but it cannot be replaced by quotient stream equality, a selected infinite branch, trichotomy, open-cover compactness, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:cantor-space-completion-namecert-obligations}. They force any accepted consumer to read $A,W,D,R,E,L$ before using the completion-facing surface.

The Cantor-space source row $A$ supplies only displayed binary cylinders. The StreamName row $W$ exposes finite observations of those cylinders, the DyadicRatCore row $D$ records the displayed diameter bounds, and the RegSeqRat row $R$ reads the same finite data as regular rational completion input before the terminal Real seal $E$ is available.

Now use the sibling routes. $\CantorIntersectionUp$ and $\BishopIntervalCantorUp$ may consume the same displayed prefix Cauchy ledger as a finite handoff, but neither sibling adds a quotient stream row, selected infinite branch, trichotomy row, open-cover compactness witness, countable-choice schedule, host real equality, or ambient completeness theorem.

Finally, $H,C,P,N$ preserve only the displayed route under transport, replay, provenance, and local naming. Thus every prefix-limit read remains inside the finite Cantor-space completion packet named in the statement.
\end{proof}

\falsifiablePrediction{An accepted $\CantorSpaceCompletionUp$ read is refuted if it reaches Real or Completion data without displaying the CantorSpace prefix row, StreamName finite windows, DyadicRatCore diameter ledger, RegSeqRat readback, RealUp seal, prefix Cauchy ledger, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{cantorspace, streamname, regseqrat, dyadicratcore, real, cantorintersection, bishopintervalcantor: $\CantorSpaceCompletionUp$ is not bijective to any listed sibling because it jointly binds binary prefix cylinders, finite windows, dyadic diameter bounds, regular readback, terminal sealing, prefix Cauchy extension, transport, replay, provenance, and local naming in one packet.}

\closureat{CantorSpaceCompletionUp}{seedStr}

\begin{closurestatus}{\CantorSpaceCompletionUp}
  \constructivestory{A $\CantorSpaceCompletionUp$ packet is generated from a CantorSpace prefix row, StreamName finite windows, a DyadicRatCore diameter ledger, RegSeqRat readback, a RealUp seal, a prefix Cauchy ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-space-completion-carrier}, \autoref{thm:cantor-space-completion-namecert-obligations}, \autoref{thm:cantor-space-completion-prefix-limit-route}, the falsifiable prediction, and the independence witness seed the finite prefix Cauchy completion route over CantorSpace, StreamName, DyadicRatCore, RegSeqRat, Real, CantorIntersection, BishopIntervalCantor, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected infinite representatives, trichotomy, open-cover compactness, countable choice, ambient $\RealUp$ completeness, Lean verification, axiom-clean audit, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, prefix Cauchy ledger exactness, StreamName window exposure, DyadicRatCore diameter accounting, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CantorSpaceCompletionUp.}
\end{closurestatus}
