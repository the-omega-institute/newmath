\chapter{A Concrete Naming Certificate for $\CompactFanModulusSelectorUp$}
\label{ch:concrete-instances-compact-fan-modulus-selector-namecert}
\origin{human}

Following Bishop--Bridges compact uniform continuity and the fan-bar proof of uniform moduli, the $\CompactFanModulusSelectorUp$ packet records the finite selector that turns a fan-bar depth ledger and compact finite-net pointwise moduli into a single $\UniformModulusUp$ handoff. It realizes the compact-uniform-continuity direction of \autoref{ch:visions-compact-uniform-continuity} as a concrete packet between the finite fan theorem of \autoref{ch:concrete-instances-fantheorem-namecert}, the Bishop fan-modulus route of \autoref{ch:concrete-instances-bishop-fan-modulus-namecert}, the compact-uniform-continuity carrier of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, and the downstream uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names only finite fan bars, compact ProbeBundle windows, pointwise modulus rows, rational lower-bound folds, transport, replay, provenance, and local naming data.

\begin{definition}[Compact fan modulus selector carrier]
\label{def:compact-fan-modulus-selector-carrier}
A $\CompactFanModulusSelectorUp$ carrier is a finite $\BHist$ packet
$$
S=(F,B,K,G,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\FanTheoremUp$ finite bar row, including the binary-prefix spread, decidable bar classifier, and unary depth bound.
\item $B$ is the $\BishopFanModulusUp$ fan-to-window row, turning the depth bound into a finite observation schedule.
\item $K$ is the $\CompactUniformContinuityUp$ source row, exposing compact finite-net and continuous-map pointwise modulus data for the inspected tolerance.
\item $G$ is the finite $\mathsf{ProbeBundle}$ selector row, listing the compact-net probes whose pointwise moduli are inspected.
\item $R$ is the positive rational lower-bound fold row over the selected probe radii and pointwise modulus margins.
\item $U$ is the $\UniformModulusUp$ handoff row obtained from $F,B,K,G,R$.
\item $H$ records componentwise $\hsame$ transport for the fan, fan-modulus, compact-uniform, probe-selector, rational-fold, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-uniform-continuity request through the fan-bar selector, ProbeBundle fold, and UniformModulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactFanModulusSelectorUp$, $\FanTheoremUp$, $\BishopFanModulusUp$, $\CompactUniformContinuityUp$, $\UniformModulusUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactFanModulusSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $F,B,K,G,R,U,H,C,P,N$. The carrier has no coordinate for open-cover compactness, countable choice, selected branches, selected real limits, quotient cover equality, host analytic moduli, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Compact fan modulus selector NameCert obligations]
\label{thm:compact-fan-modulus-selector-namecert-obligations}
For every accepted $\CompactFanModulusSelectorUp$ carrier $S=(F,B,K,G,R,U,H,C,P,N)$, the local $\NameCert_{\CompactFanModulusSelectorUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed fan bar, Bishop fan-modulus, compact-uniform source, ProbeBundle selector, rational fold, UniformModulus handoff, transport, replay, provenance, and local naming rows;
\item fan-bar coverage, requiring the row $F$ to supply the finite branch-prefix depth consumed by $B$;
\item compact selector coverage, requiring $G$ to list the finite probes and pointwise modulus rows exposed by $K$;
\item rational fold exactness, requiring $R$ to be the finite lower-bound fold over the selected probe margins;
\item UniformModulus handoff, requiring $U$ to read only the displayed rows $F,B,K,G,R$ before exposing a uniform precision schedule;
\item transport, replay, provenance, and local-name stability through $H,C,P,N$.
\end{enumerate}
No local selector obligation is stored outside these six rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:compact-fan-modulus-selector-carrier}. The accepted tuple displays exactly the fan, Bishop fan-modulus, compact-uniform source, ProbeBundle selector, rational fold, uniform handoff, and structural rows.

The fan-bar coverage row is finite. The fan theorem row $F$ supplies the bounded branch-prefix inspection, and $B$ converts that depth into the observation schedule used by the selector.

The compact selector rows are also finite. The compact-uniform source $K$ exposes pointwise modulus data, while $G$ lists the probes whose margins are used by the fold.

The fold row $R$ performs only a finite positive-rational lower-bound comparison. The uniform row $U$ consumes that fold together with the fan-bar and compact rows, so it is a handoff from displayed data rather than a choice-selected modulus.

Finally $H,C,P,N$ transport, replay, source, and name the same finite selector. Since the carrier has no coordinate for open-cover compactness, choice, selected branches, selected limits, quotient cover equality, host analytic moduli, Lean verification, or bridge checking, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact fan selector uniform handoff]
\label{thm:compact-fan-modulus-selector-uniform-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactFanModulusSelectorUp$ carrier receives the finite route
$$
\FanTheoremUp \longmapsto \BishopFanModulusUp \longmapsto \CompactUniformContinuityUp \longmapsto \mathsf{ProbeBundle} \longmapsto \RatUp \longmapsto \UniformModulusUp .
$$
The handoff exports a displayed precision schedule from the fan-bar depth, compact finite-net probes, pointwise modulus rows, and rational lower-bound fold; it exports no open-cover compactness theorem, selected branch, countable-choice schedule, quotient cover equality, or host analytic modulus.
\end{theorem}
\begin{proof}
Begin with the fan-bar side. The row $F$ exposes the finite fan data, and $B$ converts its depth into a finite observation schedule.

Read the compact-uniform side next. The row $K$ supplies the compact finite-net and pointwise modulus data, while $G$ selects the finite probe rows that participate in the fold.

The rational fold $R$ produces the displayed lower bound over those rows. The $\UniformModulusUp$ handoff $U$ can therefore read one precision schedule from finite data already present in the packet.

The structural rows preserve the same route under transport, replay, provenance, and local naming. Since no excluded compactness or choice coordinate is present, the handoff remains a finite selector packet.
\end{proof}

\begin{theorem}[Compact fan selector nonescape]
\label{thm:compact-fan-modulus-selector-nonescape}
An accepted $\CompactFanModulusSelectorUp$ carrier exports no open-cover compactness, countable choice, selected branch, selected real limit, quotient cover equality, host analytic modulus, Lean verification, or bridge checking. Every exported read factors through $F,B,K,G,R,U,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier rows. The analytic coordinates are the fan row $F$, Bishop fan-modulus row $B$, compact-uniform source row $K$, ProbeBundle selector row $G$, rational fold row $R$, and UniformModulus handoff row $U$.

The remaining coordinates $H,C,P,N$ provide componentwise transport, continuation replay, provenance, and local naming for those same rows. They do not add compactness, choice, branch-selection, limit-selection, quotient-cover, host-modulus, verification, or bridge data.

Thus every consumer is confined to the displayed finite selector route, and each excluded reading lacks a carrier coordinate from which it could be consumed.
\end{proof}

\falsifiablePrediction{Every accepted CompactFanModulusSelectorUp handoff factors through a finite fan bar, Bishop fan-modulus schedule, compact-uniform source, ProbeBundle selector, rational lower-bound fold, UniformModulus row, transport, replay, provenance, and local naming.}

\independenceWitness{fantheorem, bishop-fan-modulus, compactuniformcontinuity, uniformmodulus: $\CompactFanModulusSelectorUp$ is not bijective to any listed sibling because it jointly carries the fan-bar depth, compact finite-net selector, pointwise modulus rows, rational lower-bound fold, uniform handoff, and structural support while refusing compactness, choice, branch selection, quotient cover equality, host moduli, verification, and bridge checking.}

\closureat{CompactFanModulusSelectorUp}{seedStr}
\begin{closurestatus}{\CompactFanModulusSelectorUp}
  \constructivestory{A $\CompactFanModulusSelectorUp$ packet is generated from a FanTheorem finite bar, a BishopFanModulus schedule, CompactUniformContinuity finite-net and pointwise-modulus rows, a ProbeBundle selector, a rational lower-bound fold, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-fan-modulus-selector-carrier}, \autoref{thm:compact-fan-modulus-selector-namecert-obligations}, \autoref{thm:compact-fan-modulus-selector-uniform-handoff}, and \autoref{thm:compact-fan-modulus-selector-nonescape} seed the finite selector from fan-bar depth and compact pointwise moduli to a UniformModulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, countable choice, selected branches, selected real limits, quotient cover equality, host analytic moduli, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, fan-bar coverage, compact selector coverage, rational fold exactness, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactFanModulusSelectorUp.}
\end{closurestatus}
