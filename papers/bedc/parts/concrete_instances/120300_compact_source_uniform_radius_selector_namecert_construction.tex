\chapter{A Concrete Naming Certificate for $\CompactSourceUniformRadiusSelectorUp$}
\label{ch:concrete-instances-compact-source-uniform-radius-selector-namecert}
\origin{human}

Following Bishop and Bridges on constructive compact metric arguments, the $\CompactSourceUniformRadiusSelectorUp$ packet records the finite radius-selector surface that turns compact-source finite-net data and pointwise continuity radii into a single uniform-radius row. It sits between the compact source chapters \autoref{ch:concrete-instances-compact-namecert} and \autoref{ch:concrete-instances-compactmetric-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the pointwise continuity route of \autoref{ch:concrete-instances-continuous-namecert}, the rational lower-bound route of \autoref{ch:concrete-instances-rat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the downstream uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet is a finite NameCert carrier for Real and Completion uniform-modulus routes; it is not an open-cover compactness theorem, a sequential compactness principle, a countable-choice functional, host real equality, or an ambient Real completeness assertion.

\begin{definition}[Compact-source uniform-radius selector carrier]
\label{def:compact-source-uniform-radius-selector-carrier}
A $\CompactSourceUniformRadiusSelectorUp$ carrier is a finite $\BHist$ packet
$$
E=(K,X,M,F,T,L,Q,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row, read together with the $\CompactUp$ compact-source row $X$ before any radius is selected.
\item $M$ is the $\MetricSpaceUp$ triangle and distance-readback row for the source and target metrics used by the estimate.
\item $F$ is the $\ContinuousMapUp$ pointwise-radius table; each inspected compact-net center carries its displayed local continuity radius and target tolerance row.
\item $T$ is the finite $\MetricSpaceUp$ triangle row that compares an arbitrary source point with the chosen finite-net center and transports the target estimate along the metric triangle.
\item $L$ is the $\RatUp$ lower-bound fold row, recording the finite minimum of the displayed positive pointwise radii.
\item $Q$ is the $\RatUp$ positive-radius certificate produced by the lower-bound fold.
\item $R$ is the terminal $\RealUp$ seal row used when Real-facing consumers read the selected uniform radius.
\item $U$ is the $\UniformModulusUp$ handoff row that exports only the selected radius, the compact finite-net source rows, the pointwise-radius rows, the metric triangle rows, and the Real seal.
\item $H$ records componentwise $\hsame$ transport for $K,X,M,F,T,L,Q,R,U$ and the structural rows.
\item $C$ stores displayed $\Cont$ replay from a finite source-tolerance request through the compact source, pointwise-radius table, lower-bound fold, Real seal, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactSourceUniformRadiusSelectorUp$, $\CompactMetricUp$, $\CompactUp$, $\MetricSpaceUp$, $\ContinuousMapUp$, $\RatUp$, $\RealUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactSourceUniformRadiusSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,X,M,F,T,L,Q,R,U,H,C,P,N$. It has no coordinate for open-cover compactness, sequential compactness, countable choice, host real equality, quotient metric equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact-source uniform-radius selector NameCert obligations]
\label{thm:compact-source-uniform-radius-selector-namecert-obligations}
For every accepted $\CompactSourceUniformRadiusSelectorUp$ carrier
$$
E=(K,X,M,F,T,L,Q,R,U,H,C,P,N),
$$
the local $\NameCert_{\CompactSourceUniformRadiusSelectorUp}$ surface consists exactly of carrier admission, compact-source exposure through $K$ and $X$, metric triangle exposure through $M$ and $T$, continuous-map pointwise-radius exposure through $F$, rational lower-bound folding through $L$ and $Q$, Real sealing through $R$, UniformModulus handoff through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-source-uniform-radius-selector-carrier}. The compact source rows are $K$ and $X$, the analytic estimate rows are $M,F,T,L,Q,R,U$, and the structural rows are $H,C,P,N$.

The compact source is read first. Its finite-net row supplies only displayed centers and source-radius windows, so the packet cannot appeal to an open-cover compactness theorem, a sequential-compactness principle, or countable choice.

Next the pointwise-radius table $F$ and the metric triangle row $T$ are replayed over those displayed centers. The rational rows $L$ and $Q$ fold the finite list of positive pointwise radii into one lower-bound certificate, and the $\RealUp$ row $R$ seals the selected radius for Real-facing reads.

Finally the $\UniformModulusUp$ handoff row $U$ exports exactly that finite route, while $H,C,P,N$ transport, replay, source, and name the same packet. Since no carrier coordinate stores host real equality, quotient metric equality, or ambient Real completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Compact-source uniform-radius selector consumer boundary]
\label{thm:compact-source-uniform-radius-selector-consumer-boundary}
Every Real or Completion uniform-modulus consumer that reads an accepted $\CompactSourceUniformRadiusSelectorUp$ packet must pass through the finite route
$$
K,X,M,F,T,L,Q,R,U,H,C,P,N
$$
before using the selected uniform-radius row. The consumer receives a compact finite-net source, pointwise-radius rows, metric triangle rows, a rational lower-bound fold, a Real seal, and the $\UniformModulusUp$ handoff; it receives no open-cover compactness object, sequential compactness object, countable-choice functional, host real equality, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with the NameCert obligations. They identify $K,X,M,F,T,L,Q,R,U,H,C,P,N$ as the whole public surface of the accepted selector packet.

The Real-facing radius is downstream of the rational lower-bound rows. A consumer therefore reads $L$ and $Q$ before the seal $R$, and it cannot replace the finite fold by host real equality or by a completeness argument.

The Completion-facing uniform-modulus route is downstream of $U$. That row carries only the selected radius and the displayed compact-source, continuity, triangle, rational, and Real-seal data. Hence the consumer boundary is exactly the finite route above.
\end{proof}

\falsifiablePrediction{An accepted $\CompactSourceUniformRadiusSelectorUp$ consumer that obtains a uniform-radius row without reading $K,X,M,F,T,L,Q,R,U,H,C,P,N$ refutes the selector carrier.}

\independenceWitness{compact, compactmetric, metric, continuous, rat, real, uniformmodulus: no listed sibling is bijective to $\CompactSourceUniformRadiusSelectorUp$ because this packet jointly binds compact-source finite-net rows, pointwise-radius rows, metric triangle rows, rational lower-bound folding, a Real seal, UniformModulus handoff, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}

\closureat{CompactSourceUniformRadiusSelectorUp}{seedStr}

\begin{closurestatus}{\CompactSourceUniformRadiusSelectorUp}
  \constructivestory{A $\CompactSourceUniformRadiusSelectorUp$ packet is generated from CompactMetric and Compact finite-net source rows, ContinuousMap pointwise-radius rows, MetricSpace triangle rows, Rat lower-bound fold rows, a Real seal, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-source-uniform-radius-selector-carrier}, \autoref{thm:compact-source-uniform-radius-selector-namecert-obligations}, and \autoref{thm:compact-source-uniform-radius-selector-consumer-boundary} seed the constructive compact-source radius selector consumed by Real and Completion uniform-modulus routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter excludes open-cover compactness, sequential compactness, countable choice, host real equality, ambient Real completeness, Lean theorem verification, and bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, pointwise-radius replay, metric triangle replay, rational lower-bound folding, Real sealing, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactSourceUniformRadiusSelectorUp.}
\end{closurestatus}
