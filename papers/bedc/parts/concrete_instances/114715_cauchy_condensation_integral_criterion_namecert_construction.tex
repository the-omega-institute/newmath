\chapter{A Concrete Naming Certificate for $\CauchyCondensationIntegralCriterionUp$}
\label{ch:concrete-instances-cauchy-condensation-integral-criterion-namecert}
\origin{human}

Following Cauchy and Bishop--Bridges constructive analysis, the $\CauchyCondensationIntegralCriterionUp$ packet records the finite criterion route that carries a monotone positive series source through Cauchy condensation and then through an integral-test handoff before any Real or Completion consumer reads convergence data. It consumes the condensation test of \autoref{ch:concrete-instances-cauchy-condensation-test-namecert}, the integral-test surface of \autoref{ch:concrete-instances-cauchy-integral-test-namecert}, the bridge packet of \autoref{ch:concrete-instances-cauchy-condensation-integral-bridge-namecert}, and the L10 readback corridor of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The certificate names the criterion as displayed finite data; it does not choose a completed sum, an improper integral value, a quotient series equality, countable-choice schedules, or ambient $\RealUp$ completeness.

\begin{definition}[CauchyCondensationIntegralCriterion carrier]
\label{def:cauchy-condensation-integral-criterion-carrier}
A $\CauchyCondensationIntegralCriterionUp$ carrier is a finite $\BHist$ packet
$$
K=(S,M,D,C,I,B,W,Q,R,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the positive decreasing series-source row, recording only inspected term windows, positivity witnesses, and monotonicity comparisons.
\item $M$ is the dyadic condensation block row supplied by the condensation test route.
\item $D$ is the $\DyadicRatCoreUp$ scale ledger, carrying powers-of-two windows, endpoint comparisons, and dyadic tolerance slack.
\item $C$ is the condensation criterion row, exposing the finite reduction from the source tail to the dyadic block tail.
\item $I$ is the integral-test row, recording finite interval cells and monotone majorant or minorant comparisons.
\item $B$ is the bridge row that matches the condensation blocks with integral windows through the existing condensation-integral route.
\item $W$ is the finite readback schedule for Real and Completion consumers.
\item $Q$ is the $\RegSeqRatUp$ readback row reached from $S,M,D,C,I,B,W$.
\item $R$ is the terminal $\RealUp$ seal row read only after the regular-rational handoff is visible.
\item $H$ records componentwise $\hsame$ transport for source, condensation, dyadic, integral, bridge, readback, seal, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from the criterion request through the source, condensation, integral, bridge, readback, and seal rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyCondensationIntegralCriterionUp$, $\CauchyCondensationTestUp$, $\CauchyIntegralTestUp$, $\CauchyCondensationIntegralBridgeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCondensationIntegralCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,M,D,C,I,B,W,Q,R,H,T,P,N$. It has no coordinate for a host improper integral, selected infinite sum, quotient equality of series, countable choice, trichotomy, host real equality, or completeness of $\RealUp$.
\end{definition}

\begin{theorem}[CauchyCondensationIntegralCriterion NameCert obligations]
\label{thm:cauchy-condensation-integral-criterion-namecert-obligations}
For every accepted $\CauchyCondensationIntegralCriterionUp$ carrier $K=(S,M,D,C,I,B,W,Q,R,H,T,P,N)$, the local $\NameCert_{\CauchyCondensationIntegralCriterionUp}$ surface consists exactly of carrier admission for the displayed thirteen-row packet; positive monotone source exposure through $S$; condensation block exposure through $M$; dyadic scale exposure through $D$; condensation reduction through $C$; integral-test exposure through $I$; bridge exposure through $B$; finite schedule exposure through $W$; $\RegSeqRatUp$ readback through $Q$; $\RealUp$ sealing through $R$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $T$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier rows from \autoref{def:cauchy-condensation-integral-criterion-carrier}. The analytic route is exactly $S,M,D,C,I,B,W,Q,R$, and $H,T,P,N$ are the structural certificate rows.

The source and condensation rows are read first. The condensation test sibling supplies the finite dyadic block route, while the dyadic row records the scale data needed to compare those blocks.

The integral side is then read through $I$ and $B$. The integral-test sibling supplies finite interval cells, and the condensation-integral bridge orders the handoff before regular-rational readback is consumed.

Finally $W,Q,R,H,T,P,N$ schedule, read back, seal, transport, replay, source, and name the same finite route. Since none of the excluded host objects is a carrier coordinate, none is a NameCert obligation of the criterion packet.
\end{proof}

\begin{theorem}[CauchyCondensationIntegralCriterion condensation-to-integral handoff]
\label{thm:cauchy-condensation-integral-criterion-handoff}
Every accepted $\CauchyCondensationIntegralCriterionUp$ carrier sends a positive decreasing series source to an integral-test comparison only through the displayed route
$$
S,M,D \longmapsto C \longmapsto I,B \longmapsto W,Q,R.
$$
The condensation side is anchored by \autoref{ch:concrete-instances-cauchy-condensation-test-namecert}; the integral side is anchored by \autoref{ch:concrete-instances-cauchy-integral-test-namecert}; and the bridge is anchored by \autoref{ch:concrete-instances-cauchy-condensation-integral-bridge-namecert}. The route exports no selected subseries, host integral object, quotient series equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the source rows $S,M,D$. The source row displays only inspected monotone positive term windows, while the dyadic row fixes the finite powers-of-two scale on which condensation blocks are read.

Use the condensation test route. It places the block comparison inside $C$ before any integral window is inspected, so the consumer cannot skip directly from the source series to a host convergence theorem.

Next use the integral-test and bridge rows. The row $I$ provides finite interval cells, and $B$ matches those cells with the condensation blocks through the sibling bridge.

The readback rows $W,Q,R$ then keep the conclusion inside finite schedules, regular rational readback, and the terminal Real seal. The carrier has no coordinate for the excluded host objects, so the handoff is exactly the displayed finite route.
\end{proof}

\begin{theorem}[CauchyCondensationIntegralCriterion RegSeqRat Real nonescape]
\label{thm:cauchy-condensation-integral-criterion-regseqrat-real-nonescape}
Every Real-facing or Completion-facing read of an accepted $\CauchyCondensationIntegralCriterionUp$ carrier reaches the terminal seal $R$ only after the route
$$
S,M,D,C,I,B,W,Q,R
$$
has exposed the source, condensation, dyadic, integral, bridge, schedule, and $\RegSeqRatUp$ readback rows, together with structural rows $H,T,P,N$. Thus the criterion supplies a finite L10 convergence-read boundary and not an ambient $\RealUp$ completeness assertion.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchy-condensation-integral-criterion-handoff}. It places the analytic comparison behind the source, condensation, dyadic, integral, bridge, and finite schedule rows.

The regular rational readback row $Q$ is downstream of that comparison. A Real-facing consumer therefore sees only the scheduled regular-rational read before the terminal seal $R$ is public.

The structural rows preserve the same route under $\hsame$, $\Cont$, provenance, and local naming. Because the carrier has no row for quotient series equality, selected limits, countable-choice schedules, host real equality, or ambient completeness, the Real and Completion boundary cannot escape the displayed $S,M,D,C,I,B,W,Q,R$ route.
\end{proof}

\falsifiablePrediction{An accepted $\CauchyCondensationIntegralCriterionUp$ consumer that reaches a $\RealUp$ seal without first reading the positive monotone source row, condensation block row, dyadic scale row, condensation reduction row, integral-test row, bridge row, finite schedule, and $\RegSeqRatUp$ handoff refutes the carrier surface.}

\independenceWitness{cauchy-condensation-test, cauchy-integral-test, cauchy-condensation-integral-bridge, dyadicratcore, regseqrat, real: no listed sibling supplies the whole criterion packet $K=(S,M,D,C,I,B,W,Q,R,H,T,P,N)$, because each sibling contributes only a condensation row, integral row, bridge row, dyadic ledger, regular readback, or terminal seal while this chapter binds them into one finite criterion certificate.}

\closureat{CauchyCondensationIntegralCriterionUp}{seedStr}

\begin{closurestatus}{\CauchyCondensationIntegralCriterionUp}
  \constructivestory{A $\CauchyCondensationIntegralCriterionUp$ packet is generated from positive monotone series-source rows, condensation block rows, DyadicRatCore scale rows, condensation criterion rows, integral-test rows, condensation-integral bridge rows, finite schedules, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-condensation-integral-criterion-carrier}, \autoref{thm:cauchy-condensation-integral-criterion-namecert-obligations}, \autoref{thm:cauchy-condensation-integral-criterion-handoff}, and \autoref{thm:cauchy-condensation-integral-criterion-regseqrat-real-nonescape} seed the finite criterion route over condensation blocks, integral windows, RegSeqRat readback, and the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, ambient improper integration, selected infinite sums, quotient series equality, countable choice, trichotomy, host real equality, Real completeness, or a completed summability theorem.}
  \upgradepath{Obligation closure requires checked carrier admission, condensation-test exactness, integral-test handoff, bridge exactness, regular readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyCondensationIntegralCriterionUp.}
\end{closurestatus}
