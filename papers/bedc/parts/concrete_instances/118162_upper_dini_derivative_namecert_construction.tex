\chapter{A Concrete Naming Certificate for $\UpperDiniDerivativeUp$}
\label{ch:concrete-instances-upper-dini-derivative-namecert}
\origin{human}

Following Saks and Bishop--Bridges constructive analysis, the $\UpperDiniDerivativeUp$ packet separates the one-sided upper Dini derivative carrier from the combined Dini-window packet of \autoref{ch:concrete-instances-dini-derivative-window-namecert}. It sits beside the Denjoy--Young--Saks boundary of \autoref{ch:concrete-instances-denjoy-young-saks-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter names the upper one-sided limsup surface as finite BEDC data; it does not assert differentiability, a selected derivative value, almost-everywhere alternatives, or ambient real completeness.

\begin{definition}[UpperDiniDerivative carrier]
\label{def:upper-dini-derivative-carrier}
An $\UpperDiniDerivativeUp$ carrier is a finite $\BHist$ packet
$$
U=(I,F,X,Q,S,R,D,W,E,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $I$ is the interval-cell row on which the one-sided derivative request is displayed.
\item $F$ is the real-valued function-observation row over the inspected interval cells.
\item $X$ is the base-point row, read only through the same real-name and interval surface as $I$.
\item $Q$ is the positive approach-direction row, recording the finite side from which quotients are sampled.
\item $S$ is the $\StreamNameUp$ finite schedule row for the inspected function and base-point observations.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the stream windows in $S$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the radii, slack comparisons, and quotient-window refinements.
\item $W$ is the sibling $\DiniDerivativeWindowUp$ row whose upper Dini coordinate is being exposed.
\item $E$ is the $\RealUp$ seal row for the displayed upper-bound read.
\item $H$ records componentwise $\hsame$ transport for the interval, function, base-point, direction, stream, readback, dyadic, window, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for refinements of the same one-sided quotient window.
\item $P$ is the $\Pkg$ provenance over $\UpperDiniDerivativeUp$, $\DiniDerivativeWindowUp$, $\DenjoyYoungSaksUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UpperDiniDerivativeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $I,F,X,Q,S,R,D,W,E,H,C,P,N$. It has no coordinate for a lower Dini row, a two-sided derivative, a selected limiting slope, a measure-theoretic alternative, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[UpperDiniDerivative window stability]
\label{thm:upper-dini-derivative-window-stability}
For every accepted $\UpperDiniDerivativeUp$ carrier
$$
U=(I,F,X,Q,S,R,D,W,E,H,C,P,N),
$$
any refinement of the displayed one-sided quotient window is accepted only when it factors through the same interval row $I$, function-observation row $F$, base-point row $X$, approach-direction row $Q$, stream schedule $S$, regular readback $R$, dyadic tolerance ledger $D$, and sibling $\DiniDerivativeWindowUp$ row $W$. Consequently the public upper-Dini read is stable under finite window refinement and exports no lower-Dini coordinate, two-sided derivative, selected limiting slope, quotient equality of real names, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:upper-dini-derivative-carrier}. The one-sided quotient surface is not an isolated real row; it is tied to the interval, function observation, base point, approach direction, stream schedule, regular readback, dyadic ledger, and sibling Dini window.

For a refinement, replay through $C$ and transport through $H$. The replay can change only the inspected finite window while preserving the rows $I,F,X,Q,S,R,D,W,E,P,N$ as the same displayed packet.

The excluded data are absent from the carrier. Since no coordinate names a lower-Dini export, two-sided derivative, selected limiting slope, quotient equality, choice schedule, or ambient completeness theorem, a refinement-stable consumer cannot read any of those objects.
\end{proof}

\begin{theorem}[UpperDiniDerivative NameCert obligations]
\label{thm:upper-dini-derivative-namecert-obligations}
For every accepted $\UpperDiniDerivativeUp$ carrier $U=(I,F,X,Q,S,R,D,W,E,H,C,P,N)$, the local $\NameCert_{\UpperDiniDerivativeUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the interval cell, function-observation row, base-point row, approach-direction row, stream schedule, regular readback, dyadic tolerance ledger, sibling Dini-window row, upper real seal, and structural rows $H,C,P,N$;
\item one-sided quotient coverage, requiring each upper-Dini request to display the positive approach side $Q$ together with matching stream windows in $S$, readbacks in $R$, and dyadic tolerance entries in $D$;
\item sibling-window compatibility, requiring the upper coordinate read from $W$ to be the same finite upper-Dini row routed by the carrier;
\item window-stability replay, requiring refinements to replay through $C$ and transport through $H$ without changing the carrier-local upper row;
\item non-escape from differentiability, lower-Dini export, selected limiting slopes, quotient real equality, countable choice, measure-theoretic alternatives, or ambient $\RealUp$ completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission reads the packet named in \autoref{def:upper-dini-derivative-carrier}. The analytic rows are $I,F,X,Q,S,R,D,W,E$, while $H,C,P,N$ provide transport, replay, provenance, and local naming.

One-sided quotient coverage is finite because every request is displayed through the approach-direction row $Q$, the stream schedule $S$, the regular readback $R$, and the dyadic ledger $D$. A request lacking one of those rows is not an accepted $\UpperDiniDerivativeUp$ read.

Sibling-window compatibility uses the $\DiniDerivativeWindowUp$ row $W$. The upper coordinate is consumed from that sibling window and then sealed by $E$ only after the same route has been displayed.

Finally apply the stability theorem \autoref{thm:upper-dini-derivative-window-stability}. Refinement replay remains inside the same carrier, and the listed exclusions are not rows of the packet, so the NameCert surface is exhausted by the stated obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\UpperDiniDerivativeUp$ consumer reaching an upper-Dini read without the displayed one-sided direction, StreamName schedule, RegSeqRat readback, DyadicRatCore tolerance ledger, and DiniDerivativeWindow row refutes the carrier.}

\independenceWitness{dini-derivative-window, denjoy-young-saks, real, regseqrat, streamname, dyadicratcore: no listed sibling supplies the whole UpperDiniDerivative packet, because each sibling contributes only the combined Dini window, broader differentiability boundary, terminal real seal, rational readback, observation schedule, or tolerance ledger while this chapter binds the one-sided upper Dini carrier in one finite surface.}

\closureat{UpperDiniDerivativeUp}{seedStr}

\begin{closurestatus}{\UpperDiniDerivativeUp}
  \constructivestory{An $\UpperDiniDerivativeUp$ packet is generated from interval cells, function-observation rows, a base-point row, a positive approach-direction row, StreamName schedules, RegSeqRat readbacks, DyadicRatCore tolerance rows, a DiniDerivativeWindow upper row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:upper-dini-derivative-carrier}, \autoref{thm:upper-dini-derivative-window-stability}, and \autoref{thm:upper-dini-derivative-namecert-obligations} seed the one-sided upper Dini derivative carrier and its finite window-stability obligations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim differentiability, lower-Dini export, two-sided derivative existence, selected limiting slopes, almost-everywhere alternatives, quotient real equality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, one-sided quotient coverage, sibling-window compatibility, finite window stability, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.UpperDiniDerivativeUp.}
\end{closurestatus}
