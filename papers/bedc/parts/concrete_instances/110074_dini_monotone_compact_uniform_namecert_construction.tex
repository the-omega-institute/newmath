\chapter{A Concrete Naming Certificate for $\DiniMonotoneCompactUniformUp$}
\label{ch:concrete-instances-dini-monotone-compact-uniform-namecert}
\origin{human}

Following Dini's theorem and Bishop--Bridges constructive compactness, the $\DiniMonotoneCompactUniformUp$ packet records the finite NameCert surface by which a monotone family of continuous maps on a compact metric source yields a compact-uniform tolerance handoff. It sits beside the Dini continuity packet of \autoref{ch:concrete-instances-dini-continuity-namecert}, the Dini theorem carrier of \autoref{ch:concrete-instances-dini-theorem-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the uniform-limit handoff of \autoref{ch:concrete-instances-uniformlimit-namecert}. The packet uses finite cover and explicit probe rows; it does not invoke general open-cover compactness, a host supremum, selected minimum radius outside the carrier, quotient equality of functions, or ambient $\RealUp$ completeness.

\begin{definition}[Dini monotone compact-uniform carrier]
\label{def:dini-monotone-compact-uniform-carrier}
A $\DiniMonotoneCompactUniformUp$ carrier is a finite $\BHist$ packet
$$
D=(X,F,M,L,R,V,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the compact metric source row, carrying the metric source, compact finite-cover interface, and finite probe extraction route.
\item $F$ is the continuous-map family row, recording the displayed maps on $X$ and their local continuity data.
\item $M$ is the monotone ledger, recording the family order and the visible monotone approach toward the limiting row.
\item $L$ is the pointwise-limit row, recording the finite probe-wise route from family observations to the displayed limiting map.
\item $R$ is the rational tolerance budget, storing the requested positive tolerance, cover-cell slices, and comparison rows.
\item $V$ is the finite cover and $\ProbeBundle$ row extracted from $X$ for the requested tolerance window.
\item $U$ is the uniform-modulus and uniform-limit handoff row. It records the route from local continuity and monotone pointwise data through $V$ and $R$ to a single compact-uniform read.
\item $H$ records componentwise $\hsame$ transport for source, family, monotone ledger, limit, tolerance, cover, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-uniform requests through $X,F,M,L,R,V,U,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\DiniMonotoneCompactUniformUp$, $\DiniContinuityUp$, $\DiniTheoremUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\UniformLimitUp$, $\RatUp$, $\BHist$, $\ProbeBundle$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DiniMonotoneCompactUniformUp}$ row.
\end{enumerate}
The carrier predicate admits exactly these rows. Its classifier compares accepted packets only through $X,F,M,L,R,V,U,H,C,P,N$ and has no coordinate for host suprema, arbitrary open-cover compactness, quotient function equality, selected radii outside $V$, or ambient real completeness.
\end{definition}

\begin{theorem}[Dini monotone compact-uniform carrier obligations]
\label{thm:dini-monotone-compact-uniform-carrier-obligations}
For an accepted $\DiniMonotoneCompactUniformUp$ carrier
$$
D=(X,F,M,L,R,V,U,H,C,P,N),
$$
the local $\NameCert_{\DiniMonotoneCompactUniformUp}$ surface consists exactly of compact metric source admission through $X$, continuous-map family admission through $F$, monotone visibility through $M$, pointwise-limit visibility through $L$, rational tolerance budgeting through $R$, finite cover and probe extraction through $V$, uniform-modulus and uniform-limit handoff through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dini-monotone-compact-uniform-carrier}. The first seven rows are the analytic data, and $H,C,P,N$ are the structural certificate rows.

The monotone Dini route is forced by the displayed order. A compact-uniform request first reads the source and family rows $X,F$, then exposes monotone visibility through $M$ and probe-wise limiting data through $L$.

The finite tolerance work is stored in $R,V,U$. The rational budget $R$ is subdivided across the finite $\ProbeBundle$ row $V$, and the handoff row $U$ records how those local reads assemble into one uniform-modulus and uniform-limit request.

Finally $H,C,P,N$ preserve the same coordinates by transport, replay, provenance, and local naming. Since the excluded host principles have no carrier coordinate, they are not NameCert obligations of the packet.
\end{proof}

\begin{theorem}[Dini monotone compact finite-cover extraction]
\label{thm:dini-monotone-compact-uniform-finite-cover-extraction}
Every compact-uniform read accepted by a $\DiniMonotoneCompactUniformUp$ carrier must expose the compact source row $X$, the continuous-family row $F$, the monotone ledger $M$, the pointwise-limit row $L$, and the rational budget $R$ before extracting the finite $\ProbeBundle$ cover row $V$. The extraction is a finite compact metric route; it is not general open-cover compactness, a selected global minimum radius, or a host supremum argument.
\end{theorem}
\begin{proof}
Use the carrier obligations of \autoref{thm:dini-monotone-compact-uniform-carrier-obligations}. They place $X,F,M,L,R$ upstream of the finite cover row $V$.

The compact source row $X$ supplies the compact metric finite-cover interface. The continuous-map family row $F$ and monotone ledger $M$ determine which local continuity and monotone comparisons can be inspected on each displayed probe.

The pointwise-limit row $L$ is then read only at probes supplied by the packet, and the rational budget $R$ supplies the requested tolerance and cell slices. Hence $V$ is extracted as finite packet data rather than by an external open-cover or supremum principle.
\end{proof}

\begin{theorem}[Dini monotone uniform budget handoff]
\label{thm:dini-monotone-compact-uniform-budget-handoff}
For an accepted $\DiniMonotoneCompactUniformUp$ carrier, every consumer of the compact-uniform conclusion must route the requested tolerance through $R$, the finite probe cover $V$, and the uniform-modulus and uniform-limit handoff $U$, then through $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. The exported handoff is only the finite BEDC route through $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\UniformLimitUp$, $\DiniContinuityUp$, and $\DiniTheoremUp$.
\end{theorem}
\begin{proof}
Begin with the finite-cover extraction of \autoref{thm:dini-monotone-compact-uniform-finite-cover-extraction}. It supplies cover cells and probe rows downstream from the compact metric source, continuous family, monotone ledger, pointwise limit, and rational budget.

The handoff row $U$ consumes exactly that finite data. It first reads the local modulus information attached to the cells of $V$, then packages the assembled tolerance into the displayed uniform-limit route.

The structural rows $H,C,P,N$ export no analytic data beyond that route. Therefore the consumer receives a compact-uniform Dini handoff through the named sibling packets, with no host function equality, hidden completion, selected radius outside the carrier, or open-cover compactness.
\end{proof}

\begin{theorem}[Dini monotone compact-uniform finite descent]
\label{thm:dini-monotone-compact-uniform-finite-descent}
For an accepted $\DiniMonotoneCompactUniformUp$ carrier
$$
D=(X,F,M,L,R,V,U,H,C,P,N),
$$
every compact-uniform tolerance export descends through the pointwise monotone rows $M,L$ on the finite $\ProbeBundle$ cover $V$ and the rational budget $R$ before $U$ may expose a single uniform read. The export cannot introduce a host supremum, selected minimum radius, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dini-monotone-compact-uniform-carrier}. The only rows between the compact source and the uniform handoff are the continuous family $F$, monotone ledger $M$, pointwise-limit row $L$, rational budget $R$, finite cover row $V$, and handoff row $U$.

Apply the finite-cover extraction theorem \autoref{thm:dini-monotone-compact-uniform-finite-cover-extraction}. It forces $V$ to be read as finite $\ProbeBundle$ data after the pointwise monotone rows and the tolerance budget have been displayed.

Apply the budget handoff theorem \autoref{thm:dini-monotone-compact-uniform-budget-handoff}. The row $U$ can expose a single compact-uniform read only through the already displayed $R,V$ route, and the structural rows $H,C,P,N$ preserve that route without adding analytic data.

Row exhaustion now removes the excluded alternatives. The carrier has no coordinate for a host supremum, selected minimum radius, open-cover compactness, or ambient $\RealUp$ completeness, so none of those objects can be exported by the finite descent.
\end{proof}

\begin{theorem}[Dini monotone ProbeBundle threshold]
\label{thm:dini-monotone-compact-uniform-probebundle-threshold}
For an accepted $\DiniMonotoneCompactUniformUp$ carrier, once the compact source row $X$, monotone ledger $M$, pointwise-limit row $L$, rational budget $R$, and finite $\ProbeBundle$ cover row $V$ are displayed, every compact-uniform tolerance export factors through a single $\ProbeBundle$ threshold row before $U$ can expose a uniform-modulus handoff.

The threshold row is finite packet data assembled from $X,M,L,R,V$ and transported through $H,C,P,N$. It is not a host supremum, an externally selected radius, an open-cover compactness argument, a quotient function equality, or an ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Start with the carrier obligations of \autoref{thm:dini-monotone-compact-uniform-carrier-obligations}. They place the compact source row, monotone ledger, pointwise-limit row, rational budget, finite cover row, handoff row, and structural rows in the displayed carrier inventory.

Apply finite-cover extraction by \autoref{thm:dini-monotone-compact-uniform-finite-cover-extraction}. The requested tolerance first reads the compact source and rational budget, then extracts the finite $\ProbeBundle$ cover row $V$ on which the pointwise monotone comparisons may be inspected.

Use the budget handoff of \autoref{thm:dini-monotone-compact-uniform-budget-handoff} and the finite descent of \autoref{thm:dini-monotone-compact-uniform-finite-descent}. Together they force the local monotone rows $M,L$ and the rational budget $R$ to be consumed on $V$ before the handoff row $U$ may export a single compact-uniform read.

The common finite read on $V$ is the required threshold row. The structural rows $H,C,P,N$ transport, replay, source, and name it without adding analytic data, and the carrier has no coordinate for a host supremum, selected radius outside the finite $\ProbeBundle$, open-cover compactness, quotient function equality, or ambient Real completeness.
\end{proof}

\closureat{DiniMonotoneCompactUniformUp}{seedStr}

\begin{closurestatus}{\DiniMonotoneCompactUniformUp}
  \constructivestory{A $\DiniMonotoneCompactUniformUp$ packet is generated from compact metric source rows, continuous-map family rows, monotone ledgers, pointwise-limit rows, rational tolerance budgets, finite ProbeBundle cover rows, uniform-modulus and uniform-limit handoff rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dini-monotone-compact-uniform-carrier}, \autoref{thm:dini-monotone-compact-uniform-carrier-obligations}, \autoref{thm:dini-monotone-compact-uniform-finite-cover-extraction}, \autoref{thm:dini-monotone-compact-uniform-budget-handoff}, and \autoref{thm:dini-monotone-compact-uniform-finite-descent} seed the constructive compact-uniform Dini carrier surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, arbitrary open-cover compactness, host suprema, quotient function equality, selected radii outside the finite ProbeBundle, ambient Real completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-cover extraction, monotone pointwise visibility, uniform budget handoff, transport, replay, provenance, and local naming for BEDC.Derived.DiniMonotoneCompactUniformUp.}
\end{closurestatus}
