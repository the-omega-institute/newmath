\chapter{A Concrete Naming Certificate for $\MorseBottLemmaUp$}
\label{ch:concrete-instances-morse-bott-lemma-namecert}
\origin{human}

Following Bott's nondegenerate critical-manifold form and standard Morse theory, the $\MorseBottLemmaUp$ packet records the local normal-form route for a nondegenerate critical submanifold as finite BEDC certificate data. It sits beside the isolated critical-point packet of \autoref{ch:concrete-instances-morse-lemma-namecert}, the Morse-theory surface of \autoref{ch:concrete-instances-morse-theory-namecert}, the manifold chart rows of \autoref{ch:concrete-instances-manifold-namecert}, the derivative ledger of \autoref{ch:concrete-instances-derivative-namecert}, the vector-space coordinate carrier of \autoref{ch:concrete-instances-vecspace-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed critical-submanifold, normal-bundle, transverse Hessian, tubular-coordinate, quadratic normal-form, transport, replay, provenance, and local naming rows; it does not import a host smooth tubular-neighbourhood theorem, quotient germs, global flow data, or analytic compactness.

\begin{definition}[Morse--Bott lemma carrier]
\label{def:morse-bott-lemma-carrier}
A $\MorseBottLemmaUp$ carrier is a finite $\BHist$ packet
$$
B=(X,S,N,Q,T,L,H,C,R,P,A)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\ManifoldUp$ chart-source row, naming the displayed neighbourhood, chart window, coordinate dimension, and local domain in which the critical submanifold is inspected.
\item $S$ is the critical-submanifold row, recording the displayed submanifold chart, tangent directions, normal directions, and derivative-vanishing witness along the submanifold through the $\DerivativeUp$ ledger.
\item $N$ is the normal-bundle row, storing the finite transverse coordinate carrier and the projection from the ambient chart to the normal coordinates.
\item $Q$ is the transverse Hessian-signature row, storing the nondegenerate normal quadratic form, its positive and negative normal counts, and the vector-space coordinate rows used to read them.
\item $T$ is the tubular-coordinate row, recording the admitted local coordinate transformation from the ambient chart to submanifold coordinates together with normal coordinates.
\item $L$ is the Morse--Bott normal-form row, displaying the equation that reads the function as its submanifold value plus the signed square sum in the normal coordinates determined by $Q$ after $T$ has been admitted.
\item $H$ records componentwise $\hsame$ transport for chart, critical-submanifold, normal-bundle, transverse Hessian, tubular-coordinate, normal-form, replay, Real readback, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a normal-form query through $X,S,N,Q,T,L,H,R,P,A$.
\item $R$ is the $\RealUp$ readback row, exposing scalar values only after the chart, submanifold, normal-bundle, transverse Hessian, tubular-coordinate, and quadratic normal-form rows have been displayed.
\item $P$ is the $\Pkg$ provenance over $\MorseBottLemmaUp$, $\MorseLemmaUp$, $\MorseTheoryUp$, $\ManifoldUp$, $\DerivativeUp$, $\RealUp$, $\VecSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $A$ is the local $\NameCert_{\MorseBottLemmaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,S,N,Q,T,L,H,C,R,P,A$. It has no coordinate for a host tubular-neighbourhood theorem, selected atlas, quotient germ equality, global compactness, or ambient smooth-manifold semantics.
\end{definition}

\begin{theorem}[Morse--Bott lemma NameCert obligations]
\label{thm:morse-bott-lemma-namecert-obligations}
For every accepted $\MorseBottLemmaUp$ carrier $B=(X,S,N,Q,T,L,H,C,R,P,A)$, the local $\NameCert_{\MorseBottLemmaUp}$ surface consists exactly of chart-source admission, critical-submanifold exposure, normal-bundle projection, transverse Hessian nondegeneracy, tubular-coordinate admission, quadratic normal-form readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\RealUp$ readback, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:morse-bott-lemma-carrier}. The displayed rows are exactly $X,S,N,Q,T,L,H,C,R,P,A$, so each accepted packet exposes the chart, critical-submanifold, normal-bundle, transverse Hessian, tubular-coordinate, and normal-form data as finite BEDC rows.

The normal-form row $L$ is downstream of the transverse rows. It may be read only after the critical submanifold $S$, the normal-bundle projection $N$, the nondegenerate transverse Hessian row $Q$, and the tubular-coordinate row $T$ have been displayed.

The structural rows $H,C,R,P,A$ transport, replay, read back, source, and name that same finite route. Since no coordinate contains a host tubular-neighbourhood theorem, quotient germ equality, selected atlas, or ambient compactness theorem, the listed rows form the full local NameCert surface.
\end{proof}

\begin{theorem}[Morse--Bott critical-manifold handoff]
\label{thm:morse-bott-lemma-critical-manifold-handoff}
Every accepted $\MorseBottLemmaUp$ normal-form read specializes to the isolated critical-point route of \autoref{ch:concrete-instances-morse-lemma-namecert} when the displayed critical submanifold row is a singleton and the normal-bundle row exhausts the ambient chart directions. In general, the handoff keeps the tangent directions inside the critical-submanifold row and sends only the transverse normal coordinates through the Hessian-signature and quadratic normal-form rows.
\end{theorem}
\begin{proof}
Begin with the rows $S$ and $N$ of \autoref{def:morse-bott-lemma-carrier}. The critical-submanifold row stores the tangent directions along which the function is constant to first order, while the normal-bundle row stores the transverse coordinate carrier.

If $S$ is a singleton and $N$ exhausts the ambient chart directions, the carrier has no remaining tangent directions. The rows $X,N,Q,T,L,H,C,R,P,A$ then present exactly the isolated chart, nondegenerate Hessian, local-coordinate, quadratic normal-form, transport, replay, Real readback, provenance, and naming route used by $\MorseLemmaUp$.

For a positive-dimensional critical submanifold, the tangent rows remain in $S$ and are not sent to the signed square sum. Only the normal coordinates pass through $Q$ and $L$, so the packet records Bott's critical-manifold normal form without collapsing it to an isolated critical point.
\end{proof}

\falsifiablePrediction{Every accepted $\MorseBottLemmaUp$ normal-form read exposes chart, critical-submanifold, normal-bundle, transverse Hessian, tubular-coordinate, quadratic normal-form, componentwise transport, displayed replay, Real readback, provenance, and local NameCert rows before any Morse-theory consumer reads the local model.}

\independenceWitness{morse-lemma, morse-theory, manifold, derivative, real, vecspace: $\MorseBottLemmaUp$ is not bijective to any listed sibling because it jointly carries a critical-submanifold row, normal-bundle projection, transverse Hessian signature, tubular-coordinate row, quadratic normal-form row, transport, replay, Real readback, provenance, and local naming as one finite packet.}

The Lean follow-up namespace is expected at \texttt{BEDC.Derived.MorseBottLemmaUp}. It must preserve the critical-submanifold, normal-bundle, transverse Hessian, tubular-coordinate, normal-form, transport, replay, Real readback, provenance, and local NameCert rows before any checked marker may cite the packet.

\closureat{MorseBottLemmaUp}{seedStr}

\begin{closurestatus}{\MorseBottLemmaUp}
  \constructivestory{A $\MorseBottLemmaUp$ packet is generated from manifold chart rows, critical-submanifold rows, normal-bundle projection rows, transverse Hessian rows, tubular-coordinate rows, quadratic normal-form rows, componentwise transport, displayed replay, Real readback, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:morse-bott-lemma-carrier}, \autoref{thm:morse-bott-lemma-namecert-obligations}, and \autoref{thm:morse-bott-lemma-critical-manifold-handoff} seed the finite critical-manifold normal-form route consumed by Morse-theory packets.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host smooth-manifold Morse--Bott theory, selected tubular neighbourhoods, quotient germs, analytic compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, critical-submanifold exposure, normal-bundle exactness, transverse Hessian coverage, tubular-coordinate admission, normal-form readback, transport stability, replay stability, Real readback, provenance exhaustion, and local naming for BEDC.Derived.MorseBottLemmaUp.}
\end{closurestatus}
