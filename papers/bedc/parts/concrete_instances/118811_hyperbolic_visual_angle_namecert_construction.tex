\chapter{A Concrete Naming Certificate for $\HyperbolicVisualAngleUp$}
\label{ch:concrete-instances-hyperbolic-visual-angle-namecert}
\origin{ai}

The $\HyperbolicVisualAngleUp$ packet realizes the boundary-triple and visual readback surface of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite hyperbolic phase data. It sits after the disk-coordinate surface of \autoref{ch:concrete-instances-unitdisk-namecert}, the metric-read surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary-geodesic packet of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the cross-ratio route of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite visual-angle carrier; it does not complete the disk, quotient boundary triples, select a host geodesic, identify visual angle with physical observation, or import real completeness.

\begin{definition}[Hyperbolic visual-angle carrier]
\label{def:hyperbolic-visual-angle-carrier}
A $\HyperbolicVisualAngleUp$ carrier is a finite $\BHist$ packet
$$
A=(U,T,M,B,X,R,J,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ disk-coordinate row for the observer point and the inspected boundary data.
\item $T$ is the boundary-triple row naming the ordered finite boundary ends used by the visual-angle read.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row over the same displayed disk data.
\item $B$ is the $\HyperbolicGeodesicBoundaryUp$ row that exposes geodesic-boundary incidence for the triple.
\item $X$ is the $\HyperbolicCrossRatioUp$ row carrying the finite cross-ratio comparison attached to the same boundary triple.
\item $R$ is the $\RealUp$ seal row for the angle value, available only after $U,T,M,B,X$ have been displayed.
\item $J$ records classifier stability for disk coordinates, boundary triples, metric reads, geodesic-boundary incidence, cross-ratio data, and the real seal.
\item $H$ records componentwise $\hsame$ transport for $U,T,M,B,X,R,J,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a visual-angle request through boundary triple, hyperbolic metric, geodesic-boundary, cross-ratio, real-seal, transport, provenance, and naming rows.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicVisualAngleUp$, $\UnitDiskUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicCrossRatioUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicVisualAngleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,T,M,B,X,R,J,H,C,P,N$. It has no coordinate for a completed Poincare disk, a Klein-model equality, quotient ideal-boundary equality, selected geodesic representatives, physical observation laws, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Hyperbolic visual-angle NameCert obligations]
\label{thm:hyperbolic-visual-angle-namecert-obligations}
For every accepted $\HyperbolicVisualAngleUp$ carrier $A=(U,T,M,B,X,R,J,H,C,P,N)$, the local $\NameCert_{\HyperbolicVisualAngleUp}$ surface consists exactly of disk-coordinate admission through $U$, boundary-triple admission through $T$, hyperbolic metric-read exactness through $M$, geodesic-boundary incidence through $B$, cross-ratio comparison through $X$, real-seal exposure through $R$, classifier stability through $J$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-visual-angle-carrier}. The accepted rows are exactly $U,T,M,B,X,R,J,H,C,P,N$, so carrier admission exposes disk coordinates, a boundary triple, metric reads, geodesic-boundary incidence, cross-ratio data, a real seal, classifier stability, transport, replay, provenance, and local naming.

The geometric obligations are ordered by the replay row $C$. A visual-angle request first reads the disk-coordinate and boundary-triple data, then the hyperbolic metric and geodesic-boundary incidence, then the cross-ratio comparison, and only then the real-valued seal.

The structural rows preserve that same surface. Classifier stability, componentwise transport, provenance, and local naming cannot add a completed disk, quotient boundary equality, selected geodesic representative, physical observation law, or ambient real-completeness row, so the listed obligations exhaust the local certificate.
\end{proof}

\begin{theorem}[Hyperbolic visual-angle route nonescape]
\label{thm:hyperbolic-visual-angle-route-nonescape}
Every visual-angle read from an accepted $\HyperbolicVisualAngleUp$ packet factors through
$$
\UnitDiskUp \longmapsto \HyperbolicMetricUp \longmapsto \HyperbolicGeodesicBoundaryUp \longmapsto \HyperbolicCrossRatioUp \longmapsto \RealUp
$$
using the displayed rows $U,T,M,B,X,R,J,H,C,P,N$. Thus the packet exposes finite boundary-triple, hyperbolic metric, cross-ratio, real-seal, transport, provenance, and NameCert rows, but it exports no completed disk, quotient boundary triple, selected host geodesic, physical-observation equality, Klein-model equality, or ambient real-completeness principle.
\end{theorem}
\begin{proof}
Start with an accepted visual-angle request. The disk-coordinate row $U$ and boundary-triple row $T$ give the only available source data for the angle read.

Replay the route recorded in $C$. The metric row $M$ and boundary row $B$ make the geodesic incidence visible, the cross-ratio row $X$ carries the boundary comparison, and the real seal $R$ is exposed only after those rows are present.

The classifier, transport, provenance, and naming rows $J,H,P,N$ preserve the same finite packet. Since the carrier lacks any coordinate for completed disk objects, quotient boundary triples, selected host geodesics, physical-observation equality, Klein-model equality, or real completeness, no visual-angle consumer can read such data from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicVisualAngleUp$ read displays a UnitDisk observer row, a boundary-triple row, a HyperbolicMetric row, a HyperbolicGeodesicBoundary row, a HyperbolicCrossRatio row, a RealUp seal, classifier stability, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before exporting a visual-angle value.}

\independenceWitness{unitdisk, hyperbolic-metric, hyperbolic-geodesic-boundary, hyperbolic-cross-ratio, real: $\HyperbolicVisualAngleUp$ is not bijective to any listed sibling because it jointly carries an observer disk coordinate, ordered boundary triple, metric read, geodesic-boundary incidence, cross-ratio comparison, Real seal, classifier stability, transport, replay, provenance, and local NameCert rows.}

Lean \texttt{TasteGate.lean} for this packet will provide \texttt{Nontrivial HyperbolicVisualAngleUp} and \texttt{FieldFaithful HyperbolicVisualAngleUp} instances for the same displayed carrier. Those instances must keep $U,T,M,B,X,R,J,H,C,P,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{HyperbolicVisualAngleUp}{seedStr}

\begin{closurestatus}{\HyperbolicVisualAngleUp}
  \constructivestory{A $\HyperbolicVisualAngleUp$ packet is generated from UnitDiskUp observer rows, boundary-triple rows, HyperbolicMetricUp reads, HyperbolicGeodesicBoundaryUp incidence, HyperbolicCrossRatioUp comparison data, a RealUp seal, classifier stability, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-visual-angle-carrier}, \autoref{thm:hyperbolic-visual-angle-namecert-obligations}, and \autoref{thm:hyperbolic-visual-angle-route-nonescape} seed the finite visual-angle route over boundary triple, hyperbolic metric, geodesic-boundary, cross-ratio, Real seal, transport, provenance, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed disks, quotient boundary triples, selected host geodesics, physical-observation equality, Klein-model equality, ambient real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk-coordinate admission, boundary-triple admission, metric-read exactness, geodesic-boundary incidence, cross-ratio comparison, Real-seal exposure, classifier stability, TasteGate nontriviality, field faithfulness, and local naming for BEDC.Derived.HyperbolicVisualAngleUp.}
\end{closurestatus}
