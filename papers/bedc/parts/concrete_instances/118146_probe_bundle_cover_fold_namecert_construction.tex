\chapter{A Concrete Naming Certificate for $\ProbeBundleCoverFoldUp$}
\label{ch:concrete-instances-probe-bundle-cover-fold-namecert}
\origin{ai}

The $\ProbeBundleCoverFoldUp$ packet realizes the finite cover-fold branch of \autoref{ch:visions-compact-uniform-continuity}. It records the displayed step that folds compact finite-cover probes, local pointwise-modulus rows, and rational lower-bound ledgers into a tolerance-only $\UniformModulusUp$ handoff before any compact-uniform-continuity consumer reads the route. The packet sits beside the finite minimum fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, the compact-net selector of \autoref{ch:concrete-instances-compactnetmodulusselector-namecert}, the compact uniform-continuity carrier of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the compact source of \autoref{ch:concrete-instances-compact-namecert}, the metric source of \autoref{ch:concrete-instances-metric-space-namecert}, the continuous-map source of \autoref{ch:concrete-instances-continuousmap-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. It is a finite $\mathsf{ProbeBundle}$ cover fold, not an open-cover compactness theorem, selected subcover, host modulus oracle, quotient point equality, or ambient $\RealUp$ completeness claim.

\begin{definition}[ProbeBundle cover-fold carrier]
\label{def:probe-bundle-cover-fold-carrier}
A $\ProbeBundleCoverFoldUp$ carrier is a finite $\BHist$ packet
$$
F=(K,M,G,B,R,L,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ finite-cover or finite-net source row.
\item $M$ is the $\MetricSpaceUp$ distance row used to test membership in the displayed cover cells.
\item $G$ is the $\ContinuousMapUp$ pointwise-modulus row attached to each retained probe.
\item $B$ is the finite $\mathsf{ProbeBundle}$ of centers, cover cells, local radii, and pointwise modulus witnesses.
\item $R$ is the rational positive-radius ledger folded across the displayed bundle.
\item $L$ is the lower-bound output row, compatible with $\FiniteNetMinimumFoldUp$ and the $\CompactRadiusChainUp$ cover-chain ledger.
\item $U$ is the tolerance-only $\UniformModulusUp$ handoff row read by compact-uniform-continuity consumers.
\item $H$ records componentwise $\hsame$ transport for $K,M,G,B,R,L,U,C,P,N$.
\item $C$ records displayed $\Cont$ replay from compact cover rows through metric tests, pointwise modulus rows, finite bundle folding, lower-bound output, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\ProbeBundleCoverFoldUp$, $\CompactUp$, $\MetricSpaceUp$, $\ContinuousMapUp$, $\FiniteNetMinimumFoldUp$, $\CompactNetModulusSelectorUp$, $\CompactUniformContinuityUp$, $\CompactRadiusChainUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ProbeBundleCoverFoldUp}$ row.
\end{enumerate}
The carrier has no coordinate for open-cover compactness, selected subcovers outside the displayed bundle, a host modulus function, quotient point equality, countable choice, ambient metric completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[ProbeBundle cover-fold obligations]
\label{thm:probe-bundle-cover-fold-obligations}
For every accepted $\ProbeBundleCoverFoldUp$ carrier $F=(K,M,G,B,R,L,U,H,C,P,N)$, the local $\NameCert_{\ProbeBundleCoverFoldUp}$ surface consists exactly of compact finite-cover exposure, metric membership exposure, continuous pointwise-modulus exposure, finite $\mathsf{ProbeBundle}$ exposure, rational radius-ledger exposure, lower-bound fold output, tolerance-only $\UniformModulusUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:probe-bundle-cover-fold-carrier}. The compact source row $K$, metric row $M$, pointwise-modulus row $G$, finite bundle row $B$, rational radius ledger $R$, lower-bound output $L$, and uniform-modulus row $U$ are the whole mathematical surface.

The fold is finite because $B$ is a displayed $\mathsf{ProbeBundle}$ and $R$ is read only over the probes retained in that bundle. The lower-bound row $L$ is therefore a fold output rather than an external minimum.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no open-cover theorem, selected subcover, host modulus, quotient equality, choice, completeness, Lean theorem, or bridge coordinate, the stated rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[ProbeBundle cover-fold uniform-modulus handoff]
\label{thm:probe-bundle-cover-fold-uniform-modulus-handoff}
Every compact-uniform-continuity consumer that reads a $\UniformModulusUp$ row from an accepted $\ProbeBundleCoverFoldUp$ packet must first read
$$
\CompactUp \longmapsto \MetricSpaceUp \longmapsto \ContinuousMapUp \longmapsto \mathsf{ProbeBundle} \longmapsto \FiniteNetMinimumFoldUp \longmapsto \UniformModulusUp
$$
through $K,M,G,B,R,L,U,H,C,P,N$. The handoff is a finite cover fold over displayed probes; it is not a selected global modulus, open-cover compactness theorem, quotient point equality, countable-choice selector, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:probe-bundle-cover-fold-obligations}. It forces compact-cover data, metric membership tests, pointwise modulus rows, the finite probe bundle, and the rational radius ledger to be displayed before the lower-bound row and the uniform-modulus handoff can be consumed.

The lower-bound step is the finite fold layer. It is compatible with the $\FiniteNetMinimumFoldUp$ and $\CompactNetModulusSelectorUp$ siblings because the consumer sees the same finite probe inventory and the same rational radius ledger before it sees $U$.

The rows $H,C,P,N$ preserve the route as a displayed BEDC packet. No excluded host principle is stored in the carrier, so the consumer receives only the tolerance-only handoff generated by the finite cover fold.
\end{proof}

\begin{theorem}[ProbeBundle cover-fold compact-radius route]
\label{thm:probe-bundle-cover-fold-compact-radius-route}
Every $\CompactRadiusChainUp$ or $\CompactUniformContinuityUp$ consumer of an accepted $\ProbeBundleCoverFoldUp$ carrier must read the same $K,M,G,B,R,L,U$ route before the tolerance-only handoff is public. The compact radius chain is therefore a displayed ledger over the finite probe bundle, not an implicit compactness theorem or a selected cover refinement outside the packet.
\end{theorem}
\begin{proof}
Project $F=(K,M,G,B,R,L,U,H,C,P,N)$. The compact source, metric tests, continuous-map local moduli, finite bundle, radius ledger, lower-bound output, and uniform-modulus row appear as separate coordinates.

Apply \autoref{thm:probe-bundle-cover-fold-uniform-modulus-handoff}. It shows that the uniform-modulus read passes through the finite fold before any compact-uniform-continuity consumer can use it.

The compact radius chain can therefore be read only as provenance for the same finite route. Transport, replay, provenance, and local naming preserve the route but do not add a selected cover refinement or background compactness principle.
\end{proof}

\falsifiablePrediction{If the finite $\mathsf{ProbeBundle}$ cover-fold row is removed, the displayed route cannot derive a tolerance-only $\UniformModulusUp$ handoff from compact finite-cover rows, pointwise modulus rows, and rational lower-bound ledgers inside the same carrier.}

\independenceWitness{finitenetminimumfold, compactnetmodulusselector, uniformmodulus, compactradiuschain: no listed sibling is bijective to $\ProbeBundleCoverFoldUp$ because this packet jointly carries compact cover rows, metric membership tests, pointwise modulus rows, the finite ProbeBundle inventory, rational radius ledgers, lower-bound fold output, tolerance-only UniformModulus handoff, transport, replay, provenance, and local naming.}

The Lean TasteGate file for $\ProbeBundleCoverFoldUp$ must provide explicit $\Nontrivial$ and $\FieldFaithful$ instances before a checked paper marker can cite a carrier-level TasteGate target.

\closureat{ProbeBundleCoverFoldUp}{seedStr}

\begin{closurestatus}{\ProbeBundleCoverFoldUp}
  \constructivestory{A $\ProbeBundleCoverFoldUp$ packet is generated from Compact finite-cover rows, MetricSpace membership rows, ContinuousMap pointwise-modulus rows, finite ProbeBundle rows, rational radius ledgers, lower-bound fold output, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:probe-bundle-cover-fold-carrier}, \autoref{thm:probe-bundle-cover-fold-obligations}, \autoref{thm:probe-bundle-cover-fold-uniform-modulus-handoff}, and \autoref{thm:probe-bundle-cover-fold-compact-radius-route} seed the finite cover-fold route from compact probes to tolerance-only uniform-modulus data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, host modulus functions, quotient point equality, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite ProbeBundle exposure, rational fold exactness, compact-radius routing, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.ProbeBundleCoverFoldUp.}
\end{closurestatus}
