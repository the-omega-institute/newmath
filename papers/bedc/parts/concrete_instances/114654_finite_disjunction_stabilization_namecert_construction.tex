\chapter{A Concrete Naming Certificate for $\FiniteDisjunctionStabilizationUp$}
\label{ch:concrete-instances-finite-disjunction-stabilization-namecert}
\origin{human}

Following Bishop and Bridges/Troelstra constructive analysis, the $\FiniteDisjunctionStabilizationUp$ packet records the finite boundary object used when an inspected real-name route stabilizes a bounded disjunction without importing LLPO, WLPO, Markov principle, trichotomy, or quotient real equality. It sits beside the Ishihara route of \autoref{ch:concrete-instances-ishihara-trick-namecert}, the boundary packets of \autoref{ch:concrete-instances-llpo-boundary-namecert} and \autoref{ch:concrete-instances-markov-principle-boundary-namecert}, the lower-cut surface of \autoref{ch:concrete-instances-semi-decidable-real-cut-namecert}, the finite window route of \autoref{ch:concrete-instances-streamname-namecert}, the regular readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a reviewable finite branch window for real-boundary consumers; it does not choose a branch by a semi-classical principle.

\begin{definition}[FiniteDisjunctionStabilization carrier]
\label{def:finite-disjunction-stabilization-carrier}
A $\FiniteDisjunctionStabilizationUp$ carrier is a finite $\BHist$ packet
$$
S=(B,I,L,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite branch-window row. It names the bounded disjunction entries and the displayed inspection depth.
\item $I$ is the $\IshiharaTrickUp$ inspection row, read only as a finite route over the displayed branch window.
\item $L$ is the lower-cut evidence row supplied through $\SemiDecidableRealCutUp$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row controlling the finite comparison scale.
\item $W$ is the $\StreamNameUp$ schedule row exposing the requested real-name windows.
\item $R$ is the $\RegSeqRatUp$ regular readback row attached to the displayed windows and dyadic tolerance.
\item $E$ is the terminal $\RealUp$ seal row available only after $B,I,L,D,W,R$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for branch windows, Ishihara inspection, lower-cut evidence, dyadic tolerance, stream schedule, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the branch window, inspection row, lower-cut row, tolerance row, schedule row, readback row, and Real seal.
\item $P$ is the $\Pkg$ provenance over $\FiniteDisjunctionStabilizationUp$, $\IshiharaTrickUp$, $\LLPOBoundaryUp$, $\MarkovPrincipleBoundaryUp$, $\SemiDecidableRealCutUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteDisjunctionStabilizationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,I,L,D,W,R,E,H,C,P,N$. It has no coordinate for LLPO, WLPO, Markov principle as an axiom, trichotomy, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[FiniteDisjunctionStabilization NameCert obligations]
\label{thm:finite-disjunction-stabilization-namecert-obligations}
For every accepted $\FiniteDisjunctionStabilizationUp$ carrier $S=(B,I,L,D,W,R,E,H,C,P,N)$, the local $\NameCert_{\FiniteDisjunctionStabilizationUp}$ surface consists exactly of carrier admission for the finite branch window, Ishihara inspection, lower-cut evidence, dyadic tolerance, stream schedule, regular readback, Real seal, componentwise transport, displayed replay, provenance, and local naming rows; branch-window stability under $\hsame$; replay order through $C$; provenance through $P$; and the refusal of semi-classical branch selection outside the displayed finite rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-disjunction-stabilization-carrier}. The analytic rows are $B,I,L,D,W,R,E$, and the structural rows are $H,C,P,N$.

The replay row $C$ forces every read to begin with the finite branch window before it reaches Ishihara inspection, lower-cut evidence, dyadic tolerance, stream windows, regular readback, and the Real seal. Transport by $H$ preserves exactly that order under classifier movement.

The provenance row $P$ names the sibling boundaries only as sources for the displayed route. Since the carrier contains no independent branch selector, excluded-middle row, quotient real equality row, selected limit, choice selector, or completeness row, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[FiniteDisjunctionStabilization branch window]
\label{thm:finite-disjunction-stabilization-branch-window}
Every accepted $\FiniteDisjunctionStabilizationUp$ read factors through the finite branch window $B$ before any Ishihara inspection, lower-cut evidence, dyadic tolerance, stream schedule, regular readback, or Real seal is visible. Thus a consumer may stabilize only the displayed finite disjunction window, not an unbounded search over real names.
\end{theorem}
\begin{proof}
Start from a consumer request. Carrier admission exposes $B$ as the unique branch-window coordinate, and the continuation row $C$ places it before $I,L,D,W,R,E$.

The Ishihara row $I$ is therefore read as an inspection of the displayed finite window, while $L,D,W,R$ supply only the lower-cut, tolerance, schedule, and readback rows needed to make that window meaningful at the requested scale.

Since the Real seal $E$ appears after those rows, no consumer can bypass the finite window by asking for quotient real equality, trichotomy, selected limits, or $\RealUp$ completeness.
\end{proof}

\begin{theorem}[FiniteDisjunctionStabilization real-boundary route]
\label{thm:finite-disjunction-stabilization-real-boundary}
Every Real-facing consumer of an accepted $\FiniteDisjunctionStabilizationUp$ carrier must read the route
$$
B,I,L,D,W,R \longmapsto E \longmapsto H,C,P,N .
$$
The boundary exported to Real and completion-facing chapters is a finite stabilization certificate over displayed cuts, dyadic tolerances, stream windows, and regular readback; it is not LLPO, WLPO, Markov principle as an axiom, trichotomy, quotient real equality, a selected limit, countable choice, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the branch-window theorem first. It confines the request to the displayed finite disjunction window and its Ishihara inspection row.

Next attach the lower-cut and dyadic rows. The lower-cut evidence $L$ and tolerance row $D$ determine which finite stream windows $W$ and regular readback rows $R$ may be inspected before the Real seal is visible.

Finally replay the Real-facing route through $E,H,C,P,N$. The seal records that the displayed finite route is available to downstream consumers; it does not add a semi-classical principle or any ambient completeness object.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteDisjunctionStabilizationUp$ read factors through a finite branch window, Ishihara inspection row, semi-decidable lower-cut evidence, DyadicRatCore tolerance, StreamName schedule, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{ishihara-trick, llpo-boundary, markov-principle-boundary, semi-decidable-real-cut, streamname, regseqrat, dyadicratcore, real: $\FiniteDisjunctionStabilizationUp$ is not bijective to any listed sibling because it jointly carries finite branch-window stabilization, real-boundary inspection, lower-cut evidence, dyadic tolerance, stream and regular readback, a Real seal, transport, replay, provenance, and local naming.}

\closureat{FiniteDisjunctionStabilizationUp}{seedStr}

\begin{closurestatus}{\FiniteDisjunctionStabilizationUp}
  \constructivestory{A $\FiniteDisjunctionStabilizationUp$ packet is generated from a finite branch window, Ishihara inspection row, semi-decidable lower-cut evidence, DyadicRatCore tolerance row, StreamName schedule, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-disjunction-stabilization-carrier}, \autoref{thm:finite-disjunction-stabilization-namecert-obligations}, \autoref{thm:finite-disjunction-stabilization-branch-window}, and \autoref{thm:finite-disjunction-stabilization-real-boundary} seed the finite real-boundary route for disjunction stabilization.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close LLPO, WLPO, Markov principle as an axiom, trichotomy, quotient real equality, selected limits, countable choice, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, branch-window stability, Ishihara inspection routing, lower-cut and dyadic tolerance accounting, stream and regular readback, Real-seal nonescape, and structural replay for BEDC.Derived.FiniteDisjunctionStabilizationUp.}
\end{closurestatus}
