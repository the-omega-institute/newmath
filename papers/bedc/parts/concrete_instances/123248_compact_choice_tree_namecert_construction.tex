\chapter{A Concrete Naming Certificate for $\CompactChoiceTreeUp$}
\label{ch:concrete-instances-compact-choice-tree-namecert}
\origin{human}

Following Brouwer's fan theorem and Bishop--Bridges constructive compactness, the $\CompactChoiceTreeUp$ packet records the finite binary choice-tree surface used by compact Cantor-space and completion-modulus consumers. It sits after the Cantor-space carrier of \autoref{ch:concrete-instances-cantorspace-namecert}, the fan-theorem bar route of \autoref{ch:concrete-instances-fantheorem-namecert}, the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet exposes a finite tree of inspected choices and its boundary rows; it is not open-cover compactness, not countable choice, not a selected infinite branch, and not an ambient completeness theorem.

\begin{definition}[Compact choice-tree carrier]
\label{def:compact-choice-tree-carrier}
A $\CompactChoiceTreeUp$ carrier is a finite $\BHist$ packet
$$
T=(C,F,S,R,E,B,D,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\CantorSpaceUp$ source row naming the binary prefix surface whose finite cylinders are inspected.
\item $F$ is the $\FanTheoremUp$ bar-bound row naming the finite fan depth that bounds all accepted prefix reads.
\item $S$ is the $\StreamNameUp$ finite-window row recording the inspected prefix schedule.
\item $R$ is the $\RegSeqRatUp$ readback row reached from the displayed stream windows.
\item $E$ is the terminal $\RealUp$ seal row exposed only after $C,F,S,R$ are displayed.
\item $B$ is the finite branch-frontier row, listing the accepted prefix leaves below the fan bound.
\item $D$ is the dyadic tolerance and endpoint ledger used when a completion consumer reads the tree as a modulus witness.
\item $H$ records componentwise $\hsame$ transport for $C,F,S,R,E,B,D,K,P,N$.
\item $K$ records displayed $\Cont$ replay for the Cantor-source read, fan-bound read, stream-window read, regular readback, Real seal, branch frontier, dyadic ledger, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\CompactChoiceTreeUp$, $\CantorSpaceUp$, $\FanTheoremUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactChoiceTreeUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite branch selector, quotient equality of streams, countable choice over prefixes, open-cover compactness, selected limits, host equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact choice-tree NameCert obligations]
\label{thm:compact-choice-tree-namecert-obligations}
For the carrier of \autoref{def:compact-choice-tree-carrier}, the local $\NameCert_{\CompactChoiceTreeUp}$ surface consists exactly of carrier admission for $T=(C,F,S,R,E,B,D,H,K,P,N)$, Cantor-source exposure through $C$, fan-bound exposure through $F$, finite stream-window exposure through $S$, regular rational readback through $R$, terminal Real sealing through $E$, finite branch-frontier exposure through $B$, dyadic tolerance exposure through $D$, componentwise transport through $H$, continuation replay through $K$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:compact-choice-tree-carrier}. The Cantor-space row $C$ and fan-theorem row $F$ are the only compactness-facing source rows, and the branch frontier $B$ is bounded by the displayed fan depth rather than by an open-cover argument.

Read the completion-facing route next. The stream row $S$ names the inspected finite prefixes, the regular readback row $R$ consumes those windows, the dyadic ledger $D$ records the tolerance data, and the Real seal $E$ is exposed only after those finite rows have been displayed.

Finally use $H,K,P,N$. These rows preserve the same finite route under transport, replay, provenance, and local naming. Since the packet has no coordinate for infinite branch selection, quotient stream equality, countable choice, open-cover compactness, selected limits, host equality, or ambient Real completeness, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Compact choice-tree completion handoff]
\label{thm:compact-choice-tree-completion-handoff}
Every completion-facing consumer of an accepted $\CompactChoiceTreeUp$ packet must first read the finite route
$$
\CantorSpaceUp,\ \FanTheoremUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
through the displayed rows $C,F,S,R,E,B,D,H,K,P,N$. The handoff supplies finite choice-tree and dyadic tolerance data for L10 compactness and completion-modulus consumers, not open-cover compactness, not countable choice, not selected infinite branches, and not ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:compact-choice-tree-namecert-obligations}. They force the Cantor source, fan-bound, finite stream windows, regular readback, Real seal, branch frontier, dyadic ledger, transport, replay, provenance, and local naming rows to be read as one packet.

The rows $C,F,B$ bind compactness to finite prefix data. The stream and regular rows $S,R$ then route that finite prefix surface into regular rational readback, while $D$ carries the dyadic tolerance ledger consumed by completion-modulus readers.

The terminal row $E$ is downstream of these finite reads. The structural rows $H,K,P,N$ keep the same route available to consumers without adding a second seal or an external branch choice. Thus the packet supplies the finite compact-choice-tree handoff and excludes open-cover compactness, countable choice, quotient stream equality, selected limits, and ambient completeness.
\end{proof}

\falsifiablePrediction{Any accepted CompactChoiceTreeUp packet that exposes a completion-facing compactness handoff while omitting the Cantor-space row, fan-bound row, finite StreamName windows, RegSeqRat readback, Real seal, branch frontier, dyadic tolerance ledger, transport, replay, provenance, or local naming rows refutes the packet.}

\independenceWitness{cantorspace, fantheorem, streamname, regseqrat, real: $\CompactChoiceTreeUp$ is not bijective to any listed sibling because it jointly carries Cantor prefix data, a fan-bound row, a finite branch frontier, completion-facing dyadic tolerance data, stream windows, regular readback, Real sealing, transport, replay, provenance, and local naming in one packet.}

\closureat{CompactChoiceTreeUp}{seedStr}

\begin{closurestatus}{\CompactChoiceTreeUp}
  \constructivestory{A $\CompactChoiceTreeUp$ packet is generated from CantorSpace prefix rows, FanTheorem finite-bound rows, StreamName finite-window rows, RegSeqRat readback rows, a RealUp seal, a finite branch frontier, DyadicRatCore tolerance data, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-choice-tree-carrier}, \autoref{thm:compact-choice-tree-namecert-obligations}, and \autoref{thm:compact-choice-tree-completion-handoff} seed the finite compact-choice-tree route from Cantor prefixes and fan bounds to completion-facing Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, countable choice, quotient stream equality, selected infinite branches, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Cantor-source exposure, fan-bound exposure, finite-window exactness, regular readback, Real seal nonescape, dyadic tolerance stability, transport, replay, provenance, and local naming for BEDC.Derived.CompactChoiceTreeUp.}
\end{closurestatus}
