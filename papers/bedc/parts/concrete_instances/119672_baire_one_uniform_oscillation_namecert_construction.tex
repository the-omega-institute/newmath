\chapter{A Concrete Naming Certificate for $\BaireOneUniformOscillationUp$}
\label{ch:concrete-instances-baire-one-uniform-oscillation-namecert}
\origin{human}

Following Baire and Bishop--Bridges, $\BaireOneUniformOscillationUp$ records the finite observation surface by which a Baire-one pointwise-limit row is inspected through uniform oscillation windows before a Real-and-Completion consumer reads a terminal real seal. The packet sits after the Baire-one function surface of \autoref{ch:concrete-instances-baireonefunction-namecert}, the real oscillation surface of \autoref{ch:concrete-instances-real-oscillation-namecert}, the compact uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the finite stream-name windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It names only the finite row discipline for oscillation windows over a pointwise-limit presentation; it does not assert uniform convergence, choose a subsequence, form quotient function equality, import compactness by open covers, or use ambient $\RealUp$ completeness.

\begin{definition}[Baire-one uniform oscillation carrier]
\label{def:baire-one-uniform-oscillation-carrier}
A $\BaireOneUniformOscillationUp$ carrier is a finite $\BHist$ packet
$$
U=(B,O,K,W,Q,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BaireOneFunctionUp$ pointwise-limit row naming the approximating sequence and the inspected input windows.
\item $O$ is the $\RealOscillationUp$ row carrying lower and upper oscillation probes for the same displayed real-valued data.
\item $K$ is the $\CompactUniformContinuityUp$ row that bounds which compact-window and uniform-modulus requests may be replayed.
\item $W$ is the $\StreamNameUp$ finite-window schedule for the pointwise approximants and oscillation probes.
\item $Q$ is the $\RegSeqRatUp$ readback row for the rational tolerances attached to those windows.
\item $T$ is the terminal $\RealUp$ seal reached after the pointwise-limit, oscillation, uniform-window, stream, and regular-rational rows have been displayed.
\item $H$ records componentwise $\hsame$ transports for $B,O,K,W,Q,T$ and the structural rows.
\item $C$ records the displayed $\Cont$ replay route through the same finite window data.
\item $P$ is the $\Pkg$ provenance over $\BaireOneUniformOscillationUp$, $\BaireOneFunctionUp$, $\RealOscillationUp$, $\CompactUniformContinuityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BaireOneUniformOscillationUp}$ row.
\end{enumerate}
The carrier has no coordinate for host function equality, selected subsequences, uniform convergence of the approximating sequence, open-cover compactness, quotient stream equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Baire-one uniform oscillation obligations]
\label{thm:baire-one-uniform-oscillation-obligations}
For an accepted $\BaireOneUniformOscillationUp$ carrier $U=(B,O,K,W,Q,T,H,C,P,N)$, the local $\NameCert_{\BaireOneUniformOscillationUp}$ surface consists exactly of six finite obligations: carrier formation over the displayed rows; classifier stability under componentwise $\hsame$ transport; pointwise-limit source compatibility with the $\BaireOneFunctionUp$ row; oscillation-window compatibility with the $\RealOscillationUp$ row; compact uniform-window replay through $\CompactUniformContinuityUp$, $\StreamNameUp$, and $\RegSeqRatUp$; and terminal $\RealUp$ seal non-escape.
\end{theorem}
\begin{proof}
Carrier formation is projection from \autoref{def:baire-one-uniform-oscillation-carrier}. The packet displays the pointwise-limit source, oscillation probes, compact uniform-window row, stream schedule, regular-rational readback, terminal Real seal, transports, replay, provenance, and local name.

Classifier stability is rowwise. Transport along $H$ may replace the visible rows only by rows with the same pointwise-limit source, oscillation probes, uniform-window requests, stream windows, rational tolerances, terminal seal, replay order, provenance, and local name.

The source and oscillation obligations are the compatibility rows $B$ and $O$. A consumer cannot inspect oscillation width without first displaying the Baire-one pointwise-limit row and the real oscillation probes over the same finite windows.

The uniform-window obligation is the route through $K,W,Q$. It confines compact-uniform requests to the displayed window schedule and rational readback, so the packet exports a finite uniform oscillation window rather than open-cover compactness or uniform convergence of the approximants.

Finally, $T$ is terminal. Since no carrier row stores a selected subsequence, quotient function equality, quotient stream equality, or ambient completeness principle, the $\RealUp$ seal is the only value-facing endpoint exported by the packet.
\end{proof}

\begin{theorem}[Baire-one uniform oscillation window route]
\label{thm:baire-one-uniform-oscillation-window-route}
Every Real-and-Completion consumer of an accepted $\BaireOneUniformOscillationUp$ packet reads the rows
$$
B,O,K,W,Q,T,H,C,P,N
$$
in that order before treating a Baire-one pointwise-limit surface as controlled by finite uniform oscillation windows. In particular, the consumer must pass through the $\BaireOneFunctionUp$ source, $\RealOscillationUp$ probe row, $\CompactUniformContinuityUp$ window row, $\StreamNameUp$ schedule, $\RegSeqRatUp$ tolerance readback, and terminal $\RealUp$ seal before exporting any oscillation-window conclusion.
\end{theorem}
\begin{proof}
Begin with the source row $B$. It is the only displayed entry point for the Baire-one pointwise-limit presentation, so an oscillation-window consumer has no independent host function from which to read the limit.

Next read $O,K,W,Q$. The oscillation probes are tied to the same finite stream windows, and the compact-uniform row only bounds replay through those windows and rational tolerances.

Only after those rows have been displayed may the consumer read $T$. The structural rows $H,C,P,N$ then transport, replay, source, and name the same route, so no quotient stream equality, selected subsequence, open-cover compactness proof, uniform-convergence assertion, or ambient $\RealUp$ completeness row can replace the finite window route.
\end{proof}

\begin{closurestatus}{\BaireOneUniformOscillationUp}
  \constructivestory{A $\BaireOneUniformOscillationUp$ packet is generated from a BaireOneFunction pointwise-limit row, RealOscillation probes, CompactUniformContinuity window data, StreamName schedules, RegSeqRat tolerance readbacks, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:baire-one-uniform-oscillation-carrier}, \autoref{thm:baire-one-uniform-oscillation-obligations}, and \autoref{thm:baire-one-uniform-oscillation-window-route} seed the finite uniform-oscillation window route for Baire-one pointwise-limit rows over the L10 Real route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, uniform convergence of the approximating sequence, selected subsequences, quotient function equality, quotient stream equality, open-cover compactness, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier formation, classifier stability, source compatibility, oscillation-window compatibility, compact uniform-window replay, and terminal Real seal non-escape for the displayed packet.}
\end{closurestatus}
