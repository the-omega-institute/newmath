\chapter{A Concrete Naming Certificate for $\PrimeShadowInterfaceUp$}
\label{ch:concrete-instances-prime-shadow-interface-namecert}
\origin{ai}

The $\PrimeShadowInterfaceUp$ packet realizes the bulk--boundary prime-shadow vision of \autoref{ch:visions-rh-bulk-boundary-prime-shadow-interface} as finite NameCert data. It sits after the arithmetic prime certificate of \autoref{ch:concrete-instances-prime-namecert}, the finite prime-window gluing route of \autoref{ch:concrete-instances-finite-prime-window-gluing-namecert}, the zeta source of \autoref{ch:concrete-instances-zeta-basic-namecert}, the zeta-zero surface of \autoref{ch:concrete-instances-zeta-zeros-namecert}, the zeta zero type factorization of \autoref{ch:concrete-instances-zeta-zero-type-factorization-namecert}, and the critical-line witness route of \autoref{ch:concrete-instances-criticallinewitness-namecert}. The packet names only a finite interface row: it does not prove RH, does not discharge the analytic incompatibility row, and does not assert zero-free strips, Euler product convergence on the critical strip, quotient complex equality, or ambient analytic completeness.

\begin{definition}[PrimeShadowInterface carrier]
\label{def:prime-shadow-interface-carrier}
A $\PrimeShadowInterfaceUp$ carrier is a finite $\BHist$ packet
$$
S=(P,W,Z,A,D,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\PrimeUp$ row naming the prime classifier and the local prime-invariant read used by the boundary shadow.
\item $W$ is the $\FinitePrimeWindowGluingUp$ row recording the finite prime window through which boundary-visible prime data is inspected.
\item $Z$ is the $\ZetaUp$ and $\ZetaZerosUp$ source row, retaining only the displayed zeta and zero-locus surfaces.
\item $A$ is the $\ZetaZeroTypeFactorizationUp$ row that connects a legal zero-facing type read to the finite zeta-zero surface.
\item $D$ is the prime-shadow defect row: it records the layer index, finite readout, nonzero defect witness, and refusal of a hidden bulk invariant.
\item $H$ records componentwise $\hsame$ transport for prime, window, zeta, zero-factor, defect, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for the finite prime-shadow boundary read through $P,W,Z,A,D,H,G,N$.
\item $G$ is the $\Pkg$ provenance over $\PrimeShadowInterfaceUp$, $\PrimeUp$, $\FinitePrimeWindowGluingUp$, $\ZetaUp$, $\ZetaZerosUp$, $\ZetaZeroTypeFactorizationUp$, $\CriticalLineWitnessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PrimeShadowInterfaceUp}$ row.
\end{enumerate}
The carrier has no coordinate for an RH proof, a global zero classifier, a completed Euler product, analytic continuation outside the displayed sibling rows, quotient equality of complex data, or an ambient completeness principle.
\end{definition}

\begin{theorem}[PrimeShadowInterface NameCert obligations]
\label{thm:prime-shadow-interface-namecert-obligations}
For every accepted $\PrimeShadowInterfaceUp$ carrier $S=(P,W,Z,A,D,H,C,G,N)$, the local $\NameCert_{\PrimeShadowInterfaceUp}$ surface consists exactly of prime-row admission, finite prime-window admission, zeta-source admission, zeta-zero factorization admission, prime-shadow defect exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The obligation surface may cite \autoref{ch:visions-rh-bulk-boundary-prime-shadow-interface}, but it exports only these finite boundary rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:prime-shadow-interface-carrier}. The rows $P,W,Z,A,D$ are the complete mathematical surface of the packet: prime classifier data, a finite prime window, zeta and zero-facing rows, zero-type factorization, and a displayed defect read.

Use the structural rows $H,C,G,N$. They transport, replay, package, and name the same finite boundary interface, so the local certificate cannot introduce an analytic theorem beyond the displayed siblings.

The RH vision is therefore consumed only as a target diagram. The packet supplies a concrete boundary interface for that diagram, not an incompatibility proof or an RH conclusion.
\end{proof}

\begin{theorem}[PrimeShadowInterface finite boundary route]
\label{thm:prime-shadow-interface-finite-boundary-route}
Every RH-facing consumer of an accepted $\PrimeShadowInterfaceUp$ packet must read the finite prime-shadow boundary through
$$
\PrimeUp,\ \FinitePrimeWindowGluingUp,\ \ZetaUp,\ \ZetaZerosUp,\ \ZetaZeroTypeFactorizationUp
\longmapsto
\CriticalLineWitnessUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The route keeps bulk--boundary prime-shadow detection finite and local. It does not claim RH, analytic defect-zero incompatibility, a zero-free strip theorem, completed Euler products on the critical strip, quotient complex equality, or host analytic completeness.
\end{theorem}
\begin{proof}
Start from $P$ and $W$. The prime-facing side is a displayed prime classifier read plus a finite prime window, so the consumer receives only finite boundary-visible prime data.

Read $Z$ and $A$ next. These rows expose the zeta source and zeta-zero type factorization before any critical-line consumer can treat a zero-facing row as boundary data.

Finally preserve the route through $D,H,C,G,N$. The defect row is local to the same packet, while transport, replay, provenance, and naming keep all reads inside the finite interface. The refused analytic claims are absent from the carrier and therefore remain outside the exported boundary route.
\end{proof}

\falsifiablePrediction{If the finite prime-window row $W$ or the defect row $D$ is removed, an RH-facing consumer can no longer distinguish a visible prime-shadow defect from an untracked zeta-zero read.}

\independenceWitness{Nearest siblings: prime, finite-prime-window-gluing, zeta-basic, zeta-zeros, criticallinewitness. None carries the combined boundary interface because each sibling supplies only the prime classifier, finite window, zeta source, zero surface, or critical-line witness, while $\PrimeShadowInterfaceUp$ binds them to one defect-read packet.}

The Lean follow-up for namespace $\mathsf{BEDC.Derived.PrimeShadowInterfaceUp.TasteGate}$ must provide explicit $\mathsf{Nontrivial}\ \PrimeShadowInterfaceUp$ and $\mathsf{FieldFaithful}\ \PrimeShadowInterfaceUp$ instances before a checked carrier-alignment theorem can serve as the TasteGate witness. Those instances must preserve $P,W,Z,A,D,H,C,G,N$ as distinct fields.

\closureat{PrimeShadowInterfaceUp}{seedStr}

\begin{closurestatus}{\PrimeShadowInterfaceUp}
  \constructivestory{A $\PrimeShadowInterfaceUp$ packet is generated from a Prime row, finite prime-window gluing, zeta source rows, zeta-zero factorization rows, a prime-shadow defect read, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:prime-shadow-interface-carrier}, \autoref{thm:prime-shadow-interface-namecert-obligations}, and \autoref{thm:prime-shadow-interface-finite-boundary-route} seed the finite prime-shadow boundary interface over Prime, finite prime-window, Zeta, ZetaZeros, zero-factorization, critical-line, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RH, analytic defect-zero incompatibility, zero-free strips, completed Euler products on the critical strip, quotient complex equality, ambient analytic completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, prime-window exactness, zeta-zero factorization readback, local defect exposure, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.PrimeShadowInterfaceUp.}
\end{closurestatus}
