\chapter{A Concrete Naming Certificate for $\DoobSurvivalChainUp$}
\label{ch:concrete-instances-doob-survival-chain-namecert}
\origin{ai}

The $\DoobSurvivalChainUp$ packet realizes the Doob-conditioned survival-chain data of \autoref{def:philosophy-doob-conditioned-survival-dynamics} and \autoref{thm:philosophy-six-state-doob-transition-matrix} as finite NameCert rows. It records the killed transition matrix, Perron vector, Doob transform, stationary ledger, drift ledger, transport, replay, provenance, and local naming rows without identifying the packet with $\DoobUpcrossingUp$, $\SpectralStabilizationUp$, $\CodonTransitionMatrixUp$, or $\MartingaleConvergenceUp$.

The survival-chain packet consumes the crossing-boundary sibling \autoref{ch:concrete-instances-doob-upcrossing-namecert}, the finite transition ledger of \autoref{ch:concrete-instances-codon-transition-matrix-namecert}, and the stabilization boundary of \autoref{ch:concrete-instances-spectral-stabilization-namecert} before its killed-transition, Perron, Doob-transform, stationary, and drift rows are read.

\begin{definition}[Doob survival-chain carrier]
\label{def:doob-survival-chain-carrier}
A $\DoobSurvivalChainUp$ carrier is a finite $\BHist$ packet
$$
S=(K,V,T,\Pi,D,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the killed transition row for the finite median-closure state set and its displayed boundary refusal;
\item $V$ is the positive Perron-vector row used by the conditioned survival read;
\item $T$ is the Doob-transform row computing the conditioned transition entries from $K$ and $V$;
\item $\Pi$ is the stationary-ledger row for the conditioned chain;
\item $D$ is the drift-ledger row recording the displayed radial drift and bottleneck reads;
\item $H$ records componentwise $\hsame$ transport for $K,V,T,\Pi,D$ and the local naming rows;
\item $C$ records the displayed $\Cont$ replay from killed transition data through Perron normalization, Doob transformation, stationary mass, and drift reads;
\item $P$ is the $\Pkg$ provenance over the source vision rows, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\DoobSurvivalChainUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite survival conditioning theorem, an unbounded martingale argument, a spectral-stabilization limit, or a codon-transition classifier outside the displayed finite rows.
\end{definition}

\begin{theorem}[Doob survival-chain NameCert obligations]
\label{thm:doob-survival-chain-namecert-obligations}
For an accepted $\DoobSurvivalChainUp$ carrier $S=(K,V,T,\Pi,D,H,C,P,N)$, the local $\NameCert_{\DoobSurvivalChainUp}$ surface consists exactly of carrier admission for the killed transition row, Perron-vector row, Doob-transform row, stationary ledger, drift ledger, componentwise transport, continuation replay, provenance, and local naming. Every public survival-chain read factors through
$$
K,V,T,\Pi,D,H,C,P,N
$$
in that order.
\end{theorem}
\begin{proof}
Project the accepted carrier. The killed transition row $K$ is the only row that names the finite killed walk, and the Perron row $V$ is the only positive eigenvector data available to the conditioned survival read.

The Doob transform is then forced through $T$. It reads $K$ and $V$ before any conditioned transition entry can be consumed.

The stationary and drift ledgers are downstream rows. The row $\Pi$ records stationary mass for the conditioned chain, while $D$ records the radial drift and bottleneck reads obtained from the same finite transition surface.

Finally $H,C,P,N$ preserve and name the same route. Since no additional coordinate stores an infinite conditioning principle, martingale-convergence theorem, spectral limit, or codon-transition classifier, the NameCert surface is exactly the displayed finite survival-chain route.
\end{proof}

\falsifiablePrediction{Every public $\DoobSurvivalChainUp$ survival-chain read factors through killed transition, Perron vector, Doob transform, stationary ledger, drift ledger, componentwise transport, displayed replay, provenance, and local naming rows.}

\independenceWitness{doob-upcrossing, spectral-stabilization, codon-transition-matrix, martingale-convergence: $\DoobSurvivalChainUp$ is not bijective to these siblings because it jointly carries killed transition data, positive Perron normalization, conditioned transition entries, stationary mass, drift reads, replay, provenance, and local naming; the listed siblings expose crossing bounds, stabilization routes, codon transition rows, or convergence boundaries without this exact finite survival-chain carrier.}

Lean follow-up must provide $\mathsf{Nontrivial}\ \DoobSurvivalChainUp$ and $\mathsf{FieldFaithful}\ \DoobSurvivalChainUp$ instances for the displayed carrier in the future namespace $\mathsf{BEDC.Derived.DoobSurvivalChainUp}$.

\closureat{DoobSurvivalChainUp}{seedStr}

\begin{closurestatus}{\DoobSurvivalChainUp}
  \constructivestory{A $\DoobSurvivalChainUp$ packet is generated from killed transition rows, Perron-vector data, Doob-transform rows, stationary ledgers, drift ledgers, componentwise transport, displayed replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:doob-survival-chain-carrier} and \autoref{thm:doob-survival-chain-namecert-obligations} seed the finite Doob-conditioned survival-chain surface over killed transition, Perron, transform, stationary, and drift rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close infinite survival conditioning, unbounded martingale convergence, spectral-stabilization limits, codon-transition classifier equivalence, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, killed-transition exposure, Perron-vector positivity, Doob-transform exactness, stationary-ledger exactness, drift-ledger exactness, and TasteGate field faithfulness for BEDC.Derived.DoobSurvivalChainUp.}
\end{closurestatus}
