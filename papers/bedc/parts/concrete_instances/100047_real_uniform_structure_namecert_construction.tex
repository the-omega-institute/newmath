\chapter{A Concrete Naming Certificate for $\RealUniformStructureUp$}
\label{ch:concrete-instances-real-uniform-structure-namecert}
\origin{human}

Following Bourbaki and Kelley on uniform spaces and Bishop--Bridges constructive analysis, the $\RealUniformStructureUp$ packet names the uniformity induced by the displayed $\RealUp$ metric surface through finite metric-ball entourage requests. It sits between the endpoint and seal row of \autoref{ch:concrete-instances-real-namecert}, the distance witness row of \autoref{ch:concrete-instances-metric-namecert}, the entourage discipline of \autoref{ch:concrete-instances-uniformspace-namecert}, and the completion-facing Cauchy-filter route of \autoref{ch:concrete-instances-cauchyfilter-namecert}. The chapter records the Real-induced uniform structure as a finite NameCert boundary; it does not assert completeness of $\RealUp$ or choose limits of Cauchy data.

\begin{definition}[Real uniform-structure carrier]
\label{def:real-uniform-structure-carrier}
A $\RealUniformStructureUp$ carrier is a finite $\BHist$ packet
$$
U_{\Real}=(R,M,U,F,D,S,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ endpoint and seal row whose named points supply the source of every ball request.
\item $M$ is the $\MetricUp$ distance witness row read from the Real metric surface before any entourage is generated.
\item $U$ is the $\UniformSpaceUp$ entourage row generated by positive dyadic radius requests around the displayed metric balls.
\item $F$ is the $\CauchyFilterUp$ compatibility row for completion-facing consumers that read Cauchy data through the induced entourage surface.
\item $D$ is the $\DyadicRatCoreUp$ positive-radius ledger used by the metric-ball generator.
\item $S$ is the $\StreamNameUp$ finite-window row inspected by Real and Cauchy-filter consumers.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback row attached to those windows.
\item $H$ records componentwise $\hsame$ transport for the Real, metric, uniform, Cauchy-filter, dyadic, window, readback, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Real endpoint request through distance, positive-radius selection, ball-entourage generation, Cauchy-filter compatibility, finite windows, and regular readback.
\item $P$ is the $\Pkg$ provenance over $\RealUniformStructureUp$, $\RealUp$, $\MetricUp$, $\UniformSpaceUp$, $\CauchyFilterUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealUniformStructureUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,M,U,F,D,S,Q,H,C,P,N$. The carrier has no coordinate for quotient real equality, selected limits, trichotomy, countable choice, open-cover compactness, or ambient Real completeness.
\end{definition}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Real uniform-structure NameCert obligations]
\label{thm:real-uniform-structure-namecert-obligations}
For every accepted $\RealUniformStructureUp$ carrier $U_{\Real}=(R,M,U,F,D,S,Q,H,C,P,N)$, the local $\NameCert_{\RealUniformStructureUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Real endpoint and seal row, metric distance witness, uniform entourage row, Cauchy-filter compatibility row, dyadic radius ledger, StreamName windows, RegSeqRat readback, transport, replay, provenance, and local naming row;
\item metric-ball entourage generation, requiring every entourage request in $U$ to be generated from $M$ by a positive dyadic radius read from $D$ around endpoints displayed by $R$;
\item Cauchy-filter compatibility, requiring every completion-facing Cauchy-filter read in $F$ to consume the induced entourage row $U$ before inspecting stream windows or regular rational readbacks;
\item classifier transport, requiring componentwise $\hsame$ transport $H$ and displayed $\Cont$ replay $C$ to preserve the route $R,M,D,U,F,S,Q$;
\item ledger non-escape, requiring $P$ and $N$ to export no quotient real equality, selected limit, trichotomy, countable-choice selector, open-cover compactness, or ambient Real completeness.
\end{enumerate}
No NameCert obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-uniform-structure-carrier}. The projection exposes exactly the Real endpoint and seal row, the metric distance witness, the generated uniform entourage row, the Cauchy-filter compatibility row, the dyadic radius ledger, the finite-window and readback rows, and the structural rows $H,C,P,N$.

Metric-ball entourage generation is forced by the displayed route. An entourage request must first read endpoints from $R$, then read the distance witness in $M$, and then choose a positive dyadic radius from $D$ before it can be recorded in the uniform row $U$.

Cauchy-filter compatibility is downstream of the induced uniformity. The row $F$ is available only as a consumer of $U$, and the finite observations $S$ and $Q$ are read after that uniform row has been displayed.

Transport and replay are structural. Componentwise $\hsame$ in $H$ preserves each displayed coordinate, and $\Cont$ replay in $C$ repeats the same route $R,M,D,U,F,S,Q$ for every accepted request.

Finally, provenance is exhausted by $P$ and $N$. Since quotient real equality, selected limits, trichotomy, countable choice, open-cover compactness, and ambient Real completeness are not rows of $U_{\Real}$, the local NameCert surface cannot export them.
\end{proof}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCarrier\_namecert\_obligations}

\begin{theorem}[Real metric uniformity handoff]
\label{thm:real-uniform-structure-metric-uniformity-handoff}
Every completion-facing or Cauchy-filter consumer of an accepted $\RealUniformStructureUp$ carrier reads the Real uniformity only through the displayed rows
$$
\RealUp,\MetricUp,\UniformSpaceUp,\CauchyFilterUp,\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\BHist,\hsame,\Cont,\Pkg,\NameCert .
$$
Consequently the packet exports no quotient real equality, selected limit, trichotomy, countable-choice selector, open-cover compactness, or ambient Real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCarrier\_metric\_uniformity\_handoff}
\begin{proof}
Begin with a completion-facing or Cauchy-filter consumer. Carrier admission gives the consumer access to the local packet only through $R,M,U,F,D,S,Q,H,C,P,N$.

The Real metric uniformity is therefore read through the displayed metric-ball route. The Real row $R$ supplies endpoint names, the metric row $M$ supplies distance evidence, the dyadic row $D$ supplies positive radii, and the uniform row $U$ records the generated entourage.

Any Cauchy-filter use is then a downstream read of $F$. The window row $S$ and the regular rational row $Q$ expose finite observations only after $U$ and $F$ have been replayed.

The remaining rows do not add mathematical primitives. The $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows transport, replay, source, and name the same finite route, so the packet has no separate place from which quotient real equality, selected limits, trichotomy, countable choice, open-cover compactness, or ambient Real completeness can be exported.
\end{proof}

\begin{theorem}[Real uniform-structure L10 finite-source gate]
\label{thm:real-uniform-structure-l10-gate}
Every L10 consumer of an accepted $\RealUniformStructureUp$ carrier must inspect the $\RealUp$ endpoint and seal row, the $\MetricUp$ distance row, the $\DyadicRatCoreUp$ positive-radius ledger, the $\StreamNameUp$ finite-window row, and the $\RegSeqRatUp$ readback row before any $\UniformSpaceUp$ or $\CauchyFilterUp$ handoff is available. No quotient real equality, selected limit, open-cover compactness, or $\RealUp$ completeness row is exported by this gate.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCarrier\_l10\_gate}
\begin{proof}
Project the carrier rows of \autoref{def:real-uniform-structure-carrier}. An L10 consumer can see the packet only through $R,M,U,F,D,S,Q,H,C,P,N$.

The finite-source gate is the displayed order of inspection. The Real endpoint and seal row $R$ supplies named endpoints, $M$ supplies distance observations, $D$ supplies positive dyadic radii, $S$ supplies finite windows, and $Q$ supplies regular rational readback.

Only after those rows have been displayed can the existing metric-uniformity route of \autoref{thm:real-uniform-structure-metric-uniformity-handoff} expose $U$ or $F$. The NameCert obligations of \autoref{thm:real-uniform-structure-namecert-obligations} then force the same route through $H,C,P,N$.

Since quotient real equality, selected limits, open-cover compactness, and $\RealUp$ completeness are not carrier rows, the L10 gate has no coordinate from which to export them.
\end{proof}

\begin{theorem}[Real uniform-structure located-filter compatibility]
\label{thm:real-uniform-structure-located-filter-compatibility}
Every $\LocatedCauchyFilterUp$ consumer of the Real-induced uniformity must read the $\RealUp$ endpoint row $R$, metric row $M$, dyadic radius ledger $D$, uniform entourage row $U$, Cauchy-filter row $F$, $\StreamNameUp$ row $S$, $\RegSeqRatUp$ row $Q$, and structural rows $H,C,P,N$ before located-tail data is interpreted.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCarrier\_located\_filter\_compatibility}
\begin{proof}
Project the carrier of \autoref{def:real-uniform-structure-carrier}. The Real-induced uniformity is available only through $R,M,U,F,D,S,Q,H,C,P,N$, so a located-filter consumer has no independent row from which to interpret located-tail data.

Use the NameCert obligations of \autoref{thm:real-uniform-structure-namecert-obligations}. Metric-ball entourage generation forces the route through $R,M,D,U$, and Cauchy-filter compatibility forces the completion-facing filter row $F$ to consume that entourage row before stream windows or rational readbacks are inspected.

Finally apply the L10 finite-source gate of \autoref{thm:real-uniform-structure-l10-gate}. The rows $S,Q,H,C,P,N$ preserve and name the same finite route, so a $\LocatedCauchyFilterUp$ consumer reads the Real-induced uniformity without receiving quotient real equality, selected limits, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Real uniform-structure Cauchy-filter basis]
\label{thm:real-uniform-structure-cauchy-filter-basis}
Every accepted $\RealUniformStructureUp$ packet exports a $\CauchyFilterUp$ basis only through the $\RealUp$ endpoint and seal rows, $\MetricUp$ distance witnesses, positive $\DyadicRatCoreUp$ radii, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The Cauchy-filter row is downstream of the metric-ball entourage row, and it cannot export quotient equality, selected limits, or $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCarrier\_cauchy\_filter\_basis}
\begin{proof}
Project the carrier of \autoref{def:real-uniform-structure-carrier}. The available coordinates are exactly $R,M,U,F,D,S,Q,H,C,P,N$, so a Cauchy-filter basis read starts from displayed Real endpoints, metric distance data, positive dyadic radii, stream windows, and regular rational readback.

Use the NameCert obligations of \autoref{thm:real-uniform-structure-namecert-obligations}. They force every entourage request in $U$ to be generated from the metric row $M$ and positive dyadic radius ledger $D$ around endpoints displayed by $R$.

The row $F$ is therefore downstream of the metric-ball entourage row $U$. Its finite-window and regular-readback inspections pass through $S$ and $Q$, while $H,C,P,N$ transport, replay, source, and name the same route.

Since quotient equality, selected limits, and $\RealUp$ completeness are not rows of the carrier, the exported Cauchy-filter basis has no coordinate from which to supply them.
\end{proof}

\begin{theorem}[Real uniform-structure cofinal-refinement compatibility]
\label{thm:real-uniform-structure-filter-refinement-compatibility}
Suppose an accepted $\RealUniformStructureUp$ packet supplies its $\CauchyFilterUp$ route through a metric-ball entourage basis, and suppose an accepted $\FilterRefinementUp$ packet replaces that displayed basis by a cofinal refinement. Then every completion-facing Real-uniform consumer still reads through the same $\RealUp$ endpoint row, $\MetricUp$ distance row, positive $\DyadicRatCoreUp$ radius ledger, $\CauchyFilterUp$ row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. The refined route does not export quotient real equality, selected limits, countable choice, quotient filters, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCarrier\_filter\_refinement\_compatibility}
\begin{proof}
Start with the Cauchy-filter basis of \autoref{thm:real-uniform-structure-cauchy-filter-basis}. It forces the Real-uniform route to expose the endpoint row $R$, metric row $M$, positive-radius ledger $D$, generated entourage row $U$, Cauchy-filter row $F$, finite windows, regular readbacks, and structural rows before any completion-facing consumer reads the basis.

Apply the Cauchy-preservation theorem for $\FilterRefinementUp$, \autoref{thm:filter-refinement-cauchy-preservation}. The cofinal refinement replaces a displayed base request by a target-base request and a finite comparison row, while preserving the Cauchy route through the source basis.

Next use the completion route of \autoref{thm:filter-refinement-completion-route}. A completion-facing consumer receives only the refined finite base row, the cofinal refinement row, optional reverse comparison data when needed, and the transport, replay, provenance, and local naming rows.

Composing the two routes leaves the Real-uniform endpoint and metric data unchanged. The refinement acts on the displayed Cauchy-filter basis, so the consumer still reaches the same $R,M,D,F,H,C,P,N$ surface and gains no coordinate for quotient real equality, selected limits, choice, quotient filters, or ambient completeness.
\end{proof}

\begin{theorem}[RealUniformStructure basis refinement uniqueness]
\label{thm:real-uniform-structure-basis-refinement-uniqueness}
Any cofinal refinement accepted by the $\RealUniformStructureUp$ packet must still factor through the same Real endpoint row, Metric distance row, positive DyadicRatCore radius ledger, generated UniformSpace entourage row, CauchyFilter row, StreamName windows, RegSeqRat readbacks, and structural rows. Thus a refined basis preserves the unique Real-induced basis route and exports no quotient real equality, selected limit, countable choice, quotient filter, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCarrier\_basis\_refinement\_uniqueness}
\begin{proof}
Begin with the L10 finite-source gate of \autoref{thm:real-uniform-structure-l10-gate}. It forces every Real-uniform consumer to inspect the Real endpoint row, Metric distance row, positive dyadic radius ledger, StreamName window row, and RegSeqRat readback before any UniformSpace or CauchyFilter handoff is available.

Apply the Cauchy-filter basis theorem \autoref{thm:real-uniform-structure-cauchy-filter-basis}. The CauchyFilter row is downstream of the generated metric-ball entourage row, and the structural rows transport, replay, source, and name that same route.

Now use the cofinal-refinement compatibility theorem \autoref{thm:real-uniform-structure-filter-refinement-compatibility}. A cofinal refinement may replace the displayed basis request by a finite refined request, but it preserves the Real endpoint, metric, dyadic radius, CauchyFilter, and structural rows.

Combining the three routes leaves only one admitted Real-induced basis path:
$$
R,M,D,U,F,S,Q \longmapsto H,C,P,N .
$$
Since no carrier coordinate stores quotient real equality, selected limits, countable choice, quotient filters, open-cover compactness, or ambient Real completeness, no cofinal refinement can export those objects.
\end{proof}

\begin{theorem}[Real uniform-structure completion entourage basis]
\label{thm:real-uniform-structure-completion-entourage-basis}
Every completion-facing consumer of an accepted $\RealUniformStructureUp$ packet reads an entourage basis only through the $\RealUp$ endpoint row, the $\MetricUp$ distance row, the positive $\DyadicRatCoreUp$ radius ledger, the generated $\UniformSpaceUp$ entourage row, the $\CauchyFilterUp$ row, $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, and the structural rows $H,C,P,N$. The exported basis does not supply quotient real equality, selected limits, quotient filters, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureCompletionEntourageBasis}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:real-uniform-structure-namecert-obligations}. They admit exactly the rows $R,M,U,F,D,S,Q,H,C,P,N$, with every entourage request generated from Real endpoints, metric distance data, and a positive dyadic radius.

Apply the Cauchy-filter basis theorem \autoref{thm:real-uniform-structure-cauchy-filter-basis}. It places the $\CauchyFilterUp$ row downstream of the generated metric-ball entourage row and keeps the StreamName and RegSeqRat readbacks downstream of that same basis.

Finally use the cofinal-refinement compatibility of \autoref{thm:real-uniform-structure-filter-refinement-compatibility}. A completion-facing consumer may replace a displayed basis by a cofinal finite refinement, but the route still reads through $R,M,D,U,F,S,Q,H,C,P,N$ and gains no coordinate for quotient real equality, selected limits, quotient filters, choice, open-cover compactness, or ambient Real completeness.
\end{proof}

\begin{theorem}[Real uniform-structure finite entourage basis]
\label{thm:real-uniform-structure-finite-entourage-basis}
Every accepted $\RealUniformStructureUp$ packet generates its entourage basis from finite reads of the $\RealUp$ endpoint row $R$, the $\MetricUp$ distance row $M$, the positive $\DyadicRatCoreUp$ radius ledger $D$, the generated $\UniformSpaceUp$ row $U$, the $\CauchyFilterUp$ row $F$, $\StreamNameUp$ windows $S$, $\RegSeqRatUp$ readback $Q$, and the structural rows $H,C,P,N$. A completion-facing or Real-uniform consumer receives no basis element outside those displayed rows.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructureFiniteEntourageBasis}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:real-uniform-structure-namecert-obligations}. They admit exactly the Real endpoint, metric distance, positive radius, uniform entourage, Cauchy-filter, finite-window, readback, transport, replay, provenance, and local naming rows.

Apply the metric-uniformity handoff of \autoref{thm:real-uniform-structure-metric-uniformity-handoff}. It forces every entourage request to read endpoints from $R$, distance data from $M$, and a positive dyadic radius from $D$ before the generated uniform row $U$ is public.

Use the Cauchy-filter basis theorem \autoref{thm:real-uniform-structure-cauchy-filter-basis}. The row $F$ is downstream of the metric-ball entourage row, while $S$ and $Q$ provide only finite windows and regular rational readbacks for that same basis.

Finally compose with the completion-entourage basis of \autoref{thm:real-uniform-structure-completion-entourage-basis}. Completion-facing consumers may inspect the displayed basis, but the structural rows $H,C,P,N$ only transport, replay, source, and name it. Thus no basis element is generated outside $R,M,D,U,F,S,Q,H,C,P,N$.
\end{proof}

\begin{theorem}[Real uniform-structure positive-radius entourage refinement]
\label{thm:real-uniform-structure-positive-radius-entourage-refinement}
When an accepted $\RealUniformStructureUp$ packet exposes two positive $\DyadicRatCoreUp$ radius rows in the same $D$ ledger and the second is displayed as a refinement of the first, every refined entourage read still factors through the same $\RealUp$ endpoint row, $\MetricUp$ distance row, $\UniformSpaceUp$ entourage row, $\CauchyFilterUp$ row, $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, and structural rows. The refined request may narrow the finite metric-ball basis but cannot add quotient real equality, selected limits, quotient filters, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealUniformStructureUp.RealUniformStructurePositiveRadiusEntourageRefinement}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:real-uniform-structure-namecert-obligations}. Both positive radii are read from the same $D$ ledger, so the refinement remains attached to the displayed Real endpoint and Metric distance rows.

Apply the metric-uniformity handoff of \autoref{thm:real-uniform-structure-metric-uniformity-handoff}. It sends the refined positive-radius request to the generated entourage row $U$ without changing the endpoint row $R$ or the distance row $M$.

Use the Cauchy-filter basis theorem \autoref{thm:real-uniform-structure-cauchy-filter-basis} and the finite-entourage basis of \autoref{thm:real-uniform-structure-finite-entourage-basis}. The refined request may narrow the finite metric-ball basis, but it remains downstream of $R,M,D,U,F,S,Q,H,C,P,N$.

Since none of those rows stores quotient real equality, selected limits, quotient filters, countable choice, open-cover compactness, or ambient Real completeness, no positive-radius refinement can export those objects.
\end{proof}

\begin{closurestatus}{\RealUniformStructureUp}
  \constructivestory{A $\RealUniformStructureUp$ packet is generated from a RealUp endpoint and seal row, a MetricUp distance witness row, a UniformSpace entourage row generated by positive DyadicRatCore radius requests, a CauchyFilter compatibility row, StreamName windows, RegSeqRat readback, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-uniform-structure-carrier}, \autoref{thm:real-uniform-structure-namecert-obligations}, and \autoref{thm:real-uniform-structure-metric-uniformity-handoff} seed the Real-induced uniformity boundary for completion-facing and Cauchy-filter consumers.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.RealUniformStructureUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected limits, trichotomy, countable-choice selectors, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires carrier admission, metric-ball entourage generation, Cauchy-filter compatibility, classifier transport, and ledger non-escape proofs for the same packet.}
\end{closurestatus}
