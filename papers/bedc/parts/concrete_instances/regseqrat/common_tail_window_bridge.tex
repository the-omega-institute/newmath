\begin{definition}[RegSeqRat common-tail window packet]
\label{def:regseqrat-common-tail-window-packet}
A $\RegSeqRatUp$ common-tail window packet is a finite $\mathsf{BHist}$ packet over one accepted $\RegSeqRatUp$ source $P$ with the following displayed rows:
\begin{enumerate}
\item two certified cofinal tail shifts $T_0$ and $T_1$ of the $\StreamNameUp$ schedule carried by $P$;
\item one common $\StreamNameUp$ refinement window $W_\ast$ through which the selected tail windows of $T_0$ and $T_1$ both restrict;
\item the $\DyadicRatCoreUp$ endpoint rows read on $W_\ast$, together with the displayed radius rows and regularity bounds
$$
2^{-n}+2^{-m}
$$
for the selected unary indices;
\item the $\mathsf{RegSeqRatClassifier}$ rows for the two tail-window reads and for their transported read on $W_\ast$;
\item the $\RealUp$ seal-source rows consumed from the same common finite surface by \autoref{thm:regseqrat-real-seal-handoff};
\item the componentwise $\hsame$ rows, displayed $\Cont$ routes, $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance attached to the StreamName, DyadicRatCore, RegSeqRat, and RealUp-facing rows.
\end{enumerate}
The packet is only a finite common-tail source surface. It stores no quotient stream, host real value, selected limit, metric-completion point, $\RealUp$ completeness datum, or standard-bridge coordinate beyond the displayed rows.
\end{definition}

\begin{theorem}[RegSeqRat common-tail union classifier determinacy]
\label{thm:regseqrat-common-tail-union-classifier-determinacy}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $T_0$ and $T_1$ be two certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose the two shifted tail-window reads land in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet} with common refinement $W_\ast$. Then the union window determined by that common-tail packet determines one accepted $\mathsf{RegSeqRatClassifier}$ read for both shifted tail windows, over exactly the displayed $\StreamNameUp$ schedule and refinement rows, $\DyadicRatCoreUp$ endpoint, radius, distance, and regularity rows, $\RegSeqRatUp$ classifier rows, $\RealUp$ seal-source rows, componentwise $\hsame$, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ provenance.

The determinacy statement is only a finite BEDC classifier read on the common union window. It exports no quotient stream equality, host real equality, selected limit, ambient completeness row, metric-completeness row, hidden completion carrier, or additional seal coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.RegSeqRatUp.RegSeqRatCommonTailUnionClassifierDeterminacy}
\begin{proof}
Project the common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet}. Its rows provide the two certified cofinal tail shifts, the common $\StreamNameUp$ refinement $W_\ast$, the transported dyadic endpoint and radius ledgers, the two tail classifier rows, the common transported classifier row, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance.

Apply \autoref{thm:regseqrat-common-refinement-classifier-determinacy} to the two transported tail reads on $W_\ast$. The hypotheses identify the transported $\DyadicRatCoreUp$ endpoint rows, radius rows, regularity ledgers, classifier ledgers, componentwise comparisons, continuation routes, and package provenance, so the two shifted schedules expose one $\mathsf{RegSeqRatClassifier}$ read on the common union window.

Use \autoref{thm:regseqrat-streamname-dyadic-window-lock}. The common $\StreamNameUp$ refinement locks the dyadic endpoint, radius, distance, and regularity rows to the same finite ledger, so the accepted classifier read cannot branch through a separate schedule coordinate or a separate dyadic coordinate.

Finally route the common classifier through \autoref{thm:regseqrat-common-tail-window-classifier-bridge} and \autoref{thm:regseqrat-real-seal-handoff}. The bridge identifies the shared classifier and seal-source surface already present on $W_\ast$, and the seal handoff consumes exactly those finite regular-rational rows. Hence quotient stream equality, host real equality, selected limits, ambient completeness, metric-completeness rows, hidden completion carriers, and extra seal data have no displayed row from which to distinguish the two shifted tail windows.
\end{proof}

\begin{theorem}[RegSeqRat common-tail window classifier bridge]
\label{thm:regseqrat-common-tail-window-classifier-bridge}
Let $P$ be an accepted $\RegSeqRatUp$ source, and let $T_0$ and $T_1$ be two certified cofinal tail shifts of the $\StreamNameUp$ schedule of $P$. Suppose two tail-window classifier reads land in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet} with common refinement $W_\ast$. Then the two reads determine the same $\RealUp$ seal-source surface and the same $\mathsf{RegSeqRatClassifier}$ read after restriction to $W_\ast$.

The bridge conclusion is a statement about the displayed finite packet only. It exports no quotient stream, host real equality, selected limit, metric-completion point, $\RealUp$ completeness datum, or additional seal coordinate.
\end{theorem}
\begin{proof}
First use \autoref{thm:regseqrat-tail-stable-cover-common-window-lock}. The two seal consumers carried by the cofinal tail shifts factor through one accepted common finite window, and the visible rows of that window are exactly the StreamName schedule rows, DyadicRatCore endpoint and radius ledgers, RegSeqRat classifier and regularity ledgers, and the displayed provenance.

Next read the common-tail packet rows componentwise. The definition lists the cofinal tail shifts, the common $\StreamNameUp$ refinement $W_\ast$, the DyadicRatCore endpoint and radius ledgers, the classifier rows, the $\RealUp$ seal-source rows, and the $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ provenance. Thus each tail-window read restricts to the same finite source surface on $W_\ast$.

Apply \autoref{thm:regseqrat-refined-window-gluing-real-seal-factorization}. The refined-window gluing surface factors the RealUp-facing handoff through the displayed finite packet $W_\ast$, so the two tail-window origins cannot supply separate seal-source surfaces once their rows have been restricted to the common refinement.

Use \autoref{thm:regseqrat-streamname-dyadic-window-lock} to lock the $\StreamNameUp$ schedule rows to the same $\DyadicRatCoreUp$ endpoint and radius ledgers inside the common window. The classifier reads inspect only those locked rows together with the regularity ledgers and provenance, so their transported RegSeqRat classifier read on $W_\ast$ is the same displayed read.

Finally invoke \autoref{thm:regseqrat-real-seal-handoff}. The $\RealUp$ seal consumes exactly the accepted regular-rational finite source surface already identified, and the carrier has no coordinate for quotient streams, host real equality, selected limits, metric completions, completeness data, or further seal coordinates.
\end{proof}

\begin{theorem}[RegSeqRat common-tail window terminality]
\label{thm:regseqrat-common-tail-window-terminality}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $T_0$ and $T_1$ be two certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose the two shifted tail reads land in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet} with common refinement $W_\ast$. Every completion-facing $\RealUp$ consumer whose visible rows are exactly the two shifted reads, the common $\StreamNameUp$ window $W_\ast$, the $\DyadicRatCoreUp$ endpoint, radius, and regularity rows, the $\RegSeqRatUp$ classifier rows, the $\RealUp$ seal-source rows, the displayed $\hsame$ rows, displayed $\Cont$ routes, $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance factors through that common-tail packet.

The terminal factorization exports no quotient stream, host real equality, selected limit, metric-completion point, $\RealUp$ completeness datum, or additional seal coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regseqrat-tail-stable-cover-common-window-lock} to the two cofinal tail shifts. The tail-stable cover places both completion-facing seal consumers on one accepted common finite window with the StreamName schedule rows, DyadicRatCore endpoint and radius ledgers, RegSeqRat classifier and regularity ledgers, and displayed provenance.

Next use \autoref{thm:regseqrat-refined-window-gluing-real-seal-factorization}. The common refinement $W_\ast$ repacks the two shifted tail reads into one RealUp-facing seal source, so every consumer whose visible rows are limited to the displayed shifts and the common window is already routed through the common-tail packet.

Apply \autoref{thm:regseqrat-streamname-dyadic-window-lock}. It locks the common $\StreamNameUp$ refinement to the same $\DyadicRatCoreUp$ endpoint, radius, and regularity rows, leaving the consumer no separate dyadic or schedule coordinate outside $W_\ast$.

Now invoke \autoref{thm:regseqrat-common-tail-window-classifier-bridge}. The transported classifier read and the transported seal-source rows are the same rows listed by the common-tail packet, including the displayed $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ provenance.

Finally apply \autoref{thm:regseqrat-real-seal-handoff}. Since the $\RealUp$ seal consumes exactly this accepted regular-rational finite source surface, the consumer factors through the common-tail packet. Quotient streams, host real equality, selected limits, metric-completion points, $\RealUp$ completeness data, and extra seal coordinates have no displayed carrier row from which to be read.
\end{proof}

\begin{theorem}[RegSeqRat common-tail finite-request Real seal pullback]
\label{thm:regseqrat-common-tail-finite-request-real-seal-pullback}
Let $P$ be an accepted $\RegSeqRatUp$ packet, let $T_0$ and $T_1$ be two certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$, and let the two shifted tail reads land in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet} with common refinement $W_\ast$. For any finite-request $\RealUp$ seal consumer whose request is visible along that common-tail packet, the visible pullback of the consumer is exactly the finite source surface listed in \autoref{def:regseqrat-common-tail-window-packet}: the $\StreamNameUp$ schedule and refinement rows, the $\DyadicRatCoreUp$ endpoint, radius, and distance rows, the $\RegSeqRatUp$ classifier and regularity rows, the $\RealUp$ seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance.

Equivalently, the common-tail window source surface and the finite-request $\RealUp$ seal pullback present the same BEDC-visible rows. The equivalence exports no quotient stream, selected limit, host real equality, ambient completeness row, hidden completion carrier, or second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regseqrat-common-tail-window-terminality}. Any completion-facing consumer whose visible rows are the shifted tail reads, the common window $W_\ast$, the dyadic ledger, classifier, seal-source rows, transports, continuations, package provenance, and local naming must factor through the common-tail packet.

Next use \autoref{thm:regseqrat-tail-budget-window-seal-pullback}. A finite-request $\RealUp$ seal consumer has visible pullback exactly the StreamName schedule row, DyadicRatCore endpoint, radius, and distance rows, RegSeqRat classifier and regularity rows, displayed $\hsame$, $\Cont$, $\mathsf{Pkg}$ provenance, and local $\NameCert$ row.

Now apply \autoref{thm:regseqrat-shared-window-terminal-pullback-equivalence}. It identifies equality of the shared selector surface with equality of the terminal $\RealUp$ seal reads after both requests have been read through the ordered finite packet from $\DyadicRatCoreUp$ to $\RealUp$. Thus the common-tail presentation and the finite-request pullback presentation determine one another on the same displayed rows.

Finally invoke \autoref{thm:regseqrat-real-seal-selector-nonescape}. The selector boundary removes the excluded coordinates from both descriptions: quotient streams, selected limits, host real equality, ambient completeness data, hidden completion carriers, and second seal data have no row in either surface. The remaining rows are precisely the StreamName schedule and refinement rows, DyadicRatCore endpoint, radius, and distance rows, RegSeqRat classifier and regularity rows, RealUp seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance listed in \autoref{def:regseqrat-common-tail-window-packet}.
\end{proof}

\begin{theorem}[RegSeqRat common-tail visible packet assembly]
\label{thm:regseqrat-common-tail-visible-packet-assembly}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $T_0$ and $T_1$ be two certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose a finite-request $\RealUp$ seal consumer exposes exactly the displayed $\StreamNameUp$ schedule and refinement rows, $\DyadicRatCoreUp$ endpoint, radius, and distance rows, $\RegSeqRatUp$ classifier and regularity rows, $\RealUp$ seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance for the two shifted tail reads. Then those rows assemble a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet} with common refinement $W_\ast$.

The assembled packet is unique at the displayed-row level. It exports no quotient stream, selected limit, host real equality, ambient completeness, metric-completeness row, hidden completion carrier, or second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First use \autoref{thm:regseqrat-tail-stable-cover-common-window-lock}. The two certified cofinal tail shifts and the finite-request seal consumer are forced onto one common finite window, so the candidate refinement $W_\ast$ is the shared support on which the displayed rows may be read.

Next apply \autoref{thm:regseqrat-streamname-dyadic-window-lock}. The exposed $\StreamNameUp$ schedule and refinement rows lock the $\DyadicRatCoreUp$ endpoint, radius, and distance rows to the same finite ledger, and the $\RegSeqRatUp$ classifier and regularity rows are therefore read on that locked window.

Use \autoref{thm:regseqrat-real-seal-classifier-exhaustion} and \autoref{thm:regseqrat-real-seal-schedule-coverage}. These results say that the visible finite-request $\RealUp$ seal boundary has no further source rows beyond the listed schedule, dyadic, classifier, seal-source, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ data. Hence the displayed rows are exactly the rows required by \autoref{def:regseqrat-common-tail-window-packet}.

Finally invoke \autoref{thm:regseqrat-common-tail-finite-request-real-seal-pullback}. The pullback theorem identifies the assembled common-tail packet with the finite-request seal boundary on the same visible rows. Uniqueness follows because two assemblies with the same $W_\ast$ and the same displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ rows have no remaining BEDC coordinate on which to differ, and the excluded quotient, limit, equality, completeness, carrier, and second-seal coordinates are not part of the packet.
\end{proof}

\begin{theorem}[RegSeqRat common-tail Real boundary equivalence]
\label{thm:regseqrat-common-tail-real-boundary-equivalence}
Let $P$ be an accepted $\RegSeqRatUp$ packet, let $T_0$ and $T_1$ be two certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$, and suppose the shifted tail reads land in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet} with common refinement $W_\ast$. Then the common-tail packet and the finite-request $\RealUp$ seal boundary determine one another exactly on the $\StreamNameUp$ schedule and refinement rows, the $\DyadicRatCoreUp$ endpoint, radius, and distance rows, the $\RegSeqRatUp$ classifier and regularity rows, the $\RealUp$ seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance.

The public boundary is only this finite BEDC surface. It excludes quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, and any completion row not displayed by the common-tail packet.
\end{theorem}
\leanchecked{BEDC.Derived.RegSeqRatUp.RegSeqRatCommonTailRealBoundaryEquivalence}
\begin{proof}
First apply \autoref{thm:regseqrat-common-tail-window-classifier-bridge}. The two cofinal tail reads restrict to one classifier and one seal-source surface on $W_\ast$, so the common-tail packet already determines the RegSeqRat and RealUp-facing rows of the boundary.

Next use \autoref{thm:regseqrat-common-tail-window-terminality}. Every completion-facing consumer whose visible rows are the two shifts, the common StreamName window, the dyadic endpoint-radius-distance ledger, the classifier and regularity ledger, and the displayed structural rows factors through the common-tail packet. Thus a finite-request $\RealUp$ seal boundary cannot add a second route outside that packet.

Now apply \autoref{thm:regseqrat-common-tail-visible-packet-assembly}. It assembles any finite-request boundary exposing exactly the displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ rows back into the common-tail packet on the same refinement $W_\ast$.

Then use \autoref{thm:regseqrat-common-tail-finite-request-real-seal-pullback}. It identifies the visible pullback of any finite-request seal consumer with exactly the rows listed in \autoref{def:regseqrat-common-tail-window-packet}. Hence the finite-request boundary and the common-tail packet determine one another on the same displayed rows.

Finally compose the Real L10 four-face route results. The canonical route and exactness theorems, \autoref{thm:real-l10-four-face-canonical-route} and \autoref{thm:real-l10-four-face-exactness}, place the accepted Real-and-Completion read on the ordered $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces. The terminal correspondence and pullback uniqueness theorems, \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}, then repack the same displayed rows as one terminal finite boundary. Since none of the cited packets carries quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, or hidden completion data, the two presentations determine one another exactly on the stated BEDC-visible surface.
\end{proof}

\begin{theorem}[RegSeqRat common-tail support descent nonescape]
\label{thm:regseqrat-common-tail-support-descent-nonescape}
Let $P$ be an accepted $\RegSeqRatUp$ packet, let $T_0$ and $T_1$ be two certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$, and suppose the shifted tail reads land in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet} with common refinement $W_\ast$. A $\RealUp$-facing terminal consumer or finite-cover consumer descends through the common-tail packet if and only if its visible support rows are exactly the displayed $\StreamNameUp$ schedule and refinement rows, the $\DyadicRatCoreUp$ endpoint-radius-distance ledger, the $\RegSeqRatUp$ classifier and regularity rows, the $\RealUp$ seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ provenance.

This descent surface excludes quotient stream equality, selected limits, host real equality, countable choice, metric-completeness data, ambient $\RealUp$ completeness, aggregate route-level $\mathsf{formalstatus}$ rows, and bridge rows. No such coordinate can be used to escape the finite support displayed by the common-tail packet.
\end{theorem}
\begin{proof}
For the forward direction, suppose the terminal or finite-cover consumer descends through the common-tail packet. Apply \autoref{thm:regseqrat-common-tail-real-boundary-equivalence}. The descended consumer has the same finite boundary as the common-tail packet, hence its visible rows are confined to the displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface.

Use \autoref{thm:regseqrat-common-tail-finite-request-real-seal-pullback}. It identifies the visible pullback of any finite $\RealUp$ seal request through the packet with exactly those source rows. Then \autoref{thm:streamname-finite-request-section-terminal-coherence} keeps the $\StreamNameUp$ section rows terminal under restriction to the cofinal tail window, so a finite-cover refinement cannot introduce a second schedule coordinate.

Finally apply \autoref{thm:real-l10-terminal-cover-coordinate-exhaustion}. The L10 terminal cover exhausts the Real-facing coordinate list on the four displayed faces, and the common-tail equivalence has already identified those faces with the packet rows. Thus the descended consumer exposes precisely the stated support and nothing else.

For the reverse direction, suppose the consumer exposes exactly the displayed support rows. The $\StreamNameUp$ schedule and refinement rows, together with terminal section coherence, place the request on the same finite section window $W_\ast$. The DyadicRatCore endpoint-radius-distance ledger and the RegSeqRat classifier and regularity rows are therefore read on the same locked common-tail surface.

Apply \autoref{thm:regseqrat-common-tail-finite-request-real-seal-pullback} to read the consumer as the finite $\RealUp$ seal pullback over that surface. Then use \autoref{thm:regseqrat-common-tail-real-boundary-equivalence} to identify this pullback with the common-tail packet boundary. The terminal-cover coordinate exhaustion theorem supplies the finite-cover case by showing that every retained cover coordinate is already one of the displayed rows.

The nonescape clause is the row inventory of these four applications. The common-tail equivalence, finite-request pullback, StreamName terminal coherence, and Real L10 coordinate exhaustion expose no quotient stream equality, selected limit, host real equality, countable-choice principle, metric-completeness datum, ambient $\RealUp$ completeness row, aggregate route-level status row, or bridge row. Hence the descent criterion cannot factor through any support outside the displayed packet.
\end{proof}

\begin{theorem}[RegSeqRat common-tail four-face cocycle determinacy]
\label{thm:regseqrat-common-tail-four-face-cocycle-determinacy}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $T_0$, $T_1$, and $T_2$ be three certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose each pair $(T_i,T_j)$ lands in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet}, and suppose the three pairwise packets restrict to one triple $\StreamNameUp$ refinement $W_\ast$ with the same $\DyadicRatCoreUp$ endpoint, radius, and distance ledger and the same $\RealUp$ seal-source row.

Then the three pairwise common-tail $\RealUp$ boundaries satisfy one finite four-face cocycle exactly when the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows agree on the triple refinement $W_\ast$. The cocycle surface has no coordinate for quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, standard-bridge data, or hidden completion carriers.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regseqrat-common-tail-real-boundary-equivalence} to the three pairs $(T_0,T_1)$, $(T_1,T_2)$, and $(T_0,T_2)$. Each pairwise boundary is thereby identified with the same kind of finite common-tail source surface: StreamName schedule and refinement rows, DyadicRatCore endpoint-radius-distance rows, RegSeqRat classifier and regularity rows, RealUp seal-source rows, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance.

On the triple refinement $W_\ast$, the definition of the common-tail window packet identifies those rows componentwise. The hypotheses give one StreamName refinement, one dyadic ledger, and one Real seal-source row, so the RegSeqRat classifier and regularity rows and the support provenance are the only remaining displayed coordinates to compare.

For the forward direction, a finite four-face cocycle already pastes the three pairwise Real boundaries along the ordered $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces. The Real L10 canonical route and exactness results, \autoref{thm:real-l10-four-face-canonical-route} and \autoref{thm:real-l10-four-face-exactness}, force that pasted surface to read through the displayed rows on $W_\ast$, so disagreement on any listed row would give two different finite boundaries for the same face.

For the reverse direction, assume the displayed rows agree on $W_\ast$. Use the Real L10 terminal correspondence and pullback uniqueness results, \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}, to repack the shared rows as one terminal four-face surface. The three pairwise common-tail boundaries are the restrictions of that surface, hence they satisfy the finite cocycle.

No additional boundary coordinate remains to choose. The preceding common-tail Real boundary equivalence and finite-request seal pullback show that quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, standard bridges, and hidden completion data are not rows of the packet, so they cannot split the cocycle after the displayed surface agrees.
\end{proof}

\begin{theorem}[RegSeqRat common-tail triple-overlap cocycle]
\label{thm:regseqrat-common-tail-triple-overlap-cocycle}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $T_0$, $T_1$, and $T_2$ be three certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose each shifted pair $(T_i,T_j)$ lands in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet}, and suppose the three packets restrict to one triple refinement $W_\ast$ carrying the same $\StreamNameUp$ schedule rows, the same $\DyadicRatCoreUp$ endpoint, radius, and distance ledger, the same $\RegSeqRatUp$ classifier and regularity rows, the same $\RealUp$ seal-source rows, componentwise $\hsame$, displayed $\Cont$, displayed $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance.

Then the three pairwise $\RealUp$ boundary equivalences agree on the triple refinement if and only if $W_\ast$ exposes exactly that same finite common-tail surface. This triple-overlap cocycle exports no quotient stream, host real equality, selected limit, countable choice, ambient completeness, metric-completeness row, standard-bridge coordinate, or completion datum outside the displayed packet.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regseqrat-common-tail-window-classifier-bridge} to the three pairs $(T_0,T_1)$, $(T_1,T_2)$, and $(T_0,T_2)$. Each pairwise shifted read is transported to the same kind of common-tail classifier and seal-source surface on its refinement face.

Use \autoref{thm:regseqrat-common-tail-window-terminality} next. Every completion-facing consumer whose visible rows are the shifted reads, the common StreamName refinement, the dyadic endpoint-radius-distance ledger, the classifier and regularity rows, the seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\mathsf{Pkg}$ provenance, and local $\NameCert$ provenance factors through the corresponding common-tail packet.

For the forward direction, assume the three pairwise $\RealUp$ boundary equivalences agree on $W_\ast$. Apply \autoref{thm:regseqrat-common-tail-real-boundary-equivalence} on each face. The Real L10 canonical route and exactness results cited there, \autoref{thm:real-l10-four-face-canonical-route} and \autoref{thm:real-l10-four-face-exactness}, identify a unique BEDC-visible boundary on the ordered $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces. Therefore any row visible to the triple refinement must be one of the displayed common-tail rows, and each such row has the same value on the three faces.

For the reverse direction, suppose $W_\ast$ exposes exactly the common finite surface listed in the statement. The three pairwise boundaries are then restrictions of one displayed packet. Apply \autoref{thm:regseqrat-common-tail-real-boundary-equivalence} on each pair and use the Real L10 terminal correspondence and pullback uniqueness results, \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}, to identify the same terminal finite boundary on all three restrictions.

The exclusions are forced by the carrier rows already used. Neither \autoref{def:regseqrat-common-tail-window-packet} nor the Real L10 four-face route supplies quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, standard-bridge coordinates, or hidden completion data, so no such coordinate can distinguish the triple-overlap cocycle once the displayed common-tail surface agrees.
\end{proof}

\begin{theorem}[RegSeqRat common-tail cocycle descent]
\label{thm:regseqrat-common-tail-cocycle-descent}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $T_0$, $T_1$, and $T_2$ be three certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose each shifted pair $(T_i,T_j)$ lands in a common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet}, and suppose the three pairwise common-tail windows restrict to one triple refinement $W_\ast$ carrying the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows, componentwise $\hsame$, displayed $\Cont$, displayed $\Pkg$ provenance, and local $\NameCert$ provenance.

If the three pairwise $\RealUp$ boundary equivalences agree on $W_\ast$, then the three boundaries descend to a single finite four-face exit boundary over exactly the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ faces, with precisely the same componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows. Conversely, any single displayed four-face boundary with exactly those rows restricts to the three pairwise common-tail boundary equivalences. The descent boundary contains no quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, standard bridge data, or hidden completion carriers.
\end{theorem}
\begin{proof}
Assume first that the three pairwise $\RealUp$ boundary equivalences agree on $W_\ast$. Apply \autoref{thm:regseqrat-common-tail-triple-overlap-cocycle}. The triple refinement exposes exactly the common finite tail surface on the StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Now use \autoref{thm:regseqrat-common-tail-four-face-cocycle-determinacy}. Since the displayed rows agree on the triple refinement, the three pairwise common-tail boundaries satisfy the finite four-face cocycle criterion. This turns pairwise agreement into one cocycle surface rather than three unrelated boundary presentations.

Package that surface by applying \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}. The terminal Real L10 pullback identifies the shared StreamName, DyadicRatCore, RegSeqRat, and RealUp rows as one finite four-face exit boundary, and uniqueness prevents any second boundary with the same displayed rows.

For the converse direction, start with a single displayed four-face boundary over exactly those four faces and support rows. Restrict it along the three pairwise common-tail windows. Each restriction has precisely the rows required by \autoref{thm:regseqrat-common-tail-real-boundary-equivalence}, so it recovers the corresponding pairwise $\RealUp$ boundary equivalence.

The exclusions follow from the same displayed-row accounting. The common-tail packet, the cocycle criterion, and the Real L10 terminal pullback provide no coordinates for quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, standard bridge data, or hidden completion carriers, so none can appear in the descended boundary or in its pairwise restrictions.
\end{proof}

\begin{theorem}[RegSeqRat common-tail cofinal-bundle four-face descent]
\label{thm:regseqrat-common-tail-cofinal-bundle-four-face-descent}
Let $P$ be an accepted $\RegSeqRatUp$ packet, let $\mathcal P:\ProbeBundle$ be a finite bundle of certified unary observations served by the shared tail-compatible $\StreamNameUp$ selector, and let $T_0$, $T_1$, and $T_2$ be three certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose the bundled observations have $\mathsf{InBundle}$ witnesses, a shared finite-window budget, and pointwise $\mathsf{RatHistoryClassifier}$ handoff rows through the selector; suppose each shifted pair $(T_i,T_j)$ lands in a RegSeqRat common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet}; and suppose the three pairwise packets restrict to one triple refinement carrying the same bundled StreamName rows, the same $\DyadicRatCoreUp$ endpoint, radius, and distance ledger, the same $\RegSeqRatUp$ classifier and regularity rows, the same $\RealUp$ seal-source rows, and the same support rows $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

Then the bundled StreamName unary rows, their $\mathsf{InBundle}$ witnesses, the shared tail selector, the finite-window budget, the pointwise $\mathsf{RatHistoryClassifier}$ handoff rows, the dyadic endpoint-radius-distance ledger, the RegSeqRat classifier and regularity rows, the RealUp seal-source rows, and the support rows descend to one finite four-face boundary over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. This boundary contains no quotient streams, selected limits, host real equality, countable choice, metric-completeness rows, ambient $\RealUp$ completeness datum, standard bridge coordinate, or hidden completion carrier.
\end{theorem}
\begin{proof}
First apply \autoref{thm:streamname-tail-compatible-window-cofinal-exhaustion} to the finite bundle $\mathcal P$. The bundled unary rows, their $\mathsf{InBundle}$ witnesses, the shared selector, the budget surface, the pointwise rational-history classifier handoffs, and the support rows are exhausted by one displayed StreamName packet.

Next restrict the three RegSeqRat common-tail packets to the stated triple refinement. By \autoref{thm:regseqrat-common-tail-triple-overlap-cocycle}, the three pairwise Real boundary equivalences agree exactly when that refinement exposes the common finite surface on the StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Now use \autoref{thm:regseqrat-common-tail-cocycle-descent}. The pairwise common-tail surfaces therefore descend to a single displayed four-face exit boundary with the same StreamName rows, dyadic ledger, RegSeqRat classifier and regularity rows, RealUp seal-source rows, and support provenance.

Finally apply \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}. Terminality repacks the displayed boundary over the ordered $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces and prevents a second boundary with the same rows.

The nonescape clauses in \autoref{thm:streamname-tail-compatible-window-cofinal-exhaustion}, \autoref{thm:regseqrat-common-tail-triple-overlap-cocycle}, and \autoref{thm:regseqrat-common-tail-cocycle-descent} remove the remaining possible coordinates. Quotient streams, selected limits, host real equality, countable choice, metric completeness, ambient $\RealUp$ completeness, standard bridge data, and hidden completion carriers are not rows of the bundled packet or of the common-tail descent boundary.
\end{proof}

\begin{theorem}[RegSeqRat common-tail support induction]
\label{thm:regseqrat-common-tail-support-induction}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal P:\ProbeBundle$ be a finite bundle of certified unary observations over $P$. A common-tail four-face boundary over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ is accepted for $\mathcal P$ exactly when the displayed finite support of $\mathcal P$ admits an induction with the following constructors:
\begin{enumerate}
\item the empty support exposes only the accepted packet rows of $P$ and no boundary beyond the visible common-tail packet;
\item a singleton observation exposes the same $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows on the StreamName, DyadicRatCore, RegSeqRat, and RealUp faces, and the observation restricts through the common-tail window packet of \autoref{def:regseqrat-common-tail-window-packet};
\item a union support is accepted exactly when its two accepted sub-supports have matching displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows on their overlap, each added observation restricts through the common-tail packet, and the triple-overlap cocycle descends to the same four-face boundary.
\end{enumerate}
The induced boundary is exhausted by the bundled StreamName unary rows, their $\mathsf{InBundle}$ witnesses, the shared tail selector, the finite-window budget, the pointwise $\mathsf{RatHistoryClassifier}$ handoff rows, the dyadic endpoint-radius-distance ledger, the RegSeqRat classifier and regularity rows, the RealUp seal-source rows, and the support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

No quotient stream, selected limit, host real equality, countable choice, metric-completeness row, ambient $\RealUp$ completeness datum, standard bridge coordinate, or hidden completion carrier has a support row in this induction principle.
\end{theorem}
\begin{proof}
For the empty support, the accepted packet $P$ supplies the only rows to inspect. Apply \autoref{def:regseqrat-common-tail-window-packet} and \autoref{thm:regseqrat-common-tail-visible-packet-assembly}. They identify the vacuous support surface with the finite common-tail packet whose coordinates are the displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

For a singleton support, restrict the certified unary observation along the shared tail-compatible selector. The cofinal exhaustion theorem \autoref{thm:streamname-tail-compatible-window-cofinal-exhaustion} places the observation, its $\mathsf{InBundle}$ witness, the finite-window budget, and the rational-history handoff row on one displayed StreamName packet. Then \autoref{thm:regseqrat-common-tail-finite-request-real-seal-pullback} identifies the same observation with the common-tail Real seal pullback.

Now suppose two finite supports have already been accepted and are being joined. The induction hypotheses give the same displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows on each accepted sub-support. On the overlap, apply \autoref{thm:regseqrat-common-tail-union-classifier-determinacy} to obtain one classifier read for the union window.

The boundary comparison for the joined support is then forced by \autoref{thm:regseqrat-common-tail-real-boundary-equivalence}. It says that the common-tail packet and the finite-request Real boundary determine one another on exactly the displayed support rows, so no separate boundary can appear after the union classifier has been fixed.

It remains to check coherence for a further observation added to the joined support. Apply \autoref{thm:regseqrat-common-tail-triple-overlap-cocycle} on the two old support pieces and the new observation. The triple refinement exposes the same common finite surface, and \autoref{thm:regseqrat-common-tail-cocycle-descent} descends the pairwise restrictions to one finite four-face boundary.

Conversely, assume a finite four-face boundary for $\mathcal P$ is accepted. Restrict it to each constructor layer of the displayed finite support. The empty and singleton restrictions recover the packet and visible observation rows by the first two readings above, while every union restriction recovers matching overlap rows by union classifier determinacy and Real boundary equivalence. The triple-overlap cocycle theorem recovers the required descent datum for each added observation.

Induction on the displayed finite support therefore gives precisely the accepted common-tail four-face boundary stated in the theorem. The row inventory of the cited packet, exhaustion, pullback, determinacy, boundary-equivalence, and cocycle-descent results contains no quotient stream, selected limit, host real equality, countable choice, metric-completeness row, ambient $\RealUp$ completeness datum, standard bridge coordinate, or hidden completion carrier, so none can become a support row of the induction.
\end{proof}

\begin{theorem}[RegSeqRat common-tail face-status objectwise pullback]
\label{thm:regseqrat-common-tail-face-status-objectwise-pullback}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $T_0$, $T_1$, and $T_2$ be three certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose the shifted pairs land in accepted RegSeqRat common-tail triple-overlap packets whose pairwise boundaries agree on the shared $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Then the only status data pulled back by the resulting common-tail four-face cocycle are the four local face records read objectwise from the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ chapters' own $\mathsf{closurestatus}$ blocks. The cocycle creates no aggregate $\mathsf{formalstatus}$, no aggregate bridge token, no ambient $\RealUp$ completeness row, and no metric-completeness or countable-choice coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regseqrat-common-tail-triple-overlap-cocycle}. The accepted triple-overlap surface is exactly the finite common-tail surface on the displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, with no hidden completion or bridge coordinate.

Next use \autoref{thm:regseqrat-common-tail-real-boundary-equivalence}. The common-tail packet and the finite-request $\RealUp$ boundary determine one another on the same visible rows. Thus any status read available to the cocycle must pass through the same four visible faces rather than through a separate Real-completion carrier.

Now apply \autoref{thm:streamname-real-completion-four-object-terminal-pullback}. The terminal four-object boundary is built from the ordered $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ faces plus their displayed support rows. Terminality leaves no fifth status object in which an aggregate verification grade, bridge datum, completeness theorem, or choice principle could be stored.

Finally use \autoref{thm:streamname-real-completion-face-status-pullback}. That result reads closure grades, $\mathsf{formalstatus}$ tokens, Lean targets, and bridge rows only from the corresponding face chapters' own $\mathsf{closurestatus}$ records. Pulling those four records through the RegSeqRat common-tail cocycle therefore preserves them object by object, while metric completeness, countable choice, ambient $\RealUp$ completeness, and aggregate bridge or verification data remain absent from the packet.
\end{proof}

\begin{theorem}[RegSeqRat common-tail terminal section cover]
\label{thm:regseqrat-common-tail-terminal-section-cover}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\{T_i\}_{i \in I}$ be a finite family of certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose every pair $(T_i,T_j)$ lands in a RegSeqRat common-tail window packet, every triple overlap restricts to one common refinement carrying the same displayed rows, and these pairwise and triple-overlap rows satisfy the common-tail cocycle conditions of \autoref{thm:regseqrat-common-tail-four-face-cocycle-determinacy} and \autoref{thm:regseqrat-common-tail-cocycle-descent}. Then the family determines exactly one terminal finite section cover over the ordered faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.

The visible rows of this section cover are precisely the $\StreamNameUp$ schedule and refinement rows, the $\DyadicRatCoreUp$ endpoint, radius, and distance rows, the $\RegSeqRatUp$ classifier and regularity rows, the $\RealUp$ seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\Pkg$ provenance, and local $\NameCert$ provenance. The cover contains no quotient streams, host real equality, selected limits, countable choice, ambient completeness row, metric-completeness row, aggregate $\mathsf{formalstatus}$ token, standard-bridge data, or completion coordinate outside those displayed rows.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regseqrat-common-tail-window-terminality} to each pairwise packet. Each completion-facing consumer whose visible rows are the shifted reads, the common StreamName refinement, the dyadic endpoint-radius-distance ledger, the classifier and regularity ledger, the RealUp seal-source rows, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ provenance factors through the corresponding common-tail packet.

Next use \autoref{thm:regseqrat-common-tail-real-boundary-equivalence}. Pairwise common-tail packets and finite-request $\RealUp$ seal boundaries determine one another on exactly the displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, so the local sections have the same boundary vocabulary on every overlap.

The triple-overlap compatibility is supplied by \autoref{thm:regseqrat-common-tail-four-face-cocycle-determinacy} and \autoref{thm:regseqrat-common-tail-cocycle-descent}. The determinacy theorem turns agreement of the displayed rows on a triple refinement into the finite four-face cocycle condition, and the descent theorem packages the pairwise boundaries as one displayed four-face boundary whenever those cocycle rows agree.

Now compose the Real L10 terminal correspondence with pullback uniqueness. The theorems \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness} identify the pasted StreamName, DyadicRatCore, RegSeqRat, and RealUp rows as one terminal finite boundary. Applying \autoref{thm:regseqrat-common-tail-face-status-objectwise-pullback} then keeps the four face-status records objectwise, so the section cover does not acquire an aggregate status object while it is being pasted.

Uniqueness follows from the same pullback calculation. Any second section cover with the same visible local sections restricts to the same pairwise common-tail packets, hence to the same Real L10 terminal boundary on every four-face overlap. Since the listed packets provide no coordinates for quotient streams, host real equality, selected limits, countable choice, ambient or metric completeness, aggregate $\mathsf{formalstatus}$, standard-bridge data, or hidden completion rows, there is no remaining BEDC-visible row on which two terminal section covers could differ.
\end{proof}

\begin{theorem}[RegSeqRat common-tail finite-support gluing exhaustion]
\label{thm:regseqrat-common-tail-finite-support-gluing-exhaustion}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal P:\ProbeBundle$ be a finite bundle of certified unary observations over $P$ served by the shared tail-compatible $\StreamNameUp$ selector. Suppose every pair of observations in the displayed support lands in a RegSeqRat common-tail window packet, every triple overlap exposes the common-tail cocycle rows, and the pairwise packets are compatible with the finite support constructors of \autoref{thm:regseqrat-common-tail-support-induction}.

Then the finite support induction data determine exactly one terminal four-face boundary over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Its visible rows are precisely the $\StreamNameUp$ bundle rows, the $\DyadicRatCoreUp$ endpoint-radius-distance ledger, the $\RegSeqRatUp$ classifier and regularity rows, the $\RealUp$ seal-source row, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

This finite-support boundary has no quotient stream, selected limit, host real equality, countable-choice principle, metric-completeness row, ambient $\RealUp$ completeness datum, aggregate status row, standard bridge datum, or completion coordinate outside those rows.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regseqrat-common-tail-support-induction} to the displayed support of $\mathcal P$. The empty, singleton, and union constructors give finite support acceptance for exactly the bundled observations served by the shared tail-compatible selector, with the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows on every retained overlap.

Next use \autoref{thm:streamname-tail-compatible-window-cofinal-exhaustion}. The shared selector exhausts the StreamName finite bundle rows, their $\mathsf{InBundle}$ witnesses, the finite-window budget, and the pointwise rational-history handoff rows on one displayed StreamName surface.

The dyadic face is supplied by \autoref{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence}. It reads the same finite support through the endpoint, radius, and distance ledger and places that ledger on the common four-face packet without adding a separate completion coordinate.

Now apply \autoref{thm:regseqrat-common-tail-terminal-section-cover}. The accepted support, the pairwise common-tail packets, and the triple-overlap cocycle rows assemble into one terminal finite section cover over the ordered $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ faces.

Finally use \autoref{thm:real-l10-terminal-four-face-correspondence} together with pullback uniqueness. The Real L10 terminal correspondence identifies the section cover with the terminal four-face boundary over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, and uniqueness prevents a second boundary with the same visible finite support rows.

The exclusion list follows from the row inventories of the cited support induction, StreamName exhaustion, dyadic pullback, terminal section cover, and Real L10 terminality results. None of those packets contains a quotient stream, selected limit, host real equality, countable-choice principle, metric-completeness row, ambient $\RealUp$ completeness datum, aggregate status row, standard bridge datum, or hidden completion carrier, so none is a row of this boundary.
\end{proof}

\begin{theorem}[RegSeqRat common-tail terminal section Real seal-source uniqueness]
\label{thm:regseqrat-common-tail-terminal-section-real-seal-source-uniqueness}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\{T_i\}_{i \in I}$ be a finite family of certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose the pairwise common-tail window packets and triple-overlap refinements satisfy the hypotheses of \autoref{thm:regseqrat-common-tail-terminal-section-cover}, so that they determine a terminal finite section cover over the ordered faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.

Let $\mathcal R$ and $\mathcal S$ be two $\RealUp$ seal-source reads exposed over this cover. Suppose their visible rows are the same $\StreamNameUp$ schedule and refinement rows, the same $\DyadicRatCoreUp$ endpoint, radius, and distance rows, the same $\RegSeqRatUp$ classifier and regularity rows, the same $\RealUp$ seal row, and the same displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. Then $\mathcal R$ and $\mathcal S$ expose the same finite $\RealUp$ seal-source surface.

This uniqueness statement exports no quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness row, standard bridge data, or hidden completion carrier.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regseqrat-common-tail-terminal-section-cover} to the accepted RegSeqRat packet and the finite common-tail family. The output is the terminal finite section cover whose visible vocabulary is exactly the four-face row inventory listed in the statement.

Next use \autoref{thm:regseqrat-common-tail-terminal-four-face-cocycle-uniqueness}. Since $\mathcal R$ and $\mathcal S$ agree on the pairwise common-tail packets, triple-overlap refinements, and displayed support rows through the section cover, they determine the same BEDC-visible terminal four-face boundary.

Now apply the Real-side uniqueness results. By \autoref{thm:realup-finite-seal-source-uniqueness}, two finite $\RealUp$ seal-source reads with the same displayed StreamName, DyadicRatCore, RegSeqRat, Real seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows have the same finite seal-source surface. By \autoref{thm:real-name-seal-terminal-source-uniqueness}, this surface is the terminal Real name-seal source seen by any downstream finite readback through the same packet.

The final row inventory is the nonescape clause. The common-tail cover, the four-face cocycle uniqueness theorem, and the Real seal-source uniqueness theorems provide no coordinate for quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric completeness, standard bridge data, or hidden completion carriers. There is therefore no remaining displayed BEDC row on which the two Real seal-source surfaces can differ.
\end{proof}

\begin{theorem}[RegSeqRat terminal four-face cocycle uniqueness]
\label{thm:regseqrat-common-tail-terminal-four-face-cocycle-uniqueness}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal U$ and $\mathcal V$ be two finite common-tail terminal section covers over the same ordered faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. Suppose the two covers have identical pairwise common-tail window packets, identical triple-overlap cocycle rows, and the same displayed support rows $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ on every finite refinement. Then $\mathcal U$ and $\mathcal V$ are the same BEDC-visible terminal four-face boundary.

Conversely, if $\mathcal U$ and $\mathcal V$ are equal as terminal four-face boundaries, then their restrictions have the same pairwise common-tail packets, the same triple-overlap cocycle rows, and the same displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. This uniqueness statement exports no quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, aggregate $\mathsf{formalstatus}$ token, standard bridge data, or completion coordinate outside the displayed four-face packet.
\end{theorem}
\begin{proof}
Assume first that the pairwise packets, triple-overlap rows, and displayed support rows of $\mathcal U$ and $\mathcal V$ agree. Apply \autoref{thm:regseqrat-common-tail-terminal-section-cover} to each cover. Both covers are terminal finite section covers over the same ordered StreamName, DyadicRatCore, RegSeqRat, and RealUp faces and with the same visible local sections.

Use \autoref{thm:regseqrat-common-tail-cocycle-descent} on the common pairwise and triple-overlap data. The shared cocycle rows descend to one displayed four-face exit boundary carrying exactly the common $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Now apply \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}. The Real L10 terminal pullback repacks the shared displayed packet as a single terminal boundary and forbids a second BEDC-visible boundary with the same four faces and support rows. By \autoref{thm:regseqrat-common-tail-face-status-objectwise-pullback}, this identification keeps the four face-status records objectwise and adds no aggregate status coordinate.

For the converse direction, start from equality of the terminal four-face boundaries. Restrict the common boundary along each pairwise common-tail window and along each triple refinement. Terminal equality preserves exactly the displayed rows on those restrictions, so the pairwise packets, triple-overlap cocycle rows, and support rows $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ agree componentwise.

The exclusions follow from the same row inventory. The terminal section cover, the cocycle descent, the objectwise face-status pullback, and the Real L10 terminal pullback provide no coordinates for quotient streams, host real equality, selected limits, countable choice, ambient completeness, metric-completeness rows, aggregate $\mathsf{formalstatus}$, standard bridge data, or hidden completion carriers. Therefore such data cannot split two boundaries after the displayed four-face packet has been fixed, nor can it appear in the restrictions read back from an equal terminal boundary.
\end{proof}

\begin{theorem}[RegSeqRat common-tail terminal section restriction exactness]
\label{thm:regseqrat-common-tail-terminal-section-restriction-exactness}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\{T_i\}_{i \in I}$ be a finite family of certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Suppose every pair $(T_i,T_j)$ lands in a RegSeqRat common-tail window packet, every triple overlap carries the common-tail cocycle rows, and these rows determine the terminal finite section cover of \autoref{thm:regseqrat-common-tail-terminal-section-cover}. For any finite subfamily $J \subseteq I$, restrict the pairwise packets and triple-overlap cocycle rows to the indices in $J$.

Then the restriction of the terminal section cover on $I$ to $J$ is exactly the terminal finite section cover obtained by applying \autoref{thm:regseqrat-common-tail-terminal-section-cover} to the restricted pairwise and triple-overlap rows. The restricted cover has precisely the visible $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows inherited from the original family, and it introduces no quotient stream, selected limit, host real equality, countable choice, ambient completeness, metric-completeness row, aggregate $\mathsf{formalstatus}$ token, standard bridge datum, or completion coordinate outside those rows.
\end{theorem}
\begin{proof}
Project the finite family cover along the inclusion $J \subseteq I$. The projection keeps exactly the pairwise common-tail packets indexed by pairs in $J$, the triple-overlap cocycle rows indexed by triples in $J$, and the displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows on their finite refinements.

Apply \autoref{thm:regseqrat-common-tail-terminal-section-cover} to the restricted data. The hypotheses are inherited by finite restriction: every retained pair still lands in the same common-tail window packet, every retained triple still exposes the same cocycle rows, and the row vocabulary is still the displayed four-face vocabulary of the original cover.

It remains to identify the projected cover with the independently assembled subcover. Use \autoref{thm:regseqrat-common-tail-terminal-four-face-cocycle-uniqueness}. The projected cover and the subcover have identical restricted pairwise packets, identical restricted triple-overlap cocycle rows, and the same displayed support rows on every retained finite refinement, so they are the same BEDC-visible terminal four-face boundary.

Finally apply the row inventory from \autoref{thm:regseqrat-common-tail-face-status-objectwise-pullback} together with the Real L10 terminal correspondence and pullback uniqueness results already used in \autoref{thm:regseqrat-common-tail-terminal-section-cover}. Terminal restriction preserves the four objectwise face records and leaves no additional coordinate through which quotient streams, selected limits, host real equality, choice principles, ambient or metric completeness, aggregate status data, bridge rows, or hidden completion carriers could enter the finite subfamily cover.
\end{proof}

\begin{theorem}[RegSeqRat four-face terminal section functoriality]
\label{thm:regseqrat-four-face-terminal-section-functoriality}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\{T_i\}_{i \in I}$ be a finite family of certified cofinal tail shifts of the $\StreamNameUp$ schedule carried by $P$. Let $J \subseteq I$ and $K \subseteq J$ be finite request subfamilies. Suppose every pair in $I$ lands in a RegSeqRat common-tail window packet, every triple overlap carries the common-tail cocycle rows, and these rows determine the terminal finite section cover of \autoref{thm:regseqrat-common-tail-terminal-section-cover}.

Then the terminal section cover obtained by restricting the common-tail four-face cover directly from $I$ to $K$ is the same finite BEDC packet as the cover obtained by first restricting from $I$ to $J$ and then restricting from $J$ to $K$. Both routes display exactly the inherited $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, and neither route introduces quotient streams, selected limits, host real equality, countable choice, ambient completeness, aggregate $\mathsf{formalstatus}$ data, standard bridge data, or any non-displayed completion coordinate.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness} to the refinement $J \subseteq I$. The first restriction is exactly the terminal finite section cover assembled from the pairwise common-tail packets and triple-overlap cocycle rows whose indices lie in $J$.

Apply the same restriction exactness theorem to the refinement $K \subseteq J$. The second restriction keeps only the rows indexed by $K$ and again produces the terminal section cover determined by those restricted pairwise and triple-overlap rows.

Now apply \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness} directly to $K \subseteq I$. The direct route has the same restricted pairwise packets, the same restricted triple-overlap cocycle rows, and the same displayed support rows as the two-step route.

It remains to identify the two displayed packets. Use \autoref{thm:regseqrat-common-tail-terminal-four-face-cocycle-uniqueness} together with the descent calculation in \autoref{thm:regseqrat-common-tail-terminal-section-cover}. Since both routes expose the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows on every retained finite refinement, they are the same BEDC-visible terminal four-face boundary.

The exclusion list follows from the row inventory already used in the restriction exactness theorem. Neither direct restriction nor two-step restriction contains a coordinate for quotient streams, selected limits, host real equality, countable choice, ambient completeness, aggregate $\mathsf{formalstatus}$ data, standard bridge data, or a hidden completion carrier, so functoriality is witnessed entirely inside the displayed finite BEDC packet.
\end{proof}

\begin{theorem}[RegSeqRat common-tail terminal section intersection exactness]
\label{thm:regseqrat-common-tail-terminal-section-intersection-exactness}
Let $P$ be an accepted $\RegSeqRatUp$ packet. Let $\{T_i\}_{i \in I}$ and $\{S_j\}_{j \in J}$ be two finite common-tail terminal section covers of the $\StreamNameUp$ schedule carried by $P$, both over the ordered faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. Suppose every pairwise packet, every triple-overlap cocycle row, and every displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support row of the first cover agrees with the corresponding row of the second cover on each retained index in the finite intersection $I \cap J$.

Then the restriction of the first terminal section cover to $I \cap J$ and the restriction of the second terminal section cover to $I \cap J$ are the same BEDC-visible terminal four-face boundary. The common boundary is determined only by the retained common-tail packets and their displayed four-face support rows, and it contains no quotient stream, selected limit, host real equality, countable choice, ambient completeness, aggregate $\mathsf{formalstatus}$ data, standard bridge row, or hidden completion carrier.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness} to the first cover along the inclusion $I \cap J \subseteq I$. Apply the same theorem to the second cover along $I \cap J \subseteq J$. The two restricted covers are terminal finite section covers built from exactly the pairwise common-tail packets, triple-overlap cocycle rows, and displayed support rows retained by the finite intersection.

The hypothesis identifies those retained rows component by component. Hence the two restricted covers have identical pairwise packets, identical triple-overlap cocycle rows, and the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows on every overlap that remains in $I \cap J$.

Use \autoref{thm:regseqrat-common-tail-terminal-four-face-cocycle-uniqueness}. Since the restricted covers expose the same retained finite data, uniqueness identifies them as one BEDC-visible terminal four-face boundary.

The descent and Real terminality inputs make the identification intrinsic to the displayed packet. By \autoref{thm:regseqrat-common-tail-cocycle-descent}, the common retained pairwise packets descend to one four-face boundary. By \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}, that descended boundary is the unique terminal pullback over the ordered StreamName, DyadicRatCore, RegSeqRat, and RealUp faces.

The exclusions follow from the same row inventory. The restriction theorem, cocycle uniqueness, cocycle descent, and Real L10 pullback uniqueness provide no coordinate for quotient streams, selected limits, host real equality, countable choice, ambient completeness, aggregate $\mathsf{formalstatus}$ data, standard bridge rows, or hidden completion carriers. Thus the meet boundary is exact on the retained finite intersection and nowhere else.
\end{proof}

\begin{theorem}[RegSeqRat common-tail terminal four-face sheaf exactness]
\label{thm:regseqrat-common-tail-terminal-four-face-sheaf-exactness}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\{T_i\}_{i \in I}$ be a finite family of common-tail terminal section covers of the $\StreamNameUp$ schedule carried by $P$, all over the ordered faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. Suppose the family carries compatible pairwise common-tail packets and triple-overlap cocycle rows on the finite intersections of the index set.

Then those data determine a unique BEDC-visible terminal four-face boundary if and only if the covers agree on all finite intersections. The visible rows of the boundary are exactly the four faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ together with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. No quotient stream, host real equality, selected limit, countable-choice principle, ambient completeness row, metric-completeness row, aggregate $\mathsf{formalstatus}$ token, standard bridge row, or hidden completion carrier is part of the glued boundary.
\end{theorem}
\begin{proof}
Assume first that the covers agree on all finite intersections. Apply \autoref{thm:regseqrat-common-tail-terminal-section-cover} to each local common-tail family. Each local packet is a terminal finite section cover with the same displayed StreamName, DyadicRatCore, RegSeqRat, RealUp, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows on its finite window.

Use \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness} to pass from a cover to any retained finite subfamily, and use \autoref{thm:regseqrat-common-tail-terminal-section-intersection-exactness} on every overlap of two such subfamilies. These two exactness statements identify the retained pairwise packets and triple-overlap cocycle rows on the intersections, so the local terminal boundaries have one common displayed packet wherever they meet.

The compatible local packets now have one global boundary. Apply \autoref{thm:regseqrat-common-tail-terminal-four-face-cocycle-uniqueness}: any two terminal four-face presentations with the same pairwise packets, triple-overlap rows, and support rows are the same BEDC-visible boundary. Thus the glued packet is unique on the displayed rows.

Finally keep the glued packet on the finite Real L10 route. The theorems \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness} repack the StreamName, DyadicRatCore, RegSeqRat, and RealUp rows as the terminal four-face pullback and forbid a second boundary with the same visible support data.

Conversely, suppose a unique BEDC-visible terminal four-face boundary is already determined by the family. Restrict that boundary to any finite intersection of covers. By \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness}, each restricted cover is the terminal finite section cover obtained from the retained pairwise and triple-overlap data.

Apply \autoref{thm:regseqrat-common-tail-terminal-section-intersection-exactness} to the two restricted presentations. Since both are restrictions of the same terminal boundary, their displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows agree on the retained finite intersection.

The exclusion clauses follow from the row inventory in the cited cover, restriction, intersection, uniqueness, and Real L10 pullback results. Those results expose only the four named faces and the listed support rows, so quotient streams, host real equality, selected limits, countable choice, ambient or metric completeness, aggregate status data, standard bridge rows, and hidden completion carriers cannot appear in the sheaf boundary or distinguish two glued presentations.
\end{proof}

\begin{definition}[RegSeqRat common-tail descent atlas]
\label{def:regseqrat-common-tail-descent-atlas}
Let $P$ be an accepted $\RegSeqRatUp$ packet. A RegSeqRat common-tail descent atlas for $P$ is a finite family $\mathcal A=\{U_i\}_{i \in I}$ of common-tail terminal section covers of the $\StreamNameUp$ schedule carried by $P$, together with the following BEDC-visible rows:
\begin{enumerate}
\item for every retained pair $(i,j)$, a RegSeqRat common-tail window packet on $U_i \cap U_j$ with the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ faces;
\item for every retained triple $(i,j,k)$, the triple-overlap cocycle rows asserting that the three pairwise packets restrict to the same common-tail boundary on $U_i \cap U_j \cap U_k$;
\item restriction maps from each $U_i$ to every finite subfamily and intersection agreements for every finite meet of subfamilies;
\item support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ witnessing that all local faces, pairwise packets, triple rows, restrictions, and intersection agreements are carried by the same finite BEDC packet.
\end{enumerate}
The atlas is accepted when each local cover is terminal in the sense of \autoref{thm:regseqrat-common-tail-terminal-section-cover}, each finite restriction satisfies \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness}, and each finite meet satisfies \autoref{thm:regseqrat-common-tail-terminal-section-intersection-exactness}. The atlas contains no quotient stream, host real equality, selected limit, countable-choice principle, ambient completeness row, metric-completeness row, aggregate $\mathsf{formalstatus}$ token, standard bridge row, or hidden completion carrier outside the displayed four-face and support rows.
\end{definition}

\begin{theorem}[RegSeqRat common-tail descent atlas Real seal exactness]
\label{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A=\{U_i\}_{i \in I}$ be an accepted RegSeqRat common-tail descent atlas for $P$. Then $\mathcal A$ determines a unique Real seal-visible terminal four-face boundary over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ if and only if its finite section covers agree on all finite intersections.

The boundary displays exactly the atlas rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ together with the four named faces. It exports no quotient stream, host real equality, selected limit, countable-choice principle, ambient completeness row, metric-completeness row, aggregate $\mathsf{formalstatus}$ token, standard bridge row, or hidden completion carrier.
\end{theorem}
\begin{proof}
Assume first that the finite section covers in $\mathcal A$ agree on all finite intersections. By the atlas acceptance clauses and \autoref{thm:regseqrat-common-tail-terminal-section-cover}, every $U_i$ supplies a terminal common-tail section cover over the same ordered StreamName, DyadicRatCore, RegSeqRat, and RealUp faces. Its support rows are the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the atlas.

Use \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness} for finite subfamilies and \autoref{thm:regseqrat-common-tail-terminal-section-intersection-exactness} for finite meets. These two exactness results identify the pairwise packets, triple-overlap cocycle rows, restriction maps, and intersection agreements on every retained overlap.

Now apply \autoref{thm:regseqrat-common-tail-terminal-four-face-sheaf-exactness}. The compatible finite atlas rows glue to one BEDC-visible terminal four-face boundary, and the same theorem gives uniqueness among terminal presentations with the displayed rows.

Finally pass from the RegSeqRat common-tail boundary to the Real seal-visible boundary. The Real L10 terminal four-face correspondence and pullback uniqueness theorems, \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}, identify the glued StreamName, DyadicRatCore, RegSeqRat, and RealUp packet with the unique terminal Real seal boundary seen by downstream Phase Real and Completion reads.

Conversely, suppose $\mathcal A$ determines a unique Real seal-visible terminal four-face boundary. Restrict that boundary to any finite intersection of atlas covers. By \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness}, the restriction is the terminal finite section cover assembled from the retained pairwise packets and triple-overlap cocycle rows.

Apply \autoref{thm:regseqrat-common-tail-terminal-section-intersection-exactness} to any two such restricted presentations. Since both are restrictions of the same Real seal-visible terminal boundary, their displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows agree on the finite meet.

The nonescape statement follows from the same inventory. The atlas definition, the terminal cover theorem, the restriction and intersection exactness theorems, the sheaf exactness theorem, and the Real L10 terminal pullback provide no coordinate outside the named faces and support rows. Hence no quotient stream, host real equality, selected limit, choice principle, ambient or metric completeness row, aggregate status token, standard bridge row, or hidden completion carrier can distinguish two descended Real seal boundaries.
\end{proof}

\begin{theorem}[RegSeqRat common-tail descent atlas finite-request coverage]
\label{thm:regseqrat-common-tail-descent-atlas-finite-request-coverage}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A=\{U_i\}_{i \in I}$ be an accepted RegSeqRat common-tail descent atlas for $P$. Every finite-request $\RealUp$ seal consumer whose visible rows are carried by $\mathcal A$ factors through a unique atlas member or finite intersection. The pullback exposed to that consumer is exactly the four faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ together with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

No quotient stream, host real equality, selected limit, countable-choice row, ambient completeness row, metric-completeness row, aggregate $\mathsf{formalstatus}$ token, standard bridge row, or hidden completion carrier is exported by this finite-request factorization.
\end{theorem}
\begin{proof}
Let $C$ be a finite-request $\RealUp$ seal consumer whose visible rows are carried by $\mathcal A$. The request reads only finitely many displayed rows, so its support is contained in one retained atlas member or in a finite meet of retained members.

If the support is contained in one member $U_i$, apply \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness}. The consumer factors through the terminal section cover on $U_i$, and terminality gives uniqueness of the factorization over the displayed StreamName, DyadicRatCore, RegSeqRat, and RealUp rows.

If the support uses several members, apply \autoref{thm:regseqrat-common-tail-terminal-section-intersection-exactness}. The finite meet carries the same retained pairwise packets, triple-overlap cocycle rows, and support rows, so the request factors through that finite intersection with a unique displayed pullback.

Now use \autoref{thm:regseqrat-common-tail-finite-request-real-seal-pullback}. It identifies the local member or intersection factorization with the finite $\RealUp$ seal pullback seen by $C$, and it exposes exactly the four named faces together with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The local pullbacks glue to one atlas boundary by \autoref{thm:regseqrat-common-tail-terminal-four-face-sheaf-exactness}. Finally, \autoref{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness} identifies this glued boundary with the unique Real seal-visible terminal boundary of the accepted atlas. Therefore every finite request carried by $\mathcal A$ is covered by a unique member or finite-intersection factorization.

The exclusion statement follows from the same row inventory. The cited pullback, restriction, intersection, sheaf, and atlas exactness theorems contain no coordinate for quotient streams, host real equality, selected limits, countable choice, ambient or metric completeness, aggregate status data, standard bridge data, or hidden completion carriers.
\end{proof}

\begin{theorem}[RegSeqRat common-tail filtered colimit normal form]
\label{thm:regseqrat-common-tail-filtered-colimit-normal-form}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A=\{U_i\}_{i \in I}$ be a finite directed atlas of accepted common-tail windows for $P$. Suppose every refinement map in $\mathcal A$ preserves the displayed $\StreamNameUp$ schedule rows, $\DyadicRatCoreUp$ endpoint and radius ledgers, $\RegSeqRatUp$ classifier and readback rows, local $\RealUp$ seal rows, and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. Then the filtered colimit read of $\mathcal A$ has a unique finite BEDC normal form over exactly the four faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, together with those support rows.

The normal form is the retained finite atlas display: each finite consumer factors through one retained common-tail member or retained finite intersection, and two such displays are equal precisely when their four face rows and support rows agree there. It exports no quotient stream equality, selected limit, countable-choice row, metric-completeness datum, aggregate $\mathsf{formalstatus}$ row, aggregate bridge row, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
First reduce the directed atlas to its finite support. Apply \autoref{thm:regseqrat-common-tail-support-induction} to the retained common-tail observations. The empty, singleton, and union constructors keep every schedule row, dyadic endpoint-radius ledger, RegSeqRat classifier and readback row, local Real seal row, and support row inside the same finite BEDC packet.

Next glue the retained windows. The finite-support gluing exhaustion theorem \autoref{thm:regseqrat-common-tail-finite-support-gluing-exhaustion} says that pairwise packets, triple-overlap cocycle rows, and compatible support rows assemble to one finite four-face boundary. The descent-atlas coverage theorem \autoref{thm:regseqrat-common-tail-descent-atlas-finite-request-coverage} then places every finite $\RealUp$ seal request on a retained member or retained finite intersection.

Now identify the four faces read by the L10 route. Use \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness} to keep the StreamName schedule, DyadicRatCore ledger, RegSeqRat readback, and Real seal rows on the same handoff surface. The Real L10 terminal correspondence and pullback uniqueness theorems, \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}, repack that surface as the unique terminal finite boundary over the four named faces.

It remains to compare two proposed colimit reads. Restrict both reads to any retained member or retained finite intersection. By finite-request coverage they have the same visible local factorization, and by the terminal pullback uniqueness theorem a second normal form with the same displayed rows has no BEDC coordinate on which to differ. Hence the colimit read is the unique finite normal form carried by the atlas.

The excluded data have no route into the construction. The cited support induction, finite-support gluing, descent coverage, handoff exactness, and Real terminal boundary theorems expose only the four face rows and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. Quotient stream equality, selected limits, countable choice, metric completeness, aggregate status records, aggregate bridge records, and ambient $\RealUp$ completeness therefore cannot be exported by the normal form.
\end{proof}

\begin{theorem}[RegSeqRat common-tail descent atlas classifier completeness]
\label{thm:regseqrat-common-tail-descent-atlas-classifier-completeness}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A=\{U_i\}_{i \in I}$ be an accepted RegSeqRat common-tail descent atlas for $P$. For any two finite $\RealUp$ seal consumers visible through $\mathcal A$, the consumers determine the same Real seal-visible terminal boundary exactly when their displayed $\mathsf{RegSeqRatClassifier}$ rows agree on every retained finite section and every retained finite intersection of the atlas.

The complete finite witness for that boundary is precisely the atlas display over the four faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, together with the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. No quotient stream, host real equality, selected limit, choice principle, ambient completeness row, metric-completeness row, standard bridge row, or hidden completion carrier is exported by the classifier-complete Real seal boundary.
\end{theorem}
\begin{proof}
Assume first that the two visible $\RealUp$ seal consumers determine the same Real seal-visible terminal boundary through $\mathcal A$. Apply \autoref{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness}. The shared boundary restricts to the same accepted finite section cover on every retained section and every finite intersection of the atlas.

Use \autoref{thm:regseqrat-common-tail-terminal-four-face-sheaf-exactness}. Since the restricted presentations are one terminal four-face boundary, their displayed StreamName, DyadicRatCore, RegSeqRat, and RealUp rows agree on each retained finite meet. Applying \autoref{thm:regseqrat-common-tail-window-classifier-bridge} on those common-tail windows identifies the transported $\mathsf{RegSeqRatClassifier}$ rows seen by the two consumers.

Conversely, suppose the displayed $\mathsf{RegSeqRatClassifier}$ rows of the two consumers agree on every retained finite section and finite intersection of $\mathcal A$. The bridge theorem \autoref{thm:regseqrat-common-tail-window-classifier-bridge} turns each common-tail agreement into one shared classifier and seal-source surface on the corresponding finite window.

Apply \autoref{thm:regseqrat-common-tail-terminal-four-face-sheaf-exactness}. The matching classifier rows, together with the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, glue to one BEDC-visible terminal four-face boundary. Then \autoref{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness} identifies that glued boundary with the unique Real seal-visible terminal boundary determined by the accepted atlas.

Finally use the Real L10 terminal correspondence and pullback uniqueness theorems, \autoref{thm:real-l10-terminal-four-face-correspondence} and \autoref{thm:real-l10-terminal-pullback-uniqueness}. They say that a terminal presentation over the ordered StreamName, DyadicRatCore, RegSeqRat, and RealUp faces is uniquely read as the Real seal boundary. Thus the classifier rows are complete for the finite boundary witnessed by $\mathcal A$.

The nonescape clause is just the row inventory of the construction. The cited atlas exactness, classifier bridge, sheaf exactness, and Real L10 terminal pullback results expose only the four named faces and the listed support rows, so the excluded quotient, equality, limit, choice, completeness, bridge, and hidden-carrier data have no displayed coordinate in the classifier-complete boundary.
\end{proof}

\begin{theorem}[RegSeqRat common-tail descent atlas boundary naturality]
\label{thm:regseqrat-common-tail-descent-atlas-boundary-naturality}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A$ and $\mathcal B$ be accepted RegSeqRat common-tail descent atlases for $P$. Suppose a finite boundary-preserving atlas morphism sends every retained member, retained pairwise packet, retained triple-overlap cocycle row, and displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support row of $\mathcal A$ to the corresponding displayed row of $\mathcal B$. Then the Real seal-visible terminal boundary induced by $\mathcal A$ maps to the Real seal-visible terminal boundary induced by $\mathcal B$.

Conversely, for any such finite atlas morphism, equality of the induced Real seal-visible terminal boundary forces agreement of the displayed $\mathsf{RegSeqRatClassifier}$ rows on every retained member and every retained finite intersection of $\mathcal A$ and $\mathcal B$.

No quotient stream equality, selected limit, host real equality, countable-choice principle, ambient completeness row, metric-completeness row, aggregate status row, standard bridge row, or hidden completion carrier is exported or used by this naturality statement.
\end{theorem}
\begin{proof}
First consider the forward transport. By \autoref{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness}, the Real seal-visible terminal boundary of each accepted atlas is identified by its displayed common-tail section covers, finite intersections, and four named faces. The morphism preserves exactly those retained rows, so it carries the boundary presentation of $\mathcal A$ to a boundary presentation over the displayed rows of $\mathcal B$.

Use \autoref{thm:regseqrat-common-tail-descent-atlas-finite-request-coverage} to factor every finite Real seal request through a retained member or retained finite intersection. On those factors the morphism is already row-preserving by hypothesis. The restriction and intersection exactness theorems, \autoref{thm:regseqrat-common-tail-terminal-section-restriction-exactness} and \autoref{thm:regseqrat-common-tail-terminal-section-intersection-exactness}, keep the transported packet inside the same finite L10 display.

The transported local packets glue in $\mathcal B$ by \autoref{thm:regseqrat-common-tail-terminal-four-face-sheaf-exactness}. Since the glued packet has the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of $\mathcal B$, another application of \autoref{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness} identifies it with the Real seal-visible terminal boundary induced by $\mathcal B$.

Now assume the induced Real seal-visible terminal boundaries are equal along the same morphism. Restrict the equality to any retained member or retained finite intersection. The finite-request coverage theorem places that restriction in the displayed finite section cover, and the restriction and intersection exactness theorems identify the common-tail window on which both atlas presentations are compared.

Apply \autoref{thm:regseqrat-common-tail-descent-atlas-classifier-completeness}. Equality of the Real seal-visible terminal boundary on each retained finite window recovers equality of the displayed $\mathsf{RegSeqRatClassifier}$ rows there. Hence the morphism forces classifier agreement on every retained member and every retained finite intersection.

The nonescape clause follows from the same inventory. The atlas definition, the Real seal exactness theorem, finite-request coverage, classifier completeness, restriction and intersection exactness, and four-face sheaf exactness expose only the four named faces and the listed support rows. Therefore quotient stream equality, selected limits, host real equality, countable choice, ambient or metric completeness, aggregate status rows, standard bridge rows, and hidden completion carriers have no coordinate through which this naturality result can factor.
\end{proof}

\begin{theorem}[RegSeqRat audit-threshold atlas naturality]
\label{thm:regseqrat-audit-threshold-atlas-naturality}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A$ and $\mathcal B$ be accepted RegSeqRat common-tail descent atlases for $P$. Suppose a finite boundary-preserving atlas morphism sends every retained member, retained pairwise packet, retained triple-overlap cocycle row, and displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support row of $\mathcal A$ to the corresponding displayed row of $\mathcal B$. Then the morphism transports the Phase $\RealUp$ and Completion audit-threshold coordinate induced by $\mathcal A$ to the audit-threshold coordinate induced by $\mathcal B$.

Conversely, for any such finite atlas morphism, equality of the induced Phase $\RealUp$ and Completion audit-threshold coordinates forces agreement of the displayed $\mathsf{RegSeqRatClassifier}$ rows on every retained member and every retained finite intersection of $\mathcal A$ and $\mathcal B$.

No quotient stream, selected limit, host real equality, countable-choice principle, ambient $\RealUp$ completeness, metric-completeness row, aggregate $\mathsf{formalstatus}$ row, aggregate bridge row, or hidden completion carrier is exported or used by this audit-threshold naturality statement.
\end{theorem}
\begin{proof}
First transport the underlying atlas boundary. Apply \autoref{thm:regseqrat-common-tail-descent-atlas-boundary-naturality}. The finite morphism preserves the retained members, finite intersections, four displayed faces, and support rows, so it carries the Real seal-visible terminal boundary induced by $\mathcal A$ to the one induced by $\mathcal B$.

Next read the audit-threshold coordinate on the transported boundary. Apply \autoref{thm:regseqrat-four-face-audit-threshold-factorization}. The Phase $\RealUp$ and Completion audit-threshold read is the RegSeqRat four-face exit-threshold equivalence together with the local closurestatus and formalstatus records for $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.

It remains to identify those local records after transport. Use \autoref{thm:streamname-four-face-audit-clean-threshold-readback} for the StreamName-side threshold readback, \autoref{thm:real-l10-current-phase-exit-audit-pullback} for the Phase $\RealUp$ and Completion audit pullback, and \autoref{thm:dyadicratcore-l10-exit-namecert-interface} for the shared $\NameCert$ interface over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Since the morphism preserves each of these displayed rows, the audit-threshold coordinate read from $\mathcal A$ is the coordinate read from $\mathcal B$.

For the converse, assume the induced audit-threshold coordinates are equal along the same finite morphism. The factorization theorem reads both coordinates through the same RegSeqRat four-face exit-threshold surface and the same four local face records, while the StreamName readback, Real audit pullback, and DyadicRatCore interface theorem keep the support rows objectwise.

Apply \autoref{thm:regseqrat-common-tail-descent-atlas-boundary-naturality} to the boundary left by these equal threshold reads. Its converse clause recovers agreement of the displayed $\mathsf{RegSeqRatClassifier}$ rows on every retained member and every retained finite intersection.

The nonescape clause is the row inventory of the construction. The cited boundary naturality, audit-threshold factorization, StreamName readback, Real audit pullback, and DyadicRatCore interface theorems expose only the four named faces and the listed support rows. Therefore quotient streams, selected limits, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness data, aggregate status data, aggregate bridge data, and hidden completion carriers have no coordinate through which this naturality result can factor.
\end{proof}

\begin{theorem}[RegSeqRat common-tail atlas finality]
\label{thm:regseqrat-common-tail-atlas-finality}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A$ be an accepted common-tail descent atlas for $P$ in the sense of \autoref{def:regseqrat-common-tail-descent-atlas}. Let $\mathcal T$ be any finite L10 terminal presentation over the same four faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and local $\RealUp$. Suppose every finite $\RealUp$ seal request of $\mathcal T$ factors through a retained member or retained finite intersection of $\mathcal A$, and suppose the displayed $\mathsf{RegSeqRatClassifier}$ rows of $\mathcal T$ agree with the rows of $\mathcal A$ on all such overlaps. Then there is a unique boundary-preserving morphism from $\mathcal T$ into $\mathcal A$.

Equivalently, $\mathcal A$ is terminal among finite common-tail presentations inducing the same Real seal-visible boundary and the same Phase $\RealUp$ and Completion audit-threshold coordinate. This terminal surface exports no quotient streams, selected limits, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness rows, aggregate $\mathsf{formalstatus}$ rows, aggregate bridge rows, or hidden completion carriers.
\end{theorem}
\begin{proof}
First build the local comparison map. By \autoref{thm:regseqrat-common-tail-descent-atlas-finite-request-coverage}, each finite $\RealUp$ seal request of $\mathcal T$ is read inside a retained member or retained finite intersection of $\mathcal A$. The hypotheses therefore place every displayed StreamName, DyadicRatCore, RegSeqRat, local RealUp, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ row of $\mathcal T$ on a retained atlas window.

On each retained window, apply \autoref{thm:regseqrat-common-tail-descent-atlas-classifier-completeness}. Agreement of the displayed classifier rows on overlaps identifies the Real seal-visible boundary carried by $\mathcal T$ with the boundary of the corresponding retained part of $\mathcal A$. The Real seal exactness theorem \autoref{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness} then supplies the unique local factorization into the accepted atlas boundary.

The local factorizations glue because they agree on every retained finite intersection. This is exactly the boundary-preserving content of \autoref{thm:regseqrat-common-tail-descent-atlas-boundary-naturality}: a finite atlas morphism preserving the retained rows transports the Real seal-visible terminal boundary and, conversely, boundary equality recovers classifier agreement on the overlaps. Hence the locally defined comparison maps assemble into one boundary-preserving morphism $\mathcal T \to \mathcal A$.

Uniqueness follows by applying the same two readings in reverse. Any boundary-preserving morphism from $\mathcal T$ into $\mathcal A$ has the same finite $\RealUp$ seal requests by \autoref{thm:regseqrat-common-tail-descent-atlas-real-seal-exactness}; it has the same overlap classifier rows by \autoref{thm:regseqrat-common-tail-descent-atlas-classifier-completeness}; and it has the same Phase $\RealUp$ and Completion audit-threshold coordinate by \autoref{thm:regseqrat-audit-threshold-atlas-naturality}. Thus no second morphism can differ on any retained member, finite intersection, boundary row, or audit-threshold coordinate.

The nonescape clause is the displayed row inventory. The atlas definition and the cited exactness, coverage, classifier, boundary-naturality, and audit-threshold-naturality theorems expose only the four named faces and the support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. There is no coordinate for quotient streams, selected limits, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness rows, aggregate formal-status rows, aggregate bridge rows, or hidden completion carriers.
\end{proof}

\begin{theorem}[RegSeqRat common-tail atlas refinement invariance]
\label{thm:regseqrat-common-tail-atlas-refinement-invariance}
Let $P$ be an accepted $\RegSeqRatUp$ packet, let $\mathcal A$ be an accepted common-tail descent atlas for $P$, and let $\mathcal B$ be a finite certified refinement of $\mathcal A$. Suppose the refinement preserves every retained member of $\mathcal A$ by a finite cover in $\mathcal B$, preserves the retained pairwise packets and triple-overlap cocycle rows, and preserves the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. Then the Real seal-visible terminal boundary induced by $\mathcal B$ factors through the boundary induced by $\mathcal A$, and the induced Phase $\RealUp$ and Completion audit-threshold coordinate of $\mathcal B$ agrees with the coordinate induced by $\mathcal A$ under this factorization.

Conversely, any finite certified refinement with the same retained-row preservation and with the same induced Real seal-visible terminal boundary and audit-threshold coordinate is already equivalent to $\mathcal A$ on every retained common-tail member, pairwise packet, and triple-overlap cocycle row.

This refinement-invariance surface exports no quotient streams, selected limits, host equality, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness rows, aggregate $\mathsf{formalstatus}$ rows, aggregate bridge rows, or hidden completion carriers.
\end{theorem}
\begin{proof}
First identify the support packet visible to the refinement. Apply \autoref{thm:streamname-finite-cover-terminal-support-minimality}: the refined finite cover cannot introduce a second $\StreamNameUp$ support surface, so every retained StreamName coordinate of $\mathcal B$ reads through the support packet already displayed by $\mathcal A$.

Next perform the finite-cover comparison on the RegSeqRat and Real faces. The refinement hypotheses give exactly the retained members, pairwise packets, triple-overlap cocycle rows, and support rows required by \autoref{thm:regseqrat-finite-cover-terminal-refinement-invariance}. Applying \autoref{thm:regseqrat-real-seal-finite-cover-refinement-invariance} then identifies the refined Real seal-visible terminal boundary with the boundary obtained from $\mathcal A$ through the certified finite cover.

Now pass from finite covers to atlas boundaries. Use \autoref{thm:regseqrat-common-tail-descent-atlas-boundary-naturality} to view the refinement as a boundary-preserving atlas morphism. By \autoref{thm:regseqrat-common-tail-atlas-finality}, the resulting finite common-tail presentation has the unique factorization into $\mathcal A$, so the boundary induced by $\mathcal B$ factors through the boundary induced by $\mathcal A$.

It remains to compare the audit-threshold coordinate. Apply \autoref{thm:regseqrat-audit-threshold-atlas-naturality} to the same boundary-preserving refinement morphism. Since the refinement preserves the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, the Phase $\RealUp$ and Completion audit-threshold coordinate read from $\mathcal B$ is the one read from $\mathcal A$.

For the converse, assume the refined atlas has the same boundary and audit-threshold coordinate under the retained-row preservation hypotheses. The converse clauses of boundary naturality and audit-threshold atlas naturality recover agreement of the displayed $\mathsf{RegSeqRatClassifier}$ rows on each retained member and finite intersection. The retained pairwise packets and triple-overlap cocycle rows are therefore the same certified finite data as those of $\mathcal A$.

The nonescape statement is again a row inventory. The support-minimality, finite-cover refinement, Real-seal refinement, boundary-naturality, atlas-finality, and audit-threshold-naturality theorems use only finite retained covers and the displayed support rows, leaving no coordinate for quotient streams, selected limits, host equality, choice, ambient or metric completeness, aggregate status data, aggregate bridge data, or hidden completion carriers.
\end{proof}

\begin{theorem}[RegSeqRat descent atlas audit-threshold fusion]
\label{thm:regseqrat-common-tail-descent-atlas-threshold-fusion}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A$ be an accepted common-tail descent atlas for $P$ in the sense of \autoref{def:regseqrat-common-tail-descent-atlas}. The atlas presents the current Phase $\RealUp$ and Completion audit-threshold read if and only if the following three conditions hold:
\begin{enumerate}
\item its finite section covers satisfy common-tail terminal four-face sheaf exactness;
\item its displayed $\mathsf{RegSeqRatClassifier}$ rows satisfy descent-atlas classifier completeness on every retained member and finite intersection;
\item its Real seal-visible boundary is final among finite common-tail presentations inducing the same four local face records.
\end{enumerate}
When these conditions hold, the audit read is exhausted exactly by the four faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and local $\RealUp$, together with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

No quotient stream, selected limit, host equality, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness row, aggregate $\mathsf{formalstatus}$ row, aggregate bridge row, or hidden completion carrier is part of the presented audit-threshold read.
\end{theorem}
\begin{proof}
For the forward direction, assume that $\mathcal A$ presents the current Phase $\RealUp$ and Completion audit-threshold read. Apply \autoref{thm:regseqrat-four-face-audit-threshold-factorization}. The threshold read factors through the RegSeqRat four-face exit surface, so the only local face records available to the presentation are the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and local $\RealUp$ records.

The exactness and classifier clauses are then read back on the atlas itself. Use \autoref{thm:regseqrat-audit-threshold-atlas-naturality} to transport the threshold coordinate through boundary-preserving atlas maps, and use \autoref{thm:regseqrat-common-tail-atlas-finality} to compare any finite common-tail presentation with $\mathcal A$. The StreamName readback, Real audit pullback, and DyadicRatCore interface theorems, \autoref{thm:streamname-four-face-audit-clean-threshold-readback}, \autoref{thm:real-l10-current-phase-exit-audit-pullback}, and \autoref{thm:dyadicratcore-l10-exit-namecert-interface}, identify the support rows as $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

It remains, in the forward direction, to record that the three displayed conditions are precisely the atlas witnesses used by the presentation. The sheaf condition is \autoref{thm:regseqrat-common-tail-terminal-four-face-sheaf-exactness}; the classifier condition is \autoref{thm:regseqrat-common-tail-descent-atlas-classifier-completeness}; and the finality condition is \autoref{thm:regseqrat-common-tail-atlas-finality}. Thus a presentation of the audit-threshold read exposes all three witnesses and no additional carrier.

Conversely, assume the three conditions. Common-tail terminal four-face sheaf exactness glues the finite section covers to one BEDC-visible terminal boundary over the four named faces. Classifier completeness identifies the same Real seal-visible boundary for every finite consumer whose displayed classifier rows agree on the retained members and finite intersections.

Now apply atlas finality. Any finite common-tail presentation inducing the same four local face records has a unique boundary-preserving morphism into $\mathcal A$. Audit-threshold atlas naturality then identifies the Phase $\RealUp$ and Completion audit-threshold coordinate along that unique morphism, so the atlas carries a single objectwise audit read.

Finally apply the StreamName, Real, and Dyadic L10 interface theorems cited above. They read the unique threshold coordinate through exactly the four local faces and the support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Since none of the cited exactness, classifier, finality, naturality, or L10 pullback results exports quotient streams, selected limits, host equality, countable choice, ambient or metric completeness, aggregate status data, aggregate bridge data, or hidden completion carriers, the presented read is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[RegSeqRat common-tail descent atlas support minimality]
\label{thm:regseqrat-common-tail-descent-atlas-support-minimality}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A$ be an accepted common-tail descent atlas for $P$ in the sense of \autoref{def:regseqrat-common-tail-descent-atlas}. Suppose $\mathcal A$ presents the current Phase $\RealUp$ and Completion audit-threshold read. If one deletes any retained member, retained pairwise packet, retained triple-overlap cocycle row, or any displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, or $\NameCert$ support row, the remaining finite surface no longer presents that audit-threshold read.

Conversely, any accepted common-tail descent atlas carrying all retained members, retained pairwise packets, retained triple-overlap cocycle rows, all displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, and the three fusion conditions of \autoref{thm:regseqrat-common-tail-descent-atlas-threshold-fusion}, presents the same current Phase $\RealUp$ and Completion audit-threshold read.

This support-minimality statement exports no quotient streams, selected limits, host equality, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness rows, aggregate $\mathsf{formalstatus}$ rows, aggregate bridge rows, or hidden completion carriers.
\end{theorem}
\begin{proof}
First read the accepted atlas through the fusion theorem. By \autoref{thm:regseqrat-common-tail-descent-atlas-threshold-fusion}, presenting the audit-threshold read is equivalent to the common-tail terminal four-face sheaf condition, descent-atlas classifier completeness, and finality among finite common-tail presentations with the same four local face records. The same theorem identifies the displayed support as exactly $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

Now delete one retained member, retained pairwise packet, or retained triple-overlap cocycle row. The finite-support gluing exhaustion theorem \autoref{thm:regseqrat-common-tail-finite-support-gluing-exhaustion} says that every finite Real seal request is exhausted by retained members and finite intersections of $\mathcal A$. Removing one of those rows leaves a request or overlap without its displayed factorization, so the sheaf and classifier clauses required by the fusion theorem can no longer be witnessed.

If instead one deletes one displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, or $\NameCert$ support row, apply \autoref{thm:regseqrat-audit-threshold-atlas-naturality}. Audit-threshold transport is defined only along boundary-preserving atlas morphisms that preserve precisely those rows. With any one of them absent, the transported Phase $\RealUp$ and Completion audit-threshold coordinate has no objectwise support coordinate, so the remaining surface cannot be the same presented read.

Finality gives the necessity conclusion. By \autoref{thm:regseqrat-common-tail-atlas-finality}, every finite common-tail presentation of the same boundary and audit-threshold coordinate factors uniquely through $\mathcal A$. A surface obtained by deleting a retained row would either omit a finite request required by gluing exhaustion or fail to preserve a support coordinate required by naturality. Such a surface therefore cannot receive the unique final comparison as a presentation of the same read.

For the converse, assume an accepted atlas keeps all listed rows and satisfies the three fusion conditions. Apply \autoref{thm:regseqrat-common-tail-descent-atlas-threshold-fusion} to obtain a presented audit-threshold read. Then use \autoref{thm:regseqrat-common-tail-atlas-refinement-invariance} to compare it with $\mathcal A$ along the retained-row refinement surface, and use \autoref{thm:regseqrat-audit-threshold-atlas-naturality} to transport the coordinate. The result is the same current Phase $\RealUp$ and Completion audit-threshold read.

The exclusions are part of the same inventory. The proof uses only threshold fusion, atlas finality, refinement invariance, audit-threshold naturality, and finite-support gluing exhaustion; none of these results supplies a coordinate for quotient streams, selected limits, host equality, countable choice, ambient or metric completeness, aggregate status data, aggregate bridge data, or hidden completion carriers.
\end{proof}

\begin{theorem}[RegSeqRat terminal support carrier inversion]
\label{thm:regseqrat-common-tail-terminal-support-carrier-inversion}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $\mathcal A$ be an accepted common-tail descent atlas for $P$ in the sense of \autoref{def:regseqrat-common-tail-descent-atlas}. Suppose $\mathcal A$ presents the current Phase $\RealUp$ and Completion audit-threshold read. Then every downstream L10 read of the Real seal-visible terminal boundary presented by $\mathcal A$, when read as the same Phase $\RealUp$ and Completion audit-threshold coordinate, projects exactly the four carrier and classifier faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and local $\RealUp$, together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Conversely, if two downstream L10 reads of that presented terminal boundary have the same projected $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and local $\RealUp$ face rows and the same displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, then they determine the same Phase $\RealUp$ and Completion audit-threshold read.

The carrier-inversion surface exports no quotient streams, selected limits, host equality, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness rows, aggregate $\mathsf{formalstatus}$ rows, aggregate bridge rows, or hidden completion carriers.
\end{theorem}
\begin{proof}
First read the terminal boundary through the fusion theorem. By \autoref{thm:regseqrat-common-tail-descent-atlas-threshold-fusion}, the hypothesis that $\mathcal A$ presents the audit-threshold read is equivalent to the common-tail terminal four-face sheaf condition, descent-atlas classifier completeness, and atlas finality over the same four local face records. The same theorem identifies the complete support inventory as $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

Now apply support minimality. Theorem~\ref{thm:regseqrat-common-tail-descent-atlas-support-minimality} says that deleting any retained atlas row or displayed support row destroys the presented audit-threshold read. Hence a downstream L10 read of the same presented boundary cannot project through a smaller carrier surface, and any visible carrier row it uses must already be one of the listed rows.

It remains to identify the readback as objectwise, not merely by absence. Use \autoref{thm:regseqrat-common-tail-atlas-finality}: every finite common-tail presentation of the same Real seal-visible boundary and audit-threshold coordinate factors uniquely into $\mathcal A$. Then apply \autoref{thm:regseqrat-audit-threshold-atlas-naturality} to the resulting boundary-preserving comparison. The StreamName readback, Real audit pullback, and DyadicRatCore interface theorems, \autoref{thm:streamname-four-face-audit-clean-threshold-readback}, \autoref{thm:real-l10-current-phase-exit-audit-pullback}, and \autoref{thm:dyadicratcore-l10-exit-namecert-interface}, read that comparison through exactly the four face rows and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

For the converse, take two downstream reads with the same projected face rows and support rows. Atlas finality gives each read the same unique comparison into $\mathcal A$, because there is no retained member, finite intersection, boundary row, or support coordinate on which the comparisons can differ. Audit-threshold atlas naturality then transports both reads to the same Phase $\RealUp$ and Completion coordinate.

The exclusions follow from the same support inventory. The fusion, support-minimality, finality, naturality, StreamName readback, Real pullback, and Dyadic interface results cited above expose no coordinate for quotient streams, selected limits, host equality, countable choice, ambient or metric completeness, aggregate status rows, aggregate bridge rows, or hidden completion carriers, so no downstream readback can recover such a carrier from the terminal atlas boundary.
\end{proof}
