The $\RegSeqRatUp$ readback row is a consumed coordinate of the reciprocal-window route in \autoref{ch:concrete-instances-cauchyinversebudget-namecert}. That consumer may read the regular rational sequence only after the finite $\StreamNameUp$ window and the $\DyadicRatCoreUp$ ledger have been displayed, so the reciprocal budget receives the row as part of the finite route $W \longmapsto D \longmapsto R$ rather than as a host inverse or quotient-real equality surface.

\begin{theorem}[RegSeqRat reciprocal-budget visible route exhaustion]\label{thm:regseqrat-reciprocal-budget-visible-route-exhaustion}
Every reciprocal-facing consumer of a $\RegSeqRatUp$ readback row inside an accepted $\CauchyInverseBudgetUp$ packet factors through the displayed finite route. The route first exposes the apartness budget $A$ and modulus row $M$, then selects the finite $\StreamNameUp$ window $W$, then reads the $\DyadicRatCoreUp$ endpoint, radius, and reciprocal ledger $D$, then consumes the $\RegSeqRatUp$ readback row $R$, and finally reaches the $\RealUp$ seal row $S$ through the displayed $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

Consequently the reciprocal-facing consumer receives only the finite route
$$
A,M,W,D,R,S,H,C,P,N.
$$
It receives no host inverse, quotient stream equality, total reciprocal, trichotomy, selected limit, countable choice datum, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start from the route paragraph above: it places the RegSeqRat readback behind the finite window and dyadic ledger as $W \longmapsto D \longmapsto R$.

Apply \autoref{thm:cauchy-inverse-budget-reciprocal-readback-route}. It expands that local RegSeqRat handoff to the CauchyInverseBudget carrier rows $X,A,M,W,D,R$, with the apartness budget and modulus exposed before the dyadic reciprocal ledger can be consumed.

Apply \autoref{thm:cauchy-inverse-budget-real-seal-nonescape}. The downstream $\RealUp$ seal is then reached only after the same readback row $R$, so the seal consumer cannot bypass the displayed finite route.

Finally apply \autoref{thm:cauchy-inverse-budget-scoped-consumer-route-package}. It adds the visible $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows to the same packet, exhausting the reciprocal-facing consumer surface and excluding host inverse, quotient stream equality, total reciprocal, trichotomy, selected limits, countable choice, and ambient completeness.
\end{proof}
