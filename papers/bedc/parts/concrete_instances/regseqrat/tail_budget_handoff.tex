\begin{theorem}[RegSeqRat tail-budget meet handoff]
\label{thm:regseqrat-tail-budget-meet-handoff}
Let $P$ be an accepted $\RegSeqRatUp$ carrier and let a tail-budget consumer of
$P$ request a completion-facing read. The consumer first meets the displayed
$\StreamNameUp$ pointwise schedule from
\autoref{ch:concrete-instances-streamname-namecert}, then reads the matching
$\DyadicRatCoreUp$ endpoint, radius, and distance row from
\autoref{ch:concrete-instances-dyadicratcore-namecert}, then passes through the
regular-rational classifier rows of $P$, and only then reaches the $\RealUp$
seal row of \autoref{ch:concrete-instances-real-namecert}. Thus every
tail-budget read factors through one regular-Cauchy route before any
completion-facing surface is visible.
\end{theorem}
\begin{proof}
Start with the L10 source lattice pointer of
\autoref{thm:regseqrat-l10-source-lattice-pointer}. It places the accepted
regular-rational packet on the displayed route
$\StreamNameUp,\DyadicRatCoreUp \longrightarrow \RegSeqRatUp \longrightarrow
\RealUp$.

Next apply the common dyadic tail ledger of
\autoref{thm:regseqrat-common-dyadic-tail-ledger}. A tail-budget consumer must
read the cofinal finite ledger consisting of the stream schedule, dyadic
endpoint and radius rows, distance rows, regularity rows, classifier rows,
transport rows, continuation routes, provenance, and local naming.

Finally use the exact handoff of
\autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. The
completion-facing read is therefore downstream of the same finite
regular-Cauchy route and cannot bypass the StreamName schedule, the
DyadicRatCore tolerance row, or the RealUp seal row.
\end{proof}

\begin{theorem}[RegSeqRat StreamName dyadic source lock]
\label{thm:regseqrat-streamname-dyadic-source-lock}
For an accepted $\RegSeqRatUp$ packet, the displayed $\StreamNameUp$ pointwise
name and the displayed $\DyadicRatCoreUp$ tolerance row are locked to the same
finite source. Any two consumers that present the same tail budget, the same
stream schedule row, and the same dyadic endpoint and radius ledger must read
the same regular-rational classifier route before reaching a $\RealUp$ seal.
No separate quotient stream, host real equality, selected limit, ambient
completion datum, or second dyadic source can be inserted between those reads.
\end{theorem}
\begin{proof}
Use \autoref{thm:regseqrat-streamname-dyadic-real-handoff-single-source-lock}.
It identifies public seal reads through displayed StreamName schedules and
DyadicRatCore ledgers as reads of one finite source route.

Then apply \autoref{thm:regseqrat-l10-source-lattice-pointer}. The source route
already contains the schedule rows, endpoint rows, radius rows, distance rows,
classifier rows, regularity rows, $\hsame$ transports, $\Cont$ routes,
$\mathsf{Pkg}$ provenance, and local $\NameCert$ rows.

The same route is the input to the RealUp handoff by
\autoref{thm:regseqrat-real-seal-handoff}. Since the carrier exposes no row for
the excluded quotient, host equality, selected limit, completion datum, or
second dyadic source, the two consumers remain locked to the displayed
StreamName and DyadicRatCore source.
\end{proof}

\begin{theorem}[RegSeqRat Real seal consumer factorization]
\label{thm:regseqrat-real-seal-consumer-factorization}
Every completion-facing $\RealUp$ consumer of an accepted $\RegSeqRatUp$ packet
factors through the three visible rows named by the regular-Cauchy route:
the $\StreamNameUp$ pointwise schedule row, the $\DyadicRatCoreUp$ tolerance
row, and the $\RealUp$ seal row that consumes the regular-rational classifier
surface. The factorization preserves the displayed $\hsame$ comparisons,
$\Cont$ routes, $\mathsf{Pkg}$ provenance, and local $\NameCert$ row, and it
has no route to a hidden completion carrier or an ambient completeness
principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regseqrat-tail-budget-meet-handoff}. It orders every
tail-budget read through StreamName, DyadicRatCore, RegSeqRat, and RealUp
before any completion-facing surface is visible.

Apply \autoref{thm:regseqrat-streamname-dyadic-source-lock}. The StreamName
schedule and DyadicRatCore tolerance row cannot be separated into two sources
once the accepted regular-rational packet is fixed.

Finally use \autoref{thm:regseqrat-real-seal-public-handoff-determinacy}. The
public RealUp consumer is determined by the finite regular-rational source
surface and its displayed transport, continuation, package, and naming rows.
Thus the consumer factorization is exactly the regular-Cauchy route stated
above, with no hidden completion or ambient completeness read.
\end{proof}

\begin{theorem}[RegSeqRat tail-budget Real seal nonescape]
\label{thm:regseqrat-tail-budget-real-seal-nonescape}
A completion-facing $\RealUp$ consumer of an accepted $\RegSeqRatUp$ packet
cannot bypass the $\StreamNameUp$ schedule row, the $\DyadicRatCoreUp$
tolerance row, the $\RegSeqRatUp$ classifier surface, or the displayed
$\RealUp$ seal row at the tail-budget boundary. Every such consumer factors
through the same regular-Cauchy route with its displayed $\hsame$, $\Cont$,
$\mathsf{Pkg}$, and local $\NameCert$ rows.

The boundary has no hidden completion carrier, selected limit, quotient stream,
ambient completeness principle, or second dyadic source.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regseqrat-tail-budget-meet-handoff}. It orders the
completion-facing read through the StreamName schedule, DyadicRatCore tolerance
row, RegSeqRat classifier rows, and RealUp seal row before any completion-facing
surface is visible.

Next apply \autoref{thm:regseqrat-streamname-dyadic-source-lock}. The stream
schedule and dyadic tolerance row are locked to one finite source attached to
the accepted regular-rational packet.

Finally use \autoref{thm:regseqrat-real-seal-consumer-factorization}. It
identifies the completion-facing consumer with the displayed regular-Cauchy
route and its transport, continuation, package, and naming rows. Since that
route has no coordinate for a hidden completion carrier, selected limit,
quotient stream, ambient completeness principle, or second dyadic source, the
consumer cannot escape the stated boundary.
\end{proof}

\begin{theorem}[RegSeqRat tail-budget window-seal pullback]
\label{thm:regseqrat-tail-budget-window-seal-pullback}
Let $P$ be an accepted $\RegSeqRatUp$ packet and let $R$ be any
completion-facing tail-budget consumer of $P$. The visible pullback of $R$
along the public $\RealUp$ seal is exactly the finite packet consisting of the
$\StreamNameUp$ schedule row, the $\DyadicRatCoreUp$ endpoint, radius, and
distance rows, the $\RegSeqRatUp$ classifier and regularity rows, the displayed
$\hsame$ transports, the $\Cont$ routes, the $\mathsf{Pkg}$ provenance row,
and the local $\NameCert$ row.

Consequently every completion-facing tail-budget read reaches $\RealUp$ only
through that same finite $\StreamNameUp$--$\DyadicRatCoreUp$--$\RegSeqRatUp$
route. The pullback exposes no quotient stream, selected host limit, ambient
completeness carrier, second dyadic source, or additional Real seal
coordinate.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:regseqrat-tail-budget-meet-handoff}. It orders the
consumer route through the displayed StreamName schedule row, DyadicRatCore
tolerance ledger, RegSeqRat classifier surface, and RealUp seal before any
completion-facing read is visible.

Next apply \autoref{thm:regseqrat-streamname-dyadic-source-lock}. The schedule
row and dyadic endpoint, radius, and distance rows are locked to one finite
source attached to the accepted packet. Thus the pullback cannot split into a
second dyadic source or a quotient stream route.

Now use \autoref{thm:regseqrat-real-seal-consumer-factorization}. The RealUp
consumer factors through the same finite packet, preserving the displayed
$\hsame$ transports, $\Cont$ routes, $\mathsf{Pkg}$ provenance, and local
$\NameCert$ row.

Finally apply
\autoref{thm:regseqrat-tail-budget-real-seal-nonescape}. The nonescape
boundary removes the remaining excluded coordinates: selected host limits,
ambient completeness carriers, and additional Real seal coordinates. Hence the
visible pullback is precisely the stated finite route.
\end{proof}

\begin{theorem}[RegSeqRat tail-budget terminal Real seal]
\label{thm:regseqrat-tail-budget-terminal-real-seal}
Every completion-facing tail-budget consumer of an accepted $\RegSeqRatUp$
packet reaches $\RealUp$ only through the single finite route consisting of the
$\StreamNameUp$ schedule row, the $\DyadicRatCoreUp$ endpoint, radius, and
distance ledger, the $\RegSeqRatUp$ classifier and regularity rows, the
displayed $\hsame$ transports, the $\Cont$ routes, the $\mathsf{Pkg}$
provenance row, local $\NameCert$ naming, and the public $\RealUp$ seal row.
This terminal surface exports no quotient stream, selected host limit, ambient
completeness carrier, choice data, or second seal coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.RegSeqRatUp.RegSeqRatTailBudgetTerminalRealSeal}
\begin{proof}
First build the tail-budget route. Apply
\autoref{thm:regseqrat-tail-budget-meet-handoff} to force the
completion-facing read through the displayed StreamName schedule,
DyadicRatCore ledger, RegSeqRat classifier surface, and RealUp seal.

Next lock the source rows. By
\autoref{thm:regseqrat-streamname-dyadic-source-lock}, the schedule row and the
endpoint, radius, and distance ledger belong to one finite source attached to
the accepted packet, so a quotient stream or second dyadic source cannot enter.

Now identify the public consumer surface. Use
\autoref{thm:regseqrat-real-seal-consumer-factorization} to preserve the
displayed $\hsame$ transports, $\Cont$ routes, $\mathsf{Pkg}$ provenance, and
local $\NameCert$ row on the way to the public RealUp seal.

Finally combine
\autoref{thm:regseqrat-tail-budget-real-seal-nonescape} with
\autoref{thm:regseqrat-tail-budget-window-seal-pullback}. These rows remove
selected host limits, ambient completeness carriers, choice data, hidden
completion coordinates, and additional Real seal coordinates, leaving exactly
the finite route stated above.
\end{proof}

\begin{theorem}[RegSeqRat tail-budget composition correspondence]
\label{thm:regseqrat-tail-budget-composition-correspondence}
Let $P$ be an accepted $\RegSeqRatUp$ packet. Suppose two completion-facing
tail-budget consumer routes of $P$ are composable and suppose a certified
composite consumer route is displayed for their two-step read. The two-step
read and the certified composite determine the same finite terminal $\RealUp$
seal surface exactly when they preserve the displayed $\StreamNameUp$ schedule
row, the $\DyadicRatCoreUp$ endpoint, radius, and distance ledger, the
$\RegSeqRatUp$ classifier and regularity rows, the terminal $\RealUp$ seal row,
and the support rows
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert .
$$

No quotient stream, selected host limit, countable choice datum, ambient
completeness carrier, metric-completeness row, or second seal coordinate is part
of this correspondence.
\end{theorem}
\begin{proof}
First put the component consumers on the common route. Apply
\autoref{thm:regseqrat-tail-budget-meet-handoff} to both component routes, so
each read is displayed on the four-face path through $\StreamNameUp$,
$\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.

Next compare the two finite-window presentations. The shared-window reindex
composition law of
\autoref{thm:regseqrat-shared-window-reindex-composition-law} identifies the
two-step presentation with the certified composite exactly on the displayed
StreamName schedule and DyadicRatCore ledger rows.

Now pull the Real seal read back along that finite window. Use
\autoref{thm:regseqrat-tail-budget-window-seal-pullback} to preserve the
RegSeqRat classifier and regularity rows together with the displayed $\BHist$,
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Finally apply \autoref{thm:regseqrat-tail-budget-terminal-real-seal}. The
terminal seal surface is therefore the same for the two-step route and the
certified composite. The nonescape theorem
\autoref{thm:regseqrat-tail-budget-real-seal-nonescape} removes quotient
streams, selected host limits, countable choice data, ambient and metric
completeness rows, and second seal coordinates from the comparison.
\end{proof}

\begin{theorem}[RegSeqRat dyadic-stream tail-budget common refinement]
\label{thm:regseqrat-dyadic-stream-tail-budget-common-refinement}
Let $P$ and $Q$ be accepted $\RegSeqRatUp$ tail-budget presentations whose
$\StreamNameUp$ schedules and $\DyadicRatCoreUp$ endpoint, radius, and distance
ledgers admit a displayed common finite window. The two presentations determine
the same terminal $\RealUp$ seal surface exactly when that common window
preserves the $\StreamNameUp$ pointwise classifier, the $\DyadicRatCoreUp$
radius handoff, the $\RegSeqRatUp$ classifier rows, and the $\RealUp$ source
factorization rows.

The common-refinement certificate is only this finite BEDC route through
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. It contains no quotient
stream, selected host limit, countable choice datum, ambient completeness
carrier, or second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First view the two accepted presentations as composable tail-budget consumer
routes through the displayed four-face path. Apply
\autoref{thm:regseqrat-tail-budget-composition-correspondence}; it reduces
agreement of the terminal $\RealUp$ seal surface to preservation of the
StreamName schedule row, DyadicRatCore ledger rows, RegSeqRat classifier and
regularity rows, the terminal RealUp seal row, and the displayed support rows.

Next identify the common finite window. The shared-window reindex composition
law of \autoref{thm:regseqrat-shared-window-reindex-composition-law} supplies
the finite comparison of the two StreamName schedules and DyadicRatCore ledgers.
The pullback theorem
\autoref{thm:regseqrat-tail-budget-window-seal-pullback} transports the
RegSeqRat classifier rows and the Real-facing support rows along that window.

Finally apply \autoref{thm:regseqrat-tail-budget-terminal-real-seal}. The
terminal seal row is therefore determined by exactly the four preserved
surfaces named in the statement. The later Lean route is represented by
\texttt{BEDC.Derived.RegSeqRatUp.RegSeqRatStreamCarrier\_common\_refinement\_classifier\_determinacy},
\texttt{BEDC.Derived.DyadicRatCoreUp.DyadicRatCoreRegSeqRatWindow\_radius\_handoff},
\texttt{BEDC.Derived.StreamNameUp.StreamNameRegSeqRatPointwiseHandoff}, and
\texttt{BEDC.Derived.RealUp.RealNameSeal\_source\_factorization}; no Lean marker
is asserted here.
\end{proof}

\begin{theorem}[RegSeqRat finite tail-budget family common refinement]
\label{thm:regseqrat-finite-tail-budget-family-common-refinement}
Let $F$ be a finite family of accepted $\RegSeqRatUp$ tail-budget
presentations. Suppose the $\StreamNameUp$ schedules and the
$\DyadicRatCoreUp$ endpoint, radius, and distance ledgers for every pair of
members of $F$ admit displayed common finite windows. Then $F$ determines one
terminal $\RealUp$ seal surface exactly when the common finite refinement
preserves, for every member of $F$, the $\StreamNameUp$ pointwise classifier,
the $\DyadicRatCoreUp$ radius handoff, the $\RegSeqRatUp$ classifier and
regularity rows, the $\RealUp$ source factorization rows, and the displayed
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

The finite-family certificate is only the common BEDC refinement route. It
contains no quotient stream, selected host limit, countable choice datum,
ambient completeness carrier, metric-completeness row, or second $\RealUp$
seal coordinate.
\end{theorem}
\begin{proof}
Induct on the finite family $F$. For the empty and singleton families, the
claimed terminal seal surface is the displayed accepted presentation itself,
and the listed support rows are exactly the rows already carried by its
tail-budget certificate.

For the induction step, separate the last accepted presentation from the
refined family already produced by the induction hypothesis. The pairwise
window assumptions give common finite windows between the last presentation
and each member of the refined family, while the induction hypothesis supplies
the already preserved StreamName classifiers, DyadicRatCore radius handoffs,
RegSeqRat classifier and regularity rows, RealUp source factorization rows, and
displayed support.

Apply \autoref{thm:regseqrat-tail-budget-composition-correspondence} to the
refined family route and the last presentation. This is the binary step: it
keeps the terminal $\RealUp$ seal read on the same four-face path and records
that no extra route data is introduced by composing the two tail-budget
certificates.

Next use
\autoref{thm:dyadicratcore-four-face-scale-window-chain-induction}. It
identifies the nested $\DyadicRatCoreUp$ endpoint, radius, and distance
handoffs across the finite chain of common windows, so the composite window is
coherent with the earlier dyadic radius handoff rows.

Finally apply
\autoref{thm:regseqrat-dyadic-stream-tail-budget-common-refinement} to compare
the terminal seal surface of the composite refinement with the seal surface of
each accepted family member. The forward direction reads preservation of the
listed rows from the common finite refinement; the reverse direction repacks
those preserved rows into the same terminal $\RealUp$ seal surface.

The nonescape clauses remain facewise throughout the induction. Since every
step uses only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ support, the finite-family route has no quotient streams, selected
host limits, countable choice data, ambient or metric-completeness rows, or
second $\RealUp$ seal coordinates.
\end{proof}

\begin{theorem}[RegSeqRat tail-budget facewise status pullback]
\label{thm:regseqrat-tail-budget-facewise-status-pullback}
Every accepted tail-budget $\RealUp$-seal consumer of an accepted
$\RegSeqRatUp$ packet reads the four faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
with each face's own closurestatus, formalstatus, leantarget, and bridgestatus
rows. The consumer route reads those rows from the local face records and not
from an aggregate formalstatus token.

Conversely, a tail-budget packet carrying exactly those four local status
records together with the displayed
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert
$$
support rows gives the same accepted tail-budget terminal $\RealUp$-seal route.
This pullback has no fifth face, quotient stream equality, selected host
limits, choice data, ambient completeness carrier, metric-completeness datum,
standard-bridge row, or aggregate audit certificate.
\end{theorem}
\begin{proof}
First locate the finite route. Apply
\autoref{thm:regseqrat-tail-budget-terminal-real-seal}. It confines every
completion-facing tail-budget consumer to the StreamName schedule,
DyadicRatCore ledger, RegSeqRat classifier and regularity rows, displayed
$\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, local $\NameCert$
naming, and public $\RealUp$ seal.

Next read the status data face by face. Apply
\autoref{thm:real-terminal-seal-four-face-status-pullback}. It identifies the
terminal seal status read with the four local closurestatus, formalstatus,
leantarget, and bridgestatus rows and rejects a route-level formalstatus token
or aggregate bridge row.

For the phase boundary, apply
\autoref{thm:real-l10-current-phase-exit-audit-pullback}. The current Phase
Real and Completion exit read is the product of the four local face records, so
the tail-budget route cannot contribute a fifth face or cached completion
status.

Finally reuse the nonescape clauses of
\autoref{thm:regseqrat-tail-budget-terminal-real-seal}. They exclude quotient
stream equality, selected host limits, choice data, ambient completeness,
metric-completeness data, standard-bridge rows, and any additional Real seal
coordinate. The converse is then the same finite packet read with exactly the
four local status records and the displayed support rows.
\end{proof}

\begin{theorem}[RegSeqRat tail-budget support refinement invariance]
\label{thm:regseqrat-tail-budget-support-refinement-invariance}
Let an accepted $\RegSeqRatUp$ tail-budget route terminate in a
completion-facing $\RealUp$ seal row. Suppose this route is replaced by a
finite certified support refinement whose finite windows preserve the
$\StreamNameUp$ schedule classifier, the $\DyadicRatCoreUp$ endpoint, radius,
and distance ledger, the $\RegSeqRatUp$ classifier and regularity rows, the
terminal $\RealUp$ seal row, and the displayed
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert
$$
support rows.

Then the original route and the refined route determine the same terminal
completion-facing $\RealUp$ seal surface. The common seal is read through the
same facewise closurestatus, formalstatus, leantarget, and bridgestatus rows,
and not through quotient streams, selected host limits, countable choice,
ambient completeness, metric completeness, a second $\RealUp$ seal coordinate,
or an aggregate formalstatus token.
\end{theorem}
\begin{proof}
First compare a single replacement step. Apply
\autoref{thm:regseqrat-dyadic-stream-tail-budget-common-refinement}. It supplies
the common finite window in which the $\StreamNameUp$ classifier, the
$\DyadicRatCoreUp$ endpoint, radius, and distance rows, and the
$\RegSeqRatUp$ classifier and regularity rows are preserved on both sides of
the handoff.

Next pass from one replacement step to the finite certified support
refinement. Apply
\autoref{thm:regseqrat-finite-tail-budget-family-common-refinement}. The finite
family certificate keeps the same displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ support rows while threading the preserved dyadic and
regular-sequence rows through the whole family.

Now read the terminal surface. Apply
\autoref{thm:regseqrat-tail-budget-terminal-real-seal}. Since both routes have
the same finite tail-budget support and the same terminal $\RealUp$ seal row,
the completion-facing seal surface determined by the original route is the seal
surface determined by the refined route.

Finally apply \autoref{thm:regseqrat-tail-budget-facewise-status-pullback}. The
status data used in the conclusion is read objectwise from the four faces and
from the displayed support rows. Hence the refinement cannot introduce quotient
streams, selected host limits, countable choice, ambient or metric completeness,
a second seal coordinate, or any aggregate formalstatus token.
\end{proof}

\begin{theorem}[RegSeqRat tail-budget chain refinement exhaustion]
\label{thm:regseqrat-tail-budget-chain-refinement-exhaustion}
Let an accepted $\RegSeqRatUp$ tail-budget route terminate in a
completion-facing $\RealUp$ seal row, and let
$R_0,\ldots,R_n$ be a finite nonempty chain of accepted $\RegSeqRatUp$
tail-budget routes with $R_0$ the original route and $R_n$ the terminal route.
Suppose every adjacent replacement $(R_i,R_{i+1})$ is a certified support
refinement preserving the $\StreamNameUp$ schedule classifier, the
$\DyadicRatCoreUp$ endpoint, radius, and distance ledger, the $\RegSeqRatUp$
classifier and regularity rows, the terminal $\RealUp$ seal row, and the
displayed
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert
$$
support rows.

Then every parenthesized finite refinement chain from $R_0$ to $R_n$ and the
direct first-to-last comparison determine the same terminal completion-facing
$\RealUp$ seal surface. The common surface is exactly the terminal seal read
from the preserved tail-budget support rows, with the facewise closurestatus,
formalstatus, leantarget, and bridgestatus rows pulled back from the displayed
faces. It exports no quotient streams, selected host limits, countable choice,
ambient completeness, metric completeness, duplicate $\RealUp$ seal
coordinates, aggregate formalstatus tokens, or aggregate bridge rows.
\end{theorem}
\begin{proof}
The base case is the one-route chain. The comparison from $R_0$ to $R_0$ is
reflexive on the displayed tail-budget support, so the completion-facing
$\RealUp$ seal surface is the terminal surface already carried by that route.

For a one-step chain, apply
\autoref{thm:regseqrat-tail-budget-support-refinement-invariance}. The
certified support refinement preserves the StreamName schedule, DyadicRatCore
ledger, RegSeqRat classifier and regularity rows, displayed support rows, and
terminal $\RealUp$ seal row, hence the two routes read the same terminal seal
surface.

For a longer finite chain, first forget the parenthesization and read the chain
as a finite request support-face chain. Apply
\autoref{thm:regseqrat-finite-request-support-face-chain-induction}. It makes
all adjacent support-face composites and the direct first-to-last comparison
land on one terminal $\RealUp$ seal-facing support face over the displayed
rows.

Next build the common finite support needed by the tail-budget route. Apply
\autoref{thm:regseqrat-finite-tail-budget-family-common-refinement}. The common
refinement keeps the finite StreamName windows, the DyadicRatCore endpoint,
radius, and distance ledger, the RegSeqRat classifier and regularity rows, and
the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows on one
finite support family.

Now read the terminal completion-facing surface. Apply
\autoref{thm:regseqrat-tail-budget-terminal-real-seal}. Since the direct
comparison and every parenthesized chain have the same finite tail-budget
support and the same terminal $\RealUp$ seal row, they determine the same
completion-facing $\RealUp$ seal surface.

Finally apply \autoref{thm:regseqrat-tail-budget-facewise-status-pullback}. The
status and bridge data are read objectwise from the preserved faces and not
from an aggregate token. Therefore the finite chain cannot introduce quotient
streams, selected host limits, countable choice, ambient or metric
completeness, duplicate $\RealUp$ seal coordinates, aggregate formalstatus
tokens, or aggregate bridge rows.
\end{proof}
