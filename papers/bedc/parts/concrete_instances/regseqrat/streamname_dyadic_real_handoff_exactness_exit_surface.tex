
\begin{theorem}[RegSeqRat StreamName Dyadic Real Lean anchor audit surface]
\label{thm:regseqrat-streamname-dyadic-real-lean-anchor-audit-surface}
For an accepted $\RegSeqRatUp$ carrier whose public seal route is saturated by
the four faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and
$\RealUp$, the Lean-facing anchors
\texttt{RatStreamNameFiniteWindowClassifier\_singleton\_diagonal\_handoff},
\texttt{DyadicRatCoreCarrier\_regseqrat\_bridge\_handoff\_exactness}, and
\texttt{RegSeqRatStreamCarrier\_real\_seal\_handoff} expose exactly the same
finite audit surface. Concretely, they expose the StreamName finite-window
membership surface, the DyadicRatCore endpoint, radius, and distance handoff
surface, and the RegSeqRat-to-Real public seal handoff surface. No additional
audit coordinate is supplied by a quotient stream, host real equality, selected
limit, ambient completeness witness, choice datum, or hidden seal coordinate.
\end{theorem}
\begin{proof}
Apply
\autoref{thm:regseqrat-streamname-dyadic-real-source-seal-correspondence}. It
identifies the accepted carrier's finite source route with the public seal
route and already names the three Lean-facing anchors as the route from
finite-window rows to dyadic rows, from dyadic rows to the RegSeqRat
classifier, and from that classifier to the public Real seal.

Next use \autoref{thm:regseqrat-l10-four-face-necessity}. It shows that a
Phase Real and Completion exit read requires exactly the StreamName schedule,
DyadicRatCore ledger, RegSeqRat classifier, and Real seal faces for the L10
route. Thus the first two anchors cover the first-three-face passage through
StreamName, DyadicRatCore, and RegSeqRat, while the third anchor covers the
terminal seal passage into $\RealUp$.

Finally apply
\autoref{thm:regseqrat-real-seal-finite-window-saturation}. The saturated
route has no fifth face, and the excluded quotient stream, host real equality,
selected limit, ambient completeness, choice datum, and hidden seal coordinate
have no displayed row. Therefore the three named anchors are exactly the
finite audit surface that remains to be formalized for the four-face route.
\end{proof}

\begin{theorem}[RegSeqRat L10 exit route order lock]
\label{thm:regseqrat-l10-exit-route-order-lock}
For any accepted public $\RegSeqRatUp$ read whose terminal output is a
$\RealUp$ seal, the L10 exit route is ordered as follows: first the
$\StreamNameUp$ finite-window membership face, second the $\DyadicRatCoreUp$
endpoint-radius-distance ledger face, third the $\RegSeqRatUp$ classifier,
regularity, and transport face, and last the public $\RealUp$ seal face.

No later face may be read before its predecessor. In particular the public
seal row is not available before the RegSeqRat face has been read, the
RegSeqRat face is not available before the DyadicRatCore ledger has been read,
and the dyadic ledger is not available before the StreamName membership face
has fixed the finite window. The route excludes quotient streams, host real
equality, selected limits, ambient completeness, choice rows,
metric-completeness data, aggregate formalstatus rows, aggregate bridge rows,
and a second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
Begin with
\autoref{thm:regseqrat-streamname-dyadic-real-lean-anchor-audit-surface}. It
identifies the three Lean-facing anchors as the finite-window handoff, the
dyadic endpoint-radius-distance handoff, and the RegSeqRat-to-Real seal
handoff, with no extra audit coordinate.

Next apply \autoref{thm:regseqrat-l10-four-face-necessity}. Necessity says
that the L10 public exit needs exactly the StreamName, DyadicRatCore,
RegSeqRat, and Real faces. Thus removing or bypassing any predecessor would
leave a later face without the finite source row on which it is certified.

Use \autoref{thm:regseqrat-streamname-dyadic-real-triple-exactness} to bind
the first three faces as one ordered finite route. The triple exactness step
keeps the StreamName membership rows upstream of the DyadicRatCore ledger and
keeps the dyadic ledger upstream of the RegSeqRat classifier, regularity, and
transport rows.

Finally combine
\autoref{thm:regseqrat-streamname-dyadic-real-source-seal-correspondence} with
\autoref{thm:regseqrat-real-seal-finite-window-saturation}. The correspondence
places the terminal public seal on the same finite source route, while
saturation says that the route has no fifth face and no hidden second seal
coordinate. Hence the four public faces are read in the displayed order, and
the excluded quotient, host-equality, limit, completeness, choice,
formalstatus, bridge, and second-seal data remain outside the route.
\end{proof}

\begin{theorem}[RegSeqRat Real seal finite-cover gluing]
\label{thm:regseqrat-real-seal-finite-cover-gluing}
Let $\{\mathcal{S}_i\}_{i\in I}$ be a finite family of accepted public
$\RegSeqRatUp$ reads whose members exit as $\RealUp$ seals. Suppose each
$\mathcal{S}_i$ is saturated by exactly the four displayed faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp.
$$
Suppose also that every displayed pairwise overlap agrees on the shared
$\mathsf{BHist}$, componentwise $\hsame$, $\Cont$, $\Pkg$, $\NameCert$,
dyadic-ledger, classifier, regularity, and seal rows. Then the finite family
glues to one finite four-face L10 packet over the finite union of the
requests, with the same four faces and only the displayed
$\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.
The glued packet contains no quotient stream equality, no host real equality,
no selected limit, no ambient or metric-completeness datum, no choice data,
and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
For each family member, apply
\autoref{thm:regseqrat-real-seal-finite-window-saturation}. This places the
member on one finite L10 exit route whose only faces are the StreamName
schedule, DyadicRatCore ledger, RegSeqRat classifier and regularity surface,
and Real seal row.

Glue the StreamName windows first. The overlap hypotheses identify the
displayed $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, dyadic,
RegSeqRat, and Real rows seen from adjacent members, so
\autoref{thm:streamname-finite-request-cover-gluing} gives one finite
StreamName-side request packet over the finite union.

Next keep the Real consumer on that same finite section. Use
\autoref{thm:real-regseqrat-finite-request-fusion} to repack each seal request
through the displayed finite $\mathsf{BHist}$ packet, and use
\autoref{thm:real-regseqrat-completion-consumer-section-exactness} to force
every completion-facing or $\RealUp$-facing consumer to factor through that
section.

It remains to identify the dyadic and RegSeqRat faces on overlaps. Apply
\autoref{thm:regseqrat-common-dyadic-tail-ledger} to keep common cofinal
dyadic refinements on the same endpoint, radius, distance, classifier,
regularity, transport, continuation, provenance, and local naming ledger.
Then apply
\autoref{thm:regseqrat-real-seal-window-triple-correspondence} to recover each
of the four faces from the other three inside the same finite route.

Finish by finite induction over the request family. The empty cover gives the
empty displayed packet, and adjoining one saturated member uses the pairwise
overlap agreement above to attach it to the packet already built. At each
step the cited saturation, StreamName gluing, Real fusion, and consumer
section theorems export only the four displayed faces and their support rows,
so quotient stream equality, host real equality, selected limits, ambient or
metric completeness, choice data, and a second seal coordinate remain absent.
\end{proof}

\begin{theorem}[RegSeqRat Real seal finite-cover associativity]
\label{thm:regseqrat-real-seal-finite-cover-associativity}
Let $\mathcal{A}$, $\mathcal{B}$, and $\mathcal{C}$ be finite families of
accepted public $\RegSeqRatUp$ reads whose members exit as $\RealUp$ seals.
Assume that every member is saturated by the same four displayed faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
and that every pairwise overlap among the three families satisfies the
four-face agreement used in
\autoref{thm:regseqrat-real-seal-finite-cover-gluing}. Then gluing
$\mathcal{A}\cup\mathcal{B}$ first and then gluing with $\mathcal{C}$ displays
the same finite four-face L10 packet as gluing $\mathcal{B}\cup\mathcal{C}$
first and then gluing with $\mathcal{A}$.

The common packet has exactly the StreamName schedule and membership face, the
DyadicRatCore endpoint, radius, and distance face, the RegSeqRat classifier,
regularity, and transport face, and the RealUp seal face, with the same
$\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. It
contains no quotient stream equality, no host real equality, no selected limit,
no ambient or metric-completeness datum, no choice data, and no second
$\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
Apply
\autoref{thm:regseqrat-real-seal-finite-cover-gluing} to
$\mathcal{A}\cup\mathcal{B}$ and then to
$(\mathcal{A}\cup\mathcal{B})\cup\mathcal{C}$. Apply the same theorem to
$\mathcal{B}\cup\mathcal{C}$ and then to
$\mathcal{A}\cup(\mathcal{B}\cup\mathcal{C})$. The overlap hypotheses are
exactly the hypotheses of the binary gluing theorem for each of these steps.

For every member used in either parenthesization, apply
\autoref{thm:regseqrat-real-seal-finite-window-saturation}. This identifies
the member with the same StreamName schedule and membership row, DyadicRatCore
endpoint, radius, and distance row, RegSeqRat classifier, regularity, and
transport row, and Real seal row.

The only remaining issue is overlap agreement after the intermediate gluing
step. Use
\autoref{thm:regseqrat-real-seal-window-triple-correspondence} to read any
three of the four faces as the same fourth face inside the finite route, and
use \autoref{thm:regseqrat-common-dyadic-tail-ledger} to keep the dyadic and
RegSeqRat ledger fixed on common cofinal tails. Thus both intermediate packets
present the same finite $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$,
$\NameCert$, endpoint, radius, distance, classifier, regularity, transport,
and seal rows on every overlap.

Conclude by finite induction over the members of $\mathcal{C}$ after the first
parenthesization, and symmetrically over the members of $\mathcal{A}$ after
the second parenthesization. The base case is the already glued binary packet.
The adjoining step attaches one saturated member along the overlap rows just
identified, so it cannot introduce a new face or a different support row. Hence
both parenthesizations expose the same finite BEDC rows and no extra route.
\end{proof}

\begin{theorem}[RegSeqRat Real seal common-refinement cover completeness]
\label{thm:regseqrat-real-seal-common-refinement-cover-completeness}
Let $\{\mathcal{S}_i\}_{i\in I}$ be a finite family of accepted public
$\RegSeqRatUp$ reads whose members exit as $\RealUp$ seals. Suppose each
$\mathcal{S}_i$ is saturated by exactly the four displayed faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp.
$$
Suppose further that every finite subfamily $J\subseteq I$ has a certified
$\StreamNameUp$ common-refinement packet $\mathcal{R}_J$ whose restrictions to
the members of $J$ agree on the dyadic endpoint, radius, distance, and tail
ledger rows, on the $\RegSeqRatUp$ classifier and regularity rows, on the
$\RealUp$ public seal row, and on the displayed $\mathsf{BHist}$,
componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support.

Then the glued family is covered by one finite four-face pullback surface over
the union of the accepted reads. This surface restricts to each
$\mathcal{R}_J$ on every finite subfamily and displays only the
$\StreamNameUp$ schedule and membership face, the $\DyadicRatCoreUp$ ledger
face, the $\RegSeqRatUp$ classifier, regularity, and transport face, the
$\RealUp$ seal face, and the listed $\mathsf{BHist}$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ support rows. It exports no quotient stream, no host
equality, no selected limit, no ambient or metric-completeness datum, no choice
data, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
Begin with the common-refinement packets. By
\autoref{thm:streamname-common-refinement-public-exactness}, each
$\mathcal{R}_J$ exposes only the public StreamName source tuple and forces all
downstream $\RegSeqRatUp$ and finite-window $\RealUp$ reads to factor through
that tuple.

Next apply
\autoref{thm:streamname-real-regseqrat-seal-common-refinement}. For every
finite subfamily, the StreamName window, RegSeqRat readback,
$\DyadicRatCoreUp$ ledger, and $\RealUp$ seal row form one finite pullback
with exactly the displayed $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and
local $\NameCert$ support.

Now pass from subfamily pullbacks to the whole finite family. Apply
\autoref{thm:regseqrat-real-seal-finite-cover-gluing} to glue the accepted
reads along their shared dyadic ledger, classifier, regularity, seal, and
support rows. The hypotheses give the overlap agreement required by that
gluing theorem because every overlap is the restriction of some
$\mathcal{R}_J$.

Finally use
\autoref{thm:regseqrat-real-seal-finite-cover-associativity}. Reordering or
reparenthesizing the finite subfamilies does not change the resulting
four-face packet, so the cover obtained from the common-refinement packets is
complete for the finite family. The cited results list all exported rows, and
none supplies quotient streams, host equality, selected limits, ambient or
metric completeness, choice data, or a second Real seal coordinate.
\end{proof}

\begin{theorem}[RegSeqRat StreamName Dyadic Real audit-clean exit readiness]
\label{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness}
For any accepted public L10 exit read of a $\RegSeqRatUp$ carrier, audit-clean
readiness for the Phase Real and Completion quartet is exactly the finite
four-face route through the $\StreamNameUp$ finite schedules, the
$\DyadicRatCoreUp$ dyadic endpoint, radius, and distance ledgers, the
$\RegSeqRatUp$ classifier, regularity, and transport rows, and the public
$\RealUp$ seal row.

The readiness surface contains only the displayed $\mathsf{BHist}$,
componentwise $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ support
rows for those four faces. It contains no Real completeness theorem, no quotient
representative, no host sequence equality, no trichotomy principle, no
open-cover compactness object, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
Start with
\autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. It routes the
accepted public seal read through the StreamName schedule face, the DyadicRatCore
ledger face, the RegSeqRat classifier and regularity face, and the public Real
seal face while preserving the displayed $\hsame$, $\Cont$, $\mathsf{Pkg}$, and
$\NameCert$ rows.

Next apply
\autoref{thm:regseqrat-streamname-dyadic-real-handoff-single-source-lock} and
\autoref{thm:regseqrat-streamname-dyadic-real-source-seal-correspondence}. The
single-source lock prevents a second completion source, and the correspondence
identifies the finite source read with the public seal read in both directions.
Thus the exit criterion can read only the same finite rows from either side.

Use \autoref{thm:regseqrat-l10-four-face-necessity} together with
\autoref{thm:regseqrat-real-seal-finite-window-saturation}. These results say
that all four faces are required and that no fifth face is available to an
accepted Phase Real and Completion exit read.

Finally apply \autoref{thm:regseqrat-real-seal-finite-cover-gluing} and
\autoref{thm:regseqrat-real-seal-finite-cover-associativity}. Finite families of
accepted reads glue and reassociate without changing the StreamName,
DyadicRatCore, RegSeqRat, or Real seal rows, so audit-clean Lean work should
target this finite route. Stronger off-table claims about Real completeness,
quotient representatives, host sequence equality, trichotomy, open-cover
compactness, or a second Real seal coordinate are not part of the exit-readiness
surface.
\end{proof}

\begin{theorem}[RegSeqRat Real seal terminal-section exhaustion]
\label{thm:regseqrat-real-seal-terminal-section-exhaustion}
Every terminal public $\RealUp$ seal section of an accepted
$\RegSeqRatUp$ carrier is exhausted by the same finite L10 route through the
four displayed faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp.
$$
The exhausted section consists exactly of the displayed $\mathsf{BHist}$,
componentwise $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ rows,
together with the StreamName finite-window schedules, the DyadicRatCore
endpoint, radius, and distance ledgers, the RegSeqRat classifier, regularity,
and transport rows, and the public Real seal row.

No terminal section produced by this route contains a Real completeness
theorem, quotient representative, host equality, trichotomy principle,
open-cover compactness datum, choice datum, or second seal coordinate.
\end{theorem}
\begin{proof}
Begin with
\autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. It presents
the accepted carrier's public seal read as a finite passage from StreamName
finite-window schedules through DyadicRatCore ledgers and RegSeqRat classifier
data to the public Real seal row.

Next use
\autoref{thm:regseqrat-streamname-dyadic-real-source-seal-correspondence}. The
source side and the terminal seal side expose the same displayed section, so a
terminal $\RealUp$ seal section cannot carry rows absent from the finite source
route.

Apply \autoref{thm:regseqrat-l10-four-face-necessity} and
\autoref{thm:regseqrat-real-seal-finite-window-saturation}. Necessity gives the
four required faces, and saturation says the accepted exit read has no fifth
face beyond $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and
$\RealUp$.

For finite terminal sections, apply
\autoref{thm:regseqrat-real-seal-finite-cover-gluing} and
\autoref{thm:regseqrat-real-seal-finite-cover-associativity}. Gluing turns
overlapping accepted seal reads into one finite packet over the displayed
support rows, and associativity shows that the packet is independent of the
order in which those finite pieces are attached.

Finally apply
\autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness}. Its
negative boundary is exactly the asserted terminal boundary: no Real
completeness theorem, quotient representative, host equality, trichotomy
principle, open-cover compactness datum, choice datum, or second seal
coordinate is part of the terminal public section. Hence every terminal section
is exhausted by the finite four-face route and by no additional BEDC row.
\end{proof}

\begin{theorem}[RegSeqRat terminal descent uniqueness]
\label{thm:regseqrat-terminal-descent-uniqueness}
Let $S$ and $T$ be terminal public $\RealUp$ seal sections of accepted
$\RegSeqRatUp$ carriers. Suppose $S$ and $T$ display the same four faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
the same displayed $\mathsf{BHist}$ rows, and the same componentwise
$\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ support rows. Then
$S$ and $T$ are the same terminal finite L10 section.

Equivalently, descent along the RegSeqRat terminal route is unique at the
public $\RealUp$ seal: the displayed StreamName schedules, DyadicRatCore
ledgers, RegSeqRat classifier and regularity rows, and public Real seal row
determine one terminal section and admit no quotient stream, host sequence
equality, selected limit, ambient completeness witness, choice datum, or
second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:regseqrat-real-seal-terminal-section-exhaustion} to both
$S$ and $T$. Each terminal public $\RealUp$ seal section is exhausted by the
same finite L10 route through $\StreamNameUp$, $\DyadicRatCoreUp$,
$\RegSeqRatUp$, and $\RealUp$, with no extra BEDC row beyond the displayed
support.

Next apply
\autoref{thm:regseqrat-real-seal-finite-cover-associativity}. The finite
pieces of the two sections may be regrouped without changing any displayed
face or support row, so the comparison is independent of the order in which
StreamName, DyadicRatCore, RegSeqRat, and Real seal pieces are attached.

Use
\autoref{thm:regseqrat-real-seal-common-refinement-cover-completeness}. The
two exhausted finite covers have a common accepted refinement, and the
hypothesis identifies every displayed $\mathsf{BHist}$, $\hsame$, $\Cont$,
$\mathsf{Pkg}$, and local $\NameCert$ row on that refinement. Thus both
terminal reads descend to the same finite packet.

Finally apply
\autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness}.
The audit-clean boundary refuses quotient streams, host sequence equality,
selected limits, ambient completeness, choice data, and any second $\RealUp$
seal coordinate. Since no off-route datum remains available to separate
$S$ from $T$, the common refined packet is the unique terminal finite L10
section determined by the four displayed faces.
\end{proof}

\begin{theorem}[RegSeqRat Real seal terminal-section observation lock]
\label{thm:regseqrat-real-seal-terminal-section-observation-lock}
For every terminal public $\RealUp$ seal section of an accepted
$\RegSeqRatUp$ carrier, the exhausted four-face section determines exactly the
same finite $\StreamNameUp$ observation interface named by
\autoref{thm:streamname-realup-finite-window-seal-boundary}.

Every readback of that terminal section preserves the $\StreamNameUp$
finite-window carrier and classifier rows, the $\ProbeBundle$ and $\InBundle$
membership rows, UnaryHistory observations, the $\DyadicRatCoreUp$ ledger, the
$\RegSeqRatUp$ classifier and regularity rows, and the public $\RealUp$ seal
row. It cannot add a quotient stream, host equality, selected limit, ambient
completeness witness, trichotomy principle, open-cover compactness datum,
choice datum, or second seal coordinate.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:regseqrat-real-seal-terminal-section-exhaustion}. It identifies a
terminal public $\RealUp$ seal section with the finite route through
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, and it
excludes every row outside that route.

Next combine this exhaustion with
\autoref{thm:regseqrat-real-seal-finite-window-saturation}. Saturation locks
the four displayed faces as the whole accepted section, so a readback cannot
replace the StreamName face or attach another completion-facing coordinate.

Use \autoref{thm:streamname-realup-finite-window-seal-boundary} to identify the
first face. That theorem supplies exactly the finite-window carrier,
classifier, bundle-membership, and UnaryHistory observation interface consumed
by the Real seal boundary.

Finally apply
\autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness} and
\autoref{thm:real-regseqrat-streamname-lattice-route}. They keep the
Real-facing read on the same L10 lattice route through the DyadicRatCore ledger,
the RegSeqRat classifier and regularity rows, and the public Real seal row.
Thus terminal readback preserves the finite StreamName observation interface
and introduces none of the off-route data listed in the statement.
\end{proof}

\begin{theorem}[RegSeqRat StreamName open-phase Real exit correspondence]
\label{thm:streamname-open-phase-real-regseqrat-exit-correspondence}
For an accepted $\RegSeqRatUp$ carrier, a terminal public $\RealUp$ seal
section is an open-phase Phase Real and Completion exit boundary exactly when
it exposes the same four finite faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
together with the displayed $\mathsf{BHist}$, $\hsame$, $\Cont$,
$\mathsf{Pkg}$, and local $\NameCert$ rows.

On this correspondence the StreamName face is the finite observation boundary
locked by \autoref{thm:regseqrat-real-seal-terminal-section-observation-lock},
and the remaining faces are the DyadicRatCore ledger, the RegSeqRat classifier
and regularity rows, and the public Real seal row named by
\autoref{thm:regseqrat-real-seal-terminal-section-exhaustion}. The
correspondence contains no quotient stream, no host sequence equality, no
selected limit, no ambient $\RealUp$ completeness row, no metric-completeness
datum, no countable-choice row, and no second seal coordinate.
\end{theorem}
\begin{proof}
For the forward direction, start with
\autoref{thm:regseqrat-real-seal-terminal-section-exhaustion}. It exhausts a
terminal public $\RealUp$ seal section by the four finite faces
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, together
with the displayed history, sameness, continuity, package, and local
certificate rows. Then apply
\autoref{thm:regseqrat-real-seal-terminal-section-observation-lock} to fix the
StreamName face as the finite observation interface, and use
\autoref{thm:streamname-open-phase-four-object-sufficiency} to read that same
packet as sufficient for the current Phase Real and Completion boundary.

For the reverse direction, use
\autoref{thm:streamname-open-phase-four-face-exit-readback}. Any open-phase
Phase Real and Completion exit boundary with the four displayed faces reads
back through exactly those support rows. Combine this with
\autoref{thm:regseqrat-real-seal-finite-window-saturation} to identify the
readback with a saturated terminal public Real seal section, and then apply
\autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness} to
preserve the negative boundary. Hence the two descriptions name the same
finite section and exclude quotient streams, host sequence equality, selected
limits, ambient completeness, metric-completeness data, countable choice, and
second seal coordinates.
\end{proof}

\begin{theorem}[StreamName RegSeqRat Real observation interface]
\label{thm:streamname-regseqrat-real-observation-interface}
For any accepted public L10 exit read of a $\RegSeqRatUp$ carrier, the
$\StreamNameUp$--$\RealUp$ finite-window seal boundary of
\autoref{thm:streamname-realup-finite-window-seal-boundary} is exactly the
input face of the four-face route through $\StreamNameUp$, $\DyadicRatCoreUp$,
$\RegSeqRatUp$, and $\RealUp$ recorded by
\autoref{thm:regseqrat-real-seal-finite-window-saturation} and
\autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness}.
Thus the public observation interface consumed by the RegSeqRat-to-Real exit is
the finite StreamName window package: the $\mathsf{RatStreamNameCarrier}$ and
$\mathsf{RatStreamNameClassifier}$ rows, finite-window classifier rows,
endpoint readbacks in $\RatUp$, empty-window boundary, and constant/reindexing
stability rows.

This identification introduces no quotient stream, host real equality, selected
limit, ambient completeness witness, choice datum, or second $\RealUp$ seal
coordinate.
\end{theorem}
\begin{proof}
Begin with the StreamName boundary theorem,
\autoref{thm:streamname-realup-finite-window-seal-boundary}. It names exactly
the finite observation rows exposed by the $\StreamNameUp$ public interface and
states that those rows are available to later $\RealUp$ and $\RegSeqRat$ seal
sites without carrying regularity or completion data.

Next apply the RegSeqRat handoff exactness theorem,
\autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. The handoff
uses the same finite StreamName schedules as its first face before it reads the
DyadicRatCore ledger, the RegSeqRat classifier and regularity rows, and the
public Real seal row.

The four-face necessity and saturation results,
\autoref{thm:regseqrat-l10-four-face-necessity} and
\autoref{thm:regseqrat-real-seal-finite-window-saturation}, then force that
first face to be precisely the public finite-window observation interface. A
different StreamName input would either omit a required face or add a fifth
face, both excluded by the saturated finite route.

Finally use the Real sibling lattice summary,
\autoref{thm:real-regseqrat-streamname-lattice-route}, together with the
audit-clean exit readiness theorem
\autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness}. They
place the Real-facing exit on the same horizontal lattice edge and preserve the
negative boundary: no quotient stream, host real equality, selected limit,
ambient completeness witness, choice datum, or second $\RealUp$ seal coordinate
is introduced.
\end{proof}

\begin{theorem}[RegSeqRat Dyadic StreamName Real public seal non-escape]
\label{thm:regseqrat-dyadic-streamname-real-public-seal-nonescape}
Every accepted public L10 exit read whose four visible faces are the
$\StreamNameUp$ finite observation interface, the $\DyadicRatCoreUp$ ledger,
the $\RegSeqRatUp$ classifier and regularity rows, and the $\RealUp$ public
seal is exhausted by those four faces together with the displayed
$\mathsf{BHist}$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$
provenance.

Consequently the route exports no quotient stream, host real equality,
selected limit, ambient completeness witness, trichotomy branch, open-cover
compactness datum, choice datum, or second $\RealUp$ seal coordinate. Any
consumer requesting one of those data is no longer reading the public seal
through the displayed L10 exit route.
\end{theorem}
\begin{proof}
First identify the input face by
\autoref{thm:streamname-regseqrat-real-observation-interface}. The theorem
fixes the StreamName side as the finite-window observation package used by the
RegSeqRat-to-Real exit, rather than a quotient stream or a host-level equality
of sequences.

Next apply \autoref{thm:regseqrat-real-seal-finite-window-saturation}. The
saturation result says that the terminal public Real seal read has exactly the
StreamName observation face, the DyadicRatCore ledger face, the RegSeqRat
classifier and regularity face, and the Real seal face, carrying only the
displayed history, sameness, continuity, package, and local certificate rows.

Use \autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness}
to preserve the negative boundary through the exit audit. It rules out the
off-route completion and choice data at the same public read, so no selected
limit, ambient completeness witness, trichotomy branch, open-cover compactness
datum, or choice datum is supplied by the exit.

Finally locate the read on the sibling lattice route by
\autoref{thm:real-regseqrat-streamname-lattice-route}. Since the lattice edge
has the same four faces and the same displayed provenance rows, finite route
exhaustion leaves no coordinate for a second public seal or for any of the
off-route data listed above.
\end{proof}

\begin{theorem}[RegSeqRat four-face finite-observation completeness]
\label{thm:regseqrat-four-face-finite-observation-completeness}
For an accepted $\RegSeqRatUp$ Phase Real and Completion exit read, admission to
the public L10 exit surface is equivalent to finite observation data exposing
exactly the four faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
together with the displayed $\mathsf{BHist}$, $\hsame$, $\Cont$,
$\mathsf{Pkg}$, and $\NameCert$ rows.

On the forward reading, the accepted exit read presents the finite
$\StreamNameUp$ observation interface, the $\DyadicRatCoreUp$ ledger, the
$\RegSeqRatUp$ classifier and regularity rows, and the public $\RealUp$ seal.
On the reverse reading, any finite observation packet with exactly those four
faces and displayed rows is admitted as the same RegSeqRat-to-Real public exit
read.

The complete four-face surface exports no quotient stream, selected limit, host
equality, countable-choice row, ambient completeness row, metric-completeness
row, or standard bridge row.
\end{theorem}
\begin{proof}
For the forward direction, apply
\autoref{thm:streamname-open-phase-real-regseqrat-exit-correspondence}. It
identifies an accepted terminal public Real seal section with the open Phase
Real and Completion boundary whose visible support is precisely the four faces
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, together
with the displayed history, sameness, continuity, package, and certificate rows.

Next use \autoref{thm:streamname-regseqrat-real-observation-interface}. This
pins the first face to the finite $\StreamNameUp$ window data consumed by the
RegSeqRat-to-Real route, so the accepted read is described by finite
observation data rather than by a quotient stream or a host-level equality.

For the reverse direction, start again from the correspondence theorem and read
the same finite packet back as a Phase Real and Completion exit boundary. Then
apply
\autoref{thm:regseqrat-streamname-dyadic-real-audit-clean-exit-readiness} to
place the packet on the audit-clean L10 route through the DyadicRatCore ledger,
the RegSeqRat classifier and regularity rows, and the public Real seal row.

Finally apply
\autoref{thm:regseqrat-dyadic-streamname-real-public-seal-nonescape}. Since the
public seal is exhausted by the four displayed faces and provenance rows, no
coordinate remains for a quotient stream, selected limit, host equality,
countable-choice row, ambient completeness row, metric-completeness row, or
standard bridge row. Thus the admitted exit reads are exactly the finite
four-face observation packets described in the statement.
\end{proof}
