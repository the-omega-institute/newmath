\begin{theorem}[RegSeqRat four-face terminal pullback]
\label{thm:regseqrat-four-face-terminal-pullback}
Every accepted Phase Real terminal read and every completion-facing terminal
read obtained from a $\RegSeqRatUp$ packet factors through one finite
$\mathsf{BHist}$ pullback with exactly four source faces:
\begin{enumerate}
\item the $\DyadicRatCoreUp$ endpoint, radius, and distance ledgers;
\item the $\StreamNameUp$ scheduled window rows and displayed bundle rows;
\item the $\RegSeqRatUp$ regularity rows, classifier rows, and provenance
rows;
\item the terminal $\RealUp$ seal row.
\end{enumerate}
If two terminal reads expose the same four faces, then they classify together
through the displayed $\hsame$ comparisons, $\Cont$ routes, $\mathsf{Pkg}$
provenance, and local $\NameCert$ rows of that pullback.

The terminal pullback exports no quotient stream equality, host sequence
equality, selected limit, host real equality, ambient completeness,
metric-completion point, or standard bridge.
\end{theorem}
\begin{proof}
Start with \autoref{thm:regseqrat-streamname-dyadic-window-lock}. It places
the $\StreamNameUp$ schedule rows and the $\DyadicRatCoreUp$ endpoint and
radius ledgers inside the same accepted finite $\RegSeqRatUp$ window.

Next apply \autoref{thm:regseqrat-seal-route-visible-source-normal-form}. The
terminal $\RealUp$ seal route can read only the visible finite source surface
formed from the schedule, dyadic ledgers, regularity rows, classifier rows,
$\mathsf{BHist}$ packet rows, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local
$\NameCert$ provenance.

Use \autoref{thm:regseqrat-real-seal-route-exclusion-package} to remove every
off-surface route component. The remaining route package is exactly the
finite inventory from which the four faces of the pullback are read.

Apply \autoref{thm:regseqrat-real-seal-classifier-factorization}. Agreement on
the four faces transports the two terminal reads through the displayed
$\RegSeqRatUp$ classifier surface together with the visible $\hsame$, $\Cont$,
$\mathsf{Pkg}$, and local $\NameCert$ rows.

Finally invoke \autoref{thm:regseqrat-real-seal-schedule-confluence}. The
common displayed inventory replays to one terminal finite $\RealUp$ seal
route, and the cited exclusion package leaves no coordinate for quotient
stream equality, host sequence equality, selected limits, host real equality,
ambient completeness, metric-completion points, or standard bridge data.
\end{proof}

\begin{theorem}[RegSeqRat four-face terminal route uniqueness]
\label{thm:regseqrat-four-face-terminal-route-uniqueness}
Let two accepted terminal reads from $\RegSeqRatUp$ packets expose the same
four-face pullback inventory: the $\DyadicRatCoreUp$ endpoint, radius, and
distance ledgers; the $\StreamNameUp$ scheduled-window and bundle rows; the
$\RegSeqRatUp$ regularity, classifier, and provenance rows; and the terminal
$\RealUp$ seal row. Then the two terminal routes classify together through the
same displayed $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ rows of
that inventory.

This route uniqueness introduces no quotient stream equality, selected host
limit, metric-completion point, $\RealUp$ completeness datum, or standard
bridge.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regseqrat-four-face-terminal-pullback}. The shared four
faces are already one finite pullback surface, so both terminal reads are read
through the same displayed endpoint ledgers, schedules, regularity rows,
classifier rows, provenance rows, and terminal seal row.

Use \autoref{thm:regseqrat-real-seal-route-exclusion-package}. It removes all
off-surface coordinates from the terminal route and leaves only the visible
$\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ rows for the accepted
read.

Next apply \autoref{thm:regseqrat-real-seal-classifier-factorization}. Since
the two reads agree on the four faces, the classifier comparison factors
through the same displayed rows rather than through an external completion or
quotient coordinate.

Finally use \autoref{thm:regseqrat-real-seal-schedule-confluence}. The common
scheduled-window inventory has a single terminal replay route to the
$\RealUp$ seal row, so the terminal routes classify together through the same
finite RegSeqRat evidence, with no quotient stream equality, selected host
limit, metric-completion point, $\RealUp$ completeness datum, or standard
bridge.
\end{proof}

\begin{theorem}[RegSeqRat terminal four-face correspondence]
\label{thm:regseqrat-terminal-four-face-correspondence}
Two accepted terminal reads from $\RegSeqRatUp$ packets classify together
exactly when their visible four-face inventories agree. The four faces are the
$\DyadicRatCoreUp$ endpoint, radius, and distance ledgers; the $\StreamNameUp$
scheduled-window rows; the $\RegSeqRatUp$ regularity, classifier, and
provenance rows; and the terminal $\RealUp$ seal row.

The agreement is transported only by the displayed $\BHist$ rows, $\hsame$
comparisons, $\Cont$ routes, $\mathsf{Pkg}$ provenance, and local $\NameCert$
rows. The correspondence exports no quotient stream equality, host sequence
equality, selected limit, host real equality, metric-completion point,
completeness datum, or standard bridge row.
\end{theorem}
\begin{proof}
For the forward direction, apply
\autoref{thm:regseqrat-four-face-terminal-pullback}. Any accepted terminal
read from a $\RegSeqRatUp$ packet already factors through one finite surface
with the DyadicRatCore ledger face, StreamName schedule face, RegSeqRat
regularity and classifier face, and terminal $\RealUp$ seal face.

Next use
\autoref{thm:regseqrat-four-face-terminal-route-uniqueness}. Once two accepted
terminal reads classify together on that finite surface, their classifier
route is forced to be the displayed route through the same $\BHist$, $\hsame$,
$\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ rows. Thus the only visible
inventory on which the shared classification can be read is the four-face
inventory named in the statement.

For the reverse direction, suppose the two terminal reads expose agreeing
four-face inventories with the displayed transport rows. Apply
\autoref{thm:regseqrat-four-face-terminal-pullback} to repack the two
inventories as one finite pullback surface.

Finally use \autoref{thm:regseqrat-real-seal-route-exclusion-package} and
\autoref{thm:regseqrat-real-seal-schedule-confluence}. The exclusion package
removes quotient streams, selected limits, host equality, metric-completion
points, completeness rows, and standard bridge rows from the terminal route;
schedule confluence leaves a single terminal replay to the $\RealUp$ seal row.
The remaining route is exactly the displayed $\BHist$, $\hsame$, $\Cont$,
$\mathsf{Pkg}$, and local $\NameCert$ route, so the two terminal reads classify
together.
\end{proof}

\begin{theorem}[RegSeqRat four-face terminal classifier completeness]
\label{thm:regseqrat-four-face-terminal-classifier-completeness}
The $\RegSeqRatUp$ terminal classifier is complete for accepted terminal reads
exactly at the four visible faces: the $\DyadicRatCoreUp$ endpoint, radius,
and distance ledgers; the $\StreamNameUp$ schedules; the $\RegSeqRatUp$
regularity, classifier, and provenance rows; and the terminal $\RealUp$ seal.

Thus two accepted terminal reads classify together precisely when these four
faces agree through the displayed $\BHist$, $\hsame$, $\Cont$, $\mathsf{Pkg}$,
and local $\NameCert$ rows. No quotient stream equality, host sequence
equality, selected limit, host real equality, metric-completion point,
ambient completeness datum, or standard bridge row is part of the classifier
completion surface.
\end{theorem}
\begin{proof}
First assemble the classifier surface. Apply
\autoref{thm:regseqrat-four-face-terminal-pullback}; every accepted terminal
read factors through one finite pullback whose visible faces are exactly the
DyadicRatCore ledger face, StreamName schedule face, RegSeqRat
regularity/classifier/provenance face, and terminal $\RealUp$ seal face.

Next use route uniqueness. By
\autoref{thm:regseqrat-four-face-terminal-route-uniqueness}, agreement on
those four faces forces the two terminal routes to classify through the same
displayed $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ rows.

For the converse, apply
\autoref{thm:regseqrat-terminal-four-face-correspondence}. If two accepted
terminal reads classify together, their visible four-face inventories agree,
and if the four visible faces agree, the accepted terminal reads classify
together.

The exclusions in the cited pullback and correspondence statements remove
quotient stream equality, host sequence equality, selected limits, host real
equality, metric-completion points, ambient completeness, and standard bridge
rows from the terminal classifier. Hence the four-face surface is complete for
accepted terminal reads.
\end{proof}

\begin{theorem}[RegSeqRat dyadic-tail Real-seal status exhaustion]
\label{thm:regseqrat-dyadic-tail-real-seal-status-exhaustion}
Every accepted terminal L10 read through $\RegSeqRatUp$ is exhausted by the
status ledger formed from exactly four faces: the $\DyadicRatCoreUp$ endpoint,
radius, and distance ledgers; the $\StreamNameUp$ scheduled-window rows; the
$\RegSeqRatUp$ regularity, classifier, and provenance rows; and the terminal
$\RealUp$ seal row. Transport across this ledger uses only the displayed
$\BHist$ rows, $\hsame$ comparisons, $\Cont$ routes, $\Pkg$ provenance, and
local $\NameCert$ rows.

The ledger contains no quotient stream equality, host equality, selected
limit, ambient completeness, metric-completion point, countable-choice row,
verification-axis promotion, or standard bridge row.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regseqrat-four-face-terminal-pullback}. It places
each accepted terminal L10 read on the single finite pullback whose faces are
the DyadicRatCore ledger face, StreamName schedule face, RegSeqRat regularity
and classifier face, and terminal $\RealUp$ seal face.

Next use \autoref{thm:regseqrat-four-face-terminal-classifier-completeness}.
The classifier is complete exactly on those four visible faces, so every
accepted terminal read is accounted for by the displayed status rows rather
than by an external completion surface.

Finally combine \autoref{thm:regseqrat-real-seal-route-exclusion-package} and
\autoref{thm:regseqrat-real-seal-schedule-confluence}. The exclusion package
removes quotient stream equality, host equality, selected limits,
metric-completion points, countable-choice rows, verification-axis promotion,
and standard bridge rows; schedule confluence keeps the replay on the same
displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ inventory.
Thus the four-face status ledger is exhaustive.
\end{proof}

\begin{theorem}[RegSeqRat finite-cover terminal classifier descent]
\label{thm:regseqrat-finite-cover-terminal-classifier-descent}
Let $\{W_i\}_{i \in I}$ be a finite family of certified
$\StreamNameUp$ windows for a $\RegSeqRatUp$ terminal read. Suppose the
pairwise and triple overlaps satisfy the $\StreamNameUp$--$\RegSeqRatUp$
source cocycle and seal cocycle hypotheses, and suppose the induced handoffs
consume the same $\DyadicRatCoreUp$ endpoint, radius, and distance ledgers on
overlaps. Then the resulting $\RegSeqRatUp$ terminal classifier surface is
exactly the pasted four-face inventory consisting of the $\DyadicRatCoreUp$
endpoint, radius, and distance ledgers; the $\StreamNameUp$ window rows; the
$\RegSeqRatUp$ regularity, classifier, and provenance rows; and the terminal
$\RealUp$ seal row.

The descent transport uses only the displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ rows. It introduces no quotient streams, selected host
limits, host equality, countable choice, ambient $\RealUp$ completeness,
metric-completeness rows, or standard bridge rows.
\end{theorem}
\begin{proof}
First paste the finite StreamName cover. Apply
\autoref{thm:streamname-regseqrat-finite-cover-pasted-source-exhaustion} to
the certified windows and their pairwise and triple source cocycle data. Apply
\autoref{thm:streamname-regseqrat-finite-cover-pasted-seal-exhaustion} to the
same cover with the seal cocycle data. These two finite-cover statements keep
the pasted source and seal on the displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ rows, and they keep the DyadicRatCore ledgers shared
on overlaps.

Next identify the pasted cover with the terminal classifier surface. By
\autoref{thm:regseqrat-four-face-terminal-classifier-completeness}, an
accepted terminal $\RegSeqRatUp$ read is classified completely by the four
visible faces: the DyadicRatCore ledger face, the StreamName window face, the
RegSeqRat regularity/classifier/provenance face, and the terminal $\RealUp$
seal face. The pasted finite cover supplies exactly those rows, so no extra
classifier coordinate is available.

Finally keep the terminal seal on the same four-face route. Use
\autoref{thm:regseqrat-dyadic-tail-real-seal-status-exhaustion} to exhaust
the $\RegSeqRatUp$ terminal status by the displayed four-face ledger, and use
\autoref{thm:real-l10-terminal-four-face-correspondence} to read the terminal
$\RealUp$ seal as the same four-face packet. The exclusions in these route
statements remove quotient streams, selected host limits, host equality,
countable choice, ambient completeness, metric-completeness rows, and standard
bridge rows. Hence the descended classifier is precisely the pasted terminal
four-face inventory.
\end{proof}
