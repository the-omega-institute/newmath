\begin{definition}[RegSeqRat shared-window selector surface]
\label{def:regseqrat-shared-window-selector-surface}
For an accepted $\RegSeqRatUp$ packet $P$, the shared-window selector surface is the finite $\BHist$ surface consisting of the following displayed rows:
\begin{enumerate}
\item $\StreamNameUp$ schedule rows selecting the public finite windows read by the request;
\item $\DyadicRatCoreUp$ endpoint, radius, and dyadic-distance rows attached to those windows;
\item $\RegSeqRatUp$ classifier, regularity, and tail-budget rows for the same selected windows;
\item componentwise $\hsame$ rows, displayed $\Cont$ replay rows, $\Pkg$ provenance, and the local $\NameCert_{\RegSeqRatUp}$ row;
\item the terminal $\RealUp$ seal row consuming the displayed regular-rational source surface.
\end{enumerate}
The surface contains only the rows visible in the $\StreamNameUp$--$\DyadicRatCoreUp$--$\RegSeqRatUp$--$\RealUp$ handoff. It has no quotient stream equality, host real equality, selected limit, completion point, ambient completeness datum, hidden selector, or second seal coordinate.
\end{definition}

\begin{theorem}[RegSeqRat shared-window selector determinacy]
\label{thm:regseqrat-shared-window-selector-determinacy}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $S$ and $T$ be accepted finite requests whose displayed selector surfaces are the shared-window surface of \autoref{def:regseqrat-shared-window-selector-surface}. If $S$ and $T$ display the same $\StreamNameUp$ schedule rows, $\DyadicRatCoreUp$ endpoint, radius, and dyadic-distance rows, $\RegSeqRatUp$ classifier and tail-budget rows, componentwise $\hsame$ rows, displayed $\Cont$ rows, $\Pkg$ provenance, local $\NameCert$ row, and terminal $\RealUp$ seal row, then $S$ and $T$ determine the same RegSeqRat tail-window route up to the displayed $\hsame$ transport and $\Cont$ replay.

The determinacy is internal to the shared-window surface. It does not identify quotient streams, host real values, selected limits, completion points, ambient completeness data, hidden selector choices, or any seal outside the displayed $\RealUp$ row.
\end{theorem}
\begin{proof}
First use the scope triad of \autoref{thm:regseqrat-streamname-real-scope-triad}. It places every public $\RealUp$ seal read behind the visible StreamName schedule, the dyadic endpoint and radius rows, and the RegSeqRat classifier surface before the seal row is consumed.

Next apply the finite request source section of \autoref{thm:regseqrat-finite-request-source-section}. The two finite requests have the same displayed source rows, so their schedule, dyadic ledger, classifier, regularity, transport, continuation, package, and naming coordinates are one public source section.

Now use the tail-selector RealUp seal exhaustion theorem, \autoref{thm:regseqrat-tail-selector-real-seal-exhaustion}. It prevents either request from using a quotient stream, hidden selector, selected limit, completion point, or second seal to distinguish a separate tail-window route.

Finally cite the StreamName-Dyadic-Real handoff exactness of \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. Transport through $\hsame$ and replay through $\Cont$ preserve the same handoff rows, so the two accepted requests reach the same RegSeqRat tail-window route and then the same displayed $\RealUp$ seal row.
\end{proof}

\begin{theorem}[RegSeqRat shared-window terminal pullback equivalence]
\label{thm:regseqrat-shared-window-terminal-pullback-equivalence}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $S$ and $T$ be accepted finite requests to $P$ whose displayed selector surfaces are the shared-window surface of \autoref{def:regseqrat-shared-window-selector-surface}. The following data determine one another:
\begin{enumerate}
\item equality of the shared-window selector surface displayed by $S$ and $T$;
\item equality of the terminal $\RealUp$ seal reads obtained from $S$ and $T$ after both requests factor through the ordered four-face packet
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
\end{enumerate}
Equivalently, every terminal-budget consumer accepted by $S$ is accepted by $T$ with the same visible dyadic, stream, regular-rational, terminal seal, $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows, and conversely. The correspondence exposes no quotient stream equality, host real equality, selected limit, hidden selector, completion carrier, ambient completeness datum, or second terminal seal.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:regseqrat-shared-window-selector-determinacy}. Agreement on the shared-window selector surface identifies the finite StreamName schedule rows, DyadicRatCore endpoint, radius, and distance rows, RegSeqRat classifier and tail-budget rows, componentwise transports, continuations, package provenance, local naming row, and displayed terminal seal coordinate.

Next use \autoref{thm:regseqrat-finite-request-terminal-budget-factorization} and \autoref{thm:real-regseqrat-terminal-budget-exhaustion}. The two accepted finite requests therefore factor through the same ordered four-face packet from $\DyadicRatCoreUp$ to $\RealUp$, with the same support rows. The seal uniqueness theorem, \autoref{thm:real-regseqrat-terminal-budget-seal-uniqueness}, then identifies their visible terminal $\RealUp$ seal reads.

For the reverse direction, suppose the terminal $\RealUp$ seal reads agree after both requests have been read through the four-face packet. The exhaustion theorem places every visible terminal-budget row of each request on that packet, and seal uniqueness prevents a second terminal read from separating requests with the same displayed faces and support rows.

It remains to recover the selector surface. The finite-request factorization returns exactly the StreamName, DyadicRatCore, RegSeqRat, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows listed in \autoref{def:regseqrat-shared-window-selector-surface}. Hence equality of the terminal seal reads on the factored packet is equality of the shared-window selector surface.

For the consumer form, take any terminal-budget consumer accepted by $S$. By terminal-budget exhaustion it reads only the four displayed faces and support rows, so the already established surface equality repacks the same visible rows as an accepted consumer for $T$; the converse is symmetric. Finally, \autoref{thm:regseqrat-real-seal-selector-nonescape} excludes quotient streams, host real equality, selected limits, hidden selectors, completion carriers, ambient completeness data, and second seals from both sides of the correspondence.
\end{proof}

\begin{theorem}[RegSeqRat tail-selector section pullback]
\label{thm:regseqrat-tail-selector-section-pullback}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $S$ and $T$ be accepted finite requests to $P$ whose displayed selector surfaces are the shared-window surface of \autoref{def:regseqrat-shared-window-selector-surface}. Agreement of the shared-window selector surface displayed by $S$ and $T$ is equivalent to agreement of the finite-request source section of \autoref{thm:regseqrat-finite-request-source-section}, restricted exactly to the $\StreamNameUp$ schedule rows, $\DyadicRatCoreUp$ endpoint, radius, and dyadic-distance rows, $\RegSeqRatUp$ classifier, regularity, and tail-budget rows, the terminal $\RealUp$ seal row, componentwise $\hsame$ rows, displayed $\Cont$ rows, $\Pkg$ provenance, and local $\NameCert$ rows.

The pullback has no coordinates for quotient stream equality, host real equality, selected limits, completion points, ambient completeness data, choice data, or second seal coordinates. Thus the selector surface and the finite-request source section name the same finite request data over the ordered handoff
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
\end{theorem}
\begin{proof}
For the forward direction, begin with \autoref{thm:regseqrat-shared-window-selector-determinacy}. Agreement of the shared-window selector surface identifies the visible schedule, dyadic ledger, classifier, regularity, tail-budget, transport, continuation, package, naming, and terminal seal rows.

Apply the finite-request source section theorem, \autoref{thm:regseqrat-finite-request-source-section}. It records exactly those visible rows as the finite source section of each accepted request. The StreamName-Dyadic-Real handoff exactness theorem, \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}, keeps the schedule rows, dyadic endpoint and radius rows, regular-rational classifier rows, and terminal seal row on the same finite handoff, so the two source sections agree on precisely the displayed rows named in the statement.

For the reverse direction, suppose the finite-request source sections agree on those rows. Use \autoref{thm:regseqrat-finite-request-terminal-budget-factorization} to factor both accepted requests through the common terminal-budget section. Then \autoref{thm:real-regseqrat-terminal-budget-exhaustion} places every visible Real-facing consumer on that same four-face packet, and \autoref{thm:real-regseqrat-terminal-budget-seal-uniqueness} identifies the terminal seal row.

It remains only to rule out hidden coordinates. The selector surface definition contains no row for quotient streams, host equality, selected limits, completion points, ambient completeness, choice data, or a second seal coordinate. Applying \autoref{thm:regseqrat-real-seal-selector-nonescape} removes those candidates from the Real-facing boundary, so agreement of the finite-request source section repacks exactly as agreement of the shared-window selector surface.
\end{proof}

\begin{theorem}[RegSeqRat shared-window reindex pullback coherence]
\label{thm:regseqrat-shared-window-reindex-pullback-coherence}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $S$ and $T$ be accepted finite requests to $P$ whose displayed selector surfaces are the shared-window surface of \autoref{def:regseqrat-shared-window-selector-surface}. Suppose $R_S$ and $R_T$ are certified public $\StreamNameUp$ finite-window reindex routes over the displayed selector surfaces of $S$ and $T$. Assume that the reindex routes preserve the displayed $\StreamNameUp$ schedule rows, the $\DyadicRatCoreUp$ endpoint, radius, and dyadic-distance rows, the $\RegSeqRatUp$ classifier, regularity, and tail-budget rows, the terminal $\RealUp$ seal row, componentwise $\hsame$ rows, displayed $\Cont$ rows, $\Pkg$ provenance, and local $\NameCert$ rows.

Then the RegSeqRat tail-selector section pullback of \autoref{thm:regseqrat-tail-selector-section-pullback} before applying $R_S$ and $R_T$ is equivalent to the tail-selector section pullback after applying them. Moreover, both terminal $\RealUp$ seal reads factor through the same ordered four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
The coherence is internal to the displayed finite surface. It introduces no quotient stream equality, host real equality, selected limit, completion point, choice datum, ambient completeness row, or second seal coordinate.
\end{theorem}
\begin{proof}
First compare the StreamName windows before and after reindexing. Apply \autoref{thm:streamname-window-reindex-determinacy} to the certified routes $R_S$ and $R_T$. The hypotheses name the same schedule, endpoint, $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows, so the public finite observation section is unchanged by the reindex routes.

Next place that preserved section on the terminal side. By \autoref{thm:streamname-reindex-terminal-coverage}, the reindexed reads expose the same $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger, $\RegSeqRatUp$ readback, and $\RealUp$ seal. The four-face exit naturality theorem, \autoref{thm:streamname-reindex-four-face-exit-naturality}, keeps this terminal read natural in the certified reindex route rather than in any hidden host coordinate.

Now return to the RegSeqRat selector surface. Apply \autoref{thm:regseqrat-tail-selector-section-pullback} before reindexing and again after reindexing. Since the preserved rows are exactly the StreamName schedule rows, DyadicRatCore endpoint, radius, and distance rows, RegSeqRat classifier, regularity, and tail-budget rows, terminal seal row, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows, the two pullback readings are the same finite source-section comparison.

Finally use \autoref{thm:regseqrat-shared-window-terminal-pullback-equivalence} to identify the terminal seal read with that same selector surface, and use \autoref{thm:regseqrat-real-seal-selector-nonescape} to exclude extra coordinates. Thus the pre-reindex and post-reindex pullbacks transport through the same four-face route and cannot be separated by quotient streams, host real equality, selected limits, completion points, choice data, ambient completeness rows, or second seals.
\end{proof}

\begin{theorem}[RegSeqRat shared-window reindex composition law]
\label{thm:regseqrat-shared-window-reindex-composition-law}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $S$ and $T$ be accepted finite requests to $P$ whose displayed selector surfaces are the shared-window surface of \autoref{def:regseqrat-shared-window-selector-surface}. Suppose $R_1$ and $R_2$ are composable certified public $\StreamNameUp$ finite-window reindex routes on those displayed surfaces, and suppose their certified composite route is $R_2 \circ R_1$. Assume that $R_1$, $R_2$, and $R_2 \circ R_1$ preserve the displayed $\StreamNameUp$ schedule rows, $\DyadicRatCoreUp$ endpoint, radius, and dyadic-distance rows, $\RegSeqRatUp$ classifier, regularity, and tail-budget rows, terminal $\RealUp$ seal row, componentwise $\hsame$ rows, displayed $\Cont$ rows, $\Pkg$ provenance, and local $\NameCert$ rows.

Then applying $R_1$ and then $R_2$ gives the same RegSeqRat tail-selector section pullback, and the same terminal $\RealUp$ seal read, as applying the certified composite route $R_2 \circ R_1$ once. The equality is only equality of the displayed finite selector surface over the ordered handoff
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
It does not identify quotient stream equality, host real equality, selected limits, completion points, choice data, ambient completeness rows, or second seal coordinates.
\end{theorem}
\begin{proof}
First work inside the public StreamName window. The reindex composition law of \autoref{thm:rat-stream-name-reindexing-composition-law} identifies the two-step certified route $R_1$ followed by $R_2$ with the single certified route $R_2 \circ R_1$ on the finite StreamName observation section.

Next transport the displayed rows through this identification. Apply \autoref{thm:streamname-window-reindex-determinacy} to the two certified presentations, and use \autoref{thm:streamname-reindex-four-face-exit-naturality} to keep the StreamName window, DyadicRatCore ledger, RegSeqRat readback, and terminal Real seal on the same four-face exit route.

Now return to the RegSeqRat selector section. The hypotheses say that each route preserves exactly the rows listed in \autoref{def:regseqrat-shared-window-selector-surface}: schedule, dyadic endpoint, radius, distance, classifier, regularity, tail budget, terminal seal, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$. Applying \autoref{thm:regseqrat-shared-window-reindex-pullback-coherence} to the first route, then to the second route, and then to the composite route gives the same tail-selector section pullback on those rows.

Finally identify the terminal seal boundary. By \autoref{thm:regseqrat-shared-window-terminal-pullback-equivalence}, equality of the displayed selector pullback is equality of the terminal $\RealUp$ seal read through the same ordered handoff. The nonescape theorem, \autoref{thm:regseqrat-real-seal-selector-nonescape}, removes quotient streams, host real equality, selected limits, completion points, choice data, ambient completeness rows, and second seal coordinates from the comparison, so no hidden coordinate can distinguish the two-step route from its certified composite.
\end{proof}

\begin{theorem}[RegSeqRat shared-window triple-overlap cocycle]
\label{thm:regseqrat-shared-window-triple-overlap-cocycle}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $S$, $T$, and $U$ be accepted finite requests to $P$. Suppose every pair among $S$, $T$, and $U$ displays the shared-window selector surface of \autoref{def:regseqrat-shared-window-selector-surface}. Pairwise agreement on the displayed $\StreamNameUp$ schedule rows, $\DyadicRatCoreUp$ endpoint, radius, and dyadic-distance rows, $\RegSeqRatUp$ classifier, regularity, and tail-budget rows, terminal $\RealUp$ seal rows, componentwise $\hsame$ rows, displayed $\Cont$ rows, $\Pkg$ provenance, and local $\NameCert$ rows determines one triple-overlap selector surface over the ordered handoff
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Conversely, one such triple-overlap selector surface restricts to the three pairwise pullback equivalences between $(S,T)$, $(T,U)$, and $(S,U)$.

The triple-overlap surface contains no quotient stream equality, host real equality, selected limit, completion point, ambient completeness datum, choice data, or second seal coordinate. It is exactly the common finite $\BHist$ surface on the displayed StreamName, DyadicRatCore, RegSeqRat, terminal Real, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start with the three pairwise selector surfaces. On each pair, apply \autoref{thm:regseqrat-shared-window-selector-determinacy} to identify the displayed schedule, dyadic ledger, classifier, regularity, tail-budget, transport, continuation, package, naming, and terminal seal rows.

Next pass from pairwise selector agreement to finite-request source sections using \autoref{thm:regseqrat-tail-selector-section-pullback}. The three source-section comparisons have the same visible rows, and \autoref{thm:streamname-regseqrat-triple-overlap-source-cocycle} identifies their StreamName--RegSeqRat source restrictions as one source surface on the common triple overlap.

Now attach the terminal Real row. The terminal pullback equivalence of \autoref{thm:regseqrat-shared-window-terminal-pullback-equivalence} and the Real finite-window cocycle \autoref{thm:realup-streamname-regseqrat-triple-window-cocycle} make the three terminal seal restrictions the restrictions of one $\RealUp$ boundary over the same four-face handoff.

This gives the forward direction: the common source surface and the common terminal boundary are precisely the shared-window selector rows listed in \autoref{def:regseqrat-shared-window-selector-surface}, so they determine one triple-overlap selector surface.

For the reverse direction, restrict a single triple-overlap selector surface to the three displayed pairwise faces. Each restriction has the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, terminal $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows, so \autoref{thm:regseqrat-tail-selector-section-pullback} returns the finite-request source-section equivalence and \autoref{thm:regseqrat-shared-window-terminal-pullback-equivalence} returns the terminal pullback equivalence for each pair.

Finally apply \autoref{thm:regseqrat-real-seal-selector-nonescape}. Since the shared-window surface has no row for quotient streams, host real equality, selected limits, completion points, ambient completeness data, choice data, or second seals, none of those objects can distinguish the triple-overlap surface from its three pairwise restrictions.
\end{proof}

\begin{theorem}[RegSeqRat dyadic triangle selector equivalence]
\label{thm:regseqrat-dyadic-triangle-selector-equivalence}
Let $P$ be an accepted $\RegSeqRatUp$ packet, and let $S$, $T$, and $U$ be accepted finite requests to $P$ whose pairwise faces display the shared-window selector surface of \autoref{def:regseqrat-shared-window-selector-surface}. The following presentations determine one another on exactly the displayed finite rows:
\begin{enumerate}
\item pairwise agreement of the $\DyadicRatCoreUp$ common scale-window triangle ledger on the signed mantissa row, refined positive-unary exponent row, denominator ledger, common-exponent comparison row, and dyadic distance/radius rows;
\item agreement of the $\RegSeqRatUp$ shared-window triple-overlap selector surface on the $\StreamNameUp$ finite windows, $\RegSeqRatUp$ classifier/readback/tail-budget rows, terminal $\RealUp$ seal row, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows;
\item equality of the terminal $\RealUp$ seal read after the three requests are read through the ordered route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
\end{enumerate}
Thus the dyadic triangle ledger and the RegSeqRat selector surface are the same BEDC-visible finite data at the terminal $\RealUp$ boundary. The equivalence adds no quotient rational, quotient stream, host real equality, selected limit, completion point, choice data, ambient completeness datum, or second seal.
\end{theorem}
\begin{proof}
Begin with the dyadic presentation. Apply \autoref{thm:dyadicratcore-common-scale-window-four-face-pullback-correspondence} to pass from pairwise agreement of the common scale-window triangle ledger to the terminal four-face pullback over the ordered $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ route.

Next identify that four-face pullback with the RegSeqRat selector surface. The terminal pullback equivalence of \autoref{thm:regseqrat-shared-window-terminal-pullback-equivalence} says that equality of the shared-window selector surface and equality of the terminal $\RealUp$ seal read through the same four faces determine one another on the listed StreamName, dyadic, regular-rational, transport, continuation, package, naming, and seal rows.

For three requests, use \autoref{thm:regseqrat-shared-window-triple-overlap-cocycle}. The three pairwise selector faces restrict from one triple-overlap selector surface, and conversely that surface restricts back to the three pairwise pullbacks. Hence the dyadic triangle ledger, the RegSeqRat triple-overlap surface, and the terminal seal read are three presentations of the same finite boundary data.

Finally apply \autoref{thm:regseqrat-real-seal-selector-nonescape}. Since the selector surface has no row for quotient streams, host real equality, selected limits, completion points, choice data, ambient completeness rows, or second seals, the correspondence cannot be enlarged by any of those objects.
\end{proof}

\begin{theorem}[RegSeqRat Real seal selector non-escape]
\label{thm:regseqrat-real-seal-selector-nonescape}
A $\RealUp$ seal consumer of the RegSeqRat shared-window selector surface receives only the displayed $\StreamNameUp$ schedule rows, $\DyadicRatCoreUp$ endpoint, radius, and distance rows, $\RegSeqRatUp$ classifier and tail-budget rows, componentwise $\hsame$ rows, displayed $\Cont$ replay rows, $\Pkg$ provenance, local $\NameCert$ provenance, and the terminal $\RealUp$ seal row. The consumer receives no quotient stream, host real equality, selected limit, hidden selector, detached completeness row, or second $\RealUp$ seal.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:regseqrat-tail-selector-real-seal-exhaustion}. It exhausts a selector-facing seal read by the finite StreamName, DyadicRatCore, RegSeqRat, and RealUp rows of the accepted packet.

Apply the finite request Real seal correspondence of \autoref{thm:regseqrat-finite-request-real-seal-correspondence}. A finite request reaches the seal only through its displayed source section, not through an external real equality or selected limit.

Use the terminal tail-budget seal theorem \autoref{thm:regseqrat-tail-budget-terminal-real-seal}. It keeps the tail budget and seal route on the same finite dyadic ledger and removes detached completion and completeness coordinates from the consumer inventory.

The handoff exactness theorem \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness} supplies the remaining source-lock: the StreamName schedule and dyadic rows travel together into the Real-facing seal. Since the shared-window selector surface has no row for the excluded objects, the consumer cannot export any of them.
\end{proof}
