\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseExpansionCocycleUp$}
\label{ch:concrete-instances-hyperbolic-phase-expansion-cocycle-namecert}
\origin{ai}

The $\HyperbolicPhaseExpansionCocycleUp$ packet realizes the composable observer-phase transport route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite observer-phase, phase-distance, boundary-shadow, transport-budget, transport, replay, provenance, and NameCert data. It sits beside the observer-phase packet of \autoref{ch:concrete-instances-hyperbolic-observer-phase-namecert}, the phase-distance shell packet of \autoref{ch:concrete-instances-phase-distance-observer-expansion-namecert}, the observer-phase groupoid packet of \autoref{ch:concrete-instances-observer-phase-groupoid-namecert}, the transport-budget packet of \autoref{ch:concrete-instances-hyperbolic-phase-transport-budget-namecert}, and the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}. The packet names a finite cocycle reading; it is not a global hyperbolic space, a completed observer geometry, or a host transport theorem.

\begin{definition}[Hyperbolic phase expansion cocycle carrier]
\label{def:hyperbolic-phase-expansion-cocycle-carrier}
A $\HyperbolicPhaseExpansionCocycleUp$ carrier is a finite $\BHist$ packet
$$
Q=(O,D,B,T,G,R,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $O$ is the observer-phase row.
\item $D$ is the phase-distance row.
\item $B$ is the boundary-shadow row.
\item $T$ is the transport-budget row.
\item $G$ is the observer-phase groupoid row that composes the displayed route.
\item $R$ is the cocycle readback row, recorded only after the displayed transport and distance data have been inspected.
\item $H$ records componentwise $\hsame$ transport for the observer-phase, phase-distance, boundary-shadow, transport-budget, groupoid, readback, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the cocycle request through $O,D,B,T,G,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicPhaseExpansionCocycleUp$, $\HyperbolicObserverPhaseUp$, $\PhaseDistanceObserverExpansionUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicPhaseTransportBudgetUp$, $\ObserverPhaseGroupoidUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicPhaseExpansionCocycleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $O,D,B,T,G,R,H,C,P,N$. It has no coordinate for a completed boundary, host phase equality, quotient transport class, or ambient completeness.
\end{definition}

\begin{theorem}[Hyperbolic phase expansion cocycle NameCert obligations]
\label{thm:hyperbolic-phase-expansion-cocycle-namecert-obligations}
For every accepted $\HyperbolicPhaseExpansionCocycleUp$ carrier $Q=(O,D,B,T,G,R,H,C,P,N)$, the local $\NameCert_{\HyperbolicPhaseExpansionCocycleUp}$ surface consists exactly of carrier admission for observer-phase, phase-distance, boundary-shadow, transport-budget, observer-phase groupoid, cocycle readback, componentwise transport, displayed replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $O,D,B,T,G,R,H,C,P,N$ are the public inventory of the cocycle packet.

The observer-phase, distance, boundary-shadow, and transport-budget rows are displayed before the readback row $R$ is available. The groupoid row $G$ composes those rows into the finite cocycle route, while the structural rows $H,C,P,N$ transport, replay, source, and name that same route.

Since the carrier contains no completed boundary, host phase equality, quotient transport class, or ambient completeness coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicPhaseExpansionCocycleUp$ read exposes observer-phase, phase-distance, boundary-shadow, transport-budget, observer-phase groupoid, cocycle readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any public cocycle consumer receives the packet.}

\independenceWitness{hyperbolic-observer-phase, phase-distance-observer-expansion, observer-phase-groupoid, hyperbolic-phase-transport-budget, hyperbolic-boundary-shadow: $\HyperbolicPhaseExpansionCocycleUp$ is not bijective to any listed sibling because it jointly binds observer-phase, phase-distance, boundary-shadow, transport-budget, groupoid, readback, transport, replay, provenance, and local naming rows into one finite cocycle surface.}

The Lean TasteGate follow-up for this packet must provide explicit $\mathsf{Nontrivial}\ \HyperbolicPhaseExpansionCocycleUp$ and $\mathsf{FieldFaithful}\ \HyperbolicPhaseExpansionCocycleUp$ instances before a checked carrier-alignment theorem can witness the public route.

\closureat{HyperbolicPhaseExpansionCocycleUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseExpansionCocycleUp}
  \constructivestory{A $\HyperbolicPhaseExpansionCocycleUp$ packet is generated from observer-phase, phase-distance, boundary-shadow, transport-budget, observer-phase groupoid, cocycle readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-expansion-cocycle-carrier}, \autoref{thm:hyperbolic-phase-expansion-cocycle-namecert-obligations}, the falsifiable prediction, and the independence witness seed the finite observer-phase expansion cocycle route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed boundary geometry, host phase equality, quotient transport classes, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, observer-phase exposure, phase-distance exposure, boundary-shadow exposure, transport-budget exposure, groupoid composition, cocycle readback, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for the hyperbolic phase expansion cocycle target.}
\end{closurestatus}
