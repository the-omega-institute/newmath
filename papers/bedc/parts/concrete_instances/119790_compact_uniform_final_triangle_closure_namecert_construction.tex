\chapter{A Concrete Naming Certificate for $\CompactUniformFinalTriangleClosureUp$}
\label{ch:concrete-instances-compact-uniform-final-triangle-closure-namecert}
\origin{human}

Following Bishop and Bridges on the Heine--Cantor compact metric argument, the $\CompactUniformFinalTriangleClosureUp$ packet records the last source-and-target triangle closure in the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity}. It sits after the epsilon-triangle handoff of \autoref{ch:concrete-instances-compact-uniform-epsilon-triangle-handoff-namecert}, the source-target triangle ledger of \autoref{ch:concrete-instances-compact-uniform-source-target-triangle-ledger-namecert}, and the metric-triangle uniform estimate of \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}. The packet names a finite compact-net, pointwise-modulus, tolerance-budget, UniformModulus, and Real-seal closure route; it is not a host compactness theorem, an external modulus selector, quotient equality, choice of hidden centers, or ambient completeness principle.

\begin{definition}[Compact-uniform final triangle closure carrier]
\label{def:compact-uniform-final-triangle-closure-carrier}
A $\CompactUniformFinalTriangleClosureUp$ carrier is a finite $\BHist$ packet
$$
F=(K,M,S,T,D,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact-net source-distance row, recording the selected finite center, the source distance from the first point to that center, and the source distance between the two compared points.
\item $M$ is the pointwise modulus row consumed at the selected center.
\item $S$ is the source triangle estimate row, keeping the second point inside the same local source ball.
\item $T$ is the target triangle estimate row, joining the two pointwise target estimates through the selected center image.
\item $D$ is the dyadic tolerance budget row, carrying the finite split of the requested tolerance among source, pointwise, and target triangle contributions.
\item $U$ is the $\UniformModulusUp$ handoff row produced after $K,M,S,T,D$ have been displayed.
\item $E$ is the $\RealUp$ seal row available only after the uniform-modulus handoff has been read.
\item $H$ records componentwise $\hsame$ transport for compact-net source distances, pointwise modulus rows, source triangle rows, target triangle rows, dyadic tolerance budgets, uniform-modulus handoffs, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from compact-net coverage through pointwise modulus, source triangle, target triangle, dyadic tolerance budget, UniformModulus handoff, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformFinalTriangleClosureUp$, $\UniformModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$, together with the compact-uniform triangle sibling rows cited above.
\item $N$ is the local $\NameCert_{\CompactUniformFinalTriangleClosureUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,S,T,D,U,E,H,C,P,N$. It has no coordinate for host compactness, a hidden center choice, quotient equality, selected subsequences, semantic continuity, or ambient completeness.
\end{definition}

\begin{theorem}[Compact-uniform final triangle closure carrier]
\label{thm:compact-uniform-final-triangle-closure-carrier}
Every accepted $\CompactUniformFinalTriangleClosureUp$ packet exposes exactly the finite carrier rows of \autoref{def:compact-uniform-final-triangle-closure-carrier}. In particular, a public read of the packet must factor through compact-net source distances, a pointwise modulus row, source and target triangle estimates, a dyadic tolerance budget, a $\UniformModulusUp$ handoff, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier. The rows $K,M,S,T,D,U,E,H,C,P,N$ are the whole public surface of the accepted packet.

The analytic rows are $K,M,S,T,D,U,E$. They record the compact-net source distances, the pointwise modulus at the selected center, the two triangle estimates, the dyadic tolerance budget, the uniform-modulus handoff, and the Real seal.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly that route. Since the carrier has no additional coordinate, no host compactness theorem, hidden center choice, quotient equality, selected subsequence, semantic continuity theorem, or ambient completeness principle can be exported.
\end{proof}

\begin{theorem}[Compact-uniform final triangle closure NameCert obligations]
\label{thm:compact-uniform-final-triangle-closure-namecert-obligations}
For every accepted $\CompactUniformFinalTriangleClosureUp$ carrier
$$
F=(K,M,S,T,D,U,E,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformFinalTriangleClosureUp}$ surface consists exactly of compact-net source-distance admission, pointwise modulus admission, source triangle admission, target triangle admission, dyadic tolerance-budget admission, $\UniformModulusUp$ handoff, $\RealUp$ seal routing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Read compact-net source-distance admission from $K$. This row fixes the selected center and the two source distances used by the source triangle estimate.

Read pointwise modulus admission from $M$ and source triangle admission from $S$. The source triangle keeps the second point inside the selected local radius, so the same pointwise modulus row may be consumed for both compared source points.

Read target triangle admission from $T$ and dyadic tolerance-budget admission from $D$. The target triangle combines the two pointwise target estimates, while the dyadic budget records the finite tolerance split that makes the estimate fit the requested bound.

Finally read the handoff and seal rows $U$ and $E$. The rows $H,C,P,N$ preserve this source-and-target triangle route under transport, replay, provenance, and local naming, and no further NameCert obligation is available.
\end{proof}

\begin{theorem}[Compact-uniform final triangle closure handoff]
\label{thm:compact-uniform-final-triangle-closure-handoff}
Every $\UniformModulusUp$ or $\RealUp$ consumer of an accepted $\CompactUniformFinalTriangleClosureUp$ packet receives only the finite route
$$
K,M,S,T,D \longmapsto U \longmapsto E,H,C,P,N .
$$
The handoff is the closure of the displayed compact-net source distance, pointwise-modulus, source-triangle, target-triangle, and dyadic-budget rows, not an external modulus selector, host compactness argument, quotient equality, selected subsequence, or ambient Real completeness theorem.
\end{theorem}
\begin{proof}
Start with the carrier theorem \autoref{thm:compact-uniform-final-triangle-closure-carrier}. It identifies $K,M,S,T,D$ as the only estimate rows available before the $\UniformModulusUp$ handoff.

Use the NameCert obligations of \autoref{thm:compact-uniform-final-triangle-closure-namecert-obligations}. They force the source triangle and target triangle to be read through the same compact-net center and the same dyadic tolerance budget.

The row $U$ is therefore a handoff from those displayed rows, and the Real seal $E$ is downstream of $U$. Transport, replay, provenance, and local naming keep the same route through $H,C,P,N$.

Since no excluded coordinate occurs in the carrier, the consumer receives a finite closure packet rather than an outside compactness or completeness principle.
\end{proof}

\closureat{CompactUniformFinalTriangleClosureUp}{seedStr}

\begin{closurestatus}{\CompactUniformFinalTriangleClosureUp}
  \constructivestory{A $\CompactUniformFinalTriangleClosureUp$ packet is generated from compact-net source-distance rows, pointwise modulus rows, source and target triangle estimates, a dyadic tolerance budget, a UniformModulus handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-final-triangle-closure-carrier}, \autoref{thm:compact-uniform-final-triangle-closure-carrier}, \autoref{thm:compact-uniform-final-triangle-closure-namecert-obligations}, and \autoref{thm:compact-uniform-final-triangle-closure-handoff} seed the finite source-and-target triangle closure route from compact-net and pointwise-modulus data to UniformModulus and Real-seal consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host compactness, external modulus selection, quotient equality, selected subsequences, semantic continuity, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net source-distance admission, pointwise modulus admission, source and target triangle admission, dyadic tolerance-budget exactness, UniformModulus handoff, RealUp seal routing, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformFinalTriangleClosureUp.}
\end{closurestatus}
