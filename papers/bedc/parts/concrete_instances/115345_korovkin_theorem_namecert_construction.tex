\chapter{A Concrete Naming Certificate for $\KorovkinTheoremUp$}
\label{ch:concrete-instances-korovkin-theorem-namecert}
\origin{human}

Following Korovkin, the $\KorovkinTheoremUp$ packet records the classical test-function criterion for positive approximation operators on a compact interval as finite BEDC rows. It consumes the compact source surface of \autoref{ch:concrete-instances-compact-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the polynomial row of \autoref{ch:concrete-instances-polynomial-namecert}, the Bernstein approximation boundary of \autoref{ch:concrete-instances-bernstein-polynomial-namecert}, the uniform-convergence ledger of \autoref{ch:concrete-instances-uniform-convergence-namecert}, and the polynomial approximation handoff of \autoref{ch:concrete-instances-weierstrass-approximation-namecert}. The packet names the finite test-function route; it is not an open-cover compactness theorem, a host norm-completeness argument, a selected infinite operator sequence, quotient equality of functions, or a proof of positivity outside the displayed rows.

\begin{definition}[Korovkin theorem carrier]
\label{def:korovkin-theorem-carrier}
A $\KorovkinTheoremUp$ carrier is a finite $\BHist$ packet
$$
K=(X,F,T,O,B,W,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the compact source row, read through $\CompactUp$.
\item $F$ is the continuous-map target row, read through $\ContinuousMapUp$.
\item $T$ is the three-test-function row, naming the constant, coordinate, and square-coordinate observations.
\item $O$ is the positive-operator row, recording the finite operator family under inspection.
\item $B$ is the Bernstein comparison row, read through $\BernsteinPolynomialUp$.
\item $W$ is the polynomial approximation handoff row, read through $\WeierstrassApproximationUp$ and $\PolynomialUp$.
\item $U$ is the uniform-convergence row, recording the common tolerance schedule for the target function.
\item $H$ records componentwise $\hsame$ transport for source, target, test, operator, Bernstein, Weierstrass, uniform-convergence, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the three test functions through the polynomial handoff and into the target uniform-convergence row.
\item $P$ is the $\Pkg$ provenance over $\KorovkinTheoremUp$, $\CompactUp$, $\ContinuousMapUp$, $\PolynomialUp$, $\BernsteinPolynomialUp$, $\UniformConvergenceUp$, $\WeierstrassApproximationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\KorovkinTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,F,T,O,B,W,U,H,C,P,N$. It has no coordinate for an ambient Banach-space norm proof, a hidden positivity theorem, a selected infinite subsequence, quotient equality of functions, or a compactness principle beyond the displayed compact source row.
\end{definition}

\begin{theorem}[Korovkin test-function sufficiency]
\label{thm:korovkin-theorem-test-function-sufficiency}
For every accepted $\KorovkinTheoremUp$ carrier $K=(X,F,T,O,B,W,U,H,C,P,N)$, a consumer may read the approximation conclusion only after the positive-operator row $O$ has been checked on the three-test-function row $T$ over the compact source row $X$. The conclusion then routes through the Bernstein row $B$, the polynomial handoff row $W$, and the continuous-map row $F$ before it reaches the uniform-convergence row $U$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:korovkin-theorem-carrier}. The source, target, test, operator, Bernstein, Weierstrass, and uniform-convergence rows are displayed as separate coordinates.

The test-function row is the only row that licenses the Korovkin read. It exposes the constant, coordinate, and square-coordinate observations before a consumer can use positivity of $O$.

The approximation route then passes through the named siblings. Bernstein supplies a finite polynomial comparison, Weierstrass supplies the polynomial approximation handoff, and the continuous-map row fixes the inspected target before the uniform-convergence ledger is read.

Thus the sufficiency claim is a finite carrier route through $T,O,B,W,F,U$ over $X$, not an ambient norm argument or an unrecorded positivity theorem.
\end{proof}

\begin{theorem}[Korovkin uniform-convergence handoff]
\label{thm:korovkin-theorem-uniform-convergence-handoff}
Every $\UniformConvergenceUp$ consumer of an accepted $\KorovkinTheoremUp$ packet must receive the displayed route
$$
X,F,T,O,B,W,U,H,C,P,N .
$$
The row $U$ may be consumed only after $T$ has exposed the three test functions, $O$ has retained positivity for the inspected family, $B$ has supplied the Bernstein comparison, and $W$ has supplied the Weierstrass--polynomial handoff.
\end{theorem}
\begin{proof}
Start with \autoref{thm:korovkin-theorem-test-function-sufficiency}. It forces the approximation conclusion through the compact source, continuous target, three-test-function, positive-operator, Bernstein, polynomial, and uniform-convergence rows.

Next read the structural rows $H,C,P,N$. They transport, replay, source, and name the same finite route, so the uniform-convergence consumer receives the full packet rather than a detached convergence assertion.

Since $U$ is reached only after $T,O,B,W$ are visible, the handoff cannot be retyped as a host supremum-norm theorem, a selected infinite operator sequence, quotient equality of maps, or a compactness principle outside the displayed $\CompactUp$ row.
\end{proof}

\begin{theorem}[Korovkin theorem NameCert obligations]
\label{thm:korovkin-theorem-namecert-obligations}
For every accepted $\KorovkinTheoremUp$ carrier $K=(X,F,T,O,B,W,U,H,C,P,N)$, the local $\NameCert_{\KorovkinTheoremUp}$ surface consists exactly of compact-source exposure through $X$, continuous-target exposure through $F$, three-test-function exposure through $T$, positive-operator admission through $O$, Bernstein comparison through $B$, polynomial approximation handoff through $W$, uniform-convergence export through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:korovkin-theorem-carrier}. The analytic rows are $X,F,T,O,B,W,U$, and the structural rows are $H,C,P,N$.

The test-function sufficiency theorem keeps $T$ and $O$ visible before approximation is read. The uniform-convergence handoff theorem keeps $B,W,U$ visible before the downstream convergence consumer may use the packet.

The transport, replay, provenance, and local naming rows preserve the same route. Since no further carrier coordinate exists, the listed rows exhaust the local NameCert obligations and exclude host compactness, hidden positivity, quotient equality, selected infinite data, and ambient completeness readings.
\end{proof}

\falsifiablePrediction{An accepted $\KorovkinTheoremUp$ consumer that reaches $\UniformConvergenceUp$ without first displaying the three-test-function row and the positive-operator row is outside the carrier surface.}

\independenceWitness{compact, continuousmap, polynomial, bernstein-polynomial, uniform-convergence, weierstrass-approximation: none is bijective to $\KorovkinTheoremUp$ because only this packet binds the compact source, continuous target, Korovkin test rows, positive-operator family, Bernstein comparison, Weierstrass polynomial handoff, uniform-convergence ledger, transport, replay, provenance, and local NameCert row into one accepted route.}

\closureat{KorovkinTheoremUp}{seedStr}

\begin{closurestatus}{\KorovkinTheoremUp}
  \constructivestory{A $\KorovkinTheoremUp$ packet is generated from compact source, continuous target, three-test-function, positive-operator, Bernstein comparison, polynomial approximation, uniform-convergence, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:korovkin-theorem-carrier}, \autoref{thm:korovkin-theorem-test-function-sufficiency}, \autoref{thm:korovkin-theorem-uniform-convergence-handoff}, and \autoref{thm:korovkin-theorem-namecert-obligations} seed the finite Korovkin approximation packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, bridge checking, arbitrary compact-space Korovkin variants, host norm-completeness arguments, or positivity outside the displayed operator row.}
  \upgradepath{Obligation closure requires checked carrier admission, test-function exactness, positive-operator retention, Bernstein and Weierstrass handoff exactness, uniform-convergence export, transport, replay, provenance, and local naming for BEDC.Derived.KorovkinTheoremUp.}
\end{closurestatus}
