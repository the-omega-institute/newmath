\chapter{A Concrete Naming Certificate for $\IsbellMrowkaPsiSpaceUp$}
\label{ch:concrete-instances-isbell-mrowka-psi-space-namecert}
\origin{human}

Following Isbell and Mrowka on almost-disjoint families and $\Psi$-spaces, the $\IsbellMrowkaPsiSpaceUp$ packet records the classical countable-row construction as finite BEDC topology data. It sits beside the indexed-open carrier of \autoref{ch:concrete-instances-topology-namecert}, the observable-open classifier of \autoref{ch:concrete-instances-sierpinski-space-namecert}, the membership discipline of \autoref{ch:concrete-instances-setlike-namecert}, and the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}. The packet names isolated natural-number rows and almost-disjoint-family point rows only through displayed neighbourhood evidence; it does not import a host powerset of $\NatUp$, a selected infinite almost-disjoint family, quotient point equality, or choice over neighbourhood tails.

\begin{definition}[Isbell--Mrowka Psi-space carrier]
\label{def:isbell-mrowka-psi-space-carrier}
An $\IsbellMrowkaPsiSpaceUp$ carrier is a finite $\BHist$ packet
$$
\Psi=(N,A,T,I,F,O,S,W,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $N$ is the isolated natural-number row, recording the displayed natural-number names whose singleton neighbourhoods are admitted.
\item $A$ is the almost-disjoint-family point row, recording the displayed family-name rows and their finite intersection witnesses without selecting an ambient infinite family.
\item $T$ is the $\TopologyUp$ admission row, exposing the indexed open rows generated by isolated singletons and cofinite-tail neighbourhoods around family points.
\item $I$ is the isolated-point exactness row: a natural-number point is open exactly through its displayed singleton row and not through a hidden host subset.
\item $F$ is the cofinite-tail neighbourhood exactness row. For a displayed family point, an accepted neighbourhood is a displayed family row together with a finite forbidden subrow of that family; the remaining tail is read only through the carried membership rows.
\item $O$ is the $\SierpinskiSpaceUp$ observable-open handoff, recording every open-membership read as a displayed truth row.
\item $S$ is the $\SetLikeUp$ membership-provenance row for natural-number membership, family membership, finite forbidden tails, and generated open rows.
\item $W$ is the $\StreamNameUp$ finite-window replay row for the inspected singleton and tail-neighbourhood requests.
\item $H$ records componentwise $\hsame$ transport for isolated rows, family rows, topology admission, exactness rows, observable truth, membership provenance, finite-window replay, package provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from point-row admission through topology rows, observable-open handoff, membership provenance, and finite-window inspection.
\item $P$ is the $\Pkg$ provenance over $\IsbellMrowkaPsiSpaceUp$, $\TopologyUp$, $\SierpinskiSpaceUp$, $\SetLikeUp$, $\StreamNameUp$, $\NatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\IsbellMrowkaPsiSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $N,A,T,I,F,O,S,W,H,C,P,L$. It has no coordinate for a host powerset topology, an unrestricted subset of $\NatUp$, a choice-selected almost-disjoint family, a quotient of points, or an external separation theorem.
\end{definition}

\begin{theorem}[Isbell--Mrowka Psi-space NameCert obligations]
\label{thm:isbell-mrowka-psi-space-namecert-obligations}
For every accepted $\IsbellMrowkaPsiSpaceUp$ carrier
$$
\Psi=(N,A,T,I,F,O,S,W,H,C,P,L),
$$
the local $\NameCert_{\IsbellMrowkaPsiSpaceUp}$ surface consists exactly of topology-row admission through $T$, isolated-point exactness through $I$, cofinite-tail neighbourhood exactness through $F$, observable-open handoff through $O$, SetLike membership provenance through $S$, StreamName finite-window replay through $W$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $L$. Natural-number rows and almost-disjoint-family point rows are admitted only through $N$ and $A$, and no obligation is stored outside the displayed tuple.
\end{theorem}
\leanchecked{BEDC.Derived.IsbellMrowkaPsiSpaceUp.IsbellMrowkaPsiSpaceTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the carrier of \autoref{def:isbell-mrowka-psi-space-carrier}. The rows $N$ and $A$ are the only point-source rows, so an accepted packet can expose either an isolated natural-number point or a family point only through those displayed histories.

The topology obligations are local. Row $T$ admits the generated open rows; row $I$ fixes singleton exactness for isolated natural-number points; and row $F$ fixes cofinite-tail neighbourhood exactness for displayed family points by carrying the finite forbidden subrow.

The observational obligations then pass through $O,S,W$. Open membership is read as a $\SierpinskiSpaceUp$ truth row, all element and tail claims are sourced by $\SetLikeUp$ membership provenance, and every inspected singleton or tail request is replayed through a finite $\StreamNameUp$ window.

Finally $H,C,P,L$ transport, replay, source, and name the same finite route. Since the classifier has no coordinate for host powersets, unrestricted subsets of $\NatUp$, choice-selected almost-disjoint families, quotient points, or ambient separation theorems, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Isbell--Mrowka Psi-space topology route]
\label{thm:isbell-mrowka-psi-space-topology-route}
Every $\TopologyUp$, $\SierpinskiSpaceUp$, $\SetLikeUp$, or $\StreamNameUp$ consumer of an accepted $\IsbellMrowkaPsiSpaceUp$ packet receives neighbourhood data only through the finite route
$$
N,A \longrightarrow T \longrightarrow I,F \longrightarrow O,S,W \longrightarrow H,C,P,L .
$$
In particular, an isolated-point read must display the singleton row $I$, and a family-point neighbourhood read must display the cofinite-tail row $F$ with its finite forbidden subrow before any observable-open or membership-facing read is exported.
\end{theorem}
\begin{proof}
Start with the point-source rows. The natural-number row $N$ and the family row $A$ determine which displayed points can be inspected; no ambient point set is available outside them.

Next read $T,I,F$. The topology admission row exposes only generated open rows, while the two exactness rows distinguish isolated singleton reads from family-point cofinite-tail reads.

Then consume $O,S,W$. These rows make each open-membership observation visible as a Sierpinski truth read, bind membership and finite forbidden tails to SetLike provenance, and replay the inspected requests through StreamName windows.

The structural rows $H,C,P,L$ preserve the same route. They add no host powerset, unrestricted almost-disjoint family, selected infinite subfamily, quotient point equality, or choice principle.
\end{proof}

\falsifiablePrediction{An accepted $\IsbellMrowkaPsiSpaceUp$ consumer is refuted if it reads an isolated singleton, a family-point cofinite-tail neighbourhood, an observable-open truth value, or a membership claim without first displaying the corresponding $N,A,T,I,F,O,S,W,H,C,P,L$ rows.}

\independenceWitness{topology, sierpinski-space, setlike, streamname: no listed sibling is bijective to $\IsbellMrowkaPsiSpaceUp$ because the packet jointly carries isolated natural-number point rows, almost-disjoint-family point rows, generated topology admission, singleton exactness, cofinite-tail neighbourhood exactness, observable-open handoff, membership provenance, finite-window replay, transport, replay, provenance, and local naming.}

\closureat{IsbellMrowkaPsiSpaceUp}{seedStr}

\begin{closurestatus}{\IsbellMrowkaPsiSpaceUp}
  \constructivestory{An $\IsbellMrowkaPsiSpaceUp$ packet is generated from isolated natural-number rows, almost-disjoint-family point rows, TopologyUp admission, singleton exactness, cofinite-tail neighbourhood exactness, SierpinskiSpaceUp observable-open handoff, SetLikeUp membership provenance, StreamNameUp finite windows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:isbell-mrowka-psi-space-carrier}, \autoref{thm:isbell-mrowka-psi-space-namecert-obligations}, and \autoref{thm:isbell-mrowka-psi-space-topology-route} seed the finite Psi-space route over isolated rows, almost-disjoint-family point rows, topology admission, observable truth, membership provenance, and finite windows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host powerset topologies, unrestricted subsets of NatUp, selected infinite almost-disjoint families, quotient point equality, ambient separation theorems, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, topology-row admission, isolated-point exactness, cofinite-tail neighbourhood exactness, observable-open handoff, membership provenance, finite-window replay, transport, replay, provenance, and local naming for BEDC.Derived.IsbellMrowkaPsiSpaceUp.}
\end{closurestatus}
