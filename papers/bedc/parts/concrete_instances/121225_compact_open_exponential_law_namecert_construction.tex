\chapter{A Concrete Naming Certificate for $\CompactOpenExponentialLawUp$}
\label{ch:concrete-instances-compact-open-exponential-law-namecert}
\origin{human}

Following Kelley, the $\CompactOpenExponentialLawUp$ packet records the classical compact-open exponential law as finite NameCert data: a continuous map out of a product is compared with its curried continuous map into a compact-open function space only through displayed compact-open subbasic rows, continuity rows, Kelleyfication boundary rows, transport, replay, provenance, and local naming. It sits after the compact-open topology surface of \autoref{ch:concrete-instances-compact-open-topology-namecert}, the Kelleyfication boundary of \autoref{ch:concrete-instances-kelleyfication-namecert}, and the continuous-map interface of \autoref{ch:concrete-instances-continuousmap-namecert}. The chapter names the finite topology-facing adjunction packet; it is not host cartesian closedness, a global mapping-space equality, selected compactly generated replacement, quotient function equality, countable choice, or Lean bridge verification.

\begin{definition}[Compact-open exponential law carrier]
\label{def:compact-open-exponential-law-carrier}
A $\CompactOpenExponentialLawUp$ carrier is a finite $\BHist$ packet
$$
L=(X,Y,Z,P,E,U,V,K,A,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X,Y,Z$ are the source, exponent, and target space rows inspected by the law.
\item $P$ is the finite product-window row, recording the displayed reads of $X\times Y$ before any curried map is exposed.
\item $E$ is the evaluation row, naming the comparison between a map $X\times Y\to Z$ and a map $X\to Z^Y$ through finite observations.
\item $U$ is the $\CompactOpenTopologyUp$ row, listing the compact-open subbasic windows used on the function-space side.
\item $V$ is the $\ContinuousMapUp$ row, recording continuity for the uncurried and curried maps only through the public continuous-map interface.
\item $K$ is the $\KelleyficationUp$ boundary row, recording the compactly generated read when the compact-open topology must be sealed before the adjunction is public.
\item $A$ is the adjunction comparison row, storing the two displayed directions of the compact-open exponential law.
\item $H$ records componentwise $\hsame$ transport for spaces, product windows, evaluation, compact-open rows, continuous-map rows, Kelleyfication rows, adjunction rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from any law-facing read through $P,E,U,V,K,A,H,Q,N$ in that order.
\item $Q$ is the $\Pkg$ provenance over $\CompactOpenExponentialLawUp$, $\CompactOpenTopologyUp$, $\KelleyficationUp$, $\ContinuousMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactOpenExponentialLawUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,Z,P,E,U,V,K,A,H,C,Q,N$. It has no coordinate for host cartesian closedness, quotient function equality, selected compactly generated replacements, global mapping-space equality, countable choice, or bridge verification.
\end{definition}

\begin{theorem}[Compact-open exponential law NameCert obligations]
\label{thm:compact-open-exponential-law-namecert-obligations}
For every accepted $\CompactOpenExponentialLawUp$ carrier
$$
L=(X,Y,Z,P,E,U,V,K,A,H,C,Q,N),
$$
the local $\NameCert_{\CompactOpenExponentialLawUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the three displayed space rows, product-window row, evaluation row, compact-open row, continuous-map row, Kelleyfication boundary row, adjunction row, transport, replay, provenance, and local naming;
\item compact-open exposure through $U$, requiring every function-space observation to be read through the compact-open subbasic rows of \autoref{ch:concrete-instances-compact-open-topology-namecert};
\item continuous-map compatibility through $V$, requiring the uncurried and curried reads to use the public continuous-map interface of \autoref{ch:concrete-instances-continuousmap-namecert};
\item Kelleyfication nonescape through $K$, requiring the compactly generated boundary of \autoref{ch:concrete-instances-kelleyfication-namecert} before a topology-facing adjunction read is exported;
\item adjunction exactness through $A$, requiring both directions of the exponential-law comparison to preserve the same product, evaluation, compact-open, continuous-map, Kelleyfication, transport, replay, provenance, and local naming rows.
\end{enumerate}
No compact-open exponential-law obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:compact-open-exponential-law-carrier}. The accepted packet exposes exactly the rows $X,Y,Z,P,E,U,V,K,A,H,C,Q,N$, so carrier admission and local naming attach to those rows and to no hidden function-space object.

The compact-open obligation is read through $U$. Its subbasic windows are the only function-space topology coordinates available to the packet, and they are already routed through \autoref{ch:concrete-instances-compact-open-topology-namecert}.

The continuity obligation is read through $V$. Both the uncurried and curried sides use the same public continuous-map interface, so the packet does not introduce host continuity or quotient equality of maps.

The Kelleyfication row $K$ is the boundary before the adjunction becomes topology-facing. It records the compactly generated read supplied by \autoref{ch:concrete-instances-kelleyfication-namecert} without selecting an external replacement.

Finally the adjunction row $A$ compares the two displayed directions and the structural rows $H,C,Q,N$ preserve that comparison under transport, replay, provenance, and local naming. Since the refused host endpoints have no carrier coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact-open exponential law Kelley adjunction]
\label{thm:compact-open-exponential-law-kelley-adjunction}
Let $L=(X,Y,Z,P,E,U,V,K,A,H,C,Q,N)$ be an accepted $\CompactOpenExponentialLawUp$ carrier. Any law-facing read that exports the compact-open exponential comparison must first pass through the finite route
$$
P \longrightarrow E \longrightarrow U \longrightarrow V \longrightarrow K \longrightarrow A .
$$
Thus the packet presents the Kelley-style adjunction only as a displayed compact-open and continuous-map route sealed by the Kelleyfication boundary; it exports no host cartesian-closed structure, global mapping-space equality, selected compactly generated replacement, quotient function equality, countable choice, or bridge credential.
\end{theorem}
\begin{proof}
Start with a law-facing consumer of the accepted carrier. By \autoref{thm:compact-open-exponential-law-namecert-obligations}, the consumer can inspect the law only through the visible rows of $L$.

The route begins with the finite product and evaluation rows $P$ and $E$. These rows display the uncurried input and the evaluation comparison before any function-space topology is available.

The compact-open and continuous-map rows come next. The row $U$ supplies the subbasic topology observations, and $V$ records that both sides of the comparison are read through the continuous-map interface.

The Kelleyfication boundary $K$ then seals the topology-facing read. Only after that boundary is present may $A$ expose the two directions of the compact-open exponential comparison.

The structural rows $H,C,Q,N$ transport, replay, source, and name the same route. Because no host cartesian closedness, global mapping-space equality, selected replacement, quotient function equality, countable-choice schedule, or bridge credential appears in the carrier, the exported adjunction is exactly the displayed route.
\end{proof}

\falsifiablePrediction{Every public $\CompactOpenExponentialLawUp$ read factors through displayed product-window, evaluation, $\CompactOpenTopologyUp$, $\ContinuousMapUp$, $\KelleyficationUp$, adjunction, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. A public read that exports host cartesian closedness, global mapping-space equality, selected compactly generated replacement, quotient function equality, countable choice, or bridge verification from this packet refutes the carrier boundary.}

\independenceWitness{compact-open-topology, kelleyfication, continuousmap: $\CompactOpenExponentialLawUp$ is not bijective to any listed sibling because it jointly carries product-window, evaluation, compact-open, continuous-map, Kelleyfication, adjunction, transport, replay, provenance, and local naming rows, while the siblings isolate topology generation, compactly generated sealing, or the continuous-map interface without the exponential-law comparison.}

\closureat{CompactOpenExponentialLawUp}{seedStr}

\begin{closurestatus}{\CompactOpenExponentialLawUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-open-exponential-law-carrier} and \autoref{thm:compact-open-exponential-law-namecert-obligations} seed the finite compact-open exponential-law packet over product windows, evaluation, compact-open topology, continuous-map compatibility, Kelleyfication boundary, adjunction, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host cartesian closedness, global mapping-space equality, selected compactly generated replacement, quotient function equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-open exposure, continuous-map compatibility, Kelleyfication nonescape, adjunction exactness, transport, replay, provenance, and local naming for BEDC.Derived.CompactOpenExponentialLawUp.}
  \constructivestory{A $\CompactOpenExponentialLawUp$ packet is generated from source, exponent, target, finite product-window, evaluation, CompactOpenTopology, ContinuousMap, Kelleyfication, adjunction comparison, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
\end{closurestatus}
