\chapter{A Concrete Naming Certificate for $\FriendlyMetricCompletionUp$}
\label{ch:concrete-instances-friendly-metric-completion-namecert}
\origin{human}

Following Bishop--Bridges constructive completion and Kelley-style separated metric language, the $\FriendlyMetricCompletionUp$ packet records the finite handoff by which a metric source can be read as completion-ready without quotienting Cauchy names or selecting representatives. It sits after the separated-reflection boundary of \autoref{ch:concrete-instances-hausdorffization-namecert}, the separated metric surface of \autoref{ch:concrete-instances-separatedmetric-namecert}, and the completion handoff of \autoref{ch:concrete-instances-metriccompletion-namecert}. Its analytic readback is the L10 finite route through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}; the packet names a friendly completion carrier, not a host completed metric space.

\begin{definition}[Friendly metric completion carrier]
\label{def:friendly-metric-completion-carrier}
A $\FriendlyMetricCompletionUp$ carrier is a finite $\BHist$ packet
$$
F=(P,S,M,C,W,R,E,H,K,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the metric-source row, including the finite distance comparisons opened by completion-facing consumers.
\item $S$ is the separatedness boundary row, recording the zero-distance identification test exposed by $\HausdorffizationUp$ before completion reads begin.
\item $M$ is the $\SeparatedMetricUp$ row obtained from the displayed boundary rather than from a host quotient.
\item $C$ is the $\MetricCompletionUp$ handoff row for downstream completion consumers.
\item $W$ is the finite $\StreamNameUp$ window row through which Cauchy observations are opened.
\item $R$ is the $\RegSeqRatUp$ readback row attached to those windows.
\item $E$ is the terminal $\RealUp$ seal for the displayed distance and Cauchy readbacks.
\item $H$ is componentwise $\hsame$ transport for $P,S,M,C,W,R,E,G,N$.
\item $K$ is displayed $\Cont$ replay of the same friendly route.
\item $G$ is the $\Pkg$ provenance over $\FriendlyMetricCompletionUp$, $\HausdorffizationUp$, $\SeparatedMetricUp$, $\MetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FriendlyMetricCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $P,S,M,C,W,R,E,H,K,G,N$. It has no coordinate for quotient Cauchy equality, selected representatives, host completion points, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Friendly metric completion NameCert obligations]
\label{thm:friendly-metric-completion-namecert-obligations}
For every accepted $\FriendlyMetricCompletionUp$ carrier $F=(P,S,M,C,W,R,E,H,K,G,N)$, the local $\NameCert_{\FriendlyMetricCompletionUp}$ surface consists exactly of metric-source admission, separatedness-boundary exposure, separated metric handoff, metric-completion handoff, finite StreamName windows, RegSeqRat readback, terminal Real sealing, componentwise transport, displayed replay, provenance, and local naming. Every completion-facing consumer must read the displayed boundary route
$$
P \longmapsto S \longmapsto M \longmapsto C \longmapsto W,R,E \longmapsto H,K,G,N
$$
before it can use the packet as completion data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:friendly-metric-completion-carrier}. The displayed rows are exactly $P,S,M,C,W,R,E,H,K,G,N$, so carrier admission supplies the local inventory.

The separatedness boundary is read before the completion handoff. The row $S$ records the zero-distance test visible through the Hausdorffization route, and $M$ is the separated metric row built from that boundary.

The completion row $C$ is downstream of $M$, while $W,R,E$ provide the finite Cauchy-window, regular-rational, and Real-seal readback. The structural rows $H,K,G,N$ transport, replay, source, and name the same finite route.

Since the carrier has no quotient, selected-representative, host-completion, choice, or ambient-completeness coordinate, the displayed route exhausts the NameCert obligations.
\end{proof}

\begin{theorem}[Friendly metric completion finite Cauchy handoff]
\label{thm:friendly-metric-completion-finite-cauchy-handoff}
Every $\MetricCompletionUp$, $\HausdorffizationUp$, $\SeparatedMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer of an accepted $\FriendlyMetricCompletionUp$ packet receives Cauchy-facing data only through the finite rows $P,S,M,C,W,R,E,H,K,G,N$. In particular, the handoff exposes the source metric row, separatedness boundary, separated metric row, completion row, finite Cauchy windows, regular rational readback, Real seal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before any completion consumer reads it.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:friendly-metric-completion-namecert-obligations}. They force every completion-facing read to pass through the source row $P$, separatedness boundary $S$, separated metric row $M$, and completion handoff $C$.

Next read the L10 finite Cauchy surface. The rows $W,R,E$ expose the finite StreamName window, RegSeqRat readback, and terminal Real seal used by the completion consumer.

Finally replay the structural rows $H,K,G,N$. They preserve the same route under componentwise $\hsame$, displayed $\Cont$, package provenance, and local naming. The carrier lacks quotient stream equality, selected representatives, host completion points, countable choice, trichotomy, and ambient $\RealUp$ completeness, so no other Cauchy handoff is available.
\end{proof}

\falsifiablePrediction{Every accepted $\FriendlyMetricCompletionUp$ read exposes a metric source row, Hausdorffization separatedness boundary, SeparatedMetric row, MetricCompletion handoff, StreamName windows, RegSeqRat readback, Real seal, hsame transport, Cont replay, Pkg provenance, and local NameCert row before any completion-facing consumer reads it.}

\independenceWitness{hausdorffization, separatedmetric, metriccompletion, streamname, regseqrat, real: $\FriendlyMetricCompletionUp$ is not identical to any listed sibling because it jointly binds the non-quotient separatedness boundary, separated metric handoff, completion handoff, finite Cauchy windows, regular rational readback, Real seal, transport, replay, provenance, and local naming.}

\closureat{FriendlyMetricCompletionUp}{seedStr}

\begin{closurestatus}{\FriendlyMetricCompletionUp}
  \constructivestory{A $\FriendlyMetricCompletionUp$ packet is generated from a metric source row, separatedness boundary, SeparatedMetric row, MetricCompletion handoff, finite StreamName windows, RegSeqRat readback, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:friendly-metric-completion-carrier}, \autoref{thm:friendly-metric-completion-namecert-obligations}, and \autoref{thm:friendly-metric-completion-finite-cauchy-handoff} seed the finite non-quotient completion-friendly route from separated metric data to completion-facing Cauchy consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient Cauchy equality, selected representatives, host completion points, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, separatedness-boundary exactness, completion-handoff exactness, finite Cauchy readback, transport, replay, provenance, and local naming for BEDC.Derived.FriendlyMetricCompletionUp.}
\end{closurestatus}
