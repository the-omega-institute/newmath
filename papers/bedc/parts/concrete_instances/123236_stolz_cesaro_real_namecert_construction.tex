\chapter{A Concrete Naming Certificate for $\StolzCesaroRealUp$}
\label{ch:concrete-instances-stolz-cesaro-real-namecert}
\origin{human}

Following Stolz, Cesaro, Hardy's summability treatment, and Bishop--Bridges constructive real names, the $\StolzCesaroRealUp$ packet records the finite L10 surface by which a Stolz--Cesaro ratio-limit read becomes a terminal $\RealUp$ seal. It sits after the theorem-level packet of \autoref{ch:concrete-instances-stolz-cesaro-theorem-namecert}, the finite quotient-tail route of \autoref{ch:concrete-instances-stolz-cesaro-namecert}, the regular Cauchy transform of \autoref{ch:concrete-instances-regular-cauchy-stolz-transform-namecert}, the Cauchy tail-compression packet of \autoref{ch:concrete-instances-stolz-cesaro-cauchy-tail-compression-namecert}, the finite stream naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names only a finite ratio-limit certificate for displayed real-sequence rows; it does not import quotient reals, selected limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Stolz-Cesaro real carrier]
\label{def:stolz-cesaro-real-carrier}
A $\StolzCesaroRealUp$ carrier is a finite $\BHist$ packet
$$
S=(X,Y,D,R,M,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the numerator real-sequence row, listing the inspected finite windows and their $\StreamNameUp$ access schedule.
\item $Y$ is the denominator real-sequence row, listing the inspected finite windows, endpoint growth witnesses, and the same $\StreamNameUp$ access schedule.
\item $D$ is the monotone denominator evidence row, recording finite nondecrease and growth checks before any ratio read is exposed.
\item $R$ is the $\RegSeqRatUp$ readback row for the numerator, denominator, and difference-ratio windows.
\item $M$ is the tail-ratio modulus row, assigning each requested dyadic tolerance a displayed tail window on which the inspected quotient differences remain inside that tolerance.
\item $T$ is the $\RealUp$ seal handoff row, reached only after $X,Y,D,R,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the numerator, denominator, monotone evidence, regular readback, modulus, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a ratio-limit request through stream windows, monotone denominator checks, regular rational readback, tail-ratio modulus use, and the terminal real seal.
\item $P$ is the $\Pkg$ provenance over $\StolzCesaroRealUp$, $\StolzCesaroTheoremUp$, $\StolzCesaroUp$, $\RegularCauchyStolzTransformUp$, $\StolzCesaroCauchyTailCompressionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\StolzCesaroRealUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient-real construction, a host completeness theorem, a selected limiting subsequence, countable choice, trichotomy, or real equality outside the displayed seal route.
\end{definition}

\begin{theorem}[Stolz-Cesaro real NameCert obligations]
\label{thm:stolz-cesaro-real-namecert-obligations}
For every accepted $\StolzCesaroRealUp$ carrier $S=(X,Y,D,R,M,T,H,C,P,N)$, the local $\NameCert_{\StolzCesaroRealUp}$ surface consists exactly of carrier admission; numerator and denominator window exposure through $X$ and $Y$; monotone denominator evidence through $D$; regular rational readback through $R$; tail-ratio modulus exposure through $M$; terminal $\RealUp$ seal handoff through $T$; componentwise transport through $H$; displayed continuation replay through $C$; package provenance through $P$; local naming through $N$; and nonescape for quotient reals, selected limits, countable choice, trichotomy, ambient $\RealUp$ completeness, and host real equality.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:stolz-cesaro-real-carrier}. The analytic rows are precisely the numerator windows $X$, denominator windows $Y$, denominator monotonicity and growth evidence $D$, regular readback $R$, tail-ratio modulus $M$, and terminal seal $T$.

The source rows are finite. The numerator and denominator are exposed through the same stream naming discipline, so a consumer can inspect only the displayed windows and cannot replace them by host sequences or quotient streams.

The denominator row $D$ is read before ratio data are available. Thus every Stolz--Cesaro ratio request is tied to finite monotone denominator evidence rather than to an ambient order-completeness argument.

The rows $R$ and $M$ then provide the regular rational and tail-window surface. They make the ratio-limit request a displayed tolerance-and-tail computation, not a selected limiting object.

Finally $T,H,C,P,N$ seal, transport, replay, source, and name the same packet. Since the carrier lacks quotient-real, selected-limit, choice, trichotomy, ambient-completeness, and detached equality coordinates, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Stolz-Cesaro real tail-ratio window]
\label{thm:stolz-cesaro-real-tail-ratio-window}
Every tolerance-facing read from an accepted $\StolzCesaroRealUp$ carrier factors through the finite route
$$
X,Y\longmapsto D\longmapsto R\longmapsto M
$$
before the terminal $\RealUp$ handoff is available. In particular, a request for a ratio-limit witness must display numerator and denominator windows, monotone denominator evidence, regular rational readback, and a tail-ratio modulus window before it can be read as a real-name boundary.
\end{theorem}
\begin{proof}
Start with the carrier projection. The tolerance-facing rows appear only after the numerator and denominator windows, and the monotone denominator row precedes the regular readback row.

Read the sibling Stolz--Cesaro surfaces in order. The theorem-level packet of \autoref{ch:concrete-instances-stolz-cesaro-theorem-namecert}, the finite quotient-tail route of \autoref{ch:concrete-instances-stolz-cesaro-namecert}, the regular Cauchy transform of \autoref{ch:concrete-instances-regular-cauchy-stolz-transform-namecert}, and the tail-compression packet of \autoref{ch:concrete-instances-stolz-cesaro-cauchy-tail-compression-namecert} all contribute rows to the same displayed route; none supplies an independent host limit.

The modulus row $M$ consumes $X,Y,D,R$ and returns a tail window for the requested dyadic tolerance. This is the only ratio-limit evidence exposed by the packet before the seal.

Therefore the tolerance-facing read factors through the displayed route and cannot bypass it by quotient reals, selected limits, countable choice, trichotomy, or ambient completeness.
\end{proof}

\begin{theorem}[Stolz-Cesaro real seal handoff]
\label{thm:stolz-cesaro-real-seal-handoff}
Every $\RealUp$ consumer of an accepted $\StolzCesaroRealUp$ carrier must receive the terminal seal through the handoff row $T$ after $X,Y,D,R,M$ have been displayed. The exported object is a Real-facing NameCert boundary for the finite Stolz--Cesaro ratio-limit route, not a proof of ambient $\RealUp$ completeness, quotient-real existence, selected limit choice, or host equality of real sequences.
\end{theorem}
\begin{proof}
Project the handoff row from \autoref{def:stolz-cesaro-real-carrier}. The row $T$ is listed downstream of the sequence windows, denominator evidence, regular readback, and tail-ratio modulus.

Use the real-name boundary of \autoref{ch:concrete-instances-real-namecert}. A Real-facing consumer may read the seal only as the endpoint of a displayed regular-name route, so $T$ cannot be detached from $R$ and $M$.

The structural rows preserve the same dependency. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ all cite the same finite carrier coordinates.

Consequently the seal handoff exports a named completion-facing boundary and nothing more. A consumer needing completeness, quotient reals, selected limits, countable choice, trichotomy, or host equality must cite a separate certificate.
\end{proof}

\closureat{StolzCesaroRealUp}{seedStr}

\begin{closurestatus}{\StolzCesaroRealUp}
  \constructivestory{A $\StolzCesaroRealUp$ packet is generated from numerator and denominator real-sequence windows, monotone denominator evidence, RegSeqRat readback, tail-ratio modulus data, a RealUp seal handoff, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:stolz-cesaro-real-carrier}, \autoref{thm:stolz-cesaro-real-namecert-obligations}, \autoref{thm:stolz-cesaro-real-tail-ratio-window}, and \autoref{thm:stolz-cesaro-real-seal-handoff} seed the finite Stolz--Cesaro real-name certificate over displayed sequence windows, monotone denominator evidence, regular readback, tail-ratio modulus data, seal handoff, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient-real construction, selected limits, countable choice, trichotomy, ambient $\RealUp$ completeness, host real equality, Lean verification, or bridge checking for $\StolzCesaroRealUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, monotone denominator exposure, regular rational readback, tail-ratio modulus exactness, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.StolzCesaroRealUp.}
\end{closurestatus}
