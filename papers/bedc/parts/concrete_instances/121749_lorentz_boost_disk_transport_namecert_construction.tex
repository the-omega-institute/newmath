\chapter{A Concrete Naming Certificate for $\LorentzBoostDiskTransportUp$}
\label{ch:concrete-instances-lorentz-boost-disk-transport-namecert}
\origin{ai}

The $\LorentzBoostDiskTransportUp$ packet realizes the reversible hyperbolic disk-transport branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits beside the Poincare disk transport surfaces of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, \autoref{ch:concrete-instances-poincare-disk-automorphism-namecert}, \autoref{ch:concrete-instances-poincare-disk-boundary-transport-namecert}, and the metric row of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}. The packet names disk coordinates, boost parameters, boundary rows, metric preservation, phase transport, provenance, and local naming; it does not assert physical spacetime, light-cone ontology, a completed Lorentz group, or an ambient smooth manifold.

\begin{definition}[Lorentz boost disk-transport carrier]
\label{def:lorentz-boost-disk-transport-carrier}
A $\LorentzBoostDiskTransportUp$ carrier is a finite $\BHist$ packet
$$
B=(D,A,U,V,M,\Phi,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the Poincare disk-coordinate row, recording the source and target disk windows.
\item $A$ is the boost-parameter row, recording the finite rapidity or disk-automorphism parameter accepted by the local classifier.
\item $U$ is the source boundary row and $V$ is the transported boundary row.
\item $M$ is the $\HyperbolicMetricUp$ comparison row used to read metric preservation along the displayed disk transport.
\item $\Phi$ is the phase-transport row, recording the reversible observer-phase readback along the boost route.
\item $O$ is the refusal row, recording the absence of physical spacetime claims, light-cone ontology, completed Lorentz group structure, and hidden smooth-manifold data.
\item $H$ records componentwise $\hsame$ transport for disk, boost, boundary, metric, phase, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from disk-coordinate exposure to boost-parameter admission, boundary transport, metric comparison, phase transport, and refusal checks.
\item $P$ is the $\Pkg$ provenance over $\LorentzBoostDiskTransportUp$, $\UnitDiskUp$, $\PoincareDiskMetricUp$, $\HyperbolicMetricUp$, disk automorphism packets, phase-transport packets, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LorentzBoostDiskTransportUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,A,U,V,M,\Phi,O,H,C,P,N$. It has no coordinate for a physical observer, completed spacetime, external Lorentz group, host differentiability theorem, or unlogged boundary ideal point.
\end{definition}

\begin{theorem}[Lorentz boost disk-transport NameCert obligations]
\label{thm:lorentz-boost-disk-transport-namecert-obligations}
For every accepted $\LorentzBoostDiskTransportUp$ carrier
$$
B=(D,A,U,V,M,\Phi,O,H,C,P,N),
$$
the local $\NameCert_{\LorentzBoostDiskTransportUp}$ surface consists exactly of disk-coordinate exposure, boost-parameter exposure, source and target boundary rows, hyperbolic metric comparison, reversible phase transport, refusal of host spacetime claims, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:lorentz-boost-disk-transport-carrier}. The disk, boost, boundary, metric, phase, refusal, and structural rows are all explicit coordinates of one finite packet.

Read the hyperbolic dependencies. The disk-coordinate row is supplied through the existing Poincare disk surfaces, and the metric comparison row is read through $\HyperbolicMetricUp$ rather than through a host smooth model.

Read the reversible-transport branch next. The boost parameter $A$ controls the displayed disk transport from $U$ to $V$, while $\Phi$ records the observer-phase readback along that same finite route.

Finally $O,H,C,P,N$ close the boundary. The refusal row blocks physical spacetime and completed-group readings, and the structural rows transport, replay, source, and name only the displayed disk-transport data.
\end{proof}

\begin{theorem}[Lorentz boost disk-transport reversible route]
\label{thm:lorentz-boost-disk-transport-reversible-route}
Every accepted $\LorentzBoostDiskTransportUp$ consumer that reads a boost-style disk transport must traverse the finite route
$$
D,A,U,V,M,\Phi,O,H,C,P,N.
$$
Thus the consumer receives displayed disk coordinates, boost parameter, source and transported boundary rows, hyperbolic metric comparison, phase transport, refusal, componentwise transport, replay, provenance, and local naming before any transport claim is served.
\end{theorem}
\begin{proof}
Start with the carrier and NameCert obligations. They expose the exact rows $D,A,U,V,M,\Phi,O,H,C,P,N$ and exclude any ambient spacetime or completed Lorentz-group coordinate.

Use the disk and metric siblings to read the geometric surface. The Poincare disk rows expose $D,U,V$, while $\HyperbolicMetricUp$ supplies the displayed metric comparison $M$ for the same transport route.

Use the boost and phase rows for reversibility. The boost parameter $A$ names the finite transport instruction, and the phase row $\Phi$ records the reversible observer-phase readback without erasing the boundary rows.

The refusal and structural rows then preserve the boundary of the packet. Therefore every consumer-facing transport read remains inside $D,A,U,V,M,\Phi,O,H,C,P,N$ and cannot be repacked as host spacetime, an external Lorentz group, or a hidden smooth-manifold claim.
\end{proof}

\falsifiablePrediction{Every accepted $\LorentzBoostDiskTransportUp$ read that serves a reversible disk-transport consumer must expose disk coordinates, boost parameter, boundary rows, HyperbolicMetric comparison, phase transport, refusal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows before the consumer is served.}

\independenceWitness{poincare-disk-phase-transport, poincare-disk-automorphism, poincare-disk-boundary-transport, hyperbolic-metric: $\LorentzBoostDiskTransportUp$ is not bijective to any listed sibling because it jointly carries boost-parameter admission, boundary transport, metric comparison, phase transport, refusal, transport, replay, provenance, and local naming in one displayed packet.}

The Lean TasteGate route for \texttt{BEDC.Derived.LorentzBoostDiskTransportUp} must eventually provide explicit $\Nontrivial\ \LorentzBoostDiskTransportUp$ and $\FieldFaithful\ \LorentzBoostDiskTransportUp$ instances preserving $D,A,U,V,M,\Phi,O,H,C,P,N$ as separate displayed fields before stronger closure is claimed.

\closureat{LorentzBoostDiskTransportUp}{seedStr}

\begin{closurestatus}{\LorentzBoostDiskTransportUp}
  \constructivestory{A $\LorentzBoostDiskTransportUp$ packet is generated from disk-coordinate rows, boost-parameter rows, boundary rows, HyperbolicMetric comparison rows, phase-transport rows, refusal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:lorentz-boost-disk-transport-carrier}, \autoref{thm:lorentz-boost-disk-transport-namecert-obligations}, and \autoref{thm:lorentz-boost-disk-transport-reversible-route} seed the reversible hyperbolic disk-transport packet over disk, boost, boundary, metric, phase, refusal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, a standard bridge, physical spacetime, light-cone ontology, a completed Lorentz group, host differentiability, or ambient smooth-manifold structure.}
  \upgradepath{Obligation closure requires checked carrier admission, disk-coordinate exposure, boost-parameter exactness, boundary transport, HyperbolicMetric comparison, phase-transport reversibility, TasteGate nontriviality, field faithfulness, refusal, transport, replay, provenance, and local naming for BEDC.Derived.LorentzBoostDiskTransportUp.}
\end{closurestatus}
