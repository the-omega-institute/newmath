\chapter{A Concrete Naming Certificate for $\CauchyFilterCompletionCriterionUp$}
\label{ch:concrete-instances-cauchy-filter-completion-criterion-namecert}
\origin{human}

Following Kelley and Bishop--Bridges, the $\CauchyFilterCompletionCriterionUp$ packet records the textbook completion criterion that a displayed Cauchy-filter route may reach a completion boundary only through finite basis, window, tolerance, readback, and real-seal data. It sits after the Cauchy-filter completion boundary of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}, the filter-limit basis route of \autoref{ch:concrete-instances-filterlimitbasis-namecert}, the Cauchy-filter surface of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the uniform-completion surface of \autoref{ch:concrete-instances-uniformcompletion-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the criterion as finite BEDC data; it does not quotient filters, select a limit, use host equality, invoke choice, or import ambient completeness.

\begin{definition}[CauchyFilterCompletionCriterion carrier]
\label{def:cauchy-filter-completion-criterion-carrier}
A $\CauchyFilterCompletionCriterionUp$ carrier is a finite $\BHist$ packet
$$
K=(Q,B,F,U,S,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the $\CauchyFilterCompletionUp$ completion-boundary row whose accepted reads remain tied to displayed Cauchy-filter data.
\item $B$ is the $\FilterLimitBasisUp$ row exposing the finite basis route used by limit and completion consumers.
\item $F$ is the $\CauchyFilterUp$ source row, including the displayed base, refinement, and Cauchy evidence read by $Q$ and $B$.
\item $U$ is the $\UniformCompletionUp$ handoff row, available only after the same filter, basis, window, tolerance, and readback route has been displayed.
\item $S$ is the $\StreamNameUp$ finite-window schedule used to inspect requested tails.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the same windows and completion requests.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the finite windows and dyadic ledger.
\item $E$ is the terminal $\RealUp$ seal row reached only after $Q,B,F,U,S,D,R$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for the completion, basis, filter, uniform, window, tolerance, readback, real-seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a completion-criterion request through $Q,B,F,U,S,D,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchyFilterCompletionCriterionUp$, $\CauchyFilterCompletionUp$, $\FilterLimitBasisUp$, $\CauchyFilterUp$, $\UniformCompletionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyFilterCompletionCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,B,F,U,S,D,R,E,H,C,P,N$. There is no coordinate for quotient filters, selected representatives, host filter equality, selected completion points, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CauchyFilterCompletionCriterion NameCert obligations]
\label{thm:cauchy-filter-completion-criterion-namecert-obligations}
For every accepted $\CauchyFilterCompletionCriterionUp$ carrier $K=(Q,B,F,U,S,D,R,E,H,C,P,N)$, the local $\NameCert_{\CauchyFilterCompletionCriterionUp}$ surface consists exactly of carrier admission, CauchyFilterCompletion boundary exposure through $Q$, FilterLimitBasis route exposure through $B$, CauchyFilter source exposure through $F$, UniformCompletion handoff exposure through $U$, finite StreamName window exposure through $S$, DyadicRatCore tolerance exactness through $D$, RegSeqRat readback through $R$, Real seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

No obligation may be discharged by quotient-filter equality, selected representatives, selected completion points, host filter equality, countable choice, hidden cofinal tails, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-filter-completion-criterion-carrier}. The displayed rows are exactly $Q,B,F,U,S,D,R,E,H,C,P,N$, so carrier admission exposes only completion-boundary, basis, Cauchy-filter, uniform-handoff, window, tolerance, readback, seal, transport, replay, provenance, and naming data.

The completion side starts with $Q,B,F$. The Cauchy-filter completion row $Q$ and the limit-basis row $B$ both read the same Cauchy-filter source row $F$, so the criterion has one finite filter route rather than a quotient or detached basis route.

Next read the uniform and numerical rows. The handoff $U$ is downstream from the displayed filter route, while $S,D,R$ give the finite windows, dyadic tolerances, and regular rational readback used before the terminal seal $E$ is available.

Finally the structural rows $H,C,P,N$ preserve the same route. Since quotient-filter equality, selected representatives, selected completion points, host filter equality, choice, hidden cofinal tails, and ambient completeness are absent from the carrier, the listed rows exhaust the local surface.
\end{proof}

\begin{theorem}[CauchyFilterCompletionCriterion basis route]
\label{thm:cauchy-filter-completion-criterion-basis-route}
Let $K=(Q,B,F,U,S,D,R,E,H,C,P,N)$ be an accepted $\CauchyFilterCompletionCriterionUp$ carrier. Every completion-facing or Real-facing read factors through
$$
\CauchyFilterUp \longmapsto \FilterLimitBasisUp \longmapsto \CauchyFilterCompletionUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The $\UniformCompletionUp$ handoff reads the same route through $U$ and cannot bypass $F,B,Q,S,D,R,E,H,C,P,N$. Thus the criterion exports finite basis evidence for the L10 completion route without quotient filters, selected limits, host equality, choice, hidden cofinal tails, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with a completion-facing or Real-facing read of $K$. The carrier exposes the source Cauchy-filter row $F$, the basis row $B$, and the completion-boundary row $Q$ before any terminal Real seal can be inspected.

Replay the displayed continuation row $C$. The read passes from $F$ to $B$ and $Q$, then through the finite window schedule $S$, dyadic tolerance ledger $D$, and regular rational readback $R$ before it reaches $E$.

The uniform handoff does not open a second completion object. The row $U$ is one of the displayed coordinates and is preserved by $H,C,P,N$, so a $\UniformCompletionUp$ consumer receives the same basis route rather than a hidden quotient, chosen representative, or ambient completion.

The excluded objects are not carrier rows. Consequently every accepted read factors through the route displayed in the statement.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyFilterCompletionCriterionUp$ read factors through $\CauchyFilterCompletionUp$, $\FilterLimitBasisUp$, $\CauchyFilterUp$, $\UniformCompletionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\closureat{CauchyFilterCompletionCriterionUp}{seedStr}

\begin{closurestatus}{\CauchyFilterCompletionCriterionUp}
  \constructivestory{A $\CauchyFilterCompletionCriterionUp$ packet is generated from a CauchyFilterCompletion row, FilterLimitBasis row, CauchyFilter source row, UniformCompletion handoff row, StreamName windows, DyadicRatCore tolerance ledger, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-filter-completion-criterion-carrier}, \autoref{thm:cauchy-filter-completion-criterion-namecert-obligations}, and \autoref{thm:cauchy-filter-completion-criterion-basis-route} seed the L10 completion criterion route over CauchyFilterCompletionUp, FilterLimitBasisUp, CauchyFilterUp, UniformCompletionUp, StreamNameUp, DyadicRatCoreUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, quotient filters, selected limits, host equality, countable choice, hidden cofinal tails, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, CauchyFilterCompletion boundary exposure, FilterLimitBasis route exposure, UniformCompletion handoff nonescape, readback exactness, Real seal nonescape, and TasteGate field faithfulness for BEDC.Derived.CauchyFilterCompletionCriterionUp.}
\end{closurestatus}
