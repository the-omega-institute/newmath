\chapter{A Concrete Naming Certificate for $\FanModulusRealCompletionBoundaryUp$}
\label{ch:concrete-instances-fan-modulus-real-completion-boundary-namecert}
\origin{human}

Following Bishop and Bridges and Troelstra on fan bars, regular sequences, and constructive real completion, the $\FanModulusRealCompletionBoundaryUp$ packet records the finite point where a fan-modulus depth ledger becomes a Real-completion boundary certificate. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the fan/bar sibling route of \autoref{ch:concrete-instances-bishop-fan-modulus-namecert}. The packet names only finite windows, bar-depth bounds, dyadic tolerances, regular readbacks, Real seals, structural transport, replay, provenance, and local naming.

\begin{definition}[Fan-modulus Real-completion boundary carrier]
\label{def:fan-modulus-real-completion-boundary-carrier}
A $\FanModulusRealCompletionBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(F,D,S,Q,R,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the fan/bar modulus row, read through $\FanTheoremUp$ and $\BishopFanModulusUp$, recording the finite branch-depth bound available to the boundary.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to that same depth.
\item $S$ is the $\StreamNameUp$ window schedule whose entries are inspected only up to the displayed finite depth.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback obtained from those windows.
\item $R$ is the terminal $\RealUp$ seal boundary reached after $F,D,S,Q$ have been displayed.
\item $W$ is the Real-completion boundary row, recording which downstream completion consumer may read the sealed finite window without selecting a limit.
\item $H$ records componentwise $\hsame$ transport for the fan/bar, dyadic, stream, regular-readback, Real-seal, boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a consumer request through $F,D,S,Q,R,W,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FanModulusRealCompletionBoundaryUp$, $\FanTheoremUp$, $\BishopFanModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FanModulusRealCompletionBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $F,D,S,Q,R,W,H,C,P,N$. It has no coordinate for open-cover compactness, countable choice, selected branches, selected limits, quotient stream equality, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Fan-modulus Real-completion boundary stability]
\label{thm:fan-modulus-real-completion-boundary-stability}
Let $B=(F,D,S,Q,R,W,H,C,P,N)$ and $B'=(F',D',S',Q',R',W',H',C',P',N')$ be accepted $\FanModulusRealCompletionBoundaryUp$ carriers related by the componentwise $\hsame$ transport recorded in $H$ and $H'$. Then every Real-completion boundary read through $B$ is transported to the corresponding read through $B'$ with the same finite fan/bar depth, dyadic tolerance ledger, StreamName window schedule, RegSeqRat readback, Real seal, and boundary row.
\end{theorem}
\begin{proof}
Project the two accepted carriers. Their analytic rows are exactly $F,D,S,Q,R,W$ and $F',D',S',Q',R',W'$.

The componentwise $\hsame$ rows $H$ and $H'$ identify the fan/bar depth, dyadic tolerances, stream windows, regular rational readbacks, Real seals, and boundary rows coordinate by coordinate. Displayed $\Cont$ replay then preserves the same route for any downstream completion consumer.

The provenance and local naming rows $P,N$ and $P',N'$ record the same finite dependency list. Hence transport cannot introduce selected limits, quotient streams, countable-choice schedules, open-cover compactness, or ambient Real completeness.
\end{proof}

\begin{theorem}[Fan-modulus Real-completion boundary NameCert obligations]
\label{thm:fan-modulus-real-completion-boundary-namecert-obligations}
For every accepted $\FanModulusRealCompletionBoundaryUp$ carrier $B=(F,D,S,Q,R,W,H,C,P,N)$, the local $\NameCert_{\FanModulusRealCompletionBoundaryUp}$ surface consists exactly of carrier admission for the displayed rows, fan/bar depth admission through $F$, dyadic tolerance admission through $D$, finite StreamName window admission through $S$, RegSeqRat readback through $Q$, Real seal admission through $R$, Real-completion boundary admission through $W$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:fan-modulus-real-completion-boundary-carrier}. The projected packet contains exactly $F,D,S,Q,R,W,H,C,P,N$.

The fan/bar row $F$ bounds the branch-depth surface before any analytic read occurs. The dyadic, stream, and regular rational rows $D,S,Q$ then give the finite tolerance and readback data that the Real seal may inspect.

The terminal rows $R$ and $W$ expose only the sealed Real boundary and the downstream completion boundary. Finally $H,C,P,N$ transport, replay, source, and name that same finite route, so the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Fan-modulus Real-completion boundary L10 consumption]
\label{thm:fan-modulus-real-completion-boundary-l10-consumption}
Every L10 Real-completion consumer of an accepted $\FanModulusRealCompletionBoundaryUp$ carrier factors through
$$
\FanTheoremUp,\BishopFanModulusUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp \longmapsto W .
$$
The consumer therefore receives a finite fan/bar modulus, dyadic tolerances, finite stream windows, regular rational readback, a terminal Real seal, and a displayed completion boundary row before any completion-facing read is exported.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:fan-modulus-real-completion-boundary-carrier}. The consumer can inspect the boundary row $W$ only after the preceding rows $F,D,S,Q,R$ have been displayed.

The fan/bar row $F$ supplies the finite modulus through the sibling route of \autoref{ch:concrete-instances-bishop-fan-modulus-namecert}. The tolerance and observation rows $D,S,Q$ then pass through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-regseqrat-namecert}.

Only after those finite windows are visible may the Real seal of \autoref{ch:concrete-instances-real-namecert} and the boundary row $W$ be read. Since no carrier coordinate stores open-cover compactness, selected limits, quotient stream equality, countable choice, or ambient $\RealUp$ completeness, the L10 consumer receives only the displayed finite boundary.
\end{proof}

\falsifiablePrediction{Every accepted FanModulusRealCompletionBoundaryUp L10 read displays fan/bar depth, DyadicRatCore tolerance, StreamName windows, RegSeqRat readback, a Real seal, Real-completion boundary, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{bishop-fan-modulus, dyadicratcore, streamname, regseqrat, real: $\FanModulusRealCompletionBoundaryUp$ is not bijective to any listed sibling because it jointly carries fan/bar depth, dyadic tolerance, finite stream scheduling, regular rational readback, Real sealing, and a Real-completion boundary row while refusing compactness, choice, selected limits, quotient equality, host equality, and ambient completeness.}

\closureat{FanModulusRealCompletionBoundaryUp}{seedStr}
\begin{closurestatus}{\FanModulusRealCompletionBoundaryUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fan-modulus-real-completion-boundary-carrier}, \autoref{thm:fan-modulus-real-completion-boundary-stability}, \autoref{thm:fan-modulus-real-completion-boundary-namecert-obligations}, and \autoref{thm:fan-modulus-real-completion-boundary-l10-consumption} seed the finite fan-modulus Real-completion boundary through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and fan/bar sibling rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient $\RealUp$ completeness, selected limits, open-cover compactness, countable choice, quotient stream equality, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, fan/bar depth exactness, dyadic tolerance coverage, stream-window coverage, regular-readback coverage, Real seal admission, completion-boundary admission, transport, replay, provenance, and local naming for BEDC.Derived.FanModulusRealCompletionBoundaryUp.}
  \constructivestory{A $\FanModulusRealCompletionBoundaryUp$ packet is generated from a finite fan/bar modulus row, a DyadicRatCore tolerance row, StreamName windows, RegSeqRat readback, a Real seal, a Real-completion boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
\end{closurestatus}
