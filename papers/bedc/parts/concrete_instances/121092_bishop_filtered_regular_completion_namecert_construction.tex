\chapter{A Concrete Naming Certificate for $\BishopFilteredRegularCompletionUp$}
\label{ch:concrete-instances-bishop-filtered-regular-completion-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names and filter-base completion, the $\BishopFilteredRegularCompletionUp$ packet binds the regular Cauchy completion route to a displayed filter-base completion route through one finite window-induction carrier. It sits after the regular Cauchy completion surface of \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert}, the located filter-base packet of \autoref{ch:concrete-instances-bishop-located-filter-base-namecert}, the Cauchy filter-base surface of \autoref{ch:concrete-instances-cauchy-filter-base-namecert}, the finite stream-name route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records a finite handoff between regular Cauchy and filter-base completion readings; it does not assert quotient equality of filters or streams, select a limit, invoke countable choice, or claim ambient completeness of $\RealUp$.

\begin{definition}[Bishop filtered regular completion carrier]
\label{def:bishop-filtered-regular-completion-carrier}
A $\BishopFilteredRegularCompletionUp$ carrier is a finite $\BHist$ packet
$$
F=(B,R,W,D,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the filter-base row, exposing the directed base element and the located refinement relation consumed from $\BishopLocatedFilterBaseUp$ and $\CauchyFilterBaseUp$.
\item $R$ is the regular Cauchy readback row, reading the same request through $\BishopRegularCauchyCompletionUp$, $\StreamNameUp$, and $\RegSeqRatUp$.
\item $W$ is the finite tail-window row, recording the window in which the filter-base and regular Cauchy readings are compared.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the displayed dyadic radius, slack, and containment comparisons attached to $W$.
\item $A$ is the terminal $\RealUp$ seal row, visible only after $B,R,W,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the filter-base, regular readback, window, tolerance, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion request through $B,R,W,D,A,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopFilteredRegularCompletionUp$, $\BishopRegularCauchyCompletionUp$, $\BishopLocatedFilterBaseUp$, $\CauchyFilterBaseUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopFilteredRegularCompletionUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient filters, quotient streams, selected limits, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop filtered regular completion obligations]
\label{thm:bishop-filtered-regular-completion-obligations}
For every accepted $\BishopFilteredRegularCompletionUp$ carrier $F=(B,R,W,D,A,H,C,P,N)$, the local $\NameCert_{\BishopFilteredRegularCompletionUp}$ surface consists exactly of carrier admission for the displayed tuple, filter-base exposure through $B$, regular Cauchy readback through $R$, finite tail-window synchronization through $W$, dyadic tolerance exposure through $D$, terminal $\RealUp$ sealing through $A$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, local naming through $N$, and nonescape excluding every unlisted completion object.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-filtered-regular-completion-carrier}. The filter-base side is the row $B$, while the regular Cauchy side is the row $R$.

The finite comparison surface is $W,D$. The window row $W$ fixes the tail segment shared by the two readings, and the dyadic tolerance row $D$ records the finite radius and containment budget used inside that segment.

The terminal Real read is downstream of $B,R,W,D$. The rows $H,C,P,N$ preserve, replay, source, and name the same packet, so no quotient filter, quotient stream, selected limit, choice schedule, host real equality, or ambient completeness datum can enter the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop filtered regular completion filter-window induction]
\label{thm:bishop-filtered-regular-completion-filter-window-induction}
Every accepted $\BishopFilteredRegularCompletionUp$ completion read is obtained by finite induction on the displayed tail-window row $W$. The base window reads the filter-base row $B$ and the regular Cauchy row $R$ at the first displayed dyadic tolerance in $D$. The induction step refines the directed base element and the regular Cauchy tail inside the next displayed window while preserving the same $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. The terminal $\RealUp$ seal $A$ is exposed only after this finite window induction has synchronized $B,R,W,D$.
\end{theorem}
\begin{proof}
Use the obligation theorem to identify the rows available to the induction. The base step reads the first displayed window in $W$, then inspects the filter-base row $B$, the regular Cauchy readback row $R$, and the dyadic tolerance row $D$ for that window.

For the step, refine along the directed filter-base relation recorded in $B$ and along the regular Cauchy tail readback recorded in $R$. The next window is still a row of $W$, so the dyadic comparisons remain in $D$ and the route does not leave the finite carrier.

The structural rows $H,C,P,N$ transport, replay, source, and name each induction step componentwise. After the final displayed window has been synchronized, the Real seal row $A$ may be read; before that point no Real-facing completion consumer is exposed.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopFilteredRegularCompletionUp$ read displays a filter-base row, regular Cauchy readback row, finite tail-window row, DyadicRatCoreUp tolerance row, RealUp seal row, hsame transport, Cont replay, Pkg provenance, and local NameCert row before any completion-facing consumer can inspect the terminal Real seal.}

\independenceWitness{bishop-regular-cauchy-completion, bishop-located-filter-base, cauchy-filter-base, streamname, regseqrat, dyadicratcore, real: $\BishopFilteredRegularCompletionUp$ is not bijective to any listed sibling because it jointly binds filter-base refinement, regular Cauchy readback, finite tail-window synchronization, dyadic tolerance, Real sealing, transport, replay, provenance, and local naming.}

\closureat{BishopFilteredRegularCompletionUp}{seedStr}

\begin{closurestatus}{\BishopFilteredRegularCompletionUp}
  \constructivestory{A $\BishopFilteredRegularCompletionUp$ packet is generated from a filter-base row, regular Cauchy readback row, finite tail-window row, DyadicRatCoreUp tolerance row, RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-filtered-regular-completion-carrier}, \autoref{thm:bishop-filtered-regular-completion-obligations}, and \autoref{thm:bishop-filtered-regular-completion-filter-window-induction} seed the finite filter-compatible regular completion packet over filter bases, regular Cauchy readbacks, tail windows, dyadic tolerances, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient filters, quotient streams, selected limits, countable choice, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, filter-base exposure, regular Cauchy readback, finite window synchronization, dyadic tolerance stability, Real seal nonescape, replay stability, provenance, and local naming for BEDC.Derived.BishopFilteredRegularCompletionUp.}
\end{closurestatus}
