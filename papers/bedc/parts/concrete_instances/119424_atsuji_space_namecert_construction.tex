\chapter{A Concrete Naming Certificate for $\AtsujiSpaceUp$}
\label{ch:concrete-instances-atsuji-space-namecert}
\origin{human}

Following Atsuji's uniformly continuous metric spaces and the Bourbaki--Kelley treatment of metric continuity, the $\AtsujiSpaceUp$ packet records the finite BEDC surface on which pointwise continuous maps from a displayed metric source may be handed to uniform-modulus consumers. It sits over the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the compact-metric comparison source of \autoref{ch:concrete-instances-compactmetric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the Atsuji condition as displayed finite rows; it does not assert open-cover compactness, select a modulus, quotient maps, invoke countable choice, or import ambient $\RealUp$ completeness.

\begin{definition}[AtsujiSpace carrier]
\label{def:atsuji-space-carrier}
An $\AtsujiSpaceUp$ carrier is a finite $\BHist$ packet
$$
A=(M,F,U,K,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row whose accepted distance and neighbourhood reads are those of \autoref{ch:concrete-instances-metric-namecert}.
\item $F$ is the $\ContinuousMapUp$ testing row: every admitted map from the source is read through the displayed graph and pointwise continuity data of \autoref{ch:concrete-instances-continuousmap-namecert}.
\item $U$ is the $\UniformModulusUp$ handoff row exposed only after the same metric and continuous-map rows have been replayed.
\item $K$ is the compact-metric comparison row, allowing compact-metric Atsuji examples to read the sibling route of \autoref{ch:concrete-instances-compactmetric-namecert} without making compactness part of the carrier.
\item $R$ is the terminal $\RealUp$ tolerance and distance-seal row used by metric, continuity, and modulus reads.
\item $H$ records componentwise $\hsame$ transport for the metric, continuous-map, uniform-modulus, compact-comparison, real-seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from an Atsuji request through $M,F,U,K,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\AtsujiSpaceUp$, $\MetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\CompactMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AtsujiSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,F,U,K,R,H,C,P,N$. There is no coordinate for open-cover compactness, a chosen modulus, quotient map equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.AtsujiSpaceUp.AtsujiSpaceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[AtsujiSpace NameCert obligations]
\label{thm:atsuji-space-namecert-obligations}
For every accepted $\AtsujiSpaceUp$ carrier $A=(M,F,U,K,R,H,C,P,N)$, the local $\NameCert_{\AtsujiSpaceUp}$ surface consists exactly of carrier admission, metric-source exposure through $M$, continuous-map testing through $F$, uniform-modulus handoff through $U$, compact-metric comparison through $K$, real tolerance sealing through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

No obligation may be discharged by open-cover compactness, a selected modulus, quotient maps, countable choice, a hidden host continuity theorem, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:atsuji-space-carrier}. The displayed rows are exactly $M,F,U,K,R,H,C,P,N$, so carrier admission exposes only metric source data, pointwise continuous-map tests, a uniform-modulus handoff, compact-metric comparison, real tolerance sealing, transport, replay, provenance, and naming.

The continuity side starts with $M$ and $F$. A map is not accepted as a host function; it is read through the metric source and the displayed continuous-map row before any downstream uniform claim is visible.

Next read the handoff and comparison rows. The row $U$ is available only inside the same replay route, while $K$ records the compact-metric sibling comparison without adding open-cover compactness to the Atsuji carrier.

Finally the structural rows $H,C,P,N$ preserve the same packet. Since selected moduli, quotient maps, countable choice, host continuity theorems, and ambient completeness are absent from the carrier, the listed rows exhaust the local surface.
\end{proof}

\begin{theorem}[AtsujiSpace uniform-continuity handoff]
\label{thm:atsuji-space-uniform-continuity-handoff}
Let $A=(M,F,U,K,R,H,C,P,N)$ be an accepted $\AtsujiSpaceUp$ carrier. Every consumer that reads an admitted pointwise continuous map from the metric source as uniformly controlled must factor through
$$
\MetricUp \longmapsto \ContinuousMapUp \longmapsto \UniformModulusUp
$$
with the tolerance seal supplied through $\RealUp$ and the compact-metric comparison row available only as $K$.

Thus $\AtsujiSpaceUp$ exposes the standard metric-continuity to uniform-modulus handoff as finite NameCert data, but it does not export open-cover compactness, a selected modulus, quotient map equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a consumer that asks for uniform control of an admitted map. The carrier exposes the metric source row $M$ and the continuous-map row $F$ before the uniform-modulus handoff row $U$ can be inspected.

Replay the displayed continuation row $C$. The request passes through $M,F,U$ and then uses $R$ as the real tolerance seal, while $H,P,N$ keep the transported packet, provenance, and local name fixed.

The compact-metric comparison row $K$ does not turn the argument into a compactness theorem. It only records that compact-metric examples may be compared through the sibling compact-metric surface while the Atsuji handoff remains the route $M,F,U,R,H,C,P,N$.

Because no selected modulus, quotient map, choice functional, open-cover cover witness, or ambient completeness row occurs in $A$, every accepted uniform-continuity handoff factors through the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\AtsujiSpaceUp$ read factors through $\MetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\CompactMetricUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before it is consumed as a metric-continuity to uniform-modulus handoff.}

\closureat{AtsujiSpaceUp}{seedStr}

\begin{closurestatus}{\AtsujiSpaceUp}
  \constructivestory{An $\AtsujiSpaceUp$ packet is generated from a Metric source row, ContinuousMap testing row, UniformModulus handoff row, CompactMetric comparison row, Real tolerance seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:atsuji-space-carrier}, \autoref{thm:atsuji-space-namecert-obligations}, and \autoref{thm:atsuji-space-uniform-continuity-handoff} seed the L10 Atsuji route over MetricUp, ContinuousMapUp, UniformModulusUp, CompactMetricUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, selected moduli, quotient maps, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, metric-source exposure, continuous-map testing, uniform-modulus handoff nonescape, compact-metric comparison scope, real tolerance sealing, and TasteGate field faithfulness for BEDC.Derived.AtsujiSpaceUp.}
\end{closurestatus}
