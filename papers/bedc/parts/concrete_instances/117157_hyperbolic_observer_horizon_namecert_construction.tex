\chapter{A Concrete Naming Certificate for $\HyperbolicObserverHorizonUp$}
\label{ch:concrete-instances-hyperbolic-observer-horizon-namecert}
\origin{ai}

The $\HyperbolicObserverHorizonUp$ packet realizes the observer-horizon branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits over the visual boundary surface of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the Gromov-product comparison route of \autoref{ch:concrete-instances-hyperbolic-gromov-product-namecert}, the Busemann cocycle route of \autoref{ch:concrete-instances-hyperbolic-busemann-cocycle-namecert}, the horoball-shadow row of \autoref{ch:concrete-instances-hyperbolic-horoball-shadow-namecert}, observer-window sources such as \autoref{ch:concrete-instances-hyperbolic-observer-shadow-metric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite horizon-facing route; it does not assert a global boundary compactification, a completed spacetime horizon, a selected geodesic ray, quotient boundary equality, or ambient hyperbolic completeness.

\begin{definition}[Hyperbolic observer-horizon carrier]
\label{def:hyperbolic-observer-horizon-carrier}
A $\HyperbolicObserverHorizonUp$ carrier is a finite $\BHist$ packet
$$
O=(B,G,U,S,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\HyperbolicVisualBoundaryUp$ row naming the visible boundary window under inspection.
\item $G$ is the $\HyperbolicGromovProductUp$ comparison row for the same observer-side basepoint and boundary window.
\item $U$ is the $\HyperbolicBusemannCocycleUp$ cocycle row used to orient the boundary-facing displacement read.
\item $S$ is the $\HyperbolicHoroballShadowUp$ row naming the horoball-shadow window that a horizon consumer may inspect.
\item $W$ is the observer-window row, read through the observer-shadow or observer-expansion surface, listing the finite records visible to the focused observer.
\item $R$ is the regular readback and terminal $\RealUp$ seal row for horizon depth, shadow threshold, or visual-radius comparisons.
\item $E$ is the refusal row excluding interior far-end selection, quotient boundary equality, global compactification, and completed spacetime-horizon semantics.
\item $H$ records componentwise $\hsame$ transport for the boundary, product, cocycle, horoball-shadow, observer-window, Real-seal, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from observer-window inspection through boundary, product, cocycle, horoball-shadow, Real-seal, and refusal rows.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverHorizonUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicGromovProductUp$, $\HyperbolicBusemannCocycleUp$, $\HyperbolicHoroballShadowUp$, observer-window rows, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicObserverHorizonUp}$ row.
\end{enumerate}
The carrier has no coordinate for a global compactification object, selected geodesic ray, quotient boundary equality, completed spacetime horizon, hidden observer synchronization, or ambient hyperbolic completeness.
\end{definition}

\begin{theorem}[Hyperbolic observer-horizon NameCert obligations]
\label{thm:hyperbolic-observer-horizon-namecert-obligations}
For every accepted $\HyperbolicObserverHorizonUp$ carrier $O=(B,G,U,S,W,R,E,H,C,P,N)$, the local $\NameCert_{\HyperbolicObserverHorizonUp}$ surface consists exactly of carrier admission for the visual boundary, Gromov-product, Busemann-cocycle, horoball-shadow, observer-window, Real-seal, refusal, transport, replay, provenance, and local naming rows; boundary exposure through $B$; product comparison through $G$; cocycle orientation through $U$; shadow exposure through $S$; observer-window accountability through $W$; Real-seal readback through $R$; refusal exactness through $E$; and componentwise stability through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-observer-horizon-carrier}. The horizon-facing rows are exactly $B,G,U,S,W,R,E$, and the structural rows are $H,C,P,N$.

The boundary route is visible before any observer-horizon read is public. The visual boundary row $B$, Gromov product row $G$, Busemann cocycle row $U$, and horoball-shadow row $S$ fix the boundary, comparison, orientation, and shadow data.

The observer and Real-facing rows are then $W,R,E$. They expose the finite observer window, seal the requested real comparison, and refuse the excluded global or quotient objects. The structural rows transport and replay only those displayed rows, so the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic observer-horizon boundary handoff]
\label{thm:hyperbolic-observer-horizon-boundary-handoff}
Every horizon-facing consumer of an accepted $\HyperbolicObserverHorizonUp$ packet must first read
$$
B,G,U,S \longmapsto W,R,E \longmapsto H,C,P,N .
$$
The consumer receives visual boundary, Gromov-product, Busemann-cocycle, horoball-shadow, observer-window, Real-seal, refusal, transport, replay, provenance, and local naming rows. It receives no selected geodesic ray, quotient boundary equality, global boundary compactification, completed spacetime-horizon object, hidden synchronized observer coordinate, or ambient hyperbolic completeness principle.
\end{theorem}
\begin{proof}
Use \autoref{thm:hyperbolic-observer-horizon-namecert-obligations}. It places the visual boundary, product comparison, Busemann cocycle, and horoball-shadow rows before any observer-window or Real-seal consumer can read the horizon packet.

The observer read is finite. The row $W$ lists the inspected observer records, $R$ seals the real comparison requested by that read, and $E$ records the refusal of interior far-end selection and quotient boundary equality.

Finally $H,C,P,N$ preserve the same finite route. Since the carrier contains no global compactification, selected ray, quotient equality, synchronized observer coordinate, completed horizon, or ambient completeness row, no consumer can obtain one from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicObserverHorizonUp$ read must expose $\HyperbolicVisualBoundaryUp$, $\HyperbolicGromovProductUp$, $\HyperbolicBusemannCocycleUp$, $\HyperbolicHoroballShadowUp$, observer-window, terminal $\RealUp$ seal, refusal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any horizon-facing conclusion is visible.}

\independenceWitness{hyperbolic-visual-boundary, hyperbolic-gromov-product, hyperbolic-busemann-cocycle, hyperbolic-horoball-shadow, hyperbolic-observer-shadow-metric: none is bijective to $\HyperbolicObserverHorizonUp$ because the horizon packet jointly binds boundary readback, product comparison, Busemann orientation, horoball-shadow exposure, observer-window accountability, Real sealing, refusal, transport, replay, provenance, and local naming.}

The Lean follow-up for this packet must provide explicit $\mathsf{Nontrivial}\ \HyperbolicObserverHorizonUp$ and $\mathsf{FieldFaithful}\ \HyperbolicObserverHorizonUp$ TasteGate instances. Those instances must keep $B,G,U,S,W,R,E,H,C,P,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{HyperbolicObserverHorizonUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverHorizonUp}
  \constructivestory{A $\HyperbolicObserverHorizonUp$ packet is generated from a HyperbolicVisualBoundary row, a HyperbolicGromovProduct comparison row, a HyperbolicBusemannCocycle orientation row, a HyperbolicHoroballShadow window, an observer-window row, a RealUp seal, a refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-horizon-carrier}, \autoref{thm:hyperbolic-observer-horizon-namecert-obligations}, and \autoref{thm:hyperbolic-observer-horizon-boundary-handoff} seed the finite observer-horizon route over visual boundary, Gromov product, Busemann cocycle, horoball shadow, observer-window, Real-seal, and refusal rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, global boundary compactification, selected geodesic rays, quotient boundary equality, completed spacetime horizons, synchronized observer coordinates, or ambient hyperbolic completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary exposure, product comparison, Busemann orientation, horoball-shadow exposure, observer-window accountability, Real-seal readback, refusal exactness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicObserverHorizonUp.}
\end{closurestatus}
