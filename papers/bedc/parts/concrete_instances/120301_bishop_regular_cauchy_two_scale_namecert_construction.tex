\chapter{A Concrete Naming Certificate for $\BishopRegularCauchyTwoScaleUp$}
\label{ch:concrete-instances-bishop-regular-cauchy-two-scale-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names and explicit error estimates, the $\BishopRegularCauchyTwoScaleUp$ packet records the finite two-scale estimate used when a coarse regular-Cauchy window is refined by a smaller inspection scale. It sits beside the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the finite-window induction route of \autoref{ch:concrete-instances-regular-cauchy-window-induction-namecert}. The packet names a displayed induction object for two-scale Cauchy estimates; it is not a selected limit, quotient stream equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop regular Cauchy two-scale carrier]
\label{def:bishop-regular-cauchy-two-scale-carrier}
A $\BishopRegularCauchyTwoScaleUp$ carrier is a finite $\BHist$ packet
$$
T=(S,K,k,W_0,W_1,Q_0,Q_1,D,I,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the regular-Cauchy source schedule whose estimates are inspected at two displayed scales.
\item $K$ is the coarse scale row and $k$ is the refined scale row, with the classifier requiring the refined request to be read below the coarse request through dyadic tolerance data.
\item $W_0$ and $W_1$ are the $\StreamNameUp$ window rows inspected at the two scales.
\item $Q_0$ and $Q_1$ are the corresponding $\RegSeqRatUp$ readback rows for those windows.
\item $D$ is the $\DyadicRatCoreUp$ ledger storing the denominator budgets, slack rows, and two-scale comparison inequalities.
\item $I$ is the $\RegularCauchyWindowInductionUp$ row that records the finite induction step from the coarse window to the refined window.
\item $E$ is the terminal $\RealUp$ seal reached only after $S,K,k,W_0,W_1,Q_0,Q_1,D,I$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the source schedule, both scale rows, both window rows, both readback rows, the dyadic ledger, the induction row, the real seal, provenance, and local naming.
\item $C$ stores displayed $\Cont$ replay from a finite precision request through the same rows.
\item $P$ is the $\Pkg$ provenance over $\BishopRegularCauchyTwoScaleUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RegularCauchyWindowInductionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegularCauchyTwoScaleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a completed real selected outside the displayed windows, quotient equality of streams, host real equality, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Bishop regular Cauchy two-scale obligations]
\label{thm:bishop-regular-cauchy-two-scale-obligations}
For every accepted $\BishopRegularCauchyTwoScaleUp$ carrier
$$
T=(S,K,k,W_0,W_1,Q_0,Q_1,D,I,E,H,C,P,N),
$$
the local $\NameCert_{\BishopRegularCauchyTwoScaleUp}$ surface consists exactly of carrier admission for the displayed source schedule, the two scale rows, the two $\StreamNameUp$ windows, the two $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ tolerance ledger, the $\RegularCauchyWindowInductionUp$ step row, the $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local name.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:bishop-regular-cauchy-two-scale-carrier}. The rows $S,K,k,W_0,W_1,Q_0,Q_1,D,I,E,H,C,P,N$ are the complete finite packet, so carrier admission and local naming are componentwise projections.

Read the two scales next. The coarse row $K$ and refined row $k$ are compared only through the dyadic tolerance ledger $D$, and their inspected windows are exactly $W_0,W_1$ with readbacks $Q_0,Q_1$. Thus the estimate is a displayed finite relation between two windows, not a statement about an unshown completed stream.

Finally use the induction and structural rows. The row $I$ supplies the finite-window step from \autoref{ch:concrete-instances-regular-cauchy-window-induction-namecert}, while $H,C,P,N$ transport, replay, source, and name the same displayed data. Since $E$ is reached only after those rows are present, the certificate cannot export a selected limit, quotient stream equality, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{An accepted $\BishopRegularCauchyTwoScaleUp$ consumer that reaches a $\RealUp$ seal without reading $S,K,k,W_0,W_1,Q_0,Q_1,D,I,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, regular-cauchy-window-induction: no listed sibling supplies the joint packet containing one regular-Cauchy source schedule, two displayed scale rows, two finite window rows, two regular-rational readback rows, a dyadic two-scale tolerance ledger, a finite-window induction row, a Real seal, transport, replay, provenance, and local NameCert row.}

\closureat{BishopRegularCauchyTwoScaleUp}{seedStr}

\begin{closurestatus}{\BishopRegularCauchyTwoScaleUp}
  \constructivestory{A $\BishopRegularCauchyTwoScaleUp$ packet is generated from a regular-Cauchy source schedule, coarse and refined scale rows, paired StreamName windows, paired RegSeqRat readbacks, a DyadicRatCore tolerance ledger, a RegularCauchyWindowInduction row, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-cauchy-two-scale-carrier} and \autoref{thm:bishop-regular-cauchy-two-scale-obligations} seed the finite two-scale Cauchy estimate route through displayed stream windows, regular-rational readback, dyadic tolerance rows, window induction, and the terminal Real-facing seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean theorem verification, bridge checking, Real completeness, quotient stream equality, selected limits, host real equality, or countable choice.}
  \upgradepath{Obligation closure requires checked carrier admission, two-scale dyadic comparison, paired window readback, finite-window induction replay, transport, provenance, and local naming for BEDC.Derived.BishopRegularCauchyTwoScaleUp.}
\end{closurestatus}
