\chapter{A Concrete Naming Certificate for $\RegSeqObservationBudgetUp$}
\label{ch:concrete-instances-regseqobservationbudget-namecert}
\origin{ai}

The $\RegSeqObservationBudgetUp$ packet realizes the finite observation-budget route required by the Real-completeness target of \autoref{ch:visions-real-completeness}. It consumes the diagonal selector surface of \autoref{ch:concrete-instances-cauchydiagonalbudget-namecert}, the terminal Real seal surface of \autoref{ch:concrete-instances-real-namecert}, the finite StreamName window row, the $\RegSeqRatUp$ readback row, the $\DyadicRatCoreUp$ tolerance ledger, and the primitive $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The packet is an observation route to a seal, not an ambient completeness theorem, quotient stream equality, host real equality, or selected external limit.

\falsifiablePrediction{Every RealUp-seal consumer accepted by $\RegSeqObservationBudgetUp$ factors through the displayed observation-budget row before any seal read.}

\independenceWitness{cauchydiagonalbudget, realobservationbudget, regularcauchycomparison, streamname, real: no listed sibling is bijective to $\RegSeqObservationBudgetUp$. The diagonal budget stores selector data without the full RealUp observation row, the Real observation budget stores seal-facing budget admission without the regular-source and diagonal selector packet, the regular-Cauchy comparison row stores finite comparison data without the terminal seal route, the StreamName row stores finite windows without the RealUp budget, and the RealUp certificate stores the seal without the upstream observation schedule.}

\begin{definition}[Regular observer-budget carrier]
\label{def:regular-observer-budget-carrier}
A $\RegSeqObservationBudgetUp$ carrier is a finite $\BHist$ packet
$$
O=(W,R,E,D,\epsilon,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the $\StreamNameUp$ finite-window row that exposes the requested observation window;
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed window;
\item $E$ is the $\RealUp$ observation-budget row reached only after the regular-source readback;
\item $D$ is the $\CauchyDiagonalBudgetUp$ row selecting the diagonal observation budget consumed by the same read;
\item $\epsilon$ is the $\DyadicRatCoreUp$ tolerance row that fixes the finite precision served by the packet;
\item $H$ records componentwise $\hsame$ transports for the window, readback, RealUp budget, diagonal selector, tolerance, continuation, provenance, and local naming rows;
\item $C$ records the displayed $\Cont$ replay route from $W$ to $R$, from $R$ and $\epsilon$ to $D$, and from $D$ to $E$;
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyDiagonalBudgetUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegSeqObservationBudgetUp}$ row.
\end{enumerate}
The classifier accepts only the displayed finite route $W,R,E,D,\epsilon,H,C,P,N$. It has no coordinate for quotient stream equality, host real equality, an unbounded observation schedule, a hidden diagonal selector, an undisplayed seal read, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.RegSeqObservationBudgetUp.RegSeqObservationBudgetTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular observer-budget NameCert obligations]
\label{thm:regular-observer-budget-namecert-obligations}
For the carrier of \autoref{def:regular-observer-budget-carrier}, the local $\NameCert_{\RegSeqObservationBudgetUp}$ surface consists exactly of carrier admission for
$$
O=(W,R,E,D,\epsilon,H,C,P,N),
$$
StreamName window exposure through $W$, RegSeqRat readback through $R$, RealUp observation-budget exposure through $E$, diagonal selector exposure through $D$, dyadic tolerance exposure through $\epsilon$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier. Its public rows are precisely the finite window, regular-source readback, RealUp observation budget, diagonal selector, dyadic tolerance, transport, continuation, provenance, and local naming rows.

Read the route in dependency order. The StreamName window $W$ is available before regular readback $R$; the tolerance row $\epsilon$ and diagonal budget $D$ then determine the finite observation request; the RealUp budget row $E$ is reached only after those rows are displayed.

The rows $H,C,P,N$ preserve and name the same packet. Since the carrier lacks coordinates for quotient stream equality, host real equality, unbounded observation schedules, hidden selectors, detached seals, and ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular observer-budget filter-schedule non-escape]
\label{thm:regular-observer-budget-filter-schedule-nonescape}
Every accepted $\RegSeqObservationBudgetUp$ consumer route to a $\RealUp$ seal factors through the displayed observation-budget sequence
$$
W,R,\epsilon,D,E,H,C,P,N.
$$
No accepted route may inspect the terminal RealUp seal before the StreamName window, RegSeqRat readback, dyadic tolerance, and CauchyDiagonalBudget selector rows have been read, and no route may replace that finite schedule by an omitted filter row, hidden diagonal selector, quotient stream, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start from the NameCert obligation theorem. It identifies every accepted packet with the displayed rows $W,R,E,D,\epsilon,H,C,P,N$ and gives the local classifier no additional schedule coordinate.

The positive route is fixed by the carrier itself. The window row $W$ feeds the readback row $R$, the tolerance row $\epsilon$ and diagonal selector $D$ fix the finite observation budget, and only then is the RealUp observation-budget row $E$ available for a seal-facing consumer.

The structural rows preserve that order. Componentwise $\hsame$ transport in $H$ and replay in $C$ move the displayed route, while $P$ and $N$ record provenance and local naming for the same packet. Therefore any omitted filter row, hidden selector, quotient, host equality, or ambient completeness object remains outside the accepted route.
\end{proof}

\begin{theorem}[Regular observer-budget window admission]\label{thm:regseq-observation-budget-window-admission}
Every accepted $\RegSeqObservationBudgetUp$ route exposes the finite window row $W$ before it may read the regular-source row $R$, the dyadic tolerance row $\epsilon$, the diagonal selector row $D$, or the RealUp observation-budget row $E$. In particular, no accepted consumer may enter through $R$, $\epsilon$, $D$, or $E$ while leaving the StreamName window undisplayed.
\end{theorem}
\begin{proof}
Use the carrier of \autoref{def:regular-observer-budget-carrier}. Its accepted packet is the displayed row
$$
O=(W,R,E,D,\epsilon,H,C,P,N),
$$
and the classifier has no coordinate for an alternate entry point.

Apply \autoref{thm:regular-observer-budget-namecert-obligations}. The local NameCert surface lists StreamName window exposure through $W$ before RegSeqRat readback, dyadic tolerance exposure, diagonal selector exposure, and RealUp budget exposure.

Finally apply \autoref{thm:regular-observer-budget-filter-schedule-nonescape}. Any consumer route to the seal must factor through the finite schedule in which $W$ is already visible before $R$, $\epsilon$, $D$, and $E$ are consumed. Hence a route that enters through those later rows without exposing $W$ is outside the accepted packet.
\end{proof}

\begin{theorem}[Regular observer-budget tail monotonicity]\label{thm:regseq-observation-budget-tail-monotonicity}
Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the same finite observation budget
$$
W,R,\epsilon,D,E,H,C,P,N
$$
for an accepted $\RegSeqObservationBudgetUp$ route. They may transport or replay the displayed rows, but they cannot introduce a hidden selector, quotient stream, host real equality, unbounded observation schedule, or second terminal seal read.
\end{theorem}
\begin{proof}
Project the structural rows from \autoref{def:regular-observer-budget-carrier}. The row $H$ records only componentwise $\hsame$ transports for the displayed window, readback, RealUp budget, diagonal selector, tolerance, continuation, provenance, and local naming rows, while $C$ records only the displayed replay route from $W$ to $R$, from $R$ and $\epsilon$ to $D$, and from $D$ to $E$.

Apply \autoref{thm:regular-observer-budget-namecert-obligations}. The accepted NameCert surface is exhausted by the same finite carrier rows, so transport and replay have no independent coordinate in which a new selector, quotient, host equality, unbounded schedule, or extra seal read could be stored.

Apply \autoref{thm:regular-observer-budget-filter-schedule-nonescape}. Since every accepted seal-facing route factors through the finite observation schedule, extending along a transported or replayed tail preserves that schedule and cannot escape to an undisplayed observation object.
\end{proof}

\begin{theorem}[Regular observer-budget Real seal handoff]\label{thm:regseq-observation-budget-real-seal-handoff}
The RealUp seal-facing row $E$ of an accepted $\RegSeqObservationBudgetUp$ carrier is consumed only after the StreamName window $W$, RegSeqRat readback $R$, dyadic tolerance $\epsilon$, and CauchyDiagonalBudget selector $D$ have been displayed. Thus the terminal seal handoff is a consequence of the finite observation-budget route, not an independent Real equality, quotient stream, or ambient completeness premise.
\end{theorem}
\begin{proof}
Start with the carrier in \autoref{def:regular-observer-budget-carrier}. The row $E$ is a displayed RealUp observation-budget row inside the same finite packet as $W$, $R$, $\epsilon$, and $D$.

Apply \autoref{thm:regular-observer-budget-namecert-obligations}. The obligation surface reaches RealUp budget exposure through $E$ only as part of the listed window exposure, regular readback, dyadic tolerance, diagonal selector, transport, replay, provenance, and local naming rows.

Now apply \autoref{thm:regular-observer-budget-filter-schedule-nonescape}. It forbids inspecting the terminal RealUp seal before $W$, $R$, $\epsilon$, and $D$ have been read. Therefore any seal-facing use of $E$ is a handoff from the finite observation-budget route, and not from an independent equality, quotient, or completeness principle.
\end{proof}

The Lean TasteGate boundary for this packet must provide \texttt{Nontrivial RegSeqObservationBudgetUp} and \texttt{FieldFaithful RegSeqObservationBudgetUp} instances in \texttt{Derived/RegSeqObservationBudgetUp/TasteGate.lean}, with the canonical obligation theorem expected at \texttt{BEDC.Derived.RegSeqObservationBudgetUp.RegSeqObservationBudgetPacket\_namecert\_obligations}.

\begin{theorem}[Regular observer-budget scoped closure]\label{thm:regseq-observation-budget-scoped-closure}
The carrier definition, NameCert obligations, filter-schedule non-escape, window admission, tail monotonicity, Real seal handoff, and the two Lean TasteGate witnesses bind the scoped $\RegSeqObservationBudgetUp$ surface exactly over the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyDiagonalBudgetUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Inside this scope, every accepted seal-facing consumer is a consumer of the finite observation-budget packet
$$
W,R,\epsilon,D,E,H,C,P,N.
$$
The scope has no classifier coordinate for quotient streams, host real equality, hidden selectors, unbounded schedules, or ambient completeness.
\end{theorem}
\begin{proof}
Apply the carrier description of \autoref{def:regular-observer-budget-carrier} and the obligation surface of \autoref{thm:regular-observer-budget-namecert-obligations}. These identify the accepted object with the listed finite rows and leave no independent row beyond the displayed window, readback, Real budget, diagonal selector, tolerance, transport, continuation, provenance, and local naming data.

Next apply \autoref{thm:regular-observer-budget-filter-schedule-nonescape}, \autoref{thm:regseq-observation-budget-window-admission}, and \autoref{thm:regseq-observation-budget-tail-monotonicity}. The window is visible before later rows, every seal-facing route factors through the finite schedule, and transport or replay preserves the same observation budget rather than creating a new selector or schedule.

Finally apply \autoref{thm:regseq-observation-budget-real-seal-handoff}. The RealUp seal row is reached only as the terminal handoff of the displayed route. The Lean TasteGate names \texttt{RegSeqObservationBudgetTasteGate\_single\_carrier\_alignment} and \texttt{taste\_gate} witness that the same singleton carrier boundary supplies nontriviality and field faithfulness, so the paper scope and kernel TasteGate evidence refer to the same finite packet.
\end{proof}

\closureat{RegSeqObservationBudgetUp}{obligationStr}
\closureat{RegSeqObservationBudgetUp}{scopedStr}[BEDC.Derived.RegSeqObservationBudgetUp.RegSeqObservationBudgetTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\RegSeqObservationBudgetUp}
  \constructivestory{A $\RegSeqObservationBudgetUp$ packet is generated from a StreamName finite-window row, RegSeqRat readback row, RealUp observation-budget row, CauchyDiagonalBudget selector row, DyadicRatCore tolerance row, componentwise transports, displayed continuations, package provenance, and local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:regular-observer-budget-carrier}, \autoref{thm:regular-observer-budget-namecert-obligations}, \autoref{thm:regular-observer-budget-filter-schedule-nonescape}, \autoref{thm:regseq-observation-budget-window-admission}, \autoref{thm:regseq-observation-budget-tail-monotonicity}, \autoref{thm:regseq-observation-budget-real-seal-handoff}, \leanchecked{BEDC.Derived.RegSeqObservationBudgetUp.RegSeqObservationBudgetTasteGate\_single\_carrier\_alignment}, and \leanchecked{BEDC.Derived.RegSeqObservationBudgetUp.taste\_gate} bind the finite RegSeq observation-budget route over StreamNameUp, RegSeqRatUp, RealUp, CauchyDiagonalBudgetUp, DyadicRatCoreUp, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.RegSeqObservationBudgetUp.RegSeqObservationBudgetTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Real completeness, quotient stream equality, host real equality, selected external limits, hidden selectors, ambient completeness, or bridge checking.}
  \upgradepath{The next paper obligation is a public consumer/export theorem for the same finite observation-budget packet.}
\end{closurestatus}
