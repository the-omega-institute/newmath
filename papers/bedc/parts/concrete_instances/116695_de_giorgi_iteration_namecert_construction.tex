\chapter{A Concrete Naming Certificate for $\DeGiorgiIterationUp$}
\label{ch:concrete-instances-de-giorgi-iteration-namecert}
\origin{human}

Following De Giorgi and Evans on energy methods for elliptic and parabolic regularity, the $\DeGiorgiIterationUp$ packet records the finite BEDC surface by which level-set truncations, nested energy windows, Sobolev control, Poincare control, dyadic budgets, regular rational readback, and a terminal real-name seal are replayed before a regularity consumer reads decay. It sits beside the Sobolev source of \autoref{ch:concrete-instances-sobolev-namecert}, the Poincare control route of \autoref{ch:concrete-instances-poincare-inequality-namecert}, the ladder comparison of \autoref{ch:concrete-instances-moser-iteration-namecert}, the dyadic budget core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed finite rows; it is not a host PDE theorem, not a selected weak solution, not a quotient equality of function spaces, and not an ambient compactness argument.

\begin{definition}[DeGiorgiIteration carrier]
\label{def:de-giorgi-iteration-carrier}
A $\DeGiorgiIterationUp$ carrier is a finite $\BHist$ packet
$$
D=(L,T,S,P,M,B,Q,R,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the finite level-set row, listing the cutoff levels and superlevel or sublevel slices read by the iteration.
\item $T$ is the truncation-window row, recording positive and negative parts, clipped test functions, and the local support window.
\item $S$ is the $\SobolevUp$ source row used to pass from energy control to an integrability gain.
\item $P$ is the $\PoincareInequalityUp$ control row used on each displayed window.
\item $M$ is the $\MoserIterationUp$ comparison row, exposing the sibling ladder against which De Giorgi decay consumers compare their exponent schedule.
\item $B$ is the $\DyadicRatCoreUp$ budget row for thresholds, shrink factors, and precision tolerances.
\item $Q$ is the $\RegSeqRatUp$ readback row that turns the dyadic budget and energy ledger into regular rational comparisons.
\item $R$ is the $\RealUp$ seal for the final bound read by downstream regularity consumers.
\item $H$ records componentwise $\hsame$ transport for $L,T,S,P,M,B,Q,R$.
\item $C$ records displayed $\Cont$ replay for the level-set, truncation, energy, budget, readback, and seal routes.
\item $G$ is the $\Pkg$ provenance over $\DeGiorgiIterationUp$, $\SobolevUp$, $\PoincareInequalityUp$, $\MoserIterationUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\DeGiorgiIterationUp}$ row.
\end{enumerate}
\end{definition}
\leandef{BEDC.Derived.DeGiorgiIterationUp.DeGiorgiIterationUp}

\begin{theorem}[DeGiorgiIteration NameCert obligations]
\label{thm:de-giorgi-iteration-namecert-obligations}
For every accepted $\DeGiorgiIterationUp$ carrier $D=(L,T,S,P,M,B,Q,R,H,C,G,N)$, the local $\NameCert_{\DeGiorgiIterationUp}$ surface consists exactly of level-set exposure through $L$, truncation-window exposure through $T$, Sobolev source exposure through $S$, Poincare control through $P$, Moser-comparison exposure through $M$, dyadic budget exposure through $B$, regular rational readback through $Q$, Real-bound sealing through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $G$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:de-giorgi-iteration-carrier}. Each row named in the statement is a displayed coordinate of the accepted packet, and there is no second coordinate for an ambient solution space or host compactness theorem.

The analytic controls are read only through their sibling packets. The Sobolev row, Poincare row, and Moser comparison row point to the finite BEDC surfaces already named by their chapters, while the dyadic, regular rational, and Real rows keep the energy budget on registered numeric readbacks.

Transport and replay use $H$ and $C$, and provenance and local naming use $G$ and $N$. Since the packet inventory is exhausted by these rows, the local NameCert obligations are exactly the listed exposure, stability, replay, provenance, and non-escape clauses.
\end{proof}

\begin{theorem}[DeGiorgiIteration energy-decay handoff]
\label{thm:de-giorgi-iteration-energy-decay-handoff}
Every downstream regularity consumer of an accepted $\DeGiorgiIterationUp$ packet must first read the level-set row $L$, truncation-window row $T$, Sobolev row $S$, Poincare row $P$, Moser-comparison row $M$, dyadic budget row $B$, regular rational readback row $Q$, Real-bound seal $R$, and structural rows $H,C,G,N$. The handoff supplies a finite energy-decay surface; it exports no host De Giorgi theorem, selected weak solution, quotient function equality, countable-choice schedule, unbounded compactness principle, or ambient real-completeness theorem.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:de-giorgi-iteration-namecert-obligations}. They force every public read through the displayed packet rows before any decay claim is consumed.

The energy side is finite because the level-set and truncation rows are ledger coordinates, and the Sobolev and Poincare rows are sibling NameCert inputs. The comparison with $\MoserIterationUp$ is likewise a displayed handoff row, not an imported host regularity theorem.

The numeric side is sealed by $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows. Therefore a consumer receives only the finite decay packet and cannot extract any excluded analytic or choice-theoretic object from the handoff.
\end{proof}

\falsifiablePrediction{Every accepted regularity read of $\DeGiorgiIterationUp$ exposes level-set, truncation-window, Sobolev, Poincare, Moser-comparison, dyadic budget, regular rational readback, Real-bound seal, transport, replay, provenance, and local $\NameCert$ rows before consuming an energy-decay bound.}

\independenceWitness{sobolev, poincare-inequality, moser-iteration, dyadicratcore, regseqrat, real: none is bijective with $\DeGiorgiIterationUp$ because this carrier alone binds level-set truncation windows and energy-decay budgets to the Sobolev, Poincare, Moser-comparison, dyadic, regular rational, Real, transport, replay, provenance, and local naming rows.}

\closureat{DeGiorgiIterationUp}{seedStr}

\begin{closurestatus}{\DeGiorgiIterationUp}
  \constructivestory{A $\DeGiorgiIterationUp$ packet is generated from level-set rows, truncation-window rows, Sobolev control, Poincare control, Moser-comparison rows, dyadic budgets, regular rational readback, a Real-bound seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:de-giorgi-iteration-carrier}, \autoref{thm:de-giorgi-iteration-namecert-obligations}, and \autoref{thm:de-giorgi-iteration-energy-decay-handoff} seed the finite De Giorgi energy-decay packet consumed downstream from Sobolev, Poincare, and Moser-style regularity rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host De Giorgi regularity theorem, selected weak solutions, quotient function equality, countable choice, ambient compactness, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, level-set exactness, truncation-window exactness, Sobolev and Poincare source exactness, Moser-comparison exactness, dyadic-budget exactness, regular rational readback, Real-bound sealing, transport, replay, provenance, and local naming for BEDC.Derived.DeGiorgiIterationUp.}
\end{closurestatus}
