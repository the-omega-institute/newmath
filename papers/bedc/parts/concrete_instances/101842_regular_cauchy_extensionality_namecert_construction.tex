\chapter{A Concrete Naming Certificate for $\RegularCauchyExtensionalityUp$}
\label{ch:concrete-instances-regular-cauchy-extensionality-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyExtensionalityUp$ packet records the finite tail-coincidence route by which two regular Cauchy rational names present the same sealed real name. It sits between the regular rational rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and the separated uniqueness route of \autoref{ch:concrete-instances-regular-limit-uniqueness-namecert}. The certificate is a tail-invariance surface for Real and Completion consumers; it is not quotient stream equality, host real equality, a selected limit, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy extensionality carrier]
\label{def:regular-cauchy-extensionality-carrier}
A $\RegularCauchyExtensionalityUp$ carrier is a finite $\BHist$ packet
$$
E=(R_0,R_1,S_0,S_1,D,T,A_0,A_1,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R_0$ and $R_1$ are the two $\RegSeqRatUp$ regular rational name rows to be compared.
\item $S_0$ and $S_1$ are the $\StreamNameUp$ finite-window rows through which downstream requests read $R_0$ and $R_1$.
\item $D$ is the shared $\DyadicRatCoreUp$ tail-tolerance ledger recording the visible radii, slack comparisons, and finite dyadic requests used by both names.
\item $T$ is the eventual tail-coincidence row, displaying a threshold after which every requested window of $S_0$ and $S_1$ reads the same dyadic rational observation through $D$.
\item $A_0$ and $A_1$ are the two $\RealUp$ seal rows reached from the two regular names after the finite windows and shared ledger have been exposed.
\item $H$ records componentwise $\hsame$ transport for the two regular rows, two stream-window rows, shared dyadic ledger, tail-coincidence row, two Real seals, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each Real or Completion request through the selected tail threshold, the corresponding finite stream windows, the shared dyadic ledger, the two regular readbacks, the two Real seals, and the structural rows.
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RegularLimitUniquenessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyExtensionalityUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient stream equality, host real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyExtensionalityUp.TasteGate.RegularCauchyExtensionalityTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy extensionality NameCert obligations]
\label{thm:regular-cauchy-extensionality-namecert-obligations}
For every accepted $\RegularCauchyExtensionalityUp$ carrier
$$
E=(R_0,R_1,S_0,S_1,D,T,A_0,A_1,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyExtensionalityUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier exposure for the two $\RegSeqRatUp$ rows $R_0,R_1$, two $\StreamNameUp$ window rows $S_0,S_1$, shared $\DyadicRatCoreUp$ ledger $D$, tail-coincidence row $T$, two $\RealUp$ seals $A_0,A_1$, and structural rows $H,C,P,N$;
\item tail-threshold exactness, requiring $T$ to display a finite threshold after which the two stream-window routes make the same dyadic observations through $D$;
\item window stability, requiring every downstream request beyond the displayed threshold to be read through matching finite windows of $S_0$ and $S_1$ before either Real seal is inspected;
\item regular-readback routing, requiring the compared observations to remain attached to $R_0$ and $R_1$ as $\RegSeqRatUp$ readbacks;
\item Real-seal routing, requiring $A_0$ and $A_1$ to be downstream of the displayed regular rows, stream windows, dyadic ledger, and tail-coincidence row;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through $H,C,P,N$;
\item non-escape for quotient stream equality, host real equality, selected limits, countable choice, and ambient $\RealUp$ completeness.
\end{enumerate}
No extensionality obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-extensionality-carrier}. The analytic rows are exactly the two regular rational names, their two finite stream-window surfaces, the shared dyadic ledger, the tail-coincidence threshold, and the two terminal Real seals.

Read the threshold row next. The row $T$ is the only place where the two names are compared, and it compares them only after a displayed finite threshold. Beyond that threshold, the requested windows of $S_0$ and $S_1$ read the same dyadic observations through $D$ before either route reaches a Real seal.

The structural rows preserve the same route. The $\hsame$ rows transport the visible coordinates, the $\Cont$ rows replay the requests through the displayed windows and ledger, the $\Pkg$ row records provenance, and the local naming row names precisely this packet.

Since the omitted objects do not occur as carrier coordinates, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy extensionality tail-coincidence seal]
\label{thm:regular-cauchy-extensionality-tail-coincidence-seal}
Let
$$
E=(R_0,R_1,S_0,S_1,D,T,A_0,A_1,H,C,P,N)
$$
be an accepted $\RegularCauchyExtensionalityUp$ carrier. Suppose $T$ displays a tail threshold $k$ such that every downstream precision request served beyond $k$ reads the same finite $\StreamNameUp$ windows in $S_0$ and $S_1$ and the same $\DyadicRatCoreUp$ tolerance rows in $D$. Then every $\RealUp$ seal consumer factors through the same $\RegSeqRatUp$ tail route before reaching $A_0$ or $A_1$.

Consequently the comparison exported by the packet is tail-coincidence invariance of the two sealed regular names, with visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance. It excludes quotient stream equality, host real equality, selected limits, countable choice, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the displayed tail threshold $k$ in the row $T$. A consumer request below the threshold is not used to identify the two seals; the comparison route begins only once the request has passed into the tail certified by $T$.

After that point, both names read the same finite windows. The row $S_0$ serves the request for $R_0$, the row $S_1$ serves the request for $R_1$, and the threshold row states that these two window reads call on the same entries of the shared ledger $D$.

The dyadic ledger forces the two readbacks to use one tolerance surface. Each compared observation remains attached to its source regular row, so the consumer route is still a $\RegSeqRatUp$ route, but the tail portion of the route is common.

Only then are the two Real seals inspected. The rows $A_0$ and $A_1$ are downstream of the displayed regular rows, stream windows, shared dyadic ledger, and tail-coincidence row, so every Real-facing consumer factors through the same finite tail surface before reading a seal.

The exclusion clauses follow from the carrier inventory. No coordinate contains quotient stream equality, host real equality, a selected limit, countable choice, or ambient Real completeness, and the structural rows can replay only the displayed finite route.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyExtensionalityUp$ Real or Completion consumer that identifies two $\RealUp$ seals without reading a displayed $\RegSeqRatUp$ tail route, matching $\StreamNameUp$ windows, and the shared $\DyadicRatCoreUp$ ledger refutes the carrier surface.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real, regular-limit-uniqueness: no listed sibling supplies the whole twelve-row packet $E=(R_0,R_1,S_0,S_1,D,T,A_0,A_1,H,C,P,N)$, because each sibling contributes only a regular rational source, finite window surface, dyadic ledger, Real seal, or separated uniqueness route while this chapter binds tail-coincidence invariance with transport, replay, provenance, and local naming.}

\closureat{RegularCauchyExtensionalityUp}{seedStr}

\begin{closurestatus}{\RegularCauchyExtensionalityUp}
  \constructivestory{A $\RegularCauchyExtensionalityUp$ packet is generated from two $\RegSeqRatUp$ rows, two $\StreamNameUp$ finite-window rows, a shared $\DyadicRatCoreUp$ ledger, an eventual tail-coincidence row, two $\RealUp$ seal rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-extensionality-carrier}, \autoref{thm:regular-cauchy-extensionality-namecert-obligations}, and \autoref{thm:regular-cauchy-extensionality-tail-coincidence-seal} seed the tail-coincidence invariance route from regular Cauchy names to Real seal consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, host real equality, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier exposure, tail-threshold exactness, window stability, regular-readback routing, Real-seal routing, and tail-coincidence seal factorization for BEDC.Derived.RegularCauchyExtensionalityUp.}
\end{closurestatus}
