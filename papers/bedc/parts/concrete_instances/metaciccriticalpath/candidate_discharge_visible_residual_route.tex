\begin{theorem}[MetaCIC candidate-discharge visible residual route]
\label{thm:metacic-critical-path-candidate-discharge-visible-residual-route}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated strong-normalization, confluence, or decidability consumer that reads the critical-path handoff through $K$ must first expose the candidate row, residual-substitution row, normalization-budget row, bounded checker row, obstruction row $O$, discharge socket $D$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $L$. The route also keeps the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ visible until the consumer reaches the handoff.

The visible-residual route exports no unconditional strong normalization, full Church--Rosser theorem, closed subject reduction, global conversion decidability, quotient syntax equality, selected limit, ambient $\RealUp$ completeness, or kernel consistency claim.
\end{theorem}
\begin{proof}
Start with candidate residual stability, \autoref{thm:metacic-critical-path-candidate-residual-stability}. It attaches candidate-mediated evidence to residual-substitution rows, leaves $O,D$ visible, and keeps the four L10 faces on the same finite packet route.

Apply the candidate residual budget interface of \autoref{thm:metacic-critical-path-candidate-residual-budget-interface}. This exposes the normalization-budget row, residual-budget socket, transport, replay, provenance, and local naming before any normal-form endpoint or critical-path handoff is read.

Finally use the residual diamond budget scope of \autoref{thm:metacic-critical-path-residual-diamond-budget-scope} together with the bounded checker factorization of \autoref{thm:metacic-critical-path-candidate-sn-confluence-checker-factorization}. These supply the bounded checker and local residual-diamond route while preserving the same candidate, obstruction, socket, L10, transport, replay, provenance, and NameCert rows. Since the cited surfaces contain none of the excluded global metatheoretic, quotient, completion, or consistency coordinates, the candidate-discharge consumer can read only the displayed visible-residual route.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateDischargeVisibleResidualRoute}

\begin{theorem}[MetaCIC visible residual socket exhaustion]
\label{thm:metacic-critical-path-visible-residual-socket-exhaustion}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated strong-normalization, confluence, or decidability consumer that reads residual rows through the visible route must keep the discharge socket $D$ and obstruction row $O$ displayed until after candidate residual stability, residual budget, residual diamond budget, bounded checker factorization, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ have all been exposed.

Consequently the visible-residual socket route refuses unconditional strong normalization, global confluence, subject reduction, conversion decidability, quotient term equality, $\RealUp$ completeness, selected limits, and kernel consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathVisibleResidualSocketExhaustion}
\begin{proof}
Start with candidate residual stability from \autoref{thm:metacic-critical-path-candidate-residual-stability}. It names the residual rows through the candidate surface while keeping the obstruction row $O$ and discharge socket $D$ among the displayed coordinates.

Next apply the candidate residual budget interface of \autoref{thm:metacic-critical-path-candidate-residual-budget-interface} and the residual diamond budget scope of \autoref{thm:metacic-critical-path-residual-diamond-budget-scope}. These rows expose the finite budget ledgers used by the residual consumer, but they do not replace the obstruction or discharge coordinates by a global metatheorem.

Finally use the bounded checker factorization of \autoref{thm:metacic-critical-path-candidate-sn-confluence-checker-factorization}. The factorization is read through the same $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows, so the visible route is exhausted only after $O$ and $D$ remain visible through the full handoff. None of the cited rows exports the excluded normalization, confluence, reduction, decidability, quotient, completion, selection, or consistency claims.
\end{proof}

\begin{theorem}[MetaCIC visible residual discharge budget]
\label{thm:metacic-critical-path-visible-residual-discharge-budget}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated strong-normalization, confluence, or decidability consumer that treats the discharge socket $D$ as filled must first expose candidate residual stability, the candidate residual budget, residual diamond budget scope, bounded checker factorization, the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row.

The filled-socket read exports no unconditional strong normalization, full Church--Rosser theorem, closed subject reduction, global conversion decidability, quotient syntax equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency claim.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathVisibleResidualDischargeBudget}
\begin{proof}
Start with candidate residual stability from \autoref{thm:metacic-critical-path-candidate-residual-stability}. It keeps the candidate row attached to the residual-substitution route and leaves the discharge socket visible on the same packet as the four L10 faces.

Next apply the candidate residual budget interface of \autoref{thm:metacic-critical-path-candidate-residual-budget-interface}. This supplies the finite budget row that a filled-socket consumer must display before reading any normalization, confluence, or decidability handoff.

The residual diamond budget scope of \autoref{thm:metacic-critical-path-residual-diamond-budget-scope} then bounds the diamond-facing part of the same route. The bounded checker factorization of \autoref{thm:metacic-critical-path-candidate-sn-confluence-checker-factorization} supplies the checker-facing route without replacing candidate, obstruction, discharge, or L10 data by a global metatheorem.

Finally transport, replay, provenance, and local naming are read through $H,C,P,L$. Since the cited surfaces contain no unconditional normalization theorem, full Church--Rosser theorem, closed subject-reduction theorem, global conversion decision procedure, quotient syntax equality, selected-limit row, ambient $\RealUp$ completeness theorem, or kernel-consistency coordinate, filling $D$ is only the displayed discharge-budget boundary.
\end{proof}

\begin{theorem}[MetaCIC visible residual local-decidability budget]
\label{thm:metacic-critical-path-visible-residual-local-decidability-budget}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any bounded local decidability read through a candidate-mediated residual route must expose candidate residual stability, the candidate residual budget, residual diamond budget scope, bounded checker factorization, the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, obstruction row $O$, discharge socket $D$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert$ row $L$ before using a decidability-facing conclusion.

The read exports no global conversion decidability, quotient syntax equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency claim.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathVisibleResidualLocalDecidabilityBudget}
\begin{proof}
First read the visible residual discharge route. The candidate-discharge route, socket exhaustion, and discharge budget theorems keep the candidate row, residual-substitution route, obstruction row $O$, discharge socket $D$, and the four L10 faces visible through the same finite packet.

Next apply the bounded checker factorization of \autoref{thm:metacic-critical-path-candidate-sn-confluence-checker-factorization}. It turns the local decidability-facing use into a bounded checker read, not a global conversion procedure.

Finally transport and replay through $H,C,P,L$. Since the cited rows never introduce quotient syntax equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency, the local decidability conclusion is available only inside the displayed finite residual budget.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond visible residual consumer]
\label{thm:metacic-critical-path-parallel-diamond-visible-residual-consumer}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any consumer that routes a parallel-diamond or confluence handoff through the critical path must expose candidate residual stability, the residual-substitution row, normalization-budget row, bounded checker row, obstruction row $O$, discharge socket $D$, the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert$ row $L$ before it reads the confluence-facing handoff.

The parallel-diamond read exports no premise-free confluence, subject reduction, quotient syntax equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency claim.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathParallelDiamondVisibleResidualConsumer}
\begin{proof}
Start with the candidate-discharge visible residual route of \autoref{thm:metacic-critical-path-candidate-discharge-visible-residual-route}. It already exposes the candidate row, residual-substitution row, normalization budget, bounded checker, obstruction row, discharge socket, L10 faces, transport, replay, provenance, and local NameCert before any confluence-facing handoff is consumed.

Use socket exhaustion from \autoref{thm:metacic-critical-path-visible-residual-socket-exhaustion}. A parallel-diamond consumer is still a bounded residual consumer, so it cannot hide $O$ or $D$ after naming a diamond-facing handoff.

Apply the discharge budget of \autoref{thm:metacic-critical-path-visible-residual-discharge-budget} and the local-decidability budget of \autoref{thm:metacic-critical-path-visible-residual-local-decidability-budget}. These theorems keep the same finite residual rows visible for any filled-socket or checker-facing read.

Thus the parallel-diamond route has no additional premise-free confluence, subject-reduction, quotient-syntax, selected-limit, Real-completeness, or consistency coordinate. It can consume only the displayed visible-residual packet before reaching the confluence-facing handoff.
\end{proof}
