\begin{theorem}[MetaCIC critical-path parallel-diamond public export route]
\label{thm:metacic-critical-path-parallel-diamond-public-export-route}
Any $\MetaCICCriticalPathUp$ consumer that reads the public export of an accepted $\MetaCICParallelDiamondFrontierUp$ packet must enter through the retained residual-substitution premise, critical-pair row, candidate-join row, residual-frontier row, closed-checker row, closed-normal fragment row, bounded-check row, obstruction row, structural transport, replay, provenance, and local naming rows displayed by \autoref{thm:metacic-parallel-diamond-frontier-public-export}. The public export is therefore a finite frontier packet for the critical path; it is not subject reduction, unrestricted Church--Rosser, evaluator equality, quotient term equality, global normalization, residual-substitution discharge, or kernel consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathParallelDiamondPublicExportRoute}
\begin{proof}
Start with the public export theorem for the parallel-diamond frontier, \autoref{thm:metacic-parallel-diamond-frontier-public-export}. It exposes the residual-substitution premise, critical-pair, candidate-join, residual-frontier, checker, bounded-check, obstruction, transport, replay, provenance, and local naming rows before any outside consumer may read the frontier.

Next route the exported packet through the existing critical-path handoff of \autoref{thm:metacic-critical-path-residual-handoff}. That handoff preserves the frontier as retained finite data for $\MetaCICCriticalPathUp$ rather than as a global confluence theorem.

Finally use the consumer non-escape boundary of \autoref{thm:metacic-critical-path-frontier-consumer-nonescape}. Since the consumed public export has no coordinate for residual-substitution discharge, subject reduction, evaluator equality, quotient equality, global normalization, or kernel consistency, the critical-path route exports none of those endpoints.
\end{proof}
