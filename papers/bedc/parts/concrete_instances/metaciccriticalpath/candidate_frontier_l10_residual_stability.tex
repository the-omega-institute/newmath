\begin{definition}[MetaCIC candidate residual stability surface]
\label{def:metacic-critical-path-candidate-residual-stability-surface}
The candidate-mediated residual stability surface of an accepted $\MetaCICCriticalPathUp$ packet
$$
K=(S,N,O,U,D,H,C,P,L)
$$
is the finite $\BHist$ route that reads candidate-mediated strong-normalization evidence through $S,N,U,H,C,P,L$ while keeping the residual substitution and diamond budget of \autoref{thm:metacic-critical-path-residual-substitution-diamond-budget} attached to the four L10 source faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp .
$$
The classifier compares accepted stability surfaces only through the displayed residual rows, candidate frontier rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data. It has no coordinate for arbitrary strong normalization, global confluence, closed subject reduction, quotient term equality, selected stream limits, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[MetaCIC candidate residual stability]
\label{thm:metacic-critical-path-candidate-residual-stability}
For every accepted $\MetaCICCriticalPathUp$ packet $K=(S,N,O,U,D,H,C,P,L)$, any candidate-mediated handoff that consumes residual substitution evidence must keep the residual diamond budget and the four L10 source faces visible until the handoff reaches the confluence-decidability boundary. The stability claim is only over the displayed rows of \autoref{def:metacic-critical-path-candidate-residual-stability-surface}; it does not export arbitrary strong normalization, global confluence, beta-conversion decidability, closed subject reduction, quotient term equality, selected limits, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateResidualStability}
\begin{proof}
Start with the candidate-mediated frontier theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It identifies the finite handoff through $S,N,U,H,C,P,L$ and keeps the obstruction and socket rows visible.

Apply the residual substitution diamond budget of \autoref{thm:metacic-critical-path-residual-substitution-diamond-budget}. Residual replay is therefore read as a finite budget attached to the same displayed packet, not as a global confluence theorem.

Use the L10 entry and exit surfaces, \autoref{thm:metacic-critical-path-l10-entry-budget-synchronization} and \autoref{thm:metacic-critical-path-l10-four-object-exit-certificate}. They keep $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ visible while the residual rows are consumed.

The structural rows $H,C,P,L$ transport, replay, source, and name the same finite surface. Since none of the listed coordinates supplies the excluded metatheoretic or real-completion principles, the candidate-mediated residual handoff is stable exactly over the displayed L10 source faces.
\end{proof}

\begin{theorem}[MetaCIC candidate real-regular handoff]
\label{thm:metacic-critical-path-real-regular-handoff}
Every residual-stability handoff of \autoref{def:metacic-critical-path-candidate-residual-stability-surface} that reaches a real-facing source row must first pass through the $\DyadicRatCoreUp$ arithmetic face, the $\StreamNameUp$ observation face, and the $\RegSeqRatUp$ regular readback face before the $\RealUp$ seal is consumed. Candidate-mediated strong-normalization evidence may be replayed along this route, but it cannot replace the regular readback by host equality, selected limits, quotient stream equality, or ambient real completeness.
\end{theorem}
\begin{proof}
Read the four L10 source faces in their displayed order. The dyadic arithmetic face supplies endpoint and radius data, the stream face supplies finite observation windows, and the regular-rational face supplies the readback row used before the real seal is opened.

Apply \autoref{thm:metacic-critical-path-candidate-residual-stability}. Candidate-mediated residual replay remains attached to the same L10 faces, so it cannot jump directly from a syntactic normalization row to a real-facing conclusion.

The $\RealUp$ boundary is therefore downstream of $\DyadicRatCoreUp$, $\StreamNameUp$, and $\RegSeqRatUp$. The packet has no coordinate for host equality, selected limits, quotient stream equality, or ambient completeness, so the handoff remains a finite regular readback route.
\end{proof}

\begin{theorem}[MetaCIC candidate frontier nonescape]
\label{thm:metacic-critical-path-frontier-nonescape}
For every accepted $\MetaCICCriticalPathUp$ packet $K=(S,N,O,U,D,H,C,P,L)$, the candidate-mediated residual stability surface may be consumed only through the displayed frontier rows $S,N,U,H,C,P,L$, the obstruction and socket rows $O,D$, the residual substitution and diamond budget, and the L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. It cannot be used to erase the obstruction row, fill the discharge socket, or export closed subject reduction, arbitrary strong normalization, full confluence, beta-conversion decidability, quotient term equality, selected limits, host equality, or ambient real completeness.
\end{theorem}
\begin{proof}
Begin with obstruction nonescape, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-obstruction-nonescape}. It keeps $O$ and $D$ visible while candidate-mediated strong-normalization evidence is consumed.

Apply candidate residual stability, \autoref{thm:metacic-critical-path-candidate-residual-stability}. The residual substitution and diamond rows remain attached to the four L10 source faces and to the same frontier packet.

Use the real-regular handoff, \autoref{thm:metacic-critical-path-real-regular-handoff}. Any real-facing read must pass through the dyadic, stream, and regular-rational faces before $\RealUp$ is consumed.

Once those displayed rows have been consumed, no remaining coordinate can supply the excluded global metatheoretic or real-completion principles. The frontier therefore closes only the finite residual-stability route named above and no larger claim.
\end{proof}
