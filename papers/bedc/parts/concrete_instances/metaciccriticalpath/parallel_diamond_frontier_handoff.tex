\begin{theorem}[MetaCIC critical-path parallel-diamond frontier handoff]
\label{thm:metacic-critical-path-parallel-diamond-frontier-handoff}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Suppose a parallel-diamond discharge request for $K$ consumes the candidate-mediated strong-normalization frontier, the residual substitution budget, the local diamond witness, and the L10 four-object source ledger for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Then the request factors through the displayed candidate frontier before it can be read as a confluence consumer or a normalization consumer.

The handoff is a finite same-packet route through $S,N,O,U,D,H,C,P,L$. It does not export subject reduction, global Church--Rosser, host beta equality, evidence-free strong normalization, quotient term equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency.
\end{theorem}
\begin{proof}
First read the candidate side. The candidate-mediated SN frontier of \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and the candidate discharge frontier of \autoref{thm:metacic-critical-path-candidate-discharge-frontier} place the normalization candidate and its discharge rows on the displayed critical-path packet before any public consumer can use them.

Next attach the residual and diamond budget. The residual substitution diamond budget, the residual-diamond local-confluence frontier, and the residual diamond budget scope keep the residual substitution rows, local join witness, checker factorization, and residual readback on the same finite route.

Finally attach the L10 ledger. The four-object exit certificate and the residual completion exactness handoff force every completion-facing read through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before a confluence or normalization endpoint can be read.

Thus a parallel-diamond discharge request reaches its endpoint only through the displayed candidate frontier followed by the residual, local-diamond, and L10 rows of $K$. Since the cited route has no coordinate for subject reduction, global Church--Rosser, host beta equality, evidence-free strong normalization, quotient term equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency, none of those claims is exported.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathParallelDiamondFrontierHandoff}

\begin{theorem}[MetaCIC mature L10 parallel-diamond route]
\label{thm:metaciccriticalpath-mature-l10-parallel-diamond-route}
Every parallel-diamond discharge request that reaches the mature $\MetaCICCriticalPathUp$ route must first read the candidate-mediated strong-normalization frontier, the residual substitution diamond budget, and the L10 four-object source ledger for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. The resulting route is the displayed dependency path from \autoref{thm:metacic-critical-path-parallel-diamond-frontier-handoff} through the mature L10 source package of \autoref{thm:metacic-critical-path-mature-package}; it is not a proof of global Church--Rosser, subject reduction, kernel consistency, quotient term equality, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:metacic-critical-path-parallel-diamond-frontier-handoff}. It forces any parallel-diamond discharge request to pass through the candidate-mediated strong-normalization frontier, residual substitution budget, local diamond witness, and four-object L10 source ledger before a confluence or normalization consumer can read it.

Now apply \autoref{thm:metacic-critical-path-mature-package}. The mature package keeps $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as separate source faces while residual, confluence, and candidate-mediated strong-normalization rows remain budget data attached to the same packet.

The two routes have the same carrier $K=(S,N,O,U,D,H,C,P,L)$ and the same structural rows. Composing them therefore gives a single L10-facing dependency route for mature parallel-diamond discharge requests. Since neither cited theorem contains a row for global Church--Rosser, subject reduction, kernel consistency, quotient term equality, selected limits, or ambient $\RealUp$ completeness, the composed route exports none of those claims.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathMatureL10ParallelDiamondRoute}

\begin{theorem}[MetaCIC residual-diamond candidate readback route]
\label{thm:metaciccriticalpath-residual-diamond-candidate-readback-route}
Every residual-diamond candidate read that reaches the mature $\MetaCICCriticalPathUp$ route must first pass through the candidate-mediated strong-normalization frontier, the residual substitution budget, the local diamond witness, and the L10 four-object source ledger for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Only after those rows are displayed may the read be consumed as normalization-facing or confluence-facing evidence.

The route is a finite readback over the same accepted packet $K=(S,N,O,U,D,H,C,P,L)$. It does not export subject reduction, global Church--Rosser, kernel consistency, quotient term equality, selected limits, ambient $\RealUp$ completeness, or a theorem identifying candidate readback with host confluence.
\end{theorem}
\begin{proof}
Start with the parallel-diamond frontier handoff of \autoref{thm:metacic-critical-path-parallel-diamond-frontier-handoff}. It forces a discharge request to display the candidate-mediated strong-normalization frontier, residual substitution budget, local diamond witness, and four-object L10 source ledger before the request can be read by a confluence or normalization consumer.

Now apply the mature L10 route of \autoref{thm:metaciccriticalpath-mature-l10-parallel-diamond-route}. It keeps the same four L10 source faces, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, on the mature critical-path package rather than replacing them by an ambient completion or confluence theorem.

Composing the two routes gives the stated residual-diamond candidate readback route. The first theorem supplies the candidate, residual, and local-diamond order; the second theorem keeps the read on the mature L10 source ledger.

The composed route has only the rows of $K$ and the four displayed L10 source faces. Since neither component route contains subject reduction, global Church--Rosser, kernel consistency, quotient term equality, selected limits, ambient $\RealUp$ completeness, or host confluence equality, the readback exports none of those claims.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondCandidateReadbackRoute}
