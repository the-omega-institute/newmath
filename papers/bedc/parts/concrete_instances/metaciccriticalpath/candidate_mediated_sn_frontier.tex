\begin{definition}[MetaCIC candidate-mediated SN frontier]
\label{def:metacic-critical-path-candidate-mediated-frontier}
For an accepted $\MetaCICCriticalPathUp$ packet
$$
K=(S,N,O,U,D,H,C,P,L),
$$
the candidate-mediated strong-normalization frontier is the finite subroute that displays the strong-normalization row $S$, the normal-form endpoint $N$, the handoff row $U$, the discharge socket $D$, the componentwise transport row $H$, the continuation row $C$, the provenance row $P$, and the local naming row $L$, together with the four local source faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ already recorded for the open-phase source ledger. The frontier may be consumed only as a boundary between the candidate-mediated SN evidence and the confluence-decidability handoff; it contains no coordinate for closed subject reduction, arbitrary strong normalization, global confluence, global beta-conversion decidability, quotient term equality, host equality, selected limits, or $\RealUp$ completeness.
\end{definition}

\begin{theorem}[MetaCIC candidate-mediated SN frontier]\label{thm:metacic-critical-path-candidate-mediated-sn-frontier}
Every candidate-mediated strong-normalization read of an accepted $\MetaCICCriticalPathUp$ packet that is passed to the confluence-decidability handoff must factor through the frontier of \autoref{def:metacic-critical-path-candidate-mediated-frontier}. In particular, the read must display $S,N,U,D,H,C,P,L$, keep the obstruction row $O$ and discharge socket $D$ available to the surrounding packet, and expose the four local faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any confluence-facing or decidability-facing consumer reads the handoff row $U$.
\end{theorem}
\begin{proof}
Begin with the candidate-mediated SN finite socket normal form, \autoref{thm:metacic-critical-path-candidate-sn-finite-socket-normal-form}. It confines the candidate-mediated read to the socket-aware packet rows $S,N,O,U,D,H,C,P,L$ together with the four local source faces.

Next apply the SN confluence-decidability budget theorem, \autoref{thm:metacic-critical-path-sn-confluence-budget}. It requires any confluence-facing or decidability-facing consumer to read the same candidate budget surface before it consumes the handoff row $U$.

The frontier definition simply names the common boundary needed by these two routes: the candidate-mediated read supplies $S,N,U,D,H,C,P,L$ and the four local faces, while the enclosing packet still retains $O$ and $D$ as visible blocked-route data. Since neither cited theorem supplies a coordinate for closed subject reduction, arbitrary strong normalization, global confluence, global beta-conversion decidability, quotient equality, host equality, selected limits, or $\RealUp$ completeness, the factorization cannot export any of those claims.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathPacket\_candidate\_mediated\_sn\_frontier}

\begin{theorem}[MetaCIC candidate-mediated frontier socket separation]
\label{thm:metacic-critical-path-candidate-mediated-frontier-socket-separation}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated frontier replay that is passed toward the handoff row $U$ must keep the obstruction row $O$ and discharge socket $D$ visible as separate rows from $S,N,U,H,C,P,L$ and from the four L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp .
$$
The replay may use the frontier as strong-normalization evidence for the handoff boundary, but it cannot fill the subject-reduction socket or export closed subject reduction, arbitrary strong normalization, global confluence, beta-conversion decidability, quotient term equality, selected limits, host equality, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the candidate-mediated frontier theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places the candidate-mediated read on $S,N,U,D,H,C,P,L$ and the four L10 faces before the handoff row $U$ is consumed.

Apply the handshake theorem \autoref{thm:metacic-critical-path-sn-confluence-decidability-handshake}. The handoff may inspect the frontier rows, but the theorem keeps $O$ and $D$ visible unless a separate closed subject-reduction row is supplied.

Use the discharge-row theorem and discharge-socket induction, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. These preserve the separation between the replay rows and the socket row while finite frontier rows are consumed.

Finally apply candidate-normalization socket exhaustion, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. The exhausted surface contains no coordinate for the refused global metatheoretic or Real-completion claims, so the socket remains a displayed boundary rather than a discharged theorem.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedFrontierSocketSeparation}

\begin{theorem}[MetaCIC candidate-mediated frontier L10 readback]
\label{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every candidate-mediated read of the handoff row $U$ must expose the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before $U$ is handed to any confluence-facing or decidability-facing consumer. The obstruction row $O$ and discharge socket $D$ remain visible throughout that L10 readback. No such readback supplies closed subject reduction, arbitrary strong normalization, global confluence, beta-conversion decidability, quotient term equality, selected limits, host equality, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the candidate-mediated SN frontier \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It requires the four L10 faces to be present before a confluence-facing or decidability-facing consumer reads $U$.

Use the candidate-normalization budget theorem, \autoref{thm:metacic-critical-path-candidate-normalization-budget}. The budget identifies the finite rows $S,N,U,H,C,P,L$ together with the same four faces as the whole normalization surface available to the consumer.

Apply the handshake and consumer-boundary theorems, \autoref{thm:metacic-critical-path-sn-confluence-decidability-handshake} and \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. They allow the handoff row to be consumed only after the candidate-mediated and L10 source data have been displayed.

The discharge-row theorem keeps $D$ separate, and socket exhaustion keeps $O,D$ visible after the finite readback. Since the consumed rows are exactly the frontier rows plus the four faces, no quotient equality, selected limit, host equality, Real-completeness, or global MetaCIC theorem is exported.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedFrontierL10Readback}

\begin{theorem}[MetaCIC candidate frontier Real source lock]
\label{thm:metacic-critical-path-candidate-frontier-real-source-lock}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated handoff that reads the row $U$ must expose $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as source rows, with the obstruction row $O$ and discharge socket $D$ still visible, before any confluence-facing or decidability-facing consumer reads the handoff. The handoff therefore treats the $\RealUp$ face as a named L10 source row, not as selected limits, host equality, ambient Real completeness, quotient term equality, closed subject reduction, full confluence, or beta-conversion decidability.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateFrontierRealSourceLock}
\begin{proof}
Start from the candidate-mediated frontier and L10 readback theorems, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback}. Together they require the finite candidate rows and the four L10 faces before the handoff row $U$ is consumed.

Apply the handshake theorem, \autoref{thm:metacic-critical-path-sn-confluence-decidability-handshake}. It permits confluence-facing or decidability-facing replay only after the candidate-mediated source rows have been displayed.

Use the discharge-row theorem, socket separation, and socket exhaustion, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row}, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-socket-separation}, and \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. These keep $O$ and $D$ visible while the finite handoff reads $U$.

Since the displayed source rows contain no coordinate for selected limits, host equality, Real completeness, quotient term equality, closed subject reduction, full confluence, or beta-decidability, the Real-facing source lock exports only the named L10 row sequence.
\end{proof}

\begin{theorem}[MetaCIC candidate frontier RegSeqRat handoff]
\label{thm:metacic-critical-path-candidate-frontier-regseqrat-handoff}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. The $\RegSeqRatUp$ readback row is the only regular-rational route available to a candidate-mediated handoff through $U$. Consequently the frontier exports no selected limit, quotient term equality, host equality, ambient $\RealUp$ completeness, closed subject reduction, full confluence, or alternate regular-rational approximation route beyond the displayed $\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\RealUp$ source window.
\end{theorem}
\begin{proof}
Read the L10 readback theorem, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback}. It places $\RegSeqRatUp$ inside the four-face source window that must be exposed before any confluence-facing or decidability-facing consumer reads $U$.

Next use the frontier handoff totality theorem, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-handoff-totality}. The handoff surface is exhausted by $S,N,U,H,C,P,L$, the visible rows $O,D$, and the same four L10 faces.

Apply discharge coverage and socket exhaustion, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage} and \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. They leave no hidden coordinate through which another regular-rational route could fill $D$ or bypass the displayed source window.

Thus the only regular-rational readback available to the handoff is the named $\RegSeqRatUp$ row. The excluded selected-limit, quotient-equality, host-equality, Real-completeness, closed-subject-reduction, and full-confluence claims are not exported by the frontier.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateFrontierRegSeqRatHandoff}

\begin{theorem}[MetaCIC candidate-mediated SN L10 completion lock]
\label{thm:metacic-critical-path-candidate-mediated-sn-l10-completion-lock}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any accepted packet that reaches the candidate-mediated strong-normalization handoff through $U$ must expose exactly the source rows $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any confluence-facing or decidability-facing consumer reads the handoff. The obstruction row $O$ and discharge socket $D$ remain visible, and the handoff exports no selected limits, host equality, quotient term equality, closed subject reduction, global confluence, beta-conversion decidability, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the candidate-mediated frontier theorem and the L10 readback theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback}. Together they require the four displayed L10 source rows before a consumer may read $U$.

Use the RegSeqRat handoff theorem, \autoref{thm:metacic-critical-path-candidate-frontier-regseqrat-handoff}. It locks the regular-rational part of the route to the displayed $\RegSeqRatUp$ row inside the same four-row window.

Apply socket separation and obstruction nonescape, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-socket-separation} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-obstruction-nonescape}. These keep $O,D$ visible while the handoff rows are consumed.

Finally read discharge coverage. The consumed surface is exhausted by $S,N,U,H,C,P,L$, the visible $O,D$ boundary, and the four L10 source rows, so no selected-limit, quotient-equality, host-equality, closed-subject-reduction, global-confluence, beta-decidability, or Real-completeness claim is exported.
\end{proof}

\begin{theorem}[MetaCIC candidate-mediated frontier handoff totality]
\label{thm:metacic-critical-path-candidate-mediated-frontier-handoff-totality}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any finite confluence-facing or decidability-facing handoff that uses candidate-mediated strong-normalization evidence is exhausted by the route
$$
S,N,U,H,C,P,L
$$
together with the visible boundary rows $O,D$ and the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Thus the handoff is total only for the displayed frontier surface; it refuses closed subject reduction, arbitrary strong normalization, global confluence, beta-conversion decidability, quotient term equality, selected limits, host equality, and $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First apply the candidate-mediated frontier and the confluence-decidability handshake. Together they route any candidate-mediated handoff through $S,N,U,H,C,P,L$ while keeping the four L10 faces available before $U$ is consumed.

Next use the candidate-normalization budget theorem. It shows that the finite budget of the handoff is precisely the displayed candidate-normalization surface, not an unrestricted strong-normalization or confluence theorem.

Apply discharge-socket induction to the finite handoff rows. Each consumed row preserves $O$ and leaves $D$ unfilled, because none of the rows is an explicit closed subject-reduction coordinate.

Finally invoke candidate-normalization socket exhaustion. Once the displayed surface and the four L10 faces have been consumed, there is no remaining packet coordinate that could supply quotient term equality, selected limits, host equality, Real completeness, global confluence, beta-conversion decidability, or arbitrary strong normalization. The handoff is therefore total exactly over the named frontier and no more.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedFrontierHandoffTotality}

\begin{theorem}[MetaCIC candidate-mediated frontier obstruction nonescape]
\label{thm:metacic-critical-path-candidate-mediated-frontier-obstruction-nonescape}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every finite handoff that uses candidate-mediated strong-normalization evidence keeps the obstruction row $O$ and the discharge socket $D$ visible until a separate closed subject-reduction row is displayed. The handoff may consume $S,N,U,H,C,P,L$ and the L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, but it cannot erase $O$, fill $D$, or export closed subject reduction, full confluence, global beta-conversion decidability, quotient term equality, selected limits, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the candidate-mediated frontier and the handoff totality theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-handoff-totality}. They identify the handoff surface as the finite route through $S,N,U,H,C,P,L$, the visible boundary rows $O,D$, and the four L10 faces.

Apply the handshake theorem \autoref{thm:metacic-critical-path-sn-confluence-decidability-handshake}. It permits confluence-facing or decidability-facing replay only through the displayed route, and it leaves $O,D$ open unless the packet separately displays closed subject reduction.

Use the discharge-row theorem and discharge-socket induction, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. These keep $D$ separate from the candidate-mediated rows while finite replay consumes the handoff surface.

Finally apply socket exhaustion, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. Once $S,N,U,H,C,P,L$ and the four L10 faces have been consumed, the packet has no remaining coordinate that could hide a closed subject-reduction row or discharge the obstruction. The listed global metatheoretic, quotient, selected-limit, host-equality, and completeness claims therefore remain outside the handoff.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedFrontierObstructionNonescape}

\begin{theorem}[MetaCIC candidate frontier discharge coverage]
\label{thm:metacic-critical-path-candidate-frontier-discharge-coverage}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every finite candidate-mediated handoff that first consumes the strong-normalization frontier and then reaches the confluence-decidability boundary is exhausted by the replay rows
$$
S,N,U,H,C,P,L,
$$
the visible obstruction and socket rows $O,D$, and the four L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp .
$$
The coverage conclusion is only this finite discharge surface. It refuses closed subject reduction, arbitrary strong normalization, full confluence, beta-conversion decidability, quotient term equality, selected limits, host equality, and $\RealUp$ completeness unless a later packet displays separate rows for those claims.
\end{theorem}
\begin{proof}
Begin with the candidate-mediated frontier theorem and handoff totality, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-handoff-totality}. They force any accepted handoff through $S,N,U,H,C,P,L$ together with the four L10 faces before a confluence-facing or decidability-facing consumer reaches the handoff boundary.

Next apply obstruction nonescape, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-obstruction-nonescape}. The obstruction row $O$ and discharge socket $D$ remain visible while the frontier is consumed, so the handoff cannot erase the subject-reduction socket by replaying candidate-mediated SN evidence.

Use the discharge-row theorem and discharge-socket induction, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. These make the finite discharge route stable under each consumed row and keep $D$ separate from $S,N,U,H,C,P,L$.

Finally read the existing discharge coverage theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. It packages the same rows as the whole available frontier budget. Since none of the displayed coordinates is closed subject reduction, arbitrary strong normalization, global confluence, beta-conversion decidability, quotient term equality, selected limits, host equality, or $\RealUp$ completeness, the stated refusals remain outside the discharge coverage.
\end{proof}

\begin{theorem}[MetaCIC candidate frontier confluence socket boundary]
\label{thm:metacic-critical-path-candidate-frontier-confluence-socket-boundary}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A confluence-facing consumer may use the handoff row $U$ after candidate-mediated strong-normalization evidence only while the obstruction row $O$ and discharge socket $D$ remain visible, and only after the four L10 source faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
have been displayed. The boundary exports no closed subject reduction, global confluence, beta-conversion decidability, quotient term equality, selected limits, host equality, ambient $\RealUp$ completeness, or discharged subject-reduction socket.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateFrontierConfluenceSocketBoundary}
\begin{proof}
Start with the candidate-mediated frontier theorem and handoff totality, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-handoff-totality}. They place every candidate-mediated handoff on $S,N,U,H,C,P,L$ together with the visible rows $O,D$ and the four L10 faces.

Use the L10 readback and Real source lock theorems, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback} and \autoref{thm:metacic-critical-path-candidate-frontier-real-source-lock}. These force the dyadic, stream, regular-rational, and Real rows to remain explicit before $U$ is consumed by a confluence-facing or decidability-facing route.

Now apply socket separation and obstruction nonescape, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-socket-separation} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-obstruction-nonescape}. They keep $O$ and $D$ visible while the handoff is replayed and prevent candidate-mediated strong-normalization evidence from filling the subject-reduction socket.

Finally read the discharge coverage route in \autoref{thm:metacic-critical-path-candidate-frontier-discharge-coverage}. Since the available surface is exactly the finite frontier plus the L10 faces, no closed subject reduction, global confluence, beta-decidability, quotient equality, selected limit, host equality, ambient Real completeness, or discharged socket can be exported.
\end{proof}

\begin{theorem}[MetaCIC candidate frontier discharge refusal]
\label{thm:metacic-critical-path-candidate-frontier-discharge-refusal}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated handoff that has exposed the four L10 source faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
still refuses discharge of the subject-reduction socket unless a separate closed subject-reduction row is displayed. The same handoff refuses global confluence, beta-decidability, quotient term equality, host equality, selected limits, and $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateFrontierDischargeRefusal}
\begin{proof}
Start with the frontier and L10 readback theorems, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback}. They place the candidate-mediated handoff on $S,N,U,H,C,P,L$ and force the four L10 faces to be displayed before any confluence-facing or decidability-facing consumer reads $U$.

Use obstruction nonescape and discharge coverage, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-obstruction-nonescape} and \autoref{thm:metacic-critical-path-candidate-frontier-discharge-coverage}. These keep $O$ and $D$ visible while the handoff surface is consumed and identify the whole available discharge budget as the finite frontier together with the L10 faces.

Finally apply the confluence socket boundary, \autoref{thm:metacic-critical-path-candidate-frontier-confluence-socket-boundary}. None of the displayed rows is a closed subject-reduction coordinate, so the socket is not discharged. The same absence excludes global confluence, beta-decidability, quotient term equality, host equality, selected limits, and $\RealUp$ completeness from the exported handoff.
\end{proof}

\begin{theorem}[MetaCIC SN confluence decidability handshake]
\label{thm:metacic-critical-path-sn-confluence-decidability-handshake}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. If a closed-normal-form membership read first uses candidate-mediated strong normalization and then passes to the confluence-decidability handoff, the whole read factors through $S,N,U,H,C,P,L$ together with the four L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp .
$$
The obstruction row $O$ and discharge socket $D$ remain visible and unfilled unless the same packet also displays a separate closed subject-reduction row.
\end{theorem}
\begin{proof}
Begin with the candidate-mediated frontier. By \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}, the strong-normalization read is confined to the displayed frontier rows and the four L10 faces before any confluence-facing or decidability-facing consumer can inspect the handoff row $U$.

Next apply \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. The consumer may use the existing handoff row and route closed-normal-form membership through $C,P,L$, but it receives no closed subject-reduction coordinate and no global beta-conversion decision procedure.

The discharge boundary is preserved by \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. These results keep $O$ and $D$ visible while finite frontier rows are replayed and prevent the SN evidence from filling the socket by itself.

Finally use \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. The exhausted read surface is exactly the displayed $S,N,U,H,C,P,L$ route plus $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Hence the handshake factors through those rows and leaves $O,D$ open unless a separate closed subject-reduction row is present.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathSNConfluenceDecidabilityHandshake}

\begin{theorem}[MetaCIC confluence-decidability consumer boundary]\label{thm:metacic-critical-path-confluence-decidability-consumer-boundary}
Any confluence-facing or decidability-facing consumer that uses candidate-mediated strong-normalization evidence from $\MetaCICCriticalPathUp$ must consume only the frontier rows of \autoref{def:metacic-critical-path-candidate-mediated-frontier} and the existing handoff row $U$. The consumer may route closed-normal-form membership through $C,P,L$, but it may not treat the frontier as closed subject reduction, full consistency, arbitrary Church--Rosser, global beta-conversion decidability, a discharged socket, or an ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Use \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} to place the candidate-mediated read on the frontier rows and the four local faces. This supplies the SN-facing evidence that the confluence-decidability handoff is allowed to inspect.

Now apply the MetaCIC SN confluence-decidability handoff, \autoref{thm:metacic-critical-path-sn-confluence-decidability-handoff}. It sends closed-normal-form membership decisions through the strong-normalization, confluence, and decidability budget rows and then through $C,P,L$ before the open-phase ledger is exported.

Finally keep the socket and obstruction boundaries visible. The frontier does not fill $D$, and the handoff theorem does not erase $O$ or convert $U$ into a global theorem. Therefore the consumer obtains only the finite candidate-mediated boundary plus the existing handoff route, while the excluded subject-reduction, consistency, Church--Rosser, beta-decidability, socket-discharge, and completeness claims remain outside the packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathConfluenceDecidabilityConsumerBoundary}

\begin{theorem}[MetaCIC candidate-mediated SN discharge row]
\label{thm:metacic-critical-path-candidate-mediated-sn-discharge-row}
For every accepted $\MetaCICCriticalPathUp$ packet
$$
K=(S,N,O,U,D,H,C,P,L),
$$
any candidate-mediated strong-normalization read satisfying the frontier of \autoref{def:metacic-critical-path-candidate-mediated-frontier} can be used by a confluence-facing or decidability-facing consumer only while $D$ remains a displayed discharge socket separate from $S,N,U,H,C,P,L$ and from the four local L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. No such consumer may reinterpret the frontier as closed subject reduction, arbitrary strong normalization, full consistency, beta-conversion decidability, quotient term equality, host equality, selected limits, or $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNDischargeRow}
\begin{proof}
First apply the candidate-mediated SN frontier theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places the read on $S,N,U,D,H,C,P,L$ together with the four local L10 faces, while keeping the obstruction row $O$ and discharge socket $D$ available to the surrounding packet.

Next apply the consumer boundary theorem, \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. A confluence-facing or decidability-facing consumer may inspect the same frontier rows and the handoff row $U$, but the theorem does not fill $D$ or promote the frontier to a closed metatheoretic result.

Finally use the handoff exhaustion route of \autoref{thm:metacic-critical-path-confluence-decidability-handoff-exhaustion}. That route keeps downstream reads of $U$ mediated by $O,D,H,C,P,L$ and the finite packet rows $S,N,U$. Therefore $D$ remains a separate displayed discharge row, distinct from the packet rows and the four L10 faces, and the excluded subject-reduction, normalization, consistency, decidability, quotient, host-equality, selected-limit, and completeness claims remain outside the consumer surface.
\end{proof}

\begin{theorem}[MetaCIC discharge-socket induction]
\label{thm:metacic-critical-path-discharge-socket-induction}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet, and let $F$ be a finite closed-term frontier whose rows replay the candidate-mediated strong-normalization row $S$, the normal-form endpoint $N$, the handoff row $U$, the componentwise transport row $H$, the displayed continuation row $C$, the provenance row $P$, and the local naming row $L$. If every row of $F$ is consumed through the frontier of \autoref{def:metacic-critical-path-candidate-mediated-frontier}, then induction over $F$ preserves the obstruction row $O$ and keeps the discharge socket $D$ unfilled unless the packet also contains an explicit closed subject-reduction row. In particular, candidate-mediated strong normalization, confluence-facing replay, and decidability-facing replay do not by themselves introduce a closed subject-reduction coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathDischargeSocketInduction}
\begin{proof}
Induct over the finite frontier rows. The empty frontier exposes no consumer row, so the accepted packet remains exactly $K=(S,N,O,U,D,H,C,P,L)$ with $O$ and $D$ still visible.

For the induction step, project the next closed-term row and the accumulated packet surface. The row is allowed to replay only the displayed $S,N,U,H,C,P,L$ route named by \autoref{def:metacic-critical-path-candidate-mediated-frontier}; it has no coordinate for closed subject reduction.

Use the candidate-mediated SN frontier theorem and the confluence-decidability consumer boundary to keep the replayed row on the same frontier surface. These results route the SN, confluence-facing, and decidability-facing reads through $U$ and $C$ while leaving the obstruction row $O$ and the discharge socket $D$ in the surrounding packet.

The inductive hypothesis applies to the remaining frontier. Since neither the current replay row nor the previous frontier rows add an explicit closed subject-reduction coordinate, no constructor fills $D$. Thus the obstruction row is preserved and the discharge socket stays open until a separate subject-reduction row is supplied.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization budget]
\label{thm:metacic-critical-path-candidate-normalization-budget}
For every accepted $\MetaCICCriticalPathUp$ packet
$$
K=(S,N,O,U,D,H,C,P,L),
$$
every confluence-facing or decidability-facing use of candidate-mediated strong-normalization evidence is budgeted by the finite rows $S,N,U,H,C,P,L$ together with the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. The obstruction row $O$ and discharge socket $D$ remain visible boundary rows throughout the read. The budget exports no closed subject reduction, arbitrary strong normalization, full confluence, global beta-conversion decidability, quotient term equality, host equality, selected limits, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First apply the candidate-mediated SN frontier theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places any candidate-mediated read on the finite rows $S,N,U,D,H,C,P,L$ and the four L10 faces, with $O$ and $D$ still visible to the surrounding packet.

Next use the confluence-decidability consumer boundary, \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. A confluence-facing or decidability-facing consumer may inspect the frontier and the handoff row $U$, but it receives no closed subject-reduction row, no full confluence theorem, and no global beta-conversion decision procedure.

Apply the candidate-mediated SN discharge row and the discharge-socket induction, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. These results keep $D$ separate from the budget rows and preserve $O$ while finite frontier rows are consumed. Since the packet has no coordinate for quotient term equality, host equality, selected limits, or $\RealUp$ completeness, the displayed budget is the whole normalization surface available to the consumer.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateNormalizationBudget}

\begin{theorem}[MetaCIC candidate-normalization L10 source exactness]
\label{thm:metacic-critical-path-candidate-normalization-l10-source-exactness}
For every accepted $\MetaCICCriticalPathUp$ packet
$$
K=(S,N,O,U,D,H,C,P,L),
$$
every candidate-normalization consumer whose budget is certified by \autoref{thm:metacic-critical-path-candidate-normalization-budget} reads exactly the four L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
together with the finite packet rows $S,N,U,H,C,P,L$. The obstruction row $O$ and the discharge socket $D$ remain visible throughout the read. The source-exact budget exports no closed subject reduction, global confluence, beta-conversion decidability, selected limit, quotient equality, host equality, or $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateNormalizationL10SourceExactness}
\begin{proof}
Start with the candidate-mediated SN frontier, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places the consumer on the displayed strong-normalization rows and the four L10 faces, while keeping the obstruction row and discharge socket in the surrounding packet.

Use the confluence-decidability consumer boundary, \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. A confluence-facing or decidability-facing replay may inspect $U$ and the displayed frontier, but it receives no global confluence theorem, no beta-conversion decision procedure, and no closed subject-reduction row.

Apply discharge-socket induction, \autoref{thm:metacic-critical-path-discharge-socket-induction}. Induction over the finite frontier preserves $O$ and leaves $D$ unfilled unless a separate closed subject-reduction coordinate is displayed.

Finally use candidate-normalization socket exhaustion, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. Once $S,N,U,H,C,P,L$ and the four L10 faces have been consumed, no packet row remains from which to read selected limits, quotient equality, host equality, Real completeness, closed subject reduction, global confluence, or beta-decidability. The budget is therefore source-exact for exactly the named L10 route.
\end{proof}

\begin{theorem}[MetaCIC L10 normalization frontier]
\label{thm:metacic-criticalpath-l10-normalization-frontier}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated strong-normalization discharge route that displays the Real source lock, the $\RegSeqRatUp$ handoff, the $\StreamNameUp$ window, the $\DyadicRatCoreUp$ tolerance row, the confluence socket, and the decidable-boundary row remains on the finite L10 normalization frontier before any consistency-facing claim is exported. The frontier is the displayed route through $S,N,O,U,D,H,C,P,L$ and the four L10 faces; it is not full subject reduction, full confluence, global beta-conversion decidability, quotient term equality, ambient $\RealUp$ completeness, or kernel consistency.
\end{theorem}
\begin{proof}
First read the candidate-mediated frontier L10 readback and Real source lock, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback} and \autoref{thm:metacic-critical-path-candidate-frontier-real-source-lock}. These force the Real-facing route to expose the dyadic, stream, regular-rational, and Real rows before the handoff row is consumed.

Next apply the RegSeqRat handoff theorem, \autoref{thm:metacic-critical-path-candidate-frontier-regseqrat-handoff}. It keeps the regular-rational readback on the same finite StreamName and DyadicRatCore route.

Use the SN confluence-decidability handshake and consumer boundary, \autoref{thm:metacic-critical-path-sn-confluence-decidability-handshake} and \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. These allow the handoff to inspect the candidate-mediated frontier only while the confluence socket and decidable-boundary row remain explicit.

Finally apply discharge-socket induction and discharge coverage, \autoref{thm:metacic-critical-path-discharge-socket-induction} and \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. They preserve the discharge socket unless a separate subject-reduction coordinate is displayed, so the route remains an L10 normalization frontier and exports no consistency-facing claim beyond the finite packet.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization socket exhaustion]
\label{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any confluence-facing or decidability-facing replay that uses only the candidate-normalization budget rows $S,N,U,H,C,P,L$ together with the four L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ exhausts the SN-facing surface without filling the subject-reduction socket. The obstruction row $O$ remains visible and the discharge socket $D$ remains unfilled unless the same packet also displays an explicit closed subject-reduction row.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateNormalizationSocketExhaustion}
\begin{proof}
Start from the candidate-mediated SN frontier theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places the candidate-mediated read on the finite frontier and keeps $O$ and $D$ available to the surrounding packet.

Next apply the confluence-decidability consumer boundary, \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. A confluence-facing or decidability-facing consumer may inspect the frontier rows and the handoff row $U$, but the boundary gives no closed subject-reduction coordinate.

Use the candidate-mediated SN discharge row and discharge-socket induction, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. These two results preserve the separation between the replay rows $S,N,U,H,C,P,L$ and the socket row $D$ while finite frontier rows are consumed.

Finally apply the candidate-normalization budget theorem, \autoref{thm:metacic-critical-path-candidate-normalization-budget}. Its budget is exactly the displayed SN-facing surface plus the four L10 faces. Since that inventory omits a closed subject-reduction row, the replay can feed only the permitted consumers; it cannot erase $O$, fill $D$, or export full confluence or global beta-conversion decidability.
\end{proof}

\begin{theorem}[MetaCIC candidate-mediated subject-reduction boundary]
\label{thm:metacic-critical-path-candidate-mediated-sn-subject-reduction-boundary}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated replay over $S,N,U,H,C,P,L$ and the four L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
preserves the visible obstruction row $O$ and the discharge socket $D$. The replay cannot be read as closed subject reduction unless the packet separately displays a subject-reduction row; candidate-mediated strong normalization, confluence-facing replay, and decidability-facing replay do not fill that row by themselves.
\end{theorem}
\begin{proof}
Begin with the candidate-mediated frontier of \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It confines the SN-facing read to the displayed frontier rows and keeps $O,D$ visible to the enclosing packet.

Use the handshake theorem, \autoref{thm:metacic-critical-path-sn-confluence-decidability-handshake}, to pass from candidate-mediated normalization to confluence-facing or decidability-facing replay. The handoff reads $S,N,U,H,C,P,L$ and the four L10 faces, but it does not add a closed subject-reduction coordinate.

Apply the discharge-row and socket-induction theorems, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. They preserve the obstruction row and keep the discharge socket unfilled while finite frontier rows are consumed.

Finally use \autoref{thm:metacic-critical-path-candidate-normalization-budget} and \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. The budget is exhausted by the displayed replay route and the four source faces, so any closed subject-reduction conclusion must come from a separate displayed row. Without that row, the boundary remains exactly $O,D$ plus the finite replay surface.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNSubjectReductionBoundary}

\begin{theorem}[MetaCIC candidate SN boundary]
\label{thm:metacic-critical-path-candidate-sn-boundary}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Candidate-mediated strong-normalization evidence over $S,N,U,H,C,P,L$ and the L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
cannot be consumed as a closed subject-reduction row unless $K$ separately displays a subject-reduction coordinate. The candidate-mediated evidence may feed the frontier and the confluence-decidability handoff, but it does not identify reduction preservation for arbitrary closed terms, fill the discharge socket $D$, or erase the obstruction row $O$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateSNBoundary}
\begin{proof}
First apply the candidate-mediated SN frontier, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places the evidence on the finite rows $S,N,U,H,C,P,L$ together with the four L10 faces before any handoff consumer reads $U$.

Next use the subject-reduction boundary of \autoref{thm:metacic-critical-path-candidate-mediated-sn-subject-reduction-boundary}. This theorem states the exact missing coordinate: closed subject reduction is available only when the packet displays a separate subject-reduction row.

The discharge side is controlled by \autoref{thm:metacic-critical-path-discharge-socket-induction}. Induction over the displayed replay rows preserves the socket as a socket, so the candidate-mediated evidence cannot become a subject-reduction row by finite consumption.

Finally invoke candidate-normalization socket exhaustion, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. The exhausted surface is the candidate-normalization route plus the four L10 faces, with no additional coordinate for closed subject reduction. Hence any consumer that claims such a row must cite it separately, and the candidate-mediated boundary remains distinct from closed subject reduction.
\end{proof}

\begin{theorem}[MetaCIC candidate-mediated SN closed-normal-form readback]
\label{thm:metacic-critical-path-candidate-mediated-sn-closed-normal-form-readback}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any closed-normal-form readback that consumes candidate-mediated strong-normalization evidence factors through the finite rows $S,N,U,H,C,P,L$ and the four L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp .
$$
The obstruction row $O$ and the discharge socket $D$ remain visible to the readback unless the packet separately displays a subject-reduction row. Thus closed-normal-form readback from candidate-mediated SN is a finite frontier read, not a proof of closed subject reduction, global confluence, or beta-conversion decidability.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNClosedNormalFormReadback}
\begin{proof}
Start with the candidate-mediated SN frontier, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It confines candidate-mediated normalization evidence to the displayed finite rows and the four L10 faces before any readback consumer may inspect the handoff row $U$.

Use the handshake theorem, \autoref{thm:metacic-critical-path-sn-confluence-decidability-handshake}. The readback may feed confluence-facing or decidability-facing replay through $S,N,U,H,C,P,L$, but that handoff does not add a subject-reduction row.

Apply discharge-socket induction, \autoref{thm:metacic-critical-path-discharge-socket-induction}. Finite replay preserves the socket boundary while the closed-normal-form consumer moves through the displayed route.

Finally use socket exhaustion and the candidate SN boundary, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion} and \autoref{thm:metacic-critical-path-candidate-sn-boundary}. Together they leave exactly $O$ and $D$ as visible boundaries unless an explicit subject-reduction row is displayed, so the readback cannot be upgraded to closed subject reduction, global confluence, or beta-decidability.
\end{proof}

\begin{theorem}[MetaCIC candidate-mediated SN frontier discharge coverage]
\label{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every finite consumer replay through rows $S,N,U,H,C,P,L$ and the L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
either preserves the displayed discharge socket $D$ or cites an explicit subject-reduction row outside the candidate-mediated frontier. Candidate-mediated strong normalization, confluence-facing replay, decidability-facing replay, and L10 readback do not by themselves discharge $D$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNFrontierDischargeCoverage}
\begin{proof}
Start with the candidate-mediated SN frontier theorem, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places the replay on $S,N,U,D,H,C,P,L$ together with the four L10 faces and keeps the obstruction and discharge rows visible.

Apply the discharge-row theorem and discharge-socket induction, \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row} and \autoref{thm:metacic-critical-path-discharge-socket-induction}. These results preserve the socket while each finite consumer row is replayed.

Use the candidate-normalization budget and socket-exhaustion theorems, \autoref{thm:metacic-critical-path-candidate-normalization-budget} and \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. They show that the consumer surface is exhausted by $S,N,U,H,C,P,L$ and the four L10 faces, with no hidden subject-reduction coordinate.

Finally invoke the subject-reduction boundary, \autoref{thm:metacic-critical-path-candidate-mediated-sn-subject-reduction-boundary}. It is exactly the alternative in the statement: either the replay keeps $D$ displayed, or a separate subject-reduction row outside the candidate-mediated frontier must be cited.
\end{proof}

\begin{definition}[MetaCIC frontier budget factorization]
\label{def:metacic-critical-path-frontier-budget-factorization}
For an accepted $\MetaCICCriticalPathUp$ packet
$$
K=(S,N,O,U,D,H,C,P,L),
$$
the candidate-frontier budget factorization is the displayed packet
$$
\mathcal{F}(K)=
(S,N,U,D,H,C,P,L;\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\RealUp)
$$
read in five layers: the candidate-normalization budget rows $S,N$, the confluence handoff row $U$, the subject-reduction discharge socket $D$, the L10 entry budget $\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\RealUp$, and the closed-normal readback rows $H,C,P,L$. A consumer is factorized through $\mathcal{F}(K)$ exactly when its frontier read exposes those five layers before it exports any strong-normalization, confluence-facing, decidability-facing, Phase Real, or Completion-facing conclusion.
\end{definition}

\begin{theorem}[MetaCIC candidate frontier L10 budget lock]
\label{thm:metacic-critical-path-candidate-frontier-l10-budget-lock}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every candidate-mediated strong-normalization discharge that reaches an L10 readback must pass through the factorization packet $\mathcal{F}(K)$ of \autoref{def:metacic-critical-path-frontier-budget-factorization}. In particular, the route first exposes the candidate-normalization rows $S,N$, the confluence handoff row $U$, and the discharge socket $D$; then it reads $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as the L10 entry budget; only then may it consume $H,C,P,L$ as closed-normal readback rows.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateFrontierL10BudgetLock}
\begin{proof}
Start with discharge coverage, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. It says that any finite consumer replay through the candidate-mediated frontier either preserves the socket $D$ or cites a separate subject-reduction row outside the frontier.

Next apply the candidate-normalization budget theorem, \autoref{thm:metacic-critical-path-candidate-normalization-budget}. It identifies the candidate-facing surface as the rows $S,N,U,H,C,P,L$ together with the four L10 faces, while keeping the obstruction row and discharge socket visible.

Use the closed-normal-form readback and L10 readback theorems, \autoref{thm:metacic-critical-path-candidate-mediated-sn-closed-normal-form-readback} and \autoref{thm:metacic-criticalpath-candidate-mediated-sn-l10-readback}. These force every readback that reaches Phase Real or Completion data to expose $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before the exported rows are consumed.

The five layers listed in \autoref{def:metacic-critical-path-frontier-budget-factorization} are exactly the rows preserved by those three results. Hence any L10-reaching discharge has already passed through $\mathcal{F}(K)$, and no alternate route can bypass the displayed candidate, handoff, discharge, L10, or readback layers.
\end{proof}

\begin{theorem}[MetaCIC normalization budget nonescape]
\label{thm:metacic-critical-path-normalization-budget-nonescape}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. No route from the candidate-mediated frontier exports strong normalization, confluence, beta-conversion decidability, or Real-completion entry unless it displays the candidate rows $S,N$, the handoff row $U$, the discharge socket $D$, the L10 entry budget $\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\RealUp$, and the closed-normal readback rows $H,C,P,L$ as the factorization packet $\mathcal{F}(K)$. The route exports no closed subject reduction, quotient term equality, host equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency beyond that packet.
\end{theorem}
\begin{proof}
First read the candidate frontier L10 budget lock, \autoref{thm:metacic-critical-path-candidate-frontier-l10-budget-lock}. Any discharge that reaches L10 readback is already forced through the displayed factorization packet.

Next use socket exhaustion and subject-reduction boundary, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion} and \autoref{thm:metacic-critical-path-candidate-mediated-sn-subject-reduction-boundary}. These keep $D$ visible unless a separate subject-reduction row is cited, so the candidate budget cannot become closed subject reduction by replay alone.

Apply the candidate-mediated SN route and normal-form discharge theorems, \autoref{thm:metacic-critical-path-candidate-mediated-sn-route} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge}. They confine strong-normalization, confluence-facing, decidability-facing, and normal-form exports to the finite rows in $\mathcal{F}(K)$.

Since quotient term equality, host equality, selected limits, ambient $\RealUp$ completeness, and kernel consistency are not coordinates of $\mathcal{F}(K)$, no route that only reads the candidate-mediated frontier can export them. Real-completion entry is therefore a budgeted L10 readback, not a global metatheoretic escape.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathNormalizationBudgetNonescape}

\begin{theorem}[MetaCIC candidate-mediated SN route]
\label{thm:metacic-critical-path-candidate-mediated-sn-route}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated strong-normalization evidence consumed by confluence-facing or decidability-facing replay is routed through $S,N,U,H,C,P,L$, the visible obstruction row $O$, the discharge socket $D$, and the four L10 faces
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp .
$$
A separate subject-reduction row is required before $D$ can be filled. Candidate-mediated normalization evidence alone cannot assert global confluence, closed subject reduction, or beta-conversion decidability.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNRoute}
\begin{proof}
Begin with the candidate-mediated SN frontier, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It places normalization evidence on the finite route through $S,N,U,H,C,P,L$ while keeping $O$ and $D$ visible to any consumer.

Apply the candidate-normalization budget, \autoref{thm:metacic-critical-path-candidate-normalization-budget}. The budget available to confluence-facing and decidability-facing replay is exhausted by the displayed SN route and the four L10 faces.

Next use socket exhaustion, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}. Since exhaustion still leaves $D$ as a socket rather than a filled row, the replay cannot close subject reduction by consuming candidate-mediated SN evidence alone.

Finally invoke discharge coverage, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. Every admissible consumer either preserves $D$ or cites a separate subject-reduction row. Therefore the route exports only the finite candidate-mediated handoff and refuses global confluence, closed subject reduction, and beta-decidability claims.
\end{proof}

\begin{theorem}[MetaCIC candidate-mediated SN discharge budget]
\label{thm:metacic-criticalpath-candidate-mediated-sn-discharge-budget}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every candidate-mediated strong-normalization consumer that is allowed to pass a consistency-facing handoff must first display the normalization candidate row $S$, the confluence boundary carried by the handoff row $U$, the decidability boundary carried by the replay row $C$, the discharge socket $D$, the obstruction row $O$, componentwise transport $H$, replay $C$, provenance $P$, and the local $\NameCert$ row $L$. The consumer may then read the handoff only as the displayed finite discharge budget; it does not export full subject reduction, full confluence, global beta-conversion decidability, quotient term equality, or kernel consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNDischargeBudget}
\begin{proof}
Begin with the candidate-mediated SN frontier, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It forces every SN read that reaches the handoff row to expose the finite packet rows and the four L10 faces before a confluence-facing or decidability-facing consumer can inspect $U$.

Next apply the socket-separation and discharge-row theorems, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-socket-separation} and \autoref{thm:metacic-critical-path-candidate-mediated-sn-discharge-row}. These keep $O$ and $D$ visible while the normalization candidate and handoff rows are consumed.

Use discharge-socket induction and discharge coverage, \autoref{thm:metacic-critical-path-discharge-socket-induction} and \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. They show that any finite consumer replay either preserves the socket or cites a separate subject-reduction row outside the candidate-mediated frontier.

Finally read the candidate SN route of \autoref{thm:metacic-critical-path-candidate-mediated-sn-route}. The route exports only the displayed budget rows $S,N,O,U,D,H,C,P,L$ and therefore cannot supply the excluded global MetaCIC or quotient-equality claims.
\end{proof}

\begin{theorem}[MetaCIC candidate-mediated SN L10 readback]
\label{thm:metacic-criticalpath-candidate-mediated-sn-l10-readback}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any L10-facing read of the candidate-mediated SN frontier routes through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and the subject-reduction discharge socket $D$ before it exports a Phase Real row or a Completion budget row. The exported rows are the finite readback surface of the same packet; they are not full subject reduction, full confluence, global beta-conversion decidability, quotient term equality, ambient $\RealUp$ completeness, or kernel consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNL10Readback}
\begin{proof}
Start from the existing L10 readback theorem, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-l10-readback}. It requires $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ to be exposed before the handoff row $U$ is consumed.

Apply the closed-normal-form readback theorem and candidate-normalization budget, \autoref{thm:metacic-critical-path-candidate-mediated-sn-closed-normal-form-readback} and \autoref{thm:metacic-critical-path-candidate-normalization-budget}. These identify the Phase Real and Completion-facing budget as the finite surface $S,N,U,H,C,P,L$ together with the four L10 faces.

Next use the subject-reduction boundary, \autoref{thm:metacic-critical-path-candidate-mediated-sn-subject-reduction-boundary}. It keeps the discharge socket $D$ visible unless a separate subject-reduction row is displayed.

Finally combine socket exhaustion with \autoref{thm:metacic-criticalpath-candidate-mediated-sn-discharge-budget}. Once the L10 route and candidate-mediated rows have been read, the packet has no remaining coordinate for full subject reduction, global confluence, beta-conversion decidability, quotient term equality, ambient $\RealUp$ completeness, or kernel consistency. Thus the Phase Real and Completion rows are budget rows, not global metatheoretic exports.
\end{proof}

\begin{theorem}[MetaCIC candidate-mediated normal-form discharge]
\label{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A candidate-mediated strong-normalization consumer can export a normal-form discharge only after displaying the normalization candidate row $S$, the closed normal-form readback row $N$, the subject-reduction discharge socket $D$, the confluence boundary carried by $U$, the decidability boundary carried by $C$, the L10 rows
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp ,
$$
componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert$ row $L$. The exported discharge is exactly this finite normal-form boundary; it is not full subject reduction, full confluence, global beta-conversion decidability, quotient term equality, ambient $\RealUp$ completeness, or kernel consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedFrontierNormalFormDischarge}
\begin{proof}
First read the candidate-mediated SN frontier and the discharge budget, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-criticalpath-candidate-mediated-sn-discharge-budget}. These force the normalization candidate, handoff boundary, decidability replay, socket, obstruction, transport, replay, provenance, and local naming rows to be visible before a consistency-facing consumer can inspect the handoff.

Next apply the closed-normal-form readback and L10 readback theorems, \autoref{thm:metacic-critical-path-candidate-mediated-sn-closed-normal-form-readback} and \autoref{thm:metacic-criticalpath-candidate-mediated-sn-l10-readback}. They attach the normal-form endpoint to $S,N,U,H,C,P,L$ and require the four rows $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any Phase Real or Completion budget is exported.

Finally use socket separation and discharge coverage, \autoref{thm:metacic-critical-path-candidate-mediated-frontier-socket-separation} and \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. These keep the subject-reduction socket visible unless a separate subject-reduction row is cited, so the consumer may export only the finite normal-form discharge surface stated above. The excluded global MetaCIC and quotient-equality claims are not coordinates of that surface.
\end{proof}

\begin{theorem}[MetaCIC subject-reduction socket before normal-form discharge consumers]
\label{thm:metacic-critical-path-candidate-mediated-frontier-subject-reduction-socket}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any candidate-mediated strong-normalization handoff that reaches the normal-form discharge surface of \autoref{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge} must still expose the subject-reduction socket row $D$ as a separate row before a closed subject-reduction consumer may proceed. The handoff may use the candidate row $S$, the closed normal-form readback row $N$, the handoff row $U$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, provenance $P$, local naming $L$, and the L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, but it cannot treat that surface as global confluence, beta-decidability, quotient term equality, selected limits, host equality, ambient $\RealUp$ completeness, or a closed subject-reduction row.
\end{theorem}
\begin{proof}
Start from the candidate-mediated frontier and the candidate SN boundary, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-candidate-sn-boundary}. They place the SN handoff on the displayed rows $S,N,U,H,C,P,L$ together with the four L10 faces while keeping $O$ and $D$ visible.

Next apply closed-normal-form readback and normal-form discharge, \autoref{thm:metacic-critical-path-candidate-mediated-sn-closed-normal-form-readback} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge}. These results allow the consumer to reach the finite normal-form discharge surface, but that surface still lists $D$ as a subject-reduction socket rather than as a discharged row.

Finally use socket exhaustion, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion}, together with socket separation in \autoref{thm:metacic-critical-path-candidate-mediated-frontier-socket-separation}. Once the candidate-mediated rows and L10 faces have been consumed, no hidden coordinate remains for closed subject reduction. A closed subject-reduction consumer must therefore cite a separate subject-reduction row before proceeding, and the refused confluence, beta-decidability, quotient-equality, selected-limit, host-equality, and Real-completeness claims remain outside the packet.
\end{proof}

\begin{theorem}[MetaCIC subject-reduction socket readiness]
\label{thm:metacic-criticalpath-subject-reduction-socket-readiness}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A subject-reduction consumer may inspect the normal-form discharge only after the candidate row $S$, normal-form row $N$, handoff row $U$, subject-reduction socket $D$, and the L10 rows
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
are all visible. Candidate-mediated strong normalization by itself is readiness data for the socket; it is not a closed subject-reduction proof, not full confluence, not beta-conversion decidability, not quotient term equality, and not ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the subject-reduction socket theorem \autoref{thm:metacic-critical-path-candidate-mediated-frontier-subject-reduction-socket}. It keeps $D$ separate from $S,N,U,H,C,P,L$ and requires a closed subject-reduction consumer to cite a separate row before proceeding.

Next read the normal-form discharge theorem \autoref{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge}. Its discharge surface lists the candidate row, the normal-form endpoint, the handoff row, the socket, the structural rows, and the four L10 faces as the finite boundary visible to the consumer.

Apply the L10 readback theorem \autoref{thm:metacic-criticalpath-candidate-mediated-sn-l10-readback} and the dependency seal \autoref{thm:metacic-criticalpath-candidate-sn-l10-dependency-seal}. These force $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ to be displayed before Phase Real, Completion, or confluence-facing inspection of the normal-form boundary.

Finally use socket exhaustion and discharge coverage, \autoref{thm:metacic-critical-path-candidate-normalization-socket-exhaustion} and \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. They leave no hidden coordinate that can turn candidate-mediated SN into closed subject reduction, so the visible rows are readiness data only.
\end{proof}

\begin{theorem}[MetaCIC candidate SN L10 dependency seal]
\label{thm:metacic-criticalpath-candidate-sn-l10-dependency-seal}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every candidate-mediated strong-normalization packet whose discharge reads Real-facing evidence must route through the displayed dependency rows
$$
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegSeqRatUp,\qquad
\RealUp
$$
before any MetaCIC confluence consumer can inspect the normal-form boundary. The inspection exports only the candidate-mediated frontier and its finite L10 seal; it does not export full subject reduction, full confluence, global beta-conversion decidability, quotient term equality, ambient $\RealUp$ completeness, or kernel consistency.
\end{theorem}
\begin{proof}
Start from the candidate-mediated frontier definition and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge}. They identify the normal-form discharge boundary as the finite rows $S,N,U,D,H,C,P,L$ together with the four L10 dependency rows.

Apply the L10 readback theorem \autoref{thm:metacic-criticalpath-candidate-mediated-sn-l10-readback}. It requires $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ to be displayed before a Phase Real or Completion-facing row can be exported.

Next use the RegSeqRat handoff theorem \autoref{thm:metacic-critical-path-candidate-frontier-regseqrat-handoff}. The regular rational route is the unique readback path available to the candidate-mediated handoff, so no alternate Real-facing route can bypass the four-object seal.

Finally apply the confluence-decidability consumer boundary, \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. A confluence consumer may inspect only the displayed frontier after the L10 route has been exposed; the excluded global MetaCIC, quotient, completeness, and consistency claims are not packet coordinates.
\end{proof}

\begin{theorem}[MetaCIC confluence residual frontier route]
\label{thm:metacic-criticalpath-confluence-residual-frontier-route}
Every confluence residual frontier read of an accepted $\MetaCICCriticalPathUp$ packet consumes the same candidate-mediated strong-normalization row and the same L10 route
$$
\DyadicRatCoreUp,\quad
\StreamNameUp,\quad
\RegSeqRatUp,\quad
\RealUp .
$$
Consequently residual substitution and critical-pair reads cannot bypass the finite four-object seal before reading the normal-form boundary or the confluence-decidability handoff.
\end{theorem}
\begin{proof}
Read the candidate-mediated SN frontier theorem together with \autoref{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge}. Both place the candidate row, normal-form endpoint, discharge socket, handoff boundary, and four L10 rows on the same finite frontier.

Use \autoref{thm:metacic-criticalpath-candidate-mediated-sn-l10-readback} and \autoref{thm:metacic-critical-path-candidate-frontier-regseqrat-handoff}. These force any Real-facing or Completion-facing residual read to pass through the displayed dyadic, stream, regular-rational, and Real rows.

Finally apply the confluence-decidability consumer boundary, \autoref{thm:metacic-critical-path-confluence-decidability-consumer-boundary}. Residual substitution and critical-pair consumers can read the handoff only after the candidate SN frontier and L10 route are present, so no residual route escapes to full confluence, closed subject reduction, beta-decidability, quotient equality, selected limits, or ambient Real completeness.
\end{proof}

\begin{theorem}[MetaCIC SN discharge frontier]
\label{thm:metacic-critical-path-sn-discharge-frontier}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A candidate-mediated strong-normalization route may hand a normal-form consumer to the discharge frontier only while the subject-reduction socket $D$ and obstruction row $O$ remain visible. The confluence residual route, decidability handoff, and L10 readback rows may transport $S,N,U,H,C,P,L$, but they do not close subject reduction, fill $D$, erase $O$, or export global confluence, beta-conversion decidability, quotient term equality, host equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency.
\end{theorem}
\begin{proof}
Start from the candidate SN boundary and the normal-form discharge theorem, \autoref{thm:metacic-critical-path-candidate-sn-boundary} and \autoref{thm:metacic-critical-path-candidate-mediated-frontier-normal-form-discharge}. They place the normal-form consumer on the finite rows $S,N,U,D,H,C,P,L$ together with the L10 faces.

Next use subject-reduction socket readiness, \autoref{thm:metacic-criticalpath-subject-reduction-socket-readiness}. It permits inspection of the normal-form discharge only after $D$ is displayed as a socket, not as a closed subject-reduction proof.

Finally apply the confluence residual frontier route, \autoref{thm:metacic-criticalpath-confluence-residual-frontier-route}. Confluence, decidability, and L10 readback keep the same finite frontier visible, so they cannot discharge $D$ or erase $O$ without a separate closed subject-reduction row.
\end{proof}
