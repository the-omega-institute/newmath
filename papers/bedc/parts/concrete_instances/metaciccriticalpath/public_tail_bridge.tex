\begin{theorem}[MetaCIC critical path candidate-mediated SN boundary]
\label{thm:metaciccriticalpath-candidate-mediated-sn-boundary}
For every accepted $\MetaCICCriticalPathUp$ packet $K=(S,N,O,U,D,H,C,P,L)$, a normalization-boundary read that displays CandidateMediatedSN evidence, the closed-term substitution boundary, FiniteObservationConfluence, the SubjectReductionDischargeSocket row, and MetaCIC closure-trace rows can be exposed only through those displayed rows together with the $\RealUp$, $\RegSeqRatUp$, and $\StreamNameUp$ support already carried by the chapter dependency route. The read is a finite boundary certificate; it is not global strong normalization, host confluence, semantic model completeness, closed subject reduction, or a filled discharge socket.
\end{theorem}
\begin{proof}
Project the accepted critical-path packet. The rows $S,N,O,U,D,H,C,P,L$ are the only local coordinates, and the candidate-mediated normalization read begins at the displayed $S$ route rather than at a global normalization theorem.

Attach the boundary rows in their finite order. Closed-term substitution and finite-observation confluence are read through the handoff row $U$, the subject-reduction socket remains the displayed row $D$, and closure-trace information is replayed only through $C$ with provenance and naming in $P,L$.

Now use the L10 dependency route already recorded for the chapter. Its $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ support rows keep the boundary read attached to finite observation schedules, rational readback, and the terminal seal before any public consumer can inspect it.

The obstruction and socket theorems keep the excluded conclusions out of the packet. The same projection that exposes the candidate-mediated boundary refuses global strong normalization, host confluence, semantic model completeness, closed subject reduction, and any completed discharge for $D$.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNBoundary\_semantic\_name\_certificate}

\begin{theorem}[MetaCIC candidate-mediated SN public handoff]
\label{thm:metaciccriticalpath-candidate-mediated-sn-public}
For every accepted $\MetaCICCriticalPathUp$ carrier $K=(S,N,O,U,D,H,C,P,L)$, the public candidate-mediated strong-normalization handoff is the finite readiness surface formed by the candidate-mediated SN rows, the normalization-frontier rows, the confluence-decidability budget, and the subject-reduction discharge socket. A consumer may cite this surface only as a route through $S,N,O,U,D,H,C,P,L$ together with the displayed CandidateMediatedSN and CandidateSNConfluenceBoundary rows. It cannot cite the surface as full MetaCIC strong normalization, closed subject reduction, full confluence, full closed consistency, or a filled discharge bundle.
\end{theorem}
\begin{proof}
Start with the candidate-mediated obligation surface. Theorems~\ref{thm:metacic-confluence-candidate-mediated-sn-obligation-surface}, \ref{thm:metaciccriticalpath-candidate-mediated-sn-frontier-coverage}, and \ref{thm:metacic-critical-path-candidate-sn-readiness} identify the candidate-mediated SN rows as finite packet data rather than as a global normalization theorem.

Next read the public carrier route. Theorems~\ref{thm:metacic-critical-path-normalization-frontier-factorization}, \ref{thm:metacic-critical-path-frontier-coverage}, and \ref{thm:metacic-critical-path-normalization-obstruction-route-coverage} keep the normalization-frontier endpoint attached to $S,N,O,H,C,P,L$ and keep the obstruction row visible.

Finally attach the socket and handoff rows. Theorems~\ref{thm:metacic-critical-path-discharge-socket-nonescape}, \ref{thm:metacic-critical-path-obstruction-socket-separation}, \ref{thm:metacic-critical-path-handoff-row-coverage}, and \ref{thm:metacic-critical-path-sn-discharge-socket-readiness} keep $D$ separate from the candidate-mediated rows and keep $U$ as a handoff row. Thus the public handoff is a readiness surface and not a proof of full MetaCIC SN, closed subject reduction, full confluence, full closed consistency, or socket discharge.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedSNPublicHandoff}

\begin{theorem}[MetaCIC confluence frontier public handoff]
\label{thm:metaciccriticalpath-confluence-frontier-public}
For every accepted $\MetaCICCriticalPathUp$ carrier $K=(S,N,O,U,D,H,C,P,L)$, every public confluence-frontier read factors through CandidateMediatedSN, CandidateSNConfluenceBoundary, FiniteObservationConfluence, MetaCICNormalizationFrontier, and SubjectReductionDischargeSocket rows only as displayed packet obligations. The route may expose finite-observation confluence readiness and confluence-decidability handoff data, but it exports no arbitrary Church--Rosser theorem, no global beta-conversion decision procedure, no full MetaCIC confluence theorem, and no closed subject-reduction discharge.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathConfluenceFrontierPublicHandoff}
\begin{proof}
Read the confluence-facing packet rows first. Theorems~\ref{thm:metacic-critical-path-confluence-decidability-socket}, \ref{thm:metacic-critical-path-sn-confluence-budget}, and \ref{thm:metacic-critical-path-confluence-decidability-consumer-boundary} force confluence and decidability uses through $U$ together with the obstruction, socket, transport, replay, provenance, and local naming rows.

Next attach the candidate and finite-observation frontier. Theorems~\ref{thm:metacic-critical-path-candidate-frontier-confluence-socket-boundary}, \ref{thm:metacic-critical-path-sn-confluence-decidability-handshake}, and \ref{thm:metacic-critical-path-confluence-candidate-mediation} keep CandidateMediatedSN and CandidateSNConfluenceBoundary as mediated readiness rows, while the finite-observation confluence side remains bounded by the same packet route.

Finally use the public handoff theorem above together with discharge-socket coverage, \autoref{thm:metaciccriticalpath-candidate-mediated-sn-public} and \autoref{thm:metacic-critical-path-discharge-socket-coverage}. The subject-reduction socket remains visible and unfilled, so the confluence frontier is a public readiness surface over finite BEDC rows rather than a global Church--Rosser, beta-decidability, full-confluence, or subject-reduction theorem.
\end{proof}

\begin{theorem}[MetaCIC critical-path formal target router]
\label{thm:metaciccriticalpath-formal-target-router}
For every accepted $\MetaCICCriticalPathUp$ carrier $K=(S,N,O,U,D,H,C,P,L)$, any candidate-mediated strong-normalization consumer for $\MetaCICCriticalPathUp$ must pass through the candidate-mediated SN public boundary of \autoref{thm:metaciccriticalpath-candidate-mediated-sn-public}, the confluence frontier of \autoref{thm:metaciccriticalpath-confluence-frontier-public}, and the L10 normalization discharge budget of \autoref{thm:metaciccriticalpath-l10-normalization-discharge-budget} before any $\RealUp$, $\RegSeqRatUp$, or $\StreamNameUp$ discharge row is read. The router exposes a finite target route through $S,N,O,U,D,H,C,P,L$; it does not export hidden infinite reduction, full MetaCIC strong normalization, ambient real completeness, or a completed subject-reduction socket.
\end{theorem}
\begin{proof}
Begin with the candidate-mediated SN public boundary. It places CandidateMediatedSN and CandidateSNConfluenceBoundary on the displayed carrier route and prevents a consumer from treating them as full strong normalization.

Next attach the confluence frontier. The frontier keeps FiniteObservationConfluence and MetaCICNormalizationFrontier on the same public route, while the subject-reduction socket remains visible as a socket rather than as a closed theorem.

Finally use the L10 normalization discharge budget. That budget orders the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ discharge faces behind the finite source rows and the socket-aware route.

The nonescape clauses from these three surfaces compose: no hidden infinite reduction, ambient real-completeness claim, or closed subject-reduction discharge is available outside the displayed packet. Hence every formal target consumer must pass through the stated router before reading the L10 discharge rows.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathFormalTargetRouter}

\begin{theorem}[MetaCIC critical-path public export]
\label{thm:metacic-critical-path-public-export}
For every accepted $\MetaCICCriticalPathUp$ carrier
$$
K=(S,N,O,U,D,H,C,P,L),
$$
the public export surface is exactly the finite route through CandidateMediatedSN, CandidateSNConfluenceBoundary, FiniteObservationConfluence, MetaCICNormalizationFrontier, SubjectReductionDischargeSocket, and the carrier rows $S,N,O,U,D,H,C,P,L$. This export refuses full MetaCIC strong normalization, closed subject reduction, full confluence, full closed consistency, and discharge of the socket $D$.
\end{theorem}
\begin{proof}
First read the candidate-mediated SN public handoff, \autoref{thm:metaciccriticalpath-candidate-mediated-sn-public}. It presents CandidateMediatedSN and CandidateSNConfluenceBoundary only as finite readiness rows over the carrier route.

Next read the confluence-frontier public handoff, \autoref{thm:metaciccriticalpath-confluence-frontier-public}. It attaches FiniteObservationConfluence and MetaCICNormalizationFrontier to the same carrier rows, while keeping confluence and decidability uses bounded by the displayed handoff data.

Finally use discharge-socket coverage and obstruction-socket separation, \autoref{thm:metacic-critical-path-discharge-socket-coverage} and \autoref{thm:metacic-critical-path-obstruction-socket-separation}. These keep SubjectReductionDischargeSocket visible but unfilled, so $D$ remains a socket coordinate rather than a closed subject-reduction proof or a source of the excluded global theorem exports.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathPublicExport}

\begin{theorem}[MetaCIC critical-path candidate ledger public boundary]
\label{thm:metacic-critical-path-candidate-ledger-public-boundary}
Every public candidate-ledger read of an accepted $\MetaCICCriticalPathUp$ carrier $K=(S,N,O,U,D,H,C,P,L)$ must pass through the L10 four-object exit rows already listed for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. The ledger may expose CandidateMediatedSN, CandidateSNConfluenceBoundary, FiniteObservationConfluence, MetaCICNormalizationFrontier, SubjectReductionDischargeSocket, and the carrier route $S,N,O,U,D,H,C,P,L$, but it cannot claim global strong normalization, full confluence, global decidability, closed subject reduction, closed consistency, discharge of $D$, or Lean verification.
\end{theorem}
\begin{proof}
Project the accepted carrier and read the public export theorem \autoref{thm:metacic-critical-path-public-export}. The export surface is already the finite candidate-mediated route through the carrier rows and the visible subject-reduction socket.

Now project the L10 exit budget from \autoref{thm:metacic-critical-path-l10-four-object-exit-certificate}. Any public candidate-ledger read must pass through the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows before it reaches a downstream consumer.

The two projections compose without adding theorem content. The candidate ledger records readiness rows and visible sockets over the four-object exit surface, while the excluded global SN, confluence, decidability, subject-reduction, consistency, socket-discharge, and Lean-verification claims remain outside the carrier.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateLedgerPublicBoundary}

\begin{theorem}[MetaCIC critical-path public consumer surface]
\label{thm:metacic-critical-path-public-consumer-surface}
Every public consumer of the $\MetaCICCriticalPathUp$ critical path must route through CandidateMediatedSN, CandidateSNConfluenceBoundary, FiniteObservationConfluence, MetaCICNormalizationFrontier, SubjectReductionDischargeSocket, and the carrier rows
$$
S,N,O,U,D,H,C,P,L.
$$
The consumer surface may expose candidate-mediated normalization readiness, confluence-frontier readiness, finite-observation confluence data, normalization-frontier data, the visible subject-reduction socket, and the displayed carrier route, but it exports no closed subject reduction, full confluence, full closed consistency, or socket discharge.
\end{theorem}
\begin{proof}
First read \autoref{thm:metaciccriticalpath-candidate-mediated-sn-public}. It keeps CandidateMediatedSN and CandidateSNConfluenceBoundary on the public readiness surface and routes them through the carrier rows $S,N,O,U,D,H,C,P,L$.

Next read \autoref{thm:metaciccriticalpath-confluence-frontier-public}. It attaches FiniteObservationConfluence and MetaCICNormalizationFrontier to the same displayed packet route and refuses arbitrary Church--Rosser, global beta-decision, full confluence, and closed subject-reduction exports.

Finally read \autoref{thm:metacic-critical-path-public-export}. It names the public export surface as exactly the route through CandidateMediatedSN, CandidateSNConfluenceBoundary, FiniteObservationConfluence, MetaCICNormalizationFrontier, SubjectReductionDischargeSocket, and $S,N,O,U,D,H,C,P,L$. Therefore a public consumer inherits that exact route and none of the excluded closed subject-reduction, full-confluence, full-consistency, or socket-discharge conclusions.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathPublicConsumerSurface}

\begin{theorem}[MetaCIC critical-path residual confluence bridge boundary]
\label{thm:metacic-critical-path-bridge-boundary}
Every residual-substitution route that consumes the candidate-mediated SN row, the confluence-frontier row, and the subject-reduction obstruction row of an accepted $\MetaCICCriticalPathUp$ packet must consume them only through the displayed MetaCIC critical-path ledger $S,N,O,U,D,H,C,P,L$ together with the public candidate-ledger and confluence-frontier handoff rows. The bridge may pass residual-confluence readiness to a downstream consumer, but it cannot treat the blocked subject-reduction edge as a closed theorem, discharge socket $D$, or replace the critical-path ledger by a host confluence proof.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathBridgeBoundary}
\begin{proof}
First project the confluence-frontier slice through \autoref{thm:metaciccriticalpath-confluence-frontier-public}. This places CandidateMediatedSN, CandidateSNConfluenceBoundary, FiniteObservationConfluence, MetaCICNormalizationFrontier, and SubjectReductionDischargeSocket on the same displayed route.

Next apply the candidate-ledger public boundary, \autoref{thm:metacic-critical-path-candidate-ledger-public-boundary}. It forces the residual-substitution consumer to pass through the L10 exit rows and the packet ledger $S,N,O,U,D,H,C,P,L$ before any public read is available.

Now apply the confluence public handoff, \autoref{thm:metacic-critical-path-public-consumer-surface}. The handoff permits only the finite readiness surface, so residual confluence is read as a bridge boundary over displayed rows rather than as a global Church--Rosser or beta-conversion theorem.

Finally use obstruction-socket separation, \autoref{thm:metacic-critical-path-obstruction-socket-separation}. The obstruction row and the subject-reduction socket remain separate from a closed discharge, which prevents the blocked subject-reduction edge from being consumed as a theorem.
\end{proof}
