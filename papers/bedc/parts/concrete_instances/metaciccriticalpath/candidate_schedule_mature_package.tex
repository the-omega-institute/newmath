\begin{theorem}[MetaCIC candidate schedule mature package]
\label{thm:metaciccriticalpath-candidate-schedule-mature-package}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A mature candidate schedule package is readable only after the candidate schedule discharge of \autoref{thm:metacic-criticalpath-candidate-schedule-discharge}, the residual-budget discharge frontier of \autoref{thm:metacic-critical-path-residual-budget-discharge-frontier}, and the L10 source package of \autoref{thm:metacic-critical-path-mature-package} have all been exposed on the same packet. The package may route candidate-mediated strong normalization, residual substitution, local diamond data, bounded checker rows, and the four L10 source faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, but it exports no subject-reduction theorem, global Church--Rosser theorem, quotient term equality, kernel consistency claim, selected limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the candidate schedule discharge. It forces the candidate-mediated schedule to display candidate-set normalization, critical-pair ledger, residual-substitution boundary, finite observation confluence, and subject-reduction socket rows before the schedule is treated as discharged.

Next attach the residual-budget frontier. The residual route requires residual-budget rows, substitution-boundary rows, closed-premise ledger rows, and local-diamond rows on the same packet rows $S,N,O,U,D,H,C,P,L$ before the discharge socket can be read.

Finally attach the mature L10 source package. That route keeps $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as the four visible source faces while residual, confluence, and candidate-mediated strong-normalization rows remain budget data attached to the packet.

The three routes share the same carrier and structural rows. Their combined read is therefore a candidate schedule mature package, not a detached metatheorem. Since none of the component routes has a coordinate for subject reduction, global Church--Rosser, quotient term equality, kernel consistency, selected limits, or ambient completeness, the package exports none of those claims.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateScheduleMaturePackage}

\begin{theorem}[MetaCIC residual-socket consumer surface]
\label{thm:metaciccriticalpath-residual-socket-consumer-surface}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every consumer of the mature candidate schedule must pass through the candidate schedule discharge of \autoref{thm:metacic-criticalpath-candidate-schedule-discharge}, the residual-budget frontier of \autoref{thm:metacic-critical-path-residual-budget-discharge-frontier}, the finite observation confluence boundary of \autoref{thm:metacic-critical-path-confluence-decidability-handoff-exhaustion}, the closed-term substitution boundary of \autoref{thm:metacic-critical-path-closed-substitution-boundary-exposure}, the subject-reduction socket of \autoref{thm:metacic-critical-path-subject-reduction-socket-budget}, and the four L10 source faces in \autoref{thm:metacic-critical-path-mature-package} before reading candidate-mediated strong normalization. The resulting read exports no closed subject reduction, global Church--Rosser theorem, quotient term equality, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted packet $K$. The mature package theorem \autoref{thm:metaciccriticalpath-candidate-schedule-mature-package} already requires the candidate schedule discharge, residual-budget frontier, and mature L10 source package on the same rows $S,N,O,U,D,H,C,P,L$ before the schedule can be read.

Apply the candidate schedule discharge. It exposes candidate-set normalization, critical-pair ledgers, residual-substitution boundaries, finite observation confluence, and subject-reduction socket rows as named schedule data, so a consumer cannot read candidate-mediated strong normalization from $S$ alone.

Next apply the residual-budget frontier. It requires residual-budget, substitution-boundary, closed-premise ledger, and local-diamond families to remain attached to the same packet before any socket-facing discharge read reaches $D$.

Now use the closed-substitution boundary exposure, confluence-decidability handoff exhaustion, and subject-reduction socket budget. These routes keep closed-term substitution, finite observation confluence, and the subject-reduction socket visible as rows rather than hidden metatheorems.

Finally attach the mature L10 source package. It keeps $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as the four source faces through which the candidate-mediated read is routed.

The row inventory is exhausted by these routes and the structural rows $H,C,P,L$. Since no cited route supplies closed subject reduction, global Church--Rosser, quotient term equality, selected limits, or ambient $\RealUp$ completeness, the residual socket remains a socket while candidate-mediated strong normalization is read.
\end{proof}
