\begin{theorem}[MetaCIC critical-path real-completion source budget]
\label{thm:metacic-critical-path-confluence-decidability-source-budget}
Any $\MetaCICCriticalPathUp$ consumer attempting a candidate-mediated strong-normalization, confluence, or decidability handoff must first display the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ source rows named by the current Phase Real and Completion ledger. Only after those four source rows are visible may the consumer read the MetaCIC closure trace, critical-pair ledger, normalization frontier, subject-reduction discharge socket, and confluence-decidability budget rows. The handoff exports no closed subject-reduction theorem, global confluence theorem, global beta-conversion decision procedure, quotient term equality, selected limit, host real equality, or $\RealUp$ completeness claim.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathRealCompletionSourceBudget}
\begin{proof}
Begin with root-unblock admission. Theorem~\ref{thm:metaciccriticalpath-root-unblock-admission} requires any candidate-mediated strong-normalization, confluence, or decidability admission to expose the four L10 source rows before a candidate checker row is consumed.

Next read the Phase Real source obligations. Theorem~\ref{thm:metacic-critical-path-phase-real-source-obligations} places the dyadic finite-observation row, stream schedule, regular-rational readback, and Real seal before any discharge socket, confluence-decidability handoff, obstruction boundary, or downstream readiness boundary.

The existing source ledger then supplies the finite route. Definition~\ref{def:metacic-critical-path-open-phase-source-ledger} and Theorems~\ref{thm:metacic-critical-path-open-phase-source-closure} and~\ref{thm:metacic-critical-path-open-phase-consumer-totality} keep every current Phase Real and Completion read inside the packet rows and the four displayed source faces.

Finally attach the MetaCIC side. The confluence-decidability budget and handoff-exhaustion theorems require the closure trace, critical-pair ledger, normalization frontier, discharge socket, and budget rows to remain downstream of the source ledger. Since the route has no additional coordinate, the excluded metatheoretic and completion claims cannot be exported.
\end{proof}

\begin{theorem}[MetaCIC critical-path real-completion budget triangle]
\label{thm:metacic-critical-path-real-completion-budget-triangle}
Any $\MetaCICCriticalPathUp$ consumer combining candidate-mediated strong normalization, confluence-decidability, and the L10 Real and Completion handoff must read the four L10 source rows $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before reading the closure trace, critical-pair ledger, normalization frontier, subject-reduction socket, or confluence-decidability budget. The combined route exports no quotient stream equality, selected limit, host real equality, global confluence theorem, global beta-conversion decision procedure, discharged subject-reduction theorem, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathRealCompletionBudgetTriangle}
\begin{proof}
Start with the existing confluence-decidability source budget, \autoref{thm:metacic-critical-path-confluence-decidability-source-budget}. It places every confluence-facing or decidability-facing read downstream of the four displayed L10 source rows and keeps the closure trace, critical-pair ledger, normalization frontier, discharge socket, and budget rows on the MetaCIC side of that source boundary.

Next attach the Phase Real dependency budget. Theorems~\ref{thm:metacic-critical-path-phase-real-root-unblock-admission} and~\ref{thm:metacic-critical-path-phase-real-budget-exhaustion} require every current Real and Completion consumer to present the dyadic budget, stream schedule, regular-rational readback, and Real seal rows as the finite source surface, with no fifth source row or hidden completion credential.

Finally read the candidate-mediated strong-normalization consumer through the same packet. Theorem~\ref{thm:metacic-confluence-candidate-mediated-sn-obligation-surface} allows SN-facing data only after the packet rows and four L10 faces are displayed, and Theorem~\ref{thm:metaciccriticalpath-confluence-decidability-budget} keeps confluence and decidability reads downstream of the same open-source ledger. Thus the three sides of the route share one L10 source budget triangle, and every listed metatheoretic, quotient, choice, order, equality, and completeness claim remains outside the accepted consumer surface.
\end{proof}
