\begin{theorem}[MetaCIC residual substitution frontier]
\label{thm:metacic-critical-path-residual-substitution-frontier}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A MetaCIC critical-path candidate may enter the normalization frontier only after the closed-substitution boundary rows, residual-budget rows, and candidate-mediated strong-normalization rows are all displayed on the same packet route $S,N,O,U,D,H,C,P,L$. The frontier therefore consumes the closed-substitution boundary of \autoref{thm:metacic-critical-path-sn-discharge-socket-readiness}, the residual-budget socket route of \autoref{thm:metacic-critical-path-residual-budget-socket-route}, and the candidate-mediated SN frontier of \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} before any confluence-facing or decidability-facing handoff reads $U$.
\end{theorem}
\begin{proof}
Start with the candidate-mediated SN frontier. It requires the candidate rows $S,N,U,D,H,C,P,L$, keeps $O,D$ visible, and displays the L10 source faces before the handoff row $U$ is consumed.

Next attach the residual-budget discharge frontier. By \autoref{thm:metacic-critical-path-residual-budget-discharge-frontier} and \autoref{thm:metacic-critical-path-residual-budget-socket-route}, a socket-facing read must expose residual-budget, substitution-boundary, closed-premise, and local-diamond rows on the same packet.

Finally use the discharge-socket readiness theorem. It places the closed-substitution boundary beside the confluence-audit dependency exposure before the socket can be read, so entering the normalization frontier requires all three displayed families: closed substitution, residual budget, and candidate-mediated strong normalization.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualSubstitutionFrontier}

\begin{theorem}[MetaCIC residual-substitution diamond budget]
\label{thm:metacic-critical-path-residual-substitution-diamond-budget}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any diamond, confluence, or bounded decidability consumer exposed by $K$ must read the same residual-substitution frontier, candidate-mediated strong-normalization frontier, SN confluence-decidability budget, residual-budget socket route, and L10 source rows on the packet route $S,N,O,U,D,H,C,P,L$. Consequently no confluence-facing consumer may detach a diamond proof from the residual budget rows, bypass the closed-substitution boundary, or read $U$ through an L10 source route absent from the accepted packet.
\end{theorem}

The L10 source faces used by the residual diamond consumer route through the Cauchy name surface of \autoref{ch:concrete-instances-cauchy-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the Real seal already displayed on the packet. This is a finite-source dependency for residual diamond consumers, not a Real completeness assertion or a quotient equality between source names.

\begin{proof}
Start with the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. It places the closed-substitution boundary, residual-budget rows, and candidate-mediated strong-normalization rows on one route before any confluence-facing handoff may consume $U$.

Next apply the diamond budget of \autoref{thm:metacic-critical-path-diamond-budget}. A diamond checker that feeds decidability already consumes the residual budget exposed by that frontier, so it has no independent budget source.

Now attach the residual decidability cut of \autoref{thm:metacic-critical-path-residual-decidability-cut}. It forces the bounded decidability readback to expose the residual-substitution frontier, the candidate SN confluence checker, the SN confluence-decidability budget, and the residual-budget socket route on the same packet.

Finally use the candidate-mediated SN frontier of \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. The candidate rows and L10 source faces remain visible through $S,N,O,U,D,H,C,P,L$, so any detached diamond proof, closed-substitution bypass, or missing L10 source row leaves the accepted $\MetaCICCriticalPathUp$ packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualSubstitutionDiamondBudget}

\begin{theorem}[MetaCIC diamond residual budget]
\label{thm:metacic-critical-path-diamond-budget}
For an accepted $\MetaCICCriticalPathUp$ packet $K=(S,N,O,U,D,H,C,P,L)$, every confluence or diamond checker that feeds a decidability readback must consume exactly the residual budget displayed by the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. In particular, the checker route factors through the candidate SN confluence checker of \autoref{thm:metacic-critical-path-candidate-sn-confluence-checker-factorization}, the SN confluence-decidability budget of \autoref{thm:metacic-critical-path-sn-confluence-budget}, and the residual-budget socket route of \autoref{thm:metacic-critical-path-residual-budget-socket-route} before any bounded decidability consumer may inspect $U$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathDiamondBudget}
\leanvariant{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathDiamondResidualBudget}
\begin{proof}
Read the residual-substitution frontier first. It binds closed substitution, residual budget, and candidate-mediated SN rows to the same packet route, so the confluence checker has no separate budget source.

Now apply the candidate SN confluence checker factorization. The checker may combine candidate-mediated normalization, confluence handoff, and bounded checker rows only through $S,N,O,U,D,H,C,P,L$ and the displayed L10 faces.

Finally apply the SN confluence-decidability budget. Its decidability-facing consumer reads the same handoff row $U$ only after the candidate and confluence budget rows are visible. Since the residual-budget socket route is already attached to those rows, the diamond checker consumes that budget before any decidability readback is exposed.
\end{proof}

\begin{theorem}[MetaCIC residual decidability cut]
\label{thm:metacic-critical-path-residual-decidability-cut}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Every bounded decidability readback that consumes the diamond checker must first expose the residual-substitution frontier, the candidate SN confluence checker, the SN confluence-decidability budget, and the residual-budget socket route on the same packet route. Consequently no decidability consumer may inspect $U$ from a detached confluence proof, closed-substitution bypass, or checker route that omits any of those displayed rows.
\end{theorem}
\begin{proof}
Start from the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. It places the closed-substitution boundary, residual budget, and candidate-mediated SN rows on the same accepted packet before any confluence-facing or decidability-facing read may consume $U$.

Attach the diamond budget of \autoref{thm:metacic-critical-path-diamond-budget}. That theorem forces any confluence or diamond checker feeding a decidability readback to consume the residual budget exposed by the frontier.

Now read the checker and budget routes. The candidate SN confluence checker factorization, \autoref{thm:metacic-critical-path-candidate-sn-confluence-checker-factorization}, supplies the only checker interface, while \autoref{thm:metacic-critical-path-sn-confluence-budget} binds the bounded decidability readback to the same SN confluence-decidability budget.

Finally use the residual-budget socket route of \autoref{thm:metacic-critical-path-residual-budget-socket-route}. Since the socket route is already part of the checker budget, a consumer that reads $U$ from detached confluence, bypassed closed substitution, or an omitted residual route leaves the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDecidabilityCut}

\begin{theorem}[MetaCIC residual diamond source totality]
\label{thm:metacic-critical-path-residual-diamond-source-totality}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any residual diamond checker that feeds bounded conversion must expose the closed-substitution boundary, the residual-budget socket route, the candidate-mediated strong-normalization frontier, and the L10 source rows on the same packet route before reading $U$. Consequently such a checker cannot certify a bounded conversion row from a detached residual substitution proof, a missing candidate-mediated SN row, a confluence budget outside $D,H,C,P,L$, or a source route absent from $S,N,O$.
\end{theorem}
\begin{proof}
Begin with the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. It places closed substitution, residual budget, and candidate-mediated strong normalization on the accepted route before any confluence-facing read can consume $U$.

Attach the diamond budget of \autoref{thm:metacic-critical-path-diamond-budget}. The diamond checker has no separate budget coordinate, so its confluence-facing row must consume the same residual-budget socket route and the same SN confluence-decidability budget.

Use the residual decidability cut of \autoref{thm:metacic-critical-path-residual-decidability-cut}. A bounded decidability readback may inspect $U$ only after the residual-substitution frontier, candidate SN confluence checker, SN confluence-decidability budget, and residual-budget socket route have all been displayed on one packet.

Finally apply the candidate-mediated SN frontier of \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. The L10 source faces remain visible through $S,N,O,U,D,H,C,P,L$, so omitting any source row or moving the checker to a detached route exits the accepted $\MetaCICCriticalPathUp$ packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondSourceTotality}

\begin{theorem}[MetaCIC residual diamond L10 source separation]
\label{thm:metacic-critical-path-residual-diamond-l10-source-separation}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any residual diamond checker that feeds bounded conversion must preserve the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ source faces as distinct displayed rows before it reads $U$. The checker may pass those faces through the candidate-mediated and residual-budget routes, but it may not merge them into a single Real-readback row or reconstruct one face after bounded conversion has consumed the handoff.
\end{theorem}
\begin{proof}
Start with \autoref{thm:metacic-critical-path-residual-substitution-diamond-budget}. It forces any diamond, confluence, or bounded decidability consumer to expose the residual-substitution frontier, candidate-mediated SN frontier, residual-budget socket route, and L10 source rows on the same packet route.

Use the residual decidability cut of \autoref{thm:metacic-critical-path-residual-decidability-cut}. A bounded conversion readback can inspect $U$ only after the residual frontier, checker route, SN confluence-decidability budget, and residual-budget socket have been displayed together.

Finally apply \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} and \autoref{thm:metacic-critical-path-residual-diamond-source-totality}. The candidate-mediated frontier keeps the L10 source faces visible with the candidate rows, and source totality requires those faces before bounded conversion reads $U$. Therefore the four source faces remain separated as displayed rows throughout the residual diamond route.
\end{proof}

\begin{theorem}[MetaCIC residual diamond candidate-budget coverage]
\label{thm:metacic-critical-path-residual-diamond-candidate-budget-coverage}
For an accepted $\MetaCICCriticalPathUp$ packet $K=(S,N,O,U,D,H,C,P,L)$, the candidate-mediated strong-normalization rows, residual-budget socket rows, and L10 source rows jointly cover the bounded conversion consumer route. A bounded conversion consumer may read $U$ only after all three families are displayed on the same packet route; omitting any one family leaves no accepted residual diamond checker for the consumer.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:metacic-critical-path-residual-substitution-diamond-budget}. It names the combined route through the residual-substitution frontier, candidate-mediated strong-normalization frontier, SN confluence-decidability budget, residual-budget socket route, and L10 source rows.

Attach \autoref{thm:metacic-critical-path-residual-decidability-cut}. The cut prevents bounded conversion from reading $U$ through a detached confluence proof or a route that omits the checker, budget, or residual socket.

Now use \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier} together with \autoref{thm:metacic-critical-path-residual-diamond-source-totality}. The candidate rows supply the strong-normalization frontier, the socket rows supply the residual budget, and the L10 rows supply the finite Real and completion source faces. These three families exhaust the bounded conversion consumer route without adding full confluence, beta-conversion decidability, quotient term equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondCandidateBudgetCoverage}

\begin{theorem}[MetaCIC residual-diamond budget source nonescape]
\label{thm:metacic-critical-path-residual-diamond-budget-source-nonescape}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any residual diamond checker feeding bounded conversion must expose the residual-substitution frontier, residual-budget socket route, candidate-mediated strong-normalization frontier, SN confluence-decidability budget, and L10 source rows on the same packet route before reading $U$. A detached residual proof, missing candidate row, missing socket budget, or absent L10 source row leaves the accepted packet.
\end{theorem}
\begin{proof}
Start with the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. It binds the closed-substitution boundary, residual budget, and candidate-mediated strong-normalization rows to the accepted route before a confluence-facing consumer may inspect $U$.

Attach the diamond budget of \autoref{thm:metacic-critical-path-diamond-budget}. The diamond checker must spend the same SN confluence-decidability budget and residual-budget socket route carried by $D,H,C,P,L$, so it cannot import an external budget.

Use the residual decidability cut of \autoref{thm:metacic-critical-path-residual-decidability-cut}. Bounded conversion may read $U$ only after the residual frontier, checker route, SN confluence-decidability budget, and socket budget are displayed on one packet.

Finally apply residual diamond source totality, \autoref{thm:metacic-critical-path-residual-diamond-source-totality}. The L10 source rows remain visible through $S,N,O,U,D,H,C,P,L$, so detached residual proofs, missing candidate rows, missing socket budgets, and absent source rows are outside the accepted $\MetaCICCriticalPathUp$ packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondBudgetSourceNonescape}

\begin{theorem}[MetaCIC residual diamond candidate-mediated handoff]
\label{thm:metacic-critical-path-residual-diamond-candidate-mediated-handoff}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any consumer that uses a residual diamond checker to feed bounded conversion must expose, on one route through $S,N,O,U,D,H,C,P,L$, the candidate-mediated strong-normalization frontier, the residual-substitution frontier, the residual-budget socket route, the SN confluence-decidability budget, and the L10 source rows before reading $U$. A detached residual proof, a missing candidate row, a missing socket budget, or an absent L10 source row leaves the accepted packet.
\end{theorem}
\begin{proof}
Start from residual diamond source totality, \autoref{thm:metacic-critical-path-residual-diamond-source-totality}. It already forces the closed-substitution boundary, residual-budget socket route, candidate-mediated strong-normalization frontier, and L10 source rows onto the same packet route before a bounded-conversion consumer may inspect $U$.

Next use the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. This supplies the closed-substitution and candidate-mediated rows needed by the residual checker and prevents a residual proof from being read on a detached route.

Attach the diamond budget and residual decidability cut, \autoref{thm:metacic-critical-path-diamond-budget} and \autoref{thm:metacic-critical-path-residual-decidability-cut}. Together they place the SN confluence-decidability budget and residual-budget socket beside the bounded conversion readback.

Finally apply the candidate-mediated SN frontier, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. The candidate rows and L10 source faces remain visible through $S,N,O,U,D,H,C,P,L$, so a missing candidate row, missing socket budget, or absent source row is outside the accepted $\MetaCICCriticalPathUp$ packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondCandidateMediatedHandoff}

\begin{theorem}[MetaCIC residual diamond L10 source nonescape]
\label{thm:metacic-critical-path-residual-diamond-l10-source-nonescape}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any residual diamond checker feeding bounded conversion must preserve, on the same packet route, the L10 source faces from $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before reading the candidate-mediated handoff row $U$. Consequently a checker that detaches the residual substitution frontier, omits the diamond budget, bypasses the residual decidability cut, drops the candidate-mediated SN frontier, or reads only residual diamond source totality without those L10 faces exits the accepted packet.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondL10SourceNonescape}
\begin{proof}
Start with the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. It places closed substitution, residual budget, and candidate-mediated normalization on one route before a confluence-facing consumer may read $U$.

Attach the diamond budget of \autoref{thm:metacic-critical-path-diamond-budget}. The residual diamond checker has no independent budget coordinate; it must consume the same SN confluence-decidability and residual-budget rows carried by $D,H,C,P,L$.

Use the residual decidability cut of \autoref{thm:metacic-critical-path-residual-decidability-cut}. A bounded conversion readback can inspect $U$ only after the residual frontier, checker factorization, SN confluence-decidability budget, and residual-budget socket are visible on the same packet route.

Now apply the candidate-mediated SN frontier of \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It keeps the L10 source faces from $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ visible with the candidate rows rather than allowing them to be reconstructed after the handoff.

Finally use residual diamond source totality, \autoref{thm:metacic-critical-path-residual-diamond-source-totality}. Since source totality already requires those faces before bounded conversion reads $U$, any checker that loses one of them or moves it to a detached route is outside the accepted $\MetaCICCriticalPathUp$ packet.
\end{proof}

\begin{theorem}[MetaCIC residual diamond typed L10 budget]
\label{thm:metacic-critical-path-residual-diamond-typed-l10-budget}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A typed residual-diamond consumer may read bounded conversion only after the residual-substitution frontier, residual-budget socket, candidate-mediated strong-normalization frontier, typed-example boundary, and all four L10 source faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
remain on the same packet route. Thus the typed conversion read cannot detach its residual proof from the closed-substitution boundary, spend a socket budget outside $D,H,C,P,L$, omit the candidate-mediated SN frontier, or replace any L10 source face by a later reconstruction.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondTypedL10Budget}
\begin{proof}
Start with residual diamond source totality, \autoref{thm:metacic-critical-path-residual-diamond-source-totality}. It puts the closed-substitution boundary, residual-budget socket, candidate-mediated frontier, and L10 source rows on one route before bounded conversion may read $U$.

Use residual diamond L10 source nonescape, \autoref{thm:metacic-critical-path-residual-diamond-l10-source-nonescape}, to retain $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as displayed source faces. The typed-example boundary is then read through the same candidate-mediated rows, so the bounded conversion consumer receives one finite budget rather than a detached typed proof.
\end{proof}

\begin{theorem}[MetaCIC residual diamond source-face retention]
\label{thm:metacic-critical-path-residual-diamond-source-face-retention}
For an accepted $\MetaCICCriticalPathUp$ packet $K=(S,N,O,U,D,H,C,P,L)$, the residual-diamond route retains the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces whenever the residual-substitution frontier, residual-budget socket, candidate-mediated SN frontier, and typed-example boundary are used to feed bounded conversion. A consumer that reads one of those faces through a detached route, or reads bounded conversion after dropping one face, leaves the accepted packet.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondSourceFaceRetention}
\begin{proof}
Apply the residual diamond L10 source nonescape theorem. It already prevents the bounded conversion handoff from consuming $U$ after losing any of the four L10 source faces.

Now attach residual diamond source totality. Source totality supplies the same closed-substitution, socket-budget, and candidate-mediated rows required by the residual diamond route. The typed-example boundary can therefore be retained only as part of the displayed packet route; it has no separate source-face reconstruction step.
\end{proof}

\begin{theorem}[MetaCIC residual diamond bounded conversion nonescape]
\label{thm:metacic-critical-path-residual-diamond-bounded-conversion-nonescape}
Let $K=(S,N,O,U,D,H,C,P,L)$ be an accepted $\MetaCICCriticalPathUp$ packet. Bounded conversion may be read from the residual diamond checker only while the residual-substitution frontier, residual-budget socket, candidate-mediated strong-normalization frontier, typed-example boundary, and the four L10 source faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ remain on the same route before $U$ is consumed. The read exports no full confluence theorem, no beta-conversion decidability, no quotient equality of terms, no selected limit, and no ambient $\RealUp$ completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondBoundedConversionNonescape}
\begin{proof}
Compose residual diamond source totality with residual diamond L10 source nonescape. The first theorem supplies the closed-substitution, socket-budget, and candidate-mediated rows needed before bounded conversion can read $U$.

The second theorem keeps the four L10 source faces visible on that route. Since the typed-example boundary is also consumed before the handoff, any consumer missing one of those rows is outside the accepted packet, and the conclusion is limited to bounded conversion on the displayed finite budget.
\end{proof}

\begin{theorem}[MetaCIC residual diamond four-face budget]
\label{thm:metacic-critical-path-residual-diamond-four-face-budget}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. A residual diamond checker feeding bounded conversion may consume the candidate-mediated strong-normalization handoff only when the residual-substitution frontier, residual-budget socket, candidate-mediated frontier, and all four L10 source faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
remain displayed on the same packet route $S,N,O,U,D,H,C,P,L$. The handoff does not export full subject reduction, full confluence, beta-conversion decidability, quotient term equality, selected limits, ambient $\RealUp$ completeness, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. It places the closed-substitution boundary, residual budget, and candidate-mediated normalization rows on one accepted route before a diamond-facing consumer may read $U$.

Next apply residual diamond source totality, \autoref{thm:metacic-critical-path-residual-diamond-source-totality}. The bounded-conversion checker must expose the closed-substitution boundary, residual-budget socket route, candidate-mediated strong-normalization frontier, and L10 source rows before it consumes the handoff.

Use the residual diamond L10 source nonescape theorem \autoref{thm:metacic-critical-path-residual-diamond-l10-source-nonescape}. It keeps $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ visible on the same route and rules out a detached checker that reconstructs any source face after reading $U$.

Finally apply the candidate-mediated SN frontier of \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. The candidate-mediated handoff remains a bounded route through the displayed packet, so the theorem exports only the four-face budget and not subject reduction, full confluence, beta-conversion decidability, quotient term equality, selected limits, ambient Real completeness, or kernel consistency.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualDiamondFourFaceBudget}

\begin{theorem}[MetaCIC residual-substitution diamond budget typed boundary]
\label{thm:metacic-residual-substitution-diamond-budget-typed-boundary}
Let
$$
K=(S,N,O,U,D,H,C,P,L)
$$
be an accepted $\MetaCICCriticalPathUp$ packet. Any typed residual-substitution diamond consumer that reads the bounded conversion handoff must first keep the closed-substitution boundary, residual-budget socket, candidate-mediated strong-normalization frontier, typed-example boundary, and L10 source faces on the same packet route $S,N,O,U,D,H,C,P,L$. Consequently the bounded conversion handoff cannot be read from a detached typed example, a residual diamond proof outside the socket budget, a candidate frontier without typed examples, or an L10 source route absent from $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$.
\end{theorem}
\begin{proof}
Begin with the residual-substitution frontier of \autoref{thm:metacic-critical-path-residual-substitution-frontier}. It puts the closed-substitution boundary, residual-budget rows, and candidate-mediated normalization rows on the accepted packet route before a confluence-facing handoff can consume $U$.

Attach the diamond budget of \autoref{thm:metacic-critical-path-diamond-budget}. The residual diamond checker must spend the same residual budget that feeds the bounded conversion route, so it cannot move the checker outside the socket-facing rows.

Use the residual decidability cut \autoref{thm:metacic-critical-path-residual-decidability-cut}. It forces the bounded conversion readback to expose the residual frontier, candidate checker, SN confluence budget, and residual-budget socket on one packet.

Now apply the candidate-mediated SN frontier \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. The typed-example boundary is read through the same candidate rows, so a typed consumer cannot detach examples from the strong-normalization frontier.

Finally use the L10 source nonescape theorem \autoref{thm:metacic-critical-path-residual-diamond-l10-source-nonescape}. The $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces remain visible before $U$ is read; any handoff missing one of the closed-substitution, residual-budget, candidate-mediated, typed-example, or L10 source faces is outside the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathResidualSubstitutionDiamondBudgetTypedBoundary}
