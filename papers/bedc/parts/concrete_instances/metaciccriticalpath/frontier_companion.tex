\begin{theorem}[MetaCIC critical-path frontier companion]
\label{thm:metacic-critical-path-frontier-companion}
Every $\MetaCICCriticalPathUp$ frontier consumer that invokes the L10 source package must display the $\DyadicRatCoreUp$ tolerance row, the $\StreamNameUp$ finite-window row, the $\RegSeqRatUp$ readback row, and the $\RealUp$ seal before it uses any candidate-mediated normalization row or residual-diamond row. The companion route keeps those four L10 rows inside the same packet rows $S,N,O,U,D,H,C,P,L$ and exports no quotient stream equality, selected limit, ambient $\RealUp$ completeness, global normalization theorem, closed subject-reduction theorem, or detached residual-diamond theorem.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathFrontierCompanion}
\begin{proof}
Start with the L10 four-object exit certificate of \autoref{thm:metacic-critical-path-l10-four-object-exit-certificate}. It identifies the Phase Real and Completion source surface with exactly the dyadic tolerance row, StreamName finite schedule, RegSeqRat readback, and Real seal attached to the accepted packet.

Apply the mature L10 source package theorem, \autoref{thm:metacic-critical-path-mature-package}. That theorem requires residual-budget, confluence-budget, and candidate-mediated strong-normalization consumers to read the L10 source ledger only through those four displayed rows.

Now place the frontier consumer after that source package. Candidate-mediated normalization rows and residual-diamond rows may be read only while $S,N,O,U,D,H,C,P,L$ and the four L10 source faces remain visible on the same route.

Since the packet contains no coordinate for quotient stream equality, selected limits, ambient $\RealUp$ completeness, global normalization, closed subject reduction, or a detached residual-diamond theorem, the frontier companion exports only the displayed L10 dependency route.
\end{proof}
