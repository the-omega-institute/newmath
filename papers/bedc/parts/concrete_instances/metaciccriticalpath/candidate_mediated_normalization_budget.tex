\begin{theorem}[MetaCIC candidate-mediated normalization budget]
\label{thm:metacic-critical-path-candidate-mediated-normalization-budget}
For every accepted $\MetaCICCriticalPathUp$ packet $K=(S,N,O,U,D,H,C,P,L)$, candidate-mediated strong-normalization evidence, confluence handoff data, and normalization-certificate routing expose a finite normalization budget before any critical-path discharge is exported. The budget is the displayed route through $S,N,O,U,D,H,C,P,L$, the four L10 faces $\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\RealUp$, the $\MetaCICNormalizationFrontierUp$ and $\MetaCICNormalizationCertificateRouterUp$ rows, the confluence boundary, and the visible discharge socket. It does not claim full MetaCIC normalization, global confluence, closed subject reduction, beta-conversion decidability, quotient term equality, selected normal forms, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the candidate-mediated SN frontier, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier}. It forces the normalization read through the packet rows and the four L10 faces before any handoff row is consumed.

Use the normalization-frontier dependency exposure of \autoref{thm:metacic-critical-path-normalization-frontier-dependency-exposure}. The $\MetaCICNormalizationFrontierUp$ and $\MetaCICNormalizationCertificateRouterUp$ rows route the normal-form endpoint as finite certificate data rather than as a global normalization theorem.

Apply the confluence socket boundary, \autoref{thm:metacic-critical-path-candidate-frontier-confluence-socket-boundary}. It keeps confluence-facing reads on the same finite frontier and leaves the obstruction and discharge socket visible.

Finally read discharge coverage, \autoref{thm:metacic-critical-path-candidate-mediated-sn-frontier-discharge-coverage}. The discharge surface is available only as the displayed socket budget, so a critical-path consumer receives finite normalization readiness and none of the excluded global MetaCIC claims.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICCriticalPathUp.MetaCICCriticalPathCandidateMediatedNormalizationBudget}
