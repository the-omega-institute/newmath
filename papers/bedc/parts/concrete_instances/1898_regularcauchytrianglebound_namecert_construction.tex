\chapter{A Concrete Naming Certificate for $\RegularCauchyTriangleBoundUp$}
\label{ch:concrete-instances-regular-cauchy-triangle-bound-namecert}
\origin{human}

The $\RegularCauchyTriangleBoundUp$ interface packages the finite three-leg estimate used by the diagonal regularity step in the Real completeness capstone. It keeps the two diagonal observations, the shared intermediate observation, the two regular-sequence tail bounds, and the middle Cauchy-modulus bound in one $\NameCert$ packet. The packet is a displayed estimate surface; it is not a quotient stream, not a selected limit, and not an ambient completeness principle.

The triangle packet consumes the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular readbacks of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the downstream diagonal consumer surface of \autoref{ch:concrete-instances-diagonal-limit-compatibility-namecert}. These four rows determine the L10 dependency route on which the three-leg estimate is later read by completion-facing consumers.

The packet consumes the shared-threshold and middle-meet surfaces of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} and \autoref{ch:concrete-instances-cauchymodulusmeet-namecert} before handing a finite three-leg bound to $\RealUp$ consumers. Those sibling rows supply the L10 threshold lattice used by the triangle route; the present carrier only synchronizes their reads into one diagonal estimate packet.

\begin{definition}[RegularCauchyTriangleBound carrier]
\label{def:regular-cauchy-triangle-bound-carrier}
A $\RegularCauchyTriangleBoundUp$ carrier is a finite $\BHist$ packet
$$
T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D_m$ and $D_n$ are the two $\StreamNameUp$ diagonal observation rows for the requested indices $m$ and $n$;
\item $I_m$ and $I_n$ are the two intermediate $\StreamNameUp$ rows used to compare the diagonal observations through a common Cauchy-modulus surface;
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger carrying the two endpoint tolerances $1/(m+1)$ and $1/(n+1)$ and the summed output bound;
\item $A$ stores the two $\RegSeqRatUp$ regularity reads that bound the first and third triangle legs;
\item $B$ stores the middle $\CauchyModulusMeetUp$ read that bounds the comparison between the two intermediate observations;
\item $H$ stores componentwise $\hsame$ transports for diagonal rows, intermediate rows, tolerance rows, regularity reads, middle-modulus reads, provenance, and the local name;
\item $C$ stores the displayed $\Cont$ routes for diagonal inspection, intermediate-point selection, regularity reads, middle-modulus reads, dyadic tolerance addition, and local NameCert comparison;
\item $P$ is the $\Pkg$ provenance over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyModulusMeetUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchyTriangleBoundUp}$ row.
\end{enumerate}
The classifier compares accepted packets only after transporting every displayed row to a common diagonal, intermediate-point, and dyadic tolerance surface. The carrier has no coordinate for quotient stream equality, selected limits, host real equality, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundTasteGate\_single\_carrier\_alignment\_decode\_encode}

\begin{theorem}[RegularCauchyTriangleBound NameCert obligations]
\label{thm:regular-cauchy-triangle-bound-namecert-obligations}
The local $\NameCert_{\RegularCauchyTriangleBoundUp}$ surface for the carrier of \autoref{def:regular-cauchy-triangle-bound-carrier} consists exactly of five finite obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N)$;
\item classifier transport to a common $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\CauchyModulusMeetUp$ surface;
\item regular-leg stability for the two endpoint triangle legs stored in $A$;
\item middle-leg exactness for the shared Cauchy-modulus comparison stored in $B$;
\item ledger rows for dyadic tolerance addition, $\Cont$ routes, $\Pkg$ provenance, $\RealUp$ handoff reads, and the local naming certificate.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-cauchy-triangle-bound-carrier}. The rows display the two diagonal observations, two intermediate observations, dyadic tolerance ledger, endpoint regularity reads, middle-modulus read, transports, continuations, provenance, and local name.

Classifier transport uses $H$ to move those rows to a common finite surface. Once transported, the routes in $C$ are the only ways to inspect diagonal rows, choose intermediate rows, read endpoint regularity, read the middle modulus, and add dyadic tolerances.

The two endpoint estimates are stored in $A$, while the middle comparison is stored in $B$. Since the carrier has no coordinate for quotient streams, selected limits, host real equality, or ambient completeness, the obligations are exhausted by the five displayed rows.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound diagonal regularity]
\label{thm:regular-cauchy-triangle-bound-diagonal-regularity}
For an accepted $\RegularCauchyTriangleBoundUp$ carrier $T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N)$, the two diagonal observations $D_m$ and $D_n$ are compared by the displayed three-leg route
$$
D_m \longrightarrow I_m \longrightarrow I_n \longrightarrow D_n
$$
and the output tolerance is the dyadic sum recorded in $Q$ from the two regular endpoint bounds in $A$ and the middle Cauchy-modulus bound in $B$. Every $\RealUp$ diagonal-regularity consumer reads this finite route before receiving the bound.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundCarrier\_diagonal\_regular\_route}
\begin{proof}
Read the accepted carrier and project the four observation rows. The continuation row $C$ supplies the route from $D_m$ to $I_m$, from $I_m$ to $I_n$, and from $I_n$ to $D_n$.

The first and third legs are bounded by the two $\RegSeqRatUp$ regularity reads in $A$. The middle leg is bounded by the shared $\CauchyModulusMeetUp$ read in $B$, transported to the same intermediate surface by $H$.

Finally read the dyadic ledger $Q$. It records the two endpoint tolerances and the summed output tolerance, so the consumer receives exactly the three-leg diagonal estimate and no separate completed-real object.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound endpoint-leg scope]
\label{thm:regular-cauchy-triangle-bound-leg-scope}
For an accepted $\RegularCauchyTriangleBoundUp$ carrier $T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N)$, the first comparison leg $D_m \longrightarrow I_m$ and the third comparison leg $I_n \longrightarrow D_n$ are exactly the two $\RegSeqRatUp$ regularity reads stored in row $A$. No additional endpoint estimate, quotient-stream comparison, selected limit, host real equality, or ambient completeness datum participates in either endpoint leg.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-triangle-bound-carrier}. Its row $A$ is defined as the pair of $\RegSeqRatUp$ regularity reads, one for the $m$ endpoint and one for the $n$ endpoint, while the surrounding rows only transport, route, add tolerances, record provenance, and name the packet.

Apply the diagonal route of \autoref{thm:regular-cauchy-triangle-bound-diagonal-regularity}. The route reads the first triangle leg from $D_m$ to $I_m$ and the third triangle leg from $I_n$ to $D_n$ before the dyadic output bound is reported, so the only endpoint estimates available to that route are the two reads already displayed in $A$.

The non-escape boundary of \autoref{thm:regular-cauchy-triangle-bound-nonescape} prevents any consumer-facing route from exporting quotient streams, selected limits, host real equality, ambient completeness, or completion witnesses. Thus the endpoint-leg scope is exactly row $A$.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound middle-meet exactness]
\label{thm:regular-cauchy-triangle-bound-middle-meet-exactness}
For an accepted $\RegularCauchyTriangleBoundUp$ carrier $T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N)$, the middle comparison $I_m \longrightarrow I_n$ is exactly the $\CauchyModulusMeetUp$ read stored in row $B$, transported by the displayed $\hsame$ rows in $H$ to the common intermediate surface. No quotient stream, selected limit, host real equality, or ambient completeness datum supplies a second middle comparison.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-triangle-bound-carrier}. Row $B$ is the middle $\CauchyModulusMeetUp$ read, and row $H$ is the componentwise transport surface that aligns the intermediate observations and middle-modulus read.

Use \autoref{thm:regular-cauchy-triangle-bound-diagonal-regularity} to inspect the route $D_m \longrightarrow I_m \longrightarrow I_n \longrightarrow D_n$. The central arrow is the only place in the route where the two intermediate rows are compared, and the theorem identifies that bound as the shared middle Cauchy-modulus read in $B$.

By \autoref{thm:regular-cauchy-triangle-bound-nonescape}, every consumer-facing route is exhausted by the finite displayed rows. Since the carrier has no coordinate for quotient streams, selected limits, host real equality, or ambient completeness, row $B$ is the exact middle-meet obligation.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound tolerance-addition ledger]
\label{thm:regular-cauchy-triangle-bound-tolerance-addition-ledger}
For an accepted $\RegularCauchyTriangleBoundUp$ carrier $T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N)$, the output tolerance read by the diagonal-regularity consumer is precisely the $\DyadicRatCoreUp$ sum row in $Q$, formed from the two endpoint tolerances certified by $A$ and the middle tolerance certified by $B$. No selected limit, quotient-stream equality, host real equality, or ambient completeness datum changes the reported tolerance.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-triangle-bound-carrier}. Row $Q$ is the $\DyadicRatCoreUp$ tolerance ledger, row $A$ supplies the two endpoint regularity tolerances, and row $B$ supplies the middle Cauchy-modulus tolerance.

The diagonal-regularity statement in \autoref{thm:regular-cauchy-triangle-bound-diagonal-regularity} reads the three legs in order and then reads the dyadic sum recorded in $Q$. Therefore the reported output tolerance is not recomputed by a hidden completion object; it is the displayed ledger row attached to the finite packet.

The non-escape boundary of \autoref{thm:regular-cauchy-triangle-bound-nonescape} rules out exported quotient streams, selected limits, host real equality, ambient completeness proofs, and host completion witnesses. Hence the tolerance-addition obligation is precisely the $\DyadicRatCoreUp$ row $Q$.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound non-escape]
\label{thm:regular-cauchy-triangle-bound-nonescape}
Every consumer-facing route out of an accepted $\RegularCauchyTriangleBoundUp$ carrier factors through the finite rows $D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N$. No route exports a quotient stream, selected limit, ambient completeness proof, host real equality, or host completion witness.
\end{theorem}
\begin{proof}
Take any consumer-facing route and project the carrier. The only mathematical rows are the diagonal observations, intermediate observations, dyadic tolerance ledger, endpoint regularity reads, and middle-modulus read.

The remaining rows transport, route, record provenance, and name the packet. Thus any read by a $\RegSeqRatUp$ or $\RealUp$ consumer is exhausted by the displayed finite packet.

Because the carrier omits quotient streams, selected limits, ambient completeness proofs, host real equality, and host completion witnesses, none of those objects can be exported by a continuation route.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound phase handoff]
\label{thm:regular-cauchy-triangle-bound-phase-handoff}
The $\RegularCauchyTriangleBoundUp$ packet is the finite handoff surface from $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\CauchyModulusMeetUp$ into the Real completeness diagonal-regularity argument. A downstream $\RealUp$ consumer receives the transported three-leg route, the endpoint regularity reads, the middle-modulus read, the dyadic tolerance ledger, provenance, and the local $\NameCert$ row, and receives no additional limit-selection data.
\end{theorem}
\begin{proof}
The dependency anchors in $P$ name the dyadic arithmetic, stream schedule, regular rational readback, and shared-modulus rows consumed by the packet. Projecting the carrier exposes exactly those rows together with transports, continuations, provenance, and local naming.

The handoff to $\RealUp$ is through the diagonal-regularity route of \autoref{thm:regular-cauchy-triangle-bound-diagonal-regularity}. That route first displays the three triangle legs and then reads the summed dyadic bound.

No downstream row is available for a selected limit, quotient stream, host real equality, or ambient completeness object. Therefore the phase handoff is precisely the finite three-leg estimate surface.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound Real consumer route]
\label{thm:regular-cauchy-triangle-real-consumer-route}
Every $\RealUp$ completion consumer that uses a
$\RegularCauchyTriangleBoundUp$ packet reads the two endpoint regularity legs,
the middle $\CauchyModulusMeetUp$ meet, and the $\DyadicRatCoreUp$
tolerance-addition ledger before the $\RealUp$ seal handoff. The consumer
route exports no quotient-stream equality, selected limits, host equality, or
ambient completeness row.
\end{theorem}
\begin{proof}
Start with
\autoref{thm:regular-cauchy-triangle-bound-diagonal-regularity}. It fixes the
three-leg route through the two endpoint observations and the two intermediate
rows.

Apply \autoref{thm:regular-cauchy-triangle-bound-leg-scope} and
\autoref{thm:regular-cauchy-triangle-bound-middle-meet-exactness}. These
identify the endpoint regularity reads with row $A$ and the middle modulus meet
with row $B$ after the displayed transports.

The tolerance row is supplied by
\autoref{thm:regular-cauchy-triangle-bound-tolerance-addition-ledger}. Use
\autoref{thm:regular-cauchy-triangle-bound-nonescape} to exclude
quotient-stream equality, selected limits, host equality, and ambient
completeness, then apply
\autoref{thm:regular-cauchy-triangle-bound-phase-handoff} to present the finite
packet to the $\RealUp$ seal handoff.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound transported assembly]
\label{thm:regular-cauchy-triangle-bound-transported-assembly}
For an accepted $\RegularCauchyTriangleBoundUp$ carrier
$$
T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N),
$$
any transported consumer route over $T$ is exhausted by the endpoint-leg reads in $A$, the middle-meet read in $B$, the dyadic sum ledger $Q$, the transport and continuation routes $H,C$, and the package and naming rows $P,N$. The assembled route is the finite transported packet that a $\RealUp$ diagonal-regularity or completion-seal consumer receives; it is not a single-theorem variant of one leg obligation, because it combines both endpoint legs, the middle leg, tolerance addition, non-escape, and the Real-phase handoff into one consumer package.
\end{theorem}
\begin{proof}
Start with diagonal regularity from \autoref{thm:regular-cauchy-triangle-bound-diagonal-regularity}. It fixes the visible route $D_m \longrightarrow I_m \longrightarrow I_n \longrightarrow D_n$ and makes the dyadic output bound a read from the same packet.

Apply endpoint-leg scope from \autoref{thm:regular-cauchy-triangle-bound-leg-scope}. The first and third legs of the route are exactly the two $\RegSeqRatUp$ regularity reads stored in $A$ after the displayed transports.

Next apply middle-meet exactness from \autoref{thm:regular-cauchy-triangle-bound-middle-meet-exactness}. The central comparison is exactly the $\CauchyModulusMeetUp$ read stored in $B$, transported by $H$ to the same intermediate surface.

The tolerance-addition ledger of \autoref{thm:regular-cauchy-triangle-bound-tolerance-addition-ledger} supplies the summed dyadic output row $Q$. Non-escape from \autoref{thm:regular-cauchy-triangle-bound-nonescape} prevents quotient streams, selected limits, host real equality, ambient completeness, and completion witnesses from entering the transported route.

Finally use the phase handoff of \autoref{thm:regular-cauchy-triangle-bound-phase-handoff}. The $\RealUp$ consumer receives the assembled finite packet through $H,C,P,N$, so the transported assembly is exactly the union of the three leg obligations, the dyadic ledger, and the Real-phase handoff.
\end{proof}

\begin{theorem}[RegularCauchyTriangleBound completion-seal consumer exactness]
\label{thm:regular-cauchy-triangle-bound-completion-seal-consumer-exactness}
For an accepted $\RegularCauchyTriangleBoundUp$ carrier
$$
T=(D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N),
$$
every $\CauchyLimitSealUp$ or $\RealUp$ completion-seal consumer that invokes the triangle-bound packet factors through the diagonal route theorem, endpoint-leg scope, middle-meet exactness, tolerance-addition ledger, non-escape boundary, and phase handoff. The consumer receives exactly the displayed rows $D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N$ and receives no selected limit, quotient stream, host real equality, ambient completeness proof, or completion witness.
\end{theorem}
\begin{proof}
First apply \autoref{thm:regular-cauchy-triangle-bound-diagonal-regularity}. It fixes the route from $D_m$ through $I_m$ and $I_n$ to $D_n$ as the only diagonal comparison available to a completion-seal consumer.

Next apply \autoref{thm:regular-cauchy-triangle-bound-leg-scope} and \autoref{thm:regular-cauchy-triangle-bound-middle-meet-exactness}. These identify the two endpoint legs with row $A$ and the middle leg with row $B$ after the displayed transports.

The tolerance read is then forced by \autoref{thm:regular-cauchy-triangle-bound-tolerance-addition-ledger}. Apply \autoref{thm:regular-cauchy-triangle-bound-nonescape} to exclude selected limits, quotient streams, host real equality, ambient completeness proofs, and completion witnesses.

Finally use \autoref{thm:regular-cauchy-triangle-bound-phase-handoff}. The handoff row presents exactly $D_m,D_n,I_m,I_n,Q,A,B,H,C,P,N$ to the $\CauchyLimitSealUp$ or $\RealUp$ reader, so the consumer boundary is exact for the finite triangle-bound packet.
\end{proof}

\closureat{RegularCauchyTriangleBoundUp}{seedStr}
\closureat{RegularCauchyTriangleBoundUp}{obligationStr}[BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundCarrier\_diagonal\_regular\_route]
\closureat{RegularCauchyTriangleBoundUp}{scopedStr}[BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundCarrier\_diagonal\_regular\_route]
\closureat{RegularCauchyTriangleBoundUp}{publicStr}[BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundCarrier\_diagonal\_regular\_route]
\closureat{RegularCauchyTriangleBoundUp}{bridgedStr}[BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundCarrier\_diagonal\_regular\_route]
\closureat{RegularCauchyTriangleBoundUp}{matureStr}[BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundCarrier\_diagonal\_regular\_route]

\begin{closurestatus}{\RegularCauchyTriangleBoundUp}
  \constructivestory{A $\RegularCauchyTriangleBoundUp$ packet is generated from two diagonal observation rows, two intermediate observation rows, a $\DyadicRatCoreUp$ tolerance ledger, two $\RegSeqRatUp$ regularity reads, one $\CauchyModulusMeetUp$ middle read, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The scoped dependency surface consists exactly of $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchyModulusMeetUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows supporting the finite triangle-bound carrier, diagonal route, endpoint legs, middle meet, tolerance ledger, handoff, and non-escape boundary.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.RegularCauchyTriangleBoundUp.RegularCauchyTriangleBoundCarrier\_diagonal\_regular\_route}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient equality of streams, selected limits, host real equality, ambient completeness, completion witnesses, or bridge checking.}
  \upgradepath{No further paper-side closure grade remains; bridge checking is recorded only on the separate formal bridge axis.}
\end{closurestatus}
