\chapter{A Concrete Naming Certificate for $\RegularRectangularModulusUp$}
\label{ch:concrete-instances-regular-rectangular-modulus-namecert}
\origin{human}

Following Pringsheim, Moore--Osgood, and Bishop--Bridges, the $\RegularRectangularModulusUp$ packet records the finite rectangular modulus surface that sits between regular double Cauchy data and the $\RegSeqRatUp/\RealUp$ completion handoff. It isolates the window-and-tolerance budget used by \autoref{ch:concrete-instances-regular-double-cauchy-namecert} before the diagonal consumer reads a seal. The packet is anchored in the uniform-modulus discipline of \autoref{ch:concrete-instances-regular-cauchy-uniform-modulus-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the completion-facing route of \autoref{ch:concrete-instances-real-cauchy-completion-namecert}, and the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}. It names the rectangular budget as finite BEDC data; it does not assert a host double limit, quotient arrays, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular rectangular modulus carrier]
\label{def:regular-rectangular-modulus-carrier}
A $\RegularRectangularModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(S,D,R,Q,T,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ rectangular-window family, giving the finite row and column windows inspected for each rectangular request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for those windows, including the dyadic budget assigned to each row-column face.
\item $R$ is the row-column $\RegSeqRatUp$ modulus row, recording the regular rational bounds obtained from the displayed windows and tolerances.
\item $Q$ is the uniform rectangular comparison row inherited from the $\RegularCauchyUniformModulusUp$ discipline, restricted to the finite rectangle visible in $S,D,R$.
\item $T$ is the diagonal handoff row, available only after the rectangular surface $S,D,R,Q$ has been displayed.
\item $W$ is the $\RealCauchyCompletionUp/\RealUp$ seal route, read after the diagonal handoff or by a completion consumer that factors through the same rectangular surface.
\item $H$ records componentwise $\hsame$ transport for the window family, tolerance ledger, row-column modulus, diagonal handoff, seal route, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from a consumer request through $S,D,R,Q,T,W$ and then to the structural rows.
\item $P$ is the $\Pkg$ provenance over $\RegularRectangularModulusUp$, $\RegularDoubleCauchyUp$, $\RegularCauchyUniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCauchyCompletionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularRectangularModulusUp}$ row.
\end{enumerate}
No carrier coordinate stores a host double limit, quotient array, selected limiting value, countable-choice schedule, or ambient real-completeness theorem.
\end{definition}

\begin{theorem}[Regular rectangular modulus obligation surface]
\label{thm:regular-rectangular-modulus-obligation-surface}
For every accepted $\RegularRectangularModulusUp$ carrier $M=(S,D,R,Q,T,W,H,C,P,N)$, the local $\NameCert_{\RegularRectangularModulusUp}$ surface consists exactly of carrier admission, rectangular window display, dyadic tolerance accounting, row-column $\RegSeqRatUp$ modulus readback, uniform rectangular comparison, diagonal handoff discipline, $\RealCauchyCompletionUp/\RealUp$ seal routing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every completion-facing read must consume the rectangular surface $S,D,R,Q,H,C,P,N$ before it may read either $T$ or $W$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $S,D,R,Q$ display the finite rectangular surface: windows, tolerances, regular rational row-column bounds, and uniform comparison over the inspected rectangle.

The diagonal row $T$ and the completion-facing row $W$ appear after that surface in the packet inventory. Transport through $H$ and replay through $C$ preserve the same displayed rows for any accepted consumer request, while $P$ and $N$ keep the route local to the named packet.

Since the carrier has no coordinate for a host double limit, quotient array, selected limit, countable-choice schedule, or ambient completeness theorem, the listed rows exhaust the local obligation surface.
\end{proof}

\begin{theorem}[Regular rectangular modulus handoff factorization]
\label{thm:regular-rectangular-modulus-handoff-factorization}
Every $\RegularDoubleCauchyUp$, $\RegSeqRatUp$, $\RealCauchyCompletionUp$, or $\RealUp$ consumer that reads an accepted $\RegularRectangularModulusUp$ carrier factors through the finite route
$$
S,D \longmapsto R,Q \longmapsto T \longmapsto W .
$$
The diagonal handoff may refine a request to a cofinal line, but it cannot replace the displayed StreamName windows, DyadicRatCore tolerance ledger, or row-column RegSeqRat modulus surface.
\end{theorem}
\begin{proof}
Begin with the consumer request and read the rectangular rows $S$ and $D$. These rows fix the inspected StreamName windows and dyadic tolerance entries before any regular rational or real-facing seal is visible.

Apply the row-column modulus row $R$ and the uniform comparison row $Q$. Together they supply the finite regular rational surface that a regular double Cauchy packet or a RegSeqRat consumer may inspect.

Only then may the route read the diagonal handoff $T$ and the completion seal $W$. The structural rows $H,C,P,N$ replay and name the same path, so no consumer obtains a bypass through a selected double limit, quotient array, choice schedule, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularRectangularModulusUp$ read displays StreamName rectangular windows, a DyadicRatCore tolerance ledger, row-column RegSeqRat modulus data, a uniform comparison row, a diagonal handoff, a RealCauchyCompletion and Real seal route, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before any completion-facing consumer reads a terminal seal.}

\independenceWitness{regular-double-cauchy, regular-cauchy-uniform-modulus, streamname, regseqrat, dyadicratcore, realcauchycompletion, real: $\RegularRectangularModulusUp$ is not bijective to any listed sibling because it names the finite rectangular modulus surface itself, separating the row-column budget from both the source double Cauchy carrier and the downstream completion seal.}

\closureat{RegularRectangularModulusUp}{seedStr}

\begin{closurestatus}{\RegularRectangularModulusUp}
  \constructivestory{A $\RegularRectangularModulusUp$ packet is generated from finite StreamName rectangular windows, a DyadicRatCore tolerance ledger, row-column RegSeqRat modulus data, a uniform comparison row, a diagonal handoff, a RealCauchyCompletion and Real seal route, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-rectangular-modulus-carrier}, \autoref{thm:regular-rectangular-modulus-obligation-surface}, and \autoref{thm:regular-rectangular-modulus-handoff-factorization} seed the finite rectangular modulus surface between regular double Cauchy data and completion-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host double limits, quotient arrays, selected limits, countable choice, ambient $\RealUp$ completeness, bridge checking, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, rectangular window display, dyadic tolerance accounting, row-column modulus readback, uniform comparison, diagonal handoff, seal routing, and local NameCert surface proofs for BEDC.Derived.RegularRectangularModulusUp.}
\end{closurestatus}
