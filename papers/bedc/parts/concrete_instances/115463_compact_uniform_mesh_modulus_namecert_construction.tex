\chapter{A Concrete Naming Certificate for $\CompactUniformMeshModulusUp$}
\label{ch:concrete-instances-compact-uniform-mesh-modulus-namecert}
\origin{human}

Following the finite-net proof of Heine--Cantor in Bishop and Bridges constructive metric analysis, the $\CompactUniformMeshModulusUp$ packet realizes the compact finite-mesh route of \autoref{ch:visions-compact-uniform-continuity} as concrete NameCert data. It consumes the compact metric finite-net rows of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map pointwise-modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational lower-bound rows of \autoref{ch:concrete-instances-rat-namecert}, the finite-net fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, and the tolerance-only consumer surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet records a finite $\ProbeBundle$ mesh and a rational lower-bound fold; it does not import open-cover compactness, choice-selected centers, semantic continuity, an external modulus function, quotient metric equality, or target completeness.

\begin{definition}[Compact uniform mesh modulus carrier]
\label{def:compact-uniform-mesh-modulus-carrier}
A $\CompactUniformMeshModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(K,M,F,B,L,Q,T,R,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, tying the compact source to displayed mesh centers and coverage witnesses.
\item $M$ is the source and target metric row shared by the compact carrier and the continuous map.
\item $F$ is the $\ContinuousMapUp$ graph row, exposing pointwise-modulus ledgers at displayed mesh centers.
\item $B$ is the finite $\ProbeBundle$ mesh row, listing center names, membership rows, and source-distance coverage rows.
\item $L$ is the local radius ledger that attaches a positive $\RatUp$ precision radius to each displayed probe.
\item $Q$ is the rational lower-bound fold row over the finite mesh.
\item $T$ is the metric-triangle handoff row that converts source mesh proximity and pointwise target estimates into one uniform estimate.
\item $R$ is the $\UniformModulusUp$ tolerance-only output row consumed by downstream compact-continuity arguments.
\item $H$ records componentwise $\hsame$ transport for compact metric data, continuous-map data, mesh rows, rational folds, triangle handoff, uniform-modulus output, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a tolerance request through finite mesh inspection, pointwise-radius readback, rational lower-bound folding, triangle handoff, and the uniform-modulus row.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformMeshModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\RatUp$, $\ProbeBundle$, finite-fold material, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformMeshModulusUp}$ row.
\end{enumerate}
The classifier compares packets only through $K,M,F,B,L,Q,T,R,H,C,P,N$. It has no coordinate for an ambient open-cover compactness theorem, externally selected finite subcover, selected subsequence, host modulus function, quotient metric equality, or completeness of the target metric.
\end{definition}

\begin{theorem}[Compact uniform mesh finite-net handoff]
\label{thm:compact-uniform-mesh-modulus-finite-net-handoff}
Every accepted $\CompactUniformMeshModulusUp$ packet hands a $\UniformModulusUp$ consumer the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \ProbeBundle,\ \RatUp
\longmapsto
\UniformModulusUp .
$$
For $U=(K,M,F,B,L,Q,T,R,H,C,P,N)$, the output row $R$ is readable only after the compact metric mesh $K,B$, the pointwise-radius ledger $F,L$, the rational lower-bound fold $Q$, and the metric-triangle handoff $T$ have all been displayed in the same packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-mesh-modulus-carrier}. The compact metric row, metric ledger, continuous-map row, finite mesh, local radii, rational fold, triangle handoff, output row, transport, replay, provenance, and naming rows are the complete inventory.

Read the finite mesh first. The compact metric row $K$ and $\ProbeBundle$ row $B$ expose only displayed centers and coverage witnesses, so the proof route does not pass through a host open-cover theorem or an externally selected subcover.

Next read the pointwise data. The continuous-map row $F$ supplies local modulus evidence only at the centers displayed by $B$, and the local-radius row $L$ keeps those radii attached to the same finite mesh.

Now fold and hand off. The row $Q$ is the finite rational lower bound over $L$, and $T$ is the metric-triangle estimate that converts mesh proximity plus pointwise control into one uniform estimate.

Finally expose $R$ through $H,C,P,N$. These structural rows transport, replay, source, and name the same finite route, so no selected center, selected subsequence, external modulus, quotient equality, semantic continuity theorem, or target-completeness row can be exported.
\end{proof}

\begin{theorem}[Compact uniform mesh obligation surface]
\label{thm:compact-uniform-mesh-modulus-obligation-surface}
For the carrier of \autoref{def:compact-uniform-mesh-modulus-carrier}, the local $\NameCert_{\CompactUniformMeshModulusUp}$ surface consists of five finite rows:
\begin{enumerate}
\item carrier admission for $U=(K,M,F,B,L,Q,T,R,H,C,P,N)$;
\item classifier locality for the $\CompactMetricUp$ source, $\ContinuousMapUp$ graph, finite $\ProbeBundle$ mesh, and $\RatUp$ radius histories;
\item stability through componentwise $\hsame$ transport $H$ and displayed $\Cont$ replay $C$;
\item exactness through the local-radius row $L$, rational lower-bound fold $Q$, metric-triangle handoff $T$, and $\UniformModulusUp$ output row $R$;
\item ledger control through $\Pkg$ provenance $P$ and the local naming row $N$.
\end{enumerate}
No NameCert obligation may replace these rows by open-cover compactness, a choice-selected mesh, semantic continuity, an external modulus function, quotient metric equality, selected subsequence data, or target completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:compact-uniform-mesh-modulus-carrier}. The rows $K,M,F,B,L,Q,T,R,H,C,P,N$ are all explicit, and no compact, continuous, mesh, fold, triangle, output, transport, replay, provenance, or naming row is implicit.

Classifier locality reads only the compact metric source, continuous graph, finite mesh, and rational radius histories. This keeps the mesh-to-modulus route inside displayed NameCert data.

Stability and exactness are then separated. The rows $H$ and $C$ preserve the same packet under transport and replay, while $L,Q,T,R$ state the finite lower-bound and uniform-modulus handoff consumed downstream.

The package and naming rows close the ledger. Since every exported read must pass through $P$ and $N$, the certificate surface is exactly the five listed rows and contains no coordinate for the excluded host principles.
\end{proof}

\falsifiablePrediction{A UniformModulusUp consumer refutes CompactUniformMeshModulusUp if it reaches the tolerance-only row without displaying the CompactMetricUp finite mesh, ContinuousMapUp pointwise-radius rows, finite ProbeBundle mesh, RatUp lower-bound fold, metric-triangle handoff, transport, replay, provenance, and local naming rows.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, rat, finitenetminimumfold, and compact-uniform-ladder siblings: none is bijective to $\CompactUniformMeshModulusUp$ because only this packet binds compact finite-mesh coverage, pointwise radii, rational lower-bound folding, metric-triangle handoff, uniform-modulus output, transport, replay, provenance, and local naming as one NameCert carrier.}

\closureat{CompactUniformMeshModulusUp}{seedStr}

\begin{closurestatus}{\CompactUniformMeshModulusUp}
  \constructivestory{A $\CompactUniformMeshModulusUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise-modulus rows, finite ProbeBundle mesh rows, Rat lower-bound folds, metric-triangle handoff rows, UniformModulus output rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-mesh-modulus-carrier}, \autoref{thm:compact-uniform-mesh-modulus-finite-net-handoff}, and \autoref{thm:compact-uniform-mesh-modulus-obligation-surface} seed the finite mesh-to-uniform-modulus route for compact continuous maps.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, choice-selected meshes, selected subsequences, semantic continuity, external modulus functions, quotient metric equality, target completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact mesh exposure, pointwise-radius attachment, rational lower-bound fold exactness, metric-triangle handoff, UniformModulusUp output, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformMeshModulusUp.}
\end{closurestatus}
