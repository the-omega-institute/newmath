\chapter{A Concrete Naming Certificate for $\ExtremeValueTheoremUp$}
\label{ch:concrete-instances-extreme-value-theorem-namecert}
\origin{human}

Following Weierstrass and Bishop--Bridges constructive analysis, the $\ExtremeValueTheoremUp$ packet records the compact-real attained-extreme theorem as finite BEDC NameCert data. It sits over the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the closed bounded interval source of \autoref{ch:concrete-instances-closedboundedinterval-namecert}, the continuous-map row of \autoref{ch:concrete-instances-continuousmap-namecert}, the located supremum and upper-bound rows of \autoref{ch:concrete-instances-locatedsupremum-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet exposes the finite witness-window route for extrema without importing open-cover compactness, trichotomy, selected host extrema, or ambient $\RealUp$ completeness.

\begin{definition}[Extreme value theorem carrier]
\label{def:extreme-value-theorem-carrier}
An $\ExtremeValueTheoremUp$ carrier is a finite $\BHist$ packet
$$
E=(I,K,F,R,S,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\ClosedBoundedIntervalUp$ source row, carrying endpoint order, interval containment, finite windows, and real endpoint seals.
\item $K$ is the compact source row read through $\CompactMetricUp$, exposing the finite compact-real inspection surface used by the interval source.
\item $F$ is the $\ContinuousMapUp$ row for the real-valued map on the displayed compact source.
\item $R$ is the $\RealUp$ value seal row for inspected values and endpoint comparisons.
\item $S$ is the $\LocatedSupremumUp$ row carrying the located upper-bound surface for the displayed value family.
\item $W$ is the finite witness-window row, recording the inspected compact windows in which the located upper-bound row and the continuous-map row meet a terminal value seal.
\item $H$ records componentwise $\hsame$ transport for the interval source, compact source, continuous-map row, real value seals, located-supremum row, witness windows, replay rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-real inspection requests through $I,K,F,R,S,W,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\ExtremeValueTheoremUp$, $\ClosedBoundedIntervalUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\LocatedSupremumUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ExtremeValueTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only by the displayed interval source, compact source, continuous-map row, value seal, located upper-bound row, finite witness-window row, transport, replay, provenance, and local naming. It has no coordinate for open-cover compactness, trichotomy, selected host extrema, quotient real equality, or ambient real completeness.
\end{definition}

\begin{theorem}[Extreme value theorem NameCert obligations]
\label{thm:extreme-value-theorem-namecert-obligations}
For every accepted $\ExtremeValueTheoremUp$ carrier $E=(I,K,F,R,S,W,H,C,P,N)$, the local $\NameCert_{\ExtremeValueTheoremUp}$ surface consists exactly of carrier admission for the closed bounded interval source, compact source, continuous-map row, $\RealUp$ value seal, located-supremum and upper-bound row, finite witness-window row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:extreme-value-theorem-carrier}. The interval row $I$ and compact row $K$ expose the only source windows available to the theorem packet, while $F$ records the continuous real-valued map inspected on those windows.

Read the real and upper-bound rows next. The value seal $R$ supplies only $\RealUp$ rows for displayed function values, and $S$ supplies the located upper-bound surface through the located-supremum chapter.

The witness-window row $W$ binds the source windows, continuous-map reads, value seals, and upper-bound reads into one finite inspection route. The remaining rows $H,C,P,N$ transport, replay, source, and name the same route, so the listed data exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Extreme value theorem compact supremum route]
\label{thm:extreme-value-theorem-compact-supremum-route}
Every accepted $\ExtremeValueTheoremUp$ consumer that reads an attained extreme must factor through the displayed route
$$
I,K \longmapsto F \longmapsto R,S \longmapsto W \longmapsto H,C,P,N .
$$
Thus the compact-real theorem surface exports only a finite witness-window route from a compact source and continuous-map row to a $\RealUp$ value seal and located upper-bound row. It does not export open-cover compactness, trichotomy, a selected host maximum or minimum, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the source rows. The closed bounded interval source and compact metric source are displayed as $I$ and $K$, so every later value comparison is scoped to the same finite compact-real windows.

Feed the source into the continuous-map row. The row $F$ exposes only displayed value reads, and $R$ seals those reads as $\RealUp$ values before any upper-bound comparison is consumed.

Use the located-supremum row $S$ and the witness-window row $W$ together. The upper-bound route is visible only through the finite windows in which $F$ and $R$ have already been displayed.

Finally, transport and replay preserve the same route through $H$ and $C$, while $P$ and $N$ provide provenance and local naming. Since the carrier has no coordinate for open-cover compactness, trichotomy, selected extrema, quotient real equality, or ambient completeness, the exported theorem surface remains the finite compact-supremum route shown above.
\end{proof}

\falsifiablePrediction{Every $\ExtremeValueTheoremUp$ attained-extreme read factors through $\ClosedBoundedIntervalUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RealUp$, and $\LocatedSupremumUp$ before reaching its finite witness-window and local $\NameCert_{\ExtremeValueTheoremUp}$ rows.}

\independenceWitness{closedboundedinterval, compactmetric, continuousmap, locatedsupremum, real: $\ExtremeValueTheoremUp$ is not bijective to any listed sibling because it jointly carries a compact source, continuous-map row, Real value seal, located upper-bound row, finite witness-window row, transport, replay, provenance, and local naming.}

\closureat{ExtremeValueTheoremUp}{seedStr}

\begin{closurestatus}{\ExtremeValueTheoremUp}
  \constructivestory{An $\ExtremeValueTheoremUp$ packet is generated from a ClosedBoundedIntervalUp source row, a CompactMetricUp compact source row, a ContinuousMapUp real-valued map row, RealUp value seals, a LocatedSupremumUp upper-bound row, a finite witness-window row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:extreme-value-theorem-carrier}, \autoref{thm:extreme-value-theorem-namecert-obligations}, and \autoref{thm:extreme-value-theorem-compact-supremum-route} seed the finite compact-real attained-extreme route over interval, compact-source, continuous-map, located-supremum, witness-window, and Real seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, trichotomy, selected host extrema, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\ExtremeValueTheoremUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, NameCert obligations, finite witness-window exactness, located upper-bound routing, Real value sealing, replay stability, and non-escape for BEDC.Derived.ExtremeValueTheoremUp.}
\end{closurestatus}
