Every $\GaugeSpaceUp$ read first receives metric distance rows from \autoref{ch:concrete-instances-metric-namecert}, then packages the generated entourage rows consumed by \autoref{ch:concrete-instances-uniformspace-namecert}. The same carrier boundary is compared against the missing-symmetry surface of \autoref{ch:concrete-instances-quasi-uniform-space-namecert}: with the symmetry row present it is a uniform gauge presentation, and without that row it remains a directional quasi-uniform presentation rather than a primitive entourage filter.
