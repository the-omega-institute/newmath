Every $\GaugeSpaceUp$ packet consumes the $\MetricUp$ distance-witness rows of \autoref{ch:concrete-instances-metric-namecert} together with the $\UniformSpaceUp$ entourage rows of \autoref{ch:concrete-instances-uniformspace-namecert}. The dependency route presents gauge families as finite NameCert data: each gauge observation is read as a metric-controlled distance row, each comparison is replayed through a uniform entourage row, and the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows keep the gauge carrier from exporting an ambient topology or quotient uniformity not displayed by those siblings.
