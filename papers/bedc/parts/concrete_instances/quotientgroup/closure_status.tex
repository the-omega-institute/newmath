\closureat{QuotientGroupUp}{matureStr}[BEDC.Derived.QuotientGroupUp.QuotientGroupCentralizerNormalizer\_semanticNameCert]
\begin{closurestatus}{\QuotientGroupUp}
  \constructivestory{A quotient-group object here is not formed by making a host quotient type: it starts with a BEDC $\GroupUp$ operation, a centralizer subgroup $Z_a$, and its normalizer carrier $N_a$, and it admits precisely the representative histories in $N_a$ while the centralizer records which representative differences are invisible. Two accepted representatives $x$ and $y$ are classified either by the displayed coset-orbit witness or, equivalently, by the relative-kernel condition that $\mathsf{mul}(\mathsf{inv}(x),y)$ lies in $Z_a$; the chapter proves these readings agree and uses that classifier instead of any $\mathsf{Quot}$ soundness principle. The source specification is the consumed group and subgroup tower with normality and centralizer-normalizer rows, the pattern specification is representative multiplication, unit, inverse, identity fiber, abelian terminal projection, and kernel exactness, and the ledger keeps the two representatives, product and inverse words, centralizer witness, normalizer endpoint rows, and transport provenance visible. The name-certificate obligations are supplied by the empty representative in the normalizer, $\hsame$ transport of that representative, reflexivity, symmetry, and transitivity of the representative classifier, and closure of the carrier under replacing an accepted representative by a classified one. A representative history $h$ lies in the signature gap of a $\QuotientGroupUp$ token $p$ when the source-domain first accepts $h$ in the normalizer carrier, the signature relation pairs it with an answer representative and the kernel-or-coset evidence ledger, and the token-introduction relation names that answer as $p$; after unfolding, this says that $h$ represents the same centralizer-normalizer coset as the representative encoded by $p$. The paper bridge compares the displayed orbit and kernel criteria with the ordinary coset/kernel reading, but the BEDC surface remains the representative carrier plus classifier. The chapter registers this centralizer-normalizer construction under the name $\QuotientGroupUp$; arbitrary quotients, universal properties, host quotient objects, and checked standard-model bridge data are not introduced.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature QuotientGroupUp package is the centralizer-normalizer SemanticNameCert together with the abelian terminal projection, projection uniqueness, identity-fiber totality, and classifier-totality rows of \autoref{thm:quotientgroup-centralizer-normalizer-semantic-certificate}, \autoref{thm:abelian-centralizer-normalizer-quotient-terminal-projection}, \autoref{thm:abelian-centralizer-normalizer-terminal-projection-uniqueness}, \autoref{thm:abelian-centralizer-normalizer-quotient-terminal-fiber}, and \autoref{thm:abelian-centralizer-normalizer-quotient-classifier-totality}.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.QuotientGroupUp.QuotientGroupCentralizerNormalizer\_semanticNameCert}
  \bridgestatus{paperBridge}
  \notclaimed{Arbitrary quotient groups, universal properties, arbitrary host quotients, and checked standard-model bridge data are not closed by this chapter.}
  \upgradepath{A stronger status requires checked standard-model bridge data and broader host quotient coverage, independently of the paper-side mature QuotientGroupUp package recorded here.}
\end{closurestatus}
