\chapter{A Concrete Naming Certificate for $\HyperspaceUp$}
\label{ch:concrete-instances-hyperspace-namecert}
\origin{human}

Following Vietoris and Kelley on compact hyperspaces, the $\HyperspaceUp$ packet names the finite BEDC surface for nonempty compact subsets of a displayed compact metric source. It sits over the metric source of \autoref{ch:concrete-instances-metric-namecert}, the compact finite-net row of \autoref{ch:concrete-instances-compactmetric-namecert}, the total-boundedness schedule of \autoref{ch:concrete-instances-totallybounded-namecert}, the completeness boundary of \autoref{ch:concrete-instances-completemetric-namecert}, the diameter ledger of \autoref{ch:concrete-instances-metric-diameter-namecert}, the directed convergence row of \autoref{ch:concrete-instances-netconvergence-namecert}, and the uniform Cauchy-net route of \autoref{ch:concrete-instances-cauchy-uniform-net-namecert}. The packet is a finite Hausdorff-distance ledger for compact-subset presentations; it is not a host powerset, not a quotient compactum, not a selected limit object, not open-cover compactness, and not a choice-selected representative.

\begin{definition}[Hyperspace carrier]
\label{def:hyperspace-carrier}
A $\HyperspaceUp$ carrier is a finite $\BHist$ packet
$$
H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source $\CompactMetricUp$ row, including its $\MetricUp$ distance witnesses, finite-net schedules, total-boundedness row, and completeness boundary.
\item $K_0$ and $K_1$ are compact-subset presentations over $X$, each given by finite net schedules whose centers and radii are readable only through the displayed $\CompactMetricUp$ and $\TotallyBoundedUp$ rows.
\item $N_0$ and $N_1$ are the requested net-resolution ledgers for $K_0$ and $K_1$, recording the finite witness lists used to inspect each subset presentation.
\item $D_0$ is the directed Hausdorff-distance coverage ledger from $K_0$ to $K_1$: every displayed center of $K_0$ is matched to a displayed $K_1$ center within the current tolerance after routing through the source metric row.
\item $D_1$ is the directed Hausdorff-distance coverage ledger from $K_1$ to $K_0$ with the same source row, tolerance budget, and finite-read discipline.
\item $R$ is the dyadic and $\RealUp$ tolerance readback for the two directed ledgers, including the comparison cells that package the maximum of $D_0$ and $D_1$ as the finite Hausdorff-distance request.
\item $H_s$ records componentwise $\hsame$ transport for the compact source, the two subset presentations, the two net ledgers, the two directed coverage ledgers, the tolerance readback, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-subset request through $X,K_0,K_1,N_0,N_1,D_0,D_1,R$ and then to the structural rows.
\item $P$ is the $\Pkg$ provenance over $\HyperspaceUp$, $\MetricUp$, $\CompactMetricUp$, $\TotallyBoundedUp$, $\CompleteMetricUp$, $\MetricDiameterUp$, $\NetConvergenceUp$, $\CauchyUniformNetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $M$ is the local $\NameCert_{\HyperspaceUp}$ row.
\end{enumerate}
The carrier has no coordinate for host powersets, quotient compacta, selected limits, open-cover compactness, or choice-selected representatives.
\end{definition}
\leandef{BEDC.Derived.HyperspaceUp.HyperspaceCarrier}

\begin{theorem}[Hausdorff-distance ledger stability]
\label{thm:hyperspace-hausdorff-distance-stability}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, classifier transport preserves the two-sided finite Hausdorff-distance ledger. More precisely, any componentwise $\hsame$ transport of the displayed source, subset presentations, net ledgers, directed coverage ledgers, and tolerance readback carries the finite route through $\CompactMetricUp$, $\MetricDiameterUp$, $\NetConvergenceUp$, and $\CauchyUniformNetUp$ to a carrier with the same directed $K_0$-to-$K_1$ and $K_1$-to-$K_0$ coverage obligations.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_hausdorff\_distance\_stability}
\begin{proof}
Project the accepted carrier of \autoref{def:hyperspace-carrier}. The source row $X$ fixes the compact metric surface, while $K_0,K_1,N_0,N_1$ identify the finite subset-net data that any Hausdorff-distance read can inspect.

The two directed ledgers are then transported componentwise. The row $H_s$ compares the source, subset presentations, net schedules, directed coverage rows, and tolerance readback, and the replay row $C$ repeats the same route after transport.

The compact-source part of the route remains inside $\CompactMetricUp$. Diameter and tolerance comparisons are read through $\MetricDiameterUp$ and $R$, and the finite convergence and uniform-net inspections are routed through $\NetConvergenceUp$ and $\CauchyUniformNetUp$.

Thus each transported center match in $D_0$ and $D_1$ still cites the same kind of displayed source-distance witness and the same finite tolerance readback. Since the packet contains no host powerset, quotient compactum, selected limit, open-cover compactness, or choice-selected representative, no external coordinate can alter the two-sided ledger.
\end{proof}

\begin{theorem}[Hyperspace NameCert obligations]
\label{thm:hyperspace-namecert-obligations}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, the local $\NameCert_{\HyperspaceUp}$ surface consists exactly of the following five obligation rows:
\begin{enumerate}
\item compact-source admission for the displayed $\CompactMetricUp$ source row $X$ and its $\MetricUp$, $\TotallyBoundedUp$, and $\CompleteMetricUp$ support;
\item finite subset-net admission for $K_0,K_1,N_0,N_1$, requiring every subset presentation and net resolution to be read through the displayed compact-source schedules;
\item bidirectional distance coverage, requiring both directed ledgers $D_0$ and $D_1$ to be present before the Hausdorff-distance tolerance readback $R$ is public;
\item classifier and transport stability through $H_s$ and $C$, preserving the source, subset-net, coverage, and readback rows under componentwise $\hsame$ and displayed replay;
\item provenance and name exhaustion through $P$ and $M$, excluding host powersets, quotient compacta, selected limits, open-cover compactness, and choice-selected representatives.
\end{enumerate}
No local NameCert obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is projection from \autoref{def:hyperspace-carrier}. The packet exposes a compact metric source $X$ and only the displayed finite rows attached to $X$.

Finite subset-net admission follows from the same projection. The rows $K_0,K_1,N_0,N_1$ are the only subset-presentation and resolution coordinates, so every compact-subset inspection passes through them.

Bidirectional coverage is exactly the pair $D_0,D_1$. A Hausdorff-distance read is not public until both directed ledgers have been consumed and the dyadic or $\RealUp$ tolerance readback $R$ has compared them.

Transport is componentwise through $H_s$ and replayed through $C$. These rows preserve the compact source, subset nets, directed coverage witnesses, and tolerance readbacks without adding a second classifier surface.

Finally, $P$ and $M$ name precisely the displayed packet. Because the carrier contains no row for host powersets, quotient compacta, selected limits, open-cover compactness, or choice-selected representatives, the five listed rows exhaust the local naming obligations.
\end{proof}

\begin{theorem}[Hyperspace Hausdorff-obligation surface]
\label{thm:hyperspace-hausdorff-obligation-surface}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, any public Hausdorff-distance obligation must expose compact-source admission through $X$, finite subset-net schedules through $K_0,K_1,N_0,N_1$, both directed ledgers $D_0,D_1$, tolerance readback through $R$, componentwise $\hsame$ transport through $H_s$, displayed $\Cont$ replay through $C$, provenance through $P$, and the local name through $M$ before a consumer reads a symmetric distance.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceHausdorffObligationSurface}
\begin{proof}
Start with the carrier projection in \autoref{def:hyperspace-carrier}. It lists exactly the compact source, the two subset presentations, the two finite net schedules, the directed cover rows, the tolerance readback, transport, replay, provenance, and local naming rows.

Use \autoref{thm:hyperspace-namecert-obligations}. Its carrier-admission, finite subset-net, bidirectional coverage, transport-stability, and provenance clauses order the public obligation surface before any Hausdorff-distance consumer can read the symmetric value.

Now apply \autoref{thm:hyperspace-finite-net-hit} and \autoref{thm:hyperspace-finite-hit-directed-cover}. They force each displayed hit through the compact source and finite schedules, and they require both directed ledgers $D_0$ and $D_1$ before the symmetric readback $R$ is public.

The excluded objects are absent from the carrier. Host powersets, quotient compacta, selected limits, open-cover compactness, and choice-selected representatives therefore cannot supply a missing Hausdorff obligation or replace any of $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$.
\end{proof}

\begin{theorem}[Hyperspace carrier obligations]
\label{thm:hyperspace-carrier-obligations}
For every accepted $\HyperspaceUp$ carrier
$$
H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M),
$$
carrier projection exposes exactly compact-source admission through $X$, subset-net schedules through $K_0,K_1,N_0,N_1$, directed-coverage ledgers through $D_0,D_1$, tolerance readback through $R$, componentwise transport through $H_s$, displayed replay through $C$, provenance through $P$, and local $\NameCert$ rows through $M$. No root consumer receives any carrier row outside this list.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceCarrierObligations}
\begin{proof}
Project the displayed carrier of \autoref{def:hyperspace-carrier}. Its coordinates are precisely $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$.

Use \autoref{thm:hyperspace-namecert-obligations}. The compact-source, finite subset-net, bidirectional coverage, transport, replay, provenance, and local-name clauses account for each coordinate in the carrier.

The tolerance readback is $R$, and the directed-coverage ledgers are $D_0,D_1$. Since the excluded host powerset, quotient compactum, selected-limit, open-cover, and choice-selected coordinates are absent, no root consumer can read beyond the listed rows.
\end{proof}

\begin{theorem}[Hyperspace finite-net hit]
\label{thm:hyperspace-finite-net-hit}
Every directed Hausdorff hit exposed by an accepted $\HyperspaceUp$ carrier must first pass through the $\CompactMetricUp$ source row $X$ and the $\TotallyBoundedUp$ finite-net schedules carried by $K_0,K_1,N_0,N_1$ before the tolerance readback row $R$ can be consumed. Thus a hit in $D_0$ or $D_1$ is a scheduled finite-net comparison over the displayed compact source, not a host point-selection event.
\end{theorem}
\begin{proof}
Read \autoref{thm:hyperspace-carrier-obligations}. A directed hit is public only inside $D_0$ or $D_1$, and those rows occur after the compact-source and subset-net schedules have been exposed.

The schedules $K_0,K_1,N_0,N_1$ are attached to the same source row $X$. They carry the finite centers and resolution ledgers that a Hausdorff comparison may inspect.

Only after those rows are present can $R$ compare the directed ledgers at the requested tolerance. Therefore every hit is routed through $\CompactMetricUp$ and $\TotallyBoundedUp$ finite-net data before tolerance readback, with no selected host point added to the carrier.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceFiniteNetHit}

\begin{theorem}[Hyperspace finite hit directed cover]
\label{thm:hyperspace-finite-hit-directed-cover}
Every finite Hausdorff hit accepted by a $\HyperspaceUp$ carrier must expose both directed cover rows $D_0$ and $D_1$ before any symmetric Hausdorff-distance read is public. A consumer may read a hit from $K_0$ toward $K_1$ only as part of $D_0$, and a hit from $K_1$ toward $K_0$ only as part of $D_1$; neither direction can be replaced by a host compact-subset choice, quotient compactum, selected nearest point, or one-sided coverage oracle.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceFiniteHitDirectedCover}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. The only directed coverage coordinates are $D_0$ and $D_1$, and both are listed before the tolerance readback row $R$.

Apply \autoref{thm:hyperspace-finite-net-hit}. Each displayed hit is already a finite-net comparison over the compact source and its total-bounded schedules.

Use \autoref{thm:hyperspace-namecert-obligations}. The bidirectional distance clause requires both directed ledgers before the public Hausdorff read is admitted, so no one-sided host selection can stand in for the missing direction.
\end{proof}

\begin{theorem}[Hyperspace tolerance readback exhaustion]
\label{thm:hyperspace-tolerance-readback-exhaustion}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, the tolerance visible to a finite Hausdorff-distance consumer is exhausted by the readback row $R$. Once the compact source, subset-net schedules, and directed ledgers $D_0,D_1$ have been displayed, no downstream consumer may read an additional tolerance comparison from $\CompleteMetricUp$, $\MetricDiameterUp$, $\NetConvergenceUp$, $\CauchyUniformNetUp$, a selected limit, or a host real completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceToleranceReadbackExhaustion}
\begin{proof}
Begin with \autoref{thm:hyperspace-finite-hit-directed-cover}. The symmetric Hausdorff read is not public until both directed cover rows $D_0$ and $D_1$ have been shown.

The carrier definition names $R$ as the dyadic and $\RealUp$ tolerance readback for those two directed ledgers. Thus every comparison between the two directions passes through the same displayed row.

Finally apply \autoref{thm:hyperspace-complete-metric-nonescape}. Completeness, diameter, convergence, and uniform-net rows support the finite route, but they do not export selected limits or an ambient tolerance object outside $R$.
\end{proof}

\begin{theorem}[Hyperspace compact source scope]
\label{thm:hyperspace-compact-source-scope}
Every compact-subset read of an accepted $\HyperspaceUp$ packet is scoped to the displayed rows
$$
\CompactMetricUp,\quad \TotallyBoundedUp,\quad \CompleteMetricUp,\quad
\MetricDiameterUp,\quad \NetConvergenceUp,\quad \CauchyUniformNetUp .
$$
The read may inspect compact source admission, finite-net schedules, completion boundary, diameter or tolerance comparisons, net-convergence replay, and uniform Cauchy-net replay only through those rows and the carrier coordinates $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$. It exports no host powerset of compact subsets, open-cover compactness coordinate, quotient compactum, selected limit, or choice-selected representative.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_compact\_source\_scope}
\begin{proof}
Read the source row $X$ in \autoref{def:hyperspace-carrier}. It already contains the compact metric source, total-bounded finite schedules, and complete metric boundary available to the packet.

Use \autoref{thm:hyperspace-compact-hausdorff-distance-route}. The public Hausdorff route passes through the diameter, directed convergence, and uniform Cauchy-net rows only as displayed support for $D_0,D_1,R$ and replay $C$.

Apply \autoref{thm:hyperspace-tolerance-readback-exhaustion}. Since tolerance is exhausted by $R$ and every directed hit is finite, the listed compact-source rows are the whole compact-subset scope.
\end{proof}

\begin{theorem}[Hyperspace CompleteMetric nonescape]
\label{thm:hyperspace-complete-metric-nonescape}
For every accepted $\HyperspaceUp$ carrier, the $\CompleteMetricUp$ support carried by the source row $X$ supplies only the displayed completion boundary needed by the finite compact-metric route. It cannot export selected limits, quotient compacta, open-cover compactness, choice-selected representatives, or an ambient hyperspace completion outside
$$
X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M .
$$
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_complete\_metric\_nonescape}
\begin{proof}
Begin with \autoref{thm:hyperspace-compact-hausdorff-distance-route}. Completeness enters the route through the compact metric source row $X$ before subset-net schedules, directed ledgers, and tolerance readback are public.

Apply \autoref{thm:hyperspace-finite-net-hit}. Any Hausdorff hit must pass through the finite-net schedules before it reaches $R$, so completeness cannot be read as an independent selected limit for a compact subset.

Finally use \autoref{thm:hyperspace-carrier-obligations}. The only remaining rows are transport, replay, provenance, and local naming. None is a quotient compactum, open-cover compactness proof, choice-selected representative, or ambient hyperspace completion, so the CompleteMetric support cannot escape the displayed boundary.
\end{proof}

\begin{theorem}[Hyperspace compact Hausdorff-distance route]
\label{thm:hyperspace-compact-hausdorff-distance-route}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, the public Hausdorff-distance route is exactly the finite route through the $\CompactMetricUp$ source admission row $X$, the $\TotallyBoundedUp$ finite-net schedules in $K_0,K_1,N_0,N_1$, the $\CompleteMetricUp$ limit boundary carried by $X$, the $\MetricDiameterUp$ tolerance readback in $R$, and the $\NetConvergenceUp$ and $\CauchyUniformNetUp$ replay named by the directed ledgers $D_0,D_1$ and the replay row $C$. No downstream compact-subset consumer receives a host powerset, open-cover compactness coordinate, quotient compactum, or selected limit object.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceCompactHausdorffDistanceRoute}
\begin{proof}
Project the carrier in \autoref{def:hyperspace-carrier}. The coordinate $X$ is the only compact metric source row, and it already contains the displayed metric witnesses, total-boundedness support, and completeness boundary.

The subset coordinates $K_0,K_1,N_0,N_1$ provide the finite net schedules. Thus every center inspected by a Hausdorff-distance request is read through the same $\TotallyBoundedUp$ schedule attached to the compact source rather than through an ambient compact-subset universe.

The directed ledgers $D_0$ and $D_1$ supply the two coverage directions. The tolerance row $R$ packages their dyadic and $\RealUp$ readback, so the distance request can only compare the two directed ledgers through the displayed $\MetricDiameterUp$ boundary.

Apply \autoref{thm:hyperspace-hausdorff-distance-stability} to transport the directed coverage rows and apply \autoref{thm:hyperspace-namecert-obligations} to exhaust the local naming surface. The replay row $C$ keeps every convergence read inside the displayed $\NetConvergenceUp$ and $\CauchyUniformNetUp$ route.

Finally, $P$ and $M$ certify that no further coordinates are public. The packet therefore exports the compact Hausdorff-distance route only through $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$.
\end{proof}

\begin{theorem}[Hyperspace finite Hausdorff carrier]
\label{thm:hyperspace-finite-hausdorff-carrier}
Every public Hausdorff-distance read of an accepted $\HyperspaceUp$ carrier is exactly the pair of directed finite ledgers $D_0,D_1$ together with the tolerance readback row $R$, over the displayed source and net rows
$$
X,K_0,K_1,N_0,N_1 .
$$
The read exposes no host powerset of compact subsets, quotient compactum, selected limit object, open-cover compactness coordinate, or choice-selected representative.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceFiniteHausdorffCarrier}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. The compact source row $X$ and the finite subset-net rows $K_0,K_1,N_0,N_1$ are the only rows that can be inspected before a Hausdorff-distance request.

Apply \autoref{thm:hyperspace-compact-hausdorff-distance-route}. The public distance route consumes both directed ledgers $D_0,D_1$ and then reads $R$ as the dyadic and $\RealUp$ tolerance comparison.

Use \autoref{thm:hyperspace-namecert-obligations} to exhaust the local naming surface. Since the forbidden host powerset, quotient, selected-limit, open-cover, and choice-selected coordinates are absent from the carrier, the finite directed-ledger read is the whole public Hausdorff surface.
\end{proof}

\begin{theorem}[Hyperspace complete-metric handoff]
\label{thm:hyperspace-complete-metric-handoff}
Completeness in an accepted $\HyperspaceUp$ packet enters only through the displayed $\CompactMetricUp$ source row $X$ and its $\CompleteMetricUp$ support. It cannot be replaced by host compactness, selected limit data, open-cover compactness, or an ambient compact-subset universe outside $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$.
\end{theorem}
\begin{proof}
Read the source coordinate $X$ in \autoref{def:hyperspace-carrier}. It is the only row that carries the compact metric source together with the displayed metric witnesses, finite-net schedules, total-boundedness row, and completeness boundary.

Apply \autoref{thm:hyperspace-finite-hausdorff-carrier}. The subset and distance rows use $X$ through finite net ledgers and directed coverage rows, so a Hausdorff read never asks for a host compact-subset universe or a selected limit.

Finally apply \autoref{thm:hyperspace-hausdorff-distance-stability}. Transport preserves the same source row and directed ledgers, while $H_s,C,P,M$ add only structural comparison, replay, provenance, and local naming. Thus completeness is read only through the displayed CompactMetric and CompleteMetric source support.
\end{proof}

\begin{definition}[Hyperspace finite-window carrier]
\label{def:hyperspace-finite-window-carrier}
The finite-window surface of an accepted $\HyperspaceUp$ carrier is the same displayed packet
$$
H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)
$$
read with $X$ as the $\CompactMetricUp$ source, $K_0,K_1,N_0,N_1$ as finite subset-net schedules over $X$, $D_0,D_1$ as the two directed Hausdorff ledgers, $R$ as the dyadic and $\RealUp$ readback row, $H_s$ as componentwise $\hsame$ transport, $C$ as displayed $\Cont$ replay, $P$ as $\Pkg$ provenance, and $M$ as the local $\NameCert_{\HyperspaceUp}$ row. A finite-window read has no coordinate for a host hyperspace, selected compact subset, open-cover compactness proof, quotient compactum, or ambient completeness witness outside $X$.
\end{definition}

\begin{theorem}[Hyperspace obligation surface]
\label{thm:hyperspace-namecert-obligation-surface}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, the root-unblock obligation surface consists exactly of the $\CompactMetricUp$ source admission in $X$, the finite subset-net schedules $K_0,K_1,N_0,N_1$, the two directed Hausdorff ledgers $D_0,D_1$, the readback row $R$, componentwise $\hsame$ transport through $H_s$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local $\NameCert$ rows through $M$. Every downstream compact-metric or Cauchy-uniform-net consumer must consume those rows before any Hausdorff-distance claim is public.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_namecert\_obligation\_surface}
\begin{proof}
Start from \autoref{def:hyperspace-finite-window-carrier}. It names the same twelve coordinates as the carrier of \autoref{def:hyperspace-carrier}, but marks the finite-window roles needed by root consumers.

Apply \autoref{thm:hyperspace-namecert-obligations}. The carrier, subset-net, bidirectional coverage, transport, replay, provenance, and local-name clauses are already exhaustive for the local certificate.

The root-unblock surface is therefore obtained by reading the same clauses in consumer order. CompactMetric admission comes from $X$, finite subset-net schedules from $K_0,K_1,N_0,N_1$, directed Hausdorff coverage from $D_0,D_1$, and readback from $R$.

No further row can be inserted because $H_s,C,P,M$ only transport, replay, source, and name the displayed packet. The forbidden host hyperspace, selected compact subset, quotient compactum, open-cover compactness proof, and ambient completeness witness are absent from the carrier.
\end{proof}

\begin{theorem}[Hyperspace CompactMetric distance lift]
\label{thm:hyperspace-compactmetric-distance-lift}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Any compact-metric distance consumer of $H$ reads a Hausdorff-distance request only by lifting the displayed $\CompactMetricUp$ source row $X$ through the finite subset-net schedules $K_0,K_1,N_0,N_1$, the two directed Hausdorff ledgers $D_0,D_1$, and the tolerance readback row $R$. The lift is stable under componentwise $\hsame$ through $H_s$, replayed by the displayed $\Cont$ row $C$, and named by the $\Pkg$ and local $\NameCert$ rows $P,M$.
\end{theorem}
\begin{proof}
Project the finite-window carrier from \autoref{def:hyperspace-finite-window-carrier}. The only compact-metric source row is $X$, so every distance read starts there.

Use \autoref{thm:hyperspace-compact-hausdorff-distance-route}. It routes the Hausdorff request through the subset-net schedules, both directed coverage ledgers, and the tolerance readback row before any public distance value is available.

Finally apply \autoref{thm:hyperspace-hausdorff-distance-stability}. Componentwise $\hsame$ transport through $H_s$ and displayed replay through $C$ preserve that route, while $P,M$ name it without adding a host compact-subset universe.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_compactmetric\_distance\_lift}

\begin{theorem}[Hyperspace totally bounded net lift]
\label{thm:hyperspace-totallybounded-net-lift}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every $\TotallyBoundedUp$ net request exposed by the hyperspace packet is read through the finite subset-net schedules $K_0,K_1,N_0,N_1$ over the same $\CompactMetricUp$ source row $X$. The request may use $D_0,D_1$ and $R$ to compare directed Hausdorff ledgers, but it exports no unscheduled net, selected center family, host powerset, or quotient compactum.
\end{theorem}
\begin{proof}
Read the root-unblock obligation surface of \autoref{thm:hyperspace-namecert-obligation-surface}. The finite subset-net schedules are named before any directed Hausdorff ledger can be public.

The total-boundedness source is part of $X$ and is inspected through $K_0,K_1,N_0,N_1$. Thus a net request cannot detach a center family from the compact source row or bypass the finite schedules.

The directed ledgers $D_0,D_1$ and readback $R$ compare only the scheduled data. Transport and replay through $H_s,C$ preserve the same rows, and the local naming rows $P,M$ exclude an unscheduled net, host powerset, and quotient compactum.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_totallybounded\_net\_lift}

\begin{theorem}[Hyperspace Vietoris compact-metric dependency]
\label{thm:hyperspace-vietoris-compactmetric-dependency}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every Vietoris-topology or Hausdorff-distance consumer of $H$ must read the $\CompactMetricUp$ source row $X$, the $\TotallyBoundedUp$ finite-net schedules in $K_0,K_1,N_0,N_1$, the $\CompleteMetricUp$ boundary carried by $X$, the $\MetricUp$ distance witnesses, and the two directed Hausdorff ledgers $D_0,D_1$ before any compact-subset topology route is public.

The dependency route exports no host powerset, open-cover compactness proof, quotient compactum, selected limit, or choice-selected representative.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. The compact metric source row $X$ is the only source from which metric witnesses, total-boundedness schedules, and completeness support can be read.

Use \autoref{thm:hyperspace-namecert-obligations}. Its carrier-admission, finite subset-net, bidirectional coverage, transport, and provenance clauses force every public read to pass through $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$.

Apply \autoref{thm:hyperspace-compact-hausdorff-distance-route} and \autoref{thm:hyperspace-finite-hausdorff-carrier}. They identify the Hausdorff route as a finite two-direction ledger over the displayed compact source and exclude a host compact-subset universe.

Finally use \autoref{thm:hyperspace-totallybounded-net-lift}. The Vietoris-facing finite-open or finite-hit information can inspect only the scheduled net rows attached to $X$, and the Hausdorff-facing read must retain both directed ledgers. Since no forbidden host coordinate is present in the carrier, every compact-subset topology consumer inherits exactly the compact-metric dependency named in the statement.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceVietorisCompactMetricDependency}

\begin{theorem}[Hyperspace CompleteMetric Cauchy lift]
\label{thm:hyperspace-completemetric-cauchy-lift}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every $\CompleteMetricUp$ or $\CauchyUniformNetUp$ consumer of the hyperspace route receives completeness only through the support carried by $X$, after the finite subset-net schedules and the two directed Hausdorff ledgers have been exposed. The lift is replayed through displayed $\Cont$, transported componentwise by $\hsame$, and named locally by $P,M$; it does not export selected limits, quotient completion representatives, open-cover compactness, or ambient hyperspace completeness.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:hyperspace-complete-metric-handoff}. Completeness enters the hyperspace packet only through the displayed compact metric source row $X$ and its CompleteMetric support.

Use \autoref{thm:hyperspace-totallybounded-net-lift}. A Cauchy-uniform-net consumer first sees finite subset-net schedules over the same source row and then the directed ledgers $D_0,D_1$ with tolerance readback $R$.

Apply \autoref{thm:hyperspace-hausdorff-distance-stability} to transport the route. The rows $H_s,C,P,M$ preserve, replay, source, and name the displayed packet, so no selected limit, quotient representative, open-cover compactness proof, or ambient hyperspace completeness coordinate is exported.
\end{proof}

\begin{theorem}[Hyperspace finite-net carrier exhaustion]
\label{thm:hyperspace-finite-net-carrier-exhaustion}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every finite-net inspection of a compact-subset presentation in the $\BHist$ packet is exhausted by the $\CompactMetricUp$ source row $X$, its $\MetricSpaceUp$ distance witnesses, its $\CompleteMetricUp$ support, the displayed subset-net rows $K_0,K_1$, and the resolution ledgers $N_0,N_1$. Componentwise $\hsame$ transport through $H_s$, displayed $\Cont$ replay through $C$, and the $\NameCert$ row $M$ preserve exactly those rows; no host compact-subset family, selected net, quotient compactum, or ambient completeness coordinate is public.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. The only compact source is $X$, and the only finite compact-subset presentations are $K_0,K_1$ with their resolution ledgers $N_0,N_1$.

Use \autoref{thm:hyperspace-namecert-obligation-surface}. The root-unblock surface orders the public rows as compact-source admission, finite subset-net schedules, directed Hausdorff ledgers, readback, transport, replay, provenance, and local naming.

Thus any finite-net inspection must stop at $X,K_0,K_1,N_0,N_1$ before reading the directed ledgers. The rows $H_s,C,M$ preserve and name that same visible packet, so no extra compact-subset carrier can be exposed.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_finite\_net\_carrier\_exhaustion}

\begin{theorem}[Hyperspace finite Vietoris carrier]
\label{thm:hyperspace-finite-vietoris-carrier}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every Vietoris-facing carrier read of a compact-source finite subset is exposed only through the displayed finite-net rows $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$ of \autoref{def:hyperspace-carrier}. The read may inspect the $\CompactMetricUp$ source row, the $\TotallyBoundedUp$ schedules, the $\CompleteMetricUp$ support carried by $X$, and the $\CauchyUniformNetUp$ replay row, but it does not export a host compact-subset universe, quotient compactum, open-cover compactness proof, choice-selected representative, or ambient hyperspace completion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. The compact-source finite subsets are represented by $K_0,K_1$ and their finite resolution ledgers $N_0,N_1$ over the same source row $X$.

Use \autoref{thm:hyperspace-finite-net-carrier-exhaustion}. It exhausts every finite-net inspection at $X,K_0,K_1,N_0,N_1$ before any directed Hausdorff ledger can be read.

Then apply \autoref{thm:hyperspace-vietoris-compactmetric-dependency} and \autoref{thm:hyperspace-compact-hausdorff-distance-route}. These force the Vietoris-facing route through the compact metric source, total-bounded schedules, complete-metric support, directed ledgers, and Cauchy-uniform-net replay. Since the carrier has no row for quotient compacta, open-cover compactness, selected representatives, host powersets, or ambient hyperspace completion, the finite Vietoris carrier is exactly the displayed packet.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceFiniteVietorisCarrier}

\begin{theorem}[Hyperspace hit-miss classifier]
\label{thm:hyperspace-hit-miss-classifier}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every hit or miss comparison used by a Vietoris-facing consumer is read through the finite directed-distance rows $D_0,D_1$ and the tolerance readback row $R$, after the finite subset-net schedules $K_0,K_1,N_0,N_1$ have been exposed over $X$. The classifier is therefore a finite directed Hausdorff ledger in the $\BHist$ packet, not a comparison inside a host compact-subset universe.
\end{theorem}
\begin{proof}
Start from the carrier projection of \autoref{def:hyperspace-carrier}. The only rows that can witness a hit or miss request are the finite subset-net schedules and the two directed coverage ledgers.

Apply \autoref{thm:hyperspace-finite-net-carrier-exhaustion}. It prevents the classifier from reading a compact-subset carrier beyond $X,K_0,K_1,N_0,N_1$ before the directed ledgers are public.

Use \autoref{thm:hyperspace-hausdorff-classifier-transport}. Componentwise $\hsame$ transport preserves the same directed ledgers $D_0,D_1$ and the same readback row $R$, so a transported classifier cannot replace them by a host distance, quotient compactum, selected representative, or ambient hyperspace metric.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceHitMissClassifier}

\begin{theorem}[Hyperspace finite-cover ledger]
\label{thm:hyperspace-finite-cover-ledger}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every finite-cover ledger consumed downstream is routed through the $\CompactMetricUp$ source row $X$, the $\TotallyBoundedUp$ schedules in $K_0,K_1,N_0,N_1$, the $\CompleteMetricUp$ support carried by $X$, and the $\CauchyUniformNetUp$ replay visible through $D_0,D_1,R,C$. The ledger exports no open-cover compactness principle, quotient compactum, choice-selected representative, host compact-subset universe, selected limit, or ambient hyperspace completion.
\end{theorem}
\begin{proof}
Project \autoref{def:hyperspace-carrier}. The finite-cover information is not a separate host cover object; it is the displayed subset-net data $K_0,K_1,N_0,N_1$ attached to $X$.

Use \autoref{thm:hyperspace-complete-totallybounded-obligations}. It identifies the complete-metric and total-boundedness obligations as the source support in $X$, the finite schedules, the two directed ledgers, and the tolerance readback.

Finally apply \autoref{thm:hyperspace-completemetric-cauchy-lift} and \autoref{thm:hyperspace-compact-complete-handoff}. These route downstream Cauchy-uniform-net and compact-complete consumers through the same finite ledger. No row remains from which to read open-cover compactness, quotient compacta, selected representatives, selected limits, host compact subsets, or ambient hyperspace completion.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceFiniteCoverLedger}

\begin{theorem}[Hyperspace Hausdorff classifier transport]
\label{thm:hyperspace-hausdorff-classifier-transport}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Any Hausdorff-distance classifier comparison transported by $\hsame$ through the $\BHist$ packet must carry both directed ledgers $D_0,D_1$ over the same $\CompactMetricUp$, $\MetricSpaceUp$, and $\CompleteMetricUp$ source rows, and must replay through the displayed $\Cont$ row $C$ before the $\NameCert$ row $M$ names the comparison. The transport cannot replace either directed ledger by a host distance, quotient compactum, selected limit, or external hyperspace metric.
\end{theorem}
\begin{proof}
Apply \autoref{thm:hyperspace-hausdorff-distance-stability}. It states that componentwise $\hsame$ transport preserves the two directed Hausdorff ledgers and the tolerance readback.

The compact-source part of the comparison is fixed by \autoref{thm:hyperspace-vietoris-compactmetric-dependency}. A classifier comparison must therefore retain the same $\CompactMetricUp$, $\MetricSpaceUp$, and $\CompleteMetricUp$ source rows before any distance route is public.

Finally use \autoref{thm:hyperspace-completemetric-cauchy-lift}. Displayed $\Cont$ replay keeps the transported comparison inside the finite Cauchy-uniform-net route, and the local $\NameCert$ row names only that packet.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceHausdorffClassifierTransport}

\begin{theorem}[Hyperspace compact-complete handoff]
\label{thm:hyperspace-compact-complete-handoff}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every downstream compact-complete consumer receives a handoff whose $\BHist$ rows are exactly the $\CompactMetricUp$ source $X$, the $\MetricSpaceUp$ distance witnesses inside $X$, the $\CompleteMetricUp$ boundary carried by $X$, the finite subset-net rows $K_0,K_1,N_0,N_1$, the directed Hausdorff ledgers $D_0,D_1$, componentwise $\hsame$ transport $H_s$, displayed $\Cont$ replay $C$, and local $\NameCert$ naming through $M$. The handoff exports no ambient hyperspace completeness proof, open-cover compactness proof, selected Cauchy limit, quotient completion representative, or host powerset.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:hyperspace-completemetric-cauchy-lift}. Completeness is handed to downstream Cauchy consumers only through the support carried by $X$ after the finite subset-net schedules and directed Hausdorff ledgers are exposed.

Use \autoref{thm:hyperspace-finite-net-carrier-exhaustion}. It identifies the compact-subset carrier rows that a downstream consumer may inspect before reading the distance ledgers.

Then apply \autoref{thm:hyperspace-hausdorff-classifier-transport}. Transport and replay preserve the same compact-source, finite-net, and directed-ledger rows, and $M$ names only the displayed packet. Therefore the compact-complete handoff is exactly the finite BEDC interface stated above.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceCompactCompleteHandoff}

\begin{definition}[Hyperspace finite-net carrier]
\label{def:hyperspace-finite-net-carrier}
The finite-net carrier of an accepted $\HyperspaceUp$ packet is the displayed subpacket
$$
F_H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)
$$
read with $X$ as the $\CompactMetricUp$ source row, $K_0,K_1,N_0,N_1$ as compact finite-net data, $D_0,D_1$ as the two Hausdorff coverage ledgers, $R$ as dyadic and $\RealUp$ tolerance readback, $H_s$ as componentwise $\hsame$ transport, $C$ as displayed $\Cont$ replay, $P$ as $\Pkg$ provenance, and $M$ as the local $\NameCert_{\HyperspaceUp}$ row. This carrier has no coordinate for host powersets, selected compact representatives, open-cover compactness, quotient compacta, or ambient hyperspace completeness.
\end{definition}

\begin{theorem}[Hyperspace complete and totally bounded obligations]
\label{thm:hyperspace-complete-totallybounded-obligations}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. The complete-metric and total-boundedness obligations exposed by the finite-net carrier are exactly the $\CompleteMetricUp$ support carried by the compact source row $X$, the $\TotallyBoundedUp$ finite-net schedules read by $K_0,K_1,N_0,N_1$, the two directed Hausdorff coverage ledgers $D_0,D_1$, the tolerance readback $R$, componentwise $\hsame$ transport $H_s$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local naming $M$.

No downstream consumer receives a host powerset of compact subsets, a selected compact representative, open-cover compactness, quotient compactness, or a selected Cauchy limit.
\end{theorem}
\begin{proof}
Start from \autoref{def:hyperspace-finite-net-carrier}. It records the same finite rows as the accepted carrier and marks the compact finite-net roles used by root consumers.

Apply \autoref{thm:hyperspace-complete-metric-handoff}. Completeness is read only through the $\CompactMetricUp$ source row $X$ and its displayed $\CompleteMetricUp$ support.

Apply \autoref{thm:hyperspace-totallybounded-net-lift}. Total-boundedness is read through the finite schedules $K_0,K_1,N_0,N_1$ over the same source row. The directed ledgers and tolerance readback can compare only those scheduled rows.

The transport, replay, provenance, and local naming rows preserve this finite packet. Since the excluded host and selection coordinates are absent, the listed rows exhaust the complete and totally bounded obligations.
\end{proof}

\begin{theorem}[Hyperspace Cauchy-uniform-net handoff]
\label{thm:hyperspace-cauchy-uniform-net-handoff}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every $\CauchyUniformNetUp$ handoff from $H$ must pass through the compact finite-net carrier, inspect the $\CompactMetricUp$ source $X$, read the finite subset-net schedules $K_0,K_1,N_0,N_1$, consume both directed coverage ledgers $D_0,D_1$, and replay the same route through $C$ after componentwise $\hsame$ transport in $H_s$.

The handoff exports only finite Cauchy-uniform-net data named by $P$ and $M$. It exports no host powerset, selected compact representative, selected limit, open-cover compactness proof, or quotient compactum.
\end{theorem}
\begin{proof}
Use \autoref{thm:hyperspace-completemetric-cauchy-lift}. It sends every $\CompleteMetricUp$ or $\CauchyUniformNetUp$ consumer through the completeness support carried by $X$, after finite subset-net schedules and directed Hausdorff ledgers are exposed.

The finite-net carrier of \autoref{def:hyperspace-finite-net-carrier} fixes the rows available to that handoff. The consumer reads $X,K_0,K_1,N_0,N_1,D_0,D_1,R$ and then follows the structural rows.

Transport and replay are supplied by $H_s$ and $C$. The local naming and provenance rows $P,M$ record the same route, so no selected compact representative, quotient compactum, open-cover proof, host powerset, or selected limit can be introduced at the handoff.
\end{proof}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceCauchyUniformNetHandoff}

\begin{theorem}[Hyperspace metric compactness unblock]
\label{thm:hyperspace-metric-compactness-unblock}
Every compact-metric downstream consumer of an accepted $\HyperspaceUp$ packet can read metric compactness only through the finite-net carrier of \autoref{def:hyperspace-finite-net-carrier}. The unblocked surface consists of compact finite-net data, $\CompleteMetricUp$ support, $\TotallyBoundedUp$ schedules, the $\CauchyUniformNetUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row.

The unblock supplies no host powerset, no selected compact representative, no open-cover compactness principle, no quotient compactum, and no ambient hyperspace completeness theorem.
\end{theorem}
\begin{proof}
First read the complete and totally bounded obligations of \autoref{thm:hyperspace-complete-totallybounded-obligations}. They give exactly the compact source support, finite schedules, directed ledgers, tolerance readback, transport, replay, provenance, and local naming rows.

Next apply the Cauchy-uniform-net handoff of \autoref{thm:hyperspace-cauchy-uniform-net-handoff}. It routes every Cauchy-uniform-net consumer through the same compact finite-net carrier before any downstream compactness read is public.

Finally use \autoref{thm:hyperspace-compact-complete-handoff}. The compact-complete consumer receives only the displayed $\BHist$ rows over $X,K_0,K_1,N_0,N_1,D_0,D_1,H_s,C,M$. Thus metric compactness is unblocked as finite BEDC data and not as a host powerset, selected representative, open-cover principle, quotient compactum, or ambient completeness theorem.
\end{proof}

\begin{theorem}[Hyperspace compact-metric Vietoris unblock]
\label{thm:hyperspace-compactmetric-vietoris-unblock}
Every Vietoris-facing downstream consumer of an accepted $\HyperspaceUp$ carrier must read compact-source support through $\CompactMetricUp$, $\TotallyBoundedUp$, $\CompleteMetricUp$, $\MetricDiameterUp$, $\NetConvergenceUp$, and $\CauchyUniformNetUp$ before any hit/miss or Hausdorff-distance classifier is public. The route exports no host powerset, quotient compactum, selected representative, open-cover compactness, or ambient hyperspace completion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. Its rows expose the compact metric source, finite subset-net presentations, directed Hausdorff ledgers, tolerance readback, transport, replay, provenance, and local naming.

Use \autoref{thm:hyperspace-finite-net-carrier-exhaustion}. It forces every compact-source finite-net read through the displayed rows before a downstream consumer can inspect distance or Vietoris data.

Apply \autoref{thm:hyperspace-hit-miss-classifier} and \autoref{thm:hyperspace-finite-cover-ledger}. The hit/miss and finite-cover reads are both mediated by the same finite net schedules and directed ledgers, so neither supplies a host powerset or an open-cover compactness principle.

Finally use \autoref{thm:hyperspace-metric-compactness-unblock}. Compact-metric readiness is public only through the compact source, total-bounded schedules, completeness support, metric-diameter tolerance, net-convergence route, and Cauchy-uniform-net handoff already present in the accepted carrier. The forbidden quotient, representative, powerset, open-cover, and ambient-completion coordinates are absent.
\end{proof}

\begin{theorem}[Hyperspace finite-cover selector refusal]
\label{thm:hyperspace-finite-cover-selector-refusal}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every finite-cover read exported by $H$ is mediated by the displayed compact finite-net rows, the Vietoris hit/miss rows, the two directed coverage ledgers $D_0,D_1$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row. The packet cannot export a selected compact representative, a selected point of a cover member, a host powerset element, a quotient compactum, or an open-cover compactness oracle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. Its only compact-subset data are the compact metric source, the finite subset-net schedules, directed ledgers, tolerance readback, and structural rows.

Use \autoref{thm:hyperspace-finite-net-carrier-exhaustion}. It forces every compact finite-net read through $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$, so no selected representative or host powerset coordinate is available.

Apply \autoref{thm:hyperspace-hit-miss-classifier}, \autoref{thm:hyperspace-finite-cover-ledger}, and \autoref{thm:hyperspace-compactmetric-vietoris-unblock}. The hit/miss and finite-cover classifiers are read from the same finite-net and Vietoris rows already required by the compact-metric route. Therefore the finite-cover surface is a refusal of selectors, not a source of hidden compact subsets.
\end{proof}

\begin{theorem}[Hyperspace hit-miss coverage totality]
\label{thm:hyperspace-hit-miss-coverage-totality}
Every hit/miss coverage read from an accepted $\HyperspaceUp$ carrier factors through the displayed $\CompactMetricUp$, $\TotallyBoundedUp$, $\CompleteMetricUp$, $\MetricDiameterUp$, $\NetConvergenceUp$, and $\CauchyUniformNetUp$ rows before it is public. The coverage read is total over the accepted packet precisely because each available route is one of the finite-net, Vietoris, directed Hausdorff, metric-diameter, net-convergence, or Cauchy-uniform handoff rows already stored in the carrier.
\end{theorem}
\begin{proof}
Start with \autoref{thm:hyperspace-compactmetric-vietoris-unblock}. It lists the compact-source, total-bounded, complete-metric, metric-diameter, net-convergence, and Cauchy-uniform-net rows that must be displayed before a Vietoris-facing consumer can read hit/miss data.

Next use \autoref{thm:hyperspace-vietoris-finite-net-obligations} and \autoref{thm:hyperspace-cauchy-uniform-net-handoff}. The first theorem routes the compact-subset topology read through finite-net and Vietoris-hit rows, while the second routes the completion-facing handoff through the same finite carrier.

Finally apply \autoref{thm:hyperspace-finite-cover-selector-refusal}. Since the finite-cover surface has no selector, host powerset, quotient compactum, or open-cover oracle, every public hit/miss coverage answer is accounted for by the displayed finite rows. That is the asserted totality of coverage reads over the accepted packet.
\end{proof}

\begin{theorem}[Hyperspace root compact-source totality]
\label{thm:hyperspace-root-compact-source-totality}
Every accepted $\HyperspaceUp$ packet must expose the compact-source row, the metric-distance row, the total-bounded finite-net row, and the completeness-support row before any hit/miss read is admitted. Thus a hit/miss consumer reads the displayed $\CompactMetricUp$, $\MetricUp$, $\TotallyBoundedUp$, and $\CompleteMetricUp$ support through the carrier rows of \autoref{def:hyperspace-carrier}, not through a host powerset, selected compact representative, open-cover oracle, or ambient hyperspace completion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperspace-carrier}. The compact source, finite subset-net schedules, directed Hausdorff ledgers, tolerance readback, transport, replay, provenance, and local naming rows are the only rows exposed to a consumer.

Use \autoref{thm:hyperspace-compactmetric-vietoris-unblock} and \autoref{thm:hyperspace-hit-miss-coverage-totality}. They require compact-metric support, total-bounded schedules, complete-metric support, metric-diameter tolerance, net-convergence routes, and Cauchy-uniform handoff before hit/miss coverage is public.

The root read is therefore total only after those source rows are visible. Since the carrier has no coordinate for host powersets, selected representatives, open-cover oracles, or ambient completion, every admitted hit/miss read factors through the displayed compact-source and metric rows.
\end{proof}

\begin{theorem}[Hyperspace finite-net hit-miss determinacy]
\label{thm:hyperspace-root-finite-net-hit-miss}
For an accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, the finite-net data $K_0,K_1,N_0,N_1$ determine the public hit/miss ledger under componentwise $\hsame$ transport and displayed $\Cont$ replay. Any transported or replayed hit/miss read factors through the same finite-net schedules, directed ledgers $D_0,D_1$, readback row $R$, transport row $H_s$, replay row $C$, provenance row $P$, and local name $M$.
\end{theorem}
\begin{proof}
Start with \autoref{thm:hyperspace-vietoris-finite-net-obligations}. It routes compact-subset topology reads through the compact source, finite-net schedules, and Vietoris hit rows before a consumer can inspect compact-subset data.

Apply \autoref{thm:hyperspace-finite-net-carrier-exhaustion}. It identifies $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$ as the complete finite carrier available for compact-subset reads.

Now transport or replay the read. Componentwise $\hsame$ transport preserves the displayed rows, and $\Cont$ replay repeats the same route through $C$. Therefore finite-net data determine the hit/miss ledger for every accepted transported or replayed read, without adding a selected compact set or host powerset element.
\end{proof}

\begin{theorem}[Hyperspace Hausdorff transport root]
\label{thm:hyperspace-root-hausdorff-transport}
Every Hausdorff-distance readback exported by an accepted $\HyperspaceUp$ packet transports along the displayed $\CompactMetricUp$ and $\CompleteMetricUp$ classifier rows. The readback must pass through the compact source, finite-net schedules, directed Hausdorff ledgers, metric-diameter tolerance row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row before it is public.
\end{theorem}
\begin{proof}
Use \autoref{thm:hyperspace-hausdorff-obligation-surface}. The two directed coverage ledgers $D_0,D_1$ are the only Hausdorff-facing ledgers in the accepted carrier.

Next apply \autoref{thm:hyperspace-compact-hausdorff-distance-route} and \autoref{thm:hyperspace-compact-complete-handoff}. The readback tolerance is routed through the metric-diameter and compact-complete support rows, while completion-facing consumers are sent through the displayed $\CompleteMetricUp$ handoff.

Finally use \autoref{thm:hyperspace-root-compact-source-totality}. It fixes the compact-source and metric rows as prerequisites for hit/miss and Hausdorff-facing reads. Transport along $\CompactMetricUp$ and $\CompleteMetricUp$ therefore remains a finite carrier read through $X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$, not a host Hausdorff metric on a powerset.
\end{proof}

\begin{theorem}[Hyperspace Vietoris finite-net obligations]
\label{thm:hyperspace-vietoris-finite-net-obligations}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Before any downstream consumer reads a compact-subset topology route or a Hausdorff-distance route from $H$, the carrier must expose the $\CompactMetricUp$ source $X$, the $\TotallyBoundedUp$ finite-net schedules in $K_0,K_1,N_0,N_1$, and the Vietoris hit rows recorded by the directed ledgers $D_0,D_1$. Thus every compact-subset read is a finite-net and Vietoris-hit read over the displayed source, not a host powerset, selected compact representative, or open-cover compactness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:hyperspace-finite-net-hit}. It forces every directed Hausdorff hit to pass through the compact source row and the finite-net schedules before tolerance readback is public.

Use \autoref{thm:hyperspace-finite-net-carrier-exhaustion}. The only compact-subset rows available to a consumer are $X,K_0,K_1,N_0,N_1$ together with the directed ledgers and structural rows.

Finally apply \autoref{thm:hyperspace-metric-compactness-unblock}. Since the unblocked surface is exactly the finite-net carrier with transport, replay, provenance, and local naming, a Vietoris compact-subset read cannot bypass those obligations.
\end{proof}

\begin{theorem}[Hyperspace Cauchy-uniform complete handoff]
\label{thm:hyperspace-cauchy-uniform-complete-handoff}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. The only completion-facing handoff from $H$ is the route that reads the $\CompleteMetricUp$ support carried by $X$, consumes the $\CauchyUniformNetUp$ replay through $C$, and then names the same finite-net carrier through $P$ and $M$. No ambient hyperspace completion, selected Cauchy limit, quotient completion representative, or host equality of compact subsets is exported.
\end{theorem}
\begin{proof}
Apply \autoref{thm:hyperspace-completemetric-cauchy-lift}. It sends complete-metric and Cauchy-uniform-net consumers through the support carried by $X$ after the finite subset-net schedules and directed Hausdorff ledgers are present.

Next use \autoref{thm:hyperspace-cauchy-uniform-net-handoff}. It fixes the Cauchy-uniform handoff as a read of the compact finite-net carrier followed by componentwise $\hsame$ transport and displayed $\Cont$ replay.

The finite-net carrier of \autoref{def:hyperspace-finite-net-carrier} supplies exactly those rows. Therefore completion enters the hyperspace packet only through $\CompleteMetricUp$ and $\CauchyUniformNetUp$ rows already named by the BEDC carrier.
\end{proof}

\begin{theorem}[Hyperspace Hausdorff classifier exactness]
\label{thm:hyperspace-hausdorff-classifier-exactness}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. The Hausdorff classifier exposed by $H$ is exactly componentwise $\hsame$ transport of the displayed source, compact finite-net rows, directed ledgers, tolerance readback, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\HyperspaceUp}$ naming. It introduces no quotient equality, no extensional host equality of compact subsets, and no replacement distance outside the two directed ledgers $D_0,D_1$.
\end{theorem}
\begin{proof}
First apply \autoref{thm:hyperspace-hausdorff-distance-stability}. Componentwise $\hsame$ transport preserves the two directed Hausdorff ledgers and the tolerance readback.

Then use \autoref{thm:hyperspace-hausdorff-classifier-transport}. The classifier comparison must retain the same compact-source, complete-metric, finite-net, transport, replay, and local naming rows.

Finally invoke \autoref{thm:hyperspace-namecert-obligations}. The $\NameCert_{\HyperspaceUp}$ row names only the displayed packet, so the classifier has no coordinate on which quotient equality or host extensional equality could enter.
\end{proof}

\begin{theorem}[Hyperspace Vietoris hit-miss stability]
\label{thm:hyperspace-vietoris-hit-miss-stability}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. If a downstream Vietoris consumer reads a hit row or miss row from $H$, then the read is stable under the same componentwise $\hsame$ transport that preserves the Hausdorff classifier: the compact-source row $X$, finite-net rows $K_0,K_1,N_0,N_1$, directed ledgers $D_0,D_1$, tolerance readback $R$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\HyperspaceUp}$ row $M$ remain the public coordinates of the read.

The stability route exports no host powerset of compact subsets, no open-cover compactness principle, no selected representative of a compactum, and no quotient equality beyond the displayed transport rows.
\end{theorem}
\begin{proof}
First apply \autoref{thm:hyperspace-hit-miss-classifier}. It identifies every Vietoris-facing hit or miss read with finite carrier rows rather than with an ambient powerset.

Next use \autoref{thm:hyperspace-finite-net-hit} and \autoref{thm:hyperspace-finite-cover-ledger}. These force the hit/miss read through compact-source support, finite-net schedules, directed Hausdorff ledgers, and tolerance readback.

Finally apply \autoref{thm:hyperspace-hausdorff-classifier-exactness}. The same componentwise $\hsame$ transport that preserves the Hausdorff classifier preserves the displayed Vietoris rows, so the transported hit/miss route remains a BEDC finite-net route.
\end{proof}

\begin{theorem}[Hyperspace finite Vietoris obligation basis]
\label{thm:hyperspace-finite-vietoris-obligation-basis}
For an accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, the finite obligation basis for every compact-metric Vietoris read is exactly the basis consisting of compact-source support through $X$, finite-net presentations through $K_0,K_1,N_0,N_1$, hit/miss and finite-cover ledgers through $D_0,D_1$, complete-metric support through $\CompleteMetricUp$, total-bounded schedules through $\TotallyBoundedUp$, net-convergence routes through $\NetConvergenceUp$, Cauchy-uniform-net replay through $\CauchyUniformNetUp$, componentwise transport through $H_s$, displayed replay through $C$, provenance through $P$, and local naming through $M$.

No additional Vietoris obligation may be supplied by an ambient compact-subset topology, a host open-cover theorem, a selected compact representative, or a quotient compactum.
\end{theorem}
\begin{proof}
Read the compact-metric source obligations from \autoref{thm:hyperspace-complete-totallybounded-obligations}. They provide the $\CompleteMetricUp$ and $\TotallyBoundedUp$ support together with the finite compact-source rows.

Use \autoref{thm:hyperspace-compactmetric-vietoris-unblock} and \autoref{thm:hyperspace-vietoris-finite-net-obligations}. Together they force every Vietoris-facing read through finite-net presentations, hit/miss rows, finite-cover ledgers, and directed Hausdorff data before the compact-subset route is public.

The handoff through $\NetConvergenceUp$ and $\CauchyUniformNetUp$ is fixed by \autoref{thm:hyperspace-cauchy-uniform-net-handoff}. Repacking those rows with $H_s,C,P,M$ gives the stated finite obligation basis and leaves no coordinate for ambient topology or selected representatives.
\end{proof}

\begin{theorem}[Hyperspace Hausdorff-net completeness handoff]
\label{thm:hyperspace-hausdorff-net-completeness-handoff}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. A completeness-facing consumer may pass from the Hausdorff classifier to a net-completeness read only by following the finite route
$$
\begin{gathered}
X,K_0,K_1,N_0,N_1,D_0,D_1,R\\
\longrightarrow \CompleteMetricUp,\TotallyBoundedUp,\NetConvergenceUp,\CauchyUniformNetUp\\
\longrightarrow H_s,C,P,M.
\end{gathered}
$$
Thus Hausdorff-distance convergence and Cauchy-uniform-net completeness are handed off through the displayed compact-metric rows, not through an ambient complete hyperspace, a selected compact limit, quotient completion, or host equality of compact subsets.
\end{theorem}
\begin{proof}
Start with \autoref{thm:hyperspace-hausdorff-classifier-exactness}. It fixes the Hausdorff classifier as transport of the displayed compact source, finite-net rows, directed ledgers, tolerance readback, replay, provenance, and local naming.

Next apply \autoref{thm:hyperspace-cauchy-uniform-complete-handoff}. That theorem identifies the completion-facing handoff as a read of $\CompleteMetricUp$ support and $\CauchyUniformNetUp$ replay over the same finite-net carrier.

Finally combine \autoref{thm:hyperspace-metric-compactness-unblock} with the finite-net carrier of \autoref{def:hyperspace-finite-net-carrier}. The complete, totally bounded, net-convergent, and Cauchy-uniform rows are all displayed before the handoff reaches $H_s,C,P,M$, so no ambient complete-hyperspace object or selected compact limit is exported.
\end{proof}

\begin{definition}[Hyperspace finite carrier basis]
\label{def:hyperspace-finite-carrier}
For an accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, the finite carrier basis is the $\BHist$ subpacket consisting exactly of the compact-source row $X$, the finite subset-net rows $K_0,K_1,N_0,N_1$, the two Hausdorff-distance window rows $D_0,D_1,R$, the total-bounded schedule and complete-metric handoff rows readable through $X$, and the structural rows $H_s,C,P,M$ for $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

This basis has no coordinate for a host powerset of compact subsets, quotient compactum, selected compact limit, open-cover compactness proof, or external equality of compact sets.
\end{definition}

\begin{theorem}[Hyperspace Hausdorff distance window]
\label{thm:hyperspace-hausdorff-distance-window}
Let $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$ be an accepted $\HyperspaceUp$ carrier. Every public Hausdorff-distance read from $H$ is exhausted by the finite dyadic distance windows displayed in $D_0,D_1,R$ together with the common refinements of the finite subset-net rows $K_0,K_1,N_0,N_1$. In particular, the read can inspect only source-metric cells from $X$, scheduled finite centers, the two directed coverage ledgers, the tolerance readback row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row.

No public Hausdorff-distance read may appeal to a host powerset, quotient compactum, selected nearest point, selected compact limit, open-cover compactness theorem, or distance relation outside the displayed dyadic windows and their common refinements.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_hausdorff\_distance\_window}
\begin{proof}
Start from \autoref{def:hyperspace-finite-carrier}. The finite carrier basis lists the compact source, subset-net schedules, two directed ledgers, tolerance readback, and structural rows as the only readable coordinates.

Apply \autoref{thm:hyperspace-hausdorff-classifier-exactness}. It identifies the Hausdorff classifier with componentwise transport of those displayed rows, so any distance read must pass through $D_0,D_1,R$ after the source metric and subset nets have been fixed.

Use \autoref{thm:hyperspace-hausdorff-net-completeness-handoff}. Completeness-facing consumers inherit the same finite route through $\CompleteMetricUp$, $\TotallyBoundedUp$, $\NetConvergenceUp$, and $\CauchyUniformNetUp$, and the handoff never exports an ambient complete hyperspace or selected compact limit.
\end{proof}

\begin{theorem}[Hyperspace compact-metric handoff]
\label{thm:hyperspace-compactmetric-handoff}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, a compact-metric downstream consumer receives only the compact finite-net rows of $X,K_0,K_1,N_0,N_1$, the total-bounded schedules readable through $\TotallyBoundedUp$, the complete-metric support readable through $\CompleteMetricUp$, the metric-distance and Hausdorff-window rows $D_0,D_1,R$, and the structural rows $H_s,C,P,M$.

The handoff exports no host powerset of compact subsets, no quotient compactum, no open-cover compactness principle, no selected limit, and no untracked distance object outside the finite carrier basis.
\end{theorem}
\begin{proof}
Read the basis from \autoref{def:hyperspace-finite-carrier}. It exposes compact finite-net rows, Hausdorff-window rows, total-bounded and complete-metric support through the source row, and structural BEDC rows.

Next apply \autoref{thm:hyperspace-finite-vietoris-obligation-basis}. Every compact-metric Vietoris read is forced through compact-source support, finite-net presentations, hit/miss ledgers, finite-cover ledgers, directed Hausdorff data, and the displayed net and Cauchy-uniform handoff.

Finally use \autoref{thm:hyperspace-hausdorff-distance-window}. Since the public Hausdorff read is exhausted by finite dyadic windows and common refinements, the compact-metric consumer has no remaining coordinate from which to read host powersets, quotient compacta, selected limits, or open-cover compactness.
\end{proof}

\begin{theorem}[Hyperspace finite Vietoris subcover avoidance]
\label{thm:hyperspace-vietoris-finite-subcover-avoidance}
For every accepted $\HyperspaceUp$ carrier $H=(X,K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M)$, every Vietoris hit-miss read used by a compact-metric consumer is a finite $\BHist$ route over the displayed rows $K_0,K_1,N_0,N_1,D_0,D_1,R,H_s,C,P,M$. The route may inspect finite hit rows, finite miss rows, compact-source cells from $X$, finite-cover ledgers internal to the subset nets, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row.

It has no coordinate for an arbitrary open cover of the hyperspace, a selected finite subcover, a host powerset compactness theorem, a quotient compactum, or a selected compact limit.
\end{theorem}
\begin{proof}
Read the finite carrier basis from \autoref{def:hyperspace-finite-carrier}. The basis exposes the compact source, finite subset-net rows, Hausdorff-window rows, and structural rows before any Vietoris consumer can read hit or miss information.

Apply \autoref{thm:hyperspace-finite-vietoris-obligation-basis}. Its obligation surface already lists hit rows, miss rows, finite-cover ledgers, directed Hausdorff data, net-convergence handoff, and Cauchy-uniform-net handoff as finite rows over the same packet.

Finally use \autoref{thm:hyperspace-compactmetric-handoff}. A compact-metric consumer receives only those displayed rows and their structural replay. Since no open-cover object or selected subcover occurs in the carrier, the Vietoris read remains a finite hit-miss route.
\end{proof}

\begin{theorem}[Hyperspace Cauchy-net compact handoff]
\label{thm:hyperspace-cauchy-net-compact-handoff}
Every Cauchy-net handoff from an accepted $\HyperspaceUp$ packet to a compact-metric or completion-facing consumer factors through finite subset-net rows, the $\NetConvergenceUp$ schedule readable from those rows, the $\CauchyUniformNetUp$ replay row, the $\CompleteMetricUp$ support carried by the compact source, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row.

The handoff may assert only that the displayed finite net route is available to the consumer. It does not select a limiting compact subset, prove open-cover compactness of the hyperspace, identify quotient compacta, or export an ambient complete-hyperspace object.
\end{theorem}
\begin{proof}
Start with the accepted carrier and its finite basis. The rows $K_0,K_1,N_0,N_1$ display the finite subset nets, while $D_0,D_1,R$ display the Hausdorff-window comparisons used to compare net stages.

Apply \autoref{thm:hyperspace-hausdorff-net-completeness-handoff}. The completion-facing read is already forced through $\CompleteMetricUp$, $\TotallyBoundedUp$, $\NetConvergenceUp$, and $\CauchyUniformNetUp$ over the displayed finite-net carrier.

Use \autoref{thm:hyperspace-compactmetric-handoff} to restrict the compact-facing consumer to the same rows. The structural rows $H_s,C,P,M$ then transport, replay, source, and name the handoff, with no route to a selected compact limit or host complete hyperspace.
\end{proof}

\begin{theorem}[Hyperspace hit-miss directed refinement]
\label{thm:hyperspace-hit-miss-directed-refinement}
Let $H$ and $H'$ be accepted $\HyperspaceUp$ carriers whose compact-source rows and finite subset-net rows are related by componentwise $\hsame$ transport. If the hit rows of $H'$ refine the hit rows of $H$ and the miss rows of $H'$ refine the miss rows of $H$ by displayed finite witnesses, then every directed Hausdorff or Vietoris read of $H'$ factors through the original finite hit-miss route of $H$ together with the refinement witnesses.

The refinement may replace a displayed finite row by a finer finite row, but it cannot add open-cover compactness, selected compact limits, host powersets, quotient compacta, or untracked distance objects.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.Hyperspace\_hit\_miss\_directed\_refinement}
\begin{proof}
Project both carriers to their finite bases. The $\hsame$ transport identifies the compact source, subset-net schedules, Hausdorff windows, and structural rows before the refinement witnesses are read.

The hit refinement sends every refined hit row to a displayed hit row in the original packet. The miss refinement does the same for finite avoidance rows, so a Vietoris read of $H'$ returns to the hit-miss ledgers of $H$ after inspecting only finite witnesses.

For directed Hausdorff reads, apply \autoref{thm:hyperspace-hausdorff-distance-window}. The distance windows are exhausted by $D_0,D_1,R$ and common finite refinements, and the structural rows replay the same route. Thus no excluded open-cover, host powerset, quotient, selected-limit, or untracked-distance coordinate is exported.
\end{proof}

\begin{theorem}[Hyperspace compact-source carrier obligations]
\label{thm:hyperspace-compact-source-carrier-obligations}
Let $H$ be an accepted $\HyperspaceUp$ carrier. Before any compact-source consumer can read a hyperspace row, the packet must expose the common $\CompactMetricUp$ source, the finite subset-net rows, the $\MetricDiameterUp$ and $\TotallyBoundedUp$ resolution rows, the $\CompleteMetricUp$ support carried by the source, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row.

The exposed surface is a compact-source carrier for finite hyperspace observations only. It does not export a host powerset, quotient compactum, selected compact subset, selected limit, open-cover compactness theorem, or ambient complete-hyperspace object.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its compact source is already the $\CompactMetricUp$ row, and the finite subset-net rows are the only hyperspace objects that downstream consumers can inspect.

Apply \autoref{thm:hyperspace-compactmetric-handoff}. The compact-facing handoff is forced through the displayed compact metric source, finite subset nets, and structural rows before any consumer receives hyperspace data.

Now use \autoref{thm:hyperspace-cauchy-net-compact-handoff}. The diameter, total-boundedness, complete-metric, Cauchy-uniform, replay, provenance, and naming rows are the same finite route. Since none of the forbidden host objects is a carrier coordinate, the compact-source obligation surface is exhausted by the displayed packet.
\end{proof}

\begin{theorem}[Hyperspace Vietoris root-unblock obligations]
\label{thm:hyperspace-vietoris-root-unblock-obligations}
Every Vietoris hit-miss read from an accepted $\HyperspaceUp$ packet factors through finite hit rows, finite miss rows, finite subset-net schedules, Hausdorff-window comparisons, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row. Refinement of a Vietoris read is allowed only by replacing displayed finite hit or miss witnesses with finer displayed finite witnesses.

Thus a Vietoris root consumer receives a finite hit-miss classifier and not a host powerset topology, quotient compactum, selected compact limit, open-cover compactness proof, or untracked distance object.
\end{theorem}
\begin{proof}
Start from the hit and miss rows of the accepted carrier. These rows are finite ledgers, so a Vietoris request can only inspect listed hit witnesses and listed avoidance witnesses.

Apply \autoref{thm:hyperspace-vietoris-hit-miss-stability} and \autoref{thm:hyperspace-hit-miss-directed-refinement}. Stability keeps the classifier on the displayed hit-miss route, while directed refinement replaces rows only by explicitly displayed finite refinements.

Finally apply \autoref{thm:hyperspace-hausdorff-distance-window}. The Hausdorff side is already bounded by the finite distance windows and common refinements, so the Vietoris classifier cannot escape to host powersets, quotient compacta, selected limits, open-cover compactness, or untracked distance data.
\end{proof}

\begin{theorem}[Hyperspace completion-boundary obligations]
\label{thm:hyperspace-completion-boundary-obligations}
Any completion-boundary read exported by an accepted $\HyperspaceUp$ packet must pass through the finite subset-net carrier, the $\NetConvergenceUp$ schedule, the $\CauchyUniformNetUp$ replay row, the $\CompleteMetricUp$ support inherited from the compact source, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row before a boundary-facing consumer receives data.

The boundary handoff records only this finite Cauchy-uniform completion route. It does not select a limiting compact subset, identify quotient compacta, assert open-cover compactness, import a host powerset, or expose an ambient complete-hyperspace object.
\end{theorem}
\begin{proof}
Read the finite subset-net rows first. They provide the only net stages visible to the packet, and their schedules are the source for the $\NetConvergenceUp$ and $\CauchyUniformNetUp$ rows.

Use \autoref{thm:hyperspace-hausdorff-net-completeness-handoff}. The completion-facing read is forced through the displayed complete-metric support, net-convergence schedule, Cauchy-uniform replay, and Hausdorff-window carrier.

Then apply \autoref{thm:hyperspace-cauchy-net-compact-handoff}. The compact-source and completion-boundary routes are the same finite ledger route, so no selected limit, quotient compactum, host powerset, open-cover compactness proof, or ambient complete-hyperspace object is exported.
\end{proof}

\begin{theorem}[Hyperspace root Vietoris net basis]
\label{thm:hyperspace-root-vietoris-net-basis}
Every root Vietoris read from an accepted $\HyperspaceUp$ carrier must expose the $\CompactMetricUp$ source row, the $\TotallyBoundedUp$ finite-net schedules carried by $K_0,K_1,N_0,N_1$, the finite hit and miss ledgers, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row. The resulting basis is a finite hit/miss window over displayed nets, not a host powerset topology, open-cover compactness proof, selected subcover, quotient compactum, or selected compact limit.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceRootVietorisNetBasis}
\begin{proof}
Project the accepted carrier and its finite basis. The only compact-subset rows available to a Vietoris consumer are the compact source and the displayed finite subset-net schedules.

Apply \autoref{thm:hyperspace-vietoris-root-unblock-obligations}. It routes every Vietoris hit-miss read through finite hit rows, finite miss rows, finite subset-net schedules, Hausdorff-window comparisons, and the structural rows.

Use \autoref{thm:hyperspace-hit-miss-directed-refinement} to account for refinement. Refinement can replace a displayed finite witness by a finer displayed finite witness, but it adds no open-cover compactness, selected subcover, host powerset, quotient compactum, or selected limit.
\end{proof}

\begin{theorem}[Hyperspace root Hausdorff finite window]
\label{thm:hyperspace-root-hausdorff-finite-window}
Every root Hausdorff comparison exported by an accepted $\HyperspaceUp$ carrier must pass through the $\MetricSpaceUp$ distance witnesses inside the compact source, the terminal $\RealUp$ tolerance readback row $R$, and the finite net rows $K_0,K_1,N_0,N_1,D_0,D_1$ before any downstream distance consumer receives data. The comparison is a finite directed-window route; it is not a host Hausdorff metric on a powerset, quotient hyperspace equality, selected nearest-point operation, open-cover compactness, or ambient complete-hyperspace claim.
\end{theorem}
\leanchecked{BEDC.Derived.HyperspaceUp.HyperspaceRootHausdorffFiniteWindow}
\begin{proof}
Start from \autoref{thm:hyperspace-hausdorff-distance-window}. It exhausts public Hausdorff reads by the displayed dyadic distance windows $D_0,D_1,R$ and common refinements of the finite subset-net rows.

Apply \autoref{thm:hyperspace-root-hausdorff-transport}. The comparison transports along the displayed $\CompactMetricUp$ and $\CompleteMetricUp$ classifier rows, while the metric and Real tolerance readback stay inside the carrier.

Finally use \autoref{thm:hyperspace-compactmetric-handoff}. A downstream compact-metric consumer receives only compact finite-net rows, total-bounded schedules, complete-metric support, metric-distance windows, and structural rows, so no quotient equality, selected point, host powerset metric, open-cover proof, or ambient completion is exported.
\end{proof}

\begin{theorem}[Hyperspace root complete and totally bounded handoff]
\label{thm:hyperspace-root-complete-totallybounded-handoff}
Any compact-hyperspace or completion-facing consumer of an accepted $\HyperspaceUp$ packet must read the $\CompleteMetricUp$ support and $\TotallyBoundedUp$ finite-net rows carried by the compact source before making a compact-hyperspace claim. The handoff may then consume the finite subset-net rows, directed Hausdorff ledgers, $\NetConvergenceUp$ schedule, $\CauchyUniformNetUp$ replay, componentwise transport, displayed replay, provenance, and local naming; it does not assert open-cover compactness, selected compact limits, quotient hyperspace equality, or an ambient complete hyperspace.
\end{theorem}
\begin{proof}
Read the compact-source carrier obligations in \autoref{thm:hyperspace-compact-source-carrier-obligations}. They force the common $\CompactMetricUp$ source, finite subset-net rows, total-bounded resolution, complete-metric support, and structural rows to be visible before a compact-source consumer receives hyperspace data.

Next apply \autoref{thm:hyperspace-completion-boundary-obligations}. A completion-boundary read must pass through the finite subset-net carrier, $\NetConvergenceUp$ schedule, $\CauchyUniformNetUp$ replay, $\CompleteMetricUp$ support, and the structural rows.

Finally use \autoref{thm:hyperspace-cauchy-net-compact-handoff}. The compact-source and completion-boundary routes are the same finite ledger route, so the consumer receives only the displayed complete and total-bounded rows before any compact-hyperspace claim.
\end{proof}

\begin{theorem}[Hyperspace Vietoris refinement factorization]
\label{thm:hyperspace-vietoris-refinement-factorization}
Every accepted $\HyperspaceUp$ Vietoris refinement factors through the displayed compact source row, finite subset-net schedules, hit and miss rows, directed Hausdorff ledgers, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row. The factorization refines only finite displayed witnesses; it is not a host powerset refinement, selected subcover, quotient compactum, or open-cover compactness argument.
\end{theorem}
\begin{proof}
Start from the finite Vietoris obligation basis of \autoref{thm:hyperspace-vietoris-root-unblock-obligations}. It routes every hit-miss read through the listed finite hit rows, finite miss rows, subset-net schedules, Hausdorff-window comparisons, and structural rows.

Then apply the root Vietoris net basis of \autoref{thm:hyperspace-root-vietoris-net-basis}. The compact source and $\TotallyBoundedUp$ schedules remain visible before a root consumer receives a Vietoris basis element.

Finally use directed refinement from \autoref{thm:hyperspace-hit-miss-directed-refinement}. Refinement replaces a displayed finite witness by a finer displayed finite witness and therefore cannot import host powersets, selected subcovers, quotient compacta, or open-cover compactness.
\end{proof}

\begin{theorem}[Hyperspace Vietoris union stability]
\label{thm:hyperspace-vietoris-union-stability}
The finite union of two accepted finite Vietoris $\HyperspaceUp$ packets is routed through the union of their displayed hit rows and the intersection of their displayed miss rows. The resulting packet still reads only the compact source row, finite subset-net schedules, directed Hausdorff ledgers, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperspaceUp}$ row; it does not import a host powerset union, open-cover compactness, selected subcover, quotient compactum, or selected compact limit.
\end{theorem}
\begin{proof}
Project the two accepted finite Vietoris packets. Their hit rows and miss rows are already exhausted by \autoref{thm:hyperspace-vietoris-root-unblock-obligations} and by the finite-net carrier data of \autoref{def:hyperspace-carrier}.

Use \autoref{thm:hyperspace-vietoris-refinement-factorization}. Directed refinement replaces displayed finite witnesses by displayed finer witnesses, so the union packet can combine hit evidence by finite union and miss evidence by common intersection without leaving the recorded net schedules.

Finally apply \autoref{thm:hyperspace-hit-miss-directed-refinement}. The common refinement remains a finite displayed witness over the same Hausdorff ledgers and structural rows, and the excluded host, quotient, selected-limit, and open-cover data have no carrier coordinate.
\end{proof}

\begin{theorem}[Hyperspace Hausdorff union boundary]
\label{thm:hyperspace-hausdorff-union-boundary}
Every Hausdorff read of the finite union of two accepted $\HyperspaceUp$ packets is bounded only by the displayed finite subset-net rows and directed coverage ledgers of the two packets. A downstream consumer may inspect the union through the common compact source, the net rows $K_0,K_1,N_0,N_1,D_0,D_1$, the terminal $\RealUp$ tolerance readback, and the structural rows, but it receives no host powerset metric, quotient hyperspace equality, selected nearest-point operation, open-cover compactness, or ambient complete-hyperspace object.
\end{theorem}
\begin{proof}
Start from the Hausdorff classifier transport of \autoref{thm:hyperspace-root-hausdorff-finite-window}. It routes every comparison through the displayed compact source, finite net rows, directed Hausdorff ledgers, and Real tolerance readback.

Apply the finite-net exhaustion used by \autoref{thm:hyperspace-root-complete-totallybounded-handoff}. The union read can only combine the two displayed net families and their coverage ledgers; it cannot select a new host-level compact subset.

Then use the Vietoris union stability theorem above. Since the union packet is already expressed through displayed hit, miss, net, and refinement rows, the Hausdorff read is bounded by those same rows and exports none of the excluded host or quotient data.
\end{proof}

\closureat{HyperspaceUp}{seedStr}

\begin{closurestatus}{\HyperspaceUp}
  \constructivestory{A $\HyperspaceUp$ packet is generated from a CompactMetricUp source, two finite compact-subset net presentations, two net-resolution ledgers, two directed Hausdorff-distance coverage ledgers, a dyadic and Real tolerance readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperspace-carrier}, \autoref{thm:hyperspace-hausdorff-distance-stability}, \autoref{thm:hyperspace-namecert-obligations}, and \autoref{thm:hyperspace-compact-hausdorff-distance-route} seed the finite compact-subset Hausdorff-distance surface through CompactMetric, TotallyBounded, CompleteMetric, MetricDiameter, NetConvergence, and CauchyUniformNet rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host powersets, quotient compacta, selected limits, open-cover compactness, choice-selected representatives, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact-source admission, finite subset-net admission, bidirectional distance coverage, classifier transport stability, and provenance/name exhaustion for BEDC.Derived.HyperspaceUp.}
\end{closurestatus}
