\chapter{A Concrete Naming Certificate for $\RegularCauchySubtractionUp$}
\label{ch:concrete-instances-regular-cauchy-subtraction-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchySubtractionUp$ packet records binary subtraction of displayed regular rational Cauchy real names as a finite sign-flip-plus-sum route. It sits over the regular rational source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the synchronized window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the addition route of \autoref{ch:concrete-instances-regular-cauchy-addition-namecert}, the sign-flip route of \autoref{ch:concrete-instances-regularcauchynegation-namecert}, the algebra packet of \autoref{ch:concrete-instances-regularcauchyring-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is the binary subtraction surface consumed by later L10 real and completion algebra routes; it is not host real subtraction, a quotient Cauchy-class operation, a selected limit, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[RegularCauchySubtraction carrier]
\label{def:regular-cauchy-subtraction-carrier}
A $\RegularCauchySubtractionUp$ carrier is a finite $\BHist$ packet
$$
B_-=(X,Y,G,A,W,D,E,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two $\RegSeqRatUp$ source rows for regular rational Cauchy real names.
\item $G$ is the sign-flip route for $Y$ through the $\RegularCauchyNegationUp$ carrier, exposing only the displayed source row, finite windows, dyadic sign-flip ledger, transported classifier, and $\RealUp$ seal handoff of that packet.
\item $A$ is the sum route for $X+(-Y)$ through the $\RegularCauchyAdditionUp$ carrier, using $X$ as the first summand and the regular readback exported by $G$ as the second summand.
\item $W$ is the synchronized $\StreamNameUp$ window ledger aligning the source reads of $X$, the source reads of $Y$, the sign-flipped reads in $G$, and the paired summand reads consumed by $A$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger containing the source tolerance rows, the sign-flipped dyadic endpoint rows, and the dyadic addition rows used by the final subtraction readback.
\item $E$ is the finite error-budget accounting row combining the tolerance consumed by the sign flip, the paired summand tolerance, and the output tolerance required by the subtraction readback.
\item $S$ is the resulting $\RegSeqRatUp$ readback row for the displayed binary subtraction route.
\item $H$ records componentwise $\hsame$ transport for the two source rows, the sign-flip route, the sum route, synchronized windows, dyadic ledger, finite budget row, readback, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from any subtraction-facing precision request through $X,Y,G,A,W,D,E,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchySubtractionUp$, $\RegSeqRatUp$, $\RegularCauchyNegationUp$, $\RegularCauchyAdditionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchySubtractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,G,A,W,D,E,S,H,C,P,N$. The carrier has no coordinate for host real subtraction, quotient Cauchy classes, selected limits, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchySubtractionUp.RegularCauchySubtractionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy subtraction NameCert obligations]
\label{thm:regular-cauchy-subtraction-namecert-obligations}
For every accepted $\RegularCauchySubtractionUp$ carrier
$$
B_-=(X,Y,G,A,W,D,E,S,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchySubtractionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ source rows, the $\RegularCauchyNegationUp$ sign-flip route, the $\RegularCauchyAdditionUp$ sum route, synchronized windows, dyadic tolerance rows, finite error-budget row, resulting readback, and structural rows;
\item negation handoff, requiring $G$ to expose the sign-flipped row of $Y$ only through the finite sign-flip ledger of \autoref{ch:concrete-instances-regularcauchynegation-namecert};
\item addition handoff, requiring $A$ to consume $X$ and the readback exported by $G$ only through the finite addition route of \autoref{ch:concrete-instances-regular-cauchy-addition-namecert};
\item finite error-budget accounting, requiring $E$ to combine the sign-flip tolerance, paired summand tolerance, dyadic addition tolerance, and output tolerance before $S$ is available;
\item Real seal nonescape, requiring every $\RealUp$ consumer to read the route through $X,Y,G,A,W,D,E,S$ before any terminal seal is exposed;
\item componentwise transport, replay, provenance, and local naming, requiring $H,C,P,N$ to preserve and name exactly the same finite subtraction packet;
\item refusal of host real subtraction, quotient Cauchy classes, selected limits, trichotomy, ambient $\RealUp$ completeness, Lean verification, and bridge checking as local NameCert data.
\end{enumerate}
No subtraction obligation is stored outside these finite rows.
\end{theorem}

\begin{theorem}[Regular Cauchy subtraction Real seal handoff]
\label{thm:regular-cauchy-subtraction-real-seal-handoff}
Every Real-facing subtraction read exported by an accepted $\RegularCauchySubtractionUp$ carrier factors through the finite route
$$
(X,Y)\longmapsto G\longmapsto A\longmapsto W\longmapsto D\longmapsto E\longmapsto S\longmapsto \RealUp
$$
before it may be consumed as a completed real-name boundary. Thus the consumer receives two source rows, the sign-flip row for $Y$, the sum row for $X+(-Y)$, synchronized $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, $\RegSeqRatUp$ readback, and the $\RealUp$ seal. It receives no host real subtraction, quotient Cauchy class, selected limit, trichotomy, ambient completeness coordinate, Lean verification, or bridge checking.
\end{theorem}

\closureat{RegularCauchySubtractionUp}{seedStr}

\begin{closurestatus}{\RegularCauchySubtractionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-subtraction-carrier}, \autoref{thm:regular-cauchy-subtraction-namecert-obligations}, and \autoref{thm:regular-cauchy-subtraction-real-seal-handoff} seed the finite binary subtraction route through $\RegSeqRatUp$ source rows, $\RegularCauchyNegationUp$ sign flip, $\RegularCauchyAdditionUp$ sum, synchronized $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, resulting $\RegSeqRatUp$ readback, and $\RealUp$ seal data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real subtraction, quotient Cauchy classes, selected limits, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, negation handoff, addition handoff, finite error-budget accounting, Real seal nonescape, componentwise transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchySubtractionUp.}
  \constructivestory{A $\RegularCauchySubtractionUp$ packet is generated from two $\RegSeqRatUp$ source rows, a $\RegularCauchyNegationUp$ sign-flip route for the second source, a $\RegularCauchyAdditionUp$ sum route for the first source and the sign-flipped readback, synchronized $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, a finite error-budget row, a resulting $\RegSeqRatUp$ readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
