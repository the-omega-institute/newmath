\chapter{A Concrete Naming Certificate for $\HarnackInequalityUp$}
\label{ch:concrete-instances-harnack-inequality-namecert}
\origin{human}

Following Moser's iteration method and the elliptic regularity presentation of Gilbarg--Trudinger, the $\HarnackInequalityUp$ packet records the finite route by which a positive PDE solution read consumes Sobolev control, Poincare control, cutoff schedules, exponent-step ledgers, and terminal real bounds before a Harnack comparison is named. It sits after the Moser ladder of \autoref{ch:concrete-instances-moser-iteration-namecert}, the PDE relation surface of \autoref{ch:concrete-instances-pde-namecert}, the Sobolev source of \autoref{ch:concrete-instances-sobolev-namecert}, the Poincare control row of \autoref{ch:concrete-instances-poincare-inequality-namecert}, and the scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the displayed local comparison certificate; it is not a host regularity theorem, not a selected weak solution, not an open-cover compactness argument, and not an ambient completeness principle for $\RealUp$.

\begin{definition}[Harnack inequality carrier]
\label{def:harnack-inequality-carrier}
A $\HarnackInequalityUp$ carrier is a finite $\BHist$ packet
$$
H=(M,D,S,P,J,E,R,B,T,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MoserIterationUp$ handoff row, exposing the finite ladder consumed before any Harnack read.
\item $D$ is the $\PdeUp$ relation row, naming the displayed equation and boundary classifier over the same local domain.
\item $S$ is the $\SobolevUp$ source row whose weak-derivative and energy data feed the ladder.
\item $P$ is the $\PoincareInequalityUp$ control row for local oscillation and average comparison.
\item $J$ is the cutoff and nested-region row inherited from the Moser ladder.
\item $E$ is the exponent-step row that records the finite climb from local energy bounds to the comparison window.
\item $R$ is the $\RealUp$ bound readback row for constants, radii, and positive lower-bound data.
\item $B$ is the positive-solution boundary row, requiring the displayed solution window to remain inside the admitted PDE and Real-bound rows.
\item $T$ records componentwise $\hsame$ transport for the Moser, PDE, Sobolev, Poincare, cutoff, exponent, Real-bound, positive-boundary, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same route for downstream PDE regularity consumers.
\item $Q$ is the $\Pkg$ provenance over $\HarnackInequalityUp$, $\MoserIterationUp$, $\PdeUp$, $\SobolevUp$, $\PoincareInequalityUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HarnackInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,D,S,P,J,E,R,B,T,C,Q,N$. It has no coordinate for an arbitrary host solution, global elliptic regularity, quotient equality of functions, selected representatives, countable choice, open-cover compactness, or ambient real completeness.
\end{definition}
\leandef{BEDC.Derived.HarnackInequalityUp}

\begin{theorem}[Harnack inequality NameCert obligations]
\label{thm:harnack-inequality-namecert-obligations}
For every accepted $\HarnackInequalityUp$ carrier $H=(M,D,S,P,J,E,R,B,T,C,Q,N)$, the local $\NameCert_{\HarnackInequalityUp}$ surface consists exactly of Moser-ladder exposure through $M$; PDE relation exposure through $D$; Sobolev source exposure through $S$; Poincare control exposure through $P$; cutoff schedule exposure through $J$; exponent-step exposure through $E$; Real-bound readback through $R$; positive-solution boundary exposure through $B$; componentwise transport through $T$; displayed replay through $C$; provenance through $Q$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:harnack-inequality-carrier}. The rows $M,D,S,P,J,E,R,B,T,C,Q,N$ are exactly the Moser, PDE, Sobolev, Poincare, cutoff, exponent, Real-bound, positive-boundary, transport, replay, provenance, and naming coordinates.

The analytic rows are read in dependency order. The PDE row fixes the displayed relation, the Sobolev and Poincare rows supply the source estimates, and the Moser row brings the cutoff and exponent ladder into the same finite packet.

The endpoint rows then seal the comparison. The Real-bound row records constants and positive lower-bound data, while the positive-solution boundary row prevents the comparison from being read outside the admitted PDE window. Transport, replay, provenance, and local naming preserve only this finite route, so the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Harnack inequality Moser handoff]
\label{thm:harnack-inequality-moser-handoff}
Every accepted $\HarnackInequalityUp$ read must first consume the $\MoserIterationUp$ handoff row together with the displayed $\SobolevUp$, $\PoincareInequalityUp$, cutoff, exponent, and $\RealUp$ rows. The Harnack comparison is therefore a downstream read of the finite Moser ladder, not a separate host regularity theorem or selected-solution principle.
\end{theorem}
\begin{proof}
Start with the Moser handoff of \autoref{thm:moser-iteration-harnack-handoff}. It requires every Harnack-facing consumer to expose the Sobolev source, Poincare control, cutoff schedule, exponent ledger, Real-bound readback, and structural rows before a regularity bound is consumed.

The carrier of \autoref{def:harnack-inequality-carrier} imports exactly that handoff as $M$ and keeps the source rows visible as $S,P,J,E,R$. Thus a Harnack read cannot bypass the Moser ladder.

Finally attach the PDE and positive-boundary rows. They restrict the comparison to the displayed relation and positive solution window, while $T,C,Q,N$ only transport, replay, source, and name the same packet. Hence the handoff is finite and does not export a detached host theorem.
\end{proof}

\begin{theorem}[Harnack inequality positive-solution boundary]
\label{thm:harnack-inequality-positive-solution-boundary}
Every positive-solution comparison exported by an accepted $\HarnackInequalityUp$ packet factors through the PDE relation row $D$, the positive-boundary row $B$, and the Real-bound readback row $R$ after the Moser ladder rows $M,S,P,J,E$ have been displayed. It exports no comparison for an unnamed solution, no quotient equality of representatives, and no ambient maximum principle outside
$$
M,D,S,P,J,E,R,B,T,C,Q,N.
$$
\end{theorem}
\begin{proof}
Read the PDE-facing rows first. The row $D$ fixes the displayed PDE relation and boundary classifier, while $B$ states that the positive solution window being compared is part of that same admitted route.

Next consume the ladder and scalar seal. The rows $M,S,P,J,E$ supply the finite Moser ladder, and $R$ supplies the scalar constants and positive lower-bound data used by the comparison.

The structural rows finish the route. Transport, replay, provenance, and local naming through $T,C,Q,N$ preserve the same finite packet and introduce no selected solution, quotient representative, host maximum principle, or ambient completeness theorem. Therefore every positive-solution comparison remains inside the displayed carrier rows.
\end{proof}

\falsifiablePrediction{Every accepted $\HarnackInequalityUp$ read must expose the Moser ladder, PDE relation, Sobolev source, Poincare control, cutoff schedule, exponent ledger, Real-bound readback, positive-solution boundary, transport, replay, provenance, and local $\NameCert$ rows before any Harnack comparison is consumed.}

\independenceWitness{moser-iteration, pde, sobolev, poincare-inequality, real: no listed sibling is bijective with $\HarnackInequalityUp$ because this packet alone binds the finite Moser ladder to a displayed positive PDE solution comparison and terminal Real-bound seal.}

\closureat{HarnackInequalityUp}{seedStr}

\begin{closurestatus}{\HarnackInequalityUp}
  \constructivestory{A $\HarnackInequalityUp$ packet is generated from a MoserIteration handoff row, a PDE relation row, Sobolev source data, Poincare control data, cutoff schedules, exponent-step ledgers, Real-bound readback, a positive-solution boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:harnack-inequality-carrier}, \autoref{thm:harnack-inequality-namecert-obligations}, \autoref{thm:harnack-inequality-moser-handoff}, and \autoref{thm:harnack-inequality-positive-solution-boundary} seed the finite Harnack comparison packet consumed downstream from $\MoserIterationUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a host Harnack theorem, selected PDE solution, global regularity theorem, quotient equality of functions, countable choice, open-cover compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Moser handoff exactness, PDE relation exactness, Sobolev and Poincare source exactness, cutoff and exponent ledger exactness, Real-bound readback, positive-boundary preservation, transport, replay, provenance, and local naming for BEDC.Derived.HarnackInequalityUp.}
\end{closurestatus}
