\chapter{A Concrete Naming Certificate for $\TreeUp$}
\label{ch:concrete-instances-tree-namecert}

The tree interface $\TreeUp$ packages a connected acyclic $\NameCert_{\GraphUp}$ (\autoref{ch:concrete-instances-graph-namecert}) together with a designated root or root-free witness. It is the constructive substrate for inductive carriers and for the syntactic representation of derivations.

\section{Carrier definition}
\label{sec:tree-carrier}

\begin{theorem}[TreeUp graph-source carrier obligation]
\label{thm:tree-graph-source-carrier-obligation}
The carrier for $\TreeUp$ is a $\mathsf{BHist}$ predicate read through the
existing $\GraphUp$ source. An accepted tree history exposes a carried
$\GraphUp$ source row, a finite vertex-and-edge incidence ledger over
$\mathsf{BHist}$ endpoints, and a classifier comparison inherited from
$\hsame$ and the $\GraphUp$ classifier rows. The carrier does not identify
host graphs by equality; every comparison is recorded as a displayed
classifier row over the accepted histories and their incidence ledger.
\end{theorem}
\begin{proof}
Unpack an accepted tree history as a graph-source presentation. The first
projection is the $\GraphUp$ carrier row, so each visible vertex and edge is
read as a carried $\mathsf{BHist}$ endpoint rather than as a host-level graph
object.

The second projection is the finite incidence ledger. It lists the displayed
vertex endpoints, displayed edge endpoints, and the source-target incidence
rows that attach each edge to carried vertices. Finiteness belongs to this
ledger, so no quotient over ambient graphs is introduced.

The classifier projection is inherited rowwise. Endpoint movement is
componentwise $\hsame$, and graph-source movement is the comparison supplied
by $\GraphUp$. Transporting the finite incidence ledger along those rows gives
the same accepted tree carrier predicate at the target history, with no
appeal to equality of host graphs.
\end{proof}

\begin{definition}[TreeUp BHist carrier]\label{def:treeup-bhist-carrier}
The carrier of $\TreeUp$ is the $\mathsf{BHist}$ endpoint row whose visible
package consists of a $\NameCert_{\GraphUp}$ edge/vertex package, the
$\mathsf{GraphContEdge}$ rows witnessing the graph edges, a connectedness row,
an acyclicity row, and one branch of a root classifier. The rooted branch
records a root witness carried by the same $\Pkg$ provenance as the displayed
graph package; the root-free branch records a witness that the same visible
package has no designated root.

Two such rows are carrier-comparable only through the visible graph
$\hsame$-transport, the connectedness and acyclicity rows, and the chosen
rooted or root-free witness row. Hidden graph routes, hidden choices of root,
and hidden proofs of connectedness or acyclicity are ledger data, not carrier
equality.
\end{definition}

\begin{theorem}[TreeUp carrier obligation]\label{thm:treeup-carrier-obligation}
The $\TreeUp$ carrier is obligated to expose exactly the $\mathsf{BHist}$
endpoint of \autoref{def:treeup-bhist-carrier}, the
$\NameCert_{\GraphUp}$ source package, the $\mathsf{GraphContEdge}$ edge rows,
the connectedness row, the acyclicity row, and the rooted or root-free witness
row. Its ten-field fit is: traditional target tree carrier; BEDC source
$\mathsf{BHist}$, $\Cont$, $\hsame$, $\Pkg$, and $\NameCert$; carrier the
displayed endpoint row; classifier inherited from graph-edge transport plus
the three tree rows; stability by transport of the visible rows; ledger the
hidden route, root-choice, connectedness, and acyclicity witnesses; exactness
by visible row readback; standard bridge absent; status obligation target.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_obligation\_rows}
\begin{proof}
Use the carrier field of the roadmap ten-field template. The source row is the
displayed $\NameCert_{\GraphUp}$ package, so every visible edge is already a
$\mathsf{GraphContEdge}$ row over $\mathsf{BHist}$ and $\Cont$.

Attach the connectedness, acyclicity, and root branch rows to that same
$\Pkg$ provenance. The resulting endpoint is a tree row before any syntactic
derivation chapter consumes it, because all carrier fields are visible BEDC
rows and all non-visible choices are assigned to the ledger field.
\end{proof}

\begin{theorem}[TreeUp classifier obligation]\label{thm:treeup-classifier-obligation}
The $\TreeUp$ classifier relates two carried endpoints precisely when their
$\NameCert_{\GraphUp}$ graph packages are transported by graph-edge $\hsame$,
their $\mathsf{GraphContEdge}$ rows match under that transport, and their
connectedness, acyclicity, and root-branch rows match over the transported
$\Pkg$ provenance. It does not identify trees by host graph isomorphism or by
unrecorded route choice.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_classifier\_transport}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_classifier\_transport\_surface}
\begin{proof}
Start with the classifier field of the roadmap ten-field template and the
graph source certificate. Transport the visible edge and vertex package along
the graph $\hsame$ relation, carrying each $\mathsf{GraphContEdge}$ row by its
$\Cont$ transport surface.

It remains to compare the tree-only rows. The connectedness row, the
acyclicity row, and the rooted or root-free branch row are checked after the
$\Pkg$ provenance has been transported, so the classifier is still a BEDC
classifier on named histories rather than an external graph quotient.
\end{proof}

\begin{theorem}[TreeUp root witness obligation]\label{thm:treeup-root-witness-obligation}
For each $\TreeUp$ carrier row, the root component must split constructively
into exactly one displayed branch: either a rooted branch carrying a root
witness compatible with the $\NameCert_{\GraphUp}$ vertex row, or a root-free
branch carrying a witness that no vertex row in the same visible package has
been designated as root. Both branches are transported by the same $\hsame$
and $\Pkg$ data as the graph package.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_root\_branch\_transport}
\begin{proof}
Proceed by case analysis on the root branch recorded in the carrier row.

In the rooted case, read the vertex witness from the visible
$\NameCert_{\GraphUp}$ package and check that its $\Pkg$ provenance is the one
used by the tree endpoint. Transport along $\hsame$ carries the root witness
with the transported vertex row.

In the root-free case, keep the same graph package and read the negative root
row as a visible witness attached to that package. Its transport uses the
same $\hsame$ and $\Pkg$ data, so no hidden root selection can enter the
carrier classifier.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreeVisibleCarrier\_root\_witness\_obligation}

\begin{theorem}[TreeUp stability and ledger obligation]\label{thm:treeup-stability-ledger-obligation}
The $\TreeUp$ stability ledger consists exactly of the hidden graph route data,
the hidden root-choice data, and the hidden connectedness and acyclicity
witnesses used to justify the visible rows. Transport stability may consume
those ledger entries, but the public carrier and classifier expose only
$\mathsf{BHist}$ endpoints, $\mathsf{GraphContEdge}$ rows, $\Cont$ rows,
$\hsame$ transport, $\Pkg$ provenance, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Use the stability and ledger fields of the roadmap ten-field template. The
visible surface is fixed by the graph package, edge rows, connectedness row,
acyclicity row, and root branch, so the remaining data can only record how
those rows were obtained.

The hidden graph route supports connectedness, the hidden root-choice data
supports the rooted branch when present, and the hidden acyclicity and
connectedness witnesses justify the two graph-shape rows. Because each entry
is consumed only to transport or justify a visible row, the ledger is exact
and no additional hidden field is part of carrier equality.
\end{proof}

\begin{theorem}[TreeUp exactness obligation]\label{thm:treeup-exactness-obligation}
The visible $\TreeUp$ package is exact in three senses. Coverage says every
accepted tree endpoint exposes a $\NameCert_{\GraphUp}$ package, its
$\mathsf{GraphContEdge}$ rows, connectedness, acyclicity, and one root branch.
Soundness says any endpoint exposing those rows is a valid $\TreeUp$ carrier
only under the classifier of \autoref{thm:treeup-classifier-obligation}.
Completeness says every visible connected acyclic graph package with either a
root witness or a root-free witness is represented by a $\TreeUp$ carrier row.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeDerivationSpineLedger\_exactness\_rows}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_exactness\_rows}
\begin{proof}
For coverage, invert the carrier row of
\autoref{def:treeup-bhist-carrier}. The inversion reads back the
$\NameCert_{\GraphUp}$ package, each $\mathsf{GraphContEdge}$ row, the
connectedness row, the acyclicity row, and the root branch.

For soundness, apply the classifier obligation after the visible rows have
been read back. The only admissible identifications are the transported graph
edge rows together with matching connectedness, acyclicity, and root-branch
rows.

For completeness, begin with a visible connected acyclic graph package and a
chosen root branch. Repack those rows through $\mathsf{BHist}$, $\Pkg$, and
$\NameCert$, placing route and witness choices in the ledger, to obtain the
required $\TreeUp$ carrier endpoint.
\end{proof}

\section{The certificate}
\label{sec:tree-certificate}

\begin{theorem}[TreeUp connectedness obligation]
\label{thm:tree-connectedness-obligation}
For every accepted $\TreeUp$ history and every carried endpoint displayed by
its $\GraphUp$ source, the certificate supplies a finite $\mathsf{BHist}$ path
row through the $\GraphUp$ edge ledger from that endpoint to the selected root
component. The path row records each intermediate endpoint, each traversed
edge incidence, and the classifier comparison that carries the path under
$\hsame$ transport.
\end{theorem}
\begin{proof}
Read the endpoint from the graph-source carrier row supplied by
\autoref{thm:tree-graph-source-carrier-obligation}. The finite incidence
ledger gives the only admissible edges and their source-target endpoint rows.

The connectedness witness is a finite path ledger indexed by those admissible
edges. Its first endpoint is the displayed carried endpoint, its last endpoint
lies in the selected root component, and every adjacent step is justified by
one incidence row from the $\GraphUp$ ledger.

Transport the path by applying componentwise $\hsame$ to the endpoint rows and
the inherited $\GraphUp$ classifier to the edge rows. The transported ledger
has the same finite shape, so it remains a path from the transported endpoint
to the transported root component.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreeGraphSource\_connected\_root\_path\_readback}

\begin{theorem}[TreeUp acyclicity obligation]
\label{thm:tree-acyclicity-obligation}
For an accepted $\TreeUp$ history, every closed finite edge walk in the
$\GraphUp$ edge ledger collapses under the $\TreeUp$ classifier to the
trivial endpoint row. Equivalently, the certificate records that the only
closed walk recognized by the tree classifier is the displayed stationary
walk at its endpoint, and this recognition is stable under $\hsame$ transport.
\end{theorem}
\begin{proof}
Start with a closed finite edge walk in the incidence ledger of
\autoref{thm:tree-graph-source-carrier-obligation}. Each step is a
$\GraphUp$ edge row whose endpoints are carried $\mathsf{BHist}$ histories,
and the closing condition is itself a displayed classifier row.

Apply the acyclicity witness carried by the $\TreeUp$ certificate. It reduces
the closed walk ledger to the stationary endpoint row in the $\TreeUp$
classifier, so the edge sequence contributes no nontrivial cycle to the
accepted carrier.

Stability follows by transporting the whole walk ledger componentwise through
$\hsame$ and through the inherited $\GraphUp$ classifier. The acyclicity
witness reads the transported closed walk and again returns the stationary
endpoint row at the transported endpoint.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_closed\_walk\_tail\_empty}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistObligationCarrier\_acyclic\_empty\_transport}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistObligationCarrier\_acyclic\_unit\_loop\_exactness}
\leanchecked{BEDC.Derived.TreeUp.TreeGraphContEdge\_closed\_walk\_unit\_collapse}

\begin{theorem}[TreeUp root-or-root-free witness obligation]
\label{thm:tree-root-or-root-free-witness-obligation}
The $\TreeUp$ certificate supplies either a displayed root endpoint carried by
the $\GraphUp$ source or a root-free witness whose classifier is stable under
$\hsame$. In the rooted case the root endpoint is one of the carried
vertices used by the connectedness path rows. In the root-free case the
witness records that connectedness is read componentwise without selecting a
distinguished endpoint, and this record transports along the same
$\TreeUp$ classifier.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeRootWitness\_spine\_closed\_boundary}
\leanchecked{BEDC.Derived.TreeUp.TreeRootBranch\_empty\_root\_witness\_readback}
\begin{proof}
Inspect the root row of the certificate. If the row is rooted, it displays a
$\mathsf{BHist}$ endpoint and a proof that this endpoint is carried by the
$\GraphUp$ source ledger. The connectedness paths then target the component
containing that displayed root.

If the row is root-free, the certificate instead displays a witness that no
distinguished endpoint is part of the public tree data. Connectedness is then
read by component rows, and the witness itself is attached to the
$\TreeUp$ classifier rather than to a host graph equality.

In both cases, transport is rowwise. The rooted endpoint moves by $\hsame$
and the inherited graph classifier, while the root-free witness moves by the
stability field attached to the $\TreeUp$ classifier. Thus the certificate
retains the same rooted or root-free form after transport.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreeRootOrRootFreeWitness\_closed\_walk\_boundary}

\begin{theorem}[TreeUp NameCert obligation surface]
\label{thm:tree-namecert-obligation-surface}
The carrier row of
\autoref{thm:tree-graph-source-carrier-obligation}, the connectedness row of
\autoref{thm:tree-connectedness-obligation}, the acyclicity row of
\autoref{thm:tree-acyclicity-obligation}, and the root-or-root-free row of
\autoref{thm:tree-root-or-root-free-witness-obligation} assemble the
paper-side $\NameCert_{\TreeUp}$ obligation surface. The surface names the
stability, ledger, exactness, and dependency fields required for the certified
tree interface: stability is $\hsame$ and inherited $\GraphUp$ transport;
ledger data are finite vertex, edge, incidence, path, closed-walk, and root
rows; exactness says every public tree row is read from those ledgers; and
dependencies are only the $\mathsf{BHist}$ substrate, $\hsame$, and the
$\GraphUp$ source certificate.
\end{theorem}
\begin{proof}
Begin with the graph-source carrier obligation. It supplies the accepted
$\mathsf{BHist}$ predicate, the inherited $\GraphUp$ classifier, and the
finite incidence ledger for visible vertices and edges.

Add the connectedness obligation. Its finite path rows show that every
carried endpoint reaches the selected root component through the same edge
ledger, and its transport clause supplies the corresponding stability row.

Add the acyclicity and root-or-root-free obligations. Acyclicity gives the
closed-walk exactness clause, while the final witness records whether the
certificate is rooted or root-free and how that row transports under
$\hsame$.

These projections cover the named carrier, classifier, stability, ledger,
exactness, and dependency rows. Since every row is read from
$\mathsf{BHist}$, $\hsame$, and the $\GraphUp$ source certificate, the
assembled surface does not require host graph equality or any additional
bridge field.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_semantic\_name\_certificate}

\begin{theorem}[TreeUp carrier and classifier obligations]
\label{thm:tree-obligation-carrier-classifier}
The $\TreeUp$ carrier data refine the $\GraphUp$ $\Cont$-adjacency carrier of
\autoref{def:graph-cont-adjacency-carrier} to displayed finite $\Cont$-path
rows over $\mathsf{BHist}$ whose vertices satisfy $\mathsf{UnaryHistory}$. Its
classifier is inherited from the $\GraphUp$ endpoint/path classifier and is
transported componentwise by $\hsame$: two displayed tree-path rows are
admissibly compared only when their source vertex, target vertex, visible path
ledger, and every exposed $\mathsf{GraphEdge}$ step are identified through the
same $\hsame$ classifier used by $\NameCert_{\GraphUp}$.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeDerivationSpineLedger\_steps\_unary}
\begin{proof}
The obligation is the carrier-and-classifier portion of the roadmap ten-field
template.
\begin{enumerate}
  \item Traditional target: a finite tree path carrier for $\TreeUp$.
  \item BEDC source: $\mathsf{BHist}$ with $\mathsf{UnaryHistory}$ vertices,
  $\Cont$ rows, and the $\GraphUp$ $\NameCert$ edge predicate.
  \item Carrier: displayed finite rows of $\mathsf{GraphEdge}$ from
  \autoref{def:graph-cont-adjacency-carrier}, read as finite $\Cont$-path
  ledgers.
  \item Classifier: the $\GraphUp$ endpoint/path classifier, transported
  componentwise by $\hsame$ on vertices, edge results, and path ledgers.
  \item Stability: transport of every displayed $\Cont$ row through the
  $\hsame$ transport surface already carried by $\NameCert_{\GraphUp}$.
  \item Ledger: the source and target histories, the finite list of exposed
  $\mathsf{GraphEdge}$ rows, and the visible path-result history.
  \item Core theorem list: carrier refinement, classifier inheritance, and
  path-row transport.
  \item Exactness: a carried tree path is exactly its finite $\GraphUp$ row
  display over $\mathsf{BHist}$.
  \item Standard bridge: none.
  \item Status: paper obligation.
\end{enumerate}

Thus $\TreeUp$ does not introduce a second carrier beside $\GraphUp$. It
selects the finite displayed $\Cont$-path rows and reads their classifier
through the same $\hsame$-based $\NameCert$ surface.
\end{proof}

\begin{theorem}[TreeUp connectedness and acyclicity obligations]
\label{thm:tree-obligation-connected-acyclic}
For $\TreeUp$, connectedness is the obligation to supply, for every pair of
carried vertices, a finite $\mathsf{GraphEdge}$ path package between them.
Acyclicity is the obligation that every nontrivial closed finite path row reads
through $\hsame$ as impossible at the tree classifier: any displayed closed
path must collapse to the trivial unit loop supplied by $\GraphUp$. The
connected and acyclic rows are scoped by the $\GraphUp$ unit-loop laws, the
two-step composition surface, and the three-step path reassociation surface.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_closed\_path\_unit\_loop}
\begin{proof}
The obligation is the path-law portion of the roadmap ten-field template.
\begin{enumerate}
  \item Traditional target: connected acyclic graph data.
  \item BEDC source: $\mathsf{BHist}$, $\mathsf{UnaryHistory}$, $\Cont$, and
  the $\GraphUp$ $\NameCert$ rows.
  \item Carrier: the finite $\mathsf{GraphEdge}$ path packages named in
  \autoref{thm:tree-obligation-carrier-classifier}.
  \item Classifier: $\hsame$ on endpoints and visible path ledgers, inherited
  from the $\GraphUp$ endpoint/path classifier.
  \item Stability: path extension, transport, and reassociation through
  $\hsame$.
  \item Ledger: the chosen finite path between two carried vertices, or the
  displayed closed path whose nontriviality is being tested.
  \item Core theorem list: connected path existence, unit-loop collapse,
  two-step composition, and three-step reassociation.
  \item Exactness: the connectedness row exposes exactly a finite
  $\mathsf{GraphEdge}$ path, and the acyclicity row rejects exactly the
  nontrivial closed path rows.
  \item Standard bridge: none.
  \item Status: paper obligation.
\end{enumerate}

The unit case is read through
\autoref{thm:graph-cont-unit-loop}. Composite paths are assembled through
\autoref{thm:graph-cont-two-step-composition}, and adjacent reassociations are
compared through \autoref{thm:graph-cont-three-step-path-reassociation}.

The acyclicity obligation then says that any closed finite row whose source and
target are identified by $\hsame$ must be the unit-loop row in the displayed
$\NameCert_{\GraphUp}$ ledger. Every nontrivial closed row is therefore outside
the accepted $\TreeUp$ certificate surface.
\end{proof}

\begin{theorem}[TreeUp root, ledger, and exactness obligations]
\label{thm:tree-obligation-root-ledger-exactness}
The $\TreeUp$ certificate must expose exactly the following rows: a
root-or-root-free witness, a syntactic-representation carrier row, the finite
path witnesses of \autoref{thm:tree-obligation-connected-acyclic}, the
acyclicity witnesses of \autoref{thm:tree-obligation-connected-acyclic}, and
the dependency rows inherited from $\GraphUp$. These rows are precisely the
ledger and exactness surface for $\TreeUp$: every accepted tree certificate is
read back to those displayed rows, and every displayed row is classified by
the $\GraphUp$ $\hsame$ endpoint/path classifier over $\mathsf{BHist}$ and
$\mathsf{UnaryHistory}$.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeRootLedger\_derivation\_spine\_boundary}
\begin{proof}
The obligation is the provenance-and-exactness portion of the roadmap
ten-field template.
\begin{enumerate}
  \item Traditional target: a rooted or root-free tree certificate with
  syntactic representation.
  \item BEDC source: $\mathsf{BHist}$, $\mathsf{UnaryHistory}$, $\Cont$, and
  the dependency surface of $\NameCert_{\GraphUp}$.
  \item Carrier: the finite tree-path carrier of
  \autoref{thm:tree-obligation-carrier-classifier}, augmented by a
  root-or-root-free witness and a syntactic-representation carrier row.
  \item Classifier: the $\GraphUp$ classifier transported through $\hsame$ on
  roots, endpoints, path rows, and representation ledgers.
  \item Stability: transport of root status, path witnesses, acyclicity
  witnesses, and representation rows along the inherited classifier.
  \item Ledger: the root witness, representation row, connectedness paths,
  acyclicity witnesses, and dependency rows.
  \item Core theorem list: root coverage, root-free coverage, ledger
  readback, path exactness, acyclicity exactness, and dependency exactness.
  \item Exactness: no additional tree datum is accepted beyond the displayed
  ledger rows, and each displayed ledger row is sufficient for the certificate
  boundary.
  \item Standard bridge: none.
  \item Status: paper obligation.
\end{enumerate}

The root-or-root-free witness determines the public orientation of the tree
carrier, while the syntactic-representation carrier row records the derivation
syntax surface that consumes $\TreeUp$.

The path witnesses and acyclicity witnesses are exactly the rows named in
\autoref{thm:tree-obligation-connected-acyclic}. The remaining dependency
rows are the $\GraphUp$ carrier, classifier, unit-loop, two-step composition,
and three-step reassociation rows. Repacking this list gives the full
$\TreeUp$ ledger, and exactness says that the certificate contains no hidden
data outside this displayed $\hsame$-classified $\NameCert$ boundary.
\end{proof}

\closureat{TreeUp}{scopedStr}
\closureat{TreeUp}{publicStr}[BEDC.Derived.GraphUp.GraphCont\_namecert\_surface]
\closureat{TreeUp}{bridgedStr}
\closureat{TreeUp}{matureStr}[BEDC.Derived.GraphUp.GraphCont\_namecert\_surface]

\begin{definition}[Tree obligation surface]\label{def:tree-obligation-surface}
Fix a BEDC source history sort $\mathsf{BHist}$ and a graph certificate source $\NameCert_{\GraphUp}$ as in \autoref{ch:concrete-instances-graph-namecert}. A concrete $\TreeUp$ obligation record consists of the following rows on histories:
\begin{enumerate}
  \item $\mathsf{TreeCarrier}(h)$ is the $\mathsf{BHist}$ predicate saying that $h$ carries a $\NameCert_{\GraphUp}$ graph row, a connectedness row, an acyclicity row, a root-or-root-free witness row, and a syntactic-representation row whose visible nodes are histories.
  \item $\mathsf{TreeClassifier}(h,k)$ is $\hsame(h,k)$ together with transport of the graph-source, connectedness, acyclicity, root-or-root-free, and syntactic-representation rows from $h$ to $k$ and back along $\hsame$.
  \item The ledger records exactly three hidden rows: the graph presentation supplied by $\NameCert_{\GraphUp}$, the root witness or explicit root-free witness, and the derivation-spine syntax read over $\mathsf{BHist}$ nodes.
\end{enumerate}
The obligation surface is scoped over the existing seed row $\closureat{TreeUp}{seedStr}$, so it introduces no new kernel primitive and no independent object equality beyond $\hsame$.
\end{definition}
\leandef{BEDC.Derived.TreeUp.TreeObligationSurface}

\begin{theorem}[Tree graph-source obligation]\label{thm:tree-graph-source-obligation}
For every history $h$, $\mathsf{TreeCarrier}(h)$ exposes the underlying $\NameCert_{\GraphUp}$ source row. If $\mathsf{TreeClassifier}(h,k)$ holds, then this graph-source row is transported from $h$ to $k$ along $\hsame(h,k)$, and the reverse transport is supplied by the symmetric $\hsame$ witness contained in the same classifier row.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeGraphSource\_obligation\_rows}
\begin{proof}
Unpack $\mathsf{TreeCarrier}(h)$ from \autoref{def:tree-obligation-surface}. Its first component is the graph certificate source row, so the carrier projection directly exposes the $\NameCert_{\GraphUp}$ datum.

Now unpack $\mathsf{TreeClassifier}(h,k)$. The classifier contains $\hsame(h,k)$ and the declared row transport for the graph source. Symmetry of $\hsame$ supplies the reverse endpoint comparison, and the seed scope $\closureat{TreeUp}{seedStr}$ fixes that the transported source is the same graph-source row rather than a second graph presentation.
\end{proof}

\begin{theorem}[Tree carrier-classifier obligation]\label{thm:tree-carrier-classifier-obligation}
On carried histories, $\mathsf{TreeClassifier}$ is reflexive, symmetric, and transitive, and it transports $\mathsf{TreeCarrier}$: if $\mathsf{TreeCarrier}(h)$ and $\mathsf{TreeClassifier}(h,k)$ hold, then $\mathsf{TreeCarrier}(k)$ holds with the graph-source, connectedness, acyclicity, root-or-root-free, and syntactic-representation rows transported along $\hsame(h,k)$.
\end{theorem}
\begin{proof}
Reflexivity uses reflexivity of $\hsame$ on $\mathsf{BHist}$ and the identity transport of each row in \autoref{def:tree-obligation-surface}. The graph source comes from $\NameCert_{\GraphUp}$; the connectedness, acyclicity, root-or-root-free, and syntactic-representation rows are carried unchanged at the same endpoint.

Symmetry uses symmetry of $\hsame$. Each row transport stored in $\mathsf{TreeClassifier}(h,k)$ is part of a two-sided transport package, so reversing the endpoint comparison reverses the carrier rows without changing the visible ledger.

Transitivity composes the two $\hsame$ witnesses and then composes the corresponding row transports. The composed transport carries exactly the graph-source, connectedness, acyclicity, root-or-root-free, and syntactic-representation rows named by the surface, so carrier transport follows by repacking those transported rows as $\mathsf{TreeCarrier}(k)$.
\end{proof}

\begin{theorem}[Tree root-witness obligation]\label{thm:tree-root-witness-obligation}
Every $\mathsf{TreeCarrier}(h)$ supplies exactly one admissible root row: either a designated root witness compatible with the carried $\NameCert_{\GraphUp}$ source or an explicit root-free witness row. This witness row is stable under $\mathsf{TreeClassifier}$, so $\mathsf{TreeClassifier}(h,k)$ transports the same root-or-root-free alternative from $h$ to $k$ along $\hsame$.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeRootWitness\_spine\_extension\_exactness}
\begin{proof}
The carrier row in \autoref{def:tree-obligation-surface} includes the root-or-root-free witness as a named component of the tree certificate. The alternative is admissible because it is read against the same $\NameCert_{\GraphUp}$ source row exposed by \autoref{thm:tree-graph-source-obligation}.

For stability, unpack $\mathsf{TreeClassifier}(h,k)$. Its row-transport component explicitly includes the root-or-root-free row, and the endpoint relation is $\hsame(h,k)$. Transport therefore preserves the chosen alternative, while the ledger component records that no additional root presentation is introduced.
\end{proof}

\begin{theorem}[Tree stability-ledger obligation]\label{thm:tree-stability-ledger-obligation}
Let $u$ be a visible derivation-spine extension with $\UnaryHistory(u)$, and suppose $\Cont(h,u,r)$ is compatible with the carried graph source. If $\mathsf{TreeCarrier}(h)$ holds and the endpoints $r$ and $k$ are related by $\hsame(r,k)$, then the tree ledger rows transported to $k$ are exactly the graph presentation, root witness, and derivation-spine syntax rows obtained by extending the visible spine by $u$.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeGraphContEdge\_visible\_spine\_extension\_transport}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_stability\_ledger\_transport}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_visible\_spine\_extension\_ledger}
\begin{proof}
Start with $\mathsf{TreeCarrier}(h)$ and the $\Cont(h,u,r)$ extension. Compatibility with the graph source says that the visible extension reads edges through the same $\NameCert_{\GraphUp}$ row, and $\UnaryHistory(u)$ keeps the appended spine inside the unary derivation shape allowed by the seed scope.

The endpoint comparison $\hsame(r,k)$ supplies the classifier transport target. Applying the row transports in \autoref{def:tree-obligation-surface} moves the graph presentation, root witness, and derivation-spine syntax rows to $k$.

It remains to identify the ledger boundary. The ledger of the surface contains only those three hidden rows, so after $\Cont$ extension and $\hsame$ transport there is no extra presentation row to account for. The transported ledger is therefore exactly the extended visible-spine ledger.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreeVisibleDerivationSpine\_extension\_transport}
\leanchecked{BEDC.Derived.TreeUp.TreeVisibleSpine\_extension\_ledger}

\begin{theorem}[Tree syntactic-representation obligation]\label{thm:tree-syntactic-representation-obligation}
Every $\mathsf{TreeCarrier}(h)$ provides a syntactic derivation carrier row whose visible nodes are $\mathsf{BHist}$ histories and whose edge relation is read from the carried $\NameCert_{\GraphUp}$ source. Consequently the $\TreeUp$ certificate surface can be consumed by downstream inductive-carrier and derivation-syntax chapters without replacing the BEDC history carrier or the $\hsame$ classifier.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_syntactic\_representation}
\begin{proof}
Unpack the syntactic-representation row in \autoref{def:tree-obligation-surface}. Its node carrier is explicitly $\mathsf{BHist}$, so every visible derivation node is already a BEDC history rather than an external graph vertex.

The edge relation is not an additional primitive. It is read from the $\NameCert_{\GraphUp}$ source exposed by \autoref{thm:tree-graph-source-obligation}, and classifier movement is handled by $\hsame$ through \autoref{thm:tree-carrier-classifier-obligation}.

Downstream consumers therefore receive the same carrier, the same classifier, and the same visible derivation-spine syntax. Inductive-carrier arguments may use $\UnaryHistory$ and $\Cont$ for spine extension, while derivation-syntax arguments read edges from the graph source fixed by the existing seed scope.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreeSyntacticRepresentation\_carrier\_readback}

\begin{theorem}[TreeUp public certificate boundary]
\label{thm:treeup-public-certificate-boundary}
The public $\TreeUp$ interface is the single package consisting exactly of the $\GraphUp$ source package, the $\mathsf{GraphContEdge}$ rows, the connectedness row, the acyclicity row, the root-or-root-free branch, the syntactic-representation row, stability transports, ledger rows, and exactness clauses established by \autoref{thm:treeup-carrier-obligation}, \autoref{thm:treeup-classifier-obligation}, \autoref{thm:treeup-root-witness-obligation}, \autoref{thm:treeup-stability-ledger-obligation}, \autoref{thm:treeup-exactness-obligation}, and \autoref{thm:tree-syntactic-representation-obligation}. This boundary exports no host graph equality and no unrecorded route choice.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreeObligationSurface\_public\_certificate\_boundary}
\begin{proof}
The visible carrier package is supplied by \autoref{thm:treeup-carrier-obligation}: it exposes the $\NameCert_{\GraphUp}$ source, the $\mathsf{GraphContEdge}$ rows, connectedness, acyclicity, and the rooted or root-free branch. The classifier boundary is the rowwise transport of \autoref{thm:treeup-classifier-obligation}, so public comparison is only through the inherited graph package and the displayed tree rows.

The stability and ledger fields are exactly those named in \autoref{thm:treeup-stability-ledger-obligation}. They record the route, root-choice, connectedness, and acyclicity witnesses needed to justify the visible rows, while the transports keep every public component on the same $\mathsf{BHist}$, $\hsame$, $\Pkg$, and $\NameCert$ surface.

Exactness comes from \autoref{thm:treeup-exactness-obligation}: every accepted endpoint reads back to the displayed package, and every displayed connected acyclic graph package with one root branch repacks as a $\TreeUp$ carrier. The syntactic-representation row of \autoref{thm:tree-syntactic-representation-obligation} adds the derivation-spine consumer surface without changing the carrier or classifier.

Thus the public package is precisely the listed rows and clauses. Host graph equality is not a public component, route choices remain ledger entries rather than exported data, and no standard bridge is included in the $\TreeUp$ boundary.
\end{proof}
\leanchecked{BEDC.Derived.TreeUp.TreePublicCertificateBoundary\_semantic\_name\_certificate}

\begin{theorem}[TreeUp public derivation-syntax bridge]
\label{thm:treeup-public-derivation-syntax-bridge}
The public $\TreeUp$ package of \autoref{thm:treeup-public-certificate-boundary}, together with \autoref{def:tree-obligation-surface} and \autoref{thm:tree-syntactic-representation-obligation}, determines a paper bridge to the external finite rooted-or-root-free tree presentation whose nodes are $\mathsf{BHist}$ histories and whose edges are the displayed $\mathsf{GraphContEdge}$ rows. Interpretation from $\TreeUp$ to the external presentation and re-encoding from any externally presented tree whose rows are already displayed $\GraphUp/\TreeUp$ rows are exact up to the inherited $\hsame$ classifier. Neither direction exports host graph equality or hidden route choice.
\end{theorem}
\leanchecked{BEDC.Derived.TreeUp.TreePublicDerivationSyntaxBridge\_visible\_spine\_package}
\leanchecked{BEDC.Derived.TreeUp.TreeBHistCarrier\_derivation\_syntax\_roundtrip}
\leanchecked{BEDC.Derived.TreeUp.TreePublicDerivationSyntaxBridge\_round\_trip}
\begin{proof}
For interpretation, read the public boundary of \autoref{thm:treeup-public-certificate-boundary}. Its visible rows give the $\NameCert_{\GraphUp}$ source, the displayed $\mathsf{GraphContEdge}$ edges, connectedness, acyclicity, the root-or-root-free branch, and the syntactic-representation row. By \autoref{def:tree-obligation-surface} and \autoref{thm:tree-syntactic-representation-obligation}, the nodes of that presentation are precisely $\mathsf{BHist}$ histories, and the edge table is precisely the displayed graph-continuation edge table.

For re-encoding, start with an external finite rooted-or-root-free tree presentation whose nodes are already $\mathsf{BHist}$ histories and whose edge rows are already displayed $\GraphUp/\TreeUp$ rows. Apply \autoref{thm:treeup-exactness-obligation}: the displayed graph source, edge rows, connectedness and acyclicity rows, root branch, syntactic row, stability transports, and ledger rows repack as the same public $\TreeUp$ package.

The first round trip compares the interpreted package with the original $\TreeUp$ data. Use \autoref{thm:treeup-classifier-obligation} to compare the public graph and path rows componentwise, and use \autoref{thm:tree-carrier-classifier-obligation} to transport the tree carrier rows along the inherited $\hsame$ witness.

The second round trip compares the re-encoded external presentation with the displayed rows from which it was formed. The carrier, classifier, and syntactic-representation clauses are the same rows named in \autoref{def:tree-obligation-surface}; exactness prevents an additional route or graph-equality component from entering the package. Both comparisons are therefore exact only up to $\hsame$, which is the advertised bridge classifier.
\end{proof}

\begin{closurestatus}{\TreeUp}
  \theoryclosure{\matureClosure}
  \scopeclosed{The public $\TreeUp$ boundary, the derivation-syntax bridge of \autoref{thm:treeup-public-derivation-syntax-bridge}, and the visible-spine extension ledger rows identify the external finite rooted-or-root-free tree presentation over $\mathsf{BHist}$ nodes, $\mathsf{GraphContEdge}$ edge rows, $\Cont$, $\hsame$, $\Pkg$, $\NameCert_{\GraphUp}$, connectedness, acyclicity, root-branch rows, and derivation syntax.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.GraphUp.GraphCont\_namecert\_surface}
  \bridgestatus{paperBridge}
  \notclaimed{This chapter does not claim checked bridge verification, checked connectedness or acyclicity rows, checked root transport, or full $\TreeUp$ Lean verification.}
  \upgradepath{Lean-side audit or bridgeChecked verification for the $\TreeUp$ bridge remains a separate formal-axis obligation.}
\end{closurestatus}
