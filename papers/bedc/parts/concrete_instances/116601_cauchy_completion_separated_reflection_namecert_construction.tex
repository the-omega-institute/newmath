\chapter{A Concrete Naming Certificate for $\CauchyCompletionSeparatedReflectionUp$}
\label{ch:concrete-instances-cauchy-completion-separated-reflection-namecert}
\origin{human}

Following Bishop--Bridges constructive completion and Bourbaki--Kelley separated reflection, the $\CauchyCompletionSeparatedReflectionUp$ packet records the finite interface where a displayed Cauchy completion route is read only after its separated-reflection boundary is visible. It sits beside the completion reflection packet of \autoref{ch:concrete-instances-cauchy-completion-reflection-namecert}, the separated completion reflection packet of \autoref{ch:concrete-instances-separated-completion-reflection-namecert}, the Hausdorff reflection packet of \autoref{ch:concrete-instances-hausdorff-reflection-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names separated reflection as BEDC rows in the completion route; it does not quotient completed points, choose representatives, assert host equality, import trichotomy, use countable choice, or claim ambient completeness of $\RealUp$.

\begin{definition}[CauchyCompletionSeparatedReflection carrier]
\label{def:cauchy-completion-separated-reflection-carrier}
A $\CauchyCompletionSeparatedReflectionUp$ carrier is a finite $\BHist$ packet
$$
K=(R,S,H_0,W,Q,D,E,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\CauchyCompletionReflectionUp$ route that exposes unit, counit, comparison, and completion-reflection data before any real-facing seal is read.
\item $S$ is the $\SeparatedCompletionReflectionUp$ row that records zero-distance separation as a displayed completion-uniqueness interface.
\item $H_0$ is the $\HausdorffReflectionUp$ row that keeps the separated classifier visible before a completion consumer reads equality-facing data.
\item $W$ is the $\StreamNameUp$ finite-window row opened before rational observations are consumed.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback row shared by the completion and separation routes.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the finite windows and readback comparisons.
\item $E$ is the terminal $\RealUp$ seal reached only after $W,Q,D$ and the separated reflection rows have been displayed.
\item $T$ records componentwise $\hsame$ transport for $R,S,H_0,W,Q,D,E$ and the structural rows.
\item $C$ records displayed $\Cont$ replay of the separated completion-reflection route.
\item $P$ is the $\Pkg$ provenance over $\CauchyCompletionSeparatedReflectionUp$, $\CauchyCompletionReflectionUp$, $\SeparatedCompletionReflectionUp$, $\HausdorffReflectionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCompletionSeparatedReflectionUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient-completion equality, selected completed representatives, host equality, trichotomy, countable choice, open-cover compactness, or ambient real completeness.
\end{definition}

\begin{theorem}[CauchyCompletionSeparatedReflection NameCert obligations]
\label{thm:cauchy-completion-separated-reflection-namecert-obligations}
For every accepted $\CauchyCompletionSeparatedReflectionUp$ carrier
$$
K=(R,S,H_0,W,Q,D,E,T,C,P,N),
$$
the local $\NameCert_{\CauchyCompletionSeparatedReflectionUp}$ surface consists exactly of carrier admission, completion-reflection exposure, separated-reflection exposure, Hausdorff boundary visibility, finite-window admission, regular rational readback, dyadic tolerance readback, terminal real-seal routing, componentwise transport, continuation replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:cauchy-completion-separated-reflection-carrier}. Its displayed coordinates are exactly $R,S,H_0,W,Q,D,E,T,C,P,N$, and each coordinate has one local role in the separated completion-reflection route.

The completion-facing part is $R,S,H_0$. The row $R$ supplies the Cauchy completion-reflection surface, $S$ supplies the separated completion-reflection boundary, and $H_0$ keeps the Hausdorff reflection classifier visible before any equality-facing completion read is consumed.

The analytic readback part is $W,Q,D,E$. The finite stream window, regular rational readback, and dyadic tolerance rows must be displayed before the terminal $\RealUp$ seal is available.

Finally $T,C,P,N$ transport, replay, source, and name the same rows. Since the carrier has no further coordinate, the local certificate cannot export quotient completion equality, selected representatives, host equality, trichotomy, countable choice, compactness, or ambient real completeness.
\end{proof}

\begin{theorem}[CauchyCompletionSeparatedReflection universal route]
\label{thm:cauchy-completion-separated-reflection-universal-route}
Every completion-facing consumer of an accepted $\CauchyCompletionSeparatedReflectionUp$ carrier factors through the displayed route
$$
\CauchyCompletionReflectionUp \to \SeparatedCompletionReflectionUp \to \HausdorffReflectionUp \to \StreamNameUp,\RegSeqRatUp,\DyadicRatCoreUp \to \RealUp .
$$
The route is universal for the packet in the finite BEDC sense: any accepted read with the same completion row, separated reflection row, Hausdorff boundary, finite analytic readback, transport, replay, provenance, and local naming is the same $\NameCert$ read. It is not a quotient completion, not a selected representative, not host equality of completed points, not trichotomy, not countable choice, and not ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Start with a completion-facing consumer. The carrier admits no direct route to $E$; the consumer must first read the completion-reflection row $R$, then the separated completion-reflection row $S$, and then the Hausdorff boundary row $H_0$.

Next consume the finite analytic rows. The stream window $W$, regular rational readback $Q$, and dyadic tolerance ledger $D$ are the only rows between the separated boundary and the terminal $\RealUp$ seal $E$.

The structural rows force uniqueness of the local read. Componentwise transport $T$ and replay $C$ preserve the same displayed route, while $P$ and $N$ source and name it. Thus two accepted reads with the same displayed rows are the same local $\NameCert$ read, and none of the refused host objects can enter the universal route.
\end{proof}

\closureat{CauchyCompletionSeparatedReflectionUp}{seedStr}

\begin{closurestatus}{\CauchyCompletionSeparatedReflectionUp}
  \constructivestory{A $\CauchyCompletionSeparatedReflectionUp$ packet is generated from CauchyCompletionReflection, SeparatedCompletionReflection, HausdorffReflection, StreamName, RegSeqRat, DyadicRatCore, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-completion-separated-reflection-carrier}, \autoref{thm:cauchy-completion-separated-reflection-namecert-obligations}, and \autoref{thm:cauchy-completion-separated-reflection-universal-route} seed the finite separated-reflection interface for Cauchy completion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient completion equality, selected completed representatives, host equality, trichotomy, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, separated-reflection exactness, Hausdorff-boundary visibility, finite analytic readback, non-escape, and a Lean carrier formalization for BEDC.Derived.CauchyCompletionSeparatedReflectionUp.}
\end{closurestatus}
