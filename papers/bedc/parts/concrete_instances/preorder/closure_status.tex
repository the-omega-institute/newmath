\closureat{PreorderUp}{matureStr}[BEDC.Derived.PreorderUp.preorder\_carrier\_semantic\_name\_certificate]
\begin{closurestatus}{\PreorderUp}
  \constructivestory{A preorder in the concrete BEDC instance starts from the unary history spine: $\emp$ and its repeated b1-extensions are admitted, while any b0 branch is outside the carrier. The comparison says that one unary history is below another when a unary continuation tail extends the first to the second, and the classifier on carried endpoints is the carrier-aware $\hsame$ relation rather than a quotient of comparable histories. Thus the source-domain is the same unary carrier, the pattern is reflexive and transitive prefix comparison with congruence under endpoint replacement, and the ledger records the continuation tail witnessing each displayed comparison together with the raw unary endpoints. The five name-certificate obligations come from the unary spine itself: $\emp$ inhabits the carrier, history-sameness gives reflexivity, symmetry, and transitivity for the classifier, and unary transport keeps any history-same representative inside the carrier. A history $h$ lies in the signature gap of a $\PreorderUp$ token $p$ when the source-domain accepts $h$, the signature relation pairs it with an answer history and comparison ledger, and token introduction names that answer as $p$; unfolded, $h$ is unary and the recorded prefix continuations are exactly the order evidence encoded by $p$. The paper bridge reads this surface as the finite unary prefix preorder, where a prefix tail is the ordinary witness that one finite unary word extends another, then reads back to the same continuation data. The chapter registers this continuation-order construction under the name $\PreorderUp$; the relation is generated by BEDC append and $\hsame$, with no arbitrary preorder carrier, totality field, antisymmetry quotient, or order topology introduced.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The public unary-prefix preorder surface binds the comparison constructors, public relation readback, endpoint exactness, carrier/classifier rows, and ledger rows for $\mathsf{PreorderPrefixLE}$ over $\mathsf{UnaryHistory}$.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.PreorderUp.preorder\_carrier\_semantic\_name\_certificate}
  \bridgestatus{paperBridge}
  \notclaimed{General order theory: arbitrary preorders, quotient completions, totality, antisymmetry, lattices, order topology, or standard-model bridge closure.}
  \upgradepath{Lean bridge checking for the standard boundary bridge remains the next verification step; the paper mature closure is the recorded theory grade.}
\end{closurestatus}
