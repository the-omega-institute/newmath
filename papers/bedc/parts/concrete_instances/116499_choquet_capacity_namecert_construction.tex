\chapter{A Concrete Naming Certificate for $\ChoquetCapacityUp$}
\label{ch:concrete-instances-choquet-capacity-namecert}
\origin{human}

Following Choquet and classical capacity theory, the $\ChoquetCapacityUp$ packet records finite capacity data over a compact measurable source without promoting the host outer-measure construction to a primitive. It sits beside the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the measure interface of \autoref{ch:concrete-instances-measure-namecert}, the Radon-measure route of \autoref{ch:concrete-instances-radon-measure-namecert}, the Choquet-simplex boundary of \autoref{ch:concrete-instances-choquet-simplex-namecert}, and the Choquet-integral consumer of \autoref{ch:concrete-instances-choquet-integral-namecert}. The packet names only displayed monotonicity, continuity-window, compact-exhaustion, polar-set, transport, replay, provenance, and local naming rows; it does not assert a host capacitability theorem, completed outer measure, quotient set algebra, countable-choice selector, or ambient measure completion.

\begin{definition}[Choquet capacity carrier]
\label{def:choquet-capacity-carrier}
A $\ChoquetCapacityUp$ carrier is a finite $\BHist$ packet
$$
Q=(K,M,R,S,I,O,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ support row, exposing the compact source on which finite compact windows and exhaustion witnesses are read.
\item $M$ is the $\MeasureUp$ measurable-event row, recording only the event family and finite value reads used by the capacity ledger.
\item $R$ is the $\RadonMeasureUp$ regularity row, available when the capacity read is induced by a regular measure route.
\item $S$ is the $\ChoquetSimplexUp$ sibling boundary row, recording that probability-boundary barycentric data are adjacent but not collapsed into capacity equality.
\item $I$ is the $\ChoquetIntegralUp$ consumer row, recording that integral reads may consume the capacity only after the capacity rows are displayed.
\item $O$ is the outer-approximation ledger, giving finite open-window majorants and compact-window minorants without introducing an outer-measure primitive.
\item $W$ is the continuity-window ledger, recording finite monotonicity, continuity-from-below, and continuity-from-above checks on the displayed windows.
\item $H$ records componentwise $\hsame$ transport for $K,M,R,S,I,O,W$ and the local naming row.
\item $C$ records displayed $\Cont$ replay from compact and measurable source rows into regularity, outer-approximation, continuity-window, integral-consumer, and local naming reads.
\item $P$ is the $\Pkg$ provenance over $\ChoquetCapacityUp$, $\CompactUp$, $\MeasureUp$, $\RadonMeasureUp$, $\ChoquetSimplexUp$, $\ChoquetIntegralUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ChoquetCapacityUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,M,R,S,I,O,W,H,C,P,N$. It has no coordinate for a completed outer-measure algebra, a host capacitability theorem, selected analytic exhaustion, quotient equality of measurable sets, countable choice, or ambient measure completion.
\end{definition}

\begin{theorem}[Choquet capacity obligation surface]
\label{thm:choquet-capacity-obligation-surface}
For every accepted $\ChoquetCapacityUp$ carrier $Q=(K,M,R,S,I,O,W,H,C,P,N)$, the local $\NameCert_{\ChoquetCapacityUp}$ surface consists exactly of carrier admission for the compact, measure, Radon, Choquet-simplex boundary, Choquet-integral consumer, outer-approximation, continuity-window, transport, replay, provenance, and local naming rows; monotonicity exposure through $W$; compact-window accountability through $K,O$; measure and Radon regularity exposure through $M,R$; sibling separation through $S,I$; and structural preservation through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted packet $Q=(K,M,R,S,I,O,W,H,C,P,N)$. The compact source row $K$ and measurable-event row $M$ give the finite source surface on which a capacity can be read.

Use $R,O,W$ for the capacity-specific obligations. The Radon row supplies the regular-measure route when present, the outer-approximation ledger gives displayed majorants and minorants, and the continuity-window ledger records monotonicity and finite continuity checks.

The rows $S$ and $I$ keep the adjacent Choquet simplex and Choquet integral routes visible without identifying either one with the capacity carrier. Thus barycentric probability data and integral readback remain sibling or consumer surfaces rather than hidden components of the classifier.

Finally $H,C,P,N$ transport, replay, source, and name the same displayed rows. Since the carrier has no completed outer-measure algebra, host capacitability theorem, quotient set equality, selected exhaustion, choice schedule, or ambient measure-completion coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Choquet capacity consumer handoff]
\label{thm:choquet-capacity-consumer-handoff}
Every $\ChoquetIntegralUp$ or $\ChoquetSimplexUp$ consumer of an accepted $\ChoquetCapacityUp$ packet must first read the finite route
$$
\CompactUp,\MeasureUp \longmapsto \RadonMeasureUp \longmapsto O,W \longmapsto \ChoquetCapacityUp \longmapsto \ChoquetIntegralUp,\ChoquetSimplexUp .
$$
The exported row is a displayed capacity handoff over compact and measurable windows. It is not an outer-measure primitive, a host capacitability theorem, a quotient event algebra, a selected representing measure, or an ambient completion principle.
\end{theorem}
\begin{proof}
Start with $K$ and $M$. They expose the compact source and measurable-event surface before any capacity value or consumer route is available.

Next read $R,O,W$. The regularity row, outer-approximation ledger, and continuity-window ledger provide the finite capacity evidence; none of these rows selects an external exhaustion or invokes a completed outer measure.

Only then may a consumer reach $I$ or compare with the sibling boundary $S$. The structural rows $H,C,P,N$ preserve exactly that route, so the public handoff cannot bypass the compact, measure, Radon, approximation, and continuity rows.
\end{proof}

\falsifiablePrediction{Every accepted $\ChoquetCapacityUp$ public read exposes Compact, Measure, RadonMeasure, outer-approximation, continuity-window, ChoquetIntegral, ChoquetSimplex, transport, replay, provenance, and local NameCert rows before any capacity consumer is public.}

\independenceWitness{compact, measure, radon-measure, choquet-simplex, choquet-integral: no listed sibling supplies the joint finite capacity packet containing compact source, measurable-event, Radon regularity, outer-approximation, continuity-window, integral-consumer, simplex-boundary, transport, replay, provenance, and local NameCert rows while refusing outer-measure primitives, host capacitability, quotient set equality, selected exhaustion, countable choice, and ambient measure completion.}

\closureat{ChoquetCapacityUp}{seedStr}

\begin{closurestatus}{\ChoquetCapacityUp}
  \constructivestory{A $\ChoquetCapacityUp$ packet is generated from Compact, Measure, RadonMeasure, ChoquetSimplex, ChoquetIntegral, outer-approximation, continuity-window, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:choquet-capacity-carrier}, \autoref{thm:choquet-capacity-obligation-surface}, and \autoref{thm:choquet-capacity-consumer-handoff} seed the finite capacity route over compact source rows, measurable-event rows, Radon regularity, outer approximations, continuity windows, Choquet integral consumers, Choquet simplex boundaries, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a host capacitability theorem, outer-measure completion, quotient event equality, selected analytic exhaustion, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-window accountability, measure and Radon exposure, outer-approximation and continuity-window routing, sibling separation, transport, replay, provenance, and local naming for BEDC.Derived.ChoquetCapacityUp.}
\end{closurestatus}
