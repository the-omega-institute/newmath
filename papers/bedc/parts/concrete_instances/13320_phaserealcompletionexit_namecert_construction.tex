\chapter{A Concrete Naming Certificate for $\PhaseRealCompletionExitUp$}
\label{ch:concrete-instances-phase-real-completion-exit-namecert}
\origin{ai}

The $\PhaseRealCompletionExitUp$ packet realizes the real-and-completion exit certificate described by \autoref{sec:visions-philosophy-research-programme} as a finite four-face NameCert carrier. It sits after the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the real seal surface of \autoref{ch:concrete-instances-real-namecert}, and the exact completion boundary of \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}. These siblings expose arithmetic, observation, regularity, real sealing, and boundary exactness separately; this carrier binds their exit rows before any consumer may treat the phase as a completed real object or as an ambient completion principle.

\begin{definition}[Phase real-completion exit carrier]
\label{def:phase-real-completion-exit-carrier}
A $\PhaseRealCompletionExitUp$ carrier is a finite $\BHist$ packet
$$
E=(D,S,Q,R,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ face recording the finite dyadic endpoint, tolerance, comparison, and arithmetic ledger used by the exit;
\item $S$ is the $\StreamNameUp$ face recording the finite observation windows through which the exit is read;
\item $Q$ is the $\RegSeqRatUp$ face recording the regular rational sequence handoff generated from the dyadic and stream rows;
\item $R$ is the $\RealUp$ face recording the real seal reached only after $D,S,Q$ have been exposed;
\item $B$ is the $\RealCompletionExactBoundaryUp$ face recording that the same route supplies exact boundary information without exporting host completeness, quotient stream equality, or a selected limit outside the packet;
\item $H$ records componentwise $\hsame$ transport for the dyadic, stream, regular, real, exact-boundary, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ routes for reading $D$, then $S$, then $Q$, then $R$, and finally the boundary row $B$;
\item $P$ is the $\Pkg$ provenance over $\PhaseRealCompletionExitUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RealCompletionExactBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\PhaseRealCompletionExitUp}$ row.
\end{enumerate}
The classifier accepts exactly packets whose visible exit surface is $D,S,Q,R,B,H,C,P,N$. It has no coordinate for an ambient Cauchy completion, quotient-stream equality, selected real limit, host completeness proof, or a fused bridge token identifying the four local faces.
\end{definition}

\begin{theorem}[Phase real-completion exit NameCert obligations]
\label{thm:phase-real-completion-exit-namecert-obligations}
For the carrier of \autoref{def:phase-real-completion-exit-carrier}, the local $\NameCert_{\PhaseRealCompletionExitUp}$ surface consists exactly of carrier admission for $E=(D,S,Q,R,B,H,C,P,N)$, dyadic-face exposure through $D$, stream-window exposure through $S$, regular-rational handoff through $Q$, real-seal exposure through $R$, exact-boundary exposure through $B$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier. Its arithmetic, observation, regularity, real-seal, and exact-boundary faces are exactly $D,S,Q,R,B$.

The four local read faces are ordered by the continuation row $C$: the dyadic ledger is read together with the stream windows, the regular-rational handoff is reached from those rows, and the real seal is exposed only after the handoff has been fixed. The boundary row $B$ then records exactness for the same route.

The rows $H,P,N$ transport, source, and name that finite surface. Since the carrier lists no further coordinate, the displayed rows exhaust the local NameCert obligations.
\end{proof}
\leanchecked{BEDC.Derived.PhaseRealCompletionExitUp.PhaseRealCompletionExitTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Phase real-completion exit noncollapse boundary]
\label{thm:phase-real-completion-exit-noncollapse-boundary}
Every accepted $\PhaseRealCompletionExitUp$ consumer receives the dyadic face $D$, stream face $S$, regular face $Q$, real face $R$, exact-boundary face $B$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$ as distinct displayed rows. No accepted consumer may fuse the four local faces into a single bridge token, completeness token, quotient equality, selected limit, or host real without leaving the carrier.
\end{theorem}
\begin{proof}
Start from \autoref{def:phase-real-completion-exit-carrier}. The rows $D,S,Q,R$ occupy separate carrier coordinates, and the exact-boundary row $B$ reports on the same route rather than replacing those coordinates.

Apply \autoref{thm:phase-real-completion-exit-namecert-obligations}. Any accepted read must expose each local face before the local naming row is consumed, while $H$ and $C$ preserve componentwise transport and replay.

A fused bridge or completeness token would require a coordinate identifying the dyadic, stream, regular, and real faces as one object. No such coordinate appears in $E$, and the boundary row explicitly withholds ambient completion, quotient equality, and selected-limit data.
\end{proof}

\begin{theorem}[Phase real-completion exit objectwise face criterion]
\label{thm:phase-real-completion-exit-objectwise-face-criterion}
An accepted $\PhaseRealCompletionExitUp$ exit read is admissible precisely when it exposes the dyadic face $D$, stream face $S$, regular-rational face $Q$, real-seal face $R$, exact-boundary face $B$, and the support rows $H,C,P,N$ as separate finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. No aggregate bridge token, quotient-stream equality, selected limit, host completeness row, or fused formalstatus row may replace any one of these faces.
\end{theorem}
\begin{proof}
The forward direction is read from the carrier of \autoref{def:phase-real-completion-exit-carrier}. Its visible coordinates are $D,S,Q,R,B,H,C,P,N$, so an admissible exit read has a finite row for each local face and for each support route.

For the reverse direction, apply \autoref{thm:phase-real-completion-exit-namecert-obligations}. Once those rows are separately exposed, the local $\NameCert_{\PhaseRealCompletionExitUp}$ surface has the dyadic, stream, regular-rational, real-seal, exact-boundary, transport, replay, provenance, and naming obligations it requires.

The exclusions follow from \autoref{thm:phase-real-completion-exit-noncollapse-boundary}. The five sibling references in the chapter introduction supply the objectwise dependency surface: dyadic arithmetic, stream observation, regular rational handoff, real sealing, and exact boundary are imported as distinct faces, so none of the listed aggregate tokens can stand in for them inside the accepted exit read.
\end{proof}

\begin{theorem}[Phase real-completion exit facewise audit obligation]
\label{thm:phase-real-completion-exit-facewise-audit-obligation}
An accepted $\PhaseRealCompletionExitUp$ packet $E=(D,S,Q,R,B,H,C,P,N)$ can satisfy the Phase Real and Completion exit obligation only by separately exposing the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$ rows. Each of the four roadmap faces keeps its own local closurestatus and formalstatus record: dyadic arithmetic through $D$, stream observation through $S$, regular rational handoff through $Q$, and real-completion exit through the paired $R,B$ rows. No bridge token, fused completion row, quotient equality, selected limit, or host-completeness certificate may discharge those records jointly.
\end{theorem}
\begin{proof}
Begin with the objectwise criterion of \autoref{thm:phase-real-completion-exit-objectwise-face-criterion}. It says that an admissible exit read exposes $D,S,Q,R,B,H,C,P,N$ as separate finite rows, so the audit obligation cannot be discharged by a single aggregate report.

The noncollapse boundary in \autoref{thm:phase-real-completion-exit-noncollapse-boundary} rules out the only possible shortcuts: bridge tokens, completion tokens, quotient equalities, selected limits, and host real data have no carrier coordinate in $E$. Thus any accepted audit must still display the dyadic, stream, regular, real, and exact-boundary faces before the local naming row is read.

The sibling chapters named in the introduction supply the local records that the audit reads. The dyadic face is governed by \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream face by \autoref{ch:concrete-instances-streamname-namecert}, the regular rational handoff by \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-completion exit face by \autoref{ch:concrete-instances-real-namecert} together with \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}. The minimum-blocker result \autoref{thm:real-l10-face-audit-minimum-blocker} records why those local reports cannot be collapsed into one completion-facing audit.

Consequently the phase-exit packet can pass the roadmap audit only by exposing the five object rows while preserving the four local closurestatus and formalstatus records that produced them.
\end{proof}

\begin{theorem}[Phase real-completion exit obligation surface package]
\label{thm:phase-real-completion-exit-obligation-surface-package}
For any accepted $\PhaseRealCompletionExitUp$ packet, the obligation surface is exactly the carrier row $E=(D,S,Q,R,B,H,C,P,N)$, the five object faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\PhaseRealCompletionExitUp}$ row. No quotient stream, selected limit, host-completeness row, or fused bridge token discharges that surface.
\end{theorem}
\begin{proof}
Apply \autoref{thm:phase-real-completion-exit-namecert-obligations} to identify the displayed rows of the local $\NameCert_{\PhaseRealCompletionExitUp}$ surface. The carrier contributes exactly $E=(D,S,Q,R,B,H,C,P,N)$, with $D,S,Q,R,B$ read as the five object faces and $H,C,P,N$ read as transport, replay, provenance, and naming rows.

Apply \autoref{thm:phase-real-completion-exit-objectwise-face-criterion} and \autoref{thm:phase-real-completion-exit-facewise-audit-obligation}. These results force the accepted exit read and the audit read to keep $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$ as separate finite faces before the local naming row is consumed.

Finally apply \autoref{thm:phase-real-completion-exit-noncollapse-boundary}. It excludes the shortcut coordinates named in the statement, so quotient streams, selected limits, host-completeness rows, and fused bridge tokens cannot discharge the obligation surface.
\end{proof}

\begin{theorem}[Phase real-completion exit boundary-witness balance]
\label{thm:phase-real-completion-exit-boundary-witness-balance}
For every accepted $\PhaseRealCompletionExitUp$ packet $E=(D,S,Q,R,B,H,C,P,N)$, any Real-completion exit consumer that reads the exact-boundary face $B$ reads it over the same finite route $D,S,Q,R,H,C,P,N$. A witness-ledger consumer or exact-boundary consumer therefore cannot replace that route by quotient stream equality, selected limits, countable choice, host real equality, ambient completeness, or a detached completion witness.
\end{theorem}
\leanchecked{BEDC.Derived.PhaseRealCompletionExitUp.PhaseRealCompletionExitUp\_boundary\_witness\_balance}
\begin{proof}
Project the carrier of \autoref{def:phase-real-completion-exit-carrier}. The boundary row $B$ is displayed as the exact-boundary face of the same packet whose route rows are $D,S,Q,R,H,C,P,N$.

Apply \autoref{thm:phase-real-completion-exit-namecert-obligations}. It exposes the dyadic, stream, regular-rational, real-seal, exact-boundary, transport, replay, provenance, and local naming rows as the complete NameCert surface for the accepted packet.

The noncollapse boundary \autoref{thm:phase-real-completion-exit-noncollapse-boundary} and the objectwise face criterion \autoref{thm:phase-real-completion-exit-objectwise-face-criterion} prevent any fused completion token from standing in for those rows. The facewise audit obligation \autoref{thm:phase-real-completion-exit-facewise-audit-obligation} then identifies the five sibling records as the separate $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$ faces of the same exit read.

The witness endpoint is the ledger surface of \autoref{ch:concrete-instances-cauchy-witness-ledger-namecert}, and the boundary endpoint is \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}. Since both endpoints are consumed through the displayed route, a consumer that asks for quotient stream equality, selected limits, countable choice, host real equality, ambient completeness, or a detached completion witness has left the accepted $\PhaseRealCompletionExitUp$ carrier.
\end{proof}

\begin{theorem}[Phase real-completion exit terminal-envelope exactness]
\label{thm:phase-real-completion-exit-terminal-envelope-exactness}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. Any terminal Real-completion exit consumer that uses the $\FiniteRealRouteCommitmentUp$ route of \autoref{thm:finite-real-route-commitment-budget-nonescape} and the $\RealSealAdmissionCutUp$ route of \autoref{thm:real-seal-admission-cut-route-nonescape} is exhausted by one finite envelope: the dyadic face $D$, the StreamName face $S$, the RegSeqRat face $Q$, the Real seal $R$, the exact-boundary face $B$, the route-commitment budget, window, readback, seal, and handoff rows, the admission-cut order, and the shared transport, replay, provenance, and naming rows $H,C,P,N$. In particular, such a consumer has no coordinate for quotient streams, selected limits, countable choice, host real equality, ambient completeness, or detached completion witnesses.
\end{theorem}
\begin{proof}
First apply the boundary-witness balance of \autoref{thm:phase-real-completion-exit-boundary-witness-balance}. It says that the exact-boundary face $B$ is read over the same finite route $D,S,Q,R,H,C,P,N$ and cannot be replaced by quotient stream equality, selected limits, countable choice, host real equality, ambient completeness, or a detached completion witness.

Next apply the route commitment theorem \autoref{thm:finite-real-route-commitment-budget-nonescape}. Any Real-facing or CauchyLimitSeal-facing consumer of the route commitment must display the budget, StreamName window, RegSeqRat readback, RealUp seal, and handoff rows before it reaches the terminal seal surface.

Then apply the admission-cut theorem \autoref{thm:real-seal-admission-cut-route-nonescape}. It forces the same terminal read to pass through the ordered finite boundary from request to budget, window, readback, seal, and handoff, so the consumer cannot bypass the budget or window side of the route.

Finally return to the carrier inventory in \autoref{def:phase-real-completion-exit-carrier}. Its only displayed exit coordinates are $D,S,Q,R,B,H,C,P,N$, with the route-commitment and admission-cut rows read through the named sibling theorems. Therefore the terminal consumer is exhausted by the stated finite envelope, and every listed completion principle outside that envelope remains unavailable.
\end{proof}

\begin{theorem}[Phase real-completion exit scoped primitive dependency binding]
\label{thm:phase-real-completion-exit-scoped-primitive-dependency-binding}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. Its scoped primitive dependency surface is exactly the conjunction of the $\DyadicRatCoreUp$ face $D$, the $\StreamNameUp$ face $S$, the $\RegSeqRatUp$ face $Q$, the $\RealUp$ seal $R$, the $\RealCompletionExactBoundaryUp$ face $B$, and the support rows $H,C,P,N$, with each row read through the displayed $\BHist$, componentwise $\hsame$, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\PhaseRealCompletionExitUp}$ coordinates. No quotient stream, selected limit, host equality, ambient completeness principle, fused bridge token, or single aggregate formalstatus row lies in the scope of this accepted packet.
\end{theorem}
\begin{proof}
First apply \autoref{thm:phase-real-completion-exit-objectwise-face-criterion} and \autoref{thm:phase-real-completion-exit-facewise-audit-obligation}. They identify the accepted exit read with the separate finite rows $D,S,Q,R,B,H,C,P,N$ and force the dyadic, stream, regular-rational, real-seal, and exact-boundary reports to remain objectwise visible.

Next apply \autoref{thm:phase-real-completion-exit-boundary-witness-balance} and \autoref{thm:phase-real-completion-exit-terminal-envelope-exactness}. The boundary row $B$ and every terminal consumer are read over the same displayed route, so the support rows $H,C,P,N$ are not optional decorations; they are the transport, replay, provenance, and naming coordinates through which the primitive faces are consumed.

Finally use the sibling exact-boundary scope in \autoref{thm:real-completion-exact-boundary-scoped-obligation-closure} together with the terminal route of \autoref{thm:real-completion-exact-boundary-terminal-consumer-exactness}. These results close the exact-boundary side over the same four-face route and exclude quotient streams, selected limits, host equality, ambient completeness, bridge rows, and aggregate formalstatus witnesses from the accepted dependency surface. The displayed conjunction is therefore the whole scoped primitive binding for $E$.
\end{proof}

\begin{theorem}[Phase real-completion exit status-token product]
\label{thm:phase-real-completion-exit-status-token-product}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. The status read of $\PhaseRealCompletionExitUp$ is the product of the local records of $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$. Each face keeps its own closurestatus, formalstatus, leantarget, and bridgestatus rows, read from the corresponding local chapter boundary. The phase-exit packet creates no aggregate formalstatus token, aggregate bridge row, aggregate Lean target, quotient stream, selected limit, or host-completeness row.
\end{theorem}
\begin{proof}
Start with the facewise audit obligation in \autoref{thm:phase-real-completion-exit-facewise-audit-obligation}. It forces every accepted packet to expose the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$ rows separately before any local naming row is consumed.

Next apply the noncollapse boundary of \autoref{thm:phase-real-completion-exit-noncollapse-boundary} and the scoped primitive binding of \autoref{thm:phase-real-completion-exit-scoped-primitive-dependency-binding}. These results keep the carrier coordinates $D,S,Q,R,B,H,C,P,N$ objectwise visible and exclude fused bridge tokens, quotient streams, selected limits, host-completeness rows, and single aggregate formalstatus rows from the accepted dependency surface.

Finally read the Real L10 status lookup and pullback through \autoref{thm:real-l10-four-face-status-lookup} and \autoref{thm:real-l10-current-phase-exit-audit-pullback}. They state that the dyadic, stream, regular, and real faces contribute their own closurestatus, formalstatus, leantarget, and bridgestatus rows by reference rather than by cached aggregate status data. The exact-boundary row $B$ is the fifth face of the same displayed $\PhaseRealCompletionExitUp$ packet, so the phase status read is precisely the five-face product and nothing else.
\end{proof}

\begin{theorem}[Phase real-completion exit local status gate]
\label{thm:phase-real-completion-exit-local-status-gate}
An accepted $\PhaseRealCompletionExitUp$ packet may serve as the Phase Real and Completion exit gate only when the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$ faces are each read through their own local closurestatus, formalstatus, leantarget, and bridgestatus rows. No aggregate formalstatus token, fused bridge token, quotient stream equality, selected limit, host completeness row, or ambient completion premise can discharge this gate.
\end{theorem}
\begin{proof}
Apply \autoref{thm:phase-real-completion-exit-status-token-product}. It identifies the accepted status read as the product of the five local records and states that the phase-exit packet contributes no aggregate formalstatus row, bridge row, Lean target, quotient stream, selected limit, or host-completeness row.

Next apply \autoref{thm:phase-real-completion-exit-facewise-audit-obligation}. The audit read must expose the dyadic, stream, regular-rational, real, and exact-boundary faces separately before the local naming row can be consumed, so an exit gate cannot be accepted from a cached status summary.

Finally combine \autoref{thm:phase-real-completion-exit-noncollapse-boundary} with \autoref{thm:phase-real-completion-exit-scoped-primitive-dependency-binding}. These results keep the primitive scope at the displayed rows $D,S,Q,R,B,H,C,P,N$ and reject fused bridge tokens, quotient equalities, selected limits, host completeness, ambient completion data, and single aggregate formalstatus witnesses. Therefore the only accepted gate is the local five-face status read stated above.
\end{proof}

\begin{theorem}[Phase real-completion exit audit-clean threshold]
\label{thm:phase-real-completion-exit-audit-clean-threshold}
An accepted $\PhaseRealCompletionExitUp$ packet serves the current local packet audit threshold exactly when the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$ faces are separately read through their own local closurestatus and formalstatus records, and each of the five verification-axis records is at least $\auditCleanV$. The $\RealCompletionExactBoundaryUp$ row is the same-route exact-boundary witness inside this packet audit, while \autoref{thm:phase-real-completion-exit-four-object-threshold-projection} is the roadmap-facing projection that reads only the four roadmap objects. The threshold is not witnessed by an aggregate bridge token, quotient stream, selected limit, host completeness row, or single fused formalstatus row.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:phase-real-completion-exit-local-status-gate}. Any accepted local packet audit must read the dyadic, stream, regular-rational, real, and exact-boundary faces through their own local status rows.

Next apply \autoref{thm:phase-real-completion-exit-status-token-product} and \autoref{thm:phase-real-completion-exit-facewise-audit-obligation}. The status read is the product of five local records, and the packet audit read must expose those records before the local naming row is consumed. Thus the local $\PhaseRealCompletionExitUp$ packet audit can be met only by five objectwise verification-axis records, each at least $\auditCleanV$.

For the reverse direction, suppose the five faces have been read separately through their local closurestatus and formalstatus records, and each record is at least $\auditCleanV$. The local status gate supplies the required Phase Real and Completion exit read, while the status-token product keeps the five records in the same accepted $\PhaseRealCompletionExitUp$ packet.

Finally apply \autoref{thm:phase-real-completion-exit-noncollapse-boundary} and read the roadmap-facing projection through \autoref{thm:phase-real-completion-exit-four-object-threshold-projection}. The noncollapse result rejects aggregate bridge tokens, quotient streams, selected limits, host completeness, and single fused formalstatus rows; the projection states that the boundary row remains a same-route exact-boundary witness rather than a fifth roadmap object.
\end{proof}

\begin{theorem}[Phase real-completion exit classifier-envelope admission]
\label{thm:phase-real-completion-exit-classifier-envelope-admission}
Let $\mathcal P$ be an accepted finite Real L10 public classifier packet satisfying the public classifier audit envelope of \autoref{thm:real-l10-public-classifier-audit-envelope}. Then $\mathcal P$ is accepted as a $\PhaseRealCompletionExitUp$ classifier-envelope read exactly when its visible exit rows are the five face records
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp,\quad \RealCompletionExactBoundaryUp
$$
together with displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support, and those five faces satisfy the audit-clean threshold of \autoref{thm:phase-real-completion-exit-audit-clean-threshold}. No aggregate formalstatus row, aggregate bridge token, quotient stream, selected limit, host completeness row, metric-completeness datum, or countable-choice row is part of the accepted classifier-envelope read.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-l10-public-classifier-audit-envelope}. The Real L10 classifier packet is reduced to the four displayed public faces: local tightness rows, pairwise terminal classifier equivalences, triple common-tail cocycle data, and the objectwise closurestatus records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. The same theorem excludes aggregate route-level $\formalstatus$, aggregate bridge rows, quotient stream equality, selected limits, countable-choice data, metric-completeness data, and $\RealUp$ completeness claims from the classifier envelope.

Next apply \autoref{thm:phase-real-completion-exit-audit-clean-threshold}. A $\PhaseRealCompletionExitUp$ packet can serve the exit threshold only when the four Real L10 faces are read together with the $\RealCompletionExactBoundaryUp$ face, and each of the five local verification-axis records is at least $\auditCleanV$. The exact-boundary row is therefore the fifth visible face required to consume the four-face Real classifier envelope inside the phase-exit carrier.

Finally use \autoref{thm:phase-real-completion-exit-status-token-product} and \autoref{thm:phase-real-completion-exit-noncollapse-boundary}. They keep the five face records attached to their own local status tokens and keep $D,S,Q,R,B,H,C,P,N$ visible as separate finite rows. Thus an accepted classifier-envelope read is precisely the displayed five-face packet with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support, while the excluded aggregate, quotient, choice, limit, host-completeness, and metric-completeness rows remain outside the carrier.
\end{proof}

\begin{theorem}[Phase real-completion exit four-object boundary lift]
\label{thm:phase-real-completion-exit-four-object-boundary-lift}
An accepted four-object Phase Real and Completion read through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ is admitted as a $\PhaseRealCompletionExitUp$ read exactly when the $\RealCompletionExactBoundaryUp$ face is supplied over the same finite route. The resulting packet has displayed faces $D,S,Q,R,B,H,C,P,N$ and exports no quotient stream, selected limit, host equality, ambient completeness, metric-completeness datum, countable-choice row, aggregate formalstatus row, or bridge token.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:streamname-real-completion-four-object-exit-route}. It identifies the consumer-side four-object route through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ before any completion-facing output is accepted.

Next apply \autoref{thm:streamname-real-completion-four-object-terminal-pullback}. The terminal pullback keeps that route finite and objectwise, so a consumer may transport the same route into the real-completion boundary without adding a quotient stream, selected limit, host equality, or ambient completeness premise.

Now use \autoref{thm:real-completion-exact-boundary-four-face-exactness} and \autoref{thm:real-completion-exact-boundary-terminal-route-pullback}. These results supply the $\RealCompletionExactBoundaryUp$ face over the same four-object route and keep the exact-boundary row separate from the dyadic, stream, regular-rational, and real faces.

Finally apply \autoref{thm:phase-real-completion-exit-classifier-envelope-admission}. It admits precisely the five-face classifier-envelope read with displayed $D,S,Q,R,B,H,C,P,N$, $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support, while excluding the quotient, limit, host, completeness, choice, aggregate formalstatus, and bridge rows listed in the statement.
\end{proof}

\begin{theorem}[Phase real-completion exit scoped audit threshold]
\label{thm:phase-real-completion-exit-scoped-audit-threshold}
An accepted $\PhaseRealCompletionExitUp$ packet serves the local phase-exit packet audit exactly when the four roadmap faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ are read through separate local closurestatus and formalstatus records whose closure-axis entries are at least $\scopedClosure$ and whose verification-axis entries are at least $\auditCleanV$, while the $\RealCompletionExactBoundaryUp$ face is read as a same-route exact-boundary witness with its own local status. No aggregate formalstatus row, bridge token, quotient stream, selected limit, host completeness row, or fused completion witness may discharge the packet audit; \autoref{thm:phase-real-completion-exit-four-object-threshold-projection} gives the roadmap-facing four-object read.
\end{theorem}
\begin{proof}
First apply \autoref{thm:phase-real-completion-exit-status-token-product} and \autoref{thm:phase-real-completion-exit-local-status-gate}. They identify the accepted phase-exit status read as the product of the five visible local records and require the dyadic, stream, regular-rational, real, and exact-boundary faces to remain separate through their own closurestatus and formalstatus rows.

Next apply \autoref{thm:phase-real-completion-exit-audit-clean-threshold}. It supplies the verification-axis side of the local packet audit: the four roadmap faces meet $\auditCleanV$ inside the packet, and the exact-boundary face carries its own audit-clean same-route witness status without becoming a roadmap object. Neither a fused formalstatus row nor an external completion witness can replace those local records.

Now use \autoref{thm:phase-real-completion-exit-classifier-envelope-admission}. The accepted classifier-envelope read adds the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support, while still excluding aggregate bridge tokens, quotient streams, selected limits, host-completeness rows, metric-completeness data, and countable-choice rows.

Finally read the two axes independently through \autoref{sec:roadmap-closure-and-verification-axes} and the Phase Real and Completion criterion in \autoref{sec:roadmap-near-term-phases}, then apply the projection of \autoref{thm:phase-real-completion-exit-four-object-threshold-projection}. The roadmap gate is therefore met by the four local roadmap records whose closure-axis entries are at least $\scopedClosure$ and whose verification-axis entries are at least $\auditCleanV$; the fifth packet face is the exact-boundary witness on the same route, with no aggregate row or fused completion witness standing in for the displayed faces.
\end{proof}

\begin{theorem}[Phase real-completion exit four-object threshold projection]
\label{thm:phase-real-completion-exit-four-object-threshold-projection}
An accepted $\PhaseRealCompletionExitUp$ packet satisfies the roadmap Phase Real and Completion exit criterion precisely when its $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ face records are each read through separate local closurestatus and formalstatus rows whose closure-axis entries are at least $\scopedClosure$ and whose verification-axis entries are at least $\auditCleanV$. The $\RealCompletionExactBoundaryUp$ face is required only as the same-route exact-boundary witness over those four records; it cannot be counted as a fifth roadmap object, aggregate formalstatus row, bridge token, quotient stream, selected limit, host-completeness row, or fused completion witness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:phase-real-completion-exit-four-object-boundary-lift}. It identifies the boundary face as a $\RealCompletionExactBoundaryUp$ witness supplied over the same finite route through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, without adding quotient-stream equality, selected limits, host equality, ambient completeness, aggregate formalstatus data, or bridge tokens.

Next apply \autoref{thm:phase-real-completion-exit-scoped-audit-threshold}. The local $\PhaseRealCompletionExitUp$ gate still reads five visible faces inside the packet, so the exact-boundary witness must carry its own local status row when the packet is audited.

Now use \autoref{thm:phase-real-completion-exit-status-token-product} together with \autoref{thm:phase-real-completion-exit-noncollapse-boundary}. These results keep the status rows objectwise and prevent the boundary row from collapsing the four roadmap records into an aggregate token, quotient stream, selected limit, host-completeness row, or fused completion witness.

Finally read the roadmap criterion in \autoref{sec:roadmap-near-term-phases} through the two-axis discipline of \autoref{sec:roadmap-closure-and-verification-axes}. The roadmap exit criterion names the four objects $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$; the fifth face is the exact-boundary witness that lets the accepted packet expose the same route, not an additional roadmap object.
\end{proof}

\begin{theorem}[Phase real-completion exit public gate certificate]
\label{thm:phase-real-completion-exit-public-gate-certificate}
An accepted $\PhaseRealCompletionExitUp$ packet is the public paper-side gate certificate for the current Phase Real and Completion criterion exactly when the four roadmap faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ satisfy their local scoped-closure records and their local audit-clean records. The $\RealCompletionExactBoundaryUp$ face is used only as the same-route exact-boundary witness over those four records. The certificate exports no aggregate formalstatus row, aggregate bridge token, quotient stream, selected limit, host completeness row, metric-completeness row, countable-choice row, or fused completion witness.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:phase-real-completion-exit-four-object-threshold-projection}. Any accepted packet that satisfies the Phase Real and Completion exit criterion must expose the four roadmap faces through separate local closurestatus and formalstatus rows, with closure-axis entries at least $\scopedClosure$ and verification-axis entries at least $\auditCleanV$. The same theorem keeps $\RealCompletionExactBoundaryUp$ as a boundary witness over the same route rather than as another roadmap object.

Next apply \autoref{thm:phase-real-completion-exit-scoped-audit-threshold}. It identifies the gate consumed by the chapter as the visible status read of the phase-exit packet, so the public certificate cannot be obtained from a cached route summary or from a single fused completion row.

For the reverse direction, suppose the four roadmap faces have their local scoped-closure and audit-clean records, and suppose the accepted packet supplies $\RealCompletionExactBoundaryUp$ only as the same-route exact-boundary witness. Apply \autoref{thm:phase-real-completion-exit-status-token-product} to repack those records as the product read of the accepted $\PhaseRealCompletionExitUp$ carrier.

Finally read the product through \autoref{sec:roadmap-closure-and-verification-axes}. The roadmap discipline keeps the paper closure axis, Lean verification axis, and bridge status axis independent. Therefore the certificate can be cited as a public paper-side gate while still refusing aggregate formalstatus rows, bridge tokens, quotient streams, selected limits, host completeness, metric completeness, countable choice, and fused completion witnesses.
\end{proof}

\begin{theorem}[Real L10 terminal envelope phase-exit equivalence]
\label{thm:real-l10-terminal-envelope-phase-exit-equivalence}
An accepted Real L10 terminal envelope satisfying \autoref{thm:real-l10-terminal-order-window-witness-envelope} is equivalent to the public $\PhaseRealCompletionExitUp$ gate certificate of \autoref{thm:phase-real-completion-exit-public-gate-certificate} exactly when the $\RealCompletionExactBoundaryUp$ face is supplied as the same-route exact-boundary witness over the ordered route $\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp$, and when the four roadmap faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ are read only through their local scoped-closure and audit-clean records. Under this equivalence the Real envelope contributes the ordered L10 route and finite-window witness rows, while the phase-exit certificate contributes the same-route exact-boundary face and the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support. Neither side exports an aggregate formalstatus row, fused bridge token, quotient stream, selected limit, host completeness row, metric-completeness datum, countable-choice row, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
For the forward direction, start from the accepted Real L10 terminal envelope. Apply \autoref{thm:real-l10-terminal-order-window-witness-envelope} to read the ordered route and finite-window witness rows, then apply \autoref{thm:real-l10-terminal-envelope-status-locality} to keep the status evidence objectwise over the four local roadmap faces.

Next supply the same-route $\RealCompletionExactBoundaryUp$ face and apply \autoref{thm:phase-real-completion-exit-four-object-boundary-lift}. The boundary lift turns the ordered Real route into the five-face phase-exit read without adding quotient stream equality, selected limits, host completeness, metric-completeness data, countable choice, aggregate formalstatus data, or a fused bridge token.

Finally apply \autoref{thm:phase-real-completion-exit-public-gate-certificate}. The four roadmap faces already carry their local scoped-closure and audit-clean records, and the exact-boundary face is used only as the same-route witness, so the public $\PhaseRealCompletionExitUp$ gate certificate is obtained.

For the reverse direction, suppose the public $\PhaseRealCompletionExitUp$ gate certificate is displayed with the same-route $\RealCompletionExactBoundaryUp$ face and the four local roadmap records. Apply \autoref{thm:phase-real-completion-exit-public-gate-certificate} to recover the objectwise public gate data, then apply \autoref{thm:phase-real-completion-exit-four-object-threshold-projection} to project away the boundary face from the roadmap count while preserving the ordered four-object route.

Now apply \autoref{thm:real-l10-formal-boundary-exit-certificate}. The projected dyadic, stream, regular-rational, and Real faces, together with the displayed finite-window and support rows, recover the ordered Real terminal envelope. The same exclusion list remains in force: aggregate formalstatus rows, fused bridge tokens, quotient streams, selected limits, host completeness, metric-completeness data, countable-choice rows, and ambient $\RealUp$ completeness are not envelope data.
\end{proof}

\begin{theorem}[Phase real-completion exit bridge-pending axis separation]
\label{thm:phase-real-completion-exit-bridge-pending-axis-separation}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. Suppose the four roadmap faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ are read through their own local scoped-closure and audit-clean records, and suppose the $\RealCompletionExactBoundaryUp$ face is supplied on the same finite route. Then the public Phase Real and Completion exit gate remains admitted even when some component bridge rows stand at \texttt{paperBridge} and other component bridge rows stand at \texttt{bridgeChecked}. The packet reads bridge rows objectwise from the component boundaries and exports no aggregate $\bridgeCheckedV$ row, no fused standard bridge token, no quotient stream, no selected limit, and no host-completeness row.
\end{theorem}
\begin{proof}
Apply \autoref{thm:phase-real-completion-exit-public-gate-certificate}. It identifies the public phase-exit gate with the four roadmap faces carrying local scoped-closure and audit-clean records, together with $\RealCompletionExactBoundaryUp$ only as the same-route exact-boundary witness.

Next apply \autoref{thm:phase-real-completion-exit-status-token-product}. The accepted status read is the product of the local face records, so bridge rows are read from their component closurestatus blocks rather than from a single phase-level bridge coordinate.

Use \autoref{thm:phase-real-completion-exit-facewise-consumer-readback} to expose the consumer view. The readback returns the dyadic, stream, regular-rational, real, and exact-boundary faces with the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support rows, while leaving component bridge rows attached to their own objects.

Finally apply \autoref{thm:phase-real-completion-exit-boundary-face-nonsubstitution} and read the result through the two-axis discipline of \autoref{sec:roadmap-closure-and-verification-axes}. A bridge row at \texttt{paperBridge} or \texttt{bridgeChecked} reports a component bridge state; it neither collapses the scoped/audit exit gate nor blocks it once the four roadmap records and same-route exact-boundary witness are present. Hence the accepted packet admits the public gate while refusing an aggregate $\bridgeCheckedV$ export, a fused standard bridge token, a quotient stream, a selected limit, or a host-completeness row.
\end{proof}

\begin{theorem}[Phase real-completion exit component bridge-upgrade invariance]
\label{thm:phase-real-completion-exit-component-bridge-upgrade-invariance}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet satisfying the public Phase Real and Completion gate of \autoref{thm:phase-real-completion-exit-public-gate-certificate}. Suppose one local bridge row among the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, or $\RealCompletionExactBoundaryUp$ faces is replaced by a stronger local bridge record, and suppose the corresponding closurestatus and formalstatus rows remain attached only to that face. Then the public phase-exit projection of $E$ is unchanged: the same four roadmap records, same-route boundary witness, and support rows $H,C,P,N$ are read by the gate. The replacement creates no aggregate $\bridgeCheckedV$ row, no fused standard bridge token, no quotient stream, no selected limit, no host-completeness row, and no ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
First apply \autoref{thm:phase-real-completion-exit-status-token-product}. The status read of the accepted packet is the product of the component records, so strengthening one bridge row changes only that local factor and leaves the other face records objectwise.

Next apply \autoref{thm:phase-real-completion-exit-bridge-pending-axis-separation}. Mixed component bridge states are invisible to the scoped/audit gate once the four roadmap records and same-route $\RealCompletionExactBoundaryUp$ witness are present. Hence the public phase-exit projection is still admitted after the local bridge replacement.

Use \autoref{thm:phase-real-completion-exit-component-status-extraction} to read back the same exported data. The dyadic, stream, regular-rational, and real records remain the four roadmap rows, the boundary face remains the same-route witness, and $H,C,P,N$ remain the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support.

Finally apply \autoref{thm:phase-real-completion-exit-boundary-face-nonsubstitution}. Because the strengthened bridge row stays local to its face, it cannot supply an aggregate $\bridgeCheckedV$ row, fused standard bridge token, quotient stream, selected limit, host-completeness row, or ambient $\RealUp$ completeness claim. Therefore the bridge-status improvement preserves exactly the public phase-exit projection.
\end{proof}

\begin{theorem}[Phase real-completion exit standard-bridge source normal form]
\label{thm:phase-real-completion-exit-standard-bridge-source-normal-form}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. Any standard-bridge candidate for $E$ must take as its source exactly the five displayed face rows $D,S,Q,R,B$, namely $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and the same-route $\RealCompletionExactBoundaryUp$, together with the four displayed support rows $H,C,P,N$, namely $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support. It may not use an aggregate formalstatus row, fused bridge token, quotient stream equality, selected limit, countable-choice row, host-completeness row, metric-completeness datum, or ambient $\RealUp$ completeness coordinate as a source.
\end{theorem}
\begin{proof}
Apply \autoref{thm:phase-real-completion-exit-status-token-product}. It identifies the accepted status read as a product of local face records, so a bridge source must begin from the objectwise packet fields rather than from a route-level formalstatus token.

Next use \autoref{thm:phase-real-completion-exit-facewise-consumer-readback}. The consumer readback exposes precisely the dyadic, stream, regular-rational, real, and exact-boundary face rows with the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support. These are therefore the rows a bridge candidate can consume.

Now apply \autoref{thm:phase-real-completion-exit-support-row-nonsubstitution}. The support rows cannot be replaced by a fused support token, external completion datum, quotient equality, selected limit, countable-choice row, host-completeness row, or metric-completeness row.

Finally apply \autoref{thm:phase-real-completion-exit-bridge-pending-axis-separation}. Bridge status remains a component report rather than a source normalizer. Hence the later standard bridge must target the displayed five-face and four-support source normal form, with no aggregate formalstatus row, fused bridge token, quotient stream equality, selected limit, countable-choice row, host-completeness row, metric-completeness datum, or ambient $\RealUp$ completeness coordinate supplying the source.
\end{proof}

\begin{theorem}[Phase real-completion exit boundary-face nonsubstitution]
\label{thm:phase-real-completion-exit-boundary-face-nonsubstitution}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. If any one of the four roadmap faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ lacks its own local $\scopedClosure$ closure record and local $\auditCleanV$ verification record, or if the $\RealCompletionExactBoundaryUp$ face is not supplied on the same finite route, then $E$ fails the current Phase Real and Completion exit gate. No aggregate formalstatus row, aggregate bridge token, quotient stream, selected limit, host-completeness row, metric-completeness row, countable-choice row, or fused completion witness can replace the missing local face.
\end{theorem}
\begin{proof}
Apply \autoref{thm:phase-real-completion-exit-public-gate-certificate}. It identifies the public paper-side gate certificate with four roadmap faces that already carry their local scoped-closure and audit-clean records, together with $\RealCompletionExactBoundaryUp$ only as a same-route exact-boundary witness.

Next apply \autoref{thm:phase-real-completion-exit-four-object-threshold-projection} and \autoref{thm:phase-real-completion-exit-scoped-audit-threshold}. These theorems separate the four roadmap objects from the boundary witness while still requiring every visible face in the accepted packet to be read through its own local closurestatus and formalstatus rows.

Finally use \autoref{thm:phase-real-completion-exit-status-token-product} and \autoref{thm:phase-real-completion-exit-noncollapse-boundary}. The status product is objectwise, and the noncollapse boundary excludes aggregate formalstatus rows, bridge tokens, quotient streams, selected limits, host-completeness rows, metric-completeness rows, countable-choice rows, and fused completion witnesses from the carrier. Therefore a missing roadmap face record, or a boundary face not supplied on the same finite route, leaves the exit gate undischarged.
\end{proof}

\begin{theorem}[Phase real-completion exit facewise consumer readback]
\label{thm:phase-real-completion-exit-facewise-consumer-readback}
An accepted $\PhaseRealCompletionExitUp$ consumer reads back exactly the five local face records $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$, together with the supporting $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ rows. Conversely, any consumer that displays precisely those five local records and those support rows, with $\RealCompletionExactBoundaryUp$ supplied on the same finite route as the four roadmap faces, is admitted by the existing classifier-envelope and public-gate certificate. The readback exports no aggregate formalstatus row, bridge token, quotient stream, selected limit, host-completeness row, metric-completeness data, countable-choice row, or fused completion witness.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:phase-real-completion-exit-classifier-envelope-admission}. It identifies the accepted classifier-envelope read with the displayed carrier rows $D,S,Q,R,B,H,C,P,N$, so every accepted consumer receives the dyadic, stream, regular-rational, real, and exact-boundary faces together with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support.

Next use \autoref{thm:phase-real-completion-exit-four-object-threshold-projection} and \autoref{thm:phase-real-completion-exit-boundary-face-nonsubstitution}. These results keep $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as the four roadmap records and require $\RealCompletionExactBoundaryUp$ to remain a same-route boundary witness rather than an aggregate row or substitute face.

For the reverse direction, suppose a consumer displays exactly those five local records and support rows. Apply \autoref{thm:phase-real-completion-exit-classifier-envelope-admission} to place the displayed rows inside the classifier envelope, then apply \autoref{thm:phase-real-completion-exit-public-gate-certificate} to read the same data as the public phase-exit gate certificate.

Finally, the nonsubstitution theorem excludes the omitted rows from both directions of the readback. Thus quotient streams, selected limits, host completeness, metric completeness, countable choice, bridge tokens, aggregate formalstatus data, and fused completion witnesses cannot be consumed in place of the five displayed face records and their local support.
\end{proof}

\begin{theorem}[Phase real-completion exit status-face product injectivity]
\label{thm:phase-real-completion-exit-status-face-product-injectivity}
For accepted $\PhaseRealCompletionExitUp$ packets, the public phase-exit gate read is determined exactly by the status-face product consisting of the four roadmap status records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, together with the same-route $\RealCompletionExactBoundaryUp$ witness and the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support. Equivalently, two accepted packet reads give the same public phase-exit gate certificate precisely when they have the same four roadmap status records, the same same-route exact-boundary witness, and the same displayed support rows. The comparison exports no aggregate formalstatus row, bridge token, quotient stream, selected limit, host-completeness row, metric-completeness data, countable-choice row, or fused completion witness.
\end{theorem}
\begin{proof}
For the forward direction, suppose two accepted packet reads have the same four roadmap status records, the same same-route $\RealCompletionExactBoundaryUp$ witness, and the same displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support. Apply \autoref{thm:phase-real-completion-exit-status-token-product}. It identifies the accepted status read with the objectwise product of those local records, so the two reads have the same status-face product.

Next apply \autoref{thm:phase-real-completion-exit-public-gate-certificate}. The public certificate consumes exactly the four roadmap faces with their local scoped-closure and audit-clean records, while the boundary face is used only as the same-route witness over those faces.

Use \autoref{thm:phase-real-completion-exit-boundary-face-nonsubstitution} to rule out alternative witnesses. Since no aggregate row, bridge token, quotient stream, selected limit, host-completeness row, or fused completion witness may replace a missing local face, the shared status-face product determines a single public gate read.

For the reverse direction, suppose the two accepted reads have the same public phase-exit gate certificate. Apply \autoref{thm:phase-real-completion-exit-facewise-consumer-readback}. The certificate reads back exactly the five local face records and the supporting $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. Applying the status-token product again identifies those readback rows with the four roadmap status records plus the same-route exact-boundary witness. Hence equality of the public gate readback gives equality of the displayed status-face product.
\end{proof}

\begin{theorem}[Phase real-completion exit support-row nonsubstitution]
\label{thm:phase-real-completion-exit-support-row-nonsubstitution}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. None of the support rows $H,C,P,N$ may be replaced by an aggregate formalstatus row, bridge token, quotient stream, selected limit, host-completeness row, metric-completeness datum, countable-choice row, or fused completion witness. The packet remains admitted only when componentwise $\hsame$ support through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support through $N$ are present together with the displayed $D,S,Q,R,B$ faces.
\end{theorem}
\begin{proof}
Start from the carrier inventory in \autoref{def:phase-real-completion-exit-carrier}. The accepted packet has exactly the displayed rows $D,S,Q,R,B,H,C,P,N$, where $H$ records componentwise $\hsame$, $C$ records displayed $\Cont$ routes, $P$ records $\Pkg$ provenance, and $N$ records the local $\NameCert_{\PhaseRealCompletionExitUp}$ row.

Apply \autoref{thm:phase-real-completion-exit-scoped-primitive-dependency-binding}. Its primitive scope is the same finite row list, so the support rows are load-bearing coordinates rather than annotations attached to an external completion or status object.

Next apply \autoref{thm:phase-real-completion-exit-facewise-consumer-readback}. Any accepted consumer reads back the five local face records together with the supporting $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows. Then use \autoref{thm:phase-real-completion-exit-status-face-product-injectivity} to identify equality of the public gate read with equality of that displayed status-face product.

The remaining exclusions are the noncollapse exclusions already enforced by \autoref{thm:phase-real-completion-exit-noncollapse-boundary} and \autoref{thm:phase-real-completion-exit-boundary-face-nonsubstitution}. Aggregate formalstatus rows, bridge tokens, quotient streams, selected limits, host or metric completeness data, countable-choice rows, and fused completion witnesses have no coordinate in $E$ and cannot determine the same support-row product. Therefore $H,C,P,N$ are non-substitutable in every accepted phase-exit packet.
\end{proof}

\begin{theorem}[Phase real-completion exit all support rows necessary]
\label{thm:phase-real-completion-exit-all-support-rows-necessary}
An accepted $\PhaseRealCompletionExitUp$ public gate read is valid exactly when it displays the five face records $D,S,Q,R,B$ for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealCompletionExactBoundaryUp$, together with the support rows $H,C,P,N$ for componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$. The boundary face $B$ must lie on the same finite route as the four roadmap faces $D,S,Q,R$. No aggregate formalstatus row, bridge token, quotient stream, selected limit, host-completeness row, metric-completeness datum, countable-choice row, or fused witness may replace any one of these listed rows.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:phase-real-completion-exit-facewise-consumer-readback}. It identifies every accepted consumer read with exactly the five local face records and the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support rows. The same readback requires $\RealCompletionExactBoundaryUp$ to be supplied on the same finite route as the four roadmap faces.

Next apply \autoref{thm:phase-real-completion-exit-status-face-product-injectivity}. Equality of the public gate read is equality of that status-face product, so deleting or replacing any one coordinate changes the displayed packet rather than preserving the accepted gate.

For the reverse direction, suppose the public read displays exactly $D,S,Q,R,B,H,C,P,N$ with $B$ on the same finite route as $D,S,Q,R$. Apply \autoref{thm:phase-real-completion-exit-public-gate-certificate} to read the four roadmap faces and the exact-boundary witness as the public Phase Real and Completion gate certificate.

Finally apply \autoref{thm:phase-real-completion-exit-support-row-nonsubstitution}. It rules out aggregate formalstatus rows, bridge tokens, quotient streams, selected limits, host or metric completeness data, countable-choice rows, and fused witnesses as replacements for $H,C,P,N$. Together with the facewise readback, this proves that all five face rows and all four support rows are necessary and sufficient for the accepted public gate read.
\end{proof}

\begin{theorem}[Phase real-completion exit current local-status witness]
\label{thm:phase-real-completion-exit-current-local-status-witness}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. Suppose its four roadmap faces read the local records of $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, and suppose its $\RealCompletionExactBoundaryUp$ face is supplied on the same finite route. Then $E$ satisfies the roadmap Phase Real and Completion exit criterion in the current paper state: the four roadmap faces are each at least $\scopedClosure$ on the closure axis and at least $\auditCleanV$ on the verification axis, while the boundary face is the same-route exact-boundary witness. The packet introduces no aggregate formalstatus row, quotient stream, selected limit, host-completeness row, or fused bridge token.
\end{theorem}
\begin{proof}
Read the local component blocks. $\DyadicRatCoreUp$, $\StreamNameUp$, and $\RealUp$ carry $\matureClosure$ with $\bridgeCheckedV$ records, so each is above the required $\scopedClosure$ and $\auditCleanV$ thresholds. $\RegSeqRatUp$ carries $\matureClosure$ with an $\axiomCleanV$ record, which is still above the required $\auditCleanV$ verification threshold for this roadmap gate.

The $\RealCompletionExactBoundaryUp$ block carries the same-route exact-boundary witness with $\matureClosure$ and $\bridgeCheckedV$. This supplies the boundary face required by the accepted packet, but it does not become a fifth roadmap object.

Apply \autoref{thm:phase-real-completion-exit-scoped-audit-threshold} to the accepted packet. The threshold is met by reading the dyadic, stream, regular-rational, real, and exact-boundary faces through their own local closurestatus and formalstatus rows. In particular, the $\RegSeqRatUp$ \texttt{paperBridge} row is harmless here because this gate asks for audit-clean local verification, not for componentwise $\bridgeCheckedV$ status.

Finally apply \autoref{thm:phase-real-completion-exit-four-object-threshold-projection}. It projects the five-face packet read to the four roadmap objects $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with $\RealCompletionExactBoundaryUp$ used only as the same-route boundary witness. The projection therefore discharges the roadmap Phase Real and Completion exit criterion without adding an aggregate formalstatus row, quotient stream, selected limit, host-completeness row, or fused bridge token.
\end{proof}

\begin{theorem}[Phase real-completion exit component-status extraction]
\label{thm:phase-real-completion-exit-component-status-extraction}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet satisfying the current local-status witness of \autoref{thm:phase-real-completion-exit-current-local-status-witness}. Then the roadmap export of $E$ is exactly the four local status records $D,S,Q,R$ for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, together with the same-route $\RealCompletionExactBoundaryUp$ witness $B$ and the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support rows $H,C,P,N$. Conversely, any accepted packet displaying precisely those four local records, the same-route boundary witness, and those support rows satisfies the current Phase Real and Completion exit read. No aggregate formalstatus row, aggregate bridge token, quotient stream, selected limit, countable-choice row, metric-completeness datum, or ambient $\RealUp$ completeness claim is extracted or supplied by this equivalence.
\end{theorem}
\begin{proof}
For the forward direction, start from \autoref{thm:phase-real-completion-exit-current-local-status-witness}. It identifies the accepted packet as meeting the current roadmap exit criterion by the four local records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, while the $\RealCompletionExactBoundaryUp$ face remains only the same-route boundary witness.

Apply \autoref{thm:phase-real-completion-exit-facewise-consumer-readback}. The consumer readback exposes exactly the face rows $D,S,Q,R,B$ and the support rows $H,C,P,N$, namely the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ rows. Then use \autoref{thm:phase-real-completion-exit-all-support-rows-necessary} to see that none of those support coordinates is optional or replaceable.

The terminal-envelope comparison in \autoref{thm:real-l10-terminal-envelope-phase-exit-equivalence} keeps the same data aligned with the Real L10 route. It contributes the route and finite-window witness perspective, but it does not add an aggregate formalstatus row, aggregate bridge token, quotient stream, selected limit, countable choice, metric completeness, or ambient $\RealUp$ completeness coordinate. Therefore the extracted roadmap export is exactly the displayed four-record, one-boundary, four-support packet.

For the reverse direction, suppose an accepted packet displays exactly the four roadmap local records, the same-route $\RealCompletionExactBoundaryUp$ witness, and the support rows $H,C,P,N$. Apply \autoref{thm:phase-real-completion-exit-facewise-consumer-readback} to admit the displayed rows as the public consumer read, and apply \autoref{thm:phase-real-completion-exit-all-support-rows-necessary} to ensure the support rows are present in the required componentwise form.

Finally apply \autoref{thm:phase-real-completion-exit-current-local-status-witness}. The four local records are the roadmap objects, the boundary row is on the same finite route, and the support rows are the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. Hence the packet satisfies the current Phase Real and Completion exit read, with the same refusals of aggregate formalstatus, aggregate bridge token, quotient stream, selected limit, countable choice, metric completeness, and ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Phase real-completion exit roadmap refusal boundary]
\label{thm:phase-real-completion-exit-roadmap-refusal-boundary}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet satisfying the current local-status witness of \autoref{thm:phase-real-completion-exit-current-local-status-witness}. Its roadmap export consists exactly of the four face records $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, the same-route $\RealCompletionExactBoundaryUp$ witness, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\PhaseRealCompletionExitUp}$ support rows. It exports no Dedekind-cut reals, open-cover compactness, Heine-Borel theorem, classical trichotomy, primitive quotient stream, selected limit, countable-choice row, metric-completeness datum, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Start from \autoref{thm:phase-real-completion-exit-current-local-status-witness}. The accepted packet meets the current Phase Real and Completion exit criterion by displaying the four roadmap faces at the required local closure and verification thresholds, while $\RealCompletionExactBoundaryUp$ remains only the same-route exact-boundary witness.

Apply \autoref{thm:phase-real-completion-exit-public-gate-certificate}. The public gate consumes the face records $D,S,Q,R$ and the boundary witness $B$ together with the support rows $H,C,P,N$. Thus the exported carrier is the finite tuple $E=(D,S,Q,R,B,H,C,P,N)$ and the support it displays through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$.

Next apply \autoref{thm:phase-real-completion-exit-boundary-face-nonsubstitution}. The excluded objects in \autoref{sec:roadmap-l10-constructive-constraints} and \autoref{sec:roadmap-off-the-table} have no carrier coordinate in $E$: Dedekind-cut reals, open-cover compactness, Heine-Borel, classical trichotomy, primitive quotient streams, selected limits, countable choice, metric completeness, and ambient $\RealUp$ completeness are not among $D,S,Q,R,B,H,C,P,N$.

Finally read the same packet through the roadmap two-axis discipline of \autoref{sec:roadmap-closure-and-verification-axes}. Closure records and verification records remain attached to the local faces that supply them, so no refused off-table claim can be recovered by promoting a closure row into a verification row, by promoting a verification row into a closure row, or by fusing the component bridge reports into a completion primitive. Hence the accepted packet exports exactly the stated roadmap boundary and nothing stronger.
\end{proof}

\begin{theorem}[Phase real-completion exit zero-source nonsubstitution]
\label{thm:phase-real-completion-exit-zero-source-nonsubstitution}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet satisfying the current local-status witness of \autoref{thm:phase-real-completion-exit-current-local-status-witness}. An accepted $\RegularCauchyZeroUp$ packet may serve as a zero-source consumer witness for the regular-rational and real-facing route only after $E$ already displays the four local status records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, together with the same-route $\RealCompletionExactBoundaryUp$ face $B$. The $\RegularCauchyZeroUp$ packet cannot replace any one of $D,S,Q,R$, cannot supply $B$ as the same-route exact-boundary face, and cannot by itself discharge the roadmap audit-clean threshold for the Phase Real and Completion exit gate.
\end{theorem}
\begin{proof}
Use \autoref{thm:regular-cauchy-zero-mature-closure-package} and \autoref{thm:regular-cauchy-zero-terminal-interface-completeness}. They identify $\RegularCauchyZeroUp$ as a finite zero-source route through its rational zero seed, constant $\StreamNameUp$ window, $\RegSeqRatUp$ zero row, tail-meet rows, and $\RealUp$ seal-facing terminal interface.

That finite route is a valid consumer witness, but it is not a phase-exit status product. Apply \autoref{thm:phase-real-completion-exit-status-token-product}. The phase packet reads the roadmap gate through the objectwise product of $D,S,Q,R$ and the same-route boundary face $B$, with support through $H,C,P,N$.

Next apply \autoref{thm:phase-real-completion-exit-current-local-status-witness}. The roadmap threshold is met by the four local status records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, while $\RealCompletionExactBoundaryUp$ remains the same-route boundary witness rather than a fifth roadmap object or a zero-source replacement.

Finally use \autoref{thm:phase-real-completion-exit-boundary-face-nonsubstitution}. Since the phase gate is objectwise and status-token-local, a nearby zero-source packet has no coordinate that can stand in for a missing dyadic, stream, regular-rational, real, or exact-boundary face, and it cannot turn its terminal route into the audit-clean record required by the phase-exit roadmap gate.
\end{proof}

\begin{definition}[Phase real-completion exit cover-overlap compatibility]
\label{def:phase-real-completion-exit-cover-overlap-compatibility}
Let $\{E_i\}_{i \in I}$ be a finite family of accepted $\PhaseRealCompletionExitUp$ packets with
$$
E_i=(D_i,S_i,Q_i,R_i,B_i,H_i,C_i,P_i,N_i)
$$
on each cover member. The family is cover-overlap compatible when every common finite refinement of two cover members identifies the displayed row restrictions of the four roadmap faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
identifies the same-route $\RealCompletionExactBoundaryUp$ witness, and identifies the support rows $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ carried by $H_i,C_i,P_i,N_i$.

The compatibility datum has no coordinate for quotient streams, selected limits, countable-choice witnesses, metric-completeness data, aggregate formalstatus rows, aggregate bridge tokens, standard bridge rows, or ambient $\RealUp$ completeness claims.
\end{definition}

\begin{theorem}[Phase real-completion exit cover-overlap no-confusion]
\label{thm:phase-real-completion-exit-cover-overlap-no-confusion}
Let $\{E_i\}_{i \in I}$ and $\{F_j\}_{j \in J}$ be cover-overlap compatible finite families of accepted $\PhaseRealCompletionExitUp$ packets for the same public Phase Real and Completion consumer, where
$$
E_i=(D_i,S_i,Q_i,R_i,B_i,H_i,C_i,P_i,N_i)
$$
and
$$
F_j=(D'_j,S'_j,Q'_j,R'_j,B'_j,H'_j,C'_j,P'_j,N'_j).
$$
After passing to a common finite overlap refinement, the two families determine the same public phase-exit gate read if and only if they display the same overlap restrictions of the four roadmap faces, the same-route $\RealCompletionExactBoundaryUp$ witness, and the support rows $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$.

The common gate read recovers exactly those displayed rows. It recovers no quotient stream, selected limit, countable-choice row, metric-completeness datum, aggregate formalstatus row, aggregate bridge token, standard bridge row, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
For the forward direction, assume the two finite families determine the same public phase-exit gate read. Apply \autoref{thm:phase-real-completion-exit-component-status-extraction} to the common read. It extracts exactly the four roadmap faces, the same-route $\RealCompletionExactBoundaryUp$ witness, and the displayed support rows $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$.

Restrict the extracted rows to every common finite overlap refinement. Since both families are cover-overlap compatible in the sense of \autoref{def:phase-real-completion-exit-cover-overlap-compatibility}, the restrictions on the $E$ side and the $F$ side are the same displayed rows. Apply \autoref{thm:phase-real-completion-exit-all-support-rows-necessary} to rule out an omitted support coordinate, so equality of the public gate read forces agreement on every listed overlap row.

For the reverse direction, assume the common refinement displays the same four roadmap face restrictions, the same-route boundary witness, and the same support rows. Apply \autoref{thm:real-four-face-terminal-cover-overlap-compatibility} to identify the terminal four-face $\RealUp$ surface determined by the dyadic, stream, regular-rational, and real rows.

Next apply \autoref{thm:regseqrat-finite-cover-overlap-no-confusion}. The regular-rational audit-threshold read has no second source once the four visible faces and support rows agree on the overlap refinement. With \autoref{thm:phase-real-completion-exit-component-status-extraction}, the two phase-exit packets therefore expose the same public gate read.

All cited comparisons are finite-row comparisons. By \autoref{thm:phase-real-completion-exit-all-support-rows-necessary}, the only support rows available to the public gate are $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$. Thus no quotient stream, selected limit, countable-choice row, metric-completeness datum, aggregate formalstatus row, aggregate bridge token, standard bridge row, or ambient $\RealUp$ completeness claim can be recovered from the overlap comparison.
\end{proof}

\begin{theorem}[Phase real-completion exit finite-cover gate gluing]
\label{thm:phase-real-completion-exit-finite-cover-gate-gluing}
Let $\{E_i\}_{i \in I}$ be a finite cover of compatible accepted $\PhaseRealCompletionExitUp$ packets, with
$$
E_i=(D_i,S_i,Q_i,R_i,B_i,H_i,C_i,P_i,N_i)
$$
on each cover member. Suppose the family is cover-overlap compatible in the sense of \autoref{def:phase-real-completion-exit-cover-overlap-compatibility}. Then the cover glues to one accepted packet whose public read is the same Phase Real and Completion gate if and only if every $E_i$ displays the same four roadmap local status records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, supplies the same-route $\RealCompletionExactBoundaryUp$ witness, and agrees on all overlap restrictions of the support rows $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$.

The glued packet has exactly those four roadmap status records, the same-route boundary witness, and those support rows as its public Phase Real and Completion gate read. It exports no quotient stream, selected limit, countable-choice row, metric-completeness datum, aggregate formalstatus row, aggregate bridge token, standard bridge row, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
For the forward direction, restrict the glued public packet to each finite cover member. Apply \autoref{def:phase-real-completion-exit-cover-overlap-compatibility} to read those restrictions through the required common finite refinements, and apply \autoref{thm:phase-real-completion-exit-cover-overlap-no-confusion} to compare them as one public phase-exit gate read.

Now apply \autoref{thm:phase-real-completion-exit-current-local-status-witness} to read the common gate as the four local status records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with $\RealCompletionExactBoundaryUp$ only as the same-route boundary witness. Apply \autoref{thm:phase-real-completion-exit-component-status-extraction} on each restriction. It identifies the data visible on every $E_i$ as exactly the four roadmap records, the same boundary witness, and the displayed $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\PhaseRealCompletionExitUp}$ support rows. Since the restrictions come from one glued public read, the cover members agree on those rows over every overlap.

For the reverse direction, assume the finite cover displays the same four local records, the same-route $\RealCompletionExactBoundaryUp$ witness, and overlap agreement on the listed support rows. The compatibility hypothesis gives the common finite refinement rows of \autoref{def:phase-real-completion-exit-cover-overlap-compatibility}, and \autoref{thm:phase-real-completion-exit-cover-overlap-no-confusion} identifies the public phase-exit gate read determined by those rows.

Apply \autoref{thm:regseqrat-audit-threshold-finite-cover-gluing} to glue the regular-rational and real-facing audit-threshold sections over the shared finite four-face surface.

Next apply \autoref{thm:regseqrat-four-face-status-cofinal-descent}. This makes the glued read independent of the chosen finite refinement and keeps the status records attached to the same dyadic, stream, regular-rational, and real faces. Apply \autoref{thm:real-l10-finite-request-sheaf-exactness} to identify the resulting finite Real L10 cover certificate with one objectwise Phase Real and Completion exit certificate.

Finally repack the certificate as a $\PhaseRealCompletionExitUp$ packet by \autoref{thm:phase-real-completion-exit-current-local-status-witness} and read it back by \autoref{thm:phase-real-completion-exit-component-status-extraction}. The public gate is therefore the same gate precisely when the cover members display the same four roadmap records, same-route boundary witness, and support rows.

All constructions named above use only finite cover rows and their overlap restrictions. The refused quotient stream, selected limit, countable-choice row, metric-completeness datum, aggregate formalstatus row, aggregate bridge token, standard bridge row, and ambient $\RealUp$ completeness claim have no carrier coordinate in the displayed packet, so none is exported by the gluing.
\end{proof}

\begin{theorem}[Phase real-completion exit RegSeqRat common-tail pullback]
\label{thm:phase-real-completion-exit-regseqrat-common-tail-pullback}
Let $E=(D,S,Q,R,B,H,C,P,N)$ be an accepted $\PhaseRealCompletionExitUp$ packet. Suppose a RegSeqRat common-tail triple-overlap cocycle is displayed on the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows as $S,D,Q,R$, with the same support rows $\BHist$, componentwise $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ used by $H,C,P,N$. Then that common-tail cocycle pulls back to the same-route $\RealCompletionExactBoundaryUp$ face $B$ of $E$.

Conversely, if the boundary face $B$ of an accepted $\PhaseRealCompletionExitUp$ packet is supplied over such a RegSeqRat common-tail cocycle, then restricting the packet to the displayed four face rows recovers the RegSeqRat common-tail descent surface. In both directions the construction exports no quotient stream, selected limit, countable-choice row, metric-completeness datum, aggregate formalstatus row, aggregate bridge token, standard bridge row, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
For the pullback direction, first read the RegSeqRat input by \autoref{thm:regseqrat-common-tail-triple-overlap-cocycle}. The triple-overlap surface is exactly the common finite surface on the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Apply \autoref{thm:regseqrat-common-tail-cocycle-descent}. The pairwise common-tail rows descend to one displayed four-face boundary over the same stream, dyadic, regular-rational, and real records. Since these rows are identified with $S,D,Q,R$ and the support rows $H,C,P,N$ of $E$, \autoref{thm:phase-real-completion-exit-finite-cover-gate-gluing} glues the finite cover into the objectwise Phase Real and Completion gate on the same displayed packet.

Now apply \autoref{thm:phase-real-completion-exit-component-status-extraction}. The public read of the glued packet exposes the same-route $\RealCompletionExactBoundaryUp$ witness as the boundary coordinate $B$, rather than as an added completion carrier. Thus the common-tail cocycle pulls back to the boundary face already present in $E$.

For the converse direction, start with an accepted phase-exit packet whose boundary face $B$ is supplied over the displayed common-tail cocycle. The finite-cover gluing theorem reads that boundary through the same four local status records and the same support rows, while the component-status extraction theorem prevents any row from being absorbed into a route-level token.

Restricting to the $S,D,Q,R$ faces therefore leaves exactly the RegSeqRat common-tail triple-overlap data. Applying \autoref{thm:regseqrat-common-tail-cocycle-descent} to those restricted rows recovers the common-tail descent surface, with the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance.

All steps are objectwise over the displayed finite rows. Quotient streams, selected limits, countable choice, metric-completeness data, aggregate formalstatus rows, aggregate bridge tokens, standard bridge rows, and ambient $\RealUp$ completeness have no coordinate in $E$ or in the RegSeqRat common-tail descent boundary, so the pullback and the restriction cannot export them.
\end{proof}

\falsifiablePrediction{If a consumer fuses the dyadic, stream, regular, and real face reports into a single bridge token or completeness token, the $\PhaseRealCompletionExitUp$ packet is refuted.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, realcompletionexactboundary: no single sibling is bijective to the phase product because the packet binds dyadic arithmetic, finite stream observation, regular rational handoff, real sealing, exact boundary, transport, replay, provenance, and local naming in one displayed exit carrier.}

\begin{theorem}[Phase real-completion exit TasteGate primality rows]
\label{thm:phase-real-completion-exit-tastegate-primality-rows}
Every accepted $\PhaseRealCompletionExitUp$ packet carries four separate TasteGate primality rows: the falsifiable fused-token refusal above, the sibling independence witness against dyadicratcore, streamname, regseqrat, real, and realcompletionexactboundary, the Lean `Nontrivial' witness pair for distinct accepted and rejected carriers, and the Lean `FieldFaithful' projection over $D,S,Q,R,B,H,C,P,N$. Hence no one-element carrier, field-erasing classifier, aggregate completion token, or hidden sibling bijection discharges the chapter.
\end{theorem}
\begin{proof}
The carrier inventory in \autoref{def:phase-real-completion-exit-carrier} displays the finite exit packet as $E=(D,S,Q,R,B,H,C,P,N)$. Thus the paper surface already names the fields whose preservation is tested by the local TasteGate.

The falsifiability and independence rows immediately above supply the two external primality tests: a consumer that fuses the dyadic, stream, regular, and real face reports into one bridge or completeness token refutes the packet, and none of the five sibling chapters is bijective to the phase product.

The remaining two rows are the checked `Nontrivial' and `FieldFaithful' witnesses in \texttt{lean4/BEDC/Derived/PhaseRealCompletionExitUp/TasteGate.lean}. The `Nontrivial' instance supplies distinct accepted and rejected carriers, while the `FieldFaithful' instance projects the classifier through the displayed fields $D,S,Q,R,B,H,C,P,N$ rather than through an erased aggregate.

Together these four rows block each collapse mode. A one-element carrier violates `Nontrivial', a field-erasing classifier violates `FieldFaithful', an aggregate completion token violates the falsifiability row, and a hidden sibling bijection violates the independence witness. Therefore the accepted packet can discharge the chapter only as the displayed finite exit carrier with all four primality rows present.
\end{proof}
\leanchecked{BEDC.Derived.PhaseRealCompletionExitUp.taste\_gate}

\closureat{PhaseRealCompletionExitUp}{seedStr}
\closureat{PhaseRealCompletionExitUp}{obligationStr}
\closureat{PhaseRealCompletionExitUp}{scopedStr}[BEDC.Derived.PhaseRealCompletionExitUp.PhaseRealCompletionExitTasteGate\_single\_carrier\_alignment]
\closureat{PhaseRealCompletionExitUp}{publicStr}[BEDC.Derived.PhaseRealCompletionExitUp.PhaseRealCompletionExitTasteGate\_single\_carrier\_alignment]
\closureat{PhaseRealCompletionExitUp}{bridgedStr}[BEDC.Derived.PhaseRealCompletionExitUp.PhaseRealCompletionExitTasteGate\_single\_carrier\_alignment]
\closureat{PhaseRealCompletionExitUp}{matureStr}[BEDC.Derived.PhaseRealCompletionExitUp.PhaseRealCompletionExitTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\PhaseRealCompletionExitUp}
  \constructivestory{A $\PhaseRealCompletionExitUp$ packet is generated from dyadic, stream, regular-rational, Real-seal, $\RealCompletionExactBoundaryUp$, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. The exact-boundary face is a same-route witness over those rows, not a bridge or completion primitive. The classifier accepts the phase exit only as the visible tuple of finite rows; no single face can stand for completion, and quotient equality, selected limits, host completeness, aggregate formalstatus, and bridge tokens stay outside the carrier. The local TasteGate instance is BEDC.Derived.PhaseRealCompletionExitUp.taste\_gate.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:phase-real-completion-exit-scoped-primitive-dependency-binding} and \autoref{thm:phase-real-completion-exit-public-gate-certificate} form the mature paper boundary: the accepted packet is the dyadic, stream, regular-rational, Real-seal, exact-boundary, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ same-route gate.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.PhaseRealCompletionExitUp.PhaseRealCompletionExitTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{Standard-bridge verification remains unsupplied, and the packet exports no aggregate formalstatus row, bridge token, quotient stream, selected limit, host-completeness row, or metric-completeness row.}
  \upgradepath{The next formal obligation is standard-bridge verification for the already axiom-clean same-route gate; no aggregate formalstatus, bridge token, quotient stream, selected limit, host completeness, or metric completeness is supplied here.}
\end{closurestatus}
