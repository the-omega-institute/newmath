\chapter{A Concrete Naming Certificate for \(\LatticeUp\)}
\label{ch:concrete-instances-lattice-namecert}

The lattice interface \(\LatticeUp\) extends \autoref{ch:concrete-instances-poset-namecert} with binary meet \(\wedge\) and join \(\vee\) operations, each classified as the greatest lower bound and least upper bound of two elements under the partial order. Idempotence, commutativity, associativity, and absorption laws are derived from the bound characterizations under the carrier's classifier.

\input{parts/concrete_instances/lattice/carrier_definition}
\input{parts/concrete_instances/lattice/concrete_singleton_prefix_instance}
\input{parts/concrete_instances/lattice/orienting_paragraph}
\input{parts/concrete_instances/lattice/the_certificate}
\input{parts/concrete_instances/30_lattice_directed_associativity}

\begin{closurestatus}{\LatticeUp}
  \constructivestory{The lattice instance is the singleton endpoint sitting over the prefix poset: the carrier accepts only histories history-same to $\emp$, so both meet and join are forced to return $\emp$ no matter which carried operands are displayed. Its classifier is $\rho_{\emp}$, the same singleton relation that packages two endpoint carrier witnesses with an $\hsame$ link, and the inherited order is the restricted prefix comparison between those carried endpoints. The source is a certified poset carrier equipped with meet and join data, the pattern records lower-bound, greatest-lower-bound, upper-bound, least-upper-bound, congruence, directed associativity, absorption, and modular-law rows, and the ledger stores the operands with the bound-characterization witnesses that reduce every comparison to the empty endpoint. The five name-certificate obligations are immediate from the singleton endpoint: $\emp$ inhabits the carrier, $\hsame$ gives the classifier equivalence, and carrier closure under classified replacement follows by composing endpoint witnesses through $\emp$. A history $h$ lies in the signature gap of a $\LatticeUp$ token $p$ when the source-domain accepts $h$, the signature relation pairs it with an answer endpoint and bound ledger, and token introduction names that answer as $p$; in this instance, $h$ is classified with $\emp$, every meet or join ledger returns $\emp$, and $p$ names the one-point lattice row. No standard bridge is closed in this chapter, although the intended outside reading is the terminal one-point lattice. The chapter registers this singleton meet-join construction under the name $\LatticeUp$; lattice laws are endpoint consequences of the existing prefix order, with no arbitrary lattice carrier, representation theorem, or external meet/join algebra introduced.}
  \theoryclosure{\publicClosure}
  \scopeclosed{The public lattice certificate is closed over the displayed singleton-history carrier, classifier, order, meet, join, directed associativity, absorption, and modular-law rows.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.LatticeUp.LatticeSingletonPrefix\_laws}
  \bridgestatus{none}
  \notclaimed{It does not close full traditional lattice theory, arbitrary lattice carriers, representation theorems, or standard bridges.}
  \upgradepath{The next theory step is a bridge from the public lattice certificate to the standard bridge interface.}
\end{closurestatus}
