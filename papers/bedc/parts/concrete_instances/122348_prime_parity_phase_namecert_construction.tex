\chapter{A Concrete Naming Certificate for $\PrimeParityPhaseUp$}
\label{ch:concrete-instances-prime-parity-phase-namecert}
\origin{ai}

The $\PrimeParityPhaseUp$ packet realizes the projection dictionary of \autoref{sec:unified-reality-prime-charge-parity-phase-dictionary} as one finite NameCert surface. It binds prime-charge rows, parity and unit-boundary rows, phase-routing rows, height-ledger rows, triadic closure-defect rows, transport, replay, provenance, and local naming before any phase or energy consumer reads the packet. The chapter names the finite dictionary surface; it does not prove RH, analytic continuation, Lee--Yang zeros, host complex equality, or an ambient spectral classification.

\begin{definition}[Prime parity phase carrier]
\label{def:prime-parity-phase-carrier}
A $\PrimeParityPhaseUp$ carrier is a finite $\BHist$ packet
$$
P=(Q,U,F,H,D,T,C,V,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the prime-charge row, displaying the finite prime register, height values, and charge ledger read from the source dictionary.
\item $U$ is the parity and unit-boundary row, recording the sign boundary, unit-circle boundary, and the refusal of hidden positive real scale inside a compact phase fibre.
\item $F$ is the phase-routing row, displaying the finite phase successor, winding, and solenoid-facing route used by the phase consumer.
\item $H$ is the height-ledger row, tying the prime-charge heights to the routed phase and energy-facing reads.
\item $D$ is the triadic closure-defect row, exposing only the finite defect data visible through the prime, parity, phase, and height rows.
\item $T$ records componentwise $\hsame$ transport for prime charge, parity, unit boundary, phase route, height ledger, defect, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the dictionary request through $Q,U,F,H,D,T,V,N$.
\item $V$ is the $\Pkg$ provenance over $\PrimeParityPhaseUp$, the prime-charge dictionary of \autoref{sec:unified-reality-prime-charge-parity-phase-dictionary}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PrimeParityPhaseUp}$ row.
\end{enumerate}
The local classifier compares accepted packets only through $Q,U,F,H,D,T,C,V,N$. It has no coordinate for a completed zeta continuation, RH, Lee--Yang classification, host complex equality, noncompact hidden scale, selected infinite prime product, or global spectral theorem.
\end{definition}

\begin{theorem}[PrimeParityPhase NameCert obligations]
\label{thm:prime-parity-phase-namecert-obligations}
For an accepted $\PrimeParityPhaseUp$ carrier
$$
P=(Q,U,F,H,D,T,C,V,N),
$$
the local $\NameCert_{\PrimeParityPhaseUp}$ surface consists exactly of prime-charge exposure, parity and unit-boundary exposure, phase-routing exposure, height-ledger exposure, triadic closure-defect exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Any accepted phase or energy read must pass through
$$
Q,U,F,H,D \longmapsto T,C,V,N
$$
before the read is exported.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:prime-parity-phase-carrier}. Its visible dictionary rows are $Q,U,F,H,D$, and its structural rows are $T,C,V,N$.

The source dictionary of \autoref{sec:unified-reality-prime-charge-parity-phase-dictionary} separates prime charge, parity, phase routing, height ledger, and triadic closure defect. The carrier keeps those rows separate until the local NameCert read asks for a phase or energy-facing export.

Transport and replay are componentwise through $T$ and $C$, while provenance and local naming are exhausted by $V$ and $N$. Since no row records RH, analytic continuation, Lee--Yang classification, host complex equality, hidden noncompact scale, or a selected infinite prime product, the NameCert obligations are exactly the displayed finite dictionary rows.
\end{proof}

\begin{theorem}[PrimeParityPhase window readback]
\label{thm:prime-parity-phase-window-readback}
Every finite $\PrimeParityPhaseUp$ phase window readback is anchored in the prime-charge row, parity and unit-boundary row, phase-routing row, height-ledger row, triadic closure-defect row, transport, replay, provenance, and local naming of the same accepted carrier.
\end{theorem}
\begin{proof}
Let
$$
P=(Q,U,F,H,D,T,C,V,N)
$$
be an accepted carrier. By \autoref{thm:prime-parity-phase-namecert-obligations}, a phase or energy consumer reads $Q,U,F,H,D$ before any export.

The finite window is therefore not a host complex phase, a hidden real scale, or a selected infinite Euler product. It is the readback of the displayed route $Q,U,F,H,D$ under the componentwise transport $T$, replay $C$, provenance $V$, and local naming $N$.
\end{proof}

\falsifiablePrediction{Any accepted read of phase or energy without first displaying the prime-charge row, parity and unit-boundary row, phase-routing row, height-ledger row, transport row, replay row, provenance row, and local naming row refutes the $\PrimeParityPhaseUp$ carrier.}

\independenceWitness{prime-charge dictionary, zeta-continuation-socket, solenoid-lee-yang, dyadicratcore, streamname: the $\PrimeParityPhaseUp$ packet is not bijective to any sibling because it jointly carries prime charge, parity, unit boundary, phase routing, height ledger, closure defect, transport, replay, provenance, and local naming. The dictionary supplies the source rows, the zeta and solenoid sockets remain outside the finite NameCert claim, and the dyadic and stream siblings supply readback discipline without absorbing the prime-parity-phase carrier.}

\closureat{PrimeParityPhaseUp}{seedStr}

\begin{closurestatus}{\PrimeParityPhaseUp}
  \constructivestory{A $\PrimeParityPhaseUp$ packet is generated from finite prime-charge, parity, unit-boundary, phase-routing, height-ledger, triadic closure-defect, transport, replay, provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:prime-parity-phase-carrier}, \autoref{thm:prime-parity-phase-namecert-obligations}, and \autoref{thm:prime-parity-phase-window-readback} seed the finite prime-charge, parity, phase, height-ledger, closure-defect, transport, replay, provenance, and naming dictionary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RH, analytic continuation, Lee--Yang classification, host complex equality, noncompact hidden scale, selected infinite prime products, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked Nontrivial and FieldFaithful TasteGate instances together with carrier, readback, transport, replay, provenance, and local naming rows for BEDC.Derived.PrimeParityPhaseUp.}
\end{closurestatus}
