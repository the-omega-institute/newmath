\chapter{A Concrete Naming Certificate for $\CantorIntersectionPrincipleUp$}
\label{ch:concrete-instances-cantor-intersection-principle-namecert}
\origin{human}

Following Bishop and Bridges on nested closed intervals and regular Cauchy completion, the $\CantorIntersectionPrincipleUp$ packet records the finite route by which a descending located interval family, dyadic diameter ledger, regular rational midpoint readback, finite stream windows, and terminal real seal are offered to Real and Completion consumers. It sits on the completion spine of \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet names the choice-free Cantor intersection handoff visible to regular Cauchy readers; it is not a quotient real, an arbitrary closed-set intersection theorem, countable choice, trichotomy, open-cover compactness, or ambient completeness of $\RealUp$.

\begin{definition}[Cantor intersection principle carrier]
\label{def:cantor-intersection-principle-carrier}
A $\CantorIntersectionPrincipleUp$ carrier is a finite $\BHist$ packet
$$
I=(D,W,R,E,J,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ diameter ledger, recording the inspected dyadic precision, the nested interval endpoints visible at that precision, and the finite proof that the displayed diameters are below the requested tolerance.
\item $W$ is the $\StreamNameUp$ finite-window row, exposing only the bounded interval indices and endpoint windows read by the consumer.
\item $R$ is the $\RegSeqRatUp$ midpoint readback row obtained from $D$ and $W$; it records the regular rational name determined by the displayed nested windows.
\item $E$ is the terminal $\RealUp$ seal row reached only after the dyadic ledger, stream windows, and regular rational readback have been replayed.
\item $J$ is the intersection-claim row, recording that every displayed interval window contains the same readback route and that later windows refine earlier ones.
\item $H$ records componentwise $\hsame$ transport for the dyadic ledger, stream windows, regular readback, Real seal, intersection row, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Real or Completion request through $D,W,R,E,J,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CantorIntersectionPrincipleUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorIntersectionPrincipleUp}$ row.
\end{enumerate}
The carrier predicate compares accepted packets only through $D,W,R,E,J,H,C,P,N$. It has no coordinate for a selected point by countable choice, quotient stream equality, host real equality, arbitrary closed subsets, open-cover compactness, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cantor intersection principle NameCert obligations]
\label{thm:cantor-intersection-principle-namecert-obligations}
For every accepted $\CantorIntersectionPrincipleUp$ carrier $I=(D,W,R,E,J,H,C,P,N)$, the local $\NameCert_{\CantorIntersectionPrincipleUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed dyadic diameter ledger, finite stream-window row, regular rational midpoint readback, terminal Real seal, intersection row, transport, replay, provenance, and local naming;
\item nested-window exactness, requiring every Real or Completion read to inspect the same finite $D,W$ interval window before it reaches $R$;
\item regular-readback stability, requiring the midpoint route to be consumed as a $\RegSeqRatUp$ name before the terminal $\RealUp$ seal is visible;
\item intersection nonescape, requiring $J$ to record refinement of displayed windows without adding an arbitrary closed-set carrier;
\item ledger refusal, excluding countable choice, quotient streams, host equality, trichotomy, open-cover compactness, selected representatives, and ambient Real completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cantor-intersection-principle-carrier}. The projected rows are exactly the dyadic diameter ledger $D$, stream windows $W$, regular midpoint readback $R$, terminal Real seal $E$, intersection row $J$, and structural rows $H,C,P,N$.

Nested-window exactness is read through $D$ and $W$. The consumer sees only the bounded endpoint windows and the dyadic diameter comparison stored in the packet, so there is no separate source from which a point could be chosen.

The regular-readback row $R$ repacks the displayed midpoint route as the $\RegSeqRatUp$ handoff used by \autoref{ch:concrete-instances-regseqrat-namecert}. The terminal seal $E$ is therefore downstream of the same finite route and cannot be detached from it.

The intersection and refusal rows follow from the remaining coordinates. Row $J$ records refinement of the displayed windows, while $H,C,P,N$ transport, replay, source, and name that route; none of these rows stores countable choice, quotient equality, host equality, trichotomy, open-cover compactness, selected representatives, or ambient completeness.
\end{proof}

\begin{theorem}[Cantor intersection principle completion route]
\label{thm:cantor-intersection-principle-completion-route}
Every Real or Completion consumer of an accepted $\CantorIntersectionPrincipleUp$ carrier $I=(D,W,R,E,J,H,C,P,N)$ must factor through the finite route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The route exposes a nested located interval family by dyadic diameter windows, then reads the regular rational midpoint name, and only then reaches the terminal Real seal. It exports no quotient-real construction, selected representative, countable-choice schedule, arbitrary closed-set intersection principle, trichotomy, open-cover compactness, host equality, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with the displayed rows $D$ and $W$. They bind the inspected precision, endpoint windows, and diameter comparison before any completion-facing name can be read.

The row $R$ consumes exactly those displayed windows as a regular rational readback. Thus the completion-facing handoff has already passed through the $\DyadicRatCoreUp$, $\StreamNameUp$, and $\RegSeqRatUp$ surfaces before the Real seal is available.

Finally use the intersection row $J$ together with $H,C,P,N$. These rows record refinement, transport, replay, provenance, and local naming for the same finite route. Since the carrier has no row for quotient reals, selected representatives, countable choice, arbitrary closed sets, trichotomy, open-cover compactness, host equality, or ambient completeness, every consumer remains inside the stated completion route.
\end{proof}

\falsifiablePrediction{Every $\CantorIntersectionPrincipleUp$ Real-completion read factors through one DyadicRatCore diameter ledger, one StreamName finite-window row, one RegSeqRat midpoint readback row, one terminal $\RealUp$ seal, one intersection-refinement row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real: $\CantorIntersectionPrincipleUp$ is not bijective to any listed sibling because it jointly carries the nested diameter ledger, finite interval windows, regular midpoint readback, Real seal, refinement row, transport, replay, provenance, and local NameCert rows as one packet.}

\closureat{CantorIntersectionPrincipleUp}{seedStr}

\begin{closurestatus}{\CantorIntersectionPrincipleUp}
  \constructivestory{A $\CantorIntersectionPrincipleUp$ packet is generated from a DyadicRatCore diameter ledger, StreamName finite interval windows, RegSeqRat midpoint readback, a terminal RealUp seal, an intersection-refinement row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-intersection-principle-carrier}, \autoref{thm:cantor-intersection-principle-namecert-obligations}, and \autoref{thm:cantor-intersection-principle-completion-route} seed the finite Cantor-intersection completion route from dyadic interval diameters through StreamName, RegSeqRat, and the terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient reals, selected representatives, countable choice, arbitrary closed-set intersections, trichotomy, open-cover compactness, host equality, full $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nested-window exactness, regular-readback stability, Real-seal nonescape, intersection refinement, transport, replay, provenance, and local naming for BEDC.Derived.CantorIntersectionPrincipleUp.}
\end{closurestatus}
