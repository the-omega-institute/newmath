\chapter{A Concrete Naming Certificate for $\KreinSmulianUp$}
\label{ch:concrete-instances-krein-smulian-namecert}
\origin{human}

Following Krein and Smulian, the $\KreinSmulianUp$ packet records the finite NameCert surface for bounded weak-star closed convexity in a displayed dual-space setting. It sits over the dual-space source of \autoref{ch:concrete-instances-dual-space-namecert}, the weak-star topology of \autoref{ch:concrete-instances-weak-star-topology-namecert}, the weak-star dual-ball compactness route of \autoref{ch:concrete-instances-banach-alaoglu-namecert}, the compactness boundary of \autoref{ch:concrete-instances-weak-star-compactness-namecert}, and the compact-convex sibling of \autoref{ch:concrete-instances-krein-milman-compact-convex-namecert}. The chapter names the bounded-ball inspection surface; it does not assert an unrestricted weak-star closedness theorem, select separating functionals, identify quotient duals, or import ambient completeness.

\begin{definition}[Krein--Smulian carrier]
\label{def:krein-smulian-carrier}
A $\KreinSmulianUp$ carrier is a finite $\BHist$ packet
$$
S=(D,W,B,X,L,A,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DualSpaceUp$ source row, exposing only the named primal carrier, dual observation row, scalar readbacks, and local dual-space naming surface.
\item $W$ is the $\WeakStarTopologyUp$ row used to read finite weak-star neighbourhood tests, bounded-linear observations, and scalar tolerances.
\item $B$ is the bounded-ball window, listing the displayed radius, dual-ball membership checks, and the finite family of weak-star tests inspected inside that ball.
\item $X$ is the convex-set row for the inspected subset, with membership, finite affine-combination, and convex compatibility rows visible only through the bounded window.
\item $L$ is the closed-window ledger: it records sequential finite-window closure tests, finite cover-refinement reads, and retained boundary obligations for each displayed ball window.
\item $A$ is the $\BanachAlaogluUp$ handoff for weak-star compactness of the displayed bounded dual ball.
\item $K$ is the $\WeakStarCompactnessUp$ and $\KreinMilmanCompactConvexUp$ compatibility row, opened only when compactness or compact-convex consumers request the same bounded weak-star window.
\item $H$ records componentwise $\hsame$ transport for the dual-space source, weak-star topology, bounded-ball window, convex-set row, closed-window ledger, compactness handoffs, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a bounded weak-star closedness request through $D,W,B,X,L,A,K$ and then through $H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\KreinSmulianUp$, $\DualSpaceUp$, $\WeakStarTopologyUp$, $\BanachAlaogluUp$, $\WeakStarCompactnessUp$, $\KreinMilmanCompactConvexUp$, $\ConvexSetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\KreinSmulianUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,W,B,X,L,A,K,H,C,P,N$. It has no coordinate for all bounded sets at once, unrestricted weak-star closure, selected separating functionals, quotient dual equality, countable choice, or ambient Banach-space completeness.
\end{definition}

\begin{theorem}[Krein--Smulian NameCert obligations]
\label{thm:krein-smulian-namecert-obligations}
For every accepted $\KreinSmulianUp$ carrier
$$
S=(D,W,B,X,L,A,K,H,C,P,N),
$$
the local $\NameCert_{\KreinSmulianUp}$ surface consists exactly of dual-space admission through $D$, weak-star topology admission through $W$, bounded-ball window exposure through $B$, convex-set admission through $X$, sequential and finite closed-window ledger visibility through $L$, Banach--Alaoglu compactness handoff through $A$, weak-star compactness and compact-convex compatibility through $K$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.

Every bounded weak-star closedness consumer must read the finite route
$$
D,W \longmapsto B,X,L \longmapsto A,K \longmapsto H,C,P,N
$$
before treating the packet as a Krein--Smulian name. The route exports bounded-window certificate data only; it exports no unrestricted closedness theorem, no global separating-functional choice, no quotient dual equality, and no ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:krein-smulian-carrier}. The dual source is $D$, and the weak-star observation interface is $W$.

Open the bounded part of the packet. The row $B$ fixes the inspected dual ball and finite weak-star tests, while $X$ records the convex-set surface visible inside the same window. The ledger $L$ retains the sequential and finite closed-window reads that a bounded closedness consumer may inspect.

Use the compactness handoffs only after the bounded window is displayed. The row $A$ supplies the Banach--Alaoglu dual-ball route, and $K$ records compatibility with the weak-star compactness and compact-convex siblings. These rows do not select separating functionals or identify quotient dual carriers.

Finally transport and replay the same route through $H,C,P,N$. Since the carrier has no rows for unrestricted weak-star closedness, all bounded sets at once, quotient dual equality, countable choice, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Krein--Smulian weak-star bounded-ball route]
\label{thm:krein-smulian-weak-star-ball-route}
Every accepted $\KreinSmulianUp$ packet exposes weak-star closed convexity only through a displayed bounded-ball route. For
$$
S=(D,W,B,X,L,A,K,H,C,P,N),
$$
any consumer that reads the packet as closed-convex evidence must first display the dual-space source $D$, weak-star topology row $W$, bounded-ball window $B$, convex-set row $X$, closed-window ledger $L$, compactness handoff $A$, compatibility row $K$, and structural rows $H,C,P,N$.

Thus the public route is
$$
\DualSpaceUp,\WeakStarTopologyUp \longmapsto B,X,L \longmapsto \BanachAlaogluUp,\WeakStarCompactnessUp,\KreinMilmanCompactConvexUp .
$$
It is not a host theorem about arbitrary weak-star closed convex sets, a selected separator, a quotient-dual equality, or an ambient completeness assertion.
\end{theorem}
\begin{proof}
Start from an accepted packet and expose $D,W$. These two rows provide the only dual-space and weak-star observation data available to the consumer.

The bounded-ball row $B$ is then mandatory. It fixes the radius and finite test window before the convex-set row $X$ and closed-window ledger $L$ can be read. Consequently the closure read is bounded-window data, not a global statement over all bounded subsets.

Apply the compactness handoff rows. The Banach--Alaoglu row $A$ reads the bounded dual-ball compactness route, while $K$ keeps the same window compatible with weak-star compactness and compact-convex consumers.

The structural rows $H,C,P,N$ preserve the displayed dependency route. Because no other carrier coordinate exists, a downstream closed-convex read cannot bypass the bounded ball, cannot select a separating functional, and cannot export quotient dual equality or ambient completeness.
\end{proof}

\closureat{KreinSmulianUp}{seedStr}

\begin{closurestatus}{\KreinSmulianUp}
  \constructivestory{A $\KreinSmulianUp$ packet is generated from dual-space source rows, weak-star topology rows, a bounded-ball window, convex-set rows, sequential and finite closed-window ledgers, Banach--Alaoglu and weak-star compactness handoffs, compact-convex compatibility, transport, replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:krein-smulian-carrier}, \autoref{thm:krein-smulian-namecert-obligations}, and \autoref{thm:krein-smulian-weak-star-ball-route} seed the bounded weak-star closed convexity NameCert surface over dual-space, weak-star topology, compactness, and compact-convex sibling rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close unrestricted weak-star closedness, global bounded-set quantification, selected separating functionals, quotient dual equality, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked dual-space admission, weak-star topology admission, bounded-ball window exposure, convex-set admission, closed-window ledger visibility, compactness handoff, transport, replay, provenance, and local naming for $\KreinSmulianUp$.}
\end{closurestatus}
