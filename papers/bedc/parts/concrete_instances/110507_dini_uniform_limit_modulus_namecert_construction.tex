\chapter{A Concrete Naming Certificate for $\DiniUniformLimitModulusUp$}
\label{ch:concrete-instances-dini-uniform-limit-modulus-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and the classical Dini theorem, the $\DiniUniformLimitModulusUp$ packet extracts the finite uniform-modulus ledger visible in compact-metric monotone convergence data. It sits over \autoref{ch:concrete-instances-dini-continuity-namecert}, \autoref{ch:concrete-instances-diniuniformconvergence-namecert}, \autoref{ch:concrete-instances-compactmetric-namecert}, \autoref{ch:concrete-instances-continuousmap-namecert}, \autoref{ch:concrete-instances-uniformlimit-namecert}, \autoref{ch:concrete-instances-uniformmodulus-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names the finite net, rational tolerance budget, and extracted modulus route; it does not import open-cover compactness, chosen limiting values, quotient function equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Dini uniform-limit modulus carrier]
\label{def:dini-uniform-limit-modulus-carrier}
A $\DiniUniformLimitModulusUp$ carrier is a finite $\BHist$ packet
$$
D=(K,F,W,E,Q,U,L,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row naming the compact-metric finite-source observations consumed by the extraction.
\item $F$ is the monotone $\ContinuousMapUp$ family row, recording the displayed continuous approximants and their pointwise monotone order.
\item $W$ is the pointwise window row supplied by the Dini-continuity and Dini-uniform-convergence routes.
\item $E$ is the finite net row over $K$, naming the finite mesh on which pointwise windows are inspected.
\item $Q$ is the rational tolerance-budget row, storing the $\RegSeqRatUp$ tolerances assigned to the finite net and to the later readback.
\item $U$ is the extracted $\UniformModulusUp$ row, obtained from $E,Q,W$ and the monotone continuous family row $F$.
\item $L$ is the $\UniformLimitUp$ handoff row, exposing only the modulus-controlled limit route supplied by $U$.
\item $R$ is the $\RegSeqRatUp$ readback row for rational tolerance transport.
\item $S$ is the $\RealUp$ seal row, naming the real-valued read without adding ambient real completeness.
\item $H$ records componentwise $\hsame$ transport for compact-metric source, monotone family, pointwise window, finite net, rational tolerance, extracted modulus, uniform-limit handoff, rational readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the compact-metric source and monotone family through the finite net, tolerance budget, extracted modulus, and uniform-limit handoff.
\item $P$ is the $\Pkg$ provenance over $\DiniUniformLimitModulusUp$, $\DiniContinuityUp$, $\DiniUniformConvergenceUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\UniformModulusUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DiniUniformLimitModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,W,E,Q,U,L,R,S,H,C,P,N$. It has no coordinate for open-cover compactness, selected limiting values, quotient function equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dini uniform-limit modulus NameCert obligations]
\label{thm:dini-uniform-limit-modulus-namecert-obligations}
For every accepted $\DiniUniformLimitModulusUp$ carrier
$$
D=(K,F,W,E,Q,U,L,R,S,H,C,P,N),
$$
the local $\NameCert_{\DiniUniformLimitModulusUp}$ surface consists exactly of carrier admission for the displayed compact-metric source, monotone continuous family, pointwise window, finite net, rational tolerance budget, extracted uniform modulus, UniformLimit handoff, RegSeqRat readback, Real seal, componentwise transport, replay, provenance, and local naming rows.

The obligation surface requires monotone-window visibility through $F$ and $W$, finite-net extraction through $E$ and $Q$, modulus handoff through $U$ and $L$, Real-seal nonescape through $S$, transport and replay stability through $H$ and $C$, provenance through $P$, and local naming through $N$. It explicitly refuses open-cover compactness, selected limiting values, quotient function equality, countable choice, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only computational rows are the compact-metric source $K$, monotone family $F$, pointwise windows $W$, finite net $E$, rational budget $Q$, extracted modulus $U$, uniform-limit handoff $L$, rational readback $R$, and real seal $S$.

The monotone-window step is local to $F$ and $W$. The finite-net step is local to $E$ and $Q$, so the modulus row $U$ is extracted from displayed finite data rather than from an open cover or a choice of limiting values.

The handoff to $\UniformLimitUp$ passes only through $U$ and $L$. The rational and real readback rows $R$ and $S$ name the downstream values without adding quotient function equality or ambient completeness.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the excluded objects are not coordinates of $D$, no NameCert consumer can read them from this carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\DiniUniformLimitModulusUp$ read must expose compact-metric source rows, monotone continuous-family rows, pointwise window rows, a finite net, a rational tolerance budget, an extracted uniform modulus, a UniformLimit handoff, RegSeqRat readback, Real sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{dini-continuity, diniuniformconvergence, compactmetric, continuousmap, uniformlimit, uniformmodulus, regseqrat, real: $\DiniUniformLimitModulusUp$ is not bijective to any listed sibling because it binds the finite net and rational tolerance budget to an extracted uniform-modulus handoff before UniformLimit and Real consumers may read the result.}

\closureat{DiniUniformLimitModulusUp}{seedStr}

\begin{closurestatus}{\DiniUniformLimitModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dini-uniform-limit-modulus-carrier} and \autoref{thm:dini-uniform-limit-modulus-namecert-obligations} seed the finite uniform-modulus ledger extracted from compact-metric monotone convergence data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected limiting values, quotient function equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, monotone-window visibility, finite-net extraction, modulus handoff, Real-seal nonescape, transport and replay stability, provenance exhaustion, and local naming for BEDC.Derived.DiniUniformLimitModulusUp.}
  \constructivestory{A $\DiniUniformLimitModulusUp$ packet is generated from compact-metric source rows, monotone continuous-family rows, pointwise windows, finite net rows, rational tolerance-budget rows, extracted uniform-modulus rows, UniformLimit handoff rows, RegSeqRat readback, Real sealing, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
\end{closurestatus}
