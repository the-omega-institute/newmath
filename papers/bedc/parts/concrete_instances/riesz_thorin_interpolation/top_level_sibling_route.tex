The top-level $\RieszThorinInterpolationUp$ route records the sibling lattice that every interpolated norm-bound read must traverse. The proof surface is anchored in the three-lines estimate of \autoref{ch:concrete-instances-hadamard-three-lines-namecert}, the normed source and target rows of \autoref{ch:concrete-instances-normedspace-namecert}, the operator-action rows of \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, the interpolation-family carrier of \autoref{ch:concrete-instances-interpolation-namecert}, and the scalar seal of \autoref{ch:concrete-instances-real-namecert}.

\begin{lemma}[Riesz--Thorin sibling lattice route]
Every accepted $\RieszThorinInterpolationUp$ consumer reads its endpoint bounds, analytic-strip comparison, interpolation parameter, bounded-linear-operator action, norm rows, and terminal $\RealUp$ scalar estimate through the displayed sibling lattice before the interpolated bound is public. The route exports no host complex interpolation theorem, quotient operator equality, selected analytic family, or ambient completeness coordinate beyond those sibling rows.
\end{lemma}
