\chapter{A Concrete Naming Certificate for $\MetaCICClosureTraceUp$}
\label{ch:concrete-instances-metacicclosuretrace-namecert}
\origin{human}

The $\MetaCICClosureTraceUp$ packet gives the ClosurePreservation audit map of \autoref{ch:metacic-closure-preservation-audit-map} a finite BEDC landing. It sits beside the audit-gate packet of \autoref{ch:concrete-instances-tastegateaudittrace-namecert} and the kernel trace packet of \autoref{ch:concrete-instances-kernelacceptancetrace-namecert}: the former records admission discipline, the latter records accepted declarations, and this packet records the closedness-preservation rows that downstream MetaCIC routes may replay.

\begin{definition}[MetaCICClosureTrace carrier]
\label{def:metacicclosuretrace-carrier}
A $\MetaCICClosureTraceUp$ carrier is a finite $\BHist$ packet
$$
T=(S,U,V,B,R,G,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the shift closedness row, naming the theorem row for \texttt{shift\_one\_preserves\_closed\_succ};
\item $U$ is the variable-substitution closedness row, naming the theorem row for \texttt{substitute\_var\_preserves\_closed\_at};
\item $V$ is the full-substitution closedness row, naming the theorem row for \texttt{substitute\_preserves\_closed\_at};
\item $B$ is the one-step beta closedness row, naming the theorem row for \texttt{betaStep\_preserves\_closed};
\item $R$ is the finite beta-chain closedness row, naming the theorem row for \texttt{betaStarStep\_preserves\_closed};
\item $G$ records the generator-closure rows for \texttt{closed\_under\_shift\_classifier} and \texttt{closed\_under\_substitute\_classifier};
\item $K$ records the kernel-acceptance trace rows for the accepted MetaCIC ClosurePreservation declarations and their displayed dependency ledger;
\item $H$ records componentwise $\hsame$ transports for theorem rows, generator rows, kernel trace rows, provenance rows, and local naming rows;
\item $C$ records the displayed $\Cont$ routes by which downstream subject-reduction, closed-normal, and conversion-facing packets read the closure trace;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:metacic-closure-preservation-audit-map}, \autoref{ch:concrete-instances-tastegateaudittrace-namecert}, \autoref{ch:concrete-instances-kernelacceptancetrace-namecert}, the MetaCIC ClosurePreservation module, generator-closure rows, componentwise $\hsame$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\MetaCICClosureTraceUp}$ row.
\end{enumerate}
The carrier contains no typing preservation theorem, no subject-reduction discharge inhabitant, no confluence theorem, no normalization theorem, no hidden kernel soundness premise, and no generator outside the displayed shift, substitution, beta-step, and finite beta-chain rows.
\end{definition}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[MetaCICClosureTrace substitution and shift generator package]
\label{thm:metacicclosuretrace-substitution-shift-generator-package}
For every accepted $\MetaCICClosureTraceUp$ carrier $T=(S,U,V,B,R,G,K,H,C,P,N)$, the rows $S,U,V,G$ form a finite generator package for the closedness classifier. The shift row $S$ authorizes the one-step lift at the successor boundary, the variable row $U$ and full-substitution row $V$ authorize substitution at the receiving boundary, and the generator row $G$ records exactly those two generator-closure classifier witnesses as the package exported to downstream closure consumers.
\end{theorem}
\begin{proof}
Project the displayed packet $T$. The shift row and the two substitution rows are already separated as $S,U,V$, so the carrier has a finite inventory before any downstream consumer is considered.

Read the generator row $G$. Its shift coordinate is backed by the same closedness-preservation theorem named by $S$, and its substitution coordinate is backed by the variable and full-substitution theorem rows $U,V$.

Finally inspect $H,C,P,N$. Transport and continuation replay preserve the displayed package rows, while provenance and local naming bind them to the ClosurePreservation audit map. Since no other generator coordinate occurs in $T$, the exported generator package is exhausted by $S,U,V,G$.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_substitution\_shift\_generator\_package}

\begin{theorem}[MetaCICClosureTrace closed-substitution route]
\label{thm:metacicclosuretrace-closed-substitution-route}
Every closed-substitution consumer of an accepted $\MetaCICClosureTraceUp$ carrier $T=(S,U,V,B,R,G,K,H,C,P,N)$ must read the shift row $S$, the variable-substitution row $U$, the full-substitution row $V$, the generator row $G$, and the kernel-acceptance row $K$ before exporting closedness preservation. The route exports only the closure-preservation boundary for closed substitution; it exports no typing, conversion, confluence, normalization, subject-reduction, or kernel-soundness conclusion.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_closed\_substitution\_route}
\begin{proof}
Project the displayed carrier $T$. The rows $S,U,V,G,K$ are separate coordinates, so a closed-substitution consumer can identify the shift, variable-substitution, full-substitution, generator, and acceptance ledger data before any beta-chain or continuation row is inspected.

Use \autoref{thm:metacicclosuretrace-substitution-shift-generator-package}. It identifies $S,U,V,G$ as the finite generator package for the closedness classifier, with $U$ and $V$ supplying the substitution coordinates and $S$ supplying the boundary shift coordinate.

The row $K$ then records kernel acceptance for the displayed ClosurePreservation declarations. The substitution audit-map boundary supplies the prose routing discipline: accepted closed-substitution reads stop at closedness preservation and do not acquire typing, conversion, confluence, normalization, subject-reduction, or kernel-soundness data from any hidden carrier coordinate.
\end{proof}

\begin{theorem}[MetaCICClosureTrace beta-star preservation readback]
\label{thm:metacicclosuretrace-beta-star-preservation-readback}
Every downstream consumer that reads finite beta-chain closedness from an accepted $\MetaCICClosureTraceUp$ carrier receives it through the route
$$
B,R,H,C,P,N.
$$
The one-step beta row $B$ supplies the local preservation step, the finite beta-chain row $R$ supplies the iterated readback, and the route may cite the checked MetaCIC theorem \texttt{BEDC.MetaCIC.betaStarStep\_preserves\_closed} as the kernel-accepted declaration recorded by $K$. The readback preserves only \texttt{ClosedAt}; it exports no typing, conversion, confluence, or normalization conclusion.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_beta\_star\_preservation\_readback}
\begin{proof}
Begin with the beta rows. The row $B$ names the one-step closedness preservation fact, and the row $R$ names its finite beta-chain closure.

Use the kernel trace row $K$ only as an acceptance ledger for the named declaration. This ledger records that the finite beta-chain theorem is part of the accepted ClosurePreservation surface; it does not add a separate semantic premise.

Now replay the consumer route through $H,C,P,N$. Componentwise transport keeps $B$ and $R$ aligned, the continuation route exposes the finite read, provenance identifies the ClosurePreservation audit map, and the local NameCert row names the packet. Because the carrier has no typing or conversion coordinate, the readback stops at closedness preservation.
\end{proof}

\begin{theorem}[MetaCICClosureTrace NameCert obligations]
\label{thm:metacicclosuretrace-namecert-obligations}
For the carrier of \autoref{def:metacicclosuretrace-carrier}, the local $\NameCert_{\MetaCICClosureTraceUp}$ surface consists exactly of carrier admission for
$$
T=(S,U,V,B,R,G,K,H,C,P,N),
$$
the five ClosurePreservation theorem rows $S,U,V,B,R$, generator-closure readback through $G$, kernel-acceptance trace accounting through $K$, componentwise transport through $H$, displayed continuation reads through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is the first projection of the displayed packet $T$. This projection fixes the five theorem rows before the generator, kernel trace, transport, route, provenance, or naming rows are read.

The generator and kernel obligations are then forced by $G$ and $K$. The row $G$ exposes only the shift and substitution generator-closure witnesses, while $K$ records accepted ClosurePreservation declarations and their dependency ledger.

The remaining obligations are exactly the public routing rows. The row $H$ supplies componentwise $\hsame$ transport, $C$ supplies continuation replay, $P$ supplies package provenance, and $N$ supplies local naming. No carrier coordinate remains from which a stronger MetaCIC theorem could be exported.
\end{proof}

\begin{theorem}[MetaCICClosureTrace substitution beta-chain boundary]
\label{thm:metacicclosuretrace-substitution-beta-chain-boundary}
Every accepted $\MetaCICClosureTraceUp$ consumer that combines the substitution rows $S,U,V,G$ with the beta rows $B,R$ must still read only \texttt{ClosedAt} preservation through $K,H,C,P,N$. The combined route may replay shift, substitution, one-step beta, and finite beta-chain closedness preservation, but it cannot export typing preservation, confluence, normalization, subject reduction, conversion soundness, or kernel soundness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_substitution\_beta\_chain\_boundary}
\begin{proof}
First read the substitution side through \autoref{thm:metacicclosuretrace-substitution-shift-generator-package} and \autoref{thm:metacicclosuretrace-closed-substitution-route}. These results identify $S,U,V,G$ as a closedness-generator package and force the closed-substitution route to stop at \texttt{ClosedAt} preservation.

Next read the beta side through \autoref{thm:metacicclosuretrace-beta-star-preservation-readback}. It exposes only the one-step row $B$ and finite-chain row $R$ as closedness preservation data.

Finally apply \autoref{thm:metacicclosuretrace-namecert-obligations}. The only remaining public route is $K,H,C,P,N$, whose kernel ledger, transport, replay, provenance, and naming rows preserve the displayed ClosurePreservation declarations. Since the carrier has no coordinate for typing, confluence, normalization, subject reduction, conversion soundness, or kernel soundness, the combined consumer cannot export any of those stronger claims.
\end{proof}

\begin{theorem}[MetaCICClosureTrace kernel ledger boundary]
\label{thm:metacicclosuretrace-kernel-ledger-boundary}
For every accepted $\MetaCICClosureTraceUp$ carrier $T=(S,U,V,B,R,G,K,H,C,P,N)$, the kernel-acceptance row $K$ is only an acceptance ledger for the displayed ClosurePreservation declarations. It cannot be used as a hidden kernel-soundness premise, a typing derivation, a confluence witness, a normalization witness, or a subject-reduction witness.
\end{theorem}
\begin{proof}
Project the row $K$ from the carrier. By \autoref{thm:metacicclosuretrace-namecert-obligations}, this row accounts for accepted ClosurePreservation declarations and their displayed dependency ledger.

The substitution and beta routes do not enlarge that ledger. The route of \autoref{thm:metacicclosuretrace-closed-substitution-route} reads $K$ after $S,U,V,G$, while \autoref{thm:metacicclosuretrace-beta-star-preservation-readback} reads $K$ only as acceptance evidence for the beta-chain closedness row.

Thus $K$ remains a ledger coordinate rather than a semantic kernel premise. The stronger objects named in the statement are absent from \autoref{def:metacicclosuretrace-carrier}, so no accepted consumer can recover them from the packet.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_kernel\_ledger\_semantic\_name\_certificate}

\begin{theorem}[MetaCICClosureTrace obligation-closure package]
\label{thm:metacicclosuretrace-obligation-closure-package}
The substitution-shift generator package of \autoref{thm:metacicclosuretrace-substitution-shift-generator-package}, the beta-star preservation readback of \autoref{thm:metacicclosuretrace-beta-star-preservation-readback}, the local NameCert obligations of \autoref{thm:metacicclosuretrace-namecert-obligations}, and the kernel-ledger boundary of \autoref{thm:metacicclosuretrace-kernel-ledger-boundary} jointly exhaust the obligation-closure surface for the $\MetaCICClosureTraceUp$ packet. The package exposes only shift, substitution, beta-step, finite beta-chain closedness, generator readback, kernel acceptance ledger, transport, replay, provenance, and local naming; it exports no typing preservation, confluence, normalization, subject reduction, conversion soundness, or kernel soundness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_obligation\_closure\_package}
\begin{proof}
Project the carrier of \autoref{def:metacicclosuretrace-carrier}. The rows $S,U,V,G$ are the substitution and shift generator package, while $B,R$ are the one-step beta and finite beta-chain closedness rows.

Apply the substitution-shift generator theorem to bind $S,U,V,G$ as the complete closedness-generator surface. Apply the beta-star readback theorem to bind $B,R$ as the only finite beta-chain read available to downstream consumers.

Now use the local NameCert obligations. They account for carrier admission, ClosurePreservation theorem rows, generator readback, kernel-trace accounting, componentwise transport, continuation replay, provenance, and local naming.

Finally use the kernel-ledger boundary. The ledger row $K$ remains acceptance accounting for displayed ClosurePreservation declarations and is not a semantic premise for stronger MetaCIC results. Since the carrier has no row for typing preservation, confluence, normalization, subject reduction, conversion soundness, or kernel soundness, the listed rows exhaust the obligation-closure package.
\end{proof}

\begin{theorem}[MetaCICClosureTrace obligation-closure forwarder]
\label{thm:metacicclosuretrace-obligation-closure-forwarder}
For every accepted $\MetaCICClosureTraceUp$ carrier $T=(S,U,V,B,R,G,K,H,C,P,N)$, the rows $S,U,V,B,R,G,K,H,C,P,N$ jointly form the obligation-closure surface exported to closure-preservation consumers. The exported surface consists only of shift closedness, variable-substitution closedness, full-substitution closedness, one-step beta closedness, finite beta-chain closedness, generator readback, kernel-trace ledger accounting, componentwise transport, replay, provenance, and local naming. It refuses typing preservation, conversion, confluence, normalization, subject reduction, and hidden kernel soundness.
\end{theorem}
\begin{proof}
First apply \autoref{thm:metacicclosuretrace-substitution-shift-generator-package}. It identifies $S,U,V,G$ as the displayed generator package for shift and substitution closedness.

Next apply \autoref{thm:metacicclosuretrace-beta-star-preservation-readback}. It identifies $B,R$ as the one-step and finite beta-chain closedness readback route and keeps that readback at \texttt{ClosedAt} preservation.

Now apply \autoref{thm:metacicclosuretrace-kernel-ledger-boundary}. The row $K$ remains only an acceptance ledger for displayed ClosurePreservation declarations, not a hidden semantic premise.

Finally apply \autoref{thm:metacicclosuretrace-namecert-obligations}. The rows $H,C,P,N$ provide componentwise transport, replay, provenance, and local naming for the same finite packet. Since no carrier coordinate stores typing, conversion, confluence, normalization, subject reduction, or kernel soundness, the forwarder exports exactly the listed obligation-closure surface.
\end{proof}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_obligation\_closure\_forwarder}

\begin{theorem}[MetaCICClosureTrace obligation-closure upgrade]
\label{thm:metacicclosuretrace-obligation-closure-upgrade}
The substitution-shift generator package of \autoref{thm:metacicclosuretrace-substitution-shift-generator-package}, beta-star preservation readback of \autoref{thm:metacicclosuretrace-beta-star-preservation-readback}, NameCert obligations of \autoref{thm:metacicclosuretrace-namecert-obligations}, kernel-ledger boundary of \autoref{thm:metacicclosuretrace-kernel-ledger-boundary}, and candidate frontier of \autoref{thm:metacic-closure-trace-candidate-frontier} jointly form the obligationClosure package for $\MetaCICClosureTraceUp$. The package exports only finite \texttt{ClosedAt} preservation across shift, substitution, beta-step, finite beta-chain, generator readback, kernel-ledger accounting, transport, replay, provenance, and local naming. It exports no typing theorem, confluence theorem, normalization theorem, subject-reduction theorem, conversion-soundness theorem, or kernel-soundness theorem.
\end{theorem}
\begin{proof}
Begin with the generator package. The substitution-shift theorem identifies $S,U,V,G$ as the complete closedness-generator surface for shift and substitution reads.

Attach the beta route next. The beta-star preservation readback identifies $B,R$ as the displayed one-step and finite-chain closedness rows, and the NameCert obligations bind those rows to carrier admission, transport, replay, provenance, and local naming.

Use the kernel-ledger boundary to keep $K$ as acceptance accounting for ClosurePreservation declarations rather than as a hidden semantic premise. The candidate-frontier theorem then orders the whole route as
$$
S,U,V,G \longmapsto B,R \longmapsto K \longmapsto H,C,P,N .
$$

Thus the combined package reaches obligationClosure for the paper packet by preserving only \texttt{ClosedAt}. Since no carrier row stores typing, confluence, normalization, subject reduction, conversion soundness, or kernel soundness, none of those stronger claims is exported.
\end{proof}

\begin{theorem}[MetaCICClosureTrace candidate frontier]
\label{thm:metacic-closure-trace-candidate-frontier}
Every normalization-facing, confluence-facing, or conversion-facing read of an accepted $\MetaCICClosureTraceUp$ packet must pass through the displayed candidate frontier
$$
S,U,V,G \longmapsto B,R \longmapsto K \longmapsto H,C,P,N .
$$
The first stage is the substitution-and-shift generator package, the second stage is the beta-star closedness chain, the third stage is the kernel ledger for accepted ClosurePreservation declarations, and the final stage is componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. The frontier exports only finite \texttt{ClosedAt} preservation for the displayed trace; it exports no hidden subject-reduction hypothesis, no host reduction equality, no unrestricted strong-normalization principle, and no confluence statement outside the finite candidate trace.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_candidate\_frontier}
\begin{proof}
Start with the generator side. The substitution-shift generator package identifies $S,U,V,G$ as the complete closedness-generator surface for shift and substitution reads, while the closed-substitution route forces any closed-substitution consumer to pass through those rows before using the ledger.

Next attach the beta-chain side. The beta-star preservation readback exposes $B,R$ as the one-step and finite-chain closedness rows, and it states that the readback stops at \texttt{ClosedAt} preservation.

Now read the kernel boundary. The kernel-ledger boundary keeps $K$ as acceptance accounting for displayed ClosurePreservation declarations rather than as a semantic subject-reduction or kernel-soundness premise.

Finally package the route through the obligation-closure theorem. The rows $H,C,P,N$ provide transport, replay, provenance, and naming for the same finite packet. Since the carrier has no coordinate for host reduction equality, unrestricted strong normalization, full confluence, subject reduction, typing preservation, conversion soundness, or kernel soundness, every such consumer remains on the displayed candidate frontier.
\end{proof}

\begin{theorem}[MetaCICClosureTrace candidate-mediated closedness ledger]
\label{thm:metacicclosuretrace-candidate-mediated-closedness-ledger}
Every downstream closedness consumer of an accepted $\MetaCICClosureTraceUp$ packet $T=(S,U,V,B,R,G,K,H,C,P,N)$ must read the candidate-mediated route in the order $S,U,V,G$, then $B,R$, then $K$, then $H,C,P,N$. The exported ledger is only a finite \texttt{ClosedAt} preservation route through substitution, shift, beta-step, finite beta-chain, kernel acceptance accounting, transport, replay, provenance, and local naming. It exports no subject reduction, no confluence, no normalization, no conversion theorem, no host reduction equality, and no kernel soundness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICClosureTraceUp.MetaCICClosureTraceCarrier\_candidate\_mediated\_closedness\_ledger}
\begin{proof}
First read the substitution-shift generator package. It identifies $S,U,V,G$ as the finite candidate generator surface through which closed-substitution and shift reads enter the packet.

Next use the closed-substitution route and beta-star preservation readback. The former keeps substitution reads at \texttt{ClosedAt} preservation, and the latter attaches the one-step beta row $B$ and finite-chain row $R$ without introducing typing or conversion data.

Now apply the kernel ledger boundary. The row $K$ records accepted ClosurePreservation declarations and their dependency ledger only as accounting, not as a hidden semantic proof of subject reduction, confluence, normalization, conversion, host reduction equality, or kernel soundness.

Finally apply the candidate frontier nonescape route. The rows $H,C,P,N$ transport, replay, source, and name the same finite packet, so the candidate-mediated ledger exports only \texttt{ClosedAt} preservation for the displayed trace.
\end{proof}

\closureat{MetaCICClosureTraceUp}{seedStr}

\begin{closurestatus}{\MetaCICClosureTraceUp}
  \constructivestory{A $\MetaCICClosureTraceUp$ packet is generated from five MetaCIC ClosurePreservation theorem rows, shift and substitution generator-closure rows, kernel-acceptance trace rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacicclosuretrace-carrier}, \autoref{thm:metacicclosuretrace-substitution-shift-generator-package}, \autoref{thm:metacicclosuretrace-beta-star-preservation-readback}, and \autoref{thm:metacicclosuretrace-namecert-obligations} bind the finite closure-trace packet over MetaCIC shift, substitution, beta-step, and finite beta-chain closedness preservation.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not prove subject reduction, type preservation, confluence, normalization, kernel soundness, checked Lean carrier obligations, or a public bridge for the closure-trace packet.}
  \upgradepath{Obligation closure requires checked carrier admission, generator package exactness, beta-chain readback exactness, kernel-trace accounting, route replay, provenance, local naming, and non-escape for the same finite packet.}
\end{closurestatus}
