\chapter{A Concrete Naming Certificate for $\BihariLaSalleUp$}
\label{ch:concrete-instances-bihari-la-salle-namecert}
\origin{human}

Following Bihari and LaSalle, the $\BihariLaSalleUp$ packet records the finite nonlinear integral-inequality surface used when a nonnegative comparison function is controlled by an Osgood-type modulus before a Real-facing stability consumer reads the bound. It sits after the linear Gronwall route of \autoref{ch:concrete-instances-gronwall-inequality-namecert}, the Bellman integral-comparison route of \autoref{ch:concrete-instances-gronwall-bellman-namecert}, the finite integration rows of \autoref{ch:concrete-instances-riemannintegral-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only interval, unknown-envelope, window, Osgood-modulus, inverse-modulus, readback, Real-seal, transport, replay, provenance, and local naming rows.

\begin{definition}[Bihari--LaSalle carrier]
\label{def:bihari-la-salle-carrier}
A $\BihariLaSalleUp$ carrier is a finite $\BHist$ packet
$$
B=(I,U,W,O,G,R,Z,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $I$ is the interval row, recording the displayed time window, endpoint names, and the finite partition cells on which the nonlinear comparison is inspected.
\item $U$ is the unknown-envelope row, storing the nonnegative comparison quantity and its initial bound as finite $\RealUp$-readable data.
\item $W$ is the weight row, recording the displayed integral coefficient, cellwise majorant, and finite error budget used by the comparison.
\item $O$ is the Osgood-modulus row, displaying the nondecreasing positive modulus and the finite inverse-modulus windows on which the Bihari separation read is made.
\item $G$ is the Gronwall/Osgood handoff row, identifying the linear Gronwall route when $O$ is linear and the nonlinear Osgood route when the inverse-modulus ledger is required.
\item $R$ is the $\RegSeqRatUp$ readback row for the interval endpoints, envelope samples, coefficient bounds, Osgood-modulus values, inverse-modulus windows, and comparison budget.
\item $Z$ is the $\RealUp$ seal row for the final Bihari--LaSalle bound after the finite rows have been replayed.
\item $H$ records componentwise $\hsame$ transport for the interval, envelope, weight, Osgood-modulus, handoff, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
I,U,W,O,G,R \longmapsto Z \longmapsto \NameCert_{\BihariLaSalleUp}.
$$
\item $P$ is the $\Pkg$ provenance over $\BihariLaSalleUp$, $\GronwallInequalityUp$, $\GronwallBellmanUp$, $\RiemannIntegralUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BihariLaSalleUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through these rows and their displayed transports. It has no coordinate for a host nonlinear integral operator, selected inverse function, quotient real equality, countable choice, ambient $\RealUp$ completeness theorem, or analytic ODE solver.
\end{definition}
\leanstmt{BEDC.Derived.BihariLaSalleUp}

\begin{theorem}[Bihari--LaSalle NameCert obligations]
\label{thm:bihari-la-salle-namecert-obligations}
For every accepted $\BihariLaSalleUp$ carrier $B=(I,U,W,O,G,R,Z,H,C,P,N)$, the local $\NameCert_{\BihariLaSalleUp}$ surface consists exactly of carrier admission for the displayed interval, unknown-envelope, weight, Osgood-modulus, Gronwall/Osgood handoff, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming rows. No obligation is discharged by a host integral theorem, selected inverse-modulus function, quotient real equality, countable-choice schedule, ambient Real completeness, or external ODE solver.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:bihari-la-salle-carrier}. The rows $I,U,W,O,G,R,Z$ are the only analytic-facing coordinates, and the structural rows $H,C,P,N$ only transport, replay, source, and name those coordinates.

The interval, envelope, and weight rows determine the finite nonlinear comparison problem. The Osgood-modulus row supplies the displayed modulus and inverse-window ledger, while the handoff row identifies the linear Gronwall branch as a special finite route and the nonlinear branch as the inverse-modulus route.

The regular-rational readback and Real seal bind the comparison to displayed observations. Since every endpoint of the comparison passes through $R$ before $Z$ is consumed, the certificate cannot import a host integral theorem, selected inverse function, quotient equality, choice schedule, completeness theorem, or external solver.
\end{proof}

\begin{theorem}[Bihari--LaSalle Gronwall handoff]
\label{thm:bihari-la-salle-gronwall-handoff}
Every accepted $\BihariLaSalleUp$ carrier routes a nonlinear integral-inequality read through the finite handoff
$$
\GronwallBellmanUp \longmapsto \BihariLaSalleUp \longmapsto \RegSeqRatUp \longmapsto \RealUp.
$$
When the Osgood-modulus row $O$ is linear, the handoff row $G$ exposes the Gronwall comparison of \autoref{ch:concrete-instances-gronwall-inequality-namecert}. When $O$ is nonlinear, $G$ keeps the read on the displayed inverse-modulus windows before the regular-rational readback $R$ and Real seal $Z$ are available.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $B$. The comparison can inspect the interval row $I$, unknown envelope $U$, and weight row $W$ before it reaches any bound.

Read the Osgood-modulus row next. If the displayed modulus is the linear one, the row $G$ cites the Gronwall and Bellman comparison routes already present in the sibling chapters. Otherwise the same row keeps the comparison inside the inverse-modulus windows listed by $O$.

Finally expose $R$ and $Z$. The regular-rational row records the finite observations from both branches, and the Real seal is terminal for those observations. Thus the handoff cannot bypass the displayed rows through host integration, selected inverse functions, quotient equality, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Bihari--LaSalle Real seal nonescape]
\label{thm:bihari-la-salle-real-seal-nonescape}
For every accepted $\BihariLaSalleUp$ carrier $B=(I,U,W,O,G,R,Z,H,C,P,N)$, the $\RealUp$ seal row $Z$ exposes only the bound obtained from the interval, unknown-envelope, weight, Osgood-modulus, Gronwall/Osgood handoff, and $\RegSeqRatUp$ readback rows. It exports no host nonlinear integral value, selected inverse-modulus function, quotient stream equality, countable-choice schedule, ambient $\RealUp$ completeness result, or analytic ODE continuation theorem.
\end{theorem}
\begin{proof}
Use \autoref{thm:bihari-la-salle-namecert-obligations} to identify the local obligation surface. The seal row is listed there only as the terminal read of the finite carrier rows.

Use \autoref{thm:bihari-la-salle-gronwall-handoff} to locate the route into $Z$. The handoff opens $I,U,W,O,G,R$ before the Real seal is available, so the seal cannot be an independent host integral value or selected inverse-modulus function.

The structural rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and local naming. Therefore a consumer of $Z$ receives only the finite Bihari--LaSalle comparison packet and none of the excluded host analytic objects.
\end{proof}

\falsifiablePrediction{Every accepted $\BihariLaSalleUp$ consumer that reads a Real nonlinear comparison bound without the displayed interval, unknown-envelope, weight, Osgood-modulus, Gronwall/Osgood handoff, RegSeqRat readback, transport, replay, provenance, and local NameCert rows refutes the packet surface.}

\independenceWitness{gronwall-inequality, gronwall-bellman, riemannintegral, regseqrat, real: none is bijective to $\BihariLaSalleUp$ because this packet binds finite nonlinear modulus data, inverse-window readback, Gronwall/Osgood handoff, Real seal, transport, replay, provenance, and local naming into one integral-inequality certificate.}

\closureat{BihariLaSalleUp}{seedStr}

\begin{closurestatus}{\BihariLaSalleUp}
  \constructivestory{A $\BihariLaSalleUp$ packet is generated from one interval row, one unknown-envelope row, one weight row, one Osgood-modulus row, one Gronwall/Osgood handoff row, one RegSeqRat readback row, one RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bihari-la-salle-carrier}, \autoref{thm:bihari-la-salle-namecert-obligations}, \autoref{thm:bihari-la-salle-gronwall-handoff}, and \autoref{thm:bihari-la-salle-real-seal-nonescape} seed the finite nonlinear integral-inequality packet over interval, envelope, coefficient, Osgood-modulus, inverse-window, readback, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\encodedDefV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host nonlinear integral operators, selected inverse-modulus functions, quotient real equality, countable choice, ambient $\RealUp$ completeness, analytic ODE continuation, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Osgood-modulus stability, Gronwall/Osgood handoff, inverse-window accounting, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BihariLaSalleUp.}
\end{closurestatus}
