\chapter{A Concrete Naming Certificate for $\DiniDerivativeUp$}
\label{ch:concrete-instances-dini-derivative-namecert}
\origin{human}

Following Dini's one-sided derivative theory and Bishop--Bridges constructive analysis, the $\DiniDerivativeUp$ packet records only displayed finite one-sided secant-window rows over $\RealUp$, $\ContinuousMapUp$, $\DerivativeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\StreamNameUp$, and $\MetricUp$. It consumes the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, the graph and modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the derivative quotient boundary of \autoref{ch:concrete-instances-derivative-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, and the metric comparison rows of \autoref{ch:concrete-instances-metric-namecert}. The packet names only finite right-hand and left-hand slope windows, their rational bounds, endpoint seals, componentwise transport, replay, provenance, and local naming; it does not import host limits, trichotomy, quotient real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Dini derivative carrier]
\label{def:dini-derivative-carrier}
A $\DiniDerivativeUp$ carrier is a finite $\BHist$ packet
$$
D=(X,F,Q,S,B,R,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the local $\MetricUp$ source row naming the inspected interval cell, its endpoint order, and the distance comparison used to choose one-sided neighborhoods.
\item $F$ is the $\ContinuousMapUp$ row for the displayed function on the same metric cell, including the finite graph reads and continuity modulus rows visible to the packet.
\item $Q$ is the $\DerivativeUp$ quotient-inspection row, recording the secant quotient route without asserting a two-sided derivative or a selected limit.
\item $S$ is the finite $\StreamNameUp$ one-sided window row, listing the inspected right-hand and left-hand approach samples.
\item $B$ is the $\DyadicRatCoreUp$ slope-bound row, storing the dyadic lower and upper rational bounds for every displayed one-sided secant window.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational approximations of endpoint values, secant numerators, step sizes, and dyadic slope bounds.
\item $W$ is the boundary-seal row, storing the $\RealUp$ endpoint names and the finite evidence that the requested Dini read is confined to the displayed side and window.
\item $H$ records componentwise $\hsame$ transport for the metric source, continuous-map row, derivative quotient route, stream windows, dyadic bounds, rational readbacks, endpoint seals, replay rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
X,F,Q,S,B,R,W \longmapsto \NameCert_{\DiniDerivativeUp}.
$$
\item $P$ is the $\Pkg$ provenance over $\DiniDerivativeUp$, $\RealUp$, $\ContinuousMapUp$, $\DerivativeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\MetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\DiniDerivativeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host limit, trichotomy of reals, quotient equality of completed reals, ambient $\RealUp$ completeness, or a standard bridge.
\end{definition}

\begin{theorem}[Dini derivative NameCert obligations]
\label{thm:dini-derivative-namecert-obligations}
For every accepted $\DiniDerivativeUp$ carrier
$$
D=(X,F,Q,S,B,R,W,H,C,P,N),
$$
the local $\NameCert_{\DiniDerivativeUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item metric cell admission through $X$;
\item continuous-map row admission through $F$ over the same cell;
\item derivative quotient inspection through $Q$;
\item one-sided finite window admission through $S$;
\item dyadic slope-bound admission through $B$;
\item regular rational readback through $R$;
\item endpoint and side boundary sealing through $W$;
\item structural stability through $H,C,P,N$.
\end{enumerate}
No Dini derivative obligation is stored outside the displayed carrier.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:dini-derivative-carrier}. The metric source row $X$ fixes the inspected cell and the endpoint side, while $F$ supplies the only continuous-map data visible to the packet.

The derivative-facing part is then finite. The row $Q$ records the quotient route being inspected, and $S$ lists the right-hand and left-hand sample windows on which that route may be read.

The bound rows are also displayed. The dyadic row $B$ stores the rational lower and upper slope bounds, and $R$ supplies the regular rational readback for the endpoint values, step sizes, and bound comparisons.

Finally, $W$ seals the endpoint and side information, while $H,C,P,N$ transport, replay, source, and name the same row inventory. Since the carrier has no other coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Dini derivative finite slope window]
\label{thm:dini-derivative-finite-slope-window}
Every one-sided slope read exported by an accepted $\DiniDerivativeUp$ carrier factors through the displayed finite route
$$
\MetricUp \longmapsto \ContinuousMapUp \longmapsto \DerivativeUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp.
$$
The read first opens the metric cell and continuous-map row, then the derivative quotient route, then a finite one-sided stream window, then the dyadic slope-bound row, and only then seals the observed boundary as $\RealUp$ data.
\end{theorem}
\begin{proof}
Start with an accepted carrier $D=(X,F,Q,S,B,R,W,H,C,P,N)$. The packet exposes no slope source before the metric row $X$ and continuous-map row $F$ have been read.

Inspect the derivative row $Q$ next. Its quotient data is not a host derivative; it is only the displayed route whose one-sided samples are listed by $S$.

The finite window is bounded through $B$ and read back through $R$. These two rows provide the dyadic lower and upper slope bounds and the rational observations required to compare the displayed secant quotients.

The boundary row $W$ then seals the observation as $\RealUp$ data on the chosen side. Transport and replay through $H$ and $C$ preserve the same route, and $P,N$ add only source and naming information. Thus every exported one-sided slope read factors through the stated finite route.
\end{proof}

\begin{theorem}[Dini derivative real boundary]
\label{thm:dini-derivative-real-boundary}
Every $\RealUp$ boundary read exported by an accepted $\DiniDerivativeUp$ carrier is downstream of a displayed one-sided secant-window comparison. In particular, if a consumer reaches the boundary row $W$, then the consumer has already read the same metric cell $X$, continuous-map row $F$, derivative quotient row $Q$, stream-window row $S$, dyadic slope-bound row $B$, regular rational readback $R$, componentwise transport $H$, replay $C$, package provenance $P$, and local name $N$.
\end{theorem}
\begin{proof}
The carrier places the $\RealUp$ boundary information in the single row $W$. By the definition of the carrier, $W$ is not independent data: it records only endpoint names and side confinement for the displayed one-sided window.

To reach $W$, a consumer must therefore pass through the rows that make the window meaningful. The metric row $X$ supplies the side and neighborhood comparison, $F$ supplies the continuous-map observations, $Q$ supplies the quotient route, $S$ supplies the sampled windows, and $B,R$ supply the dyadic and regular-rational comparison data.

The structural rows do not add a second boundary route. They preserve the same packet under $\hsame$, replay it through $\Cont$, record its provenance, and assign the local $\NameCert_{\DiniDerivativeUp}$ row.

Consequently the boundary read cannot be detached from the displayed finite secant-window comparison, and it cannot be replaced by a host limit, real trichotomy, quotient real equality, or ambient completeness argument.
\end{proof}

\falsifiablePrediction{Every $\DiniDerivativeUp$ one-sided slope read factors through one metric cell row, one ContinuousMap row, one Derivative quotient row, one StreamName finite window row, one DyadicRatCore slope-bound row, one RegSeqRat readback row, one RealUp boundary-seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{real, continuousmap, derivative, dyadicratcore, regseqrat, streamname, metric: $\DiniDerivativeUp$ is not bijective to any listed sibling because it jointly carries one-sided finite secant windows, derivative quotient inspection, dyadic slope bounds, rational readback, endpoint side sealing, transport, replay, provenance, and local naming as one boundary certificate.}

\closureat{DiniDerivativeUp}{seedStr}

\begin{closurestatus}{\DiniDerivativeUp}
  \constructivestory{A $\DiniDerivativeUp$ packet is generated from a metric cell row, a ContinuousMap row, a Derivative quotient route, StreamName one-sided finite windows, DyadicRatCore slope bounds, RegSeqRat readbacks, a RealUp boundary-seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dini-derivative-carrier}, \autoref{thm:dini-derivative-namecert-obligations}, \autoref{thm:dini-derivative-finite-slope-window}, and \autoref{thm:dini-derivative-real-boundary} seed the finite one-sided derivative-window certificate over metric, continuous-map, quotient, stream, dyadic, rational, and Real boundary rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host limits, real trichotomy, quotient equality of completed reals, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, one-sided window admission, dyadic slope-bound admission, rational readback, endpoint-side sealing, transport, replay, provenance, and local naming for BEDC.Derived.DiniDerivativeUp.}
\end{closurestatus}
