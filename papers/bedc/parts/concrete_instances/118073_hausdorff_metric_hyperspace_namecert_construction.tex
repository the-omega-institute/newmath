\chapter{A Concrete Naming Certificate for $\HausdorffMetricHyperspaceUp$}
\label{ch:concrete-instances-hausdorff-metric-hyperspace-namecert}
\origin{human}

Following Hausdorff and Blaschke, the $\HausdorffMetricHyperspaceUp$ packet records the distance-ledger specialization of the compact-metric hyperspace: nonempty compact-subset names over one displayed compact metric source are compared by finite directed Hausdorff covers before a hyperspace consumer reads the result. It sits beside the compact metric hyperspace of \autoref{ch:concrete-instances-compact-metric-hyperspace-namecert}, the scalar Hausdorff comparison surface of \autoref{ch:concrete-instances-hausdorff-metric-namecert}, the Vietoris hyperspace of \autoref{ch:concrete-instances-vietoris-hyperspace-namecert}, and the Blaschke selection route of \autoref{ch:concrete-instances-blaschke-selection-compact-hyperspace-namecert}. The packet names a finite Hausdorff metric ledger, not a host powerset metric, quotient equality of compact subsets, selected nearest points, open-cover compactness, or ambient hyperspace completion.

\begin{definition}[Hausdorff metric hyperspace carrier]
\label{def:hausdorff-metric-hyperspace-carrier}
A $\HausdorffMetricHyperspaceUp$ carrier is a finite $\BHist$ packet
$$
H=(X,K,A,B,D,S,V,L,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ source row, including the metric, finite-net, total-bounded, and complete boundary rows used by compact-subset reads.
\item $K$ is the $\CompactMetricHyperspaceUp$ row naming the nonempty compact-subset carrier over $X$.
\item $A$ and $B$ are the two displayed compact-subset names being compared inside $K$.
\item $D$ is the directed Hausdorff coverage ledger, carrying one finite witness from $A$ to $B$ and one finite witness from $B$ to $A$ at every admitted tolerance.
\item $S$ is the symmetric scalar readback row obtained from the two directed coverage ledgers.
\item $V$ is the $\VietorisHyperspaceUp$ compatibility row, recording that the same finite hit-and-miss windows are read when a topology-facing consumer inspects $A$ and $B$.
\item $L$ is the Blaschke handoff row, exposing only the distance ledger and compact source rows needed by \autoref{ch:concrete-instances-blaschke-selection-compact-hyperspace-namecert}.
\item $T$ records componentwise $\hsame$ transport for $X,K,A,B,D,S,V,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay of the compact-source, compact-subset, directed-coverage, symmetric-readback, Vietoris, and Blaschke-handoff route.
\item $P$ is the $\Pkg$ provenance over $\HausdorffMetricHyperspaceUp$, $\CompactMetricHyperspaceUp$, $\HausdorffMetricUp$, $\VietorisHyperspaceUp$, $\BlaschkeSelectionCompactHyperspaceUp$, $\CompactMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HausdorffMetricHyperspaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,K,A,B,D,S,V,L,T,C,P,N$. It has no coordinate for host powersets, quotient compacta, selected nearest points, open-cover compactness, or a hidden hyperspace completion object.
\end{definition}

\begin{theorem}[Hausdorff metric hyperspace NameCert obligations]
\label{thm:hausdorff-metric-hyperspace-namecert-obligations}
For every accepted $\HausdorffMetricHyperspaceUp$ carrier
$$
H=(X,K,A,B,D,S,V,L,T,C,P,N),
$$
the local $\NameCert_{\HausdorffMetricHyperspaceUp}$ surface is exhausted by compact metric source admission, compact-metric hyperspace admission, two compact-subset rows, bidirectional finite Hausdorff coverage, symmetric scalar readback, Vietoris compatibility, Blaschke handoff, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hausdorff-metric-hyperspace-carrier}. The source row $X$ and hyperspace row $K$ are the only places where compact metric and compact-subset data enter the packet.

The two compact-subset names are the rows $A$ and $B$. The directed ledger $D$ then supplies both finite coverage directions before the symmetric readback row $S$ can be consumed.

The topology and compactness handoffs are $V$ and $L$. They reuse the same directed coverage rows for Vietoris-facing and Blaschke-facing consumers rather than importing host open-cover compactness or selected representatives.

Finally $T,C,P,N$ transport, replay, source, and name the same finite route. Since no further coordinate exists in the carrier, the listed rows exhaust the local naming surface.
\end{proof}

\begin{theorem}[Hausdorff metric hyperspace compact-set handoff]
\label{thm:hausdorff-metric-hyperspace-compact-set-handoff}
Every compact-set consumer of an accepted $\HausdorffMetricHyperspaceUp$ packet factors through
$$
\CompactMetricUp \longmapsto \CompactMetricHyperspaceUp \longmapsto \HausdorffMetricUp \longmapsto \VietorisHyperspaceUp .
$$
The consumer receives compact source rows, compact-subset names, two directed finite Hausdorff coverage ledgers, symmetric readback, Vietoris compatibility, Blaschke handoff, transport, replay, provenance, and local naming; it receives no host powerset, quotient compactum, selected nearest point, open-cover compactness theorem, or ambient completion witness.
\end{theorem}
\begin{proof}
Use \autoref{thm:hausdorff-metric-hyperspace-namecert-obligations}. It forces every public read through the compact metric source $X$, the compact-metric hyperspace row $K$, and the compact-subset rows $A,B$.

The Hausdorff metric read is then the finite route through $D$ and $S$. Both directed coverage directions are displayed before the scalar comparison can be exported.

Vietoris and Blaschke consumers receive only the rows $V$ and $L$ attached to the same finite ledger. The structural rows $T,C,P,N$ preserve and name that route, so compact-set handoff cannot escape to host powersets, quotient compacta, selected nearest points, open-cover compactness, or ambient completion.
\end{proof}

\falsifiablePrediction{An accepted $\HausdorffMetricHyperspaceUp$ compact-set read that exposes symmetric distance data without first displaying $X,K,A,B,D,S,V,L,T,C,P,N$ refutes the carrier surface.}

\independenceWitness{compact-metric-hyperspace, hausdorff-metric, vietoris-hyperspace, blaschke-selection-compact-hyperspace: the compact-metric hyperspace names the carrier without isolating the symmetric distance ledger, the Hausdorff metric chapter names directed comparison without the hyperspace handoff, the Vietoris hyperspace names hit-and-miss compatibility without the Blaschke distance ledger, and the Blaschke route consumes compact-set selection data without owning the two directed Hausdorff rows. None is bijective to $\HausdorffMetricHyperspaceUp$.}

\closureat{HausdorffMetricHyperspaceUp}{seedStr}

\begin{closurestatus}{\HausdorffMetricHyperspaceUp}
  \constructivestory{A $\HausdorffMetricHyperspaceUp$ packet is generated from a CompactMetricUp source, a CompactMetricHyperspaceUp compact-subset carrier, two displayed compact-subset names, bidirectional finite Hausdorff coverage, symmetric readback, Vietoris compatibility, Blaschke handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hausdorff-metric-hyperspace-carrier}, \autoref{thm:hausdorff-metric-hyperspace-namecert-obligations}, and \autoref{thm:hausdorff-metric-hyperspace-compact-set-handoff} seed the finite distance-ledger specialization of compact metric hyperspace data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host powersets, quotient compacta, selected nearest points, open-cover compactness, or ambient hyperspace completion.}
  \upgradepath{Obligation closure requires checked carrier admission, bidirectional Hausdorff coverage, symmetric readback, Vietoris compatibility, Blaschke handoff, transport, replay, provenance, and local naming for BEDC.Derived.HausdorffMetricHyperspaceUp.}
\end{closurestatus}
