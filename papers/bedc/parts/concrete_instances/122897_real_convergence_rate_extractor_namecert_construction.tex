\chapter{A Concrete Naming Certificate for $\RealConvergenceRateExtractorUp$}
\label{ch:concrete-instances-real-convergence-rate-extractor-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RealConvergenceRateExtractorUp$ packet records the finite route by which a displayed real-name convergence request is answered from explicit modulus data before any completed real is consumed. It sits over the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the diagonal Cauchy telescope of \autoref{ch:concrete-instances-bishop-diagonal-cauchy-telescope-namecert}. The packet names a constructive convergence-rate extractor for Real and Completion consumers; it is not a selected limit, quotient representative, countable-choice modulus, host real equality, or ambient completeness theorem.

\begin{definition}[Real convergence-rate extractor carrier]
\label{def:real-convergence-rate-extractor-carrier}
A $\RealConvergenceRateExtractorUp$ carrier is a finite $\BHist$ packet
$$
E=(Q,D,W,R,G,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the tolerance-request row, recording the requested output precision and the consumer that asks for a Real-facing convergence read.
\item $D$ is the $\DyadicRatCoreUp$ budget row, translating $Q$ into the dyadic radius and comparison slack used by the extractor.
\item $W$ is the $\StreamNameUp$ finite-window row, exposing the bounded prefix and tail windows inspected for that dyadic request.
\item $R$ is the $\RegSeqRatUp$ readback row, recording the regular rational observations obtained from $W$ under the budget $D$.
\item $G$ is the convergence-rate row, returning a displayed index after which all accepted readbacks meet the requested tolerance.
\item $S$ is the terminal $\RealUp$ seal row, reached only after $Q,D,W,R,G$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for tolerance requests, dyadic budgets, finite windows, regular rational readbacks, convergence-rate rows, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the request row through $D,W,R,G$ to the Real seal row $S$.
\item $P$ is the $\Pkg$ provenance over $\RealConvergenceRateExtractorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealConvergenceRateUp$, $\RealUp$, $\BishopDiagonalCauchyTelescopeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealConvergenceRateExtractorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,D,W,R,G,S,H,C,P,N$. It has no coordinate for a hidden limit selector, quotient representative, unbounded stream scan, countable choice, host equality of real names, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real convergence-rate extractor NameCert obligations]
\label{thm:real-convergence-rate-extractor-namecert-obligations}
For every accepted $\RealConvergenceRateExtractorUp$ carrier $E=(Q,D,W,R,G,S,H,C,P,N)$, the local $\NameCert_{\RealConvergenceRateExtractorUp}$ surface consists exactly of tolerance-request admission, dyadic budget exposure, finite $\StreamNameUp$ window readback, $\RegSeqRatUp$ rational observation, convergence-rate row extraction, terminal $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every Real or Completion consumer that uses the extracted rate must therefore read the finite route
$$
Q,\ D,\ W,\ R,\ G \longmapsto S
$$
before treating the requested convergence as Real-facing evidence.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-convergence-rate-extractor-carrier}. The rows $Q,D,W,R,G$ are the only pre-seal data available to the extractor.

The tolerance request $Q$ is first translated into the dyadic budget $D$. The finite window row $W$ then displays exactly which stream observations are inspected, and the regular rational readback row $R$ records the observations seen on those windows.

The convergence-rate row $G$ is downstream of those displayed rows. It returns an index for the request already budgeted by $D$ and read through $W$ and $R$; it cannot be replaced by a selected limit or an unbounded scan.

Finally the Real seal row $S$ and structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier contains no further coordinate, the listed obligation groups exhaust the local $\NameCert_{\RealConvergenceRateExtractorUp}$ surface.
\end{proof}

\begin{theorem}[Real convergence-rate extractor Cauchy handoff]
\label{thm:real-convergence-rate-extractor-cauchy-handoff}
Every accepted $\RealConvergenceRateExtractorUp$ consumer read that reaches a $\RealUp$ seal or Completion-facing handoff factors through the same dyadic-window-readback route used by the $\BishopDiagonalCauchyTelescopeUp$ packet: a tolerance request $Q$, a $\DyadicRatCoreUp$ budget $D$, a $\StreamNameUp$ finite window $W$, a $\RegSeqRatUp$ readback $R$, and a convergence-rate row $G$ precede the terminal Real seal $S$. Thus convergence evidence for Real and Completion consumers depends on displayed finite modulus data, not on selected limits or quotient representatives.
\end{theorem}
\begin{proof}
Start with an accepted carrier $E=(Q,D,W,R,G,S,H,C,P,N)$. The terminal seal $S$ is not public until the request row, dyadic budget, finite windows, regular rational readback, and convergence-rate row have all been displayed.

Compare this route with \autoref{ch:concrete-instances-bishop-diagonal-cauchy-telescope-namecert}. Both packets force a Real-facing consumer through dyadic tolerance data, finite stream windows, and regular rational readback before any Real seal is available.

The extractor's row $G$ is the finite modulus output attached to that route. Componentwise transport $H$, replay $C$, provenance $P$, and local naming $N$ preserve the same displayed dependency, so a downstream Real or Completion consumer has no coordinate from which to import a selected limit, quotient representative, countable-choice modulus, host real equality, or ambient completeness.
\end{proof}

\falsifiablePrediction{An accepted $\RealConvergenceRateExtractorUp$ consumer that reaches a $\RealUp$ seal or Completion-facing handoff without displaying a tolerance request, DyadicRatCore budget, StreamName finite window, RegSeqRat readback, and convergence-rate row refutes the carrier surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, bishopdiagonalcauchytelescope: $\RealConvergenceRateExtractorUp$ is not bijective to any listed sibling because it binds a tolerance request to a finite extracted convergence-rate row before the Real seal, while the siblings separately supply budgets, windows, readbacks, seals, and diagonal-tail routes.}

\closureat{RealConvergenceRateExtractorUp}{seedStr}

\begin{closurestatus}{\RealConvergenceRateExtractorUp}
  \constructivestory{A $\RealConvergenceRateExtractorUp$ packet is generated from a tolerance request, DyadicRatCore budget, StreamName finite windows, RegSeqRat readback, convergence-rate row, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-convergence-rate-extractor-carrier}, \autoref{thm:real-convergence-rate-extractor-namecert-obligations}, and \autoref{thm:real-convergence-rate-extractor-cauchy-handoff} seed the finite convergence-rate route through dyadic budgets, stream windows, regular rational readback, and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected limits, quotient real equality, countable-choice moduli, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic budget exposure, finite-window readback, regular rational handoff, convergence-rate exactness, Real seal handoff, structural transport, and ledger non-escape for BEDC.Derived.RealConvergenceRateExtractorUp.}
\end{closurestatus}
