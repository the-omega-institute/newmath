\chapter{A Concrete Naming Certificate for $\LocatedInfimumUp$}
\label{ch:concrete-instances-locatedinfimum-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\LocatedInfimumUp$ packet records greatest-lower-bound data by finite lower-bound ledgers and upper-approximation rows, before any ambient completeness principle for $\RealUp$ is available. It is the lower-bound companion to the $\LocatedSupremumUp$ certificate of \autoref{ch:concrete-instances-locatedsupremum-namecert} and consumes the bounded-family source supplied by \autoref{ch:concrete-instances-boundedrealfamily-namecert}. The carrier therefore gives later interval and bounded-family arguments a finite NameCert target for infimum-shaped reasoning while keeping quotient real equality, trichotomy, countable choice, and host completeness outside the packet.

\begin{definition}[LocatedInfimum carrier]
\label{def:located-infimum-carrier}
A $\LocatedInfimumUp$ carrier is a finite $\BHist$ packet
$$
I=(F,L,G,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the bounded real family row, read through the finite index-window, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and common-bound ledgers of $\BoundedRealFamilyUp$.
\item $L$ is the lower-bound ledger. It records the inspected lower-bound claims and the finite comparison rows that certify each requested member of $F$ above the proposed lower surface.
\item $G$ is the greatest-lower-approximation ledger. For each precision request in the displayed window, it stores a finite family-side witness above the proposed lower surface and below every displayed stronger lower-bound candidate at that precision.
\item $W$ is the $\StreamNameUp$ precision-window row that fixes the finite observations of the family, lower-bound ledger, and greatest-lower-approximation ledger in scope.
\item $R$ is the $\RegSeqRatUp$ handoff row that repacks the windowed observations as regular rational readback data.
\item $E$ is the $\RealUp$ seal row reached only through $F,L,G,W,R$.
\item $H$ records componentwise $\hsame$ transports for the family row, lower-bound ledger, greatest-lower-approximation ledger, precision window, regular handoff, real seal, continuation replay, provenance, and local naming row.
\item $C$ records displayed $\Cont$ routes that replay lower-bound reads, greatest-lower-approximation reads, classifier transport, regular-rational handoff, and real-seal readback.
\item $P$ is the $\Pkg$ provenance over $\LocatedInfimumUp$, $\LocatedSupremumUp$, $\BoundedRealFamilyUp$, $\BishopRealComparisonUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\LocatedInfimumUp}$ row.
\end{enumerate}
The accepted carrier stores no Dedekind cut completion, no arbitrary infimum operator, no quotient real equality test, no trichotomy oracle, no countable-choice functional, and no host-completeness coordinate.
\end{definition}
\leanchecked{BEDC.Derived.LocatedInfimumUp.TasteGate.LocatedInfimumTasteGate\_single\_carrier\_alignment}

\begin{theorem}[LocatedInfimum lower-bound obligation]
\label{thm:located-infimum-lower-bound-obligation}
For an accepted packet $I=(F,L,G,W,R,E,H,C,P,N)$, every lower-bound read exposed by the local $\NameCert_{\LocatedInfimumUp}$ row factors through the bounded-family row $F$, the lower-bound ledger $L$, the precision window $W$, the regular-rational handoff $R$, and the $\RealUp$ seal $E$, together with the transport, continuation, provenance, and local naming rows. No lower-bound obligation may invoke Dedekind completeness, an arbitrary infimum operator, quotient real equality, trichotomy, countable choice, or host completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumLowerBoundObligation}
\begin{proof}
Project the accepted carrier. The only family source is $F$, and the only lower-bound source is $L$, so a lower-bound consumer must first inspect the bounded-family row and then one of the displayed comparison entries of $L$.

The precision window $W$ determines which finite observations are admitted. The handoff row $R$ repacks those observations as regular rational data, and the seal $E$ is reached only after this route has been replayed.

The structural rows cannot enlarge the obligation surface. The transports in $H$, continuations in $C$, provenance in $P$, and name row $N$ preserve the displayed route, but none carries a Dedekind completion, arbitrary infimum operator, quotient equality test, trichotomy oracle, countable-choice functional, or host-completeness field.
\end{proof}

\begin{theorem}[LocatedInfimum greatest-lower-bound obligation]
\label{thm:located-infimum-greatest-lower-bound-obligation}
For an accepted packet $I=(F,L,G,W,R,E,H,C,P,N)$, the greatest-lower-bound obligation is exactly the finite demand that each precision row of $W$ receives a displayed witness in $G$ whose readback lies above the proposed lower surface and blocks every displayed stronger lower-bound candidate at that precision. The obligation is local to $F,L,G,W,R,E,H,C,P,N$ and does not assert a global infimum for arbitrary real families.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumGreatestLowerBoundObligation}
\begin{proof}
Fix a precision row in $W$. By definition of the carrier, $G$ is the only ledger that can answer greatest-lower-approximation requests at that precision.

Read the corresponding $G$ entry. It supplies a finite family-side witness, and the route through $R$ and $E$ gives the regular-rational and real-name readback for the same displayed observation.

The comparison is therefore local: the witness is checked against the lower surface and against the stronger lower-bound candidates that are actually present in the window. Since the packet has no coordinate for arbitrary families or an ambient completeness principle, the row proves only the displayed finite greatest-lower-bound obligation.
\end{proof}

\begin{theorem}[LocatedInfimum precision-window induction]
\label{thm:located-infimum-window-induction}
For an accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, induction over the finite precision-window row $W$ builds the displayed witness ledger $G$ from the bounded-family row $F$, lower-bound ledger $L$, precision window $W$, $\RegSeqRatUp$ handoff $R$, $\RealUp$ seal $E$, transport row $H$, continuation row $C$, provenance row $P$, and local naming row $N$. The induction uses no Dedekind completeness, arbitrary infimum operator, quotient real equality, trichotomy, countable choice, or host completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumWindowInduction}
\begin{proof}
Proceed over the displayed finite window $W$. The empty window has no precision request, so the witness ledger contributes no greatest-lower-approximation entry.

For a nonempty window, read the next precision request together with the already displayed bounded-family and lower-bound rows $F$ and $L$. The finite comparison data at that request supplies the corresponding $G$ entry, and the remaining window is handled by the induction hypothesis.

The handoff and seal rows are structural at this stage. The row $R$ repacks the constructed window ledger as regular rational readback, and $E$ seals only that readback. The rows $H,C,P,N$ transport, replay, source, and name the same finite construction, so no completeness, quotient-equality, trichotomy, or choice principle enters the induction.
\end{proof}

\begin{theorem}[LocatedInfimum window ledger coverage]
\label{thm:located-infimum-window-ledger-coverage}
For an accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, every precision-window entry admitted by the induction of \autoref{thm:located-infimum-window-induction} is consumed by exactly one of the lower-bound ledger $L$, the greatest-lower-approximation ledger $G$, the $\RegSeqRatUp$ handoff row $R$, or the $\RealUp$ seal row $E$, with transport, replay, provenance, and local naming read only through $H,C,P,N$. The coverage remains internal to $F,L,G,W,R,E,H,C,P,N$ and exports no Dedekind completeness, arbitrary infimum operator, quotient real equality, trichotomy, countable choice, host completeness, or unlisted real-order oracle.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumWindowLedgerCoverage}
\begin{proof}
Proceed by induction over the displayed finite precision window $W$. The empty window has no admitted precision entry, so there is no ledger row to consume.

For a successor window, read the new precision entry and apply the lower-bound obligation of \autoref{thm:located-infimum-lower-bound-obligation}. If the entry asks for a lower-bound comparison, it is consumed by the displayed lower-bound ledger $L$ and no other analytic ledger receives that entry.

If the entry asks for a greatest-lower-approximation witness, apply \autoref{thm:located-infimum-greatest-lower-bound-obligation}. The corresponding row is consumed by $G$ at the same precision, and the induction hypothesis covers the remaining window entries.

The remaining Real-facing reads are structural consequences of the same covered entries. Classifier stability, \autoref{thm:located-infimum-classifier-stability}, preserves the assignment under $H,C,P,N$, while ledger exactness, \autoref{thm:located-infimum-ledger-exactness}, forces every regular-rational handoff and Real seal read through $R$ and $E$. Thus each admitted precision-window entry is covered by exactly one displayed analytic row before any handoff or seal is exported, and no forbidden endpoint has a carrier coordinate.
\end{proof}

\begin{theorem}[LocatedInfimum classifier stability]
\label{thm:located-infimum-classifier-stability}
Let $I=(F,L,G,W,R,E,H,C,P,N)$ be an accepted $\LocatedInfimumUp$ packet. Replacing any displayed component along the corresponding $\hsame$ transport in $H$ preserves lower-bound reads, greatest-lower-approximation reads, $\RegSeqRatUp$ handoff, $\RealUp$ seal access, continuation replay, provenance, and the local $\NameCert_{\LocatedInfimumUp}$ classifier. Thus the classifier is stable under componentwise BEDC transport.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumClassifierStability}
\begin{proof}
Use the transport ledger $H$ component by component. The family row, lower-bound ledger, greatest-lower-approximation ledger, precision window, regular handoff, real seal, continuation row, provenance row, and name row each have their own displayed $\hsame$ transport.

Transport the lower-bound route first. The route still starts at $F$, reads $L$ inside the same window $W$, passes through $R$, and reaches $E$.

Transport the greatest-lower-approximation route next. The row $G$ is moved with the same precision window and the same handoff and seal rows, so the finite witness relation seen by the classifier is unchanged.

Finally replay $C$ and $P$ after transport. They can only replay and source the transported rows, so the local classifier has no new field through which to distinguish transported copies beyond the displayed $\hsame$ data.
\end{proof}

\begin{theorem}[LocatedInfimum ledger exactness]
\label{thm:located-infimum-ledger-exactness}
Every Real-facing observation exported by an accepted $\LocatedInfimumUp$ packet is obtained from one of the displayed rows $F,L,G,W,R,E,H,C,P,N$. In particular, lower-bound, greatest-lower-approximation, handoff, and seal observations are exhausted by the ledger rows, and the packet exports no hidden Dedekind-completeness, arbitrary-infimum, quotient-equality, trichotomy, countable-choice, or host-completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumLedgerExactness}
\begin{proof}
Begin with a Real-facing observation. The carrier contains only one real seal row, namely $E$, and the definition makes $E$ reachable only after the bounded-family row, lower-bound ledger, greatest-lower-approximation ledger, precision window, and regular handoff have been displayed.

Inspect the possible observation classes. Lower-bound reads come from $L$, greatest-lower-approximation reads come from $G$, window restrictions come from $W$, and regular rational readback comes from $R$.

The remaining rows are structural. They transport, replay, source, or name the same finite rows, and therefore cannot create an unlisted completion principle or choice principle. Hence every exported observation is ledger-exact for $F,L,G,W,R,E,H,C,P,N$.
\end{proof}

\begin{theorem}[LocatedInfimum lower-cut stability]
\label{thm:located-infimum-lower-cut-stability}
For every accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, the lower-cut boundary determined by the lower-bound row $L$ and the greatest-lower-bound approximation row $G$ is preserved under the classifier and ledger transport displayed by $H,C,P,N$. In particular, any transported consumer reads the same finite lower-bound comparisons, the same precision-window witnesses, the same $\RegSeqRatUp$ handoff, and the same terminal $\RealUp$ seal before it treats the packet as an infimum boundary.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumLowerCutStability}
\begin{proof}
Start with the lower-bound side. By \autoref{thm:located-infimum-lower-bound-obligation}, every lower-bound read factors through $F,L,W,R,E$ and the structural rows, so transport cannot move the lower surface to a row outside the carrier.

Now read the greatest-lower-bound side. By \autoref{thm:located-infimum-greatest-lower-bound-obligation}, every approximation witness is drawn from $G$ at a precision row of $W$, and by \autoref{thm:located-infimum-ledger-exactness} every Real-facing observation is exhausted by $F,L,G,W,R,E,H,C,P,N$.

Finally apply classifier stability from \autoref{thm:located-infimum-classifier-stability}. Componentwise $\hsame$ transport preserves the lower-bound route, the greatest-lower-approximation route, the regular-rational handoff, the real seal, continuation replay, provenance, and local naming. The transported packet therefore exposes the same lower-cut boundary and no Dedekind completeness, arbitrary infimum operator, quotient equality test, trichotomy oracle, countable-choice functional, or host-completeness coordinate.
\end{proof}

\begin{theorem}[LocatedInfimum supremum-dual route]
\label{thm:located-infimum-supremum-dual-route}
Every accepted $\LocatedInfimumUp$ packet whose bounded-family row is read through the dual upper-bound presentation first factors through the $\LocatedSupremumUp$ sibling of \autoref{ch:concrete-instances-locatedsupremum-namecert} and the $\BoundedRealFamilyUp$ row before exposing its lower-bound ledger $L$, greatest-lower-approximation ledger $G$, $\RegSeqRatUp$ handoff $R$, and terminal $\RealUp$ seal $E$. The resulting route is still the finite carrier route of \autoref{def:located-infimum-carrier}; it does not import a global supremum operator, arbitrary infimum operator, Dedekind completeness, quotient real equality, trichotomy, countable choice, or host completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumSupremumDualRoute}
\begin{proof}
Start from the carrier of \autoref{def:located-infimum-carrier}. The bounded-family source is the row $F$, and the introduction identifies its upper-bound companion as the $\LocatedSupremumUp$ certificate of \autoref{ch:concrete-instances-locatedsupremum-namecert}.

Read the lower-bound side after that dual presentation. By \autoref{thm:located-infimum-lower-bound-obligation}, every lower-bound consumer must pass through $F,L,W,R,E$ and the structural rows, so the upper-bound presentation cannot bypass the displayed lower ledger.

Now read the greatest-lower-bound approximation side. Apply \autoref{thm:located-infimum-greatest-lower-bound-obligation}: the witnesses live in $G$ at precision rows of $W$, then pass through $R$ and $E$ before the local certificate exports an infimum-facing read.

Thus the dual route has a fixed order: $\LocatedSupremumUp$ and $\BoundedRealFamilyUp$ supply the upper-bound and family presentation, while $L,G,R,E,H,C,P,N$ expose the lower-bound, approximation, handoff, seal, transport, replay, provenance, and local naming rows. Since \autoref{def:located-infimum-carrier} contains no global order-completeness or quotient coordinate, none is added by the dual read.
\end{proof}

\begin{theorem}[LocatedInfimum supremum-dual exactness]
\label{thm:located-infimum-supremum-dual-exactness}
For every accepted $\LocatedInfimumUp$ packet
$$
I=(F,L,G,W,R,E,H,C,P,N),
$$
any dual read through $\LocatedSupremumUp$ and $\BoundedRealFamilyUp$ preserves exactly the displayed lower-bound ledger $L$, greatest-lower witness ledger $G$, $\RegSeqRatUp$ handoff $R$, $\RealUp$ seal $E$, and structural rows $H,C,P,N$. The read exports no host infimum, arbitrary supremum operator, quotient real equality, trichotomy, choice principle, or completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumSupremumDualExactness}
\begin{proof}
Start from the dual presentation of \autoref{thm:located-infimum-supremum-dual-route}. It may orient the bounded-family row $F$ through $\LocatedSupremumUp$ and $\BoundedRealFamilyUp$, but the carrier still exposes the lower-bound surface only through $L$ and the greatest-lower witness surface only through $G$.

The real-facing handoff is unchanged. The row $R$ remains the only $\RegSeqRatUp$ readback before the terminal $\RealUp$ seal $E$, so the dual presentation cannot replace the lower-bound evidence by a host infimum or an arbitrary supremum operator.

Finally $H,C,P,N$ preserve the same rows under transport, replay, provenance, and local naming. Since quotient real equality, trichotomy, choice, and completeness are not coordinates of \autoref{def:located-infimum-carrier}, exactness consists precisely of preserving $L,G,R,E,H,C,P,N$ and refusing those stronger principles.
\end{proof}

\begin{theorem}[LocatedInfimum bounded-family handoff]
\label{thm:located-infimum-bounded-family-handoff}
For every accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, each bounded-family consumer reads the lower-bound and greatest-lower-approximation surface only through the displayed $\BoundedRealFamilyUp$ row $F$, the lower-bound ledger $L$, the witness ledger $G$, the precision window $W$, the $\StreamNameUp$ window discipline, the $\RegSeqRatUp$ handoff row $R$, the $\RealUp$ seal $E$, and the structural rows $H,C,P,N$. The route is the lower-bound companion of the $\LocatedSupremumUp$ packet and exports no arbitrary infimum operator.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumBoundedFamilyHandoff}
\begin{proof}
Project the accepted carrier. The bounded-family source is $F$, so a downstream bounded-family argument cannot inspect a lower-bound or greatest-lower-approximation claim before it has read the displayed family row.

The lower surface is then split between $L$ and $G$. The row $L$ supplies the finite lower-bound comparisons, while $G$ supplies the displayed witnesses that block stronger lower-bound candidates inside the same precision window $W$.

The handoff remains finite. The $\StreamNameUp$ window discipline fixes the observations in scope, $R$ repacks them as $\RegSeqRatUp$ readback, and $E$ supplies the only $\RealUp$ seal available to the consumer.

Finally $H,C,P,N$ transport, replay, source, and name the same route. The dual reference to $\LocatedSupremumUp$ identifies the companion orientation, but it adds no ambient completeness field and no arbitrary infimum operator.
\end{proof}

\begin{theorem}[LocatedInfimum RegSeqRat seal factorization]
\label{thm:located-infimum-regseqrat-seal-factorization}
For every accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, the $\RegSeqRatUp$ row $R$ factors the real-seal export: any read of the $\RealUp$ seal $E$ must pass through the bounded-family row $F$, the lower-bound ledger $L$, the greatest-lower-approximation ledger $G$, the $\StreamNameUp$ precision window $W$, and then $R$. No consumer may reach $E$ directly from $F$, from $L$, from $G$, or from a $\LocatedSupremumUp$ comparison route.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumRegSeqRatSealFactorization}
\begin{proof}
Start with a request for the real seal. By \autoref{def:located-infimum-carrier}, $E$ is reached only through the displayed family, lower-bound, greatest-lower-approximation, window, and regular-rational rows.

The window row $W$ is the gate between ledger evidence and regular readback. It fixes which family reads, lower-bound comparisons, and greatest-lower witnesses are in scope before the regular sequence row is assembled.

The row $R$ is therefore the unique $\RegSeqRatUp$ factor. It repacks the displayed $\StreamNameUp$ observations as regular rational data, and only then can $E$ be read as the public $\RealUp$ seal.

The structural rows $H,C,P,N$ preserve this order under transport and replay. A $\LocatedSupremumUp$ comparison may provide the companion orientation, but it has no coordinate in $I$ that bypasses $W$ or $R$.
\end{proof}

\begin{theorem}[LocatedInfimum Real export nonescape]
\label{thm:located-infimum-real-export-nonescape}
For every accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, every $\RealUp$ observation exported by the local certificate is sourced by $F,L,G,W,R,E,H,C,P,N$ and remains inside the $\BoundedRealFamilyUp$, $\LocatedSupremumUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ route displayed by the carrier. The export cannot escape to Dedekind completeness, arbitrary infima, quotient real equality, trichotomy, countable choice, host completeness, or an unlisted real-order oracle.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumRealExportNonescape}
\begin{proof}
Take an exported $\RealUp$ observation. Ledger exactness forces it to be read from the displayed rows $F,L,G,W,R,E,H,C,P,N$, and the carrier has no second real-facing coordinate.

The analytic route is bounded-family first, lower-bound and witness ledgers second, window and regular-rational handoff third, and real seal last. The $\StreamNameUp$ and $\RegSeqRatUp$ rows keep the export finite at each stage.

The $\LocatedSupremumUp$ reference supplies only the companion lower-versus-upper orientation used by downstream real-order arguments. It does not add a hidden completeness or equality principle to the packet.

Thus every exported real observation remains in the displayed route. Since the forbidden principles have no carrier row and no replay edge, the export cannot escape through them.
\end{proof}

\begin{theorem}[LocatedInfimum obligation tail lattice]
\label{thm:located-infimum-obligation-tail-lattice}
For every accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, any exported infimum read must expose lower-cut stability, tail-window monotonicity, $\RegSeqRatUp$ readback, $\StreamNameUp$ observation, and $\RealUp$ sealing in that order before it is treated as a lower-bound lattice value. The accepted route is read through the displayed rows $F,L,G,W,R,E,H,C,P,N$ and cannot import a host quotient, trichotomy, ambient completeness, arbitrary infimum operator, or countable-choice selector.
\end{theorem}
\begin{proof}
Read the displayed rows in order. The bounded-family row $F$ fixes the family under inspection, and the lower-cut row $L$ gives the stable lower-bound comparisons that any infimum read must preserve.

The tail lattice is controlled by $G$ and $W$. The greatest-lower-approximation witnesses in $G$ are inspected only through the finite tail windows in $W$, so monotonicity of the accepted tail route is a property of the displayed window ledger rather than an ambient order-completeness principle.

The observation route then passes through $R$ and $E$. The $\StreamNameUp$ windows in $W$ determine the observations, $R$ repacks those observations as $\RegSeqRatUp$ readback, and the $\RealUp$ seal $E$ is available only after that readback has been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same row order. Since no host quotient, trichotomy, ambient completeness theorem, arbitrary infimum operator, or choice selector appears in those rows, none is smuggled into the exported infimum read.
\end{proof}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumObligationTailLattice}

\begin{theorem}[LocatedInfimum obligation boundary]
\label{thm:located-infimum-obligation-boundary}
For every accepted $\LocatedInfimumUp$ packet $I=(F,L,G,W,R,E,H,C,P,N)$, the obligation surface is exhausted by the bounded-family row, lower-bound ledger, greatest-lower-approximation ledger, $\StreamNameUp$ precision window, $\RegSeqRatUp$ handoff, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local $\NameCert$ rows. Any consumer that asks for arbitrary infima, Dedekind completeness, quotient real equality, trichotomy, countable choice, host completeness, or a standard bridge asks for data outside the packet.
\end{theorem}
\begin{proof}
Project the carrier from \autoref{def:located-infimum-carrier}. Its analytic coordinates are exactly $F,L,G,W,R,E$, and its structural coordinates are exactly $H,C,P,N$.

Use the lower-bound obligation of \autoref{thm:located-infimum-lower-bound-obligation} and the greatest-lower-bound obligation of \autoref{thm:located-infimum-greatest-lower-bound-obligation}. These force every lower read through $F,L,W,R,E$ and every greatest-lower approximation through $F,L,G,W,R,E$.

Now apply window ledger coverage, \autoref{thm:located-infimum-window-ledger-coverage}, together with ledger exactness, \autoref{thm:located-infimum-ledger-exactness}. The finite window entries are consumed by the displayed lower-bound, approximation, handoff, and seal rows, while $H,C,P,N$ only transport, replay, source, and name those rows.

Finally read the exclusions recorded by the chapter's not-claimed boundary as absence of carrier coordinates. There is no row for arbitrary infima, Dedekind completeness, quotient real equality, trichotomy, countable choice, host completeness, or a standard bridge, so a consumer requesting one of those objects has left the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumObligationBoundary}

\begin{theorem}[LocatedInfimum scoped kernel dependency]
\label{thm:located-infimum-scoped-kernel-dependency}
The obligations of an accepted $\LocatedInfimumUp$ packet are scoped exactly by the displayed $\BoundedRealFamilyUp$, $\LocatedSupremumUp$, $\BishopRealComparisonUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. No consumer of the chapter obtains Dedekind completeness, an arbitrary infimum operator, quotient real equality, trichotomy, countable choice, host completeness, or a standard bridge.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:located-infimum-carrier}. Its analytic data are the bounded-family row, the lower-bound ledger, the greatest-lower-bound witness ledger, the finite precision window, the $\RegSeqRatUp$ handoff, and the $\RealUp$ seal; its structural data are the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

Apply the lower-bound and greatest-lower-bound obligations, \autoref{thm:located-infimum-lower-bound-obligation} and \autoref{thm:located-infimum-greatest-lower-bound-obligation}. These force lower-bound reads and greatest-lower-approximation reads through the displayed bounded-family, comparison, window, handoff, and seal rows.

Use window induction and ledger coverage from \autoref{thm:located-infimum-window-induction} and \autoref{thm:located-infimum-window-ledger-coverage}. Each finite precision entry is consumed by the listed ledger rows before any Real-facing observation is exported.

Finally apply classifier stability, ledger exactness, bounded-family handoff, and RegSeqRat seal factorization: \autoref{thm:located-infimum-classifier-stability}, \autoref{thm:located-infimum-ledger-exactness}, \autoref{thm:located-infimum-bounded-family-handoff}, and \autoref{thm:located-infimum-regseqrat-seal-factorization}. These preserve the same scoped route through $\LocatedSupremumUp$ and $\BishopRealComparisonUp$ without adding any forbidden completeness, equality, choice, or bridge coordinate.
\end{proof}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumScopedKernelDependency}

\begin{theorem}[LocatedInfimum public export]
\label{thm:located-infimum-public-export}
Every accepted $\LocatedInfimumUp$ packet exports a public lower-bound and infimum read only through the displayed $\BoundedRealFamilyUp$ row, lower-bound ledger, greatest-lower-approximation ledger, $\StreamNameUp$ precision window, $\RegSeqRatUp$ handoff, $\RealUp$ seal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. The exported public object may be consumed by L10 $\RealUp$ and completion lower-bound arguments, but it does not supply Dedekind completeness, arbitrary infima, quotient real equality, trichotomy, countable choice, host completeness, or a standard bridge.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:located-infimum-carrier}. The only analytic rows available to a public lower-bound read are the bounded-family row, lower-bound ledger, greatest-lower-approximation ledger, finite precision window, regular handoff, and real seal.

Apply \autoref{thm:located-infimum-lower-bound-obligation} and \autoref{thm:located-infimum-greatest-lower-bound-obligation}. Lower-bound reads are forced through $F,L,W,R,E$, while greatest-lower-approximation reads are forced through $F,L,G,W,R,E$.

Next use window induction and ledger coverage from \autoref{thm:located-infimum-window-induction} and \autoref{thm:located-infimum-window-ledger-coverage}. These keep each precision request inside the displayed $\StreamNameUp$ window before any $\RegSeqRatUp$ or $\RealUp$ observation is exported.

Finally apply classifier stability, RegSeqRat seal factorization, Real export nonescape, obligation boundary, and scoped-kernel dependency: \autoref{thm:located-infimum-classifier-stability}, \autoref{thm:located-infimum-regseqrat-seal-factorization}, \autoref{thm:located-infimum-real-export-nonescape}, \autoref{thm:located-infimum-obligation-boundary}, and \autoref{thm:located-infimum-scoped-kernel-dependency}. They transport, replay, source, and name the same route for L10 $\RealUp$ and completion lower-bound consumers while excluding Dedekind completeness, arbitrary infima, quotient equality, trichotomy, choice, host completeness, and bridge data.
\end{proof}
\leanchecked{BEDC.Derived.LocatedInfimumUp.LocatedInfimumPublicExport}

\begin{closurestatus}{\LocatedInfimumUp}
  \constructivestory{A $\LocatedInfimumUp$ packet is generated from a bounded real family row, a lower-bound ledger, a greatest-lower-approximation ledger, a StreamName precision window, a RegSeqRat handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:located-infimum-carrier}, \autoref{thm:located-infimum-lower-bound-obligation}, \autoref{thm:located-infimum-greatest-lower-bound-obligation}, \autoref{thm:located-infimum-window-induction}, \autoref{thm:located-infimum-window-ledger-coverage}, \autoref{thm:located-infimum-classifier-stability}, \autoref{thm:located-infimum-ledger-exactness}, \autoref{thm:located-infimum-bounded-family-handoff}, \autoref{thm:located-infimum-regseqrat-seal-factorization}, and \autoref{thm:located-infimum-scoped-kernel-dependency} bind the finite lower-bound, greatest-lower-approximation, bounded-family handoff, RegSeqRat seal, and scoped-kernel dependencies over the L10 Real and Completion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Dedekind completeness, arbitrary infima, quotient real equality, trichotomy, countable choice, host completeness, or a standard bridge.}
  \upgradepath{Formal verification can encode carrier admission, lower-bound readback, greatest-lower-approximation witnessing, classifier stability, ledger exactness, handoff stability, and real-seal factorization for BEDC.Derived.LocatedInfimumUp.}
\end{closurestatus}
