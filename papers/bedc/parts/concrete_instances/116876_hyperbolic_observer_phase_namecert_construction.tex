\chapter{A Concrete Naming Certificate for $\HyperbolicObserverPhaseUp$}
\label{ch:concrete-instances-hyperbolic-observer-phase-namecert}
\origin{ai}

The $\HyperbolicObserverPhaseUp$ packet realizes the observer phase route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as a canonical finite carrier. It binds boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, provenance, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows before any observer-expansion conclusion can be consumed. The packet is BEDC-native phase data: it is not a host hyperbolic manifold, not empirical cosmology, and not a hidden observer center.

\begin{definition}[Hyperbolic observer phase carrier]\label{def:hyperbolic-observer-phase-carrier}
A $\HyperbolicObserverPhaseUp$ carrier is a finite $\BHist$ packet
$$
Q=(B,V,D,T,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the boundary-shadow row read through $\HyperbolicBoundaryShadowUp$.
\item $V$ is the visual-boundary row read through $\HyperbolicVisualBoundaryUp$.
\item $D$ is the phase-distance row read through $\PhaseDistanceObserverExpansionUp$.
\item $T$ is the reversible-transport row read through $\PoincareBoundaryFixedTransportUp$, $\PoincareDiskPhaseTransportUp$, and the observer phase groupoid surface.
\item $R$ is the refusal row blocking hidden interior centers, unlogged global geometry, empirical expansion laws, and private phase-distance oracles.
\item $H$ records componentwise $\hsame$ transport for $B,V,D,T,R$.
\item $C$ records displayed $\Cont$ replay from boundary-shadow reads through visual-boundary reads, phase-distance reads, reversible transport, refusal, and local naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverPhaseUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\PhaseDistanceObserverExpansionUp$, $\PoincareBoundaryFixedTransportUp$, $\PoincareDiskPhaseTransportUp$, $\ObserverPhaseGroupoidUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicObserverPhaseUp}$ row.
\end{enumerate}
The classifier accepts only packets with these rows. It has no coordinate for an ambient hyperbolic space, a selected ideal boundary, a physical expansion law, a global geodesic completion, or an interior observer center.
\end{definition}

\begin{theorem}[Hyperbolic observer phase NameCert obligations]\label{thm:hyperbolic-observer-phase-namecert-obligations}
For every accepted $\HyperbolicObserverPhaseUp$ carrier $Q=(B,V,D,T,R,H,C,P,N)$, the local $\NameCert_{\HyperbolicObserverPhaseUp}$ surface consists exactly of five finite obligations:
\begin{enumerate}
\item carrier admission for the boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, transport, replay, provenance, and local naming rows;
\item boundary coverage, requiring $B$ and $V$ to be displayed before any phase-distance row is read;
\item phase-distance and transport exactness, requiring $D$ and $T$ to be the only comparison and reversible-transport rows exported by the carrier;
\item refusal coverage, requiring $R$ to block hidden center, selected boundary, global-geometry, empirical-cosmology, and private-oracle interpretations;
\item ledger non-escape, requiring downstream consumers to pass through $H,C,P,N$ rather than rereading the vision text as a proof surface.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:hyperbolic-observer-phase-carrier}. The geometric rows are exactly $B,V,D,T,R$, and the structural rows are exactly $H,C,P,N$.

Boundary coverage follows because $B$ and $V$ are the only boundary-facing rows. A phase-distance read therefore has no route into the carrier until boundary-shadow and visual-boundary data have been displayed.

The exactness and refusal clauses are row-order checks. The phase-distance row $D$ and transport row $T$ provide the only comparison and reversible movement data, while $R$ blocks hidden center, selected boundary, global geometry, empirical law, and private-oracle readings.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no further coordinate, every downstream observer-phase read is confined to the displayed BEDC route.
\end{proof}

\begin{theorem}[Hyperbolic observer phase downstream boundary]\label{thm:hyperbolic-observer-phase-downstream-boundary}
Every downstream read of an accepted $\HyperbolicObserverPhaseUp$ packet factors through boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HyperbolicObserverPhaseUp}$ row. It exports a finite observer-phase interface, not an ambient hyperbolic geometry or a total observer-expansion law.
\end{theorem}
\begin{proof}
Apply \autoref{thm:hyperbolic-observer-phase-namecert-obligations}. The accepted packet exposes only the nine rows $B,V,D,T,R,H,C,P,N$.

The boundary-shadow and visual-boundary rows keep the read boundary-facing. The phase-distance and reversible-transport rows provide only displayed comparisons and movement, and the refusal row prevents those readings from becoming global geometry or empirical cosmology.

Transport, replay, provenance, and naming preserve the same finite route. A downstream consumer therefore receives the observer-phase interface and no stronger object.
\end{proof}

The later Lean TasteGate for this packet must provide Nontrivial and FieldFaithful instances for the boundary-shadow, visual-boundary, phase-distance, reversible-transport, and refusal rows before any stronger classifier claim is recorded.

\falsifiablePrediction{An accepted $\HyperbolicObserverPhaseUp$ consumer that exports observer phase data without first displaying boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, hsame, Cont, Pkg, and local NameCert rows refutes this carrier.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-visual-boundary, phase-distance-observer-expansion, poincare-boundary-fixed-transport, observer-phase-groupoid: none is bijective to $\HyperbolicObserverPhaseUp$ because this packet jointly carries boundary-facing reads, phase-distance comparison, reversible transport, refusal, replay, provenance, and local naming.}

\closureat{HyperbolicObserverPhaseUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverPhaseUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-phase-carrier}, \autoref{thm:hyperbolic-observer-phase-namecert-obligations}, and \autoref{thm:hyperbolic-observer-phase-downstream-boundary} seed the finite observer-phase carrier over boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, ambient hyperbolic geometry, empirical cosmology, selected ideal boundaries, global geodesic completion, or observer-center existence.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary coverage, phase-distance exactness, reversible-transport exactness, refusal coverage, Nontrivial and FieldFaithful TasteGate instances, transport stability, and downstream nonescape for BEDC.Derived.HyperbolicObserverPhaseUp.}
  \constructivestory{A $\HyperbolicObserverPhaseUp$ packet is generated from boundary-shadow, visual-boundary, phase-distance, reversible-transport, refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
\end{closurestatus}
