\chapter{A Concrete Naming Certificate for $\RegulatedLeftRightLimitUp$}
\label{ch:concrete-instances-regulated-left-right-limit-namecert}
\origin{human}

Following Bishop and Bridges on regulated functions and one-sided limits, the $\RegulatedLeftRightLimitUp$ packet records the paired side-limit surface carried by a regulated real function. It sits over the source route of \autoref{ch:concrete-instances-regulated-function-namecert}, the one-sided limit interface of \autoref{ch:concrete-instances-one-sided-limit-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, the cadlag path carrier of \autoref{ch:concrete-instances-cadlag-real-function-namecert}, and the path-topology consumer of \autoref{ch:concrete-instances-skorokhod-topology-namecert}. The packet packages left and right side data as finite BEDC rows; it does not import host function quotients, selected limiting values outside the displayed rows, path-space completeness, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Regulated left--right limit carrier]
\label{def:regulated-left-right-limit-carrier}
A $\RegulatedLeftRightLimitUp$ carrier is a finite $\BHist$ packet
$$
L=(F,A,W^-,W^+,R^-,R^+,D,E^-,E^+,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\RegulatedFunctionUp$ source row, read through the regulated-function route of \autoref{ch:concrete-instances-regulated-function-namecert}.
\item $A$ is the finite anchor ledger naming the endpoint, jump site, or interior cell at which the paired side read is taken.
\item $W^-$ and $W^+$ are the left and right $\StreamNameUp$ approach-window rows.
\item $R^-$ and $R^+$ are the corresponding $\RegSeqRatUp$ readback rows obtained from those windows.
\item $D$ is the shared $\DyadicRatCoreUp$ tolerance ledger used to compare the two side windows and their real seals.
\item $E^-$ and $E^+$ are the terminal $\RealUp$ seals for the displayed left and right side limits.
\item $H$ records componentwise $\hsame$ transport for the regulated source, anchor ledger, side windows, regular readbacks, dyadic tolerance ledger, real seals, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a regulated-function request through the anchor, the two side-window rows, the readback rows, the dyadic comparison ledger, and the two real seals.
\item $P$ is the $\Pkg$ provenance over $\RegulatedLeftRightLimitUp$, $\RegulatedFunctionUp$, $\OneSidedLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CadlagRealFunctionUp$, $\SkorokhodTopologyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegulatedLeftRightLimitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the rows $F,A,W^-,W^+,R^-,R^+,D,E^-,E^+,H,C,P,N$. No coordinate stores a host quotient of functions, a path-space completion theorem, a selected limit outside the displayed side seals, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regulated left--right limit NameCert obligations]
\label{thm:regulated-left-right-limit-namecert-obligations}
For every accepted $\RegulatedLeftRightLimitUp$ carrier
$$
L=(F,A,W^-,W^+,R^-,R^+,D,E^-,E^+,H,C,P,N),
$$
the local $\NameCert_{\RegulatedLeftRightLimitUp}$ surface consists exactly of carrier admission for the regulated source, anchor ledger, left and right StreamName windows, regular rational readbacks, dyadic tolerance row, left and right Real seals, componentwise transport, displayed replay, provenance, and local naming. The side-window exactness clause requires $W^-$ and $W^+$ to be consumed as paired $\OneSidedLimitUp$ rows over the same source row $F$ and anchor ledger $A$. The nonescape clause requires every cadlag or path-topology consumer to remain downstream from $F,A,W^-,W^+,R^-,R^+,D,E^-,E^+,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regulated-left-right-limit-carrier}. Its displayed inventory is exactly the regulated source row, anchor ledger, two side-window rows, two readback rows, the shared dyadic tolerance ledger, two real seals, and the structural rows $H,C,P,N$.

The side-window clause is read from $A,W^-,W^+,R^-,R^+,D$. Both windows are attached to the same anchor and source row before either regular-rational readback or dyadic comparison row is available.

The terminal seals $E^-$ and $E^+$ are downstream of those rows. Transport and replay preserve the same paired route, while provenance and local naming record only the listed dependencies.

Since the carrier contains no host function quotient, selected external limit, path-space completion theorem, open-cover compactness proof, or ambient $\RealUp$ completeness coordinate, the local NameCert cannot export any of those principles.
\end{proof}

\begin{theorem}[Regulated left--right limit regulated handoff]
\label{thm:regulated-left-right-limit-regulated-handoff}
Every accepted $\RegulatedLeftRightLimitUp$ packet exposes its paired side limits only after the $\RegulatedFunctionUp$ source route has been displayed. The public handoff factors through
$$
F,A \longmapsto W^-,W^+ \longmapsto R^-,R^+ \longmapsto D \longmapsto E^-,E^+ \longmapsto H,C,P,N,
$$
with $F$ read through \autoref{thm:regulated-function-left-right-limit-consumer}. Thus the packet supplies a finite paired side-limit interface for regulated functions, not a host-level function object or a selected path value outside the displayed seals.
\end{theorem}
\begin{proof}
Start with the source row $F$ of \autoref{def:regulated-left-right-limit-carrier}. By \autoref{thm:regulated-function-left-right-limit-consumer}, any consumer of paired one-sided limits must keep the regulated-function route visible before one-sided limit rows or terminal Real seals are consumed.

The anchor ledger $A$ then fixes the endpoint, jump site, or interior cell for the paired read. The two StreamName windows $W^-$ and $W^+$ are therefore side-tagged observations over the same source and anchor.

Regular rational readback and dyadic comparison are downstream. The rows $R^-,R^+$ and $D$ are read before the Real seals $E^-,E^+$, while $H,C,P,N$ preserve, replay, source, and name that finite route.
\end{proof}

\begin{theorem}[Regulated left--right limit Real-seal nonescape]
\label{thm:regulated-left-right-limit-real-seal-nonescape}
Every $\RealUp$, $\CadlagRealFunctionUp$, or $\SkorokhodTopologyUp$ consumer of an accepted $\RegulatedLeftRightLimitUp$ packet receives the left and right seals only through the displayed route
$$
F,A,W^-,W^+,R^-,R^+,D,E^-,E^+,H,C,P,N.
$$
The route may be consumed as cadlag side-window data or as path-topology jump data, but it exports no quotient of functions, no path-space completeness theorem, no selected limit outside $E^-,E^+$, no open-cover compactness principle, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regulated-left-right-limit-regulated-handoff}. It places the two Real seals behind the regulated source, anchor ledger, side-window rows, readback rows, and dyadic comparison row.

A cadlag consumer reads the same paired seals as left and right path limits, while a Skorokhod-topology consumer reads them as finite jump-window data. In both cases the source route remains the displayed regulated-function packet and the side data remains the paired $\OneSidedLimitUp$ handoff.

The carrier has no coordinate for a host function quotient, a completed path space, an external selected value, open-cover compactness, or ambient Real completeness. Therefore any Real, cadlag, or topology-facing read remains inside the displayed finite route.
\end{proof}

\falsifiablePrediction{Every accepted $\RegulatedLeftRightLimitUp$ read is refuted if it reaches a $\OneSidedLimitUp$, $\RealUp$, $\CadlagRealFunctionUp$, or $\SkorokhodTopologyUp$ consumer without displaying the regulated source, anchor ledger, paired StreamName windows, paired RegSeqRat readbacks, shared DyadicRatCore tolerance row, paired Real seals, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence.}

\independenceWitness{regulated-function, one-sided-limit, streamname, regseqrat, dyadicratcore, real, cadlag-real-function, skorokhod-topology: $\RegulatedLeftRightLimitUp$ is not bijective to any listed sibling because it binds a regulated source, a shared anchor, paired side windows, paired regular-rational readbacks, one dyadic comparison ledger, paired Real seals, transport, replay, provenance, and local naming as one finite side-limit packet.}

\closureat{RegulatedLeftRightLimitUp}{seedStr}

\begin{closurestatus}{\RegulatedLeftRightLimitUp}
  \constructivestory{A $\RegulatedLeftRightLimitUp$ packet is generated from a RegulatedFunction source row, an anchor ledger, paired StreamName side windows, paired RegSeqRat readbacks, a DyadicRatCore tolerance row, paired RealUp seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regulated-left-right-limit-carrier}, \autoref{thm:regulated-left-right-limit-namecert-obligations}, \autoref{thm:regulated-left-right-limit-regulated-handoff}, and \autoref{thm:regulated-left-right-limit-real-seal-nonescape} seed the paired side-limit route between regulated functions, one-sided limits, cadlag paths, and Skorokhod topology consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function quotients, path-space completeness, selected external limits, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires a Lean declaration for BEDC.Derived.RegulatedLeftRightLimitUp covering carrier admission, side-window exactness, dyadic comparison, paired Real-seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
