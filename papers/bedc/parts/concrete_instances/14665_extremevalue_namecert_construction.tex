\chapter{A Concrete Naming Certificate for $\ExtremeValueUp$}
\label{ch:concrete-instances-extremevalue-namecert}
\origin{human}

Following Weierstrass and Bishop--Bridges constructive analysis, the $\ExtremeValueUp$ packet records the finite BEDC reading of the extreme-value theorem. Continuous real-valued maps on a compact metric source expose an attained-value packet only through finite nets, uniform-continuity moduli, a finite lower-bound fold, located supremum rows, $\RealUp$ seal rows, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$. The packet consumes the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the compact-uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the finite-net minimum fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, the located supremum boundary of \autoref{ch:concrete-instances-locatedsupremum-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It is an attainment certificate for inspected finite nets, not an open-cover compactness principle, a host maximum operator, quotient real equality, or a choice-selected point.

\begin{definition}[Extreme value carrier]
\label{def:extreme-value-carrier}
An $\ExtremeValueUp$ carrier is a finite $\BHist$ packet
$$
E=(X,F,U,M,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ source row, carrying the admitted metric name, compact finite-net schedules, radius requests, and the displayed source certificate.
\item $F$ is the $\ContinuousMapUp$ row naming a real-valued map from the source surface to $\RealUp$ output seals.
\item $U$ is the $\CompactUniformContinuityUp$ row that turns the compact metric source and continuous map into a displayed finite modulus ledger.
\item $M$ is the $\FiniteNetMinimumFoldUp$ row computing the finite lower-bound candidate over the inspected net selected by $U$.
\item $S$ is the $\LocatedSupremumUp$ row recording the located upper boundary for the displayed value windows.
\item $R$ is the terminal $\RealUp$ seal row for the attained value and for the real comparisons used by $M$ and $S$.
\item $H$ records componentwise $\hsame$ transport for the compact metric source, continuous map, uniform-continuity ledger, finite-net fold, located supremum row, real seals, continuation rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-metric radius requests to finite nets, from the map row to value windows, from uniform-continuity requests to the fold row, and from the fold and located-supremum rows to the terminal real seal.
\item $P$ is the $\Pkg$ provenance over $\ExtremeValueUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\CompactUniformContinuityUp$, $\FiniteNetMinimumFoldUp$, $\LocatedSupremumUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ExtremeValueUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for open-cover compactness, host-level maximum operators, quotient real equality, selected arbitrary points, countable choice, or detached $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.ExtremeValueUp.ExtremeValueTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Extreme value NameCert obligations]
\label{thm:extreme-value-namecert-obligations}
For every accepted $\ExtremeValueUp$ carrier $E=(X,F,U,M,S,R,H,C,P,N)$, the local $\NameCert_{\ExtremeValueUp}$ surface consists exactly of carrier admission for the displayed compact metric source, continuous real-valued map, compact uniform-continuity ledger, finite-net minimum fold, located supremum boundary, terminal real seals, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:extreme-value-carrier}. The source row $X$ is read through \autoref{ch:concrete-instances-compactmetric-namecert}, so every metric, radius, and compact-net request is already displayed before a value row is inspected.

Read the map and modulus rows next. The row $F$ uses the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, and $U$ uses the compact-uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} to bind value variation on the finite nets supplied by $X$.

Now inspect the real-valued boundary rows. The row $M$ applies the finite-net fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, while $S$ reads the located upper boundary through \autoref{ch:concrete-instances-locatedsupremum-namecert} and $R$ supplies only the real-name seals admitted by \autoref{ch:concrete-instances-real-namecert}.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name the already displayed source, map, modulus, fold, supremum, and seal rows. Since the carrier contains no further coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Extreme value finite-net attainment]
\label{thm:extreme-value-finite-net-attainment}
Every accepted $\ExtremeValueUp$ carrier factors any attained-value read through
$$
X\longmapsto F\longmapsto U\longmapsto M\longmapsto S\longmapsto R
$$
together with $H,C,P,N$. Consequently the exported attainment statement is a finite-net and real-seal certificate over the compact metric source and continuous map, not a host maximum operator, an arbitrary selected point of the source, or an open-cover compactness argument.
\end{theorem}
\begin{proof}
Begin with the compact metric source. By \autoref{ch:concrete-instances-compactmetric-namecert}, $X$ exposes the finite net schedule and the metric-radius data that any later value comparison must use.

Feed this source into the map row. The continuous-map packet of \autoref{ch:concrete-instances-continuousmap-namecert} supplies only displayed value windows, and the compact-uniform-continuity packet of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} turns those windows into a finite modulus ledger on the same compact source.

The fold row $M$ then searches the displayed net by the finite-net minimum fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}. The located supremum row $S$ supplies the opposing located boundary through \autoref{ch:concrete-instances-locatedsupremum-namecert}, and the real-seal row $R$ is read through \autoref{ch:concrete-instances-real-namecert}.

Transport and replay preserve the same factorization. Thus a consumer can see only the finite source net, the displayed map windows, the compact-uniform modulus, the finite fold, the located boundary, and the real seal; no separate host maximum or arbitrary point selector is available.
\end{proof}

\begin{theorem}[Extreme value Real seal boundary]
\label{thm:extreme-value-real-seal-boundary}
Every $\RealUp$ value seal exported by an accepted $\ExtremeValueUp$ carrier is downstream of the compact metric source $X$, continuous map row $F$, compact uniform-continuity ledger $U$, finite-net minimum fold $M$, and located supremum row $S$. The seal row $R$ therefore records only the boundary visible through finite-net attainment data and does not provide quotient real equality, detached $\RealUp$ completeness, countable choice, or an independent real maximum principle.
\end{theorem}
\begin{proof}
Start from the factorization in \autoref{thm:extreme-value-finite-net-attainment}. It places the compact metric source, map windows, compact-uniform modulus, finite-net fold, and located supremum row before any real-facing seal is exported.

The upstream chapters fix the only admissible route: \autoref{ch:concrete-instances-compactmetric-namecert} supplies $X$, \autoref{ch:concrete-instances-continuousmap-namecert} supplies $F$, \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} supplies $U$, \autoref{ch:concrete-instances-finitenetminimumfold-namecert} supplies $M$, and \autoref{ch:concrete-instances-locatedsupremum-namecert} supplies $S$.

Read $R$ through \autoref{ch:concrete-instances-real-namecert}. This makes the terminal row a $\RealUp$ seal for the displayed finite-net value and located boundary, not a quotient representative or a host completeness witness.

The continuation, transport, provenance, and local naming rows do not add new real equality or selection data. Since the carrier has no coordinate for detached $\RealUp$ completeness, countable choice, quotient equality, or a host maximum operator, every exported real seal remains inside the finite-net attainment boundary.
\end{proof}

\falsifiablePrediction{Every $\ExtremeValueUp$ attained-value read factors through $\CompactMetricUp$, $\ContinuousMapUp$, $\CompactUniformContinuityUp$, $\FiniteNetMinimumFoldUp$, $\LocatedSupremumUp$, and $\RealUp$ before reaching the local $\NameCert_{\ExtremeValueUp}$ row.}

\independenceWitness{compactmetric, continuousmap, compactuniformcontinuity, finitenetminimumfold, locatedsupremum, real: $\ExtremeValueUp$ is not bijective to any listed sibling because it jointly carries a compact metric source, a continuous real-valued map, a compact-uniform modulus ledger, a finite-net minimum fold, a located supremum boundary, terminal real seals, transport, replay, provenance, and local naming.}

\closureat{ExtremeValueUp}{seedStr}

\begin{closurestatus}{\ExtremeValueUp}
  \constructivestory{An $\ExtremeValueUp$ packet is generated from a $\CompactMetricUp$ source, a $\ContinuousMapUp$ real-valued map, a $\CompactUniformContinuityUp$ modulus ledger, a $\FiniteNetMinimumFoldUp$ finite fold, a $\LocatedSupremumUp$ boundary row, $\RealUp$ seal rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:extreme-value-carrier}, \autoref{thm:extreme-value-namecert-obligations}, \autoref{thm:extreme-value-finite-net-attainment}, and \autoref{thm:extreme-value-real-seal-boundary} seed the constructive finite-net attainment reading of extreme values on compact metric sources.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, host maximum operators, quotient real equality, selected arbitrary points, countable choice, detached $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Lean closure requires carrier admission, NameCert obligations, finite-net attainment, Real seal boundary, and TasteGate field faithfulness for BEDC.Derived.ExtremeValueUp.}
\end{closurestatus}
