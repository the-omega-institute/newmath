\chapter{A Concrete Naming Certificate for $\RegularCauchyReindexingModulusUp$}
\label{ch:concrete-instances-regular-cauchy-reindexing-modulus-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names and cofinal subsequence readings, the $\RegularCauchyReindexingModulusUp$ packet records the finite modulus row that lets a regular Cauchy name be read along a displayed cofinal reindexing schedule before its regular rational readback or $\RealUp$ seal is consumed. It sits between the finite stream schedule of \autoref{ch:concrete-instances-stream-reindexing-namecert}, the regular cofinal route of \autoref{ch:concrete-instances-regular-cauchy-reindexing-namecert}, the general handoff of \autoref{ch:concrete-instances-cauchy-reindexing-namecert}, the dyadic tolerance carrier of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational row of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only a finite cofinal-modulus carrier; it does not quotient streams, choose representatives, import countable choice, identify host equality, or invoke ambient $\RealUp$ completeness.

\begin{definition}[RegularCauchyReindexingModulus carrier]
\label{def:regular-cauchy-reindexing-modulus-carrier}
A $\RegularCauchyReindexingModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(S,T,I,U,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source $\StreamNameUp$ schedule of the regular Cauchy name.
\item $T$ is the target $\StreamNameUp$ schedule whose finite requests are sent back to $S$.
\item $I$ is the $\RegularCauchyReindexingUp$ cofinal route, including the monotone index map and its cofinal reachability witness.
\item $U$ is the modulus row assigning to each displayed target request a source-side bound along $I$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger read by the modulus row.
\item $W$ is the $\StreamReindexingUp$ finite-window replay surface for the source and target schedules.
\item $R$ is the $\RegSeqRatUp$ readback row reached only after $T,I,U,D,W$ have been displayed.
\item $E$ is the $\RealUp$ seal reached after the regular rational readback and cofinal-modulus route have been replayed.
\item $H$ records componentwise $\hsame$ transport for the schedules, route, modulus, tolerance ledger, finite windows, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from target requests through $I,U,D,W,R$ to the terminal seal $E$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyReindexingModulusUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\StreamReindexingUp$, $\RegularCauchyReindexingUp$, $\CauchyReindexingUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyReindexingModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient stream equality, selected representatives, countable choice, host equality of streams or reals, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[RegularCauchyReindexingModulus NameCert obligations]
\label{thm:regular-cauchy-reindexing-modulus-namecert-obligations}
For every accepted $\RegularCauchyReindexingModulusUp$ carrier $M=(S,T,I,U,D,W,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyReindexingModulusUp}$ surface consists exactly of source and target $\StreamNameUp$ admission, $\RegularCauchyReindexingUp$ cofinal-route exposure, modulus-row exposure, $\DyadicRatCoreUp$ tolerance exposure, $\StreamReindexingUp$ finite-window replay, $\RegSeqRatUp$ readback, $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.

Every accepted read of the packet factors through
$$
\StreamNameUp,\quad \StreamReindexingUp,\quad \RegularCauchyReindexingUp,\quad \CauchyReindexingUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
before the local $\NameCert$ row is public. No accepted read exports quotient streams, a selected representative, countable choice, host equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-reindexing-modulus-carrier}. Its analytic rows are the two stream schedules $S,T$, the cofinal reindexing route $I$, the modulus row $U$, the dyadic tolerance ledger $D$, the finite-window replay $W$, the regular readback $R$, and the Real seal $E$.

The target schedule is read through the reindexing rows before the modulus is available. Thus $U$ can only assign bounds along the displayed cofinal route, and the tolerance ledger $D$ is consumed as finite $\DyadicRatCoreUp$ data.

Next the $\StreamReindexingUp$ row $W$ replays the finite windows and the $\RegSeqRatUp$ row $R$ supplies the regular rational readback. The terminal $\RealUp$ seal $E$ is therefore downstream of the same cofinal-modulus route.

Finally $H,C,P,N$ transport, replay, source, and name the displayed packet. Since no row stores quotient equality, representatives, choice, host equality, trichotomy, or completeness, the listed rows exhaust the local $\NameCert$ obligation surface.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyReindexingModulusUp$ read exposes source and target StreamName schedules, a regular cofinal route, a modulus row, DyadicRatCore tolerance, StreamReindexing finite windows, RegSeqRat readback, Real sealing, transport, replay, provenance, and local NameCert before the Real seal is consumed.}

\independenceWitness{stream-reindexing, regular-cauchy-reindexing, cauchy-reindexing, dyadicratcore, regseqrat, real: $\RegularCauchyReindexingModulusUp$ is not bijective to any listed sibling because it jointly carries a source schedule, target schedule, cofinal route, modulus row, tolerance ledger, finite-window replay, regular rational readback, Real seal, transport, replay, provenance, and local naming as one packet.}

\closureat{RegularCauchyReindexingModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchyReindexingModulusUp}
  \constructivestory{A $\RegularCauchyReindexingModulusUp$ packet is generated from source and target StreamName schedules, a RegularCauchyReindexing cofinal route, a modulus row, DyadicRatCore tolerance, StreamReindexing finite-window replay, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-reindexing-modulus-carrier} and \autoref{thm:regular-cauchy-reindexing-modulus-namecert-obligations} seed the finite cofinal-modulus route over StreamReindexing, RegularCauchyReindexing, CauchyReindexing, DyadicRatCore, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient streams, selected representatives, countable choice, host equality, trichotomy, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cofinal-route exposure, modulus exactness, dyadic tolerance compatibility, finite-window replay, regular readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyReindexingModulusUp.}
\end{closurestatus}
