\chapter{A Concrete Naming Certificate for $\DyadicWindowReflectorUp$}
\label{ch:concrete-instances-dyadic-window-reflector-namecert}
\origin{human}

Following Bishop--Bridges real-name completeness arguments, the $\DyadicWindowReflectorUp$ packet records the finite-window reflector that turns displayed dyadic tolerance requests into the StreamName, RegSeqRat, and Real rows consumed by Real-completeness proofs. It realizes the finite-window classifier route of the Real-completeness vision through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ seal rows. The reflector consumes the dyadic tolerance, StreamName observation-window, RegSeqRat readback, and terminal Real seal surfaces through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert} before any completion-facing read is admitted. The packet names only the finite reflector surface; it does not assert selected limits, quotient Cauchy equality, open-ended choice, or ambient completeness of $\RealUp$.

\begin{definition}[Dyadic window reflector carrier]
\label{def:dyadic-window-reflector-carrier}
A $\DyadicWindowReflectorUp$ carrier is a finite $\BHist$ packet
$$
F=(D,W,R,E,Q,H,C,P,L)
$$
with the following rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance budget row, including the inspected dyadic radius and comparison window.
\item $W$ is the $\StreamNameUp$ observation-window row addressed by the same dyadic budget.
\item $R$ is the $\RegSeqRatUp$ readback row produced from $W$ under the displayed dyadic tolerance.
\item $E$ is the terminal $\RealUp$ seal for the reflected finite window.
\item $Q$ is the classifier row recording that the Real-completeness query reads only the displayed dyadic, stream, rational, and Real coordinates.
\item $H$ records componentwise $\hsame$ transport for $D,W,R,E,Q$ and the local certificate rows.
\item $C$ records displayed $\Cont$ replay from a finite-window completeness request through the dyadic, stream, rational, and Real-seal route.
\item $P$ is the $\Pkg$ provenance over $\DyadicWindowReflectorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\DyadicWindowReflectorUp}$ row.
\end{enumerate}
The classifier has no coordinate for a selected limiting real, quotient stream equality, hidden Cauchy class, or unrestricted completeness theorem.
\end{definition}

\begin{theorem}[Dyadic window reflector NameCert obligations]
\label{thm:dyadic-window-reflector-namecert-obligations}
For every accepted $\DyadicWindowReflectorUp$ carrier
$$
F=(D,W,R,E,Q,H,C,P,L),
$$
the local $\NameCert_{\DyadicWindowReflectorUp}$ surface consists exactly of the dyadic tolerance budget $D$, stream window $W$, regular rational readback $R$, terminal Real seal $E$, finite-window classifier row $Q$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-window-reflector-carrier}. The dyadic tolerance row $D$ is the only numerical budget available to the reflector, and the stream row $W$ is the only window on which that budget acts.

The rational readback $R$ and Real seal $E$ are downstream of the same displayed window. The classifier row $Q$ records that Real-completeness consumers see exactly this finite-window route rather than a hidden limiting object.

The structural rows $H,C,P,L$ preserve, replay, source, and name the route. Thus the local certificate cannot export selected limits, quotient stream equality, hidden Cauchy classes, or ambient Real completeness beyond the displayed rows.
\end{proof}

\begin{theorem}[Dyadic window reflector vision route]
\label{thm:dyadic-window-reflector-vision-route}
Every finite-window classifier read used by the Real-completeness route factors through $D,W,R,E,Q,H,C,P,L$ in an accepted $\DyadicWindowReflectorUp$ carrier. In particular, the vision-side $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ passages are realized as a concrete NameCert packet over dyadic budgets, stream windows, rational readback, Real seal rows, transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadic-window-reflector-namecert-obligations}. It fixes the reflector surface as the rows $D,W,R,E,Q,H,C,P,L$.

The dyadic budget row $D$ addresses the observation window $W$, and the readback row $R$ converts that window into the rational tolerance data required by the terminal Real seal $E$. The classifier row $Q$ is therefore the finite-window classifier seen by the Real-completeness route.

Finally $H,C,P,L$ keep the classifier stable under componentwise transport and displayed replay. The reflector is consequently a concrete NameCert route for later diagonal-limit formalization, not a selected-limit or quotient-completion principle.
\end{proof}

\begin{theorem}[Dyadic window reflector DyadicRatCore route]
\label{thm:dyadic-window-reflector-dyadicratcore-route}
Every $\DyadicWindowReflectorUp$ read first consumes its numerical tolerance budget through \autoref{ch:concrete-instances-dyadicratcore-namecert} before it can open the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ seal rows. Thus the sibling route factors through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Project the carrier rows $D,W,R,E,Q,H,C,P,L$ of \autoref{def:dyadic-window-reflector-carrier}. The row $D$ is the $\DyadicRatCoreUp$ source of the inspected radius and comparison window, so it is the only numerical budget available to the reflector.

Only after $D$ is fixed can the stream row $W$ be addressed, the rational readback row $R$ be computed, and the Real seal row $E$ be exposed. The structural rows $Q,H,C,P,L$ preserve this order: the classifier sees the dyadic, stream, rational, and Real coordinates; transport and replay keep those coordinates componentwise; provenance and local naming record the same route.

Without the $\DyadicRatCoreUp$ tolerance row supplied by \autoref{ch:concrete-instances-dyadicratcore-namecert}, the reflector has no displayed numerical budget with which to select or compare StreamName windows. Hence its sibling dependency route must pass through DyadicRatCore before the StreamName, RegSeqRat, and Real rows.
\end{proof}

\closureat{DyadicWindowReflectorUp}{seedStr}

\begin{closurestatus}{\DyadicWindowReflectorUp}
  \constructivestory{A $\DyadicWindowReflectorUp$ packet is generated from a DyadicRatCore tolerance budget, StreamName windows, RegSeqRat readback, a Real seal, a finite-window classifier row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-window-reflector-carrier}, \autoref{thm:dyadic-window-reflector-namecert-obligations}, \autoref{thm:dyadic-window-reflector-vision-route}, and \autoref{thm:dyadic-window-reflector-dyadicratcore-route} seed the Real-completeness finite-window reflector route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected limits, quotient Cauchy equality, unrestricted Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked dyadic-budget, stream-window, rational-readback, Real-seal, classifier, transport, replay, provenance, and local naming rows for BEDC.Derived.DyadicWindowReflectorUp.}
\end{closurestatus}
