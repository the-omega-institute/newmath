\begin{theorem}[Cauchy modulus refinement common-window selector extraction]
\label{thm:cauchy-modulus-refinement-common-window-selector-extraction}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet whose source schedules $M_0,M_1$, shared meet $U$, refinement witness $V$, and selector-budget row $T$ are the displayed rows of the carrier. Every $\RealUp$-facing or diagonal-completion consumer that reaches the selected-window rows must extract the same finite-window and tolerance route $W,Q$ through $T$. No hidden selector, quotient stream, selected limit, host real equality, second meet row, or ambient completeness coordinate may supply a competing selected-window route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCommonWindowSelectorExtraction}
\begin{proof}
First lock the common source. Apply common source lock and shared threshold transport, \autoref{thm:cauchy-modulus-refinement-common-source-lock} and \autoref{thm:cauchy-modulus-refinement-shared-threshold-transport}, to force every accepted read of $U$ and $V$ through the same $M_0,M_1$ schedules.

Next extract the selected-window route. Selector-budget totality, \autoref{thm:cauchy-modulus-refinement-selector-budget-totality}, makes $T$ the sole admitted route from $V$ to the selected finite-window surface, and window-tolerance exactness, \autoref{thm:cauchy-modulus-refinement-window-tolerance-exactness}, identifies that surface with the displayed $W,Q$ rows.

Finally pass to downstream consumers. The diagonal-consumer boundary, \autoref{thm:cauchy-modulus-refinement-diagonal-consumer-boundary}, says that Real-facing and diagonal-completion reads factor through the displayed sibling rows and cannot import quotient streams, selected limits, host real equality, hidden selectors, second meet rows, side channel completion objects, or ambient completeness. Hence the consumer can extract $W,Q$ only by the common route through $T$.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real-completeness handoff]
\label{thm:cauchy-modulus-refinement-real-completeness-handoff}
Every downstream Real-completeness consumer of an accepted $\CauchyModulusRefinementUp$ refinement packet must read the shared meet row, extraction witness, selector budget, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, $\RegSeqRatUp$ readbacks, and $\RealUp$ seal in the displayed order
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N
$$
before using the completion boundary. The consumer may cite the packet only through the shared $\CauchyModulusMeetUp$, $\CauchyModulusExtractionUp$, $\RegularCauchySelectorBudgetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows carried by that finite route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementRealCompletenessHandoff\_semantic\_name\_certificate}
\begin{proof}
Begin with the root-unblock package of \autoref{thm:cauchy-modulus-refinement-root-unblock-package}. It packages the local $\NameCert$ scope, budget-selector source lock, and diagonal-consumer boundary as one accepted route for downstream Real-completion consumers.

Use the diagonal-consumer boundary from \autoref{thm:cauchy-modulus-refinement-diagonal-consumer-boundary}. It forces any Real or diagonal-completion read through the displayed sibling rows before the seal row can be consumed.

Next apply Real-seal factorization and window-tolerance exactness, \autoref{thm:cauchy-modulus-refinement-real-seal-factorization} and \autoref{thm:cauchy-modulus-refinement-window-tolerance-exactness}. These identify the selected windows, dyadic tolerance rows, regular readbacks, and Real seal as the same ordered route after $M_0,M_1,U,V,T$.

The completion boundary is therefore reached only after the displayed meet, extraction, selector, window, tolerance, readback, and seal rows have been read. Since the packet has no quotient stream, selected host limit, host real equality, hidden selector, side channel completion object, second meet row, or ambient completeness coordinate, the stated handoff exhausts the Real-completeness consumer surface.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root window-selector totality]
\label{thm:cauchy-modulus-refinement-root-window-selector-totality}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Every downstream read of a selected finite window or dyadic tolerance row factors through the root route
$$
M_0,M_1,U,V,T
$$
before it reaches $W$ or $Q$. Thus the selector-budget row $T$ is the total root selector for the displayed $\StreamNameUp$ and $\DyadicRatCoreUp$ window surface used by downstream $\RegSeqRatUp$ and $\RealUp$ consumers.
\end{theorem}
\begin{proof}
First read the root-unblock package of \autoref{thm:cauchy-modulus-refinement-root-unblock-package}. It permits downstream Real-completion consumers to cite the refinement packet only through the displayed finite route.

Next apply selector-budget totality from \autoref{thm:cauchy-modulus-refinement-selector-budget-totality}. This makes $T$ the admitted selector from the extraction witness $V$ to the selected finite-window and dyadic-tolerance surface.

Finally use common-window selector extraction from \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}. It identifies every downstream selected-window read with the same $W,Q$ route through $T$, so no consumer can choose a separate selector after reading $M_0,M_1,U,V$.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementRootWindowSelectorTotality}

\begin{theorem}[Cauchy modulus refinement root readback stability]
\label{thm:cauchy-modulus-refinement-root-readback-stability}
For an accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$, the selected-window and tolerance readbacks $W,Q$ are stable under the componentwise $\hsame$ row $H$ and the displayed route row $C$. Any readback consumed by $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, or $\RealUp$ must use the displayed rows $W,Q,H,C,P,N$, with no hidden selector, quotient stream, selected host limit, or ambient completion coordinate.
\end{theorem}
\begin{proof}
Begin with classifier transport from \autoref{thm:cauchy-modulus-refinement-classifier-transport}. It says componentwise $\hsame$ and the displayed $\Cont$ route carry accepted refinement packets without adding a new coordinate.

Use window-readback exhaustion from \autoref{thm:cauchy-modulus-refinement-window-readback-exhaustion}. The selected-window and tolerance readbacks are precisely the displayed $W,Q$ rows attached to the packet.

Now combine that exhaustion with root window-selector totality. Since every selected-window read reaches $W,Q$ only through $M_0,M_1,U,V,T$, transport along $H$ and $C$ preserves the same root readback route.

The non-escape boundary of \autoref{thm:cauchy-modulus-refinement-nonescape} rules out hidden selector, quotient stream, selected host limit, host real equality, side channel completion object, second meet row, and ambient completeness coordinates. Therefore the displayed sibling rows exhaust the readback surface.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinement\_root\_readback\_stability}

\begin{theorem}[Cauchy modulus refinement root Real seal exhaustion]
\label{thm:cauchy-modulus-refinement-root-real-seal-exhaustion}
Every Real-completion consumer of an accepted $\CauchyModulusRefinementUp$ packet reaches the Real seal row $E$ only through the displayed root route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
In particular, the consumer cannot bypass the selector, selected-window readback, componentwise transport, route, package, or local naming rows by appealing to a quotient stream, selected host limit, hidden selector, side channel completion object, second meet row, host real equality, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_real\_seal\_exhaustion}
\begin{proof}
Start with the Real-completeness handoff of \autoref{thm:cauchy-modulus-refinement-real-completeness-handoff}. It places the meet, extraction, selector, finite-window, tolerance, readback, and seal rows before any completion-facing use.

Apply the diagonal-consumer boundary from \autoref{thm:cauchy-modulus-refinement-diagonal-consumer-boundary}. This forces Real-completion and diagonal-completion consumers to factor through the displayed sibling dependency lattice and the local $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Finally combine root readback stability with non-escape, \autoref{thm:cauchy-modulus-refinement-root-readback-stability} and \autoref{thm:cauchy-modulus-refinement-nonescape}. The $W,Q$ readbacks and the seal row $E$ stay on the same displayed route under $H$ and $C$, and no forbidden completion coordinate remains available. Hence $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ exhaust the root Real-seal surface.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root-threshold scope]
\label{thm:cauchy-modulus-refinement-root-threshold-scope}
Every accepted $\CauchyModulusRefinementUp$ refinement packet routes its root threshold scope through the displayed finite rows
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
No Real- or Completion-facing consumer may replace that root route by a second meet, hidden selector, selected host limit, quotient stream, host real equality, side-channel completion row, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_threshold\_scope}
\begin{proof}
Read the carrier definition and the root-unblock package of \autoref{thm:cauchy-modulus-refinement-root-unblock-package}. Together they identify the only root threshold inventory as the source schedules, meet, extraction, selector-budget, selected-window, tolerance, seal, transport, route, provenance, and local naming rows.

Apply common-window selector extraction from \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}. It forces $W,Q$ to be read from the same selector-budget route $T$ after $M_0,M_1,U,V$.

Finally use the Real-completeness handoff of \autoref{thm:cauchy-modulus-refinement-real-completeness-handoff}. The downstream seal read arrives only after the displayed root route, so the listed excluded coordinates have no packet row from which to enter.
\end{proof}

\begin{theorem}[Cauchy modulus refinement shared-tail handoff]
\label{thm:cauchy-modulus-refinement-shared-tail-handoff}
Every shared-tail handoff exported by an accepted $\CauchyModulusRefinementUp$ packet factors through the existing $\CauchySharedTailModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ surfaces. The handoff reads the same selector-budget, window, tolerance, readback, and seal route $T,W,Q,E$ already displayed by the packet, together with $H,C,P,N$.
\end{theorem}
\begin{proof}
Begin with the root-unblock package. It allows a downstream Real-completion consumer to cite the refinement packet only through the displayed finite rows and their named sibling surfaces.

Use common-window selector extraction to reach $W,Q$ through $T$. The shared-tail side can therefore consume only the finite window and tolerance rows supplied by the same route.

The Real-completeness handoff then carries those rows into the $\RegSeqRatUp$ readback and $\RealUp$ seal surface, with $\CauchySharedTailModulusUp$ serving as the shared-tail threshold sibling. No additional tail witness or host completion row is exported.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_shared\_tail\_handoff}

\begin{theorem}[Cauchy modulus refinement shared-tail selector-window determinacy]
\label{thm:cauchy-modulus-refinement-shared-tail-selector-window-determinacy}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose two accepted refinement routes expose the same shared-tail modulus sibling, the same selector-budget row $T$, the same selected $\StreamNameUp$ window $W$, the same dyadic tolerance row $Q$, and the same $\RealUp$ seal $E$. Then they determine the same Real-completion consumer surface through the displayed $\BHist$ route, componentwise $\hsame$ row $H$, displayed $\Cont$ row $C$, $\Pkg$ provenance row $P$, and local $\NameCert_{\CauchyModulusRefinementUp}$ row $N$.

No quotient stream, hidden selector, selected host limit, second meet row, or ambient completeness coordinate separates the two routes once the shared-tail sibling and the $T,W,Q,E$ rows agree.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementSharedTailSelectorWindowDeterminacy}
\begin{proof}
First force the selector window. Root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, makes both routes pass through $M_0,M_1,U,V,T$ before either route can expose the selected $\StreamNameUp$ window $W$ and dyadic tolerance row $Q$.

Next apply root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}. Agreement on the selected window and tolerance makes the readback travel through the same displayed $H$ and $C$ rows rather than through a detached stream representative.

Use shared-tail handoff, \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff}. The common shared-tail modulus sibling carries the same selector-budget, window, tolerance, RegSeqRat readback, and Real seal route into $T,W,Q,E,H,C,P,N$.

Finally combine Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}, with selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}. With $T,W,Q,E$ fixed, the Real-completion consumer surface is fixed through $H,C,P,N$, and the excluded quotient, selector, host-limit, meet, and completeness coordinates have no row in the accepted packet.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real-consumer coverage]
\label{thm:cauchy-modulus-refinement-real-consumer-coverage}
Every $\RealUp$ or Completion consumer of an accepted $\CauchyModulusRefinementUp$ packet reads only the displayed selector-budget, window, readback, and seal route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Thus the Real-completion consumer surface is exhausted by the carrier route, the common-window selector extraction, and the Real-completeness handoff.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_consumer\_coverage}
\begin{proof}
Project the accepted carrier. The consumer-visible rows are exactly the displayed finite tuple and the structural rows $H,C,P,N$ that transport, replay, package, and name it.

Apply \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}. This removes any competing selected-window path by requiring $W,Q$ to be obtained through the same $T$ route.

Finally apply \autoref{thm:cauchy-modulus-refinement-real-completeness-handoff}. It orders the meet, extraction, selector, finite-window, tolerance, readback, and seal rows before any Real-completion use. Therefore every consumer read is covered by the displayed route and no hidden selector, quotient stream, selected limit, host equality, or ambient completeness coordinate remains.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root terminal selector readback]
\label{thm:cauchy-modulus-refinement-root-terminal-selector-readback}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose two accepted terminal reads of $R$ face either $\RealUp$ or Completion consumers. The two reads determine the same completed selector-readback consumer surface exactly when they agree on the displayed selector-budget row $T$, the selected $\StreamNameUp$ window $W$, the dyadic tolerance and readback row $Q$, the $\RegSeqRatUp$ readback, the $\RealUp$ seal $E$, the componentwise $\hsame$ transport row $H$, the displayed $\Cont$ route $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\CauchyModulusRefinementUp}$ row $N$.

The determinacy is internal to the displayed sibling surfaces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. Quotient streams, selected host limits, host real equality, second meet rows, hidden selectors, and ambient completeness coordinates are not coordinates of the accepted terminal packet and cannot distinguish the two reads.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementRootTerminalSelectorReadback}
\begin{proof}
First force the terminal reads onto the common selector spine. Common-window selector extraction, \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}, and root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, make each accepted terminal read pass through the same selector-budget row $T$ before it can expose $W$ or $Q$.

Next identify the readback rows. Root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}, carries agreement on the selected $\StreamNameUp$ window and $\DyadicRatCoreUp$ tolerance row to the same $\RegSeqRatUp$ readback through the componentwise $H$ transport and displayed $C$ route.

Then seal the completion-facing surface. Root Real seal exhaustion, \autoref{thm:cauchy-modulus-refinement-root-real-seal-exhaustion}, places the $\RealUp$ seal $E$ after the displayed readback rows, and shared-tail selector-window determinacy, \autoref{thm:cauchy-modulus-refinement-shared-tail-selector-window-determinacy}, identifies the selector-window surface once $T,W,Q,E,H,C,P,N$ agree.

For the forward direction, equal completed selector-readback surfaces expose the same terminal rows by Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}. Hence the two reads agree on $T,W,Q$, the $\RegSeqRatUp$ readback, $E,H,C,P,N$, and the local naming row.

For the reverse direction, agreement on those displayed rows reconstructs the same accepted route across $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. The same cited coverage and exhaustion results exclude quotient streams, selected host limits, host real equality, second meet rows, hidden selectors, and ambient completeness coordinates, so no off-route datum remains from which a second terminal consumer surface can be read.
\end{proof}

\begin{theorem}[Cauchy modulus refinement and diagonal L10 consumer spine comparison]
\label{thm:cauchy-modulus-refinement-l10-consumer-spine-comparison}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet, and let $K$ be an accepted $\DiagonalLimitCompatibilityUp$ packet. Suppose the two packets expose the same $\StreamNameUp$ window, the same $\DyadicRatCoreUp$ tolerance ledger, the same $\RegSeqRatUp$ readback, the same $\RealUp$ seal, the same $\CauchyLimitSealUp$ boundary, and the same $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ support rows. Then every Phase Real and Completion consumer reads the same displayed L10 selector spine from $R$ and from $K$.

The comparison is internal to the shared StreamName, DyadicRatCore, RegSeqRat, Real, CauchyLimitSeal, and structural support rows. Quotient streams, selected host limits, host real equality, countable choice, ambient completeness, alternate Cauchy witnesses, and second selector sources cannot distinguish the CauchyModulusRefinement route from the DiagonalLimitCompatibility route once those rows agree.
\end{theorem}
\begin{proof}
First identify the CauchyModulusRefinement side. The root terminal selector readback theorem, \autoref{thm:cauchy-modulus-refinement-root-terminal-selector-readback}, makes every terminal Real or Completion read pass through the displayed selector-budget, window, tolerance, readback, Real-seal, transport, replay, provenance, and local naming rows. Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}, then exhausts the consumer surface by the same route.

Next identify the DiagonalLimitCompatibility side. Root readback exactness, \autoref{thm:diagonal-limit-compatibility-root-readback-exactness}, exposes the StreamName window, RegSeqRat readback, DyadicRatCore ledger, RealUp seal row, and CauchyLimitSeal boundary as the root readback surface. Root selector terminality and root L10 spine exhaustion, \autoref{thm:diagonal-limit-compatibility-root-selector-terminality} and \autoref{thm:diagonal-limit-compatibility-root-l10-spine-exhaustion}, make that surface terminal for the same Phase Real and Completion consumers.

Now use the shared-row hypothesis. The StreamName window, DyadicRatCore tolerance ledger, RegSeqRat readback, Real seal, CauchyLimitSeal boundary, $\BHist$ support, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row are the same on both packets, so the two terminal surfaces display the same L10 selector spine.

Finally apply the non-escape clauses on both sides. The CauchyModulusRefinement readback theorem rules out quotient streams, selected host limits, host real equality, hidden selectors, and second meet rows, while the DiagonalLimitCompatibility terminality and exhaustion results rule out quotient streams, selected limits, host real equality, countable-choice selectors, ambient completeness, and second completion witnesses. No remaining carrier coordinate can separate the two routes.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_l10\_consumer\_spine\_comparison}

\begin{theorem}[Cauchy modulus refinement terminal-spine choice refusal]
\label{thm:cauchy-modulus-refinement-terminal-spine-choice-refusal}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose an accepted terminal selector read agrees on the displayed selector-budget row $T$, the selected $\StreamNameUp$ window $W$, the $\DyadicRatCoreUp$ tolerance row $Q$, the $\RegSeqRatUp$ readback, the $\RealUp$ seal $E$, the structural $\BHist$ route, the componentwise $\hsame$ row $H$, the displayed $\Cont$ row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\CauchyModulusRefinementUp}$ row $N$.

Then no countable-choice row, quotient stream, host real equality, ambient completeness coordinate, or alternate selector source can distinguish the Real-completion consumer surface determined by that terminal read.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementTerminalSpineChoiceRefusal}
\begin{proof}
First place the terminal selector read on the displayed spine. The root terminal selector readback theorem, \autoref{thm:cauchy-modulus-refinement-root-terminal-selector-readback}, says that agreement on $T,W,Q$, the $\RegSeqRatUp$ readback, $E,H,C,P,N$, and the local naming row determines the same completed selector-readback consumer surface.

Next seal the Real-facing end of the route. Root Real seal exhaustion, \autoref{thm:cauchy-modulus-refinement-root-real-seal-exhaustion}, places the $\RealUp$ seal after the displayed readback rows, while Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}, exhausts every Real or Completion consumer by $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$.

Finally use shared-tail selector-window determinacy, \autoref{thm:cauchy-modulus-refinement-shared-tail-selector-window-determinacy}. Once the selector budget, stream window, dyadic tolerance, readback, seal, and structural rows agree, the terminal surface has no remaining carrier coordinate for countable choice, quotient streams, host real equality, ambient completeness, or an alternate selector source.
\end{proof}

\begin{theorem}[Cauchy modulus refinement shared-tail selector determinacy]
\label{thm:cauchy-modulus-refinement-shared-tail-selector-determinacy}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet whose common-window selector, shared-tail handoff, and Real-consumer coverage rows read the same $\RegSeqRatUp/\StreamNameUp$ window. If two selector routes expose the same dyadic tail budget $Q$ and the same $\RealUp$ seal $E$, then the refinement rows determined by those routes are classifier-equal through the displayed $\BHist$ rows. Equivalently, the routes may differ only by componentwise $\hsame$ transport along the common window and shared-tail rows; they cannot determine distinct refinement rows under the same tail budget and Real seal.
\end{theorem}
\begin{proof}
Project both selector routes to the displayed carrier rows. Root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, forces each route to pass through $M_0,M_1,U,V,T$ before it reaches the selected $\StreamNameUp$ window and dyadic budget rows.

Next apply root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}. Since both routes expose the same $Q$ row and the same $\RegSeqRatUp/\StreamNameUp$ window, their readbacks are transported by the componentwise $H$ row and replayed only through the displayed route row $C$.

Use the shared-tail handoff of \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff}. It identifies the tail-facing route with the same selector-budget, window, tolerance, readback, and seal surface $T,W,Q,E,H,C,P,N$, so no second shared-tail witness can distinguish the two selectors.

Finally apply Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}. The common Real seal is reached only through the displayed finite route, and the packet has no hidden selector, quotient stream, selected host limit, or ambient completeness coordinate. Thus the two projected $\BHist$ rows are connected by componentwise $\hsame$ transport through the common window and shared-tail routes, which is exactly classifier equality for the refinement rows.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_shared\_tail\_selector\_determinacy}

\begin{theorem}[Cauchy modulus refinement selector-budget RegSeqRat Real-seal determinacy]
\label{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose two downstream Real-completion routes expose the same selector-budget row $T$, the same finite-window $\RegSeqRatUp/\StreamNameUp$ readback through $W,Q$, and the same $\RealUp$ seal row $E$. Then the Real-completion consumer surfaces determined by those routes are classifier-equal through the displayed $\BHist$ rows, componentwise $\hsame$ row $H$, displayed $\Cont$ row $C$, $\Pkg$ provenance row $P$, and local $\NameCert_{\CauchyModulusRefinementUp}$ row $N$.
\end{theorem}
\begin{proof}
Project both downstream routes to the accepted carrier. Root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, forces each route through $M_0,M_1,U,V,T$ before either route reaches a selected finite-window row.

Next use root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}. Since the two routes expose the same $\RegSeqRatUp/\StreamNameUp$ finite-window readback and the same dyadic tolerance rows, $W,Q$ are replayed only through the displayed $H,C,P,N$ rows.

Apply the shared-tail handoff, \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff}. It aligns the selector-budget, finite-window, tolerance, readback, and seal rows with the $\CauchySharedTailModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ sibling surfaces, so no separate RegSeqRat or stream-name route remains.

Finally apply Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}. The displayed packet has no quotient stream, selected host limit, hidden selector, host equality, or ambient completeness coordinate. Therefore two routes with the same $T$, the same $W,Q$ readback, and the same $E$ seal determine the same Real-completion consumer surface through $\BHist$, $H$, $C$, $P$, and $N$.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinement\_selector\_budget\_regseqrat\_real\_seal\_determinacy}

\begin{theorem}[Cauchy modulus refinement diagonal handoff determinacy]
\label{thm:cauchy-modulus-refinement-diagonal-handoff-determinacy}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Any diagonal Real-completion handoff for $R$ that exposes the same selector-budget row $T$, selected $\StreamNameUp$ window row $W$, dyadic tolerance row $Q$, $\RegSeqRatUp$ readback surface, and $\RealUp$ seal row $E$ determines the same completion-facing consumer surface through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\CauchyModulusRefinementUp}$ rows.

The determination is only through the finite BEDC rows
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
No quotient stream, selected host limit, hidden selector, second meet row, host equality, or ambient completeness coordinate supplies another diagonal handoff.
\end{theorem}
\begin{proof}
First force the selector spine. Common-window selector extraction, \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}, and root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, make every diagonal handoff read the selected $\StreamNameUp$ window and dyadic tolerance through $M_0,M_1,U,V,T$ before it reaches $W,Q$.

Next pass through the completion-facing rows. Real-completeness handoff, \autoref{thm:cauchy-modulus-refinement-real-completeness-handoff}, places the selected window, dyadic tolerance, $\RegSeqRatUp$ readback, and $\RealUp$ seal in the displayed order, while shared-tail handoff, \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff}, carries those rows across the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ sibling surfaces.

Finally apply selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}. Agreement on $T,W,Q,E$ identifies the resulting completion-facing consumer surfaces through the displayed $\BHist$ route, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. The excluded coordinates are absent from the accepted packet, so none of them can separate the diagonal handoff after those rows are exposed.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementDiagonalHandoffDeterminacy}

\begin{theorem}[Cauchy modulus refinement uniform-criterion Real route determinacy]
\label{thm:cauchy-modulus-refinement-uniform-criterion-real-route-determinacy}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Any $\UniformCauchyCriterionUp$ consumer that exposes the same selector-budget row $T$, selected-window row $W$, dyadic-tolerance row $Q$, and $\RealUp$ seal row $E$ determines the same Real-completion consumer surface through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\CauchyModulusRefinementUp}$ rows. No quotient stream, selected host limit, ambient completeness row, hidden selector, or second meet row supplies a competing route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_uniform\_criterion\_real\_route\_determinacy}
\begin{proof}
First force the route to the displayed selector spine. Root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, places every selected-window read behind $M_0,M_1,U,V,T$, and root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}, keeps the resulting $W,Q$ readback on the same $H,C,P,N$ surface.

Next pass through the shared-tail handoff. By \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff}, the $\UniformCauchyCriterionUp$ consumer reaches the $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows through the same displayed tail route rather than through a detached completion coordinate.

Use Real-consumer coverage to identify the exposed completion surface. The theorem \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage} says that every Real-facing consumer read is exhausted by $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, so exposing $T,W,Q,E$ leaves no hidden selector, quotient stream, selected host limit, second meet row, or ambient completeness row to choose another path.

Finally apply selector-budget RegSeqRat Real-seal determinacy and tail-budget seal uniqueness, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy} and \autoref{thm:cauchy-modulus-refinement-tail-budget-seal-uniqueness}. The shared selector budget, selected window, dyadic tolerance, and Real seal determine one Real-completion consumer surface through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.
\end{proof}

\begin{theorem}[Cauchy modulus refinement uniform diagonal seal correspondence]
\label{thm:cauchy-modulus-refinement-uniform-diagonal-seal-correspondence}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Let $J$ be an accepted $\UniformCauchyCriterionUp$ tail packet and let $K$ be an accepted $\DiagonalLimitCompatibilityUp$ root packet. Suppose the selector-budget row, selected $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, $\RealUp$ seal, structural $\BHist$ route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows of $R$ agree with the corresponding terminal rows read by $J$ and $K$.

Then the $\CauchyModulusRefinementUp$ refinement-selector surface, the $\UniformCauchyCriterionUp$ tail surface, and the $\DiagonalLimitCompatibilityUp$ root surface expose the same terminal $\RealUp$ seal exactly when the three surfaces traverse the same displayed BEDC route through
$$
\StreamNameUp,\RegSeqRatUp,\DyadicRatCoreUp,\RealUp,\BHist,\hsame,\Cont,\Pkg,\NameCert.
$$
The correspondence is terminal for Real and Completion consumers: no quotient stream, selected host limit, host real equality, countable-choice selector, ambient completeness coordinate, hidden selector, second meet row, or side-channel completion row supplies another terminal seal surface.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_uniform\_diagonal\_seal\_correspondence}
\begin{proof}
For the forward direction, apply the UniformCauchyCriterion route theorem, \autoref{thm:cauchy-modulus-refinement-uniform-criterion-real-route-determinacy}. It places the uniform tail consumer on the displayed selector-budget, window, tolerance, readback, seal, transport, replay, provenance, and local naming route. Apply the diagonal handoff theorem, \autoref{thm:cauchy-modulus-refinement-diagonal-handoff-determinacy}, to place the diagonal root consumer on the same finite rows.

Now compare the two terminal readings with the refinement-selector surface. The L10 consumer spine comparison, \autoref{thm:cauchy-modulus-refinement-l10-consumer-spine-comparison}, identifies the CauchyModulusRefinement and DiagonalLimitCompatibility consumer spines once their $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows agree. Hence a common terminal $\RealUp$ seal exposes the same displayed BEDC route on all three surfaces.

For the reverse direction, assume the three surfaces traverse the same displayed route. Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}, exhausts the completion-facing consumer boundary of $R$ by $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$. Terminal-spine choice refusal, \autoref{thm:cauchy-modulus-refinement-terminal-spine-choice-refusal}, rules out any choice, quotient, host-equality, ambient-completeness, or alternate-selector coordinate after those terminal rows agree.

Therefore the shared route has only one terminal $\RealUp$ seal to expose. The agreed uniform tail packet, diagonal root packet, and refinement selector packet are three readings of that same finite BEDC terminal surface, so their Real and Completion consumers receive the same seal.
\end{proof}

\begin{theorem}[Cauchy modulus refinement selector-seal two-step transport]
\label{thm:cauchy-modulus-refinement-selector-seal-two-step-transport}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose a second downstream route has selector row $V'$, selector-budget row $T'$, tolerance-readback row $Q'$, and seal row $E'$. If the displayed $\hsame$ rows identify $V$ with $V'$ and $E$ with $E'$, and the displayed $\Cont$ rows carry the source-meet route through $U,V',T'$ and then through $T',W,Q'$, then the two routes determine the same selector-budget row and the same $\RealUp$ seal consumer surface through $H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_selector\_seal\_two\_step\_transport}
\begin{proof}
First apply selector-stability lock, \autoref{thm:cauchy-modulus-refinement-selector-stability-lock}. The $\hsame$ agreement on $V,V'$ and the continuation through $U,V',T'$ transport the selector route to the same selector-budget surface, so $T'$ cannot supply a detached selected-window ledger.

Next apply selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}. With the selector-budget agreement in place, the continuation through $T',W,Q'$ places the transported readback on the same $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ rows, while the $\hsame$ agreement on $E,E'$ identifies the $\RealUp$ seal row.

Finally use the non-escape clauses recorded by window-route factorization, \autoref{thm:cauchy-modulus-refinement-window-route-factorization}. The accepted packet has no hidden selector, quotient stream, selected host limit, second meet row, side-channel completion row, host real equality, or ambient completeness coordinate. Hence the two-step route from selector agreement to seal agreement exhausts the consumer surface through $H,C,P,N$.
\end{proof}

\begin{theorem}[Cauchy modulus refinement window-route factorization]
\label{thm:cauchy-modulus-refinement-window-route-factorization}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Every Real-completion consumer route that exposes the selected $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance, $\RegSeqRatUp$ readback, and $\RealUp$ seal factors through the ordered subroute
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
If two such routes expose the same selector-budget, selected-window, dyadic-tolerance, and seal rows $T,W,Q,E$, then they determine the same consumer surface through componentwise $\hsame$ transport along $H$, displayed replay through $C$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\CauchyModulusRefinementUp}$ row $N$. No quotient stream, selected host limit, hidden selector, second meet row, side-channel completion row, host real equality, or ambient completeness coordinate can replace that factorization.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_window\_route\_factorization}
\begin{proof}
First extract the selected-window spine. Common-window selector extraction, \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}, and root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, force every selected $\StreamNameUp$ window and $\DyadicRatCoreUp$ tolerance read through $M_0,M_1,U,V,T$ before it reaches $W,Q$.

Next stabilize the readback. Root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}, transports the same $W,Q$ readback only through $H,C,P,N$, so a consumer cannot split the $\RegSeqRatUp$ readback away from the displayed window route.

Then pass to the completion-facing seal. Real-completeness handoff, \autoref{thm:cauchy-modulus-refinement-real-completeness-handoff}, places the selected window, dyadic tolerance, regular readback, and Real seal in the displayed order before any completion boundary is consumed.

Finally apply selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}. Agreement on $T,W,Q,E$ identifies the two Real-completion consumer surfaces through componentwise $\hsame$ transport and the displayed $C,P,N$ rows. The excluded coordinates are absent from the accepted packet, so none of them supplies an alternative factorization route.
\end{proof}

\begin{theorem}[Cauchy modulus refinement tail-budget seal uniqueness]
\label{thm:cauchy-modulus-refinement-tail-budget-seal-uniqueness}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose two Real-completion or diagonal-completion tail-budget consumers expose the same selector-budget row $T$, selected-window row $W$, dyadic tolerance row $Q$, $\RealUp$ seal row $E$, and the same structural rows $H,C,P,N$. Then both consumers determine the same seal surface through the displayed route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
No quotient stream, selected host limit, side-channel completion row, host real equality, hidden selector, or second meet row can produce a different $\RealUp$ tail-budget seal.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_tail\_budget\_seal\_uniqueness}
\begin{proof}
First force the route to the common selector spine. Common-window selector extraction, \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}, together with root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, makes both consumers pass through $M_0,M_1,U,V,T$ before either can read a selected window.

Next stabilize the visible tail rows. Root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}, identifies the shared $W,Q$ readback through the same componentwise $H$ transport and displayed replay row $C$.

The seal row is then fixed by the completion-facing handoff. Real-completeness handoff, \autoref{thm:cauchy-modulus-refinement-real-completeness-handoff}, places $E$ after the same selector-budget, selected-window, dyadic-tolerance, and readback rows, with provenance and local naming carried only by $P,N$.

Finally apply selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}, and window-route factorization, \autoref{thm:cauchy-modulus-refinement-window-route-factorization}. Since the consumers agree on $T,W,Q,E,H,C,P,N$, the displayed route determines a unique seal surface; the excluded coordinates are not packet rows and cannot supply another tail-budget seal.
\end{proof}

\begin{theorem}[Cauchy modulus refinement selector-budget tail-meet non-escape]
\label{thm:cauchy-modulus-refinement-selector-budget-tail-meet-nonescape}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Every Real-completion consumer that reads the selector-budget row $T$, selected-window row $W$, dyadic tolerance row $Q$, and $\RealUp$ seal row $E$ must first pass through the shared meet and refinement route
$$
M_0,M_1,U,V,T
$$
and then through the shared-tail handoff to $W,Q,E,H,C,P,N$. No quotient stream, hidden selector, second meet row, host real equality, or ambient completeness row can replace that shared-tail meet surface.
\end{theorem}
\begin{proof}
First force the selector spine. Common-window selector extraction, \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}, and root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, make every read of $T,W,Q$ pass through $M_0,M_1,U,V,T$ before a selected window is available.

Next pass from the selector spine to the tail-facing interface. The shared-tail handoff of \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff} identifies the same route with the displayed $\CauchySharedTailModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows, so the handoff is carried by $W,Q,E,H,C,P,N$ rather than by a detached completion coordinate.

Now apply selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}. Once the consumer has exposed $T,W,Q,E$, the Real-completion surface is fixed through the displayed $\BHist$ route, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.

Finally use tail-budget seal uniqueness, \autoref{thm:cauchy-modulus-refinement-tail-budget-seal-uniqueness}. A second route with the same selector budget, selected window, dyadic tolerance, and seal collapses to the same tail-budget seal surface. The excluded quotient stream, hidden selector, second meet row, host real equality, and ambient completeness rows are not packet coordinates, so none can bypass the shared-tail meet route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_selector\_budget\_tail\_meet\_nonescape}

\begin{definition}[Cauchy modulus refinement terminal selector surface]
\label{def:cauchy-modulus-refinement-terminal-selector-surface}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. The terminal selector surface of $R$ is the displayed consumer route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N
$$
from the shared meet rows $M_0,M_1,U$, through the refinement rows $V,T$, and into the selected-window, tolerance-readback, Real-seal, transport, replay, provenance, and local naming rows $W,Q,E,H,C,P,N$.

Two terminal consumer routes agree on this surface exactly when they agree on the selector budget $T$, the selected window $W$, the dyadic tolerance $Q$, the $\RegSeqRatUp$ readback carried by $W,Q$, the $\RealUp$ seal $E$, the componentwise $\hsame$ transport row $H$, the displayed $\Cont$ replay row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert$ row $N$.
\end{definition}
\leandef{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementTerminalSelectorSurface}

\begin{theorem}[Cauchy modulus refinement terminal selector-seal correspondence]
\label{thm:cauchy-modulus-refinement-terminal-selector-seal-correspondence}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Two Real-completion consumer routes expose the same terminal selector surface of \autoref{def:cauchy-modulus-refinement-terminal-selector-surface} if and only if they expose the same ordered selector-budget row $T$, selected-window row $W$, dyadic-tolerance row $Q$, $\RegSeqRatUp$ readback row, and $\RealUp$ seal row $E$ through the same structural rows $H,C,P,N$.

Consequently no terminal consumer can identify the $\RealUp$ seal of $R$ without passing through the displayed selector-budget route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
\end{theorem}
\begin{proof}
For the forward direction, first force both routes to the common selector spine. Root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, places every selected-window read behind $M_0,M_1,U,V,T$, while root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}, keeps the $W,Q$ readback on the displayed $H,C,P,N$ surface.

Next use shared-tail handoff, \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff}. Once the routes expose the same terminal selector surface, the handoff carries the same selector budget, selected window, dyadic tolerance, RegSeqRat readback, and RealUp seal into $T,W,Q,E$ with no detached completion coordinate.

For the reverse direction, assume the two routes expose the same $T,W,Q,E,H,C,P,N$ rows. Window-route factorization, \autoref{thm:cauchy-modulus-refinement-window-route-factorization}, makes every Real-completion consumer factor through those rows, and selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}, identifies the Real-facing seal surface once those rows agree.

Finally apply tail-budget selector lock, \autoref{thm:cauchy-modulus-refinement-tail-budget-selector-lock}. Quotient streams, host real equality, hidden selectors, second meet rows, selected host limits, side-channel completion rows, and ambient completeness are not packet coordinates of $R$, so no off-route consumer can identify the terminal $\RealUp$ seal without the displayed selector-budget route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_terminal\_selector\_seal\_correspondence}

\begin{theorem}[Cauchy modulus refinement tail-budget selector lock]
\label{thm:cauchy-modulus-refinement-tail-budget-selector-lock}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. If two Real-completion consumers of $R$ expose the same selector budget $T$, selected window $W$, dyadic tolerance $Q$, $\RealUp$ seal $E$, and structural rows $H,C,P,N$, then they are classifier-equal through the displayed route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
No hidden selector, second meet row, quotient stream, selected host limit, host real equality, or ambient completeness row can split the Real-completion consumer surface once those rows have been read.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_tail\_budget\_selector\_lock}
\begin{proof}
First force both consumers through the common selector spine. Root window-selector totality, \autoref{thm:cauchy-modulus-refinement-root-window-selector-totality}, places every selected-window read behind $M_0,M_1,U,V,T$.

Next stabilize the readback rows. Root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}, keeps the shared $W,Q$ readback inside the displayed $H,C,P,N$ surface rather than a host-side stream witness.

Apply selector-budget RegSeqRat Real-seal determinacy, \autoref{thm:cauchy-modulus-refinement-selector-budget-regseqrat-real-seal-determinacy}. Since both consumers expose the same $T,W,Q,E$ rows, their Real-facing consumer surfaces are identified through the same $\BHist$ route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.

Finally use tail-budget seal uniqueness, \autoref{thm:cauchy-modulus-refinement-tail-budget-seal-uniqueness}. The agreement on $H,C,P,N$ locks the seal to the same tail-budget route, and the excluded coordinates are absent from the accepted packet.
\end{proof}

\begin{theorem}[Cauchy modulus refinement shared-meet RegSeqRat route]
\label{thm:cauchy-modulus-refinement-shared-meet-regseqrat-route}
Every $\RegSeqRatUp/\RealUp$ consumer of an accepted $\CauchyModulusRefinementUp$ packet $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ must pass through the shared meet and extraction route
$$
M_0,M_1,U,V,T
$$
before reading the selected window $W$, dyadic tolerance $Q$, and $\RealUp$ seal $E$. The consumer cannot obtain the RegSeqRat readback or Real seal from a detached selector, quotient stream, second meet row, host real equality, selected host limit, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start with common-window selector extraction, \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}. It identifies any downstream selected-window read with the same selector-budget route and therefore places $W,Q$ after $T$.

Next apply the shared-tail handoff, \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff}. The handoff carries the selected $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance, $\RegSeqRatUp$ readback, and $\RealUp$ seal on the displayed $W,Q,E,H,C,P,N$ rows.

Use Real-consumer coverage, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}, to identify the completion-facing consumer surface with the same finite packet. The Real read has no additional carrier coordinate beyond the displayed refinement rows.

Finally apply selector-budget tail-meet non-escape, \autoref{thm:cauchy-modulus-refinement-selector-budget-tail-meet-nonescape}. Once the consumer reads $T,W,Q,E$, every admissible route has already passed through $M_0,M_1,U,V,T$, so the excluded off-route data cannot supply a second RegSeqRat or Real route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_shared\_meet\_regseqrat\_route}

\begin{theorem}[Cauchy modulus refinement shared-budget pullback handoff]
\label{thm:cauchy-modulus-refinement-shared-budget-pullback-handoff}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Every $\RealUp$ or Completion consumer that uses the terminal selector-seal surface of $R$ enters through the shared-budget pullback route fixed by
$$
U,V,T,W,Q,E,H,C,P,N.
$$
If two such consumers agree on those rows, then they expose the same terminal $\RealUp$ seal surface. No quotient stream, hidden selector, second meet row, host real equality, selected host limit, side-channel completion row, or ambient completeness coordinate supplies an alternate handoff.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_shared\_budget\_pullback}
\begin{proof}
First bind the shared budget. The shared-budget pullback theorem, \autoref{thm:cauchy-modulus-refinement-shared-budget-pullback}, makes the meet row $U$ and selector-budget row $T$ determine the selected-window row $W$ and tolerance-readback row $Q$ through the displayed refinement route.

Next route the terminal handoff through the finite sibling surfaces. The shared-tail handoff of \autoref{thm:cauchy-modulus-refinement-shared-tail-handoff} carries $W,Q,E,H,C,P,N$ across the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces, while \autoref{thm:cauchy-modulus-refinement-shared-meet-regseqrat-route} places every RegSeqRat and Real consumer behind the same shared meet and extraction route $U,V,T$.

Now identify the terminal selector-seal surface. By \autoref{thm:cauchy-modulus-refinement-terminal-selector-seal-correspondence}, agreement on the displayed selector-budget, selected-window, tolerance-readback, seal, transport, replay, provenance, and local naming rows is exactly agreement on the terminal selector surface and its $\RealUp$ seal.

Finally apply tail-budget selector lock, \autoref{thm:cauchy-modulus-refinement-tail-budget-selector-lock}. The excluded off-route data are not coordinates of the accepted packet, so they cannot supply a second handoff once $U,V,T,W,Q,E,H,C,P,N$ have fixed the terminal route.
\end{proof}

\begin{theorem}[Cauchy modulus refinement terminal Real-seal pullback exhaustion]
\label{thm:cauchy-modulus-refinement-terminal-real-seal-pullback-exhaustion}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. For every $\RealUp$ or Completion consumer of $R$, identifying the terminal $\RealUp$ seal through the shared-budget pullback is equivalent to exposing the same rows
$$
U,V,T,W,Q,E,H,C,P,N
$$
through the shared meet, selector-budget, selected-window, readback, seal, transport, replay, provenance, and local naming surfaces. Conversely, agreement on those rows determines the same terminal $\RealUp$ seal surface for the consumer.

No quotient stream, hidden selector, second meet row, host real equality, selected host limit, side-channel completion row, or ambient completeness coordinate contributes an additional terminal seal route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_terminal\_real\_seal\_pullback\_exhaustion}
\begin{proof}
First force the shared meet and selector route. The shared-meet RegSeqRat route, \autoref{thm:cauchy-modulus-refinement-shared-meet-regseqrat-route}, places every $\RegSeqRatUp$ and $\RealUp$ consumer behind $U,V,T$, and the tail-budget selector lock, \autoref{thm:cauchy-modulus-refinement-tail-budget-selector-lock}, prevents a second selector surface once $T,W,Q,E,H,C,P,N$ are read.

Next pass through the finite sibling handoff. The shared-budget pullback handoff, \autoref{thm:cauchy-modulus-refinement-shared-budget-pullback-handoff}, carries the route from $U,V,T$ to $W,Q,E,H,C,P,N$ across the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces. Thus a consumer that identifies the terminal seal through the pullback must expose exactly the displayed shared-budget rows.

For the converse direction, assume the consumer exposes the same $U,V,T,W,Q,E,H,C,P,N$ rows. Terminal selector-seal correspondence, \autoref{thm:cauchy-modulus-refinement-terminal-selector-seal-correspondence}, identifies agreement on the selector-budget, selected-window, dyadic-tolerance, readback, seal, transport, replay, provenance, and local naming rows with agreement on the terminal selector surface and its $\RealUp$ seal.

Finally apply the exclusion clauses supplied by tail-budget selector lock and shared-budget pullback handoff. Quotient streams, hidden selectors, second meet rows, host real equality, selected host limits, side-channel completion rows, and ambient completeness coordinates are absent from the accepted packet, so none of them can supply a separate terminal pullback. Hence the displayed row agreement is exactly the consumer boundary for the terminal $\RealUp$ seal.
\end{proof}

\begin{theorem}[Cauchy modulus refinement terminal pullback consumer uniqueness]
\label{thm:cauchy-modulus-refinement-terminal-pullback-consumer-uniqueness}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose two $\RealUp$ or Completion consumers of $R$ expose the same shared-budget pullback rows
$$
U,V,T,W,Q,E,H,C,P,N.
$$
Then the two consumers expose the same terminal $\RealUp$ seal consumer surface. No quotient stream, hidden selector, second meet row, host real equality, selected host limit, side-channel completion row, or ambient completeness coordinate can distinguish the two terminal consumers.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_terminal\_pullback\_consumer\_uniqueness}
\begin{proof}
First lock the selector boundary. Tail-budget selector lock, \autoref{thm:cauchy-modulus-refinement-tail-budget-selector-lock}, makes the displayed selector-budget route the only accepted route from the tail budget to the selected-window and tolerance rows.

Next read both consumers through the shared meet. The shared-meet RegSeqRat route, \autoref{thm:cauchy-modulus-refinement-shared-meet-regseqrat-route}, forces every $\RegSeqRatUp$, $\RealUp$, or Completion-facing read behind the same $U,V,T$ spine before $W,Q,E$ can be consumed.

Now pass through the pullback handoff. By \autoref{thm:cauchy-modulus-refinement-shared-budget-pullback-handoff}, agreement on $U,V,T,W,Q,E,H,C,P,N$ is agreement on the finite handoff across the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surfaces.

Finally apply terminal Real-seal pullback exhaustion, \autoref{thm:cauchy-modulus-refinement-terminal-real-seal-pullback-exhaustion}. Since the two consumers expose the same displayed pullback rows, their terminal seal surfaces are the same, and the excluded off-route coordinates have no accepted slot from which to separate them.
\end{proof}

\begin{theorem}[Cauchy modulus refinement selector-budget normal form]
\label{thm:cauchy-modulus-refinement-selector-budget-normal-form}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Any consumer route of $R$ shared by $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RealUp$ is exhausted by one finite selector-budget normal form:
$$
U,V,T,W,Q,E,H,C,P,N.
$$
The rows are, in order, the shared meet row, extraction row, selector-budget row, selected-window row, tolerance-readback row, $\RealUp$ seal row, and the support, provenance, and local naming rows. No quotient stream, hidden selector, second meet row, host real equality, selected host limit, side-channel completion row, or ambient completeness coordinate supplies another shared consumer route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_selector\_budget\_normal\_form}
\begin{proof}
First force the shared meet and extraction route. The RegSeqRat--StreamName--Real coherence surface, \autoref{thm:cauchy-modulus-refinement-regseq-streamname-real-coherence}, and the selector exactness surface, \autoref{thm:cauchy-modulus-refinement-regseq-real-selector-exactness}, place the four sibling consumers behind the same $U,V,T$ spine before a selected window or Real seal is read.

Next expose the finite observation budget. The shared-budget pullback handoff, \autoref{thm:cauchy-modulus-refinement-shared-budget-pullback-handoff}, carries that spine through $W,Q,E,H,C,P,N$ across the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ faces.

Then apply terminal pullback consumer uniqueness, \autoref{thm:cauchy-modulus-refinement-terminal-pullback-consumer-uniqueness}. Once a consumer exposes the displayed $U,V,T,W,Q,E,H,C,P,N$ rows, it has the same terminal $\RealUp$ seal consumer surface as any other consumer exposing those rows.

Finally use \autoref{thm:cauchy-modulus-refinement-tail-budget-selector-lock}. The off-route coordinates listed in the statement are not rows of the accepted packet, so the selector-budget normal form is the only shared consumer route available to the four sibling surfaces.
\end{proof}

\begin{theorem}[Cauchy modulus refinement uniform monotone terminal route]
\label{thm:cauchy-modulus-refinement-uniform-monotone-terminal-route}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose a $\UniformCauchyCriterionUp$ consumer and a $\MonotoneCauchyUp$ consumer both read the selector-budget row $T$, selected-window row $W$, dyadic-tolerance row $Q$, and $\RealUp$ seal row $E$ of $R$ through the same displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\CauchyModulusRefinementUp}$ rows.

Then the two consumers determine one terminal $\RealUp$ seal surface. The shared surface is exactly the handoff through
$$
U,V,T,W,Q,E,H,C,P,N,
$$
with the $\UniformCauchyCriterionUp$ RegSeqRat--Real classifier face and the $\MonotoneCauchyUp$ located Real-seal face read as projections of that same finite route. No quotient stream, host real equality, selected limit, trichotomy row, ambient completeness principle, metric-completeness datum, hidden selector, or second meet row supplies a second terminal handoff.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_uniform\_monotone\_terminal\_route}
\begin{proof}
First place both consumers on the CauchyModulusRefinement terminal route. The terminal pullback consumer uniqueness theorem, \autoref{thm:cauchy-modulus-refinement-terminal-pullback-consumer-uniqueness}, identifies every Real or Completion consumer that agrees on $U,V,T,W,Q,E,H,C,P,N$ with the same terminal $\RealUp$ seal consumer surface.

Next read the uniform criterion face. The UniformCauchyCriterion terminal RegSeqRat--Real classifier correspondence, \autoref{thm:uniform-cauchy-criterion-terminal-regseq-real-classifier-correspondence}, says that the RegSeqRat tail classifier, StreamName finite-window classifier, and Real finite-window seal classifier are projections of one ordered route. Under the shared rows of $R$, that route is the displayed $T,W,Q,E,H,C,P,N$ suffix.

Now read the monotone face. The MonotoneCauchy Real-seal factorization obligation, \autoref{thm:monotone-cauchy-real-seal-factorization-obligation}, and the tail-window Real-seal certificate, \autoref{thm:monotone-cauchy-tail-window-real-seal-certificate}, force the located interval and monotone tail-window rows to be consumed before the Real seal, with transport, continuation, provenance, and local naming retained.

Finally combine the two projections through the CauchyModulusRefinement shared meet. Since both projected consumers reach the same $E,H,C,P,N$ only after the same selector-budget, selected-window, and dyadic-tolerance rows, terminal pullback uniqueness leaves one Real-seal surface and no off-route coordinate from which a separate uniform or monotone terminal handoff can be read.
\end{proof}

\begin{theorem}[Cauchy modulus refinement CompleteMetric consumer route]
\label{thm:cauchy-modulus-refinement-complete-metric-consumer-route}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted
$\CauchyModulusRefinementUp$ packet. Every $\CompleteMetricUp$ consumer of $R$
must read the selector-budget normal form
$$
U,V,T,W,Q,E,H,C,P,N
$$
before any completion-facing row is exposed. In this route $W,Q$ pass through
the displayed $\StreamNameUp$ and $\DyadicRatCoreUp$ surfaces, the readback
passes through $\RegSeqRatUp$, and the terminal seal passes through $\RealUp$.
No quotient stream, selected host limit, ambient completeness coordinate,
hidden selector, or second meet row can distinguish a $\CompleteMetricUp$
consumer after those rows have been read.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_complete\_metric\_consumer\_route}
\begin{proof}
First apply selector-budget normal form,
\autoref{thm:cauchy-modulus-refinement-selector-budget-normal-form}. It places
every shared $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and
$\RealUp$ consumer on the same finite route $U,V,T,W,Q,E,H,C,P,N$.

Next use terminal pullback consumer uniqueness,
\autoref{thm:cauchy-modulus-refinement-terminal-pullback-consumer-uniqueness}.
A completion-facing consumer that exposes those rows has the same terminal
$\RealUp$ seal surface as any other consumer exposing them.

Finally combine the Real and completion non-escape clauses supplied by the root
consumer surface and the uniform monotone terminal route. The packet contains
no quotient stream, selected host limit, ambient completeness coordinate,
hidden selector, or second meet row, so a $\CompleteMetricUp$ consumer can only
see the displayed completion-facing route after the $\RealUp$ seal.
\end{proof}

\begin{corollary}[Cauchy modulus refinement common-window select strict obstruction]
\label{cor:cauchy-modulus-refinement-common-window-select-strict-obstruction}
Let $R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)$ be an accepted $\CauchyModulusRefinementUp$ packet. Suppose a displayed failure, refusal, separation, or countermodel row is read at the common-window select surface after the consumer has exposed the selector-budget row $T$, selected $\StreamNameUp$ window $W$, $\DyadicRatCoreUp$ tolerance row $Q$, $\RegSeqRatUp$ readback, $\RealUp$ seal $E$, structural $\BHist$ route, componentwise $\hsame$ row $H$, displayed $\Cont$ row $C$, $\Pkg$ provenance row $P$, and local $\NameCert_{\CauchyModulusRefinementUp}$ row $N$.

Then that displayed obstruction is strict and local: it blocks only the attempted converse or reuse route that tries to distinguish the same Real-completion consumer surface by a countable-choice row, quotient stream, host real equality, ambient completeness coordinate, alternate selector source, hidden selector, second meet row, selected host limit, or detached readback while the displayed terminal rows agree. It cannot be read as a global impossibility for $\CauchyModulusRefinementUp$, as a host-side completeness theorem, or as hidden external validation of any off-packet Real object.
\end{corollary}
\begin{proof}
First place the consumer on the terminal spine. Terminal-spine choice refusal, \autoref{thm:cauchy-modulus-refinement-terminal-spine-choice-refusal}, says that once the consumer agrees on $T,W,Q$, the $\RegSeqRatUp$ readback, $E$, the structural $\BHist$ route, $H,C,P$, and the local naming row $N$, no countable-choice row, quotient stream, host real equality, ambient completeness coordinate, or alternate selector source can distinguish the Real-completion consumer surface determined by that terminal read.

Next stabilize the readback that the common-window select surface exposes. Root readback stability, \autoref{thm:cauchy-modulus-refinement-root-readback-stability}, keeps the selected-window and tolerance readbacks $W,Q$ on the displayed $H,C,P,N$ route for every $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, or $\RealUp$ consumer. Thus the obstruction has no independent readback coordinate from which to range over a detached stream representative or an ambient completion object.

Now identify the exact route being blocked. Common-window selector extraction, \autoref{thm:cauchy-modulus-refinement-common-window-selector-extraction}, and root terminal selector readback, \autoref{thm:cauchy-modulus-refinement-root-terminal-selector-readback}, make the selected-window route and terminal selector surface coincide with the finite packet rows already displayed. Therefore a failure, refusal, separation, or countermodel row at this point can only reject the converse or reuse attempt that asks for another consumer surface while preserving those rows.

Finally use Real-consumer coverage and root Real seal exhaustion, \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage} and \autoref{thm:cauchy-modulus-refinement-root-real-seal-exhaustion}. They exhaust Real and Completion consumers by
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Since the accepted packet exports no off-route host theorem, external validation row, hidden selector, second meet, quotient stream, selected host limit, or ambient completeness coordinate, the displayed obstruction has only the local scope stated above.
\end{proof}
