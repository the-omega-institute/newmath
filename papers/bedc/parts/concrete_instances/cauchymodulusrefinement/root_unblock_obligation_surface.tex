\begin{theorem}[Cauchy modulus refinement root source binding]
\label{thm:cauchy-modulus-refinement-root-source-binding}
Every root consumer of an accepted $\CauchyModulusRefinementUp$ packet first reads the source rows $M_0,M_1$, the shared meet row $U$, and the extraction row $V$ through the carrier route of \autoref{def:cauchy-modulus-refinement-carrier}. The root consumer has no admissible access to a downstream selector, seal, or structural row before that shared meet and extraction route is displayed.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_source\_binding}
\begin{proof}
Read the carrier definition. Its packet orders the two source schedules, the shared meet, and the extraction witness as the visible front of the root route.

Apply \autoref{thm:cauchy-modulus-refinement-shared-meet-dependency}. The theorem binds any extraction read of $V$ to the single displayed meet $U$ after $M_0,M_1$; hence a root consumer cannot substitute a second meet or a hidden source.

Finally use \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}. Since every Real or Completion consumer is covered by the displayed packet route, every such consumer inherits this first read of $M_0,M_1,U,V$ before any later row is available.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root selector readback]
\label{thm:cauchy-modulus-refinement-root-selector-readback}
For every accepted $\CauchyModulusRefinementUp$ packet, selected-window and tolerance readback consume $T,W,Q$ only after the source-meet-extraction row $M_0,M_1,U,V$ has been displayed. Thus any root consumer that asks for the selected-window ledger or the tolerance-readback ledger reaches those rows through the same selector-budget route $T$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_selector\_readback}
\begin{proof}
Start from \autoref{def:cauchy-modulus-refinement-carrier}. The selected-window ledger $W$ and tolerance-readback ledger $Q$ are carrier rows following $M_0,M_1,U,V,T$.

Apply \autoref{thm:cauchy-modulus-refinement-shared-meet-dependency} to fix the source side. The extraction row $V$ is already bound to the shared meet route before any selector-budget read is meaningful.

Now apply \autoref{thm:cauchy-modulus-refinement-selector-budget-dependency}. It identifies $T$ as the selector-budget row consumed by the selected-window and tolerance-readback rows, so $W,Q$ are read only after the extraction row and through $T$.
\end{proof}

\begin{theorem}[Cauchy modulus refinement paper-Lean readback sync]
\label{thm:cauchy-modulus-refinement-paper-lean-readback-sync}
For every accepted $\CauchyModulusRefinementUp$ packet whose root source-meet-extraction route
$$
M_0,M_1,U,V
$$
is displayed, the selector-budget row $T$, selected-window row $W$, tolerance-readback row $Q$, seal row $E$, and structural rows $H,C,P,N$ are preserved along that same carrier route. The readback exposes no second meet, hidden selector, quotient stream, or ambient completeness coordinate as an alternate source of those rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_paper\_lean\_readback\_sync}
\leanvariant{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinement\_root\_readback\_stability}
\begin{proof}
Apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. It fixes the root route through $M_0,M_1,U,V$ before any selector, seal, or structural row can be read.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The selected-window and tolerance-readback rows reach $W,Q$ only through the selector-budget row $T$, so their readback remains on the same displayed carrier route.

Finally apply \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape}. The Real seal and structural rows $E,H,C,P,N$ stay behind the same displayed route, while the excluded quotient stream, hidden selector, second meet, and ambient completeness coordinate have no exported entry point.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root Real seal non-escape]
\label{thm:cauchy-modulus-refinement-root-real-seal-nonescape}
Every $\RealUp$ or Completion consumer of an accepted $\CauchyModulusRefinementUp$ packet reaches the seal row $E$ only after the displayed route
$$
M_0,M_1,U,V,T,W,Q,H,C,P,N.
$$
No quotient stream, selected limit, host real equality, hidden selector, second meet, or ambient completeness row is exported as an alternate route to the Real seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-refinement-carrier}. The packet contains the source, meet, extraction, selector-budget, selected-window, tolerance-readback, Real seal, transport, continuation, package, and local naming rows as finite displayed coordinates.

Apply \autoref{thm:cauchy-modulus-refinement-selector-budget-dependency} and \autoref{thm:cauchy-modulus-refinement-real-seal-route}. Together they order the selector readback $T,W,Q$ before the Real seal $E$, with the structural rows $H,C,P,N$ stabilizing the same route.

Finally apply \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}. Every Real or Completion consumer is exhausted by the displayed route, so the excluded quotient stream, selected limit, host real equality, hidden selector, second meet, and ambient completeness rows have no exported entry point.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_real\_seal\_nonescape}

\begin{theorem}[Cauchy modulus refinement root consumer package]
\label{thm:cauchy-modulus-refinement-root-consumer-package}
Every root consumer of an accepted $\CauchyModulusRefinementUp$ packet factors through
$$
M_0,M_1,U,V,T,W,Q,H,C,P,N
$$
before reading any $\RealUp$ or Completion seal row. The package exports no quotient stream equality, selected limit, host real equality, hidden selector, second meet, or ambient completeness coordinate as an alternate consumer route.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. This binds every root consumer to the source rows $M_0,M_1$, the shared meet row $U$, and the extraction row $V$ before any downstream read is available.

Next apply \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The selected-window ledger and tolerance-readback ledger are consumed only through the selector-budget row $T$, so the consumer reaches $T,W,Q$ after the source-meet-extraction route has been displayed.

Finally apply \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape}. The Real or Completion seal row is reached only after the displayed structural route $H,C,P,N$ has stabilized the same packet, and the excluded quotient stream equality, selected limit, host real equality, hidden selector, second meet, and ambient completeness coordinates have no exported entry point.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_consumer\_package}

\begin{theorem}[Cauchy modulus refinement root route completeness]
\label{thm:cauchy-modulus-refinement-root-route-completeness}
For every accepted $\CauchyModulusRefinementUp$ packet, every $\RealUp$ or Completion-facing root read is complete exactly at the displayed route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
The source rows, shared meet, extraction row, selector-budget row, selected-window ledger, tolerance-readback ledger, Real seal row, and structural rows jointly exhaust the public consumer surface. No quotient stream equality, selected limit, host real equality, hidden selector, second meet, or ambient completeness coordinate is exported as another root route.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. It fixes the source rows $M_0,M_1$, the shared meet $U$, and the extraction row $V$ as the first public root source.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The selected-window and tolerance-readback ledgers are available only through the selector-budget row $T$, so the route continues through $T,W,Q$ without opening a hidden selector.

Then apply \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape}. The Real seal row $E$ and the structural rows remain behind the same displayed carrier route, and the excluded quotient stream, selected limit, host real equality, second meet, and ambient completeness coordinates have no exported read.

Finally apply \autoref{thm:cauchy-modulus-refinement-root-consumer-package}. It packages the Real and Completion consumers over the same carrier coordinates. Since the packet has no public coordinate beyond $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ for these reads, carrier coordinate exhaustion gives the claimed complete root route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_route\_completeness}

\begin{theorem}[Cauchy modulus refinement shared selector determinacy]
\label{thm:cauchy-modulus-refinement-shared-selector-determinacy}
For every accepted $\CauchyModulusRefinementUp$ packet, suppose two root-facing reads expose the same source rows $M_0,M_1$, shared meet $U$, extraction row $V$, selector-budget row $T$, selected-window row $W$, tolerance-readback row $Q$, Real seal row $E$, and structural rows $H,C,P,N$. Then the two reads are the same accepted selector route up to the displayed $\hsame$ transport and $\Cont$ replay. No hidden selector, second meet, quotient stream, selected limit, host equality, or ambient completeness row can distinguish another accepted root route.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. The shared source rows force the same $M_0,M_1,U,V$ spine for both reads, so there is no separate source-meet branch on which a second selector route could start.

Next apply \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The selector-budget, selected-window, and tolerance-readback rows are read back through the same displayed selector surface $T,W,Q$, and the carrier exposes no hidden selector coordinate beyond that surface.

Use \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape}. The Real seal row and structural rows remain inside the same $E,H,C,P,N$ packet under componentwise $\hsame$ transport and displayed $\Cont$ replay, so quotient streams, selected limits, host equalities, second meets, and ambient completeness rows are not root-facing witnesses.

Finally invoke \autoref{thm:cauchy-modulus-refinement-root-route-completeness}. Since every accepted Real or Completion-facing root read is exhausted by $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, two reads with those rows displayed have no remaining public coordinate on which they can differ.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_shared\_selector\_determinacy}

\begin{theorem}[Cauchy modulus refinement selector-meet noninterference]
\label{thm:cauchy-modulus-refinement-selector-meet-noninterference}
For an accepted $\CauchyModulusRefinementUp$ packet whose root route displays
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N,
$$
any consumer request for a hidden selector, second meet, quotient stream,
selected limit, host equality, or ambient completeness row cannot alter the
accepted selector route. Every accepted read remains determined by the shared
meet $U$, extraction row $V$, selector-budget row $T$, selected-window row
$W$, tolerance-readback row $Q$, Real-seal row $E$, and structural rows
$H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_selector\_meet\_noninterference}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. The
accepted root route must begin with the source rows $M_0,M_1$, the shared meet
$U$, and the extraction row $V$, so a hidden second meet has no source position
from which to change the selector route.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The
selector-facing rows $T,W,Q$ are read only after the source-meet-extraction
route has been displayed, and their readback is tied to the same
selector-budget row $T$.

Apply \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape} and
\autoref{thm:cauchy-modulus-refinement-root-route-completeness}. They keep the
Real seal and structural rows on the displayed route while excluding quotient
streams, selected limits, host equalities, hidden selectors, second meets, and
ambient completeness rows from the exported packet.

Finally invoke \autoref{thm:cauchy-modulus-refinement-shared-selector-determinacy}.
Once the displayed rows agree up to componentwise $\hsame$ transport and
displayed $\Cont$ replay, no remaining root-facing coordinate can distinguish
another accepted selector route. The requested hidden data are therefore
noninterfering with the accepted route.
\end{proof}

\begin{theorem}[Cauchy modulus refinement completion consumer route determinacy]
\label{thm:cauchy-modulus-refinement-completion-consumer-route-determinacy}
Suppose two accepted $\CauchyModulusRefinementUp$ completion-facing reads
display the same rows
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Then every $\RealUp$ or Completion consumer reaches the same selector route up
to the displayed $\hsame$ transport and $\Cont$ replay. No hidden selector,
second meet, quotient stream, selected limit, host equality, ambient
completeness coordinate, or detached completion seal can distinguish a second
accepted completion route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_completion\_consumer\_route\_determinacy}
\begin{proof}
Apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. The shared
rows $M_0,M_1,U,V$ bind both completion-facing reads to the same source,
shared-meet, and extraction spine before any selector-facing row is available.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The
displayed rows $T,W,Q$ are read back only through the selector-budget route
$T$, so the selected-window and tolerance-readback consumers have no separate
selector route to follow.

Apply \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape} and
\autoref{thm:cauchy-modulus-refinement-root-route-completeness}. They keep the
Real seal and the structural rows $E,H,C,P,N$ on the same displayed route for
every $\RealUp$ or Completion consumer, while excluding quotient streams,
selected limits, host equalities, ambient completeness coordinates, and
detached completion seals from the exported packet.

Finally invoke
\autoref{thm:cauchy-modulus-refinement-shared-selector-determinacy} and
\autoref{thm:cauchy-modulus-refinement-selector-meet-noninterference}. Once the
displayed rows agree, componentwise $\hsame$ transport and displayed $\Cont$
replay determine the accepted selector route, and the hidden completion data
listed in the statement cannot distinguish another accepted completion route.
\end{proof}

\begin{theorem}[Cauchy modulus refinement uniform diagonal consumer pullback]
\label{thm:cauchy-modulus-refinement-uniform-diagonal-consumer-pullback}
Let an accepted $\CauchyModulusRefinementUp$ packet expose the shared route
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Any $\UniformCauchyCriterionUp$ or $\DiagonalLimitCompatibilityUp$ consumer of that packet factors through the same finite selector route
$$
U,V,T,W,Q,E,H,C,P,N
$$
before any $\RealUp$ or Completion endpoint is public. The consumer first reads the shared meet $U$, extraction row $V$, selector-budget row $T$, selected-window row $W$, tolerance-readback row $Q$, $\RealUp$ seal row $E$, componentwise $\hsame$ transport row $H$, displayed $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert$ row $N$.

Thus the UniformCauchyCriterion and DiagonalLimitCompatibility routes have one consumer pullback over the displayed CauchyModulusRefinement selector surface. No quotient stream, selected host limit, host real equality, countable-choice schedule, detached completion seal, ambient completeness coordinate, hidden selector, or second meet supplies another route to the endpoint.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_uniform\_diagonal\_consumer\_pullback}
\begin{proof}
Start with the diagonal-facing side. The diagonal-budget cofinality theorem \autoref{thm:cauchy-modulus-refinement-diagonal-budget-cofinality-route} keeps every diagonal-budget consumer on the displayed route through $U,V,T,W,Q,E,H,C,P,N$, and it excludes hidden selectors, second meets, quotient streams, host equality, selected limits, and ambient completeness from that read.

Next attach the criterion side. The CauchyCriterion selector cofinality theorem \autoref{thm:cauchy-modulus-refinement-cauchycriterion-selector-cofinality} places the CauchyCriterion and UniformCauchyCriterion consumers on the same selector route before any criterion output is exposed. The UniformCauchyCriterion diagonal-budget pullback \autoref{thm:uniform-cauchy-criterion-diagonal-budget-pullback} then identifies the uniform family read with the diagonal-budget route rather than with a second schedule or completion coordinate.

Now use the completion-facing determinacy theorem \autoref{thm:cauchy-modulus-refinement-completion-consumer-route-determinacy}. Once the rows $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ are displayed, every $\RealUp$ or Completion consumer reaches the same selector route up to the visible $\hsame$ transport and $\Cont$ replay.

The pullback is therefore the finite selector surface listed in the statement. The excluded quotient streams, selected host limits, host real equalities, countable-choice schedules, detached completion seals, ambient completeness coordinates, hidden selectors, and second meets have no displayed row from which to alter the common consumer route before the endpoint becomes public.
\end{proof}

\begin{theorem}[Cauchy modulus refinement budget naturality]
\label{thm:cauchy-modulus-refinement-budget-naturality}
Suppose two accepted $\CauchyModulusRefinementUp$ packets share the same source schedules $M_0,M_1$, meet row $U$, extraction row $V$, selector budget $T$, selected window $W$, tolerance readback $Q$, Real seal $E$, and structural rows $H,C,P,N$. Then every $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, or Completion consumer reads the same budget-refinement route up to the displayed $\hsame$ transport and $\Cont$ replay.

The route is the displayed carrier path
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
No quotient stream equality, selected limit, host equality, hidden selector, second meet, or ambient completeness coordinate can distinguish another budget-refinement read for those consumers.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_budget\_naturality}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. The shared source schedules $M_0,M_1$, meet row $U$, and extraction row $V$ force both accepted packets to begin with the same source-meet-extraction route before any selector-budget data can be read.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The selected-window and tolerance-readback rows reach $W,Q$ only through the displayed selector budget $T$. Thus $\StreamNameUp$ and $\RegSeqRatUp$ consumers read the same finite-window and tolerance route rather than a hidden selector.

Apply \autoref{thm:cauchy-modulus-refinement-root-route-completeness}. Every $\RealUp$ or Completion-facing root read is exhausted by the displayed route $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, so the Real seal and completion handoff have no detached route.

Finally combine \autoref{thm:cauchy-modulus-refinement-shared-selector-determinacy} with \autoref{thm:cauchy-modulus-refinement-selector-meet-noninterference}. Since the displayed rows agree, componentwise $\hsame$ transport and displayed $\Cont$ replay determine the accepted consumer route, while quotient stream equality, selected limits, host equality, hidden selectors, second meets, and ambient completeness remain outside the public packet.
\end{proof}

\begin{theorem}[Cauchy modulus refinement complete-metric no-choice interface]
\label{thm:cauchy-modulus-refinement-complete-metric-no-choice-interface}
For an accepted $\CauchyModulusRefinementUp$ packet, suppose a terminal read reaches two completion-facing rows through the same displayed interfaces
$$
\Cont(\mathrm{terminal},c,\mathrm{completionA})
$$
and
$$
\Cont(\mathrm{terminal},c,\mathrm{completionB}),
$$
and suppose both completion rows are packaged by the same $\Pkg$ provenance. Then the two completion reads are classified by $\hsame$.

The interface exposes only the finite terminal row, the two completion rows, and the package rows. It exports no quotient stream, selected limit, host equality, choice schedule, or ambient completeness coordinate from which a second completion read could be selected.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCompleteMetricNoChoiceInterface}
\begin{proof}
First use the accepted carrier route from \autoref{def:cauchy-modulus-refinement-carrier}. The rows $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ provide the source schedules, shared meet, extraction row, selector budget, selected window, tolerance readback, Real seal, terminal transport, displayed continuation row, package provenance, and local naming row as finite packet data.

Next apply \autoref{thm:cauchy-modulus-refinement-root-selector-readback} and \autoref{thm:cauchy-modulus-refinement-root-route-completeness}. They place the selected-window and tolerance-readback route before the terminal structural rows and exhaust every Real or Completion-facing root read at the displayed carrier path, with no hidden selector, quotient stream, selected host limit, host equality, choice schedule, or ambient completeness coordinate.

It remains to compare the two completion-facing endpoints. The Lean proof closes this step by applying $\mathrm{cont\_deterministic}$ to the two displayed continuation witnesses $\Cont(\mathrm{terminal},c,\mathrm{completionA})$ and $\Cont(\mathrm{terminal},c,\mathrm{completionB})$. Thus the endpoints are $\hsame$-classified over the visible terminal interface, and the same $\Pkg$ provenance records that both completion rows belong to the finite packet surface.
\end{proof}

\begin{theorem}[Cauchy modulus refinement RegSeqRat--StreamName--Real coherence]
\label{thm:cauchy-modulus-refinement-regseq-streamname-real-coherence}
For an accepted $\CauchyModulusRefinementUp$ packet displaying
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N,
$$
every $\RegSeqRatUp$, $\StreamNameUp$, and $\RealUp$ consumer reads the same budget-refinement route up to the displayed $\hsame$ transport and $\Cont$ replay. The shared route is the carrier path through the source schedules, shared meet, extraction row, selector budget, selected window, tolerance readback, Real seal, transport, continuation, provenance, and local naming rows.

No such consumer may split the route into a separate stream selector, detached Real seal, quotient stream equality, hidden selector, second meet, selected limit, host equality, or ambient completeness coordinate. The consumer chapters \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-real-namecert} therefore consume one displayed budget-refinement route rather than three independent routes.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. It binds the source schedules $M_0,M_1$, shared meet $U$, and extraction row $V$ as the common root of every accepted budget-refinement read.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The $\RegSeqRatUp$ and $\StreamNameUp$ reads of selected windows and tolerance data pass through the same selector budget $T$ and the displayed rows $W,Q$, with no separate stream selector exposed.

Apply \autoref{thm:cauchy-modulus-refinement-root-route-completeness}. It places the $\RealUp$ seal row $E$ and structural rows $H,C,P,N$ on the same packet route and excludes quotient streams, selected limits, host equalities, hidden selectors, second meets, and ambient completeness rows from the public surface.

Finally combine \autoref{thm:cauchy-modulus-refinement-shared-selector-determinacy} with \autoref{thm:cauchy-modulus-refinement-budget-naturality}. The displayed rows determine one accepted budget-refinement route up to $\hsame$ transport and $\Cont$ replay, and the three named consumer chapters read that same route through their local NameCert surfaces.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_regseq\_streamname\_real\_coherence}

\begin{theorem}[Cauchy modulus refinement RegSeqRat--StreamName--Real terminal factorization]
\label{thm:cauchy-modulus-refinement-regseq-streamname-real-terminal-factorization}
For every accepted $\CauchyModulusRefinementUp$ packet whose displayed route is
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N,
$$
all $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, and Completion consumers factor through the same terminal finite route. The factorization is the carrier path through the source schedules, shared meet, extraction row, selector budget, selected-window row, tolerance-readback row, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. No detached completion seal, hidden selector, second meet, quotient stream, selected limit, host equality, or ambient completeness coordinate can supply a separate consumer route.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. It fixes $M_0,M_1,U,V$ as the shared source-meet-extraction spine for every accepted root read before any selector or seal row is available.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The selected-window and tolerance-readback rows $W,Q$ are read only through the displayed selector-budget row $T$, so $\RegSeqRatUp$ and $\StreamNameUp$ consumers cannot detach a hidden selector from the source spine.

Apply \autoref{thm:cauchy-modulus-refinement-root-route-completeness}. Every $\RealUp$ or Completion-facing root read is exhausted by $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, with no additional quotient, selected-limit, host-equality, or ambient-completeness coordinate.

Finally combine \autoref{thm:cauchy-modulus-refinement-shared-selector-determinacy}, \autoref{thm:cauchy-modulus-refinement-selector-meet-noninterference}, and \autoref{thm:cauchy-modulus-refinement-regseq-streamname-real-coherence}. Determinacy identifies any two reads with the displayed rows, noninterference rules out hidden selector and second-meet requests, and coherence places the RegSeqRat, StreamName, and Real consumer surfaces on the same route. Completion consumers are already covered by route completeness, so all four consumer classes factor through the terminal finite packet.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_regseq\_streamname\_real\_terminal\_factorization}

\begin{theorem}[Cauchy modulus refinement diagonal-budget cofinality route]
\label{thm:cauchy-modulus-refinement-diagonal-budget-cofinality-route}
Let an accepted $\CauchyModulusRefinementUp$ route display
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Then the same route remains cofinal when it is read by a diagonal-budget
consumer. The diagonal-budget consumer can read only the shared meet row $U$,
the extraction row $V$, the selector-budget row $T$, the selected-window row
$W$, the tolerance-readback row $Q$, the $\RealUp$ seal row $E$, and the
structural rows $H,C,P,N$ already exposed by the root route. It cannot
introduce a hidden selector, second meet, quotient stream, host equality,
selected limit, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. The
source rows $M_0,M_1$ force every accepted read to pass through the same shared
meet $U$ and extraction row $V$ before any selector-facing budget can be read.

Next use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The
diagonal-budget consumer reaches the selected-window and tolerance-readback
rows only through the displayed selector-budget row $T$, so $W$ and $Q$ cannot
come from a hidden selector.

Apply \autoref{thm:cauchy-modulus-refinement-root-route-completeness}. It
exhausts every $\RealUp$ or Completion-facing root read at
$M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, with no quotient stream, selected limit, host
equality, second meet, hidden selector, or ambient completeness coordinate.

Finally invoke
\autoref{thm:cauchy-modulus-refinement-completion-consumer-route-determinacy}.
Since completion consumers are determined by the same displayed route up to
the visible $\hsame$ transport and $\Cont$ replay, a diagonal-budget read has
no separate coordinate from which to select a different cofinal route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_diagonal\_budget\_cofinality\_route}

\begin{theorem}[Cauchy modulus refinement selector nonempty route]
\label{thm:cauchy-modulus-refinement-selector-nonempty-route}
Let an accepted $\CauchyModulusRefinementUp$ route display
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Any $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, or Completion consumer that reads selected-window or tolerance data must first expose the selector-budget row $T$ as a nonempty route following the shared meet $U$ and extraction row $V$. No hidden selector, second meet, quotient stream, selected limit, host equality, or ambient completeness coordinate can supply the selected-window row $W$, tolerance-readback row $Q$, or Real seal row $E$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_selector\_nonempty\_route}
\begin{proof}
Start with \autoref{thm:cauchy-modulus-refinement-root-source-binding}. It binds the source rows $M_0,M_1$ to the shared meet $U$ and extraction row $V$ before any selected-window or tolerance read can be accepted.

Next apply \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The selected-window and tolerance-readback rows $W,Q$ are available only through the displayed selector-budget row $T$, so a consumer that reads either row has already exposed a nonempty selector-budget route.

Use \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape}. The Real seal and structural rows $E,H,C,P,N$ remain behind the same carrier route, and the excluded quotient stream, selected limit, host equality, hidden selector, second meet, and ambient completeness coordinate have no exported entry point.

Finally apply \autoref{thm:cauchy-modulus-refinement-diagonal-budget-cofinality-route}. Completion-facing consumers that read the diagonal budget stay cofinal on the displayed route through $U,V,T,W,Q,E,H,C,P,N$, so the selector-budget route cannot be replaced by a detached completion or ambient-completeness coordinate.
\end{proof}

\begin{theorem}[Cauchy modulus refinement CauchyCriterion selector cofinality]
\label{thm:cauchy-modulus-refinement-cauchycriterion-selector-cofinality}
Let an accepted $\CauchyModulusRefinementUp$ route display
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Let a $\CauchyCriterionUp$ consumer, read through \autoref{ch:concrete-instances-cauchycriterion-namecert}, share the same $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows with this displayed route. Then the consumer must read the selector route through the shared meet $U$, extraction row $V$, selector-budget row $T$, selected-window row $W$, tolerance-readback row $Q$, and $\RealUp$ seal row $E$, with the support rows $H,C,P,N$, before exposing any CauchyCriterion output.

Equivalently, the CauchyCriterion selector surface is cofinal on the displayed CauchyModulusRefinement budget route. It has no detached selector, second meet, quotient stream, selected limit, host equality, ambient completeness coordinate, or separate $\RealUp$ seal from which to expose a criterion output.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_cauchycriterion\_selector\_cofinality}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-regseq-streamname-real-coherence}. The shared $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows already force the consumer to read one budget-refinement route through the displayed source schedules, shared meet, extraction row, selector budget, selected window, tolerance readback, seal row, and support rows.

Next use \autoref{thm:cauchy-modulus-refinement-diagonal-budget-cofinality-route}. It says that any diagonal-budget or completion-facing read remains cofinal on $U,V,T,W,Q,E,H,C,P,N$, so the CauchyCriterion consumer cannot obtain its selector data from a second meet, hidden selector, quotient stream, selected limit, host equality, or ambient completion coordinate.

Apply \autoref{thm:cauchy-modulus-refinement-selector-nonempty-route}. Since the consumer asks for selector-facing CauchyCriterion output, the selector-budget row $T$ must already be exposed as a nonempty route after $U$ and $V$, and the selected-window and tolerance-readback rows $W,Q$ must be read through that route.

Finally invoke the sibling compatibility theorem \autoref{thm:cauchycriterion-uniform-selector-seal-compatibility} from \autoref{ch:concrete-instances-cauchycriterion-namecert}. Its CauchyCriterion selector surface factors through the same finite StreamName--RegSeqRat--DyadicRatCore--Real packet before any $\RealUp$ seal is exposed, and therefore the shared CauchyModulusRefinement route is the required cofinal selector route for the criterion output.
\end{proof}

\begin{theorem}[Cauchy modulus refinement criterion-output boundary]
\label{thm:cauchy-modulus-refinement-criterion-output-boundary}
Let an accepted $\CauchyModulusRefinementUp$ route display
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Let a $\CauchyCriterionUp$ consumer and a $\UniformCauchyCriterionUp$ consumer share the same $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows with the displayed route. Then both consumers expose the same finite criterion-output boundary through
$$
U,V,T,W,Q,E,H,C,P,N.
$$
Neither consumer can add a detached selector, second meet, quotient stream, selected limit, host equality, countable-choice schedule, ambient completeness coordinate, or separate $\RealUp$ seal.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_criterion\_output\_boundary}
\begin{proof}
For the $\CauchyCriterionUp$ consumer, apply \autoref{thm:cauchy-modulus-refinement-cauchycriterion-selector-cofinality}. The criterion output must be read through the shared meet $U$, extraction row $V$, selector-budget row $T$, selected-window row $W$, tolerance-readback row $Q$, $\RealUp$ seal row $E$, and support rows $H,C,P,N$.

For the $\UniformCauchyCriterionUp$ consumer, apply \autoref{thm:uniform-cauchy-criterion-diagonal-budget-pullback}. The uniform diagonal-budget read identifies its compatible window, tolerance, readback, and seal coordinates with the same finite route rather than with a second schedule, quotient stream, selected limit, or completion coordinate.

Now use \autoref{thm:cauchy-modulus-refinement-shared-selector-determinacy}. Since the two consumers share the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows, componentwise $\hsame$ transport and displayed $\Cont$ replay identify their criterion-output route with the accepted CauchyModulusRefinement selector route.

Finally apply \autoref{thm:cauchy-modulus-refinement-selector-meet-noninterference}. The shared meet and selector rows already determine the public boundary $U,V,T,W,Q,E,H,C,P,N$, so an off-route selector, second meet, quotient stream, selected limit, host equality, countable-choice schedule, ambient completeness coordinate, or separate $\RealUp$ seal cannot be introduced by either consumer.
\end{proof}

\begin{theorem}[Cauchy modulus refinement RegSeqRat--Real selector exactness]
\label{thm:cauchy-modulus-refinement-regseq-real-selector-exactness}
For an accepted $\CauchyModulusRefinementUp$ route displaying
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N,
$$
the $\RegSeqRatUp$ tail read, the $\StreamNameUp$ selected-window read, and the $\RealUp$ seal read are three projections of the same selector route through $U,V,T,W,Q,E$. No hidden selector, second meet, quotient stream, host equality, detached seal, or ambient completeness coordinate can supply one projection without the other two.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding} and \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. They bind the accepted route to the source rows $M_0,M_1$, the shared meet $U$, the extraction row $V$, and the selector-budget row $T$ before either the selected-window row $W$ or the tolerance-readback row $Q$ can be inspected.

Next use \autoref{thm:cauchy-modulus-refinement-regseq-streamname-real-coherence}. The $\RegSeqRatUp$, $\StreamNameUp$, and $\RealUp$ consumers read one budget-refinement route through $U,V,T,W,Q,E,H,C,P,N$ rather than three independent projections.

Apply \autoref{thm:cauchy-modulus-refinement-diagonal-budget-cofinality-route} and \autoref{thm:cauchy-modulus-refinement-selector-nonempty-route}. These theorems keep diagonal-budget and selector-facing reads cofinal on the same displayed selector route and make $T$ a nonempty entry before $W,Q,E$ can be consumed.

Finally invoke \autoref{thm:cauchy-modulus-refinement-cauchycriterion-selector-cofinality}. The CauchyCriterion consumer sees the same StreamName--RegSeqRat--DyadicRatCore--Real packet before exposing its output. Hence none of the excluded coordinates can provide just one of the RegSeqRat, StreamName, or Real projections while omitting the other two.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_regseq\_real\_selector\_exactness}

\begin{theorem}[Cauchy modulus refinement RegSeqRat--Real selector lockstep]
\label{thm:cauchy-modulus-refinement-regseq-real-selector-lockstep}
For an accepted $\CauchyModulusRefinementUp$ route displaying
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N,
$$
the $\RegSeqRatUp$ tail projection, the $\StreamNameUp$ selected-window projection, and the $\RealUp$ seal projection are lockstep projections of the same selector route through $U,V,T,W,Q,E$. No hidden selector, second meet, quotient stream, detached seal, selected limit, host equality, or ambient completeness coordinate can alter one projection without altering the other two.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchy-modulus-refinement-root-source-binding} and \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. They bind the displayed route to the source rows $M_0,M_1$, the shared meet $U$, the extraction row $V$, and the selector-budget row $T$ before any of the three consumer projections can be read.

Next use \autoref{thm:cauchy-modulus-refinement-regseq-streamname-real-coherence}. It places the $\RegSeqRatUp$, $\StreamNameUp$, and $\RealUp$ consumer surfaces on one budget-refinement route rather than on separate tail, stream, and seal routes.

Apply \autoref{thm:cauchy-modulus-refinement-selector-nonempty-route}. The selected-window and tolerance-facing reads must expose the nonempty selector-budget route through $T,W,Q$ before a projected tail or seal can be consumed.

Finally invoke \autoref{thm:cauchy-modulus-refinement-regseq-real-selector-exactness}. Since the RegSeqRat tail read, StreamName selected-window read, and Real seal read are already exact projections of the same selector route through $U,V,T,W,Q,E$, changing one projection would change the shared route and therefore the other two projections as well.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_regseq\_real\_selector\_lockstep}

\begin{theorem}[Cauchy modulus refinement finite observation budget pullback]
\label{thm:cauchy-modulus-refinement-finite-observation-budget-pullback}
Let an accepted $\CauchyModulusRefinementUp$ route display
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Let this selector route be consumed by a $\UniformCauchyCriterionUp$ packet as in \autoref{ch:concrete-instances-uniformcauchycriterion-namecert}, and let
$$
F=(B,S,W_F,D_F,R_F,E_F,H_F,C_F,P_F,N_F)
$$
be an accepted $\FiniteObservationBudgetSelectorUp$ packet as in \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}. Suppose the shared $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ faces of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert} identify the CauchyModulusRefinement selected-window, tolerance-readback, regular handoff, and Real seal rows with the finite observation budget rows by displayed componentwise $\hsame$ transports
$$
W \hsame W_F,\qquad
Q \hsame D_F,\qquad
T \hsame R_F,\qquad
E \hsame E_F.
$$
Then every Real-facing read of the common route factors through the CauchyModulusRefinement rows
$$
U,V,T,W,Q,E,H,C,P,N
$$
together with the finite observation budget rows
$$
B,S,W_F,D_F,R_F,E_F,H_F,C_F,P_F,N_F
$$
before any $\RealUp$ seal is exposed. Thus finite observation budgets are the readable selector surface for this Real-completion route: the pullback introduces no quotient stream, selected limit, host equality, countable choice, hidden observation oracle, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_finite\_observation\_budget\_pullback}
\begin{proof}
Start with the CauchyModulusRefinement side. By \autoref{thm:cauchy-modulus-refinement-cauchycriterion-selector-cofinality}, a CauchyCriterion or UniformCauchyCriterion consumer of the route must read the selector through the shared meet $U$, extraction row $V$, selector-budget row $T$, selected-window row $W$, tolerance-readback row $Q$, Real seal row $E$, and support rows $H,C,P,N$ before exposing criterion output.

Next apply \autoref{thm:cauchy-modulus-refinement-regseq-real-selector-exactness} and \autoref{thm:cauchy-modulus-refinement-regseq-real-selector-lockstep}. They make the $\RegSeqRatUp$ tail projection, $\StreamNameUp$ selected-window projection, and $\RealUp$ seal projection exact lockstep projections of the same selector route, so no Real-facing read can detach the seal from $U,V,T,W,Q,E$.

Read the finite observation packet through its public route. The public export theorem \autoref{thm:finite-observation-budget-selector-public-export} exposes the finite budget-to-seal route $B \to S \to W_F \to D_F \to R_F \to E_F$ together with $H_F,C_F,P_F,N_F$, and the UniformCauchyCriterion pullback \autoref{thm:finite-observation-budget-selector-uniform-cauchycriterion-pullback} identifies that route with the uniform criterion consumer on the displayed shared rows.

Finally use terminal route no-branching and endpoint exactness, \autoref{thm:finite-observation-budget-selector-terminal-route-no-branching} and \autoref{thm:finite-observation-budget-selector-terminal-route-endpoint-exactness}. The displayed transports identify $W,Q,T,E$ with $W_F,D_F,R_F,E_F$, so the common consumer has only the finite pullback surface listed in the statement before the Real seal is reached. The non-escape clauses in the cited theorems exclude quotient streams, selected limits, host equality, countable choice, hidden observation oracles, and ambient completeness coordinates from the pullback.
\end{proof}

\begin{theorem}[Cauchy modulus refinement terminal budget route exhaustion]
\label{thm:cauchy-modulus-refinement-terminal-budget-route-exhaustion}
Let an accepted $\CauchyModulusRefinementUp$ route display
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Let this route be consumed by $\RealUp$ or Completion consumers through the $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyCriterionUp$, and $\UniformCauchyCriterionUp$ faces named in the surrounding chapters. Suppose an accepted $\FiniteObservationBudgetSelectorUp$ packet
$$
F=(B,S,W_F,D_F,R_F,E_F,H_F,C_F,P_F,N_F)
$$
is identified with the common route by the displayed componentwise transports of \autoref{thm:cauchy-modulus-refinement-finite-observation-budget-pullback}. Then every Real-facing or Completion-facing consumer of the common route factors through the combined finite surface
$$
U,V,T,W,Q,E,H,C,P,N
$$
and
$$
B,S,W_F,D_F,R_F,E_F,H_F,C_F,P_F,N_F.
$$
No quotient stream, selected host limit, host equality, countable choice, hidden observation oracle, detached seal, or ambient completeness coordinate supplies another terminal route for those consumers.
\end{theorem}
\begin{proof}
First use \autoref{thm:cauchy-modulus-refinement-regseq-streamname-real-terminal-factorization}. It makes the $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, and Completion reads factor through the displayed CauchyModulusRefinement route $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$.

Next apply \autoref{thm:cauchy-modulus-refinement-diagonal-budget-cofinality-route} and \autoref{thm:cauchy-modulus-refinement-cauchycriterion-selector-cofinality}. Diagonal-budget, CauchyCriterion, and UniformCauchyCriterion consumers remain cofinal on the same selector route before any Real or Completion endpoint is exposed.

Now use \autoref{thm:cauchy-modulus-refinement-regseq-real-selector-exactness} together with \autoref{thm:cauchy-modulus-refinement-regseq-real-selector-lockstep}. The $\RegSeqRatUp$ tail read, $\StreamNameUp$ selected-window read, and $\RealUp$ seal read are exact lockstep projections of $U,V,T,W,Q,E$, so a consumer cannot detach a host limit or seal coordinate from the common route.

Finally invoke \autoref{thm:cauchy-modulus-refinement-finite-observation-budget-pullback}, \autoref{thm:finite-observation-budget-selector-terminal-route-no-branching}, and \autoref{thm:finite-observation-budget-selector-terminal-route-endpoint-exactness}. The finite observation rows $B,S,W_F,D_F,R_F,E_F,H_F,C_F,P_F,N_F$ are the pullback surface for the shared selector route, and terminal no-branching plus endpoint exactness excludes quotient streams, selected host limits, host equality, countable choice, hidden observation oracles, detached seals, and ambient completeness coordinates as alternate terminal routes.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_terminal\_budget\_route\_exhaustion}

\begin{theorem}[Cauchy modulus refinement current phase spine]
\label{thm:cauchy-modulus-refinement-current-phase-spine}
Every accepted $\CauchyModulusRefinementUp$ consumer for the current $\RealUp$ and Completion route factors through four displayed faces: the $\DyadicRatCoreUp$ tolerance ledger, the $\StreamNameUp$ finite-window row, the $\RegSeqRatUp$ readback row, and the $\RealUp$ seal. These faces are read from the same finite $\BHist$ route through displayed $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows, and the route admits no quotient stream, selected limit, host real equality, choice data, hidden selector, second meet row, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.RootUnblockObligationSurface.CauchyModulusRefinementCarrier\_current\_phase\_spine}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_current\_phase\_spine}
\begin{proof}
Begin with root source binding and root selector readback. The accepted route displays $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, so the current consumer must read source, common meet, extraction, selector budget, selected window, tolerance readback, and seal before any endpoint is exposed.

Use route completeness and the RegSeqRat, StreamName, and Real coherence theorems in the surrounding surface. They identify the tolerance ledger, finite-window row, regular-sequence readback, and Real seal as projections of the same route rather than independent consumers.

Apply the diagonal budget cofinality route, the finite observation budget pullback, and terminal budget route exhaustion. Together they force every Real-facing or Completion-facing read through the four displayed faces and the support rows $H,C,P,N$.

The refused coordinates are exactly those excluded by the terminal route. Quotient streams, selected limits, host real equality, choice data, hidden selectors, second meet rows, and ambient completeness would each supply an endpoint outside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route, so none is accepted.
\end{proof}

\begin{theorem}[Cauchy modulus refinement open phase spine lock]
\label{thm:cauchy-modulus-refinement-open-phase-spine-lock}
For the current $\CauchyModulusRefinementUp$ route, any open $\RealUp$ or Completion consumer that names a refinement endpoint while leaving one of the four faces absent is held at the spine boundary. The required faces are the $\DyadicRatCoreUp$ tolerance ledger, $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ readback row, and $\RealUp$ seal, supported by $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Read the route through the current-phase spine of \autoref{thm:cauchy-modulus-refinement-current-phase-spine}. It supplies the four public faces and the support rows as one finite route.

If the tolerance ledger is absent, the consumer has no dyadic budget surface. If the finite-window row is absent, it has no StreamName observation boundary. If the RegSeqRat readback is absent, the regular tail cannot be read back from the selector. If the Real seal is absent, no Real or Completion endpoint is exposed.

The root source binding, root selector readback, and route completeness steps do not add a substitute face. They only route the accepted rows to the endpoint.

Thus an open consumer missing any displayed face remains at the spine boundary. The same refusal excludes quotient streams, selected limits, host real equality, choice data, hidden selectors, second meet rows, and ambient completeness from serving as replacements.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_open\_phase\_spine\_lock}

\begin{theorem}[Cauchy modulus refinement four-face consumer boundary]
\label{thm:cauchy-modulus-refinement-four-face-consumer-boundary}
The consumer boundary for the current $\CauchyModulusRefinementUp$ handoff to $\RealUp$ and Completion is exactly the four-face route consisting of the $\DyadicRatCoreUp$ tolerance ledger, $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ readback row, and $\RealUp$ seal. Any accepted downstream consumer must preserve those faces by displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before reading a Real or Completion endpoint.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_four\_face\_consumer\_boundary}
\begin{proof}
First compose root source binding with root selector readback. This places the refinement source, selector, budget, window, tolerance readback, and seal in one route.

Next use route completeness together with RegSeqRat, StreamName, and Real coherence. The three coherence faces and the dyadic tolerance ledger are therefore read as the same current route, not as four unrelated records.

Now apply diagonal budget cofinality, finite observation budget pullback, and terminal budget route exhaustion. These theorems move every downstream consumer through the finite budget surface before it reaches the endpoint, so the boundary is the four displayed faces plus $H,C,P,N$.

It remains to rule out extra terminal data. Quotient streams, selected limits, host real equality, choice data, hidden selectors, second meet rows, and ambient completeness would bypass at least one displayed face. Since the accepted carrier has no such coordinate, the consumer boundary is exactly the four-face route.
\end{proof}

\begin{corollary}[Cauchy modulus refinement root source binding strict obstruction]
\label{cor:cauchy-modulus-refinement-root-source-binding-strict-obstruction}
Let a displayed failure, refusal, separation, or countermodel row in the root-unblock surface challenge an accepted $\CauchyModulusRefinementUp$ packet by asking a root consumer either to read a selector, seal, or structural row before the source route
$$
M_0,M_1,U,V
$$
is displayed, or to reach the $\RealUp$ or Completion seal through a quotient stream, selected limit, host real equality, hidden selector, second meet, or ambient completeness coordinate outside
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
Then the obstruction is strict and local. It blocks only the named converse or reuse route that makes that challenged read. It does not assert a global impossibility for $\CauchyModulusRefinementUp$ packets, does not become a host theorem about $\RealUp$ or Completion, and does not provide hidden external validation of the seal.
\end{corollary}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_source\_binding\_strict\_obstruction}
\begin{proof}
First use root source binding, \autoref{thm:cauchy-modulus-refinement-root-source-binding}. Every root consumer of an accepted packet must first read $M_0,M_1$, the shared meet $U$, and the extraction row $V$ through the carrier route of \autoref{def:cauchy-modulus-refinement-carrier}. Hence a displayed obstruction to an early selector, seal, or structural read says only that the challenged read has not factored through the required source-meet-extraction route. It is a refusal of that local converse or reuse route, not a statement that the carrier cannot be accepted.

Now use root Real seal non-escape, \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape}. Every $\RealUp$ or Completion consumer reaches the seal row $E$ only after the displayed route
$$
M_0,M_1,U,V,T,W,Q,H,C,P,N
$$
has been displayed, and the theorem exports no quotient stream, selected limit, host real equality, hidden selector, second meet, or ambient completeness row as an alternate route to the seal. Thus an obstruction row aimed at one of those unlisted coordinates rejects precisely that coordinate as an entry point into the accepted packet.

Combining the two cited results, every consumer-facing use of the root-unblock surface factors through the named source route and then through the named seal route. Any challenged coordinate outside those rows is therefore blocked only at the point where it tries to serve as a converse route, reuse route, or seal shortcut. Since the accepted carrier remains the finite packet of \autoref{def:cauchy-modulus-refinement-carrier}, the obstruction neither proves an ambient impossibility theorem nor imports a host validation principle beyond the displayed $\CauchyModulusRefinementUp$ rows.
\end{proof}
