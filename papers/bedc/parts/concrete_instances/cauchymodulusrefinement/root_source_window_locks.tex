\begin{theorem}[Cauchy modulus refinement source-window lock]
\label{thm:cauchy-modulus-refinement-source-window-lock}
For every accepted $\CauchyModulusRefinementUp$ root read of
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
the consumer-visible source rows $M_0,M_1$, the shared meet row $U$, and the extraction row $V$ must be exposed before any selected-window row is read. Thus no root read can reach $T,W,Q,E,H,C,P,N$ as a selected-window route until the displayed source-meet-extraction front $M_0,M_1,U,V$ has been presented.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_window\_lock}
\begin{proof}
Read the carrier in \autoref{def:cauchy-modulus-refinement-carrier}. Its accepted packet presents the two source schedules, the shared meet, and the extraction witness as the finite front of the root route.

Apply \autoref{thm:cauchy-modulus-refinement-shared-meet-dependency}. The extraction row $V$ is bound to the displayed meet $U$ after $M_0,M_1$, so a downstream selected-window read cannot supply a hidden source or a second meet.

Finally apply \autoref{thm:cauchy-modulus-refinement-root-source-binding}. Every accepted root consumer is already required to read $M_0,M_1,U,V$ through the carrier route before any later selector, seal, or structural row is available. Hence the selected-window surface is locked behind the displayed source window.
\end{proof}

\begin{theorem}[Cauchy modulus refinement selector-stability lock]
\label{thm:cauchy-modulus-refinement-selector-stability-lock}
For every accepted $\CauchyModulusRefinementUp$ root read, the selector route $V \to T \to W \to Q$ is preserved by the displayed $\hsame$ and $\Cont$ rows. The selected-window and tolerance-readback ledgers cannot be replaced by a hidden selector, a second window ledger, or a detached continuation route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinement\_selector\_stability\_lock}
\begin{proof}
Start with \autoref{thm:cauchy-modulus-refinement-selector-budget-dependency}. It identifies $T$ as the selector-budget row consumed by the selected-window ledger $W$ and the tolerance-readback ledger $Q$.

Use \autoref{thm:cauchy-modulus-refinement-root-selector-readback}. The readback theorem places $T,W,Q$ after the source-meet-extraction route, so the selector is reached from $V$ and not from an independent ledger.

The displayed $\hsame$ row $H$ transports the same packet route, and the displayed $\Cont$ row $C$ continues that route without changing the selected-window ledger. Therefore the route $V \to T \to W \to Q$ remains the only exported selector path.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real-seal root handoff]
\label{thm:cauchy-modulus-refinement-real-seal-root-handoff}
Every $\RealUp$ or Completion handoff from an accepted $\CauchyModulusRefinementUp$ root read factors through
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
The handoff exports no quotient stream, selected limit, host real equality, hidden selector, second meet row, or ambient completeness coordinate as an alternate Real-seal route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_seal\_root\_handoff}
\begin{proof}
Apply \autoref{thm:cauchy-modulus-refinement-real-seal-route}. The Real seal $E$ is reached from the displayed source, meet, extraction, selector-budget, selected-window, and tolerance-readback rows.

Next apply \autoref{thm:cauchy-modulus-refinement-root-real-seal-nonescape}. It rules out an alternate route through quotient streams, selected limits, host real equality, hidden selectors, second meet rows, or ambient completeness coordinates at the root boundary.

Finally use \autoref{thm:cauchy-modulus-refinement-real-consumer-coverage}. Every Real or Completion consumer is exhausted by the displayed route, so the handoff factors through $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ and exports no additional Real-seal coordinate.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root window exhaustion]
\label{thm:cauchy-modulus-refinement-root-window-exhaustion}
Every accepted $\CauchyModulusRefinementUp$ $\RealUp$ or Completion selected-window consumer is equivalent, as a finite BEDC route, to first reading the source and meet rows $M_0,M_1,U,V$, then the selector and selected-window rows $T,W,Q,E$, and then the structural rows $H,C,P,N$. Conversely, any displayed route of this form determines the same $\RealUp$ or Completion selected-window consumer. Quotient streams, selected limits, host real equality, hidden selectors, second meet rows, and ambient completeness have no route coordinate for such a consumer.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_window\_exhaustion}
\begin{proof}
The forward direction starts with the source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It forces every accepted consumer route to expose $M_0,M_1,U,V$ before any selected-window read is available.

Next apply the selector-stability lock, \autoref{thm:cauchy-modulus-refinement-selector-stability-lock}. The displayed selector path from $V$ through $T,W,Q$ is preserved by the $\hsame$ and $\Cont$ rows, so no hidden selector or detached selected-window ledger can supply a second route.

Now use the Real-seal root handoff, \autoref{thm:cauchy-modulus-refinement-real-seal-root-handoff}. It places $E,H,C,P,N$ after the same source and selector rows for every $\RealUp$ or Completion handoff, and it excludes quotient streams, selected limits, host real equality, second meet rows, and ambient completeness as route coordinates.

For the reverse direction, read an accepted packet in exactly the displayed order. The three locks just cited identify each segment with the only source, selector, selected-window, Real-seal, and structural surface exported by the carrier. Therefore the route determines the same $\RealUp$ or Completion selected-window consumer.
\end{proof}

\begin{theorem}[Cauchy modulus refinement root-source meet determinacy]
\label{thm:cauchy-modulus-refinement-root-source-meet-determinacy}
Let two accepted $\CauchyModulusRefinementUp$ root reads agree on the source rows $M_0,M_1$, the shared meet row $U$, and the extraction row $V$ of the carrier in \autoref{def:cauchy-modulus-refinement-carrier}. Then before any selected-window row is read, the two reads determine the same source-meet front. In particular, no later selector, selected-window ledger, Real seal, transport, continuation, package, or local name can introduce a second source-meet-extraction route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_root\_source\_meet\_determinacy}
\begin{proof}
Read the carrier definition. The agreed rows $M_0,M_1,U,V$ are exactly the displayed source-meet-extraction front of the root packet.

Apply source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It places that front before every selected-window read, so neither root read can use $T,W,Q,E,H,C,P,N$ to choose a different source side.

Now use root window exhaustion, \autoref{thm:cauchy-modulus-refinement-root-window-exhaustion}. Once the front is fixed, every later selected-window or Real-completion route is a continuation of the same finite carrier route. Hence the source-meet front is determined before any selected-window consumer appears.
\end{proof}

\begin{theorem}[Cauchy modulus refinement window consumer coverage]
\label{thm:cauchy-modulus-refinement-window-consumer-coverage}
Every accepted $\CauchyModulusRefinementUp$ selected-window consumer is exhausted by the finite carrier rows
$$
M_0,M_1,U,V,T,W,Q,H,C,P,N.
$$
The consumer receives no quotient stream, selected host limit, hidden selector, second meet row, ambient completeness coordinate, detached tolerance readback, or route outside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_window\_consumer\_coverage}
\begin{proof}
Start from the source-window lock. The consumer must read $M_0,M_1,U,V$ before a selected-window route is available.

Apply selector-stability lock, \autoref{thm:cauchy-modulus-refinement-selector-stability-lock}. It forces the selected-window and tolerance-readback rows to proceed through $T,W,Q$ on the same route.

Finally use root window exhaustion. The structural rows $H,C,P,N$ preserve and name the displayed route, while quotient streams, selected host limits, hidden selectors, second meet rows, ambient completeness coordinates, and detached tolerance readbacks have no carrier coordinate. Thus the selected-window consumer is exhausted by the stated finite rows.
\end{proof}

\begin{theorem}[Cauchy modulus refinement Real-seal source conservation]
\label{thm:cauchy-modulus-refinement-real-seal-source-conservation}
Every $\RealUp$ or Completion seal read from an accepted $\CauchyModulusRefinementUp$ root packet conserves the same source-meet-extraction route $M_0,M_1,U,V$ before the Real seal row $E$ is exposed. The seal cannot replace that route by a quotient stream, selected limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or detached completion witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_seal\_source\_conservation}
\begin{proof}
Begin with source-window lock and root-source meet determinacy. They bind every root read to the displayed $M_0,M_1,U,V$ front before any selected-window or seal route can be consumed.

Apply the Real-seal root handoff, \autoref{thm:cauchy-modulus-refinement-real-seal-root-handoff}. It places the Real seal row $E$ after the same source, meet, extraction, selector-budget, selected-window, and tolerance-readback rows.

Use root window exhaustion for the final exclusion. Since every Real or Completion selected-window consumer is equivalent to the displayed carrier route, the seal has no coordinate for quotient streams, selected limits, host real equality, hidden selectors, second meet rows, ambient completeness coordinates, or detached completion witnesses. Therefore the same source-meet-extraction route is conserved before $E$ is exposed.
\end{proof}

\begin{theorem}[Cauchy modulus refinement source-window front totality]
\label{thm:cauchy-modulus-refinement-source-window-front-totality}
Every accepted $\CauchyModulusRefinementUp$ selected-window, $\RealUp$, or Completion consumer factors through the displayed source front
$$
M_0,M_1,U,V
$$
before reading any of $T,W,Q,E,H,C,P,N$. Conversely, a displayed route with that source front and the existing selector-stability, selected-window, Real-seal, transport, continuation, package, and local-name rows determines the same consumer surface.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_window\_front\_totality}
\begin{proof}
The forward direction starts with the source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It places $M_0,M_1,U,V$ before any selected-window read is available.

Apply selector-stability lock, \autoref{thm:cauchy-modulus-refinement-selector-stability-lock}. The selected-window rows $T,W,Q$ must continue from the same extraction row $V$, so a consumer cannot reach them through a hidden selector or detached window ledger.

For $\RealUp$ and Completion consumers, use the Real-seal root handoff, \autoref{thm:cauchy-modulus-refinement-real-seal-root-handoff}, together with Real-seal source conservation, \autoref{thm:cauchy-modulus-refinement-real-seal-source-conservation}. These results place $E,H,C,P,N$ after the same source-meet-extraction route and prevent the seal from replacing that route.

For the converse, read the displayed packet in the stated order. Root window exhaustion, \autoref{thm:cauchy-modulus-refinement-root-window-exhaustion}, identifies that route with the full selected-window, $\RealUp$, or Completion consumer surface, so the displayed source front and the existing selector and seal rows determine the same consumer.
\end{proof}

\begin{theorem}[Cauchy modulus refinement source-window totality]
\label{thm:cauchy-modulus-refinement-source-window-totality}
Every accepted $\CauchyModulusRefinementUp$ selected-window, $\RealUp$, or Completion consumer is determined by first exposing the source rows, shared meet, and extraction front
$$
M_0,M_1,U,V,
$$
then routing through
$$
T,W,Q,E,H,C,P,N.
$$
Conversely, any displayed route with those rows determines the same selected-window, $\RealUp$, or Completion consumer. No quotient stream, selected limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or detached completion witness supplies another consumer route.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_window\_totality}
\begin{proof}
The forward direction begins with source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It forces every accepted consumer to expose $M_0,M_1,U,V$ before a selected-window or seal row can be read.

Apply selector-stability lock, \autoref{thm:cauchy-modulus-refinement-selector-stability-lock}. The selector, selected-window, and tolerance-readback segment is exactly the displayed route from $V$ through $T,W,Q$, and the structural rows preserve that route rather than replacing it.

For $\RealUp$ and Completion consumers, use Real-seal root handoff, \autoref{thm:cauchy-modulus-refinement-real-seal-root-handoff}, together with Real-seal source conservation, \autoref{thm:cauchy-modulus-refinement-real-seal-source-conservation}. These results place $E,H,C,P,N$ after the same source and selector front and rule out quotient streams, selected limits, host real equality, hidden selectors, second meet rows, ambient completeness coordinates, and detached completion witnesses.

For the converse, read any displayed route with rows $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$. Root window exhaustion, \autoref{thm:cauchy-modulus-refinement-root-window-exhaustion}, identifies that route with the complete selected-window, $\RealUp$, or Completion surface. Hence the displayed rows determine the same consumer.
\end{proof}

\begin{theorem}[Cauchy modulus refinement source-window comparison lock]
\label{thm:cauchy-modulus-refinement-source-window-comparison-lock}
Let two accepted $\CauchyModulusRefinementUp$ root reads agree on the source rows $M_0,M_1$, the shared meet row $U$, and the extraction row $V$ of \autoref{def:cauchy-modulus-refinement-carrier}. Then every selected-window, $\RealUp$, or Completion consumer comparison between those reads agrees on the source-window front before either comparison reads any of
$$
T,W,Q,E,H,C,P,N.
$$
Thus no later selector, selected-window ledger, Real seal, transport row, continuation row, package row, or local name row can distinguish the two reads by introducing another source-window front.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_window\_comparison\_lock}
\begin{proof}
Begin with source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It places $M_0,M_1,U,V$ before every selected-window read, so a comparison cannot inspect $T,W,Q,E,H,C,P,N$ until the source front has already been exposed.

Apply root-source meet determinacy, \autoref{thm:cauchy-modulus-refinement-root-source-meet-determinacy}. Since the two root reads agree on $M_0,M_1,U,V$, the displayed source-meet-extraction route is the same for both reads before any later row can be consumed.

Finally use source-window totality, \autoref{thm:cauchy-modulus-refinement-source-window-totality}. Every selected-window, $\RealUp$, or Completion consumer is determined by first exposing the same source front and then reading the remaining displayed rows. Therefore any consumer comparison agrees on that front before it reaches the later packet rows.
\end{proof}

\begin{theorem}[Cauchy modulus refinement source-window Real-seal pullback]
\label{thm:cauchy-modulus-refinement-source-window-real-seal-pullback}
For every accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N),
$$
every $\RealUp$ or Completion seal read is the pullback of the displayed source-window front $M_0,M_1,U,V$ along the selector route $V \to T \to W \to Q \to E$ and the structural rows $H,C,P,N$. Conversely, any displayed route with those source, selector, seal, and structural rows determines the same seal consumer. The pullback has no coordinate for quotient streams, selected host limits, host real equality, hidden selectors, second meet rows, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It exposes $M_0,M_1,U,V$ before any selected-window or seal read, so the Real-seal consumer cannot choose its source side after reaching $T,W,Q,E,H,C,P,N$.

Use selector-stability lock, \autoref{thm:cauchy-modulus-refinement-selector-stability-lock}. The selected-window route proceeds from $V$ through $T,W,Q$, and the displayed $\hsame$ and $\Cont$ rows preserve that route instead of supplying a hidden selector or detached window ledger.

Now apply the Real-seal root handoff, \autoref{thm:cauchy-modulus-refinement-real-seal-root-handoff}. It places every $\RealUp$ or Completion seal read after the same source, selector, seal, transport, continuation, package, and local-name rows, ruling out quotient streams, selected host limits, host real equality, second meet rows, and ambient completeness as alternate seal coordinates.

For the converse, use source-window totality, \autoref{thm:cauchy-modulus-refinement-source-window-totality}. A route that displays $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$ already determines the selected-window, $\RealUp$, or Completion consumer surface.

Finally compare any two such reads with source-window comparison lock, \autoref{thm:cauchy-modulus-refinement-source-window-comparison-lock}. The comparison agrees on the source front before either read reaches the seal rows, so the displayed source-window front pulls back the Real-seal route and the same displayed route determines the same seal consumer.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_window\_real\_seal\_pullback}

\begin{theorem}[Cauchy modulus refinement Real consumer nonescape]
\label{thm:cauchy-modulus-refinement-real-consumer-nonescape}
Every $\RealUp$ or Completion consumer of an accepted $\CauchyModulusRefinementUp$ root packet factors through
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
The consumer has no route to quotient streams, selected host limits, host real equality, hidden selectors, second meet rows, ambient completeness, or detached completion witnesses outside that displayed BEDC packet.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_consumer\_nonescape}
\leanvariant{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_real\_seal\_consumer\_exhaustion}
\begin{proof}
Start with Real-seal root handoff, \autoref{thm:cauchy-modulus-refinement-real-seal-root-handoff}. It routes every $\RealUp$ or Completion handoff through the displayed source, meet, extraction, selector, selected-window, tolerance, seal, transport, continuation, package, and local-name rows.

Use Real-seal source conservation, \autoref{thm:cauchy-modulus-refinement-real-seal-source-conservation}. The seal preserves the same $M_0,M_1,U,V$ source-meet-extraction route and cannot replace it by a quotient stream, selected limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or detached completion witness.

Now apply root window exhaustion, \autoref{thm:cauchy-modulus-refinement-root-window-exhaustion}. It identifies every accepted $\RealUp$ or Completion selected-window consumer with the displayed finite route and rules out alternate route coordinates.

Finally apply window consumer coverage, \autoref{thm:cauchy-modulus-refinement-window-consumer-coverage}. The selected-window portion of the consumer is exhausted by the carrier rows and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface. Hence the Real or Completion consumer cannot escape the displayed finite packet.
\end{proof}

\begin{theorem}[Cauchy modulus source-window Real-consumer lock]
\label{thm:cauchy-modulus-source-window-real-consumer-lock}
Every $\RealUp$ or Completion consumer comparison of accepted $\CauchyModulusRefinementUp$ root reads first exposes the source-window rows
$$
M_0,M_1,U,V
$$
and only then routes through
$$
T,W,Q,E,H,C,P,N.
$$
Consequently the comparison cannot distinguish two accepted reads by quotient streams, selected host limits, second meet rows, hidden selectors, or detached completion witnesses outside the displayed source-window, selector, Real-seal, transport, continuation, package, and local naming rows.
\end{theorem}
\begin{proof}
First expose the source front. By source-window front totality, \autoref{thm:cauchy-modulus-refinement-source-window-front-totality}, every accepted selected-window, $\RealUp$, or Completion consumer begins with the displayed $M_0,M_1,U,V$ rows.

Now compare two accepted reads. Source-window comparison lock, \autoref{thm:cauchy-modulus-refinement-source-window-comparison-lock}, forces any consumer comparison to agree on that front before reading $T,W,Q,E,H,C,P,N$.

Finally apply Real consumer nonescape, \autoref{thm:cauchy-modulus-refinement-real-consumer-nonescape}. Every $\RealUp$ or Completion route remains inside $M_0,M_1,U,V,T,W,Q,E,H,C,P,N$, so quotient streams, selected host limits, second meet rows, hidden selectors, and detached completion witnesses cannot supply a separate distinction.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_window\_real\_consumer\_lock}

\begin{theorem}[Cauchy modulus refinement L10 completion pullback]
\label{thm:cauchy-modulus-refinement-l10-completion-pullback}
Every Phase Real and Completion consumer using an accepted $\CauchyModulusRefinementUp$ packet
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)
$$
pulls back first along the source-window front $M_0,M_1,U,V$, then along the selector and seal route $T,W,Q,E$, and finally along the structural rows $H,C,P,N$. No quotient stream, selected host limit, ambient completeness witness, detached completion witness, or host real equality supplies another L10 completion route.
\end{theorem}
\begin{proof}
Begin with source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It exposes $M_0,M_1,U,V$ before any selected-window or completion-facing row is available.

Next apply selector-stability lock, \autoref{thm:cauchy-modulus-refinement-selector-stability-lock}, and Real-seal root handoff, \autoref{thm:cauchy-modulus-refinement-real-seal-root-handoff}. These place the selector, selected-window, tolerance-readback, and Real-seal route $T,W,Q,E$ after the same source-window front.

Use Real-seal source conservation, \autoref{thm:cauchy-modulus-refinement-real-seal-source-conservation}. The seal preserves the displayed source route and cannot replace it by a quotient stream, selected host limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or detached completion witness.

Finally combine root window exhaustion, \autoref{thm:cauchy-modulus-refinement-root-window-exhaustion}, with Real consumer nonescape, \autoref{thm:cauchy-modulus-refinement-real-consumer-nonescape}. The structural rows $H,C,P,N$ preserve and name the same route, so every Phase Real and Completion consumer pulls back along the stated source, selector, seal, and structural rows.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_l10\_completion\_pullback}

\begin{theorem}[Cauchy modulus refinement completion-route pullback exactness]
\label{thm:cauchy-modulus-refinement-completion-route-pullback-exactness}
Every Phase Real and Completion consumer route of an accepted $\CauchyModulusRefinementUp$ packet factors through the source window
$$
M_0,M_1,U,V,
$$
the selector and seal rows
$$
T,W,Q,E,
$$
and the structural rows
$$
H,C,P,N.
$$
Conversely, any displayed route with those source-window, selector, seal, transport, continuation, package, and local naming rows determines the same Phase Real or Completion consumer. No quotient stream, selected host limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or detached completion witness supplies another consumer route.
\end{theorem}
\begin{proof}
First build the source-window front. By source-window front totality, \autoref{thm:cauchy-modulus-refinement-source-window-front-totality}, every selected-window, $\RealUp$, or Completion consumer begins with the displayed $M_0,M_1,U,V$ rows before reading the later selector, seal, or structural rows.

Next pull the Real and Completion seal route back along that same front. The source-window Real-seal pullback theorem, \autoref{thm:cauchy-modulus-refinement-source-window-real-seal-pullback}, identifies the route through $T,W,Q,E,H,C,P,N$ as the pullback of the displayed source-window front, with no coordinate for quotient streams, selected host limits, host real equality, hidden selectors, second meet rows, or ambient completeness.

Finally lock the consumer comparison. The source-window Real-consumer lock, \autoref{thm:cauchy-modulus-source-window-real-consumer-lock}, forces every Phase Real and Completion comparison to expose the same source window before reading $T,W,Q,E,H,C,P,N$. Hence the forward factorization is exact, and any displayed route with those rows determines the same consumer rather than a second route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_completion\_route\_pullback\_exactness}

\begin{corollary}[Cauchy modulus refinement source-window strict obstruction]
\label{cor:cauchy-modulus-refinement-source-window-strict-obstruction}
Let
$$
R=(M_0,M_1,U,V,T,W,Q,E,H,C,P,N)
$$
be an accepted $\CauchyModulusRefinementUp$ root packet. Any displayed refusal, separation, or countermodel row used against a source-window converse or consumer-reuse route for $R$ is a strict local obstruction: it blocks only the attempted route that either reads one of $T,W,Q,E,H,C,P,N$ before the source-meet-extraction front $M_0,M_1,U,V$, or sends a $\RealUp$ or Completion consumer through a quotient stream, selected host limit, host real equality, hidden selector, second meet row, ambient completeness coordinate, or detached completion witness outside the displayed packet. It does not state a global impossibility for Cauchy modulus refinement, a host theorem about all real completions, or a hidden external validation of any unlisted coordinate.
\end{corollary}
\leanchecked{BEDC.Derived.CauchyModulusRefinementUp.CauchyModulusRefinementCarrier\_source\_window\_strict\_obstruction}
\begin{proof}
Apply the source-window lock, \autoref{thm:cauchy-modulus-refinement-source-window-lock}. It places the source rows $M_0,M_1$, the shared meet row $U$, and the extraction row $V$ before any selected-window, seal, transport, continuation, package, or local-name row can be consumed. Hence a refusal or separation that concerns premature access to $T,W,Q,E,H,C,P,N$ can block only that premature selected-window route; it cannot be read as a statement about every possible Cauchy-modulus construction.

Now apply Real consumer nonescape, \autoref{thm:cauchy-modulus-refinement-real-consumer-nonescape}. Every $\RealUp$ or Completion consumer of the accepted packet factors through exactly
$$
M_0,M_1,U,V,T,W,Q,E,H,C,P,N.
$$
The same theorem excludes quotient streams, selected host limits, host real equality, hidden selectors, second meet rows, ambient completeness, and detached completion witnesses as consumer coordinates outside that packet. Therefore a consumer-facing obstruction can reject only those named off-route reuse attempts and cannot certify an unlisted host route.

Combining the two facts gives the strict scope. The first theorem fixes where the source-window route begins, and the second theorem exhausts every Real or Completion use by the displayed finite route. No cited row supplies a quantifier over all host completions, an ambient theorem about all real objects, or an external validation channel. Thus the obstruction is local to the named converse or reuse route in the accepted $\CauchyModulusRefinementUp$ packet.
\end{proof}
