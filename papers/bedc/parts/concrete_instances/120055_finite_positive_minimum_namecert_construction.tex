\chapter{A Concrete Naming Certificate for $\FinitePositiveMinimumUp$}
\label{ch:concrete-instances-finite-positive-minimum-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\FinitePositiveMinimumUp$ packet records the finite lower-bound fold by which finitely many positive rational radius rows produce one tolerance-only positive row for compact-uniform-modulus consumers. It sits after the rational source of \autoref{ch:concrete-instances-rat-namecert}, the compact finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, and the selector route of \autoref{ch:concrete-instances-compactnetmodulusselector-namecert}; it is consumed by the uniform-modulus and compact-uniform-continuity surfaces of \autoref{ch:concrete-instances-uniformmodulus-namecert}, \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}, and \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}. The packet is a displayed finite fold over positive $\RatUp$ rows, not a host minimum operator, selected subcover, open-cover compactness theorem, or ambient completeness principle.

\begin{definition}[FinitePositiveMinimum carrier]
\label{def:finite-positive-minimum-carrier}
A $\FinitePositiveMinimumUp$ carrier is a finite $\BHist$ packet
$$
F=(B,R,L,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite $\mathsf{ProbeBundle}$ index row naming the compact-net probes whose local radius rows are inspected.
\item $R$ is the family of positive $\RatUp$ radius rows indexed by $B$.
\item $L$ is the positive lower-bound row produced by primitive finite folding over the rows in $R$.
\item $K$ is the comparison ledger proving that $L$ is a positive rational row and lies below every displayed radius row in $R$.
\item $H$ records componentwise $\hsame$ transport for the probe bundle, radius family, lower-bound row, comparison ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-net selection through the positive radius family and comparison ledger to the lower-bound handoff.
\item $P$ is the $\Pkg$ provenance over $\FinitePositiveMinimumUp$, $\RatUp$, $\CompactMetricUp$, $\CompactNetModulusSelectorUp$, $\UniformModulusUp$, $\CompactUniformModulusUp$, $\CompactUniformContinuityUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FinitePositiveMinimumUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,R,L,K,H,C,P,N$. It has no coordinate for a host finite-set minimum, open-cover compactness, a selected subcover, quotient equality of compact points, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[FinitePositiveMinimum NameCert obligations]
\label{thm:finite-positive-minimum-namecert-obligations}
For every accepted $\FinitePositiveMinimumUp$ carrier
$$
F=(B,R,L,K,H,C,P,N),
$$
the local $\NameCert_{\FinitePositiveMinimumUp}$ surface consists exactly of finite probe-bundle admission, positive $\RatUp$ radius-family admission, lower-bound fold exposure, comparison-ledger exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-positive-minimum-carrier}. The mathematical rows are the probe bundle $B$, the positive rational radius family $R$, the lower-bound fold row $L$, and the comparison ledger $K$.

Read the finite fold before any consumer handoff. Since $B$ is finite and $R$ is displayed only over the probes in $B$, the row $L$ is produced by primitive finite recursion over named positive rational rows rather than by a host minimum operation.

The comparison ledger $K$ records positivity of $L$ and its lower-bound relation to every radius row in $R$. Transport and replay through $H$ and $C$ preserve the same componentwise route, while $P$ and $N$ source and name the packet. No further NameCert clause is available from compactness, choice, subcover selection, quotient equality, or completeness.
\end{proof}

\begin{theorem}[FinitePositiveMinimum compact-uniform-modulus handoff]
\label{thm:finite-positive-minimum-compact-uniform-modulus-handoff}
Every compact-uniform-modulus consumer of an accepted $\FinitePositiveMinimumUp$ carrier receives the positive lower-bound row only through the finite route
$$
\CompactMetricUp,\ \CompactNetModulusSelectorUp,\ \RatUp,\ \mathsf{ProbeBundle},\ L,K,H,C,P,N \longmapsto \UniformModulusUp .
$$
Thus the handoff supports the finite positive rational lower-bound step used by \autoref{thm:compact-uniform-finite-minimum-certificate} without exporting a host finite-set minimum, selected subcover, open-cover compactness theorem, quotient point equality, countable-choice functional, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with the compact-net side. The $\CompactMetricUp$ and $\CompactNetModulusSelectorUp$ consumers expose the finite probe bundle $B$ and the positive rational radius rows $R$ before any tolerance-only output can be read.

Apply the NameCert obligations of \autoref{thm:finite-positive-minimum-namecert-obligations}. They force the read through the lower-bound row $L$ and comparison ledger $K$, with componentwise transport and displayed replay supplied by $H$ and $C$.

Finally hand the resulting positive row to the $\UniformModulusUp$ and $\CompactUniformModulusUp$ surfaces. The provenance $P$ and local name $N$ keep the handoff attached to the same finite packet, so the downstream $\CompactUniformContinuityUp$ route receives a BEDC lower-bound ledger and not an external minimum, compactness theorem, selected subcover, quotient equality, choice principle, or ambient completeness row.
\end{proof}

\falsifiablePrediction{An accepted $\FinitePositiveMinimumUp$ read is refuted if a compact-uniform-modulus consumer receives a tolerance-only lower-bound row without displaying the finite probe bundle, positive rational radius family, comparison ledger, componentwise transport, continuation replay, provenance, and local naming rows.}

\independenceWitness{rat, compactmetric, compactnetmodulusselector, uniformmodulus, compact-uniform-modulus, compactuniformcontinuity: no listed sibling is bijective with $\FinitePositiveMinimumUp$ because this packet jointly carries the finite probe index, positive rational radius family, folded lower-bound row, comparison ledger, transport, replay, provenance, and local NameCert.}

\closureat{FinitePositiveMinimumUp}{seedStr}

\begin{closurestatus}{\FinitePositiveMinimumUp}
  \constructivestory{A $\FinitePositiveMinimumUp$ packet is generated from a finite ProbeBundle index row, positive RatUp radius rows, a folded positive lower-bound row, a comparison ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-positive-minimum-carrier}, \autoref{thm:finite-positive-minimum-namecert-obligations}, and \autoref{thm:finite-positive-minimum-compact-uniform-modulus-handoff} seed the finite positive rational lower-bound fold consumed by compact-uniform-modulus routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host finite-set minima, selected subcovers, open-cover compactness, quotient equality of compact points, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive rational family exposure, lower-bound fold exactness, comparison-ledger exactness, transport, replay, provenance, and local naming for BEDC.Derived.FinitePositiveMinimumUp.}
\end{closurestatus}
