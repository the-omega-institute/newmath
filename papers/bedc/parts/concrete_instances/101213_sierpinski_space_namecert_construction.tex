\chapter{A Concrete Naming Certificate for $\SierpinskiSpaceUp$}
\label{ch:concrete-instances-sierpinski-space-namecert}
\origin{human}

Following Sierpinski and Scott on observable opens, the $\SierpinskiSpaceUp$ packet records the two-observation classifier that turns open-set membership into finite BEDC data. It sits after the finite truth rows of \autoref{ch:concrete-instances-bool-namecert}, the open-set discipline of \autoref{ch:concrete-instances-topology-namecert}, the finite countable-name windows of \autoref{ch:concrete-instances-streamname-namecert}, and the Real-facing uniform-structure boundary of \autoref{ch:concrete-instances-real-uniform-structure-namecert}. The same observable-open truth row is the input surface for later topology consumers such as \autoref{ch:concrete-instances-tzero-reflection-namecert}, \autoref{ch:concrete-instances-urysohn-lemma-namecert}, and compact-uniform cover routes, where an open-membership read must remain a displayed Sierpinski classifier row rather than a host proposition. The packet names observable truth of opens as a classifier surface; it does not import excluded middle, selected limits, quotient streams, host propositions, or ambient $\RealUp$ completeness.

\begin{definition}[Sierpinski space carrier]
\label{def:sierpinski-space-carrier}
A $\SierpinskiSpaceUp$ carrier is a finite $\BHist$ packet
$$
S=(B,O,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BoolUp$ truth row, exposing the two accepted observable truth marks and their classifier transport.
\item $O$ is the $\TopologyUp$ open-set row, recording the open under inspection as a displayed observation row rather than as a host proposition.
\item $W$ is the $\StreamNameUp$ finite-observation row, recording the finite windows through which a countable name reports whether the open has been observed.
\item $H$ records componentwise $\hsame$ transport for truth rows, open rows, finite-observation windows, replay rows, provenance, and the local naming row.
\item $C$ records displayed $\Cont$ replay from an observable-open request through $B,O,W,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SierpinskiSpaceUp$, $\BoolUp$, $\TopologyUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SierpinskiSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,O,W,H,C,P,N$. It contains no coordinate for host propositions, excluded middle, selected limits, quotient stream equality, countable choice, or a proof of ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.SierpinskiSpaceUp.SierpinskiSpaceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Observable open-set classifier]
\label{thm:sierpinski-space-open-set-classifier}
Let $S=(B,O,W,H,C,P,N)$ be an accepted $\SierpinskiSpaceUp$ carrier. Any two accepted observable-open requests are compared only through the displayed open-truth rows $B,O,W$ and the specialization-order readback they determine. The classifier has no access to host propositions, proof irrelevance for ambient opens, excluded middle, or a hidden truth-value evaluator outside the packet.
\end{theorem}
\leanchecked{BEDC.Derived.SierpinskiSpaceUp.SierpinskiSpaceCarrier\_open\_set\_classifier}
\begin{proof}
Project the accepted carrier. The truth row $B$ supplies the only accepted observable truth marks, and the open row $O$ records the open under inspection as displayed topology data.

The finite-observation row $W$ is the only route by which a countable name can report an observation of that open. Thus a comparison may read whether one displayed observation is below another in the specialization order generated by the observable truth rows, but it cannot inspect an external proposition asserting open membership.

The structural rows $H,C,P,N$ transport, replay, source, and name that same finite route. Since none of them stores excluded middle, proof irrelevance, a hidden truth evaluator, selected limits, quotient streams, or ambient Real completeness, the classifier is exhausted by observable open-truth readback.
\end{proof}

\begin{theorem}[Sierpinski space NameCert obligation surface]
\label{thm:sierpinski-space-namecert-obligation-surface}
For every accepted $\SierpinskiSpaceUp$ carrier
$$
S=(B,O,W,H,C,P,N),
$$
the local $\NameCert_{\SierpinskiSpaceUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Bool truth row, Topology open-set row, StreamName finite-observation row, transport, replay, provenance, and local naming row;
\item open-truth classifier exactness, requiring every observable-open comparison to read $B,O,W$ before any structural row can be consumed;
\item specialization-order stability, requiring $\hsame$ transport to preserve the observable order read from the two truth marks and the displayed open row;
\item finite-observation stability, requiring every StreamName window in $W$ to report only the displayed observable truth row attached to $O$;
\item ledger non-escape, requiring $H,C,P,N$ to export no excluded middle, selected limits, quotient streams, countable choice, host proposition equality, ambient $\RealUp$ completeness, Lean verification, or bridge credential.
\end{enumerate}
No local observable-open obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:sierpinski-space-carrier}. The accepted packet displays exactly the Bool truth row, the Topology open-set row, finite StreamName observations, and the structural transport, replay, provenance, and local naming rows.

Open-truth classifier exactness follows from the order of the displayed rows. A request can compare observable opens only after reading the truth row $B$, the open row $O$, and the finite observation row $W$.

Specialization-order stability is the componentwise transport clause in $H$. Transport preserves the displayed truth marks and the open row, so the specialization readback determined by them is preserved as well.

Finite-observation stability is the projection to $W$. Each window reports the observable truth row attached to $O$, and no row selects a second stream representative or a detached open-membership proposition.

Finally, ledger non-escape is exhausted by $H,C,P,N$. These rows transport, replay, source, and name the displayed observable-open route only, and none stores excluded middle, selected limits, quotient streams, countable choice, host proposition equality, ambient Real completeness, Lean verification, or a bridge credential.
\end{proof}

\begin{theorem}[Sierpinski space consumer handoff]
\label{thm:sierpinski-space-consumer-handoff}
Every $\TopologyUp$, $\TZeroReflectionUp$, $\UrysohnLemmaUp$, or observable-open-filter consumer of an accepted $\SierpinskiSpaceUp$ packet must read the $\BoolUp$ truth row $B$, the $\TopologyUp$ open row $O$, the finite $\StreamNameUp$ windows $W$, and the structural rows $H,C,P,N$ before treating open membership as available. The consumer receives only displayed observable-open truth and its replay route; it receives no host proposition for membership, no excluded middle over opens, no proof-irrelevant open equality, no selected limit, and no quotient stream representative.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sierpinski-space-carrier}. The Bool truth row, the Topology open row, and the finite StreamName windows are the only rows that expose observable open membership.

Apply \autoref{thm:sierpinski-space-open-set-classifier}. It forces any membership comparison to read $B,O,W$ rather than a detached host proposition or hidden evaluator.

Apply \autoref{thm:sierpinski-space-namecert-obligation-surface}. The structural rows $H,C,P,N$ then transport, replay, source, and name that same displayed route before any downstream topology, T0, Urysohn, or observable-open-filter consumer may use it.

Since the carrier has no coordinate for excluded middle, proof-irrelevant open equality, selected limits, quotient stream representatives, or ambient Real completeness, none of those objects can be supplied by the handoff.
\end{proof}

\begin{theorem}[Sierpinski observable-open induction]
\label{thm:sierpinski-space-observable-open-induction}
Let $S=(B,O,W,H,C,P,N)$ be an accepted $\SierpinskiSpaceUp$ carrier, and let $\omega$ be a finite observable-open request history addressed to the displayed $\StreamNameUp$ window list $W$. The request is exhausted by induction on that finite window list. The base case reads no positive observation beyond the $\BoolUp$ truth row $B$ and the $\TopologyUp$ open row $O$. In the step case, consuming one displayed window from $W$ preserves the specialization-order readback through the transport row $H$, then replays the same observable-open route through $C,P,N$. Thus every finite observable-open request over $S$ remains inside $B,O,W,H,C,P,N$ before any topology-facing consumer can read open membership.
\end{theorem}
\begin{proof}
Read the observable-open classifier from \autoref{thm:sierpinski-space-open-set-classifier}. It restricts every membership comparison to the displayed truth row $B$, the open row $O$, the StreamName window row $W$, and the specialization-order readback determined by those rows.

Induct on the displayed finite window list inside $W$. In the empty case, the request history contains no positive window report, so the only available observable information is the pair $B,O$ together with the structural rows that name the route.

For a successor window, consume the first displayed window and apply the obligation surface of \autoref{thm:sierpinski-space-namecert-obligation-surface}. Finite-observation stability keeps the window attached to the same open row $O$, and specialization-order stability transports the readback componentwise through $H$.

After the window is consumed, apply the consumer handoff of \autoref{thm:sierpinski-space-consumer-handoff}. The remaining request is replayed through $C$ and sourced and named by $P,N$, so the induction step produces the same finite route for the tail of the window list. No host open-membership proposition, hidden evaluator, selected limit, quotient stream, or ambient Real-completeness coordinate is introduced.
\end{proof}

\closureat{SierpinskiSpaceUp}{seedStr}

\begin{closurestatus}{\SierpinskiSpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sierpinski-space-carrier}, \autoref{thm:sierpinski-space-open-set-classifier}, and \autoref{thm:sierpinski-space-namecert-obligation-surface} seed the finite L10 observable-open classifier route through Bool truth rows, Topology open rows, StreamName finite observations, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close excluded middle, choice, selected limits, quotient streams, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, open-truth classifier exactness, specialization-order stability, finite-observation stability, and ledger non-escape proofs for BEDC.Derived.SierpinskiSpaceUp.}
  \constructivestory{A $\SierpinskiSpaceUp$ packet is generated from a Bool truth row, a Topology open-set row, StreamName finite-observation windows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
