\chapter{A Concrete Naming Certificate for $\CarnotCaratheodoryMetricUp$}
\label{ch:concrete-instances-carnot-caratheodory-metric-namecert}
\origin{human}

Following Montgomery and Gromov, the $\CarnotCaratheodoryMetricUp$ packet records the finite BEDC surface for a Carnot--Caratheodory distance generated by horizontal paths and length budgets. It sits after the metric carrier of \autoref{ch:concrete-instances-metric-space-namecert}, the length-space route of \autoref{ch:concrete-instances-length-space-namecert}, the geodesic-metric packet of \autoref{ch:concrete-instances-geodesic-metric-namecert}, the manifold carrier of \autoref{ch:concrete-instances-manifold-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite horizontal-distribution rows, admissible path windows, length-budget rows, endpoint rows, metric and geodesic anchors, transport, replay, provenance, and local naming; it is not a completed sub-Riemannian structure, a host infimum over all curves, a selected minimizing path, or an ambient differentiable theorem.

\begin{definition}[Carnot--Caratheodory metric carrier]
\label{def:carnot-caratheodory-metric-carrier}
A $\CarnotCaratheodoryMetricUp$ carrier is a finite $\BHist$ packet
$$
M=(X,D,W,L,E,Q,G,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\ManifoldUp$ source row on which the horizontal distribution is read.
\item $D$ is the horizontal-distribution row, listing finite admissible directions in the displayed manifold window.
\item $W$ is the admissible path-window row, recording finite path segments whose velocities are certified through $D$.
\item $L$ is the finite length-budget row, with scalar readback sealed by $\RealUp$.
\item $E$ is the endpoint row, naming the source and target points reached by the displayed admissible windows.
\item $Q$ is the $\MetricSpaceUp$ anchor row, exposing the distance request only through the displayed endpoint and length-budget data.
\item $G$ is the $\LengthSpaceUp$ and $\GeodesicMetricUp$ anchor row, recording length-space compatibility and the finite geodesic-metric comparison boundary.
\item $T$ records componentwise $\hsame$ transport for $X,D,W,L,E,Q,G,C,P,N$.
\item $C$ records displayed $\Cont$ replay from manifold source through horizontal distribution, admissible path windows, length budgets, endpoints, metric anchors, length-space anchors, geodesic-metric anchors, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\CarnotCaratheodoryMetricUp$, $\ManifoldUp$, $\MetricSpaceUp$, $\LengthSpaceUp$, $\GeodesicMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CarnotCaratheodoryMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,D,W,L,E,Q,G,T,C,P,N$. It has no coordinate for host curve spaces, selected minimizers, unrestricted infima, quotient equality of paths, bracket-generation theorems, or ambient smooth control theory.
\end{definition}

\begin{theorem}[Carnot--Caratheodory metric NameCert obligations]
\label{thm:carnot-caratheodory-metric-namecert-obligations}
For every accepted $\CarnotCaratheodoryMetricUp$ carrier
$$
M=(X,D,W,L,E,Q,G,T,C,P,N),
$$
the local $\NameCert_{\CarnotCaratheodoryMetricUp}$ surface consists exactly of manifold-source exposure through $X$, horizontal-distribution exposure through $D$, admissible path-window exposure through $W$, finite length-budget exposure through $L$, endpoint exposure through $E$, $\MetricSpaceUp$ anchoring through $Q$, $\LengthSpaceUp$ and $\GeodesicMetricUp$ anchoring through $G$, componentwise $\hsame$ transport through $T$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:carnot-caratheodory-metric-carrier}. Its public rows are exactly the manifold source, horizontal distribution, admissible path windows, finite length budgets, endpoints, metric anchor, length-space and geodesic-metric anchors, transport, replay, provenance, and local naming.

The horizontal route is finite at the point of use. A consumer reads $D$ only as a displayed distribution row over $X$, reads $W$ only as admissible path windows certified by $D$, and reads $L$ only as finite length budgets with $\RealUp$ scalar sealing.

The metric route then passes through $E,Q,G$. Endpoints are displayed before a metric request is public, the $\MetricSpaceUp$ row records the distance-facing anchor, and the $\LengthSpaceUp$ and $\GeodesicMetricUp$ row records only finite compatibility and comparison data.

Finally $T,C,P,N$ preserve, replay, source, and name the same finite packet. Since the carrier has no coordinate for unrestricted curve spaces, selected minimizers, host infima, quotient path equality, bracket generation, or ambient smooth control theory, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Carnot--Caratheodory metric horizontal path route]
\label{thm:carnot-caratheodory-metric-horizontal-path-route}
Every metric-facing consumer of an accepted $\CarnotCaratheodoryMetricUp$ packet must factor through
$$
\ManifoldUp \longmapsto D \longmapsto W \longmapsto L \longmapsto E \longmapsto Q,G,T,C,P,N .
$$
Thus the readable distance surface is generated by displayed horizontal-distribution rows, admissible path windows, finite length budgets, endpoint rows, metric-space anchoring, length-space and geodesic-metric anchoring, transport, replay, provenance, and local NameCert rows.
\end{theorem}
\begin{proof}
Begin with the source row $X$. The horizontal distribution $D$ is attached to this displayed manifold window, so a path read cannot start from a detached host tangent bundle or an unrestricted family of curves.

Next inspect the admissible path windows and length budgets. The row $W$ lists the finite path segments admitted by $D$, while $L$ supplies the finite real-sealed budget data read by the metric request.

The endpoint and metric rows $E,Q,G$ then expose the distance-facing packet. They record the endpoints, $\MetricSpaceUp$ anchor, and $\LengthSpaceUp$ and $\GeodesicMetricUp$ comparison boundary without selecting a minimizer or asserting an ambient infimum theorem.

The structural rows $T,C,P,N$ keep this route stable under transport and replay. Therefore any public Carnot--Caratheodory metric read remains a finite horizontal-path route over the named siblings.
\end{proof}

\closureat{CarnotCaratheodoryMetricUp}{seedStr}

\begin{closurestatus}{\CarnotCaratheodoryMetricUp}
  \constructivestory{A $\CarnotCaratheodoryMetricUp$ packet is generated from a manifold source row, horizontal-distribution row, admissible path-window row, finite length-budget row, endpoint row, MetricSpace anchor, LengthSpace and GeodesicMetric anchor, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:carnot-caratheodory-metric-carrier}, \autoref{thm:carnot-caratheodory-metric-namecert-obligations}, and \autoref{thm:carnot-caratheodory-metric-horizontal-path-route} seed the finite horizontal-path metric packet over $\ManifoldUp$, $\MetricSpaceUp$, $\LengthSpaceUp$, $\GeodesicMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host sub-Riemannian geometry, bracket-generation theorems, selected minimizing paths, unrestricted curve infima, quotient path equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, horizontal-distribution exactness, admissible path-window exactness, finite length-budget readback, endpoint exposure, metric and length-space anchoring, geodesic-metric comparison, transport, replay, provenance, and local naming for BEDC.Derived.CarnotCaratheodoryMetricUp.}
\end{closurestatus}
