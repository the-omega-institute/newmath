\chapter{A Concrete Naming Certificate for $\FastCauchyCompletionUp$}
\label{ch:concrete-instances-fast-cauchy-completion-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy completion with explicit moduli, the $\FastCauchyCompletionUp$ packet records the finite route by which a modulus-controlled Cauchy name is exposed to Real and Completion consumers. It sits on the L10 spine supplied by the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the Real-facing modulus route of \autoref{ch:concrete-instances-realcauchymodulus-namecert}, and the rate-comparison boundary of \autoref{ch:concrete-instances-cauchy-rate-comparison-namecert}. The packet is a finite NameCert surface for modulus-controlled completion reads; it is not a quotient stream, a selected limit, trichotomy, countable choice, host equality, or ambient $\RealUp$ completeness.

\begin{definition}[Fast Cauchy completion carrier]
\label{def:fast-cauchy-completion-carrier}
A $\FastCauchyCompletionUp$ carrier is a finite $\BHist$ packet
$$
M=(M_0,S,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M_0$ is the $\CauchyModulusUp$ threshold row. It records the finite precision request, the displayed tail index, and the modulus budget that every downstream completion or Real read must consume.
\item $S$ is the $\StreamNameUp$ finite-window row opened at the threshold named by $M_0$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the same window, including dyadic radii, endpoint comparisons, rounding rows, and tolerance arithmetic.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from $S$ and $D$ under the budget exposed by $M_0$.
\item $E$ is the terminal $\RealUp$ seal handoff reached only after $M_0,S,D,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the modulus row, stream windows, dyadic ledger, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite completion or Real precision request through $M_0,S,D,Q,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FastCauchyCompletionUp$, $\RealCauchyModulusUp$, $\CauchyModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\CauchyRateComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FastCauchyCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M_0,S,D,Q,E,H,C,P,N$. It has no coordinate for quotient streams, selected limits, trichotomy, countable choice, host equality, or an ambient completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Fast Cauchy completion NameCert obligations]
\label{thm:fast-cauchy-completion-namecert-obligations}
For every accepted $\FastCauchyCompletionUp$ carrier $M=(M_0,S,D,Q,E,H,C,P,N)$, the local $\NameCert_{\FastCauchyCompletionUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Cauchy-modulus threshold, stream-window row, dyadic tolerance ledger, regular rational readback, Real seal handoff, and structural rows $H,C,P,N$;
\item modulus discipline, requiring every finite precision request to read the threshold row $M_0$ before any stream window, dyadic tolerance, regular readback, or Real seal is exposed;
\item stream and dyadic readback, requiring $S$ and $D$ to be read under the same displayed modulus budget before the $\RegSeqRatUp$ row $Q$ may be consumed;
\item Real-seal handoff, requiring $E$ to be terminal with respect to $M_0,S,D,Q$ and to expose no detached Real object or ambient completeness theorem;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same ordered route under componentwise sameness and displayed continuation replay;
\item ledger non-escape, requiring $P$ and $N$ to name only the displayed rows and to export no quotient stream, selected limit, trichotomy, countable-choice, host-equality, or ambient-completeness coordinate.
\end{enumerate}
No NameCert obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.FastCauchyCompletionUp.FastCauchyCompletionUp\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is projection from \autoref{def:fast-cauchy-completion-carrier}. The visible packet consists of the modulus threshold $M_0$, finite stream window $S$, dyadic tolerance ledger $D$, regular readback $Q$, Real seal $E$, and structural rows $H,C,P,N$.

Modulus discipline follows from the placement of $M_0$ at the front of the route. A completion-facing precision request first fixes the displayed threshold, and every later observation is read inside the finite budget named by that threshold.

The stream and dyadic readback obligation reads \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-regseqrat-namecert} in order. The window $S$ and ledger $D$ are consumed before $Q$ is available, so regular rational data cannot be detached from the displayed modulus budget.

The Real-seal handoff reads \autoref{ch:concrete-instances-realcauchymodulus-namecert}, \autoref{ch:concrete-instances-cauchy-rate-comparison-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The row $E$ is terminal and downstream of $M_0,S,D,Q$, while $H,C,P,N$ transport, replay, package, and name exactly that route.

Since quotient streams, selected limits, trichotomy, countable choice, host equality, and ambient Real completeness are absent from the carrier, no local NameCert obligation can export them.
\end{proof}

\begin{theorem}[Fast Cauchy completion modulus Real seal]
\label{thm:fast-cauchy-completion-modulus-real-seal}
Let $M=(M_0,S,D,Q,E,H,C,P,N)$ be an accepted $\FastCauchyCompletionUp$ carrier. Every Real-facing read exported by $M$ factors through the displayed modulus route
$$
M_0 \longmapsto S \longmapsto D \longmapsto Q \longmapsto E \longmapsto H,C,P,N .
$$
Consequently a Real or Completion consumer receives a Cauchy-modulus threshold, finite stream windows, dyadic tolerance arithmetic, regular rational readback, the terminal Real seal, transport, replay, provenance, and local naming before it can read the Real-facing datum. The route cannot use quotient streams, selected limits, trichotomy, countable choice, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FastCauchyCompletionUp.FastCauchyCompletionModulusRealSeal}
\begin{proof}
Begin with a Real-facing read of $M$. By \autoref{def:fast-cauchy-completion-carrier}, the only Real coordinate is $E$, and the definition places it downstream of the modulus threshold $M_0$, stream window $S$, dyadic ledger $D$, and regular readback row $Q$.

The first part of the route is fixed by the modulus and finite-window rows. The threshold $M_0$ supplies the finite tail budget, $S$ exposes only the corresponding stream observations, and $D$ supplies the dyadic tolerances and endpoint comparisons for those observations.

The second part of the route is fixed by regular readback and the Real seal. The row $Q$ is consumed only after $S$ and $D$, and $E$ seals the Real-facing datum only after the regular row has been displayed.

Finally, $H,C,P,N$ preserve and name the same dependency order. Because the excluded objects have no coordinate in the carrier and no replay edge in the displayed route, every Real-facing read factors through the modulus route shown in the statement.
\end{proof}

\begin{theorem}[Fast Cauchy completion finite induction]
\label{thm:fast-cauchy-completion-induction}
Let $M=(M_0,S,D,Q,E,H,C,P,N)$ be an accepted $\FastCauchyCompletionUp$ carrier. Every accepted precision-tail read of $M$ is obtained by finite induction on the displayed $\CauchyModulusUp$ threshold row $M_0$: the base read opens the first $\StreamNameUp$ window $S$ and the $\DyadicRatCoreUp$ tolerance ledger $D$; the successor step advances only by $\Cont$ replay through the same $S,D,Q$ route; and the terminal read reaches $E$ only through the $\RegSeqRatUp$ row $Q$ and the $\RealUp$ seal. No induction step uses quotient streams, selected limits, countable choice, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FastCauchyCompletionUp.FastCauchyCompletion\_precision\_tail\_finite\_induction}
\begin{proof}
Use the carrier of \autoref{def:fast-cauchy-completion-carrier} to expose the ordered rows $M_0,S,D,Q,E,H,C,P,N$. The NameCert obligations of \autoref{thm:fast-cauchy-completion-namecert-obligations} make the threshold row the first row read by any completion-facing precision request.

For the base threshold, read the first displayed stream window through $S$ and the dyadic tolerance ledger through $D$. These rows are already inside the accepted carrier, so the base read does not require a quotient stream, a selected limit, or host equality.

For the successor threshold, replay the previous route through the $\Cont$ row $C$. The replay can advance only the displayed finite window and tolerance rows, and the readback row $Q$ remains downstream of the same $S,D$ data.

The terminal step is the modulus Real-seal route of \autoref{thm:fast-cauchy-completion-modulus-real-seal}. It reaches $E$ through $Q$, then preserves the route through $H,C,P,N$. Since the excluded principles have no carrier coordinate and no replay edge, the displayed induction exhausts the accepted precision-tail reads.
\end{proof}

\begin{theorem}[Fast Cauchy completion obligation-closure surface]
\label{thm:fast-cauchy-completion-obligation-closure-surface}
The carrier, NameCert obligations, modulus Real seal, and finite induction assemble the $\obligationClosure$ surface for $\FastCauchyCompletionUp$ over $\CauchyModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The assembled surface is exactly the displayed route
$$
M_0,S,D,Q,E,H,C,P,N
$$
and refuses quotient streams, selected limits, countable choice, host equality, and ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FastCauchyCompletionUp.FastCauchyCompletionObligationClosureSurface}
\begin{proof}
Project the carrier of \autoref{def:fast-cauchy-completion-carrier}. Its rows name the Cauchy-modulus threshold, finite stream window, dyadic tolerance ledger, regular rational readback, Real seal, componentwise transport, continuation replay, provenance, and local naming surface.

Apply the NameCert obligations of \autoref{thm:fast-cauchy-completion-namecert-obligations}. These obligations force carrier admission, modulus discipline, stream and dyadic readback, Real-seal handoff, transport and replay stability, and ledger non-escape over the same rows.

Use the modulus Real seal \autoref{thm:fast-cauchy-completion-modulus-real-seal} and the finite induction theorem \autoref{thm:fast-cauchy-completion-induction}. The Real-facing read reaches $E$ only after $M_0,S,D,Q$, and each precision-tail step is replayed finitely through the displayed threshold, stream, dyadic, readback, and seal route.

Since the carrier and the three theorem blocks have no coordinate for quotient streams, selected limits, countable choice, host equality, or ambient $\RealUp$ completeness, the obligation-closure surface cannot export any of those objects.
\end{proof}

\falsifiablePrediction{Every $\FastCauchyCompletionUp$ Real-facing read factors through a CauchyModulus threshold, StreamName finite windows, DyadicRatCore tolerance arithmetic, RegSeqRat readback, a RealUp seal, transport, replay, provenance, and local naming.}

\independenceWitness{real-cauchy-modulus, cauchy-modulus, streamname, dyadicratcore, regseqrat, cauchy-rate-comparison, real: $\FastCauchyCompletionUp$ is not bijective to any listed sibling because it jointly carries the modulus threshold, stream window, dyadic tolerance ledger, regular readback, terminal Real seal, transport, replay, provenance, and local naming in one packet.}

\closureat{FastCauchyCompletionUp}{seedStr}
\closureat{FastCauchyCompletionUp}{obligationStr}

\begin{closurestatus}{\FastCauchyCompletionUp}
  \constructivestory{A $\FastCauchyCompletionUp$ packet is generated from a CauchyModulus threshold row, StreamName finite windows, a DyadicRatCore tolerance ledger, RegSeqRat readback, a RealUp seal handoff, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{thm:fast-cauchy-completion-obligation-closure-surface} assembles the carrier, NameCert obligations, modulus Real seal, and finite induction into the finite modulus-controlled completion obligation surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, trichotomy, countable choice, host equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires making the CauchyModulus, StreamName, DyadicRatCore, RegSeqRat, RealUp, BHist, hsame, Cont, Pkg, and NameCert dependencies explicit as one kernel route.}
\end{closurestatus}
