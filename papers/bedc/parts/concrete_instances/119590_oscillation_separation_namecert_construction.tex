\chapter{A Concrete Naming Certificate for $\OscillationSeparationUp$}
\label{ch:concrete-instances-oscillation-separation-namecert}
\origin{human}

Following Hardy and Bishop--Bridges, the $\OscillationSeparationUp$ packet records the finite lower-envelope and upper-envelope separation data carried by a bounded real sequence before any compactness or completeness principle is invoked. It binds the lower-tail endpoint of \autoref{ch:concrete-instances-liminf-namecert}, the upper-tail endpoint of \autoref{ch:concrete-instances-bounded-real-limsup-namecert}, the oscillation-facing row discipline of \autoref{ch:concrete-instances-real-oscillation-namecert}, the endpoint-gap packet of \autoref{ch:concrete-instances-limsup-liminf-gap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is an L10 lower/upper envelope separation carrier; it does not assert compactness, host supremum or infimum selection, quotient sequence equality, trichotomy, selected subsequences, or ambient $\RealUp$ completeness.

\begin{definition}[Oscillation separation carrier]
\label{def:oscillation-separation-carrier}
An $\OscillationSeparationUp$ carrier is a finite $\BHist$ packet
$$
O=(L,U,G,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower-envelope row, read through the $\LiminfUp$ tail lower-cut surface for the displayed bounded real sequence.
\item $U$ is the upper-envelope row, read through the $\BoundedRealLimsupUp$ finite upper-window surface for the same displayed bounded real sequence.
\item $G$ is the gap row, read through $\LimsupLiminfGapUp$ as the finite separation request between the two envelope rows.
\item $D$ is the dyadic tolerance ledger naming the rational budgets used when the gap row is replayed at the $\RealUp$ boundary.
\item $H$ records componentwise $\hsame$ transport for the lower envelope, upper envelope, gap, dyadic tolerance, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an oscillation-separation query through $L,U,G,D,H$.
\item $P$ is the $\Pkg$ provenance over $\OscillationSeparationUp$, $\LiminfUp$, $\BoundedRealLimsupUp$, $\RealOscillationUp$, $\LimsupLiminfGapUp$, $\RealUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\OscillationSeparationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,U,G,D,H,C,P,N$. No coordinate stores a compactness theorem, ambient completeness, host supremum or infimum, quotient equality of streams, selected subsequences, trichotomy, or a zero-gap convergence conclusion.
\end{definition}

\begin{theorem}[Oscillation separation seed obligations]
\label{thm:oscillation-separation-seed-obligations}
For every accepted $\OscillationSeparationUp$ carrier
$$
O=(L,U,G,D,H,C,P,N),
$$
the local $\NameCert_{\OscillationSeparationUp}$ surface contains the following seed obligations:
\begin{enumerate}
\item lower-envelope admission, requiring $L$ to be displayed as a $\LiminfUp$ row before the packet can answer a separation query;
\item upper-envelope admission, requiring $U$ to be displayed as a $\BoundedRealLimsupUp$ row over the same bounded sequence source before it can be compared with $L$;
\item gap exactness, requiring $G$ to read only the paired lower and upper envelope rows through the $\LimsupLiminfGapUp$ packet;
\item dyadic replay, requiring every comparison budget in $D$ to be replayed before the terminal $\RealUp$ boundary is consumed;
\item ledger nonescape, requiring $H,C,P,N$ to transport, replay, source, and name only the rows of $O$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:oscillation-separation-carrier}. The analytic rows are exactly the lower envelope $L$, upper envelope $U$, gap row $G$, and dyadic ledger $D$.

The lower and upper envelope obligations are row admissions. The packet has no independent sequence coordinate, so the two endpoints must be read through the sibling $\LiminfUp$ and $\BoundedRealLimsupUp$ surfaces that already name the bounded sequence source.

The gap and dyadic obligations are the next route in the carrier. The row $G$ can compare only the displayed envelope rows, and $D$ names the finite rational budgets used to replay that comparison at the real-name boundary.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier inventory has no compactness, completeness, quotient, subsequence, trichotomy, or convergence coordinate, the listed seed obligations exhaust the local surface.
\end{proof}

\begin{theorem}[Oscillation separation envelope route]
\label{thm:oscillation-separation-envelope-route}
Every Real-and-Completion consumer of an accepted $\OscillationSeparationUp$ carrier factors through the finite route
$$
L \longmapsto U \longmapsto G \longmapsto D \longmapsto H,C,P,N
$$
before reading the displayed lower/upper envelope separation. The route supplies the $\LiminfUp$ lower envelope, the $\BoundedRealLimsupUp$ upper envelope, the $\LimsupLiminfGapUp$ separation row, dyadic replay, componentwise transport, continuation replay, provenance, and local naming; it supplies no compactness theorem, host endpoint selection, quotient stream equality, selected subsequence, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the accepted carrier. The consumer first receives the lower envelope row $L$ and upper envelope row $U$, and these rows are visible only as sibling endpoint certificates.

The separation read then passes through $G$. Because $G$ is the only gap row in the carrier, it can bind only the displayed lower and upper envelopes, not a host supremum or infimum.

The dyadic ledger $D$ supplies the finite comparison budgets. The structural rows $H,C,P,N$ preserve the same route by transport, replay, provenance, and local naming, so no omitted global principle can enter the consumer surface.
\end{proof}

\begin{theorem}[Oscillation separation Real-boundary nonescape]
\label{thm:oscillation-separation-real-boundary-nonescape}
An accepted $\OscillationSeparationUp$ packet reaches the $\RealUp$ boundary only after the lower envelope, upper envelope, gap, and dyadic tolerance rows have been displayed. In particular, the packet does not export compactness, host supremum or infimum selection, quotient sequence equality, trichotomy, selected subsequences, zero-gap convergence, Lean verification, or a standard bridge.
\end{theorem}
\begin{proof}
The only Real-facing content in the carrier is reached through the rows $L,U,G,D$. The lower and upper envelope rows name the endpoint data, the gap row binds them into a separation request, and the dyadic ledger names the finite comparison budgets.

Replay through $C$ and transport through $H$ cannot add coordinates absent from $O$. Provenance and local naming through $P,N$ source the packet but do not create compactness, completeness, quotient, subsequence, trichotomy, convergence, verification, or bridge data.
\end{proof}

\falsifiablePrediction{An accepted $\OscillationSeparationUp$ read is refuted if the gap row $G$ is consumed while either the lower-envelope row $L$, the upper-envelope row $U$, or the dyadic tolerance ledger $D$ is absent.}

\independenceWitness{liminf, bounded-real-limsup, real-oscillation, limsup-liminf-gap, real: no listed sibling supplies the whole packet $O=(L,U,G,D,H,C,P,N)$, because each sibling contributes an endpoint, oscillation route, gap read, or terminal Real boundary while this chapter binds them into one finite separation carrier.}

\closureat{OscillationSeparationUp}{seedStr}

\begin{closurestatus}{\OscillationSeparationUp}
  \constructivestory{An $\OscillationSeparationUp$ packet is generated from a Liminf lower-envelope row, a bounded Real limsup upper-envelope row, a limsup-liminf gap row, a dyadic tolerance ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:oscillation-separation-carrier}, \autoref{thm:oscillation-separation-seed-obligations}, \autoref{thm:oscillation-separation-envelope-route}, and \autoref{thm:oscillation-separation-real-boundary-nonescape} seed the finite lower/upper envelope separation carrier for bounded real-sequence oscillation reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close compactness, host supremum or infimum selection, quotient sequence equality, trichotomy, selected subsequences, zero-gap convergence, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked lower-envelope admission, upper-envelope admission, gap exactness, dyadic replay, transport, replay, provenance, and local naming for BEDC.Derived.OscillationSeparationUp.}
\end{closurestatus}
