\chapter{A Concrete Naming Certificate for $\FiniteEpsilonNetUniformModulusUp$}
\label{ch:concrete-instances-finite-epsilon-net-uniform-modulus-namecert}
\origin{human}

Following the Bishop--Bridges finite-net proof of Heine--Cantor in \autoref{ch:visions-compact-uniform-continuity}, the $\FiniteEpsilonNetUniformModulusUp$ packet records the finite $\varepsilon$-net ledger that turns pointwise continuity rows on a compact metric source into a tolerance-only uniform-modulus handoff. It sits over the compact-metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the finite-net carrier of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the pointwise graph route of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the metric comparison source of \autoref{ch:concrete-instances-metric-namecert}, the rational tolerance rows of \autoref{ch:concrete-instances-rat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite net-to-modulus handoff; it is not open-cover compactness, a semantic continuity theorem, a selected infinite subcover, quotient function equality, countable choice, or ambient real completeness.

\begin{definition}[Finite epsilon-net uniform-modulus carrier]
\label{def:finite-epsilon-net-uniform-modulus-carrier}
A $\FiniteEpsilonNetUniformModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(K,F,G,L,Q,R,E,V,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the compact metric carrier by finite coverage data.
\item $F$ is the $\FiniteEpsilonNetUp$ row, storing the bundled centers, radius rows, and coverage witnesses.
\item $G$ is the $\ContinuousMapUp$ graph and pointwise-modulus row for the displayed map.
\item $L$ is the local radius ledger that attaches each bundled center to a positive $\RatUp$ tolerance row.
\item $Q$ is the folded lower-bound row over the finite center bundle.
\item $R$ is the $\MetricUp$ comparison row used to move from a source point to a nearby center and then to a second source point.
\item $E$ is the $\RealUp$ seal row for target-distance readback after rational comparisons have been displayed.
\item $V$ is the $\UniformModulusUp$ consumer row receiving the folded tolerance-only precision ledger.
\item $H$ records componentwise $\hsame$ transport for compact, net, continuous-map, rational, metric, Real, uniform-modulus, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay for the same finite-net uniform-modulus route.
\item $P$ is the $\Pkg$ provenance row over $\FiniteEpsilonNetUniformModulusUp$, $\CompactMetricUp$, $\FiniteEpsilonNetUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\MetricUp$, $\RatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteEpsilonNetUniformModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows and inherited transports. It has no coordinate for an open cover, semantic continuity, an infinite subcover selector, quotient function equality, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Finite epsilon-net uniform-modulus NameCert obligations]
\label{thm:finite-epsilon-net-uniform-modulus-namecert-obligations}
For every accepted $\FiniteEpsilonNetUniformModulusUp$ carrier
$$
U=(K,F,G,L,Q,R,E,V,H,T,P,N),
$$
the local $\NameCert_{\FiniteEpsilonNetUniformModulusUp}$ surface consists exactly of compact-metric source visibility, finite epsilon-net visibility, continuous-map pointwise-modulus visibility, rational local-radius exposure, finite lower-bound folding, metric comparison, Real seal readback, $\UniformModulusUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier. The mathematical rows are $K,F,G,L,Q,R,E,V$, and the structural rows are $H,T,P,N$.

Read $K,F,G,L$ first. These rows expose a compact metric source, a finite bundle of centers, the pointwise continuity data at those centers, and the rational local-radius ledger.

Read $Q,R,E,V$ next. The finite fold gives one tolerance-only precision row, the metric comparison row moves through the chosen center, the Real seal reads target-distance estimates, and the uniform-modulus row receives the folded ledger.

The structural rows preserve only this displayed route. Since no open-cover object, semantic function equality, infinite selector, choice schedule, or ambient completeness witness occurs in the carrier, the local NameCert cannot export such an endpoint.
\end{proof}

\begin{theorem}[Finite epsilon-net uniform-modulus compact handoff]
\label{thm:finite-epsilon-net-uniform-modulus-compact-handoff}
Every compact-uniform-continuity consumer of an accepted $\FiniteEpsilonNetUniformModulusUp$ carrier factors through
$$
\CompactMetricUp \longmapsto \FiniteEpsilonNetUp \longmapsto \ContinuousMapUp \longmapsto \RatUp \longmapsto \MetricUp \longmapsto \RealUp \longmapsto \UniformModulusUp .
$$
The handoff exports a finite tolerance-only uniform-modulus ledger. It does not export open-cover compactness, semantic continuity, selected infinite subcovers, quotient function equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the compact-metric and finite-net rows. They provide a finite bundle of centers and coverage witnesses, so the route begins with displayed finite data rather than an arbitrary open cover.

Apply the continuous-map and rational-radius rows at each center. The finite lower-bound fold then produces one precision ledger that is below all local radii in the bundle.

Finally use the metric comparison and Real seal rows to replay the usual two-triangle estimate through the chosen center before the uniform-modulus row is consumed. The packet therefore realizes the compact-uniform-continuity handoff as finite BEDC data and leaves all excluded global endpoints outside the carrier.
\end{proof}

\closureat{FiniteEpsilonNetUniformModulusUp}{seedStr}

\begin{closurestatus}{\FiniteEpsilonNetUniformModulusUp}
  \constructivestory{A $\FiniteEpsilonNetUniformModulusUp$ packet is generated from compact-metric data, finite epsilon-net centers and coverage witnesses, continuous-map pointwise rows, rational local-radius rows, a finite lower-bound fold, metric comparison, Real sealing, a UniformModulus consumer row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-epsilon-net-uniform-modulus-carrier}, \autoref{thm:finite-epsilon-net-uniform-modulus-namecert-obligations}, and \autoref{thm:finite-epsilon-net-uniform-modulus-compact-handoff} seed the finite epsilon-net route from compact pointwise continuity to a tolerance-only uniform-modulus ledger.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, semantic continuity, selected infinite subcovers, quotient function equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-metric visibility, finite-net coverage, continuous-map pointwise rows, rational fold exactness, metric comparison, Real seal nonescape, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.FiniteEpsilonNetUniformModulusUp.}
\end{closurestatus}
