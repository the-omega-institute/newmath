\chapter{A Concrete Naming Certificate for $\FiniteMinimaxUp$}
\label{ch:concrete-instances-finite-minimax-namecert}
\origin{human}

Following von Neumann's finite minimax theorem and constructive convex analysis, the $\FiniteMinimaxUp$ packet names a displayed finite saddle-certificate route over finite strategy polytopes and rational payoff brackets. It consumes the convex carrier of \autoref{ch:concrete-instances-convexset-namecert}, the finite polytope surface of \autoref{ch:concrete-instances-polytope-namecert}, the compact finite-net discipline of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous payoff route of \autoref{ch:concrete-instances-continuousmap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records a finite convex compact saddle certificate; it is not a host proof of Hahn--Banach separation, a selected global optimizer, an ambient quotient of mixed strategies, countable choice, or a completed function-space theorem.

\begin{definition}[FiniteMinimax carrier surface]
\label{def:finite-minimax-carrier-surface}
A $\FiniteMinimaxUp$ carrier is a finite $\BHist$ packet
$$
M=(X,Y,P,Q,F,A,B,S,H,C,K,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are finite strategy-index rows naming the two player faces.
\item $P$ and $Q$ are $\PolytopeUp$ strategy-simplex rows, each read through the ambient $\ConvexSetUp$ carrier.
\item $F$ is the compact finite-net row, recording the finite covering windows used to inspect the two polytopes through $\CompactMetricUp$.
\item $A$ is the $\ContinuousMapUp$ payoff row from the displayed product windows to the terminal $\RealUp$ seal.
\item $B$ is the rational tolerance bracket ledger, recording lower and upper payoff bounds for the inspected net cells.
\item $S$ is the saddle inequality ledger, recording the two displayed inequalities that certify the bracketed saddle window.
\item $H$ records componentwise $\hsame$ transport for strategy rows, polytopes, finite nets, payoff maps, rational brackets, saddle inequalities, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a finite strategy query through $X,Y,P,Q,F,A,B,S$.
\item $K$ is the $\Pkg$ provenance over $\FiniteMinimaxUp$, $\ConvexSetUp$, $\PolytopeUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteMinimaxUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,P,Q,F,A,B,S,H,C,K,N$. It has no coordinate for a host mixed-strategy quotient, selected exact optimizer, global real completeness, arbitrary separation principle, or countable choice.
\end{definition}

\begin{theorem}[FiniteMinimax NameCert obligations]
\label{thm:finite-minimax-namecert-obligations}
For every accepted $\FiniteMinimaxUp$ carrier
$$
M=(X,Y,P,Q,F,A,B,S,H,C,K,N),
$$
the local $\NameCert_{\FiniteMinimaxUp}$ surface consists exactly of carrier admission, finite strategy-row stability, convex-polytope compatibility for $P,Q$, compact finite-net coverage, continuous payoff readback, rational tolerance bracket stability, saddle inequality exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and ledger non-escape through $X,Y,P,Q,F,A,B,S,H,C,K,N$.
\end{theorem}
\begin{proof}
Project the carrier surface of \autoref{def:finite-minimax-carrier-surface}. The only analytic rows available to a minimax consumer are the displayed strategy indices, convex-polytope rows, finite-net row, continuous payoff row, rational brackets, and saddle inequality ledger.

The NameCert clauses are componentwise. Convexity is read through the sibling $\ConvexSetUp$ surface, finite mixed-strategy admission through $\PolytopeUp$, compact inspection through $\CompactMetricUp$, and payoff readback through $\ContinuousMapUp$ before the terminal $\RealUp$ seal is exposed.

The saddle ledger is then checked against the same bracket rows on the same finite windows. Transport, replay, provenance, and local naming preserve this route without adding a coordinate for a selected optimizer, quotient mixed-strategy equality, countable choice, or an ambient separation theorem.
\end{proof}

\closureat{FiniteMinimaxUp}{seedStr}

\begin{closurestatus}{\FiniteMinimaxUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-minimax-carrier-surface} and \autoref{thm:finite-minimax-namecert-obligations} seed the finite saddle-certificate carrier over convex polytopes, compact finite nets, continuous payoff rows, rational brackets, and saddle inequalities.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected exact optimizers, host mixed-strategy quotients, arbitrary separation theorems, countable choice, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires carrier admission, convex-polytope compatibility, compact finite-net coverage, continuous payoff readback, rational bracket stability, saddle inequality exactness, transport, replay, provenance, and non-escape for BEDC.Derived.FiniteMinimaxUp.}
  \constructivestory{A $\FiniteMinimaxUp$ packet is generated from finite strategy rows, two convex polytope strategy surfaces, compact finite-net windows, a continuous payoff row, rational tolerance brackets, a saddle inequality ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
