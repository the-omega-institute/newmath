\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseHorocycleUp$}
\label{ch:concrete-instances-hyperbolic-phase-horocycle-namecert}
\origin{ai}

The $\HyperbolicPhaseHorocycleUp$ packet realizes the observer-expansion branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite boundary-tangent phase transport. It sits beside the horocycle packet of \autoref{ch:concrete-instances-hyperbolic-horocycle-namecert}, the phase-ledger horocycle of \autoref{ch:concrete-instances-phase-ledger-horocycle-namecert}, the boundary-fixed transport surface of \autoref{ch:concrete-instances-poincare-boundary-fixed-transport-namecert}, the observer-expansion route of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, and the metric readback of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}. The packet records a finite phase-horocycle carrier; it does not assert an ambient hyperbolic manifold, a selected boundary limit, a global geodesic flow, or host equality of boundary points.

\begin{definition}[Hyperbolic phase horocycle carrier]
\label{def:hyperbolic-phase-horocycle-carrier}
A $\HyperbolicPhaseHorocycleUp$ carrier is a finite $\BHist$ packet
$$
H=(B,T,M,O,F,H_0,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the boundary-tangent row read through the displayed Poincare boundary data.
\item $T$ is the phase-transport row carrying the local phase ledger along the horocycle.
\item $M$ is the $\HyperbolicMetricUp$ readback row used to compare the transported phase position.
\item $O$ is the observer-expansion row recording the focused expansion ledger consumed by the packet.
\item $F$ is the refusal row excluding selected boundary limits, ambient geodesic flow, host point equality, and global hyperbolic manifold structure.
\item $H_0$ stores componentwise $\hsame$ transport for the boundary-tangent, phase-transport, metric, observer-expansion, refusal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from boundary-tangent data through phase transport and metric readback to the observer-expansion row.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicPhaseHorocycleUp$, $\HyperbolicMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$, with sibling pointers to the horocycle, phase-ledger, boundary-fixed transport, and observer-expansion packets.
\item $N$ is the local $\NameCert_{\HyperbolicPhaseHorocycleUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,T,M,O,F,H_0,C,P,N$. It has no coordinate for an ambient manifold, selected far-end limit, global flow, quotient boundary equality, or unlogged observer expansion.
\end{definition}

\begin{theorem}[Hyperbolic phase horocycle NameCert obligations]
\label{thm:hyperbolic-phase-horocycle-namecert-obligations}
For every accepted $\HyperbolicPhaseHorocycleUp$ carrier $H=(B,T,M,O,F,H_0,C,P,N)$, the local $\NameCert_{\HyperbolicPhaseHorocycleUp}$ surface consists exactly of carrier admission, boundary-tangent exposure, phase-transport replay, hyperbolic-metric readback, observer-expansion routing, refusal coverage, componentwise transport, provenance, and local naming. Any phase-horocycle consumer must display $B,T,M,O,F,H_0,C,P,N$ before it reads the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-phase-horocycle-carrier}. The displayed rows are the only classifier-visible coordinates, so carrier admission and local naming are immediate row projections.

The boundary and phase obligations are read from $B$ and $T$. The metric row $M$ supplies the hyperbolic readback for the same transported phase surface, and the observer-expansion row $O$ records the focused ledger consumed by that readback.

The refusal row $F$ excludes the ambient objects not carried by the packet. Transport $H_0$, replay $C$, and provenance $P$ preserve the same finite route, so the listed rows exhaust the local NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicPhaseHorocycleUp$ packet exposes boundary-tangent, phase-transport, hyperbolic-metric, observer-expansion, refusal, transport, replay, provenance, and NameCert rows before any phase-horocycle consumer reads it.}

\independenceWitness{hyperbolic-horocycle, phase-ledger-horocycle, poincare-boundary-fixed-transport, hyperbolic-observer-expansion, hyperbolic-metric: $\HyperbolicPhaseHorocycleUp$ is not bijective to any listed sibling because it jointly binds boundary-tangent phase transport, metric readback, observer-expansion routing, refusal, transport, replay, provenance, and local naming in one packet.}

Lean follow-up requires $\mathsf{Nontrivial}$ $\HyperbolicPhaseHorocycleUp$ and $\mathsf{FieldFaithful}$ $\HyperbolicPhaseHorocycleUp$ witnesses in the corresponding TasteGate surface.

\closureat{HyperbolicPhaseHorocycleUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseHorocycleUp}
  \constructivestory{A $\HyperbolicPhaseHorocycleUp$ packet is generated from boundary-tangent data, phase transport, hyperbolic metric readback, observer-expansion routing, refusal coverage, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-horocycle-carrier} and \autoref{thm:hyperbolic-phase-horocycle-namecert-obligations} seed the finite boundary-tangent phase-horocycle surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient hyperbolic manifolds, selected boundary limits, global geodesic flows, host boundary equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-tangent exposure, phase-transport replay, hyperbolic-metric readback, observer-expansion routing, refusal coverage, transport, provenance, and non-escape for BEDC.Derived.HyperbolicPhaseHorocycleUp.}
\end{closurestatus}
