\chapter{A Concrete Naming Certificate for $\MetaCICResidualDiamondWitnessUp$}
\label{ch:concrete-instances-metacic-residual-diamond-witness-namecert}
\origin{ai}

The $\MetaCICResidualDiamondWitnessUp$ packet realizes one bounded MetaCIC confluence frontier from \autoref{sec:metacic-open-problems-confluence} as finite residual-pair local-diamond data. It sits after the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the bounded residual-substitution frontier of \autoref{ch:concrete-instances-bounded-residual-substitution-frontier-namecert}, the residual-checker packet of \autoref{ch:concrete-instances-metacic-confluence-residual-checker-namecert}, the typed residual-budget packet of \autoref{ch:concrete-instances-metacic-typed-residual-budget-namecert}, and the closed parallel-diamond obstruction of \autoref{ch:concrete-instances-closed-parallel-diamond-obstruction-namecert}. The packet names a bounded local-diamond witness for a displayed residual pair; it does not claim full closed confluence, arbitrary parallel diamond, unrestricted substitution compatibility, global subject reduction, or a completed Church--Rosser theorem.

\begin{definition}[MetaCIC residual diamond witness carrier]
\label{def:metacic-residual-diamond-witness-carrier}
A $\MetaCICResidualDiamondWitnessUp$ carrier is a finite $\BHist$ packet
$$
J=(T,F,L,S,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row, recording the bounded closed MetaCIC source, type window, and residual-pair observation range.
\item $F$ is the residual-frontier row, naming the two residual steps whose local join is being checked.
\item $L$ is the bounded local-join checker row, recording the finite local-diamond search and its accepted join endpoint for this residual pair.
\item $S$ is the substitution-boundary row, exposing the exact residual-substitution interface consumed by the checker.
\item $O$ is the obstruction-retention row, recording the remaining frontier outside this bounded residual pair.
\item $H$ stores componentwise $\hsame$ transport for the typed window, residual frontier, local checker, substitution boundary, obstruction row, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
T,F,S \longmapsto L \longmapsto O .
$$
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualDiamondWitnessUp$, residual-substitution boundary data, bounded checker data, typed-window data, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\MetaCICResidualDiamondWitnessUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $T,F,L,S,O,H,C,P,N$. It has no coordinate for full confluence, arbitrary residual closure, unrestricted substitution compatibility, subject-reduction discharge, hidden evaluator state, or host syntax quotienting.
\end{definition}
\leanstmt{BEDC.Derived.MetaCICResidualDiamondWitnessUp}

\begin{theorem}[MetaCIC residual diamond witness obligations]
\label{thm:metacic-residual-diamond-witness-obligations}
For every accepted $\MetaCICResidualDiamondWitnessUp$ carrier $J=(T,F,L,S,O,H,C,P,N)$, the local $\NameCert_{\MetaCICResidualDiamondWitnessUp}$ surface consists exactly of typed-window exposure, residual-frontier exposure, bounded local-join checking, substitution-boundary exposure, obstruction retention, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is discharged by full confluence, arbitrary parallel diamond, global substitution compatibility, subject reduction, hidden evaluator state, or host syntax quotienting.
\end{theorem}
\begin{proof}
Project the carrier. The analytic rows are $T,F,L,S,O$, and the structural rows $H,C,P,N$ only transport, replay, source, and name those rows.

The local-diamond witness is bounded. The residual frontier $F$ and typed window $T$ determine the residual pair under inspection, while $S$ exposes the substitution boundary available to the checker.

The checker row $L$ records only the finite local join for that pair. The obstruction row $O$ remains visible, so the packet cannot be read as an arbitrary confluence theorem or as unrestricted substitution compatibility.
\end{proof}

\begin{theorem}[MetaCIC residual diamond witness local join route]
\label{thm:metacic-residual-diamond-witness-local-join-route}
Every accepted $\MetaCICResidualDiamondWitnessUp$ carrier routes its bounded local join through
$$
T,F,S \longmapsto L \longmapsto O .
$$
The row $L$ is available only after the typed window, residual frontier, and substitution boundary have been displayed, and the row $O$ records that the surrounding confluence frontier remains outside this bounded witness.
\end{theorem}
\begin{proof}
Read the typed window and residual frontier first. These rows fix the source, the observed residual pair, and the finite range over which the checker may operate.

Next read the substitution boundary. The checker receives exactly the boundary row $S$ and no hidden substitution theorem, evaluator state, or subject-reduction discharge.

Finally expose the local checker and obstruction rows. The row $L$ supplies the bounded join for the displayed pair, while $O$ retains the unclosed frontier beyond that pair.
\end{proof}

\begin{theorem}[MetaCIC residual diamond witness transport stability]
\label{thm:metacic-residual-diamond-witness-transport-stability}
Componentwise $\hsame$ transport of an accepted $\MetaCICResidualDiamondWitnessUp$ carrier preserves the typed window, residual frontier, substitution boundary, bounded local-join checker, and obstruction-retention rows as one finite witness packet. A transported consumer may replay the local diamond route, but it cannot strengthen the packet into full confluence or erase the retained obstruction boundary.
\end{theorem}
\begin{proof}
Transport acts componentwise on the displayed rows. Hence the typed window, residual pair, substitution boundary, checker result, obstruction row, replay, provenance, and local naming remain the classifier-visible coordinates.

Replay then follows the same continuation route. The transported checker row is still downstream from the typed window, residual frontier, and substitution boundary, and the obstruction row is still downstream from the checker.

The excluded endpoints are absent from the carrier. Since there is no row for arbitrary confluence, unrestricted substitution compatibility, subject reduction, hidden evaluator state, or host quotienting, transport cannot manufacture one.
\end{proof}

\falsifiablePrediction{Every accepted $\MetaCICResidualDiamondWitnessUp$ consumer that reads a bounded local join without displaying the typed window, residual frontier, substitution boundary, bounded checker, obstruction-retention row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row refutes the packet surface.}

\independenceWitness{metacic-residual-substitution-boundary, bounded-residual-substitution-frontier, metacic-confluence-residual-checker, metacic-typed-residual-budget, closed-parallel-diamond-obstruction: no listed sibling is bijective with $\MetaCICResidualDiamondWitnessUp$ because this packet jointly carries a typed window, residual pair, bounded local-join checker, substitution boundary, retained obstruction, transport, replay, provenance, and local naming.}

The Lean TasteGate for $\MetaCICResidualDiamondWitnessUp$ will include Nontrivial and FieldFaithful instances for the typed-window row, residual-frontier row, substitution-boundary row, bounded local-join checker, and obstruction-retention row, so a future checked surface can distinguish a genuine local witness from a carrier-only smoke packet.

\closureat{MetaCICResidualDiamondWitnessUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualDiamondWitnessUp}
  \constructivestory{A $\MetaCICResidualDiamondWitnessUp$ packet is generated from a typed-window row, a residual-frontier row, a bounded local-join checker, a substitution-boundary row, an obstruction-retention row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-diamond-witness-carrier}, \autoref{thm:metacic-residual-diamond-witness-obligations}, \autoref{thm:metacic-residual-diamond-witness-local-join-route}, and \autoref{thm:metacic-residual-diamond-witness-transport-stability} seed one bounded residual-pair local-diamond witness for the MetaCIC confluence frontier.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full closed confluence, arbitrary parallel diamond, unrestricted substitution compatibility, subject reduction, hidden evaluator state, host syntax quotienting, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked typed-window exposure, residual-frontier exposure, bounded local-join checking, substitution-boundary exposure, obstruction retention, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICResidualDiamondWitnessUp.}
\end{closurestatus}
