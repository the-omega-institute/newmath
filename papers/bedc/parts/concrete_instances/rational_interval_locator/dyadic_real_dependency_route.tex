\begin{theorem}[Rational interval locator dyadic Real dependency route]
\label{thm:rational-interval-locator-dyadic-real-route}
Every accepted $\RationalIntervalLocatorUp$ interval read that is consumed by $\RealSeparabilityUp$ must expose the $\DyadicRatCoreUp$ endpoint and tolerance rows, the finite $\StreamNameUp$ window, the $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal before the separability consumer can use the locator. The route cites the dyadic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and the forward link of \autoref{thm:rational-interval-locator-real-separability-forward-link}; it exports no quotient real equality, selected limit, host interval, countable-choice schedule, or ambient completeness.
\end{theorem}
\begin{proof}
Project the locator carrier of \autoref{def:rational-interval-locator-carrier}. The endpoint rows $L_0,U_0$, interval row $I$, tolerance row $T$, stream window $S$, readback row $R$, Real seal $A$, and structural rows $H,C,P,N$ are the whole accepted interval read.

The dyadic part is visible before the separability handoff. The endpoint and tolerance data are read through $L_0,U_0,T$ and the $\DyadicRatCoreUp$ provenance in $P$, while the finite window and readback rows $S,R$ keep the route inside the StreamName and RegSeqRat corridor.

Now apply the Real-facing route of \autoref{thm:rational-interval-locator-real-readback} and the RealSeparability forward link of \autoref{thm:rational-interval-locator-real-separability-forward-link}. The seal row $A$ must be displayed before any density or separability consumer can use the bracket, so the locator is consumed only after the $\RealUp$ boundary has been reached.

Since no carrier coordinate names quotient real equality, a selected limit, a host interval object, countable choice, or ambient completeness, the dependency route stops at the displayed dyadic endpoints, tolerance rows, readback row, and Real seal.
\end{proof}
