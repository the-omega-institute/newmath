The $\RationalIntervalLocatorUp$ endpoint rows supply the rational lower and upper locator consumed by the regular-Cauchy window lattice of \autoref{ch:concrete-instances-regular-cauchy-window-cofinality-namecert}. That consumption is allowed only after the $\StreamNameUp$ finite window, $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, and terminal $\RealUp$ seal rows are visible, so the edge remains a displayed L10 dependency rather than a host interval, quotient real, selected limit, countable-choice schedule, or ambient completeness claim.
