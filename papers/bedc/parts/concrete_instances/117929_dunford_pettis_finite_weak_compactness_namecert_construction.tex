\chapter{A Concrete Naming Certificate for $\DunfordPettisFiniteWeakCompactnessUp$}
\label{ch:concrete-instances-dunford-pettis-finite-weak-compactness-namecert}
\origin{human}

Following Dunford and Pettis, with the weak-topology and Banach-space background read through Bourbaki and Schaefer, the $\DunfordPettisFiniteWeakCompactnessUp$ packet records the finite BEDC window in which a Dunford--Pettis handoff can be inspected as weak-compactness evidence without importing the full compactness theorem. It sits between the Dunford--Pettis route of \autoref{ch:concrete-instances-dunford-pettis-namecert}, the weak compactness surface of \autoref{ch:concrete-instances-weak-compactness-namecert}, the weak-topology probe window of \autoref{ch:concrete-instances-weak-topology-namecert}, the Banach source of \autoref{ch:concrete-instances-banach-namecert}, and the scalar readback discipline of \autoref{ch:concrete-instances-real-namecert}. The chapter names only a finite window object: it does not claim Banach--Alaoglu, full weak compactness, selected weakly convergent subsequences, quotient function-space equality, or choice.

\begin{definition}[Dunford--Pettis finite weak-compactness carrier]
\label{def:dunford-pettis-finite-weak-compactness-carrier}
A $\DunfordPettisFiniteWeakCompactnessUp$ carrier is a finite $\BHist$ packet
$$
W=(D,K,T,B,R,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DunfordPettisUp$ source row, exposing only the already displayed uniform-integrability, weak-test, bounded-probe, scalar-budget, handoff, transport, replay, provenance, and local naming rows.
\item $K$ is the $\WeakCompactnessUp$ request row, read as a finite compactness-facing interface rather than as a completed weak-compactness theorem.
\item $T$ is the $\WeakTopologyUp$ probe-window row, listing the finitely many weak probes and tolerances visible in the present handoff.
\item $B$ is the $\BanachUp$ source row, naming the ambient linear and norm structure only through displayed finite functional-analysis data.
\item $R$ is the $\RealUp$ scalar readback row for probe values, bounds, tolerances, and comparison ledgers.
\item $A$ is the finite acceptance-window row. It records that the Dunford--Pettis handoff, weak-compactness request, weak-topology probes, Banach source, and scalar readback have been displayed in the same packet before a consumer can read weak-compactness evidence.
\item $H$ records componentwise $\hsame$ transport for $D,K,T,B,R,A$, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the route
$$
D,T,B,R \longmapsto A \longmapsto K,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\DunfordPettisFiniteWeakCompactnessUp$, $\DunfordPettisUp$, $\WeakCompactnessUp$, $\WeakTopologyUp$, $\BanachUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DunfordPettisFiniteWeakCompactnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,K,T,B,R,A,H,C,P,N$. It has no coordinate for Banach--Alaoglu, a full weak-compactness theorem, selected weakly convergent subsequences, quotient function-space equality, an ambient dual-ball compactness theorem, countable choice, or an unrestricted weak-probe family.
\end{definition}

\begin{theorem}[Dunford--Pettis finite weak-compactness obligations]
\label{thm:dunford-pettis-finite-weak-compactness-namecert-obligations}
For every accepted $\DunfordPettisFiniteWeakCompactnessUp$ carrier
$$
W=(D,K,T,B,R,A,H,C,P,N),
$$
the local $\NameCert_{\DunfordPettisFiniteWeakCompactnessUp}$ surface consists exactly of Dunford--Pettis source admission through $D$, weak-compactness request admission through $K$, finite weak-topology probe admission through $T$, Banach source admission through $B$, scalar readback through $R$, finite acceptance-window exactness through $A$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.

Every consumer must expose $D,T,B,R,A$ before reading $K$ as weak-compactness evidence. The row $K$ is therefore a finite window handoff, not Banach--Alaoglu, full weak compactness, a selected weakly convergent subsequence, quotient function-space equality, or a choice principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dunford-pettis-finite-weak-compactness-carrier}. Its public rows are exactly $D,K,T,B,R,A,H,C,P,N$, so the NameCert surface can expose only the Dunford--Pettis source, weak-compactness request, weak-topology probe window, Banach source, scalar readback, finite acceptance window, and structural rows.

The window exactness is forced by the order of the replay row. A consumer first reads the Dunford--Pettis source $D$, the weak-topology probes $T$, the Banach source $B$, and the scalar readback $R$; only then does the acceptance-window row $A$ allow the weak-compactness request $K$ to be inspected.

Transport and provenance do not add compactness data. The rows $H,C,P,N$ move, replay, source, and name the same finite route, while the classifier has no coordinate for Banach--Alaoglu, selected subsequences, quotient function-space equality, unrestricted weak compactness, or choice.
\end{proof}

\begin{theorem}[Dunford--Pettis finite weak-compactness window handoff]
\label{thm:dunford-pettis-finite-weak-compactness-window-handoff}
Every weak-compactness-facing consumer of an accepted $\DunfordPettisFiniteWeakCompactnessUp$ packet must pass through the finite route
$$
\DunfordPettisUp,\WeakTopologyUp,\BanachUp,\RealUp
\longmapsto
A
\longmapsto
\WeakCompactnessUp .
$$
The handoff may feed the finite weak-compactness interface of \autoref{ch:concrete-instances-weak-compactness-namecert}, but it exports no Banach--Alaoglu theorem, full weak compactness, selected weakly convergent subsequence, quotient function-space equality, countable-choice schedule, or ambient dual-ball compactness theorem.
\end{theorem}
\begin{proof}
Begin with the Dunford--Pettis source row $D$. It carries only the finite handoff described in \autoref{ch:concrete-instances-dunford-pettis-namecert}, so the consumer has not obtained a selected subsequence or ambient compactness theorem.

Next read the weak-topology, Banach, and Real rows $T,B,R$. These rows expose the finite probe window, source structure, scalar values, bounds, and tolerances needed for the present request.

The acceptance-window row $A$ then binds those reads to the weak-compactness request $K$. Since $A$ is a displayed finite row, it can hand the request to the sibling weak-compactness surface without promoting the route to Banach--Alaoglu, full weak compactness, quotient equality, or choice.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name the same route. No additional carrier coordinate remains through which a consumer could extract a selected weakly convergent subsequence, an ambient dual-ball compactness theorem, or unrestricted function-space equality.
\end{proof}

\closureat{DunfordPettisFiniteWeakCompactnessUp}{seedStr}

\begin{closurestatus}{\DunfordPettisFiniteWeakCompactnessUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dunford-pettis-finite-weak-compactness-carrier}, \autoref{thm:dunford-pettis-finite-weak-compactness-namecert-obligations}, and \autoref{thm:dunford-pettis-finite-weak-compactness-window-handoff} seed the finite window from Dunford--Pettis source data through weak-topology probes, Banach source rows, scalar readback, acceptance-window exactness, and weak-compactness request rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Banach--Alaoglu, full weak compactness, selected weakly convergent subsequences, quotient function-space equality, countable choice, ambient dual-ball compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite probe-window admission, Banach source compatibility, scalar readback, acceptance-window exactness, handoff nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DunfordPettisFiniteWeakCompactnessUp.}
  \constructivestory{A $\DunfordPettisFiniteWeakCompactnessUp$ packet is generated from a DunfordPettis source row, a WeakCompactness request row, a WeakTopology finite probe window, a Banach source row, Real scalar readback, an acceptance-window row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
