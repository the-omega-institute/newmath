\chapter{A Concrete Naming Certificate for $\AlternativeCodeReassignmentSpectraUp$}
\label{ch:concrete-instances-alternative-code-reassignment-spectra-namecert}
\origin{ai}

The $\AlternativeCodeReassignmentSpectraUp$ packet realizes the comparative reassignment-spectra vision of \autoref{def:philosophy-alternative-code-reassignment-spectrum} as a finite science-facing NameCert target. It refines the comparison-table carrier of \autoref{ch:concrete-instances-alternative-code-reassignment-namecert}, reads Walsh and median-closure spectral rows through \autoref{ch:concrete-instances-median-closure-spectrum-namecert}, keeps the decoded biological bridge explicit through \autoref{ch:concrete-instances-channel-genetic-code-bridge-namecert}, and routes support-family evidence through \autoref{ch:concrete-instances-support-nerve-realization-namecert}. The packet is a finite support, reassignment, spectral, transport, replay, provenance, and local naming surface; it is not a biological mechanism theorem or an exhaustive genetic-code survey.

\begin{definition}[Alternative-code reassignment spectra carrier]
\label{def:alternative-code-reassignment-spectra-carrier}
An $\AlternativeCodeReassignmentSpectraUp$ carrier is a finite $\BHist$ packet
$$
A=(S,R,W,E,T,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the sparse support row over the $13$ displayed reassigned codons and their module ledger;
\item $R$ is the alternative-code reassignment row, reading standard-table and curated alternative-table comparison through $\AlternativeCodeReassignmentUp$;
\item $W$ is the spectral row, recording Walsh, median-closure, and local module spectrum data from the reassignment-spectral-compression vision;
\item $E$ is the support-nerve realization row, tying the reassignment support to a finite boundary-subcomplex read;
\item $T$ is the transport row from channel-genetic-code bridge data to codon-window and reassignment rows;
\item $L$ is the replay row that orders support exposure, reassignment comparison, spectral readback, support-nerve realization, and local naming;
\item $H$ records componentwise $\hsame$ transport for all displayed support, reassignment, spectral, bridge, realization, replay, provenance, and naming rows;
\item $C$ records displayed $\Cont$ replay for the same route;
\item $P$ is the $\Pkg$ provenance over the source vision, sibling concrete-instance packets, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\AlternativeCodeReassignmentSpectraUp}$ row.
\end{enumerate}
The carrier has no coordinate for uncurated code-table enumeration, biological causality, probabilistic fitness, quotient codon semantics, or a hidden spectral-analysis oracle.
\end{definition}

\begin{theorem}[Alternative-code reassignment spectra NameCert obligations]
\label{thm:alternative-code-reassignment-spectra-namecert-obligations}
For every accepted $\AlternativeCodeReassignmentSpectraUp$ packet $A=(S,R,W,E,T,L,H,C,P,N)$, the local $\NameCert_{\AlternativeCodeReassignmentSpectraUp}$ surface consists exactly of support admission through $S$, reassignment comparison through $R$, spectral readback through $W$, support-nerve realization through $E$, channel-to-codon transport through $T$, ordered replay through $L,C$, componentwise $\hsame$ transport through $H$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted packet. The support row $S$ and reassignment row $R$ expose the finite comparative table data before any spectrum is read.

The spectral row $W$ is downstream from that displayed support. It records Walsh and median-closure information as finite row data and cannot introduce a host Fourier-analysis oracle or uncurated biological table family.

The rows $E,T,L,H,C,P,N$ bind the support-nerve, bridge, replay, transport, provenance, and local naming route. Since all consumer reads pass through these displayed rows, the NameCert surface is exactly the finite packet surface and not a claim of biological mechanism, exhaustive coverage, or quotient codon semantics.
\end{proof}

The Lean follow-up for this packet must provide \texttt{Nontrivial} and \texttt{FieldFaithful} instances in \texttt{BEDC/Derived/AlternativeCodeReassignmentSpectraUp/TasteGate.lean} before a checked local TasteGate can support the carrier.

\falsifiablePrediction{Every accepted $\AlternativeCodeReassignmentSpectraUp$ read displays support, reassignment, spectral, transport, replay, provenance, and local naming rows; a read that reaches a spectral conclusion without any one of those rows refutes the packet.}

\independenceWitness{alternative-code-reassignment, reassignment-spectral-compression, channel-genetic-code-bridge, support-nerve-realization: none is bijective with $\AlternativeCodeReassignmentSpectraUp$ because this packet jointly carries sparse support, comparative reassignment, Walsh and median-closure spectral rows, channel-to-codon transport, support-nerve realization, replay, provenance, and local naming.}

\closureat{AlternativeCodeReassignmentSpectraUp}{seedStr}

\begin{closurestatus}{\AlternativeCodeReassignmentSpectraUp}
  \constructivestory{An $\AlternativeCodeReassignmentSpectraUp$ packet is generated from sparse reassignment support rows, alternative-code comparison rows, spectral readback rows, support-nerve realization rows, channel-to-codon transport, ordered replay, componentwise hsame transport, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:alternative-code-reassignment-spectra-carrier} and \autoref{thm:alternative-code-reassignment-spectra-namecert-obligations} seed the finite support, reassignment, spectral, transport, replay, provenance, and naming surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim biological causality, exhaustive genetic-code coverage, probabilistic fitness, quotient codon semantics, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, support exposure, reassignment comparison, spectral readback, support-nerve realization, transport stability, replay provenance, Nontrivial AlternativeCodeReassignmentSpectraUp, and FieldFaithful AlternativeCodeReassignmentSpectraUp for BEDC.Derived.AlternativeCodeReassignmentSpectraUp.}
\end{closurestatus}
