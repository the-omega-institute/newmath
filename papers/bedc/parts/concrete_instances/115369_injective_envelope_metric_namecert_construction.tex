\chapter{A Concrete Naming Certificate for $\InjectiveEnvelopeMetricUp$}
\label{ch:concrete-instances-injective-envelope-metric-namecert}
\origin{human}

Following Aronszajn--Panitchpakdi and Isbell on injective metric envelopes, the $\InjectiveEnvelopeMetricUp$ packet records the finite NameCert surface by which a displayed metric source is admitted to an injective-envelope metric boundary. It sits over the metric source of \autoref{ch:concrete-instances-metric-namecert}, reads the Isbell tight-span carrier of \autoref{ch:concrete-instances-isbell-tight-span-namecert}, exposes the hyperconvex handoff of \autoref{ch:concrete-instances-hyperconvex-metric-namecert}, transports the source through the metric embedding route of \autoref{ch:concrete-instances-metricembedding-namecert}, keeps the completion boundary of \autoref{ch:concrete-instances-completemetric-namecert} visible, and seals all radius comparisons through \autoref{ch:concrete-instances-real-namecert}. The packet names a finite envelope admission route; it does not select a host hull point, assert arbitrary-family intersection, identify quotient completions, invoke countable choice, or close ambient real completeness.

\begin{definition}[Injective envelope metric carrier]
\label{def:injective-envelope-metric-carrier}
An $\InjectiveEnvelopeMetricUp$ carrier is a finite $\BHist$ packet
$$
E=(M,I,J,HX,K,R,T,U,V,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row, including the inspected point classifier and distance observations.
\item $I$ is the $\IsbellTightSpanUp$ envelope row, read through finite distance-envelope inequalities, extremal witnesses, one-point extension tests, separated comparison, and Real readback.
\item $J$ is the $\MetricEmbeddingUp$ row, recording the displayed isometric source inclusion into the envelope boundary.
\item $HX$ is the $\HyperconvexMetricUp$ handoff row, exposing pairwise-compatible ball data and the finite intersection request only after $M,I,J$ have been read.
\item $K$ is the $\CompleteMetricUp$ completion-boundary row, keeping completion-neighbour use downstream of the metric source, envelope row, embedding row, and hyperconvex handoff.
\item $R$ is the $\RealUp$ radius seal row for all distance, envelope, ball, and separated-comparison readings.
\item $T$ records componentwise $\hsame$ transport for $M,I,J,HX,K,R,P,N$ and for the replay row.
\item $U$ records displayed $\Cont$ replay from metric observations to tight-span envelopes, embedding transport, hyperconvex ball handoff, completion boundary, and Real radius seal.
\item $V$ is the provenance route through the sibling certificates named above, including the local source row that prevents the envelope from being reread as an ungrounded host hull.
\item $P$ is the $\Pkg$ provenance over $\InjectiveEnvelopeMetricUp$, $\MetricUp$, $\IsbellTightSpanUp$, $\MetricEmbeddingUp$, $\HyperconvexMetricUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\InjectiveEnvelopeMetricUp}$ row.
\end{enumerate}
The local classifier compares accepted packets only through $M,I,J,HX,K,R,T,U,V,P,N$. It has no coordinate for a selected injective hull, arbitrary ball-family intersection, quotient completion representative, countable-choice selector, or ambient $\RealUp$ completeness theorem.
\end{definition}

\begin{theorem}[Injective envelope metric NameCert obligations]
\label{thm:injective-envelope-metric-namecert-obligations}
For every accepted $\InjectiveEnvelopeMetricUp$ carrier $E=(M,I,J,HX,K,R,T,U,V,P,N)$, the local $\NameCert_{\InjectiveEnvelopeMetricUp}$ surface consists exactly of metric-source exposure, Isbell tight-span envelope admission, metric-embedding transport, hyperconvex handoff, completion-boundary visibility, Real radius sealing, componentwise transport, displayed replay, sibling provenance, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:injective-envelope-metric-carrier}. The rows $M,I,J,HX,K,R,T,U,V,P,N$ are the only displayed data admitted by the packet.

Use the metric source chapter for $M$, the Isbell tight-span obligations for $I$, and the metric embedding route for $J$. These rows fix the source, envelope admission, and isometric inclusion before any completion-facing read is available.

Use the hyperconvex metric obligations for $HX$ and the complete metric boundary for $K$. The hyperconvex row can be consumed only as a finite handoff over the same source and envelope data, while the completion row remains downstream of that handoff.

Finally read the Real radius seal and the structural rows $T,U,V,P,N$. They transport, replay, source, package, and name the same finite route. Since no remaining coordinate stores a selected hull point, arbitrary-family intersection, quotient completion representative, choice selector, or ambient completeness witness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Injective envelope metric hyperconvex handoff]
\label{thm:injective-envelope-metric-hyperconvex-handoff}
Every completion-neighbour or Real-radius consumer of an accepted $\InjectiveEnvelopeMetricUp$ carrier must read
$$
\MetricUp\longmapsto \IsbellTightSpanUp\longmapsto \MetricEmbeddingUp\longmapsto \HyperconvexMetricUp\longmapsto \CompleteMetricUp\longmapsto \RealUp
$$
through the displayed rows $M,I,J,HX,K,R$ before it treats the packet as an injective-envelope metric boundary.
\end{theorem}
\begin{proof}
Start with the carrier projection. The only route to the hyperconvex handoff row $HX$ passes through the metric source $M$, the Isbell envelope row $I$, and the metric embedding row $J$.

Apply the Isbell tight-span metric handoff. It keeps finite envelope inequalities, extremal witnesses, one-point extension tests, separated comparison, completion readback, and Real readback visible before an envelope consumer can proceed.

Apply the hyperconvex metric ball-intersection handoff. It reads pairwise-compatible ball data and the finite intersection request over the same metric and Real radius rows; it does not introduce a selected global hull point or an arbitrary-family intersection coordinate.

The complete metric row $K$ and Real radius row $R$ are downstream of those sibling handoffs. Transport, replay, provenance, and local naming preserve that route without adding a quotient completion representative, choice function, or ambient completeness principle. Thus the injective-envelope consumer receives exactly the displayed finite handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\InjectiveEnvelopeMetricUp$ consumer that reaches a hyperconvex, completion, or Real-radius read without first exposing the displayed metric source, Isbell tight-span envelope, metric embedding, and sibling hyperconvex handoff refutes the carrier surface.}

\independenceWitness{metric, isbell-tight-span, hyperconvex-metric, metricembedding, completemetric, real: no listed sibling supplies the whole eleven-row packet $E=(M,I,J,HX,K,R,T,U,V,P,N)$, because each sibling contributes only a source, envelope, embedding, ball-intersection handoff, completion boundary, or Real seal while this chapter binds all six faces with transport, replay, provenance, and local naming.}

\closureat{InjectiveEnvelopeMetricUp}{seedStr}

\begin{closurestatus}{\InjectiveEnvelopeMetricUp}
  \constructivestory{An $\InjectiveEnvelopeMetricUp$ packet is generated from a metric source row, an Isbell tight-span envelope row, a metric embedding row, a hyperconvex handoff row, a complete-metric boundary row, a Real radius seal row, componentwise transports, displayed replay, sibling provenance, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:injective-envelope-metric-carrier}, \autoref{thm:injective-envelope-metric-namecert-obligations}, and \autoref{thm:injective-envelope-metric-hyperconvex-handoff} seed the finite injective-envelope metric admission route over the displayed metric, Isbell, embedding, hyperconvex, completion, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected injective hulls, arbitrary ball-family intersections, quotient completion representatives, countable choice, or ambient Real completeness.}
  \upgradepath{Obligation closure requires carrier admission, sibling envelope routing, metric-embedding transport, hyperconvex handoff, completion-boundary visibility, and Real radius sealing for the same packet.}
\end{closurestatus}
