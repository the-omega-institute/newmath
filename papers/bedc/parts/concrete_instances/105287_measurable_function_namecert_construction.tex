\chapter{A Concrete Naming Certificate for $\MeasurableFunctionUp$}
\label{ch:concrete-instances-measurable-function-namecert}
\origin{human}

Following Caratheodory and Bourbaki measurable maps, the $\MeasurableFunctionUp$ packet records the finite preimage surface by which a source measurable-event family is routed into a target measurable-event family. It sits after the event rows of \autoref{ch:concrete-instances-measure-namecert}, the generated-set boundary of \autoref{ch:concrete-instances-borel-set-namecert}, the $\SetLikeUp$ set-membership carrier discipline, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}. The packet names measurable-map behavior as finite preimage ledgers, not as a host function space, quotient equality of functions, selected representatives, or ambient completed measure theory.

\begin{definition}[MeasurableFunction carrier]
\label{def:measurable-function-carrier}
A $\MeasurableFunctionUp$ carrier is a finite $\BHist$ packet
$$
F=(S,T,A,B,G,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source measurable carrier row, read through $\MeasureUp$ or through a $\BorelSetUp$ event source when the map is real-facing.
\item $T$ is the target measurable carrier row, again read through $\MeasureUp$ or the $\BorelSetUp$ generated-set boundary.
\item $A$ is the source event-family row, with membership and subset readback routed through $\SetLikeUp$.
\item $B$ is the target event-family row, with target membership readback routed through $\SetLikeUp$, $\RealUp$, and finite $\StreamNameUp$ observations when the target is a real or Borel source.
\item $G$ is the graph-read row naming the finite source-to-target observation pairs exposed to the packet.
\item $R$ is the preimage ledger assigning each displayed target event row in $B$ to a displayed source event row in $A$ and recording the finite membership readback for the graph row $G$.
\item $H$ stores componentwise $\hsame$ transport for the source carrier, target carrier, event-family rows, graph row, preimage ledger, provenance, and local naming rows.
\item $C$ stores the displayed $\Cont$ replay by which a consumer reads target events, preimage events, graph observations, and source memberships.
\item $P$ is the $\Pkg$ provenance over $\MeasureUp$, $\BorelSetUp$, $\SetLikeUp$, $\RealUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MeasurableFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,T,A,B,G,R,H,C,P,N$. It has no coordinate for a host function type, quotient equality of functions, almost-everywhere equality, completion of a measure, or a selected representative.
\end{definition}

\begin{theorem}[MeasurableFunction NameCert obligations]
\label{thm:measurable-function-namecert-obligations}
For the carrier of \autoref{def:measurable-function-carrier}, the local $\NameCert_{\MeasurableFunctionUp}$ surface consists exactly of carrier admission, source-event admission, target-event admission, graph-read admission, preimage-ledger exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming. Every accepted measurable-map consumer must read a target event through $B$, its displayed preimage through $R$, and the source-event result through $A$ before any $\MeasureUp$, $\BorelSetUp$, $\SetLikeUp$, $\RealUp$, or $\StreamNameUp$ sibling row is exposed.
\end{theorem}
\begin{proof}
Project the carrier $F=(S,T,A,B,G,R,H,C,P,N)$. The source and target carrier rows $S,T$ supply the measurable contexts, while $A$ and $B$ supply the only event-family rows available to the packet.

Read a target event request through $B$. The preimage ledger $R$ is the unique row that assigns that target event to a source event in $A$, and the graph row $G$ supplies the finite membership readback tying source observations to target observations.

The structural rows add no host function object. The componentwise transport $H$, replay row $C$, provenance row $P$, and local naming row $N$ preserve the same displayed route through the sibling certificates, so the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[MeasurableFunction simple preimage ledger]
\label{thm:measurable-function-simple-preimage-ledger}
Every simple target-event read of an accepted $\MeasurableFunctionUp$ packet factors through the finite ledger route
$$
B \longmapsto R \longmapsto A \longmapsto G \longmapsto H,C,P,N .
$$
For real-facing Borel targets, the target membership observation reaches $\RealUp$ and $\StreamNameUp$ only through the displayed $\BorelSetUp$ row in $B$ and the graph-read row $G$. Thus preimage formation exports a named source event row, not a host inverse-image function, almost-everywhere quotient, completed measure, selected representative, or ambient function equality.
\end{theorem}
\begin{proof}
Start with an accepted target-event request. The request is not a free subset of a host codomain; it is a displayed target event row in $B$, transported through the target carrier row $T$.

The preimage ledger $R$ is then inspected. It names the corresponding source event row in $A$ and records the graph-read membership comparison supplied by $G$. For real-facing Borel targets, this graph read is the only place where $\RealUp$ and $\StreamNameUp$ observations enter the route.

Finally replay the structural rows. The rows $H,C,P,N$ transport, replay, source, and name the same finite route over $\MeasureUp$, $\BorelSetUp$, and $\SetLikeUp$. Since no further coordinate is present, the excluded host inverse-image, quotient, completion, representative-selection, and ambient equality objects cannot be exported.
\end{proof}

\falsifiablePrediction{A $\MeasurableFunctionUp$ consumer that obtains a host function equality, an almost-everywhere quotient class, a completed measure, or a selected representative without first reading the displayed preimage ledger $R$ refutes the carrier surface.}

\independenceWitness{measure, borelset, setlike, real, streamname: no listed sibling supplies the whole ten-row packet $F=(S,T,A,B,G,R,H,C,P,N)$, because each sibling contributes only event-family, generated-set, membership, real-seal, or finite-window data while this chapter binds the measurable-map preimage ledger with transport, replay, provenance, and local naming.}

\begin{closurestatus}{\MeasurableFunctionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:measurable-function-carrier}, \autoref{thm:measurable-function-namecert-obligations}, and \autoref{thm:measurable-function-simple-preimage-ledger} seed the finite measurable-map surface over source events, target events, graph reads, preimage ledgers, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close random variables, distributions, integrals, almost-everywhere equality, completed measures, quotient function spaces, selected representatives, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, preimage-ledger exactness, graph-read preservation, componentwise transport, and ledger-refusal proofs for BEDC.Derived.MeasurableFunctionUp.}
  \constructivestory{A $\MeasurableFunctionUp$ packet is generated from source and target measurable carrier rows, source and target event-family rows, a graph-read row, a preimage ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
