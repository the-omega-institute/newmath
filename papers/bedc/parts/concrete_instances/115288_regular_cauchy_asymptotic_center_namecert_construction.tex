\chapter{A Concrete Naming Certificate for $\RegularCauchyAsymptoticCenterUp$}
\label{ch:concrete-instances-regular-cauchy-asymptotic-center-namecert}
\origin{human}

Following Goebel and Kirk, the $\RegularCauchyAsymptoticCenterUp$ packet records the finite naming surface for an asymptotic-center read along a displayed regular Cauchy completion path. It sits over the regular rational rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the common stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the generic metric surface of \autoref{ch:concrete-instances-metric-namecert}, and the complete-metric witness row of \autoref{ch:concrete-instances-complete-metric-space-namecert}. The packet names the center candidate by finite readback and metric-radius comparison, not by quotienting Cauchy names, selecting a limit, or importing compactness.

\begin{definition}[Regular Cauchy asymptotic-center carrier]
\label{def:regular-cauchy-asymptotic-center-carrier}
A $\RegularCauchyAsymptoticCenterUp$ carrier is a finite $\BHist$ packet
$$
Q=(M,K,A,W,D,R,E,T,H,C,P,S,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row whose distance classifier is used for every radius comparison.
\item $K$ is the $\CompleteMetricUp$ witness row supplying the completion-facing limit discipline for the ambient metric carrier.
\item $A$ is the displayed regular Cauchy source family whose tail distances are being inspected.
\item $W$ is the common $\StreamNameUp$ window through which all tail and center-candidate reads are exposed.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the metric-radius budget read through $W$.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational approximants of the tail-radius data.
\item $E$ is the candidate-center row. It is presented only through the same finite window and the same metric source as the tail reads.
\item $T$ is the finite asymptotic-radius comparison row, recording that every inspected tail request factors through $M,K,A,W,D,R,E$ before a center claim is visible.
\item $H$ records componentwise $\hsame$ transport for the metric source, complete-metric witness, Cauchy source family, common window, dyadic tolerance ledger, regular rational readback, candidate row, comparison row, package provenance, real seal, and local naming row.
\item $C$ records displayed $\Cont$ replay from an asymptotic-center request through the same rows.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyAsymptoticCenterUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\MetricUp$, $\CompleteMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $S$ is the terminal $\RealUp$ seal row reached only after the finite radius ledger and regular rational readback have been displayed.
\item $N$ is the local $\NameCert_{\RegularCauchyAsymptoticCenterUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient completion, a selected ambient limit, compactness, uniform convexity, excluded middle, countable choice, or a global minimizer principle outside the displayed packet.
\end{definition}

\begin{theorem}[Regular Cauchy asymptotic-center NameCert obligations]
\label{thm:regular-cauchy-asymptotic-center-namecert-obligations}
For every accepted $\RegularCauchyAsymptoticCenterUp$ carrier $Q=(M,K,A,W,D,R,E,T,H,C,P,S,N)$, the local $\NameCert_{\RegularCauchyAsymptoticCenterUp}$ surface consists exactly of carrier admission, common-window exposure, dyadic tolerance budgeting, regular rational readback, metric-radius comparison, complete-metric limit-discipline handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, terminal $\RealUp$ sealing, and local naming over the rows displayed in \autoref{def:regular-cauchy-asymptotic-center-carrier}. No obligation row exports quotient equality of Cauchy names, selected limits, compactness, uniform convexity, countable choice, trichotomy, or an external minimization oracle.
\end{theorem}
\begin{proof}
Project the accepted carrier. The analytic data visible to a center consumer are precisely the metric source $M$, the complete-metric witness $K$, the regular Cauchy source family $A$, the common finite window $W$, the dyadic tolerance ledger $D$, the regular rational readback row $R$, the candidate row $E$, and the comparison row $T$.

The NameCert rows $H,C,P,S,N$ add transport, replay, provenance, terminal sealing, and local naming for that same finite packet. They do not add an independent equality coordinate, because every transported read is componentwise and every replay follows the displayed route through $W,D,R,T$.

It remains to exclude hidden completion principles. The carrier lists no quotient-completion row, no selected limit row, no compactness or convexity row, and no external minimizer oracle. Thus the local certificate exhausts exactly the finite asymptotic-radius route displayed in the carrier.
\end{proof}

\begin{theorem}[Regular Cauchy asymptotic-center Real-seal nonescape]
\label{thm:regular-cauchy-asymptotic-center-real-seal}
Every $\RealUp$-facing read exported by an accepted $\RegularCauchyAsymptoticCenterUp$ carrier factors through
$$
\MetricUp,\CompleteMetricUp,\StreamNameUp,\DyadicRatCoreUp,\RegSeqRatUp,\RealUp,\BHist,\hsame,\Cont,\Pkg,\NameCert
$$
along the rows $M,K,W,D,R,S,H,C,P,N$ before the candidate-center row $E$ or comparison row $T$ may be consumed. Consequently the terminal seal reports only the displayed regular Cauchy asymptotic-radius packet and cannot be used to derive quotient equality of names, selected completion limits, compactness, uniform convexity, countable choice, trichotomy, or a global minimizer principle.
\end{theorem}
\begin{proof}
Read the carrier order in \autoref{def:regular-cauchy-asymptotic-center-carrier}. The terminal seal row $S$ is downstream of the common stream window, dyadic tolerance ledger, regular rational readback, metric source, and complete-metric witness.

The comparison row $T$ is also downstream of those finite rows. A consumer therefore cannot ask the $\RealUp$ seal for an asymptotic-radius statement until the shared window $W$, tolerance ledger $D$, readback row $R$, and metric/complete-metric handoff $M,K$ have already been displayed.

The omitted principles are absent from the packet. Since $H$ and $C$ only transport and replay the listed rows, the seal cannot escape the finite route by manufacturing quotient equality, a selected limit, compactness, convexity, choice, trichotomy, or a minimizer outside the displayed data.
\end{proof}

\closureat{RegularCauchyAsymptoticCenterUp}{seedStr}
\begin{closurestatus}{\RegularCauchyAsymptoticCenterUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-asymptotic-center-carrier}, \autoref{thm:regular-cauchy-asymptotic-center-namecert-obligations}, and \autoref{thm:regular-cauchy-asymptotic-center-real-seal} seed the finite asymptotic-center route over displayed $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\MetricUp$, and $\CompleteMetricUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, selected limits, compactness, uniform convexity, countable choice, trichotomy, or global minimizer existence.}
  \upgradepath{Obligation closure requires checked carrier admission, common-window exposure, dyadic tolerance budgeting, regular rational readback, metric-radius comparison, complete-metric handoff, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyAsymptoticCenterUp.}
  \constructivestory{A $\RegularCauchyAsymptoticCenterUp$ packet is generated from a Metric source row, a CompleteMetric witness row, a regular Cauchy source family, one common StreamName window, a DyadicRatCore tolerance ledger, a RegSeqRat readback row, a candidate-center row, a finite asymptotic-radius comparison row, componentwise hsame transport, displayed Cont replay, Pkg provenance, a RealUp seal, and a local NameCert row.}
\end{closurestatus}
