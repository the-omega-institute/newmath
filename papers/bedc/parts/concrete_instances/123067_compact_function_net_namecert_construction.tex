\chapter{A Concrete Naming Certificate for $\CompactFunctionNetUp$}
\label{ch:concrete-instances-compact-function-net-namecert}
\origin{human}

Following Bishop--Bridges constructive compactness and the finite-net reading of Arzela--Ascoli, the $\CompactFunctionNetUp$ packet realizes the compact-uniform-continuity branch in which compact source nets and uniform-modulus rows are folded into a finite function-net surface. It refines the compact function-space carrier of \autoref{ch:concrete-instances-compact-function-space-namecert} toward the finite ProbeBundle route of \autoref{ch:visions-compact-uniform-continuity}: the packet names source nets, graph-family windows, tolerance-only moduli, Real readbacks, and local NameCert data without selecting function representatives.

\begin{definition}[CompactFunctionNet carrier]
\label{def:compact-function-net-carrier}
A $\CompactFunctionNetUp$ carrier is a finite $\BHist$ packet
$$
E=(X,F,M,R,V,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ source-net row, including the finite compact-net probes consumed by the route;
\item $F$ is the $\ContinuousMapUp$ graph-family row attached to those probes;
\item $M$ is the tolerance-only $\UniformModulusUp$ row obtained from the compact-net and graph-family data;
\item $R$ is the $\RealUp$ value and distance-read seal served to downstream consumers;
\item $V$ is the $\RegSeqRatUp$ readback row for finite value windows;
\item $U$ is the $\StreamNameUp$ schedule row for source probes and graph reads;
\item $H$ records componentwise $\hsame$ transport for $X,F,M,R,V,U$ and for the structural rows;
\item $C$ records displayed $\Cont$ replay from compact source nets to graph-family reads, modulus rows, stream windows, regular-rational readback, and Real seals;
\item $P$ is the $\Pkg$ provenance over compact metric nets, continuous-map graphs, uniform-modulus handoff, Real readback, RegSeqRat, StreamName, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CompactFunctionNetUp}$ row.
\end{enumerate}
The carrier contains no selected function representative, no quotient function equality, no infinite net object, no compactness by choice, and no ambient function-space compactness theorem.
\end{definition}

\begin{theorem}[CompactFunctionNet vision realization]
\label{thm:compact-function-net-vision-realization}
Every accepted $\CompactFunctionNetUp$ packet realizes the finite ProbeBundle fold route of compact uniform continuity by routing compact source-net probes through $X$, graph-family reads through $F$, tolerance-only uniform-modulus data through $M$, and terminal readback through $U,V,R$ with $H,C,P,N$ visible. Thus the concrete packet realizes the finite compact-net and uniform-modulus branch of \autoref{ch:visions-compact-uniform-continuity} without selected function representatives, quotient function-space equality, compactness by choice, or a host Arzela--Ascoli compactness theorem.
\end{theorem}
\begin{proof}
Project the carrier. The finite source-net row $X$ supplies the compact probes, and the graph-family row $F$ records the displayed continuous-map reads attached to those probes.

Read the uniform-modulus row $M$ after the compact-net and graph rows. This is the tolerance-only handoff produced by the compact-uniform-continuity route, not a selected external modulus for a host family of functions.

Finally expose $U,V,R$ with $H,C,P,N$. The StreamName, RegSeqRat, Real, transport, replay, provenance, and local naming rows are finite packet data, so the route realizes the ProbeBundle fold branch without quotient functions, selected representatives, or choice-based compactness.
\end{proof}

\closureat{CompactFunctionNetUp}{seedStr}

\begin{closurestatus}{\CompactFunctionNetUp}
  \constructivestory{A $\CompactFunctionNetUp$ packet is generated from compact metric source-net probes, continuous-map graph-family rows, a tolerance-only UniformModulus handoff, StreamName schedules, RegSeqRat readback, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter binds the finite function-net carrier and its compact-uniform-continuity realization route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected function representatives, quotient function equality, infinite function-net objects, compactness by choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact source-net rows, graph-family visibility, tolerance-only UniformModulus handoff, readback rows, transport, provenance, and local naming.}
\end{closurestatus}
