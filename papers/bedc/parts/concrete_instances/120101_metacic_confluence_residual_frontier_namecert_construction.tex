\chapter{A Concrete Naming Certificate for $\MetacicConfluenceResidualFrontierUp$}
\label{ch:concrete-instances-metacic-confluence-residual-frontier-namecert}
\origin{ai}

The $\MetacicConfluenceResidualFrontierUp$ packet realizes the MetaCIC confluence audit map of \autoref{ch:metacic-confluence-audit-map} as finite retained-premise, candidate-join, obstruction, and closed-fragment NameCert data. It sits after the typed-window and redex-frontier packets of \autoref{ch:concrete-instances-metacic-typed-parallel-diamond-boundary-namecert} and \autoref{ch:concrete-instances-metacic-redex-frontier-namecert}, the local-join ledgers of \autoref{ch:concrete-instances-metacic-local-join-ledger-namecert}, the residual checker and envelope of \autoref{ch:concrete-instances-metacic-confluence-residual-checker-namecert} and \autoref{ch:concrete-instances-metacic-confluence-residual-envelope-namecert}, and the closed-normal confluence seal of \autoref{ch:concrete-instances-closednormalconfluenceseal-namecert}. The packet keeps the substitution-compatibility boundary visible; it does not claim full Church--Rosser, arbitrary closed-term confluence, or discharge of residual substitution.

\begin{definition}[MetaCIC confluence residual frontier carrier]
\label{def:metacic-confluence-residual-frontier-carrier}
A $\MetacicConfluenceResidualFrontierUp$ carrier is a finite $\BHist$ packet
$$
F=(T,R,J,O,D,S,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed MetaCIC source-window row naming the closed or open term shape under inspection.
\item $R$ is the residual frontier row, recording the beta redex positions and residual-substitution sites that remain visible to the audit map.
\item $J$ is the finite candidate-join row for the inspected local peak.
\item $O$ is the obstruction-retention row naming the residual-substitution compatibility input when the candidate join cannot be closed.
\item $D$ is the closed-fragment discharge row for normal, atom, or other shape-limited cases already available in the confluence substrate.
\item $S$ is the substitution-boundary handoff row, kept as a premise boundary rather than erased into a global theorem.
\item $A$ is the audit-map readback row tying $T,R,J,O,D,S$ to the confluence audit map.
\item $H$ records componentwise $\hsame$ transport for all displayed frontier rows and the local certificate row.
\item $C$ records displayed $\Cont$ replay from typed window through residual frontier, candidate join, obstruction retention, closed-fragment discharge, substitution boundary, and audit readback.
\item $P$ is the $\Pkg$ provenance row over $\MetacicConfluenceResidualFrontierUp$, MetaCIC reduction rows, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetacicConfluenceResidualFrontierUp}$ row.
\end{enumerate}
The carrier exports a finite confluence-frontier packet. It contains no coordinate for full parallel diamond discharge, arbitrary closed-star confluence, global Church--Rosser, or residual-substitution compatibility as a theorem.
\end{definition}

\begin{theorem}[MetaCIC confluence residual frontier NameCert obligations]
\label{thm:metacic-confluence-residual-frontier-namecert-obligations}
For an accepted $\MetacicConfluenceResidualFrontierUp$ carrier $F=(T,R,J,O,D,S,A,H,C,P,N)$, the local $\NameCert_{\MetacicConfluenceResidualFrontierUp}$ surface consists exactly of typed-window visibility, residual-frontier visibility, candidate-join visibility, obstruction-retention visibility, closed-fragment discharge visibility, substitution-boundary handoff, audit-map readback, componentwise transport, displayed replay, package provenance, and local naming for the finite packet of \autoref{def:metacic-confluence-residual-frontier-carrier}.
\end{theorem}
\begin{proof}
Project the carrier. The confluence-facing rows are $T,R,J,O,D,S,A$, and the structural rows are $H,C,P,N$.

Read the typed source and residual frontier first. These rows identify the local peak and the residual-substitution sites that the audit map refuses to hide.

Read the candidate join and obstruction rows next. A closed fragment may discharge through $D$, while any remaining substitution compatibility demand stays named in $O$ and $S$.

Finally read the audit-map and structural rows. They preserve the finite frontier under transport, replay, provenance, and local naming, but they do not add a theorem proving full confluence or residual substitution compatibility.
\end{proof}

\begin{theorem}[MetaCIC confluence residual frontier vision handoff]
\label{thm:metacic-confluence-residual-frontier-vision-handoff}
Every manuscript citation of $\MetacicConfluenceResidualFrontierUp$ must read the confluence audit map through the displayed finite route
$$
T,\ R,\ J,\ O,\ D,\ S,\ A,\ H,\ C,\ P,\ N .
$$
Closed normal and atom fragments may be consumed through $D$, and conditional parallel-diamond or closed-star confluence handoffs may be recorded through $S$. The route does not turn the residual-substitution boundary into a discharged theorem, and it does not assert full Church--Rosser or arbitrary closed-term confluence for $\mathrm{BetaStarStep}$.
\end{theorem}
\begin{proof}
Start from the carrier inventory. The only rows that speak about confluence are the typed window, residual frontier, candidate join, obstruction, closed-fragment discharge, substitution-boundary handoff, and audit readback rows.

Apply the NameCert obligations. They require the obstruction and substitution-boundary rows to remain displayed whenever the candidate join is not a shape-limited closed fragment.

The closed-fragment row may be consumed for the normal and atom cases isolated by the audit map. It does not rewrite the retained substitution boundary into a global parallel diamond.

Thus the packet is a finite handoff from the vision audit map to concrete NameCert data. Any future full confluence theorem must supply separate residual-substitution discharge before this frontier can be read as a closed Church--Rosser proof.
\end{proof}

The future TasteGate surface for $\MetacicConfluenceResidualFrontierUp$ must provide explicit $\mathsf{Nontrivial}\ \MetacicConfluenceResidualFrontierUp$ and $\mathsf{FieldFaithful}\ \MetacicConfluenceResidualFrontierUp$ instances. Nontriviality must separate a packet with retained residual-substitution obstruction from a packet claiming discharge, and field faithfulness must preserve $T,R,J,O,D,S,A,H,C,P,N$ as distinct rows.

\falsifiablePrediction{Every accepted $\MetacicConfluenceResidualFrontierUp$ read keeps a typed source window, residual frontier, candidate join, obstruction-retention row, closed-fragment row, substitution-boundary handoff, audit readback, hsame transport, Cont replay, Pkg provenance, and NameCert row visible before any confluence consumer cites it.}

\independenceWitness{metacic-confluence-audit-map, metacic-residual-checker, metacic-confluence-residual-envelope, closednormalconfluenceseal, metacic-local-join-ledger: none is bijective with $\MetacicConfluenceResidualFrontierUp$ because the packet jointly stores retained premises, candidate joins, obstruction rows, closed-fragment discharge, substitution-boundary handoff, audit readback, transport, replay, provenance, and local naming.}

\closureat{MetacicConfluenceResidualFrontierUp}{seedStr}

\begin{closurestatus}{\MetacicConfluenceResidualFrontierUp}
  \constructivestory{A $\MetacicConfluenceResidualFrontierUp$ packet is generated from a typed source window, residual frontier, candidate join, obstruction-retention row, closed-fragment discharge row, substitution-boundary handoff, audit-map readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-confluence-residual-frontier-carrier}, \autoref{thm:metacic-confluence-residual-frontier-namecert-obligations}, \autoref{thm:metacic-confluence-residual-frontier-vision-handoff}, the falsifiable prediction, and the independence witness seed the finite residual-frontier packet for the MetaCIC confluence audit boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full parallel diamond discharge, arbitrary closed-star confluence, global Church--Rosser, residual-substitution compatibility as a theorem, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure asks for checked typed-window visibility, residual-frontier visibility, candidate-join visibility, obstruction retention, closed-fragment discharge, substitution-boundary handoff, audit-map readback, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming.}
\end{closurestatus}
