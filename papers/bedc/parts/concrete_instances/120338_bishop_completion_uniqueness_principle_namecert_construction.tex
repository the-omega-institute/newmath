\chapter{A Concrete Naming Certificate for $\BishopCompletionUniquenessPrincipleUp$}
\label{ch:concrete-instances-bishop-completion-uniqueness-principle-namecert}
\origin{human}

Following Bishop and Bridges constructive completion theory, the $\BishopCompletionUniquenessPrincipleUp$ packet records the textbook uniqueness principle for completion presentations as finite BEDC NameCert data. It sits after the criterion surface of \autoref{ch:concrete-instances-bishop-completion-criterion-namecert}, the located uniqueness boundary of \autoref{ch:concrete-instances-located-completion-uniqueness-namecert}, the regular Cauchy completion uniqueness route of \autoref{ch:concrete-instances-regular-cauchy-completion-uniqueness-namecert}, and the regular Cauchy representative uniqueness route of \autoref{ch:concrete-instances-regular-cauchy-uniqueness-namecert}. The packet gives L10 Real and Completion consumers one finite uniqueness surface; it does not assert quotient completion equality, selected representatives, countable choice, host equality of completed points, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop completion uniqueness principle carrier]
\label{def:bishop-completion-uniqueness-principle-carrier}
A $\BishopCompletionUniquenessPrincipleUp$ carrier is a finite $\BHist$ packet
$$
U=(B,L,C,R,S,Q,D,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BishopCompletionCriterionUp$ row exposing the completion-admission criterion before any uniqueness read is available.
\item $L$ is the $\LocatedCompletionUniquenessUp$ row recording the located uniqueness comparison for the same displayed completion source.
\item $C$ is the $\RegularCauchyCompletionUniquenessUp$ row recording the regular Cauchy completion uniqueness route.
\item $R$ is the $\RegularCauchyUniquenessUp$ row comparing representative regular Cauchy names before the terminal seal is consumed.
\item $S$ is the paired $\StreamNameUp$ finite-window row inspected by the criterion and both uniqueness routes.
\item $Q$ is the paired $\RegSeqRatUp$ regular rational readback row extracted from those finite windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the displayed comparison routes.
\item $E$ is the terminal $\RealUp$ seal row reached only after $B,L,C,R,S,Q,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the criterion, located uniqueness, regular completion uniqueness, representative uniqueness, window, readback, tolerance, seal, continuation, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a completion uniqueness request back through $B,L,C,R,S,Q,D,E$.
\item $P$ is the $\Pkg$ provenance over $\BishopCompletionUniquenessPrincipleUp$, $\BishopCompletionCriterionUp$, $\LocatedCompletionUniquenessUp$, $\RegularCauchyCompletionUniquenessUp$, $\RegularCauchyUniquenessUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCompletionUniquenessPrincipleUp}$ row naming the displayed principle and its permitted completion consumers.
\end{enumerate}
The classifier compares accepted carriers only through these rows and their replay paths. It has no coordinate for quotient completion equality, selected limits, countable choice, host equality, proof-irrelevant representative collapse, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop completion uniqueness principle NameCert obligations]
\label{thm:bishop-completion-uniqueness-principle-namecert-obligations}
For every accepted $\BishopCompletionUniquenessPrincipleUp$ carrier
$$
U=(B,L,C,R,S,Q,D,E,H,K,P,N),
$$
the local $\NameCert_{\BishopCompletionUniquenessPrincipleUp}$ surface consists exactly of criterion admission through $B$, located uniqueness exposure through $L$, regular Cauchy completion uniqueness exposure through $C$, representative regular Cauchy uniqueness exposure through $R$, finite StreamName window admission through $S$, RegSeqRat readback through $Q$, DyadicRatCore tolerance comparison through $D$, Real seal nonescape through $E$, componentwise hsame transport through $H$, displayed Cont replay through $K$, Pkg provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-completion-uniqueness-principle-carrier}. The analytic rows are exactly $B,L,C,R,S,Q,D,E$, and the structural rows are $H,K,P,N$.

The completion-admission criterion is read first. Row $B$ supplies the Bishop completion criterion, so a uniqueness-facing consumer cannot enter the completion boundary without the displayed finite criterion surface.

The uniqueness comparison is then forced through the three displayed sibling routes. Row $L$ supplies located completion uniqueness, row $C$ supplies regular Cauchy completion uniqueness, and row $R$ supplies regular Cauchy representative uniqueness over the same finite source.

The finite source rows bind those sibling routes to concrete data. Rows $S,Q,D$ expose the StreamName windows, RegSeqRat readback, and DyadicRatCore tolerance ledger used by the criterion and the uniqueness comparisons before the terminal Real seal $E$ is available.

Finally $H,K,P,N$ transport, replay, source, and name the same route. Since the excluded quotient, selection, host-equality, choice, representative-collapse, and ambient-completeness objects have no carrier coordinate, none can be exported by the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop completion uniqueness principle finite route]
\label{thm:bishop-completion-uniqueness-principle-finite-route}
Every completion-facing uniqueness read exported by an accepted $\BishopCompletionUniquenessPrincipleUp$ carrier factors through the ordered finite route
$$
\BishopCompletionCriterionUp,\ \LocatedCompletionUniquenessUp,\ \RegularCauchyCompletionUniquenessUp,\ \RegularCauchyUniquenessUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp
\longmapsto
\RealUp,H,K,P,N .
$$
Thus the L10 Real and Completion consumer receives uniqueness only as a displayed NameCert route through the completion criterion, located uniqueness, regular Cauchy completion uniqueness, representative uniqueness, finite windows, regular rational readback, and dyadic tolerance data.
\end{theorem}
\begin{proof}
Use \autoref{thm:bishop-completion-uniqueness-principle-namecert-obligations} to identify the complete local obligation surface. Any exported uniqueness read must consume $B,L,C,R,S,Q,D$ before the seal row $E$ is visible.

The first four rows bind the principle to existing completion uniqueness packets. The criterion row $B$ admits the completion-facing read, while $L,C,R$ record the located, regular completion, and representative uniqueness routes over the same displayed source.

The finite source rows prevent escape from the displayed route. Rows $S,Q,D$ expose the finite windows, regular rational readback, and dyadic tolerance ledger used by the sibling uniqueness routes.

The terminal rows add no new analytic object. Row $E$ is only a Real seal, and $H,K,P,N$ only transport, replay, source, and name the route, so the exported read cannot become quotient completion equality, a selected limit, countable choice, host equality, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopCompletionUniquenessPrincipleUp$ uniqueness read must expose $\BishopCompletionCriterionUp$, $\LocatedCompletionUniquenessUp$, $\RegularCauchyCompletionUniquenessUp$, $\RegularCauchyUniquenessUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.}

\independenceWitness{bishopcompletioncriterion, locatedcompletionuniqueness, regularcauchycompletionuniqueness, regularcauchyuniqueness, streamname, regseqrat, dyadicratcore, real: $\BishopCompletionUniquenessPrincipleUp$ is not bijective to any listed sibling because it jointly carries the completion criterion, located uniqueness, regular completion uniqueness, representative uniqueness, finite windows, regular rational readback, dyadic tolerance ledger, Real seal, transport, replay, provenance, and local naming while refusing quotient completion equality and ambient completeness.}

\closureat{BishopCompletionUniquenessPrincipleUp}{seedStr}

\begin{closurestatus}{\BishopCompletionUniquenessPrincipleUp}
  \constructivestory{A $\BishopCompletionUniquenessPrincipleUp$ packet is generated from a BishopCompletionCriterion row, LocatedCompletionUniqueness row, RegularCauchyCompletionUniqueness row, RegularCauchyUniqueness row, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance ledger, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-completion-uniqueness-principle-carrier}, \autoref{thm:bishop-completion-uniqueness-principle-namecert-obligations}, and \autoref{thm:bishop-completion-uniqueness-principle-finite-route} seed the finite L10 completion uniqueness principle over BishopCompletionCriterion, LocatedCompletionUniqueness, RegularCauchyCompletionUniqueness, RegularCauchyUniqueness, StreamName, RegSeqRat, DyadicRatCore, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected limits, countable choice, host equality of completed points, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sibling uniqueness exposure, finite StreamName windows, RegSeqRat readback, DyadicRatCore tolerance comparison, Real seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
