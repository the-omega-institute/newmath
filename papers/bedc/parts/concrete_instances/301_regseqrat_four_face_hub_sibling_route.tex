\begin{theorem}[RegSeqRat four-face hub sibling route]
\label{thm:regseqrat-four-face-hub-sibling-route}
Every accepted $\RegSeqRatUp$ terminal handoff used by current L10 consumers factors through the sibling chapters \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-real-namecert} before it is read as a Real-facing route. The route is the finite four-face inventory consisting of the $\DyadicRatCoreUp$ endpoint, radius, and distance ledgers, the $\StreamNameUp$ scheduled-window rows, the $\RegSeqRatUp$ regularity and classifier rows, and the terminal $\RealUp$ seal row.

The handoff exports no quotient stream equality, selected host limit, host real equality, ambient completeness datum, metric-completion point, or additional Real seal coordinate.
\end{theorem}
\begin{proof}
Start with \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. It identifies the public handoff route as the ordered passage through $\StreamNameUp$ scheduled windows, $\DyadicRatCoreUp$ endpoint and radius ledgers, the $\RegSeqRatUp$ classifier surface, and the terminal $\RealUp$ seal.

Next apply \autoref{thm:regseqrat-four-face-terminal-pullback}. The terminal read is one finite pullback with exactly the DyadicRatCore face, StreamName face, RegSeqRat face, and Real seal face, transported by the displayed $\BHist$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ rows.

Finally use \autoref{thm:regseqrat-terminal-four-face-correspondence}. Agreement of terminal reads is detected only on that four-face inventory, so the hub route is a substantive sibling route through the named DyadicRatCore, StreamName, and Real chapters rather than an isolated local theorem. The cited exclusion clauses leave no quotient stream equality, selected host limit, host equality, ambient completeness datum, metric-completion point, or extra seal coordinate to export.
\end{proof}
