\begin{theorem}[Differential-form NameCert obligation surface]
\label{thm:diffform-namecert-obligation-surface}
The paper-side $\NameCert_{\DiffFormUp}$ obligation surface consists exactly
of the carrier of \autoref{def:diffform-bhist-form-carrier}, the classifier
of \autoref{def:diffform-bhist-form-classifier}, carrier transport,
classifier stability, wedge ledger coverage, the wedge associator ledger, the
wedge probe concatenation ledger, wedge probe concatenation classifier
stability, the wedge degree ledger, wedge degree classifier stability, the
exterior-derivative candidate ledger, its degree-raise and wedge-input
stability rows, the antisymmetry permutation ledger, the de Rham input boundary of
\autoref{thm:diffform-derham-input-boundary}, and the downstream scope row of
\autoref{thm:diffform-exterior-derivative-downstream-scope}. It asserts no
standard bridge beyond these BEDC rows and no identity $d^2=0$, closedness,
nondegeneracy, or contact nonintegrability.
\end{theorem}
\leanchecked{BEDC.Derived.DiffFormUp.DiffFormExteriorDerivativeLedger\_semantic\_name\_certificate}
\begin{proof}
The carrier and classifier fields are the two displayed definitions. Carrier
transport is \autoref{thm:diffform-carrier-hsame-transport}, and classifier
stability is \autoref{thm:diffform-form-classifier-stability}.

The multiplicative operation exposed by the surface is the wedge row, whose
admission and stability are exactly the visible tensor-concatenation statement
of \autoref{thm:diffform-wedge-ledger-coverage}. Its output probe spine is the
visible concatenation ledger of
\autoref{def:diffform-wedge-probe-concatenation-ledger}, with coverage and
transport supplied by
\autoref{thm:diffform-wedge-probe-concatenation-coverage} and
\autoref{thm:diffform-wedge-probe-concatenation-classifier-stability}. Its
associativity obligation is the visible ledger of
\autoref{def:diffform-wedge-associator-ledger} and the closure statement of
\autoref{thm:diffform-wedge-associator-closure}. Its degree bookkeeping is the
visible unary-history ledger of \autoref{def:diffform-wedge-degree-ledger},
and classifier stability for that ledger is
\autoref{thm:diffform-wedge-degree-classifier-stability}.
Antisymmetry is not hidden in the wedge operation: it is the explicit
permutation ledger retained in every carrier row.

The exterior-derivative candidate interface is the visible package of
\autoref{def:diffform-exterior-derivative-candidate-ledger}. Its degree
bookkeeping is
\autoref{thm:diffform-exterior-derivative-degree-raise-ledger}, and its
transport along the wedge-input classifier is
\autoref{thm:diffform-exterior-derivative-wedge-input-stability}.

The remaining exported row is the downstream scope for exterior derivative
consumers, bounded by \autoref{thm:diffform-derham-input-boundary}. Since all
fields are stated with $BHist$, $\hsame$, $\Cont$, $\mathsf{ProbeBundle}$,
$\NameCert_{\ManifoldUp}$, and $\NameCert_{\TensorProductUp}$, this is a BEDC
obligation surface and not a claim of equivalence with a standard
differential-form object.
\end{proof}

\closureat{DiffFormUp}{matureStr}[BEDC.Derived.DiffFormUp.DiffFormExteriorDerivativeLedger\_semantic\_name\_certificate]
\begin{closurestatus}{\DiffFormUp}
  \constructivestory{A differential form is generated as a six-coordinate $\mathsf{BHist}$ row over fixed $\ManifoldUp$ and $\TensorProductUp$ sources: a unary degree, a finite tangent-probe $\mathsf{ProbeBundle}$, a multilinear tensor row, a scalar endpoint, an antisymmetry permutation ledger, and a source-provenance ledger. The carrier accepts exactly those rows whose probes are carried by the manifold source, whose tensor and scalar endpoints are certified by the tensor and scalar sources, and whose antisymmetry and provenance are displayed; any hidden alternating-map object, quotient, closedness predicate, or operator identity is excluded. Two carried forms are classified by degree $\hsame$, matched tensor and scalar comparisons through retained source classifiers, and visible $\hsame$ on the antisymmetry, tensor, and provenance ledgers. The source is the manifold-plus-tensor certificate surface, the pattern records degree/probe/tensor/scalar/antisymmetry shape together with wedge and exterior-input routing, and the ledger keeps wedge concatenation, degree bookkeeping, permutation rows, and exterior-derivative candidate inputs visible. The five name-certificate obligations are supplied by a displayed carried form for inhabitation, by componentwise reflexivity, symmetry, and transitivity of $\hsame$ and the retained scalar classifier, and by carrier and classifier transport for closure under recognized representatives. A row $h$ lies in the signature gap of a $\DiffFormUp$ token $p$ when the source-domain accepts its six-coordinate form data, the signature relation pairs it with an answer form and evidence ledger, and the token-introduction relation names that answer as $p$; unfolded, $p$ records the same degree, probe, tensor, scalar, antisymmetry, wedge, and exterior-input rows. The chapter registers this displayed form surface under $\DiffFormUp$; downstream de Rham, symplectic, and contact consumers may read only these BEDC ledgers, with no $d^2=0$, closedness, nondegeneracy, contact nonintegrability, or standard bridge introduced here.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:diffform-namecert-obligation-surface} is the paper-side scope for the displayed BHist, hsame, Cont, ProbeBundle, ManifoldUp, TensorProductUp, wedge-ledger, exterior-derivative input, and downstream boundary rows.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.DiffFormUp.DiffFormExteriorDerivativeLedger\_semantic\_name\_certificate}
  \bridgestatus{bridgeChecked}
  \notclaimed{This chapter does not close $d^2=0$, closedness, nondegeneracy, contact nonintegrability, or standard bridge closure.}
  \upgradepath{No further upgrade -- chapter at top of the formal axis.}
\end{closurestatus}
