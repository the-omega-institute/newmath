\chapter{A Concrete Naming Certificate for $\RealLogarithmUp$}
\label{ch:concrete-instances-real-logarithm-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RealLogarithmUp$ packet names logarithm on positive real data as a finite inverse-route certificate for $\RealExponentialUp$. The construction records located positive brackets, dyadic interval refinement, exponential comparison rows, and real-equality endpoint checks; it does not import a host logarithm, analytic continuation, quotient real equality, selected infinite bisection branch, countable choice, or ambient $\RealUp$ completeness.

The inverse-route packet consumes the exponential comparison surface of \autoref{ch:concrete-instances-real-exponential-namecert}, the displayed positive-domain row of \autoref{ch:concrete-instances-located-positive-real-namecert}, the bracket row of \autoref{ch:concrete-instances-open-interval-real-namecert}, the error-control row of \autoref{ch:concrete-instances-real-abs-namecert}, and the comparison classifier of \autoref{ch:concrete-instances-bishoprealcomparison-namecert}. These are the displayed positive-domain, bracket, comparison, and error-control rows used before the endpoint classifier is read.

\begin{definition}[RealLogarithm carrier]
\label{def:real-logarithm-carrier}
A $\RealLogarithmUp$ carrier is a finite $\BHist$ packet
$$
L=(P,I,B,E,A,O,C,R,H,K,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\LocatedPositiveRealUp$ row naming the positive input real and its located apartness from $0$.
\item $I$ is the $\OpenIntervalRealUp$ row carrying the positive bracket interval on which the inverse search is performed.
\item $B$ is the finite dyadic bracket ledger, with each row recording a rational interval, midpoint comparison request, and retained positive subinterval.
\item $E$ is the $\RealExponentialUp$ inverse-route row used to compare displayed exponentials against the positive input.
\item $A$ is the $\RealAbsUp$ control row bounding comparison error and bracket width during the finite read.
\item $O$ is the $\BishopRealComparisonUp$ row that classifies the displayed real comparisons used by the bracket ledger.
\item $C$ is the displayed $\Cont$ route from positive input and bracket rows through exponential comparisons to the endpoint classifier.
\item $R$ is the real classifier row, read through $\hsame$ transport and the displayed comparison rows, that identifies the logarithm endpoint with the inverse route.
\item $H$ is the componentwise $\hsame$ transport row for $P,I,B,E,A,O,C,R,K,N$.
\item $K$ is the $\Pkg$ provenance over $\RealLogarithmUp$, $\LocatedPositiveRealUp$, $\OpenIntervalRealUp$, $\RealExponentialUp$, $\RealAbsUp$, $\BishopRealComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealLogarithmUp}$ row.
\end{enumerate}
The classifier accepts only packets whose positive input, bracket ledger, exponential inverse-route rows, absolute-error controls, Bishop comparison rows, transport, continuation, provenance, and local naming are all displayed. It has no coordinate for a host logarithm or for a hidden infinite choice of bisection branch.
\end{definition}

\begin{theorem}[RealLogarithm NameCert obligations]
\label{thm:real-logarithm-namecert-obligations}
For every accepted $\RealLogarithmUp$ carrier $L=(P,I,B,E,A,O,C,R,H,K,N)$, the local $\NameCert_{\RealLogarithmUp}$ surface consists exactly of carrier exposure for the positive real row, the open positive interval, the dyadic bracket ledger, the $\RealExponentialUp$ inverse-route comparisons, the $\RealAbsUp$ error controls, the $\BishopRealComparisonUp$ classifier rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local naming row. Any public logarithm read must pass through those rows before it can consume an endpoint value.
\end{theorem}
\begin{proof}
Project the accepted carrier to the displayed rows $P,I,B,E,A,O,C,R,H,K,N$. The positivity and interval rows give the only admissible domain data, while the dyadic ledger records the finite bracket observations that a consumer may inspect.

The inverse comparisons are then forced through $E,A,O$. The exponential row supplies the only comparison with the candidate endpoint, the absolute-value row supplies the displayed error control, and the Bishop comparison row classifies the retained side of the bracket.

Finally, $H,C,K,N$ preserve the same rows under transport, replay, provenance, and naming. Since no coordinate stores a host logarithm, an analytic-continuation datum, or a selected infinite branch, the local NameCert surface is exactly the displayed inverse-route surface.
\end{proof}

\begin{theorem}[RealLogarithm exponential inverse route]
\label{thm:real-logarithm-exponential-inverse-route}
Every public read of an accepted $\RealLogarithmUp$ packet factors through the positive $\RealUp$ input row, the located open bracket row, the finite dyadic bracket ledger, the $\RealExponentialUp$ comparison row, the $\RealAbsUp$ error-control row, and the $\BishopRealComparisonUp$ classifier row before the logarithm endpoint is consumed. Consequently the packet names logarithm as the constructive inverse route of exponential on positive real data, not as a host transcendental primitive.
\end{theorem}
\begin{proof}
Start with a public logarithm read. The carrier gives the positive input row $P$ and the bracket row $I$, so the read begins inside a located positive real interval rather than in an ambient total real function space.

The finite bracket ledger $B$ supplies the inspected dyadic intervals. Each retained bracket is justified only by the exponential comparison row $E$, the absolute-error row $A$, and the Bishop comparison row $O$.

The endpoint read is then routed through $C$ and $R$, with $H,K,N$ preserving the same displayed packet under transport, provenance, and naming. Thus the logarithm value is consumed only after the exponential inverse route has been displayed, and no hidden host $\log$ or selected infinite branch is exported.
\end{proof}

\begin{theorem}[RealLogarithm positive-domain sibling route]
\label{thm:real-logarithm-positive-domain-sibling-route}
Every accepted $\RealLogarithmUp$ carrier first consumes the positive-domain sibling rows $\LocatedPositiveRealUp$ and $\OpenIntervalRealUp$, then passes through the $\RealExponentialUp$, $\RealAbsUp$, and $\BishopRealComparisonUp$ rows before endpoint classification is read through $\RealUp$. In the carrier notation of \autoref{def:real-logarithm-carrier}, every public logarithm consumer follows the route
$$
P,I \longmapsto B,E,A,O \longmapsto C,R \longmapsto H,K,N .
$$
Thus the positive-domain handoff is tied to the sibling chapters \autoref{ch:concrete-instances-located-positive-real-namecert}, \autoref{ch:concrete-instances-open-interval-real-namecert}, \autoref{ch:concrete-instances-real-exponential-namecert}, \autoref{ch:concrete-instances-real-abs-namecert}, \autoref{ch:concrete-instances-bishoprealcomparison-namecert}, and \autoref{ch:concrete-instances-real-namecert}. It exports no host logarithm, analytic continuation, selected infinite bisection branch, quotient real equality, or $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier
$$
L=(P,I,B,E,A,O,C,R,H,K,N)
$$
from \autoref{def:real-logarithm-carrier}. The first two coordinates are the located positive real row and the open positive interval row. Hence a public consumer cannot reach the bracket ledger or endpoint classifier without first reading the positive-domain data.

The next analytic rows are $B,E,A,O$. The finite bracket ledger records the inspected dyadic intervals, the exponential row supplies the inverse comparison, the absolute-value row supplies the displayed error control, and the Bishop comparison row classifies the retained side of each bracket.

Endpoint classification then passes through $C$ and $R$. The continuation row replays the route from positive input and bracket data to the endpoint classifier, while $R$ is the only real classifier coordinate in the packet.

Finally $H,K,N$ transport, source, and name the same finite rows. Because the carrier contains no coordinate for a host logarithm, analytic continuation, selected infinite bisection branch, quotient real equality, or $\RealUp$ completeness, none of those data can be exported by the sibling route.
\end{proof}

\falsifiablePrediction{Every $\RealLogarithmUp$ public read factors through a located positive real row, an open positive interval row, a dyadic bracket ledger, a RealExponential inverse-route row, a RealAbs error-control row, a BishopRealComparison classifier row, componentwise hsame transport, displayed continuation, package provenance, and local naming.}

\independenceWitness{real-exponential, real-analytic, located-positive-real, open-interval-real, real-abs, bishop-real-comparison: $\RealLogarithmUp$ is not bijective to any listed sibling because it jointly carries positive-domain admission, finite inverse brackets, exponential comparison rows, absolute-error controls, Bishop comparison classification, endpoint transport, continuation, provenance, and local naming.}

\closureat{RealLogarithmUp}{seedStr}

\begin{closurestatus}{\RealLogarithmUp}
  \constructivestory{A $\RealLogarithmUp$ packet is generated from a located positive real input, an open positive interval, finite dyadic bracket rows, RealExponential inverse-route comparisons, RealAbs error controls, BishopRealComparison classifier rows, componentwise transport, displayed continuation, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-logarithm-carrier}, \autoref{thm:real-logarithm-namecert-obligations}, and \autoref{thm:real-logarithm-exponential-inverse-route} seed the constructive inverse-route surface for logarithm on positive real data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host logarithms, analytic continuation, quotient real equality, selected infinite bisection branches, countable choice, ambient $\RealUp$ completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-domain exposure, bracket refinement, exponential inverse-route comparison, error-control, classifier nonescape, and TasteGate field faithfulness for BEDC.Derived.RealLogarithmUp.}
\end{closurestatus}
