\chapter{A Concrete Naming Certificate for $\CauchyNameModulusExtractionUp$}
\label{ch:concrete-instances-cauchy-name-modulus-extraction-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy representatives and diagonal limits, the $\CauchyNameModulusExtractionUp$ packet records the finite modulus-extraction route behind the regular-Cauchy diagonal-limit construction of \autoref{ch:visions-real-completeness}. It sits over the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the finite separation boundary of \autoref{ch:concrete-instances-finite-window-real-separation-namecert}. The packet names the finite extraction object used by a Cauchy name; it does not select a limit, quotient Cauchy names, invoke countable choice, or assert ambient $\RealUp$ completeness.

\begin{theorem}[Cauchy name modulus extraction carrier]
\label{thm:cauchy-name-modulus-extraction-vision-carrier}
A $\CauchyNameModulusExtractionUp$ carrier is a finite $\BHist$ packet
$$
X=(S,W,D,R,E,F,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the regular-Cauchy name source row, recording the sequence of regular rational names whose diagonal route is being inspected.
\item $W$ is the $\StreamNameUp$ finite-window row selecting the visible indices used by the diagonal readback.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the requested precision and comparison budget.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same finite window and dyadic tolerance data.
\item $E$ is the terminal $\RealUp$ seal row that receives the extracted Cauchy-name data.
\item $F$ is the $\FiniteWindowRealSeparationUp$ row that records the finite separation boundary used before a Real-facing equality or apartness read.
\item $M$ is the extracted modulus row, naming the diagonal stage bound visible to the Cauchy-name consumer.
\item $H$ records componentwise $\hsame$ transport for source, window, tolerance, readback, Real seal, finite separation, modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,W,D,R,F \longmapsto M \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\CauchyNameModulusExtractionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\FiniteWindowRealSeparationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyNameModulusExtractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,W,D,R,E,F,M,H,C,P,N$. It has no coordinate for a selected limit, quotient equality of Cauchy names, countable choice, host equality of Real representatives, or ambient $\RealUp$ completeness.
\end{theorem}

\begin{theorem}[Cauchy name modulus extraction NameCert obligations]
\label{thm:cauchy-name-modulus-extraction-namecert-obligations}
For every accepted $\CauchyNameModulusExtractionUp$ carrier
$$
X=(S,W,D,R,E,F,M,H,C,P,N),
$$
the local $\NameCert_{\CauchyNameModulusExtractionUp}$ surface consists exactly of regular-Cauchy source exposure through $S$, StreamName finite-window exposure through $W$, DyadicRatCore tolerance exposure through $D$, RegSeqRat readback through $R$, Real sealing through $E$, finite separation exposure through $F$, modulus exposure through $M$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{thm:cauchy-name-modulus-extraction-vision-carrier}. The rows $S,W,D,R,F$ expose the regular-Cauchy source, finite window, dyadic tolerance, regular readback, and finite separation boundary before any modulus row is exported.

The extracted modulus row $M$ is tied to that displayed diagonal window. It is not a chosen limit, a quotient representative, or a countable-choice schedule; it is the finite stage bound named for the inspected Cauchy-name route.

The terminal seal $E$ and structural rows $H,C,P,N$ preserve the same route for Real-facing consumers. Since no selected limit, quotient Cauchy-name equality, host real equality, or ambient completeness principle appears as a carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cauchy name diagonal modulus route]
\label{thm:cauchy-name-modulus-extraction-diagonal-route}
Every diagonal-limit consumer of an accepted $\CauchyNameModulusExtractionUp$ packet factors its read through the finite route
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \FiniteWindowRealSeparationUp
\longmapsto
M
\longmapsto
\RealUp .
$$
The route realizes the regular-Cauchy diagonal-limit modulus extraction as finite NameCert data, without quotient representatives, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchy-name-modulus-extraction-namecert-obligations}. It places the source, window, tolerance, readback, and finite separation rows before the extracted modulus row $M$.

The Real seal $E$ is downstream of the same row. A diagonal-limit consumer therefore receives a finite Cauchy-name extraction packet, not a detached limit object.

Finally $H,C,P,N$ transport, replay, source, and name the route. These rows keep the exported modulus inside the displayed BEDC surface and leave no coordinate for quotient equality, countable choice, host real equality, or ambient completeness.
\end{proof}

\closureat{CauchyNameModulusExtractionUp}{seedStr}

\begin{closurestatus}{\CauchyNameModulusExtractionUp}
  \constructivestory{A $\CauchyNameModulusExtractionUp$ packet is generated from a regular-Cauchy name source, StreamName finite windows, DyadicRatCore tolerances, RegSeqRat readback, a Real seal, a finite separation boundary, an extracted modulus row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{thm:cauchy-name-modulus-extraction-vision-carrier}, \autoref{thm:cauchy-name-modulus-extraction-namecert-obligations}, and \autoref{thm:cauchy-name-modulus-extraction-diagonal-route} seed the regular-Cauchy diagonal-limit modulus route over the DyadicRatCore, StreamName, RegSeqRat, Real, and FiniteWindowRealSeparation spine.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient equality of Cauchy names, countable choice, host equality of Real representatives, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source admission, finite-window readback, dyadic tolerance, regular readback, finite separation, Real sealing, modulus exposure, transport, replay, provenance, and local naming for BEDC.Derived.CauchyNameModulusExtractionUp.}
\end{closurestatus}
