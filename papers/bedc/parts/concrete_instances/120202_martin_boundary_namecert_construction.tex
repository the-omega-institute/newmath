\chapter{A Concrete Naming Certificate for $\MartinBoundaryUp$}
\label{ch:concrete-instances-martin-boundary-namecert}
\origin{human}

Following Martin and Doob on positive harmonic functions and boundary kernels, the $\MartinBoundaryUp$ packet records the finite transition-kernel boundary route used by hyperbolic observer-expansion consumers before any host compactification or quotient boundary equality is asserted. It sits beside the hyperbolic metric rows of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the Gromov boundary of \autoref{ch:concrete-instances-gromov-boundary-namecert}, the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the Poisson-kernel route of \autoref{ch:concrete-instances-hyperbolic-poisson-kernel-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite kernel, positive-harmonic, shadow, metric, Real-seal, transport, replay, provenance, and naming rows.

\begin{definition}[Martin boundary carrier]
\label{def:martin-boundary-carrier}
A $\MartinBoundaryUp$ carrier is a finite $\BHist$ packet
$$
M=(X,K,H,B,S,P_o,V,R,T,C,G,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ is the displayed state or basepoint window consumed by the transition-kernel reads.
\item $K$ is the finite transition-kernel ledger whose positive entries are inspected by the boundary packet.
\item $H$ is the positive-harmonic row read through the kernel ledger.
\item $B$ is the $\GromovBoundaryUp$ handoff row available to hyperbolic consumers.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ shadow row attached to the same boundary-facing read.
\item $P_o$ is the $\HyperbolicPoissonKernelUp$ row that turns the boundary and kernel data into finite Real-valued weights.
\item $V$ is the $\HyperbolicMetricUp$ metric route used by the shadow and kernel comparisons.
\item $R$ is the terminal $\RealUp$ seal row for finite kernel weights and normalized comparisons.
\item $T$ records componentwise $\hsame$ transport for the state, kernel, harmonic, boundary, shadow, Poisson-kernel, metric, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same finite Martin-boundary read.
\item $G$ records $\Pkg$ provenance over $\MartinBoundaryUp$, $\HyperbolicMetricUp$, $\GromovBoundaryUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicPoissonKernelUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\MartinBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,K,H,B,S,P_o,V,R,T,C,G,N$. It has no coordinate for a host compactification, quotient boundary equality, selected minimal harmonic function, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Martin boundary NameCert obligations]
\label{thm:martin-boundary-namecert-obligations}
For every accepted $\MartinBoundaryUp$ carrier $M=(X,K,H,B,S,P_o,V,R,T,C,G,N)$, the local $\NameCert_{\MartinBoundaryUp}$ surface consists exactly of carrier admission for the displayed state window, transition-kernel ledger, positive-harmonic row, Gromov-boundary handoff, boundary-shadow row, Poisson-kernel row, hyperbolic-metric route, Real seal, componentwise transport, displayed replay, provenance, and local naming. Each boundary-facing read must expose the kernel and positive-harmonic rows before it reaches the shadow, Poisson-kernel, metric, or Real-seal rows.
\end{theorem}
\begin{proof}
Project the carrier. The state window $X$, transition-kernel ledger $K$, and positive-harmonic row $H$ are the source rows for the Martin-boundary read.

The hyperbolic boundary surface is downstream. The rows $B,S,V$ expose the Gromov boundary, boundary shadow, and hyperbolic metric route, while $P_o$ reads the Poisson-kernel weight only after those boundary-facing rows are visible.

The terminal Real seal $R$ and the structural rows $T,C,G,N$ preserve the same finite route. Since the carrier contains no host compactification, quotient boundary equality, selected minimal harmonic function, choice schedule, or ambient completeness row, the listed obligations exhaust the local certificate.
\end{proof}

\begin{theorem}[Martin boundary shadow handoff]
\label{thm:martin-boundary-shadow-handoff}
Every hyperbolic observer-expansion consumer of an accepted $\MartinBoundaryUp$ packet must read the finite route
$$
K,H \longmapsto \GromovBoundaryUp,\HyperbolicBoundaryShadowUp,\HyperbolicPoissonKernelUp,\HyperbolicMetricUp,\RealUp
$$
through $X,K,H,B,S,P_o,V,R,T,C,G,N$. The handoff unifies kernel, positive-harmonic, shadow, Poisson-kernel, metric, and Real-seal rows without asserting a host compactification or equality with any quotient boundary.
\end{theorem}
\begin{proof}
Start from \autoref{def:martin-boundary-carrier}. The only boundary-producing source rows are the transition-kernel ledger $K$ and the positive-harmonic row $H$, attached to the displayed state window $X$.

Apply \autoref{thm:martin-boundary-namecert-obligations}. It forces the Gromov-boundary handoff $B$, boundary-shadow row $S$, Poisson-kernel row $P_o$, hyperbolic-metric row $V$, and terminal Real seal $R$ to be read as downstream rows of the same finite packet.

The transport, replay, provenance, and local naming rows $T,C,G,N$ keep the route componentwise. Therefore the observer-expansion consumer receives a finite Martin-boundary handoff, not a compactification theorem, quotient boundary equality, selected minimal harmonic function, countable-choice schedule, or ambient completeness principle.
\end{proof}

\falsifiablePrediction{Every accepted $\MartinBoundaryUp$ read is refuted if it reaches a boundary shadow, Poisson-kernel weight, hyperbolic metric comparison, or terminal Real seal without displaying the transition-kernel ledger, positive-harmonic row, Gromov-boundary handoff, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{hyperbolic-metric, gromov-boundary, hyperbolic-boundary-shadow, hyperbolic-poisson-kernel, real: $\MartinBoundaryUp$ is not bijective to any listed sibling because it jointly carries transition-kernel, positive-harmonic, boundary, shadow, Poisson-kernel, metric, Real-seal, transport, replay, provenance, and local naming rows.}

\closureat{MartinBoundaryUp}{seedStr}

\begin{closurestatus}{\MartinBoundaryUp}
  \constructivestory{A $\MartinBoundaryUp$ packet is generated from a state window, transition-kernel ledger, positive-harmonic row, GromovBoundaryUp handoff, HyperbolicBoundaryShadowUp shadow row, HyperbolicPoissonKernelUp row, HyperbolicMetricUp metric route, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:martin-boundary-carrier}, \autoref{thm:martin-boundary-namecert-obligations}, and \autoref{thm:martin-boundary-shadow-handoff} seed the finite Martin-boundary route for hyperbolic observer-expansion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close host compactification, quotient boundary equality, selected minimal harmonic functions, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, transition-kernel exposure, positive-harmonic routing, Gromov-boundary handoff, shadow handoff, Poisson-kernel weighting, hyperbolic-metric routing, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.MartinBoundaryUp.}
\end{closurestatus}
