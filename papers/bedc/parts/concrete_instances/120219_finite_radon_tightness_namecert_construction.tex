\chapter{A Concrete Naming Certificate for $\FiniteRadonTightnessUp$}
\label{ch:concrete-instances-finite-radon-tightness-namecert}
\origin{human}

Following Billingsley and Prokhorov, the packet records finite compact-window tightness evidence. It sits beside the probability source of \autoref{ch:concrete-instances-probspace-namecert}, the measure surface of \autoref{ch:concrete-instances-measure-namecert}, the compact metric test interface of \autoref{ch:concrete-instances-compactmetric-namecert}, the real comparison boundary of \autoref{ch:concrete-instances-real-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. It supplies a tightness row for the Prokhorov and Portmanteau consumers of \autoref{ch:concrete-instances-prokhorov-tight-probability-namecert} and \autoref{ch:concrete-instances-portmanteau-namecert} without importing host measures, countable choice, or selected compact sets.

\begin{definition}[Finite Radon tightness carrier]
\label{def:finite-radon-tightness-carrier}
A $\FiniteRadonTightnessUp$ carrier is a finite $\BHist$ packet
$$
T=(P,M,K,S,R,E,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\ProbSpaceUp$ source row whose probability context is visible before any outside-mass read.
\item $M$ is the $\MeasureUp$ event-surface row attached to the same probability context.
\item $K$ is the $\CompactMetricUp$ compact-window row used as the finite test window.
\item $S$ is the $\StreamNameUp$ row listing the inspected compact windows and tolerance requests.
\item $R$ is the $\RegSeqRatUp$ readback row for rational tolerance and outside-mass comparisons.
\item $E$ is the $\RealUp$ comparison seal for the displayed mass bounds.
\item $H$ records componentwise $\hsame$ transport for the probability, measure, compact-window, stream, readback, real, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay of the same finite tightness route.
\item $Q$ is the $\Pkg$ provenance over $\FiniteRadonTightnessUp$, $\ProbSpaceUp$, $\MeasureUp$, $\CompactMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteRadonTightnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host Radon measure, a chosen compact exhaustion, weak compactness, selected subsequences, quotient event equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Finite Radon tightness NameCert obligations]
\label{thm:finite-radon-tightness-namecert-obligations}
For every accepted $\FiniteRadonTightnessUp$ carrier
$$
T=(P,M,K,S,R,E,H,C,Q,N),
$$
the local $\NameCert_{\FiniteRadonTightnessUp}$ surface consists exactly of carrier admission for the probability source, measure event surface, compact metric window, finite stream schedule, regular rational tolerance readback, real comparison seal, componentwise transport, displayed replay, package provenance, and local naming. Every Prokhorov- or Portmanteau-facing tightness read therefore factors through $P,M,K,S,R,E,H,C,Q,N$ before a weak-convergence consumer can inspect it.

The packet supplies no host measure object, no selected compact set, no compact exhaustion, no weak compactness theorem, no countable-choice schedule, and no quotient equality of events.
\end{theorem}
\begin{proof}
Project the displayed carrier. The rows $P,M,K,S,R,E$ are the only probability, measure, compact-window, finite-schedule, rational-readback, and real-comparison coordinates available to a tightness consumer.

The route is finite. The stream row $S$ lists the inspected compact windows, the readback row $R$ carries the tolerance comparisons, and the real seal $E$ records the visible outside-mass bounds. This is the data later consumed by $\ProkhorovTightProbabilityUp$ and $\PortmanteauUp$.

The structural rows $H,C,Q,N$ transport, replay, source, and name exactly that route. Since the carrier contains no coordinate for host measures, selected compact sets, compact exhaustions, weak compactness, countable choice, or quotient event equality, no accepted read can export those objects.
\end{proof}

\falsifiablePrediction{Every accepted FiniteRadonTightnessUp consumer that reads a compact-window tightness bound without first displaying the probability, measure, compact-window, stream, readback, and real rows violates the NameCert obligations.}

\independenceWitness{Nearest siblings: ProbSpaceUp, MeasureUp, CompactMetricUp, ProkhorovTightProbabilityUp, and PortmanteauUp. None is bijective to FiniteRadonTightnessUp because each sibling omits at least one row among finite compact-window scheduling, rational outside-mass readback, and the local tightness NameCert.}

\closureat{FiniteRadonTightnessUp}{seedStr}

\begin{closurestatus}{\FiniteRadonTightnessUp}
  \constructivestory{A $\FiniteRadonTightnessUp$ packet is generated from probability source rows, measure event-surface rows, compact metric window rows, finite stream schedules, regular rational tolerance readbacks, real comparison seals, componentwise transport, displayed replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-radon-tightness-carrier}, \autoref{thm:finite-radon-tightness-namecert-obligations}, the falsifiable prediction, and the independence witness bind the finite compact-window tightness carrier and its immediate consumer route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host Radon measures, selected compact exhaustions, weak compactness, countable choice, quotient event equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-window exposure, outside-mass exactness, Prokhorov and Portmanteau consumer nonescape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteRadonTightnessUp.}
\end{closurestatus}
