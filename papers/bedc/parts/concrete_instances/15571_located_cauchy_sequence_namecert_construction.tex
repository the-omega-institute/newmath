\chapter{A Concrete Naming Certificate for $\LocatedCauchySequenceUp$}
\label{ch:concrete-instances-located-cauchy-sequence-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\LocatedCauchySequenceUp$ packet records the sequence-level data of a located Cauchy name before any quotient stream, selected limit, countable-choice selector, trichotomy branch, or ambient completeness principle is available. It sits after the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the located Cauchy source surface of \autoref{ch:concrete-instances-locatedcauchy-namecert}, the located real boundary of \autoref{ch:concrete-instances-locatedreal-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the regular located Cauchy real packet of \autoref{ch:concrete-instances-regular-located-cauchy-real-namecert}. The chapter packages finite sequence observations and located tail data; it does not assert that the sequence has a host-selected completed value.

\begin{definition}[Located Cauchy sequence carrier]\label{def:located-cauchy-sequence-carrier}
A $\LocatedCauchySequenceUp$ carrier is a finite $\BHist$ packet
$$
L=(S,Q,D,T,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ schedule row naming the finite observation windows of the sequence.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the windows in $S$.
\item $D$ is the $\DyadicRatCoreUp$ endpoint, radius, and tolerance ledger used to compare displayed sequence terms.
\item $T$ is the located Cauchy tail row, recording the finite threshold at which later displayed terms are accepted inside the requested dyadic tolerance.
\item $W$ is the located-window row inherited from \autoref{ch:concrete-instances-locatedcauchy-namecert}; it binds each tail request to the corresponding displayed located ball.
\item $E$ is the downstream seal row toward $\LocatedRealUp$, $\RealUp$, and $\RegularLocatedCauchyRealUp$, read only after $S,Q,D,T,W$ have been exposed.
\item $H$ records componentwise $\hsame$ transport for sequence windows, regular readback, dyadic tolerance data, tail rows, located windows, seal rows, continuation rows, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each sequence precision request through $S,Q,D,T,W,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LocatedCauchySequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\LocatedCauchyUp$, $\LocatedRealUp$, $\RealUp$, $\RegularLocatedCauchyRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCauchySequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the ten displayed rows. It has no coordinate for quotient stream equality, a selected host limit, countable choice, trichotomy, hidden infinite stream inspection, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located Cauchy sequence NameCert obligations]\label{thm:located-cauchy-sequence-namecert-obligations}
For every accepted $\LocatedCauchySequenceUp$ carrier $L=(S,Q,D,T,W,E,H,C,P,N)$, the seed $\NameCert_{\LocatedCauchySequenceUp}$ surface consists exactly of carrier admission for the ten displayed rows, finite-window exposure through $S$, regular rational readback through $Q$, dyadic tolerance exposure through $D$, located tail exposure through $T$, located-window compatibility through $W$, downstream seal ordering through $E$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-cauchy-sequence-carrier}. The visible analytic rows are precisely the stream schedule, regular readback, dyadic tolerance ledger, located tail row, located-window row, and downstream seal row.

Read the rows in dependency order. A precision request first reaches the finite windows $S$, then the regular rational observations $Q$, then the dyadic endpoint and tolerance ledger $D$. The located tail row $T$ can be inspected only after those finite observations are fixed, and the located-window row $W$ binds the same tail request to the displayed located ball.

The seal and structural rows add no further analytic object. The row $E$ is downstream of $S,Q,D,T,W$, while $H,C,P,N$ transport, replay, source, and name that same route. Since no listed row stores quotient stream equality, a selected host limit, countable choice, trichotomy, hidden stream inspection, or ambient completeness, the listed rows exhaust the seed obligation surface.
\end{proof}

\begin{theorem}[Located Cauchy sequence RegSeqRat handoff]\label{thm:located-cauchy-sequence-regseqrat-handoff}
Every $\RegSeqRatUp$-facing read of an accepted $\LocatedCauchySequenceUp$ carrier factors through
$$
S\longmapsto Q\longmapsto D\longmapsto T\longmapsto W\longmapsto(H,C,P,N).
$$
Thus the regular rational consumer receives a finite sequence window, the matching regular readback row, the dyadic tolerance ledger, and the located tail witness before any Real-facing seal can be inspected.
\end{theorem}
\begin{proof}
Start from a regular rational consumer request. By \autoref{def:located-cauchy-sequence-carrier}, the consumer reaches $Q$ only through the finite schedule row $S$, so the read begins with a displayed sequence window rather than with an unbounded stream.

The dyadic and located rows then fix the tail evidence. The tolerance ledger $D$ records the endpoint and radius data used by the request, while $T$ and $W$ attach the located Cauchy tail and located-window witness to that same finite readback.

The structural rows preserve this route. The rows $H,C,P,N$ transport, replay, package, and name the displayed handoff, and none of them introduces a quotient of streams, a selected limit, a choice function, a trichotomy branch, or an ambient completeness witness.
\end{proof}

\begin{theorem}[Located Cauchy sequence Real seal nonescape]\label{thm:located-cauchy-sequence-real-seal-nonescape}
Every $\RealUp$-facing, $\LocatedRealUp$-facing, or $\RegularLocatedCauchyRealUp$-facing read exported by an accepted $\LocatedCauchySequenceUp$ carrier factors through the complete finite route
$$
S,Q,D,T,W,E,H,C,P,N.
$$
Consequently a downstream consumer cannot read a terminal seal from the packet without also reading the sequence window, regular rational source, dyadic tolerance ledger, located Cauchy tail witness, located-window row, transport, continuation, provenance, and local NameCert rows.
\end{theorem}
\begin{proof}
Begin with a downstream seal read. The carrier exposes the seal only as $E$, and $E$ is listed after the finite windows, regular readback, dyadic tolerance row, located tail row, and located-window row in \autoref{def:located-cauchy-sequence-carrier}.

The route to $E$ is therefore finite. The sequence window $S$ fixes the requested observations, $Q$ records their regular rational readback, $D$ budgets the dyadic comparisons, and $T,W$ provide the located Cauchy and located-window evidence attached to that request.

Finally read the support rows. The rows $H,C,P,N$ keep the same finite route stable under componentwise transport, continuation replay, package provenance, and local naming. Since the packet has no coordinate for quotient stream equality, selected host limits, countable choice, trichotomy, hidden infinite stream inspection, or ambient $\RealUp$ completeness, no downstream consumer can escape the displayed route.
\end{proof}

\closureat{LocatedCauchySequenceUp}{seedStr}

\begin{closurestatus}{\LocatedCauchySequenceUp}
  \constructivestory{A $\LocatedCauchySequenceUp$ packet is generated from finite StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a located Cauchy tail row, a located-window witness, a downstream Real-facing seal row, componentwise transports, displayed continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-cauchy-sequence-carrier}, \autoref{thm:located-cauchy-sequence-namecert-obligations}, \autoref{thm:located-cauchy-sequence-regseqrat-handoff}, and \autoref{thm:located-cauchy-sequence-real-seal-nonescape} seed the sequence-level located Cauchy packet between StreamName, RegSeqRat, DyadicRatCore, LocatedCauchy, LocatedReal, Real, and RegularLocatedCauchyReal consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected host limits, countable choice, trichotomy, hidden infinite stream inspection, ambient Real completeness, Lean verification, or a public standard bridge.}
  \upgradepath{Lean closure requires carrier admission, the NameCert obligation surface, the RegSeqRat handoff, and the Real seal nonescape theorem for BEDC.Derived.LocatedCauchySequenceUp.}
\end{closurestatus}
