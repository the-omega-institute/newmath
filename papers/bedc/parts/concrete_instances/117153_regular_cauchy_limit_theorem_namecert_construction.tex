\chapter{A Concrete Naming Certificate for $\RegularCauchyLimitTheoremUp$}
\label{ch:concrete-instances-regular-cauchy-limit-theorem-namecert}
\origin{human}

Following Bishop--Bridges constructive real analysis, the $\RegularCauchyLimitTheoremUp$ packet records the finite route by which regular Cauchy evidence is admitted as a Real-facing limit theorem without importing a global completeness principle. It sits after the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a theorem-facing carrier for the displayed tail-window route; it is not a selected limit, quotient equality of regular names, arbitrary trichotomy, countable choice, or ambient completeness for $\RealUp$.

\begin{definition}[Regular Cauchy limit theorem carrier]
\label{def:regular-cauchy-limit-theorem-carrier}
A $\RegularCauchyLimitTheoremUp$ carrier is a finite $\BHist$ packet
$$
L=(M,S,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the tail-modulus row, displaying the finite threshold schedule used by the accepted regular Cauchy evidence.
\item $S$ is the $\StreamNameUp$ window row whose observations request the tail after the displayed threshold.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the dyadic scale at which the tail comparison is read.
\item $R$ is the $\RegSeqRatUp$ readback row obtained only after the tail modulus, stream window, and dyadic tolerance rows are visible.
\item $E$ is the terminal $\RealUp$ seal row exposed only after the regular rational readback row $R$.
\item $H$ records componentwise $\hsame$ transport for modulus, window, tolerance, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
M,S,D \longmapsto R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLimitTheoremUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyLimitTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,S,D,R,E,H,C,P,N$. It has no coordinate for selecting a completed limit, quotienting regular Cauchy names, arbitrary real trichotomy, countable choice, or an ambient completeness theorem for $\RealUp$.
\end{definition}
\leanstmt{BEDC.Derived.RegularCauchyLimitTheoremUp}

\begin{theorem}[Regular Cauchy limit theorem obligations]
\label{thm:regular-cauchy-limit-theorem-obligations}
For every accepted $\RegularCauchyLimitTheoremUp$ carrier $L=(M,S,D,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyLimitTheoremUp}$ surface consists exactly of carrier admission; tail-modulus visibility through $M$; finite $\StreamNameUp$ tail-window visibility through $S$; $\DyadicRatCoreUp$ tolerance exposure through $D$; $\RegSeqRatUp$ readback through $R$; $\RealUp$ seal handoff through $E$; componentwise stability under $\hsame$ through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; local naming through $N$; and ledger non-escape for all Real-facing reads.
\end{theorem}
\begin{proof}
Project the carrier. The rows $M,S,D$ are the only source rows visible before the regular rational readback, so tail-modulus visibility, window visibility, and dyadic tolerance exposure are all finite carrier clauses.

Read the handoff rows next. The row $R$ is downstream of $M,S,D$, and the terminal $\RealUp$ seal $E$ is downstream of $R$.

The structural rows $H,C,P,N$ transport, replay, source, and name the same displayed route. Since no excluded selected-limit, quotient-equality, trichotomy, choice, or completeness coordinate occurs in the carrier, ledger non-escape follows for every Real-facing read.
\end{proof}

\begin{theorem}[Regular Cauchy limit theorem tail-seal handoff]
\label{thm:regular-cauchy-limit-theorem-tail-seal-handoff}
Every Real-facing read of an accepted $\RegularCauchyLimitTheoremUp$ carrier factors through the displayed route
$$
M,S,D \longmapsto R \longmapsto E
$$
before the terminal seal is consumed. Transport, replay, provenance, and local naming preserve this route and do not add an alternate limit selector or completeness-facing coordinate.
\end{theorem}
\begin{proof}
Start with the obligation theorem \autoref{thm:regular-cauchy-limit-theorem-obligations}. It identifies $M,S,D$ as the complete finite source surface for the theorem-facing tail read.

The regular readback row is next in the carrier order. A Real-facing consumer therefore reaches $R$ only after the tail modulus, stream window, and dyadic tolerance rows have been displayed.

Finally consume the seal row $E$. The rows $H,C,P,N$ preserve the same dependency order under transport, replay, provenance, and naming, so no consumer can bypass the tail-window, readback, and tolerance route before reading the seal.
\end{proof}

\closureat{RegularCauchyLimitTheoremUp}{seedStr}

\begin{closurestatus}{\RegularCauchyLimitTheoremUp}
  \constructivestory{A $\RegularCauchyLimitTheoremUp$ packet is generated from a tail-modulus row, a StreamName tail-window row, a DyadicRatCore tolerance row, a RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-limit-theorem-carrier}, \autoref{thm:regular-cauchy-limit-theorem-obligations}, and \autoref{thm:regular-cauchy-limit-theorem-tail-seal-handoff} seed the finite theorem-facing tail-window route from regular Cauchy evidence to the RealUp seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RealUp completeness, selected limits, quotient regular Cauchy equality, arbitrary trichotomy, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tail-modulus visibility, StreamName window exposure, DyadicRatCore tolerance exposure, RegSeqRat readback, RealUp seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyLimitTheoremUp.}
\end{closurestatus}
