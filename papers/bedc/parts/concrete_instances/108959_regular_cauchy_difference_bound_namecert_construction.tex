\chapter{A Concrete Naming Certificate for $\RegularCauchyDifferenceBoundUp$}
\label{ch:concrete-instances-regular-cauchy-difference-bound-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyDifferenceBoundUp$ packet records paired regular-Cauchy difference estimates as finite rational-window data rather than as equality in a completed real quotient. It sits over the regular sequence route of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the tail envelope comparison route of \autoref{ch:concrete-instances-cauchy-tail-envelope-comparison-namecert}. The packet supplies completion and Real algebra consumers with one finite difference-bound surface; it does not assert quotient equality, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy difference-bound carrier]
\label{def:regular-cauchy-difference-bound-carrier}
A $\RegularCauchyDifferenceBoundUp$ carrier is a finite $\BHist$ packet
$$
B=(X,Y,W,D,E,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the first $\RegSeqRatUp$ source row, read only through finite rational windows.
\item $Y$ is the second $\RegSeqRatUp$ source row, displayed on the same request schedule as $X$.
\item $W$ is the shared $\StreamNameUp$ window family exposing the compared prefixes.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and subtraction ledger for the displayed window.
\item $E$ is the $\CauchyTailEnvelopeComparisonUp$ envelope row bounding the paired tails after $W$ and $D$ have been read.
\item $R$ is the terminal $\RealUp$ seal row for the displayed difference bound.
\item $H$ records componentwise $\hsame$ transport for the two regular sources, the shared windows, the dyadic ledger, the envelope comparison, the Real seal, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for window inspection, tolerance subtraction, tail-envelope comparison, Real sealing, and structural transport.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyDifferenceBoundUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyTailEnvelopeComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyDifferenceBoundUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,W,D,E,R,H,C,P,N$. It has no coordinate for quotient real equality, selected representatives, countable-choice moduli, trichotomy, host subtraction, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy difference-bound NameCert obligations]
\label{thm:regular-cauchy-difference-bound-namecert-obligations}
For an accepted $\RegularCauchyDifferenceBoundUp$ packet $B=(X,Y,W,D,E,R,H,C,P,N)$, the local NameCert surface consists exactly of carrier admission for the two regular rational sources, shared finite-window inspection, dyadic difference budgeting, tail-envelope comparison, Real-seal handoff, componentwise transport, displayed replay, provenance, and local naming. Every accepted bound read follows
$$
X,Y\longmapsto W\longmapsto D\longmapsto E\longmapsto R
$$
together with $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-difference-bound-carrier}. The two source rows $X,Y$ are the only regular-rational inputs, and they are read on the shared window row $W$.

The dyadic ledger $D$ supplies the displayed tolerance and subtraction budget for that same window. The tail-envelope row $E$ can therefore compare only the paired tails already exposed by $X,Y,W,D$.

The terminal Real row $R$ is downstream of the envelope comparison, while $H,C,P,N$ only transport, replay, source, and name the same finite route. Since the carrier has no quotient equality, selected representative, countable-choice, host-subtraction, or ambient-completeness coordinate, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Regular Cauchy difference-bound RegSeqRat handoff]
\label{thm:regular-cauchy-difference-bound-regseqrat-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\RegularCauchyDifferenceBoundUp$ packet receives the paired difference estimate only through the displayed route from $X,Y$ to $W,D,E,R$. The handoff preserves the finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, and $\CauchyTailEnvelopeComparisonUp$ envelope row, and it exports no quotient real equality, selected limit, countable-choice schedule, trichotomy, host real subtraction, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:regular-cauchy-difference-bound-namecert-obligations}. They place every accepted read on the route $X,Y,W,D,E,R,H,C,P,N$.

A $\RegSeqRatUp$ consumer can inspect only the two displayed regular sources and the finite windows in $W$. It must therefore read the same dyadic ledger $D$ before any bound is available.

A $\RealUp$ consumer sees the terminal row $R$ only after the tail-envelope comparison $E$ has been consumed. The structural rows preserve this route, so no excluded equality, selector, choice, host-subtraction, or completeness object can be exported.
\end{proof}

\falsifiablePrediction{Every $\RegularCauchyDifferenceBoundUp$ read factors through two RegSeqRat source rows, a shared StreamName window row, a DyadicRatCore tolerance ledger, a CauchyTailEnvelopeComparison envelope row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real, cauchy-tail-envelope-comparison: $\RegularCauchyDifferenceBoundUp$ is not bijective to any listed sibling because it jointly carries paired regular sources, shared finite windows, dyadic difference budgeting, tail-envelope comparison, terminal Real sealing, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyDifferenceBoundUp}{seedStr}

\begin{closurestatus}{\RegularCauchyDifferenceBoundUp}
  \constructivestory{A $\RegularCauchyDifferenceBoundUp$ packet is generated from two RegSeqRat source rows, a shared StreamName window row, a DyadicRatCore tolerance and subtraction ledger, a CauchyTailEnvelopeComparison row, a terminal RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-difference-bound-carrier}, \autoref{thm:regular-cauchy-difference-bound-namecert-obligations}, and \autoref{thm:regular-cauchy-difference-bound-regseqrat-handoff} seed the finite paired regular-Cauchy difference-bound carrier, certificate surface, and RegSeqRat-to-Real handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected limits, countable choice, trichotomy, host real subtraction, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires checked carrier admission, paired regular-source exposure, shared-window exactness, dyadic difference budgeting, tail-envelope comparison, Real sealing, transport, replay, provenance, and local NameCert non-escape for $\RegularCauchyDifferenceBoundUp$.}
\end{closurestatus}
