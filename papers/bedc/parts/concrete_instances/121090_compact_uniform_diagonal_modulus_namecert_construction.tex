\chapter{A Concrete Naming Certificate for $\CompactUniformDiagonalModulusUp$}
\label{ch:concrete-instances-compact-uniform-diagonal-modulus-namecert}
\origin{human}

Following the Heine--Cantor compactness-to-uniform-continuity route in \autoref{ch:visions-compact-uniform-continuity}, the $\CompactUniformDiagonalModulusUp$ packet records the finite diagonal-modulus surface obtained from compact nets, pointwise moduli, rational lower-bound folds, and metric replay. It sits over the compact source of \autoref{ch:concrete-instances-compact-namecert}, the continuous pointwise-modulus rows of \autoref{ch:concrete-instances-continuous-namecert}, the metric distance carrier of \autoref{ch:concrete-instances-metric-namecert}, the rational lower-bound fold of \autoref{ch:concrete-instances-rat-namecert}, and the downstream $\UniformModulusUp$ handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet exposes a tolerance-only modulus as displayed BEDC data, not as an external function selector.

\begin{definition}[CompactUniformDiagonalModulus carrier]
\label{def:compact-uniform-diagonal-modulus-carrier}
A $\CompactUniformDiagonalModulusUp$ carrier is a finite $\BHist$ packet
$$
D=(K,F,\mathcal{P},M,R,B,U,H,C,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row carrying the finite net or finite-cover witness.
\item $F$ is the $\ContinuousUp$ pointwise-modulus row for the displayed source and target metric carriers.
\item $\mathcal{P}$ is the finite $\mathsf{ProbeBundle}$ spine over which the compact net is read.
\item $M$ records the local modulus rows attached to the probes in $\mathcal{P}$.
\item $R$ records the positive $\RatUp$ lower-bound candidates read from the local radius rows.
\item $B$ is the diagonal lower-bound ledger certifying one rational precision below all displayed candidates.
\item $U$ is the $\UniformModulusUp$ handoff row exposed from the tolerance history and the diagonal ledger.
\item $H$ records componentwise $\hsame$ transport for the compact source, pointwise rows, bundle, candidates, diagonal ledger, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite diagonal route.
\item $N$ is the local $\NameCert_{\CompactUniformDiagonalModulusUp}$ row, with $\Pkg$ provenance over $\CompactUniformDiagonalModulusUp$, $\CompactUp$, $\ContinuousUp$, $\MetricUp$, $\RatUp$, $\UniformModulusUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\end{enumerate}
The carrier has no coordinate for an open-cover choice function, a host minimum over a finite set, a selected point outside the bundle, quotient point equality, an external modulus function, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[CompactUniformDiagonalModulus NameCert obligations]
\label{thm:compact-uniform-diagonal-modulus-namecert-obligations}
For every accepted $\CompactUniformDiagonalModulusUp$ carrier
$$
D=(K,F,\mathcal{P},M,R,B,U,H,C,N),
$$
the local $\NameCert_{\CompactUniformDiagonalModulusUp}$ surface consists exactly of compact source exposure, pointwise-modulus admission, finite $\mathsf{ProbeBundle}$ spine admission, local-radius row exposure, positive $\RatUp$ candidate admission, diagonal lower-bound certification, $\UniformModulusUp$ handoff, componentwise transport, displayed replay, and local naming with provenance. Every uniform-modulus read factors through the finite route
$$
\CompactUp,\quad \ContinuousUp,\quad \MetricUp,\quad \RatUp,\quad \UniformModulusUp .
$$
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:compact-uniform-diagonal-modulus-carrier}. The compact source, continuous pointwise rows, finite bundle, local modulus rows, rational candidates, lower-bound ledger, and uniform-modulus handoff are displayed coordinates.

The finite diagonal route first reads the compact net $\mathcal{P}$, then reads the local modulus rows $M$ at each bundled probe. The positive rational candidates $R$ provide the finite input to the lower-bound ledger $B$.

The handoff $U$ is exposed only after the lower-bound ledger has been transported and replayed by $H$ and $C$. Since the carrier contains no open-cover choice, host minimum, off-bundle point selector, quotient point equality, external modulus function, or completeness row, the listed obligations exhaust the certificate surface.
\end{proof}

\begin{theorem}[CompactUniformDiagonalModulus uniform handoff]
\label{thm:compact-uniform-diagonal-modulus-uniform-handoff}
An accepted $\CompactUniformDiagonalModulusUp$ packet exposes a $\UniformModulusUp$ precision row only after the compact source $K$, pointwise-modulus row $F$, finite bundle $\mathcal{P}$, local modulus rows $M$, rational candidates $R$, and diagonal lower-bound ledger $B$ have been displayed. Thus the compact-uniform route is a finite diagonal-modulus packet and not a host Heine--Cantor theorem, open-cover selector, quotient equality principle, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $D=(K,F,\mathcal{P},M,R,B,U,H,C,N)$. Projection gives the compact and pointwise-continuity source rows before any uniform handoff is visible.

The finite bundle $\mathcal{P}$ is the only index surface for the diagonal route. Reading $M$ and $R$ over that bundle supplies the local radii and positive rational candidates inspected by the lower-bound ledger.

The row $B$ certifies one displayed precision below the candidates, and $U$ exposes that precision as a tolerance-only $\UniformModulusUp$ row. Transport, replay, provenance, and local naming keep the read inside the displayed packet, so none of the excluded host or completeness objects can be exported.
\end{proof}

\closureat{CompactUniformDiagonalModulusUp}{seedStr}

\begin{closurestatus}{\CompactUniformDiagonalModulusUp}
  \constructivestory{A $\CompactUniformDiagonalModulusUp$ packet is generated from a CompactUp finite-net source, a ContinuousUp pointwise-modulus row, a finite ProbeBundle spine, local modulus rows, positive RatUp candidates, a diagonal lower-bound ledger, a UniformModulusUp handoff, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-diagonal-modulus-carrier}, \autoref{thm:compact-uniform-diagonal-modulus-namecert-obligations}, and \autoref{thm:compact-uniform-diagonal-modulus-uniform-handoff} seed the finite compact-net diagonal modulus packet used by compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover choice, host finite minima, off-bundle point selection, quotient point equality, external modulus functions, ambient completeness of $\RealUp$, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires checked compact-source admission, pointwise-modulus admission, finite bundle coverage, rational lower-bound stability, uniform-modulus handoff, and non-escape for BEDC.Derived.CompactUniformDiagonalModulusUp.}
\end{closurestatus}
