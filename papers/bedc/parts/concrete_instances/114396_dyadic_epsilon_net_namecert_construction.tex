\chapter{A Concrete Naming Certificate for $\DyadicEpsilonNetUp$}
\label{ch:concrete-instances-dyadic-epsilon-net-namecert}
\origin{human}

Bishop--Bridges-style finite-net constructive analysis treats compact interval and compact metric work by displaying the actual finite schedule of centers and radii inspected at a requested precision. The $\DyadicEpsilonNetUp$ packet records that schedule as a BEDC NameCert object on the L10 route: its centers and tolerances read the $\DyadicRatCoreUp$ endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, its mesh cells use the $\DyadicIntervalArithmeticUp$ enclosure route of \autoref{ch:concrete-instances-dyadic-interval-arithmetic-namecert}, its requests are exposed through the $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, its finite readback reaches $\RegSeqRatUp$ through \autoref{ch:concrete-instances-regseqrat-namecert}, its terminal values are sealed through $\RealUp$ in \autoref{ch:concrete-instances-real-namecert}, and its coverage claims are consumed against the $\MetricUp$ source of \autoref{ch:concrete-instances-metric-namecert}. The packet is the explicit finite-net schedule used by interval compactness and total-boundedness consumers; it is not an open-cover compactness theorem, a selected limiting subnet, quotient real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Dyadic epsilon-net carrier]
\label{def:dyadic-epsilon-net-carrier}
A $\DyadicEpsilonNetUp$ carrier is a finite $\BHist$ packet
$$
E=(X,\varepsilon,W,D,I,C_{\varepsilon},R,S,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\MetricUp$ source row whose point histories and distance reads are the only coverage domain inspected by the packet.
\item $\varepsilon$ is a positive tolerance request displayed as a $\DyadicRatCoreUp$ row.
\item $W$ is the $\StreamNameUp$ finite-window schedule recording which precision requests, source probes, and center indices are inspected.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare the requested radius, mesh slack, and distance estimates along $W$.
\item $I$ is the $\DyadicIntervalArithmeticUp$ mesh-row package that stores outward endpoint enclosures and interval-cell refinements for the displayed schedule.
\item $C_{\varepsilon}$ is the finite list of center rows admitted at the requested tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the window, dyadic ledger, interval arithmetic, and center data.
\item $S$ is the terminal $\RealUp$ seal boundary reached only after $X,\varepsilon,W,D,I,C_{\varepsilon},R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the metric source, tolerance request, windows, dyadic ledger, interval arithmetic, center list, readback, real seal, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a finite-net request through $X,\varepsilon,W,D,I,C_{\varepsilon},R,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\DyadicEpsilonNetUp$, $\MetricUp$, $\DyadicRatCoreUp$, $\DyadicIntervalArithmeticUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicEpsilonNetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,\varepsilon,W,D,I,C_{\varepsilon},R,S,H,K,P,N$. It has no coordinate for an arbitrary open cover, a selected infinite subnet, quotient point equality, host real equality, countable choice, unlisted centers, or ambient completeness.
\end{definition}

\begin{theorem}[Dyadic epsilon-net NameCert obligations]
\label{thm:dyadic-epsilon-net-namecert-obligations}
For every accepted $\DyadicEpsilonNetUp$ carrier
$$
E=(X,\varepsilon,W,D,I,C_{\varepsilon},R,S,H,K,P,N),
$$
the local $\NameCert_{\DyadicEpsilonNetUp}$ surface consists exactly of carrier admission for the $\MetricUp$ source, positive dyadic tolerance exposure, finite-window exposure, dyadic tolerance exactness, dyadic interval-arithmetic mesh exposure, finite center-list admission, $\RegSeqRatUp$ readback, $\RealUp$ seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.

No obligation may be discharged by open-cover compactness, a selected infinite subnet, quotient point equality, host real equality, countable choice, hidden centers, or ambient real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-epsilon-net-carrier}. The analytic rows are exactly the metric source $X$, the tolerance request $\varepsilon$, the finite schedule $W$, the dyadic ledger $D$, the interval mesh $I$, the center list $C_{\varepsilon}$, the readback $R$, and the terminal seal $S$.

Read the numerical rows in dependency order. The tolerance request and comparison budget are displayed by $\DyadicRatCoreUp$; the mesh cells are then routed through $\DyadicIntervalArithmeticUp$; and only those displayed cells and center rows feed the $\RegSeqRatUp$ readback.

The structural rows preserve the same schedule. Componentwise transport through $H$ and replay through $K$ keep the source, tolerance, window, mesh, centers, readback, and seal in the same order, while $P$ and $N$ name exactly the sibling packet and local certificate rows.

The excluded principles are not carrier coordinates. Therefore no open-cover compactness argument, selected infinite subnet, quotient point equality, host real equality, countable-choice selector, hidden center family, or ambient real-completeness principle can enter the local NameCert surface.
\end{proof}

\begin{theorem}[Dyadic epsilon-net finite L10 route]
\label{thm:dyadic-epsilon-net-finite-l10-route}
Let $E=(X,\varepsilon,W,D,I,C_{\varepsilon},R,S,H,K,P,N)$ be an accepted $\DyadicEpsilonNetUp$ carrier. Every compactness-facing, total-boundedness-facing, or completion-facing consumer of the finite-net schedule reads the route
$$
\MetricUp \longmapsto \DyadicRatCoreUp \longmapsto \DyadicIntervalArithmeticUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
through the displayed rows $X,\varepsilon,W,D,I,C_{\varepsilon},R,S,H,K,P,N$. The consumer receives a finite dyadic center schedule at the requested tolerance, not an open-cover subcover, an infinite selected net, quotient real equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with a consumer read of $E$. The metric source row $X$ fixes the inspected domain, and the positive dyadic request $\varepsilon$ fixes the tolerance that the finite schedule must answer.

The route then exposes only finite data. The window row $W$ lists the inspected precision requests and center indices, $D$ compares their dyadic tolerances, and $I$ records the interval-cell arithmetic needed by the displayed mesh.

The readback and seal are downstream. The row $R$ is produced from the same $W,D,I,C_{\varepsilon}$ data, and the terminal row $S$ is reached only after that readback has been displayed.

Finally $H,K,P,N$ preserve and name this route. Since the carrier has no open-cover, infinite-net, quotient, host-equality, choice, or ambient-completeness coordinate, every accepted consumer receives exactly the displayed finite L10 route.
\end{proof}

\falsifiablePrediction{Every accepted $\DyadicEpsilonNetUp$ read factors through $\MetricUp$, $\DyadicRatCoreUp$, $\DyadicIntervalArithmeticUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\closureat{DyadicEpsilonNetUp}{seedStr}

\begin{closurestatus}{\DyadicEpsilonNetUp}
  \constructivestory{A $\DyadicEpsilonNetUp$ packet is generated from a MetricUp source row, a positive DyadicRatCoreUp tolerance request, StreamNameUp finite windows, DyadicRatCoreUp comparison ledgers, DyadicIntervalArithmeticUp mesh cells, a finite center list, RegSeqRatUp readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-epsilon-net-carrier}, \autoref{thm:dyadic-epsilon-net-namecert-obligations}, and \autoref{thm:dyadic-epsilon-net-finite-l10-route} seed the explicit finite epsilon-net schedule over MetricUp, DyadicRatCoreUp, DyadicIntervalArithmeticUp, StreamNameUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, open-cover compactness, selected infinite nets, quotient point equality, host real equality, countable choice, hidden centers, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, finite center-list admission, dyadic tolerance exactness, interval-mesh exactness, readback exactness, RealUp seal nonescape, classifier transport, and TasteGate field faithfulness for BEDC.Derived.DyadicEpsilonNetUp.}
\end{closurestatus}
