\chapter{A Concrete Naming Certificate for $\RegularCauchyLocatedComparisonUp$}
\label{ch:concrete-instances-regular-cauchy-located-comparison-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyLocatedComparisonUp$ packet names the finite located-comparison route for two regular Cauchy real names before any $\RealUp$ consumer reads a comparison result. It sits over the regular-rational source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the shared finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the located comparison surface of \autoref{ch:concrete-instances-located-real-comparison-namecert}, and the Bishop located Cauchy real route of \autoref{ch:concrete-instances-bishop-located-cauchy-real-namecert}. The packet is a human-origin L10 handoff from regular Cauchy readback to located $\RealUp$ comparison; it is not quotient real equality, trichotomy, countable choice, selected limits, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy located-comparison carrier]
\label{def:regular-cauchy-located-comparison-carrier}
A $\RegularCauchyLocatedComparisonUp$ carrier is a finite $\BHist$ packet
$$
L=(X_0,X_1,W,R_0,R_1,D,K,E_0,E_1,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X_0$ and $X_1$ are the two regular Cauchy source-name rows. Each is read as a displayed $\RegSeqRatUp$ packet before either endpoint is sealed as a real name.
\item $W$ is the shared $\StreamNameUp$ window family. It records the finite observation lengths, synchronized cuts, endpoint windows, and comparison requests inspected for both sources.
\item $R_0$ and $R_1$ are the $\RegSeqRatUp$ readback rows obtained from $X_0,X_1$ on the shared windows $W$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger. It records the requested scale, comparison slack, positive-gap threshold, endpoint enclosures, and exhausted finite windows.
\item $K$ is the $\LocatedRealComparisonUp$ evidence row. It reads only the displayed readbacks $R_0,R_1$, shared windows $W$, dyadic tolerances $D$, and the terminal seals $E_0,E_1$.
\item $E_0$ and $E_1$ are the terminal $\RealUp$ seal rows for the two regular Cauchy names. They are reachable only after $X_0,X_1,W,R_0,R_1,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two source rows, shared windows, readback rows, dyadic tolerance ledger, located-comparison evidence, real seals, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
X_0,X_1,W,D \longmapsto R_0,R_1 \longmapsto E_0,E_1 \longmapsto K .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLocatedComparisonUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\LocatedRealComparisonUp$, $\RealUp$, $\BishopLocatedCauchyRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyLocatedComparisonUp}$ row.
\end{enumerate}
The carrier predicate admits exactly these thirteen rows. Its classifier compares accepted packets only through $X_0,X_1,W,R_0,R_1,D,K,E_0,E_1,H,C,P,N$ and has no coordinate for quotient real equality, host trichotomy, countable choice, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Regular Cauchy located-comparison NameCert obligations]
\label{thm:regular-cauchy-located-comparison-namecert-obligations}
For every accepted $\RegularCauchyLocatedComparisonUp$ carrier
$$
L=(X_0,X_1,W,R_0,R_1,D,K,E_0,E_1,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyLocatedComparisonUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two regular Cauchy source rows, shared stream windows, two regular rational readbacks, dyadic tolerance ledger, located-comparison evidence row, two terminal real seals, transport, replay, provenance, and local naming row;
\item shared-window discipline, requiring the two readback rows $R_0,R_1$ to be read from the same window family $W$ before any comparison evidence is exported;
\item dyadic tolerance discipline, requiring $D$ to supply the requested scale, comparison slack, positive-gap threshold, endpoint enclosures, and exhaustion ledger consumed by the located comparison row;
\item real-seal discipline, requiring $E_0$ and $E_1$ to remain downstream of the displayed regular Cauchy readbacks and dyadic budget;
\item located-comparison exactness, requiring $K$ to compare only the displayed $R_0,R_1,W,D,E_0,E_1$ route and to expose positive-gap or nonseparating evidence as finite located-comparison data;
\item transport and replay stability, requiring $H$ and $C$ to preserve the two source rows, window row, readback rows, tolerance row, comparison row, seal rows, provenance row, and local name row componentwise;
\item ledger exhaustion, excluding quotient real equality, host trichotomy, countable choice, selected limits, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{enumerate}
No regular Cauchy located-comparison obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier $L$. The projection exposes exactly the two source rows, the common finite-window family, the two readback rows, the dyadic tolerance ledger, the located-comparison row, the two terminal real seals, and the structural rows.

The comparison route is finite. The shared windows $W$ determine the inspected observations for both source rows, and the two readbacks $R_0,R_1$ are read at those same windows before any comparison evidence can be exported.

The dyadic ledger is the only tolerance input. It supplies the scale, slack, positive-gap threshold, endpoint enclosures, and exhaustion record that the located-comparison row is allowed to consume.

The real seals remain attached to the route. The rows $E_0$ and $E_1$ are read only after the regular Cauchy sources, windows, readbacks, and dyadic budget have been displayed, so the located comparison cannot appeal to detached real representatives.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for quotient real equality, trichotomy, choice, selected limits, completeness, Lean verification, or a bridge, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy located-comparison real handoff]
\label{thm:regular-cauchy-located-comparison-real-handoff}
Every $\RealUp$ or located-real consumer of an accepted $\RegularCauchyLocatedComparisonUp$ carrier reads the comparison only through the finite handoff route
$$
X_0,X_1,W,D \longmapsto R_0,R_1 \longmapsto E_0,E_1 \longmapsto K \longmapsto H,C,P,N .
$$
Thus the consumer receives two displayed regular Cauchy names, their shared StreamName windows, RegSeqRat readbacks, DyadicRatCore tolerance ledger, terminal RealUp seals, LocatedRealComparison evidence, componentwise transport, continuation replay, package provenance, and local naming. It receives no quotient real equality, host trichotomy, countable-choice schedule, selected limit, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with an accepted carrier. The source rows are $X_0$ and $X_1$, and the carrier definition requires both to pass through the common window family $W$ and the dyadic tolerance ledger $D$.

The readback stage is $R_0,R_1$. These rows are the only regular rational observations supplied to the later real seals, so a consumer cannot introduce a second readback schedule or an unlisted tolerance.

The seal stage is $E_0,E_1$. Each seal remains downstream of the corresponding regular Cauchy readback and the shared dyadic budget, placing the comparison inside the displayed Bishop located Cauchy real route rather than inside a quotient-real carrier.

The comparison stage is $K$. It is the sole located-comparison evidence row and may only consume the displayed readbacks, windows, tolerances, and real seals.

The structural rows $H,C,P,N$ preserve this route under componentwise transport, continuation replay, provenance, and local naming. Because no other coordinate is present, no $\RealUp$ or located-real consumer can read quotient equality, trichotomy, countable choice, a selected limit, or ambient completeness from the packet.
\end{proof}

\closureat{RegularCauchyLocatedComparisonUp}{seedStr}

\begin{closurestatus}{\RegularCauchyLocatedComparisonUp}
  \constructivestory{A $\RegularCauchyLocatedComparisonUp$ packet is generated from two regular Cauchy source-name rows, one shared StreamName window family, two RegSeqRat readbacks, a DyadicRatCore tolerance ledger, a LocatedRealComparison evidence row, two RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-located-comparison-carrier}, \autoref{thm:regular-cauchy-located-comparison-namecert-obligations}, and \autoref{thm:regular-cauchy-located-comparison-real-handoff} seed the finite located-comparison handoff from regular Cauchy readbacks through shared StreamName windows, DyadicRatCore tolerances, RealUp seals, LocatedRealComparison evidence, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, trichotomy, countable choice, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-window discipline, readback exactness, tolerance discipline, Real seal nonescape, located-comparison handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyLocatedComparisonUp.}
\end{closurestatus}
