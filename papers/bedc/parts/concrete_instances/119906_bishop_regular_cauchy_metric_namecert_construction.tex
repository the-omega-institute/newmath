\chapter{A Concrete Naming Certificate for $\BishopRegularCauchyMetricUp$}
\label{ch:concrete-instances-bishop-regular-cauchy-metric-namecert}
\origin{human}

Following Bishop and Bridges, Constructive Analysis, the $\BishopRegularCauchyMetricUp$ packet records the metric interface on regular Cauchy rational names as finite BEDC NameCert data. It sits after the dyadic arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the common-window lattice of \autoref{ch:concrete-instances-regular-cauchy-window-lattice-namecert}. The packet names the regular-Cauchy distance read only through displayed dyadic windows and readback ledgers; it is not a quotient of streams, a selected host real representative, a trichotomy principle, or an ambient completeness theorem.

\begin{definition}[Bishop regular-Cauchy metric carrier]
\label{def:bishop-regular-cauchy-metric-carrier}
A $\BishopRegularCauchyMetricUp$ carrier is a finite $\BHist$ packet
$$
M=(L,R,D,S,Q,E,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are the left and right regular-Cauchy rational-name rows whose finite windows are being compared.
\item $D$ is the $\DyadicRatCoreUp$ distance ledger, recording the dyadic tolerance, absolute-difference row, radius comparison, and common refinement budget.
\item $S$ is the $\StreamNameUp$ observation row listing exactly the source indices inspected on the two names.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained after the dyadic ledger and finite observations have been displayed.
\item $E$ is the terminal $\RealUp$ seal for the distance value read from the same regular-Cauchy route.
\item $B$ is the Bishop metric-bound row, storing positivity, symmetry, triangle-window, and zero-distance readback clauses only as finite regular-Cauchy obligations.
\item $H$ records componentwise $\hsame$ transport for the two source names, dyadic ledger, stream observations, regular readback, Real seal, metric-bound row, replay, provenance, and local certificate.
\item $C$ records displayed $\Cont$ replay of the finite route from regular-Cauchy names to the distance seal.
\item $P$ is the $\Pkg$ provenance over $\BishopRegularCauchyMetricUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RegularCauchyWindowLatticeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegularCauchyMetricUp}$ row.
\end{enumerate}
The carrier predicate asserts that every metric read factors through the displayed dyadic common-window route. It has no coordinate for quotient equality of sequences, a selected completion representative, excluded middle over order comparisons, countable choice, or host metric completion.
\end{definition}

\begin{theorem}[Bishop regular-Cauchy metric NameCert obligations]
\label{thm:bishop-regular-cauchy-metric-namecert-obligations}
For every accepted $\BishopRegularCauchyMetricUp$ carrier
$$
M=(L,R,D,S,Q,E,B,H,C,P,N),
$$
the local $\NameCert_{\BishopRegularCauchyMetricUp}$ surface consists exactly of regular-name exposure for $L,R$, dyadic distance-ledger exposure through $D$, finite stream-window exposure through $S$, $\RegSeqRatUp$ readback through $Q$, $\RealUp$ sealing through $E$, Bishop metric-bound ledger exposure through $B$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, local naming through $N$, and non-escape from quotient, choice, order-trichotomy, or host completion principles.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only analytic input rows are $L,R,D,S,Q,E,B$ together with the structural rows $H,C,P,N$.

The distance ledger is finite. The dyadic row $D$ records the requested tolerance and absolute-difference comparison, while $S$ records the inspected indices. Thus the readback row $Q$ and seal row $E$ are reached through displayed regular-Cauchy data rather than through a semantic quotient of streams.

The Bishop metric-bound row $B$ names positivity, symmetry, triangle-window, and zero-distance clauses as ledgers over the same route. Componentwise transport and replay preserve those displayed rows, and the excluded principles have no carrier coordinate. Hence the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop regular-Cauchy metric Real-completion handoff]
\label{thm:bishop-regular-cauchy-metric-real-completion-handoff}
Every completion-facing consumer of an accepted $\BishopRegularCauchyMetricUp$ packet must first read
$$
L,R,D,S,Q,E,B,H,C,P,N
$$
in the displayed order before treating the sealed row $E$ as a regular-Cauchy distance in $\RealUp$. The handoff therefore passes through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and the common-window lattice of \autoref{ch:concrete-instances-regular-cauchy-window-lattice-namecert}; it exports no host equality of Cauchy names, selected representative, countable-choice schedule, trichotomy oracle, or ambient completion operator.
\end{theorem}
\begin{proof}
Start with a completion-facing read. The carrier exposes two regular-Cauchy source rows $L,R$ before any distance row can be consumed.

The dyadic and stream stages are $D$ and $S$. They record the common tolerance budget, absolute-difference comparison, and inspected windows that license the regular readback $Q$.

Only after this readback does the terminal Real seal $E$ become visible, and the Bishop metric-bound row $B$ is a ledger over the same displayed route. The structural rows $H,C,P,N$ transport, replay, source, and name that route without adding coordinates.

Since quotient equality of names, selected representatives, countable choice, trichotomy, and host completion are absent from the packet, every completion-facing distance read remains inside the finite regular-Cauchy metric handoff.
\end{proof}

\closureat{BishopRegularCauchyMetricUp}{seedStr}

\begin{closurestatus}{\BishopRegularCauchyMetricUp}
  \constructivestory{A $\BishopRegularCauchyMetricUp$ packet is generated from two regular-Cauchy rational-name rows, a DyadicRatCore distance ledger, finite StreamName observations, RegSeqRat readback, a RealUp seal, a Bishop metric-bound row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-cauchy-metric-carrier}, \autoref{thm:bishop-regular-cauchy-metric-namecert-obligations}, and \autoref{thm:bishop-regular-cauchy-metric-real-completion-handoff} seed the finite metric route from regular Cauchy rational names to a Real-facing distance seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality of Cauchy names, selected representatives, countable choice, trichotomy of reals, ambient completion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic distance-ledger exactness, finite stream-window exposure, RegSeqRat readback, Real seal ordering, Bishop metric-bound rows, transport, replay, provenance, and local naming for BEDC.Derived.BishopRegularCauchyMetricUp.}
\end{closurestatus}
