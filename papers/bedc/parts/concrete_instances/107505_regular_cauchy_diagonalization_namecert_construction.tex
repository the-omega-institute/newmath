\chapter{A Concrete Naming Certificate for $\RegularCauchyDiagonalizationUp$}
\label{ch:concrete-instances-regular-cauchy-diagonalization-namecert}
\origin{human}

Following Bishop and Bridges, Constructive Analysis, the $\RegularCauchyDiagonalizationUp$ packet records the finite route by which a regular Cauchy family of regular rational names is read along a unary diagonal selector before any Real or Completion consumer treats the diagonal read as a completed real name. It sits over the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the sibling diagonal extraction packet of \autoref{ch:concrete-instances-regular-cauchy-diagonal-extraction-namecert}. The packet gives the L10 Real and Completion route one displayed diagonalization carrier; it does not assert quotient stream equality, choose a host limit, invoke countable choice, use trichotomy, claim ambient $\RealUp$ completeness, provide Lean verification, or record a checked bridge.

\begin{definition}[Regular Cauchy diagonalization carrier]
\label{def:regular-cauchy-diagonalization-carrier}
A $\RegularCauchyDiagonalizationUp$ carrier is a finite $\BHist$ packet
$$
D=(F,\delta,S,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the regular Cauchy family row. It displays the source family of rational names together with the finite precision requests inspected by the diagonal read.
\item $\delta$ is the unary diagonal selector row. It names the selected source index for each inspected precision and records no independent choice schedule beyond that displayed unary selector.
\item $S$ is the aligned $\StreamNameUp$ window row. It displays the finite windows of the selected source names and aligns them with the requests carried by $F$ and $\delta$.
\item $T$ is the $\DyadicRatCoreUp$ tolerance ledger. It records the requested dyadic radius, split-tolerance budget, comparison windows, and common-scale data used by the diagonal read.
\item $R$ is the $\RegSeqRatUp$ diagonal readback row. It reads the selected stream windows through the tolerance ledger as one regular rational sequence.
\item $E$ is the terminal $\RealUp$ seal reached only after $F,\delta,S,T,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the family, selector, stream windows, tolerance ledger, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a diagonalization request through the family row, unary selector, aligned windows, dyadic tolerance ledger, regular rational readback, terminal Real seal, transports, provenance, and local name.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyDiagonalizationUp$, $\RegularCauchyDiagonalExtractionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyDiagonalizationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,\delta,S,T,R,E,H,C,P,N$. The carrier has no coordinate for quotient stream equality, selected host limits, countable-choice schedules, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge credentials.
\end{definition}

\begin{theorem}[Regular Cauchy diagonalization NameCert obligations]
\label{thm:regular-cauchy-diagonalization-namecert-obligations}
For every accepted $\RegularCauchyDiagonalizationUp$ carrier
$$
D=(F,\delta,S,T,R,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyDiagonalizationUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the regular Cauchy family, unary diagonal selector, aligned StreamName windows, DyadicRatCore tolerance ledger, RegSeqRat diagonal readback, Real seal, transport, replay, provenance, and local naming rows;
\item selector exactness, requiring every diagonal read to pass through the displayed unary selector $\delta$ and the family row $F$;
\item window alignment, requiring the $\StreamNameUp$ row $S$ to expose exactly the selected finite windows named by $F$ and $\delta$;
\item tolerance exactness, requiring the requested radius, split budget, comparison windows, and common-scale data to be read through the $\DyadicRatCoreUp$ row $T$;
\item regular readback exactness, requiring $R$ to consume the same selected windows and tolerance ledger before any Real-facing seal is visible;
\item Real-seal nonescape, requiring $E$ to appear only after $F,\delta,S,T,R$ are transported through $H$ and replayed through $C$;
\item componentwise transport, displayed replay, package provenance, and local naming for exactly the rows $F,\delta,S,T,R,E,H,C,P,N$;
\item refusal of quotient stream equality, selected host limits, countable-choice schedules, trichotomy, ambient $\RealUp$ completeness, Lean verification, and bridge checking as local certificate data.
\end{enumerate}
No local diagonalization obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-diagonalization-carrier}. The accepted inventory is exactly the family row $F$, unary selector $\delta$, aligned StreamName windows $S$, dyadic tolerance ledger $T$, regular rational readback $R$, Real seal $E$, and structural rows $H,C,P,N$.

Selector exactness is read from $F$ and $\delta$. A consumer may inspect only the named source family entries selected by the displayed unary diagonal row, so no hidden choice schedule or host enumeration is available.

Window alignment and tolerance exactness are read from $S$ and $T$. The stream row exposes the finite windows selected by the diagonal read, while the dyadic row supplies the requested radius, split budget, comparison windows, and common-scale ledger used to compare those windows.

Regular readback and the Real seal are downstream of the same route. The row $R$ consumes $S$ and $T$ as one $\RegSeqRatUp$ diagonal readback, and $E$ is visible only after that readback has been transported and replayed.

Finally, $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier inventory contains no quotient-stream equality, selected host limit, countable-choice schedule, trichotomy, ambient completeness principle, Lean verification row, or bridge credential, the listed rows exhaust the local NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyDiagonalizationUp.RegularCauchyDiagonalizationTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy diagonalization Real seal handoff]
\label{thm:regular-cauchy-diagonalization-real-seal-handoff}
Every Real or Completion consumer of an accepted $\RegularCauchyDiagonalizationUp$ carrier factors through the finite route
$$
F,\delta \longmapsto S \longmapsto T \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Equivalently, the consumer receives a regular Cauchy family row, one unary diagonal selector, aligned $\StreamNameUp$ windows, one $\DyadicRatCoreUp$ tolerance ledger, one $\RegSeqRatUp$ diagonal readback, and one terminal $\RealUp$ seal before any Real-facing output is named.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:regular-cauchy-diagonalization-namecert-obligations}. It confines every accepted packet to the displayed family, selector, stream-window, tolerance-ledger, readback, Real-seal, and structural rows.

The consumer first reads $F$ and $\delta$. These rows name the source family and the unary diagonal selector, so the diagonal route has a displayed source for every inspected precision request.

The consumer then reads $S,T,R$. The aligned stream windows expose the selected entries, the dyadic ledger supplies the tolerance and comparison budget, and the regular rational readback repacks those rows as one $\RegSeqRatUp$ diagonal sequence.

Only after that route is visible may the consumer read the terminal $\RealUp$ seal $E$ and the structural rows $H,C,P,N$. Because the carrier has no coordinate for quotient stream equality, selected host limits, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking, none of those objects can enter the Real-facing handoff.
\end{proof}

\falsifiablePrediction{Every $\RegularCauchyDiagonalizationUp$ Real or Completion read factors through a regular Cauchy family row, one unary diagonal selector, aligned StreamName windows, one DyadicRatCore tolerance ledger, one RegSeqRat diagonal readback, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, regular-cauchy-diagonal-extraction: $\RegularCauchyDiagonalizationUp$ is not bijective to any listed sibling because it jointly carries a source regular Cauchy family, a unary diagonal selector, aligned finite windows, a dyadic tolerance ledger, a regular rational diagonal readback, and a terminal RealUp seal in one displayed packet.}

\closureat{RegularCauchyDiagonalizationUp}{seedStr}

\begin{closurestatus}{\RegularCauchyDiagonalizationUp}
  \constructivestory{A $\RegularCauchyDiagonalizationUp$ packet is generated from a regular Cauchy family row, one unary diagonal selector, aligned StreamName windows, one DyadicRatCore tolerance ledger, one RegSeqRat diagonal readback, one terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-diagonalization-carrier}, \autoref{thm:regular-cauchy-diagonalization-namecert-obligations}, and \autoref{thm:regular-cauchy-diagonalization-real-seal-handoff} seed the finite diagonalization packet used by L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected host limits, countable choice, trichotomy, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, selector exactness, window alignment, dyadic tolerance exactness, regular readback exactness, Real-seal exposure, transport stability, replay stability, provenance exhaustion, and local naming for BEDC.Derived.RegularCauchyDiagonalizationUp.}
\end{closurestatus}
