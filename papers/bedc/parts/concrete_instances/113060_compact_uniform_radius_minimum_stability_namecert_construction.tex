\chapter{A Concrete Naming Certificate for $\CompactUniformRadiusMinimumStabilityUp$}
\label{ch:concrete-instances-compact-uniform-radius-minimum-stability-namecert}
\origin{human}

Following Heine--Cantor and Bishop constructive analysis, the $\CompactUniformRadiusMinimumStabilityUp$ packet records the finite stability boundary for the radius-minimum fold used in compact uniform-continuity arguments. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the ProbeBundle lower-bound fold of \autoref{ch:concrete-instances-probe-bundle-finite-minimum-fold-namecert}, the finite-radius stability route of \autoref{ch:concrete-instances-finite-cover-radius-stability-namecert}, the metric comparison surface of \autoref{ch:concrete-instances-metric-namecert}, the rational lower-bound rows of \autoref{ch:concrete-instances-rat-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet exposes classifier stability of the finite minimum as concrete NameCert data rather than hiding it inside a fold step.

\begin{definition}[Compact uniform radius-minimum stability carrier]
\label{def:compact-uniform-radius-minimum-stability-carrier}
A $\CompactUniformRadiusMinimumStabilityUp$ carrier is a finite $\BHist$ packet
$$
M=(K,B,Q,S,G,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row whose centers carry local radius witnesses.
\item $B$ is the finite $\mathsf{ProbeBundle}$ row listing the radius witnesses, fold order, and bundle membership classifier.
\item $Q$ is the $\RatUp$ positive lower-bound row produced by the finite minimum fold.
\item $S$ is the classifier-stability row, recording that componentwise transport of the bundle rows preserves the selected minimum radius ledger.
\item $G$ is the $\MetricSpaceUp$ comparison row used by the compact-uniform triangle estimate.
\item $U$ is the $\UniformModulusUp$ handoff row, downstream of $K,B,Q,S,G$.
\item $H$ records componentwise $\hsame$ transport for compact-source, ProbeBundle, rational-fold, stability, metric, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through the finite bundle, radius-minimum fold, stability check, metric comparison, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformRadiusMinimumStabilityUp$, $\CompactMetricUp$, $\mathsf{ProbeBundle}$, $\RatUp$, $\MetricSpaceUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformRadiusMinimumStabilityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,B,Q,S,G,U,H,C,P,N$. It has no coordinate for host open-cover compactness, semantic continuity, a hidden minimum selector, quotient metric equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform radius-minimum stability obligations]
\label{thm:compact-uniform-radius-minimum-stability-namecert-obligations}
For every accepted $\CompactUniformRadiusMinimumStabilityUp$ carrier
$$
M=(K,B,Q,S,G,U,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformRadiusMinimumStabilityUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item compact finite-net admission through $K$;
\item finite ProbeBundle membership and fold-order exposure through $B$;
\item rational lower-bound exactness through $Q$;
\item classifier stability of the selected minimum ledger through $S$;
\item metric comparison and uniform-modulus handoff through $G,U$;
\item structural transport, replay, provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No radius-minimum stability obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-radius-minimum-stability-carrier}. The finite compact source, ProbeBundle row, rational fold, stability row, metric row, uniform-modulus row, and structural rows are exactly the displayed coordinates.

The compact source and bundle rows expose the finite list of radius witnesses. The rational fold row $Q$ records the selected positive lower bound as a history over $B$, not as an external selector.

The stability row $S$ is then read before any uniform-modulus consumer receives $Q$. It checks that componentwise transport of the compact-source and bundle classifiers preserves the lower-bound ledger used by the fold.

Finally $G,U,H,C,P,N$ route the stable radius minimum into the uniform-modulus handoff. Since the carrier contains no hidden open-cover compactness, semantic continuity, quotient metric equality, or ambient-completeness coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact uniform radius-minimum uniform-modulus handoff]
\label{thm:compact-uniform-radius-minimum-stability-uniform-modulus-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactUniformRadiusMinimumStabilityUp$ carrier receives the finite precision ledger only after reading
$$
\CompactMetricUp,\quad
\mathsf{ProbeBundle},\quad
\RatUp,\quad
\MetricSpaceUp,\quad
\UniformModulusUp .
$$
The handoff factors through the displayed radius-minimum stability row $S$, so classifier transport of the finite bundle cannot change the uniform-modulus radius without first changing a visible compact-source, bundle-membership, rational-fold, or metric-comparison row.
\end{theorem}
\begin{proof}
Start from \autoref{thm:compact-uniform-radius-minimum-stability-namecert-obligations}. The consumer can reach $U$ only after $K,B,Q,S,G$ have been exposed.

Read the finite minimum fold as in \autoref{thm:compact-uniform-finite-minimum-certificate}. The lower-bound row is produced by finite recursion over the ProbeBundle, so its classifier is determined by displayed bundle membership and rational comparison rows.

Now apply the stability row $S$. It transports the same finite fold through componentwise $\hsame$ data before the metric comparison and uniform-modulus rows are replayed.

Thus the exported uniform-modulus radius is stable under exactly the displayed compact-source, bundle, rational-fold, metric, transport, replay, provenance, and naming rows. No host open-cover proof, hidden selector, quotient metric equality, or ambient completeness row can alter the handoff.
\end{proof}

\falsifiablePrediction{Every $\CompactUniformRadiusMinimumStabilityUp$ read must expose CompactMetric finite-net data, ProbeBundle membership and fold order, RatUp lower-bound exactness, radius-minimum classifier stability, MetricSpace comparison, UniformModulus handoff, hsame transport, Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{compactmetric, probebundle, rat, metricspace, uniformmodulus: no listed sibling supplies the whole ten-row packet $M=(K,B,Q,S,G,U,H,C,P,N)$, because each sibling contributes only compact-source, finite-bundle, rational-fold, metric-comparison, or uniform-modulus rows while this chapter binds radius-minimum stability with transport, replay, provenance, and local naming.}

\closureat{CompactUniformRadiusMinimumStabilityUp}{seedStr}

\begin{closurestatus}{\CompactUniformRadiusMinimumStabilityUp}
  \constructivestory{A $\CompactUniformRadiusMinimumStabilityUp$ packet is generated from CompactMetric finite-net data, ProbeBundle membership and fold order, RatUp lower-bound exactness, radius-minimum classifier stability, MetricSpace comparison, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-radius-minimum-stability-carrier}, \autoref{thm:compact-uniform-radius-minimum-stability-namecert-obligations}, and \autoref{thm:compact-uniform-radius-minimum-stability-uniform-modulus-handoff} seed the finite classifier-stability surface for compact-uniform radius-minimum folds.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host open-cover compactness, semantic continuity, hidden minimum selection, quotient metric equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, ProbeBundle fold-order exposure, rational lower-bound exactness, classifier stability, metric comparison, uniform-modulus handoff, and non-escape for BEDC.Derived.CompactUniformRadiusMinimumStabilityUp.}
\end{closurestatus}
