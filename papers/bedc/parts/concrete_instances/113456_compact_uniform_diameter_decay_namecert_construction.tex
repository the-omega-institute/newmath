\chapter{A Concrete Naming Certificate for $\CompactUniformDiameterDecayUp$}
\label{ch:concrete-instances-compact-uniform-diameter-decay-namecert}
\origin{human}

Following the Heine--Cantor finite-net route, the $\CompactUniformDiameterDecayUp$ packet records the diameter-decay step that turns pointwise compact-cover moduli into a tolerance-only uniform-modulus ledger. It sits over the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational fold of \autoref{ch:concrete-instances-rat-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names finite compact-net diameter control; it does not choose points from a cover, assert host compactness, identify functions by quotient equality, or import ambient real completeness.

\begin{definition}[Compact uniform diameter-decay carrier]
\label{def:compact-uniform-diameter-decay-carrier}
A $\CompactUniformDiameterDecayUp$ carrier is a finite $\BHist$ packet
$$
D=(M,K,F,N,L,T,U,H,C,P)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ comparison row, recording the source and target distance ledgers used by the triangle estimate.
\item $K$ is the $\CompactMetricUp$ finite-net source row, including the inspected centers and cover radii.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row displayed at the finite centers.
\item $N$ is the finite net schedule whose cells carry the local diameter rows.
\item $L$ is the $\RatUp$ lower-bound fold row that produces the positive radius budget from the finite local ledgers.
\item $T$ is the triangle ledger combining source-net distance, local modulus radius, and target-distance tolerance.
\item $U$ is the $\UniformModulusUp$ output row reached after the finite diameter rows and rational lower-bound fold are displayed.
\item $H$ stores componentwise $\hsame$ transport for metric, compact-net, continuous-map, net schedule, rational fold, triangle, uniform-modulus, replay, provenance, and naming rows.
\item $C$ stores displayed $\Cont$ replay from compact source data through pointwise moduli and diameter decay into the uniform-modulus row.
\item $P$ is the local $\NameCert_{\CompactUniformDiameterDecayUp}$ row together with the $\Pkg$ provenance over $\CompactUniformDiameterDecayUp$, $\MetricUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\end{enumerate}
The classifier compares accepted packets only through $M,K,F,N,L,T,U,H,C,P$ and has no coordinate for external modulus selection, host function equality, arbitrary cover choice, quotient representation, or ambient compactness.
\end{definition}

\begin{theorem}[Compact uniform diameter-decay NameCert obligations]
\label{thm:compact-uniform-diameter-decay-namecert-obligations}
For every accepted $\CompactUniformDiameterDecayUp$ carrier $D=(M,K,F,N,L,T,U,H,C,P)$, the local $\NameCert_{\CompactUniformDiameterDecayUp}$ surface consists exactly of carrier admission for the metric comparison row, compact finite-net row, continuous-map pointwise row, finite diameter schedule, rational lower-bound fold, triangle ledger, uniform-modulus output row, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-diameter-decay-carrier}. Its displayed analytic rows are exactly the metric comparison row $M$, compact finite-net row $K$, pointwise continuous-map row $F$, finite diameter schedule $N$, rational fold $L$, triangle ledger $T$, and uniform-modulus row $U$.

The lower-bound fold and triangle ledger are read after the compact-net and pointwise-modulus rows. Hence the output uniform modulus is sourced by finite diameter-decay data rather than by a detached compactness or continuity theorem.

Rows $H,C,P$ transport, replay, source, and name the same route. Since no other coordinate is present, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact uniform diameter-decay uniform-modulus handoff]
\label{thm:compact-uniform-diameter-decay-uniform-modulus-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactUniformDiameterDecayUp$ packet factors through
$$
\MetricUp,\CompactMetricUp,\ContinuousMapUp \longmapsto N,L,T \longmapsto \UniformModulusUp
$$
together with $H,C,P$. Thus the consumer receives a tolerance-only modulus ledger after finite compact-net diameter decay, not an external modulus selector, host compactness theorem, arbitrary cover choice, quotient function equality, or ambient real-completeness claim.
\end{theorem}
\begin{proof}
Begin with the compact metric source and metric comparison rows. The finite net $K$ and distance ledger $M$ determine the source-side cells that will be inspected by the diameter-decay route.

Read the continuous-map row $F$ at the displayed centers. The finite schedule $N$ records the local diameter rows, while the rational fold $L$ supplies the positive radius budget used by the common tolerance request.

The triangle ledger $T$ combines source-cell control with target-distance estimates before $U$ is exported. The structural rows $H,C,P$ preserve this finite route and contain no selector, host equality, arbitrary cover-choice, quotient, compactness, or completeness coordinate.
\end{proof}

\falsifiablePrediction{An accepted $\CompactUniformDiameterDecayUp$ read is refuted if a compact-uniform-continuity consumer exports a $\UniformModulusUp$ row without displaying metric comparisons, compact finite-net rows, ContinuousMap pointwise moduli, finite diameter schedules, RatUp lower-bound folds, triangle ledgers, transport, replay, provenance, and local naming.}

\independenceWitness{metric, compactmetric, continuousmap, rat, uniformmodulus: none alone is bijective with $\CompactUniformDiameterDecayUp$ because the packet jointly carries metric comparisons, compact finite nets, pointwise moduli, finite diameter rows, rational lower-bound folds, triangle ledgers, uniform-modulus output, transport, replay, provenance, and local NameCert.}

\closureat{CompactUniformDiameterDecayUp}{seedStr}

\begin{closurestatus}{\CompactUniformDiameterDecayUp}
  \constructivestory{A $\CompactUniformDiameterDecayUp$ packet is generated from Metric distance rows, CompactMetric finite-net rows, ContinuousMap pointwise-modulus rows, finite diameter schedules, RatUp lower-bound folds, triangle ledgers, a UniformModulus output row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-diameter-decay-carrier}, \autoref{thm:compact-uniform-diameter-decay-namecert-obligations}, and \autoref{thm:compact-uniform-diameter-decay-uniform-modulus-handoff} seed the finite compact-net diameter-decay handoff into UniformModulus.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, external modulus selection, host function equality, arbitrary cover choice, quotient representation, ambient compactness, or RealUp completeness.}
  \upgradepath{Obligation closure requires checked compact-net exposure, pointwise-modulus visibility, rational lower-bound fold exactness, triangle ledger compatibility, uniform-modulus handoff, provenance, and local naming for BEDC.Derived.CompactUniformDiameterDecayUp.}
\end{closurestatus}
