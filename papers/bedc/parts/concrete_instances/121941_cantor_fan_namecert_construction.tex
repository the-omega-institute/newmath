\chapter{A Concrete Naming Certificate for $\CantorFanUp$}
\label{ch:concrete-instances-cantor-fan-namecert}
\origin{human}

Following classical continuum theory, the $\CantorFanUp$ packet names the finite-prefix compact metric fan obtained by joining Cantor-space branch data to a single apex through dyadic height rows. It consumes the Cantor-space source of \autoref{ch:concrete-instances-cantorspace-namecert}, the compact-metric and metric-space surfaces of \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-metric-space-namecert}, the continuous-map interface of \autoref{ch:concrete-instances-continuousmap-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. The chapter reconstructs the traditional fan as NameCert data over finite branch prefixes and dyadic heights; it does not use quotient equality of branches, host compactness, selected paths, or ambient real completeness.

\begin{definition}[Cantor fan carrier]
\label{def:cantor-fan-carrier}
A $\CantorFanUp$ carrier is a finite $\BHist$ packet
$$
F=(K,M,C,\Delta,W,R,D,A,E,H,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CantorSpaceUp$ row naming the binary branch-prefix source.
\item $M$ is the $\CompactMetricUp$ row naming the compact metric presentation visible to finite-net consumers.
\item $C$ is the $\MetricSpaceUp$ row whose distance observations are read only through displayed dyadic bounds.
\item $\Delta$ is the $\ContinuousMapUp$ row carrying the apex-to-branch and branch-height continuity witnesses.
\item $W$ is the $\StreamNameUp$ window family opened on branch prefixes and height prefixes.
\item $R$ is the $\RegSeqRatUp$ readback row for the displayed branch-height regular Cauchy data.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for prefix agreement, height comparison, and apex collapse.
\item $A$ is the finite apex row identifying the zero-height endpoint shared by all displayed branches.
\item $E$ is the edge classifier row distinguishing an apex read from a positive-height branch read.
\item $H$ stores componentwise $\hsame$ transport for all displayed rows.
\item $Q$ stores displayed $\Cont$ replay from Cantor prefixes and dyadic heights through the metric and compact rows.
\item $P$ is the $\Pkg$ provenance over $\CantorFanUp$, $\CantorSpaceUp$, $\CompactMetricUp$, $\MetricSpaceUp$, $\ContinuousMapUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorFanUp}$ row.
\end{enumerate}
The carrier exports no quotient class of branches, selected infinite path, host continuum theorem, open-cover compactness proof, or ambient complete real line.
\end{definition}

\begin{theorem}[Cantor fan NameCert]\label{thm:cantor-fan-namecert}
For every accepted $\CantorFanUp$ carrier
$$
F=(K,M,C,\Delta,W,R,D,A,E,H,Q,P,N),
$$
the local $\NameCert_{\CantorFanUp}$ surface consists exactly of Cantor-space prefix admission, compact-metric finite-net exposure, metric distance exposure, continuous apex-and-height transport, StreamName finite-window exposure, RegSeqRat readback, DyadicRatCore tolerance control, apex classification, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.

The classifier is stable under any componentwise transport that preserves the displayed Cantor prefixes, dyadic height ledger, stream windows, regular readback, metric row, compact row, continuous-map row, and apex classifier. A consumer therefore receives the fan only as the finite ledger $K,M,C,\Delta,W,R,D,A,E,H,Q,P,N$.
\end{theorem}
\begin{proof}
Carrier exposure is projection from \autoref{def:cantor-fan-carrier}. The row $K$ supplies the finite Cantor-prefix source, while $D,W,R$ supply the dyadic height and prefix windows through which a consumer can inspect a branch-height point.

The classifier separates two displayed cases. In the apex case, $A$ is read with the zero-height dyadic row and all branch prefixes collapse only through the same displayed tolerance ledger. In the positive-height case, $E$ keeps the branch-prefix row visible together with the metric and compact rows $C,M$.

Continuity and compactness are consumed as finite NameCert rows, not as host principles. The row $\Delta$ records the apex and branch-height continuity handoff, and $M$ records the compact-metric finite-net surface exposed to downstream consumers.

Finally $H,Q,P,N$ transport, replay, source, and name that same finite packet. Since the carrier has no coordinate for quotient branch equality, selected paths, open-cover compactness, or ambient completeness, the listed rows exhaust the $\CantorFanUp$ NameCert surface.
\end{proof}

\closureat{CantorFanUp}{seedStr}

\begin{closurestatus}{\CantorFanUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-fan-carrier} and \autoref{thm:cantor-fan-namecert} seed the finite Cantor-prefix, compact-metric, metric, continuous-map, dyadic-height, StreamName, RegSeqRat, apex-classifier, transport, replay, provenance, and local naming surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient equality of Cantor branches, selected infinite paths, host continuum compactness, open-cover compactness, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, compact-metric finite-net exposure, continuous apex transport, dyadic height control, stream-window readback, and local naming for BEDC.Derived.CantorFanUp.}
  \constructivestory{A $\CantorFanUp$ packet is generated from CantorSpace prefixes, CompactMetric and MetricSpace rows, a ContinuousMap apex-height route, DyadicRatCore tolerances, StreamName windows, RegSeqRat readback, apex classification, componentwise transport, displayed replay, provenance, and a local NameCert row.}
\end{closurestatus}
