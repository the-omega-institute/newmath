\chapter{A Concrete Naming Certificate for $\FanTheoremBarModulusUp$}
\label{ch:concrete-instances-fan-theorem-bar-modulus-namecert}
\origin{human}

Following Brouwer's fan theorem and Bishop--Bridges constructive uniform continuity, the $\FanTheoremBarModulusUp$ packet records the finite bar-modulus extraction route by which a decidable fan bar yields a tolerance-only $\UniformModulusUp$ handoff. It sits over the finite fan certificate of \autoref{ch:concrete-instances-fantheorem-namecert}, the Bishop fan-modulus schedule of \autoref{ch:concrete-instances-bishop-fan-modulus-namecert}, the fan-modulus boundary of \autoref{ch:concrete-instances-fan-modulus-boundary-namecert}, the compact-uniform modulus carrier of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}, and the downstream consumer surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names only displayed finite bar coverage, local modulus rows, rational tolerance rows, and structural replay; it does not import open-cover compactness, countable choice, selected infinite branches, quotient stream equality, or ambient $\RealUp$ completeness.

\begin{definition}[Fan-theorem bar-modulus carrier]
\label{def:fan-theorem-bar-modulus-carrier}
A $\FanTheoremBarModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(F,B,W,L,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\FanTheoremUp$ finite fan row, recording the binary prefix tree, decidable bar classifier, unary bar-depth ledger, and finite witness window.
\item $B$ is the bar-coverage row, asserting that every admitted branch-prefix request enters the displayed bar before the depth named by $F$.
\item $W$ is the finite window row that converts the bar-depth ledger into the prefix window inspected by compact-uniform and Cantor-space modulus consumers.
\item $L$ is the local modulus ledger, listing the pointwise modulus rows attached to the finitely many retained prefixes or compact probes.
\item $R$ is the positive $\RatUp$ tolerance row used to compare the local radii and to choose the tolerance-only precision exported downstream.
\item $U$ is the $\UniformModulusUp$ handoff row, exposing the extracted precision as a modulus ledger after $F,B,W,L,R$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the fan, bar-coverage, window, local-modulus, rational-tolerance, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite extraction route
$$
F,B,W,L,R \longmapsto U .
$$
\item $P$ is the $\Pkg$ provenance over $\FanTheoremBarModulusUp$, $\FanTheoremUp$, $\BishopFanModulusUp$, $\FanModulusBoundaryUp$, $\CompactUniformModulusUp$, $\UniformModulusUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FanTheoremBarModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,B,W,L,R,U,H,C,P,N$. It has no coordinate for open-cover compactness, countable choice, a selected infinite branch, a selected subcover, quotient stream equality, a host modulus selector, or ambient real completeness.
\end{definition}

\begin{theorem}[Fan-theorem bar-modulus NameCert obligations]
\label{thm:fan-theorem-bar-modulus-namecert-obligations}
For every accepted $\FanTheoremBarModulusUp$ carrier $M=(F,B,W,L,R,U,H,C,P,N)$, the local $\NameCert_{\FanTheoremBarModulusUp}$ surface consists exactly of carrier admission, finite fan-bar exposure through $F$, bar-coverage exactness through $B$, finite window extraction through $W$, local modulus exposure through $L$, positive rational tolerance accounting through $R$, $\UniformModulusUp$ handoff through $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

In particular, every compact-uniform-continuity or finite fan consumer that extracts a uniform modulus from this packet must first read the finite fan row, the bar-coverage row, the finite window row, the local modulus ledger, and the rational tolerance row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-theorem-bar-modulus-carrier}. The projection lists exactly the finite fan, bar coverage, window, local modulus, rational tolerance, uniform-modulus, transport, replay, provenance, and local naming rows.

The fan and bar clauses are finite. The row $F$ supplies the displayed tree, bar decisions, depth ledger, and witness window, while $B$ states that branch-prefix requests meet the displayed bar before that depth. No selected branch or open cover is introduced.

The extraction clauses are $W,L,R$. The window row converts the bar depth into the finite family of inspected prefixes, the local modulus row attaches pointwise modulus data to that family, and the rational row stores the positive tolerance comparisons used to choose one exported precision.

Finally, $U,H,C,P,N$ expose, transport, replay, source, and name the extracted precision as a $\UniformModulusUp$ handoff. Since the carrier has no coordinate for open-cover compactness, countable choice, selected branches, selected subcovers, quotient stream equality, host modulus selection, or ambient completeness, the stated obligations exhaust the packet.
\end{proof}

\begin{theorem}[Fan-theorem finite bar modulus extraction]
\label{thm:fan-theorem-finite-bar-modulus-extraction}
Every $\UniformModulusUp$ read of an accepted $\FanTheoremBarModulusUp$ carrier factors through
$$
F,B,W,L,R,U,H,C,P,N .
$$
The exported precision is obtained only from the finite fan bar, its coverage row, the extracted prefix window, the local modulus ledger, and the positive rational tolerance ledger. The read cannot be replaced by open-cover compactness, a chosen infinite branch, a selected subcover, countable choice, quotient stream equality, a host modulus selector, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with a $\UniformModulusUp$ read of the accepted packet. By \autoref{thm:fan-theorem-bar-modulus-namecert-obligations}, the read must first expose $F,B,W,L,R$.

The route is finite at each step. The fan theorem row bounds branch-prefix inspection by a unary depth, the coverage row certifies that the displayed bar is met within that depth, and the window row lists the prefixes that may be inspected.

The local modulus and rational tolerance rows then determine the exported precision. The finite family of local modulus rows is read through $L$, and the positive rational comparisons in $R$ select the tolerance-only lower bound delivered to $U$.

The structural rows $H,C,P,N$ preserve the same route under transport, continuation replay, provenance, and local naming. Because the tuple contains no stronger compactness, choice, quotient, branch-selection, subcover-selection, host-selector, or completeness coordinate, every $\UniformModulusUp$ read factors through exactly the displayed finite packet.
\end{proof}

\falsifiablePrediction{Every accepted $\FanTheoremBarModulusUp$ uniform-modulus read factors through a finite FanTheorem row, bar-coverage row, finite window row, local modulus ledger, positive RatUp tolerance row, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{fantheorem, bishop-fan-modulus, fan-modulus-boundary, compact-uniform-modulus, uniformmodulus: $\FanTheoremBarModulusUp$ is not bijective to any listed sibling because it isolates the finite bar-coverage-to-tolerance extraction route before the UniformModulus handoff, while refusing open-cover compactness, choice, selected branches, selected subcovers, quotient stream equality, host modulus selectors, and ambient completeness.}

\closureat{FanTheoremBarModulusUp}{seedStr}

\begin{closurestatus}{\FanTheoremBarModulusUp}
  \constructivestory{A $\FanTheoremBarModulusUp$ packet is generated from a finite FanTheorem row, bar-coverage exactness, finite window extraction, local modulus rows, positive RatUp tolerance accounting, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fan-theorem-bar-modulus-carrier}, \autoref{thm:fan-theorem-bar-modulus-namecert-obligations}, and \autoref{thm:fan-theorem-finite-bar-modulus-extraction} seed the finite fan-bar coverage route that extracts a tolerance-only UniformModulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, countable choice, selected infinite branches, selected subcovers, quotient stream equality, host modulus selection, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bar-coverage exactness, finite window extraction, local modulus exposure, RatUp tolerance accounting, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.FanTheoremBarModulusUp.}
\end{closurestatus}
