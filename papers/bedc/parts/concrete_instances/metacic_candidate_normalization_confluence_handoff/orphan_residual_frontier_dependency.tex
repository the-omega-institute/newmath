\begin{theorem}[MetaCIC candidate confluence orphan route]
\label{thm:metacic-candidate-confluence-orphan-route}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ read that exports the normalization/confluence bridge route first consumes the residual-frontier rows of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. The consumed rows are the retained residual-substitution premise, critical-pair row, candidate-join row, residual-frontier row, closed-checker row, closed-normal fragment row, bounded-check row, obstruction row, transport, replay, provenance, and local naming surface; the handoff does not export subject reduction, unrestricted confluence, evaluator equality, quotient term equality, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Project the handoff carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. Its candidate, bounded endpoint, residual confluence, decidable-boundary, blocked-edge, transport, replay, provenance, and local naming rows can be read only as a finite handoff packet.

Next read the upstream frontier through \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. That chapter displays the retained premise row $P$, critical-pair row $K$, candidate-join row $J$, residual-frontier row $R$, checker rows $S,C,B$, obstruction row $O$, and structural rows $H,T,G,N$ before a confluence-facing consumer can inspect the local-diamond handoff.

Finally combine this frontier read with the existing bridge-budget route of \autoref{thm:metacic-candidate-normalization-parallel-frontier-residual-bridge-consumption}. The bridge route consumes exactly the displayed residual-frontier dependency before exporting normalization/confluence data, so the handoff remains inside the finite dependency lattice and none of the refused global endpoints is exposed.
\end{proof}
