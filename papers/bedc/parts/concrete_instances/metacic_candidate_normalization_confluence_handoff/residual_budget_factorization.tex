\begin{theorem}[MetaCIC candidate normalization confluence handoff residual budget exactness]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-budget-exactness}
The $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet exposes residual-budget factorization only after candidate evidence $K$, normal endpoint $N$, frontier row $F$, residual confluence row $C$, decidable boundary $D$, blocked edge $B$, transport $T$, replay $R$, provenance $P$, and local name $L$ have all been displayed. This finite route keeps the bounded residual budget tied to the carrier rows of the handoff packet and to the sibling confluence surfaces of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, and \autoref{ch:concrete-instances-parallel-diamond-residual-namecert}. The row asserts the local residual-budget exactness needed by the handoff; it does not claim full Church--Rosser, unrestricted subject reduction, evaluator soundness, quotient syntax equality, or closed consistency.
\end{theorem}
\begin{proof}
Project the candidate-normalization handoff packet. The finite route already displays candidate evidence, the bounded endpoint, the frontier row, the residual confluence row, the decidable boundary, the blocked edge, the transport row, the replay row, the provenance row, and the local name row before any residual budget is read.

Apply the sibling surfaces named in the statement. The critical-path row supplies the candidate anchor, the parallel-diamond frontier supplies the residual confluence boundary, and the residual-diamond sibling keeps the bounded budget local. Since the packet has no coordinate for full Church--Rosser, unrestricted subject reduction, evaluator soundness, quotient syntax equality, or closed consistency, the theorem records only the exact residual budget surface.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization residual budget factorization]
\label{thm:metacic-candidate-normalization-residual-budget-factorization}
Any accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ residual budget factors through candidate evidence $K$, normal endpoint $N$, frontier row $F$, residual confluence row $C$, decidable boundary $D$, blocked edge $B$, transport $T$, replay $R$, provenance $P$, and local name $L$ before any downstream residual join consumes it. The factorization routes through \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, and \autoref{ch:concrete-instances-parallel-diamond-residual-namecert}; it exports no residual join that bypasses the candidate evidence, normal endpoint, frontier row, decidable boundary, blocked-edge ledger, structural transport, replay, provenance, or local naming.
\end{theorem}
\begin{proof}
Project the displayed handoff packet. Its residual-budget row can be read only after the candidate evidence, normal endpoint, frontier row, residual confluence row, decidable boundary, blocked edge, transport, replay, provenance, and local name have all been exposed.

Use \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-budget-exactness} for the exactness surface. The same displayed rows already bind the budget to the carrier rather than to a detached residual-join theorem.

Finally read the sibling routes. $\MetaCICCriticalPathUp$ supplies the candidate-critical anchor, $\MetaCICParallelDiamondFrontierUp$ supplies the frontier boundary, and $\ParallelDiamondResidualUp$ supplies the bounded residual-confluence row. Since the packet contains no coordinate outside those rows, every downstream residual join consumes the stated factorization first.
\end{proof}
