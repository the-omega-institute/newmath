The $\MetacicCandidateNormalizationConfluenceHandoffUp$ dossier-lattice anchor consumes the parallel-diamond frontier route of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} and the local-confluence certificate route of \autoref{ch:concrete-instances-metacic-local-confluence-frontier-namecert} before residual-normalization handoff rows are public. Thus the handoff keeps candidate evidence, residual-envelope visibility, decidable-boundary routing, blocked-edge retention, transport, replay, provenance, and local naming attached to those sibling routes instead of exporting a standalone confluence endpoint.
