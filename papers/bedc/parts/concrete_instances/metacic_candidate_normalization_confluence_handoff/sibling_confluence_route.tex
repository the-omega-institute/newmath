\begin{theorem}[MetaCIC candidate normalization sibling confluence route]
\label{thm:metacic-candidate-normalization-confluence-handoff-sibling-route}
Every confluence-facing read of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must first expose the sibling route through \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}, \autoref{ch:concrete-instances-metacic-residual-join-determinacy-namecert}, and \autoref{ch:concrete-instances-parallel-diamond-residual-namecert}. The residual local-join determinacy packet supplies the finite residual-join ledger consumed before the candidate endpoint can be treated as confluence-facing evidence. The handoff may then export candidate-mediated endpoint, residual-envelope, decidable-boundary, and blocked-edge rows to a confluence consumer, but it cannot present a standalone confluence theorem before those sibling chapters have been displayed.
\end{theorem}
\begin{proof}
Project the handoff carrier from \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. Its public confluence route stores audit, candidate, frontier, confluence, decidable-boundary, blocked-edge, transport, replay, provenance, and local naming rows rather than a detached confluence theorem.

Apply the boundary and consumer-boundary theorems of this chapter. They keep the candidate evidence, normalization frontier, residual envelope, decidable boundary, and blocked edge visible before any endpoint or join row is consumed.

The sibling chapters named in the statement supply the critical-path, normalization-frontier, confluence-audit, residual local-join, and residual-diamond obstruction surfaces for those rows. The local-join determinacy route contributes the finite join ledger that the candidate endpoint must consume before it is exported as confluence-facing evidence. Residual obstruction rows therefore route through the residual-join and parallel-diamond siblings rather than through a standalone confluence theorem. Since no carrier coordinate bypasses them, every confluence-facing handoff factors through the displayed sibling route.
\end{proof}
