\begin{theorem}[MetaCIC candidate normalization confluence handoff route factorization]
\label{thm:metacic-candidate-normalization-confluence-handoff-route-factorization}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet consumed by the $\MetaCICParallelDiamondFrontierUp$ route or by the normalization-confluence bridge must first expose the residual-budget factorization row of \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-budget-exactness}. The factorization keeps the candidate evidence, normal endpoint, frontier row, residual confluence row, decidable boundary, blocked edge, transport, replay, provenance, and local name visible before the handoff reaches \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} or the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}. It does not claim full confluence, subject reduction, evaluator equality, quotient syntax equality, or closed consistency.
\end{theorem}
\begin{proof}
Project the local residual-budget exactness theorem. It displays candidate evidence, the normal endpoint, frontier row, residual confluence row, decidable boundary, blocked edge, transport, replay, provenance, and local naming before any residual budget is read.

Compose that projection with the parallel-diamond frontier route. The frontier consumer may read the handoff only after the residual-budget factorization row has fixed the bounded endpoint and kept the residual-substitution premise visible.

The same displayed row is the local premise for the normalization-confluence bridge. Since the packet has no coordinate for full confluence, subject reduction, evaluator equality, quotient syntax equality, or closed consistency, route composition exports only the residual-budget factorization boundary.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization confluence handoff critical-path consumption]
\label{thm:metacic-candidate-normalization-confluence-handoff-critical-path-consumption}
Any $\MetaCICCriticalPathUp$ or $\MetaCICParallelDiamondFrontierUp$ consumer of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must first read the residual-budget factorization row of \autoref{thm:metacic-candidate-normalization-confluence-handoff-route-factorization}. The consumed route keeps candidate evidence, normal endpoint, frontier row, residual confluence row, decidable boundary, blocked edge, transport, replay, provenance, and local name visible before the handoff is used by the critical path or the parallel-diamond frontier. It does not discharge full confluence, subject reduction, evaluator equality, quotient syntax equality, closed consistency, or residual-substitution.
\end{theorem}
\begin{proof}
Start with the route factorization theorem. It forces the handoff to expose the residual-budget row before any downstream consumer can use the normalization-confluence boundary.

For a $\MetaCICCriticalPathUp$ consumer, compose that factorization with the critical-path route. The critical path may retain the candidate evidence, normal endpoint, frontier row, residual confluence row, decidable boundary, blocked edge, transport, replay, provenance, and local name, but it cannot skip the residual-budget boundary.

For a $\MetaCICParallelDiamondFrontierUp$ consumer, compose the same factorization with the parallel-diamond frontier route. The frontier read receives the handoff only after the residual-budget row has fixed the bounded endpoint and kept the residual-substitution premise visible.

The packet contains no carrier row for full confluence, subject reduction, evaluator equality, quotient syntax equality, closed consistency, or residual-substitution discharge. Therefore critical-path and frontier consumption preserve exactly the retained-premise boundary supplied by the handoff.
\end{proof}
