\begin{theorem}[Candidate normalization handoff parallel-diamond frontier route]
\label{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-route}
Any $\MetaCICCandidateNormalizationConfluenceHandoffUp$ consumer that exports a local diamond budget must first cite \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. The export preserves the residual-substitution premise, candidate-join row, bounded-check row, obstruction row, transport row, replay row, provenance row, and local $\NameCert$ row before the handoff may be read as a frontier-facing diamond route.
\end{theorem}
\begin{proof}
Read the existing parallel-diamond dependency route for the handoff. It places the candidate-normalization budget on the same finite route as the frontier packet rather than on a global confluence theorem.

The cited frontier chapter supplies the public location for that route. A consumer that exports a local diamond budget therefore reaches the frontier only after preserving the residual-substitution premise, candidate join, bounded check, obstruction, transport, replay, provenance, and local naming rows.

Thus the handoff is a displayed dependency through $\MetaCICParallelDiamondFrontierUp$. It does not export unrestricted residual substitution, global Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}
