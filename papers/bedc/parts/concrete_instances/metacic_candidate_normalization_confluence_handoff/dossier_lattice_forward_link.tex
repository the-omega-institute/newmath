The $\MetaCICCandidateNormalizationConfluenceHandoffUp$ dossier lattice is consumed by \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} and \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert} only through candidate evidence rows, residual-envelope visibility, decidable-boundary routing, and blocked-edge retention. A downstream confluence reader must therefore keep the handoff as a finite evidence packet rather than reading it as a completed normalization theorem or as an untracked frontier shortcut.
