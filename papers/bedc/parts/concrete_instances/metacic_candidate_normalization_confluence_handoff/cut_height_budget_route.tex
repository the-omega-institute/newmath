\begin{theorem}[MetaCIC candidate-normalization handoff cut-height budget route]
\label{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget}
Any $\MetaCICCandidateNormalizationConfluenceHandoffUp$ consumer that reads a cut-height budget or local-diamond endpoint must first expose candidate evidence, the normalization frontier, residual confluence, the decidable-boundary route, and blocked-edge rows in the same finite handoff packet. In particular, the consumer must cite the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} and the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert} before any cut-height or local-diamond row is consumed.
\end{theorem}
\begin{proof}
Project the candidate-normalization confluence handoff carrier from the local NameCert construction. Its public rows expose candidate evidence, bounded normal-endpoint data, residual-envelope visibility, decidable-boundary routing, blocked-edge retention, transport, replay, provenance, and local naming.

The cut-height consumer is downstream of those rows. A local-diamond read needs the residual confluence and blocked-edge rows, while a budget read needs the normalization frontier and decidable-boundary rows before it can decide which bounded endpoint is being measured.

Now read the sibling frontiers. The parallel-diamond frontier supplies the residual diamond and local-join surface, and the normalization frontier supplies the bounded normal-endpoint surface. Since the handoff has no separate cut-height coordinate outside those displayed routes, every accepted cut-height or local-diamond consumer must first display the stated rows.
\end{proof}
