\begin{theorem}[MetaCIC candidate normalization local-confluence route]
\label{thm:metacic-candidate-normalization-local-confluence-route}
Any local-confluence consumer of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must first read the candidate-normalization carrier rows, the candidate diamond-budget dependency of \autoref{ch:concrete-instances-metacic-candidate-diamond-budget-namecert}, the residual bridge route inside the handoff packet, and then the local-confluence frontier of \autoref{ch:concrete-instances-metacic-local-confluence-frontier-namecert}. The resulting route is a finite confluence-facing dependency chain; it is not a standalone local-confluence theorem, unrestricted Church--Rosser, subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, or kernel consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. A local-confluence read starts from the same audit anchor, candidate evidence, bounded endpoint, normalization frontier, residual envelope, decidable boundary, blocked edge, transport, replay, provenance, and local naming rows as every accepted handoff read.

Apply the candidate diamond-budget dependency route. It forces the consumer to keep the bounded local peak and join-budget rows visible before any endpoint row is treated as confluence-facing evidence.

Next read the residual bridge route inside this hub. The residual envelope, decidable-boundary row, and blocked-edge ledger remain displayed, so the handoff cannot erase residual-substitution or critical-pair obligations on the way to a local-confluence consumer.

Finally pass to the local-confluence frontier of \autoref{ch:concrete-instances-metacic-local-confluence-frontier-namecert}. That sibling may consume the exposed candidate and residual rows as a frontier premise, but it receives no coordinate for global confluence, subject reduction, quotient syntax equality, evaluator equality, conversion decidability, or kernel consistency.
\end{proof}
