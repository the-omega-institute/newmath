The candidate-normalization handoff consumes the residual-join and local-confluence routes through \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} and reads the Real and RegSeqRat-style finite route discipline of \autoref{ch:concrete-instances-regular-cauchy-diagonal-meet-namecert} only as packet ordering. This dependency records the visible sibling path for residual joins, local diamonds, finite readback rows, transport, replay, provenance, and local naming; it does not select a normalization, quotient terms, identify evaluator equality, or turn the Real-facing ordering discipline into a MetaCIC equality principle.
