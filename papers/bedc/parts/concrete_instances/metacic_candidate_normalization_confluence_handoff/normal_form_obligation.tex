\begin{theorem}[MetaCIC candidate normalization confluence handoff normal-form obligation]
\label{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}
Any normal-form read exported by an accepted
$\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier must first expose
the candidate-normalization row, the residual-diamond row, and the sibling
confluence route. Concretely, the read must pass through the candidate gate and
frontier rows of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}, the
premise-exposure and normal-endpoint rows of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}
and
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback},
the residual-diamond retention row of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention},
the bounded candidate-diamond route of
\autoref{thm:metacic-candidate-normalization-diamond-budget-route}, and the
sibling confluence route of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-sibling-route}
before any normal-form endpoint is available. The obligation is a finite
premise-exposure row only; it does not assert host normalization, global
confluence, unrestricted subject reduction, quotient syntax equality, evaluator
soundness, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier from
\autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. A
normal-form read can see only the candidate, frontier, residual, decidable,
blocked-edge, transport, replay, provenance, and local naming rows displayed in
that carrier.

Apply the boundary and premise-exposure theorems. They force the candidate gate
and normalization frontier to appear before the normal endpoint is consumed,
and they keep the residual envelope, decidable boundary, and blocked-edge rows
visible for confluence-facing reads.

Next apply normal-endpoint readback and residual-diamond retention. These rows
show that an endpoint can be handed to confluence consumers only while
candidate evidence, residual data, decidable-boundary data, and blocked edges
remain present in the same packet.

Finally use the diamond-budget dependency and sibling confluence route. The
candidate diamond supplies bounded local peak and join-budget rows, and the
sibling route exposes the critical-path, normalization-frontier,
confluence-audit, and parallel-diamond residual surfaces before the normal-form
read is available. Since no carrier coordinate contains a host-level
normalization or global confluence theorem, the obligation remains the finite
handoff row stated above.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization confluence handoff obstruction retention]
\label{thm:metacic-candidate-normalization-confluence-handoff-obstruction-retention}
Every consumer of the candidate-normalization normal-form row exported by an
accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must retain
the residual-substitution premise, candidate diamond budget dependency,
cut-height budget route, and parallel frontier route before treating that row
as confluence-facing evidence. The retained route is the finite chain through
\autoref{thm:metacic-candidate-normalization-diamond-budget-route},
\autoref{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget},
\autoref{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-route},
and the residual-diamond bridge route. It does not export subject reduction,
unrestricted Church--Rosser, evaluator equality, quotient term equality, or
kernel consistency.
\end{theorem}
\begin{proof}
Start with the residual budget theorem
\autoref{thm:metacic-candidate-normalization-residual-budget}. It forces
candidate evidence, residual-diamond retention, decidable-boundary data,
blocked-edge rows, bounded local peaks, join-budget rows, and the sibling
confluence route to remain visible before a normal-form endpoint is exported.

Apply the candidate diamond budget dependency
\autoref{thm:metacic-candidate-normalization-diamond-budget-route}. The
normal-form row is therefore read only after bounded local peak and join-budget
data have been displayed; it is not an evidence-free confluence endpoint.

Next use the cut-height budget route and the parallel-diamond frontier route,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget}
and
\autoref{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-route}.
These routes keep the normalization frontier, residual confluence, decidable
boundary, blocked-edge rows, and frontier-facing residual-substitution premise
attached to the handoff.

Finally consume the residual-diamond bridge route recorded in the sibling
route file. The bridge sends only finite residual-diamond dependency data
onward while preserving residual-substitution visibility. Since the carrier has
no coordinate for subject reduction, unrestricted Church--Rosser, evaluator
equality, quotient term equality, or kernel consistency, every
confluence-facing read retains the obstruction instead of erasing it.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization normal-form budget]
\label{thm:metacic-candidate-normalization-confluence-handoff-normal-form-budget}
Every accepted normal-form endpoint exported by a
$\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier must retain the
candidate gate, residual-diamond row, cut-height budget, parallel-diamond
frontier route, and sibling confluence route as one finite budget surface before
it can be used as local confluence evidence. The endpoint is therefore consumed
only through the normal-form obligation, obstruction-retention row,
diamond-budget route, cut-height budget route, and parallel-diamond frontier
route; it is not global Church--Rosser, unrestricted subject reduction,
quotient syntax equality, evaluator equality, or consistency.
\end{theorem}
\begin{proof}
Start with the normal-form obligation of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}.
It places every endpoint downstream of the candidate-normalization row, the
residual-diamond row, and the sibling confluence route before any consumer can
read it.

Apply obstruction retention,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-obstruction-retention}.
The residual-substitution premise, candidate diamond dependency, cut-height
budget, and parallel frontier route remain attached to the same handoff row
rather than being discharged by the endpoint.

Next use the diamond-budget route and cut-height budget route,
\autoref{thm:metacic-candidate-normalization-diamond-budget-route} and
\autoref{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget}.
These rows expose bounded local peaks, join-budget data, normalization-frontier
data, residual confluence, decidable-boundary routing, and blocked-edge
retention as finite budget coordinates.

Finally apply the parallel-diamond frontier route,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-route},
and the sibling confluence route
\autoref{thm:metacic-candidate-normalization-confluence-handoff-sibling-route}.
They keep the frontier and sibling residual surfaces visible before the
endpoint is treated as local confluence evidence. Since none of the cited rows
contains a global Church--Rosser theorem, subject reduction, quotient syntax
equality, evaluator equality, or consistency, the endpoint remains the retained
finite budget surface stated above.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization normal-form budget]
\label{thm:metacic-candidate-normalization-normal-form-budget}
Every local-confluence consumer of an accepted normal-form endpoint exported by
a $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must retain the
candidate gate, residual-diamond row, residual-substitution premise,
cut-height budget, parallel frontier route, sibling confluence route, and local
$\NameCert$ row as one finite budget packet. The endpoint is therefore consumed
only through the normal-form obligation, obstruction-retention row, candidate
diamond budget route, cut-height budget route, and parallel-diamond frontier
route before it can be read as local confluence evidence.

This retained packet explicitly excludes unrestricted Church--Rosser, subject
reduction, quotient syntax equality, evaluator equality, and kernel
consistency.
\end{theorem}
\begin{proof}
Start with the normal-form obligation of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}.
It places every endpoint after the candidate gate, residual-diamond row, and
sibling confluence route, so the consumer cannot read the endpoint as a detached
normal form.

Apply obstruction retention,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-obstruction-retention}.
That row keeps the residual-substitution premise, candidate diamond dependency,
cut-height budget, and parallel frontier route attached to the endpoint as
confluence-facing evidence.

Use the candidate diamond budget route,
\autoref{thm:metacic-candidate-normalization-diamond-budget-route}, together
with the cut-height budget route
\autoref{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget}.
They expose the bounded local peak, join-budget, cut-height, normalization
frontier, residual confluence, decidable boundary, and blocked-edge rows as
finite budget coordinates.

Finally apply the parallel-diamond frontier route,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-route},
and the sibling confluence route
\autoref{thm:metacic-candidate-normalization-confluence-handoff-sibling-route}.
The local $\NameCert$ row names exactly that finite packet. Since none of these
rows contains unrestricted Church--Rosser, subject reduction, quotient syntax
equality, evaluator equality, or kernel consistency, the consumer receives only
the retained normal-form budget.
\end{proof}

\begin{theorem}[MetaCIC residual normal-form frontier]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-normal-form-frontier}
An accepted normal-form endpoint of a
$\MetacicCandidateNormalizationConfluenceHandoffUp$ packet may be consumed as
local confluence evidence only while one finite frontier packet still displays
the candidate gate, residual-diamond row, residual-substitution premise,
cut-height budget, parallel frontier route, and sibling confluence route. The
endpoint is therefore read through the normal-form obligation, obstruction
retention, normal-form budget, diamond-budget route, cut-height route, and
parallel frontier route before any local-confluence consumer sees it.

The frontier packet preserves obstruction rows. It does not export subject
reduction, unrestricted Church--Rosser, evaluator equality, quotient syntax
equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the normal-form obligation,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}.
It places the endpoint after the candidate gate, residual-diamond row, and
sibling confluence route, so the endpoint is never presented as an isolated
normal form.

Apply obstruction retention and the normal-form budget theorem,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-obstruction-retention}
and
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-budget}.
These rows keep the residual-substitution premise, residual-diamond row,
cut-height budget, parallel frontier route, and sibling confluence route
attached to the same finite handoff packet.

Next use the diamond-budget and cut-height routes,
\autoref{thm:metacic-candidate-normalization-diamond-budget-route} and
\autoref{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget}.
They expose bounded local peak, join-budget, and cut-height data as visible
frontier coordinates rather than as hidden global confluence assumptions.

Finally apply the parallel-diamond frontier route,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-route}.
Together with the sibling confluence route, it leaves the residual and frontier
rows visible when the endpoint is consumed as local confluence evidence. Since
the carrier has no coordinate for subject reduction, unrestricted
Church--Rosser, evaluator equality, quotient syntax equality, global
normalization, or kernel consistency, the endpoint remains a finite
obstruction-preserving frontier read.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization normal-form obligation route]
\label{thm:metacic-candidate-normalization-normal-form-obligation-route}
Every normal-form endpoint read exported by a
$\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier must pass through
the candidate gate, residual-diamond retention, cut-height budget,
parallel-diamond frontier route, sibling confluence route, and local
$\NameCert$ rows before it can be used as confluence-facing evidence. The route
retains the residual and frontier evidence attached to the endpoint; it does not
export subject reduction, unrestricted Church--Rosser, evaluator equality,
quotient syntax equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the normal-form obligation of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}.
It places every endpoint downstream of the candidate gate, residual-diamond row,
and sibling confluence route before any consumer can read it.

Apply obstruction retention,
\autoref{thm:metacic-candidate-normalization-confluence-handoff-obstruction-retention}.
The retained residual-substitution premise, candidate diamond dependency,
cut-height budget, and parallel frontier route remain attached to the endpoint
instead of being discharged by the normal-form read.

Finally use the normal-form budget theorem
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-budget}.
It keeps the cut-height budget, parallel-diamond frontier surface, sibling
confluence route, and local naming rows visible before the endpoint is treated
as confluence-facing evidence. Since none of the cited rows contains subject
reduction, unrestricted Church--Rosser, evaluator equality, quotient syntax
equality, global normalization, or kernel consistency, the read exports only
the retained finite obligation route.
\end{proof}

\begin{theorem}[MetaCIC retained-premise normal-form handoff]
\label{thm:metacic-candidate-normalization-confluence-retained-premise-normal-form}
Every strong-normalization-facing consumer of the normal-form endpoint exported
by an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must
retain the candidate gate, the residual-diamond retention row, the
decidable-boundary row, and the sibling confluence route. The consumer may read
the bounded normal-form endpoint only as a finite handoff through those rows; it
does not receive candidate-free normalization, quotient syntax equality, host
evaluator soundness, unrestricted subject reduction, global confluence, or
closed consistency.
\end{theorem}
\begin{proof}
Apply the normal-form obligation of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}.
It forces a normal-form read through the candidate-normalization row, residual
row, and sibling confluence route before the endpoint is available.

Next use residual-diamond retention from
\autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}.
That row keeps the residual envelope, decidable boundary, and blocked-edge data
visible while the endpoint is treated as a confluence-facing read.

The diamond-budget route of
\autoref{thm:metacic-candidate-normalization-diamond-budget-route} supplies the
bounded local peak and join-budget rows. These are the finite rows that a
strong-normalization-facing consumer may inspect; they do not discharge the
candidate gate or replace it with a global normalization theorem.

Finally apply the sibling confluence route of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-sibling-route}.
It keeps the critical-path, normalization-frontier, confluence-audit, and
parallel-diamond residual surfaces visible before the handoff reaches a
consumer. Hence the endpoint remains a retained-premise normal-form handoff and
no candidate-free normalization or quotient syntax equality is exposed.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization residual budget]
\label{thm:metacic-candidate-normalization-residual-budget}
Any normal-form endpoint exported by an accepted
$\MetacicCandidateNormalizationConfluenceHandoffUp$ packet to a
strong-normalization-facing consumer must retain, before the endpoint is read,
the candidate evidence, residual-diamond retention row, decidable-boundary data,
blocked-edge rows, bounded local peak rows, join-budget rows, and the sibling
confluence route. The endpoint is therefore a finite residual-budget read, not
candidate-free normalization, global confluence, unrestricted subject reduction,
quotient syntax equality, evaluator soundness, or closed consistency.
\end{theorem}
\begin{proof}
Start with the normal-form obligation of
\autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}
and the retained-premise handoff of
\autoref{thm:metacic-candidate-normalization-confluence-retained-premise-normal-form}.
Together they force candidate evidence and the sibling confluence route to
remain visible before a normal-form endpoint is exported.

Use residual-diamond retention from
\autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}.
It keeps the residual envelope, decidable-boundary data, and blocked-edge rows
attached to the endpoint read rather than allowing a detached normal-form token.

Finally apply the diamond-budget route of
\autoref{thm:metacic-candidate-normalization-diamond-budget-route}. Its bounded
local peak and join-budget rows are the finite budget a
strong-normalization-facing consumer may inspect. Since the cited rows exhaust
the exported endpoint surface, none of candidate-free normalization, global
confluence, unrestricted subject reduction, quotient syntax equality, evaluator
soundness, or closed consistency is supplied.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization residual exhaustion]
\label{thm:metacic-normal-form-residual-budget-exhaustion}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ handoff row
whose candidate set, bounded local peak, join-budget row, residual envelope,
decidable-boundary row, blocked-edge row, transport, replay, provenance, and
local $\NameCert$ are displayed has an exhaustive finite endpoint surface. It
either forwards a retained premise through the candidate and normalization
frontier rows, or emits the blocked residual edge as an obstruction. No
consumer may erase that obstruction and read global confluence, unrestricted
subject reduction, quotient syntax equality, closed consistency, or a
candidate-free normal-form theorem.
\end{theorem}
\begin{proof}
Project the displayed handoff carrier from the parent chapter. The candidate
set, frontier rows, residual envelope, decidable boundary, blocked-edge row,
transport, replay, provenance, and local naming data are the only rows available
to the consumer.

Apply the residual budget theorem
\autoref{thm:metacic-candidate-normalization-residual-budget}. It requires the
candidate evidence, residual-diamond retention, decidable-boundary data,
blocked-edge rows, bounded local peak rows, join-budget rows, and sibling
confluence route to remain visible before any endpoint is exported.

Now read the displayed forward-or-blocked row as finite data. In the forward
case the retained premise is carried through the candidate and frontier rows. In
the blocked case the residual edge remains an obstruction attached to the
endpoint read. The carrier's refused endpoints exclude candidate-free
normalization, global confluence, unrestricted subject reduction, quotient
syntax equality, closed consistency, and any consumer route that erases the
obstruction.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization residual-join choice-free handoff]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-join-choice-free}
When an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet exposes finite residual budget exhaustion and a displayed local-confluence certificate route, the residual-join endpoint is read as a finite handoff row. The endpoint is not a selected global normal form: it remains attached to the candidate evidence, normal-form obligation, residual budget, residual exhaustion surface, local confluence certificate route, transport, replay, provenance, and local $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start with the normal-form obligation of \autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-form-obligation}. It keeps candidate evidence and the sibling confluence route visible before any endpoint is public.

Apply the residual budget theorem, \autoref{thm:metacic-candidate-normalization-residual-budget}. The bounded local peak, join-budget, residual-diamond retention, decidable-boundary, and blocked-edge rows remain finite budget data rather than a choice of global normal form.

Now use residual exhaustion, \autoref{thm:metacic-normal-form-residual-budget-exhaustion}. The forward case carries the retained premise through the candidate and frontier rows, while the blocked case keeps the residual edge as an obstruction attached to the endpoint.

Finally read the local confluence certificate route through the sibling confluence surface already cited by the normal-form and residual-budget rows. Since the endpoint is assembled only from those displayed rows and the structural transport, replay, provenance, and naming data, it is a choice-free residual-join handoff.
\end{proof}
