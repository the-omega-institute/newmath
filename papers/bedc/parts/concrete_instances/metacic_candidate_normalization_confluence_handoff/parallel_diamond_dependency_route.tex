\paragraph{Parallel-diamond dependency route.}
The candidate-normalization confluence handoff consumes \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} for the residual critical-pair windows that remain visible before any confluence handoff row is exposed. The frontier packet supplies the typed local-join boundary and residual-budget ledger, while the handoff packet only routes candidate normalization, normal endpoints, blocked edges, transport, replay, provenance, and local naming through that boundary. Hence the handoff may advertise a confluence-facing row only after the parallel-diamond frontier has fixed the residual window and excluded full confluence, subject reduction, evaluator equality, quotient syntax equality, and closed consistency.

\begin{theorem}[MetaCIC candidate-normalization handoff parallel-diamond dependency]
\label{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-dependency}
Every accepted $\MetaCICCandidateNormalizationConfluenceHandoffUp$ read that consumes the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} does so only through the displayed critical-pair row, residual-join row, candidate-normalization row, bounded-check row, obstruction row, componentwise transport, replay, provenance, and local $\NameCert$ rows. The route is a finite sibling dependency on $\MetaCICParallelDiamondFrontierUp$, not a duplicate confluence theorem, subject-reduction theorem, evaluator equality, quotient syntax equality, residual-substitution discharge, or kernel-consistency claim.
\end{theorem}
\begin{proof}
Read the handoff carrier through its displayed candidate-normalization and bounded endpoint rows. A confluence-facing consumer cannot inspect the handoff before the critical-pair and residual-join rows supplied by \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} have fixed the frontier boundary.

Next preserve the bounded-check and obstruction rows. These rows keep failed residual-substitution, blocked-edge, and closedness obligations visible as retained data rather than discharging them into a global parallel-diamond result.

Finally transport the same route through the componentwise $\hsame$ data, displayed replay, provenance, and local $\NameCert$ row. Since no coordinate exports full confluence, subject reduction, evaluator equality, quotient syntax equality, residual-substitution discharge, or kernel consistency, the dependency is exactly the finite sibling route stated above.
\end{proof}
