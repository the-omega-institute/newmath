The candidate-normalization confluence handoff consumes the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} through the displayed residual-join, critical-pair, and candidate-diamond rows before any confluence-facing endpoint is read. In carrier terms, the $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet may route its candidate evidence, residual envelope, decidable-boundary row, blocked-edge ledger, transport, replay, provenance, and local $\NameCert$ onward only while the sibling frontier still exposes its residual-substitution premise, local critical-pair surface, candidate join, bounded checker rows, and obstruction row. The bridge is therefore a finite residual-diamond dependency route, not subject reduction, unrestricted Church--Rosser, quotient syntax equality, evaluator equality, global conversion decidability, or kernel consistency.
