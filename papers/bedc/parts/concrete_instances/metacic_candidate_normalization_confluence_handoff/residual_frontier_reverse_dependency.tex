The candidate-normalization confluence handoff consumes the residual-frontier and finite candidate-diamond rows before it exposes its local confluence route. In particular, \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} supplies the parallel frontier surface that the handoff reads as dependency data rather than as an isolated conclusion.
