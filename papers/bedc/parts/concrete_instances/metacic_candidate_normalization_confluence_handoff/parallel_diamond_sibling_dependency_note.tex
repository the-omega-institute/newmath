\begin{theorem}[MetaCIC handoff parallel-diamond sibling dependency]
\label{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-sibling-dependency}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier that exports candidate-normalization confluence data must consume the finite parallel-diamond frontier rows of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} together with the candidate-normalization router surface of \autoref{ch:concrete-instances-metacic-candidate-normalization-router-namecert}. The handoff therefore reads residual-premise retention, local critical-pair routing, candidate evidence, bounded endpoint routing, obstruction retention, transport, replay, provenance, and local naming before any confluence-facing consumer can use it.
\end{theorem}
\begin{proof}
Project the handoff carrier through \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. Its candidate row, frontier row, residual envelope, decidable boundary, blocked edge, transport, replay, provenance, and local name remain visible before the bounded endpoint row is exported.

Read the parallel-diamond frontier next. The frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} supplies the retained residual premise, local critical-pair, local-join, obstruction, and structural rows that any confluence-facing use must preserve.

Finally route candidate evidence through \autoref{ch:concrete-instances-metacic-candidate-normalization-router-namecert}. The router keeps the candidate evidence and normalization readback inside a bounded certificate route, so the handoff cannot be read as premise-free confluence, unrestricted subject reduction, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{proof}
