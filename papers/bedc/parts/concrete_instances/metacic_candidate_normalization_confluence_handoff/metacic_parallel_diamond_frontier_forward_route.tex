\begin{theorem}[MetaCIC candidate handoff forward route to the parallel-diamond frontier]
\label{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-forward-route}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet that is consumed as a local confluence handoff must expose the parallel-diamond frontier sibling of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} before the handoff is read as downstream MetaCIC evidence. In carrier form the handoff rows
$$
K,N,F,C,D,B,T,R,P,L
$$
remain visible while the sibling frontier supplies the retained residual-substitution premise, critical-pair envelope, candidate-join row, residual-frontier row, bounded checker rows, obstruction row, transport, replay, provenance, and local $\NameCert$ rows. The route is a finite dependency route for candidate normalization and confluence readers; it does not assert full Church--Rosser, subject reduction, evaluator equality, quotient syntax equality, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Read the carrier of the handoff packet first. The candidate evidence, normal endpoint, frontier, residual confluence, decidable-boundary, blocked-edge, transport, replay, provenance, and naming rows are the only rows available to the consumer.

Next read the sibling frontier at \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. Its displayed frontier budget keeps the retained residual-substitution premise and obstruction rows visible before any local diamond route is consumed.

The two packets therefore meet only along finite visible rows. Since neither packet contains a row for unrestricted confluence, subject reduction, evaluator equality, quotient equality, residual-substitution discharge, or kernel consistency, the forward route remains a dependency edge rather than a global MetaCIC endpoint.
\end{proof}
