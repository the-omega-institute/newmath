\begin{theorem}[MetaCIC candidate normalization residual-join scope]\label{thm:metacic-candidate-normalization-residual-join-scope}
An accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ candidate-normalization handoff may expose residual joins only after it has read the candidate diamond budget, the cut-height budget, the frontier dependency route, and the residual diamond bridge route. The resulting scope is the finite route through MetaCIC candidate rows, residual windows, candidate joins, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$; it is not an unscoped global confluence claim.
\end{theorem}
\begin{proof}
Project the parent carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. Its visible rows already separate candidate evidence, bounded endpoint data, residual confluence routing, decidable-boundary routing, blocked-edge retention, transport, replay, provenance, and local naming.

Read the budget rows next. The candidate diamond route of \autoref{thm:metacic-candidate-normalization-diamond-budget-route} fixes the bounded peak and join-budget surface, while the cut-height route of \autoref{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget} fixes the height boundary before a residual endpoint can be consumed.

Finally read the frontier and residual-diamond routes. The residual-frontier dependency and the bridge route of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} keep residual windows and candidate joins attached to the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Since no carrier coordinate stores global confluence, quotient syntax equality, evaluator equality, or kernel consistency, the handoff exposes only the scoped residual-join route stated above.
\end{proof}

\begin{theorem}[MetaCIC residual normal-form endpoint budget]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-normal-form-budget}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ normal-form endpoint read that is used as residual-join evidence must retain the candidate row, bounded endpoint row, candidate diamond budget, cut-height budget, residual diamond bridge route, and parallel-diamond frontier route before local confluence evidence is consumed. The retained surface is a finite route through the candidate-normalization carrier and its sibling frontier packets; it is not global Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator soundness, or kernel consistency.
\end{theorem}
\begin{proof}
Project the residual-join scope of \autoref{thm:metacic-candidate-normalization-residual-join-scope}. A residual-join consumer enters through the candidate-normalization carrier, so the candidate row and bounded endpoint row remain visible before the endpoint can count as evidence.

Read the budget rows next. The diamond-budget route of \autoref{thm:metacic-candidate-normalization-diamond-budget-route} supplies the candidate diamond budget, and the cut-height route of \autoref{thm:metacic-candidate-normalization-confluence-handoff-cut-height-budget} supplies the bounded endpoint height discipline.

Now attach the sibling frontier routes. The residual diamond bridge route keeps the residual bridge row in scope, while \autoref{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-route} keeps the parallel-diamond frontier route visible before a local confluence consumer can read the endpoint as join evidence.

Since the named rows are all finite carrier or sibling-route data, consuming local confluence evidence cannot erase them. The handoff has no coordinate for global Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator soundness, or kernel consistency, so the endpoint remains a guarded residual normal-form budget rather than an unscoped confluence theorem.
\end{proof}
