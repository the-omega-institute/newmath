\begin{theorem}[MetaCIC candidate normalization residual-matrix forward route]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-matrix-forward-route}
Every $\MetacicCandidateNormalizationConfluenceHandoffUp$ handoff consumed by \autoref{ch:concrete-instances-metacic-residual-join-matrix-namecert} must first display candidate evidence, the normalization frontier, the residual envelope, the decidable boundary, the blocked edge, componentwise transport, displayed replay, provenance, and local naming rows. The residual-join matrix therefore reads the handoff only after the finite route through the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}; it does not receive global Church--Rosser, unrestricted subject reduction, evaluator soundness, quotient syntax equality, or kernel consistency.
\end{theorem}
\begin{proof}
Project the candidate-normalization handoff carrier. The candidate evidence row, normalization-frontier row, residual-envelope row, decidable-boundary row, and blocked-edge row are visible before any structural row is replayed.

Next route the matrix consumer through the existing residual scope. The residual-join scope and residual budget exactness results keep the candidate, frontier, residual, boundary, and blocked-edge rows attached to the same finite packet.

Finally read transport, replay, provenance, and local naming. These rows preserve the displayed handoff into \autoref{ch:concrete-instances-metacic-residual-join-matrix-namecert}, so the matrix consumer receives a guarded child dependency route rather than an unscoped confluence theorem.
\end{proof}
