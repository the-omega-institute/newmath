\begin{theorem}[MetaCIC candidate-normalization confluence scoped closure route]
\label{thm:metacic-candidate-normalization-confluence-scoped-closure-route}
The scoped surface of $\MetacicCandidateNormalizationConfluenceHandoffUp$ is already fixed by the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}, the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}, the premise-exposure theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}, and the residual, decidable-boundary, and sibling route rows displayed in this chapter. Any consumer that reaches the handoff must keep the candidate evidence, residual envelope, bounded join window, decidable boundary, and blocked-edge row visible before reading a confluence-facing endpoint.
\end{theorem}
\begin{proof}
Project the carrier first. Its displayed rows already name the audit anchor, candidate evidence, bounded normal endpoint, frontier row, residual confluence envelope, decidable boundary, blocked-edge retention, transport, replay, provenance, and local naming.

Next apply the boundary and premise-exposure theorems. They force a consumer to expose the candidate-normalization and confluence premises before any endpoint can be consumed.

Finally read the residual and decidable-boundary rows together with the sibling route theorems of the hub. These rows keep the scoped handoff inside the finite candidate-normalization and residual-join surface, without adding full Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, closed consistency, or kernel consistency.
\end{proof}
