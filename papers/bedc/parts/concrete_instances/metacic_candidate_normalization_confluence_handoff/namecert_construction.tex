\chapter{MetaCIC Candidate Normalization Confluence Handoff NameCert Construction}
\label{ch:concrete-instances-metacic-candidate-normalization-confluence-handoff-namecert}
\origin{ai}

The $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet realizes the candidate-mediated strong-normalization boundary of \autoref{ch:metacic-normalization-audit-map} as a finite handoff surface for confluence and decidability consumers. It sits beside the critical-path carrier of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, the residual confluence envelope of \autoref{ch:concrete-instances-metacic-confluence-residual-envelope-namecert}, and the decidable boundary of \autoref{ch:concrete-instances-metacicdecidableboundary-namecert}. The packet names a guarded route from candidate evidence to bounded normal endpoints and residual-envelope reads; it does not assert unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, or global closed-term conversion decidability.

Candidate-mediated normalization reads the residual local-join budget of \autoref{ch:concrete-instances-metacic-residual-join-determinacy-namecert} before it can be used as a confluence handoff, so the endpoint row is never exported as an isolated normal-form claim.

This packet is also a concrete realization of the MetaCIC audit-map synthesis route in \autoref{ch:visions-subhub-metacic}: it joins normalization, confluence, decidability, and residual-substitution audit rows while keeping blocked subject-reduction edges visible.

The confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert} and the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} consume this handoff only as a guarded boundary route. Their reads keep candidate evidence, residual-envelope visibility, decidable-boundary routing, and blocked-edge retention explicit rather than turning the handoff into a global Church--Rosser or conversion theorem.

Before exporting candidate-normalization confluence data, the handoff consumes the residual-diamond sibling surface of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} and the critical-path sibling surface of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}. The handoff is consumed by \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} only after candidate evidence, residual envelope, decidable boundary, and blocked-edge rows are visible.

\begin{theorem}[MetaCIC handoff parallel-diamond forward link]
\label{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-forward-link}
Any $\MetaCICParallelDiamondFrontierUp$ consumer of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must expose the candidate row $K$, frontier row $F$, residual envelope $C$, decidable boundary $D$, blocked edge $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local name $L$ before reading the bounded endpoint row $N$. The forward dependency is the sibling route through \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}; it is not a global confluence theorem, quotient-syntax equality, unrestricted subject reduction, evaluator soundness, or closed consistency.
\end{theorem}
\begin{proof}
Start with a parallel-diamond consumer of the handoff. The consumer reaches the packet only through the displayed candidate-normalization carrier, so the candidate row $K$ and frontier row $F$ remain visible before any bounded endpoint can be read.

Next read the confluence side through the residual envelope $C$, decidable boundary $D$, and blocked edge $B$. These rows are precisely the finite obstruction and boundary data named by the parallel-diamond frontier route, and they prevent $N$ from being treated as an unconditional joinability endpoint.

Finally transport the same route through $T,R,P,L$. The componentwise transport, replay, provenance, and local name rows bind the endpoint read to the concrete sibling of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. Hence every forward read exposes $K,F,C,D,B,T,R,P,L$ before $N$, while the excluded global MetaCIC claims remain outside the carrier.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffParallelDiamondForwardLink}

Before exporting candidate-normalization confluence data, the handoff consumes residual-normal-form routing from \autoref{ch:concrete-instances-metacic-residual-normal-form-budget-router-namecert} and residual critical-pair sieve rows from \autoref{ch:concrete-instances-metacic-residual-critical-pair-sieve-namecert}.

\begin{definition}[MetaCIC candidate normalization confluence handoff carrier]
\label{def:metacic-candidate-normalization-confluence-handoff-carrier}
A $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier is a finite $\BHist$ packet
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the audit-map anchor row, binding the packet to the candidate-mediated reading of \autoref{ch:metacic-normalization-audit-map}.
\item $K$ is the candidate-evidence row, storing Candidate, ClosedCandidate, or CandidateSet membership data before any strong-normalization read is exported.
\item $N$ is the normal-endpoint row, recording the bounded normal endpoint or no-infinite-reduction projection obtained from $K$.
\item $F$ is the $\MetaCICNormalizationFrontierUp$ row that keeps candidate evidence and finished-normal evidence inside the displayed frontier packet.
\item $C$ is the confluence-facing row, read through the residual envelope of \autoref{ch:concrete-instances-metacic-confluence-residual-envelope-namecert}.
\item $D$ is the $\MetaCICDecidableBoundaryUp$ row, limiting decidability reads to bounded checker output and normal endpoint comparison.
\item $B$ is the blocked-edge row, retaining subject-reduction and residual-substitution obligations as visible boundaries rather than discharged theorems.
\item $T$ records componentwise $\hsame$ transport for the audit, candidate, normal endpoint, frontier, confluence, decidability, blocked-edge, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from a MetaCIC consumer request through $A,K,N,F,C,D,B,T$.
\item $P$ is the $\Pkg$ provenance over $\MetacicCandidateNormalizationConfluenceHandoffUp$, $\MetaCICCriticalPathUp$, $\MetaCICNormalizationFrontierUp$, $\MetacicConfluenceAuditWitnessUp$, $\MetaCICDecidableBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\MetacicCandidateNormalizationConfluenceHandoffUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,K,N,F,C,D,B,T,R,P,L$. It has no coordinate for evidence-free normalization, a global parallel diamond, unrestricted subject reduction, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{definition}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffCarrier\_candidate\_endpoint\_route}

\begin{theorem}[MetaCIC candidate normalization confluence handoff boundary]
\label{thm:metacic-candidate-normalization-confluence-handoff-boundary}
For every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier
$$
H=(A,K,N,F,C,D,B,T,R,P,L),
$$
the local $\NameCert_{\MetacicCandidateNormalizationConfluenceHandoffUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the audit anchor, candidate evidence, normal endpoint, normalization frontier, residual confluence envelope, decidable boundary, blocked-edge row, transport, replay, provenance, and local naming;
\item candidate-gate exactness, requiring every normalization read to pass through $K$ and the frontier row $F$ before $N$ is consumed;
\item confluence handoff discipline, requiring every residual or join read to pass through $C$ while retaining $B$ as a visible blocked edge;
\item decidable-boundary discipline, requiring every conversion-facing read to pass through $D$ after $N$ and before any bounded normal comparison is exported;
\item frontier non-collapse, requiring $T,R,P,L$ to preserve the same rows without promoting candidate-mediated SN into an unconditional closed-term theorem or residual-envelope rows into full Church--Rosser.
\end{enumerate}
No handoff obligation is stored outside $A,K,N,F,C,D,B,T,R,P,L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The accepted packet displays exactly the audit anchor, candidate evidence, normal endpoint, normalization frontier, residual confluence envelope, decidable boundary, blocked-edge, and structural rows.

The normalization stage is $A,K,N,F$. The audit anchor fixes the candidate-mediated reading; the candidate row $K$ is exposed before the endpoint row $N$, and the frontier row $F$ keeps both inside the public normalization boundary.

The confluence and decidability stage is $C,D,B$. A confluence-facing consumer may read residual-envelope data through $C$, but the blocked-edge row $B$ remains visible. A conversion-facing consumer may read bounded checker output through $D$ only after the normal endpoint has been displayed.

Finally $T,R,P,L$ transport, replay, source, and name the same finite route. Since the carrier contains no evidence-free normalization coordinate, global diamond theorem, unrestricted subject-reduction proof, quotient syntax equality, host evaluator soundness, or closed-consistency endpoint, the listed rows exhaust the local obligation surface.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffBoundary}

\begin{theorem}[MetaCIC candidate normalization confluence handoff seed closure]
\label{thm:metacic-candidate-normalization-confluence-handoff-seed-closure}
For every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier $H=(A,K,N,F,C,D,B,T,R,P,L)$, the seed package closes only when the candidate-evidence row $K$, normalization-frontier row $F$, residual confluence row $C$, decidable-boundary row $D$, and blocked-edge row $B$ are all displayed before the endpoint row $N$ is consumed. The support rows $T,R,P,L$ preserve this finite route by transport, replay, provenance, and local naming. The package does not assert unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffSeedClosure}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The accepted packet separates candidate evidence $K$, endpoint data $N$, frontier data $F$, residual confluence data $C$, decidable-boundary data $D$, blocked-edge data $B$, and the structural rows $T,R,P,L$.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Its candidate-gate exactness exposes $K$ and $F$ before $N$, its confluence handoff discipline keeps $C$ and $B$ visible for residual or join reads, and its decidable-boundary discipline places $D$ on the bounded comparison route.

Finally read $T,R,P,L$ only as preservation rows for the same finite package. They transport, replay, source, and name the displayed rows without adding a coordinate for the excluded global endpoints, so the seed closure remains the finite premise-exposure package rather than a global normalization, confluence, decidability, quotient, evaluator, or consistency theorem.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization confluence handoff premise exposure]
\label{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}
For every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier $H=(A,K,N,F,C,D,B,T,R,P,L)$, every confluence-facing read must expose the candidate row $K$, normalization frontier row $F$, residual envelope row $C$, decidable-boundary row $D$, and blocked-edge row $B$ before any bounded endpoint row $N$ can be consumed. Thus candidate-mediated normalization is retained as a premise surface for confluence consumers. The handoff refuses unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, and closed consistency.
\end{theorem}
\begin{proof}
Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Its candidate-gate exactness exposes $K$ and $F$ before $N$, while its confluence handoff discipline keeps $C$ and $B$ visible for every residual or join read.

Now chain the consumer boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public confluence-facing consumer receives the handoff only through the displayed route
$$
\MetaCICCriticalPathUp \longmapsto \MetaCICNormalizationFrontierUp \longmapsto C,D,B \longmapsto T,R,P,L .
$$
The decidable-boundary row $D$ is therefore part of the bounded comparison surface, not a detached global conversion decider.

Since $T,R,P,L$ only transport, replay, source, and name the same finite route, they cannot erase $K,F,C,D,B$ or promote $N$ into an unconditional endpoint theorem. The excluded global claims are not coordinates of $H$, so every confluence-facing read remains premise-exposed before consuming bounded endpoint data.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffPremiseExposure}

\begin{theorem}[MetaCIC candidate handoff residual-diamond retention]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}
For every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier
$$
H=(A,K,N,F,C,D,B,T,R,P,L),
$$
every confluence-facing read must retain the candidate evidence $K$, residual envelope $C$, decidable boundary $D$, and blocked-edge row $B$ before a bounded endpoint row $N$ is treated as joinable. The retained route is consumed through the sibling path from $\MetaCICCriticalPathUp$ to $\MetaCICParallelDiamondFrontierUp$ and remains only a residual-diamond handoff. It does not assert full Church--Rosser, unrestricted subject reduction, quotient syntax equality, or global conversion decidability.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The endpoint row $N$ is separate from the candidate evidence $K$, residual envelope $C$, decidable boundary $D$, and blocked-edge row $B$, so joinability cannot be read from $N$ alone.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Its candidate-gate exactness exposes $K$ and $F$ before $N$, while its confluence and decidable-boundary clauses keep $C,D,B$ visible on the residual comparison route.

Use premise exposure from \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. It prevents a confluence-facing consumer from consuming the bounded endpoint before $K,F,C,D,B$ are displayed in the same packet.

Finally route the read through the sibling frontier. The critical-path packet $\MetaCICCriticalPathUp$ supplies the public handoff direction, and $\MetaCICParallelDiamondFrontierUp$ reads only the residual-diamond boundary, not a global confluence theorem. Since the carrier has no coordinate for Church--Rosser, unrestricted subject reduction, quotient syntax equality, or global conversion decidability, retaining $K,C,D,B$ is exactly the finite residual-diamond condition.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateHandoffResidualDiamondRetention}

\begin{theorem}[MetaCIC candidate handoff local join budget]
\label{thm:metacic-candidate-handoff-local-join-budget}
For every confluence-facing endpoint comparison exported from an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier
$$
H=(A,K,N,F,C,D,B,T,R,P,L),
$$
the consumer must expose a finite local-join budget read through candidate evidence $K$, normalization frontier $F$, residual envelope $C$, decidable boundary $D$, blocked-edge row $B$, and structural rows $T,R,P,L$ before the endpoint row $N$ is treated as joinable.

The budget is routed through $\MetaCICCriticalPathUp$, $\MetaCICNormalizationFrontierUp$, $\MetaCICConfluenceAuditPacketUp$, and $\MetaCICParallelDiamondFrontierUp$ as retained finite evidence. It is not full Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.LocalJoinBudget.MetacicCandidateHandoffLocalJoinBudget}
\begin{proof}
Start with the boundary and premise-exposure routes, \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. They place $K,F,C,D,B$ before any confluence-facing read may consume the bounded endpoint row $N$.

Apply residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}. It keeps the residual envelope, decidable boundary, and blocked edge visible while the comparison is routed from $\MetaCICCriticalPathUp$ and $\MetaCICNormalizationFrontierUp$ toward confluence consumers.

Use the normal-endpoint readback theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback}. It reads $N$ only after $K,F,C,D,B,T,R,P,L$ have been displayed and names the $\MetaCICConfluenceAuditPacketUp$ route as an audit handoff rather than as evaluator equality.

Finally pass the retained evidence to $\MetaCICParallelDiamondFrontierUp$. The frontier receives the finite local-join budget, not a global confluence theorem, so Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, and closed consistency remain outside the carrier.
\end{proof}

\begin{theorem}[MetaCIC candidate handoff normal-endpoint readback]
\label{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback}
Any bounded normal endpoint read exported by an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must expose candidate evidence $K$, frontier row $F$, residual envelope $C$, decidable boundary $D$, blocked edge $B$, and the transport, replay, provenance, and naming rows $T,R,P,L$ before $N$ is consumed. The read may be handed to $\MetaCICCriticalPathUp$, $\MetaCICNormalizationFrontierUp$, and $\MetaCICConfluenceAuditPacketUp$ consumers only as this finite premise-visible route. It is not unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffNormalEndpointReadback}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The bounded endpoint row $N$ is displayed beside candidate evidence $K$, frontier evidence $F$, residual envelope $C$, decidable boundary $D$, blocked edge $B$, and the structural rows $T,R,P,L$.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness exposes $K$ and $F$ before $N$, confluence handoff discipline keeps $C$ and $B$ visible, and decidable-boundary discipline keeps $D$ on the bounded comparison route.

Now use the consumer boundary \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public read reaches $\MetaCICCriticalPathUp$ and $\MetaCICNormalizationFrontierUp$ only through the displayed handoff route, so the endpoint row cannot be consumed as a detached normal-form theorem.

Finally read the confluence-audit side through the same residual route. The sibling $\MetaCICConfluenceAuditPacketUp$ may consume the bounded endpoint only after $C,D,B,T,R,P,L$ remain visible, and those rows only transport, replay, source, and name the finite packet. Since the excluded global endpoints are not carrier coordinates, the normal-endpoint readback preserves the candidate and residual premises.
\end{proof}

\begin{theorem}[Candidate normal endpoint retention]
\label{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-retention}
Let
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. Any accepted endpoint read from $H$ must first expose the candidate gate $K$, normalization frontier $F$, residual envelope $C$, decidable boundary $D$, blocked-edge row $B$, residual-diamond retention row, and normal endpoint readback row before the endpoint row $N$ is consumed by a confluence or conversion-facing consumer. The retained read is a bounded handoff through $A,K,N,F,C,D,B,T,R,P,L$; it is not global confluence, unrestricted strong normalization, quotient syntax equality, or host evaluator soundness.
\end{theorem}
\begin{proof}
Start with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It displays the candidate gate, normalization frontier, residual envelope, decidable boundary, and blocked-edge rows as separate carrier coordinates before any endpoint read is available.

Apply premise exposure from \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. A public consumer cannot pass directly to $N$; it must first retain $K,F,C,D,B$ inside the same accepted packet.

Use residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}. This keeps the residual envelope, decidable boundary, and blocked edge visible when the endpoint is read by a confluence-facing route.

Finally apply normal-endpoint readback from \autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback}. The rows $T,R,P,L$ transport, replay, source, and name the same bounded route before $N$ is consumed, so the endpoint cannot be reinterpreted as any refused global MetaCIC principle.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffNormalEndpointRetention}

\begin{theorem}[MetaCIC finite endpoint comparison handoff]
\label{thm:metacic-candidate-normalization-confluence-handoff-finite-endpoint-comparison}
Let
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. Any endpoint comparison exported from $H$ to a confluence or conversion consumer must first expose candidate evidence $K$, normalization-frontier row $F$, residual-envelope row $C$, decidable-boundary row $D$, blocked-edge row $B$, and the structural rows $T,R,P,L$ before bounded normal endpoints are compared through $N$. The exported comparison is a finite candidate-mediated handoff; it is not unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, quotient syntax equality, host evaluator soundness, global conversion decidability, or closed consistency.
\end{theorem}
\begin{proof}
Start with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness exposes $K$ and $F$ before $N$, while confluence discipline keeps $C$ and $B$ visible and decidable-boundary discipline places $D$ on the bounded comparison route.

Apply premise exposure from \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. A confluence-facing consumer cannot read the bounded endpoint row until the candidate, frontier, residual-envelope, decidable-boundary, and blocked-edge rows are displayed in the same packet.

Use residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}. It prevents a comparison from treating $N$ as detached joinability data; the residual envelope and blocked edge remain part of the read.

Finally apply normal-endpoint readback from \autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback}. The structural rows $T,R,P,L$ transport, replay, source, and name the same finite route before $N$ is consumed, so the comparison cannot export any of the refused global MetaCIC endpoints.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffFiniteEndpointComparison}

\begin{theorem}[MetaCIC candidate handoff decidable normal-form comparison]
\label{thm:metacic-candidate-handoff-decidable-normal-form-comparison}
Any bounded normal-form comparison exported to a decidability consumer from an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier must expose candidate evidence $K$, frontier row $F$, residual envelope $C$, decidable boundary $D$, blocked edge $B$, and structural rows $T,R,P,L$ before the endpoint row $N$ is compared. The comparison is obtained only by chaining boundary exposure, premise exposure, residual-diamond retention, and finite endpoint comparison. It refuses full Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, and closed consistency.
\end{theorem}
\begin{proof}
Start with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It exposes the candidate row $K$, frontier row $F$, residual envelope $C$, decidable boundary $D$, and blocked edge $B$ before any endpoint comparison is available.

Apply premise exposure from \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. The bounded endpoint row $N$ remains downstream of the candidate and frontier rows, so the comparison still has to read the finite handoff surface.

Now use residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}. The residual envelope and blocked edge remain visible on the comparison route, so the decidability consumer cannot erase the guarded premise surface.

Finally apply finite endpoint comparison from \autoref{thm:metacic-candidate-normalization-confluence-handoff-finite-endpoint-comparison}. The rows $T,R,P,L$ transport, replay, source, and name the same finite route before $N$ is compared, which is exactly the bounded comparison route refused by the excluded global claims.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateHandoffDecidableNormalFormComparison}

\begin{theorem}[MetaCIC candidate handoff residual-decider inversion]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-decider-inversion}
Let
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. If a confluence or conversion consumer reads a bounded decider outcome from $H$, then the candidate row $K$, normalization frontier $F$, residual envelope $C$, decidable boundary $D$, blocked-edge row $B$, and structural rows $T,R,P,L$ are already visible before the endpoint row $N$ is compared. The inversion is a finite retained-premise read; it does not claim full Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator soundness, global conversion decidability, or closed consistency.
\end{theorem}
\begin{proof}
Start with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It exposes candidate evidence, the normalization frontier, residual envelope, decidable boundary, and blocked-edge rows as separate coordinates before any bounded endpoint comparison can be read.

Apply premise exposure from \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. A confluence-facing read cannot consume the endpoint row before $K,F,C,D,B$ are displayed in the same packet.

Use residual-diamond retention and finite endpoint comparison, \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-finite-endpoint-comparison}. They keep the residual envelope, decidable boundary, and blocked edge attached to any bounded endpoint comparison offered to a confluence or conversion consumer.

Finally apply decider-row exhaustion through \autoref{thm:metacic-candidate-handoff-decidable-normal-form-comparison}. The bounded decider outcome is read only after $D$ is on the displayed comparison route and after $T,R,P,L$ transport, replay, source, and name the same finite handoff. Since the excluded global endpoints are not carrier coordinates, the decider read inverts back to the retained candidate and residual premises rather than to any unrestricted MetaCIC theorem.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateHandoffResidualDeciderInversion}

\begin{theorem}[MetaCIC candidate handoff frontier normal-endpoint retention]
\label{thm:metacic-candidate-handoff-frontier-normal-endpoint-retention}
Let
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. Any bounded normal endpoint exported by $H$ to a confluence consumer that also reads $\MetaCICParallelDiamondFrontierUp$ must retain candidate evidence $K$, frontier row $F$, residual envelope $C$, decidable boundary $D$, blocked-edge row $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$ before the endpoint row $N$ is consumed. The same read must preserve the parallel-diamond frontier's residual and local-join rows as visible premises. It does not assert full Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, or closed consistency.
\end{theorem}
\begin{proof}
Start with premise exposure and residual-diamond retention, \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}. These require the candidate row, frontier row, residual envelope, decidable boundary, and blocked edge to remain visible before any confluence-facing endpoint read is accepted.

Apply normal-endpoint readback, \autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback}. The bounded endpoint row $N$ is consumed only through the same candidate, frontier, residual, boundary, blocked-edge, transport, replay, provenance, and local naming route.

Finally route the consumer through the sibling frontier dependency. The $\MetaCICParallelDiamondFrontierUp$ route named by \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention} and \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} keeps residual and local-join rows visible before the endpoint is used. Since none of the refused global endpoints is a carrier coordinate, the handoff retains only the bounded frontier premises.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateHandoffFrontierNormalEndpointRetention}

\begin{theorem}[MetaCIC candidate handoff consumer boundary]
\label{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}
Every public consumer of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet factors through
$$
\MetaCICCriticalPathUp \longmapsto \MetaCICNormalizationFrontierUp \longmapsto C,D,B \longmapsto T,R,P,L .
$$
The consumer receives candidate-mediated normalization evidence, bounded normal endpoint data, residual-envelope visibility, decidable-boundary routing, and blocked-edge retention. It receives no unconditional strong-normalization theorem, full Church--Rosser theorem, unrestricted subject-reduction theorem, global conversion decider, or completed closed-consistency proof.
\end{theorem}
\begin{proof}
Start from the carrier projection. The audit and critical-path rows determine which candidate-mediated normalization and boundary rows are visible to the consumer.

Apply \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. The candidate gate exposes $K,F,N$ before any normal endpoint is available, while $C,D,B$ retain the confluence and decidability boundaries as displayed rows.

The structural rows $T,R,P,L$ do not add another route. They preserve the same carrier under componentwise transport, replay it for downstream consumers, record provenance, and name the packet locally. The absent global theorems cannot be projected from those rows.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffConsumerBoundary}

\begin{theorem}[MetaCIC candidate normalization residual progress]
\label{thm:metacic-candidate-normalization-residual-progress}
An accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet consumes residual data only as a progress ledger. Every admitted candidate-normalization row read through $A,K,N,F,C,D,B,T,R,P,L$ either forwards a retained premise through the candidate and frontier rows $K,F$ or emits a residual obstruction row through $C$ and $B$, with the residual boundary read against \autoref{ch:concrete-instances-parallel-diamond-residual-namecert}. The packet does not assert full confluence, unrestricted subject reduction, unconditional strong normalization, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The candidate-normalization data are $A,K,N,F$, the residual and decidability boundary rows are $C,D,B$, and the structural rows are $T,R,P,L$.

Apply \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. The candidate gate forces any normalization row to expose $K$ and $F$ before a normal endpoint is consumed, so a retained premise remains visible when the row is forwarded.

If the row cannot be forwarded as a retained premise, the same boundary theorem routes the read through $C$ and $B$. The residual envelope is then consumed as a ledger compatible with \autoref{ch:concrete-instances-parallel-diamond-residual-namecert}, while the blocked-edge row records the obstruction rather than discharging it.

Finally use \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. The public consumer receives only the candidate-mediated route, residual-envelope visibility, decidable-boundary routing, blocked-edge retention, and structural rows. Since the excluded global endpoints are not carrier coordinates, the progress ledger cannot be read as a full confluence or strong-normalization theorem.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffResidualProgress}

\begin{theorem}[MetaCIC candidate normalization confluence bounded join window]
\label{thm:metacic-candidate-normalization-confluence-bounded-join-window}
Let $H=(A,K,N,F,C,D,B,T,R,P,L)$ be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. A confluence-facing consumer may export a join read from $H$ only after the candidate evidence $K$, normalization-frontier row $F$, residual envelope $C$, decidable-boundary row $D$, blocked-edge row $B$, and structural rows $T,R,P,L$ remain visible before the endpoint row $N$ is consumed. The window is finite and carrier-internal: it does not assert full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffBoundedJoinWindow}
\begin{proof}
Start with the handoff boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It exposes $K$ and $F$ before $N$, keeps $C$ and $B$ visible for confluence-facing reads, and routes bounded comparison through $D$.

Use premise exposure from \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. A public confluence consumer cannot read the endpoint row before the candidate, frontier, residual-envelope, decidable-boundary, and blocked-edge rows are displayed in the same packet.

Apply residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}. It prevents a join read from treating $N$ as detached joinability data; the read must retain $K,C,D,B$ on the residual route.

Finally use normal-endpoint readback from \autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback}. The structural rows $T,R,P,L$ preserve, replay, source, and name the same finite route before $N$ is consumed. Since no excluded global endpoint is a carrier coordinate, the exported join is available only through this bounded window.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization confluence rectangle]
\label{thm:metacic-candidate-normalization-confluence-rectangle}
Let $H=(A,K,N,F,C,D,B,T,R,P,L)$ be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. If both candidate-normalization edges of a bounded comparison are admitted by the candidate gate and each residual edge is read through the residual envelope, then the handoff packet either displays a common bounded endpoint row or records a residual obstruction row through $C$ and $B$. The rectangle is guarded by the candidate and residual rows; it does not assert full Church--Rosser, unconditional strong normalization, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or consistency.
\end{theorem}
\begin{proof}
Start with \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. The candidate gate forces both normalization edges to expose $K$ and $F$ before any endpoint row $N$ can be consumed.

Use the consumer boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public handoff read must pass through the residual-envelope row $C$, decidable-boundary row $D$, blocked-edge row $B$, and the structural rows $T,R,P,L$.

Apply residual progress from \autoref{thm:metacic-candidate-normalization-residual-progress}. If the two candidate-normalization edges forward through the retained premises, the packet displays the bounded endpoint row shared by those reads. If one residual edge cannot be forwarded, the same route records the obstruction through $C$ and $B$.

The residual side is read against \autoref{ch:concrete-instances-parallel-diamond-residual-namecert}. Since the carrier contains no coordinates for unrestricted Church--Rosser, unconditional strong normalization, subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or consistency, the rectangle remains a guarded handoff packet.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceRectangle}

\begin{theorem}[MetaCIC candidate normalization confluence handoff candidate residual join]
\label{thm:metacic-candidate-normalization-confluence-handoff-candidate-residual-join}
Let $H=(A,K,N,F,C,D,B,T,R,P,L)$ be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. If two candidate-normalization edges exported from $H$ expose candidate evidence $K$ and frontier evidence $F$, and every residual edge is routed through the residual-envelope row $C$ and blocked-edge row $B$, then any exported join row is guarded by $K$, $F$, $C$, $D$, $B$, and the structural rows $T,R,P,L$. The join row is a candidate-mediated residual handoff; it is not full Church--Rosser, unrestricted subject reduction, unconditional strong normalization, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffCandidateResidualJoin}
\begin{proof}
Begin with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It forces any endpoint or comparison read to display $K,F,N$ before the consumer sees bounded normalization data, and it keeps $C,D,B$ visible on the residual and decidable-boundary side.

Use residual progress from \autoref{thm:metacic-candidate-normalization-residual-progress}. Under the stated routing, residual edges either remain retained candidate premises through $K,F$ or become obstruction rows through $C,B$; neither route removes the candidate evidence.

Finally apply the rectangle theorem \autoref{thm:metacic-candidate-normalization-confluence-rectangle}. The exported join row is therefore read only as the guarded rectangle output, with $T,R,P,L$ preserving the same finite route by transport, replay, provenance, and local naming. Since no excluded global theorem is a carrier coordinate, the join cannot be promoted beyond the candidate-mediated residual handoff.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization parallel-frontier consumption]
\label{thm:metacic-candidate-normalization-confluence-handoff-parallel-frontier-consumption}
If a $\MetaCICParallelDiamondFrontierUp$ consumer reads a join row from an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier
$$
H=(A,K,N,F,C,D,B,T,R,P,L),
$$
then the read must preserve candidate evidence $K$, normalization-frontier data $F$, residual-envelope data $C$, decidable-boundary data $D$, blocked-edge data $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$ before $N$ can be treated as bounded endpoint data. The exported join is only a candidate-mediated residual handoff; it is not full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffParallelFrontierConsumption}
\begin{proof}
Start with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It prevents any endpoint read from reaching $N$ before the candidate evidence $K$, normalization frontier $F$, residual envelope $C$, decidable boundary $D$, and blocked-edge row $B$ remain visible.

Apply the premise-exposure route of \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public consumer receives the handoff only through the same carrier rows and through the structural data $T,R,P,L$.

Use residual progress from \autoref{thm:metacic-candidate-normalization-residual-progress} and the rectangle theorem \autoref{thm:metacic-candidate-normalization-confluence-rectangle}. They keep retained candidate premises and residual obstructions attached to $K,F,C,D,B$ while allowing only bounded endpoint data or an obstruction row to be displayed.

Finally invoke the candidate residual join theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-candidate-residual-join}. The join already exported by $H$ is guarded by the candidate, frontier, residual, blocked-edge, and structural rows. A $\MetaCICParallelDiamondFrontierUp$ consumer therefore receives a candidate-mediated residual handoff for bounded endpoint data, not any of the excluded global confluence, subject-reduction, conversion-decidability, quotient-equality, evaluator-soundness, or consistency claims.
\end{proof}

\begin{theorem}[MetaCIC candidate normalization confluence boundary]
\label{thm:metacic-candidate-normalization-confluence-boundary}
Let $H=(A,K,N,F,C,D,B,T,R,P,L)$ be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. If a bounded comparison exports a join row from $H$, then the read preserves candidate evidence $K$, the normalization frontier $F$, the residual envelope $C$, the decidable boundary $D$, the blocked-edge row $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$. The exported join row is therefore a guarded candidate-normalization handoff. It cannot be read as unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. A bounded comparison can export a join row only through the displayed candidate, frontier, residual-envelope, decidable-boundary, blocked-edge, transport, replay, provenance, and naming coordinates of $H$.

Apply the handoff boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness keeps $K$ and $F$ visible before endpoint data are consumed, confluence discipline keeps $C$ and $B$ visible for residual reads, and decidable-boundary discipline routes bounded comparison through $D$.

Use residual progress and the rectangle theorem from \autoref{thm:metacic-candidate-normalization-residual-progress} and \autoref{thm:metacic-candidate-normalization-confluence-rectangle}. They allow the bounded comparison to display a guarded endpoint row or an obstruction row, but only while retaining the candidate and residual coordinates.

Finally read the structural rows $T,R,P,L$. They transport, replay, source, and name the same finite route; none of them creates a coordinate for unconditional normalization, full Church--Rosser, unrestricted subject reduction, global decidability, quotient syntax equality, evaluator soundness, or closed consistency. Thus no excluded coordinate is available to the consumer.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceBoundary}

\begin{theorem}[MetaCIC candidate normalization confluence handoff decider boundary]
\label{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}
Any bounded conversion comparison exported through the decidable-boundary row $D$ of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier $H=(A,K,N,F,C,D,B,T,R,P,L)$ must first expose candidate evidence $K$, normal endpoint data $N$, the normalization frontier row $F$, the residual-envelope row $C$, the blocked-edge row $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$. The exported comparison is only a bounded comparison boundary. It is not global conversion decidability, full Church--Rosser, unrestricted subject reduction, evaluator equality, quotient syntax equality, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The only conversion-facing coordinate is $D$, and it is displayed alongside the candidate, endpoint, frontier, residual-envelope, blocked-edge, and structural rows.

Apply \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Its candidate-gate exactness forces $K,F,N$ to appear before a normal endpoint can be consumed, and its confluence handoff discipline keeps $C$ and $B$ visible before any residual or join read is offered.

Next use \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public consumer receives the bounded comparison only through the same carrier route and through $T,R,P,L$; the structural rows do not create a second decider surface.

Finally apply \autoref{thm:metacic-candidate-normalization-residual-progress}. Residual data remain progress ledger entries or obstruction rows, so the bounded comparison exported through $D$ cannot be promoted to the excluded global conversion, confluence, subject-reduction, evaluator-equality, quotient-equality, or consistency claims.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffDeciderBoundary}

\begin{theorem}[MetaCIC candidate handoff decider-row exhaustion]
\label{thm:metacic-candidate-normalization-confluence-handoff-decider-row-exhaustion}
Any bounded conversion comparison exported through row $D$ of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier $H=(A,K,N,F,C,D,B,T,R,P,L)$ must preserve the candidate evidence row $K$, endpoint row $N$, normalization frontier row $F$, residual envelope row $C$, blocked-edge row $B$, componentwise transport row $T$, displayed replay row $R$, provenance row $P$, and local naming row $L$. No consumer may read $D$ as a standalone global conversion decider. The exhausted row remains only a bounded comparison surface and does not assert full Church--Rosser, unconditional strong normalization, unrestricted subject reduction, evaluator equality, quotient syntax equality, global conversion decidability, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The decidable-boundary row $D$ appears only as one coordinate of $H$, beside the candidate, endpoint, frontier, residual-envelope, blocked-edge, and structural rows.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It places bounded comparison reads after candidate evidence, frontier evidence, endpoint data, residual-envelope visibility, and blocked-edge retention.

Apply the consumer boundary \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public consumer receives $D$ only through the displayed route with $T,R,P,L$, so there is no detached decider surface outside the carrier.

Finally use \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}. That theorem identifies $D$ as the bounded comparison row after the displayed endpoint and residual rows have been exposed. Since the excluded global endpoints are not carrier coordinates, exhausting row $D$ preserves the finite comparison boundary without collapsing into global decidability.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffDeciderRowExhaustion}

\begin{theorem}[MetaCIC candidate normalization confluence retained-premise inversion]
\label{thm:metacic-candidate-normalization-confluence-retained-premise-inversion}
Let
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. If a bounded endpoint row or join row is read from $H$, then the read determines the same candidate evidence $K$, normalization frontier $F$, residual envelope $C$, decidable-boundary row $D$, blocked-edge row $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$. Dropping any retained row changes the packet and cannot be presented as the same confluence handoff.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The endpoint and join rows are not independent data; they are read from the displayed tuple with $K,F,C,D,B,T,R,P,L$ still visible.

Apply the seed package and boundary results from \autoref{thm:metacic-candidate-normalization-confluence-handoff-seed-closure}, \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}, and \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. These force candidate evidence, frontier data, residual-envelope visibility, decidable-boundary access, and blocked-edge retention to remain part of the same handoff before any endpoint or join row is consumed.

Use the bounded conversion comparison surface of \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-row-exhaustion}. The bounded comparison row $D$ can be read only together with the structural rows $T,R,P,L$, so erasing any retained coordinate changes the packet rather than simplifying the same one.

Finally apply the nonescape clauses in \autoref{thm:metacic-candidate-normalization-confluence-boundary} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. They rule out parameter transport into a detached endpoint or join claim, leaving only constructor-style inversion on the concrete carrier rows.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceRetainedPremiseInversion}

\begin{definition}[MetaCIC finite conversion certificate carrier]
\label{def:metacic-finite-conversion-certificate-carrier}
A $\MetacicCandidateNormalizationConfluenceHandoffUp$ finite conversion certificate is the subroute of an accepted carrier
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
that displays the bounded normal endpoint row $N$, normalization frontier row $F$, residual-envelope row $C$, decidable-boundary row $D$, blocked-edge row $B$, and structural rows $T,R,P,L$ in one route. The certificate keeps $K$ as its candidate-evidence entry condition and uses $A$ only as the audit anchor inherited from the surrounding carrier. It has no coordinate for global conversion decidability, full Church--Rosser, unrestricted subject reduction, evaluator equality, quotient syntax equality, or closed consistency.
\end{definition}

\begin{theorem}[MetaCIC finite conversion certificate boundary]
\label{thm:metacic-finite-conversion-certificate-boundary}
Every bounded conversion read exported by an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must display, in one finite route, the normal endpoint row $N$, normalization frontier row $F$, residual-envelope row $C$, decidable-boundary row $D$, blocked-edge row $B$, componentwise transport row $T$, displayed replay row $R$, provenance row $P$, and local naming row $L$. Such a read is a finite conversion certificate in the sense of \autoref{def:metacic-finite-conversion-certificate-carrier}. It is not global conversion decidability, full Church--Rosser, unrestricted subject reduction, evaluator equality, quotient syntax equality, or closed consistency.
\end{theorem}
\begin{proof}
Begin with the finite certificate carrier. It is not a second carrier: it is the displayed $N,F,C,D,B,T,R,P,L$ subroute of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}, with $K$ as the candidate-evidence entry condition and $A$ as the audit anchor.

Apply the handoff boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness places $K,F,N$ before a bounded normal endpoint is consumed, while the confluence handoff discipline keeps $C$ and $B$ visible for residual reads.

Use the consumer boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public bounded conversion consumer receives the comparison only through the same structural route $T,R,P,L$ and not through a detached evaluator or quotient-syntax endpoint.

Finally combine residual progress with the decider boundary of \autoref{thm:metacic-candidate-normalization-residual-progress} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}. Residual entries remain progress or obstruction rows, and the exported comparison passes through $D$ as a bounded boundary. Since the excluded global claims are not carrier coordinates, the finite certificate cannot be read as global conversion decidability, Church--Rosser, subject reduction, evaluator equality, quotient equality, or consistency.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicFiniteConversionCertificateBoundary}

\begin{theorem}[MetaCIC candidate handoff obligation rows]
\label{thm:metacic-candidate-normalization-confluence-handoff-obligation-rows}
Any obligationClosure upgrade for $\MetacicCandidateNormalizationConfluenceHandoffUp$ must display candidate evidence $K$, normal endpoint data $N$, frontier row $F$, residual-envelope row $C$, decidable-boundary row $D$, blocked-edge row $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$ as non-collapsible rows. In particular, the candidate-evidence row cannot be folded into the normal endpoint row, the residual-envelope row cannot be folded into the decidable-boundary row, and the blocked-edge row cannot be erased by transport or replay. The upgrade remains a finite handoff obligation surface; it does not assert unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffObligationRows}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. It displays $A,K,N,F,C,D,B,T,R,P,L$ as separate rows, so an obligationClosure read must account for candidate evidence, normal endpoint data, frontier discipline, residual-envelope data, decidable-boundary data, blocked edges, and the structural route.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness keeps $K$ and $F$ visible before $N$ is consumed, confluence handoff discipline keeps $C$ and $B$ visible, and decidable-boundary discipline keeps $D$ downstream of the displayed endpoint and residual rows.

Use the consumer boundary, residual progress, decider boundary, and finite conversion certificate boundary from \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}, \autoref{thm:metacic-candidate-normalization-residual-progress}, \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}, and \autoref{thm:metacic-finite-conversion-certificate-boundary}. Together they force public reads to preserve candidate-mediated normalization, residual-envelope visibility, bounded conversion comparison, blocked-edge retention, transport, replay, provenance, and local naming as the same finite route.

Therefore collapsing any one of $K,N,F,C,D,B,T,R,P,L$ would remove a row required by the carrier and by the displayed boundary theorems. Since the excluded global endpoints are not carrier coordinates, the obligation rows remain non-collapsible without promoting the packet beyond the finite handoff surface.
\end{proof}

\begin{theorem}[MetaCIC candidate handoff row readiness]
\label{thm:metacic-candidate-normalization-confluence-handoff-candidate-row-readiness}
Every confluence-facing or decidability-facing read of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
must expose the candidate row $K$ and the frontier row $F$ before the endpoint row $N$ or the residual rows $C,D,B$ are consumed. Thus the handoff exports only a finite premise-readiness route, not a global strong-normalization endpoint, full Church--Rosser theorem, unrestricted subject-reduction theorem, or global conversion decider.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The rows $K$ and $F$ are separate from $N,C,D,B$, so a consumer can only read endpoint, residual, or decidability data through the displayed handoff packet.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Its candidate-gate exactness places $K$ and $F$ before $N$, while its confluence and decidability clauses keep $C,D,B$ on the residual and bounded-comparison side.

Use the obligation rows theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-obligation-rows}. It makes $K,N,F,C,D,B,T,R,P,L$ non-collapsible rows, so neither transport nor replay may hide the candidate and frontier premises before a consumer reaches the endpoint or residual rows.

Since the carrier contains no coordinate for global strong normalization, full Church--Rosser, unrestricted subject reduction, or global conversion decidability, the exposed route is exactly a local premise-readiness route for confluence-facing and decidability-facing consumers.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffCandidateRowReadiness}

\begin{theorem}[MetaCIC candidate retained-edge handoff]
\label{thm:metacic-candidate-normalization-confluence-handoff-retained-edge}
Every finite successor handoff from candidate normalization toward confluence in an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must retain the candidate-gate rows $K,F,N$, the residual-envelope row $C$, the decidable-boundary row $D$, the blocked-edge row $B$, and the structural rows $T,R,P,L$ before any consumer reads endpoint or conversion data. The retained edge is a finite successor route inside $H=(A,K,N,F,C,D,B,T,R,P,L)$; it does not assert Church--Rosser, strong normalization, subject reduction, evaluator equality, quotient equality, or consistency.
\end{theorem}
\begin{proof}
Start with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness keeps $K,F,N$ visible in the successor handoff, while confluence handoff discipline keeps $C$ and $B$ visible.

Apply the consumer boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A confluence-facing consumer receives the handoff only through the displayed public route, so endpoint data cannot be read before the retained candidate and residual rows.

Use the decider boundary from \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}. Any bounded conversion comparison passes through $D$ after the candidate, endpoint, residual-envelope, blocked-edge, and structural rows are displayed.

Finally apply \autoref{thm:metacic-candidate-normalization-confluence-handoff-obligation-rows}. It makes $K,F,N,C,D,B,T,R,P,L$ non-collapsible rows of the obligation surface. Since the excluded global endpoints are not carrier coordinates, a finite successor handoff retains those rows without proving Church--Rosser, strong normalization, subject reduction, evaluator equality, quotient equality, or consistency.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffRetainedEdge}

\begin{theorem}[MetaCIC candidate normalization handoff obligation stability]
\label{thm:metacic-candidate-normalization-handoff-obligation-stability}
Let $H=(A,K,N,F,C,D,B,T,R,P,L)$ be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. Any public read preserving the handoff rows must preserve candidate evidence $K$, endpoint row $N$, frontier row $F$, residual envelope $C$, decidable boundary $D$, blocked edge $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$. No $\hsame$ transport or $\Cont$ replay may erase candidate or residual obligations, and no public read may collapse the handoff into unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationHandoffObligationStability}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The rows $K,N,F,C,D,B,T,R,P,L$ occur as separate coordinates, with $A$ only anchoring the audit-map source.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness preserves $K,F,N$, confluence handoff discipline preserves $C$ and $B$, and decidable-boundary discipline preserves $D$ as the bounded comparison boundary.

Use residual progress from \autoref{thm:metacic-candidate-normalization-residual-progress}. Residual data either remain retained premises through $K,F$ or obstruction rows through $C,B$, so a public read cannot erase the candidate or residual obligation while still preserving the handoff.

Apply the finite conversion certificate boundary of \autoref{thm:metacic-finite-conversion-certificate-boundary} and the obligation rows theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-obligation-rows}. They require $N,F,C,D,B,T,R,P,L$ to remain visible in one finite route and require $K$ as the candidate-evidence entry condition.

Finally read $T$ and $R$ only as componentwise transport and displayed replay. They preserve the already displayed rows and do not create a coordinate that can discharge or remove candidate evidence, residual-envelope data, or blocked edges. The excluded global endpoints are not carrier coordinates, so stability stays inside the finite handoff surface.
\end{proof}

\begin{theorem}[MetaCIC finite conversion endpoint handoff]
\label{thm:metacic-finite-conversion-endpoint-handoff}
Every finite conversion certificate exposed by an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet reaches residual confluence consumers only through the candidate-normalization row $K$, the residual-envelope row $C$, the blocked-edge row $B$, and the decidable-boundary row $D$. In particular, a consumer of the certificate must read the endpoint route through \autoref{def:metacic-finite-conversion-certificate-carrier}, \autoref{thm:metacic-finite-conversion-certificate-boundary}, \autoref{thm:metacic-candidate-normalization-residual-progress}, and \autoref{thm:metacic-candidate-normalization-confluence-rectangle}. The handoff does not assert full Church--Rosser, unrestricted subject reduction, unconditional strong normalization, global conversion decidability, quotient syntax equality, host evaluator soundness, or consistency.
\end{theorem}
\begin{proof}
Project the finite certificate from the carrier. By \autoref{def:metacic-finite-conversion-certificate-carrier}, the exposed endpoint is the displayed subroute through $N,F,C,D,B,T,R,P,L$, with $K$ retained as the candidate-evidence entry condition.

Apply \autoref{thm:metacic-finite-conversion-certificate-boundary}. The bounded conversion read is not detached from the handoff packet: it must display the normal endpoint, frontier, residual-envelope, decidable-boundary, blocked-edge, transport, replay, provenance, and local naming rows in one route.

Use residual progress and the guarded rectangle from \autoref{thm:metacic-candidate-normalization-residual-progress} and \autoref{thm:metacic-candidate-normalization-confluence-rectangle}. A residual confluence consumer either receives the bounded endpoint row through the candidate-mediated route or receives an obstruction row through $C$ and $B$.

Finally apply the handoff decider boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}. The bounded comparison passes through $D$ after the displayed endpoint and residual rows, so no consumer can read the certificate as one of the excluded global endpoints.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicFiniteConversionEndpointHandoff}

\begin{theorem}[MetaCIC candidate normalization public handoff]
\label{thm:metacic-candidate-normalization-confluence-handoff-public}
Any public confluence or decidability consumer of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet preserves candidate evidence $K$, normal endpoint $N$, normalization frontier $F$, residual envelope $C$, decidable boundary $D$, blocked edge $B$, transport $T$, replay $R$, provenance $P$, and local naming $L$ before reading a bounded endpoint or conversion row. The handoff does not assert unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, quotient syntax equality, host evaluator soundness, global conversion decidability, or closed consistency.
\end{theorem}
\begin{proof}
First apply the finite conversion certificate boundary of \autoref{thm:metacic-finite-conversion-certificate-boundary}. It keeps the bounded endpoint read inside the displayed endpoint, frontier, residual-envelope, decidable-boundary, blocked-edge, transport, replay, provenance, and local naming route.

Next use residual progress from \autoref{thm:metacic-candidate-normalization-residual-progress}. Residual data remain visible either as retained candidate premises through $K,F$ or as obstruction rows through $C,B$, so a public confluence consumer cannot erase them before reading a bounded endpoint.

Now apply the confluence rectangle and decidable-boundary theorems, \autoref{thm:metacic-candidate-normalization-confluence-rectangle} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}. They route bounded comparison through $D$ only after the candidate, endpoint, frontier, residual-envelope, and blocked-edge rows have been displayed.

Finally combine obligation stability and public endpoint handoff from \autoref{thm:metacic-candidate-normalization-handoff-obligation-stability} and \autoref{thm:metacic-finite-conversion-endpoint-handoff}. The structural rows $T,R,P,L$ preserve the same finite packet, and none of the excluded global endpoints is a carrier coordinate.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffPublic}

\begin{theorem}[MetaCIC normalization audit-map realization]
\label{thm:metacic-candidate-normalization-handoff-vision-concretization}
The $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet is the concrete finite $\NameCert$ realization of the normalization audit-map route of \autoref{ch:metacic-normalization-audit-map}. Its carrier displays candidate evidence $K$, bounded endpoint row $N$, frontier row $F$, residual-envelope row $C$, decidable-boundary row $D$, blocked-edge row $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$ as one guarded handoff. The realization does not assert unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The audit anchor $A$ binds the packet to \autoref{ch:metacic-normalization-audit-map}, and the rows $K,N,F,C,D,B,T,R,P,L$ display the finite candidate, endpoint, frontier, residual-envelope, decidable-boundary, blocked-edge, transport, replay, provenance, and local naming data.

Apply the handoff boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness forces $K,F,N$ to appear before any endpoint read, confluence handoff discipline keeps $C$ and $B$ visible, and decidable-boundary discipline routes bounded comparison through $D$.

Use the consumer boundary and endpoint handoff from \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary} and \autoref{thm:metacic-finite-conversion-endpoint-handoff}. Together they show that every public read of the audit-map route remains inside the displayed finite conversion certificate and its residual-envelope boundary.

Thus the vision route has a concrete finite packet: candidate evidence, bounded endpoint, frontier, residual envelope, decidable boundary, blocked edge, transport, replay, provenance, and naming are all carrier rows. Since none of the excluded global endpoints is a carrier coordinate, the realization stays a guarded NameCert handoff.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffVisionConcretization}

\begin{theorem}[Finite-observation MetaCIC diamond-envelope obligations]
\label{thm:finite-observation-metacic-diamond-envelope-namecert-obligations}
For every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet, a finite-observation diamond envelope exposes candidate evidence $K$, bounded endpoint row $N$, residual-envelope row $C$, local join and checker status through the decidable-boundary row $D$, blocked-edge retention through $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local naming $L$ before any confluence consumer reads a bounded join. The envelope is a finite observation route inside the existing handoff carrier; it is not unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.FiniteObservationMetaCICDiamondEnvelopeNameCertObligations}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The finite observation envelope can only use the displayed rows $K,N,F,C,D,B,T,R,P,L$.

Apply \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness keeps $K,F,N$ visible before a bounded endpoint is consumed, while confluence handoff discipline keeps the residual-envelope row $C$ and blocked-edge row $B$ visible before a residual or join read is offered.

Use \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-boundary}. The local checker status is read through $D$ only after the endpoint, residual-envelope, blocked-edge, transport, replay, provenance, and naming rows are displayed.

Finally apply \autoref{thm:metacic-finite-conversion-endpoint-handoff}. Any bounded join read remains inside the finite certificate route, so no consumer can read the envelope as an unconditional confluence or normalization theorem.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization confluence handoff obligation surface]
\label{thm:metacic-candidate-normalization-confluence-handoff-obligation-surface}
For every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet, the handoff exposes finite obligation rows for candidate evidence $K$, residual envelope $C$, decidable-boundary routing $D$, blocked-edge retention $B$, bounded endpoint readback $N$, componentwise transport $T$, replay $R$, provenance $P$, and local naming $L$. The route remains guarded by the audit anchor $A$ and frontier row $F$, and it exports none of strong normalization, full Church--Rosser, subject reduction, evaluator equality, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The displayed rows $A,K,N,F,C,D,B,T,R,P,L$ are the only coordinates available to a confluence-facing consumer.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary} and the premise-exposure theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. They force candidate evidence and frontier data to remain visible before endpoint, residual, or bounded-comparison rows are consumed.

Use residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention} and normal-endpoint readback from \autoref{thm:metacic-candidate-normalization-confluence-handoff-normal-endpoint-readback}. These route residual evidence through $C,B$ and endpoint reads through $N$ without collapsing the decidable-boundary row $D$.

Finally read $T,R,P,L$ as structural rows. They transport, replay, source, and locally name the same finite handoff, so the obligation surface is exhausted by the displayed coordinates and cannot be reinterpreted as any of the refused global MetaCIC endpoints.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffObligationSurface}

\begin{theorem}[Finite-observation MetaCIC diamond-envelope handoff]
\label{thm:finite-observation-metacic-diamond-envelope-handoff}
Every confluence or normalization consumer of a finite-observation MetaCIC diamond envelope receives a bounded join only through the candidate evidence, bounded endpoint, residual-envelope, local checker, blocked-edge, transport, replay, provenance, and local $\NameCert$ rows of \autoref{thm:finite-observation-metacic-diamond-envelope-namecert-obligations}. The handoff exports the bounded finite-observation route and no global Church--Rosser theorem, unconditional strong-normalization theorem, unrestricted subject-reduction theorem, global conversion decider, evaluator equality, quotient syntax equality, or consistency endpoint.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.FiniteObservationMetaCICDiamondEnvelopeHandoff}
\begin{proof}
Start with the finite-observation obligations. They place the candidate, endpoint, residual-envelope, checker, blocked-edge, transport, replay, provenance, and naming rows before any bounded join read.

Use the guarded rectangle of \autoref{thm:metacic-candidate-normalization-confluence-rectangle}. A bounded comparison either displays a common bounded endpoint row or records a residual obstruction, and both alternatives keep the candidate and residual coordinates visible.

Apply the finite conversion certificate boundary of \autoref{thm:metacic-finite-conversion-certificate-boundary}. The public consumer receives only the displayed finite route, so the handoff cannot be reinterpreted as a detached global confluence or normalization theorem.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization residual-diamond totality]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-totality}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet that exposes candidate evidence $K$, bounded normal endpoint row $N$, residual-retention rows $C,B$, and decidable-boundary row $D$ must supply the finite residual-diamond join route before any public confluence consumer reads it. The supplied route is the bounded candidate-normalization handoff through $A,K,N,F,C,D,B,T,R,P,L$; it is not a global Church--Rosser theorem, unrestricted subject-reduction theorem, unconditional strong-normalization theorem, quotient syntax equality, host evaluator soundness, global conversion decidability, or closed consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The rows $K,N,C,D,B$ are displayed beside the audit anchor, frontier row, componentwise transport, replay, provenance, and local naming rows, so a public confluence read has no coordinate outside $A,K,N,F,C,D,B,T,R,P,L$ from which to obtain a join.

Apply residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}. It requires residual or join reads to retain candidate evidence, the residual envelope, the decidable boundary, and the blocked-edge row before the endpoint row is treated as joinable.

Use the consumer boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-consumer-boundary}. A public consumer may read the handoff only after the candidate-mediated route, residual-envelope visibility, bounded comparison routing, blocked-edge retention, transport, replay, provenance, and local naming rows remain visible.

Finally combine the guarded rectangle of \autoref{thm:metacic-candidate-normalization-confluence-rectangle} with the finite-observation handoff of \autoref{thm:finite-observation-metacic-diamond-envelope-handoff}. The available join is therefore total only as a finite residual-diamond route supplied by the packet, and the excluded global endpoints remain absent from the carrier.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffResidualDiamondTotality}

\begin{theorem}[MetaCIC candidate-normalization residual-strip local join]
\label{thm:metacic-candidate-normalization-confluence-handoff-residual-strip-local-join}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet exposing candidate evidence $K$, bounded endpoint row $N$, residual envelope $C$, decidable boundary $D$, blocked-edge row $B$, componentwise transport $T$, displayed replay $R$, provenance $P$, and local $\NameCert$ row $L$ supplies only a local residual-strip join route for bounded candidate-normalization consumers. The route is read through the finite handoff rows $A,K,N,F,C,D,B,T,R,P,L$ and does not export full Church--Rosser, unrestricted subject reduction, unconditional strong normalization, quotient syntax equality, host evaluator soundness, global conversion decidability, or kernel consistency.
\end{theorem}
\begin{proof}
Project the obligation surface of \autoref{thm:metacic-candidate-normalization-confluence-handoff-obligation-surface}. It displays the candidate evidence, residual envelope, decidable boundary, blocked edge, bounded endpoint, transport, replay, provenance, and local naming rows as the only handoff coordinates available to a confluence-facing consumer.

Apply the finite-observation handoff of \autoref{thm:finite-observation-metacic-diamond-envelope-handoff}. A bounded join read is offered only after the finite observation envelope has kept the candidate row, endpoint row, residual row, checker row, blocked edge, and structural rows visible.

Use residual-diamond totality from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-totality}. It supplies the join only as a finite residual-diamond route inside $A,K,N,F,C,D,B,T,R,P,L$.

The nonescape clauses are therefore inherited from the same three routes. Since none of full Church--Rosser, unrestricted subject reduction, unconditional strong normalization, quotient syntax equality, host evaluator soundness, global conversion decidability, or kernel consistency is a carrier coordinate, the consumer receives a local residual-strip join and nothing global.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffResidualStripLocalJoin}

\begin{theorem}[MetaCIC candidate-normalization local diamond handoff]
\label{thm:metacic-normalization-confluence-local-diamond}
Let
$$
H=(A,K,N,F,C,D,B,T,R,P,L)
$$
be an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ carrier. If two one-step candidate-normalization residual reads share the audit anchor $A$, candidate evidence $K$, normalization frontier $F$, residual-envelope row $C$, decidable-boundary row $D$, blocked-edge row $B$, and the structural rows $T,R,P,L$, then any displayed join between their bounded endpoint reads is available only as a local diamond handoff through the residual-window row, candidate-join row, relation-scope row, and transport rows already carried by $C,D,B,T,R,P,L$.

The handoff preserves the same boundary rows on both sides of the diamond. It exports no full Church--Rosser theorem, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, closed consistency, or unrestricted normalization theorem.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicNormalizationConfluenceLocalDiamond}
\begin{proof}
Start with the carrier and boundary rows. The carrier displays $A,K,N,F,C,D,B,T,R,P,L$, and \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary} requires every confluence-facing read to keep candidate evidence, frontier data, residual envelope, decidable boundary, and blocked-edge retention visible.

Read the two one-step residuals through the same boundary data. Premise exposure and residual-diamond retention, \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}, prevent either branch from treating the bounded endpoint row as detached joinability data.

Use residual-diamond totality and residual-strip local join, \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-totality} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-strip-local-join}. They provide only the finite join route inside $A,K,N,F,C,D,B,T,R,P,L$, so the residual-window, candidate-join, and relation-scope rows remain displayed during the handoff.

Finally transport both sides through $T,R,P,L$. These rows preserve, replay, source, and name the same local diamond route; they do not create a coordinate for the refused global MetaCIC endpoints. Hence the displayed diamond is a local handoff over the retained residual and candidate rows, not a global confluence or normalization theorem.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization confluence handoff seed status]
\label{thm:metacic-candidate-normalization-confluence-handoff-seed-status}
A seed-status reader of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must expose candidate evidence $K$, normalization frontier $F$, residual confluence envelope $C$, decidable boundary $D$, blocked-edge row $B$, componentwise $\hsame$ transport $T$, displayed $\Cont$ replay $R$, $\Pkg$ provenance $P$, and local $\NameCert$ row $L$ before it consumes any bounded normal endpoint row $N$. The seed status is the finite retained-premise boundary of the handoff; it does not claim full Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. The only available rows are the audit anchor, candidate evidence, bounded endpoint, frontier, residual envelope, decidable boundary, blocked edge, transport, replay, provenance, and local naming data.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary} and the seed-closure route of \autoref{thm:metacic-candidate-normalization-confluence-handoff-seed-closure}. They keep $K,F,C,D,B$ visible before endpoint consumption.

Use premise exposure from \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}, residual-diamond retention from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}, and decider-row exhaustion from \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-row-exhaustion}. These force the residual envelope, decidable boundary, and blocked-edge row to remain displayed whenever a bounded normal endpoint is read.

Finally read $T,R,P,L$ as structural rows. They transport, replay, source, and name the same finite packet, so the seed-status reader cannot project any of the refused global MetaCIC endpoints from the handoff.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceHandoffSeedStatus}

\falsifiablePrediction{Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ read that exports a normal endpoint, residual-envelope row, or bounded conversion comparison without first exposing candidate evidence, the normalization frontier, the confluence residual envelope, the decidable boundary, blocked-edge retention, transport, replay, provenance, and local naming is outside the carrier surface.}

\independenceWitness{metaciccriticalpath, metacicnormalizationfrontier, metacic-confluence-residual-envelope, metacicdecidableboundary, metacic-normalization-confluence-bridge: no listed sibling supplies the joint handoff containing candidate evidence, bounded normal endpoint data, residual-envelope visibility, decidable-boundary routing, blocked-edge retention, transport, replay, provenance, and local NameCert rows while refusing unconditional SN and Church--Rosser.}

Lean follow-up should provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for the carrier so the candidate row, residual-envelope row, and decidable-boundary row can be audited as distinct fields rather than as a collapsed route.

The handoff also consumes the residual-diamond route of \autoref{ch:concrete-instances-parallel-diamond-residual-namecert} as a concrete prerequisite. The candidate-normalization row must expose its cut-height budget and residual join data before the confluence handoff can be read, so the rows $K,N,F,C,D,B$ remain tied to the parallel residual packet rather than becoming a standalone confluence claim.

\begin{theorem}[MetaCIC candidate normalization handoff sibling route]
\label{thm:metacic-candidate-normalization-handoff-orphan-route}
No $\MetacicCandidateNormalizationConfluenceHandoffUp$ consumer may read bounded normal endpoints or local residual-strip joins until it has exposed the candidate evidence row $K$, the $\MetaCICNormalizationFrontierUp$ row $F$, the residual confluence-envelope row $C$, the decidable-boundary row $D$, the blocked-edge row $B$, and the sibling residual-diamond route through \autoref{ch:concrete-instances-parallel-diamond-residual-namecert}. The available read is the finite handoff along $A,K,N,F,C,D,B,T,R,P,L$; it is not full Church--Rosser, unrestricted subject reduction, evaluator equality, quotient syntax equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary} and premise exposure \autoref{thm:metacic-candidate-normalization-confluence-handoff-premise-exposure}. They force the candidate evidence, normalization frontier, residual envelope, decidable boundary, and blocked edge to remain displayed before a bounded endpoint can be consumed.

Residual-diamond retention, \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-retention}, ties any join read to $K,C,D,B$ rather than to the endpoint row alone. Decider-row exhaustion in \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-row-exhaustion} keeps the decidable-boundary row as bounded checker evidence, not as a global conversion oracle.

Use the residual-strip local join theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-strip-local-join}. Its join route is local to the displayed packet $A,K,N,F,C,D,B,T,R,P,L$ and must keep the sibling residual-diamond route visible.

Finally apply seed status, \autoref{thm:metacic-candidate-normalization-confluence-handoff-seed-status}. The seed reader receives only the retained-premise boundary, so full Church--Rosser, unrestricted subject reduction, evaluator equality, quotient syntax equality, and kernel consistency remain outside the carrier.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationHandoffOrphanRoute}

\begin{theorem}[MetaCIC candidate handoff scoped TasteGate route]
\label{thm:metacic-candidate-handoff-scoped-tastegate-route}
Every later $\TasteGate$ instance for
$\MetacicCandidateNormalizationConfluenceHandoffUp$ must cite the exact
paper-side route:
\begin{enumerate}
\item the carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier};
\item the boundary of \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary};
\item the obligation surface of \autoref{thm:metacic-candidate-normalization-confluence-handoff-obligation-surface};
\item residual-diamond totality from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-totality};
\item residual-strip local join from \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-strip-local-join};
\item seed status from \autoref{thm:metacic-candidate-normalization-confluence-handoff-seed-status};
\item the sibling confluence handoff of \autoref{thm:metacic-candidate-normalization-handoff-orphan-route}.
\end{enumerate}
This route is the one needed by the $\GroundCompiler$ TasteGate check. It does
not supply full Church--Rosser, unrestricted subject reduction, quotient syntax
equality, evaluator equality, global conversion decidability, closed
consistency, or an unrestricted normalization theorem.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateHandoffScopedTasteGateRoute}
\begin{proof}
Start with the carrier and boundary. The carrier definition displays
$A,K,N,F,C,D,B,T,R,P,L$, while the boundary theorem keeps candidate evidence,
frontier data, residual envelope, decidable boundary, and blocked-edge
retention visible before any bounded endpoint is consumed.

Next read the obligation surface. It gives the rows a local
$\NameCert$ surface and prevents the handoff from being treated as a detached
MetaCIC confluence theorem.

Use residual-diamond totality and residual-strip local join. These theorems
supply only the finite join route inside $A,K,N,F,C,D,B,T,R,P,L$, with the
residual envelope and decidable-boundary row still displayed.

Finally cite seed status and the sibling route. Seed status records the
retained-premise boundary, and the sibling route keeps the critical-path,
normalization-frontier, confluence-audit, residual local-join, and
parallel-diamond residual surfaces visible. Thus a $\GroundCompiler$
TasteGate check can cite the scoped route above, while full Church--Rosser,
unrestricted subject reduction, quotient syntax equality, evaluator equality,
global conversion decidability, closed consistency, and unrestricted
normalization remain outside the claim.
\end{proof}

\begin{theorem}[MetaCIC candidate-normalization confluence scoped surface]
\label{thm:metacic-candidate-normalization-confluence-scoped-surface}
The scoped $\MetacicCandidateNormalizationConfluenceHandoffUp$ surface is exactly the carrier row, residual-diamond row, bounded join-window row, decider-row exhaustion row, finite conversion endpoint row, audit-map realization row, and sibling route row displayed in this chapter. The surface does not claim full Church--Rosser, unrestricted subject reduction, quotient syntax equality, evaluator equality, global conversion decidability, closed consistency, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the carrier and obligation surface, \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-obligation-surface}. They name the packet rows and keep the candidate evidence, frontier, residual envelope, decidable boundary, blocked edge, transport, replay, provenance, and local naming visible.

Next use residual-diamond totality and residual-strip local join, \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-totality} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-strip-local-join}. These provide only bounded local joins and finite conversion endpoints inside the displayed packet.

Then apply decider-row exhaustion and seed status, \autoref{thm:metacic-candidate-normalization-confluence-handoff-decider-row-exhaustion} and \autoref{thm:metacic-candidate-normalization-confluence-handoff-seed-status}. They keep the decider and audit-map realization rows as retained premises rather than global conversion or kernel-consistency claims.

Finally cite the sibling route \autoref{thm:metacic-candidate-normalization-handoff-orphan-route}. It binds the residual-diamond dependency and the finite handoff route, so the scoped surface is precisely the listed packet and sibling rows.
\end{proof}
\leanchecked{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.MetacicCandidateNormalizationConfluenceScopedSurface}

\closureat{MetacicCandidateNormalizationConfluenceHandoffUp}{seedStr}
\closureat{MetacicCandidateNormalizationConfluenceHandoffUp}{obligationStr}

\begin{closurestatus}{\MetacicCandidateNormalizationConfluenceHandoffUp}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}, \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}, \autoref{thm:metacic-candidate-normalization-confluence-handoff-obligation-surface}, \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-diamond-totality}, \autoref{thm:metacic-candidate-normalization-confluence-handoff-residual-strip-local-join}, \autoref{thm:metacic-candidate-normalization-confluence-handoff-seed-status}, and \autoref{thm:metacic-candidate-normalization-confluence-scoped-closure-route} record the scoped surface for candidate normalization, residual join, confluence handoff, finite-window replay, and MetaCIC frontier dependencies.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close unconditional strong normalization, full Church--Rosser, unrestricted subject reduction, global conversion decidability, quotient syntax equality, host evaluator soundness, closed consistency, Lean proof obligations, or bridge checking.}
  \upgradepath{Public closure requires checked carrier admission, candidate-gate exactness, confluence handoff discipline, decidable-boundary discipline, frontier non-collapse, Nontrivial, and FieldFaithful instances for BEDC.Derived.MetacicCandidateNormalizationConfluenceHandoffUp.}
  \constructivestory{A $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet is generated from a MetaCIC audit anchor, candidate evidence, bounded normal endpoint row, MetaCICNormalizationFrontierUp row, residual confluence-envelope row, MetaCICDecidableBoundaryUp row, blocked-edge retention, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
