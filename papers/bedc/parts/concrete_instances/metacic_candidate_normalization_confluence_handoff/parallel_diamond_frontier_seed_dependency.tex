\begin{theorem}[MetaCIC candidate normalization parallel-diamond frontier seed dependency]
\label{thm:metacic-candidate-normalization-confluence-handoff-parallel-diamond-frontier-seed-dependency}
Before any $\MetacicCandidateNormalizationConfluenceHandoffUp$ read may use the parallel frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, it must consume the residual-substitution premise, critical-pair row, candidate-join row, bounded-check row, and obstruction row exposed by that sibling. The handoff route then reads its bounded endpoint, residual envelope, decidable-boundary, blocked-edge, transport, replay, provenance, and local naming rows as downstream data rather than as a replacement for the parallel-diamond frontier seed surface.
\end{theorem}
\begin{proof}
Read the parallel-diamond frontier chapter first. Its seed surface keeps the residual-substitution premise, critical-pair row, candidate-join row, bounded-check row, obstruction row, and structural NameCert rows visible before any local confluence-facing read is available.

Apply the handoff carrier and candidate diamond budget routes in this chapter. They expose bounded endpoint, residual-envelope, decidable-boundary, blocked-edge, transport, replay, provenance, and local naming rows only after the upstream frontier dependency has been displayed.

Finally compare the dependency order. Since the candidate-normalization handoff has no coordinate for discharging residual substitution or replacing the frontier obstruction row, every accepted read that uses \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} must keep those sibling rows ahead of the handoff read.
\end{proof}
