\begin{theorem}[MetaCIC critical-path and parallel-frontier handoff route]
\label{thm:metacic-candidate-normalization-confluence-handoff-criticalpath-parallel-frontier-route}
Every confluence-facing consumer of an accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ packet must first read the MetaCIC critical-path surface of \autoref{ch:concrete-instances-metaciccriticalpath-namecert} and the parallel-diamond frontier route of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. Only after those sibling surfaces are displayed may the handoff be treated as a confluence-facing packet over candidate evidence, bounded endpoint rows, residual-envelope visibility, decidable-boundary routing, blocked-edge retention, transport, replay, provenance, and local naming. This is a finite MetaCIC route, not subject reduction, full Church--Rosser, unrestricted conversion decidability, quotient syntax equality, or kernel consistency.
\end{theorem}
\begin{proof}
Project the handoff carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. Its rows expose audit anchoring, candidate evidence, normal endpoint data, frontier data, residual confluence data, decidable-boundary data, blocked-edge retention, transport, replay, provenance, and local naming.

Apply the boundary theorem \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. It requires a confluence-facing read to keep the candidate gate, residual envelope, decidable boundary, and blocked-edge rows visible before an endpoint row can be consumed.

The sibling route is therefore read in two steps. The MetaCIC critical-path chapter supplies the upstream audit route, and the parallel-diamond frontier supplies the confluence-facing frontier route. The handoff can then pass finite candidate and residual data onward, but no row in the carrier turns that route into subject reduction, full Church--Rosser, unrestricted conversion decidability, quotient syntax equality, or kernel consistency.
\end{proof}
