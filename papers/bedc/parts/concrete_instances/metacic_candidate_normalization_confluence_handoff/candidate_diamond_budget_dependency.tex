The $\MetacicCandidateNormalizationConfluenceHandoffUp$ route consumes the finite local peak and join-budget rows of \autoref{ch:concrete-instances-metacic-candidate-diamond-budget-namecert} before any closed-normal readback is available. Thus the handoff exposes candidate-set membership, a bounded peak, and a local join budget as sibling data for the confluence lattice, rather than treating the closed-normal endpoint as an evidence-free consequence.
