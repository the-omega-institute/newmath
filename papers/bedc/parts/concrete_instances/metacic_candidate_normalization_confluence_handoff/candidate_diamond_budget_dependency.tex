\begin{theorem}[MetaCIC candidate normalization diamond-budget route]\label{thm:metacic-candidate-normalization-diamond-budget-route}
Every accepted $\MetacicCandidateNormalizationConfluenceHandoffUp$ closed-normal read must first expose candidate-set membership, a bounded local peak, join-budget rows, the sibling confluence route of \autoref{ch:concrete-instances-metacic-candidate-diamond-budget-namecert}, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\MetacicCandidateNormalizationConfluenceHandoffUp}$ row. Thus the closed-normal endpoint is consumed only after the diamond-budget dependency surface is visible, not as an evidence-free consequence.
\end{theorem}
\begin{proof}
Project the parent carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. Its accepted read displays candidate evidence, bounded endpoint data, residual confluence routing, decidable-boundary routing, blocked-edge retention, transport, replay, provenance, and local naming before any consumer can see a normal endpoint.

Apply the boundary theorem of \autoref{thm:metacic-candidate-normalization-confluence-handoff-boundary}. Candidate-gate exactness exposes the candidate-set membership row before the endpoint row is consumed, while confluence handoff discipline keeps the residual and blocked-edge rows visible.

The sibling route through \autoref{ch:concrete-instances-metacic-candidate-diamond-budget-namecert} supplies the bounded local peak and join-budget rows. Reading that sibling before the closed-normal endpoint preserves the finite diamond budget as a dependency of the handoff, and $T,R,P,L$ transport, replay, source, and name the same route without adding a global confluence or normalization coordinate.
\end{proof}
