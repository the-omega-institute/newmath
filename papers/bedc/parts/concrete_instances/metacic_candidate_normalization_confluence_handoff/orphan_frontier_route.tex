The candidate-normalization confluence handoff reads the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} through the local candidate-diamond-budget and residual-frontier siblings before any handoff row is made public. This route keeps candidate evidence, residual-envelope visibility, decidable-boundary rows, and blocked-edge retention visible inside the finite MetaCIC packet.

\begin{theorem}[MetaCIC candidate normalization handoff obligations]
\label{thm:metacic-candidate-normalization-handoff-obligations}
Every candidate-normalization confluence handoff read factors through $\MetaCICParallelDiamondFrontierUp$, the candidate-diamond-budget route, and the residual-frontier route siblings before any confluence handoff row is public. The handoff may then expose candidate evidence, bounded endpoint data, residual-envelope visibility, decidable-boundary routing, blocked-edge retention, transport, replay, provenance, and local naming, but it cannot bypass those frontier siblings.
\end{theorem}
\begin{proof}
Project the handoff carrier of \autoref{def:metacic-candidate-normalization-confluence-handoff-carrier}. Its public rows record candidate evidence, bounded endpoint data, residual confluence data, decidable-boundary data, blocked-edge retention, transport, replay, provenance, and local naming.

Use the local candidate-diamond-budget route and residual-frontier route siblings. The former exposes bounded local-peak and join-budget rows before a closed-normal endpoint is consumed, while the latter keeps the parallel-diamond frontier visible before a confluence-facing handoff is exported.

Finally read \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert} as the upstream frontier surface. Since the carrier has no row that makes the handoff public without those sibling routes, every accepted read factors through the displayed frontier dependency before presenting confluence handoff data.
\end{proof}
