\chapter{A Concrete Naming Certificate for $\FocusedLedgerUp$}
\label{ch:concrete-instances-focused-ledger-namecert}
\origin{ai}

The $\FocusedLedgerUp$ packet realizes the finite focused multi-Hist ledger of \autoref{subsec:philosophy-focused-multi-hist-ledgers} and \autoref{def:philosophy-focused-multi-hist-ledger} as a concrete NameCert carrier. It sits beside $\ConsciousUniverseSpaceUp$ in \autoref{ch:concrete-instances-consciousuniversespace-namecert}, $\ObserverHistIdentityUp$ in \autoref{ch:concrete-instances-observerhistidentity-namecert}, $\InterHistLocalityLedgerUp$ in \autoref{ch:concrete-instances-inter-hist-locality-ledger-namecert}, $\InscriptionEventUp$ in \autoref{ch:concrete-instances-inscriptionevent-namecert}, and $\OnticResidueLedgerUp$ in \autoref{ch:concrete-instances-ontic-residue-ledger-namecert}. Those siblings consume histories, identity rows, locality rows, inscription events, or residue rows; this packet binds the whole focus-selector-digest-gap-relation-invariant-audit tuple before those readings are separated.

\begin{definition}[Focused ledger carrier]
\label{def:focused-ledger-carrier}
A $\FocusedLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite index ledger naming the available history slots;
\item $H$ assigns a finite $\Hist$ row to each slot of $I$;
\item $T$ records local continuation chains for the displayed histories, without adding a global scalar clock;
\item $F$ is the focus index selecting the visible history slot;
\item $S$ is the selector ledger that records the branch choices and focus reads used by $F$;
\item $D$ is the digest ledger recording finite summaries whose provenance remains displayed;
\item $G$ is the gap and provenance row recording boundary pressure, missing observations, and source data for each digest;
\item $R$ is the inter-history relation row;
\item $J$ is the inter-history invariant row;
\item $Q$ is the dependence row, including optional maximum-rate readings when such rows are present;
\item $\rho$ is the focused self-proxy row for the selected history;
\item $A$ is the audit row refusing subject parameters, global clocks, hidden choosers, unledgered compression, object-level $T$, and hidden dependence on meta-closure;
\item $C$ records displayed $\Cont$ replay across $I,H,T,F,S,D,G,R,J,Q,\rho,A$;
\item $P$ records $\Pkg$ provenance over $\FocusedLedgerUp$, $\FocusedMultiHistLedgerUp$, $\ConsciousUniverseSpaceUp$, $\ObserverHistIdentityUp$, $\InterHistLocalityLedgerUp$, $\InscriptionEventUp$, $\OnticResidueLedgerUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\FocusedLedgerUp}$ row.
\end{enumerate}
The carrier has no coordinate for a subject substance, ambient universe object, hidden scheduler, unrecorded quotient of histories, object-level time object, or meta-closure oracle.
\end{definition}
\leandef{BEDC.Derived.FocusedLedgerUp.FocusedLedgerUp}

\begin{theorem}[Focused ledger audit closure]
\label{thm:focused-ledger-audit-closure}
For the carrier of \autoref{def:focused-ledger-carrier}, the audit row $A$ closes exactly the refusals of no subject, no global clock, no hidden chooser, no unledgered compression, no object-level $T$, and no hidden meta-closure dependence. Every accepted $\FocusedLedgerUp$ consumer must read focus, selection, digest, gap, relation, invariant, dependence, self-proxy, replay, provenance, and local naming through
$$
I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N.
$$
\end{theorem}
\begin{proof}
Project the carrier. The rows $I,H,T,F,S,D,G,R,J,Q,\rho$ expose the finite histories, focus, selector, digest, gap, relation, invariant, dependence, and self-proxy data that the packet is allowed to use.

Read the audit row next. The no-subject refusal prevents a hidden owner coordinate; the no-global-clock refusal keeps $T$ local; the no-hidden-chooser refusal forces all branch choice through $S$; the no-unledgered-compression refusal keeps summaries tied to $D$ and $G$; the no object-level $T$ refusal prevents a time object from replacing the local continuation rows; and the no hidden meta-closure refusal prevents the packet from citing a closure source not displayed in $A$.

Finally use the structural rows. The replay row $C$, provenance row $P$, and local name $N$ preserve and name only the displayed packet, so no consumer can recover an omitted subject, clock, chooser, compression, time object, or meta-closure dependency from the certificate surface.
\end{proof}

\begin{theorem}[Focused ledger NameCert obligation surface]
\label{thm:focused-ledger-namecert-obligation-surface}
The local $\NameCert_{\FocusedLedgerUp}$ surface for \autoref{def:focused-ledger-carrier} consists of exactly five obligation rows:
\begin{enumerate}
\item carrier admission for the finite packet $L=(I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N)$;
\item classifier stability for the selected history, selector ledger, digest ledger, relation row, invariant row, dependence row, and self-proxy row under componentwise $\hsame$ transport;
\item continuation exactness for the displayed $\Cont$ routes in $C$ and no replay route outside the finite index ledger $I$;
\item audit-row exactness for the six refusals recorded in $A$;
\item provenance and local naming through $P$ and $N$.
\end{enumerate}
\end{theorem}
\begin{proof}
The carrier definition displays all content rows before transport, replay, provenance, and naming. The first obligation admits that finite packet as the only carrier surface.

The second and third obligations are forced by the BEDC structural rows: $\hsame$ transport preserves the selected carrier fields componentwise, and $\Cont$ replay is read only through $C$.

The fourth obligation is \autoref{thm:focused-ledger-audit-closure}. The fifth obligation is the terminal NameCert packaging: $P$ records the source rows and $N$ names the accepted packet. No sixth obligation is available, because every accepted consumer already factors through the displayed carrier.
\end{proof}
\leanchecked{BEDC.Derived.FocusedLedgerUp.FocusedLedgerNameCert\_obligation\_surface}

\begin{definition}[Focused ledger kernel scope]
\label{def:focused-ledger-kernel-scope}
The kernel scope of a $\FocusedLedgerUp$ consumer is the displayed $\BHist$ route
$$
I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N
$$
from \autoref{def:focused-ledger-carrier}. A consumer is kernel-scoped when carrier admission, selector, digest, gap, relation, invariant, dependence, self-proxy, audit refusals, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming are all read through this route before any focused-ledger conclusion is exported.
\end{definition}

\begin{theorem}[Focused ledger kernel-scope route]
\label{thm:focused-ledger-kernel-scope-route}
Every $\FocusedLedgerUp$ consumer factors carrier admission, selector rows, digest rows, gap rows, relation rows, invariant rows, dependence rows, self-proxy rows, audit refusals, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and $\NameCert$ naming through the kernel scope of \autoref{def:focused-ledger-kernel-scope}. In particular, it cannot route the focused ledger through a subject parameter, global clock, hidden chooser, object-level time coordinate, unledgered compression step, or meta-closure oracle.
\end{theorem}
\begin{proof}
Start from the NameCert obligation surface, \autoref{thm:focused-ledger-namecert-obligation-surface}. Its carrier-admission and classifier-stability rows already name the selected history, selector ledger, digest ledger, relation row, invariant row, dependence row, and self-proxy row as displayed coordinates.

Next apply audit closure, \autoref{thm:focused-ledger-audit-closure}. The six refusals rule out the omitted subject, clock, chooser, compression, object-level time, and meta-closure coordinates before the consumer reaches replay or naming.

Finally read the kernel scope definition. The route ends with componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row, so any exported focused-ledger conclusion remains inside the displayed $\BHist$ kernel rows.
\end{proof}

\begin{theorem}[Focused ledger sibling independence]
\label{thm:focused-ledger-sibling-independence}
$\FocusedLedgerUp$ is not bijective with $\ConsciousUniverseSpaceUp$, $\ObserverHistIdentityUp$, $\InterHistLocalityLedgerUp$, $\InscriptionEventUp$, or $\OnticResidueLedgerUp$. The focused ledger carries the whole focus-selector-digest-gap-relation-invariant-audit tuple, while each sibling omits at least one of the required focused-ledger rows or one audit refusal.
\end{theorem}
\begin{proof}
Compare the carrier inventories. $\ConsciousUniverseSpaceUp$ reads universe-slice, locality-cell, local-time, and trace-identity structure after a focused ledger row is available, but it does not carry the full selector, digest, gap, relation, invariant, dependence, and audit tuple of \autoref{def:focused-ledger-carrier}.

$\ObserverHistIdentityUp$ supplies observer-history identity rows, and $\InterHistLocalityLedgerUp$ supplies inter-history locality rows. Neither packet contains the focused selector ledger, digest provenance, self-proxy, and six-refusal audit row together.

$\InscriptionEventUp$ records inscription-event admission, while $\OnticResidueLedgerUp$ records residue-bearing observation structure. Those packets can consume focused-ledger information, but neither contains the whole finite packet $I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N$. Therefore no sibling is bijective with $\FocusedLedgerUp$ as a NameCert carrier.
\end{proof}

\falsifiablePrediction{A $\FocusedLedgerUp$ packet cannot be reduced to an $\InscriptionEventUp$ packet without losing at least the selector, digest, relation, invariant, or audit row.}

\independenceWitness{consciousuniversespace, observerhistidentity, interhistlocalityledger, inscriptionevent, onticresidueledger: none is bijective with $\FocusedLedgerUp$ because each omits at least one required focused-ledger row or audit refusal.}

The Lean TasteGate file \texttt{lean4/BEDC/Derived/FocusedLedgerUp/TasteGate.lean} provides explicit $\mathsf{Nontrivial}\ \FocusedLedgerUp$ and $\mathsf{FieldFaithful}\ \FocusedLedgerUp$ instances over the same finite carrier.

\begin{theorem}[Focused ledger carrier totality]
\label{thm:focused-ledger-carrier-totality}
For every accepted $\FocusedLedgerUp$ carrier $L=(I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N)$, the rows $I,H,T,F,S,D,G,R,J,Q,\rho,A$ are finite $\BHist$ data before the structural rows are read. The rows $C,P,N$ then provide displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\FocusedLedgerUp}$ naming for exactly that finite carrier.
\end{theorem}
\begin{proof}
Project the carrier definition, \autoref{def:focused-ledger-carrier}. It lists the index ledger, history rows, continuation chains, focus index, selector ledger, digest ledger, gap row, relation row, invariant row, dependence row, self-proxy row, and audit row before any replay, provenance, or naming row appears.

Apply the audit closure theorem, \autoref{thm:focused-ledger-audit-closure}. The audit row refuses hidden subject, clock, chooser, compression, object-level time, and meta-closure coordinates, so the displayed carrier rows are the whole object surface.

Finally use the NameCert obligation surface, \autoref{thm:focused-ledger-namecert-obligation-surface}. It packages the same finite rows through $C,P,N$, and the sibling independence theorem prevents replacement by a neighboring packet that omits part of the focused-ledger carrier.
\end{proof}

\begin{theorem}[Focused ledger classifier stability]
\label{thm:focused-ledger-classifier-stability}
The classifier of an accepted $\FocusedLedgerUp$ packet is stable only through componentwise $\hsame$ transport over $I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N$. In particular, the selected history, selector ledger, digest ledger, relation row, invariant row, dependence row, self-proxy row, audit row, replay row, provenance row, and local name remain attached to the same finite $\BHist$ carrier.
\end{theorem}
\begin{proof}
Use the classifier-stability clause in \autoref{thm:focused-ledger-namecert-obligation-surface}. The clause names the selected history, selector ledger, digest ledger, relation row, invariant row, dependence row, and self-proxy row as transported fields rather than as host-level predicates.

Read the carrier totality theorem, \autoref{thm:focused-ledger-carrier-totality}. Since every classified field is part of the finite $\BHist$ packet, componentwise $\hsame$ transport cannot substitute an unledgered object for one of the displayed rows.

The TasteGate line supplies the same nontrivial and field-faithful carrier surface. Together with audit closure and sibling independence, it keeps the classifier on the focused-ledger rows rather than on a sibling packet or hidden meta-coordinate.
\end{proof}

\begin{theorem}[Focused ledger ledger exactness]
\label{thm:focused-ledger-ledger-exactness}
The ledger exported by $\FocusedLedgerUp$ is exact for the route
$$
I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N .
$$
Every consumer must read finite $\BHist$ rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming through that route before using the focus, digest, relation, invariant, dependence, self-proxy, or audit data.
\end{theorem}
\begin{proof}
Start from carrier totality, \autoref{thm:focused-ledger-carrier-totality}. It fixes the displayed ledger rows and prevents any omitted subject, clock, chooser, compression, object-level time, or meta-closure coordinate from becoming part of the carrier.

Apply classifier stability, \autoref{thm:focused-ledger-classifier-stability}. The same rows remain attached under componentwise $\hsame$ transport, so a consumer cannot alter the selected history or ledger fields while preserving the local classifier.

Finally apply audit closure, the NameCert obligation surface, sibling independence, and the TasteGate line. These force replay through $C$, provenance through $P$, and naming through $N$, while excluding sibling replacement and hidden dependencies. Hence the exported ledger is exact for the displayed BEDC kernel route.
\end{proof}

\begin{theorem}[Focused ledger public export surface]
\label{thm:focused-ledger-public-export-surface}
The public $\FocusedLedgerUp$ export is exactly the finite route
$$
I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N .
$$
Every public consumer of the packet receives the displayed index ledger, history rows, local continuation chains, focus index, selector ledger, digest ledger, gap and provenance row, inter-history relation row, invariant row, dependence row, self-proxy row, audit row, replay row, provenance row, and local naming row. No public export adds a subject coordinate, global clock, hidden chooser, unledgered compression step, object-level time object, meta-closure oracle, or sibling replacement.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:focused-ledger-carrier}. The listed rows are precisely the finite $\BHist$ packet admitted by the definition, and the carrier has no additional public coordinate.

Use the kernel-scope route of \autoref{thm:focused-ledger-kernel-scope-route} and ledger exactness of \autoref{thm:focused-ledger-ledger-exactness}. They force every consumer through the same focused selector, digest, relation, invariant, dependence, self-proxy, audit, replay, provenance, and naming rows before any focused-ledger conclusion is exported.

Finally apply sibling independence, \autoref{thm:focused-ledger-sibling-independence}. The surrounding packets may consume or support the focused ledger, but none of them carries the whole route $I,H,T,F,S,D,G,R,J,Q,\rho,A,C,P,N$. Therefore the public surface is exactly that displayed finite route.
\end{proof}

\closureat{FocusedLedgerUp}{seedStr}

\begin{closurestatus}{\FocusedLedgerUp}
  \constructivestory{A $\FocusedLedgerUp$ packet is generated from finite history slots, local continuation rows, a focus index, selector rows, digest rows, gap and provenance rows, relation rows, invariant rows, dependence rows, a self-proxy row, audit refusals, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:focused-ledger-carrier}, \autoref{def:focused-ledger-kernel-scope}, \autoref{thm:focused-ledger-audit-closure}, \autoref{thm:focused-ledger-namecert-obligation-surface}, \autoref{thm:focused-ledger-kernel-scope-route}, \autoref{thm:focused-ledger-sibling-independence}, \autoref{thm:focused-ledger-carrier-totality}, \autoref{thm:focused-ledger-classifier-stability}, \autoref{thm:focused-ledger-ledger-exactness}, and \autoref{thm:focused-ledger-public-export-surface} bind the finite focused multi-Hist ledger, its kernel-scoped obligations, and its public export route as a concrete NameCert packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, public-object export, global spacetime, subject substance, or hidden meta-closure.}
  \upgradepath{Formal closure requires checked carrier totality, classifier stability, ledger exactness, audit closure, sibling independence, provenance, and local naming for BEDC.Derived.FocusedLedgerUp.}
\end{closurestatus}
