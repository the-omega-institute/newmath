\chapter{A Concrete Naming Certificate for $\JamesCompactnessCriterionUp$}
\label{ch:concrete-instances-james-compactness-criterion-namecert}
\origin{human}

Following James's weak compactness criterion for bounded closed convex sets in Banach spaces, the $\JamesCompactnessCriterionUp$ packet records the finite BEDC surface by which a displayed weak compactness claim is tested through norm-attaining dual rows. It sits over the Banach carrier of \autoref{ch:concrete-instances-banach-namecert}, the weak topology surface of \autoref{ch:concrete-instances-weak-topology-namecert}, the weak compactness packet of \autoref{ch:concrete-instances-weak-compactness-namecert}, and the scalar comparison rows of \autoref{ch:concrete-instances-real-namecert}. The packet names only the finite criterion ledger and its local NameCert obligations; it does not assert a host dual ball, quotient weak topology, selected maximizing functional, or an ambient compactness theorem outside the displayed rows.

\begin{definition}[JamesCompactnessCriterion carrier]
\label{def:james-compactness-criterion-carrier}
A $\JamesCompactnessCriterionUp$ carrier is a finite $\BHist$ packet
$$
J=(B,W,K,D,A,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the ambient $\BanachUp$ row, including the finite normed carrier data inspected by the criterion.
\item $W$ is the $\WeakTopologyUp$ row on the displayed Banach data.
\item $K$ is the candidate $\WeakCompactnessUp$ row whose acceptance is being routed through the James criterion ledger.
\item $D$ is the finite dual-test ledger, listing only the displayed continuous linear functional names and their inspected bounded-set windows.
\item $A$ is the norm-attainment row, recording the finite witnesses that each displayed dual test attains its supremum on the candidate set.
\item $R$ is the $\RealUp$ scalar-comparison row for the displayed supremum and norm-attainment values.
\item $H$ records componentwise $\hsame$ transport for $B,W,K,D,A,R,C,P,N$.
\item $C$ records displayed $\Cont$ replay from Banach data through weak-topology exposure, finite dual tests, norm-attainment rows, scalar comparisons, and the weak-compactness handoff.
\item $P$ is the $\Pkg$ provenance over $\JamesCompactnessCriterionUp$, $\BanachUp$, $\WeakTopologyUp$, $\WeakCompactnessUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\JamesCompactnessCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected dual-ball point, quotient weak closure, countable choice, a host weak compactness theorem, or bridge checking data.
\end{definition}

\begin{theorem}[JamesCompactnessCriterion NameCert obligations]
\label{thm:james-compactness-criterion-namecert-obligations}
For every accepted $\JamesCompactnessCriterionUp$ carrier
$$
J=(B,W,K,D,A,R,H,C,P,N),
$$
the local $\NameCert_{\JamesCompactnessCriterionUp}$ surface consists exactly of Banach carrier exposure through $B$; weak-topology exposure through $W$; candidate weak-compactness exposure through $K$; finite dual-test coverage through $D$; norm-attainment witnesses through $A$; scalar comparison through $R$; componentwise transport and displayed replay through $H,C$; and provenance with local naming through $P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:james-compactness-criterion-carrier}. The analytic rows are precisely $B,W,K,D,A,R$, so a consumer first sees the Banach carrier, the weak topology, and the candidate weak compactness row.

The criterion-specific read is forced through $D,A,R$. The dual-test ledger $D$ records the displayed functional names, the attainment row $A$ records the finite witnesses for those tests, and the Real row $R$ supplies the scalar comparison boundary.

The remaining rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no further field, no consumer can extract a selected dual-ball element, quotient weak closure, countable-choice schedule, host compactness theorem, or bridge data from this packet.
\end{proof}

\begin{theorem}[JamesCompactnessCriterion weak-compactness route]
\label{thm:james-compactness-criterion-weak-compactness-route}
Every accepted $\JamesCompactnessCriterionUp$ packet exports a weak-compactness-facing conclusion only through the finite spine
$$
\BanachUp \longmapsto
\WeakTopologyUp \longmapsto
\RealUp \longmapsto
\WeakCompactnessUp .
$$
For a carrier $J=(B,W,K,D,A,R,H,C,P,N)$, any downstream weak-compactness consumer must inspect the Banach row, weak-topology row, finite dual-test ledger, norm-attainment row, scalar comparison row, and componentwise $\hsame$ transport before it may read $K$ as weak-compactness evidence. The route is a BEDC NameCert handoff, not an ambient theorem about all bounded closed convex subsets.
\end{theorem}
\begin{proof}
Start with the sibling rows named in the chapter introduction. The $\BanachUp$ row supplies the normed carrier, the $\WeakTopologyUp$ row supplies the topology-facing exposure, the $\RealUp$ row supplies scalar comparison data, and the $\WeakCompactnessUp$ row is the only output coordinate.

Next read the James ledger rows $D,A$. They ensure that every displayed dual test and every displayed norm-attainment witness is consumed before the weak-compactness row $K$ is exported.

Finally $H,C,P,N$ preserve the same route under componentwise transport, displayed replay, provenance, and local naming. Thus the route reaches weak compactness only through the displayed finite certificate rows and not through a host duality or compactness oracle.
\end{proof}

\falsifiablePrediction{An accepted $\JamesCompactnessCriterionUp$ consumer is refuted if it claims weak compactness without displaying the $\BanachUp$ carrier row, $\WeakTopologyUp$ exposure row, finite dual-test ledger, norm-attainment row, $\RealUp$ scalar comparison, $\WeakCompactnessUp$ handoff, transport, replay, provenance, and local $\NameCert$ row.}

\independenceWitness{banach, weak-topology, weak-compactness, real: $\BanachUp$ supplies normed carrier data without the James dual-test ledger; $\WeakTopologyUp$ supplies weak-open exposure without norm-attainment witnesses; $\WeakCompactnessUp$ supplies a compactness-facing row without the criterion route; and $\RealUp$ supplies scalar comparisons without Banach or weak-topology provenance. None is bijective to $\JamesCompactnessCriterionUp$ under NameCert-row preservation.}

\closureat{JamesCompactnessCriterionUp}{seedStr}

\begin{closurestatus}{\JamesCompactnessCriterionUp}
  \constructivestory{A $\JamesCompactnessCriterionUp$ packet is generated from Banach carrier rows, weak-topology rows, weak-compactness candidate rows, finite dual-test ledgers, norm-attainment witnesses, Real scalar comparisons, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:james-compactness-criterion-carrier}, \autoref{thm:james-compactness-criterion-namecert-obligations}, \autoref{thm:james-compactness-criterion-weak-compactness-route}, the falsifiable prediction, and the independence witness seed the finite James compactness criterion carrier and weak-compactness route over Banach, WeakTopology, WeakCompactness, Real, hsame, Cont, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host dual ball, quotient weak closure, selected norm-attaining functionals beyond displayed rows, the full ambient James theorem, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite dual-test coverage, norm-attainment routing, scalar comparison, weak-compactness handoff, transport, replay, provenance, and local naming for BEDC.Derived.JamesCompactnessCriterionUp.}
\end{closurestatus}
