\chapter{A Concrete Naming Certificate for $\RealDedekindCompletenessBoundaryUp$}
\label{ch:concrete-instances-real-dedekind-completeness-boundary-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RealDedekindCompletenessBoundaryUp$ packet records the finite boundary around Dedekind completeness for the real-name route. It sits beside the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the Cauchy route of \autoref{ch:concrete-instances-real-cauchy-completeness-namecert}, and the located substitute of \autoref{ch:concrete-instances-locatedsupremum-namecert}. The packet names the separation between accepted Cauchy and located real-name handoffs and the unavailable assertion that $\RealUp$ is Dedekind complete.

\begin{definition}[Real Dedekind-completeness boundary carrier]
\label{def:real-dedekind-completeness-boundary-carrier}
A $\RealDedekindCompletenessBoundaryUp$ carrier is a finite $\BHist$ packet
$$
K=(D,R,S,L,C,H,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the cut-style request row, recording the attempted least-upper-bound or cut-completion read without admitting a Dedekind-cut carrier.
\item $R$ is the $\RealUp$ seal row, exposing only the existing real-name endpoint accepted by the surrounding real-name route.
\item $S$ is the joint $\StreamNameUp$ and $\RegSeqRatUp$ readback row, requiring every accepted real-facing observation to pass through finite stream windows and regular rational data.
\item $L$ is the located substitute row, pointing the request toward located supremum or located Cauchy-completeness surfaces when those rows are present.
\item $C$ records displayed $\Cont$ replay from the cut-style request to the real-name seal, the readback row, the located substitute, and the local boundary name.
\item $H$ records componentwise $\hsame$ transport for $D,R,S,L,C,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealDedekindCompletenessBoundaryUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedSupremumUp$, $\RealCauchyCompletenessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealDedekindCompletenessBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,R,S,L,C,H,P,N$. It has no coordinate for a Dedekind-cut construction, quotient real equality, trichotomy, countable choice, selected suprema, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real Dedekind-completeness boundary NameCert obligations]
\label{thm:real-dedekind-completeness-boundary-namecert-obligations}
For every accepted $\RealDedekindCompletenessBoundaryUp$ carrier $K=(D,R,S,L,C,H,P,N)$, the local $\NameCert_{\RealDedekindCompletenessBoundaryUp}$ surface consists exactly of carrier admission for the eight displayed rows, route exposure through the $\RealUp$ seal and the $\StreamNameUp$--$\RegSeqRatUp$ readback row, located substitute exactness through $L$, non-claim boundary for the omitted Dedekind-completeness coordinates, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-dedekind-completeness-boundary-carrier}. The packet exposes only $D,R,S,L,C,H,P,N$, so any boundary read must begin with those rows.

Route exposure is forced by $R$ and $S$. The real-facing endpoint is the existing $\RealUp$ seal $R$, and the readback surface $S$ requires finite $\StreamNameUp$ windows together with $\RegSeqRatUp$ data before the endpoint can be consumed.

The located substitute is exactly the row $L$. When a supremum-shaped request has located evidence, $L$ points to the located route rather than to an arbitrary cut-completion assertion; otherwise the request remains only the displayed boundary row $D$.

The non-claim boundary follows from the missing coordinates. Since no row stores Dedekind-cut construction data, quotient real equality, trichotomy, countable choice, selected suprema, or ambient completeness, the local certificate cannot export any of those principles.

Finally $H,C,P,N$ transport, replay, source, and name precisely the same rows. Componentwise transport preserves the boundary inventory, continuation replay preserves the read order, and the provenance ledger adds no independent completeness coordinate.
\end{proof}

\falsifiablePrediction{Every accepted $\RealDedekindCompletenessBoundaryUp$ consumer that presents a Dedekind-completeness read for RealUp must display the cut-style request row, the RealUp seal row, the StreamName and RegSeqRat readback row, the located substitute row, transport, continuation replay, provenance, and local naming.}

\independenceWitness{real, streamname, regseqrat, locatedsupremum, real-cauchy-completeness: $\RealDedekindCompletenessBoundaryUp$ is not bijective to any listed sibling because it stores the rejected cut-style request together with the accepted real-name seal, finite readback route, located substitute, transport, replay, provenance, and local NameCert row.}

\closureat{RealDedekindCompletenessBoundaryUp}{seedStr}

\begin{closurestatus}{\RealDedekindCompletenessBoundaryUp}
  \constructivestory{A $\RealDedekindCompletenessBoundaryUp$ packet is generated from a cut-style request row, a RealUp seal row, StreamName and RegSeqRat readback, a located substitute row, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-dedekind-completeness-boundary-carrier} and \autoref{thm:real-dedekind-completeness-boundary-namecert-obligations} seed the finite boundary separating accepted Cauchy and located RealUp routes from a Dedekind-completeness claim.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter excludes Dedekind-cut construction, quotient real equality, trichotomy, countable choice, selected suprema, ambient Real completeness, Lean verification, and bridge checking.}
  \upgradepath{Formal verification requires a Lean carrier and checked NameCert obligations for the displayed Real Dedekind-completeness boundary route.}
\end{closurestatus}
