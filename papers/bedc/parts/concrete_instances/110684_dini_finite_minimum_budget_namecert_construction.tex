\chapter{A Concrete Naming Certificate for $\DiniFiniteMinimumBudgetUp$}
\label{ch:concrete-instances-dini-finite-minimum-budget-namecert}
\origin{human}

Following Dini's theorem and Bishop--Bridges constructive compactness, the $\DiniFiniteMinimumBudgetUp$ packet records the finite extraction row that turns a monotone compact-window Dini certificate into one displayed tolerance budget. It consumes the $\CompactMetricUp$ source, monotone $\ContinuousMapUp$ family rows, the $\DiniTheoremUp$ compact-window route, $\UniformModulusUp$ handoff data, $\DyadicRatCoreUp$ tolerance cells, the terminal $\RealUp$ seal, $\BHist$ history rows, $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data. The packet names only the finite minimum-budget route; it is not host supremum selection, a selected minimum outside the carrier, open-cover compactness, quotient equality, or ambient $\RealUp$ completeness.

The dependency surface is horizontal: \autoref{ch:concrete-instances-dini-theorem-namecert} supplies the compact-window Dini route, \autoref{ch:concrete-instances-compactmetric-namecert} supplies the inspected compact carrier, \autoref{ch:concrete-instances-continuousmap-namecert} supplies the monotone continuous-family rows, and \autoref{ch:concrete-instances-uniformmodulus-namecert} receives the extracted budget. Thus the minimum-budget row is a downstream finite extraction from those sibling carriers, not an isolated tolerance selector.

\begin{definition}[Dini finite-minimum budget carrier]
\label{def:dini-finite-minimum-budget-carrier}
A $\DiniFiniteMinimumBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(K,F,D,U,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row naming the finite compact-metric windows inspected by the extraction.
\item $F$ is the monotone $\ContinuousMapUp$ family row, including the displayed pointwise comparison ledger used by the Dini route.
\item $D$ is the $\DiniTheoremUp$ row that supplies the monotone uniform-window certificate over $K$ and $F$.
\item $U$ is the $\UniformModulusUp$ handoff row consumed by uniform-limit and compact-uniform-modulus readers.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance row, listing the candidate dyadic budgets and their finite comparison cells.
\item $E$ is the terminal $\RealUp$ seal row reached by every displayed tolerance and value comparison.
\item $H$ records componentwise $\hsame$ transport for $K,F,D,U,Q,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from compact windows and monotone family rows through the Dini row, the finite dyadic budget comparisons, the uniform-modulus handoff, and the Real seal.
\item $P$ is the $\Pkg$ provenance over $\DiniFiniteMinimumBudgetUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\DiniTheoremUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DiniFiniteMinimumBudgetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for an ambient supremum, an externally selected minimum, open-cover compactness, quotient equality of real names, countable choice, or host Real completeness.
\end{definition}

\begin{theorem}[Dini compact finite-minimum budget]
\label{thm:dini-theorem-compact-finite-minimum-budget}
For every accepted $\DiniFiniteMinimumBudgetUp$ carrier
$$
B=(K,F,D,U,Q,E,H,C,P,N),
$$
the finite probe cover displayed by $K,F,D$ and the dyadic tolerance cells displayed by $Q$ determine one displayed minimum tolerance budget before the uniform-modulus handoff $U$ is exported. The extraction route is
$$
K,F,D,Q,E,H,C,P,N \longrightarrow U .
$$
Thus a $\UniformLimitUp$ or compact-uniform-modulus consumer receives a finite $\DyadicRatCoreUp$ budget sealed by $\RealUp$ and transported by $\hsame$ and $\Cont$, without host supremum, selected minimum outside the carrier, open-cover compactness, quotient equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted packet. The compact source row $K$, monotone family row $F$, and Dini row $D$ are the only rows that can name the finite probe cover for the tolerance request.

The dyadic row $Q$ lists the candidate tolerance cells. Since the carrier is finite, the budget comparison is a displayed finite comparison among the cells in $Q$, with every real-valued read sealed through $E$ before the handoff row is exposed.

The route to $U$ is then replayed by $C$ and transported componentwise by $H$. The provenance and naming rows $P,N$ record the same finite route as the local certificate surface.

No excluded object is a coordinate of $B$. Consequently the exported budget is the displayed finite minimum-budget row inside the carrier, not an ambient supremum, external selector, open-cover argument, quotient equality, choice schedule, or completeness argument.
\end{proof}

\falsifiablePrediction{Every accepted $\DiniFiniteMinimumBudgetUp$ consumer that exports a $\UniformModulusUp$ handoff must first expose the compact-metric windows, monotone continuous-family rows, DiniTheorem route, finite $\DyadicRatCoreUp$ budget cells, $\RealUp$ seal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows.}

\independenceWitness{dini-theorem, compactmetric, continuousmap, uniformmodulus, dyadicratcore, real: $\DiniFiniteMinimumBudgetUp$ is not bijective to any listed sibling because it binds the finite Dini probe cover to one displayed dyadic minimum-budget handoff before uniform-limit or compact-uniform-modulus consumers may read the result.}

\closureat{DiniFiniteMinimumBudgetUp}{seedStr}

\begin{closurestatus}{\DiniFiniteMinimumBudgetUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dini-finite-minimum-budget-carrier} and \autoref{thm:dini-theorem-compact-finite-minimum-budget} seed the finite minimum-budget extraction route from compact Dini data to a uniform-modulus-facing handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host supremum selection, selected minima outside the carrier, open-cover compactness, quotient equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite probe-cover exposure, dyadic minimum-budget extraction, Real-seal nonescape, transport and replay stability, provenance exhaustion, and local naming for BEDC.Derived.DiniFiniteMinimumBudgetUp.}
  \constructivestory{A $\DiniFiniteMinimumBudgetUp$ packet is generated from compact-metric source rows, monotone ContinuousMap family rows, a DiniTheorem compact-window route, UniformModulus handoff rows, DyadicRatCore tolerance cells, a Real seal, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
\end{closurestatus}
