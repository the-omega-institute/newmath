\chapter{A Concrete Naming Certificate for $\RegularCauchyReflectorLawUp$}
\label{ch:concrete-instances-regular-cauchy-reflector-law-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names and constructive completions, the $\RegularCauchyReflectorLawUp$ packet records the finite law surface by which a regular Cauchy source is reflected into the Real and Completion corridor only after its windows, tolerances, readback, reflector route, and terminal seal are displayed. It sits over the modulus ledger of \autoref{ch:concrete-instances-bishop-regular-cauchy-modulus-ledger-namecert}, the reflector route of \autoref{ch:concrete-instances-uniform-cauchy-reflector-namecert}, the comparison surface of \autoref{ch:concrete-instances-metric-completion-reflection-comparison-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a textbook-grounded L10 completion-law carrier; it does not quotient streams, select limits, compare host real values, import countable choice, or assert ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy reflector law carrier]
\label{def:regular-cauchy-reflector-law-carrier}
A $\RegularCauchyReflectorLawUp$ carrier is a finite $\BHist$ packet
$$
L=(S,R,D,M,F,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row naming the inspected regular Cauchy source windows.
\item $R$ is the $\RegSeqRatUp$ readback row for those same windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the requested radius, halving budget, and comparison cells.
\item $M$ is the $\BishopRegularCauchyModulusLedgerUp$ row that exposes the finite modulus schedule used by the law.
\item $F$ is the $\UniformCauchyReflectorUp$ route row, binding $S,R,D,M$ to the reflector-facing handoff.
\item $E$ is the terminal $\RealUp$ seal row, readable only after $S,R,D,M,F$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for windows, readback, tolerances, modulus ledger, reflector route, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a law request through $S,R,D,M,F,E$ and then into the structural rows.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyReflectorLawUp$, $\BishopRegularCauchyModulusLedgerUp$, $\UniformCauchyReflectorUp$, $\MetricCompletionReflectionComparisonUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyReflectorLawUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,D,M,F,E,H,C,P,N$. It has no coordinate for quotient regular streams, selected representatives, host completion operators, countable choice, host equality, or ambient completeness.
\end{definition}

\begin{theorem}[Regular Cauchy reflector law NameCert obligations]
\label{thm:regular-cauchy-reflector-law-namecert-obligations}
For every accepted $\RegularCauchyReflectorLawUp$ carrier
$$
L=(S,R,D,M,F,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyReflectorLawUp}$ surface consists exactly of StreamName window exposure through $S$, RegSeqRat readback through $R$, DyadicRatCore tolerance admission through $D$, modulus-ledger exposure through $M$, uniform-reflector route exposure through $F$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$. Every completion-law consumer must display those rows before treating the reflector read as a Real or Completion handoff.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-reflector-law-carrier}. The analytic rows are exactly the finite windows, regular rational readback, dyadic tolerance, modulus ledger, reflector route, and Real seal.

Read those rows in dependency order. The window row $S$ determines what is inspected, $R$ reads the same windows as regular rational data, and $D$ fixes the tolerance budget. The ledger row $M$ supplies the finite modulus schedule before the reflector row $F$ can export a law-shaped handoff.

The terminal seal $E$ is downstream of that displayed route. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same packet and do not add another completion object.

Since quotient streams, selected representatives, host completion operators, countable choice, host equality, and ambient completeness are absent from the carrier, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy reflector law completion consumer]
\label{thm:regular-cauchy-reflector-law-completion-consumer}
Every Real or Completion consumer of an accepted $\RegularCauchyReflectorLawUp$ packet factors its read through
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \BishopRegularCauchyModulusLedgerUp \longmapsto \UniformCauchyReflectorUp \longmapsto \RealUp
$$
using the rows $S,R,D,M,F,E,H,C,P,N$. The consumer may compare the result with other completion presentations only through the displayed comparison discipline of \autoref{ch:concrete-instances-metric-completion-reflection-comparison-namecert}; it cannot bypass the reflector-law packet by quotienting regular streams, selecting limits, or invoking ambient completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations. They force every law read to expose $S,R,D,M,F,E$ before the structural rows can name the packet.

The modulus ledger row $M$ supplies the finite schedule that turns a tolerance request into inspected windows. The reflector row $F$ then binds that scheduled read to the uniform reflector route, so a consumer reaches the terminal Real seal only after the displayed regular Cauchy data have been read.

If a consumer compares the result with another completion presentation, the comparison is still mediated by the sibling reflection-comparison chapter. That comparison reads finite windows, regular readback, dyadic tolerances, and Real seals; it does not create a quotient completion or a selected representative.

Thus every Real or Completion consumer remains inside the finite route shown above, and any route using quotient streams, selected limits, host equality, choice, or ambient completeness leaves the accepted carrier.
\end{proof}

\falsifiablePrediction{A completion-law consumer that reaches a Real or Completion handoff from $\RegularCauchyReflectorLawUp$ without displaying StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, the modulus ledger, the uniform reflector route, and the terminal Real seal refutes the carrier.}

\independenceWitness{bishop-regular-cauchy-modulus-ledger, uniform-cauchy-reflector, metric-completion-reflection-comparison, streamname, regseqrat, dyadicratcore, real: no listed sibling is bijective to $\RegularCauchyReflectorLawUp$ because this packet binds the regular Cauchy law surface, the reflector route, the Real seal, transport, replay, provenance, and local naming as one finite completion-law carrier.}

\closureat{RegularCauchyReflectorLawUp}{seedStr}

\begin{closurestatus}{\RegularCauchyReflectorLawUp}
  \constructivestory{A $\RegularCauchyReflectorLawUp$ packet is generated from StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, a BishopRegularCauchyModulusLedger row, a UniformCauchyReflector route, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-reflector-law-carrier}, \autoref{thm:regular-cauchy-reflector-law-namecert-obligations}, and \autoref{thm:regular-cauchy-reflector-law-completion-consumer} seed the finite regular-Cauchy reflector-law route consumed by Real and Completion handoffs.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient regular streams, selected representatives, host completion operators, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exposure, regular readback, dyadic tolerance exactness, modulus-ledger exposure, reflector-route exposure, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyReflectorLawUp.}
\end{closurestatus}
