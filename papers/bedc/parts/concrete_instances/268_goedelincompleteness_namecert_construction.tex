\chapter{A Concrete Naming Certificate for $\GoedelIncompletenessUp$}
\label{ch:concrete-instances-goedelincompleteness-namecert}

The Gödel-incompleteness interface $\GoedelIncompletenessUp$ packages an arithmetised provability predicate over a recursively-axiomatised first-order theory of arithmetic, together with a self-referential fixed-point row. It depends on $\NameCert_{\NatUp}$ (\autoref{ch:concrete-instances-nat-namecert}), $\NameCert_{\FirstOrderUp}$ (\autoref{ch:concrete-instances-firstorder-namecert}), $\NameCert_{\ModelTheoryUp}$ (\autoref{ch:concrete-instances-modeltheory-namecert}), and $\NameCert_{\ComputableUp}$ (\autoref{ch:concrete-instances-computable-namecert}).

\section{Carrier definition}
\label{sec:goedelincompleteness-carrier}

A $\GoedelIncompletenessUp$ token carries: a recursively-enumerated axiom set $T$ over the language of arithmetic, an arithmetised provability predicate $\Prov_T(x)$ via a $\NameCert_{\ComputableUp}$ proof-checking decoder, a Gödel numbering $\ulcorner\cdot\urcorner$ on formulas, and a fixed-point row $G$ with $T\vdash G\leftrightarrow\neg\Prov_T(\ulcorner G\urcorner)$. The carrier accepts an incompleteness witness when $T$ is consistent and $G$ is neither provable nor refutable in $T$. The classifier is $\hsame$ on the fixed-point rows; the ledger keeps the axiom enumeration, the proof-checker, the Gödel numbering, and the fixed-point row visible.

\begin{closurestatus}{\GoedelIncompletenessUp}
  \constructivestory{A $\GoedelIncompletenessUp$ token names a recursively-enumerated axiom set $T$ over the language of arithmetic, an arithmetised provability predicate $\Prov_T(x)$ realised by a $\NameCert_{\ComputableUp}$ proof-checking decoder, a Gödel numbering $\ulcorner\cdot\urcorner$ on formulas, and a self-referential fixed-point row $G$ such that $T\vdash G\leftrightarrow\neg\Prov_T(\ulcorner G\urcorner)$. The carrier accepts an incompleteness witness when $T$ is consistent in the sense that no proof-checker run yields $\bot$, and $G$ is neither provable nor refutable in $T$. The classifier is $\hsame$ on the fixed-point rows at fixed Gödel numbering; the source covers the axiom enumeration, the proof-checker decoder, Gödel-number rows, fixed-point rows, and provability witnesses; the pattern records consistency admissibility, fixed-point admissibility, derivability admissibility, refutability admissibility, and recursive-axiomatisation admissibility rows; the ledger keeps the axiom enumeration, the proof-checker, the Gödel numbering, and the fixed-point row visible. The five name-certificate obligations are supplied by $\NameCert_{\ComputableUp}$ for the proof-checker plus $\NameCert_{\FirstOrderUp}$ and $\NameCert_{\ModelTheoryUp}$ for the syntactic carrier: the inhabited identity certificate is the empty axiom system over the language of arithmetic; classifier stability is reflexive, symmetric, and transitive $\hsame$ on fixed-point rows; transport repacks proof-checks through Gödel-number transport; ledger exactness identifies the accepted witness with the fixed-point identity; the extension boundary fixes the recursive-axiomatisation rule. A history $h$ lies in the signature gap of a $\GoedelIncompletenessUp$ token $p$ when the source-domain accepts the consistent recursively-axiomatised theory, the signature relation pairs it with the undecidable fixed-point row, and token introduction names that fixed point as $p$. The chapter registers this self-referential incompleteness surface under the name $\GoedelIncompletenessUp$; it is a finite-axiomatisation incompleteness object with no semantic-completeness theorem, no second-incompleteness theorem ($\neg\Prov_T(\bot)$ unprovability), no $\omega$-consistency claim, and no standard set-theoretic foundations bridge.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope binds the recursively-enumerated axiom set, the proof-checker decoder, the Gödel numbering, the fixed-point row $G$, and the consistency witness for the theory.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close the second-incompleteness theorem, $\omega$-consistency, semantic-completeness, the Löb conditions, or a standard set-theoretic foundations bridge.}
  \upgradepath{Obligation closure requires a checked $\GoedelIncompletenessUp$ fixed-point obligation theorem covering consistency, fixed-point identity, and undecidability of $G$.}
\end{closurestatus}
