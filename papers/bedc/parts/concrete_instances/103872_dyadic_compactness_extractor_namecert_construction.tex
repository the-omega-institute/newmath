\chapter{A Concrete Naming Certificate for $\DyadicCompactnessExtractorUp$}
\label{ch:concrete-instances-dyadic-compactness-extractor-namecert}
\origin{ai}

The $\DyadicCompactnessExtractorUp$ packet realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} as a finite extractor surface: dyadic compactness rows provide a finite net and dyadic radius ledger, metric triangle rows transport the estimate, continuous-map rows supply pointwise moduli, and the resulting route hands a tolerance-only radius to $\UniformModulusUp$. It sits beside the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the metric triangle surface of \autoref{ch:concrete-instances-metric-namecert}, the pointwise graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the Lebesgue-radius ledger of \autoref{ch:concrete-instances-finite-lebesgue-number-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names the finite extractor route only; it does not assert open-cover compactness, sequential compactness, a host Lebesgue number, or an ambient completeness theorem.

\begin{definition}[Dyadic compactness extractor carrier]
\label{def:dyadic-compactness-extractor-carrier}
A $\DyadicCompactnessExtractorUp$ carrier is a finite $\BHist$ packet
$$
D=(K,M,T,R,F,L,U,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row, including the compact carrier and the displayed ProbeBundle center family.
\item $M$ is the $\MetricUp$ triangle row for the source and target metric estimates used by the extractor.
\item $T$ is the dyadic radius row naming the finite precision scale attached to the accepted compact net.
\item $R$ is the finite-net readback row that associates every accepted source point request with a displayed center and dyadic radius from $T$.
\item $F$ is the $\ContinuousMapUp$ row carrying pointwise modulus data at each displayed center.
\item $L$ is the $\FiniteLebesgueNumberUp$ ledger row that records the finite lower-radius comparison consumed by the uniform route.
\item $U$ is the $\UniformModulusUp$ handoff row produced from $K,M,T,R,F,L$.
\item $H$ records componentwise $\hsame$ transport for compact source, metric triangle, dyadic radius, finite-net readback, continuous-map modulus, Lebesgue ledger, uniform-modulus handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-uniform request through $K,M,T,R,F,L,U,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\DyadicCompactnessExtractorUp$, $\CompactMetricUp$, $\MetricUp$, $\ContinuousMapUp$, $\FiniteLebesgueNumberUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicCompactnessExtractorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,T,R,F,L,U,H,C,P,N$. It has no coordinate for an open cover, selected subcover, sequential compactness argument, host Lebesgue number, host minimum selector, choice principle, or ambient completeness theorem.
\end{definition}

\begin{theorem}[Dyadic compactness extractor NameCert obligations]
\label{thm:dyadic-compactness-extractor-namecert-obligations}
For every accepted $\DyadicCompactnessExtractorUp$ carrier $D=(K,M,T,R,F,L,U,H,C,P,N)$, the local $\NameCert_{\DyadicCompactnessExtractorUp}$ surface consists exactly of carrier admission, compact finite-net exposure, dyadic radius exactness, metric triangle compatibility, continuous-map pointwise-modulus exposure, finite Lebesgue-ledger readback, uniform-modulus handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every compact-uniform consumer must read the displayed finite route
$$
K,M,T,R,F,L \longmapsto U \longmapsto H,C,P,N
$$
before exporting a uniform modulus.
\end{theorem}
\begin{proof}
Project the carrier row by row. The compact source $K$ supplies the finite net, while $T$ and $R$ record the dyadic radius and the finite center selected for a source request.

The metric triangle row $M$ and continuous-map row $F$ provide the only estimate data used by the extractor. The Lebesgue ledger $L$ is then the finite lower-radius comparison that makes the resulting radius uniform over the displayed net.

The handoff row $U$ is read only after those finite rows have been exposed. The structural rows $H,C,P,N$ preserve the same route, so no open cover, selected subcover, sequential compactness argument, host Lebesgue number, host minimum selector, choice principle, or ambient completeness theorem can enter the certificate.
\end{proof}

\begin{theorem}[Dyadic compactness extractor vision handoff]
\label{thm:dyadic-compactness-extractor-vision-handoff}
The finite extractor packet $D=(K,M,T,R,F,L,U,H,C,P,N)$ realizes the compact-uniform-continuity handoff of \autoref{ch:visions-compact-uniform-continuity}: any accepted consumer factors through a displayed finite net, one dyadic radius row, the metric triangle row, the continuous-map modulus row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before it exports a $\UniformModulusUp$ row. The exported row is therefore a BEDC finite-radius ledger, not a host modulus selector.
\end{theorem}
\begin{proof}
Start with a compact-uniform consumer. The carrier forces the consumer to read $K,T,R$ as the compact finite-net and dyadic radius data before any uniform-radius output is visible.

Next the route reads the metric and continuity evidence through $M$ and $F$. The finite Lebesgue ledger $L$ is the displayed comparison tying those pointwise rows to one compact-net radius.

The uniform-modulus row $U$ is downstream of this finite chain. Since $H,C,P,N$ only transport, replay, source, and name the same packet, the handoff is exactly the BEDC route described in the vision chapter and not an external modulus selector.
\end{proof}

\falsifiablePrediction{Every accepted $\DyadicCompactnessExtractorUp$ compact-uniform consumer factors through a displayed finite net, dyadic radius row, metric triangle row, continuous-map modulus row, hsame transport, Cont replay, Pkg provenance, and local NameCert before exporting a uniform modulus.}

\independenceWitness{compactmetric, metric, continuousmap, finitelebesguenumber, uniformmodulus: $\DyadicCompactnessExtractorUp$ is not bijective to any listed sibling because it jointly binds the compact finite-net source, metric triangle estimates, dyadic radius ledger, pointwise continuous-map modulus rows, finite Lebesgue-radius readback, and UniformModulus handoff; each sibling omits at least one of these extractor coordinates.}

The Lean TasteGate follow-up is \texttt{BEDC.Derived.DyadicCompactnessExtractorUp}, with $\Nontrivial\ \DyadicCompactnessExtractorUp$ and $\FieldFaithful\ \DyadicCompactnessExtractorUp$ instances preserving $K,M,T,R,F,L,U,H,C,P,N$ as separate real extractor rows.

\closureat{DyadicCompactnessExtractorUp}{seedStr}

\begin{closurestatus}{\DyadicCompactnessExtractorUp}
  \constructivestory{A $\DyadicCompactnessExtractorUp$ packet is generated from a CompactMetric finite-net row, a Metric triangle row, a dyadic radius row, finite-net readback, ContinuousMap pointwise modulus data, a FiniteLebesgueNumber ledger, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-compactness-extractor-carrier}, \autoref{thm:dyadic-compactness-extractor-namecert-obligations}, and \autoref{thm:dyadic-compactness-extractor-vision-handoff} seed the finite dyadic compactness extractor route behind compact-uniform-continuity.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, selected subcovers, sequential compactness, host Lebesgue numbers, host minimum selectors, choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net exposure, dyadic-radius exactness, metric-triangle compatibility, continuous-map modulus exposure, finite Lebesgue-ledger readback, uniform-modulus handoff, structural transport, and ledger non-escape for BEDC.Derived.DyadicCompactnessExtractorUp.}
\end{closurestatus}
