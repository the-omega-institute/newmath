\chapter{A Concrete Naming Certificate for $\PositiveRealSquareRootUp$}
\label{ch:concrete-instances-positive-real-square-root-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\PositiveRealSquareRootUp$ packet records the finite square-root service available for a displayed positive real source before any total real square-root operator is claimed. It sits after the regular Cauchy square-root service of \autoref{ch:concrete-instances-regular-cauchy-square-root-namecert}, the Heron recurrence route of \autoref{ch:concrete-instances-heron-square-root-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a bounded constructive service for one positive input; it is not a host square-root function, a trichotomy principle, quotient real equality, selected limit construction, or ambient $\RealUp$ completeness.

\begin{definition}[Positive real square-root carrier]
\label{def:positive-real-square-root-carrier}
A $\PositiveRealSquareRootUp$ carrier is a finite $\BHist$ packet
$$
Q=(X,A,G,R,H,D,S,V,E,T,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ is the positive $\RealUp$ source row whose displayed witness keeps the input in the positive real interface;
\item $A$ is the apartness and nonnegative guard row, recording the local refusal of the zero or negative branch without invoking real trichotomy;
\item $G$ is the $\RegularCauchySquareRootUp$ readback row for the regular Cauchy square-root service;
\item $R$ is the $\HeronSquareRootUp$ recurrence route that supplies the finite iterative square-root approximants;
\item $H$ is the $\DyadicRatCoreUp$ tolerance ledger for the displayed Heron and regular Cauchy estimates;
\item $D$ is the $\StreamNameUp$ window row exposing only finitely many source and approximation observations;
\item $S$ is the $\RegSeqRatUp$ readback row for the regular rational square-root name;
\item $V$ is the terminal $\RealUp$ seal reached after the source, guard, recurrence, tolerance, window, and readback rows are displayed;
\item $E$ stores componentwise $\hsame$ transport for the source, guard, recurrence, tolerance, window, readback, and seal rows;
\item $T$ records the displayed $\Cont$ replay by which a consumer reads the same finite square-root service;
\item $P$ records $\Pkg$ provenance over $\PositiveRealSquareRootUp$, $\PositiveRealUp$, $\RealUp$, $\RegularCauchySquareRootUp$, $\HeronSquareRootUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\PositiveRealSquareRootUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a total host square-root function, a trichotomy case split on arbitrary reals, quotient real equality, a selected limit, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Positive real square-root NameCert obligations]
\label{thm:positive-real-square-root-namecert-obligations}
For the carrier of \autoref{def:positive-real-square-root-carrier}, the local $\NameCert_{\PositiveRealSquareRootUp}$ surface consists exactly of five finite obligations: carrier admission for $Q=(X,A,G,R,H,D,S,V,E,T,C,P,N)$, positivity and apartness guarding through $X,A$, regular Cauchy square-root readback through $G,S,V$, Heron recurrence and dyadic tolerance accounting through $R,H,D$, and componentwise transport, replay, provenance, and local naming through $E,T,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier rows. The source and guard rows $X,A$ are displayed before any square-root service is read, so the certificate speaks only about a positive input with an explicit apartness and nonnegative guard.

Next compose the sibling routes. The regular Cauchy square-root service is read through $G$, while the Heron recurrence row $R$ supplies the constructive approximants and the dyadic tolerance row $H$ records the finite numerical budget exposed through the stream windows $D$.

Finally pass through the readback, seal, transport, replay, provenance, and naming rows $S,V,E,T,C,P,N$. These rows preserve only the displayed finite service, and none of them adds a host square-root operator, trichotomy, quotient real equality, selected limit, or ambient completeness principle.
\end{proof}

\begin{theorem}[Positive real square-root regular Cauchy service]
\label{thm:positive-real-square-root-regular-cauchy-service}
Every accepted $\PositiveRealSquareRootUp$ consumer that asks for a square-root value must first read the positive $\RealUp$ source row, the apartness and nonnegative guard, the $\RegularCauchySquareRootUp$ readback row, the $\HeronSquareRootUp$ recurrence route, the $\DyadicRatCoreUp$ tolerance ledger, the $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback, the terminal $\RealUp$ seal, transport, replay, provenance, and local naming. The service therefore exports only the bounded regular Cauchy square-root route for the displayed positive input.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:positive-real-square-root-namecert-obligations}. They require $X,A,G,R,H,D,S,V,E,T,C,P,N$ before a consumer can read the local square-root name.

Use \autoref{ch:concrete-instances-regular-cauchy-square-root-namecert} for the regular Cauchy readback and \autoref{ch:concrete-instances-heron-square-root-namecert} for the Heron recurrence route. The dyadic ledger and stream windows keep the requested tolerance budget finite before the readback row $S$ reaches the real seal $V$.

The structural rows $E,T,C,P,N$ then transport, replay, source, and name the same bounded route. Since the carrier has no coordinate for a host square-root function, real trichotomy, quotient real equality, selected limits, or ambient $\RealUp$ completeness, no consumer can read those objects from the service.
\end{proof}

\falsifiablePrediction{Any accepted PositiveRealSquareRootUp service that bypasses the positive source guard, Heron recurrence route, dyadic tolerance ledger, stream windows, regular rational readback, or terminal Real seal refutes the carrier.}

\independenceWitness{regular-cauchy-square-root, heron-square-root, dyadicratcore, streamname, regseqrat, real: $\PositiveRealSquareRootUp$ is not bijective to any listed sibling because it jointly carries a positive source guard, recurrence route, tolerance ledger, finite windows, readback, terminal seal, transport, replay, provenance, and local NameCert row.}

\closureat{PositiveRealSquareRootUp}{seedStr}

\begin{closurestatus}{\PositiveRealSquareRootUp}
  \constructivestory{A $\PositiveRealSquareRootUp$ packet is generated from a positive RealUp source row, an apartness and nonnegative guard, regular Cauchy square-root readback, Heron recurrence, dyadic tolerance accounting, stream windows, regular rational readback, terminal Real sealing, componentwise transport, displayed replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:positive-real-square-root-carrier}, \autoref{thm:positive-real-square-root-namecert-obligations}, and \autoref{thm:positive-real-square-root-regular-cauchy-service} seed the bounded positive-input square-root service over the displayed finite rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close a host square-root function, real trichotomy, quotient real equality, selected limits, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-source guarding, Heron recurrence accounting, dyadic tolerance accounting, regular Cauchy readback, stream-window replay, Real sealing, transport, provenance, and local naming for BEDC.Derived.PositiveRealSquareRootUp.}
\end{closurestatus}
