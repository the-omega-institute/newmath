\chapter{A Concrete Naming Certificate for $\FejerTheoremUp$}
\label{ch:concrete-instances-fejer-theorem-namecert}
\origin{human}

Following Fejer, Zygmund, and Katznelson on positive summability of Fourier series, the $\FejerTheoremUp$ packet records the theorem-level handoff from finite Fourier coefficient windows and positive Fejer kernels to a uniform approximation read. It sits over the Fourier coefficient surface of \autoref{ch:concrete-instances-fourier-namecert}, the positive kernel certificate of \autoref{ch:concrete-instances-fejer-kernel-namecert}, the circle phase source of \autoref{ch:concrete-instances-s1-namecert}, the normalization route of \autoref{ch:concrete-instances-integral-namecert}, and the approximation-facing boundary of \autoref{ch:concrete-instances-uniformlimit-namecert}. The chapter names a finite summability theorem certificate; it does not import Haar measure, a host $L^{1}$ space, quotient equality of functions, selected Fourier representatives, pointwise convergence, or ambient compactness.

\begin{definition}[Fejer theorem carrier]
\label{def:fejer-theorem-carrier}
A $\FejerTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(F,K,S,I,U,A,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\FourierUp$ coefficient-window row, listing the finite symmetric coefficient requests and the polynomial window consumed by the summability read.
\item $K$ is the $\FejerKernelUp$ positivity and triangular-weight row, including the kernel normalization and finite averaging comparison exposed by the sibling kernel certificate.
\item $S$ is the $\SOneUp$ phase-sample row, recording the circle points on which the finite averages are inspected.
\item $I$ is the $\IntegralUp$ normalization row, exposing only the finite integral cells needed to read the averaged trigonometric polynomial.
\item $U$ is the $\UniformLimitUp$ handoff row, recording the tolerance, tail, and uniform-read boundary produced by the positive kernel averages.
\item $A$ is the theorem assertion row, pairing the displayed Fourier window with the positive-kernel average before any approximation consumer reads the conclusion.
\item $L$ is the non-escape ledger for Haar measure, host $L^{1}$ theory, quotient function equality, selected representatives, pointwise convergence, and ambient compactness.
\item $H$ records componentwise $\hsame$ transport for $F,K,S,I,U,A,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a Fejer-summability request through $F,K,S,I,U,A,L,H$.
\item $P$ is the $\Pkg$ provenance over $\FejerTheoremUp$, $\FourierUp$, $\FejerKernelUp$, $\SOneUp$, $\IntegralUp$, $\UniformLimitUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FejerTheoremUp}$ row.
\end{enumerate}
The classifier accepts exactly packets whose visible rows are $F,K,S,I,U,A,L,H,C,P,N$. It has no coordinate for an unbounded Fourier series, host measure theory, quotient equality of functions, selected representatives, or a convergence theorem outside the displayed positive-kernel handoff.
\end{definition}

\begin{theorem}[Fejer theorem NameCert obligations]
\label{thm:fejer-theorem-namecert-obligations}
For every accepted $\FejerTheoremUp$ carrier
$$
T=(F,K,S,I,U,A,L,H,C,P,N),
$$
the local $\NameCert_{\FejerTheoremUp}$ surface consists exactly of Fourier coefficient-window admission through $F$, positive Fejer-kernel admission through $K$, circle phase-sample exposure through $S$, integral normalization through $I$, uniform-limit handoff through $U$, theorem assertion exposure through $A$, non-escape ledger coverage through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fejer-theorem-carrier}. The rows $F,K,S,I$ display the coefficient window, positive kernel, phase samples, and normalization data before the theorem assertion is available.

The theorem-facing rows are $U,A,L$. The uniform-limit row records the approximation boundary, the assertion row binds the Fourier window to the positive-kernel average, and the ledger row states the excluded host readings.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for Haar measure, host $L^{1}$ theory, quotient function equality, selected representatives, pointwise convergence, or ambient compactness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Positive-kernel uniform handoff]
\label{thm:fejer-theorem-positive-kernel-uniform-handoff}
Every approximation consumer of an accepted $\FejerTheoremUp$ carrier factors through the displayed route
$$
\FourierUp \longmapsto \FejerKernelUp \longmapsto \SOneUp \longmapsto \IntegralUp \longmapsto \UniformLimitUp .
$$
The positive-kernel row supplies only the finite triangular averaging and normalization data named by $\FejerKernelUp$, and the theorem row exports only the uniform handoff $U$ together with $A,L,H,C,P,N$. Thus the packet supplies a BEDC-native Fejer summability certificate without exporting Haar measure, host $L^{1}$ spaces, quotient function equality, selected representatives, pointwise convergence, or ambient compactness.
\end{theorem}
\begin{proof}
Begin with an approximation request. The carrier first exposes $F$ and $K$, so the request sees a finite Fourier coefficient window and a positive Fejer kernel row rather than an infinite Fourier series or host convolution object.

The request next reads $S$ and $I$. These rows bind the averaging read to displayed circle phases and finite integral normalization cells.

Only then can the request inspect $U$ and $A$. The uniform-limit handoff records the approximation boundary, while the theorem assertion row states that the positive-kernel average is the route by which the Fourier window is consumed.

Finally $L,H,C,P,N$ block excluded host readings and replay the same finite packet. No listed consumer can bypass the positive-kernel route or turn it into Haar measure, quotient function equality, selected representatives, pointwise convergence, or ambient compactness.
\end{proof}

\falsifiablePrediction{An accepted $\FejerTheoremUp$ packet is refuted if a Fourier approximation consumer reaches the $\UniformLimitUp$ handoff while bypassing the displayed $\FejerKernelUp$ positivity row, $\SOneUp$ phase samples, or $\IntegralUp$ normalization row.}

\independenceWitness{fourier, fejer-kernel, sone, integral, uniformlimit: none is bijective to $\FejerTheoremUp$ because the packet jointly carries coefficient windows, positive triangular kernel data, circle phase samples, integral normalization, a uniform theorem handoff, a non-escape ledger, transport, replay, provenance, and local NameCert rows.}

\closureat{FejerTheoremUp}{seedStr}

\begin{closurestatus}{\FejerTheoremUp}
  \constructivestory{A $\FejerTheoremUp$ packet is generated from FourierUp coefficient windows, FejerKernelUp positivity and triangular averaging rows, SOneUp phase samples, IntegralUp normalization rows, a UniformLimitUp theorem handoff, a non-escape ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fejer-theorem-carrier}, \autoref{thm:fejer-theorem-namecert-obligations}, and \autoref{thm:fejer-theorem-positive-kernel-uniform-handoff} seed the finite theorem-level Fejer summability route over Fourier, positive-kernel, circle, integral, and uniform-limit rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, Haar measure, host $L^{1}$ theory, quotient function equality, selected Fourier representatives, pointwise convergence, or ambient compactness.}
  \upgradepath{Obligation closure requires checked coefficient-window admission, positive-kernel admission, phase-sample exposure, integral normalization, uniform-limit handoff, theorem assertion exposure, ledger coverage, transport, replay, provenance, and local naming for BEDC.Derived.FejerTheoremUp.}
\end{closurestatus}
