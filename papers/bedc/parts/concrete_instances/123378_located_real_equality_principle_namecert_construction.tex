\chapter{A Concrete Naming Certificate for $\LocatedRealEqualityPrincipleUp$}
\label{ch:concrete-instances-located-real-equality-principle-namecert}
\origin{human}

Following Bishop and Bridges on constructive real equality through located apartness, the $\LocatedRealEqualityPrincipleUp$ packet records the finite interface by which two displayed real names may be treated as equal only through the refusal of a witnessed apartness gap at every inspected dyadic tolerance. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the located-real carrier of \autoref{ch:concrete-instances-located-real-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the separated comparison discipline of \autoref{ch:concrete-instances-apartnessspace-namecert}. The chapter names equality as a displayed certificate service over rows already visible to L10 Real and Completion consumers; it does not assert host real equality, trichotomy, quotient classes, selected Cauchy representatives, or ambient $\RealUp$ completeness.

\begin{definition}[Located real equality principle packet]
\label{def:located-real-equality-principle-packet}
A $\LocatedRealEqualityPrincipleUp$ carrier is a finite $\BHist$ packet
$$
E=(X,Y,L,R,W,D,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two terminal $\RealUp$ name rows whose comparison is requested.
\item $L$ is the $\LocatedRealUp$ row exposing lower and upper located rational brackets for both names at the inspected tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row that supplies regular rational observations for the displayed brackets.
\item $W$ is the $\StreamNameUp$ finite-window row listing exactly the stream observations inspected by the equality service.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger controlling radius comparison, bracket refinement, and gap accounting.
\item $A$ is the $\ApartnessSpaceUp$ row whose positive-gap classifier is the only public route for separating the two names.
\item $H$ records componentwise $\hsame$ transport for the real-name rows, located row, readback row, window row, dyadic ledger, apartness row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from requested comparison to located brackets, regular readback, finite windows, dyadic gap accounting, apartness refusal, and the final equality-principle response.
\item $P$ is the $\Pkg$ provenance over $\LocatedRealEqualityPrincipleUp$, $\RealUp$, $\LocatedRealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\ApartnessSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedRealEqualityPrincipleUp}$ row.
\end{enumerate}
The carrier exposes equality only as the exhaustion of displayed apartness attempts through $X,Y,L,R,W,D,A,H,C,P,N$. It has no coordinate for host equality of real names, trichotomy, quotient classes of Cauchy streams, selected representatives, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located real equality principle NameCert obligations]
\label{thm:located-real-equality-principle-namecert-obligations}
For every accepted $\LocatedRealEqualityPrincipleUp$ carrier
$$
E=(X,Y,L,R,W,D,A,H,C,P,N),
$$
the local $\NameCert_{\LocatedRealEqualityPrincipleUp}$ surface consists exactly of the following five obligations:
\begin{enumerate}
\item carrier display for the rows $X,Y,L,R,W,D,A,H,C,P,N$;
\item classifier stability under $\hsame$ transport of the displayed real-name, located, readback, window, dyadic, and apartness rows;
\item soundness of mutual-apartness refusal, meaning that equality is reported only when every inspected $\ApartnessSpaceUp$ positive-gap request is refused by the displayed located brackets and dyadic tolerance ledger;
\item regular-Cauchy transport through $\RegSeqRatUp$, $\StreamNameUp$, and $\DyadicRatCoreUp$ before either terminal $\RealUp$ name may be compared by the service;
\item ledger exclusion of host equality, trichotomy, quotient classes, selected Cauchy representatives, countable choice, and ambient $\RealUp$ completeness.
\end{enumerate}
Thus an L10 Real or Completion consumer may cite the packet only as a finite located-apartness equality interface over the sibling certificates \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-located-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-apartnessspace-namecert}.
\end{theorem}
\leanstmt{BEDC.Derived.LocatedRealEqualityPrincipleUp}
\begin{proof}
Project the carrier of \autoref{def:located-real-equality-principle-packet}. The displayed rows are exactly $X,Y,L,R,W,D,A,H,C,P,N$, so carrier display is obtained by tuple projection.

Classifier stability is componentwise. The transport row $H$ preserves the two terminal real-name rows, the located bracket row, the regular readback row, the finite-window row, the dyadic ledger, and the apartness classifier row under $\hsame$.

The mutual-apartness-refusal clause is forced by the order of rows. A consumer reaches the apartness row $A$ only after the located brackets $L$, regular readback $R$, finite windows $W$, and dyadic tolerance ledger $D$ have been displayed. Equality is therefore a refusal of positive-gap evidence through those rows, not a separate host equality judgment.

Regular-Cauchy transport is the same displayed route read through the sibling chapters for $\RegSeqRatUp$, $\StreamNameUp$, and $\DyadicRatCoreUp$. The terminal $\RealUp$ rows $X,Y$ are compared by the packet only after this route has fixed the inspected finite data.

Finally inspect the ledger. Since the carrier has no coordinate for host real equality, trichotomy, quotient classes, selected representatives, countable choice, or ambient Real completeness, none of those principles can discharge a local NameCert obligation for the packet.
\end{proof}

\falsifiablePrediction{An accepted $\LocatedRealEqualityPrincipleUp$ consumer that reports equality of two $\RealUp$ names without displaying located brackets, regular rational readback, finite stream windows, dyadic tolerance accounting, apartness positive-gap refusal, transport, replay, provenance, and local naming refutes the packet.}

\independenceWitness{real, locatedreal, regseqrat, streamname, dyadicratcore, and apartnessspace: none is bijective to the located real equality principle packet because the packet jointly carries two terminal Real rows, located bracket data, regular readback, finite observation windows, dyadic tolerance accounting, apartness refusal, transport, replay, provenance, and a local NameCert row.}

\closureat{LocatedRealEqualityPrincipleUp}{seedStr}

\begin{closurestatus}{\LocatedRealEqualityPrincipleUp}
  \constructivestory{A $\LocatedRealEqualityPrincipleUp$ packet is generated from two terminal RealUp name rows, LocatedReal bracket data, RegSeqRat readback, StreamName finite windows, DyadicRatCore tolerance accounting, ApartnessSpace positive-gap refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-real-equality-principle-packet} and \autoref{thm:located-real-equality-principle-namecert-obligations} seed the finite located-apartness equality interface over RealUp, LocatedRealUp, RegSeqRatUp, StreamNameUp, DyadicRatCoreUp, ApartnessSpaceUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real equality, trichotomy, quotient classes, selected Cauchy representatives, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\LocatedRealEqualityPrincipleUp$.}
  \upgradepath{Obligation closure requires checked carrier display, classifier stability, mutual-apartness-refusal soundness, regular-Cauchy transport, ledger exclusion, and TasteGate field faithfulness for BEDC.Derived.LocatedRealEqualityPrincipleUp.}
\end{closurestatus}
