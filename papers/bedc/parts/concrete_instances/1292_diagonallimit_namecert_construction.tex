\chapter{A Concrete Naming Certificate for $\DiagonalLimitUp$}
\label{ch:concrete-instances-diagonallimit-namecert}
\origin{human}

The $\DiagonalLimitUp$ packet names the concrete regular-Cauchy-family diagonal construction that sits above the finite diagonal selectors and below the $\RealUp$ consumer seal. Following the Bishop--Bridges constructive-analysis treatment of Cauchy completion, it keeps the inspected regular rational family, explicit modulus rows, stream windows, dyadic readback ledger, diagonal completion packet, and real seal as visible certificate data. It does not quotient regular streams, select a host limit, or import ambient real completeness.

\begin{definition}[DiagonalLimit regular-Cauchy-family carrier]
\label{def:diagonallimit-regular-cauchy-family-carrier}
A $\DiagonalLimitUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
L=(F,M,S,D,C,E,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $F$ is the $\RegSeqRatUp$-indexed regular Cauchy family whose inspected members are displayed as regular rational rows;
\item $M$ is the $\CauchyModulusUp$ row selecting a late enough family member for each requested consumer precision;
\item $S$ is the finite $\StreamNameUp$ schedule of windows read by the diagonal construction;
\item $D$ is the $\DyadicRatCoreUp$ observation ledger attached to the selected rational readbacks;
\item $C$ is the $\RealDiagonalCompletionUp$ packet that repacks the selected diagonal rows as a completion-facing finite surface;
\item $E$ is the $\RealUp$ seal row exposed to downstream consumers;
\item $P$ is the $\Pkg$ provenance naming the $\RegSeqRatUp$, $\CauchyModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealDiagonalCompletionUp$, and $\RealUp$ dependencies;
\item $N$ is the local $\NameCert_{\DiagonalLimitUp}$ provenance row.
\end{enumerate}
Two accepted packets are classified together exactly when the displayed family rows, modulus rows, stream windows, dyadic ledgers, diagonal-completion packets, real seals, package rows, and local certificate rows are transported componentwise by the recorded $\hsame$ comparisons and $\Cont$ routes.
\end{definition}

\begin{theorem}[DiagonalLimit NameCert obligation surface]
\label{thm:diagonallimit-namecert-obligation-surface}
The local $\NameCert_{\DiagonalLimitUp}$ obligation surface consists of carrier habitation by the finite packet of \autoref{def:diagonallimit-regular-cauchy-family-carrier}, componentwise classifier transport through displayed $\hsame$ and $\Cont$ rows, diagonal regularity bounds over the explicit $\CauchyModulusUp$ row, handoff to $\RealDiagonalCompletionUp$ and $\RealUp$, and ledger non-escape over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\mathsf{BHist}$, $\Pkg$, and $\NameCert$.
\end{theorem}
\begin{proof}
Carrier habitation is the packet assembly in \autoref{def:diagonallimit-regular-cauchy-family-carrier}. The visible rows name the regular Cauchy family, the modulus, the finite windows, the dyadic observations, the diagonal completion packet, the real seal, and the local provenance.

Classifier transport is componentwise. A permitted replacement is either an $\hsame$ comparison of one displayed row or a $\Cont$ route identifying where that row is read inside the finite packet, so transport preserves the same carrier boundary.

The remaining obligations are the diagonal regularity bound, the completion handoff, and non-escape boundary recorded in the following theorems. Together they list exactly the rows consumed by the local naming certificate.
\end{proof}

\begin{theorem}[DiagonalLimit diagonal regularity bound]
\label{thm:diagonallimit-diagonal-regularity-bound}
For every accepted $\DiagonalLimitUp$ carrier and every displayed unary precision request $n$, the modulus row $M$ selects a finite family member and a finite $\StreamNameUp$ window such that the selected $\RegSeqRatUp$ readback is bounded by the dyadic observation ledger $D$ at the requested precision. The bound is transported only through the packet's displayed $\hsame$ comparisons and $\Cont$ routes.
\end{theorem}
\begin{proof}
Project the accepted packet through \autoref{def:diagonallimit-regular-cauchy-family-carrier}. The precision request is answered by the explicit $\CauchyModulusUp$ coordinate, so the selected family member is a displayed row rather than a hidden limit choice.

The selected member is read through the finite $\StreamNameUp$ schedule. Its rational observation is recorded in the $\RegSeqRatUp$ family row and the accompanying $\DyadicRatCoreUp$ ledger.

Since the classifier transports only displayed rows through $\hsame$ and $\Cont$, the same bound follows after any accepted transport. No quotient stream or host real endpoint is used to obtain the bound.
\end{proof}

\begin{theorem}[DiagonalLimit Real-Cauchy handoff boundary]
\label{thm:diagonallimit-real-cauchy-handoff-boundary}
Every $\RealUp$ consumer read exported by an accepted $\DiagonalLimitUp$ packet factors through the same terminal precision, selected $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealDiagonalCompletionUp$ packet, $\RealUp$ seal, visible $\hsame$ rows, visible $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitUp}$ row.
\end{theorem}
\begin{proof}
Begin with the selected diagonal window supplied by \autoref{thm:diagonallimit-diagonal-regularity-bound}. It exposes the precision, modulus-selected member, stream window, regular rational readback, and dyadic observation ledger.

The $\RealDiagonalCompletionUp$ coordinate $C$ repacks exactly those displayed rows as the completion-facing finite surface. The $\RealUp$ seal $E$ is attached to that same completion packet and not to an independent host real object.

Repacking the visible rows with the package and local certificate provenance gives the stated consumer handoff. The route has no coordinate for quotient equality, selected limits, uniqueness of limits, or ambient completeness.
\end{proof}

\begin{theorem}[DiagonalLimit non-escape boundary]
\label{thm:diagonallimit-non-escape-boundary}
An accepted $\DiagonalLimitUp$ packet exports only the displayed $\RegSeqRatUp$, $\CauchyModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealDiagonalCompletionUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. It exports no quotient of regular streams, no host real equality, no selected limit, no uniqueness theorem for limits, and no ambient real-completeness principle.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates of \autoref{def:diagonallimit-regular-cauchy-family-carrier}. Each coordinate is a displayed BEDC row or a local provenance row named in the packet.

The regularity theorem accounts for every precision-sensitive rational read, and the handoff theorem accounts for every $\RealUp$ consumer read. Thus every public route through the packet has already passed through visible finite data.

None of the carrier coordinates is a quotient class, a host-level equality witness, a selected limit object, a uniqueness theorem, or an ambient completeness field. Those objects cannot be recovered from the packet boundary.
\end{proof}

\closureat{DiagonalLimitUp}{seedStr}

\begin{closurestatus}{\DiagonalLimitUp}
  \constructivestory{A $\DiagonalLimitUp$ packet is generated from a $\RegSeqRatUp$-indexed regular Cauchy family, an explicit $\CauchyModulusUp$ row, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ observation ledgers, a $\RealDiagonalCompletionUp$ packet, a $\RealUp$ seal row, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope registers the carrier, displayed classifier route, diagonal regularity bound, completion-to-$\RealUp$ handoff boundary, and ledger non-escape surface over $\RegSeqRatUp$, $\CauchyModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealDiagonalCompletionUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close $\RealUp$ completeness, quotient equality of regular streams, uniqueness of limits, host real construction, or bridge checking.}
  \upgradepath{Obligation closure requires formal carrier transport, diagonal regularity bounds, completion handoff exactness, $\RealUp$ seal non-escape, and dependency stability for the displayed packet rows.}
\end{closurestatus}
