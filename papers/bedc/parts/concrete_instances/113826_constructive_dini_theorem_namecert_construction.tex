\chapter{A Concrete Naming Certificate for $\ConstructiveDiniTheoremUp$}
\label{ch:concrete-instances-constructive-dini-theorem-namecert}
\origin{human}

Following Dini and Bishop--Bridges constructive analysis, the $\ConstructiveDiniTheoremUp$ packet records the finite NameCert form of the constructive Dini theorem: a monotone continuous family on a compact metric source reaches a uniform limit through displayed finite modulus, window, rational readback, and Real-seal rows. It consumes the compact-uniform sibling \autoref{ch:concrete-instances-dini-monotone-compact-uniform-namecert}, the modulus sibling \autoref{ch:concrete-instances-constructive-dini-modulus-namecert}, the uniform-limit sibling \autoref{ch:concrete-instances-dini-uniform-limit-modulus-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-limit handoff of \autoref{ch:concrete-instances-uniformlimit-namecert}, the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite theorem carrier; it does not assert open-cover compactness, ambient $\RealUp$ completeness, quotient equality of functions, countable choice, or hidden limiting values.

\begin{definition}[Constructive Dini theorem carrier]
\label{def:constructive-dini-theorem-carrier}
A $\ConstructiveDiniTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(K,F,M,D,U,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing only the finite compact-metric source and probe rows used by the theorem route.
\item $F$ is the $\ContinuousMapUp$ family row, naming the displayed continuous maps and the target $\RealUp$ value rows inspected by the packet.
\item $M$ is the monotone ledger, recording the pointwise order route and the visible approach of $F$ toward its limiting row.
\item $D$ is the $\ConstructiveDiniModulusUp$ row, carrying the finite Dini modulus extracted from compact finite-net data, dyadic tolerances, pointwise windows, and finite maximum rows.
\item $U$ is the combined $\DiniMonotoneCompactUniformUp$ and $\DiniUniformLimitModulusUp$ handoff row, exposing the compact-uniform route and the resulting $\UniformLimitUp$ row.
\item $W$ is the $\StreamNameUp$ finite-window row, scheduling function values, limit values, finite net centers, and consumer precision requests.
\item $R$ is the $\RegSeqRatUp$ readback row reached from $W$ before the terminal real-name seal is exposed.
\item $E$ is the $\RealUp$ seal row for the uniform-limit read. It names the displayed real values without adding ambient real completeness.
\item $H$ records componentwise $\hsame$ transport for the compact source, continuous family, monotone ledger, Dini modulus, uniform-limit handoff, stream windows, regular rational readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a theorem request through $K,F,M,D,U,W,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveDiniTheoremUp$, $\DiniMonotoneCompactUniformUp$, $\ConstructiveDiniModulusUp$, $\DiniUniformLimitModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ConstructiveDiniTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,M,D,U,W,R,E,H,C,P,N$. It has no coordinate for general open-cover compactness, arbitrary subcover selection, quotient function equality, countable choice, hidden limit selectors, host suprema, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.ConstructiveDiniTheoremUp.TasteGate.ConstructiveDiniTheoremTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Constructive Dini theorem NameCert obligations]
\label{thm:constructive-dini-theorem-namecert-obligations}
For every accepted $\ConstructiveDiniTheoremUp$ carrier
$$
T=(K,F,M,D,U,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\ConstructiveDiniTheoremUp}$ surface consists exactly of carrier admission for the compact metric source, continuous-map family, monotone ledger, Dini modulus row, uniform-limit row, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise transport, displayed replay, provenance, and local naming rows.

The obligation surface requires the compact source and continuous family through $K,F$, monotone visibility through $M$, finite Dini modulus extraction through $D$, compact-uniform and uniform-limit handoff through $U$, L10 finite real-readback through $W,R,E$, transport and replay stability through $H,C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:constructive-dini-theorem-carrier}. The analytic rows are $K,F,M,D,U,W,R,E$, while $H,C,P,N$ are the structural certificate rows.

The compact metric source and continuous family are admitted first. The monotone ledger then fixes which pointwise comparisons may be inspected, so the Dini modulus row $D$ is downstream of displayed compact finite-net data and not of an external compactness principle.

The handoff row $U$ consumes $D$ together with the compact-uniform sibling route and exposes the $\UniformLimitUp$ read. The L10 boundary is then read only through the $\StreamNameUp$ windows $W$, the $\RegSeqRatUp$ readback $R$, and the terminal $\RealUp$ seal $E$.

Finally $H,C,P,N$ preserve, replay, source, and name the same route. Since no excluded object is a carrier coordinate, none is a NameCert obligation of the theorem packet.
\end{proof}

\begin{theorem}[Constructive Dini monotone uniform route]
\label{thm:constructive-dini-theorem-monotone-uniform-route}
Every uniform-limit consumer of an accepted $\ConstructiveDiniTheoremUp$ carrier must factor through the displayed route
$$
K,F,M \longmapsto D \longmapsto U \longmapsto W,R,E \longmapsto H,C,P,N.
$$
Thus the theorem export is the finite BEDC route from compact metric source and monotone continuous family data through $\DiniMonotoneCompactUniformUp$, $\ConstructiveDiniModulusUp$, $\DiniUniformLimitModulusUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. It exports no open-cover compactness, quotient function equality, countable choice, hidden limiting values, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:constructive-dini-theorem-namecert-obligations}. They place the compact source $K$, continuous family $F$, and monotone ledger $M$ before the theorem can expose any uniform conclusion.

The row $D$ is the finite Dini modulus row. It reads the compact finite-net and pointwise monotone data already displayed by the source and family rows, and it supplies the modulus object consumed by the compact-uniform handoff.

The row $U$ composes the compact-uniform sibling and the uniform-limit-modulus sibling. Hence a uniform-limit consumer receives a single handoff produced by finite modulus extraction, not a selected open cover or a host-level supremum.

The terminal real-readback rows $W,R,E$ keep the exported conclusion inside finite stream windows, regular rational readback, and a displayed Real seal. The structural rows $H,C,P,N$ then transport, replay, source, and name the same route for downstream consumers.
\end{proof}

\begin{theorem}[Constructive Dini L10 seal nonescape]
\label{thm:constructive-dini-theorem-l10-seal-nonescape}
For an accepted $\ConstructiveDiniTheoremUp$ carrier, the terminal $\RealUp$ values used by the uniform-limit conclusion are reachable only after the compact source row $K$, continuous-map family row $F$, monotone ledger $M$, Dini modulus row $D$, uniform-limit handoff row $U$, $\StreamNameUp$ window row $W$, and $\RegSeqRatUp$ readback row $R$ have been displayed. The $\RealUp$ seal row $E$ therefore exports the finite L10 readback route and no ambient completeness, quotient stream equality, hidden host limit, countable choice, or real trichotomy coordinate.
\end{theorem}
\begin{proof}
Use \autoref{thm:constructive-dini-theorem-monotone-uniform-route}. It places every terminal read downstream of the compact metric source, continuous family, monotone ledger, finite Dini modulus, and uniform-limit handoff rows.

The L10 readback portion is visible in $W,R,E$. The stream row $W$ schedules the finite observations, the regular rational row $R$ reads those observations at the requested precision, and $E$ seals the displayed real values only after that readback has occurred.

Transport and replay do not create another analytic route. The rows $H,C,P,N$ preserve the same carrier coordinates, so the terminal seal cannot export any object absent from $T$.

The excluded principles have no row in the carrier. Thus the theorem packet gives a finite Real-seal boundary for the constructive Dini route without importing ambient completion, quotient equality, hidden host limits, countable choice, or trichotomy.
\end{proof}

\begin{theorem}[Constructive Dini finite-net monotone-window route]
\label{thm:constructive-dini-theorem-finite-net-monotone-window}
Every uniform-limit consumer of an accepted $\ConstructiveDiniTheoremUp$ carrier
$$
T=(K,F,M,D,U,W,R,E,H,C,P,N)
$$
must expose the compact finite-net source and monotone window before the Dini modulus and $\UniformLimitUp$ row are public. Equivalently, the consumer read factors through
$$
K,F,M \longmapsto D \longmapsto U \longmapsto W,R,E \longmapsto H,C,P,N,
$$
with $K$ providing the finite compact-metric net source, $F$ the displayed continuous family, $M$ the monotone comparison window, $D$ the Dini modulus row, and $U$ the uniform-limit handoff.
\end{theorem}
\begin{proof}
Project the carrier rows $K,F,M,D,U,W,R,E,H,C,P,N$ from \autoref{def:constructive-dini-theorem-carrier}. The compact source, continuous family, and monotone ledger are the only rows available before the Dini modulus is inspected.

Use \autoref{thm:constructive-dini-theorem-monotone-uniform-route}. It already forces every uniform-limit consumer through $K,F,M$, then $D$, then $U$, and finally the L10 readback and structural rows.

The finite-net source is part of the compact metric row $K$, while the monotone window is the public comparison data in $M$. Thus the modulus row $D$ cannot be read as an ambient supremum or open-cover compactness principle; it is downstream of the displayed finite compact-metric and monotone data.

Apply \autoref{thm:constructive-dini-theorem-l10-seal-nonescape}. The $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows $W,R,E$ keep the resulting uniform-limit read inside finite L10 windows, and $H,C,P,N$ transport, replay, source, and name only that route.
\end{proof}

\falsifiablePrediction{Every accepted $\ConstructiveDiniTheoremUp$ read must expose a compact metric source, continuous-map family, monotone ledger, Dini modulus row, uniform-limit row, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{dini-monotone-compact-uniform, constructive-dini-modulus, dini-uniform-limit-modulus, compactmetric, continuousmap, uniformlimit, streamname, regseqrat, real: $\ConstructiveDiniTheoremUp$ is not bijective to any listed sibling because it binds the monotone compact-uniform theorem route to the L10 finite real-readback boundary in one local NameCert carrier.}

\closureat{ConstructiveDiniTheoremUp}{seedStr}

\begin{closurestatus}{\ConstructiveDiniTheoremUp}
  \constructivestory{A $\ConstructiveDiniTheoremUp$ packet is generated from compact metric source rows, continuous-map family rows, monotone ledger rows, ConstructiveDiniModulus rows, DiniMonotoneCompactUniform and DiniUniformLimitModulus handoff rows, UniformLimit rows, StreamName finite windows, RegSeqRat readbacks, Real seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-dini-theorem-carrier}, \autoref{thm:constructive-dini-theorem-namecert-obligations}, \autoref{thm:constructive-dini-theorem-monotone-uniform-route}, and \autoref{thm:constructive-dini-theorem-l10-seal-nonescape} seed the finite NameCert form of the constructive Dini theorem over compact metric, continuous-map, uniform-limit, StreamName, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, open-cover compactness, ambient $\RealUp$ completeness, quotient equality of functions, countable choice, hidden limiting values, host suprema, or real trichotomy.}
  \upgradepath{Obligation closure requires checked carrier admission, monotone compact-uniform route exactness, Dini modulus extraction, uniform-limit handoff, StreamName and RegSeqRat readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ConstructiveDiniTheoremUp.}
\end{closurestatus}
