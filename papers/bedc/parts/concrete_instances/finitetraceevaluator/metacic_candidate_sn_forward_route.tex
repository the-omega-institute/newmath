The MetaCIC candidate-set SN handoff of \autoref{ch:concrete-instances-metacic-candidate-set-sn-handoff-namecert} consumes \autoref{ch:concrete-instances-finitetraceevaluator-namecert} only after the finite trace evaluator has displayed checkpoint rows, runtime totality, image readback, and acceptance boundaries. This forward route keeps the SN consumer attached to the evaluator's visible checkpoint and runtime surfaces rather than to an untracked kernel execution.
