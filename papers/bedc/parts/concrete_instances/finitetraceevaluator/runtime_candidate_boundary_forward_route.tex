Every $\FiniteTraceEvaluatorUp$ runtime checkpoint read first consumes the source, transition, accepted-result, transport, route, provenance, and local-name rows of the $\RuntimeCandidateBoundaryUp$ source in \autoref{ch:concrete-instances-runtimecandidateboundary-namecert}. Thus the evaluator checkpoint route reads the candidate boundary before it reads the bounded trace and checkpoint rows; it does not fabricate a host validator, a total truth predicate, or an unbounded runtime stream.
