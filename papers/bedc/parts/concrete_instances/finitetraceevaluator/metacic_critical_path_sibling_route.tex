The $\FiniteTraceEvaluatorUp$ route supplies the bounded trace, checkpoint, runtime-totality, and image-readback rows consumed by \autoref{ch:concrete-instances-metaciccriticalpath-namecert}. A MetaCIC critical-path consumer may use finite evaluator evidence only after those displayed rows have passed through the evaluator source, accepted-result, validation-boundary, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\FiniteTraceEvaluatorUp}$ surface; it does not receive an unbounded runtime stream, hidden interpreter state, or host validation predicate.
