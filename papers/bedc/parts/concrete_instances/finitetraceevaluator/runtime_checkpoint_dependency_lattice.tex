The $\FiniteTraceEvaluatorUp$ dependency lattice consumes the runtime-candidate boundary of \autoref{ch:concrete-instances-runtimecandidateboundary-namecert}, routes finite diagonal trace evidence through \autoref{ch:concrete-instances-haltingdistinctiontrace-namecert}, and supplies bounded checkpoint evidence to \autoref{ch:concrete-instances-reflection-runtime-checkpoint-namecert}. This places the evaluator between the boundary that admits runtime candidates and the checkpoint consumer that reads bounded evidence, so the route is a finite dependency handoff rather than a detached kernel citation.
