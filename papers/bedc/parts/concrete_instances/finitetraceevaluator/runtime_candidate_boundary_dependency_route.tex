The $\FiniteTraceEvaluatorUp$ bounded evaluator route reads the $\RuntimeCandidateBoundaryUp$ packet of \autoref{ch:concrete-instances-runtimecandidateboundary-namecert} before it reads its bounded evaluator rows. The consumed dependency is the displayed source row, transition row, accepted-result row, componentwise transport row, route row, provenance row, and local-name row; these rows supply the runtime candidate boundary that the evaluator later transports through checkpoint, image, acceptance, and trace-budget surfaces.
