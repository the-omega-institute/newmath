The $\FiniteTraceEvaluatorUp$ runtime lattice reads finite trace data through
\autoref{ch:concrete-instances-turingmachine-namecert} and
\autoref{ch:concrete-instances-computable-namecert} before it records checkpoint
image and readback rows. Thus the evaluator remains a finite runtime packet:
the $\TuringMachineUp$ transition surface and the $\ComputableUp$ admission
surface are exposed before checkpoint images, source readbacks, $\BHist$
histories, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$
provenance, and the local $\NameCert_{\FiniteTraceEvaluatorUp}$ row are
consumed.
