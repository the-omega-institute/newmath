The $\FiniteTraceEvaluatorUp$ route consumes \autoref{ch:concrete-instances-runtimecandidateboundary-namecert} as its candidate source and supplies bounded trace evidence to \autoref{ch:concrete-instances-reflection-runtime-checkpoint-namecert}. The handoff is source, trace, and checkpoint routing: the evaluator does not fabricate a detached runtime citation, and the checkpoint packet reads only the bounded trace evidence exposed by this finite route.
