\chapter{A Concrete Naming Certificate for $\BoundaryShadowCompactificationUp$}
\label{ch:concrete-instances-boundary-shadow-compactification-namecert}
\origin{ai}

The $\BoundaryShadowCompactificationUp$ packet realizes the boundary-shadow compactification branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits over the visual boundary surface of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual-metric readback of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the Busemann boundary route of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite compactification-facing carrier; it does not assert a host compact Hausdorff completion, quotient ideal-boundary equality, selected boundary points, global geodesic completeness, or ambient $\RealUp$ completeness.

\begin{definition}[Boundary-shadow compactification carrier]
\label{def:boundary-shadow-compactification-carrier}
A $\BoundaryShadowCompactificationUp$ carrier is a finite $\BHist$ packet
$$
B=(V,S,M,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row, exposing the finite visual-boundary source read by the compactification surface.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row, recording the shadow budget and boundary-shadow incidence consumed by the same read.
\item $M$ is the $\HyperbolicVisualMetricUp$ row, supplying the finite visual-metric comparison used by the shadow boundary.
\item $U$ is the $\HyperbolicBusemannBoundaryUp$ row, recording the Busemann-level boundary witness before any Real-facing seal is public.
\item $E$ is the terminal $\RealUp$ seal row attached to the displayed visual metric and Busemann comparison data.
\item $H$ records componentwise $\hsame$ transport for visual-boundary, boundary-shadow, visual-metric, Busemann, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the visual-boundary request through
$$
V\longrightarrow S\longrightarrow M\longrightarrow U\longrightarrow E .
$$
\item $P$ is the $\Pkg$ provenance over $\BoundaryShadowCompactificationUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicBusemannBoundaryUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BoundaryShadowCompactificationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $V,S,M,U,E,H,C,P,N$. It has no coordinate for host compactification, quotient ideal-boundary equality, selected boundary ends, unbounded geodesic choice, global properness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Boundary-shadow compactification NameCert obligations]
\label{thm:boundary-shadow-compactification-namecert-obligations}
For every accepted $\BoundaryShadowCompactificationUp$ carrier
$$
B=(V,S,M,U,E,H,C,P,N),
$$
the local $\NameCert_{\BoundaryShadowCompactificationUp}$ surface consists exactly of visual-boundary exposure through $V$, boundary-shadow exposure through $S$, visual-metric comparison through $M$, Busemann-boundary exposure through $U$, terminal $\RealUp$ sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier to the tuple of \autoref{def:boundary-shadow-compactification-carrier}. The visual-boundary, shadow, metric, Busemann, and Real rows are exactly $V,S,M,U,E$.

The boundary-facing route is ordered. The visual boundary row $V$ is read before the boundary-shadow row $S$, the visual-metric row $M$ supplies only finite comparison data, and the Busemann row $U$ is displayed before $E$ can be consumed as a Real-facing seal.

The structural rows $H,C,P,N$ transport, replay, source, and name that same route. Since no carrier coordinate stores a host compactification, quotient ideal-boundary equality, selected boundary end, unbounded geodesic choice, global properness, or ambient completeness principle, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Boundary-shadow compactification visual-boundary route]
\label{thm:boundary-shadow-compactification-visual-boundary-route}
Every compactification-facing consumer of an accepted $\BoundaryShadowCompactificationUp$ packet factors through
$$
\HyperbolicVisualBoundaryUp
\longmapsto
\HyperbolicBoundaryShadowUp
\longmapsto
\HyperbolicVisualMetricUp
\longmapsto
\HyperbolicBusemannBoundaryUp
\longmapsto
\RealUp .
$$
The route exports only finite visual-boundary, boundary-shadow, visual-metric, Busemann, Real-seal, transport, replay, provenance, and local naming rows; it exports no host compactification object, quotient ideal-boundary equality, selected boundary point, global geodesic-completeness principle, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier $B=(V,S,M,U,E,H,C,P,N)$. The consumer first reads the visual-boundary source $V$ and the boundary-shadow row $S$.

The metric and Busemann rows then restrict the compactification-facing read to finite comparison data. The row $M$ supplies the displayed visual metric, and $U$ supplies the Busemann boundary witness before the terminal Real seal $E$ is public.

Finally $H,C,P,N$ preserve the ordering and provenance of the route. Therefore every compactification-facing read remains inside the displayed NameCert carrier and cannot project the excluded compactification, equality, selection, global-geodesic, or completeness data.
\end{proof}

\falsifiablePrediction{Every accepted $\BoundaryShadowCompactificationUp$ read displays one HyperbolicVisualBoundary row, one HyperbolicBoundaryShadow row, one HyperbolicVisualMetric row, one HyperbolicBusemannBoundary row, one RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert row before exporting compactification-facing data.}

\independenceWitness{hyperbolic-visual-boundary, hyperbolic-boundary-shadow, hyperbolic-visual-metric, hyperbolic-busemann-boundary, real: $\BoundaryShadowCompactificationUp$ is not bijective to any listed sibling because it jointly carries visual-boundary source data, boundary-shadow incidence, visual-metric comparison, Busemann boundary evidence, a Real seal, transport, replay, provenance, and local naming in one finite packet.}

The Lean TasteGate for this chapter must later provide \texttt{Nontrivial} and \texttt{FieldFaithful} instances for \texttt{BEDC.Derived.BoundaryShadowCompactificationUp}. Those instances must preserve the displayed visual-boundary, boundary-shadow, visual-metric, Busemann, Real-seal, transport, replay, provenance, and local naming rows rather than a host compactification theorem.

\closureat{BoundaryShadowCompactificationUp}{seedStr}

\begin{closurestatus}{\BoundaryShadowCompactificationUp}
  \constructivestory{A $\BoundaryShadowCompactificationUp$ packet is generated from HyperbolicVisualBoundary rows, HyperbolicBoundaryShadow rows, HyperbolicVisualMetric comparisons, HyperbolicBusemannBoundary rows, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:boundary-shadow-compactification-carrier}, \autoref{thm:boundary-shadow-compactification-namecert-obligations}, and \autoref{thm:boundary-shadow-compactification-visual-boundary-route} seed the finite visual-boundary compactification route through boundary shadow, visual metric, Busemann, Real-seal, transport, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host compactification, quotient ideal-boundary equality, selected boundary ends, global geodesic completeness, global properness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, visual-boundary exposure, boundary-shadow exposure, visual-metric comparison, Busemann-boundary exposure, RealUp seal non-escape, transport, replay, provenance, local naming, and Nontrivial and FieldFaithful TasteGate support for BEDC.Derived.BoundaryShadowCompactificationUp.}
\end{closurestatus}
