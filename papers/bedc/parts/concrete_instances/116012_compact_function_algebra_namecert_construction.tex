\chapter{A Concrete Naming Certificate for $\CompactFunctionAlgebraUp$}
\label{ch:concrete-instances-compact-function-algebra-namecert}
\origin{human}

Following the Stone--Weierstrass and Gelfand traditions for continuous functions on compact spaces, the $\CompactFunctionAlgebraUp$ packet records a finite algebra of $\RealUp$-valued continuous functions over a compact metric source. It realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} through the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, the uniform-modulus surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the compact-open function-space carrier of \autoref{ch:concrete-instances-compact-open-function-space-namecert}, and the approximation interface of \autoref{ch:concrete-instances-stone-weierstrass-namecert}. The exported object is a finite operation and norm ledger, not an ambient function quotient.

\begin{definition}[Compact function algebra carrier]
\label{def:compact-function-algebra-carrier}
A $\CompactFunctionAlgebraUp$ carrier is a finite $\BHist$ packet
$$
A=(K,F,+,\cdot,1,U,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact metric source row;
\item $F$ is the finite family of continuous $\RealUp$-valued function rows over $K$;
\item $+$ and $\cdot$ are pointwise addition and multiplication rows on displayed members of $F$;
\item $1$ is the constant-one row, read as a continuous $\RealUp$-valued function;
\item $U$ records uniform-modulus data for every displayed function and every displayed operation output;
\item $S$ is the sup-norm finite-net readback row, computed from compact-net value windows and $\RealUp$ comparison rows;
\item $H$ stores componentwise $\hsame$ transport for $K,F,+,\cdot,1,U,S$ and the local naming rows;
\item $C$ stores the displayed $\Cont$ replay from compact-source reads, continuous-function rows, operation rows, uniform-modulus rows, and sup-norm windows to the algebra ledger;
\item $P$ is the $\Pkg$ provenance over the same compact function packet;
\item $N$ is the local $\NameCert_{\CompactFunctionAlgebraUp}$ row.
\end{enumerate}
The carrier exports operation stability and uniform-norm readback only for the displayed finite packet; it does not identify functions by an ambient quotient or import a completed Banach algebra.
\end{definition}

\begin{theorem}[Compact function algebra NameCert obligations]
\label{thm:compact-function-algebra-namecert-obligations}
For the carrier of \autoref{def:compact-function-algebra-carrier}, the local $\NameCert_{\CompactFunctionAlgebraUp}$ surface consists of five finite obligations: carrier admission for $A$; stability of pointwise addition, pointwise multiplication, and the constant-one row inside the displayed family; uniform-modulus handoff for each displayed function and operation output; sup-norm readback through finite compact nets and $\RealUp$ value windows; and refusal of ambient function quotienting, completed Banach-algebra closure, unrestricted infinite linear combinations, and selected dense approximation data outside the packet.
\end{theorem}

\begin{theorem}[Compact function algebra uniform-norm route]
\label{thm:compact-function-algebra-uniform-norm-route}
Every accepted $\CompactFunctionAlgebraUp$ packet reads its sup-norm row through compact finite nets and $\RealUp$-sealed value windows. The resulting ledger is transported by $H$, replayed by $C$, recorded by $P$, and named by $N$ without exporting an ambient function quotient or a completed algebra.
\end{theorem}
\begin{proof}
Read the compact metric source row $K$ and the finite family $F$ through the displayed continuous-map rows. The pointwise operation rows $+$, $\cdot$, and $1$ stay inside the finite packet, so operation stability is checked before any norm comparison is read.

Next use the uniform-modulus row $U$ to pass from pointwise value windows to compact finite-net windows. Each sampled value is sealed as a $\RealUp$ read, and the sup-norm row $S$ is obtained by folding the displayed value-window comparisons over the finite net.

Finally transport the operation and sup-norm ledger through $H$, replay the route through $C$, record provenance in $P$, and attach the local name $N$. The route exposes finite algebra and norm data, while completed closure, quotient equality, and selected approximation witnesses remain outside $A$.
\end{proof}
\leanstmt{BEDC.Derived.CompactFunctionAlgebraUp}

\closureat{CompactFunctionAlgebraUp}{seedStr}

\begin{closurestatus}{\CompactFunctionAlgebraUp}
  \constructivestory{A $\CompactFunctionAlgebraUp$ packet is generated from a compact metric source, finite continuous real-valued function rows, pointwise addition and multiplication rows, a constant-one row, uniform-modulus rows, sup-norm finite-net readback, componentwise transport, replay, provenance, and a local naming row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-function-algebra-carrier}, \autoref{thm:compact-function-algebra-namecert-obligations}, and \autoref{thm:compact-function-algebra-uniform-norm-route} name the finite compact function algebra carrier and uniform-norm readback boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close completed Banach-algebra structure, unrestricted Stone--Weierstrass approximation, quotient function equality, selected dense witnesses, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, operation stability, uniform-modulus handoff, sup-norm finite-net readback, and local naming for the same packet.}
\end{closurestatus}
