\chapter{A Concrete Naming Certificate for $\EvenOddCauchyCriterionUp$}
\label{ch:concrete-instances-even-odd-cauchy-criterion-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\EvenOddCauchyCriterionUp$ packet records the finite parity-tail criterion by which two regular rational subsequence rows are fused into a single Real-facing Cauchy route. It sits between the regular rational carrier of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite schedule discipline of \autoref{ch:concrete-instances-streamname-namecert}, the Real-specific Cauchy consumer of \autoref{ch:concrete-instances-real-cauchy-criterion-namecert}, and the parity-selector interleaving surface of \autoref{ch:concrete-instances-regularcauchyinterleaving-namecert}. The packet is a parity-subsequence Cauchy certificate; it is not a Real completeness principle, quotient stream equality, selected limit, host real equality, trichotomy, or countable-choice source.

\begin{definition}[EvenOddCauchyCriterion carrier]
\label{def:even-odd-cauchy-criterion-carrier}
An $\EvenOddCauchyCriterionUp$ carrier is a finite $\BHist$ packet
$$
K=(A,B,S_A,S_B,\epsilon,M,D,F,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two $\RegSeqRatUp$ rows, one for the even subsequence and one for the odd subsequence.
\item $S_A$ and $S_B$ are the two $\StreamNameUp$ schedules exposing the finite observation windows for the even and odd rows.
\item $\epsilon$ is the even--odd selector row recording which schedule is inspected at each displayed parity step.
\item $M$ is the shared $\CauchyModulusUp$ ledger, carrying the common precision request, the two side budgets, and the late-tail threshold used by both parity rows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the rational radii, dyadic comparisons, and triangle-budget cells used to compare same-parity and cross-parity late windows.
\item $F$ is the cross-parity tail-fusion row, tying every accepted even late window to an accepted odd late window under the same $M,D$ budget.
\item $R$ is the $\RealCauchyCriterionUp$ handoff row, exposed only after $A,B,S_A,S_B,\epsilon,M,D,F$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two regular rows, two schedules, selector, shared modulus ledger, dyadic tolerance ledger, fusion row, Real Cauchy handoff, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from a precision request through the selector, the selected schedule, the selected regular row, the shared modulus ledger, the dyadic tolerance ledger, the cross-parity fusion row, and the Real Cauchy handoff.
\item $P$ is the $\Pkg$ provenance over $\EvenOddCauchyCriterionUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\RealCauchyCriterionUp$, $\RegularCauchyInterleavingUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EvenOddCauchyCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed rows $A,B,S_A,S_B,\epsilon,M,D,F,R,H,C,P,N$. It has no coordinate for quotient streams, selected limits, countable choice, host real equality, trichotomy, or ambient Real completeness.
\end{definition}
\leanchecked{BEDC.Derived.EvenOddCauchyCriterionUp.EvenOddCauchyCriterionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[EvenOddCauchyCriterion NameCert obligations]
\label{thm:even-odd-cauchy-criterion-namecert-obligations}
For an accepted $\EvenOddCauchyCriterionUp$ carrier $K=(A,B,S_A,S_B,\epsilon,M,D,F,R,H,C,P,N)$, the local $\NameCert_{\EvenOddCauchyCriterionUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the two regular rational rows, two stream schedules, parity selector, shared CauchyModulus ledger, DyadicRatCore tolerance ledger, cross-parity fusion row, Real Cauchy handoff, transport, replay, provenance, and local naming row;
\item parity schedule compatibility, requiring each even read to use $A,S_A$ and each odd read to use $B,S_B$ under the selector row $\epsilon$;
\item shared modulus exactness, requiring both parity sides and every cross-parity late comparison to consume the same displayed $M$ budget;
\item dyadic tolerance exactness, requiring same-parity and cross-parity tail comparisons to be recorded in $D$ before the fusion row $F$ can be exposed;
\item transport and replay stability, requiring componentwise $\hsame$ transport $H$ and displayed $\Cont$ replay $C$ to preserve the ordered route through $\epsilon,M,D,F,R$;
\item provenance exhaustion, requiring $P$ and $N$ to name only this displayed route and to export no quotient streams, selected limits, countable choice, host real equality, trichotomy, or ambient Real completeness.
\end{enumerate}
No local NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:even-odd-cauchy-criterion-carrier}. The visible packet contains exactly the two regular rows, two schedules, selector, shared modulus ledger, dyadic tolerance ledger, fusion row, Real Cauchy handoff, and the structural rows $H,C,P,N$.

Parity schedule compatibility is read from $\epsilon,S_A,S_B$. Even requests are routed through $A,S_A$, while odd requests are routed through $B,S_B$; the packet has no third schedule or unscheduled subsequence row.

Shared modulus and dyadic tolerance exactness are the roles of $M$ and $D$. The same $M$ budget is consumed on both parity sides, and $D$ stores the dyadic comparisons needed before $F$ may identify the cross-parity late-tail route.

Transport and replay are structural. Componentwise $\hsame$ in $H$ preserves the displayed rows, and $\Cont$ replay in $C$ repeats the precision-request route through the selector, selected schedule, shared budget, tolerance ledger, fusion row, and Real Cauchy handoff.

Finally, provenance is exhausted by $P$ and $N$. Since the carrier has no row for quotient stream equality, selected limits, countable choice, host real equality, trichotomy, or ambient Real completeness, the local certificate cannot export any such object.
\end{proof}

\begin{theorem}[EvenOddCauchyCriterion parity-tail fusion]
\label{thm:even-odd-cauchy-criterion-parity-tail-fusion}
Every accepted $\EvenOddCauchyCriterionUp$ carrier factors each late-tail comparison through the finite route
$$
\RegSeqRatUp \longmapsto \StreamNameUp \longmapsto \CauchyModulusUp \longmapsto \DyadicRatCoreUp \longmapsto \RealCauchyCriterionUp .
$$
The even side uses $A,S_A$ and the odd side uses $B,S_B$, while the cross-parity row $F$ records that both sides consume the same $M,D$ budget before the handoff to $R$. Thus the packet fuses the parity tails without introducing quotient streams, selected limits, countable choice, host real equality, trichotomy, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with an accepted carrier $K$. The projection of \autoref{def:even-odd-cauchy-criterion-carrier} exposes the two regular rows and their two schedules before any tolerance comparison is available.

Use the selector row $\epsilon$ to split the finite read. The even case reads through $A,S_A$, and the odd case reads through $B,S_B$.

Both cases then pass through the same shared modulus row $M$. The dyadic tolerance ledger $D$ records the corresponding same-parity and cross-parity comparison cells under that common budget.

The fusion row $F$ is downstream of $M,D$. It can therefore identify the even and odd late-tail routes only after the shared budget and dyadic comparisons have been displayed.

Finally, the Real Cauchy handoff $R$ receives the fused parity-tail route. The excluded quotient, choice, host-equality, trichotomy, and completeness objects are absent from the carrier inventory, so the fusion cannot export them.
\end{proof}

\begin{theorem}[EvenOddCauchyCriterion Real seal route]
\label{thm:even-odd-cauchy-criterion-real-seal-route}
For every accepted $\EvenOddCauchyCriterionUp$ packet
$$
K=(A,B,S_A,S_B,\epsilon,M,D,F,R,H,C,P,N),
$$
any Real-facing consumer of the packet must pass through the two $\RegSeqRatUp$ rows, the two $\StreamNameUp$ schedules, the even--odd selector, the shared $\CauchyModulusUp$ ledger, the $\DyadicRatCoreUp$ tolerance ledger, the cross-parity fusion row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row before it can consume the $\RealCauchyCriterionUp$ handoff $R$.
\end{theorem}
\begin{proof}
Project the carrier rows of $K$. The handoff row $R$ is not an independent Real seal; it is placed after the two regular rows, two schedules, selector, shared modulus, dyadic tolerance ledger, and cross-parity fusion row.

\autoref{thm:even-odd-cauchy-criterion-namecert-obligations} supplies the certificate surface controlling carrier admission, schedule compatibility, shared modulus exactness, dyadic tolerance exactness, transport, replay, and provenance exhaustion.

\autoref{thm:even-odd-cauchy-criterion-parity-tail-fusion} supplies the parity-tail route. Combining that route with the carrier projection shows that any Real-facing use of $R$ first passes through the displayed even and odd windows and the common $M,D$ comparison ledger.

The rows $H,C,P,N$ preserve and name the same route. Since no quotient stream, selected limit, countable-choice selector, host real equality, trichotomy, or ambient completeness row appears in the carrier, no consumer can bypass the displayed $\RealCauchyCriterionUp$ handoff boundary.
\end{proof}

\begin{theorem}[EvenOddCauchyCriterion shared modulus transport]
\label{thm:even-odd-cauchy-criterion-shared-modulus-transport}
For every accepted $\EvenOddCauchyCriterionUp$ packet
$$
K=(A,B,S_A,S_B,\epsilon,M,D,F,R,H,C,P,N),
$$
the shared $M$ budget and the $D$ tolerance ledger are transported componentwise through the $\hsame$ row $H$ and replayed through the $\Cont$ row $C$ before either the cross-parity fusion row $F$ or the Real Cauchy handoff row $R$ can be exposed. In particular, no accepted consumer can replace $M$ by separate parity budgets or replace $D$ by an undisplayed tolerance ledger while still reaching $F$ or $R$.
\end{theorem}
\begin{proof}
Begin with the carrier inventory of \autoref{def:even-odd-cauchy-criterion-carrier}. The rows $M$ and $D$ are displayed before $F,R,H,C,P,N$, and the classifier compares packets only through the listed coordinates.

\autoref{thm:even-odd-cauchy-criterion-namecert-obligations} identifies shared modulus exactness, dyadic tolerance exactness, transport stability, and replay stability as local NameCert obligations. Therefore $H$ must preserve the same $M,D$ cells on the even and odd sides rather than admitting parity-specific substitutes.

Next apply the replay row $C$. Its route starts from a precision request and passes through the selector, selected schedule, selected regular row, shared modulus ledger, dyadic tolerance ledger, fusion row, and Real Cauchy handoff. Thus replay cannot expose $F$ or $R$ until the transported $M,D$ ledger has already been displayed.

Finally combine the parity-tail factorization of \autoref{thm:even-odd-cauchy-criterion-parity-tail-fusion} with the Real seal boundary of \autoref{thm:even-odd-cauchy-criterion-real-seal-route}. The only late-tail route reaching $F$ or $R$ is the one carrying the same shared budget and dyadic tolerance ledger through $H$ and $C$.
\end{proof}

\begin{theorem}[EvenOddCauchyCriterion cross-parity handoff non-escape]
\label{thm:even-odd-cauchy-criterion-cross-parity-handoff-non-escape}
For every accepted $\EvenOddCauchyCriterionUp$ packet
$$
K=(A,B,S_A,S_B,\epsilon,M,D,F,R,H,C,P,N),
$$
the cross-parity fusion row $F$ and the Real Cauchy handoff row $R$ can be consumed only after the packet has displayed the two $\RegSeqRatUp$ rows $A,B$, the two $\StreamNameUp$ schedules $S_A,S_B$, the selector $\epsilon$, the shared modulus ledger $M$, the dyadic tolerance ledger $D$, componentwise transport $H$, displayed replay $C$, provenance $P$, and the local $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
The carrier definition \autoref{def:even-odd-cauchy-criterion-carrier} places $F$ and $R$ in a finite packet whose earlier coordinates name both regular rows, both schedules, the selector, the common budget, and the tolerance ledger. There is no independent coordinate from which a consumer could obtain a fusion row or a Real handoff.

\autoref{thm:even-odd-cauchy-criterion-namecert-obligations} then fixes the admission and stability surface. Carrier admission accounts for $A,B,S_A,S_B,\epsilon,M,D,F,R,H,C,P,N$, while transport, replay, and provenance exhaustion require the later rows to preserve and name that same displayed route.

The factorization in \autoref{thm:even-odd-cauchy-criterion-parity-tail-fusion} supplies the cross-parity ordering: $F$ is downstream of the two parity tails under the shared $M,D$ budget. Hence $F$ cannot be consumed before the two regular rows, two schedules, selector, shared modulus, and dyadic ledger have been displayed.

The Real seal route in \autoref{thm:even-odd-cauchy-criterion-real-seal-route} adds the handoff boundary. Since $R$ is downstream of $F$ and is preserved only through $H,C,P,N$, a Real-facing consumer reaches $R$ only through the complete displayed NameCert route, and no quotient stream, selected limit, countable-choice selector, host real equality, trichotomy, or ambient completeness object can provide an escape path.
\end{proof}

\closureat{EvenOddCauchyCriterionUp}{seedStr}

\begin{closurestatus}{\EvenOddCauchyCriterionUp}
  \constructivestory{An $\EvenOddCauchyCriterionUp$ packet is generated from two RegSeqRat rows, two StreamName schedules, an even--odd selector, a shared CauchyModulus ledger, a DyadicRatCore tolerance ledger, a cross-parity fusion row, a RealCauchyCriterion handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:even-odd-cauchy-criterion-carrier}, \autoref{thm:even-odd-cauchy-criterion-namecert-obligations}, \autoref{thm:even-odd-cauchy-criterion-parity-tail-fusion}, \autoref{thm:even-odd-cauchy-criterion-real-seal-route}, \autoref{thm:even-odd-cauchy-criterion-shared-modulus-transport}, and \autoref{thm:even-odd-cauchy-criterion-cross-parity-handoff-non-escape} seed the parity-tail criterion route through RegSeqRat, StreamName, CauchyModulus, DyadicRatCore, RealCauchyCriterion, and RegularCauchyInterleaving faces.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Real completeness, quotient streams, selected limits, host real equality, trichotomy, countable choice, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, parity schedule compatibility, shared modulus exactness, dyadic tolerance exactness, tail-fusion factorization, Real Cauchy handoff factorization, transport stability, replay stability, and provenance exhaustion for BEDC.Derived.EvenOddCauchyCriterionUp.}
\end{closurestatus}
