\chapter{A Concrete Naming Certificate for $\CauchyRealOrderedFieldUp$}
\label{ch:concrete-instances-cauchy-real-ordered-field-namecert}
\origin{human}

Following Bishop and Bridges, the $\CauchyRealOrderedFieldUp$ packet joins the $\CauchyRealFieldUp$ operation surface with the constructive comparison rows of $\CauchyRealOrderUp$, $\RealAlgOrderUp$, and $\ArchimedeanOrderedFieldUp$. It sits after the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the field-facing Cauchy real packet of \autoref{ch:concrete-instances-cauchy-real-field-namecert}, the constructive order packet of \autoref{ch:concrete-instances-cauchy-real-order-namecert}, the algebra/order comparison packet of \autoref{ch:concrete-instances-real-alg-order-namecert}, and the Archimedean ordered-field comparison packet of \autoref{ch:concrete-instances-archimedeanorderedfield-namecert}. The packet gives Real and Completion consumers a finite ordered-field interface; it is not host real arithmetic, quotient equality, trichotomy, a selected-limit operation, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Cauchy real ordered field carrier]
\label{def:cauchy-real-ordered-field-carrier}
A $\CauchyRealOrderedFieldUp$ carrier is a finite $\BHist$ packet
$$
K=(F,O,A,B,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\CauchyRealFieldUp$ field row. It records the regular rational source family, stream windows, dyadic arithmetic ledger, addition, product, reciprocal, constructive order input, and field-facing real seal already named by the Cauchy real field packet.
\item $O$ is the $\CauchyRealOrderUp$ constructive order and apartness row. It supplies the positive-gap route, finite refusal of positive apartness, and oriented comparison verdicts read by the ordered-field packet.
\item $A$ is the $\RealAlgOrderUp$ comparison row. It records the displayed addition, negation, multiplication, absolute-value, nonnegative-order, and strict-apartness reads needed to compare algebra and order data on the same real-name surface.
\item $B$ is the $\ArchimedeanOrderedFieldUp$ bound row. It records the rational-source, rational-embedding, and Archimedean comparison rows that locate the ordered-field interface inside finite rational bounds.
\item $Z$ is the shared $\RealUp$ seal row. It is downstream of the field, order, algebra/order, and Archimedean rows rather than an independent completed-real carrier.
\item $H$ records componentwise $\hsame$ transport for the field row, order row, algebra/order row, Archimedean row, real seal row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from each ordered-field request through $F,O,A,B,Z,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchyRealOrderedFieldUp$, $\CauchyRealFieldUp$, $\CauchyRealOrderUp$, $\RealAlgOrderUp$, $\ArchimedeanOrderedFieldUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyRealOrderedFieldUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite constructive ordered-field packet. It has no coordinate for host real arithmetic, quotient equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Cauchy real ordered field NameCert obligations]
\label{thm:cauchy-real-ordered-field-namecert-obligations}
For every accepted $\CauchyRealOrderedFieldUp$ carrier
$$
K=(F,O,A,B,Z,H,C,P,N),
$$
the local $\NameCert_{\CauchyRealOrderedFieldUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the Cauchy real field row, constructive order row, RealAlgOrder comparison row, Archimedean ordered-field bound row, shared real seal, transport, replay, provenance, and local naming row;
\item field projection, requiring every addition, product, reciprocal, zero, one, and negation read to pass through the displayed $\CauchyRealFieldUp$ row $F$;
\item order projection, requiring every positive-gap, apartness-refusal, and oriented comparison read to pass through the displayed $\CauchyRealOrderUp$ row $O$;
\item algebra/order compatibility, requiring the $\RealAlgOrderUp$ row $A$ to read the same real-name seal and dyadic comparison ledgers used by $F$ and $O$;
\item Archimedean boundedness, requiring the $\ArchimedeanOrderedFieldUp$ row $B$ to supply only displayed rational-source, rational-embedding, and bound rows over the same field and order surface;
\item shared seal discipline, requiring $Z$ to be downstream of $F,O,A,B$ and never a separate completed-real object;
\item transport and replay, requiring $H$ and $C$ to preserve the field, order, comparison, bound, seal, provenance, and naming rows together;
\item ledger exhaustion, excluding host real arithmetic, quotient equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{enumerate}
No Cauchy real ordered-field obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-real-ordered-field-carrier}. The projection exposes exactly the field row, order row, algebra/order comparison row, Archimedean bound row, shared seal row, and structural rows.

The field and order projections are inherited from the sibling Cauchy real surfaces. The row $F$ supplies the finite operation route of \autoref{ch:concrete-instances-cauchy-real-field-namecert}, while the row $O$ supplies the positive-gap, negative-apartness, and oriented-order routes of \autoref{ch:concrete-instances-cauchy-real-order-namecert}.

The comparison and bound projections are separate visible rows. The row $A$ keeps algebraic and order reads inside the displayed $\RealAlgOrderUp$ endpoint, dyadic, finite-window, and apartness ledgers, and the row $B$ keeps rational-bound reads inside the displayed $\ArchimedeanOrderedFieldUp$ rational-source and Archimedean comparison rows.

Finally, $Z,H,C,P,N$ seal, transport, replay, source, and name the same finite packet. Since the carrier contains no coordinate for any excluded principle, none can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Cauchy real ordered field order-field route]
\label{thm:cauchy-real-ordered-field-order-field-route}
Every accepted $\CauchyRealOrderedFieldUp$ carrier routes ordered-field reads through the finite chain
$$
F,O \longmapsto A \longmapsto B \longmapsto Z \longmapsto H,C,P,N .
$$
Thus the operation surface and the comparison surface are joined only by displayed Cauchy real field, Cauchy real order, RealAlgOrder, and Archimedean ordered-field rows.
\end{theorem}
\begin{proof}
Start from the accepted packet $K=(F,O,A,B,Z,H,C,P,N)$. The only operation coordinate is $F$, and the only constructive Cauchy comparison coordinate is $O$.

Read the algebra/order compatibility row next. The row $A$ consumes the same displayed real-name, dyadic, finite-window, and apartness data exposed by the field and order rows, so it cannot introduce a second operation or comparison surface.

Read the Archimedean row after that. The row $B$ contributes rational-source, rational-embedding, and bound evidence over the same real-name surface; it does not replace the field and order rows with an ambient ordered field.

The real seal and structural rows are downstream. The row $Z$ is reached only after $F,O,A,B$ have been displayed, and $H,C,P,N$ preserve this dependency order for every ordered-field consumer.
\end{proof}

\begin{theorem}[Cauchy real ordered field non-escape]
\label{thm:cauchy-real-ordered-field-nonescape}
For every accepted $\CauchyRealOrderedFieldUp$ carrier $K=(F,O,A,B,Z,H,C,P,N)$, every Real or Completion consumer receives only the finite rows displayed by $K$. In particular, the ordered-field packet exports no host real arithmetic, quotient equality, trichotomy, selected limit, countable-choice schedule, ambient $\RealUp$ completeness datum, Lean verification, or bridge-checked standard structure.
\end{theorem}
\begin{proof}
Inspect the carrier inventory. The only public rows are $F,O,A,B,Z,H,C,P,N$, and each of the first four rows is itself a named finite BEDC packet with visible source, ledger, comparison, and naming data.

A Real or Completion consumer may project field operations from $F$, constructive comparisons from $O$, algebra/order compatibility from $A$, or rational bounds from $B$. Each projection remains tied to the shared seal $Z$ and the same transport and replay rows.

The structural rows do not add analytic content. They transport, replay, source, and name the displayed packet. Therefore no excluded principle has a coordinate, a replay path, or a provenance row through which it can escape to the consumer.
\end{proof}

\closureat{CauchyRealOrderedFieldUp}{seedStr}
\begin{closurestatus}{\CauchyRealOrderedFieldUp}
  \constructivestory{A $\CauchyRealOrderedFieldUp$ packet is generated from a $\CauchyRealFieldUp$ field row, a $\CauchyRealOrderUp$ constructive order row, a $\RealAlgOrderUp$ comparison row, an $\ArchimedeanOrderedFieldUp$ rational-bound row, a shared $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-real-ordered-field-carrier}, \autoref{thm:cauchy-real-ordered-field-namecert-obligations}, \autoref{thm:cauchy-real-ordered-field-order-field-route}, and \autoref{thm:cauchy-real-ordered-field-nonescape} seed the finite ordered-field route over the existing Cauchy real field, Cauchy real order, RealAlgOrder, Archimedean ordered-field, and shared real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real arithmetic, quotient equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires a checked finite carrier theorem for BEDC.Derived.CauchyRealOrderedFieldUp binding the displayed field, order, algebra/order, Archimedean, real-seal, transport, replay, provenance, and local naming rows.}
\end{closurestatus}
