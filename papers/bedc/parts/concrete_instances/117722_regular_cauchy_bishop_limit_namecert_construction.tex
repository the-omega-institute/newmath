\chapter{A Concrete Naming Certificate for $\RegularCauchyBishopLimitUp$}
\label{ch:concrete-instances-regular-cauchy-bishop-limit-namecert}
\origin{human}

Following Bishop--Bridges constructive real analysis, the $\RegularCauchyBishopLimitUp$ packet records the finite operation row by which a regular Cauchy name is read as a Real-facing limit without importing an ambient completeness theorem. It sits after the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names an operation-facing carrier for the displayed limit route; it is not quotient equality of streams, a selected host limit, arbitrary trichotomy, countable choice, or completeness for $\RealUp$.

\begin{definition}[Regular Cauchy Bishop limit carrier]
\label{def:regular-cauchy-bishop-limit-carrier}
A $\RegularCauchyBishopLimitUp$ carrier is a finite $\BHist$ packet
$$
L=(S,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source regular-Cauchy row, displaying the finite modulus and tail-comparison evidence accepted by the operation.
\item $W$ is the $\StreamNameUp$ window row through which the source name is inspected at the requested precision.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the exact dyadic scale used by the window comparison.
\item $R$ is the $\RegSeqRatUp$ readback row obtained only after the source row, stream window, and dyadic ledger are visible.
\item $E$ is the terminal $\RealUp$ seal row exposed only after the regular-rational readback row $R$.
\item $H$ records componentwise $\hsame$ transport for source, window, tolerance, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,W,D \longmapsto R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyBishopLimitUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyBishopLimitUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,W,D,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected host limit, arbitrary real trichotomy, countable choice, or ambient completeness for $\RealUp$.
\end{definition}
\leanstmt{BEDC.Derived.RegularCauchyBishopLimitUp}

\begin{theorem}[Regular Cauchy Bishop limit NameCert obligations]
\label{thm:regular-cauchy-bishop-limit-namecert-obligations}
For every accepted $\RegularCauchyBishopLimitUp$ carrier $L=(S,W,D,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyBishopLimitUp}$ surface consists exactly of carrier admission; source regular-Cauchy visibility through $S$; finite-window coverage through $W$; dyadic exactness through $D$; $\RegSeqRatUp$ readback through $R$; $\RealUp$ seal nonescape through $E$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$. A limit-operation witness missing any one of these rows is not an accepted packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-bishop-limit-carrier}. The rows $S,W,D$ are the complete finite source surface for the regular-Cauchy operation, so carrier admission, finite-window coverage, and dyadic exactness are all visible before any Real-facing read.

Read the handoff rows next. The regular rational row $R$ is downstream of $S,W,D$, and the terminal $\RealUp$ seal $E$ is downstream of $R$.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name the same displayed dependency route. Since no excluded quotient equality, selected limit, trichotomy, choice, or completeness row occurs in the carrier, every accepted obligation stays inside the listed NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy Bishop limit Real-seal handoff]
\label{thm:regular-cauchy-bishop-limit-real-seal-handoff}
Every Real-facing limit read of an accepted $\RegularCauchyBishopLimitUp$ carrier factors through the displayed $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows. The handoff exports no ambient $\RealUp$ completeness, quotient stream equality, arbitrary trichotomy, countable choice, or selected host limit.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-bishop-limit-namecert-obligations}. It identifies the source row $S$, stream-window row $W$, and dyadic tolerance ledger $D$ as the only finite input surface for the operation-facing limit read.

The readback row $R$ is therefore available only through that finite input surface. A Real-facing consumer reaches the seal row $E$ only after this $\RegSeqRatUp$ readback has been displayed.

The transport, replay, provenance, and local naming rows preserve the same dependency order. No carrier row can supply a host completeness theorem, quotient stream equality, trichotomy, countable choice, or selected limit, so the terminal handoff remains a finite seal rather than an ambient Real construction.
\end{proof}

\closureat{RegularCauchyBishopLimitUp}{seedStr}
\begin{closurestatus}{\RegularCauchyBishopLimitUp}
  \constructivestory{A $\RegularCauchyBishopLimitUp$ packet is generated from a source regular-Cauchy row, a StreamName window row, a DyadicRatCore tolerance ledger, a RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-bishop-limit-carrier}, \autoref{thm:regular-cauchy-bishop-limit-namecert-obligations}, and \autoref{thm:regular-cauchy-bishop-limit-real-seal-handoff} seed the finite Bishop-style limit-operation route from regular Cauchy evidence to the RealUp seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient RealUp completeness, quotient stream equality, arbitrary trichotomy, countable choice, selected host limits, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window coverage, dyadic exactness, RegSeqRat readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyBishopLimitUp.}
\end{closurestatus}
