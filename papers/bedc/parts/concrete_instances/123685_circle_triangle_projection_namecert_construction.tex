\chapter{A Concrete Naming Certificate for $\CircleTriangleProjectionUp$}
\label{ch:concrete-instances-circle-triangle-projection-namecert}
\origin{ai}

The $\CircleTriangleProjectionUp$ packet realizes the projection-row surface of \autoref{ch:visions-rh-circle-triangle-projection-dictionary} as finite NameCert data. It packages a visible RH criterion surface, the projection read from the circle-triangle register, the retained residual ledger, and the back-transport row to the zero-read surface before any RH-facing consumer may treat Li, Weil, Nyman, Lee--Yang, dominance, or tower-cofinality criteria as related. The packet is a finite projection-row carrier; it is not a proof of $\mathsf{RH}$, an analytic continuation theorem, a zero-location theorem, a hidden-scale theorem, or a completed infinite criterion space.

The projection row consumes the zero-route initiality boundary of \autoref{ch:concrete-instances-rh-free-zero-initiality-namecert} and the typed zero-factorization surface of \autoref{ch:concrete-instances-zeta-zero-type-factorization-namecert}. The residual ledger and back-transport row are therefore not standalone RH evidence, but a finite consumer route over those sibling packets.

\begin{definition}[Circle-triangle projection NameCert carrier]
\label{def:circle-triangle-projection-namecert-carrier}
A $\CircleTriangleProjectionUp$ carrier is a finite $\BHist$ packet
$$
C=(R,V,\Pi,\Lambda,B,Z,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the RH circle-triangle register row carrying generated time, unit-circle phase, orthogonal triangle readout, Zeckendorf--prime ledger, Li scale-phase coordinates, and hidden-scale refusal data.
\item $V$ is the visible criterion surface row, such as a Li time sequence, Weil energy form, Nyman container, Lee--Yang finite boundary, dominance read, or tower-cofinality window.
\item $\Pi$ is the finite projection read from $R$ to $V$.
\item $\Lambda$ is the residual ledger retained by that projection, including hidden fibers, dominance choices, branch windows, or unexhausted tower rows.
\item $B$ is the back-transport row converting a violation, positivity certificate, boundary witness, or exhaustion claim on $V$ back to the zero-read row.
\item $Z$ is the zero-read compatibility row attaching the projection to the RH-free zero route rather than to an unrelated analytic obligation.
\item $H$ records componentwise $\hsame$ transport for $R,V,\Pi,\Lambda,B,Z,K,P,N$.
\item $K$ records displayed $\Cont$ replay of the projection read, residual ledger, and back-transport route.
\item $P$ is the $\Pkg$ provenance over $\CircleTriangleProjectionUp$, $\RhFreeZeroInitialityUp$, $\ZetaZeroTypeFactorizationUp$, $\FinitePrimeWindowGluingUp$, $\RhRouteHaltingBoundaryUp$, $\ZetaEulerTickLedgerUp$, $\PrimeShiftAnalyticCompatibilityUp$, $\ZeckendorfPrimeEdgeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CircleTriangleProjectionUp}$ row.
\end{enumerate}
The classifier compares accepted projection rows only through these coordinates. It has no coordinate for a completed RH proof, a hidden analytic scale, a selected zero ledger, a host infinite criterion space, or an unledgered projection collapse.
\end{definition}

\begin{theorem}[Circle-triangle projection NameCert obligations]
\label{thm:circle-triangle-projection-namecert-obligations}
For every accepted $\CircleTriangleProjectionUp$ carrier $C=(R,V,\Pi,\Lambda,B,Z,H,K,P,N)$, the local $\NameCert_{\CircleTriangleProjectionUp}$ surface consists exactly of register admission, visible-surface admission, finite projection readback, residual-ledger exposure, back-transport exactness, zero-read compatibility, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:circle-triangle-projection-namecert-carrier}. The rows $R,V,\Pi,\Lambda,B,Z$ are the only mathematical coordinates available before transport, replay, provenance, and naming.

The projection row is finite. The visible surface $V$ is read only through $\Pi$, and the residual ledger $\Lambda$ remains attached, so a consumer cannot erase hidden fibers, dominance choices, branch windows, or tower residues while retaining the projection.

The back-transport row $B$ and zero-read row $Z$ connect the visible criterion back to the RH-facing zero surface. The structural rows $H,K,P,N$ preserve, replay, source, and name the same route. Since no additional coordinate exists, the NameCert surface cannot introduce a completed RH proof, hidden analytic scale, selected zero ledger, host infinite criterion space, or unledgered projection collapse.
\end{proof}

\falsifiablePrediction{An accepted $\CircleTriangleProjectionUp$ consumer that reads a Li, Weil, Nyman, Lee--Yang, dominance, or tower-cofinality projection without displaying a projection read, residual ledger, back-transport row, zero-read compatibility row, transport, replay, provenance, and local NameCert data refutes the carrier surface.}

\independenceWitness{rh-free-zero-initiality, zeta-zero-type-factorization, finite-prime-window-gluing, rh-route-halting-boundary, zeta-euler-tick-ledger: $\CircleTriangleProjectionUp$ is not bijective to any listed sibling because it jointly carries a register row, visible criterion surface, projection read, residual ledger, back-transport row, zero-read compatibility, transport, replay, provenance, and local naming as one finite projection-row certificate.}

\closureat{CircleTriangleProjectionUp}{seedStr}

\begin{closurestatus}{\CircleTriangleProjectionUp}
  \constructivestory{A $\CircleTriangleProjectionUp$ packet is generated from an RH circle-triangle register row, visible criterion surface, finite projection read, residual ledger, back-transport row, zero-read compatibility row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:circle-triangle-projection-namecert-carrier} and \autoref{thm:circle-triangle-projection-namecert-obligations} seed the finite projection-row NameCert for the RH circle-triangle dictionary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close $\mathsf{RH}$, analytic continuation, zero-location, hidden-scale exclusion, infinite criterion cofinality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked register admission, visible-surface admission, projection readback, residual-ledger exposure, back-transport exactness, zero-read compatibility, transport, replay, provenance, and local naming for BEDC.Derived.CircleTriangleProjectionUp.}
\end{closurestatus}
