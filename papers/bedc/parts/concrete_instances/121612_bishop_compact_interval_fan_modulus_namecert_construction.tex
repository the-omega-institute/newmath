\chapter{A Concrete Naming Certificate for $\BishopCompactIntervalFanModulusUp$}
\label{ch:concrete-instances-bishop-compact-interval-fan-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopCompactIntervalFanModulusUp$ packet records the finite fan-modulus surface used when a compact interval argument is read through constructive interval data. It sits after the compact fan bridge of \autoref{ch:concrete-instances-compact-interval-fan-bridge-namecert}, the modulus selector of \autoref{ch:concrete-instances-compact-interval-fan-modulus-selector-namecert}, the dyadic interval-net surface of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, the finite stream-name route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite modulus row for compact interval consumers without asserting open-cover compactness, selected subcovers, quotient real equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[BishopCompactIntervalFanModulus carrier]
\label{def:bishop-compact-interval-fan-modulus-carrier}
A $\BishopCompactIntervalFanModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(B,S,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\CompactIntervalFanBridgeUp$ row, carrying the finite bridge from interval subdivision data to fan-indexed compact interval reads.
\item $S$ is the $\CompactIntervalFanModulusSelectorUp$ row, recording the selected finite modulus depth for the displayed interval and precision request.
\item $D$ is the $\DyadicIntervalNetUp$ row, exposing the dyadic interval-net cells inspected at the chosen depth.
\item $W$ is the $\StreamNameUp$ window row for the interval endpoints, sampled cells, and modulus witnesses.
\item $R$ is the $\RegSeqRatUp$ readback row turning those windows into regular rational observations.
\item $E$ is the terminal $\RealUp$ seal row available only after the finite fan, dyadic-net, stream, and regular-rational rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for the bridge, selector, dyadic-net, stream, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for compact interval modulus requests through $B,S,D,W,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopCompactIntervalFanModulusUp$, $\CompactIntervalFanBridgeUp$, $\CompactIntervalFanModulusSelectorUp$, $\DyadicIntervalNetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCompactIntervalFanModulusUp}$ row.
\end{enumerate}
The carrier predicate asserts that these rows form one finite fan-modulus history for a displayed compact interval request. It has no coordinate for open-cover compactness, a selected arbitrary subcover, quotient real equality, countable choice, trichotomy, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[BishopCompactIntervalFanModulus NameCert obligations]
\label{thm:bishop-compact-interval-fan-modulus-namecert-obligations}
For every accepted $\BishopCompactIntervalFanModulusUp$ carrier $M=(B,S,D,W,R,E,H,C,P,N)$, the local $\NameCert_{\BishopCompactIntervalFanModulusUp}$ surface consists exactly of compact fan bridge admission, finite modulus-depth selection, dyadic interval-net exposure, stream-window exposure, regular rational readback, Real seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No compact interval modulus obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-compact-interval-fan-modulus-carrier}. The fan bridge row $B$, selector row $S$, and dyadic interval-net row $D$ are the only compact interval modulus data in the packet.

Read the analytic-facing rows next. The stream row $W$ exposes finite endpoint and cell observations, the regular rational row $R$ supplies the readback used by completion-facing consumers, and the terminal row $E$ seals only those displayed observations as Real-facing data.

Finally use $H,C,P,N$. Componentwise transport, continuation replay, provenance, and local naming preserve the same finite packet, so the excluded open-cover, arbitrary subcover, quotient-equality, choice, trichotomy, and ambient-completeness principles cannot enter the local NameCert surface.
\end{proof}

\begin{theorem}[BishopCompactIntervalFanModulus finite-cover route]
\label{thm:bishop-compact-interval-fan-modulus-finite-cover-route}
Every downstream consumer of an accepted $\BishopCompactIntervalFanModulusUp$ packet receives the finite route
$$
\CompactIntervalFanBridgeUp,\ \CompactIntervalFanModulusSelectorUp,\ \DyadicIntervalNetUp
\longmapsto
\StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
Consequently the consumer may read a displayed finite fan depth and its dyadic interval-net witnesses, but it receives no theorem of open-cover compactness, no selected arbitrary subcover, no quotient real equality, no countable-choice schedule, no order trichotomy, and no ambient completeness principle for $\RealUp$.
\end{theorem}
\begin{proof}
Start with the bridge and selector rows. The row $B$ supplies the compact interval fan bridge, and $S$ records the finite modulus depth attached to the displayed request.

Use the dyadic interval-net row $D$. It exposes the concrete interval cells available at that depth, so a consumer sees finite coverage data rather than an arbitrary family of opens or a selected subcover.

Route every Real-facing read through $W,R,E$ and preserve the packet through $H,C,P,N$. Since the carrier contains only those finite rows, the public route is exactly the displayed finite means route and not an ambient compactness principle.
\end{proof}

\closureat{BishopCompactIntervalFanModulusUp}{seedStr}

\begin{closurestatus}{\BishopCompactIntervalFanModulusUp}
  \constructivestory{A $\BishopCompactIntervalFanModulusUp$ packet is generated from compact fan bridge rows, a finite modulus selector, dyadic interval-net cells, StreamName windows, RegSeqRat readback, a Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-compact-interval-fan-modulus-carrier}, \autoref{thm:bishop-compact-interval-fan-modulus-namecert-obligations}, and \autoref{thm:bishop-compact-interval-fan-modulus-finite-cover-route} seed the finite fan-modulus route over compact fan bridge, modulus selector, dyadic interval net, StreamName, RegSeqRat, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected arbitrary subcovers, quotient real equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, fan bridge admission, finite modulus-depth selection, dyadic interval-net exposure, Real seal nonescape, and structural nonescape for BEDC.Derived.BishopCompactIntervalFanModulusUp.}
\end{closurestatus}
