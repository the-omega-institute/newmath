\chapter{A Concrete Naming Certificate for $\FundamentalTheoremCalculusUp$}
\label{ch:concrete-instances-fundamental-theorem-calculus-namecert}
\origin{human}

Following Newton, Leibniz, and Bishop--Bridges constructive analysis, the $\FundamentalTheoremCalculusUp$ packet records the finite derivative-integral handoff by which a displayed derivative window and a displayed Riemann integral row are read as one theorem-facing route. It sits after the derivative carrier of \autoref{ch:concrete-instances-derivative-namecert}, the Riemann-integral row of \autoref{ch:concrete-instances-riemannintegral-namecert}, the Darboux comparison surface of \autoref{ch:concrete-instances-darboux-sum-namecert}, the tagged-partition schedule of \autoref{ch:concrete-instances-tagged-real-partition-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The certificate names only the displayed constructive route from derivative control to integral readback; it does not import a classical continuum theorem, select an antiderivative, identify quotient functions, or assert ambient completeness.

\begin{definition}[Fundamental theorem calculus carrier]
\label{def:fundamental-theorem-calculus-carrier}
A $\FundamentalTheoremCalculusUp$ carrier is a finite $\BHist$ packet
$$
F=(D,I,A,T,Q,S,G,R,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $D$ is the $\DerivativeUp$ window row, naming the displayed function, interval window, derivative witness, and local difference-quotient budget.
\item $I$ is the $\RiemannIntegralUp$ row through which the matching integral expression is inspected.
\item $A$ is the $\DarbouxSumUp$ and refinement ledger, recording upper and lower comparison rows and the mesh refinement relation used by the integral readback.
\item $T$ is the $\TaggedRealPartitionUp$ row, exposing the ordered tags, cells, endpoint windows, and compatibility with the same interval read by $D$ and $I$.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance budget for derivative windows, partition mesh, Darboux spread, integral readback, and endpoint comparison.
\item $S$ is the $\StreamNameUp$ finite-window row for the endpoint values, derivative samples, tagged cells, and integral approximants.
\item $G$ is the $\RegSeqRatUp$ readback row for the regular rational sequences exposed by $S$.
\item $R$ is the terminal $\RealUp$ seal for the Real-facing derivative-integral conclusion.
\item $H$ records componentwise $\hsame$ transport for derivative, integral, Darboux, tagged-partition, dyadic, stream, regular-rational, Real, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
D,I,A,T,Q \longmapsto S \longmapsto G \longmapsto R \longmapsto \NameCert_{\FundamentalTheoremCalculusUp}.
$$
\item $P$ is the $\Pkg$ provenance over $\FundamentalTheoremCalculusUp$, $\DerivativeUp$, $\RiemannIntegralUp$, $\DarbouxSumUp$, $\TaggedRealPartitionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FundamentalTheoremCalculusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,I,A,T,Q,S,G,R,H,C,P,N$. It has no coordinate for ambient Real completeness, measure-theoretic integration, host function extensionality, selected antiderivatives, quotient equality of functions, or bridge checking.
\end{definition}

\begin{theorem}[Fundamental theorem calculus NameCert obligations]
\label{thm:fundamental-theorem-calculus-namecert-obligations}
For every accepted $\FundamentalTheoremCalculusUp$ carrier
$$
F=(D,I,A,T,Q,S,G,R,H,C,P,N),
$$
the local $\NameCert_{\FundamentalTheoremCalculusUp}$ surface consists exactly of carrier admission for $F$, derivative-window exposure through $D$, Riemann-integral exposure through $I$, Darboux and refinement accountability through $A$, tagged-partition visibility through $T$, dyadic tolerance accounting through $Q$, StreamName window exposure through $S$, RegSeqRat readback through $G$, terminal Real sealing through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fundamental-theorem-calculus-carrier}. The analytic rows $D,I,A,T,Q$ are the only source of derivative control, integral inspection, Darboux comparison, tagged partition data, and tolerance budgets.

The readback rows are ordered. The finite StreamName row $S$ exposes the visible endpoint, derivative, cell, and integral windows before $G$ reads them as regular rational data and $R$ seals the Real-facing conclusion.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly this displayed route. Since no carrier coordinate stores ambient Real completeness, measure-theoretic integration, host function extensionality, selected antiderivatives, quotient equality, or bridge checking, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Fundamental theorem calculus Riemann derivative route]
\label{thm:fundamental-theorem-calculus-riemann-derivative-route}
Every accepted $\FundamentalTheoremCalculusUp$ theorem read that hands a derivative window to a Riemann-integral conclusion factors through the finite route
$$
\DerivativeUp \longmapsto \RiemannIntegralUp \longmapsto \DarbouxSumUp \longmapsto \TaggedRealPartitionUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
as instantiated by $D,I,A,T,Q,S,G,R$. The Real-facing conclusion receives derivative-window data, Riemann integral data, Darboux and refinement accountability, tagged partition visibility, dyadic tolerance budgets, finite stream windows, regular rational readback, terminal Real sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row; it receives no ambient Real completeness theorem, measure-theoretic integration object, host function extensionality principle, selected antiderivative, quotient equality, or checked bridge.
\end{theorem}
\begin{proof}
Start with the accepted carrier $F=(D,I,A,T,Q,S,G,R,H,C,P,N)$. The rows $D$ and $I$ display the derivative window and the matching Riemann-integral row before any theorem-facing conclusion is read.

Use the partition and comparison route next. The Darboux ledger $A$, tagged partition row $T$, and dyadic budget $Q$ expose the finite mesh and tolerance data needed by the integral readback.

Finally pass through the completion-facing rows. The stream, regular-rational, and Real rows $S,G,R$ are read in that order, and $H,C,P,N$ preserve the same displayed path. Since the carrier has no coordinate for ambient completeness, measure-theoretic integration, host extensionality, selected antiderivatives, quotient equality, or bridge checking, the route is exhausted by the finite BEDC packet.
\end{proof}

\falsifiablePrediction{Every $\FundamentalTheoremCalculusUp$ theorem-facing read factors through one derivative-window row, one Riemann-integral row, one Darboux/refinement ledger, one tagged-partition row, one dyadic tolerance budget, one StreamName finite-window row, one RegSeqRat readback row, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{derivative, riemannintegral, darboux-sum, tagged-real-partition, dyadicratcore, streamname, regseqrat, real: $\FundamentalTheoremCalculusUp$ is not bijective to any listed sibling because it jointly carries the derivative window, integral row, Darboux comparison, tagged partition schedule, dyadic tolerance budget, finite observations, regular rational readback, terminal Real sealing, transport, replay, provenance, and local naming as one certificate.}

\closureat{FundamentalTheoremCalculusUp}{seedStr}

\begin{closurestatus}{\FundamentalTheoremCalculusUp}
  \constructivestory{A $\FundamentalTheoremCalculusUp$ packet is generated from a DerivativeUp window row, a RiemannIntegralUp row, a DarbouxSum refinement ledger, a TaggedRealPartition row, DyadicRatCore tolerance budgets, StreamName windows, RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fundamental-theorem-calculus-carrier}, \autoref{thm:fundamental-theorem-calculus-namecert-obligations}, and \autoref{thm:fundamental-theorem-calculus-riemann-derivative-route} seed the finite derivative-integral route over derivative, Riemann-integral, Darboux, tagged-partition, dyadic, stream-name, regular rational, and Real rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.FundamentalTheoremCalculusUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient $\RealUp$ completeness, measure-theoretic integration, host function extensionality, selected antiderivatives, quotient equality, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative-window exposure, Riemann-integral exposure, Darboux/refinement exactness, tagged-partition visibility, dyadic tolerance accounting, readback routing, Real sealing, provenance, and local naming for BEDC.Derived.FundamentalTheoremCalculusUp.}
\end{closurestatus}
