\chapter{A Concrete Naming Certificate for $\ComputablePolishSpaceUp$}
\label{ch:concrete-instances-computable-polish-space-namecert}
\origin{human}

Following Weihrauch and Alexandrov--Urysohn, the $\ComputablePolishSpaceUp$ packet records the complete separable metric object whose dense names, Cauchy realizers, representation decoder, and terminal real seal are all visible before any computable-analysis consumer reads the space. It sits after the Polish-space boundary of \autoref{ch:concrete-instances-polishspace-namecert}, the effective complete-separable route of \autoref{ch:concrete-instances-effective-polish-space-namecert}, and the represented-space interface of \autoref{ch:concrete-instances-representedspace-namecert}. The packet also consumes the Cauchy-representation surface of \autoref{ch:concrete-instances-cauchy-representation-namecert}, the explicit-modulus Cauchy route of \autoref{ch:concrete-instances-effective-cauchy-sequence-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It is a BEDC finite naming surface for computable Polish data, not a quotient of names, a selected dense family, host equality, countable choice, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Computable Polish space carrier]\label{def:computable-polish-space-carrier}
A $\ComputablePolishSpaceUp$ carrier is a finite $\BHist$ packet
$$
C=(P,E,R,A,Q,S,G,D,L,H,K,U,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\PolishSpaceUp$ row, carrying the complete separable metric boundary read by ordinary Polish-space consumers.
\item $E$ is the $\EffectivePolishSpaceUp$ row, exposing the indexed dense family, explicit realizer route, and effective complete-separable metric data.
\item $R$ is the $\RepresentedSpaceUp$ row, presenting the finite name interface through which computable names reach target objects.
\item $A$ is the $\CauchyRepresentationUp$ decoder row, routing stream observations through regular rational readback, dyadic tolerances, and the represented-space boundary.
\item $Q$ is the $\EffectiveCauchySequenceUp$ row, carrying the explicit-modulus Cauchy sequence data used by the realizer route.
\item $S$ is the $\StreamNameUp$ finite-window schedule shared by the representation decoder and the effective Cauchy route.
\item $G$ is the $\RegSeqRatUp$ readback row obtained from the displayed stream windows and Cauchy stages.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used for the decoder, regular readback, effective Cauchy modulus, and Real seal thresholds.
\item $L$ is the terminal $\RealUp$ seal row reached only after $A,Q,S,G,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the Polish, effective Polish, represented-space, Cauchy-representation, effective Cauchy, stream-window, regular-readback, dyadic-ledger, Real-seal, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a computable-name request through $R,A,S,Q,G,D,L$ and back to the Polish and effective-Polish rows $P,E$.
\item $U$ is the $\Pkg$ provenance over $\ComputablePolishSpaceUp$, $\PolishSpaceUp$, $\EffectivePolishSpaceUp$, $\RepresentedSpaceUp$, $\CauchyRepresentationUp$, $\EffectiveCauchySequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ComputablePolishSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only by transporting the displayed rows to a common complete-separable, represented, Cauchy-realizer, stream-window, regular-readback, dyadic-tolerance, and Real-seal surface. It has no coordinate for quotient names, selected dense points, host equality of represented objects, countable choice, trichotomy, open-cover compactness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Computable Polish space NameCert obligations]\label{thm:computable-polish-space-namecert-obligations}
For every accepted $\ComputablePolishSpaceUp$ carrier $C=(P,E,R,A,Q,S,G,D,L,H,K,U,N)$, the local $\NameCert_{\ComputablePolishSpaceUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Polish, effective Polish, represented-space, Cauchy-representation, effective Cauchy, StreamName, RegSeqRat, DyadicRatCore, Real-seal, transport, replay, provenance, and local naming rows;
\item complete-separable compatibility, requiring $P$ and $E$ to expose the same complete separable metric object through the displayed dense-realizer route;
\item representation exactness, requiring every computable name to pass through the represented-space row $R$ and Cauchy-representation decoder $A$ before a target-facing read is available;
\item effective Cauchy exactness, requiring the explicit row $Q$ and the finite stream windows $S$ to precede the regular rational readback $G$, dyadic ledger $D$, and Real seal $L$;
\item componentwise transport, continuation replay, package provenance, and local naming through $H,K,U,N$ for exactly the displayed rows;
\item ledger nonescape, excluding quotient names, selected dense points, host equality, countable choice, trichotomy, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, and bridge checking as local certificate data.
\end{enumerate}
No computable-Polish-space obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:computable-polish-space-carrier}. The projection exposes the Polish row $P$, effective Polish row $E$, represented-space row $R$, Cauchy-representation decoder $A$, effective Cauchy row $Q$, stream windows $S$, regular readback $G$, dyadic ledger $D$, Real seal $L$, and structural rows $H,K,U,N$.

The complete-separable compatibility row reads the sibling surfaces of \autoref{ch:concrete-instances-polishspace-namecert} and \autoref{ch:concrete-instances-effective-polish-space-namecert}. Since $P$ and $E$ occur in the same finite packet, a consumer cannot replace the displayed dense-realizer route by a second dense family or a hidden completion equality.

The representation route is then forced through \autoref{ch:concrete-instances-representedspace-namecert} and \autoref{ch:concrete-instances-cauchy-representation-namecert}. A computable name first exposes $R$ and $A$, then reads the finite stream windows rather than an unscheduled name oracle.

The effective Cauchy route uses \autoref{ch:concrete-instances-effective-cauchy-sequence-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. These rows place $Q,S,G,D$ before $L$, so the Real seal is downstream of the explicit modulus and tolerance ledger.

Finally, $H,K,U,N$ transport, replay, source, and name only that displayed packet. They introduce no quotient names, selected dense points, host equality, countable choice, trichotomy, open-cover compactness, or ambient Real completeness, and the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Computable Polish space realizer handoff]\label{thm:computable-polish-space-realizer-handoff}
Every represented-space, Polish-space, or Real-facing consumer of an accepted $\ComputablePolishSpaceUp$ carrier $C=(P,E,R,A,Q,S,G,D,L,H,K,U,N)$ factors through the displayed route
$$
R,A \longmapsto S \longmapsto Q \longmapsto G \longmapsto D \longmapsto L \longmapsto P,E,H,K,U,N .
$$
Thus the consumer receives the represented-space name interface, Cauchy-representation decoder, finite StreamName windows, EffectiveCauchySequence modulus route, RegSeqRat readback, DyadicRatCore tolerance ledger, Real seal, Polish row, effective Polish row, transport, replay, provenance, and local naming before it can inspect the completed computable Polish object.
\end{theorem}
\begin{proof}
Begin with a represented-space, Polish-space, or Real-facing read of the carrier. The only name-facing rows are $R$ and $A$, so the read must expose the represented-space interface and the Cauchy-representation decoder before any target row can be consumed.

Next replay the finite observation route. The row $S$ supplies the inspected stream windows, while the effective Cauchy row $Q$ supplies the explicit modulus route attached to those windows.

The regular and Real-facing handoff is then fixed by $G,D,L$. The regular rational readback $G$ is read from the displayed windows and effective Cauchy stages; the dyadic ledger $D$ records the tolerance data; and the Real seal $L$ is terminal for that route.

Only after this handoff can the Polish and effective Polish rows $P,E$ be read as a completed computable Polish object. The structural rows $H,K,U,N$ preserve and name the same route, so no consumer can bypass it through quotient names, selected dense points, host equality, countable choice, trichotomy, open-cover compactness, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\ComputablePolishSpaceUp$ represented-space, Polish-space, or Real-facing read factors through a represented-space row, CauchyRepresentation decoder, StreamName finite windows, EffectiveCauchySequence modulus route, RegSeqRat readback, DyadicRatCore tolerance ledger, RealUp seal, PolishSpace row, EffectivePolishSpace row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{polishspace, effective-polish-space, representedspace, cauchy-representation, effective-cauchy-sequence, streamname, regseqrat, dyadicratcore, real: $\ComputablePolishSpaceUp$ is not bijective to any listed sibling because it jointly carries all of their displayed rows as one computable complete-separable name interface.}

\closureat{ComputablePolishSpaceUp}{seedStr}
\begin{closurestatus}{\ComputablePolishSpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:computable-polish-space-carrier}, \autoref{thm:computable-polish-space-namecert-obligations}, and \autoref{thm:computable-polish-space-realizer-handoff} seed the finite computable Polish route over PolishSpaceUp, EffectivePolishSpaceUp, RepresentedSpaceUp, CauchyRepresentationUp, EffectiveCauchySequenceUp, StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient names, selected dense points, host equality, countable choice, trichotomy, open-cover compactness, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, complete-separable compatibility, representation exactness, effective Cauchy exactness, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.ComputablePolishSpaceUp.}
  \constructivestory{A $\ComputablePolishSpaceUp$ packet is generated from PolishSpace, EffectivePolishSpace, RepresentedSpace, CauchyRepresentation, EffectiveCauchySequence, StreamName, RegSeqRat, DyadicRatCore, RealUp, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
