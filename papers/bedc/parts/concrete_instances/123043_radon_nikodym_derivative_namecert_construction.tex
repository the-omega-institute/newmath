\chapter{A Concrete Naming Certificate for $\RadonNikodymDerivativeUp$}
\label{ch:concrete-instances-radon-nikodym-derivative-namecert}
\origin{human}

Following Radon, Nikodym, and Halmos on derivatives of measures, the $\RadonNikodymDerivativeUp$ packet records the density row consumed after the theorem-level route of \autoref{ch:concrete-instances-radon-nikodym-namecert} has exposed absolute continuity. It sits beside the measure carrier of \autoref{ch:concrete-instances-measure-namecert}, the integral readback surface of \autoref{ch:concrete-instances-integral-namecert}, the probability-source surface of \autoref{ch:concrete-instances-probspace-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names derivative-density data as finite BEDC rows; it is not a host almost-everywhere quotient, completed measure algebra, unrestricted choice of density representative, or ambient integration theorem.

\begin{definition}[Radon--Nikodym derivative carrier]
\label{def:radon-nikodym-derivative-carrier}
A $\RadonNikodymDerivativeUp$ carrier is a finite $\BHist$ packet
$$
D=(M_{\mathrm{base}},M_{\mathrm{ac}},R,W,I,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M_{\mathrm{base}}$ is the base $\MeasureUp$ row whose measurable-event family and null-set reads are visible before any density is consumed.
\item $M_{\mathrm{ac}}$ is the absolutely-continuous $\MeasureUp$ row read over the same displayed event family.
\item $R$ is the density row, storing the nonnegative measurable integrand presented as the derivative candidate.
\item $W$ is the finite simple-function window: the inspected simple approximants, event windows, and tolerance requests through which $R$ is read.
\item $I$ is the $\IntegralUp$ readback row assigning the displayed density integral on each inspected event window.
\item $Z$ is the null-set boundary row, recording that base-null event reads are routed to zero target-measure and zero integral readbacks before any derivative-density conclusion is exported.
\item $H$ records componentwise $\hsame$ transport for the base measure, absolutely-continuous measure, density, finite window, integral readback, null boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from $M_{\mathrm{base}},M_{\mathrm{ac}}$ through $R,W,I,Z,H$.
\item $P$ is the $\Pkg$ provenance over $\RadonNikodymDerivativeUp$, $\RadonNikodymUp$, $\MeasureUp$, $\IntegralUp$, $\ProbSpaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RadonNikodymDerivativeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for host almost-everywhere quotient equality, completed measure algebras, unrestricted density-representative choice, ambient integration theorems, or hidden probability spaces.
\end{definition}
\leanchecked{BEDC.Derived.RadonNikodymDerivativeUp.RadonNikodymDerivativeTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Radon--Nikodym derivative NameCert obligations]
\label{thm:radon-nikodym-derivative-namecert-obligations}
For every accepted $\RadonNikodymDerivativeUp$ carrier
$$
D=(M_{\mathrm{base}},M_{\mathrm{ac}},R,W,I,Z,H,C,P,N),
$$
the local $\NameCert_{\RadonNikodymDerivativeUp}$ surface consists exactly of base-measure exposure, absolutely-continuous measure exposure, density-row admission, finite simple-function window control, integral readback, null-set boundary, componentwise transport, displayed replay, provenance, and local naming. The derivative-density consumer must therefore read \autoref{ch:concrete-instances-radon-nikodym-namecert}, \autoref{ch:concrete-instances-measure-namecert}, \autoref{ch:concrete-instances-integral-namecert}, \autoref{ch:concrete-instances-probspace-namecert}, and \autoref{ch:concrete-instances-real-namecert} as sibling dependency surfaces before using the density row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:radon-nikodym-derivative-carrier}. The two measure rows, density row, finite window, integral readback, null boundary, and structural rows are the complete local inventory.

The measure side is read first. The base row exposes the event family and null-set reads, while the absolutely-continuous row stays attached to the same event family through the Radon--Nikodym route.

The density side is finite. The row $R$ supplies the derivative candidate, $W$ lists the inspected simple-function windows and tolerance requests, and $I$ reads their integrals through the public integral surface.

Finally $Z,H,C,P,N$ bind the null-set boundary, transport, replay, provenance, and local naming. Since the carrier has no row for host almost-everywhere quotients, completed measure algebras, unrestricted representatives, ambient integration theorems, or hidden probability spaces, the listed obligations exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Radon--Nikodym derivative density-ledger route]
\label{thm:radon-nikodym-derivative-density-ledger-route}
Every downstream $\MeasureUp$, $\IntegralUp$, $\ProbSpaceUp$, or $\RealUp$ consumer of an accepted $\RadonNikodymDerivativeUp$ carrier must read the finite density ledger
$$
M_{\mathrm{base}},M_{\mathrm{ac}} \longmapsto R \longmapsto W \longmapsto I \longmapsto Z \longmapsto H,C,P,N .
$$
Thus derivative use is mediated by displayed base and absolutely-continuous measure rows, a density row, finite simple-function windows, integral readback, null-set boundary, transport, replay, provenance, and local naming. No consumer may replace that ledger by host almost-everywhere quotient equality, completed measure algebra, unrestricted representative selection, or an ambient integration theorem.
\end{theorem}
\leanchecked{BEDC.Derived.RadonNikodymDerivativeUp.RadonNikodymDerivativePacket\_density\_ledger\_route}
\begin{proof}
Begin with the two measure rows. A derivative-density read must keep the base measure and the absolutely-continuous measure over the same displayed event family.

Next read the density ledger. The candidate row $R$ is inspected only through the finite simple-function window $W$, and the resulting event-window values are read through the integral row $I$.

The null-set boundary is then applied. The row $Z$ records that base-null requests are consumed as boundary cases of the same density ledger, not as an external quotient of representatives.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly this route. Since no additional row is present, the consumer receives the finite density-ledger route and none of the excluded host measure-theoretic objects.
\end{proof}

\falsifiablePrediction{Every accepted $\RadonNikodymDerivativeUp$ density read that bypasses the base measure, absolutely-continuous measure, density row, finite simple-function window, integral readback, null-set boundary, transport, replay, provenance, or local naming rows refutes the carrier surface.}

\independenceWitness{radon-nikodym, measure, integral, probspace, real: $\RadonNikodymDerivativeUp$ is not bijective to any listed sibling because it jointly carries the derivative density row, finite simple-function window, integral readback, null-set boundary, and density-ledger provenance in one packet.}

\closureat{RadonNikodymDerivativeUp}{seedStr}

\begin{closurestatus}{\RadonNikodymDerivativeUp}
  \constructivestory{A $\RadonNikodymDerivativeUp$ packet is generated from a base Measure row, an absolutely-continuous Measure row, a displayed density row, finite simple-function windows, Integral readback, null-set boundary rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:radon-nikodym-derivative-carrier}, \autoref{thm:radon-nikodym-derivative-namecert-obligations}, and \autoref{thm:radon-nikodym-derivative-density-ledger-route} seed the finite derivative-density ledger from measure rows to integral, probability, and Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter exports no host almost-everywhere quotient equality, completed measure algebra, unrestricted choice of density representative, ambient integration theorem, hidden probability space, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, density-row admission, finite simple-function window control, integral readback exactness, null-set boundary stability, transport, replay, provenance, and local naming for BEDC.Derived.RadonNikodymDerivativeUp.}
\end{closurestatus}
