\chapter{A Concrete Naming Certificate for $\MeirKeelerContractionUp$}
\label{ch:concrete-instances-meir-keeler-contraction-namecert}
\origin{human}

Following Meir and Keeler's fixed point theorem and Bishop--Bridges constructive metric analysis, the $\MeirKeelerContractionUp$ packet records the finite epsilon-to-delta contraction ledger that sits between ordinary contraction data and completion-facing fixed-point consumers. It reads the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the fixed-point route of \autoref{ch:concrete-instances-contraction-principle-namecert}, the iteration ledger of \autoref{ch:concrete-instances-fixed-point-iteration-namecert}, the endpoint-admission surface of \autoref{ch:concrete-instances-completemetric-namecert}, and the Banach fixed-point packet of \autoref{ch:concrete-instances-banach-fixed-point-namecert}. It also sits beside the Lipschitz and nonexpansive surfaces of \autoref{ch:concrete-instances-lipschitzmap-namecert}, \autoref{ch:concrete-instances-lipschitz-constant-namecert}, and \autoref{ch:concrete-instances-regular-cauchy-nonexpansive-map-namecert}, while refusing to replace the Meir--Keeler ledger by a single host Lipschitz constant.

\begin{definition}[Meir--Keeler contraction carrier]
\label{def:meir-keeler-contraction-carrier}
A $\MeirKeelerContractionUp$ carrier is a finite $\BHist$ packet
$$
M=(X,d,T,E,I,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the accepted $\MetricUp$ carrier row for the source points.
\item $d$ is the metric-distance readback row used by all comparison requests.
\item $T$ is the self-map graph row for the candidate contraction.
\item $E$ is the Meir--Keeler epsilon-to-delta contraction ledger: for each displayed positive tolerance request it stores a displayed response window forcing a strictly smaller image distance whenever the source distance lies in the named annulus.
\item $I$ is the fixed-point iteration row, recording the finite Picard-style orbit segments and adjacent comparison reads used by downstream consumers.
\item $L$ is the Cauchy handoff ledger, listing the finite tail requests that the Meir--Keeler row transfers to the complete-metric endpoint surface.
\item $H$ records componentwise $\hsame$ transport for metric rows, distance reads, self-map graph rows, epsilon-to-delta ledger rows, iteration rows, Cauchy handoff rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a contraction request through $X,d,T,E,I,L,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MeirKeelerContractionUp$, $\MetricUp$, $\ContractionPrincipleUp$, $\FixedPointIterationUp$, $\CompleteMetricUp$, $\BanachFixedPointUp$, $\LipschitzMapUp$, $\RegularCauchyNonexpansiveMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MeirKeelerContractionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $X,d,T,E,I,L,H,C,P,N$. It has no coordinate for a global Lipschitz constant, a selected endpoint, quotient completion equality, host function extensionality, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.MeirKeelerContractionUp.MeirKeelerContractionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Meir--Keeler contraction NameCert obligations]
\label{thm:meir-keeler-contraction-namecert-obligations}
For an accepted $\MeirKeelerContractionUp$ carrier $M=(X,d,T,E,I,L,H,C,P,N)$, the local $\NameCert_{\MeirKeelerContractionUp}$ surface consists exactly of metric-carrier admission, distance-readback visibility, self-map graph admission, epsilon-to-delta ledger exactness, fixed-point iteration visibility, Cauchy handoff visibility, componentwise transport, displayed replay, provenance, and local naming. These obligations bind the Meir--Keeler route without adding a global contraction constant, endpoint selection, quotient completion equality, host fixed-point equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:meir-keeler-contraction-carrier}. The displayed rows $X,d,T,E,I,L,H,C,P,N$ are the whole local surface, so every obligation must be one of their admissions, transports, or replay checks.

The contraction content is carried by $T$ and $E$. The self-map row fixes the map being iterated, while the epsilon-to-delta row supplies the strict Meir--Keeler comparison at each displayed tolerance window. No single Lipschitz bound is extracted from this row.

The iteration and completion-facing content is carried by $I$ and $L$. These rows expose finite orbit segments and finite tail requests before any complete-metric endpoint can be consumed by another chapter.

Finally $H,C,P,N$ keep the same rows stable under classifier transport, replay, provenance, and local naming. Since the excluded host objects are not carrier coordinates, the NameCert surface cannot export them.
\end{proof}

\begin{theorem}[Meir--Keeler contraction Cauchy handoff]
\label{thm:meir-keeler-contraction-cauchy-handoff}
Every fixed-point or completion-facing read of an accepted $\MeirKeelerContractionUp$ carrier factors through
$$
\MetricUp,\ d,\ T,\ E,\ \FixedPointIterationUp,\ L,\ \CompleteMetricUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The read uses the Meir--Keeler epsilon-to-delta ledger to feed finite iteration-tail requests into the complete-metric endpoint surface. It never exposes a selected limit, a quotient representative, a host global Lipschitz constant, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the metric and map rows. A consumer first reads $X,d,T$, so the source carrier, comparison relation, and self-map graph are fixed before the Meir--Keeler ledger is inspected.

Next consume $E$. The epsilon-to-delta ledger answers each displayed tolerance request by a finite annulus response, which is the contraction data available to the iteration route. This is weaker than a global Lipschitz constant but strong enough for the displayed tail comparisons.

Then read $I$ and $L$. The iteration row supplies the finite orbit comparisons, and the Cauchy handoff row packages the resulting tail requests for the $\CompleteMetricUp$ endpoint surface.

The structural rows $H,C,P,N$ transport and replay the same route. Because no endpoint selector, quotient equality, host Lipschitz constant, or ambient completeness row occurs in the carrier, every downstream read remains inside the finite handoff just described.
\end{proof}

\falsifiablePrediction{Every accepted $\MeirKeelerContractionUp$ read factors through one MetricUp carrier row, one distance readback row, one self-map row, one Meir--Keeler epsilon-to-delta ledger, one fixed-point iteration row, one Cauchy handoff ledger, hsame transport, Cont replay, Pkg provenance, and local NameCert row before any completion-facing fixed-point consumer can read it.}

\independenceWitness{metric, contraction-principle, fixed-point-iteration, completemetric, banach-fixed-point, lipschitzmap, regular-cauchy-nonexpansive-map: none is bijective with $\MeirKeelerContractionUp$ because the packet jointly stores the metric carrier, self-map graph, epsilon-to-delta contraction ledger, finite iteration route, Cauchy handoff, transport, replay, provenance, and local naming.}

\closureat{MeirKeelerContractionUp}{seedStr}

\begin{closurestatus}{\MeirKeelerContractionUp}
  \constructivestory{A $\MeirKeelerContractionUp$ packet is generated from a MetricUp carrier, distance readback, self-map graph, Meir--Keeler epsilon-to-delta contraction ledger, fixed-point iteration row, Cauchy handoff ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:meir-keeler-contraction-carrier}, \autoref{thm:meir-keeler-contraction-namecert-obligations}, and \autoref{thm:meir-keeler-contraction-cauchy-handoff} seed the finite Meir--Keeler contraction packet and its completion-facing Cauchy handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a global Lipschitz constant, selected endpoint, quotient completion equality, host fixed-point equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, epsilon-to-delta ledger exactness, fixed-point iteration visibility, Cauchy handoff exactness, transport, replay, provenance, and local naming for BEDC.Derived.MeirKeelerContractionUp.}
\end{closurestatus}
