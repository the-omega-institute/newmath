\chapter{A Concrete Naming Certificate for $\DyadicIntervalNetCompactnessUp$}
\label{ch:concrete-instances-dyadic-interval-net-compactness-namecert}
\origin{human}

Following Bishop and Bridges on compact intervals and finite dyadic covers, the $\DyadicIntervalNetCompactnessUp$ packet records the finite interval-net compactness route as displayed BEDC data. It sits over the dyadic interval-net carrier of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names finite subdivision-depth coverage and downstream finite-cover handoff only; it does not assert open-cover compactness, selected subcovers, host real equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Dyadic interval-net compactness carrier]
\label{def:dyadic-interval-net-compactness-carrier}
A $\DyadicIntervalNetCompactnessUp$ carrier is a finite $\BHist$ packet
$$
K=(I,D,J,E,S,R,Q,H,C,P,N)
$$
whose rows are read in the following order. The row $I$ is an accepted $\DyadicIntervalNetUp$ carrier. The rows $D,J,E$ are the $\DyadicRatCoreUp$ endpoint arithmetic, finite cell list, and endpoint-incidence data displayed by that net. The row $S$ is the $\StreamNameUp$ endpoint-window row, $R$ is the $\RegSeqRatUp$ tolerance readback row, and $Q$ is the terminal $\RealUp$ seal. The structural rows $H,C,P,N$ record componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance over the named packets, and the local $\NameCert_{\DyadicIntervalNetCompactnessUp}$ row.

The classifier compares accepted compactness packets only through $I,D,J,E,S,R,Q,H,C,P,N$. It has no coordinate for arbitrary open covers, selected finite subcovers, host interval compactness, quotient stream equality, countable choice, trichotomy, or ambient real completeness.
\end{definition}

\begin{theorem}[Dyadic interval-net compactness obligations]
\label{thm:dyadic-interval-net-compactness-obligations}
For every accepted $\DyadicIntervalNetCompactnessUp$ carrier
$$
K=(I,D,J,E,S,R,Q,H,C,P,N),
$$
the local $\NameCert_{\DyadicIntervalNetCompactnessUp}$ obligation surface is exhausted by the accepted $\DyadicIntervalNetUp$ source row, dyadic endpoint arithmetic, finite cell coverage, endpoint incidence, StreamName endpoint windows, RegSeqRat tolerance readback, terminal Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A consumer may read a finite interval-net compactness conclusion only after those rows are displayed in that order.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-interval-net-compactness-carrier}. The compactness-facing data are the interval-net source $I$ together with its dyadic endpoint, finite cell, and endpoint-incidence rows $D,J,E$.

Use the obligation surface of \autoref{thm:dyadic-interval-net-obligation-surface} and the finite-chain coverage route of \autoref{thm:dyadic-interval-net-finite-chain-coverage}. They require every finite cover read to inspect the listed cells and endpoint arithmetic before StreamName, RegSeqRat, or Real-facing rows are available.

Finally read $S,R,Q,H,C,P,N$ as the downstream seal and structural rows. Since the carrier has no row for open covers, selected subcovers, quotient equality, choice, trichotomy, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Dyadic interval-net finite-cover route]
\label{thm:dyadic-interval-net-compactness-finite-cover-route}
Every compact-interval or uniform-modulus consumer that reads finite-cover compactness from an accepted $\DyadicIntervalNetCompactnessUp$ packet factors through
$$
I,D,J,E \longmapsto S,R \longmapsto Q,H,C,P,N .
$$
The read first exposes the $\DyadicIntervalNetUp$ finite cell route, then the $\DyadicRatCoreUp$ endpoint arithmetic and endpoint incidence, then the $\StreamNameUp$ and $\RegSeqRatUp$ readback rows, and only then the $\RealUp$ seal and structural packet rows. The route exports a finite interval-net cover handoff, not open-cover compactness or an ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the source net row $I$. By \autoref{thm:dyadic-interval-net-cover-refinement}, any finite cover request is refined into displayed dyadic cells whose endpoint and incidence data are read through $D,J,E$.

Apply subdivision-depth coverage from \autoref{thm:dyadic-interval-net-subdivision-depth-coverage}. The finite refinement depth keeps each refined cell inside the same endpoint arithmetic, cell list, endpoint incidence, StreamName, RegSeqRat, and Real-seal route.

The compact-interval or uniform-modulus consumer therefore receives only the displayed finite route. Replaying $H,C,P,N$ preserves the same rows, so the finite-cover conclusion cannot be replaced by selected subcovers, quotient stream equality, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Dyadic interval-net compactness Real seal boundary]
\label{thm:dyadic-interval-net-compactness-real-seal-boundary}
For every accepted $\DyadicIntervalNetCompactnessUp$ carrier $K=(I,D,J,E,S,R,Q,H,C,P,N)$, the terminal $\RealUp$ seal row $Q$ may be read only after the finite interval-net route $I,D,J,E,S,R$ is displayed. Once $Q$ is reached, the rows $H,C,P,N$ preserve the same finite packet and do not create host real equality, open-cover compactness, selected subcovers, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier. The row $Q$ is downstream of the accepted interval-net source, dyadic endpoint arithmetic, finite cell list, endpoint incidence, StreamName windows, and RegSeqRat tolerance readback.

Apply the Real seal nonescape theorem \autoref{thm:dyadic-interval-net-real-seal-nonescape}. It prevents a Real-facing consumer from opening $Q$ before the finite dyadic route has been displayed.

Finally replay the compactness packet's structural rows. The componentwise transport, continuation replay, provenance, and local naming rows keep the same finite route visible, so the Real seal remains a boundary for finite-cover handoff rather than a completeness or host-equality endpoint.
\end{proof}

\closureat{DyadicIntervalNetCompactnessUp}{seedStr}

\begin{closurestatus}{\DyadicIntervalNetCompactnessUp}
  \constructivestory{A $\DyadicIntervalNetCompactnessUp$ packet is generated from an accepted dyadic interval-net source, finite subdivision-depth coverage, StreamName windows, RegSeqRat tolerance readback, Real seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-interval-net-compactness-carrier}, \autoref{thm:dyadic-interval-net-compactness-obligations}, \autoref{thm:dyadic-interval-net-compactness-finite-cover-route}, and \autoref{thm:dyadic-interval-net-compactness-real-seal-boundary} seed the finite interval-net compactness carrier and its finite-cover route over DyadicIntervalNet, DyadicRatCore, StreamName, RegSeqRat, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, host real equality, quotient stream equality, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite subdivision-depth coverage, finite-cover consumer routing, Real seal nonescape, transport stability, replay stability, provenance, and local naming for BEDC.Derived.DyadicIntervalNetCompactnessUp.}
\end{closurestatus}
