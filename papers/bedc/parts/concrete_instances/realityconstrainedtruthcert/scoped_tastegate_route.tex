\begin{theorem}[Reality-constrained TruthCert scoped TasteGate route]
\label{thm:reality-constrained-truth-cert-scoped-tastegate-route}
The scoped surface of $\RealityConstrainedTruthCertUp$ is the route through the carrier of \autoref{def:reality-constrained-truth-cert-carrier}, the field coverage of \autoref{thm:reality-constrained-truth-cert-field-coverage}, the sibling dependency of \autoref{thm:reality-constrained-truth-cert-sibling-dependency}, the root TasteGate obligation of \autoref{thm:reality-constrained-truth-cert-root-tastegate-obligation}, the root nontrivial readiness of \autoref{thm:reality-constrained-truth-cert-root-nontrivial-readiness}, the root field-faithful readiness of \autoref{thm:reality-constrained-truth-cert-root-field-faithful-readiness}, and the L10 source readiness of \autoref{thm:reality-constrained-truth-cert-l10-source-readiness}. It binds exactly $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and the sibling TruthCert rows consumed by the displayed packet.
\end{theorem}
\begin{proof}
Start with the carrier and field-coverage rows. They expose the source, signature, classifier, tower, stability, descent, invariant, ledger, failure, and local-name entries as the whole accepted TruthCert packet.

Apply sibling dependency and L10 source readiness. The open-fit, objectivity, explanation, physical-induction, tower-compression, and L10 source rows enter as displayed dependencies of that same packet, not as separate truth authorities.

Finally use the root TasteGate obligation together with root nontrivial and field-faithful readiness. These rows force the TasteGate read to distinguish packet presence at $S,\Sigma,K,T,U,D,I,L,F,N$ and to preserve the listed fields through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Hence the scoped surface is exactly the displayed route and has no hidden model--reality identity, observer privilege, unbounded confirmation, or off-packet truth export.
\end{proof}
