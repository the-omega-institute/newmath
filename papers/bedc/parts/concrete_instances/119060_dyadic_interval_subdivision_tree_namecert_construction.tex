\chapter{A Concrete Naming Certificate for $\DyadicIntervalSubdivisionTreeUp$}
\label{ch:concrete-instances-dyadic-interval-subdivision-tree-namecert}
\origin{human}

Bishop--Bridges constructive interval bisection and Moore--Kearfott--Cloud interval analysis both use finite subdivision trees before any completed interval limit is read. The $\DyadicIntervalSubdivisionTreeUp$ packet records that finite tree as NameCert data between the one-step dyadic refinement carrier of \autoref{ch:concrete-instances-dyadic-interval-refinement-namecert}, the endpoint arithmetic surface of \autoref{ch:concrete-instances-dyadic-interval-arithmetic-namecert}, the nested Cauchy interval consumer of \autoref{ch:concrete-instances-nested-dyadic-cauchy-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It is a finite L10 subdivision carrier, not a selected infinite branch, a quotient stream equality principle, trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Dyadic interval nested subdivision carrier]
\label{def:dyadic-interval-nested-subdivision-carrier}
A $\DyadicIntervalSubdivisionTreeUp$ carrier is a finite $\BHist$ packet
$$
T=(R,D,V,E,L,S,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the root $\DyadicIntervalUp$ row whose lower and upper dyadic endpoints are read through $\DyadicRatCoreUp$.
\item $D$ is the finite depth budget, naming the largest inspected level and the dyadic scale attached to each level.
\item $V$ is the finite child-incidence table, recording for every displayed internal node the two displayed child interval rows supplied by $\DyadicIntervalRefinementUp$.
\item $E$ is the endpoint-reuse ledger, identifying each child endpoint with the parent endpoints or the displayed midpoint row through $\DyadicIntervalArithmeticUp$.
\item $L$ is the retained-leaf coverage row, listing the leaves that remain available to downstream interval consumers after the finite subdivision.
\item $S$ is the $\StreamNameUp$ observation window, naming the finite node, endpoint, and leaf reads inspected by the tree consumer.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the scheduled dyadic endpoint observations.
\item $H$ records componentwise $\hsame$ transport for the root, depth, incidence, endpoint-reuse, leaf-coverage, window, readback, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay of every accepted subdivision read through
$$
R,D,V,E,L,S \longmapsto Q .
$$
\item $P$ is the $\Pkg$ provenance over $\DyadicIntervalSubdivisionTreeUp$, $\DyadicIntervalUp$, $\DyadicIntervalRefinementUp$, $\DyadicIntervalArithmeticUp$, $\NestedDyadicCauchyUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicIntervalSubdivisionTreeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,D,V,E,L,S,Q,H,C,P,N$. It has no coordinate for a selected infinite branch, quotient stream equality, trichotomy, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or a standard bridge.
\end{definition}

\begin{theorem}[Dyadic interval nested subdivision obligations]
\label{thm:dyadic-interval-nested-subdivision-obligations}
For every accepted $\DyadicIntervalSubdivisionTreeUp$ carrier
$$
T=(R,D,V,E,L,S,Q,H,C,P,N),
$$
the local NameCert surface consists exactly of carrier admission for the root dyadic interval, finite depth budget, child-incidence table, endpoint-reuse ledger, retained-leaf coverage, $\StreamNameUp$ observation window, $\RegSeqRatUp$ readback, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. Every downstream $\NestedDyadicCauchyUp$, $\RegSeqRatUp$, or $\RealUp$ consumer of the subdivision must expose those rows before it may inspect a leaf chain or readback boundary.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-interval-nested-subdivision-carrier}. The root row, finite depth budget, child-incidence table, endpoint-reuse ledger, retained leaves, stream windows, readback, and structural rows are exactly the displayed coordinates of the packet.

The one-step refinement route is supplied by \autoref{ch:concrete-instances-dyadic-interval-refinement-namecert}, while endpoint reuse and midpoint arithmetic are read through \autoref{ch:concrete-instances-dyadic-interval-arithmetic-namecert}. Thus each internal node is certified by finite dyadic interval rows rather than by a completed path.

The observation and readback rows $S,Q$ are the only route toward $\NestedDyadicCauchyUp$, $\RegSeqRatUp$, and $\RealUp$ consumers. Since $H,C,P,N$ only transport, replay, source, and name the same displayed rows, no selected infinite branch, quotient stream equality, trichotomy, countable-choice row, open-cover compactness proof, or ambient completeness theorem can be projected from the carrier.
\end{proof}

\closureat{DyadicIntervalSubdivisionTreeUp}{seedStr}

\begin{closurestatus}{\DyadicIntervalSubdivisionTreeUp}
  \constructivestory{A $\DyadicIntervalSubdivisionTreeUp$ packet is generated from a root DyadicInterval row, finite depth budget, child-incidence table, endpoint-reuse ledger, retained-leaf coverage row, StreamName observation window, RegSeqRat readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-interval-nested-subdivision-carrier} and \autoref{thm:dyadic-interval-nested-subdivision-obligations} seed the finite subdivision-tree carrier over dyadic interval roots, one-step refinements, endpoint arithmetic, retained leaves, stream windows, regular readback, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected infinite branches, quotient stream equality, trichotomy, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, child-incidence exactness, endpoint-reuse stability, retained-leaf coverage, finite StreamName exposure, RegSeqRat readback, consumer nonescape, and local naming for BEDC.Derived.DyadicIntervalSubdivisionTreeUp.}
\end{closurestatus}
