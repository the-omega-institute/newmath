\chapter{A Concrete Naming Certificate for $\CompletionExponentialLawUp$}
\label{ch:concrete-instances-completion-exponential-law-namecert}
\origin{human}

Following Mac Lane and Bishop--Bridges, the $\CompletionExponentialLawUp$ packet records the finite completion-law surface by which a completion functor distributes over an exponential-style function object only through displayed completion rows. It sits after the functor packet of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}, the monad packet of \autoref{ch:concrete-instances-cauchycompletionmonad-namecert}, the unit packet of \autoref{ch:concrete-instances-cauchycompletionunit-namecert}, the multiplication packet of \autoref{ch:concrete-instances-cauchycompletionmultiplication-namecert}, and the universal-property packet of \autoref{ch:concrete-instances-completionuniversalproperty-namecert}. The chapter names a standard completion-law handoff as finite NameCert data; it is not host cartesian closedness, not quotient equality of completed maps, not a selected limit, and not ambient $\RealUp$ completeness.

\begin{definition}[Completion exponential law carrier]
\label{def:completion-exponential-law-carrier}
A $\CompletionExponentialLawUp$ carrier is a finite $\BHist$ packet
$$
L=(F,M,U,T,E,W,R,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\CauchyCompletionFunctorUp$ exposure row, recording the completion functor read used by the law.
\item $M$ is the $\CauchyCompletionMonadUp$ row, recording the shared monad surface consumed by the unit and multiplication faces.
\item $U$ is the $\CauchyCompletionUnitUp$ row, naming the unit side of the exponential-law comparison.
\item $T$ is the $\CauchyCompletionMultiplicationUp$ row, naming the multiplication side of the comparison.
\item $E$ is the exponential-function window row, listing the displayed source and target function windows inspected by the packet.
\item $W$ is the finite completion-window row, recording the common finite Cauchy windows through which both sides are read.
\item $R$ is the $\RegSeqRatUp$ readback row used for rational regularity checks along the finite windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare the two completion reads.
\item $H$ records componentwise $\hsame$ transport for $F,M,U,T,E,W,R,D$ and the local structural rows.
\item $C$ records displayed $\Cont$ replay from a completion-law request through the same functor, monad, unit, multiplication, window, readback, tolerance, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\CompletionExponentialLawUp$, $\CauchyCompletionFunctorUp$, $\CauchyCompletionMonadUp$, $\CauchyCompletionUnitUp$, $\CauchyCompletionMultiplicationUp$, $\CompletionUniversalPropertyUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompletionExponentialLawUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $F,M,U,T,E,W,R,D,H,C,P,N$. It has no coordinate for host exponentials, quotient equality of completed maps, selected representatives, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Completion exponential law NameCert obligations]
\label{thm:completion-exponential-law-namecert-obligations}
For every accepted $\CompletionExponentialLawUp$ carrier
$$
L=(F,M,U,T,E,W,R,D,H,C,P,N),
$$
the local $\NameCert_{\CompletionExponentialLawUp}$ surface consists exactly of the following five obligation rows:
\begin{enumerate}
\item carrier admission for the displayed packet $L$ and its structural rows $H,C,P,N$;
\item completion-functor exposure through $F$, read as the functorial source already named by \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert};
\item unit and multiplication coherence through $M,U,T$, read through \autoref{ch:concrete-instances-cauchycompletionmonad-namecert}, \autoref{ch:concrete-instances-cauchycompletionunit-namecert}, and \autoref{ch:concrete-instances-cauchycompletionmultiplication-namecert};
\item universal-property handoff through the $\CompletionUniversalPropertyUp$ source of \autoref{ch:concrete-instances-completionuniversalproperty-namecert};
\item Real, RegSeqRat, and Dyadic nonescape through $W,R,D$, requiring every Real-facing consumer to pass through finite completion windows, regular rational readback, and dyadic tolerance rows before a terminal $\RealUp$ seal is read.
\end{enumerate}
Every completion-law-facing consumer must read these rows before treating the law as a standard completion interface. It receives no host exponential object, no quotient equality of completed maps, no selected representative, no selected limit, no countable-choice schedule, no ambient $\RealUp$ completeness theorem, and no Lean-checked bridge from this packet.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:completion-exponential-law-carrier}. The accepted packet exposes exactly $F,M,U,T,E,W,R,D,H,C,P,N$.

The completion rows $F,M,U,T$ bind the law to the existing functor, monad, unit, and multiplication packets. The universal-property read is therefore a handoff through named completion rows, not an external mapping-space theorem.

The finite rows $E,W,R,D$ keep the exponential comparison observable. Function windows, completion windows, regular rational readback, and dyadic tolerances are inspected before any Real-facing seal can be consumed.

The structural rows $H,C,P,N$ transport, replay, source, and name the same packet. Since the refused host exponentials, quotient equalities, selected limits, choice schedules, and ambient completeness objects have no carrier coordinate, the five rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every public $\CompletionExponentialLawUp$ read factors through displayed $\CauchyCompletionFunctorUp$, $\CauchyCompletionMonadUp$, $\CauchyCompletionUnitUp$, $\CauchyCompletionMultiplicationUp$, $\CompletionUniversalPropertyUp$, finite completion-window, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. A public read that exports host exponentials, quotient equality of completed maps, selected representatives, selected limits, countable choice, ambient $\RealUp$ completeness, or Lean bridge verification from this packet refutes the carrier boundary.}

\independenceWitness{cauchycompletionfunctor, cauchycompletionmonad, cauchycompletionunit, cauchycompletionmultiplication, completionuniversalproperty: $\CompletionExponentialLawUp$ is not bijective to any listed sibling because it jointly carries the completion functor, monad, unit, multiplication, universal-property handoff, finite exponential windows, regular rational readback, dyadic tolerance, transport, replay, provenance, and local naming while refusing host exponential and quotient endpoints.}

\closureat{CompletionExponentialLawUp}{seedStr}

\begin{closurestatus}{\CompletionExponentialLawUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:completion-exponential-law-carrier} and \autoref{thm:completion-exponential-law-namecert-obligations} seed the finite completion-law packet over functor, monad, unit, multiplication, universal-property handoff, finite window, rational readback, dyadic tolerance, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host exponentials, quotient equality of completed maps, selected representatives, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, completion-functor exposure, unit and multiplication coherence, universal-property handoff, Real/RegSeqRat/Dyadic nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompletionExponentialLawUp.}
  \constructivestory{A $\CompletionExponentialLawUp$ packet is generated from CauchyCompletionFunctor, CauchyCompletionMonad, CauchyCompletionUnit, CauchyCompletionMultiplication, CompletionUniversalProperty, finite completion-window, RegSeqRat, DyadicRatCore, Real, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
\end{closurestatus}
