\chapter{A Concrete Naming Certificate for $\CantorIntersectionModulusUp$}
\label{ch:concrete-instances-cantor-intersection-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CantorIntersectionModulusUp$ packet records Cantor intersection as explicit finite modulus data rather than as ambient compactness. It sits after the located nested-interval surface of \autoref{ch:concrete-instances-located-real-nested-intervals-namecert}, the bisection route of \autoref{ch:concrete-instances-located-interval-bisection-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadic-rat-core-namecert}, the finite request interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the finite prefix bar of \autoref{ch:concrete-instances-finite-fan-theorem-namecert}. The chapter names the finite data needed to turn a retained nested branch into a regular Cauchy real name; it does not invoke open-cover compactness or a selected infinite branch.

\begin{definition}[Cantor intersection modulus carrier]
\label{def:cantor-intersection-modulus-carrier}
A $\CantorIntersectionModulusUp$ carrier is a finite $\BHist$ packet
$$
\mathcal{M}=(L,B,D,S,R,E,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the located closed interval row, recording the endpoint order, locatedness tests, and retained closed-interval inclusion data.
\item $B$ is the retained nested bisection branch ledger, exposing only the finite branch decisions already displayed by $\LocatedIntervalBisectionUp$ and $\LocatedRealNestedIntervalsUp$.
\item $D$ is the dyadic diameter schedule from $\DyadicRatCoreUp$, recording the depth-to-radius budget and the finite halving estimates used at each retained stage.
\item $S$ is the $\StreamNameUp$ finite-window row, listing the precision requests and interval stages inspected by the downstream real-name consumer.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the selected finite windows and their dyadic endpoint approximants.
\item $E$ is the terminal $\RealUp$ seal reached only after the interval, bisection, dyadic schedule, stream window, and regular rational readback rows have been displayed.
\item $F$ is the finite fan-depth and modulus output row. It records the finite prefix depth at which the diameter schedule answers the requested precision and the bar-window handoff used by $\FiniteFanTheoremUp$.
\item $H$ records componentwise $\hsame$ transport for the interval, branch, diameter, window, readback, real seal, fan-depth, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an interval request through $L,B,D,S,R,E,F,H$ and into the local certificate.
\item $P$ is the $\Pkg$ provenance over $\CantorIntersectionModulusUp$, $\LocatedRealNestedIntervalsUp$, $\LocatedIntervalBisectionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\FiniteFanTheoremUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorIntersectionModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for open-cover compactness, a selected infinite branch, countable choice, quotient real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cantor intersection modulus NameCert obligations]
\label{thm:cantor-intersection-modulus-namecert-obligations}
For every accepted $\CantorIntersectionModulusUp$ carrier
$$
\mathcal{M}=(L,B,D,S,R,E,F,H,C,P,N),
$$
the local $\NameCert_{\CantorIntersectionModulusUp}$ surface consists exactly of carrier admission for the located closed interval row, retained nested bisection branch ledger, dyadic diameter schedule, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, finite fan-depth and modulus output, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every public read of the packet must expose $L,B,D$ before the stream window $S$, must expose $S,R$ before the real seal $E$, and must expose the fan-depth row $F$ before exporting a precision modulus.

Thus a downstream completion or real-name consumer receives a finite modulus route from nested interval data into regular Cauchy real names. The packet exports no open-cover compactness theorem, selected infinite branch, countable-choice principle, quotient real equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cantor-intersection-modulus-carrier}. The accepted packet displays exactly the located closed interval row, retained branch ledger, dyadic diameter schedule, finite stream windows, regular rational readback, terminal real seal, finite fan-depth output, transport, replay, provenance, and local naming rows.

The route order is forced by the displayed carrier. The nested interval consumer first reads $L$ and $B$ as finite interval and branch data, then reads $D$ as the dyadic diameter schedule that bounds the retained interval at each displayed depth.

The real-name handoff is finite. The row $S$ lists the precision windows, $R$ reads back the corresponding regular rational approximants, and $E$ seals the resulting $\RealUp$ row only after those windows and readbacks are already present.

Finally the row $F$ records the finite fan-depth and modulus output used by the handoff to \autoref{ch:concrete-instances-finite-fan-theorem-namecert}. Since the carrier contains no coordinate for open covers, selected infinite branches, countable choice, quotient real equality, or ambient completeness, $H,C,P,N$ can transport, replay, source, and name only the displayed finite route.
\end{proof}

\begin{theorem}[Finite fan interval handoff]
\label{thm:cantor-intersection-modulus-fan-interval-handoff}
Every $\CantorIntersectionModulusUp$ consumer that asks for a finite precision window factors through the interval-to-fan route
$$
L,B,D \longrightarrow S,R,E \longrightarrow F,H,C,P,N.
$$
The finite fan-depth row $F$ may use the prefix-bar discipline of \autoref{ch:concrete-instances-finite-fan-theorem-namecert} only after the located interval, retained bisection branch, and dyadic diameter rows have fixed the inspected depth. Ordinary regular real readback still passes through the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows; the fan row supplies only the finite window bound.
\end{theorem}
\begin{proof}
Start with an accepted carrier $\mathcal{M}=(L,B,D,S,R,E,F,H,C,P,N)$. The first three rows determine the displayed nested interval data: $L$ gives the located closed interval, $B$ gives the retained finite bisection branch, and $D$ gives the dyadic diameter schedule.

The stream and real-name rows then read only the windows named by that displayed interval data. The rows $S$ and $R$ expose the requested finite precision windows and regular rational readback, and $E$ records the terminal real seal reached from those rows.

It remains to identify the fan use. The row $F$ is the only carrier coordinate that records a finite fan-depth or modulus output, so any consumer asking for a prefix-bar bound must pass through $F$ after $L,B,D,S,R,E$. The transport, replay, provenance, and local naming rows $H,C,P,N$ then export the same finite route and cannot introduce open-cover compactness, a selected infinite branch, countable choice, quotient real equality, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every $\CantorIntersectionModulusUp$ precision-window read factors through one located closed interval row, one retained nested bisection branch ledger, one $\DyadicRatCoreUp$ diameter schedule, one $\StreamNameUp$ finite-window row, one $\RegSeqRatUp$ readback row, one terminal $\RealUp$ seal, one finite $\FiniteFanTheoremUp$ depth row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{located-real-nested-intervals, located-interval-bisection, dyadicratcore, streamname, regseqrat, real, finite-fan-theorem: $\CantorIntersectionModulusUp$ is not bijective to any listed sibling because it jointly carries nested interval data, dyadic diameter scheduling, real-name readback, and finite fan-depth output as one modulus packet.}

\closureat{CantorIntersectionModulusUp}{seedStr}

\begin{closurestatus}{\CantorIntersectionModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-intersection-modulus-carrier}, \autoref{thm:cantor-intersection-modulus-namecert-obligations}, and \autoref{thm:cantor-intersection-modulus-fan-interval-handoff} seed the finite Cantor-intersection modulus route through located interval rows, retained bisection ledgers, dyadic diameter schedules, finite stream windows, regular rational readbacks, terminal real seals, and finite fan-depth rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.CantorIntersectionModulusUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected infinite branches, countable choice, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, fan-depth exactness, interval-window coverage, componentwise transport, replay, provenance, local naming, and non-escape for BEDC.Derived.CantorIntersectionModulusUp.}
  \constructivestory{A $\CantorIntersectionModulusUp$ packet is generated from located closed interval rows, retained nested bisection branch ledgers, dyadic diameter schedules, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, terminal $\RealUp$ seals, finite $\FiniteFanTheoremUp$ depth outputs, componentwise transport, displayed replay, provenance, and a local NameCert row.}
\end{closurestatus}
