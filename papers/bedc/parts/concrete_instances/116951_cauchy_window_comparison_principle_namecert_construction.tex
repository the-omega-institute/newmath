\chapter{A Concrete Naming Certificate for $\CauchyWindowComparisonPrincipleUp$}
\label{ch:concrete-instances-cauchy-window-comparison-principle-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyWindowComparisonPrincipleUp$ packet records the finite binary comparison surface by which two regular Cauchy windows are compared through one displayed dyadic tolerance ledger. It sits beside the finite stream rows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the window-step route of \autoref{ch:concrete-instances-cauchy-window-transducer-namecert}. The packet is a finite comparison interface for Real and Completion consumers; it is not quotient stream equality, selected limits, host equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy-window comparison principle carrier]
\label{def:cauchy-window-comparison-principle-carrier}
A $\CauchyWindowComparisonPrincipleUp$ carrier is a finite $\BHist$ packet
$$
K=(S_0,S_1,D,R_0,R_1,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S_0$ and $S_1$ are the two $\StreamNameUp$ finite-window rows opened for the compared regular Cauchy names.
\item $D$ is the shared $\DyadicRatCoreUp$ tolerance ledger recording the requested dyadic radius, the two displayed observations, and the arithmetic comparison budget used by the binary read.
\item $R_0$ and $R_1$ are the two $\RegSeqRatUp$ readback rows obtained from $S_0,S_1$ through the same dyadic ledger.
\item $E$ is the comparison-evidence row consumed before any downstream $\RealUp$ seal or $\CauchyWindowTransducerUp$ handoff can be read.
\item $H$ records componentwise $\hsame$ transport for $S_0,S_1,D,R_0,R_1,E,C,P,N$.
\item $C$ records the displayed $\Cont$ replay route from the two stream windows through the dyadic ledger, regular rational readbacks, comparison evidence, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyWindowComparisonPrincipleUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyWindowTransducerUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyWindowComparisonPrincipleUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through these ten rows. It has no coordinate for quotient stream equality, a selected limit, host equality of completed reals, countable choice, or a completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Cauchy-window comparison principle NameCert obligations]
\label{thm:cauchy-window-comparison-principle-namecert-obligations}
For every accepted $\CauchyWindowComparisonPrincipleUp$ carrier
$$
K=(S_0,S_1,D,R_0,R_1,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyWindowComparisonPrincipleUp}$ surface consists exactly of carrier admission for the two displayed $\StreamNameUp$ windows, shared $\DyadicRatCoreUp$ tolerance ledger, two $\RegSeqRatUp$ readbacks, comparison-evidence row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every downstream $\RealUp$ or $\CauchyWindowTransducerUp$ consumer must read the comparison-evidence row only after the two source windows, the dyadic ledger, and both regular rational readbacks have been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-window-comparison-principle-carrier}. Its visible inventory is precisely $S_0,S_1,D,R_0,R_1,E,H,C,P,N$.

The binary comparison is finite because $S_0$ and $S_1$ list the two inspected windows and $D$ records the single dyadic tolerance ledger used to compare the displayed observations. The readback rows $R_0$ and $R_1$ are then the regular rational data available to downstream consumers.

The comparison-evidence row $E$ is not a completed real equality. It is only the local row consumed after the stream windows, dyadic ledger, and readbacks have been exposed.

Finally $H$ and $C$ transport and replay the same rows, while $P$ and $N$ bind provenance and local naming. Since no other coordinate is present, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Cauchy-window comparison principle L10 handoff]
\label{thm:cauchy-window-comparison-principle-l10-handoff}
Every L10 consumer of an accepted $\CauchyWindowComparisonPrincipleUp$ packet receives a binary regular-Cauchy comparison only through the finite route
$$
\StreamNameUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RegSeqRatUp,\ E,H,C,P,N .
$$
If the consumer later calls a $\RealUp$ row or a $\CauchyWindowTransducerUp$ row, that call must be supplied by the downstream packet after this route has been displayed. The handoff exports no $\RealUp$ completeness theorem, no quotient stream equality, no selected limit, no host equality, and no countable-choice schedule.
\end{theorem}
\begin{proof}
Start from an accepted packet $K=(S_0,S_1,D,R_0,R_1,E,H,C,P,N)$. The only source rows are the two $\StreamNameUp$ windows, and the only tolerance row is the shared $\DyadicRatCoreUp$ ledger.

The two $\RegSeqRatUp$ rows are read from those displayed windows through the same ledger. Thus the comparison-evidence row $E$ is already tied to finite window observations before any Real-facing or transducer-facing consumer can use it.

Transport, replay, provenance, and local naming preserve the route but do not add another endpoint. A later $\RealUp$ seal or $\CauchyWindowTransducerUp$ handoff must therefore come from its own downstream packet, and the comparison principle itself exports none of the excluded completion, quotient, selection, host-equality, or choice data.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyWindowComparisonPrincipleUp$ read exposes two StreamName finite-window rows, one DyadicRatCore tolerance ledger, two RegSeqRat readback rows, one comparison-evidence row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows before any RealUp or CauchyWindowTransducer handoff is consumed.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real, cauchy-window-transducer: $\CauchyWindowComparisonPrincipleUp$ is not bijective to any listed sibling because it jointly binds two finite windows, one shared dyadic comparison ledger, two regular rational readbacks, comparison evidence, transport, replay, provenance, and local naming while refusing quotient stream equality, selected limits, host equality, countable choice, and ambient Real completeness.}

\closureat{CauchyWindowComparisonPrincipleUp}{seedStr}

\begin{closurestatus}{\CauchyWindowComparisonPrincipleUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-window-comparison-principle-carrier}, \autoref{thm:cauchy-window-comparison-principle-namecert-obligations}, and \autoref{thm:cauchy-window-comparison-principle-l10-handoff} seed the finite binary comparison surface over two StreamName windows, one DyadicRatCore ledger, two RegSeqRat readbacks, comparison evidence, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, host equality, countable choice, standard bridges, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic comparison exactness, readback stability for both rows, transducer handoff nonescape, replay, provenance, local naming, and L10 route exhaustion for BEDC.Derived.CauchyWindowComparisonPrincipleUp.}
  \constructivestory{A $\CauchyWindowComparisonPrincipleUp$ packet is generated from two StreamName finite-window rows, one DyadicRatCore tolerance ledger, two RegSeqRat readbacks, comparison evidence, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
