\chapter{A Concrete Naming Certificate for $\RegularCauchySeparationUp$}
\label{ch:concrete-instances-regularcauchyseparation-namecert}
\origin{human}

Following Bishop and Bridges-style regular Cauchy equality, the $\RegularCauchySeparationUp$ packet records how two regular rational names can be read as separated, or not yet separated at a displayed tolerance, before any $\RealUp$ seal is consumed. It sits over the regular rational rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the modulus surface of \autoref{ch:concrete-instances-cauchymodulus-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The certificate keeps separation evidence as finite NameCert data, not as quotient real equality, host real equality, trichotomy, or ambient completeness.

\begin{definition}[Regular Cauchy separation carrier]
\label{def:regular-cauchy-separation-carrier}
A $\RegularCauchySeparationUp$ carrier is a finite $\BHist$ packet
$$
K=(L,R,W,D,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the left $\RegSeqRatUp$ regular rational name row.
\item $R$ is the right $\RegSeqRatUp$ regular rational name row.
\item $W$ is the $\StreamNameUp$ finite-window ledger naming the left and right observation windows inspected by the separation read.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic radii and rational comparison rows used at those windows.
\item $M$ is the $\CauchyModulusUp$ separation-modulus evidence, stating which finite windows in $W$ must be displayed before the tolerance rows in $D$ may be read as equality or separation evidence.
\item $E$ is the $\RealUp$ seal boundary reached only after $L,R,W,D,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two regular names, finite windows, dyadic tolerance rows, separation modulus evidence, Real seal boundary, continuation, provenance, and local naming rows.
\item $C$ records the displayed $\Cont$ replay from every equality or separation read through $L,R,W,D,M,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchySeparationUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchySeparationUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient real equality, host real equality, trichotomy, selected limits, countable choice, ambient Real completeness, or a standard bridge.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchySeparationUp.RegularCauchySeparationTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy separation NameCert obligations]
\label{thm:regular-cauchy-separation-namecert-obligations}
For an accepted carrier $K=(L,R,W,D,M,E,H,C,P,N)$ of \autoref{def:regular-cauchy-separation-carrier}, the local $\NameCert_{\RegularCauchySeparationUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ source rows $L,R$, the $\StreamNameUp$ window ledger $W$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the $\CauchyModulusUp$ separation-modulus row $M$, the $\RealUp$ boundary row $E$, and the structural rows $H,C,P,N$;
\item classifier exposure, stating that every equality or separation read of the packet is classified through the displayed route $L,R,W,D,M$ before it can reach $E$;
\item finite-window stability, stating that transport through $H$ and replay through $C$ preserve the same left and right windows named by $W$;
\item dyadic tolerance exactness, stating that the rational comparison rows read from $D$ are the only tolerance rows admitted for the displayed separation read;
\item modulus boundary, stating that $M$ is the only row allowed to turn a requested equality or separation tolerance into finite windows of $W$;
\item Real seal non-escape, stating that $E$ is a boundary row consumed only after $L,R,W,D,M,H,C,P,N$ have been retained;
\item package provenance and local naming, stating that $P$ names exactly the listed sibling certificates and $N$ names the same finite packet as a local $\NameCert_{\RegularCauchySeparationUp}$.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchySeparationUp.RegularCauchySeparationCarrier\_namecert\_obligations}
\begin{proof}
Project the accepted carrier. The rows $L,R,W,D,M,E$ are the analytic coordinates, while $H,C,P,N$ transport, replay, source, and name the same finite packet.

Read the classifier in dependency order. A separation or equality request first reads the two regular rational names $L$ and $R$, then the finite observation windows $W$, then the dyadic tolerance ledger $D$, and then the modulus row $M$ that binds the requested tolerance to those windows.

The Real-facing row is downstream of that route. Since $E$ is stored only after $L,R,W,D,M$, a consumer that bypasses one of those rows has not obtained a read of the packet's Real seal boundary.

The structural rows add no hidden equality principle. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve the displayed route, so the listed rows exhaust the seed NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy separation modulus boundary]
\label{thm:regular-cauchy-separation-modulus-boundary}
For every accepted $\RegularCauchySeparationUp$ packet $K=(L,R,W,D,M,E,H,C,P,N)$, every equality or separation read factors through the finite route
$$
(L,R)\longmapsto W\longmapsto D\longmapsto M
$$
before reaching the $\RealUp$ seal boundary $E$. Thus the packet exports no equality or separation evidence whose source is a detached Real value, a host equality relation, trichotomy, a selected limit, countable choice, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchySeparationUp.RegularCauchySeparationCarrier\_modulus\_boundary}
\begin{proof}
Start from \autoref{thm:regular-cauchy-separation-namecert-obligations}. The classifier exposure places every accepted equality or separation read on the displayed left and right regular names, their finite window ledger, the dyadic tolerance ledger, and the separation-modulus row.

The modulus row is the last analytic gate before the seal boundary. It converts the requested tolerance into the finite windows already named by $W$ and the dyadic comparison rows already named by $D$; it does not select a completed real or import host equality.

Finally read $E$ as a boundary row. The package reaches $\RealUp$ only after the finite route through $L,R,W,D,M$ has been displayed, and the transport, continuation, provenance, and local naming rows preserve that route. Therefore every equality or separation read factors through $W,D,M$ before reaching the Real seal boundary.
\end{proof}

\begin{theorem}[Regular Cauchy separation zero-distance reflection]
\label{thm:regular-cauchy-separation-zero-distance-reflection}
Let $K=(L,R,W,D,M,E,H,C,P,N)$ be an accepted $\RegularCauchySeparationUp$ packet. Suppose the separation read uses the same displayed $\StreamNameUp$ windows for $L$ and $R$, and suppose the $\DyadicRatCoreUp$ tolerance ledger collapses through the $\CauchyModulusUp$ row $M$ to the zero-distance boundary. Then the $\RealUp$ seal receives equality evidence only through the finite route
$$
(L,R)\longmapsto W\longmapsto D\longmapsto M\longmapsto E .
$$
The packet exports no quotient real equality, host real equality, trichotomy, selected limit, countable choice, or ambient completeness assertion.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchySeparationUp.RegularCauchySeparationCarrier\_zero\_distance\_reflection}
\begin{proof}
Project the carrier from \autoref{def:regular-cauchy-separation-carrier}. The equality-facing read has access only to the two regular rational names $L,R$, the finite window ledger $W$, the dyadic tolerance ledger $D$, the modulus row $M$, the Real seal boundary $E$, and the structural rows $H,C,P,N$.

Use the NameCert obligations of \autoref{thm:regular-cauchy-separation-namecert-obligations}. Classifier exposure forces the two displayed StreamName windows to be read before the dyadic tolerance ledger can be interpreted as equality evidence.

Apply the modulus boundary theorem \autoref{thm:regular-cauchy-separation-modulus-boundary}. When the tolerance ledger collapses through $M$ to the zero-distance boundary, the collapse is still routed through $W,D,M$ before any Real seal read occurs.

The conclusion is therefore a finite route statement. The carrier has no quotient-real coordinate, no trichotomy coordinate, no selected-limit coordinate, and no ambient-completeness coordinate, while $H,C,P,N$ preserve only the displayed rows.
\end{proof}

\begin{theorem}[Regular Cauchy separation obligation scope]
\label{thm:regular-cauchy-separation-obligation-scope}
For every accepted $\RegularCauchySeparationUp$ packet $K=(L,R,W,D,M,E,H,C,P,N)$, the carrier row, NameCert obligations, modulus boundary, and zero-distance reflection exhaust the obligation rows over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, and $\RealUp$. Every equality or separation read must be routed through the finite windows $W$, dyadic tolerance ledger $D$, and separation-modulus row $M$ before the $\RealUp$ boundary row $E$ can be consumed. The obligation surface contains no quotient equality, trichotomy, selected limits, countable choice, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchySeparationUp.RegularCauchySeparationCarrier\_obligation\_scope}
\begin{proof}
Project the carrier rows $L,R,W,D,M,E,H,C,P,N$. The rows $L$ and $R$ provide the two regular rational names, while $W,D,M$ expose the finite observations, dyadic tolerances, and modulus evidence that any equality or separation read must inspect.

Use the NameCert obligations of \autoref{thm:regular-cauchy-separation-namecert-obligations}. Carrier admission, classifier exposure, finite-window stability, dyadic tolerance exactness, modulus boundary, Real seal non-escape, package provenance, and local naming are all listed there as rows of the same packet.

Apply the modulus boundary theorem before reading the Real-facing row. It routes every request through
$$
(L,R)\longmapsto W\longmapsto D\longmapsto M
$$
and only then permits the boundary row $E$ to be consumed.

Finally use zero-distance reflection for the equality-facing edge case. Even when the tolerance ledger collapses to the zero-distance boundary, the read remains a finite route through $W,D,M,E$. Since the carrier has no coordinate for quotient equality, trichotomy, selected limits, countable choice, or ambient completeness, no further obligation row is available.
\end{proof}

\begin{theorem}[Regular Cauchy separation Real seal scope]
\label{thm:regular-cauchy-separation-real-seal-scope}
Let $K=(L,R,W,D,M,E,H,C,P,N)$ be an accepted $\RegularCauchySeparationUp$ packet. Every accepted $\RealUp$-facing equality or separation consumer of $K$ must pass through the regular-name rows $L,R$, the finite-window row $W$, the dyadic tolerance row $D$, and the separation-modulus row $M$ before the $\RealUp$ seal boundary $E$ is consumed. The support rows $H,C,P,N$ preserve that same finite route through transport, replay, provenance, and local naming. No quotient equality, selected limit, countable choice, trichotomy, or ambient completeness coordinate is exported.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:regular-cauchy-separation-carrier}. The only analytic rows available to a Real-facing consumer are $L,R,W,D,M,E$, with $H,C,P,N$ serving as support rows for the same finite packet.

Use the NameCert obligations of \autoref{thm:regular-cauchy-separation-namecert-obligations}. They place carrier admission, classifier exposure, finite-window stability, dyadic tolerance exactness, and Real seal non-escape on the displayed rows before any consumer may read $E$.

Apply the modulus boundary theorem \autoref{thm:regular-cauchy-separation-modulus-boundary}. It forces every equality or separation request through $W,D,M$ before the Real seal boundary is available.

Use zero-distance reflection from \autoref{thm:regular-cauchy-separation-zero-distance-reflection} for equality edge cases. Even when the dyadic ledger collapses to the zero-distance boundary, the collapse still passes through the same finite route before $E$.

Finally read $H,C,P,N$ as preservation rows. They transport, replay, source, and name the displayed route, while the carrier has no coordinate for quotient equality, selected limits, countable choice, trichotomy, or ambient completeness.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchySeparationUp$ consumer that reaches an equality or separation read without the displayed $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, and $\CauchyModulusUp$ separation-modulus row refutes the carrier surface.}

\independenceWitness{regseqrat, streamname, dyadicratcore, cauchymodulus, real: no listed sibling supplies the whole ten-row packet $K=(L,R,W,D,M,E,H,C,P,N)$, because each sibling contributes only regular-name rows, finite windows, dyadic tolerances, modulus evidence, or a Real seal boundary while this chapter binds the separation route with transport, replay, provenance, and local naming.}

\begin{theorem}[Regular Cauchy separation scoped package]
\label{thm:regular-cauchy-separation-scoped-package}
The obligation-scope theorem \autoref{thm:regular-cauchy-separation-obligation-scope} and the Real-seal-scope theorem \autoref{thm:regular-cauchy-separation-real-seal-scope} give the complete scoped L10 route for $\RegularCauchySeparationUp$. Every equality or separation read of an accepted packet is confined to the displayed rows over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, and $\RealUp$, with transport, replay, provenance, and local naming preserved by $H,C,P,N$; no quotient equality, trichotomy, selected limit, countable choice, or ambient completeness row remains outside that scope.
\end{theorem}
\begin{proof}
The obligation-scope theorem lists the complete carrier and NameCert obligation boundary for the regular-name, stream-window, dyadic-tolerance, modulus, and Real-seal rows. It also states that every equality or separation read passes through the finite route before the Real boundary can be consumed.

The Real-seal-scope theorem then records the consumer-facing boundary of the same route. It requires every Real-facing equality or separation consumer to pass through $L,R,W,D,M$ before $E$, with $H,C,P,N$ preserving the packet.

Together these two scope theorems exhaust the L10 route. The carrier has no remaining coordinate for quotient equality, host equality, trichotomy, selected limits, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Regular Cauchy separation public interface]
\label{thm:regular-cauchy-separation-public-interface}
Every public consumer of the $\RegularCauchySeparationUp$ separation interface must factor through the ten displayed rows
$$
K=(L,R,W,D,M,E,H,C,P,N)
$$
of \autoref{def:regular-cauchy-separation-carrier}. Consequently a public read may export only equality or separation evidence grounded in the $\RegSeqRatUp$ rows $L,R$, the $\StreamNameUp$ finite-window row $W$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the $\CauchyModulusUp$ separation-modulus row $M$, and the $\RealUp$ seal boundary $E$, with $H,C,P,N$ preserving that same finite route. It exports no quotient real equality, host real equality, trichotomy, selected limit, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the carrier of \autoref{def:regular-cauchy-separation-carrier}. It gives the public interface the rows $L,R,W,D,M,E,H,C,P,N$ and no additional equality coordinate.

Apply the NameCert obligations of \autoref{thm:regular-cauchy-separation-namecert-obligations}. They force every equality or separation read to expose the regular-name rows, finite-window ledger, dyadic tolerance ledger, separation-modulus row, Real seal boundary, transport, replay, provenance, and local naming rows of the same packet.

Use the modulus-boundary theorem \autoref{thm:regular-cauchy-separation-modulus-boundary} and zero-distance reflection \autoref{thm:regular-cauchy-separation-zero-distance-reflection}. These place both separation reads and equality-edge reads behind the finite route through $W,D,M$ before the Real seal boundary $E$ can be consumed.

Finally apply the obligation-scope theorem \autoref{thm:regular-cauchy-separation-obligation-scope}, the Real-seal-scope theorem \autoref{thm:regular-cauchy-separation-real-seal-scope}, and the scoped package theorem \autoref{thm:regular-cauchy-separation-scoped-package}. Together they exhaust the L10 route and exclude quotient equality, host equality, trichotomy, selected limits, countable choice, and ambient completeness from the public interface.
\end{proof}

\begin{theorem}[Regular Cauchy separation public route closure]
\label{thm:regular-cauchy-separation-public-route-closure}
Every $\RealUp$ and $\CompletionUp$ public consumer of an accepted $\RegularCauchySeparationUp$ packet receives exactly the route through $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, and $\RealUp$ displayed by \autoref{thm:regular-cauchy-separation-public-interface}. Therefore a public consumer cannot use quotient real equality, trichotomy, selected limits, countable choice, or ambient Real completeness as an alternate public route.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:regular-cauchy-separation-carrier}. It exposes only the two regular rational rows $L,R$, the window row $W$, the dyadic tolerance row $D$, the modulus row $M$, the Real seal $E$, and the structural rows $H,C,P,N$.

Apply the NameCert obligations of \autoref{thm:regular-cauchy-separation-namecert-obligations}. These obligations place carrier admission, classifier exposure, finite-window stability, dyadic tolerance exactness, Real seal non-escape, provenance, and local naming on those same rows.

Use the modulus boundary theorem \autoref{thm:regular-cauchy-separation-modulus-boundary} and zero-distance reflection \autoref{thm:regular-cauchy-separation-zero-distance-reflection}. They force separation reads and equality-edge reads through $W,D,M$ before $E$ is consumed.

Read the obligation scope, Real seal scope, scoped package, and public interface in order. The theorems \autoref{thm:regular-cauchy-separation-obligation-scope}, \autoref{thm:regular-cauchy-separation-real-seal-scope}, \autoref{thm:regular-cauchy-separation-scoped-package}, and \autoref{thm:regular-cauchy-separation-public-interface} present the same route to every public consumer.

No extra carrier coordinate remains after $L,R,W,D,M,E,H,C,P,N$. Hence there is no alternate public route through quotient equality, trichotomy, selected limits, countable choice, or ambient Real completeness.
\end{proof}

\begin{closurestatus}{\RegularCauchySeparationUp}
  \constructivestory{A $\RegularCauchySeparationUp$ packet is generated from left and right $\RegSeqRatUp$ rows, a $\StreamNameUp$ finite-window ledger, a $\DyadicRatCoreUp$ tolerance ledger, a $\CauchyModulusUp$ separation-modulus row, a $\RealUp$ seal boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{thm:regular-cauchy-separation-public-interface} and \autoref{thm:regular-cauchy-separation-public-route-closure} expose the public separation surface already bound by the carrier, NameCert, modulus, zero-distance, scoped-package, and Real-seal theorems, with every public read routed through finite $\RegSeqRatUp$ and $\RealUp$ readback rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient real equality, host real equality, trichotomy, selected limits, countable choice, ambient Real completeness, Lean verification, or a standard bridge.}
  \upgradepath{Mature closure requires Lean verification and a standard bridge for the displayed separation packet.}
\end{closurestatus}
