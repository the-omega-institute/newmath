\input{parts/concrete_instances/int/06_int_history_certificate_01_core}
\input{parts/concrete_instances/int/06_int_history_certificate_02_bridge_and_transport}
\input{parts/concrete_instances/int/history_transport_context}

\closureat{IntUp}{matureStr}[BEDC.Derived.IntUp.IntHistory\_semanticNameCert]
\begin{closurestatus}{\IntUp}
  \constructivestory{The carrier of $\IntUp$ is a pair predicate on a kernel mark and a unary history: the predicate requires the mark to be either b0 or b1 (a Boolean choice acting as the sign) and the magnitude to be a unary history (a $\NatUp$ tally). The classifier on two such pairs agrees when the marks are mark-equal and the magnitudes are history-equal, with an additional zero-sign normalization rule that $(b0, \emp)$ and $(b1, \emp)$ are identified so $+0$ and $-0$ name the same integer. The source spec is the same predicate as the carrier; the pattern spec records the sign-magnitude case split, the kernel append-context behavior of the magnitude side, and the zero-sign collapse; the ledger keeps the visible signed pair aligned with its raw counterpart through history-sameness transport on the magnitude. The five name-certificate obligations follow from the carrier being inhabited by $(b0, \emp)$, mark-equality and history-equality each being an equivalence, and the zero-sign rule being symmetric and transitive. A signed-unary pair $(s, m)$ lies in the signature gap of an $\IntUp$ token $p$ when the source-domain admits $(s, m)$, the signature relation pairs it with an answer pair and an evidence ledger, and the token-introduction relation maps that answer to $p$ --- collapsing to ``$(s, m)$ has the sign and magnitude encoded in $p$, modulo the zero-sign rule''. Beyond the in-paper certificate, the chapter pairs $\IntUp$ with a standard signed-unary bridge that translates each integer pair into a signed unary word and back, and that bridge is consumed downstream by $\RatUp$'s numerator readback and other algebraic consumers. The chapter registers this signed-magnitude construction under the name $\IntUp$; the integer is not a quotient of pairs of naturals, it is a single sign mark next to a unary spine, and the only equivalence imposed is the explicit zero-sign collapse rather than a hidden equivalence-class machinery.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature public surface is the signed-unary downstream package of \autoref{thm:intup-mature-signed-unary-downstream-package}: the semantic certificate, classifier readback rows, append-context and $\hsame$ transports, pair/history and zero-sign bridges, and the standard signed-unary bridge consumed by $\RatUp$ numerator readback and later algebraic consumers.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.IntUp.IntHistory\_semanticNameCert}
  \bridgestatus{bridgeChecked}
  \notclaimed{No full traditional integer algebra, ordered-ring API, Euclidean division, divisibility theory, quotient-normalization primitive, or bridgeChecked verification is closed here.}
  \upgradepath{No further upgrade -- chapter at top of formal axis.}
\end{closurestatus}
