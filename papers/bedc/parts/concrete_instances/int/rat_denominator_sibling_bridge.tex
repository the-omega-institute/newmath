\section{Rat Denominator Sibling Bridge}
\label{sec:int-rat-denominator-sibling-bridge}

The $\IntUp$ signed-pair route is a substantive sibling dependency for \autoref{ch:concrete-instances-rat-namecert}. The $\RatUp$ denominator surface consumes the balanced integer numerator only after the signed-pair route and the additive-denominator route have been displayed, so the rational carrier is not a standalone quotient surface or a field-equality row.
