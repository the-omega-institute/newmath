\begin{proposition}[Int signed-pair sibling dependency lattice]
\label{prop:int-nat-add-rat-sibling-dependency-lattice}
The $\IntUp$ signed-pair carrier consumes the unary row discipline of $\NatUp$ from \autoref{ch:concrete-instances-nat-namecert} and the additive comparison rows of $\AddUp$ from \autoref{ch:concrete-instances-add-namecert} before $\RatUp$ denominator routes in \autoref{ch:concrete-instances-rat-namecert} can cite the integer surface. Thus the integer packet is a horizontal dependency node: it is not merely a target of rational-denominator readback, but a finite signed-pair ledger whose Nat and Add rows are already in force when Rat consumes it.
\end{proposition}
