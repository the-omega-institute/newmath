\chapter{A Concrete Naming Certificate for $\LocatedCompactIntervalSequenceUp$}
\label{ch:concrete-instances-located-compact-interval-sequence-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\LocatedCompactIntervalSequenceUp$ packet records a finite sequence of located compact interval certificates through displayed locator, finite-net, and regular-Cauchy window data. It sits over the compact-interval locator of \autoref{ch:concrete-instances-compact-interval-locator-namecert}, the Bishop located compact-set surface of \autoref{ch:concrete-instances-bishop-located-compact-set-namecert}, the finite-window induction route of \autoref{ch:concrete-instances-regular-cauchy-window-induction-namecert}, the stream-name interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a located and finite-net sequence surface for L10 compact-interval arguments; it does not use open-cover compactness, selected subsequential limits, countable choice, quotient equality, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Located compact interval sequence carrier]
\label{def:located-compact-interval-sequence-carrier}
A $\LocatedCompactIntervalSequenceUp$ carrier is a finite $\BHist$ packet
$$
K=(I,L,B,F,W,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite interval row, listing the compact rational or dyadic interval names inspected by the packet.
\item $L$ is the $\CompactIntervalLocatorUp$ row, recording left and right endpoint locator data, metric-distance requests, and displayed interval containment checks.
\item $B$ is the $\BishopLocatedCompactSetUp$ row, storing the located compact-set finite-net reading attached to the same interval sequence.
\item $F$ is the finite-net ledger, recording the finite centers, coverage witnesses, and refinement comparisons used at each displayed tolerance.
\item $W$ is the $\StreamNameUp$ finite-window row through which interval names, locator requests, and net centers are read.
\item $R$ is the $\RegSeqRatUp$ readback row for endpoint, radius, and distance comparisons.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row for denominator budgets, dyadic radii, and interval-overlap inequalities.
\item $E$ is the terminal $\RealUp$ seal reached only after $I,L,B,F,W,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for interval rows, locator rows, located compact-set rows, finite-net ledgers, stream windows, regular rational readbacks, dyadic tolerance rows, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a compact-interval sequence request through $I,L,B,F,W,R,D,E,H$.
\item $P$ is the $\Pkg$ provenance over $\LocatedCompactIntervalSequenceUp$, $\CompactIntervalLocatorUp$, $\BishopLocatedCompactSetUp$, $\RegularCauchyWindowInductionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCompactIntervalSequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,L,B,F,W,R,D,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected subsequential limits, countable-choice net selection, quotient equality of interval names, trichotomy of endpoints, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located compact interval sequence NameCert obligations]
\label{thm:located-compact-interval-sequence-obligations}
For every accepted $\LocatedCompactIntervalSequenceUp$ carrier
$$
K=(I,L,B,F,W,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedCompactIntervalSequenceUp}$ surface consists exactly of interval-row admission, compact-interval locator exposure, Bishop located compact-set handoff, finite-net ledger coverage, $\StreamNameUp$ finite-window exposure, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance accounting, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-compact-interval-sequence-carrier}. The accepted packet displays the interval row $I$, locator row $L$, located compact-set row $B$, finite-net ledger $F$, window row $W$, readback row $R$, tolerance row $D$, and Real seal $E$.

The compactness-facing data are finite. Locator reads pass through $L$, locatedness reads pass through $B$, and finite-net coverage passes through $F$ before any Real-facing seal is available.

The regular-Cauchy side is $W,R,D$. A consumer reads stream windows, rational readback, and dyadic tolerance budgets before using $E$.

Finally $H,C,P,N$ transport, replay, source, and name the same rows. Since the classifier has no coordinate for open covers, selected subsequential limits, countable-choice schedules, quotient equality, endpoint trichotomy, or ambient completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Located compact interval sequence extraction]
\label{thm:located-compact-interval-sequence-extraction}
Every Real or Completion-facing read exported by an accepted $\LocatedCompactIntervalSequenceUp$ carrier factors through the finite route
$$
I \longmapsto L,B,F \longmapsto W,R,D \longmapsto E \longmapsto H,C,P,N .
$$
Thus a consumer can extract located interval data, finite-net coverage, regular rational readback, and a terminal $\RealUp$ seal only by displaying the locator and finite-window schedules that precede them.
\end{theorem}
\begin{proof}
Start with the obligation theorem. It requires every accepted read to keep $I,L,B,F,W,R,D,E,H,C,P,N$ as one finite packet.

The locator stage reads $I,L,B,F$. The interval names are exposed before the compact-interval locator and Bishop located compact-set rows, and the finite-net ledger records the centers and coverage witnesses used by the located read.

The finite-window stage reads $W,R,D$. The $\StreamNameUp$ windows expose the inspected names, the $\RegSeqRatUp$ row reads endpoint and radius data, and the $\DyadicRatCoreUp$ row records the tolerance comparisons.

Only after those rows are displayed may $E$ be read as a terminal $\RealUp$ seal. The structural rows preserve the same route, so no hidden open-cover theorem, selected limit, countable-choice selection, quotient equality, trichotomy, or ambient completeness principle can replace the displayed extraction path.
\end{proof}

\begin{theorem}[Located compact interval sequence ledger]
\label{thm:located-compact-interval-sequence-ledger}
Every accepted $\LocatedCompactIntervalSequenceUp$ carrier exposes a single finite ledger joining compact-interval locator data, located compact-set finite nets, regular-Cauchy finite windows, rational readback, and dyadic tolerance comparisons. The ledger is stable under componentwise $\hsame$ transport and displayed $\Cont$ replay, and it exports no open-cover compactness, sequential compactness without displayed finite nets, countable choice, quotient equality, trichotomy, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the carrier named in \autoref{def:located-compact-interval-sequence-carrier}. Its finite ledger is precisely the tuple $I,L,B,F,W,R,D,E,H,C,P,N$.

The compact-interval part is $I,L,B,F$. These rows bind interval names, locator requests, located compact-set data, and finite-net coverage witnesses into one displayed packet.

The finite-window part is $W,R,D,E$. These rows record the inspected stream windows, regular rational readbacks, dyadic tolerance comparisons, and the terminal Real seal.

Componentwise transport $H$ and displayed replay $C$ preserve the same rows, while provenance $P$ and local naming $N$ keep the ledger attached to $\LocatedCompactIntervalSequenceUp$. The excluded principles are absent from the classifier, so the ledger cannot export them.
\end{proof}

\falsifiablePrediction{An accepted $\LocatedCompactIntervalSequenceUp$ consumer that reaches a $\RealUp$ seal or finite-net compact-interval read without displaying $I,L,B,F,W,R,D,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{compactintervallocator, bishoplocatedcompactset, regularcauchywindowinduction, streamname, regseqrat, dyadicratcore, real: no listed sibling supplies the joint finite packet containing compact interval locator data, located compact-set finite nets, regular-Cauchy finite windows, rational readback, dyadic tolerance accounting, Real sealing, transport, replay, provenance, and local NameCert rows.}

\closureat{LocatedCompactIntervalSequenceUp}{seedStr}

\begin{closurestatus}{\LocatedCompactIntervalSequenceUp}
  \constructivestory{A $\LocatedCompactIntervalSequenceUp$ packet is generated from finite interval rows, CompactIntervalLocator rows, BishopLocatedCompactSet finite-net rows, RegularCauchyWindowInduction schedules, StreamName windows, RegSeqRat readbacks, DyadicRatCore tolerance ledgers, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-compact-interval-sequence-carrier}, \autoref{thm:located-compact-interval-sequence-obligations}, \autoref{thm:located-compact-interval-sequence-extraction}, and \autoref{thm:located-compact-interval-sequence-ledger} seed the located compact interval sequence route from compact-interval locator data through finite nets, finite windows, rational readback, dyadic tolerance accounting, and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, sequential compactness without displayed finite nets, countable choice, quotient equality, trichotomy, selected limits, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-interval locator exactness, located finite-net coverage, finite-window induction, rational readback, dyadic tolerance accounting, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.LocatedCompactIntervalSequenceUp.}
\end{closurestatus}
