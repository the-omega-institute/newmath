\chapter{A Concrete Naming Certificate for $\CauchyModulusOfContinuityUp$}
\label{ch:concrete-instances-cauchymodulusofcontinuity-namecert}
\origin{human}

Following Bishop and Bridges' constructive-analysis use of explicit moduli,
the $\CauchyModulusOfContinuityUp$ packet records pointwise continuity data for
Cauchy names as a visible finite schedule rather than as a host continuity
predicate. It sits between the explicit Cauchy schedule surface of
\autoref{ch:concrete-instances-cauchymodulus-namecert}, the finite
Cauchy-name transformer of
\autoref{ch:concrete-instances-cauchycontinuousmap-namecert}, the regular
rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the
terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and
the compact uniform consumer boundary of
\autoref{ch:concrete-instances-uniformmodulus-namecert}. The pointwise packet
records the threshold route for one displayed source Cauchy name and its
target tolerances; it is not compactness, a uniform modulus over all inputs,
or a host function equality principle.

\begin{definition}[Cauchy modulus of continuity carrier]
\label{def:cauchy-modulus-of-continuity-carrier}
A $\CauchyModulusOfContinuityUp$ carrier is a finite $\BHist$ packet
$$
K=(S,T,\mu,F,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source $\StreamNameUp$ window surface whose finite rational
observations are inspected before a target tolerance is answered.
\item $T$ is the target $\StreamNameUp$ window surface promised by the
pointwise Cauchy-name transformer.
\item $\mu$ is the $\CauchyModulusUp$ threshold schedule assigned to each
requested target tolerance, including the finite source-window bound that the
consumer must inspect.
\item $F$ is the $\CauchyContinuousMapUp$ transformer row that consumes
$S,\mu$ and produces the displayed target windows $T$.
\item $R$ stores the $\RegSeqRatUp$ readbacks for the source and target
windows, with the target readback reached only through $F$ and $\mu$.
\item $E$ is the $\RealUp$ seal row reached from the target
$\RegSeqRatUp$ readback after the displayed schedule has been replayed.
\item $H$ records componentwise $\hsame$ transport for $S,T,\mu,F,R,E$ and
for the continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a target tolerance request
through the threshold schedule $\mu$, the source window $S$, the transformer
row $F$, the target window $T$, the regular readbacks $R$, and the seal row
$E$.
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\CauchyModulusUp$,
$\CauchyContinuousMapUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$,
$\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyModulusOfContinuityUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only by transporting these displayed
rows to a common finite source-window, target-window, threshold, transformer,
readback, and seal surface. It has no coordinate for quotient streams, host
function equality, selected limits, compactness, a uniform modulus, or ambient
completeness.
\end{definition}

\begin{theorem}[Cauchy modulus of continuity NameCert obligations]
\label{thm:cauchy-modulus-of-continuity-namecert-obligations}
For every accepted $\CauchyModulusOfContinuityUp$ carrier
$K=(S,T,\mu,F,R,E,H,C,P,N)$, the local
$\NameCert_{\CauchyModulusOfContinuityUp}$ surface consists exactly of five
obligation rows:
\begin{enumerate}
\item carrier admission for the displayed source windows, target windows,
threshold schedule, transformer row, regular readbacks, seal row, transport,
continuation, provenance, and local name;
\item classifier transport to a common $\StreamNameUp$, $\CauchyModulusUp$,
$\CauchyContinuousMapUp$, $\RegSeqRatUp$, and $\RealUp$ finite surface;
\item stability of $S,T,\mu,F,R,E$ under the componentwise $\hsame$ rows
recorded in $H$;
\item exactness of $\mu$ as the only threshold route read by the
$\CauchyContinuousMapUp$ consumer;
\item $\Cont$ replay, $\Pkg$ provenance, $\RealUp$ seal factorization, and
local NameCert rows for exactly the displayed packet.
\end{enumerate}
No NameCert obligation is stored outside those five rows.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:cauchy-modulus-of-continuity-carrier}. The projection exposes
exactly the source windows, target windows, threshold schedule, transformer
row, regular readbacks, real seal, transport rows, continuation rows,
provenance row, and local name.

Classifier transport is componentwise. The rows in $H$ move the source and
target windows, the threshold schedule, the transformer row, the readbacks,
and the seal row to the same displayed finite surface.

The threshold exactness row is exhausted by $\mu$. The transformer row $F$
reads a target tolerance by first reading the schedule $\mu$, then inspecting
the named source windows $S$, and then producing the target windows $T$.

Finally replay $C$ and attach $P,N$. These rows route, source, and name the
already displayed packet; they do not introduce quotient streams, host
function equality, selected limits, compactness, a uniform modulus, or ambient
completeness. Hence the five listed rows exhaust the local naming obligations.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusOfContinuityUp.CauchyModulusOfContinuityCarrier\_namecert\_obligations}

\begin{theorem}[CauchyContinuousMap threshold handoff]
\label{thm:cauchy-modulus-of-continuity-cauchy-handoff}
For every accepted $\CauchyModulusOfContinuityUp$ carrier
$K=(S,T,\mu,F,R,E,H,C,P,N)$, the displayed schedule $\mu$ is the only
threshold route used by a $\CauchyContinuousMapUp$ consumer. Any read of the
target window $T$ must pass through the source window $S$, the schedule $\mu$,
the transformer row $F$, the $\RegSeqRatUp$ readbacks $R$, and the displayed
$\Cont$, $\Pkg$, and local $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start with a target tolerance request. Carrier admission exposes the schedule
$\mu$ before the transformer row $F$ can be consumed.

Replay the displayed continuation row $C$. The route sends the tolerance
request to $\mu$, obtains the finite source-window threshold, reads the
corresponding source observations in $S$, and then lets $F$ produce the target
windows $T$.

The readback row $R$ is reached only after that transformer route. Transport
through $H$ preserves the same order of dependencies, and $P,N$ merely record
provenance and local naming.

Since the carrier contains no second threshold coordinate, no quotient stream,
and no host function row, a $\CauchyContinuousMapUp$ consumer has no alternate
route by which to obtain $T$.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusOfContinuityUp.CauchyModulusOfContinuityCarrier\_cauchy\_continuous\_map\_threshold\_handoff}

\begin{theorem}[Cauchy modulus of continuity scoped route]
\label{thm:cauchy-modulus-of-continuity-scoped-route}
Every accepted $\CauchyModulusOfContinuityUp$ read factors through the $\StreamNameUp$ source and target windows, the $\CauchyModulusUp$ threshold schedule, the $\CauchyContinuousMapUp$ transformer, the $\RegSeqRatUp$ readbacks, the $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any compact-uniform consumer may read it. The route exports exactly the pointwise Cauchy-name continuity surface over $S,T,\mu,F,R,E,H,C,P,N$, with no compactness coordinate, uniform modulus over all inputs, host function equality, quotient stream equality, selected-limit witness, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start from the carrier definition \autoref{def:cauchy-modulus-of-continuity-carrier}. It fixes the finite rows $S,T,\mu,F,R,E,H,C,P,N$ and gives the packet no coordinate outside source windows, target windows, threshold schedule, transformer route, regular readbacks, real seal, transport, replay, provenance, and local naming.

Apply the NameCert obligations of \autoref{thm:cauchy-modulus-of-continuity-namecert-obligations}. They bind carrier admission, classifier transport, componentwise stability, threshold exactness, replay, provenance, seal factorization, and local naming to those same rows.

Use the CauchyContinuousMap threshold handoff of \autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff}. Any target-window read must pass through $S,\mu,F,T,R$ before reaching the structural rows, so a compact-uniform consumer cannot bypass the displayed pointwise schedule.

Finally read the $\RealUp$ seal through $E$ and the structural rows $H,C,P,N$. Since the route has no compactness, uniform-modulus, host-function, quotient-stream, selected-limit, or ambient-completeness coordinate, the listed rows form the scoped route for every accepted consumer read.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusOfContinuityUp.CauchyModulusOfContinuityCarrier\_scoped\_route}

\begin{theorem}[Cauchy modulus of continuity finite-window monotonicity]
\label{thm:cauchy-modulus-of-continuity-window-monotonicity}
Let $K=(S,T,\mu,F,R,E,H,C,P,N)$ and
$K^+=(S,T^+,\mu^+,F,R^+,E^+,H^+,C^+,P^+,N^+)$ be accepted
$\CauchyModulusOfContinuityUp$ carriers with the same source window $S$,
the same transformer row $F$, the same $\RegSeqRatUp$ readback route, and
the same $\RealUp$ seal route. Suppose that, for one displayed target
tolerance, the schedule $\mu^+$ asks for a larger finite source-window bound
than $\mu$ while preserving the transformer, readback, and seal rows. Then
every target-window read admitted by $K$ through $\mu$ is also admitted by
$K^+$ through $\mu^+$, using the same $\Cont$, $\Pkg$, and local
$\NameCert$ rows after the larger source-window inspection is replayed.
\end{theorem}
\begin{proof}
Project both carriers with
\autoref{def:cauchy-modulus-of-continuity-carrier}. The shared rows identify
the same source window, transformer route, regular-rational readback route,
and real-seal route; only the displayed source-window threshold is enlarged.

Use the threshold handoff of
\autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff}. It says that a
target-window read has a unique displayed route through the schedule before
the transformer row $F$ can produce $T$.

Replay the larger schedule $\mu^+$. Its source-window inspection contains the
smaller inspection required by $\mu$ and then continues through the same
transformer, readback, and seal rows. Thus the target-window read admitted by
$K$ is admitted again by $K^+$ after the larger finite source window has been
displayed.

Finally transport through $H^+$ and attach $C^+,P^+,N^+$. These rows preserve
the same continuation, provenance, and local NameCert route, so the enlarged
schedule creates no alternate host function, quotient stream, selected limit,
or ambient completeness coordinate.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusOfContinuityUp.CauchyModulusOfContinuityCarrier\_window\_monotonicity}

\begin{theorem}[Cauchy modulus of continuity schedule stability]
\label{thm:cauchy-modulus-of-continuity-schedule-stability}
Let $K=(S,T,\mu,F,R,E,H,C,P,N)$ and
$K'=(S',T',\mu',F',R',E',H',C',P',N')$ be accepted
$\CauchyModulusOfContinuityUp$ carriers. Suppose the $S,T,F,R,E$ rows of $K$
and the $S',T',F',R',E'$ rows of $K'$ are related componentwise by the
displayed $H,H'$ $\hsame$ transports, and suppose $\mu$ and $\mu'$ answer the
same target tolerance with transported finite source-window bounds. Then every
target-window read admitted through $\mu$ is admitted through $\mu'$ after
displayed $\Cont$ replay, with the same $\Pkg$ and local $\NameCert$ route.
\end{theorem}
\begin{proof}
Project both carriers through
\autoref{def:cauchy-modulus-of-continuity-carrier}. The displayed rows expose
only the source windows, target windows, schedules, transformer rows, readback
rows, Real seals, transport, replay, provenance, and local naming data.

Use the componentwise transports $H$ and $H'$. They identify the $S,T,F,R,E$
surface of $K$ with the $S',T',F',R',E'$ surface of $K'$ at the transported
finite source-window and target-window bounds.

Replay $C$ and $C'$ through the schedules before reading the transformer row.
The schedule rows $\mu$ and $\mu'$ answer the same target tolerance, so the
target-window request reaches the transformer surface only after the matching
finite source-window inspection has been displayed.

Apply the threshold handoff of
\autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff} and the finite-window
monotonicity of \autoref{thm:cauchy-modulus-of-continuity-window-monotonicity}.
They keep the admitted target-window read inside the transported schedule route
and allow the replay to use the transported finite source-window bound.

Finally attach the same provenance and local naming route. No quotient stream,
host function equality, selected limit, compactness, or completeness coordinate
is introduced, so schedule transport preserves the accepted read.
\end{proof}

\begin{theorem}[Cauchy modulus of continuity Real consumer boundary]
\label{thm:cauchy-modulus-of-continuity-real-consumer-boundary}
For every accepted $\CauchyModulusOfContinuityUp$ carrier
$K=(S,T,\mu,F,R,E,H,C,P,N)$, every $\RealUp$ consumer factors through the
displayed $\RegSeqRatUp$ readbacks $R$ and the $\RealUp$ seal row $E$. The
consumer surface exposes only the source and target windows, the schedule
$\mu$, the transformer row $F$, the readbacks $R$, the seal $E$, and the
recorded $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows; it exposes no
quotient stream, host function equality, selected limit, or ambient
completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusOfContinuityUp.CauchyModulusOfContinuityRealConsumerBoundary}
\begin{proof}
Read the carrier inventory. The only real-facing coordinate is $E$, and it is
listed as the seal row reached from the target $\RegSeqRatUp$ readback.

Use the handoff theorem
\autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff}. It shows that the
target readback has already passed through $S,\mu,F$, and $T$ before $R$ can
be consumed by a real-facing route.

Replay the seal route through $C$ and transport it through $H$. The package
row $P$ and the local name $N$ certify that the same displayed finite rows
source the read.

Thus every $\RealUp$ consumer factors through $R$ and $E$. The packet has no
coordinate from which to read a quotient stream, host function equality,
selected limit, or ambient completeness principle.
\end{proof}

\begin{theorem}[Cauchy modulus of continuity obligation-closure package]
\label{thm:cauchy-modulus-of-continuity-obligation-closure-package}
For every accepted $\CauchyModulusOfContinuityUp$ carrier
$K=(S,T,\mu,F,R,E,H,C,P,N)$, the carrier definition, the local NameCert
obligations, the $\CauchyContinuousMapUp$ threshold handoff, finite-window
monotonicity, and the $\RealUp$ consumer boundary jointly supply the full
obligation surface over $\StreamNameUp$, $\CauchyModulusUp$,
$\CauchyContinuousMapUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$. The package exposes exactly the displayed
source windows, target windows, threshold schedule, transformer route,
regular readbacks, real seal, transport rows, continuation replay,
provenance, and local naming data.
\end{theorem}
\begin{proof}
Start with \autoref{def:cauchy-modulus-of-continuity-carrier}. It fixes the
tuple $K=(S,T,\mu,F,R,E,H,C,P,N)$ and states that every visible coordinate is
one of the named BEDC rows.

Apply
\autoref{thm:cauchy-modulus-of-continuity-namecert-obligations}. The local
NameCert surface is exhausted by carrier admission, classifier transport,
componentwise stability, threshold-route exactness, replay, provenance, seal
factorization, and local naming.

Use
\autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff} and
\autoref{thm:cauchy-modulus-of-continuity-window-monotonicity} for the
consumer side. They force every target-window read to pass through
$S,\mu,F,T,R$ and show that enlarging a finite source window preserves the
same displayed route.

Finally apply
\autoref{thm:cauchy-modulus-of-continuity-real-consumer-boundary}. The real
consumer reaches only the $\RegSeqRatUp$ readback and $\RealUp$ seal after
the threshold route has been replayed. Hence the package excludes uniform
continuity, compactness, quotient streams, host function equality, selected
limits, and ambient completeness.
\end{proof}

\begin{theorem}[Cauchy modulus of continuity obligation exactness]
\label{thm:cauchy-modulus-of-continuity-obligation-exactness}
For every accepted $\CauchyModulusOfContinuityUp$ carrier
$K=(S,T,\mu,F,R,E,H,C,P,N)$, every $\CauchyContinuousMapUp$ or $\RealUp$
consumer reads the same displayed threshold schedule $\mu$, source window
$S$, transformer row $F$, $\RegSeqRatUp$ readback $R$, and $\RealUp$ seal
$E$. The obligation surface has no alternate compactness coordinate, host
function equality row, selected-limit witness, or second uniform-modulus
coordinate.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:cauchy-modulus-of-continuity-carrier}. The rows
$S,T,\mu,F,R,E,H,C,P,N$ are the only visible coordinates, so any consumer
read begins with the displayed schedule, window, transformer, readback, and
seal surface.

Use
\autoref{thm:cauchy-modulus-of-continuity-namecert-obligations}. Its local
NameCert clauses bind carrier admission, classifier transport, threshold
route exactness, replay, provenance, and local naming to those same rows.

Apply the threshold handoff of
\autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff}, the finite-window
monotonicity of
\autoref{thm:cauchy-modulus-of-continuity-window-monotonicity}, and the
schedule stability of
\autoref{thm:cauchy-modulus-of-continuity-schedule-stability}. Together they
force every $\CauchyContinuousMapUp$ threshold read to pass through $\mu$,
$S$, $F$, $T$, and $R$ with the same transported finite-window route.

Finally apply
\autoref{thm:cauchy-modulus-of-continuity-real-consumer-boundary}. The
$\RealUp$ consumer can reach the seal only after the readback route has
passed through the displayed threshold schedule, so no compactness, host
function equality, selected-limit, or alternate uniform-modulus row is
available.
\end{proof}

\begin{theorem}[Cauchy modulus threshold transport]
\label{thm:cauchy-modulus-of-continuity-threshold-transport}
For every accepted $\CauchyModulusOfContinuityUp$ carrier
$K=(S,T,\mu,F,R,E,H,C,P,N)$, the displayed threshold schedule $\mu$ is
transported through the $\StreamNameUp$ source window $S$, the target
$\StreamNameUp$ window $T$, the $\RegSeqRatUp$ readbacks $R$, the
$\DyadicRatCoreUp$ tolerance discipline used by those readbacks, and the
$\CauchyContinuousMapUp$ transformer row $F$ before any Real-facing consumer
reads the seal row $E$.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:cauchy-modulus-of-continuity-carrier}. The threshold schedule
$\mu$ is displayed between the source and target finite-window rows and the
transformer row, so it is visible before the regular rational readback and the
Real seal.

Apply the NameCert obligations of
\autoref{thm:cauchy-modulus-of-continuity-namecert-obligations}. They bind
carrier admission, classifier transport, componentwise stability, and
threshold-route exactness to the same finite surface.

Use the handoff theorem
\autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff}. It forces each
target-window read to pass through $S,\mu,F,T,R$ before the Real consumer can
reach $E$.
\end{proof}

\begin{theorem}[Cauchy modulus Real seal nonescape]
\label{thm:cauchy-modulus-of-continuity-real-seal-nonescape}
For every accepted $\CauchyModulusOfContinuityUp$ carrier
$K=(S,T,\mu,F,R,E,H,C,P,N)$, the $\RealUp$ seal row $E$ is reachable only
after the modulus row $\mu$, threshold route, finite-window rows $S,T$,
readback row $R$, componentwise $\hsame$ transport $H$, displayed $\Cont$
replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$ have been
displayed.
\end{theorem}
\begin{proof}
Start from \autoref{thm:cauchy-modulus-of-continuity-real-consumer-boundary}.
It places every Real-facing read behind the $\RegSeqRatUp$ readbacks and the
seal row of the same carrier.

Now use threshold transport from
\autoref{thm:cauchy-modulus-of-continuity-threshold-transport}. The target
readback cannot appear before $S,\mu,F,T$ and the dyadic tolerance route used
by $R$ are visible.

Finally read the structural rows $H,C,P,N$. They transport, replay, source,
and name the already displayed threshold route; none supplies a bypass around
$\mu,F,R$ or a detached Real seal.
\end{proof}

\begin{theorem}[Cauchy modulus local NameCert surface]
\label{thm:cauchy-modulus-of-continuity-local-namecert-surface}
The rows
$$
S,T,\mu,F,R,E,H,C,P,N
$$
exhaust the local $\NameCert_{\CauchyModulusOfContinuityUp}$ obligations for
an accepted packet. The packet excludes host continuity, selected limits,
quotient stream equality, countable choice, and ambient $\RealUp$
completeness.
\end{theorem}
\begin{proof}
Project the finite tuple in
\autoref{def:cauchy-modulus-of-continuity-carrier}. Its coordinates are
exactly the source and target windows, threshold schedule, transformer,
readbacks, Real seal, transport, replay, provenance, and local naming rows.

Apply
\autoref{thm:cauchy-modulus-of-continuity-namecert-obligations},
\autoref{thm:cauchy-modulus-of-continuity-threshold-transport}, and
\autoref{thm:cauchy-modulus-of-continuity-real-seal-nonescape}. Together they
show that carrier admission, classifier transport, threshold-route exactness,
Real-seal factorization, transport, replay, provenance, and local naming are
all read from the displayed rows.

Since the carrier contains no host continuity predicate, selected limit,
quotient stream, countable-choice schedule, or ambient completeness row, those
objects are outside the local NameCert surface.
\end{proof}

\closureat{CauchyModulusOfContinuityUp}{seedStr}
\closureat{CauchyModulusOfContinuityUp}{obligationStr}

\begin{closurestatus}{\CauchyModulusOfContinuityUp}
  \constructivestory{A $\CauchyModulusOfContinuityUp$ packet is generated from source and target $\StreamNameUp$ windows, a $\CauchyModulusUp$ threshold schedule for each requested target tolerance, a $\CauchyContinuousMapUp$ transformer row, $\RegSeqRatUp$ readbacks, a $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:cauchy-modulus-of-continuity-carrier}, \autoref{thm:cauchy-modulus-of-continuity-namecert-obligations}, \autoref{thm:cauchy-modulus-of-continuity-cauchy-handoff}, \autoref{thm:cauchy-modulus-of-continuity-window-monotonicity}, \autoref{thm:cauchy-modulus-of-continuity-schedule-stability}, and \autoref{thm:cauchy-modulus-of-continuity-real-consumer-boundary} bind the carrier, NameCert obligations, threshold handoff, window monotonicity, schedule stability, and Real-consumer boundary for the pointwise Cauchy-name continuity surface.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.CauchyModulusOfContinuityUp.CauchyModulusOfContinuityCarrier\_cauchy\_continuous\_map\_threshold\_handoff}
  \bridgestatus{none}
  \notclaimed{This chapter does not close uniform continuity, compactness, quotient stream equality, host function equality, selected limits, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, threshold-route exactness, Real-consumer factorization, and TasteGate nontriviality with field faithfulness for $S,T,\mu,F,R,E,H,C,P,N$.}
\end{closurestatus}
