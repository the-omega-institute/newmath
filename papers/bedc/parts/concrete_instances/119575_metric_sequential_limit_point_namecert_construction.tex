\chapter{A Concrete Naming Certificate for $\MetricSequentialLimitPointUp$}
\label{ch:concrete-instances-metric-sequential-limit-point-namecert}
\origin{human}

Following Bishop and Bridges on metric sequential compactness, the $\MetricSequentialLimitPointUp$ packet records the general metric route by which a displayed sequence, a cofinal subsequence schedule, and a candidate point form finite limit-point evidence before any Real-specific cluster packet reads the result. It sits between the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the Real-only cluster packet of \autoref{ch:concrete-instances-real-sequence-cluster-point-namecert}. The packet gives compactness and completion consumers a metric sequential limit-point interface without folding the concept into the Real sequence cluster-point carrier.

\begin{definition}[Metric sequential limit-point carrier]
\label{def:metric-sequential-limit-point-carrier}
A $\MetricSequentialLimitPointUp$ carrier is a finite $\BHist$ packet
$$
L=(M,S,A,D,R,E,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the source $\MetricUp$ row, including the point classifier, distance-observation rows, and finite metric-law ledger used by the sequence.
\item $S$ is the $\StreamNameUp$ sequence row, exposing only the inspected windows of the displayed metric sequence.
\item $A$ is the cofinal schedule row, recording the finite subsequence indices and monotonicity comparisons used by the limit-point request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the inspected radius windows.
\item $R$ is the $\RegSeqRatUp$ distance-readback row, reading the distances $d(x_{a_i},p)$ through regular rational approximants.
\item $E$ is the terminal $\RealUp$ seal for those distance-readback comparisons.
\item $W$ is the limit-window witness row, recording that the displayed scheduled tail enters every inspected tolerance window around the candidate point.
\item $H$ records componentwise $\hsame$ transport for the metric source, stream windows, cofinal schedule, dyadic tolerance ledger, regular rational readback, Real seal, limit-window witness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
M,S,A,D,R,E,W\longmapsto \NameCert_{\MetricSequentialLimitPointUp}.
$$
\item $P$ is the $\Pkg$ provenance over $\MetricSequentialLimitPointUp$, $\MetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RealSequenceClusterPointUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\MetricSequentialLimitPointUp}$ row.
\end{enumerate}
The carrier contains no selected subsequence functional, no selected cluster point beyond the displayed candidate row, no quotient stream equality, no countable-choice principle, and no ambient compactness or completeness theorem.
\end{definition}

\begin{theorem}[Metric sequential limit-point NameCert obligations]
\label{thm:metric-sequential-limit-point-namecert-obligations}
For every accepted $\MetricSequentialLimitPointUp$ carrier $L=(M,S,A,D,R,E,W,H,C,P,N)$, the local $\NameCert_{\MetricSequentialLimitPointUp}$ obligation surface consists exactly of carrier admission, metric-source visibility, finite stream-window visibility, cofinal schedule compatibility, dyadic tolerance discipline, regular rational distance readback, terminal Real sealing, limit-window exactness, componentwise transport, displayed replay, provenance non-escape, and local naming for the rows $M,S,A,D,R,E,W,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier. The rows available to a consumer are precisely the metric source, finite stream windows, cofinal schedule, dyadic tolerances, regular rational readbacks, Real seal, limit-window witness, and the structural rows.

Metric-source and stream-window visibility follow from $M$ and $S$. The schedule row $A$ may index only windows already named by $S$, and the tolerance row $D$ supplies the displayed radii used by the limit-window request.

The readback and seal rows are downstream. Row $R$ reads distances through $\RegSeqRatUp$, and row $E$ seals those comparisons through $\RealUp$ only after the dyadic tolerance ledger has been displayed.

Finally inspect $W,H,C,P,N$. The window row records the finite limit-point evidence, while transport, replay, provenance, and local naming preserve exactly the same packet. Since no excluded selector, quotient, choice, compactness, or completeness row is present, the listed obligations exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Metric sequential limit-point stream handoff]
\label{thm:metric-sequential-limit-point-stream-handoff}
Every downstream consumer that passes from $\MetricSequentialLimitPointUp$ to a Real-specific cluster or compactness packet must first read the finite handoff
$$
\MetricUp,\quad
\StreamNameUp,\quad
\DyadicRatCoreUp,\quad
\RegSeqRatUp,\quad
\RealUp,\quad
\BHist,\quad
\hsame,\quad
\Cont,\quad
\Pkg,\quad
\NameCert .
$$
In particular, the Real sequence cluster-point route of \autoref{ch:concrete-instances-real-sequence-cluster-point-namecert} may consume the metric limit-window witness only after the stream windows, regular rational readbacks, dyadic tolerances, and terminal Real seal have been displayed.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:metric-sequential-limit-point-namecert-obligations}. It places the metric source, finite stream windows, cofinal schedule, dyadic tolerance ledger, regular rational readback, and terminal Real seal before the limit-window witness can be exported.

The stream handoff is therefore finite. A Real-specific cluster consumer reads the $\StreamNameUp$ windows first, then the $\DyadicRatCoreUp$ tolerances, the $\RegSeqRatUp$ distance readbacks, and the $\RealUp$ seal.

Transport, replay, provenance, and local naming keep the same rows visible at the handoff boundary. Because the carrier has no selector, quotient, countable-choice, compactness, or ambient-completeness coordinate, no downstream consumer can bypass the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\MetricSequentialLimitPointUp$ limit-point export factors through one metric source row, one finite stream-window row, one cofinal schedule row, one dyadic tolerance ledger, one regular rational distance-readback row, one terminal Real seal, one limit-window witness row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{metric, streamname, regseqrat, dyadicratcore, real, real-sequence-cluster-point: $\MetricSequentialLimitPointUp$ is not bijective to any listed sibling because it jointly carries the metric source, sequence windows, cofinal schedule, dyadic tolerance ledger, regular rational distance readback, terminal Real seal, limit-window witness, transport, replay, provenance, and local naming rows for a general metric sequence.}

\closureat{MetricSequentialLimitPointUp}{seedStr}

\begin{closurestatus}{\MetricSequentialLimitPointUp}
  \constructivestory{A $\MetricSequentialLimitPointUp$ packet is generated from a metric source row, finite stream windows, a cofinal schedule, dyadic tolerance ledger, regular rational distance readbacks, a terminal Real seal, a finite limit-window witness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metric-sequential-limit-point-carrier}, \autoref{thm:metric-sequential-limit-point-namecert-obligations}, and \autoref{thm:metric-sequential-limit-point-stream-handoff} seed the general metric sequential limit-point packet over Metric, StreamName, DyadicRatCore, RegSeqRat, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close sequential compactness, Bolzano-Weierstrass, selected subsequence extraction, selected cluster points, quotient stream equality, countable choice, ambient compactness, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, stream-window visibility, cofinal schedule compatibility, dyadic tolerance discipline, regular rational distance readback, Real seal exactness, limit-window non-escape, componentwise transport, replay, provenance, and local naming for BEDC.Derived.MetricSequentialLimitPointUp.}
\end{closurestatus}
