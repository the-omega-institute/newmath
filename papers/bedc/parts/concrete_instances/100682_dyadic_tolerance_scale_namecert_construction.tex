\chapter{A Concrete Naming Certificate for $\DyadicToleranceScaleUp$}
\label{ch:concrete-instances-dyadic-tolerance-scale-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\DyadicToleranceScaleUp$ packet records the single dyadic precision scale row that a regular rational or real-name consumer must read before turning finite stream observations into endpoint-facing data. It sits over the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the Cauchy threshold discipline of \autoref{ch:concrete-instances-cauchymodulus-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives Real and Completion routes one visible scale certificate instead of spreading the same precision ledger across adjacent dyadic, modulus, and readback chapters.

\begin{definition}[Dyadic tolerance scale carrier]
\label{def:dyadic-tolerance-scale-carrier}
A $\DyadicToleranceScaleUp$ carrier is a finite $\BHist$ packet
$$
T=(s,d,w,r,m,e,h,c,p,n)
$$
with the following displayed rows:
\begin{enumerate}
\item $s$ is the scale row. It records the finite dyadic precision index, the displayed tolerance requested at that index, and the monotone relation from coarser requests to finer requests.
\item $d$ is the $\DyadicRatCoreUp$ tolerance ledger attached to $s$, including the dyadic radius, rounding, comparison, and containment rows used by the scale.
\item $w$ is the $\StreamNameUp$ finite-window row opened at the precision named by $s$ and budgeted by $d$.
\item $r$ is the $\RegSeqRatUp$ readback row obtained only after the scale row, dyadic ledger, and stream window have been displayed.
\item $m$ is the $\CauchyModulusUp$ threshold row saying that the same scale controls the finite tail bound consumed by regular readback.
\item $e$ is the terminal $\RealUp$ handoff row exposed only after $s,d,w,r,m$ have been read.
\item $h$ records componentwise $\hsame$ transport for the scale row, dyadic ledger, stream window, regular readback, Cauchy threshold, Real handoff, replay, provenance, and local naming rows.
\item $c$ records displayed $\Cont$ replay from the scale request through the dyadic ledger, stream window, regular readback, Cauchy threshold, Real handoff, transport, provenance, and local naming rows.
\item $p$ is the $\Pkg$ provenance over $\DyadicToleranceScaleUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchyModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $n$ is the local $\NameCert_{\DyadicToleranceScaleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $s,d,w,r,m,e,h,c,p,n$. It has no coordinate for quotient stream equality, selected limits, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}
\leandef{BEDC.Derived.DyadicToleranceScaleUp.DyadicToleranceScaleUp}

\begin{theorem}[Dyadic tolerance scale NameCert obligations]
\label{thm:dyadic-tolerance-scale-namecert-obligations}
For every accepted $\DyadicToleranceScaleUp$ carrier $T=(s,d,w,r,m,e,h,c,p,n)$, the local $\NameCert_{\DyadicToleranceScaleUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed scale row, DyadicRatCore tolerance ledger, StreamName window, RegSeqRat readback, CauchyModulus threshold, Real handoff, transport, replay, provenance, and local naming rows;
\item scale monotonicity, requiring the row $s$ to order coarser and finer dyadic precision requests before any ledger, window, readback, threshold, or seal row can be consumed;
\item classifier transport, requiring accepted comparisons to preserve $s,d,w,r,m,e,h,c,p,n$ componentwise and to expose no hidden precision coordinate;
\item RegSeqRat handoff, requiring the readback row $r$ to consume the displayed scale, dyadic ledger, and stream window rather than a detached stream quotient or selected limit;
\item Real-seal non-escape, requiring the row $e$ to remain terminal with respect to $s,d,w,r,m$ and to export no host equality, countable choice, trichotomy, ambient completeness theorem, Lean verification, or bridge data;
\item provenance and local naming, requiring $p$ and $n$ to package exactly the displayed rows and no additional completion datum.
\end{enumerate}
No dyadic tolerance-scale obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:dyadic-tolerance-scale-carrier}. The visible packet consists exactly of the scale row $s$, dyadic ledger $d$, stream window $w$, regular readback $r$, Cauchy threshold $m$, Real handoff $e$, and structural rows $h,c,p,n$.

Scale monotonicity is the local content of $s$. Since $s$ is the only row carrying the precision index and the coarser-to-finer relation, every downstream precision comparison must read it before any ledger, window, readback, threshold, or seal can be named.

Classifier transport is componentwise through $h$ and replayed through $c$. These rows preserve the displayed route from $s$ to $d$, from $d$ to $w$, from $w$ to $r$, from $r$ to $m$, and from $m$ to $e$.

The RegSeqRat handoff and Real-seal clauses follow from the placement of $r$ and $e$. The readback row is downstream of the displayed scale, ledger, and window, while the Real handoff is terminal after the same route and the Cauchy threshold row.

The excluded objects are absent from the carrier and are not produced by transport, replay, provenance, or local naming. Hence no local NameCert obligation can be stored outside the displayed rows.
\end{proof}

\begin{theorem}[Dyadic tolerance scale RegSeqRat handoff]
\label{thm:dyadic-tolerance-scale-regseqrat-handoff}
Let $T=(s,d,w,r,m,e,h,c,p,n)$ be an accepted $\DyadicToleranceScaleUp$ carrier. Every $\RegSeqRatUp$ or $\RealUp$ consumer of $T$ must read the finite route
$$
s \longmapsto d \longmapsto w \longmapsto r \longmapsto m \longmapsto e \longmapsto h,c,p,n
$$
before the regular readback and before the Real seal are available. Consequently the dyadic scale row is the unique precision gate for those consumers, and no consumer can bypass it through quotient stream equality, a selected limit, host real equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicToleranceScaleUp.DyadicToleranceScaleRegSeqRatHandoff}
\begin{proof}
Project the accepted carrier. The only readback coordinate is $r$, and the only Real-facing coordinate is $e$.

The readback coordinate is not free-standing. By \autoref{def:dyadic-tolerance-scale-carrier}, $r$ is obtained after the scale row $s$, the DyadicRatCore ledger $d$, and the StreamName window $w$ have been displayed.

The Real-facing coordinate is also not free-standing. The row $e$ is downstream of $r$ and the CauchyModulus threshold row $m$, so a Real consumer reaches it only after the same scale, ledger, window, and regular readback route has already been read.

Finally, $h,c,p,n$ preserve and name that route without adding a second precision coordinate. Since the carrier has no excluded coordinate, every RegSeqRat or Real consumer must pass through the dyadic scale row before readback and seal.
\end{proof}

\begin{theorem}[Dyadic tolerance scale precision-gate exhaustion]
\label{thm:dyadic-tolerance-scale-precision-gate-exhaustion}
Let $T=(s,d,w,r,m,e,h,c,p,n)$ be an accepted $\DyadicToleranceScaleUp$ carrier. Every $\RegSeqRatUp$ or $\RealUp$ consumer of $T$ must pass through the scale row $s$, the $\DyadicRatCoreUp$ tolerance ledger $d$, the $\StreamNameUp$ finite window $w$, the $\CauchyModulusUp$ threshold row $m$, the $\RegSeqRatUp$ readback row $r$, the $\RealUp$ seal $e$, and the structural rows $h,c,p,n$. No quotient stream equality, selected limit, host equality, countable choice, trichotomy, or ambient $\RealUp$ completeness coordinate is available to such a consumer.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-tolerance-scale-carrier}. The visible rows are exactly the scale, dyadic ledger, finite window, regular readback, Cauchy threshold, Real seal, and structural rows $s,d,w,r,m,e,h,c,p,n$.

Apply \autoref{thm:dyadic-tolerance-scale-namecert-obligations}. The scale monotonicity row must be read before any ledger, window, readback, threshold, or seal row, and the classifier transport exposes no hidden precision coordinate.

Now apply \autoref{thm:dyadic-tolerance-scale-regseqrat-handoff}. It forces each RegSeqRat or Real consumer through the route from $s$ to $d$, then $w$, $r$, $m$, $e$, and finally $h,c,p,n$.

Because the carrier and the handoff route contain no quotient, selected-limit, host-equality, choice, trichotomy, or completeness row, the scale row exhausts the precision gate for every accepted consumer.
\end{proof}

\begin{theorem}[Dyadic tolerance scale obligation-closure scope]
\label{thm:dyadic-tolerance-scale-obligation-closure-scope}
Let $T=(s,d,w,r,m,e,h,c,p,n)$ be an accepted $\DyadicToleranceScaleUp$ carrier. Every accepted obligation read of $T$ is exhausted by the scale row $s$, the $\DyadicRatCoreUp$ tolerance ledger $d$, the $\StreamNameUp$ finite window $w$, the $\CauchyModulusUp$ threshold row $m$, the $\RegSeqRatUp$ readback row $r$, the $\RealUp$ seal $e$, componentwise $\hsame$ transport $h$, displayed $\Cont$ replay $c$, $\Pkg$ provenance $p$, and the local $\NameCert_{\DyadicToleranceScaleUp}$ row $n$. The obligation surface exports no quotient stream equality, selected limit, countable choice, trichotomy, host equality, ambient $\RealUp$ completeness, or bridge data.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:dyadic-tolerance-scale-carrier}. Its visible coordinates are exactly $s,d,w,r,m,e,h,c,p,n$, with $s$ naming the dyadic precision scale and $d,w,m,r,e$ forming the finite route toward regular readback and the Real seal.

Apply \autoref{thm:dyadic-tolerance-scale-namecert-obligations}. The obligation rows are carrier admission, scale monotonicity, classifier transport, RegSeqRat handoff, Real-seal non-escape, provenance, and local naming, and each of those rows is stored on the displayed coordinates of the carrier.

Use \autoref{thm:dyadic-tolerance-scale-regseqrat-handoff} together with \autoref{thm:dyadic-tolerance-scale-precision-gate-exhaustion}. They force every RegSeqRat or Real consumer through the same scale, DyadicRatCore ledger, StreamName window, CauchyModulus threshold, RegSeqRat readback, Real seal, transport, replay, package, and naming route.

It remains only to identify the boundary. The carrier has no coordinate for quotient stream equality, a selected limit, countable choice, trichotomy, host equality, ambient Real completeness, or bridge data, and the obligation rows do not construct such a coordinate. Therefore the displayed rows exhaust the obligation-closure surface.
\end{proof}

\begin{theorem}[Dyadic tolerance scale Real regular readback]
\label{thm:dyadic-tolerance-scale-real-regular-readback}
Let $T=(s,d,w,r,m,e,h,c,p,n)$ be an accepted $\DyadicToleranceScaleUp$ carrier. Every $\RealUp$-facing read from $T$ factors through the dyadic scale row $s$, the $\StreamNameUp$ finite window $w$, the $\CauchyModulusUp$ threshold row $m$, the $\RegSeqRatUp$ readback row $r$, the terminal $\RealUp$ seal $e$, componentwise $\hsame$ transport $h$, displayed $\Cont$ replay $c$, $\Pkg$ provenance $p$, and the local $\NameCert_{\DyadicToleranceScaleUp}$ row $n$. Consequently a Real-facing consumer receives a regular rational readback only through the displayed dyadic scale route, not through quotient stream equality, a selected limit, host real equality, countable choice, trichotomy, ambient $\RealUp$ completeness, or bridge data.
\end{theorem}
\begin{proof}
Start with \autoref{thm:dyadic-tolerance-scale-regseqrat-handoff}. It places every RegSeqRat and Real consumer downstream of the route from the scale row $s$ through the dyadic ledger $d$, the finite stream window $w$, the regular readback $r$, the Cauchy threshold $m$, the Real seal $e$, and the structural rows $h,c,p,n$.

Apply \autoref{thm:dyadic-tolerance-scale-precision-gate-exhaustion}. The scale row is the unique precision gate for those consumers, and the displayed dyadic ledger, StreamName window, Cauchy threshold, RegSeqRat readback, Real seal, transport, replay, provenance, and naming rows are the only rows available for the read.

Finally use \autoref{thm:dyadic-tolerance-scale-obligation-closure-scope}. Its closure scope says that the same rows exhaust every accepted obligation read of the packet. Therefore a Real-facing read factors through the regular readback and Real seal exactly along the local NameCert route, with no bypass through an excluded quotient, selected limit, host-equality, choice, completeness, or bridge coordinate.
\end{proof}

\closureat{DyadicToleranceScaleUp}{obligationStr}

\begin{closurestatus}{\DyadicToleranceScaleUp}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:dyadic-tolerance-scale-carrier}, \autoref{thm:dyadic-tolerance-scale-namecert-obligations}, \autoref{thm:dyadic-tolerance-scale-regseqrat-handoff}, \autoref{thm:dyadic-tolerance-scale-precision-gate-exhaustion}, and \autoref{thm:dyadic-tolerance-scale-obligation-closure-scope} close the finite dyadic precision-scale obligation surface from a scale row through dyadic ledger, StreamName window, Cauchy threshold, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{The next step is Lean verification of the displayed obligation-closure surface for BEDC.Derived.DyadicToleranceScaleUp.}
  \constructivestory{A $\DyadicToleranceScaleUp$ packet is generated from one dyadic scale row, a DyadicRatCore tolerance ledger, a StreamName finite window, RegSeqRat readback, a CauchyModulus threshold, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
