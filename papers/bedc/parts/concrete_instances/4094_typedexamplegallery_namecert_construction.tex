\chapter{A Concrete Naming Certificate for $\TypedExampleGalleryUp$}
\label{ch:concrete-instances-typedexamplegallery-namecert}
\origin{human}

The $\TypedExampleGalleryUp$ packet realizes the audit reading of \autoref{ch:metacic-typedexamples-audit-map} as finite BEDC data. Its source material is the existing MetaCIC typed-example corpus: Church booleans and naturals, pairs and options, lists and sums, identity and polymorphic identity rows, application forms, beta redexes, projections, Pi forms, constants, and sort-indexed context examples. The packet sits after the closed substitution boundary of \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}, the derivation route of \autoref{ch:concrete-instances-typecheckingderivationtrace-namecert}, and the proof-erasure route of \autoref{ch:concrete-instances-prooftermerasuretrace-namecert}. It names the gallery as a finite regression surface without turning the examples into subject reduction, normalisation, confluence, or global conversion theorems.

\begin{definition}[Typed example gallery carrier]
\label{def:typed-example-gallery-carrier}
A $\TypedExampleGalleryUp$ carrier is a finite $\BHist$ packet
$$
G=(E,T,C,B,R,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the finite gallery-entry row for the accepted Church, identity, application, beta-redex, projection, Pi, constant, and sort-context examples;
\item $T$ is the type-checking row recording each displayed $\mathsf{HasType}$ or checker-accepted example;
\item $C$ is the closedness row recording the $\mathsf{ClosedAt}$ inputs consumed by the typed examples and computation rows;
\item $B$ is the beta-computation row for the selected redex, eliminator, boolean, natural-number, list, sum, and equality examples that compute inside the gallery;
\item $R$ is the regression-surface row recording the finite families whose preservation is required when checker, closedness, substitution, beta, projection, or binder code is changed;
\item $H$ records componentwise $\hsame$ transport across $E,T,C,B,R,K,P,N$;
\item $K$ records the displayed $\Cont$ routes by which consumers replay gallery entry reads, checker reads, closedness reads, beta reads, and regression-surface reads;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:metacic-typedexamples-audit-map}, \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}, \autoref{ch:concrete-instances-typecheckingderivationtrace-namecert}, and \autoref{ch:concrete-instances-prooftermerasuretrace-namecert};
\item $N$ is the local $\NameCert_{\TypedExampleGalleryUp}$ row.
\end{enumerate}
The carrier has no coordinate for an arbitrary MetaCIC term, no global type-safety theorem, no closed-term normalisation theorem, no confluence theorem, no proof-erased compiler theorem, and no host evaluator.
\end{definition}

\begin{theorem}[Typed example gallery NameCert obligations]
\label{thm:typed-example-gallery-namecert-obligations}
For the carrier of \autoref{def:typed-example-gallery-carrier}, the local $\NameCert_{\TypedExampleGalleryUp}$ surface consists of five finite obligations:
\begin{enumerate}
\item carrier admission for $G=(E,T,C,B,R,H,K,P,N)$;
\item checker visibility through the displayed type-checking row $T$ for every accepted gallery entry in $E$;
\item closedness visibility through $C$ for each example whose typing or computation row consumes a $\mathsf{ClosedAt}$ input;
\item beta visibility through $B$ for the selected computation rows and no other conversion surface;
\item regression-surface visibility through $R$, transported only by $H,K,P,N$ and grounded in the sibling provenance listed in $P$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the finite packet $G$. Carrier admission supplies the displayed entry, checker, closedness, beta, regression, transport, replay, provenance, and local naming rows.

The checker obligation reads $T$ only against entries already present in $E$. The closedness obligation reads $C$ only for entries whose displayed theorem rows require $\mathsf{ClosedAt}$ inputs. The beta obligation reads $B$ only for the selected computations in the gallery.

Finally read $R,H,K,P,N$. The regression row names the finite families whose checked behavior is preserved as a gallery surface, while transport, replay, provenance, and local naming keep every consumer inside the same displayed packet. No row adds a general subject-reduction, normalisation, confluence, global conversion, proof-erasure, or host-evaluation endpoint.
\end{proof}

\begin{theorem}[Typed example gallery obligation frontier]
\label{thm:typed-example-gallery-obligation-frontier}
Every obligation consumer of an accepted $\TypedExampleGalleryUp$ packet must expose the rows
$$
E,T,C,B,R,H,K,P,N
$$
in that order before any gallery result is public. The entry row $E$ is read before checker visibility in $T$; checker visibility is read before closedness in $C$; closedness is read before the selected beta row $B$; beta rows are read before the regression surface $R$; and only then may componentwise transport $H$, replay $K$, provenance $P$, and the local $\NameCert_{\TypedExampleGalleryUp}$ row $N$ be consumed. The frontier exports the finite gallery obligation surface only, not subject reduction, normalization, confluence, or conversion decidability.
\end{theorem}
\begin{proof}
Project the carrier $G=(E,T,C,B,R,H,K,P,N)$ from \autoref{def:typed-example-gallery-carrier}. Its coordinates already list the entry, checker, closedness, beta, regression, transport, replay, provenance, and local naming rows in the required order.

Apply \autoref{thm:typed-example-gallery-namecert-obligations}. Carrier admission exposes the whole packet, checker visibility uses $T$ against entries in $E$, closedness visibility uses $C$ only for displayed inputs, beta visibility uses $B$ only for selected computations, and regression visibility uses $R$ through $H,K,P,N$.

Finally use \autoref{thm:typed-example-gallery-regression-surface} and \autoref{thm:typed-example-gallery-typed-normal-form-separation}. These keep the public read inside the finite regression and displayed normal-form surface. Since the carrier has no coordinate for arbitrary term reduction, global normalization, confluence, or conversion decidability, none of those conclusions can cross the obligation frontier.
\end{proof}

\begin{theorem}[Typed example gallery regression surface]
\label{thm:typed-example-gallery-regression-surface}
Every consumer of an accepted $\TypedExampleGalleryUp$ packet receives a finite regression surface for the typed-example corpus: accepted typing rows, closedness inputs, selected beta computations, and sibling provenance through \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}, \autoref{ch:concrete-instances-typecheckingderivationtrace-namecert}, and \autoref{ch:concrete-instances-prooftermerasuretrace-namecert}. The packet cannot be reread as a theorem about all represented terms, all closed substitutions, all beta steps, all erased proof terms, or all checker results.
\end{theorem}
\begin{proof}
The regression surface is the row $R$ together with the entry, checker, closedness, and beta rows $E,T,C,B$. These rows are finite and are indexed by the gallery families recorded in \autoref{ch:metacic-typedexamples-audit-map}.

The sibling references in $P$ locate the required substrate: closed substitution supplies the closed-term boundary, derivation traces supply finite checker routes, and proof-erasure traces supply the erased-readback discipline. The packet records dependence on those surfaces without merging their theorem rows into the gallery.

Since the carrier contains no row for an arbitrary represented term, arbitrary closed substitution, arbitrary beta step, arbitrary proof erasure, or arbitrary checker result, the consumer cannot export a global metatheorem. The accepted reading is exactly the finite gallery regression surface.
\end{proof}

\begin{theorem}[Typed example gallery closed beta regression route]
\label{thm:typed-example-gallery-closed-beta-regression-route}
Every closed beta example in an accepted $\TypedExampleGalleryUp$ packet routes through the displayed entry row $E$, type row $T$, closedness row $C$, beta row $B$, regression row $R$, and sibling provenance to \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}, \autoref{ch:concrete-instances-typecheckingderivationtrace-namecert}, and \autoref{ch:concrete-instances-prooftermerasuretrace-namecert}. The route records a finite gallery obligation only: it does not assert subject reduction, strong normalisation, confluence, global conversion decidability, proof-erased compiler correctness, arbitrary checker completeness, or a host evaluator.
\end{theorem}
\begin{proof}
Project the carrier $G=(E,T,C,B,R,H,K,P,N)$ from \autoref{def:typed-example-gallery-carrier}. A closed beta example is first an entry of $E$, and its displayed type evidence is read from $T$ rather than from a host checker theorem.

Read the closedness and computation rows next. The closedness row $C$ supplies exactly the $\mathsf{ClosedAt}$ inputs consumed by the example, and the beta row $B$ supplies the selected computation surface for the displayed redex or eliminator.

Finally apply the NameCert obligations and the regression surface theorem. The row $R$ records the regression family, while $P$ keeps the closed-substitution boundary, type-checking derivation trace, and proof-erasure trace as sibling provenance. Transport, replay, provenance, and local naming through $H,K,P,N$ preserve the same finite route and add no global metatheorem.
\end{proof}

\begin{theorem}[Typed example gallery normal-form separation]
\label{thm:typed-example-gallery-typed-normal-form-separation}
Every accepted $\TypedExampleGalleryUp$ packet separates displayed normal-form examples from global normalization. A public normal-form read must pass through the displayed entry row $E$, type row $T$, closedness row $C$, beta row $B$, regression row $R$, and sibling provenance $P$ to \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}, \autoref{ch:concrete-instances-typecheckingderivationtrace-namecert}, and \autoref{ch:concrete-instances-prooftermerasuretrace-namecert}. No consumer may infer strong normalization, confluence, or global conversion decidability from that finite gallery route.
\end{theorem}
\begin{proof}
Project the carrier $G=(E,T,C,B,R,H,K,P,N)$ from \autoref{def:typed-example-gallery-carrier}. A normal-form example is visible only as an entry in $E$, with its accepted type evidence read from $T$ and any closedness premise read from $C$.

The computation-facing information is still finite gallery data. The beta row $B$ records only the selected examples, while $R$ records the regression family used to keep those examples stable as displayed tests.

Finally read the sibling provenance row $P$. It routes the displayed examples through the closed-substitution boundary, typechecking-derivation trace, and proof-erasure trace already named by the chapter, while $H,K,N$ preserve transport, replay, and local naming. Since the carrier has no coordinate for arbitrary terms, a global evaluator, confluence, strong normalization, or global conversion decidability, none of those conclusions can be exported.
\end{proof}

\begin{theorem}[Typed example gallery scoped kernel route]
\label{thm:typed-example-gallery-scoped-kernel-route}
Every scoped kernel read of an accepted $\TypedExampleGalleryUp$ packet is confined to the displayed rows
$$
E,T,C,B,R,H,K,P,N
$$
and to the three sibling chapters \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}, \autoref{ch:concrete-instances-typecheckingderivationtrace-namecert}, and \autoref{ch:concrete-instances-prooftermerasuretrace-namecert}. Carrier admission, checker visibility, closedness visibility, beta visibility, regression-surface non-escape, and sibling provenance are scoped to those finite rows and cited siblings; they do not assert global subject reduction, normalization, confluence, conversion decidability, proof-erased compiler correctness, arbitrary checker completeness, or host evaluation.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:typed-example-gallery-carrier}. The rows $E,T,C,B,R$ are the only gallery, checker, closedness, beta, and regression rows available before the structural data are read.

Apply the NameCert obligations and the obligation frontier. These force checker visibility through $T$, closedness visibility through $C$, beta visibility through $B$, and regression-surface visibility through $R$ before $H,K,P,N$ can transport, replay, source, or name the packet.

The sibling provenance is exactly the three displayed chapters named in the statement. The closed-substitution boundary supplies the closedness source, the typechecking-derivation trace supplies checker routes, and the proof-term-erasure trace supplies proof-facing readback discipline.

Finally use the regression and normal-form separation theorems. They keep all public reads inside the finite gallery surface, so none of the global MetaCIC conclusions can be exported from the scoped kernel route.
\end{proof}

\closureat{TypedExampleGalleryUp}{obligationStr}

\begin{closurestatus}{\TypedExampleGalleryUp}
  \constructivestory{A $\TypedExampleGalleryUp$ packet is generated from finite typed-example entries, checker rows, closedness rows, selected beta-computation rows, a regression-surface row, componentwise transport, continuation routes, package provenance, and a local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:typed-example-gallery-carrier}, \autoref{thm:typed-example-gallery-namecert-obligations}, \autoref{thm:typed-example-gallery-regression-surface}, \autoref{thm:typed-example-gallery-closed-beta-regression-route}, and \autoref{thm:typed-example-gallery-typed-normal-form-separation} bind the finite gallery-entry, checker, closedness, beta, regression, sibling-provenance, normal-form separation, and local naming surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close subject reduction, strong normalisation, confluence, global conversion decidability, proof-erased compiler correctness, arbitrary checker completeness, or bridge checking.}
  \upgradepath{Scoped closure requires binding carrier admission, checker visibility, closedness visibility, beta visibility, regression-surface non-escape, and sibling provenance to their displayed kernel dependencies.}
\end{closurestatus}
