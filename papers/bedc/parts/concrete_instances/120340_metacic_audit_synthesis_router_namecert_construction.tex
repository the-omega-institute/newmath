\chapter{A Concrete Naming Certificate for $\MetaCICAuditSynthesisRouterUp$}
\label{ch:concrete-instances-metacic-audit-synthesis-router-namecert}
\origin{ai}

The $\MetaCICAuditSynthesisRouterUp$ packet realizes the audit-synthesis route of \autoref{ch:visions-subhub-metacic} as one finite router over subject-reduction, confluence, normalization, substitution, decidability, blocker, and open-input rows. It sits beside the audit-synthesis packet of \autoref{ch:concrete-instances-metacicauditsynthesispacket-namecert}, the critical-path packet of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}, the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, and the discharge-obligation ledger of \autoref{ch:concrete-instances-metacic-discharge-obligation-ledger-namecert}. The router names a finite audit handoff; it preserves the open-input boundary instead of claiming subject reduction, confluence, normalization, decidability, closed consistency, quotient syntax equality, evaluator soundness, or MetaCIC kernel consistency.

\begin{definition}[MetaCIC audit-synthesis router carrier]
\label{def:metacic-audit-synthesis-router-carrier}
A $\MetaCICAuditSynthesisRouterUp$ carrier is a finite $\BHist$ packet
$$
R=(S,C,N,U,D,B,O,K,F,H,T,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the subject-reduction route row, recording the obstruction boundary and the discharge rows that remain required before closed subject reduction could be consumed.
\item $C$ is the confluence route row, recording the conditional confluence audit surface, critical-pair rows, local-join rows, and retained residual-substitution premises.
\item $N$ is the normalization route row, recording candidate-mediated strong-normalization evidence, bounded normal endpoints, and the normal-frontier handoff.
\item $U$ is the substitution route row, recording closed-substitution, binder-budget, residual, and open-input side conditions that must remain visible to the router.
\item $D$ is the decidability route row, recording bounded conversion, typed-checker, and finite observation data without turning them into global decidability.
\item $B$ is the blocker row, recording the closed-normal branch, the subject-reduction obstruction, and the consistency boundary that prevents the route from becoming a closed consistency theorem.
\item $O$ is the open-input row, recording inputs, premises, sockets, and side conditions that are intentionally not discharged by the router.
\item $K$ is the critical-path row, read through $\MetaCICCriticalPathUp$ as the source ledger tying the preceding rows to the MetaCIC audit-synthesis route.
\item $F$ is the field-faithfulness readiness row, recording that future TasteGate work must keep $S,C,N,U,D,B,O,K,H,T,P,L$ as separate carrier fields.
\item $H$ records componentwise $\hsame$ transport for every route row, the critical-path row, field-faithfulness readiness, replay, provenance, and local naming.
\item $T$ records displayed $\Cont$ replay from the audit-synthesis request through the seven route rows, the critical-path source, the open-input boundary, and the local name.
\item $P$ is the $\Pkg$ provenance over the audit-synthesis packet, critical-path packet, confluence audit packet, normalization frontier, discharge-obligation ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $L$ is the local $\NameCert_{\MetaCICAuditSynthesisRouterUp}$ row.
\end{enumerate}
The classifier compares accepted routers only through $S,C,N,U,D,B,O,K,F,H,T,P,L$. It has no coordinate for premise-free subject reduction, unrestricted Church--Rosser, candidate-free normalization, global beta-conversion decidability, quotient syntax equality, host evaluator soundness, closed consistency, or MetaCIC kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC audit-synthesis router obligations]
\label{thm:metacic-audit-synthesis-router-namecert-obligations}
For every accepted $\MetaCICAuditSynthesisRouterUp$ carrier
$$
R=(S,C,N,U,D,B,O,K,F,H,T,P,L),
$$
the local $\NameCert_{\MetaCICAuditSynthesisRouterUp}$ surface consists exactly of subject-reduction route exposure, confluence route exposure, normalization route exposure, substitution route exposure, decidability route exposure, blocker retention, open-input retention, critical-path sourcing, field-faithfulness readiness, componentwise transport, displayed replay, provenance, local naming, and non-escape from premise-free subject reduction, unrestricted Church--Rosser, candidate-free normalization, global beta-conversion decidability, quotient syntax equality, host evaluator soundness, closed consistency, and MetaCIC kernel consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-audit-synthesis-router-carrier}. The route rows $S,C,N,U,D$ expose the five audit families separately, so the router has no coordinate that merges their conclusions into one theorem.

The blocker and open-input rows are then read as boundaries. Row $B$ keeps the closed-normal branch and subject-reduction obstruction visible, while $O$ records the sockets and premises that remain outside the router.

The critical-path row $K$ sources the route without discharging those boundaries. The field-faithfulness readiness row $F$ records that future TasteGate evidence must preserve every displayed field rather than ignore a route coordinate.

Finally, $H,T,P,L$ transport, replay, source, and name the same finite packet. Since no row supplies premise-free subject reduction, unrestricted Church--Rosser, candidate-free normalization, global decidability, quotient equality, evaluator soundness, closed consistency, or kernel consistency, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[MetaCIC audit-synthesis router non-escape]
\label{thm:metacic-audit-synthesis-router-nonescape}
Every consumer of an accepted $\MetaCICAuditSynthesisRouterUp$ packet must read the finite route
$$
S,C,N,U,D \longmapsto B,O,K,F \longmapsto H,T,P,L .
$$
The resulting handoff may coordinate subject-reduction, confluence, normalization, substitution, decidability, blocker, and open-input rows as audit data, but it cannot export subject reduction, confluence, normalization, decidability, closed consistency, quotient syntax equality, evaluator soundness, or MetaCIC kernel consistency as closed theorems.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:metacic-audit-synthesis-router-namecert-obligations}. They place every public read downstream of the five route rows $S,C,N,U,D$.

The blocker and open-input rows are not optional. A consumer that omits $B$ loses the closed-normal and subject-reduction obstruction boundary, and a consumer that omits $O$ loses the premises and sockets that the route intentionally leaves open.

The critical-path and field-faithfulness rows keep the handoff source-local. Row $K$ points back to the displayed MetaCIC critical path, while $F$ records the future requirement that no classifier may ignore one of the displayed fields.

Thus the structural rows $H,T,P,L$ can only replay the finite audit-synthesis router. They cannot create any of the closed theorem endpoints excluded by the carrier.
\end{proof}

\falsifiablePrediction{A purported $\MetaCICAuditSynthesisRouterUp$ read is refuted if it exports subject reduction, confluence, normalization, decidability, closed consistency, quotient syntax equality, evaluator soundness, or MetaCIC kernel consistency without displaying the subject-reduction, confluence, normalization, substitution, decidability, blocker, open-input, critical-path, transport, replay, provenance, and local naming rows.}

\independenceWitness{metacicauditsynthesispacket, metaciccriticalpath, metacicconfluenceauditpacket, metacicnormalizationfrontier, metacicdischargeobligationledger: none is bijective to $\MetaCICAuditSynthesisRouterUp$ because this packet jointly carries subject-reduction, confluence, normalization, substitution, decidability, blocker, open-input, critical-path, field-faithfulness readiness, transport, replay, provenance, and local NameCert rows.}

The Lean TasteGate follow-up for $\MetaCICAuditSynthesisRouterUp$ must provide explicit $\mathsf{Nontrivial}\ \MetaCICAuditSynthesisRouterUp$ and $\mathsf{FieldFaithful}\ \MetaCICAuditSynthesisRouterUp$ instances. These instances must preserve $S,C,N,U,D,B,O,K,F,H,T,P,L$ as separate fields before any checked carrier-alignment theorem can serve as a public gate.

\closureat{MetaCICAuditSynthesisRouterUp}{seedStr}

\begin{closurestatus}{\MetaCICAuditSynthesisRouterUp}
  \constructivestory{A $\MetaCICAuditSynthesisRouterUp$ packet is generated from subject-reduction, confluence, normalization, substitution, decidability, blocker, open-input, critical-path, field-faithfulness readiness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-audit-synthesis-router-carrier}, \autoref{thm:metacic-audit-synthesis-router-namecert-obligations}, and \autoref{thm:metacic-audit-synthesis-router-nonescape} seed the finite MetaCIC audit-synthesis router and its open-input non-escape boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close subject reduction, unrestricted confluence, candidate-free normalization, global beta-conversion decidability, quotient syntax equality, evaluator soundness, closed consistency, Lean verification, bridge checking, or MetaCIC kernel consistency.}
  \upgradepath{Obligation closure requires checked carrier admission, seven-row audit route exposure, blocker retention, open-input retention, critical-path sourcing, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICAuditSynthesisRouterUp.}
\end{closurestatus}
