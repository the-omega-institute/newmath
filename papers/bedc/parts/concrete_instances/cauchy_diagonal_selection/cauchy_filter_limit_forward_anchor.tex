The $\CauchyDiagonalSelectionUp$ packet forwards its selected finite tail through the $\CauchyFilterLimitUp$ handoff of \autoref{ch:concrete-instances-cauchyfilterlimit-namecert} before any Real-facing consumer inspects the terminal seal. This route preserves the existing $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ dependencies from the diagonal-selection carrier, so the handoff is a displayed completion-facing dependency rather than a detached selector or ambient limit principle.
