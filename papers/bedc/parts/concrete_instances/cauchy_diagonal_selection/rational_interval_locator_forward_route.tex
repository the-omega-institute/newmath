The $\CauchyDiagonalSelectionUp$ diagonal window selected from the $\StreamNameUp$ source schedule, $\RegSeqRatUp$ regular readback, and $\DyadicRatCoreUp$ tolerance ledger is the finite window read by $\RationalIntervalLocatorUp$ in \autoref{ch:concrete-instances-rational-interval-locator-namecert} before a $\RealUp$-facing interval consumer can inspect rational lower and upper bounds. This route records that the locator consumes the displayed diagonal window as interval data, rather than importing a selected real limit, quotient stream equality, countable-choice schedule, or ambient completeness coordinate.
