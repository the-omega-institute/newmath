The $\CauchyDiagonalSelectionUp$ route feeds the $\RealLocatorUp$ interval read of \autoref{ch:concrete-instances-real-locator-namecert} only after the finite source windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert} have been displayed. Thus the locator-facing consumer reads the same diagonal-selection window and sibling rows before any completion-facing seal is exposed, rather than importing a detached selector, quotient stream, selected limit, or ambient completeness coordinate.
