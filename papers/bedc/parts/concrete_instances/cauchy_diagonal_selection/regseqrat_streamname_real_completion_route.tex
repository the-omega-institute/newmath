\begin{theorem}[CauchyDiagonalSelection RegSeqRat-StreamName real-completion route]
\label{thm:cauchy-diagonal-selection-regseqrat-streamname-real-completion-route}
Every completion-facing $\CauchyDiagonalSelectionUp$ consumer that reads a diagonal candidate must expose the $\StreamNameUp$ finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} before the selected diagonal window is consumed. The route is therefore a displayed L10 dependency through finite stream windows, regular rational readback, and the real-name boundary, not a detached selector, quotient stream, selected limit, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the local $\CauchyDiagonalSelectionUp$ carrier. Its diagonal window is read only after the stream schedule, regular rational readback, dyadic tolerance ledger, Real seal, transport, replay, provenance, and local naming rows have been exposed.

The cited StreamName, RegSeqRat, and Real sibling chapters provide the three completion-facing boundaries. Since none of those rows stores a selector outside the packet, quotient stream equality, a selected host limit, or ambient completeness, the diagonal-selection consumer remains inside the displayed route.
\end{proof}
