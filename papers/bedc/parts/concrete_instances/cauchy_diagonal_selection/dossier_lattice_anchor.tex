The $\CauchyDiagonalSelectionUp$ dossier is consumed by \autoref{ch:concrete-instances-rational-interval-locator-namecert} through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows recorded in the local carrier. This cross-reference keeps the diagonal-selection window visible as the finite input to the rational interval locator, rather than as a self-contained selector detached from the surrounding real-name lattice.
