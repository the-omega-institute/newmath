Every completion-facing $\CauchyDiagonalSelectionUp$ read consumed by \autoref{ch:concrete-instances-cauchyfilterlimit-namecert} must first display the $\StreamNameUp$ finite-window row, the $\RegSeqRatUp$ regular readback row, the $\DyadicRatCoreUp$ tolerance ledger, the diagonal-window selection row, and the terminal $\RealUp$ seal. The $\CauchyFilterLimitUp$ consumer therefore receives a lattice route through displayed rows, not a detached diagonal selector, quotient stream equality, selected host limit, countable-choice schedule, or ambient completeness principle.
