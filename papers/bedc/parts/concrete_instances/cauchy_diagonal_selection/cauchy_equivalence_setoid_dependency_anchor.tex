The diagonal selector reads $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\DyadicRatCoreUp$ tolerance rows, and $\RealUp$ seal rows through the non-quotient classifier route of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}. This keeps the selected diagonal handoff attached to displayed regular-Cauchy evidence rather than to a quotient class, representative selector, ambient completeness rule, or host equality oracle.
