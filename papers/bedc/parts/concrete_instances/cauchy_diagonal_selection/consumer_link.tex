The $\CauchyDiagonalSelectionUp$ consumer link records the downstream read shared by diagonal-selection, regular-Cauchy extraction, and L10 completion consumers.

\begin{theorem}[Cauchy diagonal selection consumer link]
\label{thm:cauchy-diagonal-selection-consumer-link}
Every $\CauchyDiagonalSelectionUp$ consumer reaches its selected diagonal only through the displayed $\RegSeqRatUp$ readback row, the $\StreamNameUp$ finite-window row, and the diagonal-selection sibling routes of \autoref{thm:cauchy-diagonal-selection-streamname-regseqrat-route} and \autoref{thm:cauchy-diagonal-selection-regular-diagonal-extraction-link}. When the consumer is compared with the regular-Cauchy diagonal-extraction surface of \autoref{ch:concrete-instances-regular-cauchy-diagonal-extraction-namecert}, the shared route is the finite handoff through the source windows, regular readback, dyadic tolerance ledger, diagonal window, terminal $\RealUp$ seal, transport, replay, provenance, and local $\NameCert$ row. It exports no detached selector, quotient stream equality, countable-choice schedule, selected host limit, or ambient completion principle.
\end{theorem}
\begin{proof}
Project the carrier $D=(S,R,M,W,T,E,H,C,P,N)$ from \autoref{def:cauchy-diagonal-selection-carrier}. The rows $S$ and $R$ expose the finite $\StreamNameUp$ windows and the $\RegSeqRatUp$ readback before the selected window $W$ can be consumed.

Use \autoref{thm:cauchy-diagonal-selection-streamname-regseqrat-route} to fix the displayed StreamName, RegSeqRat, DyadicRatCore, and Real-seal route. Then use \autoref{thm:cauchy-diagonal-selection-regular-diagonal-extraction-link} to compare that route with the regular-Cauchy diagonal-extraction chapter.

The structural rows $H,C,P,N$ preserve this handoff under transport, replay, provenance, and local naming. Since the carrier contains no coordinate for quotient stream equality, countable choice, a selected host limit, or ambient completion, every consumer that reaches the selected diagonal remains inside the displayed finite route.
\end{proof}
