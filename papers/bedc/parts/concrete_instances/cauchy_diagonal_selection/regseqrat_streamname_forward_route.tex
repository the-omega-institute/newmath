The diagonal-selection read first fixes the finite stream windows and regular-rational readback before any terminal real seal is available. This places the route beside the $\StreamNameUp$ certificate of \autoref{ch:concrete-instances-streamname-namecert} and the $\RegSeqRatUp$ certificate of \autoref{ch:concrete-instances-regseqrat-namecert}.

\begin{theorem}[Cauchy diagonal StreamName RegSeqRat route]
\label{thm:cauchy-diagonal-selection-regseqrat-streamname-route}
Every accepted $\CauchyDiagonalSelectionUp$ read that exports a diagonal real candidate first consumes the finite $\StreamNameUp$ windows and the $\RegSeqRatUp$ readback rows before the $\RealUp$ seal is inspected. The route exposes no direct real equality test, selected infinite subsequence outside the displayed diagonal ledger, quotient stream equality, countable-choice functional, or host completeness principle.
\end{theorem}
\begin{proof}
Start with the finite diagonal ledger. Each selected row is read through a displayed stream window, so the consumer sees only the finite $\StreamNameUp$ observations attached to the diagonal route.

The regular-rational readback is downstream from those windows. It repacks the observed rational approximants as $\RegSeqRatUp$ data before any real-facing seal is offered.

Only after these two sibling dependencies have been replayed may the $\RealUp$ seal be read. The refused objects are not coordinates in the displayed route, so the diagonal-selection packet remains a finite StreamName and RegSeqRat dependency certificate.
\end{proof}
