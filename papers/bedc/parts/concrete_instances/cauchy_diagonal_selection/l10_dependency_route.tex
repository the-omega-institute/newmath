The $\CauchyDiagonalSelectionUp$ L10 route consumes the source windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the Real seal of \autoref{ch:concrete-instances-real-namecert} through the carrier displayed in \autoref{def:cauchy-diagonal-selection-carrier}. These dependencies are already part of the diagonal-selection packet; this child route makes them visible to the top-level dossier scan without adding a second closure ledger or theorem surface.
