The $\CauchyDiagonalSelectionUp$ dependency route also consumes the rectangular-window and cofinal-schedule surface of \autoref{ch:concrete-instances-double-cauchy-diagonal-namecert} together with the single diagonal-index route of \autoref{ch:concrete-instances-cauchy-diagonal-argument-namecert} before any $\RegSeqRatUp$ or $\RealUp$ readback is exposed. Thus the selected diagonal window is a finite handoff through displayed double-diagonal and diagonal-argument rows, not a detached selector or an ambient completion claim.
