The $\RealSeparabilityUp$ countable-dense handoff of
\autoref{ch:concrete-instances-real-separability-namecert} consumes the
$\CauchyDiagonalSelectionUp$ window only after the $\StreamNameUp$,
$\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows have been displayed.
Thus the diagonal-selection packet supplies the finite diagonal route for the
separability consumer without introducing a quotient stream, a selected real
representative, or a choice schedule.
