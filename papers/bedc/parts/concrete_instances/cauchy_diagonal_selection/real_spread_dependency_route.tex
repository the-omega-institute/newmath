\begin{theorem}[Cauchy diagonal selection real-spread route]
\label{thm:cauchy-diagonal-selection-real-spread-route}
Every $\CauchyRealSpreadUp$ or $\RealUp$ completion consumer that reads a $\CauchyDiagonalSelectionUp$ diagonal window must first pass through the StreamName-RegSeqRat route of \autoref{thm:cauchy-diagonal-selection-streamname-regseqrat-route}. The handoff then feeds the real-spread completion surface of \autoref{ch:concrete-instances-cauchy-real-spread-namecert} and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert} using only the displayed finite source windows, regular rational readback, dyadic tolerance ledger, Real seal, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Project the StreamName-RegSeqRat route already displayed in this chapter. It exposes finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, dyadic tolerance data, and the terminal $\RealUp$ seal before a diagonal-selection consumer can read the selected window.

The $\CauchyRealSpreadUp$ handoff consumes exactly that finite completion-facing data. Its sibling chapter records a real-spread completion route, and the $\RealUp$ chapter supplies the terminal seal; neither sibling adds a new selector to the diagonal-selection packet.

Transport, replay, provenance, and local naming preserve the same rows. Thus the route to $\CauchyRealSpreadUp$ and $\RealUp$ is the displayed completion route, not countable choice, quotient stream equality, selected representatives, or a detached host completion.
\end{proof}

\begin{theorem}[Cauchy diagonal selection nonescape]
\label{thm:cauchy-diagonal-selection-nonescape}
Every completion-facing consumer generated from a $\CauchyDiagonalSelectionUp$ packet and the real-spread route above remains inside the displayed StreamName, RegSeqRat, dyadic-tolerance, Real-seal, transport, replay, provenance, and local-name rows. It cannot become countable choice, a selected representative family, quotient stream equality, a detached diagonal selector, or an ambient completeness principle.
\end{theorem}
\begin{proof}
Induct over the displayed diagonal-selection packet. The base case has only the source windows, regular readback, tolerance ledger, and Real seal already named by \autoref{thm:cauchy-diagonal-selection-streamname-regseqrat-route}.

In the finite step case, the real-spread consumer may read a new diagonal window only after those rows are visible. The sibling $\CauchyRealSpreadUp$ and $\RealUp$ chapters receive the same finite completion-facing data and do not add a choice schedule or selected representative coordinate.

The structural rows then transport, replay, source, and name the packet. Since no row contains countable choice, quotient stream equality, selected representatives, a detached selector, or ambient completeness, none of those objects can escape from the displayed route.
\end{proof}
