The $\CauchyDiagonalSelectionUp$ route consumes the regularized Cauchy-modulus handoff of \autoref{ch:concrete-instances-cauchy-modulus-regularization-namecert} before any diagonal subsequence or Real seal is read. Thus a diagonal consumer must expose the $\CauchyModulusUp$ threshold row, $\StreamNameUp$ finite windows, $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, and $\RealUp$ seal route rather than reading the diagonal as a detached selector, quotient stream, or ambient completeness coordinate.
