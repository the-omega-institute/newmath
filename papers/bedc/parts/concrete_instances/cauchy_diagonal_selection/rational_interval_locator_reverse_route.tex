The rational lower and upper endpoint exports of \autoref{ch:concrete-instances-rational-interval-locator-namecert} consume the $\CauchyDiagonalSelectionUp$ finite-window route before any $\RealUp$-facing read treats those endpoints as interval evidence. This downstream use keeps the locator's bracket rows tied to displayed StreamName, RegSeqRat, and dyadic tolerance data rather than to a selected real limit, quotient stream equality, or ambient completeness coordinate.
