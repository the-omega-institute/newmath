\chapter{A Concrete Naming Certificate for $\OstrowskiInequalityUp$}
\label{ch:concrete-instances-ostrowski-inequality-namecert}
\origin{human}

Following Ostrowski and Bishop--Bridges on constructive real estimates, the $\OstrowskiInequalityUp$ packet records a finite error-bound certificate for a function on a displayed closed bounded interval. It sits over the dyadic rational arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the calculus ledger of \autoref{ch:concrete-instances-calculus-namecert}, and the interval carrier of \autoref{ch:concrete-instances-closedboundedinterval-namecert}. The chapter names only a finite constructive error-bound route; it does not import a host real floor, trichotomy, quotient equality, selected limits, or ambient Real completeness.

\begin{definition}[Ostrowski inequality carrier]
\label{def:ostrowski-inequality-carrier}
An $\OstrowskiInequalityUp$ carrier is a finite $\BHist$ packet
$$
O=(I,f,x,q,W,D,A,B,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\ClosedBoundedIntervalUp$ row naming endpoints $a\leq b$ and the inspected dyadic interval windows.
\item $f$ is the $\ContinuousMapUp$ row for the interval function, read only through finite continuity witnesses on the displayed windows.
\item $x$ is the inspected point row in $I$, represented through the interval membership and dyadic-window data.
\item $q$ is the requested error precision.
\item $W$ is the finite dyadic interval window containing the endpoint, midpoint, and point-readback cells used by the estimate.
\item $D$ is the $\CalculusUp$ derivative-bound ledger. It records the finite rows witnessing the derivative bound $M$ on the displayed interval windows.
\item $A$ is the average-value row for the finite integral approximation over $I$.
\item $B$ is the dyadic error-budget row combining the distance of $x$ from the interval midpoint, the interval length, and the derivative-bound row $D$.
\item $R$ is the $\RegSeqRatUp$ readback row for the two regular rational names whose difference is bounded by $B$ at precision $q$.
\item $E$ is the terminal $\RealUp$ seal for the displayed error estimate.
\item $H$ records componentwise $\hsame$ transport for the interval, map, inspected point, precision, dyadic window, derivative-bound ledger, average row, budget row, readback row, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
\ClosedBoundedIntervalUp \longmapsto \ContinuousMapUp \longmapsto \CalculusUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
\item $P$ is the $\Pkg$ provenance over $\OstrowskiInequalityUp$, $\ClosedBoundedIntervalUp$, $\ContinuousMapUp$, $\CalculusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\OstrowskiInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed rows $I,f,x,q,W,D,A,B,R,E,H,C,P,N$. It has no coordinate for host integration, a selected representative of a real function, quotient equality of real names, trichotomy, a host floor, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.OstrowskiInequalityUp.OstrowskiInequalityTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Ostrowski NameCert obligations]
\label{thm:ostrowski-inequality-namecert-obligations}
For every accepted $\OstrowskiInequalityUp$ carrier $O=(I,f,x,q,W,D,A,B,R,E,H,C,P,N)$, the local $\NameCert_{\OstrowskiInequalityUp}$ surface consists exactly of carrier admission for the interval row $I$, continuous-map row $f$, inspected point row $x$, precision row $q$, finite dyadic window $W$, derivative-bound ledger $D$, average-value row $A$, dyadic error budget $B$, regular-rational readback $R$, terminal $\RealUp$ seal $E$, componentwise transport $H$, displayed replay $C$, package provenance $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:ostrowski-inequality-carrier}. The analytic data visible to the certificate are exactly the interval, map, point, precision, finite window, derivative-bound ledger, average row, dyadic budget, readback row, and terminal seal.

The interval and continuity rows are read before the calculus row. Thus the derivative-bound ledger $D$ is attached to the displayed closed bounded interval windows rather than to an ambient differentiability principle over host reals.

The budget row $B$ is the only place where the Ostrowski estimate is assembled. It receives the point-location and interval-length cells from $W$, the derivative bound from $D$, and the average row $A$ before $R$ and $E$ are available.

Finally $H,C,P,N$ transport, replay, source, and name the same rows. Since no excluded object appears in the carrier, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Ostrowski dyadic error bound]
\label{thm:ostrowski-inequality-dyadic-error-bound}
Every accepted $\OstrowskiInequalityUp$ packet exposes a finite dyadic inequality row
$$
\left|f(x)-A\right|\leq B_q
$$
at the requested precision $q$, where $A$ is the displayed average-value row, $B_q$ is read from the dyadic budget row $B$, and every component of $B_q$ is obtained from the finite interval window $W$ and derivative-bound ledger $D$.
\end{theorem}
\begin{proof}
Start with the finite window $W$. It displays the endpoint cells, the midpoint cell, the inspected point cell, and the interval-length cells used by the Ostrowski estimate.

Read the calculus ledger $D$ next. Its derivative-bound rows supply the finite bound used in the estimate, and the classifier admits no separate derivative source.

The dyadic budget $B$ combines exactly those window and derivative rows with the average-value row $A$. The regular-rational row $R$ then reads the difference estimate at precision $q$, and the terminal seal $E$ records the resulting Real-facing bound without adding a new analytic principle.
\end{proof}

\begin{theorem}[Ostrowski Real-seal handoff]
\label{thm:ostrowski-inequality-real-seal-handoff}
The terminal $\RealUp$ seal of an accepted $\OstrowskiInequalityUp$ packet is reachable only after the finite dyadic interval windows, derivative-bound rows, average-value row, dyadic error-budget row, and regular-rational readback have been displayed. Hence the packet exports no host real floor, no trichotomy decision, no quotient equality of real names, no selected limit, and no ambient Real completeness witness.
\end{theorem}
\begin{proof}
Inspect the route stored by $C$. It lists the interval and map rows before the calculus ledger, the calculus ledger before the dyadic budget, and the dyadic budget before regular-rational readback.

The terminal seal $E$ is downstream from $R$. A consumer therefore cannot reach the Real-facing estimate until the finite windows $W$, derivative-bound ledger $D$, average row $A$, budget row $B$, and readback row $R$ have all been exposed.

The structural rows $H,P,N$ preserve and name this same route. They do not insert host comparison, quotient equality, selected representatives, or completeness data.
\end{proof}

\begin{theorem}[Ostrowski local naming surface]
\label{thm:ostrowski-inequality-local-naming-surface}
The local naming row $N$ of an accepted $\OstrowskiInequalityUp$ packet is stable under componentwise $\hsame$ transport and displayed $\Cont$ replay: any named consumer of the estimate must preserve the same interval row, map row, inspected point, precision request, dyadic windows, derivative-bound ledger, average row, budget row, readback row, Real seal, and $\Pkg$ provenance.
\end{theorem}
\begin{proof}
Use the transport row $H$ component by component. It relates only the rows already present in the carrier and therefore cannot rename the packet through a detached real estimate.

Replay row $C$ fixes the order in which the finite observations are consumed. Any continuation that names the estimate must follow the displayed interval, map, calculus, dyadic, regular-rational, and Real-seal route.

The package row $P$ records the sibling provenance and the local row $N$ names the packet over that provenance. Thus local naming is stable exactly over the finite Ostrowski surface and no larger host-real interface.
\end{proof}

\falsifiablePrediction{An accepted $\OstrowskiInequalityUp$ consumer that reads an error estimate without the displayed interval row $I$, map row $f$, inspected point $x$, precision $q$, finite dyadic window $W$, derivative-bound ledger $D$, average row $A$, dyadic budget $B$, regular-rational readback $R$, and terminal seal $E$ refutes the carrier surface.}

\independenceWitness{dyadicratcore, regseqrat, real, continuousmap, calculus, closedboundedinterval: no listed sibling supplies the whole Ostrowski packet because each sibling contributes arithmetic, readback, sealing, continuity, derivative bounds, or interval windows, while this chapter binds the finite error-budget route with transport, replay, provenance, and local naming.}

\closureat{OstrowskiInequalityUp}{seedStr}

\begin{closurestatus}{\OstrowskiInequalityUp}
  \constructivestory{An $\OstrowskiInequalityUp$ packet is generated from a ClosedBoundedInterval row, a ContinuousMap row, an inspected point, a precision request, finite dyadic windows, a Calculus derivative-bound ledger, an average-value row, a DyadicRatCore error budget, RegSeqRat readback, RealUp sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:ostrowski-inequality-carrier}, \autoref{thm:ostrowski-inequality-namecert-obligations}, \autoref{thm:ostrowski-inequality-dyadic-error-bound}, \autoref{thm:ostrowski-inequality-real-seal-handoff}, and \autoref{thm:ostrowski-inequality-local-naming-surface} seed the finite constructive Ostrowski error-bound route over interval, continuity, calculus, dyadic, regular-rational, and real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host integration, host real floor, trichotomy, quotient equality of real names, selected limits, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, interval-window exactness, derivative-bound exactness, dyadic error-budget routing, RegSeqRat readback, RealUp sealing, transport stability, continuation replay, and provenance nonescape for BEDC.Derived.OstrowskiInequalityUp.}
\end{closurestatus}
