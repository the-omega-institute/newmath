\chapter{A Concrete Naming Certificate for $\ProductiveCauchyNameUp$}
\label{ch:concrete-instances-productive-cauchy-name-namecert}
\origin{human}

Following Bishop and Bridges on countable regular Cauchy data, the $\ProductiveCauchyNameUp$ packet records a finite productive-name surface whose schedule rows generate inspected Cauchy windows before any value-facing seal is read. It sits over the stream schedules of \autoref{ch:concrete-instances-streamname-namecert}, the rational regular readbacks of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular sequence-space packet of \autoref{ch:concrete-instances-regular-sequence-space-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names productive finite readback, not quotient equality, countable choice, selected representatives, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[ProductiveCauchyName carrier]
\label{def:productive-cauchy-name-carrier}
A $\ProductiveCauchyNameUp$ carrier is a finite $\BHist$ packet
$$
P=(S,D,Q,R,E,H,C,K,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ schedule row, exposing the finite requests and response windows used by the productive name.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the displayed dyadic radius and slack data requested by each schedule window.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed stream windows and dyadic tolerance rows.
\item $R$ is the $\RegularSequenceSpaceUp$ source row, naming the ambient regular sequence-space carrier only through its displayed schedule, readback, tolerance, and seal route.
\item $E$ is the $\RealUp$ seal row reached only after $S,D,Q,R$ have remained visible.
\item $H$ records componentwise $\hsame$ transport for the schedule, tolerance, readback, regular-space, real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a productive-name request through $S,D,Q,R,E,H,K,N$.
\item $K$ is the $\Pkg$ provenance over $\ProductiveCauchyNameUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RegularSequenceSpaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ProductiveCauchyNameUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient equality of names, countable choice, trichotomy, a selected representative, a host limit, or a completeness principle for $\RealUp$.
\end{definition}

\begin{theorem}[ProductiveCauchyName NameCert obligations]
\label{thm:productive-cauchy-name-namecert-obligations}
For every accepted $\ProductiveCauchyNameUp$ carrier $P=(S,D,Q,R,E,H,C,K,N)$, the local $\NameCert_{\ProductiveCauchyNameUp}$ surface consists exactly of carrier admission, $\StreamNameUp$ schedule exposure, $\DyadicRatCoreUp$ tolerance exposure, $\RegSeqRatUp$ readback, $\RegularSequenceSpaceUp$ source routing, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every consumer of the real-facing boundary must read the finite route
$$
S,D,Q,R \longmapsto E,H,C,K,N
$$
and no obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:productive-cauchy-name-carrier}. The visible coordinates are exactly the stream schedule, dyadic tolerance row, regular rational readback, regular sequence-space source, terminal seal, transport, replay, provenance, and local naming rows.

The analytic rows are consumed in order. The $\StreamNameUp$ schedule opens the inspected windows, $\DyadicRatCoreUp$ records the displayed tolerance scale for those windows, $\RegSeqRatUp$ reads them as regular rational data, and $\RegularSequenceSpaceUp$ keeps the same readback inside the displayed sequence-space carrier.

Only after that route is visible can the $\RealUp$ seal be read. The structural rows $H,C,K,N$ transport, replay, source, and name the same packet, so no quotient equality, countable-choice selector, selected representative, trichotomy, host limit, or ambient completeness datum remains outside the listed obligations.
\end{proof}

\begin{theorem}[ProductiveCauchyName regular readback]
\label{thm:productive-cauchy-name-regular-readback}
Every accepted $\ProductiveCauchyNameUp$ read exposes its $\StreamNameUp$ schedule row $S$ and $\DyadicRatCoreUp$ tolerance row $D$ before the $\RegSeqRatUp$ readback row $Q$ and the $\RegularSequenceSpaceUp$ source row $R$ are available. The terminal $\RealUp$ seal $E$ therefore reads only a displayed productive Cauchy route and exports no quotient real equality, countable-choice schedule, real trichotomy, selected representative, or $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:productive-cauchy-name-namecert-obligations}. They force every accepted read to expose $S$ and $D$ before $Q$ can be consumed.

The readback row $Q$ is regular only through the displayed windows and dyadic tolerances. The source row $R$ then places that readback inside the regular sequence-space route of \autoref{ch:concrete-instances-regular-sequence-space-namecert}, without adding a quotient or a representative selector.

Finally the seal row $E$ is downstream of $S,D,Q,R$, while $H,C,K,N$ preserve the same finite route. Thus a real-facing consumer receives productive regular readback data, not countable choice, trichotomy, selected representatives, quotient equality, or an ambient completeness principle.
\end{proof}

\falsifiablePrediction{Every accepted $\ProductiveCauchyNameUp$ read exposes a StreamName schedule row, DyadicRatCore tolerance row, RegSeqRat readback row, RegularSequenceSpace source row, RealUp seal row, hsame transport, Cont replay, Pkg provenance, and local NameCert row before any value-facing seal is consumed.}

\independenceWitness{streamname, dyadicratcore, regseqrat, regular-sequence-space, real: $\ProductiveCauchyNameUp$ is not bijective with any listed sibling because it jointly carries schedule productivity, dyadic tolerance exposure, regular rational readback, regular sequence-space routing, terminal real sealing, transport, replay, provenance, and local naming in one finite NameCert packet.}

\closureat{ProductiveCauchyNameUp}{seedStr}

\begin{closurestatus}{\ProductiveCauchyNameUp}
  \constructivestory{A $\ProductiveCauchyNameUp$ packet is generated from StreamName schedules, DyadicRatCore tolerance rows, RegSeqRat readback rows, a RegularSequenceSpace source route, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:productive-cauchy-name-carrier}, \autoref{thm:productive-cauchy-name-namecert-obligations}, and \autoref{thm:productive-cauchy-name-regular-readback} seed the finite productive Cauchy-name route from countable schedule windows to regular rational readback and terminal real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient equality, countable choice, real trichotomy, selected representatives, host limit selection, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, schedule productivity, dyadic tolerance exposure, regular rational readback, RegularSequenceSpace routing, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ProductiveCauchyNameUp.}
\end{closurestatus}
