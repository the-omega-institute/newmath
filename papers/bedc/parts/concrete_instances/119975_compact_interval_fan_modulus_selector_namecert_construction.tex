\chapter{A Concrete Naming Certificate for $\CompactIntervalFanModulusSelectorUp$}
\label{ch:concrete-instances-compact-interval-fan-modulus-selector-namecert}
\origin{human}

Following Bishop and Bridges on compact intervals and fan-bar moduli, the $\CompactIntervalFanModulusSelectorUp$ packet records the finite selector that carries compact interval source data through fan-bar depth, dyadic tolerance, finite StreamName windows, RegSeqRat readback, and a terminal Real seal. It sits beside the located interval boundary of \autoref{ch:concrete-instances-located-interval-fan-barrier-namecert}, the compact interval fan route of \autoref{ch:concrete-instances-compact-interval-fan-bridge-namecert}, and the finite fan-bar modulus surface of \autoref{ch:concrete-instances-finite-fan-bar-modulus-namecert}. The packet supplies an L10 Real and Completion route without asserting open-cover compactness, choosing branches, quotienting real names, invoking countable choice, or importing ambient $\RealUp$ completeness.

\begin{definition}[Compact interval fan modulus selector carrier]
\label{def:compact-interval-fan-modulus-selector-carrier}
A $\CompactIntervalFanModulusSelectorUp$ carrier is a finite $\BHist$ packet
$$
M=(I,B,D,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the compact interval source row, read through the Bishop compact interval and located interval surfaces before any fan-bar depth is consumed.
\item $B$ is the finite fan-bar depth row, read through $\CompactIntervalFanBridgeUp$, $\LocatedIntervalFanBarrierUp$, and $\FiniteFanBarModulusUp$ as a bounded prefix-depth certificate.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row carrying the requested precision and the finite comparison budget used by the selector.
\item $S$ is the finite $\StreamNameUp$ window ledger opened by the depth and tolerance rows.
\item $Q$ is the $\RegSeqRatUp$ readback row for the regular rational approximants observed inside the displayed windows.
\item $E$ is the terminal $\RealUp$ seal, available only after $I,B,D,S,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for interval source, fan-bar depth, dyadic tolerance, stream windows, regular-rational readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact interval source data through the fan-bar depth and then through $D,S,Q,E$.
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalFanModulusSelectorUp$, $\CompactIntervalFanBridgeUp$, $\LocatedIntervalFanBarrierUp$, $\FiniteFanBarModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactIntervalFanModulusSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,B,D,S,Q,E,H,C,P,N$. It has no coordinate for open-cover compactness, a selected branch, quotient real equality, countable choice, host real comparison, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact interval fan modulus selector obligations]
\label{thm:compact-interval-fan-modulus-selector-obligations}
For every accepted $\CompactIntervalFanModulusSelectorUp$ carrier
$$
M=(I,B,D,S,Q,E,H,C,P,N),
$$
the local $\NameCert_{\CompactIntervalFanModulusSelectorUp}$ surface consists exactly of carrier admission, compact interval source exposure through $I$, finite fan-bar depth exposure through $B$, dyadic tolerance accounting through $D$, finite $\StreamNameUp$ window exposure through $S$, $\RegSeqRatUp$ readback through $Q$, terminal $\RealUp$ sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-interval-fan-modulus-selector-carrier}. The accepted packet displays exactly the interval source, finite fan-bar depth, dyadic tolerance, stream-window, regular-rational, Real-seal, transport, replay, provenance, and naming rows.

The analytic route is ordered. The compact interval source row $I$ is exposed first, the fan-bar row $B$ supplies the finite depth bound, and the dyadic row $D$ records the tolerance budget consumed by the selected windows.

The readback rows are downstream of that finite prefix. The window ledger $S$ is opened from the depth and tolerance rows, $Q$ reads regular rational data inside those windows, and $E$ seals the Real-facing result only after those rows have been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite selector. Since the carrier has no coordinate for open-cover compactness, selected branches, quotient real equality, countable choice, host real comparison, or ambient completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact interval fan modulus finite route]
\label{thm:compact-interval-fan-modulus-selector-finite-route}
Every L10 Real and Completion consumer of an accepted $\CompactIntervalFanModulusSelectorUp$ carrier factors through the finite route
$$
\CompactIntervalFanBridgeUp,\ \LocatedIntervalFanBarrierUp,\ \FiniteFanBarModulusUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
In carrier notation, the consumer reads $I,B,D,S,Q,E$ before using the structural rows $H,C,P,N$. The route exports a displayed modulus selector from compact interval and fan-bar evidence to Real-facing readback, and it exports no open-cover compactness theorem, selected branch, quotient real equality, countable-choice row, host equality test, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with the compact interval and fan rows. The compact source row $I$ supplies the interval data, while $B$ records the finite fan-bar depth available through the adjacent compact interval and finite bar chapters.

The tolerance and window rows come next. The dyadic row $D$ fixes the requested precision, and the finite window ledger $S$ lists only the observations licensed by the displayed depth and tolerance.

The Real-facing suffix is $Q,E$. RegSeqRat readback is performed inside the finite windows, and the terminal Real seal is visible only after that readback has been replayed.

The structural rows $H,C,P,N$ preserve the same order under transport, continuation replay, provenance, and local naming. Therefore every L10 consumer factors through the displayed finite route, while each excluded compactness, choice, branch, quotient, host-equality, or ambient-completeness reading lacks a carrier coordinate.
\end{proof}

\falsifiablePrediction{Every accepted CompactIntervalFanModulusSelectorUp Real or Completion-facing read factors through compact interval source, finite fan-bar depth, dyadic tolerance, finite StreamName windows, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming rows.}

\independenceWitness{located interval fan barrier, compact interval fan bridge, finite fan bar modulus, dyadicratcore, streamname, regseqrat, real: $\CompactIntervalFanModulusSelectorUp$ is not bijective to any listed sibling because it jointly carries compact interval source data, fan-bar depth, dyadic tolerance, finite observation windows, regular rational readback, Real sealing, and structural support while refusing open-cover compactness, selected branches, quotient real equality, countable choice, host real comparison, and ambient completeness.}

\closureat{CompactIntervalFanModulusSelectorUp}{seedStr}

\begin{closurestatus}{\CompactIntervalFanModulusSelectorUp}
  \constructivestory{A $\CompactIntervalFanModulusSelectorUp$ packet is generated from compact interval source rows, finite fan-bar depth rows, DyadicRatCore tolerance ledgers, finite StreamName windows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-fan-modulus-selector-carrier}, \autoref{thm:compact-interval-fan-modulus-selector-obligations}, and \autoref{thm:compact-interval-fan-modulus-selector-finite-route} seed the compact interval fan-bar route from finite depth and dyadic tolerance to Real and Completion readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected branches, quotient real equality, countable choice, host real comparison, ambient $\RealUp$ completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact interval source exposure, fan-bar depth exposure, dyadic tolerance accounting, stream-window readback, Real seal discipline, transport, replay, provenance, and local naming for BEDC.Derived.CompactIntervalFanModulusSelectorUp.}
\end{closurestatus}
