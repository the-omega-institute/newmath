\chapter{A Concrete Naming Certificate for $\CantorBendixsonRankUp$}
\label{ch:concrete-instances-cantor-bendixson-rank-namecert}
\origin{human}

Following Cantor and Bendixson, the $\CantorBendixsonRankUp$ packet records a finite derivative-rank certificate for the displayed compact-metric part of a Cantor--Bendixson analysis. It sits after the derivative ledger of \autoref{ch:concrete-instances-cantor-bendixson-namecert}, the reusable perfect-kernel face of \autoref{ch:concrete-instances-perfect-set-namecert}, the nowhere-dense boundary of \autoref{ch:concrete-instances-nowhere-dense-namecert}, the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, and the source metric row of \autoref{ch:concrete-instances-metric-namecert}. The packet names only a finite rank budget, its inspected derivative rows, and its residual-kernel handoff; it does not assert a host ordinal rank, transfinite powerset construction, selected enumeration of isolated points, quotient closed-set equality, countable choice, or ambient compactness theorem.

\begin{definition}[Cantor--Bendixson rank carrier]
\label{def:cantor-bendixson-rank-carrier}
A $\CantorBendixsonRankUp$ carrier is a finite $\BHist$ packet
$$
R=(M,K,D,r,I,P,N_0,W,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the source $\MetricUp$ row, carrying the metric observation surface used by the derivative requests.
\item $K$ is the $\CompactMetricUp$ row, exposing the finite compact inspection and cover budgets on which the derivative search is allowed to depend.
\item $D$ is the $\CantorBendixsonUp$ derivative ledger, listing the displayed derivative stages already admitted by the sibling Cantor--Bendixson packet.
\item $r$ is the finite rank budget row, recording the displayed natural-number depth at which the inspected derivative route stops.
\item $I$ is the isolated-remnant ledger, recording only those isolated reads witnessed before the budget row $r$ is exhausted.
\item $P$ is the $\PerfectSetUp$ residual-kernel row, read only for the part that survives the displayed derivative budget.
\item $N_0$ is the $\NowhereDenseUp$ boundary row, recording the nowhere-dense refusal surface consumed by scattered-remnant or category-facing reads.
\item $W$ is the finite window schedule for metric neighborhoods, compact inspections, derivative rows, isolated-remnant reads, perfect-kernel reads, and nowhere-dense boundary reads.
\item $H$ records componentwise $\hsame$ transport for $M,K,D,r,I,P,N_0,W$ and for the replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of every accepted rank request through
$$
M,K,D,r,I,P,N_0,W \longmapsto H,C,Q,N .
$$
\item $Q$ is the $\Pkg$ provenance over $\CantorBendixsonRankUp$, $\CantorBendixsonUp$, $\PerfectSetUp$, $\NowhereDenseUp$, $\CompactMetricUp$, $\MetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorBendixsonRankUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for host ordinals, transfinite derivative intersections, ambient powersets, quotient closed-set equality, a selected enumeration of isolated points, countable choice, or compactness outside the displayed compact-metric route.
\end{definition}

\begin{theorem}[Cantor--Bendixson rank NameCert obligations]
\label{thm:cantor-bendixson-rank-namecert-obligations}
For every accepted $\CantorBendixsonRankUp$ carrier
$$
R=(M,K,D,r,I,P,N_0,W,H,C,Q,N),
$$
the local $\NameCert_{\CantorBendixsonRankUp}$ surface is exhausted by the following finite obligation rows:
\begin{enumerate}
\item metric and compact admission through $M$ and $K$, exposing only displayed metric observations, compact inspection rows, and finite cover budgets;
\item derivative-ledger admission through $D$, requiring every rank read to cite the displayed $\CantorBendixsonUp$ derivative stages it consumes;
\item finite-rank exactness through $r$ and $W$, requiring the rank budget to name the inspected derivative depth and the windows used at that depth;
\item isolated-remnant exactness through $I$, requiring every scattered read to carry a displayed isolation witness before the rank budget is exhausted;
\item residual-kernel and nowhere-dense compatibility through $P$ and $N_0$, requiring perfect-kernel reads and nowhere-dense refusals to remain attached to the same displayed derivative route;
\item structural nonescape through $H,C,Q,N$, requiring transport, replay, provenance, and local naming to export no host ordinal rank, transfinite powerset construction, selected isolated-point enumeration, quotient closed-set equality, countable choice, or ambient compactness theorem.
\end{enumerate}
No finite derivative-rank obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cantor-bendixson-rank-carrier}. The metric and compact inputs are exactly $M$ and $K$, so every rank request begins with displayed observations and compact inspection data rather than with a host powerset.

The rank work is finite. The derivative ledger $D$ supplies the Cantor--Bendixson stages already visible to the packet, while $r$ and $W$ record the finite depth and the windows inspected at that depth.

The two outputs stay attached to that same route. The isolated-remnant row $I$ records only witnessed scattered reads before the budget is exhausted, and the residual rows $P,N_0$ record the perfect-kernel and nowhere-dense boundaries that survive the displayed route.

Finally $H,C,Q,N$ transport, replay, source, and name those rows. Since the carrier has no coordinate for host ordinal ranks, transfinite intersections, selected isolated-point enumerations, quotient closed-set equality, countable choice, or external compactness, the six obligation rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Cantor--Bendixson rank finite derivative budget]
\label{thm:cantor-bendixson-rank-finite-derivative-budget}
Every accepted $\CantorBendixsonRankUp$ consumer that asks for a derivative-rank read must first display the route
$$
M,K,D,r,W \longmapsto I,P,N_0 \longmapsto H,C,Q,N .
$$
Thus the exported rank is a finite BEDC budget over the $\MetricUp$, $\CompactMetricUp$, $\CantorBendixsonUp$, $\PerfectSetUp$, and $\NowhereDenseUp$ rows. The consumer receives no host ordinal, transfinite derivative chain, powerset decomposition, selected isolated-point enumeration, quotient closed-set equality, countable-choice schedule, or ambient compactness theorem.
\end{theorem}
\begin{proof}
Begin with the metric and compact rows. The source metric observations and compact inspection budgets are the only neighborhood data available to the derivative read.

Next read the derivative ledger and budget. The row $D$ supplies the displayed Cantor--Bendixson stages, while $r$ and $W$ identify the finite depth and windows at which the rank request is inspected.

The scattered and residual faces are downstream of that budget. Row $I$ records isolated remnants only when they have displayed witnesses, and rows $P,N_0$ record the retained perfect-kernel and nowhere-dense boundaries without detaching from the same derivative ledger.

The structural rows preserve the route and add no new mathematical coordinates. Therefore the exported object is exactly a finite derivative-rank budget over the named sibling packets, not any of the excluded host-level or choice-dependent data.
\end{proof}

\falsifiablePrediction{Every accepted $\CantorBendixsonRankUp$ consumer that reads a derivative-rank packet while omitting one of the Metric, CompactMetric, CantorBendixson derivative, finite rank budget, isolated-remnant, PerfectSet residual, NowhereDense boundary, window, hsame, Cont, Pkg, or NameCert rows has left the packet.}

\independenceWitness{cantorbendixson, perfectset, nowheredense, compactmetric, metric: $\CantorBendixsonRankUp$ is not bijective with any listed sibling because it joins metric observations, compact inspection, derivative stages, a finite rank budget, isolated remnants, residual perfect-kernel data, nowhere-dense refusals, windows, transport, replay, provenance, and local naming as one finite packet.}

\closureat{CantorBendixsonRankUp}{seedStr}

\begin{closurestatus}{\CantorBendixsonRankUp}
  \constructivestory{A $\CantorBendixsonRankUp$ packet is generated from MetricUp observations, CompactMetricUp inspection rows, CantorBendixsonUp derivative ledgers, a finite rank budget, isolated-remnant witnesses, PerfectSetUp residual rows, NowhereDenseUp boundary rows, finite windows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-bendixson-rank-carrier}, \autoref{thm:cantor-bendixson-rank-namecert-obligations}, and \autoref{thm:cantor-bendixson-rank-finite-derivative-budget} seed the finite derivative-rank surface for compact-metric Cantor--Bendixson packets.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host ordinal rank, transfinite powerset decomposition, selected isolated-point enumeration, quotient closed-set equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative-ledger admission, finite-rank exactness, isolated-remnant exactness, residual-kernel compatibility, nowhere-dense boundary compatibility, structural nonescape, and route replay for BEDC.Derived.CantorBendixsonRankUp.}
\end{closurestatus}
