\begin{theorem}[CauchyOscillation NameCert obligation scope]
\label{thm:cauchy-oscillation-namecert-obligation-scope}
The $\NameCert_{\CauchyOscillationUp}$ obligation scope for an accepted carrier $O=(W,M,Q,T,S,H,C,P,N)$ is exactly the finite package consisting of the tail window $W$, modulus schedule $M$, dyadic tolerance $Q$, oscillation ledger $T$, $\RealUp$ seal $S$, $\BHist$ carrier rows, $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local naming. The scope includes common tail-window classification and ledger non-escape, and it excludes quotient streams, selected limits, host real equality, countable choice, and ambient completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:cauchy-oscillation-namecert-obligations}. They list carrier habitation, common classifier, oscillation-bound exactness, transport-replay stability, and ledger non-escape over $W,M,Q,T,S,H,C,P,N$.

Apply common tail-window classifier and ledger nonescape above. The first fixes classifier comparisons to the same finite tail-window package, and the second prevents ledger reads from exporting detached completion data.

Thus the obligation scope is the displayed finite package itself. The refused quotient, selected-limit, host-equality, choice, and completeness objects are not rows of the carrier and are not introduced by transport, replay, provenance, or naming.
\end{proof}

\begin{theorem}[CauchyOscillation shared dyadic window scope]
\label{thm:cauchy-oscillation-shared-dyadic-window-scope}
Every accepted $\CauchyOscillationUp$ packet reads $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, and $\RegSeqRatUp$ through one shared finite tail window $W$. The threshold row $M$, dyadic tolerance row $Q$, oscillation ledger $T$, and structural rows $H,C,P,N$ preserve that same window before the $\RealUp$ seal $S$ is available.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyOscillationUp.CauchyOscillationSharedDyadicWindowScope}
\begin{proof}
Project the carrier and apply tail-window exposure. The row $W$ is the only StreamName or RegSeqRat window coordinate, while $M$ and $Q$ are the only threshold and dyadic tolerance rows attached to it.

Use modulus-threshold exactness and dyadic-tail exactness. These identify $T$ as the displayed comparison ledger over the same $W,M,Q$ packet, so no later read can swap in another tail window.

Finally apply ledger transport. The rows $H,C,P,N$ transport, replay, source, and name the same finite packet, keeping every completion-facing consumer inside the shared dyadic window.
\end{proof}

\begin{theorem}[CauchyOscillation RegSeqRat Real nonescape]
\label{thm:cauchy-oscillation-regseqrat-real-nonescape}
Every $\RealUp$-facing consumer of an accepted $\CauchyOscillationUp$ packet receives only the routed $\RegSeqRatUp$ handoff through $W,M,Q,T,H,C,P,N$ before the seal row $S$. It receives no quotient stream, selected limit, host equality, countable choice, or ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Begin with the RegSeqRat handoff and the shared dyadic window scope. Together they place the regular-rational readback inside the same finite tail-window, modulus, dyadic tolerance, and ledger packet.

Apply Real seal nonescape and seal-handoff factorization. The terminal seal $S$ is reached only after the displayed prefix, rather than from an independent completion object.

The carrier has no row for quotient streams, selected limits, host equality, choice, or ambient completeness. Transport, replay, provenance, and local naming therefore preserve the routed RegSeqRat handoff without adding any refused Real-facing data.
\end{proof}

\begin{theorem}[CauchyOscillation tail-modulus route]
\label{thm:cauchyoscillation-tail-modulus-route}
Every oscillation tail read using a dyadic tail bound must route through $\CauchyCriterionUp$, $\CauchyModulusUp$, $\DyadicTailBoundUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before a uniform-limit consumer can inspect it. For an accepted carrier $O=(W,M,Q,T,S,H,C,P,N)$, the dyadic tail bound is read as the displayed tolerance and oscillation ledger over the shared finite window $W$, not as a host modulus, quotient stream, selected tail, or ambient completeness witness.
\end{theorem}
\begin{proof}
Start with the NameCert obligation scope of \autoref{thm:cauchy-oscillation-namecert-obligation-scope}. It fixes $W,M,Q,T,S,H,C,P,N$ as the displayed carrier rows and keeps the oscillation ledger inside the local naming surface.

Next apply the shared dyadic window scope of \autoref{thm:cauchy-oscillation-shared-dyadic-window-scope}. The CauchyModulus and DyadicTailBound reads use the same finite StreamName and RegSeqRat window, so the CauchyCriterion-facing tail request cannot swap in an external modulus or a selected infinite tail.

Finally use \autoref{thm:cauchy-oscillation-regseqrat-real-nonescape}. The RegSeqRat handoff reaches the Real seal only after the displayed tail window, modulus, dyadic tolerance, oscillation ledger, transport, replay, provenance, and naming rows have been consumed.

Thus a uniform-limit consumer inspects only the routed finite tail-modulus evidence. The route carries no quotient stream, host real equality, countable-choice schedule, selected limit, or ambient completeness coordinate.
\end{proof}

\closureat{CauchyOscillationUp}{seedStr}

\begin{closurestatus}{\CauchyOscillationUp}
  \constructivestory{A $\CauchyOscillationUp$ packet is generated from a finite $\RegSeqRatUp$ or $\StreamNameUp$ tail window, a $\CauchyModulusUp$ threshold schedule, a $\DyadicRatCoreUp$ tolerance row, a $\DyadicTailBoundUp$ oscillation ledger, a $\RealUp$ seal handoff, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and local naming.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The seed scope records the finite carrier, common tail-window classifier, oscillation-bound exactness, transport stability, tail-window exposure, dyadic tolerance stability, modulus-threshold exactness, seal-handoff factorization, modulus-root packaging, dyadic-tail Real-seal routing, and ledger non-escape for constructive Cauchy oscillation packets.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, ambient completeness, real completeness, or a checked Lean certificate.}
  \upgradepath{Obligation closure requires a Lean carrier encoding and checked NameCert obligation theorem for the displayed tail-window, modulus, tolerance, oscillation-ledger, and seal-handoff rows.}
\end{closurestatus}
