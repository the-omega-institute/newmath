\begin{theorem}[CauchyOscillation mature status route]
\label{thm:cauchy-oscillation-mature-status-route}
The mature paper surface of $\CauchyOscillationUp$ is the finite completion-audit route assembled from the audit-clean obligation package, the Real completion audit handoff, the common tail-window classifier, and ledger nonescape. Every accepted carrier
$$
O=(W,M,Q,T,S,H,C,P,N)
$$
is read through the displayed tail window, modulus threshold, dyadic tolerance, oscillation ledger, Real seal, componentwise transport, replay, provenance, and local NameCert rows before a completion-facing consumer may inspect the seal.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyOscillationUp.CauchyOscillationMatureStatusRoute}
\begin{proof}
Start with the audit-clean obligation package of \autoref{thm:cauchy-oscillation-auditclean-obligation-package}. It exposes $W,M,Q,T,S,H,C,P,N$ as the whole carrier package for L10 completion readers.

Next use the Real completion audit handoff, \autoref{thm:cauchy-oscillation-real-completion-audit-handoff}. The seal row $S$ is available only after the finite audit package $W,M,Q,T,H,C,P,N$ has been consumed.

Apply the common tail-window classifier and ledger nonescape, \autoref{thm:cauchy-oscillation-common-tail-window-classifier} and \autoref{thm:cauchy-oscillation-ledger-nonescape}. They keep classifier comparison and ledger reads on the same displayed tail window and exclude detached modulus, second tolerance, quotient stream, selected limit, host equality, and ambient completeness rows.

Finally read the scoped status route of \autoref{thm:cauchy-oscillation-namecert-obligation-scope}. The same rows supply the mature paper surface because the audit package, seal handoff, classifier, and ledger refusal are all named inside the local $\NameCert_{\CauchyOscillationUp}$ route.
\end{proof}

\begin{theorem}[CauchyOscillation mature status formal target]
\label{thm:cauchy-oscillation-mature-status-route-formal-target}
The formalization target for the mature $\CauchyOscillationUp$ status route is the single finite assembly that consumes the carrier obligations of $\mathsf{CauchyOscillationCarrier\_namecert\_obligations}$, the semantic certificate surface of $\mathsf{CauchyOscillationPacket\_semantic\_name\_certificate}$, the seal handoff of \autoref{thm:cauchy-oscillation-real-completion-audit-handoff}, ledger transport through \autoref{thm:cauchy-oscillation-ledger-nonescape}, and the completion-consumer refusal route. For an accepted carrier $O=(W,M,Q,T,S,H,C,P,N)$, the target asserts that every mature completion-facing read is exhausted by the displayed tail window, modulus threshold, dyadic tolerance, oscillation ledger, Real seal, componentwise transport, replay, provenance, and local NameCert rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyOscillationUp.CauchyOscillationMatureStatusRouteFormalTarget}
\begin{proof}
Start with the carrier and semantic certificate obligations. They provide the finite rows $W,M,Q,T,S,H,C,P,N$ and identify the local $\NameCert_{\CauchyOscillationUp}$ surface as the only named packet available to a mature reader.

Next apply the seal handoff. The Real completion audit handoff places the seal row $S$ downstream of the audit package, so no completion-facing consumer may inspect a detached Real endpoint before the tail-window, modulus, tolerance, ledger, transport, replay, provenance, and name rows have been exposed.

Use ledger transport and completion-consumer refusal. Ledger nonescape keeps every oscillation read inside the same displayed tail window, while the completion-consumer refusal excludes quotient streams, selected limits, host equality, countable choice, and ambient completeness from the exported surface.

The theorem is therefore the paper-side target for the Lean declaration named $\mathsf{CauchyOscillationMatureStatusRouteFormalTarget}$: it repacks the checked component rows into one mature route while leaving the existing verified component markers as the active Lean evidence.
\end{proof}
