\chapter{A Concrete Naming Certificate for $\PoincareBendixsonTrapUp$}
\label{ch:concrete-instances-poincare-bendixson-trap-namecert}
\origin{human}

Following Poincare and Bendixson, the $\PoincareBendixsonTrapUp$ packet records the finite trapping-region certificate used by planar autonomous dynamics before any periodic-orbit conclusion is read. It sits over the displayed ODE row of \autoref{ch:concrete-instances-ode-namecert}, the flow-state surface of \autoref{ch:concrete-instances-dynsystem-namecert}, the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map route of \autoref{ch:concrete-instances-continuousmap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the trap boundary and finite flow-window obligations; it does not select a host trajectory, complete a global flow, quotient path equality, invoke countable choice, or import ambient $\RealUp$ completeness.

\begin{definition}[Poincare--Bendixson trap carrier]
\label{def:poincare-bendixson-trap-carrier}
A $\PoincareBendixsonTrapUp$ carrier is a finite $\BHist$ packet
$$
T=(O,D,K,F,B,I,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the $\OdeUp$ row, naming the planar vector-field equation and the finite right-hand-side reads inspected by the packet.
\item $D$ is the $\DynSystemUp$ row, naming the local flow-window discipline available to the trap without asserting global flow completion.
\item $K$ is the $\CompactMetricUp$ row for the displayed trapping carrier and its compact metric boundary.
\item $F$ is the $\ContinuousMapUp$ row used to read the local flow map on the displayed windows.
\item $B$ is the inward-boundary ledger, recording the finite boundary faces, normal-side tests, and exit-refusal checks for the trap.
\item $I$ is the invariant-window row, recording that every accepted local flow read re-enters the displayed compact trap before any downstream consumer reads a recurrence claim.
\item $R$ is the $\RealUp$ seal boundary for coordinates, distances, and boundary-side comparisons.
\item $H$ records componentwise $\hsame$ transport for ODE, dynamics, compact metric, continuous-map, boundary, invariant-window, real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from ODE reads through local flow windows, compact boundary checks, inward tests, invariant-window routing, and real-coordinate sealing.
\item $P$ records $\Pkg$ provenance over $\PoincareBendixsonTrapUp$, $\OdeUp$, $\DynSystemUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\PoincareBendixsonTrapUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $O,D,K,F,B,I,R,H,C,P,N$. It has no coordinate for selected host trajectories, completed global flows, quotient equality of paths, countable-choice selectors, or ambient real-completeness principles.
\end{definition}

\begin{theorem}[Poincare--Bendixson trap NameCert obligations]
\label{thm:poincare-bendixson-trap-namecert-obligations}
For every accepted $\PoincareBendixsonTrapUp$ carrier $T=(O,D,K,F,B,I,R,H,C,P,N)$, the local $\NameCert_{\PoincareBendixsonTrapUp}$ surface consists exactly of displayed ODE admission, local dynamical-window admission, compact metric trap exposure, continuous-map flow-window exposure, inward-boundary ledger visibility, invariant-window routing, $\RealUp$ seal separation, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:poincare-bendixson-trap-carrier}. The rows $O,D,K,F,B,I,R,H,C,P,N$ are the only data visible to the local certificate, so displayed ODE and dynamics reads arrive before any trap conclusion can be consumed.

Read the finite trap rows next. The compact metric row $K$ gives the displayed carrier, the continuous-map row $F$ gives the local flow windows, and the inward-boundary ledger $B$ records the boundary-side checks used to refuse exits.

The invariant-window row $I$ is downstream of those reads. It can only state that the displayed local windows re-enter the compact trap through the rows already named by $O,D,K,F,B,R$.

Finally attach $H,C,P,N$. Transport, replay, provenance, and local naming preserve the same route, and no carrier coordinate supplies a selected host trajectory, global flow completion, quotient path equality, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Poincare--Bendixson compact-flow handoff]
\label{thm:poincare-bendixson-trap-compact-flow-handoff}
Every compact-flow consumer of an accepted $\PoincareBendixsonTrapUp$ packet must first read the route
$$
\OdeUp \longmapsto \DynSystemUp \longmapsto \ContinuousMapUp \longmapsto \CompactMetricUp \longmapsto \RealUp .
$$
The inward-boundary and invariant-window rows may then be consumed as a trapping-region certificate for planar dynamics, but only through the displayed finite rows of \autoref{def:poincare-bendixson-trap-carrier}. The handoff exports no selected trajectory, global flow completion, quotient path equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the ODE and dynamics rows. The row $O$ names the displayed planar equation, while $D$ names the local flow-window discipline; neither row carries a completed host trajectory.

Use the continuous-map and compact metric rows. The flow-window read $F$ is interpreted only on the compact trap carrier $K$, so the consumer receives a finite compact-flow route rather than a global flow object.

Read the boundary and invariant-window rows after that route. The ledger $B$ records the inward boundary tests, and $I$ records the trap re-entry surface reached by the accepted local reads.

The terminal real-coordinate seal $R$ and the structural rows $H,C,P,N$ preserve this order. Since all public reads are projections of the displayed carrier, no hidden selected trajectory, quotient path equality, countable-choice selector, global completion, or ambient real-completeness theorem is available.
\end{proof}

\falsifiablePrediction{Every accepted $\PoincareBendixsonTrapUp$ read exposes one OdeUp row, one DynSystemUp local flow-window row, one CompactMetricUp trap row, one ContinuousMapUp flow row, one inward-boundary ledger, one invariant-window row, one RealUp seal, hsame transport, Cont replay, Pkg provenance, and a local NameCert row before any compact-flow consumer reads the trap.}

\independenceWitness{ode, dynsystem, compactmetric, continuousmap, real: none is bijective with $\PoincareBendixsonTrapUp$ because the packet jointly stores the displayed planar ODE, local flow windows, compact trap carrier, continuous-map reads, inward-boundary ledger, invariant-window route, Real seal, transport, replay, provenance, and local naming.}

\closureat{PoincareBendixsonTrapUp}{seedStr}

\begin{closurestatus}{\PoincareBendixsonTrapUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poincare-bendixson-trap-carrier}, \autoref{thm:poincare-bendixson-trap-namecert-obligations}, and \autoref{thm:poincare-bendixson-trap-compact-flow-handoff} seed the finite planar trapping-region certificate over displayed ODE, local dynamics, compact metric, continuous-map, boundary, invariant-window, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected host trajectories, global flow completion, quotient path equality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked ODE admission, local dynamics admission, compact trap exposure, continuous flow-window exposure, inward-boundary stability, invariant-window routing, Real seal separation, transport, replay, provenance, and local naming.}
  \constructivestory{A $\PoincareBendixsonTrapUp$ packet is generated from an OdeUp row, DynSystemUp local flow-window row, CompactMetricUp trap row, ContinuousMapUp row, inward-boundary ledger, invariant-window row, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
