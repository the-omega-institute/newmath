\chapter{RegularCauchyRing NameCert Construction}
\label{ch:concrete-instances-regularcauchyring-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\RegularCauchyRingUp$ packet treats the algebra of regular Cauchy real names as a finite operation surface over displayed rational observations. Addition, negation, product, and rational scaling are not imported as host real operations; they are read through the existing $\RegularCauchySumUp$, $\RegularCauchyNegationUp$, $\RegularCauchyProductUp$, and $\RegularCauchyScaleUp$ rows, then sealed through the same $\RegSeqRatUp$ and $\RealUp$ completion boundary. Thus the packet records the L10 algebraic handoff without quotient real equality, trichotomy, countable choice, selected limits, or ambient completeness.

The sibling route runs through \autoref{ch:concrete-instances-regularcauchysum-namecert}, \autoref{ch:concrete-instances-regularcauchynegation-namecert}, \autoref{ch:concrete-instances-regularcauchyproduct-namecert}, \autoref{ch:concrete-instances-regularcauchyscale-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. Those chapters supply the operation rows and terminal $\RealUp$ seal consumed by the ring packet.

\begin{definition}[Regular Cauchy ring carrier]
\label{def:regular-cauchy-ring-carrier}
A $\RegularCauchyRingUp$ carrier is a finite $\BHist$ packet
$$
K=(A,B,W_A,W_B,D_A,D_B,S,G,M,L,R_S,R_G,R_M,R_L,E_S,E_G,E_M,E_L,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $A$ and $B$ are two $\RegSeqRatUp$ source rows for regular rational Cauchy real names.
\item $W_A$ and $W_B$ are aligned $\StreamNameUp$ finite-window rows for the same requested precision scale.
\item $D_A$ and $D_B$ are the $\DyadicRatCoreUp$ endpoint ledgers read from those windows.
\item $S$ is the $\RegularCauchySumUp$ operation row on $A$ and $B$.
\item $G$ is the $\RegularCauchyNegationUp$ operation row on the source row selected for additive inverse readback.
\item $M$ is the $\RegularCauchyProductUp$ operation row on $A$ and $B$.
\item $L$ is the $\RegularCauchyScaleUp$ operation row for a displayed rational scalar and the selected source row.
\item $R_S,R_G,R_M,R_L$ are the terminal $\RealUp$ seal rows reached respectively after the sum, negation, product, and scale readbacks.
\item $E_S,E_G,E_M,E_L$ are the finite dyadic and error-budget ledgers consumed by those operation rows before the terminal seals are public.
\item $H$ stores componentwise $\hsame$ transport for the two sources, aligned windows, dyadic ledgers, four operation rows, four terminal seal rows, and four finite budget ledgers.
\item $C$ stores displayed $\Cont$ replay for opening the source windows, reading dyadic endpoint data, invoking the four operation packets, and exposing the terminal real-name seals.
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchySumUp$, $\RegularCauchyNegationUp$, $\RegularCauchyProductUp$, $\RegularCauchyScaleUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyRingUp}$ row.
\end{enumerate}
The carrier is a finite algebraic operation packet over regular Cauchy real names. It stores no quotient stream, no host real equality, no selected completed limit, no trichotomy, no countable-choice row, and no $\RealUp$ completeness principle.
\end{definition}

\begin{theorem}[Regular Cauchy ring NameCert obligations]
\label{thm:regular-cauchy-ring-namecert-obligations}
The local $\NameCert_{\RegularCauchyRingUp}$ surface for the carrier of \autoref{def:regular-cauchy-ring-carrier} consists of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $K$ with two regular-rational source rows, aligned stream windows, dyadic endpoint ledgers, four operation rows, four terminal real-name seal rows, four finite budget ledgers, transport, continuation, provenance, and local naming data;
\item source-window alignment, requiring $S$, $M$, and the selected-source rows for $G$ and $L$ to read only the displayed $\StreamNameUp$ windows and $\DyadicRatCoreUp$ ledgers;
\item operation admission, requiring the sum, negation, product, and scale rows to be accepted packets of their cited sibling surfaces;
\item componentwise classifier transport through the stored $\hsame$ rows and displayed $\Cont$ replay;
\item terminal seal handoff, requiring every real-facing consumer of the ring packet to pass through the operation readback row and its displayed budget ledger before reading one of $R_S,R_G,R_M,R_L$.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyRingUp.RegularCauchyRingCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-cauchy-ring-carrier}. The two regular-rational sources, aligned stream windows, dyadic endpoint rows, four operation rows, budget ledgers, terminal seals, transports, continuations, provenance, and local name are explicit coordinates of $K$.

The source-window obligation is inherited from the sibling operation packets. Apply the window and handoff surfaces of \autoref{thm:regular-cauchy-sum-namecert-obligations}, \autoref{thm:regular-cauchy-negation-namecert-obligations}, \autoref{thm:regular-cauchy-product-namecert-obligations}, and \autoref{thm:regular-cauchy-scale-namecert-obligations}. Each cited surface consumes the displayed source rows and their finite dyadic ledgers before exporting a regular readback.

Classifier transport is componentwise. The stored $H$ and $C$ rows transport the two sources, the aligned windows, the dyadic ledgers, the four operation rows, and the four seal rows together, so no operation can detach from the finite packet.

Finally the terminal handoff is exactly the real-seal boundary supplied by the four sibling chapters. The local $\NameCert_{\RegularCauchyRingUp}$ row therefore names the assembled algebraic surface and adds no host real arithmetic, quotient equality, selected limit, trichotomy, choice, or completeness coordinate.
\end{proof}

\begin{theorem}[Regular Cauchy ring operation-row admission]
\label{thm:regular-cauchy-ring-operation-row-admission}
For every accepted $\RegularCauchyRingUp$ carrier
$$
K=(A,B,W_A,W_B,D_A,D_B,S,G,M,L,R_S,R_G,R_M,R_L,E_S,E_G,E_M,E_L,H,C,P,N),
$$
the four operation coordinates $S,G,M,L$ are admitted only as displayed sibling rows of $\RegularCauchySumUp$, $\RegularCauchyNegationUp$, $\RegularCauchyProductUp$, and $\RegularCauchyScaleUp$. The local $\NameCert_{\RegularCauchyRingUp}$ row cannot replace any of those rows by host real addition, host real negation, host real multiplication, scalar multiplication on completed quotients, selected limits, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-ring-carrier}. The only operation coordinates are the four displayed rows $S,G,M,L$, and each is listed beside the same source rows, stream windows, dyadic endpoint ledgers, budget ledgers, terminal seal rows, transport, replay, provenance, and local naming.

Use the sibling obligation surfaces for the four rows: \autoref{thm:regular-cauchy-sum-namecert-obligations}, \autoref{thm:regular-cauchy-negation-namecert-obligations}, \autoref{thm:regular-cauchy-product-namecert-obligations}, and \autoref{thm:regular-cauchy-scale-namecert-obligations}. These chapters admit the sum, negation, product, and scale reads as finite operation packets over $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ data.

Finally read the local NameCert obligation theorem \autoref{thm:regular-cauchy-ring-namecert-obligations}. It packages those sibling rows into one ring carrier but adds no fifth operation coordinate and no host arithmetic row, so admission of the operation rows is exactly sibling-row admission.
\end{proof}

\begin{theorem}[Regular Cauchy ring componentwise transport]
\label{thm:regular-cauchy-ring-componentwise-transport}
For every accepted $\RegularCauchyRingUp$ carrier $K$, classifier transport through $H$ preserves the two $\RegSeqRatUp$ sources, aligned $\StreamNameUp$ windows, $\DyadicRatCoreUp$ endpoint ledgers, four operation rows, four terminal $\RealUp$ seal rows, four finite budget ledgers, displayed replay, provenance, and local naming component by component. A transported ring packet therefore reads the same finite operation surface through $S,G,M,L$ and cannot detach an operation row from its displayed source, budget, or seal data.
\end{theorem}
\begin{proof}
Begin with the carrier coordinates of \autoref{def:regular-cauchy-ring-carrier}. The transport row $H$ is explicitly componentwise over the two sources, the aligned windows, the dyadic ledgers, the four operation rows, the four terminal seal rows, the four finite budget ledgers, replay, provenance, and local naming.

Apply the operation-row admission theorem \autoref{thm:regular-cauchy-ring-operation-row-admission}. Since $S,G,M,L$ are admitted only as displayed sibling rows, transporting a packet transports those rows together with the source windows and dyadic ledgers that make the sibling reads meaningful.

The displayed replay row $C$ and provenance row $P$ preserve the same route for consumers. Thus transport can rename or replay the finite packet, but it cannot split an operation from its $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, budget, or $\RealUp$ seal boundary.
\end{proof}

\begin{theorem}[Regular Cauchy ring terminal-seal discipline]
\label{thm:regular-cauchy-ring-terminal-seal-discipline}
Every terminal $\RealUp$ seal read from an accepted $\RegularCauchyRingUp$ carrier is one of $R_S,R_G,R_M,R_L$, and it is reachable only after the corresponding operation row $S$, $G$, $M$, or $L$ and its finite budget ledger $E_S$, $E_G$, $E_M$, or $E_L$ have been displayed. No $\RealUp$ consumer of the ring packet can bypass the sibling operation chapters, the $\StreamNameUp$ windows, the $\RegSeqRatUp$ sources, the $\DyadicRatCoreUp$ ledgers, componentwise transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the terminal coordinates of the carrier. The only $\RealUp$ seal rows present in $K$ are $R_S,R_G,R_M,R_L$, paired respectively with $S,G,M,L$ and with the finite budget ledgers $E_S,E_G,E_M,E_L$.

Use the sibling seal boundaries. The sum route is governed by \autoref{thm:regular-cauchy-sum-realup-seal-boundary}; the negation route by \autoref{thm:regular-cauchy-negation-public-seal-export}; the product route by \autoref{thm:regular-cauchy-product-real-seal-boundary}; and the scale route by \autoref{thm:regular-cauchy-scale-realup-seal-boundary}. Each route exposes finite windows, regular readback, dyadic data, and budget rows before the terminal seal is public.

Combine those sibling boundaries with componentwise transport from \autoref{thm:regular-cauchy-ring-componentwise-transport}. The structural rows preserve the four operation-to-seal routes and add no extra seal coordinate, so every real-facing consumer remains inside one of the four displayed terminal routes.
\end{proof}

\begin{theorem}[Regular Cauchy ring operation closure]
\label{thm:regular-cauchy-ring-operation-closure}
For every accepted $\RegularCauchyRingUp$ carrier $K$, the four displayed algebraic operations close inside the finite regular-Cauchy naming surface: addition exposes $S$ and $R_S$, negation exposes $G$ and $R_G$, product exposes $M$ and $R_M$, and rational scaling exposes $L$ and $R_L$. Each operation reads only the displayed $\RegSeqRatUp$ sources, aligned $\StreamNameUp$ windows, $\DyadicRatCoreUp$ ledgers, finite budget rows, componentwise $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyRingUp.RegularCauchyRingOperationClosure}
\begin{proof}
Read the sum component first. The carrier exposes the two source rows, aligned windows, dyadic endpoint ledgers, the finite sum budget row $E_S$, and the operation row $S$; \autoref{thm:regular-cauchy-sum-regseqrat-handoff} and \autoref{thm:regular-cauchy-sum-realup-seal-boundary} then route the readback to $R_S$.

Read the unary component next. The negation row $G$ uses its selected source window and dyadic ledger, and \autoref{thm:regular-cauchy-negation-algebraic-handoff} with \autoref{thm:regular-cauchy-negation-public-seal-export} routes the sign-flipped regular readback to $R_G$.

For multiplication, project $M$ together with $E_M$. The product handoff and seal boundary of \autoref{thm:regular-cauchy-product-regseqrat-handoff} and \autoref{thm:regular-cauchy-product-real-seal-boundary} keep the product inside the displayed dyadic product and budget rows before $R_M$ is public.

For rational scaling, project $L$ together with $E_L$. The scale handoff and seal boundary of \autoref{thm:regular-cauchy-scale-regseqrat-handoff} and \autoref{thm:regular-cauchy-scale-realup-seal-boundary} keep the scaled output inside the displayed scalar, source, window, dyadic, and budget rows before $R_L$ is public.

These four projections exhaust the carrier's algebraic coordinates. The operation closure is therefore closure of the finite BEDC packet surface, not a statement about quotient real equality or ambient real algebra.
\end{proof}

\begin{theorem}[Regular Cauchy ring windowwise associativity]
\label{thm:regular-cauchy-ring-windowwise-associativity}
For every accepted $\RegularCauchyRingUp$ carrier, the displayed sum, product, and rational-scale windows compose through the same $\StreamNameUp$ and $\DyadicRatCoreUp$ ledgers under reassociation. More precisely, each reassociated route first reads the two source rows, aligned windows, dyadic endpoint ledgers, finite budget rows, operation rows, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row, and then reaches the same terminal $\RealUp$ seal boundary for the corresponding sum, product, or scale readback.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-ring-carrier}. The only operation coordinates are $S,G,M,L$ together with the aligned source windows, dyadic ledgers, budget rows, transports, replay, provenance, and terminal seal rows.

For sums, reassociation changes only the order in which the finite $\RegularCauchySumUp$ rows are consumed. The source-window alignment and terminal seal handoff of \autoref{thm:regular-cauchy-ring-namecert-obligations} keep both reads on the same displayed $\StreamNameUp$ windows and $\DyadicRatCoreUp$ ledgers before $R_S$ is public.

For products and rational scales, use the same argument with the product and scale routes. The product route is governed by \autoref{thm:regular-cauchy-product-regseqrat-handoff} and \autoref{thm:regular-cauchy-product-real-seal-boundary}; the scale route is governed by \autoref{thm:regular-cauchy-scale-regseqrat-handoff} and \autoref{thm:regular-cauchy-scale-realup-seal-boundary}. These sibling handoffs expose finite readbacks and then the terminal $\RealUp$ seal, so reassociation cannot introduce host real equality, selected limits, trichotomy, choice, or ambient completeness.
\end{proof}

\begin{theorem}[Regular Cauchy ring componentwise distributivity]
\label{thm:regular-cauchy-ring-componentwise-distributivity}
For every accepted $\RegularCauchyRingUp$ carrier, a product-over-sum read is witnessed componentwise through the $\RegularCauchyProductUp$ and $\RegularCauchySumUp$ budget rows. The left route reads the product row after the summed source row is displayed; the right route reads two product rows and then the sum row; both routes remain inside the displayed source windows, dyadic endpoint ledgers, finite budget rows, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, local $\NameCert$ row, and terminal $\RealUp$ seal boundary. No step appeals to host real distributivity.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyRingUp.RegularCauchyRingCarrier\_componentwise\_distributivity}
\begin{proof}
Start with the operation closure of \autoref{thm:regular-cauchy-ring-operation-closure}. It identifies the available addition and product rows as projections of the finite carrier, not as ambient operations on completed real quotients.

Build the left route by reading the $\RegularCauchySumUp$ budget row for the two addends and then the $\RegularCauchyProductUp$ row for the multiplier and the displayed sum readback. The sum handoff of \autoref{thm:regular-cauchy-sum-regseqrat-handoff} and the product handoff of \autoref{thm:regular-cauchy-product-regseqrat-handoff} keep the route in regular rational readback data.

Build the right route by reading the two product rows first and then the sum row on their readbacks. The seal-boundary theorems \autoref{thm:regular-cauchy-product-real-seal-boundary} and \autoref{thm:regular-cauchy-sum-realup-seal-boundary} keep both products and the final sum behind the displayed dyadic and budget ledgers.

The componentwise $\hsame$ transport and displayed $\Cont$ replay rows move the two routes together. Therefore the distributive read is a finite packet obligation through $\RegularCauchyProductUp$ and $\RegularCauchySumUp$, not an imported law of host real multiplication and addition.
\end{proof}

\begin{theorem}[Regular Cauchy ring Real seal non-escape]
\label{thm:regular-cauchy-ring-real-seal-nonescape}
Every $\RealUp$ consumer of an accepted $\RegularCauchyRingUp$ carrier reaches a terminal seal only through one of the four displayed routes
$$
S\longmapsto R_S,\qquad G\longmapsto R_G,\qquad M\longmapsto R_M,\qquad L\longmapsto R_L,
$$
together with the relevant source windows, dyadic endpoint ledgers, finite budget ledger, componentwise $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\RegularCauchyRingUp}$ row. The packet exports no quotient stream, host real equality, selected completed limit, trichotomy, countable choice, or $\RealUp$ completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyRingUp.RegularCauchyRingCarrier\_real\_seal\_nonescape}
\begin{proof}
Begin with the terminal seal clause of \autoref{thm:regular-cauchy-ring-namecert-obligations}. It forces any real-facing consumer to choose one of the four operation routes and to keep the corresponding source, window, dyadic, budget, transport, continuation, provenance, and naming rows attached.

The sibling non-escape clauses then close the four cases. For the sum route use \autoref{thm:regular-cauchy-sum-non-escape-boundary}; for the negation route use \autoref{thm:regular-cauchy-negation-apartness-nonescape}; for the product route use \autoref{thm:regular-cauchy-product-real-algebra-boundary}; for the scale route use \autoref{thm:regular-cauchy-scale-real-seal-handoff-obligation}.

Since the ring carrier contains only those four terminal routes and their shared structural rows, no real-facing consumer can read a fifth route through host arithmetic, quotient real equality, selected limits, trichotomy, countable choice, or ambient completeness.
\end{proof}

\closureat{RegularCauchyRingUp}{seedStr}

\begin{closurestatus}{\RegularCauchyRingUp}
  \constructivestory{A $\RegularCauchyRingUp$ packet is generated from two RegSeqRat source rows, aligned StreamName windows, DyadicRatCore endpoint ledgers, RegularCauchySum, RegularCauchyNegation, RegularCauchyProduct, and RegularCauchyScale operation rows, terminal RealUp seal rows, finite budget ledgers, componentwise transport, continuation replay, package provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-ring-carrier}, \autoref{thm:regular-cauchy-ring-namecert-obligations}, \autoref{thm:regular-cauchy-ring-operation-closure}, and \autoref{thm:regular-cauchy-ring-real-seal-nonescape} declare the finite algebraic operation packet and its Real seal boundary over regular Cauchy names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient real equality, host real arithmetic laws, trichotomy, countable choice, selected completed limits, RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, operation-row admission, componentwise transport, and Real seal non-escape for the same finite algebraic packet.}
\end{closurestatus}
