\begin{theorem}[Effective Cauchy completion L10 dependency route]\label{thm:effective-cauchy-completion-l10-dependency-route}
Every $\EffectiveCauchyCompletionUp$ completion read consumes its L10 dependency lattice before it exports completion evidence: finite windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational Cauchy readback from \autoref{ch:concrete-instances-regseqrat-namecert}, terminal real-name sealing from \autoref{ch:concrete-instances-real-namecert}, and the isometry-facing completion route from \autoref{ch:concrete-instances-cauchy-completion-isometry-namecert}. These sibling rows must be visible before the packet presents $Q,W,R,D,F,S,H,C,P,N$ as an effective completion surface.
\end{theorem}
\begin{proof}
Read the carrier through \autoref{def:effective-cauchy-completion-carrier}. The rows $W,R,S$ are exactly the finite-window, regular-rational, and terminal real-name entries, while $F$ is the completion-facing route row.

Apply \autoref{thm:effective-cauchy-completion-namecert-obligations}. It requires the source, window, readback, tolerance, completion route, and real seal to be exposed before transport, replay, provenance, and naming are consumed.

Thus the L10 siblings are not optional commentary. They are the dependency route through which completion evidence becomes visible, and no quotient completion, selected representative, selected limit, or ambient completeness row is exported outside that route.
\end{proof}
