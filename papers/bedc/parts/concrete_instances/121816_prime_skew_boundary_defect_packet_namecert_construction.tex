\chapter{A Concrete Naming Certificate for $\PrimeSkewBoundaryDefectPacketUp$}
\label{ch:concrete-instances-prime-skew-boundary-defect-packet-namecert}
\origin{ai}

The $\PrimeSkewBoundaryDefectPacketUp$ packet realizes the prime-skew boundary-defect route of \autoref{ch:visions-rh-prime-skew-boundary-defect-refutation} as finite NameCert data. It consumes the RH-free zero-generation initiality boundary of \autoref{ch:concrete-instances-rh-free-zero-initiality-namecert}, the zeta-zero type-factorization surface of \autoref{ch:concrete-instances-zeta-zero-type-factorization-namecert}, the finite prime-window gluing row of \autoref{ch:concrete-instances-finite-prime-window-gluing-namecert}, the prime shadow interface of \autoref{ch:concrete-instances-prime-shadow-interface-namecert}, and the zeta-zero catalogue of \autoref{ch:concrete-instances-zeta-zeros-namecert}. The packet records conditional evidence routing for prime-visible skew and boundary defects; it is not a proof of the Riemann Hypothesis, not a zero-location theorem, and not an analytic completion of the boundary faithfulness or zero-defect incompatibility premises.

\begin{definition}[Prime-skew boundary defect packet carrier]
\label{def:prime-skew-boundary-defect-packet-carrier}
A $\PrimeSkewBoundaryDefectPacketUp$ carrier is a finite $\BHist$ packet
$$
P=(Z,R,S,B,I,F,X,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the non-trivial zeta-zero source row, read through $\ZetaZerosUp$ and the zero-type factorization surface.
\item $R$ is the bulk-representation row, recording the displayed map from zero packets into a finite boundary-readout carrier.
\item $S$ is the prime-skew row, naming the finite prime-visible skew evidence extracted from displayed $\PrimeUp$ channels.
\item $B$ is the boundary-defect row, recording the finite boundary layer at which the prime-visible skew is observed as a defect.
\item $I$ is the zero-defect incompatibility row, storing the conditional refutation interface between a represented non-trivial zero atom and the displayed defect row.
\item $F$ is the boundary-faithfulness row, recording the conditional route from prime-skew evidence to boundary-defect evidence.
\item $X$ is the refusal row, retaining failed attempts to read the packet as an unconditional RH proof, a global zero-location classifier, an analytic continuation theorem, or a completed boundary-faithfulness proof.
\item $H$ records componentwise $\hsame$ transport for zero source, representation, prime skew, boundary defect, incompatibility, faithfulness, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an RH-facing audit request through $Z,R,S,B,I,F,X,H$.
\item $Q$ is the $\Pkg$ provenance over $\PrimeSkewBoundaryDefectPacketUp$, $\RhFreeZeroInitialityUp$, $\ZetaZeroTypeFactorizationUp$, $\FinitePrimeWindowGluingUp$, $\PrimeShadowInterfaceUp$, $\ZetaZerosUp$, $\PrimeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PrimeSkewBoundaryDefectPacketUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Z,R,S,B,I,F,X,H,C,Q,N$. It has no coordinate for an unconditional proof of $\mathsf{RH}$, a full zero-location classifier, completed boundary faithfulness, completed zero-defect incompatibility, host complex equality, or analytic continuation outside the displayed zeta-zero route.
\end{definition}

\begin{theorem}[Prime-skew boundary defect packet]
\label{thm:rh-prime-skew-boundary-defect-packet}
For every accepted $\PrimeSkewBoundaryDefectPacketUp$ carrier
$$
P=(Z,R,S,B,I,F,X,H,C,Q,N),
$$
the local $\NameCert_{\PrimeSkewBoundaryDefectPacketUp}$ surface consists exactly of zero-source admission, bulk-representation visibility, prime-skew visibility, boundary-defect visibility, zero-defect incompatibility routing, boundary-faithfulness routing, RH-proof refusal, componentwise transport, displayed replay, package provenance, and local naming. A consumer may use the packet only as conditional evidence routing from the prime-skew row to the boundary-defect row and then to the incompatibility row; no accepted read exports an RH proof or an off-line-zero exclusion without separately displaying the vision-side premises.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:prime-skew-boundary-defect-packet-carrier}. The mathematical rows are $Z,R,S,B,I,F,X$, and the structural rows are $H,C,Q,N$.

The zero source and representation rows are read first. They bind the zeta-zero data to the displayed boundary-readout carrier without selecting a host complex representative or completing an analytic zero-location theorem.

The prime-skew and boundary-defect rows are conditional. Row $S$ names the finite prime-visible skew evidence, row $F$ routes that evidence to $B$, and row $I$ records the incompatibility interface once the represented zero atom and defect row are both displayed.

Finally use the refusal row $X$ and the structural rows $H,C,Q,N$. They preserve the packet as conditional evidence routing and retain any attempted shortcut to RH, global zero-location, analytic continuation, or completed boundary faithfulness as a refused read rather than a public conclusion.
\end{proof}

\begin{theorem}[Prime-skew boundary defect conditional nonescape]
\label{thm:prime-skew-boundary-defect-conditional-nonescape}
Every RH-facing read of an accepted $\PrimeSkewBoundaryDefectPacketUp$ packet factors through the displayed route
$$
\ZetaZerosUp,\ \ZetaZeroTypeFactorizationUp,\ \PrimeUp,\ \FinitePrimeWindowGluingUp,\ \PrimeShadowInterfaceUp,\ \RhFreeZeroInitialityUp
$$
before the prime-skew, boundary-defect, faithfulness, incompatibility, refusal, transport, replay, provenance, and local naming rows are public. The read exports no unconditional RH theorem, no global zero-location classifier, no completed proof of boundary faithfulness, no completed proof of zero-defect incompatibility, and no host analytic continuation claim.
\end{theorem}
\begin{proof}
Apply \autoref{thm:rh-prime-skew-boundary-defect-packet}. It exposes the only public rows of the packet and keeps the evidence route downstream of the zero-source, representation, prime-skew, boundary-defect, faithfulness, and incompatibility rows.

The sibling sources supply the finite entry points. The zeta-zero and zero-type rows bind the zero packet, the prime and finite-prime-window rows bind the skew observation, the prime-shadow interface binds the boundary readout, and the RH-free initiality row keeps generated-zero no-circularity separate from fixed-RH proof claims.

The refusal row and structural rows close the boundary. Because no carrier coordinate stores an unconditional RH theorem, a global zero-location classifier, completed boundary faithfulness, completed zero-defect incompatibility, or analytic continuation outside the zeta-zero route, no consumer can extract those claims from the accepted packet.
\end{proof}

\falsifiablePrediction{Every accepted $\PrimeSkewBoundaryDefectPacketUp$ consumer that exports an RH proof, off-line-zero exclusion, or global zero-location classifier without first displaying zero-source, representation, prime-skew, boundary-defect, faithfulness, incompatibility, refusal, transport, replay, provenance, and local naming rows is outside the carrier surface.}

\independenceWitness{prime, zeta, complex, riemann-hilbert, rh-free-zero-generation-initiality: $\PrimeSkewBoundaryDefectPacketUp$ is not bijective to any listed source because it jointly carries prime-skew evidence, boundary-defect evidence, conditional faithfulness, conditional incompatibility, RH-proof refusal, transport, replay, provenance, and local naming while refusing any unconditional RH conclusion.}

\closureat{PrimeSkewBoundaryDefectPacketUp}{seedStr}

\begin{closurestatus}{\PrimeSkewBoundaryDefectPacketUp}
  \constructivestory{A $\PrimeSkewBoundaryDefectPacketUp$ packet is generated from zeta-zero source rows, bulk-representation rows, prime-skew rows, boundary-defect rows, zero-defect incompatibility rows, boundary-faithfulness rows, RH-proof refusal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:prime-skew-boundary-defect-packet-carrier}, \autoref{thm:rh-prime-skew-boundary-defect-packet}, and \autoref{thm:prime-skew-boundary-defect-conditional-nonescape} seed the finite conditional evidence route for the RH prime-skew boundary-defect vision.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim the Riemann Hypothesis, off-line-zero exclusion, global zero-location, completed boundary faithfulness, completed zero-defect incompatibility, analytic continuation, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked zero-source admission, bulk-representation visibility, prime-skew visibility, boundary-defect visibility, faithfulness routing, incompatibility routing, RH-proof refusal, transport, replay, provenance, and local naming for BEDC.Derived.PrimeSkewBoundaryDefectPacketUp.}
\end{closurestatus}
