\chapter{A Concrete Naming Certificate for $\HigsonCoronaUp$}
\label{ch:concrete-instances-higson-corona-namecert}
\origin{human}

Following Higson and Roe on coarse topology, the $\HigsonCoronaUp$ packet records the boundary consumer of the Higson compactification route. It reads the compactification carrier of \autoref{ch:concrete-instances-higson-compactification-namecert}, compares its boundary-facing rows with the Stone--Cech surface of \autoref{ch:concrete-instances-stone-cech-compactification-namecert}, and keeps the uniform source discipline of \autoref{ch:concrete-instances-uniformspace-namecert} visible through the metric, continuous-map, and real-valued observation rows. The packet names a finite corona interface; it does not choose boundary points, ultrafilters, quotient compactification classes, or ambient coarse-equivalence witnesses.

\begin{definition}[Higson corona carrier]
\label{def:higson-corona-carrier}
A $\HigsonCoronaUp$ carrier is a finite $\BHist$ packet
$$
Q=(K,B,U,M,F,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\HigsonCompactificationUp$ boundary row, read as the compactification-side source of corona observations.
\item $B$ is the $\StoneCechCompactificationUp$ comparison row, used only to compare bounded-probe boundary data with the Higson boundary route.
\item $U$ is the $\UniformSpaceUp$ source row inherited by the compactification packet.
\item $M$ is the $\MetricUp$ large-scale control row that keeps coarse escape and bounded-region information visible.
\item $F$ is the finite $\ContinuousMapUp$ row of slowly oscillating bounded real-valued observations admitted by the boundary read.
\item $R$ is the $\RealUp$ value-seal row for the finite boundary observation family.
\item $H$ records componentwise $\hsame$ transport for $K,B,U,M,F,R,C,P,N$.
\item $C$ records displayed $\Cont$ replay from the compactification boundary row through the finite observation and comparison rows.
\item $P$ is the $\Pkg$ provenance over $\HigsonCoronaUp$, $\HigsonCompactificationUp$, $\StoneCechCompactificationUp$, $\UniformSpaceUp$, $\MetricUp$, $\ContinuousMapUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HigsonCoronaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,B,U,M,F,R,H,C,P,N$. It has no coordinate for selected corona points, ultrafilter choice, quotient host compactification equality, ambient properness, or a global coarse compactification theorem.
\end{definition}

\begin{theorem}[Higson corona NameCert obligations]
\label{thm:higson-corona-namecert-obligations}
For every accepted $\HigsonCoronaUp$ carrier
$$
Q=(K,B,U,M,F,R,H,C,P,N),
$$
the local $\NameCert_{\HigsonCoronaUp}$ surface consists exactly of compactification-boundary admission, Stone--Cech comparison, inherited uniform source exposure, large-scale metric control, finite slowly oscillating continuous observation, Real value sealing, componentwise transport, displayed replay, provenance, and local naming.

Every boundary-facing consumer must read the displayed route
$$
K,B,U,M \longmapsto F,R \longmapsto H,C,P,N
$$
before it may use a corona observation.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $K,B,U,M$ expose the Higson compactification boundary source, the Stone--Cech comparison surface, the uniform source row, and the large-scale metric control row.

The observation rows $F,R$ are downstream of that boundary source. They record only finite slowly oscillating continuous real-valued observations and their Real value seals, so a consumer cannot replace them by an ambient function algebra or an unbounded host observation family.

The structural rows $H,C,P,N$ transport, replay, package, and name the same displayed route. Since the carrier has no coordinate for selected corona points, ultrafilter choice, quotient host compactification equality, ambient properness, or a global coarse compactification theorem, those data are not exported by the local certificate.
\end{proof}

\begin{theorem}[Higson corona compactification boundary]
\label{thm:higson-corona-compactification-boundary}
Every accepted $\HigsonCoronaUp$ boundary read is a consumer of the accepted $\HigsonCompactificationUp$ route before it is a compactification comparison. In particular, the boundary read must expose the $\HigsonCompactificationUp$ row, the $\StoneCechCompactificationUp$ comparison row, the $\UniformSpaceUp$ source row, the $\MetricUp$ control row, the finite $\ContinuousMapUp$ observation row, the $\RealUp$ seal row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\HigsonCoronaUp}$ row.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:higson-corona-carrier}. The row $K$ is the only compactification-boundary source available to the packet, so every corona request first passes through the Higson compactification route of \autoref{ch:concrete-instances-higson-compactification-namecert}.

The comparison with Stone--Cech compactification is then read through $B$ and not as a replacement for $K$. The inherited rows $U,M,F,R$ retain uniform source data, large-scale metric control, finite continuous observations, and Real value seals before any boundary consumer can use the comparison.

Apply Theorem~\ref{thm:higson-corona-namecert-obligations}. It exhausts the remaining structural rows $H,C,P,N$ and excludes selected boundary points, ultrafilters, quotient host compactification equality, ambient properness, and global coarse compactification data from the boundary surface.
\end{proof}

\falsifiablePrediction{Every accepted $\HigsonCoronaUp$ boundary read exposes a $\HigsonCompactificationUp$ boundary row, a $\StoneCechCompactificationUp$ comparison row, $\UniformSpaceUp$ source data, $\MetricUp$ large-scale control, finite slowly oscillating $\ContinuousMapUp$ observations, $\RealUp$ value seals, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{higson-compactification, stone-cech-compactification, uniformspace, metric, continuousmap, real: $\HigsonCoronaUp$ is not bijective to any listed sibling because it binds a Higson compactification boundary consumer to Stone--Cech comparison, uniform source data, large-scale metric control, finite continuous real-valued observations, and local NameCert provenance in one finite carrier.}

\closureat{HigsonCoronaUp}{seedStr}

\begin{closurestatus}{\HigsonCoronaUp}
  \constructivestory{A $\HigsonCoronaUp$ packet is generated from a Higson compactification boundary row, a Stone--Cech comparison row, inherited uniform and metric source rows, finite slowly oscillating continuous real-valued observations, Real value seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:higson-corona-carrier}, \autoref{thm:higson-corona-namecert-obligations}, and \autoref{thm:higson-corona-compactification-boundary} seed the finite Higson corona boundary consumer over compactification, comparison, uniform, metric, continuous-map, Real, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected corona points, ultrafilter choice, quotient host compactification equality, ambient properness, global coarse compactification theorems, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked boundary admission, comparison stability, inherited uniform and metric source exposure, finite observation replay, value sealing, transport, provenance, and local naming for BEDC.Derived.HigsonCoronaUp.}
\end{closurestatus}
