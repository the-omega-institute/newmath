\chapter{A Concrete Naming Certificate for $\BishopModulusProductClosureUp$}
\label{ch:concrete-instances-bishop-modulus-product-closure-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopModulusProductClosureUp$ packet records the explicit-modulus product closure route for two constructive real names before the result is sealed as a Real-facing product. It binds the source-modulus surface of \autoref{ch:concrete-instances-bishop-modulus-real-namecert}, the dyadic split of \autoref{ch:concrete-instances-dyadic-error-budget-namecert}, the regular-Cauchy multiplication route of \autoref{ch:concrete-instances-regular-cauchy-multiplication-namecert}, the Cauchy product sibling of \autoref{ch:concrete-instances-cauchyproduct-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite certificate that product reads consume displayed moduli, displayed dyadic budgets, and displayed regular-Cauchy product rows rather than quotient stream equality, selected limits, host real multiplication, countable choice, trichotomy, or ambient completeness.

\begin{definition}[Bishop modulus product-closure carrier]
\label{def:bishop-modulus-product-closure-carrier}
A $\BishopModulusProductClosureUp$ carrier is a finite $\BHist$ packet
$$
Z=(M_X,M_Y,Q,A,B,S,W_X,W_Y,D_X,D_Y,D_Z,R_X,R_Y,U,V,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M_X$ and $M_Y$ are the two $\BishopModulusRealUp$ source routes. Each row exposes one explicit Bishop modulus, its StreamName window discipline, DyadicRatCore tolerance ledger, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming.
\item $Q$ is the requested dyadic tolerance for the product read.
\item $A$ and $B$ are the two sub-budget rows assigned to the two source-modulus error contributions.
\item $S$ is the $\DyadicErrorBudgetUp$ split row certifying that $A$ and $B$ exhaust the local error split for $Q$.
\item $W_X$ and $W_Y$ are the finite $\StreamNameUp$ factor-window rows opened through the source moduli $M_X$ and $M_Y$.
\item $D_X$ and $D_Y$ are the two $\DyadicRatCoreUp$ factor-error rows read from the displayed windows.
\item $D_Z$ is the $\DyadicRatCoreUp$ product-error row that combines $D_X,D_Y$ under the split $Q,A,B,S$.
\item $R_X$ and $R_Y$ are the two $\RegSeqRatUp$ source readbacks exposed by the source-modulus rows before multiplication.
\item $U$ is the $\RegularCauchyMultiplicationUp$ row consuming $R_X,R_Y,W_X,W_Y,D_X,D_Y,D_Z$ under the displayed budget split.
\item $V$ is the $\CauchyProductUp$ sibling row that presents the same product read as the Cauchy-product NameCert surface.
\item $E$ is the terminal $\RealUp$ product seal reached only after $M_X,M_Y,Q,A,B,S,W_X,W_Y,D_X,D_Y,D_Z,R_X,R_Y,U,V$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two source moduli, the error-budget split, windows, dyadic rows, regular readbacks, product rows, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each Real-facing product request through the same finite route.
\item $P$ is the $\Pkg$ provenance over $\BishopModulusProductClosureUp$, $\BishopModulusRealUp$, $\DyadicErrorBudgetUp$, $\RegularCauchyMultiplicationUp$, $\CauchyProductUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopModulusProductClosureUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M_X,M_Y,Q,A,B,S,W_X,W_Y,D_X,D_Y,D_Z,R_X,R_Y,U,V,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, countable choice, host real equality, host real multiplication, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Bishop modulus product-closure NameCert obligations]
\label{thm:bishop-modulus-product-closure-namecert-obligations}
For every accepted $\BishopModulusProductClosureUp$ carrier
$$
Z=(M_X,M_Y,Q,A,B,S,W_X,W_Y,D_X,D_Y,D_Z,R_X,R_Y,U,V,E,H,C,P,N),
$$
the local $\NameCert_{\BishopModulusProductClosureUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two $\BishopModulusRealUp$ source routes, requested tolerance, two sub-budgets, split certificate, StreamName windows, DyadicRatCore factor and product rows, RegSeqRat source readbacks, RegularCauchyMultiplication row, CauchyProduct row, Real seal, transport, replay, provenance, and local naming;
\item source-modulus exactness, requiring $M_X$ and $M_Y$ to be read before any factor window, dyadic factor row, regular readback, multiplication row, CauchyProduct row, or Real product seal is exposed;
\item error-budget split exhaustion, requiring $Q,A,B,S$ to be the only local product-error budget route and to factor through the $\DyadicErrorBudgetUp$ split surface;
\item product-row compatibility, requiring $W_X,W_Y,D_X,D_Y,D_Z,R_X,R_Y$ to be the common rows consumed by both $U$ and $V$;
\item Real-seal nonescape, requiring $E$ to remain terminal with respect to the displayed source-modulus, budget, window, dyadic, regular-readback, and product rows;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same finite route under $\hsame$ transport and $\Cont$ replay;
\item provenance and local naming, requiring $P$ and $N$ to package exactly the displayed rows and no additional quotient, selected-limit, choice, trichotomy, host-equality, host-multiplication, completeness, Lean, or bridge data.
\end{enumerate}
No Bishop modulus product-closure obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-modulus-product-closure-carrier}. The packet displays the two source-modulus rows, the product tolerance and split, the two factor windows, the dyadic factor and product ledgers, the regular readbacks, the two product rows, the Real seal, and the structural rows.

Source-modulus exactness follows by reading \autoref{ch:concrete-instances-bishop-modulus-real-namecert} twice. The rows $M_X$ and $M_Y$ expose the two Bishop modulus routes before $W_X,W_Y,D_X,D_Y,R_X,R_Y$ can be consumed.

The error-budget clause is the sibling route through \autoref{ch:concrete-instances-dyadic-error-budget-namecert}. The request $Q$, sub-budgets $A,B$, and split row $S$ are the only budget coordinates in the carrier, so the split is exhausted by those displayed rows.

Product-row compatibility reads \autoref{ch:concrete-instances-regular-cauchy-multiplication-namecert} and \autoref{ch:concrete-instances-cauchyproduct-namecert} over the same source data. Both product rows consume the displayed StreamName windows, DyadicRatCore rows, and RegSeqRat readbacks, so neither row can introduce a second product source.

Finally apply the Real seal discipline from \autoref{ch:concrete-instances-real-namecert}. The row $E$ is terminal and downstream of the two source-modulus routes, the budget split, the finite windows, the dyadic rows, the regular readbacks, and the product rows, while $H,C,P,N$ transport, replay, package, and name exactly that route.
\end{proof}
\leanchecked{BEDC.Derived.BishopModulusProductClosureUp.BishopModulusProductClosureTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bishop modulus product-closure Real product seal]
\label{thm:bishop-modulus-product-closure-real-product-seal}
For every accepted $\BishopModulusProductClosureUp$ carrier
$$
Z=(M_X,M_Y,Q,A,B,S,W_X,W_Y,D_X,D_Y,D_Z,R_X,R_Y,U,V,E,H,C,P,N),
$$
every $\RealUp$-facing product read factors through the finite route
$$
M_X,M_Y \longmapsto Q,A,B,S \longmapsto W_X,W_Y \longmapsto D_X,D_Y,D_Z \longmapsto R_X,R_Y \longmapsto U,V \longmapsto E,H,C,P,N .
$$
Consequently the read factors through $\BishopModulusRealUp$, $\DyadicErrorBudgetUp$, $\RegularCauchyMultiplicationUp$, $\CauchyProductUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before the Real product seal is available.
\end{theorem}
\begin{proof}
Begin with a Real-facing product read of $Z$. By \autoref{def:bishop-modulus-product-closure-carrier}, the only Real-facing coordinate is the terminal row $E$.

The source part of the route is forced by the two $\BishopModulusRealUp$ rows of \autoref{ch:concrete-instances-bishop-modulus-real-namecert}. A consumer reaches factor windows, dyadic factor rows, and regular readbacks only after $M_X$ and $M_Y$ have displayed the corresponding modulus routes.

The product-budget part is forced by \autoref{ch:concrete-instances-dyadic-error-budget-namecert}. The rows $Q,A,B,S$ expose the finite budget split before $D_Z$ can be used as the product-error row.

The multiplication part is forced by \autoref{ch:concrete-instances-regular-cauchy-multiplication-namecert} and \autoref{ch:concrete-instances-cauchyproduct-namecert}. The rows $U$ and $V$ consume the same windows, dyadic rows, and RegSeqRat readbacks, and neither row precedes the source-modulus or error-budget routes.

The final seal is the Real boundary of \autoref{ch:concrete-instances-real-namecert}. Since $E$ is downstream of $M_X,M_Y,Q,A,B,S,W_X,W_Y,D_X,D_Y,D_Z,R_X,R_Y,U,V$, every Real-facing product read factors through the route in the statement and through the structural rows $H,C,P,N$.
\end{proof}

\closureat{BishopModulusProductClosureUp}{seedStr}

\begin{closurestatus}{\BishopModulusProductClosureUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-modulus-product-closure-carrier}, \autoref{thm:bishop-modulus-product-closure-namecert-obligations}, and \autoref{thm:bishop-modulus-product-closure-real-product-seal} seed the finite explicit-modulus product route over two BishopModulusReal source rows, a DyadicErrorBudget split, RegularCauchyMultiplication, CauchyProduct, and a Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, host real equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-modulus exactness, error-budget split exhaustion, product-row compatibility, Real-seal nonescape, transport and replay stability, provenance, and local naming for BEDC.Derived.BishopModulusProductClosureUp.}
  \constructivestory{A $\BishopModulusProductClosureUp$ packet is generated from two BishopModulusReal source routes, one dyadic product request, two displayed sub-budgets, a DyadicErrorBudget split, two StreamName factor windows, DyadicRatCore factor and product ledgers, RegSeqRat readbacks, RegularCauchyMultiplication and CauchyProduct rows, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
