\chapter{A Concrete Naming Certificate for $\RealApartnessCompletionUp$}
\label{ch:concrete-instances-realapartnesscompletion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RealApartnessCompletionUp$ packet records the separated completion boundary for $\RealUp$ names when apartness evidence is the visible equality discriminator. It reads the apartness surface of \autoref{ch:concrete-instances-apartnessreal-namecert}, the Hausdorff separation boundary of \autoref{ch:concrete-instances-hausdorffcompletion-namecert}, the constructive completion route of \autoref{ch:concrete-instances-constructive-metric-completion-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readbacks of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names the displayed boundary between regular Cauchy completion data and apartness-separated real reads; it does not assert completeness of $\RealUp$ or import quotient equality, selected limits, countable choice, trichotomy, or host real equality.

\begin{definition}[RealApartnessCompletion carrier]
\label{def:real-apartness-completion-carrier}
A $\RealApartnessCompletionUp$ carrier is a finite $\BHist$ packet
$$
A=(X,Q,M,S,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\ApartnessRealUp$ source row, exposing apartness evidence for the real-name boundary under inspection.
\item $Q$ is the $\HausdorffCompletionUp$ separation row attached to the same source read.
\item $M$ is the $\ConstructiveMetricCompletionUp$ completion-route row through which the separated completion request is replayed.
\item $S$ is the $\StreamNameUp$ finite-window schedule used by the completion read.
\item $R$ is the $\RegSeqRatUp$ regular rational readback extracted from those stream windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare the readback rows at the requested precision.
\item $E$ is the $\RealUp$ seal reached only after the apartness, separation, completion, window, readback, and tolerance rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for the apartness row, separation row, completion route, window schedule, readback row, tolerance ledger, real seal, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from apartness inspection through Hausdorff separation, constructive completion, stream windows, regular readback, dyadic tolerance, and then the real seal.
\item $P$ is the $\Pkg$ provenance over $\RealApartnessCompletionUp$, $\ApartnessRealUp$, $\HausdorffCompletionUp$, $\ConstructiveMetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealApartnessCompletionUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient completions, host real equality, selected limits, countable choice, trichotomy, or ambient completeness of $\RealUp$.
\end{definition}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[RealApartnessCompletion obligation surface]
\label{thm:real-apartness-completion-obligation-surface}
For every accepted $\RealApartnessCompletionUp$ carrier $A=(X,Q,M,S,R,D,E,H,C,P,N)$, the local $\NameCert_{\RealApartnessCompletionUp}$ surface consists exactly of the following five finite obligations:
\begin{enumerate}
\item carrier admission for the displayed apartness, Hausdorff separation, constructive completion, stream-window, regular readback, dyadic tolerance, Real seal, transport, replay, provenance, and local naming rows;
\item separated-source exactness, requiring the $\ApartnessRealUp$ row $X$ and $\HausdorffCompletionUp$ row $Q$ to be read before any completion-facing consumer can inspect $M$;
\item regular readback discipline, requiring every completion observation to pass through $S$, $R$, and $D$ before the $\RealUp$ seal $E$ is available;
\item componentwise stability, requiring $H$ and $C$ to transport and replay only the displayed rows $X,Q,M,S,R,D,E,P,N$;
\item ledger exhaustion, requiring the packet to export no quotient completion, host real equality, selected limit, countable-choice schedule, trichotomy row, or ambient $\RealUp$ completeness principle.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionObligationSurface}
\begin{proof}
Carrier admission is projection from \autoref{def:real-apartness-completion-carrier}. The tuple displays exactly the apartness row, Hausdorff separation row, constructive completion route, stream windows, regular rational readback, dyadic tolerance ledger, Real seal, transport, replay, provenance, and local name.

Separated-source exactness is forced by the order of the visible route. The completion row $M$ is named only after the apartness row $X$ and the Hausdorff separation row $Q$ have been displayed, so a consumer cannot read completion data before seeing the separated source surface.

The regular readback discipline is the role of $S,R,D,E$. The seal row $E$ is downstream of the finite windows $S$, regular readback $R$, and dyadic tolerance ledger $D$, which keeps each Real-facing observation attached to the displayed regular-Cauchy route.

Componentwise stability and ledger exhaustion follow from $H,C,P,N$. These rows transport, replay, package, and name only the displayed packet, so they cannot add quotient completions, host real equality, selected limits, countable choice, trichotomy, or ambient completeness.
\end{proof}

\begin{theorem}[RealApartnessCompletion obligation routing]
\label{thm:real-apartness-completion-obligation-routing}
Every accepted $\RealApartnessCompletionUp$ packet routes each local $\NameCert$ obligation through
$$
X,Q,M,S,R,D,E,H,C,P,N
$$
in that order. Carrier admission, separated-source exactness, regular readback discipline, componentwise stability, and ledger exhaustion are exactly the five projections of this finite route over the $\ApartnessRealUp$, $\HausdorffCompletionUp$, $\ConstructiveMetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionObligationRouting}
\begin{proof}
Project the carrier of \autoref{def:real-apartness-completion-carrier}. The displayed tuple places the apartness source, Hausdorff separation row, constructive completion route, stream windows, regular readback, dyadic tolerance ledger, and Real seal before the structural rows.

Apply the obligation surface of \autoref{thm:real-apartness-completion-obligation-surface}. Its five clauses are carrier admission, separated-source exactness, regular readback discipline, componentwise stability, and ledger exhaustion, so each clause is a projection of the same ordered route.

Finally use the non-escape boundary of \autoref{thm:real-apartness-completion-nonescape}. Since quotient completion, choice, trichotomy, host equality, and ambient completeness rows are absent, the obligation surface has no route outside the displayed packet.
\end{proof}

\begin{theorem}[RealApartnessCompletion obligation partition]
\label{thm:real-apartness-completion-obligation-partition}
For every accepted $\RealApartnessCompletionUp$ packet $A=(X,Q,M,S,R,D,E,H,C,P,N)$, the local obligation surface partitions into separated-source rows $(X,Q,M)$, finite window and readback rows $(S,R,D)$, the terminal seal row $E$, and structural ledger rows $(H,C,P,N)$. No consumer may discharge an obligation in one partition by borrowing a row from another partition unless an explicit displayed transport or replay row in $H$ or $C$ carries that row across the named boundary.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionObligationPartition}
\begin{proof}
Project the carrier of \autoref{def:real-apartness-completion-carrier}. The tuple displays the apartness source $X$, Hausdorff separation row $Q$, constructive completion row $M$, stream window $S$, regular readback $R$, dyadic tolerance row $D$, seal $E$, and structural rows $H,C,P,N$ as distinct coordinates.

Apply the obligation surface of \autoref{thm:real-apartness-completion-obligation-surface} and the routing theorem of \autoref{thm:real-apartness-completion-obligation-routing}. Their carrier, separated-source, regular readback, stability, and ledger clauses are projections of the same ordered packet, so the source rows, window/readback rows, seal row, and structural rows cannot be identified by prose alone.

The non-escape boundary of \autoref{thm:real-apartness-completion-nonescape} prevents quotient completion, selected limits, choice, trichotomy, host equality, and ambient completeness from supplying substitute rows. The separated-source exactness and regular readback discipline theorems, \autoref{thm:real-apartness-completion-separated-source-exactness} and \autoref{thm:real-apartness-completion-regular-readback-discipline}, fix the entry to $M$ through $X,Q$ and the entry to $E$ through $S,R,D$.

It remains only to account for permitted movement between partitions. Such movement must be displayed by $H$ or $C$, because those are the only transport and replay rows in the carrier. Hence a consumer cannot satisfy one partition by borrowing from another unless the borrowing is itself recorded as an explicit structural route.
\end{proof}

\begin{theorem}[RealApartnessCompletion non-escape boundary]
\label{thm:real-apartness-completion-nonescape}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Every separated completion read of $A$ factors through the finite route
$$
X,Q,M,S,R,D \longmapsto E \longmapsto H,C,P,N .
$$
Thus the boundary can expose apartness-separated real-name evidence only through the displayed $\ApartnessRealUp$, $\HausdorffCompletionUp$, $\ConstructiveMetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. It excludes quotient completions, host real equality, selected limits, countable choice, trichotomy, and ambient Real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionNonescape}
\begin{proof}
Begin with an accepted separated completion read. Carrier admission gives a single visible route from the apartness source $X$ and Hausdorff separation row $Q$ into the constructive completion row $M$.

Replay the observation path through the finite windows. The stream schedule $S$, regular rational readback $R$, and dyadic tolerance ledger $D$ must be displayed before the Real seal $E$ can be read.

Finally transport and package the same route through $H,C,P,N$. Since no other coordinate occurs in the carrier or obligation surface, the read cannot escape to quotient completions, host real equality, selected limits, countable choice, trichotomy, or ambient Real completeness.
\end{proof}

\begin{definition}[RealApartnessCompletion apartness-Cauchy window]
\label{def:real-apartness-completion-apartness-cauchy-window}
For an accepted $\RealApartnessCompletionUp$ packet $A=(X,Q,M,S,R,D,E,H,C,P,N)$, the apartness-Cauchy window is the finite prefix
$$
W_{\mathrm{ac}}(A)=(X,Q,M,S,R,D).
$$
Its rows are read with the following roles:
\begin{enumerate}
\item $X$ is the $\ApartnessRealUp$ apartness source for the boundary read.
\item $Q$ is the $\HausdorffCompletionUp$ separated-completion witness attached to that source.
\item $M$ is the $\ConstructiveMetricCompletionUp$ route by which the completion request is replayed.
\item $S$ is the $\StreamNameUp$ finite-window ledger for the inspected name.
\item $R$ is the $\RegSeqRatUp$ regular rational readback exposed by those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare the readback at the requested precision.
\end{enumerate}
The window has no coordinate for the $\RealUp$ seal $E$ and no coordinate for quotient completion, trichotomy, selected limits, countable choice, host equality, or ambient completeness.
\end{definition}
\leandef{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionApartnessCauchyWindow}

\begin{definition}[RealApartnessCompletion separated seal route]
\label{def:real-apartness-completion-separated-seal-route}
For an accepted $\RealApartnessCompletionUp$ packet $A=(X,Q,M,S,R,D,E,H,C,P,N)$, the separated seal route is the finite terminal path
$$
\Sigma_{\mathrm{sep}}(A)=(X,Q,M,S,R,D,E).
$$
It records the apartness source, Hausdorff separation row, constructive completion route, stream-window ledger, regular readback, dyadic tolerance ledger, and terminal $\RealUp$ seal as one ordered read. The route has no coordinate for a second source, quotient completion, host equality of completed names, selected limit, countable-choice schedule, trichotomy row, or ambient $\RealUp$ completeness principle.
\end{definition}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionSeparatedSealRoute}

\begin{theorem}[RealApartnessCompletion separated seal determinacy]
\label{thm:real-apartness-completion-separated-seal-determinacy}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ and $A'=(X,Q,M,S,R,D,E',H',C',P',N')$ be accepted $\RealApartnessCompletionUp$ packets with the same $\ApartnessRealUp$ source row $X$, the same $\HausdorffCompletionUp$ separation row $Q$, the same $\ConstructiveMetricCompletionUp$ route $M$, the same stream windows $S$, the same $\RegSeqRatUp$ regular readback $R$, and the same $\DyadicRatCoreUp$ tolerance ledger $D$. If both packets expose a terminal Real seal through the displayed obligation surface, then they expose the same separated terminal Real-seal route:
$$
\Sigma_{\mathrm{sep}}(A)=(X,Q,M,S,R,D,E)
$$
and
$$
\Sigma_{\mathrm{sep}}(A')=(X,Q,M,S,R,D,E')
$$
are determined by the same apartness-Cauchy window, and no alternative seal-facing route is available outside that window.
\end{theorem}
\begin{proof}
First apply the obligation partition of \autoref{thm:real-apartness-completion-obligation-partition}. It separates the source rows $(X,Q,M)$, finite window and readback rows $(S,R,D)$, terminal seal row, and structural rows, so the two packets have the same analytic prefix before either seal is read.

Separated-source exactness fixes the entry to the constructive completion route. Since both packets display the same $X,Q,M$, neither packet can enter the terminal route through a quotient completion, host equality row, selected limit, or external completion object.

Regular readback discipline then fixes the seal-facing window. The shared $S,R,D$ rows are the only rows through which a Real-facing read may reach a terminal seal, and \autoref{def:real-apartness-completion-apartness-cauchy-window} names this common prefix as $W_{\mathrm{ac}}$.

Finally use the non-escape boundary and located Real seal non-escape theorem. The structural rows $H,C,P,N$ and $H',C',P',N'$ only transport, replay, package, and name the displayed packet; they cannot add a second source, second window, quotient completion, selected limit, countable-choice schedule, trichotomy row, host equality, or ambient completeness principle. Therefore the terminal Real-seal route is determined by the shared apartness-Cauchy window.
\end{proof}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionSeparatedSealDeterminacy}

\begin{theorem}[RealApartnessCompletion separated completion handoff]
\label{thm:real-apartness-completion-separated-completion-handoff}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Every completion-facing read of $A$ that reaches the $\RealUp$ seal $E$ first factors through the apartness-Cauchy window
$$
X,Q,M,S,R,D .
$$
Thus no consumer can inspect $E$ as a separated completion seal without first reading the $\ApartnessRealUp$ source, the $\HausdorffCompletionUp$ separation row, the $\ConstructiveMetricCompletionUp$ route, the $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback, and the $\DyadicRatCoreUp$ tolerance ledger.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionSeparatedCompletionHandoff}
\begin{proof}
Project the accepted packet through \autoref{def:real-apartness-completion-carrier}. The seal row $E$ is displayed only after the source, separation, completion route, window, readback, and tolerance rows have already appeared.

Apply the obligation surface of \autoref{thm:real-apartness-completion-obligation-surface}. Its separated-source exactness clause forces $X,Q$ to be read before the completion row $M$, and its regular readback clause forces $S,R,D$ to be read before $E$.

The handoff is therefore the finite prefix named in \autoref{def:real-apartness-completion-apartness-cauchy-window}. Since the carrier contains no bypass coordinate, a completion-facing consumer cannot reach $E$ by quotient completion, selected limit, countable choice, trichotomy, host equality, or ambient completeness.
\end{proof}

\begin{theorem}[RealApartnessCompletion located Real seal non-escape]
\label{thm:real-apartness-completion-located-real-seal-nonescape}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Every $\LocatedRealUp$ or $\RealUp$ consumer of the seal receives only the displayed apartness-Cauchy window together with the seal, transport, replay, provenance, and local naming rows
$$
W_{\mathrm{ac}}(A),E,H,C,P,N .
$$
The consumer receives no quotient completion, trichotomy, selected limit, countable-choice schedule, host equality row, or ambient $\RealUp$ completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionLocatedRealSealNonescape}
\begin{proof}
Begin with a $\LocatedRealUp$ or $\RealUp$ consumer of the seal. By \autoref{thm:real-apartness-completion-separated-completion-handoff}, the consumer must first pass through $W_{\mathrm{ac}}(A)$ before it can read $E$.

After the seal is read, \autoref{thm:real-apartness-completion-nonescape} routes the remaining observation through $H,C,P,N$. These rows transport, replay, package, and name the same displayed packet; they do not add a second completion object or an external equality principle.

Combining the handoff with the non-escape boundary gives exactly $W_{\mathrm{ac}}(A),E,H,C,P,N$. The excluded principles are absent from both the carrier coordinates and the local obligation surface, so no located-real or real-seal consumer can obtain them from the packet.
\end{proof}

\begin{theorem}[RealApartnessCompletion finite apartness-completion obligation]
\label{thm:real-apartness-completion-finite-apartness-completion-obligation}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Every apartness-completion obligation exported by $A$ is exposed only through the $\DyadicRatCoreUp$ tolerance row $D$, the $\StreamNameUp$ finite-window row $S$, the $\RegSeqRatUp$ readback row $R$, the $\LocatedRealUp$ or $\ApartnessRealUp$ comparison surface carried by $X,Q,M$, and the $\RealUp$ seal $E$, followed by $H,C,P,N$.

Consequently the packet exports no quotient real equality, trichotomy, selected Cauchy limit, countable-choice schedule, host equality row, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionFiniteApartnessCompletionObligation}
\begin{proof}
Project the accepted carrier through \autoref{def:real-apartness-completion-carrier}. The apartness source, Hausdorff separation row, constructive completion route, stream windows, regular readback, dyadic tolerance ledger, and Real seal are the only analytic rows before transport, replay, provenance, and local naming.

Apply \autoref{thm:real-apartness-completion-obligation-surface}. Its separated-source exactness and regular readback clauses force every completion read to pass through $X,Q,M,S,R,D$ before $E$ is available.

The separated completion handoff of \autoref{thm:real-apartness-completion-separated-completion-handoff} and the located Real seal non-escape theorem of \autoref{thm:real-apartness-completion-located-real-seal-nonescape} identify the downstream consumer surface as $W_{\mathrm{ac}}(A),E,H,C,P,N$. Since the excluded principles are absent from both the carrier rows and the local obligation surface, they cannot be exported by the apartness-completion obligation.
\end{proof}

\begin{theorem}[RealApartnessCompletion separated-source exactness]
\label{thm:real-apartness-completion-separated-source-exactness}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Any completion-facing consumer that reads the constructive completion row $M$ must first read the $\ApartnessRealUp$ source row $X$ and the $\HausdorffCompletionUp$ separation row $Q$ through the same componentwise transport and replay rows $H,C$. The consumer cannot enter through a quotient completion, host real equality, selected limit, countable-choice schedule, trichotomy row, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-apartness-completion-carrier}. The route displays $X$ and $Q$ before $M$, and the structural rows $H,C$ transport and replay that same ordered packet.

Apply \autoref{thm:real-apartness-completion-obligation-surface}. Its separated-source exactness clause is precisely the assertion that $X,Q$ are consumed before any completion-facing inspection of $M$.

The separated completion handoff of \autoref{thm:real-apartness-completion-separated-completion-handoff} keeps every later read of the seal downstream of the same prefix. Since \autoref{thm:real-apartness-completion-located-real-seal-nonescape} excludes all bypass rows at the seal, no completion-facing consumer can reach $M$ through an external completion or equality principle.
\end{proof}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionSeparatedSourceExactness}

\begin{theorem}[RealApartnessCompletion regular readback discipline]
\label{thm:real-apartness-completion-regular-readback-discipline}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Every Real-facing read of the seal $E$ must pass through the stream-window row $S$, the $\RegSeqRatUp$ regular rational readback row $R$, and the $\DyadicRatCoreUp$ tolerance ledger $D$. Thus $E$ is available only as the terminal seal of the displayed regular-Cauchy route, not as a selected limit or host completion point.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionRegularReadbackDiscipline}
\begin{proof}
Start from \autoref{thm:real-apartness-completion-separated-completion-handoff}. It identifies the apartness-Cauchy window $X,Q,M,S,R,D$ as the finite prefix required before $E$ can be read.

Apply \autoref{thm:real-apartness-completion-obligation-surface}. Its regular readback discipline clause names $S,R,D$ as the rows that mediate every completion observation before the Real seal becomes visible.

Finally use \autoref{thm:real-apartness-completion-located-real-seal-nonescape}. The downstream $\LocatedRealUp$ or $\RealUp$ consumer receives $W_{\mathrm{ac}}(A),E,H,C,P,N$ and no selected limit, quotient completion, trichotomy, countable-choice schedule, host equality row, or ambient completeness theorem.
\end{proof}

\begin{theorem}[RealApartnessCompletion located tail obligation]
\label{thm:real-apartness-completion-located-tail-obligation}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Before any separated-completion tail of $A$ is consumed, the packet must expose the located apartness source carried by $X,Q,M$, the finite $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback row $R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, and the terminal $\RealUp$ seal $E$. Thus every tail-facing read reaches the separated completion boundary only through
$$
X,Q,M,S,R,D,E,H,C,P,N .
$$
It has no coordinate for countable choice, selected limits, quotient equality, trichotomy, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-apartness-completion-carrier}. The displayed order places the apartness source and separated completion route in $X,Q,M$, then the finite window, regular readback, dyadic tolerance, and seal rows in $S,R,D,E$.

Use separated seal determinacy, separated completion handoff, and located Real seal non-escape. These results force every seal-facing consumer to pass through $W_{\mathrm{ac}}(A)=(X,Q,M,S,R,D)$ before the seal $E$ is available, and they keep the same transport, replay, provenance, and naming rows visible downstream.

The regular readback discipline fixes the Real-facing part of the route through $S,R,D$. Therefore a separated-completion tail cannot be consumed before located apartness, finite windows, regular readback, dyadic tolerance, and the Real seal have all been exposed; the refused principles are absent from the carrier and from the local obligation surface.
\end{proof}

\begin{theorem}[RealApartnessCompletion RegSeqRat refinement]
\label{thm:real-apartness-completion-regseqrat-refinement}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. Any refinement of the separated tail that is visible to a Real-facing consumer factors through the existing $\RegSeqRatUp$ row $R$ and the $\DyadicRatCoreUp$ tolerance row $D$ after the finite window row $S$ has been read. The refinement may tighten the displayed regular rational readback along $S,R,D$, but it cannot export quotient equality, selected limits, trichotomy, countable choice, or $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionRegSeqRatRefinement}
\begin{proof}
Begin with the located tail obligation above and the regular readback discipline. Together they identify $S,R,D$ as the only finite refinement surface between the separated completion route $M$ and the terminal seal $E$.

Apply separated-source exactness and separated completion handoff. The consumer cannot enter the refinement from a quotient completion or selected limit; it reaches $M$ through $X,Q$ and then reaches $E$ only through $W_{\mathrm{ac}}(A)$.

Finally use located Real seal non-escape and ledger exhaustion. The rows $H,C,P,N$ preserve the same displayed packet, while the seal row $E$ remains downstream of $S,R,D$. Hence the refinement is a RegSeqRat-and-dyadic-tolerance refinement of the displayed tail, not an exported equality, trichotomy, choice, selected-limit, or ambient-completeness principle.
\end{proof}

\begin{theorem}[RealApartnessCompletion window determinacy]
\label{thm:real-apartness-completion-window-determinacy}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet that reaches the $\RealUp$ seal $E$. Then the packet determines the same apartness-Cauchy prefix
$$
W_{\mathrm{ac}}(A)=(X,Q,M,S,R,D)
$$
through the displayed $\ApartnessRealUp$, $\HausdorffCompletionUp$, $\ConstructiveMetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ rows. No second window source, quotient completion, selected limit, countable-choice schedule, trichotomy row, or host equality principle can supply an alternate prefix for the same seal-facing read.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionWindowDeterminacy}
\begin{proof}
Start with the carrier definition and the apartness-Cauchy window definition. They name $X,Q,M,S,R,D$ as the only prefix rows available before the seal coordinate $E$ is read.

Apply the separated completion handoff, the located Real seal non-escape theorem, and separated-source exactness. These force every completion-facing route to enter through the apartness source, Hausdorff separation row, and constructive metric completion row before it can reach the terminal seal.

Next apply the regular readback discipline. It fixes the seal-facing observation through the finite stream window, regular rational readback, and dyadic tolerance ledger, so the same $S,R,D$ rows mediate every Real-facing read.

Finally use ledger exhaustion. The remaining rows $H,C,P,N$ only transport, replay, package, and name the displayed packet, leaving no coordinate for a second window source, quotient completion, selected limit, countable choice, trichotomy, or host equality.
\end{proof}

\begin{theorem}[RealApartnessCompletion ledger exhaustion]
\label{thm:real-apartness-completion-ledger-exhaustion}
Let $A=(X,Q,M,S,R,D,E,H,C,P,N)$ be an accepted $\RealApartnessCompletionUp$ packet. The complete obligation ledger exported by $A$ is the displayed carrier route together with its transport, replay, provenance, and local naming rows. Every separated-source, regular-readback, componentwise-stability, and seal-facing consumer is therefore exhausted by $X,Q,M,S,R,D,E,H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.RealApartnessCompletionUp.RealApartnessCompletionLedgerExhaustion}
\begin{proof}
Use \autoref{thm:real-apartness-completion-obligation-routing}. It routes carrier admission, separated-source exactness, regular readback discipline, componentwise stability, and ledger exhaustion through the single finite packet $X,Q,M,S,R,D,E,H,C,P,N$.

The separated-source theorem above fixes the entry to $M$ through $X,Q$, and the regular readback theorem above fixes the entry to $E$ through $S,R,D$. These two routes are the analytic part of the same ledger.

The remaining rows are structural. By \autoref{thm:real-apartness-completion-nonescape} and \autoref{thm:real-apartness-completion-located-real-seal-nonescape}, $H,C,P,N$ only transport, replay, package, and name the displayed packet, so the ledger has no coordinate left for quotient completion, selected limits, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{proof}

\closureat{RealApartnessCompletionUp}{seedStr}

\begin{closurestatus}{\RealApartnessCompletionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-apartness-completion-carrier}, \autoref{thm:real-apartness-completion-obligation-surface}, \autoref{thm:real-apartness-completion-nonescape}, \autoref{def:real-apartness-completion-apartness-cauchy-window}, \autoref{thm:real-apartness-completion-separated-completion-handoff}, \autoref{thm:real-apartness-completion-located-real-seal-nonescape}, \autoref{thm:real-apartness-completion-separated-source-exactness}, \autoref{thm:real-apartness-completion-regular-readback-discipline}, \autoref{thm:real-apartness-completion-located-tail-obligation}, \autoref{thm:real-apartness-completion-regseqrat-refinement}, and \autoref{thm:real-apartness-completion-ledger-exhaustion} seed the separated apartness-completion boundary over ApartnessReal, HausdorffCompletion, ConstructiveMetricCompletion, StreamName, RegSeqRat, DyadicRatCore, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, host real equality, selected limits, countable choice, trichotomy, ambient $\RealUp$ completeness, or Lean verification.}
  \upgradepath{Obligation closure requires carrier admission, separated-source exactness, regular readback discipline, componentwise stability, and ledger exhaustion proofs for BEDC.Derived.RealApartnessCompletionUp.}
  \constructivestory{A $\RealApartnessCompletionUp$ packet is generated from an ApartnessReal source row, a HausdorffCompletion separation row, a ConstructiveMetricCompletion route, StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
