\chapter{A Concrete Naming Certificate for $\KummerTestUp$}
\label{ch:concrete-instances-kummer-test-namecert}
\origin{human}

Following Kummer and the Bishop--Bridges constructive-analysis reading of positive series tests, the $\KummerTestUp$ packet records the finite auxiliary-sequence comparison route that turns adjacent-term quotient data into a displayed regular Cauchy tail handoff. It refines the positive $\RealSeriesUp$ surface of \autoref{ch:concrete-instances-realseries-namecert}, sits beside the ratio route of \autoref{ch:concrete-instances-regular-cauchy-ratio-test-namecert}, the comparison route of \autoref{ch:concrete-instances-regular-cauchy-comparison-test-namecert}, and the neighboring defect-test surface of \autoref{ch:concrete-instances-raabe-duhamel-test-namecert}, and it exports endpoints only through the dyadic, stream, regular-rational, and Real seals of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet is a finite NameCert boundary for Kummer-style convergence testing; it is not a host infinite sum, selected limit, quotient series equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Kummer test carrier]\label{def:kummer-test-carrier}
A $\KummerTestUp$ carrier is a finite $\BHist$ packet
$$
K=(A,Q,U,D,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the source $\RealSeriesUp$ row, exposing only the inspected positive-term windows and partial-sum windows used by the current tail request.
\item $Q$ is the adjacent-term quotient row, recording the finite windows on which the inspected ratios of neighboring positive terms are read.
\item $U$ is the auxiliary positive comparison sequence row, displaying the finite auxiliary values and positivity witnesses used by the Kummer defect calculation.
\item $D$ is the dyadic defect-budget row, assigning each displayed tail request a finite threshold and rational margin for the Kummer inequality on the inspected windows.
\item $T$ is the comparison-test handoff row, converting $Q,U,D$ and the source windows in $A$ into the finite majorant route consumed by $\RegularCauchyComparisonTestUp$.
\item $R$ is the $\RegSeqRatUp$ readback row reached only after the comparison handoff has been displayed.
\item $E$ is the terminal $\RealUp$ seal for endpoint-facing consumers.
\item $H$ stores componentwise $\hsame$ transport for the source, quotient, auxiliary, defect-budget, comparison, readback, seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from each tail request through $Q,U,D,T,R,E$.
\item $P$ is the $\Pkg$ provenance over $\KummerTestUp$, $\RealSeriesUp$, $\RegularCauchyRatioTestUp$, $\RegularCauchyComparisonTestUp$, $\RaabeDuhamelTestUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KummerTestUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,Q,U,D,T,R,E,H,C,P,N$. It has no coordinate for host infinite sums, selected limits, quotient series equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Kummer test NameCert obligations]\label{thm:kummer-test-namecert-obligations}
For every accepted $\KummerTestUp$ carrier
$$
K=(A,Q,U,D,T,R,E,H,C,P,N),
$$
the local $\NameCert_{\KummerTestUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier exposure for the source $\RealSeriesUp$ row, adjacent-term quotient row, auxiliary positive comparison sequence row, dyadic defect-budget row, comparison-test handoff, $\RegSeqRatUp$ readback, $\RealUp$ seal, transport, replay, provenance, and local naming row;
\item auxiliary-row positivity, requiring every accepted auxiliary read to pass through the displayed finite positivity witnesses in $U$;
\item quotient-window exactness, requiring the quotient data used by the Kummer defect to be read only from the displayed neighboring-term windows $A,Q$;
\item defect-budget stability, requiring the threshold and rational margin in $D$ to be preserved under the accepted tail request and under componentwise transport;
\item comparison-test routing, requiring $T$ to be the only row that turns the Kummer defect data into a convergence-test majorant;
\item $\RegSeqRatUp$ and $\RealUp$ seal nonescape, requiring endpoint-facing data to pass through $R$ and then $E$ after the comparison row has been displayed;
\item transport, replay, and provenance discipline, requiring $H,C,P,N$ to transport, replay, source, and name exactly the displayed route and no unrecorded analytic principle.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kummer-test-carrier}. Carrier exposure is the inventory $A,Q,U,D,T,R,E,H,C,P,N$, so the local certificate has no detached row for an infinite sum, quotient series equality, selected limit, or ambient completeness theorem.

The auxiliary and quotient clauses are read before any convergence handoff. The row $U$ displays only finite auxiliary comparison values together with their positivity witnesses, while $A$ and $Q$ display the source windows and neighboring-term quotient windows used by the Kummer defect.

The defect budget is then forced through $D$. Its threshold and rational margin are the only data attached to the requested tail, and componentwise $\hsame$ transport in $H$ preserves the same threshold, margin, source windows, quotient windows, and auxiliary row.

The comparison route is exactly $T$. Once $Q,U,D$ have been displayed over the source windows in $A$, the packet may enter the $\RegularCauchyComparisonTestUp$ surface; before that row appears, no regular Cauchy conclusion is exported.

Finally, $R$ and $E$ supply the regular rational and Real-facing endpoint route. The replay, provenance, and local naming rows $C,P,N$ preserve this same route, so none of them can introduce host infinite sums, selected limits, quotient series equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge data.
\end{proof}

\begin{theorem}[Kummer test Cauchy tail handoff]\label{thm:kummer-test-cauchy-tail-handoff}
Every endpoint-facing read of an accepted $\KummerTestUp$ carrier factors through
$$
\RealSeriesUp,\ \RegularCauchyRatioTestUp,\ \RegularCauchyComparisonTestUp,\ \RaabeDuhamelTestUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp.
$$
Concretely, the read consumes source $\RealSeriesUp$ windows and neighboring quotient rows, reads the auxiliary positive comparison sequence and dyadic defect budgets, uses the ratio and Raabe--Duhamel surfaces only as neighboring quotient-defect reference faces, enters the $\RegularCauchyComparisonTestUp$ handoff, and reaches $\RegSeqRatUp$ and $\RealUp$ only through the displayed stream and dyadic rows.
\end{theorem}
\begin{proof}
Begin with an endpoint-facing read of an accepted carrier. By \autoref{def:kummer-test-carrier}, the only source data available before the defect estimate are the $\RealSeriesUp$ windows in $A$ and the adjacent-term quotient windows in $Q$.

The Kummer-specific data are then read from $U$ and $D$. The auxiliary row supplies finite positive comparison values, while the dyadic defect-budget row supplies the threshold and rational margin attached to the requested tail.

The neighboring quotient-test surfaces are reference faces rather than endpoints. The $\RegularCauchyRatioTestUp$ and $\RaabeDuhamelTestUp$ rows identify the surrounding adjacent-term and defect-test discipline, but the accepted $\KummerTestUp$ endpoint route still has to enter the comparison handoff $T$.

After $T$ is displayed, the route proceeds through the finite $\StreamNameUp$ windows and $\DyadicRatCoreUp$ budgets recorded in the carrier, then through $R$ and $E$. Thus every endpoint-facing read reaches $\RegSeqRatUp$ and $\RealUp$ only through the displayed comparison, stream, dyadic, readback, seal, transport, replay, provenance, and naming rows.
\end{proof}

\falsifiablePrediction{Every accepted $\KummerTestUp$ endpoint-facing read factors through source RealSeries windows, adjacent-term quotient rows, an auxiliary positive comparison sequence, dyadic defect budgets, a RegularCauchyComparisonTest handoff, StreamName and DyadicRatCore display rows, RegSeqRat readback, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{realseries, regularcauchyratiotest, regularcauchycomparisontest, raabeduhameltest, dyadicratcore, streamname, regseqrat, real: $\KummerTestUp$ is not bijective to any listed sibling because it jointly carries source series windows, adjacent-term quotient windows, an auxiliary positive comparison sequence, dyadic defect budgets, comparison-test routing, regular rational readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{KummerTestUp}{seedStr}

\begin{closurestatus}{\KummerTestUp}
  \constructivestory{A $\KummerTestUp$ packet is generated from source RealSeries windows, adjacent-term quotient windows, an auxiliary positive comparison sequence, dyadic defect budgets, a RegularCauchyComparisonTest handoff, StreamName and DyadicRatCore display rows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:kummer-test-carrier}, \autoref{thm:kummer-test-namecert-obligations}, and \autoref{thm:kummer-test-cauchy-tail-handoff} seed the finite Kummer convergence-test boundary over displayed source series, quotient, auxiliary positive sequence, defect-budget, comparison, $\RegSeqRatUp$, and $\RealUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host infinite sums, selected limits, quotient series equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires Lean theorems for carrier exposure, auxiliary-row positivity, quotient-window exactness, defect-budget stability, comparison-test routing, $\RegSeqRatUp$ and $\RealUp$ seal nonescape, and transport, replay, provenance, and local naming discipline for BEDC.Derived.KummerTestUp.}
\end{closurestatus}
