\chapter{A Concrete Naming Certificate for $\ObserverExperienceTraceUp$}
\label{ch:concrete-instances-observer-experience-trace-namecert}
\origin{ai}

The $\ObserverExperienceTraceUp$ packet realizes the consciousness-observer vision hub of \autoref{ch:visions-subhub-consciousness-observer} as finite observer-history, locality, classifier, transport, replay, provenance, and NameCert rows. It sits beside the finite multi-history packet of \autoref{ch:concrete-instances-finite-multihist-packet-namecert}, the locality-cell route of \autoref{ch:concrete-instances-localitycell-namecert}, the no-global-synchronization ledger of \autoref{ch:concrete-instances-no-global-synchronization-ledger-namecert}, and the cross-history causal surface of \autoref{ch:concrete-instances-crosshistcausal-namecert}. The packet names a finite trace surface for observer-facing reads; it does not assert a private mental-state oracle, privileged observer frame, global synchronization map, host phenomenology predicate, or physical consciousness law.

\begin{definition}[Observer experience trace carrier]
\label{def:observer-experience-trace-carrier}
An $\ObserverExperienceTraceUp$ carrier is a finite $\BHist$ packet
$$
T=(H,L,I,Q,R,V,A,S,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $H$ is the finite observer-history row, listing the displayed local histories being traced.
\item $L$ is the inter-history locality ledger, recording which observations may be compared without a global synchronization row.
\item $I$ is the observer-identity and perspective-classifier row, separating local identity reads from cross-history alignment reads.
\item $Q$ is the finite query row for the experience trace: displayed observation sites, local classifier tags, and allowed read windows.
\item $R$ is the replay row tying each query in $Q$ back to the displayed observer-history and locality rows.
\item $V$ is the provenance row from the consciousness-observer hub, recording which finite vision-site claims are being concretized.
\item $A$ is the TasteGate-facing audit row requiring a future Lean \texttt{Nontrivial} instance and a \texttt{FieldFaithful} instance to expose at least two distinguishable trace fields and to account for all public carrier rows.
\item $S$ records componentwise $\hsame$ transport for $H,L,I,Q,R,V,A,C,P,N$.
\item $C$ records displayed $\Cont$ replay from observer-history reads through locality, perspective classification, trace queries, audit rows, and local naming.
\item $P$ is the $\Pkg$ provenance over $\ObserverExperienceTraceUp$, $\FiniteMultiHistPacketUp$, $\LocalityCellUp$, $\mathsf{NoGlobalSynchronizationLedger}^{\uparrow}$, $\CrossHistCausalUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ObserverExperienceTraceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed histories, locality rows, identity-classifier rows, query rows, replay rows, audit rows, structural transports, provenance, and local name.
\end{definition}

\begin{theorem}[Observer experience trace boundary]
\label{thm:observer-experience-trace-boundary}
Every accepted $\ObserverExperienceTraceUp$ carrier exposes observer-facing trace data only through the finite rows
$$
H,L,I,Q,R,V,A,S,C,P,N.
$$
The public boundary permits local observer-history reads, inter-history locality checks, perspective-classifier reads, replay of finite trace queries, vision provenance, and TasteGate-facing \texttt{Nontrivial} and \texttt{FieldFaithful} obligations. It exports no private Self access, host phenomenology predicate, privileged global observer, global synchronization map, physical consciousness law, or quotient equality of observer histories.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:observer-experience-trace-carrier}. The trace source is $H,L,I,Q$, and the only replay route is $R$ together with the displayed $\Cont$ row $C$.

The audit row $A$ is explicit. It requires future Lean evidence that the carrier has a nontrivial trace distinction and that the displayed fields are faithful, but it does not itself add an external consciousness predicate.

The structural rows $S,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for private Self access, host phenomenology, global synchronization, physical consciousness laws, or quotient observer-history equality, the stated boundary is exact.
\end{proof}

\begin{theorem}[Observer experience trace sibling independence]
\label{thm:observer-experience-trace-independence}
The $\ObserverExperienceTraceUp$ carrier is not bijective to any single sibling among $\FiniteMultiHistPacketUp$, $\LocalityCellUp$, $\mathsf{NoGlobalSynchronizationLedger}^{\uparrow}$, or $\CrossHistCausalUp$. It jointly carries finite observer histories, inter-history locality, perspective-classifier rows, finite trace queries, replay provenance, TasteGate-facing audit fields, transport, continuation, package provenance, and local naming as one packet.
\end{theorem}
\begin{proof}
The finite multi-history packet supplies displayed histories but not the trace query row $Q$ or TasteGate-facing audit row $A$.

The locality-cell and no-global-synchronization packets supply locality and refusal rows, but they do not jointly carry the observer identity classifier $I$, replay row $R$, or vision provenance row $V$.

The cross-history causal packet supplies a causal comparison surface, while $\ObserverExperienceTraceUp$ records a finite trace read with its own query, replay, audit, provenance, and local naming rows. Thus no listed sibling is bijective to the full carrier.
\end{proof}

\falsifiablePrediction{An accepted $\ObserverExperienceTraceUp$ consumer that reads an observer experience trace without displaying finite observer histories, inter-history locality rows, perspective-classifier rows, finite trace queries, replay provenance, TasteGate-facing audit rows, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows refutes the carrier boundary.}

\independenceWitness{finite-multihist-packet, localitycell, noglobalsynchronizationledger, crosshistcausal: no listed sibling is bijective because $\ObserverExperienceTraceUp$ jointly carries observer-history, locality, perspective-classifier, finite query, replay, provenance, TasteGate-audit, transport, continuation, package, and local NameCert rows.}

\closureat{ObserverExperienceTraceUp}{seedStr}
\leanstmt{BEDC.Derived.ObserverExperienceTraceUp}

\begin{closurestatus}{\ObserverExperienceTraceUp}
  \constructivestory{An $\ObserverExperienceTraceUp$ packet is generated from finite observer histories, inter-history locality rows, perspective-classifier rows, trace queries, replay provenance, TasteGate-facing audit rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:observer-experience-trace-carrier}, \autoref{thm:observer-experience-trace-boundary}, \autoref{thm:observer-experience-trace-independence}, the falsifiable prediction, and the independence witness seed a finite observer-experience trace carrier with explicit locality, classifier, replay, provenance, audit, and naming rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close private Self access, host phenomenology, privileged observer frames, global synchronization, physical consciousness laws, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, observer-history exposure, locality exposure, classifier exactness, replay provenance, TasteGate Nontrivial and FieldFaithful instances, transport, continuation, package provenance, and local naming for BEDC.Derived.ObserverExperienceTraceUp.}
\end{closurestatus}
