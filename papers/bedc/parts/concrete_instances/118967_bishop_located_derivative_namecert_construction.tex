\chapter{A Concrete Naming Certificate for $\BishopLocatedDerivativeUp$}
\label{ch:concrete-instances-bishop-located-derivative-namecert}
\origin{human}

Following Bishop and Bridges on constructive differentiation, the $\BishopLocatedDerivativeUp$ packet records a located finite derivative window before any host derivative or completed real-function equality is available. It sits over the finite $\StreamNameUp$ surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the finite-difference handoff of \autoref{ch:concrete-instances-finite-difference-namecert}, and the derivative-facing carrier of \autoref{ch:concrete-instances-derivative-namecert}. The packet supplies the Real and Completion route with a finite located derivative window, not quotient real equality, selected limits, a host differentiability theorem, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop located derivative carrier]
\label{def:bishop-located-derivative-carrier}
A $\BishopLocatedDerivativeUp$ carrier is a finite $\BHist$ packet
$$
B=(W,X,M,D,Q,R,F,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the located source-window row, listing the inspected $\StreamNameUp$ input windows around the displayed base point.
\item $X$ is the location row, naming the base point, neighbourhood tag, and finite radius requests used to keep the derivative window local.
\item $M$ is the mesh row, recording the finite step choices, sidedness tags, endpoint reuse schedule, and exclusion of the zero step.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row for step size, coefficient, denominator, and local-radius budgeting.
\item $Q$ is the $\RegSeqRatUp$ readback row for the displayed difference quotients and their finite error ledger.
\item $R$ is the terminal $\RealUp$ seal row reached only after $Q$ has been read.
\item $F$ is the $\FiniteDifferenceUp$ handoff row, exposing the finite quotient packet consumed before any derivative-facing read.
\item $E$ is the $\DerivativeUp$ boundary row, publishing only the located finite-window route.
\item $H$ records componentwise $\hsame$ transport for source windows, location data, mesh choices, dyadic budgeting, regular readback, Real sealing, finite-difference handoff, derivative boundary, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the route
$$
W,X,M \longmapsto D \longmapsto Q \longmapsto R \longmapsto F \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedDerivativeUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\FiniteDifferenceUp$, $\DerivativeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedDerivativeUp}$ row.
\end{enumerate}
The classifier has no coordinate for an ambient function space, quotient real equality, selected limiting derivative, host differentiability theorem, or $\RealUp$ completeness.
\end{definition}

\begin{theorem}[BishopLocatedDerivativeUp NameCert obligations]
\label{thm:bishop-located-derivative-namecert-obligations}
For every accepted $\BishopLocatedDerivativeUp$ carrier
$$
B=(W,X,M,D,Q,R,F,E,H,C,P,N),
$$
the local $\NameCert_{\BishopLocatedDerivativeUp}$ surface consists exactly of carrier admission for the located source windows, local base-point and radius data, finite mesh schedule, dyadic tolerance budgeting, regular rational readback, terminal Real seal, finite-difference handoff, derivative boundary exposure, transport, replay, provenance, and local naming. No obligation is stored in a host derivative, a quotient real equality proof, a selected limit, or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-derivative-carrier}. The rows $W,X,M$ are the only coordinates containing source windows, location data, and finite mesh choices.

The numerical rows then force the Real-facing read through $D,Q,R$. The dyadic row $D$ supplies the finite tolerance budget, the regular row $Q$ reads the difference quotients, and the Real row $R$ seals only that readback.

The consumer rows $F,E$ expose the same finite route to \autoref{ch:concrete-instances-finite-difference-namecert} and \autoref{ch:concrete-instances-derivative-namecert}. The structural rows $H,C,P,N$ transport, replay, source, and name the displayed packet without adding hidden data.

Since the carrier contains no ambient function-space, quotient-equality, selected-limit, host-differentiability, or completeness coordinate, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[BishopLocatedDerivativeUp finite-difference handoff]
\label{thm:bishop-located-derivative-finite-difference-handoff}
Every $\DerivativeUp$ consumer of an accepted $\BishopLocatedDerivativeUp$ packet reads the finite-difference boundary $F$ before it can inspect the derivative-facing row $E$. The handoff reaches $F$ only through the source-window, location, mesh, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows displayed in the carrier, so it does not export host differentiability, quotient real equality, selected limits, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:bishop-located-derivative-namecert-obligations}. It places every accepted read on $W,X,M,D,Q,R,F,E,H,C,P,N$.

The finite-difference row $F$ is downstream of the source-window and mesh rows and downstream of the regular-real chain $D,Q,R$. Thus it receives a finite displayed quotient packet rather than a host derivative.

The derivative boundary $E$ is downstream of $F$. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve that order, while provenance and local naming identify the same finite packet.

The absent host differentiability, quotient equality, selected-limit, and completeness claims are not coordinates of the carrier, so no derivative consumer can bypass $F$.
\end{proof}

\begin{theorem}[BishopLocatedDerivativeUp Real seal nonescape]
\label{thm:bishop-located-derivative-real-seal-nonescape}
For every accepted $\BishopLocatedDerivativeUp$ packet, the Real-facing value exposed by the located derivative window is exactly the value sealed by the row $R$ after $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerances, and $\RegSeqRatUp$ readback have been displayed. The packet cannot re-expose that value as quotient real equality, a selected derivative limit, a host real function, or an ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Read $R$ from the carrier in \autoref{def:bishop-located-derivative-carrier}. By construction it is downstream of the finite source windows $W$, the local data $X,M$, the dyadic tolerance row $D$, and the regular readback row $Q$.

Apply \autoref{thm:bishop-located-derivative-namecert-obligations}. Its Real seal exposure clause identifies $R$ as the only terminal Real-facing row in the local certificate.

The finite-difference and derivative rows $F,E$ may consume that seal, but they do not replace it with a quotient equality or selected limiting object. The structural rows $H,C,P,N$ only preserve and name the same displayed route.

Because no other carrier coordinate can hold a Real-facing witness, coordinate exhaustion prevents escape from the $D,Q,R$ route.
\end{proof}

\closureat{BishopLocatedDerivativeUp}{seedStr}

\begin{closurestatus}{\BishopLocatedDerivativeUp}
  \constructivestory{A $\BishopLocatedDerivativeUp$ packet is generated from StreamName source windows, located base-point and mesh rows, DyadicRatCore tolerance budgeting, RegSeqRat readback, a RealUp seal, FiniteDifference and Derivative consumer boundaries, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-derivative-carrier}, \autoref{thm:bishop-located-derivative-namecert-obligations}, \autoref{thm:bishop-located-derivative-finite-difference-handoff}, and \autoref{thm:bishop-located-derivative-real-seal-nonescape} seed the located finite derivative-window surface over StreamName, DyadicRatCore, RegSeqRat, Real, FiniteDifference, Derivative, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host differentiability, quotient real equality, selected derivative limits, host real function equality, Real completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked source-window admission, location admission, mesh admission, dyadic budgeting, regular readback, Real sealing, finite-difference handoff, derivative-boundary exposure, and transport/provenance/name exhaustion for BEDC.Derived.BishopLocatedDerivativeUp.}
\end{closurestatus}
