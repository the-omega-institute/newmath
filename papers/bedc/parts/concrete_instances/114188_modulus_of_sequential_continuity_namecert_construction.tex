\chapter{A Concrete Naming Certificate for $\ModulusOfSequentialContinuityUp$}
\label{ch:concrete-instances-modulus-of-sequential-continuity-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\ModulusOfSequentialContinuityUp$ packet records the explicit tolerance-to-window datum that makes a sequential-continuity read constructive. It sits beside the $\SequentialContinuityUp$ route of \autoref{ch:concrete-instances-sequentialcontinuity-namecert}, reads the pointwise surface of \autoref{ch:concrete-instances-continuous-namecert}, uses the metric rows of \autoref{ch:concrete-instances-metric-namecert}, inspects finite $\StreamNameUp$ windows from \autoref{ch:concrete-instances-streamname-namecert}, hands those windows through $\RegSeqRatUp$ in \autoref{ch:concrete-instances-regseqrat-namecert}, and reaches only the displayed $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet exports a finite modulus object, not Real completeness, countable choice, quotient streams, host function extensionality, or an ambient continuity principle.

\begin{definition}[ModulusOfSequentialContinuity carrier]
\label{def:modulus-of-sequential-continuity-carrier}
A $\ModulusOfSequentialContinuityUp$ carrier is a finite $\BHist$ packet
$$
M=(X,Y,F,S,T,E,W_X,W_Y,R_X,R_Y,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the source and target $\MetricSpaceUp$ carrier rows, including the distance classifiers and visible tolerance ledgers.
\item $F$ is the displayed $\ContinuousUp$ row whose pointwise modulus may be read only at inspected source windows.
\item $S$ is the source $\StreamNameUp$ surface, and $T$ is the finite threshold ledger requested by the sequential-continuity consumer.
\item $E$ is the tolerance-to-window assignment: each displayed target tolerance in $T$ is sent to a finite source-window request before any target conclusion is read.
\item $W_X$ and $W_Y$ are the source and target finite window families exposed by $S,F,E$.
\item $R_X$ and $R_Y$ are the $\RegSeqRatUp$ readback rows attached to the source and target windows.
\item $A$ is the $\SequentialContinuityUp$ acceptance row saying that the displayed windows are the only sequence-facing route consumed by the packet.
\item $H$ stores componentwise $\hsame$ transport for the two metric rows, continuous row, source stream, threshold ledger, assignment, window rows, regular readbacks, sequential acceptance, continuation, provenance, and local naming.
\item $C$ stores displayed $\Cont$ replay from $T$ to $E$, from $E,S$ to $W_X$, from $W_X,F$ to $W_Y$, and from $W_X,W_Y$ to $R_X,R_Y,A$.
\item $P$ is the $\Pkg$ provenance over $\ModulusOfSequentialContinuityUp$, $\SequentialContinuityUp$, $\ContinuousUp$, $\MetricSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ModulusOfSequentialContinuityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,F,S,T,E,W_X,W_Y,R_X,R_Y,A,H,C,P,N$. It has no coordinate for a selected limit, quotient stream equality, countable choice, host function extensionality, detached continuity proof, or ambient Real completeness.
\end{definition}

\begin{theorem}[ModulusOfSequentialContinuity NameCert obligations]
\label{thm:modulus-of-sequential-continuity-namecert-obligations}
For every accepted $\ModulusOfSequentialContinuityUp$ carrier
$$
M=(X,Y,F,S,T,E,W_X,W_Y,R_X,R_Y,A,H,C,P,N),
$$
the local $\NameCert_{\ModulusOfSequentialContinuityUp}$ surface consists exactly of carrier admission for the displayed fifteen-row packet, metric-row stability through $X,Y,H,C$, pointwise-continuity use through $F$, tolerance-to-window exposure through $T,E,W_X,W_Y$, regular-rational readback through $R_X,R_Y$, sequential-continuity handoff through $A$, package provenance through $P$, local naming through $N$, and non-escape from selected limits, quotient streams, countable choice, host function extensionality, detached continuity, and Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:modulus-of-sequential-continuity-carrier}. The classifier can read only the displayed metric rows, continuous row, stream row, tolerance ledger, assignment row, window rows, regular readbacks, sequential acceptance row, and structural rows.

The analytic route starts with the tolerance ledger. The row $T$ lists the target tolerances requested by the consumer, and $E$ turns each listed tolerance into a finite source-window request before a target window is inspected.

Next read the window and regular-rational handoff. The row $W_X$ is opened from $S,E$, the row $W_Y$ is obtained from $W_X,F$, and the rows $R_X,R_Y$ are the only regular-rational readbacks attached to those windows.

Finally attach the sequential-continuity row and the structural rows. The row $A$ records that the displayed windows are the complete sequence-facing route, while $H,C,P,N$ transport, replay, source, and name that route. Since none of the excluded principles is a coordinate of $M$, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[ModulusOfSequentialContinuity tolerance-window handoff]
\label{thm:modulus-of-sequential-continuity-tolerance-window-handoff}
Every accepted $\ModulusOfSequentialContinuityUp$ packet sends a target tolerance to a finite source-window request before any $\SequentialContinuityUp$ or $\RealUp$ consumer can read the image sequence. The complete handoff factors through
$$
T\longmapsto E\longmapsto W_X\longmapsto W_Y\longmapsto R_X,R_Y\longmapsto A
$$
together with the displayed rows $X,Y,F,S,H,C,P,N$.
\end{theorem}
\begin{proof}
Begin with the tolerance request. The target tolerance is visible only as an entry of $T$, and the assignment row $E$ is the only coordinate that converts such an entry into a finite source-window request.

Open the source and image windows. The source row $S$ supplies $W_X$ at the window requested by $E$, and the continuous row $F$ is consumed only through that inspected source window to expose $W_Y$.

Pass to the rational and sequential surfaces. The rows $R_X$ and $R_Y$ read the two windows as regular rational data, and $A$ is the downstream $\SequentialContinuityUp$ acceptance row reached from those readbacks.

Thus a consumer that tries to read an image sequence, a Real-facing seal, or a sequential-continuity conclusion without first exposing $T,E,W_X,W_Y,R_X,R_Y$ leaves the accepted packet.
\end{proof}

\closureat{ModulusOfSequentialContinuityUp}{seedStr}

\begin{closurestatus}{\ModulusOfSequentialContinuityUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:modulus-of-sequential-continuity-carrier}, \autoref{thm:modulus-of-sequential-continuity-namecert-obligations}, and \autoref{thm:modulus-of-sequential-continuity-tolerance-window-handoff} seed the finite tolerance-to-window route for sequential-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Real completeness, countable choice, quotient streams, host function extensionality, selected limits, detached continuity, Lean verification, or standard bridge checking.}
  \upgradepath{Formal closure requires checked carrier admission, metric-row stability, tolerance-to-window exactness, finite-window replay, $\RegSeqRatUp$ readback, $\SequentialContinuityUp$ handoff, and non-escape for BEDC.Derived.ModulusOfSequentialContinuityUp.}
  \constructivestory{A $\ModulusOfSequentialContinuityUp$ packet is generated from source and target metric rows, a displayed pointwise-continuity row, a source stream, a finite tolerance ledger, a tolerance-to-window assignment, source and image windows, regular rational readbacks, sequential-continuity acceptance, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
