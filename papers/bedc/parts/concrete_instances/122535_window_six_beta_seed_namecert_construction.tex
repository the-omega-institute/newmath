\chapter{A Concrete Naming Certificate for $\WindowSixBetaSeedUp$}
\label{ch:concrete-instances-window-six-beta-seed-namecert}
\origin{ai}

The $\WindowSixBetaSeedUp$ packet realizes the internal beta seed of \autoref{thm:zeckendorf-window-six-internal-beta-seed} and the infrared-stability row of \autoref{thm:zeckendorf-window-six-beta-seed-ir-stable} as finite NameCert data. It sits beside the finite golden-mean window of \autoref{ch:concrete-instances-golden-mean-shift-window-six-namecert}, the Zeckendorf phase shell of \autoref{ch:concrete-instances-zeckendorf-phase-shell-namecert}, the prime-edge classifier source of \autoref{ch:concrete-instances-zeckendorf-prime-edge-namecert}, and the beta-sign selection packet of \autoref{ch:concrete-instances-beta-sign-selection-namecert}. The packet names the local inverse-coupling derivative and its no-pole interval certificate; it is not a high-energy running law, a continuum renormalization group, or a physical coupling measurement.

\begin{definition}[Window-six beta seed carrier]
\label{def:window-six-beta-seed-carrier}
A $\WindowSixBetaSeedUp$ carrier is a finite $\BHist$ packet
$$
B=(W,G,I,S,R,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $W$ is the window-six finite source row read through $\GoldenMeanShiftWindowSixUp$.
\item $G$ is the Green-excess spectral-response row read through the Zeckendorf phase-shell and prime-edge sibling surfaces.
\item $I$ is the inverse-coupling derivative row. It records the local derivative $\frac{d}{dz}\alpha_{Z6}^{-1}(z)$ and its negative sign on the displayed activation interval.
\item $S$ is the static beta-seed row, recording the displayed value at $z=0$ and the finite small-$z$ expansion used by the vision theorem.
\item $R$ is the infrared-stability row. It records the denominator-root separation needed to keep the spectral response pole-free on the displayed interval.
\item $H$ stores componentwise $\hsame$ transport for $W,G,I,S,R$ and the structural rows below.
\item $C$ stores displayed $\Cont$ replay from the window source through the derivative row and infrared-stability row.
\item $P$ records $\Pkg$ provenance over $\WindowSixBetaSeedUp$, $\GoldenMeanShiftWindowSixUp$, $\ZeckendorfPhaseShellUp$, $\ZeckendorfPrimeEdgeUp$, $\BetaSignSelectionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WindowSixBetaSeedUp}$ row.
\end{enumerate}
The carrier has no coordinate for continuum running, a host analytic continuation, an unbounded energy-scale law, empirical coupling data, quotient spectra, or a detached real-completion theorem.
\end{definition}

\begin{theorem}[Window-six beta seed NameCert obligations]
\label{thm:window-six-beta-seed-namecert-obligations}
For every accepted $\WindowSixBetaSeedUp$ carrier $B=(W,G,I,S,R,H,C,P,N)$, the local $\NameCert_{\WindowSixBetaSeedUp}$ surface consists exactly of the following finite rows:
\begin{enumerate}
\item carrier admission for the window source, spectral-response row, inverse-coupling derivative, static beta seed, infrared-stability row, transport, replay, provenance, and local naming;
\item inverse-coupling derivative exposure, requiring $I$ to display the negative derivative row before any beta-seed consumer may read $S$;
\item static seed exposure, requiring $S$ to store the finite small-$z$ expansion and the displayed static value rather than a host analytic function;
\item infrared-stability exposure, requiring $R$ to display the denominator-root separation that excludes poles on the inspected interval;
\item structural nonescape, requiring $H,C,P,N$ to transport, replay, source, and name only the displayed packet rows.
\end{enumerate}
No NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:window-six-beta-seed-carrier}. The analytic rows visible to a consumer are $W,G,I,S,R$, and the structural rows are $H,C,P,N$.

The inverse-coupling derivative is read from $I$. It supplies the sign and derivative route before the static seed row is consumed, so the beta seed is not a detached number.

The static row $S$ records the finite small-$z$ expansion, while $R$ records the no-pole denominator check on the inspected interval. These two rows realize the derivative and infrared-stability claims as finite packet data.

Finally $H,C,P,N$ transport, replay, source, and name the same rows through the cited window-six and Zeckendorf siblings. Since the carrier omits continuum running, host analytic continuation, empirical coupling data, quotient spectra, and real-completion theorems, none of those readings can be exported by the local certificate.
\end{proof}

\falsifiablePrediction{An accepted $\WindowSixBetaSeedUp$ read is refuted if a beta-seed consumer receives the static value without first displaying the window-six source $W$, the spectral-response row $G$, the inverse-coupling derivative row $I$, the infrared-stability row $R$, and the structural rows $H,C,P,N$.}

\independenceWitness{golden-mean-shift-window-six, zeckendorf-phase-shell, zeckendorf-prime-edge, beta-sign-selection: $\WindowSixBetaSeedUp$ is not bijective to any listed sibling because it jointly carries the window source, spectral response, inverse-coupling derivative, static seed value, infrared-stability denominator row, transport, replay, provenance, and local naming.}

\closureat{WindowSixBetaSeedUp}{seedStr}

\begin{closurestatus}{\WindowSixBetaSeedUp}
  \constructivestory{A $\WindowSixBetaSeedUp$ packet is generated from a window-six finite source row, a Green-excess spectral-response row, an inverse-coupling derivative row, a static beta-seed row, an infrared-stability row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:window-six-beta-seed-carrier} and \autoref{thm:window-six-beta-seed-namecert-obligations} seed the finite beta-seed packet over inverse-coupling derivative, static value, infrared-stability, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a continuum running law, host analytic continuation, empirical coupling data, quotient spectrum, real-completion theorem, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative exposure, static seed exposure, infrared-stability exposure, transport, replay, provenance, and local naming for the same finite packet.}
\end{closurestatus}
