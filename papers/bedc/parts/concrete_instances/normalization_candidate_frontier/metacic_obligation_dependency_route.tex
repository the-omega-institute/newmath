\begin{theorem}[NormalizationCandidateFrontier MetaCIC obligation link]
\label{thm:normalization-candidate-frontier-metacic-link}
Every candidate-mediated $\NormalizationCandidateFrontierUp$ consumer that feeds a closed-consistency or closed-normalization boundary must cite the four-face $\MetaCICSubjectReductionObligationUp$ packet of \autoref{ch:concrete-instances-metacic-subject-reduction-obligation-namecert} before using subject-reduction routes. In carrier terms, the closed typed window $T$, candidate evidence $E$, bounded route $B$, projection rows $S,I$, obstruction row $O$, transport $H$, replay $C$, provenance $P$, and local $\NameCert$ row $N$ do not license a subject-reduction read unless the beta-substitution, application-argument, lambda-domain, and Pi-domain faces remain visible through the obligation packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:normalization-candidate-frontier-carrier}. The frontier packet exposes candidate-mediated normalization rows and an obstruction row, but it has no coordinate that discharges any subject-reduction face.

Read the four-way boundary of \autoref{thm:metacic-subject-reduction-obligation-four-way-boundary}. It requires the $B,A,L,P$ faces of the $\MetaCICSubjectReductionObligationUp$ packet before the parameterised theorem row can be consumed.

Now compose this boundary with the audit handoff and typed-SN scope for $\NormalizationCandidateFrontierUp$. A closed-consistency or closed-normalization consumer may use the candidate frontier only while the subject-reduction obligation packet remains visible, so the route cannot erase any of the four discharge faces or replace them by an evidence-free subject-reduction claim.
\end{proof}
