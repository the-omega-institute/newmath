\chapter{A Concrete Naming Certificate for $\HilbertMatrixUp$}
\label{ch:concrete-instances-hilbert-matrix-namecert}
\origin{human}

Following Hilbert's classical finite Hilbert matrix construction, the $\HilbertMatrixUp$ packet records the matrix with entries $1/(i+j+1)$ as finite rational rows before any real-valued linear-algebra consumer reads a determinant, Gram, or positive-definite surface. It sits beside the matrix carrier of \autoref{ch:concrete-instances-matrix-namecert}, the rational source of \autoref{ch:concrete-instances-rat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the determinant route of \autoref{ch:concrete-instances-determinant-namecert}. The packet is a finite displayed matrix certificate; it is not a host inverse formula, an infinite moment problem, an analytic operator theorem, or an ambient completeness claim.

\begin{definition}[Hilbert matrix carrier]
\label{def:hilbert-matrix-carrier}
A $\HilbertMatrixUp$ carrier is a finite $\BHist$ packet
$$
H=(N,I,J,A,R,E,D,G,H_s,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $N$ is the displayed matrix-size row, naming the finite dimension and the bounded index windows consumed by the matrix packet.
\item $I$ and $J$ are the row and column index ledgers, each bounded by $N$ and read through the finite $\MatrixUp$ coordinate discipline.
\item $A$ is the rational entry table, assigning to each displayed coordinate pair $(i,j)$ the $\RatUp$ row for $1/(i+j+1)$.
\item $R$ is the $\RealUp$ seal table, obtained only by sealing the rational entry rows after $A$ has been exposed.
\item $E$ is the matrix-envelope row, recording the same finite table as a $\MatrixUp$ read rather than as a host array.
\item $D$ is the determinant and nondegeneracy ledger, consumed through \autoref{ch:concrete-instances-determinant-namecert} from the displayed finite matrix rows.
\item $G$ is the Gram ledger, recording the moment-style positive form over the same rational table and naming the sibling comparison with \autoref{ch:concrete-instances-gram-determinant-namecert}.
\item $H_s$ records componentwise $\hsame$ transport for the size, index, entry, seal, determinant, Gram, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Hilbert-matrix request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\HilbertMatrixUp$, $\MatrixUp$, $\RatUp$, $\RealUp$, $\DeterminantUp$, $\CauchyMatrixUp$, $\GramDeterminantUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\HilbertMatrixUp}$ row.
\end{enumerate}
The local classifier compares accepted packets only through these rows. It has no coordinate for a host inverse formula, a selected real completion, an infinite Hankel operator, quotient equality of matrices, or an analytic moment theorem.
\end{definition}

\begin{theorem}[Hilbert matrix NameCert obligations]
\label{thm:hilbert-matrix-namecert-obligations}
For every accepted $\HilbertMatrixUp$ carrier
$$
H=(N,I,J,A,R,E,D,G,H_s,C,P,L),
$$
the local $\NameCert_{\HilbertMatrixUp}$ surface consists exactly of the displayed size row, bounded row and column ledgers, rational entry table $1/(i+j+1)$, terminal real-entry seals, matrix-envelope row, determinant and nondegeneracy ledger, Gram ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hilbert-matrix-carrier}. The size and index rows $N,I,J$ are the only coordinates from which a finite matrix shape can be read.

Read the entry rows next. The table $A$ supplies each rational entry through $\RatUp$, and the real seals $R$ are downstream of those rational rows.

The matrix, determinant, and Gram consumers then read only the displayed rows $E,D,G$. The cited Matrix, Determinant, CauchyMatrix, and GramDeterminant chapters supply sibling routes for those reads, but none adds a hidden host matrix or analytic inverse.

Finally $H_s,C,P,L$ transport, replay, source, and name the same finite packet. Since no other carrier coordinate is available, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Hilbert matrix positive-definite route]
\label{thm:hilbert-matrix-positive-definite-route}
Every positive-definite or nondegeneracy-facing read of an accepted $\HilbertMatrixUp$ packet factors through the finite rational table $A$, the $\MatrixUp$ envelope $E$, the determinant ledger $D$, and the Gram ledger $G$ before it reaches any $\RealUp$-valued consumer. The route exports a displayed finite Gram-determinant certificate, not a host inverse formula, an infinite Hilbert operator, a selected measure model, or ambient real completeness.
\end{theorem}
\begin{proof}
Start with the rational entries. For displayed indices $i,j<N$, the row $A$ names $1/(i+j+1)$ as a $\RatUp$ entry, so every later read is attached to finite rational data.

Pass to the matrix envelope $E$. This packages the same table as a $\MatrixUp$ object and gives determinant-facing consumers a finite row-and-column surface.

Use the determinant ledger $D$ and the Gram ledger $G$. The determinant route records nondegeneracy on the finite table, while the Gram route records the positive-form reading of the same rows.

The real-entry seals $R$ are terminal annotations on the rational table, not additional analytic evidence. Thus any positive-definite conclusion available to the packet is the displayed finite determinant-and-Gram route, with no hidden inverse, infinite operator, selected measure, or completeness coordinate.
\end{proof}
\leanchecked{BEDC.Derived.HilbertMatrixUp.HilbertMatrixCarrier\_positive\_definite\_route}

\falsifiablePrediction{Every accepted $\HilbertMatrixUp$ read exposes the size row, bounded index ledgers, rational entry table, Real seal table, Matrix envelope, determinant ledger, Gram ledger, hsame, Cont, Pkg, and local NameCert rows before any positive-definite or nondegeneracy-facing claim is public.}

\independenceWitness{matrix, rat, real, determinant, cauchy-matrix, gram-determinant: no listed sibling supplies the joint finite packet containing the Hilbert entry rule $1/(i+j+1)$, its rational and real-entry tables, determinant ledger, Gram ledger, transport, replay, provenance, and local naming while refusing host inverse, infinite operator, selected measure, quotient matrix equality, and ambient completeness data.}

\closureat{HilbertMatrixUp}{seedStr}

\begin{closurestatus}{\HilbertMatrixUp}
  \constructivestory{A $\HilbertMatrixUp$ packet is generated from a finite size row, bounded row and column ledgers, rational entries $1/(i+j+1)$, terminal RealUp seals, a MatrixUp envelope, determinant and Gram ledgers, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hilbert-matrix-carrier}, \autoref{thm:hilbert-matrix-namecert-obligations}, and \autoref{thm:hilbert-matrix-positive-definite-route} seed the finite Hilbert matrix certificate and its determinant-Gram route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close host inverse formulas, infinite Hilbert operators, selected measure models, quotient equality of matrices, ambient $\RealUp$ completeness, checked Lean obligations, or bridge checking.}
  \upgradepath{Formal target registration requires checked carrier admission, rational entry exactness, Matrix envelope replay, determinant nonescape, Gram ledger replay, transport, provenance, and local naming for BEDC.Derived.HilbertMatrixUp.}
\end{closurestatus}
