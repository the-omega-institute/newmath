\chapter{A Concrete Naming Certificate for $\HadamardSpaceUp$}
\label{ch:concrete-instances-hadamard-space-namecert}
\origin{human}

Following Hadamard and Bridson--Haefliger on complete CAT(0) geometry, the $\HadamardSpaceUp$ packet records the finite NameCert surface for a complete geodesic metric carrier with the displayed nonpositive-curvature comparison row that convexity-facing consumers may read. It sits over the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the complete-metric boundary of \autoref{ch:concrete-instances-completemetric-namecert}, the geodesic metric route of \autoref{ch:concrete-instances-geodesic-metric-namecert}, the convex-set rows of \autoref{ch:concrete-instances-convexset-namecert}, the CAT(0) metric comparison surface of \autoref{ch:concrete-instances-cat-zero-metric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the traditional Hadamard-space interface as finite BEDC rows; it does not assert global choice of geodesics, quotient point equality, selected projections, local compactness, differentiable manifold structure, or ambient $\RealUp$ completeness.

\begin{definition}[Hadamard space carrier]
\label{def:hadamard-space-carrier}
A $\HadamardSpaceUp$ carrier is a finite $\BHist$ packet
$$
H=(M,C,G,V,K,R,A,T,D,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricSpaceUp$ carrier row, exposing the named point classifier, distance readbacks, ball observations, and metric-law ledger.
\item $C$ is the $\CompleteMetricUp$ row, recording the displayed Cauchy-thread or completion-facing service available over the same metric source.
\item $G$ is the $\GeodesicMetricUp$ row, exposing only the finite geodesic incidence and endpoint-distance observations inspected by the packet.
\item $V$ is the $\ConvexSetUp$ row for the geodesic convex subsets and segment-closure reads that downstream projection or fixed-point consumers may inspect.
\item $K$ is the $\CatZeroMetricUp$ comparison row, carrying the finite CAT(0) quadrilateral or comparison-triangle obligations used by the Hadamard read.
\item $R$ is the $\RegSeqRatUp$ tolerance readback row for the metric, geodesic, convexity, and CAT(0) distance budgets.
\item $A$ is the terminal $\RealUp$ distance and radius seal reached only after $M,C,G,V,K,R$ have been displayed.
\item $T$ records componentwise $\hsame$ transport for the metric, completeness, geodesic, convexity, comparison, rational-readback, and real-seal rows.
\item $D$ records displayed $\Cont$ replay from Hadamard-space requests through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\HadamardSpaceUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, $\GeodesicMetricUp$, $\ConvexSetUp$, $\CatZeroMetricUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HadamardSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for selected geodesics, quotient points, ambient Hilbert spaces, choice-selected projections, differentiable structure, local compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Hadamard space NameCert obligations]
\label{thm:hadamard-space-namecert-obligations}
For every accepted $\HadamardSpaceUp$ carrier
$$
H=(M,C,G,V,K,R,A,T,D,P,N),
$$
the local $\NameCert_{\HadamardSpaceUp}$ surface consists exactly of carrier admission for the $\MetricSpaceUp$ row $M$, $\CompleteMetricUp$ row $C$, $\GeodesicMetricUp$ row $G$, $\ConvexSetUp$ row $V$, $\CatZeroMetricUp$ row $K$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $A$, componentwise $\hsame$ transport $T$, displayed $\Cont$ replay $D$, provenance $P$, and local naming $N$. Every Hadamard-space consumer must read the complete geodesic CAT(0) surface through those rows and cannot import selected geodesics, quotient equality, projection selectors, Hilbert-space embeddings, local compactness, differentiable structure, or ambient real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hadamard-space-carrier}. The metric source $M$, complete-metric row $C$, geodesic row $G$, convexity row $V$, and CAT(0) comparison row $K$ are the only geometric data available to the packet.

The tolerance and real-valued distance reads are then forced through $R$ and $A$. These rows attach the regular rational readback and terminal Real seal to the same displayed metric, geodesic, convexity, and comparison surface.

Finally use the structural rows $T,D,P,N$. They transport, replay, source, and name only the displayed rows, so there is no route from the accepted packet to selected geodesics, quotient point equality, projection selectors, Hilbert-space embeddings, local compactness, differentiable structure, or ambient real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\HadamardSpaceUp$ read factors through displayed $\MetricSpaceUp$, $\CompleteMetricUp$, $\GeodesicMetricUp$, $\ConvexSetUp$, $\CatZeroMetricUp$, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows before any complete CAT(0) consumer output is visible.}

\independenceWitness{metric, complete-metric, geodesic-metric, convex-set, cat-zero-metric, regseqrat, real: none of the listed siblings alone is bijective with $\HadamardSpaceUp$ because the packet jointly carries a complete metric source, geodesic incidence, convexity reads, CAT(0) comparison, regular rational budgets, a terminal Real seal, transport, replay, provenance, and local naming.}

\closureat{HadamardSpaceUp}{seedStr}

\begin{closurestatus}{\HadamardSpaceUp}
  \constructivestory{A $\HadamardSpaceUp$ packet is generated from MetricSpace carrier rows, CompleteMetric completion rows, GeodesicMetric segment rows, ConvexSet geodesic-convex rows, CatZeroMetric comparison rows, RegSeqRat tolerance readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hadamard-space-carrier} and \autoref{thm:hadamard-space-namecert-obligations} seed the finite complete geodesic CAT(0) NameCert surface over displayed metric, completeness, geodesic, convexity, comparison, tolerance, real-seal, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected geodesics, quotient point equality, projection selectors, Hilbert-space embeddings, local compactness, differentiable structure, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, complete-metric exposure, geodesic incidence, convexity routing, CAT(0) comparison, rational readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.HadamardSpaceUp.}
\end{closurestatus}
