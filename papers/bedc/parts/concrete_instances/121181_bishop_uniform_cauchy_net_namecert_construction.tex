\chapter{A Concrete Naming Certificate for $\BishopUniformCauchyNetUp$}
\label{ch:concrete-instances-bishop-uniform-cauchy-net-namecert}
\origin{human}

Following Moore--Smith, Kelley, and Bishop--Bridges, the $\BishopUniformCauchyNetUp$ packet records the uniform-Cauchy net corridor used by constructive completion arguments before any quotient net, selected limit, cofinal-choice selector, host equality, or ambient $\RealUp$ completeness principle is available. It sits after the located Cauchy net surface of \autoref{ch:concrete-instances-located-cauchy-net-namecert}, the uniform entourage discipline of \autoref{ch:concrete-instances-uniformspace-namecert}, the completion consumer of \autoref{ch:concrete-instances-uniformcompletion-namecert}, the filter-base handoff of \autoref{ch:concrete-instances-filterbase-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Bishop uniform Cauchy net carrier]
\label{def:bishop-uniform-cauchy-net-carrier}
A $\BishopUniformCauchyNetUp$ carrier is a finite $\BHist$ packet
$$
U=(D,M,A,E,W,F,S,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the directed-index row naming finite Moore--Smith indices, preorder comparisons, and displayed upper-window witnesses.
\item $M$ is the Moore--Smith net row exposing finite value windows over $D$.
\item $A$ is the $\CauchyNetUp$ row recording common-tail Cauchy evidence for the same net.
\item $E$ is the $\UniformSpaceUp$ entourage schedule used to compare the displayed tail windows.
\item $W$ is the located-window row, listing the requested entourages, tail bounds, and displayed witnesses that the net enters each requested uniform neighbourhood.
\item $F$ is the $\FilterBaseUp$ handoff row obtained from the displayed tails of $D,M,A,W$.
\item $S$ is the $\StreamNameUp$ finite-window row consumed by sequence-facing completion readers.
\item $R$ is the $\RegSeqRatUp$ readback row assembled from $S$ and the dyadic tolerance ledger.
\item $Q$ is the $\DyadicRatCoreUp$ row recording dyadic radii, comparison slack, and tolerance names used by the located uniform reads.
\item $H$ records componentwise $\hsame$ transport for the directed, net, Cauchy, entourage, located-window, filter-base, stream, rational-readback, dyadic, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a uniform precision request through the same rows.
\item $P$ is the $\Pkg$ provenance over $\BishopUniformCauchyNetUp$, $\LocatedCauchyNetUp$, $\UniformSpaceUp$, $\UniformCompletionUp$, $\FilterBaseUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopUniformCauchyNetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,M,A,E,W,F,S,R,Q,H,C,P,N$. It has no coordinate for quotient nets, selected limits, cofinal-choice selectors, hidden host equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop uniform Cauchy net NameCert obligations]
\label{thm:bishop-uniform-cauchy-net-namecert-obligations}
For every accepted $\BishopUniformCauchyNetUp$ carrier $U=(D,M,A,E,W,F,S,R,Q,H,C,P,N)$, the local $\NameCert_{\BishopUniformCauchyNetUp}$ surface consists exactly of carrier admission, directed-index exposure, uniform entourage schedule exactness, located-window evidence, filter-base handoff, $\StreamNameUp$ and $\RegSeqRatUp$ readback, $\RealUp$ seal non-escape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-uniform-cauchy-net-carrier}. The directed and Cauchy-facing rows are $D,M,A,E,W$, so every uniform read first fixes an index window, a Moore--Smith value window, common-tail evidence, an entourage request, and the located witness for that request.

The filter and sequence-facing rows are then $F,S,R,Q$. The filter-base handoff is obtained from the displayed tails, while the stream, regular-rational, and dyadic rows give finite readback data for downstream Real and Completion consumers.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no additional coordinate, quotient nets, selected limits, cofinal-choice selectors, host equality, and ambient completeness cannot be exported by the local certificate.
\end{proof}

\begin{theorem}[Bishop uniform Cauchy net completion route]
\label{thm:bishop-uniform-cauchy-net-completion-route}
Every $\UniformCompletionUp$ consumer of an accepted $\BishopUniformCauchyNetUp$ carrier must pass through the displayed net row, uniform entourage schedule, located-window evidence, filter-base handoff, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, and terminal $\RealUp$ seal row. The consumer receives no quotient net, selected limit, cofinal-choice selector, host equality, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Begin with a completion-facing read. The route exposes $D,M,A$ before the uniform entourage row $E$ and located-window row $W$ can be interpreted.

Read the filter-base and finite-window handoff next. The rows $F,S,R,Q$ repackage the same displayed tails as filter data, stream windows, regular rational readback, and dyadic tolerance evidence without selecting a subnet or a limit.

The Real-facing seal is downstream from this finite route. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the displayed rows, so the completion consumer cannot bypass them into quotient nets, host equality, selected limits, cofinal choice, or ambient completeness.
\end{proof}

\closureat{BishopUniformCauchyNetUp}{seedStr}

\begin{closurestatus}{\BishopUniformCauchyNetUp}
  \constructivestory{A $\BishopUniformCauchyNetUp$ packet is generated from a directed index row, Moore--Smith net row, Cauchy-net row, uniform entourage schedule, located-window evidence, filter-base handoff, StreamName window, RegSeqRat readback, DyadicRatCore tolerance ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-uniform-cauchy-net-carrier}, \autoref{thm:bishop-uniform-cauchy-net-namecert-obligations}, and \autoref{thm:bishop-uniform-cauchy-net-completion-route} name the finite uniform-Cauchy net corridor for UniformCompletion, StreamName, RegSeqRat, DyadicRatCore, and Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient nets, selected limits, cofinal choice, host equality, ambient Real completeness, Lean verification, or a standard bridge.}
  \upgradepath{Formal work requires carrier admission, entourage exactness, located-window evidence, filter-base handoff, finite readback, Real seal non-escape, transport, replay, provenance, and local naming for the Bishop uniform-Cauchy net route.}
\end{closurestatus}
