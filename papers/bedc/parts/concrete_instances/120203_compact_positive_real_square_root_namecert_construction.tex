\chapter{A Concrete Naming Certificate for $\CompactPositiveRealSquareRootUp$}
\label{ch:concrete-instances-compact-positive-real-square-root-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactPositiveRealSquareRootUp$ packet records the finite square-root service available when the positive input is read from a displayed compact interval source rather than from an isolated positive real row. It sits after the one-input positive square-root packet of \autoref{ch:concrete-instances-positive-real-square-root-namecert}, the compact finite-cover route of \autoref{ch:concrete-instances-dyadic-uniform-cover-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a uniform compact-source bracket service; it is not a host square-root function on an interval, an open-cover compactness theorem, a trichotomy principle, quotient real equality, a selected limit construction, or ambient $\RealUp$ completeness.

\begin{definition}[Compact positive real square-root carrier]
\label{def:compact-positive-real-square-root-carrier}
A $\CompactPositiveRealSquareRootUp$ carrier is a finite $\BHist$ packet
$$
Q=(I,X,B,D,W,R,H,G,S,V,E,T,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $I$ is the compact interval source row whose endpoints and finite subcover route are read through the compact-source interface;
\item $X$ is the positive $\RealUp$ source row exposed inside the compact interval source;
\item $B$ is the dyadic bracket ledger that records a positive lower bracket and an upper bracket for the square-root request on the displayed interval;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the bracket endpoints, interval mesh, and requested precision;
\item $W$ is the $\StreamNameUp$ finite-window row exposing only the source observations and bracket windows needed by the request;
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational source and square-root approximants;
\item $H$ is the $\HeronSquareRootUp$ recurrence row for the bracketed Heron approximants;
\item $G$ is the $\RegularCauchySquareRootUp$ regular-Cauchy square-root row reached after the bracket and recurrence rows are displayed;
\item $S$ is the $\PositiveRealSquareRootUp$ service row that reads the positive source, bracket, recurrence, and regular-Cauchy data as one local positive-input service;
\item $V$ is the terminal $\RealUp$ seal reached after the compact source, dyadic bracket, finite windows, regular rational readback, Heron route, regular-Cauchy route, and positive service row are displayed;
\item $E$ stores componentwise $\hsame$ transport for the compact interval, positive source, bracket, tolerance, window, readback, recurrence, regular-Cauchy, positive-service, and seal rows;
\item $T$ records the displayed $\Cont$ replay by which a consumer reads the same finite compact-source square-root service;
\item $C$ is the classifier row comparing accepted packets only through the listed compact-source, bracket, readback, service, transport, and replay coordinates;
\item $P$ records $\Pkg$ provenance over $\CompactPositiveRealSquareRootUp$, $\PositiveRealSquareRootUp$, $\CompactIntervalUp$, $\PositiveRealUp$, $\RealUp$, $\RegularCauchySquareRootUp$, $\HeronSquareRootUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CompactPositiveRealSquareRootUp}$ row.
\end{enumerate}
The classifier has no coordinate for a total host square-root operator on a compact interval, an open-cover compactness theorem, a trichotomy split on reals, quotient real equality, a selected limit, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact positive real square-root NameCert obligations]
\label{thm:compact-positive-real-square-root-namecert-obligations}
For the carrier of \autoref{def:compact-positive-real-square-root-carrier}, the local $\NameCert_{\CompactPositiveRealSquareRootUp}$ surface consists exactly of six finite obligations: carrier admission for $Q=(I,X,B,D,W,R,H,G,S,V,E,T,C,P,N)$, compact interval and positive source admission through $I,X$, dyadic bracket and tolerance accounting through $B,D$, finite window and regular rational readback through $W,R$, Heron and regular-Cauchy square-root service rows through $H,G,S,V$, and componentwise transport, replay, classifier comparison, provenance, and local naming through $E,T,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier rows. The compact source $I$ and positive source $X$ must be displayed before the bracket service is read, so the certificate starts from a compact interval input and a positive real row rather than from an ambient square-root operator.

Next read the numerical rows. The dyadic bracket $B$ and tolerance ledger $D$ expose the finite lower and upper bracket data; the stream windows $W$ and regular rational readback $R$ keep the request finite.

Finally attach the square-root service rows. The Heron row $H$, regular-Cauchy row $G$, positive square-root row $S$, and Real seal $V$ give the bounded service, while $E,T,C,P,N$ transport, replay, compare, source, and name the same service. None of these rows adds open-cover compactness, trichotomy, quotient real equality, selected limits, or ambient completeness.
\end{proof}

\begin{theorem}[Compact positive real square-root uniform bracket service]
\label{thm:compact-positive-real-square-root-uniform-bracket-service}
Every accepted $\CompactPositiveRealSquareRootUp$ consumer that asks for a square-root value over the displayed compact interval must first read the compact interval source, positive real source, dyadic bracket ledger, dyadic tolerance ledger, finite StreamName windows, RegSeqRat readback, Heron recurrence row, regular-Cauchy square-root row, positive square-root service row, terminal Real seal, componentwise transport, displayed replay, classifier comparison, provenance, and local naming. The service therefore exports only the finite uniform bracket route over the compact positive source.
\end{theorem}
\begin{proof}
Begin with the obligations of \autoref{thm:compact-positive-real-square-root-namecert-obligations}. They force $I,X,B,D,W,R,H,G,S,V,E,T,C,P,N$ to be visible before a consumer can read the compact-source square-root name.

Use the compact-cover source of \autoref{ch:concrete-instances-dyadic-uniform-cover-namecert} together with the dyadic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. These rows keep the interval mesh, bracket endpoints, and requested precision finite.

Read the finite observations through \autoref{ch:concrete-instances-streamname-namecert} and the regular rational readback through \autoref{ch:concrete-instances-regseqrat-namecert}. The Heron and regular-Cauchy rows then feed the positive square-root service of \autoref{ch:concrete-instances-positive-real-square-root-namecert}, and the endpoint is sealed only through \autoref{ch:concrete-instances-real-namecert}.

The structural rows $E,T,C,P,N$ preserve that route. Since the carrier contains no coordinate for a host square-root function on an interval, open-cover compactness, trichotomy, quotient real equality, selected limits, or ambient $\RealUp$ completeness, no consumer can read those objects from the service.
\end{proof}

\falsifiablePrediction{Any accepted CompactPositiveRealSquareRootUp service that bypasses the compact interval source, positive Real source, dyadic bracket ledger, StreamName finite windows, RegSeqRat readback, Heron row, regular-Cauchy square-root row, PositiveRealSquareRoot service row, or terminal Real seal refutes the carrier.}

\independenceWitness{positive-real-square-root, dyadic-uniform-cover, dyadicratcore, streamname, regseqrat, real: $\CompactPositiveRealSquareRootUp$ is not bijective to any listed sibling because it jointly carries compact interval source data, positive source data, dyadic bracket rows, finite windows, readback, Heron and regular-Cauchy service rows, Real sealing, transport, replay, classifier comparison, provenance, and local NameCert data.}

\closureat{CompactPositiveRealSquareRootUp}{seedStr}

\begin{closurestatus}{\CompactPositiveRealSquareRootUp}
  \constructivestory{A $\CompactPositiveRealSquareRootUp$ packet is generated from a compact interval source, positive Real source, dyadic bracket ledger, tolerance accounting, finite StreamName windows, RegSeqRat readback, Heron recurrence, regular-Cauchy square-root readback, positive square-root service, terminal Real sealing, componentwise transport, displayed replay, classifier comparison, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-positive-real-square-root-carrier}, \autoref{thm:compact-positive-real-square-root-namecert-obligations}, and \autoref{thm:compact-positive-real-square-root-uniform-bracket-service} seed the compact-source uniform bracket service over the displayed finite rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close a host square-root operator on compact intervals, open-cover compactness, real trichotomy, quotient real equality, selected limits, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source admission, positive-source guarding, dyadic bracket accounting, finite-window replay, regular rational readback, Heron recurrence accounting, regular-Cauchy square-root readback, Real sealing, transport, provenance, and local naming for BEDC.Derived.CompactPositiveRealSquareRootUp.}
\end{closurestatus}
