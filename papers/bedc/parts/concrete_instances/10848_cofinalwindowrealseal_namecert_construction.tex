\chapter{A Concrete Naming Certificate for $\CofinalWindowRealSealUp$}
\label{ch:concrete-instances-cofinal-window-real-seal-namecert}
\origin{ai}

The $\CofinalWindowRealSealUp$ packet realizes the finite-window and cofinal-tail Real-completion route of \autoref{ch:visions-real-completeness}. It sits after the StreamName surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the RegSeqRat readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the Real seal of \autoref{ch:concrete-instances-real-namecert}, the tail schedule of \autoref{ch:concrete-instances-tail-cofinality-schedule-namecert}, the finite observation-budget selector of \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}, the exact boundary of \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}, and the cofinal diagonal seal of \autoref{ch:concrete-instances-cofinal-diagonal-seal-namecert}. The carrier names their shared terminal meet; it is not a quotient stream, host real equality, selected limit, hidden selector, countable-choice row, or ambient completeness principle.

\falsifiablePrediction{If a Real-facing consumer reaches the terminal seal without the displayed cofinal window, dyadic ledger, and RegSeqRat readback rows, the packet is refuted.}

\independenceWitness{finiteobservationbudgetselector, tailcofinalityschedule, realcompletionexactboundary, cofinaldiagonalseal: each sibling supplies one route face, while this packet names their cofinal-window Real-seal meet.}

\begin{definition}[Cofinal-window Real-seal carrier]
\label{def:cofinal-window-real-seal-carrier}
A $\CofinalWindowRealSealUp$ carrier is a finite $\BHist$ packet
$$
S=(B,T,W,D,R,E,X,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the accepted $\FiniteObservationBudgetSelectorUp$ selector-budget row that bounds the visible requests;
\item $T$ is the accepted $\TailCofinalityScheduleUp$ row that turns those requests into a cofinal finite-window schedule;
\item $W$ is the shared $\StreamNameUp$ finite-window row consumed by the selector, schedule, and terminal seal route;
\item $D$ is the $\DyadicRatCoreUp$ tolerance and dyadic readback ledger attached to $W$;
\item $R$ is the $\RegSeqRatUp$ readback row reached from the same window and dyadic ledger;
\item $E$ is the terminal $\RealUp$ seal row read only after $B,T,W,D,R$ have been displayed;
\item $X$ is the $\RealCompletionExactBoundaryUp$ boundary row confirming that the four Real-completion faces meet at this finite route;
\item $G$ is the $\CofinalDiagonalSealUp$ row confirming that the cofinal-tail route and diagonal Real seal route share the same terminal surface;
\item $H$ records componentwise $\hsame$ transports for all displayed rows;
\item $C$ records the displayed $\Cont$ replay from selector budget and tail schedule through window, dyadic ledger, readback, Real seal, exact boundary, and cofinal diagonal seal;
\item $P$ is the $\Pkg$ provenance over the cited sibling packets, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CofinalWindowRealSealUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for quotient streams, host real equality, selected limits, hidden selectors, countable choice, a detached Real seal, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.CofinalWindowRealSealUp.CofinalWindowRealSealTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Cofinal-window Real-seal NameCert obligations]
\label{thm:cofinal-window-real-seal-namecert-obligations}
For every accepted $\CofinalWindowRealSealUp$ carrier
$$
S=(B,T,W,D,R,E,X,G,H,C,P,N),
$$
the local $\NameCert_{\CofinalWindowRealSealUp}$ surface consists exactly of selector-budget exposure through $B$, tail-cofinality exposure through $T$, finite-window exposure through $W$, dyadic-ledger exposure through $D$, RegSeqRat readback through $R$, terminal Real-seal exposure through $E$, exact-boundary exposure through $X$, cofinal-diagonal-seal exposure through $G$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cofinal-window-real-seal-carrier}. Its displayed inventory lists the selector budget, tail schedule, finite window, dyadic ledger, RegSeqRat readback, Real seal, exact boundary, cofinal diagonal seal, and structural support rows.

Read the route in dependency order. The selector row $B$ bounds the requests, the tail schedule row $T$ supplies the cofinal finite-window service, and the rows $W,D,R,E$ form the StreamName, DyadicRatCore, RegSeqRat, and RealUp terminal route.

The boundary rows $X$ and $G$ identify that route respectively with the Real-completion exact boundary and the cofinal diagonal seal. The rows $H,C,P,N$ only transport, replay, package, and name the same finite packet.

Since quotient streams, host real equality, selected limits, hidden selectors, countable choice, detached Real seals, and ambient completeness are absent from the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cofinal-window Real-seal terminal meet]
\label{thm:cofinal-window-real-seal-terminal-meet}
Every Real-facing or completion-facing consumer of an accepted $\CofinalWindowRealSealUp$ packet reaches the terminal $\RealUp$ seal only through the displayed route
$$
B,T,W,D,R,E,X,G,H,C,P,N.
$$
The route is the common meet of the finite observation-budget selector, the cofinal tail schedule, the exact Real-completion boundary, and the cofinal diagonal seal. No consumer may replace it by a quotient stream, host real equality, selected limit, hidden selector, countable-choice row, detached Real-seal row, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start from \autoref{thm:cofinal-window-real-seal-namecert-obligations}. It exposes the complete route inventory for the accepted packet and gives no second source for budget, schedule, window, dyadic, readback, seal, boundary, or diagonal-seal data.

Attach the selector and schedule faces. The $\FiniteObservationBudgetSelectorUp$ row $B$ fixes the bounded request surface, while the $\TailCofinalityScheduleUp$ row $T$ fixes the cofinal window service before $W,D,R,E$ can be consumed.

Attach the terminal faces. The $\RealCompletionExactBoundaryUp$ row $X$ keeps the StreamName, DyadicRatCore, RegSeqRat, and RealUp faces inside the exact boundary, and the $\CofinalDiagonalSealUp$ row $G$ keeps the same terminal seal on the cofinal diagonal route.

Finally inspect the excluded objects. None occurs in the carrier or in the local classifier, and the structural rows $H,C,P,N$ preserve only the displayed packet. Therefore every accepted Real-facing or completion-facing consumer reaches the terminal seal through the stated meet.
\end{proof}

\begin{theorem}[Cofinal-window Real-seal source exhaustion]
\label{thm:cofinal-window-real-seal-source-exhaustion}
Every accepted Real-facing or completion-facing consumer of a $\CofinalWindowRealSealUp$ carrier can project only the rows
$$
B,T,W,D,R,E,X,G,H,C,P,N
$$
of \autoref{def:cofinal-window-real-seal-carrier}. A terminal $\RealUp$ seal is available only after the selector-budget row $B$, the tail-schedule row $T$, the $\StreamNameUp$ window row $W$, the $\DyadicRatCoreUp$ ledger row $D$, the $\RegSeqRatUp$ readback row $R$, the exact-boundary row $X$, and the cofinal diagonal row $G$ have been displayed on the same route, with $H,C,P,N$ carrying the componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ support.

Thus the terminal seal cannot be reached through a quotient stream, host real equality, selected host limit, hidden selector, countable-choice row, detached Real seal, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalWindowRealSealUp.CofinalWindowRealSeal\_source\_exhaustion}
\begin{proof}
Project the carrier in \autoref{def:cofinal-window-real-seal-carrier}. The packet has exactly the selector-budget, tail-schedule, window, dyadic-ledger, RegSeqRat readback, terminal Real-seal, exact-boundary, cofinal-diagonal, transport, continuation, package, and local naming rows.

Invoke \autoref{thm:cofinal-window-real-seal-namecert-obligations}. It identifies those rows as the whole local NameCert surface and gives no additional source for budget, schedule, window, dyadic, readback, boundary, diagonal, or seal data.

Use \autoref{thm:cofinal-window-real-seal-terminal-meet} for non-escape. Since every Real-facing or completion-facing consumer reaches the terminal meet through the displayed route, a consumer that tries to bypass $B,T,W,D,R,X,G$ before reading $E$ has left the accepted packet.
\end{proof}

\begin{theorem}[Cofinal-window Real-seal obligation scope]
\label{thm:cofinal-window-real-seal-obligation-scope}
Every $\CofinalWindowRealSealUp$ NameCert obligation is scoped only over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and the sibling packets $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\FiniteObservationBudgetSelectorUp$, $\TailCofinalityScheduleUp$, $\RealCompletionExactBoundaryUp$, and $\CofinalDiagonalSealUp$. No obligation row may quantify over a quotient stream, host real equality, selected host limit, hidden selector, countable-choice source, detached Real-seal coordinate, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalWindowRealSealUp.CofinalWindowRealSealObligationScope}
\begin{proof}
Project the carrier of \autoref{def:cofinal-window-real-seal-carrier}. Its rows are exactly $B,T,W,D,R,E,X,G,H,C,P,N$, where the first eight rows name the displayed sibling surfaces and the last four rows give $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support over the same finite history.

Apply \autoref{thm:cofinal-window-real-seal-namecert-obligations} to read the NameCert surface from those rows. Apply \autoref{thm:cofinal-window-real-seal-terminal-meet} to force every Real-facing consumer through the same route.

Finally apply \autoref{thm:cofinal-window-real-seal-source-exhaustion}. Since the checked source-exhaustion target admits no further source coordinate, every obligation remains inside the displayed BEDC primitive and sibling dependency scope.
\end{proof}

\begin{theorem}[Cofinal-window Real-seal public certificate]
\label{thm:cofinal-window-real-seal-public-certificate}
The public $\CofinalWindowRealSealUp$ export is exactly the finite route
$$
B,T,W,D,R,E,X,G,H,C,P,N
$$
and its local $\NameCert_{\CofinalWindowRealSealUp}$ row. It exports no quotient stream, host equality, selected limit, completeness coordinate, hidden selector, countable-choice row, or detached Real seal.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalWindowRealSealUp.CofinalWindowRealSealPublicCertificate}
\begin{proof}
Begin with \autoref{thm:cofinal-window-real-seal-namecert-obligations}. It identifies the local NameCert row with the displayed finite route and gives no second certificate component.

Use \autoref{thm:cofinal-window-real-seal-terminal-meet} and \autoref{thm:cofinal-window-real-seal-source-exhaustion} to rule out consumer-side escape from that route. The terminal Real seal can be read only after the selector, schedule, window, dyadic ledger, RegSeqRat readback, exact boundary, and cofinal diagonal rows are present.

It remains to bind the export boundary. \autoref{thm:cofinal-window-real-seal-obligation-scope} says every obligation row lives inside the same BEDC primitive and sibling dependency scope, so the public certificate is precisely the finite route together with its local $\NameCert$.
\end{proof}

\begin{theorem}[Cofinal-window Real-seal bridge boundary]
\label{thm:cofinal-window-real-seal-bridge-boundary}
Any standard bridge that later presents $\CofinalWindowRealSealUp$ to a Real-facing consumer must factor through the checked source-exhaustion route and through the public certificate of \autoref{thm:cofinal-window-real-seal-public-certificate}. A consumer row that bypasses either boundary is not a bridge for this packet.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalWindowRealSealUp.CofinalWindowRealSealUp\_StdBridge}
\begin{proof}
Use the checked source-exhaustion marker for \autoref{thm:cofinal-window-real-seal-source-exhaustion}. It fixes the only rows a Real-facing or completion-facing consumer may project from the accepted carrier.

Apply \autoref{thm:cofinal-window-real-seal-public-certificate}. The public export contains the finite route $B,T,W,D,R,E,X,G,H,C,P,N$ and the local $\NameCert$ row, and it excludes the quotient, host-equality, selected-limit, and completeness coordinates.

Therefore a standard bridge can only expose a Real-facing consumer row after passing through both boundaries. Any map that omits the checked source-exhaustion route or the displayed public certificate has left the named packet.
\end{proof}

Lean development for this route must provide \texttt{Nontrivial CofinalWindowRealSealUp} and \texttt{FieldFaithful CofinalWindowRealSealUp} in \texttt{lean4/BEDC/Derived/CofinalWindowRealSealUp/TasteGate.lean}.

\begin{closurestatus}{\CofinalWindowRealSealUp}
  \constructivestory{The cofinal-window Real seal is generated from a $BHist$ packet that records selector-budget rows, tail schedules, finite windows, dyadic ledgers, regular-rational readbacks, and the terminal Real-seal handoff. The carrier convention first exposes the window ledger, then requires every later Real-facing row to be connected by $\Cont$ to that same cofinal route. The $hsame$ relation supplies representative stability: if the history is renamed, the terminal meet and source-exhaustion rows still read back the same window evidence. No quotient stream or selected limit is introduced; the seal is the visible route by which finite windows become the only admissible Real consumer surface.}
  \leanchecked{BEDC.Derived.CofinalWindowRealSealUp.taste\_gate}
  \theoryclosure{\obligationClosure}
  \scopeclosed{This chapter binds the cofinal-window Real-seal carrier, NameCert obligations, terminal-meet theorem, and source-exhaustion theorem.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.CofinalWindowRealSealUp.CofinalWindowRealSeal\_source\_exhaustion}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim ambient real completeness, quotient-stream equality, or a selected limit beyond the cofinal window route.}
  \upgradepath{Reaching scopedClosure requires every obligation row to name its exact BEDC primitive and sibling dependency scope.}
\end{closurestatus}
