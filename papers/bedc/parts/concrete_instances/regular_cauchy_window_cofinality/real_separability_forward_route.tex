The $\RealSeparabilityUp$ density reads consume $\RegularCauchyWindowCofinalityUp$ only after $\StreamNameUp$ finite windows, $\DyadicRatCoreUp$ tolerances, $\RegSeqRatUp$ readback, the $\CauchyModulusUp$ schedule, and the $\RealUp$ seal are visible. Thus the dependency edge into \autoref{ch:concrete-instances-real-separability-namecert} remains a finite displayed route rather than a quotient stream, selected dense sequence, countable-choice enumeration, host real equality, or ambient completeness claim.
