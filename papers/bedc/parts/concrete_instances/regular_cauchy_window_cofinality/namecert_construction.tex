\chapter{A Concrete Naming Certificate for $\RegularCauchyWindowCofinalityUp$}
\label{ch:concrete-instances-regular-cauchy-window-cofinality-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyWindowCofinalityUp$ packet records the finite cofinal-window surface needed by regular Cauchy completion arguments. It sits after the stream-window route of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\CauchyModulusUp$ schedule row of \autoref{ch:concrete-instances-cauchymodulus-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives L10 Real and Completion consumers a displayed way to cite tail cofinality without quotient streams, selected limits, countable choice, or ambient real completeness.

\begin{definition}[Regular Cauchy window cofinality carrier]
\label{def:regular-cauchy-window-cofinality-carrier}
A $\RegularCauchyWindowCofinalityUp$ carrier is a finite $\BHist$ packet
$$
W=(p,S,D,R,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $p$ is the requested precision row, naming the finite tolerance threshold at which a downstream consumer asks for a tail window.
\item $S$ is the $\StreamNameUp$ window row, exposing only the finite stream entries read at that precision.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare displayed entries.
\item $R$ is the $\RegSeqRatUp$ readback row reached after $S$ and $D$ have been displayed.
\item $M$ is the $\CauchyModulusUp$ row, recording the finite cofinal schedule that sends the requested precision to an admissible tail window.
\item $H$ records componentwise $\hsame$ transport for precision, stream windows, dyadic tolerances, regular rational readback, modulus rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the route
$$
p,S,D,M \longmapsto R \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyWindowCofinalityUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyWindowCofinalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $p,S,D,R,M,H,C,P,N$. It has no coordinate for a quotient Cauchy stream, selected representative, countable-choice selector, host real equality, or ambient completeness theorem.
\end{definition}

\begin{theorem}[Regular Cauchy window cofinality NameCert obligations]
\label{thm:regular-cauchy-window-cofinality-namecert-obligations}
For every accepted $\RegularCauchyWindowCofinalityUp$ carrier $W=(p,S,D,R,M,H,C,P,N)$, the local $\NameCert_{\RegularCauchyWindowCofinalityUp}$ surface consists exactly of precision admission through $p$, finite stream-window exposure through $S$, dyadic tolerance comparison through $D$, regular rational readback through $R$, cofinal Cauchy-modulus scheduling through $M$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-window-cofinality-carrier}. The displayed rows are exactly $p,S,D,R,M,H,C,P,N$, so the precision request reaches the packet as finite data.

The stream and tolerance rows are read before the regular rational handoff. Row $S$ supplies the finite $\StreamNameUp$ window, row $D$ supplies the dyadic comparison budget, and row $M$ records the cofinal tail schedule requested by the precision row.

The readback row $R$ is therefore downstream of finite windows, dyadic tolerances, and the modulus schedule. Transport, replay, provenance, and local naming preserve that route, so no hidden quotient stream, selected limit, countable-choice selector, host equality, or completeness theorem can be exported.
\end{proof}

\begin{theorem}[Regular Cauchy window cofinality sibling route]
\label{thm:regular-cauchy-window-cofinality-sibling-route}
Every Real or Completion consumer of an accepted $\RegularCauchyWindowCofinalityUp$ packet must factor its read through the finite route
$$
\StreamNameUp,\DyadicRatCoreUp,M \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
Here $M$ is sourced from the $\CauchyModulusUp$ scheduling row of \autoref{ch:concrete-instances-cauchymodulus-namecert}. In particular, the packet consumes stream windows from \autoref{ch:concrete-instances-streamname-namecert}, dyadic tolerances from \autoref{ch:concrete-instances-dyadicratcore-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}; it cannot bypass those sibling rows by appealing to quotient streams, selected limits, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the precision row $p$. The cofinal schedule $M$ selects a displayed tail window, and the consumer may inspect only the corresponding finite stream row $S$ and dyadic tolerance row $D$.

Next use the regular rational readback. The row $R$ is available only after $S,D,M$ have been displayed, so the $\RegSeqRatUp$ handoff is a finite tail-window read rather than a quotient stream.

Finally attach the Real-facing boundary. The structural rows $H,C,P,N$ preserve the same route, and any downstream $\RealUp$ read receives only the displayed finite cofinal window with its dyadic tolerance and regular rational readback.
\end{proof}

\begin{theorem}[Regular Cauchy window cofinality tail directedness]
\label{thm:regular-cauchy-window-cofinality-tail-directedness}
For two accepted $\RegularCauchyWindowCofinalityUp$ packets over the same precision route, the displayed cofinal modulus schedules admit a later finite tail window whose $\StreamNameUp$ row, $\DyadicRatCoreUp$ tolerance row, $\RegSeqRatUp$ readback row, and $\RealUp$ seal factor through both original windows. The common-window read remains inside the displayed rows of the two packets and does not use quotient stream equality, selected limits, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project both carriers by \autoref{def:regular-cauchy-window-cofinality-carrier}. Each packet exposes the same precision route before its finite stream window $S$, dyadic tolerance row $D$, regular rational readback row $R$, and cofinal schedule row $M$ can be consumed.

Use the two displayed cofinal schedule rows. Applying $M$ in the first packet and $M$ in the second packet gives two finite tail requests; choose a displayed later unary threshold that is later than both requests in the common precision route.

At that later threshold, the stream window, dyadic tolerance, regular readback, and Real-facing seal are read through the same finite tail. By \autoref{thm:regular-cauchy-window-cofinality-sibling-route}, the consumer must still factor through
$$
\StreamNameUp,\DyadicRatCoreUp,M \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$

The construction uses only the carrier rows and the later finite threshold. It introduces no quotient stream equality, selected limit, countable-choice selector, or ambient completeness principle.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyWindowCofinalityUp$ read displays one precision row, one finite $\StreamNameUp$ window row, one $\DyadicRatCoreUp$ tolerance row, one $\RegSeqRatUp$ readback row, one Cauchy modulus schedule row, and local transport, replay, provenance, and naming rows before it is consumed by a Real or Completion route.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real: $\RegularCauchyWindowCofinalityUp$ is not bijective to any listed sibling because it binds a precision request, finite stream window, dyadic tolerance ledger, regular rational readback, cofinal modulus schedule, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ into one accepted completion-facing packet.}

\closureat{RegularCauchyWindowCofinalityUp}{seedStr}

\begin{closurestatus}{\RegularCauchyWindowCofinalityUp}
  \constructivestory{A $\RegularCauchyWindowCofinalityUp$ packet is generated from a precision request, finite StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, a Cauchy modulus schedule, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-window-cofinality-carrier}, \autoref{thm:regular-cauchy-window-cofinality-namecert-obligations}, and \autoref{thm:regular-cauchy-window-cofinality-sibling-route} seed the finite cofinal-window route over StreamName, DyadicRatCore, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient Cauchy stream equality, selected limits, countable choice, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked precision admission, stream-window exposure, dyadic tolerance comparison, RegSeqRat readback, cofinal modulus scheduling, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyWindowCofinalityUp.}
\end{closurestatus}
