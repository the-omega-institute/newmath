The $\RegularCauchyWindowCofinalityUp$ route is a sibling dependency surface for finite Real and Completion readback, not an isolated Cauchy primitive.

The cofinal window is consumed as the finite $\RegSeqRatUp$ readback scheduled through $\StreamNameUp$ windows: \autoref{ch:concrete-instances-regseqrat-namecert} supplies the regular rational handoff, while \autoref{ch:concrete-instances-streamname-namecert} supplies the inspected finite windows that make the cofinal tail visible to downstream consumers.

The same cofinal tail window also consumes the $\CauchyModulusUp$ schedule row of \autoref{ch:concrete-instances-cauchymodulus-namecert} before $\RegSeqRatUp$ and $\RealUp$ consumers may read the window. This is a substantive dependency on the displayed modulus schedule, not a host choice of a tail index.

\begin{theorem}[Regular cofinal windows feed separability consumers]
\label{thm:regular-cauchy-window-cofinality-streamname-forward-dependency}
Any $\RealSeparabilityUp$ density consumer citing the $\RegularCauchyWindowCofinalityUp$ route of \autoref{ch:concrete-instances-regular-cauchy-window-cofinality-namecert} must first read the $\StreamNameUp$ finite window, the $\DyadicRatCoreUp$ tolerance row, the $\RegSeqRatUp$ readback, and the $\CauchyModulusUp$ schedule. Thus the downstream density edge of \autoref{ch:concrete-instances-real-separability-namecert} consumes a displayed cofinal window only after the finite source window, dyadic tolerance, regular rational readback, and modulus schedule have all been exposed; it exports no quotient stream, selected limit, countable-choice schedule, or ambient completeness assertion.
\end{theorem}

Completion-functor, $\RealSeparabilityUp$, and regular-Cauchy density consumers may cite the $\RegularCauchyWindowCofinalityUp$ route only after reading the $\StreamNameUp$ finite window of \autoref{ch:concrete-instances-streamname-namecert}, the $\DyadicRatCoreUp$ tolerance row of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the $\CauchyModulusUp$ schedule row of \autoref{ch:concrete-instances-cauchymodulus-namecert}. The completion-facing edge is therefore a finite lattice dependency, not a host choice of a cofinal tail or an ambient compactness assertion.
