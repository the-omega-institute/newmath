The $\RegularCauchyWindowCofinalityUp$ completion anchor reads the same existing siblings before any sealed real-facing consumer can cite the cofinal window route. It consumes $\RegSeqRatUp$ windows from \autoref{ch:concrete-instances-regseqrat-namecert}, $\StreamNameUp$ schedules from \autoref{ch:concrete-instances-streamname-namecert}, and the terminal $\RealUp$ completion seal from \autoref{ch:concrete-instances-real-namecert}, so the hub dependency edge remains a displayed finite route rather than a quotient stream, selected limit, countable-choice selector, or ambient completeness claim.
