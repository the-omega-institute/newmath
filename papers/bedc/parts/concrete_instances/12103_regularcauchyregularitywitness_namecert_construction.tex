\chapter{A Concrete Naming Certificate for $\RegularCauchyRegularityWitnessUp$}
\label{ch:concrete-instances-regular-cauchy-regularity-witness-namecert}
\origin{ai}

The $\RegularCauchyRegularityWitnessUp$ packet realizes the diagonal-regularity row of \autoref{thm:real-completeness-diagonal-regular} as a finite concrete NameCert carrier. It sits downstream of the selected diagonal representative in \autoref{ch:concrete-instances-diagonallimit-namecert}, \autoref{ch:concrete-instances-diagonallimitrepresentative-namecert}, and the three-leg estimate surface of \autoref{ch:concrete-instances-regular-cauchy-triangle-bound-namecert}, while consuming the window, regular-rational, dyadic, and real-seal sibling rows of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names the finite regularity evidence used by downstream Real-completion routes; it is not a selected limit, host real equality, quotient-stream equality, countable choice, or ambient completeness principle.

\begin{definition}[Regular Cauchy regularity-witness carrier]
\label{def:regular-cauchy-regularity-witness-carrier}
A $\RegularCauchyRegularityWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(S,\mu,j,\Omega,R,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the selected source row naming the diagonal representative route consumed from \autoref{ch:concrete-instances-diagonallimitrepresentative-namecert}.
\item $\mu$ is the modulus index row that selects the finite regular-Cauchy comparison budget for the requested pair of precisions.
\item $j$ is the finite index arithmetic row tying the selected source row to the regularity request.
\item $\Omega$ is the $\StreamNameUp$ window exposing the diagonal observation and the intermediate observations used by the regularity consumer.
\item $R$ is the $\RegSeqRatUp$ readback row for the selected window and its regular-rational endpoint comparisons.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger recording the two endpoint tolerances, the middle comparison tolerance, and the final diagonal-regularity bound.
\item $E$ is the $\RealUp$ seal handoff row reached only after the displayed window, readback, and tolerance ledger have been read.
\item $H$ records componentwise $\hsame$ transport for $S,\mu,j,\Omega,R,Q,E$ and the local certificate row.
\item $C$ records displayed $\Cont$ replay from selected source row through modulus index, window, readback, dyadic ledger, and Real seal handoff.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyRegularityWitnessUp$, $\DiagonalLimitUp$, $\RegularCauchyTriangleBoundUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyRegularityWitnessUp}$ row.
\end{enumerate}
The carrier exports no selected host limit, host real equality, quotient-stream equality, $\mathsf{Quot.sound}$ row, $\mathsf{Classical.choice}$ row, or ambient $\RealUp$ completeness coordinate.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyRegularityWitnessUp.TasteGate.RegularCauchyRegularityWitnessTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy regularity-witness NameCert obligations]
\label{thm:regular-cauchy-regularity-witness-namecert-obligations}
For the carrier of \autoref{def:regular-cauchy-regularity-witness-carrier}, the local $\NameCert_{\RegularCauchyRegularityWitnessUp}$ surface consists exactly of carrier admission for $W=(S,\mu,j,\Omega,R,Q,E,H,C,P,N)$, selected diagonal source exposure through $S$, modulus-index computation through $\mu$ and $j$, finite $\StreamNameUp$ window exposure through $\Omega$, $\RegSeqRatUp$ regularity readback through $R$, $\DyadicRatCoreUp$ tolerance-ledger exposure through $Q$, downstream $\RealUp$ seal handoff through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyRegularityWitnessUp.TasteGate.RegularCauchyRegularityWitnessNameCertObligations}
\begin{proof}
Project the carrier. Its coordinates list the selected diagonal source row, modulus index, index arithmetic, finite stream window, regular-rational readback, dyadic tolerance ledger, Real seal handoff, transport, replay, provenance, and local name.

The sibling surfaces account for the data-bearing rows. The diagonal representative supplies $S$; the triangle-bound packet supplies the regularity estimate route; $\StreamNameUp$ supplies $\Omega$; $\RegSeqRatUp$ supplies $R$; $\DyadicRatCoreUp$ supplies $Q$; and $\RealUp$ supplies $E$ only as the terminal seal handoff.

The remaining rows do not add mathematical content. They transport, replay, source, and name the displayed rows, so the listed clauses exhaust the local NameCert obligations.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyRegularityWitnessUp.RegularCauchyRegularityWitness\_namecert\_obligations}

\begin{theorem}[Regular Cauchy regularity-witness diagonal-consumer boundary]
\label{thm:regular-cauchy-regularity-witness-diagonal-consumer-boundary}
Every diagonal-regularity consumer of an accepted $\RegularCauchyRegularityWitnessUp$ packet factors through the finite route
$$
S,\mu,j,\Omega,R,Q,E,H,C,P,N.
$$
The consumer reads the selected source row, modulus index, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ tolerance ledger before it can receive the $\RealUp$ seal handoff.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations. They require the selected diagonal source row and the modulus-index rows to be visible before any regularity evidence is read.

Next read the sibling surfaces in order. The stream window exposes the finite observations, the regular-rational row supplies the endpoint and middle readbacks, and the dyadic ledger records the tolerance arithmetic consumed by the diagonal-regularity theorem.

Finally pass through the Real seal handoff and the structural rows. Since the carrier contains no off-route equality or selection coordinate, every diagonal-regularity consumer remains inside the displayed finite route.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyRegularityWitnessUp.RegularCauchyRegularityWitnessDiagonalConsumerBoundary}

\begin{theorem}[Regular Cauchy regularity-witness no-hidden-limit boundary]
\label{thm:regular-cauchy-regularity-witness-no-hidden-limit}
An accepted $\RegularCauchyRegularityWitnessUp$ packet exports exactly the selected source row $S$, modulus-index rows $\mu,j$, finite $\StreamNameUp$ window $\Omega$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ tolerance ledger $Q$, $\RealUp$ seal handoff $E$, componentwise transport $H$, continuation replay $C$, package provenance $P$, and local naming row $N$. It exports no selected host limit, host real equality, quotient-stream equality, $\mathsf{Quot.sound}$ witness, $\mathsf{Classical.choice}$ witness, or ambient completeness row.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates of \autoref{def:regular-cauchy-regularity-witness-carrier}. Every coordinate is a displayed BEDC row, a finite modulus or index row, or structural support for transport, replay, provenance, and naming.

\autoref{thm:regular-cauchy-regularity-witness-namecert-obligations} accounts for the admissible carrier and classifier-facing rows. \autoref{thm:regular-cauchy-regularity-witness-diagonal-consumer-boundary} accounts for every diagonal-regularity consumer read.

The excluded objects require additional coordinates absent from the carrier. Hence they cannot be exported by the local naming certificate or recovered by a consumer of the displayed regularity-witness route.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyRegularityWitnessUp.RegularCauchyRegularityWitness\_no\_hidden\_limit\_boundary}

\begin{theorem}[Regular Cauchy regularity-witness Real-completion handoff]
\label{thm:regular-cauchy-regularity-witness-real-completion-handoff}
Every accepted $\RegularCauchyRegularityWitnessUp$ packet
$$
W=(S,\mu,j,\Omega,R,Q,E,H,C,P,N)
$$
consumed by a $\RealCauchyCompletionUp$ diagonal route is read through the selected diagonal source $S$, the finite $\StreamNameUp$ window $\Omega$, the $\RegSeqRatUp$ readback $R$, the $\DyadicRatCoreUp$ tolerance ledger $Q$, and the $\RealUp$ seal handoff $E$ before any completion-facing seal is available. The same route displays the supporting $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows $H,C,P,N$ carried by the packet.

Consequently the completion-facing read factors through the finite regularity-witness surface and the finite-window Real-completion support route. It exports no selected host limit, host real equality, quotient-stream equality, $\mathsf{Quot.sound}$ row, $\mathsf{Classical.choice}$ row, ambient completeness coordinate, or substitute support row outside the displayed BEDC packet.
\end{theorem}
\begin{proof}
Begin with the local packet. \autoref{thm:regular-cauchy-regularity-witness-namecert-obligations} exposes exactly the selected source, modulus-index, window, readback, tolerance-ledger, Real-seal handoff, transport, replay, provenance, and naming rows of $W$.

The diagonal-consumer boundary \autoref{thm:regular-cauchy-regularity-witness-diagonal-consumer-boundary} orders these rows before any consumer receives the $\RealUp$ seal handoff. The no-hidden-limit boundary \autoref{thm:regular-cauchy-regularity-witness-no-hidden-limit} removes selected host limits, host real equality, quotient-stream equality, $\mathsf{Quot.sound}$, $\mathsf{Classical.choice}$, and ambient completeness from the packet surface.

Now attach the completion-facing route. \autoref{thm:real-cauchy-completion-finite-window-completion-coverage} exhausts every accepted $\RealCauchyCompletionUp$ finite-window completion read by the displayed $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\DyadicRatCoreUp$ ledgers, diagonal packet, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. \autoref{thm:real-cauchy-completion-current-phase-four-face-support-minimality} says that the Phase Real and Completion exit remains valid only when $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and the displayed structural support rows are present.

Intersecting these two finite routes leaves exactly the handoff stated above. The regularity witness supplies the selected diagonal source, window, readback, dyadic ledger, Real seal, and support rows consumed by the Real-completion route, and the cited non-escape clauses leave no coordinate through which the completion-facing seal could bypass them.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyRegularityWitnessUp.RegularCauchyRegularityWitnessRealCompletionHandoff}

\begin{theorem}[Regular Cauchy regularity witness tail-window regularity]
\label{thm:regularcauchyregularitywitness-tail-window-regularity}
Every accepted $\RegularCauchyRegularityWitnessUp$ packet consumed as a regularity witness exposes its $\StreamNameUp$ finite observation windows, $\RegSeqRatUp$ regularity rows, $\DyadicRatCoreUp$ tolerance rows, $\RealUp$ sealing row, and $\RealCauchyCompletionUp$ handoff before the witness can be consumed. Equivalently, a consumer of the packet must factor through the displayed tail-window route
$$
S,\mu,j,\Omega,R,Q,E,H,C,P,N
$$
with $\Omega$ read as the finite observation window, $R$ as the regular-rational regularity readback, $Q$ as the dyadic tolerance ledger, $E$ as the Real sealing row, and $H,C,P,N$ as the componentwise transport, continuation replay, provenance, and naming rows that carry the handoff into the completion-facing route.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyRegularityWitnessUp.RegularCauchyRegularityWitnessTailWindowRegularity}
\begin{proof}
Project the accepted carrier. The coordinates of \autoref{def:regular-cauchy-regularity-witness-carrier} display the selected source row, modulus-index rows, finite observation window, regular-rational readback, dyadic tolerance ledger, Real seal, and structural support rows.

The consumer order is forced by the preceding boundaries. \autoref{thm:regular-cauchy-regularity-witness-diagonal-consumer-boundary} places the window, readback, and tolerance ledger before the Real seal; \autoref{thm:regular-cauchy-regularity-witness-real-completion-handoff} then routes the same displayed rows into the $\RealCauchyCompletionUp$ diagonal surface.

It remains to check that transport cannot alter the exposed rows. Replay the componentwise $\hsame$ row $H$ and the displayed $\Cont$ row $C$ along the packet coordinates; the $\Pkg$ row $P$ and local $\NameCert$ row $N$ only source and name the same finite data. Thus any accepted consumer sees the tail-window route before it can consume the regularity witness.
\end{proof}

\begin{theorem}[Regular Cauchy regularity-witness obligation closure]
\label{thm:regular-cauchy-regularity-witness-obligation-closure}
For the carrier $W=(S,\mu,j,\Omega,R,Q,E,H,C,P,N)$ of \autoref{def:regular-cauchy-regularity-witness-carrier}, carrier admission, diagonal source exposure, modulus-index rows, finite $\StreamNameUp$ window exposure, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RegularCauchyRegularityWitnessUp}$ row form the full obligation surface. Any accepted regularity-witness consumer factors through that finite surface before it can use the diagonal-regularity witness, and no selected host limit, host real equality, quotient-stream equality, $\mathsf{Quot.sound}$ row, $\mathsf{Classical.choice}$ row, or ambient $\RealUp$ completeness coordinate is part of the obligation closure.
\end{theorem}
\begin{proof}
Project the accepted packet $W$. The carrier definition displays exactly the source, modulus, index, finite window, readback, tolerance, Real seal, transport, replay, provenance, and naming rows, and \autoref{thm:regular-cauchy-regularity-witness-namecert-obligations} identifies those rows as the local NameCert obligations.

Induct over the displayed finite window route. The base read exposes the selected diagonal source $S$ and the first window row in $\Omega$ through the modulus-index data $\mu,j$. The step extends the same route by one displayed observation, transports the previous rows through $H$, and replays the continuation row $C$ without adding a new carrier coordinate.

The consumer boundaries close the remaining cases. \autoref{thm:regular-cauchy-regularity-witness-diagonal-consumer-boundary} forces any diagonal-regularity consumer through the listed rows; \autoref{thm:regular-cauchy-regularity-witness-no-hidden-limit} removes off-route limit and equality data; and \autoref{thm:regular-cauchy-regularity-witness-real-completion-handoff} places the $\RealUp$ seal after the window, readback, and dyadic ledger.

Thus the finite route named in \autoref{thm:regularcauchyregularitywitness-tail-window-regularity} is not merely a handoff order. It is the whole obligation surface for the accepted packet, with $P$ and $N$ recording provenance and local naming for the same rows.
\end{proof}

\begin{theorem}[Regular Cauchy regularity-witness scoped kernel route]
\label{thm:regular-cauchy-regularity-witness-scoped-kernel-route}
The accepted $\RegularCauchyRegularityWitnessUp$ carrier factors only through
the finite $\StreamNameUp$ window rows, $\RegSeqRatUp$ readback rows,
$\DyadicRatCoreUp$ tolerance ledger, $\RealUp$ seal handoff, $\BHist$ packet
rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$
provenance, and local $\NameCert$ row before a diagonal-regularity consumer can
read it. Thus the scoped kernel route for the carrier
$$
W=(S,\mu,j,\Omega,R,Q,E,H,C,P,N)
$$
is exhausted by the displayed rows
$$
S,\mu,j,\Omega,R,Q,E,H,C,P,N
$$
and cannot pass through selected host limits, host real equality,
quotient-stream equality, $\mathsf{Quot.sound}$, $\mathsf{Classical.choice}$,
or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The source, modulus, and index rows determine
which finite window route is being read, while $\Omega$ is the only
$\StreamNameUp$ window coordinate available to the consumer.

The readback and estimate rows then fix the numerical surface. The
$\RegSeqRatUp$ row $R$ supplies the regular-rational readback, and the
$\DyadicRatCoreUp$ row $Q$ records the tolerance ledger used by the
diagonal-regularity consumer before any Real seal is visible.

The terminal and structural rows close the route. The $\RealUp$ handoff $E$ is
read only after the window, readback, and tolerance rows; $H,C,P,N$ transport,
replay, source, and name the same packet. By the no-hidden-limit boundary,
there is no carrier coordinate for off-route equality, choice, quotient soundness,
or ambient completeness, so the displayed finite route is the scoped kernel
route.
\end{proof}

\begin{theorem}[Regular Cauchy regularity-witness finite-window induction]
\label{thm:regular-cauchy-regularity-witness-finite-window-induction}
Every accepted $\RegularCauchyRegularityWitnessUp$ packet
$$
W=(S,\mu,j,\Omega,R,Q,E,H,C,P,N)
$$
can be consumed by induction over the finite $\StreamNameUp$ windows displayed in $\Omega$ before the $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ tolerance ledger $Q$, and $\RealUp$ seal handoff $E$ are exposed. The induction base reads the selected diagonal source $S$ and the first window row named by $\mu,j$; each induction step appends one displayed window row, transports the prior window data through $H$, and replays the finite continuation route in $C$. Only after this finite induction has displayed the requested window may a consumer read $R$, then $Q$, and finally $E$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:regular-cauchy-regularity-witness-carrier}. The only window data available before readback is the finite $\StreamNameUp$ coordinate $\Omega$, with the selected source row $S$ and modulus-index rows $\mu,j$ determining which finite observations are requested.

Induct over the displayed window spine. In the base case, the consumer reads $S$ and the first named observation through $\Omega$; no regular-rational readback, dyadic ledger, or Real seal coordinate is yet available.

For the step case, append one displayed observation row of $\Omega$. The componentwise transport row $H$ preserves the prior selected source, modulus, index, and window rows, while the continuation row $C$ replays the finite route to the extended window without adding an off-carrier equality or choice row.

After the finite window has been displayed, \autoref{thm:regular-cauchy-regularity-witness-diagonal-consumer-boundary} and \autoref{thm:regular-cauchy-regularity-witness-real-completion-handoff} force the remaining order: the consumer reads the $\RegSeqRatUp$ row $R$, then the $\DyadicRatCoreUp$ ledger $Q$, and only then the $\RealUp$ seal handoff $E$. Thus the accepted witness is consumable by finite-window induction before any downstream readback, tolerance ledger, or Real seal is exposed.
\end{proof}

\falsifiablePrediction{Every accepted RegularCauchyRegularityWitnessUp packet exposes a finite dyadic regularity-witness row and admits no route to a selected limit, host real equality, Quot.sound, or Classical.choice.}

\independenceWitness{diagonallimit, regular-cauchy-triangle-bound, regseqrat, streamname, real: no listed sibling is bijective to $\RegularCauchyRegularityWitnessUp$ because this packet binds selected diagonal source, modulus index, finite window, regular-rational readback, dyadic tolerance ledger, Real seal handoff, transport, replay, provenance, and local NameCert rows in one consumer-facing regularity witness. The diagonal-limit sibling lacks the regularity-consumer tolerance ledger; the triangle-bound sibling lacks the selected representative seal handoff; RegSeqRatUp lacks the StreamName and Real route; StreamNameUp lacks regularity and dyadic ledger rows; RealUp lacks the finite witness route.}

\closureat{RegularCauchyRegularityWitnessUp}{obligationStr}

\begin{closurestatus}{\RegularCauchyRegularityWitnessUp}
  \constructivestory{A $\RegularCauchyRegularityWitnessUp$ packet is generated from a selected diagonal source row, modulus index, finite StreamName window, RegSeqRat readback, DyadicRatCore tolerance ledger, RealUp seal handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-regularity-witness-carrier}, \autoref{thm:regular-cauchy-regularity-witness-namecert-obligations}, \autoref{thm:regular-cauchy-regularity-witness-diagonal-consumer-boundary}, \autoref{thm:regular-cauchy-regularity-witness-no-hidden-limit}, \autoref{thm:regular-cauchy-regularity-witness-obligation-closure}, and \autoref{thm:regular-cauchy-regularity-witness-scoped-kernel-route} bind the diagonal-regularity witness packet as a finite Real-completion handoff whose consumer-facing route is scoped by the displayed StreamName, RegSeqRat, DyadicRatCore, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected host limits, host real equality, quotient-stream equality, Quot.sound, Classical.choice, or ambient Real completeness.}
  \upgradepath{Lean verification requires a checked RegularCauchyRegularityWitnessUp theorem for the scoped consumer-facing route and its sibling dependency boundary.}
\end{closurestatus}
