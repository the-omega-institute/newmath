\chapter{A Concrete Naming Certificate for $\BaireMetricUp$}
\label{ch:concrete-instances-bairemetric-namecert}
\origin{human}

Following Brouwer's finite-prefix presentation of choice sequences, the Bishop--Bridges metric treatment of countable product spaces, and Kelley's prefix topology, the $\BaireMetricUp$ packet records the standard prefix ultrametric on $\BaireSpaceUp$ as finite BEDC rows. It sits between the prefix carrier of \autoref{ch:concrete-instances-bairespace-namecert}, the metric parent surface of \autoref{ch:concrete-instances-metric-namecert}, and the strong-triangle interface of \autoref{ch:concrete-instances-ultrametricspace-namecert}. The packet names only displayed first-disagreement windows, dyadic radius comparisons, stream-name schedules, transport, replay, provenance, and a local certificate; it does not import completed streams, quotient equality of infinite names, real-valued host metrics, or external topology.

\begin{definition}[BaireMetric prefix-distance carrier]
\label{def:bairemetric-prefix-distance-carrier}
A $\BaireMetricUp$ carrier is a finite $\BHist$ packet
$$
M=(B,W,D,R,U,S,H,C,P,N)
$$
over $\BaireSpaceUp$, $\NatUp$, $\DyadicRatCoreUp$, $\MetricSpaceUp$, $\UltrametricSpaceUp$, $\StreamNameUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ with the following displayed rows:
\begin{enumerate}
\item $B$ is the accepted $\BaireSpaceUp$ prefix packet whose finite stream windows supply the two names under comparison.
\item $W$ is the $\NatUp$ first-disagreement window. It records the inspected prefix length, the equality rows below that length, and the first displayed coordinate where the two prefix windows separate, when such a coordinate is present.
\item $D$ is the dyadic radius row. It records the radius $2^{-k}$ attached to the first displayed disagreement at $k$, together with the zero-radius branch for displayed equality across the inspected window.
\item $R$ is the parent $\MetricSpaceUp$ row that reads the displayed point names, zero-distance boundary, symmetry row, and ordinary triangle route from $B,W,D$.
\item $U$ is the $\UltrametricSpaceUp$ witness whose strong max-radius comparison is read from the same first-disagreement window and dyadic radius row.
\item $S$ is the $\StreamNameUp$ schedule controlling which finite prefixes of the two Baire names may be inspected by a consumer.
\item $H$ records componentwise $\hsame$ transport for the BaireSpace row, Nat window, dyadic radius row, parent metric row, ultrametric witness, stream schedule, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a requested prefix comparison through $S$, then through $B,W,D$, and only then into the parent metric row $R$ and ultrametric witness $U$.
\item $P$ is the $\Pkg$ provenance over the listed BEDC dependencies.
\item $N$ is the local $\NameCert_{\BaireMetricUp}$ row.
\end{enumerate}
The classifier accepts the packet only when every distance comparison is read through the displayed first-disagreement window and dyadic radius row. It has no coordinate for a completed infinite stream, a quotient of Baire names, a host real-valued distance function, countable choice, or an external topological space.
\end{definition}

\begin{theorem}[BaireMetric NameCert obligations]
\label{thm:bairemetric-namecert-obligations}
For every accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$, the local $\NameCert_{\BaireMetricUp}$ surface consists exactly of the following five rows:
\begin{enumerate}
\item carrier admission for the displayed BaireSpace prefix packet, Nat first-disagreement window, dyadic radius row, parent MetricSpace row, UltrametricSpace witness, StreamName schedule, transport, replay, provenance, and local naming rows;
\item prefix-distance admission, requiring every positive radius claim to name the first displayed disagreement in $W$ and every zero-radius claim to remain inside the inspected equality window;
\item metric compatibility, requiring the parent $\MetricSpaceUp$ row $R$ to read zero-distance, symmetry, and ordinary triangle evidence only from $B,W,D$;
\item ultrametric compatibility, requiring the witness $U$ to read its strong max-radius comparison from the same first-disagreement and dyadic-radius rows;
\item ledger refusal, excluding completed streams, quotient equality of Baire names, host real-valued metrics, countable-choice selectors, and external topology.
\end{enumerate}
No obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bairemetric-prefix-distance-carrier}. The tuple lists exactly the BaireSpace prefix packet, first-disagreement window, dyadic radius row, parent metric row, ultrametric witness, stream schedule, transport, continuation, provenance, and local name.

Prefix-distance admission is read from $W$ and $D$. A positive comparison names the least displayed coordinate where the two scheduled prefixes disagree, while the zero-radius branch records only equality across the inspected finite window.

Metric compatibility is then replayed through $R$. The zero-distance boundary, symmetry row, and ordinary triangle route do not inspect a separate distance function; they use the same prefix windows and dyadic radius comparisons already displayed by $B,W,D$.

Ultrametric compatibility is read from $U$. Since the controlling radius is determined by the earliest displayed disagreement, the strong max-radius comparison factors through the same window and cannot bypass the dyadic ledger.

The structural rows $H,C,P,N$ transport, replay, source, and name only those coordinates. Hence no completed stream, quotient equality of infinite names, host real metric, countable-choice selector, or external topological datum can appear as a NameCert obligation.
\end{proof}

\begin{theorem}[BaireMetric root observation carrier]
\label{thm:bairemetric-root-observation-carrier}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ root packet. Every root observation of $M$ is carried by finite $\BHist$ prefix windows: the consumer first reads the $\StreamNameUp$ schedule $S$, then the displayed prefix packet $B$, the finite first-disagreement window $W$, and the $\DyadicRatCoreUp$ radius row $D$, before any $\MetricSpaceUp$ row $R$, $\UltrametricSpaceUp$ row $U$, $\hsame$ transport $H$, $\Cont$ replay $C$, provenance row $P$, or local $\NameCert$ row $N$ is available. No root observation asserts equality of completed streams.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:bairemetric-prefix-distance-carrier}. The only observation source is the finite prefix surface $S,B,W,D$, with $W$ recording inspected $\BHist$ prefix data and the displayed first-disagreement branch.

The parent $\MetricSpaceUp$ and $\UltrametricSpaceUp$ rows are downstream of that prefix surface. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route, so a root observation has no coordinate from which to read completed-stream equality.
\end{proof}

\begin{theorem}[BaireMetric ultrametric window stability]
\label{thm:bairemetric-ultrametric-window-stability}
For an accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$, extending a scheduled finite $\BHist$ prefix window through the same $\StreamNameUp$ row preserves the ultrametric comparison route. The $\DyadicRatCoreUp$ radius row remains read from the displayed first-disagreement window, the parent $\MetricSpaceUp$ row $R$ and $\UltrametricSpaceUp$ row $U$ consume that same radius data, and $\hsame$ transport with $\Cont$ replay preserves the local $\NameCert$ surface. The extension never supplies a completed-stream equality claim.
\end{theorem}
\begin{proof}
Start with a scheduled prefix read. By \autoref{thm:bairemetric-root-observation-carrier}, the read exposes only the finite window route $S,B,W,D$ before the parent metric and ultrametric rows are consumed.

When the window is extended through the same schedule, the displayed first-disagreement branch is either preserved or moved to a later inspected coordinate. In both cases the dyadic radius is still read from $D$ and from no other source.

The rows $R$ and $U$ consume that radius after the prefix window has been displayed. Componentwise $\hsame$ transport and $\Cont$ replay keep the route on the same carrier coordinates, while the local $\NameCert$ row names only that finite surface.
\end{proof}

\begin{theorem}[BaireMetric NameCert obligation surface]
\label{thm:bairemetric-namecert-obligation-surface}
Every root-level $\NameCert_{\BaireMetricUp}$ obligation for an accepted packet $M=(B,W,D,R,U,S,H,C,P,N)$ is exhausted by finite $\BHist$ prefix-window admission, $\StreamNameUp$ scheduling, $\DyadicRatCoreUp$ radius exactness, parent $\MetricSpaceUp$ compatibility, $\UltrametricSpaceUp$ strong-triangle compatibility, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No additional obligation asserts completed-stream equality, quotient equality of Baire names, a host real-valued metric, or external topology.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:bairemetric-namecert-obligations}. The carrier admission, prefix-distance admission, metric compatibility, ultrametric compatibility, and ledger refusal rows list the entire obligation surface.

Use \autoref{thm:bairemetric-root-observation-carrier} to identify the root observation source with finite $\BHist$ prefix windows scheduled by $\StreamNameUp$ and measured by the $\DyadicRatCoreUp$ row. Then use \autoref{thm:bairemetric-ultrametric-window-stability} to keep parent metric and ultrametric consumers on the same finite route under window extension.

The remaining rows are structural: $\hsame$ transports the displayed coordinates, $\Cont$ replays them, $\Pkg$ records provenance, and the local $\NameCert$ row names them. Since the carrier has no other coordinate, completed-stream equality and external metric data are outside the obligation surface.
\end{proof}

\begin{theorem}[BaireMetric prefix-window admission]
\label{thm:bairemetric-prefix-window-admission}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. Every accepted distance read admits a finite prefix window: before the dyadic radius row is inspected, the continuation row displays a $\StreamNameUp$ schedule $S$, an accepted $\BaireSpaceUp$ prefix packet $B$, and a $\NatUp$ window $W$ that records the inspected length and the equality rows below the first displayed separation.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bairemetric-prefix-distance-carrier}. The continuation row $C$ routes every comparison through the stream schedule $S$ and the BaireSpace prefix packet $B$ before reaching any radius coordinate.

The window row $W$ is the only Nat-valued inspection coordinate in the carrier. Thus the accepted comparison must name the inspected finite length and the displayed equality rows below any first disagreement before $D,R,$ or $U$ can be read.
\end{proof}

\begin{theorem}[BaireMetric ultrametric window obligations]
\label{thm:bairemetric-ultrametric-window-obligations}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. The ultrametric strong-triangle obligation is admitted only after the finite $\StreamNameUp$ schedule $S$ has exposed the prefix packet $B$ and the first-disagreement window $W$ before dyadic radius inspection. The witness $U$ must read the same $W,D$ branch as the parent metric row $R$, so its max-radius comparison cannot be replaced by completed-stream equality, quotient names, or an external topology.
\end{theorem}
\begin{proof}
Use \autoref{thm:bairemetric-prefix-window-admission}. Every accepted comparison first exposes the schedule, prefix packet, and Nat window, so the dyadic row is not available before a finite first-disagreement read.

Apply \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. The row $D$ is exact for the branch named by $W$, including the zero branch where the read remains inside finite inspected equality.

Finally apply \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff}. The ultrametric row $U$ is downstream of $S,B,W,D,R$ and therefore reads the same finite radius data as $R$.
\end{proof}

\begin{theorem}[BaireMetric complete-metric handoff]
\label{thm:bairemetric-complete-metric-handoff}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. A $\CompleteMetricUp$ consumer can read $M$ only as the finite prefix-window surface
$$
S,B,W,D,R,U,H,C,P,N .
$$
The handoff exposes scheduled prefix agreement, first-disagreement radii, parent metric compatibility, and ultrametric strong-triangle data, but it does not expose a completed stream, quotient of Baire names, host real-valued metric, or countable-choice selector.
\end{theorem}
\begin{proof}
Start with the finite schedule supplied by \autoref{thm:bairemetric-prefix-window-admission}. The consumer receives $S,B,W$ before any dyadic or metric conclusion is public.

Use \autoref{thm:bairemetric-parent-metricspace-handoff} and \autoref{thm:bairemetric-ultrametric-window-obligations}. These route $R$ and $U$ through the same displayed first-disagreement and dyadic-radius rows.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly that route. Since the carrier has no coordinate outside the row family displayed in the statement, the complete-metric handoff remains finite prefix-window data.
\end{proof}

\begin{theorem}[BaireMetric StreamName NameCert scope]
\label{thm:bairemetric-streamname-namecert-scope}
For every accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$, the $\StreamNameUp$ row $S$ controls the local $\NameCert_{\BaireMetricUp}$ scope: every distance, ultrametric, or complete-metric read must pass through $S$ before $B,W,D,R,U,H,C,P,N$ are inspected. Thus the certificate scope is a scheduled finite-prefix surface, not an equality theory of completed Baire names.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bairemetric-prefix-distance-carrier}. The schedule row is the only coordinate authorizing finite prefix inspection by a consumer.

Apply \autoref{thm:bairemetric-stream-schedule-nonescape}. It blocks every distance, metric, and ultrametric conclusion that tries to bypass the displayed schedule.

Then use \autoref{thm:bairemetric-complete-metric-handoff}. The complete-metric consumer receives only the scheduled finite row family, so the local NameCert scope contains no completed-stream equality route.
\end{proof}

\begin{theorem}[BaireMetric first-disagreement dyadic exactness]
\label{thm:bairemetric-first-disagreement-dyadic-radius-exactness}
For an accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$, the dyadic radius row $D$ is exact for the displayed first-disagreement window. If $W$ records first disagreement at $k$, then $D$ records the radius branch $2^{-k}$; if $W$ records only displayed equality across the inspected window, then $D$ records the zero-radius branch for that finite read. No other radius source is available to the packet.
\end{theorem}
\begin{proof}
The carrier admits dyadic radius data only through the row $D$. The row is downstream of $S,B,W$, so its positive branch is read from the first displayed disagreement already named by $W$.

In the equality branch, $W$ exposes only the inspected finite equality window. The corresponding radius branch in $D$ is therefore the displayed zero-radius read for that window, not a claim about completed infinite stream equality.

Because $R$ and $U$ are downstream consumers of $D$, neither can introduce an independent radius value. Exactness is consequently a projection of the single dyadic row attached to the first-disagreement window.
\end{proof}

\begin{theorem}[BaireMetric prefix radius monotonicity]
\label{thm:bairemetric-prefix-radius-monotonicity}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. Extending the displayed inspected prefix through the same $\StreamNameUp$ schedule can only preserve the dyadic radius branch already read from $W,D$ or move it to a smaller displayed dyadic branch. The extension cannot replace the first-disagreement route by a larger radius, a host metric, or a completed-stream comparison.
\end{theorem}
\begin{proof}
Use carrier admission from \autoref{def:bairemetric-prefix-distance-carrier} and the NameCert obligations of \autoref{thm:bairemetric-namecert-obligations}. Every distance read is routed through the scheduled prefix packet $S,B$ and the finite window $W$ before the dyadic row $D$ is inspected.

By prefix-window admission in \autoref{thm:bairemetric-prefix-window-admission}, an extended read only adds inspected coordinates after the displayed window already exposed. If the earlier first disagreement remains visible, first-disagreement exactness in \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness} keeps the same $2^{-k}$ branch.

If the extension exposes a later first displayed disagreement after an equality branch, the dyadic branch read from $D$ moves to that later index and is therefore no larger than the previous displayed prefix bound. Since $R$ and $U$ consume the same $D$ row, no downstream metric or ultrametric consumer can enlarge the radius.
\end{proof}

\begin{theorem}[BaireMetric root ultrametric cylinder carrier]
\label{thm:bairemetric-root-ultrametric-cylinder-carrier}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ root carrier. The root cylinder surface of $M$ is the finite $\BHist$ route in which the $\StreamNameUp$ schedule $S$ reads the prefix packet $B$, the first-disagreement window $W$ reads that scheduled prefix, the $\DyadicRatCoreUp$ radius row $D$ reads $W$, the parent $\MetricSpaceUp$ row $R$ reads $D$, and the $\UltrametricSpaceUp$ row $U$ reads the same radius before the structural rows $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ are exposed through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bairemetric-prefix-distance-carrier}. The tuple $M=(B,W,D,R,U,S,H,C,P,N)$ lists the prefix packet, finite window, dyadic radius, metric handoff, ultrametric handoff, stream schedule, transport, replay, provenance, and local naming rows as the only available coordinates.

Use prefix-window admission and prefix radius monotonicity. Together they force every root cylinder read to pass through $S,B,W,D$ before $R$ or $U$ can be consumed, and they keep every extension of the cylinder inside the same finite prefix schedule.

Finally read the structural rows. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row preserve the same order, so no root cylinder carrier can replace the displayed $\BHist$ packet by completed streams, quotient names, or a host metric.
\end{proof}

\begin{theorem}[BaireMetric root complete-metric nonescape]
\label{thm:bairemetric-root-complete-metric-nonescape}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ root carrier. Any $\CompleteMetricUp$-facing read from $M$ must remain inside the displayed $\BHist$ route
$$
S,B,W,D,R,U,H,C,P,N .
$$
It reads $\StreamNameUp$, $\DyadicRatCoreUp$, $\MetricSpaceUp$, $\UltrametricSpaceUp$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows from that tuple, and it has no coordinate for completed Baire streams, host completeness, quotient equality, or countable-choice selectors.
\end{theorem}
\begin{proof}
Start from a complete-metric handoff request. Carrier projection leaves only the tuple $M=(B,W,D,R,U,S,H,C,P,N)$, so every requested metric or completeness-facing row must be replayed through those displayed coordinates.

Apply \autoref{thm:bairemetric-root-ultrametric-cylinder-carrier}. It routes the request through the $\StreamNameUp$ schedule, finite prefix packet, first-disagreement window, $\DyadicRatCoreUp$ radius, parent $\MetricSpaceUp$ handoff, and $\UltrametricSpaceUp$ strong-triangle row before any structural row is exported.

The structural rows $H,C,P,N$ then preserve and name the same finite $\BHist$ route through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Since no coordinate outside the tuple is available, the complete-metric consumer cannot escape to completed streams, host completeness, quotient equality, or countable choice.
\end{proof}

\begin{theorem}[BaireMetric root StreamName prefix route]
\label{thm:bairemetric-root-streamname-prefix-route}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ root carrier. Every root prefix read begins with the $\StreamNameUp$ schedule $S$ and then consumes $B,W,D,R,U,H,C,P,N$ in the displayed $\BHist$ route. In particular, the $\DyadicRatCoreUp$, $\MetricSpaceUp$, $\UltrametricSpaceUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows are downstream of that scheduled prefix read, not independent root data.
\end{theorem}
\begin{proof}
Read the prefix request through the tuple $M=(B,W,D,R,U,S,H,C,P,N)$. The schedule row $S$ is the only row that can select finite prefixes before the Baire prefix packet $B$ and first-disagreement window $W$ are inspected.

Use \autoref{thm:bairemetric-prefix-window-admission} and \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. They place $D$ after the scheduled window and ensure that each radius branch is read from the displayed finite prefix evidence.

Then apply \autoref{thm:bairemetric-root-ultrametric-cylinder-carrier}. The parent $\MetricSpaceUp$ row $R$, $\UltrametricSpaceUp$ row $U$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row are all downstream of the scheduled prefix route, so none can introduce an unscheduled prefix comparison.
\end{proof}

\begin{theorem}[BaireMetric zero-radius branch confinement]
\label{thm:bairemetric-zero-radius-branch}
For an accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$, the zero-radius branch of $D$ is confined to displayed finite inspected-window equality. It is never a quotient equality of completed Baire streams, a global extensional equality theorem, or an ambient topological identification.
\end{theorem}
\begin{proof}
Project the zero branch through the carrier of \autoref{def:bairemetric-prefix-distance-carrier}. The only equality evidence before $D$ is the displayed finite equality row in $W$, scheduled by $S$ and admitted through $B$.

Apply the NameCert obligations of \autoref{thm:bairemetric-namecert-obligations}. The prefix-distance row requires every zero-radius read to remain inside the inspected equality window, and the ledger-refusal row excludes completed streams, quotient equality of Baire names, host real metrics, and external topology.

Finally use \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. Its equality branch identifies the zero-radius read with that finite window alone. Transport by $H$ and replay by $C$ preserve the same row family and cannot turn finite inspected equality into equality of completed streams.
\end{proof}

\begin{theorem}[BaireMetric prefix radius transport]
\label{thm:bairemetric-prefix-radius-transport}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ and $M'=(B',W',D',R',U',S',H',C',P',N')$ be accepted $\BaireMetricUp$ carriers. If componentwise $\hsame$ transports the displayed Baire prefix packets, stream schedules, first-disagreement windows, dyadic rows, parent metric rows, ultrametric rows, provenance, and local names, and the displayed $\Cont$ replay preserves the prefix-read order, then $M'$ exposes the same route
$$
S,B,W,D,R,U
$$
as $M$ for every accepted prefix-radius read.
\end{theorem}
\begin{proof}
Start from the accepted read in $M$. Prefix-window admission supplies the schedule, prefix packet, first-disagreement window, and dyadic row before any parent metric or ultrametric row is consumed.

Transport each displayed coordinate through the componentwise $\hsame$ rows recorded in $H$ and $H'$. The carrier definition gives matching coordinates $S',B',W',D',R',U'$ and the NameCert obligations require those coordinates to remain the only distance, metric, and ultrametric sources.

Replay the transported read through $C'$. First-disagreement exactness then reads $D'$ from the transported $W'$ branch, while the parent handoff and ultrametric handoff consume $R'$ and $U'$ downstream of that same branch. Thus the transported packet preserves the route and admits no alternate radius source.
\end{proof}

\begin{theorem}[BaireMetric parent MetricSpace handoff]
\label{thm:bairemetric-parent-metricspace-handoff}
Every parent $\MetricSpaceUp$ read exported by an accepted $\BaireMetricUp$ packet factors through
$$
S,B,W,D \longmapsto R .
$$
The zero-distance boundary, symmetry row, and ordinary triangle route in $R$ may read only the scheduled prefix packet, first-disagreement window, and dyadic radius row.
\end{theorem}
\begin{proof}
Start with a parent metric read. Carrier admission exposes the parent row only as $R$, and the definition places $R$ downstream of the scheduled prefix and dyadic comparison rows.

The zero-distance, symmetry, and ordinary triangle components of $R$ therefore receive their data from $S,B,W,D$. Componentwise transport by $H$ and replay by $C$ preserve this order, so the handoff cannot inspect a host metric on completed streams.
\end{proof}

\begin{theorem}[BaireMetric UltrametricSpace strong-triangle handoff]
\label{thm:bairemetric-ultrametricspace-strong-triangle-handoff}
Every $\UltrametricSpaceUp$ strong-triangle read exported by an accepted $\BaireMetricUp$ carrier factors through the same first-disagreement and dyadic-radius rows used by the parent metric handoff:
$$
S,B,W,D,R \longmapsto U .
$$
The strong max-radius comparison is a finite prefix-window comparison, not a separate topology or completed-stream theorem.
\end{theorem}
\begin{proof}
The ultrametric witness appears in the carrier only as $U$. It is listed after the scheduled prefix packet, first-disagreement window, dyadic radius row, and parent metric row.

Thus a strong-triangle read must first replay the finite prefix comparisons through $S,B,W,D$ and the parent handoff $R$. The row $U$ then packages the max-radius comparison over those displayed radii, with no coordinate for external topology or completed Baire names.
\end{proof}

\begin{theorem}[BaireMetric stream-schedule nonescape]
\label{thm:bairemetric-stream-schedule-nonescape}
For every accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$, a consumer cannot read a distance, metric, or ultrametric conclusion outside the displayed $\StreamNameUp$ schedule. Every such conclusion is replayed by $C$ through $S$ before it reaches $B,W,D,R,$ or $U$.
\end{theorem}
\begin{proof}
The stream schedule $S$ is the carrier coordinate controlling which finite prefixes may be inspected. The continuation row $C$ names the replay order from a requested comparison through $S$ before any BaireSpace, window, radius, metric, or ultrametric row is consumed.

Transport, provenance, and local naming preserve that replay order. Consequently a consumer has no slot from which to read an unscheduled prefix, completed stream, quotient equality of names, or external topological neighborhood.
\end{proof}

\begin{theorem}[BaireMetric ultrametric prefix route]
\label{thm:bairemetric-ultrametric-prefix-route}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ packet. Every accepted prefix-distance comparison exported by $M$ factors through the displayed route
$$
B,S,W,D,R,U .
$$
Equivalently, a consumer reads first the $\BaireSpaceUp$ prefix packet, then the $\StreamNameUp$ schedule, the $\NatUp$ first-disagreement window, the $\DyadicRatCoreUp$ radius row, the parent $\MetricSpaceUp$ row, and finally the $\UltrametricSpaceUp$ strong-triangle witness. The route is a finite BEDC replay over prefix rows, not a host metric on completed streams.
\end{theorem}
\begin{proof}
Start with an accepted prefix-distance comparison exported by $M$. Carrier admission exposes the comparison only through the coordinates $B,S,W,D,R,U$ together with the structural rows.

The continuation row $C$ first schedules finite prefix reads through $S$ and then reads the accepted $\BaireSpaceUp$ packet $B$. The $\NatUp$ window $W$ records how far the comparison has inspected the two prefixes and, when the positive branch is taken, which displayed coordinate is the first disagreement.

The dyadic radius row $D$ converts that window into the displayed radius comparison. The parent $\MetricSpaceUp$ row $R$ receives only this radius evidence when supplying zero-distance, symmetry, and ordinary triangle data.

Finally the $\UltrametricSpaceUp$ witness $U$ reads the same first-disagreement and radius rows to supply the strong max-radius route. Componentwise transport by $H$ and provenance by $P$ preserve this order, so no consumer-facing path bypasses the displayed BaireSpace, Nat, StreamName, DyadicRatCore, MetricSpace, and UltrametricSpace rows.
\end{proof}

\begin{theorem}[BaireMetric complete-ultrametric consumer readiness]
\label{thm:bairemetric-complete-ultrametric-consumer-readiness}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ packet. Every $\CompleteMetricUp$, $\UltrametricSpaceUp$, or $\BaireSpaceUp$ consumer of $M$ must first read the displayed route
$$
S,B,W,D,R,U,H,C,P,N .
$$
The route schedules $\StreamNameUp$ reads before $\BaireSpaceUp$ prefix rows, reads the $\NatUp$ first-disagreement window before the $\DyadicRatCoreUp$ radius row, reads the parent $\MetricSpaceUp$ row before the $\UltrametricSpaceUp$ strong-triangle row, and reads transport, replay, provenance, and local naming before exporting any consumer-facing conclusion.
\end{theorem}
\begin{proof}
Apply \autoref{thm:bairemetric-prefix-window-admission}. It places the $\StreamNameUp$ schedule $S$ before the $\BaireSpaceUp$ prefix packet $B$ and the $\NatUp$ first-disagreement window $W$ for every accepted distance read.

Apply \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. The dyadic radius row $D$ is read only after the displayed first-disagreement window, so a downstream consumer cannot obtain a radius without the finite prefix-window evidence.

Apply \autoref{thm:bairemetric-parent-metricspace-handoff} and \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff}. They order the parent $\MetricSpaceUp$ row $R$ before the $\UltrametricSpaceUp$ strong-triangle row $U$, with both rows using the same scheduled prefix, window, and radius data.

Finally apply \autoref{thm:bairemetric-ultrametric-prefix-route}. It records that the consumer-facing prefix-distance route is finite and that componentwise transport $H$, replay $C$, provenance $P$, and local naming $N$ preserve the same order before any $\CompleteMetricUp$, $\UltrametricSpaceUp$, or $\BaireSpaceUp$ conclusion is exported.
\end{proof}

\begin{theorem}[BaireMetric root prefix schedule]
\label{thm:bairemetric-root-prefix-schedule}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ root packet. The first read of the root packet exposes the finite prefix schedule
$$
S,B,W,D .
$$
Thus the consumer first reads the $\StreamNameUp$ schedule row $S$, the $\BaireSpaceUp$ prefix packet $B$, the $\NatUp$ first-disagreement window $W$, and the $\DyadicRatCoreUp$ radius row $D$ before it may inspect the parent metric row, the strong-triangle row, transport, replay, provenance, or local naming.
\end{theorem}
\begin{proof}
Apply \autoref{thm:bairemetric-prefix-window-admission}. It places every accepted distance read behind the $\StreamNameUp$ schedule, the $\BaireSpaceUp$ prefix packet, and the finite first-disagreement window.

Apply \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. The dyadic radius row is determined only after that window is available, including the zero branch where the inspected prefixes remain equal on the displayed finite window.

Finally use \autoref{thm:bairemetric-complete-ultrametric-consumer-readiness}. The complete-ultrametric route keeps the same prefix schedule as the initial root read and admits no earlier access to the metric, ultrametric, transport, replay, provenance, or local naming rows.
\end{proof}

\begin{theorem}[BaireMetric root strong-triangle route]
\label{thm:bairemetric-root-strong-triangle-route}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ root packet. Every $\UltrametricSpaceUp$ consumer of the strong-triangle row $U$ must first read the same finite prefix route
$$
S,B,W,D,R .
$$
Only after this route has supplied the stream schedule, Baire prefix packet, Nat first-disagreement window, dyadic radius row, and parent metric row may the consumer read $U$ as the strong-triangle witness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:bairemetric-root-prefix-schedule}. It fixes the initial root read as the finite $S,B,W,D$ schedule, so the strong-triangle consumer cannot start from completed streams, quotient names, or an external topology.

Next apply \autoref{thm:bairemetric-parent-metricspace-handoff}. The parent metric row $R$ receives exactly the scheduled prefix, first-disagreement, and dyadic-radius data before exporting ordinary metric compatibility.

Finally apply \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff}. The strong-triangle row $U$ reads the same finite prefix and radius route after $R$, so the ultrametric consumer cannot replace the displayed root schedule by a separate strong-triangle path.
\end{proof}

\begin{theorem}[BaireMetric root prefix ultrametric package]
\label{thm:bairemetric-root-prefix-ultrametric-package}
Every accepted $\BaireMetricUp$ root package exposes exactly the route through $\BaireSpaceUp$, $\StreamNameUp$, $\NatUp$, $\DyadicRatCoreUp$, $\MetricSpaceUp$, and $\UltrametricSpaceUp$, together with the structural rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, before any PolishSpace, BaireSpace, or ultrametric consumer reads it. In the notation of \autoref{def:bairemetric-prefix-distance-carrier}, the package surface is the finite row family
$$
B,S,W,D,R,U,H,C,P,N .
$$
No consumer-facing read may replace this row family by completed streams, quotient equality of names, host real metrics, countable choice, or external topology.
\end{theorem}
\begin{proof}
Apply \autoref{thm:bairemetric-prefix-window-admission}. It forces every distance read to expose the $\StreamNameUp$ schedule $S$, the $\BaireSpaceUp$ prefix packet $B$, and the $\NatUp$ first-disagreement window $W$ before any radius or metric row is inspected.

Apply \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. The dyadic radius row $D$ is then determined by the displayed first-disagreement window, with the zero-radius branch remaining inside the inspected finite equality window.

Apply \autoref{thm:bairemetric-parent-metricspace-handoff} and \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff}. The parent $\MetricSpaceUp$ row $R$ and the $\UltrametricSpaceUp$ row $U$ both read the same scheduled prefix, window, and dyadic-radius data.

Finally use \autoref{thm:bairemetric-stream-schedule-nonescape} and \autoref{thm:bairemetric-ultrametric-prefix-route}. They place every metric, ultrametric, PolishSpace, BaireSpace, or downstream prefix consumer behind the same replay row $C$, with componentwise transport $H$, provenance $P$, and local naming $N$. Since the carrier has no coordinate for completed streams, quotient equality of names, host real metrics, countable choice, or external topology, the displayed root package is the whole exported surface.
\end{proof}

\begin{theorem}[BaireMetric StreamName ultrametric obligation]
\label{thm:bairemetric-streamname-ultrametric-obligation}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. Every consumer that reads a complete-ultrametric or $\BaireSpaceUp$ conclusion from $M$ must first consume the finite $\StreamNameUp$ schedule $S$, the $\BaireSpaceUp$ prefix packet $B$, the $\NatUp$ first-disagreement window $W$, the $\DyadicRatCoreUp$ radius row $D$, the parent $\MetricSpaceUp$ row $R$, the $\UltrametricSpaceUp$ strong-triangle witness $U$, and the structural rows $H,C,P,N$ for $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. Consequently the obligation surface is the finite route
$$
S,B,W,D,R,U,H,C,P,N ,
$$
and it exports no completed streams, quotient names, host real metric, countable choice, or external topology.
\end{theorem}
\begin{proof}
Apply \autoref{thm:bairemetric-prefix-window-admission}. It places every distance read behind the displayed $\StreamNameUp$ schedule, the $\BaireSpaceUp$ prefix packet, and the finite first-disagreement window.

Apply \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. The dyadic radius row is therefore read from the finite window, with the zero branch remaining inside inspected prefix equality.

Next use \autoref{thm:bairemetric-parent-metricspace-handoff} and \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff}. The parent metric row and the strong-triangle witness consume the same schedule, prefix packet, window, and dyadic radius data.

Finally apply \autoref{thm:bairemetric-root-prefix-ultrametric-package}. It packages exactly the displayed route with componentwise transport, replay, provenance, and local naming. Since no carrier coordinate supplies completed streams, quotient names, host real metrics, countable choice, or external topology, no complete-ultrametric or BaireSpace consumer can read such data from $M$.
\end{proof}

\begin{theorem}[BaireMetric complete-ultrametric consumer obligations]
\label{thm:bairemetric-complete-ultrametric-consumer-obligations}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. Every complete ultrametric consumer of $M$ must display the $\BaireSpaceUp$ prefix packet $B$, $\StreamNameUp$ schedule $S$, $\NatUp$ first-disagreement window $W$, $\DyadicRatCoreUp$ radius row $D$, parent $\MetricSpaceUp$ row $R$, $\UltrametricSpaceUp$ strong-triangle row $U$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$ before it may read a $\CompleteMetricUp$ conclusion. No complete-ultrametric obligation is stored outside this row family.
\end{theorem}
\begin{proof}
Project the carrier. The rows $B,W,D,R,U,S,H,C,P,N$ are the only displayed coordinates available to a complete-ultrametric consumer.

Apply \autoref{thm:bairemetric-prefix-window-admission} and \autoref{thm:bairemetric-first-disagreement-dyadic-radius-exactness}. They force every distance read through the stream schedule, Baire prefix packet, finite Nat window, and dyadic radius row.

Apply \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff} and \autoref{thm:bairemetric-streamname-ultrametric-obligation}. The first theorem orders the strong-triangle row after the same prefix and radius data, while the second packages the complete-ultrametric consumer surface with the structural transport, replay, provenance, and local naming rows.
\end{proof}

\begin{theorem}[BaireMetric Cauchy-window completeness handoff]
\label{thm:bairemetric-cauchy-window-completeness-handoff}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. Any $\CompleteMetricUp$ handoff read from $M$ receives its Cauchy-window evidence only through the finite prefix route
$$
S,B,W,D,R,U,H,C,P,N .
$$
Thus completeness consumers may replay finite prefix agreement and dyadic-radius decay, but they cannot read completed streams, quotient names, host real metrics, or countable-choice selectors from the carrier.
\end{theorem}
\begin{proof}
Start from a handoff request to $\CompleteMetricUp$. Carrier projection leaves only the displayed prefix, window, radius, parent metric, ultrametric, and structural rows.

Use \autoref{thm:bairemetric-root-prefix-schedule}. It fixes the initial Cauchy-window read as the finite $S,B,W,D$ route, so the handoff cannot begin from a completed stream.

Next apply \autoref{thm:bairemetric-parent-metricspace-handoff} and \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff}. They pass the same window and radius data through $R$ and $U$ before any completeness-facing metric conclusion is exposed.

Finally use \autoref{thm:bairemetric-complete-ultrametric-consumer-obligations}. The structural rows $H,C,P,N$ replay and name exactly that route, so the handoff remains a finite Cauchy-window obligation.
\end{proof}

\begin{theorem}[BaireMetric BaireSpace prefix-density route]
\label{thm:bairemetric-bairespace-prefix-density-route}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. Every $\BaireSpaceUp$ prefix-density consumer of the complete ultrametric surface must pass through the same finite prefix schedule $S,B,W,D$ before reading $R,U,H,C,P,N$. The route is therefore density over displayed finite prefixes, not a density theorem for completed Baire names.
\end{theorem}
\begin{proof}
Project $M$ and read the $\BaireSpaceUp$ row $B$ together with the $\StreamNameUp$ schedule $S$. These rows are the only prefix source offered by the carrier.

Apply \autoref{thm:bairemetric-prefix-window-admission}. It places all Baire prefix reads behind the finite schedule and first-disagreement window, with no quotient equality of names available.

Apply \autoref{thm:bairemetric-cauchy-window-completeness-handoff}. The completeness-facing route uses the same $S,B,W,D,R,U,H,C,P,N$ row family, so a prefix-density consumer cannot bypass the finite window when reading the complete ultrametric surface.
\end{proof}

\begin{theorem}[BaireMetric prefix-cylinder coverage]
\label{thm:bairemetric-prefix-cylinder-coverage}
Let $M=(B,W,D,R,U,S,H,C,P,N)$ be an accepted $\BaireMetricUp$ carrier. Every prefix-cylinder read exported by $M$ is covered by the finite route
$$
S,B,W,D .
$$
The $\StreamNameUp$ schedule selects the inspected prefix window, the $\BaireSpaceUp$ row supplies the displayed cylinder base, the $\NatUp$ first-disagreement row records the inspected length and branch, and the $\DyadicRatCoreUp$ row records the matching radius. No cylinder coverage claim may use completed streams, quotient equality of Baire names, or an external topology.
\end{theorem}
\begin{proof}
Project the carrier. The only coordinates that can name a prefix cylinder before the metric rows are $S,B,W,D$.

The schedule $S$ determines which finite prefixes are visible. The row $B$ supplies the accepted BaireSpace prefix packet, and $W$ records the finite length and first-disagreement branch inside that packet.

The radius row $D$ then reads the branch named by $W$. Since $R,U,H,C,P,N$ are downstream of this finite window route, no exported prefix-cylinder coverage can bypass $S,B,W,D$.
\end{proof}

\begin{theorem}[BaireMetric Cauchy prefix thread]
\label{thm:bairemetric-cauchy-prefix-thread}
For an accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$, every $\CompleteMetricUp$ Cauchy read determines a finite prefix thread through the same $\StreamNameUp$ schedule. At each displayed radius request, the thread reads a scheduled $\BaireSpaceUp$ prefix window, a $\NatUp$ first-disagreement or equality branch, the corresponding $\DyadicRatCoreUp$ radius row, and then the parent $\MetricSpaceUp$ and $\UltrametricSpaceUp$ rows. The thread is not a completed stream and does not use countable choice.
\end{theorem}
\begin{proof}
Start with a completeness-facing Cauchy request. By \autoref{thm:bairemetric-cauchy-window-completeness-handoff}, the request sees only the finite route $S,B,W,D,R,U,H,C,P,N$.

For each displayed radius, the schedule $S$ opens the relevant finite prefix window in $B$. The row $W$ records whether the inspected prefixes agree on that window or where the first displayed disagreement lies, and $D$ converts that finite branch into the dyadic radius read.

The parent metric row $R$ and the ultrametric row $U$ consume the same window and radius data. Transport, replay, provenance, and naming keep the finite thread attached to $M$, so the Cauchy read never becomes a selected infinite stream.
\end{proof}

\begin{theorem}[BaireMetric complete-ultrametric exhaustion]
\label{thm:bairemetric-complete-ultrametric-exhaustion}
Every complete-ultrametric obligation exported by an accepted $\BaireMetricUp$ carrier $M=(B,W,D,R,U,S,H,C,P,N)$ is exhausted by prefix-cylinder coverage, the Cauchy prefix thread, parent metric compatibility, ultrametric strong-triangle compatibility, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. There is no remaining obligation slot for a host real-valued metric, completed-stream equality, quotient Baire names, countable choice, or external topological completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:bairemetric-prefix-cylinder-coverage} to identify every cylinder read with the finite $S,B,W,D$ route. This covers the prefix side of the complete-ultrametric surface.

Use \autoref{thm:bairemetric-cauchy-prefix-thread} for the completeness-facing Cauchy reads. It keeps every displayed radius request on the same StreamName schedule, BaireSpace prefix packet, Nat window, and dyadic radius row.

Finally apply \autoref{thm:bairemetric-parent-metricspace-handoff} and \autoref{thm:bairemetric-ultrametricspace-strong-triangle-handoff}. The rows $R$ and $U$ consume exactly that finite prefix data, while $H,C,P,N$ transport, replay, source, and name it. The listed rows exhaust the carrier, so no external completeness or host metric datum remains.
\end{proof}

\falsifiablePrediction{Every accepted $\BaireMetricUp$ distance comparison factors through the $\BaireSpaceUp$ prefix packet, $\StreamNameUp$ schedule, $\NatUp$ first-disagreement window, $\DyadicRatCoreUp$ radius row, parent $\MetricSpaceUp$ row, $\UltrametricSpaceUp$ witness, componentwise transport, continuation replay, provenance, and local $\NameCert$ before any consumer reads a metric or ultrametric conclusion.}

\independenceWitness{bairespace, streamname, nat, dyadicratcore, metricspace, ultrametricspace: $\BaireMetricUp$ is not bijective to any listed sibling because it jointly carries prefix windows, first-disagreement evidence, dyadic radii, parent metric compatibility, ultrametric strong-triangle evidence, transport, replay, provenance, and local naming.}

\closureat{BaireMetricUp}{seedStr}

\begin{closurestatus}{\BaireMetricUp}
  \constructivestory{A $\BaireMetricUp$ packet is generated from a BaireSpace prefix packet, StreamName schedule, Nat first-disagreement window, DyadicRatCore radius row, parent MetricSpace row, UltrametricSpace witness, componentwise transport, continuation replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bairemetric-prefix-distance-carrier}, \autoref{thm:bairemetric-namecert-obligations}, and \autoref{thm:bairemetric-ultrametric-prefix-route} seed the finite prefix ultrametric interface on $\BaireSpaceUp$ through first-disagreement windows and dyadic radius rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed streams, quotient equality of Baire names, host real-valued metrics, countable-choice selectors, external topology, completeness, category arguments, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, prefix-distance admission, metric compatibility, ultrametric compatibility, handoff ordering, and ledger refusal for BEDC.Derived.BaireMetricUp.}
\end{closurestatus}
