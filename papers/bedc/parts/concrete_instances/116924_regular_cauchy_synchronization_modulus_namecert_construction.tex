\chapter{A Concrete Naming Certificate for $\RegularCauchySynchronizationModulusUp$}
\label{ch:concrete-instances-regular-cauchy-synchronization-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchySynchronizationModulusUp$ packet records a shared-window synchronization certificate for paired regular Cauchy reads before the pair is handed to equality, completion, or Real-facing consumers. It refines the paired-source route of \autoref{ch:concrete-instances-regular-cauchy-pair-modulus-namecert} by naming the synchronization obligation as its own finite carrier surface over the regular rational rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet synchronizes two displayed reads at one shared window; it does not identify quotient names, select limits, invoke trichotomy, use countable choice, or import ambient completeness of $\RealUp$.

\begin{definition}[Regular Cauchy synchronization modulus carrier]
\label{def:regular-cauchy-synchronization-modulus-carrier}
A $\RegularCauchySynchronizationModulusUp$ carrier is a finite $\BHist$ packet
$$
S=(R_0,R_1,W,D,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R_0$ and $R_1$ are the two $\RegSeqRatUp$ source rows whose regular rational observations are to be synchronized.
\item $W$ is the shared $\StreamNameUp$ window row at which both reads are inspected for the same requested precision.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the requested dyadic radius, the two displayed observations, and the slack cells used to compare them inside $W$.
\item $M$ is the synchronization modulus row assigning each paired precision request to the displayed window and tolerance data.
\item $E$ is the terminal $\RealUp$ seal row available to downstream equality, completion, or Real-facing consumers only after $R_0,R_1,W,D,M$ have been read.
\item $H$ records componentwise $\hsame$ transport for the two sources, the shared window, the tolerance ledger, the synchronization modulus, the Real seal, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay for the route
$$
R_0,R_1 \longmapsto W \longmapsto D \longmapsto M \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchySynchronizationModulusUp$, $\RegularCauchyPairModulusUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchySynchronizationModulusUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $R_0,R_1,W,D,M,E,H,C,P,N$. It has no coordinate for quotient equality of regular Cauchy names, a selected limit, host real equality, trichotomy, countable choice, hidden independent windows, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy synchronization modulus NameCert obligations]
\label{thm:regular-cauchy-synchronization-modulus-namecert-obligations}
For every accepted $\RegularCauchySynchronizationModulusUp$ carrier
$$
S=(R_0,R_1,W,D,M,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchySynchronizationModulusUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ source rows, shared $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance ledger, synchronization modulus row, $\RealUp$ seal, transport row, replay row, provenance row, and local naming row;
\item shared-window synchronization, requiring both source reads to use the same displayed $W$ before a paired precision answer is accepted;
\item dyadic-budget synchronization, requiring $D$ to expose the requested radius, both endpoint observations, and the slack rows used by the synchronized read;
\item modulus exactness, requiring $M$ to route each paired precision request through the displayed $W,D$ data rather than through a detached selector, a second independent window, or a countable-choice schedule;
\item Real-seal handoff, requiring $E$ to be downstream of $R_0,R_1,W,D,M$ and to export no quotient equality, selected limit, trichotomy, or ambient completeness claim;
\item structural stability, requiring $H,C,P,N$ to transport, replay, source, and name the same finite synchronization route.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-cauchy-synchronization-modulus-carrier}. The accepted packet displays exactly two regular rational sources, one shared window, one dyadic ledger, one synchronization modulus row, one Real seal, and the structural rows $H,C,P,N$.

Shared-window synchronization is read from $R_0,R_1,W$. Both source rows are inspected at the same finite window before any paired precision answer is accepted, so the packet cannot replace the displayed window by two unrelated observation schedules.

Dyadic-budget synchronization is the role of $D$. The ledger stores the requested radius, the two endpoint observations, and the slack cells for the same shared window, which prevents the synchronized read from appealing to an independent tolerance source.

The modulus exactness clause is supplied by $M$. Since $M$ is the only synchronization-modulus coordinate in the carrier, every paired precision request is routed through $W,D$ and not through quotient equality, selected limits, trichotomy, or countable choice.

Finally $E,H,C,P,N$ close the route. The Real seal is downstream of the synchronized finite packet, while transport, replay, provenance, and local naming preserve only that packet; hence the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy synchronization shared-window handoff]
\label{thm:regular-cauchy-synchronization-modulus-shared-window-handoff}
Every equality, completion, null-difference, or Real-facing consumer of an accepted $\RegularCauchySynchronizationModulusUp$ carrier receives synchronized paired-source evidence only through
$$
R_0,R_1 \longmapsto W \longmapsto D \longmapsto M \longmapsto E \longmapsto H,C,P,N .
$$
The handoff preserves the two $\RegSeqRatUp$ source rows, the shared $\StreamNameUp$ window, the $\DyadicRatCoreUp$ tolerance ledger, the synchronization modulus row, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data. It exports no quotient equality, selected limit, trichotomy, countable choice, hidden independent window, or ambient Real completeness theorem.
\end{theorem}
\begin{proof}
Begin with a consumer read of an accepted packet. By \autoref{thm:regular-cauchy-synchronization-modulus-namecert-obligations}, the read starts from the displayed rows $R_0,R_1,W,D,M,E,H,C,P,N$.

For equality or null-difference uses, the consumer first reaches the two regular rational source rows through the same shared window $W$. The dyadic ledger $D$ supplies the requested radius and slack data at that window, so no quotient equality or detached comparison coordinate is present.

For completion or Real-facing uses, the consumer must still pass through the synchronization modulus row $M$ before the terminal Real seal $E$ is visible. Thus the precision evidence is the displayed synchronization certificate rather than a selected limit or ambient completeness principle.

The structural rows $H,C,P,N$ preserve the same finite route under transport, replay, provenance, and local naming. Therefore every downstream handoff factors through the shared-window packet and receives none of the excluded escape objects.
\end{proof}

\falsifiablePrediction{Every $\RegularCauchySynchronizationModulusUp$ read factors through two RegSeqRat source rows, one shared StreamName window row, one DyadicRatCore tolerance ledger, a synchronization modulus row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{regular-cauchy-pair-modulus, regseqrat, streamname, dyadicratcore, real: $\RegularCauchySynchronizationModulusUp$ is not bijective to any listed sibling because it isolates the shared-window synchronization certificate for paired regular Cauchy reads as its own finite NameCert route.}

\closureat{RegularCauchySynchronizationModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchySynchronizationModulusUp}
  \constructivestory{A $\RegularCauchySynchronizationModulusUp$ packet is generated from two RegSeqRat source rows, one shared StreamName window, a DyadicRatCore tolerance ledger, a synchronization modulus row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-synchronization-modulus-carrier}, \autoref{thm:regular-cauchy-synchronization-modulus-namecert-obligations}, and \autoref{thm:regular-cauchy-synchronization-modulus-shared-window-handoff} seed the shared-window synchronization route for paired regular Cauchy reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality of regular Cauchy names, selected limits, host real equality, trichotomy, countable choice, hidden independent windows, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-window synchronization, dyadic-budget synchronization, modulus exactness, Real-seal handoff, and structural stability for BEDC.Derived.RegularCauchySynchronizationModulusUp.}
\end{closurestatus}
