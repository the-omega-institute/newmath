\chapter{A Concrete Naming Certificate for $\ProgrammeStrengthLedgerUp$}
\label{ch:concrete-instances-programme-strength-ledger-namecert}
\origin{ai}

The $\ProgrammeStrengthLedgerUp$ packet realizes the strength-gradient discipline of \autoref{subsec:philosophy-programme-strength-gradient} inside the auditable-generativity programme of \autoref{ch:visions-philosophy-auditable-generativity}. It sits beside the philosophy-package carrier of \autoref{ch:concrete-instances-bedc-philosophy-package-namecert}, the upgrade registry of \autoref{ch:concrete-instances-upgraderegistry-namecert}, the verification-failure roadmap of \autoref{ch:concrete-instances-verification-failure-roadmap-namecert}, the gap-and-bridge audit surface of \autoref{ch:concrete-instances-gap-failure-bridge-audit-namecert}, and the formal-constant empirical-value boundary of \autoref{ch:concrete-instances-formalconstantempiricalvalueboundary-namecert}. Those siblings expose package rows, promotion rows, failure-roadmap rows, bridge-audit rows, and empirical-boundary rows separately; this packet binds claim strength, dependency status, verification status, bridge status, refusal, provenance, transport, replay, and local naming before a claim may export a reported BEDC strength.

\begin{definition}[Programme strength ledger carrier]
\label{def:programme-strength-ledger-carrier}
A $\ProgrammeStrengthLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(Q,S,D,V,B,R,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $Q$ is the claim-site row, recording the theorem, definition, chapter, bridge target, or package row whose reported strength is being inspected.
\item $S$ is the reported-strength row, recording whether the site is a naming seed, paper certificate, machine-checked certificate, or conservative bridge.
\item $D$ is the dependency row, recording lower-layer blockers, sibling inputs, open gaps, and required upstream rows.
\item $V$ is the theory/formal status pair, recording the independent theory-closure and verification-status axes without collapsing either axis into the other.
\item $B$ is the bridge-status row, recording whether the site has no bridge, a paper bridge, or a checked bridge.
\item $R$ is the refusal and not-claimed row, recording the stronger readings that the site may not export.
\item $H$ records componentwise $\hsame$ transport for $Q,S,D,V,B,R,C,P,N$.
\item $C$ records displayed $\Cont$ replay through the claim site, reported strength, dependency, status, bridge, and refusal rows.
\item $P$ is the $\Pkg$ provenance over $\ProgrammeStrengthLedgerUp$, $\BedcPhilosophyPackageUp$, $\UpgradeRegistryUp$, $\VerificationFailureRoadmapUp$, $\GapFailureBridgeAuditUp$, $\FormalConstantEmpiricalValueBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ProgrammeStrengthLedgerUp}$ row.
\end{enumerate}
The carrier has no coordinate for promoting a seed to a certificate, treating a paper certificate as machine checked, reading an unchecked bridge as conservative, erasing a blocker row, or exporting a stronger claim after the refusal row has named it.
\end{definition}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerCarrier\_displayed\_route}

\begin{theorem}[ProgrammeStrengthLedger NameCert obligations]
\label{thm:programme-strength-ledger-namecert-obligations}
For the carrier of \autoref{def:programme-strength-ledger-carrier}, the local $\NameCert_{\ProgrammeStrengthLedgerUp}$ surface consists exactly of carrier admission for $L=(Q,S,D,V,B,R,H,C,P,N)$, claim-site exposure through $Q$, reported-strength exposure through $S$, dependency exposure through $D$, theory/formal status exposure through $V$, bridge-status exposure through $B$, refusal and not-claimed exposure through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerNameCert\_obligations}
\begin{proof}
Project the displayed $\BHist$ packet. The claim site $Q$ is not yet a strength report; it becomes reportable only after the strength row $S$, dependency row $D$, status pair $V$, bridge row $B$, and refusal row $R$ have all been read.

The structural rows preserve the same display. Componentwise transport through $H$ keeps each row separate, the continuation row $C$ replays the inspection order, and the provenance row $P$ records the sibling package, registry, roadmap, audit, and empirical-boundary sources.

The local naming row $N$ therefore names the whole packet rather than a bare claim site. Since the carrier has no additional promotion coordinate, these rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[ProgrammeStrengthLedger no strength inflation]
\label{thm:programme-strength-ledger-no-strength-inflation}
An accepted $\ProgrammeStrengthLedgerUp$ packet cannot export a reported BEDC strength above the row displayed by $S$. In particular, an open dependency in $D$, a weaker theory/formal status in $V$, an absent or unchecked bridge row in $B$, or a refusal in $R$ blocks any consumer from reading $Q$ as a stronger seed, certificate, machine-check, or bridge claim.
\end{theorem}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerNoStrengthInflation}
\begin{proof}
Read the carrier rows in order. The reported strength $S$ is paired with dependency status $D$, theory/formal status $V$, bridge status $B$, and refusal row $R$ before the claim site can be exported.

Apply the strength-gradient rule of \autoref{thm:philosophy-programme-no-strength-inflation}. A stronger report would require one of the missing obligations to be treated as discharged.

The packet contains no such discharge row. The upgrade registry can record a next obligation, the verification-failure roadmap can record a blocker, and the gap-and-bridge audit can record an open bridge, but none of those sibling rows changes $S$ inside this carrier. Hence every exported strength is bounded by the displayed row.
\end{proof}

\begin{theorem}[ProgrammeStrengthLedger axis separation]
\label{thm:programme-strength-ledger-axis-separation}
For an accepted $\ProgrammeStrengthLedgerUp$ carrier, the claim site $Q$, reported-strength row $S$, dependency row $D$, theory/formal status pair $V$, bridge-status row $B$, refusal row $R$, and local $\NameCert$ row $N$ remain separate audit coordinates. A consumer may combine them into one public strength report, but it may not derive the theory-closure axis from the verification axis, derive bridge status from formal status, erase dependency blockers by provenance, or replace the refusal row by a sibling package reading.
\end{theorem}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerAxisSeparation}
\begin{proof}
Inspect the displayed packet. The status pair $V$ records two independent axes, while $B$ and $R$ record bridge and refusal information that are not components of $V$.

The support rows $H,C,P,N$ preserve and name the separation. Transport and replay can move the packet through equivalent presentations, and provenance can cite the package, registry, roadmap, audit, and boundary siblings, but none of those operations merges rows.

Thus every public report must keep the rows jointly visible. Any route that infers bridge status from formal status, erases a blocker from $D$, or replaces $R$ by prose from a sibling chapter leaves the accepted $\ProgrammeStrengthLedgerUp$ carrier.
\end{proof}

\begin{theorem}[ProgrammeStrengthLedger public report interface]
\label{thm:programmestrengthledger-public-report-interface}
A public strength report exported from an accepted $\ProgrammeStrengthLedgerUp$
carrier may expose the claim-site row $Q$ only together with the reported
strength row $S$, dependency row $D$, theory/formal status pair $V$,
bridge-status row $B$, refusal row $R$, componentwise transport row $H$,
displayed replay row $C$, provenance row $P$, and local naming row $N$. Such a
report may not promote theory closure from formal status, bridge status from
verification status, or the reported strength beyond the row displayed by $S$.
\end{theorem}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerPublicReportInterface}
\begin{proof}
Start with the no-strength-inflation theorem
\autoref{thm:programme-strength-ledger-no-strength-inflation}. It blocks any
route that reads $Q$ as a stronger seed, certificate, machine-check, or bridge
claim than the displayed row $S$ permits.

Apply the axis-separation theorem
\autoref{thm:programme-strength-ledger-axis-separation}. It keeps $Q,S,D,V,B,R$
as distinct audit coordinates, so a public report cannot derive theory closure
from verification status or bridge status from the formal axis.

The structural rows $H,C,P,N$ preserve, replay, source, and name exactly that
joint display. Therefore the public consumer surface is the full row family
$Q,S,D,V,B,R,H,C,P,N$, with no promotion coordinate outside the accepted
carrier.
\end{proof}

\begin{theorem}[ProgrammeStrengthLedger bridge-blocker readback]
\label{thm:programme-strength-ledger-bridge-blocker-readback}
Let a public strength report be exported from an accepted $\ProgrammeStrengthLedgerUp$ carrier $L=(Q,S,D,V,B,R,H,C,P,N)$. If the bridge row $B$ records no bridge or a paper bridge, or if the dependency row $D$ names an open blocker, then the report must export that blocker together with $Q,S,V,R,H,C,P,N$. It may not read the claim site $Q$ as bridge-checked, machine-verified, or stronger than the displayed rows permit.
\end{theorem}
\begin{proof}
Start from no strength inflation, \autoref{thm:programme-strength-ledger-no-strength-inflation}. An absent bridge, a paper bridge, or an open dependency blocker is one of the displayed reasons that prevents $Q$ from being read above the strength row $S$.

Apply axis separation, \autoref{thm:programme-strength-ledger-axis-separation}. The bridge row $B$ and dependency row $D$ remain separate from the theory/formal status pair $V$, so neither row can be erased by reading the other as a stronger verification coordinate.

Finally use the public report interface, \autoref{thm:programmestrengthledger-public-report-interface}. It forces every public report to expose $Q,S,D,V,B,R,H,C,P,N$ together. Hence a bridge blocker or dependency blocker is exported as part of the report surface, and no consumer can read the packet as bridgeChecked or machine-verified unless the corresponding displayed rows already carry that status.
\end{proof}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerBridgeBlockerReadback}

\begin{theorem}[ProgrammeStrengthLedger mature treatment]
\label{thm:programme-strength-ledger-mature-treatment}
Every public treatment of an accepted $\ProgrammeStrengthLedgerUp$ carrier
$L=(Q,S,D,V,B,R,H,C,P,N)$ is mature precisely when it exports $Q$ only through
the displayed strength row $S$, dependency row $D$, theory/formal status pair
$V$, bridge row $B$, refusal row $R$, componentwise transport $H$, replay row
$C$, provenance row $P$, and local naming row $N$. Such a treatment may cite
bridge blockers, verification blockers, and next-obligation rows, but it may
not turn any of them into a stronger claim about $Q$ unless the corresponding
displayed row already carries that strength.
\end{theorem}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerMatureTreatment}
\begin{proof}
Start with the public report interface,
\autoref{thm:programmestrengthledger-public-report-interface}. It fixes the
only exported report surface as the full row family $Q,S,D,V,B,R,H,C,P,N$.

Apply bridge-blocker readback,
\autoref{thm:programme-strength-ledger-bridge-blocker-readback}. It keeps every
displayed bridge or dependency blocker visible when the public report is read,
so the mature treatment cannot erase a blocker by citing provenance or another
status row.

Finally use no strength inflation and axis separation,
\autoref{thm:programme-strength-ledger-no-strength-inflation} and
\autoref{thm:programme-strength-ledger-axis-separation}. These rows prevent
promotion beyond $S$ and prevent the theory, formal, bridge, dependency,
refusal, and naming coordinates from being derived from one another. Hence the
public treatment is mature exactly as a bounded ledger read, not as a stronger
claim about the inspected site.
\end{proof}

\begin{theorem}[Finite blocker readback factors through programme strength ledger]
\label{thm:programme-strength-ledger-blocker-readback-factorization}
Let $L=(Q,S,D,V,B,R,H,C,P,N)$ be an accepted $\ProgrammeStrengthLedgerUp$
carrier whose source row cites $\UpgradeRegistryUp$, $\GapFailureBridgeAuditUp$,
and $\VerificationFailureRoadmapUp$. Any blocker readback exposed by $D$ or $B$
factors through a single $\BHist$ ledger row $K$ carried by $L$ such that $K$
preserves the displayed $\hsame$ transport and $\Cont$ provenance route from
the source certificates to the public strength report. The factorization is a
finite $\Pkg$/GAP readback inside the ledger surface, not an external judgement
about the claim site $Q$.
\end{theorem}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerBlockerReadbackFactorization}
\begin{proof}
Unpack the accepted carrier. The dependency row $D$, bridge row $B$, transport
row $H$, replay row $C$, provenance row $P$, and local naming row $N$ are all
displayed before the public report can be read, so a blocker cannot be exported
as a free-standing judgement.

Compose the three cited source certificates. The $\UpgradeRegistryUp$ row names
the current obligation and next required row, the $\GapFailureBridgeAuditUp$ row
keeps bridge blockers visible, and the $\VerificationFailureRoadmapUp$ row
records the verification blocker. Their common readback is therefore a single
finite $\BHist$ row $K$ inside the programme-strength ledger.

Finally apply the bridge-blocker readback theorem
\autoref{thm:programme-strength-ledger-bridge-blocker-readback}. It forces the
blocker to travel with $Q,S,D,V,B,R,H,C,P,N$, while $H$ preserves $\hsame$ and
$C,P$ preserve the $\Cont$ and $\Pkg$ provenance route. Hence the blocker
readback factors through $K$ as a finite ledger readback and not through any
external status judgement.
\end{proof}

\begin{theorem}[ProgrammeStrengthLedger formal target request]
\label{thm:programme-strength-ledger-formal-target-request}
Any formalization target for the public $\ProgrammeStrengthLedgerUp$ strength report must preserve the displayed row family $Q,S,D,V,B,R,H,C,P,N$. Its proof obligations are exactly no-strength-inflation for $S$, axis separation for $Q,S,D,V,B,R,N$, the public report interface for $Q,S,D,V,B,R,H,C,P,N$, and bridge-blocker readback for $D$ and $B$. In particular, the target may not derive theory closure from verification status, derive bridge status from formal status, erase a dependency blocker by provenance, or promote the report beyond the displayed strength row.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:programme-strength-ledger-namecert-obligations}. They expose the finite carrier $L=(Q,S,D,V,B,R,H,C,P,N)$, so a formal target that drops one coordinate no longer names the public ledger surface.

Apply no strength inflation, \autoref{thm:programme-strength-ledger-no-strength-inflation}, and axis separation, \autoref{thm:programme-strength-ledger-axis-separation}. These two rows keep the reported strength bounded by $S$ and prevent the theory, formal, bridge, dependency, refusal, and naming coordinates from being inferred from one another.

Finally use the public report interface \autoref{thm:programmestrengthledger-public-report-interface} together with bridge-blocker readback \autoref{thm:programme-strength-ledger-bridge-blocker-readback}. The first theorem exports the full row family, while the second keeps every displayed bridge or dependency blocker visible. These four obligations therefore give the required request surface without turning closure status, verification status, or bridge status into one another.
\end{proof}
\leanchecked{BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerFormalTargetRequest}

\falsifiablePrediction{A packet that omits $S$ or $V$ can still name a claim site, but it cannot export a reported BEDC strength.}

\independenceWitness{bedcphilosophypackage, upgraderegistry, verificationfailureroadmap, gapfailurebridgeaudit, formalconstantempiricalvalueboundary: no listed sibling is bijective with $\ProgrammeStrengthLedgerUp$ because none preserves claim site, strength row, dependency row, theory/formal status pair, bridge row, refusal row, transport, replay, provenance, and local NameCert together. The package carrier stores programme rows without owning the strength audit packet; the upgrade registry records current and next obligations without binding refusal and bridge rows; the verification-failure roadmap records blockers without exporting a strength ledger; the gap-failure bridge audit records bridge blockers without the full status pair; and the empirical-value boundary separates formal and empirical constants rather than claim-strength reporting.}

The Lean-side boundary is expected to provide explicit $\Nontrivial\ \ProgrammeStrengthLedgerUp$ and $\FieldFaithful\ \ProgrammeStrengthLedgerUp$ instances in \texttt{lean4/BEDC/Derived/ProgrammeStrengthLedgerUp/TasteGate.lean}. The field-faithfulness witness must keep $Q,S,D,V,B,R,H,C,P,N$ as distinct rows before the target \texttt{BEDC.Derived.ProgrammeStrengthLedgerUp.taste\_gate} can classify strength-ledger packets.

\closureat{ProgrammeStrengthLedgerUp}{scopedStr}

\begin{closurestatus}{\ProgrammeStrengthLedgerUp}
  \constructivestory{A $\ProgrammeStrengthLedgerUp$ packet is generated from a claim site, reported strength, dependency row, theory/formal status pair, bridge-status row, refusal row, componentwise transport, displayed replay, package provenance, and local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:programme-strength-ledger-carrier}, \autoref{thm:programme-strength-ledger-namecert-obligations}, \autoref{thm:programme-strength-ledger-no-strength-inflation}, \autoref{thm:programme-strength-ledger-axis-separation}, \autoref{thm:programmestrengthledger-public-report-interface}, and \autoref{thm:programme-strength-ledger-bridge-blocker-readback} scope the displayed carrier, NameCert obligations, no-strength-inflation row, axis-separation row, exported public strength report surface, and bridge-blocker readback through finite audit rows, $\hsame$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, correctness of every package row, discharge of every dependency blocker, or promotion of any named claim beyond its displayed strength row.}
  \upgradepath{The next obligation is Lean-side formalization of the displayed bridge-blocker readback for BEDC.Derived.ProgrammeStrengthLedgerUp.ProgrammeStrengthLedgerBridgeBlockerReadback.}
\end{closurestatus}
