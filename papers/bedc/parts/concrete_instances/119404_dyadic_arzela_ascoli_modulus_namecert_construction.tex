\chapter{A Concrete Naming Certificate for $\DyadicArzelaAscoliModulusUp$}
\label{ch:concrete-instances-dyadic-arzela-ascoli-modulus-namecert}
\origin{human}

Following Bishop and Bridges and the classical Arzela--Ascoli theorem, the $\DyadicArzelaAscoliModulusUp$ packet records the finite dyadic-net modulus surface by which an equicontinuous family of maps on a compact source is read before a $\RegSeqRatUp$ or $\RealUp$ value seal is consumed. It sits after the Arzela--Ascoli carrier of \autoref{ch:concrete-instances-arzelaascoli-namecert}, the compact source of \autoref{ch:concrete-instances-compact-namecert}, the continuous-map graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a displayed finite-net modulus certificate; it does not assert open-cover compactness, select a subsequence, quotient function-space equality, use countable choice, or import ambient $\RealUp$ completeness.

\begin{definition}[Dyadic Arzela--Ascoli modulus carrier]
\label{def:dyadic-arzela-ascoli-modulus-carrier}
A $\DyadicArzelaAscoliModulusUp$ carrier is a finite $\BHist$ packet
$$
A=(K,F,D,S,R,E,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row, exposing only the compact object through the finite-net boundary already named by the Arzela--Ascoli packet.
\item $F$ is the $\ContinuousMapUp$ family row, listing the maps whose values may be inspected.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, carrying dyadic radii, mesh scales, comparison cells, and half-radius splits.
\item $S$ is the $\StreamNameUp$ finite-window row for the sampled source points and map-value observations.
\item $R$ is the $\RegSeqRatUp$ readback row, binding the displayed stream windows and dyadic tolerance cells to regular rational value observations.
\item $E$ is the terminal $\RealUp$ seal row for value-facing consumers, exposed only after $K,F,D,S,R$ have been displayed.
\item $M$ is the finite dyadic-net modulus row, assigning each displayed tolerance request to a displayed mesh scale and finite inspection window.
\item $H$ records componentwise $\hsame$ transport for the compact source, map family, dyadic ledger, stream windows, readback, real seal, modulus row, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
K,F,D,S,R \longmapsto E,M .
$$
\item $P$ is the $\Pkg$ provenance over $\DyadicArzelaAscoliModulusUp$, $\ArzelaAscoliUp$, $\CompactUp$, $\ContinuousMapUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicArzelaAscoliModulusUp}$ row naming exactly this finite modulus packet.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for an open cover, selected subsequence, quotient function space, countable-choice selector, detached real representative, or ambient compactness theorem.
\end{definition}

\begin{theorem}[Dyadic Arzela--Ascoli modulus NameCert obligations]
\label{thm:dyadic-arzela-ascoli-modulus-namecert-obligations}
For every accepted $\DyadicArzelaAscoliModulusUp$ carrier
$$
A=(K,F,D,S,R,E,M,H,C,P,N),
$$
the local $\NameCert_{\DyadicArzelaAscoliModulusUp}$ surface consists exactly of carrier admission, compact-source visibility, continuous-map family admission, dyadic tolerance visibility, finite stream-window admission, regular rational readback, terminal real-name sealing, finite dyadic-net modulus assignment, componentwise transport, displayed replay, provenance, and local naming. Every Arzela--Ascoli or compact-uniform consumer must pass through
$$
K,F,D,S,R,E,M,H,C,P,N
$$
and through no selected subsequence, quotient function-space equality, open-cover compactness proof, countable-choice selector, host real equality, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:dyadic-arzela-ascoli-modulus-carrier}. The rows $K,F,D,S,R,E,M$ expose the compact source, map family, dyadic tolerance ledger, finite observation windows, regular rational readback, real seal, and modulus assignment before any Arzela--Ascoli consumer is available.

Read the analytic route in order. The compact row $K$ supplies only the displayed finite-net boundary, the map row $F$ supplies only displayed continuous-map observations, and the dyadic row $D$ fixes the tolerance scale inspected by $S$.

The readback row $R$ binds those windows to regular rational observations, and the seal row $E$ is downstream of the same route. The modulus row $M$ therefore records a finite mesh-scale assignment justified by $K,F,D,S,R$ rather than by an external compactness or selection principle.

The structural rows $H,C,P,N$ preserve, replay, source, and locally name this route. Since the carrier has no coordinate for the excluded subsequence, quotient, cover, choice, host-equality, or completeness data, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Dyadic finite-net induction route]
\label{thm:dyadic-arzela-ascoli-modulus-finite-net-induction}
Let
$$
A=(K,F,D,S,R,E,M,H,C,P,N)
$$
be an accepted $\DyadicArzelaAscoliModulusUp$ carrier. Every modulus read from $A$ is obtained by finite induction over the displayed dyadic net rows in $S$: the base read consumes the first displayed net point through $D,R,E$, and each induction step appends one displayed point using the same $D$-scale and the same $\ContinuousMapUp$ family row $F$. The resulting route factors through
$$
\CompactUp \longmapsto \ContinuousMapUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
and then through the modulus, transport, replay, provenance, and local naming rows $M,H,C,P,N$.
\end{theorem}
\begin{proof}
Start from the displayed finite net inside $S$. The empty or first-point case has only the compact source row $K$, the map family row $F$, and the dyadic tolerance row $D$ available before any value is read.

For the induction step, append one displayed net point. Its map-value observation is read through the same $\ContinuousMapUp$ row $F$, the same dyadic scale in $D$, and the same $\StreamNameUp$ window discipline before $R$ translates the observation into regular rational readback.

After each step, the terminal value seal $E$ remains downstream of $D,S,R$. The modulus row $M$ can therefore record only the mesh scale and finite window already displayed by the induction.

Finally, $H,C,P,N$ transport, replay, source, and name the finite induction route. No open-cover proof, selected subsequence, quotient function-space equality, countable-choice selector, host real equality, or ambient Real-completeness row is present in the carrier.
\end{proof}

\closureat{DyadicArzelaAscoliModulusUp}{seedStr}

\begin{closurestatus}{\DyadicArzelaAscoliModulusUp}
  \constructivestory{A $\DyadicArzelaAscoliModulusUp$ packet is generated from a compact source row, continuous-map family row, dyadic tolerance ledger, finite StreamName windows, RegSeqRat readback, RealUp seal, finite dyadic-net modulus assignment, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-arzela-ascoli-modulus-carrier}, \autoref{thm:dyadic-arzela-ascoli-modulus-namecert-obligations}, and \autoref{thm:dyadic-arzela-ascoli-modulus-finite-net-induction} seed the finite dyadic-net modulus route over compact sources, continuous-map families, dyadic tolerances, stream windows, regular rational readback, terminal Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected subsequences, quotient function-space equality, open-cover compactness, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source visibility, continuous-map family admission, dyadic tolerance visibility, finite window admission, regular rational readback, Real seal handoff, modulus assignment, transport, replay, provenance, and local naming for BEDC.Derived.DyadicArzelaAscoliModulusUp.}
\end{closurestatus}
