\chapter{A Concrete Naming Certificate for $\NiemytzkiPlaneUp$}
\label{ch:concrete-instances-niemytzki-plane-namecert}
\origin{human}

Following Niemytzki's classical tangent-disc topology, the $\NiemytzkiPlaneUp$ packet records the upper half-plane together with its boundary-line neighbourhood discipline as finite NameCert data. It reads the open-set carrier of \autoref{ch:concrete-instances-topology-namecert}, the metric-neighbourhood interface of \autoref{ch:concrete-instances-metric-topology-namecert}, the real-coordinate seal of \autoref{ch:concrete-instances-real-namecert}, the coordinate-pair route of \autoref{ch:concrete-instances-product-topology-namecert}, and the separated-point surface of \autoref{ch:concrete-instances-hausdorffspace-namecert}. The packet names the tangent-disc boundary route only; it does not assert metrizability, separability, compactness, classical choice principles, or a completed continuum beyond displayed real-coordinate rows.

\begin{definition}[Niemytzki plane carrier]
\label{def:niemytzki-plane-carrier}
A $\NiemytzkiPlaneUp$ carrier is a finite $\BHist$ packet
$$
N=(U,B,T,R,Q,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the upper-half-plane point row, presenting points with positive second coordinate through displayed $\RealUp$ coordinate reads.
\item $B$ is the boundary-line point row, presenting points with zero second coordinate and a displayed first-coordinate readback.
\item $T$ is the tangent-disc neighbourhood row, recording for a boundary point the basic neighbourhood consisting of that boundary point together with an open Euclidean disc tangent to the boundary line at that point.
\item $R$ is the product-real coordinate readback row, routing all point and disc-center coordinates through $\RealUp$ and the product-topology surface before any topology consumer reads the packet.
\item $Q$ is the classifier row, comparing accepted neighbourhood packets by upper-half-plane membership, boundary-line membership, tangent-disc incidence, and coordinate readback only.
\item $H$ records componentwise $\hsame$ transport for point, boundary, tangent-disc, coordinate-readback, classifier, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a neighbourhood request through $U,B,T,R,Q,H,P,L$.
\item $P$ is the $\Pkg$ provenance over $\NiemytzkiPlaneUp$, $\TopologyUp$, $\MetricTopologyUp$, $\RealUp$, $\ProductTopologyUp$, $\HausdorffSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\NiemytzkiPlaneUp}$ row.
\end{enumerate}
The classifier has no coordinate for a global metric inducing the topology, a countable base, compactness, selected centres by choice, or host equality of real pairs.
\end{definition}

\begin{theorem}[Niemytzki plane NameCert obligations]
\label{thm:niemytzki-plane-namecert-obligations}
For every accepted $\NiemytzkiPlaneUp$ carrier
$$
N=(U,B,T,R,Q,H,C,P,L),
$$
the local $\NameCert_{\NiemytzkiPlaneUp}$ surface consists exactly of upper-half-plane point admission through $U$, boundary-line point admission through $B$, tangent-disc neighbourhood admission through $T$, product-real coordinate readback through $R$, classifier transport through $Q$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:niemytzki-plane-carrier}. The accepted packet exposes upper-half-plane points, boundary-line points, tangent-disc neighbourhoods, coordinate readback, classifier transport, replay, provenance, and local naming as separate rows.

The topology-facing rows are $U,B,T$. Upper-half-plane points are ordinary displayed coordinate pairs above the boundary, while boundary points obtain their basic neighbourhoods only through the tangent-disc row.

The coordinate and classifier rows are downstream of those point rows. The row $R$ forces every coordinate read through $\RealUp$ and $\ProductTopologyUp$, and $Q$ compares packets only through visible incidence and coordinate data.

Finally $H,C,P,L$ transport, replay, source, and name the same finite route. Since no row supplies a global metric, countable base, compactness witness, selected centre, or host pair equality, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Niemytzki tangent-disc boundary route]
\label{thm:niemytzki-plane-tangent-disc-boundary-route}
Every boundary-neighbourhood read exported by an accepted $\NiemytzkiPlaneUp$ packet must pass through the boundary-line row $B$, the tangent-disc neighbourhood row $T$, and the product-real coordinate readback row $R$ before it can be consumed as a $\TopologyUp$ or $\HausdorffSpaceUp$ neighbourhood. Thus the boundary topology is read as a finite tangent-disc certificate, not as an ordinary metric-ball neighbourhood, a chosen countable basis, a compactness claim, or a classical-choice selection.
\end{theorem}
\begin{proof}
Start with the boundary-line row. It identifies a displayed point on the real boundary and separates it from upper-half-plane reads.

Next read the tangent-disc row. The neighbourhood of a boundary point is supplied by the boundary point together with a displayed tangent Euclidean disc, so the certificate does not reduce the boundary read to an ordinary product-open rectangle.

The coordinate row $R$ then routes the centre, radius, tangent point, and incident upper-half-plane points through $\RealUp$ and $\ProductTopologyUp$. Transport and replay preserve this order before provenance or local naming is inspected.

Consequently a topology or Hausdorff consumer receives only the displayed tangent-disc route. It receives no metrizability theorem, countable-base witness, compactness witness, selected family of discs, or hidden real-pair equality.
\end{proof}

\falsifiablePrediction{Every accepted $\NiemytzkiPlaneUp$ boundary-neighbourhood read displays a boundary-line row, a tangent-disc neighbourhood row, product-real coordinate readback, classifier transport, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{topology, metric-topology, real, product-topology, hausdorffspace: $\NiemytzkiPlaneUp$ is not bijective to any listed sibling because it binds ordinary upper-half-plane coordinate reads and boundary tangent-disc neighbourhood reads into one finite topology packet.}

\closureat{NiemytzkiPlaneUp}{seedStr}

\begin{closurestatus}{\NiemytzkiPlaneUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:niemytzki-plane-carrier}, \autoref{thm:niemytzki-plane-namecert-obligations}, and \autoref{thm:niemytzki-plane-tangent-disc-boundary-route} seed the finite upper-half-plane, boundary-line, tangent-disc, product-real, classifier, transport, replay, provenance, and local naming route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim metrizability, separability, compactness, classical choice principles, selected tangent-disc families, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tangent-disc neighbourhood exactness, product-real coordinate readback, classifier transport, replay, provenance, and local naming for BEDC.Derived.NiemytzkiPlaneUp.}
  \constructivestory{A $\NiemytzkiPlaneUp$ packet is generated from upper-half-plane point rows, boundary-line point rows, tangent-disc neighbourhood rows, product-real coordinate readback, classifier transport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
