\chapter{A Concrete Naming Certificate for $\RationalBallDomainUp$}
\label{ch:concrete-instances-rational-ball-domain-namecert}
\origin{human}

Following Edalat--Heckmann formal-ball domains and Bishop--Bridges constructive completion, the $\RationalBallDomainUp$ packet records the finite rational-ball domain surface used by L10 completion routes before any completed point can be consumed. It sits after the rational-ball carrier of \autoref{ch:concrete-instances-rational-ball-namecert}, the rational-ball basis of \autoref{ch:concrete-instances-rational-ball-basis-namecert}, the formal-ball domain route of \autoref{ch:concrete-instances-formal-ball-domain-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-name windows of \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet gives downstream Real and Completion chapters a non-quotient rational-ball-domain handoff; it does not choose Cauchy representatives, quotient filters, invoke host point equality, or assume ambient completeness.

\begin{definition}[RationalBallDomain carrier]\label{def:rational-ball-domain-carrier}
A $\RationalBallDomainUp$ carrier is a finite $\BHist$ packet
$$
D=(B,K,F,O,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\RationalBallUp$ observation row, carrying finite rational center-radius packets.
\item $K$ is the $\RationalBallBasisUp$ row, recording the inspected basis windows and their finite refinement comparisons.
\item $F$ is the $\FormalBallDomainUp$ row, supplying the rounded-domain order discipline into which rational balls are embedded.
\item $O$ is the overlap and directedness ledger for finite families of rational balls.
\item $S$ is the $\StreamNameUp$ finite observation schedule used by completion-facing consumers.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed rational-ball windows.
\item $E$ is the endpoint handoff row, read by $\CompletionUp$ and $\RealUp$ consumers only after $B,K,F,O,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for rational balls, basis windows, formal-domain rows, overlap ledgers, stream schedules, regular readbacks, endpoints, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from rational-ball-domain requests through $B,K,F,O,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RationalBallDomainUp$, $\RationalBallUp$, $\RationalBallBasisUp$, $\FormalBallDomainUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CompletionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RationalBallDomainUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,K,F,O,S,R,E,H,C,P,N$. It has no coordinate for quotient completion, selected representatives, host metric equality, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[RationalBallDomain NameCert obligations]\label{thm:rational-ball-domain-namecert-obligations}
For every accepted $\RationalBallDomainUp$ carrier $D=(B,K,F,O,S,R,E,H,C,P,N)$, the local $\NameCert_{\RationalBallDomainUp}$ surface consists exactly of carrier admission for the rational-ball row, rational-ball-basis row, formal-ball-domain row, overlap ledger, stream schedule, regular rational readback, endpoint handoff, componentwise transport, displayed replay, provenance, and local naming. In particular, rational-ball containment must be read through $B,K,F,O$, completion-facing observation through $S,R,E$, and structural stability through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier. The rational-ball source is visible only as $B$, the basis route only as $K$, and the rounded-domain discipline only as $F$.

The overlap row $O$ records the finite directed family of rational balls before any stream or regular-rational consumer can read it. The schedule and readback rows $S$ and $R$ therefore remain downstream of the displayed rational-ball-domain surface, and the endpoint row $E$ is terminal for that route.

The structural rows $H,C,P,N$ transport, replay, source, and name the same packet. Since no carrier coordinate stores quotient completion, selected representatives, host metric equality, countable choice, or ambient completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[RationalBallDomain completion handoff]\label{thm:rational-ball-domain-completion-handoff}
Every $\CompletionUp$ or $\RealUp$ consumer of an accepted $\RationalBallDomainUp$ carrier must first expose the finite route
$$
\RationalBallUp \longmapsto \RationalBallBasisUp \longmapsto \FormalBallDomainUp \longmapsto O \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The handoff exports rational-ball-domain coverage, finite stream observation, regular rational readback, endpoint sealing, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. It exports no quotient sequence equality, selected limit, host metric equality, countable-choice selector, or ambient completion theorem.
\end{theorem}
\begin{proof}
Start with the accepted carrier $D=(B,K,F,O,S,R,E,H,C,P,N)$. A completion-facing request first reads the rational-ball row $B$ and the basis row $K$, then checks that those windows live inside the rounded formal-domain row $F$.

The overlap ledger $O$ is the finite directedness witness for the rational-ball family. Only after that row is displayed can the consumer read the finite schedule $S$ and the regular rational readback $R$.

The endpoint row $E$ is therefore downstream of $B,K,F,O,S,R$. Transport through $H$ and replay through $C$ keep the same route, while $P$ and $N$ source and name it. The excluded quotient, selected-limit, host-equality, choice, and ambient-completeness data have no row from which a consumer could read them.
\end{proof}

\falsifiablePrediction{Every accepted $\RationalBallDomainUp$ completion-facing read displays RationalBallUp, RationalBallBasisUp, FormalBallDomainUp, DyadicRatCoreUp, StreamNameUp, RegSeqRatUp, endpoint handoff, hsame transport, Cont replay, Pkg provenance, and local NameCert rows before a Real or Completion consumer can use the endpoint.}

\independenceWitness{rationalball, rational-ball-basis, formal-ball-domain, dyadicratcore, streamname, regseqrat: $\RationalBallDomainUp$ is not bijective to any listed sibling because it jointly binds rational-ball coverage, basis refinement, rounded formal-domain discipline, dyadic tolerance, finite stream observation, regular rational readback, endpoint handoff, transport, replay, provenance, and local naming.}

\closureat{RationalBallDomainUp}{seedStr}

\begin{closurestatus}{\RationalBallDomainUp}
  \constructivestory{A $\RationalBallDomainUp$ packet is generated from RationalBallUp rows, RationalBallBasisUp rows, FormalBallDomainUp rows, finite overlap ledgers, StreamNameUp schedules, RegSeqRatUp readback rows, CompletionUp and RealUp endpoint handoffs, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rational-ball-domain-carrier}, \autoref{thm:rational-ball-domain-namecert-obligations}, and \autoref{thm:rational-ball-domain-completion-handoff} seed the finite rational-ball-domain route from rational coverage to completion-facing endpoint consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion, selected representatives, host metric equality, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, rational-ball coverage, basis refinement, formal-domain embedding, overlap directedness, stream scheduling, regular readback, endpoint handoff, transport, replay, provenance, and local naming for BEDC.Derived.RationalBallDomainUp.}
\end{closurestatus}
