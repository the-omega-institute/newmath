\noindent The endpoint route consumes the finite morphism packet of
\autoref{ch:concrete-instances-kernelmorphism-namecert}.
$\KernelMorphismUp$ supplies source-target graph admission and
classifier-lift rows, while $\CertificateCompilerUp$ adds target landing,
endpoint route, and composition ledgers.

\begin{theorem}[CertificateCompiler target endpoint route]
\label{thm:certificate-compiler-target-endpoint-route}
Let
$$
K=(S,T,G,E,C,H,P,N)
$$
be an accepted $\CertificateCompilerUp$ packet. Every route by which $K$
reaches the target certificate $T$ is exhausted by the source $\NameCert$ row
$S$, the displayed compiler graph $G$, the target landing row $E$, the
classifier transport row $H$, the continuation row $C$, the $\Pkg$
provenance row $P$, and the local $\NameCert$ row $N$. In particular, target
arrival is a finite $\BHist$ read through $S,G,E,H,C,P,N$, not a read of host
compiler semantics, optimiser correctness, ambient program equality, or
unrecorded target behavior.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:certificate-compiler-carrier}. The
accepted packet exposes exactly the source and target naming rows, graph,
landing, continuation, componentwise $\hsame$ transport, package provenance,
and local naming row.

Endpoint availability is supplied by
\autoref{thm:certificate-compiler-endpoint-certificate-availability}, so the
target certificate is already one of the displayed rows. Edge-classifier
exactness from \autoref{thm:certificate-compiler-edge-classifier-exactness}
then forces each graph edge to land through $E$ and to transport classifier
data only through $H$ and the paired $\Cont$ route in $C$.

Finally apply the public consumer boundary of
\autoref{thm:certificate-compiler-public-consumer-boundary}. A downstream
read can consume only the displayed graph, landing, classifier transport,
continuation, package, and local naming rows. Thus every target endpoint route
is the stated finite $\BHist$ route through $S,G,E,H,C,P,N$.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_target\_endpoint\_route}

\begin{theorem}[CertificateCompiler identity composite public boundary]
\label{thm:certificate-compiler-identity-composite-public-boundary}
For accepted $\CertificateCompilerUp$ packets, identity rows, binary
composites, and triple composites remain inside the same public row surface
$S,T,G,E,C,H,P,N$. The identity packet exposes only the diagonal rows of its
local $\NameCert$ input, a binary composite exposes only the two displayed
compiler packets and their shared middle landing, and a triple composite
exposes only the three displayed graph edges, the two middle landing rows,
the ordered $\hsame$ transports, the composed $\Cont$ route, the $\Pkg$
provenance, and the local $\NameCert$ rows.
\end{theorem}
\begin{proof}
For identities, apply
\autoref{thm:certificate-compiler-identity-ledger}. The diagonal packet reads
the carrier, classifier, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows
already displayed by the local certificate.

For binary composites, use
\autoref{thm:certificate-compiler-composition-stability} together with
\autoref{thm:certificate-compiler-identity-composite-ledger-exhaustion}. The
composite graph is formed from displayed graph edges, and its landing,
classifier, continuation, package, and local naming rows are projected from
the two input packets and their middle $\NameCert$ row.

For triple composites, apply
\autoref{thm:certificate-compiler-triple-composite-associativity}. The two
associations expose the same three graph edges, two middle landing matches,
ordered classifier transports, and continuation endpoint.

The left and right unit laws,
\autoref{thm:certificate-compiler-left-identity-unit} and
\autoref{thm:certificate-compiler-right-identity-unit}, identify identity
composites with the original packet surface. Combining these rows gives the
same public boundary $S,T,G,E,C,H,P,N$ for identity, binary, and triple
compiler packets.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_identity\_composite\_public\_boundary}

\begin{theorem}[CertificateCompiler endpoint-route determinacy envelope]
\label{thm:certificate-compiler-endpoint-route-determinacy-envelope}
Let
$$
K=(S,T,G,E,C,H,P,N)
$$
be an accepted $\CertificateCompilerUp$ packet. Any accepted target endpoint route for $K$, and any identity, binary composite, or triple composite route built from accepted $\CertificateCompilerUp$ packets, is determined by the same displayed finite surface $S,T,G,E,C,H,P,N$. There is no second endpoint route through host compiler semantics, optimiser correctness, ambient program equality, or unrecorded target behavior.
\end{theorem}
\begin{proof}
First apply the target endpoint route of \autoref{thm:certificate-compiler-target-endpoint-route}. It places endpoint arrival inside the displayed source naming row, compiler graph, target landing row, classifier transport, continuation row, package provenance, and local naming row of the accepted carrier.

Next apply the identity composite public boundary of \autoref{thm:certificate-compiler-identity-composite-public-boundary}. Identity rows, binary composites, and triple composites stay on the same public row surface $S,T,G,E,C,H,P,N$; their middle landings, graph edges, transports, continuation rows, package provenance, and local names are projected from displayed compiler packets.

Finally return to the carrier definition. The accepted packet has no coordinate for host compiler semantics, optimiser correctness, ambient program equality, or unrecorded target behavior. Endpoint arrival and the composite public boundary therefore share the same displayed rows, so the finite surface $S,T,G,E,C,H,P,N$ determines the available endpoint and composite routes.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_endpoint\_route\_determinacy\_envelope}

\begin{theorem}[CertificateCompiler endpoint determinacy]
\label{thm:certificate-compiler-endpoint-determinacy}
Let $K=(S,T,G,E,C,H,P,N)$ be an accepted $\CertificateCompilerUp$ packet. If
two accepted target endpoint routes for $K$ agree on the source row $S$, the
compiler graph $G$, the landing row $E$, the classifier transport $H$, the
continuation row $C$, the provenance row $P$, and the local name $N$, then
they determine the same target certificate row $T$. No host compiler
semantics, optimiser equality, ambient program equality, or unrecorded target
behavior can create a second endpoint.
\end{theorem}
\begin{proof}
First apply the target endpoint route theorem,
\autoref{thm:certificate-compiler-target-endpoint-route}. It exhausts every
route to $T$ by the displayed source, graph, landing, classifier transport,
continuation, package, and local naming rows.

Endpoint certificate availability,
\autoref{thm:certificate-compiler-endpoint-certificate-availability}, supplies
the target certificate as part of that displayed packet, and edge-classifier
exactness, \autoref{thm:certificate-compiler-edge-classifier-exactness}, keeps
graph edges tied to the same landing and classifier rows. Thus two accepted
reads agreeing on $S,G,E,H,C,P,N$ have no further endpoint coordinate to vary.

Now use the public consumer boundary,
\autoref{thm:certificate-compiler-public-consumer-boundary}, together with
\autoref{thm:certificate-compiler-identity-composite-public-boundary}. Public
consumers, identities, binary composites, and triple composites remain on the
same finite row surface. Since the packet contains no host compiler semantics,
optimiser equality, ambient program equality, or unrecorded target behavior,
the agreed displayed rows determine the same target certificate row $T$.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_endpoint\_determinacy}

\begin{theorem}[CertificateCompiler downstream stage unblock package]
\label{thm:certificate-compiler-downstream-stage-unblock-package}
The target endpoint route of
\autoref{thm:certificate-compiler-target-endpoint-route} and the
identity/composite boundary of
\autoref{thm:certificate-compiler-identity-composite-public-boundary} supply
the public package consumed by $\CertificatePipelineStageBoundaryUp$,
$\KernelMorphismUp$, and $\DecompilerRightInverseUp$. The package consists
only of finite $\BHist$ rows, displayed $\hsame$ transport, $\Cont$ routes,
$\Pkg$ provenance, and local $\NameCert$ rows over
$K=(S,T,G,E,C,H,P,N)$; it does not export host compiler semantics, optimiser
correctness, decompiler correctness, ambient program equality, or target
behavior absent from the packet.
\end{theorem}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_downstream\_stage\_unblock\_package}
\begin{proof}
First build the endpoint part of the package. By
\autoref{thm:certificate-compiler-target-endpoint-route}, every accepted
target route reads the source naming row, graph row, landing row, classifier
transport row, continuation row, package provenance, and local naming row.

Next build the compositional part. By
\autoref{thm:certificate-compiler-identity-composite-public-boundary},
identities, binary composites, and triple composites keep the same public
row surface and do not add a host compiler coordinate.

Now combine these with
\autoref{thm:certificate-compiler-source-target-landing-exhaustion} and
\autoref{thm:certificate-compiler-finite-route-non-escape}. The source and
target endpoints are exhausted by the displayed packet rows, and every finite
route stays inside $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$
coordinates.

The consumers named in the statement require exactly this public route
surface: pipeline stages read adjacent compiler boundaries, kernel morphisms
read source-to-target graph preservation, and decompiler right-inverse
packets read the displayed compiler/decompiler interface. Since all three
reads factor through the same finite package, none receives a host compiler
semantics or optimiser-correctness theorem.
\end{proof}

\begin{theorem}[CertificateCompiler KernelMorphism route scope]
\label{thm:certificate-compiler-kernelmorphism-route-scope}
Let $K=(S,T,G,E,C,H,P,N)$ be an accepted $\CertificateCompilerUp$ packet. Any downstream $\KernelMorphismUp$ consumer reads the compiler graph $G$ only through the endpoint route $S,G,E,H,C,P,N$ and the source-target graph admission and classifier-lift rows of \autoref{ch:concrete-instances-kernelmorphism-namecert}. It cannot import host compiler semantics, optimiser correctness, decompiler correctness, or target behavior absent from the finite packet.
\end{theorem}
\begin{proof}
First use the target endpoint route of \autoref{thm:certificate-compiler-target-endpoint-route}. It identifies the graph-facing route to the target certificate with the displayed source naming row, compiler graph, target landing row, classifier transport, continuation row, package provenance, and local naming row.

Next apply the identity and composite public boundary of \autoref{thm:certificate-compiler-identity-composite-public-boundary}. Identities, binary composites, and triple composites stay on the same finite endpoint surface, so a downstream morphism consumer receives graph preservation through displayed compiler rows rather than through host program semantics.

Finally combine the downstream stage package of \autoref{thm:certificate-compiler-downstream-stage-unblock-package} with the $\KernelMorphismUp$ source-target scope named in \autoref{ch:concrete-instances-kernelmorphism-namecert}. The consumer-facing route is exactly the finite $\BHist$ handoff through $S,G,E,H,C,P,N$ together with the KernelMorphism graph-admission and classifier-lift rows, leaving optimiser correctness, decompiler correctness, and unrecorded target behavior outside the packet.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_kernelmorphism\_route\_scope}

\begin{theorem}[CertificateCompiler KernelMorphism decompiler boundary]
\label{thm:certificate-compiler-kernelmorphism-decompiler-boundary}
Let $K=(S,T,G,E,C,H,P,N)$ be an accepted $\CertificateCompilerUp$ packet. Any route from $K$ into a $\KernelMorphismUp$ consumer and then into a $\DecompilerRightInverseUp$ consumer factors through the displayed finite rows $S,G,E,H,C,P,N$, the $\KernelMorphismUp$ graph-admission and classifier-lift rows, and the $\DecompilerRightInverseUp$ inverse-boundary rows of \autoref{thm:decompiler-right-inverse-non-escape-boundary}. The route exports no host compiler semantics, optimiser correctness, ambient program equality, host decompiler function, total decompiler, or unrecorded target behavior.
\end{theorem}
\begin{proof}
First apply the target endpoint route of \autoref{thm:certificate-compiler-target-endpoint-route}. It places every accepted compiler target read on the displayed finite rows $S,G,E,H,C,P,N$.

Next apply the KernelMorphism route scope of \autoref{thm:certificate-compiler-kernelmorphism-route-scope}. The intermediate morphism consumer can add only the graph-admission and classifier-lift rows licensed by $\KernelMorphismUp$, so the compiler leg remains a finite $\BHist$ handoff rather than a host compiler semantics.

Finally apply the decompiler right-inverse non-escape boundary of \autoref{thm:decompiler-right-inverse-non-escape-boundary}. A downstream inverse consumer may read only the displayed compiler packet, decompiler graph, target optimisation row, right-inverse ledger, transport, continuation, provenance, and local naming rows. Combining the three non-escape clauses leaves no route to optimiser correctness, ambient program equality, host decompiler behavior, or target behavior absent from the packet.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_kernelmorphism\_decompiler\_boundary}

\begin{theorem}[CertificateCompiler target endpoint determinacy]
\label{thm:certificate-compiler-target-endpoint-determinacy}
Let $K=(S,T,G,E,C,H,P,N)$ be an accepted $\CertificateCompilerUp$ packet. Once
the source row $S$, a displayed graph edge in $G$, its landing row $E$, the
classifier transport row $H$, and the continuation row $C$ are fixed, the
target endpoint route to $T$ is determined as the finite route through
$S,G,E,H,C,P,N$. No second endpoint route can be obtained from host compiler
semantics, optimiser correctness, ambient program equality, or an unrecorded
target behavior.
\end{theorem}
\begin{proof}
Start from \autoref{thm:certificate-compiler-target-endpoint-route}. It
identifies every target arrival with the displayed source, graph, landing,
classifier, continuation, package, and local naming rows.

Use \autoref{thm:certificate-compiler-edge-classifier-exactness}. With the
source row, graph edge, landing row, classifier transport, and continuation
row fixed, the edge has no further classifier-facing or endpoint-facing
coordinate to vary.

Finally apply \autoref{thm:certificate-compiler-finite-route-non-escape}. The
carrier exports no external compiler semantics or target behavior, so the
fixed displayed rows determine the endpoint route.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_target\_endpoint\_determinacy}

\begin{theorem}[CertificateCompiler endpoint-route pullback determinacy]
\label{thm:certificate-compiler-endpoint-route-pullback-determinacy}
Let a downstream route pass through $\CertificateCompilerUp$ into $\KernelMorphismUp$ and then into $\DecompilerRightInverseUp$. If two such routes have the same displayed source row, graph edge, landing row, classifier transport row, and $\Cont$ row, then their endpoint pullbacks are the same finite route through $S,G,E,H,C,P,N$. No host compiler semantics, optimiser correctness, ambient program equality, host decompiler function, or unrecorded target behavior can distinguish the pullbacks.
\end{theorem}
\begin{proof}
First apply target endpoint determinacy, \autoref{thm:certificate-compiler-target-endpoint-determinacy}. Agreement of the displayed source row, graph edge, landing row, classifier transport row, and continuation row fixes the compiler endpoint route through $S,G,E,H,C,P,N$.

Next use the KernelMorphism route scope, \autoref{thm:certificate-compiler-kernelmorphism-route-scope}. The morphism consumer can add only the graph-admission and classifier-lift rows licensed by $\KernelMorphismUp$, so it cannot separate two already fixed compiler endpoint routes.

Finally apply the KernelMorphism decompiler boundary, \autoref{thm:certificate-compiler-kernelmorphism-decompiler-boundary}. The decompiler-facing leg remains on the displayed compiler packet and inverse-boundary rows, leaving no coordinate for host compiler semantics, optimiser correctness, ambient equality, host decompiler functions, or unrecorded target behavior.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_endpoint\_route\_pullback\_determinacy}

\begin{theorem}[CertificateCompiler route composition exactness]
\label{thm:certificate-compiler-route-composition-exactness}
For accepted $\CertificateCompilerUp$ packets, the identity route, every
binary composite route, and every triple composite route use only the
displayed graph rows, landing rows, classifier transport rows, and $\Cont$
rows, together with the visible $\Pkg$ and local $\NameCert$ provenance. These
routes introduce no hidden program equality, optimiser correctness theorem,
ambient compiler semantics, or target behavior outside the finite packet.
\end{theorem}
\begin{proof}
For the identity and binary cases, apply
\autoref{thm:certificate-compiler-identity-composite-public-boundary}. The
identity packet reads the diagonal local naming rows, and the binary composite
reads the two displayed compiler packets and their shared middle landing.

For the triple case, use the same public boundary together with
\autoref{thm:certificate-compiler-edge-classifier-exactness}. The three graph
edges, two middle landing rows, ordered classifier transports, and composed
$\Cont$ route are exactly the rows visible to the consumer.

The finite non-escape theorem
\autoref{thm:certificate-compiler-finite-route-non-escape} excludes any
additional semantic coordinate. Hence all identity, binary, and triple
composition routes are exact over the displayed rows.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_route\_composition\_exactness}

\begin{theorem}[CertificateCompiler ledger cut elimination]
\label{thm:certificate-compiler-ledger-cut-elimination}
Let a downstream consumer read the public package supplied by
\autoref{thm:certificate-compiler-downstream-stage-unblock-package}. An
intermediate package row may be cut from that consumer route only when the
same displayed endpoint rows and the same identity, binary, or triple
composite rows remain preserved in the resulting route. A cut that changes
the endpoint route, changes the composite route, or replaces either route by
host compiler semantics, decompiler correctness, ambient program equality, or
unrecorded target behavior is not a valid $\CertificateCompilerUp$ consumer
read.
\end{theorem}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_ledger\_cut\_elimination}
\begin{proof}
First read the downstream package through
\autoref{thm:certificate-compiler-downstream-stage-unblock-package}. The
consumer-visible packet consists only of finite $\BHist$ rows, displayed
$\hsame$ transport, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$
rows.

Endpoint preservation is forced by
\autoref{thm:certificate-compiler-target-endpoint-route}. If a cut removes an
intermediate package row but keeps the same source, graph, landing,
classifier, continuation, provenance, and local naming rows, the target route
is still the displayed finite route.

Composite preservation is forced by
\autoref{thm:certificate-compiler-identity-composite-public-boundary} and
\autoref{thm:certificate-compiler-route-composition-exactness}. Identity,
binary, and triple composites remain valid exactly when their displayed graph,
landing, classifier, and continuation rows are unchanged.

Finally use \autoref{thm:certificate-compiler-finite-route-non-escape}. Any
cut that replaces a displayed row by host semantics, decompiler correctness,
ambient equality, or unrecorded target behavior leaves the finite packet
surface and is therefore unavailable to downstream consumers.
\end{proof}

\begin{theorem}[CertificateCompiler endpoint decompiler handoff determinacy]
\label{thm:certificate-compiler-endpoint-decompiler-handoff-determinacy}
Let $K=(S,T,G,E,C,H,P,N)$ be an accepted $\CertificateCompilerUp$ endpoint route. Once the displayed rows $S,G,E,H,C,P,N$ and the $\KernelMorphismUp$ graph-admission and classifier-lift rows are fixed, any $\DecompilerRightInverseUp$ consumer can read only the inverse-boundary rows already cited by \autoref{thm:certificate-compiler-kernelmorphism-decompiler-boundary}. The handoff cannot introduce host compiler semantics, optimiser correctness, ambient program equality, host decompiler functions, or unrecorded target behavior.
\end{theorem}
\begin{proof}
First use target endpoint determinacy, \autoref{thm:certificate-compiler-target-endpoint-determinacy}. With the source row, graph edge, landing row, classifier transport, continuation row, provenance, and local naming row fixed, the endpoint route is the displayed finite route through $S,G,E,H,C,P,N$.

Next apply route composition exactness and ledger cut elimination, \autoref{thm:certificate-compiler-route-composition-exactness} and \autoref{thm:certificate-compiler-ledger-cut-elimination}. Identity, binary, and triple composites may be replayed only over the same displayed endpoint rows, and no intermediate cut can replace them by host semantics or unrecorded behavior.

Now pass through the KernelMorphism route scope, \autoref{thm:certificate-compiler-kernelmorphism-route-scope}. The morphism leg contributes only the graph-admission and classifier-lift rows licensed by $\KernelMorphismUp$.

Finally apply the KernelMorphism decompiler boundary, \autoref{thm:certificate-compiler-kernelmorphism-decompiler-boundary}, together with the decompiler right-inverse non-escape boundary of \autoref{thm:decompiler-right-inverse-non-escape-boundary}. A downstream inverse consumer therefore receives only the displayed compiler packet, the fixed KernelMorphism rows, and the inverse-boundary rows, with no coordinate for host compiler semantics, optimiser correctness, ambient equality, host decompiler functions, or unrecorded target behavior.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_endpoint\_decompiler\_handoff\_determinacy}

\begin{theorem}[CertificateCompiler endpoint-composite determinacy]
\label{thm:certificate-compiler-endpoint-composite-determinacy}
Let $K=(S,T,G,E,C,H,P,N)$ be an accepted $\CertificateCompilerUp$ packet. Once the displayed source row, graph edge, landing row, classifier transport, continuation row, and identity, binary, and triple composite rows are fixed, every downstream $\KernelMorphismUp$ or $\DecompilerRightInverseUp$ consumer route is determined by the finite packet $S,G,E,H,C,P,N$ together with the relevant composite rows. The determined route exports no host compiler semantics, optimiser correctness theorem, decompiler correctness theorem, ambient program equality, or target behavior outside those finite rows.
\end{theorem}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_endpoint\_composite\_determinacy}
\begin{proof}
Start with the endpoint route and public composite boundary. The target endpoint route of \autoref{thm:certificate-compiler-target-endpoint-route} fixes the source, graph, landing, classifier transport, continuation, provenance, and local naming rows, while \autoref{thm:certificate-compiler-identity-composite-public-boundary} fixes identity, binary, and triple composites on the same displayed surface.

Next expose the downstream packet. Apply the downstream stage unblock package, \autoref{thm:certificate-compiler-downstream-stage-unblock-package}, together with KernelMorphism route scope, \autoref{thm:certificate-compiler-kernelmorphism-route-scope}. A morphism consumer can read only the finite compiler route and the corresponding graph-admission and classifier-lift rows.

Now attach the decompiler boundary. The KernelMorphism decompiler boundary, \autoref{thm:certificate-compiler-kernelmorphism-decompiler-boundary}, keeps any route into $\DecompilerRightInverseUp$ on the same finite compiler packet before the inverse-boundary rows are read.

Finally use target endpoint determinacy, route composition exactness, and ledger cut elimination, \autoref{thm:certificate-compiler-target-endpoint-determinacy}, \autoref{thm:certificate-compiler-route-composition-exactness}, and \autoref{thm:certificate-compiler-ledger-cut-elimination}. These identify all endpoint and composite reads after the listed rows are fixed, so every downstream morphism or decompiler route is determined by $S,G,E,H,C,P,N$ plus the relevant composite rows, with no remaining semantic coordinate available.
\end{proof}

\begin{theorem}[CertificateCompiler endpoint KernelMorphism roundtrip]
\label{thm:certificate-compiler-endpoint-kernelmorphism-roundtrip}
Let $K=(S,T,G,E,C,H,P,N)$ be an accepted finite $\CertificateCompilerUp$ endpoint route. If the source and target endpoint reads agree under \autoref{thm:certificate-compiler-target-endpoint-determinacy}, then the route factors through the $\KernelMorphismUp$ graph-admission and classifier-lift scope and returns through the $\DecompilerRightInverseUp$ boundary on the same displayed compiler rows.

The roundtrip uses only $S,G,E,H,C,P,N$, the relevant identity, binary, or triple composite rows, the $\KernelMorphismUp$ graph-admission and classifier-lift rows, and the $\DecompilerRightInverseUp$ inverse-boundary rows. It adds no hidden host compiler semantics, optimiser correctness theorem, decompiler correctness theorem, ambient program equality, total decompiler, or unrecorded target behavior.
\end{theorem}
\begin{proof}
First apply target endpoint determinacy, \autoref{thm:certificate-compiler-target-endpoint-determinacy}. Agreement of the source and target endpoint reads fixes the displayed finite endpoint route through $S,G,E,H,C,P,N$.

Next pass through the morphism surface using \autoref{thm:certificate-compiler-kernelmorphism-route-scope}. A downstream $\KernelMorphismUp$ consumer can read only the graph-admission and classifier-lift rows attached to the same compiler packet, so the route has no host compiler coordinate at the morphism stage.

Now return through the decompiler side by \autoref{thm:certificate-compiler-kernelmorphism-decompiler-boundary}. The decompiler-facing consumer remains on the displayed compiler packet together with the inverse-boundary rows, rather than importing host decompiler behavior or total decompiler data.

Finally compose the route and remove intermediate package cuts. The composition exactness theorem, \autoref{thm:certificate-compiler-route-composition-exactness}, keeps identity, binary, and triple composites on the displayed rows, while ledger cut elimination, \autoref{thm:certificate-compiler-ledger-cut-elimination}, permits only cuts preserving those endpoint and composite rows. Thus the endpoint, KernelMorphism, and decompiler legs form a finite roundtrip with no hidden host compiler semantics.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_endpoint\_kernelmorphism\_roundtrip}

\begin{corollary}[CertificateCompiler target endpoint consumer determinacy]
\label{cor:certificate-compiler-target-endpoint-consumer-determinacy}
Let
$$
K=(S,T,G,E,C,H,P,N)
$$
be an accepted $\CertificateCompilerUp$ packet. Fix the consumer-visible rows cited by \autoref{thm:certificate-compiler-endpoint-route-determinacy-envelope}: the source row $S$, target row $T$, compiler graph $G$, target landing row $E$, continuation row $C$, classifier transport row $H$, package provenance row $P$, local naming row $N$, and, when a composite is requested, the displayed identity, binary, or triple-composite ledger rows over the same surface. Then any accepted consumer-facing use of $K$ must select the same finite endpoint route, the same composite normal form, or the same classifier-exactness decision named by the $\CertificateCompilerUp$ chapter. It cannot select an alternate hidden coordinate, a second source classifier row, host compiler semantics, optimiser correctness, ambient program equality, or unrecorded target behavior.
\end{corollary}
\begin{proof}
First apply \autoref{thm:certificate-compiler-target-endpoint-route}. It exhausts every target arrival by the finite $\BHist$ route through the displayed source naming row, graph row, target landing row, classifier transport row, continuation row, package provenance row, and local naming row. Thus a consumer that asks for the target endpoint has no endpoint coordinate beyond $S,G,E,H,C,P,N$.

Next apply \autoref{thm:certificate-compiler-endpoint-route-determinacy-envelope}. It says that accepted endpoint routes and accepted identity, binary, and triple-composite routes are all determined by the same displayed finite surface $S,T,G,E,C,H,P,N$. Hence fixing the cited local rows also fixes the composite normal form available to a consumer: the identity normal form is the diagonal row, a binary normal form is the displayed two-packet composite through the shared middle landing, and a triple normal form is the displayed three-edge ledger with its two middle landing rows.

It remains to rule out duplicate classifier sources and alternate exactness decisions. By \autoref{thm:certificate-compiler-edge-classifier-exactness}, an accepted classifier decision reads the source classifier row in $S$, lands through $E$, transports through the componentwise $\hsame$ row $H$, and follows the paired $\Cont$ routes in $C$. With $S,E,H,C$ fixed, there is no second classifier-facing source from which a different decision could be accepted.

Finally use \autoref{thm:certificate-compiler-finite-route-non-escape}. The accepted carrier exports no host compiler semantics, optimiser correctness theorem, ambient program equality, or unrecorded target behavior. Any consumer-facing use that agrees on the displayed rows therefore factors through the same finite route surface and must return the same endpoint route, composite normal form, or classifier-exactness decision named above.
\end{proof}
\leanchecked{BEDC.Derived.CertificateCompilerUp.CertificateCompilerCarrier\_target\_endpoint\_consumer\_determinacy}
