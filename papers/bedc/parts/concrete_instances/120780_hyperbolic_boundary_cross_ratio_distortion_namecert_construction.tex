\chapter{A Concrete Naming Certificate for $\HyperbolicBoundaryCrossRatioDistortionUp$}
\label{ch:concrete-instances-hyperbolic-boundary-cross-ratio-distortion-namecert}
\origin{ai}

The $\HyperbolicBoundaryCrossRatioDistortionUp$ packet records boundary cross-ratio distortion as finite hyperbolic phase data. It sits beside the cross-ratio route of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, the visual metric route of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the Busemann-function route of \autoref{ch:concrete-instances-hyperbolic-busemann-function-namecert}, and the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}. The carrier is a NameCert surface for distortion rows; it is not a host hyperbolic boundary, a Mobius classification theorem, a quotient boundary equality, an unbounded real-valued invariant, or a global geometry oracle.

\begin{definition}[Hyperbolic boundary cross-ratio distortion carrier]
\label{def:hyperbolic-boundary-cross-ratio-distortion-carrier}
A $\HyperbolicBoundaryCrossRatioDistortionUp$ carrier is a finite $\BHist$ packet
$$
D=(X,V,B,S,E,T,R,P,H,C,L,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\HyperbolicCrossRatioUp$ row for the inspected boundary quadruple.
\item $V$ is the $\HyperbolicVisualMetricUp$ row supplying the visual-metric comparison window.
\item $B$ is the $\HyperbolicBusemannFunctionUp$ row used to read boundary displacement through finite Busemann data.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row naming the shadow window where the distortion is inspected.
\item $E$ is the terminal $\RealUp$ seal row for the displayed distortion read.
\item $T$ is the transport row connecting the cross-ratio, visual-metric, Busemann, and boundary-shadow reads.
\item $R$ is the replay row recording that the same boundary quadruple and shadow window are used throughout the packet.
\item $P$ is the provenance row over the hyperbolic boundary, metric, Busemann, shadow, and Real-seal sources.
\item $H$ records componentwise $\hsame$ transport for all analytic and structural rows.
\item $C$ records displayed $\Cont$ replay of the local distortion route.
\item $L$ records that later Lean TasteGate realizations must include \texttt{Nontrivial} and \texttt{FieldFaithful} instances before the distortion row is accepted as non-vacuous and source-faithful.
\item $N$ is the local $\NameCert_{\HyperbolicBoundaryCrossRatioDistortionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,V,B,S,E,T,R,P,H,C,L,N$. It has no coordinate for global boundary compactness, host Mobius equivalence, quotient boundary equality, hidden field choice, selected geodesics, or ambient hyperbolic geometry.
\end{definition}

\begin{theorem}[Hyperbolic boundary cross-ratio distortion obligations]
\label{thm:hyperbolic-boundary-cross-ratio-distortion-obligations}
For every accepted $\HyperbolicBoundaryCrossRatioDistortionUp$ carrier $D=(X,V,B,S,E,T,R,P,H,C,L,N)$, every distortion read exposes the cross-ratio row $X$, visual metric row $V$, Busemann row $B$, boundary-shadow row $S$, Real-seal row $E$, transport row $T$, replay row $R$, provenance row $P$, componentwise transport row $H$, displayed continuation row $C$, TasteGate instance row $L$, and local naming row $N$.

The read is accepted only as this finite packet. It cannot be replaced by host boundary equality, a Mobius-normal-form theorem, unbounded real comparison, selected geodesics, quotient boundary compactification, or an ambient global-geometry principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-boundary-cross-ratio-distortion-carrier}. The analytic rows are the cross-ratio, visual-metric, Busemann, boundary-shadow, and Real-seal rows $X,V,B,S,E$.

The structural rows keep those analytic reads attached to one boundary packet. Transport $T$ and replay $R$ preserve the same boundary quadruple and shadow window, while provenance $P$, componentwise transport $H$, displayed continuation $C$, and local naming $N$ record the NameCert route.

The instance row $L$ is part of the acceptance surface. It requires later Lean TasteGate material to carry \texttt{Nontrivial} and \texttt{FieldFaithful} instances before the distortion read can be accepted as non-vacuous and source-faithful.

No coordinate remains for a host boundary quotient, Mobius classification theorem, selected geodesic, hidden field choice, or global hyperbolic principle. The listed rows therefore exhaust the local obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicBoundaryCrossRatioDistortionUp$ distortion read exposes cross-ratio, visual metric, Busemann, boundary-shadow, Real-seal, transport, replay, provenance, and local naming rows before the read is public.}

\independenceWitness{hyperbolic-cross-ratio, hyperbolic-visual-metric, hyperbolic-busemann-function, hyperbolic-boundary-shadow: cross-ratio records boundary quadruple comparison without a distortion ledger; visual metric records a metric window without the quadruple distortion row; Busemann functions record boundary displacement without the cross-ratio row; and boundary shadows record finite shadow windows without Real-sealed distortion. None is bijective to $\HyperbolicBoundaryCrossRatioDistortionUp$.}

\closureat{HyperbolicBoundaryCrossRatioDistortionUp}{seedStr}

\begin{closurestatus}{\HyperbolicBoundaryCrossRatioDistortionUp}
  \constructivestory{A $\HyperbolicBoundaryCrossRatioDistortionUp$ packet is generated from HyperbolicCrossRatio, HyperbolicVisualMetric, HyperbolicBusemannFunction, HyperbolicBoundaryShadow, RealUp seal, transport, replay, provenance, hsame, Cont, TasteGate instance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-boundary-cross-ratio-distortion-carrier} and \autoref{thm:hyperbolic-boundary-cross-ratio-distortion-obligations} seed the finite boundary cross-ratio distortion carrier and its NameCert obligation surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, host boundary equality, Mobius classification, selected geodesics, quotient boundary compactification, global hyperbolic geometry, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cross-ratio, visual-metric, Busemann, boundary-shadow, Real-seal, transport, replay, provenance, instance, and local NameCert exactness for BEDC.Derived.HyperbolicBoundaryCrossRatioDistortionUp.}
\end{closurestatus}
