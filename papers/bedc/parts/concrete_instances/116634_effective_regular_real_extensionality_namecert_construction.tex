\chapter{A Concrete Naming Certificate for $\EffectiveRegularRealExtensionalityUp$}
\label{ch:concrete-instances-effective-regular-real-extensionality-namecert}
\origin{human}

Following Bishop and Bridges, and the computable-analysis reading of effective regular names, the $\EffectiveRegularRealExtensionalityUp$ packet records the finite substitution surface where two displayed regular real names may be reused extensionally without selecting quotient representatives. It sits after the regular Cauchy equality surface of \autoref{ch:concrete-instances-regular-cauchy-extensionality-namecert}, the Bishop real equality surface of \autoref{ch:concrete-instances-bishop-real-extensionality-namecert}, the entourage basis of \autoref{ch:concrete-instances-real-uniform-entourage-basis-namecert}, and the Cauchy filter subbasis of \autoref{ch:concrete-instances-cauchy-filter-subbasis-namecert}. It also consumes the finite stream and regular-rational rows of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Effective regular real extensionality carrier]
\label{def:effective-regular-real-extensionality-carrier}
An $\EffectiveRegularRealExtensionalityUp$ carrier is a finite $\BHist$ packet
$$
E=(R_0,R_1,S_0,S_1,D_0,D_1,W_0,W_1,A_0,A_1,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R_0,R_1$ are two $\RegSeqRatUp$ regular readbacks presented as the effective names being compared.
\item $S_0,S_1$ are the two finite $\StreamNameUp$ windows through which the comparison is inspected.
\item $D_0,D_1$ are the $\DyadicRatCoreUp$ tolerance ledgers recording the precision requests and endpoint bounds for the two windows.
\item $W_0,W_1$ are effective witness-budget rows, each naming the finite requests needed to replay the displayed comparison at the requested precision.
\item $A_0,A_1$ are the two terminal $\RealUp$ seal rows exposed only after the regular readbacks, stream windows, dyadic tolerance ledgers, and witness budgets have been displayed.
\item $H$ records componentwise $\hsame$ transport for both readbacks, both windows, both dyadic ledgers, both witness budgets, both real seals, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the finite windows through the regular readbacks and dyadic tolerance ledgers into the two real seals.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyExtensionalityUp$, $\BishopRealExtensionalityUp$, $\RealUniformEntourageBasisUp$, $\CauchyFilterSubbasisUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EffectiveRegularRealExtensionalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R_0,R_1,S_0,S_1,D_0,D_1,W_0,W_1,A_0,A_1,H,C,P,N$. It has no coordinate for quotient real equality, a selected representative, countable choice, trichotomy, ambient Cauchy completion, or host real equality.
\end{definition}

\begin{theorem}[Effective regular real extensionality NameCert obligations]
\label{thm:effective-regular-real-extensionality-namecert-obligations}
For every accepted $\EffectiveRegularRealExtensionalityUp$ carrier $E=(R_0,R_1,S_0,S_1,D_0,D_1,W_0,W_1,A_0,A_1,H,C,P,N)$, the local $\NameCert_{\EffectiveRegularRealExtensionalityUp}$ surface consists exactly of carrier admission, exposure of the two regular readbacks, exposure of the two finite stream windows, exposure of the two dyadic tolerance ledgers, exposure of the two effective witness-budget rows, exposure of the two terminal $\RealUp$ seal rows, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:effective-regular-real-extensionality-carrier}. The analytic rows are precisely the two regular readbacks, two finite windows, two dyadic ledgers, two witness-budget rows, and two real seals.

The structural rows preserve the same packet. The row $H$ transports each displayed coordinate by $\hsame$, $C$ replays the route from window evidence to readback and seal evidence, $P$ records the cited siblings, and $N$ names the local certificate.

Since the carrier contains no further coordinate, every local NameCert obligation is one of those rows. The excluded quotient, representative-selection, choice, and host-equality principles cannot be exported from the packet.
\end{proof}

\begin{theorem}[Effective regular real computable handoff]
\label{thm:effective-regular-real-extensionality-computable-handoff}
Every computable consumer of an accepted $\EffectiveRegularRealExtensionalityUp$ packet may reuse a regular real name only through the displayed route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp .
$$
The handoff is witnessed by the two finite stream windows, the two regular readbacks, the two dyadic tolerance ledgers, and the two effective witness-budget rows; it is not a request for a quotient representative or a global equality decision.
\end{theorem}
\begin{proof}
Read the two finite windows $S_0,S_1$ and their regular readbacks $R_0,R_1$. The dyadic ledgers $D_0,D_1$ supply the finite precision data, while $W_0,W_1$ record the effective budgets needed to replay the comparison at those precisions.

The route then reaches $A_0,A_1$ only through displayed replay. Thus a computable consumer can reuse the regular-name data by following the finite window-readback-ledger route.

No selected representative or global equality decision appears in the carrier. The exported handoff is therefore a finite effective substitution surface rather than a quotient-real principle.
\end{proof}

\begin{theorem}[Effective regular real Real-seal boundary]
\label{thm:effective-regular-real-extensionality-real-seal-boundary}
The two $\RealUp$ seal rows of an accepted $\EffectiveRegularRealExtensionalityUp$ carrier are boundary rows for finite regular-name substitution. A consumer reaches $A_0,A_1$ only after the corresponding stream windows, regular readbacks, dyadic ledgers, witness budgets, transport, replay, provenance, and local name have been displayed.
\end{theorem}
\begin{proof}
Use the carrier order. The seals $A_0,A_1$ are stored beside the window, readback, dyadic, and witness-budget rows, and the replay row $C$ is the only displayed route by which a consumer reaches them.

Transport by $H$ does not add a hidden seal source; it preserves the same coordinates. Provenance $P$ and local naming $N$ identify the packet but do not manufacture quotient equality or representative selection.

Therefore the real seals mark the boundary of the finite substitution certificate. Any consumer that bypasses the displayed rows is outside the $\EffectiveRegularRealExtensionalityUp$ surface.
\end{proof}

\falsifiablePrediction{Every accepted $\EffectiveRegularRealExtensionalityUp$ packet displays two regular readbacks, two StreamName windows, two DyadicRatCore tolerance ledgers, two effective witness budgets, two RealUp seals, hsame transport, Cont replay, Pkg provenance, and a local NameCert row before extensional regular-name reuse is exported.}

\independenceWitness{regular-cauchy-extensionality, bishop-real-extensionality, real-uniform-entourage-basis, cauchy-filter-subbasis, streamname, regseqrat, dyadicratcore, real: $\EffectiveRegularRealExtensionalityUp$ is not bijective to any listed sibling because it jointly carries two effective regular-name readbacks, two finite stream windows, two tolerance ledgers, two witness budgets, two real seals, transport, replay, provenance, and local naming.}

\closureat{EffectiveRegularRealExtensionalityUp}{seedStr}

\begin{closurestatus}{\EffectiveRegularRealExtensionalityUp}
  \constructivestory{An $\EffectiveRegularRealExtensionalityUp$ packet is generated from two regular readbacks, two finite StreamName windows, two DyadicRatCore tolerance ledgers, two effective witness budgets, two RealUp seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:effective-regular-real-extensionality-carrier}, \autoref{thm:effective-regular-real-extensionality-namecert-obligations}, \autoref{thm:effective-regular-real-extensionality-computable-handoff}, and \autoref{thm:effective-regular-real-extensionality-real-seal-boundary} seed the finite effective extensional-substitution packet for regular real names.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.EffectiveRegularRealExtensionalityUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected representatives, countable choice, trichotomy, ambient Cauchy completion, host real equality, theorem-checked Lean obligations, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, two-window readback, dyadic tolerance routing, witness-budget replay, Real seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.EffectiveRegularRealExtensionalityUp.}
\end{closurestatus}
\leanstmt{BEDC.Derived.EffectiveRegularRealExtensionalityUp}
