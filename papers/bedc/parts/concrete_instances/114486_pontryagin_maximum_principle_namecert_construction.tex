\chapter{A Concrete Naming Certificate for $\PontryaginMaximumPrincipleUp$}
\label{ch:concrete-instances-pontryagin-maximum-principle-namecert}
\origin{human}

Following Pontryagin, Boltyanskii, Gamkrelidze, and Mishchenko, and the optimal-control textbook route through Hamiltonian stationarity, the $\PontryaginMaximumPrincipleUp$ packet records only the finite certificate surface by which a control problem exposes state dynamics, endpoint cost, adjoint rows, a Hamiltonian comparison, and transversality data before any global existence theorem or selected optimizer is read. It sits after the differential-equation route of \autoref{ch:concrete-instances-ode-namecert}, the dynamic-system rows of \autoref{ch:concrete-instances-dynsystem-namecert}, the derivative and variational rows of \autoref{ch:concrete-instances-calculus-namecert}, the convex control rows of \autoref{ch:concrete-instances-convexset-namecert}, the scalar seal of \autoref{ch:concrete-instances-real-namecert}, and the finite vector-space rows of \autoref{ch:concrete-instances-vecspace-namecert}. The packet names a displayed maximum-principle handoff; it does not assert existence of optimal controls, measurable-selection closure, smoothness beyond the displayed rows, host Euler--Lagrange completeness, or a global phase-space theorem.

\begin{definition}[Pontryagin maximum-principle carrier]
\label{def:pontryagin-maximum-principle-carrier}
A $\PontryaginMaximumPrincipleUp$ carrier is a finite $\BHist$ packet
$$
P=(X,U,F,J,A,H,T,R,V,Q,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\OdeUp$ state-equation row, recording the finite state window, endpoint rows, and local flow witnesses under inspection.
\item $U$ is the admissible-control row, read through $\ConvexSetUp$ when the inspected control window carries convex constraints and otherwise only as the displayed finite admissibility ledger.
\item $F$ is the $\DynSystemUp$ rollout row tying the state and control windows to the same finite transition route.
\item $J$ is the $\RealUp$ cost row, storing the displayed objective, endpoint contribution, and finite comparison budget.
\item $A$ is the adjoint row. It records the covector or multiplier window in the same $\VecSpaceUp$ coordinates as the state and control rows.
\item $H$ is the Hamiltonian row, storing the finite expression obtained from the state, control, cost, and adjoint rows.
\item $T$ is the transversality row, containing only the displayed endpoint boundary relation attached to $X,J,A$.
\item $R$ is the Real-seal row for the Hamiltonian comparison and endpoint cost comparison.
\item $V$ stores componentwise $\hsame$ transport for state, control, rollout, cost, adjoint, Hamiltonian, transversality, Real-seal, replay, provenance, and local naming rows.
\item $Q$ stores displayed $\Cont$ replay from an optimal-control query through $X,U,F,J,A,H,T,R,V$.
\item $C$ is the convexity and local-variation ledger, recording only the finite variation rows needed to compare admissible controls in the displayed window.
\item $G$ is the $\Pkg$ provenance over $\PontryaginMaximumPrincipleUp$, $\OdeUp$, $\DynSystemUp$, $\CalculusUp$, $\ConvexSetUp$, $\RealUp$, $\VecSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PontryaginMaximumPrincipleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected optimal control, a host measurable-selection theorem, ambient smooth manifold completeness, quotient trajectory equality, or a global existence theorem.
\end{definition}

\begin{theorem}[Pontryagin maximum-principle NameCert obligations]
\label{thm:pontryagin-maximum-principle-namecert-obligations}
For every accepted $\PontryaginMaximumPrincipleUp$ carrier
$$
P=(X,U,F,J,A,H,T,R,V,Q,C,G,N),
$$
the local $\NameCert_{\PontryaginMaximumPrincipleUp}$ surface consists exactly of state-equation admission, admissible-control exposure, dynamic rollout, Real-valued cost readback, adjoint-row exposure, Hamiltonian construction, transversality exposure, finite variation comparison, Real-seal ordering, componentwise transport, displayed replay, provenance, local naming, and nonescape for selected optimizers, host measurable selection, quotient trajectory equality, ambient smoothness, and global existence.
\end{theorem}
\begin{proof}
Project \autoref{def:pontryagin-maximum-principle-carrier}. The accepted inventory is $X,U,F,J,A,H,T,R,V,Q,C,G,N$, so every readable optimal-control datum is one of those displayed rows.

The analytic route is finite. The state and rollout rows $X,F$ are read before the control and cost rows $U,J$ can be compared, and the adjoint row $A$ is attached to the same vector-space and calculus windows. The Hamiltonian row $H$ is then computed only from those already displayed rows.

The endpoint and stationarity boundaries are also local. Row $T$ records the transversality relation, row $C$ records the finite variation ledger, and $R$ seals only the displayed Real comparisons. The structural rows $V,Q,G,N$ transport, replay, source, and name that route without adding selected optimizers, measurable selection, quotient trajectories, ambient smoothness, or global existence.
\end{proof}

\begin{theorem}[Pontryagin maximum-principle Hamiltonian handoff]
\label{thm:pontryagin-maximum-principle-hamiltonian-handoff}
Every accepted $\PontryaginMaximumPrincipleUp$ packet reads a Hamiltonian stationarity surface only through the finite route
$$
\OdeUp \longrightarrow \DynSystemUp \longrightarrow \CalculusUp \longrightarrow \ConvexSetUp \longrightarrow \VecSpaceUp \longrightarrow \RealUp .
$$
In carrier notation, the Hamiltonian row $H$ is admissible only after the state-equation row $X$, admissible-control row $U$, rollout row $F$, cost row $J$, adjoint row $A$, variation ledger $C$, and terminal Real-seal row $R$ have been displayed. The handoff exports no selected optimizer, no host maximum theorem, no measurable-selection closure, and no global phase-space completion.
\end{theorem}
\begin{proof}
Start from an accepted carrier $P=(X,U,F,J,A,H,T,R,V,Q,C,G,N)$. The state endpoint is $X$, and the finite dynamic rollout is $F$, so a stationarity read cannot begin from an unrecorded phase-space object.

Read the control, cost, and adjoint data next. The admissible-control row $U$ is constrained by the displayed convex or finite admissibility ledger, $J$ stores the Real-valued objective window, and $A$ stores the adjoint data in the same vector-space coordinates used by the state rows.

The Hamiltonian handoff is then the displayed row $H$ together with the variation ledger $C$, transversality row $T$, and Real seal $R$. Since the carrier contains no selected optimizer, measurable-selection theorem, host maximum theorem, or global completion row, the exported stationarity surface is exactly the finite route stated above.
\end{proof}

\falsifiablePrediction{Every $\PontryaginMaximumPrincipleUp$ consumer that reaches Hamiltonian stationarity must first display state-equation, admissible-control, dynamic-rollout, Real-cost, adjoint, Hamiltonian, transversality, variation-ledger, Real-seal, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{ode, dynsystem, calculus, convexset, real, vecspace: $\PontryaginMaximumPrincipleUp$ is not bijective to any listed sibling because it jointly carries a state equation, control constraint, dynamic rollout, Real cost, adjoint row, Hamiltonian row, transversality boundary, variation ledger, structural replay, provenance, and local naming.}

\closureat{PontryaginMaximumPrincipleUp}{seedStr}

\begin{closurestatus}{\PontryaginMaximumPrincipleUp}
  \constructivestory{A $\PontryaginMaximumPrincipleUp$ packet is generated from an OdeUp state row, a DynSystemUp rollout row, a displayed admissible-control row, a RealUp cost row, a CalculusUp variation row, a VecSpaceUp adjoint row, a Hamiltonian row, transversality data, Real-seal ordering, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pontryagin-maximum-principle-carrier}, \autoref{thm:pontryagin-maximum-principle-namecert-obligations}, and \autoref{thm:pontryagin-maximum-principle-hamiltonian-handoff} seed the traditional optimal-control maximum-principle packet over state dynamics, controls, adjoints, Hamiltonian comparison, endpoint data, and finite variation rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close existence of optimal controls, measurable-selection closure, global smooth phase-space theorems, host maximum principles, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, state-equation exposure, admissible-control exposure, rollout exactness, cost readback, adjoint exposure, Hamiltonian handoff, transversality exposure, variation-ledger exactness, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.PontryaginMaximumPrincipleUp.}
\end{closurestatus}
