\chapter{A Concrete Naming Certificate for $\RiemannCauchyRealNameUp$}
\label{ch:concrete-instances-riemann-cauchy-real-name-namecert}
\origin{human}

Following Riemann and Bishop--Bridges, the $\RiemannCauchyRealNameUp$ packet records a real name as a finite BEDC certificate around rational fundamental sequences rather than as a host real. It sits between the finite stream discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational sequence route of \autoref{ch:concrete-instances-regseqrat-namecert}, the public real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and the regularizing handoff of \autoref{ch:concrete-instances-regular-sequence-regularizer-namecert}. The packet exposes the traditional Cauchy fundamental-sequence surface only through finite windows, rational readback, dyadic moduli, transport, replay, provenance, and a local name.

\begin{definition}[Riemann--Cauchy real-name carrier]
\label{def:riemann-cauchy-real-name-carrier}
A $\RiemannCauchyRealNameUp$ carrier is a finite $\BHist$ packet
$$
X=(Q,S,W,D,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the rational source row for the fundamental sequence.
\item $S$ is the $\StreamNameUp$ row exposing finite sequence windows.
\item $W$ is the Cauchy-window schedule row for requested rational tolerances.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger bounding the window comparison.
\item $R$ is the $\RegSeqRatUp$ regular readback obtained from $S,W,D$.
\item $U$ is the regularizer row that presents the same name at the $\RealUp$ boundary.
\item $H$ records componentwise $\hsame$ transport for the source, stream, window, dyadic, readback, regularizer, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route $Q,S,W,D \longmapsto R \longmapsto U$.
\item $P$ is the $\Pkg$ provenance over $\RiemannCauchyRealNameUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RiemannCauchyRealNameUp}$ row.
\end{enumerate}
The carrier has no coordinate for a completed host real, quotient stream equality, a selected limit, trichotomy, countable choice, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[Riemann--Cauchy real-name NameCert obligations]
\label{thm:riemann-cauchy-real-name-namecert-obligations}
For every accepted $\RiemannCauchyRealNameUp$ carrier $X=(Q,S,W,D,R,U,H,C,P,N)$, the local $\NameCert_{\RiemannCauchyRealNameUp}$ surface consists exactly of carrier admission for $X$, rational fundamental-sequence exposure through $Q$, finite stream windows through $S$, Cauchy-window scheduling through $W$, dyadic tolerance accounting through $D$, regular rational readback through $R$, regularized $\RealUp$ boundary exposure through $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:riemann-cauchy-real-name-carrier}. Its first four rows expose only rational source data, finite stream windows, requested tolerances, and dyadic bounds.

The readback row $R$ is therefore reached through the same finite route used by the $\RegSeqRatUp$ chapter, and the regularizer row $U$ is the only public route from that readback to the $\RealUp$ boundary.

The structural rows $H,C,P,N$ transport, replay, source, and name the same packet. Since no further coordinate is present, the local certificate cannot ask for a selected host limit, quotient stream equality, countable choice, trichotomy, or ambient completeness.
\end{proof}

\begin{theorem}[Riemann--Cauchy real-name RegSeqRat factorization]
\label{thm:riemann-cauchy-real-name-regseqrat-factorization}
Every accepted $\RiemannCauchyRealNameUp$ consumer that reads the public real-name boundary must first factor through the finite route
$$
Q,S,W,D \longmapsto R \longmapsto U
$$
where $R$ is the displayed $\RegSeqRatUp$ readback and $U$ is the regularized $\RealUp$ boundary row. The consumer receives no host real equality, quotient stream equality, selected limit, detached completion route, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:riemann-cauchy-real-name-namecert-obligations}. They order the rational source, finite windows, Cauchy-window schedule, and dyadic tolerance ledger before any regular readback is available.

The row $R$ is the unique regular rational readback inside the packet. Any consumer of the $\RealUp$ boundary must therefore pass through the $\RegSeqRatUp$ route before it can read the regularizer row $U$.

Finally inspect the refused coordinates. The carrier does not contain a quotient stream, selected limit, host real, choice schedule, detached completion route, or ambient completeness row, so none can replace the displayed $Q,S,W,D,R,U$ factorization.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\RiemannCauchyRealNameUp$ real-name boundary while omitting the rational source, finite stream window, Cauchy-window schedule, dyadic tolerance ledger, $\RegSeqRatUp$ readback, regularizer, $\hsame$, $\Cont$, $\Pkg$, or local $\NameCert$ rows refutes \autoref{def:riemann-cauchy-real-name-carrier}.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real, regular-sequence-regularizer: $\RiemannCauchyRealNameUp$ is not bijective to any listed sibling because it jointly carries a rational fundamental-sequence source, finite stream windows, Cauchy-window scheduling, dyadic tolerance accounting, regular readback, and a real-boundary regularizer. StreamNameUp lacks the tolerance ledger; DyadicRatCoreUp lacks the sequence route; RegSeqRatUp lacks the raw fundamental-sequence source; RealUp lacks the displayed Cauchy-window schedule; the regularizer chapter supplies a handoff without this full source packet.}

\closureat{RiemannCauchyRealNameUp}{seedStr}

\begin{closurestatus}{\RiemannCauchyRealNameUp}
  \constructivestory{A $\RiemannCauchyRealNameUp$ packet is generated from a rational source row, finite StreamName windows, a Cauchy-window schedule, a DyadicRatCore tolerance ledger, RegSeqRat readback, a Real-boundary regularizer, componentwise transport, displayed replay, provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:riemann-cauchy-real-name-carrier}, \autoref{thm:riemann-cauchy-real-name-namecert-obligations}, and \autoref{thm:riemann-cauchy-real-name-regseqrat-factorization} seed the finite Riemann--Cauchy real-name route over rational source rows, finite windows, dyadic tolerances, regular readback, real-boundary regularization, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real construction, selected limits, countable choice, trichotomy, host real equality, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window scheduling, dyadic tolerance accounting, RegSeqRat factorization, regularizer routing, transport, replay, provenance, and local naming for BEDC.Derived.RiemannCauchyRealNameUp.}
\end{closurestatus}
