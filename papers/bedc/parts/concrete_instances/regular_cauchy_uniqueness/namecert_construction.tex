\chapter{A Concrete Naming Certificate for $\RegularCauchyUniquenessUp$}
\label{ch:concrete-instances-regular-cauchy-uniqueness-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyUniquenessUp$ packet records the textbook uniqueness route for regular Cauchy names as finite BEDC data. It sits after the regular-rational name surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the comparison discipline of \autoref{ch:concrete-instances-cauchysequenceequivalence-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. Real-completion consumers such as \autoref{ch:concrete-instances-cauchy-completeness-witness-namecert} use $\RegularCauchyUniquenessUp$ to make the $\RealUp$ seal independent of the chosen $\RegSeqRatUp$ representative. The chapter only seeds the finite route from equality data for regular Cauchy names to the Real seal boundary.

\begin{definition}[Regular Cauchy uniqueness carrier]
\label{def:regular-cauchy-uniqueness-carrier}
A $\RegularCauchyUniquenessUp$ carrier is a finite $\BHist$ packet
$$
U=(X,Y,S,T,D,E,R,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ and $Y$ are paired $\RegSeqRatUp$ regular Cauchy name rows;
\item $S$ and $T$ are the finite $\StreamNameUp$ windows through which the two names are observed at the same requested tolerance;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the displayed radius, comparison budget, and rational-distance rows used by the uniqueness route;
\item $E$ is the $\CauchySequenceEquivalenceUp$ comparison row tying the paired windows to one finite equivalence decision;
\item $R$ is the $\RealUp$ seal row available only after the paired names, windows, dyadic tolerance ledger, and comparison row have been displayed;
\item $H$ stores componentwise $\hsame$ transport for $X,Y,S,T,D,E,R$ and the structural rows;
\item $C$ stores displayed $\Cont$ replay of the route
$$
X,Y \longmapsto S,T,D \longmapsto E \longmapsto R ;
$$
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchySequenceEquivalenceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchyUniquenessUp}$ row.
\end{enumerate}
The classifier compares two accepted carriers only by the displayed rows $X,Y,S,T,D,E,R,H,C,P,N$. It has no coordinate for quotient stream equality, a selected limit, countable choice, ambient Real completeness, or an unrecorded equality proof outside the packet.
\end{definition}

\begin{theorem}[Regular Cauchy uniqueness NameCert obligations]
\label{thm:regular-cauchy-uniqueness-namecert-obligations}
For an accepted $\RegularCauchyUniquenessUp$ carrier
$$
U=(X,Y,S,T,D,E,R,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyUniquenessUp}$ surface consists exactly of carrier admission for the paired $\RegSeqRatUp$ names, paired $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, $\CauchySequenceEquivalenceUp$ comparison row, $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming row; classifier stability under the same displayed rows; tolerance exactness through $D$; transport invariance through $H$; ledger visibility through $C$ and $P$; and Real-seal nonescape through $R$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-cauchy-uniqueness-carrier}. The projection exposes exactly the two regular Cauchy names, their finite windows, the dyadic tolerance ledger, the comparison row, the seal row, and the structural rows.

Classifier stability follows because the classifier is componentwise on the same displayed rows. Tolerance exactness is the projection of $D$, and ledger visibility is the fact that every consumer route in $C$ reads $X,Y,S,T,D,E,R$ before the provenance and naming rows.

Transport invariance is supplied by $H$: each transported accepted carrier keeps the same row roles. The Real-seal boundary is downstream from $E$, so no consumer can read $R$ without first passing through the finite comparison packet.
\end{proof}

\begin{theorem}[Regular Cauchy uniqueness Real-seal obligations]
\label{thm:regular-cauchy-uniqueness-seal-obligations}
Every accepted $\RegularCauchyUniquenessUp$ packet that exposes a $\RealUp$ seal must display the paired $\RegSeqRatUp$ rows $X,Y$, the paired $\StreamNameUp$ windows $S,T$, one $\DyadicRatCoreUp$ tolerance ledger $D$, and one $\CauchySequenceEquivalenceUp$ comparison row $E$ before the seal row $R$ is available. The seal obligation is therefore the ordered finite route
$$
X,Y \longmapsto S,T,D \longmapsto E \longmapsto R ,
$$
preserved by $H,C,P,N$ and not by quotient stream equality, selected limits, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-uniqueness-carrier}. The projection exposes exactly the paired regular Cauchy rows, paired stream windows, dyadic tolerance ledger, comparison row, and terminal seal row.

Use \autoref{thm:regular-cauchy-uniqueness-tolerance-exactness} to bind the comparison to the single row $D$. The windows $S,T$ and names $X,Y$ are opened at that same tolerance source before $E$ is read.

Now use \autoref{thm:regular-cauchy-uniqueness-real-seal-nonescape}. It places every $\RealUp$-facing read downstream from $E$ and excludes quotient stream equality, selected limits, host real equality, countable choice, and ambient completeness from the seal route. The structural rows $H,C,P,N$ transport, replay, source, and name that same finite obligation surface.
\end{proof}

\begin{theorem}[Regular Cauchy uniqueness tolerance exactness]
\label{thm:regular-cauchy-uniqueness-tolerance-exactness}
Every accepted comparison in a $\RegularCauchyUniquenessUp$ packet reads its tolerance data through the single displayed $\DyadicRatCoreUp$ row $D$. The paired $\StreamNameUp$ windows $S,T$ and paired $\RegSeqRatUp$ names $X,Y$ are opened at that row's radius and comparison budget, and the $\CauchySequenceEquivalenceUp$ row $E$ consumes no tolerance source outside $D$.

Consequently the uniqueness route exports no hidden metric estimate, quotient-stream equality, selected limit, or ambient Real-completeness argument.
\end{theorem}
\begin{proof}
Project the carrier and inspect the tolerance coordinate. The only dyadic radius and rational-distance data are stored in $D$.

The windows $S,T$ are tied to that coordinate by the continuation route $C$. Thus the two regular Cauchy names are observed through the same finite tolerance request before the comparison row is read.

The comparison row $E$ is downstream from $S,T,D$. Since no other tolerance coordinate is present in the carrier, an accepted comparison cannot be justified by an external estimate or by quotient equality of streams.
\end{proof}

\begin{theorem}[Regular Cauchy uniqueness transport invariance]
\label{thm:regular-cauchy-uniqueness-transport-invariance}
Let $U=(X,Y,S,T,D,E,R,H,C,P,N)$ be an accepted $\RegularCauchyUniquenessUp$ carrier. Componentwise $\hsame$ transport of the paired names, windows, tolerance ledger, comparison row, seal row, continuation route, provenance row, and local naming row produces another accepted carrier with the same uniqueness route. The transported classifier still reads only $X,Y,S,T,D,E,R,H,C,P,N$.
\end{theorem}
\begin{proof}
Read the transport row $H$. Its components move the two $\RegSeqRatUp$ names, the two $\StreamNameUp$ windows, the dyadic tolerance row, the comparison row, and the seal row together.

Replay the route through $C$ after transport. The same ordered route from the paired names to the windows, tolerance, comparison row, and seal row remains visible.

Repack the transported rows with the same provenance and local naming data. Since the classifier has no coordinate outside the displayed rows, the transported packet is accepted by the same finite rule.
\end{proof}

\begin{theorem}[Regular Cauchy uniqueness Real seal nonescape]
\label{thm:regular-cauchy-uniqueness-real-seal-nonescape}
Every $\RealUp$-facing read from an accepted $\RegularCauchyUniquenessUp$ packet factors through the paired $\RegSeqRatUp$ rows $X,Y$, the paired $\StreamNameUp$ windows $S,T$, the $\DyadicRatCoreUp$ tolerance row $D$, the $\CauchySequenceEquivalenceUp$ comparison row $E$, and only then the seal row $R$, with $H,C,P,N$ preserving the same route.

Thus the Real seal cannot escape to quotient stream equality, a selected limit, countable choice, ambient Real completeness, or any unrecorded host equality proof.
\end{theorem}
\begin{proof}
Start from a Real-facing read and project the continuation route $C$. The route lists the paired names, paired windows, dyadic tolerance row, and comparison row before the seal row $R$.

Use \autoref{thm:regular-cauchy-uniqueness-tolerance-exactness} to pin the comparison to $D$. Then use \autoref{thm:regular-cauchy-uniqueness-transport-invariance} to see that transport and replay preserve the same ordered finite rows.

The provenance and naming rows $P,N$ attach the route to the local packet. Because the carrier contains no quotient stream, selected limit, countable-choice, ambient-completeness, or host-equality coordinate, no Real-facing consumer can read such data from the seal.
\end{proof}

\closureat{RegularCauchyUniquenessUp}{seedStr}

\begin{closurestatus}{\RegularCauchyUniquenessUp}
  \constructivestory{A $\RegularCauchyUniquenessUp$ packet is generated from paired $\RegSeqRatUp$ regular Cauchy names, finite $\StreamNameUp$ windows, a $\DyadicRatCoreUp$ tolerance ledger, a $\CauchySequenceEquivalenceUp$ comparison row, a terminal $\RealUp$ seal, transport data, continuation replay, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-uniqueness-carrier}, \autoref{thm:regular-cauchy-uniqueness-namecert-obligations}, \autoref{thm:regular-cauchy-uniqueness-seal-obligations}, \autoref{thm:regular-cauchy-uniqueness-tolerance-exactness}, \autoref{thm:regular-cauchy-uniqueness-transport-invariance}, and \autoref{thm:regular-cauchy-uniqueness-real-seal-nonescape} name the L10 regular-Cauchy uniqueness route from finite RegSeqRat equality data through StreamName and DyadicRatCore tolerances to the Real seal boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, ambient Real completeness, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier surface, classifier stability, Real-seal obligations, tolerance exactness, transport invariance, ledger visibility, and Real-seal nonescape rows for the displayed uniqueness packet.}
\end{closurestatus}
