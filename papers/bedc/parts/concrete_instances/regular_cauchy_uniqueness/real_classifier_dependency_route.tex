The $\RegularCauchyUniquenessUp$ route belongs to the Real classifier lattice: before any terminal uniqueness consumer compares two real-name seals, it reads the source $\RegSeqRatUp$ rows of \autoref{ch:concrete-instances-regseqrat-namecert}, inspects equality through the $\RealNameClassifierUp$ boundary of \autoref{ch:concrete-instances-realnameclassifier-namecert}, and then consumes the $\RealUp$ seal rows already displayed by the carrier. This order keeps the terminal comparison attached to regular-rational source data and to the classifier inspection route rather than to quotient stream equality, selected representatives, host real equality, or ambient completeness.

\begin{proof}
Project the carrier rows and read the comparison from left to right. The regular Cauchy source rows provide the two names, the classifier boundary supplies the finite equality inspection, and the Real seal rows are consumed only after those entries have been displayed, so no uniqueness consumer receives an unrecorded terminal comparison.
\end{proof}
