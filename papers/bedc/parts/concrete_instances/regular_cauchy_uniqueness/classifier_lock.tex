\begin{theorem}[Regular Cauchy uniqueness classifier lock]
\label{thm:regular-cauchy-uniqueness-classifier-lock}
Let
$$
U=(X,Y,S,T,D,E,R,H,C,P,N)
$$
and
$$
U'=(X',Y',S',T',D',E',R',H',C',P',N')
$$
be accepted $\RegularCauchyUniquenessUp$ carriers. If a consumer reads the two terminal $\RealUp$ seal rows $R$ and $R'$ as the same regular Cauchy limit, then that read must factor through the displayed Cauchy-equivalence classifier before either seal is consumed.

Concretely, the read passes through the paired $\RegSeqRatUp$ rows $X,Y$ and $X',Y'$, the finite $\StreamNameUp$ windows $S,T$ and $S',T'$, the $\DyadicRatCoreUp$ tolerance ledgers $D,D'$, the comparison rows $E,E'$, componentwise $\hsame$ transport $H,H'$, displayed $\Cont$ replay $C,C'$, $\Pkg$ provenance $P,P'$, and the local $\NameCert$ rows $N,N'$. It receives no host quotient stream equality, no selected representatives, no countable choice, and no unrecorded equality proof outside the local packet rows.
\end{theorem}
\begin{proof}
Project both accepted carriers through \autoref{def:regular-cauchy-uniqueness-carrier}. The projection exposes the paired regular Cauchy names, finite stream windows, dyadic tolerance ledgers, comparison rows, terminal real seals, and structural rows on both sides.

Invoke the tolerance exactness row of \autoref{thm:regular-cauchy-uniqueness-tolerance-exactness}. It binds each comparison read to the displayed dyadic ledger before the comparison row can be consumed, so the two seal reads cannot be compared without first exposing the same kind of finite tolerance source.

Next use the completion-consumer route following the chapter closure block. That route says every $\CauchyCompletenessWitnessUp$ consumer reads $\RegularCauchyUniquenessUp$ through the carrier, tolerance exactness, and Real seal nonescape rows before any $\RealUp$ seal is available.

Finally apply \autoref{thm:regular-cauchy-uniqueness-real-seal-nonescape}. Since the carriers contain no quotient stream, selected representative, countable-choice, or host-equality coordinate, the only available equality route is the displayed classifier route through $E,E'$ together with $H,H',C,C',P,P',N,N'$. The read is therefore locked to the local Cauchy-equivalence classifier before either terminal seal is consumed.
\end{proof}
