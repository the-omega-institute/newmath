\chapter{A Concrete Naming Certificate for $\HilbertSchmidtUp$}
\label{ch:concrete-instances-hilbert-schmidt-namecert}
\origin{human}

Following Schatten and von Neumann operator-ideal theory, the $\HilbertSchmidtUp$ packet records the finite square-summable coefficient surface by which a bounded Hilbert operator may be read as Hilbert--Schmidt before compact, trace-class, or spectral consumers inspect it. It sits after the Hilbert carrier of \autoref{ch:concrete-instances-hilbert-namecert}, the linear-map rows of \autoref{ch:concrete-instances-linearmap-namecert}, the scalar-bound surface of \autoref{ch:concrete-instances-operator-norm-namecert}, the finite-image route of \autoref{ch:concrete-instances-finite-rank-operator-namecert}, and the operator-ideal boundary of \autoref{ch:concrete-instances-operatorideal-namecert}. The packet names only displayed Hilbert rows, a bounded linear action, a finite orthonormal-basis window, a square-sum ledger, transport, replay, provenance, and local naming; it does not select an infinite basis, assert trace theory, identify host Hilbert-space equality, or import compactness from outside the displayed rows.

\begin{definition}[Hilbert--Schmidt carrier]
\label{def:hilbert-schmidt-carrier}
A $\HilbertSchmidtUp$ carrier is a finite $\BHist$ packet
$$
S=(H,L,B,S_{\mathrm{sq}},T,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $H$ is the Hilbert source row, carrying the source and target $\HilbertUp$ endpoints together with the inner-product and norm data consumed by operator readers.
\item $L$ is the bounded $\LinearMapUp$ row for the same endpoints, read through the bounded-operator and $\OperatorNormUp$ surfaces before any ideal consumer appears.
\item $B$ is a finite orthonormal-basis window: a displayed finite list of source vectors, their orthonormality ledger, and the corresponding coefficient rows for the images under $L$.
\item $S_{\mathrm{sq}}$ is the square-summable coefficient ledger, recording the finite sum of squared image coefficients over the window $B$ and the scalar endpoint that bounds that displayed sum.
\item $T$ records componentwise $\hsame$ transport for the Hilbert rows, bounded linear-map row, basis window, square-sum ledger, replay, provenance, and local name.
\item $R$ records displayed $\Cont$ replay from Hilbert-source observations through the bounded action and coefficient ledger into operator-ideal and compact-operator consumers.
\item $P$ is the $\Pkg$ provenance over $\HilbertSchmidtUp$, $\HilbertUp$, $\LinearMapUp$, $\OperatorNormUp$, $\FiniteRankOperatorUp$, $\CompactOperatorUp$, $\OperatorIdealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HilbertSchmidtUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $H,L,B,S_{\mathrm{sq}},T,R,P,N$. It has no coordinate for a selected infinite orthonormal basis, host Hilbert-space equality, trace-class membership, singular-value decomposition, spectral theorem, or ambient compactness principle.
\end{definition}

\begin{theorem}[Hilbert--Schmidt NameCert obligations]
\label{thm:hilbert-schmidt-namecert-obligations}
For every accepted $\HilbertSchmidtUp$ carrier $S=(H,L,B,S_{\mathrm{sq}},T,R,P,N)$, the local $\NameCert_{\HilbertSchmidtUp}$ surface consists exactly of carrier admission for the Hilbert endpoints, bounded linear action, finite orthonormal-basis window, square-sum ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming; basis-window invariance for replacing $B$ by a transported finite orthonormal window over the same Hilbert rows; square-sum ledger stability under the same transport; operator-ideal handoff through the bounded action and displayed square bound; and nonescape from host Hilbert-space equality, selected infinite bases, trace theory, spectral data, or ambient compactness.
\end{theorem}
\begin{proof}
Project the carrier rows. The Hilbert endpoints, bounded linear action, finite basis window, square-sum ledger, transport, replay, provenance, and local name are the complete displayed inventory.

Basis-window invariance is a statement about transported finite windows. The row $T$ moves the Hilbert data, the list $B$, and the coefficient ledger together, so a consumer may compare two displayed finite windows only through componentwise $\hsame$ transport.

Square-sum ledger stability follows from the same finite route. The scalar endpoint attached to $S_{\mathrm{sq}}$ is read after the bounded action and the coefficient rows have been displayed, and $R$ replays that order for downstream consumers.

Finally, the operator-ideal handoff consumes only $L$ and $S_{\mathrm{sq}}$ through $P,N$. Since no row stores a selected infinite basis, host equality of Hilbert spaces, trace-class membership, spectral data, or ambient compactness, those objects are refused by the local certificate.
\end{proof}

\begin{theorem}[Hilbert--Schmidt operator-norm bound]
\label{thm:hilbert-schmidt-operator-norm-bound}
Every $\OperatorNormUp$ or $\OperatorIdealUp$ consumer that reads an accepted $\HilbertSchmidtUp$ carrier must pass through the finite route
$$
H,L,B,S_{\mathrm{sq}},T,R,P,N .
$$
The displayed square-sum ledger supplies the finite scalar bound consumed by the operator-norm boundary for the same bounded action $L$, and this bound is the only Hilbert--Schmidt data available to $\OperatorIdealUp$ and $\CompactOperatorUp$ consumers at this paper site.
\end{theorem}
\begin{proof}
Start from the bounded linear-map row $L$ over the Hilbert endpoints $H$. The operator-norm reader receives the action only after the same packet has displayed its finite coefficient window $B$.

Read the square-sum ledger next. The row $S_{\mathrm{sq}}$ records the finite scalar bound that dominates the displayed image coefficients, so the operator-norm consumer sees a finite bound-facing certificate rather than a host supremum.

The ideal and compact consumers then read through $T,R,P,N$. These rows transport, replay, source, and name the same bounded action and square ledger. They do not add trace data, selected compactness, spectral values, or equality of ambient Hilbert spaces.
\end{proof}

\falsifiablePrediction{Every accepted $\HilbertSchmidtUp$ consumer must display Hilbert source rows, a bounded LinearMapUp action, a finite orthonormal-basis window, a square-sum ledger, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows before Hilbert--Schmidt operator data is available.}

\independenceWitness{hilbert, linearmap, operator-norm, finite-rank-operator, operatorideal: no listed sibling is bijective to $\HilbertSchmidtUp$ because this packet jointly carries Hilbert endpoints, bounded action, finite orthonormal coefficient windows, square-sum ledger stability, operator-ideal handoff, transport, replay, provenance, and local naming in one operator-facing surface.}

\closureat{HilbertSchmidtUp}{seedStr}

\begin{closurestatus}{\HilbertSchmidtUp}
  \constructivestory{A $\HilbertSchmidtUp$ packet is generated from Hilbert source rows, a bounded LinearMapUp action, a finite orthonormal-basis window, a square-sum coefficient ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hilbert-schmidt-carrier}, \autoref{thm:hilbert-schmidt-namecert-obligations}, \autoref{thm:hilbert-schmidt-operator-norm-bound}, the falsifiable prediction, and the independence witness seed the finite Hilbert--Schmidt operator route over Hilbert, linear-map, operator-norm, finite-rank, compact-operator, operator-ideal, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trace-class theory, spectral decomposition, selected infinite orthonormal bases, host Hilbert-space equality, ambient compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, basis-window invariance, square-sum ledger stability, operator-ideal handoff, operator-norm bound consumption, transport, replay, provenance, and local naming for BEDC.Derived.HilbertSchmidtUp.HilbertSchmidtNameCert.}
\end{closurestatus}
