\begin{theorem}[Finite density-net real interval route]
\label{thm:finite-cauchy-density-net-real-interval-route}
Every accepted $\FiniteCauchyDensityNetUp$ packet reaches a $\RealUp$ consumer through the dyadic-cell surface of \autoref{ch:concrete-instances-dyadic-interval-net-namecert} before the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert} is public. The route reads the displayed dyadic ledger, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, interval-net cells, finite modulus, transport, replay, provenance, and local naming rows; it exports no kernel-only density assertion, quotient representative, selected limit, countable-choice schedule, host equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the finite density-net sibling route, \autoref{thm:finite-cauchy-density-net-sibling-route}. It already places the $\DyadicIntervalNetUp$ cell surface before any completion-facing export of the density packet.

Apply the Real seal nonescape theorem, \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. The terminal $\RealUp$ row is available only after the displayed dyadic ledger, stream window, regular readback, modulus, transport, replay, provenance, and local naming rows have been exposed.

Thus the hub route is a visible L10 sibling dependency through the interval-net and Real chapters, not a detached density fact. The carrier has no coordinate for quotient representatives, selected limits, countable-choice schedules, host equality, or ambient completeness, so those claims remain outside the packet.
\end{proof}
