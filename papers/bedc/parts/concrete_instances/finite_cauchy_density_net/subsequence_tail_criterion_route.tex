Every $\FiniteCauchyDensityNetUp$ density-net read consumed by subsequence extraction first passes through \autoref{ch:concrete-instances-regular-cauchy-subsequence-criterion-namecert} and \autoref{ch:concrete-instances-cauchy-tail-selection-criterion-namecert}. The route exposes the finite density ledger together with the $\RegSeqRatUp$ readback and $\StreamNameUp$ window rows before exporting a tail-density witness; it does not export a selected subsequence, quotient stream equality, countable choice schedule, chosen limit, or ambient completeness principle.
