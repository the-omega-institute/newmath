\begin{theorem}[Finite Cauchy density-net Bishop Cauchy route]
\label{thm:finite-cauchy-density-net-bishop-cauchy-route}
Every Bishop Cauchy, regular-sequence, or completion-facing consumer of an accepted $\FiniteCauchyDensityNetUp$ packet
$$
N=(D,W,R,E,M,T,C,P,L)
$$
reads the finite density witness only through the dyadic arithmetic source $D$, finite $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, modulus row $M$, componentwise $\hsame$ transport $T$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\FiniteCauchyDensityNetUp}$ row $L$. In particular, the handoff to the Bishop Cauchy route of \autoref{ch:concrete-instances-regular-sequence-space-namecert} is a finite L10 sibling dependency and not a quotient-completion argument, selected representative, host equality, countable-choice schedule, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Start with finite density-net extraction, \autoref{thm:finite-cauchy-density-net-extraction}. It identifies $D,W,R,E,M,T,C,P,L$ as the displayed density-net route and leaves no hidden completion row.

Use the regular-sequence handoff theorem, \autoref{thm:finite-cauchy-density-net-regular-sequence-handoff}. A regular-sequence consumer reaches the density witness only after the $\StreamNameUp$ window, $\RegSeqRatUp$ readback, dyadic net ledger, modulus row, and terminal $\RealUp$ seal have been displayed.

Now compare with the regular sequence-space chapter named by \autoref{ch:concrete-instances-regular-sequence-space-namecert}. Its Bishop Cauchy consumer can read the density-net packet only as this already displayed finite handoff, so the dependency edge is the same L10 route through $D,W,R,E,M,T,C,P,L$.

Finally apply Real seal nonescape, \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. Since the accepted packet has no coordinate for quotient completion, selected representatives, host equality, countable choice, or ambient Real completeness, no Bishop Cauchy consumer can bypass the finite density-net route.
\end{proof}
