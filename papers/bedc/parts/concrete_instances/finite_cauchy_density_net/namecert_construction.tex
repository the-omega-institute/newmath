\chapter{Finite Cauchy Density Net NameCert Construction}
\label{ch:concrete-instances-finite-cauchy-density-net-namecert}
\origin{human}

Following Bishop and Bridges, finite density data for real completion is represented by explicit dyadic finite nets and moduli, not by selected representatives or quotient completions. The $\FiniteCauchyDensityNetUp$ packet sits between the dyadic arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite dyadic cell surface of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It gives L10 completion consumers a finite density witness object without asserting ambient completeness of $\RealUp$.

\begin{definition}[Finite Cauchy density-net packet]
\label{def:finite-cauchy-density-net-packet}
A $\FiniteCauchyDensityNetUp$ carrier is a finite $\BHist$ packet
$$
N=(D,W,R,E,M,T,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ finite-net ledger, listing the dyadic centers, dyadic radii, and finite containment comparisons used by the density read.
\item $W$ is the $\StreamNameUp$ finite-window row that records exactly which prefixes of an inspected Cauchy name are opened before the net row is consulted.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed window and dyadic ledger.
\item $E$ is the terminal $\RealUp$ seal row, exposed only after $D,W,R$ have all been displayed.
\item $M$ is the modulus row, recording the finite precision budget that makes the displayed dyadic net adequate for the requested readback.
\item $T$ records componentwise $\hsame$ transport for dyadic net rows, stream windows, regular readback rows, Real seals, modulus rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a density-net request through $D,W,R,E,M,T$.
\item $P$ is the $\Pkg$ provenance over $\FiniteCauchyDensityNetUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $L$ is the local $\NameCert_{\FiniteCauchyDensityNetUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient stream equality, a selected representative of a completed real, countable choice over nets, host real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite Cauchy density-net obligation carrier]
\label{thm:finite-cauchy-density-net-obligation-carrier}
Every accepted $\FiniteCauchyDensityNetUp$ density read must expose dyadic tolerance rows, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, the terminal $\RealUp$ seal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\FiniteCauchyDensityNetUp}$ before it can serve a density-net consumer. In carrier notation the admissible route is the displayed packet
$$
D,W,R,E,M,T,C,P,L
$$
of \autoref{def:finite-cauchy-density-net-packet}; no consumer may replace it by quotient streams, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteCauchyDensityNetCarrier\_obligation\_carrier}
\begin{proof}
Project the carrier rows in \autoref{def:finite-cauchy-density-net-packet}. The dyadic tolerance data are contained in $D$ and $M$, the finite stream inspection is contained in $W$, the regular readback is $R$, and the terminal Real-facing row is $E$.

Use the finite density-net route already displayed by the carrier: the density read first opens the dyadic ledger and finite stream window, then reads back through $\RegSeqRatUp$, and only then exposes the $\RealUp$ seal. The structural rows $T,C,P,L$ keep the same route under transport, replay, provenance, and local naming.

The sibling routes input by the hub supply the same L10 discipline for finite density and regular readback consumers, so the carrier cannot be consumed as an unrecorded density assertion. Since the packet has no row for quotient streams, selected limits, countable choice, or ambient Real completeness, the displayed rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[Finite Cauchy density-net extraction]
\label{thm:finite-cauchy-density-net-extraction}
For every accepted $\FiniteCauchyDensityNetUp$ carrier $N=(D,W,R,E,M,T,C,P,L)$, every density-net read extracts exactly the displayed finite dyadic net rows $D$, the finite $\StreamNameUp$ observation row $W$, the $\RegSeqRatUp$ readback row $R$, the terminal $\RealUp$ seal route $E$, and the modulus row $M$, preserved by $T,C,P,L$. The extraction supplies a finite L10 density witness for Real-facing completion routes, but it does not claim $\RealUp$ completeness, quotient representatives, selected limits, countable choice, trichotomy, or host real equality.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-cauchy-density-net-packet}. The dyadic net row $D$ is the only row carrying centers, radii, and finite containment comparisons, while $W$ is the only row opening inspected stream prefixes.

The readback row $R$ is downstream of $D,W$ and the modulus row $M$. Thus any density read reaches the regular rational surface through displayed finite data rather than through a detached sequence or host completion object.

The terminal row $E$ seals only that readback route as a $\RealUp$ handoff. Rows $T,C,P,L$ transport, replay, source, and name the same finite route. Since the carrier contains no coordinate for quotient streams, selected representatives, countable choice, trichotomy, host real equality, or ambient Real completeness, the listed rows exhaust the extraction surface.
\end{proof}

\begin{theorem}[Finite Cauchy density-net Real seal nonescape]
\label{thm:finite-cauchy-density-net-real-seal-nonescape}
Every $\RealUp$ consumer of an accepted $\FiniteCauchyDensityNetUp$ packet must read the finite route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
through the displayed carrier rows $D,W,R,E,M,T,C,P,L$. The packet can be cited as a finite density witness by separability, completion, and compactness consumers, but it exports no completed-real theorem and no selector for representatives or limits.
\end{theorem}
\begin{proof}
Apply \autoref{thm:finite-cauchy-density-net-extraction}. The extraction theorem identifies the dyadic net ledger, finite stream window, regular readback, Real seal, and modulus rows as the whole analytic route.

The structural rows $T,C,P,L$ preserve that route under componentwise transport, continuation replay, provenance, and local naming. They do not add a second completion-facing row.

Therefore a Real-facing consumer receives only the displayed finite density packet. Any claim of completed Real separability, ambient completeness, quotient representatives, selected limits, or countable-choice net selection lies outside the local carrier.
\end{proof}

\begin{theorem}[Finite Cauchy density-net stream route]
\label{thm:finite-cauchy-density-net-stream-route}
Every finite density-net consumer of an accepted $\FiniteCauchyDensityNetUp$ packet must first expose the $\StreamNameUp$ finite-window row of \autoref{ch:concrete-instances-streamname-namecert} and the $\RegSeqRatUp$ readback row of \autoref{ch:concrete-instances-regseqrat-namecert} before treating the dyadic net as a $\RealUp$-facing density witness. Equivalently, the dyadic rows $D$ become Real-facing only along the displayed carrier route $D,W,R,E,M,T,C,P,L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-cauchy-density-net-packet}. The rows $D,W,R,E,M,T,C,P,L$ are the whole displayed packet, with $W$ the finite window row and $R$ the regular rational readback row.

Apply \autoref{thm:finite-cauchy-density-net-extraction}. It identifies the dyadic net ledger $D$, the $\StreamNameUp$ window $W$, the $\RegSeqRatUp$ readback $R$, the terminal $\RealUp$ seal $E$, and the modulus row $M$ as the extracted analytic route.

Apply \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. A Real-facing consumer can read a seal only after the same rows have been displayed. The structural rows $T,C,P,L$ transport, replay, source, and name that route, so the dyadic density net cannot be consumed as a Real-facing witness before $W$ and $R$ have been exposed.
\end{proof}

\begin{theorem}[Finite Cauchy density-net sibling route]
\label{thm:finite-cauchy-density-net-sibling-route}
Every completion-facing sibling consumer of an accepted $\FiniteCauchyDensityNetUp$ packet, including the real-uniform route of \autoref{ch:concrete-instances-real-uniform-structure-namecert} and the modulus route of \autoref{ch:concrete-instances-cauchymodulus-namecert}, must consume the finite density-net read only after the displayed rows $D,W,R,E,M,T,C,P,L$ have all been exposed. The read may first consume the finite dyadic cell surface of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}: $\DyadicIntervalNetUp$ supplies the displayed cells and endpoint incidence, while $\FiniteCauchyDensityNetUp$ supplies the density witness and Real-facing seal. In particular, the packet supplies a completion-facing dependency edge through dyadic cells, endpoint incidence, stream windows, regular rational readback, Real sealing, finite modulus, transport, replay, provenance, and local naming, not through a kernel-only density assertion.
\end{theorem}
\begin{proof}
Start with \autoref{thm:finite-cauchy-density-net-stream-route}. It forces every density-net consumer to expose the $\StreamNameUp$ row $W$ and $\RegSeqRatUp$ row $R$ before the dyadic net can be read as Real-facing data.

Before that Real-facing export, the consumer may read the sibling $\DyadicIntervalNetUp$ surface of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}. That sibling supplies the displayed dyadic cells and endpoint incidence; the density-net packet keeps those cells tied to $D$ and then supplies the density witness, modulus row $M$, and terminal seal $E$.

Apply \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. The terminal $\RealUp$ seal $E$ is available only along the same carrier route, so a $\RealUniformStructureUp$ or $\CauchyModulusUp$ consumer cannot bypass $D,W,R,E,M,T,C,P,L$ when it asks for completion-facing density.

Finally use \autoref{thm:finite-cauchy-density-net-extraction}. The extraction theorem keeps the dyadic ledger $D$, modulus row $M$, structural transport $T$, replay $C$, provenance $P$, and local naming $L$ attached to the read. Thus the sibling dependency edge is a finite displayed route into later completion consumers, not an ambient Real-completion fact or an unrecorded selector for dense representatives.
\end{proof}

\begin{theorem}[Finite Cauchy density-net finite-window comparison]
\label{thm:finite-cauchy-density-net-window-comparison}
Every accepted $\FiniteCauchyDensityNetUp$ comparison exported to a $\RealUp$ or $\RegSeqRatUp$ consumer factors through the displayed finite net extraction, $\StreamNameUp$ window, dyadic tolerance row, and $\RealUp$ seal row of $N=(D,W,R,E,M,T,C,P,L)$. The sibling route fixes the same $\BHist$ provenance, so the comparison uses one displayed window route rather than host limit selection, quotient representatives, or ambient completeness.
\end{theorem}
\begin{proof}
First apply finite density-net extraction, \autoref{thm:finite-cauchy-density-net-extraction}. It obtains the finite dyadic net row $D$, the finite stream observation row $W$, the regular readback $R$, the Real seal $E$, and the modulus row $M$ from the accepted carrier.

Next use the stream route of \autoref{thm:finite-cauchy-density-net-stream-route}. It permits the comparison to read only the listed StreamName windows and the RegSeqRat readback attached to those windows.

Finally apply the sibling route and Real seal nonescape, \autoref{thm:finite-cauchy-density-net-sibling-route} and \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. They keep the same $\BHist$ provenance and terminal Real seal attached to $D,W,R,E,M,T,C,P,L$, excluding host limit selection, quotient representatives, and ambient completeness.
\end{proof}

\begin{theorem}[Finite Cauchy density-net tail extraction]
\label{thm:finite-cauchy-density-net-tail-extraction}
For every accepted $\FiniteCauchyDensityNetUp$ packet $N=(D,W,R,E,M,T,C,P,L)$, every completion-facing tail read consumed by $\RegularSequenceSpaceUp$, $\RealUniformStructureUp$, or $\CauchyModulusUp$ exposes the same finite $\StreamNameUp$ window $W$, the same $\RegSeqRatUp$ readback $R$, the same dyadic net ledger $D$, the same finite modulus row $M$, the same terminal $\RealUp$ seal $E$, and the same structural rows $T,C,P,L$. The tail read is therefore a displayed finite BEDC route, not a quotient stream, not a chosen representative, and not an appeal to ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:finite-cauchy-density-net-stream-route}. It forces every completion-facing read to pass through the displayed $\StreamNameUp$ window $W$ and the $\RegSeqRatUp$ row $R$ before any tail data can be consumed.

Then apply \autoref{thm:finite-cauchy-density-net-sibling-route}. That theorem keeps the same displayed route attached to completion-facing readers such as $\RealUniformStructureUp$ and $\CauchyModulusUp$, so the tail read cannot bypass $D,W,R,E,M,T,C,P,L$.

Finally use \autoref{thm:finite-cauchy-density-net-window-comparison}. The comparison theorem factors the completion-facing read through the same finite packet data, and \autoref{def:finite-cauchy-density-net-packet} lists exactly the rows $D,W,R,E,M,T,C,P,L$ with no coordinate for quotient streams, chosen representatives, or ambient completeness. Row exhaustion gives the claim.
\end{proof}

\begin{theorem}[Finite Cauchy density-net regular-sequence handoff]
\label{thm:finite-cauchy-density-net-regular-sequence-handoff}
Every handoff from an accepted $\FiniteCauchyDensityNetUp$ packet $N=(D,W,R,E,M,T,C,P,L)$ to $\RegularSequenceSpaceUp$ exposes the finite dyadic net rows $D$, the $\StreamNameUp$ window $W$, the $\RegSeqRatUp$ readback $R$, the terminal $\RealUp$ seal $E$, the finite modulus row $M$, and the structural rows $T,C,P,L$ before any regular-sequence consumer reads it. The handoff is therefore finite BEDC evidence only: it does not export a quotient stream, a selected representative, a completed Real limit, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with finite density-net extraction, \autoref{thm:finite-cauchy-density-net-extraction}. It displays the dyadic net ledger $D$, the stream window $W$, the regular rational readback $R$, the Real seal $E$, and the modulus row $M$ from the accepted packet.

Use the stream route of \autoref{thm:finite-cauchy-density-net-stream-route}. A regular-sequence consumer can read the handoff only after the finite $\StreamNameUp$ window and the $\RegSeqRatUp$ readback have been exposed.

Now apply tail extraction, \autoref{thm:finite-cauchy-density-net-tail-extraction}. It keeps the same rows visible for completion-facing tail reads, including the structural rows $T,C,P,L$.

Finally use the sibling route of \autoref{thm:finite-cauchy-density-net-sibling-route}. The handoff to $\RegularSequenceSpaceUp$ remains a displayed route through $D,W,R,E,M,T,C,P,L$, so no quotient stream, selected representative, completed limit, choice schedule, trichotomy, host equality, or ambient completeness is introduced.
\end{proof}

\begin{theorem}[Finite Cauchy density-net subsequence-modulus route]
\label{thm:finite-cauchy-density-net-subsequence-modulus-route}
Every accepted $\FiniteCauchyDensityNetUp$ handoff to a $\RegularCauchySubsequenceModulusUp$ consumer factors through the same displayed rows
$$
D,W,R,E,M,T,C,P,L
$$
before it reaches the subsequence-modulus certificate of \autoref{ch:concrete-instances-regular-cauchy-subsequence-modulus-namecert}. Thus the subsequence-modulus route consumes the finite dyadic density ledger, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, finite modulus row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row, not a quotient stream, selected subsequence, chosen Real limit, countable-choice schedule, trichotomy, host equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with finite density-net extraction, \autoref{thm:finite-cauchy-density-net-extraction}. It displays $D,W,R,E,M$ as the analytic rows of the accepted packet and keeps $T,C,P,L$ attached as transport, replay, provenance, and local naming.

Apply the stream route and Real-seal nonescape theorems, \autoref{thm:finite-cauchy-density-net-stream-route} and \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. A downstream consumer can read the density packet only after the finite $\StreamNameUp$ window, regular rational readback, and terminal $\RealUp$ seal have been exposed along the same carrier route.

Now use the regular-sequence handoff of \autoref{thm:finite-cauchy-density-net-regular-sequence-handoff}. It already forces regular-sequence consumers through $D,W,R,E,M,T,C,P,L$. The $\RegularCauchySubsequenceModulusUp$ chapter is a further regular-subsequence modulus consumer, so its input edge must cite the same finite density route before the subsequence-modulus certificate is available.

Finally inspect the carrier of \autoref{def:finite-cauchy-density-net-packet}. It has no coordinate for quotient stream equality, selected subsequences, chosen Real limits, countable-choice schedules, trichotomy, host equality, or ambient completeness. Row exhaustion leaves only the displayed factorization through $D,W,R,E,M,T,C,P,L$ and the cited regular-subsequence modulus certificate.
\end{proof}

\begin{theorem}[Finite Cauchy density-net diagonal tightening]
\label{thm:finite-cauchy-density-net-diagonal-tightening}
For every accepted $\FiniteCauchyDensityNetUp$ packet $N=(D,W,R,E,M,T,C,P,L)$, any two completion-facing tail reads of $N$ can be tightened to a shared finite $\StreamNameUp$ window row and a shared dyadic tolerance row before either $\RegSeqRatUp$ readback or $\RealUp$ sealing is consumed. The tightening remains inside the displayed route $D,W,R,E,M,T,C,P,L$ and exports no quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with finite-window comparison, \autoref{thm:finite-cauchy-density-net-window-comparison}. It forces each completion-facing comparison to use the displayed finite net extraction, the $\StreamNameUp$ window, the dyadic tolerance row, and the terminal seal route attached to the same $\BHist$ provenance.

Apply tail extraction from \autoref{thm:finite-cauchy-density-net-tail-extraction}. Both tail reads must expose $W$, $R$, $D$, $M$, $E$, and the structural rows $T,C,P,L$ before any completion-facing consumer can use them.

Now use the sibling route of \autoref{thm:finite-cauchy-density-net-sibling-route}. A $\RealUniformStructureUp$, $\CauchyModulusUp$, $\RegSeqRatUp$, or $\RealUp$ consumer receives only this displayed density-net route, so the two reads meet at one finite stream window and one dyadic tolerance row before readback or sealing.

Since the carrier has no coordinate for quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient Real completeness, the shared window and tolerance row are obtained by row tightening inside the accepted packet rather than by an external selector.
\end{proof}

\begin{theorem}[Finite Cauchy density-net refinement]\label{thm:finite-cauchy-density-net-refinement}
Let $N=(D,W,R,E,M,T,C,P,L)$ be an accepted $\FiniteCauchyDensityNetUp$ packet. Refining the dyadic cell surface of $N$ preserves the density witness $D$, the finite modulus row $M$, the $\StreamNameUp$ window $W$, the $\RegSeqRatUp$ readback $R$, and the terminal $\RealUp$ seal route $E,T,C,P,L$. The refined packet consumes only finite dyadic cells and exports no ambient completeness, quotient representatives, selected limits, countable choice, trichotomy, or host real equality.
\end{theorem}
\begin{proof}
Start with the sibling route of \autoref{thm:finite-cauchy-density-net-sibling-route}. It lets the refinement read the $\DyadicIntervalNetUp$ cell surface before any Real-facing export, while keeping the density-net rows $D,W,R,E,M,T,C,P,L$ attached to the same finite packet.

Apply the diagonal tightening theorem, \autoref{thm:finite-cauchy-density-net-diagonal-tightening}. Any two refined completion-facing reads meet at one displayed $\StreamNameUp$ window and one dyadic tolerance row before regular rational readback or Real sealing is consumed.

Now use tail extraction and finite-window comparison, \autoref{thm:finite-cauchy-density-net-tail-extraction} and \autoref{thm:finite-cauchy-density-net-window-comparison}. These rows preserve the modulus, stream window, RegSeqRat readback, and terminal Real seal through the same displayed carrier route.

Finally replay $T,C,P,L$. Componentwise transport, continuation replay, provenance, and local naming preserve the refined finite-cell route and add no coordinate for ambient completeness, quotient representatives, selected limits, countable choice, trichotomy, or host real equality.
\end{proof}

\begin{theorem}[Finite Cauchy density-net regular readback]
\label{thm:finite-cauchy-density-net-regular-readback}
Let $N=(D,W,R,E,M,T,C,P,L)$ be an accepted $\FiniteCauchyDensityNetUp$ packet. Every $\RegSeqRatUp$-facing consumer of $N$ must read the same finite $\StreamNameUp$ window $W$, dyadic net ledger $D$, finite modulus row $M$, and readback row $R$ before the terminal $\RealUp$ seal $E$ is available. The readback is therefore the displayed route through $D,W,R,E,M,T,C,P,L$ and exports no quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with finite density-net extraction, \autoref{thm:finite-cauchy-density-net-extraction}. It displays the dyadic net ledger $D$, stream window $W$, regular readback $R$, Real seal $E$, and modulus row $M$ as the analytic rows of the accepted packet.

Apply the stream route and tail extraction, \autoref{thm:finite-cauchy-density-net-stream-route} and \autoref{thm:finite-cauchy-density-net-tail-extraction}. They force every regular-facing and completion-facing read through the same $W,R,D,M,E$ route before the terminal seal can be consumed.

Now use diagonal tightening and refinement, \autoref{thm:finite-cauchy-density-net-diagonal-tightening} and \autoref{thm:finite-cauchy-density-net-refinement}. The refined readback still meets at one displayed finite window and dyadic ledger before $\RegSeqRatUp$ readback or $\RealUp$ sealing is public.

Finally replay $T,C,P,L$. Componentwise transport, continuation replay, provenance, and local naming preserve the same finite readback route, so no coordinate remains for quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient Real completeness.
\end{proof}

\begin{definition}[Finite Cauchy density-net cofinal window]
\label{def:finite-cauchy-density-net-density-carrier}
Let $N=(D,W,R,E,M,T,C,P,L)$ be an accepted $\FiniteCauchyDensityNetUp$ packet. Its cofinal-window density subpacket is the displayed tuple
$$
N_{\mathrm{cof}}=(D,W,R,E,M,T,C,P,L)
$$
read with the following completion-facing order: $D$ is the finite $\DyadicRatCoreUp$ density ledger, $W$ is the finite $\StreamNameUp$ window through which the tail request is opened, $M$ is the finite precision budget attached to that window, $R$ is the $\RegSeqRatUp$ readback obtained from $D,W,M$, $E$ is the terminal $\RealUp$ seal exposed only after $R$, and $T,C,P,L$ transport, replay, source, and locally name that same finite route. The subpacket has no coordinate for a quotient stream, a chosen limit, an ambient complete real, trichotomy, host equality, or a selector over hidden tail representatives.
\end{definition}

\begin{theorem}[Finite Cauchy density-net cofinal window]
\label{thm:finite-cauchy-density-net-cofinal-window}
Let $N=(D,W,R,E,M,T,C,P,L)$ be an accepted $\FiniteCauchyDensityNetUp$ packet. Any two completion-facing tail reads of $N$ that are later consumed by $\RegSeqRatUp$ or $\RealUp$ refine to the displayed cofinal-window subpacket $N_{\mathrm{cof}}$: they meet at one finite $\StreamNameUp$ window $W$ and one dyadic tolerance row controlled by $D$ and $M$ before either $\RegSeqRatUp$ readback or terminal $\RealUp$ sealing is available. The refinement is internal to $D,W,R,E,M,T,C,P,L$ and exports no quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply finite-window comparison, \autoref{thm:finite-cauchy-density-net-window-comparison}. Each completion-facing comparison must pass through the displayed finite net extraction, the $\StreamNameUp$ window, the dyadic tolerance row, and the terminal seal route attached to the same $\BHist$ provenance.

Use tail extraction and diagonal tightening, \autoref{thm:finite-cauchy-density-net-tail-extraction} and \autoref{thm:finite-cauchy-density-net-diagonal-tightening}. The two tail reads therefore expose the same $W,R,D,M,E$ rows before any regular rational readback or Real sealing is consumed, and the tightening occurs inside the accepted carrier.

Finally apply refinement and regular readback, \autoref{thm:finite-cauchy-density-net-refinement} and \autoref{thm:finite-cauchy-density-net-regular-readback}. Refining the dyadic cell surface preserves $D,W,R,E,M,T,C,P,L$, while the regular readback theorem forces every $\RegSeqRatUp$-facing consumer to read that same finite route first. Hence both reads factor through $N_{\mathrm{cof}}$ with no external selector, quotient representative, chosen limit, trichotomy, host equality, or ambient completeness.
\end{proof}

\begin{theorem}[Finite Cauchy density-net RegSeqRat density handoff]
\label{thm:finite-cauchy-density-net-regseqrat-density-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\FiniteCauchyDensityNetUp$ packet receives density evidence only through the cofinal-window subpacket of \autoref{def:finite-cauchy-density-net-density-carrier}. In particular, the consumer may use the displayed finite dyadic ledger $D$, finite $\StreamNameUp$ window $W$, finite precision budget $M$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, and structural rows $T,C,P,L$, but it receives no quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient Real-completeness principle.
\end{theorem}
\begin{proof}
Start with the cofinal-window theorem, \autoref{thm:finite-cauchy-density-net-cofinal-window}. It refines any two completion-facing tail reads to one displayed $\StreamNameUp$ window and one dyadic tolerance row before regular readback or Real sealing.

Apply regular readback, \autoref{thm:finite-cauchy-density-net-regular-readback}. A $\RegSeqRatUp$-facing consumer cannot reach $R$ without first reading the same finite $W,D,M$ route, and the terminal $\RealUp$ seal $E$ remains downstream of that readback.

Use Real seal nonescape, \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. The $\RealUp$ consumer receives only the displayed finite density packet; the structural rows $T,C,P,L$ transport, replay, source, and name that packet without creating a second completion coordinate.

The carrier exhaustion clause in \autoref{def:finite-cauchy-density-net-density-carrier} removes the remaining alternatives. Since $N_{\mathrm{cof}}$ has no coordinate for quotient representatives, selected limits, countable choice, trichotomy, host equality, or ambient Real completeness, every accepted density handoff to $\RegSeqRatUp$ or $\RealUp$ is mediated by the displayed cofinal-window packet.
\end{proof}

\begin{theorem}[Finite Cauchy density-net regular-sequence forward link]
\label{thm:finite-cauchy-density-net-regular-sequence-forward-link}
Every accepted $\FiniteCauchyDensityNetUp$ packet is consumed by the regular sequence-space route of \autoref{ch:concrete-instances-regular-sequence-space-namecert} and the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert} only after the displayed finite net row $D$, dyadic tolerance and modulus rows $D,M$, finite $\StreamNameUp$ window $W$, and $\RegSeqRatUp$ readback row $R$ have all been exposed inside $N=(D,W,R,E,M,T,C,P,L)$. The forward link is therefore a finite row dependency from density nets to $\RegularSequenceSpaceUp$ and $\RegSeqRatUp$, not a host-density theorem, quotient stream equality, selected representative, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Start with the regular-sequence handoff of \autoref{thm:finite-cauchy-density-net-regular-sequence-handoff}. It requires the finite dyadic net, stream window, regular readback, Real seal, finite modulus, and structural rows to be visible before a $\RegularSequenceSpaceUp$ consumer reads the packet.

Apply the RegSeqRat density handoff of \autoref{thm:finite-cauchy-density-net-regseqrat-density-handoff}. It keeps every $\RegSeqRatUp$-facing read inside the cofinal-window subpacket, so the readback row $R$ is reached only through the displayed $D,W,M$ surface.

Finally use the regular readback theorem, \autoref{thm:finite-cauchy-density-net-regular-readback}. It ties the same finite window and dyadic ledger to the terminal seal route $E,T,C,P,L$, excluding quotient streams, selected representatives, host density assertions, and ambient Real completeness from the forward dependency.
\end{proof}

\begin{theorem}[Finite Cauchy density-net regular-approximation handoff]
\label{thm:finite-cauchy-density-net-regular-approximation-handoff}
Every $\RealApproximationSchemeUp$ or $\RealApproximationEnvelopeUp$ consumer of an accepted $\FiniteCauchyDensityNetUp$ packet must export its real-approximation handoff only after reading the displayed dyadic ledger $D$, finite $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, modulus row $M$, componentwise $\hsame$ transport $T$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\FiniteCauchyDensityNetUp}$ row $L$ of \autoref{def:finite-cauchy-density-net-packet}. The handoff is therefore a finite BEDC route into real-approximation consumers, not a quotient representative, selected limit, countable-choice schedule, host density theorem, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Project the carrier rows in \autoref{def:finite-cauchy-density-net-packet}. The only analytic rows available to a real-approximation consumer are the dyadic ledger $D$, finite stream window $W$, regular readback $R$, terminal seal $E$, and modulus row $M$, while $T,C,P,L$ preserve the same route under transport, replay, provenance, and local naming.

Apply the RegSeqRat density handoff of \autoref{thm:finite-cauchy-density-net-regseqrat-density-handoff}. It forces every $\RegSeqRatUp$ or $\RealUp$ density read through the displayed cofinal-window subpacket before a downstream approximation surface can consume it.

Now use the Real seal nonescape theorem, \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. The seal row $E$ exports only the finite route already displayed by $D,W,R,M,T,C,P,L$, so a $\RealApproximationSchemeUp$ or $\RealApproximationEnvelopeUp$ consumer cannot replace the route by a hidden completion object.

Since the carrier has no coordinate for quotient representatives, selected limits, countable choice, host density assertions, or ambient $\RealUp$ completeness, row exhaustion gives the claimed real-approximation handoff boundary.
\end{proof}

\begin{theorem}[Finite Cauchy density-net RealSeparability route]
\label{thm:finite-cauchy-density-net-real-separability-route}
Every accepted $\FiniteCauchyDensityNetUp$ read consumed by $\RealSeparabilityUp$ factors through dyadic cells, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal rows before the dense-name consumer of \autoref{ch:concrete-instances-real-separability-namecert} is exposed. The dependency route is the displayed packet $N=(D,W,R,E,M,T,C,P,L)$ together with the dyadic cell and density rows; it exports no selected dense sequence, quotient representative, countable-choice enumeration, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with finite density-net extraction, \autoref{thm:finite-cauchy-density-net-extraction}. It displays the dyadic net ledger $D$, finite $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, and modulus row $M$ as the analytic rows of an accepted packet.

Use the sibling route of \autoref{thm:finite-cauchy-density-net-sibling-route}. The route may first consume the dyadic cell surface of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, but it keeps the density witness, endpoint incidence, and finite modulus attached to $D,W,R,E,M,T,C,P,L$.

Now compose with the Real-separability chapter. The $\RealSeparabilityUp$ consumer of \autoref{ch:concrete-instances-real-separability-namecert} receives a dense-name input only after the finite stream window, dyadic tolerance and cell rows, regular rational readback, and Real seal have all been exposed.

Finally replay $T,C,P,L$. Componentwise transport, continuation replay, provenance, and local naming preserve that same displayed route, so the Real-separability edge cannot introduce selected dense sequences, quotient representatives, countable-choice enumeration, host equality, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteCauchyDensityNetUp$ read exposes a DyadicRatCore finite-net ledger, a StreamName finite-window row, a RegSeqRat readback row, a terminal RealUp seal, a finite modulus row, componentwise transport, continuation replay, provenance, and local naming.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real: $\FiniteCauchyDensityNetUp$ is not identical to any listed sibling because it binds dyadic finite-net rows and a modulus row before the regular rational readback and terminal RealUp seal.}

\closureat{FiniteCauchyDensityNetUp}{seedStr}

\begin{closurestatus}{\FiniteCauchyDensityNetUp}
  \constructivestory{A $\FiniteCauchyDensityNetUp$ packet is generated from a DyadicRatCore finite-net ledger, a StreamName finite-window row, a RegSeqRat readback row, a RealUp seal, a finite modulus row, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-cauchy-density-net-packet}, \autoref{thm:finite-cauchy-density-net-extraction}, and \autoref{thm:finite-cauchy-density-net-real-seal-nonescape} seed the finite dyadic density-net route from displayed Cauchy windows to the terminal RealUp seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim RealUp completeness, completed-real separability, compactness, quotient representatives, selected limits, countable choice, trichotomy, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic finite-net exactness, StreamName window admission, RegSeqRat readback, Real seal nonescape, finite modulus stability, replay, provenance, and local naming for BEDC.Derived.FiniteCauchyDensityNetUp.}
\end{closurestatus}
