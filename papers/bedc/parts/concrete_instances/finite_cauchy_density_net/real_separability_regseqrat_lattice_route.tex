Every $\FiniteCauchyDensityNetUp$ density read that is later consumed by a Real-facing completion route must first expose the regular rational lattice route. The finite mesh row is read through $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ tolerance ledger, the $\StreamNameUp$ finite-window row, and the $\RealSeparabilityUp$ separability surface before any $\RealUp$ completion consumer may use it. The packet therefore supplies a displayed lattice dependency for density reads, not an ambient dense-subset theorem, selected representative, countable-choice schedule, quotient real equality, or completed Real principle.
