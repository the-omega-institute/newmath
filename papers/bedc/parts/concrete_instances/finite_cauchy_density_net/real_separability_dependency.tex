The $\FiniteCauchyDensityNetUp$ packet supplies the finite witness surface consumed by \autoref{ch:concrete-instances-real-separability-namecert}: its carrier exposes explicit finite dyadic representatives, stream windows, regular-Cauchy readback, and a terminal Real seal before separability reads density. This makes the density handoff a displayed finite BEDC route rather than a host-level countability assumption or a selected dense subset.
