\begin{theorem}[Finite Cauchy density-net regular-sequence consumer route]
\label{thm:finite-cauchy-density-net-l10-regular-sequence-consumer-route}
Every regular-sequence consumer of an accepted $\FiniteCauchyDensityNetUp$ packet reads the density witness only after the route through \autoref{ch:concrete-instances-regular-sequence-space-namecert} and the finite-window extraction route of \autoref{ch:concrete-instances-regular-cauchy-window-extraction-namecert} have exposed the same $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, and terminal $\RealUp$ seal. Thus the finite density-net row is a downstream L10 dependency edge for regular Cauchy tails, not an ambient density assertion inside completed reals.
\end{theorem}
\begin{proof}
Start with \autoref{thm:finite-cauchy-density-net-l10-sibling-density-route}. It forces every completion-facing density read to pass through the displayed dyadic ledger, stream window, regular rational readback, and terminal Real seal.

The regular-sequence-space consumer contributes the ambient L10 sequence carrier, while the regular Cauchy window extraction consumer contributes the inspected finite tail window. Both consumers therefore meet the density-net packet at the already displayed route $D,W,R,E,M,T,C,P,L$ of \autoref{def:finite-cauchy-density-net-packet}.

Apply \autoref{thm:finite-cauchy-density-net-real-seal-nonescape}. Since the packet exports no quotient representatives, selected limits, countable-choice schedule, host equality, or ambient $\RealUp$ completeness, the dependency edge remains the finite L10 route through the named regular-sequence and window-extraction chapters.
\end{proof}
