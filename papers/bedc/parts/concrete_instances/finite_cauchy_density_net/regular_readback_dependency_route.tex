Every $\FiniteCauchyDensityNetUp$ density read that serves a $\RealUp$ consumer first consumes the finite mesh rows of $\DyadicIntervalNetUp$ from \autoref{ch:concrete-instances-dyadic-interval-net-namecert} and the regular readback rows of $\RegSeqRatUp$ from \autoref{ch:concrete-instances-regseqrat-namecert}. The route exposes dyadic cells, endpoint incidence, finite stream windows, regular rational readback, terminal real sealing, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data before any downstream density consumer reads the packet; it exports no ambient completeness theorem or selector for dense representatives.
