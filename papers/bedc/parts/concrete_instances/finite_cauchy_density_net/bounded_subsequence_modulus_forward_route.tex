The $\FiniteCauchyDensityNetUp$ packet supplies the visible dyadic net rows consumed by the bounded Cauchy subsequence-modulus route of \autoref{ch:concrete-instances-bounded-cauchy-subsequence-modulus-namecert}. A downstream extractor first reads the finite density ledger, $\StreamNameUp$ windows, and $\RegSeqRatUp$ readback already exposed by \autoref{ch:concrete-instances-regseqrat-namecert}, then carries those rows to the terminal $\RealUp$ consumer of \autoref{ch:concrete-instances-real-namecert}. The handoff is a finite row dependency from density nets into subsequence-modulus extraction, not a hidden sequential compactness assertion or a choice-selected subsequence.
