\chapter{A Concrete Naming Certificate for $\KelleyficationUp$}
\label{ch:concrete-instances-kelleyfication-namecert}
\origin{human}

Following Kelley and Steenrod compactly generated topology, the $\KelleyficationUp$ packet records the compactly generated reflection of a topology as finite BEDC data. It sits after the topology carrier of \autoref{ch:concrete-instances-topology-namecert}, the core-compact neighbourhood route of \autoref{ch:concrete-instances-core-compact-namecert}, and the compact-open topology ledger of \autoref{ch:concrete-instances-compact-open-topology-namecert}. The packet names only the compact-window tests through which a topology is reflected; it does not import a host category of spaces, arbitrary open-cover compactness, quotient-open equality, or choice-selected compact witnesses.

\begin{definition}[Kelleyfication carrier]
\label{def:kelleyfication-carrier}
A $\KelleyficationUp$ carrier is a finite $\BHist$ packet
$$
K=(T,C,O,W,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\TopologyUp$ source row whose carrier, open-incidence rows, and classifier are already admitted as finite topology data.
\item $C$ is the $\CoreCompactUp$ row, or an equivalent compactly generated test row, recording which compact-window probes may be used to inspect the source topology.
\item $O$ is the $\CompactOpenTopologyUp$ finite compact-window ledger. It lists the compact-domain windows and open-target tests used to decide reflected openness.
\item $W$ is the $\StreamNameUp$ inspection window, recording the finite observations through which a consumer reads the compact-window ledger.
\item $H$ records componentwise $\hsame$ transport for the source topology, compact-generation tests, compact-open ledger, inspection window, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from a reflected-open request through $T,C,O,W,H$ before any downstream topology or function-space consumer reads the result.
\item $P$ is the $\Pkg$ provenance over $\KelleyficationUp$, $\TopologyUp$, $\CoreCompactUp$, $\CompactOpenTopologyUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KelleyficationUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $T,C,O,W,H,R,P,N$. It has no coordinate for arbitrary host opens, quotient-open equality, an unrestricted category of compactly generated spaces, open-cover compactness, countable choice, or selected compact tests outside the displayed ledger.
\end{definition}
\leanchecked{BEDC.Derived.KelleyficationUp.KelleyficationTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Kelleyfication NameCert obligations]
\label{thm:kelleyfication-namecert-obligations}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the local $\NameCert_{\KelleyficationUp}$ surface consists exactly of source-topology exposure through $T$; compactly generated test exposure through $C$; compact-open finite-window exposure through $O$; finite inspection through $W$; componentwise transport through $H$; displayed replay through $R$; provenance through $P$; and local naming through $N$. Every reflected-open read must first pass through the topology source of \autoref{ch:concrete-instances-topology-namecert}, the core-compact route of \autoref{ch:concrete-instances-core-compact-namecert}, and the compact-open topology ledger of \autoref{ch:concrete-instances-compact-open-topology-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.KelleyficationUp.KelleyficationCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier in \autoref{def:kelleyfication-carrier}. The displayed rows are exactly $T,C,O,W,H,R,P,N$, so any accepted read begins with the source topology, the compact-generation test row, the compact-window ledger, and the finite inspection window.

The compactly generated reflection is read through sibling rows rather than through a host topology predicate. The topology source supplies the original open-incidence data, the core-compact row supplies compact-neighbourhood tests, and the compact-open topology row supplies the finite compact-window ledger.

Transport and replay preserve those rows componentwise. Provenance and local naming then bind the same packet as a $\KelleyficationUp$ certificate. Since no quotient-open equality, arbitrary open-cover compactness, selected compact-test family, or ambient category row occurs in the carrier, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Kelleyfication compact-generated obligation]
\label{thm:kelleyfication-compact-generated-obligation}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the compact-generated obligation is a row projection from the displayed carrier: $T$ supplies the $\TopologyUp$ source, $C$ supplies the $\CoreCompactUp$ compact-generation tests, $O$ supplies the $\CompactOpenTopologyUp$ compact-window ledger, $W$ supplies the $\StreamNameUp$ inspection window, $H$ supplies componentwise $\hsame$ transport, $R$ supplies displayed $\Cont$ replay, $P$ supplies $\Pkg$ provenance, and $N$ supplies the local $\NameCert$ row. No compact-generated read may be certified by a host topology predicate or by a compactness witness outside $T,C,O,W,H,R,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.KelleyficationUp.KelleyficationCarrier\_compact\_generated\_obligation}
\begin{proof}
Project the carrier of \autoref{def:kelleyfication-carrier}. The rows $T,C,O,W,H,R,P,N$ are the only rows available to an accepted $\KelleyficationUp$ packet.

The compact-generated obligation is therefore read from the carrier, not transported in as an external parameter. The topology source $T$ fixes the open-incidence data, the CoreCompact row $C$ fixes the compact-generation tests, and the CompactOpenTopology row $O$ fixes the finite compact-window ledger inspected through the StreamName row $W$.

The support rows preserve that same inventory. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming cannot create an additional compactness coordinate, so the compact-generated obligation is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[Kelleyfication compact-open scope]
\label{thm:kelleyfication-compact-open-scope}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the compact-open scope of a reflected-open read is exactly the finite route through the $\TopologyUp$ row $T$, the $\CoreCompactUp$ row $C$, the $\CompactOpenTopologyUp$ row $O$, the $\StreamNameUp$ row $W$, the $\BHist$ packet rows, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $R$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. A downstream read cannot leave this route to consult quotient-open equality, ambient compact-open structure, or a selected family of compact witnesses.
\end{theorem}
\begin{proof}
Read the carrier rows in order. The reflected-open request begins at the source topology row $T$, is restricted by the compact-generation row $C$, and reaches the compact-open ledger only through the finite window row $O$.

The inspection row $W$ turns that compact-open ledger into a StreamName-visible finite observation. Since the underlying packet is a finite $\BHist$ carrier, the observation has no independent ambient compact-open coordinate.

Finally apply the support rows. The $\hsame$ row $H$ and $\Cont$ row $R$ replay the same $T,C,O,W$ scope, while $P$ and $N$ record provenance and local naming for the same packet. Thus the compact-open scope is a carrier projection and not parameter transport.
\end{proof}

\begin{theorem}[Kelleyfication compact-open exactness]
\label{thm:kelleyfication-compact-open-exactness}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, a reflected-open read is admitted exactly through the finite route
$$
T \to C \to O \to W \to H \to R \to P \to N.
$$
The source topology row $T$ supplies the underlying open-incidence data, the $\CoreCompactUp$ row $C$ supplies the compact-generation tests, the $\CompactOpenTopologyUp$ row $O$ supplies the finite compact-window ledger, and the $\StreamNameUp$ row $W$ supplies the inspection windows. No reflected-open read is accepted from a host topology predicate, quotient-open equality, arbitrary compact-cover test, or selected compact family outside those rows.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:kelleyfication-carrier}. A reflected-open request has no coordinate before $T,C,O,W$ are visible, so it first reads the topology source, core-compact test row, compact-open ledger, and inspection window.

The exactness assertion is row-local. The compact-generation row decides which compact-window probes are available, and the compact-open ledger records the corresponding source-open tests before the StreamName window exposes any finite observation.

The structural rows then preserve the same route. Componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming can only replay $T,C,O,W$; since the carrier contains no host-open coordinate or selected compact-family coordinate, every accepted reflected-open read factors through the displayed compact-open route.
\end{proof}

\begin{theorem}[Kelleyfication core-compact scope]
\label{thm:kelleyfication-corecompact-scope}
In an accepted $\KelleyficationUp$ carrier, compact-generation tests are scoped by the displayed rows $C$ and $O$. A downstream topology or function-space consumer may inspect only the core-compact probes named by $C$ and the compact-open windows named by $O$, after reading the source topology row $T$ and before replay through $H,R,P,N$.
\end{theorem}
\begin{proof}
Read the rows $T,C,O$ from \autoref{def:kelleyfication-carrier}. The source topology row fixes the open-incidence data being reflected, while the core-compact row names the allowed compact-generation probes.

The compact-open ledger refines that scope to finite windows. Any consumer-facing test therefore has to pass through $O$ after $C$, so it cannot introduce an unlisted compact test or a host compactness predicate.

Finally, the replay and naming rows preserve only the displayed ledger. The rows $H,R,P,N$ transport, replay, source, and name the same compact-generation scope, so the accepted packet exports no additional compact-generation surface.
\end{proof}

\begin{theorem}[Kelleyfication inspection-window stability]
\label{thm:kelleyfication-inspection-window-stability}
Let $K=(T,C,O,W,H,R,P,N)$ be an accepted $\KelleyficationUp$ carrier. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the finite inspection window $W$: after transport and replay, every reflected-open observation still reads the same source topology row, core-compact test row, compact-open ledger, and StreamName window. Transport and replay cannot add host opens, quotient-open equality, arbitrary open-cover compactness, or selected compact tests.
\end{theorem}
\begin{proof}
Use the transport row $H$ componentwise. It relates the source topology, compact-generation tests, compact-open ledger, inspection window, replay, provenance, and local naming rows without adding a new coordinate.

Now apply the replay row $R$. A replayed reflected-open observation must still traverse $T,C,O,W$ before it reaches provenance or local naming, so the finite inspection window is unchanged.

The nonescape claim follows from the carrier shape. Host opens, quotient-open equality, arbitrary open-cover compactness, and selected compact tests are absent from $K$, and neither $\hsame$ nor $\Cont$ creates carrier fields. Thus stability preserves exactly the displayed Kelleyfication inspection route.
\end{proof}

\begin{theorem}[Kelleyfication compact-window exhaustion]
\label{thm:kelleyfication-compact-window-exhaustion}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, every accepted reflected-open read is exhausted by the displayed rows $T,C,O,W,H,R,P,N$. The compact-window ledger $O$ and the core-compact test row $C$ must be read before any downstream topology or function-space consumer receives the reflected-open result. No such read may be supplied by host opens, quotient-open equality, arbitrary open-cover compactness, or a selected compact-test family outside the carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kelleyfication-carrier}. Its only reflected-open route starts at the source topology row $T$, then exposes the compact-generation tests $C$, the compact-window ledger $O$, and the finite inspection window $W$ before any structural replay.

Apply compact-open exactness from \autoref{thm:kelleyfication-compact-open-exactness}. It identifies the accepted reflected-open read with the finite route
$$
T \to C \to O \to W \to H \to R \to P \to N.
$$

Next use core-compact scope from \autoref{thm:kelleyfication-corecompact-scope}. A downstream topology or function-space consumer may inspect only the compact-generation probes named by $C$ and the compact-open windows named by $O$, so both rows precede every consumer-facing read.

Finally replay inspection-window stability. Transport and continuation preserve the same $T,C,O,W$ rows, while the carrier contains no coordinate for host opens, quotient-open equality, arbitrary open-cover compactness, or a selected compact-test family. Thus the displayed rows exhaust the reflected-open read.
\end{proof}
\leanchecked{BEDC.Derived.KelleyficationUp.KelleyficationCarrier\_compact\_window\_exhaustion}

\begin{theorem}[Kelleyfication obligation-closure route]
\label{thm:kelleyfication-obligation-closure-route}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the obligation-closure route is the row projection that combines the compact-generated obligation, compact-open scope and exactness, core-compact scope, inspection-window stability, and compact-window exhaustion. The route exports exactly the topology source row $T$, compact-generation row $C$, compact-open ledger $O$, inspection window $W$, componentwise transport $H$, displayed replay $R$, provenance $P$, and local naming $N$, and it exports no host topology predicate, quotient-open equality, arbitrary open-cover compactness, or selected compact-test family.
\end{theorem}
\begin{proof}
Begin with the compact-generated obligation of \autoref{thm:kelleyfication-compact-generated-obligation}. It makes the compact-generated read a projection from $T,C,O,W,H,R,P,N$.

Use compact-open scope and exactness, \autoref{thm:kelleyfication-compact-open-scope} and \autoref{thm:kelleyfication-compact-open-exactness}, together with core-compact scope \autoref{thm:kelleyfication-corecompact-scope}. These force every reflected-open read through the finite route
$$
T \to C \to O \to W \to H \to R \to P \to N.
$$

Finally apply inspection-window stability and compact-window exhaustion, \autoref{thm:kelleyfication-inspection-window-stability} and \autoref{thm:kelleyfication-compact-window-exhaustion}. They keep the same rows visible after transport and replay, so the obligation route contains no external compactness or quotient-open coordinate.
\end{proof}

\begin{theorem}[Kelleyfication transport replay obligation]
\label{thm:kelleyfication-transport-replay-obligation}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the transport and replay obligation is local to the displayed rows: $H$ transports the source topology, compact-generation tests, compact-open ledger, inspection window, replay, provenance, and local naming componentwise, while $R$ replays only the reflected-open route already fixed by $T,C,O,W$. No transported or replayed read may introduce host opens, quotient-open equality, arbitrary open-cover compactness, or selected compact tests.
\end{theorem}
\begin{proof}
Project the transport row $H$ from \autoref{def:kelleyfication-carrier}. It has components only for the rows already displayed in the carrier, so it cannot add a new topology or compactness coordinate.

Now read the replay row $R$ through compact-open exactness, \autoref{thm:kelleyfication-compact-open-exactness}. The replayed request must pass through $T,C,O,W$ before provenance or local naming is exposed.

Inspection-window stability and compact-window exhaustion, \autoref{thm:kelleyfication-inspection-window-stability} and \autoref{thm:kelleyfication-compact-window-exhaustion}, then identify the transported and replayed route with the same finite compact-window ledger. The excluded host and choice-selected objects are absent from every row used in the projection.
\end{proof}

\begin{theorem}[Kelleyfication carrier reflection]
\label{thm:kelleyfication-carrier-reflection}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, reflected topology data is visible only through the displayed rows $T,C,O,W,H,R,P,N$. In particular, a compact-open or topology consumer may read the $\TopologyUp$ source, $\CoreCompactUp$ compact-generation tests, $\CompactOpenTopologyUp$ ledger, StreamName inspection window, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming, but it may not read a host compactly generated topology or quotient-open equality outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kelleyfication-carrier}. The only reflected topology data carried by $K$ are the source topology row $T$, compact-generation row $C$, compact-open ledger $O$, and inspection row $W$.

Apply the compact-generated obligation of \autoref{thm:kelleyfication-compact-generated-obligation}. It makes every compact-generation read a row projection from the same carrier rather than an external topology predicate.

Now use compact-open exactness, \autoref{thm:kelleyfication-compact-open-exactness}. The reflected-open read follows $T,C,O,W$ before the structural rows $H,R,P,N$ can transport, replay, source, or name it. Since no host topology or quotient-open coordinate is present, reflection is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[Kelleyfication compact-open test stability]
\label{thm:kelleyfication-compact-open-test-stability}
Let $K=(T,C,O,W,H,R,P,N)$ be an accepted $\KelleyficationUp$ carrier. Compact-open tests named by $O$ remain stable under componentwise $\hsame$ transport and displayed $\Cont$ replay: after transport and replay, a reflected-open observation still reads the same source topology row, core-compact test row, compact-open ledger, and StreamName inspection window before any consumer sees the result.
\end{theorem}
\begin{proof}
Begin with compact-open exactness, \autoref{thm:kelleyfication-compact-open-exactness}. It identifies an accepted reflected-open observation with the finite route through $T,C,O,W,H,R,P,N$.

Apply inspection-window stability from \autoref{thm:kelleyfication-inspection-window-stability}. The transport row $H$ preserves the source topology, compact-generation tests, compact-open ledger, inspection window, replay, provenance, and local naming componentwise.

Finally use the transport replay obligation of \autoref{thm:kelleyfication-transport-replay-obligation}. The replay row $R$ replays only the compact-open route already fixed by $T,C,O,W$, so the transported test cannot acquire host opens, quotient-open equality, arbitrary open-cover compactness, or selected compact tests.
\end{proof}

\begin{theorem}[Kelleyfication StreamName scope]
\label{thm:kelleyfication-streamname-scope}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the StreamName row $W$ bounds all reflected-open observations before a topology or function-space consumer reads the result. A consumer receives only compact-open observations visible through the finite route
$$
T \to C \to O \to W \to H \to R \to P \to N,
$$
and receives no unwindowed compact-open test, host open predicate, quotient-open equality, or selected compact-test family.
\end{theorem}
\begin{proof}
Use core-compact scope, \autoref{thm:kelleyfication-corecompact-scope}. It places the compact-generation tests in $C$ and the compact-open windows in $O$ after the source topology row $T$ and before any downstream consumer.

Next apply inspection-window stability, \autoref{thm:kelleyfication-inspection-window-stability}. The StreamName row $W$ remains attached to the same $T,C,O$ route under componentwise transport and replay.

Finally apply compact-window exhaustion, \autoref{thm:kelleyfication-compact-window-exhaustion}. It forces every reflected-open read through $T,C,O,W,H,R,P,N$, so topology and function-space consumers cannot bypass the window by consulting unlisted compact-open tests or host open predicates.
\end{proof}

\begin{theorem}[Kelleyfication local naming obligation]
\label{thm:kelleyfication-local-naming-obligation}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the local $\NameCert_{\KelleyficationUp}$ row $N$ names only the obligation surface already exposed by $T,C,O,W,H,R,P$. The naming row records the compact-generated source, compact-open window, core-compact scope, inspection-window stability, transport, replay, and provenance obligations; it does not name a host compactly generated topology object, quotient-open equality, arbitrary open-cover compactness, or selected compact-test family.
\end{theorem}
\begin{proof}
Use the NameCert obligations theorem \autoref{thm:kelleyfication-namecert-obligations}. It states that local naming is one row of the same accepted carrier and follows after source topology, compact-generation tests, compact-open ledger, inspection window, transport, replay, and provenance.

The obligation-closure route of \autoref{thm:kelleyfication-obligation-closure-route} supplies the required row inventory. It combines compact-generated obligation, compact-open exactness, core-compact scope, inspection-window stability, and compact-window exhaustion without adding an external object.

Thus $N$ names exactly the displayed Kelleyfication packet. Since the carrier lacks host compactly generated topology, quotient-open equality, arbitrary open-cover compactness, and selected compact-test coordinates, local naming cannot export them.
\end{proof}

\begin{theorem}[Kelleyfication compact-open subbase]
\label{thm:kelleyfication-compact-open-subbase}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, the compact-open subbase visible to a downstream consumer is exactly the $\BHist$-visible route through the $\TopologyUp$ source row $T$, the $\CoreCompactUp$ window row $C$, the compact-open test ledger $O$, the StreamName inspection row $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $R$, $\Pkg$ provenance $P$, and local $\NameCert$ naming $N$. No subbase element is certified by host compact-open topology data, selected compact witnesses, or quotient-open equality outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kelleyfication-carrier}. The compact-open ledger $O$ is a row in the same finite $\BHist$ packet as the topology source $T$ and the core-compact window row $C$.

The subbase read is therefore row-visible. It may inspect the $\TopologyUp$ source, the $\CoreCompactUp$ windows, and the finite compact-open tests only through $O$ and the StreamName window $W$.

The structural rows preserve that same inventory. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row authenticate the compact-open tests without adding host compact-open data or selected compact witnesses.
\end{proof}

\begin{theorem}[Kelleyfication corecompact window]
\label{thm:kelleyfication-corecompact-window}
For every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$, each $\CoreCompactUp$ window exposed by $C$ is read after the $\TopologyUp$ source row $T$ and before the compact-open test ledger $O$. The row sequence
$$
T \to C \to O \to W \to H \to R \to P \to N
$$
is the complete consumer-facing path through $\BHist$-visible compact-open tests, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.
\end{theorem}
\begin{proof}
Begin with the source row $T$. It fixes the open-incidence data on which any core-compact window can be tested.

The row $C$ then names the allowed compact-generation windows, and $O$ records how those windows are used by compact-open tests. The StreamName row $W$ is the only finite observation channel for those tests.

Finally apply the support rows. Transport $H$ and replay $R$ keep the displayed order, while $P$ and $N$ record provenance and local naming for the same packet. Since no other carrier coordinate exists, the displayed route is complete.
\end{proof}

\begin{theorem}[Kelleyfication NameCert obligation surface]
\label{thm:kelleyfication-namecert-obligation-surface}
The local $\NameCert_{\KelleyficationUp}$ obligation surface for an accepted carrier $K=(T,C,O,W,H,R,P,N)$ consists exactly of $\BHist$-visible compact-open tests over the $\TopologyUp$ source row, $\CoreCompactUp$ windows, finite inspection through $W$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the naming row $N$. Every compact-open or corecompact consumer route must factor through these rows before it can cite the Kelleyfication packet.
\end{theorem}
\begin{proof}
Use \autoref{thm:kelleyfication-compact-open-subbase}. It identifies compact-open subbase reads with the finite row route through $T,C,O,W,H,R,P,N$.

Use \autoref{thm:kelleyfication-corecompact-window}. It places each corecompact window between the topology source and the compact-open ledger, with StreamName inspection before transport, replay, provenance, and naming.

The local NameCert row cannot name anything outside that route. Thus the obligation surface is exactly the displayed compact-open and corecompact packet, not an ambient topology predicate or an external compactness witness.
\end{proof}

\begin{theorem}[Kelleyfication compact-window scope route]
\label{thm:kelleyfication-compact-window-scope-route}
Every accepted $\KelleyficationUp$ reflected-open consumer factors through the ordered finite route
$$
T \to C \to O \to W \to H \to R \to P \to N
$$
of \autoref{def:kelleyfication-carrier}. The route is fixed by the compact-open subbase of \autoref{thm:kelleyfication-compact-open-subbase}, the corecompact window ordering of \autoref{thm:kelleyfication-corecompact-window}, and the local NameCert obligation surface of \autoref{thm:kelleyfication-namecert-obligation-surface}. It exports no host compactly generated topology, quotient-open equality, arbitrary open-cover compactness, selected compact-test family, or ambient category datum.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:kelleyfication-carrier}. A reflected-open consumer has access only to the displayed rows $T,C,O,W,H,R,P,N$, so its first possible route is the source topology, compact-generation test row, compact-open ledger, and finite inspection window.

Apply the compact-open subbase theorem, \autoref{thm:kelleyfication-compact-open-subbase}. It identifies consumer-visible compact-open tests with the same finite row route and prevents a consumer from reading host compact-open data or selected compact witnesses.

Next apply the corecompact window theorem, \autoref{thm:kelleyfication-corecompact-window}. It places each corecompact window after the topology source and before the compact-open ledger, so the compact-window request has the ordered prefix $T \to C \to O \to W$ before transport and replay.

Finally use the NameCert obligation surface of \autoref{thm:kelleyfication-namecert-obligation-surface}. The local naming row authenticates only the compact-open and corecompact route already displayed through $H,R,P,N$. Since the carrier has no coordinate for host compactly generated topology, quotient-open equality, arbitrary open-cover compactness, selected compact tests, or ambient category data, no accepted reflected-open consumer can leave the ordered finite route.
\end{proof}

\begin{theorem}[Kelleyfication reflection public interface]
\label{thm:kelleyfication-reflection-public-interface}
Every accepted $\KelleyficationUp$ carrier $K=(T,C,O,W,H,R,P,N)$ exposes a reflected-open public interface only through compact-generated rows. A consumer that asks for a reflected-open read must first pass through the $\TopologyUp$ source row, the $\CoreCompactUp$ compact-window row, the $\CompactOpenTopologyUp$ compact-open ledger, the inspection window $W$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. No accepted read exports host compactly generated topology, quotient-open equality, selected compact witnesses, arbitrary open-cover compactness, or ambient category data.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:kelleyfication-carrier}. Its visible route is exactly $T,C,O,W,H,R,P,N$, so a reflected-open consumer has no coordinate outside the topology source, compact-window row, compact-open ledger, inspection window, and structural rows.

Use the compact-generated obligation and compact-open exactness surfaces, \autoref{thm:kelleyfication-compact-open-subbase} and \autoref{thm:kelleyfication-namecert-obligation-surface}. They force each reflected-open read to remain attached to the displayed compact-window and compact-open ledger before replay and naming.

Finally apply the corecompact scope and inspection-window stability blocks, \autoref{thm:kelleyfication-corecompact-window} and \autoref{thm:kelleyfication-compact-window-scope-route}. These place the $\CoreCompactUp$ window before the compact-open read and keep $W,H,R,P,N$ downstream. Since no other row is present, the public interface exports only the compact-generated reflected-open rows.
\end{proof}

\begin{theorem}[Kelleyfication compact-window public export]
\label{thm:kelleyfication-compact-window-public-export}
For every accepted $\KelleyficationUp$ carrier, compact-window tests remain visible before any reflected-open read is exported. The public export route is
$$
T \to C \to O \to W \to H \to R \to P \to N,
$$
so the compact-window row $C$ and compact-open ledger $O$ are part of the exported certificate rather than hidden witnesses.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kelleyfication-carrier}. The only route to a reflected-open read starts with the $\TopologyUp$ source row $T$ and then exposes the compact-window row $C$.

Apply the compact-open exactness theorem \autoref{thm:kelleyfication-compact-open-subbase}. It identifies the reflected-open read with the displayed compact-open ledger $O$ over the same compact-window data.

Use the inspection-window stability route of \autoref{thm:kelleyfication-compact-window-scope-route}. It keeps $W$ visible before transport, replay, provenance, and naming are consumed. Therefore the public export cannot hide compact-window tests behind quotient-open equality or an ambient compactly generated topology.
\end{proof}

\begin{theorem}[Kelleyfication corecompact consumer boundary]
\label{thm:kelleyfication-corecompact-consumer-boundary}
Every $\CoreCompactUp$ consumer of an accepted $\KelleyficationUp$ packet receives the finite neighbourhood route through $T,C,O,W,H,R,P,N$ and does not receive quotient-open equality. The consumer may cite the compact-generated source, compact-window tests, compact-open ledger, inspection window, transport, replay, provenance, and local naming, but no selected compact-test family or host topology predicate is exported.
\end{theorem}
\begin{proof}
Begin with the corecompact window theorem \autoref{thm:kelleyfication-corecompact-window}. It places the $\CoreCompactUp$ request after the source topology row and before the compact-open ledger.

Apply the NameCert obligation surface \autoref{thm:kelleyfication-namecert-obligation-surface}. The local certificate records only compact-open tests over the displayed source row, compact windows, inspection window, transport, replay, provenance, and naming.

The compact-window scope route \autoref{thm:kelleyfication-compact-window-scope-route} then gives the finite neighbourhood path $T \to C \to O \to W \to H \to R \to P \to N$. Since quotient-open equality, selected compact-test families, host topology predicates, and ambient category data are absent from the carrier, no corecompact consumer can read them.
\end{proof}

\closureat{KelleyficationUp}{publicStr}[BEDC.Derived.KelleyficationUp.KelleyficationCarrier\_namecert\_obligations]

\begin{closurestatus}{\KelleyficationUp}
  \constructivestory{A $\KelleyficationUp$ packet is generated from a topology source row, a compactly generated or CoreCompact test row, a compact-open finite-window ledger, a StreamName inspection window, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:kelleyfication-carrier}, \autoref{thm:kelleyfication-reflection-public-interface}, and \autoref{thm:kelleyfication-compact-window-public-export} expose the compact-generation, compact-open, inspection-window, transport, replay, provenance, and local naming route as the public reflected-open interface.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.KelleyficationUp.KelleyficationCarrier\_namecert\_obligations}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host compactly generated topology, quotient-open equality, arbitrary open-cover compactness, choice-selected compact tests, Lean verification, or bridge checking.}
  \upgradepath{Public export requires the same compact-generation, compact-open, StreamName inspection, transport, replay, provenance, and local naming route to be exposed as a public reflected-open interface.}
\end{closurestatus}
