\chapter{A Concrete Naming Certificate for $\CoarseSpaceUp$}
\label{ch:concrete-instances-coarse-space-namecert}
\origin{human}

Following Roe's large-scale geometry, $\CoarseSpaceUp$ records controlled-set data over BEDC histories. It sits after the metric rows of \autoref{ch:concrete-instances-metric-namecert}, the entourage discipline of \autoref{ch:concrete-instances-uniform-entourage-space-namecert}, the metric-controlled specialization of \autoref{ch:concrete-instances-coarse-metric-space-namecert}, and the completion-facing uniform route of \autoref{ch:concrete-instances-uniform-space-completion-namecert}. The packet names large-scale control as finite carrier data; it does not identify small-scale uniform equivalence, quotient coarse maps, selected representatives, countable choice, or ambient completion.

\begin{definition}[CoarseSpace carrier]
\label{def:coarse-space-carrier}
A $\CoarseSpaceUp$ carrier is a finite $\BHist$ packet
$$
X=(U,\Delta,E,J,K,I,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $U$ is the underlying history row carrying the displayed points or probes read by a coarse consumer.
\item $\Delta$ is the diagonal controlled-entourage row.
\item $E$ is a finite list of controlled-entourage rows over the same $\BHist$ source.
\item $J$ records the controlled-union ledger for finite unions of rows in $E$.
\item $K$ records the controlled-composition ledger for composable rows in $E$.
\item $I$ records the inverse or symmetry row when the presented coarse structure includes it.
\item $H$ records componentwise $\hsame$ transport for the source row, diagonal row, controlled-entourage rows, union ledger, composition ledger, inverse row, replay routes, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a consumer request through the same controlled-entourage rows.
\item $P$ is the $\Pkg$ provenance over $\CoarseSpaceUp$, $\MetricUp$, $\UniformEntourageSpaceUp$, $\CoarseMetricSpaceUp$, $\UniformSpaceCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CoarseSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed controlled rows and their structural provenance. It has no coordinate for quotient coarse categories, ambient bornologies, selected representatives, countable choice, or small-scale uniform equivalence.
\end{definition}

\begin{theorem}[CoarseSpace NameCert obligations]
\label{thm:coarse-space-namecert-obligations}
For an accepted $\CoarseSpaceUp$ carrier $X=(U,\Delta,E,J,K,I,H,C,P,N)$, the local $\NameCert_{\CoarseSpaceUp}$ surface consists of carrier admission, the diagonal row $\Delta$, controlled finite unions through $J$, controlled composition through $K$, inverse or symmetry exposure through $I$ where present, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local NameCert naming through $N$. Every public controlled-entourage read must expose those rows before it can be consumed as large-scale coarse data.
\end{theorem}
\begin{proof}
Carrier admission is the displayed finite packet. The diagonal, controlled-entourage list, union ledger, composition ledger, inverse row, transport rows, replay routes, package provenance, and local naming row are all visible coordinates of the same $\BHist$ surface.

The closure rows are finite. A union read uses $J$, a composition read uses $K$, and an inverse or symmetry read uses $I$ only when that row is present in the carrier. Componentwise transport through $H$ preserves the same rows rather than replacing them by an external coarse category.

The consumer routes are exhausted by $C,P,N$. Since no excluded object is a coordinate of the carrier, the NameCert surface cannot export quotient coarse maps, selected representatives, countable choice, ambient bornologies, or small-scale uniform equivalence.
\end{proof}

\begin{theorem}[CoarseSpace metric entourage handoff]
\label{thm:coarse-space-metric-entourage-handoff}
Every metric-controlled $\CoarseSpaceUp$ read factors through the displayed $\MetricUp$ source and the existing $\CoarseMetricSpaceUp$ specialization before the controlled-entourage rows are consumed. The handoff may also be read against $\UniformEntourageSpaceUp$ and $\UniformSpaceCompletionUp$ as provenance for entourage and completion-facing consumers, but it does not assert that the large-scale controlled structure is equivalent to the small-scale uniform structure.
\end{theorem}
\begin{proof}
Read the metric-controlled request through the provenance row $P$. Its metric side is the displayed $\MetricUp$ source, and its large-scale specialization is the $\CoarseMetricSpaceUp$ route already named as a dependency of the carrier.

The controlled-entourage rows of $X$ are then consumed through $\Delta,E,J,K,I,H,C,P,N$. The $\UniformEntourageSpaceUp$ and $\UniformSpaceCompletionUp$ references record where entourage and completion-facing readers may inspect compatible finite rows.

Nothing in the packet identifies those small-scale rows with the large-scale controlled structure. The handoff is therefore a factorization route for metric-controlled coarse reads, not a uniform-equivalence theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\CoarseSpaceUp$ read displays the source row, diagonal controlled-entourage row, controlled-entourage packet, finite union ledger, composition ledger, available inverse or symmetry row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before any large-scale control is exported.}

\independenceWitness{metric, uniform-entourage-space, coarse-metric-space, uniform-space-completion: no listed sibling is bijective to $\CoarseSpaceUp$ because this packet jointly carries diagonal, controlled union, controlled composition, optional inverse or symmetry, transport, replay, provenance, and local naming rows for an abstract controlled-set carrier.}

\closureat{CoarseSpaceUp}{seedStr}

\begin{closurestatus}{\CoarseSpaceUp}
  \constructivestory{A $\CoarseSpaceUp$ packet is generated from displayed source histories, a diagonal controlled-entourage row, finite controlled-entourage rows, controlled union and composition ledgers, an available inverse or symmetry row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:coarse-space-carrier}, \autoref{thm:coarse-space-namecert-obligations}, and \autoref{thm:coarse-space-metric-entourage-handoff} seed the finite controlled-set surface and its metric-controlled handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient coarse categories, small-scale uniform equivalence, selected representatives, countable choice, ambient bornologies, completion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, diagonal exposure, controlled union, controlled composition, inverse or symmetry transport, metric handoff, replay, provenance, and local naming for BEDC.Derived.CoarseSpaceUp.}
\end{closurestatus}
