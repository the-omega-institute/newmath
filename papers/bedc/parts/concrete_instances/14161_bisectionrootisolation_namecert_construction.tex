\chapter{A Concrete Naming Certificate for $\BisectionRootIsolationUp$}
\label{ch:concrete-instances-bisectionrootisolation-namecert}
\origin{human}

Following Bishop and Bridges, constructive root isolation is read here as a finite sign-change bisection packet over located intervals rather than as a host root-existence theorem. The $\BisectionRootIsolationUp$ surface binds endpoint windows, sign rows, branch choices, regular rational readback, and real-name seal rows into one local $\NameCert$ packet. It consumes the located interval and dyadic bisection certificates of \autoref{ch:concrete-instances-locatedinterval-namecert} and \autoref{ch:concrete-instances-dyadicbisection-namecert}, with tolerance and readback rows supplied by \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Bisection root-isolation carrier]
\label{def:bisection-root-isolation-carrier}
A $\BisectionRootIsolationUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
Q=(J,B,F,E,W,R,S,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $J$ is the $\LocatedIntervalUp$ endpoint packet, exposing the lower and upper endpoint windows, rational cell ledger, optional dyadic refinements, $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, and $\RealUp$ seal-facing rows;
\item $B$ is the $\DyadicBisectionUp$ branch ledger whose retained interval rows refine the endpoint packet of $J$ by finite midpoint comparisons;
\item $F$ is the function-inspection ledger, consisting only of finite rational and dyadic reads used to evaluate the displayed sign rows at the retained endpoints and midpoints;
\item $E$ records the sign rows at the retained lower and upper endpoints, together with the midpoint sign rows used to choose each branch;
\item $W$ is the $\StreamNameUp$ endpoint-window ledger exposed to downstream consumers after the retained interval is selected;
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the retained endpoint windows and the dyadic tolerance rows supplied through $\DyadicRatCoreUp$;
\item $S$ is the $\RealUp$ seal surface for the retained lower and upper endpoint names and for any real-facing consumer of the isolating window;
\item $H$ stores the componentwise $\hsame$ transports for the located interval packet, bisection ledger, sign rows, stream windows, regular rational readbacks, real seal rows, and local certificate row;
\item $C$ stores the displayed $\Cont$ replay routes for endpoint inspection, midpoint inspection, branch selection, retained-window readback, regular-sequence handoff, and real-name seal consumption;
\item $P$ is the $\Pkg$ provenance over $\LocatedIntervalUp$, $\DyadicBisectionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\BisectionRootIsolationUp}$ registration row.
\end{enumerate}
The carrier predicate says that these rows form one finite sign-change bisection packet. It has no coordinate for a host root, a general intermediate value theorem, trichotomy on reals, a completed intersection of nested intervals, or a completeness theorem for $\RealUp$.
\end{definition}
\leandef{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationUp}
\leanchecked{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bisection root-isolation NameCert obligations]
\label{thm:bisection-root-isolation-namecert-obligations}
For every accepted $\BisectionRootIsolationUp$ carrier $Q=(J,B,F,E,W,R,S,H,C,P,N)$, the local $\NameCert_{\BisectionRootIsolationUp}$ surface consists exactly of:
\begin{enumerate}
\item carrier admission for the displayed finite packet of \autoref{def:bisection-root-isolation-carrier};
\item endpoint-window admission through the $\LocatedIntervalUp$ rows of $J$ and the retained interval rows of $B$;
\item sign-ledger exactness, requiring each branch choice in $B$ to be justified by one displayed sign row in $E$ read through $F$;
\item retained-window handoff through $W$, $R$, and $S$;
\item componentwise transport and replay through $H$ and $C$;
\item package provenance and local naming through $P$ and $N$.
\end{enumerate}
No further analytic obligation is part of the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bisection-root-isolation-carrier}. The endpoint packet $J$ supplies the located lower and upper endpoint windows, while the bisection ledger $B$ supplies the finite retained-interval chain.

The sign-ledger obligation is local to $F$ and $E$. Each midpoint inspection reads a finite rational or dyadic approximation row, records the displayed sign row, and then selects the corresponding branch row in $B$.

The retained-window handoff is the route already available from \autoref{ch:concrete-instances-dyadicbisection-namecert} after the endpoints are read through \autoref{ch:concrete-instances-locatedinterval-namecert}. The tolerance rows come from \autoref{ch:concrete-instances-dyadicratcore-namecert}; the window, readback, and seal rows are then routed through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.

Finally, $H,C,P$, and $N$ account for transport, replay, provenance, and local naming. Since the carrier stores only these finite rows, it cannot export host root existence, an intermediate value theorem, real trichotomy, a completed interval intersection, or completeness for $\RealUp$.
\end{proof}

\begin{theorem}[Bisection root-isolation RegSeqRat window handoff]
\label{thm:bisection-root-isolation-regseqrat-window-handoff}
For every accepted $\BisectionRootIsolationUp$ carrier and every unary precision request $n$, the $\RegSeqRatUp$ readback exposed by the isolating window factors through the $\LocatedIntervalUp$ endpoint packet, the $\DyadicBisectionUp$ branch prefix that reaches the retained window at tolerance $2^{-n}$, the displayed sign rows at retained endpoints, the $\DyadicRatCoreUp$ tolerance row, the $\StreamNameUp$ endpoint windows, and the replay rows $H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationRegSeqRatWindowHandoff}
\begin{proof}
Begin with the unary request $n$. The dyadic tolerance row $2^{-n}$ is read through the $\DyadicRatCoreUp$ provenance, and the branch prefix in the $\DyadicBisectionUp$ ledger selects the retained interval window at that tolerance.

The located endpoint packet then supplies the lower and upper endpoint windows for that retained interval. The sign ledger contributes only the displayed endpoint signs and midpoint signs already used to select the branch prefix; it does not introduce a root object.

Route the retained endpoint windows through the $\StreamNameUp$ rows and then through the $\RegSeqRatUp$ readback row. The replay remains inside $H,C,P$, and $N$, so every rational read exposed to the regular-sequence consumer is one of the retained finite endpoint windows.
\end{proof}

\begin{theorem}[Bisection root-isolation Real seal nonescape]
\label{thm:bisection-root-isolation-real-seal-nonescape}
Every $\RealUp$ seal read exported by an accepted $\BisectionRootIsolationUp$ carrier is reached only after the retained $\LocatedIntervalUp$ endpoint windows, the $\DyadicBisectionUp$ branch ledger, the displayed endpoint sign rows, the $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback, and the rows $H,C,P,N$ have been read. The seal exposes the finite isolating window and exports no host root existence, general intermediate value theorem, real trichotomy, completed interval intersection, or $\RealUp$ completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationRealSealNonescape}
\begin{proof}
Read the real-facing coordinate $S$ in \autoref{def:bisection-root-isolation-carrier}. It is not an independent real object; it is stored with the endpoint packet, branch ledger, sign rows, stream windows, regular readback, transport rows, replay routes, provenance, and local certificate.

Apply \autoref{thm:bisection-root-isolation-regseqrat-window-handoff}. The retained endpoint windows first pass through the $\StreamNameUp$ and $\RegSeqRatUp$ surfaces at the requested dyadic tolerance.

Only after that route is fixed can the $\RealUp$ seal be read. The seal therefore records the same finite isolating window and no additional analytic witness. Because no coordinate of $Q$ stores a host root, an intermediate value theorem, trichotomy data, a completed intersection, or completeness for $\RealUp$, the real-facing consumer cannot escape to any of those objects.
\end{proof}

\begin{theorem}[Bisection root-isolation finite sign-change certificate]
\label{thm:bisection-root-isolation-finite-sign-change-certificate}
For every accepted $\BisectionRootIsolationUp$ carrier $Q=(J,B,F,E,W,R,S,H,C,P,N)$, every sign-change assertion visible to the local $\NameCert_{\BisectionRootIsolationUp}$ is certified by finite endpoint and midpoint rows in $F$, $E$, and $B$. The certificate contains no host intermediate value theorem, no real trichotomy oracle, no root object, and no completed intersection of retained intervals.
\end{theorem}
\leanchecked{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationFiniteSignChangeCertificate}
\begin{proof}
Project the carrier rows of \autoref{def:bisection-root-isolation-carrier}. The only function-inspection data are the finite rational and dyadic reads in $F$, and the only sign data are the endpoint and midpoint rows displayed in $E$.

Apply \autoref{thm:bisection-root-isolation-namecert-obligations}. Each branch choice in $B$ must be justified by one of those displayed rows, so the sign-change surface is exhausted by the finite ledger already stored in the packet.

The excluded analytic objects are absent from the carrier coordinates. Thus the certificate can replay the accepted sign rows and retained branch choices, but it cannot export a host root, a general intermediate value theorem, trichotomy on reals, or a completed interval intersection.
\end{proof}

\begin{theorem}[Bisection root-isolation retained-window refinement]
\label{thm:bisection-root-isolation-retained-window-refinement}
For every accepted $\BisectionRootIsolationUp$ carrier and every unary precision request $n$, the retained isolating window at tolerance $2^{-n}$ factors through the $\DyadicBisectionUp$ retained branch prefix, the $\LocatedIntervalUp$ endpoint rows, the $\DyadicRatCoreUp$ tolerance row, the $\StreamNameUp$ endpoint windows, and the $\RegSeqRatUp$ readback exposed by the carrier.
\end{theorem}
\leanchecked{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationRetainedWindowRefinement}
\begin{proof}
Begin with the unary request $n$. The tolerance row is the $\DyadicRatCoreUp$ row already named in \autoref{def:bisection-root-isolation-carrier}, and the branch prefix is the retained prefix stored in the $\DyadicBisectionUp$ ledger $B$.

The endpoint rows come from the located interval packet $J$. Once the retained prefix is fixed, the selected lower and upper windows pass through the $\StreamNameUp$ rows $W$.

Apply \autoref{thm:bisection-root-isolation-regseqrat-window-handoff}. The $\RegSeqRatUp$ row $R$ reads back exactly those retained endpoint windows at the requested tolerance, with transport and replay confined to $H,C,P$, and $N$.
\end{proof}

\begin{theorem}[Bisection root-isolation downstream boundary]
\label{thm:bisection-root-isolation-downstream-boundary}
Every downstream $\RealUp$ consumer of an accepted $\BisectionRootIsolationUp$ carrier receives only the retained isolating window determined by $J$, $B$, $W$, $R$, $S$, $H$, $C$, $P$, and $N$. Such a consumer cannot extract host root existence, a completed interval intersection, a general intermediate value theorem, real trichotomy, or $\RealUp$ completeness from the packet.
\end{theorem}
\leanchecked{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationDownstreamBoundary}
\begin{proof}
Read the downstream-facing seal row through \autoref{thm:bisection-root-isolation-real-seal-nonescape}. The seal is reached only after the retained located endpoints, branch ledger, stream windows, regular readback, and replay rows have already been fixed.

The retained-window route is the one described in \autoref{thm:bisection-root-isolation-retained-window-refinement}. It delivers a finite window at the requested tolerance, not an independent analytic object.

Since \autoref{thm:bisection-root-isolation-finite-sign-change-certificate} confines the sign-change evidence to finite endpoint and midpoint rows, no downstream consumer can recover the excluded host principles from the packet.
\end{proof}

\begin{theorem}[Bisection root-isolation public export]
\label{thm:bisection-root-isolation-public-export}
Every public consumer of an accepted $\BisectionRootIsolationUp$ carrier must read the $\LocatedIntervalUp$ endpoint packet, the $\DyadicBisectionUp$ branch ledger, the $\DyadicRatCoreUp$ tolerance row, the $\StreamNameUp$ endpoint windows, the $\RegSeqRatUp$ readback row, the $\RealUp$ seal, and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows in that order before using the isolation certificate. The exported object is the finite retained isolating window with displayed sign-change evidence; it is not a selected root, a real-trichotomy decision, quotient equality of real names, a completed nested-interval point, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the accepted carrier $Q=(J,B,F,E,W,R,S,H,C,P,N)$ of \autoref{def:bisection-root-isolation-carrier}. The carrier already orders the public data: $J$ supplies the located endpoint packet, $B$ supplies the retained dyadic branch ledger, and the dyadic tolerance rows are the arithmetic support named in the same packet.

Apply \autoref{thm:bisection-root-isolation-namecert-obligations}. It forces the endpoint-window admission and sign-ledger exactness to be read before retained-window handoff, so the public consumer cannot inspect $W,R,S$ until the located interval, bisection ledger, tolerance support, and displayed sign rows have been exposed.

Next apply \autoref{thm:bisection-root-isolation-regseqrat-window-handoff} and \autoref{thm:bisection-root-isolation-real-seal-nonescape}. These statements route the retained endpoint windows through $\StreamNameUp$, $\RegSeqRatUp$, and the $\RealUp$ seal without producing an independent real object.

Finally use \autoref{thm:bisection-root-isolation-downstream-boundary}. The structural rows $H,C,P,N$ transport, replay, package, and name exactly that finite window. Since the carrier has no coordinate for a selected root, real trichotomy, quotient real equality, a completed nested-interval point, or ambient completeness, the public export is exhausted by the ordered rows listed in the statement.
\end{proof}

\begin{theorem}[Bisection root-isolation sign-ledger determinacy]
\label{thm:bisection-root-isolation-sign-ledger-determinacy}
Let $Q=(J,B,F,E,W,R,S,H,C,P,N)$ be an accepted $\BisectionRootIsolationUp$ carrier. For every retained branch row in $B$, the corresponding finite inspection row in $F$ and displayed sign row in $E$ determine the branch visible to the local $\NameCert_{\BisectionRootIsolationUp}$ packet. The visible branch decision is therefore a finite $\BHist$ route through $F,E,B,H,C,P,N$, not a host trichotomy comparison or an external root-location oracle.
\end{theorem}
\begin{proof}
Project the carrier rows $F,E,B$ from \autoref{def:bisection-root-isolation-carrier}. The function-inspection ledger $F$ is the only source of endpoint and midpoint reads, and $E$ is the only sign ledger stored for those reads.

Apply the NameCert obligations of \autoref{thm:bisection-root-isolation-namecert-obligations}. They require each branch choice in $B$ to be justified by one displayed sign row in $E$ read through $F$, with transport and replay confined to $H,C,P,N$.

Thus two accepted replays with the same inspected row and the same displayed sign row expose the same retained branch row to the packet. Since no coordinate stores real trichotomy, a host root, or an external oracle, the branch visible to $\NameCert_{\BisectionRootIsolationUp}$ is determined exactly by the finite sign ledger.
\end{proof}

\begin{theorem}[Bisection root-isolation branch monotonicity]
\label{thm:bisection-root-isolation-branch-monotonicity}
For an accepted $\BisectionRootIsolationUp$ carrier, extending the dyadic bisection depth refines the retained interval window and never enlarges the $\RealUp$-seal-facing interval. The refinement route factors through the retained $\DyadicBisectionUp$ branch prefix, the $\LocatedIntervalUp$ endpoint rows, the displayed sign ledger, the $\StreamNameUp$ windows, and the $\RegSeqRatUp$ readback rows.
\end{theorem}
\begin{proof}
Start with two unary depth requests where the second extends the first. The retained branch prefix lives in the $\DyadicBisectionUp$ ledger $B$, and \autoref{thm:bisection-root-isolation-retained-window-refinement} routes each requested depth through the located endpoint rows, dyadic tolerance row, stream windows, and regular readback.

The extension step can only append midpoint inspection and sign rows already admitted by \autoref{thm:bisection-root-isolation-sign-ledger-determinacy}. It therefore selects a subwindow of the earlier retained window rather than a new ambient interval.

The downstream seal is constrained by \autoref{thm:bisection-root-isolation-real-seal-nonescape}. Since every real-facing read passes through the retained window already chosen by the branch ledger, increasing depth can refine the exposed interval but cannot enlarge it or introduce host root existence, completed interval intersection, trichotomy, or $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Bisection root-isolation depth coverage]
\label{thm:bisection-root-isolation-depth-coverage}
Every unary precision request made to an accepted $\BisectionRootIsolationUp$ carrier factors through some finite branch prefix in $B$, the $\DyadicRatCoreUp$ tolerance row, the $\StreamNameUp$ endpoint-window rows, and the $\RegSeqRatUp$ readback row before it reaches a $\RealUp$ seal. The packet therefore covers precision requests by finite bisection depth rather than by a completed nested-interval object.
\end{theorem}
\begin{proof}
Begin with a unary precision request $n$. The $\DyadicRatCoreUp$ row supplies the tolerance $2^{-n}$, and \autoref{thm:bisection-root-isolation-retained-window-refinement} selects a retained branch prefix whose window is read at that tolerance.

The endpoint windows then pass through the $\StreamNameUp$ rows. Apply \autoref{thm:bisection-root-isolation-regseqrat-window-handoff} to read the same retained windows through $\RegSeqRatUp$ at the requested precision.

Finally route the readback to the real-facing boundary using \autoref{thm:bisection-root-isolation-real-seal-nonescape}. The request is exhausted by the finite prefix, tolerance, window, and readback rows, so no completed branch, host root, general intermediate value theorem, trichotomy oracle, or ambient completeness principle is exposed.
\end{proof}

\begin{theorem}[BisectionRootIsolation public isolation certificate]
\label{thm:bisectionrootisolation-public-isolation-certificate}
Every accepted $\BisectionRootIsolationUp$ isolation certificate exposes the $\LocatedIntervalUp$ endpoint rows, the $\DyadicBisectionUp$ branch ledger, the displayed sign-window rows, the $\DyadicRatCoreUp$ tolerance rows, the $\StreamNameUp$ endpoint windows, the $\RegSeqRatUp$ readback, and the $\RealUp$ seal before any public isolation read is available. The public read is therefore the finite retained isolating window with its displayed sign-change ledger, not a host root, a general intermediate value theorem, real trichotomy, a completed interval intersection, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the retained-window refinement of \autoref{thm:bisection-root-isolation-retained-window-refinement}. It routes each requested precision through the located endpoint packet, the retained $\DyadicBisectionUp$ branch prefix, the $\DyadicRatCoreUp$ tolerance row, the finite $\StreamNameUp$ windows, and the $\RegSeqRatUp$ readback.

Use the branch ledger and sign stability supplied by \autoref{thm:bisection-root-isolation-sign-ledger-determinacy} together with \autoref{thm:bisection-root-isolation-finite-sign-change-certificate}. These theorems keep every branch choice inside the finite inspection row, displayed sign row, and retained branch row; no host trichotomy or external root-location oracle is added.

Finally apply the public export and Real-seal boundary of \autoref{thm:bisection-root-isolation-public-export} and \autoref{thm:bisection-root-isolation-real-seal-nonescape}. The public consumer reaches the $\RealUp$ seal only after the endpoint, branch, sign, tolerance, stream-window, and regular-readback rows have been displayed, so the exported isolation certificate is exactly the finite L10 window named in the statement.
\end{proof}

\begin{theorem}[Bisection root-isolation interval nesting]
\label{thm:bisection-root-isolation-interval-nesting}
Every accepted $\BisectionRootIsolationUp$ packet exposes a nested chain of retained windows before its public isolation certificate is consumed. For each retained branch prefix, the lower and upper endpoints of the selected window pass through the $\DyadicRatCoreUp$ tolerance row, the $\StreamNameUp$ endpoint-window rows, the $\RegSeqRatUp$ readback rows, and the $\RealUp$ seal-facing rows; a later retained prefix selects a subwindow of the earlier one and cannot replace that chain by a host root, a completed interval intersection, real trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Induct over the retained-window refinement rows of the $\DyadicBisectionUp$ ledger. The initial row is the located endpoint packet $J$ of \autoref{def:bisection-root-isolation-carrier}; its endpoints are already routed through the displayed dyadic tolerance, stream-window, regular-readback, and real-seal rows.

For the refinement step, use \autoref{thm:bisection-root-isolation-sign-ledger-determinacy}. The next retained branch is determined by the finite inspection row and displayed sign row, so the branch ledger can only select one of the two displayed subwindows of the previous retained window.

Apply \autoref{thm:bisection-root-isolation-retained-window-refinement} and \autoref{thm:bisectionrootisolation-public-isolation-certificate} to the selected prefix. They route its endpoints through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before public consumption. The nonescape boundary of \autoref{thm:bisection-root-isolation-real-seal-nonescape} excludes the host analytic objects, so the public certificate sees a nested retained-window chain rather than an external root-selection principle.
\end{proof}

\begin{theorem}[Bisection root-isolation bridge schema]
\label{thm:bisection-root-isolation-bridge-schema}
Every accepted $\BisectionRootIsolationUp$ packet exports a bridge-readable schema consisting of the located interval source, the $\DyadicRatCoreUp$ bisection ledger, the $\StreamNameUp$ endpoint-window route, the $\RegSeqRatUp$ readback, the terminal $\RealUp$ seal, the finite sign ledger, retained-window monotonicity, and the local $\NameCert_{\BisectionRootIsolationUp}$ rows. The schema is a finite isolation route and does not assert a general intermediate value theorem, host root existence, completed interval intersection, real trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the public isolation certificate of \autoref{thm:bisectionrootisolation-public-isolation-certificate}. It exposes the located endpoint rows, the retained dyadic branch ledger, the displayed sign-window rows, the dyadic tolerance rows, stream windows, regular readback, and the real seal before any public isolation read.

Use interval nesting from \autoref{thm:bisection-root-isolation-interval-nesting}. The retained prefixes form the bridge-visible window chain, and each endpoint read is still routed through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$.

Add branch monotonicity and depth coverage from \autoref{thm:bisection-root-isolation-branch-monotonicity} and \autoref{thm:bisection-root-isolation-depth-coverage}. These rows identify the retained-window monotonicity and the finite prefix selected for each unary precision request.

Finally apply the Real seal nonescape boundary of \autoref{thm:bisection-root-isolation-real-seal-nonescape}. The bridge-readable schema is therefore exhausted by the displayed carrier rows and the local $\NameCert_{\BisectionRootIsolationUp}$ route, with no host analytic principle available beyond the finite isolation packet.
\end{proof}

\closureat{BisectionRootIsolationUp}{matureStr}

\begin{closurestatus}{\BisectionRootIsolationUp}
  \constructivestory{A $\BisectionRootIsolationUp$ packet is generated from a $\LocatedIntervalUp$ endpoint packet, a $\DyadicBisectionUp$ branch ledger, finite sign rows at retained endpoints and midpoints, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\RealUp$ seal rows, displayed $\hsame$ and $\Cont$ replay, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:bisection-root-isolation-carrier}, \autoref{thm:bisection-root-isolation-namecert-obligations}, \autoref{thm:bisection-root-isolation-regseqrat-window-handoff}, \autoref{thm:bisection-root-isolation-real-seal-nonescape}, \autoref{thm:bisection-root-isolation-finite-sign-change-certificate}, \autoref{thm:bisection-root-isolation-retained-window-refinement}, \autoref{thm:bisection-root-isolation-downstream-boundary}, \autoref{thm:bisectionrootisolation-public-isolation-certificate}, and \autoref{thm:bisection-root-isolation-bridge-schema} bind the carrier, NameCert obligations, RegSeqRat window handoff, Real seal nonescape, finite sign-change certificate, retained-window refinement, downstream boundary, public isolation certificate, bridge-readable schema, and BEDC rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\RegSeqRatUp$, $\StreamNameUp$, and $\RealUp$ to a finite sign-change bisection certificate over located intervals.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.BisectionRootIsolationUp.BisectionRootIsolationDownstreamBoundary}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host root existence, a general intermediate value theorem, real trichotomy, completed interval intersection, or $\RealUp$ completeness.}
  \upgradepath{No further theory-axis upgrade remains; bridgeCheckedV remains a separate Lean-side bridge target for the displayed bridge schema and retained-window handoff.}
\end{closurestatus}
