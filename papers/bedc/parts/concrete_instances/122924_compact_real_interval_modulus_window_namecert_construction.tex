\chapter{A Concrete Naming Certificate for $\CompactRealIntervalModulusWindowUp$}
\label{ch:concrete-instances-compact-real-interval-modulus-window-namecert}
\origin{human}

Following Bishop and Bridges on compact intervals and uniform continuity, the $\CompactRealIntervalModulusWindowUp$ packet realizes the countable-completion spine of \autoref{ch:visions-countable-completion-interface-spine} as finite interval-window and modulus data. It sits after the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the compact metric finite-net route of \autoref{ch:concrete-instances-compactmetric-namecert}, and the continuous-map graph route of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names a finite compact interval modulus window for Real and completion consumers; it does not assert host Heine--Borel compactness, selected Lebesgue numbers, quotient real equality, countable choice, external modulus oracles, or ambient $\RealUp$ completeness.

\begin{definition}[Compact Real interval modulus window carrier]
\label{def:compact-real-interval-modulus-window-carrier}
A $\CompactRealIntervalModulusWindowUp$ carrier is a finite $\BHist$ packet
$$
M=(A,B,D,S,R,K,F,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the lower and upper $\RealUp$ endpoint rows for the inspected compact interval.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the requested source-window and target-window budgets.
\item $S$ is the $\StreamNameUp$ finite window row selecting endpoint, mesh, and function-value observations.
\item $R$ is the $\RegSeqRatUp$ readback row for endpoint streams, mesh centers, and displayed value windows.
\item $K$ is the $\CompactMetricUp$ finite-net row for the compact interval window.
\item $F$ is the $\ContinuousMapUp$ graph row whose values are read only on the displayed compact interval windows.
\item $W$ is the modulus-window ledger, assigning the displayed source tolerance to the target tolerance over the finite compact interval net.
\item $E$ is the terminal $\RealUp$ seal row for endpoint, mesh, and value histories after $S,R,K,F,W$ are displayed.
\item $H$ records componentwise $\hsame$ transport for endpoints, tolerance rows, finite windows, regular readback, compact-net data, continuous-map graph rows, modulus windows, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a tolerance request through finite interval windows, compact finite nets, regular readback, continuous-map graph rows, modulus-window assignment, and the Real seal.
\item $P$ is the $\Pkg$ provenance over $\CompactRealIntervalModulusWindowUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactRealIntervalModulusWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,D,S,R,K,F,W,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected Lebesgue numbers, quotient real equality, countable choice, external modulus oracles, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact Real interval modulus window obligation]
\label{thm:compact-real-interval-modulus-window-obligation}
For every accepted $\CompactRealIntervalModulusWindowUp$ carrier
$$
M=(A,B,D,S,R,K,F,W,E,H,C,P,N),
$$
the local obligation surface consists exactly of endpoint admission through $A,B$, dyadic tolerance admission through $D$, finite window exposure through $S$, regular rational readback through $R$, compact finite-net exposure through $K$, continuous-map graph exposure through $F$, modulus-window assignment through $W$, Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-real-interval-modulus-window-carrier}. The endpoint rows, dyadic tolerance row, finite window row, regular readback row, compact finite-net row, continuous-map graph row, modulus-window ledger, and Real seal are all explicit coordinates.

The countable-name route is read before the compact-modulus route. Rows $S$ and $R$ expose finite StreamName windows and RegSeqRat readback, while $D$ supplies the dyadic tolerance budget used by both source and target windows.

The compact interval route is then read through $K,F,W$. The compact finite-net row fixes the interval windows, the continuous-map graph row supplies displayed values on those windows, and the modulus-window ledger assigns source tolerances to target tolerances over that finite route.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same packet. Since no excluded host compactness, selected Lebesgue number, quotient equality, choice, external oracle, or ambient completeness coordinate is present, the listed rows exhaust the local obligation surface.
\end{proof}

\begin{theorem}[Compact Real interval modulus window NameCert surface]
\label{thm:compact-real-interval-modulus-window-namecert-surface}
Every compact-uniform-continuity, Real-completion, or countable-completion consumer of an accepted $\CompactRealIntervalModulusWindowUp$ packet must read the finite route
$$
A,B,D,S,R,K,F,W,E,H,C,P,N .
$$
This route realizes the countable-completion spine through finite interval endpoints, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance budgets, $\CompactMetricUp$ finite-net rows, $\ContinuousMapUp$ graph rows, modulus-window assignment, and the terminal $\RealUp$ seal. It exports no open-cover compactness theorem, selected Lebesgue number, quotient Real equality, countable-choice row, external modulus oracle, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Apply the obligation theorem of \autoref{thm:compact-real-interval-modulus-window-obligation}. It identifies all endpoint, tolerance, finite-window, readback, compact-net, continuous-map, modulus-window, seal, transport, replay, provenance, and naming rows visible to a consumer.

Read the countable-completion side through $D,S,R,E$. These rows provide dyadic tolerances, finite StreamName windows, RegSeqRat readback, and a terminal Real seal before any completion-facing consumer can use the packet.

Read the compact-uniform side through $K,F,W$. The compact finite-net row and continuous-map graph row provide the displayed interval windows, and the modulus-window ledger is downstream of those finite rows.

The structural rows $H,C,P,N$ preserve the same route. Since the classifier has no coordinates for the excluded host principles, the NameCert surface is exactly the finite route displayed in the statement.
\end{proof}

\falsifiablePrediction{A $\CompactRealIntervalModulusWindowUp$ consumer is refuted whenever it reads a compact interval modulus without exposing Real endpoint rows, DyadicRatCore tolerances, StreamName windows, RegSeqRat readback, CompactMetric finite-net rows, ContinuousMap graph rows, a modulus-window ledger, Real sealing, transport, replay, provenance, and local naming.}

\closureat{CompactRealIntervalModulusWindowUp}{seedStr}

\begin{closurestatus}{\CompactRealIntervalModulusWindowUp}
  \constructivestory{A $\CompactRealIntervalModulusWindowUp$ packet is generated from Real endpoint rows, DyadicRatCore tolerance budgets, finite StreamName windows, RegSeqRat readback, CompactMetric finite-net rows, ContinuousMap graph rows, a modulus-window ledger, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-real-interval-modulus-window-carrier}, \autoref{thm:compact-real-interval-modulus-window-obligation}, and \autoref{thm:compact-real-interval-modulus-window-namecert-surface} seed the finite compact interval modulus-window route for countable-completion and compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected Lebesgue numbers, quotient Real equality, countable choice, external modulus oracles, ambient $\RealUp$ completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint admission, dyadic tolerance exactness, finite window generation, regular readback, compact finite-net exposure, continuous-map graph exposure, modulus-window assignment, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.CompactRealIntervalModulusWindowUp.}
\end{closurestatus}
