\chapter{A Concrete Naming Certificate for $\PrimeSieveOddExactnessUp$}
\label{ch:concrete-instances-prime-sieve-odd-exactness-namecert}
\origin{ai}

The $\PrimeSieveOddExactnessUp$ packet realizes the finite prime-window mirror-odd defect route of \autoref{sec:unified-reality-prime-sieve-odd-exactness} as NameCert data. It consumes the zero-route initiality boundary of \autoref{ch:concrete-instances-rh-free-zero-initiality-namecert}, the typed zero-factorization surface of \autoref{ch:concrete-instances-zeta-zero-type-factorization-namecert}, the finite prime-window gluing surface of \autoref{ch:concrete-instances-finite-prime-window-gluing-namecert}, the Euler-tick ledger of \autoref{ch:concrete-instances-zeta-euler-tick-ledger-namecert}, and the analytic compatibility surface of \autoref{ch:concrete-instances-prime-shift-analytic-compatibility-namecert}. The packet records a finite refusal route; it is not a proof of $\mathsf{RH}$, an analytic continuation theorem, a completed zero theorem, or an ambient zeta oracle.

\begin{definition}[Prime-sieve odd exactness carrier]
\label{def:prime-sieve-odd-exactness-carrier}
A $\PrimeSieveOddExactnessUp$ carrier is a finite $\BHist$ packet
$$
E=(Z,T,F,O,S,R,A,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the zero-route initiality row read through $\RhFreeZeroInitialityUp$.
\item $T$ is the typed zero-factorization row read through $\ZetaZeroTypeFactorizationUp$.
\item $F$ is the finite prime-window row naming the inspected prime sieve and its transport through $\FinitePrimeWindowGluingUp$.
\item $O$ is the mirror-odd defect row for that finite prime window.
\item $S$ is the parity-row finite sieve ledger tying the odd-defect read to the prime-window surface.
\item $R$ is the zero-refusal row stating that an accepted completed zero ledger cannot coexist with the displayed nonzero mirror-odd defect.
\item $A$ is the analytic compatibility row read through $\PrimeShiftAnalyticCompatibilityUp$ and the $\ZetaEulerTickLedgerUp$ route.
\item $L$ is the replay ledger that records prime-window transport, parity-row readback, zero-refusal replay, and provenance readback.
\item $H$ records componentwise $\hsame$ transport for $Z,T,F,O,S,R,A,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a zero-defect query through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\PrimeSieveOddExactnessUp$, $\RhFreeZeroInitialityUp$, $\ZetaZeroTypeFactorizationUp$, $\FinitePrimeWindowGluingUp$, $\ZetaEulerTickLedgerUp$, $\PrimeShiftAnalyticCompatibilityUp$, $\ComplexUp$, $\ZetaUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PrimeSieveOddExactnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for an ambient zero theorem, analytic continuation, a completed Euler product, an unbounded prime sieve, a selected zero ledger, or a host $\ComplexUp$ oracle.
\end{definition}

\begin{theorem}[Prime-sieve odd exactness NameCert obligations]
\label{thm:prime-sieve-odd-exactness-namecert-obligations}
For every accepted $\PrimeSieveOddExactnessUp$ carrier $E=(Z,T,F,O,S,R,A,L,H,C,P,N)$, the local $\NameCert_{\PrimeSieveOddExactnessUp}$ surface consists exactly of zero-route initiality exposure, typed zero-factorization exposure, finite prime-window exposure, mirror-odd defect exposure, parity-row finite-sieve readback, zero-refusal readback, analytic compatibility readback, replay-ledger exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:prime-sieve-odd-exactness-carrier}. The zero-facing rows are $Z$ and $T$, the finite prime-sieve rows are $F,O,S$, and the refusal and compatibility rows are $R,A,L$.

The finite prime-window side is displayed before any zero refusal is read. The rows $F,O,S$ name the prime window, its mirror-odd defect, and the parity-row sieve ledger, so a consumer cannot replace the finite defect by an unbounded sieve or by a host analytic assertion.

The zero-facing side is also displayed. Rows $Z,T$ identify the zero route and typed factorization surface, while $R$ records the refusal that a completed zero ledger coexist with the displayed nonzero odd defect.

Rows $A,L,H,C,P,N$ preserve, replay, source, and name the same route. Since no further carrier coordinate exists, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Prime-sieve odd defect refusal route]
\label{thm:prime-sieve-odd-exactness-defect-refusal-route}
Every accepted $\PrimeSieveOddExactnessUp$ consumer that rejects a zero-defect read must display the finite prime-window odd-defect row, the parity-row sieve ledger, zero-route initiality, typed zero factorization, zero-refusal replay, analytic compatibility, transport, provenance, and local NameCert rows. The refusal route is therefore finite BEDC data, not an ambient $\mathsf{RH}$ proof or a completed analytic continuation theorem.
\end{theorem}
\begin{proof}
Use \autoref{thm:prime-sieve-odd-exactness-namecert-obligations}. It forces the finite prime-window rows $F,O,S$ to be present before the zero-refusal row $R$ can be consumed.

Read the zero-facing rows next. The rows $Z$ and $T$ route the consumer through $\RhFreeZeroInitialityUp$ and $\ZetaZeroTypeFactorizationUp$, so the refusal is attached to a typed zero surface rather than to an unrelated analytic endpoint.

Finally replay the compatibility and structural rows $A,L,H,C,P,N$. They preserve the prime-window, parity, zero-refusal, transport, and provenance data while exporting none of the excluded analytic endpoints.
\end{proof}

The Lean follow-up in \texttt{BEDC/Derived/PrimeSieveOddExactnessUp/TasteGate.lean} must provide \texttt{Nontrivial} and \texttt{FieldFaithful} instances so the finite prime-window, parity-row, zero-refusal, transport, replay, provenance, and NameCert rows cannot collapse into a smoke carrier.

\falsifiablePrediction{Any accepted zero-defect consumer without finite prime-window odd-defect, parity-row sieve, zero-refusal, transport, replay, provenance, and $\NameCert$ rows refutes the $\PrimeSieveOddExactnessUp$ carrier.}

\independenceWitness{rh-free-zero-initiality, zeta-zero-type-factorization, finite-prime-window-gluing, zeta-euler-tick-ledger, prime-shift-analytic-compatibility: $\PrimeSieveOddExactnessUp$ is not bijective to any listed sibling because it jointly binds zero-route initiality, typed zero factorization, finite prime-window gluing, Euler-tick replay, analytic compatibility, mirror-odd defect refusal, transport, replay, provenance, and local naming as one finite packet.}

\closureat{PrimeSieveOddExactnessUp}{seedStr}

\begin{closurestatus}{\PrimeSieveOddExactnessUp}
  \constructivestory{A $\PrimeSieveOddExactnessUp$ packet is generated from zero-route initiality rows, typed zero-factorization rows, finite prime-window rows, mirror-odd defect rows, parity-row finite-sieve ledgers, zero-refusal rows, analytic compatibility rows, replay ledgers, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:prime-sieve-odd-exactness-carrier}, \autoref{thm:prime-sieve-odd-exactness-namecert-obligations}, and \autoref{thm:prime-sieve-odd-exactness-defect-refusal-route} seed the finite prime-window odd-defect refusal route for zero-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close $\mathsf{RH}$, analytic continuation, completed zero exclusion, Euler-product convergence, host complex analysis, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked zero-route initiality exposure, typed zero-factorization exposure, finite prime-window exposure, mirror-odd defect exposure, parity-row finite-sieve readback, zero-refusal readback, analytic compatibility readback, replay-ledger exactness, transport, replay, provenance, and local naming for BEDC.Derived.PrimeSieveOddExactnessUp.}
\end{closurestatus}
