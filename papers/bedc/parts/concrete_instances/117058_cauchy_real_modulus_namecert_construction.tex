\chapter{A Concrete Naming Certificate for $\CauchyRealModulusUp$}
\label{ch:concrete-instances-cauchy-real-modulus-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names, the $\CauchyRealModulusUp$ packet records a real-name carrier whose convergence evidence is an explicit modulus row rather than a host completeness principle. It sits between the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational-name surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-seal boundary of \autoref{ch:concrete-instances-real-namecert}. The packet gives downstream completion consumers a finite place to cite the modulus evidence attached to a Cauchy real name; it does not select a limit, quotient streams, or import countable choice.

\begin{definition}[CauchyRealModulus carrier]
\label{def:cauchy-real-modulus-carrier}
A $\CauchyRealModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(S,R,D,\mu,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ schedule whose finite windows are available to a real-name consumer.
\item $R$ is the $\RegSeqRatUp$ row reading the rational approximants through the displayed stream schedule.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the regular-name comparisons.
\item $\mu$ is the modulus row assigning each displayed dyadic tolerance request to a visible stream window.
\item $Q$ is the Cauchy coherence classifier, comparing accepted packets only through $S,R,D,\mu$.
\item $H$ stores componentwise $\hsame$ transport for $S,R,D,\mu,Q,C,P,N$.
\item $C$ stores displayed $\Cont$ replay of the stream schedule, regular rational readback, dyadic tolerance ledger, modulus row, coherence classifier, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\CauchyRealModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CauchyRealModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for a selected real limit, host real equality, quotient-stream equality, ambient completeness, or countable choice.
\end{definition}

\begin{theorem}[CauchyRealModulus obligation surface]
\label{thm:cauchy-real-modulus-obligation-surface}
For every accepted $\CauchyRealModulusUp$ carrier $M=(S,R,D,\mu,Q,H,C,P,N)$, the local $\NameCert_{\CauchyRealModulusUp}$ surface consists exactly of stream-window admission through $S$, regular rational-name exposure through $R$, dyadic tolerance exposure through $D$, explicit modulus visibility through $\mu$, Cauchy coherence classification through $Q$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier. The analytic rows are exhausted by the stream schedule, the regular rational readback, the dyadic tolerance ledger, the modulus row, and the coherence classifier.

The modulus evidence is not inferred from ambient completeness. It is a row of the same packet, so a consumer must expose $\mu$ together with $S,R,D$ before reading the classifier $Q$.

The structural rows $H,C,P,N$ only transport, replay, source, and name those displayed coordinates. Since the carrier lists no selected limit, quotient-stream equality, host real equality, completeness theorem, or choice principle, the obligation surface is exactly the finite inventory above.
\end{proof}

\begin{theorem}[CauchyRealModulus Real-seal nonescape]
\label{thm:cauchy-real-modulus-real-seal-nonescape}
Every $\RealUp$-facing consumer of an accepted $\CauchyRealModulusUp$ packet factors its read through the $\StreamNameUp$ row $S$, the $\RegSeqRatUp$ row $R$, the $\DyadicRatCoreUp$ row $D$, the explicit modulus row $\mu$, the coherence classifier $Q$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The read cannot manufacture a selected limit, quotient equality of streams, host real equality, ambient completeness, or countable choice.
\end{theorem}
\begin{proof}
Begin with the carrier inventory of \autoref{def:cauchy-real-modulus-carrier}. The only route from the real-name packet toward a $\RealUp$ consumer is the displayed chain $S,R,D,\mu,Q$.

The replay row $C$ forces the same stream, regular-name, tolerance, and modulus rows to be reopened before the real-seal boundary is consumed. Transport through $H$ preserves those rows componentwise, and provenance through $P$ records the same source route for the local naming row $N$.

The excluded objects are absent from the packet. Therefore the $\RealUp$ read may cite an explicitly modulated Cauchy real name, but it cannot create a selected limit, quotient-stream equality, host equality of reals, completeness theorem, or choice principle.
\end{proof}

\closureat{CauchyRealModulusUp}{seedStr}

\begin{closurestatus}{\CauchyRealModulusUp}
  \constructivestory{A $\CauchyRealModulusUp$ packet is generated from stream-window rows, regular rational-name rows, dyadic tolerance rows, an explicit modulus row, a Cauchy coherence classifier, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-real-modulus-carrier}, \autoref{thm:cauchy-real-modulus-obligation-surface}, and \autoref{thm:cauchy-real-modulus-real-seal-nonescape} bind the explicit-modulus Cauchy real-name carrier to the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ route.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close selected limits, quotient equality of streams, host real equality, ambient completeness, countable choice, or a Lean carrier for $\CauchyRealModulusUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, stream-window exposure, regular rational readback, dyadic tolerance exposure, explicit modulus visibility, classifier coherence, replay, provenance, and local naming for the displayed packet.}
\end{closurestatus}
