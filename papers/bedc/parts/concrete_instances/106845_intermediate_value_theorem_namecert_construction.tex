\chapter{A Concrete Naming Certificate for $\IntermediateValueTheoremUp$}
\label{ch:concrete-instances-intermediate-value-theorem-namecert}
\origin{human}

Following Bishop and Bridges, the $\IntermediateValueTheoremUp$ packet records the constructive intermediate-value theorem as a finite theorem-object certificate rather than as host real analysis. It reads the real-name seal of \autoref{ch:concrete-instances-real-namecert} for $\RealUp$, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert} for $\RegSeqRatUp$, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert} for $\StreamNameUp$, the endpoint interval packet of \autoref{ch:concrete-instances-locatedinterval-namecert} for $\LocatedIntervalUp$, the graph-and-modulus surface of \autoref{ch:concrete-instances-continuousmap-namecert} for $\ContinuousMapUp$, and the closed interval carrier of \autoref{ch:concrete-instances-closedboundedinterval-namecert} for $\ClosedBoundedIntervalUp$. The chapter packages the theorem route as BEDC rows: it does not decide classical trichotomy, use Heine--Borel or open-cover compactness, select a zero, identify quotient real names, or assume ambient $\RealUp$ completeness.

\begin{definition}[Intermediate value theorem carrier]
\label{def:intermediate-value-theorem-carrier}
An $\IntermediateValueTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(I,J,F,A,B,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the accepted $\ClosedBoundedIntervalUp$ source interval row, exposing lower and upper endpoint reads, endpoint order, containment ledgers, and interval windows.
\item $J$ is the $\LocatedIntervalUp$ endpoint-location row used by the sign route, carrying the same endpoint surface as displayed interval data.
\item $F$ is the $\ContinuousMapUp$ graph-and-modulus row for the displayed map on the interval.
\item $A$ and $B$ are the endpoint sign rows, recording the inspected negative-side and positive-side evidence as finite real-name reads rather than as a trichotomy decision.
\item $S$ stores the $\StreamNameUp$ windows used to inspect endpoint, midpoint, and branch values along the theorem route.
\item $R$ stores the $\RegSeqRatUp$ regular rational readbacks attached to those windows.
\item $E$ is the terminal $\RealUp$ seal for the located sign-transition output, reached only after $I,J,F,A,B,S,R$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the interval, location, map, sign, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from an intermediate-value request through $I,J,F,A,B,S,R,E$.
\item $P$ is the $\Pkg$ provenance over $\IntermediateValueTheoremUp$, $\ClosedBoundedIntervalUp$, $\LocatedIntervalUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\IntermediateValueTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,J,F,A,B,S,R,E,H,C,P,N$. It has no coordinate for classical trichotomy, Heine--Borel compactness, open-cover compactness, a selected zero, quotient real equality, host semantic continuity, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Intermediate value theorem NameCert obligations]
\label{thm:intermediate-value-theorem-namecert-obligations}
For every accepted $\IntermediateValueTheoremUp$ carrier
$$
T=(I,J,F,A,B,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\IntermediateValueTheoremUp}$ surface consists exactly of carrier admission for the displayed interval and endpoint-location rows, continuous-map admission through $F$, endpoint sign exposure through $A$ and $B$, finite-window exposure through $S$, regular rational readback through $R$, terminal Real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:intermediate-value-theorem-carrier}. The accepted packet exposes the closed bounded interval row $I$, the located endpoint row $J$, the continuous-map row $F$, the sign rows $A,B$, the finite windows $S$, the regular readbacks $R$, and the terminal seal row $E$.

The theorem route is constructive because every sign and value read is a displayed window-readback pair. The $\StreamNameUp$ row supplies the finite observations, the $\RegSeqRatUp$ row supplies the regular rational readback, and the $\RealUp$ row receives only the sealed sign-transition output.

The structural rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and naming. Since the carrier has no coordinate for classical trichotomy, Heine--Borel or open-cover compactness, a selected zero, quotient real equality, host semantic continuity, or ambient $\RealUp$ completeness, those objects cannot belong to the local NameCert surface.
\end{proof}

\begin{theorem}[Intermediate value theorem finite sign-route handoff]
\label{thm:intermediate-value-theorem-finite-sign-route-handoff}
Every Real-facing consumer of an accepted $\IntermediateValueTheoremUp$ packet factors through
$$
\ClosedBoundedIntervalUp \longrightarrow \LocatedIntervalUp \longrightarrow \ContinuousMapUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp .
$$
The handoff exports only the displayed interval row, endpoint-location row, continuous-map row, endpoint sign rows, finite windows, regular readbacks, Real seal, componentwise transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Start with a Real-facing theorem read. The carrier definition places the terminal seal in row $E$, and $E$ is downstream of the interval, location, map, sign, window, and readback rows.

Trace the read backward through the finite route. The closed interval row $I$ and located endpoint row $J$ supply the domain surface, the continuous-map row $F$ supplies the displayed modulus and graph data, and the endpoint sign rows $A,B$ are inspected only through $S$ and $R$ before the seal is reached.

Finally $H,C,P,N$ transport, replay, package, and name the same displayed route. There is no second coordinate for a selected zero, quotient real equality, classical trichotomy, Heine--Borel compactness, open-cover compactness, host semantic continuity, or ambient $\RealUp$ completeness, so every accepted handoff reaches $\RealUp$ only through the displayed rows.
\end{proof}

\falsifiablePrediction{Every accepted $\IntermediateValueTheoremUp$ Real-facing read factors through a ClosedBoundedInterval source row, a LocatedInterval endpoint-location row, a ContinuousMap graph-and-modulus row, endpoint sign rows, StreamName finite windows, RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}

\independenceWitness{closedboundedinterval, locatedinterval, continuousmap, streamname, regseqrat, real: $\IntermediateValueTheoremUp$ is not bijective to any listed sibling because it jointly carries the theorem-level interval source, endpoint-location surface, continuous-map row, endpoint sign ledger, finite window route, regular rational readback, terminal seal, transport, replay, provenance, and local naming in one packet.}

\closureat{IntermediateValueTheoremUp}{seedStr}

\begin{closurestatus}{\IntermediateValueTheoremUp}
  \constructivestory{An $\IntermediateValueTheoremUp$ packet is generated from a $\ClosedBoundedIntervalUp$ source row, a $\LocatedIntervalUp$ endpoint-location row, a $\ContinuousMapUp$ graph-and-modulus row, endpoint sign rows, $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:intermediate-value-theorem-carrier}, \autoref{thm:intermediate-value-theorem-namecert-obligations}, and \autoref{thm:intermediate-value-theorem-finite-sign-route-handoff} seed the constructive intermediate-value theorem route as a finite NameCert packet over interval, map, sign, window, readback, seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close classical trichotomy, Heine--Borel compactness, open-cover compactness, selected zeros, quotient real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint sign visibility, continuous-map routing, finite-window readback, Real-seal handoff, transport and replay stability, and provenance exhaustion for BEDC.Derived.IntermediateValueTheoremUp.}
\end{closurestatus}
