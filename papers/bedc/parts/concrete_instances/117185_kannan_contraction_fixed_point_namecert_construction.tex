\chapter{A Concrete Naming Certificate for $\KannanContractionFixedPointUp$}
\label{ch:concrete-instances-kannan-contraction-fixed-point-namecert}
\origin{human}

Following Kannan's fixed point theorem and Bishop--Bridges constructive metric analysis, the $\KannanContractionFixedPointUp$ packet records a non-Lipschitz contraction endpoint route as finite NameCert data. It sits next to the complete-metric carrier of \autoref{ch:concrete-instances-completemetric-namecert}, the ordinary contraction-map packet of \autoref{ch:concrete-instances-contractionmapping-namecert}, the Picard orbit ledger of \autoref{ch:concrete-instances-picard-iteration-namecert}, the fixed-point endpoint discipline of \autoref{ch:concrete-instances-banach-fixed-point-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names the finite Kannan adjacent-distance handoff; it does not select a host fixed point, identify quotient orbit tails, use countable choice, assume trichotomy, or import ambient $\RealUp$ completeness.

\begin{definition}[Kannan contraction fixed-point carrier]
\label{def:kannan-contraction-fixed-point-carrier}
A $\KannanContractionFixedPointUp$ carrier is a finite $\BHist$ packet
$$
K=(M,F,A,O,U,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the pointed complete metric source, displaying the carrier, metric comparison rows, chosen starting point, and complete-metric handoff.
\item $F$ is the self-map row, recording the displayed endomap whose Picard orbit is inspected.
\item $A$ is the Kannan adjacent-distance inequality row, exposing a rational coefficient below one half and the comparison
$$
d(Fx,Fy)\le a\cdot(d(x,Fx)+d(y,Fy))
$$
only through displayed metric rows.
\item $O$ is the Picard orbit window row, displaying the finite orbit windows generated by repeatedly reading $F$ from the chosen point.
\item $U$ is the Cauchy modulus row obtained from the adjacent-distance ledger and the visible Picard windows.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational tail estimates consumed by the completion-facing route.
\item $E$ is the $\RealUp$ seal row, exposed only after the complete-metric source, self-map row, adjacent-distance inequality, orbit windows, Cauchy modulus, and regular-rational readback are visible.
\item $H$ records componentwise $\hsame$ transport for source, map, inequality, orbit, modulus, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
M,F,A,O,U,R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\KannanContractionFixedPointUp$, $\CompleteMetricUp$, $\ContractionMappingUp$, $\PicardIterationUp$, $\BanachFixedPointUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KannanContractionFixedPointUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,F,A,O,U,R,E,H,C,P,N$. It has no coordinate for selected host fixed points, quotient orbit equality, countable choice, trichotomy, or ambient real completeness.
\end{definition}

\begin{theorem}[Kannan contraction fixed-point obligations]
\label{thm:kannan-contraction-fixed-point-namecert-obligations}
For every accepted $\KannanContractionFixedPointUp$ carrier $K=(M,F,A,O,U,R,E,H,C,P,N)$, the local $\NameCert_{\KannanContractionFixedPointUp}$ surface consists exactly of pointed complete-metric source exposure through $M$; self-map admission through $F$; Kannan adjacent-distance inequality exposure through $A$; finite Picard orbit windows through $O$; Cauchy modulus extraction through $U$; $\RegSeqRatUp$ readback through $R$; $\RealUp$ seal non-escape through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. The rows $M,F,A,O$ are the complete source surface: a pointed complete metric object, the self-map, the Kannan adjacent-distance inequality, and the finite Picard windows.

The Cauchy handoff is displayed rather than chosen. The row $U$ records the modulus obtained from the adjacent-distance ledger, and $R$ reads it as regular rational tail data before the terminal seal $E$ is available.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since no selected host fixed point, quotient orbit equality, countable-choice selector, trichotomy principle, or ambient real completeness coordinate occurs in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Kannan Picard-to-Cauchy handoff]
\label{thm:kannan-contraction-fixed-point-picard-cauchy-handoff}
Every accepted $\KannanContractionFixedPointUp$ endpoint read factors through the displayed Picard orbit windows, the adjacent-distance Kannan ledger, the Cauchy modulus row, the $\RegSeqRatUp$ readback row, and the $\RealUp$ seal before it can be consumed by a fixed-point endpoint discipline. The route is adjacent to \autoref{ch:concrete-instances-picard-iteration-namecert} and \autoref{ch:concrete-instances-banach-fixed-point-namecert}, but its contraction evidence is the Kannan inequality row rather than a Lipschitz ratio row.
\end{theorem}
\begin{proof}
Start from \autoref{thm:kannan-contraction-fixed-point-namecert-obligations}. It orders the complete-metric source, self-map, adjacent-distance inequality, and finite Picard windows before the modulus and regular-rational readback rows.

The endpoint read is therefore completion-facing only after $U$ and $R$ are visible. Transport and replay preserve this order, and provenance names the route as a $\KannanContractionFixedPointUp$ packet rather than as an ordinary $\ContractionMappingUp$ packet.

Thus any consumer of the seal must pass through the displayed Picard-to-Cauchy handoff. The refused selected fixed point, quotient orbit equality, countable-choice selector, trichotomy principle, and ambient completeness principle have no export coordinate.
\end{proof}

\falsifiablePrediction{An accepted $\KannanContractionFixedPointUp$ endpoint read is refuted if it reaches a $\RealUp$ seal without displaying the pointed complete-metric source, self-map row, Kannan adjacent-distance inequality, Picard windows, Cauchy modulus, $\RegSeqRatUp$ readback, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{complete-metric, contractionmapping, picard-iteration, banach-fixed-point, regseqrat, real: no listed sibling is bijective to $\KannanContractionFixedPointUp$ because the Kannan packet binds the adjacent-distance inequality, Picard orbit windows, Cauchy modulus handoff, regular-rational readback, Real seal, transport, replay, provenance, and local naming as one fixed-point certificate.}

\closureat{KannanContractionFixedPointUp}{seedStr}

\begin{closurestatus}{\KannanContractionFixedPointUp}
  \constructivestory{A $\KannanContractionFixedPointUp$ packet is generated from a pointed complete metric source, a self-map row, a Kannan adjacent-distance inequality row, Picard orbit windows, a Cauchy modulus row, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:kannan-contraction-fixed-point-carrier}, \autoref{thm:kannan-contraction-fixed-point-namecert-obligations}, and \autoref{thm:kannan-contraction-fixed-point-picard-cauchy-handoff} seed the finite Kannan fixed-point handoff route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected host fixed points, quotient orbit equality, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked complete-metric source exposure, self-map admission, Kannan adjacent-distance inequality, Picard orbit windows, Cauchy modulus extraction, RegSeqRat readback, RealUp seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.KannanContractionFixedPointUp.}
\end{closurestatus}
