\chapter{A Concrete Naming Certificate for $\CriticalPairResidualStreamUp$}
\label{ch:concrete-instances-critical-pair-residual-stream-namecert}
\origin{ai}

The $\CriticalPairResidualStreamUp$ packet realizes the residual-join edge of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite MetaCIC-facing NameCert data. It sits beside the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, the same-schema certificate of \autoref{ch:concrete-instances-layeredrelationcert-sameschema-namecert}, and the inter-Hist invariant surface of \autoref{ch:concrete-instances-interhistinvariant-namecert}. These siblings supply conditional local-diamond rows, candidate-normalization rows, relation-certificate scope, and history-invariance rows; this packet records a displayed stream of residual critical-pair windows without turning the stream into a global confluence theorem.

\begin{definition}[Critical-pair residual stream carrier]
\label{def:critical-pair-residual-stream-carrier}
A $\CriticalPairResidualStreamUp$ carrier is a finite $\BHist$ packet
$$
S=(D,N,L,I,R,J,W,E,H,C,P,T)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\MetaCICParallelDiamondFrontierUp$ row supplying the conditional local-diamond frontier.
\item $N$ is the $\MetaCICNormalizationFrontierUp$ row supplying candidate-normalization and endpoint-readback evidence.
\item $L$ is the $\LayeredRelationCertUp$ row naming the same-schema comparison used to keep residual windows in one displayed schema.
\item $I$ is the $\InterHistInvariantUp$ row preserving the residual stream across the inspected Hist boundaries.
\item $R$ is the finite residual-window ledger, listing the critical-pair residuals under inspection.
\item $J$ is the candidate-join ledger attached to the residual windows of $R$.
\item $W$ is the stream-order row, recording the displayed predecessor relation among residual windows.
\item $E$ is the failure row for windows that cannot be routed through the diamond and normalization frontiers.
\item $H$ stores componentwise $\hsame$ transport for $D,N,L,I,R,J,W,E,C,P,T$.
\item $C$ stores displayed $\Cont$ replay of the route from diamond-frontier evidence through normalization, relation scope, inter-Hist invariance, residual windows, and candidate joins.
\item $P$ is the $\Pkg$ provenance over $\CriticalPairResidualStreamUp$, $\MetaCICParallelDiamondFrontierUp$, $\MetaCICNormalizationFrontierUp$, $\LayeredRelationCertUp$, $\InterHistInvariantUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $T$ is the local $\NameCert_{\CriticalPairResidualStreamUp}$ row.
\end{enumerate}
The carrier has no coordinate for residual-substitution discharge, subject reduction, premise-free parallel diamond, unrestricted Church--Rosser, evaluator equality, quotient equality of terms, or kernel consistency.
\end{definition}

\begin{theorem}[Critical-pair residual stream NameCert obligations]
\label{thm:critical-pair-residual-stream-namecert-obligations}
For every accepted $\CriticalPairResidualStreamUp$ carrier
$$
S=(D,N,L,I,R,J,W,E,H,C,P,T),
$$
the local $\NameCert_{\CriticalPairResidualStreamUp}$ surface consists exactly of carrier admission for the displayed diamond-frontier row, normalization-frontier row, same-schema relation row, inter-Hist invariant row, residual-window ledger, candidate-join ledger, stream-order row, failure row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:critical-pair-residual-stream-carrier}. The accepted tuple displays every row used by the residual-stream read: diamond frontier, normalization frontier, relation scope, inter-Hist invariance, residual windows, joins, order, failure, transport, replay, provenance, and name.

The positive residual read is routed through $D,N,L,I,R,J,W$. A consumer first exposes the conditional diamond frontier and candidate-normalization rows, then reads the same-schema and inter-Hist rows, and only then inspects the finite residual-window and candidate-join ledgers.

The failure row $E$ remains in the same packet. Thus a window that lacks the displayed frontier, normalization, relation, or invariant support is not retyped as a successful residual join.

Finally $H,C,P,T$ only preserve, replay, package, and name the displayed rows. Since the carrier contains no further coordinate, the NameCert surface exports no hidden confluence theorem or quotient-level equality.
\end{proof}

\begin{theorem}[Critical-pair residual stream route]
\label{thm:critical-pair-residual-stream-route}
Every public residual-stream read of an accepted $\CriticalPairResidualStreamUp$ packet factors through the finite route
$$
\MetaCICParallelDiamondFrontierUp \longmapsto
\MetaCICNormalizationFrontierUp \longmapsto
\LayeredRelationCertUp \longmapsto
\InterHistInvariantUp \longmapsto R \longmapsto J \longmapsto W .
$$
The route may feed a MetaCIC parallel-diamond consumer as displayed candidate-mediated residual data, but it cannot discharge residual substitution, subject reduction, unrestricted confluence, evaluator equality, quotient equality of terms, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the carrier projection. The residual-window ledger $R$ is not public until the frontier row $D$, normalization row $N$, relation row $L$, and inter-Hist row $I$ have all been exposed.

The candidate joins in $J$ are read only over the displayed windows of $R$, and the stream-order row $W$ records only the finite predecessor relation among those windows. Hence the route is a finite stream of inspected residual windows rather than an ambient reduction relation.

Transport and replay through $H$ and $C$ preserve the same displayed route, while $P$ and $T$ preserve provenance and naming. No structural row adds a residual-substitution proof, subject-reduction proof, global confluence theorem, evaluator equality, quotient equality, or kernel endpoint.
\end{proof}

\falsifiablePrediction{If the residual-window ledger $R$ is read without the displayed $\MetaCICParallelDiamondFrontierUp$, $\MetaCICNormalizationFrontierUp$, $\LayeredRelationCertUp$, and $\InterHistInvariantUp$ rows, the packet must route the request to the failure row $E$ rather than to a residual-stream certificate.}

\independenceWitness{metacicparallel, metacicnormalization, layeredrelationcert, interhistinvariant: $\MetaCICParallelDiamondFrontierUp$ supplies conditional local-diamond rows without the residual stream ledger; $\MetaCICNormalizationFrontierUp$ supplies candidate-normalization rows without the critical-pair stream order; $\LayeredRelationCertUp$ supplies same-schema relation scope without residual joins; $\InterHistInvariantUp$ supplies history-invariance rows without the diamond and normalization handoff. None is bijective to $\CriticalPairResidualStreamUp$ under NameCert-row preservation.}

Lean follow-up requires $\mathsf{Nontrivial}\ \CriticalPairResidualStreamUp$ and $\mathsf{FieldFaithful}\ \CriticalPairResidualStreamUp$ TasteGate instances before the packet can be checked as a single-carrier handoff. Those instances must preserve exactly the rows $D,N,L,I,R,J,W,E,H,C,P,T$.

\closureat{CriticalPairResidualStreamUp}{seedStr}

\begin{closurestatus}{\CriticalPairResidualStreamUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:critical-pair-residual-stream-carrier}, \autoref{thm:critical-pair-residual-stream-namecert-obligations}, \autoref{thm:critical-pair-residual-stream-route}, the falsifiable prediction, and the independence witness seed the finite residual-stream route over MetaCIC parallel-diamond, normalization-frontier, same-schema relation, inter-Hist invariant, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim residual-substitution discharge, subject reduction, premise-free parallel diamond, unrestricted Church--Rosser, evaluator equality, quotient equality of terms, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, residual-window coverage, candidate-join readback, stream-order visibility, frontier and normalization routing, relation and inter-Hist preservation, transport, replay, provenance, and local naming for BEDC.Derived.CriticalPairResidualStreamUp.}
  \constructivestory{A $\CriticalPairResidualStreamUp$ packet is generated from MetaCICParallelDiamondFrontierUp and MetaCICNormalizationFrontierUp rows, a LayeredRelationCertUp same-schema row, an InterHistInvariantUp row, residual-window and candidate-join ledgers, a stream-order row, failure row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
