\chapter{A Concrete Naming Certificate for $\RieszSchauderTheoryUp$}
\label{ch:concrete-instances-riesz-schauder-theory-namecert}
\origin{human}

Following Riesz and Schauder on compact operators in normed spaces, the $\RieszSchauderTheoryUp$ packet records the finite BEDC route by which a displayed compact linear operator exposes spectral-window and Fredholm-facing obligations. It reads the Banach endpoint rows of \autoref{ch:concrete-instances-banach-namecert}, the bounded operator action of \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, the compact-operator certificate of \autoref{ch:concrete-instances-compactoperator-namecert}, the spectral-radius boundary of \autoref{ch:concrete-instances-spectral-radius-namecert}, and the compact-perturbation alternative of \autoref{ch:concrete-instances-fredholm-alternative-namecert}. The packet names only this displayed compact-operator spectral route; it does not import a host spectrum, quotient operator algebra, selected eigenbasis, closed-range complement, or analytic Fredholm theorem.

\begin{definition}[Riesz--Schauder theory carrier]
\label{def:riesz-schauder-theory-carrier}
A $\RieszSchauderTheoryUp$ carrier is a finite $\BHist$ packet
$$
R=(X,Y,B,K,S,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\BanachUp$ source endpoint row.
\item $Y$ is the $\BanachUp$ target endpoint row, or the same endpoint when the compact operator is read endomorphically.
\item $B$ is the $\BoundedLinearOperatorUp$ row naming the displayed linear action and norm-bound ledger between the endpoints.
\item $K$ is the $\CompactOperatorUp$ row naming compactness by finite image-net schedules, tail moduli, and replay data.
\item $S$ is the $\SpectralRadiusUp$ window row used by spectral consumers that inspect finite power-growth and resolvent-test data.
\item $F$ is the $\FredholmAlternativeUp$ handoff row used by range-obstruction and kernel-facing consumers.
\item $H$ records componentwise $\hsame$ transport for the endpoint, bounded-action, compactness, spectral-window, and Fredholm-handoff rows.
\item $C$ records displayed $\Cont$ replay through the same rows.
\item $P$ is the $\Pkg$ provenance over $\RieszSchauderTheoryUp$, $\BanachUp$, $\BoundedLinearOperatorUp$, $\CompactOperatorUp$, $\SpectralRadiusUp$, $\FredholmAlternativeUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RieszSchauderTheoryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a completed spectrum, quotient operator, selected eigenvector chain, algebraic multiplicity, closed-range complement, or host Fredholm determinant.
\end{definition}
\leanstmt{BEDC.Derived.RieszSchauderTheoryUp}

\begin{theorem}[Riesz--Schauder theory NameCert obligations]
\label{thm:riesz-schauder-theory-namecert-obligations}
For an accepted $\RieszSchauderTheoryUp$ carrier $R=(X,Y,B,K,S,F,H,C,P,N)$, the local $\NameCert_{\RieszSchauderTheoryUp}$ surface consists exactly of endpoint admission through $X,Y$, bounded-action admission through $B$, compact-operator admission through $K$, spectral-window exposure through $S$, Fredholm-handoff exposure through $F$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:riesz-schauder-theory-carrier}. The tuple $X,Y,B,K,S,F,H,C,P,N$ is the full visible inventory, so endpoint, action, compactness, spectral-window, Fredholm-handoff, transport, replay, provenance, and naming obligations cannot be enlarged by an external operator object.

Read the analytic dependencies in order. The Banach rows provide the endpoints, the bounded-operator row provides the displayed action, and the compact-operator row supplies the finite compactness witness before any spectral or Fredholm consumer can inspect the packet.

The spectral and Fredholm rows are then handoffs rather than ambient theorems. The row $S$ points only to finite power-growth and resolvent windows from $\SpectralRadiusUp$, while $F$ points only to the kernel or range-obstruction boundary already named by $\FredholmAlternativeUp$.

Finally $H,C,P,N$ preserve, replay, source, and name those same rows. Since the carrier contains no completed spectrum, quotient operator, selected eigenbasis, multiplicity ledger, closed-range complement, or determinant coordinate, the listed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Riesz--Schauder compact Fredholm route]
\label{thm:riesz-schauder-theory-compact-fredholm-route}
Every consumer that reads a Riesz--Schauder spectral or Fredholm conclusion from an accepted $\RieszSchauderTheoryUp$ packet must pass through the displayed route
$$
\BanachUp,\BoundedLinearOperatorUp
\longmapsto \CompactOperatorUp
\longmapsto \SpectralRadiusUp,\FredholmAlternativeUp
\longmapsto \RieszSchauderTheoryUp .
$$
The route exposes compactness through finite image-net schedules and exposes spectral or range-obstruction data only through the sibling windows $S$ and $F$. It does not export a host spectrum, quotient operator algebra, selected eigenspace chain, algebraic multiplicity theorem, or analytic Fredholm determinant.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:riesz-schauder-theory-namecert-obligations}. They force every accepted read to expose the Banach endpoints, bounded action, compactness row, spectral window, Fredholm handoff, and structural rows before any conclusion is public.

The compactness step is the central gate. A consumer cannot enter $S$ or $F$ until $K$ has displayed the finite compact-operator schedules certified by \autoref{ch:concrete-instances-compactoperator-namecert}. The bounded action $B$ remains attached to the same endpoints throughout this replay.

Once the compact row is visible, the spectral side may inspect only the finite resolvent and power-growth windows of \autoref{ch:concrete-instances-spectral-radius-namecert}. The Fredholm side may inspect only the kernel-facing or range-obstruction handoff of \autoref{ch:concrete-instances-fredholm-alternative-namecert}.

Repack the route through $H,C,P,N$. These rows preserve and name the displayed packet, but they do not add a spectrum object, quotient operator algebra, selected eigenvectors, multiplicities, complements, or determinants. Hence every exported Riesz--Schauder read factors through the finite compact-Fredholm route above.
\end{proof}

\closureat{RieszSchauderTheoryUp}{seedStr}

\begin{closurestatus}{\RieszSchauderTheoryUp}
  \constructivestory{A $\RieszSchauderTheoryUp$ packet is generated from Banach endpoint rows, a BoundedLinearOperatorUp action row, a CompactOperatorUp compactness row, SpectralRadiusUp finite spectral windows, a FredholmAlternativeUp kernel or range-obstruction handoff, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:riesz-schauder-theory-carrier}, \autoref{thm:riesz-schauder-theory-namecert-obligations}, and \autoref{thm:riesz-schauder-theory-compact-fredholm-route} seed the compact-operator spectral route over BanachUp, BoundedLinearOperatorUp, CompactOperatorUp, SpectralRadiusUp, FredholmAlternativeUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a host Riesz--Schauder theorem, completed spectrum, quotient operator algebra, eigenbasis selection, algebraic multiplicity, closed-range complement, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint admission, bounded-action admission, compactness admission, spectral-window exposure, Fredholm-handoff exposure, transport, replay, provenance, and local naming for BEDC.Derived.RieszSchauderTheoryUp.}
\end{closurestatus}
