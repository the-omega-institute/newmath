\chapter{A Concrete Naming Certificate for $\CantorThreadRegularCauchyUp$}
\label{ch:concrete-instances-cantor-thread-regular-cauchy-namecert}
\origin{human}

Following Bishop and Bridges, the $\CantorThreadRegularCauchyUp$ packet records nested dyadic interval threads as finite regular-Cauchy readback data. It sits after the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-facing finite-window boundary of \autoref{ch:concrete-instances-metriccompletionfinite-namecert}. The packet is a displayed thread-to-real seal route, not a quotient completion, selected representative, trichotomy branch, or ambient completeness principle.

\begin{definition}[Cantor thread regular-Cauchy carrier]
\label{def:cantor-thread-regular-cauchy-carrier}
A $\CantorThreadRegularCauchyUp$ carrier is a finite $\BHist$ packet
$$
T=(I,W,D,R,E,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the nested dyadic interval thread row, storing a finite initial segment of dyadic left and right endpoints.
\item $W$ is the $\StreamNameUp$ window row that records the displayed observations of the thread.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger proving that each displayed interval width is bounded by the requested dyadic precision.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the interval midpoints or tagged endpoints over the same windows.
\item $E$ is the terminal $\RealUp$ seal row available only after $I,W,D,R$ have been replayed.
\item $F$ is the $\MetricCompletionFiniteUp$ handoff row for completion-facing consumers of the finite thread.
\item $H$ records componentwise $\hsame$ transport for the interval thread, window, tolerance, readback, seal, completion handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite precision request through $I,W,D,R,E,F,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CantorThreadRegularCauchyUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\MetricCompletionFiniteUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorThreadRegularCauchyUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for a selected limit point, quotient stream equality, countable-choice schedule, host real equality, open-cover compactness, or ambient completion theorem.
\end{definition}

\begin{theorem}[Cantor thread regular-Cauchy real seal]
\label{thm:cantor-thread-regular-cauchy-real-seal}
For every accepted $\CantorThreadRegularCauchyUp$ carrier
$$
T=(I,W,D,R,E,F,H,C,P,N),
$$
any $\RealUp$ or completion-facing consumer must first expose the nested dyadic interval thread $I$, the $\StreamNameUp$ window row $W$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the $\RegSeqRatUp$ readback row $R$, and the $\MetricCompletionFiniteUp$ handoff row $F$ before the terminal seal $E$ is public. The route exports only finite regular-Cauchy readback data.

No quotient completion, selected representative, trichotomy branch, countable-choice schedule, open-cover compactness row, or ambient $\RealUp$ completeness principle can replace the displayed thread route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cantor-thread-regular-cauchy-carrier}. The rows $I,W,D,R,E,F,H,C,P,N$ are the whole accepted packet, and the terminal Real seal appears only after the thread, window, dyadic tolerance, regular readback, and finite-completion rows.

Run finite induction on the displayed interval windows. The base window exposes one dyadic interval row, and the tolerance ledger $D$ records its width at the requested precision. In the induction step, append one displayed nested interval; the new row refines the previous one through the same $W$ and $D$ rows, so the carrier is not replaced.

Read the regular-Cauchy data through $R$. The induction supplies a compatible finite chain of dyadic windows, and the $\RegSeqRatUp$ row repacks that chain as the readback consumed by $E$ and $F$.

Finally use the inventory refusal. Since the carrier contains no quotient stream, selected representative, trichotomy branch, choice schedule, open-cover row, or ambient completion coordinate, every Real or completion consumer must use the displayed finite thread route.
\end{proof}

\falsifiablePrediction{An accepted $\CantorThreadRegularCauchyUp$ consumer that reaches a $\RealUp$ seal without displaying the nested dyadic thread, StreamName window, DyadicRatCore tolerance ledger, RegSeqRat readback, and MetricCompletionFinite handoff refutes the carrier surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, metriccompletionfinite: none is bijective to $\CantorThreadRegularCauchyUp$ because only this packet binds nested dyadic interval threads, finite windows, regular-Cauchy readback, Real sealing, completion handoff, transport, replay, provenance, and local naming as one NameCert route.}

\closureat{CantorThreadRegularCauchyUp}{seedStr}

\begin{closurestatus}{\CantorThreadRegularCauchyUp}
  \constructivestory{A $\CantorThreadRegularCauchyUp$ packet is generated from nested dyadic interval rows, StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, a RealUp seal, a MetricCompletionFinite handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-thread-regular-cauchy-carrier} and \autoref{thm:cantor-thread-regular-cauchy-real-seal} seed the finite nested-thread route from dyadic intervals to regular-Cauchy Real seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected representatives, countable choice, trichotomy, open-cover compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked nested-thread admission, dyadic width dominance, StreamName window stability, RegSeqRat readback, Real seal, MetricCompletionFinite handoff, transport, replay, provenance, and local naming for BEDC.Derived.CantorThreadRegularCauchyUp.}
\end{closurestatus}
