\chapter{A Concrete Naming Certificate for $\HardySpaceUp$}
\label{ch:concrete-instances-hardy-space-namecert}
\origin{human}

Following Hardy on boundary values of analytic functions and the classical Hardy-space tradition, the $\HardySpaceUp$ packet records the finite boundary-read surface needed by constructive real-name analysis. It sits after the Hardy inequality route of \autoref{ch:concrete-instances-hardy-inequality-namecert}, the real-series summation surface of \autoref{ch:concrete-instances-realseries-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names Hardy-space boundary analysis as finite L10 rows; it does not import host holomorphic function spaces, almost-everywhere boundary representatives, measure-class quotients, Banach-space completeness, or nonconstructive maximal theorems.

\begin{definition}[Hardy space carrier]
\label{def:hardy-space-carrier}
A $\HardySpaceUp$ carrier is a finite $\BHist$ packet
$$
H=(C,B,A,S,R,D,E,T,J,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the coefficient row naming the finite analytic polynomial or power-series prefix under inspection.
\item $B$ is the boundary-window row. It records the finitely many radial or angular boundary probes opened by the packet.
\item $A$ is the $\HardyInequalityUp$ control row used to compare displayed averages with the coefficient prefix.
\item $S$ is the $\RealSeriesUp$ summation row attached to the inspected coefficient and boundary data.
\item $R$ is the $\RegSeqRatUp$ readback row for the displayed boundary values and partial averages.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by every boundary-value comparison.
\item $E$ is the $\RealUp$ seal row available only after $C,B,A,S,R,D$ have been displayed.
\item $T$ records componentwise $\hsame$ transport for $C,B,A,S,R,D,E$.
\item $J$ records displayed $\Cont$ replay from a boundary probe through coefficient prefix, Hardy control, real-series summation, rational readback, dyadic tolerance, and real seal.
\item $K$ is the $\Pkg$ provenance over $\HardySpaceUp$, $\HardyInequalityUp$, $\RealSeriesUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $P$ records the finite boundary predicate saying that each exported boundary read uses only the displayed prefix, window, tolerance, and readback rows.
\item $N$ is the local $\NameCert_{\HardySpaceUp}$ row.
\end{enumerate}
The carrier contains no coordinate for a host Hardy space, an almost-everywhere boundary representative, a measure quotient, a selected nontangential limit, Banach completeness, or countable choice.
\end{definition}

\begin{theorem}[Hardy space NameCert obligations]
\label{thm:hardy-space-namecert-obligations}
For every accepted $\HardySpaceUp$ carrier $H=(C,B,A,S,R,D,E,T,J,K,P,N)$, the local $\NameCert_{\HardySpaceUp}$ surface consists exactly of carrier admission, coefficient-prefix exposure through $C$, finite boundary-window exposure through $B$, Hardy-control exposure through $A$, real-series exposure through $S$, regular rational readback through $R$, dyadic tolerance exposure through $D$, terminal real sealing through $E$, componentwise transport through $T$, displayed replay through $J$, package provenance through $K$, boundary-predicate exposure through $P$, and local naming through $N$.

These obligations bind the finite Hardy-space route
$$
\HardyInequalityUp \longmapsto \RealSeriesUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp
$$
without adding a host analytic function space, quotient boundary class, selected limit, completeness theorem, or nonconstructive maximal principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hardy-space-carrier}. The coefficient row and boundary-window row expose only finite analytic data and finitely many boundary probes.

The Hardy control and real-series rows are read next. Row $A$ supplies the Hardy inequality packet used for finite average control, while $S$ records the displayed real-series summation surface attached to the same prefix.

Readback and tolerance then pass through $R$ and $D$. The regular rational row supplies the finite approximants for the boundary probes, and the dyadic ledger supplies the tolerance data used by each comparison.

Finally $E,T,J,K,P,N$ seal, transport, replay, source, bound, and name the same route. Since no excluded analytic or quotient object appears among the carrier coordinates, the local NameCert surface exports exactly the listed finite obligations.
\end{proof}

\begin{theorem}[Hardy space real-boundary route]
\label{thm:hardy-space-real-boundary}
Let $H=(C,B,A,S,R,D,E,T,J,K,P,N)$ be an accepted $\HardySpaceUp$ packet. Every Real-facing boundary-value consumer of $H$ factors through a displayed coefficient prefix $C$, a finite boundary window $B$, the Hardy-control row $A$, the real-series row $S$, the regular rational readback $R$, and the dyadic tolerance ledger $D$ before the $\RealUp$ seal $E$ is available.

Thus the packet records constructive boundary-value analysis as finite NameCert data over existing L10 real-name rows. It does not assert host holomorphy, almost-everywhere boundary equivalence, selected radial limits, quotient representative equality, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the finite boundary window $B$. A boundary-value request is accepted only after it names the finite probes being inspected, so no consumer can read an unbounded boundary trace from the packet.

The coefficient prefix $C$, Hardy-control row $A$, and real-series row $S$ provide the finite analytic data for those probes. Their order prevents a Real-facing consumer from replacing the displayed prefix by a host Hardy-space element.

Rows $R$ and $D$ then expose the rational readback and dyadic tolerance ledger. The terminal seal $E$ is downstream from these rows, while $T,J,K,P,N$ only transport, replay, source, bound, and name the same finite route.

Therefore every Real-facing boundary read factors through the displayed L10 rows, and the excluded analytic, quotient, choice, and completeness principles remain outside the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\HardySpaceUp$ read exposes a coefficient prefix, finite boundary window, HardyInequalityUp control row, RealSeriesUp summation row, RegSeqRatUp readback, DyadicRatCoreUp tolerance ledger, RealUp seal, transport, replay, provenance, boundary predicate, and local NameCert before any boundary-value claim is exported.}

\independenceWitness{hardyinequality, realseries, streamname, regseqrat, dyadicratcore, real: $\HardySpaceUp$ is not bijective to any listed sibling because it jointly carries coefficient-prefix, finite boundary-window, Hardy-control, summation, readback, tolerance, and Real-seal rows for boundary-value analysis.}

\closureat{HardySpaceUp}{seedStr}

\begin{closurestatus}{\HardySpaceUp}
  \constructivestory{A $\HardySpaceUp$ packet is generated from a coefficient prefix, finite boundary window, Hardy control, RealSeries summation, RegSeqRat readback, DyadicRatCore tolerance, RealUp seal, componentwise transport, Cont replay, Pkg provenance, boundary predicate, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hardy-space-carrier}, \autoref{thm:hardy-space-namecert-obligations}, and \autoref{thm:hardy-space-real-boundary} seed the finite Hardy-space boundary route over displayed real-name rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host Hardy-space theory, almost-everywhere boundary representatives, measure quotients, selected radial limits, Banach completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier exposure, boundary-window exposure, Hardy-control handoff, real-series handoff, readback and tolerance nonescape, Real seal handoff, and transport stability for BEDC.Derived.HardySpaceUp.}
\end{closurestatus}
