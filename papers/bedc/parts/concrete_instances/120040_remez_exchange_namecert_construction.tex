\chapter{A Concrete Naming Certificate for $\RemezExchangeUp$}
\label{ch:concrete-instances-remez-exchange-namecert}
\origin{human}

Following Cheney, Approximation Theory, the $\RemezExchangeUp$ packet records the finite exchange step used by constructive minimax approximation on a displayed real interval. It consumes the density route of \autoref{ch:concrete-instances-weierstrass-approximation-namecert}, the alternation surface of \autoref{ch:concrete-instances-chebyshev-equioscillation-namecert}, the denominator-bounded comparison boundary of \autoref{ch:concrete-instances-rational-best-approximation-namecert}, the compact interval source of \autoref{ch:concrete-instances-constructive-real-closed-interval-namecert}, the finite interval-uniform-continuity readback of \autoref{ch:concrete-instances-real-interval-uniform-continuity-namecert}, and the coefficient carrier of \autoref{ch:concrete-instances-polynomial-namecert}. The packet names one displayed exchange move; it is not a host optimizer, an ambient supremum operator, a selected dense enumeration, or a completed quotient function space.

\begin{definition}[Remez exchange carrier]
\label{def:remez-exchange-carrier}
A $\RemezExchangeUp$ carrier is a finite $\BHist$ packet
$$
X=(I,S,P,E,\Sigma,A,B,G,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the compact interval sample row, read through $\ConstructiveRealClosedIntervalUp$ with endpoint containment and interval seals already displayed.
\item $S$ is the ordered sample table, listing the finitely many inspected points and the local mesh cells through which each point is read.
\item $P$ is the $\PolynomialUp$ approximant row, including the coefficient window and degree budget used by the exchange step.
\item $E$ is the residual-value row for the inspected target-minus-approximant values on $S$.
\item $\Sigma$ is the residual-sign row, recording the alternating sign pattern carried by the current sample.
\item $A$ is the alternation-window row, imported from the $\ChebyshevEquioscillationUp$ surface when the displayed signs and magnitudes form a minimax window.
\item $B$ is the dyadic sup-norm budget row, giving the finite upper and comparison tolerances used to judge the exchange.
\item $G$ is the exchange-step row, replacing one displayed sample cell by a stronger residual cell while preserving the ordered interval route.
\item $H$ records componentwise $\hsame$ transport for the interval, sample, polynomial, residual, sign, alternation, budget, exchange, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a minimax request through $I,S,P,E,\Sigma,A,B,G,H,Q,N$.
\item $Q$ is the $\Pkg$ provenance over $\RemezExchangeUp$, $\WeierstrassApproximationUp$, $\ChebyshevEquioscillationUp$, $\RationalBestApproximationUp$, $\ConstructiveRealClosedIntervalUp$, $\RealIntervalUniformContinuityUp$, $\PolynomialUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RemezExchangeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host supremum, a selected global optimizer, an unbounded sample search, quotient real-function equality, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Remez exchange NameCert obligations]
\label{thm:remez-exchange-namecert-obligations}
For every accepted $\RemezExchangeUp$ carrier
$$
X=(I,S,P,E,\Sigma,A,B,G,H,C,Q,N),
$$
the local $\NameCert_{\RemezExchangeUp}$ surface consists exactly of compact interval sample admission, polynomial approximant admission, residual-value readback, residual-sign admission, alternation-window compatibility, dyadic sup-norm budget exposure, exchange-step exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:remez-exchange-carrier}. The displayed tuple exposes the interval, sample, polynomial, residual, sign, alternation, budget, exchange, transport, replay, provenance, and local naming rows.

The approximation rows then share one finite source route. The interval row $I$ supplies the only endpoint and containment data, the sample row $S$ lists the inspected cells, and the polynomial row $P$ supplies the only approximant admitted by the packet.

The residual and alternation obligations are finite. The rows $E$ and $\Sigma$ attach residual magnitudes and signs to the inspected sample cells, while $A$ reads the equioscillation window through the sibling alternation certificate. The dyadic budget $B$ is the only sup-norm-facing tolerance available to the exchange.

Finally the exchange row $G$ records the displayed replacement cell. The structural rows $H,C,Q,N$ transport, replay, source, and name the same route, so no host optimizer, ambient supremum, unbounded search, quotient equality, choice principle, or completeness coordinate can enter the local certificate.
\end{proof}

\begin{theorem}[Remez exchange equioscillation window]
\label{thm:remez-exchange-equioscillation-window}
Every alternation read of an accepted $\RemezExchangeUp$ carrier factors through the finite route
$$
I,S,P,E,\Sigma,B \longmapsto A \longmapsto G,H,C,Q,N .
$$
Thus the equioscillation window consumed by the exchange is witnessed by the displayed sample order, residual signs, dyadic sup-norm budget, and sibling $\ChebyshevEquioscillationUp$ row, not by an ambient compactness theorem or an uninspected function-space norm.
\end{theorem}
\begin{proof}
Begin with the carrier inventory. The only data available before the alternation row are the interval row $I$, the ordered sample table $S$, the approximant row $P$, the residual row $E$, the sign row $\Sigma$, and the dyadic budget $B$.

Apply \autoref{thm:remez-exchange-namecert-obligations}. It forces the alternation-window row $A$ to read the same interval, sample, polynomial, residual, sign, and dyadic-budget rows that were admitted by the carrier.

Consequently an exchange consumer can inspect the equioscillation surface only after those rows are fixed. The exchange, transport, replay, provenance, and local name rows then package the same finite route, with no additional compactness or host norm coordinate available.
\end{proof}

\begin{theorem}[Remez exchange finite improvement]
\label{thm:remez-exchange-finite-improvement}
Every finite improvement claim exported by an accepted $\RemezExchangeUp$ packet is read from the displayed exchange row $G$ together with the dyadic budget $B$ and the alternation-window row $A$. The handoff may state that the displayed replacement cell improves the inspected residual budget on the named sample route, but it exports no global minimax optimizer, no selected limiting subsequence, no quotient function-space equality, and no ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Use \autoref{thm:remez-exchange-equioscillation-window}. It places the interval, sample, polynomial, residual, sign, and dyadic-budget rows before the alternation window is available.

The exchange row $G$ can therefore compare only the displayed replacement cell against that same finite window. Its improvement content is a budget comparison over $B$ and $A$, not a statement about all possible sample sets or a completed function space.

The provenance row records the relationship with the Weierstrass, rational-best, interval-uniform-continuity, and polynomial chapters. Those siblings supply density, comparison, interval readback, and coefficient data, but none of them adds a carrier coordinate for a host optimizer or selected limiting process inside $\RemezExchangeUp$.
\end{proof}

\falsifiablePrediction{Every accepted $\RemezExchangeUp$ read that claims an exchange improvement must display the compact interval sample row, ordered sample table, polynomial approximant row, residual-value row, residual-sign row, alternation-window row, dyadic sup-norm budget, exchange-step row, hsame transport, Cont replay, Pkg provenance, and local NameCert before the improvement conclusion is inspected.}

\independenceWitness{weierstrass-approximation, chebyshev-equioscillation, rational-best-approximation, constructive-real-closed-interval, real-interval-uniform-continuity, polynomial: $\RemezExchangeUp$ is not bijective to any listed sibling because it jointly carries a finite exchange-step row, a residual-sign row, an alternation-window row, and a dyadic sup-norm budget over one displayed compact interval sample.}

\closureat{RemezExchangeUp}{seedStr}

\begin{closurestatus}{\RemezExchangeUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:remez-exchange-carrier}, \autoref{thm:remez-exchange-namecert-obligations}, \autoref{thm:remez-exchange-equioscillation-window}, and \autoref{thm:remez-exchange-finite-improvement} seed the finite Remez exchange carrier, local obligation surface, alternation-window route, and inspected improvement handoff for constructive minimax approximation.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global minimax existence, host suprema, selected optimizers, limiting exchange convergence, quotient function spaces, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sample-window compatibility, polynomial approximant admission, residual readback, alternation-window compatibility, dyadic budget soundness, exchange-step exactness, transport, replay, provenance, and local naming for BEDC.Derived.RemezExchangeUp.}
  \constructivestory{A $\RemezExchangeUp$ packet is generated from compact interval sample rows, an ordered sample table, a displayed polynomial approximant row, residual values, residual signs, an alternation-window row, a dyadic sup-norm budget, one exchange-step row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
