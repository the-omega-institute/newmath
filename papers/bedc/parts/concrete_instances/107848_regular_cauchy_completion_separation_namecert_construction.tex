\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionSeparationUp$}
\label{ch:concrete-instances-regular-cauchy-completion-separation-namecert}
\origin{human}

Following Bishop and Bridges on constructive real numbers by regular Cauchy sequences, the $\RegularCauchyCompletionSeparationUp$ packet records the quotient-free separation certificate used when two completion names are compared before a terminal real seal is consumed. It sits on the finite observation route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the extension-and-uniqueness surface of \autoref{ch:concrete-instances-cauchy-completion-universal-property-namecert}. The packet names separation between displayed completion names; it is not quotient completion equality, selected representative choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy completion separation carrier]
\label{def:regular-cauchy-completion-separation-carrier}
A $\RegularCauchyCompletionSeparationUp$ carrier is a finite $\BHist$ packet
$$
S=(A,B,D,W,R,E,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two completion-name source rows, each opened only through the $\RegSeqRatUp$ regular rational route.
\item $D$ is the shared $\DyadicRatCoreUp$ tolerance ledger used to compare the two names at a displayed precision.
\item $W$ is the finite $\StreamNameUp$ window exposing the observations of $A$ and $B$ requested by $D$.
\item $R$ is the regular-rational comparison row recording the inspected rational entries, common-tail data, and separation witness before any Real-facing read.
\item $E$ is the terminal $\RealUp$ seal row reached only after $A,B,D,W,R$ have been displayed.
\item $U$ is the $\CauchyCompletionUniversalPropertyUp$ uniqueness row, read only as a finite extension-and-uniqueness boundary for the same displayed completion names.
\item $H$ records componentwise $\hsame$ transport for $A,B,D,W,R,E,U,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a separation request through the two source rows, dyadic tolerance, stream window, regular rational comparison, terminal seal, and universal-property uniqueness row.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionSeparationUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchyCompletionUniversalPropertyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionSeparationUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient representatives, host sequence equality, selected limits, countable choice over tails, trichotomy, or an ambient completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Regular Cauchy completion separation NameCert obligations]
\label{thm:regular-cauchy-completion-separation-namecert-obligations}
For every accepted $\RegularCauchyCompletionSeparationUp$ carrier $S=(A,B,D,W,R,E,U,H,C,P,N)$, the local $\NameCert_{\RegularCauchyCompletionSeparationUp}$ surface consists exactly of two-name source admission, shared dyadic tolerance exposure, finite StreamName window routing, regular rational comparison, terminal Real seal handoff, universal-property uniqueness routing, componentwise transport, continuation replay, package provenance, and local naming through the displayed rows $A,B,D,W,R,E,U,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-completion-separation-carrier}. The two completion names are visible only through $A$ and $B$, and the comparison has a single displayed dyadic tolerance ledger $D$.

The finite window row $W$ exposes the observations requested by $D$. The row $R$ records the inspected regular-rational entries, common-tail data, and separation witness before the terminal $\RealUp$ seal $E$ can be read.

The universal-property row $U$ supplies only the displayed uniqueness boundary already named by \autoref{ch:concrete-instances-cauchy-completion-universal-property-namecert}. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same finite route, and the carrier lacks any quotient representative, host equality, selected limit, countable-choice, trichotomy, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Regular Cauchy completion separation handoff]
\label{thm:regular-cauchy-completion-separation-handoff}
Every completion-facing consumer that compares two accepted $\RegularCauchyCompletionSeparationUp$ names must read the finite route
$$
A,B,D,W \longmapsto R \longmapsto E,U \longmapsto H,C,P,N
$$
before treating the names as separated or identified by the completion boundary. The handoff routes through $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, and $\CauchyCompletionUniversalPropertyUp$ as displayed NameCert data, and it exports no quotient equality, selected representative, host real equality, countable-choice selector, trichotomy split, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with an accepted carrier $S=(A,B,D,W,R,E,U,H,C,P,N)$. A comparison request first opens the two regular Cauchy source rows and the shared dyadic tolerance ledger.

The finite StreamName window $W$ determines which observations of the two names are inspected. The regular-rational comparison row $R$ then records the common-tail and separation data, so the request remains inside finite $\RegSeqRatUp$ and $\DyadicRatCoreUp$ evidence.

Only after that comparison can the terminal Real seal $E$ and universal-property uniqueness row $U$ be consumed. The structural rows $H,C,P,N$ transport, replay, source, and name the same route, leaving no carrier coordinate for quotient equality, selected representatives, host equality, countable choice, trichotomy, or ambient completeness.
\end{proof}

\closureat{RegularCauchyCompletionSeparationUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionSeparationUp}
  \constructivestory{A $\RegularCauchyCompletionSeparationUp$ packet is generated from two regular Cauchy completion-name rows, a shared DyadicRatCore tolerance ledger, finite StreamName windows, a RegSeqRat comparison row, a terminal RealUp seal, a CauchyCompletionUniversalProperty uniqueness row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-separation-carrier}, \autoref{thm:regular-cauchy-completion-separation-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-separation-handoff} seed the quotient-free separation surface for displayed regular Cauchy completion names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected representatives, host sequence equality, countable choice over tails, trichotomy, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-tolerance exactness, finite-window routing, regular-rational comparison, terminal seal handoff, universal-property uniqueness routing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyCompletionSeparationUp.}
\end{closurestatus}
