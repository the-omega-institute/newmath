\chapter{A Concrete Naming Certificate for $\RealZeroUp$}
\label{ch:concrete-instances-realzero-namecert}
\origin{human}

Following Bishop and Bridges, the $\RealZeroUp$ packet records the terminal zero real not as a host real equality claim but as the finite zero-source route already displayed by \autoref{ch:concrete-instances-stationary-rational-cauchy-namecert}, \autoref{ch:concrete-instances-regularcauchyzero-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The chapter packages that route as the public zero-real seal consumed by downstream positive-real, absolute-value, order, metric, and completion-facing certificates.

\begin{definition}[RealZero carrier]
\label{def:real-zero-carrier}
A $\RealZeroUp$ carrier is a finite $\BHist$ packet
$$
Z=(q,S,Z_0,D,R,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $q$ is the $\RatUp$ zero seed row.
\item $S$ is the constant $\StreamNameUp$ window whose every admitted finite request reads $q$.
\item $Z_0$ is the $\RegularCauchyZeroUp$ route, including the stationary $\RegSeqRatUp$ zero row and the zero regular-Cauchy source handoff.
\item $D$ is the $\DyadicRatCoreUp$ zero ledger recording the endpoint, tolerance, and radius data used by the zero-source route.
\item $R$ is the terminal $\RealUp$ seal reached after the constant window, regular-rational readback, and dyadic zero ledger have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the rational seed, constant window, regular-Cauchy zero route, dyadic ledger, terminal seal, provenance, and local name.
\item $C$ stores displayed $\Cont$ replay routes from $q$ through $S$, from $S$ through $Z_0$ and $D$, and from those rows into the terminal seal $R$.
\item $P$ records $\Pkg$ provenance over $\RealZeroUp$, $\RatUp$, $\StreamNameUp$, $\StationaryRationalCauchyUp$, $\RegularCauchyZeroUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealZeroUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient stream equality, host zero equality, selected limits, trichotomy, or ambient completeness.
\end{definition}
\leandef{BEDC.Derived.RealZeroUp.RealZeroCarrier}

\begin{theorem}[RealZero NameCert obligations]
\label{thm:real-zero-namecert-obligations}
For every accepted $\RealZeroUp$ carrier $Z=(q,S,Z_0,D,R,H,C,P,N)$, the local $\NameCert_{\RealZeroUp}$ surface consists exactly of the following five rows:
\begin{enumerate}
\item carrier admission for the rational zero seed, constant StreamName window, RegularCauchyZero route, DyadicRatCore zero ledger, terminal RealUp seal, transport, replay, provenance, and local naming rows;
\item stationary zero-source route, requiring the rational seed to be read only through the constant window, the stationary rational Cauchy row, and the RegularCauchyZero handoff;
\item terminal RealUp seal handoff, requiring the zero real-facing row $R$ to be exported only after the StreamName, RegSeqRat, DyadicRatCore, and RegularCauchyZero rows have been displayed;
\item componentwise transport and replay stability, requiring accepted zero-real packets to move through $H$ and replay through $C$ without changing the displayed zero-source chain;
\item ledger nonescape from quotient streams, host zero equality, selected limits, trichotomy, and ambient completeness.
\end{enumerate}
No NameCert obligation is stored outside these five finite rows.
\end{theorem}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from \autoref{def:real-zero-carrier}. The only visible data are the zero seed $q$, the constant window $S$, the regular-Cauchy zero route $Z_0$, the dyadic zero ledger $D$, the terminal seal $R$, and the structural rows $H,C,P,N$.

The stationary source row is forced by the sibling route. The packet reads $q$ through the constant $\StreamNameUp$ window, the $\StationaryRationalCauchyUp$ surface, and the $\RegularCauchyZeroUp$ route before any regular-rational or real-facing consumer can inspect the zero source.

The terminal seal is then read through $R$. Transport and replay are componentwise because $H$ and $C$ are the only rows that move or re-open the chain.

The carrier has no coordinate for quotient stream equality, host zero equality, selected limits, trichotomy, or ambient completeness. Thus the ledger cannot export any of those principles through the local $\NameCert_{\RealZeroUp}$ row.
\end{proof}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroNameCertObligations}

\begin{theorem}[RealZero terminal seal handoff]
\label{thm:real-zero-terminal-seal-handoff}
Every accepted $\RealZeroUp$ consumer must read the zero seed through the constant $\StreamNameUp$ window, the $\RegSeqRatUp$ zero row, the $\DyadicRatCoreUp$ zero ledger, and the terminal $\RealUp$ seal before any downstream $\PositiveRealUp$ or $\RealAbsUp$ consumer may use it. Equivalently, the public zero-real boundary is the finite route
$$
q \longrightarrow S \longrightarrow Z_0 \longrightarrow D \longrightarrow R
$$
together with $H,C,P,N$, and no consumer may replace that route by host zero equality, quotient real equality, selected limits, trichotomy, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_terminal\_seal\_handoff}
\begin{proof}
Start with an accepted carrier $Z=(q,S,Z_0,D,R,H,C,P,N)$. The seed row $q$ is not exported directly; it is first read through the constant window $S$ and the regular-rational zero row supplied by $Z_0$.

Next the dyadic zero ledger $D$ records the finite endpoint and tolerance data attached to that same route. The real-facing seal $R$ is reached only after those rows are visible.

Downstream $\PositiveRealUp$ and $\RealAbsUp$ consumers therefore receive the zero source as the displayed terminal seal handoff, not as a host value. Since the carrier contains no excluded principle, the handoff cannot provide quotient real equality, selected limits, trichotomy, or ambient completeness.
\end{proof}

\begin{theorem}[RealZero stationary source obligation]
\label{thm:real-zero-stationary-source-obligation}
Every accepted $\RealZeroUp$ packet $Z=(q,S,Z_0,D,R,H,C,P,N)$ displays a stationary zero-source obligation: the rational seed $q$ is read through the constant $\StreamNameUp$ window $S$, the $\BHist$ rows of $Z_0$, the componentwise $\hsame$ support in $H$, the $\Cont$ replay support in $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\RealZeroUp}$ row $N$ before any downstream consumer sees the terminal zero-real seal.

This obligation exports no host zero equality, quotient stream equality, selected tail, trichotomy, or ambient completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_stationary\_source\_obligation}
\begin{proof}
Project the carrier coordinates from \autoref{def:real-zero-carrier}. The only source row for the zero value is the rational seed $q$, and the only public route from $q$ to a stream-facing object is the constant $\StreamNameUp$ window $S$ together with the zero route $Z_0$.

Now apply the terminal handoff of \autoref{thm:real-zero-terminal-seal-handoff}. It forces every consumer of the terminal $\RealUp$ seal to read the seed through the displayed StreamName, RegSeqRat, DyadicRatCore, and RealUp rows.

The structural rows $H,C,P,N$ are part of the same carrier. Thus $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row are visible at the stationary source boundary, while excluded host and quotient principles have no coordinate in the packet.
\end{proof}

\begin{theorem}[RealZero constant-window uniqueness]
\label{thm:real-zero-constant-window-uniqueness}
Let $Z=(q,S,Z_0,D,R,H,C,P,N)$ and $Z'=(q',S',Z'_0,D',R',H',C',P',N')$ be accepted $\RealZeroUp$ packets. If the rational zero seed rows are the same displayed $\RatUp$ zero row and the constant $\StreamNameUp$ windows are related componentwise by the displayed $\hsame$ support, then every terminal $\RealUp$ seal read by a downstream consumer follows the same route through $\RegSeqRatUp$, $\DyadicRatCoreUp$, and the local $\NameCert_{\RealZeroUp}$ row.

Thus constant-window agreement determines the terminal zero-real seal route as a finite packet route, not as host zero equality, quotient real equality, selected-limit equality, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the carrier definition, \autoref{def:real-zero-carrier}. The only source row is the rational zero seed $q$, and the only stream-facing source is the constant window $S$.

Use the stationary-source obligation of \autoref{thm:real-zero-stationary-source-obligation}. It forces every downstream consumer to read the zero seed through $S$, the rows of $Z_0$, the structural support $H,C,P,N$, and the local $\NameCert_{\RealZeroUp}$ row before the terminal seal is seen.

Apply the terminal seal handoff of \autoref{thm:real-zero-terminal-seal-handoff}. It fixes the public route through the constant window, the $\RegSeqRatUp$ zero row inside $Z_0$, the $\DyadicRatCoreUp$ zero ledger $D$, and the terminal $\RealUp$ seal $R$.

Finally read the dyadic ledger obligation of \autoref{thm:real-zero-dyadic-ledger-obligation}. Since the two packets share the displayed zero seed and the constant-window rows componentwise, the downstream seal route must pass through the same RegSeqRat, DyadicRatCore, and local NameCert rows. The carrier has no coordinate for host equality, quotient equality, selected limits, trichotomy, or ambient completeness, so the uniqueness is exactly the displayed finite route.
\end{proof}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_constant\_window\_uniqueness}

\begin{theorem}[RealZero dyadic ledger obligation]
\label{thm:real-zero-dyadic-ledger-obligation}
Every accepted $\RealZeroUp$ packet $Z=(q,S,Z_0,D,R,H,C,P,N)$ displays a dyadic zero-ledger obligation: the $\DyadicRatCoreUp$ row $D$ is the unique public tolerance ledger between the stationary source route $Z_0$ and the downstream $\RealUp$ seal $R$, and its $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support is read before any consumer may use the seal.

The ledger is finite and does not introduce quotient real equality, selected limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_dyadic\_ledger\_obligation}
\begin{proof}
Read $D$ from the carrier in \autoref{def:real-zero-carrier}. Its role is fixed there as the zero ledger recording endpoint, tolerance, and radius data for the zero-source route.

Use \autoref{thm:real-zero-terminal-seal-handoff} to locate $D$ in the public route
$$
q \longrightarrow S \longrightarrow Z_0 \longrightarrow D \longrightarrow R .
$$
The downstream seal cannot be inspected before this DyadicRatCore row is displayed.

The same theorem keeps the route inside the finite StreamName, RegSeqRat, DyadicRatCore, and RealUp rows. Therefore the $\BHist$ ledger, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ support are the whole dyadic obligation surface, with no coordinate for the excluded principles.
\end{proof}

\begin{theorem}[RealZero downstream consumer obligation]
\label{thm:real-zero-downstream-consumer-obligation}
Every downstream consumer of an accepted $\RealZeroUp$ packet must consume the terminal $\RealUp$ seal $R$ only after the constant $\StreamNameUp$ window, the $\RegSeqRatUp$ zero readback row, the $\DyadicRatCoreUp$ zero ledger, the $\BHist$ display rows, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RealZeroUp}$ row have been displayed.

Consequently, downstream certificates inherit a finite zero-real seal handoff and not a host real value, quotient real equality, selected limit, trichotomy, or ambient completeness premise.
\end{theorem}
\begin{proof}
Start with a downstream consumer and unfold the accepted packet by \autoref{def:real-zero-carrier}. The consumer can see the seal only through the carrier coordinate $R$ and the visible support rows $H,C,P,N$.

Apply \autoref{thm:real-zero-terminal-seal-handoff}. It requires the route through $S$, the RegSeqRat zero row inside $Z_0$, the DyadicRatCore ledger $D$, and the terminal RealUp seal $R$ before any downstream PositiveReal, RealAbs, order, metric, or completion-facing certificate may use the zero-real boundary.

Because the carrier has no coordinate for host equality, quotient equality, selected limits, trichotomy, or ambient completeness, those objects cannot be inherited by the downstream consumer. What is inherited is exactly the displayed finite handoff through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.
\end{proof}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_downstream\_consumer\_obligation}

\begin{theorem}[RealZero positive-real and absolute-value handoff]
\label{thm:real-zero-positive-abs-handoff}
Every $\PositiveRealUp$ or $\RealAbsUp$ consumer of an accepted $\RealZeroUp$ packet may use the zero seal only through the displayed finite route
$$
\StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
Concretely, the consumer must first read the constant $\StreamNameUp$ window, the $\RegSeqRatUp$ zero readback supplied by the regular-Cauchy zero route, the $\DyadicRatCoreUp$ zero ledger, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RealZeroUp}$ row.

The handoff does not export host zero equality, quotient real equality, selected representatives, trichotomy, or ambient $\RealUp$ completeness to either downstream chapter.
\end{theorem}
\begin{proof}
Start with the terminal seal handoff of \autoref{thm:real-zero-terminal-seal-handoff}. It forces every zero-real read through the constant StreamName window, the RegSeqRat zero row inside the regular-Cauchy zero route, the DyadicRatCore zero ledger, and the terminal RealUp seal.

The stationary source obligation of \autoref{thm:real-zero-stationary-source-obligation} identifies the source side of that route. The rational zero seed is visible only through the constant window, the regular-rational zero readback, componentwise transport, continuation replay, package provenance, and the local NameCert row.

The dyadic ledger obligation of \autoref{thm:real-zero-dyadic-ledger-obligation} supplies the tolerance-facing row consumed by order, metric, positive-real, and absolute-value chapters. It places the DyadicRatCore row between the stationary source route and the terminal RealUp seal, with no extra coordinate for quotient or selected-representative data.

Finally apply the downstream consumer obligation of \autoref{thm:real-zero-downstream-consumer-obligation}. It states that downstream consumers inherit exactly the displayed finite handoff through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. Thus $\PositiveRealUp$ and $\RealAbsUp$ may consume the zero seal only through the listed route.
\end{proof}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_positive\_abs\_handoff}

\begin{theorem}[RealZero zero-distance seal]
\label{thm:realzero-zero-distance-seal}
Every $\MetricSpaceUp$, $\QuasiMetricUp$, $\CompletionUp$, $\UniformCompletionUp$, or $\CompleteMetricUp$ consumer using the zero real as a zero-distance or zero-radius boundary must first read the constant $\StreamNameUp$ window, the $\RegSeqRatUp$ zero row, the $\DyadicRatCoreUp$ zero ledger, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RealZeroUp}$ row of the accepted $\RealZeroUp$ packet.

The zero-distance seal is therefore the displayed finite route of the packet. It is not host zero equality, quotient real equality, selected-limit equality, trichotomy, or ambient completion.
\end{theorem}
\begin{proof}
First apply the terminal seal handoff, \autoref{thm:real-zero-terminal-seal-handoff}. It places the constant window, RegSeqRat zero row, DyadicRatCore zero ledger, and terminal RealUp seal upstream of any downstream read.

Next apply the dyadic ledger obligation, \autoref{thm:real-zero-dyadic-ledger-obligation}. The metric-facing zero-radius boundary must pass through the displayed tolerance ledger $D$ rather than through a host equality or selected representative.

Then apply the downstream consumer obligation, \autoref{thm:real-zero-downstream-consumer-obligation}. It covers the MetricSpace, QuasiMetric, Completion, UniformCompletion, and CompleteMetric consumers by requiring the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support before the zero seal is used.

Finally use componentwise transport, \autoref{thm:real-zero-componentwise-transport}. Transport preserves the finite zero-source route component by component, so the seal remains a BEDC packet boundary and does not become quotient real equality, trichotomy, selected-limit equality, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_zero\_distance\_seal}

\begin{theorem}[RealZero componentwise transport]
\label{thm:real-zero-componentwise-transport}
Let $Z=(q,S,Z_0,D,R,H,C,P,N)$ and $Z'=(q',S',Z'_0,D',R',H',C',P',N')$ be accepted $\RealZeroUp$ packets. If the displayed $\hsame$ rows in $H$ identify the rational seed, constant $\StreamNameUp$ window, $\RegSeqRatUp$ zero readback inside $Z_0$, $\DyadicRatCoreUp$ zero ledger, terminal $\RealUp$ seal, $\BHist$ support, replay, provenance, and local naming rows component by component, then every downstream read of $Z'$ follows the same zero-source route as a read of $Z$.

The transported route remains a finite BEDC packet route; it does not become host real equality, quotient stream equality, selected-limit equality, trichotomy, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroComponentwiseTransport}
\begin{proof}
Project the carrier rows with \autoref{def:real-zero-carrier}. The row $H$ is the only coordinate that moves accepted zero-real packets, and it is specified componentwise for the seed, window, regular-Cauchy zero route, dyadic ledger, terminal seal, provenance, and local name.

Apply the terminal seal handoff of \autoref{thm:real-zero-terminal-seal-handoff}. It fixes the order
$$
q \longrightarrow S \longrightarrow Z_0 \longrightarrow D \longrightarrow R
$$
before any downstream consumer can inspect the zero-real boundary.

The stationary source obligation of \autoref{thm:real-zero-stationary-source-obligation} and the dyadic ledger obligation of \autoref{thm:real-zero-dyadic-ledger-obligation} keep the transported seed and tolerance rows inside the same $\BHist$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ route. Since the carrier has no excluded coordinate, componentwise transport preserves the displayed packet without adding host or quotient principles.
\end{proof}

\begin{theorem}[RealZero replay stability]
\label{thm:real-zero-replay-stability}
Every accepted $\RealZeroUp$ replay through the $\Cont$ row $C$ repeats the same stationary zero-source route through the constant $\StreamNameUp$ window, the $\RegSeqRatUp$ zero readback supplied by $Z_0$, the $\DyadicRatCoreUp$ zero ledger $D$, and the terminal $\RealUp$ seal $R$. Replaying the packet cannot expose a shorter path to the zero-real seal and cannot introduce quotient streams, host zero equality, selected limits, trichotomy, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_replay\_stability}
\begin{proof}
Read $C$ from \autoref{def:real-zero-carrier}. It records displayed $\Cont$ replay from $q$ through $S$, from $S$ through $Z_0$ and $D$, and from those rows into the terminal seal $R$.

Use \autoref{thm:real-zero-namecert-obligations} to identify replay stability as part of the local NameCert surface. The obligations do not permit a replay row outside $C$, and the terminal seal handoff forces every replayed read through the same finite route.

Finally apply \autoref{thm:real-zero-downstream-consumer-obligation}. Downstream consumers inherit the displayed zero-real seal handoff through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support, so replay preserves the packet route rather than supplying an external equality or completeness principle.
\end{proof}

\begin{theorem}[RealZero ledger nonescape]
\label{thm:real-zero-ledger-nonescape}
The $\RealZeroUp$ ledger rows in an accepted packet $Z=(q,S,Z_0,D,R,H,C,P,N)$ are exhausted by the stationary source, constant $\StreamNameUp$ window, $\RegSeqRatUp$ zero readback, $\DyadicRatCoreUp$ zero ledger, terminal $\RealUp$ seal, $\BHist$ display, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\RealZeroUp}$ row. No ledger read can escape from these rows to quotient stream equality, host zero equality, selected limits, trichotomy, countable choice, or ambient Real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealZeroUp.RealZeroCarrier\_ledger\_nonescape}
\begin{proof}
Carrier admission in \autoref{def:real-zero-carrier} lists every ledger-bearing row. The finite packet contains $q,S,Z_0,D,R,H,C,P,N$ and no additional coordinate.

The dyadic ledger obligation of \autoref{thm:real-zero-dyadic-ledger-obligation} places $D$ between the stationary source route and the terminal RealUp seal. The downstream consumer obligation of \autoref{thm:real-zero-downstream-consumer-obligation} then requires every consumer to read the same $\BHist$, transport, replay, provenance, and local naming support.

Since \autoref{thm:real-zero-namecert-obligations} explicitly excludes rows for quotient streams, host zero equality, selected limits, trichotomy, and ambient completeness, the ledger has no path to those principles. Countable choice is likewise absent because the packet has only the displayed finite rows and no selector coordinate.
\end{proof}

\closureat{RealZeroUp}{seedStr}

\begin{closurestatus}{\RealZeroUp}
  \constructivestory{A $\RealZeroUp$ packet is generated from the rational zero seed, a constant StreamName window, a RegularCauchyZero route, a DyadicRatCore zero ledger, a terminal RealUp seal, componentwise transport, continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-zero-carrier}, \autoref{thm:real-zero-namecert-obligations}, and \autoref{thm:real-zero-terminal-seal-handoff} seed the finite zero-real seal route used by downstream PositiveReal and RealAbs consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completeness, quotient real equality, host equality, trichotomy, selected limits, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, stationary zero-source routing, terminal RealUp seal handoff, componentwise transport and replay stability, and ledger nonescape for the same finite packet.}
\end{closurestatus}
