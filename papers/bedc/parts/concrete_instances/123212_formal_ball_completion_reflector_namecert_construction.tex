\chapter{A Concrete Naming Certificate for $\FormalBallCompletionReflectorUp$}
\label{ch:concrete-instances-formal-ball-completion-reflector-namecert}
\origin{human}

Following Edalat and Bishop-style completion theory, the $\FormalBallCompletionReflectorUp$ packet records the finite reflector route by which a displayed formal-ball domain is sent to its rounded completion endpoint. It sits after the formal-ball domain of \autoref{ch:concrete-instances-formal-ball-domain-namecert}, the completion carrier of \autoref{ch:concrete-instances-formal-ball-completion-namecert}, the metric-specialized reflector of \autoref{ch:concrete-instances-metric-completion-reflector-namecert}, and the uniform reflector sibling of \autoref{ch:concrete-instances-uniform-completion-reflector-namecert}. The packet names only the formal-ball source, rounded completion target, dense unit, universal route, endpoint boundary, transport, replay, provenance, and local NameCert rows; it is not a quotient completion, a selected Cauchy representative, a host limit operator, or an ambient completeness theorem.

\begin{definition}[FormalBallCompletionReflector carrier]\label{def:formal-ball-completion-reflector-carrier}
A $\FormalBallCompletionReflectorUp$ carrier is a finite $\BHist$ packet
$$
R=(D,C,J,U,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\FormalBallDomainUp$ source row, exposing the rounded formal-ball domain data available before the reflector is read.
\item $C$ is the $\FormalBallCompletionUp$ target row, recording the completion carrier reached from the same displayed source windows.
\item $J$ is the dense unit row from $D$ into $C$, listing only the finite formal-ball images and radius refinements visible to the packet.
\item $U$ is the universal-route row: any completion-facing consumer must factor through the displayed $D,C,J$ surface before a reflector claim is exported.
\item $E$ is the real-endpoint boundary row, read through the completion target and the terminal real-name seal rather than through detached host points.
\item $H$ records componentwise $\hsame$ transport for the source, target, unit, universal route, endpoint boundary, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from a formal-ball completion-reflector request through $D,C,J,U,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FormalBallCompletionReflectorUp$, $\FormalBallDomainUp$, $\FormalBallCompletionUp$, $\MetricCompletionReflectorUp$, $\UniformCompletionReflectorUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FormalBallCompletionReflectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,C,J,U,E,H,T,P,N$. It has no coordinate for quotient completion equality, selected representatives, host point equality, countable choice, ambient completeness, or an untracked universal map.
\end{definition}

\begin{theorem}[FormalBallCompletionReflector NameCert obligations]\label{thm:formal-ball-completion-reflector-namecert-obligations}
For every accepted $\FormalBallCompletionReflectorUp$ carrier $R=(D,C,J,U,E,H,T,P,N)$, the local $\NameCert_{\FormalBallCompletionReflectorUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the formal-ball domain row, completion target row, dense unit row, universal-route row, endpoint boundary row, transport row, replay row, provenance row, and local naming row;
\item source visibility, requiring every reflector read to inspect the rounded domain data of $D$ before the completion target $C$ or unit row $J$ can be consumed;
\item completion-target exactness, requiring $C$ to expose only the $\FormalBallCompletionUp$ data generated from the displayed source route;
\item dense-unit accountability, requiring $J$ to list the formal-ball images and radius refinements used by the unit surface;
\item universal-route locality, requiring $U$ to factor all completion-facing reads through $D,C,J$ and to reuse the same endpoint boundary $E$;
\item structural stability, requiring $H$ and $T$ to preserve the same source, target, unit, universal, endpoint, provenance, and naming rows under $\hsame$ transport and displayed $\Cont$ replay;
\item provenance and local naming through $P$ and $N$, with no obligation row for quotient completion equality, selected representatives, host point equality, countable choice, ambient completeness, or an untracked universal map.
\end{enumerate}
No $\FormalBallCompletionReflectorUp$ obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:formal-ball-completion-reflector-carrier}. The accepted packet exposes exactly the domain, completion target, dense unit, universal route, endpoint boundary, transport, replay, provenance, and naming rows.

The source and target obligations are ordered by the carrier. A reflector read first sees the rounded formal-ball domain $D$; only then can it inspect the completion target $C$ and the dense unit $J$ generated from that displayed source data.

The universal row is local to the same surface. The row $U$ records factorization only through $D,C,J$ and therefore cannot replace the packet by a host quotient completion, selected representative, or ambient extension theorem.

Finally $E,H,T,P,N$ preserve and name the same route. The endpoint boundary is downstream of the completion target, while transport, replay, provenance, and local naming add no extra semantic coordinate. Since the excluded host principles have no carrier row, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[FormalBallCompletionReflector universal route]\label{thm:formal-ball-completion-reflector-universal-route}
Every public reflector read exported by an accepted $\FormalBallCompletionReflectorUp$ carrier factors through the displayed route
$$
\FormalBallDomainUp \longmapsto \FormalBallCompletionUp \longmapsto J \longmapsto U \longmapsto E \longmapsto H,T,P,N .
$$
Thus completion-facing consumers may read only the formal-ball source, rounded completion target, dense unit, universal-route row, real-endpoint boundary, transport, replay, provenance, and local naming data. They receive no quotient completion equality, selected Cauchy representative, countable-choice selector, host point equality, ambient completeness witness, or detached universal map.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:formal-ball-completion-reflector-namecert-obligations}. They force the source row $D$ to be inspected before any completion target or unit row is consumed.

The completion target and dense unit are the next displayed rows. The row $C$ records the rounded completion carrier, and $J$ records the unit images and radius refinements over the same source.

The universal route is then read through $U$ and the endpoint boundary through $E$. Componentwise transport by $H$, displayed replay by $T$, provenance by $P$, and local naming by $N$ preserve this row order.

The carrier has no quotient, selector, host-equality, choice, ambient-completeness, or detached-map coordinate. Therefore every public reflector read factors through the displayed formal-ball route.
\end{proof}

\begin{theorem}[FormalBallCompletionReflector real endpoint boundary]\label{thm:formal-ball-completion-reflector-real-endpoint-boundary}
For every accepted $\FormalBallCompletionReflectorUp$ carrier $R=(D,C,J,U,E,H,T,P,N)$, each real-endpoint read exported by $R$ remains downstream of the formal-ball source row $D$, completion target row $C$, dense unit row $J$, and universal-route row $U$. The endpoint boundary is therefore a terminal read of the displayed reflector route, not a detached real point, quotient-completion class, selected representative, host point equality, countable-choice selector, or ambient completeness witness.
\end{theorem}
\begin{proof}
Read the endpoint row $E$ from the carrier of \autoref{def:formal-ball-completion-reflector-carrier}. The definition places $E$ after the source, target, dense unit, and universal-route rows.

Apply \autoref{thm:formal-ball-completion-reflector-namecert-obligations}. Source visibility keeps the read inside $D$, completion-target exactness keeps the target inside $C$, dense-unit accountability keeps $J$ tied to displayed formal-ball images, and universal-route locality keeps $U$ attached to the same packet.

Transport and replay cannot move the endpoint outside that route. The rows $H$ and $T$ preserve the component order, while $P$ and $N$ record provenance and local naming.

Thus any real-endpoint read is terminal for $D,C,J,U,E,H,T,P,N$. The excluded detached real point, quotient class, selected representative, host equality, choice schedule, and ambient completeness data have no row from which a consumer could read them.
\end{proof}

\falsifiablePrediction{Every accepted $\FormalBallCompletionReflectorUp$ public reflector read factors through FormalBallDomainUp source data, FormalBallCompletionUp target data, dense unit rows, universal-route rows, real-endpoint boundaries, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{formal-ball-domain, formal-ball-completion, metric-completion-reflector, uniform-completion-reflector: $\FormalBallCompletionReflectorUp$ is not bijective to any listed sibling because it jointly carries the formal-ball source, rounded completion target, dense unit, universal route, endpoint boundary, transport, replay, provenance, and local NameCert rows in one reflector packet.}

\closureat{FormalBallCompletionReflectorUp}{seedStr}

\begin{closurestatus}{\FormalBallCompletionReflectorUp}
  \constructivestory{A $\FormalBallCompletionReflectorUp$ packet is generated from a FormalBallDomainUp source row, FormalBallCompletionUp target row, dense unit row, universal-route row, real-endpoint boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:formal-ball-completion-reflector-carrier}, \autoref{thm:formal-ball-completion-reflector-namecert-obligations}, \autoref{thm:formal-ball-completion-reflector-universal-route}, and \autoref{thm:formal-ball-completion-reflector-real-endpoint-boundary} seed the finite formal-ball completion reflector route for L10 completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected representatives, host point equality, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source visibility, completion-target exactness, dense-unit accountability, universal-route locality, real-endpoint boundary, transport, replay, provenance, and local naming for BEDC.Derived.FormalBallCompletionReflectorUp.}
\end{closurestatus}
