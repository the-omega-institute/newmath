\chapter{A Concrete Naming Certificate for $\FiniteTailFilterUp$}
\label{ch:concrete-instances-finite-tail-filter-namecert}
\origin{human}

The $\FiniteTailFilterUp$ packet is the finite-filter surface shared by the Real and Completion routes. It packages the cofinal-tail information that is read by the stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic rational core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational sequence surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the RealUp seal of \autoref{ch:concrete-instances-real-namecert}, the tail budget of \autoref{ch:concrete-instances-dyadictailbound-namecert}, and the common-tail comparison packet of \autoref{ch:concrete-instances-cauchy-tail-comparison-namecert}. The packet is the BEDC finite-filter analogue of the constructive Cauchy tail-filter interface: a consumer receives a finite set of admitted tail windows, the dyadic and regular-sequence rows attached to those windows, and the seal-facing route that preserves the same cofinal-tail packet.

\begin{definition}[FiniteTailFilter carrier]
\label{def:finite-tail-filter-carrier}
A $\FiniteTailFilterUp$ carrier is a finite $\BHist$ packet
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $S$ is the finite $\StreamNameUp$ window ledger whose entries form the admitted tail filter base;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the admitted windows;
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same admitted windows and dyadic ledger;
\item $B$ is the $\DyadicTailBoundUp$ tail-budget row that records the finite threshold and tolerance data used by the filter;
\item $Q$ is the $\CauchyTailComparisonUp$ common-tail comparison row read through the same window ledger and dyadic ledger;
\item $E$ is the $\RealUp$ seal row reached after the window, dyadic, regular-sequence, tail-budget, and common-tail comparison rows have all been displayed;
\item $H$ records componentwise $\hsame$ transports for $S,D,R,B,Q,E,C,P,N$;
\item $C$ records the displayed $\Cont$ routes for window readback, dyadic tolerance use, regular-rational readback, tail-budget consumption, common-tail comparison, and RealUp seal consumption;
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\DyadicTailBoundUp$, $\CauchyTailComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\FiniteTailFilterUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through the displayed rows $S,D,R,B,Q,E,H,C,P,N$ and their recorded read routes.
\end{definition}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.TasteGate.FiniteTailFilterUpTasteGate\_single\_carrier\_alignment}

\begin{theorem}[FiniteTailFilter NameCert obligations]
\label{thm:finite-tail-filter-namecert-obligations}
For the carrier of \autoref{def:finite-tail-filter-carrier}, the local $\NameCert_{\FiniteTailFilterUp}$ surface consists exactly of carrier admission for
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
window-filter exposure through $S$, dyadic tolerance exposure through $D$, regular-sequence readback through $R$, tail-budget exposure through $B$, common-tail comparison through $Q$, RealUp seal exposure through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_namecert\_obligations}
\begin{proof}
Project the displayed carrier. The rows $S,D,R,B,Q,E,H,C,P,N$ are the only coordinates of the finite packet, so carrier admission is the assertion that these rows are assembled as one $\BHist$ surface.

Read the analytic rows in order. The window ledger $S$ supplies the finite filter base, the dyadic ledger $D$ supplies the tolerance rows attached to that base, and the regular readback $R$ is the regular rational interpretation of the same finite windows.

The tail-budget row $B$ and the common-tail comparison row $Q$ then read the same $S,D,R$ packet rather than separate windows or separate tolerances. The RealUp seal $E$ is exposed only after those rows have been displayed.

The structural rows $H,C,P,N$ transport, replay, authenticate, and name the same packet. Hence the listed coordinates exhaust the local naming surface.
\end{proof}

\begin{theorem}[FiniteTailFilter non-escape]
\label{thm:finite-tail-filter-nonescape}
Every downstream consumer of an accepted $\FiniteTailFilterUp$ packet receives only the finite surface
$$
S,D,R,B,Q,E,H,C,P,N.
$$
It receives no quotient stream equality, selected limit, host real equality, ambient completeness datum, metric-completeness datum, countable-choice row, or standard bridge row.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_nonescape}
\begin{proof}
Inspect the carrier of \autoref{def:finite-tail-filter-carrier}. The analytic coordinates are the finite window ledger, dyadic tolerance ledger, regular-sequence readback, tail-budget row, common-tail comparison row, and RealUp seal row. The remaining coordinates are transport, continuation, package provenance, and local naming rows for that same packet.

Apply the NameCert obligation theorem. Any consumer route must pass through the displayed coordinates and their recorded $\hsame$ and $\Cont$ witnesses.

None of the excluded objects is a coordinate of the carrier or a row certified by the local naming surface. Therefore an accepted consumer cannot recover quotient stream equality, a selected limit, host real equality, ambient completeness, metric-completeness data, countable choice, or a standard bridge row from the finite filter packet.
\end{proof}

\begin{theorem}[FiniteTailFilter tail-window exactness]
\label{thm:finite-tail-filter-tail-window-exactness}
For every accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
the tail windows consumed by the $\DyadicTailBoundUp$ row $B$, the $\CauchyTailComparisonUp$ row $Q$, and the $\RealUp$ seal row $E$ are exactly the windows displayed in $S$ with the dyadic tolerances displayed in $D$. No accepted read may replace the filter base by an unrecorded window selector, split the tail-budget row from the common-tail comparison row, or substitute a separate dyadic tolerance ledger.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_tail\_window\_exactness}
\begin{proof}
Project $S$ and $D$ from the carrier. These are the only coordinates that display tail windows and dyadic tolerances.

Use the NameCert obligations to locate $B$ and $Q$. The tail-budget row and the common-tail comparison row are accepted only as rows of the same packet, and both are read after the $S,D,R$ surface has been displayed.

The RealUp seal row $E$ is downstream of that same packet. Its continuation route in $C$ can replay the displayed rows, but it has no coordinate for a second filter base or a second tolerance ledger.

Thus the visible tail-window packet is exact: all accepted budget, comparison, and seal reads use $S$ and $D$ as displayed.
\end{proof}

\begin{theorem}[FiniteTailFilter Real seal handoff]
\label{thm:finite-tail-filter-real-seal-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
reaches the seal row $E$ only through the displayed route
$$
S,D,R,B,Q,H,C,P,N.
$$
The handoff is therefore a finite cofinal-tail route shared by Real and Completion seal reads, not a quotient of streams, selected limit, host real equality, ambient completeness theorem, metric-completeness datum, countable-choice row, or standard bridge row.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_real\_seal\_handoff}
\begin{proof}
Start from the carrier. The seal coordinate $E$ is stored in the same finite packet as the stream-window ledger $S$, dyadic tolerance ledger $D$, regular readback $R$, tail-budget row $B$, common-tail comparison row $Q$, and structural rows $H,C,P,N$.

Apply tail-window exactness. The tail-budget and common-tail comparison rows use the same $S,D$ window packet, so a seal-facing read cannot detach the RealUp row from the cofinal-tail filter base.

Use the NameCert obligation theorem to read the remaining route data. The $\hsame$ rows in $H$, the $\Cont$ routes in $C$, the package row $P$, and the local naming row $N$ certify the displayed handoff and no larger completion structure.

Finally apply non-escape. Since the carrier has no excluded coordinate, the Real and Completion consumers receive the finite route $S,D,R,B,Q,H,C,P,N$ before reaching $E$ and nothing outside that route.
\end{proof}

\begin{theorem}[FiniteTailFilter Real-window consumer]
\label{thm:finite-tail-filter-real-window-consumer}
Every $\RealWindowBudgetUp$ or Real-completion consumer reading an accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
receives the displayed finite cofinal-tail route
$$
S,D,R,B,Q,E,H,C,P,N.
$$
The Real-window budget row may use only the already displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\DyadicTailBoundUp$, $\CauchyTailComparisonUp$, and $\RealUp$ rows. It receives no quotient stream equality, selected limit, host real equality, countable-choice row, ambient completeness theorem, metric-completeness datum, or unrecorded completion carrier.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_real\_window\_consumer}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:finite-tail-filter-namecert-obligations}. They expose the accepted packet exactly as $S,D,R,B,Q,E,H,C,P,N$, with $S,D,R,B,Q,E$ as the Real-facing analytic rows and $H,C,P,N$ as the structural rows.

Apply tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}. The tail-budget row $B$, common-tail comparison row $Q$, and RealUp seal row $E$ all read the same displayed $S,D$ window and tolerance packet, so a Real-window budget consumer cannot introduce a separate selector or tolerance ledger.

Use the Real seal handoff theorem, \autoref{thm:finite-tail-filter-real-seal-handoff}, to identify the downstream route. Any Real or Completion read reaches $E$ only through $S,D,R,B,Q,H,C,P,N$, and the present consumer includes $E$ as the displayed seal row reached by that route.

Finally apply non-escape, \autoref{thm:finite-tail-filter-nonescape}. Since quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, metric-completeness data, and unrecorded completion carriers are not coordinates of $F$, the $\RealWindowBudgetUp$ or Real-completion consumer receives only the finite cofinal-tail packet already displayed.
\end{proof}

\begin{theorem}[FiniteTailFilter RealWindowBudget consumer link]
\label{thm:finitetailfilter-realwindowbudget-consumer-link}
Every $\RealWindowBudgetUp$ consumer of an accepted $\FiniteTailFilterUp$ packet
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
must reuse the displayed route as its finite request, $\StreamNameUp$ window, dyadic readback, $\RegSeqRatUp$ handoff, and $\RealUp$ seal surface. It cannot introduce a private selector, separate dyadic ledger, selected limit, quotient stream, host equality, countable-choice row, or ambient completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_real\_window\_budget\_consumer\_link}
\begin{proof}
Start with the FiniteTailFilter NameCert obligations from \autoref{thm:finite-tail-filter-namecert-obligations}. They expose $S,D,R,B,Q,E,H,C,P,N$ as the complete accepted packet, with $S,D,R,B,Q,E$ carrying the finite analytic route.

Apply tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}. The tail-budget and comparison rows read the same finite window and dyadic tolerance data, so a RealWindowBudget consumer cannot replace $S$ or $D$ by a private selector or separate ledger.

Use the Real seal handoff and Real-window consumer theorems, \autoref{thm:finite-tail-filter-real-seal-handoff} and \autoref{thm:finite-tail-filter-real-window-consumer}. Together they force the consumer route through the displayed RegSeqRat readback $R$, tail budget $B$, common-tail comparison $Q$, and seal row $E$.

Finally apply the RealWindowBudget finite-route visibility supplied by the packet surface and the non-escape theorem, \autoref{thm:finite-tail-filter-nonescape}. The structural rows $H,C,P,N$ preserve the same route, while the carrier has no coordinate for selected limits, quotient streams, host equality, countable choice, ambient completeness, or an unrecorded completion surface.
\end{proof}

\begin{theorem}[FiniteTailFilter Real-window selector bridge]
\label{thm:finite-tail-filter-real-window-budget-selector-bridge}
Every $\RealWindowBudgetUp$ selector-stability read that consumes an accepted $\FiniteTailFilterUp$ packet
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
must factor through the displayed finite route $S,D,R,B,Q,E,H,C,P,N$. Through this route it reads the $\StreamNameUp$ window row, $\DyadicRatCoreUp$ ledger, $\RegSeqRatUp$ handoff, tail-budget row, common-tail row, $\RealUp$ seal, componentwise transport, continuation, package source, and local naming coordinate supplied by the packet.

Consequently, by $\RealWindowBudgetUp$ selector stability, the downstream selector surface cannot replace that route by a private window family, detached dyadic ledger, selected limit, quotient stream, host real equality, countable-choice row, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_real\_window\_budget\_selector\_bridge}
\begin{proof}
Start with the FiniteTailFilter NameCert obligations, \autoref{thm:finite-tail-filter-namecert-obligations}. They expose the accepted packet as the ten displayed rows $S,D,R,B,Q,E,H,C,P,N$, with the analytic rows and structural rows carried by one local certificate.

Apply tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}. The stream-window row $S$ and dyadic ledger $D$ are the common cofinal-tail packet used by the tail budget $B$, common-tail comparison $Q$, and the Real-facing seal row $E$.

Use the Real seal handoff and the RealWindowBudget consumer link, \autoref{thm:finite-tail-filter-real-seal-handoff} and \autoref{thm:finitetailfilter-realwindowbudget-consumer-link}. These identify the downstream read as a consumer of the same $S,D,R,B,Q,E,H,C,P,N$ route, rather than a read of any hidden completion or host-level real structure.

Finally apply $\RealWindowBudgetUp$ selector stability, \autoref{thm:real-window-budget-selector-stability}. Once the read reaches the RealWindowBudget selector surface, that theorem keeps the selector synchronized with the finite request, window, dyadic, regular, Real-facing, and support rows supplied by the displayed route, so no private window family, detached ledger, selected limit, quotient stream, host equality, countable-choice row, or ambient completeness coordinate can replace the packet route.
\end{proof}

\begin{theorem}[FiniteTailFilter sibling tail handoff]
\label{thm:finite-tail-filter-sibling-tail-handoff}
Every consumer whose route passes through $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerances, $\RegSeqRatUp$ readback, $\DyadicTailBoundUp$ budget, $\CauchyTailComparisonUp$ common-tail rows, or the $\RealUp$ seal factors through the displayed finite packet
$$
S,D,R,B,Q,E,H,C,P,N.
$$
No sibling chapter may substitute its own tail selector, tolerance ledger, common-tail row, or seal row for the packet displayed by $\FiniteTailFilterUp$.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:finite-tail-filter-carrier}. The rows $S,D,R,B,Q,E,H,C,P,N$ are the whole finite packet, so any sibling read of a stream window, dyadic tolerance, regular readback, budget, comparison, or seal row has to be a read of one of those displayed coordinates.

Apply the NameCert obligations of \autoref{thm:finite-tail-filter-namecert-obligations}. They expose the analytic rows and the structural rows as one local naming surface, with the $\hsame$ and $\Cont$ data recording how the same packet is replayed downstream.

Use tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}, to identify the common window and tolerance packet consumed by the budget, comparison, and seal-facing rows. Then use the Real seal handoff theorem, \autoref{thm:finite-tail-filter-real-seal-handoff}, to carry the same packet to Real and Completion consumers.

Finally apply non-escape. The handoff is finite and has no coordinate for a quotient stream, selected limit, host real equality, ambient completeness datum, metric-completeness datum, countable-choice row, or standard bridge row. Hence no sibling route can replace the displayed tail selector, ledger, or seal row by an unrecorded one.
\end{proof}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_sibling\_tail\_handoff}

\begin{theorem}[FiniteTailFilter shared-window meet]
\label{thm:finite-tail-filter-shared-window-meet}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ packet. Suppose an accepted $\CauchyModulusRefinementUp$ packet has its selected-window row and readback row consumed as the same displayed finite window and regular readback route used by $F$. Then every $\RealUp$ or Completion consumer reads the shared L10 route through
$$
S,D,R,B,Q,E,H,C,P,N.
$$
It cannot replace this meet by a hidden selector, second meet, quotient stream, selected limit, host real equality, countable-choice row, ambient completeness datum, or metric-completeness row.
\end{theorem}
\begin{proof}
First bind the common finite packet. Tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}, identifies $S$ and $D$ as the displayed window and tolerance packet read by the tail-budget row, common-tail row, and Real seal of $F$.

Next route the sibling consumers. The sibling tail handoff and RealWindowBudget consumer link, \autoref{thm:finite-tail-filter-sibling-tail-handoff} and \autoref{thm:finitetailfilter-realwindowbudget-consumer-link}, force StreamName, DyadicRatCore, RegSeqRat, RealWindowBudget, Real, and Completion reads through the displayed route $S,D,R,B,Q,E,H,C,P,N$.

Now attach the adjacent selected-window route. By \autoref{thm:cauchy-modulus-refinement-shared-tail-selector-seal-coherence}, an accepted $\CauchyModulusRefinementUp$ consumer whose selected-window and readback rows are the same displayed finite window and regular readback route cannot introduce a separate selector, second meet, or off-route Real seal.

Finally apply non-escape, \autoref{thm:finite-tail-filter-nonescape}. Since neither accepted packet contributes a coordinate for quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, metric-completeness data, or a hidden completion surface, the shared consumer route is exactly the finite packet displayed by $F$.
\end{proof}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_shared\_window\_meet}

\begin{theorem}[FiniteTailFilter carrier transport obligation]
\label{thm:finite-tail-filter-carrier-transport-obligation}
For every accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
the componentwise $\hsame$ row $H$ transports exactly the displayed packet rows $S,D,R,B,Q,E,C,P,N$. It has no coordinate that can introduce quotient stream equality, host real equality, selected limits, choice data, completeness data, or standard bridge rows.
\end{theorem}
\begin{proof}
Project the carrier and isolate the structural row $H$. The definition of the carrier lists $H$ only as the componentwise $\hsame$ data for the already displayed rows $S,D,R,B,Q,E,C,P,N$.

Use the NameCert obligations to read the certified transport surface. Since those obligations expose only the carrier coordinates and their recorded routes, a transport in $H$ is fieldwise over the finite packet and cannot add a new analytic row.

Finally apply non-escape. The excluded objects are not coordinates of $F$ and are not authenticated by the local naming row $N$. Thus classifier transport preserves exactly the displayed packet rows and nothing outside them.
\end{proof}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_transport\_obligation}

\begin{theorem}[FiniteTailFilter cofinal-window directedness obligation]
\label{thm:finite-tail-filter-cofinal-window-directedness-obligation}
For every accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
the displayed $\Cont$ row $C$ replays every accepted tail-window read, dyadic tolerance read, regular rational readback, tail-budget read, common-tail comparison read, and RealUp seal read through the same finite cofinal packet $S,D,R,B,Q,E,H,P,N$.
\end{theorem}
\begin{proof}
Start with the continuation coordinate $C$. By the carrier definition, $C$ records displayed routes for window readback, dyadic tolerance use, regular-rational readback, tail-budget consumption, common-tail comparison, and RealUp seal consumption.

Apply tail-window exactness. The budget, comparison, and seal-facing reads all consume the same $S,D$ window and tolerance packet, so the continuation cannot branch into an unrecorded selector or tolerance ledger.

Use the Real seal handoff theorem to identify the downstream route. Every Real or Completion consumer reaches $E$ through the finite packet $S,D,R,B,Q,H,C,P,N$, and the replay row $C$ is the route data for that same packet. Hence all accepted reads are directed through the displayed cofinal window surface.
\end{proof}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_cofinal\_window\_directedness\_obligation}

\begin{theorem}[FiniteTailFilter cofinal-route exhaustion]
\label{thm:finite-tail-filter-cofinal-route-exhaustion}
Every $\RealWindowBudgetUp$, $\RealUp$, or Completion-facing consumer of an
accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
factors through the cofinal route $S,D,R,B,Q,E,H,C,P,N$. No such consumer can
add private windows, detached dyadic ledgers, selected limits, quotient streams,
host equality, countable-choice rows, ambient completeness, or standard bridge
rows.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_cofinal\_route\_exhaustion}
\begin{proof}
First assemble the carrier route from the NameCert obligations and tail-window
exactness, \autoref{thm:finite-tail-filter-namecert-obligations} and
\autoref{thm:finite-tail-filter-tail-window-exactness}. These identify
$S,D,R,B,Q,E,H,C,P,N$ as the accepted packet and make $S,D$ the exact window and
tolerance rows read by $B,Q,E$.

Next pass to the Real-facing surface. Apply the Real seal handoff and the
RealWindowBudget consumer link,
\autoref{thm:finite-tail-filter-real-seal-handoff} and
\autoref{thm:finitetailfilter-realwindowbudget-consumer-link}. They route
RealWindowBudget, Real, and Completion-facing consumers through the same finite
packet.

Use the Real-window selector bridge to rule out a selector-side replacement.
\autoref{thm:finite-tail-filter-real-window-budget-selector-bridge} keeps
selector stability tied to $S,D,R,B,Q,E,H,C,P,N$, not to a private window family
or detached dyadic ledger.

Finally apply non-escape,
\autoref{thm:finite-tail-filter-nonescape}. Since the carrier contains no
selected-limit, quotient-stream, host-equality, countable-choice, ambient
completeness, or standard bridge coordinate, the displayed cofinal route
exhausts the admitted consumer surface.
\end{proof}

\begin{theorem}[FiniteTailFilter structural provenance obligation]
\label{thm:finite-tail-filter-structural-provenance-obligation}
For every accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
the rows $P$ and $N$ record exactly the sibling and kernel provenance over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\DyadicTailBoundUp$, $\CauchyTailComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$. They record no independent completion theorem, quotient carrier, selected limit, or bridge schema.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_structural\_provenance\_obligation}
\begin{proof}
Read $P$ and $N$ from the carrier. The package row $P$ names the sibling and kernel sources of the displayed packet, while $N$ is the local $\NameCert_{\FiniteTailFilterUp}$ row for the same surface.

Apply the NameCert obligations. They identify the provenance domain as the listed sibling chapters together with the kernel rows $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.

The carrier has no separate coordinate for an independent completion theorem, quotient carrier, selected limit, or bridge schema. Therefore the provenance and naming rows certify exactly the stated sources and no larger structure.
\end{proof}

\begin{theorem}[FiniteTailFilter terminal cofinal-consumer exclusion]
\label{thm:finite-tail-filter-terminal-cofinal-consumer-exclusion}
For every accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
every terminal $\RealUp$, $\RealWindowBudgetUp$, $\UniformCauchyCriterionUp$, or completion-facing consumer of the accepted cofinal packet factors through the displayed finite route $S,D,R,B,Q,E,H,C,P,N$.

Consequently such a consumer cannot recover quotient stream equality, selected limits, host real equality, ambient completeness, metric-completeness data, countable choice, a private selector, a separate dyadic ledger, or a standard bridge row from the cofinal packet.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_terminal\_cofinal\_consumer\_exclusion}
\begin{proof}
Start with tail-window exactness and the Real seal handoff, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-real-seal-handoff}. They identify the stream-window and dyadic-tolerance rows as the common cofinal packet used by the tail budget, common-tail comparison, Real seal, and completion-facing reads.

Next apply the RealWindowBudget selector bridge and the sibling tail handoff, \autoref{thm:finite-tail-filter-real-window-budget-selector-bridge} and \autoref{thm:finite-tail-filter-sibling-tail-handoff}. These force selector-stability and sibling reads to reuse the same $S,D,R,B,Q,E,H,C,P,N$ route rather than a detached window family, tolerance ledger, Real row, or seal row.

Use the cofinal-window directedness obligation to route every terminal read through the displayed continuation row $C$. The continuation can replay only the accepted tail-window, dyadic-tolerance, regular-rational, tail-budget, common-tail, and Real seal reads carried by the finite packet.

Finally combine non-escape with the structural provenance obligation, \autoref{thm:finite-tail-filter-nonescape} and \autoref{thm:finite-tail-filter-structural-provenance-obligation}. The accepted carrier has no coordinate for quotient stream equality, selected limits, host real equality, ambient or metric completeness, countable choice, private selectors, detached ledgers, or standard bridge rows, and the provenance rows $P,N$ certify no hidden completion source. Hence every terminal consumer remains confined to the displayed cofinal route.
\end{proof}

\begin{theorem}[FiniteTailFilter common-tail seal cofinality]
\label{thm:finite-tail-filter-common-tail-seal-cofinality}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. Any $\RealWindowBudgetUp$, $\UniformCauchyCriterionUp$, $\RealUp$, or completion-facing consumer that uses the accepted $\FiniteTailFilterUp$ packet must identify its $\StreamNameUp$ window row, $\DyadicRatCoreUp$ tolerance row, $\RegSeqRatUp$ readback row, $\DyadicTailBoundUp$ budget row, $\CauchyTailComparisonUp$ row, and $\RealUp$ seal row with the displayed finite packet $S,D,R,B,Q,E,H,C,P,N$.

No private selector, detached dyadic ledger, quotient stream, selected limit, countable-choice row, ambient completeness row, or standard bridge row is admitted by this common-tail seal.
\end{theorem}
\begin{proof}
First apply tail-window exactness and the Real seal handoff, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-real-seal-handoff}. These identify the window, tolerance, readback, budget, comparison, and Real seal rows as coordinates of the same accepted finite packet.

Next apply the RealWindowBudget consumer link and the Real-window selector bridge, \autoref{thm:finitetailfilter-realwindowbudget-consumer-link} and \autoref{thm:finite-tail-filter-real-window-budget-selector-bridge}. A Real-window consumer therefore reads its selector and budget data through $S,D,R,B,Q,E,H,C,P,N$, not through a private selector family or detached dyadic ledger.

Use the sibling tail handoff and cofinal-window directedness obligation, \autoref{thm:finite-tail-filter-sibling-tail-handoff} and \autoref{thm:finite-tail-filter-cofinal-window-directedness-obligation}. The UniformCauchyCriterion and completion-facing reads must replay the same continuation route $C$ over the displayed packet, so the common-tail comparison row cannot be replaced by a quotient stream, selected limit, countable-choice row, ambient completeness row, or standard bridge row.

Finally invoke terminal cofinal-consumer exclusion, \autoref{thm:finite-tail-filter-terminal-cofinal-consumer-exclusion}. It confines every terminal Real-window, Real, UniformCauchyCriterion, or completion-facing consumer to the accepted cofinal packet, which proves that the finite packet $S,D,R,B,Q,E,H,C,P,N$ is the common route consumed by those chapters.
\end{proof}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_common\_tail\_seal\_cofinality}

\begin{theorem}[FiniteTailFilter cofinal-window budget normal form]
\label{thm:finitetailfilter-cofinal-window-budget-normal-form}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. Every accepted $\RealWindowBudgetUp$, $\UniformCauchyCriterionUp$, $\RealUp$, or completion-facing read of $F$ has the same normal-form route $S,D,R,B,Q,E,H,C,P,N$. The rows $S,D$ are the only window and dyadic tolerance rows, $B,Q$ are the only tail-budget and common-tail comparison rows, and $E$ is the only $\RealUp$ seal row.

No private selector, detached dyadic ledger, quotient stream, selected limit, host real equality, countable-choice row, ambient completeness row, metric-completeness row, independent completion row, or standard bridge row is part of this normal form.
\end{theorem}
\begin{proof}
First build the finite route from tail-window exactness and the Real seal handoff, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-real-seal-handoff}. These identify $S,D,R,B,Q,E$ as one accepted finite packet rather than separate window, tolerance, readback, budget, comparison, and Real seal sources.

Next account for Real-window and selector-facing consumers. Apply \autoref{thm:finitetailfilter-realwindowbudget-consumer-link} and \autoref{thm:finite-tail-filter-real-window-budget-selector-bridge}. They force every $\RealWindowBudgetUp$ read to use the same route $S,D,R,B,Q,E,H,C,P,N$, with no private selector family or detached dyadic ledger.

Then route the uniform and completion-facing reads through the sibling and cofinal rows. Use \autoref{thm:finite-tail-filter-sibling-tail-handoff} together with \autoref{thm:finite-tail-filter-cofinal-window-directedness-obligation}. The continuation row $C$ replays only the displayed cofinal packet, so the $\UniformCauchyCriterionUp$ and completion-facing consumers cannot replace $B$ or $Q$ by another tail-budget or common-tail comparison row.

Finally apply \autoref{thm:finite-tail-filter-common-tail-seal-cofinality}. It states that every RealWindowBudget, UniformCauchyCriterion, Real, or completion-facing consumer identifies its window, tolerance, readback, budget, comparison, and Real seal rows with the accepted packet $S,D,R,B,Q,E,H,C,P,N$.

The exclusion clause follows from terminal cofinal-consumer exclusion and structural provenance, \autoref{thm:finite-tail-filter-terminal-cofinal-consumer-exclusion} and \autoref{thm:finite-tail-filter-structural-provenance-obligation}. Since the accepted carrier has no coordinate for the listed external structures, the normal form is exactly the displayed route.
\end{proof}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FinitetailfilterCofinalWindowBudgetNormalForm}

\begin{theorem}[FiniteTailFilter terminal classifier exactness]
\label{thm:finite-tail-filter-terminal-classifier-exactness}
For every accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
the terminal classifier available to any $\RealWindowBudgetUp$, $\UniformCauchyCriterionUp$, $\RealUp$, or completion-facing consumer is exactly the componentwise classifier on the displayed finite route $S,D,R,B,Q,E,H,C,P,N$.

Thus no terminal read may replace $S,D$ by a private window or tolerance ledger, replace $R,B,Q,E$ by detached $\RegSeqRatUp$, tail-budget, common-tail, or $\RealUp$ rows, or introduce quotient streams, selected limits, host equality, countable choice, ambient completeness, or bridge schema data.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations, \autoref{thm:finite-tail-filter-namecert-obligations}. They expose the accepted packet as the rows $S,D,R,B,Q,E,H,C,P,N$, with $H$ carrying the componentwise $\hsame$ transports, $C$ carrying the displayed continuation routes, and $P,N$ carrying package provenance and local naming.

Next apply tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}. The window and dyadic tolerance inputs of the tail-budget row, the common-tail comparison row, and the Real seal are exactly $S$ and $D$, so the classifier cannot be evaluated through a private window family or a detached tolerance ledger.

For the Real and completion-facing side, use the Real seal handoff and the RealWindowBudget consumer link, \autoref{thm:finite-tail-filter-real-seal-handoff} and \autoref{thm:finitetailfilter-realwindowbudget-consumer-link}. These route every Real-window, Real, and completion-facing read through the same finite packet before reaching the seal row $E$.

For the common-tail side, use common-tail seal cofinality, \autoref{thm:finite-tail-filter-common-tail-seal-cofinality}. It identifies the $\RegSeqRatUp$ readback, tail-budget, common-tail comparison, and $\RealUp$ seal rows with the displayed route $R,B,Q,E$ rather than with detached sibling rows.

Finally apply the cofinal-window budget normal form, \autoref{thm:finitetailfilter-cofinal-window-budget-normal-form}. That theorem states that every terminal RealWindowBudget, UniformCauchyCriterion, Real, or completion-facing read has the same normal-form route $S,D,R,B,Q,E,H,C,P,N$ and no coordinate for the excluded external structures. Therefore the only terminal classifier left available is the componentwise classifier on that displayed route.
\end{proof}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_terminal\_classifier\_exactness}

\begin{theorem}[FiniteTailFilter terminal Lean target surface]
\label{thm:finite-tail-filter-terminal-lean-target-surface}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. The paper-level Lean target for this chapter is the exact terminal packet $F$: every $\RealWindowBudgetUp$, $\UniformCauchyCriterionUp$, $\RealUp$, and completion-facing read must consume the displayed window row, dyadic tolerance row, $\RegSeqRatUp$ readback, tail-budget row, common-tail comparison row, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and local $\NameCert_{\FiniteTailFilterUp}$ row.

No terminal Lean target may replace that packet by quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, metric-completeness data, independent completion data, or a standard bridge row.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterTerminalLeanTargetSurface}
\begin{proof}
Begin with \autoref{thm:finite-tail-filter-terminal-classifier-exactness}. It identifies the terminal classifier as the componentwise classifier on $S,D,R,B,Q,E,H,C,P,N$ for every RealWindowBudget, UniformCauchyCriterion, Real, and completion-facing consumer.

Use the cofinal-window budget normal form, \autoref{thm:finitetailfilter-cofinal-window-budget-normal-form}. It states that the same finite route is the normal form for the listed consumers and that the excluded external structures are not coordinates of the carrier.

Thus the Lean target surface has no additional input row to consume. It must take exactly the displayed packet and prove the terminal reads from that packet.
\end{proof}

\begin{theorem}[FiniteTailFilter formal-target consumer route]
\label{thm:finite-tail-filter-formal-target-consumer-route}
Every formal target consuming a $\FiniteTailFilterUp$ terminal read must read the same finite cofinal-tail route
$$
S,D,R,B,Q,E,H,C,P,N.
$$
The route factors RealWindowBudget, UniformCauchyCriterion, Real, and completion-facing requests through $\BHist$ carrier rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and the local $\NameCert_{\FiniteTailFilterUp}$ row before any terminal classifier is evaluated.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_formal\_target\_consumer\_route}
\begin{proof}
First apply \autoref{thm:finite-tail-filter-terminal-lean-target-surface}. It fixes the formal target as the exact terminal packet rather than a family of host-side consumer assumptions.

Next use \autoref{thm:finite-tail-filter-common-tail-seal-cofinality}. It forces the window, tolerance, readback, budget, comparison, and Real seal rows for each terminal consumer to agree with the accepted packet.

Finally attach the consumer exclusion theorem, \autoref{thm:finite-tail-filter-terminal-cofinal-consumer-exclusion}. Since every terminal consumer is confined to the accepted cofinal packet, the formal target route is precisely the finite $\BHist$, $\hsame$, $\Cont$, provenance, and NameCert route stated above.
\end{proof}

\begin{theorem}[FiniteTailFilter Lean intake non-escape]
\label{thm:finite-tail-filter-lean-intake-nonescape}
The Lean intake for $\FiniteTailFilterUp$ is non-escaping: it may consume only the BHist packet $S,D,R,B,Q,E,H,C,P,N$ and the terminal classifier facts already displayed in this chapter. In particular, the intake cannot add a private RealWindowBudget selector, a separate UniformCauchyCriterion witness, a host Real equality, a selected completion point, a quotient stream, countable choice, ambient completeness, metric completeness, or bridge schema data.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterLeanIntakeNonescape}
\begin{proof}
Start from \autoref{thm:finite-tail-filter-formal-target-consumer-route}. The formal target reads the terminal consumers only through the displayed cofinal-tail route.

Use \autoref{thm:finite-tail-filter-terminal-classifier-exactness} to evaluate the classifier. The classifier is already exact on $S,D,R,B,Q,E,H,C,P,N$, so an additional selector, witness, equality, completion point, quotient stream, choice principle, completeness row, or bridge row would not be read by the accepted classifier.

The non-escape clause follows from \autoref{thm:finite-tail-filter-nonescape} and \autoref{thm:finite-tail-filter-structural-provenance-obligation}. Both keep accepted reads inside the finite carrier, transport, route, provenance, and local naming surface.
\end{proof}

\begin{theorem}[FiniteTailFilter formal intake route exhaustion]
\label{thm:finitetailfilter-formal-intake-route-exhaustion}
Every formal intake read of an accepted $\FiniteTailFilterUp$ carrier
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
factors through the finite tail-window row $S$, the $\RealWindowBudgetUp$ row, the cofinal-route rows $D,R,B,Q,E$, the terminal classifier, and the Lean intake non-escape surface. No formal intake read may add a hidden tail oracle, quotient stream, selected host limit, host real equality, choice datum, ambient completeness row, metric-completeness row, independent completion datum, or bridge row.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_formal\_intake\_route\_exhaustion}
\begin{proof}
First apply the formal-target consumer route, \autoref{thm:finite-tail-filter-formal-target-consumer-route}. It confines every formal target to the finite cofinal-tail route $S,D,R,B,Q,E,H,C,P,N$ before any terminal classifier is evaluated.

Use the cofinal-window budget normal form, \autoref{thm:finitetailfilter-cofinal-window-budget-normal-form}. This factors RealWindowBudget, UniformCauchyCriterion, Real, and completion-facing reads through the displayed tail-window, tolerance, regular-readback, tail-budget, comparison, and Real-seal rows.

Next apply terminal classifier exactness, \autoref{thm:finite-tail-filter-terminal-classifier-exactness}. The classifier available to every terminal consumer is exactly the componentwise classifier on the displayed finite route, so the formal intake has no second classifier outside the packet.

Finally invoke Lean intake non-escape, \autoref{thm:finite-tail-filter-lean-intake-nonescape}. Since the intake may consume only the BHist packet and the terminal classifier facts already displayed in the chapter, every formal intake read is exhausted by the finite route above and cannot add a hidden tail oracle, quotient stream, selected host limit, choice datum, completeness datum, or bridge row.
\end{proof}

\begin{theorem}[FiniteTailFilter formal target route closure]
\label{thm:finite-tail-filter-formal-target-route-closure}
The formal target for the accepted $\FiniteTailFilterUp$ packet
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
is the closed L10 cofinal-tail surface formed by the existing $\mathsf{DyadicRatCoreUp}$, $\mathsf{StreamNameUp}$, $\mathsf{RegSeqRatUp}$, $\mathsf{RealUp}$, $\mathsf{RealWindowBudgetUp}$, $\mathsf{UniformCauchyCriterionUp}$, $\mathsf{CauchyTailComparisonUp}$, and $\mathsf{DyadicTailBoundUp}$ rows. It consumes only finite cofinal tail windows, classifier-stable tail cutoffs, non-escape ledgers, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\FiniteTailFilterUp}$ row.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterFormalTargetRouteClosure}
\begin{proof}
Start from \autoref{thm:finite-tail-filter-namecert-obligations} and \autoref{thm:finitetailfilter-formal-intake-route-exhaustion}. These identify the accepted packet and exhaust every formal intake route through $S,D,R,B,Q,E,H,C,P,N$.

Next apply \autoref{thm:finitetailfilter-cofinal-window-budget-normal-form} and \autoref{thm:finite-tail-filter-terminal-classifier-exactness}. The window, dyadic tolerance, regular-readback, tail-budget, common-tail comparison, and Real-seal rows are therefore the only classifier-stable tail cutoffs visible to an L10 consumer.

Finally use \autoref{thm:finite-tail-filter-lean-intake-nonescape}. Since the intake has no hidden tail oracle, quotient stream, selected limit, host equality, choice datum, completeness row, or bridge row, the formal target is exactly the closed finite cofinal-tail surface above.
\end{proof}

\begin{theorem}[FiniteTailFilter bridge premise surface]
\label{thm:finite-tail-filter-bridge-premise-surface}
Any bridge premise that reads the L10 $\FiniteTailFilterUp$ surface must first read the same finite packet $F=(S,D,R,B,Q,E,H,C,P,N)$. The premise may expose $\mathsf{DyadicRatCoreUp}$, $\mathsf{StreamNameUp}$, $\mathsf{RegSeqRatUp}$, $\mathsf{RealUp}$, $\mathsf{RealWindowBudgetUp}$, $\mathsf{UniformCauchyCriterionUp}$, $\mathsf{CauchyTailComparisonUp}$, and $\mathsf{DyadicTailBoundUp}$ only as finite tail-window, dyadic tolerance, regular-readback, Real-seal, budget, criterion, comparison, and tail-bound rows inside the packet; it may not replace them by a standard bridge row, ambient completeness, quotient stream equality, selected host limits, or host Real equality.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterBridgePremiseSurface}
\begin{proof}
Use \autoref{thm:finite-tail-filter-formal-target-route-closure} to put every bridge-facing premise on the closed cofinal-tail surface.

Then apply \autoref{thm:finite-tail-filter-common-tail-seal-cofinality}. The Real seal, common-tail comparison, dyadic tail bound, and window-budget data are consumed together, so a bridge premise cannot separate one row into an ambient completion witness.

The excluded replacements are ruled out by \autoref{thm:finite-tail-filter-terminal-cofinal-consumer-exclusion} and \autoref{thm:finite-tail-filter-lean-intake-nonescape}. Thus a later bridge proof has a premise surface, but that surface is still the finite packet rather than a host-side bridge assumption.
\end{proof}

\begin{theorem}[FiniteTailFilter Lean-ready obligation surface]
\label{thm:finite-tail-filter-lean-ready-obligation-surface}
The Lean-ready obligation for $\FiniteTailFilterUp$ is to prove that the formal target route closes over the displayed finite cofinal-tail packet and no other data. Concretely, every $\mathsf{RealWindowBudgetUp}$, $\mathsf{UniformCauchyCriterionUp}$, $\mathsf{CauchyTailComparisonUp}$, $\mathsf{DyadicTailBoundUp}$, $\mathsf{RegSeqRatUp}$, $\mathsf{StreamNameUp}$, $\mathsf{DyadicRatCoreUp}$, and $\mathsf{RealUp}$ read must be reconstructed from $S,D,R,B,Q,E,H,C,P,N$, the terminal classifier exactness row, and the non-escape ledger already certified in this chapter.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_lean\_ready\_obligation\_surface}
\begin{proof}
First collect the carrier and naming data from \autoref{def:finite-tail-filter-carrier} and \autoref{thm:finite-tail-filter-namecert-obligations}. They provide the rows that a Lean theorem can bind without adding a new carrier.

Next combine cofinal-route exhaustion with terminal classifier exactness. Theorems~\ref{thm:finitetailfilter-formal-intake-route-exhaustion} and \ref{thm:finite-tail-filter-terminal-classifier-exactness} leave the target with only finite cofinal windows and classifier-stable tail cutoffs.

It remains to record the non-escape side. Theorems~\ref{thm:finite-tail-filter-lean-intake-nonescape} and \ref{thm:finite-tail-filter-formal-target-route-closure} show that the future Lean obligation is a closure theorem over this packet, not a request for quotient streams, selected limits, ambient completeness, or a bridge row.
\end{proof}

\begin{theorem}[FiniteTailFilter Real-completion lattice link]
\label{thm:finite-tail-filter-real-completion-lattice-link}
An accepted finite tail filter consumed by the current Real-completion route determines the same cofinal-tail lattice row for $\StreamNameUp$ finite windows, $\DyadicRatCoreUp$ tolerance windows, $\RegSeqRatUp$ regularity tails, and $\RealUp$ seal reads. The lattice row is the displayed finite packet
$$
F=(S,D,R,B,Q,E,H,C,P,N),
$$
with StreamName, DyadicRatCore, RegSeqRat, and RealUp appearing only as the four sibling faces referenced by \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_real\_completion\_lattice\_link}
\begin{proof}
Start with tail-window exactness and the Real seal handoff, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-real-seal-handoff}. They identify $S,D,R,B,Q,E$ as one finite cofinal-tail packet consumed by the window, tolerance, regular-readback, budget, comparison, and Real-seal rows.

Next apply the shared-window meet, \autoref{thm:finite-tail-filter-shared-window-meet}. The meet forces all accepted consumers to use the same cofinal window and dyadic tolerance packet rather than separate StreamName, DyadicRatCore, RegSeqRat, or RealUp witnesses.

Finally apply the Lean-ready obligation surface, \autoref{thm:finite-tail-filter-lean-ready-obligation-surface}. It records that every Real-completion route read of the four sibling chapters must be reconstructed from $S,D,R,B,Q,E,H,C,P,N$, the terminal classifier exactness row, and the non-escape ledger, so the cofinal-tail lattice row is shared across all four objects.
\end{proof}

\begin{theorem}[FiniteTailFilter refinement seal witness-ledger pullback]
\label{thm:finite-tail-filter-refinement-seal-witness-ledger-pullback}
Let an accepted $\FiniteTailFilterUp$ packet
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
expose the cofinal-tail row consumed by compatible accepted $\CauchyModulusRefinementUp$, $\CauchyWitnessLedgerUp$, and $\CauchyLimitSealUp$ packets. Then the three consumers determine one shared finite route through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. Each consumer reads the same $S,D,R,B,Q,E,H,C,P,N$ rows as its StreamName finite-window row, DyadicRatCore tolerance row, RegSeqRat readback row, Real seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ coordinate.

No quotient stream, selected limit, host real equality, choice row, or ambient completeness coordinate is available along the pullback.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_refinement\_seal\_witness\_ledger\_pullback}
\begin{proof}
First combine the local Real-completion lattice link, \autoref{thm:finite-tail-filter-real-completion-lattice-link}, with the shared-window meet, \autoref{thm:finite-tail-filter-shared-window-meet}. These two rows put the four sibling faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ on the same finite packet $F$ before any downstream consumer reads the route.

For the refinement consumer, use the CauchyModulusRefinement seal and selector rows: \autoref{thm:cauchy-modulus-refinement-real-seal-route}, \autoref{thm:cauchy-modulus-refinement-real-seal-scope-package}, and \autoref{thm:cauchy-modulus-refinement-selector-spine-terminal-pullback}. For the witness-ledger consumer, use \autoref{thm:cauchy-witness-ledger-completion-consumer-factorization}, \autoref{thm:cauchy-witness-ledger-root-streamname-pullback}, and \autoref{thm:cauchy-witness-ledger-root-real-completion-nonescape}.

Together these rows say that refinement and ledger reads enter through the displayed finite route rather than through a detached selector or completion witness.

Finally apply the CauchyLimitSeal RegSeqRat-window boundary, \autoref{thm:cauchy-limit-seal-regseqrat-window-nonescape}. It keeps the limit-seal consumer inside the same RegSeqRat and StreamName window route already fixed by $F$. Since all three consumers factor through the same finite packet and each cited non-escape row excludes quotient streams, selected limits, host equality, choice data, and ambient completeness, the displayed $S,D,R,B,Q,E,H,C,P,N$ rows form the shared pullback route.
\end{proof}

\begin{theorem}[FiniteTailFilter exact-boundary terminal normal form]
\label{thm:finite-tail-filter-exact-boundary-terminal-normal-form}
Let an accepted $\FiniteTailFilterUp$ packet
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
export its cofinal-tail route to an accepted $\RealCompletionExactBoundaryUp$ packet whose exact-boundary consumer reads that route. Then the composite terminal read is absorbed by the four-face terminal normal form
$$
\StreamNameUp,\quad \RegSeqRatUp,\quad \DyadicRatCoreUp,\quad \RealUp.
$$
The composite exports no quotient stream equality, selected limit, host real equality, countable-choice row, ambient-completeness row, metric-completeness row, bridge row, or hidden tail selector.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterExactBoundaryTerminalNormalForm}
\begin{proof}
First apply the Real-completion lattice link, \autoref{thm:finite-tail-filter-real-completion-lattice-link}. It places the StreamName finite-window face, DyadicRatCore tolerance face, RegSeqRat regularity face, and terminal Real seal face on the same finite packet $F$ before the exact-boundary consumer reads them.

Next use the refinement seal witness-ledger pullback, \autoref{thm:finite-tail-filter-refinement-seal-witness-ledger-pullback}. The pullback says that downstream refinement, witness-ledger, and seal consumers still read the displayed $S,D,R,B,Q,E,H,C,P,N$ rows and do not acquire a detached selector, quotient equality, selected limit, host equality, choice row, or ambient completeness coordinate.

Finally invoke \autoref{thm:real-completion-exact-boundary-terminal-normal-form-absorption}. Once the accepted RealCompletionExactBoundary packet consumes the finite route, its terminal read is absorbed by the same four exact-boundary faces and structural rows. Combining the two finite-tail-filter rows with that absorption theorem leaves no carrier slot for metric completeness, a bridge row, or a hidden tail selector, so the composite terminal read is exactly the displayed four-face normal form.
\end{proof}

\begin{theorem}[FiniteTailFilter exact-boundary shared consumer determinacy]
\label{thm:finite-tail-filter-exact-boundary-shared-consumer-determinacy}
Let an accepted $\FiniteTailFilterUp$ packet
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
export its cofinal-tail route to two accepted $\RealCompletionExactBoundaryUp$ packets. Suppose their exact-boundary consumers read the same displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows through $F$. Then both consumers determine the same four-face terminal normal form through $S,D,R,E,H,C,P,N$:
$$
\StreamNameUp,\quad \RegSeqRatUp,\quad \DyadicRatCoreUp,\quad \RealUp.
$$
No quotient stream equality, selected limit, host real equality, countable-choice row, ambient-completeness row, metric-completeness row, bridge row, or hidden tail selector can distinguish the two terminal consumers.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_exact\_boundary\_shared\_consumer\_determinacy}
\begin{proof}
Apply the Real-completion lattice link, \autoref{thm:finite-tail-filter-real-completion-lattice-link}. It places the four L10 faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ on the same accepted finite packet $F$ before either exact-boundary consumer reads the route.

Next use the refinement seal witness-ledger pullback, \autoref{thm:finite-tail-filter-refinement-seal-witness-ledger-pullback}. The pullback keeps refinement, witness-ledger, and seal reads on the displayed $S,D,R,B,Q,E,H,C,P,N$ route, so neither consumer can introduce a detached selector, quotient stream, selected limit, host equality, choice row, or ambient completeness coordinate.

Now invoke the exact-boundary terminal normal form, \autoref{thm:finite-tail-filter-exact-boundary-terminal-normal-form}, for each consumer. Since both consumers read the same StreamName, DyadicRatCore, RegSeqRat, RealUp, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows, both terminal reads are absorbed into the same four-face normal form through the displayed finite route.

Finally use downstream exact-boundary determinacy, \autoref{thm:real-completion-exact-boundary-terminal-seal-determinacy} and \autoref{thm:real-completion-exact-boundary-cofinal-terminal-determinacy}, only to identify what the exact-boundary consumers can observe after absorption. Those determinacy rows leave no additional carrier coordinate for metric completeness, a bridge row, or a hidden tail selector, so the two accepted consumers cannot branch into distinguishable terminal outcomes.
\end{proof}

\begin{theorem}[FiniteTailFilter public exit package]
\label{thm:finite-tail-filter-public-exit-package}
The public L10 exit package for $\FiniteTailFilterUp$ is exactly the finite route
$$
S,D,R,B,Q,E,H,C,P,N
$$
consuming the sibling faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\RealWindowBudgetUp$, $\UniformCauchyCriterionUp$, $\CauchyTailComparisonUp$, and $\DyadicTailBoundUp$. The exit package has no quotient stream equality, selected limit, host real equality, countable-choice row, ambient completeness, metric-completeness datum, or bridge row.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterUp\_StdBridge}
\begin{proof}
First combine the carrier and NameCert obligations, \autoref{def:finite-tail-filter-carrier} and \autoref{thm:finite-tail-filter-namecert-obligations}. They expose the route $S,D,R,B,Q,E,H,C,P,N$ as the finite cofinal-tail packet available to public consumers.

Next use tail-window exactness and the Real seal handoff, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-real-seal-handoff}. These identify the StreamName, DyadicRatCore, RegSeqRat, RealWindowBudget, UniformCauchyCriterion, CauchyTailComparison, DyadicTailBound, and RealUp faces as reads of the same finite tail-window surface.

Apply terminal classifier exactness and the Lean-ready obligation surface, \autoref{thm:finite-tail-filter-terminal-classifier-exactness} and \autoref{thm:finite-tail-filter-lean-ready-obligation-surface}. The classifier and obligation rows leave no public coordinate for quotient stream equality, selected limits, host real equality, choice, ambient completeness, metric completeness, or bridge data.

Finally invoke \autoref{thm:finite-tail-filter-real-completion-lattice-link}. The four Real-completion faces already share one cofinal-tail lattice row, so the public L10 exit package is precisely the displayed finite route.
\end{proof}

\closureat{FiniteTailFilterUp}{scopedStr}
\closureat{FiniteTailFilterUp}{publicStr}
\closureat{FiniteTailFilterUp}{bridgedStr}
\closureat{FiniteTailFilterUp}{matureStr}

\begin{closurestatus}{\FiniteTailFilterUp}
  \constructivestory{A $\FiniteTailFilterUp$ packet is generated from a finite StreamName window ledger, DyadicRatCore tolerance ledger, RegSeqRat readback, DyadicTailBound tail-budget row, CauchyTailComparison common-tail row, RealUp seal, componentwise hsame transports, displayed Cont routes, Pkg provenance, and local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:finite-tail-filter-carrier}, \autoref{thm:finite-tail-filter-namecert-obligations}, \autoref{thm:finite-tail-filter-nonescape}, \autoref{thm:finite-tail-filter-tail-window-exactness}, \autoref{thm:finite-tail-filter-real-seal-handoff}, \autoref{thm:finite-tail-filter-sibling-tail-handoff}, \autoref{thm:finite-tail-filter-shared-window-meet}, \autoref{thm:finite-tail-filter-carrier-transport-obligation}, \autoref{thm:finite-tail-filter-cofinal-window-directedness-obligation}, \autoref{thm:finite-tail-filter-structural-provenance-obligation}, \autoref{thm:finite-tail-filter-terminal-cofinal-consumer-exclusion}, and \autoref{thm:finite-tail-filter-common-tail-seal-cofinality} bind the finite cofinal-tail filter packet, its classifier transport, its continuation replay, and its provenance/name certification surface for Real and Completion seal reads.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.FiniteTailFilterUp.FiniteTailFilterCarrier\_real\_completion\_lattice\_link}
  \bridgestatus{bridgeChecked}
  \origin{human}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, host real equality, ambient completeness, metric-completeness data, countable choice, or standard bridge rows.}
  \upgradepath{No further upgrade remains on the formal axis; the chapter is bridgeChecked for the finite cofinal-tail filter surface.}
\end{closurestatus}

\begin{theorem}[FiniteTailFilter classifier alignment boundary]
\label{thm:finite-tail-filter-classifier-alignment-boundary}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. Any classifier-aligned receiving gate, whether presented as a local classifier read, a fiberwise certificate, a pullback surface, or a reindexing surface, is admitted only after it re-reads the displayed rows of $F$.

The transported rows are exactly
$$
S,D,R,B,Q,E,C,P,N,
$$
with $H$ serving as the componentwise $\hsame$ transport witness for those rows. The classifier then evaluates on the displayed route
$$
S,D,R,B,Q,E,H,C,P,N.
$$
The rows forgotten at this boundary are precisely all non-carrier rows: private window selectors, detached dyadic ledgers, detached $\RegSeqRatUp$ readbacks, detached tail-budget rows, detached common-tail rows, detached $\RealUp$ seals, quotient stream equalities, selected limits, host real equalities, countable-choice rows, ambient or metric completeness data, independent completion rows, and standard bridge rows.

Consequently a copied source excerpt is never sufficient evidence for classifier alignment. The receiving gate must project the rows again from the accepted $\FiniteTailFilterUp$ packet and may classify only the componentwise route displayed by $F$.
\end{theorem}
\begin{proof}
Start with the carrier definition and the NameCert obligations, \autoref{def:finite-tail-filter-carrier} and \autoref{thm:finite-tail-filter-namecert-obligations}. They identify the accepted packet as the ten displayed rows $S,D,R,B,Q,E,H,C,P,N$. The analytic coordinates are $S,D,R,B,Q,E$, and the structural coordinates $H,C,P,N$ transport, replay, source, and name that same packet.

Apply the carrier transport obligation, \autoref{thm:finite-tail-filter-carrier-transport-obligation}. It states that the componentwise $\hsame$ row $H$ transports exactly $S,D,R,B,Q,E,C,P,N$. Thus $H$ is not a new analytic coordinate and not a hidden classifier input; it is the transport witness for the already displayed packet rows.

Use tail-window exactness and the Real seal handoff, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-real-seal-handoff}. These force the window row $S$, dyadic ledger $D$, regular readback $R$, tail budget $B$, common-tail row $Q$, and seal row $E$ to remain one route. A pullback or reindexing read therefore cannot replace one of these rows by a private selector, separate ledger, detached readback, detached budget, detached comparison row, or detached seal.

Now apply terminal classifier exactness, \autoref{thm:finite-tail-filter-terminal-classifier-exactness}. The terminal classifier available to $\RealWindowBudgetUp$, $\UniformCauchyCriterionUp$, $\RealUp$, and completion-facing consumers is exactly the componentwise classifier on $S,D,R,B,Q,E,H,C,P,N$. Hence a classifier-aligned receiving gate has no second classifier surface outside the displayed packet.

Finally use non-escape and structural provenance, \autoref{thm:finite-tail-filter-nonescape} and \autoref{thm:finite-tail-filter-structural-provenance-obligation}. The carrier and its provenance rows contain no quotient stream equality, selected limit, host real equality, countable-choice row, ambient completeness row, metric-completeness row, independent completion datum, or standard bridge schema. These are precisely the rows forgotten at the alignment boundary. Therefore the receiving gate must re-read the displayed rows from $F$ and may not classify from a copied excerpt or any off-packet coordinate.
\end{proof}

\begin{theorem}[FiniteTailFilter forgetful projection boundary]
\label{thm:finite-tail-filter-forgetful-projection-boundary}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. Suppose a consumer displays this packet with explicit sibling-face names for the $\StreamNameUp$ window row, the $\DyadicRatCoreUp$ tolerance row, the $\RegSeqRatUp$ readback row, the $\DyadicTailBoundUp$ budget row, the $\CauchyTailComparisonUp$ common-tail row, the $\RealUp$ seal row, and any terminal $\RealWindowBudgetUp$ or $\UniformCauchyCriterionUp$ read that consumes those rows. Then the BEDC-native forgetful projection of that richer display is exactly the target packet
$$
S,D,R,B,Q,E,H,C,P,N.
$$
After the projection, the target carrier is $F$, the terminal classifier is the componentwise classifier on these ten displayed rows, the ledger surface is carried by $H,C,P$, and the local naming coordinate is $N$. No projected read may drop one of $S,D,R,B,Q,E,H,C,P,N$, and no sibling-face name survives as an independent coordinate outside the target packet. In particular, the projection exports no private selector, detached dyadic ledger, quotient stream equality, selected limit, host real equality, countable-choice row, ambient completeness row, metric-completeness row, independent completion row, or standard bridge row.
\end{theorem}
\begin{proof}
Start with the carrier and NameCert obligations, \autoref{def:finite-tail-filter-carrier} and \autoref{thm:finite-tail-filter-namecert-obligations}. They present the accepted $\FiniteTailFilterUp$ packet as precisely the ten rows $S,D,R,B,Q,E,H,C,P,N$. The named sibling faces are not additional coordinates of the carrier: the window face is $S$, the dyadic face is $D$, the regular-rational readback is $R$, the tail-budget face is $B$, the common-tail face is $Q$, and the Real seal face is $E$.

Next apply tail-window exactness and cofinal-window directedness, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-cofinal-window-directedness-obligation}. These show that all accepted budget, comparison, seal, and continuation reads replay the same finite cofinal packet. Thus forgetting the external face names cannot change the rows being read; it only removes the labels by which sibling chapters address those rows.

The terminal classifier is fixed by \autoref{thm:finite-tail-filter-terminal-classifier-exactness}. Every terminal Real-window, UniformCauchyCriterion, Real, or completion-facing consumer classifies the packet componentwise on $S,D,R,B,Q,E,H,C,P,N$. Therefore a projection that dropped one of these coordinates would no longer preserve the classifier surface available to the terminal consumer.

The ledger and naming coordinates are fixed by \autoref{thm:finite-tail-filter-carrier-transport-obligation}, \autoref{thm:finite-tail-filter-structural-provenance-obligation}, and \autoref{thm:finite-tail-filter-public-exit-package}. The transport row $H$, continuation row $C$, package row $P$, and local NameCert row $N$ are the only ledger and naming data certified for the public exit package. Hence the forgetful projection retains them as target coordinates rather than replacing them by a sibling provenance label.

Finally apply non-escape and terminal cofinal-consumer exclusion, \autoref{thm:finite-tail-filter-nonescape} and \autoref{thm:finite-tail-filter-terminal-cofinal-consumer-exclusion}. The accepted carrier has no coordinate for private selectors, detached dyadic ledgers, quotient stream equality, selected limits, host real equality, choice, ambient or metric completeness, independent completion data, or standard bridge rows. Any richer display retaining one of those objects after forgetting sibling-face names would therefore retain a coordinate that was never present in $F$. The only possible BEDC-native projection is the displayed target packet $S,D,R,B,Q,E,H,C,P,N$.
\end{proof}

\begin{theorem}[FiniteTailFilter substrate bisimulation boundary]
\label{thm:finite-tail-filter-substrate-bisimulation-boundary}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. Any receiving gate that presents $F$ as a $\mathsf{Rule110}$, cellular, binary, $\mathsf{FKernel}$, or $\BHist$ substrate read is admitted only as a relation certificate over the displayed rows of $F$, not as definitional equality with a host substrate.

The source of the certificate is the accepted carrier $F$. The target is the consumer-visible route
$$
S,D,R,B,Q,E,H,C,P,N.
$$
The direction is source-to-target: the gate first projects the intermediate route rows
$$
S,D,R,B,Q,
$$
then reaches the seal row $E$, and keeps the noneliminable structural boundary rows
$$
H,C,P,N.
$$
On the analytic rows $S,D,R,B,Q,E$ the certificate is a row embedding into the target route. With the componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming row $N$ retained, the certificate is a substrate mirror and hence a bisimulation boundary for the displayed finite packet.

No receiving gate may replace this relation certificate by a copied source excerpt, a host coordinate, an external source coordinate, a private window selector, a detached dyadic ledger, a detached regular readback, a detached tail-budget row, a detached common-tail row, a detached $\RealUp$ seal, quotient stream equality, selected limits, host real equality, countable-choice data, ambient completeness, metric-completeness data, independent completion data, or a standard bridge row.
\end{theorem}
\begin{proof}
Start with the carrier definition and the NameCert obligations, \autoref{def:finite-tail-filter-carrier} and \autoref{thm:finite-tail-filter-namecert-obligations}. They identify the accepted packet as exactly the ten displayed rows $S,D,R,B,Q,E,H,C,P,N$. Thus a receiving gate has a local source only when it binds this carrier, and it has no local source coordinate outside the displayed packet.

Apply tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}. The rows $S$ and $D$ are the only window and dyadic tolerance rows, and the rows $B,Q,E$ read that same window and tolerance packet. Therefore the intermediate analytic route is the listed route $S,D,R,B,Q$ followed by the seal row $E$; it cannot be replaced by a private selector, separate tolerance ledger, detached readback, detached budget, detached comparison row, or detached seal.

Use the Real seal handoff and the cofinal-route exhaustion, \autoref{thm:finite-tail-filter-real-seal-handoff} and \autoref{thm:finite-tail-filter-cofinal-route-exhaustion}. These route every Real, Real-window, and completion-facing read through the same finite cofinal packet $S,D,R,B,Q,E,H,C,P,N$. Hence a substrate presentation of the packet can only point from the accepted carrier to this consumer-visible route.

Now apply the carrier transport, continuation, and provenance obligations, \autoref{thm:finite-tail-filter-carrier-transport-obligation}, \autoref{thm:finite-tail-filter-cofinal-window-directedness-obligation}, and \autoref{thm:finite-tail-filter-structural-provenance-obligation}. The row $H$ transports the displayed rows componentwise, the row $C$ replays the displayed read route, and the rows $P,N$ record provenance and naming for that same packet. These structural rows are not eliminable when the gate claims a substrate relation, because they are exactly what records source, target, and direction.

Finally invoke terminal classifier exactness and classifier alignment, \autoref{thm:finite-tail-filter-terminal-classifier-exactness} and \autoref{thm:finite-tail-filter-classifier-alignment-boundary}. The terminal classifier evaluates only on the componentwise route $S,D,R,B,Q,E,H,C,P,N$, and a copied source excerpt is not sufficient evidence for alignment. Together with non-escape, \autoref{thm:finite-tail-filter-nonescape}, this rules out every unlisted host or external coordinate. The only admissible substrate claim is therefore the stated source-to-target row embedding with $H,C,P,N$ retained as the substrate mirror, which is precisely the bisimulation boundary for the displayed finite packet.
\end{proof}

\begin{theorem}[FiniteTailFilter left-right mirror boundary]
\label{thm:finite-tail-filter-left-right-mirror-boundary}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. Any left-oriented or right-oriented mirror read of the finite tail-filter packet has the same BEDC-native boundary: its displayed history row is $S$, its observation and tolerance rows are $D,R,B,Q$, its result row is the $\RealUp$ seal $E$, its transport and termination rows are $H,C$, and its certificate rows are $P,N$.

Consequently the right-oriented counterpart is admitted only by re-reading the same packet $S,D,R,B,Q,E,H,C,P,N$ with the endpoint words exchanged. It is not a second carrier, not a second theorem-label surface, and not a new dual row. In particular, a right-oriented read cannot introduce a private window selector, detached dyadic ledger, detached regular readback, detached tail-budget row, detached common-tail row, detached $\RealUp$ seal, quotient stream equality, selected limit, host real equality, countable-choice row, ambient completeness row, metric-completeness row, independent completion row, or standard bridge row.
\end{theorem}
\begin{proof}
Start from the carrier and NameCert obligations, \autoref{def:finite-tail-filter-carrier} and \autoref{thm:finite-tail-filter-namecert-obligations}. They expose the accepted packet as exactly the ten rows $S,D,R,B,Q,E,H,C,P,N$. Thus any orientation of a mirror read has only these rows available: $S$ supplies the finite window history, $D,R,B,Q$ supply the dyadic, regular-readback, tail-budget, and common-tail observation surface, $E$ supplies the seal-facing result, and $H,C,P,N$ supply transport, replay, provenance, and local naming.

Apply tail-window exactness and cofinal-window directedness, \autoref{thm:finite-tail-filter-tail-window-exactness} and \autoref{thm:finite-tail-filter-cofinal-window-directedness-obligation}. The rows $B,Q,E$ all consume the same displayed $S,D$ window and tolerance packet, and the continuation row $C$ replays every accepted read through that packet. Exchanging left and right endpoint words therefore cannot create a new selector, tolerance ledger, readback row, budget row, comparison row, or seal row; it can only re-read the same directed finite route.

Use the carrier transport obligation and structural provenance obligation, \autoref{thm:finite-tail-filter-carrier-transport-obligation} and \autoref{thm:finite-tail-filter-structural-provenance-obligation}. The transport row $H$ is componentwise over $S,D,R,B,Q,E,C,P,N$, while $P$ and $N$ certify the same sibling and kernel provenance. Hence the rows that terminate and certify a left-oriented read are already the rows that terminate and certify the right-oriented read.

Now apply the substrate bisimulation boundary, \autoref{thm:finite-tail-filter-substrate-bisimulation-boundary}. It identifies the only admissible substrate mirror as the source-to-target row embedding over $S,D,R,B,Q,E,H,C,P,N$, with the structural rows retained. Since that boundary is already the whole displayed packet, the mirror counterpart has no independent right-side carrier or extra theorem-label surface to occupy.

Finally use classifier alignment, terminal classifier exactness, and non-escape, \autoref{thm:finite-tail-filter-classifier-alignment-boundary}, \autoref{thm:finite-tail-filter-terminal-classifier-exactness}, and \autoref{thm:finite-tail-filter-nonescape}. The receiving classifier must re-read the rows from $F$, evaluates only on the componentwise route $S,D,R,B,Q,E,H,C,P,N$, and has no coordinate for any excluded external structure. Therefore the left-right mirror closes only as the diagonal finite-tail packet boundary stated above.
\end{proof}

\begin{theorem}[FiniteTailFilter domain-codomain mirror boundary]
\label{thm:finite-tail-filter-domain-codomain-mirror-boundary}
Let
$$
F=(S,D,R,B,Q,E,H,C,P,N)
$$
be an accepted $\FiniteTailFilterUp$ carrier. Any domain-oriented read of $F$ and any codomain-oriented mirror read of $F$ have the same BEDC-native boundary exactly when both reads project to the displayed packet rows
$$
S,D,R,B,Q,E,H,C,P,N.
$$
On this boundary, the domain history is the finite window ledger $S$, the domain observation and tolerance surface is $D,R,B,Q$, the codomain result is the $\RealUp$ seal $E$, the termination and replay data are $H,C$, and the certificate data are $P,N$.

Consequently the codomain side is not a second carrier, not a second theorem-label surface, and not a hidden dual packet. It is admitted only by re-reading the same history, observation, result, termination, and certificate rows already displayed by $F$. If a receiving gate asks for a codomain row outside this list, the request is recorded as a boundary failure: no private selector, detached dyadic ledger, detached $\RegSeqRatUp$ readback, detached tail-budget row, detached common-tail row, detached $\RealUp$ seal, quotient stream equality, selected limit, host real equality, countable-choice row, ambient completeness row, metric-completeness row, independent completion row, or standard bridge row is available from the accepted packet.
\end{theorem}
\begin{proof}
Start from the carrier definition and the NameCert obligations, \autoref{def:finite-tail-filter-carrier} and \autoref{thm:finite-tail-filter-namecert-obligations}. They expose the accepted $\FiniteTailFilterUp$ packet as exactly the rows $S,D,R,B,Q,E,H,C,P,N$. Hence a domain-oriented read has no source coordinate except the displayed finite window, dyadic, regular-readback, tail-budget, common-tail, seal, transport, replay, provenance, and naming rows.

Apply tail-window exactness, \autoref{thm:finite-tail-filter-tail-window-exactness}. The rows $B,Q,E$ consume the same $S,D$ window and tolerance packet, so a codomain-oriented read cannot obtain its result row by replacing the domain window, splitting the tail budget from the common-tail comparison, or substituting a detached tolerance ledger. The codomain result is the displayed seal row $E$ reached through $S,D,R,B,Q$.

Use the Real seal handoff and cofinal-route exhaustion, \autoref{thm:finite-tail-filter-real-seal-handoff} and \autoref{thm:finite-tail-filter-cofinal-route-exhaustion}. They route every Real, Real-window, and completion-facing read through the same finite packet before the seal is consumed. Thus exchanging the words domain and codomain changes only the orientation of the read; it does not create an additional row on either side.

The termination and certificate rows are fixed by the carrier transport, cofinal-window directedness, and structural provenance obligations, \autoref{thm:finite-tail-filter-carrier-transport-obligation}, \autoref{thm:finite-tail-filter-cofinal-window-directedness-obligation}, and \autoref{thm:finite-tail-filter-structural-provenance-obligation}. The row $H$ transports the displayed rows componentwise, $C$ replays the accepted route, and $P,N$ certify provenance and local naming for the same packet. These rows are therefore shared by the domain and codomain presentations rather than duplicated on a codomain side.

Finally apply classifier alignment, substrate bisimulation, and the left-right mirror boundary, \autoref{thm:finite-tail-filter-classifier-alignment-boundary}, \autoref{thm:finite-tail-filter-substrate-bisimulation-boundary}, and \autoref{thm:finite-tail-filter-left-right-mirror-boundary}. The classifier must re-read the rows from $F$, the only admissible substrate certificate is the row embedding over $S,D,R,B,Q,E,H,C,P,N$, and the mirror read closes only as the diagonal finite-tail packet boundary. Together with non-escape, \autoref{thm:finite-tail-filter-nonescape}, this excludes every off-packet codomain coordinate listed above. Hence the codomain mirror is exactly the same displayed packet, and any stronger codomain request is stopped at the stated boundary.
\end{proof}
