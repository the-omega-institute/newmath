\chapter{A Concrete Naming Certificate for $\RegularCauchyNameSeparationUp$}
\label{ch:concrete-instances-regular-cauchy-name-separation-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyNameSeparationUp$ packet records the finite evidence that separates two regular Cauchy real names before any quotient representative, located real equality, or completeness theorem is read. It sits between the dyadic arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the located comparison surface of \autoref{ch:concrete-instances-located-real-namecert}. The packet names a constructive separation boundary for regular Cauchy names; it does not assert quotient representatives, apartness completeness, trichotomy, choice of a least separating index, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy name-separation carrier]
\label{def:regular-cauchy-name-separation-carrier}
A $\RegularCauchyNameSeparationUp$ carrier is a finite $\BHist$ packet
$$
S=(D,W,R,E,L,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance and comparison ledger used to express a positive rational separation.
\item $W$ is the paired $\StreamNameUp$ finite-window row, recording exactly which prefixes of the two names are inspected.
\item $R$ is the paired $\RegSeqRatUp$ readback row obtained from the displayed windows and dyadic tolerances.
\item $E$ is the terminal $\RealUp$ name-seal row, exposed only after $D,W,R$ have all been displayed.
\item $L$ is the $\LocatedRealUp$ comparison row that records the located side on which the separation is consumed.
\item $B$ is the separation-boundary row, listing the finite index, dyadic tolerance, and sided comparison used by the accepted read.
\item $H$ records componentwise $\hsame$ transport for dyadic comparisons, stream windows, regular readbacks, Real seals, located comparison rows, boundary rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a separation request through $D,W,R,E,L,B,H$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyNameSeparationUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyNameSeparationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,W,R,E,L,B,H,C,P,N$. It has no coordinate for quotient stream equality, selected representatives, a least separating index, trichotomy, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy name-separation NameCert obligations]
\label{thm:regular-cauchy-name-separation-namecert-obligations}
For every accepted $\RegularCauchyNameSeparationUp$ carrier
$$
S=(D,W,R,E,L,B,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyNameSeparationUp}$ surface consists exactly of carrier admission, dyadic tolerance visibility, paired finite-window visibility, regular rational readback, Real-name sealing, located comparison exposure, separation-boundary replay, transport, provenance, and local naming. No obligation is stored outside those displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-name-separation-carrier}. The rows $D,W,R,E,L,B,H,C,P,N$ are the whole accepted packet.

The dyadic tolerance is visible in $D$, the inspected prefixes are visible in $W$, and the regular rational readback is visible in $R$. The Real-name row $E$ and the located comparison row $L$ are downstream of those finite rows, while $B$ stores the finite boundary at which the separation is consumed.

Transport, replay, provenance, and local naming are exactly $H,C,P,N$. Since the carrier has no field for quotient stream equality, selected representatives, least-index choice, trichotomy, host equality, or ambient completeness, no further NameCert obligation can be projected.
\end{proof}

\begin{theorem}[Regular Cauchy name-separation classifier stability]
\label{thm:regular-cauchy-name-separation-classifier-stability}
If two accepted $\RegularCauchyNameSeparationUp$ packets agree componentwise on $D,W,R,E,L,B,H,C,P,N$, then every accepted separation read exposes the same finite stream windows, dyadic tolerance, regular readback, Real seal, located comparison row, and local naming row. In particular, classifier equality is stable under componentwise $\hsame$ transport and cannot be changed by a hidden quotient representative or by an external choice of separating index.
\end{theorem}
\begin{proof}
Use the displayed classifier of \autoref{def:regular-cauchy-name-separation-carrier}. It reads only $D,W,R,E,L,B,H,C,P,N$.

Componentwise agreement preserves the dyadic ledger, paired stream windows, regular readback, Real seal, located comparison, and boundary row. The transport and replay rows then carry the same displayed route to provenance and local naming.

Because no other classifier coordinate exists, quotient representatives, hidden least-index choices, and ambient Real-completeness facts cannot alter the accepted read.
\end{proof}

\begin{theorem}[Regular Cauchy name-separation ledger exactness]
\label{thm:regular-cauchy-name-separation-ledger-exactness}
Every accepted $\RegularCauchyNameSeparationUp$ ledger row is exact for the finite route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \LocatedRealUp.
$$
The route records dyadic tolerance, paired finite inspection, regular rational readback, terminal Real sealing, located comparison exposure, transport, replay, provenance, and local naming; it does not record quotient equality, a chosen representative, a least separating index, trichotomy, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Apply the NameCert obligation theorem, \autoref{thm:regular-cauchy-name-separation-namecert-obligations}. It identifies all rows available to an accepted ledger read.

The analytic rows occur in the order $D,W,R,E,L,B$. Dyadic tolerance and finite windows are read before regular readback, regular readback is read before the Real seal, and the located comparison row is exposed before the boundary row is consumed.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the refused coordinates are absent from the carrier, the ledger is exact for the displayed route and for nothing outside it.
\end{proof}

\begin{theorem}[Regular Cauchy name-separation public boundary]
\label{thm:regular-cauchy-name-separation-public-boundary}
A public consumer of an accepted $\RegularCauchyNameSeparationUp$ packet may read only the finite separation boundary exposed by $D,W,R,E,L,B,H,C,P,N$. It may use the packet as a constructive separator for regular Cauchy real names, but it may not infer quotient representatives, located-real equality completeness, trichotomy, choice of a least separating index, or ambient $\RealUp$ completeness from the packet alone.
\end{theorem}
\begin{proof}
Start with ledger exactness, \autoref{thm:regular-cauchy-name-separation-ledger-exactness}. It confines public reads to the dyadic, stream, regular-readback, Real-seal, located-comparison, boundary, transport, replay, provenance, and local naming route.

Use classifier stability, \autoref{thm:regular-cauchy-name-separation-classifier-stability}. Any componentwise transport preserves that same public route, so a consumer cannot change the separation by choosing a hidden representative or a hidden index.

Finally inspect the carrier. The absent rows are exactly quotient representatives, located-real equality completeness, trichotomy, least-index choice, countable choice, host equality, and ambient Real completeness. Public consumption therefore stops at the displayed finite boundary.
\end{proof}

\closureat{RegularCauchyNameSeparationUp}{seedStr}

\begin{closurestatus}{\RegularCauchyNameSeparationUp}
  \constructivestory{A $\RegularCauchyNameSeparationUp$ packet is generated from dyadic tolerance rows, paired StreamName windows, RegSeqRat readback rows, Real seal rows, LocatedReal comparison rows, a finite separation-boundary row, componentwise transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-name-separation-carrier}, \autoref{thm:regular-cauchy-name-separation-namecert-obligations}, \autoref{thm:regular-cauchy-name-separation-classifier-stability}, \autoref{thm:regular-cauchy-name-separation-ledger-exactness}, and \autoref{thm:regular-cauchy-name-separation-public-boundary} seed the regular Cauchy name-separation boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient representatives, located-real equality completeness, trichotomy, least separating-index choice, countable choice, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, ledger exactness, and public-boundary non-escape for BEDC.Derived.RegularCauchyNameSeparationUp.}
\end{closurestatus}
