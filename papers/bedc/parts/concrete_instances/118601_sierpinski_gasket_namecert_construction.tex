\chapter{A Concrete Naming Certificate for $\SierpinskiGasketUp$}
\label{ch:concrete-instances-sierpinski-gasket-namecert}
\origin{human}

Following Sierpinski and Hutchinson-style iterated function systems, the $\SierpinskiGasketUp$ packet records the triangular three-map self-similar gasket as finite BEDC naming data. It sits after the finite contraction-family surface of \autoref{ch:concrete-instances-iterated-function-system-namecert}, beside the stagewise fractal routes of \autoref{ch:concrete-instances-koch-curve-namecert} and \autoref{ch:concrete-instances-koch-snowflake-namecert}, and before any consumer asks for a completed attractor subset. The packet names only triangular vertex data, three displayed contraction rows, finite address windows, stage-cell readback, dyadic coordinate seals, transport, replay, provenance, and local naming; it does not import an ambient planar compact set, a hyperspace fixed point, Hausdorff dimension, area measure, selected infinite branch, or host equality of points.

\begin{definition}[Sierpinski gasket carrier]
\label{def:sierpinski-gasket-carrier}
A $\SierpinskiGasketUp$ carrier is a finite $\BHist$ packet
$$
G=(T,I,F,A,S,W,D,R,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $T$ is the triangular vertex row, recording the three displayed corner seals and the orientation used by stage cells.
\item $I$ is the $\IteratedFunctionSystemUp$ dependency row. It supplies the compact complete metric source, finite map family, contraction ledger, modulus thresholds, and Real-facing constants inherited from \autoref{ch:concrete-instances-iterated-function-system-namecert}.
\item $F$ is the three-map contraction-family row, addressed by the finite indices $0,1,2$ and read only through the IFS packet $I$.
\item $A$ is the finite address row for inspected words over the three symbols, with no selected infinite branch.
\item $S$ is the stage-cell row. It records the displayed triangular subcell reached by an address word and the three endpoint rows carried by that cell.
\item $W$ is the finite $\StreamNameUp$ window family for the inspected address, stage, and endpoint-coordinate reads.
\item $D$ is the $\DyadicRatCoreUp$ stage and midpoint ledger used to route the half-scale triangular contractions and coordinate tolerances.
\item $R$ is the $\RegSeqRatUp$ readback row for the scheduled coordinate windows.
\item $E$ is the terminal $\RealUp$ coordinate-seal family for the displayed vertices and stage-cell endpoints.
\item $H$ records componentwise $\hsame$ transport for $T,I,F,A,S,W,D,R,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a gasket request through the finite address, contraction-family, stage-cell, coordinate, and structural rows.
\item $P$ is the $\Pkg$ provenance over $\SierpinskiGasketUp$, $\IteratedFunctionSystemUp$, $\KochCurveUp$, $\KochSnowflakeUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SierpinskiGasketUp}$ row.
\end{enumerate}
The classifier accepts a packet only through the displayed tuple $T,I,F,A,S,W,D,R,E,H,C,P,N$. It has no coordinate for a completed attractor subset, hyperspace quotient, Hausdorff dimension, area computation, selected infinite address, or ambient compactness theorem.
\end{definition}

\begin{theorem}[Sierpinski gasket NameCert obligations]
\label{thm:sierpinski-gasket-namecert-obligations}
For every accepted $\SierpinskiGasketUp$ carrier
$$
G=(T,I,F,A,S,W,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\SierpinskiGasketUp}$ surface consists exactly of carrier admission for the displayed packet; triangular vertex exposure through $T$; finite IFS exposure through $I$; three contraction rows through $F$; finite address exposure through $A$; stage-cell exactness through $S$; finite window scheduling through $W$; dyadic stage and midpoint exactness through $D$; regular rational readback through $R$; terminal $\RealUp$ coordinate sealing through $E$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sierpinski-gasket-carrier}. The visible rows are exactly $T,I,F,A,S,W,D,R,E,H,C,P,N$, and the classifier compares only those rows by componentwise $\hsame$.

The IFS-facing obligations are exhausted by $I$ and $F$. The dependency row $I$ supplies the finite contraction-family interface named in \autoref{ch:concrete-instances-iterated-function-system-namecert}, while $F$ fixes the three displayed half-scale maps by finite index.

The gasket-specific readback is exhausted by $A,S,W,D,R,E$. A consumer first reads a finite address word, then the corresponding displayed stage cell, then the scheduled coordinate windows and dyadic midpoint ledger before the regular rational and Real-seal rows are inspected.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier contains no completed attractor subset, hyperspace quotient, dimension row, area row, selected infinite branch, or ambient compactness row, no such object can become a local $\NameCert_{\SierpinskiGasketUp}$ obligation.
\end{proof}

\begin{theorem}[Sierpinski gasket finite IFS route]
\label{thm:sierpinski-gasket-ifs-finite-route}
Every accepted stage-cell, address, or coordinate read of a $\SierpinskiGasketUp$ packet factors through the finite route
$$
T \longmapsto I,F \longmapsto A,S \longmapsto W,D,R,E \longmapsto H,C,P,N .
$$
The route exports only the triangular vertex row, finite IFS dependency, three contraction rows, finite address and stage-cell rows, StreamName windows, dyadic midpoint ledger, regular rational readback, terminal Real seals, and the structural rows. It exports no completed planar fractal, host attractor subset, hyperspace fixed point, selected infinite branch, area computation, Hausdorff dimension, or ambient compactness principle.
\end{theorem}
\begin{proof}
Begin with an accepted consumer of a gasket stage-cell, address, or coordinate read. By \autoref{def:sierpinski-gasket-carrier}, the consumer can enter the packet only through the triangular vertex row $T$, the IFS row $I$, and the three-map row $F$.

Apply \autoref{thm:sierpinski-gasket-namecert-obligations}. It forces every finite address read through $A$, every stage-cell read through $S$, and every coordinate read through $W,D,R,E$ before the structural rows are inspected.

The IFS packet of \autoref{ch:concrete-instances-iterated-function-system-namecert} supplies only a finite contraction-family route, and the nearby Koch packets supply only displayed stagewise readbacks. None adds a carrier coordinate for a completed attractor, hyperspace fixed point, selected infinite branch, dimension, area, or compactness theorem. Hence every accepted consumer remains on the finite IFS route.
\end{proof}

\begin{theorem}[Sierpinski gasket nonescape]
\label{thm:sierpinski-gasket-nonescape}
An accepted $\SierpinskiGasketUp$ consumer cannot reach a Real coordinate seal for a stage-cell endpoint without first displaying $T,I,F,A,S,W,D,R,E$. In particular, any coordinate read that skips the finite address row, the three-map IFS row, the stage-cell row, or the dyadic midpoint ledger is outside the local NameCert surface.
\end{theorem}
\begin{proof}
Read the accepted carrier through \autoref{def:sierpinski-gasket-carrier}. The terminal coordinate seals occur only in the row $E$, and $E$ is downstream of the displayed stage and coordinate rows $A,S,W,D,R$.

Use \autoref{thm:sierpinski-gasket-ifs-finite-route}. The route places $T,I,F,A,S,W,D,R$ before $E$, so a Real coordinate seal has no independent entry point.

The structural rows $H,C,P,N$ can only transport, replay, source, and name the same packet. They do not create a second coordinate-seal route, and the carrier has no host planar subset or selected attractor row that could bypass the finite address and stage-cell data.
\end{proof}

\falsifiablePrediction{An accepted $\SierpinskiGasketUp$ consumer that reaches a $\RealUp$ coordinate seal without first displaying the triangular vertex row, IFS row, three contraction rows, finite address row, stage-cell row, StreamName windows, dyadic midpoint ledger, and RegSeqRat readback refutes the carrier surface.}

\independenceWitness{iteratedfunctionsystem, kochcurve, kochsnowflake, streamname, dyadicratcore, regseqrat, real: no listed sibling supplies the whole $\SierpinskiGasketUp$ packet, because the gasket certificate binds triangular vertices, three half-scale contraction rows, finite address and stage-cell data, coordinate readback, Real seals, transport, replay, provenance, and local naming.}

\closureat{SierpinskiGasketUp}{seedStr}

\begin{closurestatus}{\SierpinskiGasketUp}
  \constructivestory{A $\SierpinskiGasketUp$ packet is generated from triangular vertex data, an IteratedFunctionSystem dependency row, three contraction rows, finite address and stage-cell rows, StreamName windows, dyadic midpoint ledgers, RegSeqRat readbacks, RealUp coordinate seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sierpinski-gasket-carrier}, \autoref{thm:sierpinski-gasket-namecert-obligations}, \autoref{thm:sierpinski-gasket-ifs-finite-route}, and \autoref{thm:sierpinski-gasket-nonescape} seed the finite triangular IFS gasket carrier and its displayed coordinate nonescape route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host attractor subsets, hyperspace completion, Hausdorff-metric fixed-set existence, invariant measures, area or dimension computation, selected infinite branches, Lean theorem coverage, or bridge checking.}
  \upgradepath{Obligation closure requires a Lean-side SierpinskiGasketUp carrier with checked triangular vertex exposure, three contraction rows, finite address and stage-cell exactness, coordinate readback, Real coordinate sealing, transport, replay, provenance, and local naming.}
\end{closurestatus}
