\chapter{A Concrete Naming Certificate for $\CauchyKovalevskayaUp$}
\label{ch:concrete-instances-cauchy-kovalevskaya-namecert}
\origin{human}

Following Cauchy and Kovalevskaya on analytic initial-value problems, the $\CauchyKovalevskayaUp$ packet records the finite analytic-jet surface by which a displayed PDE coefficient ledger, initial hypersurface jet, derivative ledger, stream schedule, rational coefficient readback, dyadic coefficient core, and real seal are named before any local solution claim is public. It sits next to the PDE carrier of \autoref{ch:concrete-instances-pde-namecert}, the derivative rows of \autoref{ch:concrete-instances-derivative-namecert}, the continuous-map boundary of \autoref{ch:concrete-instances-continuousmap-namecert}, the stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic coefficient core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names finite analytic data for a constructive CK read; it is not analytic continuation, a selected solution germ, quotient equality of functions, countable choice over coefficients, or ambient real-completeness closure.

\begin{definition}[Cauchy--Kovalevskaya carrier]
\label{def:cauchy-kovalevskaya-carrier}
A $\CauchyKovalevskayaUp$ carrier is a finite $\BHist$ packet
$$
K=(P,D,C,I,J,S,Q,Y,R,H,T,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\PdeUp$ row naming the inspected analytic PDE operator and the distinguished evolution variable.
\item $D$ is the $\DerivativeUp$ ledger for the partial derivative rows requested by the operator.
\item $C$ is the $\ContinuousMapUp$ coefficient-boundary row for the displayed analytic coefficient maps.
\item $I$ is the initial hypersurface row, including the finite analytic initial data read by the packet.
\item $J$ is the finite jet recursion row, recording the coefficient-by-coefficient recursion available from $P,D,C,I$.
\item $S$ is the $\StreamNameUp$ row scheduling the inspected coefficient windows and finite jet depths.
\item $Q$ is the $\RegSeqRatUp$ readback row for rational coefficient bounds and convergence radii visible at those windows.
\item $Y$ is the $\DyadicRatCoreUp$ row storing the dyadic coefficient ledger used by the rational readback.
\item $R$ is the $\RealUp$ seal for the displayed real coefficient and radius reads.
\item $H$ records componentwise $\hsame$ transport for PDE, derivative, continuous-map, initial-data, jet, stream, rational, dyadic, real, replay, provenance, and local-name rows.
\item $T$ records displayed $\Cont$ replay from a finite analytic-jet request through $P,D,C,I,J,S,Q,Y,R$.
\item $G$ is the $\Pkg$ provenance over $\CauchyKovalevskayaUp$, $\PdeUp$, $\DerivativeUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyKovalevskayaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $P,D,C,I,J,S,Q,Y,R,H,T,G,N$. It has no coordinate for a host analytic solution, analytic continuation past the displayed radius, selected germ, quotient equality of functions, countable coefficient choice, or ambient $\RealUp$ completeness.
\end{definition}
\leandef{BEDC.Derived.CauchyKovalevskayaUp.CauchyKovalevskayaCarrier}

\begin{theorem}[Cauchy--Kovalevskaya NameCert obligations]
\label{thm:cauchy-kovalevskaya-namecert-obligations}
For every accepted $\CauchyKovalevskayaUp$ carrier $K=(P,D,C,I,J,S,Q,Y,R,H,T,G,N)$, the local $\NameCert_{\CauchyKovalevskayaUp}$ surface consists exactly of carrier admission for the PDE row, derivative ledger, continuous coefficient boundary, initial hypersurface data, finite jet recursion, stream schedule, regular rational readback, dyadic coefficient ledger, real seal, componentwise transport, displayed replay, provenance, and local naming. No obligation may replace those rows by an unlogged analytic solution, analytic continuation principle, selected germ, quotient equality of functions, countable-choice coefficient stream, or ambient real-completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-kovalevskaya-carrier}. The analytic rows are $P,D,C,I,J,S,Q,Y,R$, and the structural rows are $H,T,G,N$.

The PDE row, derivative ledger, coefficient boundary, and initial hypersurface data are read before the jet recursion. Thus the row $J$ records only the finite coefficient recursion determined by displayed inputs.

The coefficient windows are finite. The stream row $S$ fixes the inspected depths, the regular rational row $Q$ records the rational bounds, the dyadic row $Y$ stores the coefficient ledger, and the real row $R$ seals only those displayed reads.

Finally $H,T,G,N$ transport, replay, source, and name the same packet. Since the carrier has no solution-germ, analytic-continuation, quotient-function, choice-stream, or ambient-completeness coordinate, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Cauchy--Kovalevskaya analytic-jet scope]
\label{thm:cauchy-kovalevskaya-analytic-jet-scope}
Every constructive CK consumer of an accepted $\CauchyKovalevskayaUp$ carrier must read the finite route
$$
\PdeUp,\ \DerivativeUp,\ \ContinuousMapUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The route exposes the displayed PDE operator, partial-derivative ledger, analytic coefficient boundary, initial hypersurface data, finite jet recursion, stream window, rational coefficient readback, dyadic coefficient ledger, real seal, transport, replay, provenance, and local naming before any analytic-jet read is public. It exports no global solution theorem, uniqueness theorem, analytic continuation theorem, quotient equality of functions, or selected solution germ.
\end{theorem}
\begin{proof}
Start with a CK read request. It first opens the PDE row $P$, the derivative ledger $D$, the continuous coefficient row $C$, and the initial data row $I$.

Only then can the consumer inspect $J$. That row is a finite jet recursion over the displayed rows, so it cannot supply a host solution or a global existence theorem.

Next read the stream, rational, dyadic, and real rows $S,Q,Y,R$. These rows bind coefficient depth, rational bounds, dyadic ledgers, and real seals to the same finite analytic window.

The structural rows $H,T,G,N$ preserve that order by componentwise transport, displayed replay, provenance, and local naming. Hence the public scope is the analytic-jet NameCert surface and not analytic continuation or solution selection.
\end{proof}

\begin{theorem}[Cauchy--Kovalevskaya finite coefficient ledger]
\label{thm:cauchy-kovalevskaya-finite-coefficient-ledger}
The coefficient ledger of an accepted $\CauchyKovalevskayaUp$ packet is finite at every public read: each requested jet depth is scheduled by $S$, bounded through $Q$, stored through $Y$, and sealed through $R$ before the local $\NameCert$ row is consumed. A downstream PDE consumer cannot bypass this ledger by appealing to an infinite formal power series, a selected analytic germ, quotient equality of coefficient streams, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the stream schedule $S$. It names the finite coefficient windows and jet depths available to the public request.

Use $Q$ and $Y$ next. The regular rational row records the visible coefficient bounds, while the dyadic core row stores the displayed coefficient ledger for the same windows.

The real seal $R$ is downstream of those finite rows. It seals the displayed coefficient and radius reads only after the rational and dyadic ledgers are present.

Therefore the local naming row $N$ can certify only the finite coefficient ledger transported and replayed by $H,T,G$. It cannot certify an unbounded series, a selected germ, quotient equality, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{An accepted $\CauchyKovalevskayaUp$ packet that exports a CK analytic-jet read while omitting the PDE row, derivative ledger, coefficient boundary, finite jet recursion, stream schedule, rational readback, dyadic ledger, real seal, transport, replay, provenance, or local NameCert row refutes the carrier.}

\independenceWitness{pde, derivative, continuousmap, streamname, regseqrat, dyadicratcore, real: $\CauchyKovalevskayaUp$ is not bijective to any listed sibling because it binds PDE operator data, derivative rows, analytic coefficient boundaries, initial hypersurface data, finite jet recursion, scheduled coefficient windows, rational and dyadic coefficient ledgers, real seals, transport, replay, provenance, and local naming in one carrier.}

\closureat{CauchyKovalevskayaUp}{seedStr}

\begin{closurestatus}{\CauchyKovalevskayaUp}
  \constructivestory{A $\CauchyKovalevskayaUp$ packet is generated from a PDE row, derivative ledger, continuous coefficient boundary, initial hypersurface row, finite jet recursion, StreamName schedule, RegSeqRat readback, DyadicRatCore coefficient ledger, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-kovalevskaya-carrier}, \autoref{thm:cauchy-kovalevskaya-namecert-obligations}, \autoref{thm:cauchy-kovalevskaya-analytic-jet-scope}, and \autoref{thm:cauchy-kovalevskaya-finite-coefficient-ledger} seed the finite analytic-jet NameCert surface for constructive PDE consumers.}
  \formalstatus{\encodedDefV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close analytic continuation, global existence, uniqueness of analytic solutions, quotient equality of functions, selected solution germs, countable choice over coefficients, RealUp completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, PDE-row exposure, derivative-ledger exactness, coefficient-boundary visibility, jet-recursion exactness, finite coefficient ledger, Real-seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.CauchyKovalevskayaUp.}
\end{closurestatus}
