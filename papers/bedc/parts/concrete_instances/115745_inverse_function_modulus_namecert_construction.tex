\chapter{A Concrete Naming Certificate for $\InverseFunctionModulusUp$}
\label{ch:concrete-instances-inverse-function-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\InverseFunctionModulusUp$ packet records the local inverse data that must be visible before an inverse-function read is treated as quantitative. It sits downstream of the local inverse carrier of \autoref{ch:concrete-instances-inverse-function-namecert}, the derivative source rows of \autoref{ch:concrete-instances-derivative-namecert}, the pointwise continuity route of \autoref{ch:concrete-instances-continuousmap-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback schedule of \autoref{ch:concrete-instances-regseqrat-namecert}, and the finite Newton-step service of \autoref{ch:concrete-instances-newtoniteration-namecert}. The packet names a finite modulus boundary for local inverse construction; it does not turn local data into a global inverse theorem, host topology, or choice-selected inverse branch.

\begin{definition}[Inverse-function modulus carrier]
\label{def:inverse-function-modulus-carrier}
An $\InverseFunctionModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(W,L,B,R,C_m,N_t,H,K,P,N)
$$
with the following rows:
\begin{enumerate}
\item $W$ is the source point window, naming the base point, source radius, target radius, and the finite neighbourhood on which the local inverse read is allowed.
\item $L$ is the derivative lower-bound row, recording the displayed nonzero derivative or invertible-linear lower estimate used to keep the inverse step separated from collapse.
\item $B$ is the $\RealUp$ local ball row, sealing the source and target ball endpoints as real-name data rather than as host open sets.
\item $R$ is the $\RegSeqRatUp$ rate-budget row, carrying the rational modulus schedule and the finite tolerance ledger used to compare requested output precision with input-window precision.
\item $C_m$ is the $\ContinuousMapUp$ modulus row for the displayed map on the same window.
\item $N_t$ is the $\NewtonIterationUp$ or Picard contraction row, recording the finite contraction certificate and the first accepted iteration window.
\item $H$ records componentwise $\hsame$ transport for source windows, derivative lower bounds, Real balls, rate budgets, continuous-map modulus rows, Newton or Picard rows, provenance, and local naming.
\item $K$ records displayed $\Cont$ replay through
$$
W,L,B,R,C_m,N_t \longmapsto \NameCert_{\InverseFunctionModulusUp}.
$$
\item $P$ is the $\Pkg$ provenance over $\InverseFunctionModulusUp$, $\InverseFunctionUp$, $\DerivativeUp$, $\ContinuousMapUp$, $\RealUp$, $\RegSeqRatUp$, $\NewtonIterationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\InverseFunctionModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $W,L,B,R,C_m,N_t,H,K,P,N$. It has no coordinate for a global inverse function, a selected inverse branch outside the displayed window, ambient completeness, classical open mapping, or choice-based local selection.
\end{definition}

\begin{theorem}[Inverse-function modulus local bijection obligation]
\label{thm:inverse-function-modulus-local-bijection-obligation}
For every accepted $\InverseFunctionModulusUp$ carrier
$$
M=(W,L,B,R,C_m,N_t,H,K,P,N),
$$
the local inverse-bijection obligation is read only from the displayed source window, derivative lower-bound row, Real local ball, RegSeqRat rate budget, ContinuousMap modulus row, Newton or Picard contraction row, componentwise transport, replay, provenance, and local naming. In particular, any local inverse read exported through $M$ must keep the same window $W$, lower bound $L$, ball $B$, budget $R$, modulus $C_m$, and contraction row $N_t$ visible at the consumer boundary.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:inverse-function-modulus-carrier}. The rows $W,L,B,R,C_m,N_t$ are the only analytic rows in the packet, and each is tied to the same local window by the classifier.

The lower-bound row $L$ and the continuous-map modulus row $C_m$ provide the quantitative separation and continuity data that a local inverse read may inspect. The Real ball $B$ and the RegSeqRat budget $R$ keep the neighbourhood and tolerance data inside displayed real names and finite regular-rational schedules.

The Newton or Picard row $N_t$ supplies the finite contraction service on this same window. Componentwise $\hsame$ transport and displayed $\Cont$ replay repack exactly those rows into the local $\NameCert_{\InverseFunctionModulusUp}$ surface, so no global inverse function, host open mapping theorem, selected inverse branch, or ambient completeness coordinate is introduced.
\end{proof}

\begin{theorem}[Inverse-function modulus Newton-window stability]
\label{thm:inverse-function-modulus-newton-window-stability}
Classified $\InverseFunctionModulusUp$ packets remain classified after replacing the source point window, derivative lower-bound row, Real local ball, RegSeqRat rate budget, ContinuousMap modulus row, or Newton/Picard contraction row by componentwise $\hsame$-related rows, provided the replay row still displays the same route
$$
W,L,B,R,C_m,N_t \longmapsto \NameCert_{\InverseFunctionModulusUp}.
$$
The resulting packet exports the same local inverse-modulus boundary and does not add a fresh window, selected inverse branch, or unbudgeted convergence claim.
\end{theorem}
\begin{proof}
Start with two accepted packets and the componentwise $\hsame$ comparisons for $W,L,B,R,C_m,N_t$. The classifier of \autoref{def:inverse-function-modulus-carrier} permits transport only along those displayed coordinates and the structural rows $H,K,P,N$.

Use the transported Real ball and RegSeqRat rate budget to keep the local metric and tolerance rows attached to the same source window. Then transport the ContinuousMap modulus row and the Newton or Picard contraction row through the same comparison data. The replay row $K$ repacks this transported route as one displayed history.

Since the transported packet still uses the same ten rows, it has no coordinate from which to choose a new inverse branch, enlarge to a global inverse theorem, import completeness, or appeal to classical open mapping. The local inverse-modulus boundary is therefore stable under the displayed componentwise transport.
\end{proof}

\closureat{InverseFunctionModulusUp}{seedStr}

\begin{closurestatus}{\InverseFunctionModulusUp}
  \constructivestory{An $\InverseFunctionModulusUp$ packet is generated from a source point window, derivative lower-bound row, Real local ball, RegSeqRat rate budget, ContinuousMap modulus row, Newton or Picard contraction row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:inverse-function-modulus-carrier}, \autoref{thm:inverse-function-modulus-local-bijection-obligation}, and \autoref{thm:inverse-function-modulus-newton-window-stability} seed the finite local inverse-modulus route over Real, RegSeqRat, continuous-map, derivative, and Newton/Picard rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a global inverse-function theorem, RealUp completeness, classical open mapping, choice-based local selection, selected inverse germs, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires carrier admission, local bijection obligation coverage, Newton-window stability, rate-budget exactness, provenance exactness, and local naming for BEDC.Derived.InverseFunctionModulusUp.}
\end{closurestatus}
