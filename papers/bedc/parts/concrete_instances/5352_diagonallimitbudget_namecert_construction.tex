\chapter{A Concrete Naming Certificate for $\DiagonalLimitBudgetUp$}
\label{ch:concrete-instances-diagonal-limit-budget-namecert}
\origin{ai}

The $\DiagonalLimitBudgetUp$ packet is the finite budget carrier between the diagonal selector of \autoref{ch:concrete-instances-limitselector-namecert} and the RealUp seal route synchronized in \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert}. It isolates the modulus, selected window, dyadic-tolerance ledger, and seal handoff that the Real completeness diagonal route consumes before any completed real value is read. The observation envelope of \autoref{ch:concrete-instances-realapproximationenvelope-namecert} supplies the sibling finite readback surface; this chapter records the budget packet that keeps that readback tied to the diagonal index request.

\falsifiablePrediction{A packet with the same RealUp seal row but no displayed modulus budget row is not an accepted DiagonalLimitBudgetUp carrier.}

\independenceWitness{LimitSelectorUp selects the diagonal row but lacks the seal budget; CauchySealBudgetSynchronizerUp synchronizes selector and seal rows but lacks the isolated budget carrier; RealApproximationEnvelopeUp packages observation envelopes but not the diagonal modulus budget; DiagonalLimitCompatibilityUp compares limits but does not provide the budget packet.}

\begin{definition}[DiagonalLimitBudget carrier]
\label{def:diagonal-limit-budget-carrier}
A $\DiagonalLimitBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(D,M,W,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the diagonal index request read from the Real completeness diagonal representative of \autoref{def:real-completeness-diagonal-limit};
\item $M$ is the modulus budget row displaying the precision request and the selected modulus value used to answer $D$;
\item $W$ is the selected $\StreamNameUp$ and $\RegSeqRat$ window row chosen by the diagonal selector for that modulus budget;
\item $Q$ is the $\DyadicRatCoreUp$ tolerance and readback ledger attached to the selected window;
\item $E$ is the $\RealUp$ seal row reached by the same diagonal route;
\item $H$ stores componentwise $\hsame$ transports for $D,M,W,Q,E,C,P,N$;
\item $C$ stores the $\Cont$ routes replaying the index request, budget row, selected window, dyadic ledger, seal row, provenance, and local naming row;
\item $P$ is the $\Pkg$ provenance over $\LimitSelectorUp$, $\RealApproximationEnvelopeUp$, $\CauchySealBudgetSynchronizerUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\DiagonalLimitBudgetUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient streams, host real equality, countable choice, a host-selected limit, or ambient completeness.
\end{definition}
\leandef{BEDC.Derived.DiagonalLimitBudgetUp.DiagonalLimitBudgetUp}

\begin{theorem}[DiagonalLimitBudget non-escape]
\label{thm:diagonal-limit-budget-nonescape}
Every accepted $\DiagonalLimitBudgetUp$ packet read by a diagonal-limit or $\RealUp$ consumer factors through the displayed modulus budget row $M$ before it reaches the selected window $W$, dyadic ledger $Q$, or seal row $E$. A consumer that reads $E$ without exposing $D,M,W,Q,H,C,P,N$ is not reading the accepted budget carrier.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonalLimitBudgetUp.DiagonalLimitBudgetNonescape}
\begin{proof}
Project the carrier of \autoref{def:diagonal-limit-budget-carrier}. The only route to the seal coordinate is the displayed tuple $B=(D,M,W,Q,E,H,C,P,N)$, and the budget row $M$ is the only row that records the modulus value consumed by the diagonal request.

The transport rows $H$ and replay routes $C$ preserve the same coordinates. They can pass the selected window and dyadic ledger to a consumer, but they do not create a second modulus row, a second seal, or an external limit witness.

Thus any accepted seal read must expose the carrier rows in order: request, budget, window, dyadic readback, seal, transport, route, provenance, and local name. Since the excluded objects have no coordinate in $B$, they cannot be recovered from an accepted packet.
\end{proof}

\begin{theorem}[DiagonalLimitBudget seal handoff]
\label{thm:diagonal-limit-budget-seal-handoff}
For an accepted $\DiagonalLimitBudgetUp$ carrier $B=(D,M,W,Q,E,H,C,P,N)$, the $\RealUp$ seal row $E$ is handed to the synchronizer route only together with the same modulus budget $M$, selected window $W$, dyadic ledger $Q$, and structural rows $H,C,P,N$. The handoff supplies a budgeted seal route, not an isolated RealUp seal.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonalLimitBudgetUp.DiagonalLimitBudgetSealHandoff}
\begin{proof}
Read the budget carrier and the synchronizer sibling together. The synchronizer of \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert} accepts a seal route only when a displayed budget row is available for the same request.

The $\DiagonalLimitBudgetUp$ carrier provides exactly such a row: $M$ is tied to the request $D$, and $W$ and $Q$ record the finite window and dyadic readback selected for that budget. The seal coordinate $E$ is therefore exported only through the same packet.

The package row $P$ and continuation row $C$ replay the sibling dependencies on $\LimitSelectorUp$, $\RealApproximationEnvelopeUp$, and $\CauchySealBudgetSynchronizerUp$. No coordinate supplies an isolated seal, host-selected limit, quotient stream, or ambient completeness route.
\end{proof}

\begin{theorem}[DiagonalLimitBudget obligation surface]
\label{thm:diagonal-limit-budget-obligation-surface}
The local $\NameCert_{\DiagonalLimitBudgetUp}$ surface consists exactly of carrier admission for $B$, diagonal request exposure through $D$, modulus-budget exposure through $M$, selected $\StreamNameUp$ and $\RegSeqRat$ window exposure through $W$, $\DyadicRatCoreUp$ tolerance and readback exposure through $Q$, $\RealUp$ seal exposure through $E$, componentwise $\hsame$ classifier transport through $H$, displayed $\Cont$ classifier replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. The same displayed $D,M$ request determines the same $W,Q$ readback surface, and the seal row $E$ is readable only along the displayed $M,W,Q$ budget route.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonalLimitBudgetUp.DiagonalLimitBudgetObligationSurface}
\begin{proof}
Carrier admission is projection from the displayed tuple. The request, budget, selected window, dyadic ledger, seal, transport, continuation, provenance, and naming rows are all explicit coordinates.

The selector-facing side is exhausted by $D,M,W$: the request asks for a diagonal index, the budget row records the modulus value, and the selected window names the finite $\StreamNameUp$ and $\RegSeqRat$ read. The observation-facing side is exhausted by $Q,E$: the dyadic ledger records the tolerance and readback, and the seal row records the RealUp endpoint reached by that ledger.

Classifier transport is carried by the structural rows. The componentwise $\hsame$ row $H$ transports the displayed coordinates $D,M,W,Q,E,C,P,N$, while the $\Cont$ row $C$ replays the index request, budget row, selected window, dyadic ledger, seal row, provenance, and local naming row. Thus the classifier visible to a consumer is the classifier of the displayed packet, not a hidden external classifier.

Selected-window determinacy is the finite readback part of the same projection. Since $W$ and $Q$ are coordinates of $B$ attached to the displayed request and budget rows, two accepted reads of the same $D,M$ surface in this packet expose the same selected-window and dyadic-readback rows. By \autoref{thm:diagonal-limit-budget-nonescape}, there is no second modulus row, second selected window, or external limit witness from which a different $W,Q$ readback could be recovered.

Seal-budget exactness follows from the seal handoff. By \autoref{thm:diagonal-limit-budget-seal-handoff}, the seal row $E$ is exported only together with the same $M,W,Q,H,C,P,N$ rows. Therefore $E$ is readable precisely as the displayed budgeted seal route, and the obligation surface contains no hidden classifier, no quotient stream equality, no host real equality, and no ambient completeness premise.
\end{proof}

\begin{theorem}[DiagonalLimitBudget independence witness]
\label{thm:diagonal-limit-budget-independence-witness}
The $\DiagonalLimitBudgetUp$ carrier is not reducible to any one sibling route among $\LimitSelectorUp$, $\CauchySealBudgetSynchronizerUp$, $\RealApproximationEnvelopeUp$, or $\DiagonalLimitCompatibilityUp$. Its distinguishing data is the simultaneous presence of the diagonal request $D$, modulus budget $M$, selected window $W$, dyadic-tolerance ledger $Q$, RealUp seal $E$, and structural rows $H,C,P,N$ in one finite packet.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonalLimitBudgetUp.DiagonalLimitBudgetIndependenceWitness}
\begin{proof}
Compare the sibling carriers by their displayed rows. The limit selector exposes the diagonal selection route, but it does not isolate the modulus budget as the budget object consumed by the seal handoff.

The synchronizer exposes the joint seal route, but it treats the budget row as an input from another packet. The approximation envelope exposes the finite observation ledger, but it does not bind that ledger to the diagonal modulus budget. The compatibility packet compares limit routes, but it does not supply the budget carrier consumed before comparison.

The packet $B=(D,M,W,Q,E,H,C,P,N)$ displays all of these rows together. Removing $M$ loses the finite budget, removing $Q$ loses the dyadic-tolerance ledger, removing $E$ loses the seal handoff, and removing $H,C,P,N$ loses transport, replay, provenance, and local naming.
\end{proof}

\begin{theorem}[DiagonalLimitBudget TasteGate readiness]
\label{thm:diagonal-limit-budget-tastegate-readiness}
The Lean TasteGate module for $\DiagonalLimitBudgetUp$ must include explicit $\mathsf{Nontrivial}\ \DiagonalLimitBudgetUp$ and $\mathsf{FieldFaithful}\ \DiagonalLimitBudgetUp$ instances. Nontriviality separates the modulus budget row from the seal row, and field faithfulness preserves the displayed rows $D,M,W,Q,E,H,C,P,N$ for every TasteGate-facing consumer.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonalLimitBudgetUp.DiagonalLimitBudgetTasteGateReadiness}
\leanvariant{BEDC.Derived.DiagonalLimitBudgetUp.DiagonalLimitBudgetTasteGate\_single\_carrier\_alignment}
\begin{proof}
The carrier has two distinct analytic roles: $M$ records the finite modulus budget and $E$ records the RealUp seal reached after the budgeted route. A collapsed packet that identifies these roles would admit the seal without the displayed budget row, contradicting \autoref{thm:diagonal-limit-budget-nonescape}.

Field faithfulness is the row-preservation condition needed by the same surface. A TasteGate-facing consumer may inspect the request, budget, selected window, dyadic ledger, seal, transport, route, provenance, and local naming rows, but no unlisted analytic or host-side object.
\end{proof}

\begin{theorem}[DiagonalLimitBudget carrier admission]
\label{thm:diagonal-limit-budget-carrier-admission}
An accepted $\DiagonalLimitBudgetUp$ carrier is admitted exactly as the finite $\BHist$ packet $B=(D,M,W,Q,E,H,C,P,N)$ whose local $\NameCert_{\DiagonalLimitBudgetUp}$ row $N$ reads the diagonal request $D$, modulus budget $M$, selected window $W$, dyadic tolerance ledger $Q$, $\RealUp$ seal $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, and $\Pkg$ provenance $P$.
\end{theorem}
\begin{proof}
Inspect the carrier of \autoref{def:diagonal-limit-budget-carrier}. Its displayed coordinates are precisely $D,M,W,Q,E,H,C,P,N$, and each coordinate is a finite $\BHist$ row.

Apply \autoref{thm:diagonal-limit-budget-obligation-surface}. The local $\NameCert$ surface admits only the request, budget, selected window, dyadic ledger, seal, transport, replay, provenance, and naming rows listed there.

Finally use \autoref{thm:diagonal-limit-budget-nonescape}. A consumer that omits $D,M,W,Q,H,C,P,N$ cannot reach the accepted seal row. Thus carrier admission is the displayed finite packet and not parameter transport through an external budget object.
\end{proof}

\begin{theorem}[DiagonalLimitBudget synchronization stability]
\label{thm:diagonal-limit-budget-synchronization-stability}
For every accepted $\DiagonalLimitBudgetUp$ carrier $B=(D,M,W,Q,E,H,C,P,N)$, any two reads transported by the same componentwise $\hsame$ row $H$ and replayed through the same displayed $\Cont$ row $C$ expose the same selected $\StreamNameUp$ and $\RegSeqRatUp$ window row $W$, the same $\DyadicRatCoreUp$ tolerance ledger $Q$, the same $\Pkg$ provenance $P$, and the same local $\NameCert_{\DiagonalLimitBudgetUp}$ row $N$ attached to $D,M$.
\end{theorem}
\begin{proof}
Begin with finite row inspection of $B$. The selected-window row $W$ and dyadic ledger $Q$ are coordinates of the same packet as the request $D$ and modulus budget $M$.

Use \autoref{thm:diagonal-limit-budget-obligation-surface}. Componentwise $\hsame$ transport through $H$ preserves the displayed coordinates, and $\Cont$ replay through $C$ replays the index request, budget row, selected window, dyadic ledger, seal row, provenance, and local naming row.

The non-escape theorem excludes any second selected window, second dyadic ledger, hidden provenance row, or external local name. Hence two accepted reads along the same $H$ and $C$ surface expose the same $W,Q,P,N$ rows for the displayed $D,M$ budget.
\end{proof}

\begin{theorem}[DiagonalLimitBudget Real seal handoff]
\label{thm:diagonal-limit-budget-real-seal-handoff}
For every accepted $\DiagonalLimitBudgetUp$ carrier $B=(D,M,W,Q,E,H,C,P,N)$, the $\RealUp$ seal row $E$ is readable only after the diagonal request $D$, modulus budget $M$, selected $\StreamNameUp$ and $\RegSeqRatUp$ window $W$, $\DyadicRatCoreUp$ tolerance ledger $Q$, componentwise $\hsame$ row $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\DiagonalLimitBudgetUp}$ row $N$ have been displayed.
\end{theorem}
\begin{proof}
Apply \autoref{thm:diagonal-limit-budget-seal-handoff}. The seal is handed to the synchronizer route only together with the same modulus budget, selected window, dyadic ledger, and structural rows.

Apply \autoref{thm:diagonal-limit-budget-nonescape}. The packet has no route to $E$ that bypasses $D,M,W,Q,H,C,P,N$, and no coordinate stores an isolated RealUp seal.

The obligation surface records why this is stable under reading. The rows $H$ and $C$ transport and replay the displayed packet, while $P$ and $N$ source and name the same local carrier. Therefore the Real seal handoff is the displayed budgeted route, not a parameter-transported or host-selected Real completion claim.
\end{proof}

\closureat{DiagonalLimitBudgetUp}{seedStr}
\closureat{DiagonalLimitBudgetUp}{obligationStr}

\begin{closurestatus}{\DiagonalLimitBudgetUp}
  \constructivestory{A $\DiagonalLimitBudgetUp$ packet is generated from a diagonal request, a displayed modulus budget, selected $\StreamNameUp$ and $\RegSeqRatUp$ window data, a $\DyadicRatCoreUp$ tolerance ledger, a $\RealUp$ seal row, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:diagonal-limit-budget-carrier}, \autoref{thm:diagonal-limit-budget-nonescape}, \autoref{thm:diagonal-limit-budget-seal-handoff}, \autoref{thm:diagonal-limit-budget-obligation-surface}, \autoref{thm:diagonal-limit-budget-independence-witness}, \autoref{thm:diagonal-limit-budget-tastegate-readiness}, \autoref{thm:diagonal-limit-budget-carrier-admission}, \autoref{thm:diagonal-limit-budget-synchronization-stability}, and \autoref{thm:diagonal-limit-budget-real-seal-handoff} bind the displayed rows $D,M,W,Q,E,H,C,P,N$ to the diagonal request, modulus budget, selected StreamName and RegSeqRat window, DyadicRatCore ledger, RealUp seal, transport, replay, provenance, and local NameCert surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close quotient streams, host real equality, countable choice, selected limits, hidden RealUp completion data, or ambient completeness for $\DiagonalLimitBudgetUp$.}
  \upgradepath{Scoped grounding requires checking the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows against the same $D,M,W,Q,E,H,C,P,N$ packet.}
\end{closurestatus}
