\chapter{A Concrete Naming Certificate for $\RegularCauchyMidpointConvexUp$}
\label{ch:concrete-instances-regular-cauchy-midpoint-convex-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyMidpointConvexUp$ packet records dyadic midpoint-convexity rows over $\RegSeqRatUp$ and $\RealUp$ names. It sits over the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the metric comparison discipline of \autoref{ch:concrete-instances-metric-namecert}. The packet names the finite midpoint-convexity surface for regular Cauchy inputs; it does not import host subsets, quotient real equality, selected limits, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy midpoint-convex carrier]
\label{def:regular-cauchy-midpoint-convex-carrier}
A $\RegularCauchyMidpointConvexUp$ carrier is a finite $\BHist$ packet
$$
K=(F,R_0,R_1,S_0,S_1,D,M,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the function-observation row naming the displayed real-valued map whose midpoint-convexity is inspected.
\item $R_0$ and $R_1$ are the two $\RegSeqRatUp$ source-name rows for the endpoints.
\item $S_0$ and $S_1$ are the synchronized $\StreamNameUp$ finite-window rows exposing exactly the rational approximants requested by the dyadic midpoint calculation.
\item $D$ is the $\DyadicRatCoreUp$ endpoint ledger for the common denominator, dyadic radius, and midpoint tolerance budget.
\item $M$ is the midpoint-readback row, recording the regular-Cauchy midpoint observation before any terminal real-name seal is visible.
\item $Q$ is the midpoint-convexity comparison row, recording the displayed dyadic inequality between the midpoint value and the endpoint-value average.
\item $E$ is the $\RealUp$ seal row reached only after $F,R_0,R_1,S_0,S_1,D,M,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the function row, endpoint names, stream windows, dyadic ledger, midpoint readback, comparison, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from every midpoint-convexity query through the same finite route.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyMidpointConvexUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\MetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyMidpointConvexUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host convex subset, quotient equality of real names, a selected limit, trichotomy, or ambient real completeness.
\end{definition}

\begin{theorem}[Regular Cauchy midpoint-convex NameCert obligations]
\label{thm:regular-cauchy-midpoint-convex-namecert-obligations}
For every accepted $\RegularCauchyMidpointConvexUp$ carrier
$$
K=(F,R_0,R_1,S_0,S_1,D,M,Q,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyMidpointConvexUp}$ surface consists exactly of carrier admission for the displayed function row, the two regular Cauchy endpoint rows, the synchronized stream-window rows, the dyadic tolerance ledger, the midpoint readback row, the midpoint-convexity comparison row, the terminal Real seal, componentwise transport, displayed replay, provenance, and local naming. A consumer may inspect midpoint convexity only after the endpoint windows, dyadic midpoint budget, midpoint readback, and comparison row have all been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-midpoint-convex-carrier}. The analytic rows are exactly $F,R_0,R_1,S_0,S_1,D,M,Q,E$, while $H,C,P,N$ transport, replay, source, and name the same packet.

Read the route in dependency order. The endpoint names $R_0,R_1$ expose finite windows $S_0,S_1$, those windows feed the dyadic ledger $D$, and the midpoint readback $M$ is available before the comparison row $Q$ can state midpoint convexity.

Finally the terminal seal $E$ is reached only after the finite rows have been displayed. Since the carrier contains no host subset, quotient real equality, selected limit, trichotomy branch, or ambient completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy midpoint-convex dyadic scope]
\label{thm:regular-cauchy-midpoint-convex-dyadic-scope}
Every accepted $\RegularCauchyMidpointConvexUp$ carrier routes a midpoint-convexity read through
$$
(R_0,R_1)\longmapsto(S_0,S_1)\longmapsto D\longmapsto M\longmapsto Q\longmapsto E.
$$
Thus the public dyadic scope is the finite route from regular Cauchy endpoint rows through synchronized observation windows, dyadic midpoint accounting, midpoint readback, midpoint-convexity comparison, and the Real seal. It exports no host subset, quotient real equality, selected limit, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier rows. The two endpoint names are visible only as $\RegSeqRatUp$ rows, and each requested midpoint precision is read through the finite $\StreamNameUp$ windows $S_0,S_1$.

The dyadic ledger $D$ is the only arithmetic budget in the packet. It records the common denominator, radius, and tolerance rows needed by the midpoint readback $M$ and the comparison row $Q$.

The Real seal $E$ is terminal in this displayed route. Componentwise transport and replay keep the same row order, while provenance and local naming add no analytic coordinate. Therefore every midpoint-convexity read remains inside the stated dyadic scope.
\end{proof}

\begin{theorem}[Regular Cauchy midpoint-convex ledger nonescape]
\label{thm:regular-cauchy-midpoint-convex-ledger-nonescape}
Every $\MetricUp$ or $\RealUp$ consumer of an accepted $\RegularCauchyMidpointConvexUp$ packet receives only the rows
$$
F,R_0,R_1,S_0,S_1,D,M,Q,E,H,C,P,N.
$$
The ledger cannot be reinterpreted as a host convexity theorem, quotient equality of real names, selected limiting process, trichotomy decision, or ambient completeness principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-midpoint-convex-namecert-obligations}. It requires carrier admission and classifier comparison to pass through the displayed endpoint, window, dyadic, midpoint, comparison, and Real-seal rows.

Next use \autoref{thm:regular-cauchy-midpoint-convex-dyadic-scope}. Any Real-facing or metric-facing consumer sees the midpoint-convexity comparison only after the synchronized finite windows and dyadic budget have been read.

The remaining rows $H,C,P,N$ preserve and name this packet. Because none of the carrier coordinates stores host subsets, quotient real equality, selected limits, trichotomy, or ambient completeness, the consumer route cannot leave the finite ledger.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyMidpointConvexUp$ read is refuted if the comparison row $Q$ is preserved while either endpoint window $S_0,S_1$ or the dyadic ledger $D$ is removed.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, metric: $\RegularCauchyMidpointConvexUp$ is not bijective to any listed sibling because it jointly carries function observations, two regular Cauchy endpoint rows, synchronized finite windows, dyadic midpoint accounting, midpoint readback, comparison, Real sealing, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyMidpointConvexUp}{seedStr}

\begin{closurestatus}{\RegularCauchyMidpointConvexUp}
  \constructivestory{A $\RegularCauchyMidpointConvexUp$ packet is generated from a function-observation row, two RegSeqRat endpoint rows, synchronized StreamName windows, a DyadicRatCore midpoint ledger, a midpoint readback row, a midpoint-convexity comparison row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-midpoint-convex-carrier}, \autoref{thm:regular-cauchy-midpoint-convex-namecert-obligations}, \autoref{thm:regular-cauchy-midpoint-convex-dyadic-scope}, and \autoref{thm:regular-cauchy-midpoint-convex-ledger-nonescape} seed the finite midpoint-convexity route over DyadicRatCore, StreamName, RegSeqRat, Real, Metric, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host convex subsets, quotient real equality, selected limits, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, synchronized-window exposure, dyadic midpoint accounting, midpoint-convexity comparison exactness, Real-seal routing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyMidpointConvexUp.}
\end{closurestatus}
