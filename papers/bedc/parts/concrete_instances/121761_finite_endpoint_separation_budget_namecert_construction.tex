\chapter{A Concrete Naming Certificate for $\FiniteEndpointSeparationBudgetUp$}
\label{ch:concrete-instances-finite-endpoint-separation-budget-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\FiniteEndpointSeparationBudgetUp$ packet records the finite endpoint-separation budget read before an L10 Real observation route may treat two displayed endpoint rows as separated. It sits after the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the guarded sign-apartness surface of \autoref{ch:concrete-instances-located-sign-apartness-namecert}. The packet names only the inspected separation rows; it is not trichotomy, quotient real equality, selected endpoint representatives, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Finite endpoint-separation budget carrier]
\label{def:finite-endpoint-separation-budget-carrier}
A $\FiniteEndpointSeparationBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(L,U,D,W,Q,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower endpoint $\RealUp$ row, already sealed through the displayed real-name route.
\item $U$ is the upper endpoint $\RealUp$ row, sealed through the same route.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger storing the finite positive radius, outward slack, and comparison precision used to separate the endpoints.
\item $W$ is the $\StreamNameUp$ finite-window row from which endpoint observations are inspected at the requested tolerance.
\item $Q$ is the $\RegSeqRatUp$ readback row for those endpoint windows.
\item $S$ is the $\LocatedSignApartnessUp$ sign-apartness row certifying that the inspected endpoint difference is separated at the displayed budget.
\item $H$ records componentwise $\hsame$ transport for endpoint, tolerance, window, readback, sign-apartness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an endpoint-separation request through $L,U,D,W,Q,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteEndpointSeparationBudgetUp$, $\RealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedSignApartnessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteEndpointSeparationBudgetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,U,D,W,Q,S,H,C,P,N$. It has no coordinate for total real order, quotient representatives, selected endpoints, countable choice, compactness, or ambient completeness.
\end{definition}

\begin{theorem}[Finite endpoint-separation budget NameCert obligations]
\label{thm:finite-endpoint-separation-budget-namecert-obligations}
For every accepted $\FiniteEndpointSeparationBudgetUp$ carrier $B=(L,U,D,W,Q,S,H,C,P,N)$, the local $\NameCert_{\FiniteEndpointSeparationBudgetUp}$ surface consists exactly of endpoint admission for $L,U$, dyadic budget exposure through $D$, finite-window exposure through $W$, regular rational readback through $Q$, located sign-apartness exposure through $S$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:finite-endpoint-separation-budget-carrier}. The endpoint rows, tolerance row, finite windows, readback, sign-apartness row, and structural rows are all explicit coordinates of one finite packet.

The analytic route is ordered. Endpoint reads first expose $L,U$, then the dyadic budget $D$, then the finite windows $W$, and only then the regular rational readback $Q$ used by the sign-apartness row $S$.

Transport, replay, provenance, and local naming are componentwise. Since the tuple contains no total order decision, quotient representative, selected endpoint, countable-choice schedule, compactness theorem, or ambient completeness object, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite endpoint-separation budget Real route]
\label{thm:finite-endpoint-separation-budget-real-route}
Every L10 Real endpoint-separation consumer of an accepted $\FiniteEndpointSeparationBudgetUp$ carrier factors through the displayed route
$$
L,U \longmapsto D,W,Q \longmapsto S,H,C,P,N .
$$
Thus downstream interval, enclosure, and approximation chapters may cite one finite budget object instead of repeating endpoint-budget rows locally, while receiving no trichotomy, quotient real equality, selected endpoints, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the endpoint rows $L,U$. They are already sealed $\RealUp$ rows, so a consumer reads endpoint data only through the real-name route rather than through host representatives.

Next apply the dyadic-window-readback handoff. The dyadic budget $D$ determines the inspected tolerance, the window row $W$ exposes finite endpoint observations, and the readback row $Q$ presents those observations as regular rational data.

Finally read the located sign-apartness row $S$ and replay the structural rows $H,C,P,N$. The consumer receives a finite endpoint-separation budget for the displayed endpoints, and no coordinate remains from which the excluded global principles could be recovered.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteEndpointSeparationBudgetUp$ read that serves an L10 Real endpoint-separation consumer must expose endpoint rows, a DyadicRatCore budget, StreamName windows, RegSeqRat readback, LocatedSignApartness evidence, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows before the consumer is served.}

\independenceWitness{real, dyadicratcore, streamname, regseqrat, located-sign-apartness: none is bijective to $\FiniteEndpointSeparationBudgetUp$ because only this packet binds two sealed endpoints, one finite dyadic separation budget, finite observation windows, regular readback, guarded sign-apartness evidence, transport, replay, provenance, and local naming into one endpoint-budget carrier.}

\closureat{FiniteEndpointSeparationBudgetUp}{seedStr}

\begin{closurestatus}{\FiniteEndpointSeparationBudgetUp}
  \constructivestory{A $\FiniteEndpointSeparationBudgetUp$ packet is generated from lower and upper RealUp endpoint rows, a DyadicRatCore tolerance budget, StreamName finite windows, RegSeqRat readback, LocatedSignApartness evidence, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-endpoint-separation-budget-carrier}, \autoref{thm:finite-endpoint-separation-budget-namecert-obligations}, and \autoref{thm:finite-endpoint-separation-budget-real-route} seed the finite endpoint-separation budget route for L10 Real observation consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close total real order, quotient real equality, selected endpoint representatives, open-cover compactness, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint admission, dyadic budget exposure, finite-window readback, located sign-apartness evidence, transport, replay, provenance, and local naming for BEDC.Derived.FiniteEndpointSeparationBudgetUp.}
\end{closurestatus}
