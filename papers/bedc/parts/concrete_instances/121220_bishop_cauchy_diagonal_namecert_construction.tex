\chapter{A Concrete Naming Certificate for $\BishopCauchyDiagonalUp$}
\label{ch:concrete-instances-bishop-cauchy-diagonal-namecert}
\origin{human}

Following Bishop and Bridges on constructive Cauchy real names, the $\BishopCauchyDiagonalUp$ packet records the finite diagonal window used when a Cauchy family is read as a regular rational name and then sealed as a real-name boundary. It sits between the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite L10 Real and Completion object; it does not import host limits, quotient real equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop Cauchy diagonal carrier]
\label{def:bishop-cauchy-diagonal-carrier}
A $\BishopCauchyDiagonalUp$ carrier is a finite $\BHist$ packet
$$
B=(S,A,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source Cauchy window row, recording the finite family of rational names and the inspected Cauchy bounds.
\item $A$ is the diagonal schedule row, listing the finite index and precision pairs actually selected for the diagonal read.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the source window and diagonal schedule.
\item $W$ is the $\StreamNameUp$ window readback generated by the displayed diagonal schedule.
\item $R$ is the $\RegSeqRatUp$ handoff row, exposing the regular rational readback only after $S,A,D,W$ have been displayed.
\item $E$ is the terminal $\RealUp$ seal row for the regular name presented by the diagonal read.
\item $H$ records componentwise $\hsame$ transport for the source window, diagonal schedule, dyadic ledger, StreamName window, RegSeqRat handoff, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite diagonal request through $S,A,D,W,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopCauchyDiagonalUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCauchyDiagonalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,A,D,W,R,E,H,C,P,N$. No coordinate stores a host limit, quotient stream equality, selected representative, countable-choice sequence, trichotomy branch, or ambient $\RealUp$ completeness theorem.
\end{definition}

\begin{theorem}[Bishop Cauchy diagonal NameCert obligations]
\label{thm:bishop-cauchy-diagonal-namecert-obligations}
For every accepted $\BishopCauchyDiagonalUp$ carrier
$$
B=(S,A,D,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopCauchyDiagonalUp}$ surface consists exactly of source Cauchy window admission, finite diagonal schedule exposure, dyadic tolerance ledger visibility, $\StreamNameUp$ window readback, $\RegSeqRatUp$ handoff, terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A Real-facing consumer may read the diagonal packet only after all ten rows have been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-cauchy-diagonal-carrier}. The displayed inventory is the finite source window $S$, diagonal schedule $A$, dyadic ledger $D$, StreamName readback $W$, regular rational handoff $R$, Real seal $E$, and the structural rows $H,C,P,N$.

The analytic rows are ordered. The schedule $A$ is meaningful only against the source Cauchy window $S$; the dyadic ledger $D$ attaches the requested tolerances to that finite schedule; and the StreamName row $W$ records the actual window read from those displayed entries.

The completion-facing rows are downstream. The $\RegSeqRatUp$ handoff $R$ and terminal $\RealUp$ seal $E$ may be consumed only after the finite window and dyadic ledger have been exposed.

Finally $H,C,P,N$ preserve and name the same route. Since the carrier has no coordinate for host limits, quotient real equality, countable choice, trichotomy, or ambient Real completeness, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Bishop Cauchy diagonal Real handoff]
\label{thm:bishop-cauchy-diagonal-real-handoff}
Every $\RealUp$ consumer of an accepted $\BishopCauchyDiagonalUp$ packet must factor through the finite route
$$
S,A,D,W,R,E,H,C,P,N .
$$
The route first displays the source Cauchy window and diagonal schedule, then reads the $\DyadicRatCoreUp$ tolerance ledger, the $\StreamNameUp$ window, and the $\RegSeqRatUp$ handoff before the terminal $\RealUp$ seal is public. It exports a named finite diagonal packet, not a host limit, a quotient real representative, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:bishop-cauchy-diagonal-namecert-obligations}. They force a Real-facing read to expose $S,A,D,W,R,E,H,C,P,N$ as the accepted packet surface.

The finite diagonal schedule is read through the same completion corridor as the sibling chapters. The stream window agrees with the $\StreamNameUp$ route of \autoref{ch:concrete-instances-streamname-namecert}, the precision ledger agrees with the $\DyadicRatCoreUp$ route of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the regular rational handoff agrees with the $\RegSeqRatUp$ route of \autoref{ch:concrete-instances-regseqrat-namecert}.

Only after these rows are displayed may the terminal seal of \autoref{ch:concrete-instances-real-namecert} be consumed. Thus the handoff remains a finite BEDC certificate for a diagonal read, with the excluded completeness, choice, quotient, and host-limit coordinates absent from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopCauchyDiagonalUp$ read is refuted if it reaches a $\RealUp$ consumer without displaying the source Cauchy window, diagonal schedule, dyadic tolerance ledger, StreamName window readback, RegSeqRat handoff, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real: $\BishopCauchyDiagonalUp$ is not bijective to any listed sibling because it jointly carries a source Cauchy window, finite diagonal schedule, dyadic tolerance ledger, StreamName window readback, RegSeqRat handoff, Real seal, transport, replay, provenance, and local naming as one finite diagonal packet.}

\closureat{BishopCauchyDiagonalUp}{seedStr}

\begin{closurestatus}{\BishopCauchyDiagonalUp}
  \constructivestory{A $\BishopCauchyDiagonalUp$ packet is generated from a source Cauchy window, finite diagonal schedule, DyadicRatCore tolerance ledger, StreamName window readback, RegSeqRat handoff, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-cauchy-diagonal-carrier}, \autoref{thm:bishop-cauchy-diagonal-namecert-obligations}, and \autoref{thm:bishop-cauchy-diagonal-real-handoff} seed the finite diagonal route from Cauchy windows to Real-facing regular rational seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host limits, quotient real equality, selected representatives, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source-window admission, diagonal schedule exactness, dyadic tolerance visibility, StreamName readback, RegSeqRat handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopCauchyDiagonalUp.}
\end{closurestatus}
