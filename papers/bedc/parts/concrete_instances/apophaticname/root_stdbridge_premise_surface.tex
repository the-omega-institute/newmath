\begin{theorem}[ApophaticName root StdBridge premise surface]
\label{thm:apophatic-name-root-stdbridge-premise-surface}
For every accepted $\ApophaticNameUp$ packet
$$
A=(S,R,G,L,H,C,P,N),
$$
the fixed finite tuple route reaches a root-facing $\StdBridge$ request only after four displayed rows are present: the boundary-request image row, the refusal readback row, the componentwise $\NameCert_{\ApophaticNameUp}$ transport row, and the $\ChapterTasteGate$-facing refusal tuple. Thus the bridge premise is the public tuple surface $S,R,G,L,H,C,P,N$ together with those rows, not an internal object, host proof, quotient representative, hidden theorem, generated witness, or closure principle.
\end{theorem}
\begin{proof}
First apply the boundary-request image row, \autoref{thm:apophatic-name-boundary-request-image-row}. It confines each accepted consumer image of the request to the finite tuple $S,R,G,L,H,C,P,N$.

Next use the refusal readback row, \autoref{thm:apophatic-name-refusal-readback-row}. It sends every accepted image-row read back to the corresponding finite $\BHist$ component and prevents the readback from becoming supplied content.

Now apply componentwise $\NameCert$ transport, \autoref{thm:apophatic-name-componentwise-namecert-transport-row}. The local naming row replays the image and readback rows across the displayed transport route without adding a hidden coordinate.

The $\ChapterTasteGate$-facing refusal tuple is therefore exactly the same displayed packet: socket, request, refusal gate, ledger, transport, continuation, provenance, and local naming. A root-facing $\StdBridge$ request can consume that premise surface only after all these rows are visible.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_stdbridge\_premise\_surface}

\begin{theorem}[ApophaticName root reflection-axis refusal]
\label{thm:apophatic-name-root-reflection-axis-refusal}
For every accepted $\ApophaticNameUp$ packet
$$
A=(S,R,G,L,H,C,P,N),
$$
the root tuple admits only the reflection-axis rows carried by the refusal boundary. It supplies the socket row $S$, request row $R$, refusal gate $G$, refusal ledger $L$, componentwise transport $H$, continuation route $C$, provenance row $P$, and local $\NameCert_{\ApophaticNameUp}$ row $N$, but it supplies no object, host proof, quotient representative, hidden theorem, generated witness, or closure principle outside that boundary.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_reflection\_axis\_refusal}
\begin{proof}
Begin with the root bridge tuple source, \autoref{thm:apophatic-name-root-bridge-tuple-source}. It exposes all source rows before any downstream refusal read and rules out a first readable coordinate that is already an internalized object.

Use the root bridge tuple ledger, \autoref{thm:apophatic-name-root-bridge-tuple-ledger}. The ledger and non-internalization rows must be read before any externality handoff, so the handoff remains a refusal package rather than a supplied dependency.

Finally apply boundary citation universal factorization, \autoref{thm:apophatic-name-boundary-citation-universal-factorization}. Every downstream citation factors through the socket, gate, ledger, and provenance package $S,R,G,L,H,C,P,N$.

These three rows leave the reflection axis with the displayed refusal boundary only. There is no remaining carrier position from which a supplied object, host proof, quotient representative, hidden theorem, generated witness, or closure principle could be obtained.
\end{proof}

\begin{theorem}[ApophaticName root bridge counter surface]
\label{thm:apophatic-name-root-bridge-counter-surface}
After the boundary-request image row, refusal readback row, componentwise $\NameCert_{\ApophaticNameUp}$ transport row, and $\ChapterTasteGate$-facing refusal tuple have all been displayed for an accepted packet
$$
A=(S,R,G,L,H,C,P,N),
$$
any failed bridge attempt must locate an incompatibility between that refusal-axis requirement and the standard reflection schema. Absence of the tuple, image row, readback row, componentwise transport row, or refusal row is only a failed premise for the fixed finite tuple route, not a bridge counter-surface.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_counter\_surface}
\begin{proof}
Start from the root StdBridge premise surface, \autoref{thm:apophatic-name-root-stdbridge-premise-surface}. It identifies the tuple, image, readback, componentwise transport, and refusal rows as the rows that must be present before the fixed finite tuple route is tested.

Next use root reflection-axis refusal, \autoref{thm:apophatic-name-root-reflection-axis-refusal}. Once the rows are present, the only available reflection-axis content is the refusal boundary itself, with no supplied object or hidden theorem behind it.

Therefore a failed bridge attempt after these rows are displayed must be a disagreement with the standard reflection schema at the refusal axis. If one of the tuple, readback, transport, or refusal rows is absent, the route has not reached the bridge test and the failure is merely a missing premise.
\end{proof}

\begin{corollary}[ApophaticName root StdBridge premise strict obstruction]\label{cor:apophatic-name-root-stdbridge-premise-strict-obstruction}
Let $A=(S,R,G,L,H,C,P,N)$ be an accepted $\ApophaticNameUp$ packet. Any displayed failure, refusal, separation, or countermodel row used against the root-facing $\StdBridge$ premise is a strict local obstruction. It blocks only the named converse or reuse route whose consumer read tries to turn the displayed premise surface into an internal object, host proof, quotient representative, hidden theorem, generated witness, closure principle, ambient countermodel, or hidden external validation. It cannot be reinterpreted as a global impossibility theorem, a host theorem, or an external validation result outside the accepted packet.
\end{corollary}
\begin{proof}
Start with the root StdBridge premise surface, \autoref{thm:apophatic-name-root-stdbridge-premise-surface}. A root-facing $\StdBridge$ request is not reached from an unstructured object: it is reached only after four displayed rows are visible, namely the boundary-request image row, the refusal readback row, the componentwise $\NameCert_{\ApophaticNameUp}$ transport row, and the $\ChapterTasteGate$-facing refusal tuple. Thus any consumer-facing use of a displayed negative row against this premise must first consume the finite premise surface $S,R,G,L,H,C,P,N$ together with those four rows.

Apply the root reflection-axis refusal theorem, \autoref{thm:apophatic-name-root-reflection-axis-refusal}. Its source, ledger, and citation factorization rows leave the reflection axis with only the socket row $S$, request row $R$, refusal gate $G$, refusal ledger $L$, componentwise transport $H$, continuation route $C$, provenance row $P$, and local $\NameCert_{\ApophaticNameUp}$ row $N$. The same theorem excludes object supply, host proof, quotient representative, hidden theorem, generated witness, and closure principle outside that refusal boundary.

Now separate the permitted forward use from the barred converse or reuse use. In the permitted direction, the consumer may cite the visible boundary-request image, readback, transport, and refusal-tuple rows as the premise surface for the root-facing bridge request. In the barred converse direction, the consumer tries to reread that same surface as a stronger coordinate: an internal object behind $S$, a supplied dependency behind $R$, a host proof or hidden theorem behind $G$ and $L$, a quotient representative or generated witness behind the readback, a closure principle behind $H$ and $C$, or an external validation channel behind $P$ and $N$. The premise-surface theorem gives no such coordinate, and the reflection-axis refusal theorem says that the only reflection-axis content is the displayed refusal boundary.

Therefore a displayed failure, refusal, separation, or countermodel row can obstruct exactly the named converse or reuse route attached to one of those displayed premise rows. If a proposed interpretation asks for a global impossibility theorem, a host theorem, an ambient countermodel detached from $A$, or hidden external validation, it asks for an unlisted coordinate. The accepted packet rejects that interpretation at the local refusal boundary, so the obstruction remains strict and local to the root $\StdBridge$ premise surface.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_stdbridge\_premise\_strict\_obstruction}
