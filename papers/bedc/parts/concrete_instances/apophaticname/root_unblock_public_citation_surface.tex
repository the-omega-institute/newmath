\begin{theorem}[ApophaticName root-unblock public citation surface]
\label{thm:apophatic-name-root-unblock-public-citation-surface}
For every accepted $\ApophaticNameUp$ packet
$$
A=(S,R,G,L,H,C,P,N),
$$
every downstream consumer of the packet factors through the public socket row $S$, request row $R$, refusal gate $G$, refusal ledger $L$, transport row $H$, continuation row $C$, provenance row $P$, and local $\NameCert_{\ApophaticNameUp}$ row $N$. The consumer may cite this boundary package as a root-unblock surface, but it exports no generated witness, host datum, quotient representative, hidden theorem, assumed proposition, supplied object, or closure principle.
\end{theorem}
\begin{proof}
Start with the root-unblock refusal boundary and downstream boundary package, \autoref{thm:apophatic-name-root-unblock-refusal-boundary} and \autoref{thm:apophatic-name-root-downstream-boundary-package}. Together they put root use of the named externality on the finite refusal packet $S,R,G,L,H,C,P,N$.

Next use the boundary citation universal factorization, \autoref{thm:apophatic-name-boundary-citation-universal-factorization}. It makes every downstream citation pass through the displayed socket, request, refusal, ledger, transport, continuation, provenance, and local naming rows before any consumer can treat the packet as accepted.

The sibling surfaces \autoref{ch:concrete-instances-apophaticsocket-namecert} and \autoref{ch:concrete-instances-apophaticgatequestion-namecert} identify the socket and refusal gate as public boundary components. They therefore allow only citation of the finite rows and do not supply an internal object behind the socket.

Thus the public root-unblock citation surface is exactly the displayed package. Since no coordinate of that package is a generated witness, host datum, quotient representative, hidden theorem, assumed proposition, supplied object, or closure principle, no such item is exported to downstream consumers.
\end{proof}
