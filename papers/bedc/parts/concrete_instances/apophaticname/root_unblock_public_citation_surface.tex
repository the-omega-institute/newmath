\begin{theorem}[ApophaticName root-unblock public citation surface]
\label{thm:apophatic-name-root-unblock-public-citation-surface}
For every accepted $\ApophaticNameUp$ packet
$$
A=(S,R,G,L,H,C,P,N),
$$
every downstream consumer of the packet factors through the public socket row $S$, request row $R$, refusal gate $G$, refusal ledger $L$, transport row $H$, continuation row $C$, provenance row $P$, and local $\NameCert_{\ApophaticNameUp}$ row $N$. The consumer may cite this boundary package as a root-unblock surface, but it exports no generated witness, host datum, quotient representative, hidden theorem, assumed proposition, supplied object, or closure principle.
\end{theorem}
\begin{proof}
Start with the root-unblock refusal boundary and downstream boundary package, \autoref{thm:apophatic-name-root-unblock-refusal-boundary} and \autoref{thm:apophatic-name-root-downstream-boundary-package}. Together they put root use of the named externality on the finite refusal packet $S,R,G,L,H,C,P,N$.

Next use the boundary citation universal factorization, \autoref{thm:apophatic-name-boundary-citation-universal-factorization}. It makes every downstream citation pass through the displayed socket, request, refusal, ledger, transport, continuation, provenance, and local naming rows before any consumer can treat the packet as accepted.

The sibling surfaces \autoref{ch:concrete-instances-apophaticsocket-namecert} and \autoref{ch:concrete-instances-apophaticgatequestion-namecert} identify the socket and refusal gate as public boundary components. They therefore allow only citation of the finite rows and do not supply an internal object behind the socket.

Thus the public root-unblock citation surface is exactly the displayed package. Since no coordinate of that package is a generated witness, host datum, quotient representative, hidden theorem, assumed proposition, supplied object, or closure principle, no such item is exported to downstream consumers.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_unblock\_public\_citation\_surface}

\begin{theorem}[ApophaticName root public citation selector exclusion]
\label{thm:apophatic-name-root-public-citation-selector-exclusion}
For every accepted $\ApophaticNameUp$ packet
$$
A=(S,R,G,L,H,C,P,N),
$$
any root public citation that factors through $S,R,G,L,H,C,P,N$ cannot acquire a generated selector, supplied object, closure principle, quotient representative, hidden theorem, or host datum unless that item is already one of the displayed rows of the packet.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_public\_citation\_selector\_exclusion}
\begin{proof}
Start with \autoref{thm:apophatic-name-root-unblock-public-citation-surface}. It identifies the root public citation surface with the displayed packet $S,R,G,L,H,C,P,N$ and says downstream consumers receive only that boundary package.

Use the sibling surfaces cited there. \autoref{thm:apophatic-name-root-unblock-refusal-boundary} and \autoref{thm:apophatic-name-root-downstream-boundary-package} keep the root route on the refusal packet, while \autoref{thm:apophatic-name-boundary-citation-universal-factorization} forces each citation through the public socket, request, refusal, ledger, transport, continuation, provenance, and naming rows.

The sibling chapters \autoref{ch:concrete-instances-apophaticsocket-namecert} and \autoref{ch:concrete-instances-apophaticgatequestion-namecert} supply only public boundary components. Since none of the displayed coordinates is an undisplayed selector, supplied object, closure principle, quotient representative, hidden theorem, or host datum, the root citation cannot acquire such an item outside the packet.
\end{proof}

\begin{theorem}[ApophaticName two-step refusal-ledger induction]
\label{thm:apophatic-name-two-step-refusal-ledger-induction}
For every accepted $\ApophaticNameUp$ packet
$$
A=(S,R,G,L,H,C,P,N),
$$
any downstream root citation admitted by the packet is admitted by a two-step finite induction. The first step exposes $S,R$ as the socket and request boundary. The second step exposes $G,L$ as the refusal gate and refusal ledger. After those two steps, $H,C,P,N$ transport, replay, source, and name only that refusal package. The resulting citation surface excludes generated witnesses, supplied objects, quotient representatives, hidden theorems, host data, assumed propositions, and closure principles outside the displayed packet.
\end{theorem}
\begin{proof}
Start with the public citation surface of \autoref{thm:apophatic-name-root-unblock-public-citation-surface}. It identifies every downstream root citation with the finite packet $S,R,G,L,H,C,P,N$ and gives the consumer only that public boundary package.

Use \autoref{thm:apophatic-name-root-public-citation-selector-exclusion} to remove the hidden choices that could make the citation look like an internal supply route. In particular, no generated selector, supplied object, closure principle, quotient representative, hidden theorem, or host datum can be acquired outside the displayed rows.

Now read the package in two finite steps. First expose the socket and request rows $S,R$; the sibling chapter \autoref{ch:concrete-instances-apophaticsocket-namecert} identifies this pair as a public socket boundary rather than a supplied object.

Second expose the refusal gate and ledger rows $G,L$; the sibling chapter \autoref{ch:concrete-instances-apophaticgatequestion-namecert} identifies the gate read as a public refusal question rather than a hidden host theorem or assumed proposition. The remaining rows $H,C,P,N$ then only transport, replay, source, and name the same refusal package.

Thus the admitted downstream citation has the claimed induction shape: socket-request boundary first, refusal-gate ledger second, and only transport, replay, source, and naming afterward. Since both cited surface theorems exclude off-packet selectors and supplies, the citation cannot export generated witnesses, supplied objects, quotient representatives, hidden theorems, host data, assumed propositions, or closure principles.
\end{proof}
