\begin{theorem}[ApophaticName root-unblock refusal boundary]
\label{thm:apophatic-name-root-unblock-refusal-boundary}
Every downstream consumer of an accepted $\ApophaticNameUp$ packet
$$
A=(S,R,G,L,H,C,P,N)
$$
factors through the displayed socket-name row $S$, requested supply-shape row $R$, refusal gate $G$, refusal ledger $L$, componentwise $\hsame$ transports $H$, $\Cont$ routes $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\ApophaticNameUp}$ row $N$. No such consumer can read a generated witness, host object, quotient representative, hidden theorem, or closure principle from the packet.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:apophatic-name-carrier}. It displays exactly the socket, request, refusal gate, ledger, transport, continuation, provenance, and local naming rows.

Apply the refusal ledger theorem, \autoref{thm:apophatic-name-refusal-ledger}. The ledger names the permitted socket reads and records the barred conversions from requested supply to internal content.

Use the socket carrier theorem, \autoref{thm:apophatic-name-socket-carrier}. It identifies the downstream socket surface as the finite rows $S,R,G,L,H,C,P,N$ rather than an internal witness or supplied object.

Finally apply ledger nonescape, \autoref{thm:apophatic-name-ledger-nonescape}. Since every downstream read already factors through the displayed refusal ledger and the carrier has no extra coordinate for generated witnesses, host objects, quotient representatives, hidden theorems, or closure principles, carrier inspection leaves no other route.
\end{proof}

\begin{theorem}[ApophaticName root socket-gate factorization]
\label{thm:apophatic-name-root-socket-gate-factorization}
For every accepted $\ApophaticNameUp$ packet $A=(S,R,G,L,H,C,P,N)$, any root citation of the named externality factors through the socket row $S$, the requested supply-shape row $R$, the refusal gate $G$, and the refusal ledger $L$ before it can reach the structural rows $H,C,P,N$. The factorization is compatible with the sibling $\ApophaticSocketUp$ and $\ApophaticGateQuestionUp$ boundary surfaces and exports no generated witness, host datum, quotient representative, hidden theorem, or closure principle.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_socket\_gate\_factorization}
\begin{proof}
Begin with the carrier of \autoref{def:apophatic-name-carrier}. It displays the socket, request, refusal gate, and ledger rows as separate coordinates before the transport, continuation, provenance, and local naming rows.

Use the root-unblock refusal boundary, \autoref{thm:apophatic-name-root-unblock-refusal-boundary}. It already sends every downstream root citation through the same eight-row packet $S,R,G,L,H,C,P,N$.

Now read the sibling boundaries. The socket surface of \autoref{ch:concrete-instances-apophaticsocket-namecert} supplies the visible requested-supply shape, and the gate-question surface of \autoref{ch:concrete-instances-apophaticgatequestion-namecert} supplies the visible refusal question. Together they force the citation through $S,R,G,L$ before any structural replay row is used.
\end{proof}

\begin{theorem}[ApophaticName root refusal-ledger exhaustion]
\label{thm:apophatic-name-root-refusal-ledger-exhaustion}
For every accepted $\ApophaticNameUp$ packet $A=(S,R,G,L,H,C,P,N)$, the refusal ledger row $L$ exhausts the root-level barred conversions for the named externality. Any root consumer that asks for a supplied object, generated witness, host datum, quotient representative, hidden theorem, or closure principle is classified by the displayed gate $G$ and refused by $L$, with only $H,C,P,N$ available for transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Start from the refusal ledger theorem, \autoref{thm:apophatic-name-refusal-ledger}. It records the permitted socket reads and the barred conversions from requested supply to internal content.

Apply the obligation surface, \autoref{thm:apophatic-name-obligation-surface}. The local NameCert rows include carrier admission, classifier stability, ledger exactness, continuation provenance, and non-escape for the same packet.

Use the root socket-gate factorization, \autoref{thm:apophatic-name-root-socket-gate-factorization}. Since every root citation reaches $G$ and $L$ before structural replay, no separate root-level conversion remains outside the displayed refusal ledger.
\end{proof}

\begin{theorem}[ApophaticName root downstream socket non-escape]
\label{thm:apophatic-name-root-downstream-socket-nonescape}
Every downstream consumer that reads the root socket of an accepted $\ApophaticNameUp$ packet factors through the displayed package
$$
S,R,G,L,H,C,P,N.
$$
The read may cite the sibling $\ApophaticSocketUp$ and $\ApophaticGateQuestionUp$ boundaries as public socket and gate surfaces, but it cannot turn either sibling citation into a supplied object, generated witness, host datum, quotient representative, hidden theorem, or closure principle.
\end{theorem}
\begin{proof}
First use the root-unblock refusal boundary, \autoref{thm:apophatic-name-root-unblock-refusal-boundary}. It confines downstream root consumers to the displayed socket, request, gate, ledger, transport, route, provenance, and naming rows.

Next use root refusal-ledger exhaustion, \autoref{thm:apophatic-name-root-refusal-ledger-exhaustion}. It says that root-level attempts to internalize the named externality are already classified by $G$ and refused by $L$.

Finally compare with the sibling socket and gate-question boundaries. They make the socket and gate publicly citeable, but the ApophaticName packet supplies only the refusal package $S,R,G,L,H,C,P,N$. Thus a downstream socket read cannot escape into a supplied object or hidden proof.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_downstream\_socket\_nonescape}
