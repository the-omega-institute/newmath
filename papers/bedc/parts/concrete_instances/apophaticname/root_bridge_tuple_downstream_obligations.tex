\begin{theorem}[ApophaticName root bridge tuple source]
\label{thm:apophatic-name-root-bridge-tuple-source}
Every accepted $\ApophaticNameUp$ root bridge tuple exposes the socket row $S$, gate-question row $R$, externality refusal row $G$, refusal ledger $L$, $\BHist$ history rows, componentwise $\hsame$ row $H$, displayed $\Cont$ route $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\ApophaticNameUp}$ row $N$ before any downstream refusal read. No consumer receives a root bridge tuple whose first readable coordinate is an internalized object, generated witness, host datum, quotient representative, hidden theorem, or closure principle.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_source}
\begin{proof}
Start from root boundary package exhaustion, \autoref{thm:apophatic-name-root-boundary-package-exhaustion}. It states that downstream root consumers receive exactly the finite packet $S,R,G,L,H,C,P,N$.

Apply the refusal ledger and root ledger coverage rows, \autoref{thm:apophatic-name-refusal-ledger} and \autoref{thm:apophatic-name-root-ledger-coverage}. They force each refusal read to pass through $G,L$ after the visible socket and request rows have already been displayed.

Use the sibling socket and gate surfaces, \autoref{thm:apophatic-socket-consumer-surface} and \autoref{thm:apophatic-gate-question-visible-socket-nonescape}. These identify the socket and gate-question sides as public boundary rows, not supplied objects.

The structural rows $H,C,P,N$ replay that same boundary packet through componentwise transport, continuation, provenance, and local naming. Thus the root bridge tuple exposes all source rows before any refusal read and exports no hidden supply channel.
\end{proof}

\begin{theorem}[ApophaticName root bridge tuple classifier]
\label{thm:apophatic-name-root-bridge-tuple-classifier}
For every accepted $\ApophaticNameUp$ root bridge tuple, classifier comparison is exhausted by the displayed socket row $S$, gate-question row $R$, refusal gate $G$, refusal ledger $L$, transport row $H$, route row $C$, provenance row $P$, and local naming row $N$. The classifier cannot compare against hidden supply, host realization, quotient representative, generated witness, theorem cache, or closure row outside the tuple.
\end{theorem}
\begin{proof}
Project the finite tuple from \autoref{thm:apophatic-name-root-bridge-tuple-source}. The source, gate, refusal, transport, route, provenance, and naming rows are already present before classifier comparison begins.

Use classifier stability and request ledger classifier exhaustion, \autoref{thm:apophatic-name-classifier-stability} and \autoref{thm:apophatic-name-request-ledger-classifier-exhaustion}. They keep comparison on the displayed socket/request/refusal rows and on their componentwise transports.

Apply the visible request and socket-boundary route rows, \autoref{thm:apophatic-name-visible-request-factorization} and \autoref{thm:apophatic-name-socket-boundary-route-exhaustion}. These rows leave no second comparison source behind the public boundary citation.

The sibling gate chapter, \autoref{thm:apophatic-gate-question-visible-socket-nonescape}, supplies only the visible refusal question. Therefore classifier comparison is exhausted by the root bridge tuple itself.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_classifier}

\begin{theorem}[ApophaticName root bridge tuple ledger]
\label{thm:apophatic-name-root-bridge-tuple-ledger}
Every downstream consumer of an accepted $\ApophaticNameUp$ root bridge tuple reads the refusal ledger $L$ and the non-internalization rows before any externality handoff. The handoff may carry the socket, gate-question, refusal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, but it cannot deliver a supplied dependency, internal object, host proof, quotient representative, hidden theorem, generated witness, or closure principle.
\end{theorem}
\begin{proof}
Begin with root ledger coverage and root citation nonescape, \autoref{thm:apophatic-name-root-ledger-coverage} and \autoref{thm:apophatic-name-root-citation-nonescape}. They make $G,L$ the rows that answer downstream package reads and keep exported citations on the same finite packet.

Next apply downstream noninternalization and downstream nonimport, \autoref{thm:apophatic-name-downstream-noninternalization-package} and \autoref{thm:apophatic-name-downstream-nonimport}. These rows bar a downstream consumer from treating the requested supply as internal content.

Use the externality handoff theorem, \autoref{thm:apophatic-name-externality-handoff}. Its handoff object is the displayed refusal boundary, replayed through $H,C,P,N$.

Combining these rows with the source and classifier bridge tuple theorems above shows that every downstream consumer reads the ledger and non-internalization boundary before the externality handoff, with no hidden supply coordinate left to export.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_ledger}

\begin{theorem}[ApophaticName root bridge tuple noninternalization]
\label{thm:apophatic-name-root-bridge-tuple-noninternalization}
Every downstream consumer of an accepted $\ApophaticNameUp$ root bridge tuple must keep the socket row $S$, gate-question row $R$, refusal row $G$, transport row $H$, route row $C$, provenance row $P$, and local $\NameCert_{\ApophaticNameUp}$ row $N$ as boundary rows. The consumer may read the refusal ledger $L$ through the same packet, but it cannot internalize the requested supply as an object, generated witness, host datum, quotient representative, hidden theorem, or closure principle.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_noninternalization}
\begin{proof}
Start with the source, classifier, and ledger rows above. Theorems~\ref{thm:apophatic-name-root-bridge-tuple-source}, \ref{thm:apophatic-name-root-bridge-tuple-classifier}, and~\ref{thm:apophatic-name-root-bridge-tuple-ledger} expose the tuple as $S,R,G,L,H,C,P,N$ and exhaust both comparison and downstream ledger reads on those displayed coordinates.

Use the sibling socket and gate nonescape surfaces. Theorems~\ref{thm:apophatic-socket-consumer-surface} and~\ref{thm:apophatic-gate-question-visible-socket-nonescape} keep the socket and gate-question sides visible as boundary rows rather than supplied content.

Finally apply the downstream noninternalization and externality handoff rows, \autoref{thm:apophatic-name-downstream-noninternalization-package} and \autoref{thm:apophatic-name-externality-handoff}. They keep the refusal answer on the displayed transport, route, provenance, and local naming rows, so no downstream consumer can reclassify the requested supply as an internal object, generated witness, host datum, quotient representative, hidden theorem, or closure principle.
\end{proof}

\begin{theorem}[ApophaticName root bridge tuple source-route totality]
\label{thm:apophatic-name-root-bridge-tuple-source-route-totality}
Every accepted $\ApophaticNameUp$ root bridge tuple exhausts its source route by the rows
$$
S,R,G,L,H,C,P,N
$$
before any downstream refusal read. The downstream consumer may inspect the socket, gate-question, refusal, ledger, transport, route, provenance, and local naming rows, but it receives no supplied dependency, internal object, generated witness, host datum, quotient representative, hidden theorem, or closure principle.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_source\_route\_totality}
\begin{proof}
Start with the source and classifier tuple theorems, \autoref{thm:apophatic-name-root-bridge-tuple-source} and \autoref{thm:apophatic-name-root-bridge-tuple-classifier}. They expose the tuple before downstream reading as the finite socket, gate-question, refusal, ledger, transport, route, provenance, and naming surface.

Next apply the ledger theorem and noninternalization theorem, \autoref{thm:apophatic-name-root-bridge-tuple-ledger} and \autoref{thm:apophatic-name-root-bridge-tuple-noninternalization}. These force the refusal read through $G,L$ while keeping $S,R,H,C,P,N$ as boundary rows rather than supplied content.

Use the sibling socket and gate surfaces, \autoref{thm:apophatic-socket-consumer-surface} and \autoref{thm:apophatic-gate-question-visible-socket-nonescape}. The source side is therefore already public as a socket and visible gate question before any downstream refusal consumer can ask for an externality handoff.
\end{proof}

\begin{theorem}[ApophaticName root bridge tuple gate-refusal interface]
\label{thm:apophatic-name-root-bridge-tuple-gate-refusal-interface}
Every externality handoff from an accepted $\ApophaticNameUp$ root bridge tuple must read the gate-question row $R$ and refusal rows $G,L$ before the handoff is public. The interface is the finite route through $S,R,G,L,H,C,P,N$ and not a supplied object, host proof, generated witness, quotient representative, hidden theorem, or closure principle.
\end{theorem}
\begin{proof}
Begin with source-route totality from \autoref{thm:apophatic-name-root-bridge-tuple-source-route-totality}. It fixes the handoff source route as $S,R,G,L,H,C,P,N$ before downstream refusal reading begins.

Apply the root bridge tuple ledger theorem, \autoref{thm:apophatic-name-root-bridge-tuple-ledger}. It makes $L$ and the non-internalization rows part of the route before the externality handoff, so the refusal answer is not read after the handoff as hidden content.

Finally use the sibling gate surface, \autoref{thm:apophatic-gate-question-visible-socket-nonescape}, together with the externality handoff theorem cited in \autoref{thm:apophatic-name-root-bridge-tuple-ledger}. The gate question and refusal are read as visible boundary rows, and the handoff carries only the displayed tuple.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_gate\_refusal\_interface}

\begin{theorem}[ApophaticName root bridge tuple transport-naming exhaustion]
\label{thm:apophatic-name-root-bridge-tuple-transport-naming-exhaustion}
For every downstream consumer of an accepted $\ApophaticNameUp$ root bridge tuple, the transport row $H$, continuation row $C$, provenance row $P$, and local naming row $N$ are exhausted as boundary infrastructure for the route $S,R,G,L,H,C,P,N$. They cannot be reread as supplied dependency content, an internal object, a generated witness, host datum, quotient representative, hidden theorem, or closure principle.
\end{theorem}
\begin{proof}
First combine the source, classifier, ledger, and noninternalization tuple theorems above. Together they show that $H,C,P,N$ only replay the already displayed socket, gate-question, refusal, and ledger rows.

Next use the gate-refusal interface of \autoref{thm:apophatic-name-root-bridge-tuple-gate-refusal-interface}. Since the externality handoff has already consumed $R,G,L$ through the finite route, the structural rows cannot be reopened as a second source of supplied content.

Finally apply the sibling socket and gate surfaces, \autoref{thm:apophatic-socket-consumer-surface} and \autoref{thm:apophatic-gate-question-visible-socket-nonescape}. They keep the source and gate sides visible, so transport, continuation, provenance, and local naming remain route witnesses rather than downstream payload.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_transport\_naming\_exhaustion}

\begin{theorem}[ApophaticName root bridge tuple terminal exhaustion]
\label{thm:apophatic-name-root-bridge-tuple-terminal-exhaustion}
Every downstream externality handoff from an accepted $\ApophaticNameUp$ root bridge tuple is exhausted by the finite rows $S,R,G,L,H,C,P,N$. The handoff cannot export supplied dependency content, an internal object, a generated witness, host datum, quotient representative, hidden theorem, or closure principle outside those rows.
\end{theorem}
\begin{proof}
Start with the source, classifier, ledger, and noninternalization tuple theorems. They expose the bridge tuple as $S,R,G,L,H,C,P,N$ and force comparison, refusal reading, and downstream package reading through the displayed boundary rows.

Next apply \autoref{thm:apophatic-name-root-bridge-tuple-gate-refusal-interface}. The gate question and refusal ledger are consumed before the externality handoff is public, so the handoff cannot reopen $G$ or $L$ as supplied content.

Finally use \autoref{thm:apophatic-name-root-bridge-tuple-transport-naming-exhaustion}. The rows $H,C,P,N$ only replay transport, continuation, provenance, and local naming for the same finite route. Therefore every downstream externality handoff is exhausted by $S,R,G,L,H,C,P,N$ and has no hidden export coordinate.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_terminal\_exhaustion}

\begin{theorem}[ApophaticName downstream refusal uniqueness]
\label{thm:apophatic-name-root-bridge-tuple-downstream-refusal-uniqueness}
Every downstream externality handoff from an accepted $\ApophaticNameUp$ root bridge tuple has a unique refusal-reading route through
$$
S,R,G,L,H,C,P,N.
$$
If a second downstream route has the same socket row $S$ and gate-question row $R$, then it agrees on the refusal row $G$, refusal ledger $L$, transport row $H$, continuation route $C$, provenance row $P$, and local naming row $N$. Neither route can expose supplied content, host proof, quotient representative, hidden theorem, closure principle, internalized object, or generated witness.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_downstream\_refusal\_uniqueness}
\begin{proof}
First fix the source side. Apply \autoref{thm:apophatic-name-root-bridge-tuple-source-route-totality} to any downstream externality handoff. The handoff begins on the displayed socket and gate-question rows and then has only the route $S,R,G,L,H,C,P,N$ available before refusal reading.

Next read the refusal interface. By \autoref{thm:apophatic-name-root-bridge-tuple-gate-refusal-interface}, the same socket and gate-question rows force the handoff through the same refusal gate and ledger rows $G,L$ before the handoff is public.

Now compare the structural tail. The transport-naming exhaustion theorem \autoref{thm:apophatic-name-root-bridge-tuple-transport-naming-exhaustion} says that $H,C,P,N$ only replay the displayed refusal boundary. Therefore a second route with the same $S$ and $R$ has no alternative transport, continuation, provenance, or local naming row to select.

Finally apply terminal exhaustion, \autoref{thm:apophatic-name-root-bridge-tuple-terminal-exhaustion}, together with the sibling socket and gate surfaces \autoref{thm:apophatic-socket-consumer-surface} and \autoref{thm:apophatic-gate-question-visible-socket-nonescape}. These keep both routes on the visible boundary rows and exclude supplied content, host proof, quotient representatives, hidden theorems, closure principles, internalized objects, and generated witnesses.
\end{proof}

\begin{theorem}[ApophaticName root bridge tuple audit normal form]
\label{thm:apophatic-name-root-bridge-tuple-audit-normal-form}
Every accepted $\ApophaticNameUp$ root bridge tuple has audit normal form exactly
$$
S,R,G,L,H,C,P,N.
$$
The source and classifier read the socket, gate-question, and refusal rows; the ledger and noninternalization rows prevent supplied-content export; and terminal exhaustion leaves no hidden supply, quotient representative, generated witness, host datum, theorem cache, or closure principle.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_audit\_normal\_form}
\begin{proof}
First expose the source and classifier rows. Apply \autoref{thm:apophatic-name-root-bridge-tuple-source} and \autoref{thm:apophatic-name-root-bridge-tuple-classifier}; both reads factor through $S,R,G,L,H,C,P,N$ and never through hidden supply or host realization data.

Next bind the ledger and noninternalization boundary. Use \autoref{thm:apophatic-name-root-bridge-tuple-ledger} and \autoref{thm:apophatic-name-root-bridge-tuple-noninternalization}. These rows force downstream package reads through the refusal ledger and prevent the requested supply from becoming an internal object, generated witness, host datum, quotient representative, hidden theorem, or closure principle.

Now assemble the route interface. The source-route, gate-refusal, and transport-naming rows in \autoref{thm:apophatic-name-root-bridge-tuple-source-route-totality}, \autoref{thm:apophatic-name-root-bridge-tuple-gate-refusal-interface}, and \autoref{thm:apophatic-name-root-bridge-tuple-transport-naming-exhaustion} keep $H,C,P,N$ attached to the same visible socket, gate, refusal, and ledger rows.

Finally apply terminal exhaustion and downstream refusal uniqueness, \autoref{thm:apophatic-name-root-bridge-tuple-terminal-exhaustion} and \autoref{thm:apophatic-name-root-bridge-tuple-downstream-refusal-uniqueness}, together with the sibling ApophaticSocket and ApophaticGateQuestion surfaces \autoref{thm:apophatic-socket-consumer-surface} and \autoref{thm:apophatic-gate-question-visible-socket-nonescape}. No second route can add supplied content, quotient representatives, generated witnesses, host data, theorem caches, or closure principles, so the audit normal form is exactly $S,R,G,L,H,C,P,N$.
\end{proof}

\begin{corollary}[ApophaticName root bridge tuple source strict obstruction]
\label{cor:apophatic-name-root-bridge-tuple-source-strict-obstruction}
For every accepted $\ApophaticNameUp$ root bridge tuple, the obstruction carried by the gate-question and refusal rows is strictly local to the displayed route
$$
S,R,G,L,H,C,P,N.
$$
It blocks exactly the downstream converse or reuse attempt that would reread the handoff source as an internalized object, supplied dependency, generated witness, host datum, quotient representative, hidden theorem, closure principle, or hidden external validation. It does not assert a global impossibility theorem, a host theorem, or an ambient validation result outside the accepted tuple.
\end{corollary}
\begin{proof}
Start with the tuple source, \autoref{thm:apophatic-name-root-bridge-tuple-source}. It exposes $S,R,G,L,H,C,P,N$ before any downstream refusal read and states that no consumer receives a tuple whose first readable coordinate is an internalized object, generated witness, host datum, quotient representative, hidden theorem, or closure principle. Thus the only source available to a consumer-facing obstruction is the displayed finite tuple.

Next apply the gate-refusal interface, \autoref{thm:apophatic-name-root-bridge-tuple-gate-refusal-interface}. It forces every externality handoff to read the gate-question row $R$ and refusal rows $G,L$ before the handoff is public, and it identifies the interface as the same route $S,R,G,L,H,C,P,N$ rather than a supplied object, host proof, generated witness, quotient representative, hidden theorem, or closure principle.

The strictness follows by separating the two directions. In the permitted direction, a downstream consumer may cite the socket, gate-question, refusal, ledger, transport, continuation, provenance, and local naming rows as the public source of the handoff. In the barred converse direction, the same consumer tries to turn that handoff back into supplied content or to reuse $H,C,P,N$ as a second validation channel. The source theorem leaves no first coordinate for such a rereading, and the gate-refusal interface has already consumed $R,G,L$ as refusal rows before public handoff.

Therefore the obstruction blocks only that named converse or reuse route through the accepted root bridge tuple. Since every cited row remains inside $S,R,G,L,H,C,P,N$, the proof supplies no statement about all possible ambient hosts, no theorem object outside the tuple, and no hidden external validation beyond the local refusal boundary.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticNameUp.ApophaticNameCarrier\_root\_bridge\_tuple\_source\_strict\_obstruction}
