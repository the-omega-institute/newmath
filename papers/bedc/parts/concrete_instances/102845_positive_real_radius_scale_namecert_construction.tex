\chapter{A Concrete Naming Certificate for $\PositiveRealRadiusScaleUp$}
\label{ch:concrete-instances-positive-real-radius-scale-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and the Bourbaki--Kelley treatment of uniform structures, the $\PositiveRealRadiusScaleUp$ packet names the finite positive-radius interface that the Real uniformity exposes to completion-facing consumers. It sits between the Real uniform structure of \autoref{ch:concrete-instances-real-uniform-structure-namecert} and the Real entourage-base route of \autoref{ch:concrete-instances-real-uniform-entourage-base-namecert}: a consumer may ask for smaller positive radii, read the induced entourage row, and hand the resulting finite observation to Cauchy-filter routes without importing selected limits, quotient real equality, trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[PositiveRealRadiusScale carrier]
\label{def:positive-real-radius-scale-carrier}
A $\PositiveRealRadiusScaleUp$ carrier is a finite $\BHist$ packet
$$
\rho_{\Real}=(U,E,D,S,Q,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\RealUniformStructureUp$ source row whose metric-ball route supplies positive Real-radius requests.
\item $E$ is the $\RealUniformEntourageBaseUp$ row that receives each accepted positive radius as an entourage-basis request.
\item $D$ is the $\DyadicRatCoreUp$ positive-radius ledger, recording the admitted dyadic radii, strict positivity witnesses, smaller-radius refinements, and comparison cells used by $U$ and $E$.
\item $S$ is the $\StreamNameUp$ finite-window row inspected by Real-name and completion consumers when radius requests are read through finite observations.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to $S$ and to the dyadic radius comparisons in $D$.
\item $F$ is the $\CauchyFilterUp$ handoff row that consumes the displayed positive-radius entourage route before completion-facing filters are inspected.
\item $H$ records componentwise $\hsame$ transport for the Real uniform source, Real entourage-base route, dyadic positive-radius ledger, finite windows, rational readback, Cauchy-filter handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a positive-radius request through $U,D,E,S,Q,F,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\PositiveRealRadiusScaleUp$, $\RealUniformStructureUp$, $\RealUniformEntourageBaseUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchyFilterUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PositiveRealRadiusScaleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed tuple $U,E,D,S,Q,F,H,C,P,N$. The carrier has no coordinate for selected limits, quotient real equality, trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[PositiveRealRadiusScale NameCert obligations]
\label{thm:positive-real-radius-scale-namecert-obligations}
For every accepted $\PositiveRealRadiusScaleUp$ carrier
$$
\rho_{\Real}=(U,E,D,S,Q,F,H,C,P,N),
$$
the local $\NameCert_{\PositiveRealRadiusScaleUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the RealUniformStructure source row, RealUniformEntourageBase target row, DyadicRatCore positive-radius ledger, StreamName finite windows, RegSeqRat readback, CauchyFilter handoff, transport, replay, provenance, and local naming row;
\item positive-radius admission, requiring $D$ to display strict positivity and a dyadic comparison cell before a radius request can be consumed by the Real uniform source $U$;
\item smaller-radius refinement, requiring every accepted radius request to expose a displayed smaller positive radius in $D$ before it is routed to the entourage-base row $E$;
\item finite-window readback, requiring $S$ and $Q$ to record the finite StreamName window and regular rational readback attached to each accepted radius request;
\item Cauchy-filter handoff, requiring $F$ to inspect the displayed route $U,D,E,S,Q$ before any completion-facing consumer reads filter data;
\item classifier transport, requiring $H$ and $C$ to preserve the displayed route $U,D,E,S,Q,F$ componentwise under $\hsame$ and replay it under $\Cont$;
\item ledger non-escape, requiring $P$ and $N$ to export no selected limit, quotient real equality, trichotomy, countable-choice selector, open-cover compactness principle, or ambient Real completeness row.
\end{enumerate}
No local NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:positive-real-radius-scale-carrier}. The accepted packet displays exactly the Real uniform source, the Real entourage-base route, the dyadic positive-radius ledger, finite windows, rational readback, Cauchy-filter handoff, and the structural rows $H,C,P,N$.

Positive-radius admission is read from $D$. A radius request reaches the Real uniform source $U$ only after the ledger has displayed strict positivity and the dyadic comparison cell attached to that request.

Smaller-radius refinement is also internal to $D$. The refinement row is then routed to $E$, so the Real entourage-base consumer receives a displayed smaller positive radius rather than an unrecorded completeness or choice principle.

The finite-window readback rows $S$ and $Q$ attach the radius request to observable StreamName windows and regular rational readback. Thus the Real-name side and the completion-facing side inspect the same finite radius data.

The Cauchy-filter handoff is downstream of the radius and entourage rows. A consumer reaches $F$ only after $U,D,E,S,Q$ have been replayed.

Finally, $H,C,P,N$ add only transport, replay, provenance, and naming. Since selected limits, quotient real equality, trichotomy, countable choice, open-cover compactness, and ambient Real completeness are not rows of $\rho_{\Real}$, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[PositiveRealRadiusScale entourage handoff]
\label{thm:positive-real-radius-scale-entourage-handoff}
Every Real-uniform or Cauchy-filter consumer of an accepted $\PositiveRealRadiusScaleUp$ carrier reads the positive-radius scale only through the displayed rows
$$
\RealUniformStructureUp,\RealUniformEntourageBaseUp,\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\CauchyFilterUp,\BHist,\hsame,\Cont,\Pkg,\NameCert .
$$
Consequently the packet exports no selected limit, quotient real equality, trichotomy, countable-choice selector, open-cover compactness principle, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a consumer of $\rho_{\Real}$. Carrier admission exposes the packet only through $U,E,D,S,Q,F,H,C,P,N$.

The positive-radius request is first read through the Real uniform source $U$ and the dyadic ledger $D$. The ledger provides positivity and refinement data before the request can enter the Real entourage-base row $E$.

Finite observations are then read through $S$ and $Q$. These rows attach the radius route to StreamName windows and RegSeqRat readback, so the route remains visible to Real-name and completion-facing consumers.

Any Cauchy-filter use is downstream of $F$. The handoff row receives the already displayed Real uniform and entourage-base route; it does not select a limit or add a quotient equality coordinate.

The remaining rows are structural. The $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows transport, replay, source, and name the same finite route, so there is no separate coordinate from which selected limits, quotient real equality, trichotomy, countable choice, open-cover compactness, or ambient Real completeness can be exported.
\end{proof}

\closureat{PositiveRealRadiusScaleUp}{seedStr}

\begin{closurestatus}{\PositiveRealRadiusScaleUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:positive-real-radius-scale-carrier}, \autoref{thm:positive-real-radius-scale-namecert-obligations}, and \autoref{thm:positive-real-radius-scale-entourage-handoff} seed the finite positive-radius interface from RealUniformStructure through RealUniformEntourageBase to CauchyFilter consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient real equality, trichotomy, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-radius admission, smaller-radius refinement, finite-window readback, Cauchy-filter handoff, classifier transport, and ledger non-escape for BEDC.Derived.PositiveRealRadiusScaleUp.}
  \constructivestory{A $\PositiveRealRadiusScaleUp$ packet is generated from a RealUniformStructure source row, RealUniformEntourageBase route, DyadicRatCore positive-radius ledger, StreamName windows, RegSeqRat readback, CauchyFilter handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
