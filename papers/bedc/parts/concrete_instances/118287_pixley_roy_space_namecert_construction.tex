\chapter{A Concrete Naming Certificate for $\PixleyRoySpaceUp$}
\label{ch:concrete-instances-pixley-roy-space-namecert}
\origin{human}

Following Pixley and Roy on finite-set hyperspaces, the $\PixleyRoySpaceUp$ packet records the finite topology surface whose points are nonempty finite closed-subset rows and whose basic opens are hit windows of the form "the displayed finite row is kept inside a displayed open neighbourhood while meeting the named finite core." It sits next to the indexed-open carrier of \autoref{ch:concrete-instances-topology-namecert}, the observable-open classifier of \autoref{ch:concrete-instances-sierpinski-space-namecert}, the membership discipline of \autoref{ch:concrete-instances-setlike-namecert}, the finite-set surface of \autoref{ch:concrete-instances-finset-namecert}, and the lower-limit topology example \autoref{ch:concrete-instances-sorgenfrey-line-namecert}. The packet names only the finite hit-and-containment interface; it does not import a host powerset hyperspace, selected compact subsets, Vietoris completeness, countable choice, or ambient separation theorems.

\begin{definition}[Pixley--Roy space carrier]
\label{def:pixley-roy-space-carrier}
A $\PixleyRoySpaceUp$ carrier is a finite $\BHist$ packet
$$
P=(T,S,F,O,W,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the ambient $\TopologyUp$ indexed-open row for the source space.
\item $S$ is the $\SierpinskiSpaceUp$ observable-open truth row used to read membership in displayed opens.
\item $F$ is the nonempty finite closed-subset row, exposed through $\FinSetUp$ and $\SetLikeUp$ membership data rather than through a host powerset.
\item $O$ is the finite basic-open hit window. It displays a finite core row and an ambient open row, and it records that the finite closed-subset row lies inside the open row while hitting every named core coordinate.
\item $W$ is the finite observation schedule for the hit and containment reads.
\item $H$ records componentwise $\hsame$ transport for the topology, observable-open, finite closed-subset, hit-window, schedule, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an observable-open request through $T,S,F,O,W$.
\item $Q$ is the $\Pkg$ provenance over $\PixleyRoySpaceUp$, $\TopologyUp$, $\SierpinskiSpaceUp$, $\FinSetUp$, $\SetLikeUp$, $\SorgenfreyLineUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PixleyRoySpaceUp}$ row.
\end{enumerate}
The carrier has no coordinate for an arbitrary hyperspace powerset, a selected compact-subset representative, countable choice, host equality of closed sets, Vietoris completeness, or an ambient separation theorem.
\end{definition}
\leanchecked{BEDC.Derived.PixleyRoySpaceUp.PixleyRoySpaceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Pixley--Roy space NameCert obligations]
\label{thm:pixley-roy-space-namecert-obligations}
For every accepted $\PixleyRoySpaceUp$ carrier $P=(T,S,F,O,W,H,C,Q,N)$, the local $\NameCert_{\PixleyRoySpaceUp}$ surface consists exactly of ambient topology admission, Sierpinski observable-open admission, finite nonempty closed-subset admission through $\FinSetUp$ and $\SetLikeUp$, basic-open hit-window exactness, finite observation scheduling, componentwise transport, displayed replay, provenance, and local naming. Every public neighbourhood read must display $T,S,F,O,W$ before the structural rows $H,C,Q,N$ are consumed.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:pixley-roy-space-carrier}. The mathematical rows are exactly the ambient topology row $T$, the observable-open truth row $S$, the finite closed-subset row $F$, the hit-window row $O$, and the finite observation schedule $W$.

The finite-subset admission is local. The row $F$ is read through the displayed $\FinSetUp$ and $\SetLikeUp$ membership surfaces, so nonemptiness, membership, and closed-subset evidence are visible finite data rather than a host powerset element.

The basic-open obligation is also local. The row $O$ records the finite core and ambient open row, while $S$ supplies observable-open truth for the containment and hit reads scheduled by $W$.

Finally $H,C,Q,N$ transport, replay, source, and name the same rows. Since none of the excluded objects is a coordinate of the carrier, the local NameCert surface is exhausted by the listed finite obligations.
\end{proof}

\begin{theorem}[Pixley--Roy finite-open handoff]
\label{thm:pixley-roy-finite-open-handoff}
Every $\TopologyUp$, $\SierpinskiSpaceUp$, compactness-boundary, or lower-limit-topology consumer of an accepted $\PixleyRoySpaceUp$ packet receives a neighbourhood only through the finite route
$$
\TopologyUp \longrightarrow \SierpinskiSpaceUp \longrightarrow \FinSetUp,\SetLikeUp \longrightarrow O \longrightarrow W .
$$
The route exposes finite nonempty closed-subset rows and finite basic-open hit windows before any downstream read is public; it exports no host powerset of closed sets, selected compact-subset representative, countable choice, Vietoris completeness, or ambient separation theorem.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:pixley-roy-space-namecert-obligations}. Any neighbourhood read must first open the ambient indexed topology row and then read observable-open truth through the Sierpinski row.

The finite closed-subset row is then inspected through $\FinSetUp$ and $\SetLikeUp$. Only after those membership rows are displayed can the hit-window row $O$ record that the named finite core is hit and that the displayed finite row stays inside the ambient open.

The schedule $W$ lists the finite observations used by the handoff, while $H,C,Q,N$ preserve the same route under transport, replay, provenance, and local naming. Thus a consumer such as the lower-limit topology surface of \autoref{ch:concrete-instances-sorgenfrey-line-namecert} receives a finite-open Pixley--Roy packet, not a hidden hyperspace or an external compactness principle.
\end{proof}

\closureat{PixleyRoySpaceUp}{seedStr}

\begin{closurestatus}{\PixleyRoySpaceUp}
  \constructivestory{A $\PixleyRoySpaceUp$ packet is generated from a Topology indexed-open row, a SierpinskiSpace observable-open row, a finite nonempty closed-subset row, a finite basic-open hit window, a finite observation schedule, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pixley-roy-space-carrier}, \autoref{thm:pixley-roy-space-namecert-obligations}, and \autoref{thm:pixley-roy-finite-open-handoff} seed the finite Pixley--Roy hyperspace interface over Topology, SierpinskiSpace, FinSet, SetLike, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host powerset hyperspace, selected compact-subset representatives, countable choice, host equality of closed sets, Vietoris completeness, ambient separation theorems, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked ambient topology admission, observable-open admission, finite closed-subset admission, basic-open hit-window exactness, schedule exhaustion, transport, replay, provenance, and local naming for BEDC.Derived.PixleyRoySpaceUp.}
\end{closurestatus}
