\chapter{A Concrete Naming Certificate for $\SeriesDivergenceTestUp$}
\label{ch:concrete-instances-series-divergence-test-namecert}
\origin{human}

Following Cauchy and Bishop--Bridges constructive analysis, the packet records the nth-term divergence test as a finite BEDC refusal boundary: a displayed nonzero tail of a $\SeriesUp$ source prevents a $\RegSeqRatUp$ handoff and hence prevents a $\RealUp$ seal. It reads the series source of \autoref{ch:concrete-instances-series-namecert}, the Cauchy-facing obstruction of \autoref{ch:concrete-instances-cauchycriterion-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic comparison rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular-rational handoff boundary of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a necessary-condition refusal; it does not import quotient series equality, selected sums, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Series divergence test carrier]\label{def:series-divergence-test-carrier}
A $\SeriesDivergenceTestUp$ carrier is a finite $\BHist$ packet
$$
S=(A,W,D,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\SeriesUp$ source, read only through the finite term observations admitted by the series packet.
\item $W$ is the finite tail-window ledger, recording the selected later windows in which the necessary tail-zero condition is being tested.
\item $D$ is the displayed nonzero or apartness row, carrying a later term observation that remains apart from zero inside the accepted tail window.
\item $T$ is the $\DyadicRatCoreUp$ tolerance comparison, recording the dyadic zero-neighborhood, apartness witness, and finite comparison budget used by $D$.
\item $R$ is the refused $\RegSeqRatUp$ handoff boundary, exposed exactly where a regular Cauchy consumer would have to discharge the tail-zero condition.
\item $E$ is the refused $\RealUp$ seal boundary, downstream of $R$ and available only as a named obstruction to a real endpoint seal.
\item $H$ stores componentwise $\hsame$ transport for the source, tail-window ledger, nonzero row, dyadic comparison, refused regular handoff, refused real seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from the series source to the tail windows, from the tail windows to the nonzero row and dyadic comparison, from that comparison to the refused regular handoff, and from the handoff boundary to the refused real seal boundary.
\item $P$ is the $\Pkg$ provenance over $\SeriesDivergenceTestUp$, $\SeriesUp$, $\CauchyCriterionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SeriesDivergenceTestUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,W,D,T,R,E,H,C,P,N$. It has no coordinate for selected sums, quotient series equality, host real convergence, countable-choice selectors, trichotomy, ambient $\RealUp$ completeness, Lean verification, or a standard bridge.
\end{definition}

\begin{theorem}[Series divergence test NameCert obligations]\label{thm:series-divergence-test-namecert-obligations}
For every accepted $\SeriesDivergenceTestUp$ carrier $S=(A,W,D,T,R,E,H,C,P,N)$, the local $\NameCert_{\SeriesDivergenceTestUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed series source, tail-window ledger, nonzero tail row, dyadic comparison, refused regular handoff boundary, refused real seal boundary, transport, replay, provenance, and local naming rows;
\item tail-window exposure, requiring every later term inspected by the refusal to pass through $A$ and $W$ before any handoff boundary is read;
\item nonzero-tail stability, requiring $D$ and $T$ to display the same later apartness witness under the accepted dyadic zero-neighborhood comparison;
\item $\RegSeqRatUp$ refusal, requiring $R$ to be reached exactly at the point where a regular Cauchy handoff would need tail-zero evidence;
\item $\RealUp$ nonescape, requiring $E$ to remain downstream of the refused regular handoff and to export no selected sum, quotient series equality, host real convergence, countable choice, trichotomy, ambient completeness, Lean verification, or bridge datum;
\item componentwise transport, displayed replay, provenance, and local naming for the same refusal route through $H,C,P,N$.
\end{enumerate}
No series divergence test obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted packet $S=(A,W,D,T,R,E,H,C,P,N)$. The carrier definition exposes exactly the series source, tail-window ledger, displayed nonzero row, dyadic comparison, two refusal boundaries, and structural rows.

The tail-window exposure row forces every later term used by the test to be read through $A$ and $W$. The nonzero-tail stability row then couples $D$ to the dyadic comparison $T$, so the displayed apartness witness is not detached from the tolerance budget that made it visible.

At the regular handoff boundary, the route reaches $R$. By the obligation surface, a consumer can pass this point only by supplying the missing tail-zero condition; the displayed nonzero row blocks that discharge inside the same finite window.

Finally $E,H,C,P,N$ preserve, replay, source, and name the same refusal route. Since no excluded analytic or choice object appears as a carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Series divergence test nonzero-tail refusal]\label{thm:series-divergence-test-nonzero-tail-refusal}
Let $S=(A,W,D,T,R,E,H,C,P,N)$ be an accepted $\SeriesDivergenceTestUp$ carrier. Any consumer attempting a $\RegSeqRatUp$ handoff or a $\RealUp$ seal from the source $A$ must first discharge the tail-zero condition for the displayed later windows. The nonzero tail row $D$, measured by the dyadic comparison $T$, blocks that route without quotient series equality, selected sums, countable choice, trichotomy, host real equality, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a consumer request for a regular handoff or real seal. The carrier exposes no direct endpoint coordinate from the series source; the only available route is
$$
A \longmapsto W \longmapsto D,T \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$

The request therefore reaches the tail-window ledger $W$ before it reaches either refusal boundary. To continue toward $R$, it must turn the inspected later windows into the tail-zero evidence required by a $\RegSeqRatUp$ handoff.

The displayed row $D$ supplies an apartness witness for one of those later terms, and $T$ records the dyadic comparison that keeps the witness visible. Thus the required tail-zero discharge is unavailable inside the accepted packet.

Since the attempted $\RealUp$ seal $E$ is downstream of the refused regular handoff, the same obstruction prevents endpoint sealing. The carrier has no slot for quotient series equality, selected sums, countable choice, trichotomy, host real equality, or ambient completeness, so none of those devices can bypass the finite refusal row.
\end{proof}

\falsifiablePrediction{Every $\SeriesDivergenceTestUp$ endpoint-facing request factors through a SeriesUp source, finite tail-window ledger, displayed nonzero tail row, DyadicRatCore comparison, refused RegSeqRat handoff boundary, refused RealUp seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{series, cauchycriterion, streamname, dyadicratcore, regseqrat, real: $\SeriesDivergenceTestUp$ is not bijective to any listed sibling because it jointly carries a source series row, finite tail windows, a displayed nonzero tail obstruction, dyadic apartness accounting, refused regular and real seal boundaries, transport, continuation, provenance, and local naming.}

\closureat{SeriesDivergenceTestUp}{seedStr}

\begin{closurestatus}{\SeriesDivergenceTestUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:series-divergence-test-carrier}, \autoref{thm:series-divergence-test-namecert-obligations}, and \autoref{thm:series-divergence-test-nonzero-tail-refusal} seed the finite constructive nth-term divergence refusal route from displayed nonzero tail data to RegSeqRat and RealUp handoff denial.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected sums, quotient series equality, host real convergence, countable choice, trichotomy, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tail-window exposure, nonzero-tail stability, RegSeqRat refusal, RealUp nonescape, transport, replay, provenance, and local naming for BEDC.Derived.SeriesDivergenceTestUp.}
  \constructivestory{A $\SeriesDivergenceTestUp$ packet is generated from a Series source, finite tail-window ledger, displayed nonzero tail row, DyadicRatCore comparison, refused RegSeqRat handoff boundary, refused RealUp seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
