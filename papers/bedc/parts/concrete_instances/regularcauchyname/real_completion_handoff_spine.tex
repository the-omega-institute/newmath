\begin{theorem}[RegularCauchyName RationalStream completion seal triangle]
\label{thm:regularcauchyname-rationalstream-completion-seal-triangle}
For every accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$, the $\RationalStreamUp$ source admission, normalized $\StreamNameUp$ finite window, $\RegSeqRatUp$ readback, $\RealUp$ seal, and $\CauchyCompletionMonadUp$ completion consumer route determine one finite $\BHist$ consumer surface. The surface is the root dependency manifest of \autoref{def:regularcauchyname-root-dependency-manifest} read through the public $\RealUp$ seal factorization of $\CauchyCompletionMonadUp$. It contains no quotient stream, selected limit, host real equality, independent real carrier, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
First assemble the source and window sides. The RationalStream source admission theorem \autoref{thm:regularcauchyname-rationalstream-source-admission} fixes the scheduled $\RationalStreamUp$ source, while the root dependency manifest exhaustion theorem \autoref{thm:regularcauchyname-root-dependency-manifest-exhaustion} places the normalized $\StreamNameUp$ window, $\RegSeqRatUp$ readback, and $\RealUp$ seal in the same displayed manifest.

Next attach the completion consumer. The public $\RealUp$ seal factorization for $\CauchyCompletionMonadUp$, \autoref{thm:cauchy-completion-monad-public-real-seal-factorization}, routes every completion-facing seal read through its finite schedule, window, dyadic observation, diagonal readback, seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Along the RegularCauchyName route, those rows are exactly the completion face already recorded by the manifest.

Finally apply the non-escape boundary of \autoref{thm:regularcauchyname-root-dependency-manifest-non-escape}. Since the manifest and the completion-monad seal factorization share the same finite provenance surface and neither exposes a quotient stream, selected limit, host real equality, independent real carrier, or ambient completeness coordinate, the admitted rational source and the completion consumer determine one finite $\BHist$ consumer surface.
\end{proof}

\begin{theorem}[RegularCauchyName Real-completion selector seal compatibility]
\label{thm:regularcauchyname-real-completion-selector-seal-compatibility}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$. Any selector-facing $\RealCompletenessUp$ or $\CauchyCompletionMonadUp$ consumer that reads the displayed finite window of $G$, its dyadic radius row, its $\RegSeqRatUp$ readback, and its $\RealUp$ seal factors through two displayed routes: the root dependency manifest of \autoref{def:regularcauchyname-root-dependency-manifest}, and the ordered selector route
$$
\mu,\Delta,D,W,Q,S
$$
of \autoref{thm:real-completeness-selector-window-seal-factorization}. Consequently the selector-presented seal and the $\RegularCauchyNameUp$ finite-window seal expose the same finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface. They expose no quotient stream, selected host limit, host equality, choice row, or ambient completeness theorem.
\end{theorem}
\begin{proof}
First read the $\RegularCauchyNameUp$ side. The root dependency manifest exhaustion theorem \autoref{thm:regularcauchyname-root-dependency-manifest-exhaustion} places every downstream source, finite-window, readback, $\RealUp$ seal, and completion-consumer read of $G$ inside the finite manifest of \autoref{def:regularcauchyname-root-dependency-manifest}.

Next attach the completion-facing seal. The RationalStream completion seal triangle \autoref{thm:regularcauchyname-rationalstream-completion-seal-triangle} identifies the admitted rational source, normalized window, $\RegSeqRatUp$ readback, $\RealUp$ seal, and $\CauchyCompletionMonadUp$ consumer route with one finite $\BHist$ consumer surface.

Now compare the selector presentation. The selector-window-seal factorization theorem \autoref{thm:real-completeness-selector-window-seal-factorization} orders every selector-facing $\RealCompletenessUp$ read as $\mu,\Delta,D,W,Q,S$, with transport, route replay, package provenance, and local naming carried by the same finite seal surface.

Finally apply the non-escape theorem \autoref{thm:regularcauchyname-root-dependency-manifest-non-escape}. Since both routes read only the displayed finite rows and the provenance coordinates $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, neither presentation can export a quotient stream, selected host limit, host equality, choice row, or ambient completeness theorem.
\end{proof}

\begin{theorem}[RegularCauchyName Real-completion handoff spine]
\label{thm:regularcauchyname-real-completion-handoff-spine}
Every accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$ factors all Real-and-Completion consumer reads through the displayed finite route: the $\RationalStreamUp$ source admission, finite $\StreamNameUp$ window, $\DyadicRatCoreUp$ radius row, pointwise $\RegSeqRatUp$ readback, $\RealUp$ seal, $\CauchyCompletionMonadUp$ completion consumer, and the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance rows. This route is the public handoff surface for Real-and-Completion consumers. It exports no quotient stream equality, selected limit, host real equality, choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
First locate every consumer read in the root manifest. The root dependency manifest exhaustion theorem \autoref{thm:regularcauchyname-root-dependency-manifest-exhaustion} places the source, finite window, dyadic radius row, pointwise readback, $\RealUp$ seal, completion consumer, and provenance coordinates inside the finite manifest of \autoref{def:regularcauchyname-root-dependency-manifest}.

Next compress the admitted RationalStream route to the completion-facing surface. The RationalStream completion seal triangle \autoref{thm:regularcauchyname-rationalstream-completion-seal-triangle} identifies the $\RationalStreamUp$ source admission, normalized $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, and $\CauchyCompletionMonadUp$ completion consumer as one finite $\BHist$ consumer surface.

Then compare this surface with the selector-facing Real-and-Completion presentation. The selector seal compatibility theorem \autoref{thm:regularcauchyname-real-completion-selector-seal-compatibility} carries the same finite route across every selector-facing $\RealCompletenessUp$ or $\CauchyCompletionMonadUp$ read, including the $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Finally apply the non-escape boundary of \autoref{thm:regularcauchyname-root-dependency-manifest-non-escape}. Since the manifest, completion seal triangle, and selector-compatible route expose only the displayed finite rows, no Real-and-Completion consumer read can acquire quotient stream equality, a selected limit, host real equality, a choice row, or an ambient completeness principle from this handoff.
\end{proof}

\begin{theorem}[RegularCauchyName Real-completion prefix determinacy]
\label{thm:regularcauchyname-real-completion-prefix-determinacy}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$. Suppose two Real-and-Completion consumer reads of $G$ use the same $\RationalStreamUp$ source admission, the same finite $\StreamNameUp$ window, the same $\DyadicRatCoreUp$ radius row, the same pointwise $\RegSeqRatUp$ readback, the same $\RealUp$ seal, and the same $\CauchyCompletionMonadUp$ consumer coordinate. Then both reads factor through the same finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface of the root dependency manifest of \autoref{def:regularcauchyname-root-dependency-manifest}. Consequently the two reads cannot diverge into quotient stream equality, selected host limits, host real equality, choice rows, or ambient completeness data.
\end{theorem}
\begin{proof}
First identify the shared finite surface. The RationalStream completion seal triangle \autoref{thm:regularcauchyname-rationalstream-completion-seal-triangle} turns the common source admission, finite window, readback, $\RealUp$ seal, and completion consumer coordinate into one finite $\BHist$ consumer surface.

Next compare the selector-facing presentations. The selector seal compatibility theorem \autoref{thm:regularcauchyname-real-completion-selector-seal-compatibility} routes any compatible Real-and-Completion consumer read through that same finite surface and through the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates.

Now apply the handoff spine \autoref{thm:regularcauchyname-real-completion-handoff-spine}. Since both reads have the same source, window, radius, readback, seal, and completion coordinate, the public handoff route leaves no second finite prefix through which either read can be separated.

Finally use the root manifest non-escape theorem \autoref{thm:regularcauchyname-root-dependency-manifest-non-escape}. The shared prefix exports only the displayed finite provenance rows, so neither read can branch into quotient stream equality, selected host limits, host real equality, choice rows, or ambient completeness data.
\end{proof}

\begin{theorem}[RegularCauchyName facewise exit handoff completeness]
\label{thm:regularcauchyname-facewise-exit-handoff-completeness}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$. Its Real-and-Completion handoff is complete precisely through the four displayed L10 faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, together with the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. Omitting any one of these faces, or replacing one by quotient stream equality, a selected host limit, host real equality, countable choice, ambient completeness, metric completeness, or a bridge row, lies outside the accepted handoff.
\end{theorem}
\begin{proof}
First build the displayed handoff surface. The handoff spine \autoref{thm:regularcauchyname-real-completion-handoff-spine} places every Real-and-Completion consumer read of $G$ on the source admission, finite $\StreamNameUp$ window, $\DyadicRatCoreUp$ radius row, $\RegSeqRatUp$ readback, $\RealUp$ seal, completion consumer, and provenance rows. The prefix determinacy theorem \autoref{thm:regularcauchyname-real-completion-prefix-determinacy} then shows that this visible prefix leaves no second finite surface through which two such reads can diverge.

Next identify the face list with the exit criterion. The tail-budget handoff theorem \autoref{thm:regularcauchyname-tail-budget-streamname-regseqrat-handoff} fixes the $\StreamNameUp$ and $\RegSeqRatUp$ side of the finite tail budget, while the Real L10 facewise status theorem \autoref{thm:real-l10-facewise-exit-status-noncollapse} records that the displayed Real exit faces are not interchangeable with quotient, choice, bridge, or host-completion substitutes.

Finally apply the root manifest non-escape theorem \autoref{thm:regularcauchyname-root-dependency-manifest-non-escape}. Since the accepted packet exports only the four displayed L10 faces and the listed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, removing a face or replacing it by any refused quotient, limit-selection, equality, choice, completeness, or bridge coordinate leaves the RegularCauchyName handoff rather than completing it.
\end{proof}

\begin{theorem}[RegularCauchyName Real-completion exit threshold equivalence]
\label{thm:regularcauchyname-real-completion-exit-threshold-equivalence}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$. The packet presents the current Phase Real and Completion exit threshold if and only if its accepted handoff exposes exactly the four local faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, each face carries its own local objectwise exit-threshold record, and the only support rows beyond those faces are the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the RegularCauchyName handoff.

Equivalently, the threshold is read object by object on the four displayed faces. It is not supplied by quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, metric completeness, an aggregate $\formalstatus$ row, an aggregate bridge row, or a standard-bridge substitute.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:regularcauchyname-facewise-exit-handoff-completeness}. Any accepted Real-and-Completion handoff for $G$ is already complete exactly through the four displayed faces and the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next apply \autoref{thm:regularcauchyname-tail-budget-streamname-regseqrat-handoff}. The $\StreamNameUp$ and $\RegSeqRatUp$ sides of the handoff remain stable under the certified tail-budget route, so the objectwise threshold read cannot be moved to a quotient stream, selected limit, host sequence equality, choice row, ambient completeness witness, or metric-completeness datum.

Use \autoref{thm:real-l10-objectwise-exit-threshold-equivalence} to identify the current Phase Real and Completion threshold. That result reads the threshold from the four local face records for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with each face carrying its own local theory-closure and audit-clean verification rows.

For the reverse direction, suppose the four displayed faces expose their local objectwise exit-threshold records and the packet carries only the stated $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. Apply \autoref{thm:real-l10-objectwise-exit-threshold-equivalence} to read those four records as the current exit threshold, and then apply \autoref{thm:regularcauchyname-facewise-exit-handoff-completeness} to identify the same four records as the complete RegularCauchyName handoff surface.

Finally apply \autoref{thm:real-l10-facewise-exit-status-noncollapse}. Aggregate route-level $\formalstatus$ rows, aggregate bridge rows, quotient streams, selected host limits, ambient completeness rows, metric-completeness rows, countable-choice rows, and standard-bridge substitutes cannot replace any one of the four local threshold faces, so the equivalence is exactly the displayed objectwise threshold.
\end{proof}

\begin{theorem}[RegularCauchyName exit-threshold nonescape boundary]
\label{thm:regularcauchyname-exit-threshold-nonescape-boundary}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$. If $G$ satisfies the Real-and-Completion exit threshold through the four displayed faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, then the same handoff does not supply quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, metric completeness, an aggregate $\formalstatus$ row, an aggregate $\leantarget$ row, aggregate bridge data, or a $\RealUp$ completeness claim.

Equivalently, the RegularCauchyName handoff witnesses only the objectwise L10 threshold on those four faces together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. Every stronger read listed above lies outside the accepted threshold surface.
\end{theorem}
\begin{proof}
First identify the positive threshold surface. By \autoref{thm:regularcauchyname-real-completion-exit-threshold-equivalence}, satisfying the Real-and-Completion exit threshold is exactly the exposure of the four local faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next compare that surface with the handoff completeness criterion. Apply \autoref{thm:regularcauchyname-facewise-exit-handoff-completeness}. The accepted handoff is complete precisely through the four displayed faces and refuses replacements by quotient stream equality, selected host limits, host real equality, countable choice, ambient completeness, metric completeness, or bridge rows.

Now fix the stream and sequence sides. The tail-budget handoff theorem \autoref{thm:regularcauchyname-tail-budget-streamname-regseqrat-handoff} keeps the $\StreamNameUp$ and $\RegSeqRatUp$ coordinates finite and local, so no quotient stream equality, selected limit, or host real equality is created by the same tail-budget handoff.

Finally read the Real L10 threshold objectwise. The objectwise equivalence \autoref{thm:real-l10-objectwise-exit-threshold-equivalence} keeps the exit criterion on the four local face records, and the facewise noncollapse theorem \autoref{thm:real-l10-facewise-exit-status-noncollapse} excludes aggregate status, aggregate target, aggregate bridge, and $\RealUp$ completeness readings from replacing those local records. Thus the RegularCauchyName handoff reaches the positive exit threshold without exporting any stronger Real-and-Completion data.
\end{proof}

\begin{theorem}[RegularCauchyName exit-threshold facewise locality]
\label{thm:regularcauchyname-exit-threshold-facewise-locality}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$. Satisfying the Real-and-Completion exit threshold is equivalent to carrying four independent local exit records on $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows of the RegularCauchyName handoff.

The equivalence is facewise. Replacing any one of the four records by aggregate $\formalstatus$, aggregate $\leantarget$, bridge data, quotient stream equality, a selected limit, countable choice, metric completeness, or ambient $\RealUp$ completeness does not satisfy the threshold.
\end{theorem}
\begin{proof}
First identify the threshold itself. By \autoref{thm:regularcauchyname-real-completion-exit-threshold-equivalence}, the Real-and-Completion exit threshold for $G$ is exactly the exposure of the four local faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with only the listed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next isolate the handoff surface. The facewise handoff completeness theorem \autoref{thm:regularcauchyname-facewise-exit-handoff-completeness} says that the accepted RegularCauchyName handoff is complete precisely on those four faces and refuses quotient, selected-limit, equality, choice, completeness, and bridge substitutes.

Now read the StreamName and RegSeqRat coordinates locally. Apply \autoref{thm:regularcauchyname-tail-budget-streamname-regseqrat-handoff}. The finite tail budget keeps the $\StreamNameUp$ and $\RegSeqRatUp$ records objectwise, so their exit contribution cannot be moved into a quotient stream, selected host limit, host equality, countable choice, or metric-completeness datum.

Finally compare with the Real L10 objectwise criterion. The theorem \autoref{thm:real-l10-objectwise-exit-threshold-equivalence} reads the Phase Real and Completion threshold from the four local records, and \autoref{thm:real-l10-facewise-exit-status-noncollapse} rules out aggregate status, aggregate target, bridge data, quotient substitutes, selected limits, countable choice, metric completeness, and ambient $\RealUp$ completeness as replacements for any one face. Therefore the threshold is exactly the four independent local records plus the displayed support rows.
\end{proof}

\begin{theorem}[RegularCauchyName finite-section terminal pullback coherence]
\label{thm:regularcauchyname-finite-section-terminal-pullback-coherence}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$. Let $F$ be an accepted $\FiniteRealSectionUp$ packet reading the same displayed $\StreamNameUp$ finite window, $\DyadicRatCoreUp$ tolerance row, $\RegSeqRatUp$ readback row, $\RealUp$ seal row, and the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support as the exit-threshold handoff of $G$. Then the RegularCauchyName exit-threshold handoff and the finite-section consumer route determine one finite terminal pullback packet.

This packet is the common finite $\BHist$ section whose StreamName-facing and Real-facing terminal reads are identified by the Real finite-request terminal route. It contains no quotient stream, selected host limit, host equality, countable choice, ambient completeness, metric completeness, aggregate status row, bridge row, or second $\RealUp$ seal.
\end{theorem}
\begin{proof}
First read the RegularCauchyName side. By \autoref{thm:regularcauchyname-real-completion-exit-threshold-equivalence}, the accepted exit threshold of $G$ is the objectwise four-face surface on $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next apply \autoref{thm:regularcauchyname-exit-threshold-nonescape-boundary}. The same handoff cannot be enlarged by quotient streams, selected host limits, host equality, countable choice, ambient completeness, metric completeness, aggregate status rows, aggregate target rows, bridge data, or a separate $\RealUp$ completeness claim.

Now read the finite-section side. The route consumer exactness theorem \autoref{thm:finite-real-section-route-consumer-exactness} says that an accepted $\FiniteRealSectionUp$ packet using the displayed finite window, tolerance row, readback row, seal row, and support rows has exactly that finite consumer route. The nonescape theorem \autoref{thm:finite-real-section-nonescape} rules out any additional completion or host coordinate outside the finite section.

It remains to identify the terminal packet. Apply \autoref{thm:real-streamname-finite-request-terminal-bridge} to identify the StreamName-facing finite request with the Real-facing terminal section on the same finite $\BHist$ packet. Then apply \autoref{thm:real-regseqrat-terminal-four-face-acceptance-equivalence} to read the shared $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces as one accepted four-face terminal request.

The two routes therefore name one finite terminal pullback packet. Since the RegularCauchyName nonescape boundary and the FiniteRealSection nonescape theorem both refuse all coordinates beyond the displayed finite rows, the packet has no quotient stream, selected host limit, host equality, countable choice, ambient completeness, metric completeness, aggregate status row, bridge row, or second $\RealUp$ seal.
\end{proof}

\begin{theorem}[RegularCauchyName terminal pullback exactness]
\label{thm:regularcauchyname-terminal-pullback-exactness}
Let $G$ be an accepted $\RegularCauchyNameUp$ packet whose visible source is admitted from $\RationalStreamUp$, and let $F$ be an accepted $\FiniteRealSectionUp$ packet. Suppose $G$ and $F$ read the same displayed $\StreamNameUp$ finite window, $\DyadicRatCoreUp$ tolerance row, $\RegSeqRatUp$ readback row, $\RealUp$ seal row, and the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. Then every Real-and-Completion consumer read compatible with both projections factors through the finite terminal pullback packet of \autoref{thm:regularcauchyname-finite-section-terminal-pullback-coherence}.

The two projections are jointly determining: once the RegularCauchyName handoff projection and the FiniteRealSection route projection agree on the displayed rows, there is a unique compatible finite consumer route. No quotient stream, selected host limit, host equality, countable choice, ambient completeness, metric completeness, aggregate status row, bridge row, or second $\RealUp$ seal can be added to that route.
\end{theorem}
\begin{proof}
First identify the RegularCauchyName exit surface. Apply \autoref{thm:regularcauchyname-real-completion-exit-threshold-equivalence} to read the accepted handoff of $G$ as the four local faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ with only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next apply \autoref{thm:regularcauchyname-exit-threshold-nonescape-boundary}. The same handoff cannot acquire quotient streams, selected host limits, host equality, countable choice, ambient completeness, metric completeness, aggregate status rows, aggregate target rows, bridge data, or a separate $\RealUp$ completeness claim.

Now read the finite-section projection. The route consumer exactness theorem \autoref{thm:finite-real-section-route-consumer-exactness} pins every compatible $\FiniteRealSectionUp$ consumer to the displayed finite window, tolerance row, readback row, seal row, and support rows. The nonescape theorem \autoref{thm:finite-real-section-nonescape} excludes any completion or host coordinate outside that finite route.

It remains to compare the two projections. By \autoref{thm:regularcauchyname-finite-section-terminal-pullback-coherence}, the RegularCauchyName handoff and the finite-section route determine one finite terminal pullback packet over the shared rows. Apply \autoref{thm:real-streamname-finite-request-terminal-bridge} to identify the StreamName-facing request with the Real-facing terminal section on that packet, and use \autoref{thm:real-regseqrat-terminal-four-face-acceptance-equivalence} to read the four displayed faces as one accepted terminal request.

Therefore any Real-and-Completion consumer compatible with both projections must factor through that terminal pullback packet. Since both nonescape results refuse every coordinate beyond the displayed finite rows, the two projections are jointly determining and no quotient stream, selected host limit, host equality, countable choice, ambient completeness, metric completeness, aggregate status row, bridge row, or second $\RealUp$ seal can be attached.
\end{proof}
