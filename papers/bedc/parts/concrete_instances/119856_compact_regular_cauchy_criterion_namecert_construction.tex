\chapter{A Concrete Naming Certificate for $\CompactRegularCauchyCriterionUp$}
\label{ch:concrete-instances-compact-regular-cauchy-criterion-namecert}
\origin{human}

Following Bishop and Bridges on compact metric sources through finite nets and explicit Cauchy moduli, the $\CompactRegularCauchyCriterionUp$ packet records the finite-window criterion used by Real and Completion consumers before any ambient compactness theorem is available. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the compact regular-Cauchy criterion as explicit finite data; it does not identify compactness with open-cover compactness, quotient real equality, trichotomy, or selected limits.

\begin{theorem}[Compact regular-Cauchy criterion NameCert obligations]
\label{thm:compact-regular-cauchy-criterion-namecert-obligations}
A local $\NameCert_{\CompactRegularCauchyCriterionUp}$ packet is accepted exactly when it displays a finite carrier
$$
K=(G,W,D,R,E,H,C,P,N)
$$
where $G$ is a finite compactness grid row, $W$ is the $\StreamNameUp$ family of sampled windows, $D$ is the $\DyadicRatCoreUp$ tolerance ledger for those windows, $R$ is the $\RegSeqRatUp$ regular readback produced by the grid, $E$ is the downstream $\RealUp$ seal, $H$ is componentwise $\hsame$ transport, $C$ is displayed $\Cont$ replay, $P$ is $\Pkg$ provenance, and $N$ is the local naming row. The classifier compares accepted packets only through these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed tuple. The compactness grid $G$ is not a host compact set; it is the finite list of centers, dyadic radii, and grid-cover incidence data that the packet exposes.

The stream and dyadic rows then determine the visible read. For each grid cell, $W$ names the sampled finite windows and $D$ gives the corresponding dyadic tolerance checks, so the regular rational row $R$ is downstream of finite observations and budgets.

The real seal and structural rows are consumer boundaries. The row $E$ is read only after $R$, and $H,C,P,N$ transport, replay, source, and name the same finite route. No quotient real equality, open-cover compactness principle, trichotomy, selected subsequence, or ambient completeness coordinate can be projected from the carrier.
\end{proof}

\begin{theorem}[Compact regular-Cauchy modulus window]
\label{thm:compact-regular-cauchy-criterion-modulus-window}
For every accepted $\CompactRegularCauchyCriterionUp$ packet and every requested regular precision $m$, there is a displayed finite window budget
$$
B_m=(\Gamma_m,\rho_m,\omega_m)
$$
such that $\Gamma_m$ is a finite grid of dyadic centers, $\rho_m$ is a $\DyadicRatCoreUp$ radius row, and $\omega_m$ is a $\StreamNameUp$ read schedule. Any two sampled regular names in the same grid cell are transported by $\hsame$ to a common $\RegSeqRatUp$ readback within the dyadic budget for $m$ before any $\RealUp$ seal is exposed.
\end{theorem}
\begin{proof}
Fix the requested precision $m$. The packet reads the compactness grid coordinate $G$ at precision $m$ and obtains the finite grid $\Gamma_m$, dyadic radius row $\rho_m$, and stream-window schedule $\omega_m$.

Each cell is checked by the dyadic ledger. The row $D$ supplies the tolerance comparison for the sampled endpoints, while $W$ supplies exactly the finite observations named by $\omega_m$.

Now use componentwise $\hsame$ transport. If two sampled names land in the same cell, the transport row identifies their displayed window data and dyadic budget data, so the $\RegSeqRatUp$ readback is common at precision $m$.

Only after this finite route has been exposed may the $\RealUp$ seal row be read. The argument is a finite-window construction and not a proof by open covers, quotient reals, trichotomy, or choice.
\end{proof}

\begin{theorem}[Compact regular-Cauchy L10 scope]
\label{thm:compact-regular-cauchy-criterion-l10-scope}
The $\CompactRegularCauchyCriterionUp$ packet belongs to the L10 Real and Completion scope as a criterion surface over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. It supplies finite grids, dyadic budgets, regular readback, and seal order for downstream completion arguments, but it does not assert ambient $\RealUp$ completeness, open-cover compactness, sequential compactness without explicit data, trichotomy, quotient reals, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
The dependency surface is exactly the displayed route named in the theorem. Dyadic budgets come from $\DyadicRatCoreUp$, finite reads come from $\StreamNameUp$, regular rational names come from $\RegSeqRatUp$, and the public completion-facing boundary is the $\RealUp$ seal.

The L10 scope is therefore a finite criterion packet. A downstream consumer may ask for the grid and window budget at a requested precision, then use $\hsame$ transport to keep the regular readback stable across the displayed cell data.

Nothing in the packet supplies the excluded coordinates listed in the statement. Completeness, open-cover compactness, data-free sequential compactness, trichotomy, quotient-real equality, Lean verification, and bridge checking remain outside the named scope.
\end{proof}

\closureat{CompactRegularCauchyCriterionUp}{seedStr}

\begin{closurestatus}{\CompactRegularCauchyCriterionUp}
  \constructivestory{A $\CompactRegularCauchyCriterionUp$ packet is generated from finite compactness grids, StreamName sample windows, DyadicRatCore tolerance ledgers, RegSeqRat regular readback rows, RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{thm:compact-regular-cauchy-criterion-namecert-obligations}, \autoref{thm:compact-regular-cauchy-criterion-modulus-window}, and \autoref{thm:compact-regular-cauchy-criterion-l10-scope} seed the finite compact regular-Cauchy criterion packet for the L10 Real and Completion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, RealUp completeness, sequential compactness without explicit data, trichotomy, quotient reals, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite grid exposure, dyadic window budgets, regular readback, hsame transport, Real sealing, replay, provenance, and local naming for BEDC.Derived.CompactRegularCauchyCriterionUp.}
\end{closurestatus}
