\chapter{A Concrete Naming Certificate for $\RealCompletionEtaLawUp$}
\label{ch:concrete-instances-real-completion-eta-law-namecert}
\origin{human}

Following Bishop--Bridges completion theory and Mac Lane's unit laws, the $\RealCompletionEtaLawUp$ packet records the Real-facing finite BEDC surface saying that the completion unit returns the same displayed real completion endpoint after the StreamName, RegSeqRat, dyadic tolerance, and Real-seal routes have been read. It refines the metric eta packet of \autoref{ch:concrete-instances-metric-completion-eta-law-namecert} along the Real/RegSeqRat/StreamName route displayed by \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-streamname-namecert}. The packet names the unit law as finite NameCert data; it is not a quotient completion equality, a selected representative theorem, countable choice, or an ambient completeness principle.

\begin{definition}[Real completion eta-law carrier]
\label{def:real-completion-eta-law-carrier}
A $\RealCompletionEtaLawUp$ carrier is a finite $\BHist$ packet
$$
E=(T,U,W,R,D,Q,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the Real completion endpoint row already accepted by the displayed completion route.
\item $U$ is the completion unit row applied to that same displayed endpoint.
\item $W$ is the finite $\StreamNameUp$ window row exposing the observable real-name windows for the direct and unit-applied routes.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the finite precision ledger used by the readback.
\item $Q$ is the terminal $\RealUp$ seal row reached only after $T,U,W,R,D$ have been displayed.
\item $Z$ is the eta-law exactness row binding the direct Real completion endpoint and the unit-applied endpoint to the same displayed Real seal.
\item $H,C,P,N$ are componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and the local $\NameCert_{\RealCompletionEtaLawUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,U,W,R,D,Q,Z,H,C,P,N$. It has no coordinate for quotient Cauchy classes, selected representatives, host equality of real numbers, countable-choice schedules, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real completion eta-law NameCert obligations]
\label{thm:real-completion-eta-law-namecert-obligations}
For every accepted $\RealCompletionEtaLawUp$ carrier $E=(T,U,W,R,D,Q,Z,H,C,P,N)$, the local $\NameCert_{\RealCompletionEtaLawUp}$ surface consists exactly of Real completion endpoint exposure, completion unit exposure, finite StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, Real seal exposure, eta-law exactness, transport, replay, provenance, and local naming. Every consumer of the eta law must read those rows before it treats the direct Real completion endpoint and the unit-applied endpoint as the same named route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-completion-eta-law-carrier}. The rows $T$ and $U$ name the displayed Real completion endpoint and the completion unit applied to that endpoint, while $W,R,D$ expose the finite StreamName, RegSeqRat, and dyadic tolerance route through which a Real-facing consumer may inspect them.

Next read the terminal seal row $Q$. The seal is reached only after the endpoint, unit, window, readback, and tolerance rows have been displayed, so no quotient Cauchy class, selected representative, countable-choice schedule, or host real equality is available as an independent coordinate.

Finally use the eta-law exactness row $Z$ and the structural rows $H,C,P,N$. They bind the direct and unit-applied routes to the same displayed Real seal under transport, replay, provenance, and local naming, which exhausts the NameCert surface.
\end{proof}

\begin{theorem}[Real completion eta-law unit handoff]
\label{thm:real-completion-eta-law-unit-handoff}
Let $E=(T,U,W,R,D,Q,Z,H,C,P,N)$ be an accepted $\RealCompletionEtaLawUp$ carrier. Any Real-facing completion consumer that enters through the unit row $U$ must pass through the displayed endpoint $T$, the $\StreamNameUp$ windows $W$, the $\RegSeqRatUp$ readback $R$, the $\DyadicRatCoreUp$ tolerance row $D$, and the eta-law exactness row $Z$ before reading the terminal $\RealUp$ seal $Q$. The handoff therefore exposes the completion unit law as a finite Real-name route, not as quotient completion equality, selected representatives, host equality of real numbers, or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with the unit row $U$. By the carrier definition it is attached to the already displayed Real completion endpoint $T$, so it cannot introduce a second endpoint outside the packet.

Read the finite observation route through $W,R,D$. These rows are exactly the StreamName windows, RegSeqRat readback, and dyadic tolerance ledger used by the Real/RegSeqRat/StreamName route, and they must be consumed before the seal $Q$ is exposed.

The exactness row $Z$ supplies the eta-law handoff, while $H,C,P,N$ preserve the same displayed route. Thus a consumer receives the unit law for the existing Real completion endpoint without acquiring quotient equality, selected representatives, countable choice, host equality, or ambient Real completeness.
\end{proof}

\independenceWitness{metric-completion-eta-law, real, regseqrat, streamname, dyadicratcore: no listed sibling is bijective to $\RealCompletionEtaLawUp$ because this packet jointly binds a Real completion endpoint, completion unit row, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, Real seal, eta-law exactness, transport, replay, provenance, and local naming.}

\closureat{RealCompletionEtaLawUp}{seedStr}

\begin{closurestatus}{\RealCompletionEtaLawUp}
  \constructivestory{A $\RealCompletionEtaLawUp$ packet is generated from a Real completion endpoint row, a completion unit row, finite StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealUp seal, eta-law exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-completion-eta-law-carrier}, \autoref{thm:real-completion-eta-law-namecert-obligations}, and \autoref{thm:real-completion-eta-law-unit-handoff} seed the finite Real-facing completion-unit eta-law surface over the Real/RegSeqRat/StreamName route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected Cauchy representatives, host equality of real numbers, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, unit-row exposure, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, Real seal exposure, eta-law exactness, transport, replay, provenance, and local naming for BEDC.Derived.RealCompletionEtaLawUp.}
\end{closurestatus}
