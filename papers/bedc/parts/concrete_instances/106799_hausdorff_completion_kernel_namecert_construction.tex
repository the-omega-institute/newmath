\chapter{A Concrete Naming Certificate for $\HausdorffCompletionKernelUp$}
\label{ch:concrete-instances-hausdorff-completion-kernel-namecert}
\origin{human}

Following Hausdorff and Bourbaki-style completion language, the $\HausdorffCompletionKernelUp$ packet records the separated Cauchy-completion boundary as finite BEDC data. It sits between the separated metric classifier of \autoref{ch:concrete-instances-separatedmetric-namecert}, the complete metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy-filter completion route of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the Hausdorff completion boundary of \autoref{ch:concrete-instances-hausdorffcompletion-namecert}. The packet names the zero-distance kernel that separates completion reads before any quotient representative, host equality, countable-choice selector, or ambient $\RealUp$ completeness theorem may be consumed.

\begin{definition}[Hausdorff completion kernel carrier]
\label{def:hausdorff-completion-kernel-carrier}
A $\HausdorffCompletionKernelUp$ carrier is a finite $\BHist$ packet
$$
K=(Q,Z,F,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the Cauchy-completion thread row, recording the inspected Cauchy filter, Cauchy sequence, or regular Cauchy name requests that enter the completion boundary.
\item $Z$ is the separated zero-distance row supplied by $\SeparatedMetricUp$, recording when two displayed completion reads have zero carried distance.
\item $F$ is the $\CauchyFilterCompletionUp$ handoff row, keeping filter-completion requests tied to the same finite Cauchy thread.
\item $R$ is the $\RegSeqRatUp$ readback row, exposing only regular rational windows read from $Q$ and $F$.
\item $E$ is the terminal $\RealUp$ seal row reached only after $Q,Z,F,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the Cauchy thread, zero-distance row, filter-completion handoff, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a separated completion request through $Q,Z,F,R,E,H$.
\item $P$ is the $\Pkg$ provenance over $\HausdorffCompletionKernelUp$, $\SeparatedMetricUp$, $\CompleteMetricUp$, $\CauchyFilterCompletionUp$, $\RegSeqRatUp$, $\RealUp$, $\HausdorffCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HausdorffCompletionKernelUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,Z,F,R,E,H,C,P,N$. It has no coordinate for quotient representatives, host completion equality, selected limits, countable choice, detached Real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Hausdorff completion kernel NameCert obligations]
\label{thm:hausdorff-completion-kernel-namecert-obligations}
For every accepted $\HausdorffCompletionKernelUp$ carrier $K=(Q,Z,F,R,E,H,C,P,N)$, the local $\NameCert_{\HausdorffCompletionKernelUp}$ surface consists exactly of carrier admission for the displayed packet, Cauchy-completion thread exposure through $Q$, separated zero-distance exposure through $Z$, Cauchy-filter completion handoff through $F$, regular rational readback through $R$, terminal Real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:hausdorff-completion-kernel-carrier}. The accepted packet exposes the Cauchy thread, separated zero-distance boundary, filter-completion handoff, regular rational readback, terminal Real seal, transport, replay, provenance, and local naming rows.

The separated completion route is fixed by $Q,Z,F,R,E$. The Cauchy thread $Q$ supplies the inspected completion source, $Z$ supplies the zero-distance classifier, $F$ binds Cauchy-filter completion reads to that source, $R$ gives the regular rational readback, and $E$ seals the resulting Real-facing boundary.

The structural rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and naming. Since the carrier has no coordinate for quotient representatives, host completion equality, selected limits, countable choice, detached Real equality, or ambient $\RealUp$ completeness, those objects cannot belong to the local NameCert surface.
\end{proof}

\begin{theorem}[Hausdorff completion kernel nonescape]
\label{thm:hausdorff-completion-kernel-nonescape}
Every separated completion consumer of an accepted $\HausdorffCompletionKernelUp$ packet factors through
$$
\SeparatedMetricUp,\ \CompleteMetricUp,\ \CauchyFilterCompletionUp,\ \RegSeqRatUp,\ \RealUp,\ \HausdorffCompletionUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
In particular, equality of completed reads is exposed only by the displayed zero-distance row $Z$ and its transported replay through $H,C,P,N$.
\end{theorem}
\begin{proof}
Start with a consumer that requests equality or reuse of a completed read. The carrier exposes no equality coordinate before the Cauchy thread $Q$ and the separated zero-distance row $Z$ have been read.

Next read the completion and real-facing rows. The handoff $F$ ties the request to the Cauchy-filter completion route, $R$ gives the regular rational readback used by the real-name boundary, and $E$ is downstream of those rows.

Finally replay the structural data. The rows $H,C,P,N$ preserve and name the same finite route, while the $\HausdorffCompletionUp$ provenance identifies the separated completion boundary. No quotient representative, host equality, selected limit, countable-choice schedule, detached Real equality, or ambient completeness coordinate is available to the consumer.
\end{proof}

\falsifiablePrediction{Every accepted $\HausdorffCompletionKernelUp$ separated completion read factors through a Cauchy-completion thread row, a separated zero-distance row, a CauchyFilterCompletion handoff, a RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}

\independenceWitness{separatedmetric, completemetric, cauchyfiltercompletion, regseqrat, real, hausdorffcompletion: $\HausdorffCompletionKernelUp$ is not bijective to any listed sibling because it jointly carries the Cauchy thread, zero-distance kernel, filter-completion handoff, regular readback, Real seal, transport, replay, provenance, and local naming rows.}

\closureat{HausdorffCompletionKernelUp}{seedStr}

\begin{closurestatus}{\HausdorffCompletionKernelUp}
  \constructivestory{A $\HausdorffCompletionKernelUp$ packet is generated from a Cauchy-completion thread row, a separated zero-distance row, a CauchyFilterCompletion handoff, a RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hausdorff-completion-kernel-carrier}, \autoref{thm:hausdorff-completion-kernel-namecert-obligations}, and \autoref{thm:hausdorff-completion-kernel-nonescape} seed the separated Cauchy-completion kernel over displayed completion, zero-distance, regular-readback, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient representatives, host completion equality, selected limits, countable choice, detached Real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, separated zero-distance exactness, Cauchy-filter handoff, regular readback, Real-seal nonescape, and TasteGate field faithfulness for BEDC.Derived.HausdorffCompletionKernelUp.}
\end{closurestatus}
