\chapter{A Concrete Naming Certificate for $\LocatedOpenUp$}
\label{ch:concrete-instances-locatedopen-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\LocatedOpenUp$
packet records a neighbourhood interface whose evidence is located in finite
real and cut data before any topological consumer reads it. It sits over the
located cut surface of \autoref{ch:concrete-instances-locatedcut-namecert}, the
located real enclosure of \autoref{ch:concrete-instances-locatedreal-namecert},
the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert},
the regular rational readback of
\autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal
of \autoref{ch:concrete-instances-real-namecert}, and the completion-facing
filter route of \autoref{ch:concrete-instances-cauchyfilter-namecert}. The
chapter names only a finite L10 neighbourhood packet: it does not identify
opens with quotient subsets of host reals and does not invoke open-cover
compactness.

\begin{definition}[LocatedOpen carrier]
\label{def:located-open-carrier}
A $\LocatedOpenUp$ carrier is a finite $\BHist$ packet
$$
O=(L,R,S,Q,W,E,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\LocatedCutUp$ source row exposing the finite lower and upper
cut comparisons used by the local membership read.
\item $R$ is the $\LocatedRealUp$ enclosure row tying the same comparisons to a
located real presentation.
\item $S$ is the $\StreamNameUp$ window family recording the inspected stream
positions and replayable requests.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback attached to those
windows.
\item $W$ is the displayed located-membership window row. For each inspected
candidate it records a finite interval-entry witness or a positive dyadic
radius witness around the candidate.
\item $E$ is the $\RealUp$ seal row reached only after the located cut, located
real, stream, regular rational, and membership-window rows have been displayed.
\item $T$ is the local classifier transport row moving $L,R,S,Q,W,E$ to a
common finite comparison surface.
\item $H$ records componentwise $\hsame$ transport for the cut, real, stream,
regular rational, membership-window, Real-seal, classifier, continuation,
provenance, and local naming rows.
\item $C$ records the displayed $\Cont$ replay from a neighbourhood request
through the inspected windows, radius or interval-entry evidence, readback rows,
Real seal, and classifier transport.
\item $P$ is the $\Pkg$ provenance over $\LocatedOpenUp$, $\LocatedCutUp$,
$\LocatedRealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$,
$\CauchyFilterUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedOpenUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed finite
windows and transports. It has no coordinate for quotient subsets of reals,
host real equality, a choice-selected neighbourhood, arbitrary-union
completion, open-cover compactness, or a proof that $\RealUp$ is complete.
\end{definition}
\leanchecked{BEDC.Derived.LocatedOpenUp.TasteGate.LocatedOpenTasteGate\_single\_carrier\_alignment}

\begin{theorem}[LocatedOpen window stability]
\label{thm:located-open-window-stability}
For every accepted $\LocatedOpenUp$ packet
$O=(L,R,S,Q,W,E,T,H,C,P,N)$, the located-membership windows in $W$ are stable
under the componentwise $\hsame$ transports recorded in $H$. In particular, a
transported consumer reads the same finite interval-entry witnesses and
positive dyadic radius witnesses after $L,R,S,Q,W,E$ have been moved to the
common comparison surface named by $T$.
\end{theorem}
\leanchecked{BEDC.Derived.LocatedOpenUp.LocatedOpenCarrier\_window\_stability}
\begin{proof}
Project the carrier of \autoref{def:located-open-carrier}. The only
membership evidence available to a consumer is the row $W$, and each entry of
$W$ is already displayed as a finite interval-entry witness or a positive
dyadic radius witness.

Apply the transport row $H$ componentwise. The located cut, located real,
stream, regular rational, membership-window, and Real-seal rows are moved
together to the comparison surface named by $T$, so the transported packet
contains the same window indices and the same radius or interval-entry
witnesses.

Finally replay the continuation row $C$. Replay follows the transported
windows and readbacks; it does not introduce an ambient subset, a host equality
test, a selected neighbourhood, or an arbitrary union. Thus the located
membership surface is stable exactly over the displayed finite windows.
\end{proof}
\leanchecked{BEDC.Derived.LocatedOpenUp.LocatedOpen\_window\_stability}

\begin{theorem}[LocatedOpen Real consumer route]
\label{thm:located-open-realup-consumer-route}
Every $\RealUp$-facing consumer of an accepted $\LocatedOpenUp$ packet factors
through
$$
\LocatedCutUp,\quad \LocatedRealUp,\quad \StreamNameUp,\quad
\RegSeqRatUp,\quad \RealUp,\quad \CauchyFilterUp,\quad
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert .
$$
Concretely, the consumer first reads the located cut and located real rows,
then the finite stream and regular rational readbacks, then the displayed
located-membership windows, and only then the terminal $\RealUp$ seal and
filter-facing continuation route.
\end{theorem}
\begin{proof}
Begin with a $\RealUp$-facing read of the seal row $E$. The carrier stores $E$
after the located cut row $L$, located real row $R$, stream window row $S$,
regular rational readback row $Q$, and located-membership window row $W$.
Therefore no consumer can reach the seal without first exposing these finite
rows.

The completion-facing part is only a route. The provenance row names
$\CauchyFilterUp$ so that a filtered consumer can replay the finite
neighbourhood request through $C$, but the packet supplies no completeness
field and no arbitrary-union constructor.

Attach $T,H,P,N$ to the same route. Classifier transport, componentwise
$\hsame$, package provenance, and local naming keep the consumer inside the
listed rows. Since the carrier has no coordinate for quotient subsets, host
real equality, selected neighbourhoods, open-cover compactness, or
$\RealUp$ completeness, no such datum can be recovered from the read.
\end{proof}

\begin{theorem}[LocatedOpen obligation surface]
\label{thm:located-open-obligation-surface}
For every accepted $\LocatedOpenUp$ packet
$O=(L,R,S,Q,W,E,T,H,C,P,N)$, the rows $L,R,S,Q,W,E,T,H,C,P,N$ jointly supply
carrier admission, located-membership window stability, the Real-facing route,
classifier transport, replay, provenance, and local $\NameCert$ non-escape.
The exported surface is exactly the finite neighbourhood interface of
\autoref{def:located-open-carrier}; it exports no quotient subset, selected
neighbourhood, arbitrary union, open-cover compactness, or $\RealUp$
completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-open-carrier}. Carrier admission
is the displayed tuple $O=(L,R,S,Q,W,E,T,H,C,P,N)$, with $L,R,S,Q,W,E$ naming
the located cut, located real, stream, regular rational, membership-window, and
Real-seal rows.

Window stability is supplied by
\autoref{thm:located-open-window-stability}. The theorem keeps the interval
entry witnesses and positive dyadic radius witnesses inside $W$ after the
componentwise transports in $H$ move the packet to the comparison surface
named by $T$.

The Real-facing route is supplied by
\autoref{thm:located-open-realup-consumer-route}. It forces every consumer to
read the located cut, located real, stream windows, regular rational readbacks,
membership windows, terminal Real seal, and filter-facing replay before any
Real-facing output is available.

The remaining rows are bookkeeping rows for non-escape. The classifier
transport is $T$, replay is $C$, provenance is $P$, and local naming is $N$.
Since the carrier definition contains no coordinate for quotient subsets,
selected neighbourhoods, arbitrary unions, open-cover compactness, or
$\RealUp$ completeness, the obligation surface cannot export any such row.
\end{proof}
\leanchecked{BEDC.Derived.LocatedOpenUp.LocatedOpenObligationSurface}

\begin{theorem}[LocatedOpen obligation closure surface]
\label{thm:located-open-obligation-closure-surface}
The carrier, window stability, Real consumer route, and obligation surface
assemble an $\obligationClosure$ package for $\LocatedOpenUp$ over
$\LocatedCutUp$, $\LocatedRealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$,
$\CauchyFilterUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The
package exposes exactly the displayed located-neighbourhood rows and no
quotient subset, selected neighbourhood, arbitrary union, open-cover
compactness, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:located-open-carrier}. It supplies the
displayed located cut, located real, stream, regular rational,
membership-window, Real-seal, classifier, transport, replay, provenance, and
local naming rows.

Apply \autoref{thm:located-open-window-stability} and
\autoref{thm:located-open-realup-consumer-route}. These results keep the
membership windows stable under componentwise transport and force every
Real-facing consumer through the listed located-neighbourhood route.

Finally use \autoref{thm:located-open-obligation-surface}. It packages carrier
admission, classifier transport, replay, provenance, and local non-escape into
one obligation surface, so the closure package has no coordinate for any
excluded host-topological or completion datum.
\end{proof}

\begin{theorem}[LocatedOpen scoped kernel route]
\label{thm:located-open-scoped-kernel-route}
The carrier, window stability, $\RealUp$ consumer route, obligation surface,
and closure surface of $\LocatedOpenUp$ depend only on
$\LocatedCutUp$, $\LocatedRealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$,
$\CauchyFilterUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
Equivalently, every scoped read of the finite located-neighbourhood packet is
a read of
$$
L,R,S,Q,W,E,T,H,C,P,N
$$
from \autoref{def:located-open-carrier}, with no external topological,
choice, quotient, or ambient-completion coordinate.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:located-open-carrier}. The displayed
tuple $O=(L,R,S,Q,W,E,T,H,C,P,N)$ accounts for the located cut, located real,
stream, regular rational, membership-window, Real-seal, classifier transport,
componentwise transport, replay, provenance, and local naming rows.

Apply the window-stability theorem
\autoref{thm:located-open-window-stability}. It keeps the interval-entry and
positive-radius witnesses inside the same finite window row $W$ under the
componentwise $\hsame$ route $H$.

Apply the Real consumer route
\autoref{thm:located-open-realup-consumer-route}. It places every terminal
$\RealUp$ read downstream of the located cut, located real, stream, regular
rational, membership-window, and filter-facing continuation rows.

Finally use the obligation surface and obligation closure surface,
\autoref{thm:located-open-obligation-surface} and
\autoref{thm:located-open-obligation-closure-surface}. These two results
package carrier admission, classifier transport, replay, provenance, local
non-escape, and the displayed closure surface over exactly the same rows.
Since no cited theorem introduces any coordinate outside
$L,R,S,Q,W,E,T,H,C,P,N$, the scoped kernel route is precisely the dependency
list in the statement.
\end{proof}

\begin{closurestatus}{\LocatedOpenUp}
  \constructivestory{A $\LocatedOpenUp$ packet is generated from located cut rows, located real enclosure rows, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, displayed membership windows with positive dyadic radius or interval-entry witnesses, a terminal $\RealUp$ seal, classifier transport, componentwise $\hsame$ rows, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:located-open-scoped-kernel-route} specifies the finite L10 located-neighbourhood route over LocatedCut, LocatedReal, StreamName, RegSeqRat, RealUp, CauchyFilter, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.LocatedOpenUp.LocatedOpenObligationSurface}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim general topology, open-cover compactness, Heine--Borel, arbitrary-union completion, quotient subsets of reals, host real equality, choice-selected neighbourhoods, or $\RealUp$ completeness.}
  \upgradepath{Public closure requires exposing the scoped located-neighbourhood route as a public consumer interface without adding quotient subsets, arbitrary unions, selected neighbourhoods, or ambient RealUp completeness.}
\end{closurestatus}
