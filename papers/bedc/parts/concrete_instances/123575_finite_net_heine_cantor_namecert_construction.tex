\chapter{A Concrete Naming Certificate for $\FiniteNetHeineCantorUp$}
\label{ch:concrete-instances-finite-net-heine-cantor-namecert}
\origin{human}

Following Bishop and Bridges constructive metric analysis, the $\FiniteNetHeineCantorUp$ packet records the finite-net Heine--Cantor route as displayed NameCert data. It sits over the compact metric finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map pointwise-modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational positive minimum fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, and the final triangle handoff of \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}. The packet names a finite uniform-continuity construction; it does not invoke open-cover compactness, selected global moduli, quotient equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Finite-net Heine--Cantor carrier]
\label{def:finite-net-heine-cantor-carrier}
A $\FiniteNetHeineCantorUp$ carrier is a finite $\BHist$ packet
$$
H=(K,F,M,R,L,T,U,S,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row, including the finite center list, source metric radius ledger, and coverage read.
\item $F$ is the $\ContinuousMapUp$ row recording pointwise modulus data at each displayed compact-net center.
\item $M$ is the source and target $\MetricSpaceUp$ triangle ledger used to compare $x$, its chosen center, and $x'$.
\item $R$ is the $\RatUp$ tolerance row that keeps the requested precision and local radius comparisons visible.
\item $L$ is the finite lower-bound fold row supplied by the $\FiniteNetMinimumFoldUp$ surface.
\item $T$ is the $\MetricTriangleUniformEstimateUp$ handoff row that consumes the compact-net, pointwise-modulus, and triangle data.
\item $U$ is the exported $\UniformModulusUp$ row for the displayed continuous map on the compact metric source.
\item $S$ records componentwise $\hsame$ transport for finite-net, continuity, metric, rational, fold, triangle, and modulus rows.
\item $C$ records displayed $\Cont$ replay from pointwise-modulus reads through the finite-net fold and triangle estimate to the uniform-modulus export.
\item $P$ records $\Pkg$ provenance over $\CompactMetricUp$, $\ContinuousMapUp$, $\MetricSpaceUp$, $\RatUp$, $\FiniteNetMinimumFoldUp$, $\MetricTriangleUniformEstimateUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteNetHeineCantorUp}$ row.
\end{enumerate}
The carrier has no coordinate for open-cover compactness, a selected global modulus outside the finite fold, quotient equality of points or functions, countable choice, or ambient real-completeness supply.
\end{definition}

\begin{theorem}[Finite-net Heine--Cantor NameCert obligations]
\label{thm:finite-net-heine-cantor-namecert-obligations}
For every accepted $\FiniteNetHeineCantorUp$ carrier
$$
H=(K,F,M,R,L,T,U,S,C,P,N),
$$
the local $\NameCert_{\FiniteNetHeineCantorUp}$ surface consists exactly of finite-net source exposure, pointwise-modulus exposure, source and target triangle exposure, rational tolerance admission, finite lower-bound fold admission, triangle-estimate handoff, $\UniformModulusUp$ export, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-net-heine-cantor-carrier}. The rows $K,F,M,R,L,T,U,S,C,P,N$ are the full displayed inventory.

The compact source is read through $K$, while the pointwise continuity data are read through $F$. The rational tolerance row $R$ and finite lower-bound fold $L$ keep the local radii in one finite accumulator before any uniform read is exposed.

The metric step is the handoff through $M$ and $T$. Source and target triangle rows are read with the same selected compact-net center, so the final estimate is the displayed $\MetricTriangleUniformEstimateUp$ route rather than a host compactness theorem.

Finally $S,C,P,N$ transport, replay, source, and name the route. Since no excluded coordinate occurs in the carrier, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite-net Heine--Cantor uniform-modulus route]
\label{thm:finite-net-heine-cantor-uniform-modulus-route}
Every $\UniformModulusUp$ consumer of an accepted $\FiniteNetHeineCantorUp$ carrier reaches the exported modulus row $U$ only after reading the compact metric finite-net row $K$, the continuous-map pointwise-modulus rows $F$, the metric triangle rows $M$, the rational tolerance row $R$, the finite lower-bound fold $L$, and the triangle-estimate handoff $T$. The exported route contains no open-cover compactness object, selected global modulus outside the finite packet, quotient equality, countable choice, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:finite-net-heine-cantor-namecert-obligations}. It orders the local certificate so that the compact-net, pointwise-modulus, metric, rational, fold, and triangle rows are exposed before the $\UniformModulusUp$ row is exported.

The finite lower-bound fold supplies the shared precision, and \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert} supplies the final source-target triangle route. Thus the consumer reads a finite package rather than a compactness or selection theorem.

The exclusions follow from the carrier definition. Open-cover compactness, external global modulus selection, quotient equality, countable choice, and ambient $\RealUp$ completeness are absent coordinates, while $S,C,P,N$ only preserve the displayed route.
\end{proof}

\falsifiablePrediction{Every $\FiniteNetHeineCantorUp$ uniform-modulus read factors through compact metric finite-net rows, continuous-map pointwise-modulus rows, metric triangle rows, $\RatUp$ tolerance rows, finite lower-bound fold rows, a $\MetricTriangleUniformEstimateUp$ handoff, $\UniformModulusUp$ export, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{compactmetric, continuousmap, finitenetminimumfold, metric-triangle-uniform-estimate, uniformmodulus: $\FiniteNetHeineCantorUp$ is not bijective to any listed sibling because it joins finite-net coverage, pointwise continuity, rational folding, triangle estimation, and uniform-modulus export as one NameCert packet.}

\closureat{FiniteNetHeineCantorUp}{seedStr}

\begin{closurestatus}{\FiniteNetHeineCantorUp}
  \constructivestory{A $\FiniteNetHeineCantorUp$ packet is generated from CompactMetric finite-net data, ContinuousMap pointwise-modulus rows, MetricSpace source and target triangle rows, RatUp tolerance rows, finite lower-bound fold rows, MetricTriangleUniformEstimate handoff rows, UniformModulus export rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-net-heine-cantor-carrier}, \autoref{thm:finite-net-heine-cantor-namecert-obligations}, and \autoref{thm:finite-net-heine-cantor-uniform-modulus-route} seed the finite-net Heine--Cantor NameCert route for compact-uniform consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected global moduli, quotient equality, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net exposure, pointwise-modulus exposure, rational lower-bound folding, triangle-estimate handoff, UniformModulus export, transport, replay, provenance, and local naming for BEDC.Derived.FiniteNetHeineCantorUp.}
\end{closurestatus}
