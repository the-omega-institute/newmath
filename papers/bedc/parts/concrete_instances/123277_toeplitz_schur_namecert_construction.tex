\chapter{A Concrete Naming Certificate for $\ToeplitzSchurUp$}
\label{ch:concrete-instances-toeplitz-schur-namecert}
\origin{human}

Following Toeplitz, Schur, Hardy, and the constructive summability tradition, the $\ToeplitzSchurUp$ packet records the finite matrix-window route by which a regular sequence transform is read as a Real-facing NameCert object. It sits after the Toeplitz matrix route of \autoref{ch:concrete-instances-toeplitz-lemma-namecert}, the Cesaro averaging surface of \autoref{ch:concrete-instances-hardy-cesaro-mean-namecert}, the finite stream naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed coefficient windows, source windows, row-sum and tail-smallness ledgers, regular readback, Real sealing, transport, replay, provenance, and local naming; it does not assert ambient $\RealUp$ completeness, a selected limit, quotient equality of sequence names, or countable choice over matrix rows.

\begin{definition}[Toeplitz-Schur carrier]
\label{def:toeplitz-schur-carrier}
A $\ToeplitzSchurUp$ carrier is a finite $\BHist$ packet
$$
S=(A,W,R,D,B,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the finite Toeplitz--Schur coefficient-window row, listing the inspected matrix entries, row supports, and the row-sum comparison data.
\item $W$ is the $\StreamNameUp$ source-window row, exposing exactly the sequence entries opened before the transform read is made.
\item $R$ is the $\RegSeqRatUp$ readback row for the source windows and transformed finite partial rows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording row-sum bounds, tail-smallness checks, and dyadic error budgets.
\item $B$ is the Schur boundary row, requiring the finite off-window mass and the displayed row-sum discrepancy to remain inside the requested tolerance.
\item $T$ is the terminal $\RealUp$ seal for the transformed regular name after $A,W,R,D,B$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for coefficient windows, source windows, readback, dyadic ledger, Schur boundary, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a transform request through $A,W,R,D,B,T,H$.
\item $P$ is the $\Pkg$ provenance over $\ToeplitzSchurUp$, $\ToeplitzLemmaUp$, $\HardyCesaroMeanUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ToeplitzSchurUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,W,R,D,B,T,H,C,P,N$. It has no coordinate for host summability, ambient Real completeness, selected limits, quotient equality of streams, countable choice, trichotomy, or detached equality of transformed real names.
\end{definition}

\begin{theorem}[Toeplitz-Schur NameCert obligations]
\label{thm:toeplitz-schur-namecert-obligations}
For every accepted $\ToeplitzSchurUp$ carrier $S=(A,W,R,D,B,T,H,C,P,N)$, the local $\NameCert_{\ToeplitzSchurUp}$ surface consists exactly of carrier admission; finite coefficient-window exposure through $A$; source-window exposure through $W$; regular rational readback through $R$; dyadic row-sum, tail-smallness, and tolerance accounting through $D$; Schur boundary control through $B$; terminal $\RealUp$ sealing through $T$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:toeplitz-schur-carrier}. The analytic transform rows are precisely the coefficient window $A$, the source window $W$, the regular readback $R$, the dyadic ledger $D$, the Schur boundary row $B$, and the terminal seal $T$.

The matrix-facing route is finite. Row $A$ lists only displayed coefficients and row supports, while $W$ lists only the source entries opened for the requested read.

The readback and tolerance rows then bind the transform to BEDC arithmetic. Row $R$ supplies the regular rational readback, $D$ supplies dyadic row-sum and error accounting, and $B$ records the off-window mass and row-sum discrepancy boundary.

Finally $T,H,C,P,N$ seal, transport, replay, source, and name the same packet. Since no coordinate stores host summability, ambient Real completeness, selected limits, quotient stream equality, countable choice, trichotomy, or detached transformed-name equality, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Toeplitz-Schur regular Cauchy handoff]
\label{thm:toeplitz-schur-regular-cauchy-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\ToeplitzSchurUp$ packet must receive the transformed sequence through the finite route
$$
\ToeplitzLemmaUp,\ \HardyCesaroMeanUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp
$$
as displayed by $A,W,R,D,B,T,H,C,P,N$. The handoff is a regular Cauchy sequence-transform boundary; it is not an assertion of ambient $\RealUp$ completeness, host summability, selected limit choice, quotient equality, or trichotomy.
\end{theorem}
\begin{proof}
Start with \autoref{thm:toeplitz-schur-namecert-obligations}. It forces any accepted transform read through the coefficient-window, source-window, readback, dyadic, Schur-boundary, and Real-seal rows before a consumer can use the output.

The sibling Toeplitz and Cesaro packets provide the same row discipline. The Toeplitz route supplies finite matrix windows and row-sum accounting, while the Hardy--Cesaro route supplies the finite average consumer that reads the displayed transformed rows.

The terminal handoff remains attached to $T,H,C,P,N$. These rows make the exported object a named regular Cauchy boundary over displayed data rather than a host-level summability theorem.

Thus every $\RegSeqRatUp$ or $\RealUp$ consumer receives only the finite route listed in the statement. Any request for ambient completeness, selected limits, quotient equality, trichotomy, or detached transformed-name equality lies outside the carrier.
\end{proof}

\closureat{ToeplitzSchurUp}{seedStr}

\begin{closurestatus}{\ToeplitzSchurUp}
  \constructivestory{A $\ToeplitzSchurUp$ packet is generated from Toeplitz--Schur coefficient windows, StreamNameUp source windows, RegSeqRatUp readback, DyadicRatCoreUp row-sum and tail-smallness ledgers, a Schur boundary row, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:toeplitz-schur-carrier}, \autoref{thm:toeplitz-schur-namecert-obligations}, and \autoref{thm:toeplitz-schur-regular-cauchy-handoff} seed the finite Toeplitz--Schur sequence-transform certificate over displayed coefficient windows, source windows, regular readback, dyadic tolerance rows, Schur boundary control, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host summability, ambient $\RealUp$ completeness, selected limits, quotient equality of streams, countable choice, trichotomy, theorem-checked verification, or bridge checking for $\ToeplitzSchurUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, coefficient-window exposure, source-window exposure, regular readback, dyadic row-sum and tail-smallness accounting, Schur boundary control, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.ToeplitzSchurUp.}
\end{closurestatus}
