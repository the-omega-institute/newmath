\chapter{A Concrete Naming Certificate for $\AronszajnAscoliUp$}
\label{ch:concrete-instances-aronszajn-ascoli-namecert}
\origin{human}

Following Aronszajn and Ascoli, the $\AronszajnAscoliUp$ packet records the finite NameCert surface for compactness of a displayed equicontinuous family only after the compact source, continuous-map rows, finite-net modulus data, and terminal real-name handoff are visible. It sits beside the Arzela--Ascoli finite-net packet of \autoref{ch:concrete-instances-arzela-ascoli-finite-net-namecert}, the equicontinuity family surface of \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, and the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names only the BEDC finite route; it is not a host function-space compactness theorem, a selected subnet, quotient equality of maps, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Aronszajn--Ascoli carrier]
\label{def:aronszajn-ascoli-carrier}
An $\AronszajnAscoliUp$ carrier is a finite $\BHist$ packet
$$
A=(K,F,E,N_f,W,R,H,C,P,L)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row and $F$ is the finite family of $\ContinuousMapUp$ rows over that source.
\item $E$ is the $\EquicontinuityFamilyUp$ row, and $N_f$ is the finite-net handoff row consumed from the Arzela--Ascoli finite-net sibling.
\item $W$ records the finite observation windows through $\StreamNameUp$, and $R$ records the regular rational readback and terminal $\RealUp$ seal used by each inspected map value.
\item $H,C,P,L$ are the componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\AronszajnAscoliUp}$ row.
\end{enumerate}
\end{definition}

\begin{theorem}[Aronszajn--Ascoli NameCert obligations]
\label{thm:aronszajn-ascoli-namecert-obligations}
For every accepted $\AronszajnAscoliUp$ carrier $A=(K,F,E,N_f,W,R,H,C,P,L)$, the local $\NameCert_{\AronszajnAscoliUp}$ surface consists exactly of compact-source admission, continuous-map family admission, equicontinuity visibility, finite-net handoff, finite observation windows, regular rational readback, terminal real-name sealing, componentwise transport, displayed replay, provenance, and local naming. Every compactness-facing consumer must read those rows before citing $\AronszajnAscoliUp$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:aronszajn-ascoli-carrier}. The rows $K,F,E,N_f,W,R$ are the only mathematical rows available before structural transport and local naming are inspected.

The compact and continuous-map dependencies are supplied by \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-continuousmap-namecert}. The family-wide modulus information is supplied by \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, while the finite-net handoff is supplied by \autoref{ch:concrete-instances-arzela-ascoli-finite-net-namecert}.

Finally the readback and sealing rows pass through $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before the local name $L$ can be exported. No carrier coordinate remains for a host function-space compactness theorem, selected subnet, quotient equality of maps, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Aronszajn--Ascoli finite equicontinuity route]
\label{thm:aronszajn-ascoli-finite-equicontinuity-route}
Every accepted $\AronszajnAscoliUp$ compactness read factors through the finite route
$$
\CompactMetricUp,\ContinuousMapUp,\EquicontinuityFamilyUp,\ArzelaAscoliFiniteNetUp,\StreamNameUp,\RegSeqRatUp,\RealUp,\hsame,\Cont,\Pkg,\NameCert_{\AronszajnAscoliUp}.
$$
Thus the equicontinuity witness and finite-net witness are displayed before any compactness-facing handoff is public.
\end{theorem}
\begin{proof}
Start with the compact source row $K$ and displayed continuous family row $F$. They bind every inspected map value to the same compact-metric source and the same finite family ledger.

Next read the equicontinuity row $E$ together with the finite-net handoff row $N_f$. These rows provide the shared modulus and the finite source-net boundary that an Arzela--Ascoli style consumer may inspect.

Finally read $W,R,H,C,P,L$. The finite observation windows and real-name seals make each inspected value visible before local naming, so the compactness handoff remains a finite BEDC route and cannot import selected subnets or host function-space compactness.
\end{proof}

\begin{theorem}[Aronszajn--Ascoli compact handoff]
\label{thm:aronszajn-ascoli-compact-handoff}
The $\AronszajnAscoliUp$ packet may hand a compactness-facing consumer to the Arzela--Ascoli and compact-metric siblings only through the carrier rows of \autoref{def:aronszajn-ascoli-carrier}. In particular, the handoff cites \autoref{ch:concrete-instances-arzelaascoli-namecert}, \autoref{ch:concrete-instances-compactmetric-namecert}, \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, and \autoref{ch:concrete-instances-continuousmap-namecert} as displayed NameCert dependencies, not as hidden ambient theorems.
\end{theorem}
\begin{proof}
Use \autoref{thm:aronszajn-ascoli-namecert-obligations} to identify the complete local obligation surface. The only handoff rows are the displayed compact source, continuous family, equicontinuity, finite-net, finite-window, readback, real-seal, and structural rows.

Apply \autoref{thm:aronszajn-ascoli-finite-equicontinuity-route}. It orders the compact and equicontinuity dependencies before the real-name handoff, so the consumer cannot bypass them by citing an ambient compactness principle.

Therefore every compact handoff is a sibling-lattice read through the named concrete-instance chapters. The packet exports no selected subnet, quotient map equality, countable-choice schedule, host function-space compactness theorem, or ambient $\RealUp$ completeness.
\end{proof}

\closureat{AronszajnAscoliUp}{seedStr}

\begin{closurestatus}{\AronszajnAscoliUp}
  \constructivestory{An $\AronszajnAscoliUp$ packet is generated from CompactMetric, ContinuousMap, EquicontinuityFamily, ArzelaAscoliFiniteNet, StreamName, RegSeqRat, Real, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:aronszajn-ascoli-carrier}, \autoref{thm:aronszajn-ascoli-namecert-obligations}, \autoref{thm:aronszajn-ascoli-finite-equicontinuity-route}, and \autoref{thm:aronszajn-ascoli-compact-handoff} seed the finite compactness-facing NameCert surface over compact-source, continuous-family, equicontinuity, finite-net, window, readback, real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function-space compactness, selected subnets, quotient equality of maps, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact-source admission, continuous-family admission, equicontinuity visibility, finite-net handoff, finite-window readback, real sealing, transport, replay, provenance, and local naming for BEDC.Derived.AronszajnAscoliUp.}
\end{closurestatus}
