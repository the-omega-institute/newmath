\chapter{A Concrete Naming Certificate for $\BishopDiagonalProductCauchyUp$}
\label{ch:concrete-instances-bishop-diagonal-product-cauchy-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopDiagonalProductCauchyUp$ packet records the finite product-diagonal route by which two regular Cauchy reads are synchronized before a Real and Completion consumer asks for a product observation. It sits next to the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream scheduler of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the tail comparison surface of \autoref{ch:concrete-instances-cauchy-tail-comparison-namecert}. The packet is a finite-means product certificate: it displays the common window, the two readback rows, the dyadic product budget, and the Real seal, without quotient sequence equality, selected representatives, countable choice, trichotomy, or ambient completeness of $\RealUp$.

\begin{definition}[Bishop diagonal product Cauchy carrier]
\label{def:bishop-diagonal-product-cauchy-carrier}
A $\BishopDiagonalProductCauchyUp$ carrier is a finite $\BHist$ packet
$$
P=(W_X,W_Y,D_X,D_Y,R_X,R_Y,T,M,S,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W_X$ and $W_Y$ are finite $\StreamNameUp$ window rows for the two product coordinates.
\item $D_X$ and $D_Y$ are $\DyadicRatCoreUp$ tolerance rows attached to the same requested product scale.
\item $R_X$ and $R_Y$ are $\RegSeqRatUp$ readback rows served from $W_X,D_X$ and $W_Y,D_Y$.
\item $T$ is the $\CauchyTailComparisonUp$ row witnessing that the two coordinates are compared only after a common finite tail threshold has been displayed.
\item $M$ is the diagonal-product row. It records the finite rule that reads both coordinates at the shared threshold and forms the product observation from the displayed rational data.
\item $S$ is the downstream $\RealUp$ seal row for the accepted product observation.
\item $H$ records componentwise $\hsame$ transport for the two windows, two tolerance rows, two readbacks, tail comparison, product row, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the requested product scale through the common window, dyadic tolerances, regular readbacks, tail comparison, product row, and Real seal.
\item $G$ is the $\Pkg$ provenance over $\BishopDiagonalProductCauchyUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\CauchyTailComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopDiagonalProductCauchyUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient sequence equality, selected representatives, hidden tail choice, countable-choice schedules, trichotomy, host real multiplication, host equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Bishop diagonal product Cauchy carrier exactness]
\label{thm:bishop-diagonal-product-cauchy-carrier}
Every accepted $\BishopDiagonalProductCauchyUp$ carrier exposes exactly the rows
$$
W_X,W_Y,D_X,D_Y,R_X,R_Y,T,M,S,H,C,G,N
$$
before a product observation is served. In particular, the product route is determined by finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerances, $\RegSeqRatUp$ readbacks, the $\CauchyTailComparisonUp$ common-tail row, the diagonal-product row, and the terminal $\RealUp$ seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-diagonal-product-cauchy-carrier}. The analytic rows are the two stream windows, two dyadic tolerance rows, two regular readbacks, the tail comparison row, the product row, and the Real seal.

The structural rows are separate. Row $H$ transports the displayed rows componentwise, row $C$ replays the finite route, row $G$ records provenance, and row $N$ names the packet.

No other coordinate occurs in the carrier. Therefore an accepted product observation can be served only after the listed rows have been displayed.
\end{proof}

\begin{theorem}[Bishop diagonal product Cauchy classifier]
\label{thm:bishop-diagonal-product-cauchy-classifier}
Let $P=(W_X,W_Y,D_X,D_Y,R_X,R_Y,T,M,S,H,C,G,N)$ be an accepted $\BishopDiagonalProductCauchyUp$ carrier. Any classifier read of $P$ first consumes the common finite route
$$
\StreamNameUp \longmapsto
\DyadicRatCoreUp \longmapsto
\RegSeqRatUp \longmapsto
\CauchyTailComparisonUp
$$
on both coordinates, then reads $M$ and $S$. Thus the classifier may use the displayed product observation and its $\RealUp$ seal, but it cannot branch through quotient Cauchy classes, host sequence equality, selected representatives, trichotomy, hidden tail choice, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the two coordinate routes. The classifier must read $W_X,D_X,R_X$ for the first coordinate and $W_Y,D_Y,R_Y$ for the second coordinate before the common-tail row $T$ is meaningful.

The tail comparison row $T$ supplies the shared finite threshold. Only downstream of that row can $M$ form the product observation and $S$ serve the $\RealUp$ seal.

The transport, replay, provenance, and naming rows preserve the same order of consumption. Since none of the carrier coordinates is a quotient Cauchy class, host sequence equality, selected representative, trichotomy branch, hidden tail choice, choice schedule, or ambient-completeness theorem, no accepted classifier read can branch through those objects.
\end{proof}

\begin{theorem}[Bishop diagonal product Cauchy ledger]
\label{thm:bishop-diagonal-product-cauchy-ledger}
The local $\NameCert_{\BishopDiagonalProductCauchyUp}$ ledger for an accepted packet consists exactly of the following five rows:
\begin{enumerate}
\item two finite $\StreamNameUp$ windows and their $\DyadicRatCoreUp$ tolerance rows;
\item two $\RegSeqRatUp$ readbacks served from the displayed windows and tolerances;
\item one $\CauchyTailComparisonUp$ common-tail row and one diagonal-product row;
\item the downstream $\RealUp$ seal with componentwise $\hsame$ transport and displayed $\Cont$ replay;
\item $\Pkg$ provenance and local naming.
\end{enumerate}
These rows exhaust the finite product-diagonal obligation surface.
\end{theorem}
\begin{proof}
The first two ledger rows are the coordinate data. They are exactly the windows, tolerances, and readbacks named by $W_X,W_Y,D_X,D_Y,R_X,R_Y$.

The third row is the product synchronization surface. The common-tail row $T$ fixes the shared finite threshold, and the row $M$ records the product observation at that threshold.

The fourth and fifth rows are structural and terminal. The Real seal $S$, transports $H$, replay $C$, provenance $G$, and local name $N$ account for every remaining coordinate. No additional obligation row is present in the carrier, so the ledger is exhausted.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopDiagonalProductCauchyUp$ product read displays two $\StreamNameUp$ windows, two $\DyadicRatCoreUp$ tolerance rows, two $\RegSeqRatUp$ readbacks, a $\CauchyTailComparisonUp$ common-tail row, a diagonal-product row, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, cauchy-tail-comparison: $\BishopDiagonalProductCauchyUp$ is not bijective to any listed sibling because it jointly carries two synchronized coordinate routes, a common-tail comparison row, a diagonal-product row, a Real seal, and structural NameCert rows in one packet.}

\closureat{BishopDiagonalProductCauchyUp}{seedStr}

\begin{closurestatus}{\BishopDiagonalProductCauchyUp}
  \constructivestory{A $\BishopDiagonalProductCauchyUp$ packet is generated from two StreamName windows, two DyadicRatCore tolerance rows, two RegSeqRat readbacks, a CauchyTailComparison common-tail row, a diagonal-product row, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-diagonal-product-cauchy-carrier}, \autoref{thm:bishop-diagonal-product-cauchy-carrier}, \autoref{thm:bishop-diagonal-product-cauchy-classifier}, and \autoref{thm:bishop-diagonal-product-cauchy-ledger} seed the finite product-diagonal Cauchy route over StreamName, DyadicRatCore, RegSeqRat, CauchyTailComparison, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, quotient sequence equality, selected representatives, hidden tail choice, countable choice, trichotomy, host real multiplication, host equality, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier determinacy, ledger exactness, product-window stability, transport stability, replay stability, provenance non-escape, and local naming for BEDC.Derived.BishopDiagonalProductCauchyUp.}
\end{closurestatus}
