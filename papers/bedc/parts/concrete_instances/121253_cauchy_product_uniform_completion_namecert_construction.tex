\chapter{A Concrete Naming Certificate for $\CauchyProductUniformCompletionUp$}
\label{ch:concrete-instances-cauchy-product-uniform-completion-namecert}
\origin{human}

Following Bishop and Bridges on products of regular Cauchy names and uniform completions, the $\CauchyProductUniformCompletionUp$ packet records the finite route by which paired Cauchy-product rows are transported into the current L10 uniform-completion boundary. It sits after the product source of \autoref{ch:concrete-instances-cauchyproduct-namecert}, the regular product route of \autoref{ch:concrete-instances-regularcauchyproduct-namecert}, the product-completion fusion of \autoref{ch:concrete-instances-cauchy-product-completion-fusion-namecert}, the uniform completion handoff of \autoref{ch:concrete-instances-uniform-completion-reflector-namecert}, and the Bishop regular completion boundary of \autoref{ch:concrete-instances-bishop-regular-completion-namecert}. The packet exposes product-uniform completion transport without quotient pairs, selected limits, host product equality, or ambient completeness.

\begin{definition}[Cauchy product uniform completion carrier]
\label{def:cauchy-product-uniform-completion-carrier}
A $\CauchyProductUniformCompletionUp$ carrier is a finite $\BHist$ packet
$$
P=(A,B,J,U,D,S,Q,R,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\CauchyProductUp$ source row carrying the paired Cauchy product data.
\item $B$ is the $\RegularCauchyProductUp$ regularity row, recording the product-facing regular Cauchy witness before completion transport.
\item $J$ is the $\CauchyProductCompletionFusionUp$ row that synchronizes product-completion and projection data.
\item $U$ is the $\UniformCompletionReflectorUp$ handoff row, read only after $A,B,J$ have exposed the product route.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the product and uniform-completion comparisons.
\item $S$ is the $\StreamNameUp$ finite-window row for the paired product observations.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the same windows and dyadic tolerance budget.
\item $R$ is the terminal $\RealUp$ seal row for completion-facing product reads.
\item $H$ records componentwise $\hsame$ transport for the product, regularity, fusion, uniform-completion, tolerance, window, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a product-completion request through $A,B,J,U,D,S,Q,R,H$.
\item $G$ is the $\Pkg$ provenance over $\CauchyProductUniformCompletionUp$, $\CauchyProductUp$, $\RegularCauchyProductUp$, $\CauchyProductCompletionFusionUp$, $\UniformCompletionReflectorUp$, $\BishopRegularCompletionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CauchyProductUniformCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $A,B,J,U,D,S,Q,R,H,C,G,N$. It has no coordinate for quotient pair representatives, selected completion limits, host product equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy product uniform completion obligations]
\label{thm:cauchy-product-uniform-completion-obligations}
For every accepted $\CauchyProductUniformCompletionUp$ carrier $P=(A,B,J,U,D,S,Q,R,H,C,G,N)$, the local $\NameCert_{\CauchyProductUniformCompletionUp}$ surface consists exactly of Cauchy product admission through $A$, regular product exposure through $B$, product-completion fusion through $J$, uniform-completion handoff through $U$, dyadic tolerance readback through $D$, finite product windows through $S$, regular rational readback through $Q$, terminal Real sealing through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $G$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-product-uniform-completion-carrier}. The product rows $A,B,J$ expose the Cauchy product source, regularity witness, and product-completion fusion before any uniform-completion handoff is visible.

Read $U,D,S,Q$ next. The uniform-completion reflector row is tied to the same dyadic tolerance ledger, finite product windows, and regular rational readback, so the transport is not a host-side map between completed quotient pairs.

The terminal Real seal $R$ is downstream of those displayed rows. Transport, replay, provenance, and local naming through $H,C,G,N$ preserve the same finite packet.

Since the classifier has no coordinate for quotient pair representatives, selected completion limits, host product equality, countable choice, trichotomy, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Cauchy product uniform-completion product route]
\label{thm:cauchy-product-uniform-completion-product-route}
Every completion-facing product read from an accepted $\CauchyProductUniformCompletionUp$ carrier factors through the finite route
$$
\CauchyProductUp,\ \RegularCauchyProductUp,\ \CauchyProductCompletionFusionUp,\ \UniformCompletionReflectorUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
The route transports paired Cauchy-product rows to the uniform-completion handoff before the Real seal is public, and it exports no quotient pair, selected product limit, host product equality, countable-choice schedule, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Use the obligation theorem \autoref{thm:cauchy-product-uniform-completion-obligations}. It places the Cauchy product row, regular product row, and product-completion fusion row before the uniform-completion handoff.

Apply the sibling completion boundary of \autoref{ch:concrete-instances-bishop-regular-completion-namecert}. The dyadic tolerance, StreamName window, RegSeqRat readback, and Real-seal order used there is the same L10 order consumed by the product route.

Now read the product-completion fusion sibling of \autoref{ch:concrete-instances-cauchy-product-completion-fusion-namecert}. It synchronizes product-completion and projection data before any component or product-facing completion consumer can inspect the terminal seal.

Finally replay $H,C,G,N$. These rows keep the uniform-completion transport attached to the displayed product rows, so the public read is the finite product-uniform route rather than a quotient pair, selected limit, host product equality, choice schedule, or ambient-completeness claim.
\end{proof}

\begin{theorem}[Cauchy product uniform completion sibling compatibility]
\label{thm:cauchy-product-uniform-completion-sibling-compatibility}
The $\CauchyProductUniformCompletionUp$ carrier is compatible with the finite-data product-window route of \autoref{ch:concrete-instances-prod-namecert} and the regular sequence common-window route of \autoref{ch:concrete-instances-regular-sequence-space-namecert}. Any sibling consumer must keep the product rows $A,B,J$, the uniform-completion row $U$, the tolerance and window rows $D,S,Q$, and the Real seal row $R$ visible before it uses transport, replay, provenance, or local naming.
\end{theorem}
\begin{proof}
Project the product route theorem \autoref{thm:cauchy-product-uniform-completion-product-route}. It already fixes the order from Cauchy product rows through uniform completion, dyadic tolerances, finite windows, RegSeqRat readback, and the terminal Real seal.

Read the product finite-data route through \autoref{ch:concrete-instances-prod-namecert}. Its paired-window surface supplies product data only as displayed finite rows, not as hidden host tuples or selected projections.

Read the common-window completion route through \autoref{ch:concrete-instances-regular-sequence-space-namecert}. That route keeps StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, and Real seals public before completion-facing comparisons are consumed.

The rows $H,C,G,N$ then transport and name the same packet. Hence every sibling consumer must preserve $A,B,J,U,D,S,Q,R$ before structural replay, and no sibling can replace the route by quotient pairs, selected limits, host product equality, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyProductUniformCompletionUp$ consumer must display CauchyProduct, RegularCauchyProduct, CauchyProductCompletionFusion, UniformCompletionReflector, DyadicRatCore, StreamName, RegSeqRat, RealUp, hsame, Cont, Pkg, and local NameCert rows before a product-uniform-completion read is public.}

\independenceWitness{cauchyproduct, regularcauchyproduct, cauchy-product-completion-fusion, uniform-completion-reflector, bishop-regular-completion, prod, regular-sequence-space: $\CauchyProductUniformCompletionUp$ is not bijective to any listed sibling because it binds paired Cauchy product rows, product-completion fusion, uniform-completion transport, dyadic tolerances, finite windows, regular rational readback, Real sealing, transport, replay, provenance, and local naming as one product-uniform carrier.}

\closureat{CauchyProductUniformCompletionUp}{seedStr}

\begin{closurestatus}{\CauchyProductUniformCompletionUp}
  \constructivestory{A $\CauchyProductUniformCompletionUp$ packet is generated from CauchyProduct rows, RegularCauchyProduct regularity, CauchyProductCompletionFusion synchronization, UniformCompletionReflector handoff, DyadicRatCore tolerances, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-product-uniform-completion-carrier}, \autoref{thm:cauchy-product-uniform-completion-obligations}, \autoref{thm:cauchy-product-uniform-completion-product-route}, and \autoref{thm:cauchy-product-uniform-completion-sibling-compatibility} seed the finite product-uniform-completion route for L10 completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient pair representatives, selected product limits, host product equality, countable choice, trichotomy, ambient RealUp completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, product-row exposure, product-completion fusion, uniform-completion handoff, dyadic tolerance exactness, finite-window readback, RegSeqRat handoff, Real seal nonescape, sibling compatibility, transport, replay, provenance, and local naming for BEDC.Derived.CauchyProductUniformCompletionUp.}
\end{closurestatus}
