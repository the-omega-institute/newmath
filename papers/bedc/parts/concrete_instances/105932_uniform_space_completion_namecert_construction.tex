\chapter{A Concrete Naming Certificate for $\UniformSpaceCompletionUp$}
\label{ch:concrete-instances-uniform-space-completion-namecert}
\origin{human}

Following Bourbaki--Kelley uniform-space completion practice, the $\UniformSpaceCompletionUp$ packet records the finite route by which a uniform entourage source admits Cauchy-filter or Cauchy-net data, passes through dense-source and separated-reflection rows, and reaches a completion seal without turning the seal into quotient classes. It sits after the uniform entourage surface of \autoref{ch:concrete-instances-uniformspace-namecert}, the Cauchy filter surface of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the generic completion packet of \autoref{ch:concrete-instances-completion-namecert}, the metric comparison route of \autoref{ch:concrete-instances-metric-completion-comparison-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the L10 completion handoff as displayed NameCert data; it is not a quotient completion theorem, not a selected-limit operator, not countable choice, not trichotomy, not host equality, and not ambient $\RealUp$ completeness.

\begin{definition}[UniformSpaceCompletion carrier]
\label{def:uniform-space-completion-carrier}
A $\UniformSpaceCompletionUp$ carrier is a finite $\BHist$ packet
$$
K=(U,F,D,S,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the uniform entourage source row, read from the $\UniformSpaceUp$ interface before any Cauchy or completion demand is accepted.
\item $F$ is the Cauchy filter or Cauchy net row, exposing exactly the finite entourage tests used by the completion consumer.
\item $D$ is the dense-source embedding row, recording the source-to-completion handoff that lets original points enter the completion surface.
\item $S$ is the separated reflection row, forcing indistinguishable Cauchy data to pass through a displayed separated interface before the completion seal is read.
\item $E$ is the completion seal row, recording the accepted completion name after $U,F,D,S$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the uniform source, Cauchy row, dense-source row, separated-reflection row, completion seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from Cauchy demands through the uniform source, dense-source handoff, separated reflection, and completion seal.
\item $P$ is the $\Pkg$ provenance over $\UniformSpaceCompletionUp$, the seven sibling chapters named above, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformSpaceCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,F,D,S,E,H,C,P,N$ and their displayed replay paths. It has no coordinate for quotient representatives, selected limits, countable choice, trichotomy, host equality of completion classes, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[UniformSpaceCompletion NameCert obligations]
\label{thm:uniform-space-completion-namecert-obligations}
For every accepted $\UniformSpaceCompletionUp$ carrier $K=(U,F,D,S,E,H,C,P,N)$, the local $\NameCert_{\UniformSpaceCompletionUp}$ surface consists exactly of five completion-facing obligations: carrier admission for the displayed finite packet; uniform-Cauchy exactness of the $F$ row against the entourage source $U$; dense-source handoff through $D$; separated-reflection non-escape through $S$ before the seal is consumed; and completion-seal stability through $E,H,C,P,N$. Every completion consumer must read those five rows in that order before it can export a completion name.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-space-completion-carrier}. The packet first displays the uniform entourage source $U$ and the Cauchy row $F$, so the Cauchy demand is tested only against named finite entourages rather than against an ambient host uniformity.

The dense-source and separated-reflection rows then control the handoff. The row $D$ records how original source data enters the completion surface, and $S$ records that indistinguishable Cauchy data cannot escape the displayed separated interface before the completion seal is read.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same finite route. Because the carrier has no row for quotient completion classes, selected limits, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness, none of those principles can be projected as a NameCert obligation.
\end{proof}

\falsifiablePrediction{Every accepted $\UniformSpaceCompletionUp$ consumer that reaches a completion seal while omitting the uniform entourage source, Cauchy filter or net row, dense-source embedding row, separated-reflection row, completion-seal row, transport, replay, provenance, or local naming row refutes the packet.}

\independenceWitness{uniformspace, cauchyfilter, completion, metric-completion-comparison, streamname, regseqrat, real: no listed sibling is bijective with $\UniformSpaceCompletionUp$ because the packet jointly carries a uniform entourage source, Cauchy filter or net exactness, dense-source handoff, separated-reflection non-escape, completion seal stability, transport, replay, provenance, and local naming.}

\closureat{UniformSpaceCompletionUp}{seedStr}

\begin{closurestatus}{\UniformSpaceCompletionUp}
  \constructivestory{A $\UniformSpaceCompletionUp$ packet is generated from a uniform entourage source row, a Cauchy filter or Cauchy net row, a dense-source embedding row, a separated-reflection row, a completion seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-space-completion-carrier} and \autoref{thm:uniform-space-completion-namecert-obligations} seed the finite L10 uniform completion route over uniform source, Cauchy exactness, dense-source handoff, separated reflection, seal stability, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion classes, selected limits, countable choice, trichotomy, host equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, uniform-Cauchy exactness, dense-source handoff, separated-reflection non-escape, completion-seal stability, transport replay, provenance non-escape, and TasteGate field faithfulness for BEDC.Derived.UniformSpaceCompletionUp.}
\end{closurestatus}
