\closureat{NumFieldUp}{bridgedStr}[BEDC.Derived.NumFieldUp.NumFieldReflexiveRational\_exactness\_package]

\begin{closurestatus}{\NumFieldUp}
  \constructivestory{The number-field surface closed here is the reflexive rational extension: start with a $\RatUp$ history endpoint, use the same $\RatUp$ carrier and classifier for both the base and ambient fields, and consume the identity $\FieldExtUp$ row whose embedding sends each rational endpoint to itself. A history is admitted exactly when it is already a RatUp carrier history in this degree-one extension; no polynomial root, second basis vector, algebraic-integer endpoint, or separate ambient carrier is accepted. The classifier is simply the RatUp history classifier, transported through the identity embedding and the right-unit coordinate readback, so two displayed number-field endpoints are the same precisely when the underlying rational histories are classified as the same rational. The source specification is the RatUp field-operation table together with the reflexive $\FieldExtUp$ interface, the pattern specification is identity embedding, singleton unary basis, scalar action by RatUp multiplication, coordinate readback, basis exhaustion, and exact endpoint classification, and the ledger keeps the carrier, operation-table, finite-extension, basis, coordinate, embedding, scalar-action, and provenance rows visible. The five name-certificate obligations are inherited from RatUp and FieldExtUp: an accepted rational endpoint inhabits the carrier, the RatUp classifier supplies reflexivity, symmetry, and transitivity, and identity transport keeps every accepted endpoint inside the same degree-one package. A history $h$ lies in the signature gap of a $\NumFieldUp$ token $p$ when the source-domain accepts $h$ as a rational endpoint in this reflexive extension, the signature relation pairs it with an answer endpoint and the singleton-basis evidence ledger, and the token-introduction relation names that answer as $p$; unfolded, $h$ is the rational coordinate encoded by $p$ and its embedding coordinate is the same RatUp endpoint. The chapter registers this degree-one reflexive construction under the name $\NumFieldUp$; it introduces no nontrivial number field, Galois structure, algebraic-integer theory, public-object closure, or standard bridge.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{\autoref{thm:numfield-rat-reflexive-standard-bridge-boundary} is the paper-side bridge boundary for the RatUp reflexive NumFieldUp interface over FieldExtUp: BHist, RatHistoryCarrier, RatHistoryClassifier, the reflexive degree-one carrier, finite-extension witness, coordinate readback, singleton-basis exhaustion, ledger exactness, the NameCert obligation package, and the exactness package.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.NumFieldUp.NumFieldReflexiveRational\_exactness\_package}
  \bridgestatus{none}
  \notclaimed{Nontrivial number fields, algebraic integer theory, Galois structure, public-object closure, and standard bridges are not claimed here.}
  \upgradepath{Further movement is formal bridge verification for the displayed degree-one package, not additional paper-side closure.}
\end{closurestatus}
