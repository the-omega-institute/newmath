\chapter{A Concrete Naming Certificate for $\AffineSpaceUp$}
\label{ch:concrete-instances-affinespace-namecert}

The affine-space interface $\AffineSpaceUp$ packages a torsor over a vector-space carrier: a set with a free transitive action of vector translations. It depends on $\NameCert_{\VecSpaceUp}$ (\autoref{ch:concrete-instances-vecspace-namecert}) and $\NameCert_{\GroupUp}$ (\autoref{ch:concrete-instances-group-namecert}).

\section{Carrier definition}
\label{sec:affinespace-carrier}

\section{The certificate}
\label{sec:affinespace-certificate}

\closureat{AffineSpaceUp}{seedStr}

\begin{closurestatus}{\AffineSpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed binds $\AffineSpaceUp$ to the torsor-over-$\VecSpaceUp$ orientation and the $\GroupUp$ translation rows stated at the chapter surface.}
  \formalstatus{\unformalizedV}
  \leantarget{}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim the affine carrier, classifier, action laws, freeness, transitivity, standard bridge, or Lean verification.}
  \upgradepath{Obligation closure requires carrier, classifier, action compatibility, freeness, transitivity, and ledger-boundary theorem blocks.}
\end{closurestatus}
