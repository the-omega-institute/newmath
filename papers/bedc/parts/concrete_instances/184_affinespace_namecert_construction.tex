\chapter{A Concrete Naming Certificate for $\AffineSpaceUp$}
\label{ch:concrete-instances-affinespace-namecert}

The affine-space interface $\AffineSpaceUp$ packages a torsor over a vector-space carrier: a set with a free transitive action of vector translations. It depends on $\NameCert_{\VecSpaceUp}$ (\autoref{ch:concrete-instances-vecspace-namecert}) and $\NameCert_{\GroupUp}$ (\autoref{ch:concrete-instances-group-namecert}).

\section{Carrier definition}
\label{sec:affinespace-carrier}

\section{The certificate}
\label{sec:affinespace-certificate}

\closureat{AffineSpaceUp}{seedStr}

\begin{closurestatus}{\AffineSpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{The chapter is registered as a torsor-over-$\VecSpaceUp$ seed depending on the $\VecSpaceUp$ and $\GroupUp$ certificate chapters; no carrier, classifier, vector action, free-transitive law, or exactness obligation is closed here.}
  \formalstatus{\unformalizedV}
  \leantarget{}
  \bridgestatus{none}
  \notclaimed{No affine carrier, classifier, vector action, free-transitive law, stability certificate, ledger exactness, or standard bridge is claimed.}
  \upgradepath{Raise to obligationClosure by adding the carrier, classifier, vector-action, identity/action, free-transitive, stability, ledger, and exactness obligation theorem blocks.}
\end{closurestatus}
