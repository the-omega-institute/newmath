\chapter{A Concrete Naming Certificate for $\RegularCauchyMetricUp$}
\label{ch:concrete-instances-regularcauchymetric-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyMetricUp$ packet names the metric-facing dyadic closeness route for two displayed regular rational Cauchy names before the completed $\RealUp$ seal is read. It sits over the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the common finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the real metric consumer surface of \autoref{ch:concrete-instances-real-metric-namecert}. The packet exposes a finite distance route for regular Cauchy names; it does not identify quotient reals, assert host real equality, choose limits, use trichotomy, or claim completeness of $\RealUp$.

\begin{definition}[Regular Cauchy metric carrier]
\label{def:regular-cauchy-metric-carrier}
A $\RegularCauchyMetricUp$ carrier is a finite $\BHist$ packet
$$
M_{\mathrm{rc}}=(R_0,R_1,W,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R_0$ is the left $\RegSeqRatUp$ regular rational name row.
\item $R_1$ is the right $\RegSeqRatUp$ regular rational name row.
\item $W$ is the common $\StreamNameUp$ finite-window ledger naming the paired observation windows inspected from $R_0$ and $R_1$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the requested dyadic radius and the rational comparison budget for the two observed windows.
\item $Q$ is the $\DyadicRatCoreUp$ distance row naming the displayed dyadic distance bound extracted from the same windows and tolerance ledger.
\item $E$ is the $\RealUp$ seal boundary reached only after the regular rows, common windows, dyadic tolerance row, and dyadic distance row have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two regular names, common windows, dyadic tolerance row, dyadic distance row, Real seal row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from every metric-facing read through $R_0,R_1,W,D,Q,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyMetricUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RealMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R_0,R_1,W,D,Q,E,H,C,P,N$. The carrier has no coordinate for quotient real equality, host real equality, selected limits, trichotomy, ambient Real completeness, or a detached completed-real distance.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyMetricUp.RegularCauchyMetricTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy metric NameCert obligations]
\label{thm:regular-cauchy-metric-namecert-obligations}
For every accepted $\RegularCauchyMetricUp$ carrier $M_{\mathrm{rc}}=(R_0,R_1,W,D,Q,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyMetricUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ source rows $R_0,R_1$, the common $\StreamNameUp$ window ledger $W$, the $\DyadicRatCoreUp$ tolerance row $D$, the $\DyadicRatCoreUp$ distance row $Q$, the $\RealUp$ seal boundary $E$, and the structural rows $H,C,P,N$;
\item common-window exposure, requiring every metric-facing read to inspect the paired windows named by $W$ before any dyadic comparison row is read;
\item dyadic tolerance exactness, requiring $D$ to supply the requested radius and comparison budget before the distance bound $Q$ is exposed;
\item dyadic distance handoff, requiring $Q$ to be the only displayed distance row handed to the downstream $\RealMetricUp$ consumer;
\item Real seal boundary, requiring $E$ to be consumed only after the route through $R_0,R_1,W,D,Q$ has been displayed;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same two regular names, common windows, dyadic tolerance row, dyadic distance row, Real seal boundary, provenance, and local naming row;
\item package provenance and local naming, requiring $P$ and $N$ to name exactly the listed sibling certificates and the same finite packet;
\item refusal of quotient real equality, host real equality, selected limits, trichotomy, and Real completeness as NameCert data.
\end{enumerate}
No NameCert obligation is stored outside these finite rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The analytic coordinates are the two regular rational names $R_0,R_1$, the common window ledger $W$, the dyadic tolerance row $D$, the dyadic distance row $Q$, and the Real seal boundary $E$; the remaining rows transport, replay, source, and name the same packet.

Read the metric-facing route in dependency order. A distance request first opens the paired finite windows named by $W$ from the two $\RegSeqRatUp$ rows, then reads the dyadic radius and comparison budget in $D$, and only then exposes the dyadic distance row $Q$.

The downstream Real-facing boundary is therefore ordered. The row $E$ is reached after the regular names and dyadic rows have been displayed, and the $\RealMetricUp$ consumer receives $Q$ as a finite distance row rather than a host completed-real distance.

Finally apply componentwise transport and replay. The rows $H$ and $C$ preserve the same displayed route, while $P$ and $N$ name exactly the sibling certificate dependencies. Since no carrier coordinate stores quotient equality, host equality, selected limits, trichotomy, or Real completeness, none of those principles can be exported by the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy metric dyadic closeness route]
\label{thm:regular-cauchy-metric-dyadic-closeness-route}
Every accepted $\RegularCauchyMetricUp$ consumer factors a metric-facing closeness read through the finite route
$$
(R_0,R_1)\longmapsto W\longmapsto D\longmapsto Q\longmapsto E
$$
before it may hand the read to a $\RealMetricUp$ surface. Thus the consumer receives regular rational readback, common finite windows, dyadic tolerance data, a dyadic distance bound, and a Real seal boundary; it receives no quotient real equality, host real equality, selected limit, trichotomy, or Real completeness coordinate.
\end{theorem}
\begin{proof}
Start with an accepted carrier $M_{\mathrm{rc}}=(R_0,R_1,W,D,Q,E,H,C,P,N)$. The only source rows for the closeness read are the two regular rational names $R_0$ and $R_1$, and the only observation route attached to them is the common finite window ledger $W$.

The dyadic request then passes through $D$ and $Q$. The tolerance row $D$ names the radius and comparison budget, while the distance row $Q$ names the bound extracted from the same displayed windows; no second distance row is present.

Replay the route through $C$ and transport it through $H$. The replay reaches the Real seal boundary $E$ only after the regular rows, windows, tolerance row, and distance row have been displayed.

The provenance row $P$ points to the sibling surfaces of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-namecert}, and \autoref{ch:concrete-instances-real-metric-namecert}. Together with $N$, it packages the same finite route and adds no coordinate for quotient equality, host equality, selected limits, trichotomy, or completeness.
\end{proof}

\begin{closurestatus}{\RegularCauchyMetricUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-metric-carrier}, \autoref{thm:regular-cauchy-metric-namecert-obligations}, and \autoref{thm:regular-cauchy-metric-dyadic-closeness-route} seed the metric-facing finite dyadic closeness surface for two $\RegSeqRatUp$ rows over common $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance and distance rows, $\RealUp$ seal data, and $\RealMetricUp$ consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality, host real equality, selected limits, trichotomy, Real completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, common-window exposure, dyadic tolerance exactness, dyadic distance handoff, Real seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyMetricUp.}
  \constructivestory{A $\RegularCauchyMetricUp$ packet is generated from two $\RegSeqRatUp$ rows, common $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance and distance rows, a $\RealUp$ seal boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
