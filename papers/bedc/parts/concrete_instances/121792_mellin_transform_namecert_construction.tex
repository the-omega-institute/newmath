\chapter{A Concrete Naming Certificate for $\MellinTransformUp$}
\label{ch:concrete-instances-mellin-transform-namecert}
\origin{human}

Following Mellin and Titchmarsh, the $\MellinTransformUp$ packet records the finite positive-real source, integrand-window, Gamma handoff, Real-seal, transport, replay, provenance, and local naming rows needed before an analytic consumer may read the classical transform route. It sits above the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the stream schedule discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the Gamma interface of \autoref{ch:concrete-instances-gamma-namecert}. The analytic motivation is the Gamma bridge of \autoref{sec:analysis-spine-gamma}; this packet names the transform-facing finite data without importing host function equality, measure-theoretic choice, or an ambient integral object.

\begin{definition}[Mellin transform carrier]
\label{def:mellin-transform-carrier}
A $\MellinTransformUp$ carrier is a finite $\BHist$ packet
$$
M=(S,W,I,G,R,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the positive-real source row, exposing the $\RealUp$ domain name and the apartness-from-zero ledger used by the transform request.
\item $W$ is the integrand-window row, recording the finite $\StreamNameUp$ schedule of sampled positive-real windows.
\item $I$ is the regular-integrand row, read through $\RegSeqRatUp$ endpoints and dyadic tolerance ledgers on the displayed windows.
\item $G$ is the Gamma-handoff row, naming the $\GammaUp$ boundary data consumed by the transform-facing branch.
\item $R$ is the Real-seal row that repacks accepted window sums as a $\RealUp$ output name.
\item $T$ records componentwise $\hsame$ transport for source, window, integrand, Gamma handoff, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Mellin-transform request through $S,W,I,G,R,T,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MellinTransformUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\GammaUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\MellinTransformUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,W,I,G,R,T,C,P,N$. It has no coordinate for host function equality, selected measurable representatives, a Lebesgue integral, quotient stream equality, countable choice, or an ambient analytic completion.
\end{definition}

\begin{theorem}[Mellin transform NameCert obligations]
\label{thm:mellin-transform-namecert-obligations}
For every accepted $\MellinTransformUp$ carrier $M=(S,W,I,G,R,T,C,P,N)$, the local $\NameCert_{\MellinTransformUp}$ surface consists exactly of positive-real source exposure, integrand-window exposure, regular-integrand readback, Gamma-handoff exposure, Real-seal output exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every transform consumer must read
$$
S,W,I,G,R,T,C,P,N
$$
before the packet is consumed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:mellin-transform-carrier}. The source, window, integrand, Gamma handoff, and Real seal data occur only as displayed rows of the same finite packet.

The route first reads the positive-real source $S$ and the finite schedule $W$. The regular-integrand row $I$ then supplies the $\RegSeqRatUp$ readbacks on those windows, while $G$ records the Gamma-facing boundary that the analytic spine may consume.

The Real-seal row $R$ is downstream of $S,W,I,G$. Transport, replay, provenance, and local naming are the componentwise rows $T,C,P,N$.

Since no carrier coordinate stores host function equality, measurable representatives, a Lebesgue integral, quotient stream equality, countable choice, or ambient analytic completion, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Mellin transform Gamma handoff]
\label{thm:mellin-transform-gamma-handoff}
An accepted $\MellinTransformUp$ packet can hand data to the Gamma bridge only through the displayed Gamma row $G$ together with the positive-real source row $S$, integrand-window row $W$, regular-integrand row $I$, and Real-seal row $R$. The handoff does not create a meromorphic black box, a hidden integral, or a selected analytic representative outside the finite carrier.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:mellin-transform-namecert-obligations}. They force every consumer to expose $S,W,I,G,R$ before the transform packet is usable.

The Gamma row $G$ is only a handoff row. It names the Gamma-facing boundary already isolated in \autoref{sec:analysis-spine-gamma}, and its inputs remain the positive-real source, finite integrand windows, regular-rational readbacks, and Real seal displayed by this packet.

No additional analytic object is available to project from the carrier. Therefore the handoff is exactly the finite route through $S,W,I,G,R,T,C,P,N$ and not a hidden integral or selected representative.
\end{proof}

\falsifiablePrediction{A MellinTransformUp packet whose Gamma handoff is read without the displayed positive-real source, integrand-window, regular-integrand, and Real-seal rows fails local NameCert consumption.}

\independenceWitness{gamma, real, stream-name, regseqrat, gamma-zeta-factor-seal: $\MellinTransformUp$ is not bijective to any listed sibling because this packet jointly carries positive-real source exposure, integrand windows, regular-rational readback, Gamma handoff, Real seal, transport, replay, provenance, and local naming.}

\closureat{MellinTransformUp}{seedStr}

\begin{closurestatus}{\MellinTransformUp}
  \constructivestory{A $\MellinTransformUp$ packet is generated from a positive-real source row, finite integrand-window row, regular-integrand readback row, Gamma handoff row, Real-seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:mellin-transform-carrier}, \autoref{thm:mellin-transform-namecert-obligations}, and \autoref{thm:mellin-transform-gamma-handoff} seed the finite Mellin-transform route over source, window, integrand, Gamma handoff, Real seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, measure-theoretic choice, a Lebesgue integral, meromorphic continuation, quotient stream equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source admission, integrand-window exactness, regular-integrand readback, Gamma-handoff exactness, Real-seal exactness, transport, replay, provenance, and local naming for BEDC.Derived.MellinTransformUp.}
\end{closurestatus}
