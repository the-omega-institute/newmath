\chapter{A Concrete Naming Certificate for $\DyadicRationalBallBasisUp$}
\label{ch:concrete-instances-dyadic-rational-ball-basis-namecert}
\origin{human}

Following Bishop--Bridges constructive metric-space practice, the $\DyadicRationalBallBasisUp$ packet records a finite dyadic ball basis for real-line and metric-completion consumers before any open-ball primitive is available. It sits after the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the metric source interface of \autoref{ch:concrete-instances-metric-namecert}. The finite ball-basis route also consumes the interval-cell refinement surface of \autoref{ch:concrete-instances-dyadic-interval-net-namecert} when a metric or Real consumer turns a dyadic center-radius row into a displayed interval cover. The packet gives Real, separability, and completion arguments a displayed basis element with center, radius, tolerance, readback, seal, transport, provenance, and local naming rows; it does not import host open balls, quotient real equality, located choice of centers, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Dyadic rational ball basis carrier]
\label{def:dyadic-rational-ball-basis-carrier}
A $\DyadicRationalBallBasisUp$ carrier is a finite $\BHist$ packet
$$
B=(C,R,D,W,Q,E,H,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\DyadicRatCoreUp$ center row, recording the dyadic rational center and its endpoint arithmetic.
\item $R$ is the positive dyadic radius row, with the radius positivity and radius-comparison checks read through the same dyadic ledger as $C$.
\item $D$ is the dyadic tolerance ledger for basis containment, refinement, and finite overlap reads.
\item $W$ is the $\StreamNameUp$ finite-window row used when a real or metric consumer inspects the center, radius, or tolerance schedule.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the inspected dyadic data before any completed real boundary is opened.
\item $E$ is the $\RealUp$ seal row for the center and radius comparisons, available only after $C,R,D,W,Q$ are displayed.
\item $H$ records componentwise $\hsame$ transport for the center, radius, tolerance, window, readback, seal, provenance, and local naming rows.
\item $P$ records $\Pkg$ provenance over $\DyadicRationalBallBasisUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\MetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicRationalBallBasisUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $C,R,D,W,Q,E,H,P,N$. It has no coordinate for host open balls, selected dense enumerations, quotient real equality, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dyadic rational ball basis NameCert obligations]
\label{thm:dyadic-rational-ball-basis-namecert-obligations}
For every accepted $\DyadicRationalBallBasisUp$ carrier
$$
B=(C,R,D,W,Q,E,H,P,N),
$$
the local $\NameCert_{\DyadicRationalBallBasisUp}$ surface consists exactly of dyadic center admission through $C$, positive dyadic radius admission through $R$, tolerance-ledger admission through $D$, finite-window exposure through $W$, regular-rational readback through $Q$, real-seal exposure through $E$, componentwise transport through $H$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-rational-ball-basis-carrier}. The analytic rows are the dyadic center $C$, positive radius $R$, tolerance ledger $D$, finite window $W$, regular readback $Q$, and Real seal $E$.

Read the rows in the displayed order. The center and radius rows are dyadic data, the tolerance row records containment and refinement bounds, the window row exposes the finite observations, and the readback row turns those observations into regular rational data before the seal is consumed.

Finally use the structural rows $H,P,N$. Transport, provenance, and local naming preserve that same finite route. Since no host open-ball, quotient-equality, dense-choice, trichotomy, choice, or ambient completeness coordinate appears in the carrier, none can be exported by the local certificate.
\end{proof}

\begin{theorem}[Dyadic rational ball basis Real coverage route]
\label{thm:dyadic-rational-ball-basis-real-coverage}
Every Real-facing or metric-facing consumer of an accepted $\DyadicRationalBallBasisUp$ carrier reads a basis element through the finite route
$$
C,R,D \longmapsto W,Q \longmapsto E .
$$
The dyadic center, positive radius, and tolerance ledger expose the ball data before the $\StreamNameUp$ window, $\RegSeqRatUp$ readback, and $\RealUp$ seal are available. Thus a local metric-ball argument receives a displayed dyadic basis row, not a host open-ball primitive, selected representative, quotient real equality, countable-choice dense set, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:dyadic-rational-ball-basis-namecert-obligations}. They force every accepted read to expose the center, radius, and tolerance rows before any Real-facing endpoint can be inspected.

Next read the $\StreamNameUp$ and $\RegSeqRatUp$ rows. The finite window $W$ supplies the inspected observations, while $Q$ supplies regular rational readback for the same dyadic center and radius data.

Finally open the $\RealUp$ seal $E$. Because $E$ is downstream of $C,R,D,W,Q$ and the structural rows only transport, source, and name the same packet, the coverage route remains a finite dyadic ball-basis certificate. No excluded host open-ball, quotient, selected-representative, choice, or completeness endpoint is present.
\end{proof}

\falsifiablePrediction{A $\DyadicRationalBallBasisUp$ consumer that reaches $\RealUp$ or $\MetricUp$ ball language without displaying $C,R,D,W,Q$ refutes the carrier surface.}

\begin{closurestatus}{\DyadicRationalBallBasisUp}
  \constructivestory{A $\DyadicRationalBallBasisUp$ packet is generated from a dyadic center row, positive dyadic radius row, dyadic tolerance ledger, StreamName window row, RegSeqRat readback row, Real seal row, componentwise hsame transport, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-rational-ball-basis-carrier}, \autoref{thm:dyadic-rational-ball-basis-namecert-obligations}, and \autoref{thm:dyadic-rational-ball-basis-real-coverage} seed the finite dyadic ball-basis route over center, radius, tolerance, finite-window, readback, Real-seal, transport, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host open-ball primitives, quotient real equality, selected dense enumerations, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-radius admission, tolerance-ledger routing, StreamName window readback, RegSeqRat handoff, Real seal nonescape, transport, provenance, and local naming for BEDC.Derived.DyadicRationalBallBasisUp.}
\end{closurestatus}
