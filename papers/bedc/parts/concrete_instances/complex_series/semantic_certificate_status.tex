\closureat{ComplexSeriesUp}{seedStr}

\begin{closurestatus}{\ComplexSeriesUp}
  \constructivestory{A complex series is generated by feeding a unary index into a family of $\ComplexUp$ histories, then building its partial sums by the closed recursion that starts at the chosen zero history and appends the current term at each b1-successor. The carrier accepts term families whose partial-sum sequence comes with a convergence witness to a carried limit, while families without complex terms at unary indices, without closed partial sums, or without the convergence row remain outside this surface. Two accepted series classify together when their displayed limits agree componentwise under $\hsame$, and the source specification is the convergent term-sequence row, the pattern specification is regular Cauchy convergence of the partial sums with explicit modulus, and the ledger keeps the term sequence, partial sums, absolute-modulus sums, Cauchy product, and pointwise addition rows visible. The five name-certificate obligations are witnessed by the zero partial-sum recursion for inhabitation, by the $\hsame$ equivalence laws on limits, and by the transport results saying that pointwise-equal terms have classified partial sums and the same convergence target. A term-family history $h$ lies in the signature gap of a $\ComplexSeriesUp$ token $p$ when the source-domain accepts its convergent partial-sum package, the signature relation pairs it with an answer limit and evidence ledger, and the token-introduction relation names that limit as $p$; unfolded, the partial sums of $h$ converge with the displayed modulus to the complex limit encoded by $p$. The chapter registers this partial-sum convergence construction under the name $\ComplexSeriesUp$; it introduces no completeness theorem for arbitrary series carriers, no analytic-continuation machinery, and no standard bridge.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The chapter currently fixes complex term sequences, partial sums, absolute convergence, Cauchy product, and ledger-policy rows at the paper level.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.ComplexSeriesUp.complex\_series\_semantic\_name\_certificate}
  \bridgestatus{none}
  \notclaimed{The chapter does not close completeness of complex series carriers or analytic continuation results.}
  \upgradepath{The next paper grade requires public and bridge paper work for the complex-series semantic certificate.}
\end{closurestatus}
