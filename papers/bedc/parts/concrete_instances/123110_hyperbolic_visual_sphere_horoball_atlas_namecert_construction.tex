\chapter{A Concrete Naming Certificate for $\HyperbolicVisualSphereHoroballAtlasUp$}
\label{ch:concrete-instances-hyperbolic-visual-sphere-horoball-atlas-namecert}
\origin{ai}

The $\HyperbolicVisualSphereHoroballAtlasUp$ packet realizes the boundary-shadow and Busemann-level branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite visual-sphere, horoball-level, visual-radius, Real-seal, transport, replay, provenance, and local naming rows. It sits after the visual-boundary route of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the Busemann-boundary route of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, the horosphere route of \autoref{ch:concrete-instances-hyperbolic-horosphere-namecert}, the visual-metric route of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, and the observer-expansion route of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}. The packet names a finite atlas of boundary-visible horoballs; it is not a quotient ideal boundary, selected geodesic ray, host hyperbolic space, completed visual sphere, or physical observer trajectory.

\begin{definition}[Hyperbolic visual-sphere horoball atlas carrier]
\label{def:hyperbolic-visual-sphere-horoball-atlas-carrier}
A $\HyperbolicVisualSphereHoroballAtlasUp$ carrier is a finite $\BHist$ packet
$$
A=(V,B,O,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the visual-boundary shell row, recording the finite $\HyperbolicVisualBoundaryUp$ face and the boundary-shadow window inspected by the atlas.
\item $B$ is the Busemann and horoball-level row, recording the $\HyperbolicBusemannBoundaryUp$ level, the visible $\HyperbolicHorosphereUp$ slice, and the horoball threshold attached to the same boundary face.
\item $O$ is the observer-expansion row, recording the finite observer-facing route that consumes the visual-boundary and Busemann-level data.
\item $R$ is the visual-radius comparison row, read through $\HyperbolicVisualMetricUp$ and recording the dyadic or Real-facing radius budget for the displayed boundary shell.
\item $E$ is the terminal $\RealUp$ seal row, available only after the visual-boundary, Busemann-level, horoball, observer-expansion, and visual-radius rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for the visual shell, Busemann level, horoball threshold, observer-expansion route, visual-radius comparison, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from boundary-shadow admission through Busemann level, horoball threshold, visual-radius comparison, observer-expansion read, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicVisualSphereHoroballAtlasUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicHorosphereUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicObserverExpansionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicVisualSphereHoroballAtlasUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $V,B,O,R,E,H,C,P,N$. It has no coordinate for a quotient ideal boundary, selected geodesic ray, host hyperbolic model, completed visual sphere, physical observer trajectory, or unlogged boundary metric.
\end{definition}

\begin{theorem}[Hyperbolic visual-sphere horoball atlas NameCert obligations]
\label{thm:hyperbolic-visual-sphere-horoball-atlas-namecert-obligations}
For every accepted $\HyperbolicVisualSphereHoroballAtlasUp$ carrier
$$
A=(V,B,O,R,E,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicVisualSphereHoroballAtlasUp}$ surface consists exactly of visual-boundary shell exposure through $V$, Busemann and horoball-level exposure through $B$, observer-expansion exposure through $O$, visual-radius comparison through $R$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-visual-sphere-horoball-atlas-carrier}. The boundary-facing rows are exactly the visual shell $V$, the Busemann and horoball-level row $B$, the observer-expansion row $O$, and the visual-radius row $R$.

Read the atlas route in dependency order. The visual-boundary shell fixes the displayed boundary face; the Busemann row then supplies the horosphere slice and horoball threshold attached to that same face.

The visual-radius comparison $R$ is downstream of the boundary shell and Busemann level. The Real seal $E$ can therefore be consumed only after the boundary-shadow, horoball-level, observer-expansion, and radius rows are visible.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since quotient ideal boundaries, selected geodesic rays, host hyperbolic models, completed visual spheres, physical observer trajectories, and unlogged boundary metrics are absent from the carrier, they cannot become local NameCert obligations.
\end{proof}

\falsifiablePrediction{A boundary-shadow consumer that exports a horoball atlas without visual-radius, Busemann-level, Real-seal, transport, provenance, and NameCert rows refutes the packet.}

\independenceWitness{hyperbolic-visual-boundary, hyperbolic-busemann-boundary, hyperbolic-horosphere, hyperbolic-visual-metric, hyperbolic-observer-expansion: none is bijective to $\HyperbolicVisualSphereHoroballAtlasUp$ because this packet binds boundary shell, Busemann level, horoball threshold, visual-radius comparison, observer-expansion read, Real seal, transport, replay, provenance, and local naming as one atlas route.}

The Lean TasteGate route for this packet must provide explicit \texttt{Nontrivial HyperbolicVisualSphereHoroballAtlasUp} and \texttt{FieldFaithful HyperbolicVisualSphereHoroballAtlasUp} instances in \texttt{lean4/BEDC/Derived/HyperbolicVisualSphereHoroballAtlasUp/TasteGate.lean}. The nontrivial instance must separate an accepted atlas from a packet missing $V$, $B$, or $R$, and the field-faithful instance must preserve $V,B,O,R,E,H,C,P,N$ as distinct rows before any checked carrier-alignment theorem can serve as a public TasteGate witness.

\closureat{HyperbolicVisualSphereHoroballAtlasUp}{seedStr}

\begin{closurestatus}{\HyperbolicVisualSphereHoroballAtlasUp}
  \constructivestory{A $\HyperbolicVisualSphereHoroballAtlasUp$ packet is generated from a visual-boundary shell, Busemann and horoball-level rows, an observer-expansion row, a visual-radius comparison, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-visual-sphere-horoball-atlas-carrier} and \autoref{thm:hyperbolic-visual-sphere-horoball-atlas-namecert-obligations} seed the finite boundary-shadow atlas route over visual-boundary, Busemann, horosphere, visual-metric, observer-expansion, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient ideal boundaries, selected geodesic rays, host hyperbolic models, completed visual spheres, physical observer trajectories, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, visual-boundary shell exposure, Busemann and horoball-level exactness, observer-expansion routing, visual-radius comparison, Real-seal nonescape, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicVisualSphereHoroballAtlasUp.}
\end{closurestatus}
