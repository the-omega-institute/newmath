\chapter{A Concrete Naming Certificate for $\SieveCompleteSpaceUp$}
\label{ch:concrete-instances-sieve-complete-space-namecert}
\origin{human}

Following Kelley and Oxtoby, the $\SieveCompleteSpaceUp$ packet records the sieve-completeness surface of a topological space as finite NameCert data. It sits beside the Cech-completion surface of \autoref{ch:concrete-instances-cech-complete-space-namecert}, the dense-open Baire schedule of \autoref{ch:concrete-instances-bairecategory-namecert}, the theorem-level Baire route of \autoref{ch:concrete-instances-baire-category-theorem-namecert}, the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the displayed sieve strategy, cover-refinement, and completion-facing rows; it does not assert a host choice strategy, selected generic point, quotient completion, arbitrary open-cover compactness theorem, or ambient $\RealUp$ completeness.

\begin{definition}[Sieve complete space carrier]
\label{def:sieve-complete-space-carrier}
A $\SieveCompleteSpaceUp$ carrier is a finite $\BHist$ packet
$$
S=(T,V,C,B,M,W,R,E,H,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the ambient $\TopologyUp$ row, carrying the point-neighbourhood and open-refinement classifier used by the sieve.
\item $V$ is the sieve row, recording finite cover levels, successor refinement requests, legal response rows, and the refusal boundary for non-threaded branches.
\item $C$ is the $\CechCompleteSpaceUp$ comparison row, used only as a topological completion-facing sibling surface.
\item $B$ is the $\BaireCategoryUp$ dense-open schedule row and its theorem-facing handoff through $\BaireCategoryTheoremUp$.
\item $M$ is the $\CompleteMetricUp$ service row, present only when a complete-metric comparison supplies the inspected Cauchy route.
\item $W$ is the $\StreamNameUp$ finite-window row for sieve levels, refinement stages, centres, and radii.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular-rational bounds that control the displayed windows.
\item $E$ is the terminal $\RealUp$ seal reached after $W$ and $R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the topology, sieve, Cech-complete, Baire, complete-metric, stream, regular-rational, real-seal, replay, provenance, and local naming rows.
\item $Q$ records displayed $\Cont$ replay from a sieve request through $T,V,W,R,E$ and only then through the sibling completion or category rows $C,B,M$.
\item $P$ is the $\Pkg$ provenance over $\SieveCompleteSpaceUp$, $\TopologyUp$, $\CechCompleteSpaceUp$, $\BaireCategoryUp$, $\BaireCategoryTheoremUp$, $\CompleteMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SieveCompleteSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these twelve rows. It has no coordinate for a host strategy, selected generic point, quotient completion, arbitrary open-cover compactness theorem, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Sieve complete space NameCert obligations]
\label{thm:sieve-complete-space-namecert-obligations}
For every accepted $\SieveCompleteSpaceUp$ carrier
$$
S=(T,V,C,B,M,W,R,E,H,Q,P,N),
$$
the local $\NameCert_{\SieveCompleteSpaceUp}$ surface consists exactly of carrier admission, topology-source admission, sieve-level exposure, Cech-complete comparison, Baire schedule and theorem-route exposure, optional complete-metric service, StreamName finite-window visibility, RegSeqRat readback, Real sealing, componentwise transport, displayed replay, provenance, and local naming for the displayed rows $T,V,C,B,M,W,R,E,H,Q,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:sieve-complete-space-carrier}. The projection exposes the topology row $T$, the sieve row $V$, the completion and category comparison rows $C,B,M$, the finite-window and readback rows $W,R,E$, and the structural rows $H,Q,P,N$.

The local sieve obligations start from $T$ and $V$. A consumer must read the open-refinement classifier, finite cover levels, successor requests, legal response rows, and non-threaded refusal boundary before any completion-facing or category-facing sibling is inspected.

The sibling rows do not define a hidden completion theorem. Row $C$ supplies the Cech-complete comparison, row $B$ supplies the Baire dense-open and theorem handoff, and row $M$ supplies the complete-metric service only when that comparison is displayed.

Finally the finite windows $W$, regular-rational readback $R$, real seal $E$, transport $H$, replay $Q$, provenance $P$, and local naming $N$ preserve the same route. Since no other coordinate exists, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Sieve complete space Baire completion route]
\label{thm:sieve-complete-space-baire-completion-route}
Every Baire-facing or completion-facing consumer of an accepted $\SieveCompleteSpaceUp$ packet factors through the displayed route
$$
\TopologyUp \longmapsto \SieveCompleteSpaceUp \longmapsto \CechCompleteSpaceUp,\BaireCategoryUp,\BaireCategoryTheoremUp,\CompleteMetricUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
and through the structural rows $\BHist,\hsame,\Cont,\Pkg,\NameCert$. The route exports the finite sieve strategy, cover-refinement window, dense-open handoff, complete-metric comparison, and real-seal readback, not a host choice strategy, selected generic point, quotient completion, arbitrary open-cover compactness theorem, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier projection. The topology row $T$ and sieve row $V$ are the only entry point for a public sieve-completeness read, so a consumer cannot begin from a host strategy or detached open-cover family.

Next read the completion and category siblings. The row $C$ routes the topological completion comparison through \autoref{ch:concrete-instances-cech-complete-space-namecert}, the row $B$ routes dense-open evidence through \autoref{ch:concrete-instances-bairecategory-namecert} and \autoref{ch:concrete-instances-baire-category-theorem-namecert}, and the row $M$ routes complete-metric service through \autoref{ch:concrete-instances-completemetric-namecert}.

Then consume the finite analytic readback. The stream-window row $W$ exposes only finite sieve levels and refinement stages, the regular-rational row $R$ records the displayed bounds, and the terminal real row $E$ seals the route after those finite rows are visible.

Finally use $H,Q,P,N$ to transport, replay, source, and name the same packet. Coordinate exhaustion blocks a selected generic point, quotient completion, arbitrary open-cover compactness theorem, countable-choice strategy, or ambient $\RealUp$ completeness outside the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\SieveCompleteSpaceUp$ consumer that obtains a Baire-facing or completion-facing read without first displaying topology, sieve, Cech-complete, Baire-category, complete-metric, StreamName, RegSeqRat, Real, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows refutes the carrier surface.}

\independenceWitness{cech-complete-space, bairecategory, baire-category-theorem, completemetric, streamname, regseqrat, real: no listed sibling is bijective to $\SieveCompleteSpaceUp$ because the sieve packet binds topology, cover-level strategy, completion comparison, category handoff, finite windows, regular readback, terminal Real sealing, transport, replay, provenance, and local naming in one carrier.}

\closureat{SieveCompleteSpaceUp}{seedStr}

\begin{closurestatus}{\SieveCompleteSpaceUp}
  \constructivestory{A $\SieveCompleteSpaceUp$ packet is generated from a TopologyUp source, a finite sieve strategy row, a CechCompleteSpaceUp comparison, BaireCategoryUp and BaireCategoryTheoremUp category rows, an optional CompleteMetricUp service row, StreamName finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sieve-complete-space-carrier}, \autoref{thm:sieve-complete-space-namecert-obligations}, and \autoref{thm:sieve-complete-space-baire-completion-route} seed the finite sieve-completeness carrier, NameCert obligation surface, and Baire-completion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host choice strategy, selected generic point, quotient completion, arbitrary open-cover compactness theorem, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sieve-level exactness, completion and category sibling exposure, finite-window readback, real sealing, transport, replay, provenance, and local naming for BEDC.Derived.SieveCompleteSpaceUp.}
\end{closurestatus}
