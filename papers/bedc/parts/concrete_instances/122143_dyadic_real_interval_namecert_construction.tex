\chapter{A Concrete Naming Certificate for $\DyadicRealIntervalUp$}
\label{ch:concrete-instances-dyadic-real-interval-namecert}
\origin{human}

Following Bishop and Bridges, the $\DyadicRealIntervalUp$ packet records the finite endpoint envelope used when dyadic rational bounds are handed to Real and Completion consumers. It sits after the dyadic rational core of \autoref{ch:concrete-instances-dyadic-namecert}, the finite stream naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the closed bounded interval net route of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert}. The packet is an interval-envelope certificate for displayed dyadic endpoints; it is not a quotient real interval, a selected completion point, a compactness theorem, or a proof of ambient $\RealUp$ completeness.

\begin{definition}[DyadicRealInterval carrier]
\label{def:dyadic-real-interval-carrier}
A $\DyadicRealIntervalUp$ carrier is a finite $\BHist$ packet
$$
I=(L,R,O,S,Q,E,N,H,C,P,K)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the left $\DyadicRatCoreUp$ endpoint row.
\item $R$ is the right $\DyadicRatCoreUp$ endpoint row.
\item $O$ is the endpoint-order evidence row, recording that the displayed left endpoint is admitted before the displayed right endpoint for this interval envelope.
\item $S$ is the $\StreamNameUp$ finite-window row used to inspect endpoint approximations and interval refinements.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed stream windows and dyadic tolerance data.
\item $E$ is the $\RealUp$ seal row through which endpoint and interval-envelope reads become Real-facing.
\item $N$ is the $\ClosedBoundedIntervalNetUp$ row for finite closed bounded interval-net consumers.
\item $H$ records componentwise $\hsame$ transport for dyadic endpoints, endpoint order, stream windows, regular rational readback, Real sealing, interval-net handoff, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from an interval-envelope query through $L,R,O,S,Q,E,N,H,P,K$.
\item $P$ is the $\Pkg$ provenance over $\DyadicRealIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ClosedBoundedIntervalNetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $K$ is the local $\NameCert_{\DyadicRealIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,R,O,S,Q,E,N,H,C,P,K$. It has no coordinate for quotient real equality, selected completion points, arbitrary located compactness, host interval topology, or ambient Real completeness.
\end{definition}

\begin{theorem}[DyadicRealInterval Real seal obligations]
\label{thm:dyadic-real-interval-real-seal-obligations}
For every accepted $\DyadicRealIntervalUp$ carrier
$$
I=(L,R,O,S,Q,E,N,H,C,P,K),
$$
the local $\NameCert_{\DyadicRealIntervalUp}$ surface consists exactly of left and right dyadic endpoint exposure through $L,R$, endpoint-order evidence through $O$, finite StreamName windows through $S$, RegSeqRat readback through $Q$, Real sealing through $E$, closed bounded interval-net handoff through $N$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $K$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-real-interval-carrier}. Rows $L,R,O$ are the endpoint envelope, rows $S,Q,E,N$ are the completion-facing readback and interval-net surface, and rows $H,C,P,K$ are the structural certificate data.

The endpoint part is read first. The two dyadic endpoint rows remain separate, and $O$ records the displayed order evidence before any Real-facing seal can be consumed.

The completion-facing part is then read through $S,Q,E,N$. Stream windows precede regular rational readback, the Real seal is downstream of that readback, and the interval-net row is a finite closed bounded consumer row rather than an ambient compact interval.

Finally $H,C,P,K$ transport, replay, source, and name the same finite packet. Since no coordinate stores quotient real equality, selected completion points, host interval topology, or ambient Real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[DyadicRealInterval completion consumer route]
\label{thm:dyadic-real-interval-completion-consumer-route}
Every Real or Completion consumer of an accepted $\DyadicRealIntervalUp$ carrier must read the finite route
$$
\DyadicRatCoreUp \times \DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp \to \ClosedBoundedIntervalNetUp .
$$
The consumer may inspect dyadic endpoint rows, endpoint-order evidence, finite stream windows, regular rational readback, Real sealing, closed bounded interval-net handoff, transport, replay, provenance, and local naming. It receives no quotient real interval, selected completion point, host interval topology, arbitrary compactness theorem, or ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:dyadic-real-interval-real-seal-obligations}. They force every accepted consumer read to expose $L,R,O,S,Q,E,N,H,C,P,K$.

The endpoint segment is $L,R,O$. Both endpoint rows are dyadic rational core rows, and the order evidence is displayed as part of the same finite packet rather than inferred from an external real-order theorem.

The readback segment is $S,Q,E$. A completion-facing consumer reaches the Real seal only after finite StreamName windows and RegSeqRat readback have been displayed.

The interval-net segment is $N$, transported and replayed by $H,C$ and sourced and named by $P,K$. Thus the consumer receives a finite interval-envelope route into closed bounded interval-net data, with no hidden quotient, selected point, compactness, topology, or ambient-completeness coordinate.
\end{proof}

\falsifiablePrediction{Every accepted $\DyadicRealIntervalUp$ consumer read displays two $\DyadicRatCoreUp$ endpoint rows, endpoint-order evidence, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a $\RealUp$ seal, a $\ClosedBoundedIntervalNetUp$ handoff, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming before the interval envelope can be consumed.}

\independenceWitness{dyadic-rat-core, streamname, regseqrat, real, closed-bounded-interval-net: $\DyadicRealIntervalUp$ is not bijective to any listed sibling because it jointly binds two ordered dyadic endpoints, finite stream windows, regular rational readback, terminal Real sealing, closed bounded interval-net handoff, transport, replay, provenance, and local naming in one packet.}

\closureat{DyadicRealIntervalUp}{seedStr}

\begin{closurestatus}{\DyadicRealIntervalUp}
  \constructivestory{A $\DyadicRealIntervalUp$ packet is generated from left and right DyadicRatCore endpoint rows, endpoint-order evidence, StreamName finite windows, RegSeqRat readback, a RealUp seal, a ClosedBoundedIntervalNet handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-real-interval-carrier}, \autoref{thm:dyadic-real-interval-real-seal-obligations}, and \autoref{thm:dyadic-real-interval-completion-consumer-route} seed the finite dyadic endpoint interval-envelope route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient real intervals, selected completion points, arbitrary located compactness, host interval topology, ambient RealUp completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic endpoint exposure, endpoint-order evidence, StreamName window visibility, RegSeqRat readback, Real-seal nonescape, interval-net handoff, transport, replay, provenance, and local naming for BEDC.Derived.DyadicRealIntervalUp.}
\end{closurestatus}
