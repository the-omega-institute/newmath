\chapter{A Concrete Naming Certificate for $\CanonicalTailChoiceUp$}
\label{ch:concrete-instances-canonical-tail-choice-namecert}
\origin{ai}

The $\CanonicalTailChoiceUp$ packet realizes the finite tail-choice route needed by the Real-completeness limit theorem of \autoref{ch:visions-real-completeness}. It sits after the no-choice boundary of \autoref{ch:concrete-instances-cauchy-choice-boundary-namecert}, the shared tail schedule of \autoref{ch:concrete-instances-tail-cofinality-schedule-namecert}, the bounded prefix budget of \autoref{ch:concrete-instances-finite-prefix-limit-budget-namecert}, and the exact completion boundary of \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}. Its role is to expose the canonical finite index and tail-window witness used before the diagonal seal is read, without importing a host-selected subsequence, quotient stream equality, countable choice row, or ambient completeness principle.

\falsifiablePrediction{A Real-completion consumer that omits the canonical finite tail-choice row cannot claim the diagonal seal handoff.}

\independenceWitness{cauchychoiceboundary, tailcofinalityschedule, finiteprefixlimitbudget, realcompletionexactboundary, limitselector: none is bijective to $\CanonicalTailChoiceUp$ because this packet simultaneously carries the modulus row, tolerance-budget row, canonical finite index row, tail-window witness row, Real seal route, no-choice refusal row, componentwise transport, continuation replay, provenance, and local naming surface; the named siblings isolate boundary refusal, cofinal scheduling, prefix-budget exhaustion, exact boundary faces, or selector admission without this joint carrier.}

\begin{definition}[Canonical tail-choice carrier]
\label{def:canonical-tail-choice-carrier}
A $\CanonicalTailChoiceUp$ carrier is a finite $\BHist$ packet
$$
C=(M,E,I,T,S,R,H,C_0,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the modulus row read from the accepted regular-Cauchy route;
\item $E$ is the tolerance-budget row fixing the finite precision scale served by the packet;
\item $I$ is the canonical finite index row computed from $M$ and $E$, not from an external choice schedule;
\item $T$ is the tail-window witness row certifying that every downstream observation is read inside the finite tail determined by $I$;
\item $S$ is the $\RealUp$ seal route reached only after $M,E,I,T$ have been displayed;
\item $R$ is the no-choice refusal row excluding host-selected limits, quotient stream equality, countable choice, hidden tail oracles, and ambient completeness;
\item $H$ records componentwise $\hsame$ transport for the displayed analytic rows and the structural rows;
\item $C_0$ records the displayed $\Cont$ replay from modulus and budget admission to canonical index, tail-window witness, Real seal, refusal, and local naming;
\item $P$ is the $\Pkg$ provenance over the sibling certificates, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CanonicalTailChoiceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for a host-selected subsequence, quotient stream equality, countable choice, metric completeness, ambient completeness, or a detached Real-completion seal.
\end{definition}
\leandef{BEDC.Derived.CanonicalTailChoiceUp.CanonicalTailChoiceUp}

\begin{theorem}[Canonical tail-choice NameCert obligations]
\label{thm:canonical-tail-choice-namecert-obligations}
For every accepted $\CanonicalTailChoiceUp$ carrier
$$
C=(M,E,I,T,S,R,H,C_0,P,N),
$$
the local $\NameCert_{\CanonicalTailChoiceUp}$ surface consists exactly of carrier exposure for $C$, modulus exposure through $M$, tolerance-budget exposure through $E$, canonical finite-index exposure through $I$, tail-window witness exposure through $T$, $\RealUp$ seal-route exposure through $S$, no-choice refusal through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C_0$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.CanonicalTailChoiceUp.CanonicalTailChoiceCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:canonical-tail-choice-carrier}. Its displayed rows list the modulus, tolerance budget, canonical finite index, tail-window witness, seal route, refusal ledger, and structural support.

Read the rows in order. The modulus row $M$ and tolerance-budget row $E$ determine the finite index row $I$ before the tail-window witness $T$ can be consumed. The Real seal route $S$ is therefore downstream from a displayed finite choice, not from a hidden selector.

Finally inspect the structural rows. The rows $H,C_0,P,N$ transport, replay, record provenance for, and name the same finite packet. Since no further coordinate occurs in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Canonical tail-choice source obligations]
\label{thm:canonical-tail-choice-source-obligations}
Every accepted $\CanonicalTailChoiceUp$ read exposes source rows over $\CauchyChoiceBoundaryUp$, $\FinitePrefixLimitBudgetUp$, $\RealUp$, $\RealCompletionExactBoundaryUp$, $\RegSeqRatUp$, and $\TailCofinalityScheduleUp$ before the canonical finite index row $I$ or the tail-window witness $T$ can be consumed. These source rows are finite $\BHist$ entries in the same packet, not references to a selected subsequence, quotient Cauchy name, countable choice row, or ambient completion principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:canonical-tail-choice-carrier}. The modulus row $M$ and tolerance-budget row $E$ are read before the canonical finite index row $I$, so the finite-prefix budget and regular-rational source are already visible when the tail window is formed.

Read the sibling boundary rows next. The no-choice boundary of \autoref{ch:concrete-instances-cauchy-choice-boundary-namecert}, the exact completion boundary of \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}, and the cofinal schedule of \autoref{ch:concrete-instances-tail-cofinality-schedule-namecert} attach to the displayed route before the Real seal route $S$ is admitted.

The refusal row $R$ then excludes the non-source objects. Since $H,C_0,P,N$ only transport, replay, source, and name the same finite packet, no selected limit, quotient Cauchy name, countable choice row, or ambient $\RealUp$ completeness row is exported by the accepted read.
\end{proof}

\begin{theorem}[Canonical tail-choice window stability]
\label{thm:canonical-tail-choice-window-stability}
Let $C=(M,E,I,T,S,R,H,C_0,P,N)$ and $C'=(M',E',I',T',S',R',H',C'_0,P',N')$ be accepted $\CanonicalTailChoiceUp$ carriers. If the displayed rows are transported componentwise by the $\hsame$ data recorded in $H$ and $H'$, then the finite tail-window route $M,E,I,T$ and $M',E',I',T'$ remain on the same canonical tail-choice surface before either packet reaches its $\RealUp$ seal route.
\end{theorem}
\begin{proof}
Induct over the finite route $M,E,I,T$. The base rows $M,E$ expose the same modulus and tolerance-budget roles under componentwise $\hsame$ transport, so both packets compute their canonical index inside the displayed source surface.

At the index step, the rows $I$ and $I'$ are reached only after those transported source rows. The tail rows $T$ and $T'$ are therefore compared as finite window witnesses attached to the transported index rows, not as host-selected subsequences.

The remaining rows preserve the comparison. The seal rows $S,S'$ are downstream from the tail windows, the refusal rows $R,R'$ exclude quotient names and choice schedules, and $C_0,C'_0,P,P',N,N'$ replay, source, and name the same componentwise transport. Thus finite tail-window stability is exactly the displayed $\hsame$ transport of the packet rows.
\end{proof}
\leanchecked{BEDC.Derived.CanonicalTailChoiceUp.CanonicalTailChoiceCarrier\_window\_stability}

\begin{theorem}[Canonical tail-choice nonescape]
\label{thm:canonical-tail-choice-nonescape}
No selected limit, quotient Cauchy name, countable choice row, or ambient $\RealUp$ completeness principle is exported by an accepted $\CanonicalTailChoiceUp$ packet. Any consumer that claims one of these objects has left the displayed rows $M,E,I,T,S,R,H,C_0,P,N$ and therefore cannot be reading the local $\NameCert_{\CanonicalTailChoiceUp}$ surface.
\end{theorem}
\begin{proof}
Inspect the carrier rows. The tuple exposes the modulus, tolerance budget, canonical index, tail window, Real seal route, refusal ledger, transport, replay, provenance, and local name; it contains no coordinate for a selected limit, quotient Cauchy name, countable choice row, or ambient completeness principle.

Apply the source-obligation theorem above. Every accepted read reaches $I$ and $T$ only after the finite source rows over the no-choice boundary, prefix budget, regular-rational source, cofinal schedule, and exact Real-completion boundary have been displayed.

Finally use the refusal coordinate $R$. It blocks replacing the finite rows by the excluded objects, while $H,C_0,P,N$ keep all downstream consumption inside the same packet. Hence any consumer that exports an excluded object is outside the accepted $\CanonicalTailChoiceUp$ surface.
\end{proof}

\begin{theorem}[Canonical tail-choice obligation surface]
\label{thm:canonical-tail-choice-obligation-surface}
For every accepted $\CanonicalTailChoiceUp$ carrier
$$
C=(M,E,I,T,S,R,H,C_0,P,N),
$$
the finite tail-choice obligation surface is the simultaneous exposure of the modulus row $M$, tolerance-budget row $E$, canonical finite index row $I$, tail-window witness $T$, $\RealUp$ seal route $S$, no-choice refusal $R$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C_0$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. The surface admits no host-selected subsequence, quotient stream equality, countable-choice schedule, hidden tail oracle, detached completion seal, or ambient completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:canonical-tail-choice-carrier}. The ten displayed rows are exactly $M,E,I,T,S,R,H,C_0,P,N$, so an accepted packet exposes the modulus, tolerance budget, canonical finite index, tail window, seal route, refusal ledger, transport, replay, provenance, and local naming as one finite surface.

Use the schedule factorization of \autoref{thm:canonical-tail-choice-schedule-factorization}. It forces every Real-completion consumer to pass through $M$ and $E$ before reaching $I$, then through $T$ before the seal route $S$ is consumed. Thus the canonical finite index and tail-window witness are displayed route coordinates, not a host-selected subsequence.

Finally apply the no-choice non-escape theorem, \autoref{thm:canonical-tail-choice-no-choice-nonescape}. The refusal row $R$ and the structural rows $H,C_0,P,N$ keep the same finite packet visible and exclude quotient stream equality, countable choice, hidden tail oracles, detached completion seals, and ambient completeness. Therefore the listed rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[Canonical tail-choice schedule factorization]
\label{thm:canonical-tail-choice-schedule-factorization}
Every accepted Real-completion consumer of a $\CanonicalTailChoiceUp$ packet factors through the finite route
$$
M,E,I,T,S,R,H,C_0,P,N.
$$
When the consumer also reads the sibling surfaces of \autoref{ch:concrete-instances-tail-cofinality-schedule-namecert} and \autoref{ch:concrete-instances-finite-prefix-limit-budget-namecert}, their schedule and prefix-budget rows attach before the tail-window witness $T$ and before the Real seal route $S$.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:canonical-tail-choice-namecert-obligations}. The canonical finite index $I$ is exposed only after $M$ and $E$, so any downstream schedule must already have the modulus and tolerance budget in scope.

Attach the cofinal schedule. The $\TailCofinalityScheduleUp$ packet supplies its finite window and dyadic threshold rows as an accepted sibling route, and those rows can be consumed only through the same displayed finite schedule surface.

Attach the prefix budget next. The $\FinitePrefixLimitBudgetUp$ packet records which prefix windows have been spent before a seal-facing read is admitted. Thus the local tail-window witness $T$ is reached only after the sibling schedule and prefix-budget rows have been displayed.

The remaining rows $S,R,H,C_0,P,N$ keep the seal route and refusal ledger inside that same finite packet, so every accepted consumer factors through the stated route.
\end{proof}

\begin{theorem}[Canonical tail-choice no-choice non-escape]
\label{thm:canonical-tail-choice-no-choice-nonescape}
No host-selected limit, quotient stream equality, countable choice row, hidden tail oracle, metric completeness row, ambient completeness row, or detached Real-completion seal can be read from an accepted $\CanonicalTailChoiceUp$ packet. The packet exports only the rows $M,E,I,T,S,R,H,C_0,P,N$ of \autoref{def:canonical-tail-choice-carrier}.
\end{theorem}
\begin{proof}
Inspect the accepted carrier. The refusal row $R$ is a displayed coordinate, while none of the excluded objects is a coordinate of the tuple.

Apply \autoref{thm:canonical-tail-choice-schedule-factorization}. Every accepted consumer reaches the Real seal route only through $M,E,I,T,S,R,H,C_0,P,N$, with sibling schedule and prefix-budget rows consumed before the tail-window witness.

The finite index row $I$ is not a host-selected limit or a countable-choice schedule; it is the canonical row displayed after $M$ and $E$. The witness row $T$ and seal row $S$ are likewise route coordinates, not quotient equalities or completeness principles. Hence any read of an excluded object leaves the accepted packet.
\end{proof}

\begin{theorem}[Canonical tail-choice Real-completion handoff]
\label{thm:canonical-tail-choice-real-completion-handoff}
The Real-completion diagonal seal handoff may consume an accepted $\CanonicalTailChoiceUp$ packet only through the displayed route
$$
M,E,I,T,S,R,H,C_0,P,N.
$$
Under \autoref{ch:concrete-instances-cauchy-choice-boundary-namecert} and \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}, this route supplies the finite tail-choice row between no-choice refusal and the terminal exact-boundary seal, and supplies no host-side equality or ambient completeness data.
\end{theorem}
\begin{proof}
Begin with the no-choice boundary. The sibling $\CauchyChoiceBoundaryUp$ surface already requires a displayed finite index, tail witness, seal route, and refusal row before a diagonal consumer may claim the no-choice seal path.

Next apply the local obligations and schedule factorization. The $\CanonicalTailChoiceUp$ packet identifies the canonical finite index $I$ and tail-window witness $T$ as first-class rows between the modulus-budget pair $M,E$ and the Real seal route $S$.

Finally read the exact boundary. The $\RealCompletionExactBoundaryUp$ packet admits terminal completion reads only through its displayed StreamName, RegSeqRat, DyadicRatCore, and RealUp faces. Since the present packet exports only $M,E,I,T,S,R,H,C_0,P,N$ and the sibling boundary exports no hidden selector, the diagonal seal handoff remains a finite no-choice route.
\end{proof}

\begin{theorem}[Canonical tail-choice tail cofinality]
\label{thm:canonical-tail-choice-tail-cofinality}
Let $C=(M,E,I,T,S,R,H,C_0,P,N)$ be an accepted $\CanonicalTailChoiceUp$ carrier. The canonical finite index row $I$ remains cofinal with the displayed $\TailCofinalityScheduleUp$ and $\FinitePrefixLimitBudgetUp$ rows before any tail-window consumer reads the $\RealUp$ seal route $S$. Thus every later finite tolerance request transported through $H$ and replayed through $C_0$ is served inside the same tail-window row $T$, without a host-selected subsequence, quotient stream equality, hidden tail oracle, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with schedule factorization, \autoref{thm:canonical-tail-choice-schedule-factorization}. It places the cofinal schedule and prefix-budget rows before the tail-window witness $T$ and before the Real seal route $S$.

Read the carrier rows $M,E,I,T,S,R,H,C_0,P,N$. The index row $I$ is displayed after the modulus and budget rows, so a later tolerance request can be transported only by reusing the same finite route through $H$ and replaying it through $C_0$.

The refusal row $R$ blocks replacing that finite route by a selected subsequence, quotient equality, hidden tail oracle, choice row, or ambient completeness principle. Hence the cofinality assertion is internal to the displayed packet before any consumer reaches the seal.
\end{proof}

\begin{theorem}[Canonical tail-choice Real-completion boundary]
\label{thm:canonical-tail-choice-real-completion-boundary}
The Real-completion exact-boundary handoff for an accepted $\CanonicalTailChoiceUp$ carrier consumes only the rows
$$
M,E,I,T,S,R,H,C_0,P,N.
$$
It cannot read quotient stream equality, countable choice, hidden tail oracles, ambient completeness, or any detached completion seal outside the displayed carrier and its $\NameCert_{\CanonicalTailChoiceUp}$ surface.
\end{theorem}
\begin{proof}
Apply the Real-completion handoff theorem \autoref{thm:canonical-tail-choice-real-completion-handoff}. The diagonal seal route is reached only through the displayed packet rows $M,E,I,T,S,R,H,C_0,P,N$.

Use the no-choice non-escape theorem \autoref{thm:canonical-tail-choice-no-choice-nonescape}. It identifies quotient stream equality, countable choice, hidden tail oracles, ambient completeness, and detached seals as absent from the carrier.

The structural rows $H,C_0,P,N$ transport, replay, source, and name the same finite packet. They do not add a new completion coordinate, so the exact-boundary handoff remains confined to the listed rows.
\end{proof}

\begin{theorem}[Canonical tail-choice tail inhabitation]
\label{thm:canonical-tail-choice-tail-inhabitation}
Let $C=(M,E,I,T,S,R,H,C_0,P,N)$ be an accepted $\CanonicalTailChoiceUp$ carrier. For every finite Real-completion request admitted by the displayed rows $M,E,I,T,S,R,H,C_0,P,N$, the tail-window row $T$ is inhabited by a finite $\BHist$ witness after the canonical index row $I$ is read. The witness is produced inside the same packet and is not a host-selected subsequence, hidden tail oracle, quotient stream equality, or countable-choice row.
\end{theorem}
\begin{proof}
Induct over the displayed route $M,E,I,T,S,R,H,C_0,P,N$. The base rows $M,E$ expose the regular-Cauchy modulus and tolerance budget, so the request has a finite precision scale before any tail is inspected.

At the index step, use the carrier obligation of \autoref{thm:canonical-tail-choice-namecert-obligations}. The canonical index row $I$ is a displayed coordinate computed from $M,E$, and the next row $T$ is the packet's own tail-window witness row.

The remaining rows preserve that witness. The seal row $S$ consumes the tail only after $T$ is present; the refusal row $R$ excludes hidden selectors; and $H,C_0,P,N$ transport, replay, source, and name the same finite packet. Hence tail inhabitation is a finite row induction, not an external choice principle.
\end{proof}

\begin{theorem}[Canonical tail-choice cofinality stability]
\label{thm:canonical-tail-choice-cofinality-stability}
Let $C=(M,E,I,T,S,R,H,C_0,P,N)$ be an accepted $\CanonicalTailChoiceUp$ carrier. If the sibling cofinal schedule of \autoref{ch:concrete-instances-tail-cofinality-schedule-namecert} is read before $I$, then every later finite tolerance request transported through $H$ remains on the same tail-window route $M,E,I,T,S,R,H,C_0,P,N$. In particular, cofinality is stable under componentwise $\hsame$ transport and displayed $\Cont$ replay, and no host-selected subsequence or quotient equality is exported.
\end{theorem}
\begin{proof}
Start with the schedule factorization of \autoref{thm:canonical-tail-choice-schedule-factorization}. It places the cofinal schedule and prefix-budget rows before $T$ and before the Real seal route $S$.

Now perform induction over later finite tolerance requests. The base request is the one served by $E$ and $I$, and the induction step appends one displayed request by transporting the same modulus, budget, index, and tail rows through $H$.

The replay row $C_0$ records that this appended request follows the same route, while $R$ refuses any move to a selected subsequence, quotient stream equality, hidden tail oracle, or ambient completeness row. Therefore cofinality stability is exactly stability of the displayed finite route.
\end{proof}

\begin{theorem}[Canonical tail-choice RegSeqRat consumption]
\label{thm:canonical-tail-choice-regseqrat-consumption}
Every $\RegSeqRatUp$ consumer that reads an accepted $\CanonicalTailChoiceUp$ packet for a Real-completion handoff must consume the rows
$$
M,E,I,T,S,R,H,C_0,P,N
$$
in that order before the terminal $\RealUp$ seal is treated as available. The consumer receives a finite regular-rational tail route through $M,E,I,T$ and the structural rows, but no quotient stream equality, selected limit, countable choice, hidden tail oracle, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Begin with the Real-completion handoff theorem \autoref{thm:canonical-tail-choice-real-completion-handoff}. It makes the diagonal seal read pass through the displayed route $M,E,I,T,S,R,H,C_0,P,N$.

For the regular-rational read, use the exact-boundary sibling cited there. A $\RegSeqRatUp$ consumer can inspect a regular-rational tail only after the modulus row $M$, tolerance budget $E$, canonical index $I$, and tail-window witness $T$ have been displayed.

Finally use no-choice non-escape, \autoref{thm:canonical-tail-choice-no-choice-nonescape}. The refusal row and structural rows keep the read inside the finite packet, so consuming the regular-rational tail cannot produce quotient equality, a selected limit, countable choice, a hidden tail oracle, or ambient Real completeness.
\end{proof}

\begin{theorem}[Canonical tail-choice scoped finite-choice route]
\label{thm:canonical-tail-choice-scoped-finite-choice-route}
Let $C=(M,E,I,T,S,R,H,C_0,P,N)$ be an accepted
$\CanonicalTailChoiceUp$ packet. Every completion-facing finite choice in $C$
is scoped through $\CauchyChoiceBoundaryUp$, $\TailCofinalityScheduleUp$,
$\FinitePrefixLimitBudgetUp$, $\RegSeqRatUp$, and
$\RealCompletionExactBoundaryUp$ by the displayed route
$$
M,E,I,T,S,R,H,C_0,P,N.
$$
The route computes the finite index row $I$ from the modulus and budget rows,
inhabits the tail window $T$, reads the regular-rational tail through
$\RegSeqRatUp$, and hands the result to the exact completion boundary only
after the refusal and structural rows have been displayed. It exports no
quotient stream equality, hidden tail oracle, selected subsequence, countable
choice, selected limit, or ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Start from the schedule factorization theorem,
\autoref{thm:canonical-tail-choice-schedule-factorization}. It places the
$\TailCofinalityScheduleUp$ and $\FinitePrefixLimitBudgetUp$ rows before the
tail-window witness, so the finite index is read from the displayed packet
rather than from an external choice function.

Next apply no-choice non-escape,
\autoref{thm:canonical-tail-choice-no-choice-nonescape}. The refusal row
blocks quotient stream equality, hidden tail oracles, selected subsequences,
countable choice, selected limits, and ambient completeness before the
completion-facing handoff is available.

The Real-completion handoff,
\autoref{thm:canonical-tail-choice-real-completion-handoff}, moves the packet
from the tail window to the exact boundary through the same rows
$M,E,I,T,S,R,H,C_0,P,N$. Tail cofinality stability and
\autoref{thm:canonical-tail-choice-regseqrat-consumption} then identify the
$\RegSeqRatUp$ consumer as a downstream read of that finite route.

The transport, replay, provenance, and naming rows $H,C_0,P,N$ repackage the
same finite packet for consumers. Thus the only completion-facing choice made
by the chapter is the displayed finite index choice, scoped through the listed
L10 completion route and not through countable choice.
\end{proof}

The Lean follow-up namespace is \texttt{BEDC.Derived.CanonicalTailChoiceUp}. Its \texttt{TasteGate.lean} surface must provide \texttt{Nontrivial} and \texttt{FieldFaithful} instances for the carrier of \autoref{def:canonical-tail-choice-carrier}.

\closureat{CanonicalTailChoiceUp}{obligationStr}

\begin{closurestatus}{\CanonicalTailChoiceUp}
  \constructivestory{A $\CanonicalTailChoiceUp$ packet is generated from a regular-Cauchy modulus row, a finite tolerance-budget row, a canonical finite-index row, a tail-window witness row, a Real seal route, a no-choice refusal row, componentwise transports, continuation replay, provenance, and local naming.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:canonical-tail-choice-carrier}, \autoref{thm:canonical-tail-choice-namecert-obligations}, \autoref{thm:canonical-tail-choice-schedule-factorization}, \autoref{thm:canonical-tail-choice-no-choice-nonescape}, and \autoref{thm:canonical-tail-choice-real-completion-handoff} declare the finite tail-choice packet and its Real-completion handoff surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RealUp completeness, quotient-stream equality, host real equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, canonical-index exactness, schedule factorization, no-choice refusal, Real-completion handoff, and TasteGate carrier support for the same packet.}
\end{closurestatus}
