The top-level $\ContinuousMapUp$ node reads the metric carrier through
\autoref{ch:concrete-instances-metric-namecert}, the pointwise-modulus surface
through \autoref{ch:concrete-instances-continuous-namecert}, and the graph and
composition surface through
\autoref{ch:concrete-instances-category-namecert}. Compact-uniform consumers
read this combined horizontal lattice through the existing child rows, rather
than through an isolated kernel-only entry.
