\chapter{A Concrete Naming Certificate for $\FiniteModulusDiagonalUp$}
\label{ch:concrete-instances-finite-modulus-diagonal-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\FiniteModulusDiagonalUp$ packet records the finite diagonal extraction surface needed by L10 Real-and-Completion consumers. It sits over the modulus-controlled Cauchy read of \autoref{ch:concrete-instances-realcauchymodulus-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real seal boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite diagonal route before any quotient stream equality, selected limit, countable-choice row, or ambient $\RealUp$ completeness theorem is available.

\begin{definition}[Finite modulus diagonal carrier]
\label{def:finite-modulus-diagonal-carrier}
A $\FiniteModulusDiagonalUp$ carrier is a finite $\BHist$ packet
$$
D=(M,S,T,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the accepted $\RealCauchyModulusUp$ modulus packet that schedules each requested dyadic precision.
\item $S$ is the $\StreamNameUp$ finite-window ledger selected by $M$ for the diagonal read.
\item $T$ is the $\DyadicRatCoreUp$ tolerance ledger recording the finite diagonal comparisons inspected on those windows.
\item $Q$ is the $\RegSeqRatUp$ readback row produced from the scheduled finite windows and dyadic tolerance ledger.
\item $E$ is the terminal $\RealUp$ seal row reached only after $M,S,T,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the modulus packet, window ledger, tolerance ledger, readback row, seal row, continuation row, provenance row, and local naming row.
\item $C$ records the displayed $\Cont$ replay from a finite diagonal request through $M,S,T,Q,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealCauchyModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteModulusDiagonalUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient stream equality, a host-selected diagonal subsequence, countable choice, trichotomy, a detached limit, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite modulus diagonal NameCert obligations]
\label{thm:finite-modulus-diagonal-namecert-obligations}
The seed $\NameCert_{\FiniteModulusDiagonalUp}$ surface for \autoref{def:finite-modulus-diagonal-carrier} consists exactly of carrier exposure for $D$, modulus exposure through $M$, finite-window exposure through $S$, dyadic diagonal tolerance exposure through $T$, regular rational readback through $Q$, terminal $\RealUp$ seal exposure through $E$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$. Each accepted finite diagonal request is classified only by the displayed route through the $\RealCauchyModulusUp$ packet, the $\StreamNameUp$ window ledger, and the $\DyadicRatCoreUp$ tolerance row.
\end{theorem}
\begin{proof}
Project the carrier tuple $D=(M,S,T,Q,E,H,C,P,N)$. The analytic rows are exactly the modulus packet, stream windows, dyadic tolerance ledger, regular readback, and real seal.

Read the structural rows next. The witnesses $H,C,P,N$ transport, replay, source, and name the same finite route, so the local certificate has no hidden analytic coordinate.

The excluded principles are absent from the tuple in \autoref{def:finite-modulus-diagonal-carrier}. Hence the listed rows exhaust the seed obligation surface.
\end{proof}

\begin{theorem}[Finite modulus diagonal window cofinality]
\label{thm:finite-modulus-diagonal-window-cofinality}
Let $D=(M,S,T,Q,E,H,C,P,N)$ be an accepted $\FiniteModulusDiagonalUp$ packet. For any two finite diagonal precision requests whose second request refines the first, the window selected by $M$ and displayed in $S$ for the second request is cofinal over the earlier window. It preserves the earlier $\StreamNameUp$ observations, keeps the earlier $\DyadicRatCoreUp$ tolerance entries inside $T$, and leaves the associated $\RegSeqRatUp$ readback $Q$ and terminal $\RealUp$ seal $E$ attached to the same carrier route.
\end{theorem}
\begin{proof}
Read the underlying modulus packet $M$ through \autoref{ch:concrete-instances-realcauchymodulus-namecert}. Refining a finite precision request may enlarge the displayed StreamName window, but it does not erase the earlier scheduled window.

The dyadic ledger row $T$ records both the earlier tolerance entries and the refined entries as finite packet data. The readback row $Q$ is consumed only after this ledger is displayed, so the refined request preserves the earlier rational readback route.

Finally replay $C$ and transport by $H$. The terminal seal row $E$ remains the same displayed $\RealUp$ handoff, and the provenance and local naming rows $P,N$ keep the cofinal route inside the accepted carrier.
\end{proof}

\begin{theorem}[Finite modulus diagonal Real-seal handoff]
\label{thm:finite-modulus-diagonal-real-seal-handoff}
Every terminal $\RealUp$ seal read accepted by a $\FiniteModulusDiagonalUp$ packet factors through
$$
\RealCauchyModulusUp,\quad \StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp .
$$
Equivalently, the diagonal consumer reaches $E$ only after the finite modulus row $M$, window ledger $S$, tolerance ledger $T$, and regular readback row $Q$ have been displayed and replayed by $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The row $E$ is not a detached real value; it is a named coordinate after $M,S,T,Q$ in the finite packet.

Use the cofinal window theorem \autoref{thm:finite-modulus-diagonal-window-cofinality}. Any stronger diagonal request preserves the earlier finite window and readback route before it reaches the terminal seal.

The rows $H,C,P,N$ only transport, replay, source, and name that route. Therefore every accepted Real-seal read factors through the displayed modulus-window-ledger-readback handoff.
\end{proof}

\begin{theorem}[Finite modulus diagonal ledger nonescape]
\label{thm:finite-modulus-diagonal-ledger-nonescape}
No downstream L10 consumer of an accepted $\FiniteModulusDiagonalUp$ packet can bypass the displayed ledger $M,S,T,Q,E,H,C,P,N$. In particular, the packet does not expose a quotient stream, selected diagonal subsequence, host real equality, countable-choice row, unbounded stream read, detached limit, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Inspect the carrier in \autoref{def:finite-modulus-diagonal-carrier}. The only analytic coordinates are the modulus packet, finite stream windows, dyadic tolerance ledger, regular rational readback, and terminal real seal.

The NameCert obligation theorem \autoref{thm:finite-modulus-diagonal-namecert-obligations} says that every accepted local read is classified through those coordinates and the structural rows $H,C,P,N$.

Thus a consumer route omitting the displayed ledger is not a read of the accepted packet. The excluded principles have no carrier coordinate and cannot be introduced by the local certificate.
\end{proof}

\begin{closurestatus}{\FiniteModulusDiagonalUp}
  \constructivestory{A $\FiniteModulusDiagonalUp$ packet is generated from a RealCauchyModulus schedule, StreamName finite windows, a DyadicRatCore diagonal tolerance ledger, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-modulus-diagonal-carrier}, \autoref{thm:finite-modulus-diagonal-namecert-obligations}, \autoref{thm:finite-modulus-diagonal-window-cofinality}, \autoref{thm:finite-modulus-diagonal-real-seal-handoff}, and \autoref{thm:finite-modulus-diagonal-ledger-nonescape} seed the finite modulus diagonal carrier over $\RealCauchyModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected diagonal subsequences, host real equality, countable choice, unbounded stream reads, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Formalization should encode the carrier rows and prove the finite-window cofinality and Real-seal handoff over the displayed BEDC packet.}
\end{closurestatus}
