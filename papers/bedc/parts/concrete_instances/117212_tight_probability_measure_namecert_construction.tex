\chapter{A Concrete Naming Certificate for $\TightProbabilityMeasureUp$}
\label{ch:concrete-instances-tight-probability-measure-namecert}
\origin{human}

Following Prokhorov and Billingsley, the $\TightProbabilityMeasureUp$ packet records the finite tightness surface of a probability measure before weak-convergence or compactness consumers inspect it. It starts from the probability source of \autoref{ch:concrete-instances-probspace-namecert}, the event-surface row of \autoref{ch:concrete-instances-measure-namecert}, and the pushforward law row of \autoref{ch:concrete-instances-distribution-namecert}; it then hands the same finite outside-mass and compact-test route to the Prokhorov-facing surface of \autoref{ch:concrete-instances-prokhorov-tight-probability-namecert}. The packet names tight probability-measure data through displayed certificates, not through measure completion, selected compact sets, countable choice, host weak compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Tight probability measure carrier]
\label{def:tight-probability-measure-carrier}
A $\TightProbabilityMeasureUp$ carrier is a finite $\BHist$ packet
$$
T=(P,M,D,K,E,R,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\ProbSpaceUp$ source row carrying the total-mass and event-family surface.
\item $M$ is the $\MeasureUp$ event-surface row through which the inspected outside event and its mass are read.
\item $D$ is the $\DistributionUp$ pushforward row used when the same probability mass is consumed as a law on a target measurable surface.
\item $K$ is the compact-test row naming the finite compactness request whose outside event is being measured.
\item $E$ is the real-valued outside-mass error row, read through the displayed $\RealUp$ comparison surface.
\item $R$ is the finite tolerance ledger aligning the compact-test request, the outside event in $M$, the pushforward row in $D$, and the error row $E$.
\item $H$ records componentwise $\hsame$ transport for the probability source, measure event, distribution law, compact test, outside-mass row, tolerance ledger, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from probability-source admission to event-surface inspection, distribution readback, compact-test exposure, outside-mass comparison, and tolerance-ledger readback.
\item $Q$ is the $\Pkg$ provenance over $\TightProbabilityMeasureUp$, $\ProbSpaceUp$, $\MeasureUp$, $\DistributionUp$, $\ProkhorovTightProbabilityUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\TightProbabilityMeasureUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $P,M,D,K,E,R,H,C,Q,N$. It has no coordinate for measure completion, selected compact subsets, countable choice, host weak compactness, quotient events, or ambient real completeness.
\end{definition}

\begin{theorem}[Tight probability measure NameCert obligations]
\label{thm:tight-probability-measure-namecert-obligations}
For every accepted $\TightProbabilityMeasureUp$ carrier
$$
T=(P,M,D,K,E,R,H,C,Q,N),
$$
the local $\NameCert_{\TightProbabilityMeasureUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the probability source, measure event surface, distribution law, compact-test request, outside-mass error row, tolerance ledger, transport, replay, provenance, and local naming;
\item probability-measure compatibility, requiring the $\ProbSpaceUp$ row $P$ and $\MeasureUp$ row $M$ to name the same mass surface before a tightness read is accepted;
\item distribution compatibility, requiring the $\DistributionUp$ row $D$ to consume the same measured event through its pushforward law route rather than through a host law equality;
\item compact outside-mass exposure, requiring $K,E,R$ to display the compact-test request, real outside-mass bound, and tolerance ledger before any Prokhorov-facing consumer inspects the packet;
\item componentwise transport, displayed replay, provenance, and local naming for the same rows through $H,C,Q,N$;
\item nonescape, excluding measure completion, selected compact subsets, countable choice, host weak compactness, quotient events, and ambient $\RealUp$ completeness.
\end{enumerate}
No tight probability-measure obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The probability source $P$, event-surface measure row $M$, and distribution law row $D$ are the only probability-facing coordinates.

The compact side is finite. The compact-test request $K$, outside-mass row $E$, and tolerance ledger $R$ must be displayed before a tightness consumer can inspect the packet.

Transport and replay preserve the same route through $H$ and $C$. Provenance $Q$ and local naming $N$ record that route as the local certificate surface.

Since none of the excluded objects occurs as a carrier coordinate, the local NameCert surface cannot supply measure completion, selected compact subsets, countable choice, host weak compactness, quotient events, or ambient real completeness.
\end{proof}

\begin{theorem}[Tight probability measure Prokhorov handoff]
\label{thm:tight-probability-measure-prokhorov-handoff}
Every Prokhorov-facing consumer of an accepted $\TightProbabilityMeasureUp$ carrier reads only the finite route
$$
\ProbSpaceUp \longmapsto \MeasureUp \longmapsto \DistributionUp \longmapsto K,E,R \longmapsto \ProkhorovTightProbabilityUp
$$
together with $H,C,Q,N$. Thus the handoff to \autoref{ch:concrete-instances-prokhorov-tight-probability-namecert} is a displayed probability-source, measure-event, distribution-law, compact-test, outside-mass, tolerance-ledger, transport, replay, provenance, and local-naming route, with no measure completion, selected subsequence, countable-choice selector, host weak compactness theorem, quotient event, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:tight-probability-measure-namecert-obligations}. They force the probability source, measure event surface, and distribution law to be displayed before the compact outside-mass route is consumed.

Next read the compact and tolerance rows. The rows $K,E,R$ are the only compact-test, outside-mass, and tolerance coordinates available to the carrier, so the Prokhorov-facing consumer receives those rows rather than a host compactness theorem.

Finally pass the displayed route to the Prokhorov tight probability surface. The structural rows $H,C,Q,N$ transport, replay, source, and name the same finite packet, and the carrier has no remaining coordinate from which a consumer could extract measure completion, selected subsequences, countable choice, weak compactness, quotient events, or ambient real completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\TightProbabilityMeasureUp$ tightness read factors through $\ProbSpaceUp$, $\MeasureUp$, $\DistributionUp$, a displayed compact-test row, an outside-mass $\RealUp$ row, and a finite tolerance ledger before any $\ProkhorovTightProbabilityUp$ consumer can inspect it.}

\independenceWitness{probspace, measure, distribution, prokhorov-tight-probability: $\TightProbabilityMeasureUp$ is not bijective to any listed sibling because it jointly carries probability-source admission, event-surface measure access, pushforward-law compatibility, compact outside-mass data, tolerance alignment, transport, replay, provenance, and local naming.}

\closureat{TightProbabilityMeasureUp}{seedStr}

\begin{closurestatus}{\TightProbabilityMeasureUp}
  \constructivestory{A $\TightProbabilityMeasureUp$ packet is generated from ProbSpaceUp source rows, MeasureUp event-surface rows, DistributionUp pushforward rows, compact-test rows, outside-mass RealUp rows, a finite tolerance ledger, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:tight-probability-measure-carrier}, \autoref{thm:tight-probability-measure-namecert-obligations}, and \autoref{thm:tight-probability-measure-prokhorov-handoff} seed the finite tight probability-measure certificate surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close measure completion, weak compactness, selected compact sets, selected subsequences, countable choice, quotient events, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires carrier admission, probability-measure compatibility, distribution compatibility, compact outside-mass exposure, nonescape, transport, replay, provenance, and local naming for BEDC.Derived.TightProbabilityMeasureUp.}
\end{closurestatus}
