\chapter{A Concrete Naming Certificate for $\CompactOscillationSelectorUp$}
\label{ch:concrete-instances-compact-oscillation-selector-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\CompactOscillationSelectorUp$ packet records the finite selector route behind compact uniform continuity as one BEDC NameCert carrier. It realizes the compact-uniform-continuity vision of \autoref{ch:visions-compact-uniform-continuity} by reading compact finite nets from \autoref{ch:concrete-instances-compactmetric-namecert}, pointwise radius rows from \autoref{ch:concrete-instances-continuousmap-namecert}, rational lower-bound folds from \autoref{ch:concrete-instances-rat-namecert}, metric distance rows from \autoref{ch:concrete-instances-metric-namecert}, terminal Real seals from \autoref{ch:concrete-instances-real-namecert}, finite stream windows from \autoref{ch:concrete-instances-streamname-namecert}, and the downstream $\UniformModulusUp$ handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet selects only by finite displayed rows; it does not expose host open-cover compactness, semantic continuity, selected centers outside the finite net, a quotient point equality, an external modulus selector, or ambient completeness.

\begin{definition}[Compact oscillation selector carrier]
\label{def:compact-oscillation-selector-carrier}
A $\CompactOscillationSelectorUp$ carrier is a finite $\BHist$ packet
$$
S=(K,F,L,M,R,E,W,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, carrying bundled centers and located coverage witnesses.
\item $F$ is the $\ContinuousMapUp$ pointwise-radius row, attaching a local oscillation estimate and source radius to each bundled center.
\item $L$ is the rational lower-bound fold row over the displayed local radii, read through $\RatUp$.
\item $M$ is the $\MetricUp$ distance row used to route source and target triangle estimates.
\item $R$ is the $\RealUp$ seal row for Real-facing distance and oscillation reads.
\item $E$ is the dyadic or rational tolerance ledger that connects requested oscillation bounds to the lower-bound fold.
\item $W$ is the $\StreamNameUp$ finite-window row for center names, source points, metric reads, and Real seals.
\item $U$ is the $\UniformModulusUp$ handoff row exported only after $K,F,L,M,R,E,W$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact-net, pointwise-radius, lower-bound, metric, Real, tolerance, stream-window, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through finite net selection, pointwise-radius rows, rational lower-bound fold, metric triangle estimates, Real sealing, and the uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactOscillationSelectorUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RatUp$, $\MetricUp$, $\RealUp$, $\StreamNameUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactOscillationSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for host compactness, semantic continuity, quotient point equality, choice-selected centers outside the displayed net, an external modulus selector, or ambient completeness.
\end{definition}

\begin{theorem}[Compact oscillation selector NameCert obligations]
\label{thm:compact-oscillation-selector-namecert-obligations}
For every accepted $\CompactOscillationSelectorUp$ carrier
$$
S=(K,F,L,M,R,E,W,U,H,C,P,N),
$$
the local $\NameCert_{\CompactOscillationSelectorUp}$ surface consists exactly of compact finite-net admission, pointwise-radius admission, rational lower-bound fold exactness, metric triangle-read admission, Real-seal nonescape, tolerance-ledger visibility, finite stream-window readback, UniformModulus handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming. The obligations do not include host open-cover compactness, semantic continuity, quotient point equality, choice-selected centers outside the finite net, external modulus selection, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-oscillation-selector-carrier}. The displayed rows $K,F,L,M,R,E,W,U,H,C,P,N$ are the whole accepted packet.

Compact finite-net admission comes from $K$, and pointwise-radius admission comes from $F$. The lower-bound row $L$ folds the finitely many local radii read from those rows, while $E$ records the requested tolerance scale.

The metric row $M$, Real seal $R$, and stream-window row $W$ provide the only route for source and target triangle estimates. The uniform-modulus row $U$ is exported only after those finite rows are visible.

The structural rows $H,C,P,N$ preserve, replay, source, and name the same finite selector route. Since the refused objects are not carrier coordinates, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact oscillation selector finite-net fold]
\label{thm:compact-oscillation-selector-finite-net-fold}
Every $\UniformModulusUp$ consumer of an accepted $\CompactOscillationSelectorUp$ packet receives a precision row only after the finite net $K$, pointwise-radius row $F$, rational lower-bound fold $L$, metric row $M$, Real seal $R$, tolerance ledger $E$, and stream-window row $W$ have been displayed. The handoff is the finite net, pointwise-radius, rational lower-bound fold, and Real-seal route, not an external compactness or continuity theorem.
\end{theorem}
\begin{proof}
Start with the compact finite-net row $K$. It supplies the finite bundled centers and coverage witnesses available to the selector.

Read the pointwise-radius row $F$ at each bundled center. These rows are finite local oscillation records, so they do not invoke semantic continuity outside the displayed carrier.

Fold the displayed local radii through $L$ using the tolerance ledger $E$. The result is a rational lower-bound history carried by the packet rather than a chosen external modulus.

Finally replay the metric and Real-facing estimates through $M,R,W$ and hand them to $U$. Transport and replay through $H$ and $C$ keep the same finite route, so the consumer receives only the displayed selector data.
\end{proof}

\begin{theorem}[Compact oscillation selector nonescape]
\label{thm:compact-oscillation-selector-nonescape}
Every downstream consumer of an accepted $\CompactOscillationSelectorUp$ packet remains inside the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \RatUp,\ \MetricUp,\ \RealUp,\ \StreamNameUp,\ \UniformModulusUp .
$$
The consumer may inspect finite nets, pointwise radii, rational lower-bound folds, metric triangle rows, Real seals, finite stream windows, uniform-modulus handoff rows, transport, replay, provenance, and local naming, but it cannot obtain host open-cover compactness, semantic continuity, quotient point equality, choice-selected centers outside the displayed net, an external modulus selector, or ambient completeness from this packet.
\end{theorem}
\begin{proof}
Apply \autoref{thm:compact-oscillation-selector-namecert-obligations}. It lists the finite analytic and structural rows that form the local NameCert surface.

Use \autoref{thm:compact-oscillation-selector-finite-net-fold}. Every UniformModulus-facing read consumes the compact net, pointwise-radius rows, rational lower-bound fold, metric row, Real seal, tolerance ledger, and stream-window row before the handoff row is public.

The nonescape conclusion is the carrier inventory. None of the refused objects is a coordinate of $S$, and the transport and replay rows are componentwise over the displayed route, so no downstream consumer can acquire them from $\CompactOscillationSelectorUp$.
\end{proof}

\closureat{CompactOscillationSelectorUp}{seedStr}

\begin{closurestatus}{\CompactOscillationSelectorUp}
  \constructivestory{A $\CompactOscillationSelectorUp$ packet is generated from CompactMetric finite-net data, ContinuousMap pointwise-radius rows, a RatUp lower-bound fold, Metric triangle rows, RealUp seals, tolerance and StreamName windows, a UniformModulusUp handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-oscillation-selector-carrier}, \autoref{thm:compact-oscillation-selector-namecert-obligations}, \autoref{thm:compact-oscillation-selector-finite-net-fold}, and \autoref{thm:compact-oscillation-selector-nonescape} seed the finite selector route from compact nets and pointwise radii to UniformModulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host open-cover compactness, semantic continuity, quotient point equality, choice-selected centers outside the finite net, external modulus selection, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-net admission, pointwise-radius admission, lower-bound fold exactness, metric triangle reads, Real-seal nonescape, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactOscillationSelectorUp.}
\end{closurestatus}
