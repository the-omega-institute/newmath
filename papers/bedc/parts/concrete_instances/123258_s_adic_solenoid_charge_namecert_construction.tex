\chapter{A Concrete Naming Certificate for $\SAdicSolenoidChargeUp$}
\label{ch:concrete-instances-s-adic-solenoid-charge-namecert}
\origin{human}

Following Pontryagin duality and solenoid theory, the $\SAdicSolenoidChargeUp$ packet records the finite prime-window charge surface behind the $S$-adic solenoid charge group. It consumes the prime rows of \autoref{ch:concrete-instances-prime-namecert}, the rational charge rows of \autoref{ch:concrete-instances-rat-namecert}, the visible circle surfaces of \autoref{ch:concrete-instances-circle-namecert} and \autoref{ch:concrete-instances-s1-namecert}, the topological-group carrier of \autoref{ch:concrete-instances-topgroup-namecert}, and the solenoid phase packet of \autoref{ch:concrete-instances-solenoid-phase-namecert}. The packet names a finite denominator-window charge certificate; it is not a completed duality theorem, an infinite prime set, a selected character basis, or an ambient compact-group equality.

\begin{definition}[$S$-adic solenoid charge carrier]
\label{def:s-adic-solenoid-charge-carrier}
An $\SAdicSolenoidChargeUp$ carrier is a finite $\BHist$ packet
$$
C=(S,P,L,D,Q,V,F,H,R,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite prime-window row.
\item $P$ is the corresponding family of $\PrimeUp$ rows.
\item $L$ is the $S$-localization ledger, recording which denominator factors may be inverted inside the packet.
\item $D$ is the denominator-window charge row, read as a $\RatUp$ row whose denominator factors are justified by $S,P,L$.
\item $Q$ is the $\SolenoidPhaseUp$ row that records compatible phase data for the same finite window.
\item $V$ is the visible $\CircleUp$ and $\SOneUp$ row read from the phase packet.
\item $F$ is the compact fibre row over the $\TopGroupUp$ surface.
\item $H$ records componentwise $\hsame$ transport for $S,P,L,D,Q,V,F,R,N$.
\item $R$ records displayed $\Cont$ replay and $\Pkg$ provenance for the prime-window, localization, charge, phase, visible-circle, fibre, transport, and local naming rows.
\item $N$ is the local $\NameCert_{\SAdicSolenoidChargeUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through these rows. It has no coordinate for an infinite prime set, a denominator factor outside $S$, a selected character basis, host equality of compact groups, or a completed Pontryagin-duality proof.
\end{definition}
\leandef{BEDC.Derived.SAdicSolenoidChargeUp.SAdicSolenoidChargeUp}
\leanchecked{BEDC.Derived.SAdicSolenoidChargeUp.SAdicSolenoidChargeTasteGate\_single\_carrier\_alignment}

\begin{theorem}[$S$-adic solenoid charge NameCert obligations]
\label{thm:s-adic-solenoid-charge-namecert-obligations}
For every accepted $\SAdicSolenoidChargeUp$ carrier
$$
C=(S,P,L,D,Q,V,F,H,R,N),
$$
the local $\NameCert_{\SAdicSolenoidChargeUp}$ surface consists exactly of finite prime-window exposure through $S$, prime-row admission through $P$, localization-ledger exactness through $L$, denominator-window charge exposure through $D$, solenoid phase exposure through $Q$, visible circle exposure through $V$, compact fibre exposure through $F$, componentwise transport through $H$, replay and provenance through $R$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $S$ and $P$ display the finite prime window, while $L$ records exactly which denominator factors may be inverted.

The charge row $D$ is read only after the localization ledger is visible. Hence a rational charge whose denominator uses a prime outside the displayed window has no admitted row in the packet.

The phase and topology rows $Q,V,F$ then place the charge beside the solenoid phase, visible circle, and compact fibre surfaces. These rows give the finite NameCert boundary for the solenoid charge packet, not a completed duality theorem.

Finally $H,R,N$ transport, replay, source, and name the same carrier. Since the carrier lacks an infinite prime set, selected character basis, host compact-group equality, or ambient Pontryagin-duality coordinate, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[$S$-adic solenoid charge denominator-window route]
\label{thm:s-adic-solenoid-charge-denominator-window-route}
Every charge read exported by an accepted $\SAdicSolenoidChargeUp$ packet factors through the finite route
$$
\PrimeUp,\RatUp
\longmapsto
\SolenoidPhaseUp,\CircleUp,\SOneUp,\TopGroupUp
\longmapsto
\SAdicSolenoidChargeUp .
$$
The denominator-window row $D$ may use only the primes displayed in $S,P,L$, and every visible-circle or compact-fibre read must preserve the same localization ledger.
\end{theorem}
\begin{proof}
Start with the prime-window rows $S,P,L$. They determine the finite localization boundary before any rational charge is admitted.

The row $D$ reads rational charge data through that boundary. If a denominator factor is not justified by $S,P,L$, the charge has no row in the accepted packet.

The phase rows $Q,V,F$ consume the same charge boundary when exposing the solenoid phase, visible circle, and compact fibre. No step in this route changes the prime window or replaces it by an infinite prime set.

Transport, replay, provenance, and local naming through $H,R,N$ preserve the route componentwise. Thus the exported charge read is a finite denominator-window certificate rather than an ambient compact-group or completed-duality assertion.
\end{proof}

\falsifiablePrediction{Every accepted $\SAdicSolenoidChargeUp$ charge read factors through finite PrimeUp rows, an S-localization ledger, a RatUp denominator-window charge row, SolenoidPhaseUp phase data, visible CircleUp and SOneUp rows, a TopGroupUp compact fibre row, transport, replay, provenance, and local NameCert data.}

\independenceWitness{prime, rat, circle, s-one, top-group, solenoid-phase: $\SAdicSolenoidChargeUp$ is not bijective to any listed sibling because it jointly carries a finite prime window, localization ledger, denominator-window charge row, solenoid phase row, visible circle row, compact fibre row, transport, replay, provenance, and local naming.}

\closureat{SAdicSolenoidChargeUp}{seedStr}

\begin{closurestatus}{\SAdicSolenoidChargeUp}
  \constructivestory{An $\SAdicSolenoidChargeUp$ packet is generated from a finite prime-window row, PrimeUp rows, an S-localization ledger, a RatUp denominator-window charge row, SolenoidPhaseUp phase data, visible CircleUp and SOneUp rows, a TopGroupUp compact fibre row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:s-adic-solenoid-charge-carrier}, \autoref{thm:s-adic-solenoid-charge-namecert-obligations}, and \autoref{thm:s-adic-solenoid-charge-denominator-window-route} seed the finite prime-window solenoid charge packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, completed Pontryagin duality, infinite prime-window classification, host compact-group equality, selected character bases, or ambient solenoid equivalence.}
  \upgradepath{Obligation closure requires checked carrier admission, prime-window exposure, localization-ledger exactness, denominator-window charge exposure, solenoid phase handoff, visible-circle and compact-fibre exposure, transport, replay, provenance, and local naming for BEDC.Derived.SAdicSolenoidChargeUp.}
\end{closurestatus}
