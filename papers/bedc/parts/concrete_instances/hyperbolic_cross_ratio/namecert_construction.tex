\chapter{A Concrete Naming Certificate for $\HyperbolicCrossRatioUp$}
\label{ch:concrete-instances-hyperbolic-cross-ratio-namecert}
\origin{ai}

The $\HyperbolicCrossRatioUp$ packet realizes the four-boundary-point comparison branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits beside the Poincare disk phase route of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, the hyperbolic metric surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary-fixed transport surface of \autoref{ch:concrete-instances-poincare-boundary-fixed-transport-namecert}, the Busemann boundary packet of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, and the Schwarz--Pick disk-contraction row of \autoref{ch:concrete-instances-schwarz-pick-disk-contraction-namecert}. The packet names a local four-point invariant over displayed disk-boundary rows; it does not import a host ideal-boundary compactification, a global hyperbolic plane, selected geodesics, or quotient boundary equality.

\begin{definition}[Hyperbolic cross-ratio carrier]
\label{def:hyperbolic-cross-ratio-carrier}
A $\HyperbolicCrossRatioUp$ carrier is a finite $\BHist$ packet
$$
X=(D,M,B_0,B_1,B_2,B_3,T,Q,R,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\PoincareDiskPhaseTransportUp$ disk-source and phase-transport row for the visible disk coordinate system.
\item $M$ is the $\HyperbolicMetricUp$ comparison row attached to that disk source.
\item $B_0,B_1,B_2,B_3$ are four boundary-point rows, each read through the displayed Poincare boundary interface rather than through a host compactification.
\item $T$ is the $\PoincareBoundaryFixedTransportUp$ row proving that the four boundary rows remain attached to the same boundary-fixed disk transport.
\item $Q$ is the finite cross-ratio comparison row, exposing only the retained pairwise boundary-distance and oriented separation comparisons.
\item $R$ is the refusal row excluding host global geometry, selected geodesics, quotient boundary equality, and object-level ideal-boundary compactification.
\item $F$ is the $\SchwarzPickDiskContractionUp$ provenance row that records nonexpansive disk-map compatibility for consumers comparing the cross-ratio under disk transport.
\item $H$ records componentwise $\hsame$ transport for disk, metric, four boundary rows, boundary-fixed transport, cross-ratio comparison, refusal, Schwarz--Pick provenance, package provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from disk phase transport through metric comparison, four boundary rows, boundary-fixed transport, cross-ratio comparison, refusal, and provenance.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicCrossRatioUp$, $\PoincareDiskPhaseTransportUp$, $\HyperbolicMetricUp$, $\PoincareBoundaryFixedTransportUp$, $\HyperbolicBusemannBoundaryUp$, $\SchwarzPickDiskContractionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicCrossRatioUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,M,B_0,B_1,B_2,B_3,T,Q,R,F,H,C,P,N$. It has no coordinate for fewer than four boundary rows, a host global metric oracle, selected ideal points outside the displayed boundary interface, quotient boundary equality, or unrecorded disk transport.
\end{definition}

\begin{theorem}[Hyperbolic cross-ratio NameCert obligations]
\label{thm:hyperbolic-cross-ratio-namecert-obligations}
For every accepted $\HyperbolicCrossRatioUp$ carrier $X=(D,M,B_0,B_1,B_2,B_3,T,Q,R,F,H,C,P,N)$, the local $\NameCert_{\HyperbolicCrossRatioUp}$ surface consists exactly of Poincare disk phase transport, hyperbolic-metric comparison, four displayed boundary rows, boundary-fixed transport, finite cross-ratio comparison, global-geometry refusal, Schwarz--Pick provenance, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every accepted consumer must inspect those rows before treating the packet as a hyperbolic four-point invariant.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-cross-ratio-carrier}. The disk and metric rows $D,M$ fix the Poincare disk reading and the hyperbolic comparison surface.

The four boundary rows $B_0,B_1,B_2,B_3$ are then read together with the boundary-fixed transport row $T$. This is the only point at which the carrier exposes boundary points, and all four rows must belong to the same displayed disk-transport route.

The comparison and refusal route is $Q,R,F$. The finite cross-ratio row records the retained pairwise boundary comparisons, the refusal row prevents replacement by host global geometry, and the Schwarz--Pick row records the nonexpansive disk-map provenance consumed by downstream transport comparisons.

Finally $H,C,P,N$ transport, replay, source, and name the same finite surface. Since no other row is present, no consumer can omit a boundary row, replace hyperbolic comparison by a global oracle, or bypass the local naming route.
\end{proof}

\begin{theorem}[Hyperbolic cross-ratio nonescape]
\label{thm:hyperbolic-cross-ratio-nonescape}
Every accepted $\HyperbolicCrossRatioUp$ consumer factors through the finite route
$$
\PoincareDiskPhaseTransportUp,\ \HyperbolicMetricUp,\ \PoincareBoundaryFixedTransportUp,\ \HyperbolicBusemannBoundaryUp,\ \SchwarzPickDiskContractionUp .
$$
The route fixes the disk phase surface, the hyperbolic metric comparison, four boundary rows, boundary-fixed transport, finite cross-ratio comparison, global-geometry refusal, Schwarz--Pick provenance, transport, replay, package provenance, and local naming. It exports no host global geometry, quotient boundary equality, selected ideal-boundary compactification, or unrecorded disk-map invariant.
\end{theorem}
\begin{proof}
Let $X=(D,M,B_0,B_1,B_2,B_3,T,Q,R,F,H,C,P,N)$ be accepted. A consumer first reads $D,M$, so all later comparisons are attached to the displayed disk phase transport and hyperbolic metric rows.

Next the consumer reads $B_0,B_1,B_2,B_3,T$. The four boundary rows are simultaneous carrier fields, and the boundary-fixed transport row keeps them in the same disk-transport context.

The comparison row $Q$ is accepted only together with the refusal row $R$ and Schwarz--Pick provenance $F$. Thus cross-ratio comparison remains finite and local to the displayed disk route.

The rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and naming. The excluded host and quotient objects have no carrier coordinate, so the consumer cannot escape the finite packet.
\end{proof}

\begin{theorem}[Hyperbolic cross-ratio Mobius consumer route]
\label{thm:hyperbolic-cross-ratio-mobius-consumer-route}
Any $\MobiusTransformUp$ consumer of an accepted $\HyperbolicCrossRatioUp$ packet must read the disk row $D$, metric row $M$, four boundary rows $B_0,B_1,B_2,B_3$, boundary-fixed transport row $T$, cross-ratio row $Q$, refusal row $R$, Schwarz--Pick provenance row $F$, and structural rows $H,C,P,N$ before it may claim cross-ratio preservation. The consumer route of \autoref{ch:concrete-instances-mobius-transform-namecert} has no accepted preservation coordinate outside this four-boundary packet.
\end{theorem}
\begin{proof}
Apply \autoref{thm:hyperbolic-cross-ratio-nonescape}. Every accepted $\HyperbolicCrossRatioUp$ consumer already factors through the disk, metric, boundary, transport, comparison, refusal, provenance, replay, package, and naming rows of the finite carrier.

A $\MobiusTransformUp$ preservation read is a downstream consumer of the same packet, not a separate host-level geometric claim. Since the carrier has no coordinate for cross-ratio preservation unless the four displayed boundary rows and the boundary-fixed transport route are present, the Mobius consumer must enter through $D,M,B_0,B_1,B_2,B_3,T,Q,R,F,H,C,P,N$.
\end{proof}

\begin{theorem}[Hyperbolic cross-ratio horofunction consumer route]
\label{thm:hyperbolic-cross-ratio-horofunction-consumer-route}
Any $\HyperbolicHorofunctionBoundaryUp$ consumer of an accepted $\HyperbolicCrossRatioUp$ packet must first read the disk row $D$, hyperbolic metric row $M$, four boundary rows $B_0,B_1,B_2,B_3$, boundary-fixed transport row $T$, cross-ratio row $Q$, refusal row $R$, Schwarz--Pick provenance row $F$, and structural rows $H,C,P,N$ before treating the packet as a boundary metric comparison. The consumer route reads boundary metric data through Busemann and visual-boundary rows, not through a host compactification.
\end{theorem}
\begin{proof}
Apply \autoref{thm:hyperbolic-cross-ratio-nonescape}. Every accepted $\HyperbolicCrossRatioUp$ consumer already enters through the disk, metric, four boundary rows, boundary-fixed transport, cross-ratio comparison, refusal, provenance, replay, package, and naming rows.

A $\HyperbolicHorofunctionBoundaryUp$ read is boundary-metric facing, so it cannot treat $Q$ as an isolated scalar comparison. The four boundary rows and the metric row must first be routed through the displayed Busemann and visual-boundary surfaces that the horofunction-boundary packet consumes.

The refusal row $R$ blocks host compactification and quotient ideal-boundary equality. The structural rows $H,C,P,N$ then preserve the same finite route, so the horofunction consumer receives only the cross-ratio data that has passed through the displayed boundary metric rows.
\end{proof}

\falsifiablePrediction{Any accepted $\HyperbolicCrossRatioUp$ consumer omitting one of the four boundary rows, the $\HyperbolicMetricUp$ comparison, the $\PoincareBoundaryFixedTransportUp$ row, $\Pkg$ provenance, or the local $\NameCert$ row refutes \autoref{thm:hyperbolic-cross-ratio-nonescape}.}

\independenceWitness{poincare-disk-phase-transport, hyperbolic-metric, poincare-boundary-fixed-transport, hyperbolic-busemann-boundary, schwarz-pick-disk-contraction: $\HyperbolicCrossRatioUp$ is not bijective to any listed sibling because it jointly carries disk phase transport, hyperbolic metric comparison, four boundary rows, boundary-fixed transport, finite cross-ratio comparison, refusal of host global geometry, Schwarz--Pick provenance, transport, replay, package provenance, and local naming.}

The Lean TasteGate file for $\HyperbolicCrossRatioUp$ will provide explicit $\mathsf{Nontrivial}\ \HyperbolicCrossRatioUp$ and $\mathsf{FieldFaithful}\ \HyperbolicCrossRatioUp$ instances. These instances keep the disk, metric, boundary, transport, comparison, refusal, provenance, replay, package, and local naming rows distinguishable.

\closureat{HyperbolicCrossRatioUp}{seedStr}

\begin{closurestatus}{\HyperbolicCrossRatioUp}
  \constructivestory{A $\HyperbolicCrossRatioUp$ packet is generated from PoincareDiskPhaseTransportUp disk data, HyperbolicMetricUp comparison, four displayed boundary rows, PoincareBoundaryFixedTransportUp transport, finite cross-ratio comparison, global-geometry refusal, SchwarzPickDiskContractionUp provenance, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-cross-ratio-carrier}, \autoref{thm:hyperbolic-cross-ratio-namecert-obligations}, \autoref{thm:hyperbolic-cross-ratio-nonescape}, and \autoref{thm:hyperbolic-cross-ratio-mobius-consumer-route} seed the finite four-boundary-point hyperbolic cross-ratio route over disk, metric, boundary, transport, refusal, provenance, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host global geometry, quotient boundary equality, selected ideal-boundary compactification, unrecorded disk-map invariants, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, four boundary rows, hyperbolic metric comparison, boundary-fixed transport, finite cross-ratio comparison, global-geometry refusal, TasteGate nontriviality, field faithfulness, provenance, transport, replay, and local naming for BEDC.Derived.HyperbolicCrossRatioUp.}
\end{closurestatus}
