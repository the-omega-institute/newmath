\chapter{A Concrete Naming Certificate for $\FilterSubbasisCofinalCompletionUp$}
\label{ch:concrete-instances-filter-subbasis-cofinal-completion-namecert}
\origin{human}

Following Kelley and Bourbaki on filters generated by subbases, the $\FilterSubbasisCofinalCompletionUp$ packet records the standard cofinal-subbasis route into Cauchy-filter completion as finite BEDC NameCert data. It sits after the subbasis generator of \autoref{ch:concrete-instances-cauchy-filter-subbasis-namecert}, the generated-base interface of \autoref{ch:concrete-instances-filterbase-namecert}, the Cauchy-filter carrier of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the finite-window schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the route from displayed cofinal subbasis data to completion-facing reads; it does not quotient filters, select limits, invoke countable choice, compare host real values, or claim ambient $\RealUp$ completeness.

\begin{definition}[Filter subbasis cofinal completion carrier]
\label{def:filter-subbasis-cofinal-completion-carrier}
A $\FilterSubbasisCofinalCompletionUp$ carrier is a finite $\BHist$ packet
$$
C=(S,B,F,W,R,L,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\CauchyFilterSubbasisUp$ row, carrying finite subbasis tokens, finite intersections, generated events, and the generation ledger.
\item $B$ is the generated $\FilterBaseUp$ row read cofinally from the finite-intersection surface of $S$.
\item $F$ is the $\CauchyFilterUp$ row reached only after the generated base row has been displayed.
\item $W$ is the $\StreamNameUp$ finite-window row used by completion-facing consumers.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the displayed windows.
\item $L$ is the terminal $\RealUp$ seal row reached after the generated base, Cauchy-filter row, windows, and readback have been consumed.
\item $H$ records componentwise $\hsame$ transport for the subbasis, generated base, Cauchy filter, windows, readback, Real seal, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from subbasis tokens through generated finite intersections, the cofinal base row, Cauchy-filter handoff, windows, readback, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\FilterSubbasisCofinalCompletionUp$, $\CauchyFilterSubbasisUp$, $\FilterBaseUp$, $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FilterSubbasisCofinalCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,B,F,W,R,L,H,K,P,N$. It has no coordinate for quotient filter equality, selected limits, countable-choice schedules, host equality of generated filters, host real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Filter subbasis cofinal completion NameCert obligations]
\label{thm:filter-subbasis-cofinal-completion-namecert-obligations}
For every accepted $\FilterSubbasisCofinalCompletionUp$ carrier $C=(S,B,F,W,R,L,H,K,P,N)$, the local $\NameCert_{\FilterSubbasisCofinalCompletionUp}$ surface consists exactly of subbasis packet exposure through $S$, cofinal generated $\FilterBaseUp$ exposure through $B$, $\CauchyFilterUp$ handoff through $F$, $\StreamNameUp$ window exposure through $W$, $\RegSeqRatUp$ readback through $R$, terminal $\RealUp$ sealing through $L$, componentwise transport through $H$, displayed replay through $K$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:filter-subbasis-cofinal-completion-carrier}. The accepted packet exposes exactly the rows $S,B,F,W,R,L,H,K,P,N$.

The subbasis and generated-base rows are upstream. The row $S$ supplies the finite tokens, finite intersections, generated events, and generation ledger, while $B$ records the cofinal generated base read from that surface.

The completion-facing rows are downstream. A consumer reaches $F$ only after $B$, then reads windows through $W$, regular rational readback through $R$, and the terminal Real seal through $L$.

Finally $H,K,P,N$ transport, replay, source, and name the same route. Since quotient filters, selected limits, countable choice, host equality of generated filters, host real equality, and ambient Real completeness are absent from the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Filter subbasis cofinal completion route]
\label{thm:filter-subbasis-cofinal-completion-route}
Every completion-facing read exported by an accepted $\FilterSubbasisCofinalCompletionUp$ carrier must factor through the $\CauchyFilterSubbasisUp$ row $S$, the cofinal generated $\FilterBaseUp$ row $B$, the $\CauchyFilterUp$ row $F$, the $\StreamNameUp$ row $W$, the $\RegSeqRatUp$ row $R$, and the terminal $\RealUp$ seal row $L$ in that order. The route is a finite cofinal-subbasis completion handoff, not a quotient filter construction or a selected-limit theorem.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:filter-subbasis-cofinal-completion-namecert-obligations}. They force every accepted consumer to expose $S,B,F,W,R,L$ before any structural row can name the completion-facing read.

Use the generated-base cofinality theorem for $\CauchyFilterSubbasisUp$, \autoref{thm:cauchy-filter-subbasis-generated-base-cofinality}. It identifies the generated base as cofinal among the finite intersections displayed by the subbasis packet before Cauchy-filter and Real-facing rows are read.

Apply the refinement-stability theorem \autoref{thm:cauchy-filter-subbasis-filter-refinement-stability}. Cofinal replacement of the displayed generated base preserves the same CauchyFilter, StreamName, RegSeqRat, and Real seal handoff.

Thus a completion-facing consumer first reads the finite subbasis route, then the cofinal generated base, and only then the Cauchy-filter, window, readback, and Real seal rows. The absent classifier coordinates exclude quotient filters, selected limits, choice schedules, host generated-filter equality, host real equality, and ambient completeness.
\end{proof}
\leanstmt{BEDC.Derived.FilterSubbasisCofinalCompletionUp}

\falsifiablePrediction{Every accepted $\FilterSubbasisCofinalCompletionUp$ completion-facing read exposes a CauchyFilterSubbasisUp row, generated cofinal FilterBaseUp row, CauchyFilterUp row, StreamNameUp windows, RegSeqRatUp readback, terminal RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert naming before it can serve a Real-facing completion consumer.}

\independenceWitness{cauchy-filter-subbasis, filterbase, cauchyfilter, streamname, regseqrat, real: none is bijective with $\FilterSubbasisCofinalCompletionUp$ because this packet jointly carries the displayed cofinal subbasis route, generated base handoff, Cauchy-filter handoff, finite windows, readback, Real seal, transport, replay, provenance, and local naming rows.}

\closureat{FilterSubbasisCofinalCompletionUp}{seedStr}

\begin{closurestatus}{\FilterSubbasisCofinalCompletionUp}
  \constructivestory{A $\FilterSubbasisCofinalCompletionUp$ packet is generated from a CauchyFilterSubbasis row, a cofinal generated FilterBase row, a CauchyFilter row, StreamName windows, RegSeqRat readback, a terminal Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:filter-subbasis-cofinal-completion-carrier}, \autoref{thm:filter-subbasis-cofinal-completion-namecert-obligations}, and \autoref{thm:filter-subbasis-cofinal-completion-route} seed the cofinal subbasis route into Cauchy-filter completion over generated base, window, readback, Real seal, transport, replay, provenance, and NameCert rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient filter equality, selected limits, countable choice, host equality of generated filters, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, subbasis exposure, generated-base cofinality, CauchyFilterUp handoff, StreamNameUp window exposure, RegSeqRatUp readback, RealUp seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.FilterSubbasisCofinalCompletionUp.}
\end{closurestatus}
