\chapter{A Concrete Naming Certificate for $\CauchyEqualityCriterionUp$}
\label{ch:concrete-instances-cauchy-equality-criterion-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\CauchyEqualityCriterionUp$ packet records equality of Cauchy real names by common finite windows and zero-tail evidence before any quotient stream equality, host real equality, selected limit, or ambient $\RealUp$ completeness principle is available. It reads regular rational sources from \autoref{ch:concrete-instances-regseqrat-namecert}, finite observation schedules from \autoref{ch:concrete-instances-streamname-namecert}, dyadic tolerance rows from \autoref{ch:concrete-instances-dyadicratcore-namecert}, zero-tail handoffs from \autoref{ch:concrete-instances-nullsequence-namecert}, the real-name seal boundary from \autoref{ch:concrete-instances-real-namecert}, and the common-window comparison route from \autoref{ch:concrete-instances-regular-cauchy-comparison-namecert}. The chapter gives $\RealUp$ and completion-facing arguments a named finite criterion object instead of forcing each consumer to restate the same common-window zero-tail route.

\begin{definition}[Cauchy equality criterion carrier]
\label{def:cauchy-equality-criterion-carrier}
A $\CauchyEqualityCriterionUp$ carrier is a finite $\BHist$ packet
$$
K=(X,Y,W,D,Z,Q,R,S,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ and $Y$ are the two displayed $\RegSeqRatUp$ Cauchy names under comparison.
\item $W$ is the common $\StreamNameUp$ window schedule used to inspect paired finite observations of $X$ and $Y$.
\item $D$ is the $\DyadicRatCoreUp$ ledger recording the dyadic difference and tolerance rows read on the windows selected by $W$.
\item $Z$ is the $\NullSequenceUp$ zero-tail row for the displayed difference route.
\item $Q$ is the finite criterion row saying that each requested equality tolerance is discharged through the same window schedule, dyadic ledger, and zero-tail handoff.
\item $R$ is the pair of $\RealUp$ seal rows reached only after the criterion row has been displayed.
\item $S$ is the $\RegularCauchyComparisonUp$ sibling comparison row exposing the common-window comparison used by downstream consumers.
\item $E$ records exactness of the criterion route from $X,Y,W,D,Z,Q$ to the seal rows $R$.
\item $H$ records componentwise $\hsame$ transports for the two names, schedule, dyadic ledger, zero-tail handoff, criterion row, seal rows, sibling comparison row, and exactness row.
\item $C$ records displayed $\Cont$ replay through the same rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyEqualityCriterionUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\NullSequenceUp$, $\RealUp$, $\RegularCauchyComparisonUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyEqualityCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,W,D,Z,Q,R,S,E,H,C,P,N$. It has no coordinate for quotient stream equality, host real equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or a standard bridge.
\end{definition}

\begin{theorem}[Cauchy equality criterion NameCert obligations]
\label{thm:cauchy-equality-criterion-namecert-obligations}
For every accepted $\CauchyEqualityCriterionUp$ carrier
$$
K=(X,Y,W,D,Z,Q,R,S,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyEqualityCriterionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two regular Cauchy names, common window schedule, dyadic difference ledger, zero-tail handoff, criterion row, real seal rows, sibling comparison row, exactness row, transport, replay, provenance, and local naming row;
\item window exactness, requiring every equality request to read $X$ and $Y$ through the same displayed finite schedule $W$;
\item dyadic difference exactness, requiring the tolerance evidence to be the displayed $\DyadicRatCoreUp$ ledger $D$ read on the windows named by $W$;
\item zero-tail exactness, requiring the equality criterion to consume the $\NullSequenceUp$ row $Z$ for the displayed difference route;
\item criterion exhaustion, requiring $Q$ to be the only finite row that converts the common-window zero-tail evidence into a seal-facing equality criterion;
\item comparison compatibility, requiring the sibling $\RegularCauchyComparisonUp$ row $S$ to expose the same windows, dyadic ledger, and seal-facing route;
\item seal-boundary exactness, requiring the $\RealUp$ rows $R$ to be read only after $W,D,Z,Q,S$ have been displayed;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same finite route;
\item provenance and local naming, requiring $P$ and $N$ to package exactly the displayed rows $X,Y,W,D,Z,Q,R,S,E,H,C$;
\item refusal of quotient stream equality, host real equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, and standard bridge data as local NameCert obligations.
\end{enumerate}
No Cauchy equality criterion obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-equality-criterion-carrier}. The carrier displays the two regular names, one common schedule, one dyadic difference ledger, one zero-tail row, one criterion row, the seal rows, one sibling comparison row, exactness, transport, replay, provenance, and local naming.

Window exactness and dyadic difference exactness follow from $W$ and $D$. The packet has one common schedule and one dyadic ledger, so a consumer cannot split the two names across unrelated windows or replace the visible tolerance route.

Zero-tail exactness and criterion exhaustion follow from $Z$ and $Q$. The equality criterion is reached by applying the zero-tail row to the displayed difference route and then reading the single finite criterion row $Q$.

The rows $S,R,E,H,C,P,N$ bind the same route to the sibling comparison surface, real seal boundary, exactness, transport, replay, provenance, and local naming. Since quotient stream equality, host real equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, and bridge data are absent from the carrier, they cannot become local obligations.
\end{proof}

\begin{theorem}[Cauchy equality criterion real seal factorization]
\label{thm:cauchy-equality-criterion-real-seal-factorization}
Every $\RealUp$ seal consumer of an accepted $\CauchyEqualityCriterionUp$ packet factors through the route
$$
X,Y \longmapsto W \longmapsto D \longmapsto Z \longmapsto Q \longmapsto S \longmapsto R,E,H,C,P,N .
$$
Thus equality of the two real-name seals is exposed only as the finite common-window zero-tail criterion recorded by the packet. The consumer receives no quotient stream equality, host real equality, trichotomy decision, selected limiting real, countable-choice selector, ambient $\RealUp$ completeness theorem, Lean verification row, or standard bridge row from the criterion packet.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:cauchy-equality-criterion-carrier}. The two regular names are the only source rows, and $W$ is the only finite schedule on which they are compared.

Read the difference evidence through $D$ and $Z$. The dyadic ledger supplies the visible tolerance route, and the zero-tail handoff is the only row that converts the displayed difference into equality evidence for the criterion.

Now read $Q$ and $S$. The criterion row binds the common-window zero-tail evidence, while the sibling comparison row shows that downstream comparison consumers see the same windows, dyadic ledger, and seal-facing route.

Finally read the seal rows and structural rows $R,E,H,C,P,N$. The carrier contains no coordinate for quotient equality, host equality, trichotomy, selected limits, choice, ambient completeness, Lean verification, or a bridge, so the real-facing consumer factors exactly through the displayed finite route.
\end{proof}

\closureat{CauchyEqualityCriterionUp}{seedStr}

\begin{closurestatus}{\CauchyEqualityCriterionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-equality-criterion-carrier}, \autoref{thm:cauchy-equality-criterion-namecert-obligations}, and \autoref{thm:cauchy-equality-criterion-real-seal-factorization} seed the finite common-window zero-tail criterion over two regular Cauchy names, a StreamName schedule, a DyadicRatCore ledger, a NullSequence handoff, Real seal rows, a RegularCauchyComparison sibling row, exactness, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, host real equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or standard bridge data.}
  \upgradepath{Obligation closure requires checked carrier admission, window exactness, dyadic difference exactness, zero-tail exactness, criterion exhaustion, comparison compatibility, seal-boundary exactness, transport, replay, provenance, and local naming for BEDC.Derived.CauchyEqualityCriterionUp.}
  \constructivestory{A $\CauchyEqualityCriterionUp$ packet is generated from two $\RegSeqRatUp$ names, one common $\StreamNameUp$ schedule, one $\DyadicRatCoreUp$ difference ledger, one $\NullSequenceUp$ zero-tail row, one finite criterion row, $\RealUp$ seal rows, a $\RegularCauchyComparisonUp$ sibling row, exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
