\begin{lemma}[Directed meet associativity comparison from bounds]
\label{lem:lattice-directed-meet-associativity-comparison-from-bounds}
Let $A$ be a $\LatticeUp$ carrier with order $\le$ and classifier $\sim_C$. Assume the inherited $\PreorderUp$ transitivity field from \autoref{def:preorder-stability-certificate} and the directional meet lower-bound and greatest-lower-bound implication fields from \autoref{def:lattice-stability-certificate}. Then for all $x,y,z\in A$,
$$
(x\wedge y)\wedge z\le x\wedge(y\wedge z).
$$
\end{lemma}
\leanchecked{BEDC.Derived.LatticeUp.LatticeSingletonMeet\_assoc\_comparison\_empty}
\begin{proof}
Put
$$
w=(x\wedge y)\wedge z.
$$
The meet lower-left field gives
$$
w\le x\wedge y,
$$
and the meet lower-left field applied to $x\wedge y$ gives
$$
x\wedge y\le x.
$$
By the inherited preorder transitivity field,
$$
w\le x.
$$
The meet lower-right field applied to $x\wedge y$ gives
$$
x\wedge y\le y,
$$
so transitivity with $w\le x\wedge y$ gives
$$
w\le y.
$$
The meet lower-right field applied to $w=(x\wedge y)\wedge z$ gives
$$
w\le z.
$$
Applying meet greatestness to the two bounds $w\le y$ and $w\le z$ gives
$$
w\le y\wedge z.
$$
A second application of meet greatestness, now to $w\le x$ and $w\le y\wedge z$, gives
$$
w\le x\wedge(y\wedge z).
$$
Substituting the displayed definition of $w$ is the required comparison.
\end{proof}
\leanchecked{BEDC.Derived.LatticeUp.LatticeDirectedMeet\_associativity\_comparison\_from\_bounds}

\begin{lemma}[Directed join associativity comparison from bounds]\label{lem:lattice-directed-join-associativity-comparison-from-bounds} Let $A$ be a $\LatticeUp$ carrier with order $\le_C$ and classifier $\sim_C$. Assume the inherited $\PreorderUp$ transitivity field from \autoref{def:preorder-stability-certificate} and the directional join upper-bound and least-upper-bound implication fields from \autoref{def:lattice-stability-certificate}. Then for all $x,y,z\in A$, $(x\vee y)\vee z\le_C x\vee(y\vee z)$.
\end{lemma}
\begin{proof} Put $w=x\vee(y\vee z)$. The join upper-left field gives $x\le_C w$. The join upper-left field applied to $y\vee z$ gives $y\le_C y\vee z$, and the join upper-right field applied to $w=x\vee(y\vee z)$ gives $y\vee z\le_C w$. By the inherited preorder transitivity field, $y\le_C w$. Applying join leastness to the two bounds $x\le_C w$ and $y\le_C w$ gives $x\vee y\le_C w$. The join upper-right field applied to $y\vee z$ gives $z\le_C y\vee z$, and transitivity with $y\vee z\le_C w$ gives $z\le_C w$. A second application of join leastness, now to $x\vee y\le_C w$ and $z\le_C w$, gives $(x\vee y)\vee z\le_C w$. Substituting the definition of $w$ gives $(x\vee y)\vee z\le_C x\vee(y\vee z)$.
\end{proof}
\leanchecked{BEDC.Derived.LatticeUp.LatticeSingletonJoin\_directed\_assoc\_comparison\_from\_bounds}

\begin{lemma}[Meet absorbs a smaller ordered endpoint]
\label{lem:lattice-meet-absorbs-smaller-ordered-endpoint}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$ and classifier $\sim_C$. Assume the inherited $\PreorderUp$ reflexivity field from \autoref{def:preorder-stability-certificate}, the inherited $\POSetUp$ antisymmetry field from \autoref{def:poset-stability-certificate}, and the directional meet lower-bound and greatest-lower-bound fields from \autoref{def:lattice-stability-certificate}. If $x,z\in A$ and $x\le_C z$, then
$$
z\wedge x\sim_C x.
$$
\end{lemma}
\begin{proof}
The meet lower-right field gives
$$
z\wedge x\le_C x.
$$
The inherited preorder reflexivity field gives
$$
x\le_C x.
$$
Together with the assumed comparison $x\le_C z$, meet greatestness applied with witness endpoint $x$ and operands $z$ and $x$ gives
$$
x\le_C z\wedge x.
$$
The antisymmetry field from \autoref{def:poset-stability-certificate} applied to these two comparisons yields
$$
z\wedge x\sim_C x.
$$
\end{proof}
\leanchecked{BEDC.Derived.LatticeUp.LatticeMeet\_absorbs\_smaller\_ordered\_endpoint\_from\_bounds}

\begin{lemma}[Modular lower comparison from directional bounds]
\label{lem:lattice-modular-lower-comparison-from-directional-bounds}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$. Assume the inherited $\PreorderUp$ transitivity field from \autoref{def:preorder-stability-certificate} and the directional meet lower-bound, meet greatestness, join upper-bound, and join leastness fields from \autoref{def:lattice-stability-certificate}. If $x,y,z\in A$ and $x\le_C z$, then
$$
x\vee(y\wedge z)\le_C (x\vee y)\wedge z.
$$
\end{lemma}
\begin{proof}
Put
$$
n=x\vee(y\wedge z),\qquad m=(x\vee y)\wedge z.
$$
The join upper-left field gives
$$
x\le_C x\vee y.
$$
The meet lower-left field gives
$$
y\wedge z\le_C y,
$$
and the join upper-right field gives
$$
y\le_C x\vee y.
$$
By transitivity,
$$
y\wedge z\le_C x\vee y.
$$
Join leastness with upper target $x\vee y$ gives
$$
n\le_C x\vee y.
$$
The assumption gives $x\le_C z$, and the meet lower-right field gives
$$
y\wedge z\le_C z.
$$
Join leastness with upper target $z$ gives
$$
n\le_C z.
$$
Meet greatestness applied to the two bounds $n\le_C x\vee y$ and $n\le_C z$ gives
$$
n\le_C m.
$$
Substituting the displayed definitions of $m$ and $n$ gives the claimed comparison.
\end{proof}
\leanchecked{BEDC.Derived.LatticeUp.LatticeModular\_lower\_comparison\_from\_directional\_bounds}

\begin{theorem}[Distributivity gives the modular law]
\label{thm:lattice-distributivity-implies-modular-law}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$ and classifier $\sim_C$. Assume the classifier symmetry and transitivity fields from \autoref{def:lean-carrier-naming-certificate}, the inherited $\PreorderUp$ reflexivity and transitivity fields from \autoref{def:preorder-stability-certificate}, the inherited $\POSetUp$ antisymmetry field from \autoref{def:poset-stability-certificate}, and the directional meet and join bound-characterization fields from \autoref{def:lattice-stability-certificate}. Assume also the join classifier-congruence field from \autoref{def:lattice-classifier-specification}, and suppose the distributive law
$$
a\wedge(b\vee c)\sim_C (a\wedge b)\vee(a\wedge c)
$$
holds for all $a,b,c\in A$. If $x,y,z\in A$ and $x\le_C z$, then
$$
x\vee(y\wedge z)\le_C (x\vee y)\wedge z
\qquad\text{and}\qquad
x\vee(y\wedge z)\sim_C (x\vee y)\wedge z.
$$
\end{theorem}
\begin{proof}
The first conclusion is \autoref{lem:lattice-modular-lower-comparison-from-directional-bounds}.

Put
$$
m=(x\vee y)\wedge z,
\qquad
n=x\vee(y\wedge z).
$$
By \autoref{thm:lattice-commutativity-from-directional-bounds}, applied to the meet operands $x\vee y$ and $z$,
$$
m\sim_C z\wedge(x\vee y).
$$
The assumed distributive law with $a=z$, $b=x$, and $c=y$ gives
$$
z\wedge(x\vee y)\sim_C (z\wedge x)\vee(z\wedge y).
$$
By \autoref{lem:lattice-meet-absorbs-smaller-ordered-endpoint} and the assumption $x\le_C z$,
$$
z\wedge x\sim_C x.
$$
By \autoref{thm:lattice-commutativity-from-directional-bounds}, applied to the meet operands $z$ and $y$,
$$
z\wedge y\sim_C y\wedge z.
$$
Join classifier congruence applied to the last two classifier comparisons gives
$$
(z\wedge x)\vee(z\wedge y)\sim_C x\vee(y\wedge z).
$$
Classifier transitivity composes the three displayed classifier comparisons into
$$
m\sim_C n.
$$
Classifier symmetry reverses this comparison, giving
$$
n\sim_C m.
$$
Substituting the displayed definitions of $m$ and $n$ gives
$$
x\vee(y\wedge z)\sim_C (x\vee y)\wedge z.
$$
\end{proof}

\begin{lemma}[Join-meet lower comparison from directional bounds]
\label{lem:lattice-join-meet-lower-comparison-from-directional-bounds}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$. Assume the inherited $\PreorderUp$ transitivity field from \autoref{def:preorder-stability-certificate} and the directional meet lower-bound, meet greatestness, join upper-bound, and join leastness fields from \autoref{def:lattice-stability-certificate}. Then for all $a,b,c\in A$,
$$
a\vee(b\wedge c)\le_C (a\vee b)\wedge(a\vee c).
$$
\end{lemma}
\leanchecked{BEDC.Derived.LatticeUp.LatticeSingletonJoinMeet\_lower\_comparison\_empty}
\begin{proof}
Put
$$
n=a\vee(b\wedge c),
\qquad
m=(a\vee b)\wedge(a\vee c).
$$
The join upper-left field gives
$$
a\le_C a\vee b.
$$
The meet lower-left field gives
$$
b\wedge c\le_C b,
$$
and the join upper-right field gives
$$
b\le_C a\vee b.
$$
By transitivity,
$$
b\wedge c\le_C a\vee b.
$$
Join leastness with upper target $a\vee b$ gives
$$
n\le_C a\vee b.
$$
Similarly, the join upper-left field gives
$$
a\le_C a\vee c.
$$
The meet lower-right field gives
$$
b\wedge c\le_C c,
$$
and the join upper-right field gives
$$
c\le_C a\vee c.
$$
By transitivity,
$$
b\wedge c\le_C a\vee c.
$$
Join leastness with upper target $a\vee c$ gives
$$
n\le_C a\vee c.
$$
Meet greatestness applied to the two bounds $n\le_C a\vee b$ and $n\le_C a\vee c$ gives
$$
n\le_C m.
$$
Substituting the displayed definitions of $m$ and $n$ gives the claimed comparison.
\end{proof}

\begin{theorem}[Meet distributivity gives join distributivity]
\label{thm:lattice-meet-distributivity-gives-join-distributivity}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$ and classifier $\sim_C$. Assume the classifier symmetry and transitivity fields from \autoref{def:lean-carrier-naming-certificate}, the inherited $\PreorderUp$ reflexivity and transitivity fields from \autoref{def:preorder-stability-certificate}, the inherited $\POSetUp$ antisymmetry field from \autoref{def:poset-stability-certificate}, and the directional meet and join bound-characterization fields from \autoref{def:lattice-stability-certificate}. Assume also the meet and join classifier-congruence rows from \autoref{def:lattice-classifier-specification}, and suppose the distributive law
$$
u\wedge(v\vee w)\sim_C (u\wedge v)\vee(u\wedge w)
$$
holds for all $u,v,w\in A$. Then for all $a,b,c\in A$,
$$
a\vee(b\wedge c)\le_C (a\vee b)\wedge(a\vee c)
\qquad\text{and}\qquad
a\vee(b\wedge c)\sim_C (a\vee b)\wedge(a\vee c).
$$
\end{theorem}
\begin{proof}
The first conclusion is \autoref{lem:lattice-join-meet-lower-comparison-from-directional-bounds}.

Put
$$
d=(a\vee b)\wedge(a\vee c),
\qquad
n=a\vee(b\wedge c).
$$
The assumed distributive law with $u=a\vee b$, $v=a$, and $w=c$ gives
$$
d\sim_C ((a\vee b)\wedge a)\vee((a\vee b)\wedge c).
$$
By \autoref{thm:lattice-opposite-absorption-from-directional-bounds}, applied with $x=a$ and $y=b$,
$$
(a\vee b)\wedge a\sim_C a.
$$
By \autoref{thm:lattice-commutativity-from-directional-bounds}, applied to the meet operands $a\vee b$ and $c$,
$$
(a\vee b)\wedge c\sim_C c\wedge(a\vee b).
$$
The assumed distributive law with $u=c$, $v=a$, and $w=b$ gives
$$
c\wedge(a\vee b)\sim_C (c\wedge a)\vee(c\wedge b).
$$
Classifier transitivity composes the last two comparisons into
$$
(a\vee b)\wedge c\sim_C (c\wedge a)\vee(c\wedge b).
$$
Join classifier congruence gives
$$
((a\vee b)\wedge a)\vee((a\vee b)\wedge c)
\sim_C
 a\vee((c\wedge a)\vee(c\wedge b)).
$$
Put
$$
p=a\vee((c\wedge a)\vee(c\wedge b)),
\qquad
q=a\vee(c\wedge b).
$$
To compare $p$ and $q$, first note that the join upper-left field gives $a\le_C q$. The meet lower-right field gives $c\wedge a\le_C a$, so transitivity gives $c\wedge a\le_C q$. The join upper-right field gives $c\wedge b\le_C q$. Join leastness gives
$$
(c\wedge a)\vee(c\wedge b)\le_C q,
$$
and another application of join leastness gives
$$
p\le_C q.
$$
Conversely, the join upper-left field gives $a\le_C p$. The join upper-right field gives
$$
(c\wedge a)\vee(c\wedge b)\le_C p,
$$
and the join upper-right field applied to the inner join gives
$$
c\wedge b\le_C (c\wedge a)\vee(c\wedge b).
$$
By transitivity, $c\wedge b\le_C p$. Join leastness gives
$$
q\le_C p.
$$
The antisymmetry field from \autoref{def:poset-stability-certificate} applied to $p\le_C q$ and $q\le_C p$ yields
$$
p\sim_C q.
$$
By \autoref{lem:lattice-join-absorption-from-bounds}, applied with $x=a$ and $y=a$,
$$
a\vee(a\wedge a)\sim_C a.
$$
Classifier symmetry and transitivity applied to this comparison give
$$
a\sim_C a.
$$
By \autoref{thm:lattice-commutativity-from-directional-bounds}, applied to the meet operands $c$ and $b$,
$$
c\wedge b\sim_C b\wedge c.
$$
Join classifier congruence gives
$$
q\sim_C n.
$$
Classifier transitivity composes the displayed classifier comparisons into
$$
d\sim_C n.
$$
Classifier symmetry reverses this comparison, giving
$$
n\sim_C d.
$$
Substituting the displayed definitions of $d$ and $n$ gives
$$
a\vee(b\wedge c)\sim_C (a\vee b)\wedge(a\vee c).
$$
\end{proof}

\begin{lemma}[Lattice meet two-sided monotonicity from directional bounds]
\label{lem:lattice-meet-two-sided-monotonicity-from-directional-bounds}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$ and classifier $\sim_C$. Assume the inherited $\PreorderUp$ transitivity field from \autoref{def:preorder-stability-certificate} and the directional meet lower-bound and greatest-lower-bound fields from \autoref{def:lattice-stability-certificate}. If $a,a',b,b'\in A$ with $a\le_C a'$ and $b\le_C b'$, then
$$
a\wedge b\le_C a'\wedge b'.
$$
\end{lemma}
\leanchecked{BEDC.Derived.LatticeUp.LatticeSingletonMeet\_monotone\_empty}
\begin{proof}
Put
$$
w=a\wedge b.
$$
The meet lower-left field gives
$$
w\le_C a.
$$
Composing this comparison with the assumed comparison $a\le_C a'$ by the inherited preorder transitivity field gives
$$
w\le_C a'.
$$
The meet lower-right field gives
$$
w\le_C b.
$$
Composing this comparison with the assumed comparison $b\le_C b'$ gives
$$
w\le_C b'.
$$
Applying meet greatestness to the two bounds $w\le_C a'$ and $w\le_C b'$ gives
$$
w\le_C a'\wedge b'.
$$
Substituting the displayed definition of $w$ gives the claimed comparison.
\end{proof}

\begin{lemma}[Join is monotone in both ordered endpoints]
\label{lem:lattice-join-two-sided-monotonicity-from-directional-bounds}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$. Assume the inherited $\PreorderUp$ transitivity field from \autoref{def:preorder-stability-certificate} and the directional join upper-bound and least-upper-bound fields from \autoref{def:lattice-stability-certificate}. If $a,b,a',b'\in A$ with $a\le_C a'$ and $b\le_C b'$, then
$$
a\vee b\le_C a'\vee b'.
$$
\end{lemma}
\begin{proof}
Put
$$
u=a'\vee b'.
$$
The join upper-left field gives
$$
a'\le_C u.
$$
Composing the assumed comparison $a\le_C a'$ with $a'\le_C u$ by the inherited preorder transitivity field gives
$$
a\le_C u.
$$
The join upper-right field gives
$$
b'\le_C u.
$$
Composing the assumed comparison $b\le_C b'$ with $b'\le_C u$ by the same transitivity field gives
$$
b\le_C u.
$$
Applying join leastness to the two upper-bound comparisons $a\le_C u$ and $b\le_C u$ gives
$$
a\vee b\le_C u.
$$
Substituting the displayed definition of $u$ gives the claimed comparison.
\end{proof}
\leanchecked{BEDC.Derived.LatticeUp.LatticeJoin\_two\_sided\_monotonicity\_from\_directional\_bounds}

\begin{lemma}[Lattice meet monotonicity in the second argument from directional bounds]
\label{lem:lattice-meet-monotonicity-second-argument-from-directional-bounds}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$ and classifier $\sim_C$. Assume the inherited $\PreorderUp$ reflexivity and transitivity fields from \autoref{def:preorder-stability-certificate} and the directional meet lower-bound and greatest-lower-bound fields from \autoref{def:lattice-stability-certificate}. If $b,b',c\in A$ and $b\le_C b'$, then
$$
c\wedge b\le_C c\wedge b'.
$$
\end{lemma}
\begin{proof}
Put
$$
w=c\wedge b.
$$
The meet lower-left field gives
$$
w\le_C c.
$$
The meet lower-right field gives
$$
w\le_C b.
$$
Composing this comparison with the assumed comparison $b\le_C b'$ by the inherited preorder transitivity field gives
$$
w\le_C b'.
$$
Applying meet greatestness to the two bounds $w\le_C c$ and $w\le_C b'$ gives
$$
w\le_C c\wedge b'.
$$
Substituting the displayed definition of $w$ gives the claimed comparison.
\end{proof}
\leanchecked{BEDC.Derived.LatticeUp.LatticeMeet\_monotone\_second\_argument\_from\_directional\_bounds}

\begin{theorem}[Lattice operation monotonicity from directional bounds]
\label{thm:lattice-operation-monotonicity-from-directional-bounds}
Let $A$ be a $\LatticeUp$ carrier with order $\le_C$ and classifier $\sim_C$. Assume the inherited $\PreorderUp$ reflexivity and transitivity fields from \autoref{def:preorder-stability-certificate} and the directional meet and join bound-characterization fields from \autoref{def:lattice-stability-certificate}. If $a,a',b,b',c\in A$ with $a\le_C a'$ and $b\le_C b'$, then
$$
a\wedge c\le_C a'\wedge c,
\qquad
c\wedge a\le_C c\wedge a',
$$
and
$$
b\vee c\le_C b'\vee c,
\qquad
c\vee b\le_C c\vee b'.
$$
\end{theorem}
\begin{proof}
The inherited preorder reflexivity field gives
$$
c\le_C c.
$$
Apply \autoref{lem:lattice-meet-two-sided-monotonicity-from-directional-bounds} to the comparisons $a\le_C a'$ and $c\le_C c$. This gives
$$
a\wedge c\le_C a'\wedge c.
$$
Apply the same meet monotonicity lemma to $c\le_C c$ and $a\le_C a'$. This gives
$$
c\wedge a\le_C c\wedge a'.
$$
Apply \autoref{lem:lattice-join-two-sided-monotonicity-from-directional-bounds} to $b\le_C b'$ and $c\le_C c$. This gives
$$
b\vee c\le_C b'\vee c.
$$
Apply the same join monotonicity lemma to $c\le_C c$ and $b\le_C b'$. This gives
$$
c\vee b\le_C c\vee b'.
$$
\end{proof}
\leanchecked{BEDC.Derived.LatticeUp.LatticeSingletonOperation\_monotonicity\_empty}
