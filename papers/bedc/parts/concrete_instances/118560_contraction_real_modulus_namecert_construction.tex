\chapter{A Concrete Naming Certificate for $\ContractionRealModulusUp$}
\label{ch:concrete-instances-contraction-real-modulus-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Banach's contraction principle, the $\ContractionRealModulusUp$ packet records the finite modulus data by which a displayed contraction on a located interval or complete metric row yields a Cauchy route toward a real-name seal. It sits between the interval contraction surface of \autoref{ch:concrete-instances-bishop-interval-contraction-namecert}, the fixed-point modulus surface of \autoref{ch:concrete-instances-banach-fixed-point-modulus-namecert}, the theorem-level contraction route of \autoref{ch:concrete-instances-contraction-mapping-theorem-namecert}, and the real-completion rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet is a finite contraction-modulus carrier; it is not an ambient completeness theorem, a selected fixed point, quotient equality of real names, countable choice, or trichotomy.

\begin{definition}[Contraction real modulus carrier]\label{def:contraction-real-modulus-carrier}
A $\ContractionRealModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(L,X,F,Q,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the source located interval, located metric interval, or complete metric row. It gives the domain on which the contraction is read, using only the displayed $\LocatedIntervalUp$, $\MetricUp$, or $\CompleteMetricUp$ surface supplied by the surrounding chapter.
\item $X$ is the initial point or initial interval row. It names the finite source datum from which the contraction iteration is inspected.
\item $F$ is the contraction self-map row. It records the displayed action of the map on the source row and the finite Lipschitz witness $\lambda$ with the contraction comparison $\lambda < 1$ available only as a row of the packet.
\item $Q$ is the dyadic shrinkage ledger. It records the finite sequence of dyadic radii, slack inequalities, and comparison budgets used to read the contraction decrease.
\item $W$ is the $\StreamNameUp$ finite-window row for the inspected iterates.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same windows and dyadic budgets.
\item $E$ is the $\RealUp$ seal row. It is available only after the source row, self-map row, dyadic shrinkage ledger, finite windows, and regular rational readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source row, initial row, self-map, shrinkage ledger, stream windows, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a contraction-modulus request through $L,X,F,Q,W,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\ContractionRealModulusUp$, $\BishopIntervalContractionUp$, $\BanachFixedPointModulusUp$, $\ContractionMappingTheoremUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ContractionRealModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,X,F,Q,W,R,E,H,C,P,N$. It has no coordinate for a chosen fixed point, quotient equality of streams or real names, countable choice, trichotomy, or ambient completeness outside the displayed source row.
\end{definition}

\begin{theorem}[Contraction real modulus NameCert obligations]\label{thm:contraction-real-modulus-namecert-obligations}
For every accepted $\ContractionRealModulusUp$ carrier $M=(L,X,F,Q,W,R,E,H,C,P,N)$, the local $\NameCert_{\ContractionRealModulusUp}$ surface consists exactly of carrier admission for the source located or complete metric row, the initial datum, the contraction self-map, the dyadic shrinkage ledger, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The obligation surface may cite the interval-contraction source of \autoref{ch:concrete-instances-bishop-interval-contraction-namecert}, the fixed-point modulus row of \autoref{ch:concrete-instances-banach-fixed-point-modulus-namecert}, the contraction theorem route of \autoref{ch:concrete-instances-contraction-mapping-theorem-namecert}, and the real-name dependencies of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. It cannot add a selected fixed point, quotient stream equality, countable choice, trichotomy, host real equality, or a detached completeness principle.
\end{theorem}
\begin{proof}
Project the accepted carrier. The analytic coordinates are exactly the source row $L$, initial row $X$, contraction self-map $F$, dyadic shrinkage ledger $Q$, finite stream windows $W$, regular rational readback $R$, and terminal Real seal $E$.

The NameCert obligations follow the displayed order. The self-map row cannot be read without $L$ and $X$, the shrinkage ledger cannot be consumed without $F$, and the real-name handoff cannot inspect $E$ before $Q,W,R$ have all been displayed.

Transport and replay are structural. The row $H$ moves every listed coordinate componentwise, while $C$ repeats the same finite route for later contraction or fixed-point consumers.

Finally $P$ and $N$ source and name only that finite packet. Since the excluded objects are not carrier coordinates and are not produced by componentwise transport or replay, no local NameCert obligation exports them.
\end{proof}

\begin{theorem}[Contraction real modulus Cauchy Real handoff]\label{thm:contraction-real-modulus-cauchy-real-handoff}
Every accepted $\ContractionRealModulusUp$ carrier $M=(L,X,F,Q,W,R,E,H,C,P,N)$ gives any Cauchy or Real-facing consumer the finite route
$$
L,X \longrightarrow F \longrightarrow Q \longrightarrow W \longrightarrow R \longrightarrow E
$$
with structural rows $H,C,P,N$ attached. Thus the consumer receives a contraction-modulus route through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before it can invoke the Banach fixed-point or contraction-mapping surface; it receives no selected fixed point, quotient stream, countable-choice schedule, trichotomy row, host real equality, or ambient completeness row outside the displayed carrier.
\end{theorem}
\begin{proof}
Start with the carrier projection of \autoref{def:contraction-real-modulus-carrier}. The only route from the source row to a Real-facing read is through the contraction self-map, dyadic shrinkage ledger, finite stream windows, regular rational readback, and Real seal.

The dyadic ledger $Q$ controls the decrease row before the windows are opened. Once $W$ is fixed, the regular rational row $R$ reads the inspected iterates with the same budgets, and $E$ seals only that displayed route.

Use the structural rows $H,C,P,N$ to transport, replay, source, and locally name the same path. Therefore downstream uses by $\BanachFixedPointModulusUp$, $\BanachFixedPointConstructiveUp$, or $\ContractionMappingTheoremUp$ can cite only the finite contraction-modulus handoff and not any excluded ambient principle.
\end{proof}

\closureat{ContractionRealModulusUp}{seedStr}

\begin{closurestatus}{\ContractionRealModulusUp}
  \constructivestory{A $\ContractionRealModulusUp$ packet is generated from a displayed source row, initial datum, contraction self-map, dyadic shrinkage ledger, finite StreamName windows, RegSeqRat readback, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:contraction-real-modulus-carrier}, \autoref{thm:contraction-real-modulus-namecert-obligations}, and \autoref{thm:contraction-real-modulus-cauchy-real-handoff} seed the finite contraction-modulus carrier over displayed source, self-map, dyadic shrinkage, StreamName, RegSeqRat, Real seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected fixed points, quotient stream equality, countable choice, trichotomy, host real equality, ambient completeness beyond the displayed source row, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction self-map exactness, dyadic shrinkage accounting, StreamName window readback, RegSeqRat handoff, Real-seal routing, componentwise transport, displayed replay, provenance, and local naming for BEDC.Derived.ContractionRealModulusUp.}
\end{closurestatus}
