\chapter{A Concrete Naming Certificate for $\BesicovitchDifferentiationWindowUp$}
\label{ch:concrete-instances-besicovitch-differentiation-window-namecert}
\origin{human}

Following Besicovitch covering theory and Lebesgue differentiation, the $\BesicovitchDifferentiationWindowUp$ packet records the finite window where bounded-overlap cover data, local density estimates, measure rows, and Real seals are read together before a differentiation consumer sees a verdict. It sits after the Vitali selection surface of \autoref{ch:concrete-instances-vitali-cover-namecert}, the bounded-overlap covering packet of \autoref{ch:concrete-instances-besicovitch-covering-namecert}, the differentiation certificate of \autoref{ch:concrete-instances-lebesgue-differentiation-namecert}, the measure interface of \autoref{ch:concrete-instances-measure-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names one finite density window only; it is not a host differentiation theorem, a measure-completion principle, a selected almost-everywhere representative, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Besicovitch differentiation window carrier]
\label{def:besicovitch-differentiation-window-carrier}
A $\BesicovitchDifferentiationWindowUp$ carrier is a finite $\BHist$ packet
$$
W=(B,F,O,D,M,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite cover family row, exposing displayed balls or intervals together with their centers, radii, and request cells.
\item $F$ is the $\BesicovitchCoveringUp$ bounded-overlap ledger for the same finite family.
\item $O$ is the density-window row, recording the inspected center, radius window, local-average row, exceptional-budget row, and finite nonescape condition.
\item $D$ is the $\LebesgueDifferentiationUp$ handoff row whose local differentiation read is allowed only after $B,F,O$ are displayed.
\item $M$ is the $\MeasureUp$ row for local measure, measurable-cell admission, and finite density comparison.
\item $R$ is the terminal $\RealUp$ seal row for density values, local averages, overlap budgets, and error bounds.
\item $H$ records componentwise $\hsame$ transport for cover family, bounded-overlap ledger, density window, differentiation handoff, measure rows, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from finite cover family through bounded overlap, density-window comparison, measure read, differentiation handoff, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\BesicovitchDifferentiationWindowUp$, $\VitaliCoverUp$, $\BesicovitchCoveringUp$, $\LebesgueDifferentiationUp$, $\MeasureUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BesicovitchDifferentiationWindowUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,F,O,D,M,R,H,C,P,N$. It has no coordinate for a completed measure algebra, selected countable cover, host differentiation theorem, almost-everywhere quotient, chosen function representative, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Besicovitch differentiation window NameCert obligations]
\label{thm:besicovitch-differentiation-window-namecert-obligations}
For every accepted $\BesicovitchDifferentiationWindowUp$ carrier
$$
W=(B,F,O,D,M,R,H,C,P,N),
$$
the local $\NameCert_{\BesicovitchDifferentiationWindowUp}$ surface consists exactly of finite cover-family admission through $B$, bounded-overlap exposure through $F$, density-window exposure through $O$, differentiation handoff through $D$, local measure admission through $M$, Real sealing through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:besicovitch-differentiation-window-carrier}. The rows $B,F,O,D,M,R,H,C,P,N$ are the whole displayed packet.

The covering rows are read first. The finite cover family $B$ exposes only displayed balls or intervals, and the bounded-overlap ledger $F$ routes the same family through the $\BesicovitchCoveringUp$ surface named by \autoref{ch:concrete-instances-besicovitch-covering-namecert}.

The density rows are then attached to the same finite family. The window row $O$ records the inspected center, radius, average, and exceptional budget, while $M$ supplies the local measure row and $R$ seals the resulting real-valued comparisons.

The differentiation handoff $D$ is downstream of those displayed rows, and $H,C,P,N$ transport, replay, source, and name the same route. Since no excluded host theorem, measure completion, countable selector, quotient, selected representative, or ambient-completeness coordinate is present, the listed rows exhaust the local obligation surface.
\end{proof}

\begin{theorem}[Besicovitch differentiation window density handoff]
\label{thm:besicovitch-differentiation-window-density-handoff}
Every density read accepted by a $\BesicovitchDifferentiationWindowUp$ packet factors through the finite route
$$
\VitaliCoverUp,\ \BesicovitchCoveringUp,\ B,\ F,\ O,\ \MeasureUp,\ \RealUp
\longmapsto
\LebesgueDifferentiationUp .
$$
The handoff exports finite cover cells, bounded overlap, one density window, local measure comparisons, Real seals, transport, replay, provenance, and local naming only.
\end{theorem}
\begin{proof}
Start with an accepted carrier $W=(B,F,O,D,M,R,H,C,P,N)$. A density-facing consumer can inspect cover cells only through the displayed finite family $B$.

The covering comparison is forced through $F$. This row cites the bounded-overlap discipline of $\BesicovitchCoveringUp$ and keeps every overlap count attached to the same finite family.

The density comparison is forced through $O,M,R$. These rows expose the inspected window, local measure values, local averages, exceptional budget, and Real seal without choosing null-set representatives or completing the measure algebra.

Only after those rows are displayed may the consumer read $D$ as a $\LebesgueDifferentiationUp$ handoff. The structural rows $H,C,P,N$ preserve the route, so the packet exports a finite density-window handoff rather than an ambient differentiation theorem.
\end{proof}

\falsifiablePrediction{A $\BesicovitchDifferentiationWindowUp$ consumer is refuted whenever a density verdict is read without displaying a finite cover family, bounded-overlap ledger, density-window row, $\MeasureUp$ local measure row, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.}

\independenceWitness{vitali-cover, besicovitch-covering, lebesgue-differentiation, measure, real: $\BesicovitchDifferentiationWindowUp$ is not bijective to any listed sibling because it jointly carries finite cover cells, bounded overlap, one density window, local measure comparison, Real sealing, differentiation handoff, transport, replay, provenance, and local naming in one packet.}

\closureat{BesicovitchDifferentiationWindowUp}{seedStr}

\begin{closurestatus}{\BesicovitchDifferentiationWindowUp}
  \constructivestory{A $\BesicovitchDifferentiationWindowUp$ packet is generated from finite cover family rows, BesicovitchCovering bounded-overlap ledgers, density-window rows, LebesgueDifferentiation handoff rows, Measure rows, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:besicovitch-differentiation-window-carrier}, \autoref{thm:besicovitch-differentiation-window-namecert-obligations}, and \autoref{thm:besicovitch-differentiation-window-density-handoff} seed the finite density-window route for Besicovitch covering and Lebesgue differentiation consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close the full Besicovitch differentiation theorem, exact Euclidean multiplicity constants, measure completion, almost-everywhere quotient equality, countable-choice radius selection, selected function representatives, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked finite cover-family admission, bounded-overlap exactness, density-window exposure, Measure row admission, Real seal nonescape, differentiation handoff, transport, replay, provenance, and local naming for BEDC.Derived.BesicovitchDifferentiationWindowUp.}
\end{closurestatus}
