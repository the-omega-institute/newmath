\chapter{A Concrete Naming Certificate for $\LocatedRealCauchyBoundaryUp$}
\label{ch:concrete-instances-located-real-cauchy-boundary-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\LocatedRealCauchyBoundaryUp$ packet records the finite boundary at which a located Cauchy presentation can be inspected before a $\RealUp$ readback is sealed. It sits beside the stream-name source of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the located completion embedding of \autoref{ch:concrete-instances-bishop-located-completion-embedding-namecert}, and the finite-prefix stream certificate of \autoref{ch:concrete-instances-finite-prefix-stream-certificate-namecert}. The packet names a boundary object for displayed finite means; it is not quotient stream equality, a selected limit, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[LocatedRealCauchyBoundary carrier]
\label{def:located-real-cauchy-boundary-carrier}
A $\LocatedRealCauchyBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(\rho,S,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $\rho$ is the located-radius row, naming the dyadic tolerance radius and the finite comparison window used for the boundary read;
\item $S$ is the $\StreamNameUp$ row naming the finite stream window that supplies the observed approximants;
\item $R$ is the $\RegSeqRatUp$ row carrying the regular rational readback of the displayed stream window;
\item $D$ is the $\DyadicRatCoreUp$ row that records the dyadic arithmetic, radius comparison, and tolerance ledger;
\item $E$ is the $\RealUp$ seal row reached only after $S$, $R$, and $D$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for the located-radius, stream, regular rational, dyadic, real-seal, package, and local naming rows;
\item $C$ records the displayed $\Cont$ routes for finite-prefix inspection, located-radius tightening, regular-rational readback, dyadic comparison, and Real-facing seal handoff;
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, \autoref{ch:concrete-instances-bishop-located-completion-embedding-namecert}, \autoref{ch:concrete-instances-finite-prefix-stream-certificate-namecert}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\LocatedRealCauchyBoundaryUp}$ row.
\end{enumerate}
\end{definition}

\begin{theorem}[LocatedRealCauchyBoundary NameCert obligations]
\label{thm:located-real-cauchy-boundary-namecert-obligations}
For every accepted $\LocatedRealCauchyBoundaryUp$ carrier $B=(\rho,S,R,D,E,H,C,P,N)$, the local $\NameCert_{\LocatedRealCauchyBoundaryUp}$ surface consists exactly of carrier admission for the displayed rows, located-radius exposure through $\rho$, finite StreamName inspection through $S$, regular rational readback through $R$, dyadic tolerance accounting through $D$, Real seal readback through $E$, componentwise transport through $H$, displayed route replay through $C$, package provenance through $P$, and local naming through $N$. It exports no quotient stream, selected limit, countable choice, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-real-cauchy-boundary-carrier}. The only rows available to a consumer are $\rho,S,R,D,E,H,C,P,N$, so carrier admission is finite packet admission rather than admission to a completed ambient real line.

Read the Cauchy-facing evidence next. The stream source is named by $S$, regular rational readback by $R$, and dyadic tolerance comparison by $D$. The located-radius row $\rho$ binds the finite comparison window before the $\RealUp$ seal row $E$ can be inspected.

Finally use the structural rows. The transport row $H$ preserves the same displayed coordinates, the continuation row $C$ replays finite-prefix and seal handoff routes, and $P,N$ package and name the packet. Since no row stores quotient stream equality, selected limits, countable choice, host equality, or ambient completeness, the local NameCert surface is exactly the listed finite boundary.
\end{proof}

\begin{theorem}[LocatedRealCauchyBoundary Real handoff]
\label{thm:located-real-cauchy-boundary-real-handoff}
Every Real-facing read of an accepted $\LocatedRealCauchyBoundaryUp$ carrier must pass through the located-radius row $\rho$, the $\StreamNameUp$ row $S$, the $\RegSeqRatUp$ row $R$, the $\DyadicRatCoreUp$ tolerance row $D$, the $\RealUp$ seal row $E$, and the displayed structural rows $H,C,P,N$. The handoff may cite the located completion embedding and finite-prefix stream certificate, but it cannot replace the boundary by a quotient stream, a selected limit, a hidden representative, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the finite-prefix side. The row $S$ is the only stream-name coordinate, and the finite-prefix stream certificate supplies the discipline that a consumer reads displayed windows rather than an infinite stream quotient.

Next pass through the regular and dyadic rows. The row $R$ carries the regular rational readback, while $D$ records the dyadic tolerance comparison against the located-radius row $\rho$. The Bishop located completion embedding can therefore be cited only through those displayed rows.

The Real-facing seal is last. The row $E$ is reached through the continuation row $C$ after the preceding finite evidence has been exposed, and $H,P,N$ preserve, package, and name the same packet. Thus the handoff exports a finite Real-readback boundary, not a selected completed point or an ambient completeness theorem.
\end{proof}

\closureat{LocatedRealCauchyBoundaryUp}{seedStr}

\begin{closurestatus}{\LocatedRealCauchyBoundaryUp}
  \constructivestory{A $\LocatedRealCauchyBoundaryUp$ packet is generated from a located-radius row, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance accounting, a terminal $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-real-cauchy-boundary-carrier}, \autoref{thm:located-real-cauchy-boundary-namecert-obligations}, and \autoref{thm:located-real-cauchy-boundary-real-handoff} seed the finite located Cauchy boundary and its Real-facing handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient Cauchy streams, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-radius exposure, finite-prefix inspection, regular rational readback, dyadic tolerance accounting, Real seal handoff, and non-escape for the displayed packet.}
\end{closurestatus}
