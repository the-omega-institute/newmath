\chapter{A Concrete Naming Certificate for $\ApophaticFixedPointFiberUp$}
\label{ch:concrete-instances-apophatic-fixed-point-fiber-namecert}
\origin{ai}

The $\ApophaticFixedPointFiberUp$ packet realizes the apophatic fixed-point and local-inscription reading of \autoref{ch:visions-apophatic-fixed-point-and-inscription} as a finite NameCert surface. It consumes visible rows already separated by \autoref{ch:concrete-instances-externalitygate-namecert}, \autoref{ch:concrete-instances-supplysocketledger-namecert}, \autoref{ch:concrete-instances-generatorfixedpoint-namecert}, and \autoref{ch:concrete-instances-hash-namecert}: the gate row refuses hidden supply, the socket ledger names the refused supply kind, the generator fixed-point row controls displayed fixed-output reads, and the hash row supplies digest-facing public readback. The new carrier is the fiber that forces those rows to be read in the apophatic order rather than as a direct digest-to-far-end identification.

\begin{definition}[Apophatic fixed-point fiber carrier]
\label{def:apophatic-fixed-point-fiber-carrier}
An $\ApophaticFixedPointFiberUp$ carrier is a finite $\BHist$ packet
$$
F=(D,S,G,T,I,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the visible digest and socket readback row, carrying the public digest-facing observation together with the socket handle through which the packet is read;
\item $S$ is the external-supply socket row supplied by the finite supply-ledger discipline;
\item $G$ is the $\mathsf{GAP}$ fiber and provenance row, recording that every accepted public read passes through a boundary-fiber witness before it reaches any apophatic far-end name;
\item $T$ is the apophatic far-end boundary-name row, naming the fixed far end only as boundary data and not as a kernel object;
\item $I$ is the local inscription row, recording the chain-local site at which the boundary name is read inscription-side;
\item $H$ records componentwise $\hsame$ transport for $D,S,G,T,I,P,N$ and for the local packet shape;
\item $C$ records the displayed $\Cont$ routes by which consumers read the digest, socket, GAP fiber, far-end boundary, inscription, provenance, and naming rows in order;
\item $P$ records $\Pkg$ provenance over $\BHist$, digest rows, externality gates, supply sockets, generator fixed-point witnesses, hash readback, $\mathsf{GAP}$ fibers, local inscriptions, $\hsame$, $\Cont$, and the local naming surface;
\item $N$ is the local $\NameCert_{\ApophaticFixedPointFiberUp}$ row.
\end{enumerate}
The carrier contains no row identifying $D$ directly with $T$, no internal object for $\mathsf{T}$, and no hidden route from a public digest to an apophatic far end.
\end{definition}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiberTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Apophatic fixed-point fiber NameCert obligations]
\label{thm:apophatic-fixed-point-fiber-namecert-obligations}
For the carrier of \autoref{def:apophatic-fixed-point-fiber-carrier}, the local $\NameCert_{\ApophaticFixedPointFiberUp}$ surface consists exactly of carrier admission for
$$
F=(D,S,G,T,I,H,C,P,N),
$$
digest-and-socket readback through $D$, external-supply exposure through $S$, GAP-fiber routing through $G$, boundary-name exposure through $T$, local-inscription exposure through $I$, componentwise $\hsame$ transport through $H$, displayed continuation routing through $C$, package provenance through $P$, local naming through $N$, and refusal of any direct digest-to-$T$ route that omits the GAP fiber row.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiber\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from \autoref{def:apophatic-fixed-point-fiber-carrier}. The packet displays the digest, socket, GAP fiber, far-end boundary name, local inscription, transport, route, provenance, and local certificate rows as its complete inventory.

The readback obligations are ordered by the same inventory. A consumer first reads the digest and socket row $D$, then the external-supply socket row $S$, then the GAP fiber row $G$ before the boundary-name row $T$ and the inscription row $I$ become available.

The transport, continuation, provenance, and naming rows preserve that order. Since the carrier has no coordinate for a direct digest-to-$T$ identification, a route that bypasses $G$ has no NameCert row to project and is rejected by the packet.
\end{proof}

\begin{theorem}[Apophatic fixed-point fiber non-escape]
\label{thm:apophatic-fixed-point-fiber-nonescape}
Every accepted $\ApophaticFixedPointFiberUp$ public read reaches the GAP fiber row $G$ before it reaches the apophatic far-end boundary-name row $T$. A read that travels from the digest/socket row $D$ to $T$ without passing through $G$ is outside the local certificate and refutes the packet.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiberCarrier\_gap\_first\_route\_nonescape}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiber\_nonescape}
\begin{proof}
Start from the finite carrier inventory. The only displayed path from public digest readback to the apophatic far-end boundary is the route family $C$ applied to the rows $D,S,G,T,I$.

Apply the NameCert obligations. They make GAP-fiber routing a certificate row, not a prose convention, so any accepted read of $T$ must preserve the intervening $G$ coordinate and its provenance.

A direct route from $D$ to $T$ would require a carrier coordinate that the tuple does not contain. The local naming row $N$ therefore has no certificate surface for such a route, and the attempted read is rejected rather than exported as an apophatic fixed-point fiber.
\end{proof}

\begin{theorem}[Apophatic fixed-point fiber sibling independence]
\label{thm:apophatic-fixed-point-fiber-independence}
$\ApophaticFixedPointFiberUp$ is not bijective to the nearest sibling packets $\ExternalityGateUp$, $\SupplySocketLedgerUp$, $\GeneratorFixedPointUp$, or $\HashUp$. Each sibling omits at least one row required by the fiber: digest/socket readback, external-supply socket, GAP fiber, apophatic far-end boundary name, or local inscription. Conversely, the fiber does not replace those siblings; it binds their rows into a single ordered NameCert packet with the GAP fiber between the public digest and the apophatic far end.
\end{theorem}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiber\_independence\_witness}
\begin{proof}
Compare the carrier rows. $\ExternalityGateUp$ supplies a gate and refusal surface but lacks the digest/socket readback, generator fixed-point witness, hash digest, and local inscription rows required here.

$\SupplySocketLedgerUp$ supplies the socket ledger but lacks the digest row, GAP-fiber order row, far-end boundary-name row, and local inscription row as one packet. $\GeneratorFixedPointUp$ supplies a fixed-output witness but not the apophatic far-end boundary or inscription rows. $\HashUp$ supplies digest-facing readback but neither the socket ledger nor the GAP fiber and far-end boundary.

Thus no bijection can preserve all carrier coordinates and NameCert obligations. Forgetting any of $D,S,G,T,I$ changes the accepted public-read order, while adding such a row to a sibling changes that sibling's carrier rather than identifying it with the fiber.
\end{proof}

\falsifiablePrediction{Every public read of an accepted ApophaticFixedPointFiberUp packet reaches the GAP fiber row before the apophatic far-end row; a direct digest-to-T route refutes the packet.}

\independenceWitness{Nearest siblings: externalitygate, supplysocketledger, generatorfixedpoint, and hash. None is bijective because each omits at least one of the digest, socket, GAP fiber, far-end boundary, or local-inscription rows, while the fiber requires all five rows together with componentwise hsame transport, displayed Cont routes, Pkg provenance, and the local NameCert row.}

\begin{theorem}[Apophatic fixed-point fiber scoped route]
\label{thm:apophatic-fixed-point-fiber-scoped-route}
Every accepted $\ApophaticFixedPointFiberUp$ public read is scoped by the concrete rows
$$
D,S,G,T,I,H,C,P,N
$$
of \autoref{def:apophatic-fixed-point-fiber-carrier} and by the sibling dependencies $\ExternalityGateUp$, $\SupplySocketLedgerUp$, $\GeneratorFixedPointUp$, and $\HashUp$. The GAP fiber row $G$ lies between the digest/socket row $D$ and the apophatic far-end boundary row $T$, so the local certificate exposes only the row-scoped route through $D,S,G,T,I,H,C,P,N$ and rejects any direct digest-to-far-end read.
\end{theorem}
\begin{proof}
Use the carrier rows of \autoref{def:apophatic-fixed-point-fiber-carrier}. They give the accepted public read a finite inventory: digest/socket readback, external-supply socket, GAP fiber, far-end boundary name, local inscription, transport, continuation, provenance, and local naming.

Apply \autoref{thm:apophatic-fixed-point-fiber-namecert-obligations}. The NameCert surface exports those rows in the displayed order, with the sibling packets supplying the externality gate, supply socket ledger, generator fixed-point, and hash-facing readback dependencies.

Apply \autoref{thm:apophatic-fixed-point-fiber-nonescape}. Since the only route from $D$ to $T$ passes through $G$, the GAP fiber is not optional boundary prose but a carrier row. Finally use \autoref{thm:apophatic-fixed-point-fiber-independence}: forgetting any of the sibling-dependent rows changes the packet rather than preserving an accepted scoped read.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiber\_scoped\_route}

\begin{theorem}[Apophatic fixed-point fiber public export surface]
\label{thm:apophatic-fixed-point-fiber-public-export-surface}
Every accepted $\ApophaticFixedPointFiberUp$ packet exports exactly the public read surface
$$
D,S,G,T,I,H,C,P,N
$$
with GAP-first ordering. The sibling dependency rows are read through $\ExternalityGateUp$, $\SupplySocketLedgerUp$, $\GeneratorFixedPointUp$, and $\HashUp$, and every digest-facing public read must pass through the GAP fiber before the apophatic far-end boundary is exposed. This is the public object surface of the packet, not a bridge credential.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:apophatic-fixed-point-fiber-carrier}. The displayed rows are precisely $D,S,G,T,I,H,C,P,N$, and the carrier has no coordinate for a direct digest-to-far-end identification.

Apply the NameCert obligations of \autoref{thm:apophatic-fixed-point-fiber-namecert-obligations}. They bind the digest/socket row, external-supply socket, GAP fiber, far-end boundary, local inscription, transport, replay, provenance, and local naming as the only exported rows.

Use the non-escape theorem \autoref{thm:apophatic-fixed-point-fiber-nonescape} and the sibling-independence theorem \autoref{thm:apophatic-fixed-point-fiber-independence}. These keep $\ExternalityGateUp$, $\SupplySocketLedgerUp$, $\GeneratorFixedPointUp$, and $\HashUp$ as visible dependencies rather than hidden bridge credentials.

Finally apply the scoped-route theorem \autoref{thm:apophatic-fixed-point-fiber-scoped-route}. It forces the GAP-first public read order, so the theorem exports a public object surface only and does not certify an acceptance bridge.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiber\_public\_export\_surface}

\begin{theorem}[Apophatic fixed-point fiber bridge readback]
\label{thm:apophatic-fixed-point-fiber-bridge-readback}
The public export surface of \autoref{thm:apophatic-fixed-point-fiber-public-export-surface} is bridge-facing exactly when the same GAP-first packet
$$
D,S,G,T,I,H,C,P,N
$$
is read through displayed standard readback rows for the digest/socket entrance, the external-supply socket, the GAP fiber, the apophatic far-end boundary, the local inscription, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The $\ApophaticFixedPointFiberUp$ TasteGate row remains a carrier-admission and classifier-stability witness; it is not itself a bridge credential and cannot replace the displayed readback rows.
\end{theorem}
\begin{proof}
Start with the public export surface. By \autoref{thm:apophatic-fixed-point-fiber-public-export-surface}, an accepted packet exposes exactly $D,S,G,T,I,H,C,P,N$ with GAP-first order, and no direct digest-to-far-end route is present.

Read the standard rows over the same packet. The digest/socket entrance, external-supply socket, GAP fiber, far-end boundary, inscription, transport, replay, provenance, and local certificate rows are the only bridge-facing data; they preserve the order already forced by \autoref{thm:apophatic-fixed-point-fiber-nonescape} and \autoref{thm:apophatic-fixed-point-fiber-scoped-route}.

The TasteGate marker is used only to admit the carrier and stabilize its classifier. Since a bridge readback must display the standard rows themselves, replacing those rows by the TasteGate witness would erase the GAP-first readback surface and would leave no bridge-facing packet to inspect.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiber\_bridge\_readback}

\begin{theorem}[Apophatic fixed-point fiber mature boundary]
\label{thm:apophatic-fixed-point-fiber-mature-boundary}
The mature $\ApophaticFixedPointFiberUp$ package surface is exactly the GAP-first public export of \autoref{thm:apophatic-fixed-point-fiber-public-export-surface} together with the bridge readback rows of \autoref{thm:apophatic-fixed-point-fiber-bridge-readback}. It exposes
$$
D,S,G,T,I,H,C,P,N
$$
as the finite public and bridge-facing packet, with $G$ read before $T$, and it still refuses an object-level value for $\mathsf{T}$, hidden external supply, and any observer-substance theorem.
\end{theorem}
\begin{proof}
Read the public side from \autoref{thm:apophatic-fixed-point-fiber-public-export-surface}. The accepted packet exports precisely the GAP-first rows $D,S,G,T,I,H,C,P,N$, with no direct digest-to-far-end route.

Read the bridge-facing side from \autoref{thm:apophatic-fixed-point-fiber-bridge-readback}. The same rows are displayed as standard readback data, while the TasteGate evidence remains separate from bridge credentials.

The refused objects are exactly the coordinates absent from the carrier and from both theorem surfaces. There is no row for an object-level value of $\mathsf{T}$, hidden external supply, or observer-substance assertion, so mature closure preserves those refusals rather than exporting them.
\end{proof}
\leanchecked{BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiber\_mature\_boundary}

\closureat{ApophaticFixedPointFiberUp}{obligationStr}[BEDC.Derived.ApophaticFixedPointFiberUp.ApophaticFixedPointFiberTasteGate\_single\_carrier\_alignment]
\closureat{ApophaticFixedPointFiberUp}{publicStr}[thm:apophatic-fixed-point-fiber-public-export-surface]

\begin{closurestatus}{\ApophaticFixedPointFiberUp}
  \constructivestory{An $\ApophaticFixedPointFiberUp$ packet is generated from a digest/socket row, external-supply socket row, GAP fiber/provenance row, apophatic far-end boundary-name row, local inscription row, componentwise $\hsame$ transport, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:apophatic-fixed-point-fiber-carrier}, \autoref{thm:apophatic-fixed-point-fiber-namecert-obligations}, \autoref{thm:apophatic-fixed-point-fiber-nonescape}, \autoref{thm:apophatic-fixed-point-fiber-independence}, and \autoref{thm:apophatic-fixed-point-fiber-scoped-route} bind the finite carrier, GAP-first read order, direct digest-to-far-end refusal, nearest-sibling independence surface, and scoped public-read route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close a checked standard bridge, an object-level value for $\mathsf{T}$, a hidden external supply, or a global observer-substance theorem.}
  \upgradepath{The paper-side package is at mature closure; machine bridge verification remains outside the paper axis while TasteGate evidence stays separate from bridge credentials.}
\end{closurestatus}
