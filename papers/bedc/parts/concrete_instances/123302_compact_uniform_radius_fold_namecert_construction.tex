\chapter{A Concrete Naming Certificate for $\CompactUniformRadiusFoldUp$}
\label{ch:concrete-instances-compact-uniform-radius-fold-namecert}
\origin{human}

Following Bishop-style finite-cover proofs of Heine--Cantor, the $\CompactUniformRadiusFoldUp$ packet records the radius-only fold that sits between a subordinate compact cover and a tolerance-only uniform-modulus row. It reads the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the metric-distance rows of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map local-radius surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational order row of \autoref{ch:concrete-instances-rat-namecert}, the finite Lebesgue-number and subordinate-cover inputs of \autoref{ch:concrete-instances-finite-lebesgue-number-namecert} and \autoref{ch:concrete-instances-subordinatemoduluscover-namecert}, and the downstream $\UniformModulusUp$ handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet exposes the finite lower-bound radius fold, not an external modulus selector or host compactness theorem.

\begin{definition}[Compact uniform radius fold carrier]
\label{def:compact-uniform-radius-fold-carrier}
A $\CompactUniformRadiusFoldUp$ carrier is a finite $\BHist$ packet
$$
R=(K,M,G,S,L,Q,F,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row carrying the finite $\mathsf{ProbeBundle}$ cover, centers, and nonempty spine.
\item $M$ is the $\MetricSpaceUp$ distance row whose triangle and symmetry ledgers are used after the radius fold is consumed.
\item $G$ is the $\ContinuousMapUp$ local-modulus row over the same compact metric source.
\item $S$ is the $\SubordinateModulusCoverUp$ row displaying bundled centers, subordinate radii, pointwise precision rows, half-radius coverage, and rational subordination comparisons.
\item $L$ is the $\FiniteLebesgueNumberUp$ or finite-cover lower-bound ledger attached to the same cover.
\item $Q$ is the positive $\RatUp$ radius family and comparison row read by the fold.
\item $F$ is the finite radius-fold row, recording primitive recursion over the bundle, accumulator updates, and the certified lower-bound output.
\item $U$ is the $\UniformModulusUp$ handoff row that receives the folded radius as a tolerance-only precision.
\item $H$ records componentwise $\hsame$ transport for $K,M,G,S,L,Q,F,U,C,P,N$.
\item $C$ records displayed $\Cont$ replay through the compact source, metric rows, continuous-map rows, subordinate cover, finite cover lower bound, rational radius family, fold output, uniform-modulus handoff, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformRadiusFoldUp$, $\CompactMetricUp$, $\MetricSpaceUp$, $\ContinuousMapUp$, $\SubordinateModulusCoverUp$, $\FiniteLebesgueNumberUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformRadiusFoldUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host minimum, selected subcover, open-cover compactness theorem, external global modulus, quotient function equality, countable-choice schedule, or ambient $\RealUp$ completeness row.
\end{definition}

\begin{theorem}[CompactUniformRadiusFold NameCert obligations]
\label{thm:compact-uniform-radius-fold-namecert-obligations}
For every accepted $\CompactUniformRadiusFoldUp$ carrier
$$
R=(K,M,G,S,L,Q,F,U,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformRadiusFoldUp}$ surface consists exactly of compact-metric finite-cover exposure, metric distance exposure, continuous-map local-radius exposure, subordinate-cover exposure, finite-cover lower-bound exposure, positive $\RatUp$ radius-family readback, finite radius-fold replay through $F$, $\UniformModulusUp$ handoff through $U$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-radius-fold-carrier}. The rows $K,M,G,S,L,Q,F,U,H,C,P,N$ are the only coordinates of the accepted packet.

The finite cover and local-radius inputs are displayed before the fold. The compact source row $K$ supplies the nonempty bundle, $G$ supplies the local-radius data, $S$ supplies subordinate radii and half-radius coverage, and $L,Q$ supply the finite lower-bound and rational comparison rows.

The fold row $F$ is therefore a radius-only finite recursion over the displayed bundle. It may inspect positive rational candidates and accumulator comparisons, but it cannot select a host minimum or inspect a subcover outside $K$ and $S$.

The output row $U$ is downstream of $F$. Transport, replay, provenance, and local naming are supplied by $H,C,P,N$, so the uniform-modulus consumer receives the folded radius through one displayed NameCert route and no ambient compactness or external modulus object.
\end{proof}

\begin{theorem}[Compact uniform radius fold handoff]
\label{thm:compact-uniform-radius-fold-handoff}
Every compact-uniform-continuity consumer that reads an accepted $\CompactUniformRadiusFoldUp$ packet obtains a $\UniformModulusUp$ precision only after the route
$$
\CompactMetricUp,\MetricSpaceUp,\ContinuousMapUp,\SubordinateModulusCoverUp,\FiniteLebesgueNumberUp,\RatUp
\longmapsto
\CompactUniformRadiusFoldUp
\longmapsto
\UniformModulusUp
$$
has been displayed through $K,M,G,S,L,Q,F,U,H,C,P,N$. The exported object is the finite folded radius row, not an open-cover compactness theorem, selected global modulus, quotient function equality, countable-choice schedule, or ambient real-completeness principle.
\end{theorem}
\begin{proof}
Begin with the source rows. The compact metric row $K$ and metric row $M$ bind the finite source geometry, while $G$ exposes the local-radius data of the continuous map.

Read the finite radius inputs next. The subordinate-cover row $S$ displays the bundled radii and half-radius coverage, $L$ records the finite-cover lower-bound ledger, and $Q$ supplies the positive rational comparison surface.

Now apply the radius fold $F$. It lowers the finite radius family to one classifier-stable precision row by primitive recursion over the displayed bundle.

Finally hand the result to $U$ and replay the structural rows $H,C,P,N$. Since the carrier contains no host minimum, selected subcover, external modulus, quotient equality, countable choice, or ambient completeness coordinate, every consumer remains inside the displayed handoff route.
\end{proof}

\begin{theorem}[Compact uniform radius fold vision realization]
\label{thm:compact-uniform-radius-fold-vision-realization}
The finite compact-cover lower-bound route of \autoref{thm:compact-uniform-finite-minimum-certificate} is realized by an accepted $\CompactUniformRadiusFoldUp$ carrier at the radius-fold boundary: finite compact-cover data, pointwise local radii, rational comparisons, and subordinate coverage are folded into the $\UniformModulusUp$ precision row. This realization is a concrete NameCert packet over $K,M,G,S,L,Q,F,U,H,C,P,N$.
\end{theorem}
\begin{proof}
Read \autoref{thm:compact-uniform-finite-minimum-certificate} through the compact-uniform vision. The proof uses a finite bundle of centers, local pointwise radii, subordinate half-radius coverage, and a rational lower-bound fold.

Those objects are exactly the rows $K,G,S,L,Q,F$ of the carrier, with $M$ supplying the metric support for the later triangle estimate. The uniform-modulus conclusion is the row $U$.

The structural rows $H,C,P,N$ keep the realization in the local NameCert surface. Thus the vision route is concretized as the displayed finite radius-fold packet, without adding a second compactness principle or an external selector.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformRadiusFoldUp$ consumer displays CompactMetric, MetricSpace, ContinuousMap, SubordinateModulusCover, finite-cover lower-bound, RatUp, finite radius-fold, UniformModulus, hsame, Cont, Pkg, and local NameCert rows before reading a compact-uniform precision.}

\independenceWitness{compactmetric, metricspace, continuousmap, subordinatemoduluscover, finitelebesguenumber, rat, uniformmodulus: no listed sibling is bijective to $\CompactUniformRadiusFoldUp$ because this packet jointly carries finite compact-cover data, metric support, local-radius rows, subordinate coverage, finite-cover lower bounds, rational comparisons, a radius fold, uniform-modulus handoff, transport, replay, provenance, and local naming.}

\closureat{CompactUniformRadiusFoldUp}{seedStr}

\begin{closurestatus}{\CompactUniformRadiusFoldUp}
  \constructivestory{A $\CompactUniformRadiusFoldUp$ packet is generated from compact metric finite-cover rows, MetricSpace distance rows, ContinuousMap local-radius rows, SubordinateModulusCover rows, finite cover lower-bound rows, positive RatUp radius comparisons, a finite radius-fold ledger, a UniformModulus handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-radius-fold-carrier}, \autoref{thm:compact-uniform-radius-fold-namecert-obligations}, \autoref{thm:compact-uniform-radius-fold-handoff}, and \autoref{thm:compact-uniform-radius-fold-vision-realization} seed the finite compact-cover radius-fold route into UniformModulusUp.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, external global moduli, quotient function equality, countable choice, ambient real completeness, theorem checking, or bridge checking.}
  \upgradepath{A formal route can encode the finite carrier and prove compact-source exposure, local-radius exposure, subordinate-cover exactness, radius-fold exactness, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformRadiusFoldUp.}
\end{closurestatus}
