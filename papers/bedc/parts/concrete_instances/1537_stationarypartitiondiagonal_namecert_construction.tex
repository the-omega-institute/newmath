\chapter{A Concrete Naming Certificate for $\StationaryPartitionDiagonalUp$}
\label{ch:concrete-instances-stationarypartitiondiagonal-namecert}
\origin{human}

The $\StationaryPartitionDiagonalUp$ packet names the finite-partition diagonal surface used by the stationary rational route into the real-completeness and completion ledger. It keeps the rational input rows, finite observation-window partition, diagonal representatives, constant stream classifiers, dyadic observation ledgers, real seal rows, and local naming provenance as displayed BEDC data. The packet contains no quotient stream, choice-selected limit, host real equality, or ambient completeness field.

\begin{definition}[Stationary partition diagonal carrier]
\label{def:stationary-partition-diagonal-carrier}
A $\StationaryPartitionDiagonalUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
S=(Q,\Pi,\Delta,K,D,E,L,C,P,N)
$$
whose displayed coordinates are as follows:
\begin{enumerate}
\item $Q$ is the stationary $\RatUp$ input row pair read by the rational source classifier;
\item $\Pi$ is a finite partition of observation windows, including the segment bounds, selected anchors, and refinement routes inspected by the diagonal construction;
\item $\Delta$ is the finite list of diagonal representatives selected from the partition segments;
\item $K$ is the constant $\StreamNameUp$ classifier row that reads every segment through the same stationary source stream;
\item $D$ is the $\DyadicRatCoreUp$ observation ledger for the segment representatives and their dyadic bounds;
\item $E$ is the $\RealUp$ seal row receiving the finite diagonal representative;
\item $L$ is the $\mathsf{BHist}$ ledger of displayed segment reads, refinement witnesses, and selected representatives;
\item $C$ is the componentwise $\hsame$ and $\Cont$ route data used to transport the partition classifier;
\item $P$ is the $\Pkg$ provenance naming the $\RatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, and completion-facing dependencies;
\item $N$ is the local $\NameCert_{\StationaryPartitionDiagonalUp}$ provenance row.
\end{enumerate}
Two accepted carriers are classified together exactly when their rational input rows, partition windows, diagonal representatives, constant stream classifiers, dyadic ledgers, real seal rows, history ledgers, transport routes, package rows, and local certificate rows are transported componentwise by the displayed $\hsame$ comparisons and $\Cont$ routes.
\end{definition}

\begin{theorem}[Stationary partition diagonal NameCert obligations]
\label{thm:stationary-partition-diagonal-namecert-obligations}
The local $\NameCert_{\StationaryPartitionDiagonalUp}$ obligation surface consists of carrier habitation, classifier stability across partition refinement, finite-window exactness, the $\RealUp$ seal boundary, and public export to the Phase Real and Completion diagonal-limit route.
\end{theorem}
\leanchecked{BEDC.Derived.StationaryPartitionDiagonalUp.StationaryPartitionDiagonalCarrier\_namecert\_obligations}
\begin{proof}
Carrier habitation is the finite packaging operation of \autoref{def:stationary-partition-diagonal-carrier}. The displayed rows name the stationary rational inputs, the finite partition, the diagonal representatives, the constant stream classifier, the dyadic ledger, the real seal, the history ledger, the transport routes, and the local provenance.

Classifier stability is componentwise. A refinement may subdivide partition segments, but it can only do so through the recorded $\hsame$ comparisons and $\Cont$ routes; the constant $\StreamNameUp$ classifier therefore continues to read the same stationary rational source on each displayed segment.

Finite-window exactness, the seal boundary, and the public export are the three consumer-facing clauses proved below. Together they account for every read made by the local certificate and exclude quotient streams, selected limits, host real equality, hidden representatives, and ambient completeness data from the packet boundary.
\end{proof}

\begin{theorem}[Stationary partition diagonal window exactness]
\label{thm:stationary-partition-diagonal-window-exactness}
For every accepted $\StationaryPartitionDiagonalUp$ carrier and every displayed segment of its finite partition $\Pi$, the diagonal representative in $\Delta$ is read through the same constant $\StreamNameUp$ classifier row $K$, the corresponding $\DyadicRatCoreUp$ observation ledger row $D$, and the segment's displayed $\mathsf{BHist}$ route in $L$. Refining $\Pi$ preserves the finite observation content through the recorded $\hsame$ and $\Cont$ routes.
\end{theorem}
\leanchecked{BEDC.Derived.StationaryPartitionDiagonalUp.StationaryPartitionDiagonalCarrier\_window\_exactness}
\begin{proof}
Project the accepted carrier through \autoref{def:stationary-partition-diagonal-carrier}. A displayed segment of $\Pi$ carries its bounds, selected anchor, and representative coordinate, and the representative is listed in $\Delta$.

The classifier row $K$ is constant, so every segment read uses the same stationary rational stream. The dyadic ledger $D$ records the rational observation and bound attached to that segment, while $L$ records the visible history route used by the read.

Under partition refinement, each smaller segment points back to a displayed parent segment through the route data $C$. Since the carrier classifier transports only by $\hsame$ and $\Cont$, the refined ledger reads the same finite stationary observation content and introduces no hidden endpoint or host limit.
\end{proof}

\begin{theorem}[Stationary partition diagonal seal boundary]
\label{thm:stationary-partition-diagonal-seal-boundary}
Every $\RealUp$ seal read exported by an accepted $\StationaryPartitionDiagonalUp$ carrier factors through its stationary $\RatUp$ source row, finite partition window, diagonal representative, constant $\StreamNameUp$ classifier, $\DyadicRatCoreUp$ observation ledger, $\mathsf{BHist}$ route, displayed $\hsame$ and $\Cont$ transport, $\Pkg$ provenance, and local $\NameCert$ row.
\end{theorem}
\begin{proof}
Begin with a public seal request and project the accepted carrier. The request can reach $E$ only after choosing one finite partition window and the corresponding diagonal representative in $\Delta$.

By \autoref{thm:stationary-partition-diagonal-window-exactness}, that representative is read through $K$, $D$, and the displayed history route $L$. The route data $C$ supplies only componentwise $\hsame$ comparisons and $\Cont$ witnesses, so transport cannot add a quotient stream or an unrecorded limit object.

Repacking the selected rows with $P$ and $N$ gives the stated factorization of the $\RealUp$ seal. The seal is therefore attached to the finite BEDC packet itself, not to an ambient real-completeness principle.
\end{proof}

\begin{theorem}[Stationary partition diagonal public export]
\label{thm:stationary-partition-diagonal-public-export}
The public $\StationaryPartitionDiagonalUp$ export surface is the finite stationary diagonal packet of \autoref{def:stationary-partition-diagonal-carrier} together with the exact window reads of \autoref{thm:stationary-partition-diagonal-window-exactness} and the seal factorization of \autoref{thm:stationary-partition-diagonal-seal-boundary}. This surface is the concrete Real and Completion NameCert route consumed by the Phase Real and Completion diagonal-limit construction.
\end{theorem}
\begin{proof}
The public packet exposes exactly the carrier rows $Q,\Pi,\Delta,K,D,E,L,C,P,N$. These rows are all finite BEDC rows or local provenance rows.

The exactness theorem accounts for every finite-window read of the partition and every refinement of that finite partition. The seal-boundary theorem accounts for every downstream $\RealUp$ read and keeps it factored through the same visible rows.

Thus a Phase Real or Completion consumer receives a concrete NameCert surface: stationary rational inputs, finite partition windows, diagonal representatives, dyadic observations, real seal rows, history and transport ledgers, package provenance, and local certificate data. No exported coordinate is a quotient stream, selected host limit, hidden representative, or ambient completeness theorem.
\end{proof}

\closureat{StationaryPartitionDiagonalUp}{seedStr}
\closureat{StationaryPartitionDiagonalUp}{obligationStr}
\closureat{StationaryPartitionDiagonalUp}{scopedStr}
\closureat{StationaryPartitionDiagonalUp}{publicStr}

\begin{closurestatus}{\StationaryPartitionDiagonalUp}
  \constructivestory{A $\StationaryPartitionDiagonalUp$ packet is generated from stationary $\RatUp$ input rows, a finite partition of observation windows, diagonal representatives, a constant $\StreamNameUp$ classifier, $\DyadicRatCoreUp$ observation ledgers, a $\RealUp$ seal row, $\mathsf{BHist}$ ledgers, displayed $\hsame$ and $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{The public export is the finite stationary partition diagonal packet with exact window reads, refinement-stable classifier transport, $\RealUp$ seal factorization, and downstream Phase Real and Completion diagonal-limit consumption over the displayed rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient streams, choice-selected limits, host real equality, ambient completeness, or bridge checking.}
  \upgradepath{Bridged closure requires a standard bridge from the stationary finite-partition diagonal packet to the Real and Completion consumer schema.}
\end{closurestatus}
