\chapter{A Concrete Naming Certificate for $\FiniteMetrizableCompletionUp$}
\label{ch:concrete-instances-finite-metrizable-completion-namecert}
\origin{human}

Following Bishop and Bridges and Kelley, the $\FiniteMetrizableCompletionUp$ packet records the finite metrizable-completion handoff used by the L10 Real and Completion corridor. It sits after the metric-completion row of \autoref{ch:concrete-instances-metriccompletion-namecert}, the located completion row of \autoref{ch:concrete-instances-located-metric-completion-namecert}, the Cauchy completion embedding of \autoref{ch:concrete-instances-cauchy-completion-embedding-namecert}, the finite StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the RegSeqRat readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names completion as displayed finite carrier data, not as quotient completion equality, selected representatives, countable choice, trichotomy, compactness, or ambient $\RealUp$ completeness.

\begin{definition}[FiniteMetrizableCompletion carrier]
\label{def:finite-metrizable-completion-carrier}
A $\FiniteMetrizableCompletionUp$ carrier is a finite $\BHist$ packet
$$
F=(M,J,L,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricCompletionUp$ source row exposing the metric source, finite tolerance ledger, and completion-facing route.
\item $J$ is the $\CauchyCompletionEmbeddingUp$ row by which source observations enter the completion surface.
\item $L$ is the $\LocatedMetricCompletionUp$ row that keeps located completion evidence visible before a Real-facing read is public.
\item $W$ is the $\StreamNameUp$ finite-window row opened by the displayed tolerance and source data.
\item $R$ is the $\RegSeqRatUp$ readback row extracted from the same finite windows.
\item $E$ is the terminal $\RealUp$ seal reached only after $M,J,L,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the metric source, embedding, located-completion, window, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records the displayed $\Cont$ replay from the metric source through the Cauchy embedding, located completion row, finite windows, regular readback, and terminal Real seal.
\item $P$ is the $\Pkg$ provenance over $\FiniteMetrizableCompletionUp$, $\MetricCompletionUp$, $\CauchyCompletionEmbeddingUp$, $\LocatedMetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteMetrizableCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient completion equality, selected host limits, countable choice, trichotomy, compactness, host interval equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[FiniteMetrizableCompletion NameCert obligations]
\label{thm:finite-metrizable-completion-namecert-obligations}
For an accepted $\FiniteMetrizableCompletionUp$ carrier $F=(M,J,L,W,R,E,H,C,P,N)$, the local $\NameCert_{\FiniteMetrizableCompletionUp}$ surface consists exactly of carrier admission, metric-completion source exposure through $M$, Cauchy-completion embedding through $J$, located-completion evidence through $L$, finite StreamName windows through $W$, RegSeqRat readback through $R$, terminal Real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:finite-metrizable-completion-carrier}. Its only completion-facing rows are the metric source, Cauchy embedding, located completion, finite window, regular readback, Real seal, transport, replay, provenance, and local naming rows.

The metric source row $M$ supplies the completion-facing tolerance and source route. The embedding row $J$ records how source observations enter the Cauchy completion surface, and the located row $L$ keeps located completion evidence visible before the packet is read as Real-facing.

The finite numerical read proceeds through $W$ and $R$. The StreamName row opens only the displayed finite windows, and the RegSeqRat row supplies the corresponding regular rational readback before the terminal Real seal $E$ is consumed.

Finally $H,C,P,N$ transport, replay, package, and name the same displayed route. Since no further carrier coordinate is present, quotient completion equality, selected limits, countable choice, trichotomy, compactness, host equality, and ambient $\RealUp$ completeness are not exported.
\end{proof}

\begin{theorem}[FiniteMetrizableCompletion Real handoff]
\label{thm:finite-metrizable-completion-real-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\FiniteMetrizableCompletionUp$ packet must read the finite route
$$
\MetricCompletionUp\longmapsto \CauchyCompletionEmbeddingUp\longmapsto \LocatedMetricCompletionUp\longmapsto \StreamNameUp\longmapsto \RegSeqRatUp\longmapsto \RealUp
$$
with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support still visible. The handoff exports no quotient completion, selected endpoint or limit representative, countable-choice schedule, trichotomy, compactness theorem, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:finite-metrizable-completion-namecert-obligations}. It places every accepted consumer read on the rows $M,J,L,W,R,E,H,C,P,N$.

The completion-facing prefix is $M,J,L$. The metric-completion source is read first, the Cauchy-completion embedding then admits the displayed source observations, and the located-completion row keeps located evidence attached to the same finite packet.

The Real-facing suffix is $W,R,E$. Finite StreamName windows are opened before RegSeqRat readback, and the terminal $\RealUp$ seal is available only after that regular readback has been displayed.

The structural rows $H,C,P,N$ preserve the same route. Because the carrier has no coordinate for quotient completion equality, selected representatives, choice schedules, trichotomy, compactness, or ambient completeness, the Real and Completion corridor receives exactly the finite handoff stated above.
\end{proof}

\independenceWitness{metriccompletion, cauchycompletionembedding, locatedmetriccompletion, streamname, regseqrat, real: $\FiniteMetrizableCompletionUp$ is not bijective with any listed sibling because it joins metric source exposure, Cauchy embedding, located completion evidence, finite windows, regular rational readback, Real sealing, componentwise hsame transport, Cont replay, Pkg provenance, and one local NameCert row.}

\closureat{FiniteMetrizableCompletionUp}{seedStr}

\begin{closurestatus}{\FiniteMetrizableCompletionUp}
  \constructivestory{A $\FiniteMetrizableCompletionUp$ packet is generated from a metric-completion source row, Cauchy-completion embedding, located-completion evidence, StreamName finite windows, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-metrizable-completion-carrier}, \autoref{thm:finite-metrizable-completion-namecert-obligations}, and \autoref{thm:finite-metrizable-completion-real-handoff} display the finite metrizable-completion handoff over MetricCompletion, CauchyCompletionEmbedding, LocatedMetricCompletion, StreamName, RegSeqRat, and Real rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected host limits, countable choice, trichotomy, compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires kernel-scope routing for $\MetricCompletionUp$, $\CauchyCompletionEmbeddingUp$, $\LocatedMetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ over the same packet.}
\end{closurestatus}
