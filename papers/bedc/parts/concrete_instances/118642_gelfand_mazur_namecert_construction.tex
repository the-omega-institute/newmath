\chapter{A Concrete Naming Certificate for $\GelfandMazurUp$}
\label{ch:concrete-instances-gelfand-mazur-namecert}
\origin{human}

Following Gelfand and Mazur's theorem for complex Banach division algebras, the $\GelfandMazurUp$ packet records the finite certificate surface by which a displayed complex Banach division algebra can be read as scalar-complex data. It sits after the field packet of \autoref{ch:concrete-instances-field-namecert}, the complex-number carrier of \autoref{ch:concrete-instances-complex-namecert}, the norm boundary of \autoref{ch:concrete-instances-norm-namecert}, the Banach completion surface of \autoref{ch:concrete-instances-banach-namecert}, and the scalar-linear map rows of \autoref{ch:concrete-instances-linearmap-namecert}. The chapter records the visible NameCert rows for that classical route; it does not assert spectral theory, quotient equality, host algebra equality, selected spectrum points, or a complete Banach-algebra library.

\begin{definition}[Gelfand-Mazur carrier]
\label{def:gelfand-mazur-carrier}
A $\GelfandMazurUp$ carrier is a finite $\BHist$ packet
$$
K_{\mathrm{gm}}=(A,F,C,N,B,I,H,T,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the algebra row carrying the multiplication, unit, distributivity, and scalar-action interface over a displayed complex scalar source.
\item $F$ is the $\FieldUp$ row recording the nonzero division surface for the algebra's invertible part.
\item $C$ is the $\ComplexUp$ scalar row used for scalar embedding, scalar comparison, and terminal complex readback.
\item $N$ is the $\NormUp$ row recording the multiplicative norm bound, unit norm readback, and norm comparison ledger.
\item $B$ is the $\BanachUp$ row exposing the complete normed carrier surface consumed by completion-facing readers.
\item $I$ is the nonzero inverse row, displaying the inverse operation only after the nonzero classifier and field row have been read.
\item $H$ records componentwise $\hsame$ transport for $A,F,C,N,B,I,T,P,L$.
\item $T$ records displayed $\Cont$ replay from the algebra, scalar, norm, Banach, and inverse rows to the scalar-complex readback.
\item $P$ is the $\Pkg$ provenance over $\GelfandMazurUp$, $\FieldUp$, $\ComplexUp$, $\NormUp$, $\BanachUp$, $\LinearMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\GelfandMazurUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through these rows and exposes no spectrum selector, host algebra equality, quotient representative, external Banach-algebra library, or detached complex scalar object.
\end{definition}

\begin{theorem}[Gelfand-Mazur NameCert obligations]
\label{thm:gelfand-mazur-namecert-obligations}
For every accepted $\GelfandMazurUp$ carrier $K_{\mathrm{gm}}=(A,F,C,N,B,I,H,T,P,L)$, the local $\NameCert_{\GelfandMazurUp}$ surface consists exactly of algebra admission, field and nonzero-inverse exposure, $\ComplexUp$ scalar rows, norm and Banach rows, scalar-linear readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.

Any consumer that reads a Gelfand-Mazur conclusion must first display the sibling routes through \autoref{ch:concrete-instances-field-namecert}, \autoref{ch:concrete-instances-complex-namecert}, \autoref{ch:concrete-instances-norm-namecert}, \autoref{ch:concrete-instances-banach-namecert}, and \autoref{ch:concrete-instances-linearmap-namecert}. Replacing one of those rows by spectral theory, quotient equality, host algebra equality, selected spectrum points, or a complete Banach-algebra library leaves the accepted carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:gelfand-mazur-carrier}. The rows $A,F,C,N,B,I$ display the algebra, division, complex scalar, norm, Banach, and inverse surfaces before any structural row is read.

The sibling dependency surface is explicit. The field row supplies division, the complex row supplies scalar readback, the norm row supplies bounded comparison, the Banach row supplies completion-facing stability, and the linear-map row records the scalar-linear route used by the algebra consumer.

The rows $H,T,P,L$ transport, replay, source, and name the same finite inventory. Since the carrier has no coordinate for spectral theory, quotient equality, host algebra equality, selected spectrum points, or a complete Banach-algebra library, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Gelfand-Mazur complex scalar collapse]
\label{thm:gelfand-mazur-complex-scalar-collapse}
Every scalar-collapse read from an accepted $\GelfandMazurUp$ packet factors through the finite route
$$
A,F,C,N,B,I \longrightarrow H,T,P,L .
$$
The read first exposes the algebra and nonzero-inverse rows, then the $\ComplexUp$ scalar row, then the norm and Banach rows, and only then the transport, replay, provenance, and local naming rows. The resulting scalar-complex comparison is a NameCert readback over $K_{\mathrm{gm}}$, not a claim of host algebra equality, quotient equality, selected spectrum points, spectral theory, or a complete Banach-algebra library.
\end{theorem}
\begin{proof}
Start with the algebra row $A$ and the field row $F$. They display the multiplication, unit, scalar action, and nonzero division surface that a Gelfand-Mazur consumer is allowed to inspect.

Next read the complex, norm, Banach, and inverse rows $C,N,B,I$. These rows attach scalar-complex comparison to the displayed normed complete carrier and to the nonzero inverse route; they do not introduce a spectrum selector or a host algebra-equality principle.

Finally apply $H,T,P,L$. Componentwise transport, displayed replay, provenance, and local naming preserve exactly the finite packet, so the scalar-collapse read cannot escape to quotient equality, selected spectrum points, spectral theory, or an external Banach-algebra library.
\end{proof}

\falsifiablePrediction{An accepted $\GelfandMazurUp$ read is refuted if a scalar-complex conclusion is reached without displaying the algebra row, FieldUp division row, ComplexUp scalar row, NormUp row, BanachUp row, nonzero inverse row, hsame transport, Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{field, complex, norm, banach, and linearmap: none is bijective to the Gelfand-Mazur packet because only $\GelfandMazurUp$ simultaneously records algebra admission, complex scalar readback, Banach norm completion, nonzero inverse exposure, scalar-linear comparison, transport, replay, provenance, and local NameCert naming.}

\closureat{GelfandMazurUp}{seedStr}

\begin{closurestatus}{\GelfandMazurUp}
  \constructivestory{A $\GelfandMazurUp$ packet is generated from an algebra row, FieldUp division row, ComplexUp scalar row, NormUp row, BanachUp row, nonzero inverse row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:gelfand-mazur-carrier}, \autoref{thm:gelfand-mazur-namecert-obligations}, and \autoref{thm:gelfand-mazur-complex-scalar-collapse} seed the finite scalar-collapse route for complex Banach division algebra consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, spectral theory, quotient equality, host algebra equality, selected spectrum points, or a complete Banach-algebra library.}
  \upgradepath{Obligation closure requires checked carrier admission, scalar-collapse route determinacy, nonzero inverse exposure, Banach and norm row compatibility, transport, replay, provenance, and local naming for BEDC.Derived.GelfandMazurUp.}
\end{closurestatus}
