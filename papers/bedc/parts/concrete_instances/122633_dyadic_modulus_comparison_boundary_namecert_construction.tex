\chapter{A Concrete Naming Certificate for $\DyadicModulusComparisonBoundaryUp$}
\label{ch:concrete-instances-dyadic-modulus-comparison-boundary-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\DyadicModulusComparisonBoundaryUp$ packet realizes the finite comparison-modulus boundary of \autoref{sec:real-completeness-modulus-comparison-boundary}. It records two located-real endpoint presentations, one displayed dyadic comparison modulus, finite inspection windows, rational readbacks, and real seals before any comparison-facing consumer reads the packet. The chapter sits beside the located-real comparison packet of \autoref{ch:concrete-instances-located-real-comparison-namecert}, the comparison-modulus sibling of \autoref{ch:concrete-instances-located-real-comparison-modulus-namecert}, the tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite-window source of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet is constructive: it does not export exact trichotomy, $\mathsf{LPO}$, quotient real equality, or selected representative limits.

\begin{definition}[Dyadic modulus comparison boundary carrier]
\label{def:dyadic-modulus-comparison-boundary-carrier}
A $\DyadicModulusComparisonBoundaryUp$ carrier is a finite $\BHist$ packet
$$
C=(A,B,D,M,W,R,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the left $\LocatedRealUp$ endpoint row, exposed through finite stream windows and regular rational readback.
\item $B$ is the right $\LocatedRealUp$ endpoint row, exposed through the same finite-window discipline.
\item $D$ is the shared $\DyadicRatCoreUp$ tolerance ledger for the comparison request.
\item $M$ is the displayed dyadic comparison modulus assigning each requested tolerance to a finite common observation boundary.
\item $W$ is the paired $\StreamNameUp$ window family selected by $M$ for the two endpoint rows.
\item $R$ records the paired $\RegSeqRatUp$ readbacks reached from those windows.
\item $E$ records the two $\RealUp$ seals, readable only after $A,B,D,M,W,R$ have been projected.
\item $H$ records componentwise $\hsame$ transport for endpoint rows, tolerance ledger, modulus, windows, readbacks, real seals, replay, comparison boundary, provenance, and local name.
\item $K$ records the displayed comparison-boundary row: finite tolerance closeness, finite apartness window, or common-refinement handoff, but not exact trichotomy.
\item $P$ is the $\Pkg$ provenance over $\DyadicModulusComparisonBoundaryUp$, $\LocatedRealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicModulusComparisonBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,D,M,W,R,E,H,K,P,N$. It has no coordinate for arbitrary exact order trichotomy, $\mathsf{LPO}$, quotient real equality, countable choice, or selected completion limits.
\end{definition}

\begin{theorem}[Dyadic modulus comparison boundary NameCert obligations]
\label{thm:dyadic-modulus-comparison-boundary-namecert-obligations}
For every accepted $\DyadicModulusComparisonBoundaryUp$ carrier
$$
C=(A,B,D,M,W,R,E,H,K,P,N),
$$
the local $\NameCert_{\DyadicModulusComparisonBoundaryUp}$ surface consists exactly of six finite obligation rows: endpoint admission for $A$ and $B$; dyadic tolerance admission through $D$; comparison-modulus determinacy through $M$; paired finite-window and $\RegSeqRatUp$ readback through $W$ and $R$; Real-seal nonescape through $E$; and transport, comparison-boundary replay, provenance, and local ledgering through $H,K,P,N$. No obligation row exports exact trichotomy, $\mathsf{LPO}$, quotient real equality, countable choice, or a selected real representative.
\end{theorem}
\begin{proof}
Project the accepted carrier. The endpoint data are $A$ and $B$, while $D$ supplies the shared dyadic request against which comparison is read.

The modulus row $M$ turns a requested tolerance into a finite common observation boundary. The paired windows $W$ and readbacks $R$ then expose only the rational observations selected by that displayed modulus.

The comparison row $K$ records the constructive boundary: finite tolerance closeness, finite apartness, or a common-refinement handoff. It does not return a total exact order branch for arbitrary located-real presentations.

The real-seal row $E$ and structural rows $H,P,N$ preserve the same finite packet under sealing, transport, provenance, and local naming. Since no carrier coordinate names exact trichotomy, $\mathsf{LPO}$, quotient equality, choice, or selected representatives, the six obligation rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Dyadic modulus comparison common refinement]
\label{thm:dyadic-modulus-comparison-boundary-common-refinement}
Let $C=(A,B,D,M,W,R,E,H,K,P,N)$ be an accepted $\DyadicModulusComparisonBoundaryUp$ carrier. Every comparison read for the two endpoint rows factors through the common refinement selected by the dyadic modulus row $M$ and the tolerance ledger $D$. Thus the comparison route may compose finite moduli and read two $\RealUp$ seals, but it cannot produce exact trichotomy, $\mathsf{LPO}$, quotient real equality, or a selected limit.
\end{theorem}
\begin{proof}
Read the requested tolerance through $D$. The dyadic ledger fixes the scale at which the two endpoints may be compared.

Apply the modulus row $M$. It selects a common finite observation boundary for both endpoints, and the paired StreamName row $W$ exposes exactly those windows.

Use $R$ to read the rational observations and $K$ to record the comparison boundary. When two comparison steps are composed, the common refinement is obtained by taking the displayed finite boundary that serves both modulus rows, as in the finite comparison composition of \autoref{sec:real-completeness-modulus-comparison-boundary}.

The real seals $E$ are read only after the finite comparison rows have been projected, and $H,P,N$ preserve the same packet under transport, provenance, and local naming. Therefore the route supplies a constructive common refinement, not exact trichotomy, omniscience, quotient equality, choice, or selected limits.
\end{proof}

\falsifiablePrediction{Every accepted $\DyadicModulusComparisonBoundaryUp$ read exposes two located-real endpoint rows, a dyadic tolerance ledger, displayed comparison modulus, paired StreamName windows, paired RegSeqRat readbacks, two RealUp seals, comparison-boundary row, hsame transport, Pkg provenance, and local NameCert data before a comparison consumer receives the packet.}

\independenceWitness{locatedreal, located-real-comparison, located-real-comparison-modulus, dyadicratcore, streamname, regseqrat, real: $\DyadicModulusComparisonBoundaryUp$ is not bijective to any listed sibling because it binds two endpoint rows to one displayed dyadic comparison modulus, paired finite windows, paired readbacks, Real seals, comparison-boundary replay, transport, provenance, and local NameCert row.}

\closureat{DyadicModulusComparisonBoundaryUp}{seedStr}

\begin{closurestatus}{\DyadicModulusComparisonBoundaryUp}
  \constructivestory{A $\DyadicModulusComparisonBoundaryUp$ packet is generated from two located-real endpoint rows, a DyadicRatCore tolerance ledger, a displayed comparison modulus, paired StreamName windows, paired RegSeqRat readbacks, two RealUp seals, hsame transport, a comparison-boundary row, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-modulus-comparison-boundary-carrier}, \autoref{thm:dyadic-modulus-comparison-boundary-namecert-obligations}, \autoref{thm:dyadic-modulus-comparison-boundary-common-refinement}, the falsifiable prediction, and the independence witness seed the finite dyadic comparison-modulus boundary for located-real reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close exact trichotomy, $\mathsf{LPO}$, quotient real equality, countable choice, selected real representatives, ambient $\RealUp$ completeness, theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint admission, dyadic tolerance admission, comparison-modulus determinacy, finite-window readback, common-refinement stability, real-seal nonescape, transport, provenance, and local ledgering for BEDC.Derived.DyadicModulusComparisonBoundaryUp.}
\end{closurestatus}
