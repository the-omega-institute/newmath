\chapter{A Concrete Naming Certificate for $\InnovationInputSpectralBoundaryUp$}
\label{ch:concrete-instances-innovation-input-spectral-boundary-namecert}
\origin{ai}

The $\InnovationInputSpectralBoundaryUp$ packet realizes the spectral-boundary route of \autoref{ch:visions-innovation-input-dynamics-spectral-boundary} as finite transition-multiplier and tail-exponent rows. It records the input-flow multiplier before any consumer reads diffusion geometry, relativistic dispersion, mass scale, or spectral dimension. The packet is a finite NameCert surface for dynamical innovation input; it is not a Hilbert Gram row, not an Einstein-equation claim, not a selected continuum limit, not a host spectral theorem, and not an equality between kinematic and dynamic geometries.

\begin{definition}[Innovation input spectral-boundary carrier]
\label{def:innovation-input-spectral-boundary-carrier}
An $\InnovationInputSpectralBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(I,M,L,Q,T,D,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the innovation-input source row naming the finite state window, tick scale, and reversible transition context.
\item $M$ is the transition-multiplier row, recording the displayed finite or periodic multiplier samples used by the input-flow read.
\item $L$ is the logarithmic Hamiltonian row, exposing only the finite read of $-\log M$ on the inspected momentum cells.
\item $Q$ is the low-momentum quadratic row that may supply a diffusion inverse-metric read when the inspected cells display a quadratic leading term.
\item $T$ is the spectral-tail row, recording the finite tail exponent and mass-scale comparison used by spectral-dimension consumers.
\item $D$ is the dynamic-boundary classifier separating diffusion, relativistic, stable-tail, and refusal cases.
\item $S$ is the sibling route row naming the finite consumers that may read this boundary.
\item $H$ records componentwise $\hsame$ transport for the source, multiplier, logarithmic, quadratic, tail, classifier, sibling-route, replay, provenance, and local-name rows.
\item $C$ records displayed $\Cont$ replay from input source to multiplier samples, logarithmic readback, low-momentum row, spectral-tail row, classifier row, and sibling consumers.
\item $P$ is the $\Pkg$ provenance over $\InnovationInputSpectralBoundaryUp$, the source vision of \autoref{ch:visions-innovation-input-dynamics-spectral-boundary}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\InnovationInputSpectralBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,M,L,Q,T,D,S,H,C,P,N$. It has no coordinate for a continuum spectral measure, a host functional calculus, a selected limiting process, equality with a positive innovation Gram tensor, or a global field equation.
\end{definition}

\begin{theorem}[Innovation input spectral-boundary NameCert obligations]
\label{thm:innovation-input-spectral-boundary-namecert-obligations}
For every accepted $\InnovationInputSpectralBoundaryUp$ carrier
$$
B=(I,M,L,Q,T,D,S,H,C,P,N),
$$
the local $\NameCert_{\InnovationInputSpectralBoundaryUp}$ surface consists exactly of carrier admission for the innovation-input source row, transition-multiplier row, logarithmic Hamiltonian row, low-momentum quadratic row, spectral-tail row, dynamic-boundary classifier, sibling-route row, componentwise transport, displayed replay, package provenance, and local naming. Every spectral-boundary read must therefore expose the finite multiplier samples and the tail classifier before a consumer may read diffusion, relativistic, mass-scale, or spectral-dimension data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:innovation-input-spectral-boundary-carrier}. The source row $I$ fixes the inspected finite transition context, and $M$ is the only row containing transition-multiplier samples.

Read the dynamic rows in order. The logarithmic row $L$ is built from the displayed multiplier samples, the low-momentum row $Q$ records only the inspected quadratic leading term, and the spectral-tail row $T$ records the displayed exponent and mass-scale comparison. No step introduces a continuum spectral measure or host functional calculus.

Inspect the classifier row $D$ and sibling row $S$. They decide which finite consumer may read the boundary: diffusion reads use $Q$, relativistic and mass-scale reads use the relevant part of $T$, and stable-tail reads use the exponent component of $T$. Refusal cases stay visible as classifier outcomes rather than hidden analytic assumptions.

Finally $H,C,P,N$ transport, replay, source, and name the same finite rows. Since the packet has no coordinate for a selected continuum limit, equality with the positive innovation Gram row, global field dynamics, or a host spectral theorem, the NameCert obligations are exactly the finite boundary obligations listed above.
\end{proof}

\begin{theorem}[Innovation input spectral-boundary TasteGate readiness]
\label{thm:innovation-input-spectral-boundary-tastegate-readiness}
The Lean file \texttt{lean4/BEDC/Derived/InnovationInputSpectralBoundaryUp/TasteGate.lean} must provide explicit $\mathsf{Nontrivial}\ \InnovationInputSpectralBoundaryUp$ and $\mathsf{FieldFaithful}\ \InnovationInputSpectralBoundaryUp$ instances for the same displayed carrier. The nontriviality witness must separate an accepted packet with visible multiplier and tail data from a packet missing those rows, and field faithfulness must preserve the row order $I,M,L,Q,T,D,S,H,C,P,N$.
\end{theorem}
\begin{proof}
Nontriviality is a finite carrier obligation: the multiplier row $M$ and tail row $T$ are visible coordinates, so a packet that changes either row cannot be identified with the empty or refusal-only packet by the intended classifier.

Field faithfulness is likewise row-local. A classifier that forgets $M$ cannot distinguish diffusion reads, a classifier that forgets $T$ cannot distinguish spectral-tail reads, and a classifier that reorders $Q,T,D$ cannot tell low-momentum, tail, and refusal cases apart. The Lean TasteGate surface must therefore keep the displayed order of the carrier rows.
\end{proof}

\falsifiablePrediction{Every $\InnovationInputSpectralBoundaryUp$ diffusion, relativistic, mass-scale, or spectral-dimension consumer must display the transition-multiplier row, logarithmic Hamiltonian row, low-momentum quadratic row, spectral-tail row, dynamic-boundary classifier, sibling-route row, hsame transport, Cont replay, Pkg provenance, and local NameCert row before reading the boundary.}

\independenceWitness{innovation-input-geometry-boundary, reality-constrained-tower-packet, observer-direction, metacic-confluence-diamond-ledger, rule110-glider-phase-classifier: $\InnovationInputSpectralBoundaryUp$ is not bijective to any listed sibling because it jointly carries transition-multiplier samples, logarithmic readback, low-momentum quadratic data, spectral-tail data, dynamic classification, sibling routing, transport, replay, provenance, and local naming.}

\closureat{InnovationInputSpectralBoundaryUp}{seedStr}

\begin{closurestatus}{\InnovationInputSpectralBoundaryUp}
  \constructivestory{An $\InnovationInputSpectralBoundaryUp$ packet is generated from an innovation-input source row, transition-multiplier samples, logarithmic Hamiltonian readback, low-momentum quadratic data, spectral-tail data, a dynamic-boundary classifier, sibling routing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:innovation-input-spectral-boundary-carrier}, \autoref{thm:innovation-input-spectral-boundary-namecert-obligations}, and \autoref{thm:innovation-input-spectral-boundary-tastegate-readiness} seed the finite transition-multiplier and spectral-tail boundary packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close continuum spectral measures, host functional calculus, selected continuum limits, equality with a positive innovation Gram tensor, global field dynamics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, multiplier-row exposure, logarithmic readback, quadratic-row exposure, spectral-tail exactness, classifier nonescape, sibling routing, transport, replay, provenance, local naming, Nontrivial TasteGate evidence, and FieldFaithful TasteGate evidence for BEDC.Derived.InnovationInputSpectralBoundaryUp.}
\end{closurestatus}
