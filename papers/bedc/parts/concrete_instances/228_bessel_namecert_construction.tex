\chapter{A Concrete Naming Certificate for $\BesselUp$}
\label{ch:concrete-instances-bessel-namecert}

The Bessel-function interface $\BesselUp$ packages the holomorphic solutions to Bessel's ODE $x^2 y'' + x y' + (x^2 - n^2) y = 0$, with their integral representations and recurrence laws. It depends on $\NameCert_{\OdeUp}$ (\autoref{ch:concrete-instances-ode-namecert}) and $\NameCert_{\HolomorphicUp}$ (\autoref{ch:concrete-instances-holomorphic-namecert}).

\section{Carrier definition}
\label{sec:bessel-carrier}

\begin{definition}[BesselUp BHist carrier packet]
\label{def:bessel-bhist-carrier-packet}
The $\BesselUp$ carrier is the $\mathsf{BHist}$ packet
$$
\mathcal B=(O,H,\nu,\sigma,\tau,\rho,\kappa,\pi)
$$
where $O$ is an $\OdeUp$ source row for the displayed Bessel differential
equation, $H$ is a $\HolomorphicUp$ source row for the corresponding
disk-local solution histories, $\nu$ is the order and index row, $\sigma$ and
$\tau$ are the solution endpoint rows, $\rho$ is the finite recurrence ledger,
$\kappa$ is the family of $\hsame$ transport rows connecting the ODE,
holomorphic, order, and endpoint entries, and $\pi$ is the shared
$\mathsf{Pkg}$ provenance row. A $\BesselUp$ consumer reads solution data only
through this packet.
\end{definition}

\section{The certificate}
\label{sec:bessel-certificate}

\begin{theorem}[BesselUp root ODE source obligation]
\label{thm:bessel-root-ode-source-obligation}
Every accepted $\BesselUp$ root packet projects an $\OdeUp$ source row for
the Bessel equation, its order and index row, the solution endpoint rows, the
ODE-side endpoint classifier, and the $\mathsf{Pkg}$ provenance recorded in
\autoref{def:bessel-bhist-carrier-packet}. The root packet exports no host
analytic solution operator and no equality of ordinary functions.
\end{theorem}
\leanchecked{BEDC.Derived.BesselUp.BesselRootPacket\_root\_ode\_source\_obligation}
\begin{proof}
Project the carrier packet of
\autoref{def:bessel-bhist-carrier-packet} to its $O$, $\nu$, $\sigma$,
$\tau$, $\kappa$, and $\pi$ coordinates. These are exactly the ODE source,
order data, endpoint data, transport rows, and provenance rows named in the
statement.

Repack the projected rows through the $\OdeUp$ naming surface. Since the
carrier stores endpoint comparison as $\hsame$ rows and package provenance as
$\mathsf{Pkg}$ data, the projection never requires a host solution operator
or function equality.
\end{proof}

\begin{theorem}[BesselUp holomorphic consumer boundary]
\label{thm:bessel-holomorphic-consumer-boundary}
Every holomorphic read accepted by the $\BesselUp$ root packet is determined
by the $\HolomorphicUp$ source row, the solution endpoint rows, the
order/index row, the $\hsame$ transport packet, and the shared
$\mathsf{Pkg}$ provenance of \autoref{def:bessel-bhist-carrier-packet}. It
does not add a separate complex-analysis carrier or a standard holomorphic
function bridge.
\end{theorem}
\begin{proof}
Read the $H$ coordinate of the carrier packet together with the endpoint
rows, order/index row, transport packet, and provenance. These are the only
rows exposed to the holomorphic consumer side.

Transport the endpoint histories along the componentwise $\hsame$ rows in
$\kappa$. Repacking those rows through the $\HolomorphicUp$ naming surface
gives the same holomorphic consumer boundary, while the packet contains no
field for a separate carrier or standard bridge.
\end{proof}
\leanchecked{BEDC.Derived.BesselUp.BesselRootPacket\_holomorphic\_consumer\_boundary}

\begin{theorem}[BesselUp recurrence ledger exactness]
\label{thm:bessel-recurrence-ledger-exactness}
The recurrence ledger $\rho$ in a $\BesselUp$ carrier packet exhausts the
accepted recurrence reads for the carried order/index row and solution
endpoint rows. Each recurrence read is a finite $\Cont$ route transported by
the packet's $\hsame$ rows and justified by the same $\mathsf{Pkg}$
provenance; conversely each ledger entry repacks as one accepted recurrence
read.
\end{theorem}
\leanchecked{BEDC.Derived.BesselUp.BesselRootPacket\_recurrence\_ledger\_exactness}
\begin{proof}
Project the recurrence ledger $\rho$ from
\autoref{def:bessel-bhist-carrier-packet}. Each entry has source endpoints,
target endpoints, order/index data, and a displayed finite $\Cont$ route.

Use the $\hsame$ packet $\kappa$ to transport the endpoints and order row
inside the shared $\mathsf{BHist}$ carrier. The $\mathsf{Pkg}$ row $\pi$
records that the transported route belongs to the same Bessel packet.

For the converse direction, start from any accepted recurrence read. Its
fields are exactly a source endpoint row, target endpoint row, order/index
row, finite continuation route, transport row, and provenance row, so it
repackages as an entry of $\rho$.
\end{proof}

\begin{theorem}[BesselUp name-certificate obligation surface]
\label{thm:bessel-namecert-obligation-surface}
The $\BesselUp$ root naming obligation surface consists exactly of the
carrier packet of \autoref{def:bessel-bhist-carrier-packet}, the $\OdeUp$
source obligation of \autoref{thm:bessel-root-ode-source-obligation}, the
$\HolomorphicUp$ consumer boundary of
\autoref{thm:bessel-holomorphic-consumer-boundary}, and the recurrence ledger
exactness of \autoref{thm:bessel-recurrence-ledger-exactness}. This surface
contains no host analytic primitive, no standard Bessel model, and no
function-space quotient row.
\end{theorem}
\leanchecked{BEDC.Derived.BesselUp.BesselRootPacket\_namecert\_obligation\_surface}
\begin{proof}
Assemble the packet by projection from
\autoref{def:bessel-bhist-carrier-packet}. The ODE source, holomorphic source,
order/index row, endpoints, recurrence ledger, transport rows, and provenance
are all coordinates of the same $\mathsf{BHist}$ carrier.

Apply \autoref{thm:bessel-root-ode-source-obligation} to identify the ODE
source side and \autoref{thm:bessel-holomorphic-consumer-boundary} to
identify the holomorphic consumer side. Both projections use the same
transport packet and provenance row.

Finally apply \autoref{thm:bessel-recurrence-ledger-exactness}. The finite
recurrence ledger covers exactly the accepted recurrence reads, so repacking
the three projections gives the whole name-certificate obligation surface
without adding analytic primitives or quotient data.
\end{proof}

\begin{theorem}[BesselUp carrier, equation, and order obligation]
\label{thm:bessel-carrier-equation-order-obligation}
An accepted $\BesselUp$ name-certificate packet binds the Bessel ODE source row
$O$, the order and index row $\nu$, the two solution endpoint rows, the
ODE-side endpoint classifier, the $\hsame$ transport packet, and the shared
$\mathsf{Pkg}$ provenance to one $\mathsf{BHist}$ carrier packet
$\mathcal B$ of \autoref{def:bessel-bhist-carrier-packet}. No consumer can
read the equation row or the order row through a second carrier, a host
analytic solution operator, or an equality of ordinary functions.
\end{theorem}
\leanchecked{BEDC.Derived.BesselUp.BesselRootPacket\_carrier\_equation\_order\_obligation}
\begin{proof}
Project the accepted root packet through
\autoref{thm:bessel-namecert-obligation-surface}. Its carrier coordinate is
the packet $\mathcal B$ of \autoref{def:bessel-bhist-carrier-packet}, whose
$O$, $\nu$, endpoint, transport, and provenance coordinates are the displayed
ODE source, order and index, endpoint, $\hsame$, and $\mathsf{Pkg}$ rows.

Apply \autoref{thm:bessel-root-ode-source-obligation}. This projection already
identifies the ODE row, the order and index row, the endpoint classifiers, and
the provenance as rows of the same root packet. Since the obligation surface
has no field for a second carrier, host solution operator, or ordinary-function
equality, those data are not exported.
\end{proof}

\begin{theorem}[BesselUp recurrence Cont ledger obligation]
\label{thm:bessel-recurrence-cont-ledger-obligation}
Every recurrence read accepted by a $\BesselUp$ name-certificate packet is
exactly one of the finite $\Cont$ ledger rows already exposed by
\autoref{thm:bessel-recurrence-ledger-exactness}, transported along the packet's
$\hsame$ rows and justified by the same $\mathsf{Pkg}$ provenance. Conversely,
each such finite ledger row repacks as an accepted recurrence read for the
same order and endpoint rows.
\end{theorem}
\leanchecked{BEDC.Derived.BesselUp.BesselRootPacket\_recurrence\_cont\_ledger\_obligation}
\begin{proof}
Start from an accepted recurrence read of the name-certificate packet. Apply
\autoref{thm:bessel-namecert-obligation-surface} to place the read inside the
carrier packet, with the same order row, endpoint rows, transport packet, and
provenance as the root surface.

Now apply \autoref{thm:bessel-recurrence-ledger-exactness}. It says that the
recurrence ledger $\rho$ exhausts accepted recurrence reads and that each read
is a finite $\Cont$ route transported by the packet's $\hsame$ rows. The
reverse direction in the same theorem repacks each finite ledger entry as an
accepted recurrence read, so the exposed ledger is exact.
\end{proof}

\begin{theorem}[BesselUp asymptotic ledger scope]
\label{thm:bessel-asymptotic-ledger-scope}
Any holomorphic or asymptotic consumer read of a $\BesselUp$ packet is scoped
to the displayed Bessel ODE source row, the $\HolomorphicUp$ source row, the
order and index row, the solution endpoint rows, the recurrence ledger, the
$\hsame$ transport packet, and the shared $\mathsf{Pkg}$ provenance. The read
exposes no standard analytic primitive, no host Bessel function model, no
function-space quotient, and no separate asymptotic carrier.
\end{theorem}
\leanchecked{BEDC.Derived.BesselUp.BesselRootPacket\_asymptotic\_ledger\_scope}
\begin{proof}
For holomorphic reads, apply
\autoref{thm:bessel-holomorphic-consumer-boundary}. It restricts the consumer
surface to the $\HolomorphicUp$ source row, endpoint rows, order and index row,
transport packet, and provenance of the shared carrier.

For recurrence and endpoint movement, apply
\autoref{thm:bessel-recurrence-ledger-exactness}. The only routes available to
the consumer are the finite recurrence $\Cont$ rows transported by the same
$\hsame$ packet and justified by the same provenance.

Finally compose these two projections with
\autoref{thm:bessel-namecert-obligation-surface}. The resulting surface is the
ODE row, HolomorphicUp row, order row, endpoints, recurrence ledger, transport
packet, and $\mathsf{Pkg}$ row of one $\mathsf{BHist}$ carrier. Because the
root surface excludes host analytic primitives, standard Bessel models,
function-space quotients, and separate carriers, the asymptotic read has no
additional row to expose.
\end{proof}

\begin{theorem}[BesselUp public certificate export]
\label{thm:bessel-public-certificate-export}
The public $\BesselUp$ certificate is exactly the finite packet assembled from
the Bessel ODE source row, the $\HolomorphicUp$ source row, the order and index
rows, the solution endpoint rows, the recurrence ledger, the asymptotic ledger
scope, componentwise $\hsame$ transport, finite $\Cont$ routes, and the
$\mathsf{Pkg}$ provenance already fixed in this chapter. It exports analytic
existence, analytic uniqueness, host Bessel functions, asymptotic expansions as
functions, and a standard bridge nowhere in the public packet.
\end{theorem}
\begin{proof}
Start with the ODE and holomorphic source boundary. Apply
\autoref{thm:bessel-root-ode-source-obligation} to fix the displayed Bessel ODE
row and \autoref{thm:bessel-holomorphic-consumer-boundary} to bind every
holomorphic consumer read to the same source row, endpoint rows, order and
index rows, transport packet, and provenance.

Next project the recurrence data. By
\autoref{thm:bessel-recurrence-ledger-exactness}, each recurrence read is one
of the finite ledger entries transported by the packet's componentwise
$\hsame$ rows and justified by the same $\mathsf{Pkg}$ provenance.

Finally combine those rows with
\autoref{thm:bessel-namecert-obligation-surface} and
\autoref{thm:bessel-asymptotic-ledger-scope}. The first theorem supplies the
carrier, classifier, transport, and name-certificate obligations; the second
restricts holomorphic and asymptotic-facing reads to the displayed finite
packet. No coordinate remains for analytic existence, uniqueness, host Bessel
functions, functional asymptotic expansions, or a standard bridge.
\end{proof}

\closureat{BesselUp}{publicStr}[BEDC.Derived.BesselUp.BesselRootPacket\_namecert\_obligation\_surface]
\closureat{BesselUp}{bridgedStr}[BEDC.Derived.BesselUp.BesselRootPacket\_namecert\_obligation\_surface]

\begin{closurestatus}{\BesselUp}
  \constructivestory{A Bessel packet is generated by placing an $\OdeUp$ row for the displayed Bessel differential equation beside a $\HolomorphicUp$ row for the disk-local solution histories, then adding order and index histories, solution endpoints, recurrence entries, transport rows, and package provenance. The carrier accepts only this finite packet $(O,H,\nu,\sigma,\tau,\rho,\kappa,\pi)$; a host analytic solution operator, ordinary-function equality, function-space quotient, or standard Bessel model is not part of the surface. Two accepted packets classify together by componentwise $\hsame$ transport of the ODE source, holomorphic source, order and index rows, solution endpoints, recurrence ledgers, and provenance, with finite $\Cont$ routes carrying each recurrence readback. The source specification is the paired ODE and holomorphic certificate boundary, the pattern specification is the Bessel-equation solution surface with its recurrence laws, and the ledger keeps the order/index row, solution endpoints, recurrence routes, endpoint classifiers, and shared $\Pkg$ row visible. The five name-certificate obligations are witnessed by an accepted root packet, by the equivalence laws of the component classifiers, and by recurrence-ledger exactness, which says every transported recurrence read repacks as one of the same finite ledger entries. A packet history $h$ lies in the signature gap of a $\BesselUp$ token $p$ when the source-domain accepts its ODE and holomorphic rows, the signature relation pairs it with an answer Bessel packet and evidence ledger, and the token-introduction relation names that answer as $p$; unfolded, the order, solution endpoints, and recurrence ledger are precisely the rows encoded by $p$. The chapter registers this ODE-and-recurrence packet under the name $\BesselUp$; it introduces no analytic existence or uniqueness theorem, no asymptotic expansion package, no host Bessel function, and no standard bridge.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{The obligation scope binds the Bessel ODE row, $\HolomorphicUp$ source row, order and index rows, solution endpoints, recurrence ledger, componentwise $\hsame$ transport, finite $\Cont$ routes, and $\Pkg$ provenance of the root packet.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.BesselUp.BesselRootPacket\_namecert\_obligation\_surface}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim analytic existence or uniqueness, asymptotic expansions, host Bessel functions, function-space quotients, or a standard bridge.}
  \upgradepath{Mature treatment remains for the displayed ODE, holomorphic, recurrence-ledger, classifier, and provenance bridge surface.}
\end{closurestatus}
