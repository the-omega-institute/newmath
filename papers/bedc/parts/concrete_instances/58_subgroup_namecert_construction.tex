\input{parts/concrete_instances/58_subgroup_namecert_construction_core}
\input{parts/concrete_instances/58_subgroup_namecert_construction_quotient_empty}
\input{parts/concrete_instances/58_subgroup_namecert_quotient_classifier}

\begin{theorem}[Centralizer quotient classifier kernel equivalence]
\label{thm:subgroup-centralizer-quotient-classifier-kernel-iff}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x,y:\mathsf{BHist}$ satisfies
$$
Q^{\mathrm{std}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y).
$$
\end{theorem}
\begin{proof}
For the forward direction, unpack the standard quotient classifier. The
endpoint clauses give $N_a(x)$ and $N_a(y)$, and the quotient witness gives a
centralizer element whose right multiplication carries $x$ to $y$.

It remains to expose the relative kernel word. Multiply the endpoint
comparison on the left by $\mathsf{inv}(x)$; associativity, the left inverse
law, the unit law, and multiplication congruence identify the witness with
$\mathsf{mul}(\mathsf{inv}(x),y)$ up to $\hsame$. Carrier transport for $Z_a$
then moves centralizer membership to the displayed kernel carrier.

For the reverse direction, start from the relative kernel classifier. Its
endpoint clauses are already the two normalizer clauses needed by the standard
quotient classifier.

Choose the quotient witness $z:=\mathsf{mul}(\mathsf{inv}(x),y)$. Associativity
and the right inverse law identify $\mathsf{mul}(x,z)$ with $y$ up to
$\hsame$, so the kernel clause supplies the carried centralizer witness and
the classifier repacks.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerQuotientClassifier\_kernel\_iff}

\begin{theorem}[Centralizer quotient-kernel witness classifier equivalence]
\label{thm:subgroup-centralizer-quotient-kernel-witness-classifier-iff}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x,y:\mathsf{BHist}$ satisfies
$$
Q^{\mathrm{wit}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y).
$$
Here $Q^{\mathrm{wit}}$ presents the quotient by a right witness $z$ with
$Z_a(z)$ and $\hsame(y,\mathsf{mul}(x,z))$.
\end{theorem}
\begin{proof}
In the forward direction, unpack the right-witness classifier. Left-multiply
the endpoint comparison by $\mathsf{inv}(x)$ and reassociate; the left inverse
and unit laws reduce the exposed word to the witness $z$.

Carrier transport for $Z_a$ moves $Z_a(z)$ across that comparison, producing
$Z_a(\mathsf{mul}(\mathsf{inv}(x),y))$. Together with the two endpoint
normalizer clauses this is the relative kernel classifier.

Conversely, take the relative kernel word itself as the right witness.
Associativity, the right inverse law, and the unit law identify
$y$ with $\mathsf{mul}(x,\mathsf{mul}(\mathsf{inv}(x),y))$ up to $\hsame$,
so the same kernel carrier repacks as the witness classifier.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerQuotientKernel\_witness\_classifier\_iff}

\begin{theorem}[Centralizer right coset classifier quotient-kernel equivalence]
\label{thm:subgroup-centralizer-right-coset-classifier-quotient-kernel-iff}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x,y:\mathsf{BHist}$ satisfies
$$
Q^{\mathrm{cos}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y).
$$
\end{theorem}
\begin{proof}
The forward implication starts with a right-coset witness. The group
left-multiplication equation over the empty unit turns its endpoint comparison
into the equality of the witness with the relative kernel word.

Carrier transport for $Z_a$ then transfers centralizer membership from the
witness to $\mathsf{mul}(\mathsf{inv}(x),y)$, while the endpoint normalizer
clauses are unchanged.

For the reverse implication, choose
$z:=\mathsf{mul}(\mathsf{inv}(x),y)$. The same left-multiplication equation,
read backwards, gives the coset endpoint comparison, and the kernel carrier is
exactly the required $Z_a(z)$ witness.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerRightCosetClassifier\_quotientKernel\_iff}

\begin{theorem}[Centralizer right quotient classifier kernel equivalence]
\label{thm:subgroup-centralizer-right-quotient-classifier-kernel-iff}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x,y:\mathsf{BHist}$ satisfies
$$
Q^{\mathrm{right}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y).
$$
\end{theorem}
\begin{proof}
For the forward direction, unpack the right quotient witness and expose
$\mathsf{mul}(\mathsf{inv}(x),y)$ by multiplying the endpoint comparison on
the left. Associativity, left inverse, unit, and multiplication congruence
identify the exposed word with the carried witness.

Carrier transport converts that witness membership into the kernel carrier,
and the two endpoint normalizer clauses pass through unchanged.

For the reverse direction, use the kernel word as the right quotient witness.
Reassociation followed by the right inverse and unit laws gives the endpoint
comparison required by the right quotient classifier.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerRightQuotientClassifier\_kernel\_iff}

\begin{theorem}[Centralizer normalizer quotient classifiers coincide]
\label{thm:subgroup-centralizer-normalizer-quotient-classifiers-coincide}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x,y:\mathsf{BHist}$ satisfies
$$
Q^{\mathrm{cos}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y).
$$
\end{theorem}
\begin{proof}
This is the canonical coincidence row for the centralizer-normalizer quotient
site. It reads the right-coset classifier and the relative-kernel classifier
as two presentations of the same normalizer quotient relation.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerNormalizerQuotientClassifier\_coincide}

\begin{theorem}[Centralizer quotient presentation equivalence package]
\label{thm:subgroup-centralizer-quotient-presentation-equivalence-package}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x,y:\mathsf{BHist}$ has a quotient presentation independent of the chosen
centralizer-normalizer classifier. The standard quotient classifier
$Q^{\mathrm{std}}_{Z_a\leq N_a}(x,y)$, the right-witness classifier
$Q^{\mathrm{wit}}_{Z_a\leq N_a}(x,y)$, the right-coset classifier
$Q^{\mathrm{cos}}_{Z_a\leq N_a}(x,y)$, and the relative-kernel classifier
$Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y)$ are mutually pinned by the
three displayed equivalence rows
$$
Q^{\mathrm{std}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y),
$$
$$
Q^{\mathrm{wit}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y),
$$
$$
Q^{\mathrm{cos}}_{Z_a\leq N_a}(x,y)\Longleftrightarrow
Q^{\mathrm{ker}}_{Z_a\triangleleft N_a}(x,y),
$$
and the derived equivalence between the right-witness and right-coset
presentations obtained through the shared quotient-kernel classifier.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:subgroup-centralizer-quotient-classifier-kernel-iff}
to identify the standard classifier with the relative-kernel classifier.
Apply Theorem~\ref{thm:subgroup-centralizer-quotient-kernel-witness-classifier-iff}
to identify the right-witness classifier with the same kernel classifier.

Apply Theorem~\ref{thm:subgroup-centralizer-right-coset-classifier-quotient-kernel-iff}
to identify the right-coset classifier with that kernel classifier. Apply
Theorem~\ref{thm:subgroup-centralizer-right-quotient-classifier-kernel-iff}
for the parallel right quotient presentation.

The right-witness/right-coset equivalence is obtained by composing the
right-witness-to-kernel implication with the kernel-to-right-coset implication,
and conversely composing the right-coset-to-kernel implication with the
kernel-to-right-witness implication. Thus all four presentations classify the
same pairs $(x,y)$ in the normalizer carrier $N_a$.

The ten-field fit is: traditional target quotient presentation independence;
BEDC source $\GroupUp$ plus $\SubgroupUp$ over $\mathsf{BHist}$; carrier
$N_a$; classifier the shared quotient-kernel relation; stability the checked
equivalence rows above; ledger right witness, coset witness, and relative
kernel word; core theorem this package; exactness mutual presentation
equivalence; downstream consumer $\QuotientGroupUp$; status
$\mathsf{ProofTarget}$.
\end{proof}

\begin{theorem}[Centralizer normalizer right quotient classifier transitivity]
\label{thm:subgroup-centralizer-right-quotient-classifier-trans}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence, and
two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, the
right-witness quotient classifier $Q_{Z_a\leq N_a}$ is transitive:
$$
Q_{Z_a\leq N_a}(x,y)\land Q_{Z_a\leq N_a}(y,z)
\Longrightarrow Q_{Z_a\leq N_a}(x,z).
$$
\end{theorem}
\begin{proof}
Convert both right-witness classifier hypotheses to the relative-kernel
classifier using the centralizer-normalizer quotient classifier equivalence.
The kernel classifier is transitive by the empty-unit centralizer quotient
kernel theorem: the two relative kernel words multiply, and the middle
endpoint cancels by associativity, unit, congruence, and inverse laws. Convert
the resulting kernel classifier back to the right-witness classifier.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerRightQuotientClassifier\_trans\_from\_empty\_unit}

\begin{theorem}[Centralizer right quotient classifier hsame transport]
\label{thm:subgroup-right-quotient-classifier-hsame-transport}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence, and
two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, if
$Q_{Z_a\leq N_a}(x,y)$ is presented by the right-witness quotient classifier
and $x\hsame x'$ and $y\hsame y'$, then the same right-witness quotient
classifier holds at $(x',y')$.
\end{theorem}
\begin{proof}
Translate the right-witness quotient classifier through the quotient-kernel
equivalence to obtain $Q_{Z_a\triangleleft N_a}(x,y)$. The quotient-kernel
transport theorem moves both endpoints and the centralizer kernel row across
the two $\hsame$ comparisons. Translating back through the same equivalence
repackages the transported kernel as the right-witness quotient classifier at
$(x',y')$.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerRightQuotientClassifier\_hsame\_transport}

\begin{theorem}[Centralizer right coset classifier hsame transport]
\label{thm:subgroup-centralizer-right-coset-classifier-hsame-transport}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence, and
two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, if
$Q_{Z_a\leq N_a}(x,y)$ is presented by the right-coset classifier and
$x\hsame x'$ and $y\hsame y'$, then the same right-coset classifier holds at
$(x',y')$.
\end{theorem}
\begin{proof}
Translate the right-coset classifier through the quotient-kernel equivalence
to obtain $Q_{Z_a\triangleleft N_a}(x,y)$. The quotient-kernel transport row
moves both normalizer endpoints and the displayed kernel carrier across the
two $\hsame$ endpoint comparisons. Translating back through the same
coset/kernel equivalence repackages the transported kernel as the
right-coset classifier at $(x',y')$.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerRightCosetClassifier\_hsame\_transport}

\begin{theorem}[Centralizer right coset classifier transitivity]
\label{thm:subgroup-centralizer-right-coset-classifier-trans}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$ satisfying
associativity, right-unit, and multiplication-congruence laws, the centralizer
right-coset classifier is transitive:
$$
Q^{\mathrm{right}}_{Z_a\leq N_a}(x,y)\land
Q^{\mathrm{right}}_{Z_a\leq N_a}(y,z)
\Longrightarrow Q^{\mathrm{right}}_{Z_a\leq N_a}(x,z).
$$
\end{theorem}
\begin{proof}
Unpack the two right-coset witnesses. The first supplies a centralizer element
$u$ with $y\hsame \mathsf{mul}(x,u)$, and the second supplies a centralizer
element $v$ with $z\hsame \mathsf{mul}(y,v)$. Product closure for the
centralizer carrier gives a carried witness $\mathsf{mul}(u,v)$. Multiplication
congruence transports the second endpoint along the first comparison, and
associativity identifies
$\mathsf{mul}(\mathsf{mul}(x,u),v)$ with
$\mathsf{mul}(x,\mathsf{mul}(u,v))$. The endpoint clauses are inherited from
the two classifier hypotheses.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerRightCosetClassifier\_trans\_from\_empty\_unit}

\begin{theorem}[Centralizer quotient kernel empty fiber]
\label{thm:subgroup-centralizer-quotient-kernel-empty-fiber}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x:\mathsf{BHist}$ satisfies
$$
Q_{Z_a\triangleleft N_a}(\mathsf{e},x)\Longleftrightarrow Z_a(x).
$$
\end{theorem}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerQuotientKernel\_empty\_fiber\_iff}
\begin{proof}
For the forward direction, unpack the relative-kernel classifier
$Q_{Z_a\triangleleft N_a}(\mathsf{e},x)$. Its kernel clause gives
$Z_a(\mathsf{mul}(\mathsf{inv}(\mathsf{e}),x))$.

The inverse identity row identifies $\mathsf{inv}(\mathsf{e})$ with
$\mathsf{e}$, and the left-unit law identifies
$\mathsf{mul}(\mathsf{e},x)$ with $x$. The $Z_a$ carrier-transport row from
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate} carries the
kernel witness across this endpoint comparison and gives $Z_a(x)$.

For the reverse direction, assume $Z_a(x)$. The empty centralizer row gives
$Z_a(\mathsf{e})$, and
\autoref{thm:subgroup-centralizer-self-normalizer} sends both $Z_a(\mathsf{e})$
and $Z_a(x)$ into the normalizer carrier $N_a$.

It remains to supply the kernel word. The same inverse identity and left-unit
comparison identify $\mathsf{mul}(\mathsf{inv}(\mathsf{e}),x)$ with $x$; by
transporting $Z_a(x)$ along the reverse comparison, we obtain the carried
kernel clause and package
$Q_{Z_a\triangleleft N_a}(\mathsf{e},x)$.
\end{proof}

\begin{theorem}[Centralizer normalizer coset classifier identity fiber]
\label{thm:subgroup-centralizer-normalizer-coset-classifier-identity-fiber}
For a BEDC history-level $\GroupUp$ operation over $\mathsf{BHist}$
satisfying the same associativity, unit, multiplication-congruence,
carrier-transport, and two-sided inverse laws as in
\autoref{thm:subgroup-centralizer-normalizer-tower-certificate}, every
$x:\mathsf{BHist}$ satisfies
$$
Q_{Z_a\leq N_a}(\mathsf{e},x)\Longleftrightarrow Z_a(x).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:subgroup-centralizer-normalizer-quotient-classifiers-coincide} at
the endpoint pair $(\mathsf{e},x)$. It identifies the right-coset classifier
$Q_{Z_a\leq N_a}(\mathsf{e},x)$ with the relative-kernel classifier
$Q_{Z_a\triangleleft N_a}(\mathsf{e},x)$.

Then apply
\autoref{thm:subgroup-centralizer-quotient-kernel-empty-fiber}. Its empty
fiber calculation identifies
$Q_{Z_a\triangleleft N_a}(\mathsf{e},x)$ with $Z_a(x)$. Transitivity of these
two equivalences gives the displayed identity fiber for
$Q_{Z_a\leq N_a}$.
\end{proof}
\leanchecked{BEDC.Derived.SubgroupUp.SubgroupCentralizerRightQuotientClassifier\_empty\_fiber\_iff}

Per-object template: traditional target identity coset fiber for the
centralizer-normalizer quotient; BEDC source $\GroupUp$ plus $\SubgroupUp$
over $\mathsf{BHist}$; carrier $N_a$; classifier $Q_{Z_a\leq N_a}$; stability
identity, inclusion, left-unit endpoint transport, and $Z_a$ carrier transport;
ledger identity representative and right witness; exactness predicate identity
fiber equals lower carrier; downstream consumer $\QuotientGroupUp$; status
$\mathsf{ProofTarget}$.
