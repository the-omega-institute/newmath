\chapter{A Concrete Naming Certificate for $\FiniteNetTriangleReplayUp$}
\label{ch:concrete-instances-finite-net-triangle-replay-namecert}
\origin{ai}

The $\FiniteNetTriangleReplayUp$ packet realizes the compact-uniform-continuity triangle-estimate branch as a finite replay surface: compact finite-net source cells, continuous-map local moduli, source and target metric triangle rows, rational tolerance splits, regular readback, and a terminal Real-facing seal are displayed before a uniform-modulus consumer reads the handoff. It is localized to the finite-net source and target triangle estimate route of \autoref{ch:visions-compact-uniform-continuity}; it does not claim broad compactness, select a global modulus outside the ledger, quotient metric points, choose centers externally, or assert ambient $\RealUp$ completeness.

\begin{definition}[Finite net triangle replay carrier]
\label{def:finite-net-triangle-replay-carrier}
A $\FiniteNetTriangleReplayUp$ carrier is a finite $\BHist$ packet
$$
T=(K,F,U,M,R,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row, including the displayed center bundle and coverage ledger.
\item $F$ is the $\ContinuousMapUp$ graph and pointwise-modulus row read on the bundled source centers.
\item $U$ is the $\UniformModulusUp$ consumer-facing handoff row produced by the replay.
\item $M$ is the $\MetricSpaceUp$ source and target triangle ledger used for the two triangle estimates.
\item $R$ is the $\RatUp$ tolerance-split row, including the finite third-radius and lower-bound comparisons.
\item $Q$ is the $\RegSeqRatUp$ regular readback row for rational comparison data that must be sealed for Real-facing consumers.
\item $E$ is the terminal $\RealUp$ seal row.
\item $H$ stores componentwise $\hsame$ transport for the compact source, continuous map, uniform-modulus handoff, metric triangle ledger, rational split, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a compact finite-net request through the source triangle, local modulus, target triangle, rational readback, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\FiniteNetTriangleReplayUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\MetricSpaceUp$, $\RatUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteNetTriangleReplayUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,F,U,M,R,Q,E,H,C,P,N$. No external center selector, quotient point equality, global compactness theorem, selected uniform modulus, or ambient Real completeness coordinate is present.
\end{definition}

\begin{theorem}[Finite net triangle replay NameCert obligations]
\label{thm:finite-net-triangle-replay-namecert-obligations}
For an accepted $\FiniteNetTriangleReplayUp$ carrier $T=(K,F,U,M,R,Q,E,H,C,P,N)$, the local $\NameCert_{\FiniteNetTriangleReplayUp}$ surface consists exactly of carrier exposure for the compact finite-net source, continuous-map local rows, uniform-modulus consumer row, source and target metric triangle ledger, rational tolerance split, regular-rational readback, Real seal, componentwise transport, continuation replay, provenance, and local naming rows. A consumer must read the compact source row $K$, local map row $F$, triangle ledger $M$, and tolerance split $R$ before it can inspect the uniform-modulus handoff $U$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-net-triangle-replay-carrier}. The tuple displays the compact finite-net source, continuous-map row, uniform-modulus handoff, metric triangle ledger, rational split, regular readback, Real seal, transport, replay, provenance, and local name.

The source triangle estimate reads $K,M,R$ first. These rows identify the bundled center, the source distance comparison, and the rational tolerance split that keeps the second source point inside the same local-modulus window.

The local modulus and target triangle estimates then read $F,M,R,Q,E$. The graph and metric rows produce the two target-side bounds, while the regular-rational and Real rows seal the displayed tolerance comparison.

Finally $H,C,P,N$ preserve the same route. Since the carrier has no external center selector, quotient point equality, global compactness theorem, selected uniform modulus, or ambient Real completeness coordinate, the local NameCert surface is exhausted by the listed rows.
\end{proof}

\begin{theorem}[Finite net triangle replay uniform-modulus handoff]
\label{thm:finite-net-triangle-replay-uniform-modulus-handoff}
Every $\UniformModulusUp$ consumer reading an accepted $\FiniteNetTriangleReplayUp$ packet factors through
$$
K,F,M,R \longmapsto Q,E \longmapsto U,H,C,P,N .
$$
The handoff is the finite source-and-target triangle replay over compact-net centers, continuous-map local rows, metric-space triangle ledgers, rational tolerance splits, regular readback, and Real sealing. It exports no broad compactness theorem, external center choice, quotient point equality, selected modulus outside the packet, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:finite-net-triangle-replay-namecert-obligations}. They require the compact finite-net source $K$, continuous-map local rows $F$, triangle ledger $M$, and rational tolerance split $R$ to be displayed before $U$ is consumed.

The regular readback $Q$ and Real seal $E$ bind the rational estimates used by the source and target triangle calculations. A uniform-modulus consumer therefore reads concrete tolerance data rather than an unstated external modulus.

The transport, replay, provenance, and naming rows $H,C,P,N$ keep the handoff on the same finite route. The intended Lean surface requires nontriviality and field faithfulness for this packet, recorded as \texttt{Nontrivial FiniteNetTriangleReplayUp} and \texttt{FieldFaithful FiniteNetTriangleReplayUp} instances, so the replay cannot collapse into an empty or classifier-erasing carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteNetTriangleReplayUp$ uniform-modulus read factors through the compact finite-net source row, continuous-map local row, metric triangle ledger, rational tolerance split, regular readback, Real seal, componentwise transport, replay, provenance, and local $\NameCert$ before $\UniformModulusUp$ is consumed.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, metricspace, real: $\FiniteNetTriangleReplayUp$ is not bijective to any listed sibling because it jointly carries compact finite-net centers, continuous local rows, source and target triangle replay, rational tolerance splits, regular readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{FiniteNetTriangleReplayUp}{seedStr}

\begin{closurestatus}{\FiniteNetTriangleReplayUp}
  \constructivestory{A $\FiniteNetTriangleReplayUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap local rows, UniformModulus consumer rows, MetricSpace triangle ledgers, RatUp tolerance splits, RegSeqRat readback, RealUp sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-net-triangle-replay-carrier}, \autoref{thm:finite-net-triangle-replay-namecert-obligations}, and \autoref{thm:finite-net-triangle-replay-uniform-modulus-handoff} seed the finite source-and-target triangle replay route for compact-uniform-continuity uniform-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, broad compactness, external center choice, quotient point equality, selected uniform moduli, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net source binding, continuous local-row exposure, source and target triangle replay, rational tolerance split, regular readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.FiniteNetTriangleReplayUp.}
\end{closurestatus}
