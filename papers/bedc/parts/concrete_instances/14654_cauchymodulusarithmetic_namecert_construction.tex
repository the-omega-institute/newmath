\chapter{A Concrete Naming Certificate for $\CauchyModulusArithmeticUp$}
\label{ch:concrete-instances-cauchymodulusarithmetic-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, explicit Cauchy
moduli are part of the data by which regular rational sequences are consumed:
addition, multiplication, and common-tail comparison require visible schedules
rather than a hidden appeal to completed real equality. The
$\CauchyModulusArithmeticUp$ packet records this arithmetic surface as a finite
NameCert bridge between the modulus chapters
\autoref{ch:concrete-instances-cauchymodulus-namecert} and
\autoref{ch:concrete-instances-cauchymodulusmeet-namecert}, the regular
operation chapters \autoref{ch:concrete-instances-regularcauchysum-namecert}
and \autoref{ch:concrete-instances-regularcauchyproduct-namecert}, and the
real seal surface of \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Cauchy-modulus arithmetic carrier]
\label{def:cauchy-modulus-arithmetic-carrier}
A $\CauchyModulusArithmeticUp$ carrier is a finite $\BHist$ packet
$$
A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S_0$ and $S_1$ are the two $\StreamNameUp$ windows whose rational
observations are read back through $\RegSeqRatUp$ rows.
\item $\mu_0$ and $\mu_1$ are the two source $\CauchyModulusUp$ schedules,
each carrying its own precision request, threshold row, and late-pair ledger.
\item $\mu_{\wedge}$ is the shared-threshold $\CauchyModulusMeetUp$ row that
places the two source schedules on one dyadic tolerance surface.
\item $\sigma$ and $\pi$ are the $\RegularCauchySumUp$ and
$\RegularCauchyProductUp$ consumer rows; they read only the two source
schedules, the shared meet row, and the regular rational readbacks.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the split
tolerances for the sum route, the product route, and the shared meet route.
\item $W$ is the finite $\StreamNameUp$ window ledger that names the inspected
late observations for both source streams and both arithmetic consumers.
\item $R$ stores the $\RegSeqRatUp$ readbacks obtained from the displayed
windows, tolerance rows, and modulus schedules.
\item $E$ is the $\RealUp$ seal row reached only through the regular rational
sum or product handoff.
\item $H$ records componentwise $\hsame$ transport for the two streams, two
source schedules, shared meet row, sum row, product row, dyadic tolerance
ledger, window ledger, regular readbacks, real seal rows, continuation rows,
provenance, and local name.
\item $C$ records displayed $\Cont$ replay from a precision request through
the two source schedules and the shared meet row, then through $D,W,R$ to
$\sigma$, $\pi$, and $E$.
\item $P$ is the $\Pkg$ provenance over $\CauchyModulusUp$,
$\CauchyModulusMeetUp$, $\RegularCauchySumUp$,
$\RegularCauchyProductUp$, $\DyadicRatCoreUp$, $\StreamNameUp$,
$\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyModulusArithmeticUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only by transporting these displayed
rows to a common finite surface. It has no coordinate for quotient streams,
selected limits, host real equality, or ambient completeness.
\end{definition}
\leandef{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier}

\begin{theorem}[Cauchy-modulus arithmetic NameCert obligations]
\label{thm:cauchy-modulus-arithmetic-namecert-obligations}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N)$, the local
$\NameCert_{\CauchyModulusArithmeticUp}$ surface consists exactly of five
obligation rows:
\begin{enumerate}
\item carrier admission for the displayed packet with two source modulus
schedules, one shared meet row, two arithmetic consumer rows, tolerance
ledgers, windows, readbacks, seal rows, transport, continuation, provenance,
and local name;
\item classifier transport to a common $\StreamNameUp$, $\RegSeqRatUp$,
$\CauchyModulusUp$, $\CauchyModulusMeetUp$, and $\DyadicRatCoreUp$ surface;
\item stability of $\mu_0$, $\mu_1$, $\mu_{\wedge}$, $\sigma$, and $\pi$
under the componentwise $\hsame$ rows recorded in $H$;
\item exactness of the dyadic tolerance ledger $D$ as the only source of
split-tolerance and shared-threshold arithmetic reads;
\item $\Cont$ replay, $\Pkg$ provenance, $\RealUp$ handoff, and local
NameCert rows for exactly the displayed packet.
\end{enumerate}
No NameCert obligation is stored outside those five rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-arithmetic-carrier}. The
projection exposes precisely the two stream windows, two source modulus
schedules, shared meet row, sum and product consumers, tolerance ledger,
regular readbacks, real seal rows, transport rows, continuation rows,
provenance row, and local name.

Classifier transport is componentwise. The rows in $H$ move the two source
schedules, the shared meet schedule, and the two arithmetic consumers to the
same displayed stream, regular rational, and dyadic tolerance surface.

The dyadic exactness row is exhausted by $D$. The sum route reads split
tolerances from $D$, the product route reads product bounds from $D$, and the
meet route reads its shared tolerance from the same finite ledger.

Finally replay $C$ and attach $P,N$. These rows route, source, and name the
already displayed packet; they do not introduce quotient streams, selected
limits, host real equality, or ambient completeness. Hence the five listed
rows exhaust the local naming obligations.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic carrier admission row]
\label{thm:cauchy-modulus-arithmetic-namecert-obligation-carrier}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N)$, the
carrier-admission obligation for $\NameCert_{\CauchyModulusArithmeticUp}$ is
exactly the displayed row admitting $S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,
\pi,D,W,R,E,H,C,P,N$. No source stream, modulus schedule, meet row, arithmetic
consumer, tolerance ledger, window ledger, readback, seal, transport, replay,
provenance, or local name may be read outside this finite carrier row.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_namecert\_obligation\_carrier}
\begin{proof}
Project the first obligation row of
\autoref{thm:cauchy-modulus-arithmetic-namecert-obligations}. It is the
carrier-admission row for the tuple displayed in
\autoref{def:cauchy-modulus-arithmetic-carrier}.

The listed coordinates already contain the two source streams, the two source
moduli, the meet row, the sum and product consumers, the dyadic ledger, the
window ledger, the regular readbacks, the Real seal, and the structural rows.
Since the carrier definition has no further coordinate, admission is exhausted
by this one finite row.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic classifier transport row]
\label{thm:cauchy-modulus-arithmetic-namecert-obligation-classifier}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N)$, classifier
transport is permitted only through the common finite surface made from
$\StreamNameUp$, $\RegSeqRatUp$, $\CauchyModulusUp$,
$\CauchyModulusMeetUp$, and $\DyadicRatCoreUp$. In particular the classifier
cannot compare arithmetic carriers by quotient stream equality, selected
limits, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Read the classifier-transport row of
\autoref{thm:cauchy-modulus-arithmetic-namecert-obligations}. It names exactly
the stream, regular rational, source modulus, meet modulus, and dyadic
tolerance surfaces.

The componentwise $\hsame$ rows in $H$ move the two source streams, the two
source schedules, the meet row, the arithmetic consumers, the tolerance ledger,
and the readbacks to that same surface. The classifier therefore has only this
transport route; no quotient, selected-limit, host-equality, or completeness
coordinate is present to compare instead.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic ledger nonescape row]
\label{thm:cauchy-modulus-arithmetic-namecert-obligation-ledger}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N)$, every
completion-facing or $\RealUp$ consumer that reads the seal row $E$ must first
pass through the displayed $\Cont$ replay row $C$, the $\Pkg$ provenance row
$P$, and the local $\NameCert_{\CauchyModulusArithmeticUp}$ row $N$. Thus $E$
is a ledger endpoint for the displayed arithmetic route, not an escape hatch
to quotient streams, selected limits, host real equality, or ambient
completeness.
\end{theorem}
\begin{proof}
Start from a consumer that asks for $E$. The carrier definition places $E$
after the source schedules, shared meet, arithmetic consumers, dyadic ledger,
windows, and regular readbacks.

Replay must then pass through $C$. The continuation row records the finite
route from a precision request through the displayed source schedules and
ledger rows before the Real seal is available.

Finally attach $P$ and $N$. These rows record provenance and local naming for
the same packet, so a consumer of $E$ remains inside the displayed
$\NameCert_{\CauchyModulusArithmeticUp}$ route and cannot extract any
unlisted completion or equality principle.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_ledger\_nonescape}

\begin{theorem}[Cauchy-modulus arithmetic sum and meet closure]
\label{thm:cauchy-modulus-arithmetic-sum-meet-closure}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. Every displayed
precision request for the $\RegularCauchySumUp$ row $\sigma$ is answered by
first reading the two source schedules $\mu_0,\mu_1$, then reading the shared
meet row $\mu_{\wedge}$, and then reading the split dyadic tolerance ledger
$D$. The resulting sum schedule is a finite $\CauchyModulusUp$ read over the
same $\StreamNameUp$ windows and $\RegSeqRatUp$ readbacks, and its shared
threshold is stable under the $\CauchyModulusMeetUp$ transport rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_sum\_meet\_route\_closure}
\begin{proof}
Start with a precision request for $\sigma$. Carrier admission exposes the two
source schedules and the shared meet row before the sum row can be consumed.

Read $D$ at the requested tolerance. Its split-tolerance rows provide the
dyadic requests sent to $\mu_0$ and $\mu_1$, while its shared-threshold row is
the tolerance read by $\mu_{\wedge}$.

Replay the $\Cont$ rows from $C$. They inspect only the windows in $W$ and the
regular rational readbacks in $R$, so the produced sum schedule remains a
finite modulus read over the displayed packet.

Stability follows from $H$ and the meet obligation surface. The shared meet
row transports the two source schedules together, and the sum consumer can
therefore reuse the same threshold packet after componentwise $\hsame$
transport.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_sum\_meet\_closure}

\begin{theorem}[Cauchy-modulus arithmetic Real consumer boundary]
\label{thm:cauchy-modulus-arithmetic-real-consumer-boundary}
Every $\RealUp$ consumer of a $\CauchyModulusArithmeticUp$ packet must factor
through the displayed route
$$
S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N.
$$
The consumer may read the regular sum or product handoff and then the real
seal row $E$, but it cannot extract quotient stream equality, selected limits,
host real equality, or ambient completeness from the arithmetic carrier.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_real\_consumer\_boundary}
\begin{proof}
Begin with a $\RealUp$ consumer route. The carrier has no real-facing
coordinate before the regular rational readback rows $R$ and the seal row $E$.

For the sum route, \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure}
forces the consumer through $\mu_0,\mu_1,\mu_{\wedge},D,W,R$ before $\sigma$
can be sealed. For the product route, the carrier definition gives the same
source schedules, shared meet row, dyadic ledger, windows, and readbacks before
$\pi$ can be sealed.

The remaining rows $H,C,P,N$ only transport, replay, source, and name that
finite route. Since the carrier exposes no quotient stream row, selected-limit
row, host real equality row, or ambient-completeness row, no $\RealUp$ consumer
can extract such data from the arithmetic packet.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic shared threshold totality]
\label{thm:cauchymodulusarithmetic-shared-threshold-totality}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$$
A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N),
$$
every precision request reaching either arithmetic consumer has a displayed
shared-threshold answer in the finite $\BHist$ route. The answer is read through
the two source stream rows $S_0,S_1$, the source schedules $\mu_0,\mu_1$, the meet
row $\mu_{\wedge}$, the dyadic tolerance ledger $D$, the common window $W$, the
regular rational readback $R$, the Real seal row $E$, and the structural rows
$H,C,P,N$; no external threshold oracle or host real comparison supplies the
answer.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_shared\_threshold\_totality}
\begin{proof}
Project the carrier tuple from
\autoref{def:cauchy-modulus-arithmetic-carrier}. The only precision schedules
available to a consumer are the rows $\mu_0,\mu_1$ and the shared meet row
$\mu_{\wedge}$, and all three are inside the same displayed $\BHist$ packet.

Read the dyadic tolerance ledger $D$ next. Its split-tolerance entries address
the two source schedules, while its shared entry addresses $\mu_{\wedge}$ before
the sum row $\sigma$ or product row $\pi$ is consumed.

The window and readback rows $W,R$ then record the finite observations used by
the arithmetic consumer, and $E,H,C,P,N$ only seal, transport, replay, source,
and name that same route. Since the carrier has no coordinate for an external
threshold oracle, the displayed shared-threshold answer is total for every
accepted arithmetic request.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic sum-product threshold stability]
\label{thm:cauchymodulusarithmetic-sum-product-threshold-stability}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$$
A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N),
$$
the threshold route used by the sum consumer $\sigma$ and the threshold route
used by the product consumer $\pi$ remain the same finite shared-meet route after
componentwise $\hsame$ transport and displayed $\Cont$ replay. The stable route
is exactly the route through $S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},D,W,R,E,H,C,P,N$,
not a paired field-transport equivalence between independent arithmetic
packets.
\end{theorem}
\begin{proof}
Start with the sum route. By
\autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure}, $\sigma$ is consumed
only after the two source schedules, the shared meet row, the tolerance ledger,
the common window, and the regular readback have been displayed.

The product route has no separate threshold coordinate in the carrier. Its
consumer row $\pi$ is placed over the same source schedules, the same
$\mu_{\wedge}$ row, and the same $D,W,R$ rows before the Real seal can be read.

Transport by $H$ and replay by $C$ preserve the displayed rows rather than
creating another schedule. Thus both consumers remain attached to the same
shared threshold under the naming rows $P,N$ and the Real seal row $E$.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic Real-seal modulus nonescape]
\label{thm:cauchymodulusarithmetic-real-seal-modulus-nonescape}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$$
A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N),
$$
any $\RealUp$ seal read from $E$ must keep the modulus information inside the
same finite route through $S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,H,C,P,N$.
The seal may certify the displayed arithmetic readback, but it cannot export a
free modulus, quotient-stream equality, selected real representative, host real
equality, or ambient completeness claim.
\end{theorem}
\begin{proof}
Apply the Real consumer boundary above. A Real-facing request reaches $E$ only
after the source schedules, the shared meet row, the arithmetic consumer row,
the dyadic tolerance ledger, the common window, and the regular rational
readback have already been displayed.

Use shared-threshold totality to locate every requested modulus answer inside
the same $\BHist$ packet. The rows $H,C,P,N$ transport, replay, source, and name
that packet without adding a new modulus coordinate.

Finally inspect the carrier inventory. There is no row for quotient-stream
equality, selected representatives, host real equality, ambient completeness, or
an independent modulus export. Hence the Real seal cannot let the modulus data
escape the displayed arithmetic route.
\end{proof}

\begin{theorem}[Common tail window obligation]
\label{thm:cauchy-modulus-arithmetic-common-tail-window}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$$
A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N)
$$
inside the displayed $\BHist$ packet of
\autoref{def:cauchy-modulus-arithmetic-carrier}, the common-tail window read for
any $\RealUp$ or Completion consumer is forced through the route
$$
H,C,P,N:
\hsame,\Cont,\Pkg,\NameCert_{\CauchyModulusArithmeticUp}.
$$
Concretely, $W$ may be consumed only after the two source schedules
$\mu_0,\mu_1$ and the meet schedule $\mu_{\wedge}$ have been displayed, and any
later seal row $E$ must keep the same $\StreamNameUp$, $\RegSeqRatUp$,
$\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$
provenance. No quotient stream, selected limit, host real equality, or ambient
completeness row is exported by this common-tail obligation.
\end{theorem}
\begin{proof}
Project the carrier tuple of
\autoref{def:cauchy-modulus-arithmetic-carrier}. The common window row $W$ occurs
after the two source stream rows, the two source schedules, the shared meet row,
the arithmetic consumers, and the dyadic tolerance ledger.

The $\hsame$ row $H$ then transports those displayed rows componentwise. Thus a
consumer may not replace the common window by a quotient stream or by a host
comparison of completed values.

The replay row $C$ records the finite route from a precision request through
$\mu_0,\mu_1,\mu_{\wedge},D,W,R$ before the arithmetic consumers can reach $E$.
The provenance and local naming rows $P,N$ attach that same route to
$\Pkg$ and $\NameCert_{\CauchyModulusArithmeticUp}$.

Hence the common tail window is a visible $\BHist$ obligation anchored in the
displayed schedules and structural rows, not a hidden completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_common\_tail\_window}

\begin{theorem}[Sum-product shared threshold obligation]
\label{thm:cauchy-modulus-arithmetic-sum-product-shared-threshold}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$$
A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N),
$$
the $\RegularCauchySumUp$ row $\sigma$ and the
$\RegularCauchyProductUp$ row $\pi$ share the threshold supplied by
$\mu_{\wedge}$ through the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ route. Both consumers must read the two source modulus rows
$\mu_0,\mu_1$, the meet row $\mu_{\wedge}$, the dyadic ledger $D$, the finite
window row $W$, and the regular readback row $R$ before either arithmetic result
is sealed for $\RealUp$. No separate product threshold, quotient-stream
comparison, selected limit, host real equality, or ambient completeness witness
is available.
\end{theorem}
\begin{proof}
Begin with the sum consumer. By
\autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure}, the row $\sigma$ reads
the two source schedules, then the shared meet row, and then the dyadic split
tolerances in $D$ before it reaches $W$ and $R$.

Now turn to the product consumer. By
\autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility}, the row
$\pi$ reuses the same $\mu_0,\mu_1,\mu_{\wedge},D,W,R$ route rather than opening
a separate product comparison coordinate.

Transport through $H$ and replay through $C$ preserve this shared threshold for
both arithmetic rows. The provenance and local name rows $P,N$ then package the
same $\BHist$ route as the public $\NameCert_{\CauchyModulusArithmeticUp}$
surface.

Since the carrier contains no row for quotient streams, selected limits, host
real equality, or ambient completeness, the shared threshold obligation is
exactly the displayed meet route.
\end{proof}

\begin{theorem}[Real seal schedule nonescape obligation]
\label{thm:cauchy-modulus-arithmetic-real-seal-schedule-nonescape}
For every accepted $\CauchyModulusArithmeticUp$ carrier
$$
A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N),
$$
the terminal $\RealUp$ seal row $E$ is reachable only through the finite schedule
surface
$$
\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,H,C,P,N
$$
inside the displayed $\BHist$ carrier. The seal preserves the same $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert_{\CauchyModulusArithmeticUp}$ route used by the
source schedules, meet schedule, sum/product consumers, dyadic ledger,
$\StreamNameUp$ windows, and $\RegSeqRatUp$ readbacks. It exports no quotient
stream, selected limit, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start from a consumer that requests the terminal seal row $E$. By
\autoref{thm:cauchy-modulus-arithmetic-real-consumer-boundary}, the consumer
must pass through either the displayed sum route or the displayed product route
before the Real-facing seal can be read.

The sum and product routes are already bound to the shared threshold by
\autoref{thm:cauchy-modulus-arithmetic-sum-product-shared-threshold}. Thus both
routes expose $\mu_0,\mu_1,\mu_{\wedge},D,W,R$ before they reach the arithmetic
consumer row that feeds $E$.

The common-tail obligation of
\autoref{thm:cauchy-modulus-arithmetic-common-tail-window} keeps the window and
readback rows inside the same $\BHist$ surface. Transport $H$, replay $C$,
provenance $P$, and local naming $N$ therefore remain attached to the seal.

Consequently $E$ is a schedule endpoint for the displayed arithmetic carrier.
It is not an escape route to quotient streams, selected limits, host real
equality, or ambient completeness.
\end{proof}

\begin{theorem}[Diagonal schedule dominates arithmetic consumers]
\label{thm:cauchy-modulus-arithmetic-diagonal-dominates-sum-product}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. Any displayed
$\CauchyModulusDiagonalUp$ tail witness consumed by the sum or product route
is admissible only after the two source schedules $\mu_0,\mu_1$, the shared
meet row $\mu_{\wedge}$, the $\RegularCauchySumUp$ and
$\RegularCauchyProductUp$ rows, the $\DyadicRatCoreUp$ tolerance ledger $D$,
the $\StreamNameUp$ window ledger $W$, the $\RegSeqRatUp$ readbacks $R$, and
the $\RealUp$ seal row $E$ have all been exposed. Thus a diagonal tail
witness is a downstream read of the displayed arithmetic route, not an
independent schedule for choosing a hidden limit comparison.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_diagonal\_dominates\_sum\_product}
\begin{proof}
Read the carrier in the order of
\autoref{def:cauchy-modulus-arithmetic-carrier}. The diagonal tail request has
no coordinate before the two source schedules, the meet row, and the arithmetic
consumer rows are present.

The tolerance and window rows then force the request through $D$ and $W$.
Their entries expose exactly the dyadic tolerance and finite stream windows
from which the regular rational readbacks $R$ are obtained.

Finally the consumer reaches $E$ only through the regular sum or product
handoff. The diagonal witness therefore remains below the displayed
sum/product arithmetic route and cannot serve as a separate host limit
comparison.
\end{proof}

\begin{theorem}[Product consumers reuse the meet threshold]
\label{thm:cauchy-modulus-arithmetic-product-meet-compatibility}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. Every
$\RegularCauchyProductUp$ consumer of $A$ shares the same meet-threshold route
as the $\RegularCauchySumUp$ consumer: it reads $\mu_0,\mu_1$, then
$\mu_{\wedge}$, then the displayed ledger rows $D,W,R$, before reaching the
product row $\pi$. In particular product compatibility is witnessed by the
finite $\CauchyModulusMeetUp$ route, not by a host comparison of completed
limits.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_product\_meet\_compatibility}
\begin{proof}
Begin with a product request for $\pi$. Carrier admission places $\pi$ after
the two source schedules and the shared meet row, so the product consumer
cannot bypass $\mu_0,\mu_1,\mu_{\wedge}$.

The same ledger rows used by the sum route are then read by the product route.
The dyadic ledger $D$ supplies the tolerance row, $W$ supplies the inspected
finite windows, and $R$ supplies the regular rational readbacks.

Transport through $H$ and replay through $C$ preserve this common route. Since
no completed-limit comparison coordinate is present in the carrier, product
compatibility is exactly compatibility with the displayed meet threshold.
\end{proof}

\begin{theorem}[Real seal distribution through arithmetic rows]
\label{thm:cauchy-modulus-arithmetic-real-seal-distribution}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. Every
Real-facing consumer of the arithmetic-modulus packet distributes through
$\RegularCauchySumUp$ and $\RegularCauchyProductUp$ only by reading the finite
rows
$$
\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N.
$$
No Real-facing consumer may distribute the seal through quotient streams,
selected representatives, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Start from a Real-facing request for the seal row $E$. By
\autoref{thm:cauchy-modulus-arithmetic-real-consumer-boundary}, the request
must pass through the displayed sum or product handoff before the seal is
available.

For the sum side, \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure}
supplies the source schedules, shared meet, tolerance ledger, windows, and
readbacks. For the product side,
\autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility} supplies
the same meet-threshold route.

The structural rows $H,C,P,N$ transport, replay, source, and name this finite
route. Therefore distribution of the Real seal is distribution through the
displayed arithmetic rows alone.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic obligation closure]
\label{thm:cauchy-modulus-arithmetic-obligation-closure}
For every accepted $\CauchyModulusArithmeticUp$ carrier $A$, the obligation surface of the local packet is exhausted by carrier admission, classifier transport, the two source modulus schedules, the shared meet row, the sum and product consumer rows, dyadic tolerance exactness, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, Real seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CauchyModulusArithmeticUp}$ row. No further obligation is supplied by quotient streams, selected limits, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the carrier inventory of \autoref{def:cauchy-modulus-arithmetic-carrier}. It exposes exactly the two source streams, two modulus schedules, shared meet, arithmetic consumers, tolerance ledger, windows, readbacks, Real seal, and structural rows.

The local NameCert obligations are the five rows of \autoref{thm:cauchy-modulus-arithmetic-namecert-obligations}. The first row admits the displayed packet, the second transports it through the classifier, the third stabilizes the source schedules and arithmetic consumers, the fourth fixes the dyadic tolerance ledger, and the fifth records replay, provenance, Real handoff, and local naming.

The sum route is locked to the shared meet by \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure}. Thus the arithmetic consumer rows cannot be read without the source schedules, shared threshold, dyadic tolerance ledger, finite windows, and regular rational readbacks.

The product and diagonal routes are locked to the same finite surface by
\autoref{thm:cauchy-modulus-arithmetic-diagonal-dominates-sum-product} and
\autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility}. These
routes cannot add a separate tail selector or a host comparison of completed
limits.

Finally apply \autoref{thm:cauchy-modulus-arithmetic-real-consumer-boundary}
and \autoref{thm:cauchy-modulus-arithmetic-real-seal-distribution}. The
Real-facing consumer receives only the displayed route and cannot extract
quotient streams, selected limits, host real equality, or ambient completeness.
Hence the listed rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic obligation scope package]
\label{thm:cauchy-modulus-arithmetic-obligation-scope-package}
The carrier, classifier, sum-meet closure, product meet compatibility, diagonal domination, Real-seal distribution, dyadic sum threshold, product tail bound, and Real-seal congruence rows jointly scope the $\CauchyModulusArithmeticUp$ NameCert surface over $\CauchyModulusUp$, $\CauchyModulusMeetUp$, $\RegularCauchySumUp$, $\RegularCauchyProductUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Every accepted arithmetic read must pass through the source schedules, shared meet threshold, arithmetic consumer rows, dyadic tolerance ledger, finite windows, regular rational readbacks, terminal Real seal, transport, replay, provenance, and local naming rows before it can expose a sum, product, or Real-facing output.
\end{theorem}
\begin{proof}
Start with the carrier and classifier rows of \autoref{def:cauchy-modulus-arithmetic-carrier} and \autoref{thm:cauchy-modulus-arithmetic-namecert-obligations}. They expose the two source schedules, the shared meet row, the sum and product consumers, the dyadic ledger, the stream windows, the regular readback, the Real seal, and the structural rows.

For the arithmetic routes, use \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure}, \autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility}, and \autoref{thm:cauchy-modulus-arithmetic-diagonal-dominates-sum-product}. These force both $\RegularCauchySumUp$ and $\RegularCauchyProductUp$ consumers to reuse $\mu_0,\mu_1,\mu_{\wedge},D,W,R$ before any output can be read.

For the Real-facing boundary, apply \autoref{thm:cauchy-modulus-arithmetic-real-seal-distribution}, \autoref{thm:cauchy-modulus-arithmetic-dyadic-sum-threshold}, \autoref{thm:cauchy-modulus-arithmetic-product-tail-bound}, and \autoref{thm:cauchy-modulus-arithmetic-real-seal-congruence}. These rows keep every terminal read attached to the same finite ledger and to the same $H,C,P,N$ transport, replay, provenance, and local naming route.

Thus the scoped obligation package is finite: each named dependency is one displayed sibling or kernel row, and no quotient stream, selected limit, host real equality, or ambient completeness principle is introduced.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic sum-product carrier admission]
\label{thm:cauchy-modulus-arithmetic-sum-product-carrier-admission}
Every accepted $\CauchyModulusArithmeticUp$ arithmetic packet exposes the sum row $\sigma$, product row $\pi$, diagonal schedule reads, meet threshold $\mu_{\wedge}$, $\DyadicRatCoreUp$ tolerance ledger $D$, $\RegSeqRatUp$ readback row $R$, $\StreamNameUp$ window row $W$, $\RealUp$ seal $E$, componentwise $\hsame$ row $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\CauchyModulusArithmeticUp}$ row $N$ before any Real arithmetic consumer reads the result. The admission route is therefore a concrete finite $\BHist$ surface, not a quotient stream, selected limit, host real equality, or ambient completeness read.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-modulus-arithmetic-carrier}. It already lists the sum and product consumers, the shared meet row, the dyadic tolerance ledger, the window ledger, the regular rational readbacks, the Real seal, and the structural rows.

Use the sum-meet closure of \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure} and the product meet compatibility of \autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility}. These two routes force $\sigma$ and $\pi$ to reuse $\mu_0,\mu_1,\mu_{\wedge},D,W,R$ before either arithmetic consumer can reach $E$.

The diagonal dominance theorem \autoref{thm:cauchy-modulus-arithmetic-diagonal-dominates-sum-product} keeps any diagonal schedule read downstream of the same rows. Finally \autoref{thm:cauchy-modulus-arithmetic-real-seal-distribution} routes the Real-facing read through $H,C,P,N$, so the consumer receives only the displayed finite packet.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic diagonal-meet stability]
\label{thm:cauchy-modulus-arithmetic-diagonal-meet-stability}
For every accepted $\CauchyModulusArithmeticUp$ packet, any diagonal schedule read used by the sum or product consumer is stable only along the same meet threshold $\mu_{\wedge}$ and the same finite rows $D,W,R,E,H,C,P,N$. Thus diagonal refinement, meet-threshold transport, $\DyadicRatCoreUp$ tolerance reads, $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\RealUp$ sealing, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming remain one concrete route before any Real arithmetic consumer accepts the result.
\end{theorem}
\begin{proof}
Begin with a diagonal schedule read. By \autoref{thm:cauchy-modulus-arithmetic-diagonal-dominates-sum-product}, the read is admissible only after the source schedules, shared meet row, arithmetic consumers, tolerance ledger, windows, readbacks, and Real seal are visible.

Apply \autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility} to the product route and \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure} to the sum route. Both consumers reuse the same meet threshold $\mu_{\wedge}$ and the same ledger rows $D,W,R$.

Transport and replay are then fixed by $H$ and $C$. The provenance and local naming rows $P,N$ record the same finite route, and the Real seal distribution theorem keeps the final arithmetic read inside it. Therefore diagonal stability is meet stability for the displayed packet, not a separate tail selector or host comparison.
\end{proof}

\begin{theorem}[Dyadic sum threshold obligation]
\label{thm:cauchy-modulus-arithmetic-dyadic-sum-threshold}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. Every dyadic
tolerance request consumed by the $\RegularCauchySumUp$ row $\sigma$ is
answered by a finite threshold route through the source schedules
$\mu_0,\mu_1$, the common-tail window, the shared meet row $\mu_{\wedge}$, and
the split tolerance entries of $D$. The sum threshold therefore depends on the
displayed $\DyadicRatCoreUp$ ledger and the $\RegSeqRatUp$ readback row $R$,
not on a quotient stream, selected limit, host real equality, or ambient
completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:cauchy-modulus-arithmetic-carrier}. The sum consumer $\sigma$ has
access only to the two source schedules, the shared meet row, the dyadic
ledger, finite stream windows, and regular rational readbacks.

Use the common-tail window of
\autoref{thm:cauchy-modulus-arithmetic-common-tail-window} to place the two
late stream reads on one finite window surface. Then apply the sum-meet closure
of \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure} and the
sum-product shared threshold of
\autoref{thm:cauchy-modulus-arithmetic-sum-product-shared-threshold}.

The split entries in $D$ supply the dyadic tolerances read by $\sigma$, while
$R$ records the regular rational readback obtained from those windows. Since no
other carrier coordinate can answer the request, the dyadic sum threshold is
exactly this displayed finite route.
\end{proof}

\begin{theorem}[Product tail bound obligation]
\label{thm:cauchy-modulus-arithmetic-product-tail-bound}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. Every product
tail bound consumed by the $\RegularCauchyProductUp$ row $\pi$ is witnessed
inside the same meet threshold $\mu_{\wedge}$ and the same rows $D,W,R$ used
by the sum route. Product bounds may refine the diagonal schedule read, but
they remain downstream of $\mu_0,\mu_1,\mu_{\wedge}$ and upstream of the
$\RealUp$ seal row $E$.
\end{theorem}
\begin{proof}
Begin with a product request for $\pi$. The product-meet compatibility theorem
\autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility} forces the
request through $\mu_0,\mu_1,\mu_{\wedge},D,W,R$ before the product row is
available.

If the product request uses a diagonal tail witness, apply
\autoref{thm:cauchy-modulus-arithmetic-diagonal-dominates-sum-product} and
\autoref{thm:cauchy-modulus-arithmetic-diagonal-meet-stability}. These
theorems keep diagonal refinement on the same meet threshold and the same
finite ledger rows.

The product bound is then replayed through $H$ and $C$ and sourced by $P,N$.
It cannot introduce a separate tail selector or compare completed limits,
because the carrier has no coordinate for those principles.
\end{proof}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_product\_tail\_bound}

\begin{theorem}[Real seal congruence obligation]
\label{thm:cauchy-modulus-arithmetic-real-seal-congruence}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. If two displayed
arithmetic reads are transported by the componentwise $\hsame$ row $H$ and
share the same source schedules, meet threshold, dyadic ledger, stream
windows, and regular rational readbacks, then every $\RealUp$ seal read
through $E$ is congruent along the same $\Cont$, $\Pkg$, and local
$\NameCert_{\CauchyModulusArithmeticUp}$ route. The congruence is a seal
boundary for the displayed packet, not a proof of quotient-real equality or
ambient completeness.
\end{theorem}
\begin{proof}
Project the two displayed arithmetic reads to their common rows. By the
carrier definition and the classifier transport row, $H$ can transport only
the stream windows, source schedules, meet row, sum and product rows, dyadic
ledger, readbacks, seal row, replay, provenance, and local name.

Use \autoref{thm:cauchy-modulus-arithmetic-real-seal-distribution} to route
both Real-facing reads through $\sigma$ or $\pi$ and then through
$\mu_0,\mu_1,\mu_{\wedge},D,W,R,E,H,C,P,N$. The Real seal schedule nonescape
obligation
\autoref{thm:cauchy-modulus-arithmetic-real-seal-schedule-nonescape} keeps the
seal attached to that same finite route.

Thus congruence of the seal is exactly congruence along the displayed
transport, replay, provenance, and local NameCert rows. No quotient-real
equality or completeness principle is exported by the argument.
\end{proof}

\begin{theorem}[Cauchy-modulus arithmetic uniform product budget]
\label{thm:cauchy-modulus-arithmetic-uniform-product-budget}
Let $A$ be an accepted $\CauchyModulusArithmeticUp$ carrier. Any product
consumer that reads a requested $\RealUp$ tolerance must first pass through
the shared meet threshold $\mu_{\wedge}$, then the product tail-bound ledger
over $D,W,R$, and then the Real-seal congruence row $E,H,C,P,N$. Consequently
the exported product budget is uniform over the same $\StreamNameUp$ windows
and $\RegSeqRatUp$ readbacks that the carrier already displays; it cannot be
selected from a separate tail oracle, quotient real equality, or ambient
completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyModulusArithmeticUp.CauchyModulusArithmeticCarrier\_uniform\_product\_budget}
\begin{proof}
Project the carrier of
\autoref{def:cauchy-modulus-arithmetic-carrier}. The product row $\pi$ can
read only the source schedules, the shared meet threshold, the dyadic ledger,
the stream windows, the regular-rational readbacks, and the structural rows
of the displayed packet.

Apply the product tail bound obligation
\autoref{thm:cauchy-modulus-arithmetic-product-tail-bound}. It places every
product tail read downstream of $\mu_0,\mu_1,\mu_{\wedge},D,W,R$ and upstream
of the $\RealUp$ seal.

Use diagonal-meet stability,
\autoref{thm:cauchy-modulus-arithmetic-diagonal-meet-stability}, to keep any
diagonal refinement on the same shared threshold. Then apply Real seal
congruence,
\autoref{thm:cauchy-modulus-arithmetic-real-seal-congruence}, so the final
seal read remains attached to $E,H,C,P,N$.

The budget exported to the product consumer is therefore the unique displayed
route through the meet threshold, tail ledger, readback windows, seal,
transport, replay, provenance, and local NameCert rows. No other carrier
coordinate can supply a product budget.
\end{proof}

\falsifiablePrediction{An accepted $\CauchyModulusArithmeticUp$ consumer that
reaches a $\RegularCauchySumUp$, $\RegularCauchyProductUp$, or $\RealUp$ row
without reading the two displayed $\CauchyModulusUp$ schedules, the
$\CauchyModulusMeetUp$ shared-threshold row, the $\DyadicRatCoreUp$ tolerance
ledger, the $\StreamNameUp$ windows, and the $\RegSeqRatUp$ readbacks refutes
the carrier surface.}

\independenceWitness{cauchymodulus, cauchymodulusmeet, regularcauchysum,
regularcauchyproduct, real: no listed sibling supplies the whole arithmetic
packet $A=(S_0,S_1,\mu_0,\mu_1,\mu_{\wedge},\sigma,\pi,D,W,R,E,H,C,P,N)$,
because each sibling contributes only a source schedule, a shared threshold,
one arithmetic consumer, or the downstream real seal while this chapter binds
the schedule-combination route with transport, replay, provenance, and local
naming.}

\closureat{CauchyModulusArithmeticUp}{obligationStr}

\begin{closurestatus}{\CauchyModulusArithmeticUp}
  \constructivestory{A $\CauchyModulusArithmeticUp$ packet is generated from two $\StreamNameUp$ windows, two $\CauchyModulusUp$ schedules, one $\CauchyModulusMeetUp$ shared-threshold row, $\RegularCauchySumUp$ and $\RegularCauchyProductUp$ consumer rows, a $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readbacks, a $\RealUp$ seal row, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:cauchy-modulus-arithmetic-carrier}, \autoref{thm:cauchy-modulus-arithmetic-namecert-obligations}, \autoref{thm:cauchy-modulus-arithmetic-sum-meet-closure}, \autoref{thm:cauchy-modulus-arithmetic-real-consumer-boundary}, \autoref{thm:cauchy-modulus-arithmetic-diagonal-dominates-sum-product}, \autoref{thm:cauchy-modulus-arithmetic-product-meet-compatibility}, \autoref{thm:cauchy-modulus-arithmetic-real-seal-distribution}, \autoref{thm:cauchy-modulus-arithmetic-obligation-closure}, and \autoref{thm:cauchy-modulus-arithmetic-obligation-scope-package} bind the finite modulus-arithmetic obligation boundary over $\CauchyModulusUp$, $\CauchyModulusMeetUp$, $\RegularCauchySumUp$, $\RegularCauchyProductUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient streams, selected limits, host real equality, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the displayed carrier and prove the finite NameCert obligation surface, sum-meet closure, and real consumer boundary.}
\end{closurestatus}
