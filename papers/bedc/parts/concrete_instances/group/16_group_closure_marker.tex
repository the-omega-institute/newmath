\begin{closurestatus}{\GroupUp}
  \constructivestory{At this group marker, every motion starts and ends at the same endpoint: a history is admitted only when it is history-equal to $\emp$. On that singleton, the classifier is the restriction of history-equality, so multiplication, unit, and inverse all reduce to the empty history and the operation table has nowhere else to go. The source is the singleton predicate, the pattern records continuation multiplication returning the singleton endpoint, $\emp$ as unit, and $\emp$ again as the inverse of every carried element, while the ledger keeps the visible singleton aligned with its raw history. Since there is exactly one inhabited class and history-equality is an equivalence, $\emp$ witnesses nonemptiness and the classifier is reflexive, symmetric, transitive, and closed over the carrier without further algebra. A history $h$ lies in the signature gap of a $\GroupUp$ token $p$ when the source-domain accepts $h$, the signature relation pairs it with an answer history and evidence ledger, and the token-introduction relation names that answer as $p$; unfolded, $h$ is history-equal to $\emp$ and $p$ is the singleton group element. The checked bridge row identifies this singleton comparison with the external trivial group $\{e\}$ at the marker boundary. The chapter registers this singleton group construction under the name $\GroupUp$; the inverse laws are unit-law calculations on the endpoint, with no non-singleton group carrier or primitive inverse object introduced.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{This chapter closes the concrete singleton-history group certificate: carrier, classifier, multiplication, unit, inverse, and the named law package are bound for the $\mathsf{BHist}$ singleton instance.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.GroupUp.GroupSingletonHistory\_laws}
  \bridgestatus{bridgeChecked}
  \notclaimed{This record does not close a public group object surface, a standard bridge, or non-singleton group classifiers.}
  \upgradepath{No further upgrade -- chapter at top of formal axis.}
\end{closurestatus}
