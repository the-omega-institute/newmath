\begin{closurestatus}{\GroupUp}
  \constructivestory{Restrict the history carrier to the singleton class of $\emp$, multiply by append, and define the inverse of every carried endpoint to be $\emp$; then every product, unit, and inverse comparison is forced through the same empty endpoint. A group here is the $\MonoidUp$ append certificate with the inverse rows added on that singleton surface, so the left and right inverse laws are unit-law calculations rather than new algebraic motion. The chapter registers this singleton-history construction under $\GroupUp$; no non-singleton group carrier or primitive inverse object is introduced.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{This chapter closes the concrete singleton-history group certificate: carrier, classifier, multiplication, unit, inverse, and the named law package are bound for the $\mathsf{BHist}$ singleton instance.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.GroupUp.GroupSingletonHistory\_laws}
  \bridgestatus{bridgeChecked}
  \notclaimed{This record does not close a public group object surface, a standard bridge, or non-singleton group classifiers.}
  \upgradepath{No further upgrade -- chapter at top of formal axis.}
\end{closurestatus}
