\section{Singleton conjugation collapse}
\label{sec:group-singleton-conjugation-collapse}

\begin{theorem}[Group singleton conjugation fixed carrier]
\label{thm:group-singleton-conjugation-fixed-carrier}
For histories $a,x:\mathsf{BHist}$ in the concrete singleton history group of
Definition~\ref{def:concrete-singleton-history-group-instance}, if
$A_{\emp}(a)$, then
$$
R_{\emp}(\mathsf{mul}(\mathsf{mul}(a,x),\mathsf{inv}(a)),x)
\Longleftrightarrow A_{\emp}(x).
$$
The ten-field fit is: traditional target terminal-group inner action; BEDC
source concrete singleton $\GroupUp$ over $\mathsf{BHist}$; carrier
$A_{\emp}$; classifier $R_{\emp}$; stability from singleton multiplication
closure, inverse closure, and terminal classifier exactness; ledger the hidden
conjugating endpoint and constant inverse; core theorem list
\autoref{thm:group-singleton-history-semantic-namecert},
\autoref{thm:concrete-singleton-history-group-laws}, and
\autoref{thm:group-singleton-terminal-classifier-package}; exactness predicate
the displayed iff; standard bridge inner action of the terminal group; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For the forward direction, apply the first row of
\autoref{thm:group-singleton-terminal-classifier-package} to the displayed
classifier. It rewrites the classifier as the pair consisting of the conjugation
word carrier fact and $A_{\emp}(x)$, so projecting the second component gives
the required carrier condition on $x$.

For the reverse direction, start from $A_{\emp}(a)$ and $A_{\emp}(x)$. The
multiplication-closure row of
\autoref{thm:group-singleton-terminal-classifier-package} places
$\mathsf{mul}(a,x)$ in $A_{\emp}$, and the inverse-closure row places
$\mathsf{inv}(a)$ in $A_{\emp}$.

Apply multiplication closure once more to put
$\mathsf{mul}(\mathsf{mul}(a,x),\mathsf{inv}(a))$ in the singleton carrier.
With this carrier fact and $A_{\emp}(x)$, the terminal classifier exactness row
of \autoref{thm:group-singleton-terminal-classifier-package} packages the
classifier
$$
R_{\emp}(\mathsf{mul}(\mathsf{mul}(a,x),\mathsf{inv}(a)),x).
$$
\end{proof}

\section{Concrete singleton normalizer action certificate}
\label{sec:group-singleton-normalizer-action-certificate}

\begin{theorem}[Group singleton normalizer action certificate]
\label{thm:group-singleton-normalizer-action-certificate}
For the concrete singleton history group instance, define
$$
\mathsf{Conj}_s(x):=\mathsf{mul}(\mathsf{mul}(s,x),\mathsf{inv}(s))
$$
and take the terminal normalizer carrier to be
$$
N_{\emp}(s):\Longleftrightarrow A_{\emp}(s).
$$
If $A_{\emp}(s)$ and $A_{\emp}(x)$, then:
\begin{enumerate}
\item $A_{\emp}(\mathsf{Conj}_s(x))$.
\item $A_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(x))$.
\item $R_{\emp}(\mathsf{Conj}_s(x),x)$.
\item $R_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}
(\mathsf{Conj}_s(x)),x)$.
\end{enumerate}
The ten-field fit is: traditional target terminal normalizer action; BEDC
source concrete singleton $\GroupUp$ over $\mathsf{BHist}$; carrier
$A_{\emp}$; classifier $R_{\emp}$; stability singleton multiplication
closure, inverse closure, and terminal classifier exactness; ledger the
conjugating singleton endpoint and the acted-on singleton endpoint; core
theorem this certificate; exactness terminal normalizer preservation;
downstream consumer $\QuotientGroupUp$; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Unfold the concrete singleton definitions. The inverse operation is constant,
so $\mathsf{inv}(s)=\emp$, and the two conjugation words are formed only by
multiplying histories already in the singleton carrier with $\emp$.

Apply the multiplication-closure row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} to $s$ and $x$ to put
$\mathsf{mul}(s,x)$ in $A_{\emp}$. Apply the same row again with
$\mathsf{inv}(s)=\emp$ to obtain
$A_{\emp}(\mathsf{Conj}_s(x))$.

For the inverse conjugating word, inverse closure gives
$A_{\emp}(\mathsf{inv}(s))$. The same two multiplication-closure applications
place $\mathsf{Conj}_{\mathsf{inv}(s)}(x)$ in $A_{\emp}$.

It remains to read the classifier. The terminal classifier exactness row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} classifies any two
singleton endpoints by $R_{\emp}$, so it gives
$R_{\emp}(\mathsf{Conj}_s(x),x)$ from the carrier facts already built.

Applying the closure argument once more to the singleton endpoint
$\mathsf{Conj}_s(x)$ gives
$A_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x)))$. The same
classifier row then yields
$R_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x)),x)$.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizer\_action\_certificate}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonClassifier\_normalizer\_append\_empty\_action\_certificate}
\leanchecked{BEDC.Derived.GroupUp.GroupSingleton\_normalizer\_action\_certificate}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerAction\_certificate}

\begin{theorem}[Group singleton conjugation classifier coverage]
\label{thm:group-singleton-conjugation-classifier-coverage}
For histories $s,x,y:\mathsf{BHist}$ in the concrete singleton history group of
Definition~\ref{def:concrete-singleton-history-group-instance}, define
$$
\mathsf{Conj}_s(x):=\mathsf{mul}(\mathsf{mul}(s,x),\mathsf{inv}(s)).
$$
If $A_{\emp}(s)$, then
$$
R_{\emp}(\mathsf{Conj}_s(x),y)\Longleftrightarrow
A_{\emp}(x)\land A_{\emp}(y).
$$
The ten-field fit is: traditional target terminal conjugation coverage; BEDC
source concrete singleton $\GroupUp$ over $\mathsf{BHist}$; carrier
$A_{\emp}$; classifier $R_{\emp}$; stability from multiplication closure,
inverse closure, and terminal classifier exactness; ledger the conjugating
singleton endpoint plus the acted-on endpoint; core theorem this block;
exactness the displayed iff; downstream consumer $\QuotientGroupUp$; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Unfold $\mathsf{Conj}_s(x)$. The inverse row of
\autoref{thm:group-singleton-terminal-classifier-package} sends the assumption
$A_{\emp}(s)$ to $A_{\emp}(\mathsf{inv}(s))$.

Use the multiplication-closure iff from
\autoref{thm:group-singleton-terminal-classifier-package} twice. The first use
rewrites $A_{\emp}(\mathsf{mul}(s,x))$ as
$A_{\emp}(s)\land A_{\emp}(x)$, and the second rewrites
$A_{\emp}(\mathsf{Conj}_s(x))$ as
$A_{\emp}(\mathsf{mul}(s,x))\land A_{\emp}(\mathsf{inv}(s))$. With the two
carrier facts for $s$ and $\mathsf{inv}(s)$ fixed, this identifies
$A_{\emp}(\mathsf{Conj}_s(x))$ with $A_{\emp}(x)$.

Apply the terminal classifier exactness row of
\autoref{thm:group-singleton-terminal-classifier-package} to replace
$R_{\emp}(\mathsf{Conj}_s(x),y)$ by
$A_{\emp}(\mathsf{Conj}_s(x))\land A_{\emp}(y)$. Substituting the conjugation
carrier equivalence from the previous paragraph and reassociating the
conjunctions gives exactly
$A_{\emp}(x)\land A_{\emp}(y)$.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonClassifier\_append\_conjugation\_coverage\_iff}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonClassifier\_conjugation\_coverage\_iff}

\begin{definition}[Group singleton normalizer orbit relation]
\label{def:group-singleton-normalizer-orbit-relation}
In the concrete singleton history group instance, with $N_{\emp}$ and
$\mathsf{Conj}$ fixed as in
Theorem~\ref{thm:group-singleton-normalizer-action-certificate}, define the
terminal normalizer orbit relation by declaring
$x\sim_{N_{\emp}}y$ precisely when there exists $s:\mathsf{BHist}$ such that
$N_{\emp}(s)$ and $R_{\emp}(\mathsf{Conj}_s(x),y)$.
\end{definition}
\leandef{BEDC.Derived.GroupUp.GroupSingletonNormalizerOrbit}

\begin{theorem}[Group singleton normalizer orbit exactness]
\label{thm:group-singleton-normalizer-orbit-exactness}
For any $x,y:\mathsf{BHist}$ with $A_{\emp}(x)$ and $A_{\emp}(y)$, the
terminal normalizer orbit relation is exact:
$x\sim_{N_{\emp}}y$ if and only if $R_{\emp}(x,y)$. The ten-field fit is:
traditional target terminal normalizer orbit; BEDC source concrete singleton
$\GroupUp$ over $\mathsf{BHist}$; carrier $A_{\emp}$; classifier the displayed
orbit relation compared with $R_{\emp}$; stability from
Theorem~\ref{thm:group-singleton-normalizer-action-certificate} and
Theorem~\ref{thm:group-singleton-terminal-classifier-package}; ledger
conjugator endpoint; core theorem this block; exactness orbit iff terminal
singleton classifier; downstream consumer $\QuotientGroupUp$; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Assume first $x\sim_{N_{\emp}}y$. By
Definition~\ref{def:group-singleton-normalizer-orbit-relation}, choose
$s:\mathsf{BHist}$ with $N_{\emp}(s)$ and
$R_{\emp}(\mathsf{Conj}_s(x),y)$. Since $N_{\emp}(s)$ means
$A_{\emp}(s)$, Theorem~\ref{thm:group-singleton-normalizer-action-certificate}
applied to $s$ and $x$ gives
$R_{\emp}(\mathsf{Conj}_s(x),x)$.

Use the carrier-aware classifier rows of
Theorem~\ref{thm:group-singleton-terminal-classifier-package}. Symmetry turns
$R_{\emp}(\mathsf{Conj}_s(x),x)$ into
$R_{\emp}(x,\mathsf{Conj}_s(x))$, and transitivity with the orbit witness
$R_{\emp}(\mathsf{Conj}_s(x),y)$ gives $R_{\emp}(x,y)$.

Conversely assume $R_{\emp}(x,y)$. Take the witness $s=\emp$. The singleton
carrier inhabitance row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} gives
$A_{\emp}(\emp)$, hence $N_{\emp}(\emp)$.

Theorem~\ref{thm:group-singleton-normalizer-action-certificate}, applied to
$\emp$ and $x$, gives $R_{\emp}(\mathsf{Conj}_{\emp}(x),x)$. Transitivity
with the assumed $R_{\emp}(x,y)$ yields
$R_{\emp}(\mathsf{Conj}_{\emp}(x),y)$, so
Definition~\ref{def:group-singleton-normalizer-orbit-relation} supplies the
orbit witness and proves $x\sim_{N_{\emp}}y$.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonClassifier\_normalizer\_orbit\_source\_exact\_iff}

\begin{theorem}[Group singleton normalizer orbit total coverage]
\label{thm:group-singleton-normalizer-orbit-total-coverage}
For all histories $x,y:\mathsf{BHist}$ in the concrete singleton history group,
with the orbit relation of
Definition~\ref{def:group-singleton-normalizer-orbit-relation},
$$
x\sim_{N_{\emp}}y \Longleftrightarrow A_{\emp}(x)\land A_{\emp}(y).
$$
The ten-field fit is: traditional target terminal normalizer-orbit coverage;
BEDC source concrete singleton $\GroupUp$ over $\mathsf{BHist}$; carrier
$A_{\emp}$; classifier the orbit relation; stability from
\autoref{thm:group-singleton-conjugation-classifier-coverage} and
\autoref{thm:group-singleton-terminal-classifier-package}; ledger the
existential conjugator endpoint; core theorem this block; exactness the
displayed iff; downstream consumer $\QuotientGroupUp$; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Assume first $x\sim_{N_{\emp}}y$. By
Definition~\ref{def:group-singleton-normalizer-orbit-relation}, choose
$s:\mathsf{BHist}$ with $N_{\emp}(s)$ and
$R_{\emp}(\mathsf{Conj}_s(x),y)$. Since $N_{\emp}(s)$ means
$A_{\emp}(s)$, Theorem~\ref{thm:group-singleton-conjugation-classifier-coverage}
applies to this conjugator.

The forward direction of that coverage theorem sends the orbit witness
$R_{\emp}(\mathsf{Conj}_s(x),y)$ to
$A_{\emp}(x)\land A_{\emp}(y)$.

Conversely assume $A_{\emp}(x)\land A_{\emp}(y)$. The singleton carrier
inhabitance row of Theorem~\ref{thm:group-singleton-terminal-classifier-package}
gives $A_{\emp}(\emp)$, hence $N_{\emp}(\emp)$.

Apply the reverse direction of
Theorem~\ref{thm:group-singleton-conjugation-classifier-coverage} with
$s=\emp$. The endpoint facts $A_{\emp}(x)$ and $A_{\emp}(y)$ yield
$R_{\emp}(\mathsf{Conj}_{\emp}(x),y)$, and
Definition~\ref{def:group-singleton-normalizer-orbit-relation} supplies the
existential orbit witness.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerOrbit\_coverage\_iff}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerOrbit\_total\_coverage\_iff}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonClassifier\_normalizer\_orbit\_total\_coverage\_iff}

\begin{theorem}[Group singleton normalizer orbit visible source contradiction]
\label{thm:group-singleton-normalizer-orbit-visible-source-absurd}
For histories $p,y:\mathsf{BHist}$, neither visible singleton endpoint can be
the source of a terminal singleton normalizer orbit into $y$:
$$
\bigl(\exists s,\ A_{\emp}(s)\land
R_{\emp}(\mathsf{append}(\mathsf{append}(s,\Ezero(p)),\emp),y)\bigr)
\Longrightarrow \bot
$$
and
$$
\bigl(\exists s,\ A_{\emp}(s)\land
R_{\emp}(\mathsf{append}(\mathsf{append}(s,\Eone(p)),\emp),y)\bigr)
\Longrightarrow \bot.
$$
\end{theorem}
\begin{proof}
Unpack the orbit witness. The terminal classifier says that the conjugation
display is in the singleton carrier. Applying append-empty decomposition first
to the terminal append and then to the inner append forces either
$\Ezero(p)\hsame\emp$ or $\Eone(p)\hsame\emp$. Both contradict constructor
separation for $\mathsf{BHist}$.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerOrbit\_visible\_source\_absurd}

\begin{theorem}[Group singleton commutator normalizer-orbit kernel exactness]
\label{thm:group-singleton-commutator-orbit-kernel-exactness}
For histories $x,y:\mathsf{BHist}$ in the concrete singleton history group, with
$\mathsf{Comm}_{\emp}(x,y)$ defined as in
\autoref{thm:group-singleton-commutator-terminal-collapse} and with the
terminal normalizer orbit relation of
Definition~\ref{def:group-singleton-normalizer-orbit-relation},
$$
\mathsf{Comm}_{\emp}(x,y)\sim_{N_{\emp}}\emp
\Longleftrightarrow A_{\emp}(x)\land A_{\emp}(y).
$$
Moreover the same endpoint is the terminal orbit kernel exactly when the two
inputs lie in the same terminal normalizer orbit:
$$
\mathsf{Comm}_{\emp}(x,y)\sim_{N_{\emp}}\emp
\Longleftrightarrow x\sim_{N_{\emp}}y.
$$
The ten-field fit is: traditional target terminal commutator orbit-kernel
exactness; BEDC source concrete singleton $\GroupUp$ over $\mathsf{BHist}$;
carrier $A_{\emp}$; classifier the terminal normalizer orbit relation;
stability from total orbit coverage and commutator carrier exactness; ledger
the commutator endpoint together with the terminal endpoint $\emp$; core theorem
this block; exactness the two displayed equivalences; downstream consumer
$\QuotientGroupUp$ first-isomorphism-style quotient-kernel exactness; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply \autoref{thm:group-singleton-normalizer-orbit-total-coverage} to the pair
$(\mathsf{Comm}_{\emp}(x,y),\emp)$. The singleton carrier inhabitance row of
\autoref{thm:group-singleton-terminal-classifier-package} gives
$A_{\emp}(\emp)$, so the orbit coverage statement reduces the commutator orbit
edge to the carrier condition $A_{\emp}(\mathsf{Comm}_{\emp}(x,y))$.

Now compose this reduction with
\autoref{thm:group-singleton-commutator-carrier-exactness}. That theorem
identifies the commutator carrier condition with
$A_{\emp}(x)\land A_{\emp}(y)$, giving the first displayed equivalence.

For the comparison with the terminal orbit of the two inputs, apply
\autoref{thm:group-singleton-normalizer-orbit-total-coverage} again, now to
the pair $(x,y)$. It identifies $x\sim_{N_{\emp}}y$ with the same carrier pair
$A_{\emp}(x)\land A_{\emp}(y)$.

Combining the two identifications through the common carrier pair gives the
second displayed equivalence, with no additional group data beyond the
singleton normalizer orbit certificate.
\end{proof}

\begin{theorem}[Group singleton normalizer orbit equivalence]
\label{thm:group-singleton-normalizer-orbit-equivalence}
For histories $x,y,z:\mathsf{BHist}$ carried by $A_{\emp}$ in the concrete
singleton history group, the terminal normalizer orbit relation of
Definition~\ref{def:group-singleton-normalizer-orbit-relation} is reflexive,
symmetric, and transitive on the carried endpoints:
\begin{enumerate}
\item $x\sim_{N_{\emp}}x$.
\item If $x\sim_{N_{\emp}}y$, then $y\sim_{N_{\emp}}x$.
\item If $x\sim_{N_{\emp}}y$ and $y\sim_{N_{\emp}}z$, then
$x\sim_{N_{\emp}}z$.
\end{enumerate}
The ten-field fit is: traditional target terminal normalizer orbit
equivalence; BEDC source concrete singleton $\GroupUp$ over $\mathsf{BHist}$;
carrier $A_{\emp}$; classifier the orbit relation compared with $R_{\emp}$;
stability from Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness}
plus the SemanticNameCert reflexivity, symmetry, and transitivity rows for
$R_{\emp}$ in Theorem~\ref{thm:group-singleton-terminal-classifier-package};
ledger the conjugator endpoint carried by each orbit witness; core theorem this
block; exactness the displayed orbit equivalence laws; downstream consumer
$\QuotientGroupUp$; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For reflexivity, use the reflexivity row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} on
$A_{\emp}(x)$ to obtain $R_{\emp}(x,x)$. The reverse direction of
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness}, applied at
$(x,x)$, transports this classifier fact to $x\sim_{N_{\emp}}x$.

For symmetry, assume $x\sim_{N_{\emp}}y$. The forward direction of
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness}, applied at
$(x,y)$, gives $R_{\emp}(x,y)$. The symmetry row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} gives
$R_{\emp}(y,x)$, and the reverse direction of orbit exactness at $(y,x)$ gives
$y\sim_{N_{\emp}}x$.

For transitivity, assume $x\sim_{N_{\emp}}y$ and $y\sim_{N_{\emp}}z$. Orbit
exactness sends these two orbit classifiers to $R_{\emp}(x,y)$ and
$R_{\emp}(y,z)$. The transitivity row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} gives
$R_{\emp}(x,z)$, and the reverse direction of orbit exactness at $(x,z)$ gives
$x\sim_{N_{\emp}}z$.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerOrbit\_equivalence}

\begin{definition}[Concrete singleton normalizer-action group instance]
\label{def:group-singleton-normalizer-action-instance}
The concrete terminal normalizer action over $\mathsf{BHist}$ is the data
whose carrier is $Carrier:=A_{\emp}$, whose classifier is
$classifier:=R_{\emp}$, whose multiplication is $mul:=\mathsf{append}$, whose
unit is $e:=\emp$, whose inverse is the constant operation
$inv(h):=\emp$, and whose action map is
$$
\mathsf{Conj}_s(x):=\mathsf{mul}(\mathsf{mul}(s,x),\mathsf{inv}(s)).
$$
\end{definition}
\leandef{BEDC.Derived.GroupUp.GroupSingletonNormalizerAction}

\begin{theorem}[Group singleton normalizer action classifier determinacy]
\label{thm:group-singleton-normalizer-action-classifier-determinacy}
For histories $s,x,y:\mathsf{BHist}$, if $A_{\emp}(s)$ and
$R_{\emp}(x,y)$, then
$$
R_{\emp}(\mathsf{Conj}_s(x),\mathsf{Conj}_s(y))\land
R_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x)),y).
$$
\end{theorem}
\begin{proof}
The classifier assumption $R_{\emp}(x,y)$ carries the singleton endpoint facts
$A_{\emp}(x)$ and $A_{\emp}(y)$ by the forward direction of the terminal
classifier row in
Theorem~\ref{thm:group-singleton-terminal-classifier-package}.

Instantiate Theorem~\ref{thm:group-singleton-normalizer-action-certificate}
at $(s,x)$ and $(s,y)$. The two instances give
$A_{\emp}(\mathsf{Conj}_s(x))$ and
$A_{\emp}(\mathsf{Conj}_s(y))$, so the SemanticNameCert classifier
symmetry/transitivity rows for $R_{\emp}$ classify the two action endpoints:
$$
R_{\emp}(\mathsf{Conj}_s(x),\mathsf{Conj}_s(y)).
$$

The $(s,x)$ instance also gives the inverse-roundtrip classifier
$R_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x)),x)$.
Composing this row with the input classifier $R_{\emp}(x,y)$ by the
SemanticNameCert transitivity row for $R_{\emp}$ yields
$$
R_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x)),y).
$$

The two displayed classifiers are exactly the required conjunction.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonClassifier\_conjugation\_action\_determinacy}

\begin{theorem}[Group singleton normalizer orbit action invariance]
\label{thm:group-singleton-normalizer-orbit-action-invariance}
For histories $s,x,y:\mathsf{BHist}$ in the concrete singleton history group
instance, if $A_{\emp}(s)$, $A_{\emp}(x)$, $A_{\emp}(y)$, and
$x\sim_{N_{\emp}}y$, then
$$
\mathsf{Conj}_s(x)\sim_{N_{\emp}}\mathsf{Conj}_s(y).
$$
The ten-field fit is: traditional target normalizer action descending to the
terminal orbit quotient; BEDC source concrete singleton $\GroupUp$ over
$\mathsf{BHist}$; carrier $A_{\emp}$; classifier the normalizer orbit relation
exact against $R_{\emp}$; stability from
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness},
Theorem~\ref{thm:group-singleton-normalizer-action-classifier-determinacy}, and
Theorem~\ref{thm:group-singleton-terminal-classifier-package}; ledger the
conjugating endpoint and orbit witness; core theorem this block; exactness the
displayed orbit preservation; downstream consumer $\QuotientGroupUp$; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Start with the given orbit witness $x\sim_{N_{\emp}}y$. Since $x$ and $y$ are
in $A_{\emp}$, Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness}
turns that witness into the terminal classifier row $R_{\emp}(x,y)$.

Apply
Theorem~\ref{thm:group-singleton-normalizer-action-classifier-determinacy} with
the conjugating endpoint $s$. The hypothesis $A_{\emp}(s)$ transports
$R_{\emp}(x,y)$ to
$$
R_{\emp}(\mathsf{Conj}_s(x),\mathsf{Conj}_s(y)).
$$

It remains to use orbit exactness in the reverse direction. The terminal
classifier package supplies the carrier rows for the two acted endpoints from
the displayed $R_{\emp}$ row, and then
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness} repacks that
classifier row as
$\mathsf{Conj}_s(x)\sim_{N_{\emp}}\mathsf{Conj}_s(y)$.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerOrbit\_action\_invariance}

\begin{theorem}[Group singleton normalizer action composition collapse]
\label{thm:group-singleton-normalizer-action-composition-collapse}
For histories $s,t,x:\mathsf{BHist}$ in the concrete singleton history group
instance, define
$$
\mathsf{Conj}_u(y):=\mathsf{mul}(\mathsf{mul}(u,y),\mathsf{inv}(u)).
$$
If $A_{\emp}(s)$, $A_{\emp}(t)$, and $A_{\emp}(x)$, then:
\begin{enumerate}
\item $A_{\emp}(\mathsf{Conj}_t(x))$.
\item $A_{\emp}(\mathsf{Conj}_s(\mathsf{Conj}_t(x)))$.
\item $A_{\emp}(\mathsf{Conj}_{\mathsf{mul}(s,t)}(x))$.
\item $R_{\emp}(\mathsf{Conj}_s(\mathsf{Conj}_t(x)),x)$.
\item $R_{\emp}(\mathsf{Conj}_{\mathsf{mul}(s,t)}(x),x)$.
\end{enumerate}
The fit names the traditional target as terminal normalizer composition; the
BEDC source as the concrete singleton $\GroupUp$ over $\mathsf{BHist}$; carrier
$A_{\emp}$; classifier $R_{\emp}$; stability from singleton multiplication
closure and terminal classifier exactness; ledger the two conjugating singleton
endpoints and the acted-on singleton endpoint; core theorem this block;
exactness predicate the displayed carrier and classifier collapse; downstream
consumer $\QuotientGroupUp$ orbit classification; and status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
First apply Theorem~\ref{thm:group-singleton-normalizer-action-certificate} to
$t$ and $x$. The hypotheses $A_{\emp}(t)$ and $A_{\emp}(x)$ give
$A_{\emp}(\mathsf{Conj}_t(x))$.

Use that carrier fact as the acted-on endpoint for a second application of
Theorem~\ref{thm:group-singleton-normalizer-action-certificate}, now with
conjugator $s$. Together with $A_{\emp}(s)$ it gives
$A_{\emp}(\mathsf{Conj}_s(\mathsf{Conj}_t(x)))$.

Next apply the multiplication-closure row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} to $s$ and $t$.
This places $\mathsf{mul}(s,t)$ in $A_{\emp}$. Applying
Theorem~\ref{thm:group-singleton-normalizer-action-certificate} to
$\mathsf{mul}(s,t)$ and $x$ gives
$A_{\emp}(\mathsf{Conj}_{\mathsf{mul}(s,t)}(x))$.

It remains to read the terminal classifier. The first row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} classifies any two
singleton endpoints. Apply it to the nested endpoint and $x$, then to the
product-conjugator endpoint and $x$, using the carrier facts already displayed.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerAction\_composition\_collapse}

\begin{theorem}[Group singleton normalizer action product-coherent orbit collapse]
\label{thm:group-singleton-normalizer-action-product-coherent-orbit-collapse}
For histories $s,t,x:\mathsf{BHist}$ in the concrete singleton history group
instance, write
$$
\mathsf{Conj}_u(y):=\mathsf{mul}(\mathsf{mul}(u,y),\mathsf{inv}(u)).
$$
If $A_{\emp}(s)$, $A_{\emp}(t)$, and $A_{\emp}(x)$, then
$$
R_{\emp}(\mathsf{Conj}_s(\mathsf{Conj}_t(x)),
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x))
$$
and
$$
R_{\emp}(\mathsf{Conj}_{\mathsf{mul}(s,t)}(x),
\mathsf{Conj}_s(\mathsf{Conj}_t(x))).
$$
The fit names the traditional target as product-coherent terminal normalizer
action; the BEDC source as the concrete singleton $\GroupUp$ over
$\mathsf{BHist}$; carrier $A_{\emp}$; classifier $R_{\emp}$; stability from
Theorem~\ref{thm:group-singleton-normalizer-action-composition-collapse};
ledger the nested action endpoint and the product-conjugator endpoint; core
theorem this block; exactness predicate mutual singleton classification of the
two endpoints; downstream consumer $\QuotientGroupUp$ orbit collapse; and status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:group-singleton-normalizer-action-composition-collapse}
to $s$, $t$, and $x$. Its carrier rows place both
$\mathsf{Conj}_s(\mathsf{Conj}_t(x))$ and
$\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)$ in $A_{\emp}$.

The terminal classifier exactness row of
Theorem~\ref{thm:group-singleton-terminal-classifier-package} classifies any two
singleton endpoints. Applied to those two carrier facts, it gives
$R_{\emp}(\mathsf{Conj}_s(\mathsf{Conj}_t(x)),
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x))$.

For the reverse classifier, use the symmetry row of the same terminal classifier
package on the displayed forward classifier. This yields
$R_{\emp}(\mathsf{Conj}_{\mathsf{mul}(s,t)}(x),
\mathsf{Conj}_s(\mathsf{Conj}_t(x)))$.
\end{proof}
\leanchecked{BEDC.Derived.GroupUp.GroupSingletonNormalizerAction\_product\_coherent\_orbit\_collapse}

\begin{theorem}[Group singleton normalizer action product-coherent orbit law]
\label{thm:group-singleton-normalizer-action-product-coherent-orbit-law}
For histories $s,t,x:\mathsf{BHist}$ in the concrete singleton history group
instance, write
$$
\mathsf{Conj}_u(y):=\mathsf{mul}(\mathsf{mul}(u,y),\mathsf{inv}(u)).
$$
If $A_{\emp}(s)$, $A_{\emp}(t)$, and $A_{\emp}(x)$, then
$$
\mathsf{Conj}_s(\mathsf{Conj}_t(x))\sim_{N_{\emp}}
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)
\land
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)\sim_{N_{\emp}}
\mathsf{Conj}_s(\mathsf{Conj}_t(x)).
$$
The ten-field fit names the traditional target as terminal normalizer action
composition on orbits; BEDC source concrete singleton $\GroupUp$ over
$\mathsf{BHist}$; carrier $A_{\emp}$; classifier $\sim_{N_{\emp}}$; stability
from orbit exactness and product-coherent classifier collapse; ledger the two
conjugators, product conjugator, and acted endpoint; core theorem this block;
exactness the displayed orbit-level product law; downstream consumer
$\QuotientGroupUp$ action composition; and status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
First apply
Theorem~\ref{thm:group-singleton-normalizer-action-composition-collapse} to
$s$, $t$, and $x$. Its carrier rows place
$\mathsf{Conj}_s(\mathsf{Conj}_t(x))$ and
$\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)$ in $A_{\emp}$.

Next apply
Theorem~\ref{thm:group-singleton-normalizer-action-product-coherent-orbit-collapse}.
This gives the two terminal classifier rows between the nested conjugation
endpoint and the product-conjugator endpoint.

Finally use Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness} in the
classifier-to-orbit direction twice. The first use turns the forward
$R_{\emp}$ row into
$\mathsf{Conj}_s(\mathsf{Conj}_t(x))\sim_{N_{\emp}}
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)$, and the second use turns the reverse
$R_{\emp}$ row into
$\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)\sim_{N_{\emp}}
\mathsf{Conj}_s(\mathsf{Conj}_t(x))$.
\end{proof}

\begin{theorem}[Group singleton normalizer orbit action law package]
\label{thm:group-singleton-normalizer-orbit-action-law-package}
For histories $s,t,x,y:\mathsf{BHist}$ in the concrete singleton history group
instance, if $A_{\emp}(s)$, $A_{\emp}(t)$, $A_{\emp}(x)$, $A_{\emp}(y)$, and
$x\sim_{N_{\emp}}y$, then:
\begin{enumerate}
\item $\mathsf{Conj}_s(x)\sim_{N_{\emp}}\mathsf{Conj}_s(y)$.
\item
$\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x))\sim_{N_{\emp}}y$.
\item
$\mathsf{Conj}_s(\mathsf{Conj}_t(x))\sim_{N_{\emp}}
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)$.
\end{enumerate}
The ten-field fit names the traditional target as a concrete terminal
normalizer orbit action law package; the BEDC source as the concrete singleton
$\GroupUp$ over $\mathsf{BHist}$; carrier $A_{\emp}$; classifier the
normalizer orbit relation exact against $R_{\emp}$; stability from orbit
exactness, classifier determinacy, and product-coherent orbit collapse; ledger
the two conjugating singleton endpoints, the acted-on singleton endpoint, and
the orbit witness; core theorem this block; exactness predicate the three
displayed orbit action laws; downstream consumer $\QuotientGroupUp$; and status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
First convert the input orbit witness into the concrete classifier. Since $x$
and $y$ lie in $A_{\emp}$, the forward direction of
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness} gives
$R_{\emp}(x,y)$.

For the first law, apply
Theorem~\ref{thm:group-singleton-normalizer-orbit-action-invariance} directly
to $s$, $x$, and $y$. Its hypotheses are exactly $A_{\emp}(s)$,
$A_{\emp}(x)$, $A_{\emp}(y)$, and the given orbit relation.

For the inverse-roundtrip law, apply
Theorem~\ref{thm:group-singleton-normalizer-action-classifier-determinacy} with
the conjugating endpoint $s$ and the classifier row $R_{\emp}(x,y)$. The second
row gives
$$
R_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x)),y).
$$
The reverse direction of
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness} repacks this
classifier as
$\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x))\sim_{N_{\emp}}y$.

For the product law, apply
Theorem~\ref{thm:group-singleton-normalizer-action-product-coherent-orbit-collapse}
to $s$, $t$, and $x$. Its forward classifier row is
$$
R_{\emp}(\mathsf{Conj}_s(\mathsf{Conj}_t(x)),
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)).
$$
Again use the reverse direction of orbit exactness to convert that classifier
into the displayed orbit relation.
\end{proof}

\begin{theorem}[Group singleton normalizer orbit action unital product package]
\label{thm:group-singleton-normalizer-orbit-action-unital-product-package}
For histories $s,t,x,y:\mathsf{BHist}$ in the concrete singleton history group
instance, if $A_{\emp}(s)$, $A_{\emp}(t)$, $A_{\emp}(x)$, $A_{\emp}(y)$, and
$x\sim_{N_{\emp}}y$, then:
\begin{enumerate}
\item
$\mathsf{Conj}_{\emp}(x)\sim_{N_{\emp}}x$ and
$x\sim_{N_{\emp}}\mathsf{Conj}_{\emp}(x)$.
\item $\mathsf{Conj}_s(x)\sim_{N_{\emp}}\mathsf{Conj}_s(y)$.
\item
$\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x))\sim_{N_{\emp}}y$.
\item
$\mathsf{Conj}_s(\mathsf{Conj}_t(x))\sim_{N_{\emp}}
\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)$ and
$\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)\sim_{N_{\emp}}
\mathsf{Conj}_s(\mathsf{Conj}_t(x))$.
\end{enumerate}
The ten-field fit names the traditional target as terminal normalizer action as
a unital product action on the orbit quotient; BEDC source concrete singleton
$\GroupUp$ over $\mathsf{BHist}$; carrier $A_{\emp}$; classifier
$\sim_{N_{\emp}}$; stability from orbit exactness and the normalizer action
rows; ledger the identity conjugator, two product conjugators, acted endpoint,
and orbit witness; core theorem list
Theorem~\ref{thm:group-singleton-normalizer-action-certificate},
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness},
Theorem~\ref{thm:group-singleton-normalizer-orbit-action-invariance},
Theorem~\ref{thm:group-singleton-normalizer-action-classifier-determinacy}, and
Theorem~\ref{thm:group-singleton-normalizer-action-product-coherent-orbit-law};
exactness the displayed package; standard bridge the terminal group action on
its one-point orbit; and status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For the identity rows, apply
Theorem~\ref{thm:group-singleton-normalizer-action-certificate} with
conjugator $\emp$ and endpoint $x$. It gives the carrier data for
$\mathsf{Conj}_{\emp}(x)$ and the classifier row
$$
R_{\emp}(\mathsf{Conj}_{\emp}(x),x).
$$
The reverse direction of
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness} converts this
classifier into
$\mathsf{Conj}_{\emp}(x)\sim_{N_{\emp}}x$.

For the opposite identity orientation, use the singleton classifier symmetry
row already available to the action certificate to turn the displayed
classifier into $R_{\emp}(x,\mathsf{Conj}_{\emp}(x))$. A second reverse use of
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness} gives
$x\sim_{N_{\emp}}\mathsf{Conj}_{\emp}(x)$.

For action invariance, apply
Theorem~\ref{thm:group-singleton-normalizer-orbit-action-invariance} directly
to $s$, $x$, and $y$. The hypotheses are the carrier rows for $s$, $x$, and
$y$, together with the given orbit witness.

For the inverse-roundtrip row, first convert the orbit witness
$x\sim_{N_{\emp}}y$ to $R_{\emp}(x,y)$ by the forward direction of
Theorem~\ref{thm:group-singleton-normalizer-orbit-exactness}. Then apply
Theorem~\ref{thm:group-singleton-normalizer-action-classifier-determinacy} to
$s$ and this classifier row. Its second classifier output is
$$
R_{\emp}(\mathsf{Conj}_{\mathsf{inv}(s)}(\mathsf{Conj}_s(x)),y).
$$
The reverse direction of orbit exactness repacks this row as the displayed
orbit relation.

For product coherence, apply
Theorem~\ref{thm:group-singleton-normalizer-action-product-coherent-orbit-law}
to $s$, $t$, and $x$. Its two orbit outputs are exactly the forward and reverse
orientations between
$\mathsf{Conj}_s(\mathsf{Conj}_t(x))$ and
$\mathsf{Conj}_{\mathsf{mul}(s,t)}(x)$.
\end{proof}
