\chapter{A Concrete Naming Certificate for $\CauchyCompletionProductPreservationUp$}
\label{ch:concrete-instances-cauchy-completion-product-preservation-namecert}
\origin{human}

Following Bishop and Bridges' constructive completion theory and Mac Lane's finite-product interface, the $\CauchyCompletionProductPreservationUp$ packet records the finite BEDC surface at which completion reads commute with binary product reads. It consumes the Cauchy completion monad route of \autoref{ch:concrete-instances-cauchycompletionmonad-namecert}, the metric-completion surface of \autoref{ch:concrete-instances-metriccompletion-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the product naming envelope of \autoref{ch:concrete-instances-prod-namecert}. The packet names a finite product-preservation handoff; it does not assert quotient completion equality, selected limits, countable choice, or an ambient completed product.

\begin{definition}[Cauchy completion product-preservation carrier]
\label{def:cauchy-completion-product-preservation-carrier}
A $\CauchyCompletionProductPreservationUp$ carrier is a finite $\BHist$ packet
$$
\mathsf{CPP}(L,R,M,\Pi,S,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the left source-completion route, read through the Cauchy completion monad and the displayed $\MetricCompletionUp$ source.
\item $R$ is the right source-completion route, read through the same completion discipline for the second source.
\item $M$ is the product metric row, recording the finite product carrier, projection rows, and coordinatewise tolerance ledger.
\item $\Pi$ is the product projection row, exposing the two completed-coordinate projections and their recombination read.
\item $S$ is the $\RegSeqRatUp$ readback row for the paired finite windows and coordinatewise regular rational data.
\item $E$ is the terminal $\RealUp$ seal row for the coordinate tolerances and the product distance read.
\item $H$ stores componentwise $\hsame$ transport for the two completion routes, product row, projection row, readback, seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a product-completion request through $L,R,M,\Pi,S,E$.
\item $P$ is the $\Pkg$ provenance over $\CauchyCompletionProductPreservationUp$, $\CauchyCompletionMonadUp$, $\MetricCompletionUp$, $\RegSeqRatUp$, $\RealUp$, $\ProdUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCompletionProductPreservationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,R,M,\Pi,S,E,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, countable-choice schedules, ambient product completion, or host equality of completed points.
\end{definition}

\begin{theorem}[Cauchy completion product-preservation NameCert obligations]
\label{thm:cauchy-completion-product-preservation-namecert-obligations}
For every accepted $\CauchyCompletionProductPreservationUp$ carrier $\mathsf{CPP}(L,R,M,\Pi,S,E,H,C,P,N)$, the local $\NameCert_{\CauchyCompletionProductPreservationUp}$ surface consists exactly of carrier admission for the two source completion routes, product metric row, projection row, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming rows; coordinate compatibility through $L,R,M,\Pi$; readback compatibility through $S,E$; and non-escape from quotient completions, selected limits, countable choice, ambient completed products, and host equality.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-completion-product-preservation-carrier}. The displayed tuple exposes exactly the two completion routes, product row, projection row, readback row, Real seal, and structural rows.

Coordinate compatibility is forced by $L,R,M,\Pi$. The left and right completion routes are read before the product projection row, and the product row records only the coordinatewise product carrier and finite tolerance ledger.

Readback compatibility is then carried by $S$ and $E$. The regular rational row reads paired finite windows, and the terminal Real seal presents only the coordinate tolerances and the product distance read produced from those windows.

Finally $H,C,P,N$ transport, replay, source, and name the same finite rows. Since the carrier has no coordinate for quotient completion equality, selected representatives, countable choice, ambient product completion, or host equality, the listed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Cauchy completion finite-product handoff]
\label{thm:cauchy-completion-product-preservation-finite-product-handoff}
Every downstream consumer that reads completion after a finite product, or a finite product after two completion reads, must factor through the same displayed route
$$
\CauchyCompletionMonadUp \longrightarrow \MetricCompletionUp \longrightarrow \ProdUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp .
$$
The exported object is the finite product-preservation handoff carried by $L,R,M,\Pi,S,E,H,C,P,N$, not a quotient-level isomorphism of completions and not a selected-limit construction.
\end{theorem}
\begin{proof}
Start with the accepted carrier of \autoref{def:cauchy-completion-product-preservation-carrier}. The only completion-facing rows are $L$ and $R$, and both are routed through the Cauchy completion monad and the metric-completion source surface.

The product read is $M,\Pi$. These rows expose the finite product carrier, coordinate projections, recombination read, and coordinatewise tolerance ledger without adding an ambient completed product.

The numerical handoff is $S,E$. Paired finite windows are read through $\RegSeqRatUp$, and the terminal $\RealUp$ seal records the coordinate tolerances and product distance read.

Apply \autoref{thm:cauchy-completion-product-preservation-namecert-obligations}. The transport, replay, provenance, and local naming rows preserve exactly that route, so every finite-product completion consumer factors through the displayed handoff and cannot export quotient equality or selected limits.
\end{proof}

\begin{theorem}[Cauchy completion product-preservation scoped ledger]
\label{thm:cauchy-completion-product-preservation-scope-ledger}
Every accepted $\CauchyCompletionProductPreservationUp$ packet factors its visible product-preservation read through exactly the following scoped ledger:
\begin{enumerate}
\item the two completion routes $L$ and $R$, each read through $\CauchyCompletionMonadUp$ and $\MetricCompletionUp$;
\item the product metric row $M$ and projection row $\Pi$, including the displayed finite product carrier, coordinate projections, recombination read, and coordinatewise tolerance ledger;
\item the paired $\RegSeqRatUp$ readback row $S$ and terminal $\RealUp$ seal row $E$;
\item componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\CauchyCompletionProductPreservationUp}$ row $N$.
\end{enumerate}
No accepted packet exports quotient completion equality, selected-limit data, countable-choice schedules, ambient completed products, or host equality of completed points outside this ledger.
\end{theorem}
\begin{proof}
Start from \autoref{thm:cauchy-completion-product-preservation-finite-product-handoff}. The handoff route already forces every completion-product read through the displayed completion, metric, product, regular-rational, and Real rows.

The carrier of \autoref{def:cauchy-completion-product-preservation-carrier} names the ledger entries. The rows $L$ and $R$ are the two source completion routes, while $M$ and $\Pi$ are the only product metric and projection rows.

The numerical readback is confined to $S$ and $E$. These rows pair the regular rational windows and then seal the coordinate tolerances and product distance read at the $\RealUp$ boundary.

Finally $H,C,P,N$ preserve the same ledger under transport, replay, provenance, and local naming. Since no other carrier coordinate exists, the scoped ledger is exact and the excluded quotient, selection, choice, ambient-product, and host-equality data cannot be exported.
\end{proof}

\closureat{CauchyCompletionProductPreservationUp}{seedStr}

\begin{closurestatus}{\CauchyCompletionProductPreservationUp}
  \constructivestory{A $\CauchyCompletionProductPreservationUp$ packet is generated from two CauchyCompletionMonad and MetricCompletion source routes, a product metric row, projection rows, RegSeqRat paired-window readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-completion-product-preservation-carrier}, \autoref{thm:cauchy-completion-product-preservation-namecert-obligations}, \autoref{thm:cauchy-completion-product-preservation-finite-product-handoff}, and \autoref{thm:cauchy-completion-product-preservation-scope-ledger} seed the finite completion-product handoff over CauchyCompletionMonadUp, MetricCompletionUp, ProdUp, RegSeqRatUp, RealUp, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected representatives, selected limits, countable choice, ambient completed products, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, coordinate compatibility, paired RegSeqRat readback, Real seal exactness, projection non-escape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyCompletionProductPreservationUp.}
\end{closurestatus}
