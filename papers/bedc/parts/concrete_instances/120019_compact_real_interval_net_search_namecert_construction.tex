\chapter{A Concrete Naming Certificate for $\CompactRealIntervalNetSearchUp$}
\label{ch:concrete-instances-compact-real-interval-net-search-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\CompactRealIntervalNetSearchUp$ packet records the finite-net search route for a continuous real-valued map on a displayed compact real interval. It sits after the dyadic endpoint core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the uniform-modulus surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite epsilon-net carrier of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, and the located bisection route of \autoref{ch:concrete-instances-located-interval-bisection-namecert}. The packet names a finite compact-interval search carrier; it does not invoke open-cover compactness, selected host extrema, quotient real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Compact real interval net-search carrier]
\label{def:compact-real-interval-net-search-carrier}
A $\CompactRealIntervalNetSearchUp$ carrier is a finite $\BHist$ packet
$$
S=(I,D,U,B,V,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the compact real interval endpoint row. Its lower and upper endpoints are read through $\DyadicRatCoreUp$, finite $\StreamNameUp$ windows, and terminal $\RealUp$ names.
\item $D$ is the dyadic finite-net cell row, listing the mesh cells, their dyadic endpoints, center probes, and coverage witnesses consumed from $\FiniteEpsilonNetUp$.
\item $U$ is the $\UniformModulusUp$ row for the displayed $\ContinuousMapUp$ graph on the interval source named by $I$.
\item $B$ is the located bisection row, recording the retained cell branch and the finite bisection depth read through $\LocatedIntervalBisectionUp$.
\item $V$ is the value-bracket row, storing lower and upper value comparisons for the inspected cells with the tolerance budget supplied by $U$.
\item $R$ is the $\RegSeqRatUp$ readback row for the dyadic endpoints, cell centers, tolerance comparisons, bisection depth, and value brackets.
\item $E$ is the terminal $\RealUp$ seal reached after the interval endpoints, finite-net cells, bisection row, value bracket, and regular rational readback have been exposed.
\item $H$ stores componentwise $\hsame$ transport for the interval endpoints, dyadic finite-net cells, uniform-modulus row, located bisection, value bracket, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a compact-interval search request through $I,D,U,B,V,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactRealIntervalNetSearchUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\UniformModulusUp$, $\ContinuousMapUp$, $\FiniteEpsilonNetUp$, $\LocatedIntervalBisectionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactRealIntervalNetSearchUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $I,D,U,B,V,R,E,H,C,P,N$. No host-selected extremum, open-cover compactness proof, quotient equality of real names, or ambient completeness coordinate is part of the packet.
\end{definition}

\begin{theorem}[Compact real interval net-search obligations]
\label{thm:compact-real-interval-net-search-obligations}
For every accepted $\CompactRealIntervalNetSearchUp$ carrier $S=(I,D,U,B,V,R,E,H,C,P,N)$, the local $\NameCert_{\CompactRealIntervalNetSearchUp}$ surface consists exactly of carrier admission for the interval endpoints, dyadic finite-net cells, uniform-modulus data, located bisection, value bracket, regular rational readback, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming rows. A consumer must read $I,D,U,B,V$ before it may inspect the readback $R$ or the Real seal $E$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-real-interval-net-search-carrier}. The displayed analytic rows are the interval endpoint row $I$, the dyadic finite-net row $D$, the uniform-modulus row $U$, the located bisection row $B$, and the value bracket row $V$.

The readback rows are downstream. The regular rational row $R$ repacks only the dyadic endpoints, mesh centers, bisection depth, and value comparisons already displayed by $I,D,U,B,V$, and the Real seal $E$ is terminal after that readback.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier contains no open-cover compactness proof, host-selected extremum, quotient equality coordinate, or ambient completeness row, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact real interval net-search dyadic-net soundness]
\label{thm:compact-real-interval-net-search-dyadic-net-soundness}
Every accepted $\CompactRealIntervalNetSearchUp$ search read factors through the dyadic finite-net route
$$
I,D,U \longmapsto B,V \longmapsto R,E,H,C,P,N .
$$
The finite cells in $D$ cover the displayed interval endpoints of $I$, the located bisection row $B$ refines only those cells, and the value bracket $V$ uses only $\ContinuousMapUp$ values controlled by $U$. A Real-facing consumer receives no cell outside the finite net and no value comparison detached from the displayed modulus budget.
\end{theorem}
\begin{proof}
Start from $I$ and $D$. The interval endpoints and dyadic mesh cells are displayed before the search begins, so every cell inspected by the packet is a row of the finite net.

Use $U$ and $B$ next. The uniform-modulus row supplies the tolerance budget for graph reads, while the located bisection row refines a displayed cell from $D$ rather than choosing a point externally.

The value bracket $V$ is then attached to those cells and to that modulus budget. The readback and seal rows $R,E$ can only package the resulting finite data, and $H,C,P,N$ preserve the same route for downstream consumers.
\end{proof}

\begin{theorem}[Compact real interval net-search termination]
\label{thm:compact-real-interval-net-search-termination}
For an accepted $\CompactRealIntervalNetSearchUp$ carrier $S=(I,D,U,B,V,R,E,H,C,P,N)$, the search terminates at the displayed finite depth of $B$. The terminal read returns a finite cell family and value bracket whose mesh cells are listed in $D$, whose tolerance budget is supplied by $U$, and whose Real-facing read passes through $R$ and $E$.
\end{theorem}
\begin{proof}
The bisection row $B$ is finite carrier data. Its depth is a displayed row, not a function selected from an ambient compactness theorem.

Apply dyadic-net soundness from \autoref{thm:compact-real-interval-net-search-dyadic-net-soundness}. Each bisection step refines a cell already listed by $D$ and reads values with the tolerance budget supplied by $U$.

At the terminal depth, the packet returns only the cell rows and value brackets stored in $V$. The regular rational readback $R$ and the Real seal $E$ package those rows, so termination is a finite ledger fact of the carrier.
\end{proof}

\begin{theorem}[Compact real interval net-search NameCert export]
\label{thm:compact-real-interval-net-search-namecert-export}
The public $\NameCert_{\CompactRealIntervalNetSearchUp}$ export is exactly the finite route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \UniformModulusUp,\ \ContinuousMapUp,\ \FiniteEpsilonNetUp,\ \LocatedIntervalBisectionUp
\longmapsto
I,D,U,B,V,R,E,H,C,P,N .
$$
It exports a compact-interval finite-net search certificate for downstream real-completion, uniform-continuity, and extreme-value consumers, while excluding open-cover compactness, host-selected extrema, quotient real equality, and ambient Real completeness.
\end{theorem}
\begin{proof}
Combine the carrier obligations of \autoref{thm:compact-real-interval-net-search-obligations} with dyadic-net soundness in \autoref{thm:compact-real-interval-net-search-dyadic-net-soundness}. The source rows are exactly the dyadic endpoint core, finite stream windows, regular rational readback, Real seal, uniform modulus, continuous-map graph, finite epsilon-net cells, and located bisection route.

Apply termination from \autoref{thm:compact-real-interval-net-search-termination}. The exported search result is a finite terminal cell family with value brackets, transported and replayed through $H,C$ and named by $P,N$.

The excluded principles are absent from the carrier. Therefore no public consumer can obtain open-cover compactness, a host-selected extremum, quotient real equality, or ambient Real completeness from this NameCert export.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactRealIntervalNetSearchUp$ read must display interval endpoints, dyadic finite-net cells, a uniform-modulus row, located bisection, value brackets, RegSeqRat readback, Real sealing, hsame transport, Cont replay, Pkg provenance, and local NameCert before any compact-interval search result is consumed.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, uniformmodulus, continuousmap, finite-epsilon-net, located-interval-bisection: $\CompactRealIntervalNetSearchUp$ is not bijective to any listed sibling because it jointly carries interval endpoints, dyadic finite-net cells, uniform-modulus data, located bisection, value brackets, readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{CompactRealIntervalNetSearchUp}{seedStr}

\begin{closurestatus}{\CompactRealIntervalNetSearchUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-real-interval-net-search-carrier}, \autoref{thm:compact-real-interval-net-search-obligations}, \autoref{thm:compact-real-interval-net-search-dyadic-net-soundness}, \autoref{thm:compact-real-interval-net-search-termination}, and \autoref{thm:compact-real-interval-net-search-namecert-export} seed the constructive compact real interval finite-net search route over dyadic endpoints, finite cells, uniform-modulus data, located bisection, value brackets, readback, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, host-selected extrema, quotient real equality, ambient Real completeness, Lean verification, or bridge checking for $\CompactRealIntervalNetSearchUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic-net soundness, finite-depth termination, readback exactness, Real-seal nonescape, transport stability, and public NameCert export for BEDC.Derived.CompactRealIntervalNetSearchUp.}
  \constructivestory{A $\CompactRealIntervalNetSearchUp$ packet is generated from interval endpoints, dyadic finite-net cells, UniformModulusUp data, ContinuousMapUp graph rows, a LocatedIntervalBisectionUp finite branch, value brackets, RegSeqRatUp readback, RealUp sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
