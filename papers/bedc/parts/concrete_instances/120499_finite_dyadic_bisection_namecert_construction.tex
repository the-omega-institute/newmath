\chapter{A Concrete Naming Certificate for $\FiniteDyadicBisectionUp$}
\label{ch:concrete-instances-finite-dyadic-bisection-namecert}
\origin{human}

Following Bishop--Bridges constructive bisection and nested interval practice, the $\FiniteDyadicBisectionUp$ packet records a finite dyadic interval-halving computation as BEDC NameCert data. It sits after the dyadic endpoint rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the nested dyadic fusion route of \autoref{ch:concrete-instances-nested-dyadic-interval-fusion-namecert}. The packet supplies Real and Completion arguments with a finite bisection carrier without importing open-cover compactness, a selected host limit, quotient streams, trichotomy, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Finite dyadic bisection carrier]
\label{def:finite-dyadic-bisection-carrier}
A $\FiniteDyadicBisectionUp$ carrier is a finite $\BHist$ packet
$$
B=(I,D,M,L,R,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite interval-chain row, recording the input dyadic endpoint pair and each retained dyadic child interval.
\item $D$ is the $\DyadicRatCoreUp$ endpoint ledger, storing the displayed endpoint comparisons, width budget, and dyadic denominator data for every row of $I$.
\item $M$ is the midpoint split row, computed from the current dyadic endpoints through the same ledger $D$.
\item $L$ is the selected left-child row when the displayed finite test keeps the left half.
\item $R$ is the selected right-child row when the displayed finite test keeps the right half.
\item $S$ is the $\StreamNameUp$ finite-window row opened for the endpoint, midpoint, and selected-child observations.
\item $Q$ is the $\RegSeqRatUp$ readback row formed from those finite windows and dyadic tolerance cells.
\item $E$ is the $\RealUp$ seal row, available only after $I,D,M,L,R,S,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the interval chain, dyadic ledger, midpoint split, selected child row, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite bisection request through $I,D,M,L,R,S,Q,E$ and then through the structural rows.
\item $P$ is the $\Pkg$ provenance over $\FiniteDyadicBisectionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteDyadicBisectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,D,M,L,R,S,Q,E,H,C,P,N$. It has no coordinate for an infinite bisection tree, quotient stream equality, a selected completed real, open-cover compactness, trichotomy, countable choice, or ambient real completeness.
\end{definition}
\leanchecked{BEDC.Derived.FiniteDyadicBisectionUp.FiniteDyadicBisectionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Finite dyadic bisection NameCert obligations]
\label{thm:finite-dyadic-bisection-namecert-obligations}
For every accepted $\FiniteDyadicBisectionUp$ carrier
$$
B=(I,D,M,L,R,S,Q,E,H,C,P,N),
$$
the local $\NameCert_{\FiniteDyadicBisectionUp}$ surface consists exactly of finite interval-chain exposure through $I$, dyadic endpoint and width exposure through $D$, midpoint split exposure through $M$, selected-child exposure through $L$ or $R$, finite observation scheduling through $S$, regular rational readback through $Q$, terminal real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-dyadic-bisection-carrier}. The projection exposes the finite interval chain, dyadic ledger, midpoint split, selected child rows, finite observation window, regular rational readback, Real seal, transport, replay, provenance, and naming rows.

The analytic rows are ordered. The endpoint ledger $D$ can be read only after the current finite interval row $I$ is displayed, and the midpoint split $M$ is computed from that ledger.

The selected-child rows are finite alternatives inside the same packet. The left row $L$ and right row $R$ are not host choices; they are displayed child rows whose source endpoint and midpoint data remain in $I,D,M$.

The downstream rows $S,Q,E$ then expose only finite windows, regular rational readback, and the terminal Real seal. The structural rows $H,C,P,N$ transport, replay, source, and name the same route, so the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Finite dyadic bisection induction route]
\label{thm:finite-dyadic-bisection-bisection-induction}
Every finite induction over an accepted $\FiniteDyadicBisectionUp$ packet proceeds by the displayed route
$$
I,D \longmapsto M \longmapsto L,R \longmapsto S,Q,E,H,C,P,N .
$$
The base step reads the initial dyadic endpoint row and the first width budget. Each successor step consumes the current endpoint row, computes the midpoint split, selects one displayed child row, and then replays the same $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ downstream route. The induction exports no infinite branch selector, quotient stream equality, selected host limit, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use induction on the finite interval-chain row $I$. In the base case, the packet displays the initial dyadic endpoints and width budget through $D$, so the downstream rows cannot be read before that finite row is visible.

For the successor step, assume the current finite prefix has been read through $I,D$. The midpoint row $M$ is computed from the displayed dyadic endpoints, and the retained child is recorded by one of the finite rows $L$ or $R$.

The observation and completion-facing rows are downstream from this selected child. The $\StreamNameUp$ window $S$ opens only the endpoint and midpoint observations named by the finite step, the $\RegSeqRatUp$ readback $Q$ reads that window, and the $\RealUp$ seal $E$ is consumed only after $Q$ is displayed.

Finally $H,C,P,N$ preserve the same finite route under transport, replay, provenance, and local naming. Since the carrier contains no infinite branch selector, quotient stream, selected limit, compactness principle, or completeness coordinate, the induction remains a finite bisection route.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteDyadicBisectionUp$ Real-facing read exposes a finite dyadic interval chain, DyadicRatCore endpoint ledger, midpoint split, selected child row, StreamName finite window, RegSeqRat readback, RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert row before the terminal seal is consumed.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, nested-dyadic-interval-fusion: $\FiniteDyadicBisectionUp$ is not bijective to any listed sibling because it jointly carries a finite dyadic interval-chain, midpoint split, selected child row, downstream readback, Real seal, transport, replay, provenance, and local naming as one finite bisection packet.}

\closureat{FiniteDyadicBisectionUp}{seedStr}

\begin{closurestatus}{\FiniteDyadicBisectionUp}
  \constructivestory{A $\FiniteDyadicBisectionUp$ packet is generated from finite dyadic interval-chain rows, DyadicRatCore endpoint and width rows, midpoint split rows, selected child rows, StreamName finite observations, RegSeqRat readback, a RealUp terminal seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-dyadic-bisection-carrier}, \autoref{thm:finite-dyadic-bisection-namecert-obligations}, and \autoref{thm:finite-dyadic-bisection-bisection-induction} seed the finite dyadic bisection route from displayed dyadic endpoint data to a Real-facing readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, infinite branch selection, quotient stream equality, selected host limits, trichotomy, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic endpoint exposure, midpoint splitting, selected-child stability, StreamName scheduling, RegSeqRat readback, Real handoff, transport, replay, provenance, and local naming for BEDC.Derived.FiniteDyadicBisectionUp.}
\end{closurestatus}
