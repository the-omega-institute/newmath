\chapter{A Concrete Naming Certificate for $\CompactModulusExtractionUp$}
\label{ch:concrete-instances-compact-modulus-extraction-namecert}
\origin{human}

Following Bishop and Bridges on compact metric continuity, the $\CompactModulusExtractionUp$ packet records the finite extraction route by which pointwise moduli over a compact finite net are folded into a displayed uniform modulus. It sits over the compact source rows of \autoref{ch:concrete-instances-compact-namecert}, the continuous-map pointwise-modulus surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the metric triangle rows of \autoref{ch:concrete-instances-metric-namecert}, the rational lower-bound fold of \autoref{ch:concrete-instances-rat-namecert}, and the output interface of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names the constructive Heine--Cantor finite-net handoff as local NameCert data; it does not add open-cover compactness, selected subcovers, host function equality, real completeness, or an external continuity theorem.

\begin{definition}[Compact modulus extraction carrier]
\label{def:compact-modulus-extraction-carrier}
A $\CompactModulusExtractionUp$ carrier is a finite $\BHist$ packet
$$
M=(K,F,Q,T,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ finite-net row, listing the inspected centers, coverage radius, and finite adjacency ledger.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row attached to each displayed net center.
\item $Q$ is the $\RatUp$ finite lower-bound fold row, computing the positive rational tolerance budget over the displayed pointwise moduli.
\item $T$ is the $\MetricSpaceUp$ triangle row that routes nearby source points through a chosen net center and then through the target comparison.
\item $U$ is the $\UniformModulusUp$ handoff row obtained from $K,F,Q,T$.
\item $H$ records componentwise $\hsame$ transport for $K,F,Q,T,U$ and the local certificate rows.
\item $C$ records displayed $\Cont$ replay from finite-net coverage through pointwise moduli, rational fold, metric triangle comparison, and uniform-modulus output.
\item $P$ is the $\Pkg$ provenance over $\CompactModulusExtractionUp$, $\CompactUp$, $\ContinuousMapUp$, $\RatUp$, $\MetricSpaceUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactModulusExtractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,Q,T,U,H,C,P,N$. It has no coordinate for a selected open subcover, host continuity theorem, quotient function equality, arbitrary supremum, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact modulus extraction NameCert obligations]
\label{thm:compact-modulus-extraction-namecert-obligations}
For every accepted $\CompactModulusExtractionUp$ carrier $M=(K,F,Q,T,U,H,C,P,N)$, the local $\NameCert_{\CompactModulusExtractionUp}$ surface consists exactly of carrier admission, compact finite-net exposure through $K$, pointwise-modulus exposure through $F$, rational lower-bound fold exposure through $Q$, metric triangle exposure through $T$, UniformModulus handoff through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. The rows $K,F,Q,T,U$ are the only mathematical data coordinates available to a compact-uniform-continuity consumer.

The compact row supplies finite centers, the continuous-map row supplies pointwise moduli at those centers, the rational row folds the displayed positive bounds, and the metric row supplies the triangle comparison that lets the folded bound apply to a nearby source point.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. No selected subcover, host theorem, quotient function equality, arbitrary supremum, or ambient completeness row remains outside the listed obligations.
\end{proof}

\begin{theorem}[Compact modulus extraction finite-net fold]
\label{thm:compact-modulus-extraction-finite-net-fold}
Every accepted $\CompactModulusExtractionUp$ packet extracts its uniform modulus only through
$$
K,F\longmapsto Q\longmapsto T\longmapsto U .
$$
The rational fold $Q$ is computed over the finite centers displayed by $K$ and the pointwise moduli displayed by $F$, and the metric triangle row $T$ is consumed before the $\UniformModulusUp$ output row $U$ is public.
\end{theorem}
\begin{proof}
Start from the compact finite-net row $K$. It displays the finite list of centers and coverage data, so the fold is over a finite $\BHist$ ledger rather than over an ambient compactness principle.

Read $F$ at each displayed center and then fold the resulting rational lower bounds through $Q$. Since $Q$ is a $\RatUp$ row, the consumer sees the finite rational computation rather than a selected infimum.

Finally apply the metric triangle row $T$ and expose $U$. The output uniform modulus is therefore downstream of the finite net, pointwise moduli, rational fold, and triangle comparison.
\end{proof}

\begin{theorem}[Compact modulus extraction transport nonescape]
\label{thm:compact-modulus-extraction-transport-nonescape}
Let $M=(K,F,Q,T,U,H,C,P,N)$ and $M'=(K',F',Q',T',U',H',C',P',N')$ be accepted $\CompactModulusExtractionUp$ carriers. If $H$ transports the compact, pointwise-modulus, rational-fold, metric-triangle, and uniform-modulus rows componentwise, then the extracted $\UniformModulusUp$ row transports only through $K,F,Q,T,U,H,C,P,N$.
\end{theorem}
\begin{proof}
Inspect the classifier of \autoref{def:compact-modulus-extraction-carrier}. It names only the finite-net, pointwise-modulus, rational-fold, metric-triangle, output, and structural rows.

Componentwise $\hsame$ transport moves each of those rows to its displayed counterpart, while $\Cont$ replay preserves the route from $K,F$ through $Q,T$ to $U$.

No carrier coordinate supplies host function equality, selected open covers, arbitrary suprema, or ambient completeness. The transported output is therefore the same finite extraction route and nothing stronger.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactModulusExtractionUp$ uniform-modulus read factors through one compact finite-net row, one ContinuousMap pointwise-modulus row, one RatUp finite lower-bound fold row, one MetricSpace triangle row, one UniformModulus handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{compact, continuousmap, metric, rat, uniformmodulus: $\CompactModulusExtractionUp$ is not bijective to any listed sibling because it jointly binds finite compact-net rows, pointwise-modulus rows, rational lower-bound folds, metric triangle rows, and a UniformModulus handoff as one extraction packet.}

\closureat{CompactModulusExtractionUp}{seedStr}

\begin{closurestatus}{\CompactModulusExtractionUp}
  \constructivestory{A $\CompactModulusExtractionUp$ packet is generated from compact finite-net rows, ContinuousMap pointwise-modulus rows, RatUp lower-bound folds, MetricSpace triangle rows, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-modulus-extraction-carrier}, \autoref{thm:compact-modulus-extraction-namecert-obligations}, \autoref{thm:compact-modulus-extraction-finite-net-fold}, and \autoref{thm:compact-modulus-extraction-transport-nonescape} seed the finite compact-modulus extraction route over compact, continuous-map, rational-fold, metric-triangle, UniformModulus, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, host function equality, arbitrary suprema, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\CompactModulusExtractionUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, pointwise-modulus exposure, rational lower-bound fold, metric triangle handoff, UniformModulus output, transport, replay, provenance, and local naming for BEDC.Derived.CompactModulusExtractionUp.}
\end{closurestatus}
