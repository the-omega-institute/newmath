\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseHolonomyUp$}
\label{ch:concrete-instances-hyperbolic-phase-holonomy-namecert}
\origin{ai}

The $\HyperbolicPhaseHolonomyUp$ packet realizes the closed-loop transport reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits beside the Poincare disk phase-transport route of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, the observer phase groupoid of \autoref{ch:concrete-instances-observer-phase-groupoid-namecert}, the Mobius-invariant transport surface of \autoref{ch:concrete-instances-hyperbolic-mobius-invariant-namecert}, and the hyperbolic geodesic-flow packet of \autoref{ch:concrete-instances-hyperbolic-geodesic-flow-namecert}. The carrier records closed reversible phase transport through displayed disk, boundary, classifier, replay, provenance, and local naming rows; it is not a host holonomy group, an unlogged geometric law, a selected orbit, or global hyperbolic completeness.

\begin{definition}[Hyperbolic phase holonomy carrier]
\label{def:hyperbolic-phase-holonomy-carrier}
A $\HyperbolicPhaseHolonomyUp$ carrier is a finite $\BHist$ packet
$$
H=(D,L,B,C,R,V,P,N)
$$
whose rows display a Poincare disk readout $D$, a closed loop transport ledger $L$, a boundary-stable transport row $B$, a phase classifier row $C$, displayed replay $R$, local reversible-transport verification $V$, $\Pkg$ provenance $P$, and a local $\NameCert_{\HyperbolicPhaseHolonomyUp}$ row $N$.

The carrier may export holonomy only after the closed loop, boundary row, classifier row, replay row, provenance row, and local naming row are all displayed. It exports no ambient disk automorphism group, unbounded geodesic flow, hidden phase law, selected endpoint, host smooth connection, or global hyperbolic-completeness claim.
\end{definition}

\begin{theorem}[Hyperbolic phase holonomy NameCert obligations]
\label{thm:hyperbolic-phase-holonomy-namecert-obligations}
For every accepted $\HyperbolicPhaseHolonomyUp$ carrier $H=(D,L,B,C,R,V,P,N)$, the local $\NameCert_{\HyperbolicPhaseHolonomyUp}$ surface consists only of the displayed disk row, closed-loop transport ledger, boundary-stable transport row, phase classifier row, replay row, local reversible-transport verification, provenance row, and local naming row. The holonomy export is accepted only as a finite packet over those rows and cannot be replaced by a host holonomy group, global flow, selected orbit, or geometry-level completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-phase-holonomy-carrier}. The disk row $D$, loop ledger $L$, boundary row $B$, and classifier row $C$ are the only phase-holonomy-facing coordinates, while $R,V,P,N$ provide replay, reversible-transport verification, provenance, and local naming.

Read the transport through the sibling phase routes. The Poincare disk phase-transport packet supplies finite disk transport, the observer phase groupoid supplies inverse and composition bookkeeping, Mobius-invariant transport supplies boundary-stable disk action, and geodesic flow supplies the finite flow-facing ledger.

These sibling rows jointly determine the accepted holonomy surface. None of them adds a host holonomy group, unbounded geometric law, selected orbit, smooth connection, or global completeness coordinate, so the NameCert obligations are exactly the finite rows listed above.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicPhaseHolonomyUp$ route exposes disk row, loop transport row, boundary row, phase classifier row, replay row, provenance row, and local naming before exporting holonomy.}

\independenceWitness{poincare-disk-phase-transport, observer-phase-groupoid, hyperbolic-mobius-invariant, hyperbolic-geodesic-flow: no sibling is bijective because holonomy jointly carries a closed loop, boundary-stable transport, phase classifier, replay, provenance, and local NameCert rows.}

Lean TasteGate.lean will provide Nontrivial and FieldFaithful instances for $\HyperbolicPhaseHolonomyUp$ once the finite carrier and obligation rows are registered under BEDC.Derived.HyperbolicPhaseHolonomyUp.

\closureat{HyperbolicPhaseHolonomyUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseHolonomyUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-holonomy-carrier} and \autoref{thm:hyperbolic-phase-holonomy-namecert-obligations} seed finite hyperbolic phase holonomy over disk, closed-loop transport, boundary, classifier, replay, reversible-verification, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host holonomy groups, global geodesic flow, selected orbits, smooth connections, geometry-level completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, disk row exposure, loop and boundary transport exposure, classifier and replay exactness, provenance non-escape, and TasteGate field faithfulness for BEDC.Derived.HyperbolicPhaseHolonomyUp.}
  \constructivestory{A $\HyperbolicPhaseHolonomyUp$ packet is generated from finite Poincare disk rows, closed-loop transport ledgers, boundary-stable transport, phase classifiers, displayed replay, reversible-transport verification, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
