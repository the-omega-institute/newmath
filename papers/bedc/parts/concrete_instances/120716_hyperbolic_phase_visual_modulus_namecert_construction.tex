\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseVisualModulusUp$}
\label{ch:concrete-instances-hyperbolic-phase-visual-modulus-namecert}
\origin{ai}

The $\HyperbolicPhaseVisualModulusUp$ packet realizes the observer-expansion visual-modulus branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the visual metric of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the observer phase route of \autoref{ch:concrete-instances-hyperbolic-observer-phase-route-namecert}, the Busemann-function readback of \autoref{ch:concrete-instances-hyperbolic-busemann-function-namecert}, the boundary-shadow surface of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the dyadic tolerance carrier of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite observer phase rows, visual metric rows, Busemann rows, boundary-shadow windows, dyadic tolerance, real sealing, transport, replay, provenance, and local naming.

\begin{definition}[Hyperbolic phase visual-modulus carrier]
\label{def:hyperbolic-phase-visual-modulus-carrier}
A $\HyperbolicPhaseVisualModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(O,V,B,S,D,R,T,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the $\HyperbolicObserverPhaseRouteUp$ row, recording the observer phase route before a visual tolerance is read.
\item $V$ is the $\HyperbolicVisualMetricUp$ row, exposing the visual-metric comparison surface.
\item $B$ is the $\HyperbolicBusemannFunctionUp$ row, recording the Busemann readback used to orient the observer phase.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row, recording the finite boundary-shadow window consumed by the visual metric.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, displaying the dyadic modulus budget.
\item $R$ is the $\RealUp$ seal row, exporting the visual-modulus comparison only as a real-valued finite tolerance read.
\item $T$ is the TasteGate row requiring $\Nontrivial\ \HyperbolicPhaseVisualModulusUp$ and $\FieldFaithful\ \HyperbolicPhaseVisualModulusUp$ instances before the packet can classify observer-expansion visual modulus data.
\item $F$ is the refusal row, rejecting reads that turn the visual modulus into a global hyperbolic boundary oracle, selected geodesic representative, or ambient visual compactification.
\item $H$ records componentwise $\hsame$ transport for $O,V,B,S,D,R,T,F,C,P,N$.
\item $C$ records displayed $\Cont$ replay through
$$
O,V \longmapsto B,S \longmapsto D \longmapsto R \longmapsto T,F,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\HyperbolicPhaseVisualModulusUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicObserverPhaseRouteUp$, $\HyperbolicBusemannFunctionUp$, $\HyperbolicBoundaryShadowUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicPhaseVisualModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for a completed visual boundary, a global metric oracle, selected geodesic representatives, host hyperbolic compactification, or an ambient theorem identifying all observer phases.
\end{definition}

\begin{theorem}[Hyperbolic phase visual-modulus NameCert obligations]
\label{thm:hyperbolic-phase-visual-modulus-namecert-obligations}
For every accepted $\HyperbolicPhaseVisualModulusUp$ carrier $M=(O,V,B,S,D,R,T,F,H,C,P,N)$, the local $\NameCert_{\HyperbolicPhaseVisualModulusUp}$ surface consists exactly of observer-phase admission through $O$, visual-metric admission through $V$, Busemann readback through $B$, boundary-shadow exposure through $S$, dyadic tolerance exposure through $D$, real sealing through $R$, TasteGate nontriviality and field-faithfulness requests through $T$, refusal through $F$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-phase-visual-modulus-carrier}. The rows $O,V,B,S$ expose the observer route, visual metric, Busemann readback, and boundary shadow as separate finite fields.

The tolerance and sealing rows then determine what can be exported. The dyadic row $D$ gives the finite modulus budget, while $R$ seals the result as a real-valued comparison rather than a boundary oracle.

Finally the TasteGate, refusal, and structural rows finish the certificate. The row $T$ names the required $\Nontrivial$ and $\FieldFaithful$ instances, $F$ blocks global-boundary readings, and $H,C,P,N$ only transport, replay, source, and name the same packet. No further NameCert obligation is available outside the listed rows.
\end{proof}

\begin{theorem}[Hyperbolic phase visual-modulus boundary route]
\label{thm:hyperbolic-phase-visual-modulus-boundary-route}
Every accepted $\HyperbolicPhaseVisualModulusUp$ boundary read factors through
$$
\HyperbolicObserverPhaseRouteUp,\HyperbolicVisualMetricUp,\HyperbolicBusemannFunctionUp,\HyperbolicBoundaryShadowUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp .
$$
Thus the observer-expansion visual modulus is available only after the observer phase, visual metric, Busemann readback, boundary shadow, dyadic tolerance, and real seal have all been displayed.
\end{theorem}
\begin{proof}
Start with the observer-phase and visual-metric rows. They provide the phase route and visual comparison surface, but neither row alone carries a modulus exported to a consumer.

Next read the Busemann and boundary-shadow rows. These rows orient the boundary-facing comparison and supply the finite shadow window to which the modulus budget applies.

The dyadic tolerance row $D$ and real seal $R$ complete the boundary route. Since $H,C,P,N$ preserve only this displayed path, every accepted boundary read factors through the stated finite diagram.
\end{proof}

\begin{theorem}[Hyperbolic phase visual-modulus expansion readback]
\label{thm:hyperbolic-phase-visual-modulus-expansion-readback}
An observer-expansion read of an accepted $\HyperbolicPhaseVisualModulusUp$ packet exports only the row family $O,V,B,S,D,R,T,F,H,C,P,N$. It cannot be read as a global hyperbolic visual-boundary oracle, a selected geodesic representative, a completed compactification, or a proof that all observer phases have the same ambient visual metric.
\end{theorem}
\begin{proof}
Inspect the refusal and TasteGate rows. The row $F$ blocks global-boundary and selected-representative readings, while $T$ requires nontrivial and field-faithful Lean-side instances before the packet can classify visual-modulus data.

Readback therefore remains finite. The rows $O,V,B,S,D,R$ give the observer route, visual metric, Busemann readback, boundary shadow, dyadic tolerance, and real seal, and the rows $H,C,P,N$ transport, replay, source, and name only those data.

Hence no consumer can extract a completed compactification, global oracle, selected representative, or ambient equality of observer phases from the packet. The expansion readback is exactly the displayed row family.
\end{proof}

\falsifiablePrediction{A packet missing $D$ or $R$ may still display observer-phase and boundary-shadow rows, but it cannot export a real-valued visual modulus.}

\independenceWitness{hyperbolic-visual-metric, hyperbolic-observer-phase-route, hyperbolic-busemann-function, hyperbolic-boundary-shadow, real: no listed sibling is bijective with $\HyperbolicPhaseVisualModulusUp$ because none preserves observer phase, visual metric, Busemann readback, boundary shadow, dyadic tolerance, real seal, TasteGate request, refusal, transport, replay, provenance, and local NameCert together.}

The Lean-side boundary is expected to provide explicit $\Nontrivial\ \HyperbolicPhaseVisualModulusUp$ and $\FieldFaithful\ \HyperbolicPhaseVisualModulusUp$ instances. The field-faithfulness witness must keep $O,V,B,S,D,R,T,F,H,C,P,N$ as distinct rows before any classifier target for $\HyperbolicPhaseVisualModulusUp$ can accept the packet.

\closureat{HyperbolicPhaseVisualModulusUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseVisualModulusUp}
  \constructivestory{A $\HyperbolicPhaseVisualModulusUp$ packet is generated from observer phase, visual metric, Busemann readback, boundary shadow, dyadic tolerance, real seal, TasteGate request, refusal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-visual-modulus-carrier}, \autoref{thm:hyperbolic-phase-visual-modulus-namecert-obligations}, \autoref{thm:hyperbolic-phase-visual-modulus-boundary-route}, and \autoref{thm:hyperbolic-phase-visual-modulus-expansion-readback} seed the finite observer-expansion visual-modulus route over $O,V,B,S,D,R,T,F,H,C,P,N$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a completed visual boundary, global metric oracle, selected geodesic representatives, host hyperbolic compactification, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked observer-phase admission, visual-metric admission, Busemann readback, boundary-shadow exposure, dyadic tolerance, real sealing, TasteGate nontriviality, field faithfulness, refusal, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicPhaseVisualModulusUp.}
\end{closurestatus}
