\chapter{A Concrete Naming Certificate for $\RegularCauchyNestedIntervalUp$}
\label{ch:concrete-instances-regular-cauchy-nested-interval-namecert}
\origin{human}

Following Bishop-style nested interval convergence and constructive real analysis, the $\RegularCauchyNestedIntervalUp$ packet records the finite route by which displayed dyadic endpoint streams determine a regular Cauchy limit seal. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, the located closed interval finite-net route of \autoref{ch:concrete-instances-located-closed-interval-finite-net-namecert}, and the nested interval principle of \autoref{ch:concrete-instances-nested-interval-principle-namecert}. The packet names interval convergence from regular Cauchy data without selected limits, quotient classes, open-cover compactness, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy nested interval carrier]
\label{def:regular-cauchy-nested-interval-carrier}
A $\RegularCauchyNestedIntervalUp$ carrier is a finite $\BHist$ packet
$$
K=(L,U,S,I,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower dyadic endpoint stream, exposed through the $\StreamNameUp$ interface.
\item $U$ is the upper dyadic endpoint stream, exposed through the same $\StreamNameUp$ interface.
\item $S$ is the nested inclusion ledger, recording that each displayed interval is contained in its predecessor and that the lower endpoint never crosses the upper endpoint.
\item $I$ is the $\LocatedClosedIntervalUp$ read row for the displayed closed interval windows.
\item $D$ is the $\DyadicRatCoreUp$ diameter-modulus row, recording the dyadic bound that makes the endpoint streams regular Cauchy data.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the endpoint streams and the same diameter modulus.
\item $E$ is the terminal $\RealUp$ seal, consumed only after $L,U,S,I,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for endpoint streams, nested inclusion, interval windows, diameter data, regular readback, terminal seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from interval-convergence requests through $L,U,S,I,D,R,E,H$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyNestedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedClosedIntervalUp$, $\NestedIntervalPrincipleUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyNestedIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $L,U,S,I,D,R,E,H,C,P,N$. It has no coordinate for a choice-selected intersection point, quotient equality of real names, open-cover compactness, trichotomy, or host completeness of $\RealUp$.
\end{definition}
\leanstmt{BEDC.Derived.RegularCauchyNestedIntervalUp}

\begin{theorem}[Regular Cauchy nested interval NameCert obligations]
\label{thm:regular-cauchy-nested-interval-namecert-obligations}
For every accepted $\RegularCauchyNestedIntervalUp$ carrier $K=(L,U,S,I,D,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyNestedIntervalUp}$ surface consists exactly of carrier admission for lower and upper dyadic endpoint streams, nested inclusion, located closed interval windows, dyadic diameter modulus, regular rational readback, terminal Real seal, transport, replay, provenance, and local naming rows; classifier stability through componentwise $\hsame$ transport; exactness of the nested ledger through $S$; exactness of the diameter modulus through $D$; and nonescape from the displayed rows before any Real-facing consumer reads $E$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-nested-interval-carrier}. The analytic rows are $L,U,S,I,D,R,E$, and the structural rows are $H,C,P,N$.

The endpoint rows $L$ and $U$ are read as displayed stream names. The ledger $S$ is the only nesting witness, so interval inclusion is checked against the recorded rows rather than against a host intersection theorem.

The interval row $I$ and the diameter row $D$ supply the local regular Cauchy budget. They bind the endpoint streams to the same displayed modulus, which prevents a consumer from replacing the route by quotient equality of real names.

The readback row $R$ is the only regular rational handoff, and the Real seal $E$ is terminal. The rows $H,C,P,N$ then transport, replay, source, and name the same finite route, so the listed clauses exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy nested interval RegSeqRat handoff]
\label{thm:regular-cauchy-nested-interval-regseqrat-handoff}
Every Real-facing consumer of an accepted $\RegularCauchyNestedIntervalUp$ carrier must factor through the finite route
$$
L,U,S,I,D \longmapsto R \longmapsto E,H,C,P,N
$$
before it can consume the terminal $\RealUp$ seal. The handoff exports dyadic endpoint streams, nested inclusion, located closed interval windows, a diameter modulus, regular rational readback, transport, replay, provenance, and local naming; it exports no selected limit, quotient class, open-cover compactness, trichotomy, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the displayed endpoint streams $L$ and $U$. Their admissible comparisons are routed through the stream-name surface and the nested inclusion ledger $S$.

Next read the interval and modulus rows. The located closed interval row $I$ records the visible windows, while the dyadic row $D$ records the diameter budget that makes the endpoint data regular Cauchy data.

The regular rational row $R$ is reached only from those displayed rows. It is therefore a readback of the visible nested interval data, not a quotient representative or a choice-selected point.

Finally the terminal seal $E$ and the structural rows $H,C,P,N$ preserve the same route. Thus every Real-facing consumer receives a RegSeqRat handoff from the finite packet and none of the refused limit-selection, compactness, trichotomy, quotient, or ambient-completeness data.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyNestedIntervalUp$ consumer that reaches a $\RealUp$ seal without displaying endpoint streams, nested inclusion, located interval windows, dyadic diameter data, RegSeqRat readback, transport, replay, provenance, and local naming rows refutes the carrier surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, located-closed-interval, nested-interval-principle: no listed sibling supplies the joint packet containing dyadic endpoint streams, nested inclusion, located interval windows, diameter modulus, RegSeqRat readback, terminal Real seal, transport, replay, provenance, and local NameCert row.}

\begin{closurestatus}{\RegularCauchyNestedIntervalUp}
  \constructivestory{A $\RegularCauchyNestedIntervalUp$ packet is generated from lower and upper dyadic endpoint streams, a nested inclusion ledger, located interval windows, a dyadic diameter modulus, RegSeqRat readback, terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-nested-interval-carrier}, \autoref{thm:regular-cauchy-nested-interval-namecert-obligations}, and \autoref{thm:regular-cauchy-nested-interval-regseqrat-handoff} seed the choice-free nested-interval route from regular Cauchy endpoint data into Real-facing readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient equality of real names, open-cover compactness, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\RegularCauchyNestedIntervalUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint-stream exactness, nested-ledger exactness, located interval windows, diameter-modulus exactness, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyNestedIntervalUp.}
\end{closurestatus}
