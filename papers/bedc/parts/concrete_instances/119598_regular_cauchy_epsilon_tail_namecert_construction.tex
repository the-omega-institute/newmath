\chapter{A Concrete Naming Certificate for $\RegularCauchyEpsilonTailUp$}
\label{ch:concrete-instances-regular-cauchy-epsilon-tail-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyEpsilonTailUp$ packet records the finite epsilon-tail witness surface for regular Cauchy real names. It sits over the finite stream-name discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the limit-facing handoff of \autoref{ch:concrete-instances-limit-namecert}. The packet supplies the Real and Completion route with displayed epsilon-tail evidence only; it does not import quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy epsilon-tail carrier]
\label{def:regular-cauchy-epsilon-tail-carrier}
A $\RegularCauchyEpsilonTailUp$ carrier is a finite $\BHist$ packet
$$
E=(S,R,D,Q,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite $\StreamNameUp$ schedule row. It records the inspected indices, window requests, and tail-access cells used by the epsilon-tail witness.
\item $R$ is the $\RegSeqRatUp$ source row. It exposes the regular rational name only through the finite reads scheduled by $S$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row. It records the requested epsilon, dyadic slack, radius comparison, and finite arithmetic budget.
\item $Q$ is the precision-query row. It binds the external epsilon request to the displayed dyadic budget in $D$ before any tail assertion is available.
\item $T$ is the epsilon-tail witness row. It displays the tail threshold and the finitely checked later-window comparisons read from $S,R,D,Q$.
\item $H$ records componentwise $\hsame$ transport for the stream schedule, regular rational row, dyadic budget, precision query, tail witness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,R,D,Q \longmapsto T .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyEpsilonTailUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyEpsilonTailUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,D,Q,T,H,C,P,N$. It has no coordinate for quotient equality of streams, a host limit, a selected real representative, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.RegularCauchyEpsilonTailUp}

\begin{theorem}[Regular Cauchy epsilon-tail NameCert obligations]
\label{thm:regular-cauchy-epsilon-tail-namecert-obligations}
For every accepted $\RegularCauchyEpsilonTailUp$ carrier
$$
E=(S,R,D,Q,T,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyEpsilonTailUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the stream schedule, regular rational source, dyadic tolerance budget, precision query, epsilon-tail witness, transport, replay, provenance, and local naming rows;
\item query-budget exactness, requiring $Q$ to bind the requested epsilon to the displayed $\DyadicRatCoreUp$ budget before the tail threshold is read;
\item stream-readback exactness, requiring every later-window comparison in $T$ to be read from the same $\StreamNameUp$ schedule and $\RegSeqRatUp$ source rows;
\item transport and replay stability through $H$ and $C$ for the same displayed epsilon-tail route;
\item provenance and local naming exhaustion through $P$ and $N$;
\item refusal of quotient stream equality, host limits, selected real representatives, countable choice, trichotomy, and ambient $\RealUp$ completeness.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier $E=(S,R,D,Q,T,H,C,P,N)$. The visible analytic rows are the finite stream schedule $S$, the regular rational source $R$, the dyadic budget $D$, the precision query $Q$, and the tail witness $T$.

The query first passes through $Q$ and $D$. This forces the epsilon request to be recorded as a finite dyadic budget before any tail threshold or later-window comparison can be consumed.

The witness row $T$ is downstream of the same schedule and source. Its threshold and later-window comparisons are read only from the windows listed in $S$ and the rational observations exposed by $R$.

The structural rows preserve this route. The row $H$ transports the displayed coordinates componentwise, $C$ replays the same dependency order, and $P,N$ source and name only the resulting packet.

Since none of the excluded principles is a coordinate of the carrier or a product of the replay route, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Regular Cauchy epsilon-tail Real seal route]
\label{thm:regular-cauchy-epsilon-tail-real-seal-route}
Every $\RegSeqRatUp$, $\RealUp$, or limit-facing consumer of an accepted $\RegularCauchyEpsilonTailUp$ carrier factors through the finite route
$$
S,R,D,Q \longmapsto T \longmapsto H,C,P,N .
$$
Thus the consumer receives a displayed StreamName schedule, regular rational readback, dyadic epsilon budget, precision query, epsilon-tail witness, transport, replay, provenance, and local naming. It receives no quotient stream equality, host limit, selected real representative, countable-choice schedule, trichotomy principle, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with a consumer of the accepted packet. The stream and regular-rational rows $S$ and $R$ are the only source of observation data, and the dyadic row $D$ is the only source of epsilon-budget data.

The precision query $Q$ binds the requested tolerance to that dyadic budget. Hence a real-name or limit-facing read cannot access a tail threshold before the finite epsilon request has been displayed.

Read the witness row $T$ next. It records the finite tail threshold and the later-window comparisons obtained from $S,R,D,Q$.

Finally apply the structural rows $H,C,P,N$. They transport, replay, source, and name the same route without adding any coordinate outside it. Therefore every Real or limit-facing handoff remains inside the finite epsilon-tail witness surface and cannot export the excluded quotient, selection, choice, trichotomy, host-limit, or completeness data.
\end{proof}

\falsifiablePrediction{A $\RegularCauchyEpsilonTailUp$ read is refuted whenever a $\RegSeqRatUp$, $\RealUp$, or limit-facing consumer reaches an epsilon-tail witness without displaying the finite StreamName schedule, RegSeqRat source, DyadicRatCore tolerance row, precision query, componentwise transport, replay, provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, limit: none alone is bijective with $\RegularCauchyEpsilonTailUp$ because the packet jointly records a finite stream schedule, a regular rational source, a dyadic epsilon budget, a precision query, a displayed tail witness, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyEpsilonTailUp}{seedStr}

\begin{closurestatus}{\RegularCauchyEpsilonTailUp}
  \constructivestory{A $\RegularCauchyEpsilonTailUp$ packet is generated from a finite StreamName schedule, a RegSeqRat source row, a DyadicRatCore epsilon budget, a precision query, an epsilon-tail witness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-epsilon-tail-carrier}, \autoref{thm:regular-cauchy-epsilon-tail-namecert-obligations}, and \autoref{thm:regular-cauchy-epsilon-tail-real-seal-route} seed the finite epsilon-tail witness route over displayed StreamName, RegSeqRat, DyadicRatCore, Real, Limit, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.RegularCauchyEpsilonTailUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, host limits, selected real representatives, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean proof obligations, axiom-clean audit, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, query-budget exactness, stream-readback exactness, epsilon-tail witness coverage, transport, replay, provenance, local naming, and Real-seal route nonescape for BEDC.Derived.RegularCauchyEpsilonTailUp.}
\end{closurestatus}
