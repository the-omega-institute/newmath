The $\ComplexDifferentiabilityUp$ packet exposes its horizontal lattice route through \autoref{ch:concrete-instances-complex-namecert}, \autoref{ch:concrete-instances-complex-limit-namecert}, and \autoref{ch:concrete-instances-holomorphic-namecert}: the $\ComplexUp$ carrier row supplies the displayed difference-quotient inputs, the $\ComplexLimitUp$ row supplies the finite limit readback, and the $\HolomorphicUp$ row consumes the disk-modulus presentation only after those differentiability rows are visible.
