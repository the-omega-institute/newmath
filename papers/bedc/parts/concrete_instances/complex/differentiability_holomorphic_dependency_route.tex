The $\ComplexDifferentiabilityUp$ packet is consumed by $\HolomorphicUp$ through the open-disk differentiability predicate of \autoref{ch:concrete-instances-holomorphic-namecert}. A holomorphic read is available only after the local difference-quotient row, the displayed limit row, the $\ComplexUp$ carrier row, and the derivative row have been exposed as finite $\NameCert$ data; before those rows are visible, complex differentiability remains a local calculation surface rather than a holomorphic input. The same finite differentiability surface reaches $\RoucheTheoremUp$ only through the holomorphic and contour route of \autoref{ch:concrete-instances-rouche-theorem-namecert}.
