\chapter{A Concrete Naming Certificate for $\HyperbolicBoundaryDistortionBudgetUp$}
\label{ch:concrete-instances-hyperbolic-boundary-distortion-budget-namecert}
\origin{ai}

The $\HyperbolicBoundaryDistortionBudgetUp$ packet realizes the boundary-distortion budget route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the disk dynamics, boundary-fixed transport, visual-metric, cross-ratio, and metric-comparison rows exposed by the hyperbolic observer-expansion vision, and it packages the finite budget consumed before any Real-facing boundary-distortion read reaches a seal. The packet is not a completed hyperbolic plane, a selected ideal endpoint, quotient boundary equality, an external quasi-isometry theorem, or an ambient completeness principle.

\begin{definition}[Hyperbolic boundary-distortion budget carrier]
\label{def:hyperbolic-boundary-distortion-budget-carrier}
A $\HyperbolicBoundaryDistortionBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(D,S,V,X,M,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the disk-map row, displaying the Poincare disk map whose boundary distortion is being read.
\item $S$ is the boundary-shadow row, recording the finite boundary arc or endpoint shadow consumed by the observer-expansion route.
\item $V$ is the visual-metric row attached to the same boundary shadow.
\item $X$ is the cross-ratio or metric-comparison row that compares the displayed before-and-after boundary shadows.
\item $M$ is the metric-comparison budget row, recording the finite distortion allowance for the displayed disk map.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance row for the distortion threshold.
\item $E$ is the $\RealUp$ seal row, exposed only after disk-map, boundary-shadow, visual-metric, cross-ratio or metric-comparison, dyadic tolerance, and budget rows are visible.
\item $H$ records componentwise $\hsame$ transport for disk-map, boundary-shadow, visual-metric, comparison, tolerance, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
D,S,V,X,M,Q \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\HyperbolicBoundaryDistortionBudgetUp$, $\PoincareDiskAutomorphismUp$, $\HyperbolicMetricUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicBoundaryShadowUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicBoundaryDistortionBudgetUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,S,V,X,M,Q,E,H,C,P,N$. It has no coordinate for a completed hyperbolic surface, selected boundary endpoint, quotient boundary equality, hidden global distortion theorem, or ambient completeness principle.
\end{definition}
\leanstmt{BEDC.Derived.HyperbolicBoundaryDistortionBudgetUp}

\begin{theorem}[Hyperbolic boundary-distortion budget obligations]
\label{thm:hyperbolic-boundary-distortion-budget-obligations}
For every accepted $\HyperbolicBoundaryDistortionBudgetUp$ carrier $B=(D,S,V,X,M,Q,E,H,C,P,N)$, the local $\NameCert_{\HyperbolicBoundaryDistortionBudgetUp}$ surface consists exactly of disk-map admission through $D$; boundary-shadow exposure through $S$; visual-metric exposure through $V$; cross-ratio or metric-comparison exposure through $X$; finite distortion-budget exposure through $M$; dyadic tolerance exposure through $Q$; $\RealUp$ seal non-escape through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. The rows $D,S,V,X,M,Q$ are the complete boundary-distortion source surface: a disk map, its boundary shadow, the visual metric, the comparison read, the finite budget, and the dyadic tolerance.

The Real-facing row is downstream. The seal $E$ is exposed only after the displayed source rows have been read, so a boundary-distortion consumer cannot reach a Real value from an unregistered disk map or hidden comparison theorem.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since no completed surface, selected endpoint, quotient boundary equality, hidden global distortion theorem, or ambient completeness coordinate occurs in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic boundary-distortion budget nonescape]
\label{thm:hyperbolic-boundary-distortion-budget-nonescape}
Every accepted $\HyperbolicBoundaryDistortionBudgetUp$ Real-facing read factors through the displayed disk-map, boundary-shadow, visual-metric, cross-ratio or metric-comparison, dyadic tolerance, transport, replay, provenance, and local NameCert rows before it consumes the seal. No consumer may export boundary distortion from a hidden hyperbolic metric, an unstated cross-ratio, a selected ideal endpoint, quotient boundary equality, or ambient completeness.
\end{theorem}
\begin{proof}
Start from \autoref{thm:hyperbolic-boundary-distortion-budget-obligations}. It places the disk map $D$, boundary shadow $S$, visual metric $V$, comparison row $X$, distortion budget $M$, and dyadic tolerance $Q$ before the Real seal.

Transport and replay preserve the displayed order. The rows $H$ and $C$ may replace the packet by componentwise same rows and replay the route, but neither adds a new source for distortion.

Provenance and local naming close the finite route. Since $P$ and $N$ name only the displayed rows, the refused hidden metric, unstated comparison, selected endpoint, quotient equality, and completeness principles have no export coordinate.
\end{proof}

\falsifiablePrediction{An accepted $\HyperbolicBoundaryDistortionBudgetUp$ boundary-distortion read is refuted if it reaches a $\RealUp$ seal without displaying disk-map, boundary-shadow, visual-metric, cross-ratio or metric-comparison, dyadic tolerance, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{hyperbolic-metric, hyperbolic-visual-metric, hyperbolic-cross-ratio, hyperbolic-boundary-shadow, poincare-disk-automorphism: no listed sibling is bijective to $\HyperbolicBoundaryDistortionBudgetUp$ because the budget packet binds disk-map, boundary-shadow, visual-metric, comparison, dyadic tolerance, Real seal, transport, replay, provenance, and local naming as one finite distortion certificate.}

\closureat{HyperbolicBoundaryDistortionBudgetUp}{seedStr}

\begin{closurestatus}{\HyperbolicBoundaryDistortionBudgetUp}
  \constructivestory{A $\HyperbolicBoundaryDistortionBudgetUp$ packet is generated from disk-map rows, boundary-shadow rows, visual-metric rows, cross-ratio or metric-comparison rows, dyadic tolerance rows, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-boundary-distortion-budget-carrier}, \autoref{thm:hyperbolic-boundary-distortion-budget-obligations}, and \autoref{thm:hyperbolic-boundary-distortion-budget-nonescape} seed the finite boundary-distortion budget route for observer-expansion reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic surfaces, selected ideal endpoints, quotient boundary equality, hidden global distortion theorems, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk-map admission, boundary-shadow exposure, visual-metric exposure, cross-ratio or metric-comparison exposure, dyadic tolerance, RealUp seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicBoundaryDistortionBudgetUp.}
\end{closurestatus}
