\chapter{A Concrete Naming Certificate for $\LimsupUp$}
\label{ch:concrete-instances-limsup-namecert}
\origin{human}

Following Hardy and Bishop--Bridges, $\LimsupUp$ records the finite tail-upper-cut and tail-lower-cut surface of a bounded real sequence. The packet consumes the bounded sequence rows of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the upper-cut observation discipline of \autoref{ch:concrete-instances-bounded-real-upper-cut-namecert}, the lower-cut observation discipline of \autoref{ch:concrete-instances-bounded-real-lower-cut-namecert}, the displayed sequence-limit interface of \autoref{ch:concrete-instances-real-sequence-limit-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. It gives Real-and-Completion consumers a finite limsup boundary without importing completeness, a host supremum, or selected subsequences.

\begin{definition}[Limsup carrier]
\label{def:limsup-carrier}
A $\LimsupUp$ carrier is a finite $\BHist$ packet
$$
L=(S,U,D,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the bounded $\BoundedRealSequenceUp$ row whose finite windows, bound ledger, and displayed $\RealUp$ entries are being inspected.
\item $U$ is the tail upper-cut schedule, recording for each displayed tail index the bounded upper-cut observations read through $\BoundedRealUpperCutUp$.
\item $D$ is the dyadic tolerance ledger, recording the finite rational tolerances used to compare tail upper cuts with the lower observation surface.
\item $T$ is the terminal $\RealUp$ seal row named as the limsup candidate exposed by the packet.
\item $H$ records componentwise $\hsame$ transports for $S,U,D,T$ and the structural rows.
\item $C$ records the displayed $\Cont$ routes by which consumers replay the bounded sequence row, tail upper-cut schedule, dyadic tolerance ledger, terminal real seal, transports, provenance, and local naming row.
\item $P$ is the $\Pkg$ provenance over $\LimsupUp$, $\BoundedRealSequenceUp$, $\BoundedRealUpperCutUp$, $\BoundedRealLowerCutUp$, $\RealSequenceLimitUp$, $\RealUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LimsupUp}$ row.
\end{enumerate}
The carrier stores a finite tail-cut surface for a bounded real sequence. It has no coordinate for Real completeness, trichotomy, a host supremum, quotient stream equality, selected subsequences, or open-cover compactness.
\end{definition}
\leandef{BEDC.Derived.LimsupUp.LimsupCarrier}

\begin{definition}[Limsup classifier]
\label{def:limsup-classifier}
Two $\LimsupUp$ carriers
$$
L=(S,U,D,T,H,C,P,N)
\qquad\text{and}\qquad
L'=(S',U',D',T',H',C',P',N')
$$
are classified as the same displayed limsup packet exactly when the bounded sequence rows match componentwise, the tail upper-cut schedules expose the same finite tail observations, the dyadic tolerance ledgers carry the same rational comparison budgets, the terminal $\RealUp$ seals are transported by the displayed $\hsame$ data, the continuation replays preserve the same consumer routes, and the provenance and local NameCert rows name the same $\LimsupUp$ surface. The classifier is therefore rowwise over $S,U,D,T,H,C,P,N$ and does not identify carriers by host sequence equality, quotient streams, selected cofinal subsequences, or a completed supremum construction.
\end{definition}
\leandef{BEDC.Derived.LimsupUp.LimsupClassifier}

\begin{theorem}[Limsup NameCert obligations]
\label{thm:limsup-namecert-obligations}
For an accepted $\LimsupUp$ carrier $L=(S,U,D,T,H,C,P,N)$, the local $\NameCert_{\LimsupUp}$ surface consists of six finite obligation rows:
\begin{enumerate}
\item carrier formation for the bounded real sequence row, tail upper-cut schedule, dyadic tolerance ledger, terminal $\RealUp$ seal, transports, continuations, provenance, and local naming data;
\item classifier stability for the componentwise comparison of \autoref{def:limsup-classifier};
\item tail upper-cut exactness, stating that every upper observation in $U$ is read from a finite tail of $S$ through the bounded upper-cut surface;
\item lower-cut compatibility, stating that every tolerance row in $D$ compares the same tail data against the bounded lower-cut observation surface;
\item terminal seal routing, stating that the candidate row $T$ is consumed only as a $\RealUp$ seal after the displayed tail-cut checks have been replayed;
\item ledger non-escape, excluding Real completeness, trichotomy, host supremum selection, quotient stream equality, selected subsequences, and open-cover compactness.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier formation is projection from \autoref{def:limsup-carrier}. The packet displays exactly the bounded sequence row, tail upper-cut schedule, dyadic tolerance ledger, terminal real seal, componentwise transports, continuation routes, provenance, and local name.

Classifier stability is the componentwise comparison of \autoref{def:limsup-classifier}. Transport along $H$ may replace the visible rows only by rows with the same scheduled tail observations, the same dyadic budgets, and the same terminal seal route, while $C$ preserves the consumer replay order.

Tail upper-cut exactness is supplied by $U$ together with the bounded sequence row $S$. Each accepted upper observation names the finite tail index, the inspected sequence windows, and the bounded upper-cut certificate through which the read is made.

Lower-cut compatibility is supplied by $D$ and the sibling bounded lower-cut discipline. The tolerance ledger records only finite dyadic comparisons between the same tail data and the lower observation surface, so it does not become a host infimum or a completeness witness.

Terminal seal routing is the role of $T$. A consumer may read the candidate limsup only after the sequence tail, upper-cut, lower-cut, and tolerance rows have been replayed; the terminal coordinate remains a $\RealUp$ seal rather than a completed supremum object.

The non-escape row follows from the carrier inventory. No displayed field carries Real completeness, trichotomy, a host supremum, quotient stream equality, selected subsequences, or open-cover compactness.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupNameCertObligations}

\begin{theorem}[Limsup tail lower-cut compatibility]
\label{thm:limsup-tail-lower-cut-compatibility}
Let $L=(S,U,D,T,H,C,P,N)$ be an accepted $\LimsupUp$ carrier. Every dyadic tolerance row in $D$ compares the same finite tail data exposed by $U$ against the bounded lower-cut surface, so the limsup packet cannot read a host infimum, selected subsequence, or completed supremum when it certifies the lower side of the tail-cut boundary.
\end{theorem}
\begin{proof}
Project $S,U,D$ from the carrier of \autoref{def:limsup-carrier}. The bounded sequence row $S$ supplies the only sequence entries, the upper schedule $U$ names the finite tail observations, and the tolerance ledger $D$ records the dyadic comparison budgets attached to those same observations.

Use the componentwise classifier of \autoref{def:limsup-classifier}. It compares accepted packets by the displayed bounded sequence row, tail upper-cut schedule, dyadic tolerance ledger, and terminal seal route, not by a host sequence object or by a completed extremal value.

The structural rows $H,C,P,N$ preserve that lower-cut read. Transport may only replace the visible rows by classifier-equivalent rows, continuation replay rereads the same tail and tolerance cells, provenance names the bounded lower-cut dependency, and the local NameCert row records the finite packet. Thus the lower side of the limsup surface remains a bounded lower-cut comparison over displayed tail data.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupTailLowerCutCompatibility}

\begin{theorem}[Limsup tail upper-cut exactness]
\label{thm:limsup-tail-upper-cut-exactness}
Let $L=(S,U,D,T,H,C,P,N)$ be an accepted $\LimsupUp$ carrier. The row $U$ is the only upper-tail observation route from the bounded sequence row $S$ through finite $\BoundedRealUpperCutUp$ data. Thus no consumer can read a host supremum, selected cofinal subsequence, or detached upper bound when it consumes the upper side of the limsup packet.
\end{theorem}
\begin{proof}
Project $S$ and $U$ from \autoref{def:limsup-carrier}. The bounded sequence row $S$ supplies the finite sequence windows, while $U$ records the tail upper-cut observations attached to those windows.

Use the classifier of \autoref{def:limsup-classifier}. Accepted packets agree on the displayed bounded sequence row and the tail upper-cut schedule, so an upper-tail read cannot be replaced by a host sequence object or by a completed supremum construction.

The structural rows preserve the same upper route. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ carry the displayed $S \to U$ observation path and do not add another upper-cut coordinate.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupTailUpperCutExactness}

\begin{theorem}[Limsup terminal seal routing]
\label{thm:limsup-terminal-seal-routing}
For every accepted $\LimsupUp$ carrier $L=(S,U,D,T,H,C,P,N)$, the terminal row $T$ is consumed only after the bounded sequence row $S$, tail upper-cut schedule $U$, and dyadic comparison ledger $D$ have been replayed. The row $T$ remains a $\RealUp$ seal and is not a host supremum, selected subsequence limit, or ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with the carrier projection. The only value-facing coordinate is $T$, and the only rows that feed it are the bounded source row $S$, the upper-tail schedule $U$, and the dyadic lower-comparison ledger $D$.

Replay the route through $C$. A consumer reaches $T$ only after the displayed tail data and comparison budgets have been read, so the terminal row is downstream of the same finite tail-cut surface.

Finally apply $H,P,N$. Componentwise transport, provenance, and local naming preserve the $\RealUp$ seal route without adding a supremum operator, selected subsequence, or completeness field.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupTerminalSealRouting}

\begin{theorem}[Limsup tail-seal scope]
\label{thm:limsup-tail-seal-scope}
For every accepted $\LimsupUp$ carrier $L=(S,U,D,T,H,C,P,N)$, the candidate limsup value is exposed only after the same finite tail upper-cut row $U$, lower-cut tolerance ledger $D$, and terminal $\RealUp$ seal $T$ have all been replayed. Consequently no consumer can replace the route by a host supremum, selected subsequence, quotient stream equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First apply the upper-cut exactness theorem, \autoref{thm:limsup-tail-upper-cut-exactness}. It fixes $U$ as the only upper-tail observation route from the bounded sequence row $S$.

Next apply the lower-cut compatibility theorem, \autoref{thm:limsup-tail-lower-cut-compatibility}. It binds the dyadic ledger $D$ to the same tail data and the bounded lower-cut surface, so the lower side cannot be replaced by a host lower envelope or by a selected subsequence.

Finally apply terminal seal routing, \autoref{thm:limsup-terminal-seal-routing}. The terminal row $T$ is reached only after $S,U,D$ have been replayed, and the structural rows $H,C,P,N$ preserve that order. The combined route therefore exposes a sealed $\RealUp$ candidate through the finite tail-cut packet and no excluded host-level principle.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupTailSealScope}

\begin{theorem}[Limsup tail-window nonescape]
\label{thm:limsup-tail-window-nonescape}
Every public limsup comparison exported from an accepted $\LimsupUp$ tail window factors first through the bounded sequence row $S$, the bounded upper-cut and lower-cut rows exposed by $U$ and $D$, the $\DyadicRatCoreUp$ tolerance ledger, the sibling $\RealSequenceLimitUp$ route, and the terminal $\RealUp$ seal $T$. Thus the tail-window export cannot bypass the displayed BEDC rows by reading a host supremum, selected subsequence, quotient stream, trichotomy branch, or Real-completeness principle.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:limsup-carrier}. The only sequence-facing data are the bounded row $S$, the upper-cut schedule $U$, and the lower-cut comparison ledger $D$.

Apply \autoref{thm:limsup-tail-upper-cut-exactness} and \autoref{thm:limsup-tail-lower-cut-compatibility}. These force the upper and lower sides of the tail window through $\BoundedRealSequenceUp$, $\BoundedRealUpperCutUp$, $\BoundedRealLowerCutUp$, and the $\DyadicRatCoreUp$ tolerance budget before any comparison is exposed.

Next use \autoref{thm:limsup-terminal-seal-routing} and \autoref{thm:limsup-tail-seal-scope}. The candidate value is reached only through the terminal $\RealUp$ seal, while the $\RealSequenceLimitUp$ row remains a routed sibling dependency rather than a replacement for the displayed tail-cut package.

Finally replay \autoref{thm:limsup-real-seal-nonescape}. Since no carrier field stores the excluded host-level principles, the public comparison surface remains inside the scoped tail-window route.
\end{proof}

\begin{theorem}[Limsup scope kernel dependencies]
\label{thm:limsup-scope-kernel-dependencies}
Every accepted $\LimsupUp$ consumer must expose the bounded sequence row $S$, the bounded upper-cut and lower-cut rows read through $U$ and $D$, the $\DyadicRatCoreUp$ tolerance ledger, the $\RealSequenceLimitUp$ route, the terminal $\RealUp$ seal $T$, componentwise $\BHist$ and $\hsame$ data, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\LimsupUp}$ row before it may read the limsup surface.
\end{theorem}
\leanchecked{BEDC.Derived.LimsupUp.LimsupScopeKernelDependencies}
\begin{proof}
Start with \autoref{thm:limsup-tail-upper-cut-exactness}. It fixes $U$ as the only upper-tail route from the bounded sequence row $S$ through finite $\BoundedRealUpperCutUp$ data.

Next apply \autoref{thm:limsup-tail-lower-cut-compatibility}. The lower side is tied to the same tail data through the dyadic tolerance ledger $D$, so the $\DyadicRatCoreUp$ comparison budget and the bounded lower-cut surface are visible before any limsup read is made.

Finally use \autoref{thm:limsup-terminal-seal-routing}. The terminal $\RealUp$ row $T$ is reached only after $S,U,D$ have been replayed, and the structural rows $H,C,P,N$ supply the $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ scope. The $\RealSequenceLimitUp$ dependency is therefore a routed sibling row, not a replacement for the displayed kernel package.
\end{proof}

\begin{theorem}[Limsup tail-cut route]
\label{thm:limsup-tail-cut-route}
Every Real-and-Completion consumer of an accepted $\LimsupUp$ packet factors through the finite route
$$
S\longmapsto U\longmapsto D\longmapsto T\longmapsto H\longmapsto C\longmapsto P\longmapsto N
$$
before using the displayed limsup surface.
\end{theorem}
\begin{proof}
Begin with the bounded sequence row $S$. It supplies the only displayed source of sequence entries, finite windows, and boundedness evidence available to the packet.

The tail upper-cut schedule $U$ then names which tails and upper observations are inspected. The dyadic ledger $D$ records the tolerance comparisons that align those tail-upper observations with the bounded lower-cut surface.

Only after those rows are replayed can a consumer inspect the terminal real seal $T$. The structural rows $H,C,P,N$ then transport, replay, source, and name the same finite route, so the consumer cannot bypass the tail-cut surface by reading a host supremum or a selected subsequence.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupTailCutRoute}

\begin{theorem}[Limsup Real seal non-escape]
\label{thm:limsup-real-seal-nonescape}
An accepted $\LimsupUp$ packet exposes its candidate value only as the terminal $\RealUp$ row $T$ reached through the displayed tail-cut route. In particular, it does not export Real completeness, trichotomy, a host supremum, quotient stream equality, selected subsequences, or open-cover compactness.
\end{theorem}
\begin{proof}
The only value-facing coordinate in the carrier is $T$. All sequence-facing information is confined to $S,U,D$, and all structural access is confined to $H,C,P,N$.

A consumer that asks for the candidate value must therefore follow the continuation replay from the bounded sequence row through the tail upper-cut and tolerance ledgers before reaching $T$. The replay exposes a sealed real name, not a construction by supremum, compactness, or subsequence selection.

Since no carrier field stores the excluded principles, componentwise transport and continuation replay cannot introduce them. The $\RealUp$ seal remains terminal for the packet, and the limsup surface remains a finite tail-cut certificate.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupRealSealNonescape}

\begin{theorem}[Limsup scoped tail-cut exhaustion]
\label{thm:limsup-scoped-tail-cut-exhaustion}
Every accepted $\LimsupUp$ consumer factors through the bounded sequence row $S$, the tail upper-cut schedule $U$, the bounded lower-cut compatibility row recorded by the dyadic tolerance ledger $D$, the $\DyadicRatCoreUp$ tolerance ledger, the terminal $\RealUp$ seal $T$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\LimsupUp}$ row. Thus the scoped tail-cut surface is exhausted by the displayed finite route and exports no host supremum, selected subsequence, quotient stream, trichotomy branch, or Real-completeness principle.
\end{theorem}
\begin{proof}
First apply \autoref{thm:limsup-tail-upper-cut-exactness}. It forces every upper-side read to begin with the bounded sequence row $S$ and the finite tail upper-cut schedule $U$.

Next apply \autoref{thm:limsup-tail-lower-cut-compatibility}. The lower-side comparison is tied to the same tail data by the dyadic tolerance ledger $D$, so the bounded lower-cut row and the $\DyadicRatCoreUp$ budget are visible before any limsup consumer proceeds.

Now use \autoref{thm:limsup-terminal-seal-routing} and \autoref{thm:limsup-tail-cut-route}. These route the consumer through $S,U,D,T,H,C,P,N$, placing the terminal $\RealUp$ seal after the bounded sequence, tail-cut, tolerance, transport, replay, provenance, and local naming rows.

Finally apply \autoref{thm:limsup-real-seal-nonescape}. Since the terminal read is only the displayed Real seal and no carrier field stores the excluded host-level principles, the scoped tail-cut packet cannot export a host supremum, selected subsequence, quotient stream, trichotomy branch, or Real-completeness witness.
\end{proof}
\leanchecked{BEDC.Derived.LimsupUp.LimsupScopedTailCutExhaustion}

\begin{theorem}[Limsup scoped tail-cut witness]
\label{thm:limsup-scoped-tail-cut-witness}
Every accepted $\LimsupUp$ consumer exposes the rows $S,U,D,T,H,C,P,N$ as a single scoped witness surface. The surface binds upper-tail exactness through \autoref{thm:limsup-tail-upper-cut-exactness}, lower-cut compatibility through \autoref{thm:limsup-tail-lower-cut-compatibility}, terminal seal routing through \autoref{thm:limsup-terminal-seal-routing}, tail-cut route ordering through \autoref{thm:limsup-tail-cut-route}, and Real seal nonescape through \autoref{thm:limsup-real-seal-nonescape}.
\end{theorem}
\begin{proof}
Project the accepted consumer to its displayed carrier rows. The bounded source row $S$, upper-tail schedule $U$, and dyadic lower-cut row $D$ are fixed by the upper-cut exactness and lower-cut compatibility theorems.

Next compose the terminal seal and route-ordering results. They place $T$ after the tail-cut rows and keep $H,C,P,N$ as the transport, replay, provenance, and local naming surface for that same route.

Finally apply the nonescape theorem. Since the terminal Real read is reached only through the displayed rows, the eight-row surface is a scoped witness rather than an ambient supremum, selected subsequence, quotient stream, trichotomy branch, or Real-completeness principle.
\end{proof}

\begin{closurestatus}{\LimsupUp}
  \constructivestory{A $\LimsupUp$ packet is generated from a bounded RealUp sequence row, a finite tail upper-cut schedule, a dyadic tolerance ledger, a terminal RealUp seal, componentwise hsame transports, Cont replay routes, package provenance, and a local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:limsup-carrier}, \autoref{def:limsup-classifier}, \autoref{thm:limsup-namecert-obligations}, \autoref{thm:limsup-tail-cut-route}, and \autoref{thm:limsup-real-seal-nonescape} seed the finite tail-cut limsup surface for bounded real sequences.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Real completeness, trichotomy, a host supremum, quotient stream equality, selected subsequences, open-cover compactness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier formation, classifier stability, tail upper-cut exactness, lower-cut compatibility, terminal seal routing, tail-cut route ordering, and Real seal non-escape for the displayed packet.}
\end{closurestatus}
