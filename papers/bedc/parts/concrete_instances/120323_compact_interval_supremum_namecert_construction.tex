\chapter{A Concrete Naming Certificate for $\CompactIntervalSupremumUp$}
\label{ch:concrete-instances-compact-interval-supremum-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactIntervalSupremumUp$ packet records the finite-net supremum surface for a located compact real interval. It sits over the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the located endpoint rows of \autoref{ch:concrete-instances-locatedinterval-namecert}, the compact metric finite-net source of \autoref{ch:concrete-instances-compactmetric-namecert}, and the total-bounded finite-net surface of \autoref{ch:concrete-instances-totallybounded-namecert}. The packet names finite rows for inspected interval endpoints, finite-net coverage, dyadic tolerances, candidate upper bounds, and local supremum readback; it does not assert open-cover compactness, a selected global maximizer, quotient real equality, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Compact interval supremum carrier]
\label{def:compact-interval-supremum-carrier}
A $\CompactIntervalSupremumUp$ carrier is a finite $\BHist$ packet
$$
S=(E,I,K,T,D,U,M,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\RealUp$ endpoint seal row for the displayed lower and upper interval endpoints.
\item $I$ is the $\LocatedIntervalUp$ row recording located endpoint comparisons and interval membership windows.
\item $K$ is the $\CompactMetricUp$ compact-source row read only through finite metric coverage data.
\item $T$ is the $\TotallyBoundedUp$ finite-net row, displaying the inspected centers and coverage witnesses at the requested scale.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for mesh size, slack, and upper-bound comparison budgets.
\item $U$ records the finite family of candidate upper-bound rows inspected over the displayed net.
\item $M$ records the finite maximum-read row over those candidate bounds, as rational readback data rather than as a host supremum operator.
\item $R$ is the local supremum-candidate row exported after $E,I,K,T,D,U,M$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the endpoint, interval, compact, net, tolerance, candidate, readback, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from located interval data through finite-net inspection to the supremum-candidate row.
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalSupremumUp$, $\RealUp$, $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\CompactMetricUp$, $\TotallyBoundedUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactIntervalSupremumUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,I,K,T,D,U,M,R,H,C,P,N$. It has no coordinate for open-cover compactness, ambient completeness, quotient real equality, a selected maximizer, trichotomy, or an unrecorded comparison outside the displayed finite-net packet.
\end{definition}

\begin{theorem}[Compact interval supremum NameCert obligations]
\label{thm:compact-interval-supremum-namecert-obligations}
For every accepted $\CompactIntervalSupremumUp$ carrier
$$
S=(E,I,K,T,D,U,M,R,H,C,P,N),
$$
the local $\NameCert_{\CompactIntervalSupremumUp}$ surface consists exactly of RealUp endpoint exposure, located interval exposure, compact finite-net source exposure, total-bounded net coverage, dyadic tolerance accounting, finite candidate upper-bound inspection, finite maximum readback, supremum-candidate export, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier. The endpoint and interval side is $E,I$: every candidate is read against displayed real endpoint seals and located interval rows before any upper-bound comparison is available.

The compact finite-net side is $K,T,D$. Compactness is consumed only through finite metric coverage and total-bounded net rows, while the dyadic ledger records the mesh and slack budgets used by the comparisons.

The supremum read is $U,M,R$. The candidate upper bounds are finite rows, the maximum row is a finite readback over those rows, and the exported supremum candidate is downstream of the same displayed data.

Finally $H,C,P,N$ transport, replay, source, and name the packet. Since no carrier coordinate names open-cover compactness, ambient completeness, quotient equality, a selected maximizer, trichotomy, or a hidden comparison, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact interval finite-net supremum read]
\label{thm:compact-interval-supremum-finite-net-supremum-read}
Every accepted $\CompactIntervalSupremumUp$ read factors through the finite route
$$
\RealUp,\ \LocatedIntervalUp,\ \CompactMetricUp,\ \TotallyBoundedUp,\ \DyadicRatCoreUp
$$
inside the same carrier $S=(E,I,K,T,D,U,M,R,H,C,P,N)$. A consumer may read the exported candidate $R$ only after the endpoint seals, located interval rows, finite-net coverage, dyadic tolerance ledger, candidate upper-bound rows, and finite maximum readback have been displayed.
\end{theorem}
\begin{proof}
Begin with $E$ and $I$. These rows bind the real endpoints and located interval comparisons that define the inspected compact interval surface.

Next read $K,T,D$. The compact metric and total-bounded rows display the finite net, and the dyadic ledger fixes the tolerance and slack used for each comparison.

Then consume $U$ and $M$. Both are finite rows: $U$ lists candidate upper-bound inspections over the displayed net, and $M$ reads the finite maximum row attached to those candidates.

The replay, transport, provenance, and naming rows $H,C,P,N$ keep the route inside the same $\BHist$ packet. Therefore the exported row $R$ is a finite-net supremum candidate, not an open-cover compactness theorem, selected maximizer, quotient real equality, trichotomy decision, or ambient $\RealUp$ completeness assertion.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactIntervalSupremumUp$ read exposes RealUp endpoint seals, LocatedInterval rows, CompactMetric finite-net source data, TotallyBounded coverage rows, DyadicRatCore tolerance accounting, finite candidate upper-bound rows, finite maximum readback, hsame transport, Cont replay, Pkg provenance, and local NameCert naming before exporting a supremum candidate.}

\independenceWitness{real, dyadicratcore, locatedinterval, compactmetric, totallybounded: $\CompactIntervalSupremumUp$ is not bijective to any listed sibling because it jointly binds endpoint seals, located interval data, compact finite-net source rows, total-bounded coverage, dyadic tolerance budgets, candidate upper-bound inspection, finite maximum readback, transport, replay, provenance, and local naming.}

\closureat{CompactIntervalSupremumUp}{seedStr}

\begin{closurestatus}{\CompactIntervalSupremumUp}
  \constructivestory{A $\CompactIntervalSupremumUp$ packet is generated from RealUp endpoint seals, LocatedInterval rows, CompactMetric finite-net source data, TotallyBounded coverage rows, DyadicRatCore tolerance accounting, finite candidate upper-bound inspection, finite maximum readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-supremum-carrier}, \autoref{thm:compact-interval-supremum-namecert-obligations}, and \autoref{thm:compact-interval-supremum-finite-net-supremum-read} seed the compact interval finite-net supremum route over displayed endpoint, interval, compact, total-bounded, dyadic, candidate, readback, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, ambient RealUp completeness, quotient real equality, selected maximizers, trichotomy, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint exposure, located interval exposure, finite-net coverage, dyadic tolerance accounting, candidate upper-bound inspection, finite maximum readback, supremum-candidate export, transport, replay, provenance, and local naming for BEDC.Derived.CompactIntervalSupremumUp.}
\end{closurestatus}
