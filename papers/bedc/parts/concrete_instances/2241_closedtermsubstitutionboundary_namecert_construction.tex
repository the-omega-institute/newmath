\chapter{Closed-Term Substitution Boundary NameCert Construction}
\label{ch:concrete-instances-closedtermsubstitutionboundary-namecert}
\origin{human}

The $\ClosedTermSubstitutionBoundaryUp$ packet records the finite naming surface by which MetaCIC closed terms pass through shift and substitution without exporting a free-variable leak. It packages $\mathsf{Term}$, $\mathsf{ClosedAt}$, $\mathsf{shift}$, $\mathsf{substitute}$, closedness ledgers, audit-visible non-escape rows, transport data, continuation routes, package provenance, and the local $\NameCert$ row. The packet is not a host compiler theorem and does not assert subject reduction beyond the displayed closed-term boundary.

\begin{definition}[Closed-term substitution boundary carrier]
\label{def:closed-term-substitution-boundary-carrier}
A $\ClosedTermSubstitutionBoundaryUp$ carrier is a finite $\BHist$ packet
$$
S=(T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $T$ is the displayed $\mathsf{Term}$ row for the term being substituted into;
\item $V$ is the displayed $\mathsf{Term}$ row for the closed substituting value;
\item $d$ is the de Bruijn depth row at which the boundary is inspected;
\item $C_{\mathsf{src}}$ is the $\mathsf{ClosedAt}(d,T)$ source-closedness witness row;
\item $C_{\mathsf{val}}$ is the $\mathsf{ClosedAt}(0,V)$ value-closedness witness row;
\item $Q$ is the displayed $\mathsf{shift}$ boundary row recording that closed terms spend no future depth budget at the inspected boundary;
\item $R$ is the displayed $\mathsf{substitute}$ boundary row recording that substitution at a closed boundary returns the same closed source row;
\item $L$ is the finite closedness ledger recording which source, value, shift, and substitution rows have been read;
\item $A$ is the audit-visible non-escape row recording that no free variable, host equality witness, or untracked compiler state is exported by the packet;
\item $H$ stores componentwise $\hsame$ transports for the term rows, depth row, closedness rows, boundary-operation rows, ledger, audit row, package row, and local naming row;
\item $C$ stores the displayed $\Cont$ routes by which consumers read those finite rows;
\item $P$ is the $\Pkg$ provenance over $\mathsf{Term}$, $\mathsf{ClosedAt}$, $\mathsf{shift}$, $\mathsf{substitute}$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\ClosedTermSubstitutionBoundaryUp}$ row.
\end{enumerate}
The carrier contains no hidden typing derivation, no unrestricted compiler correctness theorem, no host substitution semantics, and no coordinate for an unregistered free variable.
\end{definition}
\leanchecked{BEDC.Derived.ClosedTermSubstitutionBoundaryUp.ClosedTermSubstitutionBoundaryPacket\_single\_carrier\_alignment}

\begin{definition}[Closed-term substitution boundary classifier]
\label{def:closed-term-substitution-boundary-classifier}
For accepted $\ClosedTermSubstitutionBoundaryUp$ packets $S$ and $S'$, the classifier $\mathsf{ClosedTermSubstitutionBoundaryClassifier}(S,S')$ holds exactly when the displayed $\mathsf{Term}$ rows, inspected depth row, $\mathsf{ClosedAt}$ witnesses, $\mathsf{shift}$ boundary row, $\mathsf{substitute}$ boundary row, closedness ledger, audit-visible non-escape row, $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row transport componentwise to the same finite boundary surface.

The classifier does not compare host syntax trees by an unrecorded equality test, does not inspect non-displayed typing derivations, and does not import a compiler-wide semantic preservation theorem.
\end{definition}

\begin{theorem}[Closed-term substitution boundary NameCert obligations]
\label{thm:closed-term-substitution-boundary-namecert-obligations}
The local $\NameCert_{\ClosedTermSubstitutionBoundaryUp}$ surface consists of five finite obligations:
\begin{enumerate}
\item carrier admission: accepted packets are exactly the finite $\BHist$ carriers of \autoref{def:closed-term-substitution-boundary-carrier};
\item classifier stability: \autoref{def:closed-term-substitution-boundary-classifier} is preserved by displayed $\hsame$ transports and $\Cont$ routes on the term rows, depth row, closedness witnesses, shift row, substitution row, ledger, audit row, package provenance, and local naming row;
\item shift boundary exactness: the $\mathsf{shift}$ row may be consumed only after the source closedness witness has been displayed, and it records that the closed boundary does not spend a future depth budget;
\item substitution boundary exactness: the $\mathsf{substitute}$ row may be consumed only after the source and value closedness witnesses have been displayed, and it records that substitution cannot manufacture a free-variable coordinate;
\item audit non-escape: the ledger and audit rows expose every closedness dependency required by the packet and export no unregistered free variable, hidden host equality, unchecked compiler state, or ambient subject-reduction theorem.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:closed-term-substitution-boundary-carrier}. The tuple displays the term rows, depth row, closedness witnesses, operation rows, ledger, audit row, transport and continuation data, package provenance, and local naming certificate.

Classifier stability follows from \autoref{def:closed-term-substitution-boundary-classifier}. The classifier is defined only on the displayed rows, so componentwise $\hsame$ movement and recorded $\Cont$ reads cannot introduce a second substitution semantics or an untracked compiler state.

The shift and substitution obligations are the two operation rows $Q$ and $R$. Their prerequisites are recorded in $C_{\mathsf{src}}$ and $C_{\mathsf{val}}$, and the finite ledger $L$ records that those witnesses have been read before the operation boundary is consumed.

Finally the audit row $A$ fixes the non-escape claim. Since the carrier has no coordinate for a free-variable leak, hidden host equality, or ambient subject-reduction theorem, no consumer route can export such data from the local certificate.
\end{proof}

\begin{theorem}[Closed-term substitution boundary ledger non-escape]
\label{thm:closed-term-substitution-boundary-ledger-non-escape}
Every accepted $\ClosedTermSubstitutionBoundaryUp$ packet exposes closed-term substitution only through the rows
$$
T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N.
$$
The ledger $L$ and audit row $A$ force every consumer read through the displayed $\mathsf{ClosedAt}$ witnesses and boundary-operation rows before it may cite the packet. No consumer receives an unregistered free variable, a hidden host equality witness, unchecked compiler state, or a subject-reduction theorem not carried by the packet.
\end{theorem}
\begin{proof}
Read the carrier coordinates from \autoref{def:closed-term-substitution-boundary-carrier}. The only rows available to a consumer are the two terms, the depth row, the closedness witnesses, the shift and substitution boundary rows, the ledger, the audit row, and the displayed provenance data.

Use the NameCert obligations of \autoref{thm:closed-term-substitution-boundary-namecert-obligations}. They require the closedness witnesses to be displayed before either operation row is consumed, and they place the non-escape check in the same finite packet.

The exclusions follow from absence of coordinates. A free-variable leak, hidden host equality witness, unchecked compiler state, or ambient subject-reduction theorem is not among the packet rows and cannot be reconstructed by $\hsame$ transport or a $\Cont$ read.
\end{proof}

\begin{theorem}[Closed-term substitution boundary consumer boundary]
\label{thm:closed-term-substitution-boundary-consumer-boundary}
A self-compilation consumer may use an accepted $\ClosedTermSubstitutionBoundaryUp$ packet only as the finite boundary surface for closed $\mathsf{shift}$ and $\mathsf{substitute}$ reads. The consumer must factor through the displayed $\mathsf{Term}$ rows, $\mathsf{ClosedAt}$ witnesses, depth row, operation rows, closedness ledger, audit-visible non-escape row, $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row.

This boundary supplies the closed-term substitution step needed by the self-compilation story, but it does not export a full compiler correctness theorem, a subject-reduction package, a host syntax quotient, or a hidden semantic model.
\end{theorem}
\begin{proof}
Start with the carrier and classifier. The carrier of \autoref{def:closed-term-substitution-boundary-carrier} displays the finite rows, and the classifier of \autoref{def:closed-term-substitution-boundary-classifier} permits only componentwise transport and recorded consumer reads of those rows.

Apply \autoref{thm:closed-term-substitution-boundary-namecert-obligations}. The shift and substitution rows become available only after the required closedness witnesses and ledger entries have been displayed.

Finally use \autoref{thm:closed-term-substitution-boundary-ledger-non-escape}. The consumer route cannot leave the finite packet to obtain compiler correctness, subject reduction, host quotient syntax, or a hidden semantic model, so the self-compilation use is exactly the closed-term substitution boundary.
\end{proof}

\begin{theorem}[Closed-term substitution boundary BinderBudgetSeal factorization]
\label{thm:closed-term-substitution-boundary-binder-seal-factorization}
Every $\BinderBudgetSealUp$ shift or substitution read that is used by a closed-term substitution consumer factors through the displayed $\ClosedTermSubstitutionBoundaryUp$ rows
$$
T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N.
$$
Thus the binder-budget seal of \autoref{ch:concrete-instances-binderbudgetseal-namecert} may consume the closed shift and substitution facts only through the finite $\BHist$ packet, the componentwise $\hsame$ transports, the displayed $\Cont$ routes, the $\Pkg$ provenance, and the local $\NameCert$ row of \autoref{def:closed-term-substitution-boundary-carrier}.
\end{theorem}
\begin{proof}
Start with a $\BinderBudgetSealUp$ read that asks for closed shift or closed substitution at the inspected binder depth. The local carrier theorem \autoref{thm:closed-term-substitution-boundary-consumer-boundary} restricts that read to the finite closed-term boundary surface.

The needed closedness witnesses are exactly $C_{\mathsf{src}}$ and $C_{\mathsf{val}}$, and the permitted operation rows are exactly $Q$ and $R$. By \autoref{thm:closed-term-substitution-boundary-namecert-obligations}, these rows are available only after the ledger $L$ records the corresponding closedness read.

Finally apply \autoref{thm:closed-term-substitution-boundary-ledger-non-escape}. Since the audit row $A$ and the continuation row $C$ admit no hidden binder convention, unregistered free-variable coordinate, or ambient compiler theorem, the binder-budget consumer factors through the displayed packet rows and no other substitution surface.
\end{proof}

\begin{theorem}[ClosedTermSubstitutionCompiler boundary factorization]
\label{thm:closed-term-substitution-boundary-compiler-factorization}
The $\ClosedTermSubstitutionCompilerUp$ schedule of \autoref{ch:concrete-instances-closedtermsubstitutioncompiler-namecert} reads the same closed finite boundary before exposing any compiler-facing $\mathsf{substitute\_closed}$ row. In particular, every accepted schedule read passes through the displayed rows
$$
T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N
$$
of $\ClosedTermSubstitutionBoundaryUp$, then repacks the result through the compiler packet's own continuation and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Project the compiler-facing read to its closed source and closed value premises. The consumer boundary theorem \autoref{thm:closed-term-substitution-boundary-consumer-boundary} identifies those premises with a read of the closed-term substitution boundary carrier rather than with an evaluator theorem.

The schedule can expose $\mathsf{substitute\_closed}$ only after it reads $C_{\mathsf{src}}$, $C_{\mathsf{val}}$, and the substitution row $R$. The $\BHist$ ledger $L$ and the $\Cont$ routes $C$ record this order, while the $\hsame$ transports $H$ keep the same finite rows under accepted packet movement.

The final compiler packet is therefore a repackaging step. The $\Pkg$ provenance $P$ and local naming row $N$ certify that the compiler-facing schedule has consumed the closed boundary, but the audit row $A$ prevents the schedule from importing open-term subject reduction, host syntax equality, or a hidden semantic evaluator.
\end{proof}

\begin{theorem}[Closed-term substitution boundary RecursorGenerator handoff]
\label{thm:closed-term-substitution-boundary-recursor-handoff}
Every generated branch output of $\RecursorGeneratorUp$ in \autoref{ch:concrete-instances-recursorgenerator-namecert} consumes closed-term substitution only through the finite $\ClosedTermSubstitutionBoundaryUp$ boundary. The handoff route is the displayed $\BHist$ packet with rows
$$
T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N,
$$
followed by the recursor generator's own branch-output packaging.
\end{theorem}
\begin{proof}
Consider a generated branch that invokes closed substitution while assembling its output. The branch has access to the boundary only through recorded $\Cont$ routes, so \autoref{thm:closed-term-substitution-boundary-consumer-boundary} forces the substitution read to enter the closed-term boundary carrier.

Inside that carrier, \autoref{thm:closed-term-substitution-boundary-namecert-obligations} supplies the required order: source closedness, value closedness, ledger entry, and then the displayed substitution boundary row. Componentwise $\hsame$ transport preserves the same rows when the branch output is moved across accepted packets.

The handoff cannot bypass the finite packet. By \autoref{thm:closed-term-substitution-boundary-ledger-non-escape}, the packet exports no unregistered free variable, host equality witness, unchecked compiler state, or subject-reduction theorem, so the generated branch output receives only the certified closed-term substitution boundary.
\end{proof}

\begin{theorem}[Closed-term substitution boundary audit route]
\label{thm:closed-term-substitution-boundary-audit-route}
Every audit-visible read of an accepted $\ClosedTermSubstitutionBoundaryUp$ packet factors through the displayed audit surface $A,H,C,P,N$. In particular, a read that cites closed shift, closed substitution, ledger non-escape, or consumer admissibility must replay the audit row $A$, componentwise $\hsame$ transport $H$, displayed $\Cont$ route $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\ClosedTermSubstitutionBoundaryUp}$ row $N$.

The audit route exports no free-variable coordinate, host equality witness, unchecked compiler state, hidden evaluator, or ambient subject-reduction theorem.
\end{theorem}
\begin{proof}
Start with the carrier and classifier of \autoref{def:closed-term-substitution-boundary-carrier} and \autoref{def:closed-term-substitution-boundary-classifier}. They make $A,H,C,P,N$ the only audit, transport, route, provenance, and naming rows available to the packet.

Use the NameCert obligations of \autoref{thm:closed-term-substitution-boundary-namecert-obligations}. The local certificate requires the shift and substitution rows to be consumed only after their displayed closedness witnesses and ledger entries have appeared.

Finally apply ledger non-escape and the consumer boundary from \autoref{thm:closed-term-substitution-boundary-ledger-non-escape} and \autoref{thm:closed-term-substitution-boundary-consumer-boundary}. Since every consumer read is already confined to the finite closed-term boundary, the audit-visible part of that read is exactly the route through $A,H,C,P,N$.
\end{proof}

\begin{theorem}[Closed-term substitution boundary shift-budget exhaustion]
\label{thm:closed-term-substitution-boundary-shift-budget-exhaustion}
For an accepted $\ClosedTermSubstitutionBoundaryUp$ packet, the shift row $Q$ consumes only the displayed source closedness witness $C_{\mathsf{src}}$ at depth $d$. Once $C_{\mathsf{src}}$ is read through the ledger $L$, the row $Q$ records that closed shifting spends no hidden future depth budget and does not consult any second binder-depth source.
\end{theorem}
\begin{proof}
Project the source term row $T$, inspected depth row $d$, source closedness witness $C_{\mathsf{src}}$, shift row $Q$, and ledger row $L$ from the carrier.

The NameCert obligations identify shift boundary exactness as a local obligation of the same packet. The row $Q$ is available only after $C_{\mathsf{src}}$ has been displayed and recorded by $L$.

Use ledger non-escape to rule out any hidden budget source. The packet has no coordinate for a second depth counter, a host binder convention, or an external compiler theorem, so the closed shift consumes exactly the displayed source closedness witness.
\end{proof}

\begin{theorem}[Closed-term substitution boundary free-variable non-escape]
\label{thm:closed-term-substitution-boundary-substitution-free-variable-nonescape}
For an accepted $\ClosedTermSubstitutionBoundaryUp$ packet, the substitution row $R$ cannot manufacture an unregistered free-variable coordinate after the displayed source and value closedness witnesses $C_{\mathsf{src}}$ and $C_{\mathsf{val}}$ have been read. Any substitution consumer receives the same finite boundary surface and no open-term residue outside $T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N$.
\end{theorem}
\begin{proof}
Read the substitution prerequisites from the carrier. The row $R$ is paired with the source term $T$, the closed value $V$, the inspected depth $d$, and the two closedness witnesses $C_{\mathsf{src}}$ and $C_{\mathsf{val}}$.

Apply the NameCert substitution boundary obligation. It permits $R$ to be consumed only after both closedness witnesses are displayed and after the ledger has recorded that read.

The ledger non-escape theorem then removes the only remaining escape route. Because no carrier coordinate names an unregistered free variable or open-term residue, neither $\hsame$ transport nor a displayed $\Cont$ read can create one after substitution.
\end{proof}

\begin{theorem}[Closed-term substitution boundary depth row stability]
\label{thm:closed-term-substitution-boundary-depth-row-stability}
Transporting an accepted $\ClosedTermSubstitutionBoundaryUp$ packet by componentwise $\hsame$ preserves the inspected depth row $d$. The transported packet may move the displayed term rows, closedness witnesses, operation rows, ledger, audit row, continuation routes, package provenance, and local name through the rows listed in \autoref{def:closed-term-substitution-boundary-carrier}, but it cannot replace $d$ by an unlisted binder budget.
\end{theorem}
\begin{proof}
Project the carrier row of \autoref{def:closed-term-substitution-boundary-carrier}. The inspected depth $d$ is a named coordinate of the same packet as $T,V,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N$.

Use the classifier definition and the NameCert obligations. Componentwise $\hsame$ transport is available only through $H$, and the classifier compares the displayed depth row together with the displayed term, closedness, operation, ledger, audit, route, provenance, and naming rows.

Apply ledger non-escape from \autoref{thm:closed-term-substitution-boundary-ledger-non-escape}. Since no coordinate names a second binder budget or a host binder convention, transported consumers keep the same inspected depth row and cannot read an unlisted depth source.
\end{proof}

\begin{theorem}[Closed-term substitution boundary closedness ledger coverage]
\label{thm:closed-term-substitution-boundary-closedness-ledger-coverage}
Every consumer of the displayed shift row $Q$ or substitution row $R$ must read $C_{\mathsf{src}}$ and $C_{\mathsf{val}}$ through the closedness ledger $L$ before the corresponding operation row is available. The shift consumer may ignore the value witness only as unused data, but it still receives a packet whose local ledger records the closedness surface for both source and value rows. The route exports no host equality, no open-term subject reduction, and no hidden compiler state.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:closed-term-substitution-boundary-namecert-obligations}. They make shift boundary exactness depend on the displayed source closedness row and substitution boundary exactness depend on the displayed source and value closedness rows.

Read the ledger non-escape theorem. The ledger $L$ is the finite row that records which closedness witnesses have been consumed before $Q$ or $R$ is exposed to a consumer route.

Use shift-budget exhaustion and substitution free-variable non-escape. The shift row cannot spend a hidden depth budget, and the substitution row cannot manufacture an unregistered free-variable coordinate after the ledger has displayed the closedness witnesses.

The exclusions are then coordinate exclusions. Host equality, open-term subject reduction, and hidden compiler state are absent from the carrier inventory, so the operation rows become visible only through $C_{\mathsf{src}}$, $C_{\mathsf{val}}$, $L$, and the displayed route rows.
\end{proof}

\begin{theorem}[Closed-term substitution boundary operation row determinacy]
\label{thm:closed-term-substitution-boundary-operation-row-determinacy}
For fixed rows $T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},L,A,H,C,P,N$ of an accepted $\ClosedTermSubstitutionBoundaryUp$ packet, the displayed shift row $Q$ and substitution row $R$ are the unique operation rows visible to any closed-term consumer. A consumer may transport or replay those rows through $H$ and $C$, but it cannot substitute a host equality proof, an open-term subject-reduction theorem, or hidden compiler state for either operation row.
\end{theorem}
\begin{proof}
First project the carrier. The only operation coordinates in \autoref{def:closed-term-substitution-boundary-carrier} are the shift boundary row $Q$ and the substitution boundary row $R$.

Use the NameCert obligations and closedness ledger coverage. The local certificate exposes $Q$ and $R$ only after the closedness witnesses have been read through $L$, and it exposes no second operation coordinate under the same fixed rows.

Apply depth row stability, audit route, and ledger non-escape. Transport through $H$ preserves the inspected depth row, audit-visible replay remains inside $A,H,C,P,N$, and the ledger excludes unregistered free variables, hidden host equality, unchecked compiler state, and ambient subject-reduction data.

Thus a closed-term consumer with the fixed carrier rows can see exactly the displayed $Q$ and $R$ rows. Any different operation row would require a carrier coordinate not present in the packet.
\end{proof}

\begin{theorem}[Closed-term substitution boundary consumer factorization]
\label{thm:closed-term-substitution-boundary-consumer-factorization}
Every self-compilation consumer of an accepted $\ClosedTermSubstitutionBoundaryUp$ packet factors through the full displayed surface
$$
T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N.
$$
The consumer may use the packet for closed shift and closed substitution only by reading the term rows, depth row, source and value closedness witnesses, operation rows, closedness ledger, audit row, componentwise transports, continuation routes, package provenance, and local naming row listed in the carrier.
\end{theorem}
\begin{proof}
Begin with the carrier and classifier. They expose exactly the rows $T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N$ and restrict accepted movement to componentwise transport and displayed route replay.

Use the NameCert obligations for the two operation rows. The shift row $Q$ depends on the displayed source closedness witness, while the substitution row $R$ depends on the displayed source and value closedness witnesses.

Apply the audit-route theorem, shift-budget exhaustion, and substitution free-variable non-escape. These three obligations force the consumer to spend no hidden depth budget, manufacture no unregistered free variable, and route every audit-visible read through $A,H,C,P,N$.

Finally invoke the consumer boundary of \autoref{thm:closed-term-substitution-boundary-consumer-boundary}. The self-compilation consumer therefore factors through the full finite boundary surface rather than through a detached compiler theorem, host syntax quotient, evaluator, or subject-reduction package.
\end{proof}

\closureat{ClosedTermSubstitutionBoundaryUp}{seedStr}
\closureat{ClosedTermSubstitutionBoundaryUp}{obligationStr}

\begin{closurestatus}{\ClosedTermSubstitutionBoundaryUp}
  \constructivestory{A $\ClosedTermSubstitutionBoundaryUp$ packet is generated from displayed $\mathsf{Term}$ rows, $\mathsf{ClosedAt}$ witnesses, a depth row, closed $\mathsf{shift}$ and $\mathsf{substitute}$ boundary rows, a finite closedness ledger, an audit-visible non-escape row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:closed-term-substitution-boundary-carrier}, \autoref{def:closed-term-substitution-boundary-classifier}, \autoref{thm:closed-term-substitution-boundary-namecert-obligations}, \autoref{thm:closed-term-substitution-boundary-ledger-non-escape}, and \autoref{thm:closed-term-substitution-boundary-consumer-boundary} bind closed-term substitution as a finite NameCert packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close full compiler correctness, subject reduction, host syntax quotienting, hidden semantic models, or bridge checking.}
  \upgradepath{Scoped closure requires one scope proof over the carrier, classifier, NameCert obligations, ledger non-escape, and consumer-boundary rows for the same closed-term substitution packet.}
\end{closurestatus}
