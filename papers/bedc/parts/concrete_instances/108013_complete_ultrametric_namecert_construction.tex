\chapter{A Concrete Naming Certificate for $\CompleteUltrametricUp$}
\label{ch:concrete-instances-complete-ultrametric-namecert}
\origin{human}

Following Bourbaki and Bishop--Bridges metric-space practice, the $\CompleteUltrametricUp$ packet records the finite certificate surface for a complete ultrametric rather than a choice-based completion. It sits after the strong-triangle surface of \autoref{ch:concrete-instances-ultrametricspace-namecert}, the limit-witness surface of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy-filter admission row of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the finite-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational tolerance route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet names one constructive non-Archimedean completeness certificate; it does not import spherical completeness, a selected quotient completion, a host completion functor, or a p-adic field structure.

\begin{definition}[CompleteUltrametric carrier]
\label{def:complete-ultrametric-carrier}
A $\CompleteUltrametricUp$ carrier is a finite $\BHist$ packet
$$
U=(M,U,C,L,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the metric-carrier row, including the underlying $\MetricUp$ distance observations and ball classifier.
\item $U$ is the ultrametric strong-triangle row consumed through $\UltrametricSpaceUp$.
\item $C$ is the Cauchy-filter and Cauchy-thread row, including the finite $\CauchyFilterUp$, $\StreamNameUp$, and $\RegSeqRatUp$ reads by which tolerance requests are displayed.
\item $L$ is the complete-limit witness row consumed through $\CompleteMetricUp$ and sealed only at the displayed $\RealUp$ endpoint row.
\item $H$ records componentwise $\hsame$ transport for the metric, strong-triangle, Cauchy-thread, limit, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from finite Cauchy-thread reads to the complete-limit handoff and from ultrametric ball comparisons to downstream consumers.
\item $P$ is the $\Pkg$ provenance over $\CompleteUltrametricUp$, $\UltrametricSpaceUp$, $\CompleteMetricUp$, $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompleteUltrametricUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $M,U,C,L,H,R,P,N$. It has no coordinate for spherical completeness, choice-based completion, quotient completion, selected limits outside the displayed thread, p-adic field operations, or a standard bridge.
\end{definition}

\begin{theorem}[CompleteUltrametric NameCert obligations]
\label{thm:complete-ultrametric-namecert-obligations}
For an accepted $\CompleteUltrametricUp$ carrier $U=(M,U,C,L,H,R,P,N)$, the local $\NameCert_{\CompleteUltrametricUp}$ surface consists exactly of five finite obligations:
\begin{enumerate}
\item carrier admission for the displayed metric row $M$ and local naming row $N$;
\item ultrametric triangle stability through the strong-triangle row $U$ under componentwise $\hsame$ transport $H$;
\item Cauchy-thread admission through $C$, with every tolerance read passing through the $\CauchyFilterUp$, $\StreamNameUp$, and $\RegSeqRatUp$ windows;
\item complete-limit handoff from $C$ to $L$ through the $\CompleteMetricUp$ witness and terminal $\RealUp$ seal;
\item ledger non-escape for $H,R,P,N$, preventing transport, replay, provenance, or naming from adding spherical completeness, choice-based completion, quotient completion, selected external limits, or p-adic field structure.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:complete-ultrametric-carrier}. The rows available to a consumer are precisely $M,U,C,L,H,R,P,N$.

Read the metric and ultrametric data first. The row $M$ supplies the carrier and ball classifier, while $U$ supplies the strong non-Archimedean triangle comparison. Transport by $H$ preserves those displayed rows rather than replacing them by an ambient theorem.

Next read the Cauchy-thread data. The row $C$ exposes only finite filter, stream, and regular-rational tolerance windows, so a limit request has to pass through the same displayed Cauchy evidence.

Finally consume the complete-limit handoff. The row $L$ is reached from $C$ through the $\CompleteMetricUp$ witness and the terminal $\RealUp$ seal. The replay, provenance, and naming rows $R,P,N$ retain that route, so no unlisted completion principle or p-adic structure enters the certificate.
\end{proof}

\closureat{CompleteUltrametricUp}{seedStr}

\begin{closurestatus}{\CompleteUltrametricUp}
  \constructivestory{A $\CompleteUltrametricUp$ packet is generated from a metric carrier row, an ultrametric strong-triangle row, Cauchy-filter and thread rows, a complete-limit witness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:complete-ultrametric-carrier} and \autoref{thm:complete-ultrametric-namecert-obligations} seed the constructive complete non-Archimedean metric certificate over displayed metric, ultrametric, Cauchy-thread, limit, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close spherical completeness, choice-based completion, quotient completion, selected limits outside the displayed thread, p-adic field structure, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, ultrametric triangle stability, Cauchy-thread admission, complete-limit handoff, and ledger non-escape for BEDC.Derived.CompleteUltrametricUp.}
\end{closurestatus}
