\chapter{A Concrete Naming Certificate for $\RegSeqRatLocatedModulusUp$}
\label{ch:concrete-instances-regseqrat-located-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegSeqRatLocatedModulusUp$ packet records the finite surface at which a located regular rational sequence exposes the modulus windows consumed by Phase Real and Completion classifiers. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the located regular-sequence route of \autoref{ch:concrete-instances-located-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a carrier for displayed located-modulus data, not a quotient completion, a selected limit, a countable-choice schedule, a trichotomy oracle, or an ambient completeness principle.

\begin{definition}[RegSeqRat located-modulus carrier]
\label{def:regseqrat-located-modulus-carrier}
A $\RegSeqRatLocatedModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(Q,L,\mu,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the $\DyadicRatCoreUp$ tolerance query row.
\item $L$ is the $\LocatedRegSeqRatUp$ located regular-sequence row.
\item $\mu$ is the displayed modulus row assigning finite stream windows to the tolerance queries admitted by $Q$.
\item $W$ is the $\StreamNameUp$ finite-window row exposed by $\mu$.
\item $D$ is the dyadic decision ledger binding the tolerance query to the displayed window.
\item $R$ is the $\RegSeqRatUp$ rational readback row at the window named by $W$.
\item $E$ is the $\RealUp$ terminal seal reached after the located regular-sequence readback.
\item $H$ stores componentwise $\hsame$ transport for $Q,L,\mu,W,D,R,E,C,P,N$.
\item $C$ stores displayed $\Cont$ replay from tolerance request to modulus window, regular readback, locatedness row, and real seal.
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatLocatedModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedRegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegSeqRatLocatedModulusUp}$ row.
\end{enumerate}
The classifier reads accepted packets only through $Q,L,\mu,W,D,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected limit representative, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[RegSeqRat located-modulus NameCert obligations]
\label{thm:regseqrat-located-modulus-namecert-obligations}
For every accepted $\RegSeqRatLocatedModulusUp$ carrier $M=(Q,L,\mu,W,D,R,E,H,C,P,N)$, the local $\NameCert_{\RegSeqRatLocatedModulusUp}$ surface consists exactly of carrier admission, located-modulus exposure, finite-window stability, $\RegSeqRatUp$ readback, $\RealUp$ seal routing, componentwise transport, displayed continuation replay, package provenance, local naming, and ledger refusal for quotient stream equality, selected limits, countable choice, trichotomy, host real equality, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regseqrat-located-modulus-carrier}. The accepted packet first exposes the tolerance query $Q$, the located regular-sequence row $L$, and the displayed modulus row $\mu$.

The modulus row opens only the finite $\StreamNameUp$ window $W$ and the dyadic decision ledger $D$ attached to that request. The rational readback row $R$ is then read at the displayed window, and the terminal real-name seal $E$ is reached only after this finite route has been replayed.

Transport and continuation are exhausted by $H$ and $C$, while $P$ and $N$ record provenance and local naming. Since the carrier has no further coordinate, no accepted packet can import quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[RegSeqRat located-modulus handoff]
\label{thm:regseqrat-located-modulus-handoff}
Every $\LocatedRegSeqRatUp$, $\RegSeqRatUp$, or $\RealUp$ consumer of an accepted $\RegSeqRatLocatedModulusUp$ packet factors through $Q,L,\mu,W,D,R,E,H,C,P,N$ before any consumer-visible located read is exported. The handoff therefore exposes the located modulus as a finite window discipline over regular rational readback and terminal real-name sealing, not as quotient stream equality, a selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier as $M=(Q,L,\mu,W,D,R,E,H,C,P,N)$. A $\LocatedRegSeqRatUp$ consumer must read the located row $L$ together with the displayed modulus row $\mu$ before it can export a locatedness response.

A $\RegSeqRatUp$ consumer reaches rational data only through the finite window $W$ named by $\mu$ and the readback row $R$ bound to that window. The dyadic ledger $D$ records which tolerance request was served, so the read cannot be replaced by an unbounded stream equality or by an arbitrary limit choice.

A $\RealUp$ consumer reaches the terminal seal $E$ only after the same window and readback route has been replayed by $C$ and transported by $H$. The remaining rows $P$ and $N$ certify provenance and local naming, so the handoff contains no coordinate for quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient completeness.
\end{proof}

\closureat{RegSeqRatLocatedModulusUp}{seedStr}

\begin{closurestatus}{\RegSeqRatLocatedModulusUp}
  \constructivestory{A $\RegSeqRatLocatedModulusUp$ packet is generated from a DyadicRatCore tolerance query, a LocatedRegSeqRat row, a displayed modulus row, StreamName finite windows, a dyadic decision ledger, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regseqrat-located-modulus-carrier}, \autoref{thm:regseqrat-located-modulus-namecert-obligations}, and \autoref{thm:regseqrat-located-modulus-handoff} seed the finite located-modulus route from regular rational sequence data to Real-facing handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, located-modulus exposure, finite-window stability, RegSeqRat readback, RealUp seal routing, and ledger refusal for BEDC.Derived.RegSeqRatLocatedModulusUp.}
\end{closurestatus}
