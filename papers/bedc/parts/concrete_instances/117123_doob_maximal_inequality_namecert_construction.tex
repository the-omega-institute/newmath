\chapter{A Concrete Naming Certificate for $\DoobMaximalInequalityUp$}
\label{ch:concrete-instances-doob-maximal-inequality-namecert}
\origin{human}

Following Doob, the $\DoobMaximalInequalityUp$ packet records the finite maximal-window inequality surface used when a bounded martingale prefix is read before any convergence or almost-sure quotient consumer sees it. It sits after the finite martingale carrier of \autoref{ch:concrete-instances-martingale-namecert}, the bounded stopped-window route of \autoref{ch:concrete-instances-optionalstopping-namecert}, the Doob upcrossing ledger of \autoref{ch:concrete-instances-doob-upcrossing-namecert}, the convergence-preparation packet of \autoref{ch:concrete-instances-martingale-convergence-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The certificate names only finite probability, martingale, maximal-window, stopping, inequality-bound, Real-boundary, transport, replay, provenance, and local naming rows.

\begin{definition}[Doob maximal inequality carrier]
\label{def:doob-maximal-inequality-carrier}
A $\DoobMaximalInequalityUp$ carrier is a finite $\BHist$ packet
$$
D=(\Omega,M,W,A,S,U,B,R,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $\Omega$ is the $\ProbSpaceUp$ probability row, carrying only the finite event and expectation observations used by the bound.
\item $M$ is the $\MartingaleUp$ source row, exposing the adapted finite prefix and conditional-expectation faces.
\item $W$ is the maximal-window row, recording the displayed finite horizon and the inspected prefix maximum.
\item $A$ is the threshold row, naming the positive $\RealUp$ boundary at which the maximal event is queried.
\item $S$ is the bounded $\OptionalStoppingUp$ route used to read first-hit and terminal stopped values inside the displayed horizon.
\item $U$ is the $\DoobUpcrossingUp$ sibling row, recording that crossing-count control is available through the same martingale and stopped-window surface.
\item $B$ is the finite Doob-bound row, relating the maximal event and threshold to the endpoint expectation read.
\item $R$ is the $\RealUp$ boundary row sealing threshold, expectation, and bound comparisons after the finite rows have been replayed.
\item $H$ records componentwise $\hsame$ transport for the probability, martingale, maximal-window, threshold, optional-stopping, upcrossing, bound, Real-boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
\Omega,M,W,A,S,U,B,R \longmapsto \NameCert_{\DoobMaximalInequalityUp}.
$$
\item $P$ is the $\Pkg$ provenance over $\DoobMaximalInequalityUp$, $\ProbSpaceUp$, $\MartingaleUp$, $\OptionalStoppingUp$, $\DoobUpcrossingUp$, $\MartingaleConvergenceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\DoobMaximalInequalityUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through these rows and their displayed transports. It has no coordinate for a completed filtration, an unbounded stopping time, almost-sure quotient equality, a selected exceptional set, ambient measure completion, or host $L^p$ maximal theorem.
\end{definition}

\begin{theorem}[Doob maximal inequality NameCert obligations]
\label{thm:doob-maximal-inequality-namecert-obligations}
For every accepted $\DoobMaximalInequalityUp$ carrier $D=(\Omega,M,W,A,S,U,B,R,H,C,P,N)$, the local $\NameCert_{\DoobMaximalInequalityUp}$ surface consists exactly of probability exposure, finite martingale-prefix exposure, maximal-window admission, threshold-boundary admission, bounded optional-stopping handoff, Doob-upcrossing compatibility, finite Doob-bound admission, $\RealUp$ boundary sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming rows. No obligation is discharged by a completed filtration, an unbounded stopping theorem, almost-sure quotient equality, a selected exceptional set, ambient measure completion, or a host $L^p$ maximal theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:doob-maximal-inequality-carrier}. The analytic-facing rows are exactly $\Omega,M,W,A,S,U,B,R$, and the structural rows $H,C,P,N$ only transport, replay, source, and name those rows.

The maximal-window row $W$ and threshold row $A$ determine the finite event being bounded. The bounded optional-stopping row $S$ keeps first-hit and terminal reads inside the displayed horizon, while $U$ records that the same martingale surface is compatible with the Doob-upcrossing ledger.

The bound row $B$ and Real-boundary row $R$ are then terminal for the inequality read. Since every comparison passes through $R$ after the finite rows have been replayed, the certificate cannot import completed filtrations, unbounded stopping, almost-sure quotient equality, selected exceptional sets, ambient measure completion, or host maximal theorems.
\end{proof}

\begin{theorem}[Doob maximal inequality convergence handoff]
\label{thm:doob-maximal-inequality-convergence-handoff}
Every accepted $\DoobMaximalInequalityUp$ carrier routes a convergence-facing maximal read through the finite dependency chain
$$
\MartingaleUp \longmapsto \OptionalStoppingUp \longmapsto \DoobUpcrossingUp \longmapsto \DoobMaximalInequalityUp \longmapsto \MartingaleConvergenceUp \longmapsto \RealUp.
$$
The exported object is only the displayed maximal-bound row $B$ together with its $\RealUp$ boundary seal $R$; it is not a martingale convergence theorem, not an almost-sure maximal process, and not a completed measure-space argument.
\end{theorem}
\begin{proof}
Begin with the martingale source row $M$. It supplies the finite adapted prefix and conditional-expectation faces consumed by the maximal-window row $W$.

Next read the bounded optional-stopping row $S$ and the Doob-upcrossing sibling row $U$. These rows keep first-hit, terminal, and crossing-control reads inside the same displayed horizon and prevent the maximal event from becoming an unbounded stopping assertion.

Finally expose the bound row $B$ and the Real-boundary row $R$. A convergence-facing consumer may cite this finite maximal-bound surface before reading \autoref{ch:concrete-instances-martingale-convergence-namecert}, but receives no convergence theorem or almost-sure quotient from this chapter.
\end{proof}

\falsifiablePrediction{Every accepted $\DoobMaximalInequalityUp$ consumer that reads a maximal-event bound without displaying the probability source, finite martingale prefix, maximal-window row, threshold boundary, bounded $\OptionalStoppingUp$ route, $\DoobUpcrossingUp$ compatibility row, finite bound row, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row refutes the packet surface.}

\independenceWitness{martingale, optionalstopping, doob-upcrossing, martingale-convergence, real: none is bijective to $\DoobMaximalInequalityUp$ because this packet jointly carries the finite maximal-window row, threshold boundary, bounded stopping route, upcrossing compatibility, Doob-bound row, Real seal, transport, replay, provenance, and local naming needed by maximal-inequality consumers.}

\closureat{DoobMaximalInequalityUp}{seedStr}

\begin{closurestatus}{\DoobMaximalInequalityUp}
  \constructivestory{A $\DoobMaximalInequalityUp$ packet is generated from one probability row, one finite martingale row, one maximal-window row, one threshold row, one bounded optional-stopping row, one Doob-upcrossing compatibility row, one finite bound row, one RealUp boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:doob-maximal-inequality-carrier}, \autoref{thm:doob-maximal-inequality-namecert-obligations}, and \autoref{thm:doob-maximal-inequality-convergence-handoff} seed the finite maximal-inequality packet over probability, martingale, bounded stopping, Doob-upcrossing, convergence-facing, Real-boundary, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed filtrations, unbounded stopping, almost-sure quotient equality, selected exceptional sets, ambient measure completion, martingale convergence, host $L^p$ maximal theorems, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, maximal-window exactness, bounded optional-stopping handoff, Doob-upcrossing compatibility, finite bound admission, Real-boundary nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DoobMaximalInequalityUp.}
\end{closurestatus}
