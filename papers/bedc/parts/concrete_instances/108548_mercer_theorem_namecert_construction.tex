\chapter{A Concrete Naming Certificate for $\MercerTheoremUp$}
\label{ch:concrete-instances-mercer-theorem-namecert}
\origin{human}

Following Mercer 1909 and the compact-operator spectral tradition represented in Reed--Simon, the $\MercerTheoremUp$ packet records the finite BEDC surface for a positive continuous kernel whose compact integral-operator shadow is read through matrix windows and Hilbert-space coordinates. It sits beside the compact-operator carrier of \autoref{ch:concrete-instances-compactoperator-namecert}, the Hilbert package of \autoref{ch:concrete-instances-hilbert-namecert}, the inner-product source of \autoref{ch:concrete-instances-innerproduct-namecert}, the finite matrix surface of \autoref{ch:concrete-instances-matrix-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only a finite positive-kernel and RKHS readback certificate; it does not assert ambient measure theory, choice-selected eigenbases, unrestricted integral operators, or the full spectral theorem.

\begin{definition}[Mercer theorem carrier]
\label{def:mercer-theorem-carrier}
A $\MercerTheoremUp$ carrier is a finite $\BHist$ packet
$$
M=(K,O,F,R,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact positive-kernel source row, recording a symmetric positive kernel request together with its compact-domain and continuity ledgers.
\item $O$ is the $\CompactOperatorUp$ shadow row, read only as a compact positive operator generated by the displayed kernel source.
\item $F$ is the finite $\MatrixUp$ window family: Gram matrices, kernel submatrices, and finite-rank approximation rows over displayed sample windows.
\item $R$ is the $\HilbertUp$ and $\mathsf{RKHS}^{\uparrow}$ readback row, exposing only inner-product evaluations and reproducing-kernel coordinates already named by $K,O,F$.
\item $L$ is the $\RealUp$ eigenvalue-window row for the finite matrix and compact-operator approximants.
\item $E$ is the expansion-read row, recording only finite Mercer-sum windows and their error budgets.
\item $H$ records componentwise $\hsame$ transport for the kernel source, compact-operator shadow, matrix windows, Hilbert and RKHS readback, Real eigenvalue windows, expansion rows, and structural rows.
\item $C$ records displayed $\Cont$ replay from kernel samples to matrix windows, from matrix windows to compact-operator reads, from compact-operator reads to Hilbert/RKHS coordinates, and from those coordinates to finite expansion windows.
\item $P$ is the $\Pkg$ provenance over $\MercerTheoremUp$, $\CompactOperatorUp$, $\HilbertUp$, $\InnerProductUp$, $\MatrixUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MercerTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,O,F,R,L,E,H,C,P,N$. It has no coordinate for ambient measure spaces, choice-selected eigenbases, unrestricted integral operators, host spectral decompositions, quotient function spaces, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Mercer theorem NameCert obligations]
\label{thm:mercer-theorem-namecert-obligations}
For every accepted $\MercerTheoremUp$ carrier $M=(K,O,F,R,L,E,H,C,P,N)$, the local $\NameCert_{\MercerTheoremUp}$ surface consists exactly of compact positive-kernel admission through $K$, compact-operator shadowing through $O$, finite matrix and kernel windows through $F$, Hilbert and RKHS readback through $R$, Real eigenvalue windows through $L$, finite expansion windows through $E$, componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:mercer-theorem-carrier}. Its analytic rows are exactly the positive-kernel source, compact-operator shadow, finite matrix windows, Hilbert/RKHS readback, Real eigenvalue windows, and finite expansion windows.

The route is finite. Kernel samples enter through $K$, become matrix and Gram windows through $F$, and are read as a compact positive operator only through $O$ before a Hilbert or RKHS consumer may inspect coordinates in $R$.

The eigenvalue row $L$ and expansion row $E$ are downstream of those displayed windows. They therefore expose only finite Real eigenvalue approximants and finite Mercer-sum windows, not a selected basis or host spectral decomposition.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier contains no row for ambient measure theory, unrestricted integral operators, choice-selected eigenbases, quotient function spaces, full spectral theorem data, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Mercer theorem positive-kernel handoff]
\label{thm:mercer-theorem-positive-kernel-handoff}
Every finite Mercer read from an accepted $\MercerTheoremUp$ carrier factors through the displayed route
$$
\MatrixUp,\RealUp \longmapsto \CompactOperatorUp \longmapsto \HilbertUp,\InnerProductUp \longmapsto \mathsf{RKHS}^{\uparrow}
$$
together with $H,C,P,N$. The positive-kernel source is exported only as finite Gram-matrix positivity, compact-operator shadowing, Hilbert/RKHS coordinate readback, Real eigenvalue-window data, and finite expansion-window budgets.
\end{theorem}
\begin{proof}
Begin with the positive-kernel source row $K$. A consumer may inspect it only by opening the finite matrix windows in $F$, where positivity is visible as displayed Gram-window data over Real entries.

The compact-operator row $O$ is then the only route from those finite windows to operator-facing reads. Thus an RKHS or Hilbert consumer cannot bypass the compact shadow with an unrestricted integral operator.

Read $R,L,E$ next. The Hilbert and RKHS coordinates are exposed through $R$, the eigenvalue approximants through $L$, and the finite Mercer-sum windows through $E$.

Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same displayed route. Because the carrier has no field for measure-theoretic integration, choice-selected eigenbases, host spectral theorem data, quotient functions, or ambient Real completeness, the positive-kernel handoff remains finite.
\end{proof}

\closureat{MercerTheoremUp}{seedStr}

\begin{closurestatus}{\MercerTheoremUp}
  \constructivestory{A $\MercerTheoremUp$ packet is generated from a compact positive-kernel source row, a CompactOperator shadow row, finite Matrix kernel windows, Hilbert and RKHS readback rows, Real eigenvalue windows, finite Mercer expansion windows, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:mercer-theorem-carrier}, \autoref{thm:mercer-theorem-namecert-obligations}, and \autoref{thm:mercer-theorem-positive-kernel-handoff} seed the finite positive-kernel Mercer route over compact operators, Hilbert and inner-product rows, matrix windows, and Real eigenvalue readbacks.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient measure theory, choice-selected eigenbases, unrestricted integral operators, the full spectral theorem, quotient function-space equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-kernel matrix-window coverage, CompactOperator shadow compatibility, Hilbert and RKHS readback exactness, Real eigenvalue-window stability, expansion-window nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MercerTheoremUp.}
\end{closurestatus}
