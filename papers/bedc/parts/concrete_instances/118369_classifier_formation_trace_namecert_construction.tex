\chapter{A Concrete Naming Certificate for $\ClassifierFormationTraceUp$}
\label{ch:concrete-instances-classifier-formation-trace-namecert}
\origin{ai}

The $\ClassifierFormationTraceUp$ packet realizes the learning classifier shift of \autoref{def:philosophy-learning-classifier-shift} as finite before-and-after classifier data. It refines the broader $\ClassifierFormingLearningUp$ route of \autoref{ch:concrete-instances-classifier-forming-learning-namecert} by naming the trace row where a learning transition exposes source scope, before classifier, after classifier, ledger residue, audit export, and local NameCert obligations. The packet records classifier formation as a finite trace; it does not claim model-family correctness, benchmark validity, causal explanation, positive net information, or metric improvement as learning by itself.

\begin{definition}[Classifier-formation trace carrier]
\label{def:classifier-formation-trace-carrier}
A $\ClassifierFormationTraceUp$ carrier is a finite $\BHist$ packet
$$
T=(S,B,A,L,R,G,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source-scope row naming the task, observations, training protocol, and common comparison domain for the before and after classifiers.
\item $B$ is the before-classifier row read from the pre-transition learning classifier state.
\item $A$ is the after-classifier row read from the post-transition learning classifier state.
\item $L$ is the classifier-shift ledger row, recording that $B$ and $A$ do not agree on the displayed common source scope.
\item $R$ is the learning-residue row for parameter motion, loss movement, benchmark evidence, memorization, shortcut behavior, and distribution-boundary residue.
\item $G$ is the registry and audit export row inherited from $\RegistryExportSurfaceUp$, $\MachineReadableAuditInterfaceUp$, and $\GroundCompilerAuditMapUp$.
\item $Q$ is the TasteGate commitment row requiring later Lean work to provide $\mathsf{Nontrivial}\ \ClassifierFormationTraceUp$ and $\mathsf{FieldFaithful}\ \ClassifierFormationTraceUp$ instances for this trace carrier.
\item $H$ records componentwise $\hsame$ transport for $S,B,A,L,R,G,Q$.
\item $C$ records displayed $\Cont$ replay from source scope through before classifier, after classifier, shift ledger, residue, audit export, TasteGate commitments, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\ClassifierFormationTraceUp$, $\ClassifierFormingLearningUp$, $\RegistryExportSurfaceUp$, $\MachineReadableAuditInterfaceUp$, $\GroundCompilerAuditMapUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\TasteGate$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ClassifierFormationTraceUp}$ row.
\end{enumerate}
The carrier compares accepted traces only by $S,B,A,L,R,G,Q,H,C,P,N$. Parameter motion, loss reduction, and benchmark movement remain residue rows unless $L$ displays classifier shift between the before and after rows on the same source scope.
\end{definition}

\begin{theorem}[Classifier-formation trace NameCert obligations]
\label{thm:classifier-formation-trace-namecert-obligations}
For every accepted $\ClassifierFormationTraceUp$ carrier $T=(S,B,A,L,R,G,Q,H,C,P,N)$, the local $\NameCert_{\ClassifierFormationTraceUp}$ surface consists exactly of these five obligation rows:
\begin{enumerate}
\item carrier admission for source scope, before classifier, after classifier, shift ledger, residue ledger, audit export, TasteGate commitments, transport, replay, provenance, and local naming;
\item before/after classifier exposure, requiring $B$ and $A$ to be read on the same displayed source scope $S$;
\item ledger-residue separation, requiring classifier shift through $L$ and keeping parameter motion, loss movement, benchmark evidence, memorization, shortcuts, and distribution residue in $R$;
\item audit and TasteGate handoff through $G,Q$, with nontriviality and field-faithfulness instances left as explicit Lean obligations for the trace carrier;
\item structural transport, replay, provenance, and naming nonescape through $H,C,P,N$.
\end{enumerate}
No obligation is discharged by parameter motion alone, lower loss alone, benchmark improvement alone, memorized examples, shortcut performance, or unregistered interpretability prose.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:classifier-formation-trace-carrier}. The source and classifier rows are $S,B,A$, while $L$ records the displayed classifier shift on the same scope.

The residue row $R$ is read separately. It may record parameter, loss, benchmark, memorization, shortcut, or distribution-boundary evidence, but none of those rows is the classifier-shift row.

The audit row $G$ and TasteGate row $Q$ make the trace exportable and record the later Lean obligations for nontriviality and field faithfulness. These commitments attach to the same finite carrier rather than to a private training narrative.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the excluded metric and prose substitutes have no carrier coordinate that can replace $L$, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Classifier-formation trace shift witness]
\label{thm:classifier-formation-trace-shift-witness}
Let $T=(S,B,A,L,R,G,Q,H,C,P,N)$ be an accepted $\ClassifierFormationTraceUp$ carrier. A learning transition exports classifier formation through $T$ only when the before classifier $B$ and after classifier $A$ are compared on the source row $S$, the shift ledger $L$ records nonagreement on that scope, the residue row $R$ keeps training side effects separate, and the audit and TasteGate rows $G,Q$ are displayed before local naming through $N$.

Thus the trace realizes the route from \autoref{def:philosophy-learning-classifier-shift} as finite NameCert data. It does not assert positive net explanation information, benchmark validity, causal explanation, field faithfulness before the later Lean instance, or nontriviality before the later Lean instance.
\end{theorem}
\begin{proof}
Begin with the accepted trace carrier. The source row $S$ fixes the common domain on which the before and after classifier rows can be compared.

The rows $B,A,L$ then form the classifier-shift witness. Without $L$, the transition remains a pair of classifier rows and does not export certified classifier formation.

The residue row $R$ keeps parameter motion, loss movement, benchmark evidence, memorization, shortcut behavior, and distribution residue outside the classifier-shift claim. The audit row $G$ and TasteGate row $Q$ preserve the public-checking and later-instance boundary.

Transport and replay through $H,C$ preserve the row order, and $P,N$ source and name the trace. Therefore the accepted packet realizes classifier shift as finite NameCert data without claiming the excluded metric, causal, positive-information, or completed TasteGate facts.
\end{proof}

\falsifiablePrediction{A $\ClassifierFormationTraceUp$ packet with before and after classifier rows but no same-scope shift ledger is rejected, even if it records parameter motion, lower loss, benchmark movement, memorization evidence, or audit prose.}

\independenceWitness{classifier-forming-learning, classifier-shift-ledger-residue, namecert, pkg, bhist: $\ClassifierFormationTraceUp$ is not bijective to its listed neighbors because it jointly carries source scope, before classifier, after classifier, classifier-shift ledger, residue separation, audit export, TasteGate commitments, transport, replay, provenance, and local naming as one finite trace.}

Later Lean TasteGate work must provide $\mathsf{Nontrivial}\ \ClassifierFormationTraceUp$ and $\mathsf{FieldFaithful}\ \ClassifierFormationTraceUp$ instances for the carrier of \autoref{def:classifier-formation-trace-carrier}. Those instances must preserve the rows $S,B,A,L,R,G,Q,H,C,P,N$ so classifier formation remains separated from training residue.

\closureat{ClassifierFormationTraceUp}{seedStr}

\begin{closurestatus}{\ClassifierFormationTraceUp}
  \constructivestory{A $\ClassifierFormationTraceUp$ packet is generated from source scope, before classifier, after classifier, classifier-shift ledger, learning-residue rows, registry and audit export, TasteGate commitments, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:classifier-formation-trace-carrier}, \autoref{thm:classifier-formation-trace-namecert-obligations}, and \autoref{thm:classifier-formation-trace-shift-witness} seed the finite trace route for learning classifier shift over before/after classifier rows, ledger residue, audit export, TasteGate commitments, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, model-family correctness, benchmark validity, causal explanation, positive net information, Nontrivial instances, FieldFaithful instances, or metric improvement as learning by itself.}
  \upgradepath{Obligation closure requires checked carrier admission, before/after classifier exposure, ledger-residue separation, audit export, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.ClassifierFormationTraceUp.}
\end{closurestatus}
