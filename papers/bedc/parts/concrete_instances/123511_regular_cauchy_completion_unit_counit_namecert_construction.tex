\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionUnitCounitUp$}
\label{ch:concrete-instances-regular-cauchy-completion-unit-counit-namecert}
\origin{human}

Following Bishop and Bridges and Mac Lane, the $\RegularCauchyCompletionUnitCounitUp$ packet records the finite unit, readback, and triangle rows by which the regular-Cauchy completion interface is consumed without importing a host adjunction theorem. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window source of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the extension-and-uniqueness route of \autoref{ch:concrete-instances-real-completion-universal-property-namecert}. The packet is a finite unit-counit triangle object; it does not assert quotient stream equality, selected limits, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy completion unit-counit packet]
\label{def:regular-cauchy-completion-unit-counit-packet}
A $\RegularCauchyCompletionUnitCounitUp$ carrier is a finite $\BHist$ packet
$$
U=(D,S,R,E,Q,K,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the regular-Cauchy source and completion readback.
\item $S$ is the $\StreamNameUp$ finite-window row naming the source approximants consumed by the unit.
\item $R$ is the $\RegSeqRatUp$ readback row for regular rational representatives before the terminal seal is exposed.
\item $E$ is the $\RealUp$ terminal seal row.
\item $Q$ is the unit row, sending a displayed source window through the completion insertion without quotienting streams.
\item $K$ is the counit or readback row, reading a completed regular-Cauchy name back through the visible $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows.
\item $L$ is the triangle ledger, containing the source triangle $KQ$ and the completion triangle $QK$ as finite row equalities mediated by displayed $\hsame$ transport.
\item $H$ records componentwise $\hsame$ transport for dyadic tolerance, finite stream windows, regular rational readback, terminal Real sealing, unit rows, counit rows, triangle rows, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a completion request through
$$
D,S,R,E,Q,K,L \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionUnitCounitUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RealCompletionUniversalPropertyUp$, $\RegularCauchyCompletionMonadUp$, $\CauchyCompletionMonadLawUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionUnitCounitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,R,E,Q,K,L,H,C,P,N$. It has no coordinate for quotient stream equality, selected representatives, countable-choice schedules, host adjunction equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy completion unit-counit obligations]
\label{thm:regular-cauchy-completion-unit-counit-obligations}
For every accepted $\RegularCauchyCompletionUnitCounitUp$ carrier
$$
U=(D,S,R,E,Q,K,L,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyCompletionUnitCounitUp}$ surface consists exactly of carrier admission, unit exposure, counit readback, triangle-row exposure, transport stability, replay stability, provenance, and local naming. Unit exposure reads $Q$ only after the dyadic, stream, regular-rational, and Real rows $D,S,R,E$ are displayed. Counit readback reads $K$ only through the same displayed rows. Triangle-row exposure reads the two triangle rows in $L$ through componentwise $\hsame$ transport and displayed $\Cont$ replay, and ledger nonescape excludes quotient equality, selected limits, countable choice, host adjunctions, and ambient completion principles.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-completion-unit-counit-packet}. The source side is visible through $D,S,R,E$, so the unit row $Q$ cannot be consumed before the dyadic, finite-window, regular-readback, and Real-seal rows have been displayed.

The readback side is the row $K$. It uses the same displayed source and seal rows, so a counit consumer receives finite readback data rather than quotient representatives or selected limits.

The triangle ledger is $L$. Its two rows are read only after $Q$ and $K$ have been displayed, and $H,C$ transport and replay those rows componentwise. Provenance and local naming through $P,N$ preserve the same packet, so the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy completion unit-counit triangle]
\label{thm:regular-cauchy-completion-unit-counit-triangle}
Every completion-facing consumer of an accepted $\RegularCauchyCompletionUnitCounitUp$ packet reads the unit-counit triangle through the finite route
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp,\quad \RealCompletionUniversalPropertyUp,\quad \RegularCauchyCompletionMonadUp,\quad \CauchyCompletionMonadLawUp,\quad \BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert .
$$
The source triangle first displays $D,S,R,E,Q,K$ and then the corresponding row of $L$; the completion triangle first displays $D,S,R,E,K,Q$ and then the other row of $L$. The export is exactly this finite triangle ledger and does not export quotient stream equality, selected representatives, countable choice, host adjunction equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:regular-cauchy-completion-unit-counit-obligations}. It places both unit and counit reads downstream of the dyadic tolerance, stream-window, regular-rational, and terminal Real rows.

Read the source triangle first. The route exposes the source rows and the unit row $Q$, then reads back through $K$, and only then consumes the source triangle entry of $L$.

Read the completion triangle second. The route exposes the same source and seal rows, reads the counit row $K$, then the unit row $Q$, and finally the completion triangle entry of $L$.

The structural rows $H,C,P,N$ transport, replay, source, and name those two finite rows. Since the carrier contains no quotient equality, selected representative, countable-choice schedule, host adjunction equality, or ambient completeness coordinate, the triangle export is exactly the finite ledger stated above.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyCompletionUnitCounitUp$ consumer that reads a unit or counit row must display DyadicRatCoreUp tolerance, StreamNameUp finite-window, RegSeqRatUp readback, RealUp seal, unit, counit, triangle, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, real-completion-universal-property, regular-cauchy-completion-monad, cauchy-completion-monad-law: none is bijective to $\RegularCauchyCompletionUnitCounitUp$ because this packet jointly carries the unit row, counit readback row, two triangle rows, transport, replay, provenance, and local naming in one finite completion-facing carrier.}

\closureat{RegularCauchyCompletionUnitCounitUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionUnitCounitUp}
  \constructivestory{A $\RegularCauchyCompletionUnitCounitUp$ packet is generated from DyadicRatCore tolerance rows, StreamName finite windows, RegSeqRat readback, a Real seal, a unit row, a counit readback row, two triangle rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-unit-counit-packet}, \autoref{thm:regular-cauchy-completion-unit-counit-obligations}, and \autoref{thm:regular-cauchy-completion-unit-counit-triangle} seed the finite completion unit-counit triangle route over dyadic tolerance, finite stream windows, regular rational readback, Real sealing, universal-property provenance, transport, replay, provenance, and local naming rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient stream equality, selected limits, countable choice, host adjunction equality, monad laws beyond the displayed rows, or ambient $\RealUp$ completeness.}
  \upgradepath{Formalization requires checked carrier admission, unit exposure, counit readback, triangle-row exposure, transport stability, replay stability, provenance, and local naming for BEDC.Derived.RegularCauchyCompletionUnitCounitUp.}
\end{closurestatus}
\leanstmt{BEDC.Derived.RegularCauchyCompletionUnitCounitUp}
