\chapter{A Concrete Naming Certificate for $\FixedBoundaryHyperbolicShadowUp$}
\label{ch:concrete-instances-fixed-boundary-hyperbolic-shadow-namecert}
\origin{ai}

The $\FixedBoundaryHyperbolicShadowUp$ packet realizes the fixed-boundary shadow branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the boundary-fixed transport route of \autoref{ch:concrete-instances-poincare-disk-boundary-transport-namecert}, the boundary-shadow carrier of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the metric readback of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the Busemann boundary row of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, and the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}. The packet names a finite shadow window whose boundary far-end is held fixed by displayed transport; it is not an interior-center fixed point, selected ideal endpoint, quotient boundary equality, physical observer motion, or completed hyperbolic compactification.

\begin{definition}[Fixed-boundary hyperbolic shadow carrier]
\label{def:fixed-boundary-hyperbolic-shadow-carrier}
A $\FixedBoundaryHyperbolicShadowUp$ carrier is a finite $\BHist$ packet
$$
S=(T,B,M,U,V,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\PoincareDiskBoundaryTransportUp$ row preserving the selected boundary commitment along the displayed disk route.
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row naming the finite shadow window attached to that boundary commitment.
\item $M$ is the $\HyperbolicMetricUp$ row used for disk and shadow distance readback.
\item $U$ is the $\HyperbolicBusemannBoundaryUp$ row recording the Busemann-facing far-end comparison.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ or $\GromovBoundaryUp$ readback row that exposes the public boundary token.
\item $W$ is the fixed-boundary witness row, asserting that the boundary token read by $V$ is preserved by $T$ while the shadow window $B$ is transported.
\item $E$ is the refusal and exactness row excluding interior-center fixed points, selected ideal endpoints, quotient boundary equality, physical observer motion, and global compactification.
\item $H$ records componentwise $\hsame$ transport for transport, shadow, metric, Busemann, visual-boundary, witness, refusal, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from boundary transport through shadow readback, metric comparison, Busemann comparison, fixed-boundary witness, refusal, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\FixedBoundaryHyperbolicShadowUp$, $\PoincareDiskBoundaryTransportUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicMetricUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicVisualBoundaryUp$, $\GromovBoundaryUp$, $\HyperbolicObserverExpansionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FixedBoundaryHyperbolicShadowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,B,M,U,V,W,E,H,C,P,N$. It has no coordinate for a fixed interior disk center, selected ideal endpoint, quotient boundary equality, physical observer trajectory, completed compactification, or unlogged global hyperbolic model.
\end{definition}

\begin{theorem}[Fixed-boundary hyperbolic shadow NameCert obligations]
\label{thm:fixed-boundary-hyperbolic-shadow-namecert-obligations}
For every accepted $\FixedBoundaryHyperbolicShadowUp$ carrier
$$
S=(T,B,M,U,V,W,E,H,C,P,N),
$$
the local $\NameCert_{\FixedBoundaryHyperbolicShadowUp}$ surface consists exactly of boundary-transport admission, boundary-shadow admission, hyperbolic metric readback, Busemann-boundary comparison, public boundary-token readback, fixed-boundary witness, refusal exactness, componentwise transport, displayed replay, provenance, and local naming. No obligation is stored outside $T,B,M,U,V,W,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fixed-boundary-hyperbolic-shadow-carrier}. The transport row $T$ and shadow row $B$ give the finite route whose boundary commitment is being tested.

The metric and boundary readback rows are downstream. Rows $M,U,V$ expose the hyperbolic metric comparison, the Busemann-facing far-end comparison, and the public boundary token before any fixedness claim is read.

The fixed-boundary content is exactly row $W$. It states preservation of the displayed boundary token along the transported shadow window, while $E$ records the refusals needed to keep the reading from becoming an interior-center or quotient-boundary claim.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier contains no fixed interior center, selected ideal endpoint, quotient boundary equality, physical observer trajectory, completed compactification, or unlogged global model, none can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Fixed-boundary hyperbolic shadow boundary route]
\label{thm:fixed-boundary-hyperbolic-shadow-boundary-route}
Every boundary-shadow consumer of an accepted $\FixedBoundaryHyperbolicShadowUp$ carrier factors through
$$
\PoincareDiskBoundaryTransportUp,\HyperbolicBoundaryShadowUp,\HyperbolicMetricUp,\HyperbolicBusemannBoundaryUp
\longmapsto
V,W,E
$$
together with $H,C,P,N$. The route exports only a finite transported shadow whose public boundary token is preserved. It does not export an interior fixed point, selected ideal endpoint, quotient boundary equality, physical observer motion, completed compactification, or host-level boundary object.
\end{theorem}
\begin{proof}
Begin with the boundary-transport and shadow rows. The transport row $T$ records the fixed boundary commitment, and the shadow row $B$ records which finite shadow window is being moved.

Read the metric and Busemann comparisons next. Rows $M$ and $U$ expose the hyperbolic readbacks needed to interpret the shadow as a boundary-facing packet, and $V$ supplies the public boundary token.

The preservation claim is then exactly $W$, constrained by the refusal row $E$. The structural rows $H,C,P,N$ keep the exported route attached to the same finite transport, shadow, metric, Busemann, boundary-token, and witness rows.

Thus any consumer bypassing $T,B,M,U,V,W,E,H,C,P,N$ is not reading the accepted fixed-boundary shadow packet.
\end{proof}

\falsifiablePrediction{Every accepted $\FixedBoundaryHyperbolicShadowUp$ read is refuted if it exports fixed-boundary shadow data without displaying $\PoincareDiskBoundaryTransportUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicMetricUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicVisualBoundaryUp$ or $\GromovBoundaryUp$, the fixed-boundary witness, refusal exactness, componentwise $\hsame$ transport, $\Cont$ replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{poincare-disk-boundary-transport, hyperbolic-boundary-shadow, hyperbolic-metric, hyperbolic-busemann-boundary, hyperbolic-observer-expansion: none is bijective to $\FixedBoundaryHyperbolicShadowUp$ because this packet alone binds boundary-fixed transport, finite shadow windows, metric comparison, Busemann readback, public boundary-token preservation, refusal exactness, and observer-expansion provenance into one finite surface.}

\closureat{FixedBoundaryHyperbolicShadowUp}{seedStr}

\begin{closurestatus}{\FixedBoundaryHyperbolicShadowUp}
  \constructivestory{A $\FixedBoundaryHyperbolicShadowUp$ packet is generated from PoincareDiskBoundaryTransport, HyperbolicBoundaryShadow, HyperbolicMetric, HyperbolicBusemannBoundary, HyperbolicVisualBoundary or GromovBoundary, HyperbolicObserverExpansion, a fixed-boundary witness, refusal exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fixed-boundary-hyperbolic-shadow-carrier}, \autoref{thm:fixed-boundary-hyperbolic-shadow-namecert-obligations}, and \autoref{thm:fixed-boundary-hyperbolic-shadow-boundary-route} seed the finite fixed-boundary shadow route for hyperbolic observer-expansion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close interior fixed points, selected ideal endpoints, quotient boundary equality, physical observer motion, completed compactification, host-level boundary objects, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary transport preservation, shadow admission, metric readback, Busemann comparison, public boundary-token readback, fixed-boundary witness exactness, refusal coverage, transport, replay, provenance, and local naming for BEDC.Derived.FixedBoundaryHyperbolicShadowUp.}
\end{closurestatus}
