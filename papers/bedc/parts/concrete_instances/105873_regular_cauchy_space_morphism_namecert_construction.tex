\chapter{A Concrete Naming Certificate for $\RegularCauchySpaceMorphismUp$}
\label{ch:concrete-instances-regular-cauchy-space-morphism-namecert}
\origin{human}

Following Bishop and Bridges on uniformly continuous maps between Cauchy presentations, the $\RegularCauchySpaceMorphismUp$ packet records the finite morphism surface between regular Cauchy sequence spaces before any completion equality or limiting representative is read. It sits after the source-space surface of \autoref{ch:concrete-instances-regular-cauchy-sequence-space-namecert}, the finite window source of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the Cauchy-name-space packet of \autoref{ch:concrete-instances-cauchysequencespace-namecert}, and the completion-functor route of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}. The packet is the L10 finite morphism interface consumed by completion-functor and Real-facing handoff chapters; it is not a proof of ambient completeness, quotient equality of streams, selected limits, or host equality of real names.

\begin{definition}[RegularCauchySpaceMorphism carrier]
\label{def:regular-cauchy-space-morphism-carrier}
A $\RegularCauchySpaceMorphismUp$ carrier is a finite $\BHist$ packet
$$
M=(S,T,A,W,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source $\RegularCauchySequenceSpaceUp$ row, including the source regular rational family, rate ledger, finite schedule, dyadic tolerance route, CauchySequenceSpace handoff, and Real seal.
\item $T$ is the target $\RegularCauchySequenceSpaceUp$ row, carrying the same visible sequence-space structure for the codomain presentation.
\item $A$ is the finite-window action row assigning each accepted source window request to a displayed target window request.
\item $W$ is the dyadic budget ledger recording the input precision, output precision, and finite modulus rows used by $A$.
\item $D$ is the classifier-transport row, binding source acceptance to target acceptance through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchySequenceSpaceUp$, and $\RealUp$ sibling routes.
\item $H$ records componentwise $\hsame$ transport for source space, target space, action, budget ledger, classifier transport, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each morphism request through the source finite window, action row, dyadic budget ledger, target finite window, target CauchySequenceSpace handoff, and target Real seal.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchySpaceMorphismUp$, $\RegularCauchySequenceSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchySequenceSpaceUp$, $\CauchyCompletionFunctorUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchySpaceMorphismUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,T,A,W,D,H,C,P,N$. It has no coordinate for quotient stream equality, selected source or target limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[RegularCauchySpaceMorphism NameCert obligations]
\label{thm:regular-cauchy-space-morphism-namecert-obligations}
For every accepted $\RegularCauchySpaceMorphismUp$ carrier $M=(S,T,A,W,D,H,C,P,N)$, the local $\NameCert_{\RegularCauchySpaceMorphismUp}$ surface consists exactly of carrier admission, source-target compatibility, finite-window exactness, rate-ledger preservation, classifier transport, non-escape, displayed replay, provenance, and local naming.

Source-target compatibility requires both $S$ and $T$ to be accepted $\RegularCauchySequenceSpaceUp$ packets. Finite-window exactness requires $A$ to transform only displayed source windows into displayed target windows. Rate-ledger preservation requires $W$ to expose the dyadic input and output budget rows before any target seal is read. Classifier transport requires $D$ to preserve the visible source and target classifiers through the same sibling routes named in the carrier. Non-escape refuses quotient stream equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, and bridge checking as certificate data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-space-morphism-carrier}. The rows $S$ and $T$ supply the source and target regular Cauchy sequence spaces, while $A$ supplies only a displayed finite-window action between them.

Next read the dyadic budget and classifier transport rows. The budget ledger $W$ records the precision rows consumed by the action, and $D$ binds acceptance of the source and target windows through the named StreamName, RegSeqRat, DyadicRatCore, CauchySequenceSpace, and Real sibling routes.

Finally attach $H,C,P,N$. These rows preserve and replay the same finite morphism packet, so every local naming obligation is exhausted by the displayed tuple. Since the tuple has no quotient, selected-limit, countable-choice, host-equality, completeness, verification, or bridge coordinate, the refused objects cannot be exported by the NameCert surface.
\end{proof}

\begin{theorem}[RegularCauchySpaceMorphism finite-window exactness]
\label{thm:regular-cauchy-space-morphism-finite-window-exactness}
Every accepted $\RegularCauchySpaceMorphismUp$ read is exhausted by the displayed source and target regular Cauchy sequence-space rows, the finite-window action row, the dyadic budget ledger, classifier transport, componentwise transport, replay, provenance, and local naming:
$$
S,T,A,W,D,H,C,P,N.
$$
Thus the morphism exports a finite L10 action between regular Cauchy sequence spaces. It exports no quotient stream equality, selected source limit, selected target limit, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from an accepted carrier $M=(S,T,A,W,D,H,C,P,N)$. The only source-space and target-space data available to the consumer are the displayed rows $S$ and $T$, each read as a $\RegularCauchySequenceSpaceUp$ packet.

Use \autoref{thm:regular-cauchy-space-morphism-namecert-obligations}. It places the action $A$ behind finite windows and places the precision behavior behind the dyadic ledger $W$ and classifier transport row $D$.

The structural rows $H,C,P,N$ then transport, replay, source, and name only this route. No remaining coordinate can supply quotient stream equality, selected limits, countable choice, host real equality, or ambient real completeness, so every accepted morphism read is finite-window exact in the displayed sense.
\end{proof}

\closureat{RegularCauchySpaceMorphismUp}{seedStr}

\begin{closurestatus}{\RegularCauchySpaceMorphismUp}
  \constructivestory{A $\RegularCauchySpaceMorphismUp$ packet is generated from source and target RegularCauchySequenceSpaceUp rows, a finite-window action, a dyadic budget ledger, classifier transport, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-space-morphism-carrier}, \autoref{thm:regular-cauchy-space-morphism-namecert-obligations}, and \autoref{thm:regular-cauchy-space-morphism-finite-window-exactness} seed the finite morphism surface between regular Cauchy sequence spaces.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-target compatibility, finite-window exactness, rate-ledger preservation, classifier transport, non-escape, replay, provenance, local naming, and TasteGate field faithfulness for BEDC.Derived.RegularCauchySpaceMorphismUp.}
\end{closurestatus}
