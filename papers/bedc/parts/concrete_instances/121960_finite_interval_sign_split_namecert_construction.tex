\chapter{A Concrete Naming Certificate for $\FiniteIntervalSignSplitUp$}
\label{ch:concrete-instances-finite-interval-sign-split-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\FiniteIntervalSignSplitUp$ packet records the finite sign-split data used by constructive bisection before any limiting root object is named. It refines the sign-change bracket surface of \autoref{ch:concrete-instances-real-bolzano-namecert} and the contraction-facing interval route of \autoref{ch:concrete-instances-bishop-interval-contraction-namecert} into a single NameCert carrier for endpoint Real seals, located signs, dyadic midpoint budgets, retained subintervals, StreamName windows, RegSeqRat readback, componentwise transport, displayed replay, provenance, and local naming. The packet is a finite handoff object; it is not classical intermediate value, real trichotomy, quotient equality, countable choice, a selected infinite branch, or ambient $\RealUp$ completeness.

\begin{definition}[Finite interval sign-split carrier]
\label{def:finite-interval-sign-split-carrier}
A $\FiniteIntervalSignSplitUp$ carrier is a finite $\BHist$ packet
$$
S=(a,b,L,U,D,M,T,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $a$ and $b$ are endpoint $\RealUp$ seal rows read only through finite regular-rational windows.
\item $L$ and $U$ are located sign witnesses at the endpoint windows, with opposed sign evidence displayed at the requested precision.
\item $D$ is the $\DyadicRatCoreUp$ interval ledger storing endpoint dyadics, width comparisons, tolerance radii, and midpoint requests.
\item $M$ is the midpoint/budget row, recording the finite bisection query and the dyadic budget consumed by the inspected midpoint.
\item $T$ is the retained subinterval row, naming the left or right half whose endpoint signs still form an opposed located pair.
\item $W$ is the $\StreamNameUp$ window row for the inspected endpoint and midpoint data.
\item $R$ is the $\RegSeqRatUp$ readback assembled from the retained subinterval windows and the same dyadic ledger.
\item $E$ is the terminal pair of $\RealUp$ seals reached only after $a,b,L,U,D,M,T,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for endpoint, sign, dyadic, midpoint, retained-subinterval, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the sign-split request through $a,b,L,U,D,M,T,W,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteIntervalSignSplitUp$, $\RealBolzanoUp$, $\BishopIntervalContractionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteIntervalSignSplitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for real trichotomy, a selected zero, quotient real equality, countable choice, an infinite bisection branch, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite interval sign-split NameCert obligations]
\label{thm:finite-interval-sign-split-namecert-obligations}
For every accepted $\FiniteIntervalSignSplitUp$ carrier
$$
S=(a,b,L,U,D,M,T,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\FiniteIntervalSignSplitUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for endpoint Real seals, located sign witnesses, dyadic interval data, midpoint budget, retained subinterval, StreamName windows, RegSeqRat readback, terminal Real seals, transport, replay, provenance, and local naming;
\item endpoint sign exposure, requiring $L,U$ to be read from $a,b$ as displayed located apartness data before any midpoint is inspected;
\item dyadic midpoint exactness, requiring $D,M$ to record the same finite width, midpoint, tolerance, and budget rows used by the split;
\item retained-subinterval stability, requiring $T$ to keep one opposed located sign pair and to stay inside the displayed endpoint interval;
\item window and readback routing, requiring $W,R,E$ to be assembled from the retained subinterval and dyadic budget rows rather than from a selected root;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming through $H,C,P,N$ for the same rows;
\item refusal of classical intermediate value, real trichotomy, quotient equality, selected roots, countable choice, infinite branches, and ambient $\RealUp$ completeness as certificate data.
\end{enumerate}
No finite sign-split obligation is stored outside $a,b,L,U,D,M,T,W,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-interval-sign-split-carrier}. The endpoint rows $a,b$ and the located sign witnesses $L,U$ are the only sources of sign data, and both are read at displayed finite precision.

The split itself is dyadic. The ledger $D$ and midpoint row $M$ record the width, midpoint, tolerance, and budget data before the retained subinterval $T$ is admitted.

The completion-facing rows are downstream of the retained subinterval. The window row $W$ and readback row $R$ are assembled from $T,D,M$, and the terminal Real seals $E$ are read only after that finite route is present.

Finally $H,C,P,N$ transport, replay, source, and name the same packet rows. Since the carrier contains no excluded order, quotient, choice, branch, root-selection, or completeness coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite interval sign-split bisection handoff]
\label{thm:finite-interval-sign-split-bisection-handoff}
Every bisection-facing consumer of an accepted $\FiniteIntervalSignSplitUp$ packet must read the finite route
$$
a,b,L,U,D,M \longmapsto T,W,R,E \longmapsto H,C,P,N
$$
before it can treat a retained half-interval as sign-change evidence. The handoff supplies endpoint Real seals, located sign witnesses, dyadic midpoint and budget rows, a retained subinterval, finite StreamName windows, RegSeqRat readback, componentwise transport, displayed continuation replay, provenance, and local naming. It supplies no unrestricted intermediate-value theorem, selected root, real trichotomy, quotient real equality, countable choice, infinite branch, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:finite-interval-sign-split-namecert-obligations}. They force endpoint sign exposure through $a,b,L,U$ and dyadic midpoint exactness through $D,M$ before a retained subinterval can be consumed.

Apply the RealBolzano sign-change bisection route of \autoref{thm:real-bolzano-sign-change-bisection}. The retained half may be exported only as opposed located sign data inside a displayed bracket, not as a selected root or unrestricted intermediate-value result.

The StreamName and RegSeqRat rows then carry the retained half to completion-facing consumers. This is the same finite kind of Cauchy handoff isolated by \autoref{thm:bishop-interval-contraction-cauchy-handoff}, with $W,R,E$ downstream of the dyadic and sign rows.

The structural rows $H,C,P,N$ preserve that route for later consumers. Since none of the cited rows contains real trichotomy, quotient real equality, countable choice, an infinite branch, selected roots, or ambient Real completeness, the handoff remains the finite bisection surface stated above.
\end{proof}

\falsifiablePrediction{An accepted $\FiniteIntervalSignSplitUp$ consumer that reaches a retained subinterval, $\RegSeqRatUp$ readback, or terminal $\RealUp$ seal without reading $a,b,L,U,D,M,T,W,R,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{real-bolzano, bishop-interval-contraction, dyadicratcore, streamname, regseqrat, real: no listed sibling supplies exactly the joint packet containing endpoint Real seals, located sign witnesses, dyadic midpoint budgets, a retained subinterval, finite windows, regular-rational readback, terminal Real seals, transport, replay, provenance, and local NameCert row.}

\closureat{FiniteIntervalSignSplitUp}{seedStr}

\begin{closurestatus}{\FiniteIntervalSignSplitUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-interval-sign-split-carrier}, \autoref{thm:finite-interval-sign-split-namecert-obligations}, and \autoref{thm:finite-interval-sign-split-bisection-handoff} seed the constructive finite sign-split route from endpoint Real seals and located sign witnesses to retained subinterval, RegSeqRat readback, and terminal Real-seal consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim classical intermediate value, real trichotomy, quotient real equality, selected roots, countable choice, infinite branches, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint sign exposure, dyadic midpoint exactness, retained-subinterval stability, window readback routing, transport, replay, provenance, and local naming for BEDC.Derived.FiniteIntervalSignSplitUp.}
  \constructivestory{A $\FiniteIntervalSignSplitUp$ packet is generated from endpoint Real seals, located sign witnesses, dyadic midpoint and budget rows, a retained subinterval row, StreamName finite windows, RegSeqRat readback, terminal Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
