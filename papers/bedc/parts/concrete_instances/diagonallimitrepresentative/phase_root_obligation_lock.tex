\begin{theorem}[Diagonal-limit representative terminal-modulus scope lock]
\label{thm:diagonal-limit-representative-terminal-modulus-scope-lock}
For every accepted $\DiagonalLimitRepresentativeUp$ terminal route consumed by a Real-and-Completion endpoint, the terminal modulus route is scoped precisely by the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DiagonalLimitRepresentativeUp}$ rows together with the sibling $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows. In particular, the route available to the consumer is the terminal-modulus packet
$$
(\mu,n,k,i,w,r,d,H,C,P,N)
$$
of \autoref{def:diagonal-limit-representative-terminal-modulus-coherence-surface}, read through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}; no off-packet selector, host Real equality, quotient stream equality, countable-choice row, or ambient completeness field lies in this scope.
\end{theorem}
\begin{proof}
Start from the carrier and classifier of \autoref{def:diagonal-limit-representative-carrier} and \autoref{def:diagonal-limit-representative-classifier}. The carrier exposes only finite route rows, while classifier transport is recorded componentwise by the $\hsame$ and $\Cont$ support used by the local route.

Apply the NameCert obligation surface through \autoref{thm:diagonal-limit-representative-namecert-obligations}, \autoref{thm:diagonal-limit-representative-namecert-ledger}, and \autoref{thm:diagonal-limit-representative-pkg-namecert-source-closure}. These results identify carrier admission, displayed replay, provenance, and local naming as the complete support by which the terminal route can be read.

Finally attach terminal-modulus coherence, \autoref{thm:diagonal-limit-representative-terminal-modulus-coherence}. It factors the terminal seal through the same packet $(\mu,n,k,i,w,r,d,H,C,P,N)$ and ties the four sibling rows to the DyadicRatCore, StreamName, RegSeqRat, and Real chapters. Therefore the terminal modulus route is locked to the stated finite scope.
\end{proof}

\begin{theorem}[Diagonal-limit representative cofinal-tail exactness lock]
\label{thm:diagonal-limit-representative-cofinal-tail-exactness-lock}
Every cofinal tail read accepted by a $\DiagonalLimitRepresentativeUp$ terminal route is exact only through the finite selector
$$
(\mu,n,k,i,w,r,d)
$$
with $k=\mu(1/(n+1))$ and $i=k+n+1$. The read uses $w$ as the displayed $\StreamNameUp$ request, $r$ as the displayed $\RegSeqRatUp$ readback, and $d$ as the displayed $\DyadicRatCoreUp$ tolerance ledger; it does not introduce quotient-stream equality, a hidden tail selector, countable choice, host-limit selection, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the finite selector from the modulus-window-readback package and the terminal diagonal selector surface, \autoref{thm:diagonal-limit-representative-modulus-window-readback-package} and \autoref{thm:diagonal-limit-representative-terminal-diagonal-coverage}. They provide the displayed computations of $k$ and $i$ and place the selected window before the regular-rational readback.

Next use cofinal-tail coverage and exactness, \autoref{thm:diagonal-limit-representative-cofinal-tail-coverage} and \autoref{thm:diagonal-limit-representative-cofinal-tail-namecert-exactness}. These theorems keep the cofinal read inside the finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ route rather than allowing a detached tail principle.

The nonescape side follows from terminal-modulus coherence and selector-modulus nonescape, \autoref{thm:diagonal-limit-representative-terminal-modulus-coherence} and \autoref{thm:diagonal-limit-representative-selector-modulus-nonescape}. Since the accepted packet has no coordinate for quotient equality, hidden selection, choice, host limits, or ambient completeness, the cofinal tail read is exact only through the displayed selector.
\end{proof}

\begin{theorem}[Diagonal-limit representative phase-exit handoff lock]
\label{thm:diagonal-limit-representative-phase-exit-handoff-lock}
The packet obtained from the terminal-modulus scope lock and the cofinal-tail exactness lock is the $\DiagonalLimitRepresentativeUp$ Phase Real and Completion handoff consumed by $\PhaseRealCompletionExitUp$. Its consumer-facing source rows are exactly $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with the support route still carried by $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DiagonalLimitRepresentativeUp}$; the handoff exports no quotient stream, selected completed point, countable-choice row, host Real limit, or ambient completion principle.
\end{theorem}
\begin{proof}
First assemble the local package. The terminal-modulus scope lock supplies the finite route support, while the cofinal-tail exactness lock supplies the selected window, readback, and dyadic ledger through the displayed selector.

Now apply the existing Phase Real and Completion surfaces: phase-source coverage, four-object source exhaustion, and the Phase Real and Completion exit certificate, \autoref{thm:diagonal-limit-representative-phase-source-coverage}, \autoref{thm:diagonal-limit-representative-four-object-source-exhaustion}, and \autoref{thm:diagonal-limit-representative-phase-real-completion-exit-certificate}. They identify the consumer-facing source inventory with the four DyadicRatCore, StreamName, RegSeqRat, and Real rows.

Finally compare with the $\PhaseRealCompletionExitUp$ carrier and obligation surface, \autoref{def:phase-real-completion-exit-carrier} and \autoref{thm:phase-real-completion-exit-obligation-surface-package}. That carrier consumes the same objectwise rows through finite support and rejects fused completion data, quotient streams, selected limits, and host-completeness shortcuts. Hence the resulting packet is precisely the handoff available to the Phase Real and Completion exit consumer.
\end{proof}
