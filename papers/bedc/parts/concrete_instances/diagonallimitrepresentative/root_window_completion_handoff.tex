\begin{theorem}[Diagonal-limit representative root-window extraction]
\label{thm:diagonal-limit-representative-root-window-extraction}
Every accepted completion-selector ledger for $\DiagonalLimitRepresentativeUp$ determines the finite five-row root-window package
$$
\Omega=(W,R,D,M,E)
$$
where $W$ is the selected $\StreamNameUp$ window supplied by \autoref{ch:concrete-instances-streamname-namecert}, $R$ is the selected $\RegSeqRatUp$ readback supplied by \autoref{ch:concrete-instances-regseqrat-namecert}, $D$ is the $\DyadicRatCoreUp$ ledger supplied by \autoref{ch:concrete-instances-dyadicratcore-namecert}, $M$ is the $\CofinalModulusSealUp$ budget row supplied by \autoref{ch:concrete-instances-cofinal-modulus-seal-namecert}, and $E$ is the terminal $\RealUp$ seal supplied by \autoref{ch:concrete-instances-real-namecert}. The package is extracted from the displayed carrier, cofinal bridge, and completion-selector rows; it carries no host limit, quotient stream equality, hidden tail selector, countable-choice row, or ambient $\RealUp$ completeness field.
\end{theorem}
\begin{proof}
Project the completion-selector ledger of \autoref{def:diagonal-limit-representative-completion-selector-ledger}. Its first block exposes the selected window $w$, readback $r$, dyadic ledger $d$, and terminal seal $e$, while its cofinal block exposes the modulus-seal budget rows.

Use the sibling identifications already recorded by the ledger. The selected window is sourced by $\StreamNameUp$, the readback by $\RegSeqRatUp$, the tolerance ledger by $\DyadicRatCoreUp$, the cofinal budget by $\CofinalModulusSealUp$, and the terminal seal by $\RealUp$ through the five chapter references in the statement.

Finally apply completion handoff exhaustion, \autoref{thm:diagonal-limit-representative-completion-handoff-exhaustion}. It keeps every Real-and-Completion consumer inside the displayed finite rows, so repacking them as $\Omega$ introduces no off-packet limit, equality, selector, choice, or completeness datum.
\end{proof}

\begin{theorem}[Diagonal-limit representative regularity handoff]
\label{thm:diagonal-limit-representative-regularity-handoff}
For every root-window package $\Omega=(W,R,D,M,E)$ extracted by \autoref{thm:diagonal-limit-representative-root-window-extraction}, the regularity handoff to a downstream completion consumer factors first through the $\StreamNameUp$ window $W$ of \autoref{ch:concrete-instances-streamname-namecert} and then through the $\RegSeqRatUp$ readback $R$ of \autoref{ch:concrete-instances-regseqrat-namecert}. The $\DyadicRatCoreUp$ ledger $D$, $\CofinalModulusSealUp$ budget $M$, and $\RealUp$ seal $E$ may only be consumed after that window-readback route is displayed.
\end{theorem}
\begin{proof}
Begin with the root-window extraction theorem. It presents all five rows as one finite packet, so the downstream consumer reads a displayed root window rather than a detached completion request.

Now apply the StreamName--RegSeqRat handoff, \autoref{thm:diagonal-limit-representative-streamname-regseqrat-handoff}. That theorem orders the selected window before the selected regular-rational readback inside the representative carrier.

The remaining rows are downstream of the same route. The dyadic ledger records the readback tolerance, the cofinal budget records the admissible tail budget, and the Real seal is terminal. Hence none of $D,M,E$ can be consumed before the $W \to R$ regularity route has been displayed.
\end{proof}

\begin{theorem}[Diagonal-limit representative Real-seal admissibility]
\label{thm:diagonal-limit-representative-real-seal-admissibility}
For every extracted root-window package $\Omega=(W,R,D,M,E)$, the terminal $\RealUp$ row $E$ is admissible for Real-and-Completion consumers exactly as the finite seal reached after the $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ ledger $D$, and $\CofinalModulusSealUp$ budget $M$. This admissibility cites \autoref{ch:concrete-instances-real-namecert} only as a displayed seal surface; it does not cite quotient equality, host Real equality, a chosen completed point, or ambient Real completeness.
\end{theorem}
\begin{proof}
Read the positive seal route from Real handoff exactness, \autoref{thm:diagonal-limit-representative-real-handoff-exactness}. It identifies the Real-facing endpoint with the terminal seal row reached after the selected window, readback, and dyadic ledger.

Add the cofinal budget row from the extracted package. The budget is already part of the completion-selector ledger and is transported through the cofinal-terminal bridge, so it constrains the terminal handoff without creating a new Real object.

The admissibility claim is therefore only the displayed seal claim. The root-window package contains $E$ as a finite $\BHist$ coordinate with $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support; absent quotient, host equality, chosen-point, choice, and completeness rows cannot be used by the consumer.
\end{proof}

\begin{theorem}[Diagonal-limit representative cofinal-budget stability]
\label{thm:diagonal-limit-representative-cofinal-budget-stability}
Let $\Omega=(W,R,D,M,E)$ and $\Omega'=(W',R',D',M',E')$ be extracted root-window packages for accepted completion-selector ledgers classified together by the recorded $\hsame$ rows and displayed $\Cont$ replay. Then the cofinal budget rows $M$ and $M'$ remain on the same $\CofinalModulusSealUp$ route, and the associated $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows are preserved componentwise as the same root-window handoff.
\end{theorem}
\begin{proof}
Start with completion-selector stability, \autoref{thm:diagonal-limit-representative-completion-selector-stability}. It preserves the representative rows, finite-prefix rows, cofinal modulus rows, terminal seal rows, provenance rows, and local naming rows under the recorded transport and replay data.

Project the two extracted root-window packages. Since $\Omega$ and $\Omega'$ are obtained only by projection from the corresponding ledgers, the $\hsame$ and $\Cont$ rows that classify the ledgers also classify $W,W'$, $R,R'$, $D,D'$, $M,M'$, and $E,E'$.

It remains to identify the cofinal budget route. The cofinal-terminal bridge keeps the cofinal modulus budget in the same finite handoff as the terminal Real seal, so stability of the cofinal row preserves the entire root-window package componentwise.
\end{proof}

\begin{theorem}[Diagonal-limit representative terminal-selector exactness]
\label{thm:diagonal-limit-representative-terminal-selector-exactness}
Every terminal selector read made by a Real-and-Completion consumer of $\DiagonalLimitRepresentativeUp$ factors through the same extracted root-window package $\Omega=(W,R,D,M,E)$. Thus the consumer reads the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CofinalModulusSealUp$, and $\RealUp$ rows, together with the existing $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DiagonalLimitRepresentativeUp}$ support, and no second selector route can supply the terminal seal.
\end{theorem}
\begin{proof}
Use root-window extraction to obtain $\Omega$ from the accepted completion-selector ledger. The extraction has already gathered the finite window, readback, dyadic ledger, cofinal budget, and terminal seal rows into one displayed package.

Next apply phase-source coverage and the Phase Real and Completion exit certificate, \autoref{thm:diagonal-limit-representative-phase-source-coverage} and \autoref{thm:diagonal-limit-representative-phase-real-completion-exit-certificate}. They identify the visible phase sources of the terminal read and exclude a hidden selector, quotient equality, selected host point, choice row, and ambient completeness principle.

Finally use cofinal-budget stability. Any accepted selector transport preserves the same root-window package under $\hsame$ and $\Cont$, so a terminal selector read can only factor through $W,R,D,M,E$ and the local support rows already present in the ledger.
\end{proof}

\begin{theorem}[Diagonal-limit representative selector choice refusal]
\label{thm:diagonal-limit-representative-selector-choice-refusal}
Every terminal selector read accepted by a Real-and-Completion consumer of $\DiagonalLimitRepresentativeUp$ factors through the extracted package $\Omega=(W,R,D,M,E)$ of \autoref{thm:diagonal-limit-representative-root-window-extraction}. The read may use only the displayed $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ ledger $D$, $\CofinalModulusSealUp$ budget $M$, $\RealUp$ seal $E$, and the recorded $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DiagonalLimitRepresentativeUp}$ support. It cannot use countable choice, hidden tail selection, quotient stream equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted completion-selector ledger to the root-window package. By root-window extraction, the terminal read exposes exactly the five displayed rows $W,R,D,M,E$ together with the local support rows of the carrier.

Next apply terminal-selector exactness and the Phase Real and Completion exit certificate, \autoref{thm:diagonal-limit-representative-terminal-selector-exactness} and \autoref{thm:diagonal-limit-representative-phase-real-completion-exit-certificate}. These results identify the terminal consumer surface with the finite packet and its four phase sources.

The refusal clauses follow from absence of coordinates. Since the package exports no countable-choice row, no tail selector, no quotient stream equality, and no ambient completeness field, a terminal read has no constructor through which any of those data can enter.
\end{proof}

\begin{theorem}[Diagonal-limit representative terminal route uniqueness]
\label{thm:diagonal-limit-representative-terminal-route-uniqueness}
Let two accepted terminal selector reads expose the same displayed root-window package $\Omega=(W,R,D,M,E)$ and the same $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DiagonalLimitRepresentativeUp}$ support. Then they expose the same terminal route through $W,R,D,M,E$ and cannot yield a second $\RealUp$ seal. In particular, equality of the displayed support identifies the terminal selector route as the unique finite route available to the Real-and-Completion consumer.
\end{theorem}
\begin{proof}
Read both terminal selector claims through the common displayed package $\Omega$. Terminal-selector exactness sends each accepted read to the route through the same window, readback, dyadic ledger, cofinal budget, and terminal seal rows.

Use cofinal-budget stability and regularity handoff, \autoref{thm:diagonal-limit-representative-cofinal-budget-stability} and \autoref{thm:diagonal-limit-representative-regularity-handoff}. The shared $\hsame$ and $\Cont$ support preserves the $W \to R$ regularity route and the cofinal budget row before the terminal seal is read.

Finally apply Real-seal admissibility and selector choice refusal. The only admissible seal is the displayed $E$ reached after $W,R,D,M$, while selector choice refusal removes hidden tails, quotient equality, countable choice, and ambient completeness as alternate seal sources. Hence a second terminal route has no available coordinate.
\end{proof}

\begin{theorem}[Diagonal-limit representative Phase Real exit obligation package]
\label{thm:diagonal-limit-representative-phase-real-exit-obligation-package}
The local Phase Real and Completion exit obligation for $\DiagonalLimitRepresentativeUp$ is the package consisting of root-window extraction, regularity handoff, Real-seal admissibility, cofinal-budget stability, terminal-selector exactness, selector choice refusal, terminal route uniqueness, and the Phase Real and Completion exit certificate of \autoref{thm:diagonal-limit-representative-phase-real-completion-exit-certificate}. A downstream exit route for $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ may consume this package as the finite terminal selector boundary and may not appeal to off-packet choice, quotient equality, hidden tail selection, selected host limits, or ambient Real completeness.
\end{theorem}
\begin{proof}
Assemble the positive route first. Root-window extraction supplies $\Omega=(W,R,D,M,E)$; regularity handoff orders the $\StreamNameUp$ window before the $\RegSeqRatUp$ readback; Real-seal admissibility identifies $E$ as the displayed terminal seal; and cofinal-budget stability keeps $M$ attached to the same finite terminal route.

Next add the selector boundary. Terminal-selector exactness says every accepted terminal read factors through $\Omega$, selector choice refusal excludes non-displayed selector principles, and terminal route uniqueness prevents a second Real seal from being produced by the same support rows.

The Phase Real and Completion exit certificate supplies the consumer-facing source inventory. Therefore the local obligation package consumed by the exit route is precisely the finite carrier route through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CofinalModulusSealUp$, and $\RealUp$, with the stated refusal and uniqueness boundaries attached.
\end{proof}
