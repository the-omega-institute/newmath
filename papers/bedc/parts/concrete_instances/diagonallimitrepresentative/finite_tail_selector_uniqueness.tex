\begin{theorem}[Diagonal-limit representative finite-tail selector uniqueness]
\label{thm:diagonal-limit-representative-finite-tail-selector-uniqueness}
Let a Real-and-Completion consumer read an accepted terminal-diagonal selector packet for $\DiagonalLimitRepresentativeUp$ through a displayed finite tail. Once the root-index rows $X,\mu,n,k,i$, the cofinal-tail rows, the terminal-pullback rows, and the terminal-diagonal selector of \autoref{def:diagonal-limit-representative-terminal-diagonal-selector} are fixed, the consumer has exactly one displayed selector route
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \DyadicRatCoreUp \longrightarrow \FinitePrefixLimitBudgetUp \longrightarrow \CofinalModulusSealUp \longrightarrow \RealUp
$$
through the packet rows
$$
(M,Q,X,\mu,n,k,i,w,r,d,e,B,S,H,C,P,N).
$$
The uniqueness is finite-route uniqueness inside $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DiagonalLimitRepresentativeUp}$; it is not quotient stream equality, host real equality, a selected host limit, countable choice, ambient $\RealUp$ completeness, or a Lean bridge claim.
\end{theorem}
\begin{proof}
Start with root-index coverage, \autoref{thm:diagonal-limit-representative-root-index-coverage}. It fixes the displayed equations $k=\mu(1/(n+1))$ and $i=k+n+1$ before the consumer can read the StreamName, RegSeqRat, DyadicRatCore, finite-prefix budget, or Real rows.

Next attach cofinal-tail coverage and terminal-pullback source coverage, \autoref{thm:diagonal-limit-representative-cofinal-tail-coverage} and \autoref{thm:diagonal-limit-representative-terminal-pullback-source-coverage}. These two surfaces place the cofinal modulus seal, finite-prefix budget, selected window, readback, dyadic ledger, and terminal seal on the same finite tail.

Finally use terminal-diagonal coverage, \autoref{thm:diagonal-limit-representative-terminal-diagonal-coverage}. It packages the same rows as one terminal selector packet, while the support rows $H,C,P,N$ only transport, replay, source, and name that packet. Hence a consumer with the fixed finite tail has exactly the displayed route and no coordinate from which to choose a second selector.
\end{proof}

\begin{theorem}[Diagonal-limit representative finite-tail selector nonescape]
\label{thm:diagonal-limit-representative-finite-tail-selector-nonescape}
Every accepted Real-and-Completion consumer of a finite-tail selector route for $\DiagonalLimitRepresentativeUp$ remains inside the displayed rows supplied by $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\FinitePrefixLimitBudgetUp$, $\CofinalModulusSealUp$, and $\RealUp$. The route may be transported only by the recorded componentwise $\hsame$ rows and displayed $\Cont$ replay of the same packet
$$
(M,Q,X,\mu,n,k,i,w,r,d,e,B,S,H,C,P,N).
$$
It cannot escape to quotient stream equality, host stream equality, host real equality, a selected host limit, countable choice, ambient Real completeness, a hidden diagonal selector, or a detached terminal seal.
\end{theorem}
\begin{proof}
Read the cofinal-tail classifier stability theorem, \autoref{thm:diagonal-limit-representative-cofinal-tail-classifier-stability}. It confines the selected index rows, finite-prefix budget rows, cofinal modulus data, and support rows to componentwise transport.

Now apply terminal-pullback classifier stability, \autoref{thm:diagonal-limit-representative-terminal-pullback-classifier-stability}. The StreamName window, RegSeqRat readback, DyadicRatCore ledger, and Real seal are preserved only through the finite terminal-pullback packet.

The terminal-diagonal transport theorem \autoref{thm:diagonal-limit-representative-terminal-diagonal-transport} closes the boundary. Since transport has no coordinate for a quotient class, host equality, selected limit, choice functional, completeness witness, hidden selector, or detached seal, every accepted consumer remains on the displayed finite-tail selector route.
\end{proof}

\begin{theorem}[Diagonal-limit representative finite-tail selector obligation package]
\label{thm:diagonal-limit-representative-finite-tail-selector-obligation-package}
The finite-tail selector obligation package for $\DiagonalLimitRepresentativeUp$ consists exactly of root-index coverage, cofinal-tail coverage, terminal-pullback source coverage, terminal-diagonal coverage, finite-tail selector uniqueness, and finite-tail selector nonescape. A downstream Real-and-Completion consumer may cite this package as the complete finite-route obligation target for the selector route through
$$
\StreamNameUp,\quad \RegSeqRatUp,\quad \DyadicRatCoreUp,\quad \FinitePrefixLimitBudgetUp,\quad \CofinalModulusSealUp,\quad \RealUp,
$$
together with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\DiagonalLimitRepresentativeUp}$ support. The package does not assert quotient stream equality, host Real equality, selected host limits, countable choice, ambient Real completeness, or any checked Lean target.
\end{theorem}
\begin{proof}
The positive route is supplied by the cited coverage surfaces. Root-index coverage fixes the computed selector indices, cofinal-tail coverage joins those indices to the finite-prefix and cofinal modulus rows, terminal-pullback source coverage exposes the StreamName, RegSeqRat, DyadicRatCore, and Real rows, and terminal-diagonal coverage repackages them for Real-and-Completion consumption.

Uniqueness supplies the determinate selector claim. By \autoref{thm:diagonal-limit-representative-finite-tail-selector-uniqueness}, fixing the finite tail fixes one displayed route through the six named sibling surfaces and the local support rows.

Nonescape supplies the ledger boundary. By \autoref{thm:diagonal-limit-representative-finite-tail-selector-nonescape}, the package has no outlet to quotient, host-equality, selected-limit, choice, completeness, hidden-selector, or Lean-bridge data. Therefore the listed theorems form exactly the finite-tail selector obligation package available to downstream consumers.
\end{proof}
