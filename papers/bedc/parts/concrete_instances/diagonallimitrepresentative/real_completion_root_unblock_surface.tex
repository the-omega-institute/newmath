\begin{theorem}[Diagonal-limit representative terminal row coverage]
\label{thm:diagonal-limit-representative-terminal-row-coverage}
Every terminal row consumed from an accepted shared-terminal $\DiagonalLimitRepresentativeUp$ packet factors through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ route. More precisely, a Real-and-Completion consumer may read the terminal seal only after the packet exposes the selected window $w$, readback $r$, dyadic ledger $d$, terminal seal $e$, support rows $H,C,P,N$, and the finite-prefix budget rows on the same finite $\BHist$ surface. The factorization is the one supplied by the root-readiness triad of \autoref{thm:diagonal-limit-representative-root-readiness-triad}, the NameCert ledger of \autoref{thm:diagonal-limit-representative-namecert-ledger}, and the cofinal-budget seal totality theorem \autoref{thm:diagonal-limit-representative-cofinal-budget-seal-totality}; it does not assert quotient stream equality, host real equality, selected limits, choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the root-readiness triad. It places the modulus request, selected indices, selected $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, finite-prefix budget rows, terminal $\RealUp$ seal, and support rows on one displayed root surface before any consumer can use the support coordinates.

Next apply the NameCert ledger exhaustion theorem. Its local ledger identifies the carrier, classifier, root-readiness route, cofinal-terminal bridge, formal handoff boundary, and hub dependency witness as the only rows available to the consumer.

Finally use cofinal-budget seal totality. That theorem orders the terminal seal through the same StreamName, RegSeqRat, DyadicRatCore, finite-prefix budget, cofinal modulus, and Real surfaces. Since no listed theorem exports a quotient, host-limit, selector, choice, or ambient-completeness row, every terminal row factors through exactly the displayed finite route.
\end{proof}

\begin{theorem}[Diagonal-limit representative cofinal selector transport]
\label{thm:diagonal-limit-representative-cofinal-selector-transport}
The cofinal selector data of an accepted shared-terminal $\DiagonalLimitRepresentativeUp$ packet transports only inside the displayed rows
$$
\BHist,\qquad \hsame,\qquad \Cont,\qquad \Pkg,\qquad \NameCert.
$$
In particular, transport of the selected equations $k=\mu(1/(n+1))$ and $i=k+n+1$, the selected window $w$, readback $r$, dyadic ledger $d$, finite-prefix budget rows, cofinal modulus rows, and terminal seal $e$ is mediated by the packet rows $H,C,P,N$ and by the cofinal-terminal budget bridge. It never passes through a hidden selector, quotient stream equality, host real equality, countable choice, or ambient completion field.
\end{theorem}
\begin{proof}
Start from the classifier transport theorem in the root obligation surface. It permits componentwise transport only through recorded $\hsame$ comparisons and displayed $\Cont$ replay over the accepted carrier rows.

The cofinal-index readiness package then attaches the selected equations, the finite-prefix budget rows, and the cofinal modulus rows to the same source route. Its budget-lock theorem makes the selected window, readback, dyadic ledger, tolerance rows, and terminal seal visible before terminal consumption.

It remains to identify the allowed containers for transport. The NameCert ledger supplies the local $\Pkg$ and $\NameCert$ rows, while the formal handoff boundary \autoref{thm:diagonal-limit-representative-formal-handoff-boundary} prevents any stronger analytic object from entering the route. Therefore cofinal selector transport stays inside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface.
\end{proof}

\begin{theorem}[Diagonal-limit representative Real-completion consumer exactness]
\label{thm:diagonal-limit-representative-real-completion-consumer-exactness}
The Real-and-Completion root-unblock surface for $\DiagonalLimitRepresentativeUp$ is exactly the package consisting of terminal row coverage, cofinal selector transport, the root-readiness triad of \autoref{thm:diagonal-limit-representative-root-readiness-triad}, the NameCert ledger of \autoref{thm:diagonal-limit-representative-namecert-ledger}, the cofinal-budget seal totality theorem of \autoref{thm:diagonal-limit-representative-cofinal-budget-seal-totality}, and the formal handoff boundary of \autoref{thm:diagonal-limit-representative-formal-handoff-boundary}. Any accepted downstream consumer must factor through this package, and every row in the package is sourced by the displayed StreamName, RegSeqRat, DyadicRatCore, finite-prefix budget, cofinal modulus, Real, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data. The package carries no quotient equality, host limit, selected limit, countable choice, Real completeness, or Lean bridge claim.
\end{theorem}
\begin{proof}
Coverage is supplied by \autoref{thm:diagonal-limit-representative-terminal-row-coverage}. It forces terminal consumption through the visible StreamName, RegSeqRat, DyadicRatCore, finite-prefix budget, cofinal modulus, and Real route.

Transport is supplied by \autoref{thm:diagonal-limit-representative-cofinal-selector-transport}. It confines the selector equations and cofinal rows to the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ containers.

The existing root-readiness triad, NameCert ledger, cofinal-budget seal totality theorem, and formal handoff boundary close the package. Together they state the finite rows available to a downstream consumer, the ledger that names those rows, the terminal ordering through the cofinal budget, and the boundary excluding stronger analytic data. Hence the consumer exactness surface is precisely the named finite package and nothing outside it.
\end{proof}
