\chapter{A Concrete Naming Certificate for $\SetValuedMapUp$}
\label{ch:concrete-instances-set-valued-map-namecert}
\origin{human}

Following Aubin--Frankowska set-valued analysis and Berge's compact-value route, the $\SetValuedMapUp$ packet records a multifunction interface as finite graph, fibre, closed-boundary, and compact-image rows. It sits between the topological and metric sources of \autoref{ch:concrete-instances-topology-namecert} and \autoref{ch:concrete-instances-metric-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, the closed-set boundary of \autoref{ch:concrete-instances-closedset-namecert}, and the compact-image closed consumer of \autoref{ch:concrete-instances-compact-image-closed-namecert}. The packet names a finite correspondence surface; it is not a host powerset-valued function, selected-value operator, quotient graph equality, open-cover compactness theorem, or ambient $\RealUp$ completeness claim.

\begin{definition}[Set-valued map carrier]
\label{def:set-valued-map-carrier}
A $\SetValuedMapUp$ carrier is a finite $\BHist$ packet
$$
M=(X,Y,G,F,L,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source $\TopologyUp$ or $\MetricUp$ row whose finite probes name the domain windows.
\item $Y$ is the target $\TopologyUp$, $\MetricUp$, or $\RealUp$ row whose finite probes name the value windows.
\item $G$ is the graph ledger, recording displayed source probes, target probes, and membership rows for the correspondence graph.
\item $F$ is the fibre ledger, recording the finite rows that expose each displayed value fibre over a source probe.
\item $L$ is the $\ClosedSetUp$ row used to read closed-graph or closed-fibre boundaries.
\item $K$ is the $\CompactImageClosedUp$ consumer row used when compact-source image data must be read as a closed target boundary.
\item $H$ records componentwise $\hsame$ transport for $X,Y,G,F,L,K$ and the structural rows.
\item $C$ records displayed $\Cont$ replay from source probes through graph membership, fibre exposure, closed-boundary readback, and compact-image consumption.
\item $P$ is the $\Pkg$ provenance over $\SetValuedMapUp$, $\TopologyUp$, $\MetricUp$, $\RealUp$, $\ClosedSetUp$, $\CompactImageClosedUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SetValuedMapUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,G,F,L,K,H,C,P,N$. It has no coordinate for an ambient multifunction object, host powerset of values, pointwise value selection, quotient equality of graphs, open-cover compactness, or detached closedness theorem.
\end{definition}

\begin{theorem}[Set-valued map NameCert obligations]
\label{thm:set-valued-map-namecert-obligations}
For every accepted $\SetValuedMapUp$ carrier
$$
M=(X,Y,G,F,L,K,H,C,P,N),
$$
the local $\NameCert_{\SetValuedMapUp}$ surface consists exactly of carrier admission for the displayed packet, source-window exposure through $X$, target-window exposure through $Y$, graph membership exposure through $G$, fibre exposure through $F$, closed-boundary readback through $L$, compact-image closed consumption through $K$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier. The source and target rows $X,Y$ determine the only windows in which the correspondence can be inspected, while $G$ records graph membership and $F$ records fibre exposure.

Read the boundary rows next. The closed-set row $L$ is the only route to closed-graph or closed-fibre information, and the compact-image row $K$ is the only route by which compact-source image data reaches a closed target consumer.

Finally inspect the structural rows $H,C,P,N$. They transport, replay, source, and name the same finite packet. Since no carrier coordinate stores an ambient multifunction, host powerset, selected value, graph quotient, open-cover proof, or detached closedness theorem, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Closed-graph and compact-image handoff]
\label{thm:set-valued-map-closed-graph-handoff}
Every closed-graph or compact-image consumer of an accepted $\SetValuedMapUp$ carrier must read the route
$$
\TopologyUp/\MetricUp \longmapsto \RealUp \longmapsto G \longmapsto F \longmapsto \ClosedSetUp \longmapsto \CompactImageClosedUp \longmapsto H,C,P,N .
$$
In particular, closed-graph reasoning and compact-image closedness are handed to consumers as finite NameCert rows, not as host semantics for a multifunction.
\end{theorem}
\begin{proof}
Begin with the source and target rows. Any consumer first receives the finite source probes in $X$ and target probes in $Y$ before it can inspect graph membership or fibre data.

The graph and fibre rows then expose the correspondence surface. Closed-graph reads pass through the $\ClosedSetUp$ row $L$, while compact-image reads pass through the $\CompactImageClosedUp$ row $K$.

The transport, replay, provenance, and local naming rows keep this handoff attached to the same packet. Thus downstream closed-set and compact-image consumers receive finite certificate data, with no ambient multifunction semantics or selected-value principle added by the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\SetValuedMapUp$ consumer read must display source, target, graph, fibre, closed-boundary, compact-image, transport, replay, provenance, and local naming rows before using closed-graph or compact-image reasoning.}

\independenceWitness{closedset and compact-image-closed: neither is bijective to $\SetValuedMapUp$ because the closed-set packet isolates a boundary row, the compact-image packet isolates a compact-source closed-target consumer, and the set-valued-map packet binds source, target, graph, fibre, closed-boundary, compact-image, transport, replay, provenance, and local naming rows in one carrier.}

\closureat{SetValuedMapUp}{seedStr}

\begin{closurestatus}{\SetValuedMapUp}
  \constructivestory{A $\SetValuedMapUp$ packet is generated from source windows, target windows, graph membership rows, fibre rows, closed-boundary readback, compact-image closed consumption, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:set-valued-map-carrier}, \autoref{thm:set-valued-map-namecert-obligations}, and \autoref{thm:set-valued-map-closed-graph-handoff} seed the finite multifunction interface for closed-graph and compact-image consumers over $\TopologyUp$, $\MetricUp$, $\RealUp$, $\ClosedSetUp$, and $\CompactImageClosedUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close host powerset semantics, pointwise value selection, quotient graph equality, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, graph and fibre exposure, closed-boundary readback, compact-image handoff, transport and replay stability, and local NameCert provenance for BEDC.Derived.SetValuedMapUp.}
\end{closurestatus}
