\chapter{A Concrete Naming Certificate for $\CompactIntervalChoiceFreeUp$}
\label{ch:concrete-instances-compact-interval-choice-free-namecert}
\origin{human}

Following Bishop and Bridges constructive compactness practice, the $\CompactIntervalChoiceFreeUp$ packet records compact interval finite-net reasoning as a finite BEDC carrier before any Real and Completion consumer may inspect a completion boundary. It sits after the located interval boundary of \autoref{ch:concrete-instances-locatedinterval-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the constructive finite-net surface of a compact interval; it is not open-cover compactness, a quotient stream construction, a selected limit, trichotomy, countable choice, or a bridge claim.

\begin{definition}[Compact interval choice-free carrier]\label{def:compact-interval-choice-free-carrier}
A $\CompactIntervalChoiceFreeUp$ carrier is a finite $\BHist$ packet
$$
K=(E,D,S,Q,R,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the located endpoint row. It names the left and right endpoint data through the $\LocatedIntervalUp$ surface and records the finite comparison windows used to keep the interval ordered.
\item $D$ is the dyadic finite mesh row. It lists a finite $\DyadicRatCoreUp$ mesh, the mesh radius, the endpoint brackets, and the predecessor-successor checks that witness coverage of the displayed interval.
\item $S$ is the $\StreamNameUp$ window row. It records exactly the finite name coordinates inspected when an interval point, endpoint, or mesh point is read by a downstream consumer.
\item $Q$ is the $\RegSeqRatUp$ readback row. It records regular rational approximations for the endpoints and mesh witnesses at the displayed tolerance requests.
\item $R$ is the $\RealUp$ seal row. It exposes only the terminal real-name seals obtained from $S$ and $Q$.
\item $T$ records componentwise $\hsame$ transport for the endpoint row, dyadic mesh row, StreamName windows, RegSeqRat readbacks, Real seals, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
E,D \longmapsto S,Q \longmapsto R,T,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalChoiceFreeUp$, $\LocatedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactIntervalChoiceFreeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,D,S,Q,R,T,C,P,N$. It has no coordinate for arbitrary open covers, selected limit points, quotient streams, trichotomy on host reals, countable choice, or a standard bridge.
\end{definition}

\begin{theorem}[Compact interval choice-free finite-net obligations]\label{thm:compact-interval-choice-free-net-obligations}
For every accepted $\CompactIntervalChoiceFreeUp$ carrier
$$
K=(E,D,S,Q,R,T,C,P,N),
$$
the local $\NameCert_{\CompactIntervalChoiceFreeUp}$ surface consists exactly of carrier admission for located endpoints, the dyadic finite mesh, StreamName windows, RegSeqRat readback, Real seals, componentwise transport, displayed replay, provenance, and local naming. The finite mesh row must witness dyadic coverage of the displayed endpoint interval, endpoint transport must preserve the inspected located rows, and every Real-facing read must pass through the displayed StreamName and RegSeqRat rows before the Real seal is inspected.
\end{theorem}
\begin{proof}
Project the accepted carrier in \autoref{def:compact-interval-choice-free-carrier}. Carrier admission is the assertion that the rows $E,D,S,Q,R,T,C,P,N$ are the only rows exposed by the packet.

The dyadic finite-net obligation is read from $D$. Its endpoint brackets, mesh radius, and predecessor-successor checks give the finite coverage witnesses needed by a compact interval consumer without appealing to an arbitrary open cover.

Endpoint transport is read from $T$ and replayed by $C$. The componentwise $\hsame$ rows identify the displayed located endpoints, mesh data, StreamName windows, RegSeqRat readbacks, and Real seals before any consumer may compare packet outputs.

The ledger is therefore nonescaping. Since the carrier has no coordinate for open-cover compactness, selected limits, quotient streams, trichotomy, countable choice, or bridge data, the listed rows exhaust the local finite-net obligation surface.
\end{proof}

\begin{theorem}[Compact interval choice-free Real-completion boundary]\label{thm:compact-interval-choice-free-real-completion-boundary}
Every Real and Completion consumer of an accepted $\CompactIntervalChoiceFreeUp$ carrier factors through the dyadic finite mesh row $D$, the finite StreamName window row $S$, the regular rational readback row $Q$, and the terminal Real seal row $R$. The exported boundary supplies only finite mesh coverage, finite windows, regular readback, and Real seals; it exports no open-cover compactness, selected limit, quotient stream, trichotomy, countable choice, host real equality, or bridge claim.
\end{theorem}
\begin{proof}
Project the consumer route through the carrier rows of \autoref{def:compact-interval-choice-free-carrier}. A completion-facing read can inspect endpoint and mesh data only through $E$ and $D$, and any name coordinate needed for that read must pass through $S$.

The regular rational interpretation is then supplied by $Q$, and the terminal real-name boundary is supplied by $R$. Thus the Real-facing surface is reached by the finite route
$$
D,S,Q,R
$$
rather than by a quotient stream or a selected completed point.

The structural rows $T,C,P,N$ transport, replay, source, and name this route only. Because no row stores open-cover compactness, countable choice, trichotomy, host equality, or bridge data, the consumer receives exactly the finite Real-completion boundary stated above.
\end{proof}

\closureat{CompactIntervalChoiceFreeUp}{seedStr}

\begin{closurestatus}{\CompactIntervalChoiceFreeUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-choice-free-carrier}, \autoref{thm:compact-interval-choice-free-net-obligations}, and \autoref{thm:compact-interval-choice-free-real-completion-boundary} seed the finite compact-interval carrier over located endpoints, dyadic mesh data, finite StreamName windows, RegSeqRat readback, Real seals, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected limits, quotient streams, trichotomy, countable choice, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic finite-net coverage, endpoint transport, Real-completion boundary routing, provenance, and local naming for BEDC.Derived.CompactIntervalChoiceFreeUp.}
  \constructivestory{A $\CompactIntervalChoiceFreeUp$ packet is generated from LocatedInterval, DyadicRatCore, StreamName, RegSeqRat, Real, BHist, hsame, Cont, Pkg, and NameCert rows.}
\end{closurestatus}
