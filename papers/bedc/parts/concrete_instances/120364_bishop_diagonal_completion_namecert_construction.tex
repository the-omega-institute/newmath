\chapter{A Concrete Naming Certificate for $\BishopDiagonalCompletionUp$}
\label{ch:concrete-instances-bishop-diagonal-completion-namecert}
\origin{human}

Following Bishop and Bridges constructive completion, the $\BishopDiagonalCompletionUp$ packet records the finite diagonal route by which rational regular names, dyadic tolerances, and inspected stream windows are organized before a terminal Real-facing seal is exposed. It sits after the dyadic arithmetic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the effective Cauchy completion route of \autoref{ch:concrete-instances-effective-cauchy-completion-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a diagonal completion interface; it is not quotient stream equality, a selected limit representative, countable choice, host real equality, or ambient completeness of $\RealUp$.

\begin{definition}[Bishop diagonal completion carrier]
\label{def:bishop-diagonal-completion-carrier}
A $\BishopDiagonalCompletionUp$ carrier is a finite $\BHist$ packet
$$
B=(D,S,R,\Delta,E,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, carrying dyadic scales, rational slack rows, and comparison budgets for the diagonal requests.
\item $S$ is the $\StreamNameUp$ finite-window row, recording the inspected rational-name windows at the scales displayed by $D$.
\item $R$ is the $\RegSeqRatUp$ regular Cauchy readback row obtained from the inspected windows and the dyadic tolerance ledger.
\item $\Delta$ is the diagonal schedule row, listing the finite request order by which later windows reuse earlier tolerance and readback data.
\item $E$ is the $\EffectiveCauchyCompletionUp$ route row, exposing the completion-facing handoff that consumes $D,S,R,\Delta$ before any terminal seal is visible.
\item $L$ is the $\RealUp$ seal row reached only after the effective completion route has displayed the diagonal windows and regular rational readback.
\item $H$ records componentwise $\hsame$ transport for the dyadic, stream-window, regular-readback, diagonal-schedule, effective-completion, Real-seal, replay, provenance, and naming rows.
\item $C$ records the displayed $\Cont$ replay from a diagonal completion request through $D,S,R,\Delta,E,L,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopDiagonalCompletionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\EffectiveCauchyCompletionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopDiagonalCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,R,\Delta,E,L,H,C,P,N$. It has no coordinate for quotient representatives, hidden limit selection, countable-choice schedules, host equality of reals, or an unmediated completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Bishop diagonal completion NameCert obligations]
\label{thm:bishop-diagonal-completion-namecert-obligations}
For every accepted $\BishopDiagonalCompletionUp$ carrier $B=(D,S,R,\Delta,E,L,H,C,P,N)$, the local $\NameCert_{\BishopDiagonalCompletionUp}$ surface consists exactly of carrier admission, dyadic tolerance exposure, finite stream-window exposure, regular rational readback, diagonal schedule display, effective Cauchy completion routing, Real-seal handoff, componentwise transport, displayed continuation replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier coordinates. The rows $D,S,R,\Delta,E,L$ display the finite dyadic tolerance data, inspected name windows, regular Cauchy readback, diagonal request schedule, effective completion route, and terminal Real-facing seal.

The structural rows $H,C,P,N$ then transport, replay, source, and name the same packet. They do not add quotient representatives, selected limits, countable-choice data, host equality, or an ambient completeness witness.

Since the carrier has no coordinate outside $D,S,R,\Delta,E,L,H,C,P,N$, every accepted local NameCert obligation is one of the listed rows, and every listed row is explicitly present in the accepted packet.
\end{proof}

\begin{theorem}[Bishop diagonal completion regular seal route]
\label{thm:bishop-diagonal-completion-regular-seal-route}
Every terminal $\RealUp$ seal of an accepted $\BishopDiagonalCompletionUp$ carrier is reached through displayed finite diagonal windows and regular rational readback. More precisely, any Real-facing consumer factors through the finite route
$$
\DyadicRatCoreUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \EffectiveCauchyCompletionUp \longrightarrow \RealUp ,
$$
with the diagonal schedule row $\Delta$ governing which displayed windows are consumed before the effective completion route is read.
\end{theorem}
\begin{proof}
Begin with a Real-facing consumer of $B$. Carrier admission gives access to the terminal seal only as the row $L$ in the same finite packet as $D,S,R,\Delta,E$.

Replay the continuation row $C$. The request first reads the dyadic tolerance ledger $D$, opens the finite $\StreamNameUp$ windows $S$, obtains the $\RegSeqRatUp$ regular readback $R$, and consults the diagonal schedule $\Delta$ that records the inspected finite window order.

Only after that displayed route is available can the effective completion row $E$ be consumed and the terminal seal $L$ be read. Componentwise transport and package provenance preserve the same order, so no host limit operation can bypass the finite diagonal windows or the regular rational readback.
\end{proof}

\begin{theorem}[Bishop diagonal completion no quotient nonescape]
\label{thm:bishop-diagonal-completion-no-quotient-nonescape}
No accepted $\BishopDiagonalCompletionUp$ packet exports quotient stream equality, a selected representative of a Cauchy class, countable choice, host real equality, or ambient $\RealUp$ completeness. Any such datum is outside the local carrier unless it is re-expressed through the displayed dyadic, stream-window, regular-readback, diagonal-schedule, effective-completion, Real-seal, transport, replay, provenance, and naming rows.
\end{theorem}
\begin{proof}
Inspect the classifier of \autoref{def:bishop-diagonal-completion-carrier}. It compares accepted packets only through the ten displayed rows $D,S,R,\Delta,E,L,H,C,P,N$.

The excluded data are not among those coordinates. The regular seal route also forces every Real-facing read through the dyadic, finite-window, regular-readback, diagonal-schedule, and effective-completion route before the terminal seal is visible.

Therefore an export using quotient equality, selected representatives, countable choice, host equality, or ambient completeness has left the accepted $\BishopDiagonalCompletionUp$ surface rather than escaping from it.
\end{proof}

\falsifiablePrediction{Every accepted BishopDiagonalCompletionUp Real-facing read factors through DyadicRatCore tolerance data, StreamName finite windows, RegSeqRat regular readback, a displayed diagonal schedule, EffectiveCauchyCompletion routing, a RealUp seal, transport, replay, provenance, and local naming.}

\independenceWitness{dyadicratcore, streamname, regseqrat, effective-cauchy-completion, real: $\BishopDiagonalCompletionUp$ is not bijective to any listed sibling because it jointly carries dyadic tolerances, finite windows, regular readback, a diagonal schedule, effective Cauchy completion routing, Real sealing, transport, replay, provenance, and local NameCert in one packet.}

\closureat{BishopDiagonalCompletionUp}{seedStr}

\begin{closurestatus}{\BishopDiagonalCompletionUp}
  \constructivestory{A $\BishopDiagonalCompletionUp$ packet is generated from a DyadicRatCore tolerance ledger, StreamName finite windows, RegSeqRat regular Cauchy readback, a diagonal schedule row, an EffectiveCauchyCompletion route, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-diagonal-completion-carrier}, \autoref{thm:bishop-diagonal-completion-namecert-obligations}, \autoref{thm:bishop-diagonal-completion-regular-seal-route}, and \autoref{thm:bishop-diagonal-completion-no-quotient-nonescape} seed the finite diagonal completion route through dyadic tolerance, finite stream-window, regular-readback, effective-completion, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exposure, finite-window readback, regular Cauchy diagonal routing, effective completion handoff, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopDiagonalCompletionUp.}
\end{closurestatus}
