\chapter{A Concrete Naming Certificate for $\BishopRealApartnessEqualityBoundaryUp$}
\label{ch:concrete-instances-bishop-real-apartness-equality-boundary-namecert}
\origin{human}

Following Bishop and Bridges on constructive real numbers, apartness, and regular Cauchy names, the $\BishopRealApartnessEqualityBoundaryUp$ packet records the finite boundary where a displayed apartness read may be transported to a real-facing equality discipline without becoming trichotomy, host equality, quotient real equality, or ambient completeness. It consumes the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the located-apartness route of \autoref{ch:concrete-instances-located-real-apartness-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, finite StreamName windows from \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet is an L10 Real and Completion boundary; it names only the displayed rows needed before apartness, equality, and real-name consumers can share a finite handoff.

\begin{definition}[Bishop real apartness equality boundary carrier]
\label{def:bishop-real-apartness-equality-boundary-carrier}
A $\BishopRealApartnessEqualityBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(R,A,D,S,Q,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ seal row naming the real-facing endpoint reached by the boundary.
\item $A$ is the located-apartness row, read through $\LocatedRealApartnessUp$, recording the positive separation witness available for the displayed real names.
\item $D$ is the dyadic tolerance row, read through $\DyadicRatCoreUp$, recording the finite rational precision budget used by the apartness comparison.
\item $S$ is the finite $\StreamNameUp$ window row exposing the inspected name prefixes.
\item $Q$ is the $\RegSeqRatUp$ readback row converting the inspected stream windows and dyadic tolerances into regular rational comparison data.
\item $T$ records componentwise $\hsame$ transport for the Real seal, located-apartness, dyadic tolerance, StreamName, RegSeqRat, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from an apartness or equality consumer through $A,D,S,Q$ before the Real seal $R$ can be read.
\item $P$ is the $\Pkg$ provenance over $\BishopRealApartnessEqualityBoundaryUp$, $\RealUp$, $\LocatedRealApartnessUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealApartnessEqualityBoundaryUp}$ row.
\end{enumerate}
The carrier has no coordinate for real trichotomy, host equality of real values, quotient equality of Cauchy names, selected representatives, countable choice, or unrestricted completeness of $\RealUp$.
\end{definition}
\leandef{BEDC.Derived.BishopRealApartnessEqualityBoundaryUp.BishopRealApartnessEqualityBoundaryUp}
\leanchecked{BEDC.Derived.BishopRealApartnessEqualityBoundaryUp.BishopRealApartnessEqualityBoundaryTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bishop real apartness equality boundary obligations]
\label{thm:bishop-real-apartness-equality-boundary-namecert-obligations}
For every accepted $\BishopRealApartnessEqualityBoundaryUp$ carrier
$$
B=(R,A,D,S,Q,T,C,P,N),
$$
the local $\NameCert_{\BishopRealApartnessEqualityBoundaryUp}$ surface consists exactly of Real seal admission through $R$, located-apartness exposure through $A$, dyadic tolerance exposure through $D$, finite window exposure through $S$, regular rational readback through $Q$, componentwise transport through $T$, displayed replay through $C$, package provenance through $P$, and local naming through $N$. Any equality-facing consumer must read $A,D,S,Q$ before the $\RealUp$ seal is public.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-apartness-equality-boundary-carrier}. The rows $R,A,D,S,Q,T,C,P,N$ are the only rows available to the local certificate.

The apartness and tolerance data are displayed before any real-facing read. Row $A$ supplies the located-apartness witness, $D$ supplies the dyadic budget, $S$ supplies the inspected windows, and $Q$ supplies the regular rational readback.

Rows $T,C,P,N$ preserve the same boundary under transport, replay, provenance, and local naming. Since no trichotomy row, host equality row, quotient-equality row, selected representative, choice row, or ambient completeness row is present, the listed rows exhaust the NameCert obligations.
\end{proof}
\leanchecked{BEDC.Derived.BishopRealApartnessEqualityBoundaryUp.BishopRealApartnessEqualityBoundaryNameCertObligations}

\begin{theorem}[Bishop real apartness equality boundary handoff]
\label{thm:bishop-real-apartness-equality-boundary-real-handoff}
Every $\RealUp$ or equality-facing consumer of an accepted $\BishopRealApartnessEqualityBoundaryUp$ packet factors through the finite route
$$
\LocatedRealApartnessUp,\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp,\RealUp,\hsame,\Cont,\Pkg,\NameCert .
$$
The handoff preserves the positive located-apartness read as displayed finite data and does not export trichotomy, host equality of completed real values, quotient equality of names, selected representatives, countable choice, or ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Start from the accepted packet $B=(R,A,D,S,Q,T,C,P,N)$. By \autoref{thm:bishop-real-apartness-equality-boundary-namecert-obligations}, every consumer reads the located-apartness row $A$, dyadic tolerance row $D$, finite window row $S$, and regular rational readback row $Q$ before the real seal $R$ is reachable.

The structural rows then bind the handoff. Componentwise transport $T$ preserves the displayed rows, replay $C$ repeats only the same read route, provenance $P$ names the source chapters, and $N$ supplies local naming.

Thus the exported real-facing boundary is exactly the listed finite route. A consumer that asks for trichotomy, host equality, quotient real equality, selected representatives, countable choice, or ambient completeness must supply a row absent from the carrier, so the request leaves the boundary.
\end{proof}
\leanchecked{BEDC.Derived.BishopRealApartnessEqualityBoundaryUp.BishopRealApartnessEqualityBoundaryRealHandoff}

\falsifiablePrediction{A purported apartness/equality handoff that exports a $\RealUp$ equality read while omitting the located-apartness, dyadic tolerance, StreamName, or RegSeqRat row is rejected by the BishopRealApartnessEqualityBoundaryUp NameCert surface.}

\independenceWitness{real, located-real-apartness, dyadicratcore, streamname, regseqrat: the Real seal alone does not store located apartness; the located-apartness row alone does not store dyadic precision, finite windows, or regular rational readback; and the arithmetic and stream siblings do not export the terminal Real seal. The boundary requires all five siblings plus transport, replay, provenance, and local NameCert rows in one carrier.}

\closureat{BishopRealApartnessEqualityBoundaryUp}{seedStr}
\begin{closurestatus}{\BishopRealApartnessEqualityBoundaryUp}
  \constructivestory{A $\BishopRealApartnessEqualityBoundaryUp$ packet is generated from a Real seal, located-apartness row, dyadic tolerance row, StreamName window row, RegSeqRat readback row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-apartness-equality-boundary-carrier}, \autoref{thm:bishop-real-apartness-equality-boundary-namecert-obligations}, and \autoref{thm:bishop-real-apartness-equality-boundary-real-handoff} seed the finite apartness/equality boundary for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not prove real trichotomy, host equality of real values, quotient equality of Cauchy names, selected representatives, countable choice, ambient completeness of $\RealUp$, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-apartness exposure, dyadic tolerance exactness, StreamName window exposure, RegSeqRat readback, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.BishopRealApartnessEqualityBoundaryUp.}
\end{closurestatus}
