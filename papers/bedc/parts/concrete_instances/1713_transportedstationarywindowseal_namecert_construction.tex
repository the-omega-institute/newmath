\chapter{A Concrete Naming Certificate for $\TransportedStationaryWindowSealUp$}
\label{ch:concrete-instances-transportedstationarywindowseal-namecert}
\origin{human}

The $\TransportedStationaryWindowSealUp$ interface records the finite seal packet consumed by the transported stationary diagonal path. It packages the transported $\RatUp$ seed classifier, the constant $\StreamNameUp$ window, the $\RegSeqRatUp$ window seal, the $\FiniteWindowEnvelopeUp$ observation boundary, and the $\RealUp$ seal-facing row. It is a finite naming certificate, not a real-completeness theorem.

The transported stationary window-seal packet consumes the constant-window
route of \autoref{ch:concrete-instances-streamname-namecert}, the regular
rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, the
finite observation boundary of
\autoref{ch:concrete-instances-finitewindowenvelope-namecert}, and the
real-facing seal row of \autoref{ch:concrete-instances-real-namecert} through
the route $R,S,G,W,E,H,C,P,N$ before any consumer reads the transported seal.

\begin{definition}[Transported stationary window-seal carrier]
\label{def:transported-stationary-window-seal-carrier}
A $\TransportedStationaryWindowSealUp$ carrier is a finite $\BHist$ packet
$$
T=(R,S,G,W,E,H,C,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $R$ is the transported $\RatUp$ classifier row for the two stationary rational seeds;
\item $S$ is the constant $\StreamNameUp$ finite-window classifier induced by $R$;
\item $G$ is the $\RegSeqRatUp$ finite-window seal row read from the same constant stream window;
\item $W$ is the $\FiniteWindowEnvelopeUp$ packet naming the selected anchor, window, dyadic ledger, regular-rational seal, and endpoint classifier;
\item $E$ is the $\RealUp$ seal-facing row obtained from $G$ and $W$;
\item $H$ records the transported stationary diagonal handoff rows that identify the displayed diagonal representatives with the same constant rational route;
\item $C$ stores the displayed $\hsame$ and $\Cont$ transports for the seed classifier, stream window, regular-rational seal, finite envelope, and real seal;
\item $P$ is the $\Pkg$ provenance naming $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ source certificates;
\item $N$ is the local $\NameCert_{\TransportedStationaryWindowSealUp}$ row.
\end{enumerate}
The local classifier relates two carriers exactly when their transported $\RatUp$ classifier rows agree and the induced $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, transport, package, and local certificate rows compare componentwise after the displayed routes in $C$.
\end{definition}

\begin{theorem}[Transported stationary window-seal NameCert obligations]
\label{thm:transported-stationary-window-seal-namecert-obligations}
The $\NameCert_{\TransportedStationaryWindowSealUp}$ surface has five obligations:
\begin{enumerate}
\item carrier: formation of the finite packet of \autoref{def:transported-stationary-window-seal-carrier};
\item classifier: componentwise comparison of the transported $\RatUp$ seed classifier, constant $\StreamNameUp$ window, $\RegSeqRatUp$ seal, $\FiniteWindowEnvelopeUp$ packet, $\RealUp$ row, transports, package provenance, and local certificate row;
\item transported window exactness: the $\RatUp$ classifier, constant $\StreamNameUp$ window, and $\RegSeqRatUp$ finite-window seal read the same transported stationary row;
\item seal exhaustion: every $\RealUp$ consumer read factors through the displayed $\FiniteWindowEnvelopeUp$ packet and the same $\RegSeqRatUp$ seal row;
\item non-escape: the carrier has no coordinate for quotient streams, host equality, choice, selected host limits, or real completeness.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.TransportedStationaryWindowSealUp.TransportedStationaryWindowSealPacket\_namecert\_obligations}
\begin{proof}
Carrier formation is the finite assembly of the displayed tuple. The transported rational classifier supplies $R$, the stationary stream read supplies $S$, the regular-rational boundary supplies $G$, and the finite observation envelope supplies $W$. The real-facing row $E$, transported diagonal rows $H$, route table $C$, provenance row $P$, and local naming row $N$ are all visible coordinates of the same $\BHist$ packet.

For the classifier obligation, compare the packet component by component after applying the displayed $\hsame$ and $\Cont$ routes. The transported $\RatUp$ row determines the constant stream window, the stream window determines the regular-rational finite seal, and the envelope ties the same window and dyadic ledger to the real-facing endpoint row.

Transported window exactness is the stationary diagonal calculation read through a finite window. The transported $\RatUp$ classifier induces the constant $\StreamNameUp$ classifier, and the $\RegSeqRatUp$ row consumes exactly that window with the displayed ledger. Thus the three public readings are the same stationary row at the window boundary.

Seal exhaustion follows by projecting $W$ and $E$. The $\FiniteWindowEnvelopeUp$ packet exposes the selected anchor, finite window, dyadic ledger, regular-rational seal, and endpoint classifier, and the $\RealUp$ row can read only those exposed fields plus the displayed transports and provenance.

The non-escape clause is constructor inspection. None of $R,S,G,W,E,H,C,P,N$ is a quotient stream, a host equality proof, a choice operator, a selected host limit, or a real-completeness witness. Therefore the certificate remains a finite transported window-seal packet.
\end{proof}

\begin{theorem}[Transported stationary window-seal readback determinacy]
\label{thm:transported-stationary-window-seal-readback-determinacy}
For an accepted $\TransportedStationaryWindowSealUp$ carrier $T=(R,S,G,W,E,H,C,P,N)$, the transported $\RatUp$ classifier row $R$ determines the constant $\StreamNameUp$ window $S$ and the $\RegSeqRatUp$ finite-window seal $G$. If two accepted carriers have the same transported $\RatUp$ classifier after the displayed $\hsame$ and $\Cont$ routes, then their constant stream windows and regular-rational finite-window seals agree at the public readback boundary.
\end{theorem}
\begin{proof}
Unpack the carrier of \autoref{def:transported-stationary-window-seal-carrier}. The row $R$ is the transported rational classifier, $S$ is the constant stream window induced by that row, and $G$ is the regular-rational seal read from the same window.

The transported window exactness clause in \autoref{thm:transported-stationary-window-seal-namecert-obligations} states that $R$, $S$, and $G$ read the same transported stationary row. The classifier clause transports these rows only componentwise through the displayed $\hsame$ and $\Cont$ routes.

Hence equality of the transported $\RatUp$ classifier at the displayed route fixes the induced constant $\StreamNameUp$ window and the $\RegSeqRatUp$ seal at the public readback boundary.
\end{proof}
\leanchecked{BEDC.Derived.TransportedStationaryWindowSealUp.TransportedStationaryWindowSealPacket\_readback\_determinacy}

\begin{theorem}[Transported stationary window-seal Real factorization]
\label{thm:transported-stationary-window-seal-real-factorization}
Every $\RealUp$ read exposed by an accepted $\TransportedStationaryWindowSealUp$ carrier factors through the displayed $\FiniteWindowEnvelopeUp$ packet $W$ and the same $\RegSeqRatUp$ finite-window seal $G$. The real-facing row $E$ may read the selected anchor, finite window, dyadic ledger, regular-rational seal, and endpoint classifier only through $W$ together with the transported seal row $G$ and the displayed transport and provenance rows.
\end{theorem}
\leanchecked{BEDC.Derived.TransportedStationaryWindowSealUp.TransportedStationaryWindowSealPacket\_real\_factorization}
\begin{proof}
Project the coordinates $G,W,E$ from the carrier tuple. The finite-window envelope $W$ names the anchor, window, dyadic ledger, regular-rational seal, and endpoint classifier, while $E$ is the real-facing row obtained from that envelope and the seal row.

Apply the seal-exhaustion clause of \autoref{thm:transported-stationary-window-seal-namecert-obligations}. It requires every $\RealUp$ consumer read to pass through $W$ and the same $\RegSeqRatUp$ seal row.

The route table $C$ and provenance row $P$ preserve that factorization under the displayed transports. Therefore no $\RealUp$ read bypasses the finite envelope and regular-rational seal.
\end{proof}

\begin{theorem}[Transported stationary window-seal uniqueness]
\label{thm:transported-stationary-window-seal-uniqueness}
Let $T=(R,S,G,W,E,H,C,P,N)$ and $T'=(R',S',G',W',E',H',C',P',N')$ be accepted $\TransportedStationaryWindowSealUp$ carriers. Suppose they are displayed over the same transported $\RatUp$ row, the same constant $\StreamNameUp$ window, the same $\RegSeqRatUp$ finite seal, the same $\FiniteWindowEnvelopeUp$ packet, and the same $\RealUp$ seal-facing row after the $\hsame$ rows and $\Cont$ routes named by $H,H',C,C'$. Then $T$ and $T'$ expose the same public seal row.

The uniqueness claim is only about the finite public seal row. It does not identify quotient streams, host equalities, selected host limits, or real-completeness data.
\end{theorem}
\begin{proof}
Apply the carrier and classifier clauses of \autoref{thm:transported-stationary-window-seal-namecert-obligations}. They restrict comparison of $T$ and $T'$ to the displayed transported rational row, constant stream window, regular-rational seal, finite envelope, real-facing row, transport rows, continuation routes, provenance, and local naming rows.

Next use \autoref{thm:transported-stationary-window-seal-readback-determinacy}. Agreement of the transported $\RatUp$ row under the displayed $\hsame$ and $\Cont$ routes fixes the constant $\StreamNameUp$ window and the $\RegSeqRatUp$ finite-window seal at the public readback boundary.

Apply \autoref{thm:transported-stationary-window-seal-real-factorization}. Any $\RealUp$ seal-facing read must factor through the same finite envelope $W$ and the same regular-rational seal row $G$, so the public seal row exposed by $T$ and $T'$ is the same row.

Finally, \autoref{thm:transported-stationary-window-seal-ledger-nonescape} blocks the only possible escapes from this finite comparison. Quotient streams, host equality, selected host limits, and real-completeness data are not carrier coordinates, so they cannot refine or split the public seal row.
\end{proof}

\begin{theorem}[Transported stationary window-seal ledger non-escape]
\label{thm:transported-stationary-window-seal-ledger-nonescape}
An accepted $\TransportedStationaryWindowSealUp$ carrier exports no quotient stream, host equality, choice, selected host limit, or real-completeness datum. Its public surface consists only of the transported $\RatUp$ classifier, constant $\StreamNameUp$ window, $\RegSeqRatUp$ seal, $\FiniteWindowEnvelopeUp$ packet, $\RealUp$ seal-facing row, displayed transports, package provenance, local $\NameCert$ row, and the ambient finite $\BHist$ packet.
\end{theorem}
\leanchecked{BEDC.Derived.TransportedStationaryWindowSealUp.TransportedStationaryWindowSealLedgerNonescape}
\begin{proof}
Inspect the carrier tuple $T=(R,S,G,W,E,H,C,P,N)$. Each coordinate is one of the finite transported classifier, window, seal, envelope, real-facing, transport, continuation, provenance, or naming rows.

The non-escape clause of \autoref{thm:transported-stationary-window-seal-namecert-obligations} explicitly excludes quotient streams, host equality, choice, selected host limits, and real completeness from that tuple. The readback determinacy and real-factorization statements use only the same visible coordinates.

Consequently the transported stationary window-seal packet has no export path outside the displayed finite BEDC ledger.
\end{proof}

\begin{theorem}[Transported stationary window-seal carrier obligation]
\label{thm:transported-stationary-window-seal-carrier-obligation}
For an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
carrier admission is exactly the finite projection of \autoref{def:transported-stationary-window-seal-carrier}. The admitted rows are the transported $\RatUp$ classifier $R$, the constant $\StreamNameUp$ window $S$, the $\RegSeqRatUp$ finite-window seal $G$, the $\FiniteWindowEnvelopeUp$ packet $W$, the $\RealUp$ seal-facing row $E$, the transported stationary handoff row $H$, the route table $C$, the provenance row $P$, and the local naming row $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier tuple. The definition lists the complete coordinate inventory of an admitted packet, and \autoref{thm:transported-stationary-window-seal-namecert-obligations} names carrier formation as the first local obligation.

The readback-determinacy theorem then identifies $R,S,G$ as the same transported stationary window read at the public boundary. The Real factorization theorem identifies $W,E$ as the only real-facing seal route.

The uniqueness and ledger non-escape theorems rule out any additional carrier coordinate. Thus carrier admission is precisely the finite row projection displayed above.
\end{proof}

\begin{theorem}[Transported stationary window-seal seed window readback]
\label{thm:transported-stationary-window-seal-seed-window-readback}
For an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
the transported $\RatUp$ seed row $R$ determines the constant $\StreamNameUp$ window $S$ and the $\RegSeqRatUp$ seal $G$ through the same finite route $H,C,P,N$. The seed-window readback obligation is therefore the public projection of $R,S,G,H,C,P,N$, not a separate stream equality or host-limit claim.
\end{theorem}
\begin{proof}
Project the carrier by \autoref{thm:transported-stationary-window-seal-carrier-obligation}. The rows $R,S,G$ are admitted as the transported seed classifier, constant stream window, and regular-rational finite-window seal of the same packet.

Apply \autoref{thm:transported-stationary-window-seal-readback-determinacy}. Agreement at $R$ fixes the public readback of $S$ and $G$ through the displayed $\hsame$ and $\Cont$ routes.

The support rows $H,C,P,N$ keep that readback inside the finite NameCert surface. Ledger non-escape prevents any stream quotient, host equality, selected limit, or real-completeness coordinate from refining the seed-window read.
\end{proof}

\begin{theorem}[Transported stationary window-seal Real seal route]
\label{thm:transported-stationary-window-seal-real-seal-route}
For an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
every $\RealUp$ seal read factors through $R,S,G,W,E,H,C,P,N$ and cannot bypass the $\FiniteWindowEnvelopeUp$ row $W$. The Real-facing route reads the transported seed through $R,S,G$, enters the finite envelope at $W$, exposes the seal row $E$, and replays only the displayed transport, route, provenance, and naming rows.
\end{theorem}
\begin{proof}
Start with \autoref{thm:transported-stationary-window-seal-real-factorization}. It forces every $\RealUp$ read exposed by the packet through the $\FiniteWindowEnvelopeUp$ packet $W$ and the same $\RegSeqRatUp$ seal row $G$.

Use \autoref{thm:transported-stationary-window-seal-seed-window-readback} to identify the seed-facing part of the route. The rows $R,S,G$ provide the transported rational seed, the constant window, and the regular-rational seal that feed the finite envelope.

Finally apply carrier admission and ledger non-escape. The admitted route has only $R,S,G,W,E,H,C,P,N$, so no consumer can reach a $\RealUp$ seal by skipping $W$ or by importing quotient streams, host equality, selected limits, choice, or real-completeness data.
\end{proof}

\begin{theorem}[Transported stationary window-seal root RegSeqRat window ownership]
\label{thm:transported-stationary-window-seal-root-regseqrat-window-ownership}
For an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
the $\RegSeqRatUp$ seal row $G$ is owned by the same transported $\RatUp$ row $R$ and the same constant $\StreamNameUp$ window row $S$. A root consumer may read $G$ only as the finite-window seal attached to the route
$$
\begin{aligned}
  R \longmapsto S \longmapsto G
\end{aligned}
$$
through the displayed rows $H,C,P,N$; it cannot detach $G$ as an independent regular-rational source.
\end{theorem}
\begin{proof}
Project the packet by \autoref{thm:transported-stationary-window-seal-carrier-obligation}. The admitted carrier contains $R$, $S$, and $G$ as visible rows of one finite transported window-seal packet, together with the same transport, continuation, provenance, and local naming rows $H,C,P,N$.

Apply \autoref{thm:transported-stationary-window-seal-seed-window-readback}. It identifies the transported rational seed row, the constant stream window, and the regular-rational seal as one public readback route inside the finite NameCert surface.

Finally use \autoref{thm:transported-stationary-window-seal-readback-determinacy}. Since agreement at $R$ fixes both $S$ and $G$ at the public boundary, the seal row $G$ has no separate owner beyond the displayed $R,S,H,C,P,N$ route.
\end{proof}

\begin{theorem}[Transported stationary window-seal root Real seal non-escape]
\label{thm:transported-stationary-window-seal-root-real-seal-nonescape}
For an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
every root-facing $\RealUp$ seal read must pass through the finite rows $R,S,G,W,E,H,C,P,N$. The read first uses the owned regular-rational window route $R,S,G$, then the $\FiniteWindowEnvelopeUp$ row $W$, then the $\RealUp$ seal row $E$, with $H,C,P,N$ carrying only displayed transport, continuation, package, and naming data. It cannot import quotient streams, host equality, selected limits, choice, or real-completeness data.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:transported-stationary-window-seal-real-factorization}. Every $\RealUp$ read exposed by the accepted packet factors through the finite envelope $W$ and the same regular-rational seal row $G$.

Use \autoref{thm:transported-stationary-window-seal-real-seal-route} to name the complete public route. It places the transported seed, constant window, regular-rational seal, envelope, real-facing row, and structural rows in the finite order $R,S,G,W,E,H,C,P,N$.

The non-escape boundary is then \autoref{thm:transported-stationary-window-seal-ledger-nonescape}. Quotient streams, host equality, selected host limits, choice, and real-completeness data are not coordinates of the packet, so no root-facing Real seal read can enter through them.
\end{proof}

\begin{theorem}[Transported stationary window-seal root downstream handoff package]
\label{thm:transported-stationary-window-seal-root-downstream-handoff-package}
For every accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the downstream root handoff package consumed by completion-facing chapters consists exactly of the finite window ownership row and the Real seal non-escape row:
\begin{enumerate}
\item $G$ is the $\RegSeqRatUp$ seal owned by the transported $\RatUp$ row $R$ and the constant $\StreamNameUp$ window row $S$ through $H,C,P,N$;
\item every root-facing $\RealUp$ seal read factors through $R,S,G,W,E,H,C,P,N$ and has no quotient-stream, host-equality, selected-limit, choice, or real-completeness input.
\end{enumerate}
Thus a downstream consumer receives a finite BEDC packet over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, not an ambient completion object.
\end{theorem}
\begin{proof}
The first row is \autoref{thm:transported-stationary-window-seal-root-regseqrat-window-ownership}. It binds the regular-rational seal row to the transported rational seed and the constant stream window before any envelope or Real-facing consumer reads the packet.

The second row is \autoref{thm:transported-stationary-window-seal-root-real-seal-nonescape}. It forces every Real seal read through the displayed finite packet and excludes all hidden quotient, equality, limit, choice, and completeness inputs.

Package these two rows with \autoref{thm:transported-stationary-window-seal-real-seal-route}. The result is the finite downstream handoff surface: consumers may reuse $R,S,G,W,E,H,C,P,N$, but they do not receive any object outside the displayed BEDC carrier.
\end{proof}

\begin{theorem}[Transported stationary window-seal classifier obligation]
\label{thm:transported-stationary-window-seal-classifier-obligation}
Let $T=(R,S,G,W,E,H,C,P,N)$ and $T'=(R',S',G',W',E',H',C',P',N')$ be accepted $\TransportedStationaryWindowSealUp$ packets. Any classifier comparison between them is componentwise across the displayed $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, and $\RealUp$ rows: $R$ compares with $R'$, $S$ with $S'$, $G$ with $G'$, $W$ with $W'$, and $E$ with $E'$ only through the $\hsame$ rows, $\Cont$ routes, provenance rows, and local naming rows carried by $H,H',C,C',P,P',N,N'$.
\end{theorem}
\begin{proof}
Apply the classifier row of \autoref{thm:transported-stationary-window-seal-namecert-obligations}. It permits only componentwise comparison of the transported seed classifier, the induced stream window, the regular-rational seal, the finite envelope, the real-facing row, and the displayed transport and provenance rows.

Use readback determinacy to keep the $\RatUp$, $\StreamNameUp$, and $\RegSeqRatUp$ comparisons on the same stationary route. Use Real factorization to keep the $\FiniteWindowEnvelopeUp$ and $\RealUp$ comparisons on the same envelope and seal row.

Uniqueness repacks these componentwise comparisons as one public seal row, while ledger non-escape prevents the classifier from reading quotient streams, host equality, selected limits, or real-completeness data.
\end{proof}

\begin{theorem}[Transported stationary window-seal ledger obligation]
\label{thm:transported-stationary-window-seal-ledger-obligation}
Every consumer read of an accepted $\TransportedStationaryWindowSealUp$ packet factors through the finite rows
$$
R,S,G,W,E,H,C,P,N.
$$
Requests for quotient streams, host equality, choice, selected limits, or real completeness have no carrier field and therefore cannot be exported by the transported stationary window-seal ledger.
\end{theorem}
\begin{proof}
Start from \autoref{thm:transported-stationary-window-seal-ledger-nonescape}. Its non-escape clause states that the packet exports only the finite transported classifier, stream, regular-rational seal, finite envelope, real-facing, transport, route, provenance, and naming rows.

Readback determinacy and Real factorization describe all displayed consumer paths: the stationary window path passes through $R,S,G$, and the real-facing path passes through $W,E$ together with the same transport and provenance rows.

It remains only to exclude hidden completion data. The carrier definition and uniqueness theorem leave no coordinate for quotient streams, host equality, selected limits, or real completeness, so the ledger obligation is exactly the finite non-escape boundary.
\end{proof}

\begin{theorem}[Transported stationary window-seal root source lock]
\label{thm:transported-stationary-window-seal-root-source-lock}
For every root consumer of an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
the first readable source is the transported $\RatUp$ seed row $R$ locked through the constant $\StreamNameUp$ window $S$ and the $\RegSeqRatUp$ finite-window seal $G$. The consumer may reach the finite envelope, Real-facing row, or completion-facing handoff only after the single route $R,S,G,H,C,P,N$ has been fixed by the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Project the root consumer through the carrier of \autoref{def:transported-stationary-window-seal-carrier}. Its available source coordinates are exactly the transported rational row $R$, the constant stream window $S$, the regular-rational seal $G$, and the displayed transport, route, provenance, and naming rows $H,C,P,N$.

Apply \autoref{thm:transported-stationary-window-seal-readback-determinacy}. It makes $R$, $S$, and $G$ one stationary readback route, so a consumer cannot read the stream window or the regular-rational seal as a second root source.

The sibling surfaces named at the start of this chapter supply the same ordering: the constant-window route of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the transported $\RatUp$ row are read before the finite envelope and Real-facing row. Ledger non-escape then blocks quotient streams, host equality, selected host limits, choice, and real-completeness data from supplying another source.
\end{proof}

\begin{theorem}[Transported stationary window-seal root Real factorization]
\label{thm:transported-stationary-window-seal-root-real-factorization}
For every Real-facing root read of an accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the read factors through the $\FiniteWindowEnvelopeUp$ row $W$ and the same $\RegSeqRatUp$ seal row $G$. Thus the root handoff from the transported rational source to $\RealUp$ is the finite factorization
$$
\begin{aligned}
  R,S,G,H,C,P,N
  \longrightarrow W
  \longrightarrow E .
\end{aligned}
$$
It does not factor through an ambient quotient stream, host equality, selected host limit, choice principle, completion witness, or real-completeness datum.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:transported-stationary-window-seal-root-source-lock}. The root source has already been fixed as the transported $\RatUp$ row read through the constant $\StreamNameUp$ window and the $\RegSeqRatUp$ seal.

Apply \autoref{thm:transported-stationary-window-seal-real-factorization}. Every $\RealUp$ consumer path must pass through the finite envelope $W$ and the same regular-rational seal row $G$, with $H,C,P,N$ carrying only the displayed transport, continuation, provenance, and local naming data.

The finite observation boundary of \autoref{ch:concrete-instances-finitewindowenvelope-namecert} and the real-facing seal row of \autoref{ch:concrete-instances-real-namecert} are therefore joined only by the displayed packet. Uniqueness identifies the resulting public seal row, and ledger non-escape removes every off-packet completion or real-completeness route.
\end{proof}

\begin{theorem}[Transported stationary window-seal root ledger exhaustion]
\label{thm:transported-stationary-window-seal-root-ledger-exhaustion}
The root handoff surface of an accepted $\TransportedStationaryWindowSealUp$ packet exposes exactly
$$
R,S,G,W,E,H,C,P,N.
$$
It exposes the transported $\RatUp$ row, constant $\StreamNameUp$ window, $\RegSeqRatUp$ finite-window seal, $\FiniteWindowEnvelopeUp$ packet, $\RealUp$ seal-facing row, displayed transports, route table, provenance row, and local naming row. It exposes no quotient stream, host equality, choice operator, selected host limit, completion witness, or real-completeness datum.
\end{theorem}
\begin{proof}
The carrier definition gives the root ledger its complete coordinate list. The NameCert obligations and classifier obligation allow only componentwise transport of those displayed rows.

Use the root source lock to account for the source side $R,S,G,H,C,P,N$. Use the root Real factorization theorem to account for the envelope and Real-facing side $W,E$.

Finally apply uniqueness and ledger non-escape. Uniqueness shows that the public root handoff is determined by the displayed finite rows, while ledger non-escape states that quotient streams, host equality, selected limits, choice, completion witnesses, and real-completeness data are not carrier coordinates. Hence the L10 handoff surface is exhausted by $R,S,G,W,E,H,C,P,N$.
\end{proof}

\begin{theorem}[Transported stationary window-seal obligation package]
\label{thm:transported-stationary-window-seal-obligation-package}
For an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
the consumer-facing obligation package is the finite conjunction of carrier admission, classifier comparison, transported $\RatUp$-to-$\StreamNameUp$ readback determinacy, $\FiniteWindowEnvelopeUp$ and $\RegSeqRatUp$ factorization of every $\RealUp$ read, public-row uniqueness, and ledger non-escape. The package ranges only over the displayed $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of $T$.
\end{theorem}
\begin{proof}
First project the carrier and local naming surface. The carrier definition supplies the rows $R,S,G,W,E,H,C,P,N$, while \autoref{thm:transported-stationary-window-seal-namecert-obligations} supplies the local carrier, classifier, transported-window exactness, seal-exhaustion, and non-escape obligations.

Next assemble the readback and Real-facing parts. Apply \autoref{thm:transported-stationary-window-seal-readback-determinacy} to read the transported $\RatUp$ row through the constant $\StreamNameUp$ window and the $\RegSeqRatUp$ finite-window seal. Apply \autoref{thm:transported-stationary-window-seal-real-factorization} to force every $\RealUp$ consumer path through the $\FiniteWindowEnvelopeUp$ packet and the same $\RegSeqRatUp$ seal row.

Finally repack the consumer boundary. The uniqueness theorem identifies the public seal row determined by the displayed components, the carrier-obligation theorem and classifier-obligation theorem expose the finite admission and comparison clauses, and \autoref{thm:transported-stationary-window-seal-ledger-obligation} together with ledger non-escape blocks quotient streams, host equality, selected limits, choice, and real-completeness data.
\end{proof}

\begin{theorem}[Transported stationary window-seal selected-tail stability]
\label{thm:transported-stationary-window-seal-selected-tail-stability}
Let $T=(R,S,G,W,E,H,C,P,N)$ and $T'=(R',S',G',W',E',H',C',P',N')$ be accepted $\TransportedStationaryWindowSealUp$ packets. Suppose the rows $R,S,G,W$ compare with $R',S',G',W'$ under the displayed $\hsame$ rows and $\Cont$ routes carried by $H,H',C,C'$. Then the selected finite-window tail route used by $G$ and $W$ is stable under the same transport: the transported route selected for $T$ and the route selected for $T'$ expose the same public $\RegSeqRatUp$ finite-window seal and $\FiniteWindowEnvelopeUp$ boundary.

Moreover, every $\RealUp$ seal read through $E$ or $E'$ still factors through the transported rows $R,S,G,W,H,C,P,N$ and $R',S',G',W',H',C',P',N'$. The comparison does not introduce quotient streams, selected host limits, choice, or real-completeness data.
\end{theorem}
\begin{proof}
First use readback determinacy. Agreement of $R$ with $R'$ through the displayed $\hsame$ and $\Cont$ routes fixes the corresponding constant $\StreamNameUp$ windows $S,S'$ and the regular-rational finite-window seals $G,G'$ at the public readback boundary.

Next apply the classifier obligation. Its componentwise comparison keeps $R,S,G,W$ aligned with $R',S',G',W'$ through the same transported rows rather than through an ambient stream equality. Hence the finite tail route selected by $G$ and $W$ is carried to the finite tail route selected by $G'$ and $W'$.

Now apply Real factorization. Any $\RealUp$ read through $E$ must pass through $W$ and $G$, and any read through $E'$ must pass through $W'$ and $G'$, together with the displayed transport, route, provenance, and local naming rows.

Finally use ledger non-escape. Since quotient streams, selected host limits, choice, and real-completeness data are not coordinates of either carrier, transport stability can only assert equality of the displayed finite-window route and its public Real-facing factorization.
\end{proof}

\begin{theorem}[Transported stationary window-seal public export certificate]
\label{thm:transported-stationary-window-seal-public-export-certificate}
For every accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
\autoref{thm:transported-stationary-window-seal-namecert-obligations}, \autoref{thm:transported-stationary-window-seal-readback-determinacy}, \autoref{thm:transported-stationary-window-seal-real-factorization}, \autoref{thm:transported-stationary-window-seal-uniqueness}, \autoref{thm:transported-stationary-window-seal-ledger-nonescape}, and \autoref{thm:transported-stationary-window-seal-obligation-package} assemble one public export certificate over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Every $\RealUp$-facing read factors through the finite public rows $R,S,G,W,E,H,C,P,N$, and the certificate exports no quotient stream, host equality, selected limit, choice principle, or real-completeness datum.
\end{theorem}
\begin{proof}
Project the accepted packet as $T=(R,S,G,W,E,H,C,P,N)$. The NameCert obligation theorem supplies the carrier, classifier, transported-window exactness, seal-exhaustion, and non-escape clauses for exactly these rows.

The readback determinacy theorem fixes the $\RatUp$ input, the $\StreamNameUp$ stationary window, and the $\RegSeqRatUp$ seal as one finite route. The Real factorization theorem then sends every $\RealUp$-facing consumer through the $\FiniteWindowEnvelopeUp$ row and that same seal route.

Use uniqueness to identify the public row determined by the displayed carrier and classifier data. The obligation package collects these clauses into one consumer-facing certificate, and ledger non-escape removes every coordinate not present in $R,S,G,W,E,H,C,P,N$.

Thus the exported certificate is public over the listed BEDC kernel and setup objects, while quotient streams, host equality, selected limits, choice, and real-completeness data remain outside its ledger surface.
\end{proof}

\begin{theorem}[Transported stationary window-seal obligation readiness]
\label{thm:transported-stationary-window-seal-obligation-readiness}
For every accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
the checked $\NameCert$ obligations, readback determinacy, and $\RealUp$ factorization rows assemble the obligation-readiness package for the Real and Completion route over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. This package consists of carrier admission for $T$, componentwise public readback from $R$ through $S$ and $G$, factorization of every $\RealUp$-facing read through $W$ and $G$, the public export certificate, and ledger non-escape. It does not claim RealUp completeness, quotient-stream equality, host equality, selected limits, or choice.
\end{theorem}
\begin{proof}
The carrier and classifier surface is the checked NameCert obligation row of \autoref{thm:transported-stationary-window-seal-namecert-obligations}. It supplies the finite packet coordinates, the componentwise comparison rule, transported-window exactness, seal exhaustion, and the non-escape boundary.

The readback and real-facing routes are the two other checked rows. Apply \autoref{thm:transported-stationary-window-seal-readback-determinacy} to fix the transported $\RatUp$ row, constant $\StreamNameUp$ window, and $\RegSeqRatUp$ finite-window seal as one public readback route. Apply \autoref{thm:transported-stationary-window-seal-real-factorization} to force every $\RealUp$ read through the displayed $\FiniteWindowEnvelopeUp$ packet and the same regular-rational seal row.

It remains to package these rows as the obligation-readiness surface. The public export certificate of \autoref{thm:transported-stationary-window-seal-public-export-certificate} collects the carrier, readback, factorization, uniqueness, and package clauses over the listed BEDC objects, while \autoref{thm:transported-stationary-window-seal-ledger-nonescape} blocks quotient streams, host equality, choice, selected limits, and real-completeness data. Therefore the accepted packet exposes exactly the finite readiness package stated above.
\end{proof}

\begin{theorem}[Transported stationary window-seal window source lock]
\label{thm:transported-stationary-window-seal-window-source-lock}
For every accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N),
$$
the rows $R,S,G$ form one stationary source route through the displayed transport row $H$, continuation row $C$, provenance row $P$, and local naming row $N$. The $\RatUp$ classifier row $R$, constant $\StreamNameUp$ window row $S$, and $\RegSeqRatUp$ seal row $G$ cannot be read as separate sources by a downstream $\RealUp$ or $\CompletionUp$ consumer.
\end{theorem}
\begin{proof}
Project the accepted carrier $T$. The rows $R$, $S$, and $G$ are listed in the same finite packet as the transport, continuation, provenance, and naming rows.

Apply \autoref{thm:transported-stationary-window-seal-readback-determinacy}. It fixes the transported $\RatUp$ row, the constant $\StreamNameUp$ window, and the $\RegSeqRatUp$ seal as one public readback route.

Now use the package and ledger rows. The route can be transported through $H$, followed through $C$, named by $P$, and exposed by $N$, but there is no carrier coordinate that separates $R$, $S$, or $G$ into an independent source for a downstream consumer.
\end{proof}

\begin{theorem}[Transported stationary window-seal stationary diagonal handoff]
\label{thm:transported-stationary-window-seal-stationary-diagonal-handoff}
For every accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the $\FiniteWindowEnvelopeUp$ row $W$ consumes the same stationary source route $R,S,G,H,C,P,N$ before the $\RealUp$ seal row $E$ is read. Thus the diagonal window handoff is ordered:
$$
\begin{aligned}
  (R,S,G,H,C,P,N)
  \longrightarrow W
  \longrightarrow E .
\end{aligned}
$$
No consumer may bypass $W$ and read $E$ directly from an ambient stream, quotient equality, selected limit, or completeness datum.
\end{theorem}
\begin{proof}
Begin with the source route of \autoref{thm:transported-stationary-window-seal-window-source-lock}. It identifies the transported rational row, stationary window row, and regular-rational seal row as one displayed route through $H,C,P,N$.

Apply \autoref{thm:transported-stationary-window-seal-real-factorization}. Every $\RealUp$-facing read must factor through the $\FiniteWindowEnvelopeUp$ row $W$ and the same regular-rational seal route.

The seal row $E$ is therefore downstream of $W$. Ledger non-escape rules out a direct route from an ambient stream, quotient equality, selected limit, or completeness datum into $E$, so the displayed handoff has exactly the order stated above.
\end{proof}

\begin{theorem}[Transported stationary window-seal Real seal non-escape package]
\label{thm:transported-stationary-window-seal-real-seal-nonescape-package}
For every accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the Real and RegSeqRat handoff package is the finite assembly of the carrier row, readback determinacy, Real-facing factorization, uniqueness, public export certificate, and ledger non-escape:
\begin{enumerate}
\item the carrier is exactly $R,S,G,W,E,H,C,P,N$;
\item readback determinacy locks $R,S,G$ to one transported stationary source route;
\item Real-facing factorization sends every seal read through $W$ and $G$ before $E$;
\item uniqueness identifies the public row determined by the displayed carrier and classifier data;
\item the ledger exports no quotient stream, host equality, selected limit, choice principle, or real-completeness datum.
\end{enumerate}
\end{theorem}
\begin{proof}
The carrier clause is the packet of \autoref{def:transported-stationary-window-seal-carrier}, and the NameCert obligations are those of \autoref{thm:transported-stationary-window-seal-namecert-obligations}.

Readback determinacy is supplied by \autoref{thm:transported-stationary-window-seal-readback-determinacy}; the source-lock theorem above repackages that determinacy as a single route through $H,C,P,N$.

For the Real-facing side, use \autoref{thm:transported-stationary-window-seal-stationary-diagonal-handoff}. It combines the Real factorization theorem with the $\FiniteWindowEnvelopeUp$ row, forcing the seal row $E$ to be downstream of $W$ and $G$.

Finally apply \autoref{thm:transported-stationary-window-seal-uniqueness}, \autoref{thm:transported-stationary-window-seal-public-export-certificate}, and \autoref{thm:transported-stationary-window-seal-ledger-nonescape}. These give the public row, the export package, and the non-escape boundary, which together form the exact Real and RegSeqRat handoff package stated above.
\end{proof}

\begin{theorem}[Transported stationary window-seal envelope window exactness]
\label{thm:transported-stationary-window-seal-envelope-window-exactness}
For every accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the $\FiniteWindowEnvelopeUp$ row $W$ may be read only after the stationary source route
$$
\begin{aligned}
  R \longmapsto S \longmapsto G
\end{aligned}
$$
has been fixed through the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The row $W$ is therefore the finite envelope over the transported $\RatUp$ classifier row $R$, the constant $\StreamNameUp$ window row $S$, and the $\RegSeqRatUp$ seal row $G$, and it must be read before the $\RealUp$ seal row $E$ is available to any completion-facing or real-facing consumer.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only rows available before the envelope are the displayed stationary route $R,S,G$ and its transport, continuation, package, and local naming provenance $H,C,P,N$.

Apply \autoref{thm:transported-stationary-window-seal-window-source-lock}. It binds $R$, $S$, and $G$ as one public source route, so a consumer cannot select an independent stream window or regular-rational seal before reading $W$.

Next use \autoref{thm:transported-stationary-window-seal-stationary-diagonal-handoff}. It places the $\FiniteWindowEnvelopeUp$ row after the stationary route and before the $\RealUp$ seal row.

Finally, \autoref{thm:transported-stationary-window-seal-ledger-nonescape} removes the possible bypasses. Quotient streams, host equality, selected limits, choice, and real-completeness data are not carrier coordinates, so $W$ is exactly the envelope window at the stated boundary.
\end{proof}

\begin{theorem}[Transported stationary window-seal constant-window triad]
\label{thm:transported-stationary-window-seal-constant-window-triad}
For every accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the rows $R$, $S$, and $G$ form one constant-window triad under the displayed rows $H,C,P,N$. The transported $\RatUp$ classifier, the constant $\StreamNameUp$ window, and the $\RegSeqRatUp$ finite-window seal are transported, replayed, packaged, and named together; no downstream $\FiniteWindowEnvelopeUp$, $\RealUp$, or $\CompletionUp$ consumer may read one of the three as a separate source.
\end{theorem}
\begin{proof}
Start from the carrier and classifier obligations in \autoref{thm:transported-stationary-window-seal-namecert-obligations}. They admit exactly the rows $R,S,G,W,E,H,C,P,N$ and compare accepted packets componentwise through the displayed transport and continuation rows.

Apply \autoref{thm:transported-stationary-window-seal-readback-determinacy}. It identifies the readback of the transported rational row with the constant stream window and the regular-rational seal, all through the same public route.

Use \autoref{thm:transported-stationary-window-seal-window-source-lock} to repackage that route under $H,C,P,N$. The structural rows can transport, replay, package, and name the triad, but they do not split it into separate source certificates.

The envelope and Real-facing rows consume the triad only through \autoref{thm:transported-stationary-window-seal-envelope-window-exactness} and \autoref{thm:transported-stationary-window-seal-real-factorization}. Thus any completion-facing or real-facing read sees $R,S,G$ as one constant-window triad.
\end{proof}

\begin{theorem}[Transported stationary window-seal completion consumer boundary]
\label{thm:transported-stationary-window-seal-completion-consumer-boundary}
Every completion-facing or real-facing consumer of an accepted $\TransportedStationaryWindowSealUp$ packet factors through the finite public packet
$$
\begin{aligned}
  R,S,G,W,E,H,C,P,N .
\end{aligned}
$$
The consumer first reads the constant-window triad $R,S,G$, then the envelope row $W$, then the seal row $E$, with $H,C,P,N$ carrying only displayed transport, continuation, package, and local naming data. The packet provides no quotient stream, host equality, selected limit, choice principle, or real-completeness datum.
\end{theorem}
\begin{proof}
First assemble the source side. By \autoref{thm:transported-stationary-window-seal-constant-window-triad}, the transported rational row, constant window row, and regular-rational seal row are one finite source triad under $H,C,P,N$.

Next assemble the envelope side. Apply \autoref{thm:transported-stationary-window-seal-envelope-window-exactness}; it forces each completion-facing or real-facing route to read $W$ after $R,S,G$ and before $E$.

Now apply the checked Real-facing package. The Real factorization theorem, the stationary diagonal handoff theorem, and \autoref{thm:transported-stationary-window-seal-real-seal-nonescape-package} bind the seal read to the same rows $R,S,G,W,E,H,C,P,N$.

Finally close the consumer boundary with \autoref{thm:transported-stationary-window-seal-ledger-nonescape}. Since quotient streams, host equality, selected limits, choice, and real-completeness data are absent from the packet, every consumer route factors through the displayed finite BEDC packet and nothing outside it.
\end{proof}

\begin{theorem}[Transported stationary window-seal tail-cofinality handoff]
\label{thm:transported-stationary-window-seal-tail-cofinality-handoff}
Let $T=(R,S,G,W,E,H,C,P,N)$ be an accepted $\TransportedStationaryWindowSealUp$ packet, and let $Q=(E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T)$ be an accepted $\TailCofinalityScheduleUp$ packet consumed on the same finite $\StreamNameUp$ window and $\RegSeqRatUp$ seal row. Every completion-facing or real-facing read first factors through the transported stationary triad
$$
R \longmapsto S \longmapsto G,
$$
then through the scheduled tail-cofinality route
$$
E_T \longmapsto W_T \longmapsto D_T \longmapsto R_T \longmapsto L_T
$$
with structural rows $H_T,C_T,P_T,N_T$, and only then through the finite envelope $W$ and seal row $E$ of $T$. The combined handoff exports no quotient-stream equality, selected host limit, host real equality, choice principle, hidden tail oracle, ambient completeness, or second completion route.
\end{theorem}
\begin{proof}
Begin with the local stationary source. By \autoref{thm:transported-stationary-window-seal-constant-window-triad}, the rows $R,S,G$ are one transported $\RatUp$, $\StreamNameUp$, and $\RegSeqRatUp$ triad under the displayed $H,C,P,N$ rows.

Next apply \autoref{thm:transported-stationary-window-seal-envelope-window-exactness}. It places the finite envelope row $W$ downstream from that triad and upstream from the local seal row $E$, so the transported packet has no route that reaches $W$ or $E$ before the triad is fixed.

Now read the schedule packet on the same window and seal row. The real-seal readback theorem \autoref{thm:tail-cofinality-schedule-real-seal-readback} routes every schedule-facing $\RealUp$ read through $E_T,W_T,D_T,R_T,L_T$, and \autoref{thm:tail-cofinality-schedule-scoped-binding} keeps the same route inside $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and the local $\NameCert$ row.

For completion-facing consumers, use \autoref{thm:tail-cofinality-schedule-regular-cauchy-completion-route}. It places the completion seal read after the same scheduled route, and \autoref{thm:tail-cofinality-schedule-shared-tail-budget-factorization} keeps the shared finite window and tail budget as the unique displayed factorization for the participating Real and Completion rows.

Finally combine the schedule exclusions with \autoref{thm:transported-stationary-window-seal-ledger-nonescape}. Neither packet contains a coordinate for quotient-stream equality, selected host limits, host real equality, choice, hidden tail oracles, ambient completeness, or another completion route. Thus every completion-facing or real-facing consumer must pass through the transported triad, the schedule rows, and then the finite envelope and seal row in the order stated.
\end{proof}

\begin{theorem}[Transported stationary window-seal root envelope exhaustion]
\label{thm:transported-stationary-window-seal-root-envelope-exhaustion}
Every root consumer read of an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N)
$$
passes first through the transported seed route
$$
\begin{aligned}
  R,S,G,H,C,P,N,
\end{aligned}
$$
then through the finite envelope row $W$, and then through the $\RealUp$-facing seal row $E$. Any $\CompletionUp$-facing or tail-cofinality consumer that accepts the packet must reuse this same envelope route:
$$
\begin{aligned}
  (R,S,G,H,C,P,N) \longrightarrow W \longrightarrow E .
\end{aligned}
$$
It cannot introduce quotient streams, host equality, selected limits, choice, hidden tail oracles, real-completeness data, or a second completion route.
\end{theorem}
\begin{proof}
Start with the transported seed. By \autoref{thm:transported-stationary-window-seal-constant-window-triad}, the rows $R,S,G$ form one constant-window triad carried only by the displayed transport, continuation, package, and local naming rows $H,C,P,N$.

Next read the finite envelope. Apply \autoref{thm:transported-stationary-window-seal-envelope-window-exactness}; it places $W$ downstream from the transported triad and upstream from the $\RealUp$ seal row $E$. The root read therefore reaches $E$ only after the same finite envelope has been fixed.

For completion-facing consumers, use \autoref{thm:transported-stationary-window-seal-completion-consumer-boundary}. It forces each completion-facing or real-facing route to factor through $R,S,G,W,E,H,C,P,N$, with $W$ between the source triad and the seal row. For tail-cofinality consumers, \autoref{thm:transported-stationary-window-seal-tail-cofinality-handoff} keeps the schedule rows downstream of the transported triad and upstream of the same envelope and seal rows.

Finally apply \autoref{thm:transported-stationary-window-seal-ledger-nonescape}. Since quotient streams, host equality, selected limits, choice, hidden tail oracles, real-completeness data, and second completion routes are not packet coordinates, every root consumer read is exhausted by the displayed route.
\end{proof}

\begin{theorem}[Transported stationary window-seal root readback totality]
\label{thm:transported-stationary-window-seal-root-readback-totality}
Every root-facing read of an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N)
$$
passes through the transported $\RatUp$ row $R$, the constant $\StreamNameUp$ window $S$, the $\RegSeqRatUp$ seal row $G$, the $\FiniteWindowEnvelopeUp$ packet $W$, and the $\RealUp$ seal row $E$ in that order. Thus the root-facing readback route is total exactly along
$$
\begin{aligned}
  R \longrightarrow S \longrightarrow G \longrightarrow W \longrightarrow E,
\end{aligned}
$$
with $H,C,P,N$ carrying only the displayed transport, continuation, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Unpack the carrier in \autoref{def:transported-stationary-window-seal-carrier}. Its visible coordinates already separate the transported rational seed row $R$, the constant stream window $S$, the regular-rational seal row $G$, the finite envelope $W$, and the real-facing seal row $E$.

The seed-window part is fixed by \autoref{thm:transported-stationary-window-seal-seed-window-readback}. It makes every root-facing seed read pass from the transported $\RatUp$ row to the induced $\StreamNameUp$ window and then to the $\RegSeqRatUp$ seal row.

Next apply \autoref{thm:transported-stationary-window-seal-real-factorization} and \autoref{thm:transported-stationary-window-seal-envelope-window-exactness}. These place $W$ after the same seed-window-seal route and place the $\RealUp$ seal row $E$ after $W$.

No other root-facing coordinate remains. The transport, continuation, provenance, and local naming rows $H,C,P,N$ only record the displayed route, so the readback is total along the stated order.
\end{proof}

\begin{theorem}[Transported stationary window-seal root package exactness]
\label{thm:transported-stationary-window-seal-root-package-exactness}
For every accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the package row $P$ is exactly the provenance boundary over
$$
\begin{aligned}
  \RatUp,\StreamNameUp,\RegSeqRatUp,\FiniteWindowEnvelopeUp,\RealUp,
  \BHist,\hsame,\Cont,\NameCert .
\end{aligned}
$$
It records no provenance outside these rows, and every public export of the packet reads $P$ only through this displayed boundary.
\end{theorem}
\begin{proof}
Begin with the obligation surface in \autoref{thm:transported-stationary-window-seal-namecert-obligations}. Its carrier, classifier, transported-window, seal-exhaustion, and non-escape clauses list exactly the source rows used by the packet.

The public export certificate \autoref{thm:transported-stationary-window-seal-public-export-certificate} then collects these rows over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The only package coordinate available in the carrier is $P$.

Therefore $P$ is the provenance boundary for the displayed BEDC objects. Since the same certificate excludes quotient streams, host equality, selected limits, choice, and real-completeness data, no external provenance can be imported through the package row.
\end{proof}

\begin{theorem}[Transported stationary window-seal root consumer nonescape]
\label{thm:transported-stationary-window-seal-root-consumer-nonescape}
Every downstream $\RealUp$ or $\CompletionUp$ consumer of an accepted $\TransportedStationaryWindowSealUp$ packet
$$
T=(R,S,G,W,E,H,C,P,N)
$$
factors through the displayed finite packet. Such a consumer cannot import quotient streams, host equality, choice, selected host limits, hidden tail oracles, real-completeness data, or a second completion route.
\end{theorem}
\begin{proof}
Start with \autoref{thm:transported-stationary-window-seal-ledger-nonescape}. It inspects the finite packet ledger and removes quotient streams, host equality, selected limits, choice, hidden tail oracles, real-completeness data, and alternate completion routes from the available coordinates.

For completion-facing reads, apply \autoref{thm:transported-stationary-window-seal-completion-consumer-boundary}. It forces each $\CompletionUp$ or $\RealUp$ consumer to factor through $R,S,G,W,E,H,C,P,N$ rather than through an ambient completion object.

Finally use \autoref{thm:transported-stationary-window-seal-shared-window-budget-exhaustion}. The shared $\StreamNameUp$ window and $\RegSeqRatUp$ seal budget is exhausted by the displayed packet and its schedule-facing rows, so no downstream consumer can attach another source outside the finite route.
\end{proof}

\begin{theorem}[Transported stationary window-seal tail schedule envelope order]
\label{thm:transported-stationary-window-seal-tail-schedule-envelope-order}
Let
$$
T=(R,S,G,W,E,H,C,P,N)
$$
be an accepted $\TransportedStationaryWindowSealUp$ packet, and let
$$
Q=(E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T)
$$
be an accepted $\TailCofinalityScheduleUp$ packet consumed on the same finite $\StreamNameUp$ window and $\RegSeqRatUp$ seal row. Every accepted completion-facing or real-facing packet read must encounter the transported rows $R,S,G$ before the TailCofinalitySchedule rows $E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T$, and must encounter those schedule rows before the envelope and seal rows $W,E$ of $T$. Hence the only exposed root-to-completion order is
$$
\begin{aligned}
  R,S,G,H,C,P,N
  \longrightarrow
  E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T
  \longrightarrow
  W
  \longrightarrow
  E .
\end{aligned}
$$
\end{theorem}
\begin{proof}
First fix the transported source. By \autoref{thm:transported-stationary-window-seal-constant-window-triad}, the rows $R,S,G$ are the constant transported $\RatUp$, $\StreamNameUp$, and $\RegSeqRatUp$ triad under the displayed $H,C,P,N$ rows. Thus any accepted read that uses the packet begins with the transported triad rather than with a schedule row.

Next attach the shared tail schedule. The handoff theorem \autoref{thm:transported-stationary-window-seal-tail-cofinality-handoff} places the TailCofinalitySchedule route after the transported triad for both completion-facing and real-facing consumers, while retaining the same finite $\StreamNameUp$ window and $\RegSeqRatUp$ seal row.

Finally apply \autoref{thm:transported-stationary-window-seal-root-envelope-exhaustion}. It places the finite envelope row $W$ and the $\RealUp$-facing seal row $E$ after every transported root read. Combining the three constraints gives exactly the displayed order and leaves no earlier access to $W$ or $E$.
\end{proof}

\begin{theorem}[Transported stationary window-seal shared window budget exhaustion]
\label{thm:transported-stationary-window-seal-shared-window-budget-exhaustion}
For any accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$ and any accepted $\TailCofinalityScheduleUp$ packet $Q=(E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T)$ consumed on its same finite window and seal row, the shared $\StreamNameUp$ window budget and $\RegSeqRatUp$ seal budget are exhausted by
$$
\begin{aligned}
  R,S,G,H,C,P,N,
  E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T,
  W,E .
\end{aligned}
$$
No accepted consumer can append a hidden tail oracle, a second $\StreamNameUp$ family, an additional $\RegSeqRatUp$ seal row, quotient-stream equality, selected host limits, host equality, choice, or ambient real-completeness data to that budget.
\end{theorem}
\begin{proof}
The root packet owns the finite window and seal budget through \autoref{thm:transported-stationary-window-seal-envelope-window-exactness}: the transported triad reaches $W$ and $E$ only through the displayed $\StreamNameUp$ and $\RegSeqRatUp$ rows. Thus the root side has no spare coordinate where a separate window family or seal family could be stored.

The tail-cofinality handoff \autoref{thm:transported-stationary-window-seal-tail-cofinality-handoff} then consumes the schedule packet on that same window and seal row. Its schedule rows therefore refine the already displayed budget instead of creating a new one.

It remains to exclude external resources. Apply \autoref{thm:transported-stationary-window-seal-ledger-nonescape}; the packet ledger has no coordinate for hidden tail oracles, quotient-stream equality, selected limits, host equality, choice, ambient completeness, or another completion route. Hence the displayed rows exhaust the shared window and seal budget.
\end{proof}

\begin{theorem}[Transported stationary window-seal completion route uniqueness]
\label{thm:transported-stationary-window-seal-completion-route-uniqueness}
Let $T$ and $Q$ be as in \autoref{thm:transported-stationary-window-seal-shared-window-budget-exhaustion}. Any completion-facing consumer and any $\RealUp$-facing consumer that preserve the displayed transported rows, schedule rows, finite envelope, and seal row determine the same route
$$
\begin{aligned}
  (R,S,G,H,C,P,N)
  \longrightarrow
  (E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T)
  \longrightarrow
  W
  \longrightarrow
  E .
\end{aligned}
$$
Consequently the transported stationary window-seal packet has a unique completion route at the exposed NameCert boundary.
\end{theorem}
\begin{proof}
Start at the consumer boundary. By \autoref{thm:transported-stationary-window-seal-completion-consumer-boundary}, every completion-facing or real-facing consumer of $T$ factors through the displayed transported rows and cannot bypass $R,S,G,W,E,H,C,P,N$.

The root envelope exhaustion theorem \autoref{thm:transported-stationary-window-seal-root-envelope-exhaustion} fixes the root route from the transported rows to $W$ and then to $E$. The tail schedule envelope order theorem \autoref{thm:transported-stationary-window-seal-tail-schedule-envelope-order} inserts the schedule rows in the only position compatible with both the transported triad and the envelope boundary.

Finally \autoref{thm:transported-stationary-window-seal-shared-window-budget-exhaustion} rules out any second window family, second seal budget, hidden tail oracle, or ambient completion datum. Two consumers preserving the displayed rows therefore preserve the same finite route, so the completion route is unique at the exposed boundary.
\end{proof}

\begin{theorem}[Transported stationary window-seal schedule-envelope determinacy]
\label{thm:transported-stationary-window-seal-completion-schedule-envelope-determinacy}
Let $T=(R,S,G,W,E,H,C,P,N)$ be an accepted $\TransportedStationaryWindowSealUp$ packet and let $Q=(E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T)$ be an accepted $\TailCofinalityScheduleUp$ packet consumed on the same $\StreamNameUp$ window and $\RegSeqRatUp$ seal row. Any completion-facing or $\RealUp$-facing consumer preserving the transported source rows $R,S,G$ and the tail schedule rows of $Q$ must read the finite envelope row $W$ before it reads the $\RealUp$ seal row $E$.

Equivalently, the consumer route has the deterministic exposed order
$$
\begin{aligned}
  R,S,G,H,C,P,N
  \longrightarrow
  E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T
  \longrightarrow
  W
  \longrightarrow
  E .
\end{aligned}
$$
\end{theorem}
\begin{proof}
The transported source is fixed by the constant-window triad and the tail-cofinality handoff. These place the $\RatUp$, $\StreamNameUp$, and $\RegSeqRatUp$ rows before the schedule packet and force the schedule packet to use the same displayed finite window and seal row.

The finite envelope position is then fixed by \autoref{thm:transported-stationary-window-seal-root-envelope-exhaustion}. It sends every accepted root read through $W$ before the $\RealUp$-facing seal row $E$.

Finally \autoref{thm:transported-stationary-window-seal-tail-schedule-envelope-order} and \autoref{thm:transported-stationary-window-seal-shared-window-budget-exhaustion} exclude a second schedule, a second finite envelope, or an earlier seal read. The displayed order is therefore the only deterministic completion-facing order.
\end{proof}

\begin{theorem}[Transported stationary window-seal Real RegSeqRat source totality]
\label{thm:transported-stationary-window-seal-real-regseqrat-source-totality}
For an accepted $\TransportedStationaryWindowSealUp$ packet $T=(R,S,G,W,E,H,C,P,N)$, the $\RealUp$-facing seal row $E$ is sourced only through the transported $\RatUp$, $\StreamNameUp$, and $\RegSeqRatUp$ triad $R,S,G$ together with the displayed transport, package, and NameCert rows $H,C,P,N$. No $\RealUp$ consumer of $E$ can draw a source from quotient-stream equality, host equality, selected limits, choice, ambient real completeness, hidden tail oracles, or an independent completion object.
\end{theorem}
\begin{proof}
Start with the envelope exactness theorem. The row $E$ is exposed as the seal-facing $\RealUp$ row reached from the finite $\StreamNameUp$ envelope $W$ and the $\RegSeqRatUp$ seal row, and both are carried by the transported triad $R,S,G$.

The provenance and nonescape theorems then identify the available BEDC rows: $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\FiniteWindowEnvelopeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. There is no additional carrier coordinate from which another real source could be read.

For completion-facing uses, \autoref{thm:transported-stationary-window-seal-tail-cofinality-handoff} and \autoref{thm:transported-stationary-window-seal-completion-consumer-boundary} keep the same source triad upstream of the shared schedule and seal. Thus every $\RealUp$-facing read of $E$ is total over exactly the displayed transported source route.
\end{proof}

\begin{theorem}[Transported stationary window-seal NameCert route exhaustion]
\label{thm:transported-stationary-window-seal-namecert-route-exhaustion}
Let $T$ and $Q$ be as above. The exposed $\NameCert$ route surface of $\TransportedStationaryWindowSealUp$ is exhausted by the transported source triad, the tail schedule packet, the finite envelope, and the $\RealUp$ seal row:
$$
\begin{aligned}
  (R,S,G,H,C,P,N),
  (E_T,W_T,D_T,R_T,L_T,H_T,C_T,P_T,N_T),
  W,E .
\end{aligned}
$$
Every accepted completion-facing or $\RealUp$-facing consumer preserving these rows determines the unique route of \autoref{thm:transported-stationary-window-seal-completion-route-uniqueness}, and every $\RealUp$ seal source is the triad-total source of \autoref{thm:transported-stationary-window-seal-real-regseqrat-source-totality}.
\end{theorem}
\begin{proof}
First apply \autoref{thm:transported-stationary-window-seal-completion-schedule-envelope-determinacy}. It fixes the order from the transported rational source through the tail schedule and finite envelope to the $\RealUp$ seal.

Next apply \autoref{thm:transported-stationary-window-seal-real-regseqrat-source-totality}. It identifies the source of the seal row with the transported $\RatUp$, $\StreamNameUp$, and $\RegSeqRatUp$ triad, with no external real or completion source.

Finally use \autoref{thm:transported-stationary-window-seal-completion-route-uniqueness}. Since the shared window budget is already exhausted, the NameCert surface has no remaining coordinate for another source, schedule, envelope, or seal. The displayed rows therefore exhaust the deterministic route surface.
\end{proof}

\input{parts/concrete_instances/transportedstationarywindowseal/source_triad_and_handoff_totality}
\input{parts/concrete_instances/transportedstationarywindowseal/root_obligation_route_completion}
\input{parts/concrete_instances/transportedstationarywindowseal/root_completion_source_order}
\input{parts/concrete_instances/transportedstationarywindowseal/l10_handoff_obligation_basis}
\input{parts/concrete_instances/transportedstationarywindowseal/root_completion_obligation_closure}
\input{parts/concrete_instances/transportedstationarywindowseal/root_downstream_readiness_surface}
\input{parts/concrete_instances/transportedstationarywindowseal/completion_source_triad_obligations}
\input{parts/concrete_instances/transportedstationarywindowseal/real_completion_route_obligations}
\input{parts/concrete_instances/transportedstationarywindowseal/root_l10_obligation_surface}
\input{parts/concrete_instances/transportedstationarywindowseal/l10_exit_readiness_packet}
\input{parts/concrete_instances/transportedstationarywindowseal/root_readiness_package}
\input{parts/concrete_instances/transportedstationarywindowseal/root_unblock_obligation_rows}
\input{parts/concrete_instances/transportedstationarywindowseal/source_triad_completion_packet}
\input{parts/concrete_instances/transportedstationarywindowseal/source_triad_l10_exit_readiness}
\input{parts/concrete_instances/transportedstationarywindowseal/checked_obligation_basis}
\input{parts/concrete_instances/transportedstationarywindowseal/open_phase_root_obligation_triad}
\input{parts/concrete_instances/transportedstationarywindowseal/constant_window_real_exit_readiness}
\input{parts/concrete_instances/transportedstationarywindowseal/scoped_dependency_basis}

\closureat{TransportedStationaryWindowSealUp}{obligationStr}[BEDC.Derived.TransportedStationaryWindowSealUp.TransportedStationaryWindowSealPacket\_namecert\_obligations]

\begin{closurestatus}{\TransportedStationaryWindowSealUp}
  \constructivestory{A $\TransportedStationaryWindowSealUp$ packet is generated from a transported RatUp classifier, constant StreamNameUp finite window, RegSeqRatUp seal row, FiniteWindowEnvelopeUp observation packet, RealUp seal-facing row, displayed transports, package provenance, and local naming.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The scoped route is closed over RatUp, StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, FiniteWindowEnvelopeUp, TailCofinalityScheduleUp, RealUp, CompletionUp, BHist, hsame, Cont, Pkg, and NameCert, read through the finite packet rows $R,S,G,W,E,H,C,P,N$.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.TransportedStationaryWindowSealUp.TransportedStationaryWindowSealPacket\_namecert\_obligations}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close full real completeness, quotient-stream equality, host real equality, selected host limits, or choice principles.}
  \upgradepath{Public closure requires a public export theorem for this scoped transported window-seal route.}
\end{closurestatus}
