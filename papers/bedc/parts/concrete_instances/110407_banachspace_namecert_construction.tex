\chapter{A Concrete Naming Certificate for $\BanachSpaceUp$}
\label{ch:concrete-instances-banachspace-namecert}
\origin{human}

Following Banach and Bishop--Bridges on constructive normed linear spaces, the $\BanachSpaceUp$ packet records the finite NameCert surface for a normed vector carrier whose Cauchy-completion service is displayed before downstream algebra, operator, Hilbert, or fixed-point consumers read it as complete. It sits over the vector-space carrier of \autoref{ch:concrete-instances-vecspace-namecert}, the norm rows of \autoref{ch:concrete-instances-norm-namecert}, the Cauchy-net completion route of \autoref{ch:concrete-instances-cauchynetcompletion-namecert}, the metric-completion handoff of \autoref{ch:concrete-instances-metriccompletion-namecert}, the separated-completion boundary of \autoref{ch:concrete-instances-separatedcompletion-namecert}, and the regular rational real-name spine of \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}. The packet names a constructive complete normed-space interface; it does not assert Hahn--Banach, choice, quotient completion equality, selected limits, or ambient $\RealUp$ completeness.

\begin{definition}[Banach space carrier]
\label{def:banach-space-carrier}
A $\BanachSpaceUp$ carrier is a finite $\BHist$ packet
$$
B=(V,N,M,Q,S,R,E,Z,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the $\VecSpaceUp$ vector-space carrier row, including zero, addition, inverse, and scalar-action readbacks for the named source.
\item $N$ is the $\NormUp$ row over $V$, exposing norm evaluation, triangle, zero, and scalar-compatibility obligations only as displayed finite rows.
\item $M$ is the metric comparison row induced by $N$, used by completion-facing consumers before any completeness read is public.
\item $Q$ is the $\CauchyNetCompletionUp$ or regular Cauchy request ledger listing the finite tail and net windows inspected by a completion read.
\item $S$ is the $\MetricCompletionUp$ handoff row for the displayed Cauchy requests over $M$.
\item $R$ is the $\RegSeqRatUp$ tolerance readback row attached to the same Cauchy windows and norm budgets.
\item $E$ is the terminal $\RealUp$ tolerance and distance seal reached after $R$ has been displayed.
\item $Z$ is the $\SeparatedCompletionUp$ comparison row, forcing equality-facing reads through separated metric evidence rather than host equality or quotient classes.
\item $H$ records componentwise $\hsame$ transport for $V,N,M,Q,S,R,E,Z$ and the local certificate rows.
\item $C$ records displayed $\Cont$ replay from normed-space requests through vector, norm, metric, Cauchy, completion, tolerance, real-seal, and separated-comparison rows.
\item $P$ is the $\Pkg$ provenance over $\BanachSpaceUp$, $\VecSpaceUp$, $\NormUp$, $\CauchyNetCompletionUp$, $\MetricCompletionUp$, $\SeparatedCompletionUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\BanachSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for an external complete metric space, quotient Cauchy classes, selected representatives, host vector equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.BanachSpaceUp.BanachSpaceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Banach space NameCert obligations]
\label{thm:banach-space-namecert-obligations}
For every accepted $\BanachSpaceUp$ carrier
$$
B=(V,N,M,Q,S,R,E,Z,H,C,P,L),
$$
the local $\NameCert_{\BanachSpaceUp}$ surface consists exactly of carrier admission for the vector-space row $V$, norm row $N$, induced metric row $M$, Cauchy request ledger $Q$, completion handoff $S$, regular rational tolerance readback $R$, terminal real seal $E$, separated comparison row $Z$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:banach-space-carrier}. The vector and norm rows provide the only linear and metric data available to the packet.

Completion-facing reads are then forced through the displayed route $M,Q,S,R,E$. The metric row is induced by the norm row, the Cauchy request ledger lists the finite windows being inspected, the completion handoff reads those windows, and the regular rational and Real rows expose the tolerance data consumed by downstream users.

The separated comparison row $Z$ fixes equality-facing reads after the completion handoff. Transport, replay, provenance, and local naming are exactly $H,C,P,L$, so no excluded external completion, quotient, selector, choice, host-equality, or ambient-completeness coordinate can be projected from the certificate.
\end{proof}

\begin{theorem}[Banach space Cauchy-completion handoff]
\label{thm:banach-space-cauchy-completion-handoff}
Every completion-facing consumer of an accepted $\BanachSpaceUp$ carrier must read the displayed route
$$
\VecSpaceUp,\ \NormUp,\ \MetricCompletionUp,\ \CauchyNetCompletionUp,\ \RegSeqRatUp,\ \RealUp,\ \SeparatedCompletionUp
$$
through the carrier rows $V,N,M,Q,S,R,E,Z$ before it may consume the structural rows $H,C,P,L$. The handoff supplies a constructive complete normed-space interface, not Hahn--Banach, selected limits, quotient completion equality, countable choice, host vector equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:banach-space-namecert-obligations}. They make $V$ and $N$ the only source of vector-space and norm data and force completion-facing reads to pass through the induced metric row $M$.

The Cauchy request ledger $Q$ and completion handoff $S$ are then read with the tolerance rows $R,E$. This records the RegSeqRat and Real readback of the completion service as displayed finite data.

Finally the separated-completion row $Z$ and the structural rows $H,C,P,L$ keep comparison, transport, replay, provenance, and naming on the same packet. Since the carrier contains no Hahn--Banach functional, selector, quotient-completion object, choice schedule, host equality, or ambient-completeness coordinate, every downstream Banach-space read is exhausted by the displayed handoff.
\end{proof}

\begin{theorem}[Banach space normed Cauchy ledger]
\label{thm:banach-space-normed-cauchy-ledger}
Every Cauchy request read from an accepted $\BanachSpaceUp$ carrier must expose the $\VecSpaceUp$ row $V$, the $\NormUp$ row $N$, the induced metric row $M$, and the Cauchy ledger row $Q$ before the completion handoff $S$ or terminal rows $R,E,Z$ are consumed. The request is therefore a normed finite-ledger read through $V,N,M,Q,S,R,E,Z,H,C,P,L$, not a selected-limit, quotient-class, or host-equality read.
\end{theorem}
\begin{proof}
Begin with the carrier inventory of \autoref{def:banach-space-carrier}. The only vector data are in $V$, the only norm data are in $N$, and the metric row $M$ is displayed beside them rather than supplied by an ambient metric space.

Apply \autoref{thm:banach-space-namecert-obligations}. The local certificate exposes $Q$ as the Cauchy request ledger before the completion handoff $S$ and the tolerance rows $R,E$ can be read.

Finally use \autoref{thm:banach-space-cauchy-completion-handoff}. It keeps $Z,H,C,P,L$ downstream of the displayed normed Cauchy ledger, so the carrier cannot export selected limits, quotient Cauchy classes, or host vector equality.
\end{proof}

\begin{theorem}[Banach space completion stability obligation]
\label{thm:banach-space-completion-stability-obligation}
For accepted $\BanachSpaceUp$ packets, componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the completion handoff route through $Q,S,R,E$. Any stable completion-facing consumer must read the same Cauchy request ledger, completion handoff, regular rational tolerance readback, and terminal Real seal before it reaches the local name.
\end{theorem}
\begin{proof}
Project $H$ and $C$ from \autoref{def:banach-space-carrier}. These rows transport and replay the displayed coordinates of the packet; they do not introduce a second completion object or an external selector.

By \autoref{thm:banach-space-namecert-obligations}, the rows $Q,S,R,E$ are part of the local NameCert surface. Transport and replay must therefore carry the Cauchy ledger, completion handoff, rational tolerance readback, and Real seal as named rows.

Apply \autoref{thm:banach-space-cauchy-completion-handoff}. The stable route remains $V,N,M,Q,S,R,E,Z,H,C,P,L$, so completion stability is a preservation obligation on the displayed handoff rather than a hidden completeness principle.
\end{proof}

\begin{theorem}[Banach space separated-completion boundary]
\label{thm:banach-space-separated-completion-boundary}
Every equality-facing consumer of an accepted $\BanachSpaceUp$ packet must pass through the $\SeparatedCompletionUp$ row $Z$ after the displayed completion route and before local naming. Such consumers cannot use host vector equality, quotient completion classes, selected limits, countable choice, trichotomy, Hahn--Banach, or ambient $\RealUp$ completeness as a replacement for $Z$.
\end{theorem}
\begin{proof}
Read $Z$ from the carrier of \autoref{def:banach-space-carrier}. It is the only separated-comparison coordinate available to equality-facing consumers.

Use \autoref{thm:banach-space-namecert-obligations} to place $Z$ in the local certificate surface with $V,N,M,Q,S,R,E,H,C,P,L$. The equality read must therefore pass through the displayed vector, norm, metric, Cauchy, completion, tolerance, and Real-seal rows before it reaches $Z$.

Finally invoke \autoref{thm:banach-space-cauchy-completion-handoff}. Since the carrier has no coordinate for host vector equality, quotient completion classes, selected limits, countable choice, trichotomy, Hahn--Banach, or ambient $\RealUp$ completeness, $Z$ is the boundary through which equality-facing Banach-space consumers must pass.
\end{proof}

\closureat{BanachSpaceUp}{seedStr}

\begin{closurestatus}{\BanachSpaceUp}
  \constructivestory{A $\BanachSpaceUp$ packet is generated from VecSpace rows, Norm rows, an induced metric comparison row, Cauchy request windows, completion handoff rows, RegSeqRat tolerance readback, Real seal rows, SeparatedCompletion comparison rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:banach-space-carrier}, \autoref{thm:banach-space-namecert-obligations}, and \autoref{thm:banach-space-cauchy-completion-handoff} seed the constructive complete normed-space packet over displayed vector, norm, metric, Cauchy, completion, tolerance, separated-comparison, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Hahn--Banach, selected limits, quotient completion equality, countable choice, host vector equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked vector, norm, metric, Cauchy, completion, tolerance, separated-comparison, transport, replay, provenance, and local naming rows for BEDC.Derived.BanachSpaceUp.}
\end{closurestatus}
