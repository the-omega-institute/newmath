\chapter{A Concrete Naming Certificate for $\RegularCauchyDominatedTailUp$}
\label{ch:concrete-instances-regular-cauchy-dominated-tail-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyDominatedTailUp$ packet records the finite L10 route by which a regular Cauchy source row, a displayed domination budget, and a tail row produce a regular rational readback before any real seal is consumed. It sits over the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and the comparison discipline of \autoref{ch:concrete-instances-regular-cauchy-comparison-test-namecert}. The packet names a dominated-tail certificate only as finite NameCert data; it is not a selected limit, quotient equality of Cauchy names, countable choice, trichotomy, host real comparison, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy dominated-tail carrier]
\label{def:regular-cauchy-dominated-tail-carrier}
A $\RegularCauchyDominatedTailUp$ carrier is a finite $\BHist$ packet
$$
K=(S,D,T,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source regular-Cauchy row whose inspected entries are read only through finite windows.
\item $D$ is the $\DyadicRatCoreUp$ dominance budget, recording the dyadic tolerance and domination margin used to compare the source tail with its displayed majorant.
\item $T$ is the dominated tail row, containing the tail index, the finite tail window, and the pointwise domination reads admitted for that window.
\item $W$ is the $\StreamNameUp$ window surface that lists every source and tail coordinate inspected before a regular readback is exported.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the source windows, tail windows, and dyadic budget.
\item $E$ is the terminal $\RealUp$ seal row, available only after $S,D,T,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source row, dominance budget, dominated tail row, stream windows, regular readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,D,T,W \longmapsto R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyDominatedTailUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RegularCauchyComparisonTestUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyDominatedTailUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,D,T,W,R,E,H,C,P,N$. It has no coordinate for a hidden limit selector, quotient equality of regular Cauchy names, countable choice, trichotomy on arbitrary rational tails, host real comparison, ambient $\RealUp$ completeness, Lean verification, or a standard bridge.
\end{definition}

\begin{theorem}[Regular Cauchy dominated-tail obligation surface]
\label{thm:regular-cauchy-dominated-tail-obligation-surface}
For every accepted $\RegularCauchyDominatedTailUp$ carrier
$$
K=(S,D,T,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyDominatedTailUp}$ surface consists exactly of carrier admission for the source regular-Cauchy row $S$, dyadic dominance budget $D$, dominated tail row $T$, stream-window surface $W$, regular readback $R$, real seal $E$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming $N$. The comparison-test use is finite: every domination read must be routed through the displayed $\RegularCauchyComparisonTestUp$ discipline on $S,D,T,W$ before $R$ or $E$ can be consumed.
\end{theorem}
\begin{proof}
Project the accepted carrier onto its ten rows. The source data are $S$, the dyadic tolerance and domination margin are $D$, and the dominated tail is $T$; no tail comparison is available outside these displayed rows.

The window and readback rows then fix the observation route. The row $W$ lists the finite stream coordinates inspected for the source and tail, while $R$ is the only regular rational readback attached to those windows and to the dyadic budget.

The real seal $E$ is downstream from $S,D,T,W,R$. Componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve exactly that route. Since the carrier has no extra coordinate, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Regular Cauchy dominated-tail L10 handoff]
\label{thm:regular-cauchy-dominated-tail-l10-handoff}
Every $\RealUp$-facing consumer of an accepted $\RegularCauchyDominatedTailUp$ carrier receives the finite L10 handoff
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegularCauchyComparisonTestUp,\ \RegSeqRatUp,\ \RealUp
$$
implemented by the displayed route $S,D,T,W,R,E,H,C,P,N$. The handoff certifies only that the dominated tail has been read through the dyadic budget and finite stream windows before the regular rational row and real seal are exposed; it exports no selected limit, quotient equality of Cauchy names, countable choice, host real comparison, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:regular-cauchy-dominated-tail-obligation-surface}. It forces the source regular-Cauchy row, dominance budget, dominated tail row, and stream windows to be displayed before any regular rational readback can be used.

The comparison-test row is consumed on that same finite surface. Its role is to check the tail domination against the dyadic budget, not to supply a host real comparison or a selected endpoint.

After the finite comparison is displayed, $R$ supplies the $\RegSeqRatUp$ readback and $E$ supplies the terminal $\RealUp$ seal. The structural rows $H,C,P,N$ transport, replay, source, and name the same route, so the public handoff is exactly the finite L10 certificate above.
\end{proof}

\closureat{RegularCauchyDominatedTailUp}{seedStr}

\begin{closurestatus}{\RegularCauchyDominatedTailUp}
  \constructivestory{A $\RegularCauchyDominatedTailUp$ packet is generated from a source regular-Cauchy row, a dyadic dominance budget, a dominated tail row, finite StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-dominated-tail-carrier}, \autoref{thm:regular-cauchy-dominated-tail-obligation-surface}, and \autoref{thm:regular-cauchy-dominated-tail-l10-handoff} seed the finite dominated-tail route from dyadic domination and stream windows through regular rational readback to the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient equality of Cauchy names, countable choice, trichotomy, host real comparison, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{A stronger status requires Lean coverage for carrier admission, dominated-tail comparison routing, regular rational readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyDominatedTailUp.}
\end{closurestatus}
