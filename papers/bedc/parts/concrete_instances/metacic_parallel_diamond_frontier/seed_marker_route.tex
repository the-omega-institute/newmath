\begin{theorem}[MetaCIC parallel-diamond frontier seed marker route]
\label{thm:metacic-parallel-diamond-frontier-seed-marker-route}
The mature and bridged hub markers for $\MetaCICParallelDiamondFrontierUp$ are read only through the child carrier, obligation surface, seed status, and closurestatus guard. Concretely, any reader entering from the hub's mature-status row or bridged-status row must first expose the carrier packet of \autoref{def:metacic-parallel-diamond-frontier-carrier}, the obligation rows of \autoref{thm:metacic-parallel-diamond-frontier-obligations}, the seed closure status of \autoref{thm:metacic-parallel-diamond-frontier-seed-closure-status}, and the guard route of \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}. The marker read therefore remains a finite residual-premise packet read, not a discharge of residual substitution, subject reduction, full Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the hub marker rows. They name a status entry for $\MetaCICParallelDiamondFrontierUp$, but the hub has no independent carrier coordinate and no proof body beyond its ordered child inputs.

Read the child carrier and obligation theorem next. The carrier displays the finite rows $P,K,J,R,S,C,B,O,H,T,G,N$, while the obligation theorem identifies the residual-premise, critical-pair, candidate-join, residual-frontier, bounded checker, obstruction, transport, replay, provenance, and local naming obligations exposed by those rows.

Finally apply the seed closure status and closurestatus guard. These force the status reader to consume the seed route and the guarded closurestatus export before using the mature or bridged marker. Since the exposed packet contains no coordinate for residual-substitution discharge, subject reduction, full Church--Rosser, evaluator equality, quotient term equality, or kernel consistency, the hub marker exports only the child finite residual-premise status route.
\end{proof}
