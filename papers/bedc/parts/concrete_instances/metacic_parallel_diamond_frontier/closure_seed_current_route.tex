\begin{theorem}[MetaCIC parallel-diamond frontier closure seed current route]
\label{thm:metacic-parallel-diamond-frontier-closure-seed-current-route}
The seed marker of $\MetaCICParallelDiamondFrontierUp$ is readable only through the carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier}, the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}, and the closurestatus guard of \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}. A status consumer must therefore expose the residual-substitution premise, critical-pair row, candidate-join row, bounded-check row, obstruction row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before it may treat the seed registration as available. The route does not claim subject reduction, full Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier}. Its displayed coordinates contain the residual-substitution premise, critical-pair data, candidate joins, bounded checker, obstruction row, and structural rows that any seed reader must expose.

Apply the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}. It makes the visible rows the local NameCert inventory for the packet, so the seed marker cannot be consumed as an independent confluence theorem.

Finally read the closurestatus guard of \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}. The guard routes every status reader through the same finite packet before seed or bridged markers are consumed, and the packet has no coordinate for subject reduction, full Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}
