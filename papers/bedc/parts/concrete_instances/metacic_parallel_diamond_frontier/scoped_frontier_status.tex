\begin{theorem}[MetaCIC parallel-diamond scoped frontier status]
\label{thm:metacic-parallel-diamond-frontier-scoped-frontier-status}
The scoped $\MetaCICParallelDiamondFrontierUp$ frontier is the finite route that begins with the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}, retains the seed-status route of \autoref{thm:metacic-parallel-diamond-frontier-seed-status-route}, and reads the public certificate and public closure of \autoref{thm:metacic-parallel-diamond-frontier-public-certificate} and \autoref{thm:metacic-parallel-diamond-frontier-public-closure}. It records the displayed residual-substitution premise, critical-pair, candidate-join, residual-frontier, checker, bounded-check, obstruction, transport, replay, provenance, and local naming rows while refusing residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, and kernel consistency.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}. It fixes the accepted packet through the finite route $P,K,J,R,S,C,B,O,H,T,G,N$ and leaves the residual-substitution premise as a displayed row rather than a discharged theorem.

Apply the seed-status route of \autoref{thm:metacic-parallel-diamond-frontier-seed-status-route}. The route keeps the same residual premise, critical-pair, candidate, residual-frontier, closed-checker, closed-normal, bounded-check, obstruction, transport, replay, provenance, and local naming rows visible before any seed consumer reads the frontier.

Finally use the public certificate and public closure, \autoref{thm:metacic-parallel-diamond-frontier-public-certificate} and \autoref{thm:metacic-parallel-diamond-frontier-public-closure}. These theorems expose the finite frontier as a public handoff only over the retained route, so none of the refused residual-substitution, subject-reduction, confluence, evaluator-equality, quotient-equality, global-normalization, or kernel-consistency endpoints is exported by the scoped status.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond scoped status register]
\label{thm:metacic-parallel-diamond-frontier-scoped-status-register}
The scoped status of $\MetaCICParallelDiamondFrontierUp$ is registered by the same finite frontier displayed in \autoref{thm:metacic-parallel-diamond-frontier-scoped-frontier-status}. In particular, the registered route is the retained residual-substitution premise, critical-pair row, candidate-join row, residual-frontier row, checker row, bounded-check row, obstruction row, transport row, replay row, provenance row, and local naming row, with no export of subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Read the scoped frontier status theorem. It already fixes the route through the obligation surface, seed-status route, public certificate, and public closure while retaining the residual-substitution premise as a visible row.

The status register does not add a new carrier coordinate. It records that this displayed route is the one later readers may cite, and it preserves the same refusals for subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, and kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond scoped route handoff]
\label{thm:metacic-parallel-diamond-frontier-scoped-route-handoff}
Every scoped public handoff from $\MetaCICParallelDiamondFrontierUp$ to MetaCIC critical-path or confluence-audit consumers must retain the finite rows
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
with the residual-substitution premise row $P$ visible before any candidate-join row $J$ or bounded-check row $B$ is consumed. The handoff may expose the frontier to public confluence consumers only as retained premise, critical-pair, candidate-join, residual-frontier, checker, bounded-check, obstruction, transport, replay, provenance, and local naming data. It does not discharge residual substitution, prove subject reduction, assert unrestricted confluence, identify evaluator equality, quotient term equality, global normalization, or certify kernel consistency.
\end{theorem}
\begin{proof}
Start with the scoped frontier status and scoped status register. They identify the frontier status with the same displayed rows and keep the residual-substitution premise as visible data rather than as a discharged theorem.

Read the public export route in the spine through \autoref{thm:metacic-parallel-diamond-frontier-public-certificate} and \autoref{thm:metacic-parallel-diamond-frontier-public-closure}. Those theorems make the frontier public only over the retained $P,K,J,R,S,C,B,O,H,T,G,N$ packet, so a critical-path or confluence-audit consumer cannot inspect $J$ or $B$ before $P$ has been displayed.

Finally apply the obstruction clauses already recorded by the two scoped status theorems. Since the public handoff keeps $P$ and $O$ as rows of the carrier route, it cannot become residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{proof}
