\begin{theorem}[MetaCIC parallel-diamond frontier additive seed closure route]
\label{thm:metacic-parallel-diamond-frontier-additive-seed-closure-route}
The $\MetaCICParallelDiamondFrontierUp$ seed closure route registered by the hub is read through the canonical Lean seed target
$$
\mathsf{BEDC.Derived.MetaCICCriticalPathUp.MetaCICParallelDiamondFrontierStatusSeed}
$$
only after the frontier carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier}, the local obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}, the closurestatus route of \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-status-route}, and the current seed-status reader of \autoref{thm:metacic-parallel-diamond-frontier-seed-status-canonical} have all been displayed. The route is additive child data for the hub; it records a seed-level frontier registration and does not assert global confluence, subject reduction, evaluator equality, quotient term equality, residual-substitution discharge, strong normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Read the frontier carrier first. It exposes the retained premise, critical-pair row, candidate-join row, residual-frontier row, checker row, obstruction ledger, transport, replay, provenance, and local naming coordinates.

Apply the local obligation surface next. The obligation rows keep the seed consumer inside the same displayed packet, so a seed registration cannot bypass the retained premise or obstruction boundary.

Finally read the closurestatus route and current seed-status reader. These rows attach the canonical Lean seed target to the paper-side hub marker while preserving the same carrier and obligation inventory.

Since every read is through the displayed child packet, the route registers seed closure only for the local frontier. None of the excluded global endpoints has a carrier coordinate or a NameCert obligation in this child route.
\end{proof}
