\begin{theorem}[MetaCIC parallel-diamond residual-budget seed status]
\label{thm:metacic-parallel-diamond-frontier-residual-budget-seed-status}
For every accepted $\MetaCICParallelDiamondFrontierUp$ packet, the residual-budget seed read is exposed only through the existing critical-pair envelope, residual-join locality, candidate-normalization scope, bounded-check rows, obstruction row, componentwise transport, displayed replay, $\Pkg$ provenance, and local $\NameCert_{\MetaCICParallelDiamondFrontierUp}$ rows. The seed-facing route is the carrier-local packet
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
read through the hub children for critical pairs, residual joins, candidate normalization, finite residual witnesses, retained premises, scoped seed packages, and seed-status registration.

The route explicitly refuses subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, residual-substitution discharge, and kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted frontier carrier. The critical-pair children expose $P,K$ as the bounded overlap and local join envelope before any residual-budget consumer reads a seed status.

Read the residual-join and candidate-normalization children next. They keep $J,R,S,C$ inside the same displayed packet and make the obstruction row $O$ visible rather than discharging it as a global residual-substitution theorem.

The bounded-check and finite-witness children then expose $B$ together with the same transport, replay, provenance, and local naming rows $H,T,G,N$. These rows record the finite residual-budget route; they do not add a subject-reduction theorem, unrestricted confluence principle, evaluator equality, quotient term equality, global-normalization theorem, residual-substitution discharge, or kernel-consistency certificate.
\end{proof}
