\begin{theorem}[MetaCIC parallel-diamond seed status]
\label{thm:metacic-parallel-diamond-seed-status}
For every accepted $\MetaCICParallelDiamondFrontierUp$ packet, the critical-pair envelope, residual join locality, candidate normalization scope, and finite residual witness rows jointly supply the seed-status surface for parallel-diamond consumers. The exported seed surface is the displayed route
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
read through \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope}, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}, \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope}, and \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. It is a carrier-local seed-status certificate, not unrestricted confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted frontier carrier. The critical-pair envelope exposes the bounded overlap and candidate-join rows while keeping the residual frontier, closed-checker, obstruction, transport, replay, provenance, and local naming rows visible.

Next read residual join locality. That theorem keeps residual joins inside the same displayed packet and prevents a downstream consumer from treating the local join as global confluence or premise-free residual substitution.

Now attach candidate normalization scope and finite residual witnesses. Candidate normalization keeps $J$ in the conditional spine with $P$ and $O$ visible, while finite residual witness consumption supplies the typed-window, bounded-check, transport, replay, provenance, and local $\NameCert$ rows needed before a parallel-diamond read can proceed.

The four rows therefore assemble exactly the seed-status surface over $P,K,J,R,S,C,B,O,H,T,G,N$. Since each component is a carrier projection and the ledger exposes no outside equality or kernel theorem, replay and provenance cannot escape the displayed seed packet.
\end{proof}
