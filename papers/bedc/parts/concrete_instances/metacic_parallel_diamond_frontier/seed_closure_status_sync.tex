\begin{theorem}[MetaCIC parallel-diamond frontier seed closure status sync]
\label{thm:metacic-parallel-diamond-frontier-seed-closure-status-sync}
The seed registration route for $\MetaCICParallelDiamondFrontierUp$ is subsumed by the existing mature and bridged hub markers together with the child closurestatus route. In particular, any status reader that enters through the seed marker must read the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}, the seed closure status of \autoref{thm:metacic-parallel-diamond-frontier-seed-closure-status}, the closurestatus export route of \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-export}, and the guard of \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard} before treating the frontier as a mature paper route.
\end{theorem}
\begin{proof}
Read the hub markers as pointers into the child files. The marker rows name the mature and bridged status of $\MetaCICParallelDiamondFrontierUp$, but they do not add carrier rows beyond the finite residual-premise, critical-pair, candidate-join, residual-frontier, checker, obstruction, transport, replay, provenance, and local naming route already displayed in the body.

Apply the obligation theorem and the seed closure status theorem. Together they expose the NameCert obligations and the finite seed registration surface before a status reader may inspect the marker.

Now use the closurestatus export route and the closurestatus guard. These theorems force the reader through the retained route $P,K,J,R,S,C,B,O,H,T,G,N$ and keep residual-substitution discharge, subject reduction, full Church--Rosser, evaluator equality, quotient term equality, and kernel consistency outside the packet. Hence the seed registration route is absorbed by the existing mature closurestatus route without downgrading the theory or verification axes.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond frontier formal-target request]
\label{thm:metacic-parallel-diamond-frontier-formal-target-request}
The next Lean target for the $\MetaCICParallelDiamondFrontierUp$ frontier is the residual-substitution premise retention route over the displayed rows
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
This target asks only that a confluence-facing read retain the residual-substitution premise row $P$ while passing through the critical-pair row $K$, candidate-join row $J$, residual-frontier row $R$, closed-checker row $S$, closed-normal fragment row $C$, bounded-check row $B$, obstruction row $O$, transport row $H$, replay row $T$, provenance row $G$, and local naming row $N$. It is not full subject reduction, arbitrary Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the seed closure status sync of \autoref{thm:metacic-parallel-diamond-frontier-seed-closure-status-sync}. It identifies the status reader with the same child route already guarded by the frontier body, so the requested formal target should be stated over the displayed carrier rows rather than over a global confluence endpoint.

Apply the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}. The obligations expose $P,K,J,R,S,C,B,O,H,T,G,N$ as the finite packet and leave $P$ as a retained residual-substitution premise, not as a discharged theorem.

Finally use the closurestatus export route and guard, \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-export} and \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}. These theorems keep the closurestatus reader on the same retained route and exclude subject reduction, arbitrary Church--Rosser, evaluator equality, quotient term equality, and kernel consistency from the exported target shape.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond frontier retained-premise target boundary]
\label{thm:metacic-parallel-diamond-frontier-retained-premise-target-boundary}
The next formal boundary for $\MetaCICParallelDiamondFrontierUp$ is precisely preservation of the retained residual-substitution premise row $P$ through the finite route
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The target asks that a reader carrying $P$ through the critical-pair row $K$, candidate-join row $J$, residual-frontier row $R$, checker row $S$, closed-normal fragment row $C$, bounded-check row $B$, obstruction row $O$, transport row $H$, replay row $T$, provenance row $G$, and local naming row $N$ still reaches the same retained premise boundary. It does not ask for subject reduction, arbitrary Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Apply the formal-target request theorem, \autoref{thm:metacic-parallel-diamond-frontier-formal-target-request}. It already restricts the Lean-facing request to the displayed carrier rows and identifies $P$ as retained premise data rather than a discharged global theorem.

Next read the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}. That surface is the finite packet $P,K,J,R,S,C,B,O,H,T,G,N$, so retained-premise preservation is the exact boundary visible to the local NameCert route.

Finally use the closurestatus guard, \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}. The guard keeps the exported target on the retained route and refuses subject reduction, arbitrary Church--Rosser, evaluator equality, quotient term equality, and kernel consistency.
\end{proof}
