\begin{theorem}[MetaCIC parallel-diamond closurestatus export route]
\label{thm:metacic-parallel-diamond-frontier-closurestatus-export}
The seed and bridged closure markers for $\MetaCICParallelDiamondFrontierUp$ are backed by the finite route through the existing critical-pair envelope, residual-substitution dependency route, finite residual witness consumption, candidate-SN handoff, and scoped-frontier status rows. In particular, a closurestatus reader reaches the frontier only through the displayed $P,K,J,R,S,C,B,O,H,T,G,N$ packet and cannot reinterpret the marker as residual-substitution discharge, unrestricted subject reduction, global Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the critical-pair and residual-substitution siblings. They retain the local critical-pair rows and the residual-substitution premise as visible finite evidence before any frontier consumer reads the packet.

Next use finite residual witness consumption and the candidate-SN handoff. These rows keep the typed window, candidate-normalization row, bounded local-join checker, obstruction row, transport, replay, provenance, and local naming inside the same displayed route.

Finally read the scoped-frontier status. It registers the same finite route as the status surface for the chapter, so the seed and bridged markers are exported as pointers to that route rather than as global confluence, subject-reduction, evaluator-equality, quotient-equality, residual-substitution, or kernel-consistency theorems.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond retained-premise marker route]
\label{thm:metacic-parallel-diamond-frontier-retained-premise-marker-route}
Every seed, public, or bridged marker read for $\MetaCICParallelDiamondFrontierUp$ is consumed only through the displayed $P,K,J,R,S,C,B,O,H,T,G,N$ route. In particular, $\MetaCICCriticalPathUp$ and $\MetaCICConfluenceAuditPacketUp$ consumers retain the critical-pair row $P$ and obstruction row $O$ while reading the marker, so the marker cannot be reinterpreted as a discharge of the residual-substitution premise, candidate-join row, bounded-check row, obstruction row, transport row, replay row, provenance row, or local $\NameCert$ row.
\end{theorem}
\begin{proof}
Apply the closurestatus export route above. It makes $P,K,J,R,S,C,B,O,H,T,G,N$ the only displayed path by which the marker is exported.

Now restrict the read to $\MetaCICCriticalPathUp$ or $\MetaCICConfluenceAuditPacketUp$. These consumers inspect the marker as a frontier status, not as a replacement for its packet rows. Hence they must retain the critical-pair premise $P$ and the obstruction row $O$ while reading candidate joins, bounded checks, transport, replay, provenance, and local naming.

The retained rows block the overreading. A marker consumer may record where the frontier sits, but it receives no residual-substitution discharge, unrestricted join theorem, global confluence theorem, or kernel-consistency certificate.
\end{proof}
