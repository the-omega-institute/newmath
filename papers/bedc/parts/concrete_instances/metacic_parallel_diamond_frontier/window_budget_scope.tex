\begin{theorem}[MetaCIC parallel-diamond window-budget scope]
\label{thm:metacic-parallel-diamond-frontier-window-budget-scope}
Every accepted $\MetaCICParallelDiamondFrontierUp$ frontier read must expose the typed premise row, critical-peak row, bounded join-budget row, residual checker row, candidate strong-normalization handoff row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before any local-join conclusion is public. In the carrier-local spine
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
$P$ is the retained typed premise, $K$ is the peak row, $J$ is the candidate join row, $R,S,C,B$ are the finite residual and bounded checker budget rows, $O$ is the visible obstruction row, and $H,T,G,N$ are the structural transport, replay, provenance, and naming rows. The scope exports only this finite window-budget surface, not global confluence, unbounded normalization, subject reduction, evaluator equality, quotient term equality, premise-free residual substitution, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the critical-pair envelope. It displays the peak and local candidate rows before a diamond request can be inspected by a frontier consumer.

Next read the residual-substitution dependency route and residual-normalization scope. These keep the typed premise row $P$ and obstruction row $O$ visible while the residual checker and bounded join-budget rows are opened.

Use the candidate strong-normalization handoff together with finite residual witness consumption. The handoff can be consumed only after the candidate row, residual witness rows, bounded checker rows, transport, replay, provenance, and local naming rows have been displayed in the same carrier-local spine.

Finally apply residual-row visibility. Since $H,T,G,N$ preserve the displayed finite packet and no carrier coordinate contains global confluence, subject reduction, evaluator equality, quotient term equality, premise-free residual substitution, or kernel consistency, the exported conclusion is exactly the local finite window-budget scope.
\end{proof}
