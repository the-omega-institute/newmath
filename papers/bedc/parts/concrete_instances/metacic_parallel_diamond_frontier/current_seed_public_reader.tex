\begin{theorem}[MetaCIC parallel-diamond current seed public reader]
\label{thm:metacic-parallel-diamond-frontier-current-seed-public-reader}
Every public seed read of an accepted $\MetaCICParallelDiamondFrontierUp$ packet factors through the current critical-pair envelope, residual-join surface, candidate-normalization row, and retained-premise rows before any confluence-facing claim is exposed. In carrier notation the reader remains inside
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
It is a public seed route for the displayed frontier packet, not a residual-substitution discharge, subject-reduction theorem, unrestricted confluence theorem, evaluator equality theorem, quotient term equality theorem, global-normalization theorem, or kernel-consistency certificate.
\end{theorem}
\begin{proof}
Start with the critical-pair envelope and residual-join locality. The current public seed read must expose the retained residual-substitution premise, critical-pair row, candidate-join row, residual-frontier row, and local join budget before any downstream consumer can inspect the frontier.

Next use the candidate-normalization scope. It keeps the closed-checker row, closed-normal fragment, bounded-check row, and obstruction row attached to the same finite packet rather than promoting them to a global normalization or confluence endpoint.

Finally apply the retained-premise closure and seed-status public export routes. They preserve $P,K,J,R,S,C,B,O,H,T,G,N$ as the public seed reader and leave residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient equality, global normalization, and kernel consistency outside the packet.
\end{proof}
