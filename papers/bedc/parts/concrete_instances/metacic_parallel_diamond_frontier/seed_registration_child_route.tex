\begin{theorem}[MetaCIC parallel-diamond seed registration child route]
\label{thm:metacic-parallel-diamond-frontier-seed-registration-child-route}
The seed registration of an accepted $\MetaCICParallelDiamondFrontierUp$ packet is read through the local child rows for critical-pair envelope, residual-join locality, retained-premise budget, seed closure status, closurestatus guard, and local $\NameCert$ obligations. Concretely, every seed-registration consumer must expose
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
by way of \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope}, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}, \autoref{thm:metacic-parallel-diamond-frontier-retained-premise-closure}, \autoref{thm:metacic-parallel-diamond-frontier-seed-closure-status}, \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}, and \autoref{thm:metacic-parallel-diamond-frontier-obligations}. The child route records seed registration only; it does not discharge residual substitution, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the carrier and local NameCert obligations. They make the retained premise, critical-pair data, candidate join, residual witness, typed window, bounded checker, obstruction, transport, replay, provenance, and local naming rows the whole public packet.

Read the critical-pair envelope and residual-join locality rows next. These keep the local diamond request carrier-local and keep the residual-substitution premise visible before any seed consumer can inspect a join.

The retained-premise closure budget then prevents the premise row from disappearing under seed registration. The seed closure status reader and closurestatus guard identify the seed marker as a pointer into the same displayed packet, rather than as an independent confluence or kernel claim.

Thus the child route is exactly the finite registration spine for the hub: it ties the seed row to existing child files while refusing residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, and kernel consistency.
\end{proof}
