\begin{theorem}[MetaCIC parallel-diamond frontier current seed closure route]
\label{thm:metacic-parallel-diamond-frontier-seed-closure-current-route}
The current seed closure route for $\MetaCICParallelDiamondFrontierUp$ is read through the existing frontier carrier, the checked obligation surface, and the seed-status child packet already attached to the hub. A seed consumer must expose the retained residual-substitution premise, critical-pair row, candidate-join row, residual-frontier row, bounded checker, obstruction ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before the hub marker can be consumed as a seed closure route.

This route registers only the paper-side seed closure over the displayed carrier and obligation surface. It does not claim subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Read the frontier carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier}. Its rows provide the retained premise, local critical-pair data, candidate join data, residual frontier, bounded checker, obstruction ledger, transport, replay, provenance, and local naming surface.

Apply the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}. The obligation route keeps every seed-facing read on those displayed rows, so the hub marker cannot detach into an unrestricted confluence or evaluator-equality assertion.

Finally compare the seed-status child packet with the hub marker. The child packet records the same carrier and obligation inventory as a current seed route, while the exclusions remain outside the carrier coordinates and outside the local $\NameCert$ surface.
\end{proof}
