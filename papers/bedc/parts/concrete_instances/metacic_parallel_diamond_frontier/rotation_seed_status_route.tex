\begin{theorem}[MetaCIC frontier rotation seed status route]\label{thm:metacic-parallel-diamond-frontier-rotation-seed-status-route}
The hub-level seed marker for $\MetaCICParallelDiamondFrontierUp$ is read through the canonical child status rows ending at
$\mathsf{BEDC.Derived.MetaCICCriticalPathUp.MetaCICParallelDiamondFrontierStatusSeed}$.
It binds only the displayed seed frontier status for the retained critical-pair and residual-join packet.
It does not claim residual-substitution discharge, subject reduction, full confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Read the hub marker together with the child rows for the seed-status route, the current seed frontier reader, and the canonical status reader. These rows expose the seed registration as a local status certificate for the displayed frontier packet.

The exclusions follow from the same readback boundary. None of residual-substitution discharge, subject reduction, full confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency is a row of this child status route, so no such endpoint is exported by the theorem.
\end{proof}
