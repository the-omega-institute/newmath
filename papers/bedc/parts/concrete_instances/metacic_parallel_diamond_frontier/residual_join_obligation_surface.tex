\begin{theorem}[MetaCIC parallel-diamond residual-join obligations]
\label{thm:metacic-parallel-diamond-frontier-residual-join-obligations}
Every frontier residual join exposed by an accepted $\MetaCICParallelDiamondFrontierUp$ packet must display the critical-pair source row, retained residual-substitution window, candidate-normalization budget, local join witness, obstruction-retention row, componentwise $\hsame$ transport, displayed $\Cont$ replay, and $\NameCert$ provenance before a consumer may read the join. Equivalently, the obligation surface is the carrier-local spine
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
where $P$ is still the residual-substitution premise, $K,J,R$ give the critical-pair and candidate frontier rows, $S,C,B,O$ give the bounded checker, closed-normal, budget, and obstruction rows, and $H,T,G,N$ give transport, replay, provenance, and local naming.

The surface is a bounded candidate-mediated residual-join obligation. It is not a parameter-transport equivalence theorem, global confluence theorem, unbounded normalization theorem, subject-reduction theorem, evaluator equality, quotient term equality, or kernel-consistency certificate.
\end{theorem}
\begin{proof}
First read residual-join locality,
\autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. It places every accepted residual join inside the finite frontier spine and keeps the critical-pair, retained premise, local join, bounded checker, obstruction, transport, replay, provenance, and naming rows visible.

Next apply the residual-scope route of \autoref{thm:metacic-parallel-diamond-frontier-residual-scope}. This prevents the retained residual-substitution premise from being discharged after the local join is consumed, and it keeps $P,K,J,R,S,C,B,O,H,T,G,N$ in the same carrier-local packet.

Now use the residual-normalization window and candidate-mediated strong-normalization handoff, \autoref{thm:metacic-parallel-diamond-frontier-residual-normalization-window} and \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. These rows expose the candidate-normalization budget and local witness only after the typed residual window, bounded checker rows, and obstruction row have been displayed.

Finally repack the displayed rows as the residual-join obligation surface. Since the packet contains no coordinate for parameter-transport equivalence, global confluence, unbounded normalization, subject reduction, evaluator equality, quotient term equality, or kernel consistency, the exposed join remains a local obligation over the stated finite frontier spine.
\end{proof}

\begin{theorem}[MetaCIC residual-join obligation handoff]
\label{thm:metacic-parallel-diamond-frontier-residual-join-obligation-handoff}
Let an accepted $\MetaCICParallelDiamondFrontierUp$ packet expose finite critical-pair coverage, residual-substitution locality, and retained-premise closure through the carrier-local rows
$$
P,K,J,R,S,C,B,O,H,T,G,N.
$$
If a residual-join consumer reads the finite critical-pair surface through $K,J,R$, keeps the residual-substitution premise $P$ local to the same packet, and preserves the retained premise and obstruction rows through $B,O,H,T,G,N$, then the packet provides the candidate-mediated strong-normalization handoff surface recorded by \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. The handoff is a bounded residual-join obligation surface; it does not claim global confluence, unrestricted normalization, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with residual-join obligations and residual critical-pair coverage, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-obligations} and \autoref{thm:metacic-parallel-diamond-frontier-residual-critical-pair-coverage}. These results force the consumer to expose the finite critical-pair, candidate, residual witness, checker, obstruction, transport, replay, provenance, and naming rows before any local join is exported.

Next use residual-join locality and the residual grid, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality} and \autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. They keep the residual-substitution premise inside the carrier-local packet and prevent the candidate join from being read as an independent confluence theorem.

Finally apply the retained-premise budget scope and the candidate-mediated SN handoff, \autoref{thm:metacic-parallel-diamond-frontier-residual-premise-budget-scope} and \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. The first keeps $P$ and $O$ visible through the structural rows, and the second packages exactly that displayed finite surface as the candidate-mediated handoff. Since every cited step is still confined to $P,K,J,R,S,C,B,O,H,T,G,N$, no global confluence, unrestricted normalization, subject reduction, evaluator equality, quotient term equality, or kernel-consistency endpoint is exported.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual critical-pair coverage]
\label{thm:metacic-parallel-diamond-frontier-residual-critical-pair-coverage}
Every residual-join consumer of an accepted $\MetaCICParallelDiamondFrontierUp$ packet must expose, before exporting its local join, the critical-pair envelope, retained residual-substitution premise, candidate-normalization scope, finite residual witness, bounded checker rows, obstruction row, and local $\NameCert$ row. Equivalently, the exported local join must still be read through the finite carrier-local surface
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
with $P$ retaining the substitution premise, $K$ exposing the critical-pair row, $J$ and $R$ retaining the candidate-normalization and residual-witness rows, $S,C,B$ exposing the bounded checker rows, $O$ retaining the obstruction row, and $H,T,G,N$ carrying transport, replay, provenance, and local naming.

The coverage claim is local to the frontier packet. It exports no global confluence theorem, subject-reduction theorem, evaluator equality, quotient term equality, or kernel-consistency certificate.
\end{theorem}
\begin{proof}
Begin with the residual-join obligations and residual-normalization envelope above. Together they keep any exported local join behind the retained residual-substitution premise, critical-pair row, candidate-normalization budget, bounded checker rows, obstruction row, transport, replay, provenance, and naming.

Next refine the obligation surface through residual-join locality and the residual grid, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality} and \autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. These theorems identify the critical-pair, residual-substitution, candidate-normalization, finite-witness, bounded-checker, obstruction, and local naming rows as one carrier-local grid rather than independent side conditions.

Finally apply the candidate strong-normalization handoff of \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. It lets the consumer use the candidate-mediated boundary only after the finite residual witness and bounded checker rows remain visible, so the exported join is covered by the displayed critical-pair surface and cannot be reread as global confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-normalization envelope]
\label{thm:metacic-parallel-diamond-frontier-residual-normalization-envelope}
Any consumer that uses the local residual join of an accepted $\MetaCICParallelDiamondFrontierUp$ packet must first expose the retained residual-substitution premise, the residual-normalization window, the candidate strong-normalization handoff, bounded checker rows, the obstruction row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row.

The envelope is the same carrier-local route
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
read with $P$ retained and $O$ visible before the candidate-mediated normalization handoff is consumed. It exports no full confluence theorem, subject-reduction theorem, evaluator equality, quotient term equality, or kernel-consistency certificate.
\end{theorem}
\begin{proof}
Start with the residual-join obligation surface above. It keeps the critical-pair source, retained residual-substitution premise, candidate-normalization budget, bounded checker rows, obstruction row, transport, replay, provenance, and local naming visible before a local join can be read.

Next apply the residual-normalization window, \autoref{thm:metacic-parallel-diamond-frontier-residual-normalization-window}. That row forces a normalization-facing residual peak to remain behind $P,J,R,S,C,O,H,T,G,N$ and prevents the candidate row from becoming a premise-free normalization theorem.

Finally use the candidate strong-normalization handoff, \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. It lets the consumer pass the bounded candidate data onward only after the checker rows and obstruction row are still displayed, so the resulting envelope remains a local residual-normalization boundary rather than full confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-premise budget scope]
\label{thm:metacic-parallel-diamond-frontier-residual-premise-budget-scope}
Every candidate-mediated strong-normalization or local residual-join consumer of an accepted $\MetaCICParallelDiamondFrontierUp$ packet must keep the retained residual-substitution premise row $P$ and obstruction row $O$ visible through the finite route
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
before exporting a budgeted join. The budget is the displayed carrier-local budget over residual premise, critical pair, candidate row, residual witness, closed checker, closed-normal fragment, bounded checker, obstruction, transport, replay, provenance, and local naming; it is not a discharge of residual substitution, global confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the residual-join obligations and critical-pair coverage, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-obligations} and \autoref{thm:metacic-parallel-diamond-frontier-residual-critical-pair-coverage}. They require every exported local join to display $P,K,J,R,S,C,B,O,H,T,G,N$, with the residual-substitution premise and obstruction rows still present.

Next apply the residual-normalization envelope, \autoref{thm:metacic-parallel-diamond-frontier-residual-normalization-envelope}. It keeps candidate-mediated normalization behind the same finite route and prevents the candidate row from becoming a premise-free normalization theorem.

Finally use the candidate strong-normalization handoff, \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. The handoff can export only the displayed budgeted join, so any consumer claiming global confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency has left the retained residual-premise surface.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-join transport induction]
\label{thm:metacic-parallel-diamond-frontier-residual-join-transport-induction}
Let an accepted $\MetaCICParallelDiamondFrontierUp$ packet display the residual critical-pair rows, residual-normalization envelope, retained-premise budget rows, componentwise $\hsame$ transport, and displayed $\Cont$ replay over the finite spine
$$
P,K,J,R,S,C,B,O,H,T,G,N.
$$
If those rows are transported componentwise and replayed by the displayed continuation data, the resulting local join remains inside the same candidate-mediated diamond surface. In particular, residual critical-pair coverage, residual-normalization envelope data, and retained-premise budget scope are preserved as frontier-local rows, not reread as global confluence, unbounded normalization, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Induct through the finite residual row list. The base row is the retained residual-substitution premise $P$; transport along the corresponding component of $H$ keeps the premise visible before any critical-pair or candidate row is consumed.

For the critical-pair and local-join segment, use \autoref{thm:metacic-parallel-diamond-frontier-residual-join-obligations} and \autoref{thm:metacic-parallel-diamond-frontier-residual-critical-pair-coverage}. These theorems keep $K,J,R$ attached to the same carrier-local residual surface under replay by $T$.

For the normalization and budget segment, apply \autoref{thm:metacic-parallel-diamond-frontier-residual-normalization-envelope} and \autoref{thm:metacic-parallel-diamond-frontier-residual-premise-budget-scope}. The rows $S,C,B,O$ remain visible before the candidate-mediated handoff can export a budgeted join.

The structural rows $H,T,G,N$ then repack the transported list as the same local diamond surface. Since each induction step uses only displayed rows and continuation replay, no quotient term equality, global confluence theorem, unbounded normalization theorem, subject-reduction theorem, evaluator equality, or kernel-consistency certificate is introduced.
\end{proof}

\begin{theorem}[MetaCIC retained-premise discharge boundary]
\label{thm:metacic-parallel-diamond-frontier-retained-premise-discharge}
Every candidate-mediated local join exported by an accepted $\MetaCICParallelDiamondFrontierUp$ packet must preserve the retained residual-substitution premise row $P$ through the full visible route
$$
P,K,J,R,S,C,B,O,H,T,G,N.
$$
The candidate rows $K,J,R,S,C,B$ may transport a bounded local join, and the structural rows $H,T,G,N$ may replay and name it, but none of those rows discharges $P$. The boundary therefore exports only a local residual-join read with $P$ retained; it refuses global confluence, subject reduction, evaluator equality, quotient term equality, unrestricted normalization, and kernel consistency.
\end{theorem}
\begin{proof}
Start with residual-join transport induction, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-transport-induction}. Its base row is $P$, so every transported local join begins with the retained residual-substitution premise still visible.

For the candidate-mediated segment, use residual critical-pair coverage and the residual-normalization envelope. These results keep $K,J,R,S,C,B$ attached to the same finite frontier packet and prevent the candidate handoff from becoming a premise-free join theorem.

Finally apply retained-premise budget scope and the candidate strong-normalization handoff. They permit a bounded local join only while $P,O,H,T,G,N$ remain in the route. Hence the exported join preserves the retained premise and reaches none of the refused global endpoints.
\end{proof}

\begin{theorem}[MetaCIC obstruction-preservation route]
\label{thm:metacic-parallel-diamond-frontier-obstruction-preservation-route}
Every candidate-mediated local join transported through an accepted $\MetaCICParallelDiamondFrontierUp$ packet preserves the obstruction row $O$ along the same finite route
$$
P,K,J,R,S,C,B,O,H,T,G,N.
$$
The obstruction remains visible before and after the local join is replayed by $T$ and named by $N$. Thus the packet records obstruction-preserving local residual-join transport only; it does not assert global confluence, subject reduction, evaluator equality, quotient term equality, unrestricted normalization, or kernel consistency.
\end{theorem}
\begin{proof}
First read the residual-normalization envelope and residual-premise budget scope. Together they place $O$ beside the bounded checker and candidate rows, so the obstruction is not an external side condition that can be erased after the join is found.

Next use residual-join transport induction. The transport row $H$ and replay row $T$ act componentwise on the visible route, so the obstruction component is carried with $P,K,J,R,S,C,B$ rather than replaced by evaluator equality or quotient term equality.

Finally repack through $G$ and $N$. Provenance and local naming certify the same obstruction-preserving frontier packet, and because no coordinate provides unrestricted normalization, subject reduction, global confluence, or kernel consistency, the route remains exactly the local obstruction-preservation route.
\end{proof}

\begin{theorem}[MetaCIC residual-budget diamond obligation]
\label{thm:metacic-parallel-diamond-residual-budget-obligation}
Every local diamond-join read exposed by an accepted $\MetaCICParallelDiamondFrontierUp$ packet must display the critical-pair envelope, retained residual-substitution window, candidate-normalization scope, finite residual-diamond ledger, and residual-join locality rows before the frontier package can expose a candidate-mediated confluence handoff. Equivalently, the handoff remains inside the carrier-local route
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
where $P$ retains the residual-substitution premise, $K$ records the critical-pair envelope, $J,R,S,C,B$ record the candidate and bounded residual window, $O$ records the visible obstruction, and $H,T,G,N$ record transport, replay, provenance, and local naming. The obligation exports only a bounded local diamond route; it does not discharge residual substitution, prove unrestricted confluence, prove subject reduction, identify evaluator equality, identify quotient term equality, or certify kernel consistency.
\end{theorem}
\begin{proof}
Start with the residual-substitution dependency route, \autoref{thm:metacic-parallel-diamond-frontier-residual-substitution-dependency-route}. It places the retained premise row $P$ before any critical-pair or candidate-join row can be consumed.

Next read the candidate-normalization scope of \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope}. That scope keeps $J$ inside the conditional spine and requires the obstruction row $O$ to remain visible when a normalization-facing consumer inspects the candidate.

Use the finite residual-diamond ledger obligations, \autoref{thm:metacic-finite-residual-diamond-ledger-local-join-obligations}. The ledger packages the critical-pair row, retained premise, candidate join, bounded checker status, obstruction, transport, replay, provenance, and local naming as finite carrier-local data.

Finally apply residual-join locality and its locality budget, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality} and \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality-budget}. These rows keep the displayed packet on the route $P,K,J,R,S,C,B,O,H,T,G,N$ before any candidate-mediated confluence handoff is public. Since every cited surface is carrier-local and still retains $P$ and $O$, the handoff remains the stated residual-budget obligation rather than any refused global endpoint.
\end{proof}

\begin{theorem}[MetaCIC parallel diamond frontier residual-join obligation carrier]
\label{thm:metacic-parallel-diamond-frontier-residual-join-obligation-carrier}
Before any candidate diamond read of an accepted $\MetaCICParallelDiamondFrontierUp$ packet is public, the residual-join obligation carrier consists of the retained premise row, critical-pair envelope, residual-substitution dependency row, finite residual witness, obstruction row, bounded checker row, transport, replay, provenance, and local $\NameCert$ row. In carrier notation the public residual-join obligation must still expose
$$
P,K,J,R,S,C,B,O,H,T,G,N,
$$
with $P$ retaining the premise, $K$ exposing the critical pair, $J,R$ carrying the candidate and finite residual witness, $S,C,B$ retaining the residual-substitution and checker rows, $O$ retaining the obstruction, and $H,T,G,N$ transporting, replaying, sourcing, and naming the same packet. The carrier is an obligation surface for a bounded residual join; it is not full confluence, subject reduction, evaluator equality, quotient term equality, unrestricted normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the residual-budget diamond obligation of \autoref{thm:metacic-parallel-diamond-residual-budget-obligation}. It already forces every local diamond-join read to expose the critical-pair envelope, retained residual-substitution window, candidate-normalization scope, finite residual-diamond ledger, and residual-join locality rows before a candidate-mediated handoff is public.

Next use finite residual witness consumption, \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. This supplies the finite residual witness, typed window, bounded checker, obstruction, transport, replay, provenance, and local naming rows as displayed data of the same packet.

Finally apply the residual-join obligation handoff and candidate-mediated SN handoff, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-obligation-handoff} and \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. They keep $P$ and $O$ visible through $H,T,G,N$ before the candidate-facing read is exported. Since the composed route never leaves $P,K,J,R,S,C,B,O,H,T,G,N$, it yields only the stated residual-join obligation carrier and none of the refused global endpoints.
\end{proof}

\begin{theorem}[MetaCIC residual-join frontier obligation]
\label{thm:metacic-residual-join-frontier-obligation}
Every candidate-mediated residual join exported by an accepted $\MetaCICParallelDiamondFrontierUp$ frontier must keep the retained residual-substitution premise row $P$ and the obstruction row $O$ visible through the same finite route
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
before any local diamond handoff is consumed. Thus the consumer may read a bounded local join only while the critical-pair, candidate, finite residual witness, bounded checker, obstruction, transport, replay, provenance, and local naming rows remain carrier-local.

The obligation does not export global confluence, subject reduction, evaluator equality, quotient term equality, unrestricted normalization, or kernel consistency.
\end{theorem}
\begin{proof}
First compose the residual-join obligation carrier of \autoref{thm:metacic-parallel-diamond-frontier-residual-join-obligation-carrier} with the residual-budget diamond obligation of \autoref{thm:metacic-parallel-diamond-residual-budget-obligation}. The carrier supplies the displayed route, and the budget obligation prevents the candidate diamond read from leaving that route before the handoff is visible.

Next use finite residual witness consumption, \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. This step keeps the residual witness, typed window, bounded checker rows, and obstruction row as displayed rows of the same packet rather than private side conditions.

Finally apply the candidate strong-normalization handoff of \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. Since the handoff consumes only the displayed candidate-mediated surface and the rows $P$ and $O$ are still present through $H,T,G,N$, the exported join remains a local frontier obligation and none of the refused global endpoints is introduced.
\end{proof}

\begin{theorem}[MetaCIC residual-join window determinacy]
\label{thm:metacic-parallel-diamond-frontier-residual-join-window-determinacy}
Let two accepted candidate-mediated residual-join reads of a $\MetaCICParallelDiamondFrontierUp$ packet expose the same retained premise row $P$, critical-pair row $K$, candidate row $J$, residual witness rows $R,S,C,B$, obstruction row $O$, transport rows $H,T$, provenance row $G$, and local $\NameCert$ row $N$. Then the two reads determine the same local join window over the finite route
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The conclusion is only local window determinacy for the displayed residual-join surface. It is not global confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
First compose the residual-join obligation carrier of \autoref{thm:metacic-parallel-diamond-frontier-residual-join-obligation-carrier}. Both reads expose the same carrier-local rows, so the retained premise, critical-pair row, candidate row, residual witness rows, obstruction row, transport, replay, provenance, and local naming are the same displayed packet.

Next apply the residual-budget diamond obligation of \autoref{thm:metacic-parallel-diamond-residual-budget-obligation}. The budgeted local diamond handoff cannot leave the route $P,K,J,R,S,C,B,O,H,T,G,N$, and the shared rows therefore force both reads to consume the same bounded residual window.

Use finite residual witness consumption to keep $R,S,C,B$ as the displayed witness and checker rows of that same window. The shared obstruction row $O$ and shared transport rows $H,T$ prevent either read from replacing the witness by an external evaluator equality or quotient term equality.

Finally apply the candidate strong-normalization handoff of \autoref{thm:metacic-parallel-diamond-frontier-candidate-sn-handoff}. Since the handoff consumes only the shared candidate-mediated surface, the two accepted reads determine one local join window. The argument never exports global confluence, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}
