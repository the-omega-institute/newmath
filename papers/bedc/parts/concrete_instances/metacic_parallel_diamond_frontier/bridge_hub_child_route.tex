\begin{theorem}[MetaCIC parallel-diamond bridge hub child route]
\label{thm:metacic-parallel-diamond-frontier-bridge-hub-child-route}
The bridged and mature markers displayed by the $\MetaCICParallelDiamondFrontierUp$ hub are readable only through the child files that expose the carrier, obligation surface, closurestatus guard, and retained-premise route. Concretely, a status reader must pass through the carrier rows
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
of \autoref{def:metacic-parallel-diamond-frontier-carrier}, the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}, the closurestatus guard of \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}, and the retained-premise budget of \autoref{thm:metacic-parallel-diamond-frontier-retained-premise-mature-budget} before it may consume those hub markers.

The route records local auditability for the hub status only. It does not discharge residual substitution, prove subject reduction, prove unrestricted confluence, identify evaluator equality, identify quotient term equality, or certify kernel consistency.
\end{theorem}
\begin{proof}
Start with the hub's two status markers. They are routing annotations for the same finite frontier packet, so a reader must follow the child inputs rather than treating the hub line itself as a proof object.

The carrier child supplies the row tuple $P,K,J,R,S,C,B,O,H,T,G,N$, and the obligation theorem names the residual-substitution premise, critical-pair, candidate-join, residual-frontier, closed-checker, closed-normal, bounded-check, obstruction, transport, replay, provenance, and local naming rows as the exposed surface.

Apply the closurestatus guard. It forces status readers to keep the retained premise, critical-pair envelope, candidate-normalization scope, finite residual witness ledger, candidate-SN handoff, obstruction row, transport, replay, provenance, and local NameCert rows visible before seed or bridged markers are read.

Finally apply the retained-premise mature budget. The same premise, candidate, residual, bounded-check, and obstruction rows remain carried through the public and bridged export route, so the hub markers are locally auditable without supplying residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}
