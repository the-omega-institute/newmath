\begin{theorem}[MetaCIC parallel-diamond critical-pair join envelope]
\label{thm:metacic-parallel-diamond-frontier-critical-pair-join-envelope}
Every candidate-mediated strong-normalization or confluence consumer of an accepted $\MetaCICParallelDiamondFrontierUp$ packet must first display the critical-pair envelope, residual-substitution dependency route, residual-join locality surface, finite residual witness row, and obstruction row for the same finite carrier
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The consumer may read the candidate join only as this bounded envelope. It does not discharge residual substitution, prove subject reduction, identify evaluator equality, identify quotient term equality, prove global confluence, or assert kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted frontier carrier. The displayed rows are the retained residual-substitution premise $P$, critical-pair row $K$, candidate join $J$, residual frontier $R$, closed-checker row $S$, closed-normal fragment $C$, bounded checker $B$, obstruction row $O$, and structural rows $H,T,G,N$.

Read the envelope rows in order. The critical-pair envelope keeps the bounded overlap data visible, while the residual-substitution dependency route places $P$ before any join handoff.

Now attach the local join and witness rows. Residual-join locality keeps $J$ inside the same frontier packet, and the finite residual witness row forces the typed windows, residual data, bounded checker rows, transport, replay, provenance, and local naming to remain visible.

Finally retain the obstruction row. Since $O,H,T,G,N$ preserve only the bounded carrier-local packet, a candidate-mediated consumer receives an envelope rather than residual-substitution discharge, subject reduction, evaluator equality, quotient term equality, global confluence, or kernel consistency.
\end{proof}
