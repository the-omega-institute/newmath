\begin{theorem}[MetaCIC parallel-diamond frontier public certificate]
\label{thm:metacic-parallel-diamond-frontier-public-certificate}
The public $\MetaCICParallelDiamondFrontierUp$ certificate is the finite route through the residual-substitution premise, critical-pair row, candidate-join row, residual-frontier row, closed-checker seal, closed-normal fragment, bounded-check row, obstruction row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. It exposes the residual frontier as a public finite handoff while refusing residual-substitution discharge, subject reduction, full Church--Rosser, evaluator equality, quotient term equality, and kernel consistency.
\end{theorem}
\begin{proof}
Apply the public export route of \autoref{thm:metacic-parallel-diamond-frontier-public-export}. It displays the residual-substitution premise, critical-pair, candidate-join, residual-frontier, checker, obstruction, transport, replay, provenance, and local naming rows before any outside consumer may read the frontier.

Apply the public seed-readiness route of \autoref{thm:metacic-parallel-diamond-frontier-public-seed-readiness}. This keeps the closed-checker seal, closed-normal fragment, bounded-check row, and obstruction row inside the same finite handoff.

The two routes name only the displayed packet rows. Since neither route contains a discharge of the residual-substitution premise, subject reduction, full Church--Rosser, evaluator equality, quotient term equality, or kernel consistency, the public certificate exports the finite frontier without claiming those endpoints.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond bridged handoff]
\label{thm:metacic-parallel-diamond-bridged-handoff}
The public residual-premise, critical-pair, candidate-join, residual-frontier, closed-checker, obstruction, transport, replay, provenance, and local naming route is the bridged handoff surface for $\MetaCICParallelDiamondFrontierUp$. The handoff records the finite rows that a standard bridge may read before interpreting the parallel-diamond frontier externally. Residual-substitution discharge, unrestricted confluence, subject reduction, evaluator equality, quotient term equality, kernel consistency, and bridge checking remain outside this chapter.
\end{theorem}

\begin{theorem}[Finite residual witness consumption]
\label{thm:metacic-parallel-diamond-finite-residual-witness-consumption}
Every candidate-mediated SN/confluence consumer of $\MetaCICParallelDiamondFrontierUp$ must consume the finite residual witness route
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
through the displayed residual-premise, critical-pair, candidate-join, residual-frontier, closed-checker, closed-normal fragment, bounded-check, obstruction, transport, replay, provenance, and local naming rows. The consumer may not retype the handoff as residual-substitution discharge, subject reduction, full Church--Rosser, evaluator equality, quotient equality, or kernel consistency.
\end{theorem}
\begin{proof}
Read the public certificate of \autoref{thm:metacic-parallel-diamond-frontier-public-certificate}. It exposes the residual premise, critical-pair, candidate-join, residual-frontier, checker, closed-normal, bounded-check, obstruction, transport, replay, provenance, and local naming rows as one finite handoff.

Now apply the bridged handoff of \autoref{thm:metacic-parallel-diamond-bridged-handoff}. The bridge-facing consumer may inspect only the rows named by that handoff before interpreting the frontier externally. The row order $P,K,J,R,S,C,B,O,H,T,G,N$ records the premise, critical-pair, join, residual, seal, checker, bounded-check, obstruction, transport, replay, provenance, and local-name witnesses as finite data.

Since none of those rows is a residual-substitution discharge, subject-reduction theorem, unrestricted Church--Rosser proof, evaluator equality, quotient equality, or kernel-consistency certificate, a consumer that claims one of those endpoints has left the displayed $\MetaCICParallelDiamondFrontierUp$ route.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond frontier public closure]
\label{thm:metacic-parallel-diamond-frontier-public-closure}
Public closure for $\MetaCICParallelDiamondFrontierUp$ is available only over the retained finite route
$$
P,K,J,R,S,C,B,O,H,T,G,N.
$$
The route retains the residual-substitution premise row, critical-pair row, candidate-join row, residual-frontier row, closed-checker seal, closed-normal fragment, bounded-check row, obstruction row, transport row, replay row, provenance row, and local naming row before any public diamond handoff. It does not discharge residual substitution, subject reduction, full Church--Rosser, evaluator equality, quotient equality, or kernel consistency.
\end{theorem}
\begin{proof}
Use the public certificate of \autoref{thm:metacic-parallel-diamond-frontier-public-certificate}. It exposes the residual premise, critical-pair, candidate-join, residual-frontier, checker, obstruction, transport, replay, provenance, and local naming rows as a finite public handoff.

Now apply the public seed-readiness route of \autoref{thm:metacic-parallel-diamond-frontier-public-seed-readiness}. It keeps the closed-checker seal, closed-normal fragment, bounded-check row, and obstruction row inside the same retained packet rather than moving them to a hidden evaluator or quotient term layer.

Finally use the retained witness consumption of \autoref{thm:metacic-parallel-diamond-finite-residual-witness-consumption}. The ordered route $P,K,J,R,S,C,B,O,H,T,G,N$ is the only public surface consumed by downstream SN/confluence fragments. Since the retained rows leave residual-substitution discharge, subject reduction, full Church--Rosser, evaluator equality, quotient equality, and kernel consistency outside the packet, the public closure is exactly this finite route.
\end{proof}

\begin{closurestatus}{\MetaCICParallelDiamondFrontierUp}
  \constructivestory{A $\MetaCICParallelDiamondFrontierUp$ packet is generated from residual-substitution premise rows, critical-pair rows, candidate-join rows, residual-frontier rows, closed checker seal rows, closed-normal fragment rows, bounded-check rows, obstruction rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{thm:metacic-parallel-diamond-frontier-public-export}, \autoref{thm:metacic-parallel-diamond-frontier-public-seed-readiness}, and \autoref{thm:metacic-parallel-diamond-finite-residual-witness-consumption} display the carrier, critical-pair exhaustion, residual-substitution scope, candidate-normalization, local-join, and seed-readiness rows that form the obligation surface for the MetaCIC parallel-diamond frontier.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.MetaCICParallelDiamondFrontierUp.MetacicParallelDiamondFrontierObligations}
  \bridgestatus{none}
  \notclaimed{This chapter does not close residual-substitution compatibility, the full parallel diamond, subject reduction, arbitrary closed Church--Rosser, evaluator equality, quotient equality of terms, kernel consistency, or checked bridge interpretation beyond the displayed handoff.}
  \upgradepath{Public closure requires discharge of the displayed residual-substitution compatibility, candidate-normalization, local-join, and frontier-export obligations before the finite handoff of \autoref{thm:metacic-parallel-diamond-bridged-handoff} may be read as public or bridged.}
\end{closurestatus}
