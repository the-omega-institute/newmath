\begin{theorem}[MetaCIC parallel-diamond frontier seed-axis summary]
\label{thm:metacic-parallel-diamond-frontier-seed-axis-summary}
Every seed-axis consumer of the $\MetaCICParallelDiamondFrontierUp$ hub must read the seed route through the child rows of \autoref{thm:metacic-parallel-diamond-frontier-seed-obligations}, \autoref{thm:metacic-parallel-diamond-frontier-seed-readiness}, \autoref{thm:metacic-parallel-diamond-seed-status}, and \autoref{thm:metacic-parallel-diamond-frontier-status-canonical}. The parent hub contributes only ordered input routing; it has no independent carrier coordinate, proof body, residual-substitution discharge, subject-reduction endpoint, unrestricted confluence endpoint, evaluator equality, quotient term equality, global normalization, or kernel-consistency row.
\end{theorem}
\begin{proof}
Start with the seed-obligation child row. It exposes the retained premise, critical-pair row, candidate-join row, residual-frontier row, checker rows, obstruction ledger, transport, replay, provenance, and local naming surface before any seed-facing read is available.

Next use the seed-readiness and parallel-diamond seed-status rows. They keep the same finite packet visible as a seed route and do not move the retained residual-substitution premise into a discharged endpoint.

Finally compare the canonical status row with the parent input list. Since the hub supplies only an ordered route into child files, any seed-axis read factors through those child rows and exports none of the refused MetaCIC endpoints.
\end{proof}
