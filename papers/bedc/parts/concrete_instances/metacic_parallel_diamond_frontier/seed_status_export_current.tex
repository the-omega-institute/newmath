\begin{theorem}[MetaCIC current seed-status export]
\label{thm:metacic-parallel-diamond-frontier-current-seed-status-export}
Every current $\MetaCICParallelDiamondFrontierUp$ seed-status reader must expose the same finite frontier packet before it reads the status row: the critical-pair envelope, residual-join surface, candidate-normalization row, seed-readiness rows, and the structural transport, replay, provenance, and local $\NameCert_{\MetaCICParallelDiamondFrontierUp}$ rows. In carrier notation the route is
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The export is a seed-status reader for this displayed frontier packet, not a premise-free confluence theorem, global normalization theorem, residual-substitution discharge, subject-reduction theorem, evaluator equality, quotient term equality, or kernel-consistency certificate.
\end{theorem}
\begin{proof}
Read the hub children in the status order. The critical-pair envelope keeps $P,K$ visible, while the residual-join and candidate-normalization children expose $J,R,S,C,B$ as local frontier rows rather than global confluence data.

The seed-readiness children then keep the obstruction row $O$ and the bounded frontier budget attached to the same packet. Transport, replay, provenance, and local naming are exactly the rows $H,T,G,N$, so the exported status cannot replace the displayed carrier by an ambient theorem.

Thus the current reader is only the finite seed-status route over $P,K,J,R,S,C,B,O,H,T,G,N$.
\end{proof}
