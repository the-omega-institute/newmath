\begin{theorem}[MetaCIC parallel-diamond frontier seed scope]
\label{thm:metacic-parallel-diamond-frontier-seed-scope}
Before any candidate-mediated strong-normalization route is exported from an accepted $\MetaCICParallelDiamondFrontierUp$ packet, the only seed-scope reads are finite critical-pair windows, residual substitution rows, candidate-normalization rows, and local join ledgers. In carrier notation these are the displayed rows $P,K,J,R,S,C,B,O,H,T,G,N$ read as retained frontier data, not as subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:metacic-parallel-diamond-frontier-carrier}. The finite critical-pair windows are carried by $K$ and $R$, the residual-substitution row remains $P$, and the candidate-normalization read is routed through $J,C,B$ with the obstruction row $O$ still visible.

Apply the seed-obligation and seed-readiness routes, \autoref{thm:metacic-parallel-diamond-frontier-seed-obligations} and \autoref{thm:metacic-parallel-diamond-frontier-seed-readiness}. They force every public seed read through the displayed frontier rows before any confluence-audit or normalization-facing handoff can be inspected.

The local join ledger is then completed only by transport, replay, provenance, and naming rows $H,T,G,N$. Since no carrier coordinate exports subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, residual-substitution discharge, or kernel consistency, the stated rows are exactly the seed-scope reads available before the candidate-mediated strong-normalization route is exported.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond scoped-status route]
\label{thm:metacic-parallel-diamond-frontier-scoped-status-route}
The scoped closure status for an accepted $\MetaCICParallelDiamondFrontierUp$ packet is witnessed by the finite dependency chain consisting of residual-substitution scope, premise preservation, critical-pair exhaustion, local-join scope, normalization-frontier handoff, candidate-mediated strong-normalization socket, typed-window candidate strong-normalization route, closed-substitution window scope, finite candidate strong-normalization extension, and residual-join induction. The chain exposes exactly the rows $P,K,J,R,S,C,B,O,H,T,G,N$ and does not assert residual-substitution compatibility, full parallel diamond, subject reduction, arbitrary closed Church--Rosser, evaluator equality, quotient equality of terms, or kernel consistency.
\end{theorem}
\begin{proof}
Start with residual-substitution scope and premise preservation, \autoref{thm:metacic-parallel-diamond-frontier-residual-substitution-scope} and \autoref{thm:metacic-parallel-diamond-frontier-premise-preservation}. They keep the premise row $P$ attached to every critical-pair, residual-frontier, closed-checker, and closed-normal read.

Read critical-pair exhaustion and local-join scope through \autoref{thm:metacic-parallel-diamond-critical-pair-exhaustion} and \autoref{thm:metacic-parallel-diamond-frontier-local-join-scope}. These routes place the candidate join inside the displayed $K,J,R,S,C,B,O$ rows before a confluence-facing consumer can inspect the packet.

Next apply the normalization-frontier handoff, candidate-mediated strong-normalization socket, and typed-window candidate route, \autoref{thm:metacic-parallel-diamond-normalization-frontier-handoff}, \autoref{thm:metacic-parallel-diamond-frontier-candidate-mediated-sn-socket}, and \autoref{thm:metacic-parallel-diamond-frontier-typed-window-candidate-sn-route}. Together they route every normalization-facing read through the same retained-premise frontier rather than through a global normalization theorem.

Finally use the closed-substitution window scope, finite candidate strong-normalization extension, and residual-join induction, \autoref{thm:metacic-parallel-diamond-frontier-closed-substitution-window-scope}, \autoref{thm:metacic-parallel-diamond-frontier-finite-candidate-sn-extension}, and \autoref{thm:metacic-parallel-diamond-frontier-residual-join-induction}. The structural rows $H,T,G,N$ transport, replay, source, and name the same finite packet, so the scoped-status evidence remains inside the displayed frontier rows and excludes the refused endpoints.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond obligation-status route]
\label{thm:metacic-parallel-diamond-frontier-obligation-status-route}
Every obligation-status read for an accepted $\MetaCICParallelDiamondFrontierUp$ packet must keep the residual-substitution premise $P$, critical-pair rows $K,R$, candidate join $J$, closed checker $S$, closed-normal fragment $C$, bounded check $B$, obstruction row $O$, transport row $H$, replay row $T$, provenance row $G$, and local name row $N$ visible before any confluence-audit, decidability, or normalization consumer inspects the frontier. The route is a retained-premise obligation surface; it does not discharge subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, residual-substitution compatibility, or kernel consistency.
\end{theorem}
\begin{proof}
Begin with the seed scope of \autoref{thm:metacic-parallel-diamond-frontier-seed-scope}. It fixes the visible frontier rows $P,K,J,R,S,C,B,O,H,T,G,N$ before any candidate-mediated strong-normalization route can be exported.

Apply the scoped-status route \autoref{thm:metacic-parallel-diamond-frontier-scoped-status-route}. The dependency chain it records already passes through residual-substitution scope, premise preservation, critical-pair exhaustion, local-join scope, normalization-frontier handoff, candidate-mediated strong-normalization, closed-substitution window scope, finite candidate extension, and residual-join induction.

Now read the candidate strong-normalization handoff and residual-join induction in that order. The handoff keeps the candidate join $J$, closed checker $S$, closed-normal fragment $C$, bounded check $B$, and obstruction row $O$ attached to the same frontier, while the residual-join induction keeps the premise $P$ and the critical-pair rows $K,R$ visible.

Finally use the critical-path retained-premise handoff carried by the displayed transport, replay, provenance, and local naming rows $H,T,G,N$. These rows identify the same packet for downstream confluence-audit, decidability, and normalization consumers. Since no row exports subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, residual-substitution discharge, or kernel consistency, the obligation-status route remains the retained-premise surface stated above.
\end{proof}
