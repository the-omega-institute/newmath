\begin{theorem}[MetaCIC parallel-diamond frontier seed-status child route]
\label{thm:metacic-parallel-diamond-frontier-seed-status-child-route}
The current seed-status child route for $\MetaCICParallelDiamondFrontierUp$ is read through the hub only after the carrier, obligation, seed-readiness, seed-status block sync, seed-status register, and seed-status closure block children expose the finite frontier packet
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The route records a seed-facing child status over the retained residual premise, critical-pair envelope, candidate join, residual frontier, checker, bounded check, obstruction, transport, replay, provenance, and local naming rows. It is not residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Read the carrier and obligation children first. They display the retained residual premise, critical-pair envelope, candidate join, residual frontier, checker, bounded check, obstruction, transport, replay, provenance, and local naming rows before the hub can route any status reader into a child file.

Next use the seed-readiness and seed-status block sync children. These keep the status read on the same packet $P,K,J,R,S,C,B,O,H,T,G,N$ and mark it as seed-facing rather than as a scoped, public, or bridged endpoint.

Finally compare with the seed-status register and seed-status closure block. They identify the hub input as a route into existing child status evidence, so the hub does not acquire theorem or proof content of its own and the child route exports none of the refused MetaCIC endpoints.
\end{proof}
