\begin{theorem}[MetaCIC parallel-diamond retained-premise discharge boundary]
\label{thm:metacic-parallel-diamond-frontier-retained-premise-discharge-boundary}
Every local diamond/frontier read of $\MetaCICParallelDiamondFrontierUp$ that consumes a candidate join row $J$ and residual frontier row $R$ must still expose the retained residual-substitution premise row $P$, the critical-pair row $K$, the closed-checker row $S$, the closed-normal fragment row $C$, the bounded-check row $B$, the obstruction row $O$, and the structural rows $H,T,G,N$. The route is not full residual-substitution discharge, subject reduction, Church--Rosser, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Read the local public certificate and finite residual witness routes. They expose the candidate join and residual frontier only inside the larger packet $P,K,J,R,S,C,B,O,H,T,G,N$, so consuming $J$ and $R$ cannot hide the premise, critical-pair, checker, normal-fragment, bounded-check, obstruction, or structural rows.

Apply the retained-premise closure and mature budgets. Those budgets require the residual-substitution premise and the obstruction-facing bounded check to remain visible through candidate-mediated diamond reads.

The listed off-table metatheorems are not rows of the packet. Since the carrier exports only the displayed finite evidence and structural transport, replay, provenance, and naming rows, the local read records a discharge boundary rather than a full residual-substitution or confluence theorem.
\end{proof}
