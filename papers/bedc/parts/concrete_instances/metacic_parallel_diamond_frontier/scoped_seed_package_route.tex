\begin{theorem}[MetaCIC parallel-diamond scoped seed package route]
\label{thm:metacic-parallel-diamond-frontier-scoped-seed-package-route}
The hub-level scoped marker for $\MetaCICParallelDiamondFrontierUp$ is read through the same finite seed package exposed by the spine: residual-substitution premise rows, critical-pair rows, candidate-join rows, residual-frontier rows, closed-checker rows, closed-normal fragment rows, bounded-check rows, obstruction rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. The route records the seed-status package already scoped by \autoref{thm:metacic-parallel-diamond-frontier-scoped-closure-status}; it does not add subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Read the scoped closure status theorem in the spine. It identifies the accepted packet by the finite route through $P,K,J,R,S,C,B,O,H,T,G,N$ and refuses the global confluence and kernel endpoints.

The hub marker is therefore a route into that same package rather than a second closure claim. It first records the residual-substitution premise, critical-pair, candidate, residual-frontier, checker, normal-fragment, bounded-check, and obstruction rows.

Finally the structural rows transport, replay, source, and name the packet. Thus the scoped marker is accounted for by a concrete child ledger while the top-level file remains structural.
\end{proof}
