\begin{theorem}[MetaCIC parallel-diamond current seed-axis record]
\label{thm:metacic-parallel-diamond-frontier-seed-axis-current-record}
The current seed-axis read of $\MetaCICParallelDiamondFrontierUp$ enters through the displayed carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier} and the checked obligation surface recorded by $\mathsf{BEDC.Derived.MetaCICParallelDiamondFrontierUp.MetacicParallelDiamondFrontierObligations}$. It exposes the retained residual-substitution premise, critical-pair row, candidate-join row, residual-frontier row, closed-checker row, closed-normal fragment, bounded-check row, obstruction row, transport, replay, provenance, and local naming before any seed-facing consumer can inspect the hub status route.
\end{theorem}
\begin{proof}
Read the carrier first. Its visible rows are exactly the retained premise, critical-pair, candidate join, residual frontier, checker, fragment, bounded check, obstruction, transport, replay, provenance, and local naming rows.

Next apply the obligation surface. The checked obligation theorem keeps the read on those displayed rows, so the seed-axis status cannot detach from the retained premise or obstruction data.

Finally compare with the existing seed-axis rotation and seed-status routes. They consume the same child packet, so the current record states only the present hub-to-child route and does not claim residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{proof}
