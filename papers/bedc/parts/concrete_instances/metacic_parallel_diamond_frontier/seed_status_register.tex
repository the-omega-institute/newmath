\begin{theorem}[MetaCIC parallel-diamond frontier seed status register]
\label{thm:metacic-parallel-diamond-frontier-seed-status-register}
The seed route for $\MetaCICParallelDiamondFrontierUp$ is registered through the existing carrier and obligation children of this hub. A seed-facing reader first consumes the carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier}, the obligation surface of \autoref{thm:metacic-parallel-diamond-frontier-obligations}, the seed-readiness route of \autoref{thm:metacic-parallel-diamond-frontier-seed-readiness}, and the seed-status block sync of \autoref{thm:metacic-parallel-diamond-frontier-seed-status-block-sync}. The registered seed surface is the displayed frontier packet over
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
read at $\seedClosure/\unformalizedV$ for seed consumers only. It is not global confluence, full Church--Rosser, unrestricted subject reduction, conversion decidability, evaluator equality, quotient term equality, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Project the carrier and obligation surface already input by the hub. These rows expose the retained residual premise, critical-pair envelope, candidate join, residual frontier, closed checker, closed-normal fragment, bounded check, obstruction, transport, replay, provenance, and local naming route.

Use the seed-readiness and seed-status block sync children next. They route seed-facing reads through the same displayed rows $P,K,J,R,S,C,B,O,H,T,G,N$ before any status reader can inspect the frontier.

The register therefore records only that this child route is available to seed consumers. Since every cited row is carrier-local and the packet has no coordinate for unrestricted confluence, Church--Rosser, subject reduction, conversion decidability, evaluator equality, quotient term equality, residual-substitution discharge, or kernel consistency, the registration does not claim any of those endpoints.
\end{proof}

The to-seed registration row for this child route is $\seedClosure/\unformalizedV$ with bridge status none. It records only the seed-facing read of \autoref{thm:metacic-parallel-diamond-frontier-seed-status-register}; it does not claim full Church--Rosser, subject reduction, conversion decidability, evaluator equality, quotient term equality, residual-substitution discharge, or kernel consistency.

\begin{theorem}[MetaCIC parallel-diamond frontier seed registration]
\label{thm:metacicparallel-diamond-frontier-seed-registration}
Every $\MetaCICParallelDiamondFrontierUp$ seed-status read must display the critical-pair envelope, residual-substitution dependency route, candidate-normalization scope, finite residual witness consumption, and candidate-mediated residual-join scope before the seed package is public. The seed package is therefore a route through the existing frontier children and not a claim of strong normalization, global confluence, or a completed parallel-diamond theorem.
\end{theorem}
\begin{proof}
Read the critical-pair envelope first through \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope}. It displays the bounded local-overlap rows before a seed-status consumer can inspect the frontier.

Next read the residual-substitution dependency route of \autoref{thm:metacic-parallel-diamond-frontier-residual-substitution-dependency-route}. This keeps the retained premise visible before the candidate and residual rows are consumed.

The candidate-normalization scope of \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope} then keeps the candidate row inside the same conditional spine. The finite residual witness consumption of \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption} records the bounded witness rows that the seed consumer may spend.

Finally use the candidate-mediated residual-join scope of \autoref{thm:metacic-parallel-diamond-candidate-mediated-residual-join-scope}. The factorization displays the required child routes before the seed package is public, while every cited child leaves strong normalization, global confluence, and the completed parallel-diamond theorem outside the packet.
\end{proof}
