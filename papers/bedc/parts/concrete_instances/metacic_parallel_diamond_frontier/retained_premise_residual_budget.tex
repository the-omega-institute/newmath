\begin{theorem}[MetaCIC retained-premise residual-budget]
\label{thm:metacic-parallel-diamond-frontier-retained-premise-residual-budget}
Every retained-premise residual-join read from an accepted $\MetaCICParallelDiamondFrontierUp$ packet must expose the critical-pair envelope, candidate-normalization scope, retained-premise boundary, and residual-join locality rows before any candidate-mediated strong-normalization handoff is consumed. Equivalently, the read remains on the carrier-local route
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
with $P$ as the retained premise, $K$ as the critical-pair envelope, $J$ as the candidate-normalization scope, $R,S,C,B$ as residual and checker locality rows, $O$ as the obstruction boundary, and $H,T,G,N$ as transport, replay, provenance, and local naming. The budget exports only this retained-premise residual surface; it does not discharge residual substitution, global confluence, subject reduction, evaluator equality, quotient term equality, unrestricted normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Read the retained-premise boundary first. It keeps $P$ visible before any critical-pair row, candidate row, residual witness, checker row, or obstruction row can be consumed.

Next apply the critical-pair envelope and candidate-normalization scope. These rows place $K$ and $J$ inside the same finite frontier packet and prevent the candidate-mediated handoff from being read as a premise-free normalization theorem.

Finally use residual-join locality. The rows $R,S,C,B,O,H,T,G,N$ keep the local join, obstruction, transport, replay, provenance, and naming data carrier-local. Therefore the residual-join read reaches only the displayed retained-premise budget before any candidate-mediated strong-normalization handoff is visible.
\end{proof}
