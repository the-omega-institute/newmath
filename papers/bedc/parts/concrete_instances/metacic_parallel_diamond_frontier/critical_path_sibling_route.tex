\begin{theorem}[MetaCIC critical-path bridge frontier]
\label{thm:metacic-critical-path-bridge-frontier}
Every accepted $\MetaCICParallelDiamondFrontierUp$ packet that is consumed by a MetaCIC critical-path reader must keep its residual diamond rows as concrete candidate and discharge data for $\MetaCICCriticalPathUp$, $\MetaCICConfluenceAuditPacketUp$, and $\MetacicNormalizationConfluenceBridgeUp$. In carrier terms, the rows $P,K,J,R,S,C,B,O,H,T,G,N$ may be routed into the critical-path, confluence-audit, and normalization-confluence bridge surfaces only as retained frontier evidence; they do not form a standalone confluence oracle, subject-reduction theorem, evaluator equality row, quotient equality row, or kernel-consistency certificate.
\end{theorem}
\begin{proof}
Project the frontier carrier through \autoref{def:metacic-parallel-diamond-frontier-carrier}. The residual-substitution premise, critical-pair data, candidate join, residual window, closed checker, closed-normal fragment, bounded check, and obstruction rows remain displayed before the transport, replay, provenance, and local name rows are read.

Use the route and handoff theorems of \autoref{thm:metacic-parallel-diamond-frontier-route} and \autoref{thm:metacic-parallel-diamond-confluence-handoff}. They make the exported handoff conditional on the retained premise and obstruction rows, so the confluence-audit packet can consume the rows only as bounded local evidence.

Now read the critical-path and normalization-confluence consumers. The $\MetaCICCriticalPathUp$ surface receives the same rows as candidate and discharge pressure, and $\MetacicNormalizationConfluenceBridgeUp$ receives them as bridge-facing boundary data attached to the confluence audit route.

Thus the sibling route is substantive: the frontier contributes concrete residual diamond rows to the critical-path and bridge consumers, while every standalone confluence-oracle, subject-reduction, evaluator-equality, quotient-equality, and kernel-consistency reading remains outside the packet.
\end{proof}
