\begin{theorem}[MetaCIC parallel-diamond frontier seed child route]
\label{thm:metacic-parallel-diamond-frontier-seed-child-route}
Every residual-diamond consumer that treats the $\MetaCICParallelDiamondFrontierUp$ seed route as public must first read the child carrier, the NameCert obligation surface, the closurestatus export row, and the closurestatus guard. In carrier notation the consumer reaches the seed route only through the displayed packet
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
and through \autoref{def:metacic-parallel-diamond-frontier-carrier}, \autoref{thm:metacic-parallel-diamond-frontier-obligations}, \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-export}, and \autoref{thm:metacic-parallel-diamond-frontier-closurestatus-guard}. The route does not export residual-substitution discharge, unrestricted subject reduction, full Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the hub shape. The parent file contributes only ordered child inputs, so any public seed read must enter through a child packet rather than through an independent hub theorem.

Project the frontier carrier and obligation theorem next. They expose the residual-substitution premise, critical-pair envelope, residual-join rows, candidate-normalization row, bounded witness rows, obstruction row, transport, replay, provenance, and local naming as the finite route $P,K,J,R,S,C,B,O,H,T,G,N$.

Finally apply the closurestatus export and guard. The export row identifies the seed route as a status read of the same packet, and the guard forces residual-diamond consumers to keep that packet visible before treating the route as public. Since the packet has no coordinate for the excluded global endpoints, the seed child route remains a finite frontier handoff.
\end{proof}
