\begin{theorem}[MetaCIC parallel-diamond frontier residual-join locality]
\label{thm:metacic-parallel-diamond-frontier-residual-join-locality}
Every accepted $\MetaCICParallelDiamondFrontierUp$ residual join is read through the local frontier route
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The read must first expose the critical-pair envelope of \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope}, the residual-substitution dependency route of \autoref{thm:metacic-parallel-diamond-frontier-residual-substitution-dependency-route}, the candidate-normalization scope of \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope}, the finite residual witness consumption row of \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}, and the carrier-local $\NameCert$ row of \autoref{thm:metacic-parallel-diamond-frontier-obligations}. It exports only a bounded residual-join locality certificate for the displayed packet, not global confluence, unbounded normalization, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICParallelDiamondFrontierUp.MetacicParallelDiamondFrontierResidualJoinLocality}
\begin{proof}
Project the accepted frontier carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier}. The only rows available to a residual-join consumer are the finite packet rows $P,K,J,R,S,C,B,O,H,T,G,N$.

Read the critical-pair and residual-substitution rows first. The critical-pair envelope keeps $K,J,R,S,C,B,O$ visible, while the dependency route places the retained residual-substitution premise $P$ before any local join can be consumed.

Next read candidate normalization and finite residual witnesses. The candidate-normalization scope keeps $J$ inside the same conditional spine, and the finite residual witness theorem consumes typed windows, residual frontier data, bounded checker rows, transport, replay, provenance, and local naming before a closed parallel-diamond route can inspect the join.

Finally use the local NameCert obligation surface. Since $H,T,G,N$ preserve only this finite packet and the obstruction row $O$ remains visible, the residual join is local to the displayed frontier and cannot be promoted to global confluence, unbounded normalization, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-join locality budget]
\label{thm:metacic-parallel-diamond-frontier-residual-join-locality-budget}
Every accepted residual-join locality read for a $\MetaCICParallelDiamondFrontierUp$ packet keeps the residual premise row $P$, critical-pair row $K$, candidate-join row $J$, residual witness row $R$, typed-window row $S$, bounded checker row $B$, obstruction row $O$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, $\Pkg$ provenance $G$, and local $\NameCert$ row $N$ inside the displayed finite packet before any normalization or confluence consumer reads it. The budget is
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
and it exports no residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with residual-join locality, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. It already forces every join read through the displayed frontier rows and keeps the retained premise and obstruction rows visible.

Next use finite residual witness consumption, \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. That route requires the typed windows, residual witness data, bounded checker rows, transport, replay, provenance, and local naming to be exposed before a local join is available to a downstream consumer.

The two routes combine as a finite budget. Rows $P,K,J,R,S,C,B,O$ remain the residual, critical-pair, candidate-join, witness, window, certificate, checker, and obstruction surface, while $H,T,G,N$ preserve the same packet under transport, replay, provenance, and local naming.

Since the carrier contains no coordinate for residual-substitution discharge, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, or kernel consistency, a normalization or confluence consumer can read only the displayed finite budget.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-substitution window]
\label{thm:metacic-parallel-diamond-residual-substitution-window}
Every candidate-mediated parallel-diamond read that invokes residual substitution must first display the critical-pair envelope, residual-substitution dependency row, finite residual witness, residual-join locality row, and candidate-normalization row before any strong-normalization or confluence-facing handoff is public. The public window is the carrier-local route
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
not a hidden substitution oracle, unrestricted confluence theorem, host conversion equality, subject-reduction theorem, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Begin with the local residual-join route of \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. It forces the accepted read through the displayed frontier rows and keeps the obstruction row visible.

Read the critical-pair and residual-substitution rows next. The critical-pair envelope of \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope} exposes the bounded overlap data, while the residual-substitution dependency route of \autoref{thm:metacic-parallel-diamond-frontier-residual-substitution-dependency-route} keeps the premise row $P$ in the carrier-local window.

Now attach the finite residual witness and candidate-normalization scope rows. The finite residual witness consumption theorem keeps the typed windows, residual witness data, bounded checker rows, transport, replay, provenance, and local naming visible, and \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope} keeps the candidate join inside the same conditional spine.

These rows compose to exactly the displayed residual-substitution window. Since the accepted packet contains no coordinate for a substitution oracle, unrestricted confluence theorem, host conversion equality, subject reduction, evaluator equality, quotient term equality, or kernel consistency, no such handoff is exported.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual grid]
\label{thm:metacic-parallel-diamond-frontier-residual-grid}
Every accepted $\MetaCICParallelDiamondFrontierUp$ residual-join consumer receives a finite grid whose rows are the critical-pair envelope, residual-substitution dependency, candidate-normalization scope, finite residual witness, carrier-local $\NameCert$, $\hsame$ transport, $\Cont$ replay, and $\Pkg$ provenance rows before any local diamond handoff is exported. The grid is the displayed packet
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
not a global confluence theorem, strong-normalization theorem, subject-reduction theorem, quotient term equality, or evaluator equality.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. It already forces a residual-join read through $P,K,J,R,S,C,B,O,H,T,G,N$ and refuses to promote the local route to global confluence or unbounded normalization.

Now refine the local route into grid rows. The critical-pair envelope of \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope} supplies the bounded overlap row, the residual-substitution dependency route of \autoref{thm:metacic-parallel-diamond-frontier-residual-substitution-dependency-route} places $P$ before any join handoff, the candidate-normalization scope of \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope} keeps $J$ inside the conditional spine, and finite residual witness consumption from \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption} supplies the typed-window and bounded-check rows.

Finally attach the carrier-local $\NameCert$ row and structural rows. The obligation theorem \autoref{thm:metacic-parallel-diamond-frontier-obligations} supplies the local naming surface, while $H,T,G,N$ transport, replay, source, and name the same carrier. Since every grid row is finite and carrier-local, the exported handoff cannot become strong normalization, subject reduction, quotient term equality, evaluator equality, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-join locality scope]
\label{thm:metacic-parallel-diamond-frontier-residual-join-locality-scope}
The residual-join locality surface of an accepted $\MetaCICParallelDiamondFrontierUp$ packet is scoped exactly by the finite rows
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The rows $P,K,J,R,S,C,B,O$ are the $\BHist$-visible confluence-frontier rows for residual premises, critical pairs, local join requests, typed windows, residual witnesses, bounded checker data, boundary certificates, and visible obstructions. The rows $H,T,G,N$ are precisely the $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows for that same packet. No residual-join locality read exports global confluence, subject reduction, evaluator equality, quotient term equality, kernel consistency, or premise-free residual substitution.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. It already forces every residual-join read through the displayed frontier rows and keeps the obstruction row visible.

Refine the route by \autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. The grid separates the confluence-facing rows $P,K,J,R,S,C,B,O$ from the structural rows $H,T,G,N$, while keeping both groups inside one finite packet.

Now apply the seed obligations and carrier obligations. The critical-pair envelope, residual-substitution dependency route, candidate-normalization scope, finite residual witness consumption, and carrier-local NameCert obligation theorem provide no extra coordinate beyond the rows displayed above.

Therefore a consumer that has read $P,K,J,R,S,C,B,O,H,T,G,N$ has exhausted the residual-join locality surface. A request for global confluence, subject reduction, evaluator equality, quotient term equality, kernel consistency, or premise-free residual substitution would require a carrier coordinate that the packet does not contain.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond scoped residual locality]
\label{thm:metacic-parallel-diamond-scoped-residual-locality}
Any accepted residual join in a $\MetaCICParallelDiamondFrontierUp$ packet must expose, before a local diamond read is public, the residual-substitution premise row $P$, the critical-pair row $K$, the candidate-join row $J$, the residual-frontier row $R$, the bounded checker row $B$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, $\Pkg$ provenance $G$, and the local $\NameCert$ row $N$. The read is a scoped residual-locality certificate over
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
not full Church--Rosser, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Begin with residual-join locality and the residual grid, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality} and \autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. These force the accepted join to pass through the displayed residual-substitution premise, critical-pair envelope, candidate join, finite residual witness, bounded checker, transport, replay, provenance, and local naming rows.

Next use the locality-scope theorem, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality-scope}. It identifies the residual-frontier and obstruction rows as part of the same finite carrier-local surface rather than as hidden global assumptions.

Finally apply finite residual witness consumption and residual-join witness locality, \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption} and \autoref{thm:metacic-parallel-diamond-residual-join-witness-locality}. They make the bounded checker rows visible before the local join can be exported and keep $P$ and $O$ inside the structural rows $H,T,G,N$.

Since the carrier contains only the displayed finite rows, the public read remains a local diamond certificate. Full Church--Rosser, subject reduction, evaluator equality, quotient term equality, and kernel consistency would require coordinates not present in the packet.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-join locality exhaustion]
\label{thm:metacic-parallel-diamond-frontier-residual-join-locality-exhaustion}
A residual-join consumer of an accepted $\MetaCICParallelDiamondFrontierUp$ packet that has read
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
has exhausted the carrier-local join surface. Any request for global confluence, subject reduction, unbounded normalization, evaluator equality, quotient term equality, or kernel consistency is outside the packet.
\end{theorem}
\begin{proof}
Apply \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. It forces every residual-join read through the displayed local frontier route and keeps the obstruction row visible.

Then apply \autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. The grid identifies the critical-pair, residual-substitution, candidate-normalization, finite-witness, transport, replay, provenance, and local naming rows as the whole carrier-local residual surface.

Finally use the carrier-local NameCert obligation theorem \autoref{thm:metacic-parallel-diamond-frontier-obligations}. Since that obligation surface admits no coordinate for global confluence, subject reduction, unbounded normalization, evaluator equality, quotient term equality, or kernel consistency, a consumer that has read the grid has no further join row to project.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual obstruction budget]
\label{thm:metacic-parallel-diamond-frontier-residual-obstruction-budget}
Any candidate-mediated residual join in an accepted $\MetaCICParallelDiamondFrontierUp$ packet that consumes the local join row $J$ and bounded checker rows must keep both the retained premise row $P$ and the obstruction row $O$ visible through the structural rows $H,T,G,N$. The handoff is only a bounded residual-join locality budget for
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
It cannot be read as residual-substitution discharge, subject reduction, global confluence, strong normalization, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Begin with residual-join locality, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. It forces every candidate-mediated join to read the displayed frontier rows and keeps the obstruction coordinate inside the same packet.

Use the residual grid theorem, \autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. The grid separates the retained premise, critical-pair, local join, residual witness, bounded checker, and obstruction rows from the transport, replay, provenance, and local naming rows.

Now apply the locality-scope theorem, \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality-scope}. It identifies $P$ and $O$ as visible rows of the residual-join surface rather than side conditions that can be discharged after $J$ is consumed.

Finally use residual-join locality exhaustion. Once the consumer has read $P,K,J,R,S,C,B,O,H,T,G,N$, there is no further coordinate for residual-substitution discharge, subject reduction, confluence, normalization, evaluator equality, quotient term equality, or kernel consistency. The obstruction budget is therefore retained as a carrier-local boundary.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-premise exhaustion]
\label{thm:metacic-parallel-diamond-frontier-residual-premise-exhaustion}
Any accepted residual-join read of a $\MetaCICParallelDiamondFrontierUp$ packet
that has consumed
$$
P,K,J,R,S,C,B,O,H,T,G,N
$$
has not discharged the retained premise row $P$. It has only made $P$ visible
as part of the finite carrier-local grid for the residual join. Consequently
the read cannot be used as premise-free residual substitution, subject
reduction, global confluence, strong normalization, evaluator equality,
quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with residual-join locality and the residual grid,
\autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality} and
\autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. These place every
accepted join inside the displayed carrier-local rows and identify $P$ as a row
of that grid, not as a premise erased by the join handoff.

Apply the locality-scope theorem and the residual obstruction budget,
\autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality-scope} and
\autoref{thm:metacic-parallel-diamond-frontier-residual-obstruction-budget}.
They keep the retained premise row and obstruction row visible through
$H,T,G,N$ after the local join and bounded checker rows have been read.

Finally use residual-join witness locality,
\autoref{thm:metacic-parallel-diamond-residual-join-witness-locality}. The
witness consumes finite residual data before exporting the local join, but it
does not add any coordinate for premise-free residual substitution, subject
reduction, global confluence, strong normalization, evaluator equality,
quotient term equality, or kernel consistency. The consumed packet therefore
exhausts visibility of the retained premise rather than discharging it.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-premise transport exhaustion]
\label{thm:metacic-parallel-diamond-frontier-residual-premise-transport-exhaustion}
An accepted residual-join read of a $\MetaCICParallelDiamondFrontierUp$
packet transports retained premises only through the displayed residual
locality route, candidate-normalization scope, finite residual witness
consumption rows, and critical-pair envelope rows. After the read has consumed
$$
P,K,J,R,S,C,B,O,H,T,G,N ,
$$
the transported premise surface is exhausted. No consumer can extend the read
to residual-substitution discharge, subject reduction, quotient equality,
evaluator equality, global confluence, strong normalization, or kernel
consistency.
\end{theorem}
\begin{proof}
Project the accepted carrier through the residual-premise exhaustion theorem,
\autoref{thm:metacic-parallel-diamond-frontier-residual-premise-exhaustion}.
It identifies $P$ as a retained premise row inside the finite grid rather than
as a discharged assumption.

Next read the locality and normalization rows. Residual-join locality,
\autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}, keeps
the premise inside the displayed frontier route, while candidate-normalization
scope from \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope}
keeps the candidate join in the same conditional spine.

Now consume the finite witness and critical-pair rows. The finite residual
witness theorem \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}
exposes the bounded residual data before the local join is available, and the
critical-pair envelope of \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope}
keeps the overlap rows inside the same carrier-local packet.

Finally inspect the carrier rows that remain after transport through $H,T,G,N$.
They replay and name only the displayed packet
$P,K,J,R,S,C,B,O,H,T,G,N$. Since the carrier has no coordinate for
residual-substitution discharge, subject reduction, quotient equality,
evaluator equality, global confluence, strong normalization, or kernel
consistency, the premise transport is exhausted at that boundary.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond frontier residual local confluence]
\label{thm:metacic-parallel-diamond-frontier-residual-local-confluence}
Let an accepted $\MetaCICParallelDiamondFrontierUp$ packet expose the finite rows
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
If the bounded checker rows accept both residual branches from the displayed critical-pair and residual-premise surface, then the consumer receives a carrier-local diamond handoff between the two residual endpoints through the displayed candidate join row $J$. The handoff is bounded by the same packet rows and does not claim global confluence, strong normalization, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
First read the residual grid of \autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. It exposes the critical-pair envelope, residual-substitution dependency, candidate-normalization scope, finite residual witness, local NameCert row, transport, replay, and provenance as one finite packet.

Next apply residual-premise transport exhaustion, \autoref{thm:metacic-parallel-diamond-frontier-residual-premise-transport-exhaustion}. It keeps the transported premise surface inside $P,K,J,R,S,C,B,O,H,T,G,N$ after both residual branches have been checked.

Now consume the finite witness and checker rows using \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. The bounded witness exposes the two residual endpoints before the candidate join row can be handed to a consumer.

Finally apply residual-join witness locality, \autoref{thm:metacic-parallel-diamond-residual-join-witness-locality}. It keeps $J$ carrier-local, retains $P$ and $O$ through $H,T,G,N$, and prevents the bounded handoff from becoming global confluence, strong normalization, subject reduction, evaluator equality, quotient term equality, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond candidate-mediated strong-normalization extension]
\label{thm:metacic-parallel-diamond-frontier-candidate-mediated-sn-extension}
Let an accepted $\MetaCICParallelDiamondFrontierUp$ packet expose the residual rows
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
Suppose a finite candidate packet reads its residual-premise row through \autoref{thm:metacic-parallel-diamond-frontier-residual-premise-exhaustion} and its residual-join row through \autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality}. Then the packet extends along the candidate-mediated frontier without leaving the same finite residual budget. A strong-normalization-facing handoff may consume the local diamond row $J$ together with $P,K,R,S,C,B,O,H,T,G,N$ rather than selecting global normal forms.
\end{theorem}
\begin{proof}
First read residual-premise exhaustion. It keeps $P$ as a retained premise row inside the displayed residual grid, so the candidate packet cannot erase the premise before the strong-normalization-facing handoff is formed.

Next apply residual-join locality and residual-premise transport exhaustion. These rows keep the candidate join $J$, residual frontier rows, checker rows, and obstruction row inside the same packet after transport through $H,T,G,N$.

Now consume the finite residual witness using \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. The witness exposes the bounded residual data and checker rows before the local diamond can be exported.

Finally apply residual local confluence, \autoref{thm:metacic-parallel-diamond-frontier-residual-local-confluence}. The local diamond row is already a carrier-local handoff over the finite residual budget, so a strong-normalization-facing consumer receives that displayed row rather than a selected global normal form.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond residual-join witness locality]
\label{thm:metacic-parallel-diamond-residual-join-witness-locality}
Every accepted residual-join witness for
$\MetaCICParallelDiamondFrontierUp$ must read the finite residual witness row
and the bounded checker rows before exporting the local join row. The read
retains both the premise row $P$ and the obstruction row $O$ through the
structural rows $H,T,G,N$, so its carrier surface is still
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
It does not discharge residual substitution, subject reduction, global
confluence, strong normalization, evaluator equality, quotient term equality,
or kernel consistency.
\end{theorem}
\begin{proof}
First apply finite residual witness consumption,
\autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}.
The witness read is forced through typed windows, residual witness data,
bounded checker rows, transport, replay, provenance, and local naming before
any local join row is exported.

Next apply residual-join locality and the residual grid,
\autoref{thm:metacic-parallel-diamond-frontier-residual-join-locality} and
\autoref{thm:metacic-parallel-diamond-frontier-residual-grid}. These results
place the join in the displayed finite route and keep the premise row $P$
inside the same carrier-local grid as the critical-pair, witness, checker, and
obstruction rows.

Finally apply the residual obstruction budget,
\autoref{thm:metacic-parallel-diamond-frontier-residual-obstruction-budget}.
It keeps $O$ visible through $H,T,G,N$ and prevents the local join witness from
being read as residual-substitution discharge, subject reduction, global
confluence, strong normalization, evaluator equality, quotient term equality,
or kernel consistency.
\end{proof}
