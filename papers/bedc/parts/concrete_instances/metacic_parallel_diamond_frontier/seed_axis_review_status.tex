\begin{theorem}[MetaCIC parallel-diamond frontier seed-axis review status]
\label{thm:metacic-parallel-diamond-frontier-seed-axis-review-status}
The seed-axis reader for $\MetaCICParallelDiamondFrontierUp$ is the displayed child-row family for critical pairs, residual joins, candidate normalization, and retained premises. A seed-facing consumer must read the critical-pair rows of \autoref{thm:metacic-parallel-diamond-critical-pair-exhaustion}, the residual-join rows of \autoref{thm:metacic-parallel-diamond-frontier-residual-peak-scope}, the candidate-normalization rows of \autoref{thm:metacic-parallel-diamond-frontier-seed-axis-summary}, and the retained-premise rows recorded by the frontier obstruction route before it may consume the hub-level seed marker. The reader records the theory axis at $\seedClosure$ for that route and leaves the formal axis to the chapter's existing local status rows; it exports no new Lean target, residual-substitution discharge, subject reduction, full Church--Rosser theorem, evaluator equality, quotient term equality, global normalization theorem, or kernel-consistency row.
\end{theorem}
\begin{proof}
Read the parent file only as an ordered hub. Its own body supplies input routing and the displayed seed marker for $\MetaCICParallelDiamondFrontierUp$, while the carrier and proof rows live in the child files.

The seed rows are therefore the child rows named in the statement. Critical-pair exposure, residual-join visibility, candidate-normalization scope, and retained-premise obstruction data are all read before the seed marker is consumed by a horizon ledger.

The final sentence is row exclusion. None of the named child rows contains a new Lean verification target or one of the refused MetaCIC endpoints, so the seed-axis reader cannot export those endpoints.
\end{proof}
