\begin{theorem}[MetaCIC parallel-diamond residual-substitution dependency route]
\label{thm:metacic-parallel-diamond-frontier-residual-substitution-dependency-route}
Every accepted $\MetaCICParallelDiamondFrontierUp$ packet that is consumed as a parallel-diamond frontier must first expose the residual-substitution premise row supplied by \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}. The consumer may route that premise into the local critical-pair, candidate-join, residual-frontier, closed-checker, and obstruction rows of $\MetaCICParallelDiamondFrontierUp$, but it cannot replace the premise by residual-substitution compatibility, subject reduction, full Church--Rosser, evaluator equality, quotient term equality, or kernel consistency.
\end{theorem}
\begin{proof}
Project the frontier carrier of \autoref{def:metacic-parallel-diamond-frontier-carrier}. The residual-substitution premise row $P$ is one of the displayed carrier coordinates, not an external theorem attached after the handoff.

Read the residual-substitution boundary chapter named in the statement. It supplies the visible premise boundary that the frontier route consumes before any candidate join or local-diamond handoff is public.

Finally apply the premise-retention and discharge-obstruction surfaces already present in the frontier chapter. They keep $P$ and the obstruction row visible for every consumer, so the dependency route remains a premise boundary rather than any of the refused confluence endpoints.
\end{proof}
