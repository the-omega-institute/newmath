\begin{theorem}[MetaCIC candidate-mediated diamond budget route]
\label{thm:metacic-parallel-diamond-frontier-seed-route}
Every candidate-mediated parallel-diamond read from an accepted $\MetaCICParallelDiamondFrontierUp$ packet must expose residual-substitution premise retention, critical-pair envelope rows, candidate normalization handoff, bounded obstruction rows, closed checker rows, and local $\NameCert$ rows before any confluence-facing conclusion is exported. The read is the finite route through the displayed frontier packet, not full normalization, global confluence, or host definitional equality.
\end{theorem}
\begin{proof}
Start with the residual-substitution route \autoref{thm:metacic-parallel-diamond-residual-substitution-route}. It keeps the residual-substitution premise row visible through the same packet that carries critical-pair and candidate rows.

Next read the critical-pair envelope and candidate-normalization scope, \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope} and \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope}. These rows expose the local pair and candidate handoff without discharging substitution or turning the handoff into a global normalization theorem.

Finally apply bounded local-confluence decision and finite residual witness consumption, \autoref{thm:metacic-parallel-diamond-frontier-bounded-local-confluence-decision} and \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. They keep the bounded obstruction, closed checker, transport, replay, provenance, and local naming rows attached before a confluence-facing consumer can inspect the result. Hence the exported conclusion is only the finite candidate-budget route stated above.
\end{proof}

\begin{theorem}[MetaCIC parallel diamond registered frontier route]
\label{thm:metacic-parallel-diamond-frontier-registered-frontier-route}
A candidate diamond budget route can expose a bounded checker status and local join candidate only after the retained-premise route, candidate-normalization row, residual envelope, and frontier register have all been displayed. The exposed status is a local registered-frontier read from the same $\MetaCICParallelDiamondFrontierUp$ packet; it is not subject reduction, residual-substitution discharge, quotient equality, global normalization, full confluence, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the seed route of \autoref{thm:metacic-parallel-diamond-frontier-seed-route}. It already forces residual-substitution premise retention, critical-pair envelope rows, candidate normalization handoff, bounded obstruction rows, closed checker rows, and local NameCert rows to remain visible before any confluence-facing conclusion is exported.

Next read the retained-premise and candidate-normalization rows through the same frontier packet. The existing residual-substitution route keeps the premise attached, while the candidate-normalization scope names the local join candidate without turning it into global normalization or subject reduction.

Finally use the frontier and local-join scope rows already recorded in the chapter. They register the bounded checker status only after the residual envelope and frontier display have been read, so the exported route remains a local finite checker route and refuses the global confluence, quotient, discharge, normalization, and consistency endpoints.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond candidate critical-pair boundary]
\label{thm:metacic-parallel-diamond-frontier-candidate-critical-pair-boundary}
Every local diamond budget read from an accepted $\MetaCICParallelDiamondFrontierUp$ packet must expose the residual-substitution premise row $P$, the two critical-pair branch rows carried by $K$ and $R$, the candidate join row $J$, the normalization-confluence handoff row, the critical-path socket, and the obstruction row $O$ before any confluence-audit consumer can inspect the read. The boundary remains candidate-mediated; it does not assert subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Begin with seed readiness and public seed readiness, \autoref{thm:metacic-parallel-diamond-frontier-seed-readiness} and \autoref{thm:metacic-parallel-diamond-frontier-public-seed-readiness}. These rows force the residual branches, candidate join, normalization-confluence handoff, and public frontier route to remain visible before any confluence-facing read is exported.

Next apply the critical-path retained-premise handoff, \autoref{thm:metacic-parallel-diamond-frontier-critical-path-retained-premise-handoff}. It keeps the residual-substitution premise and obstruction row attached to the critical-path socket rather than letting the socket discharge the premise.

Finally use the obligation-closure package \autoref{thm:metacic-parallel-diamond-frontier-obligation-closure-package}. The package assembles the residual branches, candidate join, normalization-confluence handoff, critical-path socket, and obstruction retention as the finite local surface. Since the surface is still candidate-mediated, none of the refused confluence, equality, normalization, discharge, or kernel endpoints is exported.
\end{proof}
