\begin{theorem}[MetaCIC candidate-mediated diamond budget route]
\label{thm:metacic-parallel-diamond-frontier-seed-route}
Every candidate-mediated parallel-diamond read from an accepted $\MetaCICParallelDiamondFrontierUp$ packet must expose residual-substitution premise retention, critical-pair envelope rows, candidate normalization handoff, bounded obstruction rows, closed checker rows, and local $\NameCert$ rows before any confluence-facing conclusion is exported. The read is the finite route through the displayed frontier packet, not full normalization, global confluence, or host definitional equality.
\end{theorem}
\begin{proof}
Start with the residual-substitution route \autoref{thm:metacic-parallel-diamond-residual-substitution-route}. It keeps the residual-substitution premise row visible through the same packet that carries critical-pair and candidate rows.

Next read the critical-pair envelope and candidate-normalization scope, \autoref{thm:metacic-parallel-diamond-frontier-critical-pair-envelope} and \autoref{thm:metacic-parallel-diamond-frontier-candidate-normalization-scope}. These rows expose the local pair and candidate handoff without discharging substitution or turning the handoff into a global normalization theorem.

Finally apply bounded local-confluence decision and finite residual witness consumption, \autoref{thm:metacic-parallel-diamond-frontier-bounded-local-confluence-decision} and \autoref{thm:metacic-parallel-diamond-frontier-finite-residual-witness-consumption}. They keep the bounded obstruction, closed checker, transport, replay, provenance, and local naming rows attached before a confluence-facing consumer can inspect the result. Hence the exported conclusion is only the finite candidate-budget route stated above.
\end{proof}
