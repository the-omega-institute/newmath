\begin{theorem}[MetaCIC parallel-diamond frontier seed-axis summary route]
\label{thm:metacic-parallel-diamond-frontier-seed-axis-summary-route}
Every seed-axis read of $\MetaCICParallelDiamondFrontierUp$ factors through \autoref{thm:metacic-parallel-diamond-frontier-seed-obligations}, \autoref{thm:metacic-parallel-diamond-frontier-seed-readiness}, \autoref{thm:metacic-parallel-diamond-seed-status}, and \autoref{thm:metacic-parallel-diamond-frontier-status-canonical} before any confluence or normalization consumer may inspect the frontier. The route exports only the displayed seed obligations, seed readiness, seed status, and canonical status children; it exports no subject-reduction endpoint, unrestricted confluence endpoint, evaluator equality, quotient term equality, global normalization, kernel consistency, or discharged residual-substitution premise.
\end{theorem}
\begin{proof}
Start with the seed-obligation child. It exposes the retained premise, critical-pair rows, candidate-join rows, residual-frontier row, checker rows, obstruction ledger, transport, replay, provenance, and local naming surface.

Next use seed readiness and seed status. These children keep the frontier on the same finite packet and prevent a confluence or normalization consumer from reading the hub as if it had discharged the retained premise.

Finally read the canonical status child. It identifies the status route already present in the ordered hub children, so every seed-axis consumer factors through those children before any frontier read is public.
\end{proof}
