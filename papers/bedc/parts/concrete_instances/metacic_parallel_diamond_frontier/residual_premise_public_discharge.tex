\begin{theorem}[MetaCIC residual premise public discharge]
\label{thm:metacic-parallel-diamond-frontier-residual-premise-public-discharge}
The residual-premise public export for an accepted $\MetaCICParallelDiamondFrontierUp$ packet is available only after the public reader has displayed the residual-pair rows, candidate-join rows, bounded checker rows, obstruction-retention row, componentwise transport row, replay row, provenance row, and local $\NameCert$ row. The discharge is therefore a public BEDC handoff of the retained premise surface, not a premise-free Church--Rosser theorem, subject-reduction theorem, evaluator equality, quotient equality, or kernel-consistency claim.
\end{theorem}
\begin{proof}
Start with the residual-premise public export theorem. It keeps the residual-substitution premise row visible before any confluence-facing public handoff can consume the critical-pair, candidate-join, residual-frontier, checker, bounded-check, obstruction, transport, replay, provenance, and local naming rows.

Next read the residual-pair and candidate-join rows through the existing frontier obligations. These rows display the paired endpoints and the candidate join before any bounded checker result is accepted as public data.

Finally keep the obstruction and structural rows visible. Obstruction retention prevents the packet from being reread as a premise-free confluence theorem, while transport, replay, provenance, and the local $\NameCert$ row make the public discharge a finite row exposure inside the same accepted packet.
\end{proof}
