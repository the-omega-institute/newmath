\begin{theorem}[MetaCIC parallel-diamond frontier seed obligations]
\label{thm:metacic-parallel-diamond-frontier-seed-obligations}
Every confluence consumer reading an accepted $\MetaCICParallelDiamondFrontierUp$ packet must first expose the seed-obligation surface
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The exposed rows include candidate pair rows, one-step residual rows, join-window rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the carrier-local $\NameCert$ row. A downstream reader may inspect a diamond conclusion only after these rows have been displayed as local frontier data; the seed surface is not subject reduction, global normalization, unrestricted confluence, evaluator equality, quotient term equality, residual-substitution discharge, or kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:metacic-parallel-diamond-frontier-carrier}. The candidate pair and one-step residual data are carried by the critical-pair, candidate-join, and residual-frontier rows $K,J,R$, with the retained premise row $P$ still visible.

Read the join-window and obstruction rows next. The closed-checker, closed-normal, bounded-check, and obstruction coordinates $S,C,B,O$ keep the local join inside the displayed window instead of promoting it to a global confluence theorem.

Finally apply the structural rows. Componentwise transport $H$, replay $T$, provenance $G$, and local naming $N$ preserve the same row order, so a consumer reaches a diamond conclusion only through the finite seed-obligation surface and not through subject reduction, evaluator equality, quotient equality, residual-substitution discharge, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond retained-obstruction obligation]
\label{thm:metacic-parallel-diamond-frontier-retained-obstruction-obligation}
Any confluence or normalization consumer of an accepted $\MetaCICParallelDiamondFrontierUp$ packet must keep the residual-substitution premise row $P$ and obstruction row $O$ visible through the displayed route
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
If either row is hidden, discharged, or replaced by an endpoint assertion, the read is not a discharge of subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:metacic-parallel-diamond-frontier-carrier}. The rows $P,K,J,R,S,C,B,O,H,T,G,N$ are the local data available to a confluence or normalization consumer, and the residual-substitution premise row $P$ remains part of that display.

Apply \autoref{thm:metacic-parallel-diamond-frontier-seed-obligations}. Its seed surface already forces the consumer to read the candidate pair, residual rows, join-window rows, closed-checker rows, obstruction row, transport, replay, provenance, and local naming before any diamond conclusion is inspected.

Now use the obstruction row $O$. It records that the accepted packet has not promoted the local diamond to subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency, so hiding $P$ or $O$ would erase a load-bearing premise rather than discharge it.

The public seed readiness of the chapter therefore remains conditional. A downstream consumer may pass along the finite candidate-mediated diamond surface only while $P$ and $O$ remain visible through the full route, and any endpoint promotion must be supplied by a separate packet.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond discharge boundary]
\label{thm:metacic-parallel-diamond-frontier-discharge-boundary}
A candidate-mediated diamond read from an accepted $\MetaCICParallelDiamondFrontierUp$ packet can be consumed only while the residual-substitution premise row $P$ and obstruction row $O$ remain visible through the route
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The read is therefore a retained frontier surface. It does not discharge subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:metacic-parallel-diamond-frontier-carrier}. The candidate-mediated diamond data are displayed only through the rows $P,K,J,R,S,C,B,O,H,T,G,N$.

Apply the seed-obligations theorem. It requires every confluence consumer to expose the retained premise, candidate pair, residual rows, join window, checker rows, obstruction row, transport, replay, provenance, and local naming before a diamond conclusion is inspected.

Now use the retained-obstruction obligation. That theorem keeps $P$ and $O$ visible as load-bearing rows, so a downstream read may pass along the candidate-mediated diamond surface only as a conditional frontier.

The excluded endpoint claims have no coordinate in the displayed route. Subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, and kernel consistency would require separate discharge data, not the retained candidate packet alone.
\end{proof}

\begin{theorem}[MetaCIC parallel-diamond frontier obligation scope]
\label{thm:metacic-parallel-diamond-frontier-obligation-scope}
Every accepted $\MetaCICParallelDiamondFrontierUp$ packet exposes the residual-substitution premise row $P$, critical-pair row $K$, candidate-join row $J$, residual-frontier row $R$, closed-checker row $S$, closed-normal row $C$, bounded-check row $B$, obstruction row $O$, and structural rows $H,T,G,N$ before any confluence consumer reads a parallel-diamond handoff. The visible scope is
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
It is a retained frontier surface, not a discharge of residual substitution, full Church--Rosser, subject reduction, evaluator equality, or kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:metacic-parallel-diamond-frontier-carrier}. Its finite coordinates are exactly the retained premise, critical-pair, candidate-join, residual-frontier, closed-checker, closed-normal, bounded-check, obstruction, transport, replay, provenance, and naming rows listed above.

Apply \autoref{thm:metacic-parallel-diamond-frontier-seed-obligations}. That obligation surface forces a confluence consumer to display the candidate pair, residual rows, join-window rows, closed-checker rows, obstruction row, and structural certificate rows before inspecting any local diamond conclusion.

Use \autoref{thm:metacic-parallel-diamond-frontier-retained-obstruction-obligation} for the retained boundary. The rows $P$ and $O$ remain visible as load-bearing premises, so the handoff cannot be read as residual-substitution discharge, full Church--Rosser, subject reduction, evaluator equality, or kernel consistency.
\end{proof}
