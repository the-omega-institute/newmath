\begin{theorem}[MetaCIC parallel-diamond frontier seed-status registration]
\label{thm:metacic-parallel-diamond-frontier-seed-status-frontier-registration}
The seed-status route for $\MetaCICParallelDiamondFrontierUp$ registers the parallel-diamond frontier through the already displayed finite packet
$$
P,K,J,R,S,C,B,O,H,T,G,N .
$$
The registration reads the retained premise, critical-pair envelope, candidate join, residual frontier, checker, bounded-check row, obstruction, transport, replay, provenance, and local naming before exposing the seed-facing status children. It is a frontier-local registration only; it does not discharge residual substitution, subject reduction, unrestricted confluence, evaluator equality, quotient term equality, global normalization, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the hub carrier and obligation children. They expose the retained residual premise, critical-pair row, candidate-join row, residual-frontier row, checker, bounded-check row, obstruction, transport, replay, provenance, and local naming rows before any status reader can enter the seed route.

Next use the seed-readiness, seed-status, and seed-axis children. These children keep the read on the same finite packet $P,K,J,R,S,C,B,O,H,T,G,N$ and record it as a seed-facing registration over the frontier.

Finally compare with the closurestatus and bridge-status children already routed through the hub. The registration names only the frontier-local seed status, so the refused MetaCIC endpoints remain outside the packet.
\end{proof}
