\begin{theorem}[MetaCIC parallel-diamond current premise join budget]
\label{thm:metacic-parallel-diamond-frontier-current-premise-join-budget}
Every current $\MetaCICParallelDiamondFrontierUp$ parallel-diamond read must preserve the residual-substitution premise row $P$, critical-pair row $K$, candidate-join row $J$, residual-frontier row $R$, closed-checker seal row $S$, closed-normal fragment row $C$, bounded-check row $B$, obstruction row $O$, and structural rows $H,T,G,N$ before any local-join budget is public. The budget is therefore a candidate-mediated finite frontier surface; it is not premise discharge, unrestricted Church--Rosser, subject reduction, evaluator equality, quotient term equality, global conversion decidability, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the frontier obligations of \autoref{thm:metacic-parallel-diamond-frontier-obligations}. They expose $P,K,J,R,S,C,B,O,H,T,G,N$ as the complete NameCert surface for an accepted packet.

Use the critical-pair envelope and candidate rows. The critical-pair row $K$ and candidate-join row $J$ can be read only while the residual-substitution premise $P$ remains visible.

Next open the residual-frontier, closed-checker, closed-normal, and bounded-check rows $R,S,C,B$. These rows record the finite residual windows and closed local checks that bound the candidate join, while $O$ keeps the missing endpoints visible.

Finally replay the structural rows $H,T,G,N$. Componentwise transport, continuation replay, provenance, and local naming preserve the same current packet, so the public local-join budget cannot erase $P$ or reread the frontier as global confluence, subject reduction, evaluator equality, quotient term equality, conversion decidability, or kernel consistency.
\end{proof}
