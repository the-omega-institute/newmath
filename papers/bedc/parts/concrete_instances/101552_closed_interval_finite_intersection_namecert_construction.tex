\chapter{A Concrete Naming Certificate for $\ClosedIntervalFiniteIntersectionUp$}
\label{ch:concrete-instances-closed-interval-finite-intersection-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\ClosedIntervalFiniteIntersectionUp$ packet records the finite closed-interval intersection certificate used by Real-and-Completion consumers. It reads the nested-window discipline of \autoref{ch:concrete-instances-nestedinterval-namecert}, the dyadic endpoint cells of \autoref{ch:concrete-instances-dyadicinterval-namecert}, the finite-family surface of \autoref{ch:concrete-instances-finite-intersection-property-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the stream naming windows of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite family of closed interval rows and their displayed intersection witness; it does not consume open-cover compactness, ambient set intersection, quotient real equality, selected endpoints, choice, or ambient $\RealUp$ completeness.

\begin{definition}[Closed-interval finite-intersection carrier]
\label{def:closed-interval-finite-intersection-carrier}
A $\ClosedIntervalFiniteIntersectionUp$ carrier is a finite $\BHist$ packet
$$
K=(J,I,W,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $J$ is the finite index row, recording the inspected finite family of closed-interval requests and the finite subfamily order used by the packet.
\item $I$ is the closed-interval row family. Each displayed member carries lower and upper endpoint names, its closed-boundary ledger, and a containment surface read through the $\NestedIntervalUp$ chapter when the family is inspected as a nested or retained-window route.
\item $W$ is the finite intersection witness row. It records the common interval cell, the endpoint comparison evidence, and the nonempty-window read licensed by the displayed finite rows $J$ and $I$.
\item $D$ is the dyadic endpoint ledger. It records the $\DyadicIntervalUp$ endpoint cells, dyadic refinements, mesh windows, and endpoint comparison rows used to make the finite intersection witness inspectable.
\item $S$ is the $\StreamNameUp$ window row, naming the observed endpoint streams and the common-window schedule used by $D$ and $W$.
\item $R$ is the $\RegSeqRatUp$ readback row, recording the rational approximation data that makes the endpoint windows, common cell, and terminal seal readable at finite precision.
\item $E$ is the terminal $\RealUp$ seal row reached from $J,I,W,D,S,R$ and from no other route.
\item $H$ records componentwise $\hsame$ transport for the finite index row, closed-interval rows, finite intersection witness, dyadic endpoint ledger, stream windows, regular rational readback, Real seal, replay rows, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from the finite family request through closed interval rows, dyadic endpoint inspection, stream windows, regular rational readback, and the terminal Real seal.
\item $P$ is the $\Pkg$ provenance over $\ClosedIntervalFiniteIntersectionUp$, $\NestedIntervalUp$, $\DyadicIntervalUp$, $\FiniteIntersectionPropertyUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ClosedIntervalFiniteIntersectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for open-cover compactness, ambient set intersection, arbitrary closed-family compactness, quotient real equality, selected endpoints, choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.ClosedIntervalFiniteIntersectionUp.ClosedIntervalFiniteIntersectionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Closed-interval finite-intersection NameCert obligations]
\label{thm:closed-interval-finite-intersection-namecert-obligations}
For every accepted $\ClosedIntervalFiniteIntersectionUp$ carrier
$$
K=(J,I,W,D,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\ClosedIntervalFiniteIntersectionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the finite index row, closed-interval family, intersection witness, dyadic endpoint ledger, stream windows, regular rational readback, Real seal, transport, replay, provenance, and local naming rows;
\item finite-family exactness, requiring every interval request to be read through $J$ and every displayed closed interval to be read through $I$;
\item intersection-witness exactness, requiring $W$ to expose the common cell and endpoint comparisons downstream of the displayed finite family;
\item dyadic endpoint visibility, requiring $D$ to mediate endpoint refinements and comparisons before any stream, rational, or Real-facing seal is exported;
\item stream and rational readback visibility, requiring $S$ and $R$ to expose the finite windows and regular rational approximations used by the terminal seal;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming for the same rows through $H,C,P,N$;
\item non-escape, requiring the packet to export no open-cover compactness, ambient set intersection, arbitrary closed-family compactness, quotient real equality, selected endpoint, choice principle, or ambient $\RealUp$ completeness theorem.
\end{enumerate}
No closed-interval finite-intersection obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:closed-interval-finite-intersection-carrier}. The visible packet consists exactly of the finite index row $J$, closed-interval rows $I$, intersection witness $W$, dyadic endpoint ledger $D$, stream row $S$, regular rational readback $R$, Real seal $E$, and structural rows $H,C,P,N$.

Finite-family exactness is rowwise. Every accepted request first names its finite family through $J$, and every member of that family is then read through the displayed closed-interval row family $I$ before any common cell is visible.

The intersection witness is downstream of those source rows. The row $W$ exposes the common interval cell and endpoint comparisons, while $D$ supplies the dyadic endpoint ledgers and refinements that make the comparisons inspectable.

The Real-facing handoff remains finite. The stream row $S$ and regular rational row $R$ provide the windows and readbacks used by the terminal seal $E$, and $H,C,P,N$ only transport, replay, source, and name the same route.

Since none of the excluded compactness, set-intersection, quotient-equality, endpoint-selection, choice, or ambient-completeness objects occurs in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Closed-interval finite-intersection Real handoff]
\label{thm:closed-interval-finite-intersection-real-handoff}
Every $\RealUp$-facing consumer of an accepted $\ClosedIntervalFiniteIntersectionUp$ carrier factors its intersection read through the finite route
$$
J,I,W \longmapsto D \longmapsto S,R \longmapsto E \longmapsto H,C,P,N .
$$
Consequently the consumer receives displayed finite closed-interval rows, a finite intersection witness, dyadic endpoint ledgers, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row. The handoff supplies no open-cover compactness, ambient set intersection, quotient real equality, selected endpoint, choice principle, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier $K=(J,I,W,D,S,R,E,H,C,P,N)$. The finite index row $J$, closed-interval row family $I$, and intersection witness $W$ are the only source coordinates available to the consumer.

The endpoint route is dyadic before it is real-facing. The row $D$ records the endpoint cells and refinements, so every comparison used by the common cell passes through visible $\DyadicIntervalUp$ data.

Read the stream and rational stages next. The row $S$ supplies the finite $\StreamNameUp$ windows, and $R$ supplies the $\RegSeqRatUp$ readback attached to the same endpoint and common-cell route.

Only then is the terminal seal $E$ available. The structural rows $H,C,P,N$ preserve, replay, source, and name the same displayed route, so no consumer can bypass the listed finite rows.
\end{proof}

\falsifiablePrediction{Every $\ClosedIntervalFiniteIntersectionUp$ read factors through a finite index row, displayed closed-interval rows, a finite intersection witness, dyadic endpoint ledgers, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{nestedinterval, dyadicinterval, finite-intersection-property, regseqrat, streamname, real: $\ClosedIntervalFiniteIntersectionUp$ is not bijective to any listed sibling because it jointly carries finite closed-interval family data, a finite common-cell witness, dyadic endpoint ledgers, stream windows, regular rational readback, a Real seal, transport, replay, provenance, and local naming as one Real-facing certificate.}

\closureat{ClosedIntervalFiniteIntersectionUp}{seedStr}

\begin{closurestatus}{\ClosedIntervalFiniteIntersectionUp}
  \constructivestory{A $\ClosedIntervalFiniteIntersectionUp$ packet is generated from a finite index row, displayed closed-interval rows, a finite intersection witness, dyadic endpoint ledgers, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:closed-interval-finite-intersection-carrier}, \autoref{thm:closed-interval-finite-intersection-namecert-obligations}, and \autoref{thm:closed-interval-finite-intersection-real-handoff} seed the finite closed-interval intersection certificate over closed interval rows, common-cell witnesses, dyadic endpoint ledgers, StreamName windows, RegSeqRat readback, Real seals, transport, replay, provenance, and local naming.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.ClosedIntervalFiniteIntersectionUp.ClosedIntervalFiniteIntersectionTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, ambient set intersection, arbitrary closed-family compactness, quotient real equality, selected endpoints, choice, ambient $\RealUp$ completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-family exactness, intersection-witness exactness, dyadic endpoint visibility, stream and rational readback visibility, Real seal routing, transport, replay, provenance, and local naming for BEDC.Derived.ClosedIntervalFiniteIntersectionUp.}
\end{closurestatus}
