\chapter{A Concrete Naming Certificate for $\BanachAlaogluUp$}
\label{ch:concrete-instances-banach-alaoglu-namecert}
\origin{human}

Following Banach and Alaoglu, the $\BanachAlaogluUp$ packet records the finite weak-star dual-ball window that functional-analysis consumers can inspect before any unrestricted compactness theorem is available. It sits over the Banach source row of \autoref{ch:concrete-instances-banach-namecert}, the bounded-linear-functional surface supplied by \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, the scalar norm readback of \autoref{ch:concrete-instances-normed-real-line-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the finite product compactness handoff of \autoref{ch:concrete-instances-tychonoff-finite-product-namecert}. The chapter names only finite windows in the weak-star dual ball; it does not claim unrestricted Tychonoff compactness, ultrafilter arguments, host weak-star compactness, quotient function spaces, or selected cluster points.

\begin{definition}[Banach-Alaoglu finite weak-star dual-ball carrier]
\label{def:banach-alaoglu-carrier}
A $\BanachAlaogluUp$ carrier is a finite $\BHist$ packet
$$
A=(B,L,W,S,R,F,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the Banach source row, read through the displayed $\BanachUp$ carrier and its norm, metric, regular-Cauchy, and completion ledgers.
\item $L$ is the bounded-linear functional family row, whose members are read as $\BoundedLinearOperatorUp(B,\RealUp)$ coordinates with displayed operator bounds.
\item $W$ is the finite weak-star seminorm-window row, listing the finitely many source probes and scalar tolerances allowed in the present observation.
\item $S$ is the dual-ball bound row, recording the shared scalar bound on the displayed functionals through $\NormedRealLineUp$ and $\RealUp$ readbacks.
\item $R$ is the Real and normed-real readback row for all scalar values, scalar bounds, and seminorm tolerances exposed by the window.
\item $F$ is the finite-product handoff row, sending the displayed scalar coordinate window to the finite $\TychonoffFiniteProductUp$ compactness surface.
\item $T$ records componentwise $\hsame$ transport for the Banach source, functional family, weak-star window, scalar readbacks, finite-product handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a weak-star window request through $B,L,W,S,R,F,T,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BanachAlaogluUp$, $\BanachUp$, $\BoundedLinearOperatorUp$, $\NormedRealLineUp$, $\RealUp$, $\TychonoffFiniteProductUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BanachAlaogluUp}$ row.
\end{enumerate}
The carrier has no coordinate for unrestricted Tychonoff compactness, ultrafilter or choice arguments, host weak-star compactness, quotient function spaces, selected cluster points, ambient dual-space compactness, Lean verification, or standard bridge data.
\end{definition}

\begin{theorem}[Banach-Alaoglu NameCert obligations]
\label{thm:banach-alaoglu-namecert-obligations}
For the carrier of \autoref{def:banach-alaoglu-carrier}, the local $\NameCert_{\BanachAlaogluUp}$ surface consists of exactly six finite obligations:
\begin{enumerate}
\item Banach source admission, requiring $B$ to be read through the displayed source row of \autoref{ch:concrete-instances-banach-namecert};
\item bounded-linear-functional admission, requiring each coordinate of $L$ to pass through the bounded-linear-operator surface of \autoref{ch:concrete-instances-boundedlinearoperator-namecert};
\item finite weak-star window admission, requiring $W$ to expose only finitely many source probes and scalar tolerances;
\item scalar readback admission, requiring $S$ and $R$ to read all bounds and scalar values through \autoref{ch:concrete-instances-normed-real-line-namecert} and \autoref{ch:concrete-instances-real-namecert};
\item finite-product handoff exactness, requiring every compactness-facing read to pass through $F$ and the finite product surface of \autoref{ch:concrete-instances-tychonoff-finite-product-namecert};
\item transport, replay, provenance, and local naming stability, requiring $T,C,P,N$ to preserve the same finite route under componentwise $\hsame$, displayed $\Cont$, package provenance, and local naming.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier rows. The displayed Banach source, bounded-linear functional family, finite weak-star window, scalar bound and readback rows, finite-product handoff, transport, replay, provenance, and local naming rows exhaust the visible inventory of $A$.

The source and functional obligations are read from the cited sibling certificates. The Banach row supplies the normed source data, and the bounded-linear-operator row supplies the finite bounded-functional surface into the scalar real line.

The weak-star content is only the finite row $W$. Its probes and tolerances are read through $S$ and $R$, so every scalar comparison remains inside the displayed normed-real and Real readbacks.

Finally inspect a compactness-facing consumer. It may reach only the finite-product row $F$ after the displayed scalar coordinates have been exposed. Since no unrestricted product, ultrafilter, quotient function space, or selected cluster-point row is present, the certificate closes only the finite weak-star window.
\end{proof}

\begin{theorem}[Banach-Alaoglu finite dual-ball handoff]
\label{thm:banach-alaoglu-finite-dual-ball-handoff}
Every accepted $\BanachAlaogluUp$ packet exports the route
$$
B,L,W,S,R \longmapsto F \longmapsto T,C,P,N
$$
to any duality, compactness, or functional-analysis consumer. The consumer receives a finite weak-star dual-ball window and the corresponding finite product compactness handoff, but receives no unrestricted Tychonoff compactness theorem, no ultrafilter or choice argument, no host weak-star compactness claim, no quotient function space, and no selected cluster point.
\end{theorem}
\begin{proof}
Begin with an accepted packet and a consumer request. The request first reads the Banach source row $B$ and the bounded-linear functional row $L$, so the functional data is already confined to displayed source and scalar surfaces.

The finite weak-star window $W$ then selects the only probes and scalar tolerances that may be inspected. Rows $S$ and $R$ translate those displayed values and bounds through the normed-real and Real readback interfaces.

Only after these rows are present can the consumer reach $F$. That row is the finite-product handoff, not an unrestricted product theorem or a compactness assertion over an entire host dual ball.

The structural rows $T,C,P,N$ preserve and replay the same finite route. Because the carrier contains no selector, quotient function space, ultrafilter, or unrestricted compactness coordinate, the exported handoff is exactly the finite weak-star dual-ball window named above.
\end{proof}

\closureat{BanachAlaogluUp}{seedStr}

\begin{closurestatus}{\BanachAlaogluUp}
  \constructivestory{A $\BanachAlaogluUp$ packet is generated from a Banach source row, a bounded-linear functional family row, finite weak-star seminorm windows, Real and normed-real scalar readbacks, a finite-product handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:banach-alaoglu-carrier}, \autoref{thm:banach-alaoglu-namecert-obligations}, and \autoref{thm:banach-alaoglu-finite-dual-ball-handoff} seed the finite weak-star dual-ball window consumed by later compactness and duality routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close unrestricted Tychonoff compactness, ultrafilter or choice arguments, host weak-star compactness, quotient function spaces, selected cluster points, ambient dual-space compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-linear functional family admission, finite weak-star window exactness, scalar readback admission, finite-product handoff exactness, transport, replay, provenance, and local naming for BEDC.Derived.BanachAlaogluUp.}
\end{closurestatus}
