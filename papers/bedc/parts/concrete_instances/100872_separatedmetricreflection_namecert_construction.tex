\chapter{A Concrete Naming Certificate for $\SeparatedMetricReflectionUp$}
\label{ch:concrete-instances-separatedmetricreflection-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and the Bourbaki universal-mapping language, the $\SeparatedMetricReflectionUp$ packet records the separated-reflection route consumed by L10 completion uniqueness. It reads the separated classifier surface of \autoref{ch:concrete-instances-separatedmetric-namecert}, the completion adjunction route of \autoref{ch:concrete-instances-cauchy-completion-adjunction-namecert}, the extension boundary of \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, the reflection consumer of \autoref{ch:concrete-instances-completionreflection-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records this route as finite NameCert data before any consumer treats zero-distance evidence as host equality or quotient-space structure.

\begin{definition}[SeparatedMetricReflection carrier]
\label{def:separated-metric-reflection-carrier}
A $\SeparatedMetricReflectionUp$ carrier is a finite $\BHist$ packet
$$
R=(X,S,A,U,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\MetricUp$ source row, including the point classifier and metric-distance witness surface available before separation is read.
\item $S$ is the $\SeparatedMetricUp$ zero-distance classifier row, exposing only the finite route from a displayed zero-distance witness to point-classifier equivalence.
\item $A$ is the $\CauchyCompletionAdjunctionUp$ completion route that carries dense-row admission, completion-seal access, extension, and uniqueness by dense agreement.
\item $U$ is the $\CompletionUniversalPropertyUp$ extension row read by completion-facing consumers after the separated classifier has been routed through $A$.
\item $Z$ is the reflected classifier output, obtained by reading $S$ through the completion route and then returning the classifier evidence at the separated-reflection surface.
\item $H$ stores componentwise $\hsame$ transport for the metric source, separated classifier, completion adjunction, universal row, reflected classifier, continuation row, package row, and local naming row.
\item $C$ stores the $\Cont$ replay from metric-distance witnesses to zero-distance readings, from those readings to the separated classifier, and from the separated classifier through the completion universal route to $Z$.
\item $P$ is the $\Pkg$ provenance over $\MetricUp$, $\SeparatedMetricUp$, $\CauchyCompletionAdjunctionUp$, $\CompletionUniversalPropertyUp$, $\CompletionReflectionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SeparatedMetricReflectionUp}$ row.
\end{enumerate}
The carrier exposes separated reflection only as a finite classifier route. It carries no quotient metric space, no host equality coordinate, no trichotomy row, no open-cover compactness principle, no countable-choice selector, and no ambient completeness theorem for $\RealUp$.
\end{definition}
\leanchecked{BEDC.Derived.SeparatedMetricReflectionUp.SeparatedMetricReflectionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[SeparatedMetricReflection NameCert obligations]
\label{thm:separated-metric-reflection-namecert-obligations}
The seed $\NameCert_{\SeparatedMetricReflectionUp}$ surface for \autoref{def:separated-metric-reflection-carrier} consists exactly of five finite obligation rows:
\begin{enumerate}
\item carrier exposure: an accepted object exposes only the packet $R=(X,S,A,U,Z,H,C,P,N)$;
\item zero-distance classifier exactness: every classifier read begins with the metric-distance witness in $X$, passes through the zero-distance classifier row $S$, and reaches the reflected classifier output $Z$;
\item adjunction compatibility: the completion-facing part of the route is read through the dense-row and extension coordinates carried by $A$;
\item universal-route preservation: the extension row $U$ preserves the order $\MetricUp\to\SeparatedMetricUp\to\CauchyCompletionAdjunctionUp\to\CompletionUniversalPropertyUp$ before a consumer reads $Z$;
\item ledger non-escape: the provenance row $P$ exports no quotient metric space, host equality, trichotomy, open-cover compactness, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{enumerate}
Thus the packet binds the separated-reflection boundary used by completion uniqueness without adding quotient, host-equality, choice, compactness, or ambient-completeness data.
\end{theorem}
\begin{proof}
Carrier exposure is projection from the tuple in \autoref{def:separated-metric-reflection-carrier}. The source row is $X$, the separated classifier is $S$, the completion adjunction is $A$, the universal row is $U$, the reflected classifier is $Z$, and the structural rows are $H,C,P,N$.

Zero-distance classifier exactness follows by reading the displayed route in order. A metric-distance witness starts in $X$, the zero-distance classifier row $S$ converts that witness into point-classifier evidence, and the output coordinate $Z$ records the reflected classifier read.

Adjunction compatibility and universal-route preservation are the completion-facing clauses. The row $A$ is the only dense-row and extension route carried by the packet, while $U$ keeps the order of the route fixed before any consumer reads the reflected classifier.

Ledger non-escape is an inventory check on $P$. Since quotient metric spaces, host equality, trichotomy, open-cover compactness, countable choice, ambient $\RealUp$ completeness, Lean verification, and bridge checking are not fields of $R$, no local NameCert obligation can use them.
\end{proof}

\begin{theorem}[SeparatedMetricReflection universal route]
\label{thm:separated-metric-reflection-universal-route}
Every completion-facing consumer of an accepted $\SeparatedMetricReflectionUp$ packet factors through the displayed route
$$
\MetricUp\to\SeparatedMetricUp\to\CauchyCompletionAdjunctionUp\to\CompletionUniversalPropertyUp\to\RealUp\to\NameCert
$$
before reading the reflected classifier $Z$. The consumer may inspect the metric source row, the zero-distance classifier row, the completion adjunction route, the universal extension row, the real-name seal, the structural $\hsame$ and $\Cont$ rows, the package provenance, and the local naming row. It may not bypass the route by reading quotient metric spaces, host equality, trichotomy, open-cover compactness, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:separated-metric-reflection-carrier}. Its public consumer-facing rows occur in the order $X,S,A,U,Z$, with $H,C,P,N$ supplying transport, replay, provenance, and local admission.

The metric source row $X$ supplies distance witnesses, and $S$ turns the relevant zero-distance readings into separated classifier evidence. The row $A$ moves that evidence to the completion-facing boundary, while $U$ records the universal extension surface a consumer is allowed to read.

The real-name seal and the local NameCert row are read only after this route has been preserved. Because $P$ names no quotient, host-equality, trichotomy, open-cover compactness, choice, or ambient-completeness field, every accepted completion-facing read factors through the displayed chain before reaching $Z$.
\end{proof}

\begin{closurestatus}{\SeparatedMetricReflectionUp}
  \constructivestory{A $\SeparatedMetricReflectionUp$ packet is generated from a $\MetricUp$ source row, a $\SeparatedMetricUp$ zero-distance classifier row, a $\CauchyCompletionAdjunctionUp$ completion route, a $\CompletionUniversalPropertyUp$ extension row, a reflected classifier output, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:separated-metric-reflection-carrier}, \autoref{thm:separated-metric-reflection-namecert-obligations}, and \autoref{thm:separated-metric-reflection-universal-route} seed the separated-reflection route consumed by completion-facing classifier reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient metric spaces, host equality, trichotomy, open-cover compactness, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier exposure, zero-distance classifier exactness, adjunction compatibility, universal-route preservation, and ledger non-escape for the same finite packet.}
\end{closurestatus}
