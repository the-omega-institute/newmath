\chapter{A Concrete Naming Certificate for $\MetaCICResidualCriticalPairBudgetUp$}
\label{ch:concrete-instances-metacic-residual-critical-pair-budget-namecert}
\origin{ai}

The $\MetaCICResidualCriticalPairBudgetUp$ packet realizes the residual critical-pair budget branch of the MetaCIC confluence audit map as finite NameCert data. It sits after the residual critical-pair carrier of \autoref{ch:concrete-instances-metacic-residual-critical-pair-namecert}, the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}, and the local-diamond witness of \autoref{ch:concrete-instances-metacic-local-diamond-witness-namecert}. The packet records a bounded residual-frontier budget, candidate local joins, closed-check rows, obstruction retention, TasteGate readiness, replay, provenance, and local naming; it does not claim global confluence, unrestricted residual-substitution compatibility, subject reduction, quotient syntax equality, or a hidden evaluator.

\begin{definition}[MetaCIC residual critical-pair budget carrier]
\label{def:metacic-residual-critical-pair-budget-carrier}
A $\MetaCICResidualCriticalPairBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(R,K,J,D,O,T,H,C,P,N)
$$
whose rows are the residual critical-pair source row $R$, the finite budget row $K$, the candidate local-join row $J$, the closed-check row $D$, the obstruction row $O$, the TasteGate readiness row $T$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert_{\MetaCICResidualCriticalPairBudgetUp}$ row $N$. The classifier compares accepted packets only through these rows. It has no coordinate for global Church--Rosser, arbitrary substitution compatibility, subject reduction, quotient syntax equality, or host evaluator soundness.
\end{definition}

\begin{theorem}[MetaCIC residual critical-pair budget obligations]
\label{thm:metacic-residual-critical-pair-budget-namecert-obligations}
For every accepted $\MetaCICResidualCriticalPairBudgetUp$ carrier $B=(R,K,J,D,O,T,H,C,P,N)$, the local $\NameCert_{\MetaCICResidualCriticalPairBudgetUp}$ surface consists exactly of residual critical-pair source admission through $R$, finite budget admission through $K$, candidate local-join admission through $J$, closed-check admission through $D$, obstruction retention through $O$, TasteGate readiness through $T$, transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-critical-pair-budget-carrier}. The confluence-facing rows are $R,K,J,D,O$, and the structural rows are $T,H,C,P,N$.

The residual source row $R$ is read before the finite budget row $K$ and candidate local-join row $J$. This keeps each critical-pair read inside a displayed residual frontier rather than treating a local join as an unbounded confluence theorem.

The closed-check row $D$ and obstruction row $O$ then separate the inspected closed fragment from the missing residual-substitution and parallel-diamond inputs. Finally $T,H,C,P,N$ reserve the TasteGate instance boundary, transport, replay, source, and name the same finite route.
\end{proof}

\begin{theorem}[MetaCIC residual critical-pair budget boundary]
\label{thm:metacic-residual-critical-pair-budget-boundary}
Every confluence-audit consumer of an accepted $\MetaCICResidualCriticalPairBudgetUp$ carrier receives only the finite route
$$
R,K,J,D,O,T,H,C,P,N .
$$
The route may budget residual critical-pair inspections and candidate local joins, but it cannot erase the obstruction row $O$, manufacture unrestricted residual-substitution compatibility, prove a parallel diamond, export global confluence, or supply subject reduction.
\end{theorem}
\begin{proof}
Start with the residual source and finite budget rows $R,K$. They bind the consumer to an inspected residual frontier and a displayed budget before any candidate join is read.

Read the candidate local-join and closed-check rows $J,D$. These rows may admit a bounded local join for the displayed frontier, but they do not contain arbitrary substitution compatibility or a global diamond.

The obstruction and structural rows $O,T,H,C,P,N$ preserve the boundary. The Lean Nontrivial and FieldFaithful TasteGate instances required by $T$ must be supplied before the packet can be consumed as a checked bridge; until then the route remains finite residual-budget evidence with the obstruction visible.
\end{proof}

\falsifiablePrediction{An accepted $\MetaCICResidualCriticalPairBudgetUp$ confluence-audit read that consumes a residual critical-pair budget without displaying rows $R,K,J,D,O,T,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{metacic-residual-critical-pair, metacic-confluence-audit-packet, metacic-local-diamond-witness: $\MetaCICResidualCriticalPairBudgetUp$ is not bijective to any listed sibling because it jointly carries residual critical-pair source rows, finite budget rows, candidate joins, closed checks, obstruction retention, Nontrivial and FieldFaithful TasteGate readiness, transport, replay, provenance, and local naming.}

\begin{closurestatus}{\MetaCICResidualCriticalPairBudgetUp}
  \constructivestory{A $\MetaCICResidualCriticalPairBudgetUp$ packet is generated from residual critical-pair source rows, finite budget rows, candidate local joins, closed checks, obstruction rows, Nontrivial and FieldFaithful TasteGate readiness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-critical-pair-budget-carrier}, \autoref{thm:metacic-residual-critical-pair-budget-namecert-obligations}, and \autoref{thm:metacic-residual-critical-pair-budget-boundary} seed the finite residual critical-pair budget route for the MetaCIC confluence audit map.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global confluence, unrestricted residual-substitution compatibility, parallel diamond, subject reduction, quotient syntax equality, Lean verification, or bridge checking for $\MetaCICResidualCriticalPairBudgetUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, residual-source admission, finite-budget admission, candidate-join admission, closed-check admission, obstruction retention, Nontrivial and FieldFaithful TasteGate instances, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICResidualCriticalPairBudgetUp.}
\end{closurestatus}
