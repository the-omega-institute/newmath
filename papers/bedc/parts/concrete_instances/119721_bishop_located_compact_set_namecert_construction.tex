\chapter{A Concrete Naming Certificate for $\BishopLocatedCompactSetUp$}
\label{ch:concrete-instances-bishop-located-compact-set-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopLocatedCompactSetUp$ packet records located compact subsets through finite-net locator data rather than through open-cover compactness. It sits over the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the total-bounded finite-net route of \autoref{ch:concrete-instances-totallybounded-namecert}, the finite epsilon-net surface of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the metric readback of \autoref{ch:concrete-instances-metric-namecert}, the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, and the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}. The packet names a located compact-set certificate for downstream finite-net and compact-continuity arguments; it does not assert open-cover compactness, countable choice, quotient equality, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop located compact set carrier]
\label{def:bishop-located-compact-set-carrier}
A $\BishopLocatedCompactSetUp$ carrier is a finite $\BHist$ packet
$$
K=(K_0,T,E,M,R,S,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K_0$ is the $\CompactMetricUp$ source row, naming the compact-metric interface inspected by the packet.
\item $T$ is the $\TotallyBoundedUp$ finite-net data row for displayed tolerances, centers, coverage witnesses, and refinement comparisons.
\item $E$ is the $\FiniteEpsilonNetUp$ row recording the finite epsilon-net center list and coverage ledger at each inspected scale.
\item $M$ is the $\MetricUp$ distance-readback row used by locator requests.
\item $R$ is the $\RegSeqRatUp$ handoff row for rational distance and tolerance reads.
\item $S$ is the $\StreamNameUp$ finite-window row through which inspected compact-set names and candidate centers are read.
\item $L$ is the locatedness locator row, recording finite lower and upper distance windows for the compact subset at each query and displayed tolerance.
\item $H$ records componentwise $\hsame$ transport for the compact metric source, total-bounded data, epsilon-net row, metric readback, regular rational handoff, stream windows, locator row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a located compact-set request through $K_0,T,E,M,R,S,L,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedCompactSetUp$, $\CompactMetricUp$, $\TotallyBoundedUp$, $\FiniteEpsilonNetUp$, $\MetricUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedCompactSetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K_0,T,E,M,R,S,L,H,C,P,N$. It has no coordinate for open-cover compactness, countable-choice selection of nets, quotient equality of names, host subset equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop located compact set NameCert obligations]
\label{thm:bishop-located-compact-set-namecert-obligations}
For every accepted $\BishopLocatedCompactSetUp$ carrier
$$
K=(K_0,T,E,M,R,S,L,H,C,P,N),
$$
the local $\NameCert_{\BishopLocatedCompactSetUp}$ surface consists exactly of compact-metric source admission, total-bounded finite-net data, finite epsilon-net row exposure, metric distance readback, regular rational handoff, finite stream-window exposure, locatedness locator admission, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-compact-set-carrier}. The analytic rows are exactly $K_0,T,E,M,R,S,L$.

The compact metric source is read before finite-net data. The total-bounded row and finite epsilon-net row display centers, tolerances, and coverage ledgers, while $M,R,S,L$ provide distance readback, rational handoff, stream windows, and locatedness locator data.

The rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for open covers, countable-choice net selection, quotient equality, host subset equality, or ambient completeness, these rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop located compact set finite-net locator]
\label{thm:bishop-located-compact-set-finite-net-locator}
Every locatedness read exported by an accepted $\BishopLocatedCompactSetUp$ packet factors through the compact-metric row $K_0$, the total-bounded finite-net row $T$, the $\FiniteEpsilonNetUp$ row $E$, the metric readback row $M$, the $\RegSeqRatUp$ row $R$, the $\StreamNameUp$ row $S$, and the locator row $L$ before it reaches transport, replay, provenance, and local naming. The locator is therefore a finite-net route, not an open-cover compactness theorem or a selected ambient subset.
\end{theorem}
\begin{proof}
Start with the NameCert obligations. They require the compact-metric source and both finite-net rows before any locator read can be certified.

Read the metric-facing side next. Distance requests pass through $M$, rational tolerance and comparison data pass through $R$, and inspected names or center windows pass through $S$ before the locatedness locator row $L$ is available.

Finally transport and replay the same route through $H,C,P,N$. No coordinate remains for open-cover compactness, countable-choice selection, quotient equality, host subset equality, or ambient $\RealUp$ completeness, so the finite-net locator is the complete exported surface.
\end{proof}

\falsifiablePrediction{Any accepted $\BishopLocatedCompactSetUp$ read that reaches locatedness data without displaying the compact metric source, total-bounded finite-net row, finite epsilon-net row, metric readback, RegSeqRat handoff, StreamName windows, hsame transport, Cont replay, Pkg provenance, and local NameCert row refutes the carrier.}

\independenceWitness{compactmetric, totallybounded, finite-epsilon-net, metric, regseqrat, streamname: none is bijective with $\BishopLocatedCompactSetUp$ because this packet jointly binds compact-metric source data, total-bounded finite-net ledgers, finite epsilon-net coverage, metric distance readback, rational handoff, stream windows, locatedness locator data, transport, replay, provenance, and local naming.}

\closureat{BishopLocatedCompactSetUp}{seedStr}

\begin{closurestatus}{\BishopLocatedCompactSetUp}
  \constructivestory{A $\BishopLocatedCompactSetUp$ packet is generated from CompactMetric source data, TotallyBounded finite-net rows, FiniteEpsilonNet coverage rows, Metric distance readback, RegSeqRat handoff, StreamName windows, a locatedness locator, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-compact-set-carrier}, \autoref{thm:bishop-located-compact-set-namecert-obligations}, and \autoref{thm:bishop-located-compact-set-finite-net-locator} seed the located compact-set route over compact metric, finite-net, distance, rational-readback, stream-window, locator, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, countable choice, quotient equality, host subset equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net locator exactness, metric readback, RegSeqRat handoff, StreamName window exposure, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedCompactSetUp.}
\end{closurestatus}
