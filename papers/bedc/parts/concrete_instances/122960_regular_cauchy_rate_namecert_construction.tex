\chapter{A Concrete Naming Certificate for $\RegularCauchyRateUp$}
\label{ch:concrete-instances-regular-cauchy-rate-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyRateUp$ packet records an explicit rate ledger for regular Cauchy rational names before a $\RealUp$ or Completion consumer reads the terminal seal. It sits over the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the generic Cauchy-rate row of \autoref{ch:concrete-instances-cauchyrate-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the rate ledger itself: it is not quotient stream equality, a selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy rate carrier]
\label{def:regular-cauchy-rate-carrier}
A $\RegularCauchyRateUp$ carrier is a finite $\BHist$ packet
$$
K=(S,D,R,A,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row recording which rational-name observations are inspected.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to state every requested precision.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row attached to the same finite windows.
\item $A$ is the $\CauchyRateUp$ ledger row assigning displayed precision requests to displayed index bounds.
\item $E$ is the terminal $\RealUp$ seal row reached only after $S,D,R,A$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the window, tolerance, readback, rate ledger, seal, replay, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from finite windows and dyadic requests through the regular-rational readback and rate ledger into the terminal seal.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyRateUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\CauchyRateUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyRateUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,D,R,A,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected host limit, countable choice, trichotomy, host real equality, or ambient completeness.
\end{definition}

\begin{theorem}[Regular Cauchy rate NameCert obligations]
\label{thm:regular-cauchy-rate-namecert-obligations}
For every accepted $\RegularCauchyRateUp$ carrier
$$
K=(S,D,R,A,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyRateUp}$ surface consists exactly of carrier admission for the displayed rate ledger; finite-window exposure through $S$; dyadic tolerance exposure through $D$; regular rational readback through $R$; Cauchy-rate ledger exposure through $A$; terminal real sealing through $E$; and structural transport, replay, provenance, and local naming through $H,C,P,N$.

Every $\RegSeqRatUp$, $\RealUp$, or Completion consumer that reads the explicit regular Cauchy rate must first display the same finite window $S$, tolerance ledger $D$, regular rational readback $R$, and rate ledger $A$. No local obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-rate-carrier}. The analytic rows are exactly the finite window $S$, dyadic tolerance ledger $D$, regular rational readback $R$, and rate ledger $A$.

The terminal seal row $E$ is downstream of those four rows. A Real-facing read may therefore reach $E$ only after the rate request has been stated in $D$, read through $S$ and $R$, and answered by the displayed ledger $A$.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since no carrier coordinate stores quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy rate Real handoff]
\label{thm:regular-cauchy-rate-real-handoff}
For every accepted $\RegularCauchyRateUp$ carrier $K=(S,D,R,A,E,H,C,P,N)$, any $\RealUp$ or Completion-facing read of the terminal seal factors through the finite route
$$
S,D,R \longmapsto A \longmapsto E \longmapsto H,C,P,N .
$$
The handoff exports an explicit Bishop rate ledger for the displayed regular rational name, not a quotient of streams, not a selected representative, not a host real equality proof, and not an ambient completeness principle.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:regular-cauchy-rate-namecert-obligations}. It forces every rate read to name the finite window, tolerance ledger, regular-rational readback, and rate ledger before the terminal real seal is consumed.

The continuation row $C$ records the displayed route from $S,D,R$ through $A$ to $E$. Componentwise transport $H$, provenance $P$, and local naming $N$ preserve and name that same route rather than adding a second route to the seal.

The refusal clause follows from the carrier inventory. No row stores quotient stream equality, selected representatives, host real equality, countable choice, trichotomy, or ambient completeness, so the Real handoff is exactly the finite rate-ledger route.
\end{proof}

\begin{theorem}[Regular Cauchy rate scope closure]
\label{thm:regular-cauchy-rate-scope-closure}
The carrier of \autoref{def:regular-cauchy-rate-carrier}, the obligation theorem \autoref{thm:regular-cauchy-rate-namecert-obligations}, and the Real handoff theorem \autoref{thm:regular-cauchy-rate-real-handoff} jointly bind every $\RegularCauchyRateUp$ read to $\StreamNameUp$ finite windows, $\DyadicRatCoreUp$ tolerance ledgers, $\RegSeqRatUp$ readback, $\CauchyRateUp$ ledger rows, $\RealUp$ seals, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

No quotient stream equality, selected limit, countable choice, trichotomy, host real equality, or ambient completeness datum is available from that scope.
\end{theorem}
\begin{proof}
Project the carrier inventory in \autoref{def:regular-cauchy-rate-carrier}. Its analytic rows are the finite window $S$, tolerance ledger $D$, regular-rational readback $R$, Cauchy-rate ledger $A$, and Real seal $E$, while $H,C,P,N$ supply transport, replay, provenance, and local naming inside $\BHist$.

Apply \autoref{thm:regular-cauchy-rate-namecert-obligations}. It exposes exactly those rows as the local NameCert surface and forces every rate read through the displayed finite window, tolerance, readback, and ledger rows.

Finally apply \autoref{thm:regular-cauchy-rate-real-handoff}. The handoff factors every Real or Completion-facing read through $S,D,R,A,E,H,C,P,N$, so the visible scope is exhausted by the listed rows. The excluded objects are absent from the carrier and are not introduced by $\hsame$, $\Cont$, $\Pkg$, or $\NameCert$ routing.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyRateUp$ read is refuted if a $\RealUp$ or Completion-facing consumer reaches the terminal seal without first displaying the $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback row, and $\CauchyRateUp$ ledger row.}

\independenceWitness{streamname, dyadicratcore, regseqrat, cauchyrate, real: $\RegularCauchyRateUp$ is not bijective to any listed sibling because it jointly carries one finite StreamName window, one DyadicRatCore tolerance ledger, one RegSeqRat readback row, one explicit CauchyRate ledger, one Real seal, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyRateUp}{seedStr}

\begin{closurestatus}{\RegularCauchyRateUp}
  \constructivestory{A $\RegularCauchyRateUp$ packet is generated from finite StreamName windows, a DyadicRatCore tolerance ledger, RegSeqRat readback, a displayed CauchyRate ledger, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-rate-carrier}, \autoref{thm:regular-cauchy-rate-namecert-obligations}, and \autoref{thm:regular-cauchy-rate-real-handoff} seed the finite regular Cauchy rate ledger consumed by regular-rational, Real, and Completion-facing readers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, quotient stream equality, selected representatives, selected limits, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exposure, dyadic tolerance exposure, regular-rational readback, Cauchy-rate ledger exactness, Real-seal routing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyRateUp.}
\end{closurestatus}
