\chapter{A Concrete Naming Certificate for $\CauchyDedekindBridgeUp$}
\label{ch:concrete-instances-cauchy-dedekind-bridge-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyDedekindBridgeUp$ packet records the finite bridge surface by which regular Cauchy names can be compared against located-cut-facing data before any completion principle for $\RealUp$ is available. It sits between the regular rational name route of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the located-cut comparison boundary of \autoref{ch:concrete-instances-locatedcut-namecert}, the Bishop real comparison route of \autoref{ch:concrete-instances-bishoprealcomparison-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter gives Phase Real and Completion consumers one named handoff between Cauchy-style and cut-facing reads without asserting quotient real equality or ambient completeness.

\begin{definition}[Cauchy-Dedekind bridge carrier]\label{def:cauchy-dedekind-bridge-carrier}
A $\CauchyDedekindBridgeUp$ carrier is a finite $\BHist$ packet
$$
K=(R,W,D,L,B,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row, carrying the regular rational Cauchy name before any located-cut-facing comparison is read.
\item $W$ is the $\StreamNameUp$ window schedule used to expose only the finite source and comparison observations requested by the packet.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the endpoint scales, error budgets, and refinement requests consumed by the bridge.
\item $L$ is the $\LocatedCutUp$ comparison row. It records lower-side and upper-side located evidence only through the displayed dyadic and stream windows.
\item $B$ is the $\BishopRealComparisonUp$ classification row, comparing the regular Cauchy source with the located-cut-facing row through finite rational brackets.
\item $E$ is the terminal $\RealUp$ seal reached only after $R,W,D,L,B$ have been consumed.
\item $H$ records componentwise $\hsame$ transport for the regular source, stream windows, dyadic tolerances, located comparison, Bishop comparison, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the bridge route
$$
R,W,D \longmapsto L,B \longmapsto E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CauchyDedekindBridgeUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\LocatedCutUp$, $\BishopRealComparisonUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CauchyDedekindBridgeUp}$ row.
\end{enumerate}
The classifier for accepted packets reads only these rows. It has no coordinate for quotient real equality, a Dedekind-cut construction as primitive, countable choice, trichotomy, selected representatives, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy-Dedekind bridge NameCert obligations]\label{thm:cauchy-dedekind-bridge-namecert-obligations}
For every accepted $\CauchyDedekindBridgeUp$ carrier $K=(R,W,D,L,B,E,H,C,P,N)$, the local $\NameCert_{\CauchyDedekindBridgeUp}$ surface consists exactly of carrier admission, regular-source exposure, finite-window exposure, dyadic-tolerance exposure, located-cut comparison exposure, Bishop comparison classification, real-seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, local naming, and non-escape of quotient real equality, Dedekind-cut construction as primitive, countable choice, trichotomy, selected representatives, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-dedekind-bridge-carrier}. The accepted packet exposes exactly the regular rational source row $R$, window schedule $W$, dyadic tolerance row $D$, located-cut comparison row $L$, Bishop comparison row $B$, terminal real seal $E$, and structural rows $H,C,P,N$.

The comparison rows are read in order. A consumer first reads $R,W,D$ as regular Cauchy evidence at finite precision, then reads $L$ as located-cut-facing evidence and $B$ as the finite Bishop comparison classification. None of those rows contains a host quotient of real names or a selected cut representative.

The handoff to $\RealUp$ is therefore delayed until the displayed comparison route has been consumed. Rows $H,C,P,N$ transport, replay, source, and name the same finite route, so the excluded principles have no carrier coordinate from which they could be recovered.
\end{proof}

\begin{theorem}[Cauchy-Dedekind bridge real-comparison handoff]\label{thm:cauchy-dedekind-bridge-real-comparison-handoff}
Every $\RealUp$-facing or $\BishopRealComparisonUp$-facing consumer of an accepted $\CauchyDedekindBridgeUp$ packet factors through the displayed route
$$
\RegSeqRatUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \LocatedCutUp,\ \BishopRealComparisonUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
In particular, a consumer comparing a regular Cauchy name with located-cut-facing data must read the finite window schedule and dyadic tolerance ledger before the comparison row and before the terminal real seal.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $K=(R,W,D,L,B,E,H,C,P,N)$. The source row $R$ is visible only through the window schedule $W$ and tolerance ledger $D$, so the consumer has no unscheduled access to a regular Cauchy stream.

The located-cut-facing read is then supplied by $L$ and classified by $B$. These two rows compare only the displayed rational brackets and located evidence made available by $R,W,D$.

Finally the real seal $E$ is reached through the same route and preserved by $H,C,P,N$. Thus the handoff is a finite bridge from regular Cauchy data to located comparison data, not a quotient real equality theorem, a primitive Dedekind-cut construction, a countable-choice selector, trichotomy, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyDedekindBridgeUp$ consumer comparing a regular Cauchy name with located-cut-facing data factors through $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\LocatedCutUp$, $\BishopRealComparisonUp$, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{regseqrat, streamname, dyadicratcore, locatedcut, bishoprealcomparison, real: $\CauchyDedekindBridgeUp$ is not bijective to any listed sibling because it jointly carries a regular Cauchy source, finite windows, dyadic tolerances, located-cut comparison, Bishop comparison classification, real seal, transport, replay, provenance, and local NameCert rows in one bridge packet.}

\closureat{CauchyDedekindBridgeUp}{seedStr}

\begin{closurestatus}{\CauchyDedekindBridgeUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-dedekind-bridge-carrier}, \autoref{thm:cauchy-dedekind-bridge-namecert-obligations}, and \autoref{thm:cauchy-dedekind-bridge-real-comparison-handoff} seed the finite bridge surface between regular Cauchy names and located-cut-facing comparison rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, Dedekind-cut construction as primitive, countable choice, trichotomy, selected representatives, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, regular-source exposure, finite-window exposure, dyadic-tolerance exactness, located-cut comparison exposure, Bishop comparison classification, real-seal non-escape, transport and replay stability, provenance non-escape, and TasteGate field faithfulness for BEDC.Derived.CauchyDedekindBridgeUp.}
  \constructivestory{A $\CauchyDedekindBridgeUp$ packet is generated from a RegSeqRat source, StreamName finite windows, DyadicRatCore tolerances, LocatedCut comparison evidence, BishopRealComparison classification, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
