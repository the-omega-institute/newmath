\chapter{A Concrete Naming Certificate for $\HenstockKurzweilUp$}
\label{ch:concrete-instances-henstock-kurzweil-namecert}
\origin{human}

Following Henstock and Kurzweil, the $\HenstockKurzweilUp$ packet records a gauge-integral seed route in which tagged Riemann windows are controlled by a displayed gauge before any real-valued integral read is exported. It sits beside the gauge-integral packet of \autoref{ch:concrete-instances-gauge-integral-namecert}, the Riemann-integral packet of \autoref{ch:concrete-instances-riemannintegral-namecert}, the Riemann-sum gauge packet of \autoref{ch:concrete-instances-riemann-sum-gauge-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the dyadic endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter names the finite gauge-window carrier used by this integration route; it does not claim Lebesgue measure, open-cover compactness, countable choice, selected infinite sums, or ambient $\RealUp$ completeness.

\begin{definition}[HenstockKurzweil carrier]
\label{def:henstock-kurzweil-carrier}
A $\HenstockKurzweilUp$ carrier is a finite $\BHist$ packet
$$
K=(G,I,S,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the $\GaugeIntegralUp$ source row naming the accepted gauge-integral boundary.
\item $I$ is the $\RiemannIntegralUp$ comparison row that keeps the integral read tied to tagged mesh data.
\item $S$ is the $\RiemannSumGaugeUp$ row carrying the finite tagged-sum window and gauge compatibility test.
\item $W$ is the displayed gauge-window row, recording the tag, cell, and local radius data used by $S$.
\item $D$ is the $\DyadicRatCoreUp$ endpoint and tolerance ledger for the cells in the displayed window.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the finite window and dyadic tolerance data.
\item $E$ is the terminal $\RealUp$ seal row reached only after $G,I,S,W,D,R$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for the gauge, integral, sum, window, dyadic, readback, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a Henstock--Kurzweil request through $G,I,S,W,D,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HenstockKurzweilUp$, $\GaugeIntegralUp$, $\RiemannIntegralUp$, $\RiemannSumGaugeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HenstockKurzweilUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $G,I,S,W,D,R,E,H,C,P,N$. There is no coordinate for a measure space, an open-cover compactness theorem, a chosen refinement family, a selected infinite sum, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.HenstockKurzweilUp.HenstockKurzweilTasteGate\_single\_carrier\_alignment}

\begin{theorem}[HenstockKurzweil NameCert obligations]
\label{thm:henstock-kurzweil-namecert-obligations}
For every accepted $\HenstockKurzweilUp$ carrier $K=(G,I,S,W,D,R,E,H,C,P,N)$, the local $\NameCert_{\HenstockKurzweilUp}$ surface consists exactly of carrier admission, GaugeIntegral exposure through $G$, RiemannIntegral comparison through $I$, RiemannSumGauge window exposure through $S$, gauge-window exactness through $W$, DyadicRatCore tolerance exposure through $D$, RegSeqRat readback through $R$, Real seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

No obligation may be discharged by Lebesgue measure, open-cover compactness, countable choice, selected infinite sums, host integrand equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:henstock-kurzweil-carrier}. The displayed rows are exactly $G,I,S,W,D,R,E,H,C,P,N$, so carrier admission exposes only gauge, integral-comparison, tagged-sum, window, dyadic, readback, seal, transport, replay, provenance, and naming data.

The integration side starts with $G,I,S,W$. The gauge-integral source row $G$ and Riemann-integral comparison row $I$ are read only through the finite tagged-sum gauge row $S$ and its displayed window $W$, so the packet has one gauge-window route rather than a detached refinement principle.

Next read the numerical handoff. The dyadic ledger $D$ supplies endpoint and tolerance data for the same window, $R$ records the regular rational readback, and $E$ is available only after those rows have been replayed.

Finally the structural rows $H,C,P,N$ preserve the same route. Since measure data, open-cover compactness, selected infinite sums, countable choice, host integrand equality, and ambient completeness are absent from the carrier, the listed rows exhaust the local surface.
\end{proof}

\begin{theorem}[HenstockKurzweil gauge-window route]
\label{thm:henstock-kurzweil-gauge-window-route}
Every completion-facing or Real-facing read of an accepted $\HenstockKurzweilUp$ carrier factors through the finite route
$$
\GaugeIntegralUp \longmapsto \RiemannSumGaugeUp \longmapsto \RiemannIntegralUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The gauge-window row $W$ is read before the dyadic tolerance ledger and the real seal, and the route cannot bypass $G,I,S,W,D,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Begin with an accepted carrier read. The carrier exposes $G,S,W$ before any numerical endpoint or real seal can be inspected, so the request has already passed through a displayed gauge-window row.

Replay the continuation row $C$. The read moves from the gauge source $G$ through the tagged-sum gauge row $S$ and the Riemann comparison row $I$, then through the dyadic ledger $D$ and regular rational readback $R$ before it reaches the seal $E$.

The structural rows $H,P,N$ do not add another integration object. They preserve the same finite route, and no excluded measure, compactness, choice, selected-sum, or completeness coordinate is available in the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\HenstockKurzweilUp$ read factors through $\GaugeIntegralUp$, $\RiemannIntegralUp$, $\RiemannSumGaugeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\closureat{HenstockKurzweilUp}{seedStr}

\begin{closurestatus}{\HenstockKurzweilUp}
  \constructivestory{A $\HenstockKurzweilUp$ packet is generated from a GaugeIntegral row, RiemannIntegral comparison row, RiemannSumGauge tagged-window row, gauge-window ledger, DyadicRatCore endpoint and tolerance rows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:henstock-kurzweil-carrier}, \autoref{thm:henstock-kurzweil-namecert-obligations}, and \autoref{thm:henstock-kurzweil-gauge-window-route} seed the finite gauge-window route over GaugeIntegralUp, RiemannIntegralUp, RiemannSumGaugeUp, DyadicRatCoreUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, Lebesgue measure, open-cover compactness, countable choice, selected infinite sums, host integrand equality, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, gauge-window exactness, RiemannSumGauge exposure, RiemannIntegral comparison, DyadicRatCore tolerance exactness, RegSeqRat readback, Real seal nonescape, and TasteGate field faithfulness for BEDC.Derived.HenstockKurzweilUp.}
\end{closurestatus}
