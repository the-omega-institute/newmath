The $\SpectrahedronUp$ route consumes the matrix carrier of \autoref{ch:concrete-instances-matrix-namecert}, semidefinite cone membership from \autoref{ch:concrete-instances-semidefinitecone-namecert}, convex-set provenance from \autoref{ch:concrete-instances-convexset-namecert}, the linear-map affine pencil of \autoref{ch:concrete-instances-linearmap-namecert}, and vector-space parameter rows from \autoref{ch:concrete-instances-vecspace-namecert}; those sibling surfaces are the only cross-chapter data exposed by the hub before the local spectrahedron NameCert is read.
