The $\SpectrahedronUp$ dossier lattice depends on the concrete sibling route through \autoref{ch:concrete-instances-semidefinitecone-namecert} and \autoref{ch:concrete-instances-convexset-namecert}. A spectrahedron reads a finite affine slice of the semidefinite cone, so its carrier consumes the cone classifier and the convex-set intersection rows before the local NameCert is public.
