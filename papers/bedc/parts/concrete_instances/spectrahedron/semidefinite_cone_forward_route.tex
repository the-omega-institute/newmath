Every $\SpectrahedronUp$ carrier reads its affine-linear matrix inequality through the displayed $\SemidefiniteConeUp$ membership boundary of \autoref{ch:concrete-instances-semidefinitecone-namecert} and the $\ConvexSetUp$ provenance boundary of \autoref{ch:concrete-instances-convexset-namecert} before exposing the local NameCert. Thus the affine pencil remains a finite forward route from parameter rows to cone membership and convex feasible-surface rows, not a host-level closed semialgebraic set.
