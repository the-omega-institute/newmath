\chapter{A Concrete Naming Certificate for $\LocatedDedekindRealComparisonUp$}
\label{ch:concrete-instances-located-dedekind-real-comparison-namecert}
\origin{human}

Following Bishop and Bridges on constructive real numbers and Troelstra--van Dalen on located cuts, the $\LocatedDedekindRealComparisonUp$ packet records the finite comparison surface between located lower/upper Dedekind data and the regular-Cauchy real-name route. It sits beside the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a traditional representation comparison as finite BEDC data; it is not a quotient of cuts, a host-real equality test, countable choice, trichotomy for arbitrary rationals, or ambient $\RealUp$ completeness.

\begin{definition}[Located Dedekind real comparison carrier]
\label{def:located-dedekind-real-comparison-carrier}
A $\LocatedDedekindRealComparisonUp$ carrier is a finite $\BHist$ packet
$$
L=(D,Q,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the located lower/upper Dedekind row, carrying lower membership windows, upper membership windows, inhabited-side evidence, rounded-side evidence, and the finite locatedness comparison requested by the consumer.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance-window ledger used to compare the displayed lower and upper cuts at the requested dyadic precision.
\item $W$ is the $\StreamNameUp$ finite-observation row exposing only the cut observations and dyadic windows inspected by $D$ and $Q$.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same located cut data, finite observations, and dyadic tolerance windows.
\item $E$ is the terminal $\RealUp$ seal row reached only after the located cut row, dyadic tolerance ledger, finite observations, and regular readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the located cut row, dyadic ledger, stream observations, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each located comparison request through $D,Q,W$ to $R$ and then to $E$.
\item $P$ is the $\Pkg$ provenance over $\LocatedDedekindRealComparisonUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedDedekindRealComparisonUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,Q,W,R,E,H,C,P,N$. It has no coordinate for quotient cut equality, selected representatives, host real comparison, countable choice, arbitrary-rational trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located Dedekind real comparison NameCert obligations]
\label{thm:located-dedekind-real-comparison-namecert-obligations}
For every accepted $\LocatedDedekindRealComparisonUp$ carrier $L=(D,Q,W,R,E,H,C,P,N)$, the local $\NameCert_{\LocatedDedekindRealComparisonUp}$ surface consists exactly of located lower/upper cut admission through $D$; dyadic tolerance-window accounting through $Q$; finite observation exposure through $W$; regular-Cauchy readback through $R$; terminal Real sealing through $E$; and componentwise transport, continuation replay, package provenance, and local naming through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-dedekind-real-comparison-carrier}. The tuple displays the located cut data, dyadic tolerance windows, finite stream observations, regular readback, Real seal, and structural rows.

The located comparison is finite. The row $D$ provides the lower and upper cut windows actually inspected, while $Q$ fixes the dyadic tolerance window and $W$ records the finite observations available to the request.

The row $R$ repacks those same displayed observations as regular rational data, and the seal row $E$ is downstream from that readback. Transport through $H$ and replay through $C$ preserve the same route, while $P,N$ only source and name it.
\end{proof}

\begin{theorem}[Located Dedekind real comparison regular-Cauchy handoff]
\label{thm:located-dedekind-real-comparison-regular-cauchy-handoff}
Every $\RegSeqRatUp$ or Completion consumer of an accepted $\LocatedDedekindRealComparisonUp$ carrier must read the located cut row $D$, the $\DyadicRatCoreUp$ tolerance ledger $Q$, and the $\StreamNameUp$ finite-observation row $W$ before consuming the regular readback row $R$. Thus the comparison from located Dedekind data to regular-Cauchy data is a displayed finite handoff, not a quotient of cuts, selected representative, host comparison, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with an accepted carrier and project the rows $D,Q,W,R$. The only regular-Cauchy coordinate is $R$, and the carrier places it after the located cut data, dyadic tolerance ledger, and finite observation row.

Apply \autoref{thm:located-dedekind-real-comparison-namecert-obligations}. The obligation surface requires the readback through $R$ to use the same rows $D,Q,W$ that admitted the located comparison.

The structural rows $H,C,P,N$ do not introduce a second route. They transport, replay, source, and name the same handoff, so a regular-Cauchy or Completion consumer cannot bypass the displayed finite comparison.
\end{proof}

\begin{theorem}[Located Dedekind real comparison Real-seal nonescape]
\label{thm:located-dedekind-real-comparison-real-seal-nonescape}
Every $\RealUp$ consumer of an accepted $\LocatedDedekindRealComparisonUp$ carrier reaches the terminal seal $E$ only through the route
$$
D \longmapsto Q \longmapsto W \longmapsto R \longmapsto E.
$$
The exported surface contains no quotient cut equality, selected host real, host comparison procedure, countable-choice search, trichotomy split for arbitrary rationals, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Inspect the carrier tuple. The terminal Real row is $E$, and it appears only after the located cut row, dyadic tolerance ledger, finite observation row, and regular readback row.

Use \autoref{thm:located-dedekind-real-comparison-regular-cauchy-handoff}. Any Real-facing consumer first receives the regular-Cauchy handoff from the displayed finite located comparison route.

Finally apply the nonescape part of \autoref{thm:located-dedekind-real-comparison-namecert-obligations}. Since the excluded objects have no carrier coordinate and no replay route in $C$, the terminal Real seal cannot export them.
\end{proof}

\falsifiablePrediction{An accepted $\LocatedDedekindRealComparisonUp$ consumer that reaches a $\RealUp$ seal without reading located lower/upper Dedekind data, dyadic tolerance windows, finite stream observations, and $\RegSeqRatUp$ readback refutes the carrier surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real: $\LocatedDedekindRealComparisonUp$ is not bijective to any listed sibling because it binds located lower/upper cut observations, dyadic tolerance windows, finite observation exposure, regular-Cauchy readback, and Real sealing into one local NameCert surface.}

\closureat{LocatedDedekindRealComparisonUp}{seedStr}

\begin{closurestatus}{\LocatedDedekindRealComparisonUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-dedekind-real-comparison-carrier}, \autoref{thm:located-dedekind-real-comparison-namecert-obligations}, \autoref{thm:located-dedekind-real-comparison-regular-cauchy-handoff}, and \autoref{thm:located-dedekind-real-comparison-real-seal-nonescape} seed the finite comparison route from located lower/upper Dedekind rows to regular-Cauchy readback and Real seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient cut equality, selected host reals, host comparison procedures, countable choice, arbitrary-rational trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located cut admission, dyadic tolerance accounting, finite observation exposure, regular readback, Real-seal ordering, transport, replay, provenance, and local naming for BEDC.Derived.LocatedDedekindRealComparisonUp.}
  \constructivestory{A $\LocatedDedekindRealComparisonUp$ packet is generated from located lower and upper Dedekind rows, dyadic tolerance windows, finite StreamName observations, RegSeqRat readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
