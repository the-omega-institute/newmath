\chapter{A Concrete Naming Certificate for $\CrossHistCausalRateUp$}
\label{ch:concrete-instances-crosshistcausalrate-namecert}
\origin{ai}

The $\CrossHistCausalRateUp$ packet realizes the maximum cross-Hist causal-rate direction as a finite NameCert carrier. It sits after the two-history causal witness surface of \autoref{ch:concrete-instances-crosshistcausal-namecert} and the unary bound surface of \autoref{ch:concrete-instances-maxcausalrate-namecert}, while exposing the observer-symmetric invariant reading used by \autoref{ch:concrete-instances-interhistinvariant-namecert}. The packet also consumes observer-Hist identity from \autoref{ch:concrete-instances-observerhistidentity-namecert} and finite route replay from \autoref{ch:concrete-instances-continuationtrace-namecert}: those siblings provide the observer rows and displayed continuation routes, while this chapter binds them to a maximum-rate read with no global frame.

\begin{definition}[Cross-Hist causal-rate carrier]
\label{def:cross-hist-causal-rate-carrier}
A $\CrossHistCausalRateUp$ carrier is a finite $\BHist$ packet
$$
R=(A,B,D,M,S,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two observer-history slots, each read through the observer-Hist identity surface;
\item $D$ is the displayed causal-dependence route family, whose entries are finite $\CrossHistCausalUp$ rows over the ordered pair $A,B$ and selected unary slots;
\item $M$ is the maximum-rate row, read as a $\MaxCausalRateUp$ unary-bound packet over exactly the dependence routes in $D$;
\item $S$ is the observer-symmetry row, recording that swapping the two Hist slots preserves the same maximum-rate read after displayed transport;
\item $G$ is the no-global-frame ledger, recording absence of a global synchronization row, shared clock row, metric tensor row, or privileged observer-frame row;
\item $H$ records componentwise $\hsame$ transport for the two Hist slots, dependence routes, maximum-rate row, observer-symmetry row, no-global-frame ledger, continuation routes, provenance row, and local naming row;
\item $C$ records displayed $\Cont$ routes by which a consumer reads the two histories, replays each causal-dependence route, reads the maximum-rate row, checks observer symmetry, and checks the no-global-frame ledger;
\item $P$ is the $\Pkg$ provenance over $\CrossHistCausalRateUp$, $\ObserverHistIdentityUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, $\InterHistInvariantUp$, $\ContinuationTraceUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CrossHistCausalRateUp}$ row.
\end{enumerate}
The carrier contains no coordinate for a global observer frame, synchronized clock, metric tensor, host real-valued speed, unlisted causal witness, or maximum-rate read detached from the displayed dependence routes.
\end{definition}

\begin{theorem}[Cross-Hist causal-rate NameCert obligations]
\label{thm:cross-hist-causal-rate-namecert-obligations}
For the carrier of \autoref{def:cross-hist-causal-rate-carrier}, the local $\NameCert_{\CrossHistCausalRateUp}$ surface consists exactly of carrier admission for
$$
R=(A,B,D,M,S,G,H,C,P,N),
$$
two observer-history slots, finite causal-dependence route replay, maximum-rate row exactness, observer-symmetry preservation, no-global-frame refusal, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and the local naming row.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cross-hist-causal-rate-carrier}. The rows $A,B,D,M,S,G,H,C,P,N$ expose every public coordinate.

The observer slots are read through $A$ and $B$, with observer-Hist identity supplied by the sibling surface. The dependence routes in $D$ are finite $\CrossHistCausalUp$ rows, and the continuation routes in $C$ replay those rows without adding a hidden causal witness.

The maximum-rate read is the row $M$. It is attached to exactly the displayed dependence route family $D$, so the rate surface cannot detach from the two histories or from the selected unary slots.

The observer-symmetry row $S$ and the no-global-frame ledger $G$ provide the two public gates for a maximum-rate read. The remaining rows $H,C,P,N$ transport, replay, package, and name the same packet, so the NameCert obligations are exhausted by the displayed carrier.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate carrier admission row]
\label{thm:cross-hist-causal-rate-obligation-carrier-admission}
Every accepted $\CrossHistCausalRateUp$ packet exposes exactly the finite carrier admission row
$$
A,B,D,M,S,G,H,C,P,N.
$$
The rows are respectively the two observer-history slots, the causal-dependence route family, the maximum-rate row, the observer-symmetry row, the no-global-frame ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and the local $\NameCert_{\CrossHistCausalRateUp}$ row. No root consumer may add a global frame, synchronized clock, metric tensor, host speed coordinate, hidden causal witness, or detached rate row to this admission surface.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:cross-hist-causal-rate-carrier}. Its tuple is exactly $R=(A,B,D,M,S,G,H,C,P,N)$, and the definition lists no further public coordinate.

Use the NameCert obligations of \autoref{thm:cross-hist-causal-rate-namecert-obligations}. Carrier admission for the same tuple is the first obligation, while the remaining obligations classify every displayed row as observer slots, route replay, maximum-row exactness, symmetry, frame refusal, transport, continuation, provenance, or naming.

The exclusion clause is then an inventory argument. Since the accepted packet contains no coordinate for a global frame, synchronized clock, metric tensor, host speed, hidden witness, or detached rate read, no root admission row can expose one.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate route replay row]
\label{thm:cross-hist-causal-rate-obligation-route-replay}
Every public rate read from an accepted $\CrossHistCausalRateUp$ packet replays the finite route family $D$ through the displayed continuation row $C$ before it reaches the maximum-rate row $M$. The route reads the $\CrossHistCausalUp$ sibling surface of \autoref{ch:concrete-instances-crosshistcausal-namecert} and then the $\MaxCausalRateUp$ sibling surface of \autoref{ch:concrete-instances-maxcausalrate-namecert}; it cannot read $M$ as a host-side rate detached from $D$.
\end{theorem}
\begin{proof}
Start with \autoref{def:cross-hist-causal-rate-carrier}. The dependence coordinate is $D$, the continuation coordinate is $C$, and the maximum-rate coordinate is $M$.

Apply \autoref{thm:cross-hist-causal-rate-namecert-obligations}. Its causal-dependence route replay and maximum-rate row exactness clauses force the read of $M$ to remain attached to the displayed family $D$ under the same local naming row.

Compose this with the existing route surfaces. The $\CrossHistCausalUp$ rows supply the finite causal witnesses consumed by $D$, and the $\MaxCausalRateUp$ rows supply the unary-bound rate surface consumed by $M$. Thus the root route is $D$ through $C$ before $M$, with no detached host-rate coordinate.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate frame-refusal row]
\label{thm:cross-hist-causal-rate-obligation-frame-refusal}
Any public rate read from an accepted $\CrossHistCausalRateUp$ packet must consume both the observer-symmetry row $S$ and the no-global-frame ledger $G$ before the read can be used by $\InterHistInvariantUp$ or $\MaxCausalRateUp$ consumers. The row refuses a global frame, synchronized clock, metric tensor, and host speed coordinate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cross-hist-causal-rate-carrier}. The only public frame-facing rows are $S$ and $G$: $S$ records observer symmetry, and $G$ records absence of a global synchronization surface.

Use \autoref{thm:cross-hist-causal-rate-namecert-obligations}. Observer-symmetry preservation and no-global-frame refusal are obligations of the same local $\NameCert_{\CrossHistCausalRateUp}$ surface as the maximum-rate row exactness clause.

Now compose the invariant and consumer boundaries. By \autoref{thm:cross-hist-causal-rate-invariant-boundary} and \autoref{thm:cross-hist-causal-rate-no-global-frame-boundary}, an invariant-facing rate read cannot proceed without $S$ and $G$. By \autoref{thm:cross-hist-causal-rate-consumer-route-totality}, every consumer route stays inside the displayed rows, so neither an $\InterHistInvariantUp$ consumer nor a $\MaxCausalRateUp$ consumer can introduce a global frame, clock, metric, or host speed coordinate.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate observer-slot route]
\label{thm:cross-hist-causal-rate-observer-slot-route}
Every accepted $\CrossHistCausalRateUp$ packet reads observer slots only through the displayed rows
$$
A,B,D,H,C,P,N.
$$
The rows $A$ and $B$ supply the two observer-history slots, $D$ supplies the finite causal-dependence route family over those slots, and $H,C,P,N$ supply the componentwise transport, displayed continuation replay, package provenance, and local naming rows. No observer-facing consumer may read an unlisted Hist slot, synchronized clock, privileged observer frame, or causal route outside this displayed family.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cross-hist-causal-rate-carrier}. The only observer-history coordinates are $A$ and $B$, and the only causal rows attached to them are the finite route entries in $D$.

Use the NameCert obligations of \autoref{thm:cross-hist-causal-rate-namecert-obligations}. They require observer-history slots, causal-dependence route replay, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming to be read from the same packet.

Thus every observer-slot read is routed through $A,B,D,H,C,P,N$, with no carrier coordinate left for a hidden observer slot or external frame.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate maximum-row exactness]
\label{thm:cross-hist-causal-rate-max-row-exactness}
For every accepted $\CrossHistCausalRateUp$ packet
$$
R=(A,B,D,M,S,G,H,C,P,N),
$$
the maximum-rate row $M$ is attached to the displayed dependence route family $D$. A consumer may not read $M$ as a host real-valued speed, metric row, detached unary bound, or maximum over causal data not displayed in $D$.
\end{theorem}
\begin{proof}
Project \autoref{def:cross-hist-causal-rate-carrier}. The row $M$ is declared as the $\MaxCausalRateUp$ unary-bound packet over exactly the dependence routes in $D$.

Apply \autoref{thm:cross-hist-causal-rate-namecert-obligations}. Its maximum-rate exactness obligation keeps the rate row inside the same carrier as the two observer histories, the finite causal route family, and the structural rows.

Since no additional maximum-rate coordinate occurs in the packet, the only available rate read is the one attached to $D$.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate symmetry non-escape]
\label{thm:cross-hist-causal-rate-symmetry-nonescape}
Any invariant-facing consumer of an accepted $\CrossHistCausalRateUp$ packet may read the rate only after both the observer-symmetry row $S$ and the no-global-frame ledger $G$ are present. The read is therefore an observer-symmetric finite maximum over the displayed route family $D$, not a global-frame synchronization, metric tensor, privileged observer surface, or host speed.
\end{theorem}
\begin{proof}
Begin with the carrier projection. The rows $S$ and $G$ are distinct public coordinates of the packet, with $S$ recording swap-invariance of the two Hist slots and $G$ recording the absence of a global synchronization surface.

Use \autoref{thm:cross-hist-causal-rate-namecert-obligations} to keep observer-symmetry preservation and no-global-frame refusal as obligations of the same local $\NameCert$ row. A consumer that omits either row has not consumed the displayed certificate surface.

Finally combine the maximum-rate row with those two gates. The invariant-facing read can only use $M$ through $D$ together with $S,G,H,C,P,N$, so it cannot escape to a global frame, metric tensor, privileged observer, or host-level speed.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate invariant boundary]
\label{thm:cross-hist-causal-rate-invariant-boundary}
Every accepted $\CrossHistCausalRateUp$ packet reads its maximum-rate row as an observer-symmetric inter-Hist invariant over the two displayed history slots. A consumer may reach the rate only through $A,B,D,M,S,G,H,C,P,N$, and it may not read a maximum-rate value when either the observer-symmetry row $S$ or the no-global-frame ledger $G$ is absent.
\end{theorem}
\begin{proof}
Project the accepted carrier. The two histories are the rows $A$ and $B$, and all causal content is the finite route family $D$.

Read the maximum-rate row $M$ through the same route family. Since $M$ is specified over the displayed dependence routes, it is not a host real-valued speed and not a metric row.

Now apply the observer-symmetry row $S$. It records that swapping $A$ and $B$ preserves the same maximum-rate read after componentwise transport, matching the finite invariant surface of \autoref{ch:concrete-instances-interhistinvariant-namecert}.

Finally inspect the no-global-frame ledger $G$. Without $G$, the read could not certify that the rate is independent of a privileged synchronization surface; without $S$, it could not certify observer symmetry. Hence a public maximum-rate read requires both rows.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate no-global-frame boundary]
\label{thm:cross-hist-causal-rate-no-global-frame-boundary}
For every accepted $\CrossHistCausalRateUp$ packet
$$
R=(A,B,D,M,S,G,H,C,P,N),
$$
any public rate read consumes the no-global-frame ledger row $G$ together with the two observer-history slots, causal-dependence route family, maximum-rate row, observer-symmetry row, and structural rows. No consumer route may read the maximum-rate row as a synchronized clock, privileged observer-frame row, metric tensor, or global-frame coordinate.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rate-facing coordinates are $A,B,D,M,S,G,H,C,P,N$, and the row $G$ is the only displayed ledger that records the absence of a global synchronization surface.

Apply \autoref{thm:cross-hist-causal-rate-namecert-obligations}. Its no-global-frame refusal clause belongs to the same local $\NameCert$ surface as maximum-rate exactness and observer-symmetry preservation, so a consumer that reads $M$ without $G$ has not consumed the accepted packet.

Finally use \autoref{thm:cross-hist-causal-rate-invariant-boundary}. The invariant read reaches $M$ only through $A,B,D,M,S,G,H,C,P,N$ and is blocked when $G$ is absent. Since the carrier has no synchronized-clock, privileged-frame, metric, or global-coordinate row, the boundary is exhaustive.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate consumer route totality]
\label{thm:cross-hist-causal-rate-consumer-route-totality}
Every consumer route out of an accepted $\CrossHistCausalRateUp$ carrier factors through exactly the displayed rows
$$
A,B,D,M,S,G,H,C,P,N.
$$
The route reads the two observer histories, finite causal-dependence rows, maximum-rate row, observer-symmetry row, no-global-frame ledger, componentwise transport, continuation replay, package provenance, and local naming row, with no additional host rate, hidden causal witness, synchronized clock, or privileged observer-frame coordinate.
\end{theorem}
\begin{proof}
Start from \autoref{def:cross-hist-causal-rate-carrier}. It lists the complete carrier as $A,B,D,M,S,G,H,C,P,N$ and declares that no global observer frame, synchronized clock, metric tensor, host speed, unlisted causal witness, or detached rate read is a coordinate.

The observer-slot route theorem accounts for $A,B,D,H,C,P,N$, and the maximum-row exactness theorem accounts for $M$ as attached to $D$. The symmetry non-escape and invariant-boundary theorems then account for $S$ and $G$ as required gates for invariant-facing rate reads.

Combining those projections leaves no unclassified consumer route. Every public read is one of the displayed carrier rows, replayed through the structural rows, and no outside row can be inserted without leaving the accepted $\NameCert_{\CrossHistCausalRateUp}$ surface.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate observer-swap preservation]
\label{thm:cross-hist-causal-rate-observer-swap-preservation}
For an accepted $\CrossHistCausalRateUp$ packet $R=(A,B,D,M,S,G,H,C,P,N)$, the swap row $S$ is the only admissible observer-symmetry witness for the maximum-rate read. Swapping the two history slots preserves the rate only by consuming $S$ together with the transported dependence routes in $D$ and the structural rows $H,C,P,N$; no source-equivalence transport, host symmetry principle, or global observer frame supplies a second witness.
\end{theorem}
\begin{proof}
Project the carrier. The row $S$ is the displayed observer-symmetry coordinate, and no other row is declared as a witness that swapping $A$ and $B$ preserves the maximum-rate read.

Use \autoref{thm:cross-hist-causal-rate-namecert-obligations}. Observer-symmetry preservation is a local certificate obligation for the same packet that contains $D,M,H,C,P,N$, so the swap read must remain attached to those displayed rows.

The invariant boundary confirms that a rate read requires $S$ before it can be observer-symmetric. Because the packet also records no source-equivalence transport, host symmetry principle, or global frame, any admissible observer-swap preservation witness is exactly the row $S$ transported through $H,C,P,N$.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate TasteGate obligation]
\label{thm:cross-hist-causal-rate-tastegate-obligation}
The later Lean \texttt{TasteGate.lean} development for $\CrossHistCausalRateUp$ must include explicit $\mathsf{Nontrivial}\ \CrossHistCausalRateUp$ and $\mathsf{FieldFaithful}\ \CrossHistCausalRateUp$ instances. These gate-facing instances must preserve the two Hist slots, causal-dependence route family, maximum-rate row, observer-symmetry row, no-global-frame ledger, transport rows, continuation routes, provenance row, and local naming row.
\end{theorem}
\leanchecked{BEDC.Derived.CrossHistCausalRateUp.taste\_gate}
\begin{proof}
The packet is not determined by a single event name. Its carrier has separate rows for two histories, dependence routes, the maximum-rate row, observer symmetry, and no-global-frame refusal.

Nontriviality rules out an empty encoding in which every carrier collapses to one indistinct point. Field faithfulness records preservation of the displayed row inventory, so a TasteGate consumer can distinguish the two Hist slots, the dependence routes, the maximum-rate row, the symmetry row, and the no-global-frame ledger.
\end{proof}

\begin{definition}[Cross-Hist causal-rate root source envelope]
\label{def:cross-hist-causal-rate-root-source-envelope}
A $\CrossHistCausalRateUp$ root source envelope is the finite packet
$$
E=(A,B,D,M,S,G,H,C,P,N)
$$
whose rows are read over $\ObserverHistIdentityUp$, $\CrossHistCausalUp$,
$\MaxCausalRateUp$, $\InterHistInvariantUp$, $\ContinuationTraceUp$,
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ as follows:
\begin{enumerate}
\item $A,B$ are the two observer-history identity slots;
\item $D$ is the displayed cross-Hist causal route family over those slots;
\item $M$ is the maximum causal-rate row attached to exactly the route family
  $D$;
\item $S$ is the observer-symmetry row for the rate read;
\item $G$ is the no-global-frame ledger;
\item $H,C,P,N$ are respectively the componentwise $\hsame$ transport,
  displayed continuation trace, package provenance, and local NameCert rows.
\end{enumerate}
The envelope has no row for host speed, synchronized clocks, a global frame, a
metric tensor, an unlisted causal witness, or a maximum-rate read detached from
$D$.
\end{definition}

\begin{theorem}[Cross-Hist causal-rate root classifier stability]
\label{thm:cross-hist-causal-rate-root-classifier-stability}
Let
$$
E=(A,B,D,M,S,G,H,C,P,N)
\quad\text{and}\quad
E'=(A',B',D',M',S',G',H',C',P',N')
$$
be accepted $\CrossHistCausalRateUp$ root source envelopes. If the two Hist
slots $A,A'$ and $B,B'$, the causal route families $D,D'$, the maximum-rate
rows $M,M'$, the observer-symmetry rows $S,S'$, and the no-global-frame ledgers
$G,G'$ agree componentwise under the displayed $\hsame$, $\Cont$, $\Pkg$, and
local $\NameCert$ rows, then $E$ and $E'$ classify the same root
causal-rate packet.

The classifier has no coordinate for a host speed, synchronized clock, global
frame, metric tensor, detached maximum row, hidden causal route, or unlisted
observer slot.
\end{theorem}
\begin{proof}
Project the root source envelopes. Their classifier-visible rows are exactly
$A,B,D,M,S,G,H,C,P,N$ and $A',B',D',M',S',G',H',C',P',N'$.

Use \autoref{thm:cross-hist-causal-rate-observer-slot-route} for the Hist
slots and causal route family. The observer-facing part of each packet is
therefore exhausted by $A,B,D,H,C,P,N$ and its primed counterpart.

Use \autoref{thm:cross-hist-causal-rate-max-row-exactness} together with
\autoref{thm:cross-hist-causal-rate-symmetry-nonescape} and
\autoref{thm:cross-hist-causal-rate-no-global-frame-boundary}. These results
keep the maximum-rate row attached to $D$ and keep the observer-symmetry and
no-frame rows on the same route.

Finally apply \autoref{thm:cross-hist-causal-rate-consumer-route-totality}.
Since every consumer route is one of the displayed rows, the componentwise
agreement hypotheses identify the whole classifier packet and leave no hidden
coordinate through which the two envelopes could diverge.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root observer-slot exhaustion]
\label{thm:cross-hist-causal-rate-root-observer-slot-exhaustion}
Every accepted $\CrossHistCausalRateUp$ root packet exhausts its observer-slot reads through the displayed rows
$$
A,B,D,H,C,P,N.
$$
A consumer may inspect the two observer histories only by reading $A$ and $B$, replaying the causal route family $D$, transporting the components through $H$, following the displayed continuation route $C$, citing the package provenance $P$, and closing the local $\NameCert_{\CrossHistCausalRateUp}$ row $N$. No unlisted observer slot, host clock, global frame coordinate, metric row, or detached causal route is available to such a read.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cross-hist-causal-rate-carrier}. The only observer-facing rows are $A$ and $B$, and the only displayed route family attached to them is $D$.

Apply the NameCert obligations of \autoref{thm:cross-hist-causal-rate-namecert-obligations} and the observer-slot route theorem \autoref{thm:cross-hist-causal-rate-observer-slot-route}. These identify the observer histories as the carrier-visible slots and force consumer access through the finite route family.

The structural part is fixed by $H,C,P,N$. Consumer route totality from \autoref{thm:cross-hist-causal-rate-consumer-route-totality} leaves no route outside the displayed transport, continuation, provenance, and local naming rows, while the frame-refusal boundary excludes host clocks, global frames, metric rows, and detached causal coordinates.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root route-rate factorization]
\label{thm:cross-hist-causal-rate-root-route-rate-factorization}
Every maximum-rate read from an accepted $\CrossHistCausalRateUp$ root packet factors through the displayed causal route family $D$ before it reaches the maximum-rate row $M$. Thus a $\MaxCausalRateUp$ consumer may read $M$ only after $A,B,D,H,C,P,N$ have supplied the observer slots, route replay, transport, continuation, provenance, and local naming surface.
\end{theorem}
\begin{proof}
Start from the carrier and the route obligation package. The row $M$ is not a free host-valued speed coordinate; it is the maximum-rate row attached to exactly the causal route family $D$.

Apply maximum-row exactness from \autoref{thm:cross-hist-causal-rate-max-row-exactness}. It binds the maximum-rate read to the displayed route family and rules out a second maximum row detached from $D$.

Now use observer-slot exhaustion together with consumer route totality. The read reaches $D$ through $A,B,H,C,P,N$, then reads $M$ as the rate row for that same route. Hence every maximum-rate read factors through $D$ before the row $M$ is consumed.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root frame-refusal exactness]
\label{thm:cross-hist-causal-rate-root-frame-refusal-exactness}
Every invariant-facing rate read from an accepted $\CrossHistCausalRateUp$ root packet consumes both the observer-symmetry row $S$ and the no-global-frame ledger $G$. Consequently no global frame, synchronized clock, metric tensor, host speed row, or privileged observer coordinate can enter an $\InterHistInvariantUp$ or $\MaxCausalRateUp$ consumer route.
\end{theorem}
\begin{proof}
Read the invariant boundary of \autoref{thm:cross-hist-causal-rate-invariant-boundary}. It places the invariant-facing use of the rate behind the observer-symmetry row $S$ and the no-global-frame ledger $G$.

Apply the no-global-frame boundary and frame-refusal composition theorems. The row $G$ is the carrier-visible refusal row for synchronized clocks, privileged frames, metric tensors, host speeds, and global-frame coordinates, and it composes with $S$ on the same route before any invariant-facing read is accepted.

Finally combine route-rate factorization with consumer route totality. Since the rate read has already passed through $D$ before $M$ and since every consumer route is displayed by $H,C,P,N$, there is no remaining coordinate through which a global frame or host speed row can enter.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root ledger exactness]
\label{thm:cross-hist-causal-rate-root-ledger-exactness}
Every downstream rate consumer of an accepted $\CrossHistCausalRateUp$ root
source envelope reads exactly the displayed route, observer-symmetry,
no-global-frame, transport, continuation, provenance, and local NameCert rows
$$
A,B,D,M,S,G,H,C,P,N.
$$
The read has no host speed coordinate, synchronized clock, global frame,
metric tensor, hidden causal route, detached maximum row, or unlisted observer
slot.
\end{theorem}
\begin{proof}
Start with \autoref{def:cross-hist-causal-rate-root-source-envelope}. The
envelope displays the observer slots, route family, rate row, symmetry row,
no-frame ledger, transport, continuation, provenance, and local name as one
finite packet.

Apply \autoref{thm:cross-hist-causal-rate-root-consumer-exactness}. It states
that every $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer reads exactly
the ten rows of the accepted packet.

Apply \autoref{thm:cross-hist-causal-rate-continuationtrace-factorization} to
the continuation-facing part of the route. The displayed row $C$ is the only
continuation trace through which the consumer can replay the observer slots,
causal routes, maximum-rate row, and invariant boundary.

The exclusion clause in the envelope and the totality theorem then rule out
host speeds, synchronized clocks, global frames, metric tensors, hidden
causal routes, detached maximum rows, and unlisted observer slots. Therefore
the downstream ledger is exact.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate route obligation package]
\label{thm:cross-hist-causal-rate-route-obligation-package}
For the carrier
$$
R=(A,B,D,M,S,G,H,C,P,N)
$$
of \autoref{def:cross-hist-causal-rate-carrier}, the rows $A,B,D,M,S,G,H,C,P,N$ supply the complete $\CrossHistCausalRateUp$ obligation package: carrier admission for the two observer-history slots, finite route replay for the displayed causal-dependence family, maximum-rate exactness for $M$, observer symmetry for $S$, no-global-frame refusal for $G$, componentwise $\hsame$ transport, displayed continuation replay, package provenance, and the local $\NameCert_{\CrossHistCausalRateUp}$ row.
\end{theorem}
\begin{proof}
Project the displayed carrier. The two observer slots are $A$ and $B$, the finite causal-dependence route family is $D$, and the maximum-rate row is $M$.

The rows $S$ and $G$ are the two boundary gates for reading $M$ as an observer-symmetric invariant rather than a global-frame value. The introduction fixes the sibling sources for observer identity, cross-Hist causal rows, maximum causal rate, inter-Hist invariant reading, and finite continuation replay, so the package consumes exactly those public surfaces.

Finally $H,C,P,N$ provide the transport, replay, provenance, and local naming rows. Since the carrier contains no additional coordinate, the listed rows exhaust the obligation package.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate route replay obligation]
\label{thm:cross-hist-causal-rate-route-replay-obligation}
Every accepted $\CrossHistCausalRateUp$ causal-rate read must replay the finite route family $D$ before reading the maximum-rate row $M$. A read that reaches $M$ without the displayed $D$ routes is not a read of the carrier $R=(A,B,D,M,S,G,H,C,P,N)$.
\end{theorem}
\begin{proof}
Project the carrier and inspect the order of the public rows. The row $M$ is specified as a maximum-rate packet over exactly the dependence routes displayed in $D$.

The continuation coordinate $C$ records the consumer route: read the two histories, replay each route in $D$, then read $M$. Thus $M$ is not an independent host-valued speed row and cannot be consumed before the finite route family has been replayed.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate observer symmetry obligation]
\label{thm:cross-hist-causal-rate-observer-symmetry-obligation}
An observer-symmetric $\CrossHistCausalRateUp$ rate read requires both the observer-symmetry row $S$ and the no-global-frame ledger $G$. The row $S$ supplies invariance under swapping the two Hist slots, while $G$ refuses synchronized clocks, privileged observer frames, and global synchronization data.
\end{theorem}
\begin{proof}
The invariant boundary theorem already identifies $S$ and $G$ as the two public gates for a maximum-rate read. Projecting the carrier shows that $S$ is the only row recording preservation under the observer swap, and $G$ is the only row excluding the forbidden global-frame data.

If $S$ is absent, the read has no observer-symmetry witness. If $G$ is absent, the same numerical-looking read could depend on a hidden synchronization surface. Hence an observer-symmetric rate read requires both rows.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate downstream package]
\label{thm:cross-hist-causal-rate-downstream-unblock-package}
For an accepted $\CrossHistCausalRateUp$ packet
$$
R=(A,B,D,M,S,G,H,C,P,N),
$$
the downstream package consists exactly of carrier admission, the two observer slots $A,B$, causal route replay through $D$, maximum-row exactness through $M$, observer-symmetry through $S$, no-global-frame refusal through $G$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\CrossHistCausalRateUp}$ row $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The observer slots, dependence routes, maximum-rate row, symmetry row, no-global-frame row, transports, continuation routes, provenance, and local name are exactly the displayed rows $A,B,D,M,S,G,H,C,P,N$.

Apply \autoref{thm:cross-hist-causal-rate-namecert-obligations}, \autoref{thm:cross-hist-causal-rate-route-replay-obligation}, and \autoref{thm:cross-hist-causal-rate-observer-symmetry-obligation}. These obligations keep the causal route replay before the maximum-rate read and keep the observer-symmetry and no-global-frame rows inside the same local naming surface.

Finally use \autoref{thm:cross-hist-causal-rate-consumer-route-totality}. It leaves no route for a hidden causal witness, host speed, synchronized clock, global frame, or detached maximum row, so the downstream package is exhausted by the displayed carrier projections.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root consumer exactness]
\label{thm:cross-hist-causal-rate-root-consumer-exactness}
Every $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer of an accepted $\CrossHistCausalRateUp$ packet reads exactly the rows
$$
A,B,D,M,S,G,H,C,P,N.
$$
The consumer reads no maximum-rate value outside $M$, no invariant boundary outside $S,G$, and no transport, route, provenance, or naming evidence outside $H,C,P,N$.
\end{theorem}
\begin{proof}
Start with the downstream package of \autoref{thm:cross-hist-causal-rate-downstream-unblock-package}. It names the whole consumer-facing packet and orders the maximum-rate read after the displayed causal route family.

For a $\MaxCausalRateUp$ consumer, apply \autoref{thm:cross-hist-causal-rate-max-row-exactness}. The row $M$ is attached to $D$, so the consumer cannot read a detached host-valued rate.

For an $\InterHistInvariantUp$ consumer, apply \autoref{thm:cross-hist-causal-rate-invariant-boundary} and \autoref{thm:cross-hist-causal-rate-no-global-frame-boundary}. These rows force observer symmetry and no-global-frame refusal through $S$ and $G$ before the rate can be used as an invariant.

The structural evidence is then fixed by $H,C,P,N$. Since the carrier has no additional coordinate, both root consumers read exactly the displayed ten rows.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate scoped route closure]
\label{thm:cross-hist-causal-rate-scoped-route-closure}
The scoped route for every accepted $\CrossHistCausalRateUp$ root consumer is bound to $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\ObserverHistIdentityUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, and $\InterHistInvariantUp$. Within that scope, every downstream read factors through the rows $A,B,D,M,S,G,H,C,P,N$ and no global observer frame, synchronized clock, metric tensor, host real-valued speed, or unlisted causal witness is available.
\end{theorem}
\begin{proof}
Project the carrier and read the provenance row. The row $P$ names the packet over $\CrossHistCausalRateUp$, $\ObserverHistIdentityUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, $\InterHistInvariantUp$, $\ContinuationTraceUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

Apply the downstream package and root consumer exactness theorems. They identify $A,B$ as the observer-Hist slots, $D$ as the displayed $\CrossHistCausalUp$ route family, $M$ as the $\MaxCausalRateUp$ row, and $S,G$ as the invariant-facing boundary consumed by $\InterHistInvariantUp$ readers.

The remaining rows $H,C,N$ provide componentwise transport, displayed route replay, and local naming. Because \autoref{def:cross-hist-causal-rate-carrier} contains no global frame, clock, metric, host-speed, or hidden causal coordinate, the scoped route is closed under exactly the listed BEDC primitives and sibling surfaces.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate route-bound monotonicity]
\label{thm:cross-hist-causal-rate-route-bound-monotonicity}
Extending the displayed causal-dependence route family $D$ of an accepted $\CrossHistCausalRateUp$ packet can update the maximum-rate row $M$ only through the same observer slots $A,B$ and the structural rows $H,C,P,N$. No extension of $D$ creates a detached host speed, a fresh observer coordinate, or a second maximum-rate row outside the carrier $R=(A,B,D,M,S,G,H,C,P,N)$.
\end{theorem}
\begin{proof}
Start with the route replay obligation. It places every causal-rate read through the displayed family $D$ before the row $M$ is consumed.

Use observer symmetry and the downstream package theorem. The same observer slots $A,B$ and the same boundary rows $S,G$ are carried along any accepted consumer route, so extending $D$ cannot switch to a new observer interface.

Finally apply root consumer exactness and scoped route closure. They keep every update of $M$ inside $A,B,D,M,S,G,H,C,P,N$, with $H,C,P,N$ supplying transport, replay, provenance, and naming. Thus route extension is monotone only along the displayed BEDC rows.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate invariant cap exhaustion]
\label{thm:cross-hist-causal-rate-invariant-cap-exhaustion}
Every invariant cap read by $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumes exactly the rows $A,B,D,M,S,G,H,C,P,N$ of an accepted $\CrossHistCausalRateUp$ packet. The cap cannot add a hidden host speed, metric tensor, synchronized clock, privileged frame, or unlisted frame coordinate.
\end{theorem}
\begin{proof}
Apply the downstream package theorem and root consumer exactness. They identify $\MaxCausalRateUp$ and $\InterHistInvariantUp$ consumers with the displayed carrier rows, not with an ambient frame.

Route replay contributes $D$ before $M$, and observer symmetry contributes the gate row $S$. The no-global-frame ledger $G$ stays on the same route before the rate can be read as an invariant.

Scoped route closure then accounts for the structural rows $H,C,P,N$. Since the carrier has no further coordinate, every invariant cap is exhausted by exactly the ten displayed rows.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate frame-refusal composition]
\label{thm:cross-hist-causal-rate-frame-refusal-composition}
For every accepted $\CrossHistCausalRateUp$ packet, the no-global-frame ledger $G$ composes with the observer-symmetry row $S$ and the causal route replay family $D$ before any public rate read is accepted. A public read of $M$ is therefore refused unless it passes through $D$, $S$, $G$, and the structural rows $H,C,P,N$ over the same observer slots $A,B$.
\end{theorem}
\begin{proof}
First replay the causal route family. The route replay obligation places $D$ before any accepted read of the maximum-rate row $M$.

Next use observer symmetry. The row $S$ keeps the read invariant under swapping the displayed observer-Hist slots, so the rate is not attached to a privileged observer order.

Finally apply the no-global-frame boundary together with downstream package exactness, root consumer exactness, and scoped route closure. These results place $G$ on the same route as $D$ and $S$, then repack the public read through $H,C,P,N$. Hence no public rate read is accepted before the frame-refusal ledger has composed with the observer-symmetric route replay.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root carrier admission]
\label{thm:cross-hist-causal-rate-root-carrier-admission}
Every accepted $\CrossHistCausalRateUp$ root packet exposes exactly the rows
$$
A,B,D,M,S,G,H,C,P,N
$$
over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The rows admit the two observer-history slots, causal-dependence route family, maximum-rate row, observer-symmetry row, no-global-frame ledger, componentwise transport, displayed replay, provenance, and local naming surface, with no additional root coordinate.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:cross-hist-causal-rate-carrier}. Its displayed inventory is the tuple $R=(A,B,D,M,S,G,H,C,P,N)$.

Apply \autoref{thm:cross-hist-causal-rate-route-obligation-package} and \autoref{thm:cross-hist-causal-rate-downstream-unblock-package}. These theorems identify the same rows as the carrier admission, route replay, maximum-row exactness, observer-symmetry, no-global-frame refusal, transport, continuation, provenance, and local naming obligations.

The exactness statement \autoref{thm:cross-hist-causal-rate-root-consumer-exactness} leaves no downstream read outside the ten displayed rows. Hence root carrier admission is exactly the finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface named above.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root route replay]
\label{thm:cross-hist-causal-rate-root-route-replay}
Every downstream invariant-facing read of an accepted $\CrossHistCausalRateUp$ packet replays the finite causal-dependence route family $D$ before reading the maximum-rate row $M$. The read reaches $M$ only through the displayed rows $H,C,P,N$ and the invariant boundary rows $S,G$.
\end{theorem}
\begin{proof}
Start with \autoref{thm:cross-hist-causal-rate-route-replay-obligation}. It states that a causal-rate read must replay $D$ before consuming $M$.

For invariant-facing consumers, apply \autoref{thm:cross-hist-causal-rate-invariant-boundary} and \autoref{thm:cross-hist-causal-rate-observer-symmetry-obligation}. These place the observer-symmetry row $S$ and the no-global-frame ledger $G$ on the same route before the rate can be used as an invariant.

Finally use \autoref{thm:cross-hist-causal-rate-scoped-route-closure}. It binds the route to the displayed transport, continuation, provenance, and local naming rows, so no invariant-facing read can reach $M$ by skipping the finite $D$ family.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root no-frame ledger]
\label{thm:cross-hist-causal-rate-root-no-frame-ledger}
For every accepted $\CrossHistCausalRateUp$ root packet, the row $G$ is the complete no-global-frame ledger. It refuses synchronized clocks, privileged observer frames, metric tensors, host speeds, and global-frame coordinates for any downstream maximum-rate or invariant-facing read.
\end{theorem}
\begin{proof}
Project the ledger row from the carrier and apply \autoref{thm:cross-hist-causal-rate-no-global-frame-boundary}. The row $G$ is the unique displayed boundary excluding global synchronization data from a maximum-rate read.

Apply \autoref{thm:cross-hist-causal-rate-root-route-replay} and \autoref{thm:cross-hist-causal-rate-root-carrier-admission}. They keep every downstream read inside $A,B,D,M,S,G,H,C,P,N$, with $G$ on the route before $M$ can be used by an invariant-facing consumer.

The scoped route closure theorem then rules out hidden global-frame coordinates, host speeds, metric tensors, synchronized clocks, and privileged observer frames. Since the carrier contains no second ledger row, $G$ is the complete refusal ledger for the root packet.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate route-family exhaustion]
\label{thm:cross-hist-causal-rate-route-family-exhaustion}
Every accepted $\CrossHistCausalRateUp$ packet exhausts its causal route reads through the displayed family $D$ and the structural rows $A,B,H,C,P,N$. A consumer may replay a causal route only by reading the two observer-history slots $A,B$, the route family $D$, componentwise transport $H$, displayed continuation replay $C$, package provenance $P$, and the local naming row $N$; it may not read a hidden causal route, unlisted unary slot, synchronized clock, privileged frame, or metric row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cross-hist-causal-rate-carrier}. The only causal-dependence coordinate is the finite family $D$, and it is attached to the two displayed observer-history slots $A$ and $B$.

Apply \autoref{thm:cross-hist-causal-rate-route-replay-obligation} and \autoref{thm:cross-hist-causal-rate-scoped-route-closure}. These results force route replay through $D$ and keep every downstream route inside the stated BEDC scope.

Use \autoref{thm:cross-hist-causal-rate-root-consumer-exactness}. It fixes the structural rows $H,C,P,N$ as the transport, continuation, provenance, and naming surface for root consumers. Since the carrier has no hidden causal-route coordinate, the displayed family exhausts all accepted causal reads.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate maximum observer gate]
\label{thm:cross-hist-causal-rate-max-observer-gate}
For every accepted $\CrossHistCausalRateUp$ root packet, the row $M$ is readable as an invariant only after the causal route family $D$ has been replayed and both observer gate rows $S,G$ have been displayed. Thus no $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer can read $M$ as an invariant from a route that omits the observer-symmetry row, the no-global-frame ledger, or the displayed causal replay.
\end{theorem}
\begin{proof}
Start from \autoref{thm:cross-hist-causal-rate-root-route-replay}. It places replay of $D$ before every invariant-facing read of $M$.

Apply \autoref{thm:cross-hist-causal-rate-invariant-boundary} and \autoref{thm:cross-hist-causal-rate-observer-symmetry-obligation}. They identify $S$ and $G$ as the two observer-facing gates needed before a rate read can be used as an invariant.

Finally use \autoref{thm:cross-hist-causal-rate-root-no-frame-ledger} and \autoref{thm:cross-hist-causal-rate-root-consumer-exactness}. These results keep the gate rows on the same root consumer route and exclude synchronized clocks, privileged frames, metric tensors, host speeds, and detached maximum rows.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate continuation-trace factorization]
\label{thm:cross-hist-causal-rate-continuationtrace-factorization}
Every continuation-facing read of an accepted $\CrossHistCausalRateUp$ packet factors through the $\ContinuationTraceUp$ row $C$ after the observer identity rows $A,B$, the $\CrossHistCausalUp$ route family $D$, the $\MaxCausalRateUp$ row $M$, and the $\InterHistInvariantUp$ boundary rows $S,G$ have been displayed. The factorization has no route through a hidden continuation, global frame, host speed, unlisted causal witness, or detached invariant boundary.
\end{theorem}
\begin{proof}
Read the displayed packet $R=(A,B,D,M,S,G,H,C,P,N)$ from \autoref{def:cross-hist-causal-rate-carrier}. The continuation coordinate $C$ is the only row that records the consumer route through the observer slots, route family, maximum-rate row, and invariant boundary.

Apply \autoref{thm:cross-hist-causal-rate-route-family-exhaustion}. It forces the causal part of the read through $A,B,D,H,C,P,N$ and rules out hidden causal routes.

Apply \autoref{thm:cross-hist-causal-rate-max-observer-gate}. It places the maximum-rate row $M$ behind the displayed route replay and the two observer gate rows $S,G$.

Use \autoref{thm:cross-hist-causal-rate-scoped-route-closure} and \autoref{thm:cross-hist-causal-rate-root-consumer-exactness} to repack the read as a root consumer route. The structural factor is therefore exactly $C$ together with $H,P,N$, and no continuation-facing consumer can bypass the displayed route.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root observer-gate totality]
\label{thm:cross-hist-causal-rate-root-observer-gate-totality}
Every invariant-facing read of the maximum-rate row $M$ from an accepted $\CrossHistCausalRateUp$ root packet must pass through the displayed causal route family $D$, the observer gate rows $S,G$, and the structural rows $H,C,P,N$. Thus a $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer never reads $M$ directly from the observer slots $A,B$, from a hidden frame row, or from a detached host-speed coordinate.
\end{theorem}
\begin{proof}
Read the carrier $R=(A,B,D,M,S,G,H,C,P,N)$ from \autoref{def:cross-hist-causal-rate-carrier}. The carrier gives the two observer slots and names $D,M,S,G,H,C,P,N$ as the only route, rate, gate, transport, replay, provenance, and naming rows.

Apply \autoref{thm:cross-hist-causal-rate-root-route-replay}. It places the finite route family $D$ before any invariant-facing read of $M$ and keeps the structural rows on the same route.

Apply \autoref{thm:cross-hist-causal-rate-max-observer-gate}. It adds the observer-symmetry row $S$ and the no-global-frame ledger $G$ before $M$ can be consumed by $\MaxCausalRateUp$ or $\InterHistInvariantUp$.

Finally use \autoref{thm:cross-hist-causal-rate-continuationtrace-factorization}. The continuation-facing read factors through $C$ after the observer slots, route family, maximum-rate row, and gate rows have been displayed, so no invariant-facing consumer can bypass $D,S,G,H,C,P,N$.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root route-family field faithfulness]
\label{thm:cross-hist-causal-rate-root-route-family-field-faithfulness}
For an accepted $\CrossHistCausalRateUp$ root packet, the rows
$$
A,B,D,M,S,G,H,C,P,N
$$
remain distinguishable as the two observer-history slots, causal route family, maximum-rate row, observer-symmetry row, no-global-frame ledger, componentwise transport, continuation replay, provenance, and local naming row. Any future field-faithful TasteGate encoding must preserve these rows as separate carrier fields and may not identify $D$ with $M$, $S$ with $G$, or the structural rows $H,C,P,N$ with the observer slots $A,B$.
\end{theorem}
\begin{proof}
Project the carrier definition. It lists $A,B,D,M,S,G,H,C,P,N$ as separate displayed coordinates, and each coordinate has a different consumer role in the packet.

Use \autoref{thm:cross-hist-causal-rate-root-route-replay} to separate $D$ from $M$: the route family is replayed before the maximum-rate row is read, so the two rows cannot collapse without losing the replay obligation.

Use \autoref{thm:cross-hist-causal-rate-root-no-frame-ledger} and \autoref{thm:cross-hist-causal-rate-max-observer-gate} to separate $S$ and $G$ from the rate row and from each other. The former records observer symmetry, while the latter refuses global-frame coordinates before the invariant-facing read.

The factorization theorem \autoref{thm:cross-hist-causal-rate-continuationtrace-factorization} keeps $C$ as the continuation trace, with $H,P,N$ supplying transport, provenance, and naming. Since those structural rows govern access to the packet rather than serving as observer slots, field faithfulness must keep the full displayed row inventory distinct.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root no-frame consumer cut]
\label{thm:cross-hist-causal-rate-root-no-frame-consumer-cut}
For every accepted $\CrossHistCausalRateUp$ root packet, the no-global-frame ledger $G$ is a consumer cut between valid $\MaxCausalRateUp$ or $\InterHistInvariantUp$ reads and any hidden clock, privileged frame, metric tensor, speed coordinate, or global-frame row. A consumer route that reaches $M$ as a rate or invariant must pass through $G$ together with $D,S,H,C,P,N$.
\end{theorem}
\begin{proof}
Start with \autoref{thm:cross-hist-causal-rate-root-no-frame-ledger}. It identifies $G$ as the complete ledger refusing synchronized clocks, privileged observer frames, metric tensors, host speeds, and global-frame coordinates.

Apply \autoref{thm:cross-hist-causal-rate-root-route-replay}. The valid consumer route reaches $M$ only after replaying the finite causal route family $D$ through the structural rows.

Apply \autoref{thm:cross-hist-causal-rate-max-observer-gate}. It places $S$ and $G$ before the maximum-rate row can be read as an invariant-facing value by $\MaxCausalRateUp$ or $\InterHistInvariantUp$.

Finally use \autoref{thm:cross-hist-causal-rate-continuationtrace-factorization}. Since the displayed continuation factor is $C$ and the carrier has no hidden clock, frame, metric, speed, or global-coordinate row, $G$ separates accepted consumers from every forbidden frame-facing read.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root observer-slot totality]
\label{thm:cross-hist-causal-rate-root-observer-slot-totality}
For every accepted $\CrossHistCausalRateUp$ root consumer, the first observer-history read is exhausted by the two displayed slots $A,B$ through $\ObserverHistIdentityUp$ and by the rows $D,H,C,P,N$ of the carrier
$$
R=(A,B,D,M,S,G,H,C,P,N).
$$
The consumer therefore cannot begin from an unlisted observer slot, a hidden history identity witness, or a detached causal route outside the $\CrossHistCausalRateUp$ packet.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:cross-hist-causal-rate-carrier}. It presents $A,B$ as the only observer-history slots and gives $D,H,C,P,N$ as the route, transport, continuation, provenance, and local naming rows around those slots.

Apply \autoref{thm:cross-hist-causal-rate-observer-slot-route}. It forces every observer-slot read through the displayed $\ObserverHistIdentityUp$ surface and the route rows of the packet.

Next apply \autoref{thm:cross-hist-causal-rate-root-route-replay}. The replay step keeps the consumer on the same finite $D$ route before any downstream maximum-rate read, matching the causal witness surface of \autoref{ch:concrete-instances-crosshistcausal-namecert}.

Since \autoref{ch:concrete-instances-maxcausalrate-namecert} and \autoref{ch:concrete-instances-interhistinvariant-namecert} consume the rate only after the route has been displayed, the root consumer has no earlier observer source than $A,B,D,H,C,P,N$.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root dependence-route exhaustion]
\label{thm:cross-hist-causal-rate-root-dependence-route-exhaustion}
Every dependence read of an accepted $\CrossHistCausalRateUp$ root packet is exhausted by the displayed $\CrossHistCausalUp$ route family $D$ replayed through the continuation row $C$. The replay may pass through $A,B,H,P,N$, but it cannot introduce a hidden causal witness, an unlisted dependence edge, a host clock, or a frame-side causal row.
\end{theorem}
\begin{proof}
Project the packet $R=(A,B,D,M,S,G,H,C,P,N)$. The only dependence-family coordinate is $D$, and the only continuation-facing replay coordinate is $C$.

Apply \autoref{thm:cross-hist-causal-rate-root-route-replay}. It places every downstream dependence read on the finite route family $D$ before the rate row $M$ can be consumed.

Now use \autoref{thm:cross-hist-causal-rate-observer-slot-route}. The route is anchored in the two observer slots $A,B$ and is transported only through the displayed structural rows $H,C,P,N$.

Finally apply \autoref{thm:cross-hist-causal-rate-root-no-frame-consumer-cut}. The no-frame cut excludes clocks, privileged frames, metric tensors, host speeds, and global-frame rows, so the replay through $C$ cannot acquire an extra causal witness outside the $\CrossHistCausalUp$ route family cited by \autoref{ch:concrete-instances-crosshistcausal-namecert}.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate root maximum-invariant handoff]
\label{thm:cross-hist-causal-rate-root-max-invariant-handoff}
An accepted $\CrossHistCausalRateUp$ root packet may hand the maximum-rate row $M$ to $\MaxCausalRateUp$ or $\InterHistInvariantUp$ only after the dependence route family $D$, the observer-symmetry row $S$, and the no-global-frame ledger $G$ are visible on the same root route. Thus $M$ is never exported as a detached rate value before the causal route, observer symmetry, and no-frame refusal rows have been displayed.
\end{theorem}
\begin{proof}
Start with \autoref{thm:cross-hist-causal-rate-root-route-replay}. It makes $D$ the finite route that precedes any invariant-facing read of $M$.

Apply \autoref{thm:cross-hist-causal-rate-max-observer-gate}. This places $S$ and $G$ before a $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer may read $M$ as an invariant-facing value.

Use \autoref{thm:cross-hist-causal-rate-root-no-frame-consumer-cut}. The cut keeps the handoff away from hidden clocks, privileged frames, metric tensors, host speeds, and global-frame rows.

The handoff therefore matches the sibling consumer surfaces of \autoref{ch:concrete-instances-maxcausalrate-namecert} and \autoref{ch:concrete-instances-interhistinvariant-namecert}: both receive $M$ only after the displayed $D,S,G$ route has been exposed by the $\CrossHistCausalRateUp$ carrier.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate invariant-rate ledger]
\label{thm:cross-hist-causal-rate-invariant-rate-ledger}
For an accepted $\CrossHistCausalRateUp$ packet with carrier
$$
R=(A,B,D,M,S,G,H,C,P,N)
$$
as in \autoref{def:cross-hist-causal-rate-carrier}, any $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer that reads $M$ as a rate or invariant first passes through the displayed route rows $D,S,G,H,C,P,N$. The ledger is the same finite root route supplied by route replay, observer-gate totality, and the no-frame consumer cut; it has no coordinate for a hidden causal route, synchronized clock, privileged frame, metric tensor, host speed, or detached maximum row.
\end{theorem}
\begin{proof}
Read the carrier displayed in \autoref{def:cross-hist-causal-rate-carrier}. The only rows downstream of the observer slots and upstream of the local name are the causal route family $D$, maximum-rate row $M$, observer-symmetry row $S$, no-frame ledger $G$, and structural rows $H,C,P,N$.

Apply \autoref{thm:cross-hist-causal-rate-root-route-replay}. It places $D$ before every invariant-facing read of $M$ and keeps the read inside the displayed structural route.

Now apply \autoref{thm:cross-hist-causal-rate-max-observer-gate} and \autoref{thm:cross-hist-causal-rate-root-no-frame-consumer-cut}. These results add $S$ and $G$ as mandatory gates before a $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer can use $M$.

The route is then closed by \autoref{thm:cross-hist-causal-rate-root-consumer-exactness}. Since the carrier has no extra frame-facing coordinate, the invariant-rate ledger is exactly $D,S,G,H,C,P,N$ before $M$ is consumed.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate observer-symmetry totality]
\label{thm:cross-hist-causal-rate-observer-symmetry-totality}
Every accepted $\CrossHistCausalRateUp$ root consumer that exports the maximum row $M$ to $\MaxCausalRateUp$ or $\InterHistInvariantUp$ exposes both observer-facing gates $S$ and $G$ on the same route as $D,H,C,P,N$. Thus observer symmetry and no-frame refusal are total for rate consumers: neither can be omitted, split into a second packet, or supplied by a host frame outside the carrier of \autoref{def:cross-hist-causal-rate-carrier}.
\end{theorem}
\begin{proof}
Start from \autoref{thm:cross-hist-causal-rate-invariant-rate-ledger}. It already places every invariant-rate read behind the displayed route $D,S,G,H,C,P,N$.

Use \autoref{thm:cross-hist-causal-rate-observer-symmetry-obligation}. The row $S$ records the observer-symmetry obligation for the two history slots, so a consumer that omits $S$ has no local certificate for invariant comparison.

Use \autoref{thm:cross-hist-causal-rate-root-no-frame-ledger}. The row $G$ is the finite refusal ledger for global-frame data, so a consumer that omits $G$ has no certified refusal of hidden clocks, privileged frames, metrics, or host speeds.

Finally apply \autoref{thm:cross-hist-causal-rate-root-consumer-exactness}. It keeps $S$ and $G$ on the same root route as $D,H,C,P,N$, giving totality for the observer-symmetry and no-frame gates.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate consumer boundary]
\label{thm:cross-hist-causal-rate-consumer-boundary}
The consumer boundary for an accepted $\CrossHistCausalRateUp$ packet is exact for $\MaxCausalRateUp$ and $\InterHistInvariantUp$ reads: before reading $M$, the consumer must pass through
$$
D,S,G,H,C,P,N.
$$
Consequently the root boundary is generated by the displayed carrier of \autoref{def:cross-hist-causal-rate-carrier}, route replay, observer-symmetry totality, and no-frame refusal, not by any off-carrier frame, external speed comparison, hidden causal route, or detached invariant source.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:cross-hist-causal-rate-root-no-frame-consumer-cut}. It states that the no-frame ledger $G$ separates valid maximum-rate or invariant reads from hidden clocks, privileged frames, metric tensors, and host speeds.

Apply \autoref{thm:cross-hist-causal-rate-observer-symmetry-totality}. This puts $S$ and $G$ on the same root route as $D,H,C,P,N$ for every consumer that exports $M$.

Now use \autoref{thm:cross-hist-causal-rate-invariant-rate-ledger}. The invariant-rate ledger fixes the route before $M$ is read, so the consumer boundary cannot be shortened to $D$ alone or to the observer slots alone.

The exactness claim follows from \autoref{thm:cross-hist-causal-rate-root-consumer-exactness}. The accepted packet contains no remaining coordinate from which a $\MaxCausalRateUp$ or $\InterHistInvariantUp$ consumer could recover an alternate read of $M$.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate observer identity dependence]
\label{thm:cross-hist-causal-rate-observer-identity-dependence}
For an accepted $\CrossHistCausalRateUp$ packet $R=(A,B,D,M,S,G,H,C,P,N)$, the observer slots $A$ and $B$ must be read through the $\ObserverHistIdentityUp$ identity surface before any route family $D$ is admissible. A route family detached from that identity read is not a valid causal-rate route.
\end{theorem}
\begin{proof}
Apply the observer-slot route theorem \autoref{thm:cross-hist-causal-rate-observer-slot-route}. It makes the two observer histories the entrance to the displayed causal route family.

Use \autoref{thm:cross-hist-causal-rate-root-observer-slot-totality} and \autoref{thm:cross-hist-causal-rate-root-dependence-route-exhaustion}. They place every admissible $D$ behind the same observer-slot read and prevent a hidden route outside $A,B,H,C,P,N$.

The no-global-frame boundary, root consumer exactness, and consumer boundary of \autoref{thm:cross-hist-causal-rate-no-global-frame-boundary}, \autoref{thm:cross-hist-causal-rate-root-consumer-exactness}, and \autoref{thm:cross-hist-causal-rate-consumer-boundary} then keep the identity dependence inside the packet rather than in a host frame.
\end{proof}

\begin{theorem}[Cross-Hist causal-rate falsifiability and independence surface]
\label{thm:cross-hist-causal-rate-falsifiability-independence-surface}
For an accepted $\CrossHistCausalRateUp$ packet
$$
R=(A,B,D,M,S,G,H,C,P,N),
$$
the carrier has a falsifiable consequence: a maximum-rate read is rejected unless both the observer-symmetry row $S$ and the no-global-frame ledger $G$ are present on the same displayed route as $D,M,H,C,P,N$. Its independence surface is witnessed by the simultaneous presence of the two $\Hist$ slots $A,B$, causal-dependence route family $D$, maximum-rate row $M$, observer-symmetry row $S$, no-global-frame ledger $G$, componentwise transport $H$, continuation row $C$, provenance row $P$, and local name $N$. This surface is not bijective to any listed sibling surface $\InterHistInvariantUp$, $\ObserverHistIdentityUp$, $\HistTimeStreamUp$, $\ContinuationTraceUp$, or $\SupplyKindRouterUp$.
\end{theorem}
\begin{proof}
Read the carrier definition \autoref{def:cross-hist-causal-rate-carrier}. It displays the two history slots, route family, maximum-rate row, observer-symmetry row, no-frame ledger, and structural rows as simultaneous fields of one packet.

Apply maximum-row exactness from \autoref{thm:cross-hist-causal-rate-max-row-exactness}. The maximum-rate row $M$ is attached to the finite dependence family $D$, not to a detached host speed or metric coordinate.

Use the invariant boundary and no-global-frame boundary of \autoref{thm:cross-hist-causal-rate-invariant-boundary} and \autoref{thm:cross-hist-causal-rate-no-global-frame-boundary}. Together with observer identity dependence from \autoref{thm:cross-hist-causal-rate-observer-identity-dependence}, they require the observer-symmetry and no-frame rows before a public maximum-rate read can pass to an invariant-facing consumer.

Finally use route-family field faithfulness from \autoref{thm:cross-hist-causal-rate-root-route-family-field-faithfulness}. The separate fields $A,B,D,M,S,G,H,C,P,N$ cannot collapse to the inventory of any one sibling surface, so the packet exposes a distinct independence surface while keeping the falsifiable maximum-rate rejection tied to its carrier rows.
\end{proof}

\input{parts/concrete_instances/5029_crosshistcausalrate_late_scope_and_status}
