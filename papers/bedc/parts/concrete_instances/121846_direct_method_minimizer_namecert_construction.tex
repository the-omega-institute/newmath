\chapter{A Concrete Naming Certificate for $\DirectMethodMinimizerUp$}
\label{ch:concrete-instances-direct-method-minimizer-namecert}
\origin{human}

Following Tonelli's existence theorem, Weierstrass compactness arguments, and the constructive accounts of the direct method in Dacorogna and Evans, the $\DirectMethodMinimizerUp$ packet records the finite route by which a variational minimum is read from compact sublevel data and lower-semicontinuity evidence. It sits after the compact finite-window discipline of \autoref{ch:concrete-instances-compactmetric-namecert}, the lower-semicontinuity boundary of \autoref{ch:concrete-instances-lowersemicontinuous-namecert}, the variational convergence surface of \autoref{ch:concrete-instances-gamma-convergence-namecert}, the Euler--Lagrange consumer of \autoref{ch:concrete-instances-eulerlagrange-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a displayed minimizer read; it does not import a host choice of minimizer, quotient equality of functionals, selected convergent subsequences, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[DirectMethodMinimizer carrier]
\label{def:direct-method-minimizer-carrier}
A $\DirectMethodMinimizerUp$ carrier is a finite $\BHist$ packet
$$
D=(X,K,L,S,F,M,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the metric or function-space source row on which admissible configurations are inspected.
\item $K$ is either a $\CompactMetricUp$ finite compactness-window row or a finite compact sublevel row displaying the compact part of the admissible surface.
\item $L$ is the $\LowerSemicontinuousUp$ epigraph or liminf row for the inspected functional.
\item $S$ is the coercive sublevel ledger, listing the displayed level, admissible window, and compact sublevel dependency actually used by the packet.
\item $F$ is the finite functional-value window ledger for the minimizing comparison.
\item $M$ is the minimizer-read row, exposed only after $K,L,S,F$ have all been displayed.
\item $Q$ is the $\RegSeqRatUp$ readback row for the rational value comparisons attached to $F$ and $M$.
\item $E$ is the terminal $\RealUp$ value seal row for the displayed minimum value.
\item $H$ records componentwise $\hsame$ transport for the source, compact sublevel, lower-semicontinuity, coercive ledger, value window, minimizer read, readback, and seal rows.
\item $C$ records displayed $\Cont$ replay from a minimizer request through the compact sublevel and lower-semicontinuity route.
\item $P$ is the $\Pkg$ provenance over $\DirectMethodMinimizerUp$, $\CompactMetricUp$, $\LowerSemicontinuousUp$, $\GammaConvergenceUp$, $\EulerLagrangeUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DirectMethodMinimizerUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,K,L,S,F,M,Q,E,H,C,P,N$. It has no coordinate for a host minimizer choice, arbitrary open-cover compactness, selected subsequence extraction, quotient equality of functionals, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[DirectMethodMinimizer NameCert obligations]
\label{thm:direct-method-minimizer-namecert-obligations}
For every accepted $\DirectMethodMinimizerUp$ carrier $D=(X,K,L,S,F,M,Q,E,H,C,P,N)$, the local $\NameCert_{\DirectMethodMinimizerUp}$ surface consists exactly of carrier admission for the source row, compact sublevel row, lower-semicontinuity row, coercive sublevel ledger, functional-value windows, minimizer-read row, regular rational readback, terminal real-value seal, componentwise transport, displayed replay, provenance, and local naming.

Every minimizer-facing consumer must therefore read the finite route
$$
\CompactMetricUp,\ \LowerSemicontinuousUp,\ \RegSeqRatUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert
$$
through the rows $K,L,S,F,M,Q,E,H,C,P,N$. The exported route is a NameCert boundary for a displayed direct-method minimum, not a host selection principle, quotient functional equality, selected subsequence theorem, or ambient completeness result.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:direct-method-minimizer-carrier}. The analytic rows are $X,K,L,S,F,M,Q,E$, and the structural rows are $H,C,P,N$.

Carrier admission first exposes the admissible source row $X$ and the compact sublevel row $K$. The coercive ledger $S$ is read against the same compact window, so the packet has no separate path to open-cover compactness or a selected subsequence.

The functional-value and lower-bound data are then read through $L,F$. Lower semicontinuity supplies only the displayed epigraph or liminf row, and the value windows supply only the comparisons listed in the packet.

Finally the minimizer-read row $M$ is exposed through $Q$ and sealed by $E$. Transport, replay, provenance, and local naming preserve that same displayed route, so no coordinate can add a host minimizer choice, quotient functional equality, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[DirectMethodMinimizer compact-sublevel route]
\label{thm:direct-method-minimizer-compact-sublevel-route}
Every minimizer read exported by an accepted $\DirectMethodMinimizerUp$ packet factors through the displayed route
$$
X,K,S \longmapsto L,F \longmapsto M,Q,E,H,C,P,N .
$$
In particular, the read is public only after the compact sublevel row and lower-semicontinuity row have both been displayed. It does not factor through host choice of a minimizer, ambient completeness of $\RealUp$, quotient equality of functionals, selected recovery sequences from $\GammaConvergenceUp$, or an $\EulerLagrangeUp$ necessary-condition consumer.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:direct-method-minimizer-namecert-obligations}. They require any minimizer-facing consumer to expose the compact sublevel row $K$ and coercive ledger $S$ before the minimizer-read coordinate $M$ can be inspected.

Next read the lower-semicontinuity row $L$ and the functional-value windows $F$. These rows provide the direct-method comparison surface: compactness supplies the admissible sublevel boundary, while lower semicontinuity supplies the displayed lower-bound handoff.

The readback rows $Q,E$ then present only the rational comparison and terminal real-value seal attached to that finite route. The structural rows $H,C,P,N$ transport, replay, source, and name the same packet.

Thus the public minimizer read factors through compact sublevel and lower-semicontinuity data. Gamma-convergence and Euler--Lagrange packets may consume or compare the result, but they do not supply the existence route inside this carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\DirectMethodMinimizerUp$ consumer that reaches a minimizer-read row or a terminal RealUp value seal without first reading the compact sublevel row, LowerSemicontinuous row, coercive sublevel ledger, RegSeqRat readback, transport, replay, provenance, and local NameCert row refutes the carrier surface.}

\independenceWitness{compactmetric, lowersemicontinuous, gamma-convergence, eulerlagrange, regseqrat, real: $\DirectMethodMinimizerUp$ is not bijective to any listed sibling because it jointly binds compact sublevel data, lower-semicontinuity rows, coercive ledgers, value windows, minimizer reads, rational readback, terminal real seals, transport, replay, provenance, and local naming in one packet.}

\closureat{DirectMethodMinimizerUp}{seedStr}

\begin{closurestatus}{\DirectMethodMinimizerUp}
  \theoryclosure{\seedClosure}
  \constructivestory{A $\DirectMethodMinimizerUp$ packet is generated from source rows, compact sublevel or CompactMetric rows, LowerSemicontinuous epigraph rows, coercive sublevel ledgers, functional value windows, minimizer-read rows, RegSeqRat readback, RealUp value seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \scopeclosed{\autoref{def:direct-method-minimizer-carrier}, \autoref{thm:direct-method-minimizer-namecert-obligations}, and \autoref{thm:direct-method-minimizer-compact-sublevel-route} seed the finite direct-method minimizer certificate through compact sublevel, lower-semicontinuity, regular rational readback, real-value sealing, transport, replay, provenance, and local naming rows.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.DirectMethodMinimizerUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host minimizer selection, selected subsequence extraction, quotient equality of functionals, open-cover compactness, countable choice, Euler--Lagrange necessary conditions, Gamma-convergence recovery selection, ambient RealUp completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact sublevel exposure, lower-semicontinuity exposure, coercive ledger exactness, value-window readback, minimizer-read sealing, transport, replay, provenance, and local naming for BEDC.Derived.DirectMethodMinimizerUp.}
\end{closurestatus}
\leanstmt{BEDC.Derived.DirectMethodMinimizerUp}
