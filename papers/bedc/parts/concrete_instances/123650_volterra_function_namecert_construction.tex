\chapter{A Concrete Naming Certificate for $\VolterraFunctionUp$}
\label{ch:concrete-instances-volterra-function-namecert}
\origin{human}

Following Volterra and the classical real-analysis treatment of differentiability and dense oscillation, the $\VolterraFunctionUp$ packet records the finite NameCert surface for a real-valued function whose displayed real-name continuity data and displayed dense oscillation data are kept as separate rows. It sits after the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the singular monotone comparison of \autoref{ch:concrete-instances-cantor-function-namecert}, the closed fat-set sibling of \autoref{ch:concrete-instances-smith-volterra-cantor-namecert}, and the epigraph-facing lower-semicontinuity route of \autoref{ch:concrete-instances-lowersemicontinuous-namecert}. The packet names a traditional Volterra-function interface as finite BEDC data; it does not assert host function equality, a completed real-function space, ambient $\RealUp$ completeness, or a selected analytic representative.

\begin{definition}[Volterra function carrier]
\label{def:volterra-function-carrier}
A $\VolterraFunctionUp$ carrier is a finite $\BHist$ packet
$$
V=(R,C,S,D,O,L,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ name seal row used by the source and target real readings.
\item $C$ is the $\ContinuousMapUp$ row exposing the displayed continuity modulus or local continuity route.
\item $S$ is the $\SmithVolterraCantorUp$ support row naming the closed fat-set boundary on which the oscillation ledger is inspected.
\item $D$ is the dense-window row, recording the finite observation windows that witness dense oscillation requests.
\item $O$ is the oscillation-boundary ledger, recording the lower and upper local oscillation readings attached to the displayed windows.
\item $L$ is the $\LowerSemicontinuousUp$ comparison row used when the oscillation envelope is read epigraphically.
\item $H$ records componentwise $\hsame$ transport for $R,C,S,D,O,L,K,P,N$.
\item $K$ records displayed $\Cont$ replay from a Volterra-function request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\VolterraFunctionUp$, $\RealUp$, $\ContinuousMapUp$, $\CantorFunctionUp$, $\SmithVolterraCantorUp$, $\LowerSemicontinuousUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\VolterraFunctionUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $R,C,S,D,O,L,H,K,P,N$. It has no coordinate for host equality of functions, a selected pointwise representative, ambient real completeness, or a theorem that the displayed oscillation ledger exhausts classical differentiability.
\end{definition}

\begin{theorem}[Volterra function NameCert obligations]
\label{thm:volterra-function-namecert-obligations}
For every accepted $\VolterraFunctionUp$ carrier
$$
V=(R,C,S,D,O,L,H,K,P,N),
$$
the local $\NameCert_{\VolterraFunctionUp}$ surface consists exactly of real-name sealing through $R$, continuous-map exposure through $C$, Smith-Volterra-Cantor support exposure through $S$, dense-window exposure through $D$, oscillation-boundary exposure through $O$, lower-semicontinuous envelope comparison through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $K$, $\Pkg$ provenance through $P$, and local naming through $N$. Every consumer of the Volterra-function packet must read the continuity row and the dense oscillation row as distinct certificate coordinates.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:volterra-function-carrier}. The rows $R,C,S,D,O,L,H,K,P,N$ are the complete visible packet.

The continuity read is $C$ and the dense-window read is $D$. Since these rows are separate coordinates, a consumer cannot replace the oscillation ledger by continuity alone, nor can it read dense oscillation as a host-function equality claim.

The support and envelope rows $S,O,L$ attach the oscillation request to the Smith-Volterra-Cantor boundary and to the lower-semicontinuous comparison route. The real-name row $R$ seals only the displayed values used by those rows.

Finally $H,K,P,N$ transport, replay, source, and name the same finite packet. No carrier coordinate supplies host equality of functions, a selected representative, ambient completeness, or a completed real-function space, so the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Volterra function oscillation boundary]
\label{thm:volterra-function-oscillation-boundary}
Every accepted $\VolterraFunctionUp$ packet exposes its dense oscillation boundary only after the route
$$
\RealUp,\ \ContinuousMapUp,\ \SmithVolterraCantorUp \longmapsto D,O,L
$$
has displayed real-name seals, continuity data, the closed fat-set support row, dense observation windows, oscillation-boundary rows, lower-semicontinuous comparison, transport, replay, provenance, and local naming. The route separates classical Volterra-style oscillation evidence from the continuous-map row and exports no host function equality, selected pointwise representative, completed function space, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:volterra-function-namecert-obligations}. They force a consumer to read $R,C,S,D,O,L$ before any oscillation-boundary conclusion is available.

The row $C$ supplies the continuous-map exposure, while $D$ and $O$ supply dense windows and local oscillation readings. Their separation is the certificate boundary: preserving $C$ alone does not preserve the Volterra oscillation packet, and preserving $D,O$ alone does not give a host continuous function.

The rows $S$ and $L$ keep the boundary anchored to the Smith-Volterra-Cantor support and the lower-semicontinuous comparison route. Transport, replay, provenance, and local naming through $H,K,P,N$ preserve the same finite rows.

Since the carrier has no coordinate for host function equality, selected representatives, completed function spaces, or ambient completeness, the oscillation boundary remains a displayed finite NameCert route.
\end{proof}

\closureat{VolterraFunctionUp}{seedStr}

\begin{closurestatus}{\VolterraFunctionUp}
  \constructivestory{A $\VolterraFunctionUp$ packet is generated from RealUp seals, ContinuousMap rows, SmithVolterraCantor support rows, dense observation windows, oscillation-boundary rows, LowerSemicontinuous comparison, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:volterra-function-carrier}, \autoref{thm:volterra-function-namecert-obligations}, and \autoref{thm:volterra-function-oscillation-boundary} seed the finite Volterra-function route separating continuity data from dense oscillation data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, host equality of functions, a selected analytic representative, completed function-space topology, classical differentiability exhaustion, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, real-name sealing, continuous-map exposure, support exposure, dense-window exactness, oscillation-boundary exactness, lower-semicontinuous comparison, transport, replay, provenance, and local naming for BEDC.Derived.VolterraFunctionUp.}
\end{closurestatus}
