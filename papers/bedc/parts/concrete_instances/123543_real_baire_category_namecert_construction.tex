\chapter{A Concrete Naming Certificate for $\RealBaireCategoryUp$}
\label{ch:concrete-instances-real-baire-category-namecert}
\origin{human}

Following Baire and Bishop, the $\RealBaireCategoryUp$ packet records the real-valued complete-metric Baire category route as finite BEDC NameCert data. It sits over the terminal real-name seal of $\RealUp$ in \autoref{ch:concrete-instances-real-namecert}, the regular-rational readback of $\RegSeqRatUp$ in \autoref{ch:concrete-instances-regseqrat-namecert}, the finite observation windows of $\StreamNameUp$ in \autoref{ch:concrete-instances-streamname-namecert}, the metric carrier of $\MetricSpaceUp$ in \autoref{ch:concrete-instances-metric-namecert}, the Cauchy service of $\CompleteMetricUp$ in \autoref{ch:concrete-instances-completemetric-namecert}, and the topology-facing neighbourhood rows of $\TopologyUp$ in \autoref{ch:concrete-instances-topology-namecert}. The packet names the L10 neighbour of the complete-metric Baire route without asserting classical choice, a host intersection point, an open-cover compactness theorem, or ambient completeness beyond the displayed rows.

\begin{definition}[RealBaireCategory carrier]
\label{def:real-baire-category-carrier}
A $\RealBaireCategoryUp$ carrier is a finite $\BHist$ packet
$$
Q=(E,R,S,M,K,T,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\RealUp$ terminal seal reached only after the dense-open schedule, metric comparisons, stream windows, and regular-rational readback have been displayed.
\item $R$ is the $\RegSeqRatUp$ row reading real-valued centers, radii, tolerances, and oscillation bounds from regular rational data.
\item $S$ is the $\StreamNameUp$ finite-window row through which the dense-open schedule, centers, radii, and requested tolerances are inspected.
\item $M$ is the $\MetricSpaceUp$ carrier row naming the distance, ball classifier, and comparison data used by the Baire route.
\item $K$ is the $\CompleteMetricUp$ Cauchy-service row carrying the displayed thread and limit-service handoff.
\item $T$ is the $\TopologyUp$ neighbourhood row exposing only the open and dense reads consumed by the schedule.
\item $D$ is the dense-open ledger, listing the finite schedule prefix, predecessor-ball refinements, successor-ball witnesses, and requested indices.
\item $H$ records componentwise $\hsame$ transport for the real seal, rational readback, stream windows, metric carrier, complete-metric service, topology rows, dense-open ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same real, rational, stream, metric, complete-metric, topology, and dense-open route.
\item $P$ is the $\Pkg$ provenance over $\RealBaireCategoryUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, $\TopologyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealBaireCategoryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,R,S,M,K,T,D,H,C,P,N$. It has no coordinate for a classical choice function, a selected generic point, quotient real equality, open-cover compactness, an ambient theorem over all complete metric spaces, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[RealBaireCategory NameCert obligations]
\label{thm:real-baire-category-namecert-obligations}
For every accepted $\RealBaireCategoryUp$ carrier
$$
Q=(E,R,S,M,K,T,D,H,C,P,N),
$$
the local $\NameCert_{\RealBaireCategoryUp}$ surface exposes exactly carrier admission for the terminal real seal, regular-rational readback, finite stream windows, metric carrier, complete-metric Cauchy service, topology-facing neighbourhood rows, dense-open ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every real-valued Baire-category read must pass through those rows before using the terminal real seal, and no obligation is stored outside them.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:real-baire-category-carrier}. Its analytic rows are exactly $E,R,S,M,K,T,D$, followed by the structural rows $H,C,P,N$.

The dense-open request is read through $T$ and $D$. The topology row exposes the open and dense neighbourhood data, while the dense-open ledger lists only the finite schedule prefix, predecessor-ball refinements, successor witnesses, and requested indices.

The complete-metric part is then forced through $M,K,S,R$. The metric row supplies ball and distance comparisons, the complete-metric row supplies the displayed Cauchy service, the stream row supplies the inspected windows, and the regular-rational row reads centers, radii, and tolerances before the terminal seal can be inspected.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same route. Since the carrier has no coordinate for choice, a selected generic point, quotient real equality, compactness, an ambient theorem over all complete metric spaces, Lean verification, or bridge checking, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[RealBaireCategory complete-metric route]
\label{thm:real-baire-category-complete-metric-route}
Every downstream real-valued Baire-category consumer of an accepted $\RealBaireCategoryUp$ packet must factor through the displayed L10 route
$$
\TopologyUp \longmapsto \MetricSpaceUp \longmapsto \CompleteMetricUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
together with the dense-open ledger and the structural rows $\BHist,\hsame,\Cont,\Pkg,\NameCert$. The route exports finite complete-metric Baire data for real-valued reads, not a classical choice selector, host intersection object, selected generic point, quotient real equality, open-cover compactness theorem, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:real-baire-category-namecert-obligations}. They force any consumer to expose the topology row $T$, dense-open ledger $D$, metric row $M$, complete-metric row $K$, stream windows $S$, regular-rational readback $R$, and real seal $E$ inside the same packet.

The route begins with topology and metric data rather than with an external intersection. The row $T$ supplies only displayed neighbourhood reads, $D$ records the finite dense-open prefix, and $M$ supplies the ball and distance comparisons used by the successor refinements.

The complete-metric handoff is carried by $K$ and inspected through $S$ and $R$. Thus every center, radius, tolerance, and Cauchy-service read is visible before the terminal real seal $E$ is reached.

The structural rows $H,C,P,N$ preserve the route under transport, replay it as displayed continuation data, record its provenance, and name the local certificate. Coordinate exhaustion then rules out the excluded choice, host intersection, selected point, quotient, compactness, and ambient completeness objects.
\end{proof}

\closureat{RealBaireCategoryUp}{seedStr}

\begin{closurestatus}{\RealBaireCategoryUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-baire-category-carrier}, \autoref{thm:real-baire-category-namecert-obligations}, and \autoref{thm:real-baire-category-complete-metric-route} seed the real-valued complete-metric Baire category route over topology, metric, complete-metric, stream, regular-rational, real, dense-open, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close classical choice, host intersections, selected generic points, quotient real equality, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, topology-row exposure, dense-open ledger exactness, metric-row compatibility, complete-metric Cauchy-service exactness, finite-window readback, regular-rational readback, real sealing, and nonescape for BEDC.Derived.RealBaireCategoryUp.}
  \constructivestory{A $\RealBaireCategoryUp$ packet is generated from topology-facing neighbourhood rows, a dense-open ledger, a MetricSpace carrier row, a CompleteMetric Cauchy-service row, StreamName finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
