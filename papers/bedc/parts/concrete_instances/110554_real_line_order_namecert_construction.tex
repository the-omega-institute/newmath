\chapter{A Concrete Naming Certificate for $\RealLineOrderUp$}
\label{ch:concrete-instances-real-line-order-namecert}
\origin{human}

Following Bishop and Bridges, the $\RealLineOrderUp$ packet records the constructive real-line order and apartness boundary visible to finite BEDC NameCert consumers. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, the rational Cauchy completion route of \autoref{ch:concrete-instances-rational-cauchy-completion-namecert}, and the Bishop regular Cauchy limit seal of \autoref{ch:concrete-instances-bishop-regular-cauchy-limit-seal-namecert}. The packet gives L10 consumers a located comparison interface before any real-order algebra can read it; it does not import trichotomy, quotient equality, selected representatives, host real equality, or ambient completeness of $\RealUp$.

\begin{definition}[Real line order carrier]
\label{def:real-line-order-carrier}
A $\RealLineOrderUp$ carrier is a finite $\BHist$ packet
$$
O=(D,S,R,L,A,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the positive dyadic radius used by every finite comparison request.
\item $S$ is the $\StreamNameUp$ window row exposing only the finite stream observations inspected at that tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row translating the inspected stream windows into regular rational comparison data.
\item $L$ is the located comparison row, recording the finite verdict surface for $x<y$ as a witnessed positive gap at the displayed tolerance, an unresolved window requiring a smaller displayed tolerance, or a symmetric failure of the current request.
\item $A$ is the apartness row, recording positive-gap readback for $x\# y$ only through $D,S,R,L$.
\item $E$ is the terminal $\RealUp$ seal row reached from the same displayed stream and regular-rational data, not from host real equality.
\item $H$ records componentwise $\hsame$ transport for dyadic tolerance, stream windows, regular-rational readback, located comparison, apartness, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite order request through $D,S,R,L,A,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealLineOrderUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealLineOrderUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,S,R,L,A,E,H,C,P,N$. It has no coordinate for trichotomy, quotient equality, selected representatives, host real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Real line order NameCert obligations]
\label{thm:real-line-order-namecert-obligations}
For every accepted $\RealLineOrderUp$ carrier
$$
O=(D,S,R,L,A,E,H,C,P,N),
$$
the local $\NameCert_{\RealLineOrderUp}$ surface consists exactly of carrier admission for the dyadic tolerance row, stream-window row, regular-rational readback row, located comparison row, apartness row, $\RealUp$ seal row, componentwise transport row, replay row, provenance row, and local naming row.

The obligation surface requires finite tolerance visibility through $D$, window visibility through $S$, regular-rational comparison readback through $R$, located order exposure through $L$, apartness exposure through $A$, real-seal nonescape through $E$, transport and replay stability through $H$ and $C$, provenance through $P$, and local naming through $N$. It explicitly refuses trichotomy, quotient equality, selected representatives, host real equality, and ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:real-line-order-carrier}. The only analytic rows are the dyadic tolerance $D$, the finite stream windows $S$, the regular-rational readback $R$, the located comparison row $L$, the apartness row $A$, and the terminal real seal $E$.

The comparison step is located rather than trichotomic. A finite request may display a positive gap, request a smaller displayed tolerance, or fail symmetrically at the current window; none of these outcomes supplies a global three-way case split.

The equality-sensitive routes are absent. The readback row $R$ is regular-rational data over the displayed windows, and the seal row $E$ is reached only after $D,S,R,L,A$ have been inspected. Thus the packet refuses quotient equality, selected representatives, host real equality, and ambient completeness of $\RealUp$.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the excluded principles are not coordinates of $O$, no local NameCert obligation can demand them.
\end{proof}

\begin{theorem}[Real line order apartness readback]
\label{thm:real-line-order-apartness-readback}
Every apartness read exported by an accepted $\RealLineOrderUp$ carrier factors through the finite route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp
\longmapsto
\RealLineOrderUp
\longmapsto
\RealUp .
$$
The apartness row $A$ may expose only a positive-gap witness already visible through $D,S,R,L$. It exports no trichotomy datum, quotient equality, selected representative, host real equality, or ambient completeness theorem for $\RealUp$.
\end{theorem}
\begin{proof}
Start with an apartness request. The request must first choose a displayed positive dyadic tolerance in $D$ and inspect the finite windows in $S$.

The regular-rational readback $R$ is then the only comparison source. The located comparison row $L$ reads this finite data and either displays a positive gap at the requested tolerance or leaves the request unresolved for a smaller displayed tolerance.

The apartness row $A$ can cite only the positive-gap branch already exposed by $L$. It refuses trichotomy, quotient equality, selected representatives, host real equality, and ambient completeness of $\RealUp$ because those objects are not carrier rows.

The terminal seal $E$ preserves the same finite route into $\RealUp$. Transport and replay through $H$ and $C$ keep the route stable, while $P$ and $N$ only record provenance and local naming.
\end{proof}

\begin{theorem}[Real line order L10 handoff]
\label{thm:real-line-order-l10-handoff}
Every L10 consumer of an accepted $\RealLineOrderUp$ carrier must read the dyadic tolerance, stream-window, regular-rational readback, located comparison, apartness, and $\RealUp$ seal rows in the order
$$
D,S,R,L,A,E .
$$
Consequently the handoff supplies a finite non-quotient comparison boundary for later real algebra and order packets, while refusing trichotomy, quotient equality, selected representatives, host real equality, and ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
An L10 order request begins with finite tolerance data. The row $D$ fixes the displayed dyadic radius, and $S$ opens only the stream windows inspected at that radius.

The row $R$ repacks those windows as regular-rational readback. The located comparison row $L$ can then answer only with the finite comparison surface generated from $D,S,R$.

Apartness is downstream of located comparison. The row $A$ may expose a positive gap only when $L$ has already displayed one, and $E$ seals the same finite route as a $\RealUp$-facing endpoint.

Because no earlier or later row stores trichotomy, quotient equality, selected representatives, host real equality, or ambient completeness of $\RealUp$, the L10 handoff cannot export those principles to later consumers.
\end{proof}

\falsifiablePrediction{Every accepted $\RealLineOrderUp$ read must expose dyadic tolerance, StreamName windows, RegSeqRat readback, located comparison, apartness, RealUp sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before an L10 order consumer can read a comparison boundary.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, rational-cauchy-completion, bishop-regular-cauchy-limit-seal: $\RealLineOrderUp$ is not bijective to any listed sibling because it binds located comparison and apartness rows to the finite readback route before the RealUp seal is available.}

\closureat{RealLineOrderUp}{seedStr}

\begin{closurestatus}{\RealLineOrderUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-line-order-carrier}, \autoref{thm:real-line-order-namecert-obligations}, \autoref{thm:real-line-order-apartness-readback}, and \autoref{thm:real-line-order-l10-handoff} seed the finite non-quotient order and apartness boundary for displayed RealUp names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, quotient equality, selected representatives, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite tolerance visibility, stream-window visibility, regular-rational readback, located comparison, apartness readback, RealUp seal handoff, componentwise transport and replay stability, provenance exhaustion, and local naming for BEDC.Derived.RealLineOrderUp.}
  \constructivestory{A $\RealLineOrderUp$ packet is generated from a DyadicRatCoreUp tolerance row, StreamName finite windows, RegSeqRat readback, located comparison, apartness, a RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
