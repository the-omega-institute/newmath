\chapter{A Concrete Naming Certificate for $\FiniteBrunnMinkowskiGridUp$}
\label{ch:concrete-instances-finite-brunn-minkowski-grid-namecert}
\origin{human}

Following Brunn and Minkowski on convex-body volume comparison, the $\FiniteBrunnMinkowskiGridUp$ packet records the finite dyadic-grid surface on which a Brunn--Minkowski style read can be inspected before any host measure theory or arbitrary compact-convex-body theorem is invoked. It sits beside the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the compact source of \autoref{ch:concrete-instances-compact-namecert}, the convex-set row of \autoref{ch:concrete-instances-convexset-namecert}, the polytope carrier of \autoref{ch:concrete-instances-polytope-namecert}, and the metric-distance interface of \autoref{ch:concrete-instances-metric-namecert}. The packet names a finite grid certificate for displayed convexity and volume-proxy comparison; it is not general measure theory, not Lebesgue volume, not arbitrary compact convex bodies, and not a quotient equality of subsets.

\begin{definition}[Finite Brunn--Minkowski grid carrier]
\label{def:finite-brunn-minkowski-grid-carrier}
A $\FiniteBrunnMinkowskiGridUp$ carrier is a finite $\BHist$ packet
$$
G=(K,A,B,S,L,V,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the finite dyadic grid row. It lists the inspected dyadic coordinate cells, their metric adjacency data, and the endpoint comparisons read through $\MetricUp$ and $\RealUp$.
\item $A$ and $B$ are the two displayed convex-grid subsets. Each row records membership decisions only for cells in $K$, with the convexity checks routed through $\ConvexSetUp$ and the finite polytope faces routed through $\PolytopeUp$.
\item $S$ is the grid Minkowski-sum classifier. For each displayed cell of $K$, it records whether that cell is admitted by a visible pair of cells from $A$ and $B$ under the finite addition table of the grid.
\item $L$ is the compact bounding row, naming the finite bounding window supplied by $\CompactUp$ before any grid count is consumed.
\item $V$ is the finite cardinal and volume-proxy ledger. It stores the displayed cell counts for $A$, $B$, and $S$, the common cell-volume scale, and the dyadic comparison rows used by the requested Brunn--Minkowski read.
\item $W$ is the public comparison row. It exposes only the finite grid inequality request generated from $K,A,B,S,L,V$.
\item $H$ records componentwise $\hsame$ transport for the grid, subset rows, sum classifier, compact bound, volume-proxy ledger, comparison row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from a requested finite-grid comparison through $K,A,B,S,L,V,W$.
\item $P$ is the $\Pkg$ provenance over $\FiniteBrunnMinkowskiGridUp$, $\RealUp$, $\CompactUp$, $\ConvexSetUp$, $\PolytopeUp$, $\MetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteBrunnMinkowskiGridUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,A,B,S,L,V,W,H,C,P,N$. It has no coordinate for Lebesgue measure, arbitrary compact convex bodies, host subset equality, quotient completion, selected limits, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Finite Brunn--Minkowski grid NameCert obligations]
\label{thm:finite-brunn-minkowski-grid-namecert-obligations}
For every accepted $\FiniteBrunnMinkowskiGridUp$ carrier
$$
G=(K,A,B,S,L,V,W,H,C,P,N),
$$
the local $\NameCert_{\FiniteBrunnMinkowskiGridUp}$ surface consists exactly of finite dyadic-grid admission, displayed convex-subset admission for $A$ and $B$, grid Minkowski-sum classification through $S$, compact bounding through $L$, finite cardinal and volume-proxy accounting through $V$, public comparison exposure through $W$, componentwise transport, displayed replay, package provenance, local naming, and non-escape from the displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-brunn-minkowski-grid-carrier}. The geometric rows are exactly the grid row $K$, the displayed subset rows $A,B$, the grid sum classifier $S$, the compact bounding row $L$, and the finite ledger rows $V,W$.

The certificate first fixes the finite grid. Metric adjacency and real endpoint comparisons are read through $K$, while $A$ and $B$ expose only those convex and polytope checks whose cells occur in that displayed grid.

The sum classifier is then determined inside the same finite table. The row $S$ stores the visible pair witnesses for admitted sum cells, and $L$ keeps the comparison inside the displayed compact bounding window.

Finally the ledger $V$ records cardinal counts and the common dyadic cell-volume scale before $W$ exposes the public comparison row. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite surface, so no measure-theoretic, quotient, choice, selected-limit, or ambient-completeness datum is exported.
\end{proof}

\begin{theorem}[Finite Brunn--Minkowski grid comparison route]
\label{thm:finite-brunn-minkowski-grid-comparison-route}
Every public comparison read exported by an accepted $\FiniteBrunnMinkowskiGridUp$ packet factors through the finite route
$$
K,A,B \longmapsto S \longmapsto L,V \longmapsto W \longmapsto H,C,P,N .
$$
Thus the consumer receives a dyadic grid, two displayed convex-grid subsets, a finite Minkowski-sum classifier, a compact bounding window, a finite cardinal and volume-proxy ledger, and structural transport/replay rows before it reads the comparison boundary. It receives no general measure theory, no arbitrary compact-convex-body volume, no host subset equality, and no bridge-checked standard theorem.
\end{theorem}
\begin{proof}
Begin with an accepted packet $G=(K,A,B,S,L,V,W,H,C,P,N)$. The carrier lists no comparison row before the grid, subset, and finite sum-classifier rows have been displayed.

The compact and volume-proxy rows are also forced. The bounding row $L$ supplies the finite compact window, while $V$ supplies the only cardinal counts, cell-volume scale, and dyadic comparison rows available to the packet.

The public row $W$ is therefore downstream of $K,A,B,S,L,V$. Componentwise $\hsame$ transport and displayed $\Cont$ replay carry exactly those rows through $H$ and $C$, and $P,N$ only source and name the same packet.

Since no carrier coordinate stores Lebesgue measure, arbitrary convex-body volume, quotient subset equality, selected limits, choice, or ambient completeness, the exported comparison cannot use such data.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteBrunnMinkowskiGridUp$ comparison read displays a finite dyadic grid, two convex-grid subset rows, a grid Minkowski-sum classifier, a compact bounding row, a finite cardinal and volume-proxy ledger, a public comparison row, componentwise hsame transport, Cont replay, Pkg provenance, and local naming before it is consumed.}

\independenceWitness{real, compact, convexset, polytope, metric: $\FiniteBrunnMinkowskiGridUp$ is not bijective to any listed sibling because it jointly carries a finite dyadic grid, two convex-grid subset rows, a Minkowski-sum classifier, compact bounding data, cardinal and volume-proxy ledgers, transport, replay, provenance, and local naming.}

\closureat{FiniteBrunnMinkowskiGridUp}{seedStr}

\begin{closurestatus}{\FiniteBrunnMinkowskiGridUp}
  \constructivestory{A $\FiniteBrunnMinkowskiGridUp$ packet is generated from a finite dyadic grid, two displayed convex-grid subset rows, a finite Minkowski-sum classifier, compact bounding data, finite cardinal and volume-proxy ledgers, a public comparison row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-brunn-minkowski-grid-carrier}, \autoref{thm:finite-brunn-minkowski-grid-namecert-obligations}, and \autoref{thm:finite-brunn-minkowski-grid-comparison-route} seed the finite dyadic-grid convexity and volume-proxy certificate over $\RealUp$, $\CompactUp$, $\ConvexSetUp$, $\PolytopeUp$, and $\MetricUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close general measure theory, Lebesgue volume, arbitrary compact convex bodies, host subset equality, quotient completion, selected limits, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, grid exactness, convex-grid membership routing, Minkowski-sum classifier soundness, compact-bound routing, finite ledger non-escape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteBrunnMinkowskiGridUp.}
\end{closurestatus}
