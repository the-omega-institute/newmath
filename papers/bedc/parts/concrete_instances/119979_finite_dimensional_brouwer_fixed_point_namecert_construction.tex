\chapter{A Concrete Naming Certificate for $\FiniteDimensionalBrouwerFixedPointUp$}
\label{ch:concrete-instances-finite-dimensional-brouwer-fixed-point-namecert}
\origin{human}

Following Brouwer and Sperner, the $\FiniteDimensionalBrouwerFixedPointUp$ packet records the finite-dimensional fixed-point route in which a compact convex carrier, a continuous self-map, and a finite mesh certificate are displayed before any fixed-point-facing read is allowed. It consumes the Brouwer packet of \autoref{ch:concrete-instances-brouwer-fixed-point-namecert}, the finite labelled-simplex route of \autoref{ch:concrete-instances-sperner-lemma-namecert}, the metric approximation route of \autoref{ch:concrete-instances-brouwer-fixed-point-metric-namecert}, the compact finite-net discipline of \autoref{ch:concrete-instances-compactmetric-namecert}, the convex interval and cell discipline of \autoref{ch:concrete-instances-convex-compact-interval-namecert}, the self-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite-dimensional mesh route itself; it does not choose an exact host fixed point or assert an unrestricted classical theorem.

\begin{definition}[Finite-dimensional Brouwer fixed-point carrier]
\label{def:finite-dimensional-brouwer-fixed-point-carrier}
A $\FiniteDimensionalBrouwerFixedPointUp$ carrier is a finite $\BHist$ packet
$$
F=(K,V,M,S,L,A,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row, read through $\CompactMetricUp$ data and recording the finite radius, cover, and total-boundedness surface used by the argument.
\item $V$ is the finite-dimensional convex-cell row, recording the listed coordinates, face incidences, convex-combination cells, and boundary faces inspected by the self-map route.
\item $M$ is the $\ContinuousMapUp$ self-map row, exposing only the displayed modulus and finite image data used on the mesh.
\item $S$ is the $\SpernerLemmaUp$ handoff row, carrying the boundary-admissible finite labelling and the fully labelled simplex witness.
\item $L$ is the $\BrouwerFixedPointUp$ mesh route row, tying the Sperner witness to the dyadic subdivision and approximate fixed-simplex readback.
\item $A$ is the $\BrouwerFixedPointMetricUp$ approximation row, recording metric residual bounds, mesh scale, and finite approximation evidence.
\item $R$ is the $\RealUp$ seal row reached only after the compact, convex, continuous-map, Sperner, Brouwer, and metric approximation rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for $K,V,M,S,L,A,R$ and the structural rows.
\item $C$ records displayed $\Cont$ replay for the finite route
$$
K,V,M,S,L,A \longmapsto R,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\FiniteDimensionalBrouwerFixedPointUp$, $\BrouwerFixedPointUp$, $\SpernerLemmaUp$, $\BrouwerFixedPointMetricUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteDimensionalBrouwerFixedPointUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,V,M,S,L,A,R,H,C,P,N$. It has no coordinate for an exact host fixed point, a selected limit, open-cover compactness, quotient stream equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Finite-dimensional Brouwer fixed-point NameCert obligations]
\label{thm:finite-dimensional-brouwer-fixed-point-namecert-obligations}
For every accepted $\FiniteDimensionalBrouwerFixedPointUp$ carrier
$$
F=(K,V,M,S,L,A,R,H,C,P,N),
$$
the local $\NameCert_{\FiniteDimensionalBrouwerFixedPointUp}$ surface consists exactly of compact finite-net admission through $K$, finite-dimensional convex-cell exposure through $V$, continuous self-map access through $M$, Sperner boundary and fully labelled simplex handoff through $S$, Brouwer mesh routing through $L$, metric residual readback through $A$, terminal $\RealUp$ sealing through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-dimensional-brouwer-fixed-point-carrier}. Its accepted rows are precisely the compact finite-net row, finite-dimensional convex-cell row, continuous-map row, Sperner handoff, Brouwer mesh route, metric approximation row, Real seal, and structural transport, replay, provenance, and naming rows.

The geometric part is finite. The compact row $K$ gives only finite-net and radius data, and the convex row $V$ gives only listed cells, faces, and convex-combination checks.

The map and fixed-point-facing part is routed. The self-map row $M$ supplies the displayed modulus data used by the mesh, the Sperner row $S$ supplies the labelled-simplex certificate, the Brouwer row $L$ ties that certificate to the dyadic fixed-simplex route, and the metric row $A$ records residual bounds at the displayed mesh scale.

Finally $R,H,C,P,N$ seal and preserve the same finite packet. Since no excluded exact fixed point, selected limit, open-cover compactness principle, quotient equality, choice, trichotomy, ambient completeness, Lean verification, or bridge row occurs in the carrier, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Finite-dimensional Brouwer fixed-point Sperner mesh route]
\label{thm:finite-dimensional-brouwer-fixed-point-sperner-mesh-route}
Every fixed-point-facing read of an accepted $\FiniteDimensionalBrouwerFixedPointUp$ packet factors through the displayed finite route
$$
\CompactMetricUp,\quad \ContinuousMapUp,\quad \SpernerLemmaUp,\quad \BrouwerFixedPointUp,\quad \BrouwerFixedPointMetricUp,\quad \RealUp .
$$
Concretely, the consumer first reads the compact finite-net and finite-dimensional convex-cell rows $K,V$, then the continuous self-map row $M$, then the Sperner labelled-simplex handoff $S$, then the Brouwer mesh route $L$, then the metric residual row $A$, and only then the terminal Real seal $R$. No host fixed-point choice or ambient compactness theorem is exported by this route.
\end{theorem}
\begin{proof}
Start from an accepted carrier $F=(K,V,M,S,L,A,R,H,C,P,N)$. The rows $K,V$ display the compact finite-net and finite-dimensional convex-cell surface on which the self-map is inspected.

The self-map information is then read through $M$. A consumer cannot interpret the mesh labels before this row is displayed, because the boundary labels and residual comparisons are induced by the carried continuous-map data.

The combinatorial handoff is supplied by $S$ and $L$. The Sperner row exposes the fully labelled simplex, while the Brouwer row records the finite dyadic mesh route that turns this handoff into approximate fixed-simplex evidence.

The metric row $A$ and Real seal $R$ are downstream. They record residual bounds and real-name nonescape only after the compact, convex, map, Sperner, and Brouwer rows have already been displayed. Transport, replay, provenance, and local naming preserve the same route, so the packet exports no host fixed-point selection or unrestricted compactness principle.
\end{proof}

\falsifiablePrediction{A $\FiniteDimensionalBrouwerFixedPointUp$ consumer is refuted if it reaches a fixed-point-facing read without displaying compact finite-net data, finite-dimensional convex-cell rows, continuous-map modulus data, a Sperner labelled-simplex handoff, a Brouwer mesh route, metric residual bounds, Real sealing, transport, replay, provenance, and local NameCert evidence.}

\independenceWitness{brouwer, sperner, metric, compact, convex, continuous, real: $\FiniteDimensionalBrouwerFixedPointUp$ is not bijective to any listed sibling because it binds the finite-dimensional convex-cell route, finite mesh handoff, residual readback, and Real seal into one NameCert packet.}

\closureat{FiniteDimensionalBrouwerFixedPointUp}{seedStr}

\begin{closurestatus}{\FiniteDimensionalBrouwerFixedPointUp}
  \constructivestory{A $\FiniteDimensionalBrouwerFixedPointUp$ packet is generated from compact finite-net rows, finite-dimensional convex-cell rows, continuous self-map rows, Sperner labelled-simplex handoff rows, Brouwer mesh-route rows, Brouwer fixed-point metric approximation rows, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-dimensional-brouwer-fixed-point-carrier}, \autoref{thm:finite-dimensional-brouwer-fixed-point-namecert-obligations}, and \autoref{thm:finite-dimensional-brouwer-fixed-point-sperner-mesh-route} seed the finite-dimensional Brouwer fixed-point mesh route through compact finite-net, convex-cell, continuous-map, Sperner, Brouwer, metric approximation, Real seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close an exact host fixed-point theorem, selected limits, open-cover compactness, quotient stream equality, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net routing, finite-dimensional convex-cell routing, continuous-map access, Sperner handoff exactness, Brouwer mesh routing, metric residual readback, Real seal ordering, transport, replay, provenance, and local naming for BEDC.Derived.FiniteDimensionalBrouwerFixedPointUp.}
\end{closurestatus}
