\chapter{A Concrete Naming Certificate for $\ClosedIntervalCauchyCriterionUp$}
\label{ch:concrete-instances-closed-interval-cauchy-criterion-namecert}
\origin{human}

Following Bishop and Bridges, the $\ClosedIntervalCauchyCriterionUp$ packet names the finite closed-interval criterion by which a uniformly Cauchy family of interval-local real windows is admitted before any uniform-limit endpoint or completion-facing seal is read. It sits after the closed-interval completion route of \autoref{ch:concrete-instances-closed-interval-cauchy-completion-namecert}, uses the shared threshold surface of \autoref{ch:concrete-instances-uniformcauchycriterion-namecert}, hands off to the uniform-limit packet of \autoref{ch:concrete-instances-uniformlimit-namecert}, and keeps its finite observations inside the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ sibling surfaces of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The chapter records a criterion packet for closed-interval Cauchy data; it does not import open-cover compactness, selected limits, quotient function-space equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Closed interval Cauchy criterion carrier]
\label{def:closed-interval-cauchy-criterion-carrier}
A $\ClosedIntervalCauchyCriterionUp$ carrier is a finite $\BHist$ packet
$$
J=(A,B,I,F,W,T,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the lower closed endpoint row and $B$ is the upper closed endpoint row, both read through the same finite closed-interval discipline as the source interval.
\item $I$ is the closed interval row that records endpoint containment, interval-local request scope, and the closed-interval completion handoff consumed from $\ClosedIntervalCauchyCompletionUp$.
\item $F$ is the function-window family, recording the finite interval-local real-valued windows visible to the criterion.
\item $W$ is the $\StreamNameUp$ window row opened by $F$ for the finite family of interval requests.
\item $T$ is the uniform Cauchy threshold row, a single shared threshold surface from $\UniformCauchyCriterionUp$ for every displayed tolerance and every retained interval-local window.
\item $S$ is the criterion ledger asserting that all displayed pairs of windows beyond $T$ are compared through the same interval row $I$ and the same function-window family $F$.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from $W,T,S$ before any limit or completion-facing consumer reads the terminal row.
\item $E$ is the terminal $\RealUp$ seal row reached only after $I,F,W,T,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for endpoints, the closed-interval row, function-window rows, StreamName windows, uniform Cauchy threshold, criterion ledger, RegSeqRat readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a closed-interval Cauchy criterion request through $A,B,I,F,W,T,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\ClosedIntervalCauchyCriterionUp$, $\ClosedIntervalCauchyCompletionUp$, $\UniformCauchyCriterionUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ClosedIntervalCauchyCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,I,F,W,T,S,R,E,H,C,P,N$. The carrier has no coordinate for open-cover compactness, selected limits, quotient function-space equality, countable choice, host function equality, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.ClosedIntervalCauchyCriterionUp.ClosedIntervalCauchyCriterionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Closed interval Cauchy criterion NameCert obligations]
\label{thm:closed-interval-cauchy-criterion-namecert-obligations}
For every accepted $\ClosedIntervalCauchyCriterionUp$ carrier
$$
J=(A,B,I,F,W,T,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\ClosedIntervalCauchyCriterionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the endpoint rows, closed-interval row, function-window family, StreamName windows, uniform Cauchy threshold, criterion ledger, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming rows;
\item closed-interval anchoring, requiring every criterion read to use the displayed row $I$ obtained from the $\ClosedIntervalCauchyCompletionUp$ route rather than an ambient compact interval object;
\item function-window locality, requiring $F$ and $W$ to expose exactly the finite interval-local windows inspected by the criterion request;
\item uniform-threshold exactness, requiring the single threshold row $T$ from $\UniformCauchyCriterionUp$ to govern all retained displayed window pairs;
\item readback ordering, requiring $R$ and the terminal $\RealUp$ seal $E$ to be exposed only after $I,F,W,T,S$ have been displayed;
\item componentwise classifier transport, displayed replay, provenance, and local naming through $H,C,P,N$;
\item ledger non-escape, requiring the packet to export no open-cover compactness, selected limit, quotient function-space equality, countable-choice schedule, host function equality, or ambient $\RealUp$ completeness theorem.
\end{enumerate}
No closed-interval Cauchy criterion obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:closed-interval-cauchy-criterion-carrier}. The visible packet consists exactly of the endpoint rows $A,B$, closed-interval row $I$, function-window family $F$, StreamName windows $W$, threshold row $T$, criterion ledger $S$, readback row $R$, terminal seal $E$, and structural rows $H,C,P,N$.

Closed-interval anchoring is upstream of every criterion read. The row $I$ is the only closed-interval coordinate, and it is supplied by the $\ClosedIntervalCauchyCompletionUp$ sibling route, so no host compact interval object can replace it.

Function-window locality and uniform-threshold exactness are the middle route. The rows $F$ and $W$ expose the finite interval-local windows, while $T$ is the single shared $\UniformCauchyCriterionUp$ threshold governing all displayed window pairs recorded by $S$.

The readback order is then forced. The $\RegSeqRatUp$ row $R$ and the terminal $\RealUp$ seal $E$ occur only after the closed-interval, window, threshold, and criterion-ledger rows have been displayed.

Finally, $H,C,P,N$ transport, replay, package, and name the same finite route. Since none of the excluded objects occurs in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Closed interval Cauchy criterion uniform-window handoff]
\label{thm:closed-interval-cauchy-criterion-uniform-window-handoff}
Every $\UniformLimitUp$ or completion-facing consumer that reads an accepted $\ClosedIntervalCauchyCriterionUp$ carrier factors its request through the finite route
$$
A,B \longmapsto I \longmapsto F,W \longmapsto T,S \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Consequently the consumer receives displayed closed endpoints, the closed-interval completion row, interval-local function windows, StreamName windows, one shared UniformCauchyCriterion threshold, one criterion ledger, one RegSeqRat readback row, one RealUp seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row. The handoff supplies no open-cover compactness, selected limit, quotient function-space equality, countable choice, host function equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier $J=(A,B,I,F,W,T,S,R,E,H,C,P,N)$. The endpoint rows $A,B$ and the closed-interval row $I$ are the only interval source coordinates available to the consumer.

The finite observation route then opens through $F$ and $W$. These rows expose only interval-local function windows and their displayed $\StreamNameUp$ readings.

Uniformity is supplied before any readback. The threshold row $T$ is shared across all retained windows, and the criterion ledger $S$ records the displayed pairwise comparisons under that same threshold.

The downstream handoff reaches $R$ and then $E$. Thus a $\UniformLimitUp$ or completion-facing consumer reads the $\RegSeqRatUp$ route and the terminal $\RealUp$ seal only after the closed-interval, window, and uniform-threshold rows are in scope.

The structural rows $H,C,P,N$ preserve and name the same route. Because the carrier has no coordinate for the excluded compactness, selection, quotient, choice, host-equality, or completeness objects, every public read is exhausted by the displayed handoff.
\end{proof}

\falsifiablePrediction{Every $\ClosedIntervalCauchyCriterionUp$ read factors through displayed closed endpoints, one closed-interval completion row, interval-local function windows, StreamName windows, one UniformCauchyCriterion threshold, one criterion ledger, one RegSeqRat readback row, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{closed-interval-cauchy-completion, uniformcauchycriterion, uniformlimit, streamname, regseqrat, real: $\ClosedIntervalCauchyCriterionUp$ is not bijective to any listed sibling because it jointly carries closed-interval completion anchoring, interval-local function windows, a shared uniform Cauchy threshold, the criterion ledger, regular rational readback, and the terminal Real seal as one criterion packet.}

\closureat{ClosedIntervalCauchyCriterionUp}{seedStr}

\begin{closurestatus}{\ClosedIntervalCauchyCriterionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:closed-interval-cauchy-criterion-carrier}, \autoref{thm:closed-interval-cauchy-criterion-namecert-obligations}, and \autoref{thm:closed-interval-cauchy-criterion-uniform-window-handoff} seed the finite closed-interval Cauchy criterion route from displayed endpoints and closed-interval completion rows through interval-local function windows, StreamName windows, a shared UniformCauchyCriterion threshold, criterion ledger, RegSeqRat readback, Real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected limits, quotient function-space equality, countable choice, host function equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, closed-interval anchoring, function-window locality, uniform-threshold exactness, readback ordering, classifier transport, replay, provenance, local naming, and uniform-window handoff for BEDC.Derived.ClosedIntervalCauchyCriterionUp.}
  \constructivestory{A $\ClosedIntervalCauchyCriterionUp$ packet is generated from displayed closed endpoint rows, one closed-interval completion row, interval-local function windows, finite StreamName windows, one UniformCauchyCriterion threshold row, one criterion ledger, one RegSeqRat readback row, one terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
