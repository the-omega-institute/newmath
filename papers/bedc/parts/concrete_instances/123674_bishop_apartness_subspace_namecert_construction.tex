\chapter{A Concrete Naming Certificate for $\BishopApartnessSubspaceUp$}
\label{ch:concrete-instances-bishop-apartness-subspace-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopApartnessSubspaceUp$ packet records the finite subspace certificate by which a displayed predicate over observations inherits constructive apartness data from an ambient source. It sits after the constructive separation surface of \autoref{ch:concrete-instances-constructive-apartness-space-namecert}, the located positive-gap route of \autoref{ch:concrete-instances-located-apartness-namecert}, the separated completion boundary of \autoref{ch:concrete-instances-apartness-completion-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the subspace route without importing quotient equality, trichotomy, selected representatives, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop apartness-subspace carrier]
\label{def:bishop-apartness-subspace-carrier}
A $\BishopApartnessSubspaceUp$ carrier is a finite $\BHist$ packet
$$
S=(X,A,I,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the parent $\ConstructiveApartnessSpaceUp$ row whose observations, positive-separation data, transport, replay, provenance, and local naming remain visible.
\item $A$ is the subcarrier predicate over displayed observations of $X$.
\item $I$ is the inclusion row from the subcarrier observations into the parent carrier.
\item $T$ is inherited positive-separation transport along $I$.
\item $H$ records componentwise $\hsame$ stability for the parent row, subcarrier predicate, inclusion, inherited transport, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the parent apartness classifier through the inclusion row.
\item $P$ is the $\Pkg$ provenance over $\BishopApartnessSubspaceUp$, $\ConstructiveApartnessSpaceUp$, $\LocatedApartnessUp$, $\ApartnessCompletionUp$, $\MetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopApartnessSubspaceUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient equality of subspace points, total equality decision, trichotomy, selected representatives, countable-choice schedules, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.BishopApartnessSubspaceUp.TasteGate.BishopApartnessSubspaceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bishop apartness-subspace NameCert obligations]
\label{thm:bishop-apartness-subspace-namecert-obligations}
For every accepted $\BishopApartnessSubspaceUp$ carrier $S=(X,A,I,T,H,C,P,N)$, the local $\NameCert_{\BishopApartnessSubspaceUp}$ surface consists exactly of carrier admission, inherited classifier stability, inclusion transport, subspace separation exactness, provenance exhaustion, and ledger nonescape. Each obligation is read through the parent $\ConstructiveApartnessSpaceUp$ row, the displayed subcarrier predicate, the inclusion row, inherited positive-separation transport, componentwise $\hsame$ stability, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-apartness-subspace-carrier}. The rows $X,A,I,T,H,C,P,N$ are the whole accepted packet, so carrier admission and local naming have no source outside the displayed finite data.

Inherited classifier stability and inclusion transport are read through $I,T,H,C$. The inclusion sends subcarrier observations to the parent apartness carrier, the transport row preserves positive-separation evidence along that inclusion, and the $\hsame$ and $\Cont$ rows keep the same parent classifier visible.

Subspace separation exactness follows because the packet never creates a new apartness relation. It replays the parent constructive apartness route through the displayed subcarrier predicate and inclusion row.

Finally $P$ and $N$ exhaust provenance and ledger data. Since quotient equality, trichotomy, selected representatives, countable choice, and ambient completeness are absent from the carrier, none can be exported by a local obligation.
\end{proof}

\begin{theorem}[Bishop apartness-subspace nonescape]
\label{thm:bishop-apartness-subspace-nonescape}
No accepted $\BishopApartnessSubspaceUp$ consumer can extract quotient equality, trichotomy, selected representatives, countable choice, or ambient $\RealUp$ completeness from the packet. Every subspace separation read is routed through the displayed parent apartness row, subcarrier predicate, inclusion, inherited transport, componentwise stability, replay, provenance, and local NameCert rows.
\end{theorem}
\begin{proof}
Apply the obligation theorem \autoref{thm:bishop-apartness-subspace-namecert-obligations}. It lists all rows available to a consumer of the subspace certificate.

The parent apartness row supplies positive separation, while $A$ and $I$ only restrict and include displayed observations. The transport and replay rows preserve that same parent route; they do not add an equality classifier or a choice schedule.

The provenance and local naming rows record only the finite packet. Hence every excluded principle lacks a carrier coordinate and cannot escape from an accepted subspace read.
\end{proof}

\closureat{BishopApartnessSubspaceUp}{seedStr}

\begin{closurestatus}{\BishopApartnessSubspaceUp}
  \constructivestory{A $\BishopApartnessSubspaceUp$ packet is generated from a parent ConstructiveApartnessSpace row, a displayed subcarrier predicate, an inclusion row, inherited positive-separation transport, componentwise hsame stability, displayed Cont replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-apartness-subspace-carrier}, \autoref{thm:bishop-apartness-subspace-namecert-obligations}, and \autoref{thm:bishop-apartness-subspace-nonescape} seed the constructive apartness-subspace route over parent apartness, located apartness, apartness completion, metric, real-name, transport, replay, provenance, and NameCert rows.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.BishopApartnessSubspaceUp.TasteGate.BishopApartnessSubspaceTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient equality, trichotomy, selected representatives, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, inherited classifier stability, inclusion transport, subspace separation exactness, provenance exhaustion, and ledger nonescape for BEDC.Derived.BishopApartnessSubspaceUp.}
\end{closurestatus}
