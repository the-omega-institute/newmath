\input{parts/concrete_instances/spingroup/boundary_consumer_exactness}

\closureat{SpinGroupUp}{bridgedStr}[BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_root\_namecert\_obligation\_package]
\closureat{SpinGroupUp}{matureStr}[BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_root\_namecert\_obligation\_package]

\begin{closurestatus}{\SpinGroupUp}
  \constructivestory{A spin row is generated by taking the displayed $\CliffordUp$ unit surface, pairing it with the singleton-history $\GroupUp$ law surface, and keeping the Clifford endpoint, unit-like endpoint, multiplication and inverse transports, parity history, projection row, continuation witnesses, and provenance in one finite packet. The carrier accepts only those $\mathsf{BHist}$ rows whose Clifford, group, $\mathsf{UnaryHistory}$, $\Cont$, $\hsame$, and $\Pkg$ fields are present; a host orthogonal group, matrix realization, Clifford quotient, or topological connected-component predicate is not part of the root boundary. Two accepted spin packets classify together by componentwise $\hsame$ on the Clifford lift, group-law row, parity ledger, projection endpoint, multiplication and inverse rows, and package provenance, with finite continuation routes carrying the double-cover and conjugation readbacks. The source specification is the combined $\CliffordUp$ and $\GroupUp$ boundary, the pattern specification records unit, multiplication, inverse, even-parity, double-cover fiber, and conjugation-action rows, and the ledger keeps the Clifford-unit comparison, parity row, orthogonal-action endpoint, projection witness, and provenance visible. The five name-certificate obligations are supplied by the root obligation package: an accepted packet inhabits the carrier, componentwise $\hsame$ gives reflexive, symmetric, and transitive classifier movement, and source-scope plus ledger-exhaustion repack every transported unit, product, inverse, and fiber row inside the same boundary. A packet history $h$ lies in the signature gap of a $\SpinGroupUp$ token $p$ when the source-domain accepts its Clifford-group boundary, the signature relation pairs it with an answer spin packet and evidence ledger, and the token-introduction relation names that answer as $p$; unfolded, the unit, multiplication, inverse, parity, projection, and fiber rows are exactly those encoded by $p$. The chapter registers this Clifford-unit boundary construction under the name $\SpinGroupUp$; it introduces no full double-cover theorem, no topological connectedness proof, and no standard bridge.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The obligation surface binds the carrier, classifier, transport, unit, multiplication, inverse, and ledger rows over $\CliffordUp$, $\GroupUp$, $\mathsf{BHist}$, $\Cont$, $\hsame$, $\mathsf{UnaryHistory}$, and $\Pkg$.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_root\_namecert\_obligation\_package}
  \bridgestatus{bridgeChecked}
  \notclaimed{This boundary surface does not close the full double-cover theorem, topological connectedness, or a standard bridge.}
  \upgradepath{no further upgrade -- chapter at top of formal axis}
\end{closurestatus}
