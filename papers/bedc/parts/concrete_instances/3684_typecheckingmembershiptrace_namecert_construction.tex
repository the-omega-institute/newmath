\chapter{A Concrete Naming Certificate for $\TypeCheckingMembershipTraceUp$}
\label{ch:concrete-instances-typecheckingmembershiptrace-namecert}
\origin{human}

The $\TypeCheckingMembershipTraceUp$ surface packages a type-checking read as finite classifier membership rather than as a host typing judgment. It joins the membership row of $\TypeClassifierMembershipUp$, the classifier typing row of $\ClassifierTypingUp$, the subject-reduction handoff of $\SubjectReductionUp$, the finite derivation route of $\TypeCheckingDerivationTraceUp$, and the proof-erasure readback of $\ProofTermErasureTraceUp$ into one locally named packet.

\autoref{ch:concrete-instances-subjectreduction-namecert} supplies the displayed $\Cont$ subject-reduction handoff consumed by the row $R$, so the membership-trace packet records preservation as a sibling route rather than as an ambient typing theorem.

\begin{definition}[TypeCheckingMembershipTrace carrier]
\label{def:typechecking-membership-trace-carrier}
A $\TypeCheckingMembershipTraceUp$ carrier is a finite $\BHist$ packet
$$
T=(M,D,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\Ext$-membership row read as the local type-classifier decision;
\item $D$ is the finite derivation trace row recording the classifier typing route;
\item $R$ is the displayed $\Cont$ subject-reduction route attached to the same membership read;
\item $S$ is the $\SigRel$ readback row relating the derivation route to the visible classifier name;
\item $H$ records componentwise $\hsame$ transport for $M,D,R,S$ and the local naming row;
\item $C$ records the displayed $\Cont$ replay by which consumers reread membership, derivation, subject-reduction, and readback rows;
\item $P$ is the $\Pkg$ provenance row over $\TypeClassifierMembershipUp$, $\ClassifierTypingUp$, $\SubjectReductionUp$, $\TypeCheckingDerivationTraceUp$, $\ProofTermErasureTraceUp$, $\Ext$, $\Cont$, $\SigRel$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\TypeCheckingMembershipTraceUp}$ row.
\end{enumerate}
The local classifier compares two accepted carriers only through the displayed membership, derivation, subject-reduction, readback, transport, continuation, package, and naming rows.
\end{definition}
\leanchecked{BEDC.Derived.TypeCheckingMembershipTraceUp.TypeCheckingMembershipTraceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[TypeCheckingMembershipTrace classifier stability]
\label{thm:typechecking-membership-trace-classifier-stability}
Let
$$
T=(M,D,R,S,H,C,P,N)
$$
and
$$
T'=(M',D',R',S',H',C',P',N')
$$
be accepted $\TypeCheckingMembershipTraceUp$ carriers whose classifier rows are related by the displayed local comparison. Then the $\Ext$-membership read, finite derivation route, $\Cont$ subject-reduction handoff, and $\SigRel$ readback are preserved through the componentwise $\hsame$ rows and the displayed $\Cont$ replay. The preserved object is the same finite membership-trace packet; it supplies no host typing judgment, conversion oracle, or quotient proof-term equality.
\end{theorem}
\leanchecked{BEDC.Derived.TypeCheckingMembershipTraceUp.TypeCheckingMembershipTraceClassifier\_stability}
\begin{proof}
Project both carriers through \autoref{def:typechecking-membership-trace-carrier}. The rows $M,D,R,S$ name the classifier membership, derivation route, subject-reduction handoff, and readback boundary, while $H$ records componentwise transport for those rows.

Use the displayed continuation replay $C$ and $C'$ to reread the same rows after transport. Since replay is attached to the packet coordinates, it cannot introduce an external typing derivation or conversion decision.

Finally repack the transported rows with the provenance and local naming rows $P,N$ and $P',N'$. The result is still the finite $\TypeCheckingMembershipTraceUp$ surface, so quotient proof-term equality and host type equality remain outside the exported classifier evidence.
\end{proof}

\begin{theorem}[TypeCheckingMembershipTrace NameCert obligations]
\label{thm:typechecking-membership-trace-namecert-obligations}
The local $\NameCert_{\TypeCheckingMembershipTraceUp}$ surface has five finite obligations:
\begin{enumerate}
\item carrier admission exposes exactly $T=(M,D,R,S,H,C,P,N)$;
\item membership classification reads accepted type-checking only from the displayed $\Ext$ row $M$;
\item derivation-route exactness keeps the classifier typing route inside $D$ and its $\SigRel$ readback inside $S$;
\item subject-reduction handoff uses only the displayed $\Cont$ route $R$ and its replay through $C$;
\item the non-escape ledger excludes host type equality, global normalization, proof irrelevance, quotient syntax, conversion oracles, and any proof-term equality not displayed in the packet.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.TypeCheckingMembershipTraceUp.TypeCheckingMembershipTraceNameCert\_obligation\_surface}
\leanchecked{BEDC.Derived.TypeCheckingMembershipTraceUp.TypeCheckingMembershipTraceNameCert\_obligations}
\begin{proof}
Carrier admission is projection from the finite tuple. The membership row, derivation row, subject-reduction row, readback row, transport row, replay row, package row, and local naming row are all coordinates of the same $\BHist$ packet.

The classifier and derivation obligations are read from $M,D,S$. The row $M$ is the only accepted $\Ext$-membership decision, $D$ is the only derivation route, and $S$ is the only signature readback attached to that route.

The subject-reduction handoff is read from $R$ and replayed through $C$. Because every exported row is packaged with $H,P,N$, no host type equality, global normalization theorem, proof irrelevance principle, quotient syntax, conversion oracle, or external proof-term equality can be recovered from the certificate surface.
\end{proof}

\begin{theorem}[TypeCheckingMembershipTrace route coverage]
\label{thm:typechecking-membership-trace-route-coverage}
Every accepted $\TypeCheckingMembershipTraceUp$ packet
$$
T=(M,D,R,S,H,C,P,N)
$$
exposes the membership row $M$, the derivation route $D$, the subject-reduction handoff $R$, the $\SigRel$ readback $S$, the $\hsame$ transport row $H$, the $\Cont$ replay row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\TypeCheckingMembershipTraceUp}$ row $N$ before any consumer may cite type-checking membership from the packet.
\end{theorem}
\begin{proof}
Project the accepted packet through \autoref{def:typechecking-membership-trace-carrier}. The carrier definition lists exactly the eight displayed coordinates, so a consumer has no membership citation until the row $M$ and its companion rows $D,R,S,H,C,P,N$ have been exposed.

Apply \autoref{thm:typechecking-membership-trace-classifier-stability} to transport the displayed classifier read through $H$ and $C$. Apply \autoref{thm:typechecking-membership-trace-namecert-obligations} to require the local naming row before the packet is used as type-checking membership evidence.
\end{proof}

\begin{theorem}[TypeCheckingMembershipTrace non-escape]
\label{thm:typechecking-membership-trace-nonescape}
No accepted $\TypeCheckingMembershipTraceUp$ consumer can export host type equality, global normalization, proof irrelevance, quotient syntax, a conversion oracle, or proof-term equality outside the displayed coordinates $M,D,R,S,H,C,P,N$.
\end{theorem}
\begin{proof}
The carrier has no coordinate for host type equality, global normalization, proof irrelevance, quotient syntax, a conversion oracle, or proof-term equality. Projection from $T$ therefore reaches only $M,D,R,S,H,C,P,N$.

The final row of \autoref{thm:typechecking-membership-trace-namecert-obligations} is the non-escape ledger for this packet. It binds every exported consumer to the finite rows already displayed, so no additional equality or conversion principle can be recovered from the certificate surface.
\end{proof}

\begin{theorem}[TypeCheckingMembershipTrace obligation assembly]
\label{thm:typechecking-membership-trace-obligation-assembly}
The carrier of \autoref{def:typechecking-membership-trace-carrier}, the classifier stability of \autoref{thm:typechecking-membership-trace-classifier-stability}, the route coverage of \autoref{thm:typechecking-membership-trace-route-coverage}, and the non-escape ledger of \autoref{thm:typechecking-membership-trace-nonescape} assemble the paper-side $\obligationClosure$ surface for $\TypeCheckingMembershipTraceUp$. This assembly leaves the verification axis at $\unformalizedV$.
\end{theorem}
\begin{proof}
The carrier fixes the finite packet shape, and classifier stability fixes how the displayed classifier rows persist under $\hsame$ transport and $\Cont$ replay. Route coverage then states which rows must be exposed before the packet is cited by a consumer.

The non-escape theorem supplies the refusal side of the local ledger. Together with the NameCert obligations in \autoref{thm:typechecking-membership-trace-namecert-obligations}, these rows specify the complete paper-side obligation surface while making no Lean-side verification claim.
\end{proof}

\begin{theorem}[TypeCheckingMembershipTrace scoped kernel surface]
\label{thm:typechecking-membership-trace-scoped-kernel-surface}
The carrier, classifier stability, route coverage, non-escape ledger, and obligation assembly for $\TypeCheckingMembershipTraceUp$ bind every accepted packet
$$
T=(M,D,R,S,H,C,P,N)
$$
to the displayed $\Ext$ membership row $M$, finite derivation route $D$, $\Cont$ subject-reduction handoff $R$, $\SigRel$ readback row $S$, componentwise $\hsame$ transport row $H$, displayed $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert_{\TypeCheckingMembershipTraceUp}$ row $N$. The scoped kernel surface has no host type equality, quotient syntax, conversion oracle, proof irrelevance, or global normalization coordinate.
\end{theorem}
\begin{proof}
Project the finite carrier with \autoref{def:typechecking-membership-trace-carrier}. It gives exactly the rows $M,D,R,S,H,C,P,N$ and no host typing or conversion coordinate.

Use classifier stability, \autoref{thm:typechecking-membership-trace-classifier-stability}, to keep membership, derivation, subject-reduction, and readback data attached to the same packet under the displayed $\hsame$ transport and $\Cont$ replay.

Apply route coverage and non-escape, \autoref{thm:typechecking-membership-trace-route-coverage} and \autoref{thm:typechecking-membership-trace-nonescape}. The first requires all displayed rows before a consumer cites type-checking membership, while the second excludes host type equality, global normalization, proof irrelevance, quotient syntax, conversion oracles, and external proof-term equality.

Finally use \autoref{thm:typechecking-membership-trace-obligation-assembly}. The assembled obligation surface is therefore scoped to the local certificate: $\Ext$ membership, finite derivation, subject-reduction handoff, $\SigRel$ readback, transport, replay, provenance, and naming are all discharged inside $T$, with no appeal to a host equality or global normalizer.
\end{proof}

\closureat{TypeCheckingMembershipTraceUp}{seedStr}
\closureat{TypeCheckingMembershipTraceUp}{obligationStr}
\closureat{TypeCheckingMembershipTraceUp}{scopedStr}
\closureat{TypeCheckingMembershipTraceUp}{publicStr}

\begin{closurestatus}{\TypeCheckingMembershipTraceUp}
  \constructivestory{A $\TypeCheckingMembershipTraceUp$ packet is generated from finite Ext-membership, derivation-route, subject-reduction, SigRel readback, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{thm:typechecking-membership-trace-scoped-kernel-surface} binds Ext membership, finite derivation, subject-reduction, SigRel readback, hsame transport, Cont replay, Pkg provenance, and local NameCert rows inside the finite membership-trace certificate.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close host type equality, global normalization, proof irrelevance, quotient syntax, Lean verification, or bridge checking.}
  \upgradepath{Lean verification requires a kernel proof of the scoped membership, derivation, subject-reduction, readback, transport, replay, package, and naming surface.}
\end{closurestatus}
