\chapter{A Concrete Naming Certificate for $\UniformCauchyFunctionLimitUp$}
\label{ch:concrete-instances-uniform-cauchy-function-limit-namecert}
\origin{human}

Following Bishop and Bridges, the $\UniformCauchyFunctionLimitUp$ packet records the finite certificate by which a uniformly Cauchy family of Cauchy-name maps is handed to a limit-function reader without selecting a host function or appealing to ambient completeness. It sits after the Cauchy-name transformer boundary of \autoref{ch:concrete-instances-cauchycontinuousmap-namecert}, the real-valued uniform Cauchy family surface of \autoref{ch:concrete-instances-real-function-uniform-cauchy-namecert}, the shared endpoint criterion of \autoref{ch:concrete-instances-uniform-limit-cauchy-criterion-namecert}, the regular rational readback row of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the displayed rows by which a completion-facing consumer may read a limit candidate, not a quotient function, selected pointwise limit, choice schedule, host continuity theorem, or ambient $\RealUp$ completeness principle.

\begin{definition}[Uniform Cauchy function limit carrier]\label{def:uniform-cauchy-function-limit-carrier}
A $\UniformCauchyFunctionLimitUp$ carrier is a finite $\BHist$ packet
$$
L=(F,Q,M,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the finite Cauchy-name map family row, listing the inspected maps, source Cauchy-name windows, and target value windows.
\item $Q$ is the $\RealFunctionUniformCauchyUp$ row, recording that the same displayed tolerance request controls every retained source window and family index.
\item $M$ is the $\CauchyContinuousMapUp$ boundary row, ensuring each inspected map is read through finite stream windows, regular rational image rows, dyadic tolerance ledgers, and a real-seal handoff.
\item $S$ is the $\UniformLimitCauchyCriterionUp$ row, joining the shared uniform Cauchy evidence to the limit-endpoint read.
\item $R$ is the $\RegSeqRatUp$ readback row opened from the displayed map-family windows before the terminal seal is inspected.
\item $E$ is the $\RealUp$ seal row for the limit candidate, available only after $F,Q,M,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the family row, uniform Cauchy row, Cauchy-continuous map boundary, criterion row, readback row, seal row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a limit-function request through $F,Q,M,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\UniformCauchyFunctionLimitUp$, $\CauchyContinuousMapUp$, $\RealFunctionUniformCauchyUp$, $\UniformLimitCauchyCriterionUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformCauchyFunctionLimitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,Q,M,S,R,E,H,C,P,N$. It has no coordinate for host function equality, quotient Cauchy-name maps, selected pointwise limits, countable-choice selectors, global function-space completeness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Uniform Cauchy function limit NameCert obligations]\label{thm:uniform-cauchy-function-limit-namecert-obligations}
For every accepted $\UniformCauchyFunctionLimitUp$ carrier
$$
L=(F,Q,M,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\UniformCauchyFunctionLimitUp}$ surface consists exactly of carrier admission for the finite Cauchy-name map family, real-function uniform Cauchy row, Cauchy-continuous map boundary, uniform-limit Cauchy criterion row, regular rational readback, terminal real seal, componentwise transport, displayed replay, provenance, and local naming. Every limit read must display the shared uniform Cauchy evidence $Q$, the Cauchy-continuous map boundary $M$, and the criterion row $S$ before $R$ and $E$ can be consumed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-cauchy-function-limit-carrier}. The only admitted rows are $F,Q,M,S,R,E,H,C,P,N$, so the local certificate has no place to store an external limit function or a host equality proof.

The uniform Cauchy and continuity obligations are the rows $Q$ and $M$. Their placement before $S,R,E$ forces the family comparison and map-readback surfaces to be displayed before any endpoint seal is reached.

The criterion row $S$ connects those rows to the limit endpoint. The regular rational readback $R$ and terminal seal $E$ then provide the Real-facing route, while $H,C,P,N$ transport, replay, source, and name the same finite packet.
\end{proof}

\begin{theorem}[Uniform Cauchy function limit Real seal]\label{thm:uniform-cauchy-function-limit-real-seal}
Let $L=(F,Q,M,S,R,E,H,C,P,N)$ be an accepted $\UniformCauchyFunctionLimitUp$ carrier. Any $\RealUp$-facing read of the limit candidate factors through the displayed route
$$
\CauchyContinuousMapUp \longmapsto \RealFunctionUniformCauchyUp \longmapsto \UniformLimitCauchyCriterionUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
using the rows $M,Q,S,R,E$ together with transport, replay, provenance, and local naming. The seal cannot be reached from a quotient function, a selected pointwise limit, a countable-choice schedule, or an ambient completeness argument.
\end{theorem}
\begin{proof}
Read the endpoint rows in \autoref{def:uniform-cauchy-function-limit-carrier}. The seal $E$ is downstream of the map boundary $M$, uniform Cauchy row $Q$, criterion row $S$, and regular readback $R$.

Replay the request through $C$. The replay route exposes exactly those rows before the seal is visible, and componentwise transport through $H$ preserves the same dependency order.

The provenance row $P$ sources only the listed packet rows. Therefore a Real-facing consumer receives the displayed route to $E$ and no detached quotient function, selected pointwise limit, choice schedule, or ambient completeness datum.
\end{proof}

\begin{theorem}[Uniform Cauchy function limit continuity handoff]\label{thm:uniform-cauchy-function-limit-continuity-handoff}
Every continuity-facing consumer of an accepted $\UniformCauchyFunctionLimitUp$ packet must first inspect the $\CauchyContinuousMapUp$ row $M$, the $\RealFunctionUniformCauchyUp$ row $Q$, and the $\UniformLimitCauchyCriterionUp$ row $S$. The packet therefore hands forward a finite Cauchy-name continuity boundary for the limit candidate, not a host-level proof of continuity over completed reals.
\end{theorem}
\begin{proof}
The continuity-facing data are the rows $M,Q,S$ in the carrier. They record the map boundary, the shared uniform Cauchy control, and the limit criterion before any regular readback or Real seal is exported.

Use the replay row $C$ and transport row $H$ to present the same rows to downstream consumers. Since $P$ and $N$ source and name only this finite packet, the consumer cannot replace the handoff by a host function on completed reals.

Thus the limit packet exposes a Cauchy-name continuity boundary, while host-level continuity, quotient function equality, selected limits, and completeness remain outside the local certificate.
\end{proof}

\closureat{UniformCauchyFunctionLimitUp}{seedStr}

\begin{closurestatus}{\UniformCauchyFunctionLimitUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-cauchy-function-limit-carrier}, \autoref{thm:uniform-cauchy-function-limit-namecert-obligations}, \autoref{thm:uniform-cauchy-function-limit-real-seal}, and \autoref{thm:uniform-cauchy-function-limit-continuity-handoff} seed the finite certificate for uniform Cauchy limits of Cauchy-name maps through CauchyContinuousMap, RealFunctionUniformCauchy, UniformLimitCauchyCriterion, RegSeqRat, Real, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, quotient Cauchy-name maps, selected pointwise limits, countable choice, global function-space completeness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, uniform Cauchy row stability, Cauchy-continuous map boundary exactness, criterion routing, RegSeqRat readback, Real-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.UniformCauchyFunctionLimitUp.}
  \constructivestory{A $\UniformCauchyFunctionLimitUp$ packet is generated from a finite Cauchy-name map family, RealFunctionUniformCauchy evidence, a CauchyContinuousMap boundary, UniformLimitCauchyCriterion routing, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
