\chapter{A Concrete Naming Certificate for $\ModulusOfUniformConvergenceUp$}
\label{ch:concrete-instances-modulus-of-uniform-convergence-namecert}
\origin{human}

Following Bishop and Bridges, the $\ModulusOfUniformConvergenceUp$ packet records a uniform Cauchy modulus for a family of real-valued approximants as finite NameCert data. It sits after the finite source windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the criterion surface of \autoref{ch:concrete-instances-uniform-convergence-cauchy-criterion-namecert}, and the downstream $\UniformModulusUp$ consumer boundary. The packet gives Real and Completion routes a finite uniform-Cauchy bridge; it does not assert host function equality, countable choice, selected limits, or ambient $\RealUp$ completeness.

\begin{definition}[Modulus of uniform convergence carrier]
\label{def:modulus-of-uniform-convergence-carrier}
A $\ModulusOfUniformConvergenceUp$ carrier is a finite $\BHist$ packet
$$
M=(S,F,U,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ source-window row, naming the finite domain observations inspected for each requested uniform tolerance.
\item $F$ is the function-family row, recording the displayed approximant indices and the finite value reads used in the comparison.
\item $U$ is the uniform tolerance and modulus row, exposing the requested precision and the threshold delivered to the $\UniformModulusUp$ consumer surface.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, carrying the dyadic comparison cells, positivity witnesses, and slack data used by the uniform estimate.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same windows, family reads, tolerance ledger, and modulus threshold.
\item $E$ is the $\RealUp$ seal row reached only after $S,F,U,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for source windows, family reads, tolerance data, readback, seal, continuation, provenance, and naming.
\item $C$ records displayed $\Cont$ replay from a uniform-Cauchy request through $S,F,U,D,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\ModulusOfUniformConvergenceUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\UniformConvergenceCauchyCriterionUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ModulusOfUniformConvergenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,F,U,D,R,E,H,C,P,N$. It has no coordinate for host equality of functions, a countable-choice schedule, a selected limiting function, quotient equality of real names, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Modulus of uniform convergence NameCert obligations]
\label{thm:modulus-of-uniform-convergence-namecert-obligations}
For every accepted $\ModulusOfUniformConvergenceUp$ carrier
$$
M=(S,F,U,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\ModulusOfUniformConvergenceUp}$ surface consists exactly of source-window exposure through $S$, function-family read exposure through $F$, uniform tolerance and modulus exposure through $U$, dyadic tolerance-ledger exposure through $D$, regular rational readback through $R$, real seal handoff through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$. Every Real or Completion consumer must read
$$
S,F,U,D \longmapsto R \longmapsto E \longmapsto H,C,P,N
$$
before treating the packet as a uniform convergence modulus.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:modulus-of-uniform-convergence-carrier}. The visible coordinates are exactly $S,F,U,D,R,E,H,C,P,N$, and the classifier contains no further analytic row.

The source and family data are read first. The row $S$ supplies finite $\StreamNameUp$ windows, while $F$ supplies the displayed approximant reads attached to those windows.

The uniform estimate is then forced through $U$ and $D$. The row $U$ names the common threshold and tolerance request, and $D$ supplies the dyadic ledger that makes the comparison finite.

Finally $R,E,H,C,P,N$ deliver regular rational readback, the real-facing seal, transport, replay, provenance, and local naming for the same displayed route. Since host function equality, countable choice, selected limits, quotient real-name equality, and ambient $\RealUp$ completeness are absent from the carrier, the listed obligations exhaust the certificate surface.
\end{proof}

\begin{theorem}[Modulus of uniform convergence Cauchy-uniform route]
\label{thm:modulus-of-uniform-convergence-cauchy-uniform-route}
Every accepted $\ModulusOfUniformConvergenceUp$ packet consumed by $\UniformConvergenceCauchyCriterionUp$, $\UniformModulusUp$, $\RegSeqRatUp$, or $\RealUp$ factors through the finite route
$$
\StreamNameUp,\ F,\ \UniformModulusUp,\ \DyadicRatCoreUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
The route exposes the same rows $S,F,U,D,R,E,H,C,P,N$ and refuses host function equality, countable choice, selected limits, quotient equality of real names, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the accepted carrier. A criterion or modulus consumer first receives the source-window row $S$ and the family-read row $F$, so the request is grounded in finite observations rather than in a host function object.

Next read the uniform modulus row $U$ together with the dyadic ledger $D$. These rows give a single threshold and tolerance comparison surface for all displayed windows, which is precisely the finite boundary expected by $\UniformConvergenceCauchyCriterionUp$ and $\UniformModulusUp$.

The only downstream analytic readback is $R$ followed by $E$. Thus $\RegSeqRatUp$ and $\RealUp$ consumers receive data only after the uniform-Cauchy route has been displayed.

The transport, replay, provenance, and naming rows $H,C,P,N$ preserve the same route. No excluded host equality, choice schedule, selected limit, quotient equality, or completeness coordinate can be inserted by a consumer of the accepted packet.
\end{proof}

\falsifiablePrediction{Every accepted $\ModulusOfUniformConvergenceUp$ read used by a Real or Completion consumer displays one $\StreamNameUp$ source-window row, one function-family read row, one $\UniformModulusUp$ tolerance-threshold row, one $\DyadicRatCoreUp$ tolerance ledger, one $\RegSeqRatUp$ readback row, one $\RealUp$ seal row, and the structural rows $H,C,P,N$ before it is treated as a uniform convergence modulus.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real, uniformmodulus, uniform-convergence-cauchy-criterion: $\ModulusOfUniformConvergenceUp$ is not bijective to any listed sibling because only it binds finite source windows, displayed family reads, one uniform threshold, a dyadic tolerance ledger, regular rational readback, a real seal, and local NameCert support into one uniform-Cauchy bridge.}

\closureat{ModulusOfUniformConvergenceUp}{seedStr}

\begin{closurestatus}{\ModulusOfUniformConvergenceUp}
  \constructivestory{A $\ModulusOfUniformConvergenceUp$ packet is generated from StreamName source windows, function-family read rows, a UniformModulus tolerance-threshold row, a DyadicRatCore tolerance ledger, RegSeqRat readback, a RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:modulus-of-uniform-convergence-carrier}, \autoref{thm:modulus-of-uniform-convergence-namecert-obligations}, and \autoref{thm:modulus-of-uniform-convergence-cauchy-uniform-route} seed the finite uniform-Cauchy bridge over StreamName, DyadicRatCore, RegSeqRat, Real, UniformConvergenceCauchyCriterion, and UniformModulus rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, countable choice, selected limits, quotient equality of real names, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source-window exposure, function-family read exposure, uniform threshold exposure, dyadic tolerance-ledger exposure, readback, real seal ordering, transport, replay, provenance, and local naming for BEDC.Derived.ModulusOfUniformConvergenceUp.}
\end{closurestatus}
