\chapter{A Concrete Naming Certificate for $\DirectedCauchyNetUp$}
\label{ch:concrete-instances-directed-cauchy-net-namecert}
\origin{human}

Following Moore and Smith's theory of nets and Kelley's topology text, the $\DirectedCauchyNetUp$ packet records a directed Cauchy net as finite BEDC completion data. It sits after the directed-index substrate of \autoref{ch:concrete-instances-directedset-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a net-style L10 completion neighbour for the sequence-based $\RealUp$ route; it does not choose a cofinal sequence, quotient nets, selected limits, countable choice, or ambient $\RealUp$ completeness.

\falsifiablePrediction{A completion consumer that reads a directed Cauchy net without the directed-index row, finite cofinal windows, StreamName readback, RegSeqRat handoff, DyadicRatCore tolerance ledger, and terminal Real seal has left the $\DirectedCauchyNetUp$ packet.}

\independenceWitness{directedset, streamname, regseqrat, dyadicratcore, real: $\DirectedCauchyNetUp$ is not bijective to any one sibling because it jointly carries directed-window data, Cauchy-tail stability, sequence-style readback, dyadic tolerances, terminal sealing, transport, replay, provenance, and local naming.}

\begin{definition}[Directed Cauchy net carrier]
\label{def:directed-cauchy-net-carrier}
A $\DirectedCauchyNetUp$ carrier is a finite $\BHist$ packet
$$
N=(D,V,W,T,S,Q,Y,R,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DirectedSetUp$ row naming the inspected directed vertices, comparison rows, finite request windows, and common upper-window witnesses;
\item $V$ is the net-value row storing the finite values visible at the inspected directed vertices;
\item $W$ is the finite cofinal-window row whose entries compare directed tails before any completion consumer reads a Real-facing seal;
\item $T$ is the Cauchy-tail stability row, stating that every requested dyadic tolerance is witnessed after a displayed upper window;
\item $S$ is the $\StreamNameUp$ row obtained by reading a retained cofinal window as a finite stream window;
\item $Q$ is the $\RegSeqRatUp$ handoff row that records the regular rational readback of the retained window;
\item $Y$ is the $\DyadicRatCoreUp$ tolerance ledger used by the tail-stability and regular readback rows;
\item $R$ is the terminal $\RealUp$ seal for completion-facing consumers allowed to read a real name;
\item $H$ records componentwise $\hsame$ transport for the directed-index, value, cofinal-window, tail-stability, stream, regular, dyadic, Real, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from a directed request through $D,V,W,T,S,Q,Y,R,H,P,L$;
\item $P$ is the $\Pkg$ provenance over $\DirectedCauchyNetUp$, $\DirectedSetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows, with sibling anchors in \autoref{ch:concrete-instances-directedset-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert};
\item $L$ is the local $\NameCert_{\DirectedCauchyNetUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for a host net function, quotient of nets, selected cofinal sequence, selected limit, countable-choice schedule, or ambient completeness theorem.
\end{definition}

\begin{theorem}[Directed Cauchy net NameCert obligations]
\label{thm:directed-cauchy-net-namecert-obligations}
For every accepted $\DirectedCauchyNetUp$ carrier
$$
N=(D,V,W,T,S,Q,Y,R,H,C,P,L),
$$
the local $\NameCert_{\DirectedCauchyNetUp}$ surface consists exactly of directed-index admission through $D$, finite value exposure through $V$, cofinal-window extraction through $W$, tail-stability evidence through $T$, StreamName readback through $S$, RegSeqRat handoff through $Q$, DyadicRatCore tolerance accounting through $Y$, terminal Real sealing through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:directed-cauchy-net-carrier}. The directed-index row $D$, value row $V$, cofinal-window row $W$, and tail-stability row $T$ are visible before the completion-facing readback rows.

The sequence-style readback is fixed by $S,Q,Y,R$. The retained window is read as a $\StreamNameUp$ row, then as a $\RegSeqRatUp$ handoff controlled by the $\DyadicRatCoreUp$ ledger, and only then reaches the terminal $\RealUp$ seal.

The rows $H,C,P,L$ transport, replay, source, and name the same packet through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Since the carrier has no coordinate for the excluded host objects, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Directed Cauchy net finite window extraction]
\label{thm:directed-cauchy-net-finite-window-extraction}
Every completion-facing read of an accepted $\DirectedCauchyNetUp$ packet extracts a finite cofinal window from the displayed $\DirectedSetUp$ row before it may inspect $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, or $\RealUp$ data. The extraction supplies only the finite window $W$ and its common upper witnesses; it does not supply a selected cofinal sequence, quotient index, host net function, or countable-choice operator.
\end{theorem}
\begin{proof}
Start with the directed substrate. The row $D$ names the inspected directed vertices, comparison rows, and upper-window witnesses, and $W$ records which finite windows are retained for completion readback.

The downstream rows cannot be read before this extraction. The $\StreamNameUp$ row $S$, $\RegSeqRatUp$ handoff $Q$, $\DyadicRatCoreUp$ ledger $Y$, and $\RealUp$ seal $R$ all appear after $D,V,W,T$ in the carrier, and their provenance is routed through the sibling certificates named in \autoref{def:directed-cauchy-net-carrier}.

Therefore a consumer receives finite cofinal-window data, not a host-level cofinal sequence or quotient of directed indices. The structural rows $H,C,P,L$ preserve that same finite extraction boundary.
\end{proof}

\begin{theorem}[Directed Cauchy net tail stability]
\label{thm:directed-cauchy-net-tail-stability}
In an accepted $\DirectedCauchyNetUp$ carrier, every dyadic tolerance requested by the completion route is witnessed by a displayed upper window in $W$ and by the $\DyadicRatCoreUp$ ledger $Y$. The resulting tail-stability row $T$ is read through $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and the local $\NameCert_{\DirectedCauchyNetUp}$ row; it is not an external Cauchy principle over all directed indices.
\end{theorem}
\begin{proof}
Read the tail-stability row $T$ together with the tolerance ledger $Y$. The row $T$ does not quantify over an unbounded host net; it points to the finite windows in $W$ and to the dyadic tolerances recorded by $\DyadicRatCoreUp$.

The directed side is supplied by $D$ and $V$. The common upper-window witnesses named by $\DirectedSetUp$ are the only witnesses that the stability row may use.

Finally, $H,C,P,L$ keep the stability evidence inside the BEDC packet. The $\hsame$ transports preserve row identity, the $\Cont$ row replays only the displayed route, the $\Pkg$ row records the sibling sources, and the local $\NameCert$ row names the packet. Hence $T$ is a finite carrier row, not an external Cauchy theorem.
\end{proof}

\begin{theorem}[Directed Cauchy net Real-completion handoff]
\label{thm:directed-cauchy-net-real-completion-handoff}
Every $\RealUp$-facing consumer of an accepted $\DirectedCauchyNetUp$ packet must pass through
$$
D,V,W,T,S,Q,Y,R,H,C,P,L .
$$
The handoff compares the directed cofinal windows with the sequence-style route by reading $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ in that order. It exports no selected Moore--Smith limit, quotient-net equality, countable-choice row, host real equality, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Apply the NameCert obligation theorem. It identifies $D,V,W,T,S,Q,Y,R,H,C,P,L$ as the whole local surface and gives no second source for directed tails, stream windows, regular rational readback, dyadic tolerances, or the Real seal.

The completion handoff is sequential only after it is finite and directed. The packet first reads the $\DirectedSetUp$ substrate and the cofinal-window stability rows, then reads the retained window through $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ before serving the terminal $\RealUp$ seal.

The componentwise $\hsame$ row, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row preserve the same order of access. Thus the handoff compares net-style cofinal windows with the existing sequence-based Real route without adding a selected limit, quotient net, countable-choice schedule, host equality, or ambient completeness principle.
\end{proof}

\closureat{DirectedCauchyNetUp}{seedStr}

\begin{closurestatus}{\DirectedCauchyNetUp}
  \constructivestory{A $\DirectedCauchyNetUp$ packet is generated from a DirectedSet row, finite net-value rows, cofinal-window rows, Cauchy-tail stability evidence, StreamName readback, RegSeqRat handoff, DyadicRatCore tolerance accounting, a terminal Real seal, componentwise transport, continuation replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:directed-cauchy-net-carrier}, \autoref{thm:directed-cauchy-net-namecert-obligations}, \autoref{thm:directed-cauchy-net-finite-window-extraction}, \autoref{thm:directed-cauchy-net-tail-stability}, and \autoref{thm:directed-cauchy-net-real-completion-handoff} seed the finite directed Cauchy-net carrier and its Real-completion handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected Moore--Smith limits, quotient-net equality, cofinal-choice schedules, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite window extraction, tail stability, Real-completion handoff, componentwise transport, replay, provenance, and local naming for BEDC.Derived.DirectedCauchyNetUp.}
\end{closurestatus}
