\chapter{A Concrete Naming Certificate for $\AdeleUp$}
\label{ch:concrete-instances-adele-namecert}

The adele interface $\AdeleUp$ packages the restricted product of $\NameCert_{\RealUp}$ and $\NameCert_{\PadicUp}$ over all primes $p$. It depends on $\NameCert_{\PadicUp}$ (\autoref{ch:concrete-instances-padic-namecert}) and supplies the global object on which automorphic and L-function certificates are built.

\section{Carrier definition}
\label{sec:adele-carrier}

\begin{definition}[Adele history carrier]
\label{def:adele-history-carrier}
For a history $h$, the predicate $\mathsf{AdeleHistoryCarrier}(h)$ holds when
there are histories $r,p,e,s$ such that $r$ is a constant real history,
$\mathsf{PadicPrimeScale}(p,e,s)$ holds, and
$$
\hsame(h,\mathsf{append}(r,s)).
$$
\end{definition}
\leandef{BEDC.Derived.AdeleUp.AdeleHistoryCarrier}

\begin{theorem}[Adele history carrier nonempty]
\label{thm:adele-history-carrier-not-empty}
If $\mathsf{AdeleHistoryCarrier}(h)$ holds, then $h$ is not internally the same
as $\emp$.
\end{theorem}
\begin{proof}
Unpack the carrier into its constant real endpoint, $p$-adic scale endpoint,
and append witness. If $h\hsame\emp$, the append witness identifies the
appended real and scale endpoints with $\emp$, so append-empty exactness makes
the real endpoint empty. The constant real endpoint is one-sealed, and empty
cannot be internally the same as a one-sealed history.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_not\_empty}

\begin{theorem}[Adele carrier continuation result nonempty]
\label{thm:adele-history-carrier-cont-result-nonempty}
If $\mathsf{AdeleHistoryCarrier}(h)$ holds, $\mathsf{Cont}(h,k,r)$ holds, and
$r\hsame\emp$, then contradiction follows.
\end{theorem}
\begin{proof}
Transport the continuation result along $r\hsame\emp$ to a continuation with
empty result. The empty-result inversion theorem for $\mathsf{Cont}$ forces
the source endpoint $h$ to be $\emp$. This contradicts the nonempty theorem
for the adele history carrier.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_cont\_result\_nonempty}

\begin{theorem}[Adele right carrier continuation result nonempty]
\label{thm:adele-history-carrier-cont-right-result-nonempty}
If $\mathsf{AdeleHistoryCarrier}(k)$ holds, $\mathsf{Cont}(h,k,r)$ holds, and
$r\hsame\emp$, then contradiction follows.
\end{theorem}
\begin{proof}
Transport the continuation result along $r\hsame\emp$ to a continuation with
empty result. The empty-result inversion theorem for $\mathsf{Cont}$ forces
the right endpoint $k$ to be $\emp$, contradicting the nonempty theorem for the
adele history carrier.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_cont\_right\_result\_nonempty}

\begin{theorem}[Adele appended right carrier continuation result nonempty]
\label{thm:adele-history-carrier-cont-append-right-result-nonempty}
If $\mathsf{AdeleHistoryCarrier}(k)$ holds, $\mathsf{Cont}(h,\mathsf{append}(p,k),r)$
holds, and $r\hsame\emp$, then contradiction follows.
\end{theorem}
\begin{proof}
Transport the continuation result along $r\hsame\emp$ to a continuation with
empty result. The empty-result inversion theorem for $\mathsf{Cont}$ forces
$\mathsf{append}(p,k)\hsame\emp$. Empty append inversion recovers
$k\hsame\emp$, contradicting the nonempty theorem for the adele history
carrier.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_cont\_append\_right\_result\_nonempty}

\begin{theorem}[Adele real-padic e1 real nonempty scale result package]
\label{thm:adele-real-padic-e1-real-nonempty-scale-result-package}
For histories $r,p,e,s:\mathsf{BHist}$, if $r$ is a rational-history carrier
and $\mathsf{PadicPrimeScale}(p,\mathsf{e1}(e),s)$ holds, then the real
component $\mathsf{e1}(r)$ is a real constant-history carrier and the padic
scale result is not history-same as $\emp$:
$$
\mathsf{RatHistoryCarrier}(r)\land
\mathsf{PadicPrimeScale}(p,\mathsf{e1}(e),s)
\Longrightarrow
\mathsf{RealConstantHistoryCarrier}(\mathsf{e1}(r))\land
(\hsame(s,\emp)\to\bot).
$$
The real component is certified by the $\RealUp$ $e1$ readback theorem from
the rational carrier. The padic component uses the empty-result iff for
$\PadicUp$ scale: an empty result would force
$\hsame(\mathsf{e1}(e),\emp)$, which is excluded by the BEDC history
constructor separation.
\end{theorem}
\begin{proof}
Apply the real constant-history $e1$ iff to the rational carrier witness for
$r$. For the padic result, assume $\hsame(s,\emp)$. The padic empty-result iff
turns this into $\hsame(\mathsf{e1}(e),\emp)$, contradicting the BEDC
$e1$-versus-empty separation theorem.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealPadic\_e1\_real\_nonempty\_scale\_result\_package}

\begin{theorem}[Adele carrier from the first-prime unit scale]
\label{thm:adele-history-carrier-first-prime-unit-scale}
For any rational-history carrier tail \(r\), the history formed by appending
the one-sealed real component to the concrete first-prime unit scale result is
an adele-history carrier:
$$
\mathsf{RatHistoryCarrier}(r)\Longrightarrow
\mathsf{AdeleHistoryCarrier}(
  \mathsf{append}(\Eone(r),\Eone(\Eone(\emp)))).
$$
\end{theorem}
\begin{proof}
The real component is a constant real history by the \(e1\) readback theorem
for rational carriers. The padic component is the concrete first-prime unit
scale from \autoref{thm:padic-prime-scale-first-prime-unit-exponent-result}.
The final carrier equality is reflexive history sameness for the displayed
append history.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_first\_prime\_unit\_scale}

\begin{theorem}[Adele visible scale result carrier and nonempty result]
\label{thm:adele-history-carrier-visible-scale-result-nonempty}
For histories $r,p,e,s:\mathsf{BHist}$, if
$\mathsf{RealConstantHistoryCarrier}(r)$ and
$\mathsf{PadicPrimeScale}(p,\mathsf{e1}(e),s)$ hold, then the appended history
$\mathsf{append}(r,s)$ is an adele-history carrier and the visible scale result
is not history-same as $\emp$:
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(r,s))\land
(\hsame(s,\emp)\to\bot).
$$
\end{theorem}
\begin{proof}
Use $r,p,\mathsf{e1}(e),s$ as the four carrier witnesses for the appended
adele history. The append witness is reflexive history sameness. If
$s\hsame\emp$, the $p$-adic empty-result iff forces
$\hsame(\mathsf{e1}(e),\emp)$, contradicting the BEDC separation of
one-sealed histories from $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_visible\_scale\_result\_nonempty}

\begin{theorem}[Adele visible zero-scale result carrier and nonempty result]
\label{thm:adele-history-carrier-visible-zero-scale-result-nonempty}
For histories $r,p,e,s:\mathsf{BHist}$, if
$\mathsf{RealConstantHistoryCarrier}(r)$ and
$\mathsf{PadicPrimeScale}(p,\Ezero(e),s)$ hold, then the appended history
$\mathsf{append}(r,s)$ is an adele-history carrier and the visible zero-scale
result is not history-same as $\emp$:
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(r,s))\land
(\hsame(s,\emp)\to\bot).
$$
\end{theorem}
\begin{proof}
Use $r,p,\Ezero(e),s$ as the four carrier witnesses for the appended adele
history. The append witness is reflexive history sameness. If $s\hsame\emp$,
the $p$-adic empty-result iff forces $\hsame(\Ezero(e),\emp)$, contradicting
the BEDC separation of zero-headed histories from $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_visible\_zero\_scale\_result\_nonempty}

\begin{theorem}[Adele prime-swap scale readback]
\label{thm:adele-prime-swap-scale-readback}
Let \(real,p,q,r,s:\mathsf{BHist}\). If \(real\) is a constant real-history
carrier and the two swapped prime scales
\(\mathsf{PadicPrimeScale}(p,q,r)\) and \(\mathsf{PadicPrimeScale}(q,p,s)\)
hold, then both appended real-padic histories are adele-history carriers and
they are history-same:
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(real,r))\land
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(real,s))\land
\hsame(\mathsf{append}(real,r),\mathsf{append}(real,s)).
$$
\end{theorem}
\begin{proof}
Use the displayed real component and the two displayed prime-scale witnesses
as the adele carrier payloads. The \(p\)-adic scale results are identified by
prime-exponent commutativity for \(\mathsf{PadicPrimeScale}\), and applying
the fixed real append context to that \(\hsame\) identifies the two adele
histories.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_prime\_swap\_scale\_readback}

\begin{theorem}[Adele visible scale continuation nonempty package]
\label{thm:adele-history-carrier-visible-scale-cont-nonempty-package}
For histories $r,p,e,s,k,out:\mathsf{BHist}$, if
$\mathsf{RealConstantHistoryCarrier}(r)$,
$\mathsf{PadicPrimeScale}(p,\mathsf{e1}(e),s)$, and
$\ContR(\mathsf{append}(r,s),k,out)$ hold, then
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(r,s))\land
(\hsame(s,\emp)\to\bot)\land(\hsame(out,\emp)\to\bot).
$$
\end{theorem}
\begin{proof}
The visible scale theorem supplies both the adele-history carrier for
$\mathsf{append}(r,s)$ and the nonempty scale endpoint $s$. Feeding that carrier
and the displayed continuation into the adele continuation-result theorem gives
the nonempty output endpoint.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_visible\_scale\_cont\_nonempty\_package}

\begin{theorem}[Adele visible zero-scale continuation nonempty package]
\label{thm:adele-history-carrier-visible-zero-scale-cont-nonempty-package}
For histories $r,p,e,s,k,out:\mathsf{BHist}$, if
$\mathsf{RealConstantHistoryCarrier}(r)$,
$\mathsf{PadicPrimeScale}(p,\mathsf{e0}(e),s)$, and
$\ContR(\mathsf{append}(r,s),k,out)$ hold, then
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(r,s))\land
(\hsame(s,\emp)\to\bot)\land(\hsame(out,\emp)\to\bot).
$$
\end{theorem}
\begin{proof}
The visible zero-scale theorem supplies both the adele-history carrier for
$\mathsf{append}(r,s)$ and the nonempty scale endpoint $s$. Feeding that carrier
and the displayed continuation into the adele continuation-result theorem gives
the nonempty output endpoint.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_visible\_zero\_scale\_cont\_nonempty\_package}

\begin{theorem}[Adele e1 real append carrier is nonempty]
\label{thm:adele-history-carrier-e1-real-append-nonempty}
For histories $realTail,p,exponent,result:\mathsf{BHist}$, if
$\mathsf{RealConstantHistoryCarrier}(\mathsf{e1}(realTail))$ and
$\mathsf{PadicPrimeScale}(p,exponent,result)$ hold, then the appended real-padic
history is an adele-history carrier and cannot be history-same as $\emp$:
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(\mathsf{e1}(realTail),result))\land
(\hsame(\mathsf{append}(\mathsf{e1}(realTail),result),\emp)\to\bot).
$$
\end{theorem}
\begin{proof}
Use the displayed one-sealed real component, $p$, $exponent$, and $result$ as
the four witnesses for the adele carrier. If the append were empty, append-empty
exactness would force $\mathsf{e1}(realTail)\hsame\emp$, contradicting BEDC
constructor separation.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_e1\_real\_append\_nonempty}

\begin{theorem}[Adele visible scale append is not empty]
\label{thm:adele-visible-scale-append-not-empty}
For histories $r,p,e,s:\mathsf{BHist}$, if
$\mathsf{RealConstantHistoryCarrier}(r)$ and
$\mathsf{PadicPrimeScale}(p,\mathsf{e1}(e),s)$ hold, then the appended visible
scale history is not history-same as $\emp$:
$$
\hsame(\mathsf{append}(r,s),\emp)\to\bot.
$$
\end{theorem}
\begin{proof}
Assume the appended history is empty. Append-empty exactness makes the scale
result $s$ empty. The $p$-adic empty-result iff then forces
$\hsame(\mathsf{e1}(e),\emp)$, contradicting the BEDC separation of one-sealed
histories from $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_visible\_scale\_append\_not\_empty}

\begin{theorem}[Adele empty scale real readback]
\label{thm:adele-history-carrier-empty-scale-real-readback}
Let $real,p,result:\mathsf{BHist}$. If $real$ is a constant real-history
carrier and $\mathsf{PadicPrimeScale}(p,\emp,result)$ holds, then
$\mathsf{append}(real,result)$ is an adele-history carrier and
$$
\mathsf{append}(real,result)\hsame real.
$$
\end{theorem}
\begin{proof}
The padic empty-result iff identifies $result$ with $\emp$ because the
exponent is $\emp$. The adele carrier uses the displayed real component, prime,
empty exponent, scale result, and reflexive append witness. The readback to
$real$ follows from the identified empty scale result and append-right unit.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_empty\_scale\_real\_readback}

\begin{theorem}[Adele unit scale real-prime readback]
\label{thm:adele-history-carrier-unit-scale-real-prime-readback}
Let $real,p,result:\mathsf{BHist}$. If $real$ is a constant real-history
carrier and $\mathsf{PadicPrimeScale}(p,\Eone(\emp),result)$ holds, then
$\mathsf{append}(real,result)$ is an adele-history carrier and
$$
\mathsf{append}(real,result)\hsame\mathsf{append}(real,p).
$$
\end{theorem}
\begin{proof}
Use the displayed real component, prime base, unit exponent, and scale result
as the four witnesses for the adele carrier. The prime-scale unit-exponent
readback identifies $result$ with $p$; applying append congruence under the
fixed real component gives the displayed real-prime readback.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_unit\_scale\_real\_prime\_readback}

\begin{theorem}[Adele unit-left scale continuation readback]
\label{thm:adele-unit-left-scale-cont-readback}
Let $real,p,q,result:\mathsf{BHist}$. If $real$ is a constant real-history
carrier, $q$ is unary, and
$\mathsf{PadicPrimeScale}(p,\mathsf{append}(\Eone(\emp),q),result)$ holds,
then $\mathsf{append}(real,result)$ is an adele-history carrier and
$$
\exists e.\,\mathsf{PadicPrimeScale}(p,q,e)\land\ContR(p,e,result).
$$
\end{theorem}
\begin{proof}
The adele carrier uses the displayed real component, prime base, appended
unit-left exponent, and scale result. The continuation readback is exactly the
unit-left prime-scale append factorization applied to the unary right exponent.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_unit\_left\_scale\_cont\_readback}

\begin{theorem}[Adele unit-left scale append readback]
\label{thm:adele-history-carrier-unit-left-scale-append-readback}
Let $real,p,q,result:\mathsf{BHist}$. If $real$ is a constant real-history
carrier, $q$ is unary, and
$\mathsf{PadicPrimeScale}(p,\mathsf{append}(\Eone(\emp),q),result)$ holds,
then $\mathsf{append}(real,result)$ is an adele-history carrier and
$$
\exists e.\,
\mathsf{PadicPrimeScale}(p,q,e)\land
\mathsf{append}(real,result)\hsame
\mathsf{append}(real,\mathsf{append}(p,e)).
$$
\end{theorem}
\begin{proof}
The adele carrier uses the displayed real component, prime base, appended
unit-left exponent, and scale result. The unit-left prime-scale factorization
extracts a right-factor result $e$ with $\ContR(p,e,result)$; reading that
continuation as the append endpoint and applying the fixed real append context
gives the displayed real-padic readback.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_unit\_left\_scale\_append\_readback}

\begin{theorem}[Adele unit-right scale continuation readback]
\label{thm:adele-history-carrier-unit-right-scale-cont-readback}
Let $real,p,w,result:\mathsf{BHist}$. If $real$ is a constant real-history
carrier, $w$ is unary, and
$\mathsf{PadicPrimeScale}(p,\mathsf{append}(w,\Eone(\emp)),result)$ holds,
then $\mathsf{append}(real,result)$ is an adele-history carrier and
$$
\exists n.\,\mathsf{PadicPrimeScale}(p,w,n)\land\ContR(n,p,result).
$$
\end{theorem}
\begin{proof}
The adele carrier uses the displayed real component, prime base, appended
unit-right exponent, and scale result. The continuation readback is exactly the
unit-right prime-scale append factorization applied to the unary left exponent.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_unit\_right\_scale\_cont\_readback}

\begin{theorem}[Adele unit-right scale append readback]
\label{thm:adele-history-carrier-unit-right-scale-append-readback}
Let $real,p,w,result:\mathsf{BHist}$. If $real$ is a constant real-history
carrier, $w$ is unary, and
$\mathsf{PadicPrimeScale}(p,\mathsf{append}(w,\Eone(\emp)),result)$ holds,
then $\mathsf{append}(real,result)$ is an adele-history carrier and
$$
\exists n.\,
\mathsf{PadicPrimeScale}(p,w,n)\land
\mathsf{append}(real,result)\hsame
\mathsf{append}(real,\mathsf{append}(n,p)).
$$
\end{theorem}
\begin{proof}
The adele carrier uses the displayed real component, prime base, appended
unit-right exponent, and scale result. The unit-right prime-scale factorization
extracts a left-factor result $n$ with $\ContR(n,p,result)$; reading that
continuation as the append endpoint and applying the fixed real append context
gives the displayed real-padic readback.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_unit\_right\_scale\_append\_readback}

\begin{theorem}[Adele real-stream prefix visible scale carrier]
\label{thm:adele-real-stream-prefix-visible-scale-carrier}
Let $x,y:\mathbb{N}\to\mathsf{BHist}$ be real stream representatives whose visible
left stream is unary at every index. If
$\mathsf{RealStreamPrefixClassifier}(x,y,m+n)$ holds, $x(n)\hsame
\Eone(\Eone(d))$, $y(n)\hsame\Eone(i)$, and the concrete first-prime
$p$-adic scale $\mathsf{PadicPrimeScale}(\Eone(\Eone(\emp)),e,r)$ holds,
then $\mathsf{RealStreamPrefixClassifier}(x,y,n)$ holds and
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(\Eone(\Eone(d)),r)).
$$
\end{theorem}
\begin{proof}
The add-left prefix projection recovers the classifier at index $n$. The
visible real-pair readback at that prefix identifies the displayed
$\Eone(\Eone(d))$ endpoint as a rational denominator carrier, hence as a
constant real-history carrier. Combining that real component with the stored
first-prime $p$-adic scale and reflexive history sameness of the displayed
append endpoint gives the adele carrier.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_visible\_scale\_carrier}

\begin{theorem}[Adele long-prefix visible scale carrier]
\label{thm:adele-real-stream-prefix-long-prefix-visible-scale-carrier}
Let $x,y:\mathbb{N}\to\mathsf{BHist}$ be real stream representatives. If
$\mathsf{RealStreamPrefixClassifier}(x,y,m+n)$ holds, $x(n)\hsame
\Eone(\Eone(d))$, $y(n)\hsame\Eone(i)$, and the concrete first-prime
$p$-adic scale $\mathsf{PadicPrimeScale}(\Eone(\Eone(\emp)),e,r)$ holds,
then $\mathsf{RealStreamPrefixClassifier}(x,y,n)$ holds and
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(\Eone(\Eone(d)),r)).
$$
\end{theorem}
\begin{proof}
Induct on $m$ through the recursive finite-prefix classifier to descend from
$m+n$ to $n$. At the selected index, the displayed one-extension endpoints
read back through the rational stream-prefix classifier: the left displayed
tail is unary, hence $\Eone(d)$ is a rational-history carrier and
$\Eone(\Eone(d))$ is a constant real-history carrier. Combining this real
component with the stored first-prime $p$-adic scale and reflexive sameness of
the appended endpoint gives the adele-history carrier.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_long\_prefix\_visible\_scale\_carrier}

\begin{theorem}[Adele long-prefix visible $p$-adic scale carrier]
\label{thm:adele-real-stream-prefix-long-prefix-visible-padic-scale-carrier}
Let $x,y:\mathbb{N}\to\mathsf{BHist}$ be real stream representatives. If
$\mathsf{RealStreamPrefixClassifier}(x,y,m+n)$ holds, $x(n)\hsame
\Eone(\Eone(d))$, $y(n)\hsame\Eone(i)$, and
$\mathsf{PadicPrimeScale}(p,e,r)$ holds, then
$\mathsf{RealStreamPrefixClassifier}(x,y,n)$ holds and
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(\Eone(\Eone(d)),r)).
$$
\end{theorem}
\begin{proof}
Induct on $m$ through the finite-prefix classifier to descend from $m+n$ to
$n$. The visible one-extension pair at index $n$ reads back a unary denominator
tail, so $\Eone(d)$ is a rational-history carrier and $\Eone(\Eone(d))$ is a
constant real-history carrier. The arbitrary displayed $p$-adic scale supplies
the non-real component of the adele carrier, and reflexive history sameness
identifies the displayed append endpoint.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_long\_prefix\_visible\_padic\_scale\_carrier}

\begin{theorem}[Adele long-prefix visible unit-scale readback]
\label{thm:adele-long-prefix-visible-unit-scale-readback}
Let $x,y:\mathbb{N}\to\mathsf{BHist}$ be real stream representatives. If
$\mathsf{RealStreamPrefixClassifier}(x,y,m+n)$ holds, $x(n)\hsame
\Eone(\Eone(d))$, $y(n)\hsame\Eone(i)$, and the displayed $p$-adic unit scale
$\mathsf{PadicPrimeScale}(p,\Eone(\emp),r)$ holds, then
$\mathsf{RealStreamPrefixClassifier}(x,y,n)$ holds,
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(\Eone(\Eone(d)),r)),
$$
and the appended adele endpoint reads back to the displayed prime endpoint:
$$
\mathsf{append}(\Eone(\Eone(d)),r)
\hsame
\mathsf{append}(\Eone(\Eone(d)),p).
$$
\end{theorem}
\begin{proof}
Induct on $m$ through the finite-prefix classifier to descend from $m+n$ to
$n$. The visible endpoints at index $n$ recover a unary denominator tail, hence
$\Eone(d)$ is a rational-history carrier and $\Eone(\Eone(d))$ is a constant
real-history carrier. The unit-exponent prime-scale readback identifies
$r$ with $p$, and applying the fixed real append context gives the displayed
adele endpoint readback.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_long\_prefix\_visible\_unit\_scale\_readback}

\begin{theorem}[Adele long-prefix visible scale result is not empty]
\label{thm:adele-real-stream-prefix-long-prefix-visible-scale-result-nonempty}
Let $x,y:\mathbb{N}\to\mathsf{BHist}$ be real stream representatives. If
$\mathsf{RealStreamPrefixClassifier}(x,y,m+n)$ holds, $x(n)\hsame
\Eone(\Eone(d))$, $y(n)\hsame\Eone(i)$, and the concrete first-prime
$p$-adic visible scale
$\mathsf{PadicPrimeScale}(\Eone(\Eone(\emp)),\Eone(e),r)$ holds, then
$\mathsf{RealStreamPrefixClassifier}(x,y,n)$ holds,
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(\Eone(\Eone(d)),r)),
$$
and the visible scale result is not history-same as $\emp$:
$$
\hsame(r,\emp)\to\bot.
$$
\end{theorem}
\begin{proof}
Induct on $m$ through the prefix classifier to descend from $m+n$ to $n$.
The selected one-extension endpoints read back to a unary denominator tail,
hence to a rational carrier and then to the constant real-history carrier
$\Eone(\Eone(d))$. The visible $p$-adic scale theorem packages this real
component with the displayed scale and separates the scale result from
$\emp$.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_long\_prefix\_visible\_scale\_result\_nonempty}

\begin{theorem}[Adele long-prefix visible zero-scale result is not empty]
\label{thm:adele-real-stream-prefix-long-prefix-visible-zero-scale-result-nonempty}
Let $x,y:\mathbb{N}\to\mathsf{BHist}$ be real stream representatives. If
$\mathsf{RealStreamPrefixClassifier}(x,y,m+n)$ holds, $x(n)\hsame
\Eone(\Eone(d))$, $y(n)\hsame\Eone(i)$, and the concrete first-prime
$p$-adic visible zero scale
$\mathsf{PadicPrimeScale}(\Eone(\Eone(\emp)),\Ezero(e),r)$ holds, then
$\mathsf{RealStreamPrefixClassifier}(x,y,n)$ holds,
$$
\mathsf{AdeleHistoryCarrier}(\mathsf{append}(\Eone(\Eone(d)),r)),
$$
and the visible zero-scale result is not history-same as $\emp$:
$$
\hsame(r,\emp)\to\bot.
$$
\end{theorem}
\begin{proof}
Induct on $m$ through the prefix classifier to descend from $m+n$ to $n$.
The selected one-extension endpoints read back to a unary denominator tail,
hence to a rational carrier and then to the constant real-history carrier
$\Eone(\Eone(d))$. The visible zero-scale theorem packages this real component
with the displayed $p$-adic scale and separates the scale result from $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_long\_prefix\_visible\_zero\_scale\_result\_nonempty}

\begin{theorem}[Adele long-prefix visible scale continuation result is not empty]
\label{thm:adele-real-stream-prefix-long-prefix-visible-scale-cont-result-nonempty}
Let $x,y:\mathbb{N}\to\mathsf{BHist}$ be real stream representatives. If
$\mathsf{RealStreamPrefixClassifier}(x,y,m+n)$ holds, $x(n)\hsame
\Eone(\Eone(d))$, $y(n)\hsame\Eone(i)$, and the concrete first-prime
$p$-adic visible scale
$\mathsf{PadicPrimeScale}(\Eone(\Eone(\emp)),\Eone(e),r)$ holds, then every
continuation out of the appended visible scale endpoint has a non-empty
result:
$$
\ContR(\mathsf{append}(\Eone(\Eone(d)),r),k,out)
\Longrightarrow
\hsame(out,\emp)\to\bot.
$$
\end{theorem}
\begin{proof}
The long-prefix visible scale result theorem supplies an adele-history carrier
for $\mathsf{append}(\Eone(\Eone(d)),r)$. Applying the adele carrier
continuation-result nonemptiness theorem to the displayed continuation rules
out $out\hsame\emp$.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_long\_prefix\_visible\_scale\_cont\_result\_nonempty}

\begin{theorem}[Adele long-prefix visible scale append is not empty]
\label{thm:adele-real-stream-prefix-long-prefix-visible-scale-append-not-empty}
Under the hypotheses of \autoref{thm:adele-real-stream-prefix-long-prefix-visible-scale-carrier}, the appended visible scale endpoint is not history-same as $\emp$:
$$
\hsame(\mathsf{append}(\Eone(\Eone(d)),r),\emp)\to\bot.
$$
\end{theorem}
\begin{proof}
The long-prefix visible scale carrier theorem supplies the adele-history carrier
for the displayed append endpoint. Adele-history carriers cannot be
history-same as $\emp$, so the assumed empty append endpoint is impossible.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealStreamPrefix\_long\_prefix\_visible\_scale\_append\_not\_empty}

\begin{theorem}[Real--padic empty-result split]
\label{thm:adele-real-padic-e1-append-empty-padic-result}
Let the two real coordinates be displayed as endpoint histories
\(\Eone(d)\) and \(\Eone(e)\), and let two \(p\)-adic prime-scale factors
\(\mathsf{PadicPrimeScale}(p,w,n)\) and
\(\mathsf{PadicPrimeScale}(p,q,r)\) be linked by
\(\mathsf{Cont}(n,r,out)\). Then \(out\hsame\emp\) exactly when the real
endpoint payloads are rational carriers and both \(p\)-adic exponent factors
satisfy \(w\hsame\emp\) and \(q\hsame\emp\).
\end{theorem}
\begin{proof}
The endpoint theorem for \(\mathsf{RealConstantHistoryCarrier}\) reads
\(\Eone(d)\) and \(\Eone(e)\) back to rational-carrier payloads. The
\(p\)-adic append empty-result theorem applies to the two prime-scale factors
and the continuation witness, splitting \(out\hsame\emp\) into the two empty
exponent conditions. Combining these independent readbacks gives the displayed
equivalence.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleRealPadic\_e1\_append\_empty\_padic\_result\_iff}

\begin{theorem}[Adele constant-slice carrier transport]
\label{thm:adele-constant-slice-carrier-hsame-transport}
Let $\mathsf{AdeleConstantSliceCarrier}(h)$ mean that histories $d,p,e,r$
exist with
$$
\hsame(h,\Eone(d))\ \wedge\ \mathsf{RatHistoryCarrier}(d)\ \wedge\
\mathsf{PadicPrimeScale}(p,e,r).
$$
If $h\hsame h'$ and $\mathsf{AdeleConstantSliceCarrier}(h)$, then
$\mathsf{AdeleConstantSliceCarrier}(h')$.
\end{theorem}
\begin{proof}
Unpack the constant-slice witnesses $d,p,e,r$. The rational endpoint and the
$p$-adic scale witnesses are unchanged. Symmetry and transitivity of $\hsame$
move the displayed $\Eone(d)$ endpoint from $h$ to $h'$.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleConstantSliceCarrier\_hsame\_transport}

\section{The certificate}
\label{sec:adele-certificate}

\begin{theorem}[Adele history-carrier semantic name certificate]
\label{thm:adele-history-carrier-semantic-namecert}
The adele history carrier forms a semantic naming certificate with $\hsame$ as
classifier, using the same adele carrier as source, pattern, and ledger policy.
\end{theorem}
\begin{proof}
A rational tail $\Eone(\emp)$ supplies the constant-real side, and the
first-prime unit scale supplies the $p$-adic side, so the adele carrier is
inhabited by the displayed append endpoint from
\autoref{thm:adele-history-carrier-first-prime-unit-scale}. The classifier
laws are the BEDC $\hsame$ equivalence laws. For carrier transport, unpack an
adele witness into its real component, prime, exponent, result, and append
comparison; composing that comparison with the symmetric endpoint $\hsame$
moves the same witnesses to the target history. Pattern and ledger soundness
are identity maps on the carrier predicate.
\end{proof}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_semanticNameCert}

\begin{theorem}[Adele visible-scale append result hsame transport]
\label{thm:adele-history-carrier-visible-scale-append-result-hsame-transport}
For histories $a,a',s:\mathsf{BHist}$, if $a$ is an adele-history carrier,
the append endpoint $\mathsf{append}(a,s)$ is not history-same as $\emp$, and
$a\hsame a'$, then the same visible scale $s$ has a nonempty append endpoint
over $a'$:
$$
\mathsf{AdeleHistoryCarrier}(a)\land
(\hsame(\mathsf{append}(a,s),\emp)\to\bot)\land
 a\hsame a'
\Longrightarrow
\hsame(\mathsf{append}(a',s),\emp)\to\bot.
$$
\end{theorem}
\leanchecked{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_visible\_scale\_append\_result\_hsame\_transport}
\begin{proof}
The carrier premise fixes the source endpoint in the adele layer. Let
$H$ be the nonempty premise for $\mathsf{append}(a,s)$ and let
$\alpha:a\hsame a'$. By the reverse direction of
\autoref{thm:append-hsame-right-context-cancel}, applied with the reflexive
right-context comparison $s\hsame s$, the endpoint comparison $\alpha$ gives
$$
\hsame(\mathsf{append}(a,s),\mathsf{append}(a',s)).
$$
If $\mathsf{append}(a',s)\hsame\emp$, transitivity of $\hsame$ carries this
empty comparison back to $\mathsf{append}(a,s)\hsame\emp$, contradicting $H$.
\end{proof}

\closureat{AdeleUp}{obligationStr}[BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_semanticNameCert]

\begin{closurestatus}{\AdeleUp}
  \theoryclosure{\obligationClosure}
  \scopeclosed{The obligation inventory covers the adele carrier, nonempty carrier witnesses, append and $\mathsf{Cont}$ nonempty transport, real and $p$-adic scale readback rows, $\hsame$ carrier transport, and semantic certificate rows.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.AdeleUp.AdeleHistoryCarrier\_semanticNameCert}
  \bridgestatus{none}
  \notclaimed{This does not close local-global product formulae, automorphic theory, analytic continuation of $L$-functions, or a standard-model bridge.}
  \upgradepath{$\scopedClosure$ requires binding each obligation row to the exact BEDC primitives $\mathsf{BHist}$, $\mathsf{Cont}$, $\mathsf{append}$, $\hsame$, the RealUp constant side, and the PadicPrimeScale side.}
\end{closurestatus}
