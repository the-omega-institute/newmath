\chapter{A Concrete Naming Certificate for $\HausdorffMetricCompletionUniversalUp$}
\label{ch:concrete-instances-hausdorff-metric-completion-universal-namecert}
\origin{human}

Following Bishop--Bridges constructive completions and the Bourbaki--Kelley universal-mapping account of Hausdorff metric completion, the $\HausdorffMetricCompletionUniversalUp$ packet records the separated metric-completion universal property as finite BEDC NameCert data. It refines the metric extension route of \autoref{ch:concrete-instances-metric-completion-universal-property-namecert}, the Hausdorff reflection boundary of \autoref{ch:concrete-instances-hausdorff-reflection-namecert}, and the separated metric reflection route of \autoref{ch:concrete-instances-separatedmetricreflection-namecert}. Its Real-facing source reads are exactly the displayed rows of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}; it does not assert quotient completions, selected Cauchy limits, host equality of completed points, or ambient $\RealUp$ completeness.

\begin{definition}[HausdorffMetricCompletionUniversal carrier]
\label{def:hausdorff-metric-completion-universal-carrier}
A $\HausdorffMetricCompletionUniversalUp$ carrier is a finite $\BHist$ packet
$$
U=(M,V,S,Q,R,W,D,A,E,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricCompletionUniversalPropertyUp$ route that supplies the metric completion extension surface.
\item $V$ is the $\HausdorffReflectionUp$ boundary row through which indistinguishable completion reads are separated before public comparison.
\item $S$ is the $\SeparatedMetricReflectionUp$ row that turns zero-distance evidence into a separated metric comparison.
\item $Q$ is the $\StreamNameUp$ finite-window source row for Cauchy names.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row.
\item $W$ is the $\DyadicRatCoreUp$ tolerance ledger used by the finite-window comparisons.
\item $D$ is the dense-source handoff row from the metric source into the completion seal.
\item $A$ is the terminal $\RealUp$ seal-facing row reached only after $Q,R,W,D$ are displayed.
\item $E$ is the extension row for uniformly continuous maps into separated metric targets.
\item $L$ is the uniqueness ledger comparing two extension rows after their dense-source reads and separated target rows agree.
\item $H$ records componentwise $\hsame$ transport for the universal-property, Hausdorff reflection, separated reflection, source, readback, tolerance, dense handoff, seal, extension, uniqueness, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
M,V,S,Q,R,W,D,A \longmapsto E,L,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\HausdorffMetricCompletionUniversalUp$, $\MetricCompletionUniversalPropertyUp$, $\HausdorffReflectionUp$, $\SeparatedMetricReflectionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HausdorffMetricCompletionUniversalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,V,S,Q,R,W,D,A,E,L,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives of Cauchy streams, hidden tail selectors, host real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[HausdorffMetricCompletionUniversal NameCert obligations]
\label{thm:hausdorff-metric-completion-universal-namecert-obligations}
For every accepted $\HausdorffMetricCompletionUniversalUp$ carrier $U=(M,V,S,Q,R,W,D,A,E,L,H,C,P,N)$, the local $\NameCert_{\HausdorffMetricCompletionUniversalUp}$ surface consists exactly of carrier admission; metric universal-property exposure through $M$; Hausdorff and separated reflection through $V,S$; finite source naming through $Q,R,W$; dense-source handoff through $D$; Real seal-facing read through $A$; extension and uniqueness rows $E,L$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hausdorff-metric-completion-universal-carrier}. Its coordinates are exactly the universal-property row, Hausdorff and separated reflection rows, finite stream-name source rows, dense handoff, Real seal, extension, uniqueness, transport, replay, provenance, and local naming rows.

The metric-completion universal-property read is confined to $M$, while $V$ and $S$ are the only reflection rows available to a separated target. The source side is confined to the displayed $Q,R,W,D,A$ route, so the extension row cannot inspect a quotient of Cauchy names or a selected completed point.

Finally $E,L,H,C,P,N$ repack the extension, uniqueness, transport, replay, provenance, and naming obligations over the same displayed rows. Since the carrier has no other coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hausdorff separated extension route]
\label{thm:hausdorff-metric-completion-universal-separated-extension}
Every separated-target extension read exported by an accepted $\HausdorffMetricCompletionUniversalUp$ carrier factors through the displayed finite route
$$
\MetricCompletionUniversalPropertyUp
\longmapsto \HausdorffReflectionUp
\longmapsto \SeparatedMetricReflectionUp
\longmapsto \StreamNameUp
\longmapsto \RegSeqRatUp
\longmapsto \DyadicRatCoreUp
\longmapsto \RealUp .
$$
The consumer receives an extension row and a uniqueness ledger only after that route is displayed; it receives no quotient completion, no chosen Cauchy limit, no host real equality, no hidden tail selector, and no ambient completeness theorem for $\RealUp$.
\end{theorem}
\begin{proof}
Start with carrier admission from \autoref{def:hausdorff-metric-completion-universal-carrier}. The universal-property row $M$ supplies the completion-facing extension surface, but its public comparison is not separated until the Hausdorff and separated reflection rows $V,S$ are read.

Next read the source rows $Q,R,W,D,A$. They expose only the finite stream windows, regular rational readback, dyadic tolerance ledger, dense-source handoff, and terminal Real seal named by the sibling chapters.

Apply the NameCert obligations of \autoref{thm:hausdorff-metric-completion-universal-namecert-obligations}. They place the extension row $E$ and uniqueness ledger $L$ downstream of those displayed rows and keep transport, replay, provenance, and local naming inside $H,C,P,N$. The carrier contains no quotient, selector, host equality, or ambient-completeness coordinate, so the stated route is the only extension export.
\end{proof}

\falsifiablePrediction{Every accepted $\HausdorffMetricCompletionUniversalUp$ consumer reads MetricCompletionUniversalProperty, HausdorffReflection, SeparatedMetricReflection, StreamName, RegSeqRat, DyadicRatCore, Real, dense-source handoff, extension, uniqueness, transport, replay, provenance, and local naming rows before citing the separated metric-completion universal property.}

\independenceWitness{metric-completion-universal-property, hausdorff-reflection, separatedmetricreflection, streamname, regseqrat, dyadicratcore, real: $\HausdorffMetricCompletionUniversalUp$ is not bijective to any listed sibling because it jointly carries separated metric-completion extension, Hausdorff reflection, separated reflection, finite source naming, dense handoff, Real seal, uniqueness, transport, replay, provenance, and local naming as one universal-property packet.}

\closureat{HausdorffMetricCompletionUniversalUp}{seedStr}

\begin{closurestatus}{\HausdorffMetricCompletionUniversalUp}
  \constructivestory{A $\HausdorffMetricCompletionUniversalUp$ packet is generated from a MetricCompletionUniversalProperty route, HausdorffReflection boundary, SeparatedMetricReflection row, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance ledger, dense-source handoff, Real seal, extension row, uniqueness ledger, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hausdorff-metric-completion-universal-carrier}, \autoref{thm:hausdorff-metric-completion-universal-namecert-obligations}, and \autoref{thm:hausdorff-metric-completion-universal-separated-extension} seed the separated metric-completion universal-property route over $\MetricCompletionUniversalPropertyUp$, $\HausdorffReflectionUp$, $\SeparatedMetricReflectionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, selected Cauchy limits, host equality of completed points, countable-choice schedules, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, reflection handoff, separated-target extension, finite source naming, uniqueness ledger, transport stability, replay stability, provenance non-escape, and local naming for BEDC.Derived.HausdorffMetricCompletionUniversalUp.}
\end{closurestatus}
