\chapter{A Concrete Naming Certificate for $\BanachSaksUp$}
\label{ch:concrete-instances-banach-saks-namecert}
\origin{human}

Following Banach and Saks on Cesaro-convergent subsequences in Banach spaces, the $\BanachSaksUp$ packet records the finite NameCert surface by which a bounded Banach-space sequence, a weak-compactness handoff, a Cesaro block schedule, and normed real readback are exposed before any consumer treats the route as a Banach--Saks conclusion. It sits after the Banach carrier of \autoref{ch:concrete-instances-banach-namecert}, the weak-compactness packet of \autoref{ch:concrete-instances-weak-compactness-namecert}, the bounded real sequence source of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite Cesaro-subsequence certificate; it does not choose an infinite subsequence in the host, quotient sequence equality, countable choice, weak compactness itself, or ambient $\RealUp$ completeness.

\begin{definition}[Banach--Saks carrier]
\label{def:banach-saks-carrier}
A $\BanachSaksUp$ carrier is a finite $\BHist$ packet
$$
S=(B,X,W,A,Q,R,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BanachUp$ source row, naming the normed complete carrier through the already displayed Banach packet.
\item $X$ is the bounded Banach sequence source row, read through the bounded sequence frontier and not through a host sequence object.
\item $W$ is the $\WeakCompactnessUp$ handoff row, recording the weakly compact source surface that licenses a subsequence obligation without selecting a host subsequence.
\item $A$ is the finite Cesaro block schedule, listing the inspected block indices and averaging windows.
\item $Q$ is the norm and $\RealUp$ readback row for the displayed Cesaro averages.
\item $R$ is the $\RegSeqRatUp$ route by which the real-valued norm observations are read as regular rational windows.
\item $T$ records transport from the bounded-source and weak-compactness rows to the Cesaro schedule and real readback rows.
\item $H$ records componentwise $\hsame$ transport for $B,X,W,A,Q,R,T$ and the local naming row.
\item $C$ records displayed $\Cont$ replay through the bounded source, weak-compactness handoff, Cesaro schedule, norm readback, regular rational route, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\BanachSaksUp$, $\BanachUp$, $\WeakCompactnessUp$, $\BoundedRealSequenceUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BanachSaksUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,X,W,A,Q,R,T,H,C,P,N$. It has no coordinate for host subsequence choice, quotient sequence equality, countable choice, weak compactness as a theorem, completed Cesaro convergence outside the displayed windows, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Banach--Saks NameCert obligations]
\label{thm:banach-saks-namecert-obligations}
For every accepted $\BanachSaksUp$ carrier $S=(B,X,W,A,Q,R,T,H,C,P,N)$, the local $\NameCert_{\BanachSaksUp}$ surface consists exactly of Banach source exposure through $B$; bounded sequence exposure through $X$; weak-compactness handoff through $W$; finite Cesaro block scheduling through $A$; norm and $\RealUp$ readback through $Q$; $\RegSeqRatUp$ routing through $R$; transport through $T$ and $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:banach-saks-carrier}. The rows $B,X,W,A,Q,R,T,H,C,P,N$ are exactly the Banach source, bounded source, weak-compactness handoff, Cesaro schedule, readback, route, transport, replay, provenance, and naming coordinates.

The analytic content is split between the source rows and the schedule rows. The rows $B,X,W$ expose the Banach, boundedness, and weak-compactness data, while $A,Q,R$ expose only finite Cesaro averages and finite real readback.

Finally $T,H,C,P,N$ preserve and name the same rows. Since no coordinate names a host subsequence selector, quotient sequence equality, countable-choice object, independent weak-compactness theorem, or ambient completeness principle, the displayed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Banach--Saks Cesaro subsequence route]
\label{thm:banach-saks-cesaro-subsequence-route}
Every Cesaro-subsequence consumer of an accepted $\BanachSaksUp$ packet must read the route
$$
B,X,W,A,Q,R,T,H,C,P,N
$$
in that order: the $\BanachUp$ source row and bounded sequence row precede the $\WeakCompactnessUp$ handoff, the handoff precedes the displayed Cesaro block schedule, and the schedule precedes normed $\RealUp$ readback and $\RegSeqRatUp$ routing. The route exports no host subsequence choice, quotient sequence equality, countable choice, weak-compactness theorem, selected limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the source rows. The Banach carrier $B$ and bounded sequence row $X$ are the only sequence-source coordinates available to the packet.

Next read the weak-compactness handoff $W$ and the finite schedule $A$. The handoff names the obligation surface, while $A$ records only displayed block indices and averaging windows.

The readback rows $Q$ and $R$ then expose norm observations as finite real and regular-rational windows. Transport, replay, provenance, and local naming through $T,H,C,P,N$ preserve the same route. Therefore the accepted packet supplies a finite Cesaro-subsequence route and none of the refused host principles.
\end{proof}

\falsifiablePrediction{Every accepted $\BanachSaksUp$ consumer reaches Cesaro-subsequence data only through the displayed Banach source, bounded sequence source, weak-compactness handoff, Cesaro block schedule, norm and Real readback, RegSeqRat route, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{banach, weakcompactness, boundedrealsequence, regseqrat, real: none is bijective with $\BanachSaksUp$ because this packet alone binds a bounded Banach sequence to a weak-compactness handoff and a finite Cesaro block schedule before the normed Real readback route is exposed.}

\closureat{BanachSaksUp}{seedStr}

\begin{closurestatus}{\BanachSaksUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:banach-saks-carrier}, \autoref{thm:banach-saks-namecert-obligations}, and \autoref{thm:banach-saks-cesaro-subsequence-route} seed the finite Cesaro-subsequence NameCert route over $\BanachUp$, $\WeakCompactnessUp$, $\BoundedRealSequenceUp$, $\RegSeqRatUp$, and $\RealUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host subsequence choice, quotient sequence equality, countable choice, weak compactness as a theorem, selected Cesaro limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-source exactness, weak-compactness handoff, Cesaro schedule exactness, norm and Real readback, RegSeqRat routing, transport, replay, provenance, and local naming for BEDC.Derived.BanachSaksUp.}
  \constructivestory{A $\BanachSaksUp$ packet is generated from a Banach source row, bounded sequence source, weak-compactness handoff, finite Cesaro block schedule, normed Real readback, RegSeqRat route, transport rows, displayed replay, provenance, and local naming.}
\end{closurestatus}
