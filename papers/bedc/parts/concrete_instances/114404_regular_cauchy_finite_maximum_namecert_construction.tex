\chapter{A Concrete Naming Certificate for $\RegularCauchyFiniteMaximumUp$}
\label{ch:concrete-instances-regular-cauchy-finite-maximum-namecert}
\origin{human}

Following Bishop and Bridges on constructive finite suprema of regular Cauchy real names, the $\RegularCauchyFiniteMaximumUp$ packet records a finite-family maximum route as displayed comparison-tree data. It sits after the regular rational source surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the binary maximum packet of \autoref{ch:concrete-instances-regular-cauchy-maximum-namecert}, the finite product packet of \autoref{ch:concrete-instances-regular-cauchy-finite-product-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The certificate gives compact finite-net, Dini, and uniform-modulus consumers a named finite maximum object without importing trichotomy, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy finite maximum carrier]
\label{def:regular-cauchy-finite-maximum-carrier}
A $\RegularCauchyFiniteMaximumUp$ carrier is a finite $\BHist$ packet
$$
F=(I,S,W,D,T,B,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite unary index row, listing exactly the component slots opened by the maximum request.
\item $S$ is the component $\RegSeqRatUp$ source family, one row for each slot of $I$.
\item $W$ is the synchronized $\StreamNameUp$ window family for the component reads, intermediate pairwise maximum reads, and final maximum read.
\item $D$ is the $\DyadicRatCoreUp$ comparison ledger, storing the dyadic observations and tolerance cells used at each finite branch.
\item $T$ is the finite comparison-tree row. Each internal node cites a displayed $\RegularCauchyMaximumUp$ route over two previously displayed child rows.
\item $B$ records branch exactness: every leaf of $T$ is a source row from $S$, every internal branch consumes two earlier nodes, and the root is the only final maximum output.
\item $R$ is the $\RegSeqRatUp$ readback row for the root of the comparison tree, obtained only from $S,W,D,T,B$.
\item $E$ is the $\RealUp$ seal row reached after the root readback $R$ has been displayed.
\item $H$ records componentwise $\hsame$ transport for the index row, sources, windows, dyadic comparison ledger, comparison tree, branch exactness row, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from any finite maximum request through the same rows.
\item $P$ records $\Pkg$ provenance over $\RegularCauchyFiniteMaximumUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyMaximumUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyFiniteMaximumUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,S,W,D,T,B,R,E,H,C,P,N$. It has no coordinate for a host finite supremum, trichotomy, quotient real equality, selected limit, countable-choice schedule, ambient $\RealUp$ completeness theorem, Lean verification, or standard bridge.
\end{definition}

\begin{theorem}[Regular Cauchy finite maximum NameCert obligations]
\label{thm:regular-cauchy-finite-maximum-namecert-obligations}
For every accepted $\RegularCauchyFiniteMaximumUp$ carrier
$$
F=(I,S,W,D,T,B,R,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyFiniteMaximumUp}$ surface consists exactly of carrier admission for the displayed finite packet, finite comparison-tree exactness through $T$ and $B$, coordinate readback stability through $S,W,D,R$, $\RealUp$-seal nonescape through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:regular-cauchy-finite-maximum-carrier}. Carrier admission is exactly admission of the rows $I,S,W,D,T,B,R,E,H,C,P,N$.

Comparison-tree exactness is read from $T$ and $B$. The index row $I$ names the leaves, the source family $S$ supplies those leaves, and each internal node of $T$ cites a binary $\RegularCauchyMaximumUp$ route over two earlier displayed rows. The branch row $B$ records that the root is the only final output.

Coordinate readback stability is downstream of the same tree. The synchronized window family $W$ and dyadic ledger $D$ are the only rows from which component and intermediate observations may be read, so the root readback $R$ cannot depend on an unstated coordinate or a selected representative.

The seal row $E$ is reached only after $R$ has been exposed. The transport, replay, provenance, and naming rows $H,C,P,N$ preserve and cite this displayed route; none adds a host finite supremum, trichotomy, quotient real equality, selected limit, countable-choice schedule, ambient completeness theorem, Lean verification, or bridge credential. Thus the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy finite maximum comparison-tree route]
\label{thm:regular-cauchy-finite-maximum-comparison-tree-route}
Every accepted $\RegularCauchyFiniteMaximumUp$ read factors through the finite route
$$
I,S \longmapsto W,D \longmapsto T,B \longmapsto R \longmapsto E,H,C,P,N .
$$
In particular, the finite-family maximum is obtained by iterated displayed $\RegularCauchyMaximumUp$ rows, not by host trichotomy, quotient real equality, a selected limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the finite index and source rows. The request first names the slots in $I$ and reads the matching component $\RegSeqRatUp$ rows in $S$.

The observation data is then synchronized by $W$ and measured by $D$. These rows provide the finite windows and dyadic tolerances used by every binary comparison in the displayed tree.

The comparison tree $T$ and branch row $B$ order the binary maximum calls. Each internal node is a $\RegularCauchyMaximumUp$ route over already displayed child rows, and the root is the only row available for the final readback $R$.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same root route. Since no excluded host or quotient object is a coordinate of the carrier, every accepted read factors through the displayed finite tree.
\end{proof}

\begin{theorem}[Regular Cauchy finite maximum coordinate stability]
\label{thm:regular-cauchy-finite-maximum-coordinate-stability}
If two accepted $\RegularCauchyFiniteMaximumUp$ carriers have the same finite index row, component $\RegSeqRatUp$ classifiers, synchronized $\StreamNameUp$ windows, $\DyadicRatCoreUp$ comparison ledger, comparison tree, and branch exactness row, then their root $\RegSeqRatUp$ readback and terminal $\RealUp$ seal are the same displayed route. A coordinate-facing consumer cannot separate the packets except through one of those displayed rows.
\end{theorem}
\begin{proof}
Project the two carriers through \autoref{def:regular-cauchy-finite-maximum-carrier}. The classifier has no coordinate outside the index, source, window, dyadic ledger, comparison-tree, branch, readback, seal, transport, replay, provenance, and local naming rows.

Use the comparison-tree route theorem \autoref{thm:regular-cauchy-finite-maximum-comparison-tree-route}. Once $I,S,W,D,T,B$ agree, the same iterated $\RegularCauchyMaximumUp$ nodes are read in the same order, so the root readback row $R$ is forced to be the same displayed $\RegSeqRatUp$ route.

The terminal seal $E$ is downstream of $R$, and $H,C,P,N$ only transport, replay, source, and name that route. Therefore a coordinate-facing consumer has no additional row from which to separate the packets.
\end{proof}

\begin{theorem}[Regular Cauchy finite maximum Real seal non-escape]
\label{thm:regular-cauchy-finite-maximum-real-seal-nonescape}
For every accepted $\RegularCauchyFiniteMaximumUp$ carrier, the $\RealUp$ seal row exposes only the displayed root readback of the finite comparison tree. It exports no host finite supremum, trichotomy, quotient real equality, selected limit, countable-choice schedule, ambient $\RealUp$ completeness principle, Lean verification, or standard bridge.
\end{theorem}
\begin{proof}
Inspect the carrier boundary. The seal row $E$ appears only after the index, source, window, dyadic comparison, comparison-tree, branch exactness, and root readback rows have been displayed.

The structural rows $H,C,P,N$ preserve the same route. Transport and replay can move the displayed packet, provenance can cite its dependencies, and local naming can name it, but none supplies an independent finite supremum or quotient-equality coordinate.

Since every accepted read factors through \autoref{thm:regular-cauchy-finite-maximum-comparison-tree-route}, the $\RealUp$ seal cannot escape the comparison tree or import any excluded completion principle.
\end{proof}

\falsifiablePrediction{Every $\RegularCauchyFiniteMaximumUp$ read factors through a finite unary index row, component RegSeqRat sources, synchronized StreamName windows, DyadicRatCore comparison cells, iterated RegularCauchyMaximum rows, a root RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{regseqrat, streamname, dyadicratcore, regular-cauchy-maximum, regular-cauchy-finite-product, real: $\RegularCauchyFiniteMaximumUp$ is not bijective to any listed sibling because it jointly carries a finite index row, component source family, synchronized observation windows, dyadic comparison ledger, comparison-tree exactness, root readback, RealUp sealing, transport, continuation, provenance, and local naming.}

\closureat{RegularCauchyFiniteMaximumUp}{seedStr}

\begin{closurestatus}{\RegularCauchyFiniteMaximumUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-finite-maximum-carrier}, \autoref{thm:regular-cauchy-finite-maximum-namecert-obligations}, \autoref{thm:regular-cauchy-finite-maximum-comparison-tree-route}, \autoref{thm:regular-cauchy-finite-maximum-coordinate-stability}, and \autoref{thm:regular-cauchy-finite-maximum-real-seal-nonescape} seed the finite maximum route from displayed regular Cauchy sources through comparison-tree rows, root readback, and RealUp sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host finite suprema, trichotomy, quotient real equality, selected limits, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, comparison-tree exactness, coordinate readback stability, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyFiniteMaximumUp.}
  \constructivestory{A $\RegularCauchyFiniteMaximumUp$ packet is generated from a finite unary index row, component RegSeqRat source rows, synchronized StreamName windows, DyadicRatCore comparison cells, a comparison tree of RegularCauchyMaximum rows, branch exactness, root RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
