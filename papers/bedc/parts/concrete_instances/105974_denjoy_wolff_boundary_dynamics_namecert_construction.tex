\chapter{A Concrete Naming Certificate for $\DenjoyWolffBoundaryDynamicsUp$}
\label{ch:concrete-instances-denjoy-wolff-boundary-dynamics-namecert}
\origin{ai}

The $\DenjoyWolffBoundaryDynamicsUp$ packet realizes the Denjoy--Wolff-style fixed-end reading of \autoref{sec:hyperbolic-disk-dynamics-fixed-end} in \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It consumes the disk-coordinate surface of \autoref{ch:concrete-instances-unitdisk-namecert}, the hyperbolic metric-read rows of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the disk phase-transport route of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, and the boundary-tangent comparison rows of \autoref{ch:concrete-instances-hyperbolic-horocycle-namecert}. The packet names boundary dynamics without importing a completed ideal boundary, a classical holomorphic self-map theorem, or an observer substance.

\begin{definition}[DenjoyWolffBoundaryDynamics carrier]
\label{def:denjoy-wolff-boundary-dynamics-carrier}
A $\DenjoyWolffBoundaryDynamicsUp$ carrier is a finite $\BHist$ packet
$$
W=(D,M,T,B,R,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\UnitDiskUp$ disk-map row, including the finite source observations and target observations accepted by the packet.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row used to compare the accepted disk observations.
\item $T$ is the $\PoincareDiskPhaseTransportUp$ route row, recording how the disk observations move through displayed phase transport.
\item $B$ is the boundary-read row, recording the finite boundary-facing observation used as the fixed far-end.
\item $R$ is the nonexpansive-route row, recording that every accepted step is read through the metric and transport rows before the boundary far-end is inspected.
\item $F$ is the fixed-boundary row, recording stability of the same boundary read under the displayed route.
\item $H$ records componentwise $\hsame$ transport for disk-map, metric-read, phase-transport, boundary-read, nonexpansive-route, fixed-boundary, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from $D,M,T$ into $B,R,F$ and then into provenance and naming.
\item $P$ is the $\Pkg$ provenance over $\DenjoyWolffBoundaryDynamicsUp$, the four sibling chapters named above, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DenjoyWolffBoundaryDynamicsUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host ideal-boundary quotient, a selected fixed point, a global contraction theorem, a physical horizon, or a completed disk compactification.
\end{definition}

\begin{theorem}[DenjoyWolffBoundaryDynamics NameCert obligations]
\label{thm:denjoy-wolff-boundary-dynamics-namecert-obligations}
Every accepted $\DenjoyWolffBoundaryDynamicsUp$ packet satisfies the following finite obligations:
\begin{enumerate}
\item carrier admission exposes exactly the disk-map, metric-read, phase-transport, boundary-read, nonexpansive-route, fixed-boundary, transport, replay, provenance, and local naming rows of \autoref{def:denjoy-wolff-boundary-dynamics-carrier};
\item boundary-read exactness requires $B$ to be sourced through the displayed $\UnitDiskUp$ and $\HyperbolicMetricUp$ rows;
\item nonexpansive-route exactness requires $R$ to pass through the $\PoincareDiskPhaseTransportUp$ row before a boundary read is exported;
\item fixed-boundary stability requires $F,H,C$ to preserve the same boundary-facing read under accepted route transport;
\item nonescape prevents any consumer from replacing these finite rows by a host fixed-point theorem, selected ideal point, quotient boundary equality, completed compactification, or physical observer trajectory.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:denjoy-wolff-boundary-dynamics-carrier}. The disk-map, metric-read, and phase-transport rows are the only route by which the boundary read can be formed.

The nonexpansive-route row then records the displayed metric and transport discipline before the fixed-boundary row is inspected. Transport and replay preserve the same boundary-facing read because $H$ and $C$ have no endpoint outside the displayed rows.

Finally the provenance and naming rows repack the route as a local NameCert surface. Since the packet contains no host fixed-point theorem, selected ideal point, quotient boundary equality, completed compactification, or physical observer trajectory, those readings cannot be exported.
\end{proof}

\falsifiablePrediction{Every accepted $\DenjoyWolffBoundaryDynamicsUp$ read factors through displayed disk-map, boundary-read, nonexpansive-route, fixed-boundary, provenance, and local NameCert rows.}

\independenceWitness{unitdisk, hyperbolic-metric, poincare-disk-phase-transport, hyperbolic-horocycle: none is bijective to $\DenjoyWolffBoundaryDynamicsUp$ because this packet jointly carries disk-map observations, metric-read discipline, phase-transport routing, boundary-read data, nonexpansive-route rows, fixed-boundary stability, transport, replay, provenance, and local naming. The UnitDisk packet supplies coordinates without boundary dynamics; the HyperbolicMetric packet supplies metric reads without a fixed-boundary row; the Poincare disk phase-transport packet supplies movement without a Denjoy--Wolff far-end commitment; and the horocycle packet supplies boundary-tangent rows without nonexpansive disk-map dynamics.}

The Lean follow-up must provide \texttt{Nontrivial DenjoyWolffBoundaryDynamicsUp} and \texttt{FieldFaithful DenjoyWolffBoundaryDynamicsUp} in \texttt{lean4/BEDC/Derived/DenjoyWolffBoundaryDynamicsUp/TasteGate.lean}, keeping $D,M,T,B,R,F,H,C,P,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{DenjoyWolffBoundaryDynamicsUp}{seedStr}

\begin{closurestatus}{\DenjoyWolffBoundaryDynamicsUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:denjoy-wolff-boundary-dynamics-carrier} and \autoref{thm:denjoy-wolff-boundary-dynamics-namecert-obligations} seed the finite boundary-dynamics route over displayed disk-map, metric-read, phase-transport, boundary-read, nonexpansive-route, fixed-boundary, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close the classical Denjoy--Wolff theorem, holomorphic self-map classification, ideal-boundary quotient equality, selected fixed points, physical observer trajectories, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-read exactness, nonexpansive-route exactness, fixed-boundary stability, nonescape, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.DenjoyWolffBoundaryDynamicsUp.}
  \constructivestory{A $\DenjoyWolffBoundaryDynamicsUp$ packet is generated from UnitDisk disk-map rows, HyperbolicMetric metric reads, PoincareDiskPhaseTransport rows, a boundary-read row, a nonexpansive-route row, a fixed-boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
