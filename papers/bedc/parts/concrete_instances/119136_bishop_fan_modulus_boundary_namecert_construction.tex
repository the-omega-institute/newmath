\chapter{A Concrete Naming Certificate for $\BishopFanModulusBoundaryUp$}
\label{ch:concrete-instances-bishop-fan-modulus-boundary-namecert}
\origin{human}

Following Brouwer and Bishop--Bridges finite fan analysis, the $\BishopFanModulusBoundaryUp$ packet records the boundary object consumed when finite fan-bar data are handed to compact-uniform-continuity moduli. It sits after the finite fan-bar modulus carrier of \autoref{ch:concrete-instances-finite-fan-bar-modulus-namecert}, the fan theorem packet of \autoref{ch:concrete-instances-fantheorem-namecert}, and the finite fan theorem packet of \autoref{ch:concrete-instances-finite-fan-theorem-namecert}. It realizes the compact finite-bar boundary used by \autoref{ch:visions-compact-uniform-continuity} without adding selected infinite branches, open-cover compactness, countable choice, quotient map equality, selected limits, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop fan modulus boundary carrier]
\label{def:bishop-fan-modulus-boundary-carrier}
A $\BishopFanModulusBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(F,A,K,L,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the finite fan-bar source row, read through $\FiniteFanBarModulusUp$ and the fan theorem boundary.
\item $A$ is the admitted finite-bar boundary row, recording the prefix depth, bar classifier, and coverage witness used by downstream compact consumers.
\item $K$ is the compact finite-probe alignment row that matches fan prefixes with compact-source observations.
\item $L$ is the local modulus ledger attached to the admitted fan leaves.
\item $R$ is the positive rational lower-bound row obtained by finite recursion over the admitted leaves.
\item $U$ is the $\UniformModulusUp$ handoff row exposed after $F,A,K,L,R$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the finite-bar, boundary, compact-probe, local-modulus, lower-bound, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
F,A,K,L \longmapsto R \longmapsto U .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopFanModulusBoundaryUp$, $\FiniteFanBarModulusUp$, $\FanTheoremUp$, $\FiniteFanTheoremUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopFanModulusBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,A,K,L,R,U,H,C,P,N$.
\end{definition}

\begin{theorem}[Bishop fan modulus boundary NameCert obligations]
\label{thm:bishop-fan-modulus-boundary-namecert-obligations}
For every accepted $\BishopFanModulusBoundaryUp$ carrier $B=(F,A,K,L,R,U,H,C,P,N)$, the local $\NameCert_{\BishopFanModulusBoundaryUp}$ surface consists exactly of carrier admission, finite fan-bar source exposure, admitted boundary coverage, compact finite-probe alignment, local modulus exposure, positive rational lower-bound extraction, $\UniformModulusUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored in an open cover, selected infinite branch, countable-choice row, quotient function equality, selected limit, host compactness theorem, or ambient real-completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-fan-modulus-boundary-carrier}. The rows $F,A,K,L,R,U,H,C,P,N$ are exactly the finite fan-bar source, boundary, compact-probe, local-modulus, lower-bound, handoff, transport, replay, provenance, and local naming coordinates.

The source and boundary obligations are forced by $F$ and $A$. They expose the finite fan-bar and its admitted prefix boundary before any compact-uniform-continuity consumer can inspect a modulus row.

The rows $K,L,R$ then align the finite boundary with compact-source observations, read the local moduli, and fold their positive rational radii into a lower-bound ledger. The handoff row $U$ can consume only that displayed lower-bound row.

Transport, replay, provenance, and local naming preserve the same packet. Since the carrier has no coordinate for open-cover compactness, branch selection, countable choice, quotient function equality, selected limits, host compactness, or ambient real completeness, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Bishop fan modulus boundary finite-fan handoff]
\label{thm:bishop-fan-modulus-boundary-finite-fan-handoff}
Every compact-uniform-continuity read of an accepted $\BishopFanModulusBoundaryUp$ packet must pass through the finite fan-bar modulus route of \autoref{ch:concrete-instances-finite-fan-bar-modulus-namecert} before the $\UniformModulusUp$ handoff is exposed. The handoff factors through
$$
\FiniteFanBarModulusUp \longmapsto \BishopFanModulusBoundaryUp \longmapsto \UniformModulusUp
$$
with compact finite-probe alignment and positive rational lower-bound extraction displayed inside the boundary packet.
\end{theorem}
\begin{proof}
Begin with the obligations of \autoref{thm:bishop-fan-modulus-boundary-namecert-obligations}. They require the finite fan-bar source row $F$ and the admitted boundary row $A$ before the local-modulus and lower-bound rows can be read.

The sibling finite fan-bar modulus chapter supplies the source route for $F$. The boundary packet then records the compact-probe row $K$, the local modulus ledger $L$, and the positive lower-bound row $R$ that the compact-uniform-continuity consumer needs.

Only after those rows are displayed can $U$ expose the uniform-modulus handoff. Thus the finite fan-bar data are consumed as a boundary object rather than as an open-cover compactness principle, branch selector, chosen limit, or quotient function equality.
\end{proof}

\closureat{BishopFanModulusBoundaryUp}{seedStr}

\begin{closurestatus}{\BishopFanModulusBoundaryUp}
  \constructivestory{A $\BishopFanModulusBoundaryUp$ packet is generated from finite fan-bar source rows, admitted prefix-boundary coverage, compact finite-probe alignment, local modulus ledgers, positive rational lower-bound extraction, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-fan-modulus-boundary-carrier}, \autoref{thm:bishop-fan-modulus-boundary-namecert-obligations}, and \autoref{thm:bishop-fan-modulus-boundary-finite-fan-handoff} seed the finite fan-bar boundary consumed by compact-uniform-continuity and UniformModulus rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected infinite branches, countable choice, quotient function equality, selected limits, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite fan-bar source exposure, admitted boundary coverage, compact finite-probe alignment, local modulus exposure, rational lower-bound extraction, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.BishopFanModulusBoundaryUp.}
\end{closurestatus}
