\chapter{A Concrete Naming Certificate for $\StreamNameFiniteRequestSectionUp$}
\label{ch:concrete-instances-streamname-finite-request-section-namecert}
\origin{ai}

The $\StreamNameFiniteRequestSectionUp$ packet realizes the finite request-section route of \autoref{ch:visions-real-completeness} as a concrete four-face NameCert surface. It sits after the stream-name carrier of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic rational core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the Cauchy sequence source of \autoref{ch:concrete-instances-cauchysequencespace-namecert}. The packet packages one finite precision request before any Real-completeness consumer can treat the request as a limit, quotient stream, selected representative, or ambient completeness witness.

\begin{definition}[StreamName finite request-section carrier]
\label{def:streamname-finite-request-section-carrier}
A $\StreamNameFiniteRequestSectionUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,E,D,R,A,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ source row naming the accepted finite request window;
\item $E$ is the endpoint and precision-request row exposed to CauchySequenceSpace consumers;
\item $D$ is the $\DyadicRatCoreUp$ tolerance and mesh row for the request;
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same window;
\item $A$ is the $\RealUp$ seal row available only after the stream, dyadic, and regular-rational faces have been displayed;
\item $W$ is the four-face exactness row asserting that $S,D,R,A$ belong to the same finite request section;
\item $H$ records componentwise $\hsame$ transport for stream, endpoint, dyadic, regular-rational, Real-seal, exactness, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from StreamName admission through endpoint request, dyadic tolerance, RegSeqRat readback, Real seal, exactness, provenance, and local naming;
\item $P$ is the $\Pkg$ provenance over $\StreamNameFiniteRequestSectionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchySequenceSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\StreamNameFiniteRequestSectionUp}$ row.
\end{enumerate}
The classifier compares accepted sections only through these displayed rows. It has no coordinate for quotient stream equality, selected limit representatives, hidden tail choice, ambient metric completeness, or countable choice.
\end{definition}

\begin{theorem}[StreamName finite request-section NameCert obligations]
\label{thm:streamname-finite-request-section-namecert-obligations}
For the carrier of \autoref{def:streamname-finite-request-section-carrier}, the local $\NameCert_{\StreamNameFiniteRequestSectionUp}$ surface consists exactly of carrier admission for $Q$, finite StreamName window exposure through $S$, endpoint request exposure through $E$, dyadic tolerance exposure through $D$, regular rational readback through $R$, Real-seal exposure through $A$, four-face exactness through $W$, and structural transport, replay, provenance, and local naming through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted packet $Q$. The StreamName row $S$ and endpoint row $E$ name the finite request before any dyadic or real-facing consumer can inspect it.

The dyadic row $D$ supplies the tolerance face, and the regular rational row $R$ supplies the readback face. The Real seal row $A$ is exposed only after those finite faces have been displayed.

The exactness row $W$ binds $S,D,R,A$ as one request section. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same packet, so the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[StreamName finite request-section real handoff]
\label{thm:streamname-finite-request-section-real-handoff}
Every $\RealUp$-facing read exported by an accepted $\StreamNameFiniteRequestSectionUp$ packet factors through
$$
S,E,D,R,A,W,H,C,P,N .
$$
The handoff is the four-face StreamName, dyadic, RegSeqRat, and Real-seal route for one finite request section; it does not export a quotient stream, selected limit, hidden tail choice, countable choice, or ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Start with a Real-facing consumer. The consumer must first read the StreamName request window $S$ and the endpoint request $E$, so the request is finite before any Real seal can be addressed.

The dyadic tolerance row $D$ and RegSeqRat readback row $R$ are then paired by the exactness row $W$. Only after that pairing is visible may the terminal Real seal row $A$ be consumed.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since no coordinate stores quotient stream equality, a selected limit representative, a hidden tail choice, countable choice, or ambient real completeness, the handoff remains the displayed finite request-section packet.
\end{proof}

\falsifiablePrediction{An accepted $\StreamNameFiniteRequestSectionUp$ consumer is refuted if it reads the terminal Real seal without first displaying the StreamName window, dyadic tolerance row, RegSeqRat readback, four-face exactness row, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real, cauchysequencespace: no listed sibling supplies the full four-face request-section packet because StreamName supplies the window, DyadicRatCore supplies tolerance, RegSeqRat supplies readback, RealUp supplies the terminal seal, and this chapter binds the faces into one local NameCert row.}

\closureat{StreamNameFiniteRequestSectionUp}{seedStr}

\begin{closurestatus}{\StreamNameFiniteRequestSectionUp}
  \constructivestory{A $\StreamNameFiniteRequestSectionUp$ packet is generated from one finite StreamName request window, endpoint precision data, DyadicRatCore tolerance, RegSeqRat readback, RealUp seal, four-face exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:streamname-finite-request-section-carrier}, \autoref{thm:streamname-finite-request-section-namecert-obligations}, and \autoref{thm:streamname-finite-request-section-real-handoff} seed the finite four-face request-section route over StreamName, DyadicRatCore, RegSeqRat, RealUp, CauchySequenceSpace, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, hidden tail choice, countable choice, Lean verification, bridge checking, or completeness of $\RealUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, finite request exposure, dyadic tolerance exactness, RegSeqRat readback, Real-seal handoff, four-face exactness, transport, replay, provenance, and local naming for BEDC.Derived.StreamNameFiniteRequestSectionUp.}
\end{closurestatus}
