\begin{definition}[Bounded monotone Cauchy witness carrier]
\label{def:bounded-monotone-cauchy-witness-carrier}
A $\BoundedMonotoneCauchyWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $S$ is the bounded monotone source row, displaying the generator history, monotone comparison rows, and the finite upper-bound ledger for the source observations;
\item $F$ is the $\RegSeqRatUp$ row extracted from $S$ and read as the regular rational name exposed to completion-facing consumers;
\item $\sigma$ is the $\StreamNameUp$ window schedule that selects the inspected source and regular-sequence windows;
\item $\mu$ is the $\CauchyModulusUp$ tail witness, sending each dyadic tolerance request to a concrete finite tail window;
\item $\lambda$ is the $\DyadicRatCoreUp$ ledger containing the dyadic tolerance rows, the source upper-bound rows, and the monotone comparison checks consumed by $\mu$;
\item $I$ is the $\LocatedIntervalUp$ trap row recording the lower observation, upper guard, and tolerance enclosure read before the real seal;
\item $Q$ is the $\RealUp$ seal row reached only after the displayed regular sequence, selected window, Cauchy witness, dyadic ledger, and located trap have been exposed;
\item $H$ records componentwise $\hsame$ transports for the bounded source, regular sequence, stream schedule, Cauchy witness, dyadic ledger, located trap, and real-seal rows;
\item $C$ records the displayed $\Cont$ routes by which consumers inspect those rows;
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and source certificates;
\item $N$ is the local $\NameCert_{\BoundedMonotoneCauchyWitnessUp}$ row.
\end{enumerate}
The bounded monotone source row is displayed as
$$
\begin{gathered}
S_{\mathrm{gen}}(i): \text{the } i\text{-th generated source observation},\\
S_{\mathrm{mono}}(i,j): i \leq j \Rightarrow S_{\mathrm{gen}}(i) \leq S_{\mathrm{gen}}(j),\\
S_{\mathrm{ub}}(i): S_{\mathrm{gen}}(i) \leq U
\end{gathered}
$$
for a dyadic upper guard $U$ recorded in $\lambda$. The Cauchy witness is the explicit row
$$
\mu(\varepsilon): \text{a scheduled tail window on which } F \text{ has diameter below } \varepsilon.
$$
The carrier contains no choice-selected limit point, quotient-name equality, host real equality, unrestricted real-history range, ambient completeness theorem, trichotomy row, or open-cover compactness row.
\end{definition}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_route\_local\_name\_closure}

\begin{theorem}[Bounded monotone Cauchy witness NameCert obligations]
\label{thm:bounded-monotone-cauchy-witness-namecert-obligations}
The local $\NameCert_{\BoundedMonotoneCauchyWitnessUp}$ surface consists exactly of five rows:
\begin{enumerate}
\item carrier admission for the displayed packet of \autoref{def:bounded-monotone-cauchy-witness-carrier};
\item generator range discipline, stating that every source observation read by a consumer is routed through $S_{\mathrm{gen}}$ and the bounded monotone ledgers of $S$;
\item explicit Cauchy witness discipline, stating that every tail request is answered by the row $\mu$ and the scheduled $\StreamNameUp$ window $\sigma$;
\item located trap and $\RealUp$ seal handoff, stating that the real-seal row $Q$ is reached only through $F,\sigma,\mu,\lambda,I,H,C,P,N$;
\item ledger exhaustion, stating that all public reads of bounded monotone convergence are exhausted by the displayed generator, witness, classifier transport, package provenance, and local naming rows.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is projection of the finite packet in \autoref{def:bounded-monotone-cauchy-witness-carrier}. The packet lists the bounded source row, regular rational row, stream schedule, Cauchy witness, dyadic ledger, located trap, real seal, transports, continuations, package provenance, and local naming row.

Generator range discipline uses the displayed row $S_{\mathrm{gen}}$ and its monotone and upper-bound ledgers. Type I range pressure is discharged by that displayed generator, because no consumer ranges over an unrestricted intended class of real histories.

Explicit Cauchy witness discipline uses $\mu$ and $\sigma$ to answer each tolerance request with a concrete tail window. Type II selection pressure is discharged by this visible Cauchy witness, not by choosing a limit point from nonempty completeness data.

The seal handoff and ledger exhaustion then use $I$, $Q$, $H$, $C$, $P$, and $N$. Type III collapse pressure is discharged by $\RealUp$ classifier transport and componentwise $\hsame$ rows, not by host equality or quotient-name identification.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness generator range exactness]
\label{thm:bounded-monotone-cauchy-witness-generator-range-exactness}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every source observation exported from $W$ is exactly a displayed generator read through $S_{\mathrm{gen}}$, with its monotonicity and upper-bound certificates read through $S_{\mathrm{mono}}$ and $S_{\mathrm{ub}}$ and with dyadic upper guard data read through $\lambda$. No consumer may replace this generator range by an unrestricted real-history range, a selected limit, trichotomy data, quotient equality, or ambient completeness.
\end{theorem}
\begin{proof}
Project the source row $S$ from the carrier. The definition displays $S_{\mathrm{gen}}$, $S_{\mathrm{mono}}$, and $S_{\mathrm{ub}}$ as the generator, monotonicity, and upper-bound ledgers, with the dyadic guard recorded in $\lambda$.

Use the generator range discipline row of \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations}. It requires every source observation read by a consumer to pass through those displayed source ledgers.

Finally replay the transport and route rows $H$ and $C$ and attach the provenance rows $P$ and $N$. Since the packet has no coordinate for unrestricted real-history range, selected limit, trichotomy, quotient equality, or ambient completeness, the displayed generator range is exact.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_generator\_range\_exactness}

\begin{theorem}[Bounded monotone Cauchy witness tail modulus scope]
\label{thm:bounded-monotone-cauchy-witness-tail-modulus-scope}
For an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier $W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)$, every tail request is scoped by the Cauchy witness row $\mu$, the scheduled StreamName windows $\sigma$, and the dyadic tolerance ledger $\lambda$. The request may inspect $S_{\mathrm{gen}}$, $S_{\mathrm{mono}}$, $S_{\mathrm{ub}}$, and the regular-sequence row $F$, but it cannot use an existential completeness theorem, a selected limiting representative, quotient equality, host real equality, or trichotomy.
\end{theorem}
\begin{proof}
Read the tail witness from the carrier. The row $\mu$ answers tolerance requests, $\sigma$ chooses the inspected windows, and $\lambda$ records the dyadic tolerance and upper-bound ledgers consumed by the witness.

Apply the explicit Cauchy witness discipline from \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations}. It sends every tail request through $\mu$ and $\sigma$ rather than through an external completion assertion.

The source rows $S_{\mathrm{gen}}$, $S_{\mathrm{mono}}$, $S_{\mathrm{ub}}$ and the regular-sequence row $F$ supply the displayed observations. Transport through $H$, continuation through $C$, and provenance through $P,N$ do not add a selected limit, quotient equality, host equality, ambient completeness, or trichotomy coordinate.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_tail\_modulus\_scope}

\begin{theorem}[Bounded monotone Cauchy witness Real seal boundary]
\label{thm:bounded-monotone-cauchy-witness-real-seal-boundary}
For an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier $W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)$, every $\RealUp$ seal read through $Q$ is routed through $F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ and remains tied to the displayed bounded source row $S$. The boundary exports no selected limit, no trichotomy row, no quotient equality, no host real equality, and no ambient completeness principle.
\end{theorem}
\begin{proof}
Project the seal row $Q$ and the pre-seal rows $F,\sigma,\mu,\lambda,I$ from the carrier. The real-facing consumer reaches $Q$ only after the regular sequence, scheduled windows, tail witness, dyadic ledger, and located trap have been displayed.

Apply the located trap and $\RealUp$ seal handoff row of \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations}. It binds the seal to $F,\sigma,\mu,\lambda,I,H,C,P,N$ and keeps the source row $S$ visible through the generator range discipline.

Use the tail modulus scope and generator range exactness rows to rule out alternate inputs. The remaining transport, continuation, package, and local naming rows replay the same finite packet, so no selected limit, trichotomy, quotient equality, host real equality, or ambient completeness coordinate can cross the seal boundary.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_real\_seal\_boundary}

\begin{theorem}[Bounded monotone Cauchy witness socket separation]
\label{thm:bounded-monotone-cauchy-witness-socket-separation}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The three analysis sockets in bounded monotone convergence are separated inside $W$: Type I range pressure is read only through the displayed generator row $S$, Type II selection pressure is read only through the explicit Cauchy witness $\mu$ with the scheduled windows $\sigma$, and Type III collapse pressure is read only through $\RealUp$ classifier transport and the componentwise rows in $H$ and $C$.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_socket\_separation}
\begin{proof}
For the range site, inspect the bounded monotone source row. The generator $S_{\mathrm{gen}}$, monotone ledger $S_{\mathrm{mono}}$, and upper-bound ledger $S_{\mathrm{ub}}$ are carrier coordinates, so source reads have a displayed finite route before any regular-sequence or real-seal row is consumed.

For the selection site, inspect the Cauchy witness row. A tolerance request is routed to $\mu(\varepsilon)$ and then through the scheduled window $\sigma(\mu(\varepsilon))$, so the tail witness is present as data rather than selected from an existential completion statement.

For the collapse site, compare accepted packets only through the transported regular sequence, located trap, and real-seal classifier rows. The $\hsame$ transports and $\Cont$ routes preserve the displayed rows, while the $\RealUp$ seal is reached through classifier transport rather than host equality.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness classifier transport]
\label{thm:bounded-monotone-cauchy-witness-classifier-transport}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
and
$$
W'=(S',F',\sigma',\mu',\lambda',I',Q',H',C',P',N')
$$
be accepted $\BoundedMonotoneCauchyWitnessUp$ carriers. If the displayed classifier relates $W$ to $W'$, then the replacement transports the packet componentwise:
\begin{enumerate}
\item bounded source rows $S$ to $S'$;
\item $\RegSeqRatUp$ rows $F$ to $F'$ and $\StreamNameUp$ schedules $\sigma$ to $\sigma'$;
\item $\CauchyModulusUp$ witnesses $\mu$ to $\mu'$ and $\DyadicRatCoreUp$ ledgers $\lambda$ to $\lambda'$;
\item $\LocatedIntervalUp$ traps $I$ to $I'$ and $\RealUp$ seals $Q$ to $Q'$;
\item $\hsame$ rows $H$ to $H'$, $\Cont$ routes $C$ to $C'$, $\Pkg$ provenance $P$ to $P'$, and local $\NameCert_{\BoundedMonotoneCauchyWitnessUp}$ rows $N$ to $N'$.
\end{enumerate}

No quotient real equality, selected limit, ambient completeness theorem, trichotomy row, or open-cover compactness coordinate can enter the transported packet.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_classifier\_transport}
\begin{proof}
Project the two accepted carriers. The source, regular sequence, stream schedule, Cauchy witness, dyadic ledger, located trap, real seal, transport, continuation, provenance, and local naming rows are exactly the displayed coordinates of \autoref{def:bounded-monotone-cauchy-witness-carrier}.

Invoke the five rows of \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations}. Carrier admission fixes the packet shape, generator range discipline keeps source observations inside $S_{\mathrm{gen}}$ and $S'_{\mathrm{gen}}$, explicit Cauchy witness discipline keeps tolerance requests inside $\mu,\sigma$ and $\mu',\sigma'$, the seal handoff keeps real-facing reads routed through the located trap and $\RealUp$ seal, and ledger exhaustion lists the remaining public routes.

Finally use \autoref{thm:bounded-monotone-cauchy-witness-socket-separation}. Type I range, Type II selection, and Type III collapse sites occupy disjoint carrier sockets, so classifier replacement can only replay the listed componentwise rows. The excluded completion objects are absent from the carrier definition and from the NameCert rows, hence there is no coordinate through which they could be transported into the accepted packet.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness Real seal handoff]
\label{thm:bounded-monotone-cauchy-witness-real-seal-handoff}
For an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier $W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)$, every $\RealUp$ seal read factors through the displayed bounded source row, the $\RegSeqRatUp$ row, the scheduled $\StreamNameUp$ windows, the $\CauchyModulusUp$ tail witness, the $\DyadicRatCoreUp$ tolerance and upper-bound ledger, and the $\LocatedIntervalUp$ trap before reaching $Q$.

The handoff exports a witnessed monotone Cauchy packet, not a choice-selected limiting representative, quotient equality, host real equality, ambient completeness, trichotomy, or open-cover compactness.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_real\_seal\_handoff}
\begin{proof}
Project the carrier. The source and regular-sequence rows $S$ and $F$ display the observations available to the real-facing consumer, and $\sigma$ records the finite windows on which those observations are inspected.

Next use the Cauchy witness row $\mu$ together with the dyadic ledger $\lambda$. Each tolerance request reaches a displayed tail window with its monotone comparison and upper-bound data before any located trap is read.

The located trap $I$ is the final enclosure row before the seal. Transport through $H$ and continuation through $C$ replay only those displayed rows, with $P$ and $N$ recording provenance and local naming.

Thus the seal coordinate $Q$ receives exactly the witnessed packet described above. None of the excluded completion objects is present as a carrier coordinate or as a handoff route.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness ledger exhaustion]
\label{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}
Every public read of an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier is exhausted by the bounded monotone source rows, the regular-sequence row, the stream windows, the Cauchy witness row, the dyadic tolerance and upper-bound ledger, the located trap, the $\RealUp$ seal row, $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and the local $\NameCert$ row of \autoref{def:bounded-monotone-cauchy-witness-carrier}.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates. The source observations, monotone comparisons, upper-bound rows, regular rational name, window schedule, Cauchy witness, dyadic ledger, located trap, and real-seal row are all fields of the same finite $\BHist$ packet.

The transport and continuation fields account for the only permitted movement of those rows. The package and local naming fields record provenance and certificate membership, but they do not add analytic data.

Consequently every public read has already appeared in the displayed ledger. The packet has no remaining coordinate for a hidden range over all real histories, an unwitnessed existential limit choice, quotient-name collapse, host real equality, ambient completeness, trichotomy, or open-cover compactness.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_ledger\_exhaustion}

\begin{theorem}[Bounded monotone Cauchy witness obligation closure]
\label{thm:bounded-monotone-cauchy-witness-obligation-closure}
The carrier of \autoref{def:bounded-monotone-cauchy-witness-carrier} and the local theorems of this chapter assemble the $\NameCert_{\BoundedMonotoneCauchyWitnessUp}$ obligations for displayed bounded-source range, explicit Cauchy witness, $\StreamNameUp$ window schedule, $\DyadicRatCoreUp$ tolerance ledger, $\LocatedIntervalUp$ trap, $\RealUp$ seal handoff, classifier transport, and non-escape.

The resulting packet admits only the rows $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$. It has no coordinate for selected limits, quotient equality, host real equality, trichotomy, ambient completeness, or open-cover compactness.
\end{theorem}
\begin{proof}
Begin with the carrier and the NameCert obligation theorem. They provide carrier admission and split the public surface into generator range discipline, explicit Cauchy witness discipline, located trap with $\RealUp$ handoff, and ledger exhaustion.

For the bounded-source and Cauchy-witness parts, apply \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness} and \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope}. These force source reads through $S_{\mathrm{gen}},S_{\mathrm{mono}},S_{\mathrm{ub}}$ and force tolerance requests through $\mu,\sigma,\lambda$.

For the seal and transport parts, apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary}, \autoref{thm:bounded-monotone-cauchy-witness-socket-separation}, \autoref{thm:bounded-monotone-cauchy-witness-classifier-transport}, and \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}. These route the located trap and real seal through the displayed rows and keep the three analysis sockets separate.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. Since every public read is already exhausted by $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, the excluded completion and compactness coordinates cannot be projected from the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_obligation\_closure}

\input{parts/concrete_instances/boundedmonotonecauchywitness/shared_budget_meet}

\begin{theorem}[Bounded monotone Cauchy witness convergence-seal selector synchronization]
\label{thm:bounded-monotone-cauchy-witness-convergence-seal-selector-synchronization}
Let $W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)$ be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Any $\BoundedMonotoneConvergenceSealUp$ consumer using $W$ synchronizes its selected tail window through the witness rows $\mu,\sigma,\lambda$ and the seal-facing rows before it reads the convergence-seal selector.

Concretely, a tolerance request is first answered by $\mu$ on the scheduled window $\sigma$ and recorded in the dyadic ledger $\lambda$. The resulting window is then routed through $I,Q,H,C,P,N$ and only then through the convergence-seal rows
$$
W,M,K,F',\sigma',D,L,R,H',C',P',N'.
$$
Thus the selected limit, quotient equality, host real equality, ambient completeness, and detached tail-oracle readings are unavailable unless they are already exposed by the displayed witness and seal packets.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_convergence\_seal\_selector\_synchronization}
\begin{proof}
Begin with the local carrier. \autoref{def:bounded-monotone-cauchy-witness-carrier} exposes exactly $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, so the consumer has no selector coordinate outside those rows.

Next apply \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope}. It sends each tolerance request to $\mu$, ties the answer to the scheduled windows $\sigma$, and records the requested dyadic tolerance in $\lambda$.

Now route the synchronized tail into the seal-facing side. By \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}, the read of $Q$ passes through the regular-sequence row, the window schedule, the tail modulus, the dyadic ledger, the located trap, transport, continuation, provenance, and local naming rows.

Finally use \autoref{thm:bounded-monotone-cauchy-witness-convergence-seal-factorization}. The convergence-seal consumer reads the witness packet before it reads $M,K,F',\sigma',D,L,R,H',C',P',N'$. Consequently the selected window is synchronized across $\mu,\sigma,\lambda$ and the seal rows before the convergence-seal selector is available, and the excluded completion and equality coordinates remain outside the combined route.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness scoped dependency package]
\label{thm:bounded-monotone-cauchy-witness-scoped-dependency-package}
The scoped dependency package of \autoref{def:bounded-monotone-cauchy-witness-carrier} consists exactly of the displayed $\RegSeqRatUp$, $\StreamNameUp$, $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Within this package, generator range reads are controlled by $S$, tail requests are controlled by $\mu,\sigma,\lambda$, located enclosures are controlled by $I$, classifier transport is controlled by $H,C$, the real seal is controlled by $Q$, and provenance and naming are controlled by $P,N$. Selected limits, quotient equality, host real equality, trichotomy, ambient completeness, and open-cover compactness remain outside the scoped package.
\end{theorem}
\begin{proof}
Start with generator range exactness. By \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness}, every source observation is read through $S_{\mathrm{gen}}$ together with the monotone and upper-bound ledgers of $S$ and the dyadic guard data in $\lambda$.

Next use tail modulus scope. By \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope}, every tolerance request is answered by $\mu$ on the scheduled windows $\sigma$ with the displayed dyadic tolerance ledger $\lambda$.

For the seal-facing rows, apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary} and \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}. These route every $\RealUp$ read through $F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ while keeping the bounded source row visible.

Classifier movement is supplied by \autoref{thm:bounded-monotone-cauchy-witness-classifier-transport}. It transports only the displayed carrier coordinates and does not add any coordinate for selected limits, quotient equality, host real equality, trichotomy, ambient completeness, or open-cover compactness.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. Every public read is exhausted by the listed carrier rows, so the scoped dependency package is exactly the displayed package and no excluded completion or compactness object belongs to it.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_scoped\_dependency\_package}

\begin{theorem}[Bounded monotone Cauchy witness L10 exit-consumer package]
\label{thm:bounded-monotone-cauchy-witness-l10-exit-consumer-package}
Any Phase Real and Completion consumer of an accepted $\BoundedMonotoneCauchyWitnessUp$ packet must read the displayed $\RegSeqRatUp$ row, $\StreamNameUp$ window schedule, $\CauchyModulusUp$ tail witness, $\DyadicRatCoreUp$ tolerance ledger, $\LocatedIntervalUp$ trap, $\RealUp$ seal, $\BHist$ carrier, $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before exporting a real-facing convergence claim.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_l10\_exit\_consumer\_package}
\begin{proof}
Apply generator range exactness and tail modulus scope. These force the source reads through $S_{\mathrm{gen}},S_{\mathrm{mono}},S_{\mathrm{ub}}$ and force each tolerance request through $\mu,\sigma,\lambda$.

Next apply the real-seal boundary and real-seal handoff. The located trap and $\RealUp$ seal are read only through the displayed $I,Q,H,C,P,N$ rows.

Finally apply classifier transport, ledger exhaustion, and the scoped dependency package. The consumer receives exactly the displayed witness packet before any real-facing convergence export, with no selected limit, quotient equality, host real equality, trichotomy, ambient completeness, or compactness coordinate added at the exit.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness source-to-seal triad]
\label{thm:bounded-monotone-cauchy-witness-source-to-seal-triad}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every $\RealUp$-facing bounded monotone witness read from $W$ factors through exactly three stages:
\begin{enumerate}
\item the bounded source generator rows $S_{\mathrm{gen}},S_{\mathrm{mono}},S_{\mathrm{ub}}$ over the displayed $\RegSeqRatUp$ and $\StreamNameUp$ interface;
\item the scheduled finite tail-window service $\sigma,\mu,\lambda$ over the displayed $\CauchyModulusUp$ and $\DyadicRatCoreUp$ interface;
\item the located $\RealUp$ seal route $I,Q,H,C,P,N$ through the displayed $\LocatedIntervalUp$ and $\RealUp$ rows.
\end{enumerate}
No selected limit, quotient equality, host real equality, trichotomy, ambient completeness, or open-cover compactness row enters between these stages or supplies an additional $\RealUp$-facing route.
\end{theorem}
\begin{proof}
Start with the bounded source. By \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness}, every generator-facing observation is already confined to $S_{\mathrm{gen}},S_{\mathrm{mono}},S_{\mathrm{ub}}$ together with the displayed regular-sequence, stream-name, and dyadic guard data.

Next pass to the finite tail service. \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope} sends each tolerance request through $\mu$ on the windows scheduled by $\sigma$ and records the tolerance ledger in $\lambda$.

For the seal-facing route, use \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}. It routes every $\RealUp$ read through the located interval row $I$, the seal row $Q$, the transport row $H$, the continuation row $C$, provenance $P$, and the local naming row $N$.

Now compare the three pieces with \autoref{thm:bounded-monotone-cauchy-witness-scoped-dependency-package}. That package lists $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ as the whole public surface of the accepted carrier, with the source rows, scheduled tail service, and located seal route as the only BEDC packet stages visible to consumers.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. Since the public ledger is exhausted by the displayed rows, no selected limit, quotient equality, host real equality, trichotomy, ambient completeness, or open-cover compactness row can enter between the three stages or provide a further seal route.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_source\_to\_seal\_triad}

\begin{theorem}[Bounded monotone Cauchy witness source row scope]
\label{thm:bounded-monotone-cauchy-witness-source-row-scope}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every source read exposed by $W$ is read through the generator, monotonicity, and upper-bound rows
$$
S_{\mathrm{gen}},S_{\mathrm{mono}},S_{\mathrm{ub}}
$$
together with the dyadic guard data carried by $\lambda$.

The source interface exposes no selected limit, quotient equality, host real equality, ambient completeness coordinate, trichotomy split, open-cover compactness row, or choice-selected tail.
\end{theorem}
\begin{proof}
Start with generator range exactness. By \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness}, any source observation available to a consumer is already confined to $S_{\mathrm{gen}}$, $S_{\mathrm{mono}}$, and $S_{\mathrm{ub}}$, with the tolerance guard supplied by $\lambda$.

Next compare this row with the scoped dependency package. \autoref{thm:bounded-monotone-cauchy-witness-scoped-dependency-package} lists $S$ as the only bounded-source coordinate of the accepted carrier, while the remaining rows $F,\sigma,\mu,I,Q,H,C,P,N$ are handoff, witness, seal, transport, route, provenance, and naming rows rather than additional source coordinates.

Finally apply classifier transport and ledger exhaustion. \autoref{thm:bounded-monotone-cauchy-witness-classifier-transport} moves only displayed coordinates, and \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion} leaves no public row from which a selected limit, host equality, quotient collapse, ambient completeness object, trichotomy split, compactness principle, or choice-selected tail could be read.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_source\_row\_scope}

\begin{theorem}[Bounded monotone Cauchy witness window modulus row scope]
\label{thm:bounded-monotone-cauchy-witness-window-modulus-row-scope}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every tolerance request made to $W$ factors through the displayed window schedule $\sigma$, Cauchy modulus answer $\mu$, and dyadic tolerance ledger $\lambda$ before any located trap $I$ or $\RealUp$ seal row $Q$ is read.

Thus the window-modulus interface is the finite service $\sigma,\mu,\lambda$ over the displayed source and regular handoff rows. It exports no located trap or Real seal read that bypasses that service, and it exposes no selected limit, quotient equality, host real equality, ambient completeness, trichotomy, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_window\_modulus\_row\_scope}
\begin{proof}
Begin with tail modulus scope. \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope} sends each tolerance request through $\mu$ on the scheduled windows $\sigma$ and records the dyadic tolerance data in $\lambda$.

Use the scoped dependency package to place this service inside the whole packet. It identifies $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ as the public surface, so the window-modulus rows have to be read before the later located-trap and seal-facing rows can be consumed.

Now apply the Real seal boundary and Real seal handoff. \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary} and \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff} route $I$ and $Q$ through the displayed witness rows rather than around them.

Ledger exhaustion gives the exclusion. Since \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion} exhausts public reads by the displayed carrier rows, no completion coordinate, quotient collapse, host equality, trichotomy split, compactness principle, or choice-selected tail can answer a tolerance request independently of $\sigma,\mu,\lambda$.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness Real seal row scope]
\label{thm:bounded-monotone-cauchy-witness-real-seal-row-scope}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every $\RealUp$ seal read through $Q$ factors through the displayed route
$$
F,\sigma,\mu,\lambda,I,H,C,P,N
$$
and remains tied to the bounded source row $S$.

The Real seal interface therefore exports only the witnessed packet $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$. It does not export a selected limit, quotient real equality, host real equality, ambient completeness, trichotomy, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_real\_seal\_row\_scope}
\begin{proof}
Start with the seal boundary and handoff rows. By \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary} and \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}, the seal coordinate $Q$ is available only through the regular handoff, scheduled windows, Cauchy witness, dyadic ledger, located trap, classifier transport, continuation, provenance, and local naming rows.

The source row remains attached to this route. Generator range exactness keeps source observations inside $S_{\mathrm{gen}},S_{\mathrm{mono}},S_{\mathrm{ub}}$, and tail modulus scope keeps the tolerance route inside $\sigma,\mu,\lambda$ before the seal-facing suffix is read.

Classifier transport supplies the only allowed movement of the seal packet. \autoref{thm:bounded-monotone-cauchy-witness-classifier-transport} transports the displayed rows componentwise, so the Real seal cannot be replaced by a host equality or quotient-name collapse.

Finally apply ledger exhaustion. The accepted carrier has no public coordinate beyond $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, so every seal read through $Q$ remains a read of the displayed witnessed packet and cannot import completeness, trichotomy, compactness, selected limits, or choice-selected tails.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness tail-window service]
\label{thm:bounded-monotone-cauchy-witness-tail-window-service}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every tolerance request made to $W$ factors through the bounded source row $S$, the regular rational handoff $F$, the scheduled $\StreamNameUp$ windows $\sigma$, the Cauchy modulus answer $\mu$, and the dyadic tolerance ledger $\lambda$ before any located trap or completion-facing row can be read.

The service exposes exactly the displayed $\StreamNameUp$ windows and their dyadic tolerance rows. It does not supply a selected limit, quotient equality, host real equality, trichotomy, ambient completeness, or open-cover compactness.
\end{theorem}
\begin{proof}
Project the accepted carrier and use the NameCert obligation theorem. The rows $S,F,\sigma,\mu,\lambda$ are part of the displayed packet, so a tolerance request has no independent input surface outside the carrier.

Apply \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness}. It keeps source observations inside the bounded generator, monotone, and upper-bound rows of $S$ together with the regular rational handoff $F$.

Next apply \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope}. The requested tolerance is answered by $\mu$ only after the scheduled windows $\sigma$ and the dyadic ledger $\lambda$ have been displayed.

Finally use \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. Since every tolerance read has already passed through $S,F,\sigma,\mu,\lambda$, no completion coordinate or compactness object remains available to the service.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_tail\_window\_service}

\begin{theorem}[Bounded monotone Cauchy witness located trap refinement]
\label{thm:bounded-monotone-cauchy-witness-located-trap-refinement}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The located trap row $I$ refines the bounded source row $S$, the dyadic ledger $\lambda$, and the $\RealUp$ seal route $Q,H,C,P,N$ through the same displayed packet. It does not add a selected limit, quotient equality, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.

The refinement is local to the carrier: a trap consumer may inspect $I$ only after the generator range, tail-modulus, dyadic tolerance, and NameCert rows of $W$ have fixed the route from $S,F,\sigma,\mu,\lambda$ toward $Q$.
\end{theorem}
\begin{proof}
Project the accepted carrier using \autoref{def:bounded-monotone-cauchy-witness-carrier}. The trap row $I$ is displayed beside the bounded source, regular rational handoff, scheduled windows, Cauchy witness, dyadic ledger, seal, transports, continuations, provenance, and local naming row.

Use the NameCert obligations of \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations}. They make carrier admission, explicit witness access, trap access, seal access, classifier transport, and ledger non-escape part of the same local package.

Now apply generator range exactness and tail modulus scope. By \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness}, the source read remains inside the bounded generator and upper-bound rows of $S$; by \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope}, the tolerance request reaches $I$ only after $\sigma,\mu,\lambda$ have supplied the displayed tail service.

Finally use \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary}, \autoref{thm:bounded-monotone-cauchy-witness-classifier-transport}, and \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. The trap is therefore a refinement of the bounded source and dyadic ledger on the path to the Real seal, and every excluded completion or compactness coordinate remains absent from the carrier.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_located\_trap\_refinement}

\begin{theorem}[Bounded monotone Cauchy witness tail-seal synchronization]
\label{thm:bounded-monotone-cauchy-witness-tail-seal-synchronization}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every tail request answered by the scheduled windows $\sigma$, Cauchy witness $\mu$, and dyadic ledger $\lambda$ reaches the same $\RealUp$ seal row $Q$ through the displayed route
$$
I,H,C,P,N.
$$
The synchronization supplies one tail-to-seal path through $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ and cannot create a second hidden tail service or an ambient completion read.
\end{theorem}
\begin{proof}
Begin with the tail-window service of \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service}. It sends each tolerance request through $S,F,\sigma,\mu,\lambda$ before any located trap or completion-facing row can be consumed.

The carrier and NameCert rows then identify the seal-facing suffix. By \autoref{def:bounded-monotone-cauchy-witness-carrier} and \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations}, the only rows downstream of the tail service are the located trap $I$, the seal $Q$, the transports $H$, continuations $C$, provenance $P$, and naming row $N$.

Use generator range exactness and tail modulus scope to keep the source and tolerance sides fixed. \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness} fixes the bounded source read, while \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope} fixes the $\sigma,\mu,\lambda$ answer that the seal-facing route receives.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary}, \autoref{thm:bounded-monotone-cauchy-witness-classifier-transport}, and \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. They force the read of $Q$ through the same $I,H,C,P,N$ suffix and leave no carrier coordinate for a second tail source, selected limit, quotient equality, host equality, trichotomy, ambient completeness, or compactness principle.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_tail\_seal\_synchronization}

\begin{theorem}[Bounded monotone Cauchy witness completion-consumer refusal]
\label{thm:bounded-monotone-cauchy-witness-completion-consumer-refusal}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Any $\RealUp$ or completion-facing consumer receives only the witnessed packet displayed by $W$: the bounded source $S$, regular rational row $F$, window schedule $\sigma$, witness $\mu$, dyadic ledger $\lambda$, located trap $I$, seal $Q$, transports $H$, continuations $C$, provenance $P$, and local naming row $N$.

The consumer cannot read ambient completeness, quotient equality, trichotomy, open-cover compactness, host real equality, selected limiting representatives, or a choice-selected tail from this packet.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_completion\_consumer\_refusal}
\begin{proof}
Project the consumer-facing surface from the carrier of \autoref{def:bounded-monotone-cauchy-witness-carrier}. The displayed tuple $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ is the whole accepted packet.

Apply the NameCert obligations and the generator range row. \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations} identifies the admissible read coordinates, and \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness} keeps source observations inside the bounded generator and upper-bound data.

Next use \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope}, \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service}, and \autoref{thm:bounded-monotone-cauchy-witness-located-trap-refinement}. The consumer therefore reaches the seal only after the scheduled witness, dyadic ledger, and located trap rows have been read.

The final refusal is ledger exhaustion. By \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary}, \autoref{thm:bounded-monotone-cauchy-witness-classifier-transport}, and \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}, every completion-facing read stays inside the witnessed packet and the excluded completion, equality, order, compactness, and host-real principles have no row to project.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness located trap handoff]
\label{thm:bounded-monotone-cauchy-witness-located-trap-handoff}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The $\LocatedIntervalUp$ trap row $I$ is reached only after the dyadic tolerance ledger $\lambda$ and the source upper-bound rows in $S$ have been displayed through the scheduled tail-window service.

Thus any handoff from $W$ to a located interval consumer preserves the same $S,F,\sigma,\mu,\lambda$ route before $I$ and carries forward only $Q,H,C,P,N$ as the seal, transport, continuation, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Start with the source rows. By \autoref{thm:bounded-monotone-cauchy-witness-generator-range-exactness}, the upper-bound data are read from the displayed bounded source packet $S$ and not from an ambient order-completeness assumption.

Use the tail-window service supplied by \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service}. It routes the tolerance request through $F,\sigma,\mu,\lambda$, so the dyadic ledger is already visible before the trap row is consumed.

Now apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary}. The located trap $I$ belongs to the seal-facing route that also carries $Q,H,C,P,N$, and it cannot be read before the source and tolerance ledgers have been displayed.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. The handoff has no coordinate for a hidden trap, selected limit, quotient equality, host real equality, trichotomy, ambient completeness, or open-cover compactness.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_located\_trap\_handoff}

\begin{theorem}[Bounded monotone Cauchy witness completion boundary]
\label{thm:bounded-monotone-cauchy-witness-completion-boundary}
Every $\RealUp$ or completion-facing consumer of an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier reads only the displayed packet
$$
S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N.
$$
The boundary supplies the bounded source rows, regular rational handoff, scheduled tail windows, modulus answer, dyadic tolerance ledger, located trap, Real seal, transport rows, continuation rows, package provenance, and local naming row. It supplies no selected limit, quotient equality, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_completion\_boundary}
\begin{proof}
Use the scoped dependency package. By \autoref{thm:bounded-monotone-cauchy-witness-scoped-dependency-package}, the whole public surface of the accepted carrier is the displayed package over $\RegSeqRatUp$, $\StreamNameUp$, $\CauchyModulusUp$, $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Apply \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service} and \autoref{thm:bounded-monotone-cauchy-witness-located-trap-handoff}. These rows force completion-facing reads to pass through the source, window, modulus, dyadic tolerance, and located-trap route before the seal is available.

Next apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}. It sends every real-facing read through $I,Q,H,C,P,N$ while keeping the source and witness rows visible.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. Since the packet is exhausted by $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, the excluded completion and equality principles cannot be projected by any consumer.
\end{proof}

\begin{theorem}[Bounded monotone witness bridge to public tail and criterion packets]
\label{thm:bounded-monotone-witness-monotone-cauchy-criterion-bridge}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Suppose the bounded source row $S$ is read through the finite tail-window surface of \autoref{ch:concrete-instances-monotonecauchy-namecert}, and the witness rows $\sigma,\mu,\lambda$ supply the explicit modulus and tail-bound route of \autoref{ch:concrete-instances-cauchycriterion-namecert}. Then every $\RealUp$ or completion-facing read of $W$ factors through the public $\MonotoneCauchyUp$ tail-window certificate and the public $\CauchyCriterionUp$ packet before the local $\RealUp$ seal handoff.

The factorization preserves only the displayed rows $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$. It exports no quotient real equality, selected representative, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice principle.
\end{theorem}
\begin{proof}
Start from \autoref{thm:bounded-monotone-cauchy-witness-scoped-dependency-package}. It exposes the bounded source row, regular rational row, stream schedule, Cauchy modulus witness, dyadic ledger, located trap, real seal, transports, continuations, package provenance, and local naming row as the whole dependency surface of $W$.

Read the bounded source through the finite route supplied by \autoref{thm:monotone-cauchy-public-tail-window-certificate}. That public certificate consumes the displayed tail-window schedule, monotone rows, located interval trap, real-seal row, componentwise transports, continuation routes, package provenance, and local $\NameCert_{\MonotoneCauchyUp}$ row, so the source socket of $W$ is routed through the existing $\MonotoneCauchyUp$ public surface.

Read the witness rows through \autoref{thm:cauchycriterion-public-certificate}. The rows $\sigma,\mu,\lambda$ provide the explicit modulus, dyadic tolerance, finite tail ledger, regular rational handoff, and real-seal route required by the public $\CauchyCriterionUp$ packet, so the witness socket of $W$ is routed through the existing constructive criterion surface.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff} and \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. The handoff sends each real-facing read through $F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ while keeping the bounded source row visible, and ledger exhaustion rules out every remaining coordinate. Therefore the composed route passes through the MonotoneCauchy public tail-window certificate and the CauchyCriterion public packet before the local seal, with no route to quotient real equality, selected representatives, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_monotone\_cauchy\_criterion\_bridge}

\begin{theorem}[Bounded monotone Cauchy witness Real observation-budget handoff]
\label{thm:bounded-monotone-cauchy-witness-real-observation-budget-handoff}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. If a real-facing consumer asks $W$ for finite observation data, then the request factors through the finite observation route of \autoref{ch:concrete-instances-real-observation-budget-namecert}: the windows are read through $\sigma$, dyadic readback and radius data are read through $\lambda$, the regular rational handoff is read through $F$, and the $\RealUp$ seal is read through $Q$.

The handoff preserves the displayed bounded source row $S$, the Cauchy witness $\mu$, the located trap $I$, transports $H$, continuations $C$, provenance $P$, and the local naming row $N$. It exports no quotient stream equality, host real equality, selected limit, trichotomy, ambient completeness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_real\_observation\_budget\_handoff}
\begin{proof}
Start from the scoped dependency package of \autoref{thm:bounded-monotone-cauchy-witness-scoped-dependency-package}. It lists $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ as the whole public surface of the accepted packet.

Read the finite observation request through \autoref{ch:concrete-instances-real-observation-budget-namecert}. That route accepts finite window rows, dyadic readback and radius ledgers, regular-rational handoff rows, and a $\RealUp$ seal boundary; these are supplied here by $\sigma,\lambda,F,Q$.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff} and \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. The seal remains tied to the displayed bounded source and witness rows, while ledger exhaustion rules out every absent completion coordinate.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness Cauchy modulus-extraction factorization]
\label{thm:bounded-monotone-cauchy-witness-cauchy-modulus-extraction-factorization}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Before $W$ exports a tail threshold to any Real-completion route, the witness rows $\sigma,\mu,\lambda$ factor through the modulus-indexed schedule of \autoref{ch:concrete-instances-cauchymodulusextraction-namecert}. The schedule reads $\sigma$ as its finite window source, $\mu$ as the tail-threshold answer for each dyadic tolerance request, and $\lambda$ as the dyadic tolerance and radius ledger that justifies the answer.

The factorization may pass along the regular sequence row $F$, the source row $S$, and the seal-facing rows $I,Q,H,C,P,N$, but it cannot replace the displayed modulus extraction by quotient stream equality, host real equality, a selected limit, trichotomy, ambient completeness, or a choice-selected tail.
\end{theorem}
\begin{proof}
Project the witness rows from the carrier. The rows $\sigma,\mu,\lambda$ already describe a window schedule, a tolerance-indexed tail answer, and the dyadic ledger that the tail answer consumes.

Route those rows through \autoref{ch:concrete-instances-cauchymodulusextraction-namecert}. Its modulus-indexed schedule is exactly the public surface that receives a finite window source, a requested tolerance, and the ledger rows needed to produce a tail threshold.

Then reattach the source, regular-sequence, located-trap, seal, transport, continuation, provenance, and naming rows by \autoref{thm:bounded-monotone-cauchy-witness-tail-modulus-scope} and \autoref{thm:bounded-monotone-cauchy-witness-real-seal-boundary}. These rows carry the extracted threshold toward the Real-completion route without adding any excluded completion coordinate.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_cauchy\_modulus\_extraction\_factorization}

\begin{theorem}[Bounded monotone Cauchy witness downstream unblock package]
\label{thm:bounded-monotone-cauchy-witness-downstream-unblock-package}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The downstream Real and Completion consumers of $W$ receive a finite package through four public routes: the $\MonotoneCauchyUp$ tail-window certificate, the $\CauchyCriterionUp$ criterion packet, the $\RealObservationBudgetUp$ finite observation route, and the $\CauchyModulusExtractionUp$ modulus-indexed schedule.

The package consists only of the displayed rows $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$. It gives downstream consumers a bounded-source route, an explicit Cauchy witness route, a finite observation route, and a modulus-extraction route, with no quotient stream equality, host real equality, selected limit, trichotomy, ambient completeness, or choice-selected tail.
\end{theorem}
\begin{proof}
First apply \autoref{thm:bounded-monotone-witness-monotone-cauchy-criterion-bridge}. It routes the source and witness sides of $W$ through the public $\MonotoneCauchyUp$ tail-window certificate and the public $\CauchyCriterionUp$ packet before the local $\RealUp$ seal handoff.

Next apply \autoref{thm:bounded-monotone-cauchy-witness-real-observation-budget-handoff}. It routes every real-facing finite observation read through $\sigma,\lambda,F,Q$ and keeps the remaining carrier rows visible as the same displayed packet.

Then apply \autoref{thm:bounded-monotone-cauchy-witness-cauchy-modulus-extraction-factorization}. It routes $\sigma,\mu,\lambda$ through the modulus-indexed schedule before any tail threshold is exported to the Real-completion route.

Finally use \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion}. Since every public read is exhausted by $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, the four-route package exports only finite displayed data and none of the excluded equality, limit, completeness, trichotomy, or tail-selection objects.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_downstream\_unblock\_package}

\begin{theorem}[Bounded monotone Cauchy witness budget regularity lock]
\label{thm:bounded-monotone-cauchy-witness-budget-regularity-lock}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ packet. The regular-sequence row $F$ and the Cauchy witness row $\mu$ are locked to the same finite budget rows $\sigma$ and $\lambda$: any downstream read of regularity, tail control, or tolerance response must pass through the displayed window schedule $\sigma$ and dyadic ledger $\lambda$ before it may read either $F$ or $\mu$.

No quotient equality, host real equality, selected limit, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail can supply an independent budget for $F$ or $\mu$.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_budget\_regularity\_lock}
\begin{proof}
Start with the carrier of \autoref{def:bounded-monotone-cauchy-witness-carrier}. The only budget-facing coordinates are the stream schedule $\sigma$, the witness row $\mu$, and the dyadic ledger $\lambda$, with $F$ as the regular rational handoff.

Apply the NameCert obligations of \autoref{thm:bounded-monotone-cauchy-witness-namecert-obligations} and the tail-window service of \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service}. These force every tolerance request through $F,\sigma,\mu,\lambda$ as one finite service.

Finally use the downstream package of \autoref{thm:bounded-monotone-cauchy-witness-downstream-unblock-package}. Since every exported route still uses the displayed rows $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, no external budget row can separate $F$ from $\mu$ or replace $\sigma,\lambda$.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness located trap factorization]
\label{thm:bounded-monotone-cauchy-witness-located-trap-factorization}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ packet. Every read of the located trap $I$ factors through $S,F,\sigma,\mu,\lambda,H,C,P,N$ before any $\RealUp$ seal read is available. The trap therefore records a bounded-source, regular-handoff, scheduled-window, Cauchy-witness, dyadic-budget, transport, continuation, provenance, and naming route, rather than an independent interval selector.

The factorization exports no host order-completeness argument, quotient equality, selected limit, trichotomy split, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_located\_trap\_factorization}
\begin{proof}
Begin with the carrier admission and local NameCert obligations. They make $I$ one coordinate of the accepted packet and place it after the bounded source row, regular sequence row, schedule row, witness row, and dyadic ledger.

Use \autoref{thm:bounded-monotone-cauchy-witness-located-trap-handoff}. It is the direct handoff theorem for the trap row and routes reads of $I$ through $S,F,\sigma,\mu,\lambda$ with the structural rows $H,C,P,N$ still visible.

Then apply the Real observation-budget handoff and the Cauchy modulus-extraction factorization from \autoref{thm:bounded-monotone-cauchy-witness-real-observation-budget-handoff} and \autoref{thm:bounded-monotone-cauchy-witness-cauchy-modulus-extraction-factorization}. These keep the trap route inside the finite observation and modulus schedule before the Real seal can be consumed.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness Real seal schedule exhaustion]
\label{thm:bounded-monotone-cauchy-witness-real-seal-schedule-exhaustion}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ packet. Every $\RealUp$ seal consumer of $W$ reads the displayed schedule $\sigma$, dyadic ledger $\lambda$, Cauchy witness $\mu$, and located trap $I$ through the same finite route $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$. Such a consumer receives no selected limit, quotient equality, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\begin{proof}
First invoke the budget regularity lock and located trap factorization above. These place $F,\mu$ over the same schedule and dyadic budget, and they force the trap row $I$ through the displayed source, regularity, witness, budget, transport, continuation, provenance, and naming rows.

Next apply \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}. The seal row $Q$ is readable only after the located trap and witness-facing rows have been displayed.

Finally use \autoref{thm:bounded-monotone-cauchy-witness-ledger-exhaustion} together with \autoref{thm:bounded-monotone-cauchy-witness-cauchy-modulus-extraction-factorization}. The ledger exhausts every public read, and the modulus factorization keeps tolerance responses inside $\sigma,\mu,\lambda$. Therefore the seal schedule has no remaining coordinate for any excluded equality, limit, completeness, trichotomy, compactness, or choice object.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_real\_seal\_schedule\_exhaustion}

\begin{theorem}[Bounded monotone Cauchy witness seal-trap-budget determinacy]
\label{thm:bounded-monotone-cauchy-witness-seal-trap-budget-determinacy}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ packet. Suppose two Real-completion-facing consumers agree on the bounded source row $S$, regular handoff row $F$, schedule $\sigma$, Cauchy modulus row $\mu$, dyadic ledger $\lambda$, located trap $I$, and structural rows $H,C,P,N$. Then they determine the same $\RealUp$ seal read $Q$ and the same completion-facing export of $W$.

The determinacy is only a row-level determinacy for the displayed carrier route. It does not assert a selected limit, quotient equality, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_seal\_trap\_budget\_determinacy}
\begin{proof}
First apply the tail-window service. It makes each tolerance request pass through the same displayed $S,F,\sigma,\mu,\lambda$ service, so the two consumers have no separate tail budget to read.

Next use located trap factorization. Since both consumers agree on $I$ and on the structural rows $H,C,P,N$, every trap-facing read is already fixed by the shared source, handoff, schedule, modulus, ledger, and structural route.

Now apply Real seal schedule exhaustion. The seal row $Q$ is readable only through that shared route, hence both consumers determine the same Real seal read.

Finally invoke the Real completion export theorem. The completion-facing export has no coordinate beyond $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, so equality of the shared route and of the seal read forces the same export while excluding all selected-limit, quotient, host-equality, trichotomy, completeness, compactness, and choice-tail data.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness source-tail synchronization]
\label{thm:bounded-monotone-cauchy-witness-source-tail-synchronization}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. A tolerance request to $W$ cannot read the modulus answer $\mu$ or the scheduled windows $\sigma$ unless the bounded source row $S$ and the regular handoff row $F$ are already displayed in the same packet.

Thus the source, handoff, window, modulus, and dyadic-budget rows form one synchronized service. No consumer may replace that service by quotient equality, host real equality, a selected limit, trichotomy, ambient completeness, or a choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_source\_tail\_synchronization}
\begin{proof}
Start from the tail-window service of \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service}. It routes every tolerance request through $S,F,\sigma,\mu,\lambda$ before any trap or completion-facing row can be read.

Use the downstream package of \autoref{thm:bounded-monotone-cauchy-witness-downstream-unblock-package}. Its four public routes keep the bounded-source route and the explicit Cauchy-witness route inside the same displayed packet.

Finally invoke ledger exhaustion. Since the accepted carrier has only the rows $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$, a request that reads $\mu$ or $\sigma$ has no separate route around $S$ and $F$.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness trap-budget monotonicity]
\label{thm:bounded-monotone-cauchy-witness-trap-budget-monotonicity}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The located trap row $I$ is monotone in the displayed dyadic budget route $\lambda$: tightening or transporting the tolerance budget can only be read through the same source row $S$, regular handoff $F$, scheduled windows $\sigma$, modulus answer $\mu$, and located-trap handoff.

Consequently the trap route exports no ambient order-completeness argument, trichotomy split, hidden interval selector, host real equality, quotient equality, selected limit, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_trap\_budget\_monotonicity}
\begin{proof}
First read the budget through \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service}. The dyadic row $\lambda$ is displayed with the window schedule and modulus answer, so budget movement is not an external operation.

Next use \autoref{thm:bounded-monotone-cauchy-witness-located-trap-handoff}. It places $I$ after the route $S,F,\sigma,\mu,\lambda$ and keeps the upper-bound data from $S$ visible at the handoff.

The classifier-transport and Real-seal rows then preserve the same displayed budget route under $H,C,Q,P,N$. Ledger exhaustion excludes any additional selector or completeness coordinate, so the trap can vary only along the displayed $\lambda$ route.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness seal factorization triad]
\label{thm:bounded-monotone-cauchy-witness-seal-factorization-triad}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Any $\RealUp$ seal read of $W$ factors through the triad of public routes already cited by this chapter: the $\MonotoneCauchyUp$ tail-window certificate, the $\CauchyCriterionUp$ criterion packet, and the $\RealObservationBudgetUp$ finite observation route.

The factorized seal still reads only $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$. It does not import quotient equality, host real equality, selected limits, trichotomy, ambient completeness, or choice-selected tails.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_seal\_factorization\_triad}
\begin{proof}
Begin with \autoref{thm:bounded-monotone-witness-monotone-cauchy-criterion-bridge}. It routes the bounded source through $\MonotoneCauchyUp$ and the witness rows through $\CauchyCriterionUp$ before the local seal is consumed.

Then use \autoref{thm:bounded-monotone-cauchy-witness-real-observation-budget-handoff}. The finite observation request reads windows, dyadic data, regular-rational handoff, and Real seal data through $\sigma,\lambda,F,Q$.

Finally apply the downstream package and ledger exhaustion. The package contains the same carrier rows throughout the triad, and exhaustion rules out every absent quotient, host-equality, limit, completeness, trichotomy, and tail-selection coordinate.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness located interval trap certificate]
\label{thm:bounded-monotone-cauchy-witness-located-interval-trap-certificate}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The located-trap row $I$ is certified only as the displayed $\LocatedIntervalUp$ trap carried by the dyadic tolerance ledger $\lambda$ and by the $hsame$ transport rows $H$. Any consumer that reads $I$ as a trap certificate must therefore keep the source row $S$, regular handoff $F$, scheduled windows $\sigma$, Cauchy witness $\mu$, dyadic ledger $\lambda$, transport rows $H$, continuation row $C$, provenance row $P$, and NameCert row $N$ in the same package.

The certificate exports no host interval selector, quotient stream equality, host real equality, selected limit, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_located\_interval\_trap\_certificate}
\begin{proof}
Begin with \autoref{thm:bounded-monotone-cauchy-witness-located-trap-factorization}. It already places every read of $I$ after the displayed rows $S,F,\sigma,\mu,\lambda,H,C,P,N$, so the trap cannot be separated from the finite BoundedMonotone source, regular-rational handoff, scheduled window, and Cauchy witness data.

The $\LocatedIntervalUp$ certificate is then read through the same dyadic tolerance ledger. The local trap row is not an interval chosen outside the carrier; it is the row paired with $\lambda$ and transported through $H$ as part of the displayed packet.

Finally use the sibling $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\StreamNameUp$, and $\LocatedIntervalUp$ handoffs cited by the scoped dependency package. Together with ledger exhaustion, these rows leave no additional coordinate from which a quotient equality, host equality, selected limit, completeness principle, trichotomy split, compactness argument, or choice-selected tail could enter.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness monotone tail coherence]
\label{thm:bounded-monotone-cauchy-witness-monotone-tail-coherence}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The monotone tail schedule of $W$ factors through the public $\MonotoneCauchyUp$ tail-window route, the $\CauchyCriterionUp$ criterion packet, and the $\CauchyModulusExtractionUp$ modulus schedule. A downstream tail request may read only the displayed $\sigma,\mu,\lambda$ service over $S$ and $F$, not an independently selected tail.

This coherence exports no quotient equality, host real equality, selected limit, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_monotone\_tail\_coherence}
\begin{proof}
First apply \autoref{thm:bounded-monotone-cauchy-witness-source-tail-synchronization}. It forces every tolerance request that reads $\sigma$ or $\mu$ to keep the bounded source row $S$ and the regular handoff row $F$ visible in the same packet.

Next use \autoref{thm:bounded-monotone-witness-monotone-cauchy-criterion-bridge}. This is the chapter's bridge from the bounded monotone source and explicit witness rows to the sibling $\MonotoneCauchyUp$ and $\CauchyCriterionUp$ certificates.

Finally apply \autoref{thm:bounded-monotone-cauchy-witness-cauchy-modulus-extraction-factorization}. It routes the schedule through $\sigma,\mu,\lambda$ before any tail threshold is exported. Since ledger exhaustion gives no separate coordinate for a tail choice, the monotone tail service remains a finite displayed route rather than a selected subsequence.
\end{proof}

\begin{theorem}[Bounded monotone Cauchy witness Real completion export]
\label{thm:bounded-monotone-cauchy-witness-real-completion-export}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Every Real-completion-facing consumer of $W$ must pass through the finite observation budget, the regular rational handoff, and the Real seal rows before using the witness. Concretely, such a consumer reads the route $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$ through the finite observation service of $\RealObservationBudgetUp$, the regular-sequence service of $\RegSeqRatUp$, and the seal handoff to $\RealUp$.

The exported witness therefore carries only the displayed finite observation, regular-rational, trap, seal, transport, continuation, provenance, and naming data. It does not close a quotient real construction, host real equality, selected limit, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\begin{proof}
Start with \autoref{thm:bounded-monotone-cauchy-witness-real-observation-budget-handoff}. It routes every real-facing observation through the finite rows $\sigma,\lambda,F,Q$ while keeping the carrier rows $S,\mu,I,H,C,P,N$ visible.

Next use \autoref{thm:bounded-monotone-cauchy-witness-seal-factorization-triad}. The Real seal read factors through $\MonotoneCauchyUp$, $\CauchyCriterionUp$, and $\RealObservationBudgetUp$, so the consumer cannot skip the finite observation budget before reaching the seal row.

It remains to account for the regular handoff and trap data used by the witness. The located interval trap certificate above keeps $I$ tied to $\LocatedIntervalUp$, $\lambda$, and $H$, while source-tail synchronization keeps $\sigma,\mu,\lambda$ tied to $S$ and $F$.

Finally apply ledger exhaustion. The displayed packet has no additional completion coordinate, quotient representative, host equality, selected limit, completeness principle, trichotomy split, compactness proof, or choice-selected tail, so every Real-completion-facing export is forced through the finite observation, regular rational, and Real seal route already named.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_real\_completion\_export}

\begin{theorem}[Bounded monotone Cauchy witness formal handoff package]
\label{thm:bounded-monotone-cauchy-witness-formal-handoff-package}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. The paper-side formal handoff for this carrier is exactly the finite package consisting of the bounded source row $S$, regular rational row $F$, $\StreamNameUp$ schedule $\sigma$, Cauchy modulus witness $\mu$, $\DyadicRatCoreUp$ ledger $\lambda$, $\LocatedIntervalUp$ trap $I$, $\RealUp$ seal $Q$, componentwise $\hsame$ transports $H$, displayed $\Cont$ routes $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\BoundedMonotoneCauchyWitnessUp}$ row $N$.

Every Lean-side consumer that claims Real-completion-facing use of $\BoundedMonotoneCauchyWitnessUp$ must formalize those rows before it consumes the Real route. The handoff contains no selected limit, quotient real, host equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\begin{proof}
The carrier definition displays exactly the coordinates $S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N$. The local NameCert obligations then read carrier admission, regular rational exposure, schedule exposure, Cauchy witness exposure, dyadic ledger exposure, trap exposure, seal exposure, transport, continuation, provenance, and local naming from those same rows.

The scoped dependency package identifies the sibling sources of the rows. Monotone tail coherence keeps $\sigma,\mu,\lambda$ tied to the bounded source and regular handoff, while the located interval trap certificate keeps $I$ tied to $\lambda$ and the componentwise transports $H$.

Apply the Real completion export theorem. It forces every Real-completion-facing consumer through the finite observation budget, regular rational handoff, trap, seal, transport, continuation, provenance, and naming rows already displayed by the carrier.

Ledger exhaustion finishes the package comparison. Since the displayed carrier has no remaining coordinate for a selected limit, quotient real, host equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail, the listed rows are precisely the handoff to be formalized by downstream Lean work.
\end{proof}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_formal\_handoff\_route}

\begin{theorem}[Bounded monotone Cauchy witness convergence-seal terminal synchronization]
\label{thm:bounded-monotone-cauchy-witness-convergence-seal-terminal-synchronization}
Let
$$
W=(S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N)
$$
be an accepted $\BoundedMonotoneCauchyWitnessUp$ carrier. Any convergence-seal-facing or Real-completion-facing consumer of $W$ synchronizes the same finite tail answer through
$$
S,F,\sigma,\mu,\lambda,I,Q,H,C,P,N
$$
before it can read the convergence-seal rows. In particular, the terminal tail service is the displayed route through the bounded source, regular handoff, schedule, Cauchy witness, dyadic ledger, located trap, Real seal, transport, continuation, provenance, and local naming rows.

The synchronized route exports no selected limit, quotient equality, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail.
\end{theorem}
\leanchecked{BEDC.Derived.BoundedMonotoneCauchyWitnessUp.BoundedMonotoneCauchyWitnessCarrier\_convergence\_seal\_terminal\_synchronization}
\begin{proof}
First use \autoref{thm:bounded-monotone-cauchy-witness-convergence-seal-factorization}. It gives the two-stage route from the bounded monotone witness through the finite tail service and only then to the convergence-seal rows.

Next apply \autoref{thm:bounded-monotone-cauchy-witness-convergence-seal-selector-synchronization}. The selector rows $\mu,\sigma,\lambda$ are read as one shared terminal service, so a seal-facing consumer cannot replace the scheduled tail answer by an independently selected threshold.

For consumers that continue to the Real route, use \autoref{thm:bounded-monotone-cauchy-witness-real-completion-export}. It forces the Real-completion-facing read through the same finite observation, regular handoff, trap, seal, transport, continuation, provenance, and naming rows before any terminal export is visible.

Ledger exhaustion closes the synchronization claim. The accepted packet contains no coordinate for a selected limit, quotient equality, host real equality, trichotomy, ambient completeness, open-cover compactness, or choice-selected tail; hence every seal or completion-facing consumer reaches the terminal convergence-seal read only through the displayed finite route above.
\end{proof}

\input{parts/concrete_instances/boundedmonotonecauchywitness/tail_service_and_seal_handoff}
\input{parts/concrete_instances/boundedmonotonecauchywitness/real_route_nonescape}
\input{parts/concrete_instances/boundedmonotonecauchywitness/terminal_window_split_exactness}
