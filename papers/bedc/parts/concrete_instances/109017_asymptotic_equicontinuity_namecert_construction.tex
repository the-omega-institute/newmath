\chapter{A Concrete Naming Certificate for $\AsymptoticEquicontinuityUp$}
\label{ch:concrete-instances-asymptotic-equicontinuity-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley-style compact-uniform language, the $\AsymptoticEquicontinuityUp$ packet records the finite tail-index surface by which an eventually stable family of continuous maps receives a shared compact-source modulus. It sits after the compact radius-choice packet of \autoref{ch:concrete-instances-compact-uniform-radius-choice-namecert}, the uniform family-modulus route of \autoref{ch:concrete-instances-uniform-equicontinuity-modulus-namecert}, the equicontinuity family ledger of \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, and the graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}. Its terminal reads pass through finite $\StreamNameUp$ windows and the $\RealUp$ seal rather than through a host function-space topology, a selected subsequence, or an ambient completeness theorem.

\begin{definition}[Asymptotic equicontinuity carrier]
\label{def:asymptotic-equicontinuity-carrier}
An $\AsymptoticEquicontinuityUp$ carrier is a finite $\BHist$ packet
$$
A=(K,F,E,U,T,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the compact finite-net carrier and the metric-distance ledger used by all later rows.
\item $F$ is the finite family ledger of $\ContinuousMapUp$ rows whose graph and pointwise-modulus data are inspected at displayed compact-source centers.
\item $E$ is the $\EquicontinuityFamilyUp$ row, binding the family rows to a shared local tolerance-radius ledger before any tail condition is read.
\item $U$ is the $\UniformEquicontinuityModulusUp$ row, exporting the compact finite-net fold that yields a single tolerance-indexed source radius for the displayed family window.
\item $T$ is the asymptotic tail row. It records the finite index threshold after which the family window must use the same compact-source radius and target tolerance route.
\item $S$ is the $\StreamNameUp$ window row opened after $T$, used to inspect only the tail-admitted source and target observations.
\item $R$ is the terminal $\RealUp$ seal row reached by the tail-admitted distance and tolerance reads after $K,F,E,U,T,S$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for compact source, continuous-family, equicontinuity, uniform-modulus, tail, stream-window, real-seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a tail-index request through the compact finite-net and uniform-equicontinuity rows to the real-seal boundary.
\item $P$ is the $\Pkg$ provenance over $\AsymptoticEquicontinuityUp$, $\CompactUniformRadiusChoiceUp$, $\UniformEquicontinuityModulusUp$, $\EquicontinuityFamilyUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AsymptoticEquicontinuityUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for host function-space compactness, a selected subsequence, quotient equality of maps, countable choice over tails, an external modulus function, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Asymptotic equicontinuity NameCert obligations]
\label{thm:asymptotic-equicontinuity-namecert-obligations}
For every accepted $\AsymptoticEquicontinuityUp$ carrier $A=(K,F,E,U,T,S,R,H,C,P,N)$, the local $\NameCert_{\AsymptoticEquicontinuityUp}$ surface consists exactly of compact-source admission through $K$, continuous-family admission through $F$, family equicontinuity admission through $E$, shared uniform-modulus export through $U$, asymptotic tail admission through $T$, finite stream-window inspection through $S$, terminal real-seal routing through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:asymptotic-equicontinuity-carrier}. The rows $K,F,E,U,T,S,R$ are the complete analytic inventory, and $H,C,P,N$ are the structural rows that transport, replay, source, and name that inventory.

The compact and family rows are read before the tail row. The compact source $K$ supplies only finite-net and metric-distance data, while $F$ and $E$ expose the continuous-family and shared equicontinuity ledger. The row $U$ then records the uniform family modulus already folded through compact finite-net data.

The asymptotic step is exactly the row $T$. Once $T$ has displayed the finite threshold, the stream row $S$ may inspect only tail-admitted windows, and the real seal $R$ may be reached only through those windows.

Finally the carrier has no coordinate for a selected subsequence, host function-space topology, quotient map equality, external modulus function, countable tail choice, or ambient Real completeness. The listed rows therefore exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Asymptotic equicontinuity modulus handoff]
\label{thm:asymptotic-equicontinuity-modulus-handoff}
Every compact-window or Real-facing consumer of an accepted $\AsymptoticEquicontinuityUp$ carrier receives its tail-stable modulus through the displayed route
$$
\CompactUniformRadiusChoiceUp,\ \UniformEquicontinuityModulusUp,\ \EquicontinuityFamilyUp,\ \CompactMetricUp,\ \ContinuousMapUp,\ \StreamNameUp,\ \RealUp .
$$
The handoff supplies one finite tail threshold and one compact-source modulus route, not a selected subsequence, host function-space compactness proof, quotient equality of functions, countable-choice tail, or ambient Real-completeness witness.
\end{theorem}
\begin{proof}
Start with the compact-source and family evidence. The rows $K,F,E$ display the compact metric source, the continuous-map family window, and the equicontinuity-family ledger. The compact radius-choice and uniform-equicontinuity rows are then read through $U$, so the source radius is already a finite compact-net handoff.

Next expose the tail row $T$. It records the threshold after which the same compact-source modulus route must serve the accepted family window. The stream row $S$ opens only the finite windows admitted by this threshold, and $R$ seals the resulting distance and tolerance reads as $\RealUp$ data.

The structural rows do not add analytic content. They preserve the displayed route componentwise, replay it as a continuation, record its provenance, and name it locally. Hence every consumer receives the same finite modulus handoff, with no hidden selector or external compactness principle.
\end{proof}

\falsifiablePrediction{An accepted $\AsymptoticEquicontinuityUp$ read is refuted if it reaches a Real-facing tail claim without first exposing the compact source, continuous-family ledger, equicontinuity family row, uniform-equicontinuity modulus row, asymptotic tail threshold, StreamName windows, Real seal, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{compact-uniform-radius-choice, uniform-equicontinuity-modulus, equicontinuityfamily, compactmetric, continuousmap, streamname, real: none alone is bijective with $\AsymptoticEquicontinuityUp$ because the packet binds compact finite-net modulus data to an eventual tail threshold before any Real-facing consumer can read the route.}

\closureat{AsymptoticEquicontinuityUp}{seedStr}

\begin{closurestatus}{\AsymptoticEquicontinuityUp}
  \constructivestory{An $\AsymptoticEquicontinuityUp$ packet is generated from CompactUniformRadiusChoice, UniformEquicontinuityModulus, EquicontinuityFamily, CompactMetric, ContinuousMap, StreamName, RealUp, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:asymptotic-equicontinuity-carrier}, \autoref{thm:asymptotic-equicontinuity-namecert-obligations}, and \autoref{thm:asymptotic-equicontinuity-modulus-handoff} seed the finite compact-source tail-modulus route for asymptotic equicontinuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Arzela--Ascoli compactness, host function-space topology, selected subsequences, quotient equality of maps, countable tail choice, Lean verification, or bridge checking for $\AsymptoticEquicontinuityUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source admission, family equicontinuity admission, uniform-modulus handoff, tail-threshold exactness, StreamName window restriction, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.AsymptoticEquicontinuityUp.}
\end{closurestatus}
