\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionFunctorialityUp$}
\label{ch:concrete-instances-regular-cauchy-completion-functoriality-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real constructions and Mac Lane on functorial action, the $\RegularCauchyCompletionFunctorialityUp$ packet records the finite BEDC route by which a uniformly continuous map on regular rational Cauchy sources induces a completion-map row through bounded windows. It sits after the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-map route of \autoref{ch:concrete-instances-cauchy-completion-functoriality-namecert}. The packet names only the finite functoriality surface visible to completion consumers; it does not form quotient streams, choose limit representatives, assert host function equality, or import ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy completion-functoriality carrier]
\label{def:regular-cauchy-completion-functoriality-carrier}
A $\RegularCauchyCompletionFunctorialityUp$ carrier is a finite $\BHist$ packet
$$
R=(A,B,W,D,F,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the source $\RegSeqRatUp$ row, exposing only the regular rational Cauchy data accepted by the source packet.
\item $B$ is the target $\RegSeqRatUp$ row, exposing the regular rational Cauchy data produced after the source-map read.
\item $W$ is the $\StreamNameUp$ finite-window row for the inspected source and target prefixes.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the dyadic bounds used to compare the displayed windows.
\item $F$ is the uniformly continuous source-map row, carrying the finite modulus and graph evidence that transports the source window to the target window.
\item $M$ is the induced completion-map row, read through $\CauchyCompletionFunctorialityUp$ after $A,B,W,D,F$ have been displayed.
\item $E$ is the terminal $\RealUp$ seal for the endpoint readback produced by the regular Cauchy completion route.
\item $H$ records componentwise $\hsame$ transport for the source row, target row, stream windows, tolerance ledger, source map, completion map, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a regular Cauchy map request through $A,B,W,D,F,M,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionFunctorialityUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyCompletionFunctorialityUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionFunctorialityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,W,D,F,M,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected representatives, countable choice, host function equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Regular Cauchy completion-functoriality NameCert obligations]
\label{thm:regular-cauchy-completion-functoriality-namecert-obligations}
For every accepted $\RegularCauchyCompletionFunctorialityUp$ carrier $R=(A,B,W,D,F,M,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyCompletionFunctorialityUp}$ surface consists exactly of carrier admission for the two $\RegSeqRatUp$ source rows, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, uniformly continuous source-map row, induced $\CauchyCompletionFunctorialityUp$ completion-map row, terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-completion-functoriality-carrier}. The accepted packet displays exactly the rows $A,B,W,D,F,M,E,H,C,P,N$.

The regular Cauchy source and target data are visible before any completion-map row is consumed. The stream-window row $W$ restricts the read to finite prefixes, and $D$ records the dyadic tolerance cells used to compare those prefixes.

The uniformly continuous source-map row $F$ is then the only source-side map evidence. The induced completion-map row $M$ is read through the existing $\CauchyCompletionFunctorialityUp$ surface, and the endpoint is sealed by $E$ as a finite $\RealUp$ readback.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no listed row stores quotient streams, selected representatives, countable choice, host function equality, or ambient Real completeness, the displayed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy completion-map stability]
\label{thm:regular-cauchy-completion-functoriality-map-stability}
Let $R=(A,B,W,D,F,M,E,H,C,P,N)$ and $R'=(A',B',W',D',F',M',E',H',C',P',N')$ be accepted $\RegularCauchyCompletionFunctorialityUp$ carriers whose source regular-rational rows, finite windows, dyadic tolerance ledgers, and uniformly continuous source-map rows compare componentwise by $\hsame$. Then the induced completion-map row and terminal $\RealUp$ seal compare by the same transported route. A completion consumer must read
$$
\RegSeqRatUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ F
\longmapsto
\CauchyCompletionFunctorialityUp
\longmapsto
\RealUp
$$
before it can consume the completed map.
\end{theorem}
\begin{proof}
Begin with the componentwise comparison of $A,W,D,F$ against $A',W',D',F'$. The source regular-rational row, finite windows, tolerance ledger, and uniformly continuous map evidence are therefore the same visible route for both packets.

Project the completion-map rows $M$ and $M'$. Each is generated only after the displayed regular source, stream-window, dyadic tolerance, and source-map data pass through the $\CauchyCompletionFunctorialityUp$ route.

Transport the resulting endpoint through $H$ and $H'$. The terminal rows $E$ and $E'$ are finite $\RealUp$ seals attached to the same completion-map route, so the componentwise comparison of the source route determines the map-and-seal comparison.

The replay and provenance rows $C,P,N$ and $C',P',N'$ repack the same transported route. They add no quotient stream equality, selected representative, countable-choice schedule, host function equality, or ambient Real-completeness principle.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyCompletionFunctorialityUp$ consumer is refuted if it reaches an induced completion-map row or terminal $\RealUp$ seal without displaying a $\RegSeqRatUp$ source, $\StreamNameUp$ windows, a $\DyadicRatCoreUp$ tolerance ledger, and a uniformly continuous source-map row.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real, cauchy-completion-functoriality: no listed sibling is bijective to $\RegularCauchyCompletionFunctorialityUp$ because this packet jointly binds regular rational source and target rows, finite window inspection, dyadic tolerance accounting, source-map evidence, induced completion-map transport, Real sealing, componentwise transport, replay, provenance, and local naming.}

\closureat{RegularCauchyCompletionFunctorialityUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionFunctorialityUp}
  \constructivestory{A $\RegularCauchyCompletionFunctorialityUp$ packet is generated from RegSeqRat source and target rows, StreamName finite windows, a DyadicRatCore tolerance ledger, a uniformly continuous source-map row, an induced CauchyCompletionFunctoriality completion-map row, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-functoriality-carrier}, \autoref{thm:regular-cauchy-completion-functoriality-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-functoriality-map-stability} seed the finite regular Cauchy completion-map packet over displayed regular sources, finite windows, tolerance ledgers, source-map evidence, completion-map routing, Real sealing, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected representatives, countable choice, host function equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source and target regular-rational visibility, stream-window routing, dyadic tolerance accounting, source-map exactness, completion-map stability, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyCompletionFunctorialityUp.}
\end{closurestatus}
