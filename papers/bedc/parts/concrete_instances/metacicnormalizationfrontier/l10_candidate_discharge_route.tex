\begin{theorem}[MetaCIC normalization frontier L10 candidate-discharge route]
\label{thm:metacic-normalization-frontier-l10-candidate-discharge-route}
Every candidate-normalization discharge read of an accepted $\MetaCICNormalizationFrontierUp$ packet must display the four L10 Real and Completion source faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
before the frontier rows are consumed as a normalization-facing discharge. The read must also pass through the existing normalization audit and closed-normal route siblings, including \autoref{ch:concrete-instances-metacicnormalizationaudit-namecert}, \autoref{ch:concrete-instances-metacicnormalizationauditseal-namecert}, \autoref{ch:concrete-instances-closed-normal-route-certificate-namecert}, \autoref{thm:metacic-normalization-frontier-ground-compiler-route}, and \autoref{thm:metacic-normalization-frontier-discharge-route}. The route exports no full strong normalization, global confluence, beta-conversion decidability, quotient term equality, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierL10CandidateDischargeRoute}
\begin{proof}
Start with the displayed carrier $F=(C,K,E,N,S,H,R,P,L)$. The candidate rows $C,K$ can be consumed only through the same frontier packet that also displays endpoint evidence, obstruction retention, replay, provenance, and local naming.

Read the ground-compiler route and the discharge route. They force the normalization-facing consumer through the carrier, candidate-evidence boundary, endpoint boundary, residual obstruction, and closed-normal handoff before any discharge is public.

Now attach the L10 faces as source rows for the discharge. $\DyadicRatCoreUp$ supplies the finite tolerance ledger, $\StreamNameUp$ supplies finite observation windows, $\RegSeqRatUp$ supplies regular rational readback, and $\RealUp$ supplies the terminal Real seal. These faces are source displays, not selected limits or ambient completeness principles.

Finally use the audit and closed-normal route siblings named in the statement. They keep the candidate discharge tied to finite audit, seal, and route rows, so the consumer cannot replace it by full strong normalization, global confluence, beta-conversion decidability, quotient term equality, selected limits, or Real completeness.
\end{proof}
