\begin{theorem}[MetaCIC normalization frontier ground-compiler route]
\label{thm:metacic-normalization-frontier-ground-compiler-route}
Every accepted $\MetaCICNormalizationFrontierUp$ candidate-evidence boundary routes its TasteGate-facing conservativity and no-hidden-input obligations through the displayed ground-compiler recognition rows before any normalization boundary is exported. Concretely, a consumer of
$$
F=(C,K,E,N,S,H,R,P,L)
$$
must first read the carrier surface of \autoref{def:metacic-normalization-frontier-carrier}, then the candidate-evidence route of \autoref{thm:metacic-normalization-frontier-candidate-evidence-totality}, and then the root normal-form boundary of \autoref{thm:metacic-normalization-frontier-normal-form-boundary}. Only after those rows are visible may the consumer use the TasteGate row certified at the core surface, and that use exports no hidden normalizer, host normalization theorem, erased subject-reduction transport, or quotient-level term equality.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierGroundCompilerRoute}
\begin{proof}
Project the core carrier. The rows $C,K,E,N,S,H,R,P,L$ are the only accepted frontier coordinates, and the core surface records the TasteGate-facing packet through the same finite carrier rather than through an external normalization oracle.

Next read the candidate-evidence boundary. The route through $C,K$ is fixed by \autoref{thm:metacic-normalization-frontier-candidate-evidence-totality}, while \autoref{thm:metacic-normalization-frontier-seal-boundary} keeps the seal and audit-facing rows visible before closed-normal use.

The root normal-form boundary then forces finished-normal reads through $E,N$ with $S,H,R,P,L$ still displayed. Thus the conservativity and no-hidden-input obligations face the ground-compiler recognition rows before normalization is exported.

Finally apply the non-escape rows \autoref{thm:metacic-normalization-frontier-normal-route-nonescape} and \autoref{thm:metacic-normalization-frontier-substitution-stability}. They exclude hidden normalizers, host normalization, erased subject-reduction transport, and quotient term equality, so the TasteGate-facing route remains a ground-compiler route over the displayed packet.
\end{proof}

\begin{theorem}[MetaCIC normalization frontier TasteGate route]
\label{thm:metacic-normalization-frontier-tastegate-route}
Any promotion of $\MetaCICNormalizationFrontierUp$ beyond its seed and scaffold boundary must cite the finite candidate-evidence rows $C,K$, the finished-normal endpoint rows $E,N$, the subject-reduction obstruction row $S$, and the ground-compiler route rows $H,R,P,L$ already displayed in this chapter. It may not replace those rows by a hand-written global normalization claim, a hidden normalizer, erased subject-reduction transport, or quotient-level term equality.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierTasteGateRoute}
\begin{proof}
Begin with the core surface \autoref{def:metacic-normalization-frontier-carrier} and \autoref{thm:metacic-normalization-frontier-namecert-obligations}. These rows expose $C,K,E,N,S,H,R,P,L$ as the finite packet and make candidate evidence, finished-normal evidence, obstruction accounting, and downstream replay separate obligations.

Next read the formal-target rows, including \autoref{thm:metacic-normalization-frontier-formal-target-carrier-coverage}, \autoref{thm:metacic-normalization-frontier-formal-target-classifier-totality}, and \autoref{thm:metacic-normalization-frontier-formal-target-ledger-nonescape}. They keep any promotion attached to the same carrier and classifier rows rather than to a blanket normalization assertion.

Finally use the ground-compiler route of \autoref{thm:metacic-normalization-frontier-ground-compiler-route}. Since this chapter has AI lineage, the roadmap discipline requires TasteGate and ground-compiler evidence before the chapter leaves seed-level closure; the displayed route supplies exactly that evidence and excludes a hand-written global normalization replacement.
\end{proof}

\begin{theorem}[MetaCIC normalization frontier discharge route]
\label{thm:metacic-normalization-frontier-discharge-route}
The candidate evidence rows $C,K$, endpoint rows $E,N$, obstruction row $S$, and ground-compiler rows $H,R,P,L$ jointly discharge any $\MetaCICNormalizationFrontierUp$ normalization-frontier consumer through the displayed packet. The discharge exports neither a hidden normalizer nor a host strong-normalization theorem, erased subject-reduction transport, or quotient-level term equality.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierDischargeRoute}
\begin{proof}
Start with the TasteGate route of \autoref{thm:metacic-normalization-frontier-tastegate-route}. It requires any promotion beyond the seed and scaffold boundary to cite $C,K,E,N,S,H,R,P,L$ together, so the consumer cannot isolate candidate evidence from endpoints, obstruction accounting, or the ground-compiler rows.

Now apply the ground-compiler route of \autoref{thm:metacic-normalization-frontier-ground-compiler-route}. That route reads the carrier, candidate-evidence boundary, and root normal-form boundary before exporting any normalization boundary, and it states the same exclusions for hidden normalizers, host normalization, erased subject-reduction transport, and quotient-level term equality.

Finally read the formal-target rows cited in the TasteGate proof, especially carrier coverage, classifier totality, and ledger nonescape. These rows keep the discharge attached to the finite packet rather than to an ambient normalizer. Hence the listed rows jointly discharge the local frontier consumer while preserving the chapter's no-hidden-input boundary.
\end{proof}

\begin{definition}[MetaCIC normalization frontier ground-compiler formal target]
\label{def:metacic-normalization-frontier-ground-compiler-formal-target}
A ground-compiler formal target for $\MetaCICNormalizationFrontierUp$ is a finite packet
$$
F=(C,K,E,N,S,H,R,P,L)
$$
whose carrier rows $C,K$ display candidate evidence, whose endpoint rows $E,N$ display finished-normal evidence, whose obstruction row $S$ retains subject-reduction failure data, whose ground-compiler rows $H,R$ record recognition and replay, whose provenance row $P$ names the source packet, and whose local ledger $L$ binds the exported normalization boundary to the same displayed rows.
\end{definition}
\leandef{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierGroundCompilerFormalTarget}

\begin{theorem}[MetaCIC normalization frontier ground-compiler target scope]
\label{thm:metacic-normalization-frontier-ground-compiler-target-scope}
Any formal target for the $\MetaCICNormalizationFrontierUp$ normalization frontier must read candidate evidence $C,K$, finished-normal endpoint evidence $E,N$, obstruction retention $S$, and ground-compiler rows $H,R,P,L$ before exporting a normalization boundary. A target that exports the boundary without those rows is outside the displayed ground-compiler packet and does not inhabit the local formal-target surface.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierGroundCompilerTargetScope}
\begin{proof}
Begin with the ground-compiler route of \autoref{thm:metacic-normalization-frontier-ground-compiler-route}. It requires a consumer to read the carrier surface, candidate evidence, and root normal-form boundary before any normalization boundary is exported.

Next apply the TasteGate route in \autoref{thm:metacic-normalization-frontier-tastegate-route}. It keeps every promotion beyond the seed and scaffold boundary attached to $C,K,E,N,S,H,R,P,L$ and excludes a hand-written global normalization claim or hidden normalizer.

Finally use the discharge route of \autoref{thm:metacic-normalization-frontier-discharge-route}. The discharge theorem joins candidate evidence, endpoint evidence, obstruction retention, and ground-compiler rows into the same packet, so the formal target of \autoref{def:metacic-normalization-frontier-ground-compiler-formal-target} has exactly the stated scope.
\end{proof}

\begin{theorem}[MetaCIC normalization frontier candidate-discharge stability]
\label{thm:metacic-normalization-frontier-candidate-discharge-stability}
Let $F=(C,K,E,N,S,H,R,P,L)$ be an accepted $\MetaCICNormalizationFrontierUp$ carrier whose ground-compiler route of \autoref{thm:metacic-normalization-frontier-ground-compiler-route} and TasteGate route of \autoref{thm:metacic-normalization-frontier-tastegate-route} are displayed. If $F'$ is obtained from $F$ by componentwise $\hsame$ transport of $C,K,E,N,S,H,R,P,L$ and by displayed $\Cont$ replay of the same ground-compiler recognition and discharge rows, then $F'$ preserves the same candidate-mediated discharge boundary. In particular, the transported packet may export only the local candidate evidence, endpoint evidence, obstruction-retention, route, replay, provenance, and ledger rows already visible in $F$; it does not acquire strong normalization, confluence, erased subject-reduction transport, quotient-level term equality, or a hidden normalizer.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierCandidateDischargeStability}
\begin{proof}
Start with the ground-compiler route. It makes the carrier surface, candidate-evidence boundary, and root normal-form boundary visible before any normalization boundary is exported.

Next use the TasteGate route. It requires the same rows $C,K,E,N,S,H,R,P,L$ for any promotion beyond the seed and scaffold boundary, so componentwise $\hsame$ transport can only rename displayed rows and cannot introduce a hidden normalizer or a global normalization assertion.

Now apply the discharge route and the target-scope theorem. The discharge route joins candidate evidence, endpoint evidence, obstruction retention, and ground-compiler rows inside the packet, while the target-scope theorem excludes targets that export a boundary without those rows.

Finally replay the displayed $\Cont$ route for $F'$. Since the replay reads exactly the transported recognition and discharge rows, the candidate-mediated boundary is preserved, and none of the excluded strong-normalization, confluence, subject-reduction, quotient-equality, or hidden-normalizer claims is added.
\end{proof}

\begin{theorem}[MetaCIC normalization frontier residual-premise retention]
\label{thm:metacic-normalization-frontier-residual-premise-retention}
Let $F=(C,K,E,N,S,H,R,P,L)$ be an accepted $\MetaCICNormalizationFrontierUp$ packet. Any candidate-mediated discharge that preserves the ground-compiler route must retain the obstruction row $S$ as a visible residual premise until a separate subject-reduction row is supplied. The route may export candidate evidence, endpoint evidence, ground-compiler recognition, replay, provenance, and local ledger data, but it may not export hidden normalization, full strong normalization, confluence, erased subject reduction, quotient term equality, or a hidden normalizer.
\end{theorem}
\begin{proof}
Begin with the ground-compiler route of \autoref{thm:metacic-normalization-frontier-ground-compiler-route}. It requires a consumer to read the carrier surface, candidate-evidence route, and root normal-form boundary before any normalization boundary is exported.

Next apply the TasteGate route of \autoref{thm:metacic-normalization-frontier-tastegate-route}. That route keeps $C,K,E,N,S,H,R,P,L$ visible together, so the obstruction row $S$ cannot be silently erased by a promotion that still claims to use the displayed frontier packet.

Use the discharge route of \autoref{thm:metacic-normalization-frontier-discharge-route}. It joins candidate evidence, endpoint evidence, obstruction accounting, and ground-compiler rows in one packet, and it excludes hidden normalizers, host strong-normalization theorems, erased subject-reduction transport, and quotient-level term equality.

Finally apply candidate-discharge stability from \autoref{thm:metacic-normalization-frontier-candidate-discharge-stability}. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve exactly the same boundary, so the transported discharge still retains $S$ until an independent subject-reduction row is displayed.
\end{proof}

\begin{theorem}[MetaCIC normalization frontier ground-compiler readback]
\label{thm:metacic-normalization-frontier-ground-compiler-readback}
Let $F=(C,K,E,N,S,H,R,P,L)$ be an accepted $\MetaCICNormalizationFrontierUp$ carrier. Any normalization-frontier readback consumed by a MetaCIC audit or TasteGate consumer must pass through candidate evidence $C,K$, endpoint evidence $E,N$, residual premise $S$, and the ground-compiler rows $H,R,P,L$. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve that readback, and the route exports no strong normalization, confluence, erased subject reduction, quotient term equality, or hidden normalizer.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICNormalizationFrontierUp.MetaCICNormalizationFrontierGroundCompilerReadback}
\begin{proof}
Begin with the ground-compiler route of \autoref{thm:metacic-normalization-frontier-ground-compiler-route}. It requires every normalization-boundary consumer to read the carrier surface, candidate-evidence route, and root normal-form boundary before any frontier readback is exported.

Next apply the TasteGate route and discharge route of \autoref{thm:metacic-normalization-frontier-tastegate-route} and \autoref{thm:metacic-normalization-frontier-discharge-route}. These rows keep $C,K,E,N,S,H,R,P,L$ visible together, so an audit-facing readback cannot separate candidate evidence from endpoints, residual obstruction accounting, recognition, replay, provenance, or the local ledger.

Use candidate-discharge stability from \autoref{thm:metacic-normalization-frontier-candidate-discharge-stability}. Componentwise $\hsame$ transport only renames the displayed rows, and displayed $\Cont$ replay replays the same recognition and discharge route for the transported packet.

Finally retain the residual premise by \autoref{thm:metacic-normalization-frontier-residual-premise-retention}. Since $S$ remains visible until a separate subject-reduction row is supplied, the readback exports none of the excluded strong-normalization, confluence, erased subject-reduction, quotient-equality, or hidden-normalizer claims.
\end{proof}
