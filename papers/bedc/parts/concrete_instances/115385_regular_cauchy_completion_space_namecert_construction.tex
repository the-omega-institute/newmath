\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionSpaceUp$}
\label{ch:concrete-instances-regular-cauchy-completion-space-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyCompletionSpaceUp$ packet records the L10 completion carrier whose points are read through regular Cauchy rational data rather than through an ambient quotient-completion theorem. It uses the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational tail surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal sealing discipline of \autoref{ch:concrete-instances-real-namecert}. The chapter names the space-level carrier sitting behind the route and functor fragments already visible in the Cauchy and completion dossiers.

\begin{definition}[Regular Cauchy completion space carrier]
\label{def:regular-cauchy-completion-space-carrier}
A $\RegularCauchyCompletionSpaceUp$ carrier is a finite $\BHist$ packet
$$
S=(X,Q,W,D,R,L,E,H,C,P,N)
$$
with the following roadmap rows:
\begin{enumerate}
\item Traditional target. $X$ names the Bishop--Bridges regular Cauchy completion space whose completed points are represented by regular rational Cauchy data.
\item BEDC source. $Q$ is the $\RegSeqRatUp$ source row and $W$ is the $\StreamNameUp$ observation-window row through which every finite precision read is opened.
\item Carrier. $D$ is the $\DyadicRatCoreUp$ tolerance ledger and $R$ is the $\RealUp$ seal row reached after the regular rational tail has been inspected.
\item Classifier. The local classifier compares accepted packets only through $X,Q,W,D,R,L,E,H,C,P,N$ and never through host quotient equality or selected limit representatives.
\item Stability obligations. $L$ records tail stability for later windows preserving the same regular rational source, dyadic tolerance ledger, and Real seal route.
\item Ledger. $E$ records the completion ledger row: every completed point read factors through the displayed $Q,W,D,R,L$ rows before a space-level consumer sees $X$.
\item Core theorem list. The local seed surface is \autoref{thm:regular-cauchy-completion-space-namecert-obligations} and \autoref{thm:regular-cauchy-completion-space-l10-handoff}.
\item Exactness predicate. Exactness means that the space-level completion read is precisely the displayed route from regular Cauchy rational tails through finite windows, dyadic tolerances, and the Real seal.
\item Standard bridge status. No standard bridge is asserted in this chapter; the packet is a paper-side NameCert carrier awaiting a bridge theorem.
\item Status. $H$ supplies componentwise $\hsame$ transport, $C$ supplies displayed $\Cont$ replay, $P$ supplies $\Pkg$ provenance over the listed sibling certificates, and $N$ is the local $\NameCert_{\RegularCauchyCompletionSpaceUp}$ row.
\end{enumerate}
The carrier has no coordinate for countable choice, a selected limit, quotient stream equality, host metric completion, trichotomy, or an ambient completeness principle.
\end{definition}

\begin{theorem}[Regular Cauchy completion space NameCert obligations]
\label{thm:regular-cauchy-completion-space-namecert-obligations}
For every accepted $\RegularCauchyCompletionSpaceUp$ packet
$$
S=(X,Q,W,D,R,L,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyCompletionSpaceUp}$ surface consists exactly of the space row $X$, regular rational source row $Q$, finite $\StreamNameUp$ window row $W$, $\DyadicRatCoreUp$ tolerance row $D$, terminal $\RealUp$ seal row $R$, tail-stability row $L$, completion ledger row $E$, componentwise transport row $H$, displayed replay row $C$, provenance row $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-completion-space-carrier}. Its analytic rows are $X,Q,W,D,R,L,E$, and its structural rows are $H,C,P,N$.

The source row $Q$ and window row $W$ expose the regular rational tail at a finite precision. The dyadic row $D$ supplies tolerance data for that read, and the Real seal row $R$ is reached only after those finite rows have been consumed.

Rows $L$ and $E$ keep the tail-stability and completion-ledger obligations attached to the same packet. Since $H,C,P,N$ only transport, replay, source, and name these rows, the displayed list exhausts the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy completion space L10 handoff]
\label{thm:regular-cauchy-completion-space-l10-handoff}
Every L10 completion consumer of an accepted $\RegularCauchyCompletionSpaceUp$ carrier reads the completed point through the finite route
$$
\RegSeqRatUp\longmapsto\StreamNameUp\longmapsto\DyadicRatCoreUp\longmapsto\RealUp\longmapsto\RegularCauchyCompletionSpaceUp .
$$
The handoff exports no selected limit, quotient stream equality, countable-choice scheduler, host metric completion, or ambient Real completeness theorem.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-completion-space-namecert-obligations}. The rows $Q,W,D,R,L,E$ place the regular rational source, finite window, dyadic tolerance ledger, Real seal, tail-stability witness, and completion ledger in one displayed packet.

The structural rows $H,C,P,N$ preserve that same route under componentwise transport, replay, provenance, and local naming. They do not add a second path from a host completion object to the completed point.

Thus a consumer reaches the space-level completion carrier only after the regular Cauchy tail has passed through the finite observation and Real-seal route. The refused readings would need coordinates absent from $S$.
\end{proof}

\closureat{RegularCauchyCompletionSpaceUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionSpaceUp}
  \constructivestory{A $\RegularCauchyCompletionSpaceUp$ packet is generated from a space row, RegSeqRat source, StreamName window, DyadicRatCore tolerance ledger, Real seal, tail-stability row, completion ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-space-carrier}, \autoref{thm:regular-cauchy-completion-space-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-space-l10-handoff} seed the space-level L10 regular Cauchy completion carrier and its finite read route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, countable choice, host metric completion, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window stability, tolerance exactness, Real-seal handoff, completion-ledger exactness, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyCompletionSpaceUp.}
\end{closurestatus}
