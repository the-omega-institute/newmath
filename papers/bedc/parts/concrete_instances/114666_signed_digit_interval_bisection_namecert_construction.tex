\chapter{A Concrete Naming Certificate for $\SignedDigitIntervalBisectionUp$}
\label{ch:concrete-instances-signed-digit-interval-bisection-namecert}
\origin{human}

Bishop--Bridges constructive analysis and signed-digit interval methods read bisection as a finite schedule of inspected endpoint windows, not as a selected point of an ambient complete real line. The $\SignedDigitIntervalBisectionUp$ packet sits between the signed-digit interval carrier of \autoref{ch:concrete-instances-signed-digit-interval-real-namecert}, the finite signed-digit window surface of \autoref{ch:concrete-instances-signeddigitstream-namecert}, the signed-digit real endpoint route of \autoref{ch:concrete-instances-signeddigitreal-namecert}, the dyadic endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the closed bounded interval carrier of \autoref{ch:concrete-instances-closedboundedinterval-namecert}. The packet records interval bisection as finite NameCert data for Real and Completion consumers; it does not form quotient streams, choose representatives, invoke countable choice, use trichotomy, or import ambient Real completeness.

\begin{definition}[Signed digit interval bisection carrier]
\label{def:signed-digit-interval-bisection-carrier}
A $\SignedDigitIntervalBisectionUp$ carrier is a finite $\BHist$ packet
$$
B=(L,U,K,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower signed-endpoint row, carrying the finite signed-digit endpoint windows admitted by $\SignedDigitIntervalRealUp$ and $\SignedDigitStreamUp$.
\item $U$ is the upper signed-endpoint row, carrying the matching finite signed-digit endpoint windows for the upper side of the interval.
\item $K$ is the bisection schedule row, recording the finite left/right bisection decisions and the midpoint requests already inspected.
\item $D$ is the $\DyadicRatCoreUp$ enclosure ledger, recording normalized dyadic endpoint and midpoint rows for the scheduled bisection cuts.
\item $W$ is the $\StreamNameUp$ window row, listing exactly the endpoint and midpoint windows opened by the schedule.
\item $R$ is the $\RegSeqRatUp$ readback row for the normalized signed endpoints and midpoint enclosures.
\item $E$ is the $\RealUp$ seal row exposed only after the signed endpoints, bisection schedule, dyadic enclosure ledger, windows, and regular readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for signed endpoint rows, bisection schedules, dyadic enclosure rows, window rows, readback rows, seal rows, replay rows, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a bisection request through signed endpoint windows, dyadic midpoint normalization, enclosure refinement, regular readback, and the terminal seal.
\item $P$ is the $\Pkg$ provenance over $\SignedDigitIntervalBisectionUp$, $\SignedDigitIntervalRealUp$, $\SignedDigitStreamUp$, $\SignedDigitRealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ClosedBoundedIntervalUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SignedDigitIntervalBisectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,U,K,D,W,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, a chosen infinite signed-digit expansion, real trichotomy, countable choice, open-cover compactness, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Signed digit interval bisection NameCert obligations]
\label{thm:signed-digit-interval-bisection-namecert-obligations}
For every accepted $\SignedDigitIntervalBisectionUp$ carrier
$$
B=(L,U,K,D,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\SignedDigitIntervalBisectionUp}$ obligations are exhausted by carrier admission for the displayed rows; signed-endpoint admission through $L,U$; bisection-schedule admission through $K$; dyadic midpoint and enclosure admission through $D$; finite-window admission through $W$; regular rational readback through $R$; terminal Real sealing through $E$; componentwise transport through $H$; continuation replay through $C$; package provenance through $P$; and local naming through $N$.

No obligation may call on quotient streams, chosen representatives, countable choice, trichotomy, open-cover compactness, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:signed-digit-interval-bisection-carrier}. The endpoint rows $L,U$ and the schedule row $K$ are the only sources for bisection data, and the window row $W$ records the finite observations allowed by that schedule.

The dyadic enclosure row $D$ normalizes endpoint and midpoint observations before any readback is consumed. Thus the bisection route is ledger-first: midpoint refinement is displayed as dyadic enclosure data rather than as a host order comparison or an infinite choice of sides.

The regular readback and seal rows $R,E$ then receive only the displayed enclosure route. The structural rows $H,C,P,N$ transport, replay, source, and name the same packet, so the listed rows exhaust the local certificate and leave the excluded principles outside the carrier.
\end{proof}

\begin{theorem}[Signed digit interval bisection nested interval route]
\label{thm:signed-digit-interval-bisection-nested-interval-route}
Every accepted $\SignedDigitIntervalBisectionUp$ packet routes its bisection schedule as a finite nested closed-interval read
$$
\SignedDigitIntervalRealUp \longrightarrow \DyadicRatCoreUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp .
$$
The route keeps each midpoint refinement attached to the same signed endpoint rows and the same closed bounded interval provenance before any Real-facing consumer reads the terminal seal.
\end{theorem}
\begin{proof}
Start with the signed endpoint rows. The interval source is supplied by $\SignedDigitIntervalRealUp$, with the lower and upper endpoint windows carried by $L$ and $U$.

Apply the bisection schedule row $K$ and the dyadic enclosure ledger $D$. Each scheduled midpoint request refines the displayed closed interval by a finite dyadic row, so nestedness is witnessed by endpoint and midpoint enclosure data rather than by a completed intersection point.

Finally read through $W,R,E,H,C,P,N$. The $\StreamNameUp$ windows and $\RegSeqRatUp$ readback keep the nested route finite, while the $\RealUp$ seal and structural rows preserve the same packet for downstream consumers. The route imports no quotient stream, chosen representative, countable-choice schedule, trichotomy branch, or ambient Real completeness theorem.
\end{proof}

\begin{theorem}[Signed digit interval bisection Real seal nonescape]
\label{thm:signed-digit-interval-bisection-real-seal-nonescape}
No $\RealUp$ seal read accepted by a $\SignedDigitIntervalBisectionUp$ packet can bypass the displayed route
$$
L,U,K,D,W,R,E,H,C,P,N .
$$
Thus every Real-facing consumer receives only signed endpoint rows, the finite bisection schedule, the dyadic enclosure ledger, StreamName windows, RegSeqRat readback, transport, replay, provenance, and local naming; it receives no quotient stream equality, chosen infinite expansion, selected representative, countable choice, trichotomy, open-cover compactness, or ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with carrier projection. The terminal seal row $E$ is not a detached real object; it is displayed only after $L,U,K,D,W,R$ have been read in the accepted packet.

Use the nested interval route above. It identifies the path to the seal with signed endpoints, scheduled bisection decisions, dyadic midpoint enclosures, finite windows, and regular rational readback.

The structural rows $H,C,P,N$ then preserve and name that same route. Since the carrier contains no quotient stream coordinate, representative chooser, countable-choice schedule, trichotomy row, compactness object, or ambient completeness witness, none of those objects can escape through the terminal seal.
\end{proof}

\closureat{SignedDigitIntervalBisectionUp}{seedStr}

\begin{closurestatus}{\SignedDigitIntervalBisectionUp}
  \constructivestory{A $\SignedDigitIntervalBisectionUp$ packet is generated from signed endpoint rows, a bisection schedule, DyadicRatCore enclosure rows, StreamName finite windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:signed-digit-interval-bisection-carrier}, \autoref{thm:signed-digit-interval-bisection-namecert-obligations}, \autoref{thm:signed-digit-interval-bisection-nested-interval-route}, and \autoref{thm:signed-digit-interval-bisection-real-seal-nonescape} seed the finite signed-digit interval bisection route from endpoint windows to RealUp seal data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, chosen signed-digit representatives, countable choice, trichotomy, open-cover compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bisection schedule admission, dyadic enclosure exactness, regular readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.SignedDigitIntervalBisectionUp.}
\end{closurestatus}
