\chapter{A Concrete Naming Certificate for $\CauchyFilterLocatedRealExtractionUp$}
\label{ch:concrete-instances-cauchy-filter-located-real-extraction-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\CauchyFilterLocatedRealExtractionUp$ packet records the finite route by which a Cauchy-filter surface is read as located-real window data. It consumes the Cauchy filter interface of \autoref{ch:concrete-instances-cauchy-filter-criterion-namecert}, the located-real comparison surface of \autoref{ch:concrete-instances-located-real-line-namecert}, the stream-name observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite extraction route only; it is not ambient Real completeness, a quotient filter construction, a selected limit, or a countable-choice principle.

\begin{definition}[Cauchy-filter located-real extraction carrier]
\label{def:cauchy-filter-located-real-extraction-carrier}
A $\CauchyFilterLocatedRealExtractionUp$ carrier is a finite $\BHist$ packet
$$
E=(F,L,S,Q,D,R,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\CauchyFilterUp$ base row, exposing the filter-facing Cauchy surface and its finite request windows.
\item $L$ is the $\LocatedRealUp$ window row, recording the lower and upper located cuts read from the accepted filter request.
\item $S$ is the $\StreamNameUp$ observation row, listing the finite observation windows through which the filter data are sampled.
\item $Q$ is the $\RegSeqRatUp$ readback row, translating the sampled filter windows into regular rational approximants.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic mesh, modulus, and separation checks used by the located window.
\item $R$ is the $\RealUp$ seal row for the extracted endpoint and readback data.
\item $T$ records componentwise $\hsame$ transport for $F,L,S,Q,D,R,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a filter request through the observation, rational readback, dyadic tolerance, located-window, and Real-seal rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyFilterLocatedRealExtractionUp$, $\CauchyFilterUp$, $\LocatedRealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyFilterLocatedRealExtractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,L,S,Q,D,R,T,C,P,N$. It has no coordinate for ambient $\RealUp$ completeness, quotient filters, selected limits, countable choice, host real equality, or an unrestricted completion principle.
\end{definition}

\begin{theorem}[Cauchy-filter located-real extraction]
\label{thm:cauchy-filter-located-real-extraction}
Every accepted $\CauchyFilterLocatedRealExtractionUp$ packet extracts a located-real finite-window route from the Cauchy-filter surface:
$$
\CauchyFilterUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \LocatedRealUp \longmapsto \RealUp .
$$
The exported route is the displayed finite packet $E=(F,L,S,Q,D,R,T,C,P,N)$ and no stronger completeness, quotient-filter, selected-limit, or countable-choice object is produced.
\end{theorem}
\begin{proof}
Project the accepted carrier. The Cauchy-filter surface is $F$, the finite observation windows are $S$, the regular rational readback is $Q$, the dyadic tolerance ledger is $D$, the located-real window is $L$, and the terminal Real-facing seal is $R$.

Replay through $C$ keeps these rows in extraction order. A filter request is sampled only through the displayed stream-name windows, those observations are read back as regular rational data, and the dyadic ledger checks the tolerance needed before the located window is exposed.

Transport and provenance do not add new mathematical objects. The rows $T,P,N$ move and name only the displayed packet, so the extraction cannot escape to quotient filters, selected limits, countable choice, ambient Real completeness, or host real equality.
\end{proof}

\closureat{CauchyFilterLocatedRealExtractionUp}{seedStr}

\begin{closurestatus}{\CauchyFilterLocatedRealExtractionUp}
  \constructivestory{A $\CauchyFilterLocatedRealExtractionUp$ packet is generated from a CauchyFilter base row, LocatedReal finite window, StreamName observations, RegSeqRat readback, DyadicRatCore tolerance ledger, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-filter-located-real-extraction-carrier} and \autoref{thm:cauchy-filter-located-real-extraction} seed the finite extraction route from Cauchy-filter windows to located-real readback data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient Real completeness, quotient filters, selected limits, countable choice, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, observation-window readback, dyadic tolerance exactness, located-window extraction, Real-seal compatibility, non-escape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyFilterLocatedRealExtractionUp.}
\end{closurestatus}
