\chapter{A Concrete Naming Certificate for $\MetricSpaceTotalBoundedUp$}
\label{ch:concrete-instances-metric-space-total-bounded-namecert}
\origin{human}

Following Bishop and Bridges on total boundedness in constructive metric spaces, the $\MetricSpaceTotalBoundedUp$ packet records the finite-net bridge needed by completion-facing consumers. It sits after the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the metric-space surface of \autoref{ch:concrete-instances-metric-space-namecert}, the finite-net row of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the dyadic tolerance source of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a metric-space-specific total-bounded handoff without treating total boundedness as a free abstract parameter and without importing open-cover compactness, quotient point equality, selected centers, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[MetricSpaceTotalBounded carrier]
\label{def:metric-space-total-bounded-namecert-carrier}
A $\MetricSpaceTotalBoundedUp$ carrier is a finite $\BHist$ packet
$$
T=(M,S,B,E,D,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ distance row for the source under inspection.
\item $S$ is the $\MetricSpaceUp$ carrier row, recording the point surface on which the distance row is read.
\item $B$ is the $\TotallyBoundedUp$ budget row, displayed as finite requests rather than as an ambient compactness principle.
\item $E$ is the $\FiniteEpsilonNetUp$ row, naming the centers and cover checks actually consumed at the requested precision.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to read the positive precision and comparison cells.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular-rational precision data downstream from $D$.
\item $L$ is the terminal $\RealUp$ seal boundary reached only after the metric, total-bounded, finite-net, dyadic, and regular-rational rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for $M,S,B,E,D,R,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a finite precision request through the same rows.
\item $P$ is the $\Pkg$ provenance over $\MetricSpaceTotalBoundedUp$, $\MetricUp$, $\MetricSpaceUp$, $\TotallyBoundedUp$, $\FiniteEpsilonNetUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetricSpaceTotalBoundedUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,S,B,E,D,R,L,H,C,P,N$. It has no coordinate for open-cover compactness, quotient point equality, selected centers outside $E$, countable choice, host compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[MetricSpaceTotalBounded NameCert obligations]
\label{thm:metric-space-total-bounded-namecert-obligations}
For every accepted $\MetricSpaceTotalBoundedUp$ carrier
$$
T=(M,S,B,E,D,R,L,H,C,P,N),
$$
the local $\NameCert_{\MetricSpaceTotalBoundedUp}$ surface consists exactly of carrier admission for the metric row, metric-space source, total-bounded budget, finite-epsilon-net row, dyadic tolerance ledger, regular-rational readback, terminal Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The finite-net exactness clause requires every cover read to expose $E$ under the precision named by $D$ and $R$. The metric-source clause requires $M$ and $S$ to be consumed before any total-bounded budget is read. The non-escape clause refuses open-cover compactness, quotient point equality, selected centers outside the displayed net, countable choice, host compactness, and ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metric-space-total-bounded-namecert-carrier}. The rows $M,S,B,E,D,R,L,H,C,P,N$ are the complete visible inventory of the accepted packet.

Metric-source exposure is the first projection step. A consumer receives the distance row $M$ only together with the metric-space source row $S$, so the total-bounded budget cannot be read as an untyped external predicate.

Finite-net exactness is the next projection step. The budget row $B$ reaches a concrete finite net only through $E$, and the positive precision is read through the dyadic row $D$ and regular-rational row $R$ before any Real seal $L$ is available.

Transport, replay, provenance, and local naming preserve the same route. Since the carrier contains no coordinate for open-cover compactness, quotient point equality, selected centers outside the finite net, countable choice, host compactness, or ambient Real completeness, none of those data can be exported by the local NameCert surface.
\end{proof}

\begin{theorem}[MetricSpaceTotalBounded finite-net handoff]
\label{thm:metric-space-total-bounded-finite-net-handoff}
Every compact-metric or completion-facing consumer of an accepted $\MetricSpaceTotalBoundedUp$ carrier reaches finite total-bounded data only through the displayed route
$$
M,S,B,E,D,R,L,H,C,P,N .
$$
In particular, the consumer receives a metric-space-specific finite-net handoff: the $\FiniteEpsilonNetUp$ row $E$ is read under the $\MetricUp$ row $M$, $\MetricSpaceUp$ row $S$, $\TotallyBoundedUp$ budget $B$, $\DyadicRatCoreUp$ precision row $D$, and $\RegSeqRatUp$ readback row $R$ before the terminal $\RealUp$ seal $L$ may be consumed.
\end{theorem}
\begin{proof}
Begin with the obligation surface of \autoref{thm:metric-space-total-bounded-namecert-obligations}. It places the metric and metric-space rows before the total-bounded budget and finite-net rows.

Read the finite net through $E$. The centers and cover checks are visible only at the displayed precision, so the dyadic and regular-rational rows $D$ and $R$ remain upstream of the terminal Real seal.

Finally use the structural rows $H,C,P,N$. They replay the same finite route for compact-metric and completion-facing consumers and do not add open-cover compactness, quotient point equality, a selected center outside $E$, countable choice, host compactness, or ambient completeness. The handoff is therefore the concrete finite-net surface over the named metric space.
\end{proof}

\falsifiablePrediction{Every accepted $\MetricSpaceTotalBoundedUp$ read is refuted if it reaches a finite-net, compact-metric, or completion-facing consumer without displaying the metric row, metric-space source row, total-bounded budget, finite-epsilon-net row, dyadic tolerance row, regular-rational readback row, Real seal boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence.}

\independenceWitness{metric, metric-space, total-bounded, finite-epsilon-net, dyadicratcore, regseqrat, real: $\MetricSpaceTotalBoundedUp$ is not bijective to any listed sibling because it jointly carries a metric-space source, a total-bounded budget, a finite-net row, dyadic and regular-rational precision rows, a Real seal, transport, replay, provenance, and local naming as one finite handoff packet.}

\closureat{MetricSpaceTotalBoundedUp}{seedStr}

\begin{closurestatus}{\MetricSpaceTotalBoundedUp}
  \constructivestory{A $\MetricSpaceTotalBoundedUp$ packet is generated from a metric row, metric-space source row, total-bounded budget, finite-epsilon-net row, dyadic tolerance row, regular-rational readback row, terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metric-space-total-bounded-namecert-carrier}, \autoref{thm:metric-space-total-bounded-namecert-obligations}, and \autoref{thm:metric-space-total-bounded-finite-net-handoff} seed the metric-space finite-net route over total-bounded, dyadic, regular-rational, and Real-facing rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient point equality, selected centers outside the displayed net, countable choice, host compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked metric-source admission, total-bounded budget exactness, finite-net handoff, dyadic and regular-rational precision nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MetricSpaceTotalBoundedUp.}
\end{closurestatus}
