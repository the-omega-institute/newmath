\chapter{A Concrete Naming Certificate for $\BinderBudgetSealUp$}
\label{ch:concrete-instances-binderbudgetseal-namecert}
\origin{ai}

The $\BinderBudgetSealUp$ surface turns closed-term substitution into finite NameCert data. It records the binder-depth boundary at which a MetaCIC term is closed, the shift route at that boundary, the substitution route at the same boundary, and the consumer handoff that lets self-compilation read the boundary without receiving an unregistered free variable.

\begin{definition}[BinderBudgetSeal carrier]
\label{def:binder-budget-seal-carrier}
A $\BinderBudgetSealUp$ carrier is a finite $\BHist$ packet
$$
B=(D,T,V,S,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the binder-depth budget row naming the finite boundary at which the term is certified closed;
\item $T$ is the MetaCIC term row whose free variables are contained below the boundary named by $D$;
\item $V$ is the displayed substitution payload row, allowed to be inspected only through the boundary route;
\item $S$ is the shift route row recording that shifting at the closed boundary returns the same term;
\item $U$ is the substitution route row recording that substitution at the closed boundary returns the same term;
\item $H$ records the componentwise $\hsame$ transports for the depth, term, payload, shift, substitution, package, and local naming rows;
\item $C$ records the displayed $\Cont$ routes by which self-compilation consumers read $D,T,V,S,U$ without leaving the finite packet;
\item $P$ is the $\Pkg$ provenance row over $\BHist$, $\hsame$, $\Cont$, the MetaCIC closed-term syntax rows, and local certificate dependencies;
\item $N$ is the local $\NameCert_{\BinderBudgetSealUp}$ row.
\end{enumerate}
The local classifier compares two accepted carriers only by the displayed binder-depth row, term row, payload row, shift route, substitution route, and their recorded $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance.
\end{definition}

\begin{theorem}[BinderBudgetSeal NameCert obligations]
\label{thm:binder-budget-seal-namecert-obligations}
The local $\NameCert_{\BinderBudgetSealUp}$ surface for \autoref{def:binder-budget-seal-carrier} has five finite obligations: expose the binder-depth carrier $B=(D,T,V,S,U,H,C,P,N)$; restrict comparison to the displayed depth, term, payload, route, transport, package, and local naming rows; route shift reads through the closed-boundary row $S$; route substitution reads through the closed-boundary row $U$; and exclude any export of fresh free variables, host capture-avoidance primitives, quotient syntax equality, or compiler-stage equality outside the packet.
\end{theorem}
\leanchecked{BEDC.Derived.BinderBudgetSealUp.BinderBudgetSealTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier exposure is projection from the finite tuple. The binder-depth row, term row, payload row, shift route, substitution route, transport rows, continuation routes, package provenance, and local naming row are all displayed coordinates.

Classifier locality follows from the last sentence of the carrier definition. A comparison cannot inspect a hidden syntax quotient, a host-level capture-avoidance predicate, or a compiler-stage equality because no such coordinate is present.

The two route obligations are read from $S$ and $U$. The shift route records the closed-boundary non-escape law, and the substitution route records the matching non-escape law for the displayed payload. Thus every consumer reaches the closed-term boundary before it can read binder movement or substitution data.
\end{proof}

\begin{theorem}[BinderBudgetSeal shift non-escape]
\label{thm:binder-budget-seal-shift-nonescape}
For an accepted $\BinderBudgetSealUp$ carrier $B=(D,T,V,S,U,H,C,P,N)$, every shift request at the boundary named by $D$ factors through the displayed route $S$ before the term row $T$ is returned to a consumer. The route exports the same MetaCIC term row and no fresh free variable, depth extension, quotient syntax equality, or host binder operation.
\end{theorem}
\begin{proof}
Begin with the binder-depth coordinate. The row $D$ names the finite closed boundary for the term row $T$, so a shift request at that boundary must first read $D$.

Next read the shift route $S$. The route is the packet coordinate that records the closed-boundary identity for binder movement, so the consumer receives $T$ through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Finally apply the non-escape clause of the NameCert obligations. Since the carrier has no coordinate for a new free variable, depth extension, syntax quotient, or host binder operation, the shift request cannot export any such object.
\end{proof}

\begin{theorem}[BinderBudgetSeal substitution non-escape]
\label{thm:binder-budget-seal-substitution-nonescape}
For an accepted $\BinderBudgetSealUp$ carrier $B=(D,T,V,S,U,H,C,P,N)$, every substitution request at the boundary named by $D$ factors through the displayed route $U$ before the term row $T$ is returned to a consumer. The payload row $V$ is read only as boundary data, and the route exports no substituted free variable, host capture-avoidance primitive, quotient syntax equality, or compiler-stage equality outside the finite packet.
\end{theorem}
\begin{proof}
Start from the closed-boundary row $D$ and the term row $T$. The substitution request is accepted only at the boundary named by $D$, so the payload row $V$ is not a separate source of free-variable data.

Read the substitution route $U$. This row records the closed-boundary return of the original term, and the continuation routes in $C$ let a consumer inspect that result without leaving the packet.

Use the NameCert boundary obligation. Because the carrier contains no field for host capture avoidance, quotient syntax equality, compiler-stage equality, or a fresh substituted free variable, the substitution request is exhausted by $D,T,V,U$ and the displayed provenance rows.
\end{proof}

\begin{theorem}[BinderBudgetSeal self-compile handoff]
\label{thm:binder-budget-seal-self-compile-handoff}
Every self-compilation consumer that receives an accepted $\BinderBudgetSealUp$ carrier receives a finite packet over the same binder-depth row, MetaCIC term row, substitution payload row, shift route, substitution route, $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row. The handoff is exactly the closed-boundary budget for shift and substitution non-escape, and it does not claim full compiler closure, normalization, definitional equality, quotient syntax equality, or ambient host soundness.
\end{theorem}
\begin{proof}
First bind the shift side. By \autoref{thm:binder-budget-seal-shift-nonescape}, a boundary shift request reads $D$ and $S$ before returning the displayed term row.

Next bind the substitution side. By \autoref{thm:binder-budget-seal-substitution-nonescape}, a boundary substitution request reads $D$, $T$, $V$, and $U$ before any consumer can inspect the result.

Now assemble the consumer packet. The displayed transports, continuation routes, package provenance, and local certificate rows are the only comparison and handoff data named by \autoref{thm:binder-budget-seal-namecert-obligations}.

The remaining exclusions follow from the same carrier inventory. No coordinate supplies full compiler closure, normalization, definitional equality, quotient syntax equality, or host soundness, so the handoff is exactly the finite binder-budget boundary for shift and substitution non-escape.
\end{proof}

\closureat{BinderBudgetSealUp}{seedStr}

\begin{closurestatus}{\BinderBudgetSealUp}
  \constructivestory{A $\BinderBudgetSealUp$ packet is generated from a binder-depth boundary, a MetaCIC term row, a substitution payload row, shift and substitution non-escape routes, componentwise hsame transports, displayed Cont routes, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:binder-budget-seal-carrier}, \autoref{thm:binder-budget-seal-namecert-obligations}, \autoref{thm:binder-budget-seal-shift-nonescape}, \autoref{thm:binder-budget-seal-substitution-nonescape}, and \autoref{thm:binder-budget-seal-self-compile-handoff} bind the closed-term substitution boundary as finite NameCert data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close a Lean implementation, bridge checking, full compiler closure, normalization, definitional equality, quotient syntax equality, or host soundness.}
  \upgradepath{Obligation closure requires Lean encoding of the displayed carrier and checked theorems for shift non-escape, substitution non-escape, and self-compilation handoff.}
\end{closurestatus}
