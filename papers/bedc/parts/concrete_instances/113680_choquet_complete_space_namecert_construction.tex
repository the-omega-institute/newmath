\chapter{A Concrete Naming Certificate for $\ChoquetCompleteSpaceUp$}
\label{ch:concrete-instances-choquet-complete-space-namecert}
\origin{human}

Following Choquet and Oxtoby, the $\ChoquetCompleteSpaceUp$ packet records the complete-space side of the Choquet and Baire route as finite BEDC NameCert rows. It sits beside the game interface of \autoref{ch:concrete-instances-choquet-game-namecert}, the complete-metric route of \autoref{ch:concrete-instances-completemetric-namecert}, the Baire surface of \autoref{ch:concrete-instances-complete-metric-baire-surface-namecert}, the Baire-space prefix of \autoref{ch:concrete-instances-bairespace-namecert}, the finite schedule discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names a constructive complete-space object whose evidence is a nested-open play ledger, not a selected generic point, countable-choice sequence, quotient completion, or ambient $\RealUp$ completeness theorem.

\begin{definition}[Choquet-complete-space carrier]
\label{def:choquet-complete-space-carrier}
A $\ChoquetCompleteSpaceUp$ carrier is a finite $\BHist$ packet
$$
Q=(G,M,L,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the $\ChoquetGameUp$ row naming the legal nested-open game requests and the finite play prefixes visible to the packet.
\item $M$ is the complete metric and Baire route row, read through $\CompleteMetricUp$, $\CompleteMetricBaireSurfaceUp$, and $\BaireSpaceUp$ data before any terminal seal is available.
\item $L$ is the nested-open play ledger, recording each inspected open request, successor open row, metric-ball refinement, inclusion check, player tag, and finite stability witness.
\item $S$ is the $\StreamNameUp$ finite-window row selecting the visible play prefixes, open rows, centers, radii, and requested tolerances.
\item $R$ is the $\RegSeqRatUp$ readback row for rational centers, radius budgets, and tolerance comparisons used by the nested-open route.
\item $E$ is the $\RealUp$ seal reached only after the Choquet game row, complete metric and Baire route, nested-open ledger, stream windows, and regular rational readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for Choquet-game rows, complete-metric and Baire route rows, nested-open play ledgers, finite windows, regular readbacks, Real seals, replay data, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a complete-space request to the same game, metric, Baire, ledger, window, readback, seal, transport, provenance, and naming rows.
\item $P$ is the $\Pkg$ provenance over $\ChoquetCompleteSpaceUp$, $\ChoquetGameUp$, $\CompleteMetricUp$, $\CompleteMetricBaireSurfaceUp$, $\BaireSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ChoquetCompleteSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected generic point, countable choice, quotient completion, global winning strategy, host intersection point, or ambient $\RealUp$ completeness principle.
\end{definition}
\leanchecked{BEDC.Derived.ChoquetCompleteSpaceUp.ChoquetCompleteSpaceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Choquet-complete-space NameCert obligations]
\label{thm:choquet-complete-space-namecert-obligations}
For every accepted $\ChoquetCompleteSpaceUp$ carrier $Q=(G,M,L,S,R,E,H,C,P,N)$, the local $\NameCert_{\ChoquetCompleteSpaceUp}$ surface consists exactly of carrier admission, Choquet-game legality, complete-metric and Baire route exposure, nested-open ledger exactness, finite stream-window readback, regular rational tolerance readback, Real-seal ordering, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored outside $G,M,L,S,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:choquet-complete-space-carrier}. The analytic rows are $G,M,L,S,R,E$, and the structural rows are $H,C,P,N$.

The game and completeness content is finite. The row $G$ supplies the legal Choquet-game requests, while $M$ supplies only the displayed complete-metric and Baire route data needed by those requests.

The nested-open content is stored in $L$ and inspected through $S$ and $R$. Thus each open request, successor row, metric-ball refinement, inclusion check, center, radius, and tolerance read is visible before any Real-facing seal is read.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same route. Since the carrier has no coordinate for the excluded objects, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Choquet-complete-space nested-play stability]
\label{thm:choquet-complete-space-nested-play-stability}
Let $Q=(G,M,L,S,R,E,H,C,P,N)$ be an accepted $\ChoquetCompleteSpaceUp$ carrier. Every finite nested-open play prefix recorded by $L$ remains stable under the displayed $\hsame$ transport and $\Cont$ replay exactly when its Choquet-game row, complete-metric and Baire route row, stream windows, regular rational readback, and Real-seal handoff remain on the route
$$
G \longmapsto M \longmapsto L \longmapsto S \longmapsto R \longmapsto E .
$$
The stability claim exports no selected generic point, countable choice, quotient completion, global winning strategy, host intersection point, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the finite play prefix displayed in $L$. Its predecessor and successor open rows, metric-ball refinements, inclusion checks, and player tags are carrier rows rather than predicates over a host topology.

Read the surrounding route through $G$ and $M$. The Choquet-game row supplies legality of the finite play prefix, and the complete-metric and Baire row supplies the displayed ball and dense-open data used by that prefix.

The observable numerical data passes through $S$ and $R$. The stream windows choose the finite centers, radii, open rows, and tolerances, while the regular rational row reads those values without adding a selected limit point.

Transport and replay stability are exactly the rows $H$ and $C$, followed by provenance and local naming in $P$ and $N$. The Real seal $E$ is downstream from the same route, so a consumer cannot replace the packet by a choice sequence, quotient completion, host intersection, global strategy, or ambient completeness theorem.
\end{proof}

\closureat{ChoquetCompleteSpaceUp}{seedStr}

\begin{closurestatus}{\ChoquetCompleteSpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:choquet-complete-space-carrier}, \autoref{thm:choquet-complete-space-namecert-obligations}, and \autoref{thm:choquet-complete-space-nested-play-stability} seed the Choquet-complete-space certificate over game rows, complete-metric and Baire route rows, nested-open ledgers, finite windows, rational readback, real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected generic points, countable choice, quotient completion, global winning strategies, host intersection points, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Choquet-game legality, complete-metric and Baire route exposure, nested-open ledger exactness, finite-window readback, rational tolerance readback, real-seal ordering, transport, replay, provenance, and local naming for BEDC.Derived.ChoquetCompleteSpaceUp.}
  \constructivestory{A $\ChoquetCompleteSpaceUp$ packet is generated from ChoquetGame rows, complete-metric and Baire route rows, nested-open play ledgers, StreamName finite windows, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
