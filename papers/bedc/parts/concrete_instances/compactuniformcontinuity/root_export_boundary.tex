\begin{theorem}[CompactUniformContinuity four-source handoff correspondence]
\label{thm:compact-uniform-continuity-four-source-handoff-correspondence}
For every accepted $\CompactUniformContinuityUp$ root packet, the finite $\mathsf{ProbeBundle}$ compact-net rows, the pointwise $\ContinuousMapUp$ modulus rows, the source and target $\mathsf{MetricDistanceWitness}$ rows, the positive $\RatUp$ fold rows, and the $\RealUp$ seal row form one finite handoff correspondence. A consumer may enter the handoff through any displayed compact, continuous, metric, rational, or real row, but every such entry reads the same carrier packet with the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\CompactUniformContinuityUp}$ provenance.

The correspondence has no coordinate for open-cover compactness, sequence compactness, host minimum selection, or an external modulus. It is the root handoff of the displayed finite rows.
\end{theorem}
\begin{proof}
Start with the compact-net side. Apply \autoref{thm:compact-uniform-continuity-finite-net-handoff} to bind the finite $\mathsf{ProbeBundle}$ net to the centerwise $\ContinuousMapUp$ modulus rows.

Next read the metric and rational rows. Metric-distance routing supplies the only source and target distance observations, while radius-fold exactness identifies the positive $\RatUp$ fold as the precision row consumed by the handoff.

Finally attach the real-facing boundary. Use the real seal boundary together with the rational fold export theorem to repack the compact, continuous, metric, rational, and real rows as one carrier correspondence. Since each step is a projection from the accepted packet followed by displayed transport, no outside compactness or modulus coordinate is introduced.
\end{proof}
\leanchecked{BEDC.Derived.CompactUniformContinuityUp.CompactUniformContinuityPacket\_four\_source\_handoff\_correspondence}

\begin{theorem}[CompactUniformContinuity rational fold metric compatibility]
\label{thm:compact-uniform-continuity-rational-fold-metric-compatibility}
In an accepted $\CompactUniformContinuityUp$ packet, the rational-fold precision row is compatible with every source-center and target-output metric read consumed by the handoff of \autoref{thm:compact-uniform-continuity-four-source-handoff-correspondence}. Each $\mathsf{MetricDistanceWitness}$ read is routed through the displayed compact-net center, then compared only against the precision exported by the positive $\RatUp$ lower-bound fold and its recorded $\hsame$ and $\Cont$ transports.

Thus no metric consumer can replace the rational fold by a host minimum, an unrecorded real radius, or an external modulus choice.
\end{theorem}
\begin{proof}
Project the displayed metric witnesses from the carrier. By metric-distance routing, every source-center or target-output distance read is one of these witnesses and not a host metric call.

Apply finite-net handoff to place those metric witnesses on the same route as the centerwise $\ContinuousMapUp$ modulus rows. Radius-fold exactness then identifies the precision read by that route with the positive $\RatUp$ lower-bound fold.

The rational fold export theorem says that all downstream precision consumers factor through this fold. Therefore every metric read consumed by the handoff is compatible with the same rational precision row, and no hidden radius selector is available.
\end{proof}
\leanchecked{BEDC.Derived.CompactUniformContinuityUp.CompactUniformContinuityPacket\_rational\_fold\_metric\_compatibility}

\begin{theorem}[CompactUniformContinuity compact-continuous NameCert export]
\label{thm:compact-uniform-continuity-compact-continuous-namecert-export}
The compact-net rows, pointwise $\ContinuousMapUp$ modulus rows, $\mathsf{MetricDistanceWitness}$ rows, positive $\RatUp$ fold rows, and $\RealUp$ seal row of an accepted $\CompactUniformContinuityUp$ packet export a finite $\NameCert_{\CompactUniformContinuityUp}$ package. The exported package is exactly the displayed carrier, classifier, transport, continuation, package, and naming ledger over those rows.

It exports no open-cover compactness, sequence compactness, host minimum, host modulus, or additional real-coordinate selector.
\end{theorem}
\begin{proof}
First assemble the finite handoff correspondence from \autoref{thm:compact-uniform-continuity-four-source-handoff-correspondence}. This supplies the compact, continuous, metric, rational, and real rows as one accepted packet with fixed provenance.

Next apply \autoref{thm:compact-uniform-continuity-rational-fold-metric-compatibility}. The metric observations and rational precision row agree along the displayed finite-net route, so the exported package has a single precision ledger.

Finally use the real seal boundary and rational fold export theorem. The real-facing row can only read the sealed finite packet, while the rational fold remains the only exported precision. Repacking these rows gives the finite $\NameCert_{\CompactUniformContinuityUp}$ package and leaves no coordinate for open covers, sequence compactness, host minima, or external modulus data.
\end{proof}
\leanchecked{BEDC.Derived.CompactUniformContinuityUp.CompactUniformContinuityPacket\_compact\_continuous\_namecert\_export}
