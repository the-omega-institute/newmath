\begin{theorem}[CompactUniformContinuity finite-row audit boundary]
\label{thm:compact-uniform-continuity-finite-row-audit-boundary}
Every accepted $\CompactUniformContinuityUp$ consumer route factors through exactly the five displayed analytic rows of the packet: the $\CompactMetricUp$ $\mathsf{ProbeBundle}$ finite net, the $\ContinuousMapUp$ pointwise-modulus row, the $\mathsf{MetricDistanceWitness}$ routing row, the positive $\RatUp$ finite lower-bound fold, and the $\RealUp$ seal boundary. The factorization is carried by the same $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\CompactUniformContinuityUp}$ provenance as the carrier.

The accepted consumer surface has no coordinate for open-cover compactness, sequential compactness, host minima, or an external modulus function.
\end{theorem}
\begin{proof}
First assemble the compact and continuous rows. Apply \autoref{thm:compact-uniform-continuity-finite-net-handoff} to route source reads through the displayed finite $\mathsf{ProbeBundle}$ centers and into the centerwise $\ContinuousMapUp$ pointwise-modulus rows.

Next attach the metric and rational rows. The metric-distance routing theorem supplies the source-center and target-output $\mathsf{MetricDistanceWitness}$ reads, while \autoref{thm:compact-uniform-continuity-radius-fold-exactness} identifies the exported precision with the positive $\RatUp$ finite lower-bound fold.

The real-facing side is fixed by \autoref{thm:compact-uniform-continuity-real-seal-boundary}. It says that a $\RealUp$ consumer reads the same compact, continuous, metric, and rational rows rather than a host real minimum or external modulus.

Finally use \autoref{thm:compact-uniform-continuity-rational-fold-export} and \autoref{thm:compact-uniform-continuity-compact-continuous-namecert-export}. These theorems package the rational fold and the compact-continuous rows as the finite $\NameCert_{\CompactUniformContinuityUp}$ export. Since each cited route is a projection from the displayed carrier followed by $\hsame$, $\Cont$, $\Pkg$, and local naming transport, the five rows exhaust the accepted consumer surface and exclude open-cover compactness, sequential compactness, host minima, and external modulus functions.
\end{proof}
\leanchecked{BEDC.Derived.CompactUniformContinuityUp.CompactUniformContinuityPacket\_finite\_row\_audit\_boundary}
