\chapter{A Concrete Naming Certificate for $\DyadicUp$}
\label{ch:concrete-instances-dyadic-namecert}
\origin{human}

Following Bishop and Bridges, the $\DyadicUp$ interface records the finite dyadic naming surface used by constructive real-name and completion routes. It is the direct handoff object between $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the $\RealUp$ seal: every consumer receives displayed finite tolerance, stream-window, regular-sequence, transport, replay, provenance, and naming rows, rather than host real equality, selected binary expansions, trichotomy, or completed quotient data.

\begin{definition}[DyadicUp carrier]
\label{def:dyadic-up-carrier}
A $\DyadicUp$ carrier is a finite $\BHist$ packet
$$
D=(Q,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the $\DyadicRatCoreUp$ row, carrying the dyadic rational numerator, denominator, tolerance budget, and arithmetic ledger read by dyadic consumers.
\item $S$ is the $\StreamNameUp$ finite-window row through which requested precision windows are opened.
\item $R$ is the $\RegSeqRatUp$ regular rational sequence row attached to the same window surface.
\item $E$ is the $\RealUp$ seal row, exposing only the displayed real-name boundary reached after $Q,S,R$ have been read.
\item $H$ records componentwise $\hsame$ transport for the dyadic core, stream window, regular sequence, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a precision request through $Q,S,R$ and then to the real seal $E$.
\item $P$ is the $\Pkg$ provenance over $\DyadicUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicUp}$ row.
\end{enumerate}
The carrier contains no coordinate for selected binary expansions, host real equality, countable choice, trichotomy, quotient-stream representatives, or ambient completeness.
\end{definition}
\leandef{BEDC.Derived.DyadicUp.DyadicUp}

\begin{theorem}[DyadicUp NameCert obligations]
\label{thm:dyadic-up-namecert-obligations}
For every accepted $\DyadicUp$ carrier
$$
D=(Q,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\DyadicUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the dyadic core, stream window, regular sequence, real seal, transport, replay, provenance, and local naming rows;
\item dyadic core admission, requiring every tolerance and arithmetic read to pass through $Q$;
\item stream and regular-sequence admission, requiring the precision window $S$ and regular rational row $R$ to be displayed before any real-name consumer reads the dyadic packet;
\item real-seal handoff, requiring $E$ to expose only the $\RealUp$ boundary reached from $Q,S,R$;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same dyadic core, stream, regular sequence, and real-seal rows componentwise;
\item nonescape from selected binary expansions, host real equality, countable choice, trichotomy, quotient-stream representatives, and ambient completeness.
\end{enumerate}
No $\DyadicUp$ naming obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:dyadic-up-carrier}. The accepted packet displays exactly the dyadic rational core $Q$, finite stream window $S$, regular rational row $R$, real seal $E$, and the structural rows $H,C,P,N$.

Dyadic core, stream, and regular-sequence admission follow from the displayed route order. A precision request first reads the dyadic tolerance ledger $Q$, opens the finite window $S$, and then reads the regular rational row $R$ attached to that window.

The real-seal handoff is then forced through $E$. Since $E$ is the only Real-facing coordinate of the carrier, a downstream Real or Completion consumer cannot replace the displayed handoff by host equality, a selected binary expansion, or a quotient-stream representative.

Transport and replay are componentwise. The $\hsame$ row $H$ preserves each displayed coordinate, while the $\Cont$ row $C$ repeats the same finite path from precision request to real seal.

Finally inspect $P$ and $N$. Provenance and local naming mention only $Q,S,R,E,H,C$, so the excluded choice, trichotomy, quotient, host-equality, and completeness data have no projection from the packet.
\end{proof}
\leanchecked{BEDC.Derived.DyadicUp.DyadicNameCertObligations}

\begin{theorem}[DyadicUp obligation-closure route]
\label{thm:dyadic-up-obligation-closure-route}
For every accepted $\DyadicUp$ carrier $D=(Q,S,R,E,H,C,P,N)$, the rows $Q,S,R,E,H,C,P,N$ listed in \autoref{def:dyadic-up-carrier} and certified by \autoref{thm:dyadic-up-namecert-obligations} are sufficient to witness the $\obligationClosure$ surface consumed by downstream $\DyadicUp$ readers. The route binds $Q$ to $\DyadicRatCoreUp$, $S$ to $\StreamNameUp$, $R$ to $\RegSeqRatUp$, $E$ to $\RealUp$, and $H,C,P,N$ to $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before any Real-facing or Completion-facing read can inspect the dyadic packet.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUp\_obligation\_closure\_route}
\begin{proof}
Start from the carrier and NameCert obligations. The carrier displays exactly the dyadic core, stream window, regular rational readback, Real seal, transport, replay, provenance, and local naming rows, while the obligation theorem states that no local naming obligation is stored outside those rows.

Read the mathematical rows first. The dyadic tolerance ledger $Q$, the finite stream window $S$, the regular rational row $R$, and the terminal Real seal $E$ appear before any Real-facing or Completion-facing consumer can use the packet.

The structural rows $H,C,P,N$ then preserve, replay, source, and name the same route over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Since every obligation row is displayed before the consumer-facing reads and no excluded analytic or quotient object is a carrier coordinate, the displayed rows witness the obligation-closure surface for $\DyadicUp$ consumers.
\end{proof}

\begin{theorem}[DyadicUp obligation-closure dependency surface]
\label{thm:dyadic-up-obligation-closure-dependency-surface}
For every accepted $\DyadicUp$ obligation read, the dependency surface factors through exactly the displayed rows
$$
Q,S,R,E,H,C,P,N .
$$
Here $Q$ is read from $\DyadicRatCoreUp$, $S$ from $\StreamNameUp$, $R$ from $\RegSeqRatUp$, and $E$ from $\RealUp$ before any Real or Completion consumer can use the dyadic packet. The rows $H,C,P,N$ then transport, replay, source, and locally name that same route. No selected binary expansion, quotient stream, trichotomy, countable choice, host equality, or ambient completeness row is admitted by the dependency surface.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUp\_obligation\_closure\_dependency\_surface}
\begin{proof}
Start with \autoref{thm:dyadic-up-namecert-obligations}. Its finite obligation list admits only the dyadic core, stream window, regular rational sequence, real seal, transport, replay, provenance, and local naming rows of the carrier.

Next apply \autoref{thm:dyadic-up-streamname-regseqrat-route}. It forces every Real-facing read to expose $Q$, then $S$, then $R$ before the terminal row $E$ becomes visible.

Finally use \autoref{thm:dyadic-up-real-seal-factorization}. Since Real and Completion consumers can reach the packet only through that terminal factorization, the remaining rows $H,C,P,N$ merely preserve and name the same finite route, and the excluded binary-expansion, quotient-stream, trichotomy, choice, host-equality, and completeness data have no coordinate from which they can be read.
\end{proof}

\begin{theorem}[DyadicUp real-seal factorization]
\label{thm:dyadic-up-real-seal-factorization}
Let $D=(Q,S,R,E,H,C,P,N)$ be an accepted $\DyadicUp$ carrier. Every $\RealUp$-facing consumer of $D$ must read the displayed dyadic core $Q$, the finite stream window $S$, the regular rational row $R$, and the terminal real seal $E$ in that order before it can export a Real-facing name.

Consequently a downstream Real or Completion route cannot introduce selected binary expansions, quotient stream representatives, trichotomy, host real equality, countable choice, or ambient completeness while still claiming to consume the same $\DyadicUp$ packet.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUpRealSealFactorization}
\begin{proof}
Project the carrier of \autoref{def:dyadic-up-carrier}. The only Real-facing coordinate is $E$, and it is displayed after the dyadic rational core $Q$, finite window row $S$, and regular rational row $R$.

Apply \autoref{thm:dyadic-up-namecert-obligations}. Its dyadic core, stream, regular-sequence, and real-seal obligations force each precision request through $Q,S,R,E$ before any local name can be read by a downstream Real consumer.

The structural rows $H,C,P,N$ only preserve, replay, account for, and name that same displayed route. Since the excluded analytic and quotient objects are absent from the carrier, they cannot be inserted by a Real-facing consumer without leaving the accepted $\DyadicUp$ NameCert surface.
\end{proof}

\begin{theorem}[DyadicUp StreamName RegSeqRat route]
\label{thm:dyadic-up-streamname-regseqrat-route}
Let $D=(Q,S,R,E,H,C,P,N)$ be an accepted $\DyadicUp$ carrier. Every $\RealUp$-facing $\DyadicUp$ read first opens the $\DyadicRatCoreUp$ tolerance ledger $Q$, then the $\StreamNameUp$ finite window $S$, then the $\RegSeqRatUp$ readback row $R$, before the terminal $\RealUp$ seal $E$ is visible. Thus the dyadic route depends on the sibling chapters \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-regseqrat-namecert} as finite packet rows, not as ambient host-real completeness data.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUp\_streamname\_regseqrat\_route}
\begin{proof}
Project the carrier of \autoref{def:dyadic-up-carrier}. The displayed coordinates place $Q$, $S$, and $R$ before the only terminal seal coordinate $E$.

Apply \autoref{thm:dyadic-up-real-seal-factorization}. A Real-facing consumer cannot reach $E$ until the dyadic core, stream window, and regular rational row have all been read in the carrier order.

The sibling chapters provide exactly those finite rows: the tolerance ledger from \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite-window classifier from \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}. The structural rows $H,C,P,N$ preserve and name that route, so no selected binary expansion, quotient stream, trichotomy, host real equality, countable choice, or ambient completeness principle is exposed by the handoff.
\end{proof}

\begin{theorem}[Dyadic exponent-shift composition]
\label{thm:dyadic-exponent-shift-composition}
Two displayed $\DyadicUp$ exponent-shift tolerance ledgers compose only by common precision refinement. Any $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer of the composed tolerance row must first read the two $\DyadicRatCoreUp$ ledgers, refine them to a common finite precision window, expose the corresponding $\StreamNameUp$ and $\RegSeqRatUp$ rows, and only then consume the terminal $\RealUp$ seal.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicExponentShiftComposition}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:dyadic-up-namecert-obligations}. Each exponent-shift read is a displayed dyadic core row together with its stream window, regular sequence row, Real seal boundary, transport, replay, provenance, and local naming.

Use \autoref{thm:dyadic-up-real-seal-factorization} for both ledgers. A Real-facing read of either ledger is unavailable until the dyadic core, finite stream window, and regular rational row have all been displayed.

Now compare the two exponent-shift windows by common precision refinement. The refinement is finite because it is formed from the two displayed $\StreamNameUp$ windows and the two displayed dyadic tolerance rows; no quotient stream or selected limit is introduced.

Finally apply \autoref{thm:dyadic-up-streamname-regseqrat-route}. The composed tolerance row reaches $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ consumers only through the refined finite route, so it exposes no host quotient, selected limit, trichotomy, countable choice, or ambient Real completeness.
\end{proof}

\begin{theorem}[DyadicUp finite-prefix tolerance stabilization]
\label{thm:dyadic-up-finite-prefix-tolerance-stabilization}
Let $D=(Q,S,R,E,H,C,P,N)$ be an accepted $\DyadicUp$ carrier. Any composed dyadic tolerance read consumed by $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ must stabilize through a displayed finite prefix window before the terminal $\RealUp$ seal is visible. The stabilized route reads the $\DyadicRatCoreUp$ tolerance ledger $Q$, the finite prefix window in $S$, the $\RegSeqRatUp$ readback row $R$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\DyadicUp}$ naming before any downstream Real consumer can inspect $E$.

The route exposes a finite prefix tolerance surface only. It does not expose a selected binary expansion, quotient stream representative, trichotomy branch, host real equality, countable-choice selector, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Begin with the real-seal factorization of \autoref{thm:dyadic-up-real-seal-factorization}. It places the terminal seal $E$ strictly after the dyadic core row $Q$, finite stream window $S$, and regular rational row $R$.

Use the StreamName-RegSeqRat route of \autoref{thm:dyadic-up-streamname-regseqrat-route}. A $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer first sees the finite window row, then the regular readback row, so the consumer has no path to $E$ before the prefix window is displayed.

Now apply exponent-shift composition from \autoref{thm:dyadic-exponent-shift-composition}. Composed tolerance reads are refined to a common finite precision window, and that refinement is made from displayed dyadic ledgers and finite stream windows rather than from a quotient stream or selected limit.

The structural rows $H,C,P,N$ preserve, replay, source, and name the same stabilized prefix route. Since the carrier has no coordinate for selected binary expansions, quotient streams, trichotomy, choice, host equality, or ambient completeness, the composed tolerance read stabilizes exactly at the displayed finite prefix surface before the Real seal is visible.
\end{proof}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUpFinitePrefixToleranceStabilization}

\begin{theorem}[DyadicUp common-exponent normalization]
\label{thm:dyadic-up-common-exponent-normalization}
Let $D_0=(Q_0,S_0,R_0,E_0,H_0,C_0,P_0,N_0)$ and $D_1=(Q_1,S_1,R_1,E_1,H_1,C_1,P_1,N_1)$ be accepted $\DyadicUp$ carriers whose dyadic tolerance ledgers are read by a common $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer. Then the two accepted reads admit a finite common exponent window before the consumer inspects either terminal row.

Concretely, the consumer first projects the dyadic core rows $Q_0,Q_1$, refines the displayed stream windows $S_0,S_1$ to one finite precision request, transports the regular rational readbacks $R_0,R_1$ across the componentwise $\hsame$ rows, and only then exposes $E_0,E_1$ to the terminal $\RealUp$ handoff. The normalization exports the shared finite exponent window; it does not add a selected binary expansion, quotient stream, host equality, trichotomy, countable choice, or ambient Real-completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUpCommonExponentNormalization}
\begin{proof}
Project the two carriers of \autoref{def:dyadic-up-carrier}. Each accepted read exposes its dyadic core row, finite stream window, regular rational readback, terminal real seal, and the structural rows that transport and replay that route.

Apply \autoref{thm:dyadic-exponent-shift-composition} to the two dyadic core ledgers. The common exponent is obtained by finite precision refinement of the two displayed windows, so the construction remains inside the $\BHist$ rows already present in $D_0$ and $D_1$.

Use \autoref{thm:dyadic-up-streamname-regseqrat-route} after the refinement. Both regular rational readbacks are attached to the common window before the consumer reaches either $\RealUp$ seal.

Finally apply \autoref{thm:dyadic-up-real-seal-factorization}. Since the terminal rows are visible only after the refined finite route, the normalized handoff cannot read any quotient, choice, trichotomy, host-equality, selected-expansion, or ambient-completeness data outside the accepted packets.
\end{proof}

\begin{theorem}[Dyadic common-exponent totality for shared finite consumers]
\label{thm:dyadic-common-exponent-totality}
For any two accepted $\DyadicUp$ carriers read by the same $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer, the displayed finite windows determine a common exponent and tolerance window before either terminal $\RealUp$ seal is exposed. The total common window is read from the two dyadic core rows, the two finite stream windows, the two regular rational readbacks, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\DyadicUp}$ rows.

The route excludes selected binary expansions, quotient streams, trichotomy, choice, host equality, and ambient Real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicCommonExponentTotality}
\begin{proof}
Project the two accepted carriers through \autoref{def:dyadic-up-carrier}. The only rows available to the shared consumer are the two dyadic core ledgers, finite stream windows, regular rational readbacks, terminal seals, transport, replay, provenance, and local naming rows.

Apply dyadic exponent-shift composition from \autoref{thm:dyadic-exponent-shift-composition}. The two displayed exponent-shift ledgers compose by finite precision refinement, so a common exponent and tolerance window is determined before any terminal seal can be read.

Use common-exponent normalization, \autoref{thm:dyadic-up-common-exponent-normalization}. It places both $\StreamNameUp$ windows and both $\RegSeqRatUp$ readbacks over the common finite window and keeps the route inside componentwise $\hsame$ transport and displayed $\Cont$ replay.

Finally apply the StreamName-RegSeqRat route of \autoref{thm:dyadic-up-streamname-regseqrat-route} and the real-seal factorization of \autoref{thm:dyadic-up-real-seal-factorization}. The shared consumer reaches the terminal Real seals only after the finite common window is visible, and no carrier row supplies a selected binary expansion, quotient stream, trichotomy, choice principle, host equality, or ambient Real-completeness witness.
\end{proof}

\begin{theorem}[Dyadic common-exponent canonical window]
\label{thm:dyadic-common-exponent-canonical-window}
Any two accepted $\DyadicUp$ packets read by the same $\RealUp$ or $\RegSeqRatUp$ consumer admit a shared finite exponent window whose mantissa scaling rows, dyadic tolerance scale, componentwise $\hsame$ transport, displayed $\Cont$ replay, and $\Pkg$ provenance are stable under the $\DyadicUp$ classifier. The canonical window is read before either terminal Real seal or regular rational readback is used, and it exports no selected binary expansion, quotient stream equality, trichotomy branch, countable-choice schedule, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with common-exponent totality, \autoref{thm:dyadic-common-exponent-totality}. It determines a common exponent and tolerance window from the two dyadic core rows, finite stream windows, regular rational readbacks, transport, replay, provenance, and local naming rows.

Next apply the common-window tail envelope of \autoref{thm:dyadic-up-common-window-tail-envelope}. The shared tail envelope is the finite window in which the two mantissa scaling rows and the dyadic tolerance scale are compared before any terminal row is visible.

Finally use the Real-RegSeqRat common-window readback theorem \autoref{thm:dyadic-up-real-regseqrat-common-window-readback}. It keeps both regular rational readbacks and both Real seals downstream of the same finite window, so classifier transport, replay, and provenance preserve the canonical window without importing quotient or choice data.
\end{proof}

\begin{theorem}[DyadicUp common-window tail envelope]
\label{thm:dyadic-up-common-window-tail-envelope}
Let $D_0=(Q_0,S_0,R_0,E_0,H_0,C_0,P_0,N_0)$ and $D_1=(Q_1,S_1,R_1,E_1,H_1,C_1,P_1,N_1)$ be accepted $\DyadicUp$ carriers read by one $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer. Then the two reads determine a shared finite tail-envelope window before either terminal $\RealUp$ seal is visible.

The envelope is read from the two dyadic core ledgers $Q_0,Q_1$, the shared finite StreamName window refined from $S_0,S_1$, the two regular rational readbacks $R_0,R_1$, componentwise transport, displayed replay, provenance, and the two local $\NameCert_{\DyadicUp}$ rows. It exposes no selected binary expansion, quotient stream, trichotomy branch, countable-choice selector, host real equality, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUpCommonWindowTailEnvelope}
\begin{proof}
Project the two carriers through \autoref{def:dyadic-up-carrier}. The only rows available to the shared consumer are the two dyadic core ledgers, stream windows, regular rational readbacks, terminal seals, and structural rows.

Apply \autoref{thm:dyadic-exponent-shift-composition}. The two dyadic ledgers compose by common finite precision refinement, so the consumer obtains a single candidate window before any terminal seal can be read.

Use \autoref{thm:dyadic-up-common-exponent-normalization}. It places both StreamName windows and both RegSeqRat readbacks over the refined common exponent window, which is exactly the tail-envelope window seen by the shared consumer.

Finally apply \autoref{thm:dyadic-up-streamname-regseqrat-route}. The terminal Real seals $E_0,E_1$ become visible only after the finite window and readback rows are displayed, and the excluded binary-expansion, quotient, trichotomy, choice, host-equality, and completeness data have no coordinate in either accepted carrier.
\end{proof}

\begin{theorem}[DyadicUp Real-RegSeqRat common-window readback]
\label{thm:dyadic-up-real-regseqrat-common-window-readback}
Any two accepted $\DyadicUp$ packets consumed by the same $\RegSeqRatUp$ or $\RealUp$ route expose their $\DyadicRatCoreUp$ tolerance ledgers through one finite $\StreamNameUp$ window before either $\RegSeqRatUp$ readback or $\RealUp$ seal is available.

The readback route first projects the two dyadic core rows, refines their displayed StreamName windows to the common finite window, attaches the two regular rational readback rows to that window, and only then exposes the terminal Real seals. It exports no selected binary expansion, quotient stream, trichotomy branch, countable-choice selector, host equality, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadic-up-streamname-regseqrat-route} to each accepted packet. A shared $\RegSeqRatUp$ or $\RealUp$ consumer must read the dyadic core row, then the StreamName window, then the regular rational readback row before any terminal Real seal can be used.

Use \autoref{thm:dyadic-up-common-exponent-normalization}. The two dyadic core tolerance ledgers are refined to a common finite exponent window, and the two StreamName rows are transported into that same displayed window.

Now apply \autoref{thm:dyadic-up-common-window-tail-envelope}. It identifies the shared tail-envelope window as the finite route seen by the common consumer before either terminal row is visible.

Thus the RegSeqRat readbacks and Real seals are downstream of one finite StreamName window over the two DyadicRatCore ledgers. Since every step is a displayed carrier projection or finite refinement, no selected binary expansion, quotient stream, trichotomy, countable choice, host equality, or ambient Real completeness data is exposed.
\end{proof}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUpRealRegSeqRatCommonWindowReadback}

\begin{theorem}[DyadicUp terminal Real-seal boundary]
\label{thm:dyadicup-terminal-real-seal-boundary}
Let
$$
D=(Q,S,R,E,H,C,P,N)
$$
be an accepted $\DyadicUp$ carrier. Every $\RealUp$ or Completion-facing consumer of $D$ factors through exactly the rows $Q,S,R,E,H,C,P,N$, with $Q$ read from $\DyadicRatCoreUp$, $S$ from $\StreamNameUp$, $R$ from $\RegSeqRatUp$, and $E$ from $\RealUp$. No selected binary expansion, quotient stream, host equality, trichotomy, countable choice, or ambient Real-completeness row is readable from the terminal handoff.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUpTerminalRealSealBoundary}
\begin{proof}
Project the carrier of \autoref{def:dyadic-up-carrier}. It displays the mathematical route rows $Q,S,R,E$ and the structural rows $H,C,P,N$, and it contains no additional Real-facing or Completion-facing coordinate.

Apply \autoref{thm:dyadic-up-namecert-obligations}. The accepted NameCert surface forces dyadic core admission through $Q$, stream-window admission through $S$, regular-sequence admission through $R$, and real-seal admission through $E$.

Now use \autoref{thm:dyadic-up-real-seal-factorization} and \autoref{thm:dyadic-up-streamname-regseqrat-route}. Together they exhaust the route from the dyadic tolerance ledger through finite windows and regular rational readback before the terminal seal can be read.

The remaining rows $H,C,P,N$ only transport, replay, source, and name that route. Since the excluded binary, quotient, host-equality, trichotomy, choice, and completeness data are absent from every displayed coordinate, no terminal consumer can read them while staying inside the accepted $\DyadicUp$ packet.
\end{proof}

The $\DyadicUp$ terminal route consumes the finite dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window classifier of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. These siblings are the displayed L10 dependency route for the dyadic packet, not external host-real infrastructure.

The dyadic Cauchy interface packets collect finite approximation and
tail-envelope certificates for displayed real-name routes before Real and
Completion consumers can read them. This interface consumes the DyadicRatCore
tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert},
the RegSeqRat regular-sequence handoff of
\autoref{ch:concrete-instances-regseqrat-namecert}, and the RealUp terminal
seal of \autoref{ch:concrete-instances-real-namecert}.

\input{parts/concrete_instances/dyadic/cauchy_tail_envelope_namecert_construction}
\input{parts/concrete_instances/dyadic/dyadic_core_sibling_route}
\input{parts/concrete_instances/dyadic/real_route_sibling_dependency}

\begin{theorem}[DyadicUp obligation-closure scope]
\label{thm:dyadic-up-obligation-closure-scope}
Every accepted $\DyadicUp$ obligation read is exhausted by the dyadic core ledger, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, $\DyadicCauchyTailEnvelopeUp$ route, $\DyadicRatCoreUp$ sibling route, Real-route sibling dependency, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows. It exports no selected binary expansion, quotient stream, trichotomy, countable choice, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadic-up-namecert-obligations}. The local NameCert surface already restricts the dyadic packet to the dyadic core, stream window, regular readback, Real seal, transport, replay, provenance, and local naming rows.

Use \autoref{thm:dyadic-cauchy-tail-envelope-obligation-closure}, \autoref{thm:dyadic-core-sibling-route}, and \autoref{thm:dyadic-cauchy-real-route-sibling-dependency}. These sibling routes add the Cauchy tail-envelope obligation, DyadicRatCore dependency, and Real-facing handoff surface without introducing a second carrier coordinate.

The remaining rows are exactly componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ data. Since none of the cited routes contains selected binary expansions, quotient streams, trichotomy, countable choice, host equality, or ambient completeness, the accepted obligation read is closed at the displayed obligation surface.
\end{proof}
\leanchecked{BEDC.Derived.DyadicUp.DyadicUp\_obligation\_closure\_scope}

\begin{theorem}[DyadicUp public consumer boundary]
\label{thm:dyadic-public-consumer-boundary}
Every public $\RealUp$ or Completion-facing $\DyadicUp$ consumer must read the $\DyadicRatCoreUp$ tolerance ledger $Q$, the $\StreamNameUp$ finite window $S$, the $\RegSeqRatUp$ readback row $R$, the terminal $\RealUp$ seal $E$, the $\DyadicCauchyTailEnvelopeUp$ sibling route, the $\DyadicRatCoreUp$ sibling route, the Real-route sibling dependency, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows before exporting a dyadic public name. The boundary exports no selected binary expansion, quotient stream, trichotomy, countable choice, host equality, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with the real-seal factorization of \autoref{thm:dyadic-up-real-seal-factorization}. A Real-facing consumer cannot reach the terminal row $E$ without first reading the dyadic core row $Q$, the finite window $S$, and the regular rational readback row $R$.

Next apply the StreamName-RegSeqRat route of \autoref{thm:dyadic-up-streamname-regseqrat-route}. It identifies the visible route from $Q$ through $S$ and $R$ as the only finite real-name path available to the consumer.

Use the terminal Real-seal boundary of \autoref{thm:dyadicup-terminal-real-seal-boundary}. This pins every $\RealUp$ or Completion-facing read to the rows $Q,S,R,E,H,C,P,N$ and excludes the selected binary, quotient, choice, trichotomy, host-equality, and ambient-completeness data.

Finally apply \autoref{thm:dyadic-up-obligation-closure-scope}. The Cauchy tail-envelope route, DyadicRatCore sibling route, Real-route sibling dependency, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows are already part of the closed obligation surface, so the public consumer receives exactly that displayed package before it may export a dyadic public name.
\end{proof}

\closureat{DyadicUp}{seedStr}
\closureat{DyadicUp}{obligationStr}

\begin{closurestatus}{\DyadicUp}
  \constructivestory{A $\DyadicUp$ packet is generated from a DyadicRatCore ledger, a finite StreamName window, a RegSeqRat readback row, a RealUp seal, componentwise transport, continuation replay, package provenance, and local naming.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:dyadic-up-carrier}, \autoref{thm:dyadic-up-namecert-obligations}, \autoref{thm:dyadic-up-real-seal-factorization}, and \autoref{thm:dyadic-up-obligation-closure-scope} exhaust the finite dyadic obligation surface over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\DyadicCauchyTailEnvelopeUp$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.DyadicUp.DyadicNameCertObligations}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient streams, selected binary expansions, trichotomy, host real equality, ambient Real completeness, or a completed bridge.}
  \upgradepath{Scoped closure requires a public consumer boundary over the same dyadic core, stream window, regular readback, Real seal, sibling routes, transport, replay, provenance, and local naming rows.}
\end{closurestatus}
