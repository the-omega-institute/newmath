\chapter{A Concrete Naming Certificate for $\CauchySubbasisTailFactorizationUp$}
\label{ch:concrete-instances-cauchy-subbasis-tail-factorization-namecert}
\origin{human}

Following Kelley and Bourbaki on subbases and cofinite tail filters, the packet records the finite BEDC route from $\CauchyFilterSubbasisUp$ through $\FrechetFilterUp$ and $\FilterBaseUp$ to $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. It is grounded in the subbasis generation packet of \autoref{ch:concrete-instances-cauchy-filter-subbasis-namecert}, the cofinite-tail filter packet of \autoref{ch:concrete-instances-frechetfilter-namecert}, the filter-base interface of \autoref{ch:concrete-instances-filterbase-namecert}, the finite window naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite tail-factorization object only; it does not quotient filters, select limits, invoke countable choice, use host equality of generated filters, or assume ambient $\RealUp$ completeness.

\begin{definition}[Cauchy subbasis tail-factorization carrier]
\label{def:cauchy-subbasis-tail-factorization-carrier}
A $\CauchySubbasisTailFactorizationUp$ carrier is a finite $\BHist$ packet
$$
T=(S,A,F,B,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\CauchyFilterSubbasisUp$ row exposing primitive subbasis tokens, finite intersections, generated events, and the generated-base ledger.
\item $A$ is the tail-specialization row saying that the requested generated-base reads have been restricted to eventual sequence tails.
\item $F$ is the $\FrechetFilterUp$ row carrying the cofinite tail threshold discipline.
\item $B$ is the $\FilterBaseUp$ handoff row obtained after the subbasis route has passed through the Frechet tail surface.
\item $W$ is the $\StreamNameUp$ finite-window row inspected by completion-facing consumers.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row for the retained tail windows.
\item $E$ is the terminal $\RealUp$ seal row reached only after $S,A,F,B,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for subbasis rows, tail-specialization rows, Frechet thresholds, filterbase handoffs, stream windows, regular readbacks, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from generated subbasis reads through cofinite tail thresholds, filterbase handoffs, stream windows, regular readbacks, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\CauchySubbasisTailFactorizationUp$, $\CauchyFilterSubbasisUp$, $\FrechetFilterUp$, $\FilterBaseUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchySubbasisTailFactorizationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient filters, ultrafilter completion, selected limits, countable-choice schedules, host equality of generated filters, or ambient Real completeness.
\end{definition}

\begin{theorem}[Cauchy subbasis tail-factorization NameCert obligations]
\label{thm:cauchy-subbasis-tail-factorization-namecert-obligations}
For every accepted $\CauchySubbasisTailFactorizationUp$ carrier $T=(S,A,F,B,W,R,E,H,C,P,N)$, the local $\NameCert_{\CauchySubbasisTailFactorizationUp}$ surface consists exactly of carrier admission; $\CauchyFilterSubbasisUp$ generated-base exposure; cofinite-tail specialization; $\FrechetFilterUp$ threshold exposure; $\FilterBaseUp$ handoff; $\StreamNameUp$ finite-window exposure; $\RegSeqRatUp$ readback; $\RealUp$ sealing; componentwise $\hsame$ transport; displayed $\Cont$ replay; $\Pkg$ provenance; and local naming.
\end{theorem}
\begin{proof}
Project the carrier rows from \autoref{def:cauchy-subbasis-tail-factorization-carrier}. The row $S$ supplies only the finite subbasis generation surface, and the row $A$ narrows that surface to eventual sequence tails.

Read the cofinite-tail route next. The $\FrechetFilterUp$ row $F$ supplies the threshold discipline used by the specialized tail reads, and the $\FilterBaseUp$ row $B$ is visible only after that cofinite route has been displayed.

The completion-facing rows are downstream. The stream window row $W$, regular rational readback row $R$, and terminal Real seal row $E$ are reached after the generated subbasis and Frechet tail rows have been opened.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since none of the carrier coordinates supplies quotient filters, selected limits, countable choice, host equality, or ambient Real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cauchy subbasis tail-factorization Real handoff]
\label{thm:cauchy-subbasis-tail-factorization-real-handoff}
Every $\RealUp$-facing read from an accepted $\CauchySubbasisTailFactorizationUp$ packet must pass through
$$
\CauchyFilterSubbasisUp \longmapsto \FrechetFilterUp \longmapsto \FilterBaseUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
Thus the Real seal is public only after the subbasis-generated Cauchy read has been specialized to cofinite tails, routed through the Frechet threshold discipline, handed to a displayed filter base, and read back through finite stream windows and regular rational names.
\end{theorem}
\begin{proof}
Start with the carrier $T=(S,A,F,B,W,R,E,H,C,P,N)$. The rows $S$ and $A$ expose the generated subbasis read and the tail specialization before any completion-facing object can be inspected.

Apply the NameCert obligations of \autoref{thm:cauchy-subbasis-tail-factorization-namecert-obligations}. They force the Frechet threshold row $F$ and the generated filterbase handoff $B$ to be displayed before the stream, regular-readback, and Real-seal rows are visible.

Use the sibling routes of \autoref{ch:concrete-instances-cauchy-filter-subbasis-namecert} and \autoref{ch:concrete-instances-frechetfilter-namecert}. The former exposes the subbasis-generated base, while the latter supplies the cofinite tail discipline used by eventual sequence reads.

The rows $W,R,E$ then complete the route through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The structural rows preserve the same dependency, so no quotient filter, selected limit, countable-choice schedule, host equality of generated filters, or ambient completeness object can bypass the displayed handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchySubbasisTailFactorizationUp$ Real-facing read exposes a CauchyFilterSubbasisUp row, cofinite tail-specialization row, FrechetFilterUp threshold row, FilterBaseUp handoff, StreamNameUp finite-window row, RegSeqRatUp readback, RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{cauchy-filter-subbasis, frechetfilter, filterbase, streamname, regseqrat, real: no listed sibling supplies the whole packet $T=(S,A,F,B,W,R,E,H,C,P,N)$, because each sibling contributes only a subbasis-generation row, cofinite-tail row, filterbase handoff, finite-window row, regular readback, or terminal seal while this chapter binds them into one tail-factorization carrier.}

\closureat{CauchySubbasisTailFactorizationUp}{seedStr}

\begin{closurestatus}{\CauchySubbasisTailFactorizationUp}
  \constructivestory{A $\CauchySubbasisTailFactorizationUp$ packet is generated from a CauchyFilterSubbasis row, a cofinite tail-specialization row, a FrechetFilter threshold row, a FilterBase handoff, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-subbasis-tail-factorization-carrier}, \autoref{thm:cauchy-subbasis-tail-factorization-namecert-obligations}, and \autoref{thm:cauchy-subbasis-tail-factorization-real-handoff} seed the finite route from generated Cauchy subbases through cofinite tails to Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient filters, selected limits, countable choice, host equality of generated filters, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, subbasis-generated exposure, cofinite-tail specialization, Frechet threshold exposure, FilterBase handoff, StreamName windows, RegSeqRat readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchySubbasisTailFactorizationUp.}
\end{closurestatus}
