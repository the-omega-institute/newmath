\chapter{A Concrete Naming Certificate for $\BrouwerInvarianceDomainUp$}
\label{ch:concrete-instances-brouwer-invariance-domain-namecert}
\origin{human}

Brouwer's invariance of domain and its constructive topological descendants treat an injective Euclidean map as carrying local open information, but the $\BrouwerInvarianceDomainUp$ packet records only the finite NameCert rows visible before such a handoff reaches a Real or topology consumer. It sits beside the circle boundary carrier of \autoref{ch:concrete-instances-s1-namecert}, the deformation rows of \autoref{ch:concrete-instances-homotopy-namecert}, and the L10 Real finite-name route through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names finite local chart, boundary, embedding, and Real-seal rows; it does not claim classical full open-cover compactness, host topological equality, selected inverse images, quotient embeddings, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Brouwer invariance-domain carrier]
\label{def:brouwer-invariance-domain-carrier}
A $\BrouwerInvarianceDomainUp$ carrier is a finite $\BHist$ packet
$$
I=(U,E,S,Hm,W,D,R,A,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the local Euclidean-domain chart row, read as a finite neighborhood and coordinate-window surface rather than as a host open subset.
\item $E$ is the displayed embedding row, carrying the injective graph and local-image request consumed by downstream topology readers.
\item $S$ is the $\SOneUp$ boundary row used when the local chart is probed through a finite circle or sphere boundary.
\item $Hm$ is the $\HomotopyUp$ deformation row connecting boundary probes, local chart windows, and embedding data without passing to a quotient homotopy class.
\item $W$ is the $\StreamNameUp$ finite-window schedule attached to the Real-coordinate reads.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for coordinate and radius comparisons.
\item $R$ is the $\RegSeqRatUp$ readback row for the Real-coordinate windows.
\item $A$ is the terminal $\RealUp$ seal row reached only after $W,D,R$ have been displayed.
\item $T$ records componentwise $\hsame$ transport for the local chart, embedding, boundary, homotopy, window, tolerance, readback, and seal rows.
\item $C$ records displayed $\Cont$ replay from a local embedding request through the same finite route.
\item $P$ is the $\Pkg$ provenance over $\BrouwerInvarianceDomainUp$, $\SOneUp$, $\HomotopyUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BrouwerInvarianceDomainUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these twelve displayed rows. It has no coordinate for host topological equality, global openness, open-cover compactness, selected inverse branches, quotient embeddings, countable choice, or ambient Real completeness.
\end{definition}

\begin{theorem}[Brouwer invariance-domain NameCert obligations]
\label{thm:brouwer-invariance-domain-namecert-obligations}
For every accepted $\BrouwerInvarianceDomainUp$ carrier $I=(U,E,S,Hm,W,D,R,A,T,C,P,N)$, the local $\NameCert_{\BrouwerInvarianceDomainUp}$ surface consists exactly of carrier admission for the displayed local chart, embedding graph, circle-boundary row, homotopy row, finite StreamName schedule, DyadicRatCore tolerance ledger, RegSeqRat readback, terminal Real seal, componentwise transport, continuation replay, package provenance, and local naming. Every consumer that reads an invariance-domain handoff must first see the finite route
$$
\SOneUp,\ \HomotopyUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp .
$$
No obligation is stored as a host open set, a selected inverse image, a quotient embedding, a countable-choice schedule, or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:brouwer-invariance-domain-carrier}. The projection exposes exactly the local chart row, embedding row, boundary row, homotopy row, analytic finite-name rows, and structural rows.

The boundary and deformation rows are read through the existing sibling packets. The row $S$ supplies only the finite $\SOneUp$ boundary surface of \autoref{ch:concrete-instances-s1-namecert}, while $Hm$ supplies only the displayed deformation ledger of \autoref{ch:concrete-instances-homotopy-namecert}.

The Real-coordinate part is downstream. The rows $W,D,R,A$ force every coordinate or radius read through the L10 Real finite-name route before a Real-facing conclusion is sealed.

Finally, $T,C,P,N$ transport, replay, source, and name that same route. Since the carrier contains no host topology, selected inverse, quotient embedding, choice schedule, open-cover compactness, or ambient completeness row, the listed obligations exhaust the certificate surface.
\end{proof}

\begin{theorem}[Brouwer invariance-domain Euclidean open-embedding handoff]
\label{thm:brouwer-invariance-domain-euclidean-open-embedding-handoff}
Every topology, manifold, Real, or Completion consumer of an accepted $\BrouwerInvarianceDomainUp$ carrier must read the Euclidean open-embedding request as a finite packet:
$$
U,E,S,Hm,W,D,R,A \longmapsto T,C,P,N .
$$
The handoff preserves the displayed local chart and embedding graph, reads boundary control through \autoref{ch:concrete-instances-s1-namecert}, reads deformation control through \autoref{ch:concrete-instances-homotopy-namecert}, and reads coordinate precision through the L10 Real finite-name route. It does not assert classical full open-cover compactness, host topological equality, selected inverse branches, quotient stream equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:brouwer-invariance-domain-namecert-obligations}. They require a consumer to inspect the displayed chart, embedding, boundary, homotopy, finite-name, transport, replay, provenance, and naming rows before using the packet.

The Euclidean chart and embedding rows remain finite carrier data. They record the local window and graph request, not a host open subset or a completed topological equality.

Boundary and deformation information is then routed through $\SOneUp$ and $\HomotopyUp$. These sibling chapters supply the visible circle and homotopy rows used by local topological arguments while refusing quotient-circle, host path-space, and selected deformation objects.

The coordinate handoff closes through $W,D,R,A$. Thus the consumer reaches $\RealUp$ only after StreamName windows, DyadicRatCore tolerance rows, and RegSeqRat readback have been displayed, leaving no route by which compactness, choice, quotient equality, or ambient completeness can enter.
\end{proof}

\falsifiablePrediction{Every accepted $\BrouwerInvarianceDomainUp$ open-embedding read displays one local chart row, one embedding row, one $\SOneUp$ boundary row, one $\HomotopyUp$ deformation row, one $\StreamNameUp$ schedule, one $\DyadicRatCoreUp$ tolerance ledger, one $\RegSeqRatUp$ readback, one terminal $\RealUp$ seal row, and the structural rows $T,C,P,N$.}

\independenceWitness{s1, homotopy, streamname, dyadicratcore, regseqrat, real: $\BrouwerInvarianceDomainUp$ is not bijective to any listed sibling because it jointly carries local chart data, embedding graph data, boundary rows, deformation rows, finite Real-coordinate schedules, tolerance ledgers, readback, terminal sealing, transport, replay, provenance, and local naming.}

\closureat{BrouwerInvarianceDomainUp}{seedStr}

\begin{closurestatus}{\BrouwerInvarianceDomainUp}
  \constructivestory{A $\BrouwerInvarianceDomainUp$ packet is generated from a displayed local Euclidean chart, an embedding graph row, a circle-boundary row, a homotopy deformation row, finite StreamName coordinate windows, a DyadicRatCore tolerance ledger, RegSeqRat readback, a terminal Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:brouwer-invariance-domain-carrier}, \autoref{thm:brouwer-invariance-domain-namecert-obligations}, and \autoref{thm:brouwer-invariance-domain-euclidean-open-embedding-handoff} seed the finite invariance-domain handoff over local chart, embedding, boundary, homotopy, StreamName, DyadicRatCore, RegSeqRat, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close classical full open-cover compactness, host topological equality, selected inverse branches, quotient embeddings, countable choice, ambient $\RealUp$ completeness, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, local chart exposure, embedding graph routing, boundary and homotopy handoff, Real finite-name ordering, transport, replay, provenance, and local naming for BEDC.Derived.BrouwerInvarianceDomainUp.}
\end{closurestatus}
