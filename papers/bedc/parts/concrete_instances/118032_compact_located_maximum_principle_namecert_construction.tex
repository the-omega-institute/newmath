\chapter{A Concrete Naming Certificate for $\CompactLocatedMaximumPrincipleUp$}
\label{ch:concrete-instances-compact-located-maximum-principle-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\CompactLocatedMaximumPrincipleUp$ packet names the finite-net route by which a located real-valued continuous map on a compact metric source exposes a maximum witness as BEDC certificate data. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the finite epsilon-net schedule of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the located real continuity modulus of \autoref{ch:concrete-instances-located-real-continuity-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite witness ledger for one displayed maximum read; it is not open-cover compactness, Heine--Borel compactness for arbitrary subsets, choice, quotient completion, or ambient $\RealUp$ completeness.

\begin{definition}[Compact located maximum principle carrier]
\label{def:compact-located-maximum-principle-carrier}
A $\CompactLocatedMaximumPrincipleUp$ carrier is a finite $\BHist$ packet
$$
M=(K,E,C,R,W,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the compact metric packet only through its displayed metric, total-bounded, and finite-net consumer coordinates.
\item $E$ is the $\FiniteEpsilonNetUp$ schedule row, containing the finite centers, tolerance request, and coverage ledger consumed by the maximum search.
\item $C$ is the $\LocatedRealContinuityUp$ modulus row, read over the same compact metric source and the displayed finite-net tolerance.
\item $R$ is the $\RealUp$ seal row for all located real comparisons reached by the finite schedule.
\item $W$ is the maximum-witness ledger, recording the selected displayed center, its located real value seal, and the comparison rows showing that every inspected finite-net center is bounded by that value within the consumed modulus budget.
\item $H$ records componentwise $\hsame$ transport for the compact metric packet, finite epsilon-net schedule, continuity modulus, real comparison seals, witness ledger, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from a maximum request through $K,E,C,R,W,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactLocatedMaximumPrincipleUp$, $\CompactMetricUp$, $\FiniteEpsilonNetUp$, $\LocatedRealContinuityUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactLocatedMaximumPrincipleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,E,C,R,W,H,T,P,N$. It has no coordinate for open-cover compactness, Heine--Borel compactness for arbitrary subsets, choice, quotient completion, ambient $\RealUp$ completeness, a host-level supremum, or an unrestricted global maximum theorem.
\end{definition}

\begin{theorem}[Compact located maximum principle NameCert obligations]
\label{thm:compact-located-maximum-principle-namecert-obligations}
For every accepted $\CompactLocatedMaximumPrincipleUp$ carrier
$$
M=(K,E,C,R,W,H,T,P,N),
$$
the local $\NameCert_{\CompactLocatedMaximumPrincipleUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the compact metric packet, finite epsilon-net schedule, located real continuity modulus, real comparison seals, maximum witness ledger, transport, replay, provenance, and local naming;
\item classifier transport, requiring $\hsame$ to move $K,E,C,R,W,T,P,N$ componentwise without replacing any row by host compactness or host real equality;
\item finite-net coverage, requiring every maximum read to consume the displayed $\FiniteEpsilonNetUp$ centers and coverage ledger before inspecting the witness row;
\item located-continuity modulus consumption, requiring the modulus row $C$ to translate the finite-net tolerance into the comparison budget used by $W$;
\item maximum-witness ledger nonescape, requiring $W$ to record only the displayed center, its real seal, and the finite comparison rows certified through $R$, with no host supremum, choice-selected maximizer, quotient completion, open-cover compactness, Heine--Borel compactness for arbitrary subsets, or ambient $\RealUp$ completeness.
\end{enumerate}
No compact located maximum obligation is stored outside these displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-located-maximum-principle-carrier}. The compact metric packet $K$, finite epsilon-net schedule $E$, located real continuity modulus $C$, real seal row $R$, and witness ledger $W$ are explicit coordinates of the accepted packet.

Carrier admission and classifier transport are therefore finite-row projections. The transport row $H$ moves the displayed coordinates componentwise, and the replay row $T$ replays the same route; no host compactness theorem or host real equality appears as an alternate classifier coordinate.

Finite-net coverage is the projection to $E$. A maximum request can inspect the witness ledger only after the displayed centers, tolerance, and coverage ledger have been read, so the route is the finite schedule of \autoref{ch:concrete-instances-finite-epsilon-net-namecert} rather than an open-cover argument.

Located-continuity modulus consumption is the projection to $C$ together with the real seal row $R$. The modulus row supplies the comparison budget used by the finite search, and the real seals record the located comparisons exposed to the witness ledger.

Finally, nonescape follows from row exhaustion. The carrier contains no coordinate for a host supremum, choice-selected maximizer, quotient completion, open-cover compactness, Heine--Borel compactness for arbitrary subsets, or ambient $\RealUp$ completeness, so the local $\NameCert$ can certify only the displayed finite witness ledger.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\CompactLocatedMaximumPrincipleUp$ maximum read while omitting the $\CompactMetricUp$ packet, $\FiniteEpsilonNetUp$ schedule, $\LocatedRealContinuityUp$ modulus, $\RealUp$ comparison seal, maximum witness ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, or local $\NameCert$ row refutes the carrier of \autoref{def:compact-located-maximum-principle-carrier}.}

\independenceWitness{compactmetric, finite-epsilon-net, located-real-continuity, real: no listed sibling is bijective to $\CompactLocatedMaximumPrincipleUp$ because this packet alone binds a compact metric source, a finite epsilon-net schedule, a located real continuity modulus, real comparison seals, a maximum witness ledger, transport, replay, provenance, and local naming as one finite maximum-principle certificate.}

\closureat{CompactLocatedMaximumPrincipleUp}{seedStr}

\begin{closurestatus}{\CompactLocatedMaximumPrincipleUp}
  \constructivestory{A $\CompactLocatedMaximumPrincipleUp$ packet is generated from compact metric rows, finite epsilon-net schedule rows, located real continuity modulus rows, RealUp comparison seals, a maximum witness ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-located-maximum-principle-carrier} and \autoref{thm:compact-located-maximum-principle-namecert-obligations} seed the finite-net located maximum witness route for compact metric real-valued consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, Heine--Borel compactness for arbitrary subsets, choice, quotient completion, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, finite-net coverage, located-continuity modulus consumption, real comparison sealing, maximum-witness ledger nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactLocatedMaximumPrincipleUp.}
\end{closurestatus}
