\chapter{A Concrete Naming Certificate for $\LocatedZeroCrossingCertificateUp$}
\label{ch:concrete-instances-located-zero-crossing-certificate-namecert}
\origin{human}

Following Bishop and Bridges on constructive analysis, the $\LocatedZeroCrossingCertificateUp$ packet records a finite bracketed zero-crossing witness rather than a classical intermediate-value selection. It sits after the located real route of \autoref{ch:concrete-instances-locatedreal-namecert}, the dyadic rational core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the bisection handoff of \autoref{ch:concrete-instances-located-interval-bisection-namecert}. The packet names the finite data needed by later root-isolation and real-completion boundary chapters without importing a selected root, trichotomy, quotient real equality, countable choice, or ambient completeness of $\RealUp$.

\begin{definition}[Located zero-crossing certificate carrier]
\label{def:located-zero-crossing-certificate-carrier}
A $\LocatedZeroCrossingCertificateUp$ carrier is a finite $\BHist$ packet
$$
Z=(I,F,D,R,L,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the located interval row containing rational endpoints and located endpoint apartness data;
\item $F$ is the function-read row whose endpoint signs and finite value windows are inspected only through displayed histories;
\item $D$ is the $\DyadicRatCoreUp$ refinement row carrying dyadic mesh bounds for the bracket;
\item $R$ is the $\RegSeqRatUp$ readback row for endpoint and midpoint value windows;
\item $L$ is the $\LocatedRealUp$ and $\RealUp$ seal ledger that records the two located endpoint reads and the terminal real-name boundary;
\item $B$ is the $\LocatedIntervalBisectionUp$ handoff row, containing the finite bracket projection and bisection window chosen for the next read;
\item $H$ records componentwise $\hsame$ transport for interval, function-read, dyadic, regular-rational, located-real, bisection, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from interval admission through endpoint reads, dyadic refinement, regular rational readback, bisection handoff, located-real readback, and terminal Real seal;
\item $P$ is the $\Pkg$ provenance over $\LocatedZeroCrossingCertificateUp$, $\LocatedRealUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedIntervalBisectionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\LocatedZeroCrossingCertificateUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected root, sign trichotomy away from the displayed endpoint rows, quotient equality of real names, countable choice, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Located zero-crossing NameCert obligations]
\label{thm:located-zero-crossing-certificate-namecert-obligations}
For the carrier of \autoref{def:located-zero-crossing-certificate-carrier}, the local $\NameCert_{\LocatedZeroCrossingCertificateUp}$ obligation surface consists exactly of carrier admission for $Z$, located interval admission through $I$, endpoint sign and value-window exposure through $F$, dyadic bracket refinement through $D$, regular rational readback through $R$, located-real and terminal Real sealing through $L$, bisection handoff through $B$, and structural transport, replay, provenance, and local naming through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted packet $Z$. The interval row $I$ and function-read row $F$ expose the finite bracket and endpoint signs before any downstream consumer can read a crossing.

The dyadic row $D$ and regular rational row $R$ refine the same bracket. The located-real and Real seal ledger $L$ records the endpoint and terminal-name reads, while the bisection row $B$ gives the next finite bracket projection.

Transport, replay, provenance, and naming are exhausted by $H,C,P,N$. Since no carrier coordinate stores a selected root, sign trichotomy outside the displayed endpoint rows, quotient real equality, countable choice, or real completeness, the listed rows are exactly the local obligations.
\end{proof}

\begin{theorem}[Located zero-crossing bracket handoff]
\label{thm:located-zero-crossing-certificate-bracket-handoff}
Every bracket handoff exported by an accepted $\LocatedZeroCrossingCertificateUp$ carrier factors through
$$
I,F,D,R,L,B,H,C,P,N .
$$
The handoff may pass a finite located interval and dyadic bisection window to later root-isolation consumers, but it does not select a root or assert an intermediate-value theorem in the ambient $\RealUp$ carrier.
\end{theorem}
\begin{proof}
Start with the bracket rows $I$ and $F$. They display the located interval and the endpoint value windows, so a crossing read begins from finite endpoint data rather than from a global real function.

The dyadic and regular-rational rows $D,R$ refine the bracket into a readback window. The located-real and Real ledger $L$ then seals the endpoint reads, and the bisection row $B$ records the next finite interval projection.

Finally $H,C,P,N$ preserve and name the same route. The resulting handoff is therefore a finite bracket packet for later consumers; it contains no selected root, quotient equality of real names, choice sequence, trichotomy principle, or ambient completeness argument.
\end{proof}

\begin{theorem}[Located zero-crossing Real-seal boundary]
\label{thm:located-zero-crossing-certificate-real-seal-boundary}
Every $\RealUp$-facing read of an accepted $\LocatedZeroCrossingCertificateUp$ packet must pass through the displayed located interval, dyadic bracket, regular rational readback, located-real ledger, bisection handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local $\NameCert$ rows. In particular, the terminal Real seal cannot be used to infer quotient real equality, a selected zero, countable choice, trichotomy, or completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Read the terminal ledger $L$. It is available only after the interval row $I$, function row $F$, dyadic row $D$, and regular rational readback $R$ have displayed the finite bracket data.

The bisection row $B$ keeps the downstream route finite by exporting a bracket projection rather than a chosen real. Componentwise transport through $H$ and displayed replay through $C$ preserve the same finite route under classifier movement.

The provenance and local naming rows $P,N$ expose exactly this packet. Since the carrier has no coordinate for quotient real equality, selected roots, countable choice, trichotomy, or ambient real completeness, no Real-facing consumer can obtain those principles from the seal.
\end{proof}

\closureat{LocatedZeroCrossingCertificateUp}{seedStr}

\begin{closurestatus}{\LocatedZeroCrossingCertificateUp}
  \constructivestory{A $\LocatedZeroCrossingCertificateUp$ packet is generated from a located interval row, finite endpoint value reads, DyadicRatCore bracket refinement, RegSeqRat readback, LocatedReal and RealUp seal rows, LocatedIntervalBisection handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-zero-crossing-certificate-carrier}, \autoref{thm:located-zero-crossing-certificate-namecert-obligations}, \autoref{thm:located-zero-crossing-certificate-bracket-handoff}, and \autoref{thm:located-zero-crossing-certificate-real-seal-boundary} seed the finite zero-crossing bracket certificate over located real, dyadic, regular rational, bisection, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a selected root, a classical intermediate-value theorem, sign trichotomy outside displayed endpoint rows, quotient equality of real names, countable choice, Lean verification, bridge checking, or completeness of $\RealUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint sign exposure, dyadic bracket refinement, regular rational readback, bisection handoff, transport, replay, provenance, local naming, and Real-seal non-escape for BEDC.Derived.LocatedZeroCrossingCertificateUp.}
\end{closurestatus}
