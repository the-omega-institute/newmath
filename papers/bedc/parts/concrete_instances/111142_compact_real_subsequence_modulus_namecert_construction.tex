\chapter{A Concrete Naming Certificate for $\CompactRealSubsequenceModulusUp$}
\label{ch:concrete-instances-compact-real-subsequence-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\CompactRealSubsequenceModulusUp$ packet records the explicit modulus route by which a bounded compact-real sequence yields inspected subsequence windows before any downstream consumer reads a real cluster seal. It sits over the constructive subsequence packet of \autoref{ch:concrete-instances-constructive-bolzano-weierstrass-namecert}, the sibling Bolzano--Weierstrass route of \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite subsequence-modulus carrier for compact-real consumers; it is not an open-cover compactness theorem, not an arbitrary-choice selector, not quotient real equality, and not ambient $\RealUp$ completeness.

\begin{definition}[Compact real subsequence modulus carrier]
\label{def:compact-real-subsequence-modulus-carrier}
A $\CompactRealSubsequenceModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(K,S,B,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact real source row, exposing the bounded $\RealUp$ sequence request and the compact-metric inspection data visible to the packet.
\item $S$ is the $\StreamNameUp$ window row, listing exactly the finite source windows inspected by the modulus.
\item $B$ is the $\ConstructiveBolzanoWeierstrassUp$ and $\BolzanoWeierstrassUp$ route row, carrying the interval-refinement and subsequence scheduling surface without adding a host compactness principle.
\item $W$ is the subsequence-modulus row, recording selected indices, monotonicity, cofinal scheduling, and the finite window assignment for each requested precision.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording radius requests, split budgets, comparison windows, and common-scale data used by the selected windows.
\item $R$ is the $\RegSeqRatUp$ readback row for the same selected windows and tolerance ledger.
\item $E$ is the terminal $\RealUp$ cluster seal, visible only after $K,S,B,W,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, stream windows, route, modulus, tolerance, readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-real subsequence request through source inspection, stream windows, route selection, modulus scheduling, dyadic tolerance accounting, regular rational readback, and the real seal.
\item $P$ is the $\Pkg$ provenance over $\CompactRealSubsequenceModulusUp$, $\ConstructiveBolzanoWeierstrassUp$, $\BolzanoWeierstrassUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactRealSubsequenceModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,S,B,W,D,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, arbitrary choice, quotient real equality, selected least cluster points, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact real subsequence modulus NameCert obligations]
\label{thm:compact-real-subsequence-modulus-namecert-obligations}
For every accepted $\CompactRealSubsequenceModulusUp$ carrier
$$
M=(K,S,B,W,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactRealSubsequenceModulusUp}$ surface consists exactly of compact real source exposure through $K$, finite stream-window exposure through $S$, constructive Bolzano--Weierstrass route exposure through $B$, subsequence-modulus scheduling through $W$, dyadic tolerance accounting through $D$, regular rational readback through $R$, terminal real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-real-subsequence-modulus-carrier}. Its analytic rows are the compact real source $K$, stream windows $S$, constructive route $B$, subsequence modulus $W$, tolerance ledger $D$, regular readback $R$, and real seal $E$.

The modulus obligation is finite because $W$ records selected indices, monotonicity, cofinal scheduling, and selected windows as packet data. The route row $B$ imports the displayed $\ConstructiveBolzanoWeierstrassUp$ and $\BolzanoWeierstrassUp$ subsequence construction rather than an ambient compactness theorem.

The readback obligation then passes through $D$ and $R$. The same selected windows receive the dyadic tolerance budget before they can be read as a $\RegSeqRatUp$ sequence.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name only those rows. Since the carrier contains no open-cover, arbitrary-choice, quotient-equality, selected-least-cluster, or ambient-completeness coordinate, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Constructive Bolzano--Weierstrass modulus route]
\label{thm:compact-real-subsequence-modulus-constructive-bolzano-weierstrass-route}
Every $\CompactRealSubsequenceModulusUp$ consumer that reads a compact real subsequence route from an accepted carrier must factor through
$$
\ConstructiveBolzanoWeierstrassUp,\quad \BolzanoWeierstrassUp,\quad \StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp .
$$
The factorization exposes finite source windows, a displayed subsequence-modulus row, dyadic tolerance accounting, regular rational readback, and a terminal real seal; it exports no open-cover compactness, arbitrary-choice selector, quotient real equality, or ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Apply \autoref{thm:compact-real-subsequence-modulus-namecert-obligations}. It places the constructive and sibling Bolzano--Weierstrass route row before the modulus row and before any regular rational readback.

The finite windows are exactly the row $S$, and the selected windows are exactly the row $W$. Thus any consumer that changes the source window or selected window must leave the displayed carrier.

The dyadic tolerance ledger $D$ is read before $R$, and $R$ is read before the terminal real seal $E$. This orders the packet through the stated route and prevents the excluded host principles from entering the subsequence-modulus handoff.
\end{proof}

\closureat{CompactRealSubsequenceModulusUp}{seedStr}

\begin{closurestatus}{\CompactRealSubsequenceModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-real-subsequence-modulus-carrier}, \autoref{thm:compact-real-subsequence-modulus-namecert-obligations}, and \autoref{thm:compact-real-subsequence-modulus-constructive-bolzano-weierstrass-route} seed the explicit compact-real subsequence-modulus route through finite windows, dyadic tolerances, regular readback, and real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, arbitrary choice, quotient real equality, selected least cluster points, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, stream-window exactness, constructive Bolzano--Weierstrass route exposure, subsequence-modulus scheduling, dyadic tolerance accounting, RegSeqRat readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactRealSubsequenceModulusUp.}
  \constructivestory{A $\CompactRealSubsequenceModulusUp$ packet is generated from compact real source data, finite StreamName windows, ConstructiveBolzanoWeierstrass and BolzanoWeierstrass route data, subsequence-modulus scheduling, DyadicRatCore tolerance accounting, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, provenance, and a local NameCert row.}
\end{closurestatus}
