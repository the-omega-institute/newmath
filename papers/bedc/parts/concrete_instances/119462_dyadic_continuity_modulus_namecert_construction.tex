\chapter{A Concrete Naming Certificate for $\DyadicContinuityModulusUp$}
\label{ch:concrete-instances-dyadic-continuity-modulus-namecert}
\origin{human}

Following Bishop and Bridges on moduli of continuity for constructive real functions, the $\DyadicContinuityModulusUp$ packet records a finite dyadic tolerance-to-window service for L10 continuity consumers. It sits after the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the map-facing service of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names a local modulus table over displayed rows; it is not a host continuity function, a quotient-real equality principle, a selected external modulus, or an ambient completeness theorem for $\RealUp$.

\begin{definition}[Dyadic continuity modulus carrier]
\label{def:dyadic-continuity-modulus-carrier}
A $\DyadicContinuityModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(E,S,R,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\DyadicRatCoreUp$ tolerance row naming the finite dyadic precisions requested by a continuity consumer.
\item $S$ is the $\StreamNameUp$ source-window row naming the finite input observations sufficient for those dyadic tolerances.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same source windows.
\item $W$ is the response-window row naming the finite output observations certified for the requested tolerances.
\item $H$ records componentwise $\hsame$ transport for $E,S,R,W$ and the structural rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
E \longmapsto S \longmapsto R \longmapsto W .
$$
\item $P$ is the $\Pkg$ provenance over $\DyadicContinuityModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ContinuousMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicContinuityModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed tolerance, source-window, readback, response-window, transport, replay, provenance, and naming rows. It has no coordinate for quotient real equality, selected external moduli, host functions, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[DyadicContinuityModulus NameCert obligations]
\label{thm:dyadic-continuity-modulus-namecert-obligations}
For an accepted $\DyadicContinuityModulusUp$ carrier $M=(E,S,R,W,H,C,P,N)$, the local $\NameCert_{\DyadicContinuityModulusUp}$ surface consists of exactly five obligations: carrier admission for the eight displayed rows; tolerance-window stability binding each $E$ row to its source window $S$; regular readback compatibility binding $S$ to $R$; response-window exactness binding $R$ to $W$ for the requested dyadic precision; and local provenance plus naming over $H,C,P,N$. These obligations export no quotient real equality, selected external modulus, host continuity function, countable-choice schedule, or ambient Real completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:dyadic-continuity-modulus-carrier}. The packet exposes exactly the tolerance, source-window, readback, response-window, transport, replay, provenance, and local naming rows.

Tolerance-window stability reads $E,S,H,C$. A transported dyadic tolerance request remains attached to the same finite source-window row, and the replay route prevents the request from being served by a hidden host modulus.

Regular readback compatibility reads $S,R$. The source window is inspected only through the displayed $\RegSeqRatUp$ readback, so the input side of the modulus remains a finite BEDC readback route.

Response-window exactness reads $R,W$ after the tolerance row. The output response window is certified only for the dyadic precision named in $E$ and the readback named in $R$.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no carrier coordinate supplies quotient real equality, selected external moduli, host functions, countable choice, or ambient completeness, the five obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[DyadicContinuityModulus unary-window induction]
\label{thm:dyadic-continuity-modulus-window-induction}
Let $M=(E,S,R,W,H,C,P,N)$ be an accepted $\DyadicContinuityModulusUp$ carrier. A finite chain of dyadic tolerance refinements read from $E$ proceeds by unary-window induction: the base window is the displayed source window $S$ with readback $R$, and each successor refinement tightens the same dyadic tolerance ledger before exposing the next response window $W$. The induction stays inside $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ContinuousMapUp$, and the structural rows; it exports no quotient real equality, selected external modulus, host function, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
For the base window, project the carrier. The source window $S$ and readback $R$ are already paired with the initial dyadic tolerance row $E$, so the first response window $W$ is read without any external continuity function.

For the successor step, apply the tolerance-window stability and regular readback clauses of \autoref{thm:dyadic-continuity-modulus-namecert-obligations}. A refined dyadic request remains attached to the transported source window and regular rational readback.

Now apply response-window exactness. The next response window is certified only after the refined tolerance and the corresponding readback have been displayed, so the refinement step is unary over the visible window row rather than a choice over an infinite family of representatives.

Induction over the finite displayed chain gives the claimed route. The structural rows $H,C,P,N$ preserve the same finite packet at every step, and the absent coordinates exclude quotient equality, selected moduli, host functions, countable choice, and ambient completeness.
\end{proof}

\closureat{DyadicContinuityModulusUp}{seedStr}

\begin{closurestatus}{\DyadicContinuityModulusUp}
  \constructivestory{A $\DyadicContinuityModulusUp$ packet is generated from a dyadic tolerance row, source-window row, regular rational readback row, response-window row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-continuity-modulus-carrier}, \autoref{thm:dyadic-continuity-modulus-namecert-obligations}, and \autoref{thm:dyadic-continuity-modulus-window-induction} seed the finite dyadic tolerance-to-window route over DyadicRatCore, StreamName, RegSeqRat, Real, ContinuousMap, hsame, Cont, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected external moduli, host continuity functions, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tolerance-window stability, regular readback compatibility, response-window exactness, provenance, and local naming for the dyadic continuity modulus packet.}
\end{closurestatus}
