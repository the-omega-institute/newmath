\chapter{A Concrete Naming Certificate for $\SeparatedCauchyCompletionLimitUp$}
\label{ch:concrete-instances-separated-cauchy-completion-limit-namecert}
\origin{human}

Following Bishop and Bridges on constructive completion, the $\SeparatedCauchyCompletionLimitUp$ packet records the finite limit-facing interface by which a regular rational Cauchy name is admitted as a separated completion limit without claiming ambient $\RealUp$ completeness. It consumes the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It also sits downstream of the quotient-free separated completion surface of \autoref{ch:concrete-instances-separated-cauchy-completion-namecert}.

\begin{definition}[Separated Cauchy completion limit carrier]
\label{def:separated-cauchy-completion-limit-carrier}
A $\SeparatedCauchyCompletionLimitUp$ carrier is a finite $\BHist$ packet
$$
L=(S,R,D,Q,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row, recording the inspected stages of the Cauchy name.
\item $R$ is the $\RegSeqRatUp$ regular rational readback attached to those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare the displayed windows.
\item $Q$ is the separated completion-limit row, recording that the named regular Cauchy data has passed the zero-distance boundary inside the displayed route.
\item $E$ is the terminal $\RealUp$ seal, available only after $S,R,D,Q$ have been exposed.
\item $H$ records componentwise $\hsame$ transport for the window, readback, tolerance, separated-limit, Real seal, provenance, and local naming rows.
\item $T$ records the displayed continuity boundary
$$
\Cont(L)=\Cont(S,R,D,Q,E,H).
$$
\item $P$ records
$$
\Pkg(L)=\Pkg(\SeparatedCauchyCompletionLimitUp,\StreamNameUp,\RegSeqRatUp,\DyadicRatCoreUp,\RealUp,\BHist,\hsame,\Cont,\NameCert).
$$
\item $N$ is the local $\NameCert_{\SeparatedCauchyCompletionLimitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,D,Q,E,H,T,P,N$. It has no coordinate for quotient stream equality, selected host limits, choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.SeparatedCauchyCompletionLimitUp}

\begin{theorem}[Separated Cauchy completion limit NameCert obligations]
\label{thm:separated-cauchy-completion-limit-namecert-obligations}
For every accepted $\SeparatedCauchyCompletionLimitUp$ carrier
$$
L=(S,R,D,Q,E,H,T,P,N),
$$
the local $\NameCert_{\SeparatedCauchyCompletionLimitUp}$ surface consists of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $L$;
\item finite observation exposure through the $\StreamNameUp$ row $S$;
\item regular rational readback through the $\RegSeqRatUp$ row $R$;
\item dyadic comparison confinement through the $\DyadicRatCoreUp$ row $D$;
\item separated completion-limit sealing through $Q$;
\item terminal $\RealUp$ readback through $E$, reachable only after $S,R,D,Q$ have been displayed;
\item componentwise transport, continuity replay, provenance, and local naming through $H,T,P,N$.
\end{enumerate}
Thus the packet names a separated completion-limit interface, not a proof that every Cauchy object in an ambient Real carrier has a selected limit.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:separated-cauchy-completion-limit-carrier}. The finite-window row $S$, regular rational readback row $R$, and dyadic tolerance row $D$ expose the only data through which a consumer may inspect the Cauchy name.

The separated-limit row $Q$ is downstream of that finite route. It records the displayed zero-distance boundary for this packet rather than a quotient class of streams or a host-selected Real element.

Finally read the terminal Real seal $E$ through $H,T,P,N$. These structural rows preserve and name the same displayed route, so the NameCert surface contains exactly the listed obligations and exports no ambient completeness principle.
\end{proof}

\begin{theorem}[Separated Cauchy completion limit uniqueness route]
\label{thm:separated-cauchy-completion-limit-uniqueness-route}
If two accepted $\SeparatedCauchyCompletionLimitUp$ packets agree componentwise on $S,R,D,Q,H,T,P,N$ by $\hsame$, then every $\RealUp$-facing consumer reads the same displayed limit route through $E$. The comparison is made through the finite route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \SeparatedCauchyCompletionLimitUp \longmapsto \RealUp
$$
and does not invoke quotient equality of Cauchy streams, trichotomy, countable choice, a selected host limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the shared rows $S,R,D,Q,H,T,P,N$. The window, readback, and dyadic tolerance rows determine the displayed Cauchy route available to both packets.

Use \autoref{thm:separated-cauchy-completion-limit-namecert-obligations}. It places the separated-limit row $Q$ after the common finite data and the terminal Real seal $E$ after $Q$, so a Real-facing consumer cannot bypass the displayed comparison boundary.

Transport by $H$ and replay by $T$ preserve the same finite route, while $P$ and $N$ name it locally. Since the carrier contains no quotient, choice, trichotomy, selected host-limit, or ambient-completeness coordinate, the uniqueness route is exhausted by the displayed seal path.
\end{proof}

\falsifiablePrediction{An accepted $\SeparatedCauchyCompletionLimitUp$ packet whose $\RealUp$ seal is read without first exposing $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ tolerance ledger, and the separated-limit row $Q$ refutes the carrier surface.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, separated-cauchy-completion: no listed sibling supplies the whole packet $L=(S,R,D,Q,E,H,T,P,N)$, because each sibling contributes only finite windows, regular rational readback, dyadic tolerances, a Real seal, or separated completion context while this chapter binds them into one limit-facing NameCert surface.}

\closureat{SeparatedCauchyCompletionLimitUp}{seedStr}

\begin{closurestatus}{\SeparatedCauchyCompletionLimitUp}
  \constructivestory{A $\SeparatedCauchyCompletionLimitUp$ packet is generated from StreamNameUp finite windows, RegSeqRatUp readback, a DyadicRatCoreUp tolerance ledger, a separated completion-limit row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:separated-cauchy-completion-limit-carrier}, \autoref{thm:separated-cauchy-completion-limit-namecert-obligations}, and \autoref{thm:separated-cauchy-completion-limit-uniqueness-route} seed the finite separated Cauchy completion-limit interface over window, readback, tolerance, separated-limit, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected host limits, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exposure, regular rational readback, dyadic tolerance confinement, separated-limit sealing, Real readback, transport, replay, provenance, and local naming for BEDC.Derived.SeparatedCauchyCompletionLimitUp.}
\end{closurestatus}
