\chapter{A Concrete Naming Certificate for $\FiniteNetEquicontinuityUp$}
\label{ch:concrete-instances-finite-net-equicontinuity-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and the Arzela--Ascoli equicontinuity route, the $\FiniteNetEquicontinuityUp$ packet records the family-level handoff by which a compact finite-net ledger supplies one displayed source-radius row for a finite family of continuous maps. It sits between the compact-uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the compact-metric finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the graph and pointwise-modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, and the downstream output interface of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The certificate names the reusable family-level finite-net route, not an open-cover compactness theorem, a host function-space topology, a selected global modulus function, or an ambient Arzela--Ascoli compactness principle.

\begin{definition}[Finite-net equicontinuity carrier]
\label{def:finite-net-equicontinuity-carrier}
A $\FiniteNetEquicontinuityUp$ carrier is a finite $\BHist$ packet
$$
E=(K,F,B,R,M,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact-metric finite-net row read from $\CompactMetricUp$, including the finite center $\ProbeBundle$ and its coverage radius ledger.
\item $F$ is the finite family ledger of $\ContinuousMapUp$ rows sharing the same source and target metric surfaces.
\item $B$ records the center-indexed pointwise bounds for every displayed family member at every center of the finite $\ProbeBundle$.
\item $R$ is the rational tolerance and source-radius ledger used to compare the center bounds and the compact finite-net radius.
\item $M$ is the family-level finite-net modulus row produced by folding $K$, $F$, $B$, and $R$.
\item $U$ is the downstream $\UniformModulusUp$ handoff row, exposing the resulting source-radius row as a reusable modulus boundary.
\item $H$ records componentwise $\hsame$ transports for the compact-net, family, bound, rational, modulus, output, route, provenance, and local naming rows.
\item $C$ records the displayed $\Cont$ routes by which compact-uniform-continuity, continuous-map, compact-metric, equicontinuity, and uniform-modulus consumers read the same packet.
\item $P$ is the $\Pkg$ provenance over $\FiniteNetEquicontinuityUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\ProbeBundle$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteNetEquicontinuityUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,F,B,R,M,U,H,C,P,N$. It has no coordinate for host function equality, selected subcovers, selected global moduli, open-cover compactness, or ambient function-space compactness.
\end{definition}
\leandef{BEDC.Derived.FiniteNetEquicontinuityUp.FiniteNetEquicontinuityUp}
\leanchecked{BEDC.Derived.FiniteNetEquicontinuityUp.FiniteNetEquicontinuityTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Finite-net equicontinuity obligation surface]
\label{thm:finite-net-equicontinuity-obligation-surface}
For an accepted $\FiniteNetEquicontinuityUp$ carrier $E=(K,F,B,R,M,U,H,C,P,N)$, the local $\NameCert_{\FiniteNetEquicontinuityUp}$ surface consists exactly of carrier admission for the compact-metric finite-net row, finite family ledger, center-indexed bound rows, rational tolerance ledger, family-level modulus row, uniform-modulus handoff row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every downstream read must preserve the same finite $\ProbeBundle$ centers, the same family ledger, the same rational radius comparison, and the same local NameCert row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-net-equicontinuity-carrier}. The finite-net data are exactly $K$, the family data are exactly $F$, and the center-indexed estimates are exactly $B$.

The rational ledger $R$ compares the compact-net radius with the source-radius rows needed by the displayed family estimates. Folding those rows gives $M$, and the handoff row $U$ exposes that folded radius only as a $\UniformModulusUp$ boundary.

Transport, replay, provenance, and local naming are the rows $H,C,P,N$. Since no further coordinate exists, the certificate surface has no path to host function equality, selected subcovers, selected global moduli, open-cover compactness, or ambient function-space compactness.
\end{proof}

\begin{theorem}[Finite-net equicontinuity uniform-modulus handoff]
\label{thm:finite-net-equicontinuity-uniform-modulus-handoff}
Every $\UniformModulusUp$ read exported from an accepted $\FiniteNetEquicontinuityUp$ packet factors through the finite route
$$
K,F,B,R,M,U,H,C,P,N.
$$
The route first consumes compact finite-net centers from $\CompactMetricUp$, then reads the finite $\ContinuousMapUp$ family rows and center-indexed bounds, then folds the rational radius ledger into the family-level modulus row $M$, and only then exposes the output row $U$. The handoff does not export open-cover compactness, an Arzela--Ascoli compactness theorem, host function extensionality, selected global moduli, or a function-space topology.
\end{theorem}
\begin{proof}
Begin with the finite-net row $K$. It supplies the displayed $\ProbeBundle$ centers and coverage radius through the compact-metric certificate, so the route starts from finite carrier data rather than from an open cover.

Next read the family ledger $F$ and the center-indexed rows $B$. Each family member is consumed through its $\ContinuousMapUp$ row, and the estimates are attached only to centers already named by $K$.

Finally use the rational ledger $R$ to form $M$ and expose $U$. The $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows preserve the same finite packet during transport and replay, so the exported uniform-modulus row is exactly the finite-net family handoff and not an ambient compactness or function-space assertion.
\end{proof}

\begin{theorem}[Compact finite-net coverage row]
\label{thm:finite-net-equicontinuity-compact-net-coverage}
Let $E=(K,F,B,R,M,U,H,C,P,N)$ be an accepted $\FiniteNetEquicontinuityUp$ carrier. Every accepted read of $E$ exposes the compact finite-net row $K$ before any source-family radius row is read from $F$, $B$, or $R$. Thus the inspected $\ProbeBundle$ centers and their compact-metric coverage ledger are visible before the family estimates can be folded toward $M$ or exported through $U$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-net-equicontinuity-carrier}. The row $K$ is the only coordinate carrying compact-metric finite-net centers and the associated coverage ledger, while $F,B,R$ are downstream rows for family membership, center-indexed estimates, and rational radius comparison.

Apply \autoref{thm:finite-net-equicontinuity-obligation-surface}. It requires every downstream read to preserve the same finite $\ProbeBundle$ centers and the same rational radius comparison. Apply \autoref{thm:finite-net-equicontinuity-uniform-modulus-handoff}. The handoff route begins at $K$ and reaches $U$ only after the family rows and rational ledger have been consumed.

Since the carrier has no coordinate for open-cover compactness, selected moduli, host function-space compactness, or ambient $\RealUp$ completeness, no consumer can bypass $K$ by appealing to one of those external surfaces.
\end{proof}

\begin{theorem}[Finite family row stability]
\label{thm:finite-net-equicontinuity-family-row-stability}
Let $E=(K,F,B,R,M,U,H,C,P,N)$ be an accepted $\FiniteNetEquicontinuityUp$ carrier. The finite family row $F$ and bound ledger $B$ are transported componentwise only by the $\hsame$ row $H$, and every replay of those rows to compact-metric, continuous-map, equicontinuity, or uniform-modulus consumers factors through the displayed $\Cont$ routes in $C$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-net-equicontinuity-carrier}. The family ledger is $F$, the center-indexed bound ledger is $B$, the transport coordinate is $H$, and the displayed replay coordinate is $C$.

Use \autoref{thm:finite-net-equicontinuity-obligation-surface}. Its obligation surface names componentwise $\hsame$ transport and displayed $\Cont$ replay as part of the local $\NameCert_{\FiniteNetEquicontinuityUp}$ surface, with no additional transport coordinate.

Use \autoref{thm:finite-net-equicontinuity-uniform-modulus-handoff}. The route to $U$ reads $F$ and $B$ after $K$ and before $R,M,U$, so the same family and bound ledgers are preserved during the handoff. The carrier excludes open-cover compactness, selected moduli, host function-space compactness, and ambient $\RealUp$ completeness, so those surfaces cannot supply an alternate replay path.
\end{proof}

\begin{theorem}[Rational radius folding order]
\label{thm:finite-net-equicontinuity-rational-radius-folding}
Let $E=(K,F,B,R,M,U,H,C,P,N)$ be an accepted $\FiniteNetEquicontinuityUp$ carrier. The rational radius data $R$ fold into the uniform-modulus handoff row $U$ only through the family-level modulus row $M$, and only after the compact-net row $K$, finite family row $F$, and bound ledger $B$ have been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-net-equicontinuity-carrier}. The row $R$ is the rational tolerance and source-radius ledger, $M$ is the family-level finite-net modulus row, and $U$ is the downstream $\UniformModulusUp$ handoff row.

Apply \autoref{thm:finite-net-equicontinuity-obligation-surface}. It places carrier admission, compact finite-net coverage, finite family data, bound rows, rational comparison, modulus formation, and uniform-modulus output in the same local NameCert surface.

Apply \autoref{thm:finite-net-equicontinuity-uniform-modulus-handoff}. The exported route is exactly $K,F,B,R,M,U,H,C,P,N$, so $R$ cannot reach $U$ before $K,F,B$ are displayed and cannot skip the fold row $M$. Because the packet has no coordinate for open-cover compactness, selected moduli, host function-space compactness, or ambient $\RealUp$ completeness, no external compactness or function-space argument can replace this finite fold.
\end{proof}

\begin{closurestatus}{\FiniteNetEquicontinuityUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-net-equicontinuity-carrier}, \autoref{thm:finite-net-equicontinuity-obligation-surface}, and \autoref{thm:finite-net-equicontinuity-uniform-modulus-handoff} seed the family-level finite-net equicontinuity route over compact-metric centers, continuous-map family rows, rational radius ledgers, uniform-modulus handoff, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, selected global moduli, open-cover compactness, Arzela--Ascoli compactness, host function extensionality, function-space topology, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net coverage, finite family row stability, rational radius folding, uniform-modulus handoff, and non-escape for BEDC.Derived.FiniteNetEquicontinuityUp.}
  \constructivestory{A $\FiniteNetEquicontinuityUp$ packet is generated from CompactMetricUp finite-net centers, ContinuousMapUp family rows, center-indexed estimates, rational tolerance histories, a family-level finite-net modulus row, a UniformModulusUp output row, componentwise hsame transport, Cont replay, provenance, and local naming.}
\end{closurestatus}
