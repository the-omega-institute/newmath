\chapter{A Concrete Naming Certificate for $\CompactUniformModulusEnvelopeUp$}
\label{ch:concrete-instances-compact-uniform-modulus-envelope-namecert}
\origin{human}

Following Bishop and Bridges on compact metric uniform continuity, the $\CompactUniformModulusEnvelopeUp$ packet records the finite envelope by which compact finite nets, local continuity moduli, rational lower-bound folds, and a tolerance-only uniform modulus row are read as one NameCert carrier. It realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} beside the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map source of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the metric distance source of \autoref{ch:concrete-instances-metric-namecert}, and the rational fold source of \autoref{ch:concrete-instances-rat-namecert}. The packet exports a finite modulus envelope only; it is not an open-cover compactness theorem, not a selected pointwise-radius function, not host function equality, and not ambient Real completeness.

\begin{definition}[Compact uniform modulus envelope carrier]
\label{def:compact-uniform-modulus-envelope-carrier}
A $\CompactUniformModulusEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
M=(K,L,F,B,D,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, recording the compact source, displayed net probes, and coverage witnesses.
\item $L$ is the local modulus row, read from the $\ContinuousMapUp$ carrier at each displayed finite-net center.
\item $F$ is the rational lower-bound fold row, using $\RatUp$ comparison data to fold the finitely many local radius ledgers into one positive precision history.
\item $B$ is the dyadic tolerance row, carrying the $\DyadicRatCoreUp$ comparisons used to align finite-net coverage, local radii, and target tolerance.
\item $D$ is the $\MetricUp$ distance-window row, recording the source and target triangle reads needed by the finite-net argument.
\item $U$ is the $\UniformModulusUp$ handoff row reached only after $K,L,F,B,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for $K,L,F,B,D,U,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a tolerance request through the compact finite-net row, local modulus row, rational lower-bound fold, dyadic tolerance row, metric window row, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusEnvelopeUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\MetricUp$, $\RatUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactUniformModulusEnvelopeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,L,F,B,D,U,H,C,P,N$. It has no coordinate for open-cover compactness, selected subcovers, selected pointwise radii, host function equality, quotient real representatives, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform modulus envelope NameCert obligations]
\label{thm:compact-uniform-modulus-envelope-namecert-obligations}
For every accepted $\CompactUniformModulusEnvelopeUp$ carrier $M=(K,L,F,B,D,U,H,C,P,N)$, the local $\NameCert_{\CompactUniformModulusEnvelopeUp}$ surface consists exactly of compact finite-net exposure, local modulus exposure, rational lower-bound fold exactness, dyadic tolerance compatibility, metric distance-window replay, $\UniformModulusUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Any uniform-modulus consumer must read those finite rows before the tolerance-only modulus row is exposed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-envelope-carrier}. The compact, local-modulus, rational-fold, dyadic, metric, and uniform-modulus rows are the analytic coordinates, while $H,C,P,N$ only transport, replay, source, and name that route.

The local rows cannot produce a uniform modulus by themselves. They first pass through the finite rational lower-bound fold $F$, and the dyadic row $B$ aligns the selected lower bound with the tolerance history read by the metric window $D$.

The handoff row $U$ is downstream of this envelope. Since the carrier has no open-cover theorem, selected subcover, selected pointwise-radius function, host equality row, quotient representative, or ambient completeness theorem, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Compact uniform modulus envelope vision handoff]
\label{thm:compact-uniform-modulus-envelope-vision-handoff}
Every consumer of the compact-uniform-continuity vision that cites an accepted $\CompactUniformModulusEnvelopeUp$ packet must pass through the finite route
$$
K,L,F,B,D \longmapsto U .
$$
Thus the vision-facing read sees the compact finite net, local continuity moduli, rational lower-bound fold, dyadic tolerance row, and metric distance window before it reaches $\UniformModulusUp$. The handoff exports no open-cover compactness theorem, selected subcover, selected pointwise-radius function, host function equality, quotient real representative, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from a consumer asking for the uniform-modulus row $U$. The carrier definition places $U$ after the compact finite-net row, local modulus row, rational fold, dyadic tolerance row, and metric window.

The continuation row $C$ records the same order of exposure for each tolerance request. It reads the finite compact net, inspects the local moduli at displayed centers, folds their rational lower bounds, and only then applies the dyadic and metric windows that justify the uniform precision.

Transport through $H$, provenance through $P$, and naming through $N$ preserve the same finite envelope. Therefore the vision handoff is a concrete NameCert route and cannot be reread as any of the excluded host compactness, selection, equality, quotient, or completeness data.
\end{proof}

\closureat{CompactUniformModulusEnvelopeUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusEnvelopeUp}
  \constructivestory{A $\CompactUniformModulusEnvelopeUp$ packet is generated from CompactMetric finite-net data, ContinuousMap local moduli, a RatUp lower-bound fold, DyadicRatCore tolerance compatibility, Metric distance windows, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-envelope-carrier}, \autoref{thm:compact-uniform-modulus-envelope-namecert-obligations}, and \autoref{thm:compact-uniform-modulus-envelope-vision-handoff} seed the finite compact-uniform-modulus envelope route from compact nets and local moduli to a tolerance-only uniform-modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, selected pointwise radii, host function equality, quotient real representatives, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-net exposure, local modulus exposure, rational lower-bound fold exactness, dyadic tolerance compatibility, metric distance-window replay, uniform-modulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformModulusEnvelopeUp.}
\end{closurestatus}
