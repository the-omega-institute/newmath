\chapter{A Concrete Naming Certificate for $\PoincareDiskMetricUp$}
\label{ch:concrete-instances-poincare-disk-metric-namecert}
\origin{ai}

The $\PoincareDiskMetricUp$ packet realizes the displayed disk-map, metric-read, boundary, provenance, and refusal rows of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the disk carrier of \autoref{ch:concrete-instances-unitdisk-namecert}, the phase-transport route of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, the metric interface of \autoref{ch:concrete-instances-metric-namecert}, the circle boundary source of \autoref{ch:concrete-instances-s1-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a metric-reading route on displayed disk rows; it is not a completed hyperbolic plane, quotient ideal boundary, selected geodesic, host analytic distance, or global compactification.

\begin{definition}[Poincare disk metric carrier]
\label{def:poincare-disk-metric-carrier}
A $\PoincareDiskMetricUp$ carrier is a finite $\BHist$ packet
$$
M=(U,T,Q,R,S,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ disk-coordinate row for the finite source and target disk observations.
\item $T$ is the $\PoincareDiskPhaseTransportUp$ route carrying disk-surface transport and phase provenance between displayed observations.
\item $Q$ is the $\MetricUp$ read row, recording the finite metric comparison requested for the displayed disk observations.
\item $R$ is the $\RealUp$ seal row attached to the metric readback.
\item $S$ is the $\SOneUp$ boundary row used when the metric read approaches or cites the visible boundary source.
\item $B$ records boundary refusal data: no interior disk point, ideal boundary quotient, selected global endpoint, or completed hyperbolic plane is exposed by the packet.
\item $H$ records componentwise $\hsame$ transport for disk rows, phase transport, metric reads, Real seals, boundary rows, refusal rows, replay rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a metric request through disk-coordinate inspection, phase transport, metric read, Real-seal consumption, boundary read, refusal, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\PoincareDiskMetricUp$, $\UnitDiskUp$, $\PoincareDiskPhaseTransportUp$, $\MetricUp$, $\RealUp$, $\SOneUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PoincareDiskMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,T,Q,R,S,B,H,C,P,N$. It has no coordinate for a host analytic metric, a completed disk, quotient ideal-boundary equality, selected geodesics, physical horizons, or global compactification.
\end{definition}

\begin{theorem}[Poincare disk metric NameCert obligations]
\label{thm:poincare-disk-metric-namecert-obligations}
For every accepted $\PoincareDiskMetricUp$ carrier
$$
M=(U,T,Q,R,S,B,H,C,P,N),
$$
the local $\NameCert_{\PoincareDiskMetricUp}$ surface consists exactly of disk-coordinate admission, phase-transport admission, metric-read admission, Real-seal boundary, $\SOneUp$ boundary readback, refusal rows, componentwise transport, displayed replay, provenance, and local naming. No metric obligation is stored outside those finite rows.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:poincare-disk-metric-carrier}. The visible geometric rows are $U,T,Q,R,S,B$, and the structural rows are $H,C,P,N$.

Read disk coordinates and phase transport first. The metric row $Q$ is available only after the displayed $\UnitDiskUp$ observations and the $\PoincareDiskPhaseTransportUp$ route have been exposed.

The Real and boundary rows are downstream. The row $R$ seals the metric read as a finite Real-facing datum, while $S$ and $B$ record the boundary source and the refusal of completed ideal-boundary or global-geometry data.

Transport, replay, provenance, and local naming preserve exactly that route. Since no carrier coordinate stores a host analytic metric, completed disk, quotient boundary, selected geodesic, physical horizon, or global compactification, none can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Poincare disk metric transport nonescape]
\label{thm:poincare-disk-metric-transport-nonescape}
Every accepted $\PoincareDiskMetricUp$ transport of a metric read must remain inside the displayed route
$$
\UnitDiskUp \longmapsto \PoincareDiskPhaseTransportUp \longmapsto \MetricUp \longmapsto \RealUp \longmapsto \SOneUp
$$
together with boundary refusal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. It cannot export a completed disk, quotient ideal-boundary equality, selected global endpoint, or host analytic distance.
\end{theorem}
\begin{proof}
Start with the transport rows $H$ and $C$. They can move only the displayed disk observations, phase-transport route, metric read, Real seal, boundary row, refusal row, provenance, and local naming.

Apply carrier admission. The route reaches the metric read $Q$ only through $U$ and $T$, and reaches the Real and boundary rows only after that metric read has been displayed.

The refusal row $B$ remains attached to the same route. Thus transported metric data cannot escape into a completed disk, quotient ideal boundary, selected endpoint, host analytic distance, or global compactification.
\end{proof}

\begin{theorem}[Poincare disk metric phase-transport handoff]
\label{thm:poincare-disk-metric-phase-transport-handoff}
Every phase-transport consumer of an accepted $\PoincareDiskMetricUp$ packet receives the metric read only after the $\UnitDiskUp$ disk-coordinate row, the $\PoincareDiskPhaseTransportUp$ route, the $\MetricUp$ comparison row, the $\RealUp$ seal, the $\SOneUp$ boundary row, and the refusal row have been displayed. The handoff is a finite Poincare-disk metric packet over BEDC rows, not a host-level hyperbolic metric.
\end{theorem}
\begin{proof}
Begin with the phase-transport request. The carrier exposes the disk coordinates $U$ and the transport route $T$ before any metric or boundary-facing row is available.

Read the metric row next. The comparison $Q$ is tied to those displayed disk observations, and the Real seal $R$ records only the finite readback attached to $Q$.

Finally read the boundary and refusal rows. The row $S$ supplies the visible circle boundary source, while $B$ prevents the handoff from becoming a completed hyperbolic plane, quotient ideal boundary, selected geodesic, physical horizon, or global compactification.
\end{proof}

\falsifiablePrediction{An accepted $\PoincareDiskMetricUp$ read is refuted if a metric consumer obtains a completed disk, quotient ideal-boundary equality, selected global endpoint, host analytic distance, or global compactification without displaying UnitDisk coordinates, PoincareDiskPhaseTransport routing, Metric rows, Real seals, SOne boundary rows, boundary refusal, transport, replay, provenance, and local naming.}

\independenceWitness{unitdisk, poincare-disk-phase-transport, metric, s1: no listed sibling is bijective to $\PoincareDiskMetricUp$ because this packet jointly carries disk coordinates, phase transport, metric reads, Real seals, circle-boundary rows, refusal data, transport, replay, provenance, and local naming as one finite route.}

\closureat{PoincareDiskMetricUp}{seedStr}

\begin{closurestatus}{\PoincareDiskMetricUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poincare-disk-metric-carrier}, \autoref{thm:poincare-disk-metric-namecert-obligations}, \autoref{thm:poincare-disk-metric-transport-nonescape}, and \autoref{thm:poincare-disk-metric-phase-transport-handoff} seed the finite Poincare disk metric route over UnitDisk, PoincareDiskPhaseTransport, Metric, Real, SOne, refusal, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a completed hyperbolic plane, quotient ideal-boundary equality, selected geodesics, host analytic distance, physical horizon data, global compactification, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk-coordinate admission, phase-transport admission, metric-read exactness, Real-seal handoff, boundary refusal, transport, replay, provenance, and local naming for BEDC.Derived.PoincareDiskMetricUp.}
  \constructivestory{A $\PoincareDiskMetricUp$ packet is generated from UnitDisk coordinates, PoincareDiskPhaseTransport rows, Metric readbacks, RealUp seals, SOne boundary rows, boundary-refusal data, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
