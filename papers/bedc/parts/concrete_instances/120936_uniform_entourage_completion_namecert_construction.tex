\chapter{A Concrete Naming Certificate for $\UniformEntourageCompletionUp$}
\label{ch:concrete-instances-uniform-entourage-completion-namecert}
\origin{human}

Following Kelley and Bourbaki on uniform spaces, the $\UniformEntourageCompletionUp$ packet records the L10 completion boundary where displayed entourage-Cauchy data are routed to a separated real-facing completion surface as finite BEDC rows. It sits after the uniform-space entourage discipline of \autoref{ch:concrete-instances-uniformspace-namecert}, the finite StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the separated-completion boundary of \autoref{ch:concrete-instances-separatedcompletion-namecert}. The packet names an entourage-source completion handoff without quotient completion equality, selected representatives, countable choice, host equality of completed points, or ambient $\RealUp$ completeness.

\begin{definition}[Uniform entourage completion carrier]
\label{def:uniform-entourage-completion-carrier}
A $\UniformEntourageCompletionUp$ carrier is a finite $\BHist$ packet
$$
K=(U,E,W,R,D,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UniformSpaceUp$ source row, exposing the finite entourage requests and uniformity witnesses consumed before any completion-facing read.
\item $E$ is the entourage-Cauchy row. It records the displayed entourage windows used to recognize Cauchy behavior inside the source uniformity.
\item $W$ is the $\StreamNameUp$ window row through which observed points, tails, and comparison requests are read.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the finite windows in $W$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the dyadic radii and comparison budgets used by the Cauchy-window reads.
\item $S$ is the separated $\RealUp$ and $\SeparatedCompletionUp$ handoff row. It is readable only after $U,E,W,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the uniform source, entourage-Cauchy, window, readback, tolerance, separated handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
U,E \longmapsto W,R,D \longmapsto S,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\UniformEntourageCompletionUp$, $\UniformSpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\SeparatedCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformEntourageCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,E,W,R,D,S,H,C,P,N$. It has no coordinate for quotient completion equality, quotient entourage filters, selected representatives, selected limits, countable-choice schedules, host equality, trichotomy, or ambient completeness.
\end{definition}

\begin{theorem}[Uniform entourage completion NameCert obligations]
\label{thm:uniform-entourage-completion-namecert-obligations}
For every accepted $\UniformEntourageCompletionUp$ carrier $K=(U,E,W,R,D,S,H,C,P,N)$, the local $\NameCert_{\UniformEntourageCompletionUp}$ surface consists exactly of carrier admission for the uniform source row, entourage-Cauchy row, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance rows, separated Real-completion handoff, componentwise transport, displayed replay, provenance, and local naming. Every completion-facing obligation is read through those rows before the separated handoff $S$ is opened.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-entourage-completion-carrier}. The displayed tuple exposes $U,E,W,R,D,S,H,C,P,N$ and no other completion-facing coordinate.

The source and Cauchy evidence are read first. The uniform source row $U$ supplies the entourage discipline, while $E$ records the Cauchy-window rows inside that discipline.

The finite observation route is then forced through $W,R,D$. StreamName windows expose the observed tails, RegSeqRat rows read back the finite windows, and DyadicRatCore rows record the tolerance comparisons used by the Cauchy request.

Finally read $S,H,C,P,N$. The separated Real-completion handoff is downstream of the displayed source, window, readback, and tolerance rows, and the structural rows preserve only that route. Since the excluded quotient, selector, choice, host-equality, trichotomy, and ambient-completeness objects are not carrier coordinates, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Uniform entourage completion L10 handoff]
\label{thm:uniform-entourage-completion-l10-handoff}
Every L10 Real or Completion consumer of an accepted $\UniformEntourageCompletionUp$ carrier reads the finite route
$$
\UniformSpaceUp \longmapsto E \longmapsto \StreamNameUp,\RegSeqRatUp,\DyadicRatCoreUp \longmapsto \RealUp,\SeparatedCompletionUp .
$$
In carrier notation, every such read factors through
$$
U,E \longmapsto W,R,D \longmapsto S,H,C,P,N .
$$
Thus the separated completion boundary is a displayed handoff from finite entourage-source Cauchy data, not a quotient completion equality, selected representative, countable-choice schedule, host equality, or ambient completeness premise.
\end{theorem}
\begin{proof}
Apply the NameCert obligation surface of \autoref{thm:uniform-entourage-completion-namecert-obligations}. It confines every accepted completion-facing read to the carrier rows $U,E,W,R,D,S,H,C,P,N$.

Read the route in row order. The source row $U$ and entourage-Cauchy row $E$ precede the observation rows, so the consumer cannot start from a detached quotient filter or a selected completed point.

The finite observation rows $W,R,D$ precede the separated handoff. They expose StreamName windows, regular rational readback, and dyadic tolerance budgets before the Real or separated-completion boundary in $S$ is available.

The structural rows then replay the same route. Componentwise transport, displayed continuation, provenance, and local naming preserve the finite handoff, so no omitted quotient, selector, choice, host-equality, trichotomy, or ambient-completeness coordinate can be introduced downstream.
\end{proof}

\falsifiablePrediction{Every accepted $\UniformEntourageCompletionUp$ completion-facing read factors through $\UniformSpaceUp$, entourage-Cauchy rows, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, a separated $\RealUp$ and $\SeparatedCompletionUp$ handoff, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows.}

\independenceWitness{uniformspace, streamname, regseqrat, dyadicratcore, real, separatedcompletion: no listed sibling is bijective to $\UniformEntourageCompletionUp$ because this packet jointly carries the uniform source, entourage-Cauchy windows, finite readback, dyadic tolerances, separated handoff, transport, replay, provenance, and local naming as one completion boundary.}

\closureat{UniformEntourageCompletionUp}{seedStr}

\begin{closurestatus}{\UniformEntourageCompletionUp}
  \constructivestory{A $\UniformEntourageCompletionUp$ packet is generated from UniformSpaceUp entourage rows, entourage-Cauchy window rows, StreamNameUp observations, RegSeqRatUp readback, DyadicRatCoreUp tolerances, RealUp and SeparatedCompletionUp handoff data, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-entourage-completion-carrier}, \autoref{thm:uniform-entourage-completion-namecert-obligations}, and \autoref{thm:uniform-entourage-completion-l10-handoff} seed the L10 entourage-source completion boundary over UniformSpaceUp, StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, RealUp, SeparatedCompletionUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient completion equality, selected representatives, countable choice, host equality, trichotomy, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, entourage-Cauchy window routing, StreamName and RegSeqRat readback, DyadicRatCore tolerance ordering, separated Real-completion handoff, transport, replay, provenance, and local naming for BEDC.Derived.UniformEntourageCompletionUp.}
\end{closurestatus}
