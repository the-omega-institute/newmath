\begin{theorem}[Metric completion existence Cauchy-convergence dependency route]
\label{thm:metric-completion-existence-cauchy-convergence-space-dependency-route}
Every $\MetricCompletionExistenceUp$ completion-seal consumer that uses Cauchy-convergence data first reads the Cauchy filter basis of \autoref{ch:concrete-instances-cauchy-filter-basis-namecert}, the regular Cauchy tail row, the separated-completion boundary of \autoref{ch:concrete-instances-separatedcompletion-namecert}, and the terminal real-name readback row before the completion seal is public. Thus the consumer receives finite basis, tail, separated-completion, and $\RealUp$ rows attached to the same displayed completion packet, not quotient completion, selected representatives, host limit choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project the accepted $\MetricCompletionExistenceUp$ carrier. Its existing route displays the source metric, Cauchy schedule, dense embedding, finite stream windows, regular rational readback, dyadic tolerance ledger, completion seal, transport, replay, provenance, and local naming rows.

Insert the Cauchy-filter and separated-completion dependencies before the seal is read. The basis row fixes the finite Cauchy requests, the regular tail row supplies the readback thresholds, and the separated-completion boundary fixes the equality route that downstream consumers may inspect.

The final readback remains terminal. The $\RealUp$ row is reached only through the same displayed packet, so the completion-seal consumer cannot replace the dependency route by quotient completion, selected representatives, host limit choice, or ambient completeness.
\end{proof}
