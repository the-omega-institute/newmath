The $\MetricCompletionExistenceUp$ seal consumes $\RegularCauchyTailEstimateUp$ before exposing completion-facing rows: finite tail thresholds enter through \autoref{ch:concrete-instances-regularcauchytailestimate-namecert}, then travel along the existing metric-completion sibling handoff of \autoref{thm:metric-completion-existence-sibling-handoff}. Thus a completion consumer sees the tail estimate as displayed threshold and readback data attached to the same $\MetricSpaceUp$, $\CauchyUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\MetricCompletionUp$, and $\CompleteMetricUp$ route, not as a quotient completion or ambient completeness principle.
