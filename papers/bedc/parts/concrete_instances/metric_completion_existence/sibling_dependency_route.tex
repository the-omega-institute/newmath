Every $\MetricCompletionExistenceUp$ existence seal is read after the separated uniqueness boundary of \autoref{ch:concrete-instances-metric-completion-uniqueness-namecert} and the reflective completion route of \autoref{ch:concrete-instances-cauchy-completion-reflective-subcategory-namecert}. The handoff is finite completion-existence data: a source metric row, completion-facing route rows, uniqueness transport, reflective visibility, replay, provenance, and local $\NameCert_{\MetricCompletionExistenceUp}$ naming. It is not quotient completion, selected representative equality, host limit choice, or an ambient completeness construction.

\begin{theorem}[Metric completion existence sibling handoff]
\label{thm:metric-completion-existence-sibling-handoff}
For every accepted $\MetricCompletionExistenceUp$ packet $E=(M,S,D,W,R,Y,U,H,C,P,N)$, a completion-existence consumer may use $U$ only after the uniqueness route of \autoref{ch:concrete-instances-metric-completion-uniqueness-namecert} and the reflective-subcategory route of \autoref{ch:concrete-instances-cauchy-completion-reflective-subcategory-namecert} have been displayed through the same finite packet. The consumer receives the completion seal, uniqueness transport, reflective visibility, replay, provenance, and local naming rows; it receives no quotient completion, choice-selected limit, or host completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metric-completion-existence-carrier}. The packet exposes $M,S,D,W,R,Y,U,H,C,P,N$ as the only completion-existence surface.

Use the local obligations of \autoref{thm:metric-completion-existence-namecert-obligations} to order every read of $U$ through the source metric, Cauchy schedule, dense embedding, finite stream windows, rational readback, dyadic tolerance ledger, uniqueness ledger, transport, replay, provenance, and local naming rows.

The two sibling chapters supply the horizontal route: metric-completion uniqueness contributes the separated comparison boundary, and Cauchy-completion reflective subcategory contributes the reflective visibility boundary. Since both are consumed as displayed rows inside the same finite handoff, no quotient completion or choice-selected limit construction can be substituted for the packet.
\end{proof}
