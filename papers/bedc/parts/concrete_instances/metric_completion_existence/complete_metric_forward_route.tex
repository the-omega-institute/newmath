The $\MetricCompletionExistenceUp$ seal is read only after the Cauchy-filter basis route of \autoref{ch:concrete-instances-cauchy-filter-basis-namecert} and the separated-completion route of \autoref{ch:concrete-instances-separatedcompletion-namecert} have both been displayed for the same completion packet. Thus a completion-existence consumer first sees the finite Cauchy-basis rows, then the separated-completion boundary, and only then treats the $\MetricCompletionExistenceUp$ row as a seal for a complete metric forward route.
