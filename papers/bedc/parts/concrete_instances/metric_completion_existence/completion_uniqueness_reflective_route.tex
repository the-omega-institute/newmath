\begin{theorem}[Metric completion existence uniqueness-reflective route]
\label{thm:metric-completion-existence-uniqueness-reflective-route}
Every accepted $\MetricCompletionExistenceUp$ read factors first through the existence carrier of \autoref{def:metric-completion-existence-carrier}. Before any completion consumer receives the completion seal $U$, the read must display the separated comparison boundary of $\MetricCompletionUniquenessUp$ and the reflective visibility boundary of $\CauchyCompletionReflectiveSubcategoryUp$ over the same finite source, Cauchy schedule, dense embedding, window, readback, dyadic, transport, replay, provenance, and local naming rows. It exports no quotient completion, selected representative, host limit choice, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the existence carrier. Its rows $M,S,D,W,R,Y,U,H,C,P,N$ are the whole route by which the source metric and Cauchy schedule reach a completion seal.

Use \autoref{thm:metric-completion-existence-namecert-obligations} to keep the source metric, schedule, dense embedding, finite windows, regular rational readback, dyadic tolerance, completion seal, transport, replay, provenance, and local naming in the displayed order.

Now read the sibling surfaces named in the packet provenance. The separated comparison boundary of \autoref{ch:concrete-instances-metric-completion-uniqueness-namecert} supplies the uniqueness side, and the reflective visibility boundary of \autoref{ch:concrete-instances-cauchy-completion-reflective-subcategory-namecert} supplies the reflective side.

Because both sibling reads are consumed through the same finite carrier route, the completion consumer receives a uniqueness-reflective handoff rather than a quotient object, selected representative, host limit choice, countable-choice schedule, or ambient completeness theorem.
\end{proof}
