\begin{theorem}[Metric completion existence to Cauchy completion monad law forward link]
\label{thm:metric-completion-existence-cauchy-completion-monad-law-forward-link}
Every $\MetricCompletionExistenceUp$ completion seal consumed by a completion-law reader must pass through the finite $\MetricSpaceUp$, $\CauchyUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\MetricCompletionUp$, and $\CompleteMetricUp$ rows before the $\CauchyCompletionMonadLawUp$ law surface of \autoref{ch:concrete-instances-cauchy-completion-monad-law-namecert} is read. The forward link exports only this displayed completion-existence route, together with componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\MetricCompletionExistenceUp}$ row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metric-completion-existence-carrier}. The source metric row $M$, Cauchy schedule $S$, selected stream windows $W$, regular rational readback $R$, dyadic tolerance ledger $Y$, and completion seal $U$ form the ordered finite route toward $\MetricCompletionUp$ and $\CompleteMetricUp$ consumers.

Apply \autoref{thm:metric-completion-existence-namecert-obligations}. It states that the local certificate is exhausted by carrier admission, the displayed completion-existence rows, transport, replay, provenance, and local naming. Hence a completion-law reader cannot bypass those rows when reaching the law surface.

Finally read the structural rows $H,C,P,N$. They preserve and replay the same finite route for the consumer, so the $\CauchyCompletionMonadLawUp$ handoff is a forward sibling dependency through the completion-existence carrier rather than a quotient completion, selected representative, or ambient completeness principle.
\end{proof}
