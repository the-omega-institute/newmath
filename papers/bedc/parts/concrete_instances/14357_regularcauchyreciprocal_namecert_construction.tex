\chapter{A Concrete Naming Certificate for $\RegularCauchyReciprocalUp$}
\label{ch:concrete-instances-regular-cauchy-reciprocal-namecert}
\origin{human}

Bishop and Bridges-style constructive reciprocal data begin from a regular Cauchy rational name together with a displayed apartness window that keeps the inspected tail away from zero. The $\RegularCauchyReciprocalUp$ packet names the finite interface needed to read that reciprocal surface. It refines the budget-only surface of \autoref{ch:concrete-instances-cauchyinversebudget-namecert} by binding the $\RegSeqRatUp$ source of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic endpoint and radius arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert} into one local NameCert carrier. The chapter records only the finite reciprocal-read surface; it does not construct a total reciprocal operation on $\RealUp$.

\begin{definition}[Regular Cauchy reciprocal carrier]
\label{def:regular-cauchy-reciprocal-carrier}
A $\RegularCauchyReciprocalUp$ carrier is a finite $\BHist$ packet
$$
R=(Q,A,M,W,D,B,T,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the accepted $\RegSeqRatUp$ source row whose regular rational entries are being inspected.
\item $A$ is the apartness-window row displaying a rational lower bound that keeps the inspected tail of $Q$ away from zero.
\item $M$ is the finite modulus row selecting the precision at which the reciprocal read is requested.
\item $W$ is the finite $\StreamNameUp$ window opened from $Q$ under $A$ and $M$.
\item $D$ is the $\DyadicRatCoreUp$ endpoint, radius, and comparison ledger read from $W$.
\item $B$ is the sibling $\CauchyInverseBudgetUp$ row that records the reciprocal-window budget over the same $Q,A,M,W,D$ rows.
\item $T$ is the regular rational readback row naming the reciprocal candidate as a $\RegSeqRatUp$ surface.
\item $E$ is the terminal $\RealUp$ seal row reached only after the displayed source, apartness, modulus, window, dyadic, budget, and readback rows have been consumed.
\item $H$ records componentwise $\hsame$ transport for $Q,A,M,W,D,B,T,E,C,P,N$.
\item $C$ records the displayed $\Cont$ replay for source inspection, apartness exposure, finite-window selection, dyadic reciprocal arithmetic, budget handoff, regular readback, and Real seal handoff.
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyInverseBudgetUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyReciprocalUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through the displayed rows $Q,A,M,W,D,B,T,E,H,C,P,N$.
\end{definition}

\begin{theorem}[Regular Cauchy reciprocal apartness window]
\label{thm:regular-cauchy-reciprocal-apartness-window}
Every accepted $\RegularCauchyReciprocalUp$ carrier exposes the apartness row $A$ before any reciprocal dyadic row $D$, budget row $B$, regular readback row $T$, or $\RealUp$ seal row $E$ can be consumed. A route that omits $A$ remains a boundary attempt and cannot export a reciprocal candidate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-reciprocal-carrier}. The source-side rows are $Q,A,M,W$, and the reciprocal-facing rows begin only after the apartness and modulus rows have selected a finite window.

The dyadic ledger $D$ reads endpoint, radius, and comparison data from $W$. Since the reciprocal denominator is controlled by the displayed lower bound in $A$, the dyadic reciprocal row has no carrier route that bypasses $A$.

The budget row $B$, readback row $T$, and seal row $E$ are downstream of that dyadic row. Therefore omitting $A$ leaves no displayed route from the source to a reciprocal candidate; the local classifier records such a route as boundary evidence.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalCarrier\_apartness\_window}

\begin{theorem}[Regular Cauchy reciprocal readback route]
\label{thm:regular-cauchy-reciprocal-readback-route}
For every accepted $\RegularCauchyReciprocalUp$ carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$, the reciprocal readback row $T$ factors through
$$
Q\longmapsto A\longmapsto M\longmapsto W\longmapsto D\longmapsto B\longmapsto T.
$$
The terminal $\RealUp$ seal $E$ is reached only after this finite route has been displayed.
\end{theorem}
\begin{proof}
Begin with the apartness-window theorem. It places $A$ before any dyadic reciprocal arithmetic, so the route to $T$ cannot start from $Q$ alone.

Next open the finite window row $W$ by using the modulus row $M$. The dyadic ledger $D$ is a read of that window, and the sibling budget row $B$ is the same reciprocal-window discipline named by \autoref{ch:concrete-instances-cauchyinversebudget-namecert}.

Finally project $T$ and then $E$ from the carrier. The readback row names the reciprocal candidate as regular rational data, while the seal row is terminal and adds no second route around the displayed finite chain.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal classifier stability]
\label{thm:regular-cauchy-reciprocal-classifier-stability}
Let
$$
R=(Q,A,M,W,D,B,T,E,H,C,P,N)
\quad\hbox{and}\quad
R'=(Q',A',M',W',D',B',T',E',H',C',P',N')
$$
be accepted $\RegularCauchyReciprocalUp$ carriers. If the displayed transport row $H$ supplies componentwise $\hsame$ comparisons from each row of $R$ to the corresponding row of $R'$, then the local classifier preserves the $\RegSeqRatUp$ source, apartness row, modulus row, finite $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger, $\CauchyInverseBudgetUp$ budget row, reciprocal readback row, and terminal $\RealUp$ seal route.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalClassifierStability}
\begin{proof}
Project the transport coordinate $H$ from the carrier. Its comparisons are componentwise, so the source rows $Q,Q'$, apartness rows $A,A'$, modulus rows $M,M'$, finite windows $W,W'$, dyadic ledgers $D,D'$, budget rows $B,B'$, readback rows $T,T'$, and seal rows $E,E'$ are matched before any classifier conclusion is read.

Apply the apartness-window theorem to both carriers. Since the transported apartness rows remain the first reciprocal-facing rows, the reciprocal denominator witness is preserved by the classifier.

Apply the readback-route theorem to both carriers. The transported route from $Q$ through $B$ to $T$ matches the transported route from $Q'$ through $B'$ to $T'$, and the terminal seal rows are reached only after those finite routes.

Thus classifier equality cannot forget, reorder, or replace any analytic row of the reciprocal packet. It transports exactly the displayed source, apartness, modulus, window, dyadic, budget, readback, and Real seal coordinates.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal terminal seal non-totality]
\label{thm:regular-cauchy-reciprocal-terminal-seal-non-totality}
The terminal $\RealUp$ seal row $E$ of an accepted $\RegularCauchyReciprocalUp$ carrier cannot be reread as a total reciprocal operation on all $\RealUp$ names. Any route from the seal back to a reciprocal candidate must still factor through the displayed $\RegSeqRatUp$ source row $Q$, apartness row $A$, modulus row $M$, finite $\StreamNameUp$ window $W$, dyadic ledger $D$, $\CauchyInverseBudgetUp$ budget row $B$, and regular readback row $T$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalTerminalSealNonTotality}
\begin{proof}
Inspect the carrier definition. The seal coordinate $E$ is listed after the source, apartness, modulus, window, dyadic, budget, and readback rows, and the classifier reads it as one coordinate of the twelve-row packet.

The readback-route theorem states that $E$ is reached only after the finite route through $T$ has been displayed. Therefore the seal does not introduce a second route from arbitrary $\RealUp$ names to reciprocal data.

The apartness-window theorem keeps $A$ mandatory for every reciprocal-facing row. Since a total reciprocal on all $\RealUp$ names would erase the need for such a displayed apartness row, it is not present in the packet.

Consequently the terminal seal records only the end of one finite regular-Cauchy reciprocal read. It cannot be exported as a total reciprocal operation or as a boundary bypass around the apartness obligation.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal denominator apartness]
\label{thm:regular-cauchy-reciprocal-denominator-apartness}
For every accepted $\RegularCauchyReciprocalUp$ carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$, the dyadic reciprocal row $D$ and the $\CauchyInverseBudgetUp$ budget row $B$ are admissible only after the apartness row $A$ displays a positive lower bound for the inspected $\RegSeqRatUp$ tail named by $Q,M,W$. A consumer that reads $D$ or $B$ without that positive lower-bound display has not reached a reciprocal row of the packet.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalDenominatorApartness}
\begin{proof}
Project \autoref{def:regular-cauchy-reciprocal-carrier}. The source row $Q$, apartness row $A$, modulus row $M$, and finite window row $W$ precede the dyadic reciprocal row $D$ and budget row $B$ in the carrier route.

Apply \autoref{thm:regular-cauchy-reciprocal-apartness-window}. It states that $A$ is exposed before $D,B,T,E$ can be consumed, so the denominator-side condition is part of the carrier admission rather than a later Real-facing consequence.

Finally read \autoref{thm:regular-cauchy-reciprocal-readback-route}. The route from $Q$ to $T$ passes through $A,M,W,D,B$ in that order, and therefore $D$ and $B$ are reciprocal rows only when the inspected regular tail has the displayed positive lower bound supplied by $A$.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal nonzero-denominator window]
\label{thm:regular-cauchy-reciprocal-nonzero-denominator-window}
Any accepted $\RegularCauchyReciprocalUp$ packet whose dyadic inverse budget is read must expose a nonzero-denominator window consisting of the $\RegSeqRatUp$ source row $Q$, apartness row $A$, finite modulus row $M$, $\StreamNameUp$ window $W$, and $\DyadicRatCoreUp$ row $D$. Without all five rows the packet has not reached the $\CauchyInverseBudgetUp$ row $B$.
\end{theorem}
\begin{proof}
Project the carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$ from \autoref{def:regular-cauchy-reciprocal-carrier}. The only source, apartness, modulus, window, and dyadic rows available to the reciprocal budget are $Q,A,M,W,D$.

Apply \autoref{thm:regular-cauchy-reciprocal-denominator-apartness}. It places the positive lower-bound display in $A$ before the dyadic reciprocal row $D$ and budget row $B$ can be consumed.

Then apply \autoref{thm:regular-cauchy-reciprocal-readback-route}. The route to the reciprocal readback passes through $Q,A,M,W,D,B$, so a consumer that omits any one of $Q,A,M,W,D$ remains before the $\CauchyInverseBudgetUp$ row rather than at the dyadic inverse budget.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalNonzeroDenominatorWindow}

\begin{theorem}[Regular Cauchy reciprocal tail stability]
\label{thm:regular-cauchy-reciprocal-tail-stability}
Let $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$ be an accepted $\RegularCauchyReciprocalUp$ carrier. If a later inspected $\StreamNameUp$ window preserves the $\RegSeqRatUp$ source row $Q$ together with the apartness row $A$, modulus row $M$, finite window row $W$, and dyadic reciprocal row $D$, then the same reciprocal readback row $T$ and terminal $\RealUp$ seal row $E$ transport through $H,C,P,N$. Thus shifting to a later tail window cannot change the reciprocal readback unless it changes one of $A,M,W,D$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalTailStability}
\begin{proof}
Use \autoref{thm:regular-cauchy-reciprocal-denominator-apartness}. A later window that preserves $A,M,W,D$ keeps the same displayed positive lower-bound and dyadic reciprocal data for the inspected regular tail.

Apply \autoref{thm:regular-cauchy-reciprocal-readback-route}. Since the route to $T$ passes through $Q,A,M,W,D,B$, preserving the apartness, window, and dyadic rows preserves the reciprocal readback route once the same budget row is read.

Apply \autoref{thm:regular-cauchy-reciprocal-classifier-stability}. The transport row $H$ compares corresponding carrier rows componentwise, while $C,P,N$ replay, source, and name the same packet. The terminal seal $E$ is therefore transported with $T$ rather than recomputed from a new denominator condition.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal window composition]
\label{thm:regular-cauchy-reciprocal-window-composition}
Let $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$ be an accepted $\RegularCauchyReciprocalUp$ carrier. Suppose a later finite $\StreamNameUp$ window preserves the displayed rows $Q,A,M,W,D$ and then reads the same $\CauchyInverseBudgetUp$ row $B$. The composed finite route still reaches the same regular rational readback row $T$ and terminal $\RealUp$ seal row $E$ through
$$
Q\longmapsto A\longmapsto M\longmapsto W\longmapsto D\longmapsto B\longmapsto T\longmapsto E.
$$
Thus window composition changes neither the reciprocal readback nor the Real seal unless it changes one of the displayed source, apartness, modulus, window, dyadic, or budget rows.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-reciprocal-denominator-apartness}. It keeps the apartness row $A$ in front of every dyadic reciprocal and budget read, so the later window cannot compose into a reciprocal route while dropping the positive lower-bound display.

Use \autoref{thm:regular-cauchy-reciprocal-nonzero-denominator-window}. The preserved rows $Q,A,M,W,D$ are exactly the nonzero-denominator window needed before the same budget row $B$ can be read.

Apply \autoref{thm:regular-cauchy-reciprocal-readback-route}. Once the same budget row is present, the route to $T$ is the displayed chain through $Q,A,M,W,D,B$, and the seal $E$ is terminal after that chain.

Finally use \autoref{thm:regular-cauchy-reciprocal-tail-stability}. Since the later window preserves the source, apartness, modulus, window, and dyadic rows and reads the same budget row, the transported packet reaches the same $T$ and $E$ rather than recomputing a new reciprocal surface.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalWindowComposition}

\begin{theorem}[Regular Cauchy reciprocal window denominator stability]
\label{thm:regularcauchyreciprocal-window-denominator-stability}
Let $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$ be an accepted $\RegularCauchyReciprocalUp$ carrier. Suppose a later finite $\StreamNameUp$ window preserves the displayed rows $Q,A,M,W,D$ and reads the same $\CauchyInverseBudgetUp$ row $B$. Then the denominator apartness witness recorded by $A$ remains visible before the reciprocal readback row $T$ and the terminal $\RealUp$ seal row $E$ are consumed. In particular, the later window cannot treat $B,T,E$ as a hidden choice of denominator data.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-reciprocal-denominator-apartness}. The dyadic reciprocal row $D$ and budget row $B$ are admissible only after $A$ displays the positive lower-bound witness for the inspected regular tail.

Use \autoref{thm:regular-cauchy-reciprocal-nonzero-denominator-window}. Since the later window preserves $Q,A,M,W,D$, it preserves exactly the nonzero-denominator surface that must be present before the same $\CauchyInverseBudgetUp$ row is read.

Apply \autoref{thm:regular-cauchy-reciprocal-tail-stability}. Preserving the source, apartness, modulus, window, and dyadic rows transports the same readback row $T$ and terminal seal row $E$ through the carrier rows $H,C,P,N$.

Finally use \autoref{thm:regular-cauchy-reciprocal-window-composition}. The composed route reaches $T$ and then $E$ only through the displayed chain $Q,A,M,W,D,B,T,E$, so the denominator apartness witness remains visible throughout the reciprocal handoff rather than becoming hidden choice data.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal located apartness window]
\label{thm:regularcauchyreciprocal-located-apartness-window}
Any $\RealUp$-seal or budget-facing consumer of an accepted $\RegularCauchyReciprocalUp$ carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$ must expose the $\RegSeqRatUp$ source row $Q$, apartness row $A$, modulus row $M$, $\StreamNameUp$ window row $W$, $\DyadicRatCoreUp$ comparison row $D$, and $\CauchyInverseBudgetUp$ budget row $B$ before reading the reciprocal readback row $T$ or terminal seal row $E$. The route exports no total reciprocal on arbitrary $\RealUp$ names, trichotomy, quotient stream equality, selected limit, or ambient completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only reciprocal-facing analytic rows before readback are the source, apartness, modulus, window, dyadic comparison, and budget rows $Q,A,M,W,D,B$.

Apply \autoref{thm:regular-cauchy-reciprocal-apartness-window}, \autoref{thm:regular-cauchy-reciprocal-denominator-apartness}, and \autoref{thm:regular-cauchy-reciprocal-nonzero-denominator-window}. These place the located apartness display in $A$ before the dyadic comparison row and identify $Q,A,M,W,D$ as the nonzero-denominator window required before $B$ is read.

Next use \autoref{thm:regular-cauchy-reciprocal-window-composition}. It preserves the same route through $Q,A,M,W,D,B,T,E$ under later finite windows, so reading $T$ cannot detach the reciprocal candidate from its displayed denominator witness.

Finally apply \autoref{thm:regular-cauchy-reciprocal-real-seal-handoff}. The terminal seal $E$ is offered only after the source, apartness, modulus, window, dyadic, budget, and readback rows have been displayed. Since the carrier has no coordinate for total reciprocals, trichotomy, quotient stream equality, selected limits, or ambient completeness, those principles cannot be exported by either the budget-facing route or the Real-seal route.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal scoped denominator apartness]
\label{thm:regular-cauchy-reciprocal-scoped-denominator-apartness}
For every accepted $\RegularCauchyReciprocalUp$ carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$, the denominator-apartness witness is scoped exactly by the $\RegSeqRatUp$ source row $Q$, apartness row $A$, finite modulus row $M$, $\StreamNameUp$ window $W$, $\DyadicRatCoreUp$ ledger $D$, $\CauchyInverseBudgetUp$ row $B$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. It exports no total $\RealUp$ reciprocal, quotient stream, selected limit, or host equality.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-reciprocal-carrier}. The only denominator-facing rows before reciprocal readback are $Q,A,M,W,D,B$, and the only structural rows that can transport or replay them are $H,C,P,N$.

Apply \autoref{thm:regular-cauchy-reciprocal-apartness-window} and \autoref{thm:regular-cauchy-reciprocal-denominator-apartness}. These place the apartness row $A$ before every dyadic reciprocal, budget, readback, and seal coordinate, and they make the positive lower-bound display part of carrier admission.

Use \autoref{thm:regular-cauchy-reciprocal-nonzero-denominator-window} and \autoref{thm:regularcauchyreciprocal-window-denominator-stability}. The first theorem identifies $Q,A,M,W,D$ as the nonzero-denominator window required before $B$ is read, while the second preserves that witness under later finite windows.

The remaining rows $H,C,P,N$ carry only componentwise transport, replay, provenance, and local naming for the same finite surface. Thus the scoped apartness witness is exactly the displayed BEDC primitive surface $Q,A,M,W,D,B,H,C,P,N$, with no exported total reciprocal on $\RealUp$, quotient stream, selected limit, or host equality.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.TasteGate.RegularCauchyReciprocalScopedDenominatorApartness}

\begin{theorem}[Regular Cauchy reciprocal Real seal handoff]
\label{thm:regular-cauchy-reciprocal-real-seal-handoff}
Let $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$ be an accepted $\RegularCauchyReciprocalUp$ carrier. Any reciprocal consumer that is offered the terminal $\RealUp$ seal row $E$ must first be offered the $\RegSeqRatUp$ source row $Q$, apartness row $A$, modulus row $M$, finite $\StreamNameUp$ window $W$, $\DyadicRatCoreUp$ ledger $D$, $\CauchyInverseBudgetUp$ row $B$, and reciprocal readback row $T$. Thus the handoff from finite reciprocal-read data to the L10 $\RealUp$ boundary is the displayed route
$$
Q\longmapsto A\longmapsto M\longmapsto W\longmapsto D\longmapsto B\longmapsto T\longmapsto E
$$
and not a direct operation from arbitrary $\RealUp$ names to reciprocals.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.RegularCauchyReciprocalRealSealHandoff}
\begin{proof}
Apply \autoref{thm:regular-cauchy-reciprocal-apartness-window}. It places the apartness row $A$ before every reciprocal-facing coordinate, so a consumer cannot treat the terminal seal as available before the denominator witness is displayed.

Next use \autoref{thm:regular-cauchy-reciprocal-readback-route}. The route to the readback row $T$ factors through $Q,A,M,W,D,B$, and the seal row $E$ is terminal only after that finite route has been exposed.

Apply \autoref{thm:regular-cauchy-reciprocal-denominator-apartness} and \autoref{thm:regular-cauchy-reciprocal-nonzero-denominator-window}. These identify the source, apartness, modulus, window, and dyadic rows as the nonzero-denominator surface required before the budget row $B$ can be consumed.

Finally use \autoref{thm:regular-cauchy-reciprocal-tail-stability} together with \autoref{thm:regular-cauchy-reciprocal-window-composition}. Transport to later windows and composition of finite windows preserve the same $T$ and $E$ only when the displayed rows $Q,A,M,W,D,B$ are preserved. The handoff therefore exports neither a total reciprocal, quotient real equality, trichotomy, selected limit, nor ambient completeness principle; it exports only the finite route from reciprocal readback data to the terminal $\RealUp$ seal.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal NameCert obligations]
\label{thm:regular-cauchy-reciprocal-namecert-obligations}
The seed $\NameCert_{\RegularCauchyReciprocalUp}$ surface consists exactly of carrier exposure for $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$, apartness-window exposure through $A$, finite-window exposure through $W$, dyadic reciprocal arithmetic through $D$, sibling budget exposure through $B$, regular readback through $T$, terminal $\RealUp$ sealing through $E$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.RegularCauchyReciprocalNameCertObligations}
\begin{proof}
Carrier exposure is \autoref{def:regular-cauchy-reciprocal-carrier}. It supplies the full row list and states that the classifier reads only those rows.

The analytic route is given by \autoref{thm:regular-cauchy-reciprocal-apartness-window} and \autoref{thm:regular-cauchy-reciprocal-readback-route}. Together they force every reciprocal read to pass through the source, apartness, modulus, finite window, dyadic arithmetic, and sibling budget rows before reaching $T$ and $E$.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since none of these rows contains a total reciprocal, quotient stream equality, selected limit, trichotomy decision, or ambient completeness principle, the displayed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal window-budget coverage]
\label{thm:regular-cauchy-reciprocal-window-budget-coverage}
Every budget-facing read of an accepted $\RegularCauchyReciprocalUp$ carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$ must expose the rows
$$
Q\longmapsto A\longmapsto M\longmapsto W\longmapsto D\longmapsto B
$$
in that order before it treats the packet as carrying a reciprocal-window budget.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-reciprocal-carrier}. The budget row $B$ is displayed only after the source row $Q$, apartness row $A$, modulus row $M$, finite window row $W$, and dyadic row $D$.

Apply \autoref{thm:regular-cauchy-reciprocal-apartness-window} and \autoref{thm:regular-cauchy-reciprocal-denominator-apartness}. These place the positive lower-bound display before any dyadic or budget read.

Use \autoref{thm:regular-cauchy-reciprocal-nonzero-denominator-window}. It identifies $Q,A,M,W,D$ as the nonzero-denominator window required before the $\CauchyInverseBudgetUp$ row is reached.

Finally apply \autoref{thm:regular-cauchy-reciprocal-window-composition} and \autoref{thm:regular-cauchy-reciprocal-namecert-obligations}. Composed windows preserve the same displayed budget route, and the local NameCert surface contains no alternate budget coordinate.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.RegularCauchyReciprocalWindowBudgetCoverage}

\begin{theorem}[Regular Cauchy reciprocal readback-budget exactness]
\label{thm:regular-cauchy-reciprocal-readback-budget-exactness}
For every accepted $\RegularCauchyReciprocalUp$ carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$, the readback row $T$ is exactly the finite reciprocal-read route through the displayed budget row $B$. It is not a total reciprocal operation on arbitrary $\RealUp$ names, and it cannot be read without the preceding rows $Q,A,M,W,D,B$.
\end{theorem}
\begin{proof}
Use \autoref{thm:regular-cauchy-reciprocal-readback-route}. It factors $T$ through the displayed chain from $Q$ to $B$.

Apply \autoref{thm:regular-cauchy-reciprocal-window-budget-coverage}. Any budget-facing read must first expose $Q,A,M,W,D,B$, so $T$ has no route that bypasses the finite budget row.

Apply \autoref{thm:regular-cauchy-reciprocal-tail-stability} and \autoref{thm:regular-cauchy-reciprocal-window-composition}. Later windows and composed windows preserve the same readback only when the displayed source, apartness, modulus, window, dyadic, and budget rows are preserved.

Finally use \autoref{thm:regular-cauchy-reciprocal-terminal-seal-non-totality}. Since the terminal seal is not a total reciprocal on $\RealUp$, the preceding readback row is also only the finite route named by the carrier.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.RegularCauchyReciprocalReadbackBudgetExactness}

\begin{theorem}[Regular Cauchy reciprocal seal-route conservativity]
\label{thm:regular-cauchy-reciprocal-seal-route-conservativity}
Any $\RealUp$ seal consumer of an accepted $\RegularCauchyReciprocalUp$ carrier receives only the displayed route
$$
Q,A,M,W,D,B,T,E,H,C,P,N .
$$
The seal row $E$ conservatively packages that route and adds no total reciprocal, trichotomy, quotient stream equality, selected limit, or ambient completeness principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-cauchy-reciprocal-real-seal-handoff}. The terminal seal is offered only after the source, apartness, modulus, finite window, dyadic, budget, and readback rows have been displayed.

Use \autoref{thm:regular-cauchy-reciprocal-readback-budget-exactness}. It fixes $T$ as the finite route through $B$, so the seal cannot detach $E$ from the reciprocal-window budget.

Use \autoref{thm:regular-cauchy-reciprocal-namecert-obligations}. The remaining rows $H,C,P,N$ transport, replay, source, and name the same packet, and no additional analytic coordinate exists.

The non-totality clause follows from \autoref{thm:regular-cauchy-reciprocal-terminal-seal-non-totality}. A $\RealUp$ consumer therefore receives only the displayed conservative route.
\end{proof}

\begin{theorem}[Regular Cauchy reciprocal scope dependency lock]
\label{thm:regular-cauchy-reciprocal-scope-dependency-lock}
For every accepted $\RegularCauchyReciprocalUp$ carrier $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$, the reciprocal readback row $T$ and the terminal $\RealUp$ seal row $E$ are scoped exactly by five L10 source rows: the $\RegSeqRatUp$ source $Q$, the finite $\StreamNameUp$ window $W$, the $\DyadicRatCoreUp$ ledger $D$, the $\CauchyInverseBudgetUp$ row $B$, and the $\RealUp$ seal route $E$. A consumer that drops any one of these rows remains outside the reciprocal readback and seal surface. The dependency lock exports no total reciprocal, quotient real equality, selected limit, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyReciprocalUp.RegularCauchyReciprocalScopeDependencyLock}
\begin{proof}
Begin with \autoref{thm:regular-cauchy-reciprocal-apartness-window}. It places the apartness row before every reciprocal-facing coordinate, so the route to the five named source rows is not optional.

Next apply \autoref{thm:regular-cauchy-reciprocal-readback-route}. The readback row $T$ factors through the displayed chain from the $\RegSeqRatUp$ source to the reciprocal budget row.

Use \autoref{thm:regular-cauchy-reciprocal-window-budget-coverage} together with \autoref{thm:regular-cauchy-reciprocal-readback-budget-exactness}. These two theorems identify the $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger, and $\CauchyInverseBudgetUp$ row as the finite budget boundary for $T$.

Finally invoke \autoref{thm:regular-cauchy-reciprocal-seal-route-conservativity}. The $\RealUp$ seal $E$ only packages the same displayed route, and its nonescape clause excludes total reciprocal, quotient equality, selected limits, and ambient completeness.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyReciprocalUp$ consumer that reaches a reciprocal readback row or a $\RealUp$ seal without the displayed $\RegSeqRatUp$ source, apartness row, finite $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger, and $\CauchyInverseBudgetUp$ row refutes the carrier surface.}

\independenceWitness{regseqrat, streamname, dyadicratcore, cauchyinversebudget, real: no listed sibling supplies the whole twelve-row packet $R=(Q,A,M,W,D,B,T,E,H,C,P,N)$, because each sibling contributes only a regular source, finite window, dyadic ledger, reciprocal budget, or Real seal while this chapter binds all five analytic faces with transport, replay, provenance, and local naming.}

\begin{closurestatus}{\RegularCauchyReciprocalUp}
  \constructivestory{A $\RegularCauchyReciprocalUp$ packet is generated from a $\RegSeqRatUp$ source, an apartness-window row, a finite modulus row, a $\StreamNameUp$ window, a $\DyadicRatCoreUp$ ledger, a $\CauchyInverseBudgetUp$ budget row, a reciprocal readback row, a terminal $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-reciprocal-carrier}, \autoref{thm:regular-cauchy-reciprocal-apartness-window}, \autoref{thm:regular-cauchy-reciprocal-readback-route}, \autoref{thm:regular-cauchy-reciprocal-classifier-stability}, \autoref{thm:regular-cauchy-reciprocal-terminal-seal-non-totality}, \autoref{thm:regular-cauchy-reciprocal-namecert-obligations}, \autoref{thm:regular-cauchy-reciprocal-window-budget-coverage}, \autoref{thm:regular-cauchy-reciprocal-readback-budget-exactness}, and \autoref{thm:regular-cauchy-reciprocal-seal-route-conservativity} bind the reciprocal NameCert obligation surface for regular Cauchy names over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyInverseBudgetUp$, and $\RealUp$.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.RegularCauchyReciprocalUp.RegularCauchyReciprocalScopeDependencyLock}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a total reciprocal, trichotomy, quotient stream equality, selected limits, ambient real completeness, or a public bridge.}
  \upgradepath{Public exposure requires a consumer-facing reciprocal surface and bridge checking for the canonical dependency lock.}
\end{closurestatus}
