\chapter{A Concrete Naming Certificate for $\RegularCauchyCompactnessCriterionUp$}
\label{ch:concrete-instances-regular-cauchy-compactness-criterion-namecert}
\origin{human}

Following Bishop and Bridges constructive compactness criteria, the $\RegularCauchyCompactnessCriterionUp$ packet records the finite route by which regular Cauchy real data are admitted to a compactness read. It sits after the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the interval finite-net surface of \autoref{ch:concrete-instances-heineborelinterval-namecert}, and the finite Cauchy completion criterion of \autoref{ch:concrete-instances-finite-cauchy-completeness-criterion-namecert}. The packet names a criterion surface for Real and Completion consumers; it does not import sequential compactness, open-cover compactness, quotient stream equality, selected cluster points, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy compactness criterion carrier]
\label{def:regular-cauchy-compactness-criterion-carrier}
A $\RegularCauchyCompactnessCriterionUp$ carrier is a finite $\BHist$ packet
$$
K=(S,Q,D,R,C,W,H,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row that records which name windows are inspected before the compactness read.
\item $Q$ is the $\RegSeqRatUp$ readback row for the regular rational data exposed by those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row controlling mesh size, Cauchy radius, and comparison budget.
\item $R$ is the terminal $\RealUp$ seal row reached only after $S,Q,D$ have been displayed.
\item $C$ is the compact finite-window witness row, read through the public finite-net interval export of \autoref{ch:concrete-instances-heineborelinterval-namecert}.
\item $W$ is the finite-window extraction row that records the inspected window and net-cell data consumed by a compactness query.
\item $H$ records componentwise $\hsame$ transport for $S,Q,D,R,C,W,P,N$.
\item $P$ is the displayed $\Cont$ replay and $\Pkg$ provenance over $\RegularCauchyCompactnessCriterionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\HeineBorelIntervalUp$, $\FiniteCauchyCompletenessCriterionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyCompactnessCriterionUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that the compactness criterion is read from these finite rows. It has no coordinate for sequential compactness, open-cover compactness, a selected subsequence, a selected cluster point, quotient equality of real names, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy compactness criterion NameCert obligations]
\label{thm:regular-cauchy-compactness-criterion-namecert-obligations}
For every accepted $\RegularCauchyCompactnessCriterionUp$ carrier $K=(S,Q,D,R,C,W,H,P,N)$, the local $\NameCert_{\RegularCauchyCompactnessCriterionUp}$ surface consists exactly of carrier admission for the $\StreamNameUp$ window row, $\RegSeqRatUp$ readback row, $\DyadicRatCoreUp$ tolerance row, $\RealUp$ seal row, compact finite-window witness row, finite-window extraction row, componentwise $\hsame$ transport, displayed $\Cont$ replay with $\Pkg$ provenance, and local naming. No obligation row may be discharged by host compactness, sequential compactness, open-cover compactness, quotient stream equality, selected representatives, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-compactness-criterion-carrier}. Its displayed coordinates are exactly $S,Q,D,R,C,W,H,P,N$, so carrier admission and local naming can only inspect those rows.

Read the L10 source rows in order. The finite windows $S$ expose the regular rational readback $Q$, the dyadic tolerance row $D$ controls the Cauchy and mesh budgets, and the terminal real seal $R$ is downstream of those displayed rows.

Now read the compactness side. The row $C$ is a compact finite-window witness through the Heine--Borel interval finite-net surface, and $W$ records the actual extraction window consumed by the query. Transport, replay, provenance, and local naming preserve that same finite route, so the excluded compactness and choice principles have no carrier coordinate.
\end{proof}

\begin{theorem}[Regular Cauchy compactness criterion finite-window extraction]
\label{thm:regular-cauchy-compactness-criterion-finite-window-extraction}
Every compactness read of an accepted $\RegularCauchyCompactnessCriterionUp$ carrier factors through the finite extraction route
$$
S,Q,D,R,C,W,H,P,N.
$$
In particular, the read consumes StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, the Real seal, the Heine--Borel finite-net witness, the displayed extraction window, transport, replay, provenance, and local naming before it can expose compactness data. It is therefore a finite-window extraction, not sequential compactness, not open-cover compactness, and not an appeal to ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:regular-cauchy-compactness-criterion-namecert-obligations}. They identify the complete local surface of an accepted carrier, so a compactness read cannot leave the rows $S,Q,D,R,C,W,H,P,N$.

Use the Real-facing route next. Rows $S,Q,D$ provide finite observation, regular rational readback, and tolerance accounting before the terminal seal $R$ is available.

Finally apply the compact finite-net rows $C,W$. The public finite-net interval export supplies the compact witness as displayed net data, while $W$ records the finite extraction actually inspected by the consumer. Since neither row contains a selected subsequence, an open-cover subcover choice, quotient stream equality, or an ambient completeness theorem, the compactness read is exactly the stated finite-window extraction.
\end{proof}

\closureat{RegularCauchyCompactnessCriterionUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompactnessCriterionUp}
  \constructivestory{A $\RegularCauchyCompactnessCriterionUp$ packet is generated from StreamName windows, RegSeqRat readback, DyadicRatCore tolerance, a terminal RealUp seal, a compact finite-window witness, a finite-window extraction row, componentwise hsame transport, displayed Cont replay with Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-compactness-criterion-carrier}, \autoref{thm:regular-cauchy-compactness-criterion-namecert-obligations}, and \autoref{thm:regular-cauchy-compactness-criterion-finite-window-extraction} seed the finite compactness criterion route over StreamName, RegSeqRat, DyadicRatCore, Real, Heine--Borel interval, and finite Cauchy completion data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim sequential compactness, open-cover compactness, selected subcovers, selected cluster points, quotient equality of real names, countable choice, Lean verification, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exactness, regular rational readback, dyadic tolerance accounting, Real seal routing, Heine--Borel finite-net handoff, finite Cauchy completion handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyCompactnessCriterionUp.}
\end{closurestatus}
