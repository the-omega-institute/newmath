\chapter{A Concrete Naming Certificate for $\PanitchpakdiHyperconvexInjectivityUp$}
\label{ch:concrete-instances-panitchpakdi-hyperconvex-injectivity-namecert}
\origin{human}

Following Aronszajn and Panitchpakdi, the $\PanitchpakdiHyperconvexInjectivityUp$ packet records the finite boundary where hyperconvex ball-intersection data may be read as an injective metric-extension interface. It sits over the hyperconvex metric row of \autoref{ch:concrete-instances-hyperconvex-metric-namecert}, the injective-envelope route of \autoref{ch:concrete-instances-injective-envelope-metric-namecert}, the isometric embedding handoff of \autoref{ch:concrete-instances-isometric-embedding-namecert}, the metric source of \autoref{ch:concrete-instances-metric-namecert}, the complete-metric seal of \autoref{ch:concrete-instances-completemetric-namecert}, and the terminal real-radius rows of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed finite extension boundaries; it does not assert arbitrary-family intersections, choice-selected injective hull points, quotient completions, or ambient $\RealUp$ completeness.

\begin{definition}[Panitchpakdi hyperconvex injectivity carrier]
\label{def:panitchpakdi-hyperconvex-injectivity-carrier}
A $\PanitchpakdiHyperconvexInjectivityUp$ carrier is a finite $\BHist$ packet
$$
P=(M,H,E,I,K,R,T,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the common $\MetricSpaceUp$ source row whose point and distance requests are inspected by all downstream rows.
\item $H$ is the $\HyperconvexMetricUp$ ball-family row, including displayed closed balls, real radii, pairwise compatibility, and the finite intersection handoff.
\item $E$ is the finite injective-extension row, recording a displayed one-step metric extension boundary over the same source rather than a selected host hull point.
\item $I$ is the $\IsometricEmbeddingUp$ handoff row, keeping the source-to-extension distance-preserving route visible before any extension consumer is admitted.
\item $K$ is the $\CompleteMetricUp$ seal row attached to the same metric source and read only after the ball-family and embedding rows have been displayed.
\item $R$ is the $\RealUp$ radius and distance row used by the ball-family inequalities, extension budgets, and isometric comparison.
\item $T$ records componentwise $\hsame$ transport for the metric source, hyperconvex row, finite-extension row, isometric handoff, complete-metric seal, real-radius row, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from metric observations through ball-family compatibility, extension admissibility, isometric embedding handoff, complete-metric sealing, and real-radius readback.
\item $Q$ is the $\Pkg$ provenance over $\PanitchpakdiHyperconvexInjectivityUp$, $\HyperconvexMetricUp$, $\InjectiveEnvelopeMetricUp$, $\IsometricEmbeddingUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PanitchpakdiHyperconvexInjectivityUp}$ row.
\end{enumerate}
The carrier has no coordinate for arbitrary ball-family intersections, choice-selected injective hull points, quotient completion representatives, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Panitchpakdi hyperconvex injectivity NameCert obligations]
\label{thm:panitchpakdi-hyperconvex-injectivity-namecert-obligations}
For every accepted $\PanitchpakdiHyperconvexInjectivityUp$ carrier $P=(M,H,E,I,K,R,T,C,Q,N)$, the local $\NameCert_{\PanitchpakdiHyperconvexInjectivityUp}$ surface consists exactly of carrier admission for the displayed packet, common-source exposure through $M$, hyperconvex finite ball-family exposure through $H$, finite injective-extension exposure through $E$, isometric embedding handoff through $I$, complete-metric sealing through $K$, real-radius and distance readback through $R$, componentwise $\hsame$ transport through $T$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $Q$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:panitchpakdi-hyperconvex-injectivity-carrier}. The packet exposes exactly the metric source $M$, hyperconvex ball-family row $H$, finite extension row $E$, isometric handoff $I$, complete-metric seal $K$, real-radius row $R$, and structural rows $T,C,Q,N$.

Read the ball-family side first. The row $H$ imports only the finite hyperconvex route of \autoref{ch:concrete-instances-hyperconvex-metric-namecert}: displayed closed balls, real radii, pairwise compatibility, and the finite intersection handoff over the common metric source.

Read the extension side next. The row $E$ records the finite injective-extension boundary and is consumed through the injective-envelope neighbour of \autoref{ch:concrete-instances-injective-envelope-metric-namecert}; the row $I$ keeps the isometric embedding handoff visible before the extension is treated as distance preserving.

Finally inspect $K,R,T,C,Q,N$. The complete-metric and real-radius rows seal the same displayed route, while transport, replay, provenance, and local naming preserve the packet without adding a selected hull point, quotient completion representative, arbitrary-family intersection, choice selector, or ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\PanitchpakdiHyperconvexInjectivityUp$ read that treats finite hyperconvex ball data as an injective metric-extension boundary must first expose the displayed metric source, hyperconvex ball-family row, finite extension row, isometric embedding handoff, complete-metric seal, real-radius row, transport, replay, provenance, and local naming.}

\independenceWitness{hyperconvex-metric, injective-envelope-metric, isometric-embedding, metric, completemetric, real: no listed sibling supplies the whole packet $P=(M,H,E,I,K,R,T,C,Q,N)$, because each sibling contributes only a metric source, ball-intersection handoff, envelope route, embedding route, completion seal, or real-radius seal while this chapter binds the Panitchpakdi injectivity boundary across all six faces.}

\closureat{PanitchpakdiHyperconvexInjectivityUp}{seedStr}

\begin{closurestatus}{\PanitchpakdiHyperconvexInjectivityUp}
  \constructivestory{A $\PanitchpakdiHyperconvexInjectivityUp$ packet is generated from a displayed MetricSpace source, HyperconvexMetric finite ball-family row, finite injective-extension row, IsometricEmbedding handoff, CompleteMetric seal, Real radius and distance rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:panitchpakdi-hyperconvex-injectivity-carrier} and \autoref{thm:panitchpakdi-hyperconvex-injectivity-namecert-obligations} seed the finite boundary between hyperconvex ball-intersection data and injective metric-extension reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, arbitrary-family intersections, choice-selected injective hull points, quotient completions, countable choice, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, hyperconvex ball-family exposure, finite extension exposure, isometric handoff, complete-metric sealing, Real radius readback, transport, replay, provenance, and local naming for the same packet.}
\end{closurestatus}
