\chapter{A Concrete Naming Certificate for $\MartingaleQuadraticVariationUp$}
\label{ch:concrete-instances-martingale-quadratic-variation-namecert}
\origin{human}

Following Doob and Meyer, the $\MartingaleQuadraticVariationUp$ packet records the finite predictable-bracket data attached to an inspected martingale prefix. It sits over the adapted martingale surface of \autoref{ch:concrete-instances-martingale-namecert}, the random-variable endpoint rows of \autoref{ch:concrete-instances-randomvar-namecert}, the probability source of \autoref{ch:concrete-instances-probspace-namecert}, the conditional-expectation route of \autoref{ch:concrete-instances-condexp-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the bracket as displayed finite BEDC rows; it does not identify quotient stochastic processes, complete a measure space, select limiting variation, or import ambient $\RealUp$ completeness.

\begin{definition}[Martingale quadratic-variation carrier]
\label{def:martingale-quadratic-variation-carrier}
A $\MartingaleQuadraticVariationUp$ carrier is a finite $\BHist$ packet
$$
Q=(M,F,X,D,B,A,E,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $M$ is the consumed $\MartingaleUp$ adapted-sequence packet over a displayed $\ProbSpaceUp$ source.
\item $F$ is the finite filtration and visibility row inherited from the martingale packet.
\item $X$ is the finite endpoint family $X_0,\ldots,X_n$ of $\RandomVarUp$ rows read through $M$.
\item $D$ is the finite difference family $\Delta X_k=X_k-X_{k-1}$ recorded as displayed random-variable endpoint rows for $1\leq k\leq n$.
\item $B$ is the predictable-bracket row whose $k$th face is read through the conditional-expectation packet as the displayed previous-time expectation of the squared difference row.
\item $A$ is the accumulated bracket ledger, built by finite prefix summation of the displayed faces of $B$.
\item $E$ stores the $\CondExpUp$ rows used to read each previous-time predictable face.
\item $H$ stores componentwise $\hsame$ transport for the martingale row, filtration row, endpoint family, difference rows, bracket faces, accumulated ledger, conditional-expectation rows, replay, provenance, and local naming data.
\item $C$ stores displayed $\Cont$ replay for opening the martingale prefix, reading a difference row, reading the predictable face, and appending it to the accumulated ledger.
\item $P$ is the $\Pkg$ provenance over $\MartingaleUp$, $\RandomVarUp$, $\ProbSpaceUp$, $\CondExpUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MartingaleQuadraticVariationUp}$ row.
\end{enumerate}
The carrier is a finite predictable-bracket packet over the inspected martingale prefix. It stores no almost-sure quotient equality of processes, no completed-measure representative, no selected infinite bracket limit, and no $\RealUp$ completeness principle.
\end{definition}

\begin{theorem}[Martingale quadratic-variation predictable bracket]
\label{thm:martingale-quadratic-variation-predictable-bracket}
For every accepted $\MartingaleQuadraticVariationUp$ carrier $Q$, each bracket face in $B$ is admitted only through the displayed predecessor filtration row, the adjacent martingale endpoints in $X$, the difference row in $D$, and the corresponding $\CondExpUp$ row in $E$. The accumulated row $A$ may append only those admitted predictable faces, so a bracket consumer cannot replace the finite route by an almost-sure process equality, a completed-measure argument, a selected limiting variation, or ambient real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:martingale-quadratic-variation-carrier}. The rows $M,F,X,D,B,A,E$ are explicit coordinates of the accepted packet, and the predictable face is listed only beside the previous-time filtration data and the corresponding conditional-expectation row.

Use the martingale finite-prefix and finite-suffix restrictions from \autoref{thm:martingale-finite-prefix-restriction} and \autoref{thm:martingale-finite-suffix-restriction-carrier}. They ensure the bracket read is made on an inspected finite time spine, with the same consumed $\NameCert_{\RandomVarUp}$ and $\NameCert_{\CondExpUp}$ rows as the parent martingale packet.

Finally apply componentwise transport through $H$ and replay through $C$. These rows preserve the displayed difference and predictable-face coordinates while adding no carrier slot for quotient process equality, completed measures, selected limits, or $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Martingale quadratic-variation ledger]
\label{thm:martingale-quadratic-variation-ledger}
The local $\NameCert_{\MartingaleQuadraticVariationUp}$ surface for the carrier of \autoref{def:martingale-quadratic-variation-carrier} consists exactly of carrier admission for $Q$, predictable-face admission through $\CondExpUp$, finite accumulation of the bracket faces, componentwise classifier transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$. No obligation row outside $M,F,X,D,B,A,E,H,C,P,N$ is part of the packet.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:martingale-quadratic-variation-carrier}. The martingale row, filtration row, endpoint family, difference family, bracket faces, accumulated ledger, conditional-expectation rows, transport, replay, provenance, and local name are the whole displayed tuple.

Predictable-face admission is supplied by \autoref{thm:martingale-quadratic-variation-predictable-bracket}. The same theorem fixes the finite accumulation boundary for $A$: every appended face first passes through the martingale prefix and the conditional-expectation route.

The structural rows $H,C,P,N$ then name the packet without adding semantic coordinates. Therefore the NameCert ledger has no hidden almost-sure quotient equality row, no measure-completion row, no selected-limit row, and no ambient completeness row.
\end{proof}

\falsifiablePrediction{Every $\MartingaleQuadraticVariationUp$ bracket-facing read factors through a displayed finite martingale prefix, adjacent random-variable endpoint rows, finite difference rows, predictable $\CondExpUp$ faces, accumulated bracket ledger, componentwise transport, replay, provenance, and local naming.}

\independenceWitness{martingale, randomvar, condexp, probspace, real: $\MartingaleQuadraticVariationUp$ is not bijective to any listed sibling because it jointly stores martingale prefix data, difference rows, predictable conditional-expectation faces, an accumulated bracket ledger, structural transport, replay, provenance, and local naming.}

\closureat{MartingaleQuadraticVariationUp}{seedStr}

\begin{closurestatus}{\MartingaleQuadraticVariationUp}
  \constructivestory{A $\MartingaleQuadraticVariationUp$ packet is generated from a finite Martingale row, filtration row, random-variable endpoints, difference rows, predictable bracket faces, accumulated bracket ledger, conditional-expectation rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:martingale-quadratic-variation-carrier}, \autoref{thm:martingale-quadratic-variation-predictable-bracket}, and \autoref{thm:martingale-quadratic-variation-ledger} seed the finite predictable-bracket packet over martingale prefixes and conditional-expectation faces.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close almost-sure quotient equality, measure completion, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, predictable-face admission, finite accumulation, componentwise transport, replay, provenance, and local naming for BEDC.Derived.MartingaleQuadraticVariationUp.}
\end{closurestatus}
