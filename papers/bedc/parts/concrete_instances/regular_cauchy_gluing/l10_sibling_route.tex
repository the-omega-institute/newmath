\begin{theorem}[Regular Cauchy gluing L10 sibling route]
\label{thm:regular-cauchy-gluing-l10-sibling-route}
Every accepted $\RegularCauchyGluingUp$ packet reads its local gluing route
through the L10 sibling lattice before it exports a NameCert row. The route
first consumes the $\DyadicRatCoreUp$ tolerance rows of
\autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\StreamNameUp$
finite observation windows of
\autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$
regular readbacks of \autoref{ch:concrete-instances-regseqrat-namecert}, the
terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and
the Cauchy reindexing handoff of
\autoref{ch:concrete-instances-cauchy-reindexing-namecert}.

Thus the gluing packet exports only a local finite NameCert route over the
displayed dyadic, stream, regular-rational, Real-seal, and reindexing rows. It
does not expose quotient stream equality, selected representatives, host real
equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-gluing-carrier}. Its
visible rows are two local regular windows, the common dyadic ledger, the
finite observation row, the glued regular readback, the Real seal, and the
transport, replay, provenance, and naming rows.

Apply \autoref{thm:regular-cauchy-gluing-namecert-obligations}. The local
NameCert surface must read exactly those rows, with the dyadic ledger,
StreamName windows, RegSeqRat readback, and Real seal appearing before the
certificate is exported.

Use \autoref{thm:regular-cauchy-gluing-cauchy-reindexing-handoff} for the
reindexing component. It identifies the same dyadic, stream, and regular
readback rows as a $\CauchyReindexingUp$-compatible route.

Thus the dependency on \autoref{ch:concrete-instances-cauchy-reindexing-namecert}
is consumed before the $\RegSeqRatUp$ readback and terminal $\RealUp$ seal are
available to the gluing certificate.

The exclusions are inherited from the carrier inventory and the overlap
stability theorem. Since no coordinate stores quotient stream equality,
selected representatives, host real equality, choice, or ambient completeness,
the L10 sibling route remains a finite local NameCert route.
\end{proof}

\begin{theorem}[Regular Cauchy gluing RegSeqRat route]
\label{thm:regular-cauchy-gluing-regseqrat-route}
Every glued readback row of an accepted $\RegularCauchyGluingUp$ packet must pass through the existing $\RegSeqRatUp$ sibling surface of \autoref{ch:concrete-instances-regseqrat-namecert} and the $\CauchyReindexingUp$ handoff of \autoref{ch:concrete-instances-cauchy-reindexing-namecert} before the terminal $\RealUp$ seal is visible. The local obligations of \autoref{thm:regular-cauchy-gluing-namecert-obligations} therefore expose a regular rational readback and a reindexing route before any Real-facing consumer may read the glued value.
\end{theorem}
\begin{proof}
Start from the local L10 route of \autoref{thm:regular-cauchy-gluing-l10-sibling-route}. It already orders the dyadic ledger, finite StreamName windows, $\RegSeqRatUp$ readback, $\CauchyReindexingUp$ handoff, and terminal $\RealUp$ seal before the gluing NameCert row is exported.

Apply the local obligations of \autoref{thm:regular-cauchy-gluing-namecert-obligations}. The glued readback row is one of the displayed certificate rows, so it cannot be consumed without the same regular rational surface and structural support rows.

Use \autoref{thm:regular-cauchy-gluing-cauchy-reindexing-handoff}. The reindexing handoff keeps the dyadic, stream, and regular readback data aligned before the terminal Real seal appears. Thus a Real-facing read factors through RegSeqRat and CauchyReindexing rather than bypassing them.
\end{proof}

\begin{theorem}[Regular Cauchy gluing pasting nonescape]
\label{thm:regular-cauchy-gluing-pasting-nonescape}
Every accepted pasting consumer of a $\RegularCauchyGluingUp$ packet must read the finite route of \autoref{def:regular-cauchy-gluing-carrier}, satisfy the local obligations of \autoref{thm:regular-cauchy-gluing-namecert-obligations}, and pass through the L10 sibling route of \autoref{thm:regular-cauchy-gluing-l10-sibling-route} before it exports any NameCert row. In particular, the consumer must display the $\DyadicRatCoreUp$ tolerance rows, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\CauchyReindexingUp$ handoff, and terminal $\RealUp$ seal.

Consequently no such pasting consumer exports quotient stream equality, selected representatives, host real equality, countable choice, or ambient $\RealUp$ completeness as a coordinate of the $\RegularCauchyGluingUp$ read.
\end{theorem}
\begin{proof}
Project the accepted carrier in \autoref{def:regular-cauchy-gluing-carrier}. Its displayed route already contains the common dyadic ledger, finite StreamName observations, glued RegSeqRat readback, Real seal, and the support rows for transport, replay, provenance, and naming.

Apply \autoref{thm:regular-cauchy-gluing-namecert-obligations}. A pasting consumer can export a local NameCert row only by preserving exactly those displayed rows, so the consumer has no separate entry point around the dyadic, stream, regular readback, or seal coordinates.

Apply \autoref{thm:regular-cauchy-gluing-l10-sibling-route}. The L10 route orders the same rows through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchyReindexingUp$, and $\RealUp$ before export.

The refused objects are absent from the carrier and absent from the sibling route. Thus quotient stream equality, selected representatives, host real equality, countable choice, and ambient completeness cannot be repacked as a pasting consumer of the accepted $\RegularCauchyGluingUp$ packet.
\end{proof}

\begin{theorem}[Regular Cauchy gluing separator-use route]
\label{thm:regular-cauchy-gluing-separator-use-route}
Every accepted $\RegularCauchyGluingUp$ consumer that exports an apartness read or a located-comparison read must first pass through the $\RegularSeqApartnessSeparatorUp$ route of \autoref{ch:concrete-instances-regular-seq-apartness-separator-namecert}. Ordinary gluing consumers still export only the finite dyadic, stream, $\RegSeqRatUp$, $\CauchyReindexingUp$, and $\RealUp$ seal rows named by \autoref{thm:regular-cauchy-gluing-l10-sibling-route}.
\end{theorem}
\begin{proof}
Start with the L10 sibling route of \autoref{thm:regular-cauchy-gluing-l10-sibling-route}. It exposes the dyadic tolerance rows, finite stream windows, regular-rational readback, Cauchy reindexing handoff, and terminal Real seal before the local gluing NameCert row is exported.

For a consumer that reads only gluing data, those rows exhaust the available surface by \autoref{thm:regular-cauchy-gluing-pasting-nonescape}. The carrier has no coordinate for apartness, located comparison, quotient equality, selected representatives, choice, or ambient completeness.

If the same consumer asks for apartness or located comparison, apply the separator sibling dependency of \autoref{thm:regular-seq-apartness-separator-sibling-dependency}. That route requires the stream, regular-rational, dyadic, Real-seal, located-cut, and apartness-field rows to be displayed through $\RegularSeqApartnessSeparatorUp$ before such a read is available.

Thus the gluing packet itself remains the ordinary finite route, while apartness-facing or located-comparison-facing reads factor through the separator chapter rather than being smuggled into the gluing carrier.
\end{proof}

\begin{theorem}[Regular Cauchy gluing completion consumer route]
\label{thm:regular-cauchy-gluing-completion-gluing-consumer}
Every Bishop-completion or Real-completion consumer of an accepted $\RegularCauchyGluingUp$ packet must read the same finite L10 sibling route before exporting a gluing $\NameCert$ row. In particular, the consumer first exposes the $\DyadicRatCoreUp$ tolerance rows, the $\StreamNameUp$ finite observation windows, the $\RegSeqRatUp$ regular readback, the $\CauchyReindexingUp$ handoff, and the terminal $\RealUp$ seal. The exported gluing certificate has no coordinate for quotient stream equality, selected representatives, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the L10 sibling route of \autoref{thm:regular-cauchy-gluing-l10-sibling-route}. It already orders the dyadic ledger, stream windows, regular-rational readback, Cauchy reindexing handoff, and terminal Real seal before any local gluing $\NameCert$ row is exported.

Apply the RegSeqRat route of \autoref{thm:regular-cauchy-gluing-regseqrat-route}. A completion-facing read of the glued value therefore cannot bypass the regular rational surface or the reindexing route on its way to the terminal $\RealUp$ seal.

Apply the pasting nonescape theorem, \autoref{thm:regular-cauchy-gluing-pasting-nonescape}. It keeps every gluing consumer inside the finite carrier route and excludes quotient stream equality, selected representatives, host real equality, countable choice, and ambient completeness as exportable coordinates.

Finally use the separator-use route of \autoref{thm:regular-cauchy-gluing-separator-use-route}. Apartness-facing or located-comparison-facing completion reads factor through the separator sibling; ordinary Bishop-completion and Real-completion gluing reads still expose only the dyadic, stream, regular-rational, reindexing, and Real-seal rows before the gluing $\NameCert$ is named.
\end{proof}

\begin{theorem}[Regular Cauchy gluing L10 sibling dependency]
\label{thm:regular-cauchy-gluing-l10-sibling-dependency}
Before any gluing consumer reads the local $\NameCert_{\RegularCauchyGluingUp}$ row, it must display the finite sibling dependency route through the $\RegSeqRatUp$ rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\StreamNameUp$ observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\DyadicRatCoreUp$ tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the completion-facing handoff of \autoref{ch:concrete-instances-bishop-real-completion-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The route may also be read by Bishop-completion consumers through the completion consumer theorem \autoref{thm:regular-cauchy-gluing-completion-gluing-consumer}, but it remains a finite dependency lattice rather than quotient sequence equality, selected representatives, countable choice, host real equality, or ambient real completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:regular-cauchy-gluing-l10-sibling-route}. It already orders the dyadic tolerance rows, stream observation windows, regular-rational readback, Cauchy reindexing handoff, and Real seal before the local gluing $\NameCert$ row is exported.

Apply \autoref{thm:regular-cauchy-gluing-completion-gluing-consumer}. A Bishop-completion or Real-completion consumer therefore reads the same finite row sequence before it can use the gluing packet as completion-facing data.

The dependency is finite because every cited sibling appears as a displayed row or handoff in the local route. No step asks for quotient sequence equality, a selected representative of a Cauchy class, countable choice over tails, host real equality, or an ambient completeness theorem.
\end{proof}
