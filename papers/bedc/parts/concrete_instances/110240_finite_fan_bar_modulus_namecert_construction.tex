\chapter{A Concrete Naming Certificate for $\FiniteFanBarModulusUp$}
\label{ch:concrete-instances-finite-fan-bar-modulus-namecert}
\origin{human}

Following Brouwer's fan theorem and Bishop--Bridges finite-bar analysis, the $\FiniteFanBarModulusUp$ packet records the finite carrier that turns admitted fan-bar rows and local modulus rows into a tolerance-only uniform-modulus ledger. It sits over the fan certificate of \autoref{ch:concrete-instances-fantheorem-namecert}, the compact metric finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational lower-bound fold of \autoref{ch:concrete-instances-probe-bundle-finite-minimum-fold-namecert}, the $\RatUp$ arithmetic source of \autoref{ch:concrete-instances-rat-namecert}, and the $\UniformModulusUp$ handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}. It realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} without importing open-cover compactness, selected infinite branches, countable choice, quotient function equality, selected limits, or ambient real completeness.

\begin{definition}[Finite fan-bar modulus carrier]
\label{def:finite-fan-bar-modulus-carrier}
A $\FiniteFanBarModulusUp$ carrier is a finite $\BHist$ packet
$$
F=(B,K,L,Q,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite fan-bar admission row, recording the fan, decidable bar classifier, finite depth bound, and finite branch-prefix witness window.
\item $K$ is the $\CompactMetricUp$ finite-probe row, aligning the bar prefixes with the compact finite-net probes consumed by the compact-uniform-continuity route.
\item $L$ is the local modulus row read from the $\ContinuousMapUp$ packet at each admitted bar or probe entry.
\item $Q$ is the positive $\RatUp$ radius and tolerance row, carrying the finitely many local radii and requested tolerances.
\item $R$ is the lower-bound modulus extraction row, obtained by primitive recursion over the finite fan-bar and probe bundle and by certified rational comparison in $Q$.
\item $U$ is the $\UniformModulusUp$ consumer row, exposing the extracted lower bound as a tolerance-only modulus ledger.
\item $H$ records componentwise $\hsame$ transport for the bar, compact-probe, local-modulus, rational, extraction, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
B,K,L,Q \longmapsto R \longmapsto U .
$$
\item $P$ is the $\Pkg$ provenance over $\FiniteFanBarModulusUp$, $\FanTheoremUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteFanBarModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,K,L,Q,R,U,H,C,P,N$. It has no coordinate for an open cover, a selected infinite branch, countable choice, quotient equality of maps, selected limits, a host compactness theorem, or ambient real completeness.
\end{definition}

\begin{theorem}[Finite fan-bar modulus NameCert obligations]
\label{thm:finite-fan-bar-modulus-namecert-obligations}
For every accepted $\FiniteFanBarModulusUp$ carrier $F=(B,K,L,Q,R,U,H,C,P,N)$, the local $\NameCert_{\FiniteFanBarModulusUp}$ surface consists exactly of carrier admission, finite bar admission through $B$, compact finite-probe alignment through $K$, local continuous-map modulus exposure through $L$, positive rational radius accounting through $Q$, lower-bound modulus extraction through $R$, $\UniformModulusUp$ handoff through $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

In particular, a compact-uniform-continuity consumer may read a uniform modulus from this packet only after the finite bar, compact-probe, local-modulus, rational-radius, and lower-bound extraction rows have all been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-fan-bar-modulus-carrier}. The tuple displays exactly the finite bar, compact-probe, local-modulus, rational-radius, lower-bound extraction, uniform-modulus, transport, replay, provenance, and local naming rows.

Finite bar admission is the row $B$. It supplies the decidable bar classifier and finite depth witness, so the argument ranges over displayed finite prefixes rather than selected infinite branches.

The compact and local-modulus data are $K,L,Q$. The compact-probe row aligns finite bar prefixes with compact finite-net probes, the continuous-map row supplies local moduli at those probes, and the rational row stores the positive local radii and tolerance requests.

Lower-bound extraction is the row $R$. It is obtained by primitive recursion over the finite bar and probe rows, using the positive rational comparisons in $Q$ to retain a certified lower bound for all admitted local radii.

Finally, $U,H,C,P,N$ expose, transport, replay, source, and name that extracted lower bound as a $\UniformModulusUp$ ledger. Since the carrier has no coordinate for open-cover compactness, branch selection, countable choice, quotient function equality, selected limits, host compactness, or ambient completeness, the displayed obligations exhaust the accepted packet.
\end{proof}

\begin{theorem}[Finite fan-bar lower-bound modulus extraction]
\label{thm:finite-fan-bar-lower-bound-modulus-extraction}
Every compact-uniform-continuity read of an accepted $\FiniteFanBarModulusUp$ packet factors through
$$
B,K,L,Q,R,U,H,C,P,N .
$$
The extracted row $R$ is the only lower-bound modulus source available to the $\UniformModulusUp$ consumer: it is computed from finite fan-bar admission, compact finite-probe alignment, local continuous-map moduli, and positive $\RatUp$ radius comparisons, and it exports no open-cover subcover, selected branch, selected limit, quotient map equality, or ambient compactness datum.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:finite-fan-bar-modulus-namecert-obligations}. They require every compact-uniform-continuity consumer to read $B,K,L,Q$ before the lower-bound row $R$ is available.

The extraction step is finite. The bar depth and compact probe rows determine the finite index family, and the positive rational ledger compares the finitely many local radii by primitive recursion to produce a certified lower bound.

Once $R$ is displayed, the uniform-modulus row $U$ may consume it as a tolerance-only ledger. The structural rows $H,C,P,N$ preserve and name the same route under componentwise transport and replay.

No excluded datum can be read from the carrier. Open-cover compactness, selected branches, selected limits, quotient map equality, countable choice, and ambient compactness are absent from $B,K,L,Q,R,U,H,C,P,N$.
\end{proof}

\closureat{FiniteFanBarModulusUp}{seedStr}

\begin{closurestatus}{\FiniteFanBarModulusUp}
  \constructivestory{A $\FiniteFanBarModulusUp$ packet is generated from finite fan-bar admission, compact finite-probe alignment, local ContinuousMap modulus rows, positive RatUp radius ledgers, lower-bound extraction, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-fan-bar-modulus-carrier}, \autoref{thm:finite-fan-bar-modulus-namecert-obligations}, and \autoref{thm:finite-fan-bar-lower-bound-modulus-extraction} seed the finite fan-bar lower-bound route consumed by compact-uniform-continuity and UniformModulus rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected infinite branches, countable choice, quotient function equality, selected limits, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite bar admission, compact finite-probe alignment, local modulus exposure, RatUp lower-bound extraction, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.FiniteFanBarModulusUp.}
\end{closurestatus}
