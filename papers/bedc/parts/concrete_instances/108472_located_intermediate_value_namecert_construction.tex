\chapter{A Concrete Naming Certificate for $\LocatedIntermediateValueUp$}
\label{ch:concrete-instances-located-intermediate-value-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\LocatedIntermediateValueUp$ packet records the finite bisection route used to locate a sign change on a real interval without selecting a root as host data. It sits over the real-name surface of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the located interval carrier of \autoref{ch:concrete-instances-locatedinterval-namecert}, and the nested interval route of \autoref{ch:concrete-instances-nested-interval-selection-namecert}. The packet names only endpoint rows, located sign-apartness observations, finite bisection data, regular Cauchy readback, transport, replay, provenance, and local naming obligations; it does not export trichotomy, open-cover compactness, quotient real equality, selected roots, or ambient $\RealUp$ completeness.

\begin{definition}[Located intermediate value carrier]
\label{def:located-intermediate-value-carrier}
A $\LocatedIntermediateValueUp$ carrier is a finite $\BHist$ packet
$$
I=(E,S,B,W,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the located interval endpoint row naming the two $\RealUp$ endpoint seals and their $\LocatedIntervalUp$ order witness.
\item $S$ is the located sign-apartness row recording finite negative and positive separation observations for the endpoint values.
\item $B$ is the bisection ledger row listing the inspected dyadic midpoint stages, the retained subinterval side, and the sign row used at each stage.
\item $W$ is the finite $\StreamNameUp$ window row inspected by the bisection ledger.
\item $R$ is the $\RegSeqRatUp$ readback row that turns the inspected windows and dyadic tolerances into regular rational observations.
\item $Q$ is the $\RealUp$ seal row exposed only after $E,S,B,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for $E,S,B,W,R,Q$ and for the replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located sign-change request through finite bisection and regular Cauchy readback.
\item $P$ is the $\Pkg$ provenance over $\LocatedIntermediateValueUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\NestedIntervalSelectionUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedIntermediateValueUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,S,B,W,R,Q,H,C,P,N$. The carrier has no coordinate for real trichotomy, open-cover compactness, quotient equality of real names, an externally selected zero, ambient $\RealUp$ completeness, Lean verification, or bridge credentials.
\end{definition}

\begin{theorem}[Located intermediate value NameCert obligations]
\label{thm:located-intermediate-value-namecert-obligations}
For every accepted $\LocatedIntermediateValueUp$ carrier $I=(E,S,B,W,R,Q,H,C,P,N)$, the local $\NameCert_{\LocatedIntermediateValueUp}$ surface consists exactly of endpoint admission, located sign-apartness rows, bisection ledger exposure, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-intermediate-value-carrier}. The analytic rows are the endpoint row $E$, the sign-apartness row $S$, the bisection ledger $B$, the finite window row $W$, the regular rational readback $R$, and the real seal $Q$.

The endpoint signs are read as located apartness data. They are not converted into a trichotomy principle, a global sign decision for every point, or a quotient equality test for real names.

The bisection ledger is finite. Each step records the dyadic midpoint, the inspected windows, and the retained side before the next row is read. The regular Cauchy readback $R$ is the only route from these finite windows to the terminal $\RealUp$ seal $Q$.

Finally $H,C,P,N$ transport, replay, source, and name the same rows. Since the packet contains no coordinate for open-cover compactness, selected roots, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Located intermediate value nested interval route]
\label{thm:located-intermediate-value-nested-interval-route}
Every accepted $\LocatedIntermediateValueUp$ carrier exposes its intermediate-value read through the finite bisection ledger $B$, the $\NestedIntervalSelectionUp$ dependency named in $P$, the finite window row $W$, the $\DyadicRatCoreUp$ tolerance discipline, the $\RegSeqRatUp$ readback row $R$, and the terminal $\RealUp$ seal $Q$. A downstream consumer must read those rows before it may cite the packet as a located intermediate-value route.
\end{theorem}
\begin{proof}
Read $P$ to identify the nested interval dependency and the dyadic tolerance source. The only displayed interval refinement data in the carrier is the ledger $B$, so a consumer has no alternate route to a candidate interval.

Read $W$ and $R$ next. The windows are finite, and the regular rational readback is the handoff that turns the bisection ledger into a Cauchy-facing observation.

The endpoint of the route is the seal row $Q$, transported by $H$ and replayed by $C$. The proof therefore uses finite bisection and regular Cauchy handoff only; it does not use trichotomy, open-cover compactness, quotient real equality, selected roots, or ambient $\RealUp$ completeness.
\end{proof}

\closureat{LocatedIntermediateValueUp}{seedStr}

\begin{closurestatus}{\LocatedIntermediateValueUp}
  \constructivestory{A $\LocatedIntermediateValueUp$ packet is generated from located interval endpoint rows, located sign-apartness rows, a finite bisection ledger, finite StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-intermediate-value-carrier}, \autoref{thm:located-intermediate-value-namecert-obligations}, and \autoref{thm:located-intermediate-value-nested-interval-route} seed the finite bisection packet consumed by constructive intermediate-value routes over located real intervals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, open-cover compactness, quotient real equality, selected roots, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sign-apartness exposure, bisection ledger exactness, finite StreamName windows, DyadicRatCore tolerance stability, RegSeqRat readback, RealUp sealing, transport, replay, provenance, and local naming for BEDC.Derived.LocatedIntermediateValueUp.}
\end{closurestatus}
