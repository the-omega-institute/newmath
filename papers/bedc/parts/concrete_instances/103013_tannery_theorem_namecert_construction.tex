\chapter{A Concrete Naming Certificate for $\TanneryTheoremUp$}
\label{ch:concrete-instances-tannery-theorem-namecert}
\origin{human}

Following Bishop and Bridges and the classical theorem of Tannery, the $\TanneryTheoremUp$ packet records the finite dominated-exchange route for a double-indexed real row whose pointwise limit row may be summed after a displayed majorant has supplied tail control. It sits over the real-valued series surface of $\RealSeriesUp$ in \autoref{ch:concrete-instances-realseries-namecert}, the absolute-convergence route of $\AbsoluteConvergenceUp$ in \autoref{ch:concrete-instances-absolute-convergence-namecert}, the majorant discipline of $\WeierstrassMTestUp$ in \autoref{ch:concrete-instances-weierstrassmtest-namecert}, the rational readback row of $\RegSeqRatUp$ in \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window interface of $\StreamNameUp$ in \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of $\DyadicRatCoreUp$ in \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of $\RealUp$ in \autoref{ch:concrete-instances-real-namecert}. The packet names a finite constructive dominated exchange; it does not import host infinite sums, quotient stream equality, countable choice, dominated convergence as an ambient theorem, or ambient real completeness.

\begin{definition}[Tannery theorem carrier]
\label{def:tannery-theorem-carrier}
A $\TanneryTheoremUp$ carrier is a finite $\BHist$ packet
$$
K=(A,L,M,W,Q,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the double-indexed real row. It records the inspected entries $a_{n,k}$ only through finite real-valued windows and the partial-sum surfaces allowed by $\RealSeriesUp$.
\item $L$ is the pointwise limit row. It records the displayed limiting term $l_k$ for each inspected column together with the finite convergence window that compares $a_{n,k}$ to $l_k$.
\item $M$ is the dominating majorant route. It records the absolute majorant row, the summable majorant handoff, and the domination checks that bind $A$ and $L$ to $\AbsoluteConvergenceUp$ and $\WeierstrassMTestUp$.
\item $W$ is the finite $\StreamNameUp$ window family. It stores the row windows, column windows, diagonal comparison windows, and finite cutoffs used by the exchange.
\item $Q$ is the $\RegSeqRatUp$ readback row for the same real entries, limit entries, majorant entries, and partial sums named by $A,L,M,W$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger. It stores the requested precision, tail split, finite head budget, domination slack, and endpoint tolerance used by the exchange.
\item $E$ is the $\RealUp$ endpoint seal. It is available only after the displayed finite row sum, limit-row sum, majorant route, readback, and dyadic ledger have been read.
\item $H$ stores componentwise $\hsame$ transport for $A,L,M,W,Q,D,E$ and for the replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from the double-indexed row and pointwise limit row through the majorant route, finite windows, rational readback, dyadic ledger, and endpoint seal.
\item $P$ is the $\Pkg$ provenance over $\TanneryTheoremUp$, $\RealSeriesUp$, $\AbsoluteConvergenceUp$, $\WeierstrassMTestUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\TanneryTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,L,M,W,Q,D,E,H,C,P,N$. It has no coordinate for a host exchange of limits and sums, a selected infinite row sum, quotient stream equality, countable choice, an ambient dominated convergence theorem, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Tannery theorem NameCert obligations]
\label{thm:tannery-theorem-namecert-obligations}
For every accepted $\TanneryTheoremUp$ carrier
$$
K=(A,L,M,W,Q,D,E,H,C,P,N),
$$
the local $\NameCert_{\TanneryTheoremUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the double-indexed real row, pointwise limit row, dominating majorant route, finite window family, rational readback, dyadic ledger, real endpoint seal, transport, replay, provenance, and local naming rows;
\item pointwise-window exactness, requiring each displayed comparison between $a_{n,k}$ and $l_k$ to pass through $A,L,W,Q,D$ before it can be used in a finite head estimate;
\item dominated-row exactness, requiring $M$ to display the absolute majorant row, the summable majorant handoff, and the domination checks for every inspected entry of $A$ and $L$;
\item finite-exchange accounting, requiring $W$ and $D$ to split each requested tolerance into a finite head comparison and a dominated tail comparison;
\item $\RegSeqRatUp$ readback, requiring $Q$ to read the same rows and windows before any $\RealUp$ endpoint seal is exported;
\item $\RealUp$ seal nonescape, requiring $E$ to remain downstream of $A,L,M,W,Q,D$ and to export no host exchange theorem, selected infinite sum, quotient stream equality, countable choice, ambient dominated convergence theorem, Lean verification, or bridge datum;
\item componentwise transport, displayed replay, provenance, and local naming for the same route through $H,C,P,N$.
\end{enumerate}
No Tannery dominated-exchange obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted packet $K=(A,L,M,W,Q,D,E,H,C,P,N)$. The carrier exposes exactly the double-indexed row, pointwise limit row, majorant route, window family, readback, ledger, endpoint seal, and structural rows.

The pointwise part is finite at every read. A requested column comparison must pass through the row data $A$, the limit data $L$, the finite windows $W$, the rational readback $Q$, and the tolerance ledger $D$ before it contributes to the finite head estimate.

The dominated part is also displayed. The row $M$ records the majorant, its summable handoff, and the domination checks; the rows $W$ and $D$ record the cutoff and tolerance split used to separate the finite head from the dominated tail.

Finally $Q,E,H,C,P,N$ read back, seal, transport, replay, source, and name the same route. Since no excluded exchange theorem, selected sum, quotient, choice, verification, or bridge coordinate appears in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Tannery theorem dominated row handoff]
\label{thm:tannery-theorem-dominated-row-handoff}
Let $K=(A,L,M,W,Q,D,E,H,C,P,N)$ be an accepted $\TanneryTheoremUp$ carrier. Every dominated row comparison exported by the packet factors through
$$
A,L \longmapsto M \longmapsto W,D \longmapsto Q \longmapsto E,H,C,P,N .
$$
Thus the majorant route and dyadic tolerance ledger determine the finite handoff from pointwise row convergence to the endpoint-facing series read, not a host dominated convergence theorem, selected infinite sum, quotient stream equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with an endpoint-facing dominated row comparison. The carrier has no direct coordinate for exchanging a limit with an infinite sum, so the comparison first reads the displayed row data $A$ and the pointwise limit data $L$.

The majorant route is then forced. The row $M$ displays the absolute majorant and summable handoff, while $W$ and $D$ provide the finite window cutoff, tolerance split, head comparison, and tail budget.

The row $Q$ reads back the same data as regular rational observations. Only after this readback can $E$ seal the endpoint-facing real row, and $H,C,P,N$ transport, replay, source, and name the route.

Since the carrier offers no host exchange theorem, selected sum, quotient equality, choice object, or ambient completeness coordinate, every dominated row comparison is exhausted by the displayed factorization.
\end{proof}

\begin{theorem}[Tannery theorem real-series exchange boundary]
\label{thm:tannery-theorem-real-series-exchange-boundary}
For every accepted $\TanneryTheoremUp$ carrier, any $\RealSeriesUp$ or $\RealUp$ consumer of the exchange endpoint must read the double-indexed row, pointwise limit row, dominating majorant route, finite stream windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, endpoint seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row of \autoref{def:tannery-theorem-carrier}. The exchange boundary exports no host infinite sum, no unrecorded interchange of limit and summation, no quotient stream equality, no countable choice, no ambient dominated convergence theorem, no Lean verification, and no standard bridge.
\end{theorem}
\begin{proof}
Let a series or real endpoint consumer read the seal row $E$. By the carrier definition, $E$ is downstream of the row data $A,L$, the majorant route $M$, the finite windows $W$, the readback $Q$, and the dyadic ledger $D$.

Apply \autoref{thm:tannery-theorem-namecert-obligations}. Pointwise-window exactness binds the finite head comparisons, dominated-row exactness binds the majorant route, and finite-exchange accounting binds the tolerance split before the endpoint seal can be read.

Apply \autoref{thm:tannery-theorem-dominated-row-handoff}. Every dominated comparison reaches the endpoint only through the displayed route from $A,L$ to $E,H,C,P,N$.

Therefore the consumer receives exactly the finite dominated-exchange packet. It cannot read a host infinite sum, unrecorded interchange theorem, quotient stream equality, countable choice, ambient dominated convergence theorem, Lean verification, or standard bridge from the seal.
\end{proof}

\falsifiablePrediction{Every $\TanneryTheoremUp$ endpoint-facing read factors through a double-indexed real row, pointwise limit row, dominating majorant route, finite StreamName windows, RegSeqRat readback, DyadicRatCore tolerance ledger, RealUp endpoint seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{realseries, absolute-convergence, weierstrassmtest, regseqrat, streamname, dyadicratcore, real: $\TanneryTheoremUp$ is not bijective to any listed sibling because it jointly carries double-indexed row data, pointwise limit data, a dominating majorant route, finite exchange windows, regular rational readback, dyadic tolerance accounting, endpoint sealing, transport, continuation, provenance, and local naming.}

\closureat{TanneryTheoremUp}{seedStr}

\begin{closurestatus}{\TanneryTheoremUp}
  \constructivestory{A $\TanneryTheoremUp$ packet is generated from a double-indexed real row, a pointwise limit row, a dominating majorant route, finite StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealUp endpoint seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:tannery-theorem-carrier}, \autoref{thm:tannery-theorem-namecert-obligations}, \autoref{thm:tannery-theorem-dominated-row-handoff}, and \autoref{thm:tannery-theorem-real-series-exchange-boundary} seed the finite dominated-exchange route over the Real and Completion spine.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host infinite sums, unrecorded interchange of limit and summation, quotient stream equality, countable choice, ambient dominated convergence, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, pointwise-window exactness, dominated-row exactness, finite-exchange accounting, RegSeqRat readback, Real seal nonescape, and structural transport for BEDC.Derived.TanneryTheoremUp.}
\end{closurestatus}
