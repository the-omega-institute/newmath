\chapter{A Concrete Naming Certificate for $\BioRealityQSixLeaveOneCodonBoundaryUp$}
\label{ch:concrete-instances-bioreality_q_six_leave_one_codon_boundary-namecert}
\origin{ai}

The $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet names the finite boundary behavior of the claim h1.leave.one.codon.out. The displayed observation panel has $0$ anchor rows, $5$ boundary rows, $8$ predictable rows, and high-coverage closure count $8$. The recovery readings are exact on this panel: the recovery rate is $1.0$, and the recovery rate above threshold is also $1.0$.

This construction is intentionally local. It does not introduce a codon table, a p-value, a stochastic biological model, or an unobserved list of omitted codons. The only public content is the finite row inventory and the two recovery readings stated in the packet. The NameCert task is therefore not to explain why a biological mechanism holds, but to preserve the displayed finite boundary surface under name transport.

\section{Carrier}
\label{sec:bioreality-q-six-leave-one-codon-boundary-carrier}

\begin{definition}[Leave-one codon boundary carrier]
\label{def:bioreality-q-six-leave-one-codon-boundary-carrier}
A $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier is a finite tuple
$$
\begin{gathered}
B=(A,D,P,H,R,T,N),
\end{gathered}
$$
with the following visible rows.
\begin{itemize}
\item $A$ is the anchor row and records anchor count $0$.
\item $D$ is the boundary row and records boundary count $5$.
\item $P$ is the predictable-row ledger and records predictable count $8$.
\item $H$ is the high-coverage closure row and records closure coverage count $8$.
\item $R$ is the recovery row and records recovery rate $1.0$.
\item $T$ is the threshold recovery row and records recovery rate above threshold $1.0$.
\item $N$ is the local naming row for $\BioRealityQSixLeaveOneCodonBoundaryUp$.
\end{itemize}
The tuple has no coordinate for a hidden anchor, an omitted codon list, a host probability law, a p-value, a biological fitness surface, or a mechanism outside the displayed finite panel.
\end{definition}

The carrier keeps the zero-anchor reading separate from the boundary inventory. This matters because the $5$ boundary rows are not licensed by an unseen anchor source: they are read directly from $D$. Likewise, the two appearances of the number $8$ occupy different rows. The predictable count $8$ belongs to $P$, while the high-coverage closure count $8$ belongs to $H$.

\section{NameCert Surface}
\label{sec:bioreality-q-six-leave-one-codon-boundary-namecert-surface}

\begin{definition}[Local certificate surface]
\label{def:bioreality-q-six-leave-one-codon-boundary-namecert-surface}
For an accepted carrier $B=(A,D,P,H,R,T,N)$, the local $\NameCert_{\BioRealityQSixLeaveOneCodonBoundaryUp}$ surface consists exactly of the seven public reads
$$
\begin{aligned}
A &\mapsto 0,\\
D &\mapsto 5,\\
P &\mapsto 8,\\
H &\mapsto 8,\\
R &\mapsto 1.0,\\
T &\mapsto 1.0,\\
N &\mapsto \BioRealityQSixLeaveOneCodonBoundaryUp.
\end{aligned}
$$
A history is accepted at this surface only when all seven rows are present with these displayed values and no extra public row is used to discharge the carrier name.
\end{definition}

\begin{lemma}[Anchor exclusion]
\label{lem:bioreality-q-six-leave-one-codon-boundary-anchor-exclusion}
Every accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier exposes no anchor-supported boundary route.
\end{lemma}

\begin{proof}
The only anchor row in the carrier is $A$, and $A$ records anchor count $0$. The boundary inventory is read from $D$, which records $5$ boundary rows. Since the tuple has no additional coordinate from which an anchor can be projected, any accepted boundary read is independent of anchor support.
\end{proof}

\begin{lemma}[Finite boundary inventory]
\label{lem:bioreality-q-six-leave-one-codon-boundary-finite-inventory}
Every accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier presents a finite inventory with $5$ boundary rows and $8$ predictable rows.
\end{lemma}

\begin{proof}
The boundary count is read only from $D$, and $D$ records $5$. The predictable count is read only from $P$, and $P$ records $8$. No row in the tuple supplies an unbounded enumerator, a hidden codon table, or an external sampling procedure. Thus the inventory exposed by the carrier is finite and has exactly the displayed counts.
\end{proof}

\begin{lemma}[Coverage row is not a second predictable ledger]
\label{lem:bioreality-q-six-leave-one-codon-boundary-coverage-separated}
In an accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier, the high-coverage closure count $8$ is a separate public read from the predictable count $8$.
\end{lemma}

\begin{proof}
The predictable count is read from $P$, while the high-coverage closure count is read from $H$. The two rows record the same numeral $8$, but the NameCert surface keys them by role. Therefore equality of the displayed numerals does not merge the rows or permit one row to substitute for the other.
\end{proof}

\begin{theorem}[Leave-one codon boundary recovery certificate]
\label{thm:bioreality-q-six-leave-one-codon-boundary-recovery-certificate}
For every accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier, the displayed boundary panel closes with high-coverage count $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$.
\end{theorem}

\begin{proof}
The high-coverage closure row $H$ records closure coverage count $8$. The recovery row $R$ records recovery rate $1.0$, and the threshold recovery row $T$ records recovery rate above threshold $1.0$. These rows are coordinates of the accepted tuple $B=(A,D,P,H,R,T,N)$, and the local NameCert surface permits the corresponding reads only through those coordinates. Hence every accepted carrier returns exactly the three displayed recovery readings and no stronger biological, probabilistic, or asymptotic conclusion.
\end{proof}

\section{Falsifiability and Independence}
\label{sec:bioreality-q-six-leave-one-codon-boundary-falsifiability}

\falsifiablePrediction{Recomputing the same finite leave-one codon boundary panel must return anchor count $0$, boundary count $5$, predictable count $8$, closure coverage count $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$. A mismatch in any one of these six displayed quantities falsifies this $\BioRealityQSixLeaveOneCodonBoundaryUp$ instance.}

\independenceWitness{The $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet is not interchangeable with a generic codon-boundary or recovery carrier. It jointly requires zero anchors, five boundary rows, eight predictable rows, a separate high-coverage closure count of eight, exact recovery, exact above-threshold recovery, and the local carrier name. Removing any one of these rows changes the available NameCert reads.}

\section{Provenance Field}
\label{sec:bioreality-q-six-leave-one-codon-boundary-provenance}

The provenance field records that the displayed finite panel was verified at 2026-06-05T22:50:33+00:00. This timestamp is not an additional mathematical hypothesis. It identifies the audit time for the stored counts and rates; the kernel content remains the seven-row tuple and its role-keyed NameCert surface.

\section{Closure Status}
\label{sec:bioreality-q-six-leave-one-codon-boundary-closure-status}

\closureat{BioRealityQSixLeaveOneCodonBoundaryUp}{seedStr}
\begin{closurestatus}{\BioRealityQSixLeaveOneCodonBoundaryUp}
  \constructivestory{A $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet is generated from a finite leave-one codon boundary tuple with anchor count $0$, boundary count $5$, predictable count $8$, high-coverage closure count $8$, recovery rate $1.0$, recovery rate above threshold $1.0$, and a local naming row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{Closed for the named finite leave-one codon boundary inventory, its zero-anchor condition, its separated predictable and coverage rows, and its two displayed recovery readings.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a biological mechanism, stochastic fitness law, empirical codon table, p-value, external codon list, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires a checked finite carrier for the seven displayed rows and a bridge that validates the stored counts and rates against the provenance field.}
\end{closurestatus}