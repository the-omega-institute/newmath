\chapter{A Concrete Naming Certificate for $\MetaCICConfluenceDiamondLedgerUp$}
\label{ch:concrete-instances-metacic-confluence-diamond-ledger-namecert}
\origin{ai}

The $\MetaCICConfluenceDiamondLedgerUp$ packet realizes the confluence audit map of \autoref{ch:metacic-confluence-audit-map} as finite NameCert data. It packages typed-window exposure, residual-substitution boundary rows, local-join ledgers, bounded checker rows, obstruction retention, transport, replay, provenance, and local naming before any confluence handoff can be read. It consumes the MetaCIC critical path of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the candidate-mediated strong-normalization route of \autoref{ch:concrete-instances-candidate-mediated-sn-namecert}, the critical-pair boundary of \autoref{ch:concrete-instances-metacic-confluence-critical-pair-boundary-namecert}, the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, and the local-join ledger of \autoref{ch:concrete-instances-metacic-local-join-ledger-namecert}. The packet is a finite confluence-diamond ledger, not a closed Church--Rosser theorem or an unconditional substitution-compatibility proof.

\begin{definition}[MetaCIC confluence diamond ledger carrier]
\label{def:metacic-confluence-diamond-ledger-carrier}
A $\MetaCICConfluenceDiamondLedgerUp$ carrier is a finite $\BHist$ packet
$$
G=(T,R,J,B,O,X,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row exposing the finite MetaCIC source window, redex shape, and bounded context in which the confluence handoff is requested.
\item $R$ is the residual-substitution row naming the displayed substitution-boundary data that the diamond route must consume.
\item $J$ is the local-join row recording the finite candidate joins and common-reduct witnesses accepted for the inspected critical pairs.
\item $B$ is the bounded-checker row recording the finite checker budget for the residual diamond route.
\item $O$ is the obstruction row retaining the substitution-compatibility and closed-diamond boundary names that are not discharged by this packet.
\item $X$ is the confluence handoff row exposed only after $T,R,J,B,O$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the typed window, residual-substitution row, local-join row, bounded checker, obstruction row, handoff row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a confluence-diamond request through $T,R,J,B,O$ and then to $X$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICConfluenceDiamondLedgerUp$, $\MetaCICCriticalPathUp$, $\CandidateMediatedSNUp$, $\MetaCICConfluenceCriticalPairBoundaryUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\MetaCICLocalJoinLedgerUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICConfluenceDiamondLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $T,R,J,B,O,X,H,C,P,N$. It has no coordinate for an unconditional parallel diamond, closed Church--Rosser theorem, global substitution-compatibility proof, unbounded checker, hidden quotient, or host-side confluence oracle.
\end{definition}

\begin{theorem}[MetaCIC confluence diamond ledger NameCert obligations]
\label{thm:metacic-confluence-diamond-ledger-namecert-obligations}
For every accepted $\MetaCICConfluenceDiamondLedgerUp$ carrier $G=(T,R,J,B,O,X,H,C,P,N)$, the local $\NameCert_{\MetaCICConfluenceDiamondLedgerUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed typed-window, residual-substitution, local-join, bounded-checker, obstruction, handoff, transport, replay, provenance, and local naming rows;
\item typed-window exposure, requiring the MetaCIC source window, redex shape, and bounded context to be displayed before any residual row is consumed;
\item residual-substitution exposure, requiring $R$ to be read before local joins or bounded checker rows can be trusted;
\item local-join accounting, requiring $J$ to list the finite candidate joins and common-reduct witnesses used by the inspected critical pairs;
\item bounded-checker accounting, requiring $B$ to record the finite checker budget and to consume the same $T,R,J$ rows;
\item obstruction retention, requiring $O$ to keep the substitution-compatibility and closed-diamond boundary names visible rather than silently discharging them;
\item handoff non-escape, requiring $X$ to expose only the displayed confluence-diamond ledger and not an unconditional Church--Rosser theorem;
\item componentwise transport, displayed replay, package provenance, and local naming through $H,C,P,N$.
\end{enumerate}
The future Lean TasteGate for this packet must supply both a Nontrivial instance, separating nonempty ledger data from the empty carrier, and a FieldFaithful instance, asserting that the classifier reads every displayed field $T,R,J,B,O,X,H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.MetaCICConfluenceDiamondLedgerUp.MetaCICConfluenceDiamondLedgerNameCertObligations}
\begin{proof}
Project the carrier of \autoref{def:metacic-confluence-diamond-ledger-carrier}. The projection exposes exactly the typed-window, residual-substitution, local-join, bounded-checker, obstruction, handoff, transport, replay, provenance, and local naming rows.

Read the confluence rows in order. The typed window $T$ fixes the inspected MetaCIC source window; the residual row $R$ records the substitution boundary; the local-join row $J$ records candidate joins; and the bounded checker $B$ accounts for the finite checking budget.

The obstruction row $O$ remains visible before the handoff $X$. Therefore the packet can display a confluence-diamond ledger without claiming the global substitution-compatibility input or a closed Church--Rosser endpoint.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite route. A Nontrivial TasteGate instance must witness that accepted packets carry genuine ledger data, and a FieldFaithful instance must witness that the classifier cannot ignore any displayed row.
\end{proof}

\begin{theorem}[MetaCIC confluence diamond ledger handoff]
\label{thm:metacic-confluence-diamond-ledger-handoff}
Every accepted $\MetaCICConfluenceDiamondLedgerUp$ carrier that exposes a confluence handoff must display the typed-window, residual-substitution, local-join, bounded-checker, obstruction, transport, replay, provenance, and local NameCert rows on the same route
$$
T \longmapsto R \longmapsto J \longmapsto B \longmapsto O \longmapsto X \longmapsto H,C,P,N .
$$
Consequently the handoff is a finite ledger consumed by MetaCIC confluence routes, not an unconditional parallel diamond, not arbitrary closed-term Church--Rosser, not a global substitution-compatibility proof, and not a host confluence oracle.
\end{theorem}
\begin{proof}
Let $G=(T,R,J,B,O,X,H,C,P,N)$ be accepted. The handoff row $X$ is downstream of the typed-window, residual-substitution, local-join, bounded-checker, and obstruction rows by the carrier definition.

Use the NameCert obligations of \autoref{thm:metacic-confluence-diamond-ledger-namecert-obligations}. They force every confluence-facing reader to keep $T,R,J,B,O$ visible before $X$ can be consumed.

The structural rows then preserve the same route. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ do not add an unconditional diamond theorem, a closed Church--Rosser theorem, a global substitution-compatibility proof, or an unbounded checker.
\end{proof}

\begin{theorem}[MetaCIC confluence diamond ledger candidate join exhaustion]
\label{thm:metacic-confluence-diamond-ledger-candidate-join}
Every accepted $\MetaCICConfluenceDiamondLedgerUp$ carrier $G=(T,R,J,B,O,X,H,C,P,N)$ that exposes a common-reduct claim in $X$ must first expose the typed-window row $T$, residual-substitution row $R$, local-join row $J$, bounded-checker row $B$, and obstruction row $O$. Moreover, every common-reduct claim in $X$ is backed by a row of $J$ with the same typed window and the same residual-substitution boundary, while $O$ remains present for any substitution-compatibility boundary not discharged by that row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-confluence-diamond-ledger-carrier}. The handoff coordinate $X$ is not available before $T,R,J,B,O$ have been displayed.

Apply the NameCert obligations of \autoref{thm:metacic-confluence-diamond-ledger-namecert-obligations}. Typed-window exposure fixes $T$, residual-substitution exposure fixes $R$, local-join accounting forces $J$ to list the finite candidate joins, and bounded-checker accounting keeps those rows inside the finite checker budget $B$.

Apply \autoref{thm:metacic-confluence-diamond-ledger-handoff}. Any common-reduct read exported by $X$ follows the route through $T,R,J,B,O$, so its local-join witness must be a row of $J$ over the same typed window and residual-substitution boundary.

The obstruction row $O$ is preserved rather than consumed silently. Thus the candidate-join ledger exhausts the displayed common-reduct claims without turning missing substitution compatibility into a global Church--Rosser proof. The existing Lean TasteGate evidence for this chapter records the Nontrivial and FieldFaithful obligations that keep these displayed fields visible to the classifier.
\end{proof}

\falsifiablePrediction{Every accepted $\MetaCICConfluenceDiamondLedgerUp$ packet exposing a confluence handoff must display typed-window, residual-substitution, local-join, bounded-checker, obstruction, transport, replay, provenance, and NameCert rows.}

\independenceWitness{metaciccriticalpath, candidate-mediated-sn, metacic-confluence-critical-pair-boundary, metacic-residual-substitution-boundary, metacic-local-join-ledger: $\MetaCICConfluenceDiamondLedgerUp$ is not bijective to any listed sibling because it jointly carries typed-window, residual-substitution, local-join, bounded-checker, obstruction-retention, handoff, transport, replay, provenance, and local naming rows.}

\closureat{MetaCICConfluenceDiamondLedgerUp}{seedStr}

\begin{closurestatus}{\MetaCICConfluenceDiamondLedgerUp}
  \constructivestory{A $\MetaCICConfluenceDiamondLedgerUp$ packet is generated from typed-window exposure, residual-substitution boundary data, local-join rows, bounded-checker rows, obstruction retention, a confluence handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-confluence-diamond-ledger-carrier}, \autoref{thm:metacic-confluence-diamond-ledger-namecert-obligations}, \autoref{thm:metacic-confluence-diamond-ledger-handoff}, and \autoref{thm:metacic-confluence-diamond-ledger-candidate-join} seed the finite confluence-diamond ledger consumed by MetaCIC confluence routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, unconditional parallel diamond, arbitrary closed-term Church--Rosser, global substitution compatibility, unbounded checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, typed-window exposure, residual-substitution exposure, local-join accounting, bounded-checker accounting, obstruction retention, handoff non-escape, transport, replay, provenance, local naming, Nontrivial TasteGate evidence, and FieldFaithful TasteGate evidence for BEDC.Derived.MetaCICConfluenceDiamondLedgerUp.}
\end{closurestatus}
