\chapter{A Concrete Naming Certificate for $\SubjectReductionDischargeLedgerUp$}
\label{ch:concrete-instances-subjectreductiondischargeledger-namecert}
\origin{ai}

The $\SubjectReductionDischargeLedgerUp$ packet realizes the audit map of \autoref{ch:metacic-subject-reduction-obstruction} as a finite carrier for the four preservation obligations consumed by the $\SubjectReductionUp$ route of \autoref{ch:concrete-instances-subjectreduction-namecert}. It records the obligation rows, transport rows, continuation routes, package provenance, and local naming row as a concrete BEDC object, while keeping closed subject reduction outside the exported surface.

\begin{definition}[SubjectReductionDischargeLedger carrier]
\label{def:subject-reduction-discharge-ledger-carrier}
A $\SubjectReductionDischargeLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(B,A,LD,PD,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the beta-substitution preservation row, matching $\mathrm{BetaSubstitutionPreservation}$;
\item $A$ is the application-argument stability row, matching $\mathrm{AppArgTypeStable}$;
\item $LD$ is the lambda-domain subject-reduction row, matching $\mathrm{LamDomainSubjectReduction}$;
\item $PD$ is the Pi-domain subject-reduction row, matching $\mathrm{PiDomainSubjectReduction}$;
\item $R$ records the $\SubjectReductionUp$ route that consumes exactly $B,A,LD,PD$;
\item $H$ records componentwise $\hsame$ transports for the four obligation rows, the route row, provenance, and local naming;
\item $C$ records the displayed $\Cont$ replays by which consumers read the four obligations and the $\SubjectReductionUp$ route;
\item $P$ is the $\Pkg$ provenance over MetaCIC subject reduction, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\SubjectReductionDischargeLedgerUp}$ row.
\end{enumerate}
The carrier contains no unparameterised closed subject-reduction theorem, no host conversion principle, no hidden substitution theorem, no erased derivation semantics, and no quotient equality of MetaCIC terms.
\end{definition}

\begin{theorem}[SubjectReductionDischargeLedger obligations]
\label{thm:subject-reduction-discharge-ledger-obligations}
For the carrier of \autoref{def:subject-reduction-discharge-ledger-carrier}, the local $\NameCert_{\SubjectReductionDischargeLedgerUp}$ surface consists exactly of carrier admission for
$$
L=(B,A,LD,PD,R,H,C,P,N),
$$
four-row obligation exposure for $\mathrm{BetaSubstitutionPreservation}$, $\mathrm{AppArgTypeStable}$, $\mathrm{LamDomainSubjectReduction}$, and $\mathrm{PiDomainSubjectReduction}$, the $\SubjectReductionUp$ route row $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier $L$. The four obligation rows $B,A,LD,PD$ are the only rows that can feed the subject-reduction route.

Read the structural rows next. The transport row $H$ keeps the four obligations, the route row, provenance, and local name componentwise stable, while the continuation row $C$ records the finite replay routes used by downstream consumers.

Finally inspect $P$ and $N$. The package row records provenance for the same displayed rows, and the local certificate names no further coordinate. Hence the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger SubjectReduction handoff]
\label{thm:subject-reduction-discharge-ledger-subjectreduction-handoff}
Every accepted $\SubjectReductionDischargeLedgerUp$ carrier hands its MetaCIC preservation data to $\SubjectReductionUp$ only through the route row $R$ and the four obligation rows $B,A,LD,PD$. The handoff may be transported by $H$, replayed through $C$, and accounted for by $P,N$, but it does not supply a closed subject-reduction theorem or an unrecorded fifth preservation premise.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:subject-reduction-discharge-ledger-obligations}. It identifies the accepted carrier with the four obligation rows, the subject-reduction route row, and the structural rows.

The route row $R$ is displayed as the only $\SubjectReductionUp$ consumer coordinate. Since $R$ consumes exactly $B,A,LD,PD$, any accepted handoff to \autoref{ch:concrete-instances-subjectreduction-namecert} passes through those four rows.

The remaining rows $H,C,P,N$ transport, replay, package, and name the same handoff. They do not add another preservation obligation, so the ledger has no route for a fifth premise or a closed theorem.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger non-escape]
\label{thm:subject-reduction-discharge-ledger-nonescape}
Every downstream read of an accepted $\SubjectReductionDischargeLedgerUp$ carrier factors through
$$
B,A,LD,PD,R,H,C,P,N.
$$
In particular, the finite ledger cannot be read as an unparameterised closed subject-reduction theorem, host conversion rule, hidden substitution preservation theorem, erased derivation semantics, quotient equality, or semantic MetaCIC interpreter.
\end{theorem}
\begin{proof}
Project the carrier and apply the obligation theorem. The only public preservation rows are $B,A,LD,PD$, and the only public subject-reduction consumer row is $R$.

The transport, continuation, package, and naming rows preserve access to the displayed packet but do not create new coordinates. Therefore a consumer can read the ledger only through $B,A,LD,PD,R,H,C,P,N$.

The excluded closed or host-side principles have no row in the packet. A request for one of them therefore remains outside the accepted carrier rather than becoming a hidden route through the ledger.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger TasteGate boundary]
\label{thm:subject-reduction-discharge-ledger-taste-gate-boundary}
A future TasteGate surface for $\SubjectReductionDischargeLedgerUp$ must expose the carrier only through the single field projection
$$
L=(B,A,LD,PD,R,H,C,P,N).
$$
The projection admits the beta-substitution, application-argument, lambda-domain, Pi-domain, route, transport, continuation, provenance, and local naming rows as one displayed ledger packet; it does not expose a closed subject-reduction theorem, host conversion row, hidden substitution theorem, or erased derivation semantics.
\end{theorem}
\begin{proof}
Use the carrier definition and the non-escape theorem. They identify the ledger with the displayed tuple and exclude every closed or host-side principle from the accepted packet.

The TasteGate-facing projection can therefore read the ledger only as $L$. Splitting the read into an unrecorded preservation theorem or adding a host conversion coordinate would require a field outside $B,A,LD,PD,R,H,C,P,N$, which the carrier does not contain.
\end{proof}
\leanchecked{BEDC.Derived.SubjectReductionDischargeLedgerUp.TasteGate.SubjectReductionDischargeLedgerTasteGate\_single\_carrier\_alignment}

\begin{theorem}[SubjectReductionDischargeLedger field-faithful surface]
\label{thm:subject-reduction-discharge-ledger-field-faithful-surface}
The field-faithful row for a future $\SubjectReductionDischargeLedgerUp$ TasteGate must separate the displayed fields $B,A,LD,PD,R,H,C,P,N$. A consumer that preserves the ledger must preserve beta-substitution exposure, application-argument stability, lambda-domain subject-reduction exposure, Pi-domain subject-reduction exposure, the $\SubjectReductionUp$ route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming as distinct public rows.
\end{theorem}
\begin{proof}
Project $L$ and apply the obligation theorem. Each displayed coordinate has a different ledger role: four rows expose preservation obligations, one row is the route consumer, and four rows provide transport, replay, provenance, and naming.

Field faithfulness is the requirement that these roles are not identified by a TasteGate read. If two of the rows were collapsed, the handoff theorem could no longer state that $\SubjectReductionUp$ consumes exactly $B,A,LD,PD$ through $R$ while $H,C,P,N$ remain structural rows.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger nontrivial witness surface]
\label{thm:subject-reduction-discharge-ledger-nontrivial-witness-surface}
The nontriviality row for a future $\SubjectReductionDischargeLedgerUp$ TasteGate must distinguish at least two displayed ledger packets. One packet may vary the route row $R$ while preserving $B,A,LD,PD,H,C,P,N$; another may vary a structural row such as $C$ while preserving the four preservation obligations and route row. A TasteGate that identifies all such packets cannot witness the displayed ledger surface.
\end{theorem}
\begin{proof}
The carrier has separate coordinates for obligation data, route data, and structural replay data. Varying $R$ changes the $\SubjectReductionUp$ handoff while leaving the four preservation rows fixed.

Varying $C$ changes the displayed replay route while leaving the preservation obligations and route row fixed. These two changes affect different public coordinates of $L$.

The nontrivial witness row records that the ledger surface can observe those coordinate differences. A collapsed TasteGate would erase the distinction between obligation, route, and replay fields, contradicting the field-faithful surface.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger carrier exposure]
\label{thm:subject-reduction-discharge-ledger-carrier-exposure}
Every accepted $\SubjectReductionDischargeLedgerUp$ carrier exposes exactly the finite rows
$$
B,A,LD,PD,R,H,C,P,N.
$$
The public ledger surface consists of the four preservation rows, the $\SubjectReductionUp$ route, componentwise transport, displayed continuation replay, package provenance, and local naming; it contains no additional MetaCIC preservation coordinate.
\end{theorem}
\begin{proof}
Begin with the carrier definition \autoref{def:subject-reduction-discharge-ledger-carrier}. It lists $B,A,LD,PD,R,H,C,P,N$ as the full finite packet.

Apply the obligation theorem \autoref{thm:subject-reduction-discharge-ledger-obligations}. Its carrier-admission and four-row exposure clauses identify the public preservation surface with $B,A,LD,PD$ and the subject-reduction route with $R$.

The handoff theorem \autoref{thm:subject-reduction-discharge-ledger-subjectreduction-handoff} and non-escape theorem \autoref{thm:subject-reduction-discharge-ledger-nonescape} leave only $H,C,P,N$ as structural replay, provenance, and naming rows. Hence the accepted carrier exposes exactly the displayed ledger rows.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger transport replay]
\label{thm:subject-reduction-discharge-ledger-transport-replay}
For every accepted $\SubjectReductionDischargeLedgerUp$ carrier, the transport row $H$ and continuation row $C$ transport and replay only the four preservation rows $B,A,LD,PD$ and the $\SubjectReductionUp$ route row $R$, together with the provenance and local naming rows needed to authenticate that finite route.
\end{theorem}
\begin{proof}
Project the carrier using \autoref{def:subject-reduction-discharge-ledger-carrier}. The transport coordinate $H$ is declared componentwise over the four obligation rows, the route row, provenance, and local naming.

Use \autoref{thm:subject-reduction-discharge-ledger-obligations} to read the continuation coordinate $C$. It replays the displayed obligations and the $\SubjectReductionUp$ route, with no fifth preservation row and no closed subject-reduction row.

Finally apply \autoref{thm:subject-reduction-discharge-ledger-subjectreduction-handoff} and \autoref{thm:subject-reduction-discharge-ledger-nonescape}. They force every replay to remain inside $B,A,LD,PD,R,H,C,P,N$, so $H$ and $C$ cannot transport or replay an off-ledger preservation premise.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger provenance exactness]
\label{thm:subject-reduction-discharge-ledger-provenance-exactness}
For an accepted $\SubjectReductionDischargeLedgerUp$ carrier, the rows $P$ and $N$ account exactly for the finite MetaCIC, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance of $B,A,LD,PD,R,H,C$. They do not add closed subject reduction, host conversion, hidden substitution preservation, erased derivation semantics, or quotient equality of MetaCIC terms.
\end{theorem}
\begin{proof}
Read $P$ and $N$ from the carrier definition. The package row names the finite provenance dependencies, and the local naming row names the same displayed ledger.

Apply \autoref{thm:subject-reduction-discharge-ledger-obligations}. The obligation surface already accounts for the four preservation rows, route row, transport row, continuation row, package row, and name row.

Use the handoff theorem and non-escape theorem to close the provenance boundary. Since all downstream reads factor through the same displayed packet, $P$ and $N$ authenticate the finite ledger without creating a closed subject-reduction theorem or any excluded host-side principle.
\end{proof}

\begin{theorem}[SubjectReductionDischargeLedger route exhaustion]
\label{thm:subject-reduction-discharge-ledger-route-exhaustion}
Every accepted $\SubjectReductionDischargeLedgerUp$ carrier whose public rows are
$$
B,A,LD,PD,R,H,C,P,N
$$
routes $\SubjectReductionUp$ consumption only through the four obligation rows $B,A,LD,PD$ and the route row $R$. The rows $H,C,P,N$ may transport, replay, authenticate, and name the same finite route, but they cannot create closed subject reduction, host conversion, hidden substitution preservation, erased derivation semantics, quotient equality, or a fifth preservation premise.
\end{theorem}
\begin{proof}
Project the displayed tuple from \autoref{def:subject-reduction-discharge-ledger-carrier}. Its only preservation coordinates are $B,A,LD,PD$, and its only $\SubjectReductionUp$ consumer coordinate is $R$.

Apply \autoref{thm:subject-reduction-discharge-ledger-obligations} and \autoref{thm:subject-reduction-discharge-ledger-subjectreduction-handoff}. These identify $R$ as the row that consumes exactly the four obligation rows and account for the handoff through the local certificate surface.

It remains to exhaust the structural rows. The non-escape theorem \autoref{thm:subject-reduction-discharge-ledger-nonescape} confines every downstream read to the displayed packet, while \autoref{thm:subject-reduction-discharge-ledger-provenance-exactness} makes $P$ and $N$ exact provenance and naming rows for that same packet.

Thus $H,C,P,N$ can only transport, replay, authenticate, and name the finite route already determined by $B,A,LD,PD,R$. None of those rows supplies an additional preservation premise or any of the excluded closed or host-side principles.
\end{proof}

\closureat{SubjectReductionDischargeLedgerUp}{seedStr}

\begin{closurestatus}{\SubjectReductionDischargeLedgerUp}
  \constructivestory{A $\SubjectReductionDischargeLedgerUp$ packet is generated from beta-substitution, application-argument, lambda-domain, and Pi-domain obligation rows, a $\SubjectReductionUp$ route row, componentwise $\hsame$ transports, displayed $\Cont$ replays, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:subject-reduction-discharge-ledger-carrier}, \autoref{thm:subject-reduction-discharge-ledger-obligations}, \autoref{thm:subject-reduction-discharge-ledger-subjectreduction-handoff}, and \autoref{thm:subject-reduction-discharge-ledger-nonescape} bind the finite subject-reduction discharge ledger and its handoff to $\SubjectReductionUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not prove BetaSubstitutionPreservation, AppArgTypeStable, LamDomainSubjectReduction, PiDomainSubjectReduction, closed subject reduction, host conversion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, four-row exposure, subject-reduction route handoff, transport stability, continuation replay, provenance, and non-escape for the displayed ledger.}
\end{closurestatus}
