\chapter{A Concrete Naming Certificate for $\LocatedIntervalFanBarrierUp$}
\label{ch:concrete-instances-located-interval-fan-barrier-namecert}
\origin{human}

Following Bishop and Bridges on compact intervals and finite fan barriers, the $\LocatedIntervalFanBarrierUp$ packet records the finite boundary object that prevents an L10 located-interval cover route from being read as open-cover compactness. It sits beside the located interval source of \autoref{ch:concrete-instances-locatedinterval-namecert}, the closed bounded interval fan surface of \autoref{ch:concrete-instances-closed-bounded-interval-fan-namecert}, the finite fan compactness packet of \autoref{ch:concrete-instances-finite-fan-compactness-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window source of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the barrier row used before finite-cover handoff; it does not import open-cover compactness, selected branches, quotient streams, or ambient $\RealUp$ completeness.

\begin{definition}[Located interval fan barrier carrier]
\label{def:located-interval-fan-barrier-carrier}
A $\LocatedIntervalFanBarrierUp$ carrier is a finite $\BHist$ packet
$$
B=(I,K,F,D,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\LocatedIntervalUp$ source row carrying located endpoint comparison and interval-membership data.
\item $K$ is the closed bounded interval fan row that supplies the finite binary-prefix and bar boundary.
\item $F$ is the finite fan compactness row read only as a bounded fan of cover probes, not as an open-cover compactness oracle.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare barrier prefixes and interval endpoints.
\item $S$ is the $\StreamNameUp$ window row for endpoint names, barrier prefixes, and finite cover probes.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to those windows and dyadic tolerance checks.
\item $E$ is the terminal $\RealUp$ seal reached only after $I,K,F,D,S,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for interval, fan, dyadic, stream, rational, real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located-interval request through the finite fan barrier and then through $D,S,Q,E$.
\item $P$ is the $\Pkg$ provenance over $\LocatedIntervalFanBarrierUp$, $\LocatedIntervalUp$, $\ClosedBoundedIntervalFanUp$, $\FiniteFanCompactnessUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedIntervalFanBarrierUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,K,F,D,S,Q,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected infinite branches, quotient streams, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.LocatedIntervalFanBarrierUp}

\begin{theorem}[Located interval fan barrier NameCert obligations]
\label{thm:located-interval-fan-barrier-namecert-obligations}
For every accepted $\LocatedIntervalFanBarrierUp$ carrier
$$
B=(I,K,F,D,S,Q,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedIntervalFanBarrierUp}$ surface consists exactly of carrier admission for the displayed rows, located interval admission through $I$, closed bounded interval fan admission through $K$, finite fan compactness admission through $F$, dyadic tolerance admission through $D$, stream-window admission through $S$, regular rational readback through $Q$, terminal real sealing through $E$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-interval-fan-barrier-carrier}. Its public rows are exactly $I,K,F,D,S,Q,E,H,C,P,N$, so every local obligation is attached to one displayed row.

The located interval row is read before the fan boundary rows, and the fan boundary rows are read before dyadic tolerance, finite stream, regular rational, and real-seal rows. Thus a finite-cover consumer cannot skip from an interval request directly to a completed cover theorem.

Transport, replay, provenance, and local naming preserve the same packet route. Since no excluded coordinate occurs in the carrier, the NameCert surface cannot call on open-cover compactness, selected branches, quotient streams, countable choice, or ambient real completeness.
\end{proof}

\begin{theorem}[Located interval fan barrier finite-cover handoff]
\label{thm:located-interval-fan-barrier-finite-cover-handoff}
Every L10 finite-cover consumer of an accepted $\LocatedIntervalFanBarrierUp$ packet factors through the route
$$
\LocatedIntervalUp,\ \ClosedBoundedIntervalFanUp,\ \FiniteFanCompactnessUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
In carrier notation, every cover handoff first reads $I,K,F,D,S,Q,E$ and then the structural rows $H,C,P,N$. The handoff exports only the displayed finite barrier route and exports no open-cover compactness, branch selector, quotient stream equality, countable-choice row, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Begin with the located interval row $I$. It supplies the endpoint comparison and interval-membership surface consumed by the barrier.

The closed bounded interval fan row $K$ and finite fan compactness row $F$ then expose a finite prefix and probe boundary. These rows are barrier data: they bound the cover handoff before any real-facing conclusion is read.

The dyadic ledger $D$, stream windows $S$, regular rational readback $Q$, and terminal real seal $E$ are downstream of that barrier. The structural rows $H,C,P,N$ replay and name the same route. Therefore the finite-cover handoff remains inside the displayed packet and cannot export any excluded compactness or choice principle.
\end{proof}
\leanchecked{BEDC.Derived.LocatedIntervalFanBarrierUp.LocatedIntervalFanBarrierFiniteCoverHandoff}

\begin{theorem}[Located interval fan barrier obligation package]
\label{thm:located-interval-fan-barrier-obligation-package}
For every accepted $\LocatedIntervalFanBarrierUp$ carrier
$$
B=(I,K,F,D,S,Q,E,H,C,P,N),
$$
the rows $I,K,F,D,S,Q,E,H,C,P,N$ project the full local $\NameCert_{\LocatedIntervalFanBarrierUp}$ obligation surface. The surface contains carrier admission, located-interval admission, fan-barrier admission, finite-cover handoff, dyadic tolerance, StreamName window, regular-rational readback, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming obligations, and it contains no open-cover compactness, branch selector, quotient stream, countable-choice, or ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Project the displayed carrier of \autoref{def:located-interval-fan-barrier-carrier}. The first seven rows $I,K,F,D,S,Q,E$ are the only mathematical rows available to a cover-facing consumer: located interval data, closed bounded interval fan data, finite fan compactness data, dyadic tolerance data, stream-window data, regular rational readback, and terminal real sealing.

The structural rows then exhaust the remaining NameCert clauses. The row $H$ supplies componentwise transport, $C$ supplies displayed replay, $P$ supplies provenance, and $N$ supplies local naming for exactly the same packet.

Since every obligation is attached to one of the displayed rows, there is no residual coordinate from which open-cover compactness, a selected branch, quotient stream equality, countable choice, or ambient completeness could be read. Thus the carrier rows project the stated obligation package.
\end{proof}

\begin{theorem}[Located interval fan barrier dyadic-window induction]
\label{thm:located-interval-fan-barrier-dyadic-window-induction}
Every finite consumer induction over an accepted $\LocatedIntervalFanBarrierUp$ packet reads dyadic tolerance, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal only after the located interval row $I$ and finite fan rows $K,F$ have been exposed. In particular, no induction step may consume $D,S,Q,E$ as an initial cover premise independent of the located interval and finite barrier rows.
\end{theorem}
\begin{proof}
Use induction over the displayed finite row list of the carrier. The base segment consists of $I,K,F$: first the located interval row supplies endpoint and membership data, then the closed bounded interval fan row and finite fan compactness row expose the finite barrier boundary.

For the downstream segment, the next rows $D,S,Q,E$ are introduced only after that base segment has been displayed. The dyadic row $D$ compares barrier prefixes and endpoints, the window row $S$ names the finite observations, the readback row $Q$ records the regular rational values, and the seal row $E$ closes the real-facing boundary.

The structural rows $H,C,P,N$ preserve the same order because they transport, replay, source, and name the already displayed packet. Hence every finite row induction consumes dyadic tolerance, window, readback, and seal data downstream of $I,K,F$, and the finite-cover consumer cannot treat them as independent initial cover premises.
\end{proof}

\closureat{LocatedIntervalFanBarrierUp}{obligationStr}

\begin{closurestatus}{\LocatedIntervalFanBarrierUp}
  \constructivestory{A $\LocatedIntervalFanBarrierUp$ packet is generated from a located interval row, closed bounded interval fan row, finite fan compactness row, dyadic tolerance ledger, finite StreamName windows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:located-interval-fan-barrier-carrier}, \autoref{thm:located-interval-fan-barrier-namecert-obligations}, \autoref{thm:located-interval-fan-barrier-finite-cover-handoff}, \autoref{thm:located-interval-fan-barrier-obligation-package}, and \autoref{thm:located-interval-fan-barrier-dyadic-window-induction} bind the carrier, full NameCert obligation package, finite-cover handoff, and dyadic-window induction order for L10 located-interval finite-cover consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected branches, quotient streams, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Scoped closure requires checked carrier admission, fan-barrier admission, finite-cover handoff, dyadic tolerance, stream-window, regular-rational, real-seal, transport, replay, provenance, and local naming obligations for BEDC.Derived.LocatedIntervalFanBarrierUp.}
\end{closurestatus}
