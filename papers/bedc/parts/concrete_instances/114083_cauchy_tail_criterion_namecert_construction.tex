\chapter{A Concrete Naming Certificate for $\CauchyTailCriterionUp$}
\label{ch:concrete-instances-cauchy-tail-criterion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyTailCriterionUp$ packet records the finite tail-test route by which a Cauchy read is accepted before a regular rational or real-seal consumer may use it. It sits over the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the inspected modulus discipline of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the fast subsequence route of \autoref{ch:concrete-instances-fastcauchysubsequence-namecert}. The packet is a local L10 finite tail-test object: it does not select a limit, quotient streams, use countable choice, invoke trichotomy, compare host real values, or import ambient $\RealUp$ completeness.

\begin{definition}[Cauchy tail criterion carrier]
\label{def:cauchy-tail-criterion-carrier}
A $\CauchyTailCriterionUp$ carrier is a finite $\BHist$ packet
$$
T=(S,D,R,M,E,F,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ tail-window row listing the inspected source windows and the finite stage pairs used by the test.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the requested radius, comparison slack, and dyadic budget consumed by each inspected pair.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed windows and dyadic budgets.
\item $M$ is the $\CauchyModulusUp$ inspection row naming the stage threshold that licenses the tail test.
\item $E$ is the $\RealUp$ seal row visible only after the window, dyadic, readback, and modulus rows have been consumed.
\item $F$ is the $\FastCauchySubsequenceUp$ handoff row available only as a downstream finite witness that the same tail-test data can feed a fast regular subsequence route.
\item $B$ is the tail-test ledger, recording that every displayed pair of inspected tail windows beyond the threshold in $M$ is contained by the tolerance row $D$.
\item $H$ records componentwise $\hsame$ transport for $S,D,R,M,E,F,B$ and the structural support rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,D,M \longmapsto R \longmapsto E \longmapsto F \longmapsto B .
$$
\item $P$ is the $\Pkg$ provenance over $\CauchyTailCriterionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\CauchyModulusUp$, $\RealUp$, $\FastCauchySubsequenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyTailCriterionUp}$ row.
\end{enumerate}
Accepted carriers are compared only through these displayed rows. There is no coordinate for a selected limit, quotient Cauchy class, host real equality, countable-choice selector, trichotomy row, or ambient completeness principle.
\end{definition}

\begin{theorem}[Cauchy tail criterion NameCert obligations]
\label{thm:cauchy-tail-criterion-namecert-obligations}
For every accepted $\CauchyTailCriterionUp$ carrier $T=(S,D,R,M,E,F,B,H,C,P,N)$, the local $\NameCert_{\CauchyTailCriterionUp}$ surface consists exactly of carrier admission, $\StreamNameUp$ tail-window exposure through $S$, $\DyadicRatCoreUp$ tolerance accounting through $D$, $\RegSeqRatUp$ readback through $R$, inspected $\CauchyModulusUp$ threshold exposure through $M$, terminal $\RealUp$ seal delivery through $E$, $\FastCauchySubsequenceUp$ handoff through $F$, tail-test accountability through $B$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted packet. The carrier displays exactly the tail-window, dyadic tolerance, regular readback, inspected modulus, real seal, fast-subsequence handoff, tail-test ledger, transport, replay, provenance, and local naming rows.

The finite test is read through $S,D,M,B$. The row $S$ lists only inspected windows, $M$ names the displayed threshold, $D$ records the tolerance budget, and $B$ asserts the pairwise tail containment for those displayed rows.

The consumer-facing rows are downstream of that test. The regular rational row $R$ reads the same windows and dyadic budgets, while the real seal $E$ is exposed only after $R$ and the tail-test route have been projected.

Finally $F,H,C,P,N$ preserve and name the same route. The fast-subsequence row may consume the displayed tail-test data as finite evidence, but the carrier has no coordinate for a selected infinite subsequence, quotient stream equality, countable choice, host real equality, trichotomy, or ambient completeness. The listed rows therefore exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cauchy tail criterion Real seal handoff]
\label{thm:cauchy-tail-criterion-real-seal-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\CauchyTailCriterionUp$ carrier must read the finite route
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \CauchyModulusUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp
\longmapsto
\FastCauchySubsequenceUp
$$
through the displayed carrier rows before it may use the tail criterion as real-facing evidence. The handoff supplies only a finite tail-test packet; it does not supply a selected limit, quotient stream equality, host real comparison, countable-choice schedule, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $T=(S,D,R,M,E,F,B,H,C,P,N)$. Projection exposes the finite window row, dyadic tolerance row, inspected modulus row, regular readback row, real seal row, fast-subsequence handoff row, and tail-test ledger.

The tail criterion is available only after $S,D,M,B$ have been read. These rows bind every inspected pair of tail windows to a displayed modulus threshold and a displayed dyadic tolerance budget.

The regular rational and real-facing rows preserve that order. The row $R$ reads the same window and tolerance data, and the seal row $E$ is downstream of the regular readback rather than a host limit-selection operation.

The fast-subsequence handoff $F$ may consume the same finite evidence after the seal route is visible. Since $H,C,P,N$ only transport, replay, source, and name the carrier, no excluded principle remains available to a Real or completion consumer.
\end{proof}
\leanchecked{BEDC.Derived.CauchyTailCriterionUp.CauchyTailCriterionRealSealHandoff}

\closureat{CauchyTailCriterionUp}{seedStr}

\begin{closurestatus}{\CauchyTailCriterionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-tail-criterion-carrier}, \autoref{thm:cauchy-tail-criterion-namecert-obligations}, and \autoref{thm:cauchy-tail-criterion-real-seal-handoff} seed the finite tail-test route from displayed stream windows, dyadic tolerance budgets, and inspected modulus data to regular rational readback, Real seal delivery, and fast-subsequence handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, selected limits, quotient stream equality, host real comparison, countable choice, trichotomy, ambient $\RealUp$ completeness, public closure, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tail-window exposure, dyadic tolerance accounting, modulus-threshold exposure, regular readback, Real seal delivery, fast-subsequence handoff, tail-test accountability, transport, replay, provenance, and local naming for BEDC.Derived.CauchyTailCriterionUp.}
  \constructivestory{A $\CauchyTailCriterionUp$ packet is generated from StreamName tail windows, DyadicRatCore tolerance budgets, RegSeqRat readback, inspected CauchyModulus thresholds, a RealUp seal, a FastCauchySubsequence handoff, a finite tail-test ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
