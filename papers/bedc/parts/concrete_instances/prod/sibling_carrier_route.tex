The $\ProdUp$ pair carrier also records a finite-data sibling route through the endpoint, alternative, and spine interfaces already named by the concrete-instance lattice. Bool endpoints are read through \autoref{ch:concrete-instances-bool-namecert}, tagged alternatives through \autoref{ch:concrete-instances-sum-namecert}, and finite routing spines through \autoref{ch:concrete-instances-list-namecert}; the product packet consumes those rows as dependencies rather than as hidden host projections.

\begin{theorem}[Prod sibling carrier route]
\label{thm:prod-sibling-carrier-route}
Every accepted $\ProdUp$ pair carrier may consume $\BoolUp$ endpoint discrimination, $\SumUp$ tagged alternatives, and $\ListUp$ finite spine routing only as displayed finite-data sibling dependencies. The carrier exposes those rows through its product ledger, componentwise transport, continuation replay, provenance, and local $\NameCert$ row, and it does not replace them by host product extensionality or an unrecorded pairing oracle.
\end{theorem}
\begin{proof}
Read the pair carrier through the existing product ledger. Endpoint discrimination is supplied by the $\BoolUp$ sibling, so a branch decision entering the pair route is a displayed finite row.

Tagged alternatives are then supplied by the $\SumUp$ sibling. The pair carrier records which side of the product dependency is being consumed without replacing that tag by host-level case analysis.

Finally the finite spine route is supplied by $\ListUp$. Componentwise transport, continuation replay, provenance, and the local $\NameCert$ row preserve the same finite sibling surface, so the product carrier exports no hidden pairing oracle.
\end{proof}
