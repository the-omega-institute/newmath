Public $\ProdUp$ reads use the two displayed component projections before they enter the finite-data sibling lattice generated by \autoref{ch:concrete-instances-sum-namecert} and \autoref{ch:concrete-instances-list-namecert}. The product packet therefore contributes paired component ledgers, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any downstream consumer treats the data as a tagged branch or finite spine.
