\begin{theorem}[Prod Bool Option Sum List forward link]
\label{thm:prod-bool-option-sibling-forward-link}
The $\ProdUp$ componentwise carrier is the binary product node in the finite-data lattice whose projections are consumed alongside $\BoolUp$, $\OptionUp$, $\SumUp$, and $\ListUp$ through \autoref{ch:concrete-instances-bool-namecert}, \autoref{ch:concrete-instances-option-namecert}, \autoref{ch:concrete-instances-sum-namecert}, and \autoref{ch:concrete-instances-list-namecert}. Every product classifier read therefore remains a displayed finite-data route rather than an ambient tuple primitive.
\end{theorem}
\begin{proof}
Read the existing product component rows from the $\ProdUp$ carrier. They expose left and right projections through the componentwise history envelope, classifier fields, transport, continuation, provenance, and local naming rows.

The sibling finite-data routes supply the four adjacent surfaces. $\BoolUp$ distinguishes endpoint positions, $\OptionUp$ records optional component presence, $\SumUp$ separates tagged alternatives, and $\ListUp$ records the finite-spine discipline used by iterated product windows.

Since the product carrier exports no host tuple equality, selected projection, quotient product, or unrecorded recursion primitive, the forward link is exactly the displayed route from the product node to those sibling finite-data certificates.
\end{proof}
