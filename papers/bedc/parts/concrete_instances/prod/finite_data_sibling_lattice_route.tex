\begin{theorem}[Prod finite-data sibling lattice route]
\label{thm:prod-finite-data-sibling-lattice-route}
Every accepted $\ProdUp$ carrier consumes $\BoolUp$ endpoint discrimination, $\SumUp$ tagged alternatives, $\OptionUp$ absence and presence rows, and $\ListUp$ finite-spine rows through componentwise $\NameCert$ projection before exporting the ordered-pair certificate. The route cites \autoref{ch:concrete-instances-bool-namecert}, \autoref{ch:concrete-instances-sum-namecert}, \autoref{ch:concrete-instances-option-namecert}, and \autoref{ch:concrete-instances-list-namecert} as sibling finite-data surfaces, not as host product projections.
\end{theorem}
\begin{proof}
Start with the product finite-data sibling lattice, \autoref{thm:prod-finite-data-sibling-lattice}. It places product reads behind Bool endpoint tags, Sum branch partitions, Option possible-absence surfaces, and List finite transport before the ordered pair certificate is visible.

Apply the option-sum component route, \autoref{thm:prod-option-sum-component-sibling-route}. Component reads pass through the optional and tagged alternatives as displayed sibling rows rather than as host projections from a product object.

Finally use the public finite-data exit package. It exports the ordered pair through the displayed product history carrier, componentwise classifier, displayed-pair ledger, source $\NameCert$ rows, $\hsame$ transport, $\Cont$ continuation, and $\Pkg$ provenance. Thus the sibling lattice is consumed by componentwise $\NameCert$ projection and by no unrecorded tuple oracle.
\end{proof}
