The finite-data readback for $\ProdUp$ consumes the $\SumUp$ route of \autoref{ch:concrete-instances-sum-namecert} and the $\OptionUp$ route of \autoref{ch:concrete-instances-option-namecert} componentwise: left and right product coordinates retain their sibling finite-data witnesses, $\BoolUp$ tag discrimination keeps the endpoint cases separated, and the $\ListUp$ spine-readback route remains an external ledger rather than a hidden product coordinate.
