\begin{lemma}[Prod finite-data sibling dependency]
\label{lem:prod-finite-data-sibling-dependency}
The $\ProdUp$ finite-data route consumes its sibling packets only through displayed finite rows: $\BoolUp$ supplies tag discrimination for the two component projections, $\OptionUp$ supplies endpoint absence and presence discipline for partial component reads, $\SumUp$ supplies side-tag separation for coproduct-facing projections, and $\ListUp$ supplies finite-spine readback for iterated product windows. No sibling is read as an ambient equality, selector, quotient, or external recursion principle.
\end{lemma}
\begin{proof}
Project the product hub through its displayed finite-data rows. Boolean tags decide which component field is inspected, option endpoints record whether a component read is absent or present, sum tags keep left and right alternatives disjoint, and list spines enumerate the finite sequence of product windows consumed by readback.

These rows are all finite carrier coordinates already named by the product dossier. Transport and replay preserve the displayed rows componentwise, while provenance and the local NameCert only record the same finite route. Since no row stores quotient equality, selected data, or an external recursion principle, the sibling dependency remains confined to the displayed finite-data surface.
\end{proof}
