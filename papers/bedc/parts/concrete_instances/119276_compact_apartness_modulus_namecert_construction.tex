\chapter{A Concrete Naming Certificate for $\CompactApartnessModulusUp$}
\label{ch:concrete-instances-compact-apartness-modulus-namecert}
\origin{human}

Bishop--Bridges-style constructive compactness reads compact information through finite approximating data rather than through an arbitrary subcover principle. The $\CompactApartnessModulusUp$ packet names the modulus row that tells an apartness-sensitive consumer how fine a finite net must be before a positive separation request is stable. It sits between the compact finite profile of \autoref{ch:concrete-instances-compact-apartness-profile-namecert} and the located separation route of \autoref{ch:concrete-instances-compact-apartness-located-separation-namecert}, while retaining the $\RegSeqRatUp$ and $\RealUp$ readback rows needed by numerical consumers.

\begin{definition}[Compact apartness modulus carrier]
\label{def:compact-apartness-modulus-carrier}
A $\CompactApartnessModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(K,T,A,M,D,R,E,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row whose compact reads are exposed by finite windows or finite-net data.
\item $T$ is the $\TotallyBoundedUp$ finite-net row naming the selected mesh scale, centers, and coverage ledger.
\item $A$ is the apartness request row, recording the positive separation test that the modulus is asked to preserve.
\item $M$ is the modulus row: it sends an apartness request and a compact finite-net scale to a displayed dyadic tolerance.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger governing the mesh and lower-bound comparisons.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational modulus values generated by $M,D$.
\item $E$ is the terminal $\RealUp$ seal for the numerical separation bound read from $R$.
\item $S$ is the separated or located-separation handoff row consumed by $\CompactApartnessLocatedSeparationUp$.
\item $H$ records componentwise $\hsame$ transport for compact rows, finite-net rows, apartness requests, modulus rows, dyadic ledgers, readback rows, Real seals, separation handoff rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a consumer request through $K,T,A,M,D,R,E,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactMetricUp$, $\TotallyBoundedUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\CompactApartnessProfileUp$, $\CompactApartnessLocatedSeparationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactApartnessModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It contains no arbitrary open-cover compactness principle, selected subcover, apartness trichotomy, quotient completion equality, selected limit point, host equality, or ambient Real completeness assertion.
\end{definition}

\begin{theorem}[Compact apartness modulus NameCert obligations]
\label{thm:compact-apartness-modulus-namecert-obligations}
For every accepted $\CompactApartnessModulusUp$ carrier
$$
U=(K,T,A,M,D,R,E,S,H,C,P,N),
$$
the local $\NameCert_{\CompactApartnessModulusUp}$ surface consists exactly of carrier admission, compact finite-net exposure, apartness-request exposure, modulus-to-tolerance exposure, dyadic ledger readout, $\RegSeqRatUp$ readback, terminal $\RealUp$ sealing, separated handoff, componentwise transport, displayed replay, provenance, and local naming. A consumer may use the modulus only after $K,T,A,M,D,R,E,S$ have been displayed in the same finite packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-apartness-modulus-carrier}. The rows $K,T,A,M,D,R,E,S,H,C,P,N$ are the complete public inventory of the packet.

The compact and apartness rows are read first. The finite-net row $T$ supplies the mesh data for the compact source $K$, and the apartness row $A$ names the positive separation request that the modulus must control.

The numerical route is then forced through $M,D,R,E$. The modulus row produces only a displayed dyadic tolerance, the dyadic ledger checks it, the regular rational row reads it back, and the Real row seals the terminal numerical bound.

Finally $S,H,C,P,N$ hand the same finite route to separated consumers, transport it componentwise, replay it, record provenance, and name it locally. Since no row stores an arbitrary subcover, trichotomy, quotient equality, selected limit point, host equality, or ambient completeness theorem, those objects cannot become NameCert obligations.
\end{proof}

\begin{theorem}[Compact apartness modulus finite-net separation]
\label{thm:compact-apartness-modulus-finite-net-separation}
Every accepted $\CompactApartnessModulusUp$ finite-net separation read factors through
$$
K,T,A,M,D,R,E,S,H,C,P,N.
$$
The row $T$ supplies the finite net, $A$ supplies the apartness request, $M$ supplies the modulus-to-tolerance map, $D$ checks the dyadic tolerance, $R$ and $E$ provide the $\RegSeqRatUp$ and $\RealUp$ numerical readback, and $S$ is the only separation-facing handoff. Thus the modulus row is a finite compact-apartness coordinate, not an open-cover compactness theorem, an apartness trichotomy, quotient completion equality, selected limit data, host equality, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the finite-net row $T$ over the compact source $K$. A separation-facing consumer must first read the displayed centers, mesh scale, and coverage ledger rather than an arbitrary cover.

Read the apartness request through $A$ and the modulus through $M$. The modulus is only accepted as a row that turns the displayed apartness request and finite-net scale into a dyadic tolerance governed by $D$.

The numerical value remains internal to the finite route until $R$ and $E$ read it back as a regular rational name and terminal Real seal. Only after those rows are visible may $S$ pass the result to the located separation surface named in \autoref{ch:concrete-instances-compact-apartness-located-separation-namecert}.

The exclusions are coordinate exhaustion. The carrier has no row for open-cover compactness, trichotomy, quotient completion equality, selected limits, host equality, choice schedules, or ambient Real completeness, so the finite-net separation read cannot export them.
\end{proof}

\falsifiablePrediction{A compact-apartness consumer that claims a numerical separation modulus without displaying finite-net data, apartness request data, dyadic tolerance, RegSeqRat readback, Real sealing, and the separated handoff row is not a $\CompactApartnessModulusUp$ packet.}

\independenceWitness{compactapartnessprofile, compactapartnesslocatedseparation, totallybounded, dyadicratcore, regseqrat, real: the packet is not bijective to these siblings because it singles out the modulus-to-tolerance row $M$ and binds it to compact finite-net data, apartness requests, dyadic checking, numerical readback, Real sealing, separation handoff, transport, replay, provenance, and local naming in one carrier.}

\closureat{CompactApartnessModulusUp}{seedStr}

\begin{closurestatus}{\CompactApartnessModulusUp}
  \constructivestory{A $\CompactApartnessModulusUp$ packet is generated from compact finite-net data, apartness request rows, a modulus-to-tolerance row, dyadic tolerance ledgers, RegSeqRat readback, Real sealing, separated handoff data, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-apartness-modulus-carrier}, \autoref{thm:compact-apartness-modulus-namecert-obligations}, and \autoref{thm:compact-apartness-modulus-finite-net-separation} seed the finite compact-apartness modulus carrier and its NameCert obligation surface over displayed compact, finite-net, apartness, dyadic, RegSeqRat, Real, and separated handoff rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean theorem verification, bridge checking, open-cover compactness, selected subcover existence, apartness trichotomy, quotient completion equality, selected limit data, host equality, countable choice, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net exposure, apartness-request exposure, modulus-to-tolerance exposure, dyadic tolerance readout, RegSeqRat readback, Real sealing, separated handoff, transport, replay, provenance, and local naming for the same packet.}
\end{closurestatus}
