The accepted-declaration exposure row $A$ of $\KernelAcceptanceBoundaryUp$ is supplied by the $\KernelAcceptanceWitnessUp$ source route of \autoref{ch:concrete-instances-kernelacceptancewitness-namecert}, while the forbidden-ancestry refusal row $F$ is supplied by the $\AxiomQueryLedgerUp$ route of \autoref{ch:concrete-instances-axiomqueryledger-namecert}. This is a substantive sibling dependency: the carrier $Q=(G,K,A,E,L,F,H,C,P,N)$ cannot expose its local $\NameCert_{\KernelAcceptanceBoundaryUp}$ row $N$ until $A$ and $F$ have both been displayed, because $A$ names the declaration that crossed the kernel boundary and $F$ records the dependency query that keeps forbidden ancestry outside the accepted row. For the L10 Real and Completion path, a downstream selector such as \autoref{ch:concrete-instances-realcauchymodulusselector-namecert} may cite the boundary only after those two audit rows and the replay ledger $L$ have been displayed, so kernel acceptance remains an audit boundary rather than a Real completeness, selector existence, kernel self-correctness, compiler correctness, or semantic adequacy principle.
