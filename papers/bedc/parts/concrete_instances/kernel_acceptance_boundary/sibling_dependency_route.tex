The accepted-declaration exposure row of $\KernelAcceptanceBoundaryUp$ is supplied by \autoref{ch:concrete-instances-kernelacceptancewitness-namecert}, while the forbidden-ancestry refusal row is supplied by \autoref{ch:concrete-instances-axiomqueryledger-namecert}. This sibling route only states that the boundary packet reads those two displayed audit dependencies before local naming; it does not claim kernel self-correctness, compiler correctness, or semantic adequacy. For the L10 Real and Completion path, a downstream selector such as \autoref{ch:concrete-instances-realcauchymodulusselector-namecert} may cite the boundary only after the accepted-declaration and axiom-query rows have been displayed, so kernel acceptance remains an audit boundary rather than a Real completeness, selector existence, or semantic adequacy principle.
