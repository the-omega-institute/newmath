The accepted-declaration exposure row of $\KernelAcceptanceBoundaryUp$ is supplied by \autoref{ch:concrete-instances-kernelacceptancewitness-namecert}, while the forbidden-ancestry refusal row is supplied by \autoref{ch:concrete-instances-axiomqueryledger-namecert}. This sibling route only states that the boundary packet reads those two displayed audit dependencies before local naming; it does not claim kernel self-correctness, compiler correctness, or semantic adequacy.
