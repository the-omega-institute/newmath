\chapter{A Concrete Naming Certificate for $\InnovationInputCorrelationCarryUp$}
\label{ch:concrete-instances-innovation-input-correlation-carry-namecert}
\origin{ai}

The $\InnovationInputCorrelationCarryUp$ packet realizes the carry-recursion boundary of \autoref{ch:visions-innovation-input-self-substitution-correlation-boundary} as finite phase-word, coefficient, and correlation-window rows. It sits beside the modular input boundary of \autoref{ch:concrete-instances-innovation-input-modular-arithmetic-boundary-namecert}, the geometry boundary of \autoref{ch:concrete-instances-innovation-input-geometry-boundary-namecert}, and the spectral boundary of \autoref{ch:concrete-instances-innovation-input-spectral-boundary-namecert}: those siblings expose modular, geometric, and spectral consumers, while this packet records the finite self-substitution correlation rows that determine the two-point carry route before any downstream boundary consumes it. The packet is a finite NameCert carrier for phase correlations; it is not a Hilbert-space limit, a probability measure, a host ergodic theorem, a selected infinite sequence, or equality with a geometric or spectral boundary.

\begin{definition}[Innovation input correlation-carry carrier]
\label{def:innovation-input-correlation-carry-carrier}
An $\InnovationInputCorrelationCarryUp$ carrier is a finite $\BHist$ packet
$$
K=(V,A,S,T,G,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the phase-word row, naming the finite self-substitution word $v=(v_0,\ldots,v_{N-1})$ and the inspected alphabet size $N$.
\item $A$ is the digit-address row, recording the finite base-$N$ addresses used by the layered phase sequence.
\item $S$ is the forward carry-coefficient row, recording the displayed values $s_r(v)$.
\item $T$ is the wrap carry-coefficient row, recording the displayed values $t_r(v)$.
\item $G$ is the correlation-window row, recording the finite reads of $\gamma(q)$, $\gamma(q+1)$, and $\gamma(Nq+r)$ consumed by the recursion.
\item $W$ is the witness row that records which finite phase windows have been summed before the boundary term is discarded.
\item $H$ records componentwise $\hsame$ transport for the phase word, digit addresses, coefficient rows, correlation windows, finite-window witness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from phase-word rows through carry coefficients and correlation windows to the consumer route.
\item $P$ is the $\Pkg$ provenance over $\InnovationInputCorrelationCarryUp$, \autoref{ch:visions-innovation-input-self-substitution-correlation-boundary}, $\InnovationInputModularArithmeticBoundaryUp$, $\InnovationInputGeometryBoundaryUp$, $\InnovationInputSpectralBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\InnovationInputCorrelationCarryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $V,A,S,T,G,W,H,C,P,N$. It has no coordinate for an infinite limiting sequence, a hidden probability space, a host correlation theorem, a continuum spectral measure, an ambient geometric metric, or an unrecorded modular boundary.
\end{definition}

\begin{theorem}[Innovation input correlation-carry NameCert obligations]
\label{thm:innovation-input-correlation-carry-namecert-obligations}
For every accepted $\InnovationInputCorrelationCarryUp$ carrier
$$
K=(V,A,S,T,G,W,H,C,P,N),
$$
the local $\NameCert_{\InnovationInputCorrelationCarryUp}$ surface consists exactly of carrier admission for the phase-word row, digit-address row, forward carry coefficients, wrap carry coefficients, correlation-window row, finite-window witness, componentwise transport, displayed replay, package provenance, and local naming. Every self-substitution correlation read must expose the finite route
$$
V,A,S,T,G,W,H,C,P,N
$$
before it may consume the carry recursion for $\gamma(Nq+r)$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:innovation-input-correlation-carry-carrier}. The phase word $V$ and digit-address row $A$ are the only source rows for the layered phase sequence.

Read the coefficient rows next. The row $S$ supplies the non-wrapping coefficient values $s_r(v)$, while $T$ supplies the wrapping coefficient values $t_r(v)$. Both are finite word-level rows, not limits or ambient analytic objects.

The correlation-window row $G$ records only the three finite reads used by the carry relation: $\gamma(q)$, $\gamma(q+1)$, and $\gamma(Nq+r)$. The witness row $W$ records the finite phase windows whose boundary terms are being controlled.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since no coordinate carries a hidden probability space, selected infinite sequence, host ergodic theorem, geometric metric, spectral measure, or unrecorded modular boundary, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Innovation input correlation-carry recursion route]
\label{thm:innovation-input-correlation-carry-recursion-route}
Every accepted self-substitution correlation read of the form $m=Nq+r$ factors through the displayed coefficient and window route of an $\InnovationInputCorrelationCarryUp$ carrier. In particular, before a consumer reads
$$
\gamma(Nq+r)
=
\frac1N
\bigl(s_r(v)\gamma(q)+t_r(v)\gamma(q+1)\bigr),
$$
it must expose the phase-word row $V$, digit-address row $A$, forward coefficient row $S$, wrap coefficient row $T$, correlation-window row $G$, finite-window witness $W$, transport $H$, replay $C$, provenance $P$, and local name $N$.
\end{theorem}
\begin{proof}
Begin with the carry recursion theorem in \autoref{ch:visions-innovation-input-self-substitution-correlation-boundary}. Its coefficients are word-level sums over the inspected phase word, and its recursive reads use only $\gamma(q)$, $\gamma(q+1)$, and $\gamma(Nq+r)$.

Apply the carrier definition. The row $V$ fixes the phase word, $A$ fixes the base-$N$ addresses, $S$ and $T$ record the two carry coefficient families, and $G$ records the three correlation-window reads used by the formula.

Use the finite-window witness $W$ to keep the finite-level boundary terms visible. The route does not select an infinite sequence or import a probability space; it records which finite windows have been inspected before the correlation read is consumed.

The structural rows $H,C,P,N$ preserve the same packet for downstream modular, geometric, or spectral consumers. Hence every accepted carry-recursion read factors through the displayed finite carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\InnovationInputCorrelationCarryUp$ consumer must display the phase word, digit addresses, forward carry coefficients, wrap carry coefficients, correlation windows, finite-window witness, hsame transport, Cont replay, Pkg provenance, and local NameCert row before reading a self-substitution correlation recurrence.}

\independenceWitness{innovation-input-modular-arithmetic-boundary, innovation-input-geometry-boundary, innovation-input-spectral-boundary: $\InnovationInputCorrelationCarryUp$ is not bijective to any listed sibling because it jointly carries phase-word rows, digit addresses, two carry-coefficient families, correlation windows, finite-window witnesses, transport, replay, provenance, and local naming in one packet.}

The $\mathsf{TasteGate}$ follow-up is a finite carrier in \texttt{BEDC.Derived.InnovationInputCorrelationCarryUp} with nontriviality separating a packet with visible carry coefficients from a packet missing $S,T$, and field faithfulness preserving the row order $V,A,S,T,G,W,H,C,P,N$.

\closureat{InnovationInputCorrelationCarryUp}{seedStr}

\begin{closurestatus}{\InnovationInputCorrelationCarryUp}
  \constructivestory{An $\InnovationInputCorrelationCarryUp$ packet is generated from a phase-word row, digit-address row, forward and wrap carry-coefficient rows, correlation-window row, finite-window witness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:innovation-input-correlation-carry-carrier}, \autoref{thm:innovation-input-correlation-carry-namecert-obligations}, and \autoref{thm:innovation-input-correlation-carry-recursion-route} seed the finite carry-recursion surface for self-substitution phase correlations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close infinite correlation limits, probability-space semantics, host ergodic theorems, continuum spectral measures, ambient geometric metrics, modular-boundary conclusions, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, phase-word exposure, digit-address exposure, carry-coefficient exactness, correlation-window exactness, finite-window witness nonescape, transport, replay, provenance, local naming, and TasteGate support for BEDC.Derived.InnovationInputCorrelationCarryUp.}
\end{closurestatus}
