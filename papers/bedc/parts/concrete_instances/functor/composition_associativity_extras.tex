\begin{theorem}[Prefix functor preserves associative hom composition]
\label{thm:functor-prefix-hom-carrier-comp-assoc-preserves}
For the unary-continuation category, if \(p\) is unary and \(f,g,h\) are
composable hom-carriers with composites \(fg\), \(gh\), \(left\), and
\(right\), then \(left\) and \(right\) are hom-carriers from
\(\mathsf{append}(p,a)\) to \(\mathsf{append}(p,d)\), and
\(\HSame(left,right)\).
\end{theorem}
\begin{proof}
Use category-level associative composition closure to obtain the two
unprefixed composite hom-carriers and their classifier sameness. Prefix
preservation transports each composite hom-carrier across the unary prefix
\(p\), while the classifier sameness is the same continuation associativity
witness.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_assoc\_preserves}

\begin{theorem}[Prefix functor displayed associative hom determinacy]
\label{thm:functor-prefix-hom-carrier-comp-assoc-displayed-deterministic}
For the unary-continuation category, if $p$ is unary and $f,g,h$ are
composable hom-carriers with composites $fg$, $gh$, $left$, and $right$, then
any displayed hom-carrier from $\mathsf{append}(p,a)$ to
$\mathsf{append}(p,d)$ determines both bracketed composite histories up to
$\HSame$.
\end{theorem}
\begin{proof}
Prefix associative composition preservation supplies the two prefixed
hom-carriers over the displayed endpoints. Category hom-carrier morphism
determinacy compares each bracketed composite carrier with the displayed
carrier.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_assoc\_displayed\_deterministic}

\begin{theorem}[Prefix functor associative empty result components]
\label{thm:functor-prefix-hom-carrier-comp-assoc-empty-result-components}
For the unary-continuation category, if \(p\) is unary and the unprefixed
hom-carriers \(f,g,h\) compose with associated results \(left\) and \(right\),
then a displayed empty hom-carrier from \(\mathsf{append}(p,a)\) to
\(\mathsf{append}(p,d)\) forces both associated composite histories to be
\(\hsame\) to \(\emp\), and forces \(a\hsame d\).
\end{theorem}
\begin{proof}
Use prefix displayed associative determinacy with the displayed empty
hom-carrier to compare both bracketed composites with \(\emp\). Prefix empty
identity exactness cancels the common prefix in the same displayed empty
hom-carrier and reads back \(a\hsame d\).
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_assoc\_empty\_result\_components}

\begin{theorem}[Prefix functor unary suffix preserves associative hom composition]
\label{thm:functor-prefix-hom-carrier-unary-suffix-assoc-preserves}
For the unary-continuation category, if $p$ and $q$ are unary and $f,g,h$ are
composable hom-carriers with composites $fg$, $gh$, $left$, and $right$, then
$left$ and $right$ are displayed from
$\mathsf{append}(\mathsf{append}(p,a),q)$ to
$\mathsf{append}(\mathsf{append}(p,d),q)$, and $\HSame(left,right)$.
\end{theorem}
\begin{proof}
Category-level associative composition closure supplies the two central
carriers and the associativity witness between their morphism histories. Unary
context exactness transports both carriers through the shared prefix and
suffix, while the associativity witness is unchanged.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_unary\_suffix\_comp\_assoc\_preserves}

\begin{theorem}[Prefix functor unary suffix reflects associative hom composition]
\label{thm:functor-prefix-hom-carrier-unary-suffix-comp-assoc-reflects}
For histories $p,q,a,b,c,d,f,g,h,fg,gh,left,right$, suppose the three
displayed hom-carriers run through the shared unary context
$\mathsf{append}(\mathsf{append}(p,-),q)$ from $a$ to $b$, from $b$ to $c$,
and from $c$ to $d$. If the four continuation ledgers display the two
associative composites, then $left$ and $right$ are central hom-carriers from
$a$ to $d$, and $\HSame(left,right)$.
\end{theorem}
\begin{proof}
Apply unary-context exactness to each of the three displayed hom-carriers.
This reflects them to central hom-carriers $a\to b$, $b\to c$, and $c\to d$.
Category associative composition closure then applies to the reflected
carriers and the four displayed continuation ledgers, producing both central
composite carriers and the associativity classifier.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_unary\_suffix\_comp\_assoc\_reflects}

\begin{theorem}[Prefix functor reflects associative hom composition]
\label{thm:functor-prefix-hom-carrier-comp-assoc-reflects}
For histories $p,a,b,c,d,f,g,h,fg,gh,left,right$, suppose $f$ is a
hom-carrier from $\mathsf{append}(p,a)$ to $\mathsf{append}(p,b)$, $g$ is a
hom-carrier from $\mathsf{append}(p,b)$ to $\mathsf{append}(p,c)$, and $h$ is
a hom-carrier from $\mathsf{append}(p,c)$ to $\mathsf{append}(p,d)$. If
$\ContR(f,g,fg)$, $\ContR(g,h,gh)$, $\ContR(fg,h,left)$, and
$\ContR(f,gh,right)$, then $left$ and $right$ are hom-carriers from $a$ to
$d$, and $\HSame(left,right)$.
\end{theorem}
\begin{proof}
Reflect the three prefixed hom-carrier witnesses through
the unary-context exactness theorem
\autoref{thm:category-hom-carrier-unary-context-exactness}. This gives unprefixed
hom-carriers for $f$ from $a$ to $b$, $g$ from $b$ to $c$, and $h$ from $c$ to
$d$.

Apply Theorem~\ref{thm:category-hom-carrier-comp-assoc-closed} to these
reflected witnesses and to the four displayed continuation relations. The category
closure theorem returns both composite carriers from $a$ to $d$ together with
the classifier witness $\HSame(left,right)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_assoc\_reflects}

\begin{theorem}[Prefix functor preserves empty identity hom carriers]
\label{thm:functor-prefix-hom-carrier-empty-identity-preserves}
For the unary-continuation category, if \(p\) and \(a\) are unary, then the
empty history is a hom-carrier from \(\mathsf{append}(p,a)\) to
\(\mathsf{append}(p,a)\).
\end{theorem}
\begin{proof}
First use the empty identity hom-carrier theorem at \(a\). Prefix preservation
then transports that hom-carrier along the unary prefix \(p\).
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_empty\_identity\_preserves}

\begin{theorem}[Prefix functor empty identity hom carrier exactness]
\label{thm:functor-prefix-hom-carrier-empty-identity-iff}
For histories \(p,a,b\), the empty history is a hom-carrier from
\(\mathsf{append}(p,a)\) to \(\mathsf{append}(p,b)\) exactly when the two
prefixed endpoints are unary and the unprefixed tails \(a,b\) are internally
the same:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),\mathsf{append}(p,b),\emp)
\quad\Longleftrightarrow\quad
\mathsf{UnaryHistory}(\mathsf{append}(p,a))\wedge
\mathsf{UnaryHistory}(\mathsf{append}(p,b))\wedge \hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply empty identity hom-carrier exactness in the category chapter to obtain
sameness of the two prefixed endpoints. Left cancellation for generated-history
append then reflects this sameness to the tails \(a,b\). Conversely, tail
sameness lifts to sameness of the prefixed endpoints, and category empty
identity exactness reconstructs the hom-carrier.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_empty\_identity\_iff}

\begin{theorem}[Prefix functor empty morphism target exactness]
\label{thm:functor-prefix-hom-carrier-empty-morphism-target-exactness}
For histories $p,a,target$, the empty history is a hom-carrier from the
prefixed source $\mathsf{append}(p,a)$ to $target$ exactly when $p$ and $a$ are
unary and $target$ is history-same to that prefixed source:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),target,\emp)
\quad\Longleftrightarrow\quad
\mathsf{UnaryHistory}(p)\wedge\mathsf{UnaryHistory}(a)\wedge
\hsame(target,\mathsf{append}(p,a)).
$$
\end{theorem}
\begin{proof}
Apply category empty-identity exactness to the hom-carrier with source
$\mathsf{append}(p,a)$. The source unary witness splits into unary witnesses
for $p$ and $a$, and endpoint sameness is read in the target-to-source
direction. Conversely, append closure rebuilds the unary source; transport
along endpoint sameness gives the unary target, and category empty-identity
exactness reconstructs the hom-carrier.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_empty\_morphism\_target\_iff}

\begin{theorem}[One-headed prefix empty morphism target exactness]
\label{thm:functor-prefix-hom-carrier-e1-prefix-empty-morphism-target}
For histories $p,a,target:\mathsf{BHist}$, the empty history is a hom-carrier
from the one-headed prefixed source $\mathsf{append}(\Eone(p),a)$ to $target$
exactly when $p$ and $a$ are unary and $target$ is history-same to that source:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(\Eone(p),a),target,\emp)
\quad\Longleftrightarrow\quad
\mathsf{UnaryHistory}(p)\wedge\mathsf{UnaryHistory}(a)\wedge
\hsame(target,\mathsf{append}(\Eone(p),a)).
$$
\end{theorem}
\begin{proof}
Apply prefix empty morphism target exactness with prefix $\Eone(p)$. The
forward direction turns unary membership of $\Eone(p)$ into unary membership
of $p$ by one-headed unary inversion. Conversely, unary closure of the
one-headed constructor rebuilds the prefixed unary source data, and the
general empty morphism target exactness reconstructs the hom-carrier.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_e1\_prefix\_empty\_morphism\_target\_iff}

\begin{theorem}[One-headed prefix empty identity hom carrier exactness]
\label{thm:functor-prefix-hom-carrier-e1-prefix-empty-identity-iff}
For histories \(p,a,b\), the empty history is a hom-carrier from
\(\mathsf{append}(\Eone(p),a)\) to \(\mathsf{append}(\Eone(p),b)\) exactly
when \(p\), \(a\), and \(b\) are unary and the two tails are internally the
same:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(\Eone(p),a),
\mathsf{append}(\Eone(p),b),\emp)
\quad\Longleftrightarrow\quad
\mathsf{UnaryHistory}(p)\wedge\mathsf{UnaryHistory}(a)\wedge
\mathsf{UnaryHistory}(b)\wedge\hsame(a,b).
$$
\end{theorem}
\begin{proof}
The forward direction applies prefix empty identity exactness with prefix
\(\Eone(p)\), then splits the unary append witnesses for the source and
target. One-headed unary inversion extracts unary membership of \(p\). The
reverse direction closes \(\Eone(p)\) under unary generation, rebuilds the two
prefixed unary endpoints by append closure, and invokes prefix empty identity
exactness.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_e1\_prefix\_empty\_identity\_iff}

\begin{theorem}[Prefix functor empty composite exactness]
\label{thm:functor-prefix-hom-carrier-comp-empty-iff}
For histories $p,a,b,c,f,g$, if $f$ is a hom-carrier from
$\mathsf{append}(p,a)$ to $\mathsf{append}(p,b)$ and $g$ is a hom-carrier from
$\mathsf{append}(p,b)$ to $\mathsf{append}(p,c)$, then their empty composite is
equivalent to both morphism histories being empty and the two unprefixed
adjacent endpoints agreeing:
$$
\ContR(f,g,\emp)
\quad\Longleftrightarrow\quad
\HSame(f,\emp)\wedge\HSame(g,\emp)\wedge\HSame(a,b)\wedge\HSame(b,c).
$$
\end{theorem}
\begin{proof}
Category empty-composite exactness gives the two empty morphism histories and
the two prefixed endpoint equalities. Left cancellation removes the common
prefix $p$ from both endpoint equalities. Conversely, the unprefixed endpoint
equalities rebuild the prefixed endpoint equalities, and category
empty-composite exactness supplies the empty continuation ledger.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_empty\_iff}

\begin{theorem}[Prefix functor one-headed endpoint empty composite exactness]
\label{thm:functor-prefix-hom-carrier-e1-endpoint-empty-composite-iff}
For histories $p,a,b,c,f,g$, if $f$ is a hom-carrier from
$\mathsf{append}(p,\Eone(a))$ to $b$ and $g$ is a hom-carrier from $b$ to
$\mathsf{append}(p,\Eone(c))$, then their empty composite is equivalent to
both morphism histories being empty and the one-headed endpoint payloads
agreeing:
$$
\ContR(f,g,\emp)
\quad\Longleftrightarrow\quad
\HSame(f,\emp)\wedge\HSame(g,\emp)\wedge\HSame(a,c).
$$
\end{theorem}
\begin{proof}
Category empty-composite exactness gives the two empty morphism histories and
the adjacent endpoint equalities through $b$. Transitivity identifies the two
prefixed one-headed endpoints. Left cancellation removes the shared prefix
$p$, and one-headed constructor inversion gives $\HSame(a,c)$. Conversely, the
empty morphism histories reduce the displayed continuation ledger to
$\ContR(\emp,\emp,\emp)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_e1\_endpoint\_empty\_composite\_iff}

\begin{theorem}[Prefix functor one-headed empty composite middle endpoint]
\label{thm:functor-prefix-hom-carrier-e1-endpoint-empty-composite-middle-hsame}
For histories $p,a,b,c,f,g$, if $f$ is a hom-carrier from
$\mathsf{append}(p,\Eone(a))$ to $b$, $g$ is a hom-carrier from $b$ to
$\mathsf{append}(p,\Eone(c))$, and their composite is empty, then the shared
middle endpoint is classified with both visible prefixed endpoints:
$$
\ContR(f,g,\emp)
\Longrightarrow
\hsame(b,\mathsf{append}(p,\Eone(a)))\wedge
\hsame(b,\mathsf{append}(p,\Eone(c))).
$$
\end{theorem}
\begin{proof}
The one-headed empty-composite exactness theorem gives
$\hsame(f,\emp)$ and $\hsame(g,\emp)$. Transport the two hom-carriers along
these morphism classifiers so that both displayed morphisms are empty. Category
empty-identity exactness then reads the left leg as equality of its source and
middle endpoint, and the right leg as equality of the middle endpoint and its
target.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_e1\_endpoint\_empty\_composite\_middle\_hsame}

\begin{theorem}[Prefix functor empty-target component exactness]
\label{thm:functor-prefix-hom-carrier-empty-target-components-iff}
For histories $p,a,f$, a hom-carrier from the prefixed source
$\mathsf{append}(p,a)$ into the empty target is exactly componentwise emptiness
of the prefix, the tail, and the morphism:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),\emp,f)
\quad\Longleftrightarrow\quad
\hsame(p,\emp)\wedge\hsame(a,\emp)\wedge\hsame(f,\emp).
$$
\end{theorem}
\begin{proof}
Apply category empty-target exactness to obtain
$\hsame(\mathsf{append}(p,a),\emp)$ and $\hsame(f,\emp)$. Empty splitting for
the recursive append representative separates the first witness into
$p=\emp$ and $a=\emp$. Conversely, eliminating the three sameness witnesses
reduces the hom-carrier to the empty source, empty target, empty morphism
case.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_empty\_target\_components\_iff}

\begin{theorem}[Prefix functor one-headed empty-target impossibility]
\label{thm:functor-prefix-hom-carrier-e1-prefix-empty-target-absurd}
For histories $p,a,f$, no hom-carrier can run from
$\mathsf{append}(\Eone(p),a)$ into the empty target:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(\Eone(p),a),\emp,f)
\to \mathsf{False}.
$$
\end{theorem}
\begin{proof}
Apply the empty-target component exactness theorem just above with prefix
$\Eone(p)$. It reads back $\hsame(\Eone(p),\emp)$, contradicting the closed
constructor separation for one-headed histories.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_e1\_prefix\_empty\_target\_absurd}

\begin{theorem}[Prefix functor empty-target source morphism swap]
\label{thm:functor-prefix-hom-carrier-empty-target-source-morphism-swap}
For histories $p,a,f$, a hom-carrier from the prefixed source
$\mathsf{append}(p,a)$ into the empty target is unchanged when the displayed
source and morphism fields are exchanged:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),\emp,f)
\Longleftrightarrow
\mathsf{CategoryHomCarrier}(f,\emp,\mathsf{append}(p,a)).
$$
\end{theorem}
\begin{proof}
Apply category empty-target exactness at the prefixed source. The hom-carrier
is exactly the pair
$\hsame(\mathsf{append}(p,a),\emp)\wedge\hsame(f,\emp)$. Swapping those two
boundary classifiers gives the exchanged empty-target hom-carrier, and the
reverse implication swaps them back.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_empty\_target\_source\_morphism\_swap\_iff}

\begin{theorem}[Prefix functor empty-source component exactness]
\label{thm:functor-prefix-hom-carrier-empty-source-components}
For histories $p,b,f$, a hom-carrier from the empty source into the prefixed
target $\mathsf{append}(p,b)$ is exactly unary component data for $p$ and $b$
together with morphism equality to that prefixed target:
$$
\mathsf{CategoryHomCarrier}(\emp,\mathsf{append}(p,b),f)
\quad\Longleftrightarrow\quad
\mathsf{UnaryHistory}(p)\wedge
\mathsf{UnaryHistory}(b)\wedge
\hsame(f,\mathsf{append}(p,b)).
$$
\end{theorem}
\begin{proof}
Category empty-source exactness turns the hom-carrier into unary closure of the
prefixed target and equality of $f$ with that target. Unary append factorization
separates the target carrier into the prefix and tail carriers. Conversely,
unary append closure rebuilds the target carrier, and category empty-source
exactness reconstructs the hom-carrier.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_empty\_source\_components\_iff}

\begin{theorem}[Prefix functor one-headed morphism-target component exactness]
\label{thm:functor-prefix-hom-carrier-e1-morphism-target-components-iff}
For histories $p,a,k,r$, a hom-carrier from the prefixed source
$\mathsf{append}(p,a)$ to the one-headed target $\Eone(r)$ with one-headed
morphism $\Eone(k)$ is exactly unary component data for $p,a,k$ together with
the tail continuation:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),\Eone(r),\Eone(k))
\quad\Longleftrightarrow\quad
\mathsf{UnaryHistory}(p)\land\mathsf{UnaryHistory}(a)\land
\mathsf{UnaryHistory}(k)\land\ContR(\mathsf{append}(p,a),k,r).
$$
\end{theorem}
\begin{proof}
Apply category one-headed morphism-target exactness to obtain unary membership
of the appended source, unary membership of $k$, and the tail continuation.
Unary append factorization splits the source carrier into the two component
witnesses for $p$ and $a$. Conversely, unary append closure rebuilds the source
carrier, and the category exactness theorem lifts the tail continuation through
the visible one constructor on both target and morphism.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_e1\_morphism\_target\_components\_iff}
\begin{theorem}[Prefix functor visible endpoint morphism exactness]
\label{thm:functor-prefix-hom-carrier-e1-endpoint-visible-morphism-iff}
For histories $p,a,r,k$, a prefixed visible morphism between one-headed
endpoints is equivalent to a unary prefix, unary visible tails, and the
unprefixed tail continuation:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,\Eone(a)),
\mathsf{append}(p,\Eone(r)),\Eone(k))
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\wedge\mathsf{UnaryHistory}(a)\wedge
\mathsf{UnaryHistory}(k)\wedge\mathsf{UnaryHistory}(r)\wedge
\ContR(\Eone(a),k,r).
$$
\end{theorem}
\begin{proof}
Forward, reflect the prefixed hom-carrier to the base category and apply the
one-headed endpoint visible morphism exactness theorem; the prefix carrier is
read from the prefixed source. Reverse, first rebuild the base visible
hom-carrier from the tail continuation and then apply prefix preservation.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_e1\_endpoint\_visible\_morphism\_iff}
\input{parts/concrete_instances/functor/visible_endpoint_morphism}

\begin{theorem}[Functor prefix hom-carrier zero-headed component impossibility]
\label{thm:functor-prefix-hom-carrier-zero-headed-component-absurd}
For histories $p,a,b,f$, a prefixed category hom-carrier has no zero-headed
prefix, source tail, target tail, or morphism component:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),\mathsf{append}(p,b),f)
\Longrightarrow
\left(
\exists z,\ p=\Ezero(z)
\right)
\lor
\left(
\exists z,\ a=\Ezero(z)
\right)
\lor
\left(
\exists z,\ b=\Ezero(z)
\right)
\lor
\left(
\exists z,\ f=\Ezero(z)
\right)
\Longrightarrow
\mathsf{False}.
$$
\end{theorem}
\begin{proof}
The source and target carrier fields make
$\mathsf{append}(p,a)$ and $\mathsf{append}(p,b)$ unary. Unary append
factorization exposes unary witnesses for $p$, $a$, and $b$, while the
hom-carrier morphism field gives unary history for $f$. Each zero-headed
witness contradicts the unary zero-extension separation theorem.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_zero\_headed\_component\_absurd}

\begin{theorem}[Functor prefix hom-carrier source prefix determinism]
\label{thm:functor-prefix-hom-carrier-source-prefix-deterministic}
For histories $p,q,a,target,f$, if the same morphism $f$ is a category
hom-carrier from $\mathsf{append}(p,a)$ to $target$ and from
$\mathsf{append}(q,a)$ to $target$, then the two source prefixes agree:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),target,f)
\land
\mathsf{CategoryHomCarrier}(\mathsf{append}(q,a),target,f)
\Longrightarrow
\hsame(p,q).
$$
\end{theorem}
\begin{proof}
Category hom-carrier source determinism gives
$\hsame(\mathsf{append}(p,a),\mathsf{append}(q,a))$. Cancelling the common
right append suffix $a$ yields $\hsame(p,q)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_source\_prefix\_deterministic}

\begin{theorem}[Functor prefix hom-carrier composite source-prefix determinacy]
\label{thm:functor-prefix-hom-carrier-comp-source-prefix-deterministic}
For histories $p,q,a,b,c,f,g,fg$, suppose $f$ and $g$ are composable
hom-carriers over the common prefix $p$:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),\mathsf{append}(p,b),f)
\land
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,b),\mathsf{append}(p,c),g)
\land
\mathsf{Cont}(f,g,fg).
$$
If the displayed composite also has source $\mathsf{append}(q,a)$ and target
$\mathsf{append}(p,c)$, then $\hsame(p,q)$.
\end{theorem}
\begin{proof}
Close the two displayed $p$-prefixed hom-carriers under composition to obtain a
canonical carrier from $\mathsf{append}(p,a)$ to $\mathsf{append}(p,c)$ with
morphism $fg$. Source determinacy compares this carrier with the displayed
carrier whose source is $\mathsf{append}(q,a)$. Cancelling the common suffix
$a$ gives $\hsame(p,q)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_source\_prefix\_deterministic}

\begin{theorem}[Functor prefix hom-carrier composite source-object determinacy]
\label{thm:functor-prefix-hom-carrier-comp-source-object-deterministic}
For histories $p,a,d,b,c,f,g,fg$, suppose $f$ and $g$ are composable
hom-carriers over the common prefix $p$:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),\mathsf{append}(p,b),f)
\land
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,b),\mathsf{append}(p,c),g)
\land
\mathsf{Cont}(f,g,fg).
$$
If the displayed composite also has source $\mathsf{append}(p,d)$ and target
$\mathsf{append}(p,c)$, then $\hsame(a,d)$.
\end{theorem}
\begin{proof}
Close the expected composite from $\mathsf{append}(p,a)$ to
$\mathsf{append}(p,c)$. Source determinacy compares it with the displayed
carrier from $\mathsf{append}(p,d)$ to the same target and morphism. Cancelling
the common left prefix $p$ gives $\hsame(a,d)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_source\_object\_deterministic}

\begin{theorem}[Functor prefix hom-carrier source-object determinacy]
\label{thm:functor-prefix-hom-carrier-source-object-deterministic}
For histories $p,a,b,target,f$, if the same morphism $f$ is a category
hom-carrier from $\mathsf{append}(p,a)$ to $target$ and from
$\mathsf{append}(p,b)$ to $target$, then the two source objects agree:
$$
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,a),target,f)
\land
\mathsf{CategoryHomCarrier}(\mathsf{append}(p,b),target,f)
\Longrightarrow
\hsame(a,b).
$$
\end{theorem}
\begin{proof}
Category hom-carrier source determinism gives
$\hsame(\mathsf{append}(p,a),\mathsf{append}(p,b))$. Cancelling the common
left append prefix $p$ yields $\hsame(a,b)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_source\_object\_deterministic}

\begin{theorem}[Functor prefix hom-carrier target-prefix determinacy]
\label{thm:functor-prefix-hom-carrier-target-prefix-deterministic}
For histories $p,q,a,source,f$, if the same morphism $f$ is a category
hom-carrier from $source$ to $\mathsf{append}(p,a)$ and from $source$ to
$\mathsf{append}(q,a)$, then the two target prefixes agree:
$$
\mathsf{CategoryHomCarrier}(source,\mathsf{append}(p,a),f)
\land
\mathsf{CategoryHomCarrier}(source,\mathsf{append}(q,a),f)
\Longrightarrow
\hsame(p,q).
$$
\end{theorem}
\begin{proof}
Category hom-carrier target determinism gives
$\hsame(\mathsf{append}(p,a),\mathsf{append}(q,a))$. Cancelling the common
right append suffix $a$ yields $\hsame(p,q)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_target\_prefix\_deterministic}

\begin{theorem}[Functor prefix hom-carrier target-object determinacy]
\label{thm:functor-prefix-hom-carrier-target-object-deterministic}
For histories $p,a,b,source,f$, if the same morphism $f$ is a category
hom-carrier from $source$ to $\mathsf{append}(p,a)$ and from $source$ to
$\mathsf{append}(p,b)$, then the two target objects agree:
$$
\mathsf{CategoryHomCarrier}(source,\mathsf{append}(p,a),f)
\land
\mathsf{CategoryHomCarrier}(source,\mathsf{append}(p,b),f)
\Longrightarrow
\hsame(a,b).
$$
\end{theorem}
\begin{proof}
Category hom-carrier target determinism gives
$\hsame(\mathsf{append}(p,a),\mathsf{append}(p,b))$. Cancelling the common
left append prefix $p$ yields $\hsame(a,b)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_target\_object\_deterministic}

\begin{theorem}[Functor prefix hom-carrier composite middle-object determinacy]
\label{thm:functor-prefix-hom-carrier-comp-middle-object-deterministic}
For histories $p,a,b,b',c,f,g,fg$, suppose $f$ is a category hom-carrier from
$\mathsf{append}(p,a)$ to $\mathsf{append}(p,b)$, $g$ is a category
hom-carrier from $\mathsf{append}(p,b')$ to $\mathsf{append}(p,c)$, and
$\ContR(f,g,fg)$ holds. If $fg$ is displayed as a category hom-carrier from
$\mathsf{append}(p,a)$ to $\mathsf{append}(p,c)$, then $\hsame(b,b')$.
\end{theorem}
\begin{proof}
Apply category hom-carrier composite middle-object determinacy to the prefixed
hom-carriers and the displayed composite. This identifies
$\mathsf{append}(p,b)$ and $\mathsf{append}(p,b')$. Cancelling the common left
append prefix $p$ gives $\hsame(b,b')$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_middle\_object\_deterministic}

\begin{theorem}[Functor prefix hom-carrier composite target-prefix determinacy]
\label{thm:functor-prefix-hom-carrier-comp-target-prefix-deterministic}
For histories $p,q,a,b,c,f,g,fg$, if prefixed hom-carriers compose from
$\mathsf{append}(p,a)$ through $\mathsf{append}(p,b)$ to
$\mathsf{append}(p,c)$, and the same composite is displayed with target
$\mathsf{append}(q,c)$, then the two target prefixes agree:
$$
\hsame(p,q).
$$
\end{theorem}
\begin{proof}
Apply category composite target determinacy to compare the expected composite
target $\mathsf{append}(p,c)$ with the displayed target
$\mathsf{append}(q,c)$. Cancelling the common right suffix $c$ gives
$\hsame(p,q)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_target\_prefix\_deterministic}

\begin{theorem}[Functor prefix hom-carrier composite target-object determinacy]
\label{thm:functor-prefix-hom-carrier-comp-target-object-deterministic}
For histories $p,a,b,c,d,f,g,fg$, if prefixed hom-carriers compose from
$\mathsf{append}(p,a)$ through $\mathsf{append}(p,b)$ to
$\mathsf{append}(p,c)$, and the same composite is displayed with target
$\mathsf{append}(p,d)$, then the two target objects agree:
$$
\hsame(c,d).
$$
\end{theorem}
\begin{proof}
Close the expected composite target $\mathsf{append}(p,c)$ from the two
prefixed hom-carriers and the continuation witness. Target determinacy compares
that carrier with the displayed target $\mathsf{append}(p,d)$, and cancellation
of the common left prefix $p$ gives $\hsame(c,d)$.
\end{proof}
\leanchecked{BEDC.Derived.FunctorUp.FunctorPrefixHomCarrier\_comp\_target\_object\_deterministic}
