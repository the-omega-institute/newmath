\chapter{A Concrete Naming Certificate for $\AdjointRepUp$}
\label{ch:concrete-instances-adjointrep-namecert}

The adjoint-representation interface $\AdjointRepUp$ packages the conjugation action $\mathrm{Ad}\colon G \to \mathrm{Aut}(\mathfrak{g})$ together with its differential $\mathrm{ad}$. It depends on $\NameCert_{\LieGroupUp}$ (\autoref{ch:concrete-instances-liegroup-namecert}) and $\NameCert_{\LieAlgebraUp}$ (\autoref{ch:concrete-instances-liealgebra-namecert}).

\section{Carrier definition}
\label{sec:adjointrep-carrier}

\section{The certificate}
\label{sec:adjointrep-certificate}

\closureat{AdjointRepUp}{seedStr}

\begin{closurestatus}{\AdjointRepUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter binds the $\LieGroupUp$ conjugation-action source surface, the $\LieAlgebraUp$ adjoint-action source surface, the differential row, classifier-stability obligations, and ledger obligations as the seed boundary for $\AdjointRepUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{It does not close a carrier, classifier, differential law, representation law, standard bridge, or Lean verification target for $\AdjointRepUp$.}
  \upgradepath{Obligation closure requires explicit carrier, classifier, conjugation-action, differential, stability, ledger, exactness, and dependency rows for the $\AdjointRepUp$ certificate.}
\end{closurestatus}
