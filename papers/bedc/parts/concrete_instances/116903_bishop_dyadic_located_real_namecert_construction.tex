\chapter{A Concrete Naming Certificate for $\BishopDyadicLocatedRealUp$}
\label{ch:concrete-instances-bishop-dyadic-located-real-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, located real data are read through explicit rational approximations, apartness tests, and finite tolerance responses rather than through quotient reals, selected limits, countable choice, or an ambient completeness theorem. The $\BishopDyadicLocatedRealUp$ packet records the dyadic located-real route as finite NameCert data over the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the rational interval locator of \autoref{ch:concrete-instances-rational-interval-locator-namecert}, the locatedness surface of \autoref{ch:concrete-instances-locatedreal-namecert}, the terminal seal of \autoref{ch:concrete-instances-real-namecert}, and the threshold selector of \autoref{ch:concrete-instances-located-cauchy-modulus-selector-namecert}.

\begin{definition}[Bishop dyadic located-real carrier]
\label{def:bishop-dyadic-located-real-carrier}
A $\BishopDyadicLocatedRealUp$ carrier is a finite $\BHist$ packet
$$
B=(D,W,Q,I,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the positive dyadic radius and comparison budget requested by the located read;
\item $W$ is the $\StreamNameUp$ finite-window row opened for that tolerance;
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed windows;
\item $I$ is the $\RationalIntervalLocatorUp$ row exposing lower and upper rational brackets for the same dyadic request;
\item $L$ is the $\LocatedRealUp$ row deciding the displayed overlap or separation test between the readback and the rational interval at the named tolerance;
\item $E$ is the $\RealUp$ seal row exposed only after $D,W,Q,I,L$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for the dyadic tolerance, stream-window, readback, interval-locator, locatedness, Real seal, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from dyadic tolerance through finite windows, regular rational readback, rational interval location, locatedness, Real seal, and the structural rows;
\item $P$ is the $\Pkg$ provenance over $\BishopDyadicLocatedRealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RationalIntervalLocatorUp$, $\LocatedRealUp$, $\RealUp$, $\LocatedCauchyModulusSelectorUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\BishopDyadicLocatedRealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient real equality, a selected host limit, countable choice, an unlocated Cauchy witness, an ambient completeness theorem, or a Real seal read that bypasses the dyadic locatedness data.
\end{definition}

\begin{theorem}[Bishop dyadic located-real NameCert obligations]
\label{thm:bishop-dyadic-located-real-namecert-obligations}
For every accepted $\BishopDyadicLocatedRealUp$ carrier
$$
B=(D,W,Q,I,L,E,H,C,P,N),
$$
the local $\NameCert_{\BishopDyadicLocatedRealUp}$ surface consists exactly of carrier admission for the dyadic tolerance, finite stream-window, regular rational readback, rational interval locator, locatedness, Real seal, transport, replay, provenance, and local naming rows; dyadic tolerance exactness; window and readback compatibility; rational bracket exposure; locatedness exactness at the displayed tolerance; Real-seal nonescape; componentwise $\hsame$ transport; displayed $\Cont$ replay; $\Pkg$ provenance; and local naming for the same finite packet.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-dyadic-located-real-carrier}. The accepted packet exposes exactly the rows $D,W,Q,I,L,E,H,C,P,N$.

The analytic route is finite. The dyadic tolerance row $D$ opens the stream window $W$, the readback row $Q$ records the regular rational data read from that window, and the locator row $I$ supplies the rational bracket for the same request.

Locatedness is then local to the displayed tolerance. The row $L$ decides the overlap or separation test after $D,W,Q,I$ are visible, and the Real seal $E$ is downstream of that locatedness row.

Finally $H,C,P,N$ transport, replay, source, and name the same data. Since the carrier contains no quotient equality, selected host limit, choice schedule, unlocated witness, ambient completion theorem, or bypass seal coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop dyadic located-real Real handoff]
\label{thm:bishop-dyadic-located-real-real-handoff}
Every Real-facing consumer of an accepted $\BishopDyadicLocatedRealUp$ packet must read the finite route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RationalIntervalLocatorUp \longmapsto \LocatedRealUp \longmapsto \RealUp .
$$
For a carrier $B=(D,W,Q,I,L,E,H,C,P,N)$, the consumer receives the dyadic tolerance request, finite window, regular rational readback, rational interval locator, locatedness row, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. It receives no quotient real, selected limit, countable-choice schedule, unlocated Cauchy witness, or ambient completion datum.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bishop-dyadic-located-real-namecert-obligations}. They force any Real-facing read to stay on the displayed carrier rows.

Read the finite analytic rows in order. The dyadic request $D$ opens $W$, the regular readback $Q$ is obtained from the window, and the rational interval locator $I$ brackets the same request.

The locatedness row $L$ is then read before the seal. By the carrier definition, $E$ is exposed only after $D,W,Q,I,L$ have been displayed, and the structural rows $H,C,P,N$ preserve that route for downstream consumers.

Thus the Real handoff is a dyadic located-real certificate, not a quotient real construction, a selected host limit, a countable-choice route, an unlocated Cauchy witness, or an ambient completion theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopDyadicLocatedRealUp$ Real-facing read exposes one DyadicRatCore tolerance row, one StreamName finite-window row, one RegSeqRat readback row, one RationalIntervalLocator bracket row, one LocatedReal locatedness row, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before the Real seal is public.}

\independenceWitness{dyadicratcore, streamname, regseqrat, rational-interval-locator, locatedreal, real, located-cauchy-modulus-selector: $\BishopDyadicLocatedRealUp$ is not bijective to any listed sibling because it jointly carries dyadic tolerance data, finite stream windows, regular rational readback, rational brackets, locatedness at the displayed tolerance, Real seal handoff, transport, replay, provenance, and local naming as one finite located-real certificate.}

\closureat{BishopDyadicLocatedRealUp}{seedStr}

\begin{closurestatus}{\BishopDyadicLocatedRealUp}
  \constructivestory{A $\BishopDyadicLocatedRealUp$ packet is generated from dyadic tolerance requests, finite stream windows, regular rational readback, rational interval locator rows, locatedness rows, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-dyadic-located-real-carrier}, \autoref{thm:bishop-dyadic-located-real-namecert-obligations}, and \autoref{thm:bishop-dyadic-located-real-real-handoff} seed the finite Bishop dyadic located-real route over DyadicRatCore, StreamName, RegSeqRat, RationalIntervalLocator, LocatedReal, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient reals, selected host limits, countable choice, unlocated Cauchy witnesses, ambient completion, Lean verification, or bridge checking.}
  \upgradepath{Formal verification requires a Lean carrier and checked NameCert obligations for BEDC.Derived.BishopDyadicLocatedRealUp.}
\end{closurestatus}
