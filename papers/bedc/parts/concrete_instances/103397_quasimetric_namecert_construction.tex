\chapter{A Concrete Naming Certificate for $\QuasiMetricUp$}
\label{ch:concrete-instances-quasi-metric-namecert}
\origin{human}

Following Lawvere, Nachbin, and Kelley, the $\QuasiMetricUp$ packet records the finite directed-distance surface that sits beside the symmetric metric certificate of \autoref{ch:concrete-instances-metric-namecert}, the directed entourage carrier of \autoref{ch:concrete-instances-quasi-uniform-space-namecert}, the filter route of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the net route of \autoref{ch:concrete-instances-cauchynet-namecert}, and the completion consumer of \autoref{ch:concrete-instances-uniformcompletion-namecert}. Its Real-valued distance rows read the terminal seal of \autoref{ch:concrete-instances-real-namecert} through the dyadic tolerance source of \autoref{ch:concrete-instances-dyadicratcore-namecert}, so completion-facing consumers see an oriented distance ledger before any uniform-entourage handoff is exposed. The chapter names only that directed NameCert surface; it does not assert symmetry, separatedness, metric completeness, open-cover compactness, quotient distance, selected limits, or a standard bridge.

\begin{definition}[QuasiMetric carrier surface]
\label{def:quasi-metric-carrier}
A $\QuasiMetricUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source carrier row. It names the traditional quasi-metric source and the finite observation context in which directed distance requests are admitted.
\item $X$ is the point carrier row. It records the admitted point names and their classifier, but it does not identify points by zero distance.
\item $D$ is the directed $\RealUp$-valued distance row. It records oriented requests $d(x,y)$ and keeps the reverse request $d(y,x)$ as a separate row when that reverse request is present.
\item $Z$ is the zero-on-diagonal row. It records $d(x,x)=0$ as a displayed boundary condition on directed distance requests.
\item $T$ is the forward triangle ledger. It records the directed comparison from $d(x,z)$ through the ordered pair of rows $d(x,y)$ and $d(y,z)$.
\item $B$ is the directed-ball row. It records finite requests of the form $B^+(x,\varepsilon)$ by displaying the center, radius, and incoming point through $D,Z,T$.
\item $F$ is the $\CauchyFilterUp$ row. It records filter-base Cauchy reads whose directed balls are supplied by $B$ and whose comparisons remain ordered.
\item $R$ is the $\CauchyNetUp$ row. It records net-tail Cauchy reads whose eventual directed-ball comparisons are supplied by $B$.
\item $E$ is the uniform-reflection row. It records the finite handoff from directed balls to $\QuasiUniformSpaceUp$, $\UniformSpaceUp$, and completion-facing entourage requests without installing symmetry as primitive data.
\item $H$ records componentwise $\hsame$ transport for source, point-carrier, directed distance, zero diagonal, triangle, directed-ball, Cauchy filter, Cauchy net, uniform reflection, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a consumer request through point admission, directed distance readback, zero diagonal or triangle evidence, directed-ball inspection, Cauchy filter or net readback, uniform reflection, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\QuasiMetricUp$, $\MetricSpaceUp$, $\QuasiUniformSpaceUp$, $\UniformSpaceUp$, $\CauchyFilterUp$, $\CauchyNetUp$, $\UniformCompletionUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\QuasiMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,X,D,Z,T,B,F,R,E,H,C,P,N$. It has no coordinate for symmetry, separated point identity, metric completeness, open-cover compactness, quotient distance, selected limits, quotient completion, or bridge checking.
\end{definition}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricTasteGate\_single\_carrier\_alignment}

\begin{theorem}[QuasiMetric NameCert obligation surface]
\label{thm:quasimetric-obligation-surface}
For every accepted $\QuasiMetricUp$ carrier
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N),
$$
the local $\NameCert_{\QuasiMetricUp}$ surface consists exactly of the following obligations:
\begin{enumerate}
\item carrier admission for the source, point carrier, directed $\RealUp$-valued distance row, zero diagonal row, forward triangle row, directed-ball row, Cauchy filter row, Cauchy net row, uniform-reflection row, componentwise transport, displayed replay, provenance, and local name;
\item asymmetric distance readback, requiring every exported distance request to keep the ordered pair of point rows visible before the $\RealUp$ distance seal is read;
\item zero diagonal stability, requiring self-distance to be stored in $Z$ rather than imported from a host metric law;
\item oriented triangle stability, requiring each triangle comparison to be displayed in $T$ with its ordered endpoints;
\item directed-ball handoff, requiring every ball consumer to pass through $B$ after $D,Z,T$ have been displayed;
\item Cauchy filter and Cauchy net routing, requiring filter-base and net-tail comparisons to read $F,R$ through the directed-ball row;
\item classifier transport, requiring accepted packets to move only by the componentwise $\hsame$ row $H$ and the replay row $C$;
\item uniform-reflection handoff, requiring every quasi-uniform, uniform, or completion-facing request to pass through $E$ after the directed balls and Cauchy rows have been displayed;
\item ledger non-escape, excluding symmetry, separatedness, metric completeness, open-cover compactness, quotient distance, quotient completion, selected limits, and any standard bridge.
\end{enumerate}
No quasi-metric NameCert obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:quasi-metric-carrier}. The tuple lists exactly the source, point carrier, directed distance, zero diagonal, forward triangle, directed-ball, Cauchy filter, Cauchy net, uniform-reflection, transport, replay, provenance, and local naming rows.

Asymmetric distance readback is forced by $D$. A request for $d(x,y)$ keeps $x$ and $y$ in that order, and a request for $d(y,x)$ is a distinct displayed row unless the carrier separately records a comparison between them.

The zero diagonal and triangle obligations are the displayed rows $Z$ and $T$. Thus reflexive zero and forward triangle comparison are local quasi-metric data, not imported symmetry or host metric structure.

Directed-ball reads use $B$ only after $D,Z,T$ have been displayed. The Cauchy filter and net rows $F,R$ read those directed balls rather than a quotient completion object.

Classifier transport is componentwise through $H$, while $C$ replays the same ordered route for consumers. The uniform-reflection handoff uses $E$ only after the directed distance, ball, and Cauchy rows have been read.

The remaining exclusions follow from the carrier inventory. Since no coordinate names symmetry, separated identity, metric completeness, open-cover compactness, quotient distance, quotient completion, selected limits, or a standard bridge, none of those data can be exported by the local certificate.
\end{proof}

\begin{theorem}[QuasiMetric directed ball handoff]
\label{thm:quasi-metric-directed-ball-handoff}
Let
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N)
$$
be an accepted $\QuasiMetricUp$ packet. Every $\QuasiUniformSpaceUp$, $\UniformSpaceUp$, $\CauchyFilterUp$, $\CauchyNetUp$, or $\UniformCompletionUp$ consumer that asks for a distance-presented directed ball reaches it only through the finite route
$$
S,X,D,Z,T \longmapsto B \longmapsto F,R,E \longmapsto H,C,P,N .
$$
The handoff may reflect to quasi-uniform, uniform, Cauchy-filter, Cauchy-net, or completion-facing rows, but it receives no symmetry theorem, no separated point identity, no metric completeness, no open-cover compactness, no quotient distance, no quotient completion, and no standard bridge.
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_directed\_ball\_handoff}
\begin{proof}
Start with the accepted carrier. The consumer first reads the source and point rows $S,X$, then the directed distance ledger $D$.

The diagonal and triangle rows $Z,T$ are read before any ball-facing handoff. They supply the quasi-metric boundary conditions needed to form directed balls without turning the directed distance into a symmetric metric.

Only after those rows are displayed does the consumer reach $B$. Filter and net consumers then read $F,R$, while quasi-uniform, uniform, and completion-facing consumers read the reflection row $E$.

The support rows $H,C,P,N$ transport, replay, package, and name the same finite route. Therefore the handoff exports finite directed-distance and directed-ball data, not symmetry, separatedness, metric completeness, open-cover compactness, quotient distance, quotient completion, or bridge evidence.
\end{proof}

\begin{theorem}[QuasiMetric non-escape boundary]
\label{thm:quasi-metric-nonescape}
Every accepted $\QuasiMetricUp$ read used by $\MetricSpaceUp$, $\QuasiUniformSpaceUp$, $\UniformSpaceUp$, $\CauchyFilterUp$, $\CauchyNetUp$, or $\UniformCompletionUp$ factors through the displayed rows of \autoref{def:quasi-metric-carrier}. In particular, a metric or completion consumer cannot bypass the directed-distance row $D$, the forward triangle ledger $T$, the directed-ball row $B$, the Cauchy rows $F,R$, and the uniform-reflection row $E$ by importing symmetry, quotient completion, selected limits, host metric equality, \texttt{Classical.choice}, or \texttt{Quot.sound}.
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_nonescape}
\begin{proof}
Apply \autoref{thm:quasimetric-obligation-surface}. The NameCert surface stores all quasi-metric obligations in $S,X,D,Z,T,B,F,R,E,H,C,P,N$ and stores none outside those rows.

Apply \autoref{thm:quasi-metric-directed-ball-handoff}. Every directed-ball, Cauchy, quasi-uniform, uniform, and completion-facing consumer reaches the public route only after $D,Z,T,B,F,R,E$ have been displayed.

A metric-facing consumer may read the sibling metric surface of \autoref{ch:concrete-instances-metric-namecert} only as a reflected reader named by $E$ or $P$. Since the carrier has no primitive coordinate for symmetry, quotient completion, selected limits, host equality, \texttt{Classical.choice}, or \texttt{Quot.sound}, those exits remain unavailable.
\end{proof}

\begin{theorem}[QuasiMetric scope tightening]
\label{thm:quasimetric-scope-tightening}
Let
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N)
$$
be an accepted $\QuasiMetricUp$ packet. Its directed-distance scope is exactly the displayed route through source, point carrier, directed $\RealUp$ distance, zero diagonal, forward triangle, directed balls, Cauchy filter, Cauchy net, uniform reflection, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. Consequently every $\MetricSpaceUp$, $\QuasiUniformSpaceUp$, $\UniformSpaceUp$, $\CauchyFilterUp$, $\CauchyNetUp$, $\UniformCompletionUp$, $\DyadicRatCoreUp$, or $\RealUp$ consumer of $Q$ is bounded by the finite route
$$
S,X,D,Z,T \longmapsto B \longmapsto F,R,E \longmapsto H,C,P,N .
$$
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_scope\_tightening}
\begin{proof}
Apply the obligation surface of \autoref{thm:quasimetric-obligation-surface}. It identifies the carrier rows $S,X,D,Z,T,B,F,R,E,H,C,P,N$ as the full local NameCert inventory and places directed distance, zero diagonal, and forward triangle data before every public ball or completion-facing read.

Next use the directed-ball handoff of \autoref{thm:quasi-metric-directed-ball-handoff}. It routes quasi-uniform, uniform, Cauchy-filter, Cauchy-net, and completion-facing consumers through $B,F,R,E$ only after the directed-distance prefix has been displayed.

Finally apply the non-escape boundary of \autoref{thm:quasi-metric-nonescape}. Since the carrier has no coordinate for symmetry, separated point identity, metric completeness, open-cover compactness, quotient distance, quotient completion, selected limits, host metric equality, \texttt{Classical.choice}, or \texttt{Quot.sound}, the displayed row route is not merely sufficient but exact for the scoped directed-distance surface.
\end{proof}

\begin{theorem}[QuasiMetric directed-ball triangle refinement]
\label{thm:quasimetric-directed-ball-triangle-refinement}
Let
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N)
$$
be an accepted $\QuasiMetricUp$ packet. If two displayed directed-ball reads in $B$ have compatible middle endpoint, with the first read routed from $x$ to $y$ and the second from $y$ to $z$, then their composed directed-ball read is exposed only through the asymmetric triangle row $T$, preserving the source row $S$, point rows $X$, directed-distance ledger $D$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert_{\QuasiMetricUp}$ row $N$.
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_directed\_ball\_triangle\_refinement}
\begin{proof}
Project the carrier surface of \autoref{def:quasi-metric-carrier}. The two directed-ball reads have no source other than $S,X,D,Z,T,B$, and the middle endpoint compatibility is a displayed point-row match inside $X$ rather than a separated point identity.

Use the oriented triangle obligation in \autoref{thm:quasimetric-obligation-surface}. It requires each triangle comparison to be stored in $T$ with ordered endpoints, so composing the $x$ to $y$ and $y$ to $z$ ball reads can only read the displayed asymmetric triangle witness.

The handoff theorem \autoref{thm:quasi-metric-directed-ball-handoff} then routes the composed directed ball through $B$ and onward to any Cauchy, uniform, or completion-facing consumer. The support rows $H,C,P,N$ transport, replay, package, and name the same directed-distance witnesses, while \autoref{thm:quasi-metric-nonescape} excludes symmetry, quotient completion, selected limits, host metric equality, \texttt{Classical.choice}, and \texttt{Quot.sound}.
\end{proof}

\begin{theorem}[QuasiMetric directed-distance scope upgrade]
\label{thm:quasimetric-directed-distance-scope-upgrade}
Let
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N)
$$
be an accepted $\QuasiMetricUp$ packet. Every accepted directed-distance read used by $\MetricSpaceUp$, $\QuasiUniformSpaceUp$, $\UniformSpaceUp$, $\CauchyFilterUp$, $\CauchyNetUp$, $\UniformCompletionUp$, $\DyadicRatCoreUp$, or $\RealUp$ must pass through the displayed rows $S,X,D,Z,T,B,F,R,E,H,C,P,N$ of \autoref{def:quasi-metric-carrier}. The scope is bound to the finite $\BHist$ carrier, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, local $\NameCert$ naming, Real-valued distance readback, dyadic tolerance comparison, and directed-ball routing.

Consequently the directed-distance route is a scoped BEDC surface, not a host metric object: no consumer receives symmetry, separated point identity, quotient distance, selected completion limit, ambient metric completeness, open-cover compactness, \texttt{Classical.choice}, \texttt{Quot.sound}, or bridge evidence from the $\QuasiMetricUp$ packet.
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_directed\_distance\_scope\_upgrade}
\begin{proof}
Start with \autoref{thm:quasimetric-obligation-surface}. It identifies $S,X,D,Z,T,B,F,R,E,H,C,P,N$ as the complete local NameCert inventory and places the Real-valued directed-distance row $D$, zero row $Z$, and forward triangle row $T$ before every public read.

Next apply \autoref{thm:quasi-metric-directed-ball-handoff}. Directed-ball consumers reach $B$ only after $S,X,D,Z,T$ have been displayed, and Cauchy, quasi-uniform, uniform, and completion-facing consumers then pass through $F,R,E$ before the support rows $H,C,P,N$ are read.

Use \autoref{thm:quasi-metric-nonescape} to close the non-escape boundary. It rules out host symmetry, separated equality, quotient completion, selected limits, \texttt{Classical.choice}, and \texttt{Quot.sound} as alternate exits from the packet.

Finally invoke \autoref{thm:quasimetric-directed-ball-triangle-refinement}. The composed directed-ball comparisons read the asymmetric triangle row $T$ and preserve the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. Thus every listed consumer receives exactly the directed-distance scope carried by the displayed rows.
\end{proof}

\begin{theorem}[QuasiMetric public certificate]
\label{thm:quasimetric-public-certificate}
For every accepted $\QuasiMetricUp$ packet
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N),
$$
the public directed-distance certificate is the assembled row route
$$
\MetricSpaceUp,\ \QuasiUniformSpaceUp,\ \UniformSpaceUp,\ \CauchyFilterUp,\ \CauchyNetUp,\ \UniformCompletionUp,\ \DyadicRatCoreUp,\ \RealUp
\quad\text{through}\quad
S,X,D,Z,T,B,F,R,E,H,C,P,N .
$$
It is obtained by combining the carrier surface of \autoref{def:quasi-metric-carrier}, the NameCert obligations of \autoref{thm:quasimetric-obligation-surface}, the directed-ball handoff of \autoref{thm:quasi-metric-directed-ball-handoff}, the non-escape boundary of \autoref{thm:quasi-metric-nonescape}, the directed-ball triangle refinement of \autoref{thm:quasimetric-directed-ball-triangle-refinement}, and the directed-distance scope upgrade of \autoref{thm:quasimetric-directed-distance-scope-upgrade}. The certificate exports directed $\RealUp$-valued distance, zero diagonal, forward triangle, directed-ball, Cauchy, uniform-reflection, transport, replay, provenance, and local naming rows, but it exports no symmetry, separatedness, quotient completion, selected limits, host equality, \texttt{Classical.choice}, \texttt{Quot.sound}, or bridge evidence.
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_public\_certificate}
\begin{proof}
First read the carrier surface of \autoref{def:quasi-metric-carrier}. It displays the source and point rows $S,X$, the directed $\RealUp$ distance row $D$, the zero and forward-triangle rows $Z,T$, the directed-ball row $B$, Cauchy filter and net rows $F,R$, the uniform-reflection row $E$, and the structural rows $H,C,P,N$.

Next apply the NameCert obligation surface of \autoref{thm:quasimetric-obligation-surface}. That theorem identifies carrier admission, directed distance readback, zero diagonal stability, forward triangle stability, directed-ball handoff, Cauchy routing, classifier transport, uniform reflection, and ledger non-escape as the exact local obligations.

Use \autoref{thm:quasi-metric-directed-ball-handoff} and \autoref{thm:quasimetric-directed-ball-triangle-refinement} to assemble the public route. Directed balls are read only after $S,X,D,Z,T$, composed directed-ball comparisons read the asymmetric triangle row $T$, and Cauchy, quasi-uniform, uniform, and completion-facing consumers pass through $B,F,R,E$ before the support rows are read.

Finally use \autoref{thm:quasi-metric-nonescape} and \autoref{thm:quasimetric-directed-distance-scope-upgrade}. They close the route as a public certificate over the displayed rows and rule out alternate exits through symmetry, separatedness, quotient completion, selected limits, host equality, \texttt{Classical.choice}, \texttt{Quot.sound}, or bridge evidence.
\end{proof}

\begin{theorem}[QuasiMetric public NameCert export]
\label{thm:quasimetric-public-namecert-export}
For every accepted $\QuasiMetricUp$ packet
$$
Q=(S,X,D,Z,T,B,F,R,E,H,C,P,N),
$$
the public directed-distance certificate of \autoref{thm:quasimetric-public-certificate} is exactly the local $\NameCert_{\QuasiMetricUp}$ export over the source row $S$, point carrier $X$, directed $\RealUp$ distance row $D$, zero row $Z$, forward triangle row $T$, directed-ball row $B$, Cauchy filter row $F$, Cauchy net row $R$, uniform-reflection row $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local naming row $N$. The export records dyadic tolerance comparison and directed-ball routing for the Real-valued distance surface, but it exports no symmetry, separated equality, quotient completion, selected limit, ambient completeness, open-cover compactness, or bridge evidence.
\end{theorem}
\leanchecked{BEDC.Derived.QuasiMetricUp.QuasiMetricCarrier\_public\_namecert\_export}
\begin{proof}
First apply \autoref{thm:quasimetric-public-certificate}. It assembles the public directed-distance certificate from the carrier, obligation surface, directed-ball handoff, triangle refinement, scope route, and non-escape boundary.

Next use \autoref{thm:quasimetric-directed-distance-scope-upgrade}. It keeps every Real- or dyadic-facing distance read inside $S,X,D,Z,T,B,F,R,E,H,C,P,N$, so the public certificate has no hidden parameter transport outside the displayed packet.

Finally apply \autoref{thm:quasi-metric-nonescape}. Since symmetry, separated equality, quotient completion, selected limits, host equality, \texttt{Classical.choice}, \texttt{Quot.sound}, and bridge evidence are absent from the carrier, the public certificate is precisely the local $\NameCert_{\QuasiMetricUp}$ export over the listed rows.
\end{proof}

\closureat{QuasiMetricUp}{publicStr}
\closureat{QuasiMetricUp}{scopedStr}
\closureat{QuasiMetricUp}{bridgedStr}[thm:quasimetric-public-namecert-export]

\begin{closurestatus}{\QuasiMetricUp}
  \constructivestory{A $\QuasiMetricUp$ packet is generated from a source row, a point carrier, a directed RealUp-valued distance row, a zero diagonal row, a forward triangle row, a directed-ball row, Cauchy filter and Cauchy net rows, a uniform-reflection row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:quasimetric-public-namecert-export} is the bridge-facing public directed-distance route assembled from the displayed directed-distance, directed-ball, non-escape, uniform-reflection, transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{paperBridge}
  \notclaimed{This chapter does not close symmetry, separatedness, metric completeness, open-cover compactness, quotient distance, quotient completion, selected limits, standard bridge evidence, Lean verification, or bridge checking.}
  \upgradepath{Bridge checking requires checked carrier admission, directed distance readback, zero diagonal stability, forward triangle stability, directed-ball handoff, Cauchy routing, uniform reflection, classifier transport, and ledger non-escape for BEDC.Derived.QuasiMetricUp.}
\end{closurestatus}
