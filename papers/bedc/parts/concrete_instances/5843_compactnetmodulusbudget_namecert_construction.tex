\chapter{A Concrete Naming Certificate for $\CompactNetModulusBudgetUp$}
\label{ch:concrete-instances-compactnetmodulusbudget-namecert}
\origin{ai}

The $\CompactNetModulusBudgetUp$ packet realizes the compact-uniform-continuity budget handoff as one finite NameCert carrier. It sits after the compact finite-net and continuous-map modulus ledger of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, consumes the tolerance-to-precision target surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and records the finite rational fold supplied by \autoref{ch:concrete-instances-finitenetminimumfold-namecert}. The sibling compact cover and selector chapters \autoref{ch:concrete-instances-compactmoduluscover-namecert} and \autoref{ch:concrete-instances-compactnetmodulusselector-namecert} provide the compact-cover and center-selection faces, while this packet binds the finite-net, pointwise-modulus, rational-fold, and uniform-modulus budget rows as a single downstream object.

\begin{definition}[Compact net modulus budget carrier]
\label{def:compact-net-modulus-budget-carrier}
A $\CompactNetModulusBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(K,M,F,U,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row, including the nonempty $\mathsf{ProbeBundle}$ spine and located coverage rows;
\item $M$ is the pointwise-modulus row attached to the bundled centers and to the requested positive $\RatUp$ tolerance history;
\item $F$ is the rational lower-bound fold row over the finite radius ledger;
\item $U$ is the uniform-modulus handoff row exported to downstream compact-continuous consumers;
\item $S$ is the selector and cover compatibility row tying $\CompactModulusCoverUp$ and $\CompactNetModulusSelectorUp$ to the same finite net;
\item $H$ records componentwise $\hsame$ transport for finite-net, pointwise-modulus, fold, uniform-modulus, selector, route, provenance, and naming rows;
\item $C$ records displayed $\Cont$ routes through which budget consumers read the compact net, pointwise modulus, fold ledger, selector compatibility, and uniform-modulus handoff;
\item $P$ is the $\Pkg$ provenance over $\CompactUniformContinuityUp$, $\UniformModulusUp$, $\FiniteNetMinimumFoldUp$, $\CompactModulusCoverUp$, $\CompactNetModulusSelectorUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CompactNetModulusBudgetUp}$ row.
\end{enumerate}
The carrier exports no open-cover compactness coordinate, host minimum, selected point outside the finite probe spine, external modulus function, sequential compactness argument, or quotient point equality.
\end{definition}

\begin{theorem}[Compact net modulus budget NameCert obligations]
\label{thm:compact-net-modulus-budget-namecert-obligations}
The local $\NameCert_{\CompactNetModulusBudgetUp}$ surface consists exactly of five finite rows:
\begin{enumerate}
\item carrier admission for the packet of \autoref{def:compact-net-modulus-budget-carrier};
\item finite-net exactness for the compact $\mathsf{ProbeBundle}$ row $K$;
\item pointwise-modulus exactness for the rows $M$ over the same bundled centers;
\item rational-fold exactness for the lower-bound ledger $F$ and its exported uniform-modulus row $U$;
\item downstream non-escape through the displayed $\hsame$, $\Cont$, $\Pkg$, selector-compatibility, and local $\NameCert$ rows $S,H,C,P,N$.
\end{enumerate}
The later Lean \texttt{TasteGate.lean} development for $\CompactNetModulusBudgetUp$ must provide $\mathsf{Nontrivial}\ \CompactNetModulusBudgetUp$ and $\mathsf{FieldFaithful}\ \CompactNetModulusBudgetUp$ instances preserving these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:compact-net-modulus-budget-carrier}. The packet exposes exactly the compact net, pointwise modulus, rational fold, uniform handoff, selector compatibility, transport, route, provenance, and local naming rows.

Finite-net exactness reads $K$ through the compact-uniform-continuity and compact-cover sibling surfaces. The pointwise-modulus row $M$ is attached to the same finite spine, so no pointwise row can be read at an unlisted center.

Rational-fold exactness reads $F$ through the finite-net-minimum-fold sibling and exports $U$ as the uniform-modulus budget row. The selector row $S$ keeps the compact-cover and selector faces on that same finite spine.

Finally $H,C,P,N$ transport, route, package, and name the packet. Since no excluded analytic object is a coordinate of $B$, the local NameCert obligations are exhausted by the five rows above.
\end{proof}

\begin{theorem}[Compact net modulus budget handoff exactness]
\label{thm:compact-net-modulus-budget-handoff-exactness}
Every downstream compact-continuous consumer of an accepted $\CompactNetModulusBudgetUp$ packet reads the uniform-modulus budget only through
$$
K,M,F,U,S,H,C,P,N.
$$
The consumer reaches the finite-net face before the pointwise-modulus face, reaches the rational-fold row before the uniform-modulus handoff, and reads the selector and cover compatibility row on the same finite probe spine. It has no route through open-cover compactness before the rational fold row, host minima, selected external centers, sequential compactness, quotient point equality, or an external modulus function.
\end{theorem}
\begin{proof}
Project the accepted carrier. The finite-net row $K$, pointwise-modulus row $M$, rational-fold row $F$, uniform handoff row $U$, and selector-compatibility row $S$ are the only budget-facing coordinates.

Apply \autoref{thm:compact-net-modulus-budget-namecert-obligations}. The five rows force carrier admission, finite-net exactness, pointwise-modulus exactness, rational-fold exactness, and downstream non-escape through the displayed structural rows.

Read the sibling surfaces in order. The compact-uniform-continuity and compact-modulus-cover rows supply the finite-net and cover data, the finite-net-minimum-fold row supplies the rational fold, and the uniform-modulus row supplies the exported precision budget.

Because $U$ is exported only after $F$ and because the packet has no open-cover, host-minimum, external-center, sequential-compactness, quotient, or external-modulus coordinate, every downstream consumer route is exactly the displayed budget handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactNetModulusBudgetUp$ packet has no consumer route that reads an open-cover compactness coordinate before the rational fold row.}

\independenceWitness{Compare compactuniformcontinuity, uniformmodulus, finitenetminimumfold, compactmoduluscover, compactnetmodulusselector: this carrier records all four budget faces in one packet, while each sibling lacks at least one of the finite-net, pointwise-modulus, fold-ledger, or uniform-modulus handoff rows.}

\closureat{CompactNetModulusBudgetUp}{seedStr}

\begin{closurestatus}{\CompactNetModulusBudgetUp}
  \constructivestory{A $\CompactNetModulusBudgetUp$ packet is generated from compact finite-net rows, pointwise-modulus rows, a rational lower-bound fold row, a uniform-modulus handoff row, selector and cover compatibility, componentwise transport, displayed continuation, provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-net-modulus-budget-carrier}, \autoref{thm:compact-net-modulus-budget-namecert-obligations}, and \autoref{thm:compact-net-modulus-budget-handoff-exactness} specify the finite budget packet, its local NameCert obligations, and its downstream handoff exactness.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, bridge checking, open-cover compactness, sequential compactness, host minima, selected external centers, quotient point equality, or external modulus functions for $\CompactNetModulusBudgetUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net exactness, pointwise-modulus exactness, rational-fold exactness, uniform-modulus handoff exactness, nontriviality, and field faithfulness.}
\end{closurestatus}
