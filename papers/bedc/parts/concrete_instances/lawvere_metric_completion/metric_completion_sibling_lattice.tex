The $\LawvereMetricCompletionUp$ route consumes ordinary metric-completion data only after its directed profile rows have already displayed the $\QuasiMetricUp$, $\YonedaCompletionUp$, $\FormalBallUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ path. Its symmetric sibling edge therefore points through the dense embedding of \autoref{ch:concrete-instances-metric-completion-dense-embedding-namecert} and the dense-reflector route of \autoref{ch:concrete-instances-cauchy-completion-dense-reflector-namecert}; neither sibling replaces the directed source row or the one-sided profile classifier.
