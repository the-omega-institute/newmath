Every $\BishopRealLineInterfaceUp$ packet consumes the $\RegSeqRatUp$ readback row of \autoref{ch:concrete-instances-regseqrat-namecert} and the terminal $\RealUp$ seal row of \autoref{ch:concrete-instances-real-namecert} before exporting the real-line interface. The sibling route keeps the L10 dependency explicit: finite dyadic and stream rows are read first, the regular rational sequence row is then admitted as the real-line source, and the Real seal is the last value-facing row available to downstream interface consumers.
