\chapter{A Concrete Naming Certificate for $\AuditGateBoundaryUp$}
\label{ch:concrete-instances-auditgateboundary-namecert}
\origin{human}

The $\AuditGateBoundaryUp$ surface packages the manuscript and Lean audit gates as a finite naming certificate. It is not a proof that the entire repository is complete. It records the exact boundary packets by which source-token scans, axiom-purity reports, paper-Lean target audits, origin and import conservativity ledgers, transport rows, continuation routes, package provenance, GAP boundary rows, and the local naming row remain visible to downstream governance consumers. The displayed audit consumer routes use the finite continuation-trace reading of \autoref{ch:concrete-instances-continuationtrace-namecert} as the sibling surface that makes replay and route inspection explicit.

\begin{definition}[AuditGateBoundary carrier]
\label{def:audit-gate-boundary-carrier}
An $\AuditGateBoundaryUp$ carrier is a finite $\BHist$ packet
$$
A=(S,D,L,O,H,C,P,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source-token scan row, recording the finite file set, token classifier, and refusal result for substrate-internal axiom declarations;
\item $D$ is the axiom-purity dependency report row, recording the theorem names inspected, the reported proof ancestry, and the forbidden dependency classifier for $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$;
\item $L$ is the paper-Lean target audit row, recording each manuscript marker, its named Lean target, and the finite resolution result used by the drift gate;
\item $O$ is the origin and import conservativity report row, recording the chapter-origin ledger, the protected module prefixes, and the import-edge classifier that separates baseline and non-baseline surfaces;
\item $H$ records componentwise $\hsame$ transports for $S,D,L,O,G$, the continuation row, the package row, and the local naming row;
\item $C$ records the displayed $\Cont$ routes by which consumers read the token scan, dependency report, target audit, origin/import report, boundary row, provenance, and local certificate;
\item $P$ is the $\Pkg$ provenance over $\BHist$, $\Hist$, $\mathsf{GAP}$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, Lean target names, source-file scans, dependency reports, and origin ledgers;
\item $G$ is the $\mathsf{GAP}$ boundary row recording that the gates refuse only the displayed boundary crossings and do not provide a hidden theorem of global correctness;
\item $N$ is the local $\NameCert_{\AuditGateBoundaryUp}$ row.
\end{enumerate}
The carrier is exactly this finite packet. It contains no unrestricted semantic conservativity theorem, no host-kernel completeness proof, no hidden permission to use source axioms, no selected Lean declaration beyond the named audit rows, and no global guarantee that paper prose carries a stronger interpretation than the resolving markers display.
\end{definition}
\leandef{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryUp}

\begin{theorem}[AuditGateBoundary NameCert obligations]
\label{thm:audit-gate-boundary-namecert-obligations}
The local $\NameCert_{\AuditGateBoundaryUp}$ surface for the carrier of \autoref{def:audit-gate-boundary-carrier} consists of carrier admission for
$$
A=(S,D,L,O,H,C,P,G,N),
$$
source-token refusal, axiom-purity dependency refusal, paper-Lean target exactness, origin/import conservativity separation, componentwise $\hsame$ stability, displayed $\Cont$ locality, $\Pkg$ provenance, the $\mathsf{GAP}$ boundary row, and non-escape for global semantic conservativity, host-kernel completeness, source axioms, unresolved marker names, and untracked origin flow.
\end{theorem}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is projection from \autoref{def:audit-gate-boundary-carrier}. The rows $S,D,L,O,H,C,P,G,N$ are the only data admitted to the packet.

The first four rows expose the gate faces. The source-token scan is read from $S$, the dependency report from $D$, the paper-Lean target audit from $L$, and the origin/import conservativity report from $O$.

Transport and consumer access are then confined by $H$ and $C$. These rows replay the displayed audits under componentwise $\hsame$ movement and do not add any unrecorded proof principle or import edge.

Finally $P,G,N$ record provenance, the boundary socket, and the local certificate. Since the excluded objects have no coordinate in the carrier, no NameCert projection can export them.
\end{proof}

\begin{theorem}[AuditGateBoundary axiom-purity soundness]
\label{thm:audit-gate-boundary-axiom-purity-soundness}
For an accepted $\AuditGateBoundaryUp$ packet, the source-token row $S$ and dependency report row $D$ are sufficient to decide the audit-facing axiom boundary claimed by the packet. A passed source-token scan means no displayed BEDC source file contributes a substrate-internal axiom declaration, and a passed dependency report means no inspected public theorem row is reported with a forbidden dependency among $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$.

This soundness is local to the reported rows. It does not assert host-kernel completeness, semantic completeness of the theorem inventory, or any theorem outside the finite report carried by $D$.
\end{theorem}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_axiom\_purity\_soundness}
\begin{proof}
Read the source side first. The row $S$ contains the finite file set, token classifier, and refusal result, so the source-axiom boundary claimed by the packet is exactly the boundary displayed by $S$.

Read the theorem-dependency side next. The row $D$ contains the inspected theorem names, their reported proof ancestry, and the forbidden dependency classifier. Therefore the axiom-purity claim is the finite absence result recorded by $D$.

The $\mathsf{GAP}$ row $G$ fixes the scope. The packet certifies only these displayed reports and refuses to turn them into a hidden theorem about all possible host behaviour or every unlisted declaration.
\end{proof}

\begin{theorem}[AuditGateBoundary paper-Lean drift exactness]
\label{thm:audit-gate-boundary-paper-lean-drift-exactness}
For an accepted $\AuditGateBoundaryUp$ packet, the paper-Lean audit row $L$ is the exact public surface for resolving manuscript markers against Lean declarations. A consumer may read a marker as a naming certificate only through the tuple consisting of the marker text, the target name, the finite resolution result, the componentwise transport row, the continuation route, the package provenance, and the local $\NameCert_{\AuditGateBoundaryUp}$ row.

No consumer of $L$ may replace a missing target with a similar theorem, a nearby namespace, a paper-side interpretation, or an unresolved string.
\end{theorem}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_paper\_lean\_drift\_exactness}
\begin{proof}
Project $L$ from the carrier. By definition it records each manuscript marker, the target name being claimed, and the finite resolution result.

Use $H$ and $C$ to transport and read the row. The marker remains the same displayed marker after transport, and the continuation route exposes only the named resolution result.

The non-escape clause follows from the absence of any coordinate for fuzzy matching, namespace guessing, or prose interpretation. A marker therefore functions as a certificate only when $L$ records that the named target resolves.
\end{proof}

\begin{theorem}[AuditGateBoundary origin conservativity]
\label{thm:audit-gate-boundary-origin-conservativity}
For an accepted $\AuditGateBoundaryUp$ packet, the origin/import row $O$ is the exact finite ledger by which non-baseline surfaces are kept from silently entering baseline theorem space. A consumer may use $O$ to read chapter origins, protected module prefixes, inspected import edges, and the pass-or-refuse result, but it may not extract a semantic conservativity theorem for arbitrary extensions or a global proof that all recognized content is baseline content.
\end{theorem}
\begin{proof}
Project $O$ from the carrier. It contains origin rows, protected prefixes, inspected import edges, and the classifier result for those edges.

Transport and replay through $H$ and $C$. The consumer sees the same finite ledger and the same route to the origin/import classifier, with provenance recorded by $P$ and local naming recorded by $N$.

The boundary row $G$ prevents overreading. Since $O$ has no coordinate for arbitrary model-theoretic conservativity or for reclassifying non-baseline material as baseline, the packet certifies only the displayed import separation.
\end{proof}

\begin{theorem}[AuditGateBoundary source-token refusal]
\label{thm:audit-gate-boundary-source-token-refusal}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
the row $S$ is the only public source-token refusal face. Every consumer read of a source-token refusal factors through $S$ together with the componentwise transport row $H$, the displayed continuation routes $C$, the package provenance $P$, and the local naming row $N$.

The read does not expose a hidden source proof, a host-kernel axiom inventory, or a refusal predicate outside the finite file set and token classifier recorded by $S$.
\end{theorem}
\begin{proof}
Project the accepted packet. The source-token scan row is the coordinate $S$, while $D,L,O,G$ record different audit faces and cannot be substituted for the source-token refusal row.

Route the consumer access through $H$ and $C$. The transport row preserves the displayed scan componentwise, and the continuation row exposes exactly the route from the packet to the token classifier and refusal result carried by $S$.

The package row $P$ and local name row $N$ bind this route to the $\AuditGateBoundaryUp$ certificate. The $\mathsf{GAP}$ row $G$ then fixes the non-escape boundary: the packet refuses only the displayed source-token crossing and supplies no host-wide source theorem.
\end{proof}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_source\_token\_refusal}

\begin{theorem}[AuditGateBoundary forbidden-dependency ledger]
\label{thm:audit-gate-boundary-dependency-forbidden-set}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
the dependency row $D$ fixes the finite forbidden-dependency classifier for $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$. A consumer may read only the theorem names inspected, the reported proof ancestry, and the pass-or-refuse result carried by $D$, transported through $H$ and routed through $C$.

This ledger does not claim host-kernel completeness, proof-irrelevance completeness, or a dependency result for declarations absent from the finite report.
\end{theorem}
\begin{proof}
Project $D$ from the packet. Its coordinates are the inspected theorem names, reported proof ancestry, and the classifier for $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$.

Transport is componentwise through $H$. The dependency report remains the same finite classifier row after the accepted $\hsame$ movement, and the continuation row $C$ is the only consumer route back to that report.

Finally use $P,N,G$ to close the boundary. Provenance and local naming identify the certificate surface, while the $\mathsf{GAP}$ row prevents the finite dependency report from becoming a hidden host-kernel completeness theorem.
\end{proof}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_dependency\_forbidden\_set}

\begin{theorem}[AuditGateBoundary marker-resolution non-escape]
\label{thm:audit-gate-boundary-marker-resolution-nonescape}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
the paper-Lean row $L$ resolves manuscript markers only by exact target names. After componentwise transport through $H$ and consumer routing through $C$, the public certificate may expose the marker text, the named Lean target, and the finite resolution result recorded by $L$.

It cannot replace a non-resolving marker by a nearby namespace, an unresolved string, a prose interpretation, or a theorem whose name is not the exact target recorded in $L$.
\end{theorem}
\begin{proof}
Project the marker row $L$ from the accepted packet. The row records the manuscript marker, the claimed Lean target name, and the finite resolution result.

Replay the read through $H$ and $C$. Componentwise transport preserves the displayed marker row, and the continuation route exposes only the exact target-name resolution already present in $L$.

The remaining coordinates $P,G,N$ supply provenance, the boundary row, and local naming. Since the carrier has no coordinate for fuzzy namespace search, unresolved-string substitution, or prose interpretation, marker resolution cannot escape the exact-name ledger.
\end{proof}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_marker\_resolution\_nonescape}

\begin{theorem}[AuditGateBoundary origin-gate replay]
\label{thm:audit-gate-boundary-origin-gate-replay}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
the displayed audit replay of origin conservativity is exactly the origin/import row $O$ transported through $H$, routed through $C$, named by $N$, and packaged by $P$. A governance consumer may replay chapter-origin rows, protected module prefixes, inspected import edges, and the pass-or-refuse classifier, but it receives no semantic conservativity theorem beyond the finite $\BHist$ row carried by $O$.
\end{theorem}
\begin{proof}
Project the carrier row $O$. By \autoref{def:audit-gate-boundary-carrier}, this row is the packet's origin and import conservativity report.

Apply \autoref{thm:audit-gate-boundary-origin-conservativity}. The consumer may inspect exactly the displayed origin rows, protected prefixes, import edges, and classifier result.

Replay the row through the common support surface. The transport row $H$ preserves $O$ componentwise, the continuation row $C$ exposes it, and $P,N$ bind provenance and local naming.

The replay ledger of \autoref{thm:audit-gate-boundary-replay-ledger} keeps this read inside the audit packet. Therefore the replay exports the finite origin gate row and not a host-wide conservativity theorem.
\end{proof}

\begin{theorem}[AuditGateBoundary strict axiom-purity replay]
\label{thm:audit-gate-boundary-strict-axiom-purity-replay}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
strict axiom-purity replay is the joint read of the source-token row $S$ and forbidden-dependency row $D$ through $H,C,P,G,N$. The replay exposes the finite source-token refusal, the inspected theorem names, their reported proof ancestry, and the forbidden-dependency classifier for $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$, with no hidden host-kernel completeness claim.
\end{theorem}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_strict\_axiom\_purity\_replay}
\begin{proof}
Project the rows $S$ and $D$ from the accepted packet. The source-token row records file and token refusal data, while the dependency row records theorem names, ancestry, and forbidden dependency classification.

Use \autoref{thm:audit-gate-boundary-axiom-purity-soundness} and \autoref{thm:audit-gate-boundary-source-token-refusal}. These theorems bind the audit-facing axiom boundary to the finite source-token and dependency rows.

Use \autoref{thm:audit-gate-boundary-dependency-forbidden-set} for the strict forbidden set. The only named forbidden dependencies are $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$ inside the displayed report.

Finally replay through $H,C,P,G,N$. Transport, continuation, provenance, GAP boundary, and naming keep the result inside the same $\BHist$ packet and block any host-wide completeness reading.
\end{proof}

\begin{theorem}[AuditGateBoundary marker-resolution closure]
\label{thm:audit-gate-boundary-marker-resolution-closure}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
the marker-resolution row $L$ is closed under audit replay through the displayed ledger. A governance consumer replaying $L$ may recover only the manuscript marker text, exact Lean target name, finite resolution result, componentwise $\hsame$ transport, continuation route, $\Pkg$ provenance, GAP boundary, and local $\NameCert$ row.
\end{theorem}
\begin{proof}
Project the paper-Lean row $L$ from the accepted carrier. This row contains the marker text, named Lean target, and finite resolution result.

Apply \autoref{thm:audit-gate-boundary-marker-resolution-nonescape}. Exact target names are preserved, and the row cannot be replaced by namespace search, unresolved strings, prose interpretation, or a different theorem name.

Replay $L$ through the ledger of \autoref{thm:audit-gate-boundary-replay-ledger}. The same support rows $H,C,P,G,N$ transport, expose, package, delimit, and name the marker-resolution read.

Thus marker resolution is closed only as a finite audit-row replay. It does not become a broader manuscript-validity theorem or a substitute for resolving the exact target recorded in $L$.
\end{proof}

\begin{theorem}[AuditGateBoundary replay ledger]
\label{thm:audit-gate-boundary-replay-ledger}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
any governance consumer replaying the packet must read the source-token row $S$, forbidden-dependency row $D$, marker-resolution row $L$, and origin/import row $O$ through the same transport, continuation, provenance, GAP-boundary, and naming support rows $H,C,P,G,N$. The replay cannot replace exact marker resolution, forbidden dependency reports, source-token refusal, or origin/import separation by a host-wide correctness claim.
\end{theorem}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_replay\_ledger}
\begin{proof}
Project the carrier of \autoref{def:audit-gate-boundary-carrier}. Its four audit-facing rows are exactly $S,D,L,O$, and the shared support rows are exactly $H,C,P,G,N$.

Apply the local NameCert obligations to bind those rows to one accepted packet. Source-token refusal uses $S$ through the same transport and continuation coordinates, while the dependency forbidden set uses $D$ to report only the displayed ancestry and forbidden names.

Next replay marker resolution and origin separation. The marker-resolution non-escape theorem keeps $L$ on exact target names, and origin conservativity keeps $O$ on the displayed origin/import ledger.

Finally close the replay through $P,G,N$. Provenance, the GAP boundary, and local naming prevent the four audit faces from becoming a global repository-correctness theorem, so a governance consumer receives only the finite replay ledger carried by $A$.
\end{proof}

\begin{theorem}[AuditGateBoundary origin-import replay exactness]
\label{thm:audit-gate-boundary-origin-import-replay-exactness}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
origin and import replay is exact only as the row $O$ transported through $H$, routed through $C$, packaged by $P$, delimited by $G$, and named by $N$. A governance consumer may replay chapter-origin rows, protected module prefixes, inspected import edges, and their pass-or-refuse classifier, but it may not replace them by a host-wide semantic conservativity claim.
\end{theorem}
\begin{proof}
Project the carrier row $O$. It is the only coordinate that records chapter origins, protected module prefixes, inspected import edges, and the origin/import classifier.

Replay proceeds through the shared support rows. The transport row $H$ preserves the displayed origin/import ledger, the continuation row $C$ exposes it, and the package and local naming rows $P,N$ bind the replay to the same certificate.

The boundary row $G$ fixes the scope of the replay. Since the carrier contains no coordinate for host-wide semantic conservativity, the exact replay exports only the finite origin/import row.
\end{proof}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_origin\_import\_replay\_exactness}

\begin{theorem}[AuditGateBoundary drift row transport]
\label{thm:audit-gate-boundary-drift-row-transport}
For an accepted $\AuditGateBoundaryUp$ packet, the paper-Lean drift row $L$ is transported exactly as a finite marker-resolution ledger. Every transported read of $L$ exposes only the manuscript marker text, the exact Lean target name, the finite resolution result, the componentwise $\hsame$ row $H$, the displayed $\Cont$ route $C$, the $\Pkg$ provenance row $P$, the $\mathsf{GAP}$ boundary row $G$, and the local $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
Project $L$ from the accepted packet. The row records marker text, target name, and finite resolution result, and it contains no fuzzy target selector.

Transport the row componentwise through $H$. The transported ledger is still the same exact-name marker-resolution row, so the consumer cannot replace it by a nearby namespace or prose interpretation.

Route and package the transported row through $C,P,G,N$. The replay ledger and marker-resolution non-escape theorems then keep the read inside the finite audit packet, with no host-wide correctness claim attached to the transported drift row.
\end{proof}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_drift\_row\_transport}

\begin{theorem}[AuditGateBoundary axiom-purity report non-escape]
\label{thm:audit-gate-boundary-axiom-purity-report-nonescape}
For an accepted $\AuditGateBoundaryUp$ packet, the axiom-purity report is the joint finite read of the source-token row $S$ and dependency row $D$ through $H,C,P,G,N$. It reports only the displayed source-token refusal, inspected theorem names, reported proof ancestry, and forbidden-dependency classifier for $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$. It cannot be replaced by host-kernel completeness, theorem inventory completeness, global proof purity, or a semantic repository-correctness claim.
\end{theorem}
\begin{proof}
Project the source and dependency rows. The source-token refusal is carried by $S$, while the dependency report and forbidden-dependency classifier are carried by $D$.

Use source-token refusal, forbidden-dependency ledger, and strict axiom-purity replay. Together they identify the axiom-facing report as exactly the finite pair of rows $S,D$ plus the shared support rows.

Finally inspect the carrier boundary. The rows $H,C,P,G,N$ preserve, expose, package, delimit, and name the finite report, and no coordinate records host-kernel completeness, theorem inventory completeness, global proof purity, or semantic repository correctness. The report therefore cannot escape the exact audit rows.
\end{proof}

\begin{theorem}[AuditGateBoundary obligation closure surface]
\label{thm:audit-gate-boundary-obligation-closure}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
the local obligation surface is the joint assembly of the source-token scan $S$, strict axiom-purity report $D$, paper-Lean drift exactness row $L$, origin-conservativity row $O$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, GAP boundary $G$, and local $\NameCert_{\AuditGateBoundaryUp}$ row $N$. Every governance-facing audit claim for the packet factors through these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:audit-gate-boundary-carrier}. The rows $S,D,L,O,H,C,P,G,N$ are the complete public packet, so carrier admission contributes no additional audit face.

Use the existing source and dependency rows for the axiom side. The source-token refusal theorem binds $S$, while strict axiom-purity replay and axiom-purity report non-escape bind $D$ to the displayed forbidden-dependency classifier.

Use the existing drift and origin rows for the manuscript side. Marker-resolution closure and drift row transport bind $L$, while origin-import replay exactness binds $O$ to the finite origin/import ledger.

Finally replay the shared support rows. The replay ledger keeps $H,C,P,G,N$ as transport, route, provenance, boundary, and naming support for the same packet, so no hidden global correctness claim is exported.
\end{proof}

\begin{theorem}[AuditGateBoundary ledger exactness]
\label{thm:audit-gate-boundary-ledger-exactness}
For an accepted $\AuditGateBoundaryUp$ packet, the audit ledger is exact: $S$ is the only source-token refusal row, $D$ is the only strict axiom-purity report row, $L$ is the only exact marker-resolution row, and $O$ is the only origin/import conservativity row. The rows $H,C,P,G,N$ may transport, route, package, delimit, and name these four ledgers, but they cannot replace them by fuzzy Lean target matching, source-axiom admission, forbidden host dependency acceptance, or a host-wide semantic correctness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryCarrier\_ledger\_exactness}
\begin{proof}
Read the four audit ledgers from the accepted carrier. Source-token refusal uses $S$, forbidden-dependency classification uses $D$, marker resolution uses $L$, and origin/import separation uses $O$.

Apply the corresponding exactness theorems. Source-token refusal, forbidden-dependency ledger, marker-resolution non-escape, and origin-import replay exactness identify the public content of the four rows.

The support rows do not create substitute ledgers. They only preserve, expose, package, delimit, and name the displayed rows; the GAP boundary blocks fuzzy target matching, source-axiom admission, forbidden host dependency acceptance, and hidden semantic correctness.
\end{proof}

\begin{theorem}[AuditGateBoundary obligation non-escape]
\label{thm:audit-gate-boundary-nonescape}
For an accepted $\AuditGateBoundaryUp$ packet, the obligation closure surface exports no hidden global correctness claim. Its public reads are exactly source-token refusal, strict axiom-purity reporting, paper-Lean exact target resolution, origin/import separation, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, GAP boundary, and local naming; no read yields fuzzy Lean target matching, source-axiom admission, forbidden host dependency acceptance, theorem-inventory completeness, host-kernel completeness, or semantic conservativity for arbitrary imports.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:audit-gate-boundary-obligation-closure}. It assembles only the carrier rows $S,D,L,O,H,C,P,G,N$.

Use ledger exactness to inspect every public read. Each governance claim is forced back to the displayed source-token, dependency, marker-resolution, or origin/import row, with the shared support rows providing only transport and replay.

The excluded claims have no coordinate in the carrier. Since the GAP boundary is part of the same packet, the surface cannot export fuzzy target matching, source-axiom admission, forbidden host dependency acceptance, inventory completeness, host-kernel completeness, or arbitrary-import conservativity.
\end{proof}

\begin{theorem}[AuditGateBoundary kernel-query scope]
\label{thm:audit-gate-boundary-kernel-query-scope}
For an accepted $\AuditGateBoundaryUp$ packet
$$
A=(S,D,L,O,H,C,P,G,N),
$$
the source-token row $S$ and the dependency report row $D$ are the only rows by which source-token reads and axiom-query reads enter the boundary. Every such read must then pass through the shared support rows $H,C,P,G,N$ before it can be consumed as an $\AuditGateBoundaryUp$ verdict surface.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:audit-gate-boundary-carrier}. The row $S$ is the only source-token scan row, and the row $D$ is the only axiom-purity dependency report row.

Apply \autoref{thm:audit-gate-boundary-axiom-purity-soundness}. It binds the audit-facing axiom boundary to the finite pair consisting of the source-token scan and the dependency report.

Apply \autoref{thm:audit-gate-boundary-source-token-refusal} for the source side and \autoref{thm:audit-gate-boundary-dependency-forbidden-set} for the query side. These theorems route the two reads through $H,C,P,G,N$ and leave $L$ and $O$ as distinct audit faces.

Finally use \autoref{thm:audit-gate-boundary-nonescape}. Since no other carrier row can introduce source-token or axiom-query content, the kernel-query scope is exactly $S,D$ plus the shared support rows.
\end{proof}

\begin{theorem}[AuditGateBoundary forbidden-dependency scope]
\label{thm:audit-gate-boundary-forbidden-dependency-scope}
For an accepted $\AuditGateBoundaryUp$ packet, the forbidden-dependency classifier is exactly the displayed row for $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$ transported through $H$ and routed through $C$. No consumer may replace that finite classifier by a broader host-dependency predicate, a theorem-inventory completeness claim, or a different forbidden-name set.
\end{theorem}
\begin{proof}
Start from the dependency row $D$ in \autoref{def:audit-gate-boundary-carrier}. Its displayed classifier names exactly $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$.

Use \autoref{thm:audit-gate-boundary-dependency-forbidden-set}. It fixes the inspected theorem names, reported proof ancestry, and pass-or-refuse result as the content carried by $D$.

Now apply \autoref{thm:audit-gate-boundary-axiom-purity-soundness}. The strict axiom-facing conclusion is local to the finite dependency report, so transport through $H$ and replay through $C$ cannot widen the forbidden set.

The non-escape theorem \autoref{thm:audit-gate-boundary-nonescape} closes the boundary. It excludes host-kernel completeness and theorem-inventory completeness, leaving only the displayed three-name classifier.
\end{proof}

\begin{theorem}[AuditGateBoundary verdict scope]
\label{thm:audit-gate-boundary-verdict-scope}
Every verdict read from an accepted $\AuditGateBoundaryUp$ packet factors through
$$
S,D,L,O,H,C,P,G,N.
$$
The verdict may combine source-token refusal, strict dependency reporting, exact marker resolution, and origin/import separation, but it cannot be replaced by an ambient host claim about repository correctness, theorem inventory completeness, source admission, or semantic conservativity.
\end{theorem}
\begin{proof}
Project the carrier. The public audit faces are $S,D,L,O$, and the support rows are $H,C,P,G,N$.

Use \autoref{thm:audit-gate-boundary-source-token-refusal} and \autoref{thm:audit-gate-boundary-dependency-forbidden-set} for the source and dependency verdict components. Use \autoref{thm:audit-gate-boundary-axiom-purity-soundness} to bind their joint axiom-facing reading.

The remaining rows stay inside the same accepted packet. The carrier definition provides marker-resolution and origin/import rows, while the support rows transport, replay, package, delimit, and name the verdict surface.

Apply \autoref{thm:audit-gate-boundary-nonescape}. It blocks ambient host claims and leaves the verdict as a finite read through $S,D,L,O,H,C,P,G,N$ alone.
\end{proof}

\begin{theorem}[AuditGateBoundary standard bridge]
\label{thm:audit-gate-boundary-standard-bridge}
The accepted $\AuditGateBoundaryUp$ packet forms the standard
audit-boundary schema: the public source-token row, axiom-purity dependency
row, paper-Lean drift row, origin/import separation row, replay ledger, GAP
boundary row, and local $\NameCert$ row are carried by one finite
$\mathsf{BHist}$ surface and transported through the shared $H,C,P$ support
rows. The schema exports exactly that audit-boundary verdict surface. It
exports no repository completeness theorem, theorem-inventory completeness
claim, host semantic correctness statement, or semantic conservativity result
for arbitrary imports.
\end{theorem}
\leanchecked{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryUp\_StdBridge}
\begin{proof}
Project the carrier of \autoref{def:audit-gate-boundary-carrier}. Its public
audit faces are the source-token scan, axiom-purity dependency report,
paper-Lean target audit, and origin/import separation ledger, and its support
faces are the replay, transport, package, GAP, and local naming rows.

Apply \autoref{thm:audit-gate-boundary-verdict-scope}. Every verdict read is
therefore a finite read through the displayed audit rows and the shared support
rows.

Now use \autoref{thm:audit-gate-boundary-kernel-query-scope} and
\autoref{thm:audit-gate-boundary-forbidden-dependency-scope} for the
source-token and forbidden-dependency components. Use the replay and
origin/import rows named in the carrier for the remaining audit faces. These
are precisely the rows required by the standard audit-boundary schema.

Finally apply \autoref{thm:audit-gate-boundary-nonescape}. The excluded host
claims have no carrier coordinate, so the bridge is only the finite
audit-boundary schema and not a repository-completeness or host-correctness
theorem.
\end{proof}

\begin{theorem}[AuditGateBoundary visible verdict exhaustion]
\label{thm:audit-gate-boundary-visible-verdict-exhaustion}
Every audit verdict read of an accepted $\AuditGateBoundaryUp$ packet
$A=(S,D,L,O,H,C,P,G,N)$ is exhausted by the displayed source-token,
dependency, marker-resolution, origin/import, transport, continuation,
provenance, GAP, and local $\NameCert$ rows. The verdict may report only the
finite surface carried by those rows. It does not assert repository
completeness, theorem-inventory completeness, host-kernel correctness, or a
semantic correctness theorem for arbitrary imports.
\end{theorem}
\begin{proof}
First apply \autoref{thm:audit-gate-boundary-verdict-scope}. It factors every
verdict read through $S,D,L,O,H,C,P,G,N$, so the visible verdict already has a
finite carrier surface.

Next apply \autoref{thm:audit-gate-boundary-kernel-query-scope} and
\autoref{thm:audit-gate-boundary-forbidden-dependency-scope}. These identify
the source-token query and the forbidden-dependency report as the displayed
audit rows, rather than as host-wide kernel assertions.

Use \autoref{thm:audit-gate-boundary-standard-bridge} to package the
source-token, dependency, marker-resolution, origin/import, transport,
continuation, provenance, GAP, and local naming rows as one standard
audit-boundary surface.

Finally apply \autoref{thm:audit-gate-boundary-nonescape}. The non-escape row
removes repository completeness, theorem-inventory completeness,
host-kernel correctness, and semantic correctness for arbitrary imports, so the
visible verdict is exhausted by the displayed packet alone.
\end{proof}

\begin{theorem}[AuditGateBoundary formal-axis refusal]
\label{thm:audit-gate-boundary-formal-axis-refusal}
An $\AuditGateBoundaryUp$ mature paper packet exports only the displayed audit-gate carrier, classifier, refusal, transport, $\Cont$, $\Pkg$, and $\NameCert$ rows, together with the named Lean theorem target
$$
\texttt{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryUp\_StdBridge}.
$$
This supports a $\bridgeCheckedV$ verification-axis row for that declaration alone. It does not create host-kernel completeness, semantic conservativity for arbitrary imports, or theorem-inventory completeness.
\end{theorem}
\begin{proof}
First read the mature paper surface through the chapter theorems. The carrier, NameCert obligations, source-token refusal, forbidden-dependency scope, marker-resolution boundary, origin-import replay, standard bridge row, non-escape row, and visible verdict exhaustion theorem all operate on the displayed packet rows.

Next apply the two-axis discipline of \autoref{ch:closure-and-verification-discipline}. The theory-axis grade records what this paper packet closes, while the formal verification axis is supplied only by Lean markers and closurestatus targets that resolve to real declarations.

The closurestatus target names the existing Lean declaration for the standard audit-boundary bridge. Therefore the packet may report the corresponding checked-bridge row, while host-kernel completeness, semantic conservativity for arbitrary imports, and theorem-inventory completeness remain outside the packet.
\end{proof}

\closureat{AuditGateBoundaryUp}{seedStr}
\closureat{AuditGateBoundaryUp}{obligationStr}
\closureat{AuditGateBoundaryUp}{scopedStr}
\closureat{AuditGateBoundaryUp}{publicStr}
\closureat{AuditGateBoundaryUp}{bridgedStr}
\closureat{AuditGateBoundaryUp}{matureStr}

\begin{closurestatus}{\AuditGateBoundaryUp}
  \constructivestory{An $\AuditGateBoundaryUp$ packet is generated from a source-token scan, an axiom-purity dependency report, a paper-Lean target audit, an origin/import separation ledger, finite $\hsame$ transport, visible $\Cont$ routes, package provenance, a GAP boundary row, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The public packet binds the carrier, NameCert obligations, source-token refusal, forbidden-dependency scope, marker-resolution non-escape, origin-import replay exactness, replay ledger, \autoref{thm:audit-gate-boundary-verdict-scope}, \autoref{thm:audit-gate-boundary-standard-bridge}, \autoref{thm:audit-gate-boundary-visible-verdict-exhaustion}, and the replay and non-escape rows to one finite audit-boundary surface.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.AuditGateBoundaryUp.AuditGateBoundaryUp\_StdBridge}
  \bridgestatus{bridgeChecked}
  \origin{human}
  \notclaimed{This chapter does not close host-kernel completeness, semantic conservativity for arbitrary imports, or theorem inventory completeness.}
  \upgradepath{No further formal-axis upgrade remains for the checked standard bridge.}
\end{closurestatus}
