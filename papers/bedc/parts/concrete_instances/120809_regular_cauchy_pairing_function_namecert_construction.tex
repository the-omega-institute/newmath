\chapter{A Concrete Naming Certificate for $\RegularCauchyPairingFunctionUp$}
\label{ch:concrete-instances-regular-cauchy-pairing-function-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\RegularCauchyPairingFunctionUp$ packet records the finite pairing and unpairing interface used when a regular Cauchy construction must fold a finite precision window into one unary request and then recover the two visible sides. It sits over the $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the diagonal route of \autoref{ch:concrete-instances-regular-cauchy-diagonalization-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite NameCert for precision-window pairing; it is not a host pairing oracle, quotient equality of streams, selected real representatives, countable choice, or ambient completeness of $\RealUp$.

\begin{definition}[Regular Cauchy pairing-function carrier]
\label{def:regular-cauchy-pairing-function-carrier}
A $\RegularCauchyPairingFunctionUp$ carrier is a finite $\BHist$ packet
$$
K=(P,U,V,W,D,R,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the unary pairing row. It records the finite code by which two precision requests are packed into one request index.
\item $U$ and $V$ are the first and second unpairing rows. They recover the two request windows from the displayed code $P$ and do not read an ambient host pairing function.
\item $W$ is the finite $\StreamNameUp$ window row inspected after $U$ and $V$ expose the two sides of the paired request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance-transport ledger. It records the dyadic radius, split budget, slack comparison cells, and common-scale data transported across the two unpaired request windows.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the paired window and dyadic ledger.
\item $H$ records componentwise $\hsame$ transport for the pairing code, unpairing rows, StreamName windows, dyadic tolerance transport, regular readback, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a finite precision request through $P,U,V,W,D,R,H,G,N$.
\item $G$ is the $\Pkg$ provenance over $\RegularCauchyPairingFunctionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RegularCauchyDiagonalizationUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyPairingFunctionUp}$ row naming the finite pairing-function surface.
\end{enumerate}
The classifier compares accepted packets only through $P,U,V,W,D,R,H,C,G,N$. It has no coordinate for hidden host pairing functions, quotient stream equality, selected Real representatives, countable-choice schedules, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy pairing-function NameCert obligations]
\label{thm:regular-cauchy-pairing-function-namecert-obligations}
For every accepted $\RegularCauchyPairingFunctionUp$ carrier
$$
K=(P,U,V,W,D,R,H,C,G,N),
$$
the local $\NameCert_{\RegularCauchyPairingFunctionUp}$ surface consists exactly of carrier admission for the unary pairing row, the two unpairing rows, the finite StreamName window, the DyadicRatCore tolerance-transport ledger, the RegSeqRat readback, componentwise transport, displayed replay, package provenance, and local naming; pairing exactness for $P$; unpairing exactness for $U,V$; window exactness for $W$; tolerance transport for $D$; readback exactness for $R$; and ledger refusal for host pairing, quotient equality, selected representatives, countable choice, trichotomy, host real equality, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-pairing-function-carrier}. The accepted tuple displays exactly $P,U,V,W,D,R,H,C,G,N$.

Pairing exactness is the row $P$. The two unpairing rows $U$ and $V$ consume the displayed code and recover the two finite precision windows before any StreamName or RegSeqRat readback is public.

Window and tolerance exactness are the rows $W$ and $D$. The StreamName row reads only the unpaired finite windows, and the dyadic ledger transports the requested tolerance and common-scale data across those two windows.

The readback row $R$ is downstream of the same route. The structural rows $H,C,G,N$ transport, replay, source, and name the finite packet. Since the carrier contains no hidden host pairing function, quotient stream equality, selected representative, countable-choice schedule, trichotomy, host equality, or ambient completeness coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy pairing-window exactness]
\label{thm:regular-cauchy-pairing-window-exactness}
Every $\RegSeqRatUp$, $\RegularCauchyDiagonalizationUp$, or $\RealUp$ consumer of an accepted $\RegularCauchyPairingFunctionUp$ packet must read the finite route
$$
P \longmapsto U,V \longmapsto W \longmapsto D \longmapsto R \longmapsto H,C,G,N .
$$
Thus the public readback receives only a displayed unary pairing code, its two displayed unpairing rows, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance transport, one $\RegSeqRatUp$ readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:regular-cauchy-pairing-function-namecert-obligations}. They force every accepted consumer to expose $P$ before $U,V$, to expose $U,V$ before $W$, and to expose $W$ before the dyadic transport ledger $D$.

The regular readback is then row-local. The row $R$ consumes exactly the displayed window and tolerance rows; it cannot replace them by a host pairing function or by an unbounded search over request pairs.

Finally, $H,C,G,N$ preserve and name the same finite route. The downstream diagonal and Real consumers therefore inherit the paired precision window as displayed BEDC data, with no quotient stream equality, selected representative, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness exported by the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyPairingFunctionUp$ read displays one unary pairing row, two unpairing rows, one finite $\StreamNameUp$ window row, one $\DyadicRatCoreUp$ tolerance-transport ledger, one $\RegSeqRatUp$ readback row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence before a diagonal, completion, or Real-facing consumer may read the paired precision window.}

\independenceWitness{streamname, regseqrat, dyadicratcore, regular-cauchy-diagonalization, real: $\RegularCauchyPairingFunctionUp$ is not bijective to any listed sibling because it jointly carries a unary pairing row, two unpairing rows, a finite StreamName window, a DyadicRatCore tolerance-transport ledger, a RegSeqRat readback, and the structural transport, replay, provenance, and local naming rows.}

\closureat{RegularCauchyPairingFunctionUp}{seedStr}

\begin{closurestatus}{\RegularCauchyPairingFunctionUp}
  \constructivestory{A $\RegularCauchyPairingFunctionUp$ packet is generated from one unary pairing row, two unpairing rows, a finite StreamName window, a DyadicRatCore tolerance-transport ledger, a RegSeqRat readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-pairing-function-carrier}, \autoref{thm:regular-cauchy-pairing-function-namecert-obligations}, and \autoref{thm:regular-cauchy-pairing-window-exactness} seed the finite precision-window pairing route used by RegSeqRat, diagonalization, completion, and Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected representatives, countable choice, trichotomy, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, pairing exactness, unpairing exactness, finite-window exactness, dyadic tolerance transport, readback exactness, transport stability, replay stability, provenance exhaustion, and local naming for BEDC.Derived.RegularCauchyPairingFunctionUp.}
\end{closurestatus}
