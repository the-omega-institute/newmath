\chapter{A Concrete Naming Certificate for $\HyperbolicCrossRatioPhaseUp$}
\label{ch:concrete-instances-hyperbolic-cross-ratio-phase-namecert}
\origin{ai}

The $\HyperbolicCrossRatioPhaseUp$ packet realizes the boundary cross-ratio phase branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as one finite NameCert surface. It sits after the hyperbolic metric packet of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the visual-boundary route of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the Busemann boundary route of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, and the Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet records phase as finite boundary cross-ratio data with displayed metric, boundary, shadow, transport, replay, provenance, and local naming rows; it is not a selected ideal endpoint, quotient boundary equality, host Mobius geometry, or ambient real-valued phase theory.

\falsifiablePrediction{An accepted $\HyperbolicCrossRatioPhaseUp$ read is refuted if a consumer exports a boundary cross-ratio phase without first displaying HyperbolicMetric, visual-boundary, Busemann-boundary, boundary-shadow, Real-seal, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{hyperbolicmetric, hyperbolicvisualboundary, hyperbolicbusemannboundary, hyperbolicboundaryshadow, real: none is bijective to $\HyperbolicCrossRatioPhaseUp$ because this packet jointly records metric scale, visual-boundary endpoints, Busemann calibration, shadow incidence, phase readback, Real sealing, transport, replay, provenance, and local NameCert rows.}

\begin{definition}[Hyperbolic cross-ratio phase carrier]
\label{def:hyperbolic-cross-ratio-phase-carrier}
A $\HyperbolicCrossRatioPhaseUp$ carrier is a finite $\BHist$ packet
$$
X=(M,V,B,S,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicMetricUp$ row, naming the finite metric-read surface used by the boundary comparison.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row, naming the displayed visual-boundary points visible to the packet.
\item $B$ is the $\HyperbolicBusemannBoundaryUp$ row, calibrating the boundary reads through Busemann data.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row, recording the shadow incidence attached to the same boundary data.
\item $Q$ is the finite cross-ratio phase row, recording the ordered boundary quadruple and its phase readback.
\item $R$ is the $\RealUp$ seal row reached only after $M,V,B,S,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for metric, visual-boundary, Busemann, shadow, phase, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a boundary-phase request through metric readback, visual-boundary exposure, Busemann calibration, shadow incidence, phase readback, and Real sealing.
\item $P$ records $\Pkg$ provenance over $\HyperbolicCrossRatioPhaseUp$, $\HyperbolicMetricUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicBoundaryShadowUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicCrossRatioPhaseUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,V,B,S,Q,R,H,C,P,N$. It has no coordinate for selected ideal endpoints, quotient boundary equality, host Mobius transformations, hidden phase normalization, or ambient real-valued geometry.
\end{definition}

\begin{theorem}[Hyperbolic cross-ratio phase NameCert obligations]
\label{thm:hyperbolic-cross-ratio-phase-namecert-obligations}
For the carrier of \autoref{def:hyperbolic-cross-ratio-phase-carrier}, the local $\NameCert_{\HyperbolicCrossRatioPhaseUp}$ surface consists exactly of carrier admission for
$$
X=(M,V,B,S,Q,R,H,C,P,N),
$$
metric-read exposure through $M$, visual-boundary exposure through $V$, Busemann calibration through $B$, shadow incidence through $S$, cross-ratio phase readback through $Q$, Real sealing through $R$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-cross-ratio-phase-carrier}. Its visible rows are exactly $M,V,B,S,Q,R,H,C,P,N$.

The boundary comparison side is read through $M,V,B,S,Q$. The metric row fixes the finite scale surface, the visual-boundary row names the boundary entries, the Busemann row calibrates them, the shadow row records incidence, and the phase row reads the ordered cross-ratio datum.

The Real seal and structural rows are downstream. The row $R$ seals the displayed phase readback, while $H,C,P,N$ preserve, replay, source, and name the same route. Since the carrier has no coordinate for selected endpoints, quotient boundary equality, host Mobius transformations, hidden phase normalization, or ambient real-valued geometry, the listed rows exhaust the obligations.
\end{proof}

The Lean \texttt{TasteGate.lean} development for $\HyperbolicCrossRatioPhaseUp$ must include explicit $\mathsf{Nontrivial}\ \HyperbolicCrossRatioPhaseUp$ and $\mathsf{FieldFaithful}\ \HyperbolicCrossRatioPhaseUp$ instances. These instances must preserve the hyperbolic metric, visual-boundary, Busemann-boundary, boundary-shadow, cross-ratio phase, Real-seal, transport, replay, provenance, and local naming rows.

\closureat{HyperbolicCrossRatioPhaseUp}{seedStr}

\begin{closurestatus}{\HyperbolicCrossRatioPhaseUp}
  \constructivestory{A $\HyperbolicCrossRatioPhaseUp$ packet is generated from HyperbolicMetric rows, visual-boundary rows, Busemann-boundary calibration, boundary-shadow incidence, cross-ratio phase readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-cross-ratio-phase-carrier} and \autoref{thm:hyperbolic-cross-ratio-phase-namecert-obligations} seed the finite boundary cross-ratio phase route for hyperbolic observer-expansion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected ideal endpoints, quotient boundary equality, host Mobius geometry, hidden phase normalization, or ambient real-valued geometry.}
  \upgradepath{Obligation closure requires checked carrier admission, metric exposure, visual-boundary exposure, Busemann calibration, shadow incidence, phase readback, Real-seal nonescape, nontriviality, and field faithfulness for BEDC.Derived.HyperbolicCrossRatioPhaseUp.}
\end{closurestatus}
