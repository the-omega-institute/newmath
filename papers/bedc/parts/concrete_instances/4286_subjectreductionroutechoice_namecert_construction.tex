\chapter{A Concrete Naming Certificate for $\SubjectReductionRouteChoiceUp$}
\label{ch:concrete-instances-subject-reduction-route-choice-namecert}
\origin{ai}

The $\SubjectReductionRouteChoiceUp$ packet records the finite choice point between explicit bundle consumption and the checked obstruction ledger of \autoref{ch:sr-discharge-routes}. It reads the discharge bundle surface of \autoref{ch:concrete-instances-subjectreductiondischarge-namecert}, the socket boundary of \autoref{ch:concrete-instances-subject-reduction-discharge-socket-namecert}, and the downstream subject-reduction consumer of \autoref{ch:concrete-instances-subjectreduction-namecert}; it does not choose or discharge the missing closed-CIC obstruction.

\begin{definition}[SubjectReductionRouteChoice carrier]
\label{def:subject-reduction-route-choice-carrier}
A $\SubjectReductionRouteChoiceUp$ carrier is a finite $\BHist$ packet
$$
R=(B,V,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the explicit bundle row naming the $\mathrm{SubjectReductionDischargeBundle}$ shape and its four discharge coordinates;
\item $V$ is the bundle-consumption row corresponding to $\mathrm{subject\_reduction\_from\_bundle}$;
\item $O$ is the obstruction-boundary row stating that neither route is inhabited for the present closed-CIC substrate inside this packet;
\item $H$ records componentwise $\hsame$ transports for the bundle, consumption, obstruction, provenance, and naming rows;
\item $C$ records the displayed $\Cont$ routes by which consumers read the bundle route, the obstruction row, the subject-reduction socket, and the subject-reduction packet;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:sr-discharge-routes}, \autoref{ch:concrete-instances-subjectreductiondischarge-namecert}, \autoref{ch:concrete-instances-subject-reduction-discharge-socket-namecert}, and \autoref{ch:concrete-instances-subjectreduction-namecert};
\item $N$ is the local $\NameCert_{\SubjectReductionRouteChoiceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows and the visible transport and continuation routes.
\end{definition}

\begin{theorem}[SubjectReductionRouteChoice obligations]
\label{thm:subject-reduction-route-choice-obligations}
For the carrier of \autoref{def:subject-reduction-route-choice-carrier}, the local $\NameCert_{\SubjectReductionRouteChoiceUp}$ surface consists exactly of carrier admission for
$$
R=(B,V,O,H,C,P,N),
$$
route visibility for $B,V$, obstruction-boundary visibility for $O$, componentwise transport through $H$, displayed continuation reads through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceObligations}
\begin{proof}
Project the displayed carrier. The rows $B,V$ are the only route rows, and the row $O$ is the only obstruction-boundary row.

Transport and replay are then read through $H$ and $C$. These rows preserve the displayed carrier coordinates and do not create a discharge coordinate outside $R$.

Finally, $P$ and $N$ account for the finite provenance and local name. Hence the listed rows exhaust the NameCert obligations for the route-choice packet.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice obligation surface]
\label{thm:subject-reduction-route-choice-obligation-surface}
For an accepted $\SubjectReductionRouteChoiceUp$ carrier $R=(B,V,O,H,C,P,N)$, the obligation surface exposes the explicit bundle row $B$, the bundle-consumption row $V$, the obstruction-boundary row $O$, the componentwise $\hsame$ row $H$, the displayed $\Cont$ row $C$, the package provenance row $P$, and the local naming row $N$. The same finite route is read through the discharge bundle surface, the socket boundary, and the downstream subject-reduction consumer of the sibling chapters.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceObligationSurface}
\begin{proof}
Project the carrier of \autoref{def:subject-reduction-route-choice-carrier}. The bundle row, bundle-consumption row, obstruction row, transport row, replay row, provenance row, and local name are all visible coordinates of one finite packet.

The discharge bundle and socket boundary are not hidden behind the packet: they are the sibling surfaces that motivate the route-choice rows already displayed in $B,V,O$. The accepted carrier therefore exposes the same finite route before any downstream consumer can read it.

Since the carrier has no coordinate for closed-CIC discharge, hidden route choice, or host proof search, the displayed rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice route-obligation surface]
\label{thm:subject-reduction-route-choice-route-obligation-surface}
For an accepted $\SubjectReductionRouteChoiceUp$ carrier $R=(B,V,O,H,C,P,N)$, every route-obligation read exposes exactly the bundle row $B$ and the bundle-consumption row $V$ while retaining the obstruction row $O$ as a visible boundary. The transport, replay, provenance, and naming rows $H,C,P,N$ may preserve and replay only those displayed rows.

Thus the route-obligation surface exports no closed subject-reduction theorem, quotient syntax equality, host evaluator soundness, hidden typeclass route, or third discharge path.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:subject-reduction-route-choice-carrier}. The route-facing rows are $B,V$, the obstruction-facing row is $O$, and the structural rows are $H,C,P,N$.

Apply the obligation-surface theorem \autoref{thm:subject-reduction-route-choice-obligation-surface}. It exhausts the visible rows of the packet before any downstream consumer reads the route choice.

Use the obstruction-boundary theorem \autoref{thm:subject-reduction-route-choice-obstruction-boundary}. It keeps $O$ separate from the bundle-consumption route, so an uninhabited obstruction cannot be reclassified as a closed subject-reduction proof.

Finally apply nonescape \autoref{thm:subject-reduction-route-choice-nonescape} and obstruction-route exhaustion \autoref{thm:subject-reduction-route-choice-obstruction-route-exhaustion}. These rule out a hidden typeclass route, quotient syntax equality, host evaluator soundness, closed consistency, and any third discharge path.
\end{proof}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceRouteObligationSurface}

\begin{theorem}[SubjectReductionRouteChoice MetaCIC scope]
\label{thm:subject-reduction-route-choice-metacic-scope}
Every accepted $\SubjectReductionRouteChoiceUp$ read remains inside the MetaCIC route of the discharge bundle, socket boundary, substitution/type-preservation rows, and the downstream subject-reduction consumer. The route may cite the sibling surfaces of \autoref{ch:concrete-instances-subjectreductiondischarge-namecert}, \autoref{ch:concrete-instances-subject-reduction-discharge-socket-namecert}, and \autoref{ch:concrete-instances-subjectreduction-namecert}, but it does not claim closed-CIC discharge or host proof search.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceCarrier\_metacic\_scope}
\begin{proof}
Use the obligation surface theorem above. It identifies the displayed rows and binds them to the finite route-choice packet before any consumer may proceed.

The sibling discharge and socket chapters provide the finite MetaCIC support surface, while the subject-reduction consumer chapter names the downstream use site. Because the packet has no coordinate for closed-CIC discharge or host proof search, those claims remain outside the scope.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice nonescape]
\label{thm:subject-reduction-route-choice-nonescape}
For an accepted $\SubjectReductionRouteChoiceUp$ carrier $R=(B,V,O,H,C,P,N)$, no consumer can extract closed-CIC discharge, hidden route choice, or host proof search from the packet. The accepted route-choice data remain exactly the displayed bundle, consumption, obstruction, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Project the carrier. The route-choice packet consists exactly of the displayed rows $B,V,O,H,C,P,N$.

Apply the MetaCIC scope theorem of \autoref{thm:subject-reduction-route-choice-metacic-scope}. It keeps the route inside the sibling discharge, socket, substitution, and consumer surfaces.

Since no extra coordinate is present, the packet cannot export closed-CIC discharge, hidden route choice, or host proof search.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice substitution stability]
\label{thm:subject-reduction-route-choice-substitution-stability}
For an accepted $\SubjectReductionRouteChoiceUp$ carrier $R=(B,V,O,H,C,P,N)$, a MetaCIC worker may read substitution stability only as a finite route-choice obligation over the displayed $\BHist$ packet. The obligation exposes the bundle row $B$, the bundle-consumption row $V$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, and the local $\NameCert_{\SubjectReductionRouteChoiceUp}$ row $N$.

It states that substitution-facing reads must preserve the same route-choice rows before any subject-reduction discharge is consumed. It does not assert unrestricted subject reduction, quotient syntax equality, host evaluator soundness, or closed consistency.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceSubstitutionStability}
\begin{proof}
Project the carrier of \autoref{def:subject-reduction-route-choice-carrier}. The only rows available to a substitution-facing consumer are the explicit bundle row $B$, the consumption row $V$, the obstruction boundary $O$, transport $H$, continuation $C$, provenance $P$, and naming $N$.

Apply the NameCert obligations of \autoref{thm:subject-reduction-route-choice-obligations}. They require every read to keep route visibility for $B,V$, obstruction visibility for $O$, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming.

Thus substitution stability is an obligation to preserve the displayed route-choice surface. Since the carrier has no quotient syntax-equality row, no host evaluator row, and no closed-consistency coordinate, no global MetaCIC theorem follows from this packet.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice type-preservation window]
\label{thm:subject-reduction-route-choice-type-preservation-window}
For an accepted $\SubjectReductionRouteChoiceUp$ carrier $R=(B,V,O,H,C,P,N)$, every bounded type-preservation window exposed to a MetaCIC worker is a finite $\BHist$ read through $B,V,H,C,P,N$ with the obstruction row $O$ still visible. The window records which discharge bundle route may be consumed and how $\hsame$ and $\Cont$ replay preserve that route inside the local $\NameCert$ surface.

The window is not a proof of full subject reduction. It exports no unrestricted reduction theorem, quotient syntax equality, host evaluator soundness, or closed consistency claim.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceTypePreservationWindow}
\begin{proof}
Start from the accepted carrier. The route rows $B,V$ identify the discharge bundle surface and its consumption route, while $O$ records the boundary where the present substrate has not supplied the missing closed-CIC discharge.

The transport and continuation rows $H,C$ keep the bounded read inside the same finite packet. The provenance and naming rows $P,N$ then record that the worker is using the local $\NameCert_{\SubjectReductionRouteChoiceUp}$ surface rather than a detached evaluator theorem.

Consequently the type-preservation window is bounded by the displayed rows. It can tell a worker where a discharge bundle would be consumed, but it cannot export full subject reduction or any of the refused host-side principles.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice blocked-edge nonescape]
\label{thm:subject-reduction-route-choice-blocked-edge-nonescape}
For an accepted $\SubjectReductionRouteChoiceUp$ carrier $R=(B,V,O,H,C,P,N)$, every blocked subject-reduction edge remains inside the obstruction row $O$ and the finite replay rows $H,C,P,N$. A downstream MetaCIC worker may cite the edge only as a visible blocked route in the same $\BHist$ packet and must keep the local $\NameCert_{\SubjectReductionRouteChoiceUp}$ row attached.

No blocked edge escapes as unrestricted subject reduction, quotient syntax equality, host evaluator soundness, closed consistency, or an inhabited discharge bundle.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceBlockedEdgeNonescape}
\begin{proof}
Inspect the obstruction boundary in \autoref{def:subject-reduction-route-choice-carrier}. The row $O$ is separate from the route rows $B,V$, so a blocked edge cannot be reclassified as an inhabited discharge route.

The NameCert obligations of \autoref{thm:subject-reduction-route-choice-obligations} keep every read under componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming. These rows preserve the blocked-edge status instead of creating a new discharge coordinate.

Therefore a downstream worker can use the blocked edge as finite route-choice information only. The refused global subject-reduction, quotient-equality, evaluator-soundness, and closed-consistency claims remain outside the packet.
\end{proof}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceCarrier\_blocked\_edge\_nonescape}

\begin{theorem}[SubjectReductionRouteChoice bundle-route exhaustion]
\label{thm:subject-reduction-route-choice-bundle-typeclass-exhaustion}
Every consumer read of an accepted $\SubjectReductionRouteChoiceUp$ packet reaches exactly one public route surface: the explicit bundle surface $B,V$. That surface exposes the four discharge coordinates named by the subject-reduction discharge interface, and no second route, hidden instance, or host conversion rule is exported by the packet.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceBundleRouteExhaustion}
\begin{proof}
Inspect the route coordinates in \autoref{def:subject-reduction-route-choice-carrier}. The explicit route is the pair $B,V$.

Since every consumer read factors through $C$ and every transported read factors through $H$, no route coordinate outside the displayed bundle surface is available.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice constructor exhaustion]
\label{thm:subject-reduction-route-choice-constructor-exhaustion}
Every accepted $\SubjectReductionRouteChoiceUp$ route-choice read is generated by exactly one displayed carrier packet $R=(B,V,O,H,C,P,N)$. Each consumer path is either the explicit bundle route through $B,V$ with the obstruction row $O$ still visible, or the blocked-edge route through $O$ with $B,V$ preserved as non-inhabited route data.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:subject-reduction-route-choice-carrier}. The displayed packet has exactly the route rows $B,V$, the obstruction row $O$, and the structural rows $H,C,P,N$.

Case-split on the public route surface versus the obstruction surface. In the first case, apply \autoref{thm:subject-reduction-route-choice-bundle-typeclass-exhaustion}: the consumer reaches the explicit bundle route $B,V$, while $O$ remains a visible boundary row. In the second case, apply \autoref{thm:subject-reduction-route-choice-obstruction-boundary}: the read stops at $O$ and keeps $B,V$ only as route data not inhabited by the present substrate.

Finally use \autoref{thm:subject-reduction-route-choice-nonescape}. The rows $H,C,P,N$ transport and replay only those displayed coordinates, so no hidden instance, host conversion rule, quotient syntax equality, evaluator soundness, or closed consistency claim is exported.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice obstruction boundary]
\label{thm:subject-reduction-route-choice-obstruction-boundary}
The $\SubjectReductionRouteChoiceUp$ packet keeps the obstruction row $O$ separate from the route rows. It may record that a downstream substrate can use the explicit bundle route, but it does not supply an inhabited $\mathrm{SubjectReductionDischargeBundle}$, a proof of the dependent-codomain discharge row, or a closed subject-reduction theorem for the present substrate.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceObstructionBoundary}
\begin{proof}
Read the carrier rows in order. The route rows $B,V$ name how a discharge would be consumed once supplied, while $O$ names the boundary where the present substrate lacks the required discharge.

The provenance row ties this boundary to the discharge, socket, and subject-reduction chapters. Those references identify the finite interfaces, not inhabitants of the missing closed-CIC discharge.

Therefore the packet can expose the route choice to downstream audits while keeping closed subject reduction outside its exported surface.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice obstruction-route exhaustion]
\label{thm:subject-reduction-route-choice-obstruction-route-exhaustion}
Every accepted $\SubjectReductionRouteChoiceUp$ packet read factors through exactly one of the displayed bundle-consumption route $B,V$ or the displayed obstruction row $O$, and both routes are transported only by the structural rows $H,C,P,N$. No third route, hidden typeclass instance, closed-CIC discharge, quotient syntax equality, host evaluator soundness, or closed consistency claim is exported by the packet.
\end{theorem}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceObstructionRouteExhaustion}
\begin{proof}
Project the carrier rows from \autoref{def:subject-reduction-route-choice-carrier}. The route-facing coordinates are $B,V$, the obstruction-facing coordinate is $O$, and all remaining rows are transport, replay, provenance, and local naming data.

Apply \autoref{thm:subject-reduction-route-choice-bundle-typeclass-exhaustion}. Any consumer that reads the displayed bundle route reaches the explicit surface $B,V$ and no hidden instance or host conversion rule.

Apply \autoref{thm:subject-reduction-route-choice-obstruction-boundary}. Any consumer that does not receive an inhabited bundle route remains at the obstruction row $O$, where the closed-CIC discharge and closed subject-reduction theorem are not supplied.

Finally use \autoref{thm:subject-reduction-route-choice-nonescape}. The structural rows $H,C,P,N$ preserve only the displayed bundle and obstruction coordinates, so quotient syntax equality, evaluator soundness, closed consistency, and any third route remain outside the packet.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice confluence nonescape]
\label{thm:subject-reduction-route-choice-confluence-nonescape}
Any confluence or subject-reduction consumer reading an accepted $\SubjectReductionRouteChoiceUp$ packet must keep the explicit bundle rows $B,V$, the obstruction row $O$, and the structural rows $H,C,P,N$ visible. Thus local confluence, closed subject reduction, quotient syntax equality, evaluator soundness, and closed consistency cannot escape the route-choice boundary.
\end{theorem}
\begin{proof}
Start with the bundle route. By \autoref{thm:subject-reduction-route-choice-bundle-typeclass-exhaustion}, any consumer that reaches the displayed discharge route sees only the explicit rows $B,V$ and no hidden instance or host conversion rule.

Next read the obstruction boundary. By \autoref{thm:subject-reduction-route-choice-obstruction-boundary} and \autoref{thm:subject-reduction-route-choice-obstruction-route-exhaustion}, a consumer that lacks an inhabited bundle remains at the row $O$, while the structural rows $H,C,P,N$ only transport and replay the same finite choice.

Finally apply \autoref{thm:subject-reduction-route-choice-nonescape}. Since every route is exhausted by $B,V,O,H,C,P,N$, no confluence, closed subject-reduction, quotient-equality, evaluator-soundness, or closed-consistency claim is exported from the packet.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice vision handoff]
\label{thm:subject-reduction-route-choice-vision-handoff}
The formal route of \autoref{ch:sr-discharge-routes} is realized as the finite $\SubjectReductionRouteChoiceUp$ carrier of \autoref{def:subject-reduction-route-choice-carrier}. The handoff consists of the explicit bundle row, the consumption row, the obstruction-boundary row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.
\end{theorem}
\begin{proof}
The vision chapter exposes the bundle route and the checked obstruction ledger. The carrier mirrors that boundary by placing $B,V$ and $O$ in separate displayed rows.

The route-choice packet also records $O$, so the handoff is not read as a closed subject-reduction proof for the present substrate. Consumers can cite the finite choice surface and must still obtain discharge data elsewhere.
\end{proof}

\begin{theorem}[SubjectReductionRouteChoice TasteGate obligation handoff]
\label{thm:subject-reduction-route-choice-tastegate-obligation-handoff}
Any obligation-closure read of $\SubjectReductionRouteChoiceUp$ must first expose the $\TasteGate$ single-carrier alignment row, the route-choice carrier rows $B,V,O,H,C,P,N$, and the MetaCIC obstruction rows consumed by the packet. The read is therefore a finite alignment and obstruction handoff; it is not a hidden subject-reduction discharge, quotient syntax equality, evaluator soundness theorem, or closed consistency claim.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:subject-reduction-route-choice-carrier}. It displays the bundle route rows $B,V$, the obstruction row $O$, and the structural rows $H,C,P,N$ as one packet.

Next use the obstruction boundary and route-exhaustion theorems, \autoref{thm:subject-reduction-route-choice-obstruction-boundary} and \autoref{thm:subject-reduction-route-choice-obstruction-route-exhaustion}. They force every accepted consumer to choose between the displayed bundle route and the displayed obstruction row, with no third discharge channel.

Finally read the checked $\TasteGate$ alignment row. Since the alignment names the same single-carrier packet, an obligation-closure consumer must keep the route-choice rows and the MetaCIC obstruction rows visible before any later closure step can be claimed.
\end{proof}
\leanchecked{BEDC.Derived.SubjectReductionRouteChoiceUp.SubjectReductionRouteChoiceTasteGate\_single\_carrier\_alignment}

\closureat{SubjectReductionRouteChoiceUp}{seedStr}

\begin{closurestatus}{\SubjectReductionRouteChoiceUp}
  \constructivestory{A $\SubjectReductionRouteChoiceUp$ packet is generated from explicit bundle, bundle-consumption, obstruction-boundary, transport, continuation, provenance, and local naming rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:subject-reduction-route-choice-carrier}, \autoref{thm:subject-reduction-route-choice-obligations}, \autoref{thm:subject-reduction-route-choice-substitution-stability}, \autoref{thm:subject-reduction-route-choice-type-preservation-window}, \autoref{thm:subject-reduction-route-choice-blocked-edge-nonescape}, \autoref{thm:subject-reduction-route-choice-bundle-typeclass-exhaustion}, \autoref{thm:subject-reduction-route-choice-obstruction-boundary}, \autoref{thm:subject-reduction-route-choice-obstruction-route-exhaustion}, and \autoref{thm:subject-reduction-route-choice-vision-handoff} bind the finite route-choice packet for bundle discharge access.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not discharge the four MetaCIC subject-reduction obligations, construct an inhabited discharge bundle, prove closed subject reduction, or give Lean verification of the route-choice packet.}
  \upgradepath{Obligation closure requires checked carrier admission, route-choice exhaustion, obstruction-boundary non-escape, route replay, provenance, and local naming for the same finite packet.}
\end{closurestatus}
