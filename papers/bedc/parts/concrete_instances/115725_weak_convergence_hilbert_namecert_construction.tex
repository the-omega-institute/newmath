\chapter{A Concrete Naming Certificate for $\WeakConvergenceHilbertUp$}
\label{ch:concrete-instances-weak-convergence-hilbert-namecert}
\origin{human}

Following Hilbert space weak convergence theory, the $\WeakConvergenceHilbertUp$ packet records weak convergence only through finite scalar-test rows over a displayed Hilbert source. It sits over the Hilbert carrier of \autoref{ch:concrete-instances-hilbert-namecert}, the finite weak-topology window of \autoref{ch:concrete-instances-weak-topology-namecert}, the norm comparison surface of \autoref{ch:concrete-instances-norm-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the scalar seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names the finite observation certificate used by weak-convergence consumers; it does not select a countable dual, assert compactness, identify a quotient sequence space, or import ambient completeness beyond the displayed sibling rows.

\begin{definition}[Weak convergence in Hilbert carrier]
\label{def:weak-convergence-hilbert-carrier}
A $\WeakConvergenceHilbertUp$ carrier is a finite $\BHist$ packet
$$
Q=(H,W,N,R,S,T,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $H$ is the $\HilbertUp$ source row. It records the carrier endpoint, inner-product readback surface, and Hilbert-facing provenance used by the scalar tests.
\item $W$ is the sibling $\WeakTopologyUp$ finite-window row, reused as the weak-neighbourhood interface for the displayed Hilbert source.
\item $N$ is the $\NormUp$ comparison row. It records norm bounds and norm-facing tolerance data without replacing weak convergence by norm convergence.
\item $R$ is the $\RegSeqRatUp$ readback row for indexed scalar-test tolerances and finite convergence budgets.
\item $S$ is the scalar-test row. Each entry displays a Hilbert source point, a test vector or functional row already accepted by $H,W$, and the scalar observation sent to the $\RealUp$ seal.
\item $T$ records componentwise $\hsame$ transport for the Hilbert source, weak-window row, norm comparison, regular rational readback, scalar-test row, replay, provenance, and local naming row.
\item $C$ records displayed $\Cont$ replay from a weak-convergence request through
$$
H,W,N,R,S \longmapsto T,P,L .
$$
\item $P$ is the $\Pkg$ provenance over $\WeakConvergenceHilbertUp$, $\HilbertUp$, $\WeakTopologyUp$, $\NormUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\WeakConvergenceHilbertUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $H,W,N,R,S,T,C,P,L$. It has no coordinate for a selected dual basis, countable choice, compactness, quotient sequence equality, norm convergence, weak-star compactness, or an ambient host theorem.
\end{definition}

\begin{theorem}[Weak convergence in Hilbert NameCert obligations]
\label{thm:weak-convergence-hilbert-namecert-obligations}
For every accepted $\WeakConvergenceHilbertUp$ carrier $Q=(H,W,N,R,S,T,C,P,L)$, the local $\NameCert_{\WeakConvergenceHilbertUp}$ surface consists exactly of Hilbert source exposure through $H$, finite weak-topology window exposure through $W$, norm comparison through $N$, regular rational tolerance readback through $R$, scalar-test exposure through $S$, componentwise transport through $T$, displayed replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:weak-convergence-hilbert-carrier}. The only source row is $H$, the only weak-neighbourhood interface is $W$, and the only norm-facing data are the comparison budgets in $N$.

The convergence evidence is finite. The regular rational row $R$ records the inspected tolerance budgets, and the scalar-test row $S$ records the finitely many Hilbert observations sent to the $\RealUp$ seal.

The remaining rows are structural. Transport through $T$, replay through $C$, provenance through $P$, and local naming through $L$ keep the same finite packet visible to every consumer. Since the carrier has no selected dual basis, choice schedule, compactness row, quotient sequence equality, norm-convergence coordinate, weak-star compactness row, or ambient host theorem, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Weak convergence in Hilbert scalar readback route]
\label{thm:weak-convergence-hilbert-scalar-readback-route}
Every scalar readback consumed from an accepted $\WeakConvergenceHilbertUp$ packet factors through the displayed route
$$
H,W,N,R,S,T,C,P,L .
$$
Thus a weak-convergence consumer may read only finite Hilbert scalar tests, regular rational tolerance budgets, norm-comparison boundaries, $\RealUp$ scalar seals, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The route exports no selected dual sequence, no quotient function-space equality, no norm-convergence theorem, no compactness theorem, and no countable-choice principle.
\end{theorem}
\begin{proof}
Begin with a scalar observation in $S$. Its Hilbert source is the same row $H$ admitted by the carrier, and its weak-neighbourhood meaning is read through the same finite window $W$.

Read the tolerance data next. The row $R$ supplies the regular rational budget for the observation, while $N$ records the norm-facing comparison boundary without turning the weak read into a norm-convergence claim.

Finally send the scalar value to the $\RealUp$ seal and replay the route through $T,C,P,L$. Componentwise transport and displayed replay preserve the same observation window for consumers, so no selected dual sequence, quotient equality, norm-convergence theorem, compactness theorem, or choice principle can be obtained from the scalar readback.
\end{proof}

\begin{theorem}[Weak convergence in Hilbert finite-window nonescape]
\label{thm:weak-convergence-hilbert-finite-window-nonescape}
Any public read of an accepted $\WeakConvergenceHilbertUp$ packet is exhausted by the finite Hilbert source row, weak-topology window, norm comparison, regular rational tolerance row, scalar-test row, transport, replay, provenance, and local naming data. In carrier notation the public route is
$$
H,W,N,R,S,T,C,P,L .
$$
The packet does not export Banach--Alaoglu compactness, Mazur convex-block convergence, Kadec--Klee norm recovery, selected subsequences, a countable total test family, quotient sequence equality, or ambient Hilbert-space weak compactness.
\end{theorem}
\begin{proof}
Use the NameCert obligations theorem to expose the carrier rows. A public read must display $H,W,N,R,S$ before it reaches any structural row.

Apply the scalar readback route. It sends every finite test through regular rational tolerances and the scalar seal before transport, replay, provenance, and local naming are consumed.

The refused analytic conclusions are not carrier coordinates. Since compactness, convex-block convergence, norm recovery, subsequence selection, countable test families, quotient equality, and ambient weak compactness are absent from $H,W,N,R,S,T,C,P,L$, no public read can export them.
\end{proof}

\falsifiablePrediction{Every accepted $\WeakConvergenceHilbertUp$ read must expose the $\HilbertUp$ source, $\WeakTopologyUp$ finite window, $\NormUp$ comparison boundary, $\RegSeqRatUp$ tolerance row, scalar-test row, $\RealUp$ seal, $\BHist$ transport, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows before any weak-convergence conclusion is public.}

\independenceWitness{hilbert, weaktopology, norm, regseqrat, real: $\WeakConvergenceHilbertUp$ is not bijective to any listed sibling because it jointly carries Hilbert source data, weak finite-window observations, norm comparison, regular rational tolerance readback, scalar-test rows, transport, replay, provenance, and local naming while refusing compactness, norm recovery, selected dual bases, and quotient sequence equality.}

\closureat{WeakConvergenceHilbertUp}{seedStr}

\begin{closurestatus}{\WeakConvergenceHilbertUp}
  \constructivestory{A $\WeakConvergenceHilbertUp$ packet is generated from a Hilbert source row, a WeakTopology finite-window row, a Norm comparison row, RegSeqRat tolerance readback, finite Real scalar-test rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weak-convergence-hilbert-carrier}, \autoref{thm:weak-convergence-hilbert-namecert-obligations}, \autoref{thm:weak-convergence-hilbert-scalar-readback-route}, and \autoref{thm:weak-convergence-hilbert-finite-window-nonescape} seed the finite weak-convergence certificate over Hilbert scalar-test rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close compactness, Mazur convex-block convergence, Kadec--Klee norm recovery, selected subsequences, countable total test families, quotient sequence equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite scalar-test stability, regular rational readback exactness, scalar nonescape, transport, replay, provenance, and local naming for BEDC.Derived.WeakConvergenceHilbertUp.}
\end{closurestatus}
