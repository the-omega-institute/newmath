\chapter{A Concrete Naming Certificate for $\FluxOperatorGeometryUp$}
\label{ch:concrete-instances-flux-operator-geometry-namecert}
\origin{ai}

The $\FluxOperatorGeometryUp$ packet realizes the window-six flux-operator geometry branch of \autoref{prin:zeckendorf-window-six-flux-operator-geometry} as finite NameCert data. It sits beside the stable Zeckendorf phase-shell surface of \autoref{ch:concrete-instances-zeckendorf-phase-shell-namecert}, the finite golden-mean window of \autoref{ch:concrete-instances-golden-mean-shift-window-six-namecert}, and the static boundary-flux route of \autoref{ch:concrete-instances-static-green-boundary-flux-namecert}. The packet names stable addresses, transition operators, phase gates, boundary escape flux, and golden-shell projection as one finite operator-geometry surface; it does not assert a hidden metric geometry, invariant bilinear form, invariant multiplication, physical impedance law, or curvature-scalar readout.

\begin{definition}[Flux operator geometry carrier]
\label{def:flux-operator-geometry-carrier}
A $\FluxOperatorGeometryUp$ carrier is a finite $\BHist$ packet
$$
F=(A,T,G,B,S,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the stable Zeckendorf address row, read through the visible window-six address surface of $\ZeckendorfPhaseShellUp$.
\item $T$ is the finite transition-operator table on those displayed addresses.
\item $G$ is the phase-gate row, recording which transition reads preserve, flip, or refuse the visible phase shell.
\item $B$ is the boundary escape-flux row, read through the static boundary-flux sibling where the boundary cut is visible.
\item $S$ is the golden-shell projection row, read through $\GoldenMeanShiftWindowSixUp$ and $\ZeckendorfPhaseShellUp$ rather than through a hidden inner product.
\item $O$ is the operator-algebra refusal row, recording the absence of any displayed invariant bilinear form or invariant multiplication in the accepted packet.
\item $H$ records componentwise $\hsame$ transport for address, transition, phase-gate, boundary-flux, projection, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from stable address reads to transition operators, phase gates, escape-flux reads, golden-shell projection, and refusal checks.
\item $P$ is the $\Pkg$ provenance over $\FluxOperatorGeometryUp$, $\ZeckendorfPhaseShellUp$, $\GoldenMeanShiftWindowSixUp$, $\StaticGreenBoundaryFluxUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FluxOperatorGeometryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,T,G,B,S,O,H,C,P,N$. It has no coordinate for a hidden metric tensor, invariant symplectic form, bracket, commutative product, physical coupling constant, or curvature scalar.
\end{definition}

\begin{theorem}[Flux operator geometry NameCert obligations]
\label{thm:flux-operator-geometry-namecert-obligations}
For every accepted $\FluxOperatorGeometryUp$ carrier $F=(A,T,G,B,S,O,H,C,P,N)$, the local $\NameCert_{\FluxOperatorGeometryUp}$ surface consists exactly of stable-address exposure through $A$, finite transition-operator exposure through $T$, phase-gate exposure through $G$, boundary escape-flux exposure through $B$, golden-shell projection through $S$, operator-algebra refusal through $O$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:flux-operator-geometry-carrier}. The visible packet contains only the stable-address row, transition table, phase gates, boundary escape-flux row, golden-shell projection, refusal row, and structural rows.

The stable-address and projection data are finite. The rows $A$ and $S$ are read through the Zeckendorf phase shell and golden-mean window siblings, so a consumer receives visible address and projection rows rather than a hidden metric space.

The operator data are likewise row-local. The transition table $T$ and phase gates $G$ record finite operator action on displayed addresses, while the boundary row $B$ records the escape-flux surface inherited from the static boundary-flux packet.

Finally $O,H,C,P,N$ close the local boundary. The refusal row $O$ blocks rereading the packet as an invariant bilinear form, symplectic form, bracket, or multiplication, and the structural rows transport, replay, source, and name only the displayed operator-geometry data.
\end{proof}

\begin{theorem}[Flux operator geometry boundary route]
\label{thm:flux-operator-geometry-boundary-route}
Every accepted $\FluxOperatorGeometryUp$ consumer that reads transition dynamics, phase gates, or escape flux must pass through the finite route
$$
A,T,G,B,S,O,H,C,P,N .
$$
Thus the consumer sees stable Zeckendorf addresses, finite transition operators, phase gates, boundary escape flux, golden-shell projection, operator-algebra refusal, componentwise transport, continuation replay, provenance, and local naming. It receives no hidden metric geometry, invariant bilinear form, invariant multiplication, physical impedance constant, or curvature-scalar readout.
\end{theorem}
\begin{proof}
Begin with the accepted carrier of \autoref{def:flux-operator-geometry-carrier}. A downstream read has access only to the rows $A,T,G,B,S,O,H,C,P,N$.

First read the visible address surface. The stable address row $A$ and golden-shell projection row $S$ are inherited from the finite Zeckendorf phase-shell and golden-mean window routes, so they do not expose a hidden ambient geometry.

Next read the operator rows. The transition table $T$ records finite moves between displayed addresses, the phase-gate row $G$ records the visible phase effect of those moves, and the boundary row $B$ records the escape-flux contribution before any operator-geometry read is exported.

Finally apply the refusal and structural rows. The row $O$ records that no invariant bilinear form or invariant multiplication is available in the packet, while $H,C,P,N$ transport, replay, source, and name the same finite route. Hence every consumer-facing read remains inside the displayed operator-geometry surface.
\end{proof}

\falsifiablePrediction{Every accepted $\FluxOperatorGeometryUp$ read that serves a transition, phase-gate, or boundary-flux consumer must expose stable Zeckendorf addresses, finite transition operators, phase gates, boundary escape flux, golden-shell projection, operator-algebra refusal, componentwise transport, Cont replay, Pkg provenance, and local NameCert rows before the consumer is served.}

\independenceWitness{zeckendorf-phase-shell, golden-mean-shift-window-six, static-green-boundary-flux: $\FluxOperatorGeometryUp$ is not bijective to any listed sibling because this packet jointly carries stable address rows, transition operators, phase gates, boundary escape-flux rows, golden-shell projection, refusal of invariant algebraic structure, transport, replay, provenance, and local naming.}

The Lean TasteGate route for \texttt{BEDC.Derived.FluxOperatorGeometryUp} must eventually provide explicit $\Nontrivial\ \FluxOperatorGeometryUp$ and $\FieldFaithful\ \FluxOperatorGeometryUp$ instances preserving $A,T,G,B,S,O,H,C,P,N$ as separate displayed fields before stronger closure is claimed.

\closureat{FluxOperatorGeometryUp}{seedStr}

\begin{closurestatus}{\FluxOperatorGeometryUp}
  \constructivestory{A $\FluxOperatorGeometryUp$ packet is generated from stable Zeckendorf address rows, finite transition-operator tables, phase-gate rows, boundary escape-flux rows, golden-shell projection rows, operator-algebra refusal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:flux-operator-geometry-carrier}, \autoref{thm:flux-operator-geometry-namecert-obligations}, and \autoref{thm:flux-operator-geometry-boundary-route} seed the finite flux-operator geometry packet over stable addresses, transition operators, phase gates, escape flux, golden-shell projection, refusal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, a standard bridge, hidden metric geometry, invariant bilinear forms, invariant multiplication, physical impedance laws, or curvature-scalar readouts.}
  \upgradepath{Obligation closure requires checked carrier admission, stable-address exposure, transition-operator table exactness, phase-gate exposure, boundary escape-flux handoff, golden-shell projection, operator-algebra refusal, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.FluxOperatorGeometryUp.}
\end{closurestatus}
