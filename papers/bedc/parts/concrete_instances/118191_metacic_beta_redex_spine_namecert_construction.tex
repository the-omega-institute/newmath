\chapter{A Concrete Naming Certificate for $\MetaCICBetaRedexSpineUp$}
\label{ch:concrete-instances-metacic-beta-redex-spine-namecert}
\origin{ai}

The $\MetaCICBetaRedexSpineUp$ packet realizes the beta-redex spine branch of \autoref{ch:metacic-beta-audit-map} as finite NameCert data over the checked MetaCIC beta reduction grammar. It sits beside the beta-step boundary of \autoref{ch:concrete-instances-betastepboundary-namecert}, the beta-reduction audit trail of \autoref{ch:concrete-instances-betareductionaudittrail-namecert}, the beta critical-pair packet of \autoref{ch:concrete-instances-beta-critical-pair-namecert}, and the subject-reduction boundary of \autoref{ch:concrete-instances-metacic-subject-reduction-boundary-namecert}. The packet records the displayed redex spine and contraction handoff without claiming subject reduction, confluence, evaluator equality, quotient syntax equality, or kernel consistency.

\begin{definition}[MetaCIC beta redex spine carrier]
\label{def:metacic-beta-redex-spine-carrier}
A $\MetaCICBetaRedexSpineUp$ carrier is a finite $\BHist$ packet
$$
S=(M,\Gamma,t,u,\beta,\chi,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the MetaCIC $\mathrm{Term}$ row naming the source term, its visible application spine, and the binder depth at which the inspected redex occurs.
\item $\Gamma$ is the local context and binder-window row, recording the finite variables, displayed domains, and shift boundary available to the contraction.
\item $t$ is the redex row, exposing the displayed $\mathrm{app}(\mathrm{lam}\ dom\ body)\ arg$ site, its subterm address, and the surrounding spine segments.
\item $u$ is the contracted target row, naming the $\mathrm{substitute}\ 0\ arg\ body$ result and the reassembled target term inside the same spine.
\item $\beta$ is the $\mathrm{BetaStep}$ and $\mathrm{BetaConv}$ row, recording the one-step contraction and the common-reduct conversion interface available after the step.
\item $\chi$ is the obstruction-retention row for substitution preservation, dependent codomain stability, binder-domain subject reduction, subject reduction, confluence, evaluator equality, quotient syntax equality, and kernel consistency.
\item $H$ records componentwise $\hsame$ transport for $M,\Gamma,t,u,\beta,\chi$ and for the replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the source $\mathrm{Term}$ and binder window through the redex address, contraction target, $\mathrm{BetaStep}$ witness, $\mathrm{BetaConv}$ handoff, and obstruction-retention row.
\item $P$ is the $\Pkg$ provenance over $\MetaCICBetaRedexSpineUp$, $\mathrm{Term}$, $\mathrm{BetaStep}$, $\mathrm{BetaConv}$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\MetaCICBetaRedexSpineUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,\Gamma,t,u,\beta,\chi,H,C,P,N$. It has no coordinate for an unbounded evaluator, a subject-reduction theorem, a full confluence proof, quotient equality of terms, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC beta redex spine NameCert obligations]
\label{thm:metacic-beta-redex-spine-namecert-obligations}
For every accepted $\MetaCICBetaRedexSpineUp$ carrier
$$
S=(M,\Gamma,t,u,\beta,\chi,H,C,P,N),
$$
the local $\NameCert_{\MetaCICBetaRedexSpineUp}$ surface consists exactly of source $\mathrm{Term}$ admission for $M$, local binder-window admission for $\Gamma$, redex-site exposure for $t$, contraction-target exposure for $u$, $\mathrm{BetaStep}$ and $\mathrm{BetaConv}$ routing through $\beta$, obstruction retention through $\chi$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-beta-redex-spine-carrier}. The displayed rows are $M,\Gamma,t,u,\beta,\chi,H,C,P,N$.

The syntax rows come first. The source term row $M$, context row $\Gamma$, redex row $t$, and target row $u$ name the inspected application-lambda site, the binder window, and the reassembled contracted term.

The reduction row is then read through $\beta$. It records the $\mathrm{BetaStep}$ contraction and the $\mathrm{BetaConv}$ handoff made available by the beta conversion interface, while $\chi$ keeps the subject-reduction and confluence endpoints outside the packet.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for an evaluator, quotient syntax equality, subject reduction, full confluence, or kernel consistency, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[MetaCIC beta redex spine contraction handoff]
\label{thm:metacic-beta-redex-spine-contraction-handoff}
Every consumer that reads an accepted $\MetaCICBetaRedexSpineUp$ packet as a beta contraction handoff must expose the source $\mathrm{Term}$ row, binder-window row, displayed redex site, contracted target, $\mathrm{BetaStep}$ witness, $\mathrm{BetaConv}$ interface, obstruction-retention row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before using the handoff in subject-reduction or confluence work. The handoff supplies a finite contraction spine only; it does not discharge substitution preservation, dependent-codomain stability, binder-domain preservation, subject reduction, confluence, evaluator equality, quotient syntax equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:metacic-beta-redex-spine-namecert-obligations}. They force every accepted contraction read through $M,\Gamma,t,u,\beta,\chi,H,C,P,N$.

The redex and contraction rows determine the local spine. The row $t$ names the application-lambda site and its address, while $u$ names the term obtained by the displayed substitution and reassembly route.

The beta interface is finite. The row $\beta$ records the one-step $\mathrm{BetaStep}$ witness and the available $\mathrm{BetaConv}$ comparison route, while $\chi$ keeps the substitution, dependent-codomain, binder-domain, subject-reduction, confluence, evaluator, quotient, and consistency obligations visible as non-exported endpoints.

Finally the structural rows transport, replay, source, and name the same finite contraction spine. Thus downstream subject-reduction and confluence packets may consume the handoff as a local beta-redex route, but not as any of the stronger typing or semantic theorems.
\end{proof}

\falsifiablePrediction{If the redex-site row $t$ is removed, two packets with different application-lambda addresses but the same contracted target can be classified together; if the obstruction-retention row $\chi$ is removed, the contraction handoff can be misread as subject-reduction evidence.}

\independenceWitness{betastepboundary, betareductionaudittrail, beta\_critical\_pair, typedbetacongruencetrigger, and metacic\_subjectreductionboundary are nearby MetaCIC siblings, but none is bijective to $\MetaCICBetaRedexSpineUp$ because this packet jointly carries the source term spine, binder window, redex address, contracted target, BetaStep witness, BetaConv route, obstruction retention, transport, replay, provenance, and local naming.}

Lean TasteGate support for $\MetaCICBetaRedexSpineUp$ will provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances so the source term, binder window, redex site, contraction target, beta relation, obstruction, transport, replay, provenance, and local naming fields remain distinguishable.

\closureat{MetaCICBetaRedexSpineUp}{seedStr}

\begin{closurestatus}{\MetaCICBetaRedexSpineUp}
  \constructivestory{A $\MetaCICBetaRedexSpineUp$ packet is generated from MetaCIC Term, binder-window, redex-site, contracted-target, BetaStep, BetaConv, obstruction-retention, transport, replay, provenance, and local naming rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-beta-redex-spine-carrier}, \autoref{thm:metacic-beta-redex-spine-namecert-obligations}, and \autoref{thm:metacic-beta-redex-spine-contraction-handoff} seed the finite beta-redex spine surface over Term, BetaStep, BetaConv, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close substitution preservation, dependent-codomain stability, binder-domain preservation, subject reduction, confluence, evaluator equality, quotient syntax equality, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, redex-site exactness, contraction-target exactness, BetaStep routing, BetaConv routing, obstruction retention, transport, replay, provenance, Nontrivial, FieldFaithful, and local naming for $M,\Gamma,t,u,\beta,\chi,H,C,P,N$.}
\end{closurestatus}
