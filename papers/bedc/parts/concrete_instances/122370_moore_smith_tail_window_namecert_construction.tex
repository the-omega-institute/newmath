\chapter{A Concrete Naming Certificate for $\MooreSmithTailWindowUp$}
\label{ch:concrete-instances-moore-smith-tail-window-namecert}
\origin{human}

Following Moore--Smith net convergence practice as presented in Kelley-style general topology, the $\MooreSmithTailWindowUp$ packet records the finite tail-window certificate by which a directed net read is restricted to a displayed final segment before any Real or Completion consumer may inspect a tolerance seal. It sits beside the cofinal reindexing surface of $\DirectedSubnetUp$ in \autoref{ch:concrete-instances-directedsubnet-namecert}, the Cauchy-window carrier of $\CauchyNetUp$ in \autoref{ch:concrete-instances-cauchynet-namecert}, the finite observation route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the traditional tail-window route without quotienting nets, choosing subnets, selecting limits, invoking countable choice, or using ambient $\RealUp$ completeness.

\begin{definition}[Moore--Smith tail-window carrier]
\label{def:moore-smith-tail-window-carrier}
A $\MooreSmithTailWindowUp$ carrier is a finite $\BHist$ packet
$$
T=(I,W,N,S,R,D,E,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the directed-index row, naming the source preorder, the requested lower bound, and the finite final-segment window used by the net reader.
\item $W$ is the tail-window row, recording that every inspected index lies beyond the displayed lower bound and that refinements are read only through the same directed route.
\item $N$ is the $\CauchyNetUp$ row, exposing the net-side Cauchy requests visible on the displayed tail.
\item $S$ is the $\StreamNameUp$ finite-observation row attached to the inspected tail values.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the finite observations on the tail window.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row controlling the tail diameter and endpoint ledger.
\item $E$ is the terminal $\RealUp$ seal row, available only after $I,W,N,S,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the directed index row, tail window, net row, observation row, readback row, tolerance row, real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay for tail refinement, Cauchy-net inspection, StreamName observation, RegSeqRat readback, DyadicRat tolerance checking, and terminal Real sealing.
\item $P$ is the $\Pkg$ provenance over $\MooreSmithTailWindowUp$, $\DirectedSubnetUp$, $\CauchyNetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $L$ is the local $\NameCert_{\MooreSmithTailWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,W,N,S,R,D,E,H,C,P,L$. It has no coordinate for quotient-net equality, a selected subnet representative, a selected limit, countable choice over directed tails, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Moore--Smith tail-window NameCert obligations]
\label{thm:moore-smith-tail-window-namecert-obligations}
For every accepted $\MooreSmithTailWindowUp$ carrier $T=(I,W,N,S,R,D,E,H,C,P,L)$, the local $\NameCert_{\MooreSmithTailWindowUp}$ surface consists exactly of carrier admission for the displayed rows, directed-tail coverage through $I,W$, Cauchy-net exposure through $N$, finite observation through $S$, regular rational readback through $R$, dyadic tolerance control through $D$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier $T=(I,W,N,S,R,D,E,H,C,P,L)$. The directed source and the final-segment request are visible first through $I$, and the actual tail discipline is the row $W$.

Read $N,S,R,D$ next. The Cauchy-net request is confined to the displayed tail, the finite observations are recorded by the StreamName row, the regular rational readback is downstream of those observations, and the dyadic tolerance ledger controls the diameter and endpoint data before any Real seal is exposed.

Finally read $E,H,C,P,L$. The terminal $\RealUp$ seal is therefore downstream of the finite Moore--Smith tail route, while transport, replay, provenance, and local naming preserve only the listed coordinates. Since quotient nets, selected subnets, selected limits, countable choice, host real equality, and ambient Real completeness are absent from the carrier, the obligations are exhausted by the displayed rows.
\end{proof}

\begin{theorem}[Moore--Smith tail-window RegSeqRat and Real handoff]
\label{thm:moore-smith-tail-window-regseqrat-real-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\MooreSmithTailWindowUp$ packet must read the displayed route
$$
\DirectedSubnetUp,\ \CauchyNetUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp
$$
through the carrier rows $I,W,N,S,R,D,E$ before it may consume the terminal seal. The handoff supplies a Moore--Smith final-segment certificate for L10 Real and Completion comparisons; it supplies no quotient net, selected subnet, selected limit, countable-choice schedule, host real equality, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start with the directed tail rows. The row $I$ names the source direction and requested lower bound, while $W$ records the final segment actually inspected by the consumer.

The analytic read then passes through $N,S,R,D$. The Cauchy-net row supplies the tail request, the StreamName row stores the finite observations, the RegSeqRat row reads those observations as regular rational data, and the DyadicRatCore row fixes the tolerance ledger used by the comparison.

Only after those rows are displayed can the consumer reach the $\RealUp$ seal $E$. The structural rows $H,C,P,L$ transport and replay the same route, so the handoff cannot manufacture a quotient net, selected subnet, selected limit, countable-choice schedule, host equality, or ambient completeness principle.
\end{proof}

\falsifiablePrediction{A Real or Completion consumer that reads a terminal seal from $\MooreSmithTailWindowUp$ without displaying the directed-index row, tail-window row, CauchyNet row, StreamName observations, RegSeqRat readback, DyadicRatCore tolerance row, transport, replay, provenance, and local NameCert row is outside this packet.}

\independenceWitness{Nearest siblings: directedsubnet, cauchynet, streamname, regseqrat, dyadicratcore, real. None is bijective to $\MooreSmithTailWindowUp$ because this packet binds a Moore--Smith final-segment request, a Cauchy-net row, finite observations, regular rational readback, dyadic tolerance control, terminal Real sealing, transport, replay, provenance, and local naming into one finite carrier.}

\closureat{MooreSmithTailWindowUp}{seedStr}

\begin{closurestatus}{\MooreSmithTailWindowUp}
  \constructivestory{A $\MooreSmithTailWindowUp$ packet is generated from a directed-index row, displayed tail window, CauchyNet row, StreamName finite observations, RegSeqRat readback, DyadicRatCore tolerance ledger, Real seal, componentwise transport, displayed replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:moore-smith-tail-window-carrier}, \autoref{thm:moore-smith-tail-window-namecert-obligations}, and \autoref{thm:moore-smith-tail-window-regseqrat-real-handoff} seed the finite Moore--Smith tail-window certificate used by L10 Real and Completion net/filter comparisons.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient nets, selected subnets, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, directed-tail coverage, Cauchy-net exposure, finite observation, regular readback, dyadic tolerance control, terminal Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.MooreSmithTailWindowUp.}
\end{closurestatus}
