\chapter{A Concrete Naming Certificate for $\BishopLocatedSequenceUp$}
\label{ch:concrete-instances-bishop-located-sequence-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, a located sequence is a displayed real-name sequence whose finite windows, locatedness moduli, and regular rational readback are visible before any completion-facing consumer may inspect a limiting claim. The $\BishopLocatedSequenceUp$ packet records this L10 neighbour of constructive Real and Completion objects. It sits beside the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the located-space route of \autoref{ch:concrete-instances-bishop-located-space-namecert}. The certificate names only the finite sequence surface; it does not select a limit, quotient streams, invoke countable choice, compare by trichotomy, or assert ambient $\RealUp$ completeness.

\begin{definition}[Bishop located sequence carrier]
\label{def:bishop-located-sequence-carrier}
A $\BishopLocatedSequenceUp$ carrier is a finite $\BHist$ tuple
$$
X=(S,L,R,E,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ row of finite sequence windows. Each consumer request names a displayed prefix or tolerance-indexed observation window before reading any Real-facing data.
\item $L$ is the locatedness-modulus row. It records the finite modulus by which the requested window is witnessed as located in the source sequence surface.
\item $R$ is the $\RegSeqRatUp$ readback row. It packages the rational comparison data extracted from the sequence windows and locatedness modulus.
\item $E$ is the $\RealUp$ seal row. It is exposed only after $S,L,R$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for $S,L,R,E$ and for the structural certificate rows.
\item $C$ stores displayed $\Cont$ replay from stream windows through locatedness moduli, regular rational readback, and the terminal Real seal.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedSequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedSequenceUp}$ row.
\end{enumerate}
The classifier compares packets only through $S,L,R,E,H,C,P,N$. It has no coordinate for a selected limit, quotient stream equality, countable choice, real trichotomy, host sequence equality, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.BishopLocatedSequenceUp.BishopLocatedSequenceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[BishopLocatedSequence NameCert obligations]
\label{thm:bishop-located-sequence-namecert-obligations}
For every accepted $\BishopLocatedSequenceUp$ carrier
$$
X=(S,L,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopLocatedSequenceUp}$ surface is exhausted by four obligation rows:
\begin{enumerate}
\item stream-window admission, requiring every sequence read to begin at the displayed $\StreamNameUp$ row $S$;
\item locatedness-modulus visibility, requiring the row $L$ to be visible before any located-sequence or $\LocatedSpaceUp$ consumer reads the sequence as located data;
\item regular rational and Real readback, requiring $R$ to expose the rational comparison windows before $E$ seals the read as $\RealUp$ data;
\item structural replay, requiring $H,C,P,N$ to preserve the same finite route under transport, continuation replay, provenance, and local naming.
\end{enumerate}
No obligation row may call on a selected limit, quotient stream map, countable choice, trichotomy, host equality of sequences, or ambient Real completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-located-sequence-carrier}. The only analytic rows are $S,L,R,E$, and the only structural rows are $H,C,P,N$.

The first two obligations bind the sequence source. A consumer starts at finite $\StreamNameUp$ windows and can treat the sequence as located data only after the modulus row $L$ is displayed.

The third obligation routes numerical data through $R$ and $E$. Regular rational readback supplies the finite comparisons, and the Real seal is downstream of those comparisons.

Finally, $H,C,P,N$ transport, replay, source, and name the same packet. Since the excluded principles have no carrier coordinate, these four rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[BishopLocatedSequence window stability]
\label{thm:bishop-located-sequence-window-stability}
Every accepted $\BishopLocatedSequenceUp$ packet keeps finite window reads stable under componentwise $\hsame$ transport: if a consumer reads the row $S$ and then uses the locatedness modulus $L$, regular rational readback $R$, and Real seal $E$, the transported read must expose the same route
$$
S,L,R,E,H,C,P,N .
$$
The transported consumer receives a finite located sequence surface and not a quotient stream, selected limit, countable-choice witness, trichotomy decision, or ambient Real-completeness theorem.
\end{theorem}
\begin{proof}
Read $H$ in \autoref{def:bishop-located-sequence-carrier}. It is componentwise transport for the stream windows, locatedness modulus, regular rational readback, and Real seal.

Replay the consumer route through $C$. The replay row can inspect $S$ before $L$, then $R$, then $E$, so transport preserves the displayed order of the sequence read.

The provenance and local name rows $P,N$ keep the transported read attached to the same packet. Since no quotient, selected-limit, choice, trichotomy, or completeness coordinate is present, window stability remains a finite NameCert statement.
\end{proof}

\begin{theorem}[BishopLocatedSequence L10 route]
\label{thm:bishop-located-sequence-l10-route}
Every Real-facing or Completion-facing consumer of an accepted $\BishopLocatedSequenceUp$ carrier must traverse the L10 route
$$
\StreamNameUp \longrightarrow L \longrightarrow \RegSeqRatUp \longrightarrow \RealUp
$$
as displayed by $S,L,R,E,H,C,P,N$. The same route may be compared with the located-space sibling \autoref{ch:concrete-instances-bishop-located-space-namecert}, but it does not supply selected limits, quotient streams, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bishop-located-sequence-namecert-obligations}. They force every public read through stream-window admission, locatedness-modulus visibility, regular rational readback, and the Real seal.

Use window stability from \autoref{thm:bishop-located-sequence-window-stability}. Transport and replay preserve the same displayed rows, so a downstream Real or Completion consumer cannot bypass the finite sequence windows.

The sibling comparison with $\LocatedSpaceUp$ uses only the locatedness row $L$ and the shared structural rows $H,C,P,N$. It is therefore a finite L10 route over StreamName, RegSeqRat, Real, and LocatedSpace data, not a claim of selected convergence or ambient completeness.
\end{proof}

\falsifiablePrediction{Any accepted $\BishopLocatedSequenceUp$ consumer that reaches a Real-facing sequence read while bypassing the StreamName row, locatedness modulus, RegSeqRat readback, Real seal, transport, replay, provenance, or local NameCert row refutes the carrier surface.}

\independenceWitness{streamname, regseqrat, real, locatedspace: $\BishopLocatedSequenceUp$ is not bijective to any listed sibling because it jointly carries finite sequence windows, a locatedness-modulus row, regular rational readback, Real seal, componentwise transport, continuation replay, package provenance, and local NameCert registration as one Bishop-style located-sequence packet.}

\begin{closurestatus}{\BishopLocatedSequenceUp}
  \constructivestory{A $\BishopLocatedSequenceUp$ packet is generated from StreamName windows, a locatedness-modulus row, RegSeqRat readback, a Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-sequence-carrier}, \autoref{thm:bishop-located-sequence-namecert-obligations}, \autoref{thm:bishop-located-sequence-window-stability}, and \autoref{thm:bishop-located-sequence-l10-route} seed the finite located-sequence route over StreamName, RegSeqRat, Real, and LocatedSpace rows.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.BishopLocatedSequenceUp.BishopLocatedSequenceTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient streams, countable choice, trichotomy, ambient Real completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, locatedness-modulus visibility, regular rational readback, Real-seal nonescape, transport replay, and local NameCert coverage for BEDC.Derived.BishopLocatedSequenceUp.}
\end{closurestatus}
