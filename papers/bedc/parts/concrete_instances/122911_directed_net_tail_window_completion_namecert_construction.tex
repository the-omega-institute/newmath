\chapter{A Concrete Naming Certificate for $\DirectedNetTailWindowCompletionUp$}
\label{ch:concrete-instances-directed-net-tail-window-completion-namecert}
\origin{human}

Following Moore and Smith on directed nets and Bishop--Bridges constructive completion, the $\DirectedNetTailWindowCompletionUp$ packet records the finite tail-window route by which a displayed directed index row reaches the Real and Completion corridor. It sits after the directed-net source of \autoref{ch:concrete-instances-moore-smith-net-namecert}, the finite StreamName observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the completion boundary of \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert}. The packet exposes a cofinal tail-window handoff without quotient nets, selected cofinal maps, countable choice, host net equality, or ambient $\RealUp$ completeness.

\begin{definition}[Directed-net tail-window completion carrier]
\label{def:directed-net-tail-window-completion-carrier}
A $\DirectedNetTailWindowCompletionUp$ carrier is a finite $\BHist$ packet
$$
T=(I,U,W,R,D,E,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the directed-index row, carrying finitely many index names, preorder comparison rows, and displayed common-upper witnesses.
\item $U$ is the cofinal tail-window row. For each requested precision it records a displayed late-index threshold and the finite tail slice inspected above that threshold.
\item $W$ is the $\StreamNameUp$ observation row attached to the retained tail slice.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same windows and rational observations.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic precision, comparison, and tail-radius cells used by the request.
\item $E$ is the terminal $\RealUp$ seal row, visible only after $I,U,W,R,D$ have been displayed.
\item $B$ is the $\CompletionUp$ handoff row, read through the Bishop regular Cauchy completion boundary and never as an ambient completed object.
\item $H$ records componentwise $\hsame$ transport for the directed-index, cofinal tail-window, StreamName, RegSeqRat, DyadicRatCore, Real, Completion, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion request through $I,U,W,R,D,E,B$.
\item $P$ is the $\Pkg$ provenance over $\DirectedNetTailWindowCompletionUp$, $\MooreSmithNetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CompletionUp$, $\BishopRegularCauchyCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DirectedNetTailWindowCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,U,W,R,D,E,B,H,C,P,N$. It has no coordinate for quotient nets, selected cofinal maps, countable choice, host net equality, a selected limit, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Directed-net tail-window completion NameCert obligations]
\label{thm:directed-net-tail-window-completion-namecert-obligations}
For every accepted $\DirectedNetTailWindowCompletionUp$ carrier $T=(I,U,W,R,D,E,B,H,C,P,N)$, the local $\NameCert_{\DirectedNetTailWindowCompletionUp}$ surface consists exactly of directed-index admission through $I$, cofinal tail-window exposure through $U$, StreamName observation through $W$, RegSeqRat readback through $R$, DyadicRatCore tolerance exposure through $D$, Real sealing through $E$, Completion handoff through $B$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:directed-net-tail-window-completion-carrier}. Its object rows are exactly the directed-index row, cofinal tail-window row, observation window, regular rational readback, dyadic tolerance ledger, Real seal, and Completion handoff.

The directed portion is exhausted by $I$ and $U$. A completion request first displays the finite index comparisons and common-upper witnesses, then records the late tail slice selected for the requested precision.

The analytic portion is exhausted by $W,R,D,E,B$. The finite tail slice is observed as StreamName windows, read back as regular rational data, checked against the dyadic tolerance ledger, and only then served to the Real seal and Completion handoff.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since no row stores quotient nets, selected cofinal maps, countable choice, host equality, selected limits, or ambient completeness, those objects cannot become local NameCert obligations.
\end{proof}

\begin{theorem}[Directed-net tail-window Real and Completion handoff]
\label{thm:directed-net-tail-window-completion-handoff}
Every Real-facing or Completion-facing consumer of an accepted $\DirectedNetTailWindowCompletionUp$ packet factors through the finite route
$$
\MooreSmithNetUp \longmapsto
\StreamNameUp \longmapsto
\RegSeqRatUp \longmapsto
\DyadicRatCoreUp \longmapsto
\RealUp,\CompletionUp .
$$
The handoff uses the displayed cofinal tail-window row as its source and exports no quotient net, selected cofinal map, countable-choice schedule, host net equality, selected limit, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the accepted packet $T=(I,U,W,R,D,E,B,H,C,P,N)$. The directed-index row $I$ and cofinal tail-window row $U$ are the only source rows available to a Real or Completion consumer.

Read the observation and readback rows next. The StreamName window $W$ is opened on the displayed tail slice, the RegSeqRat row $R$ reads back the same finite observations, and the DyadicRatCore row $D$ records the tolerance arithmetic used by the tail request.

Finally $E$ and $B$ are terminal handoff rows for Real and Completion consumers. The transport, replay, provenance, and local naming rows preserve the same source route, so the consumer never obtains a quotient-net representative, selected cofinal function, countable-choice schedule, host equality, selected limit, or ambient completeness theorem.
\end{proof}

\falsifiablePrediction{A Real or Completion consumer that reads a $\DirectedNetTailWindowCompletionUp$ seal while omitting the directed-index row, cofinal tail-window row, StreamName window, RegSeqRat readback, or DyadicRatCore tolerance ledger is outside the accepted packet surface.}

\independenceWitness{moore-smith-net, streamname, regseqrat, dyadicratcore, real, bishop-regular-cauchy-completion: no listed sibling supplies the whole $\DirectedNetTailWindowCompletionUp$ packet because this packet binds directed indices, cofinal tail windows, finite observation, regular rational readback, dyadic tolerance, terminal Real sealing, Completion handoff, transport, replay, provenance, and local naming in one route.}

\closureat{DirectedNetTailWindowCompletionUp}{seedStr}

\begin{closurestatus}{\DirectedNetTailWindowCompletionUp}
  \constructivestory{A $\DirectedNetTailWindowCompletionUp$ packet is generated from a directed-index row, cofinal tail-window row, $\StreamNameUp$ finite observations, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, $\RealUp$ seal, $\CompletionUp$ handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:directed-net-tail-window-completion-carrier}, \autoref{thm:directed-net-tail-window-completion-namecert-obligations}, and \autoref{thm:directed-net-tail-window-completion-handoff} seed the finite directed-tail route from Moore--Smith net data into Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient nets, selected cofinal maps, countable choice, host net equality, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked directed-index admission, cofinal tail-window exposure, StreamName observation, RegSeqRat readback, DyadicRatCore tolerance exactness, Real and Completion handoff, transport, replay, provenance, and local naming for BEDC.Derived.DirectedNetTailWindowCompletionUp.}
\end{closurestatus}
