\chapter{Menger Sponge NameCert Construction}
\label{ch:concrete-instances-menger-sponge-namecert}
\origin{human}

Following Menger and classical fractal topology, the $\MengerSpongeUp$ packet records the standard ternary-cube sponge as finite BEDC naming data. It sits beside the square deletion surface of \autoref{ch:concrete-instances-sierpinski-carpet-namecert}, the Cantor-address source of \autoref{ch:concrete-instances-cantorspace-namecert}, and the finite contraction-family row of \autoref{ch:concrete-instances-iterated-function-system-namecert}. The packet names only inspected cube stages, deleted middle slabs, boundary incidence, coordinate readback, transport, replay, provenance, and local naming; it does not import a completed compact subset of the cube, a selected infinite branch, Hausdorff dimension, measure computation, or ambient $\RealUp$ completeness.

\begin{definition}[Menger sponge carrier]
\label{def:menger-sponge-carrier}
A $\MengerSpongeUp$ carrier is a finite $\BHist$ packet
$$
M=(C,D,B,A,W,Q,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the ternary cube-stage row. It records an inspected finite word over the $27$ subcube positions, the displayed depth, and the retained cube cell at that depth.
\item $D$ is the deleted middle-slab ledger. For each inspected cube address it records the three coordinate directions and refuses exactly those subcubes whose address lies in at least two middle-coordinate slabs.
\item $B$ is the boundary-incidence row. It records which retained faces, edges, and corner incidences remain visible after the deleted-slab ledger has been applied.
\item $A$ is the finite Cantor-address row read from \autoref{ch:concrete-instances-cantorspace-namecert}; it supplies only inspected prefixes and no selected infinite branch.
\item $W$ is the coordinate $\StreamNameUp$ window row for the three coordinate schedules attached to the retained cube.
\item $Q$ is the $\RegSeqRatUp$ readback row for those coordinate schedules, with dyadic tolerance comparisons routed through $\DyadicRatCoreUp$ before any terminal seal is exposed.
\item $E$ is the triple of $\RealUp$ coordinate seals obtained from the same cube-stage, deleted-slab, boundary, and coordinate-window rows.
\item $H$ records componentwise $\hsame$ transport for $C,D,B,A,W,Q,E,T,P,N$.
\item $T$ records displayed $\Cont$ replay from a sponge request through retained cube stages, deleted slabs, boundary incidence, coordinate windows, rational readbacks, and Real coordinate seals.
\item $P$ is the $\Pkg$ provenance over $\MengerSpongeUp$, $\SierpinskiCarpetUp$, $\CantorSpaceUp$, $\IteratedFunctionSystemUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MengerSpongeUp}$ row.
\end{enumerate}
The classifier accepts a packet only through $C,D,B,A,W,Q,E,H,T,P,N$. It has no coordinate for a completed subset of $[0,1]^3$, hyperspace equality, selected infinite address, dimension or measure data, quotient equality of coordinate triples, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.MengerSpongeUp.MengerSpongeTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Menger sponge NameCert obligations]
\label{thm:menger-sponge-namecert-obligations}
For every accepted $\MengerSpongeUp$ carrier
$$
M=(C,D,B,A,W,Q,E,H,T,P,N),
$$
the local $\NameCert_{\MengerSpongeUp}$ surface consists exactly of carrier admission for the displayed packet; ternary cube-stage exposure through $C$; deleted middle-slab exclusion through $D$; boundary incidence through $B$; finite Cantor-address exposure through $A$; coordinate window exposure through $W$; regular rational readback through $Q$; Real coordinate sealing through $E$; componentwise transport through $H$; displayed replay through $T$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:menger-sponge-carrier}. The accepted packet displays exactly the ternary cube-stage, deleted middle-slab, boundary-incidence, finite address, coordinate-window, rational-readback, Real-seal, and structural rows.

The sponge-specific obligations are exhausted by $C,D,B$. A consumer first reads the inspected ternary cube address, then the deleted-slab ledger for that address, and only then the boundary incidences that remain available.

The coordinate obligations are exhausted by $A,W,Q,E$. The Cantor-address row supplies finite prefixes, the StreamName row opens the finite coordinate windows, the RegSeqRat row reads the scheduled rational data through the dyadic tolerance ledger, and the terminal Real row seals exactly those three coordinates.

Finally $H,T,P,N$ transport, replay, source, and name the same finite packet. Since the carrier contains no completed subset, hyperspace equality, selected branch, dimension row, measure row, quotient coordinate equality, or ambient-completeness coordinate, none of those objects can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Menger sponge Cantor-cube handoff]
\label{thm:menger-sponge-cantor-cube-handoff}
Every accepted $\MengerSpongeUp$ coordinate read factors through the finite route
$$
\CantorSpaceUp,\IteratedFunctionSystemUp \longmapsto C,D,B,A \longmapsto W,Q,E \longmapsto H,T,P,N .
$$
The route first displays the Cantor-prefix and finite contraction-family sources, then reads retained ternary cube stages, deleted middle slabs, boundary incidence, coordinate windows, regular rational readback, and terminal Real coordinate seals. It exports no completed fractal subset, selected infinite branch, hyperspace fixed point, quotient equality of coordinate triples, Hausdorff-dimension computation, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Begin with an accepted sponge consumer. By \autoref{def:menger-sponge-carrier}, the consumer enters through the displayed cube-stage row $C$ and the source rows named by $\CantorSpaceUp$ and $\IteratedFunctionSystemUp$.

Apply \autoref{thm:menger-sponge-namecert-obligations}. It forces every retained-cell read through $D$ and $B$, every finite address read through $A$, and every coordinate read through $W,Q,E$ before the structural rows are inspected.

The sibling chapters supply only their own finite routes: \autoref{ch:concrete-instances-cantorspace-namecert} supplies finite prefix data, \autoref{ch:concrete-instances-iterated-function-system-namecert} supplies a finite contraction-family interface, and \autoref{ch:concrete-instances-sierpinski-carpet-namecert} supplies the two-dimensional deletion sibling. None adds a carrier coordinate for a completed sponge subset, selected branch, hyperspace fixed point, dimension, measure, quotient coordinate equality, or ambient completeness. Hence every accepted coordinate consumer remains on the displayed Cantor-cube handoff.
\end{proof}

\falsifiablePrediction{An accepted $\MengerSpongeUp$ consumer that reaches a $\RealUp$ coordinate seal without first displaying the retained ternary cube stage, deleted middle-slab ledger, boundary-incidence row, finite Cantor-address prefix, StreamName coordinate windows, and RegSeqRat readback refutes the carrier surface.}

\independenceWitness{sierpinski-carpet, cantorspace, iterated-function-system, streamname, regseqrat, dyadicratcore, real: no listed sibling supplies the whole $\MengerSpongeUp$ packet, because the sponge certificate binds three-dimensional ternary cube stages, deleted middle slabs, boundary incidence, coordinate readback, Real seals, transport, replay, provenance, and local naming.}

\closureat{MengerSpongeUp}{seedStr}

\begin{closurestatus}{\MengerSpongeUp}
  \constructivestory{A $\MengerSpongeUp$ packet is generated from retained ternary cube-stage rows, deleted middle-slab ledgers, boundary-incidence rows, finite Cantor-address prefixes, StreamName coordinate windows, RegSeqRat readbacks, DyadicRatCore tolerance checks, RealUp coordinate seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:menger-sponge-carrier}, \autoref{thm:menger-sponge-namecert-obligations}, and \autoref{thm:menger-sponge-cantor-cube-handoff} seed the finite ternary-cube sponge carrier and its Cantor-cube coordinate handoff.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.MengerSpongeUp.MengerSpongeTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed sponge subsets, hyperspace fixed points, selected infinite branches, Hausdorff dimension, measure computation, quotient equality of coordinate triples, or bridge checking.}
  \upgradepath{Obligation closure requires a Lean-side MengerSpongeUp carrier with checked cube-stage exposure, deleted middle-slab exclusion, boundary incidence, coordinate readback, Real coordinate sealing, transport, replay, provenance, and local naming.}
\end{closurestatus}
