\chapter{A Concrete Naming Certificate for $\FiniteTotalBoundedNetUp$}
\label{ch:concrete-instances-finite-total-bounded-net-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\FiniteTotalBoundedNetUp$ packet records total boundedness through displayed finite epsilon-net data rather than through open-cover compactness or a selected choice principle. It sits over the metric and total-bounded sources of \autoref{ch:concrete-instances-totallybounded-namecert}, the finite epsilon-net carrier of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the compact-metric consumer surface of \autoref{ch:concrete-instances-compactmetric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}.

\begin{definition}[Finite total bounded net carrier]
\label{def:finite-total-bounded-net-carrier}
A $\FiniteTotalBoundedNetUp$ carrier is a finite $\BHist$ packet
$$
F=(M,T,E,W,D,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row, carrying the point classifier, distance classifier, and metric-distance ledger read by the net.
\item $T$ is the $\TotallyBoundedUp$ source row, exposing the total-bounded request at the displayed tolerance.
\item $E$ is the $\FiniteEpsilonNetUp$ witness row, naming the finite center bundle, center-admission ledger, and coverage-distance ledger.
\item $W$ is the $\StreamNameUp$ window row used when compact-metric or uniform-continuity consumers inspect the net through finite source schedules.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the positive dyadic tolerance and comparison cells used by the coverage ledger.
\item $R$ is the $\RegSeqRatUp$ readback row for rational coverage values and tolerance comparisons.
\item $S$ is the terminal $\RealUp$ seal for downstream real-valued distance reads.
\item $H$ records componentwise $\hsame$ transport for the metric source, total-bounded source, finite-net witness, stream windows, dyadic tolerance, regular-rational readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a total-bounded coverage request through $M,T,E,W,D,R,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteTotalBoundedNetUp$, $\MetricUp$, $\TotallyBoundedUp$, $\FiniteEpsilonNetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteTotalBoundedNetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,T,E,W,D,R,S,H,C,P,N$. It has no coordinate for an ambient finite subset, an open cover, a selected compactness proof, countable choice, quotient metric equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Finite total bounded net NameCert obligations]
\label{thm:finite-total-bounded-net-namecert-obligations}
For every accepted $\FiniteTotalBoundedNetUp$ carrier $F=(M,T,E,W,D,R,S,H,C,P,N)$, the local $\NameCert_{\FiniteTotalBoundedNetUp}$ surface consists exactly of metric-source admission, total-bounded request exposure, finite epsilon-net witness exposure, finite stream-window inspection, dyadic tolerance comparison, regular-rational readback, Real seal handoff, componentwise transport, displayed replay, provenance, and local naming for the same finite packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-total-bounded-net-carrier}. The analytic source rows are the metric source $M$, the total-bounded request $T$, and the finite epsilon-net witness $E$.

The tolerance and readback route is finite. The stream windows $W$ restrict the inspected source schedules, the dyadic row $D$ supplies the positive tolerance comparisons, and the regular-rational row $R$ records the coverage readback before the Real seal $S$ is offered.

The structural rows $H,C,P,N$ then transport, replay, source, and name the same packet. Since no additional coordinate is present, the local NameCert obligations are exhausted by the listed rows and cannot import an open cover, a selected compactness proof, countable choice, quotient metric equality, or ambient Real completeness.
\end{proof}

\begin{theorem}[Finite total bounded net Real handoff]
\label{thm:finite-total-bounded-net-real-handoff}
Every $\CompactMetricUp$ or real-valued uniform-continuity consumer of an accepted $\FiniteTotalBoundedNetUp$ packet reaches the terminal $\RealUp$ seal only through
$$
M,T,E,W,D,R \longmapsto S .
$$
Thus each Real-facing distance or tolerance read factors through the displayed metric source, total-bounded source, finite epsilon-net witness, stream window, dyadic tolerance row, and regular-rational readback before any sealed Real output is public.
\end{theorem}
\begin{proof}
Start from the obligation surface of \autoref{thm:finite-total-bounded-net-namecert-obligations}. It places $M,T,E$ before any coverage consumer can read a total-bounded net.

Use the finite epsilon-net route of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}. The row $E$ supplies center admission and coverage-distance data, while $W$ restricts the inspection to displayed finite source schedules.

The tolerance rows are then forced by $D$ and $R$. Dyadic comparisons are read before regular-rational coverage values, and the Real seal $S$ receives only that already displayed readback.

Therefore a compact-metric or uniform-continuity consumer reaches $\RealUp$ through the finite route shown above. The carrier has no coordinate from which it could export open-cover compactness, countable choice, quotient equality, or ambient completeness.
\end{proof}

\begin{theorem}[Finite total bounded net sibling dependency]
\label{thm:finite-total-bounded-net-sibling-dependency}
The $\FiniteTotalBoundedNetUp$ packet is consumed only as a sibling route tying $\MetricUp$, $\TotallyBoundedUp$, $\FiniteEpsilonNetUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows together. Dropping any one of $M,T,E,W,D,R,S$ leaves the read before the finite total-bounded net boundary and cannot be retyped as a $\CompactMetricUp$ coverage witness.
\end{theorem}
\begin{proof}
Read the carrier rows in \autoref{def:finite-total-bounded-net-carrier}. The packet has separate coordinates for the metric source, total-bounded source, finite epsilon-net witness, stream windows, dyadic tolerance, regular-rational readback, and Real seal.

Apply \autoref{thm:finite-total-bounded-net-namecert-obligations}. Each coordinate is a local NameCert obligation, so none can be inferred from a different sibling row after the packet has been accepted.

Use \autoref{thm:finite-total-bounded-net-real-handoff}. Compact-metric and Real-facing consumers read the route through $M,T,E,W,D,R,S$ in order. If any one of these rows is absent, the consumer has not reached the finite total-bounded net handoff and receives no compact-metric coverage witness.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteTotalBoundedNetUp$ consumer exposes metric, total-bounded, finite epsilon-net, stream-window, dyadic tolerance, regular-rational readback, Real seal, transport, replay, provenance, and local NameCert rows before reading a compact-metric or uniform-continuity coverage route.}

\independenceWitness{totallybounded, compactmetric, finite-epsilon-net, real: $\FiniteTotalBoundedNetUp$ is not bijective to any listed sibling because it binds total-bounded requests to finite epsilon-net data, finite source windows, dyadic tolerances, regular rational readbacks, Real seals, transport, replay, provenance, and local naming as one packet.}

\closureat{FiniteTotalBoundedNetUp}{seedStr}

\begin{closurestatus}{\FiniteTotalBoundedNetUp}
  \constructivestory{A $\FiniteTotalBoundedNetUp$ packet is generated from Metric, TotallyBounded, FiniteEpsilonNet, StreamName, DyadicRatCore, RegSeqRat, and Real rows, plus componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-total-bounded-net-carrier}, \autoref{thm:finite-total-bounded-net-namecert-obligations}, \autoref{thm:finite-total-bounded-net-real-handoff}, and \autoref{thm:finite-total-bounded-net-sibling-dependency} seed the finite total-bounded net packet over displayed metric, total-bounded, finite epsilon-net, stream-window, dyadic tolerance, regular-rational readback, and Real seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, countable choice, quotient metric equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite epsilon-net exposure, tolerance readback, Real seal nonescape, sibling dependency, transport, replay, provenance, and local naming for BEDC.Derived.FiniteTotalBoundedNetUp.}
\end{closurestatus}
