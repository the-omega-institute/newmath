\chapter{A Concrete Naming Certificate for $\LocatedRealSquareRootUp$}
\label{ch:concrete-instances-located-real-square-root-namecert}
\origin{human}

Following Bishop and Bridges on located reals and constructive square roots, the $\LocatedRealSquareRootUp$ packet records the finite bracket-readback service for a displayed located nonnegative real before any total host square-root map is available. It sits after the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the positive-input square-root service of \autoref{ch:concrete-instances-positive-real-square-root-namecert}. The packet names a finite located bracket around the square-root readback; it does not assert quotient streams, selected limits, host real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Located real square-root packet]
\label{def:located-real-square-root-packet}
A $\LocatedRealSquareRootUp$ carrier is a finite $\BHist$ packet
$$
L=(X,B,D,W,R,S,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the located nonnegative $\RealUp$ source row whose sign information is read only through displayed lower and upper rational brackets.
\item $B$ is the finite dyadic bracket row, recording lower and upper dyadic endpoints whose squares bracket the source read at the requested tolerance.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for endpoint arithmetic, squared-error comparisons, and bracket refinement.
\item $W$ is the $\StreamNameUp$ finite-window row exposing exactly the source and approximation observations inspected by the bracket.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from $D,W,B$.
\item $S$ is the $\PositiveRealSquareRootUp$ service row used when the bracket displays a positive guard for the local square-root read.
\item $E$ is the terminal $\RealUp$ seal reached only after $B,D,W,R,S$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for source, bracket, tolerance, window, readback, service, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route from located source read to bracketed square-root seal.
\item $P$ is the $\Pkg$ provenance over $\LocatedRealSquareRootUp$, $\PositiveRealSquareRootUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\LocatedRealSquareRootUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $X,B,D,W,R,S,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected completed limits, host real equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located square-root bracket narrowing]
\label{thm:located-real-square-root-bracket}
Every accepted $\LocatedRealSquareRootUp$ carrier $L=(X,B,D,W,R,S,E,H,C,P,N)$ narrows a square-root request through the finite dyadic bracket row $B$ and the $\DyadicRatCoreUp$ tolerance ledger $D$ before any $\RegSeqRatUp$ or $\RealUp$ consumer can read the result. The narrowing route refines only displayed brackets around the located source row $X$ and exports no quotient stream, selected limit, host real equality, countable-choice schedule, trichotomy, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-real-square-root-packet}. The bracket row $B$ and dyadic ledger $D$ are the only rows carrying endpoints, squared-error comparisons, and tolerance refinement.

Read those rows through \autoref{ch:concrete-instances-dyadicratcore-namecert} and the finite observation row through \autoref{ch:concrete-instances-streamname-namecert}. The source row $X$ is therefore inspected only by displayed windows and dyadic comparisons.

The positive branch, when available, is consumed through \autoref{ch:concrete-instances-positive-real-square-root-namecert}; otherwise the located bracket remains the named finite evidence. Since the carrier has no coordinate for quotient streams, selected limits, host equality, countable choice, trichotomy, or ambient completeness, the stated narrowing route is exhaustive.
\end{proof}

\begin{theorem}[Located square-root regular readback]
\label{thm:located-real-square-root-readback}
Every $\RegSeqRatUp$ readback exported by an accepted $\LocatedRealSquareRootUp$ packet factors through the displayed route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp
$$
inside $D,W,R$ after the bracket row $B$ has been fixed. The readback may cite the positive-input square-root service row $S$, but it remains a finite regular rational route and not a selected completed limit, quotient representative, host square-root function, countable-choice construction, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with bracket narrowing, \autoref{thm:located-real-square-root-bracket}. It fixes the bracket row $B$ and the dyadic tolerance ledger $D$ before readback is public.

Next read the finite windows through \autoref{ch:concrete-instances-streamname-namecert}. The $\RegSeqRatUp$ row of \autoref{ch:concrete-instances-regseqrat-namecert} is obtained from those displayed observations and the dyadic comparisons, so the readback cannot bypass $D,W,B$.

Finally inspect the positive square-root service row through \autoref{ch:concrete-instances-positive-real-square-root-namecert}. That row supplies only the bounded positive-input service already sealed by its own finite carrier. The structural rows $H,C,P,N$ preserve the same route and add no selected limit, quotient representative, host operation, choice schedule, or ambient completeness row.
\end{proof}

\begin{theorem}[Located square-root Real-seal nonescape]
\label{thm:located-real-square-root-real-seal}
Every $\RealUp$ seal read from an accepted $\LocatedRealSquareRootUp$ packet is terminal in the displayed carrier route $X,B,D,W,R,S,E,H,C,P,N$. A Real-facing consumer must first read the dyadic bracket, finite stream windows, regular rational readback, and positive square-root service rows through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-positive-real-square-root-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The seal exports no quotient streams, selected limits, host real equality, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Apply the regular readback theorem \autoref{thm:located-real-square-root-readback}. It forces the route through $B,D,W,R$ and the positive-input service row $S$ before the terminal row can be inspected.

Now read the terminal seal through \autoref{ch:concrete-instances-real-namecert}. The $\RealUp$ row $E$ seals only the displayed bracket-readback route; it is downstream of the dyadic ledger, stream windows, regular rational readback, and positive square-root service.

Finally replay $H,C,P,N$. Componentwise transport, continuation replay, provenance, and local naming keep the same finite packet visible to the consumer. Since no carrier row names quotient stream equality, a selected limit, host real equality, countable choice, or ambient completeness, the seal cannot escape the displayed route.
\end{proof}

\closureat{LocatedRealSquareRootUp}{seedStr}

\begin{closurestatus}{\LocatedRealSquareRootUp}
  \constructivestory{A $\LocatedRealSquareRootUp$ packet is generated from a located nonnegative RealUp source row, finite dyadic bracket data, DyadicRatCore tolerance accounting, StreamName finite windows, RegSeqRat readback, a PositiveRealSquareRoot service row, terminal Real sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-real-square-root-packet}, \autoref{thm:located-real-square-root-bracket}, \autoref{thm:located-real-square-root-readback}, and \autoref{thm:located-real-square-root-real-seal} seed the finite located bracket square-root route over displayed dyadic, stream, regular-readback, positive-service, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected completed limits, host real equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\LocatedRealSquareRootUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, bracket narrowing, dyadic tolerance accounting, finite-window coverage, regular readback, positive-service handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.LocatedRealSquareRootUp.}
\end{closurestatus}
