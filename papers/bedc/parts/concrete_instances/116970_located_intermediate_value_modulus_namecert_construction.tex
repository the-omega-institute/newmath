\chapter{A Concrete Naming Certificate for $\LocatedIntermediateValueModulusUp$}
\label{ch:concrete-instances-located-intermediate-value-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\LocatedIntermediateValueModulusUp$ packet records the finite real-window modulus used when a sign-change interval is inspected by bisection before a Real-facing consumer reads the terminal seal. It sits after the finite-window naming discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the located real intermediate-value route of \autoref{ch:concrete-instances-located-real-intermediate-value-namecert}, and the constructive IVT modulus route of \autoref{ch:concrete-instances-constructive-ivt-modulus-namecert}. The packet names the finite modulus surface consumed by L10 real-window arguments; it does not select a zero, decide trichotomy, import open-cover compactness, invoke choice, or assert ambient completeness of $\RealUp$.

\begin{definition}[LocatedIntermediateValueModulus carrier]\label{def:located-intermediate-value-modulus-carrier}
A $\LocatedIntermediateValueModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(I,S,D,B,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the bracket row, recording the located real interval endpoints, the retained subinterval, and the finite sign-change boundary consumed by the route.
\item $S$ is the sign-change row, recording the inspected endpoint signs and the midpoint sign reads used by the retained branch prefix.
\item $D$ is the dyadic refinement row, recording the $\DyadicRatCoreUp$ mesh, halving, radius comparison, and retained-width budget.
\item $B$ is the bisection-modulus row, recording the finite number of branch reads required before the requested located window is exposed.
\item $W$ is the $\StreamNameUp$ finite-window row for endpoint, midpoint, and retained-interval observations.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row attached to the same windows.
\item $E$ is the terminal $\RealUp$ seal row used only after the bracket, sign-change, dyadic refinement, bisection modulus, finite windows, and readback rows have been displayed.
\item $H$ stores componentwise $\hsame$ transport for $I,S,D,B,W,R,E$ and the structural rows.
\item $C$ stores displayed $\Cont$ replay for bracket inspection, sign readback, dyadic refinement, modulus consumption, regular rational readback, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\LocatedIntermediateValueModulusUp$, $\LocatedRealIntermediateValueUp$, $\ConstructiveIVTModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\LocatedIntermediateValueModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for a selected zero, real trichotomy, open-cover compactness, countable choice, quotient real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[LocatedIntermediateValueModulus NameCert obligations]\label{thm:located-intermediate-value-modulus-namecert-obligations}
For every accepted carrier $M=(I,S,D,B,W,R,E,H,C,P,N)$ of \autoref{def:located-intermediate-value-modulus-carrier}, the local $\NameCert_{\LocatedIntermediateValueModulusUp}$ surface consists exactly of bracket admission, sign-change exposure, dyadic refinement, bisection-modulus consumption, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier rows in order. The bracket and sign-change rows $I,S$ provide the only source of opposite-sign information, while the dyadic row $D$ and bisection-modulus row $B$ record the finite refinement budget.

The observation and readback rows $W,R$ expose only the windows requested by that finite budget. The terminal seal $E$ is therefore downstream of bracket inspection, sign readback, dyadic refinement, and regular rational readback.

The structural rows $H,C,P,N$ transport, replay, source, and name the same packet. Since no further coordinate appears in $M$, the NameCert surface exports exactly the displayed rows and excludes selected zeros, trichotomy, choice, open-cover compactness, quotient equality, and ambient completeness.
\end{proof}

\begin{theorem}[LocatedIntermediateValueModulus source stability]\label{thm:located-intermediate-value-modulus-source-stability}
Let $M=(I,S,D,B,W,R,E,H,C,P,N)$ be an accepted $\LocatedIntermediateValueModulusUp$ carrier. Transporting $M$ along its displayed $\hsame$ row keeps the bracket row $I$, sign-change row $S$, dyadic refinement row $D$, bisection-modulus row $B$, finite windows $W$, regular readback $R$, and Real seal $E$ in the same packet. A consumer cannot replace the transported source by $\RealUp$ completeness, real trichotomy, a host root selector, or an off-packet intermediate-value theorem.
\end{theorem}
\begin{proof}
Read the componentwise transport row $H$. Its source and target components are indexed by the rows $I,S,D,B,W,R,E$ together with the structural rows, so transport preserves the same bracket, sign-change, refinement, window, readback, and seal inventory.

Replay through $C$ does not add a new source row. It only reopens the displayed bracket, sign, dyadic, modulus, window, readback, and seal routes already present in the packet.

The provenance row $P$ names the $\LocatedRealIntermediateValueUp$ and $\ConstructiveIVTModulusUp$ routes as sources, but it names them through this finite carrier. Thus no transported read can escape to a selected zero, trichotomy, open-cover compactness, choice, or ambient completeness of $\RealUp$.
\end{proof}

\begin{theorem}[LocatedIntermediateValueModulus ledger exactness]\label{thm:located-intermediate-value-modulus-ledger-exactness}
For an accepted $\LocatedIntermediateValueModulusUp$ carrier $M=(I,S,D,B,W,R,E,H,C,P,N)$, the bisection-modulus row $B$ records exactly the finite branch budget justified by the bracket row $I$, sign-change row $S$, dyadic refinement row $D$, finite observation row $W$, regular rational readback row $R$, and Real seal row $E$. The ledger does not add open-cover compactness, a selected zero, trichotomy, choice, quotient real equality, or a completed $\RealUp$ object.
\end{theorem}
\begin{proof}
The row $B$ is read only after $I,S,D$ have fixed the source bracket, sign boundary, and dyadic refinement budget. It therefore records the modulus for that displayed sign-change route rather than an ambient existence theorem.

The rows $W,R,E$ give the finite observation, regular rational readback, and terminal Real seal attached to the same branch budget. The replay row $C$ may present these reads to a consumer, but it cannot enlarge $B$ into a completed choice sequence or a selected root.

Package provenance $P$ and local naming $N$ keep the ledger attached to $\LocatedIntermediateValueModulusUp$. Since the carrier has no row for open-cover compactness, trichotomy, choice, quotient equality, or complete $\RealUp$, the ledger is exact for the displayed finite route.
\end{proof}

\closureat{LocatedIntermediateValueModulusUp}{seedStr}

\begin{closurestatus}{\LocatedIntermediateValueModulusUp}
  \constructivestory{A $\LocatedIntermediateValueModulusUp$ packet is generated from bracket, sign-change, dyadic refinement, bisection-modulus, finite StreamName windows, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-intermediate-value-modulus-carrier}, \autoref{thm:located-intermediate-value-modulus-namecert-obligations}, \autoref{thm:located-intermediate-value-modulus-source-stability}, and \autoref{thm:located-intermediate-value-modulus-ledger-exactness} bind the finite L10 real-window modulus surface over $\LocatedRealIntermediateValueUp$, $\ConstructiveIVTModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close open-cover compactness, selected zeros, real trichotomy, countable choice, quotient real equality, bridge checking, or complete $\RealUp$.}
  \upgradepath{Obligation closure asks for checked carrier admission, sign-change stability, dyadic refinement exactness, finite-window readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.LocatedIntermediateValueModulusUp.}
\end{closurestatus}
