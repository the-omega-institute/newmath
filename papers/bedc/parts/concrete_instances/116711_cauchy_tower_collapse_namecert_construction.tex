\chapter{A Concrete Naming Certificate for $\CauchyTowerCollapseUp$}
\label{ch:concrete-instances-cauchy-tower-collapse-namecert}
\origin{human}

Following Bishop and Bridges on constructive completion, the $\CauchyTowerCollapseUp$ packet records the finite flattening route by which iterated regular-Cauchy completion data is read as one Real-facing certificate surface. It sits over the dyadic rational carrier of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window layer of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the route comparison packet of \autoref{ch:concrete-instances-cauchy-completion-associativity-namecert}, and the common-tail uniqueness boundary of \autoref{ch:concrete-instances-regular-cauchy-completion-uniqueness-namecert}. The chapter names the finite collapse window for towers of regular-Cauchy completion rows; it does not assert quotient completion equality, selected host limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy tower collapse carrier]
\label{def:cauchy-tower-collapse-carrier}
A $\CauchyTowerCollapseUp$ carrier is a finite $\BHist$ packet
$$
T=(B,S,R,E,A,U,W,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the base $\DyadicRatCoreUp$ tolerance ledger, including half-radius and fused-radius cells used by every tower level.
\item $S$ is the $\StreamNameUp$ finite-window schedule that chooses which observations of each tower level are inspected.
\item $R$ is the $\RegSeqRatUp$ readback row extracted from the same displayed windows.
\item $E$ is the terminal $\RealUp$ seal reached only after the dyadic, stream, and regular-readback rows have been displayed.
\item $A$ is the $\CauchyCompletionAssociativityUp$ route-comparison row that keeps nested completion flattening orders on the same finite ledger.
\item $U$ is the $\RegularCauchyCompletionUniquenessUp$ common-tail row comparing the flattened route with the single displayed completion boundary.
\item $W$ is the finite tower-window projection row, recording the observed depth, precision request, and component read at each inspected level.
\item $F$ is the dyadic tolerance-fusion row, combining the displayed level tolerances into one requested $\DyadicRatCoreUp$ bound before the Real seal is consumed.
\item $H$ records componentwise $\hsame$ transport for the dyadic, stream, readback, Real-seal, associativity, uniqueness, projection, fusion, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an iterated completion request through $W$, $F$, $A$, $U$, and then through $B,S,R,E$.
\item $P$ is the $\Pkg$ provenance over $\CauchyTowerCollapseUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyCompletionAssociativityUp$, $\RegularCauchyCompletionUniquenessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyTowerCollapseUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,S,R,E,A,U,W,F,H,C,P,N$. No coordinate stores quotient completion equality, a selected host limit, countable-choice schedules, or ambient real completeness.
\end{definition}

\begin{theorem}[Cauchy tower collapse NameCert obligations]
\label{thm:cauchy-tower-collapse-namecert-obligations}
For every accepted $\CauchyTowerCollapseUp$ carrier $T=(B,S,R,E,A,U,W,F,H,C,P,N)$, the local $\NameCert_{\CauchyTowerCollapseUp}$ surface consists exactly of carrier admission for the dyadic tolerance ledger, finite stream windows, regular rational readback, Real seal, associativity row, uniqueness row, finite tower-window projection, dyadic tolerance fusion, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming rows. No NameCert obligation is supplied by quotient completion equality, selected host limits, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-tower-collapse-carrier}. The rows $B,S,R,E,A,U,W,F,H,C,P,N$ are exactly the dyadic tolerance, stream-window, regular-readback, Real-seal, associativity, uniqueness, projection, fusion, transport, replay, provenance, and local naming coordinates.

The tower-facing part is $W,A,U$. The projection row $W$ exposes only the inspected finite depth and precision requests, the associativity row $A$ compares the displayed flattening orders, and the uniqueness row $U$ identifies the common-tail boundary available after flattening.

The Real-facing part is $B,S,R,E,F$. Tolerance requests are fused by $F$ into the dyadic ledger $B$, the finite observations are read through $S$, the rational schedules are read through $R$, and the terminal seal $E$ is consumed only after those rows are present.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for quotient completion equality, selected host limits, countable choice, or ambient completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Cauchy tower finite-window flattening]
\label{thm:cauchy-tower-collapse-finite-window-flattening}
Let $T=(B,S,R,E,A,U,W,F,H,C,P,N)$ be an accepted $\CauchyTowerCollapseUp$ carrier. Every iterated regular-Cauchy completion read factors through the finite route
$$
W \longmapsto F \longmapsto B,S,R \longmapsto A,U \longmapsto E \longmapsto H,C,P,N .
$$
Thus an iterated completion tower is flattened only by finite-window projection and dyadic tolerance fusion before reaching the $\RealUp$ seal. The route exports no quotient completion equality, selected host limit, countable-choice schedule, or ambient Real completeness principle.
\end{theorem}
\begin{proof}
Begin with the finite tower-window projection $W$. It chooses the displayed tower depth, the inspected components, and the requested precision cells without selecting a completed host object.

Fuse tolerances next. The row $F$ combines the displayed level tolerances into one $\DyadicRatCoreUp$ request in $B$, after which the same observation windows $S$ and regular rational readbacks $R$ serve the flattened route.

Read the completion comparison rows. The associativity row $A$ keeps the left- and right-nested collapse orders on one finite ledger, while $U$ supplies the regular-Cauchy common-tail boundary that permits the single Real-facing seal to be read.

The terminal seal $E$ and structural rows $H,C,P,N$ preserve exactly that route. Because the packet contains no quotient equality, selected limit, countable-choice, or ambient-completeness coordinate, no such principle can be exported by the flattening consumer.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyTowerCollapseUp$ consumer that reaches the $\RealUp$ seal must first read the finite tower-window projection, dyadic tolerance-fusion row, $\DyadicRatCoreUp$ ledger, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\CauchyCompletionAssociativityUp$ route row, and $\RegularCauchyCompletionUniquenessUp$ common-tail row.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, cauchy-completion-associativity, regular-cauchy-completion-uniqueness: no listed sibling supplies the whole packet $T=(B,S,R,E,A,U,W,F,H,C,P,N)$, because each sibling contributes only a tolerance ledger, finite window, regular readback, Real seal, route comparison, or common-tail boundary while this chapter binds the finite tower projection and dyadic fusion route.}

\closureat{CauchyTowerCollapseUp}{seedStr}

\begin{closurestatus}{\CauchyTowerCollapseUp}
  \constructivestory{A $\CauchyTowerCollapseUp$ packet is generated from a DyadicRatCore tolerance ledger, StreamName windows, RegSeqRat readbacks, a RealUp seal, CauchyCompletionAssociativity and RegularCauchyCompletionUniqueness rows, finite tower-window projection, dyadic tolerance fusion, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-tower-collapse-carrier}, \autoref{thm:cauchy-tower-collapse-namecert-obligations}, and \autoref{thm:cauchy-tower-collapse-finite-window-flattening} seed the finite collapse surface for iterated regular-Cauchy completion windows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected host limits, countable choice, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, finite tower-window projection, dyadic tolerance fusion, completion-route comparison, regular-Cauchy common-tail exposure, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyTowerCollapseUp.}
\end{closurestatus}
