\chapter{A Concrete Naming Certificate for $\LinearlyOrderedRealUp$}
\label{ch:concrete-instances-linearly-ordered-real-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\LinearlyOrderedRealUp$ packet records the finite order-facing route by which real-name consumers may read located comparison data without importing a host real line, quotient equality, trichotomy, selected representatives, countable choice, or ambient $\RealUp$ completeness. It sits after the located line interface of \autoref{ch:concrete-instances-located-real-line-namecert}, the located real-name surface of \autoref{ch:concrete-instances-located-real-namecert}, the order rows of \autoref{ch:concrete-instances-order-namecert}, the finite comparison discipline of $\TotalOrderUp$, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names a constructive order boundary for L10 Real and Completion routes, not a classical ordered field of reals.

\begin{definition}[LinearlyOrderedReal carrier]
\label{def:linearly-ordered-real-carrier}
A $\LinearlyOrderedRealUp$ carrier is a finite $\BHist$ packet
$$
\Lambda=(R,L,A,O,T,D,S,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the terminal $\RealUp$ seal row, visible only after the located and order comparison rows have been displayed.
\item $L$ is the $\LocatedRealLineUp$ row presenting the shared located line interface for order-facing real consumers.
\item $A$ is the $\LocatedRealUp$ row presenting the point-facing located real-name surface and its finite apartness-ready windows.
\item $O$ is the $\OrderUp$ comparison row, carrying the displayed preorder or order request read from $L,A,D,S,Q$.
\item $T$ is the $\TotalOrderUp$ comparison row, recording only the finite comparison branch exposed by the located line and order rows; it is not a trichotomy oracle outside the displayed request.
\item $D$ is the $\DyadicRatCoreUp$ endpoint, radius, and tolerance ledger used to compare the located windows named by $L,A,O,T$.
\item $S$ is the $\StreamNameUp$ finite-window row opened by each order-facing comparison request.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the same stream windows and dyadic tolerances.
\item $H$ records componentwise $\hsame$ transport for the real seal, located line, located real, order rows, dyadic ledger, stream windows, regular readback, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an order-facing request through $L,A,O,T,D,S,Q$ and then to the $\RealUp$ seal $R$.
\item $P$ is the $\Pkg$ provenance over $\LinearlyOrderedRealUp$, $\RealUp$, $\LocatedRealLineUp$, $\LocatedRealUp$, $\OrderUp$, $\TotalOrderUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LinearlyOrderedRealUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient real equality, a chosen representative of a real name, unrestricted trichotomy, host real-line completeness, selected limits, countable choice, or a public bridge.
\end{definition}

\begin{theorem}[LinearlyOrderedReal NameCert obligations]
\label{thm:linearly-ordered-real-namecert-obligations}
For every accepted $\LinearlyOrderedRealUp$ carrier $\Lambda=(R,L,A,O,T,D,S,Q,H,C,P,N)$, the local $\NameCert_{\LinearlyOrderedRealUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed $\RealUp$, $\LocatedRealLineUp$, $\LocatedRealUp$, $\OrderUp$, $\TotalOrderUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, transport, replay, provenance, and local naming rows;
\item displayed comparison stability, requiring every comparison read to transport componentwise through $H$ and to replay through the same continuation route $C$;
\item located-order handoff, requiring every order-facing consumer to pass through $L,A,D,S,Q,O,T$ before the $\RealUp$ seal $R$ is exposed;
\item ledger nonescape, excluding quotient real equality, selected representatives, unrestricted trichotomy, selected limits, countable choice, bridge data, and ambient $\RealUp$ completeness as local data;
\item Real-seal routing, requiring the terminal seal $R$ to preserve the same located line, located real, dyadic tolerance, stream-window, regular readback, and order rows already displayed by the carrier.
\end{enumerate}
No $\LinearlyOrderedRealUp$ obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:linearly-ordered-real-carrier}. The analytic rows are exactly the located line row $L$, located real row $A$, order rows $O,T$, dyadic ledger $D$, stream windows $S$, regular readback $Q$, and terminal real seal $R$.

Displayed comparison stability is componentwise. The transport row $H$ relates the displayed rows one by one, while the replay row $C$ preserves the route from located line and located real data through the dyadic, stream, regular, and order rows.

The located-order handoff follows because $R$ is terminal. An order-facing consumer reaches it only after the located line and located real rows have supplied the finite windows, the dyadic ledger has supplied the tolerance cells, and the order rows have recorded the displayed comparison.

Finally, $P$ and $N$ source and name the same finite packet. Since the excluded principles are not coordinates of the carrier or clauses of the obligation surface, the ledger cannot export them.
\end{proof}

\begin{theorem}[LinearlyOrderedReal located-order handoff]
\label{thm:linearly-ordered-real-located-order-handoff}
Every order-facing $\RealUp$ consumer of an accepted $\LinearlyOrderedRealUp$ carrier factors through the finite route
$$
\LocatedRealLineUp,\ \LocatedRealUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \OrderUp,\ \TotalOrderUp
\longmapsto
\RealUp .
$$
Equivalently, the consumer must read the located line row $L$, located real row $A$, dyadic tolerance row $D$, finite window row $S$, regular readback row $Q$, order row $O$, and displayed total-order comparison row $T$ before it may use the Real seal $R$.
\end{theorem}
\begin{proof}
Start with an accepted carrier $\Lambda=(R,L,A,O,T,D,S,Q,H,C,P,N)$. By \autoref{thm:linearly-ordered-real-namecert-obligations}, the only order-facing comparison rows are $O$ and $T$, and both are downstream of the located line, located real, dyadic, stream, and regular readback rows.

The sibling located-line interface of \autoref{ch:concrete-instances-located-real-line-namecert} supplies the line-facing entry point $L$, while \autoref{ch:concrete-instances-located-real-namecert} supplies the point-facing located row $A$. The dyadic, stream, and regular readback rows are exactly the finite comparison data named by \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-regseqrat-namecert}.

After these rows are displayed, the order request may read $O$ and the displayed comparison branch $T$. The terminal seal $R$ is then the same $\RealUp$ route cited by \autoref{ch:concrete-instances-real-namecert}, repacked through $H,C,P,N$.

Thus every order-facing Real consumer passes through the located line, dyadic tolerance, finite window, regular readback, and order rows before the Real seal. The handoff exports no quotient equality, selected representative, unrestricted trichotomy, selected limit, countable choice, bridge component, or ambient completeness principle.
\end{proof}

\closureat{LinearlyOrderedRealUp}{seedStr}

\begin{closurestatus}{\LinearlyOrderedRealUp}
  \constructivestory{A $\LinearlyOrderedRealUp$ packet is generated from a RealUp seal, LocatedRealLineUp row, LocatedRealUp row, OrderUp and TotalOrderUp comparison rows, DyadicRatCore tolerance ledger, StreamName windows, RegSeqRat readback, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:linearly-ordered-real-carrier}, \autoref{thm:linearly-ordered-real-namecert-obligations}, and \autoref{thm:linearly-ordered-real-located-order-handoff} seed the finite located-order route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected representatives, unrestricted trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, displayed comparison stability, located-order handoff, ledger nonescape, Real-seal routing, transport stability, replay stability, provenance exhaustion, and local naming for BEDC.Derived.LinearlyOrderedRealUp.}
\end{closurestatus}
