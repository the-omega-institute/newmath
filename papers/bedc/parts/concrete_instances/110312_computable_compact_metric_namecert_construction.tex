\chapter{A Concrete Naming Certificate for $\ComputableCompactMetricUp$}
\label{ch:concrete-instances-computable-compact-metric-namecert}
\origin{human}

Following Weihrauch-style computable analysis and the Bishop--Bridges compact metric arguments, the $\ComputableCompactMetricUp$ packet records the effective compact-metric surface as finite BEDC NameCert data. It sits after the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the effective Polish source of \autoref{ch:concrete-instances-effectivepolishspace-namecert}, the total-bounded finite-net route of \autoref{ch:concrete-instances-totallybounded-namecert}, the complete metric source of \autoref{ch:concrete-instances-completemetric-namecert}, and the L10 readback route through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names the finite-cover realizer surface consumed by effective compact analysis; it does not assert open-cover compactness, selected subcovers, quotient completion equality, or an ambient real-completeness principle.

\begin{definition}[Computable compact metric carrier]
\label{def:computable-compact-metric-carrier}
A $\ComputableCompactMetricUp$ carrier is a finite $\BHist$ packet
$$
K_{\mathrm{ccm}}=(K,E,T,C,S,R,L,H,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row carrying the compact metric packet whose finite-net behavior is being made effective.
\item $E$ is the $\EffectivePolishSpaceUp$ source row exposing the effective dense-name and semidecidable basic-ball interface.
\item $T$ is the $\TotallyBoundedUp$ row naming the finite-net source for each displayed dyadic tolerance request.
\item $C$ is the $\CompleteMetricUp$ row recording the convergence service available to Cauchy and compact consumers.
\item $S$ is the $\StreamNameUp$ finite-window row for inspected dense names, finite net centers, tolerance requests, and cover indices.
\item $R$ is the $\RegSeqRatUp$ handoff row for the regular rational readback of distances and tolerance comparisons.
\item $L$ is the terminal $\RealUp$ seal row reached only after the finite windows and regular rational comparisons have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact metric, effective Polish, total-bounded, complete-metric, stream-window, regular-readback, real-seal, finite-cover realizer, provenance, and local naming rows.
\item $Q$ records displayed $\Cont$ replay from an effective compactness request through $K,E,T,C,S,R,L,H$.
\item $P$ is the $\Pkg$ provenance over $\ComputableCompactMetricUp$, $\CompactMetricUp$, $\EffectivePolishSpaceUp$, $\TotallyBoundedUp$, $\CompleteMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ComputableCompactMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,E,T,C,S,R,L,H,Q,P,N$. It has no coordinate for an open-cover subcover, a choice-selected finite set, quotient completion equality, excluded-middle compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Computable compact metric NameCert obligations]
\label{thm:computable-compact-metric-namecert-obligations}
For every accepted $\ComputableCompactMetricUp$ carrier
$$
K_{\mathrm{ccm}}=(K,E,T,C,S,R,L,H,Q,P,N),
$$
the local $\NameCert_{\ComputableCompactMetricUp}$ surface consists exactly of the displayed rows in \autoref{def:computable-compact-metric-carrier}. Carrier admission must expose the compact metric source, effective Polish source, total-bounded finite-net source, complete-metric convergence service, finite stream windows, regular rational readback, terminal real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local naming row. Classifier transport is componentwise through those same rows, and ledger exhaustion says that every finite-cover or compact-analysis consumer can inspect only $K,E,T,C,S,R,L,H,Q,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:computable-compact-metric-carrier}. The rows $K,E,T,C$ are the only structural compactness sources, so carrier admission exposes compact metric data, effective Polish naming data, total-bounded finite nets, and complete-metric convergence before any realizer can be read.

The readback rows $S,R,L$ then bind numerical access to finite windows, regular rational comparisons, and the terminal real seal. This is the same L10 readback discipline used by the StreamName, RegSeqRat, and Real siblings named in the chapter introduction.

Transport and replay are visible rows rather than hidden meta-operations. The row $H$ records componentwise $\hsame$ transport, $Q$ records displayed $\Cont$ replay, and $P,N$ source and name the same finite packet.

For ledger exhaustion, read any compact-analysis consumer from the accepted packet. The carrier exposes only $K,E,T,C,S,R,L,H,Q,P,N$, so no open-cover subcover, selected finite set, quotient completion equality, excluded-middle compactness, or ambient real-completeness principle can be obtained from this NameCert surface.
\end{proof}

\begin{theorem}[Computable compact metric finite-cover realizer]
\label{thm:computable-compact-metric-finite-cover-realizer}
Every effective finite-cover read exported by an accepted $\ComputableCompactMetricUp$ packet factors through the route
$$
\CompactMetricUp,\ \EffectivePolishSpaceUp,\ \TotallyBoundedUp,\ \CompleteMetricUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert_{\ComputableCompactMetricUp}.
$$
Thus a consumer receives a compact metric source, effective dense-name interface, total-bounded finite-net source, complete-metric convergence service, finite stream windows, regular rational tolerance comparisons, terminal real seal, componentwise transport, replay, provenance, and local naming rows before it uses the finite-cover realizer. The packet exports no selected subcover, open-cover compactness theorem, quotient completion representative, countable-choice selector, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Start with an accepted carrier $K_{\mathrm{ccm}}=(K,E,T,C,S,R,L,H,Q,P,N)$. The compact metric row $K$ and effective Polish row $E$ name the metric and effective dense-name surfaces, while $T$ supplies the finite-net source used for the requested tolerance.

The convergence row $C$ keeps the compact route compatible with complete-metric consumers. A finite-cover realizer cannot leave these displayed sources, because the later rows are read only after the compact, effective, total-bounded, and complete rows are present.

The numerical readback is finite. The stream row $S$ exposes inspected names, center windows, cover indices, and tolerance requests; the regular rational row $R$ compares the displayed distances; and the terminal seal $L$ is reached only after those windows and comparisons have been read.

Finally $H,Q,P,N$ preserve the route under transport, replay, provenance, and local naming. Since selected subcovers, open-cover compactness theorems, quotient representatives, choice selectors, and ambient completeness witnesses are not carrier coordinates, the exported realizer is exactly the displayed finite-cover surface.
\end{proof}

\closureat{ComputableCompactMetricUp}{seedStr}

\begin{closurestatus}{\ComputableCompactMetricUp}
  \constructivestory{A $\ComputableCompactMetricUp$ packet is generated from CompactMetric, EffectivePolishSpace, TotallyBounded, CompleteMetric, StreamName, RegSeqRat, RealUp, componentwise hsame, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:computable-compact-metric-carrier}, \autoref{thm:computable-compact-metric-namecert-obligations}, and \autoref{thm:computable-compact-metric-finite-cover-realizer} seed the effective finite-cover realizer route for computable compact metric consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected subcovers, open-cover compactness, quotient completion equality, countable-choice selectors, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, effective dense-name exposure, total-bounded finite-net exposure, complete-metric convergence exposure, finite-window readback, regular rational comparison, real sealing, finite-cover realizer nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ComputableCompactMetricUp.}
\end{closurestatus}
